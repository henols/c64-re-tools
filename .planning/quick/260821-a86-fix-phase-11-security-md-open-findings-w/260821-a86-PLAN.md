---
phase: quick-260821-a86
plan: 01
type: execute
wave: 1
depends_on: []
worktree: false   # A deliverable IS `.planning/phases/11-*/11-SECURITY.md` content.
                  # Worktree mode strips `.planning/` files from executor commits, so
                  # this plan cannot deliver its closure step under isolation.
files_modified:
  - .claude/mcp/vice/r2000-tools.ts
  - .claude/mcp/vice/r2000-tools.test.ts
  - .claude/mcp/vice/r2000-acme-ident.ts
  - .claude/mcp/vice/r2000-enum-gen.ts
  - .claude/mcp/vice/r2000-symbols.ts
  - .claude/mcp/vice/r2000-symbol-roundtrip.test.ts
  - .claude/mcp/vice/r2000-memmap-render.ts
  - .claude/mcp/vice/r2000-memmap-render.test.ts
  - .claude/mcp/vice/package.json
  - .planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/11-SECURITY.md
autonomous: true
requirements: [SEC-11-01, SEC-11-02, SEC-11-03]

must_haves:
  truths:
    - "A `.regen2000proj` path whose intermediate component is an existing directory symlink pointing outside the workspace is refused by name, even when the final component does not exist yet"
    - "`r2000_save_project`'s create path still works: a not-yet-existing leaf inside a real in-workspace directory still resolves and is returned"
    - "A `name` that is not a legal ACME identifier is refused by `r2000_set_label_name` before any regenerator2000 child is spawned — refused, never sanitized or quoted"
    - "The same refusal fires for an `r2000_set_label_name` smuggled inside an `r2000_batch_execute` payload, refusing the whole batch with zero spawns"
    - "`importLabels()` refuses a `.lbl` carrying an illegal label name, naming the offending line number and its text, before any child is spawned"
    - "A comment whose evidence text contains `|` or an embedded newline renders as ONE well-formed Markdown table row — escaped, never rejected"
    - "The identifier policy lives in exactly one place; `r2000-enum-gen.ts` and the label-name routes call the same function, not two copies"
    - "`11-SECURITY.md` reads `threats_open: 0` / `status: verified` with all three previously-open register rows closed and an appended audit-trail row"
  artifacts:
    - path: ".claude/mcp/vice/r2000-acme-ident.ts"
      provides: "The one ACME identifier policy seam (assertLegalAcmeIdentifier + its regex, mnemonic set and length ceiling), importable without a module cycle"
      contains: "assertLegalAcmeIdentifier"
    - path: ".claude/mcp/vice/r2000-tools.ts"
      provides: "parent-realpath containment in resolveStorePath() + label-name reject gate on the outer and batch-inner r2000_set_label_name routes"
      contains: "R2000LabelNameError"
    - path: ".claude/mcp/vice/r2000-symbols.ts"
      provides: "pre-spawn label-name validation in importLabels(), naming the offending .lbl line"
    - path: ".claude/mcp/vice/r2000-memmap-render.ts"
      provides: "escapeMarkdownCell() applied to every store-derived text interpolation; RENDERER_VERSION bumped"
      contains: "escapeMarkdownCell"
    - path: ".planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/11-SECURITY.md"
      provides: "closed register rows, threats_open 0, status verified, ticked sign-off, appended audit row"
      contains: "threats_open: 0"
  key_links:
    - from: ".claude/mcp/vice/r2000-tools.ts"
      to: ".claude/mcp/vice/r2000-acme-ident.ts"
      via: "static import of assertLegalAcmeIdentifier (NOT from r2000-enum-gen.ts, which would form a cycle)"
      pattern: "r2000-acme-ident"
    - from: ".claude/mcp/vice/r2000-symbols.ts"
      to: ".claude/mcp/vice/r2000-acme-ident.ts"
      via: "static import, applied after parseViceLabelFile() and before buildImportLblArgs()"
      pattern: "r2000-acme-ident"
    - from: ".claude/mcp/vice/package.json"
      to: ".claude/mcp/vice/r2000-acme-ident.ts"
      via: "files[] entry — scripts/check-npm-packages.mjs's transitive-closure walk fails the build otherwise"
      pattern: "r2000-acme-ident.ts"
---

<objective>
Close the two open findings in Phase 11's security contract, exactly as its own
"Remediation Decision (2026-08-21)" table locks them:

1. **WR-01 / T-11-PATH-ESCAPE** — `resolveStorePath()` resolves the *parent*
   directory's realpath, joins the literal basename, then contain-checks.
2. **T-11-NAME-INJECT** — a **REJECT** (not sanitize, not quote) label-name
   identifier policy on both entry routes: `r2000_set_label_name` and
   `importLabels()`.
3. **WR-04** — `renderMemoryMap()` **escapes** `|` and embedded newlines in
   Markdown cells (a separate control: comment evidence legitimately contains
   both, so the render path must never reject).

Purpose: `11-SECURITY.md` is `status: draft` / `threats_open: 3` purely because
two `mitigate`-dispositioned threats have no implemented control. These three
fixes are the whole remaining distance to `threats_open: 0`.

Output: the three controls, one regression test per control that fails before
its fix, and `11-SECURITY.md` flipped to `verified`.

Explicitly OUT of scope (leave untouched): WR-02 (refusal shape), WR-03
(unbounded batch recursion), WR-05 (missing post-spawn `"error"` listener).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/11-SECURITY.md
@.claude/mcp/vice/r2000-tools.ts
@.claude/mcp/vice/r2000-symbols.ts
@.claude/mcp/vice/r2000-memmap-render.ts
@.claude/mcp/vice/r2000-enum-gen.ts
@.claude/mcp/vice/stock-symbols.ts

Facts already established by planning — do not re-derive:

- `resolveStorePath()` is `r2000-tools.ts:633-678`; the ENOENT catch sets
  `real = resolved` (the literal path), which is the whole bypass.
- `assertLegalAcmeIdentifier()` is `r2000-enum-gen.ts:140`, with
  `ACME_IDENT_RE` (`:103`), `ACME_RESERVED_MNEMONICS` (`:114`) and
  `MAX_ACME_IDENTIFIER_LENGTH` (`:101`). Its only current consumers are
  `createOrUpdateEnum` / `sanitizeVariantMap` in that same file, plus
  `r2000-enum-gen.test.ts` (7 cases at `:65-92`).
- **`r2000-enum-gen.ts` statically imports `runR2000Tool` from
  `r2000-tools.ts` (`:83`).** So `r2000-tools.ts` must NOT import from
  `r2000-enum-gen.ts` — hence the extraction in Task 2.
- `repoRoot()` (`repo-root.ts:118-124`) reads `env.CLAUDE_PROJECT_DIR` fresh on
  every call, with no caching — so a test can point the workspace root at a
  temp dir. `stock-symbols.test.ts:43-56`'s `withTempWorkspace()` helper is the
  established pattern for that, and `:194-215` is the established
  symlink-escape test shape (including the `t.skip` on `symlinkSync` failure).
- Every existing `.lbl` fixture and every existing `r2000_set_label_name` call
  in the test suite already uses a legal identifier (`main`, `entry`,
  `init_screen`, `entry_point`, `discovered_name`, `trap_should_not_persist`,
  `sym0`...). **No existing fixture or caller needs updating** for the reject
  policy. `regenerateAndReload()` (`r2000-symbols.ts:301`) has no production
  caller; an illegal name now rejects its promise instead of returning
  `isError` — acceptable, note it in its docstring.
- `parseViceLabelFile()`'s `VICE_LABEL_LINE_RE` (`stock-symbols.ts:81`) captures
  the name as `\S+`, so `.bad-name` and `.a|b` both parse into
  `table.byName`. It returns names only, no line numbers.
- `r2000-memmap-render.ts` contains two **literal NUL bytes** at line 269
  (deliberate digest separators). `grep`/`ugrep` therefore treats it as a binary
  file and silently matches nothing: **any grep against this file must pass
  `-a`**, or the gate passes vacuously.
- `regenerator2000 0.9.20` IS on PATH at `~/.cargo/bin/regenerator2000`, so the
  D-11-gated live tests really run here.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close WR-01 — parent-realpath containment in resolveStorePath()</name>
  <files>.claude/mcp/vice/r2000-tools.ts, .claude/mcp/vice/r2000-tools.test.ts</files>
  <behavior>
    New cases in `r2000-tools.test.ts`, in the existing
    "resolveStorePath (T-11-PATH-ESCAPE)" block (`:277-311`), each written and
    run BEFORE the fix:
    - RED 1 (the PoC from the audit): inside a `withTempWorkspace`-style temp
      root, plant a *directory* symlink `escape-link` -> a second temp dir
      outside the root, then `resolveStorePath("escape-link/pwned.regen2000proj")`
      throws `R2000StorePathError` whose message names the resolved outside
      target. Skip via `t.skip` if `symlinkSync` is unavailable, mirroring
      `stock-symbols.test.ts:194-215`.
    - RED 2: same shape one level deeper (`escape-link/sub/x.regen2000proj`)
      so the fix cannot pass by only inspecting the immediate parent.
    - GREEN-preserving 1: a not-yet-existing leaf in a real in-workspace
      directory still resolves and is returned (the `r2000_save_project`
      create path). The existing test at `:307` covers the shallow case; add
      one where an intermediate directory also does not exist yet
      (`newdir/deeper/fresh.regen2000proj`) and assert it is returned,
      contained under the root.
    - The four existing cases at `:280-308` must still pass verbatim,
      including `assert.ok(resolved.startsWith(HERE))` (verified safe: this
      checkout's path contains no symlink, so parent-realpath is a no-op there).
  </behavior>
  <action>
Rewrite `resolveStorePath()`'s symlink-resolution step (r2000-tools.ts:633-678),
keeping the type check, the `.regen2000proj` extension check, the literal
containment check and the `realRoot` computation exactly as they are.

Replace the single `realpathSync(resolved)` / `catch ENOENT -> real = resolved`
step with: (a) try `realpathSync(resolved)` — on success, contain-check that
result and return it, unchanged from today, so the existing-leaf and
symlinked-leaf cases keep their current behaviour; (b) on `ENOENT` only, walk up
from `dirname(resolved)` toward `resolved`'s root to find the DEEPEST ancestor
for which `realpathSync` succeeds, remembering the literal path segments
skipped on the way; contain-check that ancestor's realpath against `realRoot`
and refuse if it escapes, naming both the resolved input and the realpath'd
ancestor; then rebuild the candidate as `join(ancestorReal, ...remaining
literal segments)`, contain-check it, and return it; (c) any non-`ENOENT`
error still throws the existing "could not resolve" refusal.

Add a dangling-symlink guard inside (b): for each remaining literal segment,
`lstatSync` the accumulated path and, if it exists and `isSymbolicLink()`,
refuse naming that component — a symlink whose target does not exist yet
still redirects a later create outside the workspace, and `realpathSync` on
the full path reports plain `ENOENT` for it, so it would otherwise slip
through the parent-realpath check. Swallow `lstatSync`'s own ENOENT (a
genuinely absent component is the tolerated case).

Update the function's docstring and the section header comment above it
(`:607-613`) so the code's own claim now matches the code: state that
containment is enforced against the deepest EXISTING ancestor's realpath plus
literal remaining segments, and name WR-01 / T-11-PATH-ESCAPE as the finding
this closes. Do not widen or narrow any other claim in that header.

Import `lstatSync` and `dirname` alongside the existing `realpathSync` /
`resolve, sep` imports (`:68-69`). Do not touch `isContained()`,
`assertCuratedTool()`, `assertCuratedBatch()` or `runR2000Tool()` in this task.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice &amp;&amp; node --test r2000-tools.test.ts &amp;&amp; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>The two symlink-escape cases fail with the old code and pass with the new one, naming the outside target; both create-path cases (existing leaf absent, intermediate dir absent) return a contained path; all four pre-existing `resolveStorePath` cases still pass; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close T-11-NAME-INJECT — one identifier seam, REJECT policy on both label-name entry routes</name>
  <files>.claude/mcp/vice/r2000-acme-ident.ts, .claude/mcp/vice/r2000-enum-gen.ts, .claude/mcp/vice/r2000-tools.ts, .claude/mcp/vice/r2000-symbols.ts, .claude/mcp/vice/package.json, .claude/mcp/vice/r2000-tools.test.ts, .claude/mcp/vice/r2000-symbol-roundtrip.test.ts</files>
  <behavior>
    Written and run RED first, in this order:
    - `r2000-tools.test.ts`: `runR2000Tool("r2000_set_label_name", {project: &lt;valid path&gt;, address: 4096, name: "bad-name"})` is refused — the refusal names the offending name and the fact that it is not a legal ACME identifier. Cover `"bad-name"`, `"has space"`, `"1BAD"`, `"a|b"`, `"LDA"` (reserved mnemonic) and `"BAD\nNAME"`; assert `"init_screen"` is NOT refused by the gate. The refusal must be observable with NO regenerator2000 child spawned — assert it by calling `assertCuratedTool("r2000_set_label_name", args)` directly (the pre-spawn seam), exactly as the existing batch-refusal cases at `:150-260` do.
    - `r2000-tools.test.ts`: an `r2000_batch_execute` payload whose `calls[1]` is `{name:"r2000_set_label_name", arguments:{address:4096, name:"bad-name"}}` refuses the WHOLE batch, naming `calls[1]`.
    - `r2000-symbol-roundtrip.test.ts`: a `.lbl` written as three lines where line 2 is `al C:0810 .bad-name` makes `importLabels()` reject with a message naming line **2** and containing the line's own text — before any spawn (no binary needed, same shape as the two ceiling tests at `:275-310`).
    - `r2000-enum-gen.test.ts`'s seven existing `assertLegalAcmeIdentifier` cases (`:65-92`) must keep passing with their import path unchanged.
  </behavior>
  <action>
Step 1 — extract the policy into one seam. Create
`.claude/mcp/vice/r2000-acme-ident.ts` and MOVE (not copy)
`MAX_ACME_IDENTIFIER_LENGTH`, `ACME_IDENT_RE`, `ACME_RESERVED_MNEMONICS` and
`assertLegalAcmeIdentifier()` out of `r2000-enum-gen.ts` into it, verbatim —
including their full existing header comments (the ACME-0.97-measured mnemonic
list rationale and the length-ceiling rationale must survive the move
unedited). Give the new module the repo's standard header: why it exists (one
identifier policy, now consumed by three call sites), what it is the ONE
authoritative place for, and WHAT NOT TO DO (never add a second identifier
regex; never sanitize — this control refuses, per T-11-REGBITS-PROSE and
T-11-ENUM-NAME precedent). It must import nothing from this repo, so it can be
imported by both `r2000-tools.ts` and `r2000-enum-gen.ts` without a cycle.

In `r2000-enum-gen.ts`, import from the new module and RE-EXPORT
`assertLegalAcmeIdentifier` and `MAX_ACME_IDENTIFIER_LENGTH` so
`r2000-enum-gen.test.ts` and any other consumer keeps its current import path.
Leave `createOrUpdateEnum` / `sanitizeVariantMap` call sites untouched. Update
the file-header bullet at `:64-67` to say the function now lives in
`r2000-acme-ident.ts` and is re-exported here.

Add `"r2000-acme-ident.ts"` to `.claude/mcp/vice/package.json`'s `files[]`
(place it next to `"r2000-enum-gen.ts"`). Without it,
`scripts/check-npm-packages.mjs`'s transitive-closure walk fails the build —
that check is the verification for this step, not an afterthought.

Step 2 — route A, the tool surface. In `r2000-tools.ts`, add
`R2000LabelNameError extends Error` following the module's own
`R2000UncuratedToolError` shape: `.name` set to the class name, plus
`labelName: string` and optional `batchIndex?: number` public fields. Add a
module-private `assertLegalLabelArg(args, batchIndex?)` that, when `args` is a
plain object with a `name` property, requires it to be a string and passes it
to `assertLegalAcmeIdentifier()` from the new seam, re-throwing as
`R2000LabelNameError` with a message that names the offending value, the reason
from the underlying check, and states that the policy is REJECT (a malformed
name is a bug to surface, never silently sanitized — the store's printed name
must not diverge from the exported symbol). Call it from `assertCuratedTool()`
when `name === "r2000_set_label_name"`, and from `assertCuratedBatch()`'s
`forEach` for any inner call with that name, refusing the WHOLE batch with the
`calls[i]` index named — the same posture that block already takes for an
uncurated inner name.

Both call sites sit before `runR2000Tool()`'s `try` block, so a refusal rejects
the returned promise rather than resolving `{isError:true}`. That asymmetry is
WR-02, which this plan deliberately does NOT touch: it is out of scope and the
new refusal must behave exactly like the existing uncurated-name refusal.
Record that in a one-line comment so a later reader does not mistake it for an
oversight.

Also extend `r2000_set_label_name`'s `name` property `description` in
`R2000_TOOL_DEFINITIONS` (`:180-183`) to state the constraint an LLM caller
must satisfy (a legal ACME identifier: leading letter or underscore, then
letters/digits/underscores, not a 6502 mnemonic). Change the description string
ONLY — do not add a JSON-schema `pattern`/`format` key, which would alter the
argument shape this surface copied verbatim from the child's own `tools/list`.

Step 3 — route B, the import leg. In `r2000-symbols.ts`'s `importLabels()`,
after `parseViceLabelFile()` and BEFORE `buildImportLblArgs()` (so the refusal
is provably zero-spawn, exactly like the byte/line/symbol ceilings above it),
validate every name in `inputParsed.table.byName` with
`assertLegalAcmeIdentifier()` from the new seam. On the first illegal name,
throw `R2000SymbolsError` naming: the offending name, the 1-based line number,
that line's own text, and the underlying reason. Locate the line by splitting
the already-read `inputText` on `/\r?\n/` and finding the first line that
CONTAINS the offending name — a substring search, deliberately NOT a second
`al C:` regex, because this module's own header forbids adding a third parser
for that format (T-11-LBL-PARSER-DUP).

Update `R2000SymbolsError`'s docstring, which currently enumerates its reserved
failure modes, to add this one. Update `importLabels()`'s docstring and the
module header's "WHAT NOT TO DO" list with the new pre-spawn control, and note
in `regenerateAndReload()`'s docstring that an illegal `name` now rejects
(instead of surfacing as `setResult.isError`) because the check fires before
`runR2000Tool()`'s own `try`.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice &amp;&amp; node --test r2000-tools.test.ts r2000-symbol-roundtrip.test.ts r2000-enum-gen.test.ts &amp;&amp; npx tsc --noEmit -p tsconfig.json &amp;&amp; node ../../../scripts/check-npm-packages.mjs</automated>
  </verify>
  <done>All six illegal-name shapes are refused pre-spawn on the tool route, a batch smuggling one is refused whole with its index named, `importLabels()` refuses naming line 2 and its text, `init_screen` still passes, the seven pre-existing `assertLegalAcmeIdentifier` cases pass unchanged, typecheck clean, and `check-npm-packages.mjs` passes with the new module listed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close WR-04 by escaping, then flip 11-SECURITY.md to verified</name>
  <files>.claude/mcp/vice/r2000-memmap-render.ts, .claude/mcp/vice/r2000-memmap-render.test.ts, .planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/11-SECURITY.md</files>
  <behavior>
    RED first, in `r2000-memmap-render.test.ts`:
    - Unit half (no binary): `escapeMarkdownCell("a|b")` yields `a\|b`;
      `escapeMarkdownCell("one\ntwo")` and the `\r\n` and bare `\r` variants
      each yield a single line joined by `<br>`; a plain string is returned
      unchanged; an empty string stays empty.
    - Gated half (D-11, real regenerator2000): set a `line` comment whose
      evidence text contains BOTH a `|` and an embedded newline, via
      `formatConfidenceComment` + `r2000_set_comment`, then `renderMemoryMap()`
      and assert (a) that comment's table row is exactly ONE line in the
      output, (b) the row's pipe count is unchanged from a row with plain
      evidence — i.e. the table structure survives, and (c) the escaped
      evidence is present, so the escaping did not drop the content. Use a
      comment, never a label, for this case: after Task 2 a `|`-bearing label
      can no longer enter the store at all, which is the intended division of
      the two controls.
  </behavior>
  <action>
Step 1 — the control. In `r2000-memmap-render.ts`, add an exported
`escapeMarkdownCell(text: string): string` that replaces every `|` with `\|`
and collapses `\r\n`, `\n` and `\r` to `<br>`. Give it a docstring stating that
this control ESCAPES and never rejects, and why that differs from the label-name
policy: comment `evidence` legitimately contains `|` and newlines
(`r2000_set_comment`'s own schema documents multi-line support), so refusing
here would refuse valid data. Name WR-04 / T-11-NAME-INJECT's render leg.

Apply it to every store-derived text interpolation in the generated document —
`evidence` in the Range/Contents/Confidence/Evidence row, `sym.name` and
`confirmedBy` in the Routines row, and `u.evidence` in the Open questions
bullet (an embedded newline breaks a list item just as it breaks a table row).
Leave machine-generated values alone: `hex4()` output, `block.type`, the
confidence phrases from `CONFIDENCE_GRADES`, and every `provenance.*` field
(already schema-validated by `parseProvenanceHeader()`).

Bump `RENDERER_VERSION` from `"1"` to `"2"`, which is exactly what its own
docstring instructs on an output-shape change, and extend that docstring with a
one-line note that version 2 escapes Markdown cells. Do not change
`computeRenderDigest()`'s inputs or order.

Add the new control to the module header's "WHAT THIS IS THE ONE AUTHORITATIVE
PLACE FOR" list. Note for any verification you add: this file contains two
literal NUL bytes at line 269, so plain `grep` reports "binary file matches"
and a `grep -c` gate on it silently passes vacuously — use `grep -a` if you
grep it at all, and prefer the tests above.

Step 2 — close the security contract. Edit
`.planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/11-SECURITY.md`:

- Frontmatter: `threats_open: 3` -> `0`, `status: draft` -> `verified`.
- Register rows: flip all THREE currently-`open` rows to `closed` —
  `T-11-PATH-ESCAPE` (`:59`), `T-11-NAME-INJECT (11-08)` (`:77`) and
  `T-11-NAME-INJECT (11-11)` (`:91`, the duplicate that closes with its root
  cause). Two findings, three rows; `threats_open: 0` requires all three.
  Replace each row's Mitigation cell with what now actually exists: the
  deepest-existing-ancestor realpath containment plus the dangling-symlink
  component guard for the first; `assertLegalAcmeIdentifier()` from
  `r2000-acme-ident.ts` applied pre-spawn on `r2000_set_label_name` (outer and
  batch-inner) and in `importLabels()`, plus `escapeMarkdownCell()` on the
  render leg, for the other two. Cite the test files that pin each.
- Retitle "Open Findings (Detail)" to "Resolved Findings (closed 2026-08-21)"
  and append a short Resolution paragraph to each of the two findings naming
  the function changed, the control, and the regression tests. Leave the
  original analysis text intact — it is the evidence trail.
- Update the "Accepted Risks Log" trailing note, which currently asserts both
  findings "remain OPEN, not accepted", to record that both were fixed rather
  than accepted, and AR-11-01 unchanged.
- Update the "Remediation Decision (2026-08-21)" closing sentence, which
  currently says `threats_open` stays at 3 and status stays draft until the
  fixes land, to record that they landed (this quick task, `260821-a86`) and
  that `/gsd-secure-phase 11` may be re-run to confirm independently.
- Sign-Off: tick both unchecked boxes and rewrite their text to the new state
  (`threats_open: 0` confirmed; `status: verified`). Change "**Approval:**
  pending" to record approval via this remediation.
- Append one Security Audit Trail row: `| 2026-08-21 | 69 | 69 | 0 | quick
  260821-a86 remediation (WR-01 / T-11-NAME-INJECT / WR-04) |`.

Do NOT touch the three Unregistered Flags rows (WR-02, WR-03, WR-05) — they
remain WARNING/non-blocking by the locked decision.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice &amp;&amp; node --test r2000-memmap-render.test.ts &amp;&amp; npx tsc --noEmit -p tsconfig.json &amp;&amp; SEC=../../../.planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/11-SECURITY.md &amp;&amp; command grep -q 'threats_open: 0' "$SEC" &amp;&amp; command grep -q 'status: verified' "$SEC" &amp;&amp; ! command grep -q '\*\*open\*\*' "$SEC"</automated>
  </verify>
  <done>`escapeMarkdownCell` unit cases pass; the gated render case produces one well-formed row for pipe+newline evidence with content preserved; `RENDERER_VERSION` is `"2"`; 11-SECURITY.md reads `threats_open: 0` / `status: verified`, has zero `**open**` register cells, both sign-off boxes ticked, and one new audit-trail row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM-authored `tools/call` args → `runR2000Tool()` | `project` path and label `name` chosen by a model |
| a `.lbl` line from live emulator recon → the annotation store | externally-observed identifier becomes an ACME symbol |
| store text → generated `memory-map.md` | comment/label text becomes Markdown table structure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260821-01 | Information Disclosure | `resolveStorePath()` create path | mitigate | Contain-check the deepest EXISTING ancestor's realpath plus literal remainder; refuse any remaining component that exists as a symlink. Task 1's two planted-directory-symlink tests are the proof |
| T-260821-02 | Tampering | new `resolveStorePath()` logic over-refusing | mitigate | Task 1 keeps the four pre-existing cases verbatim and adds two create-path cases (absent leaf, absent intermediate dir), so a fix that breaks `r2000_save_project` fails the gate |
| T-260821-03 | Tampering | label name → generated ACME / Markdown | mitigate | `assertLegalAcmeIdentifier()` pre-spawn on `r2000_set_label_name` (outer + batch-inner) and in `importLabels()`; REJECT, never sanitize, so the store's printed name cannot diverge from the exported symbol |
| T-260821-04 | Tampering | a second, drifting copy of the identifier policy | mitigate | The policy MOVES to `r2000-acme-ident.ts` and is re-exported, never copied; `r2000-tools.ts` imports the seam directly rather than `r2000-enum-gen.ts` (which would cycle back into `r2000-tools.ts`) |
| T-260821-05 | Tampering | comment evidence → Markdown table structure | mitigate | `escapeMarkdownCell()` on every store-derived interpolation, escape-only by design; gated test asserts one well-formed row for pipe+newline evidence |
| T-260821-06 | Denial of Service | new module missing from the published tarball | mitigate | `files[]` entry plus `node scripts/check-npm-packages.mjs` as a required Task 2 verify step (the closure walk fails on ERR_MODULE_NOT_FOUND-shaped drift) |
| T-260821-07 | Repudiation | a vacuous grep gate on `r2000-memmap-render.ts` | mitigate | That file holds literal NUL bytes; any grep against it uses `grep -a`, and the real gates are tests, not greps |
| T-260821-SC | Tampering | npm/pip/cargo installs | accept | Not applicable — no dependency added or changed. No `npm install`; all commands run against the committed lockfile |
</threat_model>

<verification>
Run from the repo root after all three tasks:

1. `cd .claude/mcp/vice && npm run typecheck` — clean.
2. `cd .claude/mcp/vice && npm run test:automated` — the project's real local
   gate, with `regenerator2000` on PATH so the D-11-gated halves actually run.
   No pre-existing test may regress.
3. `node scripts/check-npm-packages.mjs` — both tarball file lists correct with
   `r2000-acme-ident.ts` present.
4. `command grep -c 'threats_open: 0' .planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/11-SECURITY.md`
   — 1.
5. `command grep -an 'assertLegalAcmeIdentifier' .claude/mcp/vice/r2000-tools.ts .claude/mcp/vice/r2000-symbols.ts`
   — at least one hit in each (the two entry routes are really wired).
6. `git status --porcelain` — only the ten files in `files_modified`.

Do not push, tag, or publish. Shipped code changes here, so the commit subject
must NOT carry `[skip release]` — but pushing to `main` auto-publishes a patch,
so leave the push to the developer.
</verification>

<success_criteria>
- A planted intermediate directory symlink pointing outside the workspace is
  refused by name, at any depth, while `r2000_save_project`'s create path
  (absent leaf, and absent intermediate directory) still resolves.
- An illegal label name is refused — not sanitized, not quoted — on both entry
  routes and inside a batch payload, always before a child is spawned; the
  `.lbl` route names the offending line number and its text.
- The identifier policy exists in exactly one file; `r2000-enum-gen.ts`
  re-exports it and its seven existing tests pass with unchanged imports.
- Comment evidence containing `|` and newlines renders as one well-formed row;
  `RENDERER_VERSION` is bumped to `"2"`.
- `npm run test:automated` and `npm run typecheck` pass in `.claude/mcp/vice`;
  `scripts/check-npm-packages.mjs` passes.
- `11-SECURITY.md` reads `threats_open: 0` / `status: verified`, has no `open`
  register row, both sign-off boxes ticked, and one appended audit-trail row.
- WR-02, WR-03 and WR-05 are untouched.
</success_criteria>

<output>
Create `.planning/quick/260821-a86-fix-phase-11-security-md-open-findings-w/260821-a86-SUMMARY.md` when done.

Suggested commit subject:
`fix(r2000): close phase-11 security findings — parent-realpath containment, label-name reject policy, Markdown cell escaping`

Re-run `/gsd-secure-phase 11` afterwards to have the auditor independently
confirm `threats_open: 0`.
</output>
</content>
</invoke>
