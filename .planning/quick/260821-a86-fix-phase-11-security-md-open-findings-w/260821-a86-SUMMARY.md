---
phase: quick-260821-a86
plan: 01
subsystem: anno
tags: [the external analyser, acme, security, markdown, symlink, mcp-tools]

requires:

  - phase: 11-annotation-store-enums-and-the-symbol-round-trip
    provides: the anno MCP tool surface (anno-tools.ts), the symbol round trip
      (anno-symbols.ts), the enum generator (anno-enum-gen.ts), and the
      Markdown memory-map renderer (anno-memmap-render.ts) this plan patches
provides:

  - "resolveStorePath() parent-realpath containment closing WR-01/T-11-PATH-ESCAPE"
  - "anno-acme-ident.ts: the one ACME identifier policy seam, extracted from anno-enum-gen.ts"
  - "REJECT-policy label-name validation on anno_set_label_name (outer + batch-inner) and importLabels()"
  - "escapeMarkdownCell() closing WR-04's Markdown-cell injection in renderMemoryMap()"
  - "11-SECURITY.md flipped to threats_open: 0 / status: verified"

affects: [anno-tools, anno-symbols, anno-enum-gen, anno-memmap-render, phase-11-security]

tech-stack:
  added: []
  patterns:

    - "One-seam identifier policy, re-exported (not copied) to avoid a module cycle between anno-tools.ts and anno-enum-gen.ts"
    - "Deepest-existing-ancestor realpath containment for a not-yet-existing leaf, with a dangling-symlink lstat guard on every remaining literal segment"
    - "Escape-only (never reject) control for legitimate multi-line/pipe-bearing user text destined for a Markdown table cell"

key-files:
  created:

    - .claude/mcp/vice/anno-acme-ident.ts
  modified:

    - .claude/mcp/vice/anno-tools.ts
    - .claude/mcp/vice/anno-tools.test.ts
    - .claude/mcp/vice/anno-enum-gen.ts
    - .claude/mcp/vice/anno-symbols.ts
    - .claude/mcp/vice/anno-symbol-roundtrip.test.ts
    - .claude/mcp/vice/anno-memmap-render.ts
    - .claude/mcp/vice/anno-memmap-render.test.ts
    - .claude/mcp/vice/package.json
    - .planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/11-SECURITY.md

key-decisions:

  - "Label-name policy is REJECT, never sanitize/quote, matching T-11-ENUM-NAME/T-11-REGBITS-PROSE precedent — the store's printed name must never diverge from the exported ACME symbol"
  - "Identifier policy extracted to a new import-nothing module (anno-acme-ident.ts) rather than importing it from anno-enum-gen.ts, because anno-enum-gen.ts statically imports runAnnoTool FROM anno-tools.ts and the reverse import would cycle"
  - "Markdown-cell escaping is a separate control from the identifier policy: comment evidence legitimately contains '|' and newlines, so the render leg escapes rather than rejects"

requirements-completed: [SEC-11-01, SEC-11-02, SEC-11-03]

duration: 13min (commit span; ~35min including research/verification)
completed: 2026-08-21
audit_acknowledged:
  milestone: v0.4.0
  at: 2026-08-23
  status: unknown
---

# Quick Task 260821-a86: Close Phase 11 Security Findings Summary

**Closed all three open Phase 11 security register rows: parent-realpath symlink containment in `resolveStorePath()`, a one-seam REJECT identifier policy on both label-name entry routes, and Markdown-cell escaping in the memory-map renderer — `11-SECURITY.md` now reads `threats_open: 0` / `status: verified`.**

## Performance

- **Duration:** 13 min between the first and last task commit (de788b9 → 2c287cb); total session time including reading source, writing RED tests, and running the full verification suite was longer (~35 min)
- **Tasks:** 3
- **Files modified:** 10 (9 modified, 1 created)

## Accomplishments

- **WR-01 / T-11-PATH-ESCAPE closed:** `resolveStorePath()`'s ENOENT fallback no longer trusts the literal (unresolved) candidate path. It now walks up to the deepest existing ancestor's realpath, rebuilds the candidate from the literal remaining segments, and lstat-guards each remaining segment against being an unresolved (e.g. dangling) symlink — closing a directory-symlink bypass for a not-yet-existing `.regen2000proj` leaf, at any depth, while preserving `anno_save_project`'s create path (including when an intermediate directory is also absent).
- **T-11-NAME-INJECT closed (both duplicate register rows, 11-08 and 11-11):** the ACME identifier policy (`assertLegalAcmeIdentifier`, `MAX_ACME_IDENTIFIER_LENGTH`, `ACME_IDENT_RE`, `ACME_RESERVED_MNEMONICS`) was extracted from `anno-enum-gen.ts` into a new, dependency-free module `anno-acme-ident.ts`, then wired into two additional entry routes with a REJECT (never sanitize) posture: `anno-tools.ts`'s `anno_set_label_name` (both the outer dispatch and the batch-inner call inside `anno_batch_execute`, refusing the whole batch and naming `calls[i]`), and `anno-symbols.ts`'s `importLabels()` (validated before any child is spawned, naming the offending `.lbl` line number and its own text). `anno-enum-gen.ts` re-exports the two names so its own existing consumers/tests (`anno-enum-gen.test.ts`'s seven cases) keep their current import path unchanged.
- **WR-04 closed:** `anno-memmap-render.ts` gained `escapeMarkdownCell()`, an escape-only control (never rejects) applied to every store-derived text interpolation in the generated document — comment evidence in the Range/Contents/Confidence/Evidence table, symbol names and confirmed-by evidence in the Routines table, and evidence text in the Open questions bullets. `RENDERER_VERSION` bumped `"1"` → `"2"` per its own docstring's instruction for an output-shape change.
- **`11-SECURITY.md` closed:** frontmatter flipped to `threats_open: 0` / `status: verified`; all three previously-`open` register rows (T-11-PATH-ESCAPE, T-11-NAME-INJECT ×2) flipped to `closed` with citations of the new controls and their pinning tests; "Open Findings (Detail)" retitled "Resolved Findings (closed 2026-08-21)" with a Resolution paragraph appended to each finding; Accepted Risks Log, Remediation Decision, and Sign-Off sections all updated to reflect the fix (not an accept); one new Security Audit Trail row appended (`69 closed / 0 open`).

## Task Commits

1. **Task 1: Close WR-01 — parent-realpath containment in resolveStorePath()** - `de788b9` (fix)
2. **Task 2: Close T-11-NAME-INJECT — one identifier seam, REJECT policy on both label-name entry routes** - `bb08c46` (fix)
3. **Task 3: Close WR-04 by escaping, then flip 11-SECURITY.md to verified** - `2c287cb` (fix)

_Each task followed the RED→GREEN TDD flow within a single commit: new regression tests were written and confirmed failing against the pre-fix code (verified via `node --test` before implementing), then the fix landed and the same commit captures both the failing-then-passing tests and the implementation together._

## Files Created/Modified

- `.claude/mcp/vice/anno-acme-ident.ts` - New module: the one ACME identifier policy seam (`assertLegalAcmeIdentifier`, `MAX_ACME_IDENTIFIER_LENGTH`, `ACME_IDENT_RE`, `ACME_RESERVED_MNEMONICS`), moved out of `anno-enum-gen.ts`, importing nothing from this repo
- `.claude/mcp/vice/anno-tools.ts` - `resolveStorePath()` rewritten with deepest-existing-ancestor realpath containment (`resolveViaDeepestExistingAncestor()`); new `AnnoLabelNameError` and `assertLegalLabelArg()`, wired into `assertCuratedTool()` and `assertCuratedBatch()`; `anno_set_label_name`'s schema description extended (text only, no schema-shape change)
- `.claude/mcp/vice/anno-tools.test.ts` - Two planted-directory-symlink regression tests plus a deeper-absent-intermediate-directory create-path test for `resolveStorePath()`; six illegal-ACME-identifier-shape tests, one legal-name acceptance test, and one batch-smuggling refusal test for the label-name policy
- `.claude/mcp/vice/anno-enum-gen.ts` - `MAX_ACME_IDENTIFIER_LENGTH`/`assertLegalAcmeIdentifier` now imported from `anno-acme-ident.ts` and re-exported for backward compatibility
- `.claude/mcp/vice/anno-symbols.ts` - `importLabels()` validates every discovered `.lbl` label name via `assertLegalAcmeIdentifier()` before `buildImportLblArgs()` is ever called, naming the offending line number and its own text; docstrings updated (`AnnoSymbolsError`, `importLabels()`, `regenerateAndReload()`, module header)
- `.claude/mcp/vice/anno-symbol-roundtrip.test.ts` - One new ceiling-style test proving `importLabels()` refuses an illegal `.lbl` name pre-spawn, naming line 2 and its text
- `.claude/mcp/vice/anno-memmap-render.ts` - New `escapeMarkdownCell()`; applied to `evidence`, `sym.name`, `confirmedBy`, and `u.evidence` interpolations; `RENDERER_VERSION` bumped to `"2"`; module header updated
- `.claude/mcp/vice/anno-memmap-render.test.ts` - Four unit tests for `escapeMarkdownCell()`, one `RENDERER_VERSION` pin, and one gated test proving pipe-and-newline-bearing comment evidence renders as one well-formed row with structural pipe count unchanged and escaped content preserved
- `.claude/mcp/vice/package.json` - Added `"anno-acme-ident.ts"` to `files[]`
- `.planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/11-SECURITY.md` - Flipped to `threats_open: 0` / `status: verified`; all three open register rows closed; findings section retitled and given Resolution paragraphs; Accepted Risks Log, Remediation Decision, and Sign-Off updated; one audit-trail row appended

## Decisions Made

- Label-name policy is **REJECT**, never sanitize or quote — matches this codebase's existing T-11-ENUM-NAME/T-11-REGBITS-PROSE precedent (a malformed name is a bug to surface, never silently rewritten, so the store's printed name can never diverge from what actually gets exported into ACME source).
- The identifier policy was **moved** into a brand-new, import-nothing module (`anno-acme-ident.ts`) rather than importing it from `anno-enum-gen.ts`, because `anno-enum-gen.ts` statically imports `runAnnoTool` FROM `anno-tools.ts` — importing the policy the other way would form a module cycle. `anno-enum-gen.ts` re-exports both names so its own tests and consumers needed zero changes to their import path.
- Markdown-cell escaping (WR-04) is a **separate control** from the identifier policy, by design: comment `evidence` legitimately contains `|` and embedded newlines (`anno_set_comment`'s own schema documents multi-line support), so the render leg escapes rather than rejects — refusing here would refuse valid data, not an attack.
- The dangling-symlink guard inside `resolveStorePath()`'s new ancestor-walk was added even though neither RED test in the plan's own `<behavior>` block strictly required it (both RED cases already resolve via a live, non-dangling symlink target) — it closes a related bypass shape (a symlink whose OWN target does not yet exist) named explicitly in the plan's `<action>` instructions, so it was implemented as part of the same fix rather than deferred.

## Deviations from Plan

None — plan executed exactly as written. One test-design adjustment during Task 3's TDD RED phase is worth noting (not a deviation from the plan's *behavior* requirements, just an implementation detail of how the gated test proves them): the plan's `<behavior>` block describes setting a single tricky comment and rendering it; the first draft of that test placed a second (plain-evidence) comment inside the same 5-byte block, but both comments then fell inside the same block's address range, and `findGradeInRange()`'s `.find()` picked the earliest-address comment for the block row, never surfacing the tricky one in the table row under test. Restructured to render two separate small synthesized projects (one plain-evidence, one tricky-evidence) and compare their single block rows directly — still satisfies all three behavior clauses (one well-formed row, unchanged structural pipe count, content preserved) without relying on which comment `.find()` happens to pick.

**Total deviations:** 0 auto-fixed (0 Rule 1/2/3, 0 Rule 4)
**Impact on plan:** None. All three tasks landed exactly as scoped.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required. `the external analyser 0.9.20` was already on `PATH` (per this repo's established environment), so every D-11-gated test in the three touched test files ran live rather than skipping.

## Next Phase Readiness

- All three plan-level verification gates pass: `npx tsc --noEmit -p tsconfig.json` (clean), `npm run test:automated` (1947 tests, 1942 pass, 0 fail, 5 pre-existing TODO — no regression), `node scripts/check-npm-packages.mjs` (both tarballs correct, `anno-acme-ident.ts` present in `@henols/vice-mcp`'s 73-file list).
- `git status --porcelain` shows only this quick task's own `.planning/quick/260821-a86-.../` directory as untracked (SUMMARY.md, not yet committed by this agent per the orchestrator's docs-commit convention) — the ten `files_modified` files are all committed across the three task commits, matching the plan's frontmatter exactly.
- WR-02 (refusal-shape inconsistency), WR-03 (unbounded batch recursion), and WR-05 (missing post-spawn `"error"` listener) were left untouched, exactly as the plan's objective specified as out of scope.
- **Recommended next step (per the plan's own `<output>` instruction):** re-run `/gsd-secure-phase 11` to have the auditor independently confirm `threats_open: 0`.

---
*Phase: quick-260821-a86*
*Completed: 2026-08-21*

## Self-Check: PASSED

All five key files confirmed present on disk (`anno-acme-ident.ts`, `anno-tools.ts`,
`anno-symbols.ts`, `anno-memmap-render.ts`, `11-SECURITY.md`) and all three task commits
(`de788b9`, `bb08c46`, `2c287cb`) confirmed present in `git log --oneline --all`.
