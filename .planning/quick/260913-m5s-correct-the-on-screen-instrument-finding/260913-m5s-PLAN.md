---
phase: quick-260913-m5s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/vice/fixtures/hazard-subject/FIXTURE-DESIGN.md
autonomous: true
requirements: [QUICK-260913-m5s]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The document no longer asserts that the autostart mechanism loaded nothing. The stated cause of the failing captures is the one measured in this run: the emulator's DEFAULT autostart mode is the disk-image path, and this installation has no disk-image infrastructure configured for it — a mode default, not an absence of loading capability."
    - "The document names the working route by flag (`-autostartprgmode 1`, direct RAM injection) and cites this run's own emulator log evidence that a program really was injected and run through it."
    - "The document records, as a fact a future reader will otherwise waste time rediscovering, that this flag does not appear in the emulator's own `-help` output on this build yet is accepted and works."
    - "The document no longer offers this installation's drive-type resource or absent drive ROM images as an explanation, sound or 'consistent with'. That explanation was measured false in this run: the plain 1541 ROM the emulator actually wants is present."
    - "The section's CONCLUSION survives the correction and is restated on the measured evidence: the screen-capture route licenses no per-program on-screen claim about this subject, because through the WORKING route the hazard subject and an unrelated committed program produce byte-identical captures at every capture moment tried."
    - "That conclusion is stated at exactly the strength the evidence carries and no further: a capture of a settled screen after a program has returned to BASIC is program-blind BY CONSTRUCTION, since any program that returns leaves the same screen; whether some mid-execution capture could discriminate was probed at several distinct cycle counts and did not, and the document calls that a bounded negative result rather than a proof that no such capture exists."
    - "The captures establishing that the capture moment genuinely moves are in the record: the md5 varies across cycle counts while the subject and the control never differ from each other at any single one of them."
    - "Every md5, exit status and argv line stated in the section comes from a capture the executor took in THIS run. No md5 and no numeric claim is carried across from the prior pass's prose."
    - "The document carries a short, plainly-worded note that further screen-capture investment on this fixture is not recommended, and that this fixture's alignment claims rest on byte-level assertions in `hazard-subject-fixture.test.ts` which never depended on a screen. Stated once, as guidance, not as a lament."
    - "PRESERVED, not rewritten: the PREDICTION-then-OBSERVATION voice; the `### Superseded observations` subsection byte-for-byte; the statement that the isolated single-construction builds were not re-run; the stated reason no PNG is committed; the closing paragraph on what no automated check in this repository can establish."
    - "Exactly ONE tracked file changed: `git diff --name-only HEAD` over `src/`, `docs/`, `scripts/`, `installer/` and `.claude-plugin/` prints that one path and nothing else. No fixture source, no committed image, no annostore, no hazard report, no reassembly gate and no test was touched."
    - "No captured PNG entered the tracked tree; the captures live under the project's gitignored tool-written root and the document carries md5s and argv lines instead."
    - "No planning vocabulary entered the document."
  artifacts:
    - src/mcp/vice/fixtures/hazard-subject/FIXTURE-DESIGN.md
    - .c64-re-tools/runs/hazard-screens/MANIFEST.txt
  key_links:
    - "Each md5 quoted in the document <-> a `capture p2-` record in MANIFEST.txt carrying that same md5 and the argv line that produced it."
    - "The emulator log line recording the program injection <-> the document's claim that a working load route exists; without that log line the correction is itself an assumption."
    - "The working route's subject md5 <-> the working route's unrelated-control md5 at each cycle count: their equality is what carries the surviving conclusion, and their variation ACROSS cycle counts is what proves the capture moment moved."
---

<objective>
Correct the stated CAUSE inside `## On-screen observations` in the hazard
subject's fixture design document, from this run's own captures, while leaving
that section's conclusion standing.

Purpose: the section currently asserts that no program was ever loaded by any
autostart capture in the prior pass, and offers this installation's drive
configuration as consistent with that. Both are false. A working load route
exists and loads this program; the emulator's default autostart mode is simply
the disk-image path, which this installation is not set up for. This
document's entire value is that its stated causes are measured rather than
assumed, so a false cause is a defect in the document's own currency, not a
wording nit. The conclusion the false cause was propping up — that this
capture route licenses no per-program on-screen claim — turns out to be true
for a better and more durable reason, and must come out of this correction
stronger rather than weaker.

Output: a corrected `## On-screen observations` section, and an appended
evidence manifest of every capture taken in this pass.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/mcp/vice/fixtures/hazard-subject/FIXTURE-DESIGN.md
</context>

<planning_time_observations>
These were MEASURED on this host at planning time, against genuine stock
`/usr/bin/x64sc` reporting `x64sc (VICE 3.9)`, `DISPLAY=:0`, 2026-09-13. They
exist ONLY to tell the executor which route is already known to work, so no
run time is spent rediscovering it. **They are not observations of record and
must not be transcribed into the document.** Every number the document ends up
carrying is re-derived by the executor in its own run.

- The working invocation shape:
  `timeout 180 /usr/bin/x64sc -autostartprgmode 1 -warp -limitcycles <N> -autostart <file.prg> -exitscreenshot <out.png>`
- Exit status 1 on `-limitcycles` termination is the normal path, not a failure.
- `-autostartprgmode` does NOT appear in this build's `-help` output, yet is
  accepted and works. A future reader will grep the help text and conclude the
  flag does not exist, so this is worth recording in the document.
- Through that route the emulator log carries `AUTOSTART: Loading PRG file ...
  with direct RAM injection` followed by `AUTOSTART: Injecting program data at
  $0801 (size $08e7)`.
- Drive ROMs ARE present at `~/.local/share/vice/DRIVES/`; the emulator does
  not complain about the plain 1541 (it complains about 1540, 1541-II and
  1570). `Drive8Type=0`.
- Captures taken through the working route at 20,000,000 cycles are readable
  settled screens, not black frames.
- Prior pass's evidence lives at `.c64-re-tools/runs/hazard-screens/`, with
  capture labels `A` through `H` already taken. Use a fresh label prefix so
  the two passes never collide in one manifest.
</planning_time_observations>

<halt_rules>
- The ONLY tracked file this plan may change is
  `src/mcp/vice/fixtures/hazard-subject/FIXTURE-DESIGN.md`. If a measurement
  suggests a fixture source, a committed image, the annostore, the hazard
  report, the reassembly gate or a test is wrong, STOP and report it in the
  summary. Do not edit it under this plan.
- Never install anything, and never reconfigure this installation to make a
  route work — in particular, do not set a drive type, add a disk image, or
  fetch a ROM. The point of this pass is to record what this machine does as
  it stands. If a capability is absent, refuse it by name in the manifest with
  what would have provided it, and carry on.
- No captured PNG is committed, for the reason the document already gives and
  must keep giving: nothing in this repository's suite renders or inspects a
  screen, so a committed screenshot would be a binary artifact no check could
  regenerate or falsify. The md5 plus the argv line is the regenerable residue.
- Do not re-open the four hazard classes, the amendment, or anything else in
  this document. The target is one section, and within it, one stated cause.
</halt_rules>

<tasks>

<task type="tracer">
  <name>Task 1: Reproduce the working load route end-to-end and take this pass's captures</name>
  <files>.c64-re-tools/runs/hazard-screens/MANIFEST.txt</files>
  <precondition>Genuine stock VICE is present at `/usr/bin/x64sc` and reports `x64sc (VICE 3.9)`; `DISPLAY=:0` is set. Assert both before the first launch and halt by name if either is absent.</precondition>
  <action>
Resolve the evidence directory rather than hardcoding it: import `toolsDir()`
from `src/mcp/vice/repo-root.ts` (Node 24 imports the `.ts` directly) and use
`<toolsDir>/runs/hazard-screens/`, the same directory the prior pass wrote.
APPEND to the existing `MANIFEST.txt` there under a new heading comment for
this pass; do not rewrite or truncate what is already in it.

Label every capture taken in this pass with the prefix `p2-` so it can never
be confused with the prior pass's `A` through `H` records.

Drive the whole capture set from ONE script, not one bash call per launch.
Each launch writes its own PNG and its own log file; the script appends one
line per capture beginning `capture ` followed by the label, the md5, the byte
size, the process exit status and the full argv line, matching the record
format already in the manifest.

**Argv shape, held constant across the whole pass so comparisons stay
like-for-like.** Start from `timeout 180 /usr/bin/x64sc -default
-autostartprgmode 1 -warp -limitcycles <N> -autostart <file> -exitscreenshot
<png>`, with `-default` first per this project's recorded flag-order habit for
this binary. If `-default` turns out to suppress the injection route, record
that BY NAME in the manifest as a finding of its own and fall back to the
shape without it — then use that fallback shape for every capture in the pass,
including the controls. Treat exit status 1 from `-limitcycles` as the normal
path and keep going.

Take these captures. Redirect each launch's stdout and stderr to its own log
file; the logs are the evidence for what the emulator did, not just what it
drew.

1. **Positive control**, no `-autostart` at all, at 20,000,000 cycles. Open
   this PNG yourself and describe what is on it. This re-establishes, in THIS
   run, that the rig renders a readable screen.
2. **Default-mode set**, WITHOUT `-autostartprgmode`, at 20,000,000 cycles,
   for three files: the committed aligned build, the committed mis-aligned
   build, and `src/mcp/vice/fixtures/petcat/computed-sys.prg` (an unrelated
   committed program). Grep each of these three logs for the emulator's own
   autostart error lines and quote the exact wording into the manifest. These
   three captures are what the corrected document will cite for "the default
   mode fails here, and here is the message it fails with."
3. **Working-route set**, WITH `-autostartprgmode 1`, at 20,000,000 cycles,
   for the same three files. Grep each log for the emulator's own lines about
   loading the PRG and injecting program data, and quote the injection line
   verbatim into the manifest, including the address and size it names. Open
   at least one of these three PNGs yourself and describe what is on it.
4. **Mid-execution probes**, WITH `-autostartprgmode 1`, at THREE further
   distinct cycle counts of your choosing below 20,000,000, running BOTH the
   aligned build and the unrelated control at each one, plus the mis-aligned
   build at one of them. Pick counts that straddle the moment the program is
   still running and the moment it has settled, so the set can answer two
   separate questions: does the md5 CHANGE as the cycle count changes (which
   is what proves the capture moment genuinely moved), and do the subject and
   the control ever DIFFER from each other at any single count.
5. **Pre-amendment images through the working route**, at 20,000,000 cycles.
   Extract the pre-amendment aligned and mis-aligned images from commit
   `758d7df6` into the session scratchpad — NOT into the repo tree, where
   scratch files have previously raced this project's own test suite — and
   push both through the identical working-route harness. Record each
   extracted file's own md5 as a `# ` note in the manifest as well, so every
   md5 the document may later quote is traceable to this manifest.

Also check, and record as a `# ` note: whether `-autostartprgmode` appears
anywhere in this build's own `-help` output. A reader who greps the help text
and finds nothing needs to know the flag works regardless.

Also check, and record as a `# ` note: which drive ROM files are actually
present under this user's VICE data directory, and which drive types (if any)
the emulator complains about in the logs you already captured. The prior pass
offered absent drive ROM images as an explanation; this note is what retires
that explanation on evidence rather than on assertion.

Close the pass with a verdict comment in the manifest stating, in plain
sentences and whichever way the numbers came out: (a) whether the default mode
loaded anything; (b) whether the working route loaded the program, citing the
injection log line; (c) whether the subject and the unrelated control ever
produced differing bytes at any single cycle count; and (d) whether the md5
varied across cycle counts at all. Keep (c) and (d) separate — they are
different facts and the document will use them for different purposes.
  </action>
  <verify>
    <automated>M="$(node --input-type=module -e 'import {toolsDir} from "./src/mcp/vice/repo-root.ts"; console.log(toolsDir());')/runs/hazard-screens/MANIFEST.txt"; test -s "$M" && test "$(grep -c '^capture p2-' "$M")" -ge 16 && test "$(grep '^capture p2-' "$M" | grep -oE '\b[0-9a-f]{32}\b' | wc -l)" -ge 16 && test "$(grep '^capture p2-' "$M" | grep -oE '\b[0-9a-f]{32}\b' | sort -u | wc -l)" -ge 2 && grep -q 'autostartprgmode' "$M" && grep -qi 'injecting program data' "$M" && test "$(ls "$(dirname "$M")"/p2-*.png | wc -l)" -ge 16</automated>
    <human-check>One positive-control PNG and one working-route PNG were each opened and described from what is actually on them, not inferred from file size.</human-check>
  </verify>
  <done>`MANIFEST.txt` carries at least sixteen `capture p2-` records with md5s, byte sizes, exit statuses and full argv lines; at least two distinct md5s appear among them; the emulator's own injection log line is quoted verbatim; the `-help` check and the drive-ROM presence check are recorded as notes; and a four-part verdict comment closes the pass.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the stated cause in the on-screen observations section</name>
  <files>src/mcp/vice/fixtures/hazard-subject/FIXTURE-DESIGN.md</files>
  <action>
Edit `## On-screen observations` (line 341 in the currently committed file) in
place. This is a correction of a stated cause, NOT a rewrite of the section:
touch the paragraphs named below and leave the rest of the section alone.

**PRESERVE, byte for byte.** The `### Superseded observations (pre-amendment
bytes, commit 758d7df6)` subsection and everything under it, down to but not
including the closing paragraph. It is history and it is already correctly
marked as history. Also preserve the paragraph stating that the isolated
single-construction builds were not rebuilt or re-run, the sentence explaining
why no PNG is committed, and the closing paragraph about what no automated
check in this repository can establish. Also preserve the section's voice
throughout: PREDICTION stated first from the source, OBSERVATION recorded
second, the two free to disagree.

**CORRECT the paragraph beginning "Investigating that equality rather than
stopping at it found the actual cause"** (line 372 in the currently committed
file). Its claim that the autostart mechanism ran nothing at all on this
installation is false, and its closing sentence — the one beginning "This
installation's own" — offers a drive-configuration explanation this run
measured false. Replace the whole paragraph with what this run measured:

- The emulator's DEFAULT autostart mode is the disk-image path, which needs
  infrastructure this installation does not have configured. That is why those
  captures failed — a mode default, not an absence of loading capability.
  Quote the emulator's own error wording from this run's default-mode logs.
- A working route exists and loads this program: `-autostartprgmode 1`, direct
  RAM injection. Cite this run's own emulator log line naming the injection and
  the address and size it lands at. Without that log line the correction would
  itself be an assumption, which is the failure being corrected.
- Record that this flag is absent from the build's own `-help` output yet is
  accepted and works, so the next reader does not grep the help text and
  conclude otherwise.
- Say plainly that the drive-configuration explanation the paragraph used to
  offer is retired, and why: the ROM the emulator actually wants for the plain
  drive is present on this installation, so that was never a sound
  explanation. State this as a correction of the record, in one sentence, not
  as a confession.

Keep the two sentences in that paragraph that remain true and were separately
measured last pass — that a second attempt through the binary monitor's own
wire command refused with its own error code, and that combining the CLI flag
with the binary monitor terminated the process before a session was possible.
Mark them as observations of the DEFAULT-mode route specifically, so they no
longer read as evidence about loading capability in general.

**REWRITE the two OBSERVATION paragraphs** (the ALIGNED build at line 407 and
the MIS-ALIGNED build at line 427 in the currently committed file) so each
carries two observations from this run, by md5, in this order: the
default-mode capture and what it shows, and then the working-route capture and
what it shows. Delete the sentences that read the black frame as proof the
program was never loaded to attempt rendering; the working-route capture is
what actually settles what these builds put on a settled screen. State what
the working-route capture shows from your own inspection of it.

**RESTATE the conclusion on the corrected evidence, as its own paragraph.**
This is the most important paragraph in the section and it must come out of
the correction stronger, not hedged:

- Through the WORKING route, the hazard subject and an unrelated committed
  program produced byte-identical captures at every capture moment tried. Name
  the cycle counts and the md5s.
- The md5 varied ACROSS cycle counts while the subject and the control never
  differed from each other at any single one. Those are two separate facts and
  the paragraph must keep them separate: the first is what proves the capture
  moment genuinely moved, the second is what carries the conclusion.
- Give the honest reading and stop there. A capture taken after a program has
  returned to BASIC shows the settled BASIC screen, which is the same for any
  program that returns — so a settled-screen capture is program-blind BY
  CONSTRUCTION, not by accident. Whether some mid-execution capture could
  discriminate was probed at the cycle counts named and did not; call that a
  bounded negative result, and say in the same breath that it is not a proof
  that no such capture exists. Do not let the paragraph claim more than the
  captures support — overreaching here would repeat, in the opposite
  direction, exactly the defect this pass is correcting.

**UPDATE the Instrument paragraph** (line 343 in the currently committed file)
so the argv shape it states is the one actually used in this pass, and so it
names both routes — the default mode and the injection mode — rather than one.
Keep its existing sentence about where the captures live and why none is
committed.

**UPDATE the "Post-amendment versus pre-amendment, as measured" paragraph**
(line 441 in the currently committed file) with this run's working-route
captures of the pre-amendment images, by md5, replacing the prior pass's
numbers. Say what that comparison is worth given the conclusion above — which
is still little, but now for the stated construction reason rather than
because nothing loaded.

**ADD, once, a short closing note before the superseded subsection.** In plain
words: further screen-capture investment on this fixture is not recommended,
because the route is program-blind by construction at a settled screen; and
this fixture's alignment claims rest on byte-level assertions in
`hazard-subject-fixture.test.ts`, which never depended on a screen and are not
weakened by anything in this section. Guidance for the next reader, stated
once — not a lament, and not repeated elsewhere in the section.

Every md5, cycle count, exit status and argv line you write must come from a
`capture p2-` record in this pass's manifest. Carry nothing across from the
prior pass's prose.

Write no planning vocabulary anywhere in the document: no planning-directory
path, no slash-prefixed workflow command name, no phase or plan number, no
bare decision or gap identifier. Commit hashes are fine; they resolve for any
reader of the repository.
  </action>
  <verify>
    <automated>F=src/mcp/vice/fixtures/hazard-subject/FIXTURE-DESIGN.md; M="$(node --input-type=module -e 'import {toolsDir} from "./src/mcp/vice/repo-root.ts"; console.log(toolsDir());')/runs/hazard-screens/MANIFEST.txt"; grep -q '^## On-screen observations' "$F" && grep -q 'autostartprgmode' "$F" && grep -qi 'injecting program data' "$F" && grep -q 'hazard-subject-fixture.test.ts' "$F" && ! grep -qi 'missing drive ROM' "$F" && ! grep -qi 'no program was ever loaded' "$F" && test "$(grep -c 'PREDICTION' "$F")" -ge 4 && test "$(grep -c 'OBSERVATION' "$F")" -ge 4 && grep -q '758d7df6' "$F" && grep -qi 'no captured PNG is committed' "$F" && grep -qi 'isolated single-construction builds' "$F" && grep -qi 'no automated check in this repository' "$F" && diff <(git show HEAD:"$F" | sed -n '/^### Superseded observations/,/^\*\*What no automated check/p') <(sed -n '/^### Superseded observations/,/^\*\*What no automated check/p' "$F") && for h in $(grep -oE '\b[0-9a-f]{32}\b' "$F" | sort -u); do grep -q "$h" "$M" || exit 1; done && ! grep -qE '\.planning/|/gsd-|\bD-[0-9]|\bG-[0-9]-[0-9]' "$F" && test "$(git diff --name-only HEAD -- src/ docs/ scripts/ installer/ .claude-plugin/)" = "$F"</automated>
    <human-check>The restated conclusion paragraph claims exactly what the captures support — program-blind by construction at a settled screen, plus a bounded negative result for mid-execution captures — and does not assert that no discriminating capture exists.</human-check>
  </verify>
  <done>The stated cause is the measured one (default disk-image autostart mode, with a working injection route named and evidenced from this run's logs); the drive-configuration explanation and the never-loaded claim are gone; both builds carry default-mode and working-route observations by this run's md5s; the conclusion is restated at exactly the strength the captures carry; the superseded subsection, the isolation-build statement, the no-PNG reasoning and the closing paragraph are intact; the next-reader note names the test file; no planning vocabulary; and this is the only tracked file changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host process -> emulator | The plan launches `/usr/bin/x64sc` with attacker-irrelevant but host-affecting flags, including one that injects bytes into emulated RAM. |
| repo tree -> gitignored tool root | Capture artifacts and logs are written outside the tracked tree. |
| this document -> npm/plugin consumer | `src/mcp/vice/**` ships verbatim; a reader of the shipped document has no planning tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-m5s-01 | Tampering | tracked fixture tree | high | mitigate | Task 2's verify asserts `git diff --name-only HEAD` over the product directories equals exactly the one document path, so a stray edit to a fixture source, a committed image, the annostore or a test fails the gate. |
| T-m5s-02 | Tampering | preserved history | high | mitigate | Task 2's verify diffs the `### Superseded observations` region against `git show HEAD` and fails on any change, so the retained history cannot be silently reworded while correcting the current prose. |
| T-m5s-03 | Repudiation | evidence manifest | medium | mitigate | Every md5 appearing in the document is asserted to appear in this pass's manifest alongside the argv that produced it, so a reader regenerates the capture and contradicts the prose rather than trusting it. |
| T-m5s-04 | Information disclosure | shipped document | medium | mitigate | The verify negative-greps the document for planning-directory paths, workflow command names and bare decision or gap identifiers, none of which resolve for an npm or plugin reader. |
| T-m5s-05 | Denial of service | host emulator launches | low | accept | Every launch is wrapped in `timeout 180` and bounded by `-limitcycles`; a hung emulator is reaped rather than left running. |
| T-m5s-06 | Tampering | package manifest | low | accept | No package-manager install occurs anywhere in this plan and the halt rules forbid one, so no legitimacy gate is required. |
</threat_model>

<verification>
- `MANIFEST.txt` under the gitignored tool-written root carries at least
  sixteen `capture p2-` records, each with an md5 and a full argv line, plus
  the `-help` note, the drive-ROM presence note and the four-part verdict.
- Every md5 quoted in the document also appears in that manifest.
- The `### Superseded observations` region is byte-identical to `HEAD`.
- `git diff --name-only HEAD -- src/ docs/ scripts/ installer/ .claude-plugin/`
  prints exactly `src/mcp/vice/fixtures/hazard-subject/FIXTURE-DESIGN.md`.
- `git status --porcelain` shows no new tracked PNG or log anywhere.
- From `src/mcp/vice`, `npm run test:automated` reports zero failures and its
  SKIPPED SET, compared member by member against the set captured before any
  edit in this plan, is unchanged. Stop any running broker first: a live
  broker deterministically reddens one of this suite's tests. Never pipe the
  command into `tail` — capture the status on the same line, because a pipe
  reports the pipe's status and fakes a green baseline.
</verification>

<success_criteria>
The section's stated cause is the one measured in this run; the working load
route is named, flagged and evidenced from the emulator's own log; the
drive-configuration explanation is retired on evidence; the surviving
conclusion is restated at exactly the strength the captures carry, with the
construction reason and the bounded negative result kept distinct; the
preserved material is untouched; a single next-reader note points at the
byte-level test file; and exactly one tracked file changed.
</success_criteria>

<output>
Create `.planning/quick/260913-m5s-correct-the-on-screen-instrument-finding/260913-m5s-SUMMARY.md` when done.
</output>
