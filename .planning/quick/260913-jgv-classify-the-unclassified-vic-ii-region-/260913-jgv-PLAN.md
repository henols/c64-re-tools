---
phase: quick-260913-jgv
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/vice/fixtures/hazard-subject/hazard-subject-align.a
  - src/mcp/vice/fixtures/hazard-subject/hazard-subject-align-misaligned.a
  - src/mcp/vice/fixtures/hazard-subject/hazard-subject.prg
  - src/mcp/vice/fixtures/hazard-subject/hazard-subject-misaligned.prg
  - src/mcp/vice/fixtures/hazard-subject/hazard-subject.annostore.json
  - src/mcp/vice/fixtures/hazard-subject/FIXTURE-DESIGN.md
  - src/mcp/vice/reassembly-gate-run.test.ts
  - .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-tree-rebuild.md
  - .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-movement-rebuild.md
  - .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-hazard-disposition.md
  - .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-red-controls.md
  - .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-guards.md
  - docs/phase49-the-reassembly-gate-findings.md
autonomous: true
requirements: [QUICK-260913-jgv, BUILD-06]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The real hazard report over the committed subject reports ZERO regions whose outcome is the undecided one. The region `$087A..$0FFF` — the one that was undecided — carries a decided outcome, reached because the subject now STATES its two missing VIC-II dependencies statically, never because a detector was widened to guess or default a register."
    - "The subject's planted page-alignment construction is visible in the report for the first time: at least one finding whose class is `page-alignment` exists, so the report's finding set now reaches all FOUR planted constructions the fixture's own design document claims, not three."
    - "`hazard-subject-align.a` and `hazard-subject-align-misaligned.a` received the SAME routine-body edit: their non-comment, non-blank line counts still differ by exactly the two deliberate `!byte 0` filler lines and nothing else — the property `hazard-subject-fixture.test.ts` already asserts, re-passing after the edit rather than relaxed."
    - "`hazard-subject.prg`, `hazard-subject-misaligned.prg` and `hazard-subject.annostore.json` were REGENERATED by their two committed generator scripts against a real ACME, never hand-edited: re-running both generators leaves nothing further to commit."
    - "`FIXTURE-DESIGN.md` records the amendment in its own established voice — what changed, why the read-modify-write could not stay, and what was GIVEN UP (the CIA2 port bits the read-modify-write preserved and a bare immediate store overwrites) — so the cost is on the record rather than glossed."
    - "Every one of the seven gate inputs was re-measured in ONE `node --test` invocation, and each re-measured value is appended as a new bare column-0 outcome line to its own declared evidence file — the prior run's lines left in place, superseded by final-occurrence-wins rather than overwritten."
    - "`docs/phase49-the-reassembly-gate-findings.md`'s seven `inputs.*` values each equal the FINAL column-0 occurrence of their declared line in their declared evidence file, and its `verdict` / `verdict_rule_applied` are what `runReassemblyGate()` itself returns when handed those seven transcribed values — checked mechanically, never asserted in prose."
    - "`evidence/DECISION-RULE.md` and `evidence/SCHEMA.md` are byte-unchanged: `git diff --quiet HEAD` over both passes clean. The rule table decided this verdict; nothing about the rules moved to accommodate it."
    - "The findings document distinguishes THIS run from the prior one in its own body — a reader can see that the subject was amended between them and what the earlier verdict was — rather than reading as if only one measurement ever existed."
    - "`npm run test:automated` from `src/mcp/vice` reports zero failures, and its SKIPPED SET (compared member by member against the set captured before any edit in this plan) is unchanged."
  artifacts:
    - src/mcp/vice/fixtures/hazard-subject/hazard-subject-align.a
    - src/mcp/vice/fixtures/hazard-subject/hazard-subject-align-misaligned.a
    - src/mcp/vice/fixtures/hazard-subject/hazard-subject.annostore.json
    - src/mcp/vice/fixtures/hazard-subject/FIXTURE-DESIGN.md
    - docs/phase49-the-reassembly-gate-findings.md
  key_links:
    - "The two immediate stores added to `hazard-subject-align.a` <- are what `recoverImmediateStoreFacts()` in `anno-hazard-report.ts` can recover -> which is what lets `deriveGraphicsRanges()` in `anno-graphics.ts` return a map with an EMPTY `missingRegisters`. If the store is not an immediate load followed directly by the matching store to a watched address, nothing is recovered and the region stays undecided."
    - "Zero undecided regions <- is what `hazardCoverageOutsideDiffScope()` reads -> to score `DIFF_SCOPE_COVERAGE`. Its committed rule makes ANY undecided region `incomplete` unconditionally, and that rule is not to be touched."
    - "The frozen acknowledgement array in `reassembly-gate-run.test.ts` <- must cover every finding the amended subject's report carries -> or `disposeHazardReport()` returns the blocked disposition. The entries are hand-written to match the MEASURED findings; the array is never trimmed and the report is never narrowed to fit it."
    - "The five evidence files' final column-0 outcome lines <- are the ONLY admissible source for -> the findings document's seven `inputs.*` values. Writing the document from a run transcript held in context, or from this plan's own expectations, breaks the transcription rule that is the whole basis of the verdict's honesty."
---

<objective>
Classify the one region the committed hazard subject's own report cannot decide
(`$087A..$0FFF`, an incomplete VIC-II register recovery) by fixing the gap where the gap
actually is — in the SUBJECT — then re-run the reassembly gate end to end and update the
published verdict from the re-run's own evidence.

Purpose: the subject's design document claims FOUR planted constructions; the report currently
surfaces three, because two of the three VIC-II registers the page-alignment derivation needs
are not recoverable from the subject as written. That single gap is also what makes the gate's
`DIFF_SCOPE_COVERAGE` input `incomplete` and drives the published `red` verdict under `R7`.
Closing it makes the fixture prove what it claims and lets the gate be scored on its real
byte-level behaviour instead of on a subject-side recovery hole.

Output: an amended subject (both twins) with regenerated artifacts, an honest record of the
amendment's cost in `FIXTURE-DESIGN.md`, five evidence files carrying an appended
re-measurement, and a findings document whose verdict is derived — mechanically — from those
appended lines.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

@src/mcp/vice/fixtures/hazard-subject/hazard-subject-align.a
@src/mcp/vice/fixtures/hazard-subject/hazard-subject-align-misaligned.a
@src/mcp/vice/fixtures/hazard-subject/FIXTURE-DESIGN.md
@src/mcp/vice/anno-graphics.ts
@src/mcp/vice/reassembly-gate-run.test.ts
@.planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/SCHEMA.md
@.planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/DECISION-RULE.md
@docs/phase49-the-reassembly-gate-findings.md
</context>

<measured_baseline>
Measured live at planning time, 2026-09-13, against the committed tree. Treat these as the
BEFORE state; re-derive anything you depend on rather than trusting this block blindly.

- The committed subject's real report: **3 findings** — `indexed-dispatch` `$081C`
  (`stack-return-dispatch`), `self-modifying-code` `$0825`
  (`store-target-in-instruction-opcode-byte`), `cycle-exact-raster` `$10C2`
  (`timer-reload-in-vectored-handler`) — and **16 regions**, of which exactly **one** is
  undecided: `$087A..$0FFF`.
- The store's own ranges place `hazard_align_entry` at `$087A`, and the block `$087A..$0FFF`
  spans the alignment routine PLUS the padding the `!align $7ff, 0` directive emits.
  `align_char_base` is `$1000`, `align_sprite_base` `$1040`, `align_level_table` `$107F`,
  `align_music_table` `$1087`, `hazard_raster_entry` `$108F`, image end `$10E7`,
  committed image 2281 bytes on disk.
- **The consequence that matters for sizing this change, and that contradicts a plausible
  assumption:** because `!align $7ff, 0` pads to `$1000` regardless of how long the routine
  above it is, growing or shrinking the alignment ROUTINE is absorbed by that padding.
  Everything from `$1000` up — including the `$10C2` anchor the frozen acknowledgement array
  names — is expected to stay put, and the image length is expected to stay 2281 bytes.
  **This is an expectation, not a licence to skip checking it:** measure the real addresses
  after regenerating and reconcile any drift rather than assuming none.
- `acme` release 0.97 "Zem" resolves on `$PATH` (`/home/henrik/.local/bin/acme`). No VICE
  broker and no `x64sc` process is running.
- The existing fixture `src/mcp/vice/fixtures/ghidra/charset-phantom.a` already carries the
  exact recoverable combination this plan needs the subject to state — a `$dd00` write naming
  bank 0, a `$d018` write, and a `$d011` write whose bit 5 is clear for character-set mode —
  and `anno-hazard-report.test.ts`'s cross-check table pins it positive for `page-alignment`
  at `$1000`. It is the model to mirror, not a file to edit.
</measured_baseline>

<prohibitions>
These are hard. Violating any of them defeats the point of the work.

1. **Do not widen the detector.** `recoverImmediateStoreFacts()` declining a read-modify-write
   result is correct — an RMW result is a runtime fact, not a static one — and
   `anno-graphics.ts`'s own module header forbids substituting a power-on default. Neither
   `anno-hazard-report.ts` nor `anno-graphics.ts` is edited by this plan.
2. **Do not narrow the diff scope, swap the subject, or relax
   `hazardCoverageOutsideDiffScope()`.** Re-measuring with a different subject, scope or extent
   to obtain a better verdict is explicitly forbidden by the published findings document.
3. **Do not edit `evidence/DECISION-RULE.md` or `evidence/SCHEMA.md`.** They are frozen
   pre-commitments; a test and the findings document both assert they are unedited. Only the
   measured INPUTS may change.
4. **Do not hand-edit `hazard-subject.prg`, `hazard-subject-misaligned.prg` or
   `hazard-subject.annostore.json`.** They are generated. Regenerate, never hand-write.
5. **No planning vocabulary in any file under `src/` or `docs/`** — no `.planning/` path, no
   `/gsd-` command name, no phase or plan number, no bare `D-NN` / `G-NN-N` id, no `ROADMAP` /
   `REQUIREMENTS` cross-reference. State the mechanism instead. (`docs/phase49-...-findings.md`
   is the standing exemption for its own `phase:` / `requirement:` frontmatter keys and its own
   citations of evidence paths — that document's whole contract is those citations.)
6. **Never pipe a test command into `tail`.** Redirect to a scratch file and read `$?` on the
   same line; a pipeline reports the tail process's status and fakes a green baseline.
7. **No scratch files inside the repository tree.** The suite races on repo-tree scratch files
   and one such site leaks a gitignored directory that then fails deterministically. Use
   `/tmp/claude-1000/quick-260913-jgv/` (create it with `mkdir -p`).
</prohibitions>

<tasks>

<task type="tracer">
  <name>Task 1: State the two missing VIC-II dependencies in the subject, regenerate, and prove the region is classified</name>
  <precondition>ACME release 0.97 "Zem" resolves on `$PATH` (measured 2026-09-13 at `/home/henrik/.local/bin/acme`). Both fixture generators refuse rather than write a partial fixture if it is absent, and this project never auto-installs an external tool — if it is missing, halt and name it.</precondition>
  <files>src/mcp/vice/fixtures/hazard-subject/hazard-subject-align.a, src/mcp/vice/fixtures/hazard-subject/hazard-subject-align-misaligned.a, src/mcp/vice/fixtures/hazard-subject/hazard-subject.prg, src/mcp/vice/fixtures/hazard-subject/hazard-subject-misaligned.prg, src/mcp/vice/fixtures/hazard-subject/hazard-subject.annostore.json, src/mcp/vice/fixtures/hazard-subject/FIXTURE-DESIGN.md</files>
  <reversibility rating="reversible">The subject amendment is a source edit plus two deterministic regenerations; `git checkout` restores the prior fixture exactly, and re-running both generators re-derives either state from source.</reversibility>
  <action>
FIRST, before touching anything: capture the pre-change suite baseline. `mkdir -p /tmp/claude-1000/quick-260913-jgv`, then run the project's automated suite from `src/mcp/vice`, redirecting to `/tmp/claude-1000/quick-260913-jgv/suite-before.txt` and reading the exit status on the same line. This runner is not a glob — it removes a frozen manual-only list from the directory listing — and its reporter, measured on this tree, marks a skipped case with a line beginning `﹣ ` (U+FE63 SMALL HYPHEN-MINUS) and reports totals on lines beginning `ℹ `. The set of skipped case NAMES, with the per-case timing parenthesis stripped, is the baseline Task 3 diffs against — a count is not sufficient, because one skip appearing and another disappearing leaves the count unchanged.

Then amend `hazard-subject-align.a` so the two VIC-II registers the page-alignment derivation needs are recoverable STATICALLY:

(a) Replace the four-instruction read-modify-write of `$dd00` (`lda $dd00` / `and #...` / `ora #...` / `sta $dd00`) with a single immediate load followed directly by the store — the only shape `recoverImmediateStoreFacts()` recovers. Choose the literal so its low two bits select VIC bank 0 (those bits are active low) and its serial-bus output bits are left in their released state; mirror the value the existing `fixtures/ghidra/charset-phantom.a` fixture already uses for the same purpose rather than inventing one. Do NOT derive the value through arithmetic on another label — an immediate operand is required, and the recovery reads the literal.

(b) Add an immediate load followed directly by a store to `$d011` naming text (character-set) mode explicitly — bit 5 clear. Place it beside the `$d018` write, since the two together are what name the character-set block.

Rewrite the surrounding source comments so they state, in the file's own voice, WHY each write is an immediate store rather than a read-modify-write: a read-modify-write's result is a runtime fact and cannot be recovered from a static image, so a subject that depends on a register value and never states it statically is asking a static report to guess. Name the cost in the same place: a bare store to `$dd00` overwrites the CIA2 port A bits the read-modify-write preserved — the serial-bus lines among them — and say which bits the chosen literal writes and what state it leaves them in.

(c) Apply the IDENTICAL routine-body edit to `hazard-subject-align-misaligned.a`. The twin's routine body must stay byte-identical to its sibling's: the only differences permitted between the two files are header comments, the two `!byte 0` filler lines, and the two mis-alignment comment blocks. `hazard-subject-fixture.test.ts` asserts exactly this (non-comment, non-blank line counts differing by exactly two, and exactly two `!byte 0` lines) — that case re-passing is the check, not a separate assertion to add.

Then regenerate, from `src/mcp/vice`, in this order: `node fixtures/hazard-subject/make-hazard-subject-fixtures.mjs` (writes both images), then `node fixtures/hazard-subject/make-hazard-subject-annostore.mjs` (writes the store export). Run each generator a SECOND time and confirm `git status --porcelain` shows nothing further changed — the committed artifacts must be a pure function of the committed sources.

Then MEASURE the amended subject's real report with a throwaway script under `/tmp/claude-1000/quick-260913-jgv/`: load `hazard-subject.prg` and the regenerated `hazard-subject.annostore.json`, map the store's `ranges` rows into the `start_address` / `end_address` / `type` shape `buildHazardReport()` takes, build the report through the real `buildHazardReport()`, and print data rows only — no legend, no header prose, because the gate below reads this file literally. One row per finding carrying class, anchor, mechanism and blocked address; one row per region carrying its start, its end and its outcome token LAST on the line. Record the output into `/tmp/claude-1000/quick-260913-jgv/report-after.txt` — Task 2 writes the acknowledgement entries from THIS output, never from expectation.

Reconcile the measurement against the baseline block above: confirm whether the `$10C2` raster anchor and the `$081C` / `$0825` anchors moved, and whether the image length changed. If any of them DID move, that is a real result — do not force it back; carry the new addresses into Task 2's acknowledgement array and say so in the summary.

Finally, amend `FIXTURE-DESIGN.md`. Its class-3 section and its "what a weaker construction would not have proved" framing both currently describe a subject whose alignment dependency the report could not see. Record, in the document's own prediction/observation voice: what the two writes now state; that the read-modify-write form was the reason the dependency was invisible to a static report, and that the detector declining it is correct rather than a defect; and what was GIVEN UP — the CIA2 port bits the read-modify-write preserved, now overwritten by a fixed literal — stated as a cost accepted, not as an improvement. Do not let this edit land silently, and do not remove the existing on-screen observation sections.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test hazard-subject-fixture.test.ts anno-hazard-report.test.ts hazard-subject-reassembly.test.ts > /tmp/claude-1000/quick-260913-jgv/task1-tests.txt 2>&1; echo "exit=$?"; grep -c '^ℹ fail 0$' /tmp/claude-1000/quick-260913-jgv/task1-tests.txt</automated>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice/fixtures/hazard-subject && for f in hazard-subject-align.a hazard-subject-align-misaligned.a; do code=$(sed 's/;.*//' "$f"); s=$(printf '%s\n' "$code" | grep -cE '^[[:space:]]*sta \$(dd00|d011|d018)\b'); r=$(printf '%s\n' "$code" | grep -cE '^[[:space:]]*(lda|and|ora)[[:space:]]+\$dd00\b'); echo "$f immediate-store-targets=$s residual-dd00-reads=$r"; [ "$s" = 3 ] && [ "$r" = 0 ] || exit 1; done; echo "both-twins-state-all-three-registers=ok"</automated>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -Eqi 'page-alignment' /tmp/claude-1000/quick-260913-jgv/report-after.txt && ! grep -qiE 'outcome[^a-z]*unclassified|[[:space:]]unclassified$' /tmp/claude-1000/quick-260913-jgv/report-after.txt; echo "page-alignment-present-and-no-undecided-region=$?"</automated>
  </verify>
  <done>Both twin sources carry the same amended routine body; both images and the store export are regenerated by their own scripts and are idempotent under a second run; the measured report in `report-after.txt` carries at least one `page-alignment` finding and zero undecided regions; `FIXTURE-DESIGN.md` records the change, its reason and its cost; the three fixture-facing test files pass.</done>
</task>

<task type="auto">
  <name>Task 2: Decide the acknowledgement, re-run all seven gate inputs in one invocation, and append the re-measurement to the five evidence files</name>
  <precondition>No VICE broker and no `x64sc` process is running (measured 2026-09-13: none). A live broker deterministically reddens an unrelated backend test and would void the suite result — stop it before measuring, and leave it stopped.</precondition>
  <files>src/mcp/vice/reassembly-gate-run.test.ts, .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-tree-rebuild.md, .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-movement-rebuild.md, .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-hazard-disposition.md, .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-red-controls.md, .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-guards.md</files>
  <reversibility rating="costly">Acknowledging a finding is a recorded human acceptance a downstream phase may read as a precondition. Reversing it means republishing a verdict, not just editing a file — so the reason is written to be true of this run specifically, and is checkable against the run's own byte-diff.</reversibility>
  <action>
**The acknowledgement decision, decided here rather than left to the executor.** Every finding the amended report carries — including each new `page-alignment` finding — gets its own acknowledgement entry in the frozen array in `reassembly-gate-run.test.ts`, with a reason that is true of THIS run. The reasoning, which must be carried into the array's own header comment in the executor's own words:

- The reason is true and checkable, not convenient: this run re-exports and reassembles the subject at its ORIGINAL layout, and the rebuild's own byte-diff against the exporter's expected bytes is what proves nothing moved. The character set and the sprite shape stay at their committed addresses, so the register-held index and the pointer byte still name the correct block.
- It is the same reasoning already frozen for the other three entries, each of which turns on this run not relocating the thing the finding names. Acknowledging three findings on that ground while refusing the page-alignment ones on identical ground would be inconsistent, not conservative.
- The resulting disposition is `acknowledged`, which is NOT clean and NOT green. It records that a human accepted a named movement constraint — exactly what that token was chosen to mean.
- The limit that keeps it honest, and that the reason text must NOT overstep: the acknowledgement says nothing about whether the alignment dependency is harmless in general. The mis-aligned twin proves it is not. Each reason is scoped to "this run does not relocate it" and says no more.

If, on reading the measured report, the executor judges that reason UNTRUE of some finding — for instance a finding whose blocked address this run does relocate — it does not acknowledge that one. A `blocked` disposition and a red verdict under the blocked-hazard rule is an acceptable, honest outcome; a reason written to turn the verdict green is not. Whatever is decided, the array's header comment states it.

Write the entries from `/tmp/claude-1000/quick-260913-jgv/report-after.txt` — class, anchor address and mechanism copied from the measured findings, one entry per finding. The array's existing header comment currently explains why the run comes back blocked and why no region acknowledgement is supplied; that explanation is now false and must be replaced, not left standing. Keep the comment free of planning vocabulary: describe the mechanism, never a phase or plan id.

Then re-measure ALL SEVEN gate inputs in ONE `node --test` invocation covering `acme-seam.test.ts`, `acme-verify.test.ts`, `reassembly-gate.test.ts`, `reassembly-gate-ack.test.ts`, `reassembly-gate-movement.test.ts` and `reassembly-gate-run.test.ts`, redirected to `/tmp/claude-1000/quick-260913-jgv/gate-run.txt` with the exit status read on the same line. One invocation is required, not a convenience: the rule that produces `all-observed` for the planted controls demands they be observed red in the SAME suite run that produced the result being reported, and the subject just changed — carrying that input forward from the previous run would be asserting a property of a run that no longer exists. The seam guard's two set equalities are re-run in that same invocation for the same reason.

Re-verify the ordering property separately and for real, from git history rather than from any document's claim about itself: `git log --diff-filter=A` over the two frozen pre-commitment files, confirming both add-commits still precede every measurement commit in this phase. Capture that transcript.

Then APPEND — never overwrite — a dated re-measurement section to each of the five evidence files. This is the schema's own mechanism: a declared line written more than once in its file resolves to its FINAL column-0 occurrence, so appending supersedes the earlier value while leaving the prior run's section and its lines intact as history. Each appended section states plainly that it is a re-measurement taken after the subject was amended, names what changed about the subject, and quotes the run's own raw output for the lines that file declares. Re-emit all seven declared names (eight lines, counting both declared occurrences of the diff-scope input) across the five files, because all seven were re-measured in one invocation — do not append only the ones whose value moved.

Do not touch `evidence/DECISION-RULE.md` or `evidence/SCHEMA.md`.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && git diff --quiet HEAD -- .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/DECISION-RULE.md .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/SCHEMA.md; echo "frozen-files-unedited=$?"</automated>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/.planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence && for pair in "TREE_REBUILD 49-tree-rebuild.md" "DIFF_SCOPE_COVERAGE 49-tree-rebuild.md" "MOVEMENT_REBUILD 49-movement-rebuild.md" "DIFF_SCOPE_COVERAGE 49-movement-rebuild.md" "HAZARD_DISPOSITION 49-hazard-disposition.md" "RED_CONTROLS 49-red-controls.md" "SECOND_PATH_GUARD 49-guards.md" "ORDERING_PROOF 49-guards.md"; do set -- $pair; n=$(grep -c "^$1: " "$2"); echo "$1 $2 occurrences=$n"; [ "$n" -ge 2 ] || { echo "MISSING re-measurement occurrence for $1 in $2"; exit 1; }; done; echo "all-appended=ok"</automated>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test acme-seam.test.ts acme-verify.test.ts reassembly-gate.test.ts reassembly-gate-ack.test.ts reassembly-gate-movement.test.ts reassembly-gate-run.test.ts > /tmp/claude-1000/quick-260913-jgv/gate-run.txt 2>&1; echo "exit=$?"; grep -c '^ℹ fail 0$' /tmp/claude-1000/quick-260913-jgv/gate-run.txt</automated>
  </verify>
  <done>The frozen acknowledgement array covers every finding the measured report carries, each with a reason true of this run, and its header comment states the decision and its limit; all seven inputs were re-measured in one invocation with the ordering property re-checked against real git history; each of the five evidence files carries an appended, dated re-measurement section whose bare column-0 lines supersede the prior run's by final-occurrence; the two frozen pre-commitment files are byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Transcribe the verdict into the findings document and prove the transcription mechanically</name>
  <files>docs/phase49-the-reassembly-gate-findings.md</files>
  <action>
Update `docs/phase49-the-reassembly-gate-findings.md` by TRANSCRIBING from the evidence files written in Task 2 — reading them off disk, taking the final column-0 occurrence of each declared line — never from the run transcript still in context and never from this plan's expectations. Update each `inputs.*` value and its trailing citation comment to name the evidence file and the line number of the occurrence actually taken.

Derive `verdict` and `verdict_rule_applied` by walking the frozen rule table in written order against the transcribed values, first match wins. Do not assume which rule fires; the mechanical check below is what settles it.

Keep the frontmatter key order exactly as the frozen schema fixed it. The `probe_date` is the date of THIS run.

Rewrite the body so it reports this run rather than the previous one, and so it distinguishes the two rather than overwriting history silently:

- The verdict section states which rule fired and reproduces that rule's text verbatim from the frozen table.
- The walk table shows, rule by rule, which antecedents did not match and the transcribed value that failed each.
- "What was measured" reports the amended subject: what the two added immediate stores state, the finding set the report now carries, the region outcomes, the byte length and block/file shape as MEASURED, and the diff-scope extents for both runs.
- A section records that this is a RE-MEASUREMENT after the subject was amended: what the prior run's verdict and rule were, what changed about the subject between the two runs, and why that change is not the forbidden re-measurement with a different subject or scope to obtain a better verdict — the subject was amended to state a dependency it always had, the rule table and the diff-scope rule are untouched, and both runs remain on the record.
- If the verdict is `acknowledged`, the "what the next phase may rely on" section says plainly that acknowledged is not green: a named movement constraint was accepted by a human for a rebuild that relocates nothing, and the alignment dependency itself remains real — the mis-aligned twin is the standing proof.
- The existing "Override" section about the declared-line count is now stale, because the appended re-measurement adds more occurrences. Update it to the newly measured count and state why the count grew, keeping its original point intact: the rule found not to match reality was a verify-command literal, never a rule in the frozen table.

Then write a mechanical transcription checker to `/tmp/claude-1000/quick-260913-jgv/transcription-check.mjs` and run it. It must, without hard-coding any expected VALUE: (1) for each of the seven declared line names, read the final column-0 occurrence from its declared evidence file, treating the diff-scope input as its two declared occurrences and resolving to the pessimistic one exactly as the frozen rule does; (2) compare each against the corresponding `inputs.*` value parsed out of the findings document's frontmatter, ignoring the trailing citation comment; (3) import `runReassemblyGate` from `src/mcp/vice/reassembly-gate.ts`, hand it the seven transcribed values, and compare its returned outcome and rule id against the document's own `verdict` and `verdict_rule_applied`. It exits non-zero on any drift and prints which key drifted.

Finally, re-run the project's automated suite from `src/mcp/vice`, redirected to `/tmp/claude-1000/quick-260913-jgv/suite-after.txt` with the exit status read on the same line, and diff the skipped case names — the lines beginning `﹣ `, timing parenthesis stripped, sorted — against the baseline captured in Task 1. Report any member that appears or disappears by name. A count comparison is not sufficient: one skip appearing while another disappears leaves the count unchanged.
  </action>
  <verify>
    <automated>node /tmp/claude-1000/quick-260913-jgv/transcription-check.mjs; echo "transcription-and-verdict-derived=$?"</automated>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && head -25 docs/phase49-the-reassembly-gate-findings.md | grep -cE '^verdict: (green|acknowledged|red)$' && head -25 docs/phase49-the-reassembly-gate-findings.md | grep -cE '^verdict_rule_applied: R([1-9]|1[0-2])$'</automated>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated > /tmp/claude-1000/quick-260913-jgv/suite-after.txt 2>&1; echo "exit=$?"; grep -c '^ℹ fail 0$' /tmp/claude-1000/quick-260913-jgv/suite-after.txt && diff <(grep '^﹣ ' /tmp/claude-1000/quick-260913-jgv/suite-before.txt | sed -E 's/ \([0-9.]+ms\)//' | sort -u) <(grep '^﹣ ' /tmp/claude-1000/quick-260913-jgv/suite-after.txt | sed -E 's/ \([0-9.]+ms\)//' | sort -u) && echo "skip-set-unchanged"</automated>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && git diff --quiet HEAD -- .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/DECISION-RULE.md .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/SCHEMA.md; echo "frozen-files-still-unedited=$?"</automated>
  </verify>
  <done>The findings document's seven `inputs.*` values each equal the final column-0 occurrence of their declared line in their declared evidence file, with an updated citation; its `verdict` and `verdict_rule_applied` are what `runReassemblyGate()` returns for those seven values, proven by a checker that hard-codes no expected value; the body reports this run, distinguishes it from the prior one and explains why the amendment is not verdict-shopping; the automated suite reports zero failures with an unchanged skipped set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed source -> generated artifact | Two generator scripts spawn a local assembler and write three committed binary/JSON artifacts. A hand-edited artifact would not be traceable to source. |
| measurement run -> published verdict | The findings document is a machine-readable precondition a downstream phase reads. Its values cross from a transient run into a durable claim. |
| frozen pre-commitment -> measured inputs | The rule table and schema were committed before any measurement. Only the inputs may move. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-jgv-01 | Tampering | `hazard-subject.prg`, `hazard-subject-misaligned.prg`, `hazard-subject.annostore.json` | medium | mitigate | Regenerate through the two committed generator scripts only; run each generator twice and require `git status --porcelain` clean on the second run, so the artifacts are provably a pure function of committed sources. |
| T-jgv-02 | Tampering | `evidence/DECISION-RULE.md`, `evidence/SCHEMA.md` | high | mitigate | `git diff --quiet HEAD` over both files is a verify gate in Tasks 2 and 3; a rule table edited after a measurement makes the whole instrument advisory. |
| T-jgv-03 | Repudiation | `docs/phase49-the-reassembly-gate-findings.md` | high | mitigate | Every `inputs.*` value is checked against the final column-0 occurrence in its declared evidence file, and the verdict is recomputed by `runReassemblyGate()` itself, by a checker that hard-codes no expected value. Prose cannot drift from the measurement undetected. |
| T-jgv-04 | Elevation of Privilege | the frozen acknowledgement array in `reassembly-gate-run.test.ts` | high | mitigate | An acknowledgement can turn a red verdict into an accepted one. Task 2 fixes the only admissible reason shape (true of this run, scoped to "this run does not relocate it"), requires it in the array's own header comment, and explicitly permits a `blocked` outcome over a convenient one. |
| T-jgv-05 | Information Disclosure | `src/**`, `docs/**` | low | mitigate | The no-planning-vocabulary rule is restated as a prohibition; the fixture's own source test already negative-checks the alignment source, and edits to `src/` comments must state the mechanism rather than cite a phase or plan id. |
| T-jgv-SC | Tampering | npm / pip / cargo installs | n/a | accept | This plan installs nothing. No package-manager install task exists; the only external tool is a user-installed assembler that is detected and refused by name if absent, never fetched. No legitimacy audit is required because no package is added. |
</threat_model>

<verification>
- The amended subject's report carries at least one `page-alignment` finding and zero undecided regions, measured from the regenerated artifacts.
- Both twin alignment sources received the same routine-body edit; the fixture test's structural twin assertion re-passes untouched.
- Both generators are idempotent: a second run leaves nothing to commit.
- All seven gate inputs were re-measured in a single `node --test` invocation; the ordering property was re-checked against real git history.
- Each of the five evidence files carries an appended re-measurement whose bare column-0 line supersedes the prior run's by final-occurrence-wins, with the prior section intact.
- The findings document's seven inputs and its verdict are mechanically proven to be a faithful transcription and a correct rule-table derivation.
- `evidence/DECISION-RULE.md` and `evidence/SCHEMA.md` are byte-unchanged.
- `npm run test:automated` reports zero failures with a skipped SET identical to the pre-change baseline.
</verification>

<success_criteria>
The region that could not be classified is classified, because the subject now states the two
VIC-II dependencies it always had in a form a static report can recover — with the detector,
the diff-scope rule, the decision rule table and the schema all untouched. The gate was re-run
in full from that amended subject, its verdict re-derived mechanically from freshly appended
evidence, and the cost of the amendment — both to the fixture's own serial-bus preservation and
to the earlier published verdict — is on the record rather than absorbed silently.
</success_criteria>

<output>
Create `.planning/quick/260913-jgv-classify-the-unclassified-vic-ii-region-/260913-jgv-SUMMARY.md` when done.

The summary MUST record, as measured values rather than expectations: the finding set the
amended subject produces (class, anchor, mechanism for each); whether any pre-existing anchor
address or the image byte length moved, and if so what moved and by how much; the seven
re-measured input values; the verdict token and the rule id that fired; and which findings were
acknowledged with the reason used. If any of this disagrees with the `<measured_baseline>`
block above, say so explicitly — a disagreement is a result, not an error to be reconciled away.
</output>
