---
phase: quick-260913-t43
plan: 01
subsystem: testing
tags: [ci, container-guard, host-tool, node-test, github-actions]

requires: []
provides:
  - "skill-acme-build-cli.test.ts, host-tool.test.ts, host-tool-transport.test.ts pin their host-route/loopback-route intent instead of inheriting CI's job-wide container-simulation env"
  - "CI's 'Assemble the acme-build scaffold (library-free)' step now runs a committed test file (skill-acme-build-cli.test.ts) instead of a CI-only shell block"
  - "ci-guardrails.test.mjs binds that step's name, working-directory, invoked file, and blocking-ness to a regression-tested assertion"
affects: [ci, host-tool, acme-build, container-guard]

actuals:
  tokens: 4328
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Host-route test files strip CONTAINER_WORKSPACE_PATH from spawned-subprocess environments via a named helper (hostRouteChildEnv), rather than letting isInsideContainer() read a job-wide path-translation fixture as a container proof"
    - "Loopback-listener test files pin VICE_BROKER_CONTROL_DIAL_HOST=127.0.0.1 at module scope (mirrors broker-e2e.test.ts's existing idiom) so resolveControlTarget() never falls through to the container bridge alias"

key-files:
  created: []
  modified:
    - src/mcp/vice/skill-acme-build-cli.test.ts
    - src/mcp/vice/host-tool.test.ts
    - src/mcp/vice/host-tool-transport.test.ts
    - .github/workflows/ci.yml
    - src/mcp/vice/ci-guardrails.test.mjs

key-decisions:
  - "Root cause was a single job-wide env var (CONTAINER_WORKSPACE_PATH) doing double duty as both a path-translation fixture and container-guard's third detection signal -- fixed by narrowing which CHILD processes/dial targets inherit that signal, never by deleting the job-wide env block (three MANUAL_ONLY_TESTS files gate real in-container coverage on it)."
  - "Moved the CI scaffold check from a standalone shell block into node --test skill-acme-build-cli.test.ts so the same check runs in the ordinary developer suite, not only in CI."
  - "Added a ci-guardrails.test.mjs regression test binding the step's name/scope/invoked-file/blocking-ness together, verified by hand to red when the fix is reverted."

requirements-completed: [QUICK-260913-t43]

coverage:
  - id: D1
    description: "Under CI's own environment values (CONTAINER_WORKSPACE_PATH/HOST_WORKSPACE_PATH set as the workflow sets them), the narrowed automated suite reports 0 failures, 9 skips, 4405 tests (baseline 4399 + 6 tests this plan adds)."
    requirement: "QUICK-260913-t43"
    verification:
      - kind: unit
        ref: "npm run test:automated (CI env values) -- tests 4405, pass 4396, fail 0, skipped 9"
        status: pass
    human_judgment: false
  - id: D2
    description: "With both workspace variables unset, the same narrowed suite still reports 0 failures, 9 skips."
    requirement: "QUICK-260913-t43"
    verification:
      - kind: unit
        ref: "npm run test:automated (clean env) -- tests 4405, pass 4396, fail 0, skipped 9"
        status: pass
    human_judgment: false
  - id: D3
    description: "CI's 'Assemble the acme-build scaffold (library-free)' step exists under that exact name, blocking, positioned between the ACME install step and Test, and now runs skill-acme-build-cli.test.ts."
    requirement: "QUICK-260913-t43"
    verification:
      - kind: unit
        ref: "ci-guardrails.test.mjs -- new real-file and synthetic-fixture cases, all pass; hand-verified to red on a reverted ci.yml"
        status: pass
    human_judgment: false
  - id: D4
    description: "acme.mjs is byte-for-byte unchanged; no .mts file or resources/ artifact is modified; buildHostToolArgv() remains the sole argv construction."
    requirement: "QUICK-260913-t43"
    verification:
      - kind: other
        ref: "git diff --name-only HEAD -- src/skills src/mcp/vice/resources '*.mts' .planning/todos (empty output, checked after every task)"
        status: pass
    human_judgment: false

duration: 55min
completed: 2026-09-13
status: complete
---

# Quick Task 260913-t43: Unblock CI's Scaffold Step and Its 11 Container-Simulation Failures Summary

**Stripped CI's job-wide container-simulation env var from three host-route/loopback test files, and moved the CI scaffold gate from a CI-only shell block into the committed test file it already mirrors.**

## Performance

- **Duration:** 55 min
- **Started:** 2026-09-13T19:03:00Z (approx, per commit timestamps)
- **Completed:** 2026-09-13T19:58:00Z (approx)
- **Tasks:** 3
- **Files modified:** 5

## Accomplishments

- `skill-acme-build-cli.test.ts` now builds its assembling-case child environment through a named `hostRouteChildEnv({ libraryFree })` helper that strips `CONTAINER_WORKSPACE_PATH` from the copy, with a save/restore test proving the strip survives even when the *parent* process carries the variable. This fixed the 4 failures in that file under CI's environment values (`hostToolOverControlPlane: broker.json not present or unreadable`).
- `host-tool.test.ts` and `host-tool-transport.test.ts` each pin `VICE_BROKER_CONTROL_DIAL_HOST = "127.0.0.1"` at module scope (mirroring the existing `broker-e2e.test.ts:47` idiom), so their own loopback control-plane listener is always the dial target regardless of the ambient container signal. This fixed the 6 + 1 = 7 remaining failures (`getaddrinfo ENOTFOUND host.docker.internal`).
- `.github/workflows/ci.yml`'s "Assemble the acme-build scaffold (library-free)" step now runs `node --test skill-acme-build-cli.test.ts` (with `VICE_REQUIRE_ACME=1`, `working-directory: src/mcp/vice`) instead of a standalone shell block, under the exact same step name, position, and blocking behaviour. The job-wide `CONTAINER_WORKSPACE_PATH`/`HOST_WORKSPACE_PATH` env block is deliberately left untouched (three `MANUAL_ONLY_TESTS` files gate real in-container coverage on it).
- `ci-guardrails.test.mjs` gained five new cases (three synthetic-fixture controls, two real-file assertions) binding the scaffold step's name, `working-directory`, invoked test file, and blocking-ness to a regression-tested assertion — plus the anti-rot check that the invoked file genuinely exists on disk and is absent from `MANUAL_ONLY_TESTS`.

## Task Commits

1. **Task 1: Make every host-side invocation in the job pin the route it is testing, instead of inheriting a simulation** - `3f69ab4d` (fix, tdd)
2. **Task 2: Point the CI scaffold step at the file that already makes its claim, and say why in the comment** - `f7ef5a05` (fix)
3. **Task 3: Bind the step's existence and its blocking-ness to a test, so it cannot be quietly weakened or deleted** - `54a6be56` (test)

**Plan metadata:** committed separately by the orchestrator (this SUMMARY, STATE.md, ROADMAP.md are excluded from these commits per plan instructions).

## Files Created/Modified

- `src/mcp/vice/skill-acme-build-cli.test.ts` - added `hostRouteChildEnv()` helper (strips `CONTAINER_WORKSPACE_PATH`), a save/restore test for it, `runAcme()` now calls the helper
- `src/mcp/vice/host-tool.test.ts` - module-scope `VICE_BROKER_CONTROL_DIAL_HOST = "127.0.0.1"` pin with rationale comment
- `src/mcp/vice/host-tool-transport.test.ts` - same module-scope pin with rationale comment
- `.github/workflows/ci.yml` - scaffold step body replaced with `node --test skill-acme-build-cli.test.ts`; rationale comment rewritten in prose, no planning-tree path or bare decision id
- `src/mcp/vice/ci-guardrails.test.mjs` - five new cases binding the scaffold step's identity/scope/blocking-ness to a test, reusing existing helpers

## Decisions Made

- Confirmed the planner's root-cause framing by direct measurement before editing: baseline run under CI's exact env values reproduced exactly 11 failures across exactly the three named files (4 in `skill-acme-build-cli.test.ts`, 6 in `host-tool.test.ts`, 1 in `host-tool-transport.test.ts`) — matching the plan's stated numbers precisely.
- Did not delete or relocate the job-wide `env:` block in `ci.yml` (per plan and per the three `MANUAL_ONLY_TESTS` files that gate real in-container coverage on it).
- Did not touch `src/skills/acme-build/scripts/acme.mjs`, any `.mts` file, or anything under `src/mcp/vice/resources/` — confirmed empty via `git diff --name-only HEAD -- src/skills src/mcp/vice/resources '*.mts' .planning/todos` after every task.

## Deviations from Plan

None - plan executed exactly as written. One environmental discrepancy was investigated and found out of scope (see below), not fixed.

### Out-of-scope observation (not fixed, per scope-boundary rule)

While running the full-suite verification, `anno-verb-coverage.test.ts`'s "the shipped skill tree's markdown is byte-identical to the source tree's" case failed intermittently (2 of 4 full-suite runs). Root cause: `installer/skills/` is a generated, gitignored tree produced by `installer/scripts/sync-skills.mjs`, and a parallel session (per this repo's standing multi-session policy) was concurrently editing `src/skills/**` during this task's execution, making the generated tree transiently stale. Running `node installer/scripts/sync-skills.mjs` before each final measurement resolved it; this is unrelated to CI's scaffold-step defect (a different test file, a different root cause, pre-existing infrastructure) and outside this plan's five-file scope, so it was not edited. Both final full-suite measurements below were taken with the generated tree freshly in sync.

### Todo file observation (not touched, no action needed)

The plan's must-haves named `.planning/todos/pending/2026-08-19-acme-build-scaffold-library-missing-on-both-provisioning-routes.md` and required it stay untouched. That file was already relocated to `.planning/todos/completed/` by an unrelated prior commit (`232b057e`, part of a different earlier phase), before this task began. This plan's diff never touches `.planning/todos` (confirmed via the untouchables check after every task), so the constraint — "untouched by this plan" — holds; the file's completed/pending status itself predates this session and was not altered here.

## Issues Encountered

None beyond the out-of-scope flake noted above.

## Verification Evidence

**Baseline (before this plan's changes), CI environment values, narrowed to the three affected files:**
```
tests 131, pass 120, fail 11, skipped 0
```
11 failing test names (all reproduced and matched the plan's stated breakdown):
- `host-tool-transport.test.ts:384` - "two host_tool requests issued over the real control plane without awaiting the first resolve..."
- `host-tool.test.ts` (6): "END TO END: an acme.build request whose includes escapes the workspace root is refused..."; "END TO END: a real control-plane round trip assembles a real source file with real ACME..."; "two overlapping host_tool requests on the same listener both resolve..."; "END TO END (slow): a ghidra.analyze request whose fake launcher sleeps longer than the connect-timeout constant..."; "END TO END: an oracle.probe request carrying the retired \"command\" key is refused..."; "two overlapping slow ghidra.analyze host_tool requests over the real control-plane route..."
- `skill-acme-build-cli.test.ts` (4): "assembling the scaffolded source with the library path cleared produces a program file whose first two bytes are the documented load address..."; "the symbol verb against the scaffolded source exits 0 and lists at least one symbol..."; "a source file with a deliberate syntax error: exits non-zero..."; "the output-path (-o) and output-directory (--out-dir) options place the artefact where they say..."

**After this plan, same three files, CI environment values:**
```
tests 132, pass 132, fail 0, skipped 0
```
**After this plan, same three files, clean environment (both variables unset):**
```
tests 132, pass 132, fail 0, skipped 0
```

**Full narrowed suite (`npm run test:automated`), CI environment values, after resyncing the (unrelated, parallel-session-touched) generated skills tree:**
```
tests 4405, pass 4396, fail 0, skipped 9
```
**Full narrowed suite, clean environment (both variables unset), after the same resync:**
```
tests 4405, pass 4396, fail 0, skipped 9
```
4405 = 4399 (stated baseline) + 6 tests this plan adds (1 in `skill-acme-build-cli.test.ts`, 5 in `ci-guardrails.test.mjs`).

**What this proves:** the route decision, the scaffold verb, the real assembler invocation through the seam, and the load-address assertion all behave correctly under the exact environment values CI's job sets, and identically with those variables absent. The full narrowed suite is unharmed in both directions and the three `MANUAL_ONLY_TESTS` files' container-gated coverage is untouched (no edit to the job-wide `env:` block).

**What this does NOT prove:** that the Debian-trixie distribution-package assembler on an actual GitHub-hosted runner behaves identically to the local build used in this measurement (a different build, possibly a different version); that the runner's temp directory and permissions behave the same as this host; or that no LATER step in the CI job fails. Only a real run after this commit is pushed settles those.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

CI's first substantive step (the scaffold gate) and the eleven failures downstream of the same root cause are all fixed and regression-tested locally under both the CI and clean environments. The next real signal is the actual GitHub Actions run after push: confirm the `Assemble the acme-build scaffold (library-free)` step goes green, and confirm `Test` (the full `npm test` glob) also goes green now that the same root cause no longer affects `host-tool.test.ts`/`host-tool-transport.test.ts` under CI's real runner. No blockers identified.

## Self-Check: PASSED

All 5 modified files and the SUMMARY.md itself confirmed present on disk; all 3 task commit hashes (`3f69ab4d`, `f7ef5a05`, `54a6be56`) confirmed present in `git log --oneline --all`.

---
*Phase: quick-260913-t43*
*Completed: 2026-09-13*
