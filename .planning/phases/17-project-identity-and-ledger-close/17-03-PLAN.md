---
phase: 17-project-identity-and-ledger-close
plan: 03
type: execute
wave: 3
depends_on: ["17-01", "17-02"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/todos/completed/2026-08-20-relocate-plugin-payload-under-src-and-merge-mcp-json.md
  - .planning/todos/completed/2026-08-21-stale-phase-pointers-in-stock-cia-and-stock-dispatch-comments.md
autonomous: true
requirements: [DEBT-04, CORE-01]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`grep -cE '^\\| [A-Z]+-[0-9]+ \\| [0-9.]+ \\| Complete \\|$' .planning/REQUIREMENTS.md` reports 16, up from the 14 it reports before this plan, and the companion count of non-Complete Traceability rows is 0 — every v0.4.0 requirement is closed."
    - "Both DEBT-04 and CORE-01 carry a closure-note blockquote naming the plan that closed them, where the outcome is recorded, and what pins it mechanically — or, where nothing does, stating that plainly rather than leaving it unsaid."
    - "DEBT-04's closure note closes the forward reference DEBT-01's own note opened, recording the pending count as moving 2 → 0 after Phase 16 discharged PKG-01 and PKG-03, and citing the guard fix and the STATE.md edit by plan and task."
    - "`.planning/todos/pending/` still holds zero `.md` files after this plan's own edits, re-checked at the end rather than assumed from plan 17-01's measurement — the count DEBT-04 reports was taken with nothing left in this milestone able to change it."
    - "The closure record names `/gsd-audit-milestone` as the backstop for any finding this phase's own post-execution code review files after the count was taken, because Phase 17 is the last phase of v0.4.0 and there is no Phase 18 to inherit one."
    - "`cd src/mcp/vice && npm test` reports 0 failures across the full suite — not `npm run test:automated`, which skips the nine `MANUAL_ONLY_TESTS` suites and has hidden CI failures on this project before."
    - "ROADMAP.md's Phase 17 section states a real plan count and a real plans list in place of `**Plans**: TBD`, and the Progress table's Phase 17 row reads a real completed count and `Complete` rather than `0/TBD` and `Not started`."
    - "No `docs-*.test.ts` guard is red at the point the phase is recorded complete — `GATE-01` (Phase 12) forbids recording `status: passed` while any is."
  prohibitions:
    - statement: "MUST NOT tick a requirement `[x]` or flip a Traceability row to `Complete` on the strength of a plan having run. The tick asserts the requirement's own criterion was met; a closure note that narrates activity instead of naming the evidence that closes the criterion is the failure mode, and this milestone's own audit history is the reason the note convention exists."
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT record this phase — or the milestone — as complete while any `docs-*.test.ts` guard is red, and MUST NOT reach green by narrowing the gate to `npm run test:automated`. A close is only worth the guards that were actually satisfied to reach it."
      category: values
      status: resolved
      verification: judgment
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "DEBT-04 and CORE-01 ticked with closure notes, and a Traceability table with no open row"
      contains: "CORE-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 17's real plan list, outcome notes, and a completed Progress row"
      contains: "Phase 17"
    - path: ".planning/STATE.md"
      provides: "Current Position, Session Continuity and Operator Next Steps advanced to the v0.4.0 milestone close"
      contains: "Operator Next Steps"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/STATE.md"
      via: "DEBT-04's closure note cites the ledger count STATE.md's Deferred Items section derives and docs-deferred-ledger.test.ts guards"
      pattern: "DEBT-04"
---

<objective>
Close Phase 17 and, with it, v0.4.0's requirement set: tick DEBT-04 and CORE-01
with closure notes that name their evidence, bring ROADMAP.md's Phase 17 section
and Progress row to their real values, advance STATE.md to the milestone close,
and — the part that only this plan can do — re-take the pending-todo measurement
*after* every edit this phase makes, so DEBT-04's criterion 3 is satisfied by
observation rather than by assumption.

Purpose: this is the phase's designated closer, the role plan 15-12 and plan
16-11 each played for their own phases. It exists for a structural reason
RESEARCH.md names as this phase's second-largest hazard: Phase 17 is the last
phase of v0.4.0, `code_review` is enabled, and every prior phase in this milestone
hit the same cause — the review gate runs after the last plan's SUMMARY, so no
plan can disposition its own phase's findings. There is no Phase 18 to inherit one.
If this phase's own review files a pending todo, the "true close" count is stale by
one and nothing downstream in the milestone catches it. This plan re-checks, and
names the backstop for anything filed later.

Output: a closed requirement set, an honest closure record, and a full green suite.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-project-identity-and-ledger-close/17-RESEARCH.md
@.planning/phases/17-project-identity-and-ledger-close/17-PATTERNS.md
@.planning/phases/17-project-identity-and-ledger-close/17-01-SUMMARY.md
@.planning/phases/17-project-identity-and-ledger-close/17-02-SUMMARY.md
</context>

<execution_notes>
**Worktree mode is OFF for this phase — stated explicitly, not inherited.**
`.planning/config.json` sets `workflow.use_worktrees: false` and ROADMAP.md
carries an explicit planning note. Every deliverable in this plan is `.planning/`
*content*; worktree mode strips those files from commits, so a plan of this shape
silently cannot deliver under it.

**The gate is the full `cd src/mcp/vice && npm test`.** `npm run test:automated`
skips the nine `MANUAL_ONLY_TESTS` suites and has hidden CI failures on this
project before. Task 3 runs the full suite and nothing else counts.

**No nested headless sessions.** No task here may invoke `claude -p` or any
nested headless agent session.

**Milestone-close work is out of scope for this plan.** Rewriting ROADMAP.md's
`**v0.4.0 in progress:**` narrative into a `**v0.4.0 final state:**` paragraph,
archiving phase directories, and cutting the milestone all belong to
`/gsd-complete-milestone` and `/gsd-cleanup`, which run after this phase. This
plan closes Phase 17 and its two requirements; it does not close the milestone.
The one exception is the four transposed phase numbers named concretely in
task 2, which are factual errors about this phase and are in scope to fix.

**Do NOT touch the frozen 19-item baseline citations** at `ROADMAP.md:533`,
`PROJECT.md:165`, `PROJECT.md:485` or `MILESTONES.md:11`. They are the v0.3.0
close-time snapshot — historical record, not a live ledger.
</execution_notes>

<tasks>

<task type="auto">
  <name>Task 1: Tick DEBT-04 and CORE-01 in REQUIREMENTS.md with closure notes, flip both Traceability rows, and backfill the two missing todo Resolutions</name>
  <reversibility rating="costly">Flipping a requirement to Complete is the record the milestone audit reads and archives days later; undoing it after the audit means correcting the archived milestone record, not reverting a line.</reversibility>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the file being modified. Read in full; you need the exact placement of the `### Debt Disposition` and `### Project Identity` blocks and the Traceability table.
    - `.planning/REQUIREMENTS.md:85-105` — the FORK-01/FORK-02 closure note, the shape to copy: who decided, where it is recorded with a date, what pins it mechanically, what evidence closes it.
    - `.planning/REQUIREMENTS.md:111-130` — the DEBT-01 closure note, which contains the forward reference DEBT-04's note must close ("Phase 17 measures `DEBT-04` after Phase 16 discharges the payload-relocation todo (`PKG-01`), the last item this milestone's own work still removes").
    - `.planning/phases/17-project-identity-and-ledger-close/17-01-SUMMARY.md` and `17-02-SUMMARY.md` — the actual evidence and, for CORE-01, the verdict branch and whether the discretionary guard was built.
    - `.planning/todos/completed/2026-08-20-relocate-plugin-payload-under-src-and-merge-mcp-json.md` and `.planning/todos/completed/2026-08-21-stale-phase-pointers-in-stock-cia-and-stock-dispatch-comments.md` — the two files being backfilled.
    - Any completed todo that already carries a `## Resolution` section (30 of the 35 do) — read one for the section's shape before writing two more.
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/todos/completed/2026-08-20-relocate-plugin-payload-under-src-and-merge-mcp-json.md, .planning/todos/completed/2026-08-21-stale-phase-pointers-in-stock-cia-and-stock-dispatch-comments.md</files>
  <action>
    Three items. The first two close the phase's requirements; the third is
    explicitly low-priority record-keeping that must not block either success
    criterion — if it turns out to cost more than a few minutes, record it as
    deliberately skipped in the SUMMARY and move on.

    **1. DEBT-04.** Change its checkbox from `[ ]` to `[x]` and add a closure-note
    blockquote immediately beneath it in the house shape — a `> ` blockquote opening
    `**Closure note (Phase 17, plans 17-01/17-03).**`. It must carry, concretely: the
    count moving **2 → 0**, with the arithmetic (0 pending todo files + 0 UAT-gap
    rows = 0) and the cause (Phase 16 discharging `PKG-01` and `PKG-03`); the explicit
    statement that 0 is strictly lower than the 19 items inherited at the v0.3.0
    close; that the ledger is still *derived* from `.planning/todos/pending/` and
    still *guarded in both directions* by `src/mcp/vice/docs-deferred-ledger.test.ts`,
    naming plan 17-01 task 1 as the plan that lowered its non-vacuity floor to zero
    because the previous floor of two could not express an empty tree, and plan 17-01
    task 2 as the STATE.md edit; and the categories the 0 excludes by the table's own
    accounting rule (the still-`Deferred` carried-forward rows and the carried
    WR-class review findings), so the note cannot be read as a claim of zero known
    debt. It must also close DEBT-01's forward reference by name — DEBT-01's own note
    promised Phase 17 would take this measurement after Phase 16, and this is where
    that promise is discharged.

    **2. CORE-01.** Change its checkbox from `[ ]` to `[x]` and add a closure note
    beneath it opening `**Closure note (Phase 17, plans 17-02/17-03).**`. Mirror
    FORK-01's field set exactly, because CORE-01 is its direct analog: the verdict
    word the human actually chose; that it was decided by a **human**, at plan 17-02
    task 1's `gate="blocking-human"` checkpoint, not inferred and not auto-approved
    (FORK-01's note uses that phrasing and CORE-01 earns the same); where it is
    recorded (`.planning/PROJECT.md` → `## Core Value`, dated 2026-08-23); the
    specific evidence weighed, named rather than gestured at; and what pins it
    mechanically — `src/mcp/vice/docs-core-value-decision.test.ts` if plan 17-02 task
    3 built it, or, if the verdict was `restate` and it deliberately was not built,
    the plain statement that no guard pins this and why that was the right call. Do
    not write "guarded" if nothing guards it.

    **3. Traceability.** In the Traceability table, change the `DEBT-04 | 17` row and
    the `CORE-01 | 17` row from their current status to `Complete`, matching the
    other fourteen rows exactly in spacing and pipe placement. These are the last two
    open rows in the table.

    **4. The `## Resolution` backfill — low priority, non-blocking.** Commit
    `a2835a5` closed both of the named todos with a pure `git mv` (0 insertions, 0
    deletions), so neither carries the `## Resolution` section 30 of the 35 completed
    todos have. No test enforces it and no success criterion depends on it; it is
    included because this is the milestone close, the record stops being editable in
    passing after it, and `/gsd-audit-milestone` runs immediately after this phase.
    Append a `## Resolution` section to each, matching the shape used by the existing
    30: what the disposition was (both `fixed`, via promotion to a Phase 16
    requirement that then shipped), which requirement and phase closed it (`PKG-01`
    for the payload relocation, `PKG-03` for the stale phase pointers), and a pointer
    to the closing evidence in `.planning/phases/16-packaging-and-repo-shape/`. Keep
    each to a short paragraph — this is backfilled record-keeping, not new analysis,
    and inventing detail that was not established at the time would be worse than
    the gap it fills.
  </action>
  <verify>
    <automated>grep -cE '^\| [A-Z]+-[0-9]+ \| [0-9.]+ \| Complete \|$' .planning/REQUIREMENTS.md | grep -qx 16 && grep -qE '^- \[x\] \*\*DEBT-04\*\*' .planning/REQUIREMENTS.md && grep -qE '^- \[x\] \*\*CORE-01\*\*' .planning/REQUIREMENTS.md && grep -q 'Closure note (Phase 17' .planning/REQUIREMENTS.md && cd src/mcp/vice && node --test docs-deferred-ledger.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^\| [A-Z]+-[0-9]+ \| [0-9.]+ \| Complete \|$' .planning/REQUIREMENTS.md` reports exactly `16`. It reports `14` before this task.
    - `grep -cE '^\| [A-Z]+-[0-9]+ \| [0-9.]+ \|' .planning/REQUIREMENTS.md` also reports `16` — the total Traceability data-row count equals the Complete-row count, so no row is left in any other state.
    - `grep -cE '^- \[x\] \*\*DEBT-04\*\*' .planning/REQUIREMENTS.md` reports `1` and `grep -cE '^- \[ \] \*\*DEBT-04\*\*' .planning/REQUIREMENTS.md` reports `0`.
    - `grep -cE '^- \[x\] \*\*CORE-01\*\*' .planning/REQUIREMENTS.md` reports `1` and `grep -cE '^- \[ \] \*\*CORE-01\*\*' .planning/REQUIREMENTS.md` reports `0`.
    - `grep -c 'Closure note (Phase 17' .planning/REQUIREMENTS.md` reports `2`.
    - `grep -c 'docs-deferred-ledger.test.ts' .planning/REQUIREMENTS.md` reports at least `1` — DEBT-04's note names the guard that pins it.
    - `grep -cE '2 → 0|2 -> 0' .planning/REQUIREMENTS.md` reports at least `1` — the count transition is stated, not just the final figure.
    - `grep -c 'blocking-human' .planning/REQUIREMENTS.md` reports at least `2` — FORK-01's existing mention plus CORE-01's new one.
    - Backfill item: `grep -c '^## Resolution' .planning/todos/completed/2026-08-20-relocate-plugin-payload-under-src-and-merge-mcp-json.md` reports `1`, and the same for the 2026-08-21 file — or the SUMMARY records the item as deliberately skipped with a reason. Either outcome is acceptable; a silent omission is not.
    - `cd src/mcp/vice && node --test docs-deferred-ledger.test.ts` exits 0 — plan 17-01's ledger work is intact after these edits.
  </acceptance_criteria>
  <done>Both phase requirements are ticked with closure notes that name their evidence and what pins them; the Traceability table has no open row; the two `## Resolution`-less todo closures are backfilled or their skip is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Bring ROADMAP.md's Phase 17 section, Progress row and consequence callout to their real values</name>
  <reversibility rating="reversible">A planning-document edit; reverting restores the prior text with no dependent artifact.</reversibility>
  <read_first>
    - `.planning/ROADMAP.md` → the Phase 17 section (Goal, Depends on, the Fork-decision consequence block at lines 473-482, the planning note, Requirements, Success Criteria, and the `**Plans**: TBD` line at line 494) — the region being modified.
    - `.planning/ROADMAP.md` → the Phase 16 closure block (lines 382-457) — the freshest analog for filling in a `TBD`, writing a plans list with wave structure, and adding outcome Notes.
    - `.planning/ROADMAP.md` → the Progress table (the `17. Project Identity and Ledger Close | v0.4.0 | 0/TBD | Not started | -` row) and the `**v0.4.0 in progress:**` paragraph at lines 537-547.
    - `.planning/phases/17-project-identity-and-ledger-close/17-01-SUMMARY.md` and `17-02-SUMMARY.md` — the real outcomes to summarise.
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
    Four scoped edits inside ROADMAP.md. Use targeted replacements; do not rewrite
    the file, and do not touch any phase section other than 17's.

    **1. The `**Plans**: TBD` line** (line 494) becomes the real count in Phase 16's
    format, `**Plans**: 3/3 plans executed`, followed by a `Plans:` list in the same
    style Phase 16 uses — wave headings, `- [x]` entries, one imperative, colon-free
    one-line summary each. Three entries, matching the plans as executed:
    17-01 as the tracer (the guard's zero-pending fix plus the STATE.md ledger
    correction), 17-02 as the human-gated CORE-01 verdict, 17-03 as the closer.
    Include the wave numbers (1, 2, 3) so the sequential dependency chain is visible;
    the phase ran sequentially on the main checkout with worktrees off, so plan order
    and wave order coincide here.

    **2. Outcome Notes** for Phase 17, in the same voice Phases 12-16 use. Three
    things belong in them and would be lost otherwise: that the ROADMAP's own Phase
    17 Goal claim "This phase edits planning documents only and touches no source"
    was **false and is corrected here**, refuted by a live failing test run rather
    than by inference — `docs-deferred-ledger.test.ts` was red at 1 pass / 3 fail
    because the pending tree reached zero and the guard could not express that state,
    and fixing it was real source work and this phase's tracer; the CORE-01 verdict
    and that a human took it at a blocking checkpoint; and the true-close ledger count
    with its derivation. Correct the Goal's "touches no source" clause in place rather
    than only contradicting it in the Notes — a stale claim left standing in the Goal
    is exactly the drift this project builds guards against.

    **3. The Progress table row.** `| 17. Project Identity and Ledger Close | v0.4.0 |
    0/TBD | Not started | - |` becomes `3/3`, `Complete`, and `2026-08-23`, matching
    the Phase 16 row's formatting exactly.

    **4. Two stale-phase-pointer corrections**, both factual errors about this phase
    and its neighbour, in the `**v0.4.0 in progress:**` paragraph at lines 537-547.
    It currently reads that the packaging move is Phase 17 and that `PKG-01` (Phase
    17) is sequenced last "see Phase 17's Goal for the full rationale", and that
    `DEBT-04`'s ledger measurement and `CORE-01` are "(Phase 16)". Those are
    transposed: the packaging move and `PKG-01` are **Phase 16**, and `DEBT-04` /
    `CORE-01` are **Phase 17**. Fix all four references so they name the right phase.
    This is the same defect class `PKG-03` swept out of source comments one phase
    ago, and leaving it in the roadmap paragraph that describes this very phase would
    be a poor way to close the milestone. Change nothing else in that paragraph — in
    particular, leave the `**v0.4.0 in progress:**` opener and the "16/16 in-scope
    requirements mapped" figure alone; rewriting that paragraph into a final-state
    paragraph is `/gsd-complete-milestone`'s job, not this plan's.

    **5. The Fork-decision consequence callout** at lines 473-482 already carries a
    "(resolved 2026-08-22, Phase 14 plan 14-05)" heading and ends by stating that its
    21 → 20 figure is **not** the true-close count criterion 2 requires, because
    Phases 15 and 16 still had work outstanding. That forward-looking sentence is now
    answerable. Append one closing sentence recording the full chain to its terminus —
    21 (Phase 14 open) → 20 (14-05) → 2 (Phase 15 close) → 0 (measured Phase 17 plan
    17-01, after Phase 16 discharged `PKG-01` and `PKG-03`) — so the block reads as
    closed rather than as an open forward reference. Follow the "(resolved [date],
    Phase [N] plan [N-NN])" convention the file already uses.
  </action>
  <verify>
    <automated>grep -q '^\*\*Plans\*\*: 3/3 plans executed' .planning/ROADMAP.md && ! grep -q '^\*\*Plans\*\*: TBD' .planning/ROADMAP.md && grep -qE '^\| 17\. Project Identity and Ledger Close \| v0\.4\.0 \| 3/3 \| Complete' .planning/ROADMAP.md && cd src/mcp/vice && node --test docs-dangling-refs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^\*\*Plans\*\*: TBD' .planning/ROADMAP.md` reports `0`.
    - `grep -c '^\*\*Plans\*\*: 3/3 plans executed' .planning/ROADMAP.md` reports `1`.
    - `grep -cE '^\| 17\. Project Identity and Ledger Close \| v0\.4\.0 \| 3/3 \| Complete +\| 2026-08-23 \|$' .planning/ROADMAP.md` reports `1`.
    - `grep -c '17-01-PLAN.md' .planning/ROADMAP.md` reports at least `1`, and the same for `17-02-PLAN.md` and `17-03-PLAN.md` — all three plans are listed.
    - The refuted claim is corrected in the Goal itself, not merely contradicted elsewhere: `sed -n '/^\*\*Goal\*\*: PROJECT/,/^\*\*Depends on\*\*/p' .planning/ROADMAP.md | grep -c 'touches no source'` reports `0`. This gate is deliberately region-scoped to the Phase 17 Goal block rather than file-wide, because the outcome Notes legitimately quote the retired claim while recording that it was refuted.
      <!-- planner-discipline-allow: touches no source -->
      <!-- planner-region-allow: touches no source -->
    - All four phase attributions in the `v0.4.0 in progress` paragraph name the right phase. Check each against the paragraph flattened to one line with `PARA=$(sed -n '/v0.4.0 in progress/,/^$/p' .planning/ROADMAP.md | tr '\n' ' ')`, then: `printf '%s' "$PARA" | grep -cE 'packaging move \(Phase 16\)'` reports `1`; `printf '%s' "$PARA" | grep -cE 'PKG-01[^(]{0,4}\(Phase 16\)'` reports `1`; `printf '%s' "$PARA" | grep -cE "Phase 16's Goal"` reports `1`; `printf '%s' "$PARA" | grep -cE 'CORE-01[^(]{0,4}\(Phase 17\)'` reports `1`.
    - `cd src/mcp/vice && node --test docs-dangling-refs.test.ts` exits 0 — no new dangling phase pointer was introduced by these edits.
    - `cd src/mcp/vice && node --test docs-linerefs.test.ts` exits 0.
    - `git diff --name-only` lists exactly `.planning/ROADMAP.md`.
  </acceptance_criteria>
  <done>Phase 17's ROADMAP section states its real plan count, plan list and outcome notes; its Progress row reads Complete; the refuted "touches no source" claim is corrected at source; the four transposed phase attributions name the right phases; the fork-decision consequence block is closed to its terminus.</done>
</task>

<task type="auto">
  <name>Task 3: Advance STATE.md to the v0.4.0 close, re-take the pending measurement after this phase's own edits, and run the full suite</name>
  <precondition>`.planning/phases/17-project-identity-and-ledger-close/17-01-SUMMARY.md` and `17-02-SUMMARY.md` both exist and record their outcomes — this task summarises them and cannot be written from the plans alone. Assert with `ls .planning/phases/17-project-identity-and-ledger-close/17-0{1,2}-SUMMARY.md` before starting.</precondition>
  <reversibility rating="reversible">Planning-document content plus a test run; reverting restores the prior text.</reversibility>
  <read_first>
    - `.planning/STATE.md` → `## Current Position` (line 57), `### Decisions` (line 285), `## Deferred Items` (line 577, as edited by plan 17-01), `## Session Continuity` (line 786) and `## Operator Next Steps` (line 792) — the regions being modified.
    - `.planning/phases/17-project-identity-and-ledger-close/17-01-SUMMARY.md`, `17-02-SUMMARY.md` — the real outcomes.
    - `.planning/phases/17-project-identity-and-ledger-close/17-RESEARCH.md` → `## Hazards` item 2 — the structural reason this task's re-check exists.
    - `.planning/REQUIREMENTS.md` as edited by task 1, and `.planning/ROADMAP.md` as edited by task 2.
  </read_first>
  <files>.planning/STATE.md</files>
  <action>
    Four items, in this order. The re-check is the point of the task; the prose
    updates are around it.

    **1. Re-take the measurement, after every edit this phase has made.** Run
    `find .planning/todos/pending -name '*.md' | wc -l` and confirm it still reports
    0. This is not a repeat of plan 17-01's measurement — it is criterion 3's actual
    check, taken at the end of the last plan of the last phase of the milestone. If
    it reports anything other than 0, do NOT proceed to the prose updates as written:
    a new todo has been filed since the measurement, and STATE.md's `## Deferred
    Items` table, its "Current, as of Phase 17" paragraph and the `### Pending Todos`
    prose figure must all be corrected together in this same task — the three figures
    are required to agree and STATE.md itself records that the prose one has gone
    stale twice before. Then re-run `docs-deferred-ledger.test.ts` before continuing.

    **2. Name the backstop in the record.** Phase 17 is the last phase of v0.4.0 and
    `code_review` is enabled, so this phase's own post-execution review runs *after*
    this SUMMARY and there is no Phase 18 to disposition anything it files. State
    that explicitly in `## Operator Next Steps`, and name `/gsd-audit-milestone` —
    which runs next, and which `GATE-01` already blocks while any `docs-*.test.ts`
    guard is red — as the backstop for any finding filed after this count was taken.
    A reader who later finds a Phase 17 todo in `pending/` should be able to see from
    the record that this was anticipated, not missed.

    **3. Advance the prose.** `## Current Position` currently reads `Phase: 17 —
    Project Identity and Ledger Close / Plan: Not started / Status: Ready to plan`,
    followed by a Phase 16 narrative; replace the position lines with the completed
    state (3/3 plans, phase complete) and lead the narrative with Phase 17's own
    outcome — the guard fix and why it was needed, the ledger at 0, and the CORE-01
    verdict with its provenance — keeping the Phase 16 and Phase 15 narrative beneath
    as history. Append `- [Phase 17]: ...` bullets to `### Decisions` in the section's
    existing style, one for the CORE-01 verdict (naming the branch, that a human took
    it at a blocking-human checkpoint, and where it is recorded) and one for the
    guard's floor drop to zero and the conditional positive control. Update
    `## Session Continuity`'s `Stopped at:` line to the real state. Replace
    `## Operator Next Steps` wholesale: it currently opens "**Phase 15 (Debt and
    Review Disposition) is complete.**" and routes to Phases 16 and 17, both of which
    are now done. The new content should state that Phase 17 and the v0.4.0
    requirement set are complete, and route to `/gsd-audit-milestone` first, then
    `/gsd-complete-milestone`, with `/gsd-cleanup` as the optional archival step the
    prior milestone closes also left until last.

    **4. Run the full suite** as the phase gate: `cd src/mcp/vice && npm test`. Not
    `npm run test:automated` — it skips the nine `MANUAL_ONLY_TESTS` suites and has
    hidden CI failures on this project before. Record the counts in the SUMMARY the
    way Phase 16's close did (its baseline was 2386 tests, 2342 pass, 0 fail, 39
    skipped, 5 todo, 24 suites; expect a similar shape, plus five more tests if plan
    17-02 built the discretionary Core Value guard). Zero failures is the bar. If any
    `docs-*.test.ts` guard is red, the phase cannot be recorded complete — `GATE-01`
    forbids it — so fix or escalate rather than recording a partial pass.
  </action>
  <verify>
    <automated>test "$(find .planning/todos/pending -name '*.md' | wc -l)" -eq 0 && cd src/mcp/vice && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `find .planning/todos/pending -name '*.md' | wc -l` reports `0`, re-checked after this plan's own edits to REQUIREMENTS.md, ROADMAP.md and STATE.md — not carried over from plan 17-01.
    - `cd src/mcp/vice && npm test` exits 0 and its summary reports `# fail 0`. The command run is the full suite; `npm run test:automated` does not satisfy this criterion.
    - `cd src/mcp/vice && for f in docs-*.test.ts; do node --test "$f" || exit 1; done` exits 0 — every doc guard green, which is `GATE-01`'s condition for recording the phase complete.
    - `sed -n '/^## Current Position/,/^## Performance Metrics/p' .planning/STATE.md | grep -c '^Plan: Not started'` reports `0` — anchored to the position line itself, since the narrative beneath it may legitimately describe the prior state.
      <!-- planner-discipline-allow: Not started -->
    - `sed -n '/^## Current Position/,/^## Performance Metrics/p' .planning/STATE.md | grep -c 'Phase 17'` reports at least `1`.
    - `grep -c '^- \[Phase 17\]:' .planning/STATE.md` reports at least `2`.
    - `sed -n '/^## Operator Next Steps/,$p' .planning/STATE.md | grep -c 'gsd-audit-milestone'` reports at least `1`.
    - `sed -n '/^## Operator Next Steps/,$p' .planning/STATE.md | grep -c '^\*\*Phase 15 (Debt and Review Disposition) is complete'` reports `0` — the stale routing header is gone.
      <!-- planner-discipline-allow: Phase 15 (Debt and Review Disposition) is complete -->
    - `sed -n '/^## Session Continuity/,/^## Operator Next Steps/p' .planning/STATE.md | grep -ci 'ready to plan phase 17'` reports `0`.
    - `git diff --name-only` lists exactly `.planning/STATE.md`.
  </acceptance_criteria>
  <done>The pending set is confirmed empty by a measurement taken after every edit this phase makes; the full suite is green with zero failures and no doc guard red; STATE.md's position, decisions, continuity and next steps describe the v0.4.0 close and name the milestone audit as the backstop for anything filed after the count.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols this plan creates that do not exist in the codebase today — the
source-grounding pass should exclude these from drift verification:

- `.planning/REQUIREMENTS.md` → a new closure-note blockquote under DEBT-04
  opening `**Closure note (Phase 17, plans 17-01/17-03).**`
- `.planning/REQUIREMENTS.md` → a new closure-note blockquote under CORE-01
  opening `**Closure note (Phase 17, plans 17-02/17-03).**`
- `.planning/ROADMAP.md` → a new Phase 17 `Plans:` list with three `- [x]` entries
  and wave headings, and a new Phase 17 outcome Notes block
- `.planning/ROADMAP.md` → a new closing sentence on the Fork-decision consequence
  callout recording the 21 → 20 → 2 → 0 chain to its terminus
- `.planning/STATE.md` → new `- [Phase 17]:` bullets in `### Decisions`
- `.planning/STATE.md` → a rewritten `## Operator Next Steps` section routing to
  `/gsd-audit-milestone` and `/gsd-complete-milestone`
- `.planning/todos/completed/2026-08-20-relocate-plugin-payload-under-src-and-merge-mcp-json.md`
  → a new `## Resolution` section
- `.planning/todos/completed/2026-08-21-stale-phase-pointers-in-stock-cia-and-stock-dispatch-comments.md`
  → a new `## Resolution` section

Sibling plans in this phase produce further new symbols; see `17-01-PLAN.md` and
`17-02-PLAN.md` for those — including `src/mcp/vice/docs-core-value-decision.test.ts`,
which is conditional and may legitimately not exist.
</artifacts_this_phase_produces>

<probe_coverage>
No SPEC exists for this phase, so the spec-less probe fallback is active.

**Edges.** All 6 applicable rows in the fallback report are carried by the two
sibling plans that own the requirements they belong to: 5 DEBT-04 rows in
`17-01-PLAN.md` (4 resolved-explicit, 1 resolved-backstop) and 1 CORE-01 row in
`17-02-PLAN.md` (unresolved, surfaced as a flagged assumption). This plan
introduces no new probe rows and drops none.

**Phase-level no-silent-drop equality:** 6 probe-surfaced edges == 4 authored as
plain `must_haves.truths` strings + 1 authored as a structured
`{statement, verification: backstop}` marker + 1 surfaced as an explicit flagged
assumption. 0 dismissed, 0 dropped.

**Prohibitions.** 2 kept here, closure-scoped and spanning both requirements, both
authored descriptor-less (no `check_*` scalar) so each disposes flagged-unverified.
Phase-level roll-up: 6 kept across the three plans — 2 ledger-integrity
(`17-01`), 2 decision-provenance (`17-02`), 2 closure-honesty (here). Candidates
dropped as routine engineering across the phase: "must not break Markdown table
alignment", "must not leave a stale plan reference", "must not commit with a
dirty working tree". No canon-referral drops — none of the kept items is an
OWASP/GDPR-class constraint another tool already owns.
</probe_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this phase's own diff → the count it reports | The one genuinely adversarial boundary. The closure count is taken by the same run that produces the diff a later code review will scrutinise. A finding filed by that review lands *after* the count, on the far side of a boundary this phase cannot see across. |
| planning record → milestone audit | `/gsd-audit-milestone` runs next and reads these documents as the authoritative statement of what v0.4.0 delivered. Integrity of that statement, not confidentiality, is the asset. |

No new attack surface. RESEARCH.md's `## Security Domain` assessed ASVS
V2/V3/V4/V5/V6 as not applicable and nothing in this plan changes that: no auth,
no session, no access control, no external or user-controlled input, no
cryptography. The only executable this plan runs is the project's own test suite.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-08 | Repudiation | the DEBT-04 close count | medium | mitigate | A count taken before this phase's own review runs is stale by whatever that review files, and with no Phase 18 to inherit it, nothing downstream in the milestone catches the drift. Mitigated by task 3's re-check taken after every edit this phase makes, plus an explicit named backstop (`/gsd-audit-milestone`, itself gated by `GATE-01` while any guard is red) recorded in `## Operator Next Steps` so a later reader can see the residual window was known rather than missed. |
| T-17-09 | Tampering | `.planning/REQUIREMENTS.md` closure record | medium | mitigate | A requirement ticked Complete on the strength of activity rather than evidence corrupts the record the milestone audit trusts. Mitigated by acceptance criteria that require each closure note to name its guard (or state plainly that none exists), to state the `2 → 0` transition rather than only the final figure, and to close DEBT-01's outstanding forward reference by name. |
| T-17-10 | Denial of service | the phase gate | low | accept | The full `npm test` run is roughly two minutes and roughly 2386 tests. Substituting the faster `npm run test:automated` would skip nine suites and has hidden CI failures on this project before, so the slower gate is accepted deliberately rather than optimised away. |
| T-17-11 | Information disclosure | planning documents | low | accept | Every file touched is a repo-local planning document already committed to a public repository. Nothing sensitive is written; the closure notes cite plan numbers, commits and test names only. Accepted with no control. |
| T-17-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages and adds no dependencies; RESEARCH.md's `## Package Legitimacy Audit` records the gate as not applicable, so no install task exists for a legitimacy checkpoint to precede. Accepted on the grounds that the supply chain is untouched. |

No threat is rated `high`, so nothing here trips `security_block_on: high`.
</threat_model>

<verification>
1. `grep -cE '^\| [A-Z]+-[0-9]+ \| [0-9.]+ \| Complete \|$' .planning/REQUIREMENTS.md`
   reports `16` and the total Traceability data-row count is also `16`.
2. `find .planning/todos/pending -name '*.md' | wc -l` reports `0`, re-taken after
   all of this phase's edits.
3. `cd src/mcp/vice && npm test` — 0 failures across the full suite.
4. `cd src/mcp/vice && for f in docs-*.test.ts; do node --test "$f" || exit 1; done`
   — every doc guard green, satisfying `GATE-01`.
5. ROADMAP.md's Phase 17 section and Progress row read as a completed phase, and
   the refuted "touches no source" claim is corrected at source.
6. `## Operator Next Steps` routes to `/gsd-audit-milestone` and names it as the
   backstop for post-count findings.
</verification>

<success_criteria>
- DEBT-04 and CORE-01 are both closed with notes that name their evidence and
  what pins them, and no v0.4.0 requirement remains open in the Traceability table.
- The deferred-items count of 0 was confirmed with no phase of this milestone left
  to run and no edit of this phase's own still outstanding — criterion 3 satisfied
  by observation, with the residual post-review window named and backstopped.
- The full test suite is green and no `docs-*.test.ts` guard is red, so the phase
  can be recorded complete without violating `GATE-01`.
- ROADMAP.md and STATE.md describe the phase and the milestone position as they
  actually are, including the correction of the ROADMAP's own false "touches no
  source" claim.
</success_criteria>

<output>
Create `.planning/phases/17-project-identity-and-ledger-close/17-03-SUMMARY.md` when done.
</output>
