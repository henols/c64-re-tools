---
phase: 17-project-identity-and-ledger-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/vice/docs-deferred-ledger.test.ts
  - .planning/STATE.md
autonomous: true
requirements: [DEBT-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`.planning/todos/pending/` holds zero `.md` files at the moment the ledger count is taken, and the resulting count of 0 is strictly below the frozen 19-item v0.3.0 inheritance baseline."
    - "With zero pending todos, `cd src/mcp/vice && node --test docs-deferred-ledger.test.ts` reports 4 passing tests and 0 failures: direction A vacuously clean, direction B clean, the non-vacuity test green against a zero pending floor with the completed-todo floor of 5 unchanged, and the planted-violation test still firing on synthetic input."
    - "STATE.md's `## Deferred Items` prose count figure, that section's ledger table row count, and `### Pending Todos`'s own prose count figure all read 0 and agree exactly with one another."
    - "No assertion in the fixed guard depends on the enumeration order `readdirSync` returns: both predicates are compared against the empty array in the real-tree tests, and the planted-violation test compares single-element arrays only."
    - statement: "Every count figure this plan writes into STATE.md is derived from the `.md` file count of `.planning/todos/pending/` and states its arithmetic in the same sentence, rather than being hand-typed as a bare final number."
      verification: backstop
    - "STATE.md's `## Deferred Items` section states plainly which categories the 0 figure excludes by the section's own accounting rule — the still-`Deferred` rows of `### Carried forward from earlier closes` and the carried WR-class review findings — so 0 cannot be misread as zero known debt."
  prohibitions:
    - statement: "MUST NOT lower the ledger count by moving, renaming, or deleting a todo file that is not genuinely resolved — the count is only meaningful because every removal from `.planning/todos/pending/` was earned by a fix, a recorded `wont-fix`, or a promotion with a named owner."
      category: values
      status: resolved
      verification: judgment
    - statement: "MUST NOT disable, delete, skip, `--test-skip-pattern` away, or weaken into a tautology any assertion in `docs-deferred-ledger.test.ts` in order to make the ledger report green — the guard's own header records that an unsatisfiable guard gets switched off, and a guard switched off to produce a milestone-close number is the exact failure AUDIT-04 exists to stop."
      category: transparency
      status: resolved
      verification: judgment
  artifacts:
    - path: "src/mcp/vice/docs-deferred-ledger.test.ts"
      provides: "A non-vacuity test that tolerates a genuinely empty pending tree, with the positive control conditional on a pending todo existing"
      contains: "non-vacuity"
    - path: ".planning/STATE.md"
      provides: "The v0.4.0-close deferred-items ledger, derived and reading 0 items"
      contains: "Current, as of Phase 17"
  key_links:
    - from: ".planning/todos/pending/"
      to: ".planning/STATE.md"
      via: "docs-deferred-ledger.test.ts derives the Deferred Items section from the todo tree in both directions"
      pattern: "Deferred Items"
---

<objective>
Make the deferred-items ledger tell the truth at the v0.4.0 close, and make the
mechanical guard that enforces it able to express that truth.

Purpose: DEBT-04's criterion is not "the number is small" — it is "the number is
still *derived* and still *guarded*, and it is lower than 19". Both halves are
currently broken in different ways. `.planning/todos/pending/` reached **0 files**
for the first time in this project's history when Phase 16 discharged the
payload-relocation todo, and `src/mcp/vice/docs-deferred-ledger.test.ts` is
**RED** (1 pass / 3 fail) as a direct result: its non-vacuity floor asserts at
least two pending todos exist, and its positive control hard-codes a stem that
has since moved to `.planning/todos/completed/`. Independently, STATE.md's
`## Deferred Items` section still narrates both of Phase 16's discharged todos as
Pending. ROADMAP.md's claim that this phase "touches no source" is therefore
false, refuted by a live test run — the guard is real, reviewable source work and
it is this phase's tracer.

Output: a guard that goes green on an empty pending tree without losing its
non-vacuity teeth, and a STATE.md ledger that reads 0 with its arithmetic shown.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-project-identity-and-ledger-close/17-RESEARCH.md
@.planning/phases/17-project-identity-and-ledger-close/17-PATTERNS.md
@src/mcp/vice/docs-deferred-ledger.test.ts
</context>

<execution_notes>
**Worktree mode is OFF for this phase — stated explicitly, not inherited.**
`.planning/config.json` sets `workflow.use_worktrees: false` and ROADMAP.md
carries an explicit planning note to the same effect. This plan's deliverable is
`.planning/STATE.md` *content*; worktree mode strips `.planning/` files from
commits, and a plan of this shape silently cannot deliver under it. Do not enable
worktrees for this plan under any circumstance.

**The full `cd src/mcp/vice && npm test` is the phase gate, not
`npm run test:automated`.** The automated subset skips the nine
`MANUAL_ONLY_TESTS` suites and has hidden CI failures before. `GATE-01` (Phase 12)
separately forbids recording `status: passed` while any `docs-*.test.ts` guard is
red, so the ledger guard going green is a phase-completion precondition and not
merely a task criterion. The full suite is run by plan 17-03 (the closer); this
plan runs the fast per-task guard command.

**No nested headless sessions.** No task here may invoke `claude -p` or any
nested headless agent session.

**Do NOT update the frozen 19-item baseline citations.** `ROADMAP.md:533`,
`PROJECT.md:165`, `PROJECT.md:485` and `MILESTONES.md:11` all cite "19" as the
v0.3.0 *close-time* snapshot. Those are historical records of a past milestone
close, not a second live ledger. Leave every one of them exactly as written.
RESEARCH.md confirmed by grep that no second live copy of the ledger exists
anywhere in `.planning/`.

<!-- planner-discipline-allow: 2026-08-20-relocate-plugin-payload-under-src-and-merge-mcp-json -->
<!-- planner-discipline-allow: 2026-08-21-stale-phase-pointers-in-stock-cia-and-stock-dispatch-comments -->
</execution_notes>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — make the ledger guard able to express a zero-pending tree, end to end</name>
  <reversibility rating="reversible">Two assertions in one test file; a revert is a single-commit change with no dependent contract.</reversibility>
  <read_first>
    - `src/mcp/vice/docs-deferred-ledger.test.ts` (full file, 180 lines — the file being modified; read the whole thing, the running comment history is the convention you must extend rather than replace)
    - `.planning/phases/17-project-identity-and-ledger-close/17-PATTERNS.md` → the `docs-deferred-ledger.test.ts` section, which quotes the exact block that must change and the "doc-guard family conventions" the edit must match
    - `.planning/phases/17-project-identity-and-ledger-close/17-RESEARCH.md` → `## Ledger mechanics` → "Recommended fix shape"
    - `src/mcp/vice/docs-fork-decision.test.ts` (header comment only, lines 1-16 — the shared `WHY THIS EXISTS` voice for this guard family)
  </read_first>
  <files>src/mcp/vice/docs-deferred-ledger.test.ts</files>
  <action>
    This is the phase's tracer: it proves the one mechanism every other DEBT-04 task
    depends on — that the guard can report green against a genuinely empty pending
    tree — before any documentation edit is made. Touch ONLY this file in this task.
    Do NOT edit `.planning/STATE.md` here; task 2 owns that, and the tracer's whole
    value is that it is verified in isolation.

    Edit the fourth test, named `non-vacuity: the Deferred Items section is located,
    non-empty, and the scanned sets clear a floor` (currently at lines 112-140), and
    nothing else in the file. Two changes:

    1. The pending floor. The assertion `pending.length >= 2` with message
    `expected at least 2 pending todos, got ${pending.length}` is now structurally
    unsatisfiable: zero pending todos is a legitimate, intended, earned state and
    is precisely what DEBT-04's own success criterion asks the project to reach.
    Drop the pending floor to zero — either remove that single assertion outright or
    restate it as a floor of 0 — while leaving `completed.length >= 5` (currently 35)
    and both of the section-located / section-non-empty assertions above it
    completely unchanged. Those three are what keep the test non-vacuous now that
    the pending floor no longer can be.

    2. The positive control. The block introduced by the comment beginning
    `Positive control:` hard-codes `knownStem` as the literal
    `2026-08-20-relocate-plugin-payload-under-src-and-merge-mcp-json` and asserts
    both that `pending.includes(knownStem)` and that `missingPendingStems([knownStem],
    section)` finds it in the real section text. That stem now lives in
    `.planning/todos/completed/`, so both assertions are false. Take RESEARCH.md's
    option (a), the smaller fix that keeps faith with what a positive control is for:
    make the whole control block conditional on `pending.length > 0`, selecting the
    control stem from the live `pending` array rather than hard-coding a filename,
    so the control asserts "when something pending exists, predicate 1's own matcher
    actually finds it" and is skipped only when there is genuinely nothing to control
    against. Do NOT manufacture a synthetic stem here — the planted-violation test at
    lines 142-179 already owns the synthetic-input proof and must be left untouched.

    Extend the existing floor-history comment (lines 120-127) with one further
    sentence in the same narrating voice it already uses for phases 15-10 and 15-12,
    naming Phase 17 / DEBT-04, recording that the pending tree reached empty for the
    first time and that the positive control became conditional as a consequence.
    The prior author's own instruction sits in that comment — lower rather than
    raise — so follow it and record that you did, rather than silently rewriting the
    history block. Match the file's existing style: 2-space indent, double quotes,
    `.ts` extensions on relative imports, and assertion messages that name the likely
    cause rather than restating the assertion.
  </action>
  <verify>
    <automated>cd src/mcp/vice && node --test --test-name-pattern='non-vacuity' docs-deferred-ledger.test.ts && node --test --test-name-pattern='direction A' docs-deferred-ledger.test.ts && ! node --test --test-name-pattern='direction B' docs-deferred-ledger.test.ts && ! node --test --test-name-pattern='planted violation' docs-deferred-ledger.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && node --test --test-name-pattern='non-vacuity' docs-deferred-ledger.test.ts` exits 0. It exits 1 before this task; that transition is the tracer's proof.
    - `cd src/mcp/vice && node --test --test-name-pattern='direction A' docs-deferred-ledger.test.ts` exits 0 (unchanged from before this task).
    - `cd src/mcp/vice && node --test --test-name-pattern='direction B' docs-deferred-ledger.test.ts` exits 1 — still red, because STATE.md has not been touched yet. A green direction B at this point means this task changed something it should not have.
    - `cd src/mcp/vice && node --test --test-name-pattern='planted violation' docs-deferred-ledger.test.ts` exits 1 — still red for the same STATE.md-text cause (its closing `realStale` assertion reads the real file). It goes green in task 2, not here.
    - `cd src/mcp/vice && node --test docs-deferred-ledger.test.ts 2>&1 | grep -c '^ok '` reports `2`, and the same run's `grep -c '^not ok '` reports `2`. The whole-file run still exits non-zero at this point and that is the expected, correct state.
    - `git diff --name-only` lists exactly `src/mcp/vice/docs-deferred-ledger.test.ts` and no other path.
    - `grep -c 'DEBT-04' src/mcp/vice/docs-deferred-ledger.test.ts` is at least 2 — the pre-existing forward reference plus the new history sentence this task adds.
    - `cd src/mcp/vice && npx tsc --noEmit -p tsconfig.json` exits 0.
  </acceptance_criteria>
  <done>The non-vacuity test passes against an empty `.planning/todos/pending/`; direction B and the planted-violation test are still red for the STATE.md-text reason task 2 fixes; nothing outside the guard file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Bring STATE.md's deferred-items ledger to the true v0.4.0-close count of 0, table and prose in one edit</name>
  <precondition>`.planning/todos/pending/` contains zero `.md` files — Phase 16 has discharged the payload-relocation todo (PKG-01) and the stale-phase-pointers todo (PKG-03), and no later todo has been filed. Assert with `find .planning/todos/pending -name '*.md' | wc -l` before editing; if it is non-zero, halt — the count this task writes would be wrong and the narrative below would need rewriting rather than adjusting.</precondition>
  <reversibility rating="reversible">A prose and table edit in one planning document; reverting restores the prior text with no dependent artifact.</reversibility>
  <read_first>
    - `.planning/STATE.md` → `## Deferred Items` (starts at line 577, runs to `## Session Continuity`) — the file being modified. Read the WHOLE section; it is long, layered history and you must edit inside it without flattening it.
    - `.planning/STATE.md` → `### Pending Todos` (starts at line 398, runs to `### Quick Tasks Completed`) — the second half of this task's single atomic edit.
    - `src/mcp/vice/docs-deferred-ledger.test.ts` (as fixed by task 1) — specifically `deferredItemsSection()` and `wronglyListedCompletedStems()`, so you know the guard does a substring match over the raw section text and not a structured table parse.
    - `.planning/phases/17-project-identity-and-ledger-close/17-PATTERNS.md` → the `.planning/STATE.md` section, which quotes the current text verbatim and states the house convention for the replacement paragraph.
    - `.planning/phases/17-project-identity-and-ledger-close/17-RESEARCH.md` → `## The true count` and `## Hazards` items 5 and 6.
  </read_first>
  <files>.planning/STATE.md</files>
  <action>
    One task, one commit, both sections — STATE.md itself records that the
    `### Pending Todos` prose figure "has gone stale twice before and is not itself
    guarded", and Phase 15's own cross-cutting constraint requires the table and both
    prose figures to move together. Splitting this across two edits is the exact
    recurrence this discipline exists to prevent.

    There are four literal occurrences of the two discharged stems inside the
    `## Deferred Items` section, and predicate B is a substring match over the whole
    section text, so all four must go:

    - Line 617 and line 629 sit inside the historical narrative that enumerates the
      18-item set. Each is a bare backticked filename stem, unlike every sibling
      entry in the same sentence, which is described in prose and followed by its
      resolution. Rewrite both in that sibling style: describe the item and name its
      closure without using the filename stem. For line 617, the plugin-payload
      relocation under `src/` with the `.mcp.json` merge, resolved by Phase 16 as
      `PKG-01`. For line 629, the stale phase pointers in `stock-cia.ts` and
      `stock-dispatch.ts` comments, resolved by Phase 16 as `PKG-03`. Both should
      point at `.planning/todos/completed/` for the record, exactly as their
      siblings in the same sentences already do.
    - Lines 739 and 740 are the two remaining data rows of the ledger table. Delete
      both rows. Keep the table's header row and its separator row, and add a single
      italic line immediately under the (now data-row-free) table reading that the
      ledger is empty at the v0.4.0 close and the header is retained deliberately so
      a reader can see the table was emptied rather than truncated.

    Replace the paragraph currently beginning `**Current, as of phase 15 plan 15-12
    Task 3, the phase's final reconciliation (2026-08-22): 2 items` with the Phase 17
    equivalent, following the section's own established convention exactly (a bolded
    `**Current, as of ... (date): N items ...**` opener, the accounting rule restated,
    and the arithmetic spelled out in full rather than a bare final number). It must
    carry, concretely: `Phase 17 plan 17-01 Task 2 (2026-08-23)`; `0 items — 0 pending
    todos, zero UAT gaps`; the arithmetic `0 pending todo files in
    .planning/todos/pending/ + 0 UAT-gap rows = 0`; the statement that this is the
    first time the tree has been empty; that the count is derived from the todo tree
    and guarded in both directions by `docs-deferred-ledger.test.ts`, whose
    non-vacuity floor was lowered to zero by this phase's own plan 17-01 task 1
    because the previous floor of two could not express this state; and the running
    `before → after` figure `2 → 0` in the same sentence that names the cause
    (Phase 16 discharging `PKG-01` and `PKG-03`), matching the section's own
    convention of never stating a count without its derivation.

    The paragraph MUST also state plainly what the 0 excludes, so it cannot be
    misread as zero known debt: the four still-`Deferred` rows in
    `### Carried forward from earlier closes`, naming five items (UP-01/UP-02
    share one row; then QUAL-01, QUAL-02, QUAL-03) and the roughly fifteen
    carried WR-class code-review findings are
    outside this table's accounting rule (pending todo files plus any open UAT-gap
    row) and are unchanged by this measurement. The guard's own header comment
    records that neither the `uat_gap` row nor the carried-forward table is scanned;
    say so rather than leaving the reader to infer it.

    In the same edit, update `### Pending Todos`. Its opening figure currently reads
    `2 pending (2 files in .planning/todos/pending/ + 0 UAT-gap rows = 2)` — make it
    read 0 with the same parenthesised arithmetic shape. Update the paragraph
    beginning `**As of phase 15 plan 15-12 Task 1 (2026-08-22), only two todos remain
    pending` and the closing paragraph beginning `The two todos that remain pending
    are` so that neither asserts anything is still pending; both promoted items are
    now `Complete` under Phase 16's `PKG-01` and `PKG-03` closures. Keep the standing
    caution sentence — that this prose figure must always equal the Deferred Items
    table's row count, has gone stale twice before, and is not itself guarded —
    verbatim in substance, updated to the new figure. Preserve the historical
    narrative of how the count moved across Phases 13/14/15; you are appending the
    Phase 16/17 terminus to that history, not deleting it.

    Note that `### Pending Todos` sits outside the guard's scanned section, so the
    two stems may legitimately still appear there in historical prose; the guard's own
    header comment records exactly this scoping decision. Do not chase them out of the
    rest of the file.
  </action>
  <verify>
    <automated>cd src/mcp/vice && node --test docs-deferred-ledger.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && node --test docs-deferred-ledger.test.ts` exits 0 and its summary reports `# pass 4` and `# fail 0`.
    - `find .planning/todos/pending -name '*.md' | wc -l` reports `0` (unchanged by this task — it measures, it does not move files).
    - `awk '/^## Deferred Items/,/^## Session Continuity/' .planning/STATE.md | grep -c 'Current, as of Phase 17'` reports at least `1`.
    - `awk '/^## Deferred Items/,/^## Session Continuity/' .planning/STATE.md | grep -cE '0 pending todo files.*\+ 0 UAT-gap rows = 0'` reports at least `1`.
    - `awk '/^## Deferred Items/,/^## Session Continuity/' .planning/STATE.md | grep -cE '^\| todo \|'` reports `0` — the ledger table has no remaining data rows.
    - `awk '/^## Deferred Items/,/^## Session Continuity/' .planning/STATE.md | grep -cE 'QUAL-01|UP-01'` reports at least `1` — the exclusions are named, not implied.
    - `sed -n '398,420p' .planning/STATE.md | grep -cE '^0 pending \(0 files'` reports `1`.
    - `git diff --name-only` lists exactly `.planning/STATE.md`.
    - `git diff --stat .planning/STATE.md` shows both the `## Deferred Items` region and the `### Pending Todos` region changed in this one diff — the two are never committed separately.
  </acceptance_criteria>
  <done>All four tests in `docs-deferred-ledger.test.ts` pass against a zero-pending tree; STATE.md's ledger reads 0 with its arithmetic and exclusions shown; the Deferred Items table and the Pending Todos prose figure were changed in the same edit and agree.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols this plan creates that do not exist in the codebase today — the
source-grounding pass should exclude these from drift verification:

- `.planning/STATE.md` → a new bolded paragraph inside `## Deferred Items`
  opening `**Current, as of Phase 17 plan 17-01 Task 2 (2026-08-23): 0 items ...**`
- `.planning/STATE.md` → a new italic line under the emptied ledger table stating
  the ledger is empty at the v0.4.0 close
- `src/mcp/vice/docs-deferred-ledger.test.ts` → a new sentence in the floor-history
  comment block naming Phase 17 / DEBT-04, and a `pending.length > 0` conditional
  wrapping the positive-control block (no new test names; the four existing test
  names are unchanged)

Sibling plans in this phase produce further new symbols; see
`17-02-PLAN.md` and `17-03-PLAN.md` for those.
</artifacts_this_phase_produces>

<probe_coverage>
No SPEC exists for this phase, so the spec-less probe fallback is active and the
edge/prohibition predicates below were authored into `must_haves` rather than
lifted from a SPEC.

**Edges — DEBT-04 rows, 5 of the report's 6 applicable items (the sixth is
CORE-01's and is carried by `17-02-PLAN.md`):**

| Category | Disposition | Where it lives |
|---|---|---|
| boundary | resolved (explicit) | `must_haves.truths[0]` — 0 against the 19 baseline, plus the guard's two remaining floors |
| empty | resolved (explicit) | `must_haves.truths[1]` — the zero-pending case, which is exactly why the guard is red today |
| adjacency | resolved (explicit) | `must_haves.truths[2]` — the three count figures must be exactly equal |
| ordering | resolved (explicit) | `must_haves.truths[3]` — `readdirSync` order cannot affect any assertion |
| precision | resolved (backstop) | `must_haves.truths[4]` — structured `{statement, verification: backstop}` marker; "derivation is shown, not hand-typed" cannot be fully confirmed mechanically, so it abstains to `human_needed` rather than silently passing |

**No-silent-drop equality for this plan:** 5 DEBT-04 probe rows surfaced == 5
authored into `must_haves.truths` (4 explicit + 1 backstop) + 0 dropped.

**Prohibitions:** 2 kept from the recall/precision pass, both authored
descriptor-less (no `check_*` scalar) so each disposes flagged-unverified.
Candidates dropped as routine engineering: "must not break the type check",
"must not leave the guard file's imports unused", "must not reorder unrelated
sections of STATE.md". No canon-referral drops — none of the kept items is an
OWASP/GDPR-class constraint another tool already owns.
</probe_coverage>

<assumption_delta_decision>
The `assumption-delta` detector fired `detected: true`, one `pluralization`
signal, on the term "another" in the phase goal's criterion 3 ("nothing
remaining that could remove **another** item from the pending set").

- **Primary noun:** the pending-todo item. Unchanged.
- **Decision:** `no-change`. The trigger is a counting statement about a set that
  is already empty, not the introduction of a second variant of any modelled
  noun. Nothing in this phase pluralizes a previously-singular concept: there is
  still exactly one live ledger (RESEARCH.md confirmed by grep that no second
  copy exists), one guard over it, and one accounting rule. The detector is
  deterministic, not infallible, and this is a prose false positive.
- No invariant test is proposed, because no generalization occurred to encode.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo filesystem → guard process | `docs-deferred-ledger.test.ts` reads `.planning/STATE.md` and the two todo directories via paths resolved through the existing `repo-root.ts` seam. Every input is repo-local and trusted; nothing crosses a network, process, or user boundary. |
| planning record → future reader | The only genuinely adversarial boundary in this plan is temporal: a milestone-close count is read later by `/gsd-audit-milestone`, by the next milestone's planning, and by anyone auditing whether v0.4.0's debt claim was honest. Integrity of that record, not confidentiality, is what is at stake. |

No new attack surface is created. RESEARCH.md's `## Security Domain` assessed
ASVS V2/V3/V4/V5/V6 as not applicable and this plan does not change that: no
auth, no session, no access control, no external or user-controlled input, no
cryptography.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering | `src/mcp/vice/docs-deferred-ledger.test.ts` | medium | mitigate | A guard weakened to produce a green milestone number is indistinguishable in the diff from a guard legitimately widened. Mitigated structurally: the tracer's acceptance criteria require the non-vacuity test to go red→green while direction B and the planted-violation test stay red, so a blanket weakening (which would green all four) fails the tracer's own gate. The `completed.length >= 5` floor, both section-located assertions, and the entire planted-violation test are explicitly out of scope for the edit. |
| T-17-02 | Repudiation | `.planning/STATE.md` → `## Deferred Items` | medium | mitigate | A count stated without its derivation cannot later be distinguished from a hand-typed figure. Mitigated by requiring the arithmetic, the `2 → 0` transition, the cause, and the named exclusions in the same paragraph, and by keeping the derivation guarded in both directions by a committed test. |
| T-17-03 | Information disclosure | guard filesystem reads | low | accept | The guard reads only `.planning/STATE.md` and two repo-local directory listings through the pre-existing `repo-root.ts` seam. No secret, no user input, no path from outside the repository. Accepted with no control. |
| T-17-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages and adds no dependencies; RESEARCH.md's `## Package Legitimacy Audit` records the gate as not applicable. No install task exists for a legitimacy checkpoint to precede. Accepted on the grounds that the supply chain is untouched. |

No threat is rated `high`, so nothing here trips `security_block_on: high`.
</threat_model>

<verification>
1. `cd src/mcp/vice && node --test docs-deferred-ledger.test.ts` — 4 pass, 0 fail.
2. `find .planning/todos/pending -name '*.md' | wc -l` — reports `0`.
3. `cd src/mcp/vice && npx tsc --noEmit -p tsconfig.json` — exits 0.
4. STATE.md's three count figures (the `## Deferred Items` prose figure, that
   section's ledger table row count, and `### Pending Todos`'s prose figure) all
   read 0 and agree — read them and confirm by eye; only the first two are
   mechanically guarded.

The full `cd src/mcp/vice && npm test` gate is run by plan 17-03, which is this
phase's designated closer.
</verification>

<success_criteria>
- `docs-deferred-ledger.test.ts` is green in both directions against a genuinely
  empty pending tree, with its non-vacuity teeth intact (completed floor, section
  located, section non-empty, planted violation all still asserting).
- STATE.md reports the v0.4.0-close deferred-items count as 0, strictly below the
  19 items inherited at the v0.3.0 close, with the derivation, the `2 → 0`
  transition, the cause, and the excluded categories all stated in the record.
- DEBT-04's criterion 2 — "still derived and guarded, and lower than 19" — is
  satisfiable mechanically from this point forward.
</success_criteria>

<output>
Create `.planning/phases/17-project-identity-and-ledger-close/17-01-SUMMARY.md` when done.
</output>
