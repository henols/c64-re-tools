---
phase: 17-project-identity-and-ledger-close
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - .planning/PROJECT.md
  - src/mcp/vice/docs-core-value-decision.test.ts
autonomous: false
requirements: [CORE-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "PROJECT.md's `## Core Value` section carries an ISO `YYYY-MM-DD` date. The section matches zero date patterns before this plan runs, so the presence of one is itself the evidence a decision was taken at a point in time rather than drifting in."
    - "The `## Core Value` section names, by literal string, at least one specific piece of evidence that was actually weighed — Phase 11's two-session sealed-question test, the symbol round trip, or the 17 curated `r2000_*` tools — the same way FORK-01's row is checkably required to name `KEYBOARD_MATRIX_SET` verbatim."
    - "The verdict recorded in `## Core Value` is the option a human selected at this plan's `gate=\"blocking-human\"` checkpoint, not one an executor picked while unattended."
    - "If the verdict is keep-as-is, the entry names a concrete, stated condition under which the question would be reopened, in the same voice as FORK-01's reversal clause; if the verdict is restate, the Core Value's leading statement itself names the outlives-the-session axis and no separate reversal clause is required."
    - "The dated entry lives inside `## Core Value` itself, not in `## Key Decisions` — ROADMAP.md criterion 1 places it there explicitly, and `docs-fork-decision.test.ts` is scoped exclusively to the Key Decisions table and must remain green and untouched."
    - statement: "The entry reads as evidence weighed rather than a bookkeeping edit made in passing — it engages with the case against the verdict it reached, not only the case for it."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT record the Core Value verdict as a bookkeeping edit — an entry that asserts a conclusion without naming the specific evidence weighed, or that weighs only the side it concluded for, is the precise failure `REQUIREMENTS.md`'s Out of Scope table names and must be rejected even though no test can catch it."
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT let an executor choose the restate-versus-keep verdict unilaterally. FORK-01, the directly analogous decision, is recorded as decided by a human at a blocking checkpoint, 'not inferred, not auto-approved'; auto-mode is active on this project and a `gate=\"blocking\"` decision would be auto-selected, which is why this checkpoint carries `gate=\"blocking-human\"`."
      category: values
      status: resolved
      verification: judgment
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "The dated CORE-01 verdict inside the Core Value section, with the evidence weighed named"
      contains: "## Core Value"
  key_links:
    - from: ".planning/PROJECT.md"
      to: ".planning/PROJECT.md"
      via: "the dated Core Value entry cites the Phase 11 evidence already recorded in the Requirements → Validated list (R2000-10's sealed-question test, R2000-14/R2000-15's symbol round trip)"
      pattern: "Phase 11"
---

<objective>
Settle CORE-01: decide, with a human in the loop, whether PROJECT.md's Core Value
is restated to name the axis v0.3.0 proved — that what a session learns outlives
it — or is deliberately kept as-is with a dated record of the evidence weighed.
Then write that verdict down in a form a later reader can tell apart from a
bookkeeping edit.

Purpose: the Core Value statement has been carried unchanged through two
milestone closes and is flagged in the file itself as "deliberately not rewritten
yet". REQUIREMENTS.md's Out of Scope table names the failure mode by name —
"Restating Core Value as a bookkeeping edit" — and the requirement permits either
verdict provided the evidence was actually weighed. The evidence is genuinely
balanced: RESEARCH.md found six concrete items for restating and five against,
and deliberately declined to resolve it. This is the phase's one judgment call.

Output: a dated, evidence-citing entry inside `## Core Value`, and — only on the
keep-as-is branch — a committed guard that stops it drifting back out.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-project-identity-and-ledger-close/17-RESEARCH.md
@.planning/phases/17-project-identity-and-ledger-close/17-PATTERNS.md
@src/mcp/vice/docs-fork-decision.test.ts
</context>

<execution_notes>
**Worktree mode is OFF for this phase — stated explicitly, not inherited.**
`.planning/config.json` sets `workflow.use_worktrees: false` and ROADMAP.md
carries an explicit planning note. This plan's primary deliverable is
`.planning/PROJECT.md` *content*; worktree mode strips `.planning/` files from
commits, so a plan of this shape silently cannot deliver under it.

**Auto-mode is active on this project** (`workflow._auto_chain_active: true`).
That is exactly why task 1 carries `gate="blocking-human"`: a `gate="blocking"`
decision checkpoint is auto-resolved to its first option under auto-mode, which
would reproduce the "silent carry" this requirement exists to end. This
checkpoint stops for a human in every mode.

**No nested headless sessions.** No task here may invoke `claude -p` or any
nested headless agent session.

**`docs-fork-decision.test.ts` must stay green and untouched.** It is scoped
exclusively to `## Key Decisions` and the FORK-01 row; adding text to
`## Core Value` does not affect it, and any change to that file in this plan's
diff is out of scope.
</execution_notes>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Decide the CORE-01 verdict — restate the Core Value, or keep it with a dated confirmation</name>
  <decision>
    Does PROJECT.md's `## Core Value` get **restated** to name the outlives-the-session
    axis v0.3.0 proved, or does it get **kept as-is** with a dated entry recording that
    the evidence was weighed and the statement deliberately retained?
  </decision>
  <context>
    This is a one-way door. It rewrites (or permanently dates) the project's
    published identity statement, at the last phase of the milestone, immediately
    before `/gsd-audit-milestone` and `/gsd-complete-milestone` archive the record.
    Undoing it later is not a revert — it is a second, contradicting dated entry.

    The directly analogous precedent, FORK-01, is recorded in `REQUIREMENTS.md:89-91`
    as decided "by a **human**, at plan 14-01's `gate=\"blocking-human\"` checkpoint,
    after explicit escalation (not inferred, not auto-approved)". CORE-01 gets the
    same treatment. `REQUIREMENTS.md`'s Out of Scope table names the failure mode to
    avoid in as many words: "Restating Core Value as a bookkeeping edit — `CORE-01`
    requires the evidence to be weighed. An edit made while opening the milestone
    would have been the third silent carry."

    The current text, for reference, is three paragraphs: the statement itself ("A
    Claude session can reliably drive a real C64 emulator to reverse-engineer a
    program ... and keep working when the emulator misbehaves"), a "*Still correct
    after v0.2.0*" note, and a "**Flagged at the v0.3.0 close, deliberately not
    rewritten yet**" paragraph which already contains a pre-drafted candidate
    restatement: "*a Claude session can drive a real C64 emulator to reverse-engineer
    a program, and what it learns outlives the session.*" The task is not to invent
    wording — it is to decide whether to adopt that candidate, adopt a variant, or
    explicitly decline it.

    **The evidence, both ways, verified item by item in RESEARCH.md's
    `## Core Value evidence` — you should not need to re-read the research to decide.**

    FOR restating (six items):
    1. `PROJECT.md:78` — `c64-program-recon` writes findings as queryable annotation
       state, "proven by the two-session falsifiability test — session B answered a
       question sealed before it existed, from store queries alone" (Phase 11,
       R2000-10). A genuinely independent second session answered a question it could
       not have known the answer to except by querying persisted state.
    2. `PROJECT.md:275` — the Key Decisions row calls that sealed-question test "the
       strongest evidence this milestone produced"; the canonical line hashed
       identically (`e64463d8…`).
    3. `PROJECT.md:81` — the symbol round trip (R2000-14/R2000-15): symbols export as
       VICE label files and names discovered live flow back, demonstrated as one
       closed loop against genuine unpatched stock `x64sc`, "with the inbound name's
       prior absence shown rather than claimed".
    4. `PROJECT.md:79-80` — 17 curated `r2000_*` tools let a later session query
       cross-references, labels and comments over a program another session analysed.
    5. `STATE.md:27-33` already states the open question plainly and calls restating
       or explicitly confirming it "a v0.4.0 target feature, not a bookkeeping edit".
    6. The annotation store is persistent by construction (a project file
       regenerator2000 owns, not session memory), and the generated memory map and
       `docs/tool-support.md` are themselves artifacts that outlive any one session.

    AGAINST restating (five items):
    1. The Core Value's actual subject is still exactly true and is still what every
       phase of all three milestones served. Nothing in v0.3.0 or v0.4.0 contradicts
       or narrows "reliably drive a real C64 emulator ... and keep working when it
       misbehaves".
    2. The outlives-the-session property belongs to a component **structurally
       incapable of touching VICE at all** — `PROJECT.md:82` (R2000-01) records the
       static-analysis backend is never launched with `--vice`, guarded in code.
       Folding it into a sentence otherwise entirely about live emulator driving
       conflates two genuinely separate subsystems.
    3. The project's own prior author flagged one milestone of evidence as thin
       ground (`PROJECT.md:47`), and `REQUIREMENTS.md:200` explicitly permits keeping
       the statement provided the evidence was weighed. Keeping is a legitimate
       verdict, not indecision.
    4. The pre-drafted candidate is a conjunction, not a replacement — it appends a
       second capability with "and" rather than naming which axis is primary.
    5. v0.4.0 produced **zero new evidence** on the question. Every item in the FOR
       column is Phase 11 (v0.3.0) evidence, re-weighed a milestone later.

    Whichever way this goes, task 2 records it with a date and names the evidence.
    Task 3 builds a guard only on the keep-as-is branch.
  </context>
  <options>
    <option id="restate">
      <name>Restate — adopt the outlives-the-session axis into the Core Value statement</name>
      <pros>Acts on the strongest evidence v0.3.0 produced (a falsifiable sealed-question test, not an assertion). Ends a flag that has been carried unamended through two closes. The new wording is its own evidence, so no separate guard is needed. Matches `STATE.md`'s own framing of this as a v0.4.0 target feature.</pros>
      <cons>Bundles two structurally separate subsystems — the live `vice_*` surface and the emulator-incapable `r2000_*` surface — into one sentence that arguably names neither cleanly. Rests entirely on one milestone of evidence, which the project's own prior author called thin ground. A Core Value is meant to name the ONE thing.</cons>
    </option>
    <option id="keep-dated">
      <name>Keep as-is — record a dated confirmation that the evidence was weighed and the statement deliberately retained</name>
      <pros>Honest about the fact that v0.4.0 produced no new evidence on the question. Keeps the Core Value naming a single, still-entirely-true primary axis. Explicitly permitted by CORE-01's own wording. Reuses FORK-01's proven shape — dated verdict, named evidence, named reversal condition.</pros>
      <cons>A third close with the statement unchanged; the entry must work hard to read as a decision rather than a deferral. Requires the discretionary guard in task 3 (extra work) precisely because a "kept" verdict is the shape most prone to silent future drift. Leaves a real, proven capability unnamed in the project's identity statement.</cons>
    </option>
  </options>
  <resume-signal>Reply `restate` or `keep-dated`. If you want a variant of the restated wording rather than the pre-drafted candidate, reply `restate` and give the wording you want.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the CORE-01 verdict as a dated, evidence-citing entry inside PROJECT.md's Core Value section</name>
  <reversibility rating="one-way">This rewrites or permanently dates the project's published identity statement at the milestone close, immediately before the milestone audit archives it; undoing it later means a second contradicting dated entry, not a revert.</reversibility>
  <read_first>
    - `.planning/PROJECT.md` → `## Core Value` (lines 32-51) — the file being modified. Read the three existing paragraphs in full; you are extending this section's own voice.
    - `.planning/PROJECT.md` → `## Key Decisions`, the FORK-01 row (line 278) — the dated-decision shape to mirror: verdict, rationale naming the specific evidence, reversal criterion named by its concrete trigger, caveats carried rather than resolved.
    - `.planning/PROJECT.md` → `## Requirements → Validated` (lines 78-82) — the R2000-10 / R2000-14 / R2000-15 / R2000-01 bullets are the exact evidence anchors to cite.
    - `.planning/REQUIREMENTS.md` → `## Out of Scope` (the "Restating Core Value as a bookkeeping edit" row) and the CORE-01 requirement line.
    - `.planning/phases/17-project-identity-and-ledger-close/17-PATTERNS.md` → the `.planning/PROJECT.md → ## Core Value` section, which quotes the current text verbatim and lists the fields the new paragraph must carry.
  </read_first>
  <files>.planning/PROJECT.md</files>
  <action>
    Write the verdict the human selected at task 1 into `## Core Value`. Do not
    re-decide it, do not hedge it, and do not record both branches. Edit only the
    `## Core Value` section; leave `## Key Decisions` alone (ROADMAP.md criterion 1
    places this entry inside Core Value, and `docs-fork-decision.test.ts` owns the
    Key Decisions table and must stay green).

    The paragraph replaces the existing third paragraph — the one beginning
    `**Flagged at the v0.3.0 close, deliberately not rewritten yet.**` — because that
    flag is what this task discharges; leaving it in place alongside a verdict would
    leave the section self-contradicting. Keep the first paragraph (the statement
    itself) and the second (`*Still correct after v0.2.0.*`) as history.

    Fields the new entry must carry, in either branch, mirroring FORK-01's structure:
    the ISO date `2026-08-23`; an explicit verdict word; the requirement id `CORE-01`;
    the evidence actually weighed, named by literal string rather than gestured at —
    at minimum Phase 11's two-session sealed-question test, and preferably also the
    symbol round trip (R2000-14/R2000-15) and the 17 curated `r2000_*` tools; and a
    statement that the decision was taken by a human at plan 17-02 task 1's
    `gate="blocking-human"` checkpoint, matching how `REQUIREMENTS.md:89-91` records
    FORK-01's provenance.

    It must also engage with the case it did **not** take. An entry that rehearses
    only the winning side reads as a bookkeeping edit no matter how well dated. Name
    at least one item from the losing column: on the restate branch, the R2000-01
    fact that the persistence-side backend is structurally incapable of touching VICE
    and is guarded in code; on the keep branch, the sealed-question test's strength as
    the strongest evidence the prior milestone produced.

    Branch-specific requirements:

    - **If the verdict is `restate`:** rewrite the leading statement so it names the
      outlives-the-session axis, using the human's wording if one was supplied at the
      checkpoint and otherwise the candidate already drafted in the current text ("a
      Claude session can drive a real C64 emulator to reverse-engineer a program, and
      what it learns outlives the session"), preserving the concrete enumeration
      ("read and write memory, set checkpoints, capture RAM, inspect chip state") and
      the "keep working when the emulator misbehaves" clause the existing statement
      carries. Follow it with the dated entry explaining what changed and why. No
      reversal clause is required — the new wording is itself the acted-on evidence.

    - **If the verdict is `keep-dated`:** leave the leading statement byte-identical
      and add the dated entry recording that the evidence was weighed and the
      statement deliberately retained. It MUST name a concrete condition under which
      the question is reopened, in the same voice as FORK-01's "This decision reverses
      if UP-01 lands" sentence — a second milestone's worth of persistent-state
      evidence, or a shipped workflow that demonstrably depends on cross-session
      recall, are both defensible; pick one and state it specifically enough that a
      future reader can tell whether it has been met. Also state plainly that the
      trigger is manually tracked rather than mechanically probed, the way FORK-01's
      row does.

    Record in the SUMMARY which branch the human chose, verbatim, so task 3's
    condition is unambiguous.
  </action>
  <verify>
    <automated>awk '/^## Core Value/,/^## Requirements/' .planning/PROJECT.md | grep -qE '[0-9]{4}-[0-9]{2}-[0-9]{2}' && awk '/^## Core Value/,/^## Requirements/' .planning/PROJECT.md | grep -qE 'sealed.question|Phase 11' && awk '/^## Core Value/,/^## Requirements/' .planning/PROJECT.md | grep -q 'CORE-01' && cd src/mcp/vice && node --test docs-fork-decision.test.ts && node --test docs-deferred-ledger.test.ts</automated>
    <human-check>
      Read the new `## Core Value` entry end to end and confirm it reads as evidence
      weighed rather than a conclusion asserted. Specifically: (a) it names the
      verdict the human actually chose at the checkpoint; (b) it cites at least one
      piece of evidence by name rather than referring to "the evidence"; (c) it
      engages with the case against the verdict it reached; (d) on the keep branch,
      the reversal condition is specific enough that a future reader could tell
      whether it has been met. An entry reading only "Core Value confirmed, no change
      needed" fails this check even though it would pass the automated one.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `awk '/^## Core Value/,/^## Requirements/' .planning/PROJECT.md | grep -cE '[0-9]{4}-[0-9]{2}-[0-9]{2}'` reports at least `1`. It reports `0` before this task.
    - `awk '/^## Core Value/,/^## Requirements/' .planning/PROJECT.md | grep -cE 'sealed.question|Phase 11'` reports at least `1`.
    - `awk '/^## Core Value/,/^## Requirements/' .planning/PROJECT.md | grep -c 'CORE-01'` reports at least `1`.
    - `awk '/^## Core Value/,/^## Requirements/' .planning/PROJECT.md | grep -c 'blocking-human'` reports at least `1` — the provenance of the decision is on the record, matching FORK-01's closure note.
    - `awk '/^## Core Value/,/^## Requirements/' .planning/PROJECT.md | grep -c '^\*\*Flagged at the v0.3.0 close'` reports `0` — the standing flag this task discharges no longer opens a paragraph of its own. The gate is anchored to the bolded run-in at line start rather than to the phrase, because the new verdict entry may legitimately quote the retired flag while recording that it was discharged.
      <!-- planner-discipline-allow: deliberately not rewritten yet -->
    - `cd src/mcp/vice && node --test docs-fork-decision.test.ts` exits 0 (unchanged and untouched).
    - `cd src/mcp/vice && node --test docs-deferred-ledger.test.ts` exits 0 (plan 17-01's work still green).
    - `git diff --name-only` lists exactly `.planning/PROJECT.md`.
    - On the `keep-dated` branch only: `awk '/^## Core Value/,/^## Requirements/' .planning/PROJECT.md | grep -cE 'reverses if|would reverse|reversal criteria|reopen'` reports at least `1`.
  </acceptance_criteria>
  <done>`## Core Value` carries a dated, `CORE-01`-labelled entry recording the human's verdict, naming the specific evidence weighed on both sides and the checkpoint the decision was taken at; the v0.3.0 "not rewritten yet" flag is discharged; the fork-decision and deferred-ledger guards are both still green.</done>
</task>

<task type="auto">
  <name>Task 3: Conditionally pin the kept verdict with docs-core-value-decision.test.ts</name>
  <reversibility rating="reversible">A new, self-contained doc guard; deleting it restores the prior state exactly, and no shipped artifact depends on it.</reversibility>
  <read_first>
    - `src/mcp/vice/docs-fork-decision.test.ts` (full file, 181 lines) — the template. Reproduce its shape: the `WHY THIS EXISTS` header naming the specific drift it stops, the `repoRoot({ from: HERE })` resolution, the section-extraction helper returning `null` on a missing heading, the ISO-date test, the named-evidence test, the reversal-phrase array whose own length is asserted non-zero, and the non-vacuity test.
    - `src/mcp/vice/docs-deferred-ledger.test.ts` → the planted-violation test (lines 142-179) — the synthetic-input shape every guard in this family carries so the predicate is proven able to fire.
    - `.planning/PROJECT.md` → `## Core Value` as written by task 2 — the text the guard must be satisfied by.
    - `.planning/phases/17-project-identity-and-ledger-close/17-PATTERNS.md` → the `docs-core-value-decision.test.ts` section, which names each pattern to copy and the one adaptation needed (Core Value is prose, so there is no table-row filter and no row-count floor).
    - `src/mcp/vice/package.json` → `files[]` (lines 10-104), to confirm the new guard is excluded from the published tarball by construction.
  </read_first>
  <files>src/mcp/vice/docs-core-value-decision.test.ts</files>
  <action>
    **This task is conditional on task 1's verdict. Read the branch recorded in task
    2's SUMMARY before doing anything.**

    - **If the verdict was `keep-dated`: build the guard.** A verdict of "kept, with a
      dated confirmation" is the shape most prone to silent future drift — a later
      edit can strip the date, soften the evidence citation, or quietly delete the
      reversal condition, and the section would still read plausibly. That is the
      identical argument that motivated `docs-fork-decision.test.ts`'s own existence,
      and RESEARCH.md's Open Question 1 recommends exactly this conditional.

    - **If the verdict was `restate`: do NOT create the file.** The new wording is
      itself the acted-on evidence, no Core Value edit in this project's history has
      ever carried a guard, and CORE-01's text — unlike DEBT-04's — never uses the
      word "guarded". Instead, record in this task's SUMMARY section, in one
      paragraph, that the guard was deliberately not built, on which branch, and on
      what grounds, so the decision is on the record rather than looking like an
      omission. Then treat the task as complete. Do not invent substitute work.

    On the keep branch, create `src/mcp/vice/docs-core-value-decision.test.ts` as a
    sibling of the existing doc-guard family, matching every family convention:
    a header block comment opening `WHY THIS EXISTS: CORE-01.` that names the
    specific drift this guard stops (a dated Core Value verdict silently reverting to
    an undated, unattributed statement across a future milestone), and states that
    like its siblings this file verifies planning-facing documentation rather than
    shipped runtime behaviour and is deliberately kept out of `package.json`'s
    `files[]`. Imports exactly as the fork guard has them, including `repoRoot` from
    `./repo-root.ts` with the `.ts` extension. Resolve `PROJECT_MD` as
    `join(ROOT, ".planning/PROJECT.md")`.

    Section extraction: adapt the fork guard's `keyDecisionsSection()` to a
    `coreValueSection()` matching `## Core Value` up to the next `## ` heading,
    returning `null` when the heading is absent, with every dependent test asserting
    non-null first and naming a renamed heading as the likely cause.

    Tests to carry, adapted from the named fork-guard analogs:
    1. Non-vacuity — the section is located and its trimmed text clears a character
       floor. Core Value is prose, not a table, so the fork guard's `>= 20 rows` floor
       does not transfer; use a character-length floor generous enough not to be
       brittle and small enough to fail on an emptied section, and assert the
       reversal-phrase array's own length is non-zero so test 4 cannot pass vacuously.
    2. ISO date — the section matches `/\d{4}-\d{2}-\d{2}/`, with the fork guard's own
       failure message adapted: a decision recorded without a date cannot be
       distinguished from the default-carried status quo this requirement exists to
       stop.
    3. Named evidence — the section contains at least one of the literal strings the
       entry was required to cite, case-sensitively, the same way the fork guard
       requires `KEYBOARD_MATRIX_SET` verbatim. Use the anchors task 2 actually wrote;
       `Phase 11` and `sealed-question` are the two RESEARCH.md recommends.
    4. Reversal condition — reuse the fork guard's `REVERSAL_PHRASES` array shape
       (`reverses if`, `would reverse`, `reversal criteria`), extended with the phrase
       task 2 actually used if it differs, asserting the section carries at least one
       verbatim.
    5. Planted violation — the family's signature test. Run all of the above
       predicates against synthetic strings that violate each one (an undated
       paragraph, an evidence-free paragraph, a reversal-condition-free paragraph),
       proving each predicate fires, and then assert the real, current
       `## Core Value` section is flagged by none of them. A guard that cannot be
       satisfied by the real fixed state gets switched off; this is the test that
       proves it can be.

    Match the file's family style throughout: 2-space indent, double quotes,
    semicolons, JSDoc-style block comments on each helper explaining why the helper
    exists rather than what it does, and assertion messages that name the likely
    cause. Do not add the file to `package.json`'s `files[]`.
  </action>
  <verify>
    <automated>cd src/mcp/vice && npm run typecheck && node --test docs-fork-decision.test.ts && node --test docs-deferred-ledger.test.ts && { [ ! -f docs-core-value-decision.test.ts ] || node --test docs-core-value-decision.test.ts; }</automated>
  </verify>
  <acceptance_criteria>
    - Keep branch: `cd src/mcp/vice && node --test docs-core-value-decision.test.ts` exits 0 with 5 passing tests and 0 failures.
    - Keep branch: temporarily deleting the ISO date from `## Core Value` makes `cd src/mcp/vice && node --test --test-name-pattern='date' docs-core-value-decision.test.ts` exit non-zero; the edit is reverted before commit. Demonstrate the guard bites rather than asserting that it does — this family accepts no guard that has not been shown to fail on a planted violation.
    - Keep branch: `grep -c 'docs-core-value-decision' src/mcp/vice/package.json` reports `0` — the guard is excluded from the published tarball by `files[]` being an allowlist.
    - Keep branch: `node scripts/check-npm-packages.mjs` exits 0 — neither published tarball gained a test file.
    - Restate branch: `test ! -f src/mcp/vice/docs-core-value-decision.test.ts` succeeds, and the SUMMARY carries a paragraph recording the deliberate non-build and its grounds.
    - Either branch: `cd src/mcp/vice && npm run typecheck` exits 0.
    - Either branch: `cd src/mcp/vice && node --test docs-fork-decision.test.ts` and `node --test docs-deferred-ledger.test.ts` both exit 0.
  </acceptance_criteria>
  <done>On the keep branch, a committed guard pins the dated Core Value verdict and has been demonstrated to fail on a planted violation; on the restate branch, no file was created and the SUMMARY records why. Both existing doc guards remain green either way.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols this plan creates that do not exist in the codebase today — the
source-grounding pass should exclude these from drift verification:

- `.planning/PROJECT.md` → a new dated `CORE-01` verdict entry inside
  `## Core Value`, replacing the "**Flagged at the v0.3.0 close, deliberately not
  rewritten yet.**" paragraph. On the `restate` branch, the section's leading
  statement is also rewritten.
- `src/mcp/vice/docs-core-value-decision.test.ts` — **NEW FILE, conditional on the
  `keep-dated` branch only.** May legitimately not exist at the end of this phase.
- New test names inside that file, conditional on the same branch: the
  non-vacuity, ISO-date, named-evidence, reversal-condition and planted-violation
  tests described in task 3. Their final wording is the executor's, following the
  `docs-fork-decision.test.ts` naming style.
- A `coreValueSection()` helper and a `REVERSAL_PHRASES` constant inside that file,
  conditional on the same branch.

Sibling plans in this phase produce further new symbols; see `17-01-PLAN.md` and
`17-03-PLAN.md` for those.
</artifacts_this_phase_produces>

<probe_coverage>
No SPEC exists for this phase, so the spec-less probe fallback is active.

**Edges — the CORE-01 row, 1 of the report's 6 applicable items (the other 5 are
DEBT-04's and are carried by `17-01-PLAN.md`):**

| Requirement | Category | Status | Disposition |
|---|---|---|---|
| CORE-01 | unclassified | **unresolved** | Surfaced as a flagged assumption below — NOT auto-resolved, NOT dismissed. |

**Flagged assumption (CORE-01, `unclassified`, unresolved).** The edge probe could
not classify CORE-01 into any of its eight shape categories, and that is the
correct outcome rather than a classification miss: CORE-01 has no data-shape axis
at all. It asks for a *judgment about a sentence*, and the probe's taxonomy —
boundary, adjacency, empty, ordering, precision and the rest — reaches
data/behaviour shapes only. There is no min/max, no equality collision, no empty
input, no ordering and no rounding contract in "was the evidence weighed". The
consequence carried into execution, stated rather than hidden: **the load-bearing
half of CORE-01 is not mechanically checkable.** What task 2's automated verify
can prove is only that the *artifacts* of a weighed decision are present — a date,
a named evidence citation, a recorded checkpoint provenance, a reversal condition.
Whether the weighing was genuine is carried by the `<human-check>` block on task 2
and by the `verification: backstop` truth in this plan's `must_haves`, which
abstains to `human_needed` at verify time rather than passing silently. Reviewers
should treat a passing automated gate here as necessary and not sufficient.

**No-silent-drop equality for this plan:** 1 CORE-01 probe row surfaced == 0
authored into `must_haves.truths` + 1 surfaced as a flagged assumption. Combined
with `17-01-PLAN.md`'s 5, the phase accounts for all 6 applicable rows: 4 explicit
truths + 1 backstop truth + 1 flagged assumption.

**Prohibitions:** 2 kept from the recall/precision pass, both authored
descriptor-less (no `check_*` scalar) so each disposes flagged-unverified.
Candidates dropped as routine engineering: "must not break Markdown rendering",
"must not leave the section without a trailing newline", "must not duplicate the
Key Decisions row". No canon-referral drops.
</probe_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human decision → written record | The only boundary that matters here. A verdict reached by a human at a checkpoint is transcribed by an agent into a durable document, and the transcription is what every future reader sees. Fidelity of that transcription — and attribution of who decided — is the asset. |
| repo filesystem → guard process | On the keep branch, `docs-core-value-decision.test.ts` reads `.planning/PROJECT.md` through the existing `repo-root.ts` seam. Repo-local, trusted input; no network, no user-controlled data. |

No new attack surface. RESEARCH.md's `## Security Domain` assessed ASVS
V2/V3/V4/V5/V6 as not applicable to this phase and nothing in this plan changes
that: no auth, no session, no access control, no external input, no cryptography.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-04 | Spoofing | the CORE-01 verdict's provenance | medium | mitigate | An agent-selected verdict recorded as if a human took it would forge the exact attribution FORK-01's precedent makes load-bearing. Mitigated by `gate="blocking-human"` on task 1 (never auto-approved even under this project's active auto-mode) plus the acceptance criterion that the written entry names the checkpoint it was decided at, so the claim is on the record and falsifiable against the execution log. |
| T-17-05 | Repudiation | `.planning/PROJECT.md` → `## Core Value` | medium | mitigate | An undated or evidence-free entry cannot later be distinguished from a change made at any time by anyone, which is the "third silent carry" the requirement exists to end. Mitigated by the ISO-date, named-evidence and provenance criteria; on the keep branch, additionally pinned mechanically by task 3's guard, which is itself required to be demonstrated failing on a planted violation before it is accepted. |
| T-17-06 | Tampering | `src/mcp/vice/docs-core-value-decision.test.ts` (keep branch) | low | mitigate | A new guard written to pass vacuously would provide false assurance. Mitigated by the family's planted-violation convention, carried into this guard as test 5, plus the explicit acceptance criterion requiring a live demonstration that removing the date turns the date test red. |
| T-17-07 | Information disclosure | guard filesystem reads | low | accept | Reads one repo-local planning document through a pre-existing seam. Nothing sensitive, nothing external. Accepted with no control. |
| T-17-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages and adds no dependencies; RESEARCH.md's `## Package Legitimacy Audit` records the gate as not applicable, so no install task exists for a legitimacy checkpoint to precede. Accepted on the grounds that the supply chain is untouched. |

No threat is rated `high`, so nothing here trips `security_block_on: high`.
</threat_model>

<verification>
1. `awk '/^## Core Value/,/^## Requirements/' .planning/PROJECT.md` shows a dated,
   `CORE-01`-labelled entry naming specific evidence and the checkpoint provenance.
2. `cd src/mcp/vice && node --test docs-fork-decision.test.ts` — green, untouched.
3. `cd src/mcp/vice && node --test docs-deferred-ledger.test.ts` — green, plan
   17-01's work intact.
4. Keep branch only: `cd src/mcp/vice && node --test docs-core-value-decision.test.ts`
   — 5 pass, 0 fail, with the date test demonstrated red on a planted violation.
5. `cd src/mcp/vice && npm run typecheck` — exits 0.
6. The `<human-check>` on task 2 is harvested at end of phase per
   `workflow.human_verify_mode: end-of-phase`; it is the only check that reaches
   the "weighed, not bookkeeping" half of criterion 1.

The full `cd src/mcp/vice && npm test` gate is run by plan 17-03, the closer.
</verification>

<success_criteria>
- CORE-01's verdict was chosen by a human at a checkpoint that auto-mode cannot
  bypass, and the written record says so.
- `## Core Value` carries an ISO date it did not carry before, names the specific
  evidence weighed on both sides, and discharges the v0.3.0 "not rewritten yet"
  flag rather than leaving it standing beside a verdict.
- On the keep branch the verdict is pinned by a committed guard proven to bite; on
  the restate branch the deliberate non-build is recorded rather than silent.
</success_criteria>

<output>
Create `.planning/phases/17-project-identity-and-ledger-close/17-02-SUMMARY.md` when done.
</output>
