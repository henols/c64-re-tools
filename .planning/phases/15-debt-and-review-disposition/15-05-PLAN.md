---
phase: 15-debt-and-review-disposition
plan: 05
type: execute
wave: 3
depends_on: ["15-04"]
files_modified:
  - .claude/mcp/vice/stock-connect.ts
  - .claude/mcp/vice/stock-connect.test.ts
  - .planning/todos/pending/2026-08-21-phase-09-review-in-01-in-03-never-dispositioned.md
  - .planning/todos/pending/2026-08-22-phase-13-review-wr-01-wr-02-in-01-in-02-never-dispositioned.md
  - .planning/todos/completed/2026-08-22-phase-02-review-in-05-stockreconnect-names-the-wrong-function.md
  - .planning/STATE.md
autonomous: true
requirements: [GATE-02]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`stockReconnect()`'s thrown message names `stockReconnect`, matching the `where` field it already sets correctly — the one-word defect `02-REVIEW.md IN-05` names is closed at source (GATE-02's fourth named item)."
    - "`09-REVIEW.md`'s `IN-01`, `IN-02` and `IN-03` are each recorded `wont-fix` with the evidence-immutability rationale, in a completed todo, and the three harness files are byte-identical to the versions that produced the committed probe transcripts."
    - "`13-REVIEW.md`'s `WR-01` is recorded fixed with the commit that landed it, and `WR-02`, `IN-01`, `IN-02` each carry a recorded verdict with a stated trigger that would reopen it."
    - "`checkCommandAvailable()` in `probe-binmon.mjs` passes the binary name as a positional shell argument, never spliced into a command line, and a source-level gate fails if that is ever simplified back."
    - "No finding closed by this plan is dispositioned solely by a todo that mentions its phase number incidentally — every disposition source also names that finding's own `<N>-REVIEW.md` filename."
  prohibitions:
    - "MUST NOT edit any file under `.planning/phases/09-the-assumption-probe-go-no-go/evidence/` — those harnesses produced the committed transcripts backing the milestone's `degrade`/`R4` go/no-go verdict, and editing them breaks the provenance link between the harness in the tree and the evidence it is recorded as having produced."
    - "MUST NOT close a todo `wont-fix` without a written rationale in its `## Resolution`."
    - "MUST NOT record a finding as fixed unless the fix has landed and the disposition cites the commit or the re-runnable check that proves it."
  artifacts:
    - .claude/mcp/vice/stock-connect.ts
    - .planning/todos/completed/2026-08-22-phase-02-review-in-05-stockreconnect-names-the-wrong-function.md
  key_links:
    - "**STATE.md ledger ownership.** `docs-deferred-ledger.test.ts` is a strict two-directional invariant between `## Deferred Items` and `.planning/todos/pending/`, and it is IN the automated gate — so any plan that files or moves a todo MUST update that table in the same commit, or `npm run test:automated` goes red. That is why `.planning/STATE.md` is declared in this plan's `files_modified` and why the ledger-touching plans of this phase run in separate waves: two concurrent plans editing that one table would collide."
    - "`docs-review-disposition.test.ts:133-146` (`todoMentionsPhase()`) binds a todo to a phase only if it names `<N>-REVIEW.md` or a word-bounded `Phase N` — every disposition this plan writes must satisfy that, or the guard cannot see it."
    - "`.planning/phases/09-.../evidence/*.mjs` → the committed `.txt` transcripts → the `degrade`/`R4` verdict in `docs/phase9-regenerator2000-probe-findings.md`: this chain is why `IN-01`..`IN-03` are `wont-fix` rather than cheap cleanups."
    - "`13-REVIEW.md WR-01`'s fix landed in commit `f73d0fa` — verified at plan time. `15-RESEARCH.md` still describes it as needing a fix; that row is stale and must not be re-implemented."
---

<objective>
Close the three remaining review-finding clusters GATE-02 names outside Phase 08 and
Phase 03: `02-REVIEW.md`'s `IN-05` (a one-word fix at source), Phase 09's `IN-01`..`IN-03`
(`wont-fix`, evidence immutability), and Phase 13's `WR-01`/`WR-02`/`IN-01`/`IN-02` (one
already fixed, three deferred with stated reopen triggers).

Purpose: GATE-02's success criterion names `02-REVIEW.md`'s `IN-05` and Phase 09's
`IN-01`..`IN-03` explicitly. `IN-05` is the cheapest open finding in the whole milestone —
one word in one thrown message — and has been carried across two milestone closes purely
because `stock-connect.ts` sat outside every intervening phase's scope fence. Phase 09's
three are the opposite: they must NOT be fixed, because the files they name are the
throwaway harnesses whose committed transcripts back the milestone's go/no-go verdict, and
editing them severs that provenance link. Phase 13's four already carry fully-reasoned
per-finding recommendations in their own todo; this plan executes those recommendations.

Correction to `15-RESEARCH.md`, verified at plan time: `13-REVIEW.md`'s `WR-01` is
**already fixed**. `probe-binmon.mjs`'s `checkCommandAvailable()` now passes the name as a
positional argument (`sh -c 'command -v "$1"' sh cmd`), landed in commit `f73d0fa` with an
explanatory comment forbidding the simplification back. The research's row calling for a
two-line fix is stale. Do not re-fix it — cite it, and pin it.

Output: one source fix, one already-landed fix cited and pinned, and seven recorded
verdicts across three closed todos.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-debt-and-review-disposition/15-RESEARCH.md
@.planning/phases/15-debt-and-review-disposition/15-PATTERNS.md
@.planning/todos/pending/2026-08-21-phase-09-review-in-01-in-03-never-dispositioned.md
@.planning/todos/pending/2026-08-22-phase-13-review-wr-01-wr-02-in-01-in-02-never-dispositioned.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stockReconnect()'s misnamed thrown message and record the disposition (02-REVIEW.md IN-05)</name>
  <files>.claude/mcp/vice/stock-connect.ts, .claude/mcp/vice/stock-connect.test.ts, .planning/todos/completed/2026-08-22-phase-02-review-in-05-stockreconnect-names-the-wrong-function.md</files>
  <read_first>
    - `.planning/phases/02-stock-backend-connection/02-REVIEW.md` lines 667-673 — `IN-05` in full. It cites `stock-connect.ts:322`; the real current site is `:537`, inside `stockReconnect()` which starts at `:529`. Verify before editing.
    - `.claude/mcp/vice/stock-connect.ts` — read `stockReconnect()` in full. The thrown message's prefix disagrees with the `where` field the same throw sets, which correctly names `stock-connect.ts:stockReconnect`.
    - `.claude/mcp/vice/stock-connect.test.ts` — search for any assertion on the current message prefix. At plan time `grep -rn "stockConnect: reconnect to target" *.test.ts` returned nothing, so probably none exists; re-run that grep and confirm before assuming.
    - `.planning/todos/completed/2026-08-21-phase-10-and-11-review-residual-dispositions.md` — the file that currently *mentions* `IN-05`. Read what it says about it: it records the finding as a confirmed-live straggler left open for scope reasons, which is why the guard is green while the defect is real. This plan supersedes that entry.
    - `.planning/todos/completed/2026-08-21-r2000-cli-wr-08-option-values-silently-swallowed.md` — the completed-todo frontmatter and body shape to copy.
  </read_first>
  <action>
Change the thrown message's leading function name in `stockReconnect()` so it names the
function that actually throws. Nothing else in the message changes. Confirm no test asserts
the old prefix; if one does, update it in the same commit.

Add a small regression assertion in `stock-connect.test.ts` pinning that the thrown
message's prefix and the `where` field name the same function — not a literal-string
assertion, but a derived one: parse the function name out of the message prefix and assert
it is a substring of `where`. A literal assertion would need editing the next time either
string changes; the derived one encodes the actual invariant the finding is about, and it
covers `stockConnect()` too rather than only the one site just fixed.

Create the completed todo recording the disposition. It must contain the literal
`02-REVIEW.md` and the word-bounded `IN-05` so the guard binds it to phase 02. Frontmatter:
`created` (today), `title` naming `02-REVIEW.md IN-05`, `area: mcp`, `priority: low`,
`resolves_phase: 15`, `files:` citing `.claude/mcp/vice/stock-connect.ts` with the real
current line and `.planning/phases/02-stock-backend-connection/02-REVIEW.md:667-673`. Body:
`## Problem` (one paragraph, quoting the old and new prefix), `## Why it stayed open` (it
was correctly recorded as a confirmed-live straggler in plan 11.1-07's disposition ledger
but `stock-connect.ts` sat outside every intervening phase's scope fence — name that ledger
file), and `## Resolution` with the bold verdict, this plan's commit sha, and the name of
the new derived assertion that keeps it closed.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && npm run typecheck && node --test stock-connect.test.ts && node --test docs-review-disposition.test.ts && npm run test:automated</automated>
  </verify>
  <acceptance_criteria>
    - `cd .claude/mcp/vice && test "$(grep -v -E '^\s*(//|\*|/\*)' stock-connect.ts | grep -c 'stockConnect: reconnect to target' || true)" = "0"`.
    - `cd .claude/mcp/vice && grep -c 'stockReconnect: reconnect to target' stock-connect.ts` returns 1.
    - `cd .claude/mcp/vice && node --test stock-connect.test.ts` exits 0 and reports at least one more test than before.
    - The new assertion is non-vacuous: temporarily restore the wrong prefix, confirm the new test fails naming the mismatch, then restore the fix. Record both outputs in the SUMMARY.
    - `.planning/todos/completed/2026-08-22-phase-02-review-in-05-stockreconnect-names-the-wrong-function.md` exists, contains the literal `02-REVIEW.md` and a word-bounded `IN-05`, and has exactly one `## Resolution` heading.
    - The Resolution cites a resolvable commit: `git cat-file -e <sha>` exits 0.
    - `cd .claude/mcp/vice && npm run typecheck && npm run test:automated` both exit 0.
  </acceptance_criteria>
  <done>The thrown message names its own function, a derived invariant assertion prevents the class of mismatch recurring, and `IN-05` has a commit-citing disposition the guard can see.</done>
</task>

<task type="auto">
  <name>Task 2: Record Phase 09's IN-01..IN-03 wont-fix on evidence immutability and close the todo</name>
  <files>.planning/todos/pending/2026-08-21-phase-09-review-in-01-in-03-never-dispositioned.md</files>
  <read_first>
    - `.planning/phases/09-the-assumption-probe-go-no-go/09-REVIEW.md` lines 108-150 — `IN-01`, `IN-02`, `IN-03` in full. All three name files under `.planning/phases/09-the-assumption-probe-go-no-go/evidence/`.
    - `.planning/todos/pending/2026-08-21-phase-09-review-in-01-in-03-never-dispositioned.md` — it already contains the fully-reasoned recommendation this task executes. Do not re-derive the argument; execute it and cite it.
    - `docs/phase9-regenerator2000-probe-findings.md` — the frontmatter `verdict` / `verdict_rule_applied` / `criteria` fields. This is the artifact the harnesses' transcripts back; the `wont-fix` rationale depends on the harness-to-transcript-to-verdict chain being real, so confirm the transcripts are committed and named there.
    - `ls .planning/phases/09-the-assumption-probe-go-no-go/evidence/` — enumerate the harness files and the committed transcripts, and record the list in the SUMMARY so the immutability claim names concrete files.
    - `.planning/todos/pending/2026-08-12-vice-broker-tests-stall-outside-devcontainer.md` — its in-file `## DISPOSITION` section is the model shape (bold one-line verdict, rationale paragraph, consequence) that `15-PATTERNS.md` names for a `wont-fix`.
    - `15-RESEARCH.md` § Pitfall 3 — the explicit warning against "helpfully" cleaning up these harnesses.
  </read_first>
  <action>
Move the todo to `.planning/todos/completed/` with `git mv` and append a `## Resolution`
section recording all three findings `wont-fix`.

The rationale, stated once and applied to all three: the files are throwaway probe
harnesses whose committed transcripts are Phase 09's actual deliverable, backing the
`degrade` verdict and the `R4` rule in `docs/phase9-regenerator2000-probe-findings.md`.
Editing a harness after the fact breaks the correspondence between the harness in the tree
and the evidence it is recorded as having produced — and all three findings are Info-level
code-quality observations (a dead accumulator, a lenient argument pairing, a missing
best-effort close on an error path) with no effect on any recorded result. Name the concrete
consequence: if a future phase re-runs any of these harnesses, it should re-run the fixed
version and record fresh transcripts, not retro-fit the old ones.

Include a per-finding table (`Id | File | Verdict | Reason`) so each id is word-bounded in
the file and the guard sees all three individually, not just the range in the title.

State explicitly that no file under `.planning/phases/09-the-assumption-probe-go-no-go/`
was modified, and prove it in the acceptance criteria with `git diff`.

Do not edit `09-REVIEW.md`.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && node --test docs-review-disposition.test.ts && node --test docs-deferred-ledger.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/todos/completed/2026-08-21-phase-09-review-in-01-in-03-never-dispositioned.md` succeeds and the `pending/` path no longer exists.
    - `for id in IN-01 IN-02 IN-03; do grep -qw "$id" .planning/todos/completed/2026-08-21-phase-09-review-in-01-in-03-never-dispositioned.md || echo "MISSING $id"; done` prints nothing.
    - The file names its phase's review file: `grep -c '09-REVIEW.md' .planning/todos/completed/2026-08-21-phase-09-review-in-01-in-03-never-dispositioned.md` returns at least 1.
    - `grep -c '## Resolution' .planning/todos/completed/2026-08-21-phase-09-review-in-01-in-03-never-dispositioned.md` returns 1, and the section contains the word `wont-fix` and a non-empty Reason cell for each of the three ids.
    - `git diff --stat HEAD~1 -- .planning/phases/09-the-assumption-probe-go-no-go/` is empty at the end of this task (no harness, transcript, review or verification file under Phase 09 was touched).
    - `cd .claude/mcp/vice && node --test docs-review-disposition.test.ts` exits 0.
    - STATE.md's `## Deferred Items` table and both prose count figures were updated in THIS task's commit for every todo this plan moved out of `pending/`, and `cd .claude/mcp/vice && node --test docs-deferred-ledger.test.ts` exits 0 in both directions.
  </acceptance_criteria>
  <done>All three Phase 09 Info findings carry a `wont-fix` verdict with the evidence-immutability rationale and a named reopen consequence, and not one byte under Phase 09's directory changed.</done>
</task>

<task type="auto">
  <name>Task 3: Execute Phase 13's four per-finding recommendations, pin the already-landed WR-01 fix, and close the todo</name>
  <files>.planning/todos/pending/2026-08-22-phase-13-review-wr-01-wr-02-in-01-in-02-never-dispositioned.md, .claude/mcp/vice/stock-connect.test.ts</files>
  <read_first>
    - `.planning/todos/pending/2026-08-22-phase-13-review-wr-01-wr-02-in-01-in-02-never-dispositioned.md` — read in full, including `## Recommended disposition` (per-finding) and `## The transferable half` (the recurring structural cause). This task executes those recommendations.
    - `.planning/phases/13-external-verification/13-REVIEW.md` — the four findings' own text.
    - `.claude/mcp/vice/probe-binmon.mjs` around `checkCommandAvailable()` (near :1487-:1496 at plan time) — read the current implementation AND its comment block. The fix is already there: the name arrives as a positional argument, and the comment explicitly forbids reverting to interpolation. `git log -S` a distinctive fragment of that comment to confirm the landing commit is `f73d0fa`.
    - `.claude/mcp/vice/probe-binmon.mjs` — its total line count and the mode list in its header, for `WR-02`'s deferral record (the finding is about the file's size and concern count).
    - `.planning/phases/13-external-verification/13-PROBE-RESULTS.md` § A3 — `IN-01`'s context: A3 is `INCONCLUSIVE`, which is why the recommendation is fix-only-if-re-probed.
    - `.claude/gsd-core/workflows/code-review.md` — `IN-02` is about this workflow's `files:` derivation, i.e. GSD tooling outside this repo's own source. Confirm that before recording the verdict.
    - `.claude/mcp/vice/hostpath-consumers.test.ts` or `.claude/mcp/vice/r2000-spawn-seam.test.ts` — the established shape for a source-level gate that scans a derived file set rather than a hand-typed list; copy it for the `WR-01` pin.
  </read_first>
  <action>
`WR-01` — record **fixed**, do not re-implement. Confirm the current implementation passes
the binary name as a positional argument rather than splicing it into a command line, cite
commit `f73d0fa` (verify the sha resolves), and quote the current line. Then add the pin:
a source-level assertion, in `stock-connect.test.ts` alongside Task 1's new invariant test
(so this plan adds exactly one new test file section rather than two), that no `.mjs` or
`.ts` file in `.claude/mcp/vice` interpolates a template placeholder into a `sh -c` command
string. Derive the file set with `readdirSync`, strip comment lines before matching, and
name `13-REVIEW.md WR-01` plus the forbidden shape in the failure message. This is what
makes the disposition durable: the comment at the fix site tells a human not to simplify it
back, and this gate tells the test suite.

`WR-02` — record **deferred with intent**, per the todo's own recommendation. The suggested
split is right but doing it now would rewrite the file that produced Phase 13's committed
probe transcripts, which is the same evidence-immutability argument governing Phase 09's
three. State the concrete reopen trigger the todo names: do the split when a sixth probe
mode is actually added. Record the file's real current line count and mode count so a future
reader can see whether it has grown since.

`IN-01` — record **wont-fix for now, with a named trigger**: the short-circuit blind spot in
the A3 polarity check matters only if A3 is re-probed, and A3 is `INCONCLUSIVE` today with
its `[ASSUMED]` label still on. State the trigger precisely: tighten the polarity reporting
to name both CIA1 ports independently *before* any A3 re-probe, not after, or the same
ambiguity recurs in the new evidence.

`IN-02` — record **promoted out of this repo**: the scoping omission is in
`code-review.md`'s file-list derivation (matched evidence `.txt`/`.json` pairs should be
scoped as a unit), which is GSD tooling, not this project's source. Name it as a
promotion with an owner rather than a `wont-fix`, so DEBT-01's criterion is satisfied by a
named owner rather than by silence. Plan 15-12 records the promotion in
`REQUIREMENTS.md` → Future Requirements; this task's job is to state the verdict and hand it
over by name.

Then `git mv` the todo to `completed/` and append the `## Resolution` section with a
per-finding table (`Id | Verdict | Evidence or trigger | Owner`). Carry forward the todo's
`## The transferable half` analysis verbatim rather than summarising it — it is the fourth
instance of the same structural cause and the ROADMAP's Phase 15 section already names
Phase 14 as the fourth, so this todo remains the single place that analysis lives.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && npm run typecheck && node --test stock-connect.test.ts && node --test docs-review-disposition.test.ts && npm run test:automated</automated>
  </verify>
  <acceptance_criteria>
    - `cd .claude/mcp/vice && grep -c "command -v \\\\\"\\\$1\\\\\"" probe-binmon.mjs` returns 1 (the positional-argument form is present and unmodified by this plan).
    - `git diff --stat HEAD~1 -- .claude/mcp/vice/probe-binmon.mjs` is empty — `WR-01` was cited, not re-fixed.
    - `git cat-file -e f73d0fa` exits 0 and the Resolution cites it.
    - The new source-level gate exists and is derived, not hand-typed: `cd .claude/mcp/vice && grep -c 'readdirSync' stock-connect.test.ts` returns at least 1 and `grep -c 'WR-01' stock-connect.test.ts` returns at least 1.
    - The gate is non-vacuous: temporarily add a `sh -c` command string with an interpolated placeholder to a scratch `.mjs` in `.claude/mcp/vice`, confirm `node --test stock-connect.test.ts` fails naming that file, then delete the scratch file. Record both outputs in the SUMMARY.
    - `test -f .planning/todos/completed/2026-08-22-phase-13-review-wr-01-wr-02-in-01-in-02-never-dispositioned.md` succeeds and the `pending/` path no longer exists.
    - `for id in WR-01 WR-02 IN-01 IN-02; do grep -qw "$id" .planning/todos/completed/2026-08-22-phase-13-review-wr-01-wr-02-in-01-in-02-never-dispositioned.md || echo "MISSING $id"; done` prints nothing, and the file contains the literal `13-REVIEW.md`.
    - The Resolution's per-finding table has a non-empty Owner cell for `IN-02` and a non-empty trigger for `WR-02` and `IN-01`.
    - `cd .claude/mcp/vice && node --test docs-review-disposition.test.ts docs-deferred-ledger.test.ts` exits 0.
    - `cd .claude/mcp/vice && npm run typecheck && npm run test:automated` both exit 0.
  </acceptance_criteria>
  <reversibility rating="reversible">Every verdict here is a recorded decision in a markdown file plus one additive test; each has a stated reopen trigger and reverting means editing the same file back.</reversibility>
  <done>Phase 13's four findings each carry the verdict their own todo recommended, the already-landed shell-argument fix is cited and mechanically pinned, and `IN-02`'s promotion has a named owner handed to plan 15-12.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols and files created by this plan:

- A derived message-prefix-vs-`where` invariant assertion in `.claude/mcp/vice/stock-connect.test.ts`
- A derived `sh -c` interpolation gate in `.claude/mcp/vice/stock-connect.test.ts`
- `.planning/todos/completed/2026-08-22-phase-02-review-in-05-stockreconnect-names-the-wrong-function.md`
- `.planning/todos/completed/2026-08-21-phase-09-review-in-01-in-03-never-dispositioned.md` (renamed from `pending/`)
- `.planning/todos/completed/2026-08-22-phase-13-review-wr-01-wr-02-in-01-in-02-never-dispositioned.md` (renamed from `pending/`)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-derived string → shell command line | `checkCommandAvailable()` resolves a binary name via a shell; splicing rather than passing positionally would make a caller-derived name parseable as shell syntax |
| committed evidence transcript ↔ the harness that produced it | Editing the harness after the fact silently breaks the provenance of a recorded verdict |
| thrown error message → agent | The message text is what reaches the caller; a wrong function name misdirects diagnosis |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Tampering | `checkCommandAvailable()` in `.claude/mcp/vice/probe-binmon.mjs` (`13-REVIEW.md WR-01`) | medium | mitigate | **Already mitigated at source in commit `f73d0fa`** — verified live at plan time: the name arrives as positional `$1`, never spliced into the command line, and the fix site carries a comment forbidding the reversion. This plan adds a derived source-level gate over `.claude/mcp/vice`'s own file set so the reversion also fails the test suite, not only a code review. This is the single injection-shape defect `15-RESEARCH.md` § Security Domain identifies in this phase's scope |
| T-15-18 | Repudiation | Phase 09 evidence harnesses under `.planning/phases/09-.../evidence/` | medium | mitigate | Hard prohibition against editing them; the `wont-fix` verdict records the harness→transcript→verdict chain by name, and `git diff` over the phase directory is an acceptance criterion |
| T-15-19 | Repudiation | `stockReconnect()`'s thrown message naming the wrong function (`02-REVIEW.md IN-05`) | low | mitigate | Fixed at source plus a derived invariant assertion tying the message prefix to the `where` field for both connect and reconnect paths |
| T-15-SC | Tampering | npm/pip/cargo installs | — | accept | Not applicable — this plan installs no packages and adds no dependency |
</threat_model>

<verification>
- `cd .claude/mcp/vice && npm run typecheck && npm run test:automated` both exit 0
- `cd .claude/mcp/vice && node --test docs-review-disposition.test.ts docs-deferred-ledger.test.ts` exits 0
- `git diff --stat` over `.planning/phases/09-the-assumption-probe-go-no-go/` and `.claude/mcp/vice/probe-binmon.mjs` is empty
- Every one of `02-REVIEW.md IN-05`, `09-REVIEW.md IN-01`/`IN-02`/`IN-03`, `13-REVIEW.md WR-01`/`WR-02`/`IN-01`/`IN-02` is named in a completed todo that also names its own review filename
</verification>

<success_criteria>
- Eight findings across three phases carry cited verdicts
- The one injection-shape defect in this phase's scope is confirmed already fixed and is now mechanically pinned
- Phase 09's evidence tree is byte-identical
- `IN-02`'s promotion has a named owner handed to plan 15-12
</success_criteria>

<output>
Create `.planning/phases/15-debt-and-review-disposition/15-05-SUMMARY.md` when done.
Record: the per-finding verdict table across all three phases, the `f73d0fa` confirmation
and current `checkCommandAvailable()` line, both planted-violation probe outputs, the
enumerated Phase 09 evidence file list, and `probe-binmon.mjs`'s current line and mode
counts for `WR-02`'s deferral record. Hand `IN-02`'s promotion to plan 15-12 by name.
</output>
