---
phase: 15-debt-and-review-disposition
plan: 12
type: execute
wave: 9
depends_on: ["15-02", "15-03", "15-04", "15-05", "15-06", "15-07", "15-08", "15-09", "15-10", "15-11"]
files_modified:
  - .planning/todos/pending/2026-08-21-phase-08-review-wr-04-through-wr-12-never-dispositioned.md
  - .planning/todos/pending/2026-08-12-vice-broker-tests-stall-outside-devcontainer.md
  - .planning/todos/pending/2026-08-20-vsf-as-a-bootstrap-input.md
  - .planning/todos/pending/2026-08-19-keyboard-fallback-load-does-not-progress-within-bounded-poll.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [GATE-02, DEBT-01, DEBT-02, DEBT-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every file remaining in `.planning/todos/pending/` after this plan is promoted with a named owner — each is either an existing `PKG-*` requirement or a `REQUIREMENTS.md` Future Requirements entry naming its owner. Nothing is carried silently."
    - "Phase 08's `WR-04`..`WR-13` each carry a per-finding verdict with evidence in one completed todo, transcribed from plans 15-02 and 15-03's SUMMARYs rather than restated from the review."
    - "`docs-review-disposition.test.ts` runs green from a clean checkout with the widened parser, discovering 150 findings across 14 review files, and no finding is dispositioned solely by a todo that mentions its phase number incidentally."
    - "`docs-deferred-ledger.test.ts` is green in both directions, and STATE.md's prose pending-todo count equals the Deferred Items table's row count — the figure that has gone stale twice before."
    - "STATE.md's Deferred Items table rows appear in ascending todo-filename order, so an independent regeneration produces the same row order."
    - "GATE-02, DEBT-01, DEBT-02 and DEBT-03 are each marked Complete in `REQUIREMENTS.md`'s checklist and its Traceability table, and each has a closure note where a carve-out or promotion was taken."
    - "All four `docs-*.test.ts` guards and the fifth planning-doc guard are green, which is `audit-gate.mjs`'s own mechanically-enforced precondition for any future milestone audit recording `status: passed`."
    - {statement: "With the reduced pending set, `docs-deferred-ledger.test.ts` still passes when a pending todo carries no `priority:` field and when the pending directory holds only promoted items.", verification: backstop}
  prohibitions:
    - "MUST NOT promote an item to Future Requirements without a named owner."
    - "MUST NOT delete a pending todo file — the record moves to `completed/`, it never disappears."
    - "MUST NOT close a todo `wont-fix` without a written rationale in its `## Resolution`."
    - "MUST NOT mark a requirement Complete while any part of its ROADMAP success criterion is unmet; record the unmet part as a closure note instead, the way `EXTV-03` and `FORK-02` already do."
    - "MUST NOT weaken, narrow, or skip any of the planning-doc guards to make this plan's final gate run green."
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
  key_links:
    - "`audit-gate.mjs` spawns the four `docs-*.test.ts` guards and blocks a milestone audit recording `status: passed` while any is red (GATE-01). This plan's final gate run is the same check, so leaving one red would block Phase 17's close, not merely this phase's."
    - "`docs-deferred-ledger.test.ts` checks STATE.md's Deferred Items against `.planning/todos/pending/` in BOTH directions — every pending file needs a row and every row needs a file. Closing ten todos across this phase moves both sides at once."
    - "STATE.md's prose pending count is not itself guarded and has gone stale twice; it must be made equal to the table's row count by hand, and this plan is the last chance before Phase 17 measures DEBT-04 against it."
    - "Phase 17 measures DEBT-04 at the true close, after Phase 16 discharges PKG-01's own todo — so this plan must not claim the final count, only the count as of Phase 15."
---

<objective>
Close the phase's record: give Phase 08's ten findings their per-finding verdicts, close the
three remaining `wont-fix` todos, promote every item that stays open with a named owner, flip
the four requirements to Complete with closure notes where a carve-out was taken, and reconcile
the deferred-items ledger in both directions with a final green gate run.

Purpose: DEBT-01's criterion is not "most todos are handled" — it is that **every** file
remaining in `.planning/todos/pending/` after this phase is fixed with a commit reference,
moved to `completed/` with a rationale, or promoted with a named owner. That is an
all-or-nothing criterion and this is the plan that makes it true or false. It is also the last
plan that can: Phase 16 relocates the payload and Phase 17 measures the ledger, so a gap left
here surfaces as a failed measurement two phases later with no owner.

The specific traps this plan exists to avoid, all of them observed in this repository before:
a review finding recorded as fixed with no commit; a `wont-fix` with no rationale; a promotion
with no owner; a requirement marked Complete over an unmet criterion; and STATE.md's prose
pending count disagreeing with its own table, which has happened twice and is not itself
guarded.

Output: four closed todos, a promotion set with named owners, four requirements flipped with
closure notes, a reconciled ledger, and a green run of every planning-doc guard.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-debt-and-review-disposition/15-RESEARCH.md
@.planning/phases/15-debt-and-review-disposition/15-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close the Phase 08 review todo with per-finding verdicts, and the three remaining wont-fix todos</name>
  <files>.planning/todos/pending/2026-08-21-phase-08-review-wr-04-through-wr-12-never-dispositioned.md, .planning/todos/pending/2026-08-12-vice-broker-tests-stall-outside-devcontainer.md, .planning/todos/pending/2026-08-20-vsf-as-a-bootstrap-input.md, .planning/todos/pending/2026-08-19-keyboard-fallback-load-does-not-progress-within-bounded-poll.md</files>
  <read_first>
    - `.planning/phases/15-debt-and-review-disposition/15-02-SUMMARY.md` and `15-03-SUMMARY.md` — the per-finding evidence for Phase 08's `WR-04`..`WR-13`: which file and line each landed at, each planted-violation probe's output, `WR-05`'s landing commit sha, and the re-measured stock-manifest handler count. Transcribe from these, not from the review.
    - `.planning/todos/pending/2026-08-21-phase-08-review-wr-04-through-wr-12-never-dispositioned.md` — its own scope is `WR-04`..`WR-12`; `WR-13` is named by the ROADMAP separately and was landed by plan 15-03, so the Resolution covers ten findings, not nine. Its own `## What to do` also asks for the non-vacuity assertions each finding's fix suggested — confirm from the two SUMMARYs that they exist.
    - `.planning/todos/pending/2026-08-12-vice-broker-tests-stall-outside-devcontainer.md` — it already carries a dated in-file `## DISPOSITION` from a 2026-08-12 user decision. `15-PATTERNS.md` names this section as the model shape; the closure promotes that existing decision into a `## Resolution`, it does not re-decide it.
    - `.planning/todos/pending/2026-08-20-vsf-as-a-bootstrap-input.md` — `.planning/REQUIREMENTS.md`'s Out of Scope table already pre-decided this: covered by DEBT-01 as a disposition, not as a build, with D-34 standing unless a consumer has only snapshot captures and cannot re-capture as raw. Quote that line as the rationale rather than inventing one.
    - `.planning/todos/pending/2026-08-19-keyboard-fallback-load-does-not-progress-within-bounded-poll.md` — read it in full, including its own statement that it does not block DIST-03, whose passing route was the autostart tool. Then read `.planning/phases/15-debt-and-review-disposition/15-08-SUMMARY.md`: scenario 1 exercised that same autostart route live, and scenario 2 injected keyboard input against a running program — if either produced evidence bearing on this todo's stall, cite it.
    - `.planning/todos/completed/2026-08-20-fully-remove-the-forked-vice-mcp-backend.md` — the larger completed-todo `## Resolution` shape to copy.
    - `.claude/mcp/vice/docs-review-disposition.test.ts` :133-146 — `todoMentionsPhase()`. Each closed todo must still name its own phase's review file or a word-bounded phase reference after the move, or the guard loses the disposition it was providing.
  </read_first>
  <action>
Close the Phase 08 review todo. `git mv` it to `completed/` and append a `## Resolution` with a
per-finding table covering all ten of `WR-04` through `WR-13`: columns `Id | Verdict | Landed at
(file:line) | Evidence`. Verdicts and evidence come from plans 15-02 and 15-03's SUMMARYs —
`WR-05` is fixed-already-cited with its commit sha and the TS7016 reason its route diverged from
the review's suggestion; the rest are fixed with a commit plus a planted-violation probe output.
Verify each cited commit resolves with `git cat-file -e` before writing it down. Also record, per
the todo's own `## What to do`, that the non-vacuity assertions each fix's suggestion asked for
exist — naming them — or which do not and why.

Close the broker-tests todo `wont-fix`, promoting its existing dated user decision into a
`## Resolution` in the established shape: bold one-line verdict, the rationale (they depend on
manual host setup — a real broker topology and a real emulator and display environment — so they
cannot be driven unattended), then the consequence (they stay excluded from the automated gate
and are treated as manual, environment-dependent checks). Cite the 2026-08-12 decision date so
the closure reads as recording a decision, not making one. Cross-reference plan 15-11's CI
decision, which touched the same manual-only set.

Close the snapshot-as-bootstrap-input todo `wont-fix`, quoting the pre-existing Out of Scope
decision verbatim and naming the reversal condition it states.

Close the keyboard-fallback todo. Decide between `wont-fix` and promote-with-owner from the
evidence: it is explicitly non-blocking, it is not one of DEBT-02's five named behaviours, and
plan 15-08's live work may or may not have produced a bearing observation. If 15-08 saw the same
stall, record `wont-fix` with the reproduction cited and the plausible cause the todo already
names (the load needs uninterrupted real CPU time, and every inbound monitor byte re-halts the
CPU). If 15-08 saw the keyboard route work, that is new evidence — record it and close on that
basis instead. If neither, promote it with a named owner rather than closing it on inference.

Every one of the four must keep naming its own phase where it was serving as a disposition
source, and none may be deleted.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && node --test docs-review-disposition.test.ts && node --test docs-deferred-ledger.test.ts && npm run test:automated</automated>
  </verify>
  <acceptance_criteria>
    - All four todos are renamed, not deleted: each `completed/` path exists, each `pending/` path is gone, and `git status --porcelain` shows four renames.
    - Each of the four has exactly one `## Resolution` heading.
    - The Phase 08 Resolution covers ten findings: `for id in WR-04 WR-05 WR-06 WR-07 WR-08 WR-09 WR-10 WR-11 WR-12 WR-13; do grep -qw "$id" .planning/todos/completed/2026-08-21-phase-08-review-wr-04-through-wr-12-never-dispositioned.md || echo "MISSING $id"; done` prints nothing, and every row's Evidence cell is non-empty.
    - Every commit sha cited in any of the four resolves: extract them and run `git cat-file -e` on each; all exit 0.
    - Each `wont-fix` Resolution contains a rationale paragraph, not only a verdict line — assert non-trivially by word count or by eye and quote each in the SUMMARY.
    - `cd .claude/mcp/vice && node --test docs-review-disposition.test.ts` exits 0 — no finding lost its disposition when its todo moved.
    - STATE.md's `## Deferred Items` table and both prose count figures were updated in THIS task's commit for all four moves, and `cd .claude/mcp/vice && node --test docs-deferred-ledger.test.ts` exits 0 in both directions. Task 3 then regenerates the whole table from the directory as a final consistency pass; it does not substitute for updating it here.
    - `cd .claude/mcp/vice && npm run test:automated` exits 0.
  </acceptance_criteria>
  <done>Ten Phase 08 findings have per-finding verdicts with resolvable commits and named non-vacuity assertions, three todos are closed `wont-fix` with real rationales, and no disposition was lost in the moves.</done>
</task>

<task type="auto">
  <name>Task 2: Record every promotion with a named owner and flip the four requirements Complete</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the whole file. The `## Debt Disposition` checklist items for DEBT-01/02/03, the `## Audit Integrity` item for GATE-02, the `## Future Requirements` section at :99-113 with its `### Upstream Contributions` and `### Fork Backend Follow-on` subsections and the `### Promoted by DEBT-01` placeholder, and the Traceability table at :153-166.
    - The closure-note shape already in the file: `EXTV-03`'s note (a carve-out recorded explicitly, naming the decision that authorised it) and `FORK-02`'s note (what the branch taken did and did not require). These are the model for a requirement whose criterion was met by a promotion or a carve-out rather than a plain fix.
    - Every plan SUMMARY in `.planning/phases/15-debt-and-review-disposition/` — collect the complete promotion set. Expected, but verify against the SUMMARYs rather than trusting this list: the `vice_disk_attach` contract-redesign question (15-11); `13-REVIEW.md IN-02`'s code-review file-list scoping improvement (15-05); the warp-speed measurement the 2026-08-20 probe left open (15-09); the derive-the-capture-kind-automatically sub-item (15-07); conditionally, a negative A4 result's fix (15-10); conditionally, a new skill-corpus violation from 15-02.
    - `.planning/ROADMAP.md` Phase 15's four Success Criteria and Phase 16's goal — Phase 16's goal names `PKG-03` as covering orphaned planning references in source comments, and `PKG-01` is the payload relocation. Those are the named owners for the two todos that legitimately remain pending.
    - `ls .planning/todos/pending/` — the actual remaining set at this point. Every file in it must map to a named owner by the end of this task, and the mapping goes in the SUMMARY.
  </read_first>
  <action>
Fill in the `### Promoted by DEBT-01` subsection with one bullet per promoted item, in the
file's existing Future Requirements bullet shape (bold name, one-line summary, em-dash
rationale) and each ending with an explicit `Owner:` clause. A bullet without an owner is not a
promotion — it is a deferral with better formatting, which is the exact failure DEBT-01 exists
to stop. Collect the set from the phase's SUMMARYs, not from this plan's expectation.

Map every file still in `.planning/todos/pending/` to a named owner and record the mapping in
the SUMMARY. The two long-standing ones are owned by Phase 16 requirements — the payload
relocation by `PKG-01` and the orphaned source-comment pointers by `PKG-03` — so they need no
new Future Requirements bullet, only the mapping stated. Any file that maps to nothing gets a
Future Requirements bullet with an owner, here, now.

Then flip the four checklist items and the four Traceability rows to Complete, each with a
closure note where anything less than a plain fix happened, modelled on `EXTV-03`'s and
`FORK-02`'s existing notes:

- **GATE-02** — note that the guard's parser was widened in-phase (119 to 150 findings
  discovered), name the two heading shapes that had been invisible, and record that the guard
  runs green from a clean checkout. If any finding was disposed `superseded` rather than fixed,
  say which and why.
- **DEBT-01** — note the count moved and by how much, name every promotion and its owner, and
  state which items were `wont-fix` versus fixed. Do not claim the milestone-final count; Phase
  17 measures that after Phase 16 discharges `PKG-01`'s todo.
- **DEBT-02** — note where each of the five behaviours is documented, by file and heading. If
  the warp item's caveat landed in project-owned files rather than the tool description it
  corrects, say so and why the generated manifest was not edited.
- **DEBT-03** — note each scenario's verdict. If any scenario recorded `fail` or `partial`,
  the requirement's note must say so plainly and name the promoted fix; a requirement is
  Complete when its criterion is satisfied, and the criterion is "recorded pass or fail with
  evidence", not "passed".

Mark nothing Complete whose ROADMAP criterion is unmet. If one is, record the unmet part as a
closure note and leave the checkbox unticked, and say so in the SUMMARY so the orchestrator
sees it rather than discovering it at verification.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node .claude/gsd-core/bin/gsd-tools.cjs query requirements.ready-ids 2>/dev/null | head -20; cd .claude/mcp/vice && npm run test:automated</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\[x\] \*\*GATE-02' .planning/REQUIREMENTS.md`, and the same for DEBT-01, DEBT-02, DEBT-03, each return 1 — or, for any left unticked, the SUMMARY names it and the unmet criterion.
    - The Traceability rows agree with the checkboxes: `awk '/^\| GATE-02/ || /^\| DEBT-0[123]/' .planning/REQUIREMENTS.md` shows `Complete` for exactly the ones ticked above.
    - Every bullet under `### Promoted by DEBT-01` names an owner: `awk '/^### Promoted by DEBT-01/,/^## /' .planning/REQUIREMENTS.md | grep -c '^- ' ` equals the same range's `grep -c 'Owner:'`.
    - The placeholder sentence that previously stood in for the promotion list is gone: `test "$(grep -c 'lands here by construction' .planning/REQUIREMENTS.md || true)" = "0"`.
    - Every remaining pending todo is mapped: the SUMMARY contains a table of `pending file → named owner`, its row count equals `ls .planning/todos/pending/*.md | wc -l`, and no Owner cell is empty.
    - Each of the four requirements has a closure note where a carve-out, promotion, `superseded` verdict or non-passing UAT result occurred — quote each note in the SUMMARY.
    - `cd .claude/mcp/vice && npm run test:automated` exits 0.
  </acceptance_criteria>
  <done>Every promotion has a named owner, every remaining pending todo maps to one, and the four requirements are flipped with closure notes that state exactly what was and was not achieved.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the ledger and STATE.md, update ROADMAP Phase 15, and run the full gate</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/STATE.md` — the `### Pending Todos` prose paragraph (its count figure, which the file itself warns has gone stale twice and is not guarded), the `## Deferred Items` section's current-count paragraph and its table, and the `## Operator Next Steps` section.
    - `.claude/mcp/vice/docs-deferred-ledger.test.ts` — read it in full. It checks STATE.md's Deferred Items against `.planning/todos/pending/` in both directions; understand exactly what it matches on before editing rows, and whether it is sensitive to row order (if it is not, the ascending-filename ordering is still required by this plan's own must-have, for reproducibility).
    - `ls .planning/todos/pending/` and `ls .planning/todos/completed/` — the actual post-phase state. The table is derived from the first of these, not hand-maintained.
    - `.planning/ROADMAP.md` Phase 15's section — its `**Plans**: TBD` line and its four Success Criteria. Also its `**Requirements**` line, for the traceability cross-check.
    - Every plan SUMMARY in `.planning/phases/15-debt-and-review-disposition/` — for the Phase 15 Notes paragraph and for the criterion-by-criterion evidence.
    - `.planning/phases/15-debt-and-review-disposition/15-VALIDATION.md` — its sign-off checklist and `status: draft`. Note whether `/gsd-validate-phase` is expected to flip it; do not flip it here unless this plan is the one that satisfies its items.
  </read_first>
  <action>
Regenerate STATE.md's `## Deferred Items` table from `.planning/todos/pending/` — do not
hand-edit rows one at a time. List the files in ascending filename order (which is ascending
date order, given the naming convention) so an independent regeneration produces the same row
order, and add the one non-todo row for any remaining UAT gap only if one remains after plan
15-10; if `03-HUMAN-UAT.md` now has zero pending scenarios, that row is removed and its removal
is stated.

Then make the two prose counts equal the table's row count: the current-count paragraph
immediately above the table, and the `### Pending Todos` paragraph higher up. The file's own
text warns that this figure has gone stale twice and is not guarded — so state the arithmetic
explicitly in the paragraph (pending files plus any UAT-gap row equals the table's rows) rather
than just writing a number.

Update STATE.md's `## Current Position` for the phase close, and rewrite `## Operator Next
Steps` for what actually comes next: Phase 16 (`PKG-01`..`PKG-04`), then Phase 17 (`DEBT-04`,
`CORE-01`). Note that Phase 17's DEBT-04 measurement must be taken after Phase 16 discharges
`PKG-01`'s own todo, so this plan's count is the Phase-15 count and not the milestone-final one.
Add the phase's decisions to the Decisions list in the file's existing per-phase bullet style —
at minimum: the guard-parser widening and its two new invariants, the resolution of where the
warp caveat lives and why the generated manifest was not edited, the manifest todo's inverted
`wont-fix` on the D-16 ground, the decision to fix rather than promote the test-gate migration,
and each UAT scenario's verdict.

Update ROADMAP.md's Phase 15 section with `Edit` on the scoped region only — never a whole-file
`Write`, which destroys every phase entry outside the diff window. Replace `**Plans**: TBD`
with the real plan count and the plan list in the established checkbox form, and add a Notes
paragraph recording the criterion-by-criterion outcome plus the one discovery worth carrying
forward: the guard was green while blind to 31 findings across three phases, and the two shape
invariants added in plan 15-01 are what stop that recurring.

Finally, run the whole gate and record it: `npm run typecheck`, `npm run test:automated`, all
five `docs-*.test.ts` guards individually, and both skill-lint scripts plus the package
validator. Then run the phase's own no-silent-drop audit and record the result: for each of the
nine probe items this phase's planning surfaced, confirm it is represented either in a plan's
`must_haves.truths` or as a flagged assumption, and for each kept prohibition confirm it appears
in a plan's `must_haves.prohibitions`. Report the counts, not a claim.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && npm run typecheck && node --test docs-review-disposition.test.ts && node --test docs-deferred-ledger.test.ts && node --test docs-linerefs.test.ts && node --test docs-dangling-refs.test.ts && node --test docs-fork-decision.test.ts && npm run test:automated && cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-skill-tool-coverage.mjs && node scripts/check-skill-fork-honesty.mjs && node scripts/check-npm-packages.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd .claude/mcp/vice && node --test docs-deferred-ledger.test.ts` exits 0 in both directions.
    - The table's row count equals the pending file count plus any UAT-gap row: compute both in the SUMMARY and show they are equal.
    - Both prose counts equal that number: `grep -o '[0-9]\+ pending' .planning/STATE.md | sort -u` yields a single value that matches, and the SUMMARY quotes both paragraphs.
    - The table rows are in ascending filename order: `diff <(awk -F'|' '/^\| todo \|/{gsub(/ /,"",$3); print $3}' .planning/STATE.md) <(ls .planning/todos/pending/ | sed 's/\.md$//')` reports no differences.
    - `.planning/ROADMAP.md` Phase 15 no longer says `TBD`: `test "$(awk '/^### Phase 15:/,/^### Phase 16:/' .planning/ROADMAP.md | grep -c 'TBD' || true)" = "0"`, and its plan list has one checkbox line per committed plan file.
    - ROADMAP.md was edited scoped, not rewritten: `git diff --stat .planning/ROADMAP.md` shows changes confined to the Phase 15 section, and every other `### Phase` heading is byte-identical (`git diff .planning/ROADMAP.md | grep -c '^-### Phase'` returns 0 except for the Phase 15 line if it was reflowed).
    - All five planning-doc guards, both skill-lint scripts, the package validator, the typecheck and the automated gate all exit 0 — the full command in the verify block passes as one chain.
    - The no-silent-drop audit is recorded with counts: nine probe items accounted for (authored as truths plus flagged assumptions), and each kept prohibition located in a plan's `must_haves.prohibitions`.
    - `git log --oneline -1` shows this plan's commit and `git status --porcelain` is clean.
  </acceptance_criteria>
  <done>The ledger is regenerated and green in both directions with all three counts agreeing, ROADMAP's Phase 15 section is scoped-edited with real plan and outcome data, and every gate this phase touches passes in one chain.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols and files created by this plan:

- Bullets under `### Promoted by DEBT-01` in `.planning/REQUIREMENTS.md`, each with an `Owner:` clause
- Closure notes for GATE-02, DEBT-01, DEBT-02 and DEBT-03 in `.planning/REQUIREMENTS.md`
- A regenerated `## Deferred Items` table in `.planning/STATE.md`
- A Phase 15 Notes paragraph and plan list in `.planning/ROADMAP.md`
- `.planning/todos/completed/2026-08-21-phase-08-review-wr-04-through-wr-12-never-dispositioned.md` (renamed from `pending/`)
- `.planning/todos/completed/2026-08-12-vice-broker-tests-stall-outside-devcontainer.md` (renamed from `pending/`)
- `.planning/todos/completed/2026-08-20-vsf-as-a-bootstrap-input.md` (renamed from `pending/`)
- `.planning/todos/completed/2026-08-19-keyboard-fallback-load-does-not-progress-within-bounded-poll.md` (renamed from `pending/`)
</artifacts_this_phase_produces>

<flagged_assumptions>
## Flagged assumptions — probe items this phase could not resolve

The spec-less probe fallback surfaced nine edge items across this phase's four requirement
texts. Seven were resolved into `must_haves.truths` (four on GATE-02 in plan 15-01: parsed-id
de-duplication, the empty and token-bearing-but-zero-parse cases, the id terminator, and the
sorted failure list; three on DEBT-01 here and in plan 15-05: no incidental-phase-match
disposition, the ledger's both-direction health with the reduced set, and the table's
ascending-filename row order).

**Two remain UNRESOLVED and are flagged rather than answered.** Both were classified
`unclassified` by the probe — no adjacency, empty, ordering or encoding question applies to
their requirement text — and neither was auto-resolved with a backstop marker, because
inventing a predicate for a requirement whose edge shape the probe could not classify would
be fabrication, not coverage:

| Requirement | Probe | Why it stays unresolved | Consequence if it matters |
|---|---|---|---|
| DEBT-02 | `unclassified` — review manually | The requirement is "documented where a user would actually look", which is a placement judgment with no boundary, ordering or encoding edge to probe. `15-VALIDATION.md` independently records it as manual-only for the same reason. | If a placement is wrong, the automated gates cannot detect it. Plan 15-06 Task 3's three user-journey walks and the `<human-check>` blocks in plans 15-06 and 15-09 are the only detection, and they are human judgment. |
| DEBT-03 | `unclassified` — review manually | The requirement is "executed against real fixtures and a running program, recorded pass or fail with evidence". Its edge shape is the live emulator's own timing and single-client behaviour, which is not a data-boundary question the probe's categories cover. | If a live run is mis-recorded, only the quoted raw observations catch it. Plans 15-08 and 15-10 require before/after bytes and a `<human-check>` on every verdict for exactly this reason. |

Neither is dismissed and neither is silently dropped: 7 authored + 2 flagged = 9, matching the
probe report's `applicable: 9`.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan SUMMARYs → the phase record | This plan transcribes ten findings' verdicts from other plans' SUMMARYs; a transcription error becomes the milestone's evidence |
| `.planning/todos/pending/` ↔ STATE.md's Deferred Items table | The table is derived from the directory and guarded in both directions; the prose counts beside it are not guarded at all |
| requirement checkbox → milestone close | A requirement marked Complete over an unmet criterion is the failure mode this milestone was opened to stop |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-37 | Repudiation | a requirement marked Complete over an unmet ROADMAP criterion | high | mitigate | Closure notes modelled on `EXTV-03`/`FORK-02` are required wherever anything less than a plain fix happened; an unmet part leaves the checkbox unticked and is surfaced to the orchestrator in the SUMMARY rather than absorbed |
| T-15-38 | Repudiation | a promotion recorded without an owner, i.e. a silent carry into the next milestone | high | mitigate | Every Future Requirements bullet must carry an `Owner:` clause, asserted by counting bullets against owner clauses; every remaining pending file must map to a named owner in a SUMMARY table whose row count equals the directory listing |
| T-15-39 | Tampering | a whole-file rewrite of ROADMAP.md destroying phase entries outside the edit window | high | mitigate | Scoped `Edit` only, with an acceptance criterion asserting no other `### Phase` heading changed |
| T-15-40 | Repudiation | STATE.md's unguarded prose count disagreeing with its own guarded table | medium | mitigate | The table is regenerated from the directory in a deterministic order, both prose paragraphs are made to state the arithmetic explicitly, and the equality is computed and shown in the SUMMARY |
| T-15-SC | Tampering | npm/pip/cargo installs | — | accept | Not applicable — this plan installs no packages and adds no dependency; it edits planning documents only |
</threat_model>

<verification>
- `cd .claude/mcp/vice && npm run typecheck && npm run test:automated` both exit 0
- All five `docs-*.test.ts` guards exit 0 individually
- `node scripts/check-skill-tool-coverage.mjs`, `node scripts/check-skill-fork-honesty.mjs`, `node scripts/check-npm-packages.mjs` all exit 0
- `docs-deferred-ledger.test.ts` green in both directions; table row count equals pending file count plus any UAT-gap row; both prose counts equal it
- Every file remaining in `.planning/todos/pending/` maps to a named owner
- GATE-02, DEBT-01, DEBT-02, DEBT-03 each Complete-with-closure-note, or unticked with the unmet criterion named
- ROADMAP.md Phase 15 has no `TBD` and every other phase entry is byte-identical

<human-check>
Read `.planning/REQUIREMENTS.md`'s four new closure notes and confirm each states what was
actually achieved rather than what was intended — in particular that any `superseded` finding
verdict, any non-passing UAT scenario, and the reason the generated manifest was not edited are
all visible to someone reading only the requirement.
</human-check>
</verification>

<success_criteria>
- Every file remaining in `.planning/todos/pending/` is promoted with a named owner; nothing is carried silently
- Phase 08's ten findings each carry a verdict with a resolvable commit
- The deferred ledger is green in both directions and all three counts agree
- The four requirements are flipped only where their criteria are met, with closure notes where they were met by carve-out or promotion
- Every planning-doc guard, both skill-lint scripts and the package validator pass in one chain
</success_criteria>

<output>
Create `.planning/phases/15-debt-and-review-disposition/15-12-SUMMARY.md` when done.
Record: the ten-finding verdict table transcribed with its resolvable commits, each `wont-fix`
rationale quoted, the full promotion list with owners, the `pending file → named owner` mapping
table with its row count equal to the directory listing, the three agreeing ledger counts, the
four closure notes quoted, the no-silent-drop audit counts, and the full gate chain's output.
</output>
