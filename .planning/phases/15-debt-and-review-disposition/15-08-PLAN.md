---
phase: 15-debt-and-review-disposition
plan: 08
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - .claude/mcp/vice/stock-broker-live.test.ts
  - .planning/phases/15-debt-and-review-disposition/15-UAT-EVIDENCE.md
  - .planning/phases/03-direct-tools/03-HUMAN-UAT.md
autonomous: true
requirements: [DEBT-03]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`03-HUMAN-UAT.md` scenario 1 records `pass` or `fail` for all three of `vice_autostart`, `vice_disk_attach` and `vice_snapshot_load` against real fixtures on genuine stock VICE, each with cited evidence — a memory or register delta, not a bare verdict."
    - "`03-HUMAN-UAT.md` scenario 2 records `pass`, `fail` or `inconclusive` for `vice_keyboard_petscii` and `vice_joystick_set` against a program that is actually running, with the observed before/after bytes quoted."
    - "A `vice_snapshot_save` → `vice_snapshot_load` round trip is exercised live and its verdict is decided by a byte comparison of the restored region against the saved region, not by the absence of an error."
    - "`15-UAT-EVIDENCE.md` records the binary path and version, the tested artifact sha, the fixture provenance, and the raw tool payloads for every scenario step."
    - "Every emulator process and scratch directory this plan starts is confirmed gone at the end: no orphaned `x64sc`, no orphaned broker daemon, no leftover scratch directory."
    - "Scenario 2's joystick result is cross-referenced against Phase 13's A3 verdict (INCONCLUSIVE, zero CIA1 delta observed in two sessions), because a running-program precondition is one of A3's three named candidate explanations."
    - {statement: "If a scenario cannot be executed for an environmental reason, the scenario is recorded as blocked with the reason and the missing prerequisite named — never as pass, and never left pending.", verification: backstop}
  prohibitions:
    - "MUST NOT record `pass` for a scenario that was not actually executed, and MUST NOT soften a scenario's criteria to force a pass."
    - "MUST NOT open a second binary-monitor client against an instance under test — stock VICE's binary monitor services exactly one client, and a second connect sits unserviced with no reply and no EOF, indistinguishable from a wedge."
    - "MUST NOT leave an emulator process, a broker daemon, or a scratch directory orphaned after a live probe."
    - "MUST NOT start a nested headless agent session (`claude -p` or equivalent) from inside a live task — it stalls the executor and the stop signal is unreliable."
  artifacts:
    - .planning/phases/15-debt-and-review-disposition/15-UAT-EVIDENCE.md
  key_links:
    - "`stock-broker-live.test.ts` already dispatches `vice_disk_attach` + `vice_autostart` through `dispatchStock()` against a genuine-stock instance the real spawned broker granted, with a `.d64` built at runtime by `/usr/bin/c1541`. Scenario 1's missing third leg is `vice_snapshot_load`; do not build a parallel harness."
    - "The harness's own header records FINDING-D1: a raw machine-code program at `$0801` whose first two bytes are not a valid BASIC link pointer is overwritten by VICE's own relink during the simulated RUN. Any new fixture must reserve those two bytes as sacrificial, exactly as the existing one does."
    - "The binary monitor halts the emulated CPU on ANY inbound byte, and only the run command's EXIT resumes it — so a scenario needing a program to be *running* while input is injected must resume between injection and observation, or it observes a halted machine."
    - "Phase 13 A3 (`JOYPORT_SET` bit mapping) is INCONCLUSIVE with zero CIA1 delta in two sessions; its `[ASSUMED]` label is pinned by `assumption-label-discipline.test.ts`. Scenario 2 is the running-program variant A3 named as a candidate explanation."
---

<objective>
Execute Phase 03's UAT scenarios 1 and 2 live against genuine stock VICE and record each as
`pass`, `fail`, `inconclusive` or `blocked` with cited evidence — closing two of the three
rows that have sat `pending` in `03-HUMAN-UAT.md` since 2026-08-16.

Purpose: DEBT-03's criterion is execution with evidence, not a claim. Both scenarios are
live-testable in this environment: genuine unpatched stock VICE is at `/usr/bin/x64sc`
(the fork shadows bare `x64sc` earlier on `PATH`, so always use the absolute path),
`/usr/bin/c1541` builds a `.d64`, `acme` is available for a fixture, and a display is
present. Nothing blocks either scenario.

Scenario 1 is two-thirds already built: `stock-broker-live.test.ts` dispatches
`vice_disk_attach` and `vice_autostart` through the real broker against a real `.d64` and
proves the payload lands by polling memory. The missing leg is `vice_snapshot_load`.
Scenario 2 needs a program that visibly reacts to injected input, and carries a real
possibility of an honest negative result: Phase 13's A3 probe drove every single-bit
`JOYPORT_SET` value against fork 3.10 and observed zero delta at both CIA1 ports, twice.
A running-program precondition is one of the three explanations A3 recorded, so scenario 2
is that experiment — and if it also shows nothing, that is a `fail` or `inconclusive` to
record, not a result to massage.

Output: two recorded scenarios, one new evidence document, and a `vice_snapshot_load`
round-trip case in the existing live harness.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-debt-and-review-disposition/15-RESEARCH.md
@.planning/phases/15-debt-and-review-disposition/15-PATTERNS.md
@.planning/phases/15-debt-and-review-disposition/15-VALIDATION.md
@.planning/phases/03-direct-tools/03-HUMAN-UAT.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Add the vice_snapshot_load round trip to the live harness and run scenario 1 end to end</name>
  <files>.claude/mcp/vice/stock-broker-live.test.ts, .planning/phases/15-debt-and-review-disposition/15-UAT-EVIDENCE.md</files>
  <precondition>Genuine stock VICE exists and is executable at the absolute path `/usr/bin/x64sc`, `/usr/bin/c1541` exists and is executable, and a display is available (`DISPLAY` is set). Assert all three before starting; halt and record `blocked` with the missing prerequisite named if any is absent.</precondition>
  <read_first>
    - `.claude/mcp/vice/stock-broker-live.test.ts` — read the whole file, header included. It is the harness: `withBrokerHarness()`, the `.d64` builder using the absolute `/usr/bin/c1541`, `pollUntilBytesMatch()`, the `VERIFIED_PAYLOAD_ADDRESS` / `KERNAL_RELINK_BYTES` fixture design and the FINDING-D1 explanation for why the first two loaded bytes are sacrificial. Do not build a second harness.
    - `.claude/mcp/vice/stock-machine.ts` — `handleSnapshotSave` and `handleSnapshotLoad`: their argument shapes, what they return, and any documented approximation. Note `handleDiskAttach`'s `approximation` string too — plan 15-11 corrects it, and scenario 1's evidence should record what `vice_disk_attach` actually does to machine state, since that is the same behaviour.
    - `.claude/mcp/vice/test-gate.mjs` :83-92 — `stock-broker-live.test.ts` is `MANUAL_ONLY_TESTS` entry 7 and default-skips; the opt-in variable is `VICE_LIVE_STOCK_BIN`.
    - `.planning/phases/08-capability-honesty-and-the-install-story/08-HUMAN-UAT.md` — the frontmatter and per-test result shape `15-PATTERNS.md` names as the model: `status`, `resolved_by`, `tested_artifact_sha`, `tested_artifact_route`, `vice_version`, `evidence`, and per-test prose citing concrete observations.
    - `.claude/skills/vice-wedge-triage/SKILL.md` — the bounded-polling and recovery discipline for any live run, and how to tell a real wedge from a checkpoint stop.
    - `.planning/phases/13-external-verification/13-PROBE-RESULTS.md` § A5 — what `AUTOSTART` with the run flag clear actually does (full machine reset plus a program load). Scenario 1's `vice_disk_attach` leg should record that side effect rather than only whether the call succeeded.
  </read_first>
  <action>
Add one new case to `stock-broker-live.test.ts`, in the file's own established shape and
under its existing opt-in skip reason: after the `.d64` case's payload has landed, take a
snapshot with `vice_snapshot_save`, deliberately perturb a known memory region (write a
distinct byte pattern to an address the loaded program does not touch), then
`vice_snapshot_load` the saved file back and assert the perturbed region has returned to its
pre-perturbation bytes AND the program's verified payload region still matches. Both halves
matter: the first proves the load restored state, the second proves it did not restore some
*other* machine.

Write the snapshot to the harness's existing scratch directory so the existing teardown
removes it. Reuse `pollUntilBytesMatch()` rather than adding a new wait primitive, and
remember the monitor halts the CPU on every inbound byte — resume via the run command where
the existing cases do.

Run the whole file opted in with the absolute stock path, and capture the raw tool payloads
the existing cases already log plus the new case's before/after byte arrays.

Then create `15-UAT-EVIDENCE.md` and record scenario 1's three legs. Frontmatter modelled on
`08-HUMAN-UAT.md`: the binary's absolute path and its self-reported version string, the
tested artifact sha (`git rev-parse HEAD`), the route, and the fixture provenance (the `.d64`
was built at run time by the absolute-path `c1541` from the harness's synthetic program —
say so, with the byte layout, so a reader knows it is not a copyrighted image). Body: one
subsection per tool, each stating the call made, the raw payload returned, the observed
memory or register delta, and the verdict. For `vice_disk_attach`, record explicitly whether
the machine was reset and whether a program loaded, because Phase 13's A5 says it does both
despite the advertised contract.

Before finishing, confirm cleanup: no `x64sc` process remains that this run started, no
broker daemon remains, and the scratch directory is gone. Record the commands and their
output.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && npm run typecheck && VICE_LIVE_STOCK_BIN=/usr/bin/x64sc node --test stock-broker-live.test.ts && node --test stock-broker-live.test.ts && npm run test:automated</automated>
  </verify>
  <acceptance_criteria>
    - The opt-in run exits 0 with one more passing test than before this task, and its log contains the new case's before/after byte arrays.
    - The default (non-opt-in) run still skips every case: `cd .claude/mcp/vice && node --test stock-broker-live.test.ts` exits 0 with all tests skipped, so the automated gate is unaffected.
    - `cd .claude/mcp/vice && npm run test:automated` exits 0.
    - `.planning/phases/15-debt-and-review-disposition/15-UAT-EVIDENCE.md` exists and its frontmatter contains a `vice_version` value that matches the output of running the binary's own version flag in this session, plus a `tested_artifact_sha` that `git cat-file -e` resolves.
    - The evidence document names all three of `vice_autostart`, `vice_disk_attach` and `vice_snapshot_load`, each with a raw payload and an observed delta.
    - The snapshot round trip's verdict rests on a byte comparison: the document quotes the perturbed region's bytes before the perturbation, after it, and after the load.
    - No orphan remains: `pgrep -af x64sc` and `pgrep -af vice-broker` show nothing this run started, and the scratch directory path recorded in the evidence no longer exists. Both commands' output is in the SUMMARY.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Scenario 1's three tools are exercised end to end against genuine stock VICE through the real broker, the snapshot round trip is decided by bytes, and the evidence document cites binary version, artifact sha, fixture provenance and raw payloads.</done>
</task>

<task type="auto">
  <name>Task 2: Build a reacting program and run scenario 2's keyboard and joystick injection</name>
  <files>.claude/mcp/vice/stock-broker-live.test.ts, .planning/phases/15-debt-and-review-disposition/15-UAT-EVIDENCE.md</files>
  <precondition>`acme` is on `PATH` or at `~/.local/bin/acme` and assembles a minimal source file, and genuine stock VICE at `/usr/bin/x64sc` is executable. Assert both before starting; halt and record `blocked` naming the missing prerequisite if either is absent.</precondition>
  <read_first>
    - `.planning/phases/03-direct-tools/03-HUMAN-UAT.md` scenario 2 — the expectation is that raw PETSCII bytes and joystick bit patterns are *observed to affect a running program*, and the stated reason it was deferred is that no running program existed to observe against.
    - `.claude/mcp/vice/stock-input.ts` — `vice_keyboard_petscii`'s and `vice_joystick_set`'s argument shapes, and the `JOYPORT_BITS` constant that still carries its `[ASSUMED]` label. Read the label's wording; it names what is unverified.
    - `.planning/phases/13-external-verification/13-PROBE-RESULTS.md` § A3 — the full negative result: every single-bit value driven individually, both CIA1 port bytes read before and after, zero delta across two independent sessions, and the three candidate explanations (no CIA1 effect on that build, a different wire port value, or a running-program precondition). Scenario 2 is the third.
    - `.claude/mcp/vice/assumption-label-discipline.test.ts` — read it before considering any label change. It is an all-or-nothing guard; understand exactly what it permits before touching a label, and if a single-assumption removal is not permitted, do not attempt one.
    - `.claude/skills/acme-build/SKILL.md` and `.claude/skills/acme-build/template.a` — how to assemble here, and the known library gap (the scaffold's own template needs a library this host may not have; a hand-rolled source with literal hardware addresses avoids it, which is the route Phase 8.1 took by explicit decision).
    - `.claude/mcp/vice/stock-broker-live.test.ts`'s FINDING-D1 header paragraph — the two sacrificial bytes any autostarted program must reserve at its load address.
  </read_first>
  <action>
Write a minimal ACME source, in this repo's scratch area, for a program that loops forever
doing exactly two observable things: copy the byte it reads from the keyboard buffer (or the
current-key register, whichever `vice_keyboard_petscii` actually drives — read
`stock-input.ts` and decide from the code, not from the tool name) into a fixed screen-memory
cell, and copy the CIA1 port byte the joystick maps to into a second fixed screen-memory
cell. Hand-roll the hardware addresses as literals rather than including a C64 library — the
library gap on this host is a recorded Phase 8.1 finding and working around it by hand was an
explicit decision there. Reserve the two sacrificial bytes at the load address per FINDING-D1.

Assemble it, autostart it through the same harness Task 1 used, and confirm it is genuinely
running before injecting anything — poll the observation cell and require it to change on its
own, or check the program counter is inside the loop. A scenario 2 result obtained against a
halted machine is worthless, and the monitor halts the CPU on every inbound byte.

Then, resuming between each step: inject a PETSCII byte and read the first observation cell;
drive each joystick bit value and read the second. Record the before and after bytes for
every injection, not a summary.

Record the verdict honestly. If the keyboard half shows the injected byte and the joystick
half shows nothing, that is `partial` — record it as such, and say in the evidence that this
reproduces A3's zero-delta result under a running program, which eliminates the
running-program candidate explanation and leaves the other two. That is a genuinely valuable
negative result and it must not be dressed up. If the joystick half DOES show a delta, record
the exact bit-to-port mapping observed, note that it contradicts A3's INCONCLUSIVE verdict
under a running program, and hand the `[ASSUMED]` label question to plan 15-12 rather than
editing the label here — the guard is all-or-nothing and a Phase 13 verified artifact depends
on it.

Add the scenario-2 case to `stock-broker-live.test.ts` only if it can be expressed as a
deterministic assertion; if the result is a measurement rather than a pass/fail (which the
joystick half may well be), keep it out of the test file and record it in the evidence
document only, and say in the SUMMARY why. Do not commit a test that asserts whatever
happened to be observed.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && npm run typecheck && node --test stock-broker-live.test.ts && npm run test:automated</automated>
    <human-check>
Read `15-UAT-EVIDENCE.md`'s scenario 2 section and confirm the verdict matches the observed
bytes — specifically that a zero-delta joystick observation is recorded as a negative result
with its A3 cross-reference, and not as a pass.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The evidence document's scenario 2 section quotes, for every injection, the observation cell's byte before and after — no summarised "input was observed" prose.
    - The running-state proof is recorded: either the observation cell changed on its own before any injection, or a program-counter read placed execution inside the program's loop. Quote it.
    - The ACME source is recorded verbatim in the evidence document (it is a handful of lines) so the observation cells and hardware addresses are auditable, and the assembler's own version banner is recorded.
    - The joystick result is cross-referenced to A3 by name, and states which of A3's three candidate explanations the result eliminates or supports.
    - If a joystick delta was observed, the evidence names the exact bit-to-port mapping and the SUMMARY hands the `[ASSUMED]` label question to plan 15-12; `git diff --stat .claude/mcp/vice/stock-input.ts` is empty either way.
    - `cd .claude/mcp/vice && node --test stock-broker-live.test.ts` exits 0 with everything skipped by default, and `npm run test:automated` exits 0.
    - No orphan remains: `pgrep -af x64sc` and `pgrep -af vice-broker` output is recorded and shows nothing from this run.
  </acceptance_criteria>
  <done>Scenario 2 is executed against a program proven to be running, every injection's before/after bytes are recorded, and the verdict — including a negative one — is stated with its A3 cross-reference.</done>
</task>

<task type="auto">
  <name>Task 3: Record scenarios 1 and 2 in 03-HUMAN-UAT.md with the established frontmatter and result shape</name>
  <files>.planning/phases/03-direct-tools/03-HUMAN-UAT.md</files>
  <read_first>
    - `.planning/phases/03-direct-tools/03-HUMAN-UAT.md` — the current file: `status: partial`, three tests each with `expected:` / `why_human:` / `result: [pending]`, and a `## Summary` block with counts.
    - `.planning/phases/08-capability-honesty-and-the-install-story/08-HUMAN-UAT.md` — the target shape: frontmatter with `status`, `resolved_by`, `driven_by`, `tested_artifact_sha`, `tested_artifact_route`, `vice_version`, `evidence`; and per-test `result:` prose that cites concrete observations rather than a bare verdict.
    - `.planning/phases/15-debt-and-review-disposition/15-UAT-EVIDENCE.md` — this plan's own evidence document, which the `evidence:` frontmatter key must name.
  </read_first>
  <action>
Fill in scenarios 1 and 2's `result:` fields with the verdict plus a short evidence-citing
paragraph each, pointing at the corresponding section of `15-UAT-EVIDENCE.md` and quoting the
one or two observations that decide the verdict. Follow `08-HUMAN-UAT.md`'s per-test prose
style — a verdict word, then the specific bytes, registers or log lines behind it.

Add the frontmatter keys the established shape uses: `resolved_by` naming Phase 15 plan
15-08, `driven_by` naming how it was driven (an agent-run opt-in live test suite, not a human
at a keyboard — say which, honestly), `tested_artifact_sha`, `tested_artifact_route`,
`vice_version` for the genuine stock binary, and `evidence` naming this plan's evidence
document.

Leave scenario 3's `result:` as `[pending]` — plan 15-10 owns it — and leave the top-level
`status:` as `partial` for the same reason. Update the `## Summary` counts to reflect exactly
what is now decided (two scenarios closed, one still pending), so the file's own arithmetic
is correct at this point rather than anticipating plan 15-10.

Update `updated:` to today's date.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && test "$(grep -c 'result: \[pending\]' .planning/phases/03-direct-tools/03-HUMAN-UAT.md)" = "1" && cd .claude/mcp/vice && npm run test:automated</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one `result: [pending]` remains in `03-HUMAN-UAT.md` (scenario 3, owned by plan 15-10).
    - Scenarios 1 and 2 each have a `result:` line beginning with a verdict word and followed by at least two sentences citing concrete observations.
    - The frontmatter contains `resolved_by`, `driven_by`, `tested_artifact_sha`, `tested_artifact_route`, `vice_version` and `evidence`, and `evidence` names `15-UAT-EVIDENCE.md`.
    - `tested_artifact_sha`'s value resolves: `git cat-file -e <sha>` exits 0.
    - The `## Summary` block's counts sum to 3 and show exactly 1 pending.
    - `status:` is still `partial` and `updated:` is today's date.
    - `cd .claude/mcp/vice && npm run test:automated` exits 0.
  </acceptance_criteria>
  <done>Two of three scenarios are recorded with verdicts and cited evidence, the file's own counts are internally consistent, and scenario 3 is left explicitly for plan 15-10.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols and files created by this plan:

- `.planning/phases/15-debt-and-review-disposition/15-UAT-EVIDENCE.md` — new evidence document
- A `vice_snapshot_save` → `vice_snapshot_load` round-trip case in `.claude/mcp/vice/stock-broker-live.test.ts`
- A minimal hand-rolled ACME source for the input-observation program (recorded verbatim in
  the evidence document; not committed as a fixture unless Task 2 commits a deterministic test)
- New frontmatter keys in `.planning/phases/03-direct-tools/03-HUMAN-UAT.md`:
  `resolved_by`, `driven_by`, `tested_artifact_sha`, `tested_artifact_route`, `vice_version`, `evidence`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this plan ↔ a real emulator process on the host | Live probes spawn and kill real processes and write real files outside the repo's scratch area if not contained |
| one binary-monitor socket ↔ one client | Stock VICE's monitor services exactly one client; a second connect is unserviced with no reply and no EOF |
| recorded UAT verdict → the milestone record | A `pass` recorded without execution becomes the evidence a future close relies on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-26 | Denial of service | a second monitor client against an instance under test | high | mitigate | Reuse the existing harness's single-session discipline (`clearHeldStockSession()` plus one granted instance per case); never open an ad-hoc second connection, and treat an unanswered connect as the documented single-client condition rather than a wedge |
| T-15-27 | Repudiation | a UAT verdict recorded without execution, or softened to pass | high | mitigate | Every verdict must quote before/after bytes or a register delta; a zero-delta joystick observation is explicitly required to be recorded as a negative result with its A3 cross-reference |
| T-15-28 | Denial of service | orphaned `x64sc` or broker daemon processes accumulating on the host | medium | mitigate | The harness's existing teardown plus explicit process and scratch-directory checks as acceptance criteria, with the command output recorded |
| T-15-29 | Information disclosure | committed evidence containing host paths | low | accept | The evidence records absolute binary paths (`/usr/bin/x64sc`, `/usr/bin/c1541`), which are already published throughout this repo's docs and CLAUDE.md; no credential, no port beyond loopback, no user data |
| T-15-SC | Tampering | npm/pip/cargo installs | — | accept | Not applicable — this plan installs no packages. `acme` and `c1541` are pre-existing system binaries addressed by absolute path or documented location |
</threat_model>

<verification>
- `cd .claude/mcp/vice && npm run typecheck && npm run test:automated` both exit 0
- `cd .claude/mcp/vice && node --test stock-broker-live.test.ts` skips everything by default
- The opt-in run against `/usr/bin/x64sc` exits 0 with the new snapshot round-trip case passing
- `03-HUMAN-UAT.md` has exactly one remaining `result: [pending]`
- `pgrep -af x64sc` and `pgrep -af vice-broker` show no process this plan started

<human-check>
Read `15-UAT-EVIDENCE.md` end to end and confirm every verdict is traceable to a quoted
observation, and that any negative or partial result is stated as such rather than framed as
a success.
</human-check>
</verification>

<success_criteria>
- Two of Phase 03's three UAT scenarios are executed live and recorded with cited evidence
- The snapshot round trip is decided by a byte comparison, not by the absence of an error
- A negative joystick result, if that is what happens, is recorded honestly and cross-referenced to A3
- No emulator process, broker daemon or scratch directory is left behind
</success_criteria>

<output>
Create `.planning/phases/15-debt-and-review-disposition/15-08-SUMMARY.md` when done.
Record: the binary version strings measured this session, the artifact sha, every scenario's
verdict with its deciding observation, the ACME source and assembler banner, the
process-and-scratch cleanup command output, and — if the joystick half produced a delta — the
observed bit-to-port mapping handed to plan 15-12 for the `[ASSUMED]` label decision.
</output>
