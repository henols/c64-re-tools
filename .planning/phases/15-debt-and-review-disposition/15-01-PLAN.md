---
phase: 15-debt-and-review-disposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/mcp/vice/docs-review-disposition.test.ts
  - .claude/mcp/vice/fixtures/planted-review-fixture.md
  - .planning/todos/completed/2026-08-22-phase-14-review-in-01-fixed-in-phase-never-cited.md
  - .planning/todos/pending/2026-08-22-phase-03-review-wr-06-through-in-06-never-dispositioned.md
  - .planning/STATE.md
autonomous: true
requirements: [GATE-02]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`node --test docs-review-disposition.test.ts` is green, and its parser discovers 150 findings across the 14 `*-REVIEW.md` files under `.planning/phases/` (119 before this plan)."
    - "Every `*-REVIEW.md` that contains at least one `(WR|IN|CR)-NN` token yields at least one parsed finding — the blind spot that hid 31 findings across three phases cannot recur silently."
    - "Every finding id that appears immediately after a heading marker in a `*-REVIEW.md` is in the parsed set, so a fourth heading shape fails the guard loudly instead of vanishing."
    - "A finding id declared twice in the same `*-REVIEW.md` under two different heading styles yields exactly ONE finding, not two — parsed ids are de-duplicated per (phase directory, review file, id)."
    - "A `*-REVIEW.md` containing zero finding-id tokens contributes zero findings and does not fail the guard; a `*-REVIEW.md` that contains id tokens but parses to zero findings fails it."
    - "The parser's id terminator is `not a digit`, so `### IN-01 (Info) — …`, `#### WR-06:`, a heading whose id is followed directly by an em-dash, and a bare `#### IN-05` at end-of-line all parse — while `WR-011` never parses as `WR-01`."
    - "The undispositioned-findings failure list is sorted by (phase number, review file, id), so two runs against the same tree emit byte-identical failure text regardless of `readdirSync` order."
    - "`14-REVIEW.md`'s `IN-01` carries a cited disposition in a recognised source that also names `14-REVIEW.md` by filename."
    - "`03-REVIEW.md`'s `WR-06`, `WR-07`, `WR-08`, `IN-02`, `IN-03`, `IN-04`, `IN-05`, `IN-06` are all named in one pending todo that names `03-REVIEW.md` by filename, so the guard is green at the end of this plan and plan 15-04 has a single tracked work item."
  prohibitions:
    - "MUST NOT weaken, narrow, skip, or delete any assertion in `docs-review-disposition.test.ts` in order to make it green."
    - "MUST NOT delete or reword a `*-REVIEW.md` finding heading so the parser stops seeing it — the parser widens to the document, never the reverse."
    - "MUST NOT record a finding as fixed unless the fix has landed and the disposition cites the commit or the re-runnable check that proves it."
  artifacts:
    - .claude/mcp/vice/docs-review-disposition.test.ts
    - .claude/mcp/vice/fixtures/planted-review-fixture.md
    - .planning/todos/completed/2026-08-22-phase-14-review-in-01-fixed-in-phase-never-cited.md
    - .planning/todos/pending/2026-08-22-phase-03-review-wr-06-through-in-06-never-dispositioned.md
  key_links:
    - "**STATE.md ledger ownership.** `docs-deferred-ledger.test.ts` is a strict two-directional invariant between `## Deferred Items` and `.planning/todos/pending/`, and it is IN the automated gate — so any plan that files or moves a todo MUST update that table in the same commit, or `npm run test:automated` goes red. That is why `.planning/STATE.md` is declared in this plan's `files_modified` and why the ledger-touching plans of this phase run in separate waves: two concurrent plans editing that one table would collide."
    - "`parseFindingIds()` → `scanAllReviewFindings()` → `undispositionedFindings()`: widening the first without keeping the other two's de-duplication and ordering contracts turns one real defect into nine duplicated failures."
    - "`todoMentionsPhase()` requires a todo to name `<N>-REVIEW.md` or `Phase N`; a disposition todo that names neither is invisible to the guard even though a human reads it fine."
    - "`audit-gate.mjs` spawns the four `docs-*.test.ts` guards; a red `docs-review-disposition.test.ts` blocks any milestone audit recording `status: passed` (GATE-01)."
---

<objective>
This is the phase's tracer: take ONE review finding all the way through the full
disposition pipeline — invisible to the guard → guard parser widened so it is seen →
guard goes red on it → disposition recorded in this repo's established shape → guard
green → committed — and leave the guard's newly-widened ground truth in place for the
eleven expansion plans that fan the same path across the remaining findings and todos.

Purpose: `docs-review-disposition.test.ts` is the instrument GATE-02's own success
criterion names. It is green today **for the wrong reason**: its heading regex
(`^### (WR|IN|CR)-(\d+):`) only matches level-3 headings ending in a colon, so it sees
119 of the 150 findings that exist. `03-REVIEW.md` (14 findings, all `####`),
`05-REVIEW.md` (16 findings, all `####`) and `14-REVIEW.md` (1 finding,
`### IN-01 (Info) — …`) are wholly or partly invisible to it. Until this is fixed, no
disposition work in this phase can be verified by the gate the requirement cites — and
worse, "green" means "the parser saw nothing", which is a smaller, literal repeat of
the AUDIT-01 defect this guard was built to catch.

Measured live at plan time against the tree at `3f0089f`: narrow regex → 119 findings /
0 undispositioned; widened regex → 150 findings / **9 undispositioned** (8 in
`03-REVIEW.md`, 1 in `14-REVIEW.md`). `05-REVIEW.md`'s 16 newly-visible findings are
all already dispositioned via `05-REVIEW-FIX.md` — they need no work, only discovery.

Output: a widened, shape-guarded parser; a cited disposition for `14-REVIEW.md`'s
`IN-01`; and one pending todo enumerating `03-REVIEW.md`'s eight, which is plan 15-04's
work item.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-debt-and-review-disposition/15-RESEARCH.md
@.planning/phases/15-debt-and-review-disposition/15-PATTERNS.md
@.claude/mcp/vice/docs-review-disposition.test.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Widen the disposition guard's finding parser and pin the two shape invariants</name>
  <files>.claude/mcp/vice/docs-review-disposition.test.ts, .claude/mcp/vice/fixtures/planted-review-fixture.md</files>
  <read_first>
    - `.claude/mcp/vice/docs-review-disposition.test.ts` — full file (334 lines). `parseFindingIds()` is at :91, `scanAllReviewFindings()` at :100, `undispositionedFindings()` at :243, the positive-control test at :271, the planted-violation test at :291, the planted-false-negative test at :319. The 45-line header documents the five disposition sources and the deliberate "mentioned anywhere" weakness — read it before touching anything so the new comment matches its voice.
    - `.claude/mcp/vice/fixtures/planted-review-fixture.md` — the committed synthetic-heading fixture the planted-violation test parses.
    - `.claude/mcp/vice/docs-linerefs.test.ts` and `.claude/mcp/vice/docs-dangling-refs.test.ts` — sibling guards; copy their `repoRoot({ from: HERE })` + `readdirSync`-derived, never-hardcode-a-list idiom for any new helper.
    - `.planning/phases/03-direct-tools/03-REVIEW.md` lines 77-330 (heading shapes only — every finding heading is level 4) and `.planning/phases/14-backend-decision/14-REVIEW.md` line 35 (the level-3, no-colon, em-dash shape). These two files are the motivating defects the new comment must cite.
    - `.planning/phases/05-skill-critical-derived-tools/05-REVIEW.md` line 308 — `#### WR-04:`'s own heading text contains the cross-reference `CR-01/CR-02`. This is why a declaration must be anchored immediately after the heading marker: a permissive "any id on a heading line" rule false-positives here.
  </read_first>
  <action>
Replace `parseFindingIds()`'s regex with one that accepts any heading level 2 through 6, requires at least one space after the marker, and terminates the id on any non-digit — `/^#{2,6} +(WR|IN|CR)-(\d+)(?![0-9])/gm`. Measured at plan time: this yields exactly the same 150-finding set as the narrower `#{3,4} ... [:\s]` shape the research proposed, and is additionally robust to a level-2 or level-5 heading, to an id followed directly by an em-dash, and to an id at end-of-line — none of which the research's shape accepts. Return de-duplicated ids (preserve first-seen order) so one id declared under two heading styles in the same file produces ONE finding, not two duplicated failure rows.

Add a second exported-for-test helper alongside it — name it `declaredFindingIdsInHeadings()` — that collects every id appearing immediately after a heading marker at any level via `/^#{1,6} +((?:WR|IN|CR)-\d+)\b/gm`. This is the shape-drift detector: it is deliberately anchored at the marker, not anywhere on the heading line, so a cross-reference inside a heading's prose does not count as a declaration.

Sort `undispositionedFindings()`'s return value by (`phaseNum` numerically-then-lexically, `reviewFile`, `id`) before returning, so the assertion's failure text is byte-identical across runs and platforms regardless of `readdirSync` order.

Extend the header comment block with a new paragraph in the file's existing "WHY THIS EXISTS" voice, naming the two real defects that motivated the widening by file and id — `03-REVIEW.md`'s level-4 headings (14 findings, 8 of them undispositioned) and `14-REVIEW.md`'s `IN-01` level-3-no-colon heading — plus the measured before/after counts (119 → 150) and the fact that the guard was green while blind. Cite plan 15-01 the way the existing header cites AUDIT-01 and Phase 10/11.

Append two synthetic entries to `fixtures/planted-review-fixture.md` so both new shapes are regression-tested against a committed fixture rather than only against real files that could later be edited: a level-4 heading declaring `WR-98`, and a level-3 heading declaring `IN-97` followed by a parenthesised word and an em-dash (the Phase 14 shape). Keep the existing `WR-99` entry untouched — the planted-violation test asserts on it.

Add four new assertions:
(a) extend the existing positive-control test with named anchors for `03-REVIEW.md` → `WR-06` in the `03-direct-tools` phase directory and `14-REVIEW.md` → `IN-01` in `14-backend-decision`, using the same `hasAnchor()` idiom already at :275-289, and raise its floor from `>= 100` to `>= 150`;
(b) a shape-coverage test asserting that for every `*-REVIEW.md`, `declaredFindingIdsInHeadings()` is a subset of the parsed id set — a fourth heading shape then fails here by name instead of disappearing;
(c) a non-emptiness test asserting that any `*-REVIEW.md` whose raw text contains at least one `(WR|IN|CR)-\d+` token parses to at least one finding — this is the exact assertion that would have caught the `03`/`05`/`14` blind spot on the day it appeared;
(d) a fixture-driven test asserting the parser finds `WR-98` and `IN-97` in the planted fixture, and that a synthetic `WR-980` token does not parse as `WR-98`.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && npm run typecheck && node --test docs-review-disposition.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd .claude/mcp/vice && grep -c '#{2,6}' docs-review-disposition.test.ts` returns at least 1.
    - `cd .claude/mcp/vice && grep -c 'declaredFindingIdsInHeadings' docs-review-disposition.test.ts` returns at least 2 (definition plus at least one call site).
    - `cd .claude/mcp/vice && grep -c 'WR-98' fixtures/planted-review-fixture.md` and `grep -c 'IN-97' fixtures/planted-review-fixture.md` each return at least 1.
    - `cd .claude/mcp/vice && node --test docs-review-disposition.test.ts` reports at least 7 tests, `# fail 0`, and its own output includes the widened total — assert the count in a test message so the number is visible in the run, not only in a passing boolean.
    - The parsed total is exactly 150: `cd .claude/mcp/vice && node -e 'import("./docs-review-disposition.test.ts")'` is not the check — instead the positive-control test's floor assertion is `>= 150` and passes.
    - `cd /home/henrik/dev/henrik/git/c64-re-tools && grep -cE '^#{1,6} +(WR|IN|CR)-[0-9]+' .planning/phases/03-direct-tools/03-REVIEW.md` returns 14 (the headings were not edited).
    - `npm run typecheck` exits 0.
    - The guard is expected to be RED after this task — exactly 9 undispositioned findings, reported in sorted order. Record that failure text in the plan SUMMARY verbatim; it is the tracer's evidence that widening the parser actually changed what the instrument can see. Tasks 2 and 3 return it to green.
  </acceptance_criteria>
  <done>The parser sees 150 findings, both shape invariants are asserted against a committed fixture, the failure list is deterministically ordered, and the guard reports exactly the 9 findings measured at plan time.</done>
</task>

<task type="auto">
  <name>Task 2: Record the cited disposition for 14-REVIEW.md's IN-01</name>
  <files>.planning/todos/completed/2026-08-22-phase-14-review-in-01-fixed-in-phase-never-cited.md</files>
  <read_first>
    - `.planning/phases/14-backend-decision/14-REVIEW.md` — the whole file is short; read `### IN-01 (Info) — …` at :35 and the `## Disposition` section at :81, which claims the finding was "fixed in this phase rather than deferred".
    - `.claude/mcp/vice/fork-live.test.ts` lines 75-95 — the skip-reason sentence the finding is about. Confirm the corrected wording is present before writing that the fix landed.
    - `.planning/todos/completed/2026-08-20-fully-remove-the-forked-vice-mcp-backend.md` — the larger completed-todo shape: frontmatter with `resolves_phase`, `files:` entries using `path:line-range` citations, and a `## Resolution` section.
    - `.planning/todos/completed/2026-08-21-anno-cli-wr-08-option-values-silently-swallowed.md` — the smaller completed-todo shape (`## Problem` / `## Why it was deferred` / `## What to do`).
    - `.claude/mcp/vice/docs-review-disposition.test.ts` :133-146 (`todoMentionsPhase()`) — the file must contain the literal filename `14-REVIEW.md` or a word-bounded `Phase 14` for the guard to count it.
  </read_first>
  <action>
Create the completed todo directly in `.planning/todos/completed/` — this finding's code fix already landed inside Phase 14, so there is no pending state to pass through; the missing artifact is the citation, not the work. Do NOT retro-edit `14-VERIFICATION.md`: it is a sealed verification report and appending a finding disposition to it after the fact is the weaker of the two shapes `15-PATTERNS.md` offers.

Frontmatter: `created` (today), `title` naming `14-REVIEW.md IN-01`, `area: testing`, `priority: low`, `resolves_phase: 15`, and a `files:` list citing `.claude/mcp/vice/fork-live.test.ts` with the real line range of the corrected sentence and `.planning/phases/14-backend-decision/14-REVIEW.md` with `IN-01`'s heading line.

Body sections, following the completed-todo analogs:
- `## Problem` — quote `IN-01`'s issue in one short paragraph: the skip-reason sentence described behaviour the code did not have. State plainly that the finding was invisible to `docs-review-disposition.test.ts` before plan 15-01 widened its parser, because its heading is level-3 with no colon after the id.
- `## Why it had no citation` — the structural cause the ROADMAP names as its fourth instance: the code-review gate runs after the last plan's SUMMARY, so no plan of Phase 14 could disposition Phase 14's own findings. Name `.planning/todos/pending/2026-08-22-phase-13-review-wr-01-wr-02-in-01-in-02-never-dispositioned.md` as the todo that already carries the transferable analysis, so this file does not duplicate it.
- `## Resolution` — bold one-line verdict `**Fixed in Phase 14, cited here.**`, then the citation: the corrected sentence's current location in `fork-live.test.ts`, the commit that landed it (find it with `git log -S` on a distinctive fragment of the corrected sentence), and the date/plan closing the record (2026-08-22, Phase 15 plan 15-01). Name the re-runnable check a reader can use: `node --test fork-live.test.ts` skips by default (it is `MANUAL_ONLY_TESTS` entry 8) so the citation is the source line plus the commit, not a test run — say so rather than implying a green test proves it.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && node --test docs-review-disposition.test.ts 2>&1 | grep -c '14-REVIEW.md (14-backend-decision): IN-01' </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/todos/completed/2026-08-22-phase-14-review-in-01-fixed-in-phase-never-cited.md` exists and its text contains both the literal `14-REVIEW.md` and the literal `IN-01`.
    - `grep -c '## Resolution' .planning/todos/completed/2026-08-22-phase-14-review-in-01-fixed-in-phase-never-cited.md` returns 1.
    - The file cites a real commit sha: `git cat-file -e $(grep -oE '\b[0-9a-f]{7,40}\b' .planning/todos/completed/2026-08-22-phase-14-review-in-01-fixed-in-phase-never-cited.md | head -1)` exits 0.
    - `cd .claude/mcp/vice && node --test docs-review-disposition.test.ts 2>&1 | grep -c '14-backend-decision): IN-01'` returns 0 — the finding no longer appears in the undispositioned list.
    - The guard is still red on 8 findings (all in `03-REVIEW.md`) after this task; Task 3 closes that.
  </acceptance_criteria>
  <done>`14-REVIEW.md`'s `IN-01` has a dated, commit-citing disposition in a source the guard recognises, and it drops out of the undispositioned list.</done>
</task>

<task type="auto">
  <name>Task 3: File the tracked work item for 03-REVIEW.md's eight newly-surfaced findings and return the guard to green</name>
  <files>.planning/todos/pending/2026-08-22-phase-03-review-wr-06-through-in-06-never-dispositioned.md</files>
  <read_first>
    - `.planning/phases/03-direct-tools/03-REVIEW.md` — read each of the eight findings in full: `WR-06` at :217, `WR-07` at :234, `WR-08` at :262, `IN-02` at :291, `IN-03` at :301, `IN-04` at :311, `IN-05` at :318, `IN-06` at :326. Each carries its own cited location and, for several, a written fix.
    - `.planning/todos/pending/2026-08-21-phase-08-review-wr-04-through-wr-12-never-dispositioned.md` — the exact precedent: one pending todo enumerating a run of findings from one review, with a per-finding table and an explicit "verify before fixing" caveat. Copy this shape.
    - `.planning/phases/03-direct-tools/03-REVIEW-FIX.md` :17 — records `WR-01`/`WR-02`/`WR-03` fixed and `IN-01` explicitly out of scope; `.planning/phases/03-direct-tools/03-VERIFICATION.md` :195-196 — records `WR-04`/`WR-05` as "Warning, latent only". State in the todo that these six are already dispositioned and only the eight below are new work, so a reader does not re-litigate them.
    - `15-RESEARCH.md` § "Newly-surfaced Phase 03 findings" — the per-finding table with this session's verification status.
  </read_first>
  <action>
Create one pending todo enumerating all eight ids, in the shape of the Phase 08 precedent. It must contain the literal filename `03-REVIEW.md` so `todoMentionsPhase()` binds it to phase 03, and each of the eight ids word-bounded so `isDispositioned()` sees them.

Frontmatter: `created` (today), `title` naming `03-REVIEW.md WR-06..IN-06`, `area: planning`, `priority: low`, `resolves_phase: 15`, `files:` listing `.planning/phases/03-direct-tools/03-REVIEW.md` plus the four source files the findings point at (`.claude/mcp/vice/stock-live.test.ts`, `.claude/mcp/vice/vice-proxy.test.ts`, `.claude/mcp/vice/stock-registers.ts`, `.claude/mcp/vice/stock-registers.test.ts`).

Body:
- `## Problem` — these eight had no disposition anywhere, and were **not merely undispositioned but undiscovered**: the guard's parser could not see any of `03-REVIEW.md`'s level-4 finding headings until plan 15-01 widened it. Note that `05-REVIEW.md`'s 16 findings shared the blind spot but were already dispositioned via `05-REVIEW-FIX.md`, so `03-REVIEW.md`'s eight are the only real backlog the widening exposed.
- `## The eight findings` — a table with columns `Id | Cited location | One-line issue | Verified at plan time (2026-08-22)`. Carry the plan-time verification results verbatim rather than restating the review: `WR-06` STILL OPEN and now present at three sites (`stock-live.test.ts:83`, `:165`, `:177`, not the single site the review cited); `WR-08` MOOT — `README.md` no longer contains the manual-only-count or Environment-table text the finding was about (`grep -c` returns 0), so it must be disposed as superseded rather than fixed; `IN-03` APPEARS SUPERSEDED — `NetServer` is now genuinely used at `vice-proxy.test.ts:2258`, `:2295`, `:2436`, so the "dead union member" premise no longer holds; `IN-05` STILL OPEN — the interpreter shebang is still line 1 of `stock-live.test.ts`; `IN-06` PARTLY ADDRESSED — `vice-proxy.test.ts`… no: the site is `stock-live.test.ts:466`, which now escapes the register name's regex metacharacters, leaving only the weak-anchoring half of the finding; `WR-07`, `IN-02`, `IN-04` NOT re-verified at plan time.
- `## Why it is a todo rather than a fix here` — plan 15-01's charter is the instrument and one end-to-end proof, not eight source fixes; a pending todo is one of the guard's five recognised sources, so filing it returns the guard to green honestly (the findings are tracked, with named owner and per-finding status) rather than by silencing the parser. Name plan 15-04 as the owner.
- `## What will close it` — per-finding: re-verify against current source first, then fix or record `superseded`/`wont-fix` with the reason. Explicitly forbid editing `03-REVIEW.md`'s headings to make anything disappear.
- Add the standing warning that `vice-proxy.test.ts` is `MANUAL_ONLY_TESTS` entry 2 and hangs outside a devcontainer (confirmed timing out at 150 s on 2026-08-13), so `WR-07`/`IN-03` must be verified by source assertion plus `npm run typecheck`, never by running that suite.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && npm run typecheck && node --test docs-review-disposition.test.ts && node --test docs-deferred-ledger.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `for id in WR-06 WR-07 WR-08 IN-02 IN-03 IN-04 IN-05 IN-06; do grep -qw "$id" .planning/todos/pending/2026-08-22-phase-03-review-wr-06-through-in-06-never-dispositioned.md || echo "MISSING $id"; done` prints nothing.
    - `grep -c '03-REVIEW.md' .planning/todos/pending/2026-08-22-phase-03-review-wr-06-through-in-06-never-dispositioned.md` returns at least 1.
    - `cd .claude/mcp/vice && node --test docs-review-disposition.test.ts` exits 0 with `# fail 0` — the guard is GREEN with the widened parser and all 150 findings dispositioned.
    - STATE.md's `## Deferred Items` table gained a row for the newly-filed todo, in ascending-filename position, and both prose count figures were incremented to match — done in THIS task's commit, because the ledger guard is in the automated gate and filing a pending todo without its row reddens it.
    - `cd .claude/mcp/vice && node --test docs-deferred-ledger.test.ts` exits 0 in both directions.
    - `cd .claude/mcp/vice && npm run test:automated` exits 0.
    - `git log --oneline -1` shows this plan's commit and `git status --porcelain .planning/phases/03-direct-tools/03-REVIEW.md` is empty (the review file was not edited).
  </acceptance_criteria>
  <done>The widened guard is green from a clean checkout, 150 findings are discovered and all dispositioned, and `03-REVIEW.md`'s eight are a single tracked work item owned by plan 15-04.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols and files created by this plan (excluded from drift verification — they do not exist before it runs):

- `declaredFindingIdsInHeadings()` — new helper in `.claude/mcp/vice/docs-review-disposition.test.ts`
- `WR-98`, `IN-97` — new synthetic finding ids in `.claude/mcp/vice/fixtures/planted-review-fixture.md`
- `.planning/todos/completed/2026-08-22-phase-14-review-in-01-fixed-in-phase-never-cited.md`
- `.planning/todos/pending/2026-08-22-phase-03-review-wr-06-through-in-06-never-dispositioned.md`
</artifacts_this_phase_produces>

<assumption_delta_decision>
Detector fired: `pluralization`, term `second`, from the ROADMAP's "`WR-13`'s dead
**second** hardcoded capability-refusal string".

- **Primary noun:** the capability-refusal message. `capabilityRefusalMessage()` in
  `.claude/mcp/vice/capability-registry.ts` is the generalized representation; the
  hardcoded string at `.claude/mcp/vice/stock-dispatch.ts:736` is an un-promoted second
  copy of it.
- **Decision:** `promote` — route `dispatchStock()`'s miss branch through
  `capabilityRefusalMessage()` and remove the local copy, which is `WR-13`'s own written
  fix (`08-REVIEW.md:623-658`). Rationale: the registry's header already declares itself
  the ONE authoritative place for this data and forbids a second copy; adding alongside
  is what produced the defect.
- **Owner:** plan 15-03, Task 3.
- **Companion invariant test (accepted):** a source-level assertion that no shipped
  module other than `capability-registry.ts` contains the refusal phrasing, so a third
  copy fails loudly. Specified as an acceptance criterion in plan 15-03.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record ↔ source tree | A disposition asserts something about source; the assertion can drift from, or misrepresent, the code it cites |
| machine-generated artifact ↔ hand-authored doc | `tools-manifest.json` / `docs/tool-support.md` are generated; a hand edit is silently reverted by the next generation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-07 | Repudiation | disposition records written by this plan | medium | mitigate | Every "fixed" disposition cites a commit sha verified with `git cat-file -e`, plus the current source location; `docs-linerefs.test.ts` and `docs-review-disposition.test.ts` mechanically check the citations |
| T-15-08 | Tampering | `docs-review-disposition.test.ts` itself | high | mitigate | Two new invariants (heading-declaration subset, per-file non-emptiness) plus a committed planted fixture make a narrowing of the parser fail loudly; the prohibition against weakening the guard is a must-have of this plan |
| T-15-SC | Tampering | npm/pip/cargo installs | — | accept | Not applicable — this phase installs no packages and adds no dependency. `15-RESEARCH.md` has no Package Legitimacy Audit because there is nothing to audit; `.claude/mcp/vice/package.json` dependencies are unchanged by every plan in this phase |
</threat_model>

<verification>
- `cd .claude/mcp/vice && npm run typecheck` exits 0
- `cd .claude/mcp/vice && node --test docs-review-disposition.test.ts` exits 0 with at least 7 tests and `# fail 0`
- `cd .claude/mcp/vice && npm run test:automated` exits 0
- The widened parser's total is 150; the guard's undispositioned list is empty
- `git diff --stat` touches no `*-REVIEW.md` file
</verification>

<success_criteria>
- The guard discovers 150 findings (was 119) and is green
- `14-REVIEW.md IN-01` and `03-REVIEW.md`'s eight are each named in a recognised disposition source that also names their own review filename
- Two shape invariants and a committed fixture prevent this exact blind spot from recurring
- No `*-REVIEW.md` heading was edited
</success_criteria>

<output>
Create `.planning/phases/15-debt-and-review-disposition/15-01-SUMMARY.md` when done.
Record verbatim: the guard's red failure text after Task 1 (the 9 sorted findings), the
before/after finding counts, and the commit sha cited for `14-REVIEW.md IN-01`.
</output>
