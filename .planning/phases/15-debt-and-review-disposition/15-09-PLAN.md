---
phase: 15-debt-and-review-disposition
plan: 09
type: execute
wave: 6
depends_on: ["15-03", "15-07"]
files_modified:
  - .planning/research/GAINS-PROTOCOL.md
  - docs/stock-vice-parity.md
  - .claude/mcp/vice/capability-registry.ts
  - docs/tool-support.md
  - .claude/mcp/vice/vice-proxy.ts
  - .claude/mcp/vice/vice-proxy-ping.test.ts
  - .planning/todos/pending/2026-08-20-warp-over-resource-set-refuted-on-stock-3-10.md
  - .planning/todos/pending/2026-08-19-vice-ping-resolvedbinarypath-misleading-under-broker-pool.md
  - .planning/STATE.md
autonomous: true
requirements: [DEBT-01, DEBT-02]

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "STATE.md's `## Deferred Items` table and both of its prose count figures are updated in the same commits that move this plan's todos out of `pending/`, so `docs-deferred-ledger.test.ts` — which is in the automated gate — is never left red."
    - "`GAINS-PROTOCOL.md` states the measured error codes for the refuted warp claim — the object-does-not-exist code for a read and for a string-typed write, and the invalid-parameter code only for an int-typed write — cited to the 2026-08-20 live probe against genuine stock 3.10, not as a source prediction."
    - "`GAINS-PROTOCOL.md`'s resource notes record that setting the launch-time warp resource at runtime returns success AND reads back as set while changing nothing, so a readback proves nothing."
    - "`docs/stock-vice-parity.md`'s licensed-divergence register states that `vice_machine_config_set`'s advertised warp capability is fork-only, that stock has no runtime warp resource at all, and that warp on stock is a launch-time flag — marking the capability fork-only per SKILL-01 rather than deleting the description, per the ROADMAP's Phase 15 fork-decision consequence."
    - "`capability-registry.ts`'s existing entry for `vice_machine_config_set` carries the fork-only warp caveat in its existing text fields — no new schema field is added — so the caveat reaches both the stock runtime refusal message and the generated support table."
    - "`docs/tool-support.md` is regenerated, not hand-edited, and its byte-identity guard is green."
    - "`vice_ping`'s `resolvedBinaryPath` is documented at the point of definition and in its response as a one-time MCP-server-startup PATH probe that is independent of which binary the broker leased for this request."
    - "`.claude/mcp/vice/tools-manifest.json` is byte-identical — the generated manifest carries the fork binary's own compiled description and is not this repo's to edit."
  prohibitions:
    - "MUST NOT hand-edit `.claude/mcp/vice/tools-manifest.json`. It is machine-generated from a live external binary this repo does not own, `refresh-manifest.ts` is its only writer, and the next regeneration silently reverts any hand edit with no CI gate to catch it."
    - "MUST NOT hand-edit `docs/tool-support.md`; regenerate it."
    - "MUST NOT document behaviour the code does not have — every claim added here is checked against the module or the probe record that establishes it."
    - "MUST NOT delete the fork tool's warp description or the tool itself; FORK-01 decided `retain`, and the ROADMAP's applicable route for this item is to mark the capability fork-only."
  artifacts:
    - docs/stock-vice-parity.md
    - .claude/mcp/vice/vice-proxy-ping.test.ts
  key_links:
    - "**STATE.md ledger ownership.** `docs-deferred-ledger.test.ts` is a strict two-directional invariant between `## Deferred Items` and `.planning/todos/pending/`, and it is IN the automated gate — so any plan that files or moves a todo MUST update that table in the same commit, or `npm run test:automated` goes red. That is why `.planning/STATE.md` is declared in this plan's `files_modified` and why the ledger-touching plans of this phase run in separate waves: two concurrent plans editing that one table would collide."
    - "`capability-registry.ts` entry text → `generate-tool-support-table.mjs`'s note assembly → `docs/tool-support.md` → `tool-support-table.test.mjs` byte-identity guard: editing the registry text without regenerating the document reddens the guard."
    - "`capability-registry.ts` entry text → `capabilityRefusalMessage()` → the stock runtime refusal a caller actually sees. This is why the registry's text field is the right home for the caveat: it reaches a stock caller at the moment they ask for the tool."
    - "`tools-manifest.json` → the fork server's own `tools/list` answer. The overclaiming sentence lives in the fork binary's compiled schema; this repo can caveat it in project-owned files but cannot fix it."
    - "`vice-proxy.ts`'s `ACTIVE_BACKEND` is resolved once at module scope; the broker is a separate already-running process that leases whichever instance it chose. Nothing connects the two, which is the whole finding."
---

<objective>
Close DEBT-02's remaining two behaviours: the refuted warp-over-resource-set claim (three
concrete defects across a research doc and a tool description) and `vice_ping`'s misleading
`resolvedBinaryPath` field.

Purpose: DEBT-02 names both. The warp item is the phase's one genuinely open design question
— `15-RESEARCH.md`'s Open Question 1 asks where the caveat belongs, given that
`tools-manifest.json` is generated from the fork binary's own compiled schema and
`docs/tool-support.md` is generated from `capability-registry.ts`. This plan resolves it:
the caveat goes in `docs/stock-vice-parity.md`'s licensed-divergence register (a
project-owned document, the same register D-14 lives in) **and** in the existing text fields
of `capability-registry.ts`'s existing `vice_machine_config_set` entry — no schema growth,
because that text already flows into both the generated support table and the stock runtime
refusal a caller sees. The generated manifest is left alone.

The `vice_ping` item is a documentation fix by deliberate choice, not by shortcut: the
alternative — re-querying the broker's launch record per request — is a real behavioural
change to a reporting field, and a disposition phase should not take that on. The todo itself
offers both options and this plan takes the second, on the record.

Output: corrected measured error codes, a silent-success trap documented, a fork-only warp
caveat in two project-owned places, a regenerated support table, an honestly-named ping
field, and two closed todos.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-debt-and-review-disposition/15-RESEARCH.md
@.planning/phases/15-debt-and-review-disposition/15-PATTERNS.md
@.planning/todos/pending/2026-08-20-warp-over-resource-set-refuted-on-stock-3-10.md
@.planning/todos/pending/2026-08-19-vice-ping-resolvedbinarypath-misleading-under-broker-pool.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct GAINS-PROTOCOL.md's predicted error codes and record the launch-only silent-success trap</name>
  <files>.planning/research/GAINS-PROTOCOL.md, .planning/todos/pending/2026-08-20-warp-over-resource-set-refuted-on-stock-3-10.md</files>
  <read_first>
    - `.planning/todos/pending/2026-08-20-warp-over-resource-set-refuted-on-stock-3-10.md` — the whole file, especially the measured request/result table from the 2026-08-20 live probe against genuine stock 3.10 and the four numbered Solution items. Items 1, 2 and 3 are this task; item 4 is Task 2.
    - `.planning/research/GAINS-PROTOCOL.md` around :1255-1266 — the resource table plus the paragraph asserting a single error code for the write. The wrong prediction is in that paragraph; the surrounding claims about there being no runtime warp resource are correct and must be preserved.
    - `.planning/research/GAINS-PROTOCOL.md` around :1345-1355 — the Tier 4 table already has a row for the launch-time warp resource reading that it is only honoured at launch, and a row saying the runtime warp resource does not exist. The missing nuance is that setting the launch-time one at runtime *succeeds and reads back*, so a client has no wire-level signal.
    - `.planning/research/GAINS-PROTOCOL.md` around :1485-1490 and :1519-1524 — two more places that already state the launch-time-only rule correctly. Solution item 3 is about confirming nothing contradicts them, not adding a new fact; check them and record that they were checked.
    - `CLAUDE.md`'s Capability constraint about there being no runtime warp resource, and `.planning/PROJECT.md` around the same claim — both already correct; confirm and leave alone.
    - `.claude/mcp/vice/docs-linerefs.test.ts` — if any of the edited lines is cited elsewhere by line number, this guard will catch the drift; run it after editing.
  </read_first>
  <action>
Solution item 1: replace the single-error-code prediction with the measured codes from the
2026-08-20 probe — the object-does-not-exist code for the read of the non-existent runtime
warp resource and for a string-typed write of it, and the invalid-parameter code only for an
int-typed write. State plainly why the distinction matters: a client keying "unsupported
resource" off the invalid-parameter code alone misclassifies the failure it is most likely to
actually see, because the string-typed convenience form returns the other code. Cite the
probe by date and binary (genuine unpatched stock, VICE 3.10, over the loopback binary
monitor) rather than leaving it a source-derived prediction.

Solution item 2: extend the Tier 4 row for the launch-time warp resource with the
silent-success trap — a runtime set returns success and reads back as set, while the value is
only consulted at launch, so the readback proves nothing. Carry the todo's own honesty about
what was not measured: whether a runtime set has *any* effect on emulation speed is still
open; only the readback-proves-nothing half is established. Then answer the decision the item
asks for: state whether a stock-backend resource-set tool should refuse this resource outright
or return an explicit launch-time-only result, and record which and why. Recommended answer,
unless the code says otherwise: return an explicit launch-time-only result rather than a bare
refusal, because a refusal is indistinguishable from an unsupported resource and this one
genuinely exists.

Solution item 3: verify the three other places that already state the launch-time-only rule
still say it correctly and that nothing in the document contradicts the corrected codes.
Record in the SUMMARY which lines you checked and that they were left unmodified — item 3 is
a consistency check, not an edit, and recording that it was performed is the deliverable.

Do not close the todo in this task — Task 2 owns item 4, and the todo closes there.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && node --test docs-linerefs.test.ts && node --test docs-dangling-refs.test.ts && npm run test:automated</automated>
  </verify>
  <acceptance_criteria>
    - The corrected paragraph names two distinct error codes and attributes each to the right operation: `grep -c '0x01' .planning/research/GAINS-PROTOCOL.md` returns at least 1 in the warp section, and the sentence that previously named only the invalid-parameter code for the write no longer does so unqualified.
    - `grep -c '2026-08-20' .planning/research/GAINS-PROTOCOL.md` returns at least 1 — the probe is cited by date.
    - The Tier 4 launch-time-warp row mentions the readback: `awk '/Tier 4/,/^### /' .planning/research/GAINS-PROTOCOL.md | grep -ci 'reads back'` returns at least 1.
    - The recorded decision for a stock resource-set tool is present and unambiguous — one sentence naming refuse-outright or launch-time-only-result, with a reason.
    - The three other launch-time-only statements are unmodified: `git diff .planning/research/GAINS-PROTOCOL.md` shows changes only in the two regions this task edits, and the SUMMARY lists the checked line numbers.
    - `cd .claude/mcp/vice && node --test docs-linerefs.test.ts docs-dangling-refs.test.ts` exits 0.
    - `cd .claude/mcp/vice && npm run test:automated` exits 0.
  </acceptance_criteria>
  <done>The research document's warp section states measured codes with a dated probe citation, records the silent-success trap and the tool-behaviour decision, and its three consistent statements elsewhere are confirmed untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Mark the fork tool's warp capability fork-only in two project-owned places and regenerate the support table</name>
  <files>docs/stock-vice-parity.md, .claude/mcp/vice/capability-registry.ts, docs/tool-support.md, .planning/todos/pending/2026-08-20-warp-over-resource-set-refuted-on-stock-3-10.md</files>
  <read_first>
    - The ROADMAP's Phase 15 section, paragraph "Fork-decision consequence" — it is normative on the content decision: mark the description fork-only, do NOT delete it. FORK-01 decided `retain`, so the fork tool is not going away and its warp claim is not moot.
    - `.claude/mcp/vice/tools-manifest.json` around the `vice_machine_config_set` entry — read the description so the caveat quotes what it actually advertises. Then read `.claude/mcp/vice/refresh-manifest.ts`'s header ("the ONLY writer") and `.claude/mcp/vice/fork-manifest-surface.test.ts`'s header (the manifest is written EXACTLY as the live fork host answers). This file is read-only for this plan.
    - `.claude/mcp/vice/capability-registry.ts` — the entries for `vice_machine_config_set` and `vice_machine_config_get` (both `category: "descoped"`, `providedBy: "fork"`, currently sharing identical `reason` text around :270-284). Read the `CapabilityEntry` type to see which text fields already exist (`reason`, `alternative`) — the caveat goes in existing fields, and no new field is added. Check whether the `get` entry's fork description also advertises warp; caveat whichever ones do.
    - `scripts/generate-tool-support-table.mjs` around :187-208 — how `reason` and `alternative` become the note cell, so the caveat's length and shape suit a table cell. Plan 15-03 added pipe/newline escaping at the emission point; a pipe in the new text is now safe but still reads badly in a cell, so prefer prose without one.
    - `docs/stock-vice-parity.md` §A item 7 (the licensed-divergence register, from around :161, with the D-14 disk-attach bullet at :270) — the register's bullet shape and voice. Also §B item 3, which describes full resource get/set as a stock gain; check it does not contradict the new caveat.
    - `.claude/mcp/vice/tool-support-table.test.mjs` — the byte-identity drift guard. It decides whether the regeneration was done properly.
  </read_first>
  <action>
Add a bullet to `docs/stock-vice-parity.md` §A item 7's licensed-divergence register stating:
the fork tool `vice_machine_config_set` advertises a warp resource in its own compiled
description; that capability is **fork-only**; stock has no runtime warp resource at all
(the launch-time route is the only one, and the launch-time resource is a silent-success trap
at runtime); and a skill written against the fork's description therefore breaks on stock
rather than degrading, which is exactly the SKILL-01 hazard. Say explicitly that the
description itself cannot be corrected here because the manifest is generated from the fork
binary's own schema, and name this bullet as the project-owned caveat that stands in for it.
Cross-reference the todo and the 2026-08-20 probe.

Extend the `capability-registry.ts` entry for `vice_machine_config_set` — using its existing
text fields only — so the caveat reaches a stock caller through
`capabilityRefusalMessage()` and reaches a reader through the generated support table. Keep
it to one or two sentences: the fork tool's warp claim is fork-only, stock has no runtime
warp resource, and warp on stock is a launch-time flag. Respect the registry's own contract:
no future-phase framing, and the providing backend comes from the entry's own field rather
than being restated in prose. Apply the same to the `get` entry only if its fork description
also advertises warp.

Regenerate `docs/tool-support.md` with the generator's own command. The diff should be exactly
the changed note cell(s) — if it is larger, stop and find out why before committing.

Then `git mv` the todo to `completed/` and append a `## Resolution` covering all four Solution
items: items 1-3 as landed by Task 1 with their commit, item 4 as landed here with its two
project-owned locations, and the explicit record that `tools-manifest.json` was deliberately
NOT edited and why. Also record item 5 (the optional follow-up measuring whether a runtime
launch-time-warp set changes emulation speed at all) as a named, promoted follow-on rather
than dropping it — it is the one thing the probe left open.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/generate-tool-support-table.mjs >/dev/null && cd .claude/mcp/vice && npm run typecheck && node --test tool-support-table.test.mjs capability-registry.test.ts stock-dispatch.test.ts && npm run test:automated</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --quiet .claude/mcp/vice/tools-manifest.json` — the generated manifest is byte-identical.
    - `grep -ci 'fork-only' docs/stock-vice-parity.md` returns at least 1 and the new bullet is inside §A item 7: `awk '/^7\. \*\*Expected divergences/,/^## B\./' docs/stock-vice-parity.md | grep -ci 'warp'` returns at least 1.
    - `cd .claude/mcp/vice && grep -ci 'warp' capability-registry.ts` returns at least 1, and `grep -c 'note:' capability-registry.ts` is unchanged from before this task (no new schema field was introduced).
    - `docs/tool-support.md` was regenerated: after running the generator, `git diff --quiet docs/tool-support.md` succeeds, and the committed diff for this task touches only the note cell(s) for the machine-config tool(s) — `git diff --stat docs/tool-support.md` shows a small line count and the SUMMARY quotes the changed row.
    - The caveat reaches a stock caller: `cd .claude/mcp/vice && node --test stock-dispatch.test.ts` exits 0 and the SUMMARY quotes the refusal message `capabilityRefusalMessage()` now renders for the machine-config set tool, showing the caveat text in it.
    - `cd .claude/mcp/vice && node --test tool-support-table.test.mjs capability-registry.test.ts` exits 0.
    - The todo is renamed to `completed/` with exactly one `## Resolution` that names all five Solution items, records item 5 as a promoted follow-on with an owner, and states that the manifest was deliberately not edited.
    - `cd .claude/mcp/vice && npm run typecheck && npm run test:automated` both exit 0.
  </acceptance_criteria>
  <reversibility rating="reversible">This is text in two project-owned files plus a regeneration of a generated document; no schema field is added, no tool surface changes, and reverting is a one-commit revert plus one regeneration. The alternative considered — growing `capability-registry.ts`'s schema with a dedicated note field — would have been costly (a load-bearing single-seam module with generated consumers and a byte-identity guard) and was rejected for that reason, since the existing text fields already reach both consumers.</reversibility>
  <done>The fork tool's warp claim is caveated fork-only in the licensed-divergence register and in the registry text a stock caller actually sees, the generated manifest is untouched, and the support table is regenerated with a minimal diff.</done>
</task>

<task type="auto">
  <name>Task 3: Name vice_ping's resolvedBinaryPath honestly at its definition and in its response</name>
  <files>.claude/mcp/vice/vice-proxy.ts, .claude/mcp/vice/vice-proxy-ping.test.ts, .planning/todos/pending/2026-08-19-vice-ping-resolvedbinarypath-misleading-under-broker-pool.md</files>
  <read_first>
    - `.planning/todos/pending/2026-08-19-vice-ping-resolvedbinarypath-misleading-under-broker-pool.md` — the finding and its two offered options. Note the corroborating evidence: Phase 8.2 plan 04's walkthrough had to route around this field entirely and proved backend identity from the broker's own launch record plus a live process-argument read instead.
    - `.claude/mcp/vice/vice-proxy.ts` — the module-scope resolution of the active backend (around :316) and the ping response fields (around :3312-3313). Record the real current line numbers.
    - `.claude/mcp/vice/backend-detect.mts` around :444-449 — `binPathFields()`, which already correctly distinguishes a resolved absolute path from a bare command name. That was a *different*, already-fixed finding; this one is orthogonal and about per-request accuracy, so the doc note must not imply the earlier bug is still open.
    - `.claude/mcp/vice/vice-broker.mts` and the broker's epoch/launch state file — what the broker actually records about each leased instance's binary. The note should tell a caller where the authoritative answer lives, not merely that this field is not it.
    - Existing tests covering `vice_ping`'s response shape — find them (`grep -rln 'vice_ping' .claude/mcp/vice/*.test.*`). If the only coverage is in `vice-proxy.test.ts`, that file HANGS and must not be run; put the new assertion in a runnable sibling instead and say so.
    - `.claude/mcp/vice/test-gate.mjs` :83-92 — confirm which candidate test file is in the automated set before choosing where the new assertion goes.
  </read_first>
  <action>
Take the todo's second option — document the field rather than re-query the broker per request
— and say on the record that the first option was considered and rejected as a behavioural
change out of a disposition phase's remit.

Document it in two places, because two different readers hit it:
1. At the definition of the module-scope backend resolution in `vice-proxy.ts`, a comment
   stating that this is resolved exactly once at MCP-server process startup, by a PATH probe
   in the server's own environment, and that it is independent of which binary the broker
   leased for any given request — with a pointer to where the authoritative per-instance
   answer lives (the broker's own launch record) and a note that Phase 8.2 plan 04's
   walkthrough had to use that route instead.
2. In the ping response itself, so a caller reading the output learns it too. Do not rename
   the existing field — a rename is a response-shape change and callers may read it. Add a
   short sibling field whose value states what the path is (a server-startup PATH probe, not
   the leased instance's binary), or extend the existing adjacent boolean's documentation, and
   pick whichever keeps the response shape backward-compatible. Record which you chose and
   why.

Add an assertion in a runnable test file (never `vice-proxy.test.ts`) that the ping response
carries the qualifying text, so the honesty cannot be silently dropped. If no suitable
runnable file exists, create `vice-proxy-ping.test.ts` — a focused, non-hanging sibling that
asserts only on the response shape's documentation fields — and confirm it lands in the
automated set rather than needing a `MANUAL_ONLY_TESTS` entry.

Then `git mv` the todo to `completed/` with a `## Resolution` naming the date, plan, commit,
both documentation locations with their real line numbers, the rejected option and why, and
the authoritative alternative a caller should use instead.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && npm run typecheck && node --test vice-proxy-ping.test.ts && node test-gate.mjs --list 2>/dev/null | grep -c vice-proxy-ping.test.ts; npm run test:automated</automated>
  </verify>
  <acceptance_criteria>
    - `cd .claude/mcp/vice && grep -c 'resolvedBinaryPath' vice-proxy.ts` returns at least 2, and the comment at the module-scope resolution site mentions both the once-at-startup nature and the broker's launch record as the authoritative alternative.
    - The response carries the qualification: `cd .claude/mcp/vice && node --test vice-proxy-ping.test.ts` exits 0 and its assertion names the qualifying text.
    - The new test file is in the automated set, not the manual-only list: `test "$(grep -c 'vice-proxy-ping.test.ts' test-gate.mjs || true)" = "0"` (absence from `MANUAL_ONLY_TESTS` is what puts it in the automated set) and `npm run test:automated` includes it.
    - The assertion is non-vacuous: temporarily remove the qualifying text from the response, confirm the test fails, then restore. Record both outputs in the SUMMARY.
    - The response shape stays backward-compatible: the existing field name is unchanged (`grep -c 'resolvedBinaryPath:' vice-proxy.ts` returns at least 1) and the SUMMARY records which addition shape was chosen and why.
    - The todo is renamed to `completed/` with exactly one `## Resolution` naming both documentation locations by real line number, the rejected option, and the authoritative alternative.
    - `cd .claude/mcp/vice && npm run typecheck && npm run test:automated` both exit 0.
    - STATE.md's `## Deferred Items` table lost one row per todo this plan moved out of `pending/`, both prose
      count figures were decremented to match, and `cd .claude/mcp/vice && node --test docs-deferred-ledger.test.ts`
      exits 0 in both directions. This is not deferrable to plan 15-12: the guard is in the automated gate, so a
      missing row reddens `npm run test:automated` for every later wave.
  </acceptance_criteria>
  <reversibility rating="reversible">Additive documentation plus one additive response field and one new test; the existing field name and shape are preserved, so no caller breaks and a revert is a one-commit revert.</reversibility>
  <done>The field's real meaning is documented where it is defined and where it is read, a runnable assertion keeps the qualification present, and the rejected per-request-requery option is on the record.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols and files created by this plan:

- A fork-only warp caveat bullet in `docs/stock-vice-parity.md` §A item 7
- Extended `reason` / `alternative` text on `capability-registry.ts`'s existing
  `vice_machine_config_set` entry (existing fields only — no new schema field)
- A qualifying sibling field on `vice_ping`'s response in `.claude/mcp/vice/vice-proxy.ts`
- `.claude/mcp/vice/vice-proxy-ping.test.ts` — new focused, non-hanging test file (created only
  if no existing runnable test covers the ping response shape)
- `.planning/todos/completed/2026-08-20-warp-over-resource-set-refuted-on-stock-3-10.md` (renamed from `pending/`)
- `.planning/todos/completed/2026-08-19-vice-ping-resolvedbinarypath-misleading-under-broker-pool.md` (renamed from `pending/`)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fork binary's compiled tool schema → this repo's committed manifest | The description text is the external binary's own; this repo mirrors it and cannot correct it |
| `capability-registry.ts` text → stock runtime refusal + generated support table | One text field reaches two consumers; a wrong or overlong string degrades both |
| MCP server startup environment → per-request answer | A value probed once at startup is reported per request as though it were current |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-30 | Spoofing | `vice_ping`'s `resolvedBinaryPath` reported as the leased instance's binary | medium | mitigate | Document at both the definition and the response that it is a server-startup PATH probe, name the broker's launch record as the authoritative source, and pin the qualification with a runnable assertion. The behavioural fix (per-request requery) is explicitly rejected here and recorded as such |
| T-15-31 | Repudiation | a fork-only capability advertised without a backend qualifier (SKILL-01) | medium | mitigate | Caveat in the licensed-divergence register and in the registry text a stock caller sees at refusal time; the generated manifest is left untouched so the next regeneration cannot revert the caveat |
| T-15-32 | Tampering | hand-editing a machine-generated artifact to make a documentation claim true | high | mitigate | Hard prohibition on editing `tools-manifest.json` and `docs/tool-support.md`; both are asserted byte-identical or regenerated-and-guarded as acceptance criteria |
| T-15-SC | Tampering | npm/pip/cargo installs | — | accept | Not applicable — this plan installs no packages and adds no dependency |
</threat_model>

<verification>
- `cd .claude/mcp/vice && npm run typecheck && npm run test:automated` both exit 0
- `git diff --quiet .claude/mcp/vice/tools-manifest.json`
- `docs/tool-support.md` regenerated with a minimal diff and its byte-identity guard green
- `cd .claude/mcp/vice && node --test docs-linerefs.test.ts docs-dangling-refs.test.ts` exits 0
- Both todos are in `completed/`, each with one `## Resolution`

<human-check>
Read the new `docs/stock-vice-parity.md` bullet and the changed `docs/tool-support.md` row and
confirm a reader learns three things: the capability is fork-only, stock has no runtime warp
resource, and warp on stock is a launch-time flag. Confirm the row still reads as prose rather
than as an escaped fragment.
</human-check>
</verification>

<success_criteria>
- DEBT-02's remaining two behaviours are documented where a user would look, each citation resolving to a real heading or field
- The generated manifest and the generated support table are both handled correctly — one untouched, one regenerated
- `15-RESEARCH.md`'s Open Question 1 is resolved with the rejected alternative recorded
- Two pending todos closed, with the one thing the warp probe left open promoted rather than dropped
</success_criteria>

<output>
Create `.planning/phases/15-debt-and-review-disposition/15-09-SUMMARY.md` when done.
Record: the corrected error-code sentence, the checked-but-unmodified line numbers from
Solution item 3, the changed support-table row quoted verbatim, the refusal message
`capabilityRefusalMessage()` now renders for the machine-config set tool, the chosen ping
response-shape addition and why, both planted-violation probe outputs, and the named owner for
the promoted warp-speed measurement follow-on.
</output>
