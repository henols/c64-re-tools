---
phase: 15-debt-and-review-disposition
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - scripts/generate-tool-support-table.mjs
  - docs/tool-support.md
  - .claude/mcp/vice/tool-support-table.test.mjs
  - .claude/mcp/vice/capability-registry.test.ts
  - .claude/mcp/vice/stock-dispatch.ts
  - .claude/mcp/vice/stock-dispatch.test.ts
autonomous: true
requirements: [GATE-02]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Registry prose reaches `docs/tool-support.md` through one escaping helper at the single emission point, and a cell-count assertion fails if a future `reason` or `alternative` string ever splits a row (WR-04)."
    - "All three independent copies of the `ToolDefinition` declaration scan bound their search to the declaration's own body, so a declaration without a `name:` throws instead of borrowing a later declaration's name (WR-08)."
    - "`capability-registry.test.ts`'s synthetic-tool set is derived mechanically from `vice-proxy.ts`, not hand-typed, and that fix is cited with the commit that landed it (WR-05)."
    - "`dispatchStock()`'s miss branch renders its refusal through `capabilityRefusalMessage()`; no shipped module outside `capability-registry.ts` carries a competing capability-refusal wording, and none uses future-phase framing for a hardware loss (WR-13)."
    - "`docs/tool-support.md` on disk is byte-identical to what the generator produces — the drift guard is green, and the document was regenerated rather than hand-edited."
  prohibitions:
    - "MUST NOT hand-edit `docs/tool-support.md`; it is generated, and a hand edit is silently reverted by the next generation while the byte-identity guard locks in the wrong bytes."
    - "MUST NOT record a finding as fixed unless the fix has landed and the disposition cites the commit or the re-runnable check that proves it."
  artifacts:
    - scripts/generate-tool-support-table.mjs
    - .claude/mcp/vice/stock-dispatch.ts
  key_links:
    - "`capability-registry.ts` → `capabilityRefusalMessage()` → `dispatchStock()` miss branch: this is the promote-the-general-representation link the assumption-delta decision in plan 15-01 records; a second local wording is the defect."
    - "`generate-tool-support-table.mjs` → `docs/tool-support.md` → `tool-support-table.test.mjs` byte-identity guard: the guard makes an unescaped-cell bug *worse* than harmless by locking corrupted bytes in as expected."
    - "Three independent copies of the declaration scan exist by design (generator, `tool-support-table.test.mjs`, `capability-registry.test.ts`) so a bug in one is caught by the others — bounding them with one shared expression would destroy that property."
---

<objective>
Close the four remaining Phase 08 findings that live outside the two skill-lint scripts:
`WR-04` (markdown-cell escaping), `WR-05` (hand-typed synthetic-tool set — already fixed,
needs the citation), `WR-08` (unbounded declaration scan, in three places) and `WR-13`
(the dead second capability-refusal wording the ROADMAP explicitly says is still open).

Purpose: `WR-13` is the one the ROADMAP calls out by name and says Phase 14 left
untouched — plan 14-02 verdicted all 17 `capability-registry.ts` mention sites
`holds-as-is` and made zero edits, so `stock-dispatch.ts:736` still hardcodes a
fork-provides claim that is false for two stock-only-gain names, and still uses the
future-phase framing `capability-registry.ts:335-346` explicitly forbids for a hardware
loss. It is unreachable today (all 38 stock-manifest tools have a `stockHandlerFor()`
entry), which is exactly why it survived a consolidation unnoticed. `WR-04` and `WR-08`
are latent-but-real correctness defects in the generation pipeline whose own byte-identity
drift guard would happily lock in a corrupted rendering.

Output: escaped cell emission with a cell-count invariant, three bounded declaration
scans, a cited disposition for `WR-05`, and a single-source refusal message.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-debt-and-review-disposition/15-RESEARCH.md
@.planning/phases/15-debt-and-review-disposition/15-PATTERNS.md
@.planning/todos/pending/2026-08-21-phase-08-review-wr-04-through-wr-12-never-dispositioned.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escape registry prose at the single markdown emission point and assert the cell-count invariant (WR-04)</name>
  <files>scripts/generate-tool-support-table.mjs, docs/tool-support.md, .claude/mcp/vice/tool-support-table.test.mjs</files>
  <read_first>
    - `.planning/phases/08-capability-honesty-and-the-install-story/08-REVIEW.md` lines 330-362 — `WR-04` with both written snippets (the `cell()` helper and the cell-count test).
    - `scripts/generate-tool-support-table.mjs` — read the whole file. The note is assembled around :187-208 (`note = \`${label}: ${entry.reason}\``, then `entry.alternative` appended) and emitted at :241 (`lines.push(\`| ${row.name} | ${forkCell} | ${stockCell} | ${row.note} |\`)`). There is exactly one emission point; keep it that way.
    - `.claude/mcp/vice/tool-support-table.test.mjs` — read the whole file, especially its header explaining why it is `.mjs` and not `.ts`, and the byte-identity drift assertion against `docs/tool-support.md`.
    - `docs/tool-support.md` — the generated artifact. Note the exact regeneration command the generator's own header or `scripts/` usage documents, and use that command; do not write the file by hand.
    - `.claude/mcp/vice/capability-registry.ts` — the `reason` and `alternative` fields whose prose flows into cells. Confirm with `grep` that no current entry contains a pipe or a newline, so the escaping change produces a zero-byte diff in `docs/tool-support.md` today.
  </read_first>
  <action>
Add a single-purpose escaping helper at the emission point in
`scripts/generate-tool-support-table.mjs` — collapse any CR/LF to a space, then backslash-
escape pipes — and apply it to the two cells that carry free prose (the tool name and the
note). Give it the doc comment `08-REVIEW.md:341-343` supplies: markdown cells cannot hold
a raw pipe or newline, registry prose is user-facing and may acquire either, escaped here
at the ONE emission point.

Regenerate `docs/tool-support.md` with the generator's own documented command. Expect a
zero-byte diff today (no current registry entry contains a pipe or a newline) — if the
diff is non-empty, stop and investigate before committing: a non-empty diff means either
an entry already contains one of those characters (in which case the committed document
has been silently corrupt) or the escaping is over-broad.

Add the cell-count invariant to `.claude/mcp/vice/tool-support-table.test.mjs` per
`08-REVIEW.md:349-357`: for every generated row whose first cell is a tool name, assert the
row has exactly four cells. Prove it non-vacuous rather than asserting it: temporarily add
a pipe character to one `capability-registry.ts` entry's `reason`, confirm the escaped
generator still yields four cells and the test stays green, then remove the pipe and
confirm that reverting the `cell()` escaping (temporarily) makes the same planted pipe fail
the new test. Record both outputs in the SUMMARY, then restore both files.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && node --test tool-support-table.test.mjs && npm run typecheck && cd /home/henrik/dev/henrik/git/c64-re-tools && git diff --quiet docs/tool-support.md && echo "tool-support.md byte-stable"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'replace\(/\\\\r\?\\\\n/g' scripts/generate-tool-support-table.mjs` returns at least 1, or equivalently `grep -c 'const cell' scripts/generate-tool-support-table.mjs` returns 1.
    - The emission point applies the helper: `grep -cE '^\s*lines\.push\(`\| \$\{cell\(' scripts/generate-tool-support-table.mjs` returns at least 1.
    - `cd .claude/mcp/vice && node --test tool-support-table.test.mjs` exits 0 and reports at least one more test than before this task.
    - `git diff --stat docs/tool-support.md` shows zero changed lines after regeneration (or, if non-zero, the SUMMARY explains exactly which registry entry contains a pipe or newline and why the previous committed bytes were wrong).
    - The planted-pipe probe is recorded in the SUMMARY with both the escaped-green and unescaped-red outputs, and `git status --porcelain .claude/mcp/vice/capability-registry.ts` is empty at the end of the task.
  </acceptance_criteria>
  <done>Registry prose can no longer split a markdown row, a cell-count assertion proves it, and the regenerated document is byte-stable.</done>
</task>

<task type="auto">
  <name>Task 2: Bound all three ToolDefinition declaration scans, and cite WR-05 as already fixed (WR-08, WR-05)</name>
  <files>scripts/generate-tool-support-table.mjs, .claude/mcp/vice/tool-support-table.test.mjs, .claude/mcp/vice/capability-registry.test.ts</files>
  <read_first>
    - `.planning/phases/08-capability-honesty-and-the-install-story/08-REVIEW.md` lines 464-507 — `WR-08` with the bounded-search snippet; lines 363-398 — `WR-05` and why its own suggested import route is blocked by `tsconfig.json`'s `allowJs: false` (TS7016).
    - `scripts/generate-tool-support-table.mjs` around :119 — the first copy of the unbounded lazy pattern, inside `discoverSyntheticToolNames()`. Read the header comment at :78-83 that promises a loud throw for an unresolvable identifier — the unbounded window is what breaks that promise.
    - `.claude/mcp/vice/tool-support-table.test.mjs` around :79 — the second copy, inside the test's deliberately independent re-derivation. Read the file header's statement that it never imports the generator's function, so a bug shared between generator and test cannot pass silently.
    - `.claude/mcp/vice/capability-registry.test.ts` around :163-185 — the third copy, added when `WR-05` was fixed: the synthetic set is now derived dynamically with a `>= 3` non-vacuity assertion and a throw on an unresolved identifier. Confirm this is the current shape before writing that `WR-05` is fixed.
    - `.claude/mcp/vice/vice-proxy.ts` — the three `ToolDefinition` declarations the scans resolve (search for `: ToolDefinition = {`). All three put `name` first today, which is why the defect is latent; record their real current line numbers.
  </read_first>
  <action>
`WR-08`: bound each of the three scans to its own declaration's body before extracting the
name. Locate the declaration opener, then cut the search at the next top-level `const`,
`function` or `export` at line start, and match the name only within that slice — so a
declaration with no `name:` produces the loud throw the generator's header already
promises instead of silently borrowing a later declaration's name.

Bound the three copies with three *independently written* expressions. This is deliberate
and load-bearing: the whole reason `tool-support-table.test.mjs` re-derives rather than
imports is so a bug in one implementation is caught by the others. Extracting one shared
bounding helper would collapse three independent witnesses into one and destroy that
property — do not do it. Note this reasoning in a short comment at each of the three sites
so a future reader does not "helpfully" deduplicate them.

`WR-05`: this finding is already fixed — `capability-registry.test.ts` no longer hardcodes
the two-name synthetic set and instead discovers it from `vice-proxy.ts`, with the
non-vacuity floor and the unresolved-identifier throw the review asked for. Do not
re-implement it. Instead find the commit that landed it (`git log -S` on a distinctive
fragment of the current discovery block, or `git log --follow` on the file around the
change) and record `WR-05` as fixed-with-commit in the plan SUMMARY, naming the commit sha
and the current line range. Also record explicitly that the review's own suggested fix
(importing `discoverSyntheticToolNames` from the generator) was NOT the route taken,
because `tsconfig.json` sets `allowJs: false` and a `.ts` test importing a repo-root
`.mjs` fails `tsc --noEmit` with TS7016 — the review anticipated this in its parenthetical
and the implementation took the other branch. That distinction is the citation's substance;
without it a reader comparing the review to the code concludes the fix is missing.

Prove `WR-08`'s fix non-vacuous: in a scratch copy of `vice-proxy.ts` (or temporarily, then
revert), add a fourth `ToolDefinition` declaration with no `name:` field ahead of an
existing one, and confirm each of the three scans now throws by identifier name rather than
returning the following declaration's name. Record all three outputs in the SUMMARY.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && npm run typecheck && node --test tool-support-table.test.mjs capability-registry.test.ts && cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/generate-tool-support-table.mjs >/dev/null && git diff --quiet docs/tool-support.md</automated>
  </verify>
  <acceptance_criteria>
    - No copy of the unbounded pattern remains: `test "$(grep -rl 'ToolDefinition\\\\s\*=\\\\s\*\\\\{\[\\\\s\\\\S\]\*?name' scripts/generate-tool-support-table.mjs .claude/mcp/vice/tool-support-table.test.mjs .claude/mcp/vice/capability-registry.test.ts 2>/dev/null | wc -l)" = "0"`.
    - All three sites bound their search: `grep -c 'declEnd\|declBody\|declStart' scripts/generate-tool-support-table.mjs` returns at least 2, and the equivalent bounding identifiers are present in both test files (each with its own distinct expression, verified by eye — the three bounding expressions must not be textually identical).
    - Each of the three sites carries a comment saying the three copies are independent by design and must not be deduplicated: `grep -rc 'independent' scripts/generate-tool-support-table.mjs .claude/mcp/vice/tool-support-table.test.mjs .claude/mcp/vice/capability-registry.test.ts` each return at least 1.
    - `cd .claude/mcp/vice && node --test tool-support-table.test.mjs capability-registry.test.ts` exits 0.
    - `npm run typecheck` exits 0.
    - `git diff --quiet docs/tool-support.md` after a regeneration run — the bounding change must not alter the discovered synthetic-tool set.
    - The SUMMARY names `WR-05`'s landing commit sha, verified with `git cat-file -e`, plus the TS7016 reason the review's own suggested import route was not taken.
    - The SUMMARY records the planted no-`name:` declaration probe's output from all three scans.
  </acceptance_criteria>
  <done>Three independently-bounded declaration scans throw rather than borrow a wrong name, and `WR-05`'s existing fix is cited with a real commit and the reason it diverged from the review's suggestion.</done>
</task>

<task type="auto">
  <name>Task 3: Route dispatchStock()'s miss branch through the registry's single refusal renderer (WR-13)</name>
  <files>.claude/mcp/vice/stock-dispatch.ts, .claude/mcp/vice/stock-dispatch.test.ts</files>
  <read_first>
    - `.planning/phases/08-capability-honesty-and-the-install-story/08-REVIEW.md` lines 623-658 — `WR-13` with its written fix, including the internal-inconsistency fallback wording for a name with no registry entry.
    - `.claude/mcp/vice/stock-dispatch.ts` — read the whole `dispatchStock()` function and its miss branch at :735-738. Record the real current line numbers.
    - `.claude/mcp/vice/capability-registry.ts` — `capabilityRefusalMessage()`'s signature and return type (it returns `undefined`/`null` for an unregistered name — check which), the three reason categories, and the contract paragraph at :335-346 that forbids future-phase framing for a hardware loss and reads the providing backend from `entry.providedBy`. Also read the module header's "WHAT NOT TO DO: do not hand-maintain a second copy of this data anywhere else in the repo".
    - `.claude/mcp/vice/stock-dispatch.test.ts` — find every assertion that keys off the current miss-branch wording; each must be updated in the same commit as the source change.
    - `.claude/mcp/vice/tools-manifest.stock.json` and `stockHandlerFor()` — confirm the branch is still unreachable (every advertised stock name has a handler) and record the count in the SUMMARY, so the disposition says "latent, verified unreachable at commit X" rather than repeating a year-old claim.
    - `.planning/phases/15-debt-and-review-disposition/15-01-PLAN.md` — the `<assumption_delta_decision>` block records this as a `promote` decision with this task as its owner; keep the implementation consistent with it.
  </read_first>
  <action>
Replace the miss branch's locally-composed refusal with a call to
`capabilityRefusalMessage(name, "stock")`, falling back — only when the registry has no
entry for the name — to an internal-inconsistency message stating that the name is
advertised on the stock manifest but has no handler in the stock dispatch table, which is a
bug to report rather than a capability gap. Keep whatever error-wrapping helper the
existing branch uses (`isErrorText` or its current equivalent); the change is the message
source, not the response shape.

Delete the local wording entirely. Do not leave it as a commented-out alternative: a
second copy of the refusal text in a comment is still a second copy a future reader can
resurrect, and the module header names exactly that as the thing not to do.

Update `stock-dispatch.test.ts`'s assertions in the same commit. Where a test asserted the
old phrasing, assert instead that the message the branch returns is the one
`capabilityRefusalMessage()` renders for that name — derive the expectation by calling the
registry function in the test rather than re-typing the string, so the test cannot drift
from the renderer.

Add the invariant test the assumption-delta decision accepted: a source-level assertion,
in `stock-dispatch.test.ts`, that no shipped module other than `capability-registry.ts`
contains a capability-refusal wording. Implement it by reading the shipped module set from
`package.json`'s `files[]` (the same technique `r2000-spawn-seam.test.ts` already uses in
this repo, so the file set is never hand-typed), stripping comment lines, and asserting
zero occurrences of the fork-provides claim shape and zero occurrences of future-phase
framing paired with a backend-selection instruction. Cite `WR-13` and
`capability-registry.ts:335-346` in the test's own message so a failure explains itself.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && npm run typecheck && node --test stock-dispatch.test.ts capability-registry.test.ts && npm run test:automated</automated>
  </verify>
  <acceptance_criteria>
    - `cd .claude/mcp/vice && test "$(grep -v -E '^\s*(//|\*|/\*)' stock-dispatch.ts | grep -c 'fork backend provides' || true)" = "0"`.
    - `cd .claude/mcp/vice && test "$(grep -v -E '^\s*(//|\*|/\*)' stock-dispatch.ts | grep -c 'wait for a later phase' || true)" = "0"`.
    - `cd .claude/mcp/vice && grep -c 'capabilityRefusalMessage' stock-dispatch.ts` returns at least 1.
    - The new invariant test exists and reads the shipped file set from `package.json`: `grep -c 'files' stock-dispatch.test.ts` returns at least 1 and `grep -c 'WR-13' stock-dispatch.test.ts` returns at least 1.
    - The invariant test is proven non-vacuous: temporarily add the fork-provides claim shape to a shipped module's non-comment code, confirm `node --test stock-dispatch.test.ts` fails naming that file, then revert. Record both outputs in the SUMMARY.
    - `cd .claude/mcp/vice && node --test stock-dispatch.test.ts capability-registry.test.ts` exits 0.
    - `cd .claude/mcp/vice && npm run typecheck && npm run test:automated` both exit 0.
    - The SUMMARY records the stock-manifest tool count and the confirmation that every one has a `stockHandlerFor()` entry, so the "unreachable today" claim is re-measured rather than inherited.
  </acceptance_criteria>
  <reversibility rating="reversible">Changing a refusal message's source from a local literal to the registry renderer is a pure text-path change with no persisted state and no external contract; reverting is a one-commit revert.</reversibility>
  <done>One module renders capability refusals, the dead competing wording is gone with no commented-out remnant, and an invariant test proven by a reverted plant stops a third copy appearing.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols and files created by this plan:

- `cell()` — escaping helper in `scripts/generate-tool-support-table.mjs`
- `declStart` / `declEnd` / `declBody` bounding locals at the three declaration-scan sites
  (`scripts/generate-tool-support-table.mjs`, `.claude/mcp/vice/tool-support-table.test.mjs`,
  `.claude/mcp/vice/capability-registry.test.ts`)
- A cell-count invariant test in `.claude/mcp/vice/tool-support-table.test.mjs`
- A single-source refusal-wording invariant test in `.claude/mcp/vice/stock-dispatch.test.ts`

Removed by this plan (not drift): the locally-composed refusal message in
`.claude/mcp/vice/stock-dispatch.ts`'s `dispatchStock()` miss branch.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `capability-registry.ts` prose → generated markdown | Repo-owned strings are interpolated into a structured document format; unescaped separators corrupt structure |
| `vice-proxy.ts` source text → discovery regex | Source is parsed as text by three scanners; an unbounded window lets one declaration's data be attributed to another |
| caller ↔ `dispatchStock()` refusal text | The message tells a caller why a tool is refused; a wrong reason invites a pointless retry against the wrong backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-12 | Tampering | `docs/tool-support.md` row structure via unescaped registry prose (`WR-04`) | low | mitigate | `cell()` escaping at the single emission point plus a four-cell-per-row assertion; content is repo-owned, not untrusted input, so impact is document integrity rather than injection |
| T-15-13 | Spoofing | `discoverSyntheticToolNames()` attributing one declaration's `name` to another identifier (`WR-08`) | medium | mitigate | Bound each of the three independent scans to the declaration's own body so an unresolvable identifier throws; proven by a planted `name:`-less declaration |
| T-15-14 | Repudiation | a refusal message asserting the wrong providing backend (`WR-13`) | low | mitigate | Single-source rendering via `capabilityRefusalMessage()`, plus a shipped-module-wide invariant test against a second wording |
| T-15-SC | Tampering | npm/pip/cargo installs | — | accept | Not applicable — this plan installs no packages and adds no dependency |
</threat_model>

<verification>
- `cd .claude/mcp/vice && npm run typecheck && npm run test:automated` both exit 0
- `git diff --quiet docs/tool-support.md` after a fresh generator run
- Every one of `WR-04`, `WR-05`, `WR-08`, `WR-13` is named in the SUMMARY with its real current file:line and its evidence (commit sha for `WR-05`, planted-violation output for the other three)
- No shipped module outside `capability-registry.ts` carries a capability-refusal wording

<human-check>
Read `docs/tool-support.md`'s `vice_machine_config_set` row and confirm its note still reads
naturally after the escaping change — the escaping must be invisible in the rendered
document, not a visible backslash. Plan 15-09 edits that same row's text next.
</human-check>
</verification>

<success_criteria>
- Four Phase 08 findings closed with citable evidence
- The generated document is byte-stable and structurally guarded
- Capability refusals have exactly one source
</success_criteria>

<output>
Create `.planning/phases/15-debt-and-review-disposition/15-03-SUMMARY.md` when done.
Record: each finding id with its real current file:line, `WR-05`'s landing commit sha, all
planted-violation before/after outputs, and the re-measured stock-manifest handler count.
Do NOT move the Phase 08 review todo — plan 15-12 closes it once plan 15-02's findings
land too.
</output>
