---
phase: 15-debt-and-review-disposition
plan: 04
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - .claude/mcp/vice/stock-live.test.ts
  - .claude/mcp/vice/vice-proxy.test.ts
  - .claude/mcp/vice/stock-registers.ts
  - .claude/mcp/vice/stock-registers.test.ts
  - .planning/todos/pending/2026-08-22-phase-03-review-wr-06-through-in-06-never-dispositioned.md
  - .planning/STATE.md
autonomous: true
requirements: [GATE-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`stock-live.test.ts`'s three opt-in skip messages describe what the code actually does — no message promises a fallback that the branching cannot reach (WR-06)."
    - "`stock-live.test.ts` carries no interpreter shebang, matching every other `*.test.ts` in the directory (IN-05)."
    - "The live flag-refusal assertion matches the full phrase the handler emits, so a single-character status register name cannot make it match arbitrary refusal text (IN-06)."
    - "`registerCatalogFor()` caches the in-flight promise, so two concurrent handlers on a fresh session send exactly one REGISTERS_AVAILABLE — the module's stated fetched-once invariant is now true (IN-02)."
    - "No test comment in `stock-registers.test.ts` cites a source line range as the pre-fix location; the citation is a plan and commit instead (IN-04)."
    - "Each of `WR-07`, `WR-08` and `IN-03` is recorded with a verdict of fixed, superseded, or wont-fix, each verdict backed by a command whose output a reader can reproduce."
    - "`03-REVIEW.md` is unmodified — every heading and every finding body is byte-identical to before this plan."
  prohibitions:
    - "MUST NOT delete or reword a `*-REVIEW.md` finding heading so the parser stops seeing it — the finding is disposed in a disposition source, never edited away."
    - "MUST NOT record a finding as fixed unless the fix has landed and the disposition cites the commit or the re-runnable check that proves it."
    - "MUST NOT run `vice-proxy.test.ts`; it is `MANUAL_ONLY_TESTS` entry 2 and was confirmed timing out at 150 s outside a devcontainer on 2026-08-13, and this host has no devcontainer."
  artifacts:
    - .claude/mcp/vice/stock-live.test.ts
    - .claude/mcp/vice/stock-registers.ts
  key_links:
    - "**STATE.md ledger ownership.** `docs-deferred-ledger.test.ts` is a strict two-directional invariant between `## Deferred Items` and `.planning/todos/pending/`, and it is IN the automated gate — so any plan that files or moves a todo MUST update that table in the same commit, or `npm run test:automated` goes red. That is why `.planning/STATE.md` is declared in this plan's `files_modified` and why the ledger-touching plans of this phase run in separate waves: two concurrent plans editing that one table would collide."
    - "`stock-live.test.ts` is `MANUAL_ONLY_TESTS` entry 4 but is default-SKIP and safe to run: measured 14 tests / 14 skipped / exit 0 in 165 ms at plan time. It is the automated verify for three of these findings."
    - "`vice-proxy.test.ts` is in the same list but HANGS. `WR-07` and `IN-03` must be verified by source assertion plus `npm run typecheck` — there is no runnable gate for them."
    - "`registerCatalogFor()`'s promise-caching change alters a `WeakMap`-style cache's value type; every reader of `catalogs` must be updated in the same commit or `tsc --noEmit` fails."
---

<objective>
Disposition the eight `03-REVIEW.md` findings that plan 15-01's widened parser made
visible for the first time. Each is re-verified against current source before anything is
touched, then either fixed or recorded as superseded — because several of them already are.

Purpose: these eight sat in a file whose every finding heading is level 4, so
`docs-review-disposition.test.ts` could not see any of them until plan 15-01. They are not
merely undispositioned; they were undiscovered. Plan-time re-verification against the tree
at `3f0089f` already found the ground truth has moved: `WR-08`'s subject (a stale README
Development section) no longer exists at all, `IN-03`'s premise (a dead `NetServer` union
member) is falsified because `NetServer` is now genuinely used at three sites, and `IN-06`
is half-fixed (the metacharacter escaping landed; the weak anchoring did not). Applying the
review's written fixes without re-verifying would produce three wrong changes.

Output: five real fixes, three recorded supersession/wont-fix verdicts, and a closed
tracking todo — with `03-REVIEW.md` itself untouched.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-debt-and-review-disposition/15-RESEARCH.md
@.planning/phases/15-debt-and-review-disposition/15-PATTERNS.md
@.planning/phases/03-direct-tools/03-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the three stock-live.test.ts findings (WR-06, IN-05, IN-06)</name>
  <files>.claude/mcp/vice/stock-live.test.ts</files>
  <read_first>
    - `.planning/phases/03-direct-tools/03-REVIEW.md` lines 217-233 (`WR-06`), 318-325 (`IN-05`), 326-333 (`IN-06`) — each with a written fix.
    - `.claude/mcp/vice/stock-live.test.ts` — read the whole file. `WR-06`'s subject now appears at three sites, not the single one the review cited: around :83 (the 3.9-or-later default), :165 and :177 (the 3.9 and 3.10 variants added later). `IN-06`'s site has moved from the review's :373-377 to around :466, where the register name is now regex-escaped but still unanchored. `IN-05`'s subject is line 1.
    - Any other `*.test.ts` in `.claude/mcp/vice` — confirm by `head -1` across the directory that none carries an interpreter shebang, so removing it is convergence with the local convention rather than an isolated change.
    - `.claude/mcp/vice/test-gate.mjs` :83-92 — `MANUAL_ONLY_TESTS`; `stock-live.test.ts` is entry 4, default-SKIP, and safe to run. Confirm the skip behaviour before relying on it as this task's verify.
  </read_first>
  <action>
`WR-06`: the skip messages describe a fallback that the branching cannot reach — the
resolved path only falls back when the environment variable is unset, and when it is unset
the earlier branch skips unconditionally, so the documented default is never what the tests
run against. A user setting the variable to a non-path value gets the second skip reason
instead. Take the review's first option and delete the false sentence from all three sites,
rather than its second option (making the fallback real by accepting only values starting
with a slash) — the second option changes opt-in behaviour for a live test suite, which is
out of a disposition plan's remit and would need its own live re-verification. Say so in
the SUMMARY so the choice between the review's two offered fixes is on the record.

`IN-05`: remove the interpreter shebang from line 1. The file is only ever run by
`node --test` and no sibling test file carries one.

`IN-06`: the assertion currently builds a pattern from the live catalog's status-register
name, escaped but unanchored. On a build whose status register is a single character the
pattern matches almost any refusal text, including one that never mentions the register.
Apply the review's fix: assert on the full phrase the handler actually emits around the
register name, so the assertion cannot pass on unrelated text. Read the handler's real
message shape in `.claude/mcp/vice/stock-registers.ts` first and quote the phrase the
handler emits today — do not invent a phrase from the review's illustrative example.

Do not weaken any assertion to make the default-skip run pass; all 14 tests skip by design
and a skip is not evidence about `IN-06`. Record in the SUMMARY that `IN-06`'s fix is
verified structurally (the pattern now contains the phrase) and is not exercised by the
default-skip run, so a future live opt-in run is what actually exercises it.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && npm run typecheck && node --test stock-live.test.ts && npm run test:automated</automated>
  </verify>
  <acceptance_criteria>
    - `cd .claude/mcp/vice && test "$(head -1 stock-live.test.ts | grep -c 'usr/bin/env' || true)" = "0"`.
    - `cd .claude/mcp/vice && test "$(grep -v -E '^\s*(//|\*|/\*)' stock-live.test.ts | grep -c 'truthy non-path' || true)" = "0"` — all three sites, not just the one the review cited.
    - The anchored assertion is present: `cd .claude/mcp/vice && grep -c 'as \\"' stock-live.test.ts` returns at least 1, and the pattern built at the former `new RegExp(statusEntry` site now includes a literal phrase fragment (verify by eye, and quote the new line in the SUMMARY).
    - `cd .claude/mcp/vice && node --test stock-live.test.ts` exits 0 with 14 tests and 14 skipped — the same shape as before the change, proving no test was accidentally un-skipped or dropped.
    - `cd .claude/mcp/vice && npm run typecheck && npm run test:automated` both exit 0.
    - `git diff --stat .planning/phases/03-direct-tools/03-REVIEW.md` is empty.
  </acceptance_criteria>
  <done>Three `stock-live.test.ts` findings are fixed at every current site, the default-skip run has the same test/skip counts as before, and the choice between `WR-06`'s two offered fixes is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the two stock-registers findings, and record the vice-proxy.test.ts pair's verdicts (IN-02, IN-04, WR-07, IN-03)</name>
  <files>.claude/mcp/vice/stock-registers.ts, .claude/mcp/vice/stock-registers.test.ts, .claude/mcp/vice/vice-proxy.test.ts</files>
  <read_first>
    - `.planning/phases/03-direct-tools/03-REVIEW.md` lines 291-300 (`IN-02`), 311-317 (`IN-04`), 234-261 (`WR-07` in full — its body is longer than the others and names both the false-negative class and the mid-word `text:` marker problem), 301-310 (`IN-03`).
    - `.claude/mcp/vice/stock-registers.ts` — `registerCatalogFor()` and the module-scope `catalogs` cache. Note the doc comment above the function and the module's WHAT-NOT-TO-DO rule about re-fetching the catalog per call; the finding is that the code contradicts its own stated invariant. Find every other reader of `catalogs`.
    - `.claude/mcp/vice/stock-registers.test.ts` lines 45-58 — the comment citing a source line range as the pre-fix location. The cited range has drifted; the review's fix is to cite the plan and commit instead.
    - `.claude/mcp/vice/vice-proxy.test.ts` — the identity detector around the range `WR-07` cites, and the `OPEN_SERVERS` set plus every `NetServer` use site (`:38`, `:145`, `:2258`, `:2295`, `:2436` at plan time). This file must be READ, never RUN.
    - `.claude/mcp/vice/docs-linerefs.test.ts` — the existing guard that mechanically checks source line citations in prose. Check whether the `IN-04` comment is within its scope; if it is, the fix must keep that guard green.
  </read_first>
  <action>
`IN-02`: cache the in-flight promise instead of the resolved catalog, so two concurrent
handlers on a fresh session share one REGISTERS_AVAILABLE round trip. Keep the existing
zero-registers throw semantics: a build enumerating zero registers must still fail on every
call rather than caching an empty catalog, so the rejected promise must not be left in the
cache — evict it on rejection. Update the function's doc comment so the fetched-once claim
is now true, and update every other reader of the cache for the changed value type.
Add a test in `stock-registers.test.ts` that drives two concurrent `registerCatalogFor()`
calls against one stubbed session and asserts the stub's send count is exactly 1, plus a
second test asserting that after a rejected first call a subsequent call retries (the
cache did not memoise the failure).

`IN-04`: replace the drifted source-line citation in the test comment with a plan-and-commit
citation, per the review's fix. Verify the commit sha the review names still resolves
(`git cat-file -e`); if it does not, find the real commit for that change with `git log -S`
on a distinctive fragment and cite that instead.

`WR-07`: re-verify first. Read the current identity detector and decide whether the
false-negative class the review names still exists — specifically whether thrown-error paths
are still exempted, and whether the marker match can still hit mid-word inside a longer
identifier. If it still exists, apply the review's fix and word-boundary the markers. If the
detector has since been rewritten, record `superseded` with the current line range and the
command whose output shows it. Either way the verdict is a source assertion plus
`npm run typecheck`; there is no runnable gate, because this file hangs.

`IN-03`: re-verify first. The review's premise is that the leak registry's `NetServer` union
member is dead. At plan time `NetServer` is used at three sites in this file, so the premise
appears falsified. Confirm whether a `net.Server` instance is actually added to the leak
registry today, and whether the registry's teardown path still relies on a method that does
not exist on `net.Server`. If the union member is genuinely exercised and torn down
correctly, record `superseded` with the three use sites. If it is exercised but torn down by
a method `net.Server` lacks, that is a live defect — apply the review's fix (unref, or
destroy tracked sockets, for that branch) rather than recording it away.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && npm run typecheck && node --test stock-registers.test.ts && node --test docs-linerefs.test.ts && npm run test:automated</automated>
  </verify>
  <acceptance_criteria>
    - `cd .claude/mcp/vice && node --test stock-registers.test.ts` exits 0 and reports at least two more tests than before this task.
    - The concurrency test is non-vacuous: temporarily revert `registerCatalogFor()` to caching the resolved value, confirm the new send-count test fails with a count of 2, then restore. Record both outputs in the SUMMARY.
    - The rejection-eviction test is non-vacuous the same way: temporarily leave the rejected promise cached, confirm the retry test fails, then restore.
    - The doc comment's fetched-once claim is now true rather than aspirational: the SUMMARY quotes the updated comment verbatim alongside the new cache write, and the send-count test above is the mechanical proof that the claim holds.
    - `cd .claude/mcp/vice && test "$(grep -c 'stock-registers.ts:260-268' stock-registers.test.ts || true)" = "0"`, and the replacement citation's commit sha resolves: `git cat-file -e <sha>` exits 0.
    - `cd .claude/mcp/vice && node --test docs-linerefs.test.ts` exits 0.
    - `WR-07` and `IN-03` each have a verdict recorded in the SUMMARY with the exact command and output that establishes it (for example the `grep -n` of the current use sites), and — if either was recorded `superseded` — an explicit statement that no source change was made and why.
    - `vice-proxy.test.ts` was never executed: the SUMMARY states this, and no run of it appears in the plan's command log.
    - `cd .claude/mcp/vice && npm run typecheck && npm run test:automated` both exit 0.
  </acceptance_criteria>
  <done>`IN-02` and `IN-04` are fixed with non-vacuous tests, and `WR-07` and `IN-03` each carry a re-verified verdict backed by a reproducible command rather than by the review's year-old premise.</done>
</task>

<task type="auto">
  <name>Task 3: Record WR-08 as superseded and close the tracking todo</name>
  <files>.planning/todos/pending/2026-08-22-phase-03-review-wr-06-through-in-06-never-dispositioned.md</files>
  <read_first>
    - `.planning/phases/03-direct-tools/03-REVIEW.md` lines 262-279 (`WR-08`) — the finding is about `README.md`'s Development section claiming three manual-only files when there were four, and omitting an environment variable from an Environment table.
    - `README.md` — read the current Development section. At plan time the section was simplified to a short install-and-test block and contains neither the manual-only count nor an Environment table, so the specific wrong claim no longer exists to be wrong.
    - `.planning/todos/pending/2026-08-22-phase-03-review-wr-06-through-in-06-never-dispositioned.md` — the todo plan 15-01 filed, with its per-finding table.
    - `.planning/todos/completed/2026-08-21-anno-cli-wr-08-option-values-silently-swallowed.md` — the completed-todo `## Resolution` shape to copy (bold one-line verdict, rationale, consequence).
    - `.planning/todos/pending/2026-08-12-vice-broker-tests-stall-outside-devcontainer.md` — its in-file `## DISPOSITION` section is `15-PATTERNS.md`'s named model for the `wont-fix` verdict shape.
  </read_first>
  <action>
Record `WR-08` as **superseded**, not fixed and not wont-fix. Its subject text is gone:
`README.md` no longer contains the manual-only count or the Environment table the finding is
about, because the Development section was deliberately simplified after the review was
written. Establish that with a command rather than asserting it — a grep whose zero-count
output goes in the SUMMARY — and note the current manual-only file count (8 entries in
`test-gate.mjs`'s `MANUAL_ONLY_TESTS` at plan time) so a reader can see that even the
corrected number the review wanted would now be wrong again. That is the substance of the
supersession: the document stopped making the claim rather than the claim being corrected.

Then move the todo from `pending/` to `completed/` with `git mv` and append a `## Resolution`
section in the established shape: bold one-line verdict, then a per-finding table with
columns `Id | Verdict | Evidence`, then the consequence. Verdicts must be exactly the ones
Tasks 1 and 2 recorded, each with its citation — the commit for a fix, the reproducible
command output for a supersession. Name the date (2026-08-22), the phase and plan
(Phase 15, plan 15-04), and state that `03-REVIEW.md` was not edited.

Do not delete the todo file. The record moves; it never disappears.

Closing the todo removes a `pending/` file, which reddens `docs-deferred-ledger.test.ts` — a
strict two-directional invariant that is IN the automated gate. So remove that row from
STATE.md's `## Deferred Items` table and decrement both prose count figures in this same
commit. This is not conditional: the guard is one of the four `audit-gate.mjs` spawns, and
leaving it red blocks any milestone audit recording `status: passed`.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && node --test docs-review-disposition.test.ts && node --test docs-deferred-ledger.test.ts && npm run test:automated</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/todos/completed/2026-08-22-phase-03-review-wr-06-through-in-06-never-dispositioned.md` succeeds and `test -e .planning/todos/pending/2026-08-22-phase-03-review-wr-06-through-in-06-never-dispositioned.md` fails.
    - `git log --diff-filter=R --name-status -1` (or `git status --porcelain`) shows the file as a rename, not a delete-plus-add.
    - `for id in WR-06 WR-07 WR-08 IN-02 IN-03 IN-04 IN-05 IN-06; do grep -qw "$id" .planning/todos/completed/2026-08-22-phase-03-review-wr-06-through-in-06-never-dispositioned.md || echo "MISSING $id"; done` prints nothing.
    - `grep -c '## Resolution' .planning/todos/completed/2026-08-22-phase-03-review-wr-06-through-in-06-never-dispositioned.md` returns 1, and the section contains one table row per finding with a non-empty Evidence cell.
    - `test "$(grep -c 'manual-only' README.md || true)" = "0"` — the recorded supersession claim is reproducible.
    - `cd .claude/mcp/vice && node --test docs-review-disposition.test.ts` exits 0 — all eight ids remain dispositioned after the move (the guard reads `completed/` as well as `pending/`).
    - `cd .claude/mcp/vice && node --test docs-deferred-ledger.test.ts` exits 0 in both directions.
    - `cd .claude/mcp/vice && npm run test:automated` exits 0.
  </acceptance_criteria>
  <done>All eight `03-REVIEW.md` findings carry a cited verdict in a completed todo, the guard and the ledger guard are both green, and the review file is untouched.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols and files created by this plan:

- A concurrency test and a rejection-retry test in `.claude/mcp/vice/stock-registers.test.ts`
- `.planning/todos/completed/2026-08-22-phase-03-review-wr-06-through-in-06-never-dispositioned.md`
  (renamed from `pending/` by this plan; created by plan 15-01)

Removed by this plan (not drift): the interpreter shebang on line 1 of
`.claude/mcp/vice/stock-live.test.ts`; the three false skip-message sentences in the same
file; the drifted source-line citation in `.claude/mcp/vice/stock-registers.test.ts`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test skip-reason text → operator | A skip message tells an operator how to opt in; a false message sends them down a path that cannot work |
| concurrent handlers → one stock session | Two handlers share one `StockConnectSession`; an unsynchronised cache lets both issue the same wire command |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-15 | Repudiation | `stock-live.test.ts` opt-in skip messages (`WR-06`) | low | mitigate | Delete the unreachable-fallback sentence at all three sites; the message then describes only branches the code has |
| T-15-16 | Denial of service | duplicate REGISTERS_AVAILABLE from concurrent handlers on one session (`IN-02`) | low | mitigate | Cache the in-flight promise with eviction on rejection; pinned by a send-count test proven against a reverted implementation |
| T-15-17 | Tampering | a weakly-anchored live assertion passing on unrelated refusal text (`IN-06`) | low | mitigate | Assert the handler's full emitted phrase, not a bare register name that may be one character |
| T-15-SC | Tampering | npm/pip/cargo installs | — | accept | Not applicable — this plan installs no packages and adds no dependency |
</threat_model>

<verification>
- `cd .claude/mcp/vice && npm run typecheck && npm run test:automated` both exit 0
- `cd .claude/mcp/vice && node --test docs-review-disposition.test.ts docs-deferred-ledger.test.ts docs-linerefs.test.ts` exits 0
- `node --test stock-live.test.ts` reports 14 tests / 14 skipped / exit 0 — unchanged shape
- `git diff --stat` touches no `*-REVIEW.md`
- `vice-proxy.test.ts` was never executed
</verification>

<success_criteria>
- All eight newly-surfaced `03-REVIEW.md` findings carry a cited verdict
- Every verdict recorded as `superseded` is backed by a reproducible command, not by inference
- Five real fixes land, each with a non-vacuous test or a structural assertion
</success_criteria>

<output>
Create `.planning/phases/15-debt-and-review-disposition/15-04-SUMMARY.md` when done.
Record: the per-finding verdict table with evidence, both planted-revert probe outputs, the
real current line numbers for every site touched (the review's cited lines have drifted),
and an explicit statement that `vice-proxy.test.ts` was read but never run.
</output>
