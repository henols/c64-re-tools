---
phase: 15-debt-and-review-disposition
plan: 07
type: execute
wave: 5
depends_on: ["15-06"]
files_modified:
  - .claude/mcp/vice/build-atomic.test.ts
  - .claude/mcp/vice/fixtures/binmon/cpuhistory-get.json
  - .claude/mcp/vice/fixtures/binmon/cpuhistory-get-multi.json
  - .claude/mcp/vice/fixtures/binmon/README.md
  - .claude/mcp/vice/r2000-test-gate.ts
  - .claude/mcp/vice/r2000-cli.test.ts
  - .claude/mcp/vice/r2000-project.test.ts
  - .claude/mcp/vice/disasm-roundtrip.test.ts
  - .planning/todos/pending/2026-08-22-build-atomic-cleanup-test-races-on-shared-tmp.md
  - .planning/todos/pending/2026-08-22-cpuhistory-get-sidecars-mislabel-the-fork-as-stock.md
  - .planning/todos/pending/2026-08-21-migrate-hand-copied-acme-gates-to-r2000-test-gate.md
  - .planning/STATE.md
autonomous: true
requirements: [DEBT-01]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "STATE.md's `## Deferred Items` table and both of its prose count figures are updated in the same commits that move this plan's todos out of `pending/`, so `docs-deferred-ledger.test.ts` — which is in the automated gate — is never left red."
    - "`build-atomic.test.ts`'s cleanup assertion scans a private parent directory, not the shared system temp directory, so a concurrent `build()` caller in another test file can no longer redden it — the flake that produced 1 failure in run 1 and 0 in run 2 with no code change between them is gone."
    - "`cpuhistory-get.json` and `cpuhistory-get-multi.json` record `capturedFrom` naming the fork build that actually answered, and the fixture provenance table agrees with both files."
    - "No test file in `.claude/mcp/vice` carries its own copy of the ACME gate or the regenerator2000 gate; all five consumers import from `r2000-test-gate.ts`, and the timeout divergence the todo observed (two copies pass no timeout, the seam passes 10 s) is gone."
    - "`r2000-test-gate.ts`'s HONEST SCOPE paragraph is deleted, because it documented exactly this todo and becomes false the moment the migration lands."
    - "Each of the three todos has moved to `completed/` with a `## Resolution` citing the commit and the re-runnable check."
  prohibitions:
    - "MUST NOT close a todo `wont-fix` without a written rationale in its `## Resolution`."
    - "MUST NOT delete a pending todo file — the record moves to `completed/`, it never disappears."
    - "MUST NOT change the pass counts of the migrated test files. `11-VERIFICATION.md` cites `r2000-cli.test.ts`'s test 35 by name as already-verified Phase 11 evidence; a gate migration that alters what runs re-cuts the ground under that evidence."
  artifacts:
    - .claude/mcp/vice/fixtures/binmon/README.md
  key_links:
    - "**STATE.md ledger ownership.** `docs-deferred-ledger.test.ts` is a strict two-directional invariant between `## Deferred Items` and `.planning/todos/pending/`, and it is IN the automated gate — so any plan that files or moves a todo MUST update that table in the same commit, or `npm run test:automated` goes red. That is why `.planning/STATE.md` is declared in this plan's `files_modified` and why the ledger-touching plans of this phase run in separate waves: two concurrent plans editing that one table would collide."
    - "`r2000-test-gate.ts` exports `R2000_BIN`, `probeR2000()`, `R2000_AVAILABLE`, `skipReasonFor()`, `assertR2000RequiredIfEnvSet()`, `ACME_BIN`, `probeAcme()`, `ACME_AVAILABLE`, `acmeSkipReasonFor()`, `assertAcmeRequiredIfEnvSet()` — the migration replaces local copies with these exact names, nothing new is added."
    - "`node --test '*.test.*'` runs test FILES concurrently, which is why `build-atomic.test.ts`'s scan of the shared temp directory races ten other `build()` callers."
    - "`binmon-fixtures.test.ts`'s `capturedFrom` assertions run against synthetic fixtures it writes itself, plus one EXTV-01 test scoped to the three re-recorded fixtures — check whether either reaches the two `cpuhistory-get*` files before editing them."
---

<objective>
Fix the three pending todos that are genuinely cheap, fully diagnosed, self-contained, and
owned by no other phase: a test-isolation flake, two mislabelled fixture sidecars, and the
hand-copied test-gate migration.

Purpose: DEBT-01's criterion admits three outcomes per todo — fixed with a commit, `wont-fix`
with rationale, or promoted with a named owner. These three are all cheapest as fixes: each
carries its root cause and its exact fix already written in the todo. The gate migration is
`15-RESEARCH.md`'s Open Question 2, resolved here as **fix in-phase**: it is small, its
replacement code is specified export-by-export, and its stated reason for deferral (an audit
should not re-cut the ground under evidence it is auditing) does not apply to a disposition
phase whose whole job is closing carried items — provided the migrated files' pass counts are
proven unchanged, which is this plan's own acceptance bar.

Output: three fixes, three closed todos, and no change to any migrated file's pass count.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-debt-and-review-disposition/15-RESEARCH.md
@.planning/phases/15-debt-and-review-disposition/15-PATTERNS.md
@.planning/todos/pending/2026-08-22-build-atomic-cleanup-test-races-on-shared-tmp.md
@.planning/todos/pending/2026-08-22-cpuhistory-get-sidecars-mislabel-the-fork-as-stock.md
@.planning/todos/pending/2026-08-21-migrate-hand-copied-acme-gates-to-r2000-test-gate.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scope build-atomic.test.ts's cleanup scan to a private parent (test-isolation flake)</name>
  <files>.claude/mcp/vice/build-atomic.test.ts, .planning/todos/pending/2026-08-22-build-atomic-cleanup-test-races-on-shared-tmp.md</files>
  <read_first>
    - `.planning/todos/pending/2026-08-22-build-atomic-cleanup-test-races-on-shared-tmp.md` — the full diagnosis: `tempSiblingsOf()` lists `dirname(outDir)`, and `outDir` comes from a temp-root `mkdtempSync`, so the parent IS the shared system temp directory. The fix shape is given.
    - `.claude/mcp/vice/build-atomic.test.ts` — read the whole file. The failing test starts around :183; `tempSiblingsOf()` is around :186-189; the success and failure paths each create their own out-dir.
    - `.claude/mcp/vice/build.ts` — the staging-directory naming (`.build-tmp-` prefix) and where it is created relative to `outDir`. The assertion's meaning depends on that relationship; the fix must preserve exactly what is being asserted.
    - `grep -rln 'build(' .claude/mcp/vice/*.test.ts` — the todo names ten other files that call `build()`. Confirm the list, and record it in the SUMMARY as the concurrency surface the fix immunises against.
  </read_first>
  <action>
Create one private wrapper directory per test run and place both the success out-dir and the
failure out-dir inside it, so the sibling scan's parent is owned by this test alone. The
assertion's meaning is unchanged — no staging directory survives next to the out-dir — but it
is no longer sensitive to any other process's temp usage.

Clean up the wrapper in a `finally` so the test leaves nothing behind itself; the todo's own
evidence notes the staging directories *are* cleaned up correctly, which is the very thing
the test proves, so the fix must not weaken that.

Prove the fix addresses the real race rather than merely passing: while the test runs, create
a directory with the staging prefix directly in the shared system temp directory, confirm the
test still passes (it no longer sees it), then remove it. Before the fix that same plant
reddens the assertion — demonstrate that too, against a temporary revert. Record both
outcomes in the SUMMARY; a fix for a flake that is only ever observed passing is
indistinguishable from luck.

Then `git mv` the todo to `completed/` with a `## Resolution` citing the commit, the two
planted-directory outcomes, and the fact that the flake was pre-existing and unreachable from
`audit-gate.mjs` (which spawns only the four planning-doc guards), so no milestone-audit
verdict was ever affected by it.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && npm run typecheck && node --test build-atomic.test.ts && npm run test:automated</automated>
  </verify>
  <acceptance_criteria>
    - `cd .claude/mcp/vice && node --test build-atomic.test.ts` exits 0 with 6 tests passing, run three times consecutively with no failure.
    - The scan parent is private: `grep -c 'mkdtempSync' build-atomic.test.ts` returns at least 3 (wrapper plus both out-dirs), and no `dirname(` call in the cleanup test resolves to the process-wide temp root — verify by eye and quote the new `tempSiblingsOf()` in the SUMMARY.
    - The planted shared-temp directory probe is recorded in the SUMMARY with both outcomes: green after the fix, red against a temporary revert.
    - `cd .claude/mcp/vice && npm run test:automated` exits 0, run twice consecutively (the flake's original signature was a difference between two consecutive runs).
    - The todo is renamed to `completed/`, not deleted, with exactly one `## Resolution` citing a resolvable commit sha.
  </acceptance_criteria>
  <done>The cleanup assertion is immune to concurrent `build()` callers, proven by a planted shared-temp directory that reddens the old code and not the new, and two consecutive full-gate runs are green.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the two cpuhistory-get sidecars' provenance labels and the fixture table</name>
  <files>.claude/mcp/vice/fixtures/binmon/cpuhistory-get.json, .claude/mcp/vice/fixtures/binmon/cpuhistory-get-multi.json, .claude/mcp/vice/fixtures/binmon/README.md, .planning/todos/pending/2026-08-22-cpuhistory-get-sidecars-mislabel-the-fork-as-stock.md</files>
  <read_first>
    - `.planning/todos/pending/2026-08-22-cpuhistory-get-sidecars-mislabel-the-fork-as-stock.md` — the full diagnosis, including that the third sidecar in the same family is correctly labelled and must not be touched.
    - `.claude/mcp/vice/fixtures/binmon/cpuhistory-get.json` and `cpuhistory-get-multi.json` — the `capturedFrom` values. Both name a kind and a path; the path is this host's fork build while the kind says otherwise.
    - `.claude/mcp/vice/fixtures/binmon/cpuhistory-get-unsupported.json` — the correctly-labelled sibling, for the target shape.
    - `.claude/mcp/vice/fixtures/binmon/README.md` — the provenance table rows for these two cases.
    - `.claude/mcp/vice/binmon-fixtures.test.ts` — check every `capturedFrom` assertion. Most run against synthetic fixtures the test writes itself; the EXTV-01 test around :326 is scoped to the three re-recorded fixtures. Confirm whether any assertion reaches these two files by literal name before editing, and record the answer.
    - Re-verify the binary identity claim yourself rather than inheriting it: run the fork binary's help output and count its `-mcpserver` mentions, and do the same for the genuine stock binary, so the SUMMARY records a measurement from this session rather than a quotation.
  </read_first>
  <action>
Correct `capturedFrom`'s kind token in both sidecars so it names the fork build that
actually answered, keeping the same path and the same string shape as the correctly-labelled
sibling. Change nothing else in either file — these are real captures, and the defect is the
operator-typed kind, not the bytes.

Update the two provenance-table rows in the fixtures README to agree, and add a one-line note
to the README recording that the kind token is operator-supplied rather than derived, so a
future reader knows the field can be wrong in this specific way. The todo's last bullet asks
whether the capture path should derive the kind automatically instead of trusting an
environment variable — do NOT implement that here: it changes `probe-binmon.mjs`, the file
whose evidence-immutability status plan 15-05 just recorded for `13-REVIEW.md WR-02`. Record
it as a named follow-on in the todo's Resolution instead, so DEBT-01's "promoted with a named
owner" applies to that sub-item.

Then `git mv` the todo to `completed/` with a `## Resolution` citing the commit, the
re-measured `-mcpserver` counts for both binaries, and the named follow-on for the
derive-the-kind-automatically sub-item.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && node --test binmon-fixtures.test.ts && npm run test:automated</automated>
  </verify>
  <acceptance_criteria>
    - `cd .claude/mcp/vice && grep -c 'fork:' fixtures/binmon/cpuhistory-get.json` returns 1 and the same for `cpuhistory-get-multi.json`.
    - `cd .claude/mcp/vice && test "$(grep -l 'stock:/usr/local/bin' fixtures/binmon/*.json 2>/dev/null | wc -l)" = "0"` — no sidecar still pairs the stock kind with the fork path.
    - The correctly-labelled sibling is untouched: `git diff --stat .claude/mcp/vice/fixtures/binmon/cpuhistory-get-unsupported.json` is empty.
    - Only the kind token changed: `git diff .claude/mcp/vice/fixtures/binmon/cpuhistory-get.json` shows exactly one changed line, and the same for the multi file.
    - The README table agrees: `grep -c 'fork' fixtures/binmon/README.md` returns at least 2, and no row for these two cases still says otherwise.
    - `cd .claude/mcp/vice && node --test binmon-fixtures.test.ts` exits 0.
    - The SUMMARY records the re-measured `-mcpserver` counts for both binaries from this session, and states whether any test asserted these two files' `capturedFrom` by literal name.
    - The todo is renamed to `completed/` with exactly one `## Resolution` naming the follow-on owner for the derive-the-kind sub-item.
  </acceptance_criteria>
  <done>Both sidecars name the build that actually answered, the provenance table agrees, the correctly-labelled sibling is untouched, and the derive-the-kind follow-on has a named owner.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the hand-copied ACME and regenerator2000 gates onto r2000-test-gate.ts</name>
  <files>.claude/mcp/vice/r2000-test-gate.ts, .claude/mcp/vice/r2000-cli.test.ts, .claude/mcp/vice/r2000-project.test.ts, .claude/mcp/vice/disasm-roundtrip.test.ts, .planning/todos/pending/2026-08-21-migrate-hand-copied-acme-gates-to-r2000-test-gate.md</files>
  <read_first>
    - `.planning/todos/pending/2026-08-21-migrate-hand-copied-acme-gates-to-r2000-test-gate.md` — the full instruction, including the corrected file list (three files, not two: `r2000-project.test.ts` carries a third independently-divergent copy) and the exact export names to import.
    - `.claude/mcp/vice/r2000-test-gate.ts` — the seam. Exports at plan time: `R2000_BIN` :39, `probeR2000()` :46, `R2000_AVAILABLE` :56, `skipReasonFor()` :66, `assertR2000RequiredIfEnvSet()` :87, `ACME_BIN` :124, `probeAcme()` :130, `ACME_AVAILABLE` :142, `acmeSkipReasonFor()` :151, `assertAcmeRequiredIfEnvSet()` :166. The HONEST SCOPE paragraph is at :108.
    - `.claude/mcp/vice/r2000-cli.test.ts` — its local ACME block (~:698-726) and its local regenerator2000 block (~:457-472).
    - `.claude/mcp/vice/r2000-project.test.ts` — its local regenerator2000 block (~:133-175).
    - `.claude/mcp/vice/disasm-roundtrip.test.ts` — its local ACME block, the one that established the environment-variable convention.
    - `.planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/11-VERIFICATION.md` — find where it cites `r2000-cli.test.ts`'s test 35 by name. That citation is why the pass count must be proven unchanged, not assumed.
    - Record each file's baseline pass/skip/total counts BEFORE any edit, with and without the two opt-in environment variables set, so the after-comparison is against measured numbers rather than the todo's remembered ones.
  </read_first>
  <action>
Replace each local block with imports from `r2000-test-gate.ts`, using the export names
listed above and adding nothing new to the seam. Note the divergence the todo observed and
which way to resolve it: two of the local copies pass no timeout to their probe spawn while
the seam passes 10 seconds. Converge on the seam's timeout — that is the drift the seam
exists to stop — and record in the SUMMARY that the migrated files now have a bounded probe
where two of them previously had none.

Delete the HONEST SCOPE paragraph from `r2000-test-gate.ts`'s ACME header. It documents this
todo and becomes false the moment the migration lands; leaving it is the same stale-provenance
defect as plan 15-02's copied-from comment.

Verify with the todo's own command and with the baselines measured in `read_first`: run each
migrated file with both opt-in environment variables set, and confirm the pass/skip/total
counts match the baseline exactly. If any count moves, stop — that is the exact risk the
todo's deferral rationale named, and a changed count means the gate semantics changed rather
than their implementation. Investigate and record before proceeding; do not adjust a baseline
to match a new number.

Then `git mv` the todo to `completed/` with a `## Resolution` citing the commit, the
before/after counts per file (both with and without the opt-ins), and the timeout convergence.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && npm run typecheck && VICE_REQUIRE_R2000=1 VICE_REQUIRE_ACME=1 node --test r2000-cli.test.ts disasm-roundtrip.test.ts r2000-answer-key.test.ts r2000-project.test.ts && npm run test:automated</automated>
  </verify>
  <acceptance_criteria>
    - No local gate copy remains: `cd .claude/mcp/vice && test "$(grep -v -E '^\s*(//|\*|/\*)' r2000-cli.test.ts r2000-project.test.ts disasm-roundtrip.test.ts | grep -cE '(function probeAcme|function probeR2000|const ACME_AVAILABLE|const R2000_AVAILABLE)' || true)" = "0"`.
    - All three files import the seam: `grep -c 'r2000-test-gate' r2000-cli.test.ts`, `... r2000-project.test.ts`, `... disasm-roundtrip.test.ts` each return at least 1.
    <!-- planner-discipline-allow: HONEST SCOPE -->
    - The stale scope paragraph is gone: `cd .claude/mcp/vice && test "$(grep -c 'HONEST SCOPE' r2000-test-gate.ts || true)" = "0"`.
      (The marker is named in this task's `<action>` because the executor cannot delete a paragraph it is not told how to find; the grep targets a `.ts` source file, never this plan.)
    - Pass counts unchanged: the SUMMARY shows a before/after table per migrated file for both the default run and the opt-in run, with identical numbers in every cell.
    - `11-VERIFICATION.md`'s cited test still runs: the opt-in run of `r2000-cli.test.ts` shows the same test name and the same verdict as the baseline.
    - `cd .claude/mcp/vice && npm run typecheck` exits 0.
    - `cd .claude/mcp/vice && npm run test:automated` exits 0.
    - The todo is renamed to `completed/` with exactly one `## Resolution` citing a resolvable commit sha.
    - STATE.md's `## Deferred Items` table lost one row per todo this plan moved out of `pending/`, both prose
      count figures were decremented to match, and `cd .claude/mcp/vice && node --test docs-deferred-ledger.test.ts`
      exits 0 in both directions. This is not deferrable to plan 15-12: the guard is in the automated gate, so a
      missing row reddens `npm run test:automated` for every later wave.
  </acceptance_criteria>
  <reversibility rating="reversible">A test-gate migration touches only how the skip decision is computed, not what is asserted; the before/after count table is the proof, and reverting is a one-commit revert.</reversibility>
  <done>Five consumers share one gate implementation, two previously-unbounded probes are bounded, the paragraph documenting the duplication is gone, and every migrated file's pass count is proven identical.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols and files created by this plan:

- A private wrapper temp directory helper in `.claude/mcp/vice/build-atomic.test.ts`
- A provenance note about the operator-supplied kind token in `.claude/mcp/vice/fixtures/binmon/README.md`
- `.planning/todos/completed/2026-08-22-build-atomic-cleanup-test-races-on-shared-tmp.md` (renamed from `pending/`)
- `.planning/todos/completed/2026-08-22-cpuhistory-get-sidecars-mislabel-the-fork-as-stock.md` (renamed from `pending/`)
- `.planning/todos/completed/2026-08-21-migrate-hand-copied-acme-gates-to-r2000-test-gate.md` (renamed from `pending/`)

Removed by this plan (not drift): the local ACME and regenerator2000 gate blocks in
`r2000-cli.test.ts`, `r2000-project.test.ts` and `disasm-roundtrip.test.ts`; the HONEST SCOPE
paragraph in `r2000-test-gate.ts`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this test process ↔ the shared system temp directory | An assertion scoped to a world-writable directory is decided by other processes' behaviour, not by the code under test |
| operator-typed environment variable → committed fixture provenance | The capture path records a kind token the operator supplies; nothing validates it against the binary that answered |
| one test-gate implementation → five consumers | Converging five gates onto one seam means a defect in the seam skips or hard-fails all five at once |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-23 | Tampering | `build-atomic.test.ts`'s cleanup assertion reading a world-writable directory | low | mitigate | Scope the scan to a private per-run parent; proven by a planted staging directory in the shared temp root that reddens the old code and not the new |
| T-15-24 | Spoofing | committed fixture provenance naming the wrong backend kind | medium | mitigate | Correct both labels against a re-measured binary identity from this session, and record in the fixtures README that the kind token is operator-supplied so a reader knows the failure mode exists |
| T-15-25 | Denial of service | an unbounded probe spawn in a migrated test file | low | mitigate | Converge on the seam's bounded probe timeout, replacing two previously-unbounded spawns |
| T-15-SC | Tampering | npm/pip/cargo installs | — | accept | Not applicable — this plan installs no packages and adds no dependency |
</threat_model>

<verification>
- `cd .claude/mcp/vice && npm run typecheck` exits 0
- `cd .claude/mcp/vice && npm run test:automated` exits 0 on two consecutive runs
- `cd .claude/mcp/vice && node --test build-atomic.test.ts` exits 0 on three consecutive runs
- The opt-in run of the four regenerator2000/ACME test files matches the pre-migration baseline cell for cell
- Three todos are in `completed/`, each with one `## Resolution` citing a resolvable commit
</verification>

<success_criteria>
- Three pending todos fixed rather than deferred, each with a citable commit
- Every fix proven by a planted violation or a measured before/after baseline, never by a single green run
- No migrated test file's pass count changed
</success_criteria>

<output>
Create `.planning/phases/15-debt-and-review-disposition/15-07-SUMMARY.md` when done.
Record: the planted shared-temp probe's two outcomes, the re-measured binary identity counts,
the per-file before/after pass-count table for the gate migration (default and opt-in), the
ten-file `build()` concurrency surface, and the named owner for the derive-the-kind follow-on.
</output>
