---
phase: 15-debt-and-review-disposition
plan: 06
type: execute
wave: 4
depends_on: ["15-05"]
files_modified:
  - .claude/skills/c64-ram-capture/SKILL.md
  - .claude/skills/c64-ram-capture/RELEASES.json.example
  - .planning/todos/pending/2026-08-19-drive-type-prerequisite-undocumented-in-readme-and-skill.md
  - .planning/todos/pending/2026-08-19-project-paths-git-marker-requirement-undocumented.md
  - .planning/todos/pending/2026-08-19-releases-json-schema-undocumented.md
  - .planning/STATE.md
autonomous: true
requirements: [DEBT-01, DEBT-02]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A user reading `.claude/skills/c64-ram-capture/SKILL.md`'s disk-boot procedure learns that the drive type is configured automatically by the broker at launch and needs no user action — DEBT-02 item 1, documented where the procedure lives rather than in a changelog."
    - "A user reading the same file learns, before the first procedure step, that the toolkit's project root is `C64RE_PROJECT_ROOT` when set and otherwise the nearest ancestor holding a `.git` entry — so a scratch project needs `git init` or the env var — DEBT-02 item 2."
    - "The same file documents `RELEASES.json`'s shape: the top-level `schema_version` / `schema_notes` / `releases` keys, each entry's `id` / `canonical` / `disk_image` / `dumps` fields, and that `dumps` must be present as an array because the registry accessor reads its length with no guard — DEBT-02 item 3."
    - "A copyable `RELEASES.json.example` ships in the skill's own directory and parses as JSON."
    - "Each of the three DEBT-02 doc todos has moved to `completed/` with a `## Resolution` citing the file and heading the behaviour is now documented at."
    - "`installer/skills/` is untouched — it is a generated, gitignored mirror regenerated by `installer/scripts/sync-skills.mjs`, so editing it by hand would be a second copy of the same prose."
  prohibitions:
    - "MUST NOT document behaviour the code does not have — every sentence added here is checked against the module that implements it, and the check is recorded."
    - "MUST NOT close a todo `wont-fix` without a written rationale in its `## Resolution`."
    - "MUST NOT delete a pending todo file — the record moves to `completed/`, it never disappears."
  artifacts:
    - .claude/skills/c64-ram-capture/SKILL.md
    - .claude/skills/c64-ram-capture/RELEASES.json.example
  key_links:
    - "**STATE.md ledger ownership.** `docs-deferred-ledger.test.ts` is a strict two-directional invariant between `## Deferred Items` and `.planning/todos/pending/`, and it is IN the automated gate — so any plan that files or moves a todo MUST update that table in the same commit, or `npm run test:automated` goes red. That is why `.planning/STATE.md` is declared in this plan's `files_modified` and why the ledger-touching plans of this phase run in separate waves: two concurrent plans editing that one table would collide."
    - "`.claude/skills/` is the canonical skill source; `installer/skills/` is generated and gitignored. `scripts/check-npm-packages.mjs` validates that skills are present in both published tarballs — run it after adding a new file to a skill directory."
    - "`scripts/check-skill-tool-coverage.mjs` and `scripts/check-skill-fork-honesty.mjs` lint every skill file's tool mentions; new prose that names an `mcp__*` tool must satisfy both, and plan 15-02 tightens the second one's annotation rule."
    - "`project-paths.mjs` checks `C64RE_PROJECT_ROOT` BEFORE walking for `.git` — the order matters and the todo's own wording puts them the other way round."
---

<objective>
Close the three DEBT-02 documentation gaps that belong in `c64-ram-capture`'s playbook:
the drive-type prerequisite (now a closing note, not a workaround), the project-root
`.git`-marker requirement, and `RELEASES.json`'s schema. All three were confirmed silent in
both `README.md` and the skill at research time — a grep for every one of their key terms
across both files returned nothing.

Purpose: DEBT-02's criterion is "documented at the location a user would actually look".
For all three of these that location is the skill playbook, not `README.md` and certainly
not a `.planning/` todo. The `RELEASES.json` gap is the sharpest: its shape had to be
learned during the Phase 8.1 walkthrough by reading `releases.mjs` and `project-paths.mjs`
source, and an omitted `dumps` array throws rather than degrading, because the accessor
reads `r.dumps.length` with no guard.

Output: three prose additions in the skill's own terse imperative voice, a copyable
`RELEASES.json.example`, and three closed todos.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-debt-and-review-disposition/15-RESEARCH.md
@.planning/phases/15-debt-and-review-disposition/15-PATTERNS.md
@.planning/todos/pending/2026-08-19-drive-type-prerequisite-undocumented-in-readme-and-skill.md
@.planning/todos/pending/2026-08-19-project-paths-git-marker-requirement-undocumented.md
@.planning/todos/pending/2026-08-19-releases-json-schema-undocumented.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the drive-type closing note and the project-root prerequisite (DEBT-02 items 1 and 2)</name>
  <files>.claude/skills/c64-ram-capture/SKILL.md, .planning/todos/pending/2026-08-19-drive-type-prerequisite-undocumented-in-readme-and-skill.md, .planning/todos/pending/2026-08-19-project-paths-git-marker-requirement-undocumented.md</files>
  <read_first>
    - `.claude/skills/c64-ram-capture/SKILL.md` — read the whole file (322 lines). Headings at plan time: `## The order` :36, `## Read the disk first` :46, `## Boot a disk` :74, `## Capture at a trigger address` :84, `## Which skill does what` :276, `## References` :290, `## Troubleshooting` :307. Match the file's terse imperative voice and its numbered-step style; the file's own top note says there is no `references/` split because the workflow fits in one file.
    - `.claude/mcp/vice/resources/vice-launcher.sh` is not the source — read `.claude/mcp/vice/broker-launch.mts`'s `buildViceArgs()` and confirm the stock branch includes the drive-type flag **unconditionally**, not conditioned on a disk being attached. The todo's whole "moot rather than merely narrower" conclusion rests on that; verify it, and if the flag is conditional after all, the note must say what the condition is.
    - `.claude/skills/c64-ram-capture/scripts/project-paths.mjs` lines 1-45 — the real resolution order: `C64RE_PROJECT_ROOT` is checked FIRST (:28), and only then does it walk ancestors for a `.git` entry (:32); the throw at :37-38 names both. Note that the todo's own wording puts these the other way round — document the real order, not the todo's.
    - `.planning/phases/08-capability-honesty-and-the-install-story/08.1-WALKTHROUGH-EVIDENCE.md` FINDING-C1 and `08.1-WALKTHROUGH-SETUP.md` FINDING-B2 — the original findings, for the historical framing.
    - `scripts/check-skill-fork-honesty.mjs` and `scripts/check-skill-tool-coverage.mjs` — both lint skill prose. Run both after editing; new prose naming an `mcp__*` tool must satisfy them.
  </read_first>
  <action>
Drive-type note: add one sentence to `## Boot a disk`, placed after the numbered step that
confirms the program counter moved and before the keyboard-typed fallback paragraph, stating
that the broker configures the drive type at launch for every stock instance so no drive
setup is needed before attaching. Keep it to one sentence in the section's existing voice —
the todo is explicit that this is a closing note, not an active workaround, because the
Phase 8.2 launch-time fix covers both the disk route and the bare-program route identically.
Do not add a new heading and do not add a `README.md` changelog entry; a user hits this while
following the disk-boot procedure, which is where the sentence goes.

Project-root prerequisite: add a short prerequisite line immediately before `## The order`
— the file has no Setup section and this is the first thing a scratch project trips over.
State the real resolution order: the toolkit uses `C64RE_PROJECT_ROOT` when set, and
otherwise the nearest ancestor directory containing a `.git` entry; so a throwaway project
needs `git init` first, or that variable set. Mention that the error message names both, so a
user who hits it can self-serve. Also add a matching row to the `## Troubleshooting` table
in its existing two-column backtick-quoted-symptom shape, keying off the actual throw text
from `project-paths.mjs` — read the real message and quote its distinctive fragment, do not
paraphrase it, because the table's value is that a user can match what they saw.

Then close both todos: `git mv` each to `.planning/todos/completed/` and append a
`## Resolution` naming the date (2026-08-22), Phase 15 plan 15-06, the exact file and heading
the behaviour is now documented at, and — for the drive-type one — the verification that
`buildViceArgs()`'s stock branch sets the flag unconditionally, with the real current line.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-skill-tool-coverage.mjs && node scripts/check-skill-fork-honesty.mjs && node scripts/check-npm-packages.mjs && cd .claude/mcp/vice && node --test docs-deferred-ledger.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'C64RE_PROJECT_ROOT' .claude/skills/c64-ram-capture/SKILL.md` returns at least 2 (the prerequisite line and the Troubleshooting row).
    - `grep -c 'git init' .claude/skills/c64-ram-capture/SKILL.md` returns at least 1.
    - `grep -c 'drive' .claude/skills/c64-ram-capture/SKILL.md` returns at least 1, and the sentence appears inside the `## Boot a disk` section: `awk '/^## Boot a disk/,/^## Capture at a trigger address/' .claude/skills/c64-ram-capture/SKILL.md | grep -ci 'drive'` returns at least 1.
    - The prerequisite line precedes the first procedure heading: the line number of the `C64RE_PROJECT_ROOT` mention nearest the top is less than the line number of `## The order`.
    - The Troubleshooting row quotes the real throw fragment: the quoted fragment also appears in `.claude/skills/c64-ram-capture/scripts/project-paths.mjs` (`grep -q` the same fragment in both files).
    - Both todos are renamed, not deleted: `test -f` each `completed/` path succeeds, each `pending/` path is gone, each has exactly one `## Resolution`, and `git status --porcelain` shows renames.
    - `node scripts/check-skill-tool-coverage.mjs`, `node scripts/check-skill-fork-honesty.mjs` and `node scripts/check-npm-packages.mjs` all exit 0.
    - `git status --porcelain installer/skills/` is empty.
    - STATE.md's `## Deferred Items` table and both prose count figures were updated in THIS plan's commits for all three todos moved out of `pending/`, and `cd .claude/mcp/vice && node --test docs-deferred-ledger.test.ts` exits 0 in both directions.
  </acceptance_criteria>
  <done>Both behaviours are documented at the point of use in the skill's own voice, each claim is verified against the module that implements it, and both todos are closed with file-and-heading citations.</done>
</task>

<task type="auto">
  <name>Task 2: Document RELEASES.json's shape and ship a copyable example (DEBT-02 item 3)</name>
  <files>.claude/skills/c64-ram-capture/SKILL.md, .claude/skills/c64-ram-capture/RELEASES.json.example, .planning/todos/pending/2026-08-19-releases-json-schema-undocumented.md</files>
  <read_first>
    - `.claude/skills/c64-ram-capture/scripts/releases.mjs` — read the whole file. The authoritative field list: top-level `schema_version`, `schema_notes` (documented at :46-56), `releases`; per entry `id`, `canonical` (a boolean, deliberately demoted from "the canonical image" per the header at :5-6), `disk_image`, `dumps`. The `list` command at :98 reads `r.dumps.length` with no nullish guard — that is why `dumps` is required rather than optional. Record the real current line numbers.
    - `.claude/skills/c64-ram-capture/scripts/project-paths.mjs` — `registryFile()` and `releaseDataDir()`, plus the `C64RE_DATA_DIR` / `C64RE_REGISTRY` overrides that decide where `RELEASES.json` actually lives. The new section must say where the file goes, not only what is in it.
    - Any real `RELEASES.json` in this repo or in a consuming project, if one exists (`find . -name 'RELEASES.json' -not -path '*/node_modules/*'`) — use its real shape as the example's basis rather than inventing one; if none exists, build the example strictly from `releases.mjs`'s readers and say so in the SUMMARY.
    - `.claude/skills/c64-ram-capture/SKILL.md` `## Which skill does what` :276-288 and `## References` :290-300 — the two table shapes to match, and the two candidate insertion points `15-PATTERNS.md` names.
    - `scripts/check-npm-packages.mjs` — it validates the published tarballs' exact file lists. A new file in a skill directory changes those lists; confirm it stays green, and if it fails because the example is not matched by the package `files[]` pattern, fix the pattern rather than dropping the example.
  </read_first>
  <action>
Add one new `## ` section to `SKILL.md` — the file's convention is one heading per concern
with no `references/` split — placed immediately before `## References`. Document, as a
two-column field table matching the file's existing table style: each top-level key and each
per-release field, what it is for, and whether it is required. Make the `dumps` requirement
explicit and say why (the registry accessor reads its length with no guard, so an omitted
`dumps` throws rather than defaulting). Say where the file lives and name the two
environment overrides that relocate it.

Ship `RELEASES.json.example` in the skill's own directory: one release entry with every
documented field populated with obviously-placeholder values, plus a real `schema_notes`
string. Reference it from the new section by relative path, in the `## References` table's
`| Path | Covers |` shape if that reads better than an inline mention.

Verify every field name against `releases.mjs`'s readers by grep before writing it down. A
field documented here that no reader consumes is the same defect in the other direction —
record in the SUMMARY the grep you used per field.

Then `git mv` the todo to `completed/` and append a `## Resolution` naming the date, the
plan, the new section heading, the example file's path, and the `releases.mjs` line that
establishes the `dumps` requirement.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node --input-type=module -e 'import {readFileSync} from "node:fs"; JSON.parse(readFileSync(".claude/skills/c64-ram-capture/RELEASES.json.example","utf8"))' && node scripts/check-skill-tool-coverage.mjs && node scripts/check-skill-fork-honesty.mjs && node scripts/check-npm-packages.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The example parses: the `node --input-type=module` JSON.parse command above exits 0.
    - Every documented field is real: `for f in schema_version schema_notes releases id canonical disk_image dumps; do grep -q "$f" .claude/skills/c64-ram-capture/scripts/releases.mjs || echo "NOT A REAL FIELD: $f"; done` prints nothing.
    - Every real field is documented: each of the seven names above appears in `.claude/skills/c64-ram-capture/SKILL.md`.
    - The new section is a top-level heading and sits before `## References`: `grep -n '^## ' .claude/skills/c64-ram-capture/SKILL.md` shows the new heading's line number is lower than `## References`'s.
    - The relocation overrides are named: `grep -c 'C64RE_DATA_DIR' .claude/skills/c64-ram-capture/SKILL.md` and `grep -c 'C64RE_REGISTRY' .claude/skills/c64-ram-capture/SKILL.md` each return at least 1.
    - The `dumps`-is-required rationale is present: `awk '/^## /{s=$0} /dumps/{print s}' .claude/skills/c64-ram-capture/SKILL.md` names the new section, and the surrounding text mentions that the length is read without a guard.
    - `node scripts/check-npm-packages.mjs` exits 0 with the new file present in both tarball listings — if it fails on the example not matching a package `files[]` pattern, the pattern is widened and the SUMMARY records that.
    - `node scripts/check-skill-tool-coverage.mjs` and `node scripts/check-skill-fork-honesty.mjs` both exit 0.
    - The todo is renamed, not deleted, with exactly one `## Resolution` citing the new heading and the example path.
    - `git status --porcelain installer/skills/` is empty.
  </acceptance_criteria>
  <done>`RELEASES.json`'s shape is documented in the skill against a field-by-field grep of its only reader, a parseable example ships alongside it, and both published tarballs still validate.</done>
</task>

<task type="auto">
  <name>Task 3: Re-read all three additions as a first-time user and record the placement verification</name>
  <files>.claude/skills/c64-ram-capture/SKILL.md</files>
  <read_first>
    - `.claude/skills/c64-ram-capture/SKILL.md` — read the whole file top to bottom after Tasks 1 and 2, as a user who has never seen it. This is the only way to check what DEBT-02 actually asks for: not that the sentences exist, but that they are where a user would look.
    - `.planning/phases/15-debt-and-review-disposition/15-VALIDATION.md` § Manual-Only Verifications — the first row is exactly this check, and it is recorded as manual because prose placement is a judgment call with no automated assertion worth writing.
    - `.planning/phases/08-capability-honesty-and-the-install-story/08-HUMAN-UAT.md` — the shape for recording a manual verification result with cited evidence rather than a bare pass.
  </read_first>
  <action>
Walk the file as three user journeys and record, for each, the heading the user lands on and
whether the new prose is above the point where they would have failed:

1. A user with a scratch directory who wants to capture RAM — do they read the project-root
   prerequisite before the first command that would throw?
2. A user booting a `.d64` who has heard about drive types — do they find the closing note
   inside the procedure they are following, rather than in a changelog?
3. A user adding a second release — do they find the registry's field table before they hand-write
   a `RELEASES.json` with `dumps` omitted?

If any answer is no, move the prose and re-check; do not record a pass against a placement
that failed its own journey. Record all three journeys in the SUMMARY with the heading names
and line numbers, so the DEBT-02 criterion's "cited by file" is satisfied concretely.

Also confirm the three additions do not contradict anything already in the file — in
particular that no existing Troubleshooting row or procedure step now says something the new
prose supersedes. If one does, update it in the same commit and say which.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-skill-tool-coverage.mjs && node scripts/check-skill-fork-honesty.mjs && node scripts/check-npm-packages.mjs && cd .claude/mcp/vice && npm run test:automated</automated>
    <human-check>
Read the three new passages in `.claude/skills/c64-ram-capture/SKILL.md` and confirm each
reads in the file's own terse imperative voice rather than as a transplanted planning note,
and that the project-root prerequisite is short enough not to become a wall of setup prose
before the first procedure step.
    </human-check>
  </verify>
  <acceptance_criteria>
    - All three user journeys are recorded in the SUMMARY with the heading name and line number the prose sits at, and each journey's answer is yes.
    - `node scripts/check-skill-tool-coverage.mjs`, `node scripts/check-skill-fork-honesty.mjs` and `node scripts/check-npm-packages.mjs` all exit 0.
    - `cd .claude/mcp/vice && npm run test:automated` exits 0.
    - All three DEBT-02 todos are in `completed/`, each with exactly one `## Resolution`; `ls .planning/todos/pending/ | wc -l` is 3 lower than at the start of this plan.
    - `cd .claude/mcp/vice && node --test docs-deferred-ledger.test.ts` exits 0.
    - Any existing passage the new prose supersedes is named in the SUMMARY, or the SUMMARY states explicitly that none was found.
  </acceptance_criteria>
  <done>All three additions are verified in place by a first-time-reader walk, with heading and line citations, and no existing passage contradicts them.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols and files created by this plan:

- `.claude/skills/c64-ram-capture/RELEASES.json.example` — new file
- A new `## ` section in `.claude/skills/c64-ram-capture/SKILL.md` documenting the release-registry shape
- A new project-root prerequisite line before `## The order`, a new sentence in `## Boot a disk`, and a new `## Troubleshooting` table row in the same file
- `.planning/todos/completed/2026-08-19-drive-type-prerequisite-undocumented-in-readme-and-skill.md` (renamed from `pending/`)
- `.planning/todos/completed/2026-08-19-project-paths-git-marker-requirement-undocumented.md` (renamed from `pending/`)
- `.planning/todos/completed/2026-08-19-releases-json-schema-undocumented.md` (renamed from `pending/`)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill prose → user action | The playbook is executed by an agent or a human; a false instruction causes real emulator or filesystem actions |
| `RELEASES.json` on disk → `releases.mjs` | A user-authored registry file is parsed with no schema validation; a missing required field throws at read time |
| `.claude/skills/` → `installer/skills/` → published tarball | The canonical source is mirrored by a generator into two published packages; a hand edit to the mirror creates a silent second copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-20 | Repudiation | new skill prose asserting behaviour the code does not have | medium | mitigate | Every added claim is grep-verified against the implementing module (`buildViceArgs()`, `project-paths.mjs`, `releases.mjs`) and the verifying command is recorded per claim; the field table is checked in both directions |
| T-15-21 | Information disclosure | `RELEASES.json.example` shipping real release identifiers or local paths | low | mitigate | Every value in the example is an obvious placeholder; no host path, no local directory, no real disk-image name |
| T-15-22 | Tampering | hand-editing the generated `installer/skills/` mirror | low | mitigate | Prohibited and asserted: `git status --porcelain installer/skills/` must be empty; the mirror is regenerated by `installer/scripts/sync-skills.mjs` |
| T-15-SC | Tampering | npm/pip/cargo installs | — | accept | Not applicable — this plan installs no packages and adds no dependency |
</threat_model>

<verification>
- `node scripts/check-skill-tool-coverage.mjs`, `node scripts/check-skill-fork-honesty.mjs`, `node scripts/check-npm-packages.mjs` all exit 0
- `cd .claude/mcp/vice && npm run test:automated && node --test docs-deferred-ledger.test.ts` exits 0
- `RELEASES.json.example` parses as JSON
- Every documented registry field is consumed by `releases.mjs`, and every field `releases.mjs` consumes is documented
- `git status --porcelain installer/skills/` is empty

<human-check>
Confirm the three additions land where a user would look, not merely somewhere in the file:
the project-root prerequisite above the first procedure, the drive-type note inside the
disk-boot procedure, and the registry field table before `## References`.
</human-check>
</verification>

<success_criteria>
- Three of DEBT-02's five behaviours are documented at the point of use, each citation resolving to a real heading
- Every claim is verified against the module that implements it
- Three pending todos closed with file-and-heading citations
- Both published tarballs still validate with the new example file present
</success_criteria>

<output>
Create `.planning/phases/15-debt-and-review-disposition/15-06-SUMMARY.md` when done.
Record: the heading and line number of each addition, the per-claim verification command and
output, the per-field grep results for the registry table (both directions), the three user
journeys from Task 3, and `check-npm-packages.mjs`'s output after the new file was added.
</output>
