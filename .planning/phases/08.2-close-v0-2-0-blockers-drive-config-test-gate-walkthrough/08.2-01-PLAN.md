---
phase: 08.2-close-v0-2-0-blockers-drive-config-test-gate-walkthrough
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-d-checklist.sh
autonomous: true
requirements: [I-3]
audit_items: [I-3]

must_haves:
  truths:
    - "`cd .claude/mcp/vice && node test-gate.mjs` reports 0 failures"
    - "`cd .claude/mcp/vice && node --test host-scripts.test.ts` exits 0"
    - "The repo-wide tracked shell-script set is exactly the three permanent scripts the allowlist names"
    - "No evidence Phase 8.1 recorded is lost by the removal — its D-1..D-7 output survives in committed Markdown"
  artifacts:
    - path: ".claude/mcp/vice/host-scripts.test.ts"
      provides: "EXPECTED_TRACKED_SHELL_SCRIPTS allowlist — unchanged by this plan, green after it"
      contains: "EXPECTED_TRACKED_SHELL_SCRIPTS"
  key_links:
    - from: "git ls-files -- '*.sh'"
      to: ".claude/mcp/vice/host-scripts.test.ts:188"
      via: "execFileP('git', ['ls-files', '--', '*.sh'])"
      pattern: "ls-files"
---

<objective>
Close audit item **I-3**: the automated test gate is red and would fail CI on the
tagging push. Phase 8.1 committed a throwaway D-1..D-7 checklist harness
(`08.1-d-checklist.sh`, commit `19211e1`) into `.planning/phases/08.1-.../`. That
file trips `host-scripts.test.ts`'s **repo-wide** `EXPECTED_TRACKED_SHELL_SCRIPTS`
allowlist, which is driven by `git ls-files -- '*.sh'` and is not scoped to source
directories. `node test-gate.mjs` currently reports 1650 pass / **1 fail** / 5 todo;
CI runs bare `npm test`, a superset, so the failure materialises on the first push —
which is the tagging push. HEAD is ~331 commits ahead of `origin/main`, which is the
only reason nobody has seen it go red yet.

This plan takes the untrack route recommended by both the audit (§4.3, "untrack the
throwaway script") and `08.2-RESEARCH.md` (Assumptions Log A2): outright `git rm` of
the script, leaving the allowlist array untouched at its three permanent entries.
The script's own commit message calls it throwaway, and its recorded output is
already captured verbatim in committed Markdown (`08.1-d-checklist-baseline.txt`,
`08.1-CONSISTENCY-READ.md`, `08.1-VERIFICATION.md`), so nothing is retracted by
removing the executable.

Purpose: item 2 of the ROADMAP's four ordered items. It is independent of items 1
and 3 and must land **before any push**.
Output: a green `node test-gate.mjs`, a green `node --test host-scripts.test.ts`,
and a tracked-shell-script set back at exactly three paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.2-close-v0-2-0-blockers-drive-config-test-gate-walkthrough/08.2-PATTERNS.md
@.planning/phases/08.2-close-v0-2-0-blockers-drive-config-test-gate-walkthrough/08.2-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm nothing still depends on the checklist script, then untrack it with a full `git rm`</name>
  <files>.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-d-checklist.sh</files>
  <read_first>
    - `.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-d-checklist.sh` — the file being removed; read it in full so the removal is an informed decision, not a blind delete
    - `.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-d-checklist-baseline.txt` — the committed RED baseline that must survive the removal
    - `.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-CONSISTENCY-READ.md` — the committed prose record of the checklist's final green run
    - `.claude/mcp/vice/host-scripts.test.ts` lines 155-205 — `EXPECTED_TRACKED_SHELL_SCRIPTS` and the `git ls-files` assertion whose message names both legitimate routes
    - `.planning/phases/08.2-close-v0-2-0-blockers-drive-config-test-gate-walkthrough/08.2-RESEARCH.md` § "Pitfall 4" — the untrack-versus-delete distinction and the specific trap of a half-untrack
    - `.planning/v0.2.0-MILESTONE-AUDIT.md` §4.3 — the reproduced failure and its three reasons for being a blocker
  </read_first>
  <action>
    First establish that no committed artifact needs the script to remain executable
    on disk. Run `grep -rn '08.1-d-checklist.sh' --include='*.md' --include='*.ts'
    --include='*.mjs' --include='*.yml' --include='*.json' . ` (excluding
    `node_modules`) and record every hit in the SUMMARY. Every hit is expected to be
    a *narrative* reference (a phase SUMMARY, VERIFICATION, CONSISTENCY-READ or plan
    describing what the script did) rather than an *execution* reference (a CI step,
    an npm script, a test that shells out to it). If any hit is an execution
    reference, stop and switch to the ROADMAP's other licensed route instead: add
    `.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-d-checklist.sh`
    to `EXPECTED_TRACKED_SHELL_SCRIPTS` in `.claude/mcp/vice/host-scripts.test.ts`
    and keep the array `.sort()`ed. Record which route was taken and why.

    Assuming (as expected) all hits are narrative: remove the file with `git rm
    .planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-d-checklist.sh`
    — a full removal, **not** `git rm --cached`. A `--cached` half-untrack leaves the
    file on disk as an untracked stray (the file is confirmed NOT gitignored, per
    `08.2-RESEARCH.md`'s live `git check-ignore` check), which is exactly the
    half-state Pitfall 4 warns against. Do **not** edit
    `.claude/mcp/vice/host-scripts.test.ts` on this route — the array is already
    correct at its three permanent entries
    (`.claude/mcp/vice/resources/vice-launcher.sh`, `scripts/ensure-mcp-deps.sh`,
    `scripts/package.sh`) and goes green the moment the drifting path leaves
    `git ls-files`.

    Do not delete, move or rewrite `08.1-d-checklist-baseline.txt` or any 08.1
    Markdown — the evidence stays exactly where it is. Do not touch any other
    `.planning/phases/08.1-.../` file.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && node --test host-scripts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files -- '*.sh'` prints exactly these three paths and nothing else: `.claude/mcp/vice/resources/vice-launcher.sh`, `scripts/ensure-mcp-deps.sh`, `scripts/package.sh`
    - `git ls-files -- '*.sh' | wc -l` outputs `3`
    - `test -e .planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-d-checklist.sh` exits non-zero (the file is gone from the working tree, not merely from the index)
    - `git status --porcelain -- '.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/'` shows no `??` (untracked) entry for a `.sh` path
    - `cd .claude/mcp/vice && node --test host-scripts.test.ts` exits 0 with 0 failures
    - `git diff --cached --name-only .claude/mcp/vice/host-scripts.test.ts` is empty on the untrack route (the allowlist array was NOT edited)
    - `test -e .planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-d-checklist-baseline.txt` exits 0 (Phase 8.1's recorded evidence survives)
    - The SUMMARY records the full `grep -rn '08.1-d-checklist.sh'` hit list and classifies each hit as narrative or execution
  </acceptance_criteria>
  <done>The tracked shell-script set is exactly three paths, `host-scripts.test.ts` passes, the removed script's evidence is still committed, and the route taken (untrack, or allowlist-extend if an execution reference was found) is recorded with its reason.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the whole automated gate is green, and that CI's superset command is green too</name>
  <files>.planning/phases/08.2-close-v0-2-0-blockers-drive-config-test-gate-walkthrough/08.2-01-SUMMARY.md</files>
  <read_first>
    - `.claude/mcp/vice/test-gate.mjs` lines 1-100 — the `MANUAL_ONLY_TESTS` array and the `automatedTestFiles()` narrowing, so the difference between `node test-gate.mjs` and bare `npm test` is understood before it is asserted
    - `.claude/mcp/vice/package.json` lines 88-98 — the `test` / `test:automated` / `test:manual` script definitions (`test` is `node --test '*.test.*'`, the CI superset)
    - `.github/workflows/ci.yml` around line 75 — the step that runs bare `npm test`, i.e. the command that would have gone red on the tagging push
    - `.planning/phases/08.2-close-v0-2-0-blockers-drive-config-test-gate-walkthrough/08.2-VALIDATION.md` § Sampling Rate — the per-wave gate this task is executing
  </read_first>
  <action>
    Run the narrowed gate: `cd .claude/mcp/vice && node test-gate.mjs`. Record the
    literal pass/fail/skip/todo tallies in the SUMMARY, alongside the pre-fix
    baseline of `1650 pass / 1 fail / 5 todo` that `08.2-RESEARCH.md` reproduced live,
    so the delta is on the record rather than asserted in prose.

    Then run CI's actual command, the superset: `cd .claude/mcp/vice && npm test`.
    This globs every `*.test.*` including the six `MANUAL_ONLY_TESTS` files; those
    default-skip via their own `{ skip: SKIP_REASON }` options when their opt-in env
    vars are unset, so a green result here is the real proof that the tagging push
    will not produce a red CI run. Record its tallies too. If `npm test` reports any
    failure that `test-gate.mjs` did not, record it in the SUMMARY as a finding —
    the audit's own §4.3 lesson is that "touched no source" is not the same as "the
    suite is green," and the mirror-image lesson applies here: "the narrowed gate is
    green" is not the same as "CI is green."

    Do not modify any test file, any allowlist, or `test-gate.mjs` in this task.
    This is a measurement task; if it finds a second red test, that is a finding to
    report to the orchestrator, not a licence to widen this plan's scope.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && node test-gate.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd .claude/mcp/vice && node test-gate.mjs` exits 0 and its output line reports `0` in the fail position
    - `cd .claude/mcp/vice && npm test` exits 0
    - The SUMMARY records both commands' literal pass / fail / skip / todo tallies, and the pre-fix `1650 pass / 1 fail / 5 todo` baseline for comparison
    - The SUMMARY states explicitly that CI's command is bare `npm test` (`.github/workflows/ci.yml`) and that it was run, not merely inferred from the narrowed gate
    - No file under `.claude/mcp/vice/` appears in `git diff --name-only HEAD` for this task
  </acceptance_criteria>
  <done>Both the narrowed gate and CI's bare `npm test` superset run green from a clean tree, with tallies recorded, and no further red test was discovered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git index ↔ working tree | A tracked-file removal changes what CI and downstream consumers see; a half-untrack leaves the two views disagreeing |
| repo ↔ CI | `git ls-files` is the input to a structural guardrail; anything that changes the tracked set changes CI's verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.2-03 | Tampering | `git rm` of `08.1-d-checklist.sh` | mitigate | Task 1 greps every `.md`/`.ts`/`.mjs`/`.yml`/`.json` reference first and classifies each as narrative or execution; an execution reference forces the allowlist-extend route instead. Phase 8.1's recorded output stays committed in `08.1-d-checklist-baseline.txt` and `08.1-CONSISTENCY-READ.md`, so no evidence is retracted |
| T-08.2-04 | Repudiation | half-untrack (`git rm --cached`) leaving a stray untracked `.sh` | mitigate | Task 1 mandates a full `git rm`; acceptance criteria assert both that the file is absent from the working tree and that `git status --porcelain` shows no `??` `.sh` entry in that directory |
| T-08.2-05 | Denial of Service | a green narrowed gate masking a red CI superset | mitigate | Task 2 runs CI's literal bare `npm test` in addition to `node test-gate.mjs`, and records both tallies |
| T-08.2-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no package and adds no dependency; `node:test` and `git` are already present. No package-legitimacy checkpoint is required |
</threat_model>

<verification>
- `git ls-files -- '*.sh'` returns exactly 3 paths
- `cd .claude/mcp/vice && node --test host-scripts.test.ts` exits 0
- `cd .claude/mcp/vice && node test-gate.mjs` reports 0 failures
- `cd .claude/mcp/vice && npm test` exits 0
- `cd .claude/mcp/vice && npx tsc --noEmit -p tsconfig.json` exits 0 (unchanged by this plan; run as a regression check)
</verification>

<success_criteria>
The red test gate that would have failed CI on the v0.2.0 tagging push is green, by
removing a throwaway phase-scoped shell script from git tracking rather than by
widening a permanent-scripts allowlist to accommodate it. Both the narrowed gate and
CI's own bare `npm test` are demonstrated green from a clean tree, and Phase 8.1's
recorded checklist evidence is untouched.
</success_criteria>

<notes>
- **Worktree isolation:** safe for this plan. It touches one file under
  `.planning/phases/08.1-.../` and no `.planning/STATE.md` or `.planning/ROADMAP.md`.
  The change is a plain `git rm` that merges normally. If the orchestrator's
  wave-close cleanup would restore removed `.planning/` files, verify after merge
  that `git ls-files -- '*.sh' | wc -l` is still `3` in the main checkout.
- **This plan must land before any push**, per the ROADMAP's ordering constraint. It
  is independent of items 1 and 3 and can run in parallel with plan 02.
- If Task 1's grep finds an execution reference and the allowlist-extend route is
  taken instead, `files_modified` becomes
  `.claude/mcp/vice/host-scripts.test.ts` and the `.sh` file stays tracked — record
  that deviation prominently in the SUMMARY, since plan 05's E-1..E-5 work and any
  future audit round will read the tracked-script count.
</notes>

<output>
Create `.planning/phases/08.2-close-v0-2-0-blockers-drive-config-test-gate-walkthrough/08.2-01-SUMMARY.md` when done.
</output>
