---
phase: 08.2-close-v0-2-0-blockers-drive-config-test-gate-walkthrough
plan: 01
subsystem: testing
tags: [git, test-gate, node-test, ci, allowlist]

# Dependency graph
requires:
  - phase: 08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift
    provides: "08.1-d-checklist.sh (throwaway D-1..D-7 harness) and its committed baseline/evidence"
provides:
  - "A green `node test-gate.mjs` and green `host-scripts.test.ts` (I-3 closed)"
  - "Repo-wide tracked shell-script set restored to exactly 3 permanent entries"
  - "Confirmation that CI's bare `npm test` command was actually run, not merely inferred"
affects: [08.2-02, 08.2-03, 08.2-04, 08.2-05, 08.2-06]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created:
    - .planning/phases/08.2-close-v0-2-0-blockers-drive-config-test-gate-walkthrough/deferred-items.md
  modified: []

key-decisions:
  - "Took the untrack route (git rm, full removal) over the allowlist-extend route: grep across .md/.ts/.mjs/.yml/.json confirmed every reference to 08.1-d-checklist.sh is narrative (SUMMARY/PLAN/AUDIT/ROADMAP prose), never an execution reference, so nothing on disk depends on the script remaining tracked or present"
  - "Did not modify host-scripts.test.ts's EXPECTED_TRACKED_SHELL_SCRIPTS array -- it already names exactly the three permanent scripts and goes green the moment the drifting path leaves git ls-files"
  - "Provisioned .claude/mcp/vice/node_modules via npm ci (the project's own documented ensure-mcp-deps.sh mechanism) before measuring the gate -- this worktree's node_modules was empty, which would have produced a false mass-failure reading unrelated to I-3"
  - "Logged a second, pre-existing, worktree-execution-specific test failure (repo-root.test.ts's 'not under .claude' assertion) to deferred-items.md rather than fixing it -- confirmed by direct repoRoot() inspection that it is caused by this worktree's own checkout path under .claude/worktrees/, not by this plan's change, and it will not reproduce in CI or the merged main checkout"

patterns-established: []

requirements-completed: [I-3]

# Metrics
duration: 13min
completed: 2026-08-19
---

# Phase 08.2 Plan 01: Untrack Phase 8.1's throwaway checklist script Summary

**Restored a green automated test gate by `git rm`-ing the throwaway `08.1-d-checklist.sh` that tripped `host-scripts.test.ts`'s repo-wide shell-script allowlist, closing audit item I-3.**

## Performance

- **Duration:** 13 min (plus SUMMARY authoring)
- **Started:** 2026-08-19T15:24:01+02:00
- **Completed:** 2026-08-19T15:36:41+02:00
- **Tasks:** 2
- **Files modified:** 1 deleted (`08.1-d-checklist.sh`), 1 created (`deferred-items.md`)

## Accomplishments
- `host-scripts.test.ts`'s `EXPECTED_TRACKED_SHELL_SCRIPTS` structural gate is green: `git ls-files -- '*.sh'` returns exactly the three permanent entries (`.claude/mcp/vice/resources/vice-launcher.sh`, `scripts/ensure-mcp-deps.sh`, `scripts/package.sh`)
- Confirmed, by full-repo grep, that no execution surface (CI workflow, npm script, or test) ever shelled out to `08.1-d-checklist.sh` — every one of the ~60 hits is narrative prose in committed Markdown, so removing the file from git tracking retracts no evidence
- Ran both the narrowed gate (`node test-gate.mjs`) and CI's actual bare `npm test` superset (`.github/workflows/ci.yml:75`) against a clean tree with dependencies freshly provisioned, and recorded literal tallies for both

## Task Commits

Each task was committed atomically:

1. **Task 1: Confirm nothing depends on the checklist script, then untrack it with a full `git rm`** - `869497b` (fix)
2. **Task 2: Prove the whole automated gate is green, and that CI's superset command is green too** - `e38782b` (docs — measurement task, no source change; committed the resulting `deferred-items.md` finding)

_No plan-metadata-only commit is listed separately; this SUMMARY commit (below) serves that role per worktree-mode convention._

## Files Created/Modified
- `.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-d-checklist.sh` - **deleted** (full `git rm`, not `--cached`); a throwaway D-1..D-7 harness whose recorded output already lives verbatim in committed Markdown (`08.1-d-checklist-baseline.txt`, `08.1-CONSISTENCY-READ.md`, `08.1-VERIFICATION.md`), none of which were touched
- `.planning/phases/08.2-close-v0-2-0-blockers-drive-config-test-gate-walkthrough/deferred-items.md` - new; records D-1, an out-of-scope, worktree-execution-specific test-gate false-failure discovered while measuring Task 2

## Grep hit classification (Task 1 acceptance criterion)

Ran `grep -rn '08.1-d-checklist.sh' --include='*.md' --include='*.ts' --include='*.mjs' --include='*.yml' --include='*.json' .` (excluding `node_modules`). Every hit falls into exactly one bucket:

| File(s) | Hit count | Classification |
|---|---|---|
| `.planning/v0.2.0-MILESTONE-AUDIT.md` | 4 | Narrative — audit prose describing/reproducing the defect |
| `.planning/ROADMAP.md` | 2 | Narrative — Phase 8.2 scope notes naming the file to untrack |
| `.planning/phases/08.2-.../08.2-01-PLAN.md` | 9 | Narrative — this plan's own text, describing the removal it commissions |
| `.planning/phases/08.2-.../08.2-PATTERNS.md` | 8 | Narrative — pattern-mapper's recorded precedent/analog citations |
| `.planning/phases/08.2-.../08.2-RESEARCH.md` | 7 | Narrative — research findings and recommendation |
| `.planning/phases/08.1-.../08.1-01-SUMMARY.md`, `08.1-01-PLAN.md`, `08.1-02-PLAN.md`, `08.1-02-SUMMARY.md`, `08.1-05-PLAN.md`, `08.1-05-SUMMARY.md`, `08.1-VERIFICATION.md`, `08.1-CONSISTENCY-READ.md` | ~35 | Narrative — Phase 8.1's own SUMMARY/PLAN/VERIFICATION prose recording what the script did and its results, historical record only |

**Zero hits** in any `.ts`, `.mjs`, `.yml`, or `.json` file anywhere in the repo (confirmed by the same grep's include filters) — no execution reference exists. This licenses the untrack route per the plan's own Task 1 branch logic; the allowlist-extend fallback route was not needed.

## Test gate tallies (Task 2)

| Run | Pre-fix baseline (08.2-RESEARCH.md, reproduced live before Task 1) | Post-fix (this session, after `npm ci`) |
|---|---|---|
| `node test-gate.mjs` | 1650 pass / **1 fail** (`host-scripts.test.ts`) / 5 todo | 1650 pass / **1 fail** (different test — see Finding below) / 5 todo |
| `npm test` (CI's literal command, `.github/workflows/ci.yml:75`) | not run pre-fix in this session | 1787 pass / **1 fail** (same test as above) / 27 skipped / 5 todo |

`host-scripts.test.ts`'s allowlist assertion (`EXPECTED_TRACKED_SHELL_SCRIPTS`) is confirmed green on its own: `cd .claude/mcp/vice && node --test host-scripts.test.ts` → **4/4 pass, 0 fail**. I-3's target failure is closed.

### Finding: a second, unrelated, pre-existing failure surfaced (recorded, not fixed)

Immediately after Task 1's fix, `node test-gate.mjs` still reported "1 fail" — but a different test: `repo-root.test.ts`'s "path agreement ... the agreed path is not under .claude" assertion (test #404 in the narrowed gate, #416 in bare `npm test`). Investigated in full and confirmed **not caused by this plan's change**:

- `repoRoot()` walks up from the caller's location to the nearest `.git` entry. This agent's own worktree checkout lives at `<repo>/.claude/worktrees/agent-a7b71e4317f8cb341/`, whose own `.git` file is the nearest ancestor — so `repoRoot()` correctly (per its documented resolution ladder) returns a path containing the literal substring `.claude`, tripping the test's `!nodeVals.supervisorDir.includes(".claude")` assertion.
- Confirmed directly: `node --input-type=module -e "import { repoRoot } from './repo-root.ts'; console.log(repoRoot());"` printed `/home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a7b71e4317f8cb341`.
- This is provably an artifact of the parallel-executor worktree's own checkout location, not a regression: neither GitHub Actions' fresh checkout (`.github/workflows/ci.yml`) nor the main repo checkout (`/home/henrik/dev/henrik/git/c64-re-tools`, no `.claude` substring) will exhibit it.
- **Not fixed** per this task's explicit instruction ("do not modify any test file... in this task. This is a measurement task") and the general SCOPE BOUNDARY rule (out-of-scope failures in unrelated files). Logged as **D-1** in `deferred-items.md` with full root-cause evidence and a recommendation that the orchestrator re-run both gate commands from the merged main checkout to get the true 0-fail confirmation.

**Also confirmed as an environment-provisioning gap, auto-fixed (Rule 3):** this worktree's `.claude/mcp/vice/node_modules` was present but essentially empty (only a stale `.cache` directory) when Task 2 began — the first `node test-gate.mjs` run before provisioning reported 29 failures across broker/build/telemetry tests, none related to I-3. Ran `npm ci --no-audit --no-fund` (the project's own documented `ensure-mcp-deps.sh` mechanism, against the committed lockfile — no new package added or substituted) to restore the exact locked dependency set, then re-ran the gate and got the tallies above. This is standard environment provisioning, not a package-legitimacy concern (no install target changed from what the committed `package-lock.json` already specifies).

`npx tsc --noEmit -p tsconfig.json` (regression check per the plan's `<verification>` section): **0 errors.**

## Decisions Made
See `key-decisions` in frontmatter above. In short: untrack (not allowlist-extend) route taken per the grep evidence; no code change to `host-scripts.test.ts`; a stale/empty `node_modules` was reprovisioned via the project's own `npm ci` convention before measuring; a second unrelated worktree-artifact failure was documented, not fixed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Provisioned empty `.claude/mcp/vice/node_modules` via `npm ci`**
- **Found during:** Task 2 (measuring the automated gate)
- **Issue:** This worktree's `node_modules` directory existed but held no installed packages (only a stale `.cache` dir), which produced 29 unrelated test failures (broker/build/resources-sync/telemetry tests) that would have made the tallies unreadable against the pre-fix baseline.
- **Fix:** Ran `npm ci --no-audit --no-fund` in `.claude/mcp/vice`, installing exactly the packages the committed `package-lock.json` already specifies — the same command `scripts/ensure-mcp-deps.sh` runs automatically on a normal session start. No package name was changed, added, or substituted, so this is standard environment provisioning, not a new/unvetted install.
- **Verification:** Re-ran `node test-gate.mjs` after provisioning; tallies dropped from 1622 pass/29 fail to 1650 pass/1 fail, matching the pre-fix baseline's pass count exactly (modulo the one target failure this plan exists to fix).
- **Files modified:** none tracked (`node_modules/` is gitignored)
- **Committed in:** n/a (no tracked files changed by this action)

---

**Total deviations:** 1 auto-fixed (1 blocking — Rule 3, environment provisioning)
**Impact on plan:** Necessary to get an accurate measurement; no scope creep, no source or test file touched.

## Issues Encountered
A second, pre-existing, unrelated test failure (`repo-root.test.ts`'s "not under .claude" assertion) surfaced while measuring the gate — investigated fully, confirmed to be a worktree-execution artifact rather than a regression from this plan's change, and logged as a finding (D-1 in `deferred-items.md`) rather than fixed, per Task 2's explicit "measurement task, not a licence to widen scope" instruction. See "Finding" above for full detail.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- I-3 is closed: the tracked shell-script set is back at exactly 3 paths, `host-scripts.test.ts` passes 4/4, and Phase 8.1's recorded checklist evidence (`08.1-d-checklist-baseline.txt`, `08.1-CONSISTENCY-READ.md`, `08.1-VERIFICATION.md`) is untouched.
- **Recommend the orchestrator (or `/gsd-verify-work`) re-run `cd .claude/mcp/vice && node test-gate.mjs` and `npm test` from the merged main checkout** (not from within any worktree) to get the true 0-failure confirmation — this worktree's own nested checkout path under `.claude/worktrees/` causes the one remaining, unrelated failure documented above (D-1), which is not expected to reproduce there.
- This plan is independent of items 1 (drive-config defect) and 3 (walkthrough re-run) per the ROADMAP's own ordering note, and can be merged as soon as its wave completes — it does not block or depend on plan 02's work.

---
*Phase: 08.2-close-v0-2-0-blockers-drive-config-test-gate-walkthrough*
*Completed: 2026-08-19*
