---
phase: 08.2-close-v0-2-0-blockers-drive-config-test-gate-walkthrough
verified: 2026-08-19T16:49:27Z
status: gaps_found
score: 6/7 must-haves verified
overrides_applied: 0
gaps:
  - truth: "The tree's own verification/validation paperwork is truthful and internally consistent enough to tag (phase goal's second clause)"
    status: failed
    reason: >
      Two documents directly contradict the outcome this phase itself produced. (1)
      08.2-VALIDATION.md's frontmatter still reads status: draft, nyquist_compliant:
      false, wave_0_complete: false, and every row of its Per-Task Verification Map is
      still marked pending -- even though all six plans are complete, committed, and
      the two Wave-0 test files it names (stock-broker-live.test.ts, the extended
      broker-launch.test.ts cases) exist and pass. (2) 08-VERIFICATION.md (Phase 8's
      own verification report, the document that owns DIST-03) still carries
      `status: human_needed` and its human_verification item still reads
      `resolved: "... outcome: failed"`, even though 08-HUMAN-UAT.md was updated by
      this phase to `status: passed` / `resolved_by: Phase 8.2, plan 08.2-04` and
      08.2-WALKTHROUGH-EVIDENCE.md records `capture_result: pass`. A reader who opens
      08-VERIFICATION.md today is told the milestone's finish-line requirement failed;
      it did not. This is independently confirmed by `.planning/v0.2.0-MILESTONE-AUDIT.md`
      (round 3, gaps V-2/V-3), whose findings this verifier re-derived directly rather
      than trusting.
    artifacts:
      - path: ".planning/phases/08.2-close-v0-2-0-blockers-drive-config-test-gate-walkthrough/08.2-VALIDATION.md"
        issue: "Frontmatter still says draft/false/false; Per-Task Verification Map rows still read pending, unreconciled after all six plans completed"
      - path: ".planning/phases/08-capability-honesty-and-the-install-story/08-VERIFICATION.md"
        issue: "status: human_needed and its one human_verification item's `resolved:` field still says outcome: failed, contradicting 08-HUMAN-UAT.md's passed re-run this same phase recorded"
    missing:
      - "Flip 08.2-VALIDATION.md's frontmatter to status: audited / nyquist_compliant: true / wave_0_complete: true and mark its Per-Task Verification Map rows complete, citing the actual passing test runs (broker-launch.test.ts 68/68, vice-broker-acquire.test.ts 15/15, stock-broker-live.test.ts default-skip verified, node test-gate.mjs and npm test both 0 fail)"
      - "Update 08-VERIFICATION.md's frontmatter status and its human_verification item's `resolved:` field to point at the actual passed re-run (08-HUMAN-UAT.md Test 1, resolved_by Phase 8.2 plan 08.2-04, 08.2-WALKTHROUGH-EVIDENCE.md capture_result: pass), preserving the original failed record as history the same way 08-HUMAN-UAT.md itself does"
deferred: []
human_verification: []
---

# Phase 08.2: Close v0.2.0 blockers (drive config, test gate, walkthrough) Verification Report

**Phase Goal:** A user who installs stock VICE and this plugin reaches a verified 64K
RAM capture on a broker-launched stock instance, and the tree is green and truthful
enough to tag.

**Verified:** 2026-08-19T16:49:27Z
**Status:** gaps_found (verification-paperwork gap only — no product/code defect found)
**Re-verification:** No — initial verification (no prior `08.2-VERIFICATION.md` existed)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A broker-launched stock `x64sc` boots with a 1541 on unit 8 — one fix site, `-default -drive8type 1541` ahead of `-binarymonitor`, fork branch byte-identical | ✓ VERIFIED | `broker-launch.mts:198` emits exactly `["-default","-drive8type","1541","-binarymonitor","-binarymonitoraddress",...]`; `git diff dc9ad85..HEAD -- .claude/mcp/vice/broker-launch.mts` shows the fork branch's `return ["-mcpserver",...]` line untouched (no diff hunk touches it) |
| 2 | Production stock launches never touch the operator's real `$HOME/.config/vice/vicerc` — scratch `XDG_CONFIG_HOME` reaches every real launch path via the real composition, not an injected stub | ✓ VERIFIED | `vice-broker-acquire.test.ts`'s two composition tests (`buildColdSpawnFactory` and any spawn override deliberately OMITTED) ran live: `node --test vice-broker-acquire.test.ts` → 15/15 pass, including both I-1 composition tests. `grep -c 'baseSpawn(cmd, args)'` (no third arg) in `broker-launch.mts` = 0; `grep -c 'nodeSpawn(cmd, cmdArgs, { stdio'` (hardcoded, non-merging) in `vice-broker.mts` = 0; the warm-floor `stashingSpawn`'s two-arg dropper form is also gone (0 matches) |
| 3 | A live, opt-in test drives `vice_disk_attach`/`vice_autostart` against a real `.d64` through the real broker primitive, and the `.prg`-blast-radius question is settled by measurement | ✓ VERIFIED | `stock-broker-live.test.ts` (741 lines) launches through `buildViceArgs()`/`tryLaunchOne()`/the real spawned `resources/vice-broker.mjs`; registered as the 7th `MANUAL_ONLY_TESTS` entry (`test-gate.mjs`); confirmed it default-skips (ran `node --test stock-broker-live.test.ts` with no env var → 3/3 skipped, 0 run). `08.2-BROKER-LIVE-EVIDENCE.md` records the measured verdict `blast_radius: all-program-loads` with literal argv/JSON transcripts (not re-run live myself per the task's live-test prohibition, but the evidence document, test file structure, and registration are all directly verified) |
| 4 | `node test-gate.mjs` AND CI's own bare `npm test` both report zero failures | ✓ VERIFIED | Ran both myself from the main checkout (not a worktree): `node test-gate.mjs` → 1663 pass / 0 fail / 5 todo (exit 0); `npm test` → 1800 pass / 0 fail / 30 skipped / 5 todo (exit 0) |
| 5 | `c64-ram-capture` driven end to end against a provably broker-launched genuine-stock instance; `08-HUMAN-UAT.md` Test 1 records pass or fail, never pending | ✓ VERIFIED | `08-HUMAN-UAT.md` frontmatter: `status: passed`, `resolved_by: Phase 8.2, plan 08.2-04`; no `pending`/`TBD` marker describing the outcome (only `pending: 0` in the tallied Summary section). Disclosed limitations (local checkout not published release; agent-proxy driver; Node MCP client script rather than LLM turn-by-turn calls) match exactly what the task description says was accepted; developer's verbatim "approved" recorded in `08.2-04-SUMMARY.md`. FINDING-E2 (keyboard fallback) correctly recorded as open/non-blocking, not silently fixed |
| 6 | REQUIREMENTS.md/ROADMAP.md/STATE.md agree with the actual re-run outcome, coverage arithmetic self-consistent, E-1..E-5 all have tracked homes | ✓ VERIFIED | REQUIREMENTS.md: "51 in scope — 51 complete, 0 open" (51=51+0, self-consistent); DIST-03 `[x]`/Complete with an evidence-cited correction paragraph. ROADMAP.md: all three "two-vs-three" tool count sites now say "three" / name `vice_keyboard_restore`. STATE.md: frontmatter `percent: 80` matches body `Progress: [████████░░] 80%`; Blockers/Concerns and Deferred Items both flipped from open to Fixed, blast-radius wording corrected to "all program loads" (not the narrower "disk loads"). `.planning/todos/pending/` contains all 5 mandatory Phase 8.1 findings (A1, B1, B2, C3/C4-class) plus FINDING-E2 as 6 new tracked todos |
| 7 | The tree's own verification/validation paperwork is truthful enough to tag (phase goal's own second clause) | ✗ FAILED | `08.2-VALIDATION.md` still reads `status: draft` / `nyquist_compliant: false` / `wave_0_complete: false` with every task row `⬜ pending`, despite all six plans being complete and green. `08-VERIFICATION.md` (Phase 8's report, DIST-03's home) still reads `status: human_needed` and its `human_verification[0].resolved` field still says `outcome: failed`, directly contradicting `08-HUMAN-UAT.md`'s now-`passed` re-run this same phase produced |

**Score:** 6/7 truths verified. Truth 7 is a documentation/paperwork gap, not a
product or code defect — every functional claim (drive-config fix, config isolation,
live coverage, green test gate, verified capture, requirements bookkeeping) is
independently confirmed true against the actual codebase and live command output.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.claude/mcp/vice/broker-launch.mts` | stock branch emits `-default -drive8type 1541` first; fork branch unchanged; widened `spawnFn`/deps types | ✓ VERIFIED | Confirmed by direct read + `git diff dc9ad85..HEAD` |
| `.claude/mcp/vice/resources/broker-launch.mjs` | fresh compile of the above | ✓ VERIFIED | `node --test resources-sync.test.ts` passes; contains `-drive8type`/`XDG_CONFIG_HOME` |
| `.claude/mcp/vice/vice-broker.mts` | `makeLoggingSpawn()` merges options; both real spawn-factory closures forward them | ✓ VERIFIED | grep-confirmed 0 remaining two-arg droppers |
| `.claude/mcp/vice/resources/vice-broker.mjs` | fresh compile | ✓ VERIFIED | `resources-sync.test.ts` passes; contains `XDG_CONFIG_HOME` |
| `.claude/mcp/vice/vice-broker-acquire.test.ts` | non-bypassable composition tests, `buildColdSpawnFactory` omitted | ✓ VERIFIED | Read in full; ran live, 15/15 pass |
| `.claude/mcp/vice/stock-broker-live.test.ts` | broker-primitive live `.d64`/`.prg` coverage, 7th `MANUAL_ONLY_TESTS` entry | ✓ VERIFIED | 741 lines; registered; default-skips confirmed live |
| `.claude/mcp/vice/test-gate.mjs` | 7-entry `MANUAL_ONLY_TESTS`, explanatory paragraph for the new file | ✓ VERIFIED | Read in full |
| `08.2-BROKER-LIVE-EVIDENCE.md` | recorded transcripts + settled blast-radius verdict | ✓ VERIFIED | `blast_radius: all-program-loads` in frontmatter and body |
| `08.2-WALKTHROUGH-EVIDENCE.md` | recorded pass verdict, sha256, argv/env proof | ✓ VERIFIED | `capture_result: pass`, sha256 cross-checked against `08-HUMAN-UAT.md` |
| `08-HUMAN-UAT.md` | Test 1 recorded as passed | ✓ VERIFIED | frontmatter `status: passed` |
| `.planning/REQUIREMENTS.md` | DIST-03 corrected, coverage self-consistent | ✓ VERIFIED | 51/51/0 |
| `.planning/ROADMAP.md` | "three" tool count everywhere | ✓ VERIFIED | grep across all cited lines |
| `.planning/STATE.md` | body/frontmatter self-consistent; Drive8Type reconciled | ✓ VERIFIED (self-consistency); STALE (Current Position section, see Truth 7 discussion below) |
| `08.2-VALIDATION.md` | reconciled to reflect actual execution | ✗ STUB (still draft) | See Truth 7 |
| `08-VERIFICATION.md` | reflects the actual DIST-03 outcome | ✗ STALE | See Truth 7 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `git ls-files -- '*.sh'` | `host-scripts.test.ts` allowlist | untrack of `08.1-d-checklist.sh` | ✓ WIRED | Returns exactly 3 paths; `node --test host-scripts.test.ts` passes |
| `buildViceArgs()` stock branch | spawned `x64sc` process argv | `tryLaunchOne()`/`spawnAndRecordInstance()` | ✓ WIRED | `-default -drive8type 1541 -binarymonitor ...` confirmed via source read and via `08.2-WALKTHROUGH-EVIDENCE.md`'s live `ps -o args=` transcript |
| `spawnAndRecordInstance()`'s scratch `XDG_CONFIG_HOME` | `nodeSpawn()`'s env | `withCrashSupervision` → `makeLoggingSpawn` merge, forwarded at every hop | ✓ WIRED | Proven live by the non-bypassable composition tests (options seam never injected below `handleAcquire()`/`_maintainWarmFloorForRealBroker()`) |
| `handleAcquire()`'s cold-launch spawnFactory closure | `makeLoggingSpawn` + `withCrashSupervision` | the real, un-injected composition | ✓ WIRED | `vice-broker-acquire.test.ts` test 1 (`ok 1`), passing |
| `stock-broker-live.test.ts` | `buildViceArgs()` via spawned `resources/vice-broker.mjs` | `openBrokerControl()`/`acquire()` against a real broker artifact | ✓ WIRED (structurally; not re-run live per task constraints) | Read in full; matches `08.2-BROKER-LIVE-EVIDENCE.md`'s claimed route |
| `08-HUMAN-UAT.md` | `.planning/REQUIREMENTS.md` DIST-03 | traceability row citing the recorded verdict | ✓ WIRED | Cross-checked, consistent |
| `08-HUMAN-UAT.md` (passed) | `08-VERIFICATION.md` (still failed) | the same requirement's two homes | ✗ NOT WIRED | This is Truth 7's gap — the two documents disagree |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Narrowed automated gate is green | `cd .claude/mcp/vice && node test-gate.mjs` | 1663 pass / 0 fail / 5 todo, exit 0 | ✓ PASS |
| CI's bare `npm test` superset is green | `cd .claude/mcp/vice && npm test` | 1800 pass / 0 fail / 30 skipped / 5 todo, exit 0 | ✓ PASS |
| The I-1 composition proof itself runs green | `cd .claude/mcp/vice && node --test vice-broker-acquire.test.ts` | 15/15 pass, including both composition tests | ✓ PASS |
| `stock-broker-live.test.ts` default-skips outside opt-in | `cd .claude/mcp/vice && node --test stock-broker-live.test.ts` | 3/3 skipped, 0 run, exit 0 | ✓ PASS |
| Generated artifacts match source (`.mts` → `.mjs` drift guard) | `cd .claude/mcp/vice && node --test resources-sync.test.ts` | 2/2 pass | ✓ PASS |
| `test-gate.mjs`'s own self-consistency guard | `cd .claude/mcp/vice && node --test test-gate.test.ts` | 3/3 pass | ✓ PASS |
| Typecheck is clean | `npx tsc --noEmit -p .claude/mcp/vice/tsconfig.json` | 0 errors | ✓ PASS |
| Fork branch argv byte-identical to before this phase | `git diff dc9ad85..HEAD -- .claude/mcp/vice/broker-launch.mts` | No hunk touches the `["-mcpserver",...]` return line | ✓ PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` files exist in this repository and no plan/summary in
this phase declares a probe script. Step 7c: **SKIPPED (no runnable probe entry points;
this project's live-verification harness is `node:test` files under
`.claude/mcp/vice/`, exercised directly above)**.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|--------------|-------------|-------------|--------|----------|
| I-3 | 08.2-01 | Untrack throwaway checklist script, restore green test gate | ✓ SATISFIED | 3 tracked `.sh` files; `host-scripts.test.ts` green |
| I-2 | 08.2-02, 08.2-03 | `Drive8Type` launch-config fix + live coverage | ✓ SATISFIED | Source fix confirmed; live evidence recorded |
| I-1 | 08.2-02, 08.2-06 | Config isolation (`XDG_CONFIG_HOME`) end to end | ✓ SATISFIED | Non-bypassable composition tests pass |
| E-1..E-5 | 08.2-05 | Documentation drift closure | ✓ SATISFIED (mostly) | REQUIREMENTS/ROADMAP/STATE reconciled and self-consistent, 6 todos tracked — but see Truth 7: two related documents (this phase's own `08.2-VALIDATION.md` and Phase 8's `08-VERIFICATION.md`) were not brought into the same reconciliation pass |
| DIST-03 | 08.2-04 | Verified 64K RAM capture on broker-launched stock instance, human-approved | ✓ SATISFIED | `08-HUMAN-UAT.md` passed, developer approved | 

No orphaned requirements found for this phase — `.planning/ROADMAP.md`'s Phase 8.2 entry states `Requirements: TBD` by design (this phase closes an existing requirement, DIST-03, and maps its own work to audit items I-1/I-2/I-3/E-1..E-5 rather than new requirement IDs), matching the task's own framing.

### Anti-Patterns Found

Scanned every file this phase's plans modified (`broker-launch.mts`, `resources/broker-launch.mjs`,
`broker-launch.test.ts`, `vice-broker.mts`, `resources/vice-broker.mjs`,
`vice-broker-acquire.test.ts`, `stock-broker-live.test.ts`, `test-gate.mjs`,
`REQUIREMENTS.md`, `ROADMAP.md`, `STATE.md`):

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `.planning/ROADMAP.md` | 541, 799, 821, 850-851 | `TBD` | ℹ️ Info | Pre-existing, not introduced by this phase (confirmed via `git diff dc9ad85..HEAD`); `Requirements: TBD` on Phase 8.2's own entry is intentional per the phase's stated framing (no new requirement, maps to audit items instead), and the other three are unrelated future-phase (9, 10) placeholders |
| `.claude/mcp/vice/broker-launch.mts` | 300-309 | Stale present-tense comment ("this options argument is dropped again at four further hops... Plan 08.2-06 (wave 2) owns threading it") | ⚠️ Warning (doc-drift, not functional) | Plan 08.2-06 already closed all four hops within this same phase; the comment was never updated to past tense. Independently confirmed by `08.2-REVIEW.md` WR-01/WR-02 (0 critical, 2 warnings, both this same comment-drift class) |
| `.planning/phases/.../08.2-VALIDATION.md` | frontmatter | Stale `draft`/`false`/`false` status, unreconciled `⬜ pending` task rows | 🛑 Blocker-class for Truth 7 | See gaps section above |
| `.planning/phases/08-.../08-VERIFICATION.md` | frontmatter, human_verification | Stale `human_needed`/`outcome: failed` | 🛑 Blocker-class for Truth 7 | See gaps section above |

No `TODO`/`FIXME`/`XXX`/`HACK`/`PLACEHOLDER` markers found in any source or test file this
phase touched. No empty-implementation or hardcoded-empty-data patterns found in the
production/test files reviewed.

### Human Verification Required

None. The phase's one blocking human-verify checkpoint (Plan 04, Task 3 — reviewing the
DIST-03 re-run evidence) was already presented to and explicitly approved by the developer
("approved", verbatim, recorded in `08.2-04-SUMMARY.md`) before this verification ran. No
new human-verification items were identified.

### Gaps Summary

Six of seven observable truths are fully verified against source, git history, and live
command output — not against SUMMARY prose. Every product-level claim this phase makes
(the `Drive8Type` launch-config fix, the `XDG_CONFIG_HOME` isolation reaching every real
launch hop through a non-bypassable composition test, the new broker-primitive live
coverage and its measured `all-program-loads` blast-radius verdict, a fully green
`node test-gate.mjs` and `npm test` from the main checkout, and a human-approved verified
65536-byte RAM capture) holds up under direct, independent re-derivation.

The one gap is documentation-paperwork, not product: this phase's own `08.2-VALIDATION.md`
was never reconciled after execution (still reads `draft`/`false`/`false` with every task row
`pending`), and Phase 8's `08-VERIFICATION.md` — the report that owns DIST-03 — was never
updated to reflect the passed re-run this same phase produced, so it still tells a reader the
finish-line requirement failed. Both gaps are independently confirmed by
`.planning/v0.2.0-MILESTONE-AUDIT.md` (round 3, gaps V-2/V-3), whose own verdict
(`gaps_found`, `gap_class: verification_paperwork`, "no product blocker remains") this
verification corroborates rather than merely repeats — every underlying claim in that audit
was re-checked directly here (test runs, git diffs, source reads, live command output), not
taken on faith.

This is a small, mechanical closure: flip `08.2-VALIDATION.md`'s frontmatter and Per-Task
Verification Map to reflect the actual (green) execution, and update `08-VERIFICATION.md`'s
frontmatter/human_verification block to point at the passed re-run while preserving the
original failed record as history (the same pattern `08-HUMAN-UAT.md` itself already uses).
Neither requires touching any source file, re-running any live test, or reopening any closed
defect.

---

_Verified: 2026-08-19T16:49:27Z_
_Verifier: Claude (gsd-verifier)_
