---
phase: 08.2
slug: close-v0-2-0-blockers-drive-config-test-gate-walkthrough
status: audited
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-19
reconciled: 2026-08-19
reconciled_by: Phase 8.2 execution close-out (audit round 3 gap V-2)
---

# Phase 08.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `08.2-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node's built-in `node:test` — no separate test framework |
| **Config file** | none — `.claude/mcp/vice/test-gate.mjs` is the one source of truth for which files are automated vs. manual-only (`MANUAL_ONLY_TESTS`) |
| **Quick run command** | `cd .claude/mcp/vice && node --test broker-launch.test.ts host-scripts.test.ts` |
| **Full suite command** | `cd .claude/mcp/vice && node test-gate.mjs` |
| **Estimated runtime** | ~90 seconds full gate (1650 pass / 1 fail / 5 todo at phase start; 1663 pass / 0 fail / 5 todo at close-out) |

**Live stock-VICE opt-in command (manual-only, not part of the gate):**
`cd .claude/mcp/vice && VICE_LIVE_STOCK_BIN=/usr/bin/x64sc node --test stock-broker-live.test.ts`

---

## Sampling Rate

- **After every task commit:** `cd .claude/mcp/vice && node --test broker-launch.test.ts host-scripts.test.ts`
- **After every plan wave:** `cd .claude/mcp/vice && node test-gate.mjs` — must return **0 failures** (it returns 1 at phase start; item 2 is what turns it green)
- **Live gate before item 3 (walkthrough re-run):** `VICE_LIVE_STOCK_BIN=/usr/bin/x64sc node --test stock-broker-live.test.ts` must pass — enforces the ROADMAP's ordering constraint that item 1 lands before item 3
- **Before `/gsd-verify-work`:** full `test-gate.mjs` green **and** `08-HUMAN-UAT.md` records the re-run outcome as pass or fail (never pending)
- **Max feedback latency:** ~90 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 08.2-01-xx | 01 | 1 | I-3 (red test gate) | — | N/A — repo hygiene; no secret or path surface | structural/unit | `node --test host-scripts.test.ts` | ✅ exists (now green, 4/4) | ✅ green |
| 08.2-02-xx | 02 | 1 | I-2 (drive fix argv) | T-08.2-01 | Stock argv contains `-drive8type 1541` positioned after `-default` and before `-binarymonitor`; fork argv byte-identical to before (BACK-02 standing gate) | unit | `node --test broker-launch.test.ts` | ✅ extended | ✅ green (68/68; fork branch confirmed unchanged via `git diff dc9ad85..HEAD`) |
| 08.2-02-xx | 02 | 1 | I-1 (config isolation rider) | T-08.2-02 | `spawnFn`'s options parameter threads a scratch `XDG_CONFIG_HOME` for stock launches; the user's real VICE config is never written | unit | `node --test broker-launch.test.ts` | ✅ extended | ✅ green (68/68 seam-level; end-to-end proof in `vice-broker-acquire.test.ts` 15/15, plan 06) |
| 08.2-02-xx | 02 | 1 | resources-sync (mandatory rebuild) | — | Committed `resources/broker-launch.mjs` matches a fresh `node build.ts` rebuild | structural/unit | `node --test resources-sync.test.ts` | ✅ exists — no new test, but the rebuild step is mandatory | ✅ green (2/2; both `broker-launch.mjs` and `vice-broker.mjs` rebuilt) |
| 08.2-03-xx | 03 | 2 | I-2 (broker-launched coverage) | — | A real broker-launched stock instance's `vice_autostart` / `vice_disk_attach` succeeds against a real `.d64` | manual-only live | `VICE_LIVE_STOCK_BIN=/usr/bin/x64sc node --test stock-broker-live.test.ts` | ✅ **Wave 0 delivered** — new file, 7th `MANUAL_ONLY_TESTS` entry | ✅ green (passed live; default-skips with no env var, verified 3/3 skipped) |
| 08.2-03-xx | 03 | 2 | I-2 (`.prg`-only blast radius) | — | Empirically resolves whether `.prg` autostart hits the same `Drive8Type=0` wall as `.d64` loads | manual-only live | same new file, second `test()` case | ✅ **Wave 0 delivered** — same new file | ✅ green — **MEASURED verdict `all-program-loads`** (a bare `.prg` hit the identical wall; wider than the audit's inferred "disk loads"). See `08.2-BROKER-LIVE-EVIDENCE.md`. |
| 08.2-04-xx | 04 | 3 | DIST-03 (walkthrough re-run) | — | N/A | human / agent-proxy manual | not automatable — see `08-HUMAN-UAT.md`'s own `why_human` | N/A — recorded outcome, not a test file | ✅ green — `capture_result: pass`, verified 65536-byte capture (sha256 `33bf6105…65967286`), human-approved at plan 04's blocking gate. Limitations recorded, not glossed. |
| 08.2-05-xx | 05 | 4 | E-1..E-5 (doc drift) | — | N/A | structural | doc-consistency assertions (body value == frontmatter value; never a pinned magic number) | ✅ pattern established in Phase 8.1 | ✅ green — self-consistency only, no pinned literals; 3 stale two-vs-three sites corrected; 6 todos tracked |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Task IDs are placeholders (`xx`) until the planner assigns them; plan/wave assignment above reflects the ROADMAP's ordering constraint (item 1 → item 3; item 2 independent) and is the planner's to confirm.*

---

## Wave 0 Requirements

- [ ] `.claude/mcp/vice/stock-broker-live.test.ts` — **new file.** Covers I-2's mandated "coverage through the broker" and the `.prg`-only blast-radius question. Opt-in via `VICE_LIVE_STOCK_BIN`, following the convention already used by `stock-live.test.ts`, `stock-live-triage.test.ts`, and `stock-live-broker-monitor.test.ts`.
- [ ] `.claude/mcp/vice/test-gate.mjs` — register the new file as the **seventh** `MANUAL_ONLY_TESTS` entry. That file's own header comment names it as the one place to extend, "never a parallel list."
- [ ] `.claude/mcp/vice/broker-launch.test.ts` — extend with unit cases for (a) the fixed stock argv shape and flag ordering, (b) the widened `spawn` signature threading an options object, (c) a regression assertion that the fork branch's argv is unchanged.
- [ ] No framework install needed — `node:test` is already wired.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `c64-ram-capture` reaches a verified 64K capture on a **broker-launched** stock instance | DIST-03 | The claim is about a human-observable end-to-end walkthrough through the real host broker and a real emulator window; no in-process harness can witness it. This is the exact claim Phase 8.1's walkthrough falsified. | Reuse Phase 8.1's protocol verbatim — its `08-HUMAN-UAT.md` frontmatter and `08.1-WALKTHROUGH-EVIDENCE.md` scratch-project recipe. Record the outcome as **pass or fail, never pending.** |
| `vice_autostart` / `vice_disk_attach` against a real `.d64` on a real broker-launched stock instance | I-2 | Requires spawning a genuine unpatched `/usr/bin/x64sc` and a real host broker; stubbed fixtures structurally cannot catch a resource-default defect, which is precisely how this one hid. | `VICE_LIVE_STOCK_BIN=/usr/bin/x64sc node --test stock-broker-live.test.ts`. Not in the automated gate; run explicitly as the item-1-before-item-3 gate. |
| `.prg`-only autostart blast radius | I-2 (open question) | Only a live run against the pre-fix and post-fix argv can answer whether `.prg` autostart bypasses the drive entirely. | Second `test()` case in the same new file, exercised both with and without `-drive8type 1541`. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (`stock-broker-live.test.ts` + its `test-gate.mjs` registration)
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s
- [ ] Doc-drift assertions (plan 05) assert **self-consistency**, never a pinned magic number like `78%` or `51/51/0` — those are recomputed from disk by `phase.complete` and `state.*`
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** approved — phase executed and independently verified; see `08.2-VERIFICATION.md`. Full gate green at close-out: `node test-gate.mjs` 1663 pass / 0 fail / 5 todo, CI's bare `npm test` 1800 pass / 0 fail, `npx tsc --noEmit` clean.
