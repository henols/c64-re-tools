---
phase: 10-adoption-boundaries-automated-bootstrap-and-the-removal
plan: 09
type: execute
wave: 6
depends_on: ["10-06", "10-07", "10-08"]
worktree: false
worktree_reason: "This plan's deliverables are .planning/ROADMAP.md and .planning/REQUIREMENTS.md content plus todo-file moves. Worktree isolation strips ROADMAP.md and STATE.md from executor commits (precedent: plan 09-08), so under worktree isolation this plan reports success while delivering nothing. Run it sequentially on the main tree."
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/notes/regenerator2000-integration.md
  - .planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-RESEARCH.md
  - .claude/skills/vice-wedge-triage/SKILL.md
  - .planning/todos/pending/2026-08-17-document-second-binmon-client-as-a-wedge-lookalike.md
  - .planning/todos/pending/2026-08-19-acme-build-scaffold-library-missing-on-both-provisioning-routes.md
autonomous: true
requirements: [R2000-03, R2000-01]
must_haves:
  truths:
    - "D-03: the ROADMAP no longer claims an input set the phase does not ship — criterion 3's wording names `.prg`, `.d64` (named entry) and flat 64K, and the milestone's standing 'prefer `.vsf` over `.raw`' constraint is marked as not applying to Phase 10, with the reason"
    - "D-14: the Apache-2.0-only claim stops propagating — `R2000-03`'s requirement text and `.planning/notes/regenerator2000-integration.md` both say `MIT OR Apache-2.0` (dual), matching what Phase 9 verified and what plan 10-08's notice now records"
    - "Folded todo 1, item 1: `vice-wedge-triage` names the second-binmon-client discriminator explicitly — a socket that accepts a connection but never answers is contention, not a wedge — and names the concrete non-r2000 causes"
    - "R2000-01 stays honest at the surface a reader meets: the wedge playbook states that this plugin's own regenerator2000 route can never be that second client, because the launch path refuses `--vice` in code"
    - "Both folded todos are closed on disk with their resolution recorded, not left pending after the work that resolved them shipped"
    - "`10-RESEARCH.md`'s § Open Questions no longer reads as open: the heading carries `(RESOLVED)` and each of the three questions names the plan that settled it and the outcome — including that Q2 was settled **against** the research recommendation, which Phase 11 inherits the package-boundary reasoning from"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Criterion 3 wording matching what the phase shipped, and the amended standing constraint"
    - path: ".planning/REQUIREMENTS.md"
      provides: "R2000-03 with the corrected dual-licence wording"
    - path: ".claude/skills/vice-wedge-triage/SKILL.md"
      provides: "The contention discriminator and its named causes"
    - path: ".planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-RESEARCH.md"
      provides: "An Open Questions section marked RESOLVED, each question annotated with the plan that settled it"
      contains: "## Open Questions (RESOLVED)"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: "docs/phase9-regenerator2000-probe-findings.md"
      via: "the amendment's citation, so the reason is traceable"
      pattern: "phase9-regenerator2000-probe-findings"
---

<objective>
Stop three false claims and one stale-by-omission section from propagating past this phase,
close the second half of folded todo 1, and mark both folded todos resolved.

**D-03 requires this explicitly** — CONTEXT.md's own words: "Planner must reconcile
wording. ROADMAP.md's criterion 3 says 'A `.prg` or a `.vsf`', and the milestone's standing
constraint says 'prefer `.vsf` over `.raw`'. Both are superseded here. Amend the
criterion-3 wording and mark the standing `.vsf` preference as not applying to Phase 10,
rather than silently shipping a different input set than the ROADMAP claims."

**D-14 requires the second** — `R2000-03`'s own requirement text and
`.planning/notes/regenerator2000-integration.md` still say Apache-2.0 only. Phase 9's
findings flagged this for Phase 10 to pick up; plan 10-08 wrote the correct notice, and
this plan stops the wrong claim living on in the two places that would seed it again.

**The fourth is stale by omission rather than by assertion.** `10-RESEARCH.md`'s
§ Open Questions poses three questions that plans 10-03 and 10-04 have since settled, but
the heading still says "Open" and no question names the plan that closed it. Phase 11
inherits Q2's package-boundary reasoning directly, and Q2 was settled **against** the
research's own recommendation — so a Phase 11 reader who trusts the page as written would
reopen a shipped decision and reintroduce the coupling this phase avoided. The fix is
annotation, not deletion: the reasoning stays, the status is corrected.

**Folded todo 1's item 1** is the third: `vice-wedge-triage` already carries the
`monitor_held_elsewhere` row and verdict (added in Phase 7), so what is missing is only the
discriminator and the named causes — that is an extension, not a new section, and the plan
must not duplicate what is already there.

**Why this plan does not run in a worktree:** its deliverables include `ROADMAP.md`, which
worktree mode strips from executor commits. Plan 09-08 hit exactly this and recorded it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-CONTEXT.md
@.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-08-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: reconcile ROADMAP criterion 3 and the standing .vsf constraint with what the phase shipped</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` § Standing Constraints, the "Prefer `.vsf` over `.raw` for anything leaving the emulator" bullet (around line 187) and its stated reasoning
    - `.planning/ROADMAP.md` § Phase 10, success criterion 3 and the Notes block beneath the criteria, including the two Phase 9 amendments already recorded there
    - `.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-02-SUMMARY.md` and `10-04-SUMMARY.md` — the input set actually implemented, so the amended wording describes shipped behaviour rather than intent
    - `docs/phase9-regenerator2000-probe-findings.md` § Accepted limits entry 2 — the machine-type finding the standing constraint's amendment cites
  </read_first>
  <action>
Two edits to `.planning/ROADMAP.md`, both additive-and-corrective rather than deletions of
history.

(a) **Phase 10, criterion 3.** Its current wording is "A `.prg` or a `.vsf` becomes a
`.regen2000proj` without a human — or, if Phase 9's verdict was *degrade*, it is a
documented one-time interactive step…". Amend it to state what shipped: a `.prg`, a `.d64`
with the file inside it named explicitly, or a flat 64K capture becomes a
`.regen2000proj` without a human. Keep the "honest at the surface a user reads" clause.
Add a bracketed note recording that `.vsf` was dropped from this phase by D-03 and that
its home is Phase 11's `c64-ram-capture` extension (`R2000-14`/`R2000-15`), with the
reason in one clause: the bootstrap synthesises the project file in Node and never hands
regenerator2000 a container format, so parsing VICE snapshots ourselves would be new work
whose only payoff Phase 9 already found unreliable for the machine-type field.

Do not delete the criterion's history — this ROADMAP records what was planned as well as
what happened, and a Notes entry explaining the amendment is worth more than a silent
rewrite.

(b) **§ Standing Constraints, the `.vsf` preference bullet.** Append a sentence marking it
as **not applying to Phase 10**, with the reason and a citation to
`docs/phase9-regenerator2000-probe-findings.md` § Accepted limits entry 2: the
machine-type field it promises is correct only by coincidence for C64 snapshots, and Phase
10's route never loads a container format at all. State that the preference still stands
for phases that do hand a file to regenerator2000's own loader.

Keep the edits surgical: no reflow of unrelated bullets, so the diff reads as two
amendments.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -c 'd64' .planning/ROADMAP.md && grep -c 'phase9-regenerator2000-probe-findings' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - Phase 10's criterion 3 names all three shipped inputs — verify `.prg`, `.d64` and a flat 64K phrase each appear within that criterion's text
    - Criterion 3 (or the Notes beneath it) states `.vsf` is out of Phase 10 and names Phase 11 as its home
    - The Standing Constraints `.vsf` bullet contains an explicit "does not apply to Phase 10" statement with a citation to the findings document
    - `git diff --stat .planning/ROADMAP.md` shows changes confined to those two regions
    - The change is present in the committed tree, not only in the working copy: `git show HEAD:.planning/ROADMAP.md | grep -c 'd64'` returns at least 1 after the task's commit
  </acceptance_criteria>
  <done>The ROADMAP describes the input set the phase actually shipped, and the standing constraint no longer promises something Phase 9 disproved.</done>
</task>

<task type="auto">
  <name>Task 2: correct the Apache-2.0-only claim, and close out RESEARCH.md's Open Questions</name>
  <files>.planning/REQUIREMENTS.md, .planning/notes/regenerator2000-integration.md, .planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-RESEARCH.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 54-75 — `R2000-03`'s text ("with its Apache-2.0 notice in `THIRD-PARTY-NOTICES.md`") and `R2000-09`'s text ("a `.prg` or `.vsf` becomes a `.regen2000proj`")
    - `.planning/notes/regenerator2000-integration.md` around line 253 — the "Licensing. Apache-2.0." claim
    - `docs/phase9-regenerator2000-probe-findings.md` § Build tested and § Corrections entry 2 — the verified dual licence and the explicit hand-off of this correction to Phase 10
    - `.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-08-SUMMARY.md` — the notice text as written, so the requirement and the artifact agree
    - `.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-RESEARCH.md` § Open Questions (heading at line 408, questions at 410, 415, 420) — all three still written in the future tense, with no marker saying they were settled
    - `.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-03-SUMMARY.md` and `10-04-SUMMARY.md` — what those plans actually built, so each RESOLVED line records the shipped outcome rather than restating the research's own recommendation
  </read_first>
  <action>
Three corrections, each replacing a claim rather than annotating it, since all three are
simply wrong as written:

1. `.planning/REQUIREMENTS.md`, `R2000-03`: "with its Apache-2.0 notice" becomes the dual
   `MIT OR Apache-2.0` wording. Add a short parenthetical citing Phase 9's correction so a
   later reader can see why the requirement text changed rather than suspecting drift.
2. `.planning/REQUIREMENTS.md`, `R2000-09`: its "a `.prg` or `.vsf`" phrasing is superseded
   by D-03 the same way criterion 3 was. Amend it to the shipped set (`.prg`, `.d64` with a
   named entry, flat 64K) and note `.vsf`'s deferral to Phase 11, keeping the requirement's
   own intent ("automated rather than a documented manual step") intact.
3. `.planning/notes/regenerator2000-integration.md` around line 253: "Licensing.
   Apache-2.0." becomes the dual claim, with the same citation. This note is treated as
   research already performed and is read by later phases, so leaving it wrong reseeds the
   error in Phase 11.

4. `10-RESEARCH.md` § Open Questions. All three questions **are** settled by plans this
   phase already executed, but nothing on the page says so, and Phase 11 inherits Q2's
   package-boundary reasoning directly — a reader arriving cold would reopen a decision that
   has already shipped. Retitle the heading to `## Open Questions (RESOLVED)` and give each
   question an inline `RESOLVED: plan 10-0X — <one-line outcome>` line, placed immediately
   under the question's bolded title so it is read before the "What's unclear" prose that is
   now historical.

   Read `10-03-SUMMARY.md` and `10-04-SUMMARY.md` first and state what shipped, not what was
   recommended. The three outcomes as planned:
   - **Q1** (reaching the D-06 seam across the package boundary) — resolved by plan 10-04:
     argv-subcommand dispatch on the existing `vice-mcp` bin, which is what the research
     recommended.
   - **Q2** (`.d64` extraction: extend `d64-parse.mjs`, or land beside the seam) — resolved
     by plan 10-03 **against** the research's recommendation: a container-side `r2000-d64.ts`
     beside the seam, not an extension of `d64-parse.mjs`. Say so explicitly, and say why in
     one clause, because the research page argues the opposite and a later reader who trusts
     it will reintroduce the coupling this phase deliberately avoided.
   - **Q3** (CLI verb names) — resolved by plan 10-04: the verbs are `bootstrap`,
     `export-asm` and `verify`, and they live on the `vice-mcp` bin rather than inside
     `acme.mjs`, consistent with D-12.

   Annotate; do not delete the original question text. This is a research artifact and its
   reasoning is the record of why each decision went the way it did — a bare "RESOLVED" with
   the reasoning deleted is worse than leaving it open.

Do not mark any requirement's checkbox in this task — requirement completion is the phase
close's bookkeeping, not a doc-correction task's business.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && ! grep -n 'Apache-2.0' .planning/REQUIREMENTS.md .planning/notes/regenerator2000-integration.md | grep -v 'MIT OR Apache-2.0' && test "$(grep -c 'RESOLVED: plan 10-0' .planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-RESEARCH.md)" -eq 3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'Apache-2.0' .planning/REQUIREMENTS.md .planning/notes/regenerator2000-integration.md | grep -vc 'MIT OR Apache-2.0'` returns 0 — every remaining mention is inside the dual phrase
    - `R2000-09`'s text names `.d64` and flat 64K and states `.vsf` is deferred to Phase 11
    - No requirement checkbox changed: `git diff .planning/REQUIREMENTS.md | grep -c '^[+-]- \[x\]'` returns 0
    - `grep -c '^## Open Questions (RESOLVED)$' 10-RESEARCH.md` returns 1, and `grep -c '^## Open Questions$'` on the same file returns 0
    - `grep -c 'RESOLVED: plan 10-0' 10-RESEARCH.md` returns exactly 3 — one per question, none left unannotated
    - Q2's annotation names `r2000-d64` and states the outcome went against the research's own recommendation: `grep -c 'r2000-d64' 10-RESEARCH.md` returns at least 1
    - Q3's annotation names all three shipped verbs — `bootstrap`, `export-asm` and `verify` each appear on a RESOLVED line
    - The original question text survives: `git diff --numstat 10-RESEARCH.md` shows added lines and, apart from the retitled heading, one deleted line at most
    - All three files' changes are in the committed tree after the task's commit
  </acceptance_criteria>
  <done>The two documents that would otherwise reseed the Apache-2.0-only claim and the `.vsf` input claim now say the verified thing, and RESEARCH.md's Open Questions read as settled — each naming the plan that settled it, with Q2's departure from its own recommendation stated rather than left for Phase 11 to trip over.</done>
</task>

<task type="auto">
  <name>Task 3: give vice-wedge-triage the contention discriminator, and close both folded todos</name>
  <files>.claude/skills/vice-wedge-triage/SKILL.md, .planning/todos/pending/2026-08-17-document-second-binmon-client-as-a-wedge-lookalike.md, .planning/todos/pending/2026-08-19-acme-build-scaffold-library-missing-on-both-provisioning-routes.md</files>
  <read_first>
    - `.claude/skills/vice-wedge-triage/SKILL.md` lines 8-35 — the five-state table, which ALREADY has the "Monitor held elsewhere (stock only)" row, and lines 57-70 — the verdict table, which ALREADY has `monitor_held_elsewhere`. This task extends; it must not duplicate either
    - `.planning/todos/pending/2026-08-17-document-second-binmon-client-as-a-wedge-lookalike.md` — items 1, 2 and 3, and which plan resolved each
    - `.planning/todos/pending/2026-08-19-acme-build-scaffold-library-missing-on-both-provisioning-routes.md`
    - `.planning/todos/completed/2026-08-19-ci-acme-install-has-no-timeout-or-retry.md` — the frontmatter shape a completed todo carries in this repo
    - `.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-08-SUMMARY.md` — the README wording, so the skill does not restate it differently
  </read_first>
  <action>
(a) Extend `.claude/skills/vice-wedge-triage/SKILL.md` with the discriminator folded todo 1
item 1 asks for, placed with the existing contention material rather than as a new
competing section. Add, in the skill's own compact voice:
  - The **cheap tell**: on the stock backend a socket that accepts a connection but never
    answers is contention, not a wedge — and the broker knows whether it already holds a
    lease on that port, which is the discriminator a human or agent can actually check.
  - The **named causes**, so a reader can go looking: a hand-run `nc` session left open, a
    second Claude session driving the same instance, VICE's own `-remotemonitor`, and any
    other 6502 debugger — including regenerator2000's `--vice` flag.
  - The **one thing that is not a cause here**: this plugin's own regenerator2000 route
    never passes `--vice`. State that it is refused in code, by construction and by a scan
    that throws, not by documentation (`R2000-01`, plan 10-01) — so a user chasing a silent
    emulator can rule it out immediately instead of suspecting it.
  - Keep the standing advice unchanged: contention is **never** a reason to recycle, because
    the instance is healthy and merely claimed elsewhere.

Do not add a duplicate table row or a second verdict entry — both already exist; this is an
extension of the existing material.

(b) Close both folded todos by moving them from `.planning/todos/pending/` to
`.planning/todos/completed/` with `git mv`, and append to each a short resolution block
naming the plan(s) that resolved it and the file(s) that carry the resolution:
  - `2026-08-17-document-second-binmon-client-as-a-wedge-lookalike.md` — item 1 resolved
    here, item 2 in plan 10-08's README section, item 3 (the real `--vice` guard mirroring
    `DENY_LIST`) in plan 10-01.
  - `2026-08-19-acme-build-scaffold-library-missing-on-both-provisioning-routes.md` —
    resolved in plan 10-07 (library-free `template.a`, corrected Setup claim, CI assembles
    the scaffold).
Match the frontmatter shape completed todos in this repo already use, and do not rewrite
the original body — append the resolution rather than editing history.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && test ! -e .planning/todos/pending/2026-08-17-document-second-binmon-client-as-a-wedge-lookalike.md && test ! -e .planning/todos/pending/2026-08-19-acme-build-scaffold-library-missing-on-both-provisioning-routes.md && node scripts/check-skill-fork-honesty.mjs && node scripts/check-skill-tool-coverage.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Both todo files exist under `.planning/todos/completed/` and neither remains under `pending/`
    - Each completed todo's appended block names the resolving plan number and at least one file carrying the resolution
    - `grep -ci 'remotemonitor' .claude/skills/vice-wedge-triage/SKILL.md` returns at least 1
    - `grep -c 'never answers' .claude/skills/vice-wedge-triage/SKILL.md` returns at least 1 (the discriminator), and the file still contains exactly one `monitor_held_elsewhere` verdict-table row and one "Monitor held elsewhere" state-table row — verify the counts did not double
    - `grep -ci 'vice' .claude/skills/vice-wedge-triage/SKILL.md` includes the statement that this plugin refuses the `--vice` flag in code
    - `node scripts/check-skill-fork-honesty.mjs` and `node scripts/check-skill-tool-coverage.mjs` both exit 0
  </acceptance_criteria>
  <done>A reader chasing a silent emulator gets the contention discriminator and its real causes, can rule this plugin's own r2000 route out immediately, and both folded todos are closed on disk with their resolutions recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning documents → later phases | A wrong claim left in ROADMAP/REQUIREMENTS/notes is read as ground truth by Phase 11 |
| triage playbook → an operator's recovery action | Wrong diagnosis leads to `vice_recycle` destroying a healthy instance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-15 | Repudiation | the Apache-2.0-only licence claim | mitigate | Corrected in both remaining sources (`REQUIREMENTS.md`, the integration notes) so the claim cannot reseed; the grep in Task 2's verification asserts every surviving mention sits inside the dual phrase |
| T-10-17 | Repudiation | a ROADMAP that claims an input set the code does not implement | mitigate | Criterion 3 and the standing constraint are amended against the shipped behaviour recorded in plans 10-02 and 10-04's summaries, with the reason and a citation, per D-03's explicit instruction |
| T-10-18 | Denial of Service | misdiagnosing monitor contention as a wedge | mitigate | The discriminator (a socket that accepts but never answers; the broker's own lease knowledge) plus the named causes are documented, and the standing "never recycle on contention" advice is preserved — recycling here destroys a healthy instance |
| T-10-01 | Denial of Service | a second binary-monitor client | mitigate | Documentation is the only control over a user's own separate process; the playbook now names every known cause and states that this plugin's own r2000 route is structurally not one of them |
| T-10-SC | Tampering | npm/cargo installs | accept | This plan changes documentation and moves two todo files; no dependency, no code path, no published artifact content changes beyond the skill prose that `sync-skills.mjs` regenerates at pack time |
</threat_model>

<verification>
1. `node scripts/check-skill-fork-honesty.mjs`, `node scripts/check-skill-tool-coverage.mjs`
   and `node scripts/check-npm-packages.mjs` all exit 0.
2. `cd .claude/mcp/vice && npm test` is still green.
3. `git show HEAD --stat` confirms `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md`
   are actually in the commit — this plan ran with `worktree: false` precisely because
   worktree mode would have stripped them, and the summary must state that it did.
4. `grep -rn 'Apache-2.0' .planning/ | grep -v 'MIT OR Apache-2.0'` returns nothing outside
   append-only phase history.
5. `10-RESEARCH.md`'s Open Questions heading carries `(RESOLVED)` and the file contains
   exactly three `RESOLVED: plan 10-0…` lines, one per question.
</verification>

<success_criteria>
- The ROADMAP's criterion 3 and standing `.vsf` constraint match what shipped, with the
  reason cited.
- No document outside append-only history claims regenerator2000 is Apache-2.0 only.
- `vice-wedge-triage` carries the contention discriminator, its named causes, and the fact
  that this plugin's r2000 route cannot be one of them.
- Both folded todos are in `completed/` with their resolutions recorded.
- `10-RESEARCH.md`'s Open Questions section reads as settled, each question naming the plan
  that settled it and the shipped outcome — with Q2 recorded as having gone against the
  research's own recommendation, so Phase 11 inherits the decision rather than the question.
</success_criteria>

<output>
Create `.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-09-SUMMARY.md` when done.
</output>
