---
phase: 10-adoption-boundaries-automated-bootstrap-and-the-removal
plan: 08
type: execute
wave: 5
depends_on: ["10-06"]
files_modified:
  - README.md
  - .claude/mcp/vice/THIRD-PARTY-NOTICES.md
  - scripts/check-skill-fork-honesty.mjs
autonomous: true
requirements: [R2000-03]
must_haves:
  truths:
    - "R2000-03: README.md names regenerator2000 as a required prerequisite alongside VICE, not an optional accelerator (D-R2)"
    - "D-15: the documented facts are the measured ones — `cargo install regenerator2000` with no upstream release assets, a rustc floor of 1.90, the two container-cost figures as absolute sizes, and the one-project-per-namespace limit stated rather than detected"
    - "D-14: the licence is stated as the true dual `MIT OR Apache-2.0`, with the notice in `.claude/mcp/vice/THIRD-PARTY-NOTICES.md`, the canonical file the root one points at"
    - "D-13: the CI honesty guard is inverted, not worked around — `regenerator2000` moves from `FORBIDDEN_README_SUBSTRINGS` to `REQUIRED_README_SUBSTRINGS` with its own `whatIsLost` string, and the file's stale header narrative is corrected in the same commit"
    - "Folded todo 1, item 2: the install docs state the one-holder rule positively — on the stock backend exactly one process may hold the binary monitor — and name the concrete traps, r2000 among them but not uniquely"
    - "R2000-05 stays deleted: a CI assertion fails the build if `toacme` or the `disasm` verb reappears in `acme-build`"
  artifacts:
    - path: "README.md"
      provides: "The regenerator2000 prerequisite section with measured costs, the stated two-project limit, and the one-holder rule"
      contains: "regenerator2000"
    - path: ".claude/mcp/vice/THIRD-PARTY-NOTICES.md"
      provides: "The dual MIT / Apache-2.0 notice for regenerator2000"
      contains: "Apache-2.0"
    - path: "scripts/check-skill-fork-honesty.mjs"
      provides: "The inverted README guard plus a permanent regression assertion for the deletion"
  key_links:
    - from: "scripts/check-skill-fork-honesty.mjs"
      to: "README.md"
      via: "REQUIRED_README_SUBSTRINGS containment check"
      pattern: "REQUIRED_README_SUBSTRINGS"
    - from: "scripts/check-skill-fork-honesty.mjs"
      to: ".claude/skills/acme-build/"
      via: "the toacme/disasm absence regression assertion"
      pattern: "toacme"
---

<objective>
Make regenerator2000 a declared prerequisite in the documentation a user actually reads,
with the measured numbers and the honest limits (criterion 5, `R2000-03`), and flip the CI
guard that currently forbids the name.

Three connected pieces. **D-15**: every documented fact is the measured one — no upstream
release assets exist, so install is `cargo install regenerator2000`; the toolchain floor is
rustc 1.90 (both the earlier 1.85 and 1.88 readings undercounted, and a `rust:1.88-slim`
image fails a real install); container cost is ~1.26 GB single-stage (5m39s build) or
~251 MB multi-stage (4m48s build), stated as absolute sizes with no baseline to diff; and
the one-project-per-namespace limit is **stated, not detected** (the `R2000-04` fold —
building detection for an upstream port collision is work in the wrong place). **D-14**:
the licence is the dual `MIT OR Apache-2.0`, which is what the notice must say. **D-13**:
`scripts/check-skill-fork-honesty.mjs` currently lists `regenerator2000` in
`FORBIDDEN_README_SUBSTRINGS` — Phase 8's guard, now inverted rather than worked around.

Folded todo 1's item 2 lands here too: state positively that on the stock backend exactly
one process may hold the binary monitor, and name the concrete traps. The hazard is not
r2000-specific — a stray `nc`, a second Claude session, or VICE's own `-remotemonitor`
does the same thing — and this project's own r2000 route can no longer cause it, because
plan 10-01's guard refuses `--vice` by construction and by scan.

The permanent regression assertion for plan 10-06's deletion lands here as well, because
this file is the single owner of documentation-honesty checks over first-party prose and
already carries per-file "this stale claim must not return" blocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-CONTEXT.md
@.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-PATTERNS.md
@.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-06-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README — regenerator2000 as a required prerequisite, with the measured costs and the stated limits</name>
  <files>README.md</files>
  <read_first>
    - `README.md` lines 20-62 (`## Install`, its two routes, and `### Dependencies`) and lines 63-168 (`## Installing VICE, and choosing a backend`, its per-package-manager table, the `### What a sub-3.10 VICE costs` subsection, and the `### Verifying a stock install` note that already states the one-client rule at line 164)
    - `.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-CONTEXT.md` D-15 — the measured figures, verbatim
    - `docs/phase9-regenerator2000-probe-findings.md` § Build tested and § Corrections — the primary source for the version, licence and toolchain-floor numbers
    - `.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-PATTERNS.md` lines 415-419 — the section shape to replicate from the existing VICE install section
  </read_first>
  <action>
Add a `## Installing regenerator2000` section to `README.md`, placed adjacent to the
existing VICE install section so a reader meets both prerequisites together, and modelled
on that section's shape: a short "what it is and why you need it" lead-in, a table of
measured facts, and an explicit statement of what breaks without it.

Content, all of it measured rather than estimated (D-15):
  - regenerator2000 is a **required prerequisite**, not an optional accelerator (D-R2).
    Say what depends on it in one line: the static disassembly route the `acme-build` and
    `c64-program-recon` playbooks now point at. Optional-with-detection was rejected
    because it forbids any removal — every skill would need a working fallback.
  - Install is `cargo install regenerator2000`. State plainly that **no upstream release
    assets exist**, so there is no binary download; this is a Rust toolchain cost.
  - Toolchain floor: **rustc >= 1.90**. Note that this is the verified figure and that the
    crate's own edition and lockfile understate it.
  - Measured build cost and image size, both as absolute numbers with no baseline to diff
    against: single-stage ~1.26 GB / ~5m39s, multi-stage ~251 MB / ~4m48s.
  - The version this was verified against (0.9.20) and the date, so a reader can tell how
    old the measurement is.
  - **The one-project-per-namespace limit, stated not detected**: regenerator2000's MCP
    server binds a hardcoded port, so two projects cannot run its server side by side in
    one network namespace. Say that this project documents the limit rather than detecting
    and reporting it (the `R2000-04` fold), and that separate containers sidestep it.
  - Licence: `MIT OR Apache-2.0` (dual), with a pointer to `THIRD-PARTY-NOTICES.md`.

Then extend the existing one-client statement near `### Verifying a stock install`
(currently a single sentence at line 164) into the positive rule folded todo 1 asks for:
on the stock backend **exactly one process may hold the binary monitor**, and a second
connection sits unserviced with no reply and no EOF — indistinguishable from a hang. Name
the concrete traps: a hand-run `nc` session, a second Claude session against the same
instance, VICE's own `-remotemonitor`, and any other 6502 debugger — explicitly including
regenerator2000's own `--vice` flag, with the note that this plugin never passes it (the
launch path refuses it in code, not merely in documentation). Point at
`vice-wedge-triage` for what to do when an emulator has gone silent.

Do not remove or reword any string in `REQUIRED_README_SUBSTRINGS`
(`VICE_BACKEND`, `vice_sid_get_state`, `vice_keyboard_matrix`, `docs/tool-support.md`,
`3.10`) — the honesty gate asserts each is present, and this edit must not knock one out.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -c 'regenerator2000' README.md && grep -c 'cargo install regenerator2000' README.md && grep -c '1.90' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'cargo install regenerator2000' README.md` returns at least 1
    - README contains each measured figure: `1.90`, `1.26 GB`, `251 MB`, `0.9.20` — verify each with its own grep
    - README contains the phrase describing the limit as stated rather than detected, and mentions `MIT OR Apache-2.0`
    - README still contains all five existing required substrings: `VICE_BACKEND`, `vice_sid_get_state`, `vice_keyboard_matrix`, `docs/tool-support.md`, `3.10`
    - README names the one-holder traps: `grep -c 'remotemonitor' README.md` returns at least 1 and `grep -ci 'exactly one process' README.md` returns at least 1
    - `node scripts/check-skill-fork-honesty.mjs` FAILS at this point with the message about the forbidden `regenerator2000` substring — record that observed failure in the summary; Task 3 is what makes it pass again, and seeing the guard bite proves it was real
  </acceptance_criteria>
  <done>A reader installing this plugin is told regenerator2000 is required, what it costs in measured numbers, what limit they inherit, and the one-holder rule with its named traps.</done>
</task>

<task type="auto">
  <name>Task 2: THIRD-PARTY-NOTICES — the dual MIT / Apache-2.0 notice for regenerator2000</name>
  <files>.claude/mcp/vice/THIRD-PARTY-NOTICES.md</files>
  <read_first>
    - `.claude/mcp/vice/THIRD-PARTY-NOTICES.md` in full — especially the `## Build/CI tools — not incorporated` section describing ACME, which is the closest analog (an external binary invoked as a subprocess, with nothing incorporated), and the `## Existing runtime dependencies` section
    - `THIRD-PARTY-NOTICES.md` at the repository root — the four-line pointer to the canonical file, which needs no edit
    - `docs/phase9-regenerator2000-probe-findings.md` § Build tested — the crates.io licence field (`MIT OR Apache-2.0`), publisher and publication date
  </read_first>
  <action>
Add a regenerator2000 notice to `.claude/mcp/vice/THIRD-PARTY-NOTICES.md`, modelled on the
existing ACME entry's honesty about what is and is not incorporated.

It must state:
  - regenerator2000 is invoked as an **external CLI subprocess**; no regenerator2000
    source, data table or output is included in this repository or in either published
    package, so its licence does not attach to anything shipped.
  - Its licence is **`MIT OR Apache-2.0`** — dual, at the user's option — confirmed from
    the crates.io licence field, with both `LICENSE-MIT` and `LICENSE-APACHE` shipping in
    the crate. Do NOT write "Apache-2.0" alone: that stale claim is what `R2000-03`'s own
    requirement text and `.planning/notes/regenerator2000-integration.md` still carry, and
    Phase 9 flagged it explicitly for this phase to correct.
  - The verified version (0.9.20), publisher (`ricardoquesada`, matching the linked GitHub
    repo owner) and publication date, so the provenance claim is re-checkable.
  - That it never appears in `.claude/mcp/vice/package.json`'s `files[]`, `dependencies`
    or `devDependencies` — it is a `cargo install`-provided tool, never an npm package —
    mirroring the ACME entry's own closing sentence, which is a checkable claim rather
    than a prose one.

Leave the root `THIRD-PARTY-NOTICES.md` pointer file unchanged; it already points here and
this file is canonical.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -c 'regenerator2000' .claude/mcp/vice/THIRD-PARTY-NOTICES.md && grep -c 'MIT OR Apache-2.0' .claude/mcp/vice/THIRD-PARTY-NOTICES.md && node scripts/check-npm-packages.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'MIT OR Apache-2.0' .claude/mcp/vice/THIRD-PARTY-NOTICES.md` returns at least 1
    - `grep -c 'ricardoquesada' .claude/mcp/vice/THIRD-PARTY-NOTICES.md` returns at least 1
    - `grep -c '0.9.20' .claude/mcp/vice/THIRD-PARTY-NOTICES.md` returns at least 1
    - The notice does not claim Apache-2.0 alone: every `Apache-2.0` occurrence in the new section sits inside the dual-licence phrase — verify by reading the added section
    - `node scripts/check-npm-packages.mjs` exits 0 (the notices file is still packed)
    - `git diff --name-only` shows the root `THIRD-PARTY-NOTICES.md` unchanged
  </acceptance_criteria>
  <done>The notices file records the true dual licence, the verified provenance, and the fact that nothing of regenerator2000 ships in either package.</done>
</task>

<task type="auto">
  <name>Task 3: invert the honesty guard, correct its stale header, and pin the deletion permanently</name>
  <files>scripts/check-skill-fork-honesty.mjs</files>
  <read_first>
    - `scripts/check-skill-fork-honesty.mjs` lines 1-30 (the header comment, whose item 2 still narrates "the regenerator2000 name Phase 8 removed") and lines 225-270 (`REQUIRED_README_SUBSTRINGS`, `FORBIDDEN_README_SUBSTRINGS`, and the two loops with their `need(...)` message shapes)
    - `scripts/check-skill-fork-honesty.mjs`'s later per-file regression block for `docs/stock-vice-parity.md` — the precedent for "assert this specific stale claim has not returned to this specific file"
    - `.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-PATTERNS.md` lines 377-405 — the exact tuple shapes and the Pitfall-4 warning that moving the array entry without fixing the header leaves a self-contradicting file
    - `.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-06-SUMMARY.md` — what plan 10-06 actually deleted, so the regression assertions name real strings
  </read_first>
  <action>
Three edits to `scripts/check-skill-fork-honesty.mjs`, all in one commit (D-13 plus the
deletion pin).

1. **Move the tuple.** Remove
   `["regenerator2000", "D-B: this phase's install docs must stay regenerator2000-free"]`
   from `FORBIDDEN_README_SUBSTRINGS` and add `["regenerator2000", "<whatIsLost>"]` to
   `REQUIRED_README_SUBSTRINGS`, in the identical `[needle, whatIsLost]` shape the existing
   entries use. The `whatIsLost` string must read naturally inside the existing message
   template ("README.md is missing the required string … without it, …") and must say what
   a reader actually loses: they are not told that a required prerequisite exists, so they
   hit the static-disassembly route with no tool installed and no explanation.

2. **Fix the header comment.** Item 2 of the file's opening block still says the README
   "must never re-introduce … the regenerator2000 name Phase 8 removed". Rewrite that
   clause to state the current rule: the README must now NAME regenerator2000 as a
   required prerequisite (Phase 10, `R2000-03`), and the ghost guardrail-test claims remain
   forbidden. Leaving the header stale would make the file contradict its own arrays —
   the exact half-done edit RESEARCH.md's Pitfall 4 names.

3. **Pin the deletion.** Add a new per-file regression block, modelled on the existing
   `docs/stock-vice-parity.md` block, asserting that plan 10-06's removal cannot silently
   return:
   - `.claude/skills/acme-build/scripts/acme.mjs` contains neither `toacme` nor `cmdDisasm`;
   - `.claude/skills/acme-build/SKILL.md` contains neither `toacme` nor `disasm`;
   - `.claude/skills/acme-build/SKILL.md` still contains the replacement pointer string
     (`r2000 export-asm`), so the deletion cannot be "fixed" by deleting the pointer too.
   Read each file once with `readFileSync` and use plain substring containment, matching
   this file's existing style — never a regex built from file content, never `eval`, never
   a spawn. Give each assertion a `need(...)` message naming what is lost: a playbook that
   advertises a verb the script no longer has sends an agent into an unknown-verb failure.

Then run the gate and confirm it now passes with Task 1's README in place — the sequence
matters: it must have failed before this task and pass after.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-skill-fork-honesty.mjs && node scripts/check-skill-tool-coverage.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-skill-fork-honesty.mjs` exits 0
    - `grep -c 'regenerator2000' scripts/check-skill-fork-honesty.mjs` returns at least 3 (the required tuple, the corrected header clause, and any assertion message)
    - The `regenerator2000` tuple appears inside `REQUIRED_README_SUBSTRINGS` and not inside `FORBIDDEN_README_SUBSTRINGS` — verify by reading both arrays
    - `grep -ci 'the regenerator2000 name Phase 8 removed' scripts/check-skill-fork-honesty.mjs` returns 0
    - Mutation check, both directions, recorded in the summary: (a) temporarily delete the regenerator2000 section from README.md and confirm the gate FAILS naming the missing required string, then restore; (b) temporarily re-add the word `toacme` to `.claude/skills/acme-build/SKILL.md` and confirm the gate FAILS naming it, then restore. A guard nobody has watched bite is not yet evidence
  </acceptance_criteria>
  <done>The guard now requires the name it used to forbid, its header narrates the current rule, and the deletion is pinned by assertions that have been observed to fail on reintroduction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install documentation → a user's machine | Prose determines what a user installs and what they run against a broker-managed emulator |
| CI honesty gate → the repository's own claims | A stale or half-moved guard lets a false claim ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Denial of Service | a user pointing a second debugger at a broker-managed instance | mitigate | The README states the one-holder rule positively and names the concrete traps (`nc`, a second Claude session, `-remotemonitor`, any 6502 debugger including regenerator2000's `--vice`), plus the fact that this plugin's own route refuses that flag in code (plan 10-01). Documentation is the only available control for a user's own separate process |
| T-10-15 | Repudiation | a licence claim that is not the true one | mitigate | D-14: the notice states the dual `MIT OR Apache-2.0` sourced from the crates.io licence field and Phase 9's recorded correction, rather than repeating the Apache-2.0-only claim two planning documents still carry |
| T-10-11 | Repudiation | the deletion silently returning | mitigate | Task 3's per-file regression block fails CI if `toacme`/`cmdDisasm`/`disasm` reappear in `acme-build`, and the mutation check proves the assertion bites |
| T-10-16 | Tampering | the honesty gate itself | mitigate | The gate reads files with `readFileSync` and matches by plain substring containment only — no regex built from file content, no `eval`, no spawn, matching the file's own standing rule (ASVS V5: untrusted content is never interpreted) |
| T-10-SC | Tampering | published tarball contents | mitigate | `node scripts/check-npm-packages.mjs` is re-run after the notices edit, asserting `THIRD-PARTY-NOTICES.md` is still packed and the runtime dependency set is unchanged |
</threat_model>

<verification>
1. `node scripts/check-skill-fork-honesty.mjs` exits 0.
2. `node scripts/check-skill-tool-coverage.mjs` exits 0.
3. `node scripts/check-npm-packages.mjs` exits 0.
4. `cd .claude/mcp/vice && npm test` is still green.
5. The three observed failures are recorded in the summary with their messages: the
   pre-Task-3 forbidden-substring failure, and both Task 3 mutation checks.
</verification>

<success_criteria>
- README names regenerator2000 as required, with every D-15 figure present verbatim and
  the two-project limit stated as documented-not-detected.
- The notices file carries the dual `MIT OR Apache-2.0` claim with re-checkable provenance.
- The honesty gate requires the name it used to forbid, and its header says so.
- A reintroduced `toacme` or `disasm` verb fails CI.
</success_criteria>

<output>
Create `.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-08-SUMMARY.md` when done.
</output>
