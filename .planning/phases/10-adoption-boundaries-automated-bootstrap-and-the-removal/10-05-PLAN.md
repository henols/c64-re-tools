---
phase: 10-adoption-boundaries-automated-bootstrap-and-the-removal
plan: 05
type: execute
wave: 3
depends_on: ["10-02", "10-04"]
files_modified:
  - .claude/mcp/vice/r2000-verify.ts
  - .claude/mcp/vice/r2000-verify.test.ts
  - .claude/mcp/vice/r2000-cli.ts
  - .claude/mcp/vice/package.json
  - .planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/evidence/10-verify-transcript.txt
autonomous: true
requirements: [R2000-06]
must_haves:
  truths:
    - "D-09: the reassembly proof leans on regenerator2000's own `--verify`, which spawns a real assembler and diffs — no hand-rolled export-assemble-diff harness was built"
    - "D-10: the check keys on the parsed ACME result line and never on the exit code — an `ACME … (skipped)` line is a FAILURE even when regenerator2000 exits 0, because that false pass was observed live"
    - "R2000-06: a `.prg` and a flat 64K capture each produce ACME source proven reassemblable by running a real assembler, recorded as a transcript rather than asserted"
    - "D-11: CI does not install regenerator2000 — absence is a named SKIP, and a hard FAIL only under `VICE_REQUIRE_R2000`, mirroring the `VICE_REQUIRE_ACME` availability-gate shape without ever touching Phase 4's protected `disasm-roundtrip.test.ts`"
    - "Criterion 3 precedes criterion 4 in fact, not just in wording: `--verify` implies `--headless`, so this proof runs against a project file the Node synthesiser produced"
  artifacts:
    - path: ".claude/mcp/vice/r2000-verify.ts"
      provides: "The `--verify` stdout parser and the ACME-specific verdict, plus verifyProject()"
      contains: "skipped"
    - path: ".claude/mcp/vice/r2000-verify.test.ts"
      provides: "Unit tests over both captured transcripts (honest pass and the exit-0 false pass) plus a gated live run"
      contains: "VICE_REQUIRE_R2000"
    - path: ".planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/evidence/10-verify-transcript.txt"
      provides: "This phase's own live `--verify` evidence for a .prg and a flat 64K image, since CI never runs it"
  key_links:
    - from: ".claude/mcp/vice/r2000-cli.ts"
      to: ".claude/mcp/vice/r2000-verify.ts"
      via: "the verify verb"
      pattern: "verifyProject"
    - from: ".claude/mcp/vice/r2000-verify.ts"
      to: ".claude/mcp/vice/r2000-launch.ts"
      via: "buildVerifyArgs + runR2000, the guarded spawn"
      pattern: "buildVerifyArgs"
---

<objective>
Prove the replacement disassembly route's output actually reassembles, by running a real
assembler — criterion 4's second half and `R2000-06`.

Per **D-09** (the user's own decision, with the "this is r2000 checking its own export"
caveat raised and accepted) the proof is regenerator2000's `--verify`, which already
spawns a real assembler and diffs byte for byte. Per **D-10** the check parses the ACME
result line and never trusts the exit code: with ACME absent and ca65 present,
regenerator2000 prints `✗ ACME — ACME not found in PATH (skipped)` followed by
`✓ All roundtrip verifications passed.` and **exits 0** — a false pass with ACME never
run, observed live. Criterion 4 is specifically about ACME matching this project's
`!cpu 6510` expectations, so a skipped ACME must be a failure. Per **D-11** CI does not
install regenerator2000 (a 4m48s–5m39s Rust build on every merge contradicts the
"cheapest by far" reason `--verify` was chosen), so the gate is a named SKIP by default
and a hard FAIL only under `VICE_REQUIRE_R2000`.

Purpose: this is what earns the deletion in plan 10-06. The `toacme` caveats existed
because a flat linear decode cannot produce reassemblable source; the replacement must be
shown to, not claimed to.

Output: `r2000-verify.ts`, `r2000-verify.test.ts`, the `verify` verb, and a live
transcript recorded in the phase's evidence directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-CONTEXT.md
@.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-PATTERNS.md
@.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: r2000-verify.ts — parse the per-assembler result lines, and make a skipped ACME a failure</name>
  <files>.claude/mcp/vice/r2000-verify.ts, .claude/mcp/vice/package.json</files>
  <read_first>
    - `.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-RESEARCH.md` § Code Examples — both captured `--verify` transcripts verbatim, including the exit-0 false pass
    - `.claude/mcp/vice/r2000-launch.ts` — `buildVerifyArgs`, `runR2000`, `R2000_BIN` (plan 10-01)
    - `.claude/mcp/vice/disasm-roundtrip.test.ts` lines 30-50 — the standing rule that an ACME stderr WARNING is not a failure, which applies to any warning text `--verify` relays
    - `.claude/mcp/vice/package.json` lines 10-62 — the `files[]` array this module must join
  </read_first>
  <action>
Create `.claude/mcp/vice/r2000-verify.ts` — the ONE place that interprets regenerator2000's
`--verify` output.

Header comment: state WHY parsing is required at all, quoting the concrete incident in
prose — with ACME absent and ca65 present, `--verify` reports the ACME line as
`(skipped)` and still prints `✓ All roundtrip verifications passed.` and exits 0. Say
plainly that trusting the exit code here would let the one assembler this project cares
about be silently skipped, and that this is the WHAT-NOT-TO-DO for any future edit.

Exports:
  - `type AssemblerOutcome = "ok" | "skipped" | "failed"`
  - `interface VerifyLine { assembler: string; outcome: AssemblerOutcome; detail: string }`
  - `function parseVerifyOutput(stdout: string): VerifyLine[]` — one entry per
    per-assembler result line. Recognise the success form (a leading `✓` with a
    `byte-identical` detail), the skipped form (a `(skipped)` suffix, typically
    `not found in PATH`), and treat any other non-summary result line as `failed`. The
    aggregate `All roundtrip verifications passed.` line is a summary, not an assembler
    line — exclude it, and say why in a comment. Match the assembler name
    case-insensitively (`ACME`, `ca65`, `64tass`, `KickAssembler`), and tolerate an
    em-dash or hyphen separator, since only the ACME verdict is load-bearing and the
    exact glyph is an upstream formatting detail.
  - `function acmeVerdict(lines: VerifyLine[]): { ok: boolean; reason: string }` — `ok`
    only when an ACME line exists with outcome `ok`. A missing ACME line, an ACME line
    with outcome `skipped`, and an ACME line with outcome `failed` each return
    `ok: false` with a distinct, quotable reason. Never conflate "skipped" with
    "passed" and never fall back to the summary line.
  - `function verifyProject(projectPath: string): { ok: boolean; reason: string; lines: VerifyLine[]; status: number | null; stdout: string; stderr: string }`
    — builds argv with `buildVerifyArgs()` and runs it through `runR2000()` (so the
    `--vice` scan applies here too), parses stdout, and derives `ok` from
    `acmeVerdict()` — deliberately NOT from `status`. Return the raw status and streams
    so a caller can print them, but never let a zero status alone make `ok` true. Do not
    treat a warning on stderr as failure.

Add `"r2000-verify.ts"` to `.claude/mcp/vice/package.json`'s `files[]` in the same commit
— it becomes reachable from `vice-proxy.ts` through `r2000-cli.ts` in Task 3, and
`scripts/check-npm-packages.mjs`'s transitive-closure walk fails the pack otherwise.

Import nothing from `hostpath.ts`/`containerpath.ts`; plan 10-01's absence assertion
already names `r2000-verify.ts`.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && npm run typecheck && grep -v '^\s*//' r2000-verify.ts | grep -c 'skipped'</automated>
  </verify>
  <acceptance_criteria>
    - `cd .claude/mcp/vice && npm run typecheck` exits 0
    - `grep -v '^\s*//' .claude/mcp/vice/r2000-verify.ts | grep -c 'skipped'` returns at least 2
    - `grep -c 'status === 0' .claude/mcp/vice/r2000-verify.ts` returns 0 — no code path derives `ok` from the exit status
    - `node -e "const p=require('./.claude/mcp/vice/package.json'); if(!p.files.includes('r2000-verify.ts')) process.exit(1)"` exits 0
    - `grep -c 'from "./hostpath.ts"\|from "./containerpath.ts"' .claude/mcp/vice/r2000-verify.ts` returns 0
  </acceptance_criteria>
  <done>A parser exists that reports ACME's own verdict, treats a skipped ACME as a failure, and never lets a zero exit code stand in for a passing assemble.</done>
</task>

<task type="auto">
  <name>Task 2: r2000-verify.test.ts — pin both captured transcripts, then prove it live under the D-11 gate</name>
  <files>.claude/mcp/vice/r2000-verify.test.ts, .planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/evidence/10-verify-transcript.txt</files>
  <read_first>
    - `.claude/mcp/vice/r2000-verify.ts` (Task 1)
    - `.claude/mcp/vice/disasm-roundtrip.test.ts` lines 58-120 — `probeAcme()`, the module-scope `SKIP_REASON`, the single always-running availability gate, and the "never a hand-rolled `if (!available) return`" rule. **Mirror the shape into the new file; do NOT edit, import from, or otherwise touch `disasm-roundtrip.test.ts`** — it is Phase 4's protected stock-disassembler round-trip test (ROADMAP § Standing Constraints: "Phase 4's disassembler stays … all ~55KB of tests are load-bearing") and shares only the word "disasm" with the verb this phase deletes
    - `.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-RESEARCH.md` § Code Examples — the two transcripts to embed as fixtures
    - `.github/workflows/ci.yml` lines 85-95 — the `VICE_REQUIRE_ACME: "1"` Test step, and the deliberate asymmetry that CI must NOT gain a `VICE_REQUIRE_R2000` equivalent (D-11)
  </read_first>
  <action>
Create `.claude/mcp/vice/r2000-verify.test.ts`.

Unit half — always runs, no binary needed. Embed both captured transcripts as string
fixtures, verbatim from RESEARCH.md § Code Examples:
  1. The honest-pass transcript (64tass skipped, ACME byte-identical, ca65 byte-identical,
     KickAssembler skipped): `parseVerifyOutput` returns four lines with the right
     outcomes, and `acmeVerdict` returns `ok: true`.
  2. **The trap transcript** (ACME skipped, summary says all passed, EXIT=0):
     `acmeVerdict` returns `ok: false` with a reason naming `skipped`. Give this test a
     title that states it is pinning a live-observed false pass, so nobody later
     "simplifies" the parser back to an exit-code check.
  3. A synthetic failed-ACME line yields `ok: false` with a reason distinct from the
     skipped one.
  4. Output with no ACME line at all yields `ok: false` — absence is not success.
  5. The summary line `✓ All roundtrip verifications passed.` is not parsed as an
     assembler entry.

Gated half — mirrors D-11's shape, with `probeR2000()`, a module-scope `SKIP_REASON`,
`{ skip: SKIP_REASON }` on each dependent test, and exactly ONE availability-gate test
that always runs and asserts only when `process.env.VICE_REQUIRE_R2000` is set:
  6. Synthesise a project from a small illegal-opcode `.prg` (reuse `r2000-project.ts`),
     run `verifyProject()`, and assert `ok === true` and that the ACME line's outcome is
     `ok` — a live, real-assembler proof for `R2000-06`'s `.prg` half.
  7. The same for a flat 64K image, which is the other input `R2000-06` names by
     wording ("a `.prg` **or flat 64K capture**") and the one `c64-ram-capture` produces.
     If a full-64K verify is slow, keep the payload realistic but bound the subprocess
     with an explicit timeout rather than skipping the case.

Then record this phase's own live evidence, because CI will never run these two tests:
write `.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/evidence/10-verify-transcript.txt`
containing the actual, unedited stdout of both live runs plus their exit codes, the
regenerator2000 and ACME versions, and the date. This is the artifact criterion 4's
"proven by running a real assembler, not asserted" claim rests on.

Do NOT add a `VICE_REQUIRE_R2000` env var to `.github/workflows/ci.yml`. The asymmetry
with `VICE_REQUIRE_ACME` is deliberate (D-11), not an oversight to tidy up — record that
in a comment beside the availability gate.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && node --test r2000-verify.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd .claude/mcp/vice && node --test r2000-verify.test.ts` exits 0 on this host with 0 skipped tests
    - `R2000_BIN=definitely-not-installed-r2000 node --test r2000-verify.test.ts` exits 0, reports tests 6 and 7 as SKIPPED, and still runs the availability gate
    - `VICE_REQUIRE_R2000=1 R2000_BIN=definitely-not-installed-r2000 node --test r2000-verify.test.ts` exits non-zero naming `VICE_REQUIRE_R2000`
    - `grep -c 'VICE_REQUIRE_R2000' .github/workflows/ci.yml` returns 0
    - `git diff --name-only` shows no change to `disasm-roundtrip.test.ts`, `disasm-opcodes.ts`, `disasm-decoder.ts`, `disasm-renderer.ts`, `stock-disassemble.ts`
    - The evidence file contains a line matching `ACME` with `byte-identical` for both the `.prg` run and the 64K run, plus both exit codes
  </acceptance_criteria>
  <done>The false-pass transcript is pinned as a failing case, ACME really assembled the exported source on this host for both input shapes, and the evidence is on disk because CI will never reproduce it.</done>
</task>

<task type="auto">
  <name>Task 3: wire the verify verb into the CLI</name>
  <files>.claude/mcp/vice/r2000-cli.ts</files>
  <read_first>
    - `.claude/mcp/vice/r2000-cli.ts` — the verb dispatch and usage block from plan 10-04
    - `.claude/mcp/vice/r2000-verify.ts` (Task 1)
    - `.claude/mcp/vice/r2000-cli.test.ts` — the existing CLI test file, to extend rather than duplicate
  </read_first>
  <action>
Add a third verb, `verify <input-or-project>`, to `r2000-cli.ts`, following the same
shape as `export-asm`: accept either a `.regen2000proj` or a raw input (bootstrapping to
a temp project first), call `verifyProject()`, print each parsed assembler line, and
return 0 only when `acmeVerdict` is `ok`. On failure print the reason verbatim —
especially the `skipped` reason, which must read as a failure and not as an absence.

Extend the usage block with the new verb, and add one test to
`.claude/mcp/vice/r2000-cli.test.ts` (gated the same way as its existing regenerator2000
test) asserting `verify` on a bare `.prg` returns 0 on this host, and one always-running
test asserting `verify --help`-level usage text names the verb.

Update the comment above the verb table to say that `verify` is the criterion-4 proof
route and that its verdict comes from the parsed ACME line, never the child exit code
(D-10) — so a future maintainer reading only the CLI does not reintroduce the shortcut.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && node --test r2000-cli.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd .claude/mcp/vice && node --test r2000-cli.test.ts` exits 0
    - `node .claude/mcp/vice/vice-proxy.ts r2000 --help` lists three verbs: `bootstrap`, `export-asm`, `verify`
    - `node .claude/mcp/vice/vice-proxy.ts r2000 verify <a synthesised .prg>` exits 0 on this host and prints an ACME line containing `byte-identical`
    - `grep -c 'verifyProject' .claude/mcp/vice/r2000-cli.ts` returns at least 1
  </acceptance_criteria>
  <done>The replacement route a skill will document can bootstrap, export ACME source, and prove that source reassembles — three verbs, one seam, no human.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| regenerator2000 stdout → `parseVerifyOutput` | An external tool's human-formatted output is interpreted as a security-relevant verdict |
| `verifyProject` → regenerator2000 → ACME | Two nested subprocess layers, both spawned with argv arrays |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Spoofing | `--verify`'s exit code as a verdict | mitigate | A zero exit with ACME skipped is a false pass observed live. `acmeVerdict()` derives the result only from the parsed ACME line, `grep -c 'status === 0'` is asserted to be 0, and the trap transcript is pinned as a unit test (D-10) |
| T-10-02 | Tampering | subprocess invocation | mitigate | `verifyProject` spawns only through `r2000-launch.ts`'s argv builders and `runR2000`, which applies the `--vice` scan first; no shell string anywhere. The nested ACME spawn is regenerator2000's own, out of this project's control and accepted as part of D-09's decision |
| T-10-06 | Denial of Service | the gated live tests | mitigate | Explicit subprocess timeouts, `finally`-scoped temp cleanup, and the file stays out of `MANUAL_ONLY_TESTS` so it must terminate. Absent regenerator2000 produces a named SKIP, never a hang |
| T-10-10 | Repudiation | "verified by running a real assembler" as a claim | mitigate | CI never runs this gate (D-11), so the claim is backed by a committed transcript (`evidence/10-verify-transcript.txt`) carrying raw stdout, exit codes and tool versions, rather than by a green tick nobody can re-read |
| T-10-SC | Tampering | npm/cargo installs | accept | No new dependency; regenerator2000 stays out of CI by design, and its Phase 9 provenance record (crates.io publisher `ricardoquesada`, `MIT OR Apache-2.0`) is unchanged |
</threat_model>

<verification>
1. `cd .claude/mcp/vice && npm test` — full suite green.
2. `node scripts/check-npm-packages.mjs` exits 0 with `r2000-verify.ts` declared.
3. The three env-var permutations from Task 2's acceptance criteria are each run and
   their outcomes recorded in the summary.
4. `evidence/10-verify-transcript.txt` is committed and quoted in the summary — the
   ACME line for both the `.prg` and the flat-64K run.
</verification>

<success_criteria>
- ACME's own verdict, parsed from the result line, decides the outcome; a skipped ACME
  fails even on exit 0, and a test proves it.
- A `.prg` and a flat 64K capture both produce ACME source that a real ACME reassembled
  byte-identically, with the transcript on disk.
- CI stays free of a regenerator2000 install and of `VICE_REQUIRE_R2000`.
- No Phase 4 disassembler file was touched.
</success_criteria>

<output>
Create `.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-05-SUMMARY.md` when done.
</output>
