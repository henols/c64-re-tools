---
phase: 14-backend-decision
plan: 04
type: execute
wave: 2
depends_on: ["14-01"]
worktree: false
files_modified:
  - .claude/mcp/vice/backend-detect.mts
  - .claude/mcp/vice/resources/backend-detect.mjs
  - .claude/mcp/vice/backend-detect.test.ts
  - .claude/mcp/vice/vice-errors.ts
  - .claude/mcp/vice/vice.ts
  - .claude/mcp/vice/package.json
autonomous: true
requirements: [FORK-01]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "On a non-retain branch, an indeterminate --help probe no longer silently lands on the fork backend"
    - "On a non-retain branch, resolving the fork backend emits a one-time notice naming FORK-01 as the decision that resolved to withdraw it"
    - "The committed compiled host mirror resources/backend-detect.mjs matches its .mts source, so resources-sync.test.ts stays green"
    - "On the remove-now branch, ViceError, MachineRestartedError, readEpoch, mcpHost and ToolInfo live in a module that survives deleting the fork transport, and every existing importer keeps compiling unchanged"
    - "On the retain branch this plan makes zero source edits, and that zero is recorded as the decided outcome rather than an omission"
  artifacts:
    - path: ".claude/mcp/vice/backend-detect.mts"
      provides: "resolvedBackend() whose indeterminate fallback reflects the FORK-01 decision"
      contains: "resolvedBackend"
    - path: ".claude/mcp/vice/vice-errors.ts"
      provides: "The shared error types and utilities the stock path needs, extracted out of the fork transport module (remove-now branch only)"
      exports: ["ViceError", "MachineRestartedError", "readEpoch", "mcpHost"]
  key_links:
    - from: ".claude/mcp/vice/resources/backend-detect.mjs"
      to: ".claude/mcp/vice/backend-detect.mts"
      via: "node build.ts compiles the host-bound .mts into the committed .mjs mirror; resources-sync.test.ts fails CI on drift"
      pattern: "backend-detect"
    - from: ".claude/mcp/vice/vice.ts"
      to: ".claude/mcp/vice/vice-errors.ts"
      via: "vice.ts re-exports the extracted symbols so all 29 existing importers keep compiling with no edit"
      pattern: "vice-errors"
  prohibitions:
    - statement: "A change to the resolved-backend default must not reach npm consumers as a silent patch release — a behavioural default flip is a deliberate release event, not a side effect of merging"
      status: resolved
      verification: judgment
    - statement: "The shared error types and utilities the stock backend depends on must not be deleted, relocated behind a fork-only import, or duplicated into a second copy in the course of preparing the fork transport for removal"
      status: resolved
      verification: judgment
---

<objective>
Make the code state reflect the FORK-01 decision, on the branches where the
decision has a code consequence.

Purpose: ROADMAP Phase 14 criterion 3 asks for the decision to be visible in the
code, not only in a document. On `retain` there is nothing to change and the plan
records a deliberate zero. On `deprecate-first` the fork stops being the silent
default — research and the standing todo both identify `resolvedBackend()`'s
indeterminate fallback as the mechanism by which the fork is retained *by default*
for anyone who has not read the README, since `.mcp.json` sets no `VICE_BACKEND`.
On `remove-now` the same flip applies, plus the one preparatory refactor research
names as mandatory before any deletion: `vice.ts` is not a separable fork module,
and the shared symbols must move out of it first.
Output: a branch-appropriate change to `backend-detect.mts` and its compiled
mirror, and on `remove-now` a new `vice-errors.ts`.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-backend-decision/14-RESEARCH.md
@.planning/phases/14-backend-decision/14-PATTERNS.md
@.planning/phases/14-backend-decision/14-01-SUMMARY.md
@.planning/todos/pending/2026-08-20-fully-remove-the-forked-vice-mcp-backend.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stop the fork being the silent default (deprecate-first and remove-now branches only)</name>
  <reversibility rating="costly">Flipping the indeterminate-probe default changes observable behaviour for any npm consumer whose environment does not answer the `--help` discriminator, so undoing it means another published release rather than a local edit.</reversibility>
  <precondition>`.planning/phases/14-backend-decision/14-01-SUMMARY.md` records a branch token; halt if none is recorded, and skip this task entirely if the token is `retain`.</precondition>
  <files>.claude/mcp/vice/backend-detect.mts, .claude/mcp/vice/resources/backend-detect.mjs, .claude/mcp/vice/backend-detect.test.ts</files>
  <read_first>
    - `.planning/phases/14-backend-decision/14-01-SUMMARY.md` → `## Decision` — the branch token. If it is `retain`, this task is skipped, visibly.
    - `.claude/mcp/vice/backend-detect.mts` — `resolvedBackend()`'s indeterminate branch around lines 500-510, which currently logs a note and returns `{ backend: "fork", source: "indeterminate", ... }`; the header comment around lines 435-445 that documents *why* `"fork"` was chosen there; `classifyHelpOutput()` at line 83; `probeBackend()` at line 145; the `VICE_BACKEND` override handling at line 464; and the on-disk capability-cache read/write around lines 250-260 and 512-520
    - `.claude/mcp/vice/backend-detect.test.ts` — the tests that exercise the fork-fallback branch directly (it carries 28 fork references at plan time); these encode the current default and must be updated, not deleted
    - `.claude/mcp/vice/build.ts` — the compiler for host-bound `.mts` sources, and its assertion that the emitted file set exactly matches the expected list
    - `.claude/mcp/vice/resources-sync.test.ts` — the drift guard that fails CI when a committed `resources/*.mjs` does not match its `.mts` source
    - `.planning/PROJECT.md` → the `FORK-01` Key Decisions row written by 14-01, which the new notice cites by id
    - `README.md` lines 72-157 — the backend-choice section, whose description of the fallback becomes wrong the moment the default flips
    - `CLAUDE.md` → the Tech-stack constraint that host-bound `.mts` files must be compiled by `build.ts` into committed `resources/*.mjs`
  </read_first>
  <action>
    Branch condition, first: if `14-01-SUMMARY.md` records `retain`, make no edit.
    Write the skip and its reason into the plan SUMMARY — a recorded skip, never a
    silent one — and stop.

    On `deprecate-first` or `remove-now`, make two changes to
    `.claude/mcp/vice/backend-detect.mts`:

    1. Flip the indeterminate branch's returned backend from `"fork"` to
       `"stock"`, keeping `source: "indeterminate"` and the existing
       `binPathFields(...)` spread untouched. Rewrite the accompanying `note`
       string so it still names both discriminators and still tells the reader how
       to override explicitly, and additionally states that the fallback now
       resolves to stock because a dated decision resolved to withdraw the fork
       backend, citing `FORK-01` by that literal id. Update the header comment
       that explains why `"fork"` was the historical choice so it records the flip
       rather than contradicting the code.

    2. Add a one-time notice, emitted the first time `resolvedBackend()` returns
       the fork backend from any source (explicit `VICE_BACKEND` override, probe
       verdict, or cache hit), saying that the fork backend is retained but
       resolved-to-be-withdrawn and naming `FORK-01`. Guard it with a
       module-level `warnedForkDeprecated` boolean so it fires once per process,
       following the exact shape of the existing one-time stderr warning in this
       module tree (`warnedRemoteMonitorBindWidened` in `broker-launch.mts` is the
       reference implementation — a module-level flag, a `process.stderr.write`,
       no throw). It must never throw and never change the returned value.

    Two hard constraints on the notice text: do not put a numbered phase
    reference on the same line as any of the words `deferred`, `not yet`,
    `until`, or `unavailable` — that pairing is what
    `scripts/check-skill-fork-honesty.mjs`'s stale-forward-reference rule fails on
    — and do not introduce a string literal naming a phase number at all, because
    `backend-detect.mts` ships in `package.json`'s `files[]` and
    `docs-dangling-refs.test.ts` scans shipped module string literals for exactly
    that. Name the decision by its requirement id.

    Then update `.claude/mcp/vice/backend-detect.test.ts` so the tests assert the
    new behaviour: the indeterminate verdict resolves to stock, the note names
    both discriminators and `FORK-01`, an explicit `VICE_BACKEND=fork` override
    still resolves to the fork, and the deprecation notice fires exactly once per
    process rather than on every call. Do not delete a test to make the suite
    pass; change what it expects.

    Rebuild the committed host mirror with `node build.ts` from
    `.claude/mcp/vice`, and commit `resources/backend-detect.mjs` alongside the
    source — `resources-sync.test.ts` fails CI on drift.

    Finally, note in the plan SUMMARY that this is a behavioural default change to
    a published module: every merge to `main` auto-publishes a patch unless the
    commit subject carries `[skip release]`, so the commit for this task must
    carry that marker and plan 14-05 records the deliberate release handling. Do
    not edit `README.md` here — plan 14-05 owns the documentation sweep for the
    decision's consequences, and `README.md` is not in this plan's
    `files_modified`.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice &amp;&amp; npx tsc --noEmit -p tsconfig.json &amp;&amp; node build.ts &amp;&amp; node --test backend-detect.test.ts resources-sync.test.ts docs-dangling-refs.test.ts &amp;&amp; node test-gate.mjs</automated>
  </verify>
  <acceptance_criteria>
    - On branch `retain`: `git diff --quiet -- .claude/mcp/vice` exits 0 and the SUMMARY records the skip with its reason
    - On a non-`retain` branch: `cd .claude/mcp/vice && npx tsc --noEmit -p tsconfig.json` exits 0
    - On a non-`retain` branch: `cd .claude/mcp/vice && node --test backend-detect.test.ts` exits 0, and the file contains a test asserting the indeterminate verdict resolves to the stock backend
    - On a non-`retain` branch: `grep -c 'FORK-01' .claude/mcp/vice/backend-detect.mts` returns at least 1
    - On a non-`retain` branch: a test asserts the deprecation notice is emitted once and not twice across two consecutive resolutions in the same process
    - `cd .claude/mcp/vice && node build.ts && node --test resources-sync.test.ts` exits 0, and `git status --porcelain .claude/mcp/vice/resources` is empty after the rebuild is committed
    - `cd .claude/mcp/vice && node --test docs-dangling-refs.test.ts` exits 0
    - `cd .claude/mcp/vice && node test-gate.mjs` exits 0 (full automated gate, not just the touched files)
    - `node scripts/check-npm-packages.mjs` exits 0
    - The commit subject for this task contains the literal `[skip release]`
  </acceptance_criteria>
  <done>On a non-retain branch the fork is no longer what an undetermined environment silently gets, resolving it says so once with the deciding requirement named, the compiled host mirror matches its source, and the release path is handled deliberately. On retain, nothing changed and the record says why.</done>
</task>

<task type="auto">
  <name>Task 2: Move the stock backend's shared error types and utilities out of the fork transport module (remove-now branch only)</name>
  <reversibility rating="reversible">A pure extraction behind a re-export barrel: no symbol is renamed, no importer moves, and reverting is a single file merge back into `vice.ts`.</reversibility>
  <precondition>`.planning/phases/14-backend-decision/14-01-SUMMARY.md` records the branch token `remove-now`; skip this task entirely for `retain` or `deprecate-first`.</precondition>
  <files>.claude/mcp/vice/vice-errors.ts, .claude/mcp/vice/vice.ts, .claude/mcp/vice/package.json</files>
  <read_first>
    - `.planning/phases/14-backend-decision/14-01-SUMMARY.md` → `## Decision` — the branch token
    - `.claude/mcp/vice/vice.ts` in full — specifically `mcpHost()` at line 76, `EPOCH_FILE` at line 88, `ViceErrorOptions` at line 245 and `ViceError` at line 250, `MachineRestartedErrorOptions` at line 262 and `MachineRestartedError` at line 277, `EpochResult` at line 508 and `readEpoch()` at line 530, `ToolInfo` at line 739, and the fork-only surface that stays behind: `DENY_LIST` at 201, `denyListRefusalMessage()` at 229, `useInstance()` at 155, `activeInstance()` at 170, `beginSession()` at 566, `assertSameMachine()` at 618, `call()` at 697, `serverInfo()` at 766
    - The importer set, re-derived from the tree rather than trusted from this plan: at plan time 29 files import from `./vice.ts`, of which 14 are production modules that need only the shared symbols (`stock-condition.ts`, `stock-recycle.ts`, `stock-connect.ts`, `stock-diagnose.ts`, `stock-dispatch.ts`, `stock-address.ts`, `stock-derived.ts`, `stock-handler.ts`, `stock-paths.ts`, `stock-petscii.ts`, `stock-timing.ts`, `stock-symbols.ts`, `stock-protocol.ts`, `vice-broker-client.ts`) and none of which uses `call()`
    - `.claude/mcp/vice/load-order.test.ts` and `scripts/check-npm-packages.mjs` — both assert that every locally-reachable module is an entry in `package.json`'s `files[]`, so a new runtime module must be added there or the published tarball throws a module-not-found at import time
    - `.claude/mcp/vice/repo-root.ts` and `.claude/mcp/vice/container-guard.mts` — the two imports `mcpHost()` and `readEpoch()` depend on; `repo-root.ts`'s own header documents a deliberately avoided module cycle, so the new module must not import anything that imports it back
  </read_first>
  <action>
    Branch condition, first: skip entirely unless the recorded token is
    `remove-now`, and record the skip and its reason in the plan SUMMARY.

    Research Pitfall 1 is the whole reason this task exists: `vice.ts` is
    simultaneously the fork's HTTP transport and the home of the error types and
    utilities the stock backend depends on. Deleting it wholesale breaks a dozen
    unrelated stock modules at typecheck time. This task removes that coupling
    *before* any deletion, and deletes nothing.

    Create `.claude/mcp/vice/vice-errors.ts` and move — not copy — these symbols
    into it, unchanged in name, signature and behaviour: `mcpHost()`,
    `EPOCH_FILE`, `ViceErrorOptions`, `ViceError`, `MachineRestartedErrorOptions`,
    `MachineRestartedError`, `EpochResult`, `readEpoch()`, and `type ToolInfo`.
    Give the file a header in this project's style stating why it exists — that
    these symbols are shared with the stock backend and must outlive the fork
    transport — and that it is the one home for them, so a future deletion of
    `call()` and its dependents is a local operation.

    Then make `.claude/mcp/vice/vice.ts` import from `./vice-errors.ts` and
    re-export every moved symbol under its existing name, so all 29 current
    importers keep compiling with zero edits. This barrel is deliberate, not
    transitional: re-pointing 29 import sites is churn the deletion does not
    require, and leaving `vice.ts` as the stable public name for these symbols is
    what makes the extraction a no-risk step. Say so in a comment, so a later
    session does not "finish" the migration by mass-editing importers.

    Add `"vice-errors.ts"` to `package.json`'s `files[]`, positioned next to
    `vice.ts`, since it is now reachable at runtime from every shipped stock
    module.

    Watch two committed tripwires: `docs-linerefs.test.ts` mechanically checks
    CLAUDE.md's two `rewriteArguments()` line citations against `vice-proxy.ts`,
    and any edit that shifts those lines reds it — this task does not touch
    `vice-proxy.ts`, so if that guard goes red, the cause is drift to re-verify
    and re-pin, per CLAUDE.md's own instruction. And do not introduce a string
    literal naming a phase number in the new module: it ships in `files[]` and
    `docs-dangling-refs.test.ts` scans for exactly that.

    Do not delete `call()`, `vice-probe.ts`, `vice-sync.ts`,
    `refresh-manifest.ts`, `fork-manifest-surface.test.ts` or
    `tools-manifest.json` in this task or this phase. That deletion is the
    one-way step, it breaks a published 62-tool contract, and plan 14-05 records
    it as the follow-on phase's owned work.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice &amp;&amp; npx tsc --noEmit -p tsconfig.json &amp;&amp; node --test load-order.test.ts docs-linerefs.test.ts docs-dangling-refs.test.ts &amp;&amp; node test-gate.mjs</automated>
  </verify>
  <acceptance_criteria>
    - On branch `retain` or `deprecate-first`: no `vice-errors.ts` is created, `git diff --quiet -- .claude/mcp/vice/vice.ts .claude/mcp/vice/package.json` exits 0, and the SUMMARY records the skip with its reason
    - On branch `remove-now`: `cd .claude/mcp/vice && npx tsc --noEmit -p tsconfig.json` exits 0 with no import edited outside `vice.ts`
    - On branch `remove-now`: `git diff --name-only` for this task lists exactly `vice-errors.ts`, `vice.ts` and `package.json` under `.claude/mcp/vice/` — proof that no importer needed re-pointing
    - On branch `remove-now`: each of `ViceError`, `MachineRestartedError`, `readEpoch`, `mcpHost` and `ToolInfo` is defined in `vice-errors.ts` and re-exported from `vice.ts`, verified by a node one-liner importing each name from both module paths and asserting identity
    - On branch `remove-now`: `node -e "process.exit(require('./.claude/mcp/vice/package.json').files.includes('vice-errors.ts')?0:1)"` exits 0
    - `cd .claude/mcp/vice && node test-gate.mjs` exits 0 — the full automated gate, unchanged pass count relative to the pre-task baseline recorded in the SUMMARY
    - `cd .claude/mcp/vice && node --test load-order.test.ts docs-linerefs.test.ts docs-dangling-refs.test.ts` exits 0
    - `node scripts/check-npm-packages.mjs` exits 0
    - No file was deleted: `git diff --diff-filter=D --name-only` for this task is empty
  </acceptance_criteria>
  <done>On the remove-now branch the stock backend no longer depends on the fork transport's module for its error types and utilities, every existing importer compiles untouched, and the eventual deletion of the transport is a local change instead of a 29-file break.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → published npm tarball | `backend-detect.mts` and any new runtime module ship in `@henols/vice-mcp`; a behavioural default flip reaches consumers on the next publish |
| `.mts` source → committed `resources/*.mjs` mirror | The host broker runs the compiled copy, not the source; a stale mirror means the host behaves differently from the code under test |
| environment → backend selection | An indeterminate `--help` probe decides which emulator protocol the whole server speaks; the fallback is a security-relevant default because the two backends expose different tool surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-14 | Tampering | Published-package behaviour | high | mitigate | The default flip's commit carries `[skip release]` and plan 14-05 records the deliberate release handling, so a behavioural change cannot reach consumers as an incidental patch bump |
| T-14-15 | Repudiation | `resources/backend-detect.mjs` drift | medium | mitigate | `node build.ts` is run and the mirror committed in the same change; `resources-sync.test.ts` is an acceptance gate, and `git status --porcelain` over `resources/` must be empty afterwards |
| T-14-16 | Denial of Service | The one-time deprecation notice | medium | mitigate | The notice is guarded by a module-level flag, writes to stderr, never throws and never alters the returned value — copying `broker-launch.mts`'s existing one-time-warning shape rather than inventing one |
| T-14-17 | Tampering | Shared error types during extraction | high | mitigate | Symbols are moved, not copied, and re-exported under their existing names; an acceptance criterion asserts identity across both module paths and that exactly three files changed, so a silent second copy or a renamed symbol fails |
| T-14-18 | Information Disclosure | `package.json` `files[]` | low | mitigate | The new module is added to `files[]`; `load-order.test.ts` and `check-npm-packages.mjs` both gate on reachability, so a missing entry fails instead of shipping a tarball that throws at import |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages; the package-legitimacy gate does not fire |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 14-04)

Branch-conditional throughout. On `retain` this plan creates nothing.

- Flipped indeterminate-probe default and one-time deprecation notice in `.claude/mcp/vice/backend-detect.mts`, plus the rebuilt `.claude/mcp/vice/resources/backend-detect.mjs` mirror (**`deprecate-first` and `remove-now` branches only**)
- `warnedForkDeprecated` (new module-level flag in `backend-detect.mts`) (**`deprecate-first` and `remove-now` only**)
- New assertions in `.claude/mcp/vice/backend-detect.test.ts` for the flipped default and the once-per-process notice (**`deprecate-first` and `remove-now` only**)
- `.claude/mcp/vice/vice-errors.ts` (new module holding `mcpHost`, `EPOCH_FILE`, `ViceErrorOptions`, `ViceError`, `MachineRestartedErrorOptions`, `MachineRestartedError`, `EpochResult`, `readEpoch`, `ToolInfo`) (**`remove-now` branch only**)
- `"vice-errors.ts"` added to `.claude/mcp/vice/package.json` `files[]` (**`remove-now` branch only**)

Nothing is deleted on any branch in this plan.
</artifacts_produced>

<verification>
- `cd .claude/mcp/vice && npx tsc --noEmit -p tsconfig.json` exits 0
- `cd .claude/mcp/vice && node test-gate.mjs` exits 0 with a pass count at or above the pre-plan baseline
- `cd .claude/mcp/vice && node build.ts && node --test resources-sync.test.ts` exits 0 with an empty `git status --porcelain` over `resources/`
- `cd .claude/mcp/vice && node --test backend-detect.test.ts load-order.test.ts docs-linerefs.test.ts docs-dangling-refs.test.ts` exits 0
- `node scripts/check-npm-packages.mjs` exits 0
- `git diff --diff-filter=D --name-only` across this plan is empty — nothing was deleted
</verification>

<success_criteria>
- The code state matches the FORK-01 decision on the branch chosen, or the absence of a code change is a recorded decision rather than an omission
- The fork stops being the silent default on any branch that resolved to withdraw it
- The stock backend's dependence on the fork transport's module is removed before any deletion is attempted, with every importer untouched
- No published-behaviour change reaches consumers as an incidental patch release
</success_criteria>

<output>
Create `.planning/phases/14-backend-decision/14-04-SUMMARY.md` when done. It MUST
state the branch token, which tasks ran and which were skipped with their
reasons, the `node test-gate.mjs` pass counts before and after, and — on a
non-`retain` branch — that the commit carried the release-skip marker.
</output>
