---
phase: 14-backend-decision
plan: 01
type: execute
wave: 1
depends_on: []
worktree: false
files_modified:
  - .planning/PROJECT.md
  - .claude/mcp/vice/docs-fork-decision.test.ts
  - .planning/phases/14-backend-decision/14-DECISION-BRIEF.md
  - .planning/phases/14-backend-decision/COVERAGE.md
autonomous: false
requirements: [FORK-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "PROJECT.md's `## Key Decisions` table carries exactly one row whose Decision cell names FORK-01"
    - "That row states an ISO date, at least one named reversal trigger, and the literal KEYBOARD_MATRIX_SET"
    - "The FORK-01 co-occurrence guard matches KEYBOARD_MATRIX_SET case-sensitively on the canonical upper-case opcode spelling, and its failure message says so — a lower-case-only mention fails the guard rather than passing it"
    - "PROJECT.md's `## Out of Scope` bullet about the fork backend cites FORK-01 instead of asserting a retention that the new row may have superseded"
    - "The branch chosen (retain / deprecate-first / remove-now) is recorded by a human at a blocking checkpoint, not inferred by an executor"
  artifacts:
    - path: ".claude/mcp/vice/docs-fork-decision.test.ts"
      provides: "Mechanical guard that PROJECT.md's FORK-01 row is present, dated, carries a reversal trigger, and names KEYBOARD_MATRIX_SET"
      min_lines: 60
      contains: "KEYBOARD_MATRIX_SET"
    - path: ".planning/phases/14-backend-decision/14-DECISION-BRIEF.md"
      provides: "The one document the human reads at the checkpoint: both branches' costs, the three options, the two open questions, the reversal-criteria draft"
      min_lines: 60
    - path: ".planning/phases/14-backend-decision/COVERAGE.md"
      provides: "Reasoned no-external-API-integration declaration for the api-coverage seal gate"
    - path: ".planning/PROJECT.md"
      provides: "The dated FORK-01 Key Decisions row"
      contains: "FORK-01"
  key_links:
    - from: ".claude/mcp/vice/docs-fork-decision.test.ts"
      to: ".planning/PROJECT.md"
      via: "readFileSync of the real PROJECT.md, isolating the Key Decisions row containing FORK-01"
      pattern: "PROJECT\\.md"
    - from: ".planning/PROJECT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "the FORK-01 row folds UP-01's reversal-criteria sentence in as the entry itself rather than a forward reference"
      pattern: "KEYBOARD_MATRIX_SET"
  prohibitions:
    - statement: "The recorded decision must not be a restatement of the status quo dressed as a decision — a `retain` outcome must be chosen on weighed evidence with named reversal criteria, never by omission or by whichever branch was cheaper to plan"
      status: resolved
      verification: judgment
    - statement: "The reversal criteria must not overstate what an upstream KEYBOARD_MATRIX_SET landing would buy — it must not imply that SID read-back or RESTORE/NMI are closed by that opcode"
      status: resolved
      verification: judgment
---

<objective>
Answer the fork-backend question with a dated decision, and make that answer
mechanically checkable.

This plan is the phase's tracer: it assembles the evidence a human needs, stops
at a blocking checkpoint for the actual call, writes the dated FORK-01 row into
PROJECT.md, and proves that row with the one guard that reads it back — green
before any expansion to the three hard-loss routes (14-02), the live criterion-3
exercise (14-03), the code consequence (14-04), or the ledger close (14-05).

Purpose: FORK-01 exists because this project has answered the fork question by
default at two milestone closes. A third default carry is the failure mode. The
deliverable is a *decision*, either way — REQUIREMENTS.md's Out-of-Scope table
names "Removing the fork backend without a recorded decision" as out of scope.
Output: `14-DECISION-BRIEF.md`, `COVERAGE.md`, a new `FORK-01` row in
PROJECT.md's `## Key Decisions`, and `docs-fork-decision.test.ts`.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-backend-decision/14-RESEARCH.md
@.planning/phases/14-backend-decision/14-PATTERNS.md
@.planning/phases/14-backend-decision/14-VALIDATION.md
@.planning/todos/pending/2026-08-20-fully-remove-the-forked-vice-mcp-backend.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble the FORK-01 decision brief (and the api-coverage declaration)</name>
  <files>.planning/phases/14-backend-decision/14-DECISION-BRIEF.md, .planning/phases/14-backend-decision/COVERAGE.md</files>
  <read_first>
    - `.planning/phases/14-backend-decision/14-RESEARCH.md` — Q1 (code-surface inventory), Q2 (what remove costs), Q3 (what retain costs and the cheapest real exercise), Q4 (the three routes verbatim), Q5 (where two artifacts disagree), Q6 (the KEYBOARD_MATRIX_SET coupling), Q8 (downstream phases), Open Questions 1 and 2, Assumptions Log A1/A2/A3
    - `.planning/todos/pending/2026-08-20-fully-remove-the-forked-vice-mcp-backend.md` — the standing todo, including its own unanswered open question
    - `.planning/REQUIREMENTS.md` — FORK-01, FORK-02, the `UP-01` Future Requirement sentence, and the Out of Scope row "Removing the fork backend without a recorded decision"
    - `.planning/PROJECT.md` lines 104-118 (`## Out of Scope`, including the "Removing or deprecating the fork backend" bullet at line 109) and lines 253-278 (`## Key Decisions`, the three-column table)
    - `.planning/ROADMAP.md` Phase 14 section (goal, the three success criteria) plus the Phase 15/16/17 sections that sequence after it
  </read_first>
  <action>
    Write `.planning/phases/14-backend-decision/14-DECISION-BRIEF.md` — the single
    document the human reads at Task 2's checkpoint. Do NOT recommend an outcome;
    present the evidence and the consequences. Required sections, in this order:

    1. `## The question` — quote FORK-01's requirement text and ROADMAP Phase 14's
       three success criteria verbatim.
    2. `## The three options` — name them exactly `retain`, `deprecate-first`,
       `remove-now`, because Task 2's resume signal and every downstream plan's
       branch condition use those three literal tokens. For each: what gets
       recorded, what code changes in this phase, which of the three success
       criteria it satisfies in-phase, and which it does not.
       - `retain`: the fork stays the default; PROJECT.md's line-109 Out-of-Scope
         bullet is formalised with reversal criteria instead of superseded. Code
         change: none. Criterion 3 satisfied by 14-03's live exercise.
       - `deprecate-first`: the decision recorded is "remove, one release later";
         `resolvedBackend()`'s indeterminate-probe fallback flips from `fork` to
         `stock` and a one-time deprecation notice is emitted (14-04). Criterion 3
         satisfied in-phase (the fork path still exists and is live-exercised).
       - `remove-now`: full deletion. State plainly that this phase's plan set
         CANNOT satisfy criterion 3's remove clause in-phase, with the sizing
         evidence from research Q1/Q2 — ~15 production modules, 23 test files
         carrying between 1 and 53 fork-string mentions each, 4 skill playbooks,
         `README.md`, `docs/stock-vice-parity.md`, and a premise rewrite (not a
         parameter tweak) of the 505-line `scripts/check-skill-fork-honesty.mjs`.
         State that choosing it means the orchestrator must insert a follow-on
         phase and that Phase 14 seals with that criterion recorded unmet.
    3. `## What removal costs` — the published-package-contract finding: 62 named
       tools in `tools-manifest.json` are what `@henols/vice-mcp` advertises,
       `scripts/check-npm-packages.mjs` asserts that file's presence in the pack,
       every merge to `main` auto-publishes a patch unless the commit subject
       carries `[skip release]`, so a breaking removal needs a deliberate
       major-version release. Plus research Pitfall 1: `.claude/mcp/vice/vice.ts`
       is not a separable fork module — it is simultaneously the fork's HTTP
       `call()` seam and the home of `ViceError`, `MachineRestartedError`,
       `readEpoch()`, `mcpHost()` and `type ToolInfo`, imported by 13 stock
       modules, none of which use `call()`.
    4. `## What retain costs` — the steady-state cost is already paid, but no test
       in the repository drives the fork's `-mcpserver` HTTP transport against a
       real live process: `broker-e2e.test.ts` stubs the binary to `/bin/sleep`,
       and Phase 13's live captures spoke `-binarymonitor` to the fork *binary*,
       not the fork's own endpoint. That first live exercise is the one unpaid
       cost, and 14-03 pays it on every branch.
    5. `## The two open questions` — reproduce research Open Question 1
       (full deletion vs deprecate-and-default-to-stock-first) and Open Question 2
       (does FORK-02's "route" on a remove branch require a replacement capability,
       or is an honest statement of permanent loss itself the satisfying route)
       as the two sub-questions Task 2's checkpoint puts to the human.
    6. `## Reversal criteria — draft text` — a ready-to-paste sentence for the
       PROJECT.md row, folding in `UP-01`'s existing wording. It must state, per
       research Q6 and Assumption A1: the opcode has NOT landed as of VICE 3.10
       (checked against the 3.10 manual's binary-monitor command list, whose only
       keyboard command is `0x72` buffer-text feed); Debian/Ubuntu `apt` is a
       release behind so a landed opcode reaches the documented primary install
       path only after a further cycle; landing it closes matrix keyboard
       specifically, its effect on RESTORE/NMI is unconfirmed, and it closes SID
       read-back not at all because that is a write-only-hardware fact and not a
       missing opcode; and the trigger is manually tracked, since building a
       version probe for an opcode with zero wire presence is out of scope.
    7. `## Stale framing to ignore` — `docs/roadmap-stock-vice.md` frames the
       binary monitor's single-TCP-connection model as a cost of going stock;
       `.planning/notes/stock-vice-migration-revised-loss-ledger.md` corrects that
       as a non-issue (the fork's HTTP session is already one stateful connection
       per instance). Record the correction so the human is not handed the stale
       claim.
    8. `## Caveats carried into the decision` — research Assumptions A1, A2
       (the fork maintainer's activity was not checked this session, so retain's
       "near zero" framing dates from 2026-08-11) and A3 (no evidence that any
       consumer runs the fork transport in production today), each as one line.

    Then write `.planning/phases/14-backend-decision/COVERAGE.md` containing a
    single reasoned declaration line beginning `No external API integration:`
    and naming what the phase touches instead — a decision record, documentation
    routes, and a live exercise of an already-integrated transport; no new
    external capability surface is wired. This satisfies the `api-coverage`
    seal-time gate, which otherwise re-runs its detector over this phase's PLAN
    bodies (the detector returns `detected: false` today, before the plans
    existed, and a plan body naming an MCP endpoint could flip it).

    Do not decide FORK-01 in this task. Do not recommend an option.
  </action>
  <verify>
    <automated>test -s .planning/phases/14-backend-decision/14-DECISION-BRIEF.md &amp;&amp; grep -c 'remove-now' .planning/phases/14-backend-decision/14-DECISION-BRIEF.md &amp;&amp; grep -q '^No external API integration:' .planning/phases/14-backend-decision/COVERAGE.md &amp;&amp; node .claude/gsd-core/bin/gsd-tools.cjs query check api-coverage.verify-pre .planning/phases/14-backend-decision</automated>
  </verify>
  <acceptance_criteria>
    - `14-DECISION-BRIEF.md` exists and contains all eight required `##` headings
    - The brief contains the three literal option tokens `retain`, `deprecate-first`, `remove-now`
    - The brief contains the literal `KEYBOARD_MATRIX_SET` and the literal `[skip release]`
    - The brief states, in the `remove-now` section, that criterion 3's remove clause cannot be satisfied in-phase, and names the follow-on-phase consequence
    - The brief contains no sentence recommending one option over another (checked by reading, recorded in the SUMMARY)
    - `COVERAGE.md` exists, its first non-blank line starts with `No external API integration:`, and the reason clause after the colon is non-empty
    - `node .claude/gsd-core/bin/gsd-tools.cjs query check api-coverage.verify-pre .planning/phases/14-backend-decision` exits 0
  </acceptance_criteria>
  <done>The human has one document that states both branches' real costs, the three options with their in-phase consequences, and the two open questions — with no recommendation embedded.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: FORK-01 decision checkpoint</name>
  <decision>FORK-01: retain the forked VICE MCP backend, deprecate it first, or remove it now — plus the two sub-questions the research left open</decision>
  <context>
    This is the decision the phase exists to take. Two milestone closes have
    answered it by default; REQUIREMENTS.md's Out-of-Scope table names
    "Removing the fork backend without a recorded decision" as out of scope, so
    a decision either way is the deliverable — but it must be an actual choice.

    Read `.planning/phases/14-backend-decision/14-DECISION-BRIEF.md` first. It
    carries the sizing evidence, the published-package-contract cost, and the
    reversal-criteria draft.

    What undoing this costs: `remove-now` (and, one release later,
    `deprecate-first`) deletes the fork's HTTP transport. That breaks a published
    contract — 62 named tools that `@henols/vice-mcp` currently advertises — and
    destroys the only documented route for all three hard losses. SID read-back
    is unrecoverable on stock as a hardware fact, and RESTORE/NMI has no
    client-side substitute at all. `retain` is the reversible branch.

    Two sub-questions must be answered in the same breath, because otherwise an
    executor will infer them mid-task:

    Sub-question A — if the answer is not `retain`: does "remove" mean full
    deletion now (`remove-now`), or deprecate-and-default-to-stock first with
    deletion a release later (`deprecate-first`)? The standing todo raises this
    and never answers it.

    Sub-question B — on a non-`retain` branch, does FORK-02's "a route they can
    actually follow" require a *replacement capability*, or is an honest,
    complete statement of permanent loss with no dangling promise of a fix
    itself the satisfying route? Research finds no client-side substitute exists
    for SID read-back or RESTORE/NMI. The plan's chosen reading, if you do not
    override it, is that an honest statement of loss satisfies FORK-02 for those
    two cases — matching how `capability-registry.ts` already words genuinely
    unbuilt losses.
  </context>
  <options>
    <option id="retain">
      <name>retain — keep both backends, formalise the retention with named reversal criteria</name>
      <pros>Reversible. Preserves the hedge against all three hard losses and the 24 fork-only tools. Zero code change. All three success criteria satisfiable in-phase (criterion 3 via 14-03's first-ever live exercise of the fork transport). Lets the fork-removal todo close with a resolution citing the new decision row, which reduces the count Phase 17's DEBT-04 measures.</pros>
      <cons>Keeps paying for two backends in code, tests, docs and skill prose. The fork stays the *default* for anyone who has not read the README, since `.mcp.json` sets no `VICE_BACKEND`. Does not take the simplification ROADMAP.md names as "the largest single simplification available".</cons>
    </option>
    <option id="deprecate-first">
      <name>deprecate-first — record the decision to remove, flip the default to stock now, delete a release later</name>
      <pros>A real recorded decision to remove, without walking through the one-way door in this phase. Stops the fork being retained *by default*, which is FORK-01's specific complaint. Preserves the 24 tools for anyone already running the custom build. All three criteria satisfiable in-phase. De-risks the eventual deletion by giving consumers a release of warning.</pros>
      <cons>Still technically retaining the code, so it is the weaker reading of "not retained by default". Adds a deprecation notice and a default flip to a published module (`backend-detect.mts`), which needs the compiled `resources/` mirror rebuilt. Leaves the deletion work owned but unfinished.</cons>
    </option>
    <option id="remove-now">
      <name>remove-now — full deletion of the fork backend</name>
      <pros>Takes the simplification outright: one transport, one manifest, no `VICE_BACKEND`, no `--help` discriminator, an install story that works on a VICE anyone can apt-install.</pros>
      <cons>ONE-WAY. Breaks the published 62-tool contract of `@henols/vice-mcp` (semver-major, and every merge to `main` auto-publishes a patch). Destroys the documented route for all three hard losses. Cannot be completed in this phase: ~15 production modules, 23 test files, 4 skill playbooks, README, the parity doc, and a premise rewrite of the 505-line honesty guard. Choosing it means Phase 14 seals with criterion 3's remove clause recorded UNMET and the orchestrator must insert a follow-on phase for the deletion itself.</cons>
    </option>
  </options>
  <resume-signal>Reply with exactly one of `retain`, `deprecate-first`, or `remove-now`. Add `sub-B: replacement-required` only if you are overriding the plan's reading of sub-question B. Anything else — including silence, "you decide", or a preference for whichever is cheapest — is not a decision and must be escalated, not inferred.</resume-signal>
</task>

<task type="tracer">
  <name>Task 3: Record the dated FORK-01 decision and prove it with a mechanical guard</name>
  <reversibility rating="one-way">A `remove-now` or `deprecate-first` outcome recorded here authorises deleting the fork transport, which breaks the 62-tool surface `@henols/vice-mcp` publishes and destroys the only documented route for SID read-back and RESTORE/NMI; a `retain` outcome is reversible, so the rating is asymmetric and the checkpoint above is what resolves it.</reversibility>
  <precondition>Task 2's checkpoint has recorded one of the three literal branch tokens (`retain`, `deprecate-first`, `remove-now`); halt if no branch token was recorded.</precondition>
  <files>.planning/PROJECT.md, .claude/mcp/vice/docs-fork-decision.test.ts</files>
  <read_first>
    - `.planning/PROJECT.md` lines 253-278 — the `## Key Decisions` table. It has exactly three columns (`Decision | Rationale | Outcome`) and no row anywhere in its history carries a dedicated reversal-criteria column. Match the existing rows' shape; do not add a fourth column.
    - `.planning/PROJECT.md` line 109 — the `## Out of Scope` bullet "Removing or deprecating the fork backend", which currently asserts the retention this row either formalises or supersedes.
    - `.planning/phases/14-backend-decision/14-DECISION-BRIEF.md` §6 — the reversal-criteria draft text written in Task 1.
    - `.planning/REQUIREMENTS.md` — `UP-01`'s Future Requirement sentence, the ready-made source wording to fold in, and FORK-01's own requirement text.
    - `.claude/mcp/vice/docs-linerefs.test.ts` — the project's established doc-content-guard idiom: read the real prose (never a copy pasted into the test), isolate the relevant line by substring scan, extract with a regex, assert non-vacuity before asserting content, and write failure messages that name the exact missing string and what mechanism is broken if the count is zero.
    - `.claude/mcp/vice/docs-deferred-ledger.test.ts` — the same idiom applied to a `.planning/` document, including its header note that planning-facing guards are deliberately kept OUT of `package.json`'s `files[]`.
    - `.claude/mcp/vice/test-gate.mjs` — `automatedTestFiles()` derives the automated set from the directory minus `MANUAL_ONLY_TESTS`, so a new fast test file needs no registration to be picked up.
  </read_first>
  <action>
    Two artifacts, one commit each.

    First, add exactly one new row to `.planning/PROJECT.md`'s `## Key Decisions`
    table, appended after the existing last row (the document-guarding row at
    line 277). Three cells only, matching the table's existing shape:

    - `Decision` cell: a short imperative statement of the branch the checkpoint
      chose, carrying the literal requirement id `FORK-01` and the ISO date the
      decision was taken (`YYYY-MM-DD`).
    - `Rationale` cell: why, plus the named reversal criteria. Fold in `UP-01`'s
      existing sentence so the entry *is* the criteria rather than a forward
      reference to them. It must carry the literal `KEYBOARD_MATRIX_SET` in that
      exact upper-case spelling, and it must state per research Q6 and Assumption
      A1 that the opcode has not landed as of VICE 3.10, that `apt` lags a
      release behind, that landing it closes matrix keyboard specifically, that
      its effect on RESTORE/NMI is unconfirmed, that SID read-back is a
      write-only-hardware fact no opcode closes, and that the trigger is manually
      tracked rather than probed. Include at least one reversal-trigger phrase
      from this set, verbatim: `reverses if`, `would reverse`, `reversal criteria`.
      Do NOT claim the opcode closes all three losses.
    - `Outcome` cell: follow the table's `✓ Good` / `⚠️ Revisit` convention, and
      cite the evidence this phase produced (the guard file name, and the live
      criterion-3 evidence document 14-03 writes) rather than asserting adequacy.

    Then edit the `## Out of Scope` bullet at line 109 so the two records agree:
    it must cite `FORK-01` and point at the new Key Decisions row. On `retain`
    the bullet is formalised (retained, now with dated reversal criteria); on
    `deprecate-first` or `remove-now` the bullet must no longer assert that
    removal is out of scope — it must record that FORK-01 superseded it, with the
    date. Keep the bullet's existing three-hard-losses framing intact; do not
    delete the SID / matrix-keyboard / RESTORE-NMI enumeration.

    Second, create `.claude/mcp/vice/docs-fork-decision.test.ts` following
    `docs-linerefs.test.ts`'s idiom exactly — `test` from `node:test`, `assert`
    from `node:assert/strict`, `readFileSync`, `repoRoot({ from: HERE })` from
    `./repo-root.ts`, and a header comment stating why the file exists and that
    it verifies planning-facing documentation rather than shipped runtime
    behaviour. It reads the real `.planning/PROJECT.md` and asserts, as separate
    `test()` cases:

    1. Non-vacuity: the `## Key Decisions` table has at least 20 data rows (a
       table rewrite that empties it must fail, not pass).
    2. Exactly one data row's text contains the literal `FORK-01`.
    3. That row contains an ISO `YYYY-MM-DD` date.
    4. That row contains the literal `KEYBOARD_MATRIX_SET`. The comparison is
       case-sensitive on that canonical upper-case opcode spelling — the same
       spelling REQUIREMENTS.md's `UP-01` and VICE's own opcode naming use — so a
       lower-case-only mention fails. Say so in the failure message, naming that a
       case-insensitive match was deliberately not used.
    5. That row contains at least one member of a named, non-empty reversal-phrase
       set, and the set's own length is asserted non-zero so an emptied set cannot
       make the check vacuous.
    6. The `## Out of Scope` bullet that mentions the fork backend contains the
       literal `FORK-01`, so the two records cannot silently contradict.

    Every failure message must name the exact string that was missing and what
    mechanism is broken if the extraction found nothing. Keep the file out of
    `package.json`'s `files[]` — it is a planning-facing guard, exactly like
    `docs-linerefs.test.ts` and `docs-deferred-ledger.test.ts`. Do not add a
    string literal naming a phase number: `docs-dangling-refs.test.ts` fails on
    that.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice &amp;&amp; node --test docs-fork-decision.test.ts docs-linerefs.test.ts docs-dangling-refs.test.ts docs-deferred-ledger.test.ts docs-review-disposition.test.ts test-gate.test.ts</automated>
    <human-check>Read the new FORK-01 row against FORK-01's own requirement text. Confirm it is a dated entry, that the criteria named would actually reverse it, that the KEYBOARD_MATRIX_SET coupling is stated explicitly and not overstated, and that the row reads as a decision taken rather than a status quo restated.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c 'FORK-01' .planning/PROJECT.md` returns at least 2 (the Key Decisions row and the Out of Scope bullet)
    - `grep 'FORK-01' .planning/PROJECT.md | grep -c 'KEYBOARD_MATRIX_SET'` returns at least 1
    - `grep 'FORK-01' .planning/PROJECT.md | grep -Ec '[0-9]{4}-[0-9]{2}-[0-9]{2}'` returns at least 1
    - `cd .claude/mcp/vice && node --test docs-fork-decision.test.ts` exits 0 with 6 passing tests and 0 failures
    - Non-vacuity proven, not asserted: temporarily lower-case the `KEYBOARD_MATRIX_SET` token in the row, confirm `node --test docs-fork-decision.test.ts` exits non-zero naming that token, then restore the row and confirm it exits 0 again. Record both observed outcomes in the SUMMARY.
    - `.claude/mcp/vice/docs-fork-decision.test.ts` is absent from `package.json`'s `files[]` — `node -e "process.exit(require('./.claude/mcp/vice/package.json').files.includes('docs-fork-decision.test.ts')?1:0)"` exits 0
    - `cd .claude/mcp/vice && node --test test-gate.test.ts` exits 0 (the new file is picked up by the automated set with no registration needed)
    - `node scripts/check-npm-packages.mjs` exits 0
  </acceptance_criteria>
  <done>PROJECT.md carries one dated FORK-01 row naming reversal criteria including KEYBOARD_MATRIX_SET, the Out of Scope bullet cites it rather than contradicting it, and a committed guard proven non-vacuous by a real break-and-restore fails if any of that drifts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning documents → CI guards | `.planning/PROJECT.md` is read as *data* by a committed test; its prose is load-bearing for a green build |
| repo → published npm tarball | `@henols/vice-mcp`'s `files[]` decides what third parties receive; a planning-facing guard must not leak into it |
| human decision → autonomous executor | The branch token recorded at the checkpoint is what every downstream plan dispatches on; an inferred token walks a one-way door unattended |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Repudiation | `.planning/PROJECT.md` `## Key Decisions` FORK-01 row | medium | mitigate | The committed `docs-fork-decision.test.ts` asserts the row is present, dated, carries a reversal trigger and names `KEYBOARD_MATRIX_SET`; a later silent edit reds CI instead of passing |
| T-14-02 | Tampering | `docs-fork-decision.test.ts` non-vacuity | medium | mitigate | Assertions 1 and 5 gate on a row-count floor and a non-empty phrase set; the acceptance criteria require a real break-and-restore proof rather than a claim |
| T-14-03 | Elevation of Privilege | The checkpoint's branch token | high | mitigate | `gate="blocking-human"` — never auto-approved in any mode; the resume signal names the three legal tokens and explicitly refuses "you decide" as a decision |
| T-14-04 | Information Disclosure | `package.json` `files[]` | low | mitigate | The new guard is kept out of `files[]`; `check-npm-packages.mjs` (which forbids test files in the pack) is run as an acceptance criterion |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages; the package-legitimacy gate does not fire, and RESEARCH.md carries no `## Package Legitimacy Audit` because there is nothing to audit |
</threat_model>

<flagged_assumptions>
Probe-surfaced items from the spec-less edge probe over FORK-01's requirement
text. Both rows came back `unresolved`; neither is silently dropped.

- **FORK-01 / `empty` — "What is the result for empty, single-element, or null input?"** —
  **Does not apply as posed.** FORK-01's subject is a prose row in a markdown
  table, not a function with an input domain, so there is no empty, single-element
  or null input to define a result for. The nearest real reading — "what if the row
  exists but is empty of reversal criteria" — is covered by Task 3's assertions 3
  and 5 (a row with no date or no reversal-trigger phrase fails), and by assertion
  1's row-count floor (an emptied table fails). No acceptance criterion is
  fabricated for the probe as literally posed.
- **FORK-01 / `encoding` — "Whose definition of length/equality applies?"** —
  **Applicable and authored.** The guard's token equality is a real design choice.
  Resolved as a `must_haves.truths` entry: the `KEYBOARD_MATRIX_SET` match is
  case-sensitive on the canonical upper-case opcode spelling, a lower-case-only
  mention fails, and the guard's failure message states that a case-insensitive
  match was deliberately not used. Research's own example grep used `-i`; that
  looser reading is deliberately not adopted, because the requirement names an
  opcode whose canonical spelling is upper-case.

Also carried, from RESEARCH.md's Assumptions Log, as caveats the PROJECT.md row
records rather than resolves: A1 (whether a `KEYBOARD_MATRIX_SET`-shaped opcode
could also drive the RESTORE/NMI line is unconfirmed), A2 (the fork maintainer's
current activity was not checked, so retain's "near zero" carrying cost dates
from 2026-08-11), A3 (no evidence establishes whether any consumer runs the fork
transport in production today).
</flagged_assumptions>

<artifacts_produced>
## Artifacts this phase produces (plan 14-01)

New symbols created by this plan — all shared, i.e. produced on every branch:

- `.planning/phases/14-backend-decision/14-DECISION-BRIEF.md` (new file)
- `.planning/phases/14-backend-decision/COVERAGE.md` (new file)
- `.claude/mcp/vice/docs-fork-decision.test.ts` (new test file; deliberately NOT in `package.json` `files[]`)
- A new `FORK-01` row in `.planning/PROJECT.md` → `## Key Decisions` (new document row, no new file)
- The three branch tokens `retain`, `deprecate-first`, `remove-now` (new literal vocabulary that plans 14-02 through 14-05 dispatch on)
</artifacts_produced>

<verification>
- `cd .claude/mcp/vice && node --test docs-fork-decision.test.ts docs-linerefs.test.ts docs-dangling-refs.test.ts docs-deferred-ledger.test.ts docs-review-disposition.test.ts test-gate.test.ts` exits 0
- `node scripts/check-npm-packages.mjs` exits 0
- `node .claude/gsd-core/bin/gsd-tools.cjs query check api-coverage.verify-pre .planning/phases/14-backend-decision` exits 0
- The recorded branch token is one of the three literals, and the SUMMARY names which
</verification>

<success_criteria>
- ROADMAP Phase 14 criterion 1 is satisfied: PROJECT.md → Key Decisions carries a dated FORK-01 entry naming the criteria that would reverse it, explicitly including the upstream `KEYBOARD_MATRIX_SET` coupling
- The branch token is recorded by a human, not inferred
- `docs-fork-decision.test.ts` is green and proven non-vacuous by a real break-and-restore
- PROJECT.md's Out of Scope bullet and its Key Decisions row do not contradict each other
</success_criteria>

<output>
Create `.planning/phases/14-backend-decision/14-01-SUMMARY.md` when done. It MUST
record the branch token the checkpoint returned and the sub-question B answer, in
a `## Decision` section, because plans 14-02 through 14-05 read it from there.
</output>
