---
phase: 14-backend-decision
plan: 05
type: execute
wave: 3
depends_on: ["14-01", "14-02", "14-03", "14-04"]
worktree: false
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/todos/pending/2026-08-20-fully-remove-the-forked-vice-mcp-backend.md
  - .planning/todos/pending/2026-08-20-warp-over-resource-set-refuted-on-stock-3-10.md
  - README.md
autonomous: true
requirements: [FORK-01, FORK-02]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The standing fork-removal todo is disposed against the recorded decision — closed with a resolution on retain, rescoped with a named owner on a branch that resolved to withdraw"
    - "STATE.md's Deferred Items table and the .planning/todos/pending/ tree agree in both directions in the same commit, so docs-deferred-ledger.test.ts stays green"
    - "REQUIREMENTS.md marks FORK-01 and FORK-02 complete with their Traceability rows updated"
    - "Any work this phase deliberately did not do is recorded as owned follow-on work with a named requirement or todo, never left as an unstated gap"
    - "ROADMAP.md's Phase 15 and Phase 16 dependency notes state what the decision means for the work they were sequenced to wait for"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "FORK-01/FORK-02 marked complete, Traceability updated, and the deferred fork work recorded under Future Requirements"
      contains: "FORK-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 14 outcome recorded, and Phase 15/16 dependency notes resolved against it"
      contains: "Phase 14"
    - path: ".planning/STATE.md"
      provides: "Deferred Items reconciled against the post-phase pending-todo tree"
  key_links:
    - from: ".planning/STATE.md"
      to: ".planning/todos/pending/"
      via: "the Deferred Items table is derived from the directory listing and guarded in both directions by docs-deferred-ledger.test.ts"
      pattern: "Deferred Items"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/PROJECT.md"
      via: "FORK-01's completion cites the dated Key Decisions row 14-01 wrote"
      pattern: "FORK-01"
  prohibitions:
    - statement: "Work this phase chose not to do must not be left as an unstated gap — every excluded item needs a named owner in REQUIREMENTS.md or a pending todo, and a success criterion that ends the phase unmet must be recorded as unmet rather than reported as satisfied"
      status: resolved
      verification: judgment
    - statement: "A todo must not be moved to completed on the strength of a decision that did not actually resolve it — closing the fork-removal todo is honest only on a branch where the decision genuinely settles the question it asks"
      status: resolved
      verification: judgment
---

<objective>
Close the phase's record: dispose the standing todo the decision resolves, mark
the requirements, reconcile the deferred ledger so its two-directional guard
stays green, and hand the downstream phases the outcome they were sequenced to
wait for.

Purpose: ROADMAP.md sequences Phase 14 first precisely so Phases 15, 16 and 17 do
not touch fork-coupled work before the answer exists. That ordering only pays off
if the answer is written where those phases will read it. This plan is also where
anything this phase deliberately did not do gets an owner — including, on a
`remove-now` branch, the fact that criterion 3's remove clause ends the phase
unmet and the deletion needs a follow-on phase.
Output: reconciled `REQUIREMENTS.md`, `ROADMAP.md`, `STATE.md`, a disposed todo,
and a branch-appropriate `README.md` correction.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-backend-decision/14-01-SUMMARY.md
@.planning/phases/14-backend-decision/14-02-SUMMARY.md
@.planning/phases/14-backend-decision/14-03-SUMMARY.md
@.planning/phases/14-backend-decision/14-04-SUMMARY.md
@.planning/phases/14-backend-decision/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispose the standing todo and reconcile the deferred ledger in one commit</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md, .planning/todos/pending/2026-08-20-fully-remove-the-forked-vice-mcp-backend.md</files>
  <read_first>
    - `.planning/phases/14-backend-decision/14-01-SUMMARY.md` → `## Decision` — the branch token and the sub-question B answer
    - `.planning/phases/14-backend-decision/14-02-SUMMARY.md`, `14-03-SUMMARY.md`, `14-04-SUMMARY.md` — what each plan actually delivered, including any finding filed as a new todo by 14-03's live run
    - `.planning/todos/pending/2026-08-20-fully-remove-the-forked-vice-mcp-backend.md` in full — its `resolves_phase: 14` frontmatter, its five-step removal sketch, its 24-tool inventory, and its own unanswered open question
    - `.planning/STATE.md` → `## Deferred Items` — the table derived from the pending-todo tree, and the prose above it that explains how the count moved
    - `.claude/mcp/vice/docs-deferred-ledger.test.ts` — what it actually asserts: only the `## Deferred Items` section is scanned, the `uat_gap` row and the "Carried forward from earlier closes" table are deliberately excluded, and the relationship is checked in both directions
    - `.planning/todos/completed/` — an existing closed todo, for the Resolution-section shape to follow
    - `.planning/REQUIREMENTS.md` — the FORK-01/FORK-02 checkbox lines, the Traceability table rows, the `## Future Requirements` structure including its `### Promoted by DEBT-01` bucket, and the `## Out of Scope` table
  </read_first>
  <action>
    Dispose the standing todo according to the recorded branch, and do it in the
    same commit as the STATE.md reconciliation — the ledger guard checks the
    directory against the table in both directions, so a move without the table
    edit reds CI.

    - Branch `retain`: `git mv` the todo to `.planning/todos/completed/` and append
      a `## Resolution` section citing the dated `FORK-01` row in PROJECT.md by
      date, the live criterion-3 evidence document, and the route evidence
      document. State plainly that the todo asked whether to remove the fork and
      the answer recorded is no, with named reversal criteria — so this is a
      settled question, not an abandoned one. Then remove its row from STATE.md's
      Deferred Items table.
    - Branch `deprecate-first`: keep the todo pending and rewrite it. Its title and
      Problem section become the *execution* of a decision already taken rather
      than a question. Delete its "Open question to settle first" section, because
      it is answered. Record the answer, name the owner, and state what remains:
      the 24-tool disposition, the transport deletion, the honesty-guard premise
      rewrite, the doc and skill rewrites, and the major-version release. Keep its
      row in STATE.md's Deferred Items table and update the row's status note to
      match the new scope.
    - Branch `remove-now`: same rewrite as above, plus record that the deletion is
      scheduled rather than deferred and cross-reference the follow-on phase Task
      2 proposes.

    On every branch: if 14-03's live run filed a new todo, add its row to STATE.md's
    Deferred Items table too, and update the prose above the table so the stated
    count matches the table rather than a remembered figure — that prose has gone
    stale twice before and the guard does not check it.

    Then in `.planning/REQUIREMENTS.md`: tick `FORK-01` and `FORK-02` and set both
    Traceability rows to Complete. Add a short closure note under FORK-02, in the
    shape of `EXTV-03`'s existing closure note, recording which reading of "a route
    they can actually follow" was applied and on whose authority — the plan's
    reading if it stood, the human's if sub-question B was overridden at 14-01's
    checkpoint.

    On a `remove-now` branch, FORK-01's tick needs an honest qualifier: the
    decision is recorded and criterion 1 is met, but ROADMAP criterion 3's remove
    clause is not satisfied in-phase. Say so in the closure note. Do not tick a
    requirement whose criterion is unmet without recording which part is
    outstanding and who owns it.

    Use scoped edits throughout. Do not rewrite `STATE.md` or `REQUIREMENTS.md`
    wholesale — a whole-file replacement destroys every section outside the diff
    window.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice &amp;&amp; node --test docs-deferred-ledger.test.ts docs-dangling-refs.test.ts docs-review-disposition.test.ts docs-linerefs.test.ts docs-fork-decision.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd .claude/mcp/vice && node --test docs-deferred-ledger.test.ts` exits 0 — the ledger agrees with the pending tree in both directions
    - All five `docs-*.test.ts` guards exit 0 in one run
    - `grep -c '^- \[x\] \*\*FORK-01\*\*' .planning/REQUIREMENTS.md` returns 1 and the same for `FORK-02`
    - Both Traceability rows for FORK-01 and FORK-02 read Complete
    - On branch `retain`: the todo file is absent from `.planning/todos/pending/` and present under `.planning/todos/completed/` with a `## Resolution` section that contains an ISO date and the literal `FORK-01`
    - On a non-`retain` branch: the todo remains in `.planning/todos/pending/`, no longer contains its "Open question to settle first" heading, and names an owner
    - The count stated in STATE.md's Deferred Items prose equals the number of rows in its table, verified by counting both
    - On branch `remove-now`: REQUIREMENTS.md's FORK-01 closure note states that criterion 3's remove clause is unmet in-phase and names who owns it
  </acceptance_criteria>
  <done>The decision has moved the ledger: the todo it resolves is disposed honestly, the two requirements are marked with any unmet part named, and the deferred-items guard is green in both directions.</done>
</task>

<task type="auto">
  <name>Task 2: Hand the outcome to the phases that were sequenced to wait for it</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/todos/pending/2026-08-20-warp-over-resource-set-refuted-on-stock-3-10.md, README.md</files>
  <read_first>
    - `.planning/ROADMAP.md` → the Phase 14 section (goal, the three success criteria, the `**Plans**:` line), and the Phase 15, 16 and 17 sections including their `**Depends on**:` lines, which each name the fork decision as the thing they were waiting for
    - `.planning/phases/14-backend-decision/14-RESEARCH.md` Q8 — the four named downstream couplings: the `warp-over-resource_set` todo's fork-facing half, `WR-13`'s second hardcoded capability-refusal string, Phase 16's `src/` relocation and line-reference sweep, and Phase 17's ledger count
    - `.planning/todos/pending/2026-08-20-warp-over-resource-set-refuted-on-stock-3-10.md` — item 3 of its Solution section, which explicitly branches on whether the fork is retained
    - `.planning/phases/14-backend-decision/14-04-SUMMARY.md` — whether the resolved-backend default was flipped, which decides whether `README.md`'s backend-choice section is now wrong
    - `README.md` lines 72-157 — the "Installing VICE, and choosing a backend" section, specifically its description of what an indeterminate probe falls back to
    - `.planning/REQUIREMENTS.md` → `## Future Requirements` and `## Out of Scope`, for the shape new entries follow
    - `.planning/STATE.md` → the note that this project's code-review gate runs after the last plan's SUMMARY, so no plan can disposition its own phase's review findings; three phases have filed the same todo
  </read_first>
  <action>
    Write the outcome where the downstream phases will read it.

    In `.planning/ROADMAP.md`, using scoped edits and never a whole-file
    rewrite — a whole-file `Write` destroys every phase entry outside the diff
    window:

    1. Phase 14's section: record the outcome under a `**Notes**:` line — the
       branch token, the date, a pointer to PROJECT.md's `FORK-01` row, and a
       per-criterion verdict for all three success criteria. Criterion 3's verdict
       must be honest: satisfied by the live fork-transport exercise on `retain`
       and `deprecate-first`; on `remove-now`, satisfied for the retained-path
       reading and explicitly NOT satisfied for the remove clause, naming the
       follow-on phase that owns the deletion.
    2. Phase 15's section: resolve its dependency note. State what the decision
       means for the fork-coupled disposition work — whether the
       `warp-over-resource_set` todo's fork-facing half is fixed as a fork-only
       annotation or dropped, and whether `WR-13`'s dead second refusal string is
       fixed or superseded.
    3. Phase 16's section: resolve its dependency note. State whether code will be
       deleted under the current tree before the `src/` relocation, so the
       sweep-path-and-line-references-once rationale still holds or is amended.
    4. Phase 17's section: state whether this phase's disposition reduced the
       pending-todo count that `DEBT-04` measures, and by how much.

    On a non-`retain` branch, add a `**Recommended follow-on**` note to Phase 14's
    section proposing a new phase for the fork-removal execution, with its scope
    named from research Q1/Q2 rather than guessed: extract-then-delete the
    transport and its five cleanly separable dependents, collapse the backend seam
    across `backend-detect.mts` and its compiled mirror plus `broker-launch.mts`,
    `vice-broker.mts`, `broker-control.mts`, `stock-dispatch.ts` and
    `capability-registry.ts`, rework the 23 fork-referencing test files, rewrite
    the premise of `scripts/check-skill-fork-honesty.mjs`, rewrite the four skill
    playbooks plus `README.md` and `docs/stock-vice-parity.md`, decide the 24
    fork-only tools' dispositions, and ship the whole thing as a deliberate
    major-version release. Propose it as a recommendation for the developer to
    accept; do not insert a phase entry yourself.

    In `.planning/REQUIREMENTS.md`, add to `## Future Requirements` the work this
    phase deliberately excluded, each with an owner:
    - The 24-tool disposition. Research Pitfall 4 is explicit that Phase 14's
      requirements are the decision and the routes, not reimplement-or-drop 24
      tools. On a non-`retain` branch this becomes owned follow-on work rather
      than an unstated gap.
    - The release handling for a breaking tool-surface change: 62 named tools are
      what `@henols/vice-mcp` advertises today, and every merge to `main`
      auto-publishes a patch unless the commit subject carries `[skip release]`,
      so a breaking removal needs a deliberate major-version release.
    - The `KEYBOARD_MATRIX_SET` reversal trigger's manual tracking, since no
      version probe exists for an opcode with zero wire presence and building one
      is out of scope.
    Add to `## Out of Scope` an entry recording that a hypothetical future
    `KEYBOARD_MATRIX_SET` probe is not being built, with that reason.

    In `.planning/todos/pending/2026-08-20-warp-over-resource-set-refuted-on-stock-3-10.md`,
    answer item 3's open branch with the recorded decision so Phase 15 does not
    have to re-derive it, and nothing more — the todo's own fix stays Phase 15's
    work.

    In `README.md`: on a branch where 14-04 flipped the resolved-backend default,
    correct the backend-choice section so its description of the indeterminate
    fallback matches the code, and state that the fork is retained but
    resolved-to-be-withdrawn, citing `FORK-01`. Keep every substring
    `scripts/check-skill-fork-honesty.mjs` requires present — `VICE_BACKEND`,
    `vice_sid_get_state`, `vice_keyboard_matrix`, `docs/tool-support.md`, `3.10`,
    `regenerator2000` — and keep both forbidden ghost test-file names absent. Do
    not put a numbered phase reference on the same line as any of the words
    `deferred`, `not yet`, `until`, or `unavailable`. On branch `retain`, make no
    `README.md` edit and record the zero.

    Finally, record one handoff line in Phase 15's section: this phase's own
    code-review findings will land after its last SUMMARY and therefore cannot be
    dispositioned by any plan of this phase, which is the same structural cause
    three earlier phases each filed a todo for. `GATE-02` already owns
    dispositioning them; naming Phase 14 in that list now saves a fourth
    rediscovery.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice &amp;&amp; node --test docs-deferred-ledger.test.ts docs-dangling-refs.test.ts docs-review-disposition.test.ts docs-linerefs.test.ts docs-fork-decision.test.ts &amp;&amp; cd ../../.. &amp;&amp; node scripts/check-skill-fork-honesty.mjs &amp;&amp; node scripts/check-skill-tool-coverage.mjs</automated>
    <human-check>Read ROADMAP.md's Phase 14 Notes against its own three success criteria. Confirm each criterion carries a verdict, that no criterion is reported satisfied on evidence this phase did not produce, and that on a remove-now branch the unmet remove clause is stated plainly rather than softened.</human-check>
  </verify>
  <acceptance_criteria>
    - All five `docs-*.test.ts` guards exit 0 in one run
    - `node scripts/check-skill-fork-honesty.mjs` exits 0 with its counts unchanged from 14-02's recorded post-edit baseline
    - `node scripts/check-skill-tool-coverage.mjs` exits 0
    - ROADMAP.md's Phase 14 section contains a `**Notes**:` entry naming the branch token, an ISO date, and a verdict for each of the three success criteria
    - ROADMAP.md's Phase 15, 16 and 17 sections each state the decision's consequence for the work they were sequenced to wait for
    - REQUIREMENTS.md's `## Future Requirements` gained an entry for the 24-tool disposition, one for the breaking-release handling, and one for the manual reversal-trigger tracking, each naming an owner
    - `grep -c 'skip release' .planning/REQUIREMENTS.md` returns at least 1
    - The `warp-over-resource_set` todo records the decision's answer to its item 3
    - On a non-`retain` branch: ROADMAP.md's Phase 14 section contains a `**Recommended follow-on**` note whose scope names the transport deletion, the backend-seam collapse, the test rework, the honesty-guard premise rewrite, the doc and skill rewrites, the 24-tool disposition, and the major-version release
    - On branch `retain`: `git diff --quiet -- README.md` exits 0 and the SUMMARY records the zero as decided
    - ROADMAP.md's structure is intact: every phase heading present before this task is still present, verified by comparing `grep -c '^### Phase' .planning/ROADMAP.md` against its pre-task value
  </acceptance_criteria>
  <done>Phases 15, 16 and 17 can read the decision and its consequences where their own sections point, every excluded piece of work has a named owner, and any success criterion left unmet says so.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning documents → CI guards | `STATE.md`'s Deferred Items and the pending-todo tree are checked against each other by a committed test; a one-sided edit reds the build |
| this phase → downstream phases | Phases 15, 16 and 17 act on what ROADMAP.md's dependency notes say; a wrong or missing note propagates into their plans |
| repo → published README | `README.md` is the install story consumers read; a description of the backend fallback that no longer matches the code misleads every new user |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-19 | Repudiation | Phase 14's success-criterion verdicts in ROADMAP.md | high | mitigate | Each of the three criteria gets an explicit verdict, and an acceptance criterion plus a human check require an unmet remove clause to be stated rather than softened; a phase reported green on evidence it did not produce is the failure mode the milestone's audit-integrity work exists to stop |
| T-14-20 | Tampering | `STATE.md` / `ROADMAP.md` / `REQUIREMENTS.md` whole-file rewrites | high | mitigate | The action mandates scoped edits and forbids whole-file replacement; an acceptance criterion compares the phase-heading count before and after so a truncating write is caught |
| T-14-21 | Spoofing | A todo moved to `completed/` on a branch that did not resolve it | medium | mitigate | The disposition rule is branch-conditional and explicit: closed only on `retain`, rewritten with a named owner otherwise; the prohibition block records the constraint |
| T-14-22 | Information Disclosure | `README.md` edits | low | mitigate | The required-substring and forbidden-substring sets that `check-skill-fork-honesty.mjs` enforces are named in the action and the guard is an acceptance gate |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages; the package-legitimacy gate does not fire |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 14-05)

- A `## Resolution` section on `2026-08-20-fully-remove-the-forked-vice-mcp-backend.md`, relocated to `.planning/todos/completed/` (**`retain` branch only**)
- A rewritten, rescoped `2026-08-20-fully-remove-the-forked-vice-mcp-backend.md` with a named owner, still under `.planning/todos/pending/` (**`deprecate-first` and `remove-now` branches only**)
- A `**Notes**:` entry on ROADMAP.md's Phase 14 section carrying the branch token and a per-criterion verdict (shared)
- A `**Recommended follow-on**` note on ROADMAP.md's Phase 14 section proposing a fork-removal-execution phase (**`deprecate-first` and `remove-now` branches only**; proposed, not inserted)
- Three new `## Future Requirements` entries in REQUIREMENTS.md — the 24-tool disposition, the breaking-tool-surface release handling, and the manual `KEYBOARD_MATRIX_SET` reversal-trigger tracking (shared)
- A new `## Out of Scope` entry in REQUIREMENTS.md recording that a version probe for an opcode with no wire presence is not being built (shared)
- A closure note under FORK-02 in REQUIREMENTS.md recording which reading of "a route they can actually follow" was applied (shared)
- Corrected backend-fallback prose in `README.md` (**`deprecate-first` and `remove-now` branches only**)
</artifacts_produced>

<verification>
- `cd .claude/mcp/vice && node --test docs-deferred-ledger.test.ts docs-dangling-refs.test.ts docs-review-disposition.test.ts docs-linerefs.test.ts docs-fork-decision.test.ts` exits 0
- `node scripts/check-skill-fork-honesty.mjs` and `node scripts/check-skill-tool-coverage.mjs` exit 0
- `cd .claude/mcp/vice && node test-gate.mjs` exits 0
- `grep -c '^### Phase' .planning/ROADMAP.md` is unchanged from its pre-task value
- FORK-01 and FORK-02 are ticked in REQUIREMENTS.md with Traceability rows Complete
</verification>

<success_criteria>
- The decision is written where Phases 15, 16 and 17 will read it, with their dependency notes resolved
- The standing todo is disposed honestly against the branch actually taken
- Every piece of work this phase excluded has a named owner
- Any success criterion left unmet is recorded as unmet
- The deferred-items ledger is green in both directions and its stated count matches its table
</success_criteria>

<output>
Create `.planning/phases/14-backend-decision/14-05-SUMMARY.md` when done. Record
the branch token, the todo's disposition and its new location, the per-criterion
verdicts written into ROADMAP.md, the pending-todo count before and after, and —
on a non-`retain` branch — the follow-on phase recommended and its named scope.
</output>
