---
phase: 14-backend-decision
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
worktree: false
files_modified:
  - .planning/phases/14-backend-decision/14-ROUTE-EVIDENCE.md
  - .claude/mcp/vice/capability-registry.ts
  - .claude/mcp/vice/capability-registry.test.ts
  - .claude/skills/c64-program-recon/SKILL.md
  - .claude/skills/c64-program-recon/references/observation-hazards.md
  - .claude/skills/c64-program-recon/references/control-flow.md
  - .claude/skills/c64-program-recon/references/sound-and-input.md
  - .claude/skills/c64-program-recon/references/tool-selection.md
  - .claude/skills/c64-ram-capture/SKILL.md
  - README.md
  - docs/stock-vice-parity.md
autonomous: true
requirements: [FORK-02]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every point-of-use mention of vice_sid_get_state, vice_keyboard_matrix and vice_keyboard_restore is enumerated with a file:line citation and a per-site verdict against the branch FORK-01 chose"
    - "The runtime tool-call refusal for each of the three hard-loss tools states a route that is true on the branch chosen — a fork pointer on retain, a fork pointer plus the recorded withdrawal on deprecate-first or remove-now"
    - "No point-of-use text promises a future fix that no requirement owns"
    - "node scripts/check-skill-fork-honesty.mjs exits 0 after the edits, with its non-vacuity floors unchanged"
    - "The literal token unrecoverable still appears only in the hardware-category refusal wording and never in the descoped-category wording"
  artifacts:
    - path: ".planning/phases/14-backend-decision/14-ROUTE-EVIDENCE.md"
      provides: "The enumerated 17-site route inventory with per-site verdicts and the two guards' recorded output"
      min_lines: 60
    - path: ".claude/mcp/vice/capability-registry.ts"
      provides: "capabilityRefusalMessage() wording that is true on the branch chosen"
      contains: "capabilityRefusalMessage"
    - path: ".claude/mcp/vice/capability-registry.test.ts"
      provides: "Assertions pinning the refusal wording for all three hard-loss tools"
  key_links:
    - from: "scripts/check-skill-fork-honesty.mjs"
      to: ".claude/mcp/vice/capability-registry.ts"
      via: "imports CAPABILITY_REGISTRY and derives FORK_ONLY_NAMES from providedBy === fork; never a hand-copied list"
      pattern: "CAPABILITY_REGISTRY"
    - from: ".claude/skills/c64-program-recon/references/observation-hazards.md"
      to: ".claude/mcp/vice/capability-registry.ts"
      via: "section-scoped annotation matching the honesty script's ANNOTATION_RE alternatives"
      pattern: "requires the fork"
  prohibitions:
    - statement: "No point-of-use text may offer a route the user cannot actually take on the branch decided, and none may carry a promise of a future fix that no requirement owns"
      status: resolved
      verification: judgment
    - statement: "A hardware-category permanent loss and a merely-unbuilt descoped tool must remain distinguishable to the reader — the wording must not collapse both into an undifferentiated not-supported"
      status: resolved
      verification: judgment
---

<objective>
Make FORK-02 true on whichever branch FORK-01 chose: a user who hits SID
read-back, matrix keyboard, or RESTORE/NMI is told, at the point of use, a route
they can actually follow.

Purpose: ROADMAP Phase 14 criterion 2 demands this be *evidenced by the live
doc/skill text*, not asserted. Research Q4 found all three routes already exist,
are already annotated per-section, and are already policed by two mechanical
guards — so on the `retain` branch this plan's work is verification and
evidence-recording, not authoring (research Pitfall 3 warns explicitly against
budgeting new writing there). On `deprecate-first` or `remove-now`, each site
gains the recorded withdrawal so the route is honest about being on its way out.
Output: `14-ROUTE-EVIDENCE.md`, plus branch-conditional edits to the runtime
refusal and the 17 documented mention sites.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-backend-decision/14-RESEARCH.md
@.planning/phases/14-backend-decision/14-PATTERNS.md
@.planning/phases/14-backend-decision/14-01-SUMMARY.md
@.claude/mcp/vice/capability-registry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the 17 hard-loss mention sites and judge each against the decided branch</name>
  <files>.planning/phases/14-backend-decision/14-ROUTE-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/14-backend-decision/14-01-SUMMARY.md` → `## Decision` — the branch token (`retain`, `deprecate-first`, or `remove-now`) and the sub-question B answer. Everything below is conditioned on it.
    - `.planning/phases/14-backend-decision/14-RESEARCH.md` Q4 — the three routes quoted verbatim from each current site, with the researcher's per-route judgment
    - `.claude/mcp/vice/capability-registry.ts` — the three `category: "hardware"` entries at lines 104-127 (`vice_sid_get_state`, `vice_keyboard_matrix`, `vice_keyboard_restore`), the shared `KEYBOARD_ALTERNATIVE` constant at lines 91-95, and `capabilityRefusalMessage()` at lines 350-388 with its header comment at lines 335-349
    - `scripts/check-skill-fork-honesty.mjs` — `ANNOTATION_RE` (the alternatives `fork-only`, `requires the fork backend`, `requires the fork`, `fork backend`, `VICE_BACKEND`, case-insensitive), the section-scoped proximity rule, `REQUIRED_README_SUBSTRINGS` at lines 244-260, `FORBIDDEN_README_SUBSTRINGS` at lines 270-279, `REQUIRED_PARITY_SUBSTRINGS` at lines 296-301, `FORBIDDEN_PARITY_SUBSTRINGS` at lines 309+, the `STALE_WORDS_RE` / phase-possessive pairing at lines 162-192, and the non-vacuity floors at lines 464-490
    - Every mention site, read in place: `.claude/skills/c64-program-recon/references/observation-hazards.md` (lines 79, 88, 103, 106), `.claude/skills/c64-program-recon/references/control-flow.md` (lines 86, 90), `.claude/skills/c64-program-recon/references/sound-and-input.md` (lines 64, 65), `.claude/skills/c64-program-recon/references/tool-selection.md` (line 18), `.claude/skills/c64-program-recon/SKILL.md` (line 296), `.claude/skills/c64-ram-capture/SKILL.md` (line 158), `README.md` (line 145), `docs/stock-vice-parity.md` (lines 20, 243, 248, 254, 259)
    - `CLAUDE.md` → `### Constraints` — the settled facts these texts rest on: SID `$D400-$D418` is write-only in hardware and the binary monitor has no SID command; the matrix keyboard is not recoverable on stock because `KEYBOARD_FEED` (0x72) injects buffer text only; and `SKILL-01`, which says a skill written against the full fork surface breaks on stock rather than degrading
  </read_first>
  <action>
    Re-derive the site list from the tree rather than trusting the line numbers
    above — they drift. Run a repo grep for the three tool names across
    `.claude/skills/`, `README.md` and `docs/stock-vice-parity.md`; the numbers
    listed in `read_first` are the count and locations measured at plan time
    (17 mentions across 8 files) and any difference is drift to record, not an
    error to hide.

    Write `.planning/phases/14-backend-decision/14-ROUTE-EVIDENCE.md` with:

    1. A `## Branch` line naming the token read from `14-01-SUMMARY.md`, and the
       sub-question B answer.
    2. A `## Site inventory` table, one row per mention: file, line, which of the
       three hard losses, the annotation phrase currently present in that
       section, and a verdict of exactly one of `holds-as-is`, `needs-amendment`,
       or `needs-rewrite`, chosen by this rule and no other:
       - branch `retain` → every site is `holds-as-is`. The fork route is real
         and followable: set `VICE_BACKEND=fork` and relaunch. No edits.
       - branch `deprecate-first` or `remove-now` → every site is
         `needs-amendment`. The fork route still works today (the transport is
         not deleted in this phase on any branch) but the reader must be told it
         is being withdrawn by a recorded decision, so they do not design a
         method around a route that is going away.
       - `needs-rewrite` is reserved for a site whose text would become *false*
         on the branch chosen. Record it if you find one; none is expected,
         because nothing in this phase deletes the transport.
    3. A `## Runtime refusal` section quoting the exact string
       `capabilityRefusalMessage()` currently returns for each of the three
       tools when `activeBackend` is `stock`, produced by actually calling the
       function, not by reading the format string.
    4. A `## The two totally-unrecoverable cases` section recording the plan's
       reading of FORK-02's open question, unless `14-01-SUMMARY.md` says the
       human overrode it: for SID read-back and RESTORE/NMI no client-side
       substitute exists, so an honest and complete statement of the loss with no
       dangling promise of a fix is what satisfies FORK-02 there. Matrix keyboard
       is different — `vice_joystick_set` is a genuine partial route that does
       not depend on the fork, already carried in the shared `KEYBOARD_ALTERNATIVE`
       string, and it survives every branch.
    5. A `## Guards, before` section recording the verbatim stdout of
       `node scripts/check-skill-fork-honesty.mjs` and
       `node scripts/check-skill-tool-coverage.mjs` run before any edit, so the
       later run can be compared against a real baseline rather than a memory.
       At plan time the honesty guard reported 11 fork-only mentions across 30
       files in 6 skill directories and 24 fork-only names derived from
       `CAPABILITY_REGISTRY`; record the actual current numbers.

    Make no edits to any skill, doc or source file in this task.
  </action>
  <verify>
    <automated>test -s .planning/phases/14-backend-decision/14-ROUTE-EVIDENCE.md &amp;&amp; grep -Ec 'holds-as-is|needs-amendment|needs-rewrite' .planning/phases/14-backend-decision/14-ROUTE-EVIDENCE.md &amp;&amp; node scripts/check-skill-fork-honesty.mjs &amp;&amp; node scripts/check-skill-tool-coverage.mjs &amp;&amp; git diff --quiet -- .claude/skills README.md docs/stock-vice-parity.md .claude/mcp/vice/capability-registry.ts</automated>
  </verify>
  <acceptance_criteria>
    - `14-ROUTE-EVIDENCE.md` exists with all five required `##` sections
    - Its site-inventory table has one row per mention found by the re-run grep, and the row count is stated explicitly alongside the plan-time figure of 17 so any drift is visible
    - Every row carries exactly one of the three verdict tokens
    - The `## Runtime refusal` section quotes three distinct strings, each naming its tool by name, and the SID and RESTORE strings each contain the literal `unrecoverable`
    - The `## Guards, before` section contains the literal stdout of both guard scripts, including their reported counts
    - `git diff --quiet -- .claude/skills README.md docs/stock-vice-parity.md .claude/mcp/vice/capability-registry.ts` exits 0 — this task edits nothing outside the evidence file
  </acceptance_criteria>
  <done>Every hard-loss mention site is enumerated from the tree with a per-site verdict derived from the recorded branch, and both guards' pre-edit output is captured as a real baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Make the runtime refusal true on the decided branch</name>
  <files>.claude/mcp/vice/capability-registry.ts, .claude/mcp/vice/capability-registry.test.ts</files>
  <read_first>
    - `.planning/phases/14-backend-decision/14-ROUTE-EVIDENCE.md` — the branch and the three quoted current refusal strings
    - `.claude/mcp/vice/capability-registry.ts` lines 335-388 — `capabilityRefusalMessage()`'s header contract and its three category branches (`hardware`, `descoped`, `stock-only-gain`). The header states two rules that must survive any edit: the literal token `unrecoverable` must NEVER appear in the `descoped` wording, and `entry.alternative` must be rendered in every branch that has one (it was once scoped to a single branch, which made the field dead at runtime — the comment says explicitly not to re-scope it back)
    - `.claude/mcp/vice/capability-registry.test.ts` — the existing assertions on the refusal strings, so new ones follow the same shape
    - `.planning/PROJECT.md` → the `FORK-01` Key Decisions row written by 14-01, which any new wording must cite rather than paraphrase
  </read_first>
  <action>
    Branch condition, stated first because it decides whether this task does
    anything at all:

    - If the branch is `retain`: make NO source edit. Research Q4 found the
      refusal already names a real, followable route for all three tools. Instead
      add assertions to `capability-registry.test.ts` that pin the three
      hard-loss refusal strings as they stand — each names its tool, contains the
      literal `unrecoverable`, names the fork backend, and carries the actionable
      `Set VICE_BACKEND=fork`; and the keyboard entries additionally render the
      shared alternative. Record in the SUMMARY that the source was deliberately
      unchanged, citing the evidence file, so a zero-diff is a recorded outcome
      and not an omission.

    - If the branch is `deprecate-first` or `remove-now`: extend the `hardware`
      branch's wording so it states, in addition to everything it says today,
      that this route rests on a backend a dated decision has resolved to
      withdraw, and points the reader at PROJECT.md's `FORK-01` row by that
      literal id. Keep the existing structure intact: the tool name, then
      `entry.reason`, then the `providedBy` backend with the actionable
      `Set VICE_BACKEND=` clause, then `entry.alternative`. Do not add a fourth
      category and do not change any entry's `category` or `providedBy` field —
      the fork still provides these tools until the deletion lands, and
      `scripts/check-skill-fork-honesty.mjs` derives its whole fork-only name set
      from `providedBy === "fork"`, so changing it would empty that set and trip
      the script's own 20-name non-vacuity floor.

      Three hard constraints on the new wording:
      * The literal token `unrecoverable` stays in the `hardware` branch and stays
        out of the `descoped` branch, so a reader can still tell a hardware loss
        from an unbuilt tool.
      * Do not put a numbered phase reference on the same line as any of the words
        `deferred`, `not yet`, `until`, or `unavailable` — that pairing is what
        `check-skill-fork-honesty.mjs`'s stale-forward-reference rule fails on.
        Name the decision by its requirement id, never by a phase number.
      * Do not promise a replacement. There is none for SID read-back or
        RESTORE/NMI.

      Then update `capability-registry.test.ts` to pin the new wording for all
      three tools, and confirm the `descoped` branch's wording is unchanged.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice &amp;&amp; npx tsc --noEmit -p tsconfig.json &amp;&amp; node --test capability-registry.test.ts &amp;&amp; cd ../../.. &amp;&amp; node scripts/check-skill-fork-honesty.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd .claude/mcp/vice && npx tsc --noEmit -p tsconfig.json` exits 0
    - `cd .claude/mcp/vice && node --test capability-registry.test.ts` exits 0 with zero failures
    - The test file contains an assertion naming each of `vice_sid_get_state`, `vice_keyboard_matrix` and `vice_keyboard_restore` against the refusal string produced by calling `capabilityRefusalMessage(name, "stock")`
    - On a non-`retain` branch: the refusal string for each of the three tools contains the literal `FORK-01`, and a test asserts it
    - On any branch: the refusal string for `vice_sid_get_state` with `activeBackend` `stock` contains the literal `unrecoverable`, and no `descoped`-category tool's refusal string does — asserted in the test, not by grep over the source
    - `node scripts/check-skill-fork-honesty.mjs` exits 0 and its reported fork-only-name count is unchanged from the Task 1 baseline
    - `cd .claude/mcp/vice && node scripts/../../../scripts/check-skill-tool-coverage.mjs` is not required here; instead `node scripts/check-skill-tool-coverage.mjs` from the repo root exits 0
  </acceptance_criteria>
  <done>The tool-call refusal a user actually reads is true on the branch chosen, the hardware-versus-descoped distinction is intact, and the wording is pinned by tests rather than left to drift.</done>
</task>

<task type="auto">
  <name>Task 3: Make the documented routes true at every point of use</name>
  <files>.claude/skills/c64-program-recon/SKILL.md, .claude/skills/c64-program-recon/references/observation-hazards.md, .claude/skills/c64-program-recon/references/control-flow.md, .claude/skills/c64-program-recon/references/sound-and-input.md, .claude/skills/c64-program-recon/references/tool-selection.md, .claude/skills/c64-ram-capture/SKILL.md, README.md, docs/stock-vice-parity.md</files>
  <read_first>
    - `.planning/phases/14-backend-decision/14-ROUTE-EVIDENCE.md` — the per-site verdicts produced by Task 1; this task applies them and invents nothing new
    - `scripts/check-skill-fork-honesty.mjs` — re-read `ANNOTATION_RE`, the section-scoped proximity rule, the two positive controls it requires to stay compliant (`references/tool-selection.md` with an inline same-line annotation, `references/control-flow.md` with a same-section annotation), the required and forbidden README substrings, the required and forbidden parity-doc substrings, and the non-vacuity floors
    - `.planning/PROJECT.md` → the `FORK-01` Key Decisions row, which every amendment cites by id
    - Each site file, in place, before editing it
  </read_first>
  <action>
    Apply exactly the verdicts recorded in `14-ROUTE-EVIDENCE.md`. Do not
    re-derive them here and do not widen the edit beyond them.

    - Every site marked `holds-as-is` (the whole set on branch `retain`) gets no
      edit. Zero diff is the correct outcome and the evidence file plus the two
      green guards are the proof; record that in the SUMMARY explicitly.

    - Every site marked `needs-amendment` gets one added clause in the same
      section as the mention, saying that the fork route rests on a backend a
      dated decision has resolved to withdraw and naming `FORK-01` as that
      decision. Keep the amendment to one sentence per site and keep the
      existing text intact — this is an addition, not a rewrite.

    Four mechanical constraints, each of which will red a committed guard if
    broken:
    * Every amended section must still contain one of the honesty script's
      annotation alternatives (`fork-only`, `requires the fork backend`,
      `requires the fork`, `fork backend`, `VICE_BACKEND`) in the same section as
      the tool mention. An amendment that replaces the annotation instead of
      joining it fails the section-scoped proximity rule.
    * `references/tool-selection.md` must keep its annotation on the same line as
      the mention, and `references/control-flow.md` must keep its annotation in
      the same section — both are named positive controls in the script's own
      non-vacuity block.
    * No amendment line may put a numbered phase reference alongside any of the
      words `deferred`, `not yet`, `until`, or `unavailable`. Name the decision by
      its requirement id.
    * `README.md` must keep all six required substrings present (including
      `VICE_BACKEND`, `vice_sid_get_state`, `vice_keyboard_matrix`,
      `docs/tool-support.md`, `3.10`, `the external analyser`) and both forbidden ghost
      test-file names absent; `docs/stock-vice-parity.md` must keep
      `docs/tool-support.md` present and all of its forbidden strings absent.

    `installer/skills/` needs no manual sync — it is a gitignored copy
    regenerated by the installer package's `prepack` script from `.claude/skills/`.
  </action>
  <verify>
    <automated>node scripts/check-skill-fork-honesty.mjs &amp;&amp; node scripts/check-skill-tool-coverage.mjs &amp;&amp; cd .claude/mcp/vice &amp;&amp; node --test docs-dangling-refs.test.ts docs-linerefs.test.ts capability-registry.test.ts</automated>
    <human-check>For each of the three hard losses, read the text a user actually meets at the point of use — the skill section and the runtime refusal string — and confirm it names a route that exists on the branch decided, with no promise of a fix that no requirement owns. This is ROADMAP criterion 2's judgement half and cannot be reduced to a grep.</human-check>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-skill-fork-honesty.mjs` exits 0, and its reported fork-only mention count, file count, directory count and fork-only-name count all match the Task 1 baseline recorded in `14-ROUTE-EVIDENCE.md`
    - `node scripts/check-skill-tool-coverage.mjs` exits 0
    - `cd .claude/mcp/vice && node --test docs-dangling-refs.test.ts docs-linerefs.test.ts capability-registry.test.ts` exits 0
    - No non-vacuity floor in `scripts/check-skill-fork-honesty.mjs` was lowered — `git diff -- scripts/check-skill-fork-honesty.mjs scripts/lib/skill-honesty-checks.mjs` is empty
    - On branch `retain`: `git diff --quiet -- .claude/skills README.md docs/stock-vice-parity.md` exits 0, and the SUMMARY records the zero-diff as the decided outcome with the evidence file cited
    - On a non-`retain` branch: every file whose verdict rows were `needs-amendment` has a diff, each added clause contains the literal `FORK-01`, and the count of amended sites equals the count of `needs-amendment` rows in `14-ROUTE-EVIDENCE.md`
    - `14-ROUTE-EVIDENCE.md` gains a `## Guards, after` section with the verbatim stdout of both guard scripts post-edit
  </acceptance_criteria>
  <done>Criterion 2 is evidenced by the live doc and skill text: every point-of-use mention of the three hard losses states a route that is true on the branch decided, with both mechanical guards green at unchanged non-vacuity floors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill/doc prose → CI guards | `scripts/check-skill-fork-honesty.mjs` reads skill text, README and the parity doc as data; prose is load-bearing for a green build |
| `CAPABILITY_REGISTRY` → every consumer | One table feeds the runtime refusal, the honesty script's name set, and the generated support table; a field change propagates to all three |
| user at a tool call → the refusal string | The refusal is the only surface some users ever read about a capability; a false route there is a user-facing defect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-05 | Tampering | `scripts/check-skill-fork-honesty.mjs` non-vacuity floors | high | mitigate | An acceptance criterion requires `git diff` over the script and its predicate library to be empty, so the instrument cannot be weakened to make this phase's edits pass |
| T-14-06 | Spoofing | `capabilityRefusalMessage()` `hardware` branch | medium | mitigate | The `unrecoverable` token discipline is asserted by test in both directions (present for hardware, absent for descoped), so a reader can still tell a permanent hardware loss from an unbuilt tool |
| T-14-07 | Repudiation | `CAPABILITY_REGISTRY` `providedBy` fields | medium | mitigate | The action forbids changing `category` or `providedBy` in this plan; the honesty script's 20-name floor would silently stop protecting anything if the fork-only set were emptied |
| T-14-08 | Information Disclosure | `.claude/skills/**` edits reaching the published installer tarball | low | accept | `installer/skills/` is gitignored and regenerated from `.claude/skills/` on `prepack`; skill prose is public documentation by design and carries no secret |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages; the package-legitimacy gate does not fire |
</threat_model>

<flagged_assumptions>
Probe-surfaced item from the spec-less edge probe over FORK-02's requirement text.
The row came back `unclassified` / `unresolved`; it is not silently dropped.

- **FORK-02 / `unclassified` — "review manually"** — Reviewed. The genuine
  ambiguity behind this row is RESEARCH.md's Open Question 2: does FORK-02's "a
  route they can actually follow" require a *replacement capability*, or is an
  honest, complete statement of permanent loss itself the satisfying route? The
  plan's chosen reading, stated here rather than buried: for SID read-back and
  RESTORE/NMI, where research establishes that no client-side substitute exists
  at all, an honest statement of the loss with no dangling promise of a fix
  satisfies FORK-02 — matching how `capability-registry.ts` already words
  genuinely unbuilt losses. Matrix keyboard is the exception and keeps a real
  partial route (`vice_joystick_set`, in the shared `KEYBOARD_ALTERNATIVE`
  string) that survives every branch. This reading is put to the human as
  sub-question B at 14-01's checkpoint and can be overridden there; Task 1
  records whichever reading was returned. It remains an assumption, not a
  resolved criterion, because the requirement text itself does not settle it.
</flagged_assumptions>

<artifacts_produced>
## Artifacts this phase produces (plan 14-02)

- `.planning/phases/14-backend-decision/14-ROUTE-EVIDENCE.md` (new file, shared — produced on every branch)
- New assertions in `.claude/mcp/vice/capability-registry.test.ts` pinning the three hard-loss refusal strings (shared)
- Amended `capabilityRefusalMessage()` `hardware`-branch wording citing `FORK-01` (**`deprecate-first` and `remove-now` branches only**)
- One added clause per `needs-amendment` site across 8 skill/doc files (**`deprecate-first` and `remove-now` branches only**)
- The verdict vocabulary `holds-as-is` / `needs-amendment` / `needs-rewrite` (new literal tokens used by the evidence file and this plan's acceptance criteria)

No file is deleted and no `CAPABILITY_REGISTRY` entry's `category` or
`providedBy` field changes on any branch in this plan.
</artifacts_produced>

<verification>
- `node scripts/check-skill-fork-honesty.mjs` exits 0 with counts matching the pre-edit baseline
- `node scripts/check-skill-tool-coverage.mjs` exits 0
- `cd .claude/mcp/vice && npx tsc --noEmit -p tsconfig.json` exits 0
- `cd .claude/mcp/vice && node --test capability-registry.test.ts docs-dangling-refs.test.ts docs-linerefs.test.ts` exits 0
- `git diff -- scripts/check-skill-fork-honesty.mjs scripts/lib/skill-honesty-checks.mjs` is empty
</verification>

<success_criteria>
- ROADMAP Phase 14 criterion 2 is satisfied and evidenced by live doc/skill text: a user hitting SID read-back, matrix keyboard, or RESTORE/NMI is told a followable route at the point of use
- The evidence is an enumerated per-site inventory with verdicts, not an assertion
- Both mechanical guards are green at unchanged non-vacuity floors
- On `retain`, the zero-diff outcome is recorded as a decision with evidence, never as an omission
</success_criteria>

<output>
Create `.planning/phases/14-backend-decision/14-02-SUMMARY.md` when done. Record
the branch token acted on, the site count and verdict breakdown, whether any
source or doc file was edited, and both guards' before/after counts.
</output>
