---
phase: 49-the-reassembly-gate-committed-before-the-phase-it-gates
plan: 07
type: execute
wave: 4
depends_on: ["49-03", "49-04", "49-05", "49-06"]
files_modified:
  - src/mcp/vice/reassembly-gate-run.test.ts
  - .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-tree-rebuild.md
  - .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-movement-rebuild.md
  - .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-hazard-disposition.md
  - .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-red-controls.md
  - .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-guards.md
  - docs/phase49-the-reassembly-gate-findings.md
autonomous: true
requirements: [BUILD-06]

user_setup:
  - service: acme-cross-assembler
    why: "The gate's verdict is a byte-diff against a real reassembly. With no assembler on the host both rebuild inputs record the no-assembler token, which the frozen rule table maps to red, so the phase cannot close green without it. This project detects external tools and never installs them."
    env_vars:
      - name: ACME_BIN
        source: "Optional. Absolute path to the assembler binary when it is not resolvable on PATH; the availability gate reads it once at module load."
    dashboard_config:
      - task: "Install the ACME cross-assembler, release 0.97 or later, so that running it with a version flag prints its release banner"
        location: "The per-platform package line in README.md, run by the operator"

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The gate has been run for real against the committed purpose-built subject and a real assembler, and its seven inputs exist as bare outcome lines at column zero of the five declared evidence files."
    - "The verdict is derived by evaluating the committed rule table in order against those recorded inputs, and the verdict document names the rule that fired rather than asserting a conclusion."
    - "Every value in the verdict document's frontmatter cites the evidence file and line it was read from; no value is present without a traceable source."
    - "The ordering property is proved from git history and not from any document's claim about itself: the commit that added the rule table contains the rule table and the schema and nothing that measured anything."
    - "A red or acknowledged verdict is recorded as what it is, with the rule that produced it, rather than being adjusted, re-measured until it changes, or softened in prose."
    - "The next phase can read the verdict token and the rule id out of the document's frontmatter without parsing prose."
  prohibitions:
    - "The rule table and the schema must never be edited, refined, re-scoped or clarified once this plan's first measurement is written. A rule found to be wrong is recorded as an explicit override in the verdict document, where a reader can see both the rule and the departure from it."
    - "A measurement must never be repeated with different inputs, a different subject, or a narrowed scope in order to obtain a better verdict. A verdict that came out red is the finding."
    - "The assembler must never be installed, fetched, built on demand, or reached through a package manager to make this run possible. It is detected; if it is absent the run records the no-assembler token and the refusal names the tool and the remedy for the operator to apply."
  artifacts:
    - src/mcp/vice/reassembly-gate-run.test.ts
    - .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-tree-rebuild.md
    - .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-movement-rebuild.md
    - .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-hazard-disposition.md
    - .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-red-controls.md
    - .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-guards.md
    - docs/phase49-the-reassembly-gate-findings.md
  key_links:
    - "the run harness's printed outcome lines -> the evidence files' column-zero lines -> the verdict document's inputs, each with a file-and-line citation -> the rule table evaluated in order -> the recorded verdict token and rule id. Every step is transcription or evaluation; none is judgement."
    - "the rule table's add-commit, read from git history -> the guards evidence file's ordering line -> the rule that fires first when it is breached. The ordering property is an input to the gate, not a claim beside it."
---

<objective>
Run the gate for real, record what it said in the vocabulary frozen before it ran, and publish
a machine-readable verdict the next phase reads as a precondition.

Purpose: everything up to here built an instrument. This is the measurement, and its integrity
rests on the ordering the first plan established — the rules were committed, alone, before any
of this existed. The verdict is whatever the rule table says it is when evaluated against the
recorded inputs; a red one is a finding to record, not a problem to re-measure away.
Output: the run harness, five evidence files carrying the seven outcome lines, and the verdict
document with its rule id and its per-input citations.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/49-01-PLAN.md
@.planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/49-02-PLAN.md
@.planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/49-PATTERNS.md
</context>

**The phase-wide decisions D49-A through D49-F live in `49-01-PLAN.md` and bind this plan
unchanged.** D49-E (test-only, unpublished) applies to the run harness this plan adds, and
D49-F (no planning bookkeeping in source) applies to it too — the harness prints outcome line
names, which are a vocabulary rather than a citation, and it must carry no planning path, plan
number or requirement id.

## Artifacts this plan produces

**New files** — `src/mcp/vice/reassembly-gate-run.test.ts`; the five measurement evidence files
named in this plan's frontmatter; `docs/phase49-the-reassembly-gate-findings.md`

**New test-name prefix used as a grep target** — `gate run:`

<tasks>

<task type="auto">
  <name>Task 1: The run harness — assemble the gate's in-process inputs for real and print them as outcome lines</name>
  <precondition>The ACME cross-assembler resolves on PATH or through the assembler-binary environment variable, so that running it with a version flag prints its release banner. This project detects external tools and never installs them; with no assembler present both rebuild inputs record the no-assembler token and the frozen rule table maps that to red, so a measurement taken without one is a recorded red rather than a result worth transcribing.</precondition>
  <files>src/mcp/vice/reassembly-gate-run.test.ts</files>
  <read_first>
    - `.planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/SCHEMA.md` — the seven line names, their value sets, their derivations and the table assigning each to its declared source file. The harness prints line names from this document and invents none.
    - `src/mcp/vice/reassembly-gate.ts` — the gate function, its input type and the diff-scope helper.
    - `src/mcp/vice/reassembly-gate-movement.ts` — the relocation transform and the two movement-result builders.
    - `src/mcp/vice/reassembly-gate-ack.ts` — the disposition function and the deterministic acknowledgement rendering.
    - `src/mcp/vice/acme-verify.ts` — the tree entry point and its options.
    - `src/mcp/vice/reassembly-gate-movement.test.ts` — the movement subject built by the movement plan: its ranges, its labels, its split table's byte positions and its chosen delta. Reuse that subject rather than inventing a second one; if reuse needs it, lift its construction into a small exported helper rather than copying the bytes.
    - `src/mcp/vice/hazard-subject-reassembly.test.ts` lines 44-108 — loading the committed purpose-built subject's store export and its program image.
    - `src/mcp/vice/acme-gate.ts` — the availability gate and its skip reason.
  </read_first>
  <action>
    Create the run harness as a test file with one always-running availability-gate case, copied
    in shape from the other assembler-dependent files, and one `gate run:` case carrying the
    shared skip reason.

    In the run case, build the four inputs that can be derived in process, each from the real
    machinery and never by hand. The baseline rebuild: load the committed purpose-built subject's
    store export into a throwaway store, export it as a tree into a fresh directory, verify it
    through the tree entry point, and take its outcome token. The diff scope: build the real
    hazard report over that same subject and run the diff-scope helper against the export's own
    half-open extent. The movement rebuild: run the relocation transform over the movement
    subject, export and verify at the new layout, and build the movement result from that verdict.
    The hazard disposition: dispose the real hazard report against a frozen acknowledgement array
    declared in this file, one entry per finding, each carrying the reason a person would give for
    accepting that movement constraint.

    Declare that acknowledgement array as a frozen constant with a comment stating what it is — a
    recorded human acceptance of each named movement constraint, written out individually because
    a blanket acceptance would hide the decision from the reader. If the report carries a finding
    the array does not name, the disposition comes back blocked and the harness prints that token;
    it does not add an entry to make the run pass.

    Print, to standard output, one bare line at column zero per declared line name, spelled exactly
    as the schema declares it, with its value. Print the rendered per-finding acknowledgement lines
    below them under a heading, so a transcriber has the detail beside the token. Print the subject
    name, the moved symbol, the delta and both addresses as context lines that are visibly not
    outcome lines.

    Assert only that each printed token is a member of its declared value set, and that the
    movement context reports two different addresses whenever the movement outcome is not the
    refused token. Do not assert that any token has a particular value: the harness measures and
    the rule table decides. Put that in a comment, because a later reader will be tempted to add an
    assertion that the run is green, and that assertion would quietly convert a measuring
    instrument into a second, unfrozen decision rule.

    Give the file a header stating why it exists in terms a reader can act on: the gate's inputs
    have to be produced by running it rather than by describing it, and this file is the one place
    that run happens, printing its results in the vocabulary that was frozen before it existed.
    Carry no planning bookkeeping in the file.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck</automated>
    <fails_when>non-zero exit, or any line matching "error TS" in the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test reassembly-gate-run.test.ts</automated>
    <fails_when>non-zero exit, or a "# fail" line above 0, or a "# pass 0" line</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test reassembly-gate-run.test.ts 2>&1 | grep -ac 'TREE_REBUILD: '</automated>
    <fails_when>a count below 1 — the baseline rebuild outcome line must be printed at column zero</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test reassembly-gate-run.test.ts 2>&1 | grep -ac 'MOVEMENT_REBUILD: '</automated>
    <fails_when>a count below 1 — the relocated rebuild outcome line must be printed at column zero</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test reassembly-gate-run.test.ts 2>&1 | grep -ac 'HAZARD_DISPOSITION: '</automated>
    <fails_when>a count below 1 — the hazard disposition outcome line must be printed at column zero</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test reassembly-gate-run.test.ts 2>&1 | grep -ac 'DIFF_SCOPE_COVERAGE: '</automated>
    <fails_when>a count below 1 — the diff-scope coverage outcome line must be printed at column zero</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node -e 'const p=require("./package.json");process.exit(p.files.includes("reassembly-gate-run.test.ts")?1:0)'</automated>
    <fails_when>non-zero exit — the run harness is test-only and must not appear in the published file list</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && ! grep -aE '\.planning/|ROADMAP\.md|[0-9]{2}-[0-9]{2}-PLAN' reassembly-gate-run.test.ts</automated>
    <fails_when>non-zero exit — a source file under the server tree carrying this project's planning bookkeeping, which a consumer of the published package cannot resolve</fails_when>
  </verify>
  <acceptance_criteria>
    - The harness prints exactly the four in-process line names, spelled as the schema declares them, each once, as a bare line at column zero.
    - Every printed value is a member of its declared value set, asserted in the case.
    - The acknowledgement array is a frozen constant with one entry per finding, each carrying a non-empty reason, and the harness does not add an entry when the disposition comes back blocked.
    - The rendered per-finding acknowledgement lines are printed beneath a heading and are visibly distinct from the outcome lines.
    - No assertion in the file requires any token to hold a particular value, and a comment states why.
    - The movement context reports two different addresses whenever the movement outcome is not the refused token.
    - The harness reuses the movement subject from the movement plan rather than declaring a second one.
    - The file is absent from the published file list and carries no planning-tree path, roadmap reference or plan filename.
  </acceptance_criteria>
  <done>One command produces the gate's four in-process inputs from real machinery and prints them in the frozen vocabulary, asserting their shape and never their value.</done>
</task>

<task type="auto">
  <name>Task 2: The five evidence files — transcribe the run, the observed controls, the guards and the ordering proof</name>
  <files>.planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-tree-rebuild.md, .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-movement-rebuild.md, .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-hazard-disposition.md, .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-red-controls.md, .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-guards.md</files>
  <read_first>
    - `.planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/SCHEMA.md` — the line-to-file assignment table and the absence rules. Each file written here declares exactly the lines that table assigns to it and no others.
    - `.planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/39-idle-coexist.md` — a measurement evidence file from the precedent gate: how the raw observation, the command that produced it and the derived outcome line sit together, and how an accepted limit is recorded when something could not be measured.
    - `src/mcp/vice/acme-verify.test.ts` and `src/mcp/vice/reassembly-gate.test.ts` and `src/mcp/vice/reassembly-gate-movement.test.ts` — the names of the planted-control cases, so the red-controls evidence cites case names verbatim rather than describing them.
    - `src/mcp/vice/acme-seam.test.ts` — the two frozen set names and the accepted-limit note the guard records about its two pre-existing comparison members, so the guards evidence states the guard's scope exactly as the guard states it.
  </read_first>
  <action>
    Run the harness and redirect its output to a file, then read the file. Do not pipe a test
    command and read its exit code in the same statement — the pipeline reports the last stage's
    status and this project has a recorded incident from exactly that mistake.

    Transcribe the four printed outcome lines verbatim into their assigned evidence files, each
    accompanied by the exact command that produced it, the date, and the raw excerpt of the output
    the line was read from. Do not paraphrase a value and do not round one.

    In the tree-rebuild file, record the baseline rebuild and the diff-scope coverage: the subject,
    how many files the tree carried, the byte length compared, and the half-open extent the scope
    check ran against. In the movement-rebuild file, record the moved symbol, the delta, both
    addresses and the byte length compared at the new layout. In the hazard-disposition file,
    record the finding count, the undecided-region count, the disposition token and the full
    rendered per-finding acknowledgement list, so the acceptance is visible in the evidence and
    not only in the source that produced it.

    In the red-controls file, run the three test files carrying the planted controls, redirect
    their output to a file, and record which named cases were observed passing — a planted
    control's case passing IS the observation that the gate went red on it, because the case is
    what asserts the red. Name every case individually rather than reporting a count. Derive the
    red-controls outcome line: the all-observed value only when every planted control the phase
    criteria name was observed in that same run, and a lower value otherwise with the missing
    control named.

    In the guards file, record two things. First, run the seam guard, redirect its output, record
    the guard outcome line, and record the accepted limit the guard itself states about its two
    pre-existing comparison members — the evidence must not read as a stronger claim than the
    guard makes. Second, establish the ordering proof from git history rather than from any
    document's claim about itself: find the commit that added the rule table, list the files that
    commit touched, and record that it contains the rule table and the schema and nothing that
    measured anything. Record the commit identifier and the file list verbatim, then derive the
    ordering outcome line. If that commit contains anything else, record the breached value and
    leave the finding as it is.

    Where any measurement could not be taken, record it as an explicit accepted limit inside that
    file, with the reason, and derive the input's value under the schema's own absence rule rather
    than leaving the line out or inventing a value.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && test "$(cat .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/49-*.md | grep -acE '^(TREE_REBUILD|MOVEMENT_REBUILD|HAZARD_DISPOSITION|DIFF_SCOPE_COVERAGE|RED_CONTROLS|SECOND_PATH_GUARD|ORDERING_PROOF): ')" = "7"</automated>
    <fails_when>non-zero exit — the seven declared outcome lines must appear exactly once each at column zero across the five evidence files; a different total means an input is missing, duplicated, or written at the wrong indentation</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/.planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence && test -n "$(git log --diff-filter=A --format=%H -- DECISION-RULE.md)"</automated>
    <fails_when>non-zero exit — the rule table has no add-commit in git history, so the ordering the whole gate rests on cannot be established at all</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && git show --name-only --format= "$(git log --diff-filter=A --format=%H -- .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/DECISION-RULE.md | tail -1)" > /tmp/claude-1000/rules-commit-files.txt</automated>
    <fails_when>non-zero exit, or an empty output file — the rule table's add-commit must be resolvable and must list the files it touched</fails_when>
    <automated>test "$(grep -ac 'evidence/49-' /tmp/claude-1000/rules-commit-files.txt)" = "0"</automated>
    <fails_when>a count above 0 — a measurement evidence file inside the commit that added the rule table means the rules and a measurement landed together, which is the one property this whole phase exists to establish</fails_when>
    <automated>test "$(grep -ac 'docs/phase49-' /tmp/claude-1000/rules-commit-files.txt)" = "0"</automated>
    <fails_when>a count above 0 — the verdict document inside the rule table's own add-commit means the verdict predates or accompanies its own rules</fails_when>
  </verify>
  <acceptance_criteria>
    - Each of the five evidence files declares exactly the outcome lines the schema's assignment table gives it, at column zero, and no others.
    - Every outcome line is accompanied by the exact command that produced it, the date it was run, and the raw output excerpt the value was read from.
    - The hazard-disposition file carries the full rendered per-finding acknowledgement list, not a count and not a summary.
    - The red-controls file names every planted-control case individually and derives its outcome line from whether all of them were observed in one run.
    - The guards file records the seam guard's own accepted limit about its two pre-existing comparison members, in terms no stronger than the guard states.
    - The guards file records the rule table's add-commit identifier and the verbatim list of files that commit touched, and derives the ordering line from that list.
    - No test command's exit code is read through a pipe anywhere in this task's work.
    - Any measurement that could not be taken is recorded as an explicit accepted limit with its reason, and its input value is derived under the schema's absence rule.
  </acceptance_criteria>
  <done>All seven gate inputs exist as recorded, sourced, dated measurements, and the ordering property is established from what the rule table's own commit actually contained.</done>
</task>

<task type="auto">
  <name>Task 3: The verdict document — evaluate the frozen rules against the recorded inputs and publish what they say</name>
  <files>docs/phase49-the-reassembly-gate-findings.md</files>
  <read_first>
    - `.planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/DECISION-RULE.md` — the ordered rules. Evaluate them in order against the recorded inputs and take the first that matches. Do not skip a rule because a later one looks like a better fit.
    - `.planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/SCHEMA.md` — the verdict document's declared frontmatter key order and the requirement that every value carry a citation to the evidence file and line it was read from.
    - `docs/phase39-dual-channel-coexistence-gate-findings.md` lines 1-40 — the precedent verdict document: its frontmatter shape, its inline per-input citations, and how the body explains the rule that fired without restating the rules themselves.
    - The five evidence files this plan's previous task wrote — the inputs, read from the files rather than from memory of the run.
  </read_first>
  <action>
    Read each of the seven inputs out of its declared evidence file, taking the final occurrence
    of its line at column zero, exactly as the schema's absence and final-occurrence rules say.
    Note the line number each value came from, because the frontmatter cites it.

    Evaluate the rule table in order and take the first rule that matches. Record its id.

    Write the verdict document with the declared frontmatter key order: the phase slug, the
    requirement id list, the date of the probe, the verdict token, the id of the rule that fired,
    and one input key per declared line name carrying its value with an inline comment citing its
    evidence file and line number. No value may appear without that citation.

    Write the body in three parts. First, what was measured: the subject, the assembler and its
    release, the moved symbol and the delta, and the number of hazard findings and undecided
    regions — facts, with their evidence file named beside each. Second, why the verdict is what
    it is: name the rule that fired and state, in one or two sentences, which input values made it
    the first match. Do not restate the rule table and do not argue with it. Third, what the next
    phase may rely on: the verdict token and the rule id are the machine-readable precondition; a
    verdict other than the green token means the equivalence work reads a gate that did not pass
    cleanly, and the document says so plainly rather than burying it.

    If the verdict is not green, record it as the finding. Do not re-run a measurement with
    different inputs, a different subject or a narrowed scope to obtain a better one. If a rule is
    found to be wrong during this evaluation, record an explicit override section in this document
    naming the rule, what it produced, what it should have produced and why — and leave the frozen
    rule table unedited, so a reader sees both the rule and the departure from it.

    Add a short ordering section quoting the rule table's add-commit identifier and the file list
    from the guards evidence, so a reader can confirm the ordering property from this document
    without re-deriving it.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && head -30 docs/phase49-the-reassembly-gate-findings.md | grep -ac '^verdict: '</automated>
    <fails_when>a count other than 1 — the verdict token must appear exactly once as a frontmatter key, so the next phase can read it without parsing prose</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && head -30 docs/phase49-the-reassembly-gate-findings.md | grep -ac '^verdict_rule_applied: '</automated>
    <fails_when>a count other than 1 — the rule that fired must be recorded as its own frontmatter key</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && test "$(grep -ac 'evidence/49-.*\.md:[0-9]' docs/phase49-the-reassembly-gate-findings.md)" -ge 7</automated>
    <fails_when>a count below 7 — every one of the seven inputs must carry an inline citation naming its evidence file and a line number; a value with no traceable source is not acceptable</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/.planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence && git diff --quiet HEAD -- DECISION-RULE.md SCHEMA.md</automated>
    <fails_when>non-zero exit — the frozen rule table or schema has uncommitted edits, which means a pre-commitment was adjusted while its own measurement was being written</fails_when>
    <human-check>Read the verdict document top to bottom. Confirm the rule named in the frontmatter is the first rule in the committed table that matches the recorded inputs, that the body does not argue with that rule, and — if the verdict is not the green token — that the document says so plainly in its first paragraph rather than only in the frontmatter.</human-check>
  </verify>
  <acceptance_criteria>
    - The frontmatter carries the declared key order and exactly one verdict key and one rule-id key.
    - Every one of the seven input values carries an inline citation naming its evidence file and a line number.
    - The recorded rule id is the first rule in the committed table that matches the recorded inputs.
    - The body names the rule that fired and which input values made it the first match, and does not restate or argue with the rule table.
    - The body states what the next phase may rely on, and says plainly in prose when the verdict is anything other than the green token.
    - The frozen rule table and schema have no uncommitted edits, and any rule found wrong is recorded as an override section in this document instead.
    - An ordering section quotes the rule table's add-commit identifier and the file list recorded in the guards evidence.
  </acceptance_criteria>
  <done>The verdict exists as a machine-readable document naming the rule that produced it, every input traceable to a line of recorded evidence, and the frozen rules unedited.</done>
</task>

</tasks>

<!-- planner-discipline-allow: evidence/49- -->
<!-- planner-discipline-allow: docs/phase49- -->
<!-- planner-discipline-allow: \.planning/|ROADMAP\.md|[0-9]{2}-[0-9]{2}-PLAN -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the run's raw output → the recorded evidence lines | A transcription step where a paraphrase would replace a measurement. |
| the recorded inputs → the rule table → the published verdict | The consequential boundary: the next phase treats this document as a precondition. |
| git history → the ordering claim | The ordering is a property of commits; a document asserting its own priority proves nothing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-23 | Repudiation (false assurance) | the verdict derivation | high | mitigate | A verdict judged rather than derived, or derived from an edited rule, would make every gate after this one advisory. Mitigated by evaluating the frozen table in order and recording the rule id, by a verify command failing when the rule table or schema has uncommitted edits, by requiring a wrong rule to be recorded as an override rather than fixed in place, and by the reader confirming the first-match property. |
| T-49-24 | Tampering | the transcription from run output to evidence | high | mitigate | A paraphrased or rounded value silently replaces the measurement. Mitigated by recording the exact command, the date and the raw output excerpt beside every derived line, and by a verify command counting the seven column-zero lines across the five files so a missing or misindented one fails. |
| T-49-25 | Repudiation (false assurance) | a red verdict re-measured until it turns | high | mitigate | Re-running with a different subject or a narrowed scope until the verdict improves is the failure this whole phase legislates against. Mitigated by the recorded prohibition, by the subject and delta being named in the evidence so a substitution is visible in the diff, and by the override mechanism giving a legitimate disagreement somewhere to go that is not a quiet edit. |
| T-49-26 | Spoofing | the ordering proof | high | mitigate | A document claiming its own priority is not evidence. Mitigated by deriving the ordering from the rule table's actual add-commit, by two verify commands asserting that commit contains no measurement evidence file and no verdict document, and by quoting the commit identifier and file list into both the evidence and the verdict document. |
| T-49-27 | Information Disclosure | the captured run output written to a temporary path | low | accept | The captured output contains assembler diagnostics and byte counts over a committed synthetic fixture. No secret, no host path outside the workspace and no user data is involved, and the file is scratch rather than an artifact. |
| T-49-SC | Tampering | npm / pip / cargo installs | high | accept | No package-manager install task exists in this plan. The one external tool is the assembler, which this project detects and never installs; its absence is recorded as the no-assembler token rather than resolved by installing anything. |
</threat_model>

<verification>
- The seven declared outcome lines exist exactly once each at column zero across the five evidence files, each with its command, date and raw excerpt.
- The rule table's add-commit contains the rule table and the schema and nothing that measured anything.
- The verdict document's frontmatter carries one verdict token, one rule id, and a citation for every input.
- The frozen rule table and schema are unedited.
- The suite is green when run through the project's automated driver, with its exit code read directly rather than through a pipe.
</verification>

<success_criteria>
The gate has been run against a real subject with a real assembler, its seven inputs are
recorded in the vocabulary frozen before the run, and the verdict — whatever it turned out to
be — is published as a document naming the rule that produced it, with every value traceable
to a line of evidence and the ordering established from git rather than from prose.
</success_criteria>

<output>
Create `.planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/49-07-SUMMARY.md` when done
</output>
