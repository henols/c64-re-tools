---
phase: 28-the-store-core
plan: 11
type: execute
wave: 9
depends_on: [28-10]
files_modified:
  - src/mcp/vice/anno-store.ts
  - src/mcp/vice/anno-store.test.ts
autonomous: true
gap_closure: true
requirements: [STORE-04, STORE-07]

estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "STORE-04 concurrency: a snapshot file a concurrent writer has PUBLISHED but not yet COMMITTED is never deleted by another connection's sweep — the sweep either holds the store's write lock while it decides, or it does nothing and says so"
    - "`reconcileSnapshotRing` reports what it did in three fields — `droppedRows`, `droppedFiles` and `deferred` — so a caller and a test can assert that a sweep declined to judge, rather than inferring it from an absence"
    - "Inside `reconcileSnapshotRing` the pointer-row deletes are COMMITTED before any file is unlinked, so an interrupted sweep leaves extra files and never a pointer row aimed at a deleted file — trap 10's rule, now enforced in the second place that deletes rows as well as in `pruneSnapshots`"
    - "The module still contains exactly ONE commit statement: the sweep commits through `commitTransaction`, so the single planted-violation site the durability proof depends on stays unique"
    - "WR-01: a throw anywhere between the staged snapshot and the pointer-row insert leaves NO open transaction with the compare-and-swap applied, NO staged `.tmp` file, and an error inside the `ViceError` family — proven with a deterministic planted primary-key collision on the pointer-row insert"
    - statement: "WR-02: a `pruneSnapshots` failure after a successful `commitTransaction` never reports the committed write to the caller as a failure — the ring is left un-pruned, which is the harmless direction, and the next accepted write's sweep reconciles it"
      verification: backstop
    - statement: "WR-04: a throw from `pragma integrity_check` closes the connection and refuses inside the `ViceError` family rather than leaking the connection and escaping the family"
      verification: backstop
  artifacts:
    - "src/mcp/vice/anno-store.ts — a `reconcileSnapshotRing` that serialises its judgement under `begin immediate`, commits its row deletes through `commitTransaction`, and unlinks only afterwards"
    - "src/mcp/vice/anno-store.ts — a guarded staging→publish→pointer-insert window in `runWriteSequence`, a guarded `pragma integrity_check` in `openStore`, and a guarded step-9 prune call"
    - "src/mcp/vice/anno-store.test.ts — the two-connection publish-to-commit interleaving test, the WR-01 primary-key-collision test, and one structural control covering the two remaining unguarded call sites"
  key_links:
    - "`reconcileSnapshotRing` → `begin immediate` → `commitTransaction` → the unlink pass — the sweep's own transaction, opened and closed inside the function, never nested"
    - "`publishSnapshot` → the won compare-and-swap → the same write lock the sweep takes — the exclusion that makes a published-uncommitted file unobservable to a sweep"
    - "`runWriteSequence` step 9 → `pruneSnapshots` → `reconcileSnapshotRing` — the one production call site, now wrapped so a prune failure cannot un-report a committed write"
  prohibitions:
    - statement: "MUST NOT destroy the only remaining route back to a state the store still advertises as reachable. A reconciliation that resolves a half-state by deleting the surviving half in the addressable direction is the loss this gap exists to close, not a repair of it. (carried forward: 28-07 P1, recorded VIOLATED in round 2 — reproduced twice, by the reconciliation written to govern it)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT publish a floor, bound or 'retained' claim the store cannot honour on the very next call — in EITHER direction. (carried forward: 28-07 P2, recorded PARTIALLY HELD in round 2)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT substitute a tuned timing constant for an exact exclusion when the exact exclusion is already available from a lock the code takes anyway. A grace period is a guess about how slow a writer can be; the write lock is the answer, and the next defect would arrive as 'just raise the grace period'."
      status: unverified
      verification: judgment
    - statement: "MUST NOT let a repair judge a state it cannot have produced. The sweep may only delete a file inside a ring that only this store publishes into, at a moment when no other writer can be mid-publication — and if it cannot establish that moment, it must decline and report the declining rather than guess."
      status: unverified
      verification: judgment
    - statement: "MUST NOT convert a committed write into a caller-visible failure. Once the commit has returned, the write happened; a later housekeeping failure that throws makes a caller retry an additive verb and produce a second row."
      status: unverified
      verification: judgment
---

<objective>
Close the last third of verification gap 1: **who may judge an unclaimed
snapshot file.** Plan 28-10 settled which store owns a ring and how a revision's
file is located; this plan settles the one remaining question, which
`28-VERIFICATION.md` states as *"who may judge an unclaimed file, which is
nobody who cannot have created it"* — and folds in the three warnings that live
inside the exact code being rewritten.

CR-02, reproduced: `reconcileSnapshotRing` runs on every accepted write and
decides ownership from **this connection's committed view only**. A concurrent
writer's snapshot is a filesystem fact (the rename) before it is a transactional
one (the pointer row), so for the width of that window the file looks unowned and
the sweep deletes it. The destroyed revision then becomes permanently
unrevertible — *"manufacturing the exact orphan state trap 10 declares
unsurvivable"*, and doing it from the reconciliation that exists to prevent it.

Purpose: this is the one of the three ring defects that is a *regression
introduced by the previous gap-closure round*. 28-07 P1 is recorded **violated**
against it. Leaving it open would ship, behind a Phase 29 MCP tool family, a
store that silently loses a revision under ordinary write contention.

Output: a sweep that decides under the same write lock a publication is already
ordered by — so a published-but-uncommitted file is unobservable to it by
construction rather than by timing — that commits its row deletes before it
unlinks anything, and that reports when it declined to judge; plus guarded
versions of the three unguarded regions (WR-01, WR-02, WR-04) that sit inside the
same code.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-the-store-core/28-VERIFICATION.md
@.planning/phases/28-the-store-core/28-08-SUMMARY.md
@.planning/phases/28-the-store-core/28-10-PLAN.md
@src/mcp/vice/anno-store.ts
</context>

<gap_closure_source>
Closes the remaining third of **gap 1** of `28-VERIFICATION.md` (truth 7;
requirement `STORE-04` BLOCKED), specifically review finding **CR-02** and gap
1's `missing:` remedy **2**:

2. *"Make the sweep unable to judge a file it cannot have created — age it against a grace bound comfortably longer than the publish-to-commit window, or serialise it under `begin immediate` — with a two-connection test planting the exact interleaving"*

Also folds in the three `## Anti-Patterns Found` warnings that live inside the
regions this plan rewrites: **WR-01** (`anno-store.ts:805-848`, 43 unguarded
lines), **WR-02** (`:870-877`, the unguarded post-commit prune) and **WR-04**
(`:394`, the unguarded `pragma integrity_check`). The verification did not gate
on any of them; the fold-in is a deliberate decision recorded below, not scope
creep.
</gap_closure_source>

<design_the_property_not_the_repro>
**The remedy chosen, and why the other offered one is refused.** Gap 1's remedy 2
offers two routes. This plan takes the second — **serialise under `begin
immediate`** — and refuses the first, on the record.

A grace bound is a guess about how long a writer can sit between its rename and
its commit. It has a tuned constant, it is wrong for a writer that is paged out
or stopped at a debugger, and the next reproduction of this defect would arrive
as a request to raise the constant. That is precisely the "closed the specific
reproduction, the property stayed false one step to the side" pattern the
verification named. The prohibition against it is carried in `must_haves` so the
next verification can rule on it.

**The exclusion is exact, and it is exact by derivation rather than by testing.**
A writer can publish a snapshot only *after* winning the compare-and-swap, and
the compare-and-swap runs inside `begin immediate` — so a writer that is
published-but-uncommitted **holds the store's write lock**. A sweep that takes
`begin immediate` before it reads anything therefore cannot run at all while any
writer is inside that window: either the sweep gets the lock first (and no
publication can begin until it releases it), or the writer has it (and the sweep
waits, after which the pointer row is committed and visible). The window and the
lock are the same interval. Nothing is timed and no constant is tuned.

**The adjacent (fourth) case, and why the design makes it unreachable.** Ask what
else could put a file into this store's ring that the sweep would see as
unowned:

- *A writer in another OS process.* Same store file, same SQLite write lock —
  the exclusion is cross-process, which is the level SQLite's lock operates at
  and the level this module already relies on for its cross-process CAS proof.
- *A writer that publishes without winning the CAS.* Not reachable: 28-08 made
  `publishSnapshot` the only rename site and put it between the won CAS and the
  pointer insert; `anno-seam.test.ts` bounds the seam-private exports so no
  shipped module can stage from outside.
- *A neighbouring store publishing into this ring.* Made structurally impossible
  by 28-10 — distinct store files have distinct ring directories.
- *A staging `.tmp` mid-`vacuum`.* Invisible to the sweep by the deliberate
  anchoring of `SNAPSHOT_FILE_PATTERN` on `r<digits>.db`, which 28-08 documented
  as a contract between the two functions. Unchanged here.
- *The sweep interrupted between its own row delete and its own unlink.* This is
  the one genuinely new hazard the transaction introduces, and it is closed by
  ordering rather than left: the sweep commits its row deletes and only then
  unlinks, so an interruption leaves extra files — the harmless direction trap 10
  already argues for — and never a row aimed at a deleted file.

That enumeration is closed: the only way a file reaches `<store>.snapshots/rN.db`
is `publishSnapshot`, and `publishSnapshot` is reachable only from behind the
lock the sweep now takes. **The judge cannot see a file it could not have
created, because while it is judging, nobody else can create one.**

**The honest residual.** A sweep that cannot take the lock within the
connection's five-second `busy_timeout` **declines**: it returns
`{ droppedRows: [], droppedFiles: [], deferred: true }` and changes nothing. The
consequence is that the ring may temporarily exceed `MAX_SNAPSHOT_REVISIONS`
files until the next accepted write sweeps successfully. That is the extra-files
direction, which trap 10's own premise calls harmless and reconcilable by
revision number, and it is reported rather than silent. Declining is required by
28-07 P2 read in its second direction: a sweep that guesses when it cannot know
would be publishing a judgement it cannot support.
</design_the_property_not_the_repro>

<warning_foldin_decision>
**Stated explicitly, as the orchestrator required.** WR-01 is folded into this
plan rather than deferred, because this plan already rewrites
`runWriteSequence`'s staging→publish→insert region for CR-02's ordering and the
guard is a `try`/`catch` around code that is being touched anyway. Folding it
elsewhere would mean editing the same 43 lines twice, in two plans, with the
second edit reasoning about the first from a SUMMARY.

WR-02 is folded in for the same reason at one remove: its subject is the step-9
`pruneSnapshots` call, which is the single production caller of the function this
plan rewrites, and the failure it describes (`SQLITE_BUSY` on the post-commit
autocommit deletes) becomes *more* likely once the sweep takes a lock. Leaving it
would mean shipping a new lock-taking path under an unguarded call.

WR-04 is one line in `openStore` and is included because it is the single
remaining `ViceError`-family escape the phase knows about; leaving one known
escape open while writing a plan whose whole subject is error discipline would be
a strange trade.

**What is NOT folded in:** the eight remaining `28-REVIEW.md` warnings (WR-03,
WR-05..WR-10) and the four info items, which the verification re-read and found
falsify no must-have and no success criterion. And the named residual — backstop
D11's link 3 being unproven under *power loss* as distinct from `SIGKILL` — is
recorded as a known limit in the SUMMARY and deliberately NOT attempted: it is
outside the phase's stated failure model and the verification explicitly says it
does not gate the phase.
</warning_foldin_decision>

<edge_coverage_lift>
This plan authors edge item **16** (`STORE-04` / `concurrency` — *"If interrupted
or run in parallel, what is guaranteed?"*) as an **explicit** truth: the first
`must_haves.truths` entry. That item is the direct statement of CR-02.

The remaining 20 items are accounted for in `28-10-PLAN.md`'s
`<edge_coverage_accounting>` table, which carries the whole-plan-set
no-silent-drop equality (21 == 8 authored + 13 explicitly recorded as already
discharged). Nothing is dropped here.

Two `must_haves.truths` entries in this plan carry `verification: backstop`
(WR-02 and WR-04) because neither failure is deterministically constructible
in-process — WR-02 needs another connection to acquire the write lock in the
instant between one connection's commit and its next statement, and WR-04 needs a
genuinely corrupt SQLite page. Both are pinned by a source-structural control with
a reddenable planting, following 28-08's own established pattern for this exact
situation; a verifier that cannot confirm them with explicit evidence must abstain
rather than pass them silently.
</edge_coverage_lift>

<artifacts_this_phase_produces>
See `28-10-PLAN.md` → `<artifacts_this_phase_produces>` for the full
cross-plan list. This plan's own additions:

**New in `src/mcp/vice/anno-store.ts`:**
- `deferred` — a third field on `reconcileSnapshotRing`'s returned object, type `boolean`

**New test names in `src/mcp/vice/anno-store.test.ts`:**
- `"CR-02: a sweep cannot delete a snapshot a concurrent writer has published but not yet committed -- the sweep either holds the write lock or does nothing"`
- `"the sweep's SOURCE ORDER is the guarantee too: inside reconcileSnapshotRing the pointer-row delete precedes the commit, and the commit precedes the unlink"`
- `"WR-01: a throw between the staged snapshot and the pointer-row insert leaves no open transaction, no staged .tmp, and an error inside the ViceError family"`
- `"WR-02 and WR-04, STRUCTURAL: the two remaining unguarded calls are inside handlers"`

**New files:** none.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The judge rule — the sweep decides under the store's write lock, commits its rows, then unlinks</name>
  <files>src/mcp/vice/anno-store.ts, src/mcp/vice/anno-store.test.ts</files>
  <precondition>Plan 28-10 has landed: `snapshotDirFor` exists, `anno_snapshot` has one column, and `SCHEMA_VERSION` is 2. Assert by opening a fresh store and checking `pragma table_info(anno_snapshot)` returns one row named `revision`; halt if it does not, because this task's sweep rewrite reads `snapshotDirFor(handle)` and would otherwise be editing the pre-identity code.</precondition>
  <read_first>
    - src/mcp/vice/anno-store.ts lines 105-128 (header trap 10 — its conclusion and its "same premise, same decision" clause are the rule this task extends to a second site)
    - src/mcp/vice/anno-store.ts (`reconcileSnapshotRing` in full, including its doc comment's "ITS CALL SITES ARE EXACTLY TWO" paragraph and the reason `openStore` is deliberately not one of them)
    - src/mcp/vice/anno-store.ts (`pruneSnapshots` in full, and `commitTransaction`)
    - src/mcp/vice/anno-store.ts (`runWriteSequence` — specifically that `publishSnapshot` sits between the won CAS and the pointer insert, both inside `begin immediate`)
    - src/mcp/vice/anno-store.ts (`revertTo` step 6 — the second call site, on the NEW handle)
    - src/mcp/vice/anno-store.test.ts (the test named `"the prune's SOURCE ORDER is the guarantee"` — the instrument shape to copy: extract the function body from `codeOnly(src, true)`, assert a body-length non-vacuity floor, assert every statement is PRESENT before comparing indices)
    - src/mcp/vice/anno-store.test.ts (the tests named `"prune half-state A, the orphan ROW"`, `"prune half-state B, the orphan FILE"` and `"STORE-04 idempotency across the half-states"` — all three call `reconcileSnapshotRing` directly and assert on its return object)
    - src/mcp/vice/anno-seam.test.ts (the test named `"the seam contains exactly one commit statement"` — why the sweep must commit through `commitTransaction` and never through a second `exec` of the bare word)
  </read_first>
  <behavior>
    - `reconcileSnapshotRing(handle)` returns `{ droppedRows: number[]; droppedFiles: string[]; deferred: boolean }`.
    - On a quiet store it behaves exactly as today: orphan rows dropped, orphan files unlinked, `deferred` false. The three existing half-state tests keep passing with only the added field.
    - With a second connection holding the write lock (a writer between its publish and its commit), the call returns `deferred: true`, `droppedRows: []`, `droppedFiles: []`, and the concurrent writer's published file is still on disk.
    - Inside the function, every pointer-row delete is committed before any `rmSync` runs.
    - Calling it from inside an already-open transaction is not supported and is documented as such: both production call sites are outside one.
  </behavior>
  <action>
Rewrite `reconcileSnapshotRing` so that the sweep can only judge from a position
where nothing can be mid-publication, and so that an interrupted sweep fails in
the harmless direction.

**Structure, in this order:**

1. Take the store's write lock with `handle.db.exec("begin immediate")` inside a
   `try`. On failure — the only expected one is `SQLITE_BUSY` after the
   connection's five-second `busy_timeout`, which means another writer holds the
   lock — return `{ droppedRows: [], droppedFiles: [], deferred: true }` and do
   nothing else. Do not throw, and do not partially sweep.
2. With the lock held, read `retainedRevisions(handle)` (unchanged — it is still
   THE one predicate, and this function still must not re-decide with a second
   `existsSync`), read the pointer rows, and read the directory listing from
   `snapshotDirFor(handle)`. Compute the full drop set for BOTH directions before
   changing anything: the orphan row revisions, and the orphan file paths.
3. Delete the orphan pointer rows.
4. Commit through `commitTransaction(handle.db)`. **It must be
   `commitTransaction`, never `handle.db.exec` of the bare word:**
   `anno-seam.test.ts` asserts the module contains exactly ONE commit statement,
   because the durability proof's planted violation must have a single site. A
   second literal would split that planting and let half of it survive.
5. Only now, with the rows durably gone, unlink the orphan files — each `rmSync`
   with `force: true` inside a swallowing `try`, and only an unlink that actually
   happened reported in `droppedFiles`, exactly as today. Return with
   `deferred: false`.

**The absent-directory case** (a store that has never been written has no ring
directory, and `readdirSync` throws on an absent one) stays a non-half-state and
must still return early — but it must return AFTER the transaction is properly
closed, so move the check inside step 2 and let the function fall through to the
commit rather than returning from inside an open transaction.

**Doc-comment work, and it is not optional here.** The existing doc comment
argues at length for the two directions and names its two call sites. Extend it
in place, per this module's recorded discipline of keeping the premise that was
right and recording the reversal:

- Add the paragraph that this function now takes the store's write lock BEFORE it
  decides, and say why in one sentence: a snapshot becomes a filesystem fact (the
  rename) before it becomes a transactional one (the pointer row), so a sweep
  reading only its own committed view sees a live writer's file as unowned and
  deletes it — reproduced as CR-02, and the deletion made that revision
  permanently unrevertible.
- Say why the lock is the exact instrument rather than a timing heuristic: the
  publish-to-commit window and the write-lock hold are the same interval, because
  publication is gated on the won compare-and-swap.
- Say what `deferred` means and why declining is correct rather than
  best-effort.
- Add the sentence that the row deletes are committed before any unlink, for the
  same reason `pruneSnapshots`' loop deletes the row first: a sweep interrupted
  between the two must leave extra files and never a pointer row aimed at a
  deleted file.
- Keep the "ITS CALL SITES ARE EXACTLY TWO, AND `openStore` IS DELIBERATELY NOT
  ONE OF THEM" paragraph intact, and add that both sites are outside any open
  transaction — which is now a requirement rather than an incidental fact.

**In `pruneSnapshots`:** no logic change beyond what 28-10 already did. Its own
loop keeps its row-then-file order and its source-order control must stay green.
Add one sentence to its doc comment noting that its first statement now takes and
releases a transaction, so `pruneSnapshots` itself must not be called from inside
one.

**Tests, written FIRST (`tdd="true"`) — this is the two-connection test gap 1's
remedy 2 names:**

*The CR-02 interleaving.* One temp directory, one store, two connections. Open
connection A and connection B on the same store file. Write twice through A so
the ring holds revisions 0 and 1. Then use `applyWriteWithoutCommit` on B — the
existing seam-private export, which the verifier used for exactly this and whose
purpose is to hold a writer deterministically between its `renameSync` and its
`commit`. Assert as a non-vacuity pin that B's published file for the current
revision EXISTS on disk and that no committed pointer row claims it yet. Then
call `reconcileSnapshotRing(A)`, timing it. Assert three things: the elapsed time
is at least 1000 ms (proving A genuinely blocked on the lock rather than
completing for an unrelated reason); the returned object reports
`deferred: true`, `droppedRows: []` and `droppedFiles: []`; and B's published file
is STILL on disk. Close B in a `finally` — closing a connection with an open
transaction rolls it back.

State in the test's own comment that it takes roughly five seconds by design,
because the connection's `busy_timeout` is five seconds and the whole point is
that A waits rather than guessing, so a reader does not mistake it for a hang.

**How it goes RED:** against the pre-change sweep this reproduces the verifier's
own output — `droppedFiles: [ 'r2.db' ]`, B's file gone, revision 2 permanently
unrevertible. Observe that red before implementing, by running the new test
against the current `reconcileSnapshotRing`, and record the output verbatim. The
elapsed-time half also reddens on its own if a future edit removes the
`begin immediate` while keeping the drop-set computation, which is the most likely
way this repair would be undone.

*The sweep's source order.* Add a second source-order control modelled exactly on
the existing prune one: extract `reconcileSnapshotRing`'s body from
`codeOnly(anno-store.ts, true)`, assert a body-length non-vacuity floor, assert
that the row delete, the `commitTransaction` call and the `rmSync` are each
PRESENT, and only then assert their indices are in that order. **How it goes RED:**
moving the unlink pass above the commit — the single most plausible "tidying"
edit — flips it, exactly as the prune control does for its own pair.
  </action>
  <verify>
    <automated>cd src/mcp/vice && node --test anno-store.test.ts anno-seam.test.ts anno-durability.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/mcp/vice/anno-store.ts` contains `deferred` in `reconcileSnapshotRing`'s return type annotation, and `commitTransaction(handle.db)` inside that function's body.
    - `cd src/mcp/vice && node --test anno-seam.test.ts` exits 0 — the exactly-one-commit-statement assertion still passes, proving the sweep commits through `commitTransaction` and did not introduce a second bare commit.
    - The CR-02 test passes. **How it goes RED:** its pre-change run reported `droppedFiles: [ 'r2.db' ]` and B's file absent — the verifier's own reproduction — and that output is recorded verbatim in `28-11-SUMMARY.md`. Its elapsed-time assertion additionally reddens if the `begin immediate` is removed while the rest is kept.
    - The sweep source-order control passes, with its body-length floor and its three presence assertions preceding the index comparison. **How it goes RED:** moving the unlink pass above the `commitTransaction` call flips the index comparison.
    - The three existing sweep-consuming tests (`"prune half-state A"`, `"prune half-state B"`, `"STORE-04 idempotency across the half-states"`) all still pass, and the existing `"the prune's SOURCE ORDER is the guarantee"` control is unedited.
    - `cd src/mcp/vice && node --test anno-durability.test.ts` exits 0 with 4 passing — ROADMAP success criterion 4's home is unaffected by the sweep's new transaction.
    - `cd src/mcp/vice && npx tsc --noEmit -p tsconfig.json` exits 0.
  </acceptance_criteria>
  <done>
A sweep run by one connection cannot delete a snapshot another connection has
published and not yet committed; when it cannot take the lock it declines and
says so; and an interrupted sweep leaves extra files rather than a pointer row
aimed at a deleted file.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard the three unguarded regions — WR-01, WR-02, WR-04</name>
  <files>src/mcp/vice/anno-store.ts, src/mcp/vice/anno-store.test.ts</files>
  <read_first>
    - src/mcp/vice/anno-store.ts (`runWriteSequence` in full — the region from `stageSnapshot` through the pointer-row insert, and the mutation `try` that follows it)
    - src/mcp/vice/anno-store.test.ts (the test named `"WR-11, STRUCTURAL: the CAS-failure refusal carries BOTH revisions, and reads the second one BEFORE the rollback"` — it extracts the CAS-failure branch between `cas.changes` and `publishSnapshot` and asserts `rollback` is PRESENT inside it, after the `select revision from anno_meta`)
    - src/mcp/vice/anno-store.ts (`openStore` — the `pragma integrity_check` call and the two blocks around it that 28-08 already wrapped)
    - src/mcp/vice/anno-store.ts (`discardSnapshot`, and its comment about being a safe no-op on either side of the publication)
    - src/mcp/vice/anno-store.test.ts (the test named `"a refusal leaves NOTHING behind on disk"` — the `.tmp`-cleanliness reader and its planting-based non-vacuity pin, which this task's WR-01 test reuses)
    - .planning/phases/28-the-store-core/28-VERIFICATION.md (`## Anti-Patterns Found`, the WR-01/WR-02/WR-04 rows)
  </read_first>
  <behavior>
    - A write whose pointer-row insert violates the `anno_snapshot` primary key throws an error that is an instance of the module's `AnnoStoreError` family, leaves `currentRevision(handle)` at its pre-write value, leaves no `.tmp` file in the ring directory, and leaves the handle usable — a following, unobstructed `applyWrite` on the SAME handle succeeds and advances the revision by exactly one.
    - The CAS-failure refusal is unchanged: `AnnoStoreStaleRevisionError` carrying both `baseRevision` and `currentRevision`, with the second number still read before the rollback.
    - A failure inside `pruneSnapshots` after a successful commit does not propagate to the caller of `applyWrite`.
    - A failure of `pragma integrity_check` closes the connection and refuses with `AnnoStoreCorruptError` naming the path.
  </behavior>
  <action>
Guard the three regions the verification listed under `## Anti-Patterns Found`,
each with the reason recorded beside it.

**WR-01 — the staging→publish→pointer-insert window.** Wrap the region from the
`stageSnapshot` call through the pointer-row insert so that nothing in it can
escape as a bare error or leave state behind:

- Wrap the `stageSnapshot(handle, rev)` call itself in a small `try` that rethrows
  a `ViceError` unchanged and wraps anything else as `AnnoStoreError` naming the
  store path and the failed step. This is the reachable arm the test below drives:
  a regular file sitting where the ring directory should be makes `mkdirSync`
  throw, with no privilege and no race.
- Wrap `begin immediate`, the CAS `.run()`, `publishSnapshot` and the pointer-row
  insert in one outer `try`. Its `catch` performs a swallowing `rollback`, then
  `discardSnapshot(staging)`, then rethrows: a `ViceError` unchanged, anything
  else wrapped as `AnnoStoreError` naming the store path, the revision and the
  step. Wrapping rather than replacing is what keeps the family closed without
  changing any existing refusal's class or message.
- **Do NOT remove the CAS-failure branch's own `rollback` and `discardSnapshot`.**
  They look redundant under the new outer handler and they are not: the WR-11
  structural control extracts the slice between `cas.changes` and
  `publishSnapshot` and asserts `rollback` is present inside it and positioned
  after the `select revision from anno_meta` read. Deleting the inner rollback
  reddens a VERIFIED behaviour to tidy up a duplicate. The outer handler's second
  `rollback` on an already-rolled-back connection throws "no transaction is
  active", which its own swallowing `try` absorbs; the second `discardSnapshot`
  is a `force: true` no-op. State both facts in the outer catch's comment so a
  later reader does not "simplify" them away.
- The mutation `try` that follows stays exactly as it is. Do not merge it into
  the new outer one: its catch is documented as the rollback for refusals raised
  from INSIDE a mutation, which is a different case with a different comment, and
  merging them would lose the distinction the comment carries.

**WR-02 — the post-commit prune.** Wrap the step-9 `pruneSnapshots(handle)` call
in a `try` whose `catch` does NOT rethrow. Comment it fully: the commit has
already returned, so the write HAPPENED; a housekeeping failure that throws makes
the caller retry an additive verb and produce a second row, which is WR-02's exact
complaint. The consequence of swallowing is an un-pruned ring — extra files, the
direction trap 10 already argues is harmless and reconcilable by revision number —
and the next accepted write's sweep resolves it. Do not add a logging channel;
this module has none and introducing one is new surface with its own stdio
hazards.

**WR-04 — `pragma integrity_check`.** Wrap the `prepare("pragma
integrity_check").all()` call in a `try` whose `catch` closes the connection and
throws `AnnoStoreCorruptError` naming the path, matching the shape 28-08 already
used for the two blocks either side of it. The non-`ok` result branch that follows
is unchanged.

**Tests, written FIRST:**

*The WR-01 deterministic case.* Open a store, write once so the ring exists, then
plant a primary-key collision directly on the connection: insert a row into
`anno_snapshot` for the CURRENT revision, which is the revision the next write's
pointer-row insert will claim. Then call `applyWrite` with a trivial mutation and
assert all four halves: the thrown error is an instance of the module's store
error family (assert with `assert.throws` against `AnnoStoreError`, since a bare
SQLite error is what the pre-fix code produces); `currentRevision(handle)` is
unchanged, proving the CAS was rolled back; the ring directory contains no file
whose name ends in `.tmp`, reusing the `.tmp` reader from the existing
`"a refusal leaves NOTHING behind on disk"` test; and — the half that proves no
transaction was left open — after deleting the planted row, a following
`applyWrite` on the SAME handle succeeds and advances the revision by exactly one.

**How it goes RED:** against the pre-fix code all three of the last three halves
fail — the error is a bare SQLite `Error` outside the family, the transaction
stays open with the CAS applied so the revision reads advanced for a write that
was refused, and a `.tmp` is left in the ring. Observe and record which halves
failed and with what messages.

Add the reachable-arm sibling for the pre-lock half: with a regular FILE planted
at the ring directory's path, `applyWrite` throws inside the family and leaves the
store usable once the file is removed.

*The WR-02 and WR-04 structural control, honestly labelled.* One test asserting
both remaining sites are inside handlers, using the same instrument as the
existing structural controls: extract `runWriteSequence`'s and `openStore`'s
bodies from `codeOnly(anno-store.ts, true)`, assert a body-length non-vacuity
floor for each, and assert for each site that the call in question is positioned
after a `try` opening and before the matching `catch` within its own enclosing
block. State in the test's header comment WHY it is structural rather than
behavioural — WR-02 needs a second connection to acquire the write lock in the
instant between one connection's commit and its next statement, and WR-04 needs a
genuinely corrupt SQLite page, and neither is deterministically constructible
in-process — and NAME the sibling tests that pin the reachable arms behaviourally:
the ordinary accepted-write tests for WR-02's non-throwing path, and the four
corrupt-file refusal tests for WR-04's family membership. This is the pattern
28-08 established for exactly this situation and it must be followed rather than
reinvented. **How it goes RED:** removing either `try` flips the corresponding
index comparison.
  </action>
  <verify>
    <automated>cd src/mcp/vice && node --test anno-store.test.ts anno-types.test.ts anno-confinement.test.ts anno-seam.test.ts anno-index.test.ts anno-overlap.test.ts anno-durability.test.ts block-class.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All eight targeted test files run green in one `node --test` invocation, with a test count no lower than the count after 28-10 plus the 4 tests this plan adds.
    - `cd src/mcp/vice && node --test hostpath-consumers.test.ts` exits 0 with 11 passing.
    - The WR-01 test passes, and `28-11-SUMMARY.md` records which of its four halves failed against the pre-fix code and with what error text — a criterion that claims a test proves something must name how the test goes red, and this one goes red in three independent halves.
    - The WR-11 structural control (`"the CAS-failure refusal carries BOTH revisions..."`) still passes, proving the inner rollback was not removed while adding the outer handler.
    - The `"a refusal leaves NOTHING behind on disk"` test still passes with its planting-based non-vacuity pin intact.
    - `cd src/mcp/vice && node --test anno-durability.test.ts` exits 0 with 4 passing, and the SUMMARY records the reasoning that the planted violation still reddens: with `commitTransaction` no-op'd, the write sequence's transaction stays open, the sweep's own `begin immediate` fails inside it, WR-02's wrap swallows that, the mutator self-`SIGKILL`s with nothing committed, and the reopen finds revision 0.
    - `cd src/mcp/vice && npx tsc --noEmit -p tsconfig.json` exits 0.
  </acceptance_criteria>
  <done>
No path from `stageSnapshot` to the pointer-row insert can leave an open
transaction, a staged `.tmp`, or an error outside the family; a post-commit
housekeeping failure never un-reports a committed write; and the last known
family escape in `openStore` is closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| connection A ↔ connection B on one store | Two writers, possibly in two OS processes, sharing one SQLite file and one ring directory. This is where CR-02 lives. |
| the ring directory ↔ the pointer table | A filesystem fact and a transactional fact that are true at different instants; the window between them is the vulnerability. |
| a thrown error ↔ the caller | An error escaping the `ViceError` family reaches the MCP transport unclassified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-11-01 | Denial of Service | `reconcileSnapshotRing` deleting a live writer's published snapshot | high | mitigate | The sweep takes `begin immediate` before it reads anything. Publication is gated on the won compare-and-swap, which holds that same lock, so the publish-to-commit window and the lock hold are the same interval — a published-uncommitted file is unobservable to the sweep by construction. Pinned by the two-connection interleaving test with its elapsed-time non-vacuity half. |
| T-28-11-02 | Tampering | an interrupted sweep leaving a pointer row aimed at a deleted file | high | mitigate | The sweep commits its row deletes through `commitTransaction` before any `rmSync`, so an interruption leaves extra files. Pinned by a second source-order control with a body-length floor and three presence assertions. |
| T-28-11-03 | Denial of Service | a sweep that cannot take the lock | low | accept | It declines and reports `deferred: true`; the ring may temporarily exceed the bound until the next accepted write. Accepted because the alternative — guessing — is the defect being closed, and because extra files are the direction trap 10 already argues is harmless. |
| T-28-11-04 | Repudiation | an error escaping the `ViceError` family from the staging→insert window | medium | mitigate | The window is wrapped; a `ViceError` is rethrown unchanged so no existing refusal's class or message moves, anything else is wrapped as `AnnoStoreError` naming the path, the revision and the step. |
| T-28-11-05 | Tampering | an open transaction left with the compare-and-swap applied | high | mitigate | The outer catch rolls back before it rethrows, and the WR-01 test proves it by performing a second successful `applyWrite` on the same handle. |
| T-28-11-06 | Denial of Service | unbounded `.tmp` growth in the ring directory | medium | mitigate | The outer catch calls `discardSnapshot` on every failing exit. The sweep is deliberately anchored NOT to collect `.tmp` files, so an unguarded throw was the one route to unbounded growth in the directory `MAX_SNAPSHOT_REVISIONS` exists to bound. |
| T-28-11-07 | Repudiation | a committed write reported to the caller as a failure | medium | mitigate | Step 9's prune call is wrapped and does not rethrow; the caller sees the commit that actually happened. Pinned structurally, with the reason it is structural stated in the test. |
| T-28-11-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan and none is added; every import is a Node builtin or an existing intra-repo module. The package-legitimacy gate is not applicable. |
</threat_model>

<verification>
Run the eight targeted files plus the consumer set. **Do not run the full
workspace suite** (~19 minutes, with a hang in `vice-proxy.test.ts`); a truncated
run is not a test result.

```
cd src/mcp/vice
node --test anno-store.test.ts anno-types.test.ts anno-confinement.test.ts \
             anno-seam.test.ts anno-index.test.ts anno-overlap.test.ts \
             anno-durability.test.ts block-class.test.ts
node --test hostpath-consumers.test.ts
npx tsc --noEmit -p tsconfig.json
```

The CR-02 test blocks on the write lock for the connection's five-second
`busy_timeout` by design; a run of these files taking roughly five seconds longer
than before is expected, not a hang.

`node:sqlite` emits an unconditional `ExperimentalWarning` on the Node 22 line —
nothing here may assert stderr is empty.
</verification>

<success_criteria>
1. A sweep run by one connection leaves a concurrent writer's published-but-uncommitted snapshot on disk, proven by the two-connection test whose pre-change RED reproduced the verifier's `droppedFiles: [ 'r2.db' ]` output.
2. The sweep commits its row deletes before it unlinks, pinned by a reddenable source-order control.
3. The module still contains exactly one commit statement, so the durability proof's single planted-violation site is intact.
4. A throw at the deepest point of the staging→insert window leaves no open transaction, no `.tmp` and no non-family error, proven by a deterministic primary-key collision.
5. The two structurally-pinned warnings are guarded, with the reason for the structural instrument stated and the behavioural sibling named.
6. All eight targeted test files green; `hostpath-consumers.test.ts` 11/11; `tsc --noEmit` clean; `anno-durability.test.ts` 4/4.
</success_criteria>

<output>
Create `.planning/phases/28-the-store-core/28-11-SUMMARY.md` when done. It must
record: the verbatim pre-change RED output of the CR-02 test; which halves of the
WR-01 test failed pre-fix and with what error text; the reasoning that criterion
4's planted violation still reddens under the sweep's new transaction; and the
named residual carried forward unattempted (backstop D11 link 3 under power loss,
which the verification states does not gate the phase).
</output>
