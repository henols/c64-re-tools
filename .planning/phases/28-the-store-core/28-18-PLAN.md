---
phase: 28-the-store-core
plan: 18
type: execute
wave: 16
depends_on: [28-17]
files_modified:
  - src/mcp/vice/anno-seam.test.ts
  - src/mcp/vice/anno-store.ts
  - .planning/phases/28-the-store-core/28-REVIEW.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [STORE-04, STORE-05]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "TRACE: round-4 WR-15. The control that keeps the durability proof's planted violation UNIQUE counts commit STATEMENTS, not the word `commit`: a second commit site spelled as SQLite's `end` or `end transaction` synonym is counted and reddens the control, where today it passes it unchanged. Driven by a positive control over a local fixture string containing each of the three spellings, so the matcher's coverage is asserted rather than assumed."
    - "TRACE: round-4 WR-15's mirror cost + prohibition 28-07 P3. The structural invariant no longer constrains user-facing error prose: the coupling comment in `anno-store.ts` that records `the spelling is load-bearing` for the `step` value is removed together with the constraint that created it, so the next person to improve an error message cannot redden an unrelated file."
    - "TRACE: round-4 verification, Requirements Coverage table, STORE-05 row. `.planning/REQUIREMENTS.md` records STORE-05 as `Complete`, transcribing the verifier's own verdict that the `Gaps Found` row `is NOT correct on the evidence` — every clause verified in the DDL and by green controls, and no open finding touches it."
    - "TRACE: round-4 verification, Requirements Coverage table, STORE-04 row. STORE-04 stays `Gaps Found` and its recorded REASON names CR-08 rather than the pending `integrity_check` human item, which the verifier states is not the real blocker."
    - "TRACE: round-4 `behavior_unverified_items`. The one carried-forward human-verification item — `openStore`'s `integrity_check could not be run at all` arm (`anno-store.ts:432`) — is recorded as still OPEN by this round, with its unchanged reason, and is NOT claimed closed by any plan in it."
    - "TRACE: all six round-4 finding ids. Every one of CR-08, WR-13, WR-14, WR-15, WR-16 and WR-17 carries a recorded DECISION — fix or accept — in `28-REVIEW.md`'s round-4 disposition table, and each `accept` states what makes it acceptable, in the form the verifier used for the under-claim residual."
    - "TRACE: ROADMAP Phase 28 success criteria 1-5 and the goal's REVERTIBLE clause, re-established by regression rather than assumed. On the final tree: the whole anno surface is green at a real exit code with a recorded count, `tsc --noEmit` is clean, every named non-vacuity control is re-run individually and quoted, and criterion 4's planted red is re-observed and reverted."
  artifacts:
    - "src/mcp/vice/anno-seam.test.ts — the single-commit-site control matching `exec()` statements with `commit`, `end` or `end transaction`, plus a positive control over a local fixture proving all three spellings are counted"
    - "src/mcp/vice/anno-store.ts — the coupling comment about the load-bearing `step` spelling removed, together with the constraint it recorded"
    - ".planning/phases/28-the-store-core/28-REVIEW.md — a `### Round-4 finding dispositions` table covering all six ids"
    - ".planning/REQUIREMENTS.md — STORE-05 returned to `Complete` in the status table and its checklist entry; the round-4 record note replacing the round-3 one, naming CR-08 as STORE-04's real blocker"
  key_links:
    - "`anno-seam.test.ts`'s statement matcher → `anno-durability.test.ts`'s planted-violation site — the control exists so removing THE commit has exactly one site; a synonym spelling must not split it"
    - "`28-VERIFICATION.md` Requirements Coverage → `.planning/REQUIREMENTS.md` status rows — the verifier is the authority and this task transcribes its verdict, never substitutes its own"
    - "`28-REVIEW.md` round-4 disposition table + the three SUMMARYs of this round → `docs-review-disposition.test.ts`'s five recognised disposition sources — the guard is GREEN before this plan and must still be green after it"
  prohibitions:
    - statement: "MUST NOT make a structural invariant that constrains the wording of user-facing error messages. (NEW this round — WR-15's mirror cost, already materialised once: a word count over string literals forced an error message to be worded around it and put the explanation in a different file from the constraint)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT move a requirement's status on an executor's own judgement of its own work. Only a verdict already recorded in `28-VERIFICATION.md` may be transcribed, and the sentence that transcribes it must name its source. (NEW this round — the record corrections are the verifier's findings, not this round's opinion of them)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT close, silently drop, or re-file as done the carried-forward `behavior_unverified` item. (NEW this round — `openStore`'s `integrity_check` throw arm has no reachable input without fault injection and no plan in this round constructs one)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT leave in place, or introduce, a comment that asserts a guarantee the code does not provide. (carried forward: 28-07 P3, recorded HELD in round 4 — the removed coupling comment is an instance, and the round-4 disposition table must not claim a fix that its plan's SUMMARY does not evidence)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT publish a floor, bound or `retained` claim the store cannot honour on the very next call, in EITHER direction. (carried forward: 28-07 P2 / 28-10 P2 — the closing gate re-observes the OVER-CLAIM direction closed and the UNDER-CLAIM residual still accepted and still documented)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT destroy the only remaining route back to a state the store still advertises as reachable. (carried forward: 28-07 P1 / 28-10 P1 / 28-11 P1 — the closing gate re-drives the CR-08 reproduction on the final tree)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT adopt, migrate, claim or sweep a snapshot ring whose ownership this store cannot establish. (carried forward: 28-10 P3 / 28-11 P4)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT convert a committed write into a caller-visible failure. (carried forward: 28-11 P5)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT change the meaning of an on-disk column or of the on-disk directory layout without a schema version bump that makes the older shape refuse BY NAME. (carried forward: 28-10 P4 — the closing gate re-observes `SCHEMA_VERSION` unchanged at 2)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT report a conflict without both of the numbers that conflicted. (carried forward: 28-08 P2)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT substitute a tuned timing constant for an exact exclusion when the exact exclusion is already available from a lock the code takes anyway. (carried forward: 28-11 P3)"
      status: unverified
      verification: judgment
---

<objective>
Close WR-15, put a recorded DECISION against all six round-4 finding ids, carry
the two record corrections the round-4 verifier asked for, and close the round
with the gate that re-establishes its claims as numbers on the final tree.

Purpose: three things are left after 28-16 and 28-17. (1) WR-15 — the control
that keeps the durability proof's planted violation unique counts the WORD
`commit` over `anno-seam.test.ts`'s stripped source, and SQLite accepts `END` and
`END TRANSACTION` as exact synonyms, so a second commit site spelled `db.exec("end")`
passes the control unchanged and lets half the planting survive. Its mirror cost
has already materialised: the CR-06 refusal had to be worded around the count, and
`anno-store.ts` now carries a comment saying the `step` spelling is load-bearing.
(2) The record — WR-14, WR-15, WR-16 and WR-17 have no recorded decision anywhere;
`28-REVIEW.md`'s disposition table covers round-3 ids only. (3) The closing gate,
in 28-15's style: numbers re-observed on the final tree, not an aggregate asserted.

**A correction the executor must act on, because the verification report is wrong
about it.** `28-VERIFICATION.md` states that `docs-review-disposition.test.ts`
(AUDIT-01) and its `D-12-02` cascade in `audit-integrity.test.ts` are RED on the
current tree for the six round-4 ids. **They are not.** Both were run on the
current tree: `docs-review-disposition.test.ts` is 7/7 pass and
`audit-integrity.test.ts` is 44/44 pass. That guard recognises a phase's own
`*-VERIFICATION.md` as a disposition source (source 2 of five, named in its own
header comment), so writing the round-4 report is what made it green. **No task in
this plan may be premised on a red guard, and no acceptance criterion may take the
form "make that guard go from red to green" — it cannot go red now and such a
criterion is vacuous.** Both guards appear here only as REGRESSION criteria: they
are green before this plan and must still be green after it edits the file they
read.

Output: a statement-level commit control, four decisions on the record, two
corrected records, and the round's numbers.

**Ordering note.** This plan is serialised after 28-17 because its acceptance
surface runs the whole anno suite over the code 28-16 and 28-17 rewrite, and
because task 1 removes a constraint 28-17's task 2 had to write its message
around. It touches `anno-store.ts` only to delete a comment — no logic — so the
"one plan per wave edits the store" rule is honoured in substance as well as in
form.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-the-store-core/28-VERIFICATION.md
@.planning/phases/28-the-store-core/28-REVIEW.md
@.planning/phases/28-the-store-core/28-16-PLAN.md
@.planning/phases/28-the-store-core/28-17-PLAN.md
</context>

<environment_preconditions>
- Plans 28-16 and 28-17 are executed and committed. This plan measures the tree
  they left.
- Measured on the round-3 tree at plan time, as the round's starting numbers:
  `cd src/mcp/vice && node --test anno-*.test.ts block-class.test.ts` →
  `# tests 159`, `# pass 159`, `# fail 0`, `# skipped 0`, exit 0, 13.6 s;
  `npx tsc --noEmit` clean; `node --test docs-review-disposition.test.ts` 7/7
  pass; `node --test audit-integrity.test.ts` 44/44 pass.
- Never run the full glob (`node --test '*.test.*'`): ~660 s, outlives the tool
  timeout, and carries its own failure baseline. `npm run test:automated` exits 1
  on a clean tree. Scope every run to a named file set.
- A live VICE broker deterministically reddens `broker-e2e.test.ts`. It is
  outside every acceptance surface in this plan — do not widen a run to "check".
- Run everything as uid 1000. The two root-sensitive controls only discriminate
  as a non-root uid.
- `node:sqlite` emits an unconditional `ExperimentalWarning`; no test may assert
  stderr is empty.
</environment_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-15 — the single-commit-site control counts statements, and stops constraining error prose</name>

  <files>src/mcp/vice/anno-seam.test.ts, src/mcp/vice/anno-store.ts</files>

  <reversibility rating="reversible">
    A matcher change in one test plus a comment deletion. No production logic and
    no contract changes.
  </reversibility>

  <read_first>
    - `src/mcp/vice/anno-seam.test.ts` `:360-400` — the whole test named `the seam
      contains exactly one commit statement, so the single planted-violation site
      is unique`: its `codeOnly(seamSource(), true)` call (literal bodies KEPT,
      comments stripped), the word-count matcher, and the comment explaining that
      `commitTransaction`, `applyWriteWithoutCommit` and `doCommit` are not
      matched because of the word boundary. That last reason changes when the
      matcher does; it must be rewritten, not left.
    - `src/mcp/vice/anno-store.ts` `:1305-1325` — the CR-06 commit handler's
      `data` object and the comment above the `step` value explaining that its
      spelling is load-bearing because the seam control is word-based. That
      comment exists only because of the defect this task removes.
    - `src/mcp/vice/anno-store.ts` `:295-310` — `commitTransaction`, the module's
      single commit site, so the matcher can be checked against the real shape of
      the call it must find.
    - `src/mcp/vice/anno-durability.test.ts` — the planted-violation harness that
      removes THE commit. That is what the control protects; read enough of it to
      confirm what "one site" means operationally.
    - `.planning/phases/28-the-store-core/28-REVIEW.md` §WR-15 for the fix sketch
      and the synonym argument. Do not re-derive them.
  </read_first>

  <behavior>
    - The matcher finds exactly one commit statement in the shipped module today, so the control's current verdict is unchanged.
    - A fixture string containing `db.exec("commit")`, `db.exec("end")` and `db.exec("end transaction")` yields THREE matches from the same matcher, so all three spellings are demonstrably covered.
    - A fixture string containing the identifiers `commitTransaction`, `applyWriteWithoutCommit`, `doCommit` and an error message mentioning a commit in prose yields ZERO matches, so the matcher does not fire on identifiers or on user-facing wording.
  </behavior>

  <action>
    Replace the word count with a statement match, and delete the coupling it
    created.

    - In `anno-seam.test.ts`, match `exec(` calls whose single string-literal
      argument is a commit statement in any of SQLite's three spellings —
      `commit`, `end`, `end transaction` — case-insensitively and tolerant of
      surrounding whitespace and of any of the three quote characters. Keep
      `codeOnly(..., true)` (literal bodies KEPT) exactly as it is: the statement
      IS a string literal, and stripping literals would make the assertion
      vacuous. Keep the assertion at `equal(..., 1)` and keep its message.
    - Rewrite the test's comment. The old reason (a word boundary is what excludes
      `commitTransaction` and `doCommit`) is no longer the mechanism; the new one
      is that the matcher requires an `exec(` call with a bare statement literal,
      which identifiers and prose cannot satisfy. State the synonym fact
      explicitly and name it as the defect being closed: SQLite accepts two more
      spellings of the same statement, and a count over one of them left a second,
      working commit site invisible to the control that exists to keep the
      planting unique.
    - Add the two fixture-based controls from `<behavior>` in the same file,
      asserted against LOCAL string constants rather than against the module
      source, so the matcher's coverage is proven rather than inferred from a
      module that happens to contain one spelling.
    - In `anno-store.ts`, delete the comment block that records the `step`
      spelling as load-bearing, and leave the `step` value itself unchanged —
      changing the value would be a gratuitous behaviour change in a round whose
      whole discipline is not to. Replace the deleted block with one sentence
      recording that the constraint was removed by the statement matcher and that
      error prose is no longer coupled to a structural control in another file.
  </action>

  <verify>
    <automated>cd src/mcp/vice &amp;&amp; node --test anno-seam.test.ts anno-durability.test.ts 2>&amp;1 | tail -12</automated>
  </verify>

  <acceptance_criteria>
    - `node --test src/mcp/vice/anno-seam.test.ts` reports `# fail 0` with a pass count exactly two greater than before this task, and the test named `the seam contains exactly one commit statement, so the single planted-violation site is unique` is among the `ok` lines. Quote it.
    - Behavioural, the synonym is covered: the fixture control asserts THREE matches over a local string containing the `commit`, `end` and `end transaction` spellings. Against the pre-task matcher the same fixture yields ONE — record both numbers in the SUMMARY.
    - Behavioural, no false positives: the fixture control asserts ZERO matches over a local string containing `commitTransaction`, `applyWriteWithoutCommit`, `doCommit` and a prose sentence mentioning a commit.
    - Planted-violation control, observed and recorded: adding a second commit site to `anno-store.ts` spelled with the `end` synonym makes the control report `not ok`; removing it makes it `ok` again and leaves `git diff --stat -- src/mcp/vice/anno-store.ts` showing only this task's intended comment change. Against the pre-task matcher the same planting is observed to PASS — that contrast is the evidence WR-15 was real, and both runs must be quoted.
    - `grep -c "load-bearing" src/mcp/vice/anno-store.ts` returns a count exactly one lower than before this task, and `grep -c 'step: "committing the write transaction"' src/mcp/vice/anno-store.ts` returns `1` — the coupling comment is gone and the value it constrained is unchanged.
    - `node --test src/mcp/vice/anno-durability.test.ts` reports `# fail 0`, so the planted-violation harness the control protects still works.
    - `npx tsc --noEmit -p src/mcp/vice/tsconfig.json` exits 0 with no output.
  </acceptance_criteria>

  <done>
    A second commit site cannot hide behind a synonym, and no error message in
    this codebase is worded around a structural control in a different file.
  </done>
</task>

<task type="auto">
  <name>Task 2: the record — six dispositions, two corrected requirement rows, one item carried forward open</name>

  <files>.planning/phases/28-the-store-core/28-REVIEW.md, .planning/REQUIREMENTS.md</files>

  <reversibility rating="reversible">
    Documentation edits, each traceable to a quoted source. Reverting is a
    revert of this task's commit.
  </reversibility>

  <precondition>
    Execution is NOT in worktree-isolation mode: this task's entire deliverable
    lives under `.planning/`, and a worktree-isolated commit strips `.planning/`
    files, so the task would report success while delivering nothing. Assert
    before starting that the isolation mode is `none`, and halt if it is not.
  </precondition>

  <read_first>
    - `.planning/phases/28-the-store-core/28-VERIFICATION.md` — the Requirements
      Coverage table's STORE-04 and STORE-05 rows in full (they contain the exact
      verdicts to transcribe, including the sentence that STORE-05's `Gaps Found`
      row is not correct on the evidence), the `behavior_unverified_items` block,
      and the Gaps Summary's two-paragraph record-corrections passage.
    - `.planning/phases/28-the-store-core/28-REVIEW.md` `:140-165` — the existing
      `### Round-3 finding dispositions` table. The new table must match its
      format and sit beside it, not replace it.
    - `.planning/REQUIREMENTS.md` `:81-87` — the STORE-01..07 checklist entries,
      and `:210-240` — the status table and the round-3 record note beginning
      "Four STORE rows returned to `Complete`". Both need editing; note the exact
      current values so the diff is scoped.
    - `src/mcp/vice/docs-review-disposition.test.ts`, the header comment listing
      the five recognised disposition sources. Read it before writing anything:
      it explains that the guard catches SILENCE and does not require particular
      wording, and that a phase's own `*-VERIFICATION.md` and its `*-SUMMARY.md`
      files are both recognised sources.
    - `.planning/phases/28-the-store-core/28-16-SUMMARY.md` and
      `28-17-SUMMARY.md` — the evidence each disposition must cite. A disposition
      that claims a fix its SUMMARY does not evidence is a 28-07 P3 violation.
  </read_first>

  <action>
    Write the round's record. Three edits, each transcribing a source rather than
    forming a new opinion.

    (a) Add a `### Round-4 finding dispositions` section to `28-REVIEW.md`, in
    the same two-column table format as the round-3 section, immediately after
    it. One row per id, each naming the decision, the plan that carries it and
    the evidence:
      - CR-08 — `fix`, by 28-16 (the staged image is opened before the caller's
        handle is closed, and `retained` promoted to the openable definition).
        Cite the before/after byte length from 28-16's SUMMARY.
      - WR-13 — `fix`, by 28-17 (staged image and ring directory fsynced through
        the module's existing helper). Note that the evidence is structural, with
        the reason.
      - WR-14 — `fix`, by 28-17 (both root guards converted to node:test's real
        skip). Note the verifier's own qualifier: its round-4 CR-07 evidence did
        not depend on these controls.
      - WR-15 — `fix`, by 28-18 task 1 (statement matcher, synonyms covered, the
        error-prose coupling removed). Cite the one-versus-three fixture numbers.
      - WR-16 — `fix`, by 28-17 (the rollback is recorded, not asserted; the
        sweep reports it without throwing because 28-11 P5 forbids the
        alternative).
      - WR-17 — `fix`, by 28-16 task 3 (both reopens inside the guarantee), with
        the sentence naming why it was decided together with CR-08.
      Any id whose plan's SUMMARY does not evidence the claimed outcome is
      recorded as `accept` instead, with what makes it acceptable stated in the
      same cell — the form the verifier used for the under-claim residual. Do not
      write `fix` against evidence that is not there.

    (b) Add, to the same section, an explicit CARRIED-FORWARD row for the one
    `behavior_unverified` item: `openStore`'s `integrity_check could not be run
    at all` arm at `anno-store.ts:432`. Record it as STILL OPEN and NOT claimed
    closed by this round, with the unchanged reason — the arm is defensive and
    has no reachable input without filesystem- or SQLite-level fault injection,
    so presence and wiring are verified in source and no test exercises it. This
    row is a record, not a task: no plan in this round constructs that input.

    (c) `.planning/REQUIREMENTS.md`, two scoped edits:
      - STORE-05 → `Complete` in the status table, and its checklist entry ticked
        to match. The authorising sentence is the verifier's own: the `Gaps
        Found` row is not correct on the evidence, every clause verifies, and no
        open finding touches it. Quote that provenance in the record note.
      - Replace the round-3 record note (the paragraph beginning "Four STORE rows
        returned to `Complete`") with a round-4 note that keeps every fact still
        true, states STORE-05's return with its provenance, and corrects
        STORE-04's recorded reason: it stays `Gaps Found`, and the blocker is
        CR-08 — the revert path installing an image it never opened — NOT the
        pending `integrity_check` human item, which the verifier states is not
        the real blocker. Keep the sentence naming the phase verifier as the
        authority on both.
      Edit ONLY those regions. Do not rewrite the file, do not touch any other
      requirement's row, and do not touch `.planning/STATE.md` or
      `.planning/ROADMAP.md` — the orchestrator owns those.
  </action>

  <verify>
    <automated>cd src/mcp/vice &amp;&amp; node --test docs-review-disposition.test.ts audit-integrity.test.ts 2>&amp;1 | tail -12</automated>
  </verify>

  <acceptance_criteria>
    - REGRESSION, not a red-to-green claim: `node --test src/mcp/vice/docs-review-disposition.test.ts` reports 7 pass / 0 fail both BEFORE and AFTER this task, and `node --test src/mcp/vice/audit-integrity.test.ts` reports 44 pass / 0 fail both before and after. Quote all four runs. These guards are green on the current tree; the criterion is that editing the file they read does not break them.
    - `grep -c "^| CR-08 \|^| WR-13 \|^| WR-14 \|^| WR-15 \|^| WR-16 \|^| WR-17 " .planning/phases/28-the-store-core/28-REVIEW.md` returns `6` — one disposition row per round-4 id.
    - `grep -c "Round-4 finding dispositions" .planning/phases/28-the-store-core/28-REVIEW.md` returns `1`, and `grep -c "Round-3 finding dispositions" ...` still returns `1` — the round-3 table was added to, not replaced.
    - `grep -n "| STORE-05 | Phase 28 |" .planning/REQUIREMENTS.md` shows `Complete`; before this task it shows `Gaps Found`. `grep -n "| STORE-04 | Phase 28 |" .planning/REQUIREMENTS.md` still shows `Gaps Found` — unchanged.
    - The STORE-05 checklist entry is ticked and the STORE-04 entry is not: `grep -c "^- \[x\] \*\*STORE-05\*\*" .planning/REQUIREMENTS.md` returns `1` and `grep -c "^- \[ \] \*\*STORE-04\*\*" .planning/REQUIREMENTS.md` returns `1`.
    - The record note names CR-08 as STORE-04's blocker: `grep -c "CR-08" .planning/REQUIREMENTS.md` returns at least `1`, where before this task it returns `0`.
    - The carried-forward item is present and open: the round-4 section contains a row naming `anno-store.ts:432` and the words identifying it as still open and not claimed closed by this round.
    - `git diff --stat .planning/REQUIREMENTS.md` shows changes confined to the STORE-04/STORE-05 checklist entries, the STORE-05 status row and the record-note paragraph — no other requirement row appears in the diff.
    - `git status --porcelain .planning/STATE.md .planning/ROADMAP.md` is empty.
    - Every disposition cell citing a fix names the plan and quotes a number or a line reference from that plan's SUMMARY. A cell that cannot do so reads `accept` with its reason instead.
  </acceptance_criteria>

  <done>
    All six round-4 ids carry a decision with evidence, the two record
    corrections the verifier asked for are on the record with their provenance,
    the one open human-verification item is carried forward rather than lost, and
    both docs guards are still green.
  </done>
</task>

<task type="auto">
  <name>Task 3: the closing gate — the round's claims re-observed on the final tree as numbers</name>

  <files>src/mcp/vice/anno-seam.test.ts</files>

  <read_first>
    - `.planning/phases/28-the-store-core/28-15-PLAN.md`, task 3 — the closing
      gate this task repeats. Match its shape: re-observe individually, record
      verbatim, never assert an aggregate.
    - `.planning/phases/28-the-store-core/28-VERIFICATION.md`, the Observable
      Truths table rows 1, 2, 3, 10 and 12 — for the exact named controls that
      make ROADMAP criteria 1, 2 and 3 able to go red, and for the CR-08
      reproduction's exact numbers.
    - `src/mcp/vice/anno-types.test.ts` — the split-orientation control and its
      collapse planting (criterion 1's red).
    - `src/mcp/vice/anno-index.test.ts` — the exhaustive 65,536-address
      cross-validation and its `comparisons` non-vacuity assertion (criterion 2's
      red).
    - `src/mcp/vice/anno-overlap.test.ts` — the five overlap cases and the
      `filter()`-and-insert planting (criterion 3's red).
    - `.planning/phases/28-the-store-core/28-16-SUMMARY.md` and
      `28-17-SUMMARY.md` — the numbers this gate re-observes rather than repeats.
  </read_first>

  <action>
    Run and record the closing gate. This task writes no production code; its
    deliverable is measured evidence in the SUMMARY, plus one small addition to
    `anno-seam.test.ts` only if the count check below reveals a missing control.

    - Run `node --test anno-*.test.ts block-class.test.ts` from `src/mcp/vice` as
      uid 1000 and record the exact `# tests` / `# pass` / `# fail` / `# skipped`
      / duration lines and the real exit code. `# fail` must be `0` and
      `# skipped` must be `0`. Record the pass count as a NUMBER: the round-4
      baseline is 159, this round's three plans add 6 + 2 + 2 = 10 tests, so 169
      is expected. That arithmetic is pinned upstream — 28-16 task 2 carries an
      explicit three-test criterion, which is what makes its 6 add up — but the
      AUTHORITY here is the recorded actual, not this sum: read 28-16's and
      28-17's SUMMARY pass counts first and derive the expected number as
      `28-17's recorded actual + 2`. If the derived number and 169 disagree, the
      recorded actuals win and the discrepancy is stated. If the observed count
      differs from the derived one, record the actual AND the list of added test
      names — a count that did not GROW would mean the round's tests were not
      added, and a count that grew by a different amount must be explained, not
      rounded.
    - Re-observe the four named non-vacuity controls from 28-15's gate
      individually and quote each result verbatim: the split-orientation control
      and its collapse planting in `anno-types.test.ts`; the exhaustive index
      cross-validation with its `comparisons` assertion in `anno-index.test.ts`;
      the fully-contained overlap case and its `filter()`-and-insert planting in
      `anno-overlap.test.ts`; and the no-splitter structural scan with its
      non-vacuity companion in `anno-store.test.ts`.
    - Re-observe THIS ROUND's three load-bearing plantings on the final tree,
      because 28-16, 28-17 and this plan all rewrote code they run over:
      * 28-16's step 3b planted red — delete the staged-image validation block by
        hand, run `node --test anno-store.test.ts`, record the truncated-image and
        foreign-bytes tests reporting `not ok`, restore, confirm green;
      * 28-17's fsync planted red — delete one of the two new `fsyncPath` calls,
        record the structural control reporting `not ok`, restore, confirm green;
      * this plan's synonym planting — add a second commit site spelled with the
        `end` synonym, record the seam control reporting `not ok`, restore,
        confirm green.
      After each restore, confirm `git diff --stat -- src/mcp/vice` is empty.
    - Re-drive the CR-08 REPRODUCTION ITSELF on the final tree, through
      production entry points only and with no hand edit of the store file: build
      a store at revision 3, truncate `r1.db` to 0 bytes, call `revertTo(handle,
      1)`, and record — as numbers — the store's byte length before and after,
      the thrown error's class and `instanceof ViceError`, whether the same
      handle still answers, `retainedRevisions()` before and after the truncation,
      and whether a later `openStore` succeeds. The round-4 verifier's recorded
      numbers for the same drive are `69632 -> 0` with no handle; this gate exists
      to show the pair is now `69632 -> 69632` with the handle alive.
    - Re-observe criterion 4's planted red on the final tree: replace the
      module's single commit statement with a comment by hand, run
      `node --test anno-durability.test.ts`, record that the combined test and its
      planted-violation sibling both report `not ok`, then restore and confirm
      `git diff --stat -- src/mcp/vice` is empty and the file is green again.
      This is a manual step and it must not be skipped: a durability proof whose
      red has not been observed against the shipped code is a claim, not a proof.
    - Run `npx tsc --noEmit -p src/mcp/vice/tsconfig.json` and record it clean.
    - Confirm the phase's structural invariants are unmoved and record each:
      `node:sqlite` reachable from exactly one shipped module; exactly one commit
      statement in `anno-store.ts` under the NEW statement matcher; `anno-store.ts`
      and `anno-types.ts` both absent from `hostpath-consumers.test.ts`'
      five-element consumer set; `SCHEMA_VERSION` still 2 and `anno_snapshot`
      still one column; no test in the round's diff asserting stderr is empty.
    - Re-run both docs guards and record them: `docs-review-disposition.test.ts`
      7/7 and `audit-integrity.test.ts` 44/44. State plainly that they were green
      before this round as well — this gate records a non-regression, not a
      repair.
    - If, and only if, the seam file is missing one of task 1's two fixture
      controls, add it rather than adjusting the expectation.

    Finally, make three EXECUTOR SELF-ASSERTIONS and write all three into the
    SUMMARY as statements of fact with their evidence. They are self-assertions
    rather than a human checkpoint because this plan is `autonomous: true` and
    the phase's `human_verify_mode` is `end-of-phase`, so the human gate for the
    round sits at the phase boundary and not inside this task:
      * criterion 4's planted red WAS re-observed on the final tree and reverted
        — quote the `not ok` lines and the empty `git diff --stat`;
      * the CR-08 reproduction WAS re-driven on the final tree through production
        entry points and is now a refusal — quote the byte-length pair;
      * the `pragma integrity_check` throw arm remains an OPEN human-verification
        item, was NOT claimed closed by this round, and the reason is unchanged.

    Deliberately NOT done here: re-running the full-glob suite (it outlives the
    tool timeout and its failure baseline is unrelated), touching
    `.planning/STATE.md` or `.planning/ROADMAP.md` (the orchestrator owns them),
    and re-verifying the deliberately accepted UNDER-CLAIM residual — it is
    recorded as accepted and this round did not touch it.
  </action>

  <verify>
    <automated>cd src/mcp/vice &amp;&amp; node --test anno-types.test.ts anno-index.test.ts anno-overlap.test.ts anno-store.test.ts anno-seam.test.ts anno-durability.test.ts anno-confinement.test.ts block-class.test.ts 2>&amp;1 | tail -12</automated>
  </verify>

  <acceptance_criteria>
    - `node --test anno-*.test.ts block-class.test.ts` from `src/mcp/vice` reports `# fail 0`, `# skipped 0`, a real exit code of 0, and a `# pass` count strictly greater than 159. The expected number is derived, not asserted flat: `28-17's SUMMARY recorded pass count + this plan's 2`, which on the planned arithmetic (159 + 6 + 2 + 2) is 169. The SUMMARY records the derived number, the actual number, and — if they differ — the list of added test names that accounts for the difference. The STRICTLY-GREATER-THAN-159 clause is the part that must hold unconditionally; the exact number is recorded evidence, not a gate.
    - `npx tsc --noEmit -p src/mcp/vice/tsconfig.json` produces no output and exits 0.
    - The CR-08 reproduction re-driven on the final tree records the byte-length pair `69632 -> 69632` (unchanged) where the round-4 verifier recorded `69632 -> 0`, with the thrown value `instanceof ViceError` true, the same handle still answering `currentRevision()`, and a later `openStore` succeeding.
    - `retainedRevisions()` on that same fixture records `[0, 1, 2]` before the truncation and `[0, 2]` after, where round 4 recorded `[0, 1, 2]` both times.
    - All four of 28-15's named non-vacuity controls are re-run individually and quoted verbatim in the SUMMARY — not summarised as an aggregate.
    - All three of this round's plantings are re-observed red and restored, each with its quoted `not ok` line and an empty `git diff --stat -- src/mcp/vice` afterwards.
    - Criterion 4's planted red is re-observed: with the single commit replaced by a comment, `anno-durability.test.ts` reports the combined test and its planted sibling as `not ok`; after restore, `git diff --stat -- src/mcp/vice` is empty and the file is green.
    - The structural invariants are recorded individually: one shipped module names `node:sqlite`; the seam control counts exactly one commit statement under the new matcher; both store modules are absent from the five-element host-path consumer set; `SCHEMA_VERSION` is 2.
    - `node --test src/mcp/vice/docs-review-disposition.test.ts` reports 7 pass / 0 fail and `node --test src/mcp/vice/audit-integrity.test.ts` reports 44 pass / 0 fail, recorded as a non-regression with the note that both were green before this round too.
    - No change is made to `.planning/STATE.md` or `.planning/ROADMAP.md` by this task: `git status --porcelain .planning/STATE.md .planning/ROADMAP.md` is empty.
    - The SUMMARY carries all three executor self-assertions with their evidence.
  </acceptance_criteria>

  <done>
    The whole anno surface is green with zero failures and zero skips at a real
    exit code, every control that makes this phase's criteria able to go red has
    been re-observed on the final tree, the CR-08 reproduction has been re-driven
    and is now a refusal, and all of it is recorded as numbers rather than as
    adjectives.
  </done>
</task>

</tasks>

<assumption_delta_decision>
**Noun now primary:** `retained` — resolved in `28-16-PLAN.md` as `promote`.

**Decision here: `no-change`.** This plan introduces no new meaning for any of
the round's contested nouns. It does resolve a second, smaller one for the
record: the noun `commit`, which had two answers in the tree — the WORD, counted
by the seam control, and the STATEMENT, which is what SQLite executes and what
the durability planting removes. Task 1 **promotes** the statement reading to the
only one, and the evidence that the two really had diverged is that a working
site spelled with the `end` synonym passed the word-based control.

**What would force a later change:** a fourth SQLite spelling, or a commit issued
other than through a bare `exec()` with a statement literal — either would make
the statement matcher miss a site, and the fixture controls added in task 1 are
what would surface it as a failing coverage assertion rather than as a silently
split planting.
</assumption_delta_decision>

<edge_coverage_disposition>
The full 21-row `28-EDGE-COVERAGE.json` disposition for this round is recorded
once, in `28-16-PLAN.md` §`<edge_coverage_disposition>`: 2 rows lifted (STORE-04
idempotency → 28-16, STORE-04 concurrency → 28-17) and 19 recorded as already
carried by named executed plans. No row is lifted to this plan; its closing gate
re-observes the controls that carry the 19, individually and by name, which is
what turns "already carried" from an assertion into an observation.
</edge_coverage_disposition>

<threat_model>
ASVS enforcement level: **1**. Blocking severity threshold: **high**. This plan
changes one control and two records; its realistic surface is the integrity of
the evidence a later round will trust, plus the one structural control that keeps
the durability proof honest.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a commit statement's SPELLING → the control that counts commit sites | The control is what guarantees the durability planting has exactly one site. Anything that can be spelled past it splits the planting and lets half the violation survive a green run. |
| the recorded requirement status → the next round's planner and verifier | Status rows are read as facts by everything downstream. A row moved on an executor's own judgement of its own work is fact-laundering, and this repo has already been bitten by it. |
| `28-REVIEW.md` → `docs-review-disposition.test.ts` | The guard reads this file. An edit that breaks its parser turns a repo-wide silence detector off. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-29 | Tampering | the single-commit-site control (`anno-seam.test.ts`) | high | mitigate | Task 1 matches `exec()` statements across all three SQLite spellings instead of counting a word, and proves the coverage with fixture controls over local strings. The planting is re-observed red on the final tree with the `end` synonym, and the pre-task matcher is observed to PASS the same planting. |
| T-28-30 | Repudiation | requirement status rows (`.planning/REQUIREMENTS.md`) | high | mitigate | Task 2 transcribes verdicts already recorded in `28-VERIFICATION.md` and names its source in the record note; the diff is asserted to be confined to the two rows and the note; STATE.md and ROADMAP.md are asserted untouched. |
| T-28-31 | Repudiation | the carried-forward `behavior_unverified` item | medium | mitigate | Task 2 records it as an explicit OPEN row with its unchanged reason, so it cannot be lost between rounds by being absent rather than by being closed. |
| T-28-32 | Tampering | `docs-review-disposition.test.ts`'s parser | medium | mitigate | The new table uses the existing round-3 section's format and adds no heading shaped like a finding id; both guards are run before and after and asserted unchanged at 7/7 and 44/44. |
| T-28-33 | Spoofing | a disposition claiming a fix the evidence does not support | high | mitigate | Every `fix` cell must name its plan and quote a number or line reference from that plan's SUMMARY; a cell that cannot reads `accept` with its reason. Asserted as an acceptance criterion rather than left to good intentions. |
| T-28-34 | Denial of service | a closing gate that reports an aggregate instead of numbers | medium | mitigate | Task 3 re-runs every named control individually and quotes it, re-drives the CR-08 reproduction, and re-observes four plantings red and restored — the failure mode being defended against is three rounds of green suites sitting on top of live blockers. |
| T-28-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan and no dependency is added, so the package-legitimacy gate has nothing to audit. |
</threat_model>

<artifacts_this_phase_produces>
**Changed tests (`src/mcp/vice/anno-seam.test.ts`)**
- `the seam contains exactly one commit statement, so the single planted-violation site is unique` — matcher changed from a word count to an `exec()` statement match covering `commit`, `end` and `end transaction`; name unchanged.

**New tests (`src/mcp/vice/anno-seam.test.ts`) — 2**
- a fixture control asserting the matcher counts all three commit spellings (THREE matches)
- a fixture control asserting the matcher counts neither identifiers nor prose (ZERO matches)

**Changed documentation (`src/mcp/vice/anno-store.ts`)**
- the comment recording the `step` value's spelling as load-bearing — deleted, replaced by one sentence recording that the constraint was removed. The `step` value itself is unchanged.

**New planning records**
- `.planning/phases/28-the-store-core/28-REVIEW.md` — a `### Round-4 finding dispositions` table with one row per round-4 id plus one carried-forward row for the open human-verification item.
- `.planning/REQUIREMENTS.md` — STORE-05 returned to `Complete` (status row and checklist entry); the round-3 record note replaced by a round-4 note naming CR-08 as STORE-04's real blocker.

**No new exported symbols. No new error classes. No new source files. No production logic changes. No on-disk column, directory layout or `SCHEMA_VERSION` change. `.planning/STATE.md` and `.planning/ROADMAP.md` are NOT modified by this plan.**
</artifacts_this_phase_produces>

<verification>
- The seam control counts statements across all three SQLite spellings, proven by fixture controls, and the `end`-synonym planting is observed red where the pre-task matcher passed it.
- No error message in `anno-store.ts` is coupled to a structural control in another file.
- All six round-4 finding ids carry a recorded decision with cited evidence; the one open human-verification item is carried forward as open.
- STORE-05 reads `Complete`, STORE-04 still reads `Gaps Found` with CR-08 as its recorded reason, and the diff is confined to those regions.
- `docs-review-disposition.test.ts` is 7/7 and `audit-integrity.test.ts` is 44/44 both before and after — a recorded non-regression, not a repair.
- `node --test anno-*.test.ts block-class.test.ts` reports `# fail 0`, `# skipped 0`, exit 0, `# pass` strictly greater than 159 — expected `28-17's recorded actual + 2`, which is 169 on the planned arithmetic (159 + 6 + 2 + 2, with 28-16's 6 pinned by its task-2 three-test criterion) — and `npx tsc --noEmit` is clean.
- The CR-08 reproduction is re-driven on the final tree and records `69632 -> 69632` where round 4 recorded `69632 -> 0`.
- Criterion 4's planted red and this round's three plantings are each re-observed and reverted, with empty diffs afterwards.
</verification>

<success_criteria>
- A commit site cannot hide behind a synonym, and no user-facing message is worded around a control in a different file.
- Every round-4 finding has a decision on the record, with evidence, and every `accept` says what makes it acceptable.
- The two record corrections the verifier asked for are made, with their provenance quoted, and nothing else in `REQUIREMENTS.md` moved.
- The round closes on numbers re-observed on the final tree — the suite count at a real exit code, each named control individually, the four plantings red and restored, and the CR-08 reproduction turned from a destruction into a refusal.
</success_criteria>

<output>
Create `.planning/phases/28-the-store-core/28-18-SUMMARY.md` when done. Record the
gate's numbers verbatim — the suite counts, the four re-observed controls, the
four planted-red `not ok` lines with their restore diffs, the CR-08 byte-length
pair, the `retainedRevisions()` before/after lists, and both docs-guard counts.
The next round's verifier reads those numbers; adjectives are what let three
rounds of green suites sit on top of live blockers.
</output>
