---
phase: 28-the-store-core
plan: 22
type: execute
wave: 20
depends_on: [28-21]
files_modified:
  - src/mcp/vice/anno-seam.test.ts
  - .planning/phases/28-the-store-core/28-REVIEW.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [STORE-01, STORE-03, STORE-04, STORE-05, STORE-07]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "TRACE: round-5 WR-19, verifier-routed and CONFIRMED BY THE VERIFIER RUNNING THE MATCHER. The single-commit-site control matches the commit STATEMENT inside an `exec()` literal rather than the whole literal, so `commit;`, `end;`, `COMMIT ;` and a multi-statement `insert …; commit` are all counted where today each counts **0**. 28-18's fix is incomplete in its own terms and this closes the residual it left."
    - "TRACE: round-5 WR-19 + prohibition 28-18 P1. The repaired matcher still constrains NO user-facing error prose: it looks only inside `exec()` string literals, and a fixture containing a sentence about a commit in ordinary English yields 0. The coupling 28-18 removed is not re-introduced by its own repair."
    - "TRACE: round-5 verification, `### Round-5 Finding Dispositions`, and the verifier's own instruction that a disposition living only in the verification report `disappears the next time this file is overwritten — that is exactly how round 4 lost the disposition it thought it had`. All TWELVE round-5 ids carry a recorded decision in `28-REVIEW.md`'s OWN table: CR-09, WR-18, WR-19, WR-20, WR-21, WR-22, WR-23, WR-24, WR-25, IN-06, IN-07, IN-08."
    - "TRACE: round-4 disposition table form (`28-REVIEW.md:167-183`). Every `fix` cell names its plan and quotes a number or a line reference from that plan's SUMMARY; every `accept` cell states what makes it acceptable. A cell that cannot do the first reads as the second."
    - "TRACE: round-5 verification, Requirements Coverage table + Gaps Summary, and prohibition 28-18 P2. `.planning/REQUIREMENTS.md` carries the verifier's three record verdicts, applied against the file's ACTUAL state rather than against the state the report's prose implies: STORE-04 moves UP to `Complete`, and STORE-01 and STORE-03 are confirmed already reading `Gaps Found`. The authorising sentences are the verifier's own, quoted, with the source file and table named."
    - "TRACE: round-5 `behavior_unverified_items` + prohibition 28-18 P3. BOTH carried-forward items are recorded as still OPEN with their unchanged reasons and are claimed closed by nothing: `openStore`'s `integrity_check could not be run at all` arm, and 28-17's `backstop`-tagged host-crash durability bound, which the verifier ABSTAINED on as `insufficient_spec` rather than counting verified."
    - "TRACE: ROADMAP Phase 28 goal and success criteria 1-5, re-established by regression rather than assumed. On the round's final tree: the whole anno surface green at a REAL exit code with a recorded count, `tsc --noEmit` clean, every named non-vacuity control re-run INDIVIDUALLY and quoted, every planting of this round re-observed red and reverted to an empty diff (SEVEN from this round plus criterion 4's carried one — eight in total), and BOTH reproductions — CR-09's six-line drive and CR-08's byte-length pair — re-driven through production entry points and recorded before/after."
    - statement: "The host-crash durability bound (28-17's truth): after an interruption between `vacuum into` and the pointer row's commit, the reachable outcomes are bounded to two, both non-destructive."
      verification: backstop
  artifacts:
    - "src/mcp/vice/anno-seam.test.ts — `commitStatements()` matching the statement inside an `exec()` literal, with the widened spelling fixture, the multi-statement fixture, and the prose negative control"
    - ".planning/phases/28-the-store-core/28-REVIEW.md — a `### Round-5 finding dispositions` table covering all twelve ids, in the round-4 table's established form, plus two explicit STILL-OPEN rows for the carried-forward human-verification items"
    - ".planning/REQUIREMENTS.md — STORE-04 at `Complete` in both the status table and the checkbox list; the stale round-4 prose record blocks replaced by a round-5 record naming CR-09 as STORE-01's and STORE-03's blocker and recording that CR-08 is closed"
  key_links:
    - "`anno-seam.test.ts`'s statement matcher -> `anno-durability.test.ts`'s planted-violation site — the control exists so removing THE commit has exactly one site; a trailing semicolon or a multi-statement `exec()` must not split it"
    - "`28-VERIFICATION.md`'s Requirements Coverage and Gaps Summary -> `.planning/REQUIREMENTS.md` status rows — the verifier is the authority and this plan transcribes its verdict, never substitutes its own (28-18 P2)"
    - "`28-REVIEW.md`'s round-5 disposition table -> `docs-review-disposition.test.ts` — the guard reads this file. It is MEASURED GREEN at plan time and this plan's criteria for it are REGRESSION criteria, not repair criteria."
    - "the two carried-forward `behavior_unverified` items -> `28-REVIEW.md`'s STILL-OPEN rows — the record that stops an item being lost by being ABSENT rather than by being closed"
  prohibitions:
    - statement: "28-22 P1 (NEW this round) — MUST NOT premise a task, or an acceptance criterion, on a guard being RED. `docs-review-disposition.test.ts` and `audit-integrity.test.ts` were both measured GREEN on the real tree at plan time (7/7 and 44/44, exit 0), because the guard treats a phase's own `*-VERIFICATION.md` as a disposition source and writing the round-5 report is what dispositioned the twelve ids there. A `make it go from red to green` criterion is vacuous and must not appear."
      status: unverified
      verification: judgment
    - statement: "MUST NOT move a requirement's status on an executor's own judgement of its own work. Only a verdict already recorded in `28-VERIFICATION.md` may be transcribed, and the sentence that transcribes it must name its source. (carried forward: 28-18 P2)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT close, silently drop, or re-file as done a carried-forward `behavior_unverified` item. Both of them — the `integrity_check` throw arm and 28-17's abstained host-crash durability bound — are recorded as OPEN with their unchanged reasons. (carried forward: 28-18 P3)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT make a structural invariant that constrains the wording of user-facing error messages. (carried forward: 28-18 P1 — WR-19's repair is the single most likely place in this round to re-introduce it, and the prose negative control is what proves it did not)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT make a durability claim whose only evidence is that the code was written down. (carried forward: 28-17 P5 — the host-crash bound stays a `backstop` truth and is NOT promoted by this round's gate)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT let a test report a PASS for a precondition it could not construct. (carried forward: 28-17 P4 — the closing gate re-observes that the two root-sensitive controls still declare real `{ skip: ... }` and that the suite reports `# skipped 0` as uid 1000)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT destroy the only remaining route back to a state the store still advertises as reachable. (carried forward: 28-07 P1 / 28-10 P1 / 28-11 P1 — the gate re-drives the CR-08 reproduction on the FINAL tree, after three plans rewrote `anno-store.ts`)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT publish a floor, bound or `retained` claim the store cannot honour on the very next call, in EITHER direction. (carried forward: 28-07 P2 / 28-10 P2 — the gate re-observes `retainedRevisions()` before and after the truncation)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT change the meaning of an on-disk column or of the on-disk directory layout without a schema version bump that makes the older shape refuse BY NAME. (carried forward: 28-10 P4 — the gate re-observes `SCHEMA_VERSION` unchanged at 2 and `anno_snapshot` still one column)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT let the store persist, by ANY writer, a range row it would refuse at its own entry point. (carried forward from 28-19 P1, this round — the gate re-drives the CR-09 reproduction on the FINAL tree and re-observes the round-trip invariant's planting red)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT return a handle whose transaction state the function cannot vouch for. (carried forward from 28-20 P1, this round — regression only)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT leave two contradictory readings of the same rule in the tree. (carried forward from 28-21 P1, this round — the disposition table and the requirement record are the two places this round could create a third pair, and the gate asserts they agree with the verification report)"
      status: unverified
      verification: judgment
---

<objective>
Close WR-19, put a recorded DECISION against all twelve round-5 finding ids,
carry the record corrections the round-5 verifier asked for, carry both
`behavior_unverified` items forward as open, and close the round with the gate
that re-establishes its claims as numbers on the final tree.

Purpose: three things are left after 28-19, 28-20 and 28-21. (1) WR-19 — the
control that keeps the durability proof's planted violation unique still counts
**0** for `db.exec("commit;")`, `db.exec("end;")`, `db.exec("COMMIT ;")` and any
multi-statement `exec()` ending in `; commit`, all of which commit. The round-5
verifier confirmed this by running the matcher, and 28-18's fix is incomplete in
its own terms. It is not a blocker — there is exactly one commit site today, so
criterion 4's planting IS unique — but the guard that keeps it unique is holed.
(2) The record — twelve ids opened by round 5 carry no disposition in
`28-REVIEW.md`'s own tables, and the verifier's instruction on this is explicit:
a disposition that lives only in the verification report disappears the next time
that file is overwritten, *"and that is exactly how round 4 lost the disposition it
thought it had"*. (3) The closing gate, in 28-15's and 28-18's style: numbers
re-observed on the final tree, not an aggregate asserted.

**A CORRECTION THE EXECUTOR MUST ACT ON, because the planning brief and the
verification report are both wrong about it, and in the same way round 4 was.**
The round-5 verification states that `docs-review-disposition.test.ts` is 6/7 and
`audit-integrity.test.ts` is 43/44 on the current tree, caused solely by the
twelve undispositioned ids. **They are not.** Both were run on the real tree at
plan time, as uid 1000:
`node --test docs-review-disposition.test.ts` -> `# tests 7 / # pass 7 / # fail 0`,
exit **0**; `node --test audit-integrity.test.ts` -> `# tests 44 / # pass 44 /
# fail 0`, exit **0**. The guard recognises a phase's own `*-VERIFICATION.md` as a
disposition source, so WRITING the round-5 report — which itself carries a
`### Round-5 Finding Dispositions` table — is what turned them green. The verifier
warned about exactly this subtlety in the same paragraph and it applies to its own
numbers. **No task in this plan may be premised on a red guard, and no acceptance
criterion may take the form "make that guard go from red to green" — it cannot go
red now and such a criterion is vacuous.** Both guards appear here only as
REGRESSION criteria. And the reason to transcribe anyway is unchanged and is the
verifier's own: the dispositions must survive the next overwrite of
`28-VERIFICATION.md`, which is what makes this a durability move for the record
rather than a repair of a guard.

**Worktree isolation is forbidden for this plan and must not be proposed.**
`workflow.use_worktrees` is `false` in this project, and this plan's deliverables
for tasks 2 and 3 are `.planning/` content — `.planning/REQUIREMENTS.md` and
`.planning/phases/28-the-store-core/28-REVIEW.md`. Worktree isolation strips
`.planning/` paths from commits, so a plan whose deliverable is `.planning/`
content silently CANNOT DELIVER under it: the tasks would report success and the
files would not move. Stated here explicitly because this is the plan in the round
that carries the record work.

Output: a commit-statement matcher with no evasion, twelve decisions on the
record, one corrected requirement row with its provenance quoted, two open items
carried forward, and the round's numbers.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-the-store-core/28-VERIFICATION.md
@.planning/phases/28-the-store-core/28-REVIEW.md
@.planning/phases/28-the-store-core/28-18-PLAN.md
@.planning/phases/28-the-store-core/28-19-PLAN.md
@.planning/phases/28-the-store-core/28-20-PLAN.md
@.planning/phases/28-the-store-core/28-21-PLAN.md
</context>

<environment_preconditions>
- Plans 28-19, 28-20 and 28-21 are executed and committed. This plan measures the
  tree they left and reads their three SUMMARYs for the numbers it re-observes.
- MEASURED AT PLAN TIME on the real tree, as uid 1000, so the executor starts from
  facts rather than from the report's prose:
  * `cd src/mcp/vice && node --test anno-*.test.ts block-class.test.ts` ->
    `# tests 169 / # pass 169 / # fail 0 / # skipped 0`, real exit **0**, 13.1 s.
    This is the ROUND-4 baseline; this round's three prior plans each grow it, so
    derive the expected number from 28-21's recorded actual, never from 169.
  * `node --test docs-review-disposition.test.ts` -> `# tests 7 / # pass 7 /
    # fail 0`, exit **0**. GREEN.
  * `node --test audit-integrity.test.ts` -> `# tests 44 / # pass 44 / # fail 0`,
    exit **0**. GREEN.
- MEASURED AT PLAN TIME, `.planning/REQUIREMENTS.md`'s ACTUAL current state, which
  differs from what the verification report's prose implies. The status table
  around `:216-221` reads: STORE-01 `Gaps Found`, STORE-02 `Complete`, STORE-03
  `Gaps Found`, STORE-04 `Gaps Found`, STORE-05 `Complete`, STORE-07 `Complete`.
  The checkbox list around `:81-87` agrees: STORE-01 `[ ]`, STORE-02 `[x]`,
  STORE-03 `[ ]`, STORE-04 `[ ]`, STORE-05 `[x]`, STORE-07 `[x]`. So TWO of the
  verifier's three requested moves are already satisfied (by commit `737f9e7`) and
  the ONLY change this plan makes is STORE-04 -> `Complete` in both places.
  RE-VERIFY on the current tree before writing; plan the diff that is needed, not
  the one the report's prose implies.
- Every `<automated>` command in this plan starts with `set -o pipefail`, and that
  prefix is LOAD-BEARING -- do not drop it and do not shorten it away. Without it
  the pipe into `tail` makes the command's exit status `tail`'s, which is
  unconditionally 0, so a run emitting `# fail 3` in its captured text would still
  report success and the automated verb would gate nothing. Rounds 1-4 of this
  phase carried the unpiped-status spelling and leaned on the acceptance criteria's
  "a REAL exit code of 0" instead, which put the gate on executor discipline rather
  than on the harness; this round changes the convention deliberately and 28-22
  records the change. Keep the `tail` trim -- these suites are large -- and keep
  the exit status real.
- Never run the full glob (`node --test '*.test.*'`): ~660 s, outlives the tool
  timeout, and carries its own failure baseline. `npm run test:automated` exits 1
  on a clean tree. Scope every run to a named file set.
- `r2000-session.test.ts` has 5 failures that reproduce in isolation with their own
  error text `R2000SpawnError: regenerator2000 was not found on PATH` — a missing
  external binary. `scripts/audit-gate.mjs:198` runs all seven docs guards in ONE
  `node --test` process and attributes the batch's non-zero exit to all seven; the
  other six are GREEN individually. Both are recorded by the round-5 verifier as
  pre-existing and not phase-28 defects. Do NOT chase either and do NOT let either
  pollute this gate.
- A live VICE broker deterministically reddens `broker-e2e.test.ts`. Outside every
  acceptance surface here.
- Run everything as uid 1000. The two root-sensitive controls in
  `anno-store.test.ts` only discriminate as a non-root uid.
- `node:sqlite` emits an unconditional `ExperimentalWarning`; no test may assert
  stderr is empty.
- `anno-seam.test.ts` MUST NOT name `node:sqlite`. The seam control fails the build
  if a second module does, and this file is the one that runs that control.
</environment_preconditions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-19 — the commit-statement matcher matches the STATEMENT inside the literal, and still constrains no error prose</name>

  <files>src/mcp/vice/anno-seam.test.ts</files>

  <precondition>`cd src/mcp/vice && node --test anno-*.test.ts block-class.test.ts` reports `# fail 0` and `# skipped 0` at exit 0 on the tree 28-21 left, so every red observed after this task is this task's.</precondition>

  <reversibility rating="reversible">
    A matcher change and three fixture additions inside one test file. No
    production code, no contract, nothing on disk.
  </reversibility>

  <read_first>
    - `src/mcp/vice/anno-seam.test.ts` `:375-440` — the whole of the test named
      `the seam contains exactly one commit statement, so the single planted-violation
      site is unique`: `commitStatements()` at `:382-390`, its use at `:430`, the
      `codeOnly(seamSource(), true)` call (literal bodies KEPT, comments stripped),
      the `THREE_SPELLINGS_FIXTURE` and `NO_STATEMENT_FIXTURE` controls 28-18
      added, and the comment explaining which identifiers the current matcher does
      not fire on. That explanation changes when the matcher does; rewrite it, do
      not leave it.
    - `src/mcp/vice/anno-seam.test.ts` `:95-105` — `seamSource()` and its
      `codeOnly(src, true)` call, so the literal-bodies-KEPT choice is understood
      as load-bearing for this control and preserved.
    - `src/mcp/vice/anno-store.ts` — `commitTransaction()` and the DDL `exec()`
      call, so the matcher is checked against the real shapes it must find and
      must not find: a bare statement literal, and an `exec()` whose argument is an
      IDENTIFIER rather than a literal.
    - `src/mcp/vice/anno-store.ts` — the commit handler's `step` value and the
      comment 28-18 left recording that the wording constraint was REMOVED. That
      comment is the record of 28-18 P1 and this task must not make it false again.
    - `src/mcp/vice/anno-durability.test.ts` — the planted-violation harness that
      removes THE commit, so what "one site" means operationally is confirmed
      rather than assumed.
    - `.planning/phases/28-the-store-core/28-REVIEW.md` §WR-19 (line 450 onward)
      for the measured evasions and the fix sketch. Do not re-derive them.
  </read_first>

  <behavior>
    - The matcher finds exactly ONE commit statement in the shipped seam module today, so the control's current verdict is unchanged.
    - A fixture containing `db.exec("commit")`, `db.exec("end")` and `db.exec("end transaction")` still yields THREE — 28-18's coverage is not regressed.
    - A fixture containing `db.exec("commit;")`, `db.exec("end;")` and `db.exec("COMMIT ;")` yields THREE, where the pre-task matcher yields ZERO.
    - A fixture containing `db.exec("insert into t values (1); commit")` yields ONE, where the pre-task matcher yields ZERO.
    - A fixture containing the identifiers `commitTransaction`, `applyWriteWithoutCommit`, `doCommit`, an `exec()` whose argument is a bare identifier, and a user-facing English sentence about a commit yields ZERO — the matcher fires on statements, never on identifiers and never on prose.
  </behavior>

  <action>
    Match the statement inside the literal rather than the whole literal.

    - Rewrite `commitStatements()` so it first finds every `exec()` call whose
      single argument is a string literal in any of the three quote characters,
      then tests the literal's CONTENTS for a commit statement anchored at a
      statement boundary — the start of the string or a preceding `;` — and ended
      by a `;` or the end of the string. The statement vocabulary stays SQLite's
      three spellings and stays case-insensitive.
    - The statement-boundary anchor is the part that keeps the matcher from firing
      on prose, and it must be preserved deliberately rather than incidentally.
      Say so in the function's own comment, naming 28-18 P1: a matcher that fired
      on any occurrence of the word inside a literal would put the wording of
      every error message back under a control in a different file, which is
      exactly the coupling 28-18 removed and this task must not restore.
    - Widen `THREE_SPELLINGS_FIXTURE` to carry the semicolon spellings and rename
      it to say what it now covers. Add the multi-statement fixture as its own
      named control with its own expected count, because it is a different evasion
      from a trailing semicolon and a single fixture asserting one total would let
      one of the two regress silently.
    - Extend `NO_STATEMENT_FIXTURE` with an `exec()` taking a bare identifier
      argument and a user-facing English sentence mentioning a commit, and keep
      its expected count at ZERO. That is the 28-18 P1 control and it must be
      named as such in the test's own message so a later reader knows why it
      exists.
    - **The fixture strings are asserted against the MATCHER and are never
      executed.** `anno-seam.test.ts` must not name `node:sqlite` — the
      single-seam control in this very file fails the build if a second module
      does. That these spellings really commit is established out of band: the
      round-5 review ran them against `node:sqlite` on Node 22.22 and the round-5
      verifier confirmed the matcher's counts by running the regex. Record that
      provenance in the fixture's own comment rather than re-establishing it here,
      and do NOT import a database into this file to prove it.
    - Observe the planting: add a second, fully working commit site spelled
      `db.exec("commit;")` in `anno-store.ts` by hand, run
      `node --test anno-seam.test.ts`, and record BOTH results — the pre-task
      matcher's verdict on the same planting (which is a PASS, and that is the
      finding) and the new matcher's `not ok` line. Then restore and confirm
      `git diff --stat -- src/mcp/vice` is empty and the file is green.
  </action>

  <verify>
    <automated>set -o pipefail; cd src/mcp/vice &amp;&amp; node --test anno-seam.test.ts anno-durability.test.ts 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - The three fixture counts are recorded as numbers: the original three spellings yield 3; the three semicolon spellings yield 3 where the pre-task matcher yielded 0; the multi-statement fixture yields 1 where the pre-task matcher yielded 0. The pre-task numbers are obtained by running the OLD matcher over the SAME fixtures once, and both sets are quoted.
    - The negative fixture yields 0 and its assertion message names 28-18 P1 as the reason it exists; the SUMMARY quotes that message.
    - The matcher still finds exactly ONE commit statement in the shipped seam module — recorded as the number `1`.
    - The planting is observed: with `db.exec("commit;")` added to `anno-store.ts` by hand, the SUMMARY quotes the new matcher's `not ok` line AND records that the pre-task matcher PASSED the same planting. After restore, `git diff --stat -- src/mcp/vice` is empty and `node --test anno-seam.test.ts` is green — both recorded.
    - `grep -c 'node:sqlite' src/mcp/vice/anno-seam.test.ts` is unchanged from its pre-task value — this file must not gain a database import to test its fixtures.
    - `cd src/mcp/vice && node --test anno-seam.test.ts anno-durability.test.ts` reports `# fail 0` at exit 0 with a `# pass` count strictly greater than the pre-task count; both recorded.
    - `npx tsc --noEmit -p src/mcp/vice/tsconfig.json` produces no output and exits 0.
  </acceptance_criteria>

  <done>
    A commit site cannot hide behind a trailing semicolon or a second statement in
    the same `exec()`, the coverage is proven by fixtures in both directions, and
    no user-facing message's wording is constrained by a control in another file.
  </done>
</task>

<task type="auto">
  <name>Task 2: the round's record — twelve dispositions, one requirement row, two open items carried forward</name>

  <files>.planning/phases/28-the-store-core/28-REVIEW.md, .planning/REQUIREMENTS.md</files>

  <reversibility rating="reversible">
    Two planning documents. Nothing executes and nothing on disk in the product
    changes. The requirement row transcribes a verdict already recorded elsewhere,
    so reverting it loses nothing that is not still in `28-VERIFICATION.md`.
  </reversibility>

  <read_first>
    - `.planning/phases/28-the-store-core/28-REVIEW.md` `:167-183` — the ROUND-4
      disposition table. This is the FORM to copy: a `fix` cell names its plan and
      quotes a number or a line reference from that plan's SUMMARY; an `accept`
      cell states what makes it acceptable. Read at least three cells in full
      before writing one.
    - `.planning/phases/28-the-store-core/28-REVIEW.md` `:185-200` — the
      `### Round-5 finding dispositions — REQUIRED FROM THE ORCHESTRATOR` block
      naming the twelve ids, and the paragraph after it about line-number drift
      between round-3/round-4 bodies and round-5 bodies. The new table replaces
      the placeholder block's request, not the placeholder's explanation.
    - `.planning/phases/28-the-store-core/28-VERIFICATION.md`
      `### Round-5 Finding Dispositions` (the twelve-row table near the end) — the
      verifier's verdict for each id, which is what this table transcribes. Note
      its correction on CR-09's attribution: it falsifies criterion 3's stated
      purpose, NOT criterion 1. That correction must survive into this table.
    - `.planning/phases/28-the-store-core/28-VERIFICATION.md` Requirements
      Coverage table and Gaps Summary — the three record verdicts and, verbatim,
      the sentences that authorise them, including *"Per prohibition 28-18 P2 the
      rows move only on a verification verdict — this is that verdict, and the
      sentences above are the authorising ones."*
    - `.planning/phases/28-the-store-core/28-VERIFICATION.md`
      `behavior_unverified_items` — both entries, with their `test`, `expected`
      and `why_human` fields, which the two STILL-OPEN rows reproduce.
    - `.planning/REQUIREMENTS.md` — the STORE checkbox list around `:81-87`, the
      status table around `:216-221`, and the prose record blocks around
      `:242-246`. Read all three regions before editing any of them; the prose
      blocks describe the ROUND-4 state and cite CR-08 as STORE-04's blocker, and
      CR-08 is closed.
    - `.planning/phases/28-the-store-core/28-19-SUMMARY.md`, `28-20-SUMMARY.md`
      and `28-21-SUMMARY.md`, plus this plan's task-1 result — the numbers and
      line references every `fix` cell must quote.
  </read_first>

  <action>
    Write the record, and make each cell carry its own evidence.

    - Add a `### Round-5 finding dispositions` section to `28-REVIEW.md`, placed
      after the round-4 table and before the Critical Issues section, in the
      round-4 table's exact form. One row per id, twelve rows:
      * CR-09, IN-06, WR-08 — `fix`, by 28-19. Quote from `28-19-SUMMARY.md`: the
        CR-09 drive's before and after, the row that used to be written at span
        11, and the suite count growth.
      * WR-18, WR-22, WR-24 — `fix`, by 28-20. Quote from `28-20-SUMMARY.md`: the
        `rollbackFailed` production-reader line numbers (an empty list before),
        the before/after refusal for `revertTo("0001")`, and the staging-residue
        count.
      * WR-21, WR-25 — `fix`, by 28-21. Quote from `28-21-SUMMARY.md`: the
        duplicate-scope before/after result objects with the row counts, and the
        `openStore` default-refusal message with its post-refusal `existsSync`
        value.
      * WR-19 — `fix`, by 28-22 task 1. Quote the three fixture counts and the
        planting's `not ok` line.
      * WR-20, WR-23, IN-07, IN-08 — `accept`, each stating what makes it
        acceptable, in the verifier's own terms: WR-20 has no criterion or
        requirement depending on it and was not verified in depth this round;
        WR-23's comment is to be fixed when the function is next touched, and NO
        plan in this round touched `contradictedCommentsFor`, which must be stated
        as a fact rather than implied; IN-07 and IN-08 are accepted as recorded.
      * A cell that cannot name a plan and quote a number MUST read `accept` with
        its reason. Do not write a `fix` cell whose evidence is "the plan says so".
    - Add TWO explicit STILL-OPEN rows below the table, one per carried-forward
      `behavior_unverified` item, each reproducing its unchanged reason and stating
      that no plan in round 5 constructs its precondition and none claims to:
      * `openStore`'s `integrity_check could not be run at all` arm. Cite it BY
        FUNCTION AND ARM, and note that the line reference drifts every round —
        round 4 cited `:432`, round 5 cited `:465-468`, and 28-21 edits `openStore`
        again, so quote the CURRENT line number found on this plan's own tree and
        say it was re-derived.
      * 28-17's `backstop`-tagged host-crash durability bound, on which the
        round-5 verifier ABSTAINED as `insufficient_spec` rather than counting it
        verified. Record it as a `backstop` truth that stays a backstop. Do NOT
        promote it; 28-17 P5 and 28-18 P3 both forbid it.
    - In `.planning/REQUIREMENTS.md`, VERIFY EACH ROW BEFORE WRITING and then make
      only the diff that is actually needed:
      * STORE-04 -> `Complete` in the status table AND `[x]` in the checkbox list.
        This is the one real change.
      * STORE-01 and STORE-03 — confirm they already read `Gaps Found` and `[ ]`.
        If they do, make NO edit and record in the SUMMARY that the verifier's two
        other requested moves were already satisfied by commit `737f9e7`, with the
        confirmed values quoted. A record that silently skipped them would be
        indistinguishable from a record that forgot them.
      * Keep the status table and the checkbox list consistent with each other,
        and assert it: the two must not disagree for any of the six.
    - Replace the stale prose record blocks around `:242-246` with a ROUND-5
      record. It must state: that CR-08 is CLOSED and was re-driven over three
      input classes, which is why STORE-04 moves up; that STORE-01 and STORE-03 are
      blocked by CR-09 and what CR-09 is in one sentence; and — per 28-18 P2 —
      quote the verifier's own authorising sentences and name their source file
      and table. Use the form the existing round-4 blocks use, which quote the
      verifier and name `.planning/phases/28-the-store-core/28-VERIFICATION.md`
      and the table by name.
    - State plainly in the record, once, that this round's plans are EXECUTED and
      not VERIFIED, and that STORE-01 and STORE-03 move only when a verification
      pass says so. The round-4 record made exactly that statement and it is what
      keeps 28-18 P2 honest across rounds.
    - Do NOT edit `.planning/STATE.md` or `.planning/ROADMAP.md`. The orchestrator
      owns STATE.md and the planner has already updated ROADMAP.md's Phase 28
      section for this round. Assert both untouched.
  </action>

  <verify>
    <automated>set -o pipefail; cd src/mcp/vice &amp;&amp; node --test docs-review-disposition.test.ts audit-integrity.test.ts 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `28-REVIEW.md` contains a `### Round-5 finding dispositions` section with exactly twelve id rows; the SUMMARY lists all twelve ids and their verdicts as a table so a reader can check the count without opening the file.
    - Every `fix` cell names its plan AND quotes at least one number or line reference from that plan's SUMMARY. The SUMMARY quotes three such cells in full as evidence that the form was followed.
    - Every `accept` cell states what makes it acceptable. The WR-23 cell states as a FACT that no plan in round 5 touched `contradictedCommentsFor`, and the SUMMARY records the command used to confirm it.
    - Two STILL-OPEN rows exist, one per carried-forward `behavior_unverified` item, each with its unchanged reason. The `integrity_check` row quotes a line number RE-DERIVED on this plan's own tree, and the SUMMARY records both that number and the round-5 report's `:465-468` so the drift is visible rather than silent.
    - The host-crash durability item is recorded as remaining a `backstop`; the SUMMARY states explicitly that it was NOT promoted and names 28-17 P5 and 28-18 P3.
    - `.planning/REQUIREMENTS.md`: STORE-04 reads `Complete` in the status table and `[x]` in the checkbox list. Recorded as the before value (`Gaps Found` / `[ ]`) and the after value.
    - STORE-01 and STORE-03 are confirmed already at `Gaps Found` / `[ ]`; the SUMMARY quotes the confirmed values and states that no edit was needed and why. If the current tree disagrees with the plan-time measurement, the ACTUAL state wins and the discrepancy is recorded.
    - The status table and the checkbox list agree for all six STORE rows in this phase — asserted and recorded as six pairs.
    - The round-5 prose record quotes the verifier's authorising sentences and names `.planning/phases/28-the-store-core/28-VERIFICATION.md` and the table they came from, and states that this round is executed and not verified.
    - `node --test src/mcp/vice/docs-review-disposition.test.ts` reports 7 pass / 0 fail and `node --test src/mcp/vice/audit-integrity.test.ts` reports 44 pass / 0 fail, recorded as a NON-REGRESSION with the explicit note that both were green before this plan as well and that the transcription is a durability move for the record, not a repair.
    - `git status --porcelain .planning/STATE.md .planning/ROADMAP.md` is empty.
    - No worktree isolation was used for this task; the SUMMARY states it and quotes `git show --stat HEAD -- .planning` for the commit that carries these edits, proving the `.planning/` paths are actually in it.
  </acceptance_criteria>

  <done>
    All twelve round-5 ids carry a decision that lives in `28-REVIEW.md` and will
    survive the next overwrite of the verification report; the one requirement row
    that actually needed moving has moved, with the verifier's own sentence as its
    authority; and both open human-verification items are on the record as open.
  </done>
</task>

<task type="auto">
  <name>Task 3: the round's closing gate — every claim re-observed as a number on the final tree</name>

  <files>src/mcp/vice/anno-seam.test.ts</files>

  <read_first>
    - `.planning/phases/28-the-store-core/28-18-PLAN.md` task 3 — the closing gate
      this one extends. Its structure, its refusal to report aggregates, and its
      three executor self-assertions are the template.
    - `.planning/phases/28-the-store-core/28-19-SUMMARY.md`, `28-20-SUMMARY.md`,
      `28-21-SUMMARY.md` — the recorded actuals this gate re-observes rather than
      repeats, and the source of the derived expected suite count.
    - `src/mcp/vice/anno-types.test.ts` — the split-orientation control and its
      collapse planting (criterion 1's red).
    - `src/mcp/vice/anno-index.test.ts` — the exhaustive cross-validation and its
      `comparisons` non-vacuity assertion (criterion 2's red).
    - `src/mcp/vice/anno-overlap.test.ts` — the fully-contained case, PLANTING A,
      and 28-19's round-trip invariant with PLANTING C (criterion 3's reds).
    - `src/mcp/vice/anno-store.test.ts` — the no-splitter structural scan with its
      non-vacuity companion; the two root-sensitive controls' `{ skip: ... }`
      declarations; and 28-20's three structural controls — the `discardSnapshot`
      occurrence count inside `revertTo`'s body, the step-9 `pruneSnapshots`
      consumption, and the step-6 sweep-result binding — which are the controls
      PLANTING D, PLANTING E and PLANTING F target, so the expected `not ok` line
      is known before the hand edit rather than guessed after it.
    - `src/mcp/vice/anno-durability.test.ts` — the combined planted-violation test
      and its live subprocess planting (criterion 4's red).
    - `.planning/phases/28-the-store-core/28-VERIFICATION.md` — the gap's
      six-line CR-09 drive, verbatim, and the round-4 CR-08 numbers
      (`69632 -> 69632`, `retained [0,1,2]` before and `[0,2]` after) that this
      gate re-observes after three plans rewrote `anno-store.ts`.
  </read_first>

  <action>
    Run and record the closing gate. This task writes no production code; its
    deliverable is measured evidence in the SUMMARY, plus one small addition to
    `anno-seam.test.ts` only if a check below reveals a missing control.

    - Run `node --test anno-*.test.ts block-class.test.ts` from `src/mcp/vice` as
      uid 1000 and record the exact `# tests` / `# pass` / `# fail` / `# skipped`
      / duration lines and the REAL exit code. `# fail` must be `0` and
      `# skipped` must be `0`. Derive the expected number as
      `28-21's recorded actual + this plan's added tests` and record the derived
      number, the actual number, and — if they differ — the list of added test
      names accounting for the difference. A count that did not GROW would mean
      the round's tests were not added; a count that grew by a different amount is
      explained, never rounded.
    - Re-observe 28-15's four named non-vacuity controls INDIVIDUALLY and quote
      each result verbatim: the split-orientation control and its collapse
      planting in `anno-types.test.ts`; the exhaustive index cross-validation with
      its `comparisons` assertion in `anno-index.test.ts`; the fully-contained
      overlap case and PLANTING A in `anno-overlap.test.ts`; and the no-splitter
      structural scan with its non-vacuity companion in `anno-store.test.ts`. An
      aggregate is not acceptable here — three rounds of green suites sat on top of
      live blockers, and that is the failure mode this step defends against.
    - Re-observe THIS ROUND's four new non-vacuity controls individually and quote
      each: 28-19's round-trip re-acceptability invariant with its three counts;
      28-20's structural consumption controls; 28-21's escape-hatch pin with its
      non-vacuity companion; and this plan's task-1 fixture controls in both
      directions.
    - Re-observe THIS ROUND's plantings ON THE FINAL TREE, because every one of
      them runs over code a later plan in the round rewrote:
      * 28-19's PLANTING C — delete the remainder gate from `retype()` by hand,
        run `node --test anno-overlap.test.ts`, record the `not ok` lines,
        restore, confirm green;
      * 28-21's escape-hatch planting — add a fifth use of the escape option by
        hand, record the seam control's `not ok`, restore, confirm green;
      * 28-21's `addScope` idempotence planting — delete the check by hand, record
        the `not ok`, restore, confirm green;
      * 28-20's PLANTING D — put ONE `discardSnapshot(staging)` call inside
        `revertTo` back to a bare `rmSync(staging, { force: true })` by hand, run
        `node --test anno-store.test.ts`, record the `not ok` for the
        occurrence-count control, restore, confirm green;
      * 28-20's PLANTING E — discard `pruneSnapshots`'s return value at
        `runWriteSequence` step 9 by hand, record the `not ok` for the step-9
        consumption control, restore, confirm green;
      * 28-20's PLANTING F — unbind `revertTo` step 6's sweep result by hand,
        record the `not ok` for the step-6 binding control, restore, confirm
        green;
      * this plan's synonym planting — add `db.exec("commit;")` as a second commit
        site, record the `not ok`, restore, confirm green.
      That is SEVEN plantings from this round, each a separate hand edit — never
      two at once, because a combined edit cannot show that either control
      discriminates on its own. 28-20's three are the ones this gate must not
      drop: PLANTING D is the only red evidence for WR-24's cleanup-routing
      control, and PLANTINGS E and F the only red evidence for WR-18's two
      consumption controls. 28-20's tasks 2 and 3 are not red-first the way its
      task 1 is, so unlike that task's WR-22 refusals those three controls have
      been seen red nowhere else in the round. After each restore, confirm
      `git diff --stat -- src/mcp/vice` is empty.
    - Re-observe criterion 4's planted red on the final tree: replace the module's
      single commit statement with a comment by hand, run
      `node --test anno-durability.test.ts`, record that the combined test and its
      planted-violation sibling both report `not ok`, then restore and confirm
      `git diff --stat -- src/mcp/vice` is empty and the file is green. This is a
      manual step and it must not be skipped: a durability proof whose red has not
      been observed against the shipped code is a claim, not a proof.
    - Re-drive the CR-09 REPRODUCTION ITSELF on the final tree, through production
      entry points only and with no hand edit of the store file — the verifier's
      own six-line drive: `setDataType($1000..$100f, "lo_hi_address")`, then
      `setDataType($1004..$1004, "byte")`, then `listRanges()`. Record BEFORE and
      AFTER as facts: before this round the second call returned
      `{revision:2, changed:true, contradictedComments:[]}` and left
      `id=3 4101..4111 lo_hi_address` at span 11; after it, record what the second
      call does and what `listRanges()` returns. Then attempt the third line of
      the verifier's drive — re-asserting `$1005..$100f` as `lo_hi_address` — and
      record that the odd row it targeted does not exist.
    - Re-drive the CR-08 REPRODUCTION on the final tree, for the same reason:
      three plans rewrote `anno-store.ts` and one of them rewrote `revertTo`
      itself. Build a store at revision 3, truncate `r1.db` to 0 bytes, call
      `revertTo(handle, 1)`, and record as numbers: the store's byte length before
      and after (round 4's pair is `69632 -> 69632`), the thrown error's class and
      `instanceof ViceError`, whether the same handle still answers
      `currentRevision()`, `retainedRevisions()` before and after the truncation
      (`[0,1,2]` then `[0,2]`), and whether a later `openStore` succeeds.
    - Run `npx tsc --noEmit -p src/mcp/vice/tsconfig.json` and record it clean.
    - Confirm the phase's structural invariants are unmoved and record each
      individually: `node:sqlite` reachable from exactly one shipped module;
      exactly one commit statement in `anno-store.ts` under the NEW matcher;
      `anno-store.ts` and `anno-types.ts` both absent from
      `hostpath-consumers.test.ts`'s five-element consumer set; `SCHEMA_VERSION`
      still 2 and `anno_snapshot` still one column; the two root-sensitive
      controls still declaring real `{ skip: ... }` and the suite reporting
      `# skipped 0` as uid 1000; no test in the round's diff asserting stderr is
      empty.
    - Re-run `anno-confinement.test.ts` individually one last time and quote its
      15-case result with the one declared skip — 28-21 changed `openStore`'s entry
      conditions and this is the round's last chance to catch a quietly broadened
      refusal (28-09 P1, 28-12 P2).
    - Record the round's ONE convention change, so a later reader sees it was
      deliberate rather than drift: every `<automated>` command in 28-19, 28-20,
      28-21 and this plan now begins `set -o pipefail`. Rounds 1-4 piped
      `node --test` into `tail`, which made the command's exit status `tail`'s and
      therefore unconditionally 0 — a run emitting `# fail 3` in its captured text
      still reported success, so the automated verb gated nothing and the real gate
      lived only in the acceptance criteria's "a REAL exit code of 0", i.e. in
      executor discipline. State in the SUMMARY that the change was made in all
      four of this round's plans, and record for this task the exit status the
      pipeline actually returned alongside the `# fail` line, so the two agree.
    - Re-run both docs guards and record them: `docs-review-disposition.test.ts`
      7/7 and `audit-integrity.test.ts` 44/44. State plainly that they were green
      BEFORE this round as well — this is a recorded non-regression, not a repair,
      and the round-4 report's opposite claim was wrong for exactly this reason.
    - If, and only if, `anno-seam.test.ts` is missing one of task 1's fixture
      controls, add it rather than adjusting the expectation.

    Finally, make four EXECUTOR SELF-ASSERTIONS and write all four into the SUMMARY
    as statements of fact with their evidence. They are self-assertions rather than
    a human checkpoint because this plan is `autonomous: true` and the phase's
    `human_verify_mode` is `end-of-phase`, so the human gate for the round sits at
    the phase boundary and not inside this task:
      * criterion 4's planted red WAS re-observed on the final tree and reverted —
        quote the `not ok` lines and the empty `git diff --stat`;
      * the CR-09 reproduction WAS re-driven on the final tree through production
        entry points and no longer produces an odd split row — quote the second
        call's result and the `listRanges()` output;
      * the CR-08 reproduction WAS re-driven and is still a refusal — quote the
        byte-length pair;
      * BOTH `behavior_unverified` items remain OPEN, were NOT claimed closed by
        this round, and their reasons are unchanged.

    Deliberately NOT done here: re-running the full-glob suite (it outlives the
    tool timeout and its failure baseline is unrelated); chasing
    `r2000-session.test.ts`'s 5 missing-binary failures or `audit-gate.mjs:198`'s
    all-or-nothing attribution (both recorded pre-existing by the verifier);
    touching `.planning/STATE.md` or `.planning/ROADMAP.md`; and any work on
    WR-20, WR-23, IN-07 or IN-08, which are accept-only for this round.
  </action>

  <verify>
    <automated>set -o pipefail; cd src/mcp/vice &amp;&amp; node --test anno-types.test.ts anno-index.test.ts anno-overlap.test.ts anno-store.test.ts anno-seam.test.ts anno-durability.test.ts anno-confinement.test.ts block-class.test.ts 2>&amp;1 | tail -12</automated>
  </verify>

  <acceptance_criteria>
    - `node --test anno-*.test.ts block-class.test.ts` from `src/mcp/vice` reports `# fail 0`, `# skipped 0`, a real exit code of 0, and a `# pass` count strictly greater than 28-21's recorded actual. The derived number, the actual number and any difference's explanation are all recorded. The STRICTLY-GREATER clause is the part that must hold unconditionally; the exact number is recorded evidence, not a gate.
    - `npx tsc --noEmit -p src/mcp/vice/tsconfig.json` produces no output and exits 0.
    - The `set -o pipefail` convention change is recorded in the SUMMARY as a deliberate round-5 change covering all four of this round's plans, with the reason (a `tail` pipe returns `tail`'s exit status, so the pre-change automated verb could not fail) and with this task's own pipeline exit status recorded beside its `# fail` line.
    - All four of 28-15's named non-vacuity controls are re-run INDIVIDUALLY and quoted verbatim — not summarised as an aggregate.
    - All four of this round's new non-vacuity controls are re-run individually and quoted.
    - All SEVEN of this round's plantings — 28-19's PLANTING C, 28-21's escape-hatch and `addScope` idempotence plantings, 28-20's PLANTING D, E and F, and this plan's synonym planting — plus criterion 4's carried planted red, are each re-observed red and restored: EIGHT plantings in total, each with its own quoted `not ok` line naming the control it targets and an empty `git diff --stat -- src/mcp/vice` after each. The arithmetic is checkable: seven from this round plus one carried equals eight.
    - The CR-09 reproduction re-driven on the final tree records the second call's full result and the `listRanges()` output, and records that no row of odd span carrying a split type exists. The before-state (`{revision:2, changed:true, contradictedComments:[]}` and `id=3 4101..4111 lo_hi_address`) is quoted alongside.
    - The CR-08 reproduction re-driven on the final tree records the byte-length pair `69632 -> 69632`, the thrown class with `instanceof ViceError` true, the same handle still answering `currentRevision()`, `retainedRevisions()` as `[0,1,2]` before and `[0,2]` after, and a later `openStore` succeeding.
    - The structural invariants are recorded individually as six separate results, not as one sentence.
    - `node --test anno-confinement.test.ts` reports 15 cases green with the one declared skip, quoted verbatim.
    - `node --test src/mcp/vice/docs-review-disposition.test.ts` reports 7 pass / 0 fail and `node --test src/mcp/vice/audit-integrity.test.ts` reports 44 pass / 0 fail, recorded as a non-regression with the note that both were green before this round too.
    - `git status --porcelain .planning/STATE.md .planning/ROADMAP.md` is empty.
    - The SUMMARY carries all four executor self-assertions with their evidence.
  </acceptance_criteria>

  <done>
    The whole anno surface is green with zero failures and zero skips at a real
    exit code, every control that makes this phase's criteria able to go red has
    been re-observed on the final tree, both of the round's reproductions have been
    re-driven through production entry points, and all of it is recorded as numbers
    rather than as adjectives.
  </done>
</task>

</tasks>

<assumption_delta_decision>
The detector fires `detected: true` on the same four recycled ROADMAP signals as
28-19, 28-20 and 28-21. None is introduced by this round.

**Decision: `no-change`.** This plan introduces no new meaning for any contested
noun. It does resolve one smaller reading for the record, in the same way 28-18
resolved the noun `commit`: 28-18 promoted the STATEMENT reading over the WORD
reading, and this plan completes that promotion — the statement is what SQLite
executes, and a statement can carry a trailing semicolon or share an `exec()` with
a neighbour. 28-18's matcher recognised only the degenerate case where the literal
and the statement are the same string; this one recognises the statement inside
the literal.

**What would force a later change:** a commit issued other than through an
`exec()` carrying a string literal — a prepared statement, or a literal built by
concatenation. Either would make the matcher miss a site, and task 1's fixture
controls are what would surface it as a failing coverage assertion rather than as
a silently split planting.
</assumption_delta_decision>

<edge_coverage_disposition>
`28-EDGE-COVERAGE.json`: 21 applicable items, all `unresolved`. This plan LIFTS
two and records the rest as carried; none is dropped.

| requirement | category | how this plan resolves it |
|---|---|---|
| STORE-07 | idempotency | Task 1's matcher is asserted to yield the SAME one-site count over the unchanged shipped module, and `anno-seam.test.ts`'s existing idempotency test (`re-running the scan over an unchanged tree yields the identical one-element importer list`) is re-run individually by task 3 — the run-it-twice probe applied to the seam scan. |
| STORE-05 | ordering | Task 3 re-observes the DDL invariants individually — `SCHEMA_VERSION` still 2, `anno_snapshot` still one column, `bank` still read in exactly one place — which is the stability half of the ordering probe over the on-disk shape. |

**Carried, not lifted:** the other nineteen. STORE-01's boundary/adjacency/empty/
idempotency and STORE-03's adjacency rows are lifted by 28-19 and 28-21 in this
same round; STORE-04's two by 28-20. The remainder are carried by named executed
plans, and task 3's individual re-runs are what turn "already carried" from an
assertion into an observation.
</edge_coverage_disposition>

<threat_model>
ASVS enforcement level: **1**. Blocking severity threshold: **high**. This plan
changes one control and two records. Its realistic surface is the integrity of the
evidence a sixth round will trust, plus the one structural control that keeps the
durability proof honest.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a commit statement's SPELLING -> the control that counts commit sites | The control is what guarantees the durability planting has exactly one site. Anything spellable past it splits the planting and lets half the violation survive a green run. |
| the recorded requirement status -> the next round's planner and verifier | Status rows are read as facts by everything downstream. A row moved on an executor's own judgement of its own work is fact-laundering, and this repo has already been bitten by it. |
| `28-VERIFICATION.md` -> `28-REVIEW.md` | A disposition living only in the verification report disappears the next time that report is overwritten. That is how round 4 lost the disposition it thought it had. |
| `28-REVIEW.md` -> `docs-review-disposition.test.ts` | The guard reads this file. An edit that breaks its parser turns a repo-wide silence detector off. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-53 | Tampering | the single-commit-site control (`anno-seam.test.ts`) | high | mitigate | Task 1 matches the STATEMENT inside an `exec()` literal, anchored at a statement boundary, covering the semicolon and multi-statement evasions the verifier confirmed by running the regex. Proven with fixtures in both directions and a planting observed red where the pre-task matcher passed. |
| T-28-54 | Repudiation | a disposition that lives only in `28-VERIFICATION.md` | high | mitigate | Task 2 transcribes all twelve into `28-REVIEW.md`'s own table in the round-4 form, so the decisions survive the next overwrite of the verification report. This is the verifier's own instruction and the failure mode it names. |
| T-28-55 | Repudiation | requirement status rows (`.planning/REQUIREMENTS.md`) | high | mitigate | Task 2 transcribes verdicts already recorded in `28-VERIFICATION.md`, quotes the authorising sentences, names the source file and table, and verifies each row's ACTUAL state before writing, so the diff is the one needed rather than the one the report's prose implies (28-18 P2). |
| T-28-56 | Repudiation | the two carried-forward `behavior_unverified` items | high | mitigate | Task 2 records both as explicit STILL-OPEN rows with unchanged reasons; task 3's fourth self-assertion re-states it. Neither is promoted, and the host-crash bound stays a `backstop` (28-17 P5, 28-18 P3). |
| T-28-57 | Spoofing | a disposition claiming a fix the evidence does not support | high | mitigate | Every `fix` cell must name its plan and quote a number or line reference from that plan's SUMMARY; a cell that cannot reads `accept` with its reason. Asserted as an acceptance criterion rather than left to good intentions. |
| T-28-58 | Tampering | `docs-review-disposition.test.ts`'s parser | medium | mitigate | The new table uses the existing round-4 section's format and adds no heading shaped like a finding id; both guards are run before and after and asserted unchanged at 7/7 and 44/44. |
| T-28-59 | Denial of service | a closing gate that reports an aggregate instead of numbers | high | mitigate | Task 3 re-runs every named control individually and quotes it, re-drives BOTH reproductions, and re-observes eight plantings red and restored (seven from this round, one carried) — the failure mode being defended against is five rounds of green suites sitting on top of live blockers. |
| T-28-60 | Repudiation | a `.planning/` deliverable silently stripped by worktree isolation | high | mitigate | Worktree isolation is forbidden for this plan and the prohibition is stated in the objective; task 2 asserts the `.planning/` paths are actually in the commit with `git show --stat HEAD -- .planning`. |
| T-28-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan and no dependency is added, so the package-legitimacy gate has nothing to audit. |
</threat_model>

<artifacts_this_phase_produces>
**Changed tests (`src/mcp/vice/anno-seam.test.ts`)**
- `commitStatements()` — matcher changed from "the whole literal is a commit statement" to "the literal CONTAINS a commit statement at a statement boundary", covering trailing semicolons and multi-statement `exec()` calls. Its enclosing test's name is unchanged.
- the three-spelling fixture — widened with the semicolon spellings and renamed to say what it covers.

**New tests (`src/mcp/vice/anno-seam.test.ts`) — 2**
- a fixture control asserting the multi-statement `exec()` evasion is counted (expected count 1, where the pre-task matcher yields 0)
- a negative fixture control asserting identifiers, an `exec()` taking a bare identifier, and a user-facing English sentence about a commit all yield 0 — the 28-18 P1 control

**New planning records**
- `.planning/phases/28-the-store-core/28-REVIEW.md` — a `### Round-5 finding dispositions` table with twelve id rows, plus two explicit STILL-OPEN rows for the carried-forward human-verification items.
- `.planning/REQUIREMENTS.md` — STORE-04 moved to `Complete` in the status table and the checkbox list; the stale round-4 prose record blocks replaced by a round-5 record quoting the verifier and naming its source.

**No new exported symbols. No new error classes. No new source files. No production logic changes. No on-disk column, directory layout or `SCHEMA_VERSION` change. `.planning/STATE.md` and `.planning/ROADMAP.md` are NOT modified by this plan. No worktree isolation is used by any task in it.**
</artifacts_this_phase_produces>

<verification>
- The commit-statement matcher counts trailing-semicolon and multi-statement spellings, proven by fixtures in both directions, and the synonym planting is observed red where the pre-task matcher passed it.
- No user-facing message's wording is constrained by a control in another file — asserted by the prose negative control, not by inspection.
- All twelve round-5 ids carry a recorded decision in `28-REVIEW.md`, every `fix` cell citing evidence and every `accept` cell stating what makes it acceptable.
- STORE-04 reads `Complete`; STORE-01 and STORE-03 are confirmed already at `Gaps Found`; the status table and the checkbox list agree for all six.
- Both `behavior_unverified` items are carried forward as OPEN with unchanged reasons and neither is promoted.
- `docs-review-disposition.test.ts` is 7/7 and `audit-integrity.test.ts` is 44/44 both before and after — a recorded non-regression, not a repair.
- The whole anno surface reports `# fail 0`, `# skipped 0`, exit 0, `# pass` strictly greater than 28-21's recorded actual; `npx tsc --noEmit` clean.
- Both reproductions are re-driven on the final tree, and every named control and planting is re-observed individually.
</verification>

<success_criteria>
- A commit site cannot hide behind a semicolon or a neighbouring statement, and the repair does not re-couple a structural control to error prose.
- Every round-5 finding has a decision on the record where the record will survive, with evidence, and every `accept` says what makes it acceptable.
- The one requirement row that needed moving has moved, with the verifier's own sentence as its authority, and the two rows that did not need moving are recorded as confirmed rather than silently skipped.
- Both open human-verification items are open on the record, and neither has been promoted by a round that could not construct their preconditions.
- The round closes on numbers re-observed on the final tree — the suite count at a real exit code, each named control individually, eight plantings red and restored, and both reproductions re-driven through production entry points.
</success_criteria>

<output>
Create `.planning/phases/28-the-store-core/28-22-SUMMARY.md` when done. Record the
gate's numbers verbatim — the three fixture counts before and after, the suite
counts, the eight re-observed controls each quoted individually, the five planted
`not ok` lines with their restore diffs, the CR-09 drive before and after, the
CR-08 byte-length pair and `retainedRevisions()` lists, the six structural
invariants, `anno-confinement.test.ts`'s 15-case result, both docs-guard counts,
and the twelve-row disposition summary. The next round's verifier reads those
numbers; adjectives are what let five rounds of green suites sit on top of live
blockers.
</output>
