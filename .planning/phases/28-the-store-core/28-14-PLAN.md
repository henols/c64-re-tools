---
phase: 28-the-store-core
plan: 14
type: execute
wave: 12
depends_on: [28-13]
files_modified:
  - src/mcp/vice/anno-store.ts
  - src/mcp/vice/anno-store.test.ts
  - src/mcp/vice/anno-durability.test.ts
  - src/mcp/vice/anno-durability-mutator.mjs
autonomous: true
gap_closure: true
requirements: [STORE-04, STORE-05, STORE-07]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "TRACE: existing truth 12 + gap-2 missing items 5 and 8. With a genuinely separate OS process holding a read transaction on the store, an accepted-path write REFUSES inside the ViceError family — an `AnnoStoreError` naming the store path and the revision — rather than escaping as a bare `Error: database is locked`; `currentRevision()` afterwards is UNCHANGED; and `handle.db.exec(\"begin immediate\")` on that same handle SUCCEEDS, proving the write lock was released."
    - "TRACE: existing truth 12 + gap-1 missing item 4. `revertTo` never hands the caller an exception for a revert that already succeeded on disk. This is a COMPOSITE property of 28-13 plus this plan and its two controls are declared as such: the behavioural control (ring directory unreadable, `revertTo` returns a usable handle at the reverted revision with `currentRevision()` and `listRanges()` both answering) passes because 28-13 made the sweep non-throwing, and does NOT discriminate this plan's handler; the handler itself is pinned by a declared source-structural backstop, because after 28-13 no reachable input makes the sweep throw."
    - "TRACE: existing truth 12. The store is usable again after a contention refusal: once the reading process exits, the same handle performs an accepted `setDataType` that advances the revision by exactly one, so a refused write left no residue on the connection."
  artifacts:
    - "src/mcp/vice/anno-store.ts — step 8's `commitTransaction` call inside a handler that rolls back, discards the staging file and refuses by name; `revertTo` step 6's sweep inside a handler that closes `restored` and returns a usable handle"
    - "src/mcp/vice/anno-durability-mutator.mjs — a fourth mode that holds a READ transaction open, signalled by a marker file"
    - "src/mcp/vice/anno-durability.test.ts — the cross-process reader-versus-commit test"
    - "src/mcp/vice/anno-store.test.ts — the unreadable-ring revertTo test and the step-6 structural backstop"
  key_links:
    - "`runWriteSequence` step 8 → `commitTransaction` → the ViceError family — the one statement round 3 reproduced escaping it"
    - "`revertTo` step 6 → `openStore` → `reconcileSnapshotRing` — the path that threw a bare EACCES AFTER the revert had already succeeded on disk"
    - "`anno-durability-mutator.mjs` → a genuinely separate OS process — the only construction that makes SQLite's EXCLUSIVE-lock contention real rather than simulated"
  prohibitions:
    - statement: "MUST NOT convert a committed write into a caller-visible failure. Once the commit has returned, the write happened; a later housekeeping failure that throws makes a caller retry an additive verb and produce a second row. (carried forward: 28-11 P5, recorded HELD in round 3 — the new commit handler runs BEFORE the commit returns and must not weaken this)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT report a conflict without both of the numbers that conflicted. (carried forward: 28-08 P2, recorded HELD in round 3 — the new refusal message must name the revision the store is still at)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT let a writer that is about to be refused mutate anything a committed writer owns. (carried forward: 28-08 P1, recorded PARTIALLY HELD in round 3 — the new commit-failure path must roll the CAS back so a refused write leaves the revision where it was)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT leave in place, or introduce, a comment that asserts a guarantee the code does not provide. (carried forward: 28-07 P3, recorded VIOLATED in round 3)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT destroy the only remaining route back to a state the store still advertises as reachable. (carried forward: 28-07 P1, recorded VIOLATED in round 3; this plan must not re-open it while editing the same function)"
      status: unverified
      verification: judgment
---

<objective>
Close the two remaining halves of verification gap 2 that live on the write and
revert paths: **CR-06**, where step 8's `commitTransaction` sits outside every
handler, and **CR-07's third property**, where `revertTo` step 6 throws a
non-family error for a revert that has already succeeded on disk and leaks the
`restored` connection.

Purpose: round 3 reproduced, with a genuinely separate OS process holding a READ
transaction, an accepted-path write throwing a bare `Error: database is locked`
after 5016 ms — outside the ViceError family — with the transaction left OPEN,
the CAS applied, and `currentRevision()` reporting 2 for a write that never
landed. A `COMMIT` needs an EXCLUSIVE lock, so an ordinary reader is enough;
`begin immediate` never excluded readers. On the Phase 29 tool path a handle
lives as long as the session, which makes the leaked write lock strictly worse.

Output: two handlers, one new mutator mode, and three tests that fail against the
pre-plan code.

**Scope discipline.** This plan touches exactly two statement ranges in
`anno-store.ts` — step 8's commit and `revertTo`'s step 6 — and adds tests. It
does not revisit `reconcileSnapshotRing` (plan 28-13 owns it), the WR-02 swallow,
the staging/publish window, `openStore`, the DDL or the confinement predicate
(plan 28-15 owns that). Three rounds of evidence say opportunistic edits in this
file are how the next blocker arrives.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-the-store-core/28-VERIFICATION.md
@.planning/phases/28-the-store-core/28-REVIEW.md
@.planning/phases/28-the-store-core/28-13-PLAN.md
</context>

<environment_preconditions>
- Plan 28-13 is executed and committed. This plan reads `reconcileSnapshotRing`
  in the shape 28-13 left it — in particular, the sweep no longer throws, which
  is why task 2's handler is defence-in-depth with a stated backstop rather than
  a behaviourally reachable arm.
- Node >= 22.18. `anno-store.ts` and the `.test.ts` files run as TypeScript
  directly; `anno-durability-mutator.mjs` is plain ESM and is spawned, never
  imported.
- Task 3's test waits on a real 5 s `busy_timeout`, so it is slow by
  construction. Do not shorten it by lowering the timeout: the connection's
  5000 ms `timeout` option is what makes the contention real rather than
  instantaneous, and lowering it would change the behaviour under test.
- Scope every test run to the anno files. The full-glob suite runs ~660 s,
  outlives the Bash timeout, and carries a known 44-failure baseline.
</environment_preconditions>

<tasks>

<task type="auto">
  <name>Task 1: CR-06 — bring step 8's commit inside the ViceError family, rolling back so the write lock is released</name>

  <files>src/mcp/vice/anno-store.ts</files>

  <read_first>
    - `src/mcp/vice/anno-store.ts`, `runWriteSequence` in full (~:1016-1195) — read the CURRENT state after 28-13, not the line numbers quoted in the review, which drift. Pay particular attention to the pre-lock staging handler and the WR-01 main-window handler: the new handler must match their shape rather than invent a third one.
    - `src/mcp/vice/anno-store.ts` ~:250-310 — the `AnnoStoreError` / `ViceError` family definitions and `ViceErrorOptions`' fields, so the new refusal carries the fields the family already uses.
    - `src/mcp/vice/anno-store.ts`, `discardSnapshot` and `publishSnapshot`, so you can state correctly what the staging path is at the moment the commit runs.
    - `.planning/phases/28-the-store-core/28-REVIEW.md` §CR-06 for the verbatim reproduction and the fix sketch, and §IN-05 for the optional `code` propagation. Do not re-derive them.
  </read_first>

  <action>
    Wrap step 8's `commitTransaction(handle.db)` call inside `runWriteSequence`'s
    `doCommit` branch, per gap-2 missing item 5.

    The handler must, in this order:
    - attempt `handle.db.exec("rollback")` inside its own swallowing inner `try`,
      matching the two existing handlers' shape and for the same stated reason —
      there is nothing useful to do with a second error and reporting it would
      replace the caller's actual refusal. The rollback is what releases the
      store's write lock and undoes the CAS, the caller's mutation and the
      pointer-row insert together, so `currentRevision()` on this connection goes
      back to `rev`;
    - call `discardSnapshot(staging)`. State in the comment that this is a no-op
      by the time control reaches here, because `publishSnapshot` has already
      renamed the staged file onto the revision's path, and that the PUBLISHED
      file is deliberately left behind: with the pointer row rolled back it is an
      orphan FILE, the harmless direction, which the next accepted write's sweep
      reclaims;
    - rethrow a `ViceError` unchanged so no existing refusal's class or message
      moves;
    - otherwise throw a new `AnnoStoreError` naming the store path, the revision
      the write would have become, the underlying message, and — explicitly —
      that NOTHING was written, the transaction has been rolled back, and the
      store is still at revision `rev`. Carry `data: { path, revision, step }`
      with a `step` value that distinguishes this arm from the two existing ones.
    - Fold in IN-05's cheap half ON THIS WRAP ONLY: set the error's `code` from
      the underlying error's own `code` property when it has one, so a caller can
      ask whether the failure was lock contention without substring-matching the
      message. Do NOT add a `cause` field to `ViceErrorOptions` and do NOT touch
      `vice.ts` — that is a shared module outside this phase's scope, and IN-05
      is explicitly optional/additive.

    Do NOT move, rewrite or re-order the `pruneSnapshots` call below it, its
    WR-02 swallow, or that block's comment. The WR-02 structural control asserts
    that call sits inside a still-open `try`; the new handler must close BEFORE
    it, leaving that control's subject untouched.

    Deliberately NOT touched by this task: the pre-lock staging handler, the
    WR-01 main-window handler, the mutation-refusal rollback, the stale-base
    refusal, `currentRevision`, `applyWrite`, `applyWriteWithoutCommit`, and
    every other error class in the family.
  </action>

  <verify>
    <automated>cd src/mcp/vice &amp;&amp; node --test anno-store.test.ts anno-seam.test.ts anno-durability.test.ts 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `node --test src/mcp/vice/anno-store.test.ts src/mcp/vice/anno-seam.test.ts src/mcp/vice/anno-durability.test.ts` reports `# fail 0` — the existing 135-test surface, including the WR-02 and WR-11 structural controls and the cross-process CAS proof, is unmoved.
    - Source assertion: in `runWriteSequence`, the `commitTransaction(handle.db)` call inside the `doCommit` branch is inside a `try` whose `catch` contains a `rollback`. Assert it the way the WR-02 control already does — extract the function body from the stripped source and check the commit call's index falls between a `try` opener and its handler — and record it in the SUMMARY as a source assertion, because the behaviour itself is proven cross-process by task 3 rather than by this structural check.
    - `grep -c "db.exec(\"commit\")" src/mcp/vice/anno-store.ts` still returns `1`.
    - The new refusal message names the store path, the word `rollback` or `rolled back`, and the revision the store is still at. `grep` the message template in the source for the revision interpolation.
    - `npx tsc --noEmit -p src/mcp/vice/tsconfig.json` reports no error (the new `code` field must type-check against `ViceErrorOptions`).
    - `git diff --stat src/mcp/vice/anno-store.ts` shows changes confined to `runWriteSequence`'s `doCommit` branch. `reconcileSnapshotRing`, `pruneSnapshots`, `openStore` and `revertTo` are byte-identical to their post-28-13 state.
  </acceptance_criteria>

  <reversibility rating="reversible">
    Wrapping an existing statement and adding an error class instance changes no
    on-disk shape and no published schema. The refusal is a new message inside an
    existing error class, not a new class.
  </reversibility>

  <done>
    A commit failure at step 8 rolls the transaction back, releases the store's
    write lock, and refuses by name inside the ViceError family stating that
    nothing was written and the store is still at revision N.
  </done>
</task>

<task type="auto">
  <name>Task 2: revertTo step 6 — a housekeeping failure never costs the caller a handle for a revert that already succeeded</name>

  <files>src/mcp/vice/anno-store.ts, src/mcp/vice/anno-store.test.ts</files>

  <read_first>
    - `src/mcp/vice/anno-store.ts`, `revertTo` in full (~:1450-1560) — steps 1 through 6, and in particular step 6's long comment about the sweep's conditional second clause, which must be extended rather than replaced.
    - `src/mcp/vice/anno-store.ts`, `reconcileSnapshotRing` AS LEFT BY 28-13 — you need to know it no longer throws, because that determines how this handler is classified in the SUMMARY.
    - `src/mcp/vice/anno-store.test.ts` around the existing revert tests and the STORE-04 idempotency test, for the temp-directory and cleanup idioms this file uses.
    - `.planning/phases/28-the-store-core/28-VERIFICATION.md` gap 1, missing item 4, for the exact acceptance wording.
    - `.planning/phases/28-the-store-core/28-13-PLAN.md` task 3 — the corrections it made to this exact comment, so this task extends around them instead of overwriting them.
  </read_first>

  <action>
    Wrap `revertTo`'s step 6, per gap-1 missing item 4.

    Replace the bare `const restored = openStore(storePath); reconcileSnapshotRing(restored);`
    sequence with: open `restored` as today, then run the sweep inside a `try`
    whose `catch` closes `restored` inside its own swallowing inner `try` and
    then returns a freshly opened handle for the same store path. Closing and
    reopening rather than returning the same handle is deliberate: if the sweep
    threw, that connection's transaction state is unknown, and handing the caller
    a connection that may hold the store's write lock is the defect being closed,
    not a repair of it. The reopen routes through `openStore`, which is already
    inside the ViceError family, so a genuine failure to reopen refuses by name.

    Extend step 6's existing comment rather than replacing it, and note what you
    must NOT re-introduce: plan 28-13 task 3 already corrected the sentence
    "Both halves are resolved here" to say only the FILE half is resolved. Do not
    restore it, do not paraphrase it back, and do not write any new sentence
    implying the row half is swept — that sentence is a corrected 28-07 P3
    violation and re-asserting it here would ship the violation this round exists
    to close. Record: the revert has ALREADY succeeded on disk by this line, so
    the sweep is housekeeping and must never cost the caller a handle; round 3 observed `revertTo` throw a bare
    non-family `Error EACCES` AFTER a successful `rev 4 -> rev 2`, returning no
    handle and leaking a connection nothing could close; and — stated honestly —
    that after plan 28-13 the sweep no longer throws on any reachable input, so
    this handler is defence-in-depth against a future edit rather than a
    currently reachable arm. Do not write a comment claiming the arm is reachable.

    In `src/mcp/vice/anno-store.test.ts` add two controls. NEITHER is a
    discriminating behavioural proof of THIS task's handler, and the plan says so
    rather than letting a green test imply one — after 28-13 the sweep catches
    every reachable throw and returns `deferred: true`, so no input available to
    a test reaches this handler's `catch`. Round 3 found three live blockers
    sitting under controls whose reach was never declared; do not add a fourth.
    - COMPOSITE / POST-28-13 PROPERTY CONTROL — it pins the end-to-end property
      and it passes identically with and without this task's change, e.g.
      `"revertTo returns a usable handle even when its step-6 sweep cannot read the ring (composite: 28-13's non-throwing sweep plus this handler)"`.
      Its header comment must state, in as many words, that it does not
      discriminate this task's edit and that the structural backstop below is the
      control that does.
      Shape: create the store, perform enough accepted writes that at least two
      revisions are retained, record the target revision; `chmodSync` the ring
      directory to writable-but-not-readable; call `revertTo(handle, target)`;
      assert it RETURNS a handle (does not throw), that `currentRevision()` on it
      equals `target`, that `listRanges()` answers on it, and that
      `handle.db.exec("begin immediate")` on it succeeds followed by a rollback.
      Restore the mode in a `finally`. Skip with an explicit message when
      `process.getuid?.() === 0`.
    - STRUCTURAL BACKSTOP, AND IT IS THE CONTROL THAT ACTUALLY BITES for this
      task, labelled as such in its own header comment with the reason: extract
      `revertTo`'s body from the stripped source and assert the
      `reconcileSnapshotRing` call's index falls inside a `try` region, with the
      usual non-vacuity assertions first (body length bounded below, the call
      found). State in the comment that this is a backstop because 28-13 made the
      throwing arm unreachable from any test input, that the handler is therefore
      defence-in-depth against a future edit that reintroduces a throw, and that
      the composite control above pins the end-to-end property rather than this
      edit.

    Deliberately NOT touched: steps 1 through 5 of `revertTo`, its refusal
    message, the staged-copy ordering, the `closeStore` before the rename, the
    residual about the rename being the one step that cannot be done with the
    connection open, and — explicitly — the two sentences 28-13 task 3 corrected
    in this same comment (the FILE-half-only statement, and the `deferred`
    two-bullet qualification of the directory bound).
  </action>

  <verify>
    <automated>cd src/mcp/vice &amp;&amp; node --test anno-store.test.ts anno-durability.test.ts 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `node --test src/mcp/vice/anno-store.test.ts src/mcp/vice/anno-durability.test.ts` reports `# fail 0`.
    - Composite control: with the ring directory mode `0o300`, `revertTo(handle, target)` returns a handle rather than throwing; `currentRevision(returned) === target`; `listRanges(returned)` returns without throwing; and `returned.db.exec("begin immediate")` succeeds. Its header comment states explicitly that it passes with and without this task's handler and therefore does not discriminate this edit.
    - The composite control carries a root-skip guard with a message naming why the case cannot be constructed as root.
    - The structural backstop passes AND its header comment states the word `backstop` together with the reason the arm is unreachable — a structural control that does not say why it is structural reads as laziness and this file's own conventions forbid it.
    - Nothing in this task's diff, in the SUMMARY, or in either test's name claims a discriminating behavioural proof of the step-6 handler. `grep -n "load-bearing" src/mcp/vice/anno-store.test.ts` returns no line inside either new control.
    - `git diff --stat src/mcp/vice/anno-store.ts` for this task shows changes confined to `revertTo`'s step 6 and its comment.
    - `grep -n "Both halves are resolved here" src/mcp/vice/anno-store.ts` still returns nothing after this task — 28-13 task 3's correction survives and was not paraphrased back.
    - No test added by this task asserts stderr is empty.
  </acceptance_criteria>

  <reversibility rating="reversible">
    A `try` around one call plus a reopen changes no on-disk shape and no
    published contract; the function's return type is unchanged.
  </reversibility>

  <done>
    A sweep failure inside `revertTo` closes the restored connection and still
    hands the caller a usable handle inside the ViceError family, for a revert
    that already succeeded on disk.
  </done>
</task>

<task type="auto">
  <name>Task 3: the cross-process proof — a reader in a separate OS process makes the commit refuse in-family, leave the revision unchanged, and release the lock</name>

  <files>src/mcp/vice/anno-durability-mutator.mjs, src/mcp/vice/anno-durability.test.ts</files>

  <read_first>
    - `src/mcp/vice/anno-durability-mutator.mjs` in full — the three existing modes, the argv contract, the "TEST-ONLY" header clause and the reason the filename does not match the `*.test.*` glob. A fourth mode must obey every clause of that header.
    - `src/mcp/vice/anno-durability.test.ts` in full — the `execFileSync` idiom, the `stdio: "pipe"` requirement (it keeps `node:sqlite`'s unconditional `ExperimentalWarning` out of the TAP stream), the parent-does-the-cleaning rule, and the test that asserts the mutator is absent from `package.json`'s `files[]`.
    - `src/mcp/vice/anno-store.ts`, `runWriteSequence`'s `doCommit` branch AS LEFT BY TASK 1 — the refusal this test asserts against.
    - `.planning/phases/28-the-store-core/28-VERIFICATION.md` gap 2, missing item 8, for the exact acceptance wording.
  </read_first>

  <action>
    Add a fourth mode to `anno-durability-mutator.mjs` that holds a READ
    transaction open, and one test in `anno-durability.test.ts` that uses it.

    The mutator mode:
    - Name it in the same style as the three existing mode constants, spelled
      once as a constant so the test file and the mutator cannot disagree by a
      typo that would look like a store bug — the header's own stated reason for
      that idiom.
    - It must `openStore` the given path, execute a read transaction that
      genuinely acquires SQLite's SHARED lock — `begin deferred` alone does not;
      a SELECT inside it does — and then hold it. Reading a row from `anno_meta`
      is the smallest sufficient statement.
    - Signal readiness by CREATING A MARKER FILE whose path is passed as a
      further argv token, and do it only AFTER the read has actually run. A
      marker file rather than stdout, because the parent's wait must be a bounded
      synchronous spin and because the header forbids the parent inspecting the
      child's chatter.
    - Hold for a bounded lifetime — long enough to outlive the parent's 5 s
      `busy_timeout` with margin — then close and exit 0, so a parent that dies
      cannot leave an orphan holding the lock forever.
    - Extend the mutator's header comment with the mode and its purpose, matching
      the density of the three decisions already recorded there.

    The test, e.g.
    `"CR-06: with a separate OS process holding a READ transaction, the commit REFUSES inside the ViceError family, the revision is unchanged, and the write lock is released"`:
    - Create the store and perform one accepted write so the revision under test
      is non-zero and there is something to observe.
    - Spawn the mutator in the new mode with `spawn` (not `execFileSync`, which
      would block the parent for the child's whole lifetime) and `stdio: "pipe"`.
      Wait for the marker file with a bounded spin — `existsSync` in a loop with
      a hard iteration cap and `Atomics.wait` on a `SharedArrayBuffer` or
      `execFileSync` of a short sleep for the pause, whichever the executor finds
      cleanest — and FAIL with a named message if the cap is reached, so a child
      that never started is a loud failure rather than a silent pass.
    - Record `currentRevision(handle)` before the write.
    - Attempt one ordinary accepted-path write through a store entry point, and
      assert with `assert.throws`: the thrown value is `instanceof AnnoStoreError`
      AND `instanceof ViceError`; its message names the store path; and it states
      the store is still at the recorded revision.
    - After the throw, assert `currentRevision(handle)` equals the recorded
      revision — the CAS was rolled back.
    - Assert `handle.db.exec("begin immediate")` SUCCEEDS, then roll it back —
      this is the proof the write lock was released.
    - Kill the child, wait for it to be gone, then perform one more
      `setDataType` and assert it succeeds and advances the revision by exactly
      one — the refusal left no residue.
    - Clean the temp directory in an unconditional `finally`, per this file's
      parent-does-the-cleaning rule.
    - Assert nothing about stderr.

    Deliberately NOT touched: the three existing mutator modes, the shared
    `mutateStore` helper, the `observeMutateKillReopen` helper, the combined
    STORE-04 test and its planted-violation sibling, and the test that asserts
    the mutator is absent from `package.json`'s `files[]`.
  </action>

  <verify>
    <automated>cd src/mcp/vice &amp;&amp; node --test anno-durability.test.ts 2>&amp;1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `node --test src/mcp/vice/anno-durability.test.ts` reports `# fail 0` and its pass count is exactly one greater than before this task.
    - Behavioural, cross-process: while the child holds its read transaction, the parent's write throws a value satisfying `e instanceof AnnoStoreError && e instanceof ViceError`. Against the pre-plan code the same construction throws a bare `Error` whose `instanceof ViceError` is `false`.
    - Behavioural: after that throw, `currentRevision(handle)` equals the value recorded before the write, and `handle.db.exec("begin immediate")` succeeds. Against the pre-plan code the revision has advanced and that statement reports a transaction within a transaction.
    - Behavioural: after the child exits, one `setDataType` succeeds and advances the revision by exactly one.
    - The readiness wait has a hard cap and fails with a named message when the marker never appears — assert this by reading the test source, since a spin with no cap can hang the suite.
    - `node -e "const p=require('./src/mcp/vice/package.json'); if (p.files.includes('anno-durability-mutator.mjs')) { process.exit(1) }"` exits 0, and the existing test asserting that absence is still green.
    - The mutator's filename still does not match `*.test.*`.
    - `node --test src/mcp/vice/anno-*.test.ts` reports `# fail 0` overall.
  </acceptance_criteria>

  <reversibility rating="reversible">
    A new mutator mode and a new test add no production surface and change no
    on-disk shape; the mutator is excluded from the published tarball by an
    assertion that already exists.
  </reversibility>

  <done>
    The CR-06 reproduction is a passing regression test driven by a genuinely
    separate OS process, and it fails against the pre-plan code.
  </done>
</task>

</tasks>

<threat_model>
ASVS enforcement level: **1**. Blocking severity threshold: **high**. Local,
integrity- and availability-shaped surface only.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP caller → `anno-store.ts` | Store arguments arrive unvalidated; the proxy validates nothing by design |
| this process's connection → other OS processes' connections | SQLite locks are the shared resource; a leaked write transaction is a cross-process denial of service |
| parent test process → spawned mutator child | The child is spawned with `stdio: "pipe"` and a marker-file handshake; it must never be importable by a production module or shipped in the tarball |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-06 | Denial of Service | `runWriteSequence` step 8's commit (`anno-store.ts`) | high | mitigate | Task 1: the commit is wrapped and its handler rolls back, so a `SQLITE_BUSY` on COMMIT can no longer leave the store's write lock held for a session-lifetime handle. Proven cross-process by task 3. |
| T-28-07 | Tampering | `runWriteSequence`'s CAS on commit failure (`anno-store.ts`) | high | mitigate | Task 1's rollback undoes the CAS, the mutation and the pointer-row insert together, so `currentRevision()` can no longer report an advanced revision for a write that never landed (prohibition 28-08 P1). Asserted behaviourally in task 3. |
| T-28-08 | Denial of Service | `revertTo` step 6 (`anno-store.ts`) | medium | mitigate | Task 2: the caller can no longer be left with no handle and an unreachable connection holding the write lock after a revert that already succeeded on disk. |
| T-28-09 | Information disclosure | the new refusal message (`anno-store.ts`) | low | accept | The message interpolates the store path and the underlying SQLite message, matching every existing refusal in this module. The store path is caller-supplied and the process is local; no new information crosses a boundary that the existing refusals do not already cross. |
| T-28-10 | Tampering | `anno-durability-mutator.mjs` shipped to consumers | medium | mitigate | Task 3 keeps every clause of the mutator's TEST-ONLY header: absent from `package.json` `files[]` (asserted mechanically on every suite run), never imported by a production module (asserted by `anno-seam.test.ts`), and a filename outside the `*.test.*` glob. |
| T-28-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan; no dependency is added, so the package-legitimacy gate has nothing to audit. |
</threat_model>

<artifacts_this_phase_produces>
**Changed behaviour (`src/mcp/vice/anno-store.ts`)**
- `runWriteSequence` — step 8's `commitTransaction` call is wrapped; a new `AnnoStoreError` refusal is thrown on commit failure, carrying `code` from the underlying error and `data: { path, revision, step }` with a step value distinct from the two existing arms.
- `revertTo` — step 6's `reconcileSnapshotRing(restored)` call is wrapped; on a throw the restored connection is closed and a freshly opened handle is returned.

**No new exported symbols, no new error classes, no signature changes.**

**New symbols (`src/mcp/vice/anno-durability-mutator.mjs`)**
- a fourth mode constant naming the read-transaction-holding mode
- the marker-file argv token contract for that mode

**New tests**
- `src/mcp/vice/anno-durability.test.ts` — the cross-process reader-versus-commit test (the one genuinely discriminating behavioural control this plan adds)
- `src/mcp/vice/anno-store.test.ts` — the unreadable-ring `revertTo` COMPOSITE control (declared non-discriminating for this plan's handler), and the step-6 structural BACKSTOP (the control that bites)

**No new files. No new on-disk shape, column, directory layout or schema version. `vice.ts` and `ViceErrorOptions` are NOT modified — IN-05's `cause` half is deliberately out of scope.**
</artifacts_this_phase_produces>

<verification>
- `node --test src/mcp/vice/anno-*.test.ts` reports `# fail 0`.
- The CR-06 reproduction from `28-REVIEW.md` is a passing test rather than a script: the throw is in-family, the revision is unchanged, and `begin immediate` succeeds afterwards.
- The CR-07 third property is closed: `revertTo` hands back a usable handle when its sweep cannot read the ring.
- `grep -c "db.exec(\"commit\")" src/mcp/vice/anno-store.ts` returns `1`; `anno-seam.test.ts` is green.
- `npx tsc --noEmit -p src/mcp/vice/tsconfig.json` reports no error.
- `git diff --stat src/mcp/vice/anno-store.ts` shows changes confined to `runWriteSequence`'s `doCommit` branch and `revertTo`'s step 6.
</verification>

<success_criteria>
- Nothing the store throws on the accepted write path escapes the ViceError family, proven with a genuinely separate OS process rather than a simulated lock.
- A refused write leaves the revision where it was and the write lock released.
- A revert that succeeded on disk always returns a handle.
- Every prohibition carried forward is still honourable, and nothing in the phase's "already true" set was re-opened.
</success_criteria>

<output>
Create `.planning/phases/28-the-store-core/28-14-SUMMARY.md` when done. Record
explicitly which controls are behavioural and which are source-structural
backstops, and for each backstop the reason its arm is unreachable — round 3
found three live blockers sitting under green structural controls, and an
unlabelled structural control is how that happens.
</output>
