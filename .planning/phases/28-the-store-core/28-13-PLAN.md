---
phase: 28-the-store-core
plan: 13
type: execute
wave: 11
depends_on: [28-10, 28-11, 28-12]
files_modified:
  - src/mcp/vice/anno-store.ts
  - src/mcp/vice/anno-store.test.ts
  - .planning/phases/28-the-store-core/28-10-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [STORE-04, STORE-07]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "TRACE: existing truth 7 + gap-1 missing items 1 and 2. A store reached through a SYMLINK ALIAS and written ONCE through that alias keeps every pointer row and every snapshot file it had before the alias write — the sweep deletes no row, and the prune's doomed loop has not been reached because one write cannot advance past MAX_SNAPSHOT_REVISIONS: reopened by the REAL path, `retainedRevisions()` reports the same list it reported before the alias write, and `revertTo(handle, 1)` SUCCEEDS."
    - "TRACE: existing truth 7 + gap-1 missing items 1 and 2. `mv proj.annostore other.annostore`, ONE accepted write, `mv` back leaves the pre-rename floor intact: `oldestRetainedRevision()` after the rename-back equals its pre-rename value, and `revertTo` to that revision succeeds. The claim is BOUNDED at one write and is not a claim of unconditional recovery — see this plan's residuals, where the prune's doomed loop still reaps rows once the wrong-spelling handle advances past MAX_SNAPSHOT_REVISIONS."
    - "TRACE: existing truth 12 + gap-2 missing item 6. A throw anywhere inside `reconcileSnapshotRing`'s own transaction leaves the CALLER's connection with NO open transaction: with the ring directory writable-but-not-readable, an ordinary `setDataType` reports success and `handle.db.exec(\"begin immediate\")` immediately afterwards SUCCEEDS rather than reporting a transaction within a transaction."
    - "TRACE: existing truth 8's recorded caveat + gap-1 missing item 3 (prohibition 28-07 P3). `snapshotDirFor`'s residual paragraph, `revertTo` step 6's comment, `retainedRevisions`' consumer list and the matching key decision in `28-10-SUMMARY.md` all state what the code actually does — a second spelling publishes into a second ring; the first ring's files survive and its rows survive THE SWEEP, bounded by the prune's doomed loop; and only the FILE half of a half-state is resolved by `revertTo` step 6 — rather than the falsified claims each of them carries today."
  artifacts:
    - "src/mcp/vice/anno-store.ts — `reconcileSnapshotRing` returning `{ droppedFiles: string[]; deferred: boolean }`, its transaction bracketed by a structural handler, and three corrected comment sites: `snapshotDirFor`'s residual paragraph, `revertTo` step 6's 'both halves are resolved here', and `retainedRevisions`' consumer-list clause"
    - "src/mcp/vice/anno-store.test.ts — the symlink-alias survival test, the store-file rename/rename-back floor test, the unreadable-ring no-leaked-transaction test, the strengthened sweep source-order control, the two updated `droppedRows` assertions, and the FOUR re-disposed assertions whose subject was the deleted row-delete (half-state A's two, and the three post-revert `orphanRowRevisions` sites)"
    - ".planning/phases/28-the-store-core/28-10-SUMMARY.md — the corrected key decision and corrected residual bullet"
  key_links:
    - "`runWriteSequence` step 9 → `pruneSnapshots` → `reconcileSnapshotRing` — the production path on which CR-05 destroyed rows and CR-07 leaked a transaction; both reproduced here, both closed here"
    - "`reconcileSnapshotRing` → `commitTransaction` — still the module's SINGLE commit site; the new rollback path must not introduce a second commit statement"
    - "`retainedRevisions` → `snapshotPathFor` → `snapshotDirFor` — the one location authority, unchanged by this plan on purpose"
  prohibitions:
    - statement: "MUST NOT destroy the only remaining route back to a state the store still advertises as reachable. A reconciliation that resolves a half-state by deleting the surviving half in the addressable direction is the loss this gap exists to close, not a repair of it. (carried forward: 28-07 P1 / 28-10 P1 / 28-11 P1, recorded VIOLATED in round 3 by CR-05)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT publish a floor, bound or 'retained' claim the store cannot honour on the very next call, in EITHER direction. An UNDER-claim is a violation too. (carried forward: 28-07 P2 / 28-10 P2, recorded VIOLATED in round 3)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT leave in place, or introduce, a comment that asserts a guarantee the code does not provide. (carried forward: 28-07 P3, recorded VIOLATED in round 3 at anno-store.ts:479-489)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT adopt, migrate, claim or sweep a snapshot ring whose ownership this store cannot establish; MUST NOT let a repair judge a state it cannot have produced. If it cannot establish that moment it must decline and report the declining rather than guess. (carried forward: 28-10 P3 / 28-11 P4, recorded VIOLATED in round 3)"
      status: unverified
      verification: judgment
    - statement: "MUST NOT convert a committed write into a caller-visible failure. Once the commit has returned, the write happened. (carried forward: 28-11 P5, recorded HELD in round 3 — this plan must keep it held)"
      status: unverified
      verification: judgment
---

<objective>
Close the ROOT of verification gap 1 (CR-05) and the sweep half of gap 2 (CR-07)
by answering, once and on the record, the question 28-10 answered wrongly:
**what identifies a snapshot ring, and who may delete a pointer row?**

Round 3's blocker is not a bug in the ring's location. It is the identity model:
28-10 keyed the ring on `basename(handle.path)` — a PATH SPELLING — so a second
spelling of the same inode (a symlink alias, or `mv proj.annostore
other.annostore`) makes `retainedRevisions()` report every existing pointer row
as unretained, after which `reconcileSnapshotRing` classifies them as orphan ROWS
and deletes them under its own committed transaction. Reproduced twice through
production entry points; renaming back recovers nothing.

Purpose: the phase goal promises annotation state that is *"durable across a
`SIGKILL`, REVERTIBLE, and reachable through exactly one persistence seam"*.
Durability and the single seam are VERIFIED for three rounds. Revertible is
false, for the third round, for a third cause. This plan makes the class
STRUCTURALLY unreachable rather than patching the third cause.

Output: a sweep that cannot delete a pointer row at all, a sweep transaction
whose lifetime is structural, and two doc corrections that stop asserting a
guarantee the code does not provide.
</objective>

<the_identity_decision>
**This section is the deliverable the `assumption_delta_checkpoint` asks for, and
it is recorded here rather than left implicit in the code change.**

The detector's question — *"a second X was introduced where there was one → does
the current primary key / identity model still name the right noun?"* — is CR-05
verbatim. The ring's identity currently names a *path spelling*; a second
spelling of the same inode was introduced and the identity model did not survive
it.

**CHOSEN: the decline rule, generalised — `reconcileSnapshotRing` abstains from
the pointer-ROW direction entirely and sweeps only the FILE direction.**

Four reasons, in order of weight:

1. **An orphan ROW is inert, not fatal.** Trap 10 calls the orphan-ROW direction
   "the one the revert path cannot survive", and that was true when it was
   written. It is no longer true of this code: `retainedRevisions()` requires
   BOTH halves, `oldestRetainedRevision()` routes through it, and `revertTo`
   step 2 refuses on `!pointer || !existsSync(snapPath)` BEFORE anything is
   destroyed. Every consumer of "retained" already requires the file. So
   deleting orphan rows is HYGIENE, not safety — and hygiene that has now
   destroyed a reachable revert history.
2. **Rows stay bounded without the sweep.** `pruneSnapshots`' doomed loop
   deletes every row below `currentRevision() - MAX_SNAPSHOT_REVISIONS` (32), so
   orphan rows are reaped on the ordinary path once they age out. Under a wrong
   spelling that reaping is *exactly what the correct spelling would also have
   done* at the same revision — so the behaviour converges to correct instead of
   diverging into loss.
3. **Its failure mode cannot destroy data.** The residual is extra ROWS and
   extra FILES — the direction this codebase's own trap-10 premise already calls
   harmless and reconcilable by revision number. Every deletion-side fix in
   rounds 1–3 found a new way to delete something reachable; this one has no
   deletion side.
4. **No schema bump, no on-disk layout change, no one-way door.** Prohibition
   28-10 P4 (verdict HELD in round 3) is untouched by construction.

**REJECTED (a): the review's LITERAL minimum decline condition** — *"an ABSENT
ring directory beside a non-empty `anno_snapshot`"*, i.e.
`!existsSync(snapshotDir) && rows.length > 0`. **It does not fire on either
reproduced path.** `stageSnapshot` (`anno-store.ts:908`) calls `mkdirSync` on the
ring directory BEFORE the transaction opens, and `publishSnapshot` renames the
new `r<rev>.db` into it — so by the time step 9's sweep runs the ring EXISTS and
holds one file, `retained` is non-empty, and the guard is skipped while rows
0..N-1 are still deleted. Verified by reading `runWriteSequence`'s call order and
by the review's own transcript (`new ring: [ 'r3.db' ]` in the same run that
reports `pointer rows: [ 3 ]`). The chosen rule is that guard's generalisation:
the sweep cannot establish ownership of the row direction in ANY spelling, so it
abstains from it in all of them.

**REJECTED (b): the complete route — a ring id minted into `anno_meta` at first
open, with the ring at a relative sibling `.anno-ring-<id>` of `handle.dir`.**
Three reasons:
  * It **does not remove the class; it moves it.** `anno_meta` travels with the
    store's CONTENTS — which is the point — but contents are copied. `cp
    a.annostore b.annostore` gives two distinct store FILES one shared ring id,
    so both sweep the same ring and each deletes the other's files. That is
    CR-01's destructive shape with a new cause and no test watching. A `vacuum
    into` snapshot carries the same id for the same reason.
  * It requires **`SCHEMA_VERSION` 2 → 3**, which prohibition 28-10 P4 makes
    mandatory for an on-disk layout change. That is a **one-way door**: it makes
    the previously published shape refuse by name.
  * It is a large edit to `openStore`, the DDL, `AnnoStoreHandle` and ~10 test
    literal sites — in the exact ~200 lines whose churn produced the blockers of
    rounds 1, 2 and 3.

**The `deferred` tension, resolved and recorded.** `deferred: true` is WIDENED to
mean "this sweep changed nothing" — covering both *another writer holds the write
lock* and *this sweep could not complete and rolled back*. **No second
discriminator is added.** Two reasons: its only consumer (`pruneSnapshots`)
returns early identically in both cases, so a discriminator would have no reader;
and CR-07's concealment complaint — that a leaked transaction makes every LATER
sweep report `deferred` indistinguishably from contention — is removed **at its
source** by task 2 (the transaction can no longer leak), not papered over with a
label. A label that describes a state the code can no longer reach is exactly the
comment prohibition 28-07 P3 forbids.

**Residuals that remain STATED rather than claimed closed** (task 3 writes them
into `snapshotDirFor`'s doc comment). Read them as a set: each one is a limit on
the recovery claim, and the recovery claim must never be written down without
them.
  * each spelling of the store file accretes its OWN ring, so
    `MAX_SNAPSHOT_REVISIONS` bounds each ring but not the on-disk footprint
    across spellings;
  * **the recovery is BOUNDED, not unconditional.** The sweep never deletes a
    pointer row, but `pruneSnapshots`' doomed loop still deletes every row below
    `currentRevision() - MAX_SNAPSHOT_REVISIONS`. So restoring the original name
    restores the floor **only while the wrong-spelling handle has not advanced
    past `MAX_SNAPSHOT_REVISIONS` further revisions**; past that the old rows are
    reaped on the ordinary path — bounded, delayed by 32 revisions, and identical
    to what the correct spelling would have done at the same revision. Anywhere
    this plan or its SUMMARY says rows "survive", the qualifier is **by the
    sweep**;
  * **prohibition 28-07 P2 stays VIOLATED in the under-claim direction, by
    design, and is not claimed closed.** Opened under a second spelling the store
    still reports `retainedRevisions() == []` and
    `oldestRetainedRevision() == NO_RETAINED_REVISION` while the first ring's
    files sit on disk. This route removes the DESTRUCTION that under-claim used
    to drive (the sweep no longer acts on it) but not the under-claim itself.
    Closing it would mean teaching `retainedRevisions` to look in a ring this
    handle cannot establish ownership of, which is prohibition 28-10 P3 / 28-11
    P4 and is the guess this whole decision exists to refuse.
</the_identity_decision>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-the-store-core/28-VERIFICATION.md
@.planning/phases/28-the-store-core/28-REVIEW.md
</context>

<environment_preconditions>
- `use_worktrees` is `false` in `.planning/config.json`. Task 3 edits a
  `.planning/` file, which a worktree-isolated executor would silently strip from
  its commit. If isolation has been forced on for this dispatch, task 3 cannot
  deliver — halt and report rather than reporting a success it did not achieve.
- Node >= 22.18: `anno-store.ts` runs as TypeScript directly, with no build step.
  There is nothing to compile after editing it.
- Scope every test run to `node --test src/mcp/vice/anno-*.test.ts`. The
  full-glob suite runs ~660 s, outlives the Bash timeout, and carries a known
  44-failure baseline unrelated to this work.
</environment_preconditions>

<tasks>

<task type="tracer">
  <name>Task 1: the sweep can no longer delete a pointer row — proven end-to-end through a symlink alias before anything else is touched</name>

  <files>src/mcp/vice/anno-store.ts, src/mcp/vice/anno-store.test.ts</files>

  <read_first>
    - `src/mcp/vice/anno-store.ts` in full for `reconcileSnapshotRing` (starts ~:690, ends ~:781), `retainedRevisions` (~:568-575), `snapshotDirFor` (~:479-493), `pruneSnapshots` (~:813-878) and `runWriteSequence`'s step 9 (~:1160-1190) — read the CURRENT state, not the line numbers quoted in the review, which drift.
    - `src/mcp/vice/anno-store.test.ts` around :1780-1900 (the half-state resolver tests and the STORE-04 idempotency test), :1980-2010 (the contention/decline test) and :2085-2115 (the sweep source-order control).
    - `src/mcp/vice/anno-store.test.ts` :1251-1284 — the `ringHalves` and `orphanRowRevisions` helpers. Every assertion this task must re-dispose runs through one of them, and `ringHalves` reads the pointer rows and the directory listing INDEPENDENTLY of `retainedRevisions`, which is what keeps the replacement assertions non-vacuous.
    - `src/mcp/vice/anno-store.test.ts` :1483-1500, :1550-1556, :1605-1615 and :1726-1772 — the FOUR existing assertions this task's change reddens. Read each in full, including its in-test comment, before touching it: each comment states the guarantee the assertion was protecting, and the replacement has to carry that same guarantee rather than drop it.
    - `.planning/phases/28-the-store-core/28-10-PLAN.md`, the `<objective>` and the alternatives table — so you can see which decision this task reverses and why the reversal is not a regression.
    - `.planning/phases/28-the-store-core/28-REVIEW.md` §CR-05 for the two verbatim reproductions. Do not re-derive them.
  </read_first>

  <action>
    Implement the CHOSEN route recorded in this plan's `<the_identity_decision>`
    section, and nothing beyond it.

    In `reconcileSnapshotRing`:
    - Change the return type to `{ droppedFiles: string[]; deferred: boolean }`.
      Remove the `droppedRows` field and the local `droppedRows` array. Removing
      the field rather than leaving it permanently empty is deliberate: a field
      that can only ever answer one value is a claim the next reader has to
      falsify by experiment.
    - Delete step 3 entirely — the `dropRow` prepared statement and the loop that
      runs it. Delete the `orphanRows` local and the line that computes it from
      `rows` and `retained`, since nothing consumes them any more. Keep the
      `rows` read only if the doc comment still needs it; if nothing reads it,
      delete that read too rather than leaving an unused query.
    - Keep step 1 (`begin immediate` inside its swallowing `try`, returning
      `deferred: true` on failure) exactly as it is. It is 28-11's CR-02 fix and
      it still governs the FILE direction: the lock is what stops the sweep
      unlinking a concurrent writer's just-published snapshot.
    - Keep step 2's directory scan, `SNAPSHOT_FILE_PATTERN` anchoring, the
      `existsSync(snapshotDir)` guard and the `retained` set read through
      `retainedRevisions(handle)`. Keep step 4's `commitTransaction(handle.db)`
      call and step 5's swallowing unlink loop, in that order.
    - Do NOT introduce a second `commit` statement anywhere. `anno-seam.test.ts`
      asserts this module contains exactly ONE, because the durability proof's
      planted violation must have a single site.
    - Rewrite the ORPHAN ROW bullet in the function's doc comment so it records
      the decision and its evidence: the row direction is no longer swept; an
      orphan row is inert because every consumer of "retained" requires the FILE
      as well as the row; rows are still bounded because the prune loop reaps
      everything below `currentRevision() - MAX_SNAPSHOT_REVISIONS`; and the
      reason is CR-05, reproduced twice, where deleting rows under a second path
      spelling destroyed a reachable revert history irreversibly. Name the two
      reproductions (a symlink alias and a store-file rename) explicitly.
    - Do NOT write the removed SQL statement text into any comment or string
      literal inside this function. The source-order control extracts this
      function's body with literal bodies KEPT, so a surviving string literal
      would make the absence assertion below vacuously fail.

    Deliberately NOT touched by this task, and say so in the commit message:
    `snapshotDirFor`'s location formula, `SNAPSHOT_DIR_SUFFIX`,
    `retainedRevisions`' BODY and its two-halves argument, `snapshotPathFor`, the
    DDL, `SCHEMA_VERSION`, `openStore`'s `resolve` versus realpath branch,
    `pruneSnapshots`' doomed loop, and `revertTo` in its entirety. Two comment
    sites that this task's change falsifies are owned by TASK 3 and must not be
    edited here, so the two tasks cannot collide in the same lines:
    `retainedRevisions`' consumer-list sentence and `revertTo` step 6's comment.
    This task owns exactly one comment site — `reconcileSnapshotRing`'s own ORPHAN
    ROW bullet. Every one of them is either VERIFIED or the
    subject of a held prohibition, and three rounds of evidence say opportunistic
    edits in this file are how the next blocker arrives.

    In `src/mcp/vice/anno-store.test.ts`:
    - Update the two assertions that read `droppedRows` off a sweep result (one
      in the STORE-04 idempotency test around :1894, one in the contention
      decline test around :1995) so they assert the new shape. Assert the field
      is genuinely gone rather than deleting the line: check that the returned
      object has no own property of that name, so a future re-introduction is
      caught.
    - RE-DISPOSE, BY NAME, THE FOUR EXISTING ASSERTIONS THIS CHANGE REDDENS.
      They are enumerated here rather than left to discovery, because each one's
      SUBJECT is the deleted statement and an executor meeting them at a red
      suite would improvise a weakening of controls the round-3 verifier counted
      as evidence. Do not delete any of the four tests; re-dispose the assertion
      and rewrite its in-test comment to say why.
      * `:1768` and `:1769`, in "prune half-state A, the orphan ROW" — the
        assertions that the orphan row is gone after one accepted write and that
        no orphan row survives at all. INVERT them: with `writes` at
        `MAX_SNAPSHOT_REVISIONS + 8` the victim is the NEWEST retained revision,
        so it sits far above the prune's floor and its row now SURVIVES the
        accepted write. Assert that survival explicitly, and assert alongside it
        the properties that make the survival harmless and that the test already
        proves earlier — the row is not retained, is never published as the
        floor, and `revertTo` to it refuses BY NAME inside the family. Change the
        test's own title: the trailing clause "and swept by the next accepted
        write" becomes a statement that it is TOLERATED by the next accepted
        write. Write the reason into the test comment, naming CR-05: the sweep
        can no longer distinguish this row from a row belonging to a ring under
        another spelling, and deleting it under that ambiguity destroyed a
        reachable revert history.
      * `:1493-1498`, after `revertTo(store, firstFloor)` — `orphanRowRevisions(store, dir)`
        deep-equal `[]`, with the in-test comment "the restored image reinstates
        rows 0..firstFloor-1 whose files the prune removed". That cleanup WAS
        step 6's sweep. RE-STATE it as the guarantee it was protecting rather
        than the mechanism that used to deliver it: no revision the store
        ADVERTISES may lack either half. Assert, against `ringHalves`' raw output
        so the check does not run through the filtering helper and go vacuous:
        `retainedRevisions(store)` is non-empty; every element of it appears in
        BOTH `ringHalves(...).rowRevisions` and `ringHalves(...).fileRevisions`;
        and `oldestRetainedRevision(store)` is not `NO_RETAINED_REVISION`.
      * `:1614`, and the conditional `:1553` in the same file — the same
        `orphanRowRevisions(...)` deep-equal `[]` invariant after a second
        revert. Re-state both exactly as above.
      * In every one of the three re-stated sites, KEEP the companion assertion
        that no FILE survives unclaimed. That direction is still resolved by the
        sweep, it is the half this task does not touch, and dropping it would
        weaken a control the change does not affect.
      * RECORD IN THE SUMMARY that the round-3 `backstop_assessment` entry
        `28-07 D11` changes shape: its half-state-A construction now demonstrates
        that an orphan ROW is TOLERATED and inert, not that it is RESOLVED. The
        substitution it accepted (two directly constructed half-states plus a
        source-order control, in place of a timed mid-prune kill) is unchanged;
        what changed is what half-state A demonstrates. A backstop whose meaning
        moved without being re-declared is how a green control comes to sit on
        top of a live defect.
    - Strengthen the sweep source-order control (around :2085). Keep its
      non-vacuity assertions and its surviving ordering clause (the commit
      precedes the unlink) intact. Replace the row-delete clause with an ABSENCE
      assertion over the same extracted body: the sweep body must contain no
      pointer-row DELETE at all. Rewrite the control's own header comment to say
      why the clause changed — the statement it ordered was removed on purpose,
      and the absence is now the guarantee.
    - Add the tracer's own test, named so it carries its finding id, e.g.
      `"CR-05: a store reached through a SYMLINK ALIAS keeps every pointer row and every snapshot file — the sweep can no longer delete a row it does not own"`.
      Its shape: create the store at `<dir>/real.annostore` with
      `{ workspaceRoot: dir }`; perform three accepted `setDataType` writes;
      record `retainedRevisions()`, the sorted ring filenames and
      `oldestRetainedRevision()`; close. Create `<dir>/alias.annostore` as a
      relative symlink to `real.annostore`. Open the ALIAS with `openStore(alias)`
      and NO `workspaceRoot` — that is the exact production shape `revertTo`
      itself uses, and supplying a root would realpath the alias away and make
      the test prove nothing; assert that non-vacuity explicitly by checking the
      alias handle's own ring directory differs from the real one. Perform ONE
      `setDataType` through the alias handle and close it. Then assert, reopening
      by the REAL path: the pointer rows still include every pre-alias revision;
      every pre-alias snapshot file is still on disk; `retainedRevisions()`
      equals the pre-alias list; and `revertTo(handle, 1)` SUCCEEDS and lands at
      revision 1.
  </action>

  <verify>
    <automated>cd src/mcp/vice &amp;&amp; node --test anno-store.test.ts anno-seam.test.ts 2>&amp;1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - `node --test src/mcp/vice/anno-store.test.ts src/mcp/vice/anno-seam.test.ts` reports `# fail 0`.
    - The new symlink-alias test exists, passes, and its non-vacuity check confirms the alias handle's ring directory path differs from the real handle's — a test that accidentally opened the same ring would prove nothing.
    - Behavioural, through production entry points only: after one `setDataType` through the alias handle, reopening by the real path gives `retainedRevisions()` equal to the pre-alias list (three entries), and `revertTo(handle, 1)` returns a handle at `currentRevision() === 1`. Before this change the same sequence yields `retained []`, `oldestRetainedRevision() === NO_RETAINED_REVISION` and a refusal.
    - `node -e` against the compiled-free source: calling `reconcileSnapshotRing` on any handle returns an object for which `Object.prototype.hasOwnProperty.call(result, "droppedRows")` is `false`.
    - `grep -c "db.exec(\"commit\")" src/mcp/vice/anno-store.ts` returns `1` — the module still has exactly one commit statement, and `anno-seam.test.ts`'s single-commit assertion is green.
    - The sweep source-order control passes with its non-vacuity assertions intact (extracted body length still bounded below, the commit and the unlink both still found) AND its new absence clause: the extracted `reconcileSnapshotRing` body contains no pointer-row delete statement.
    - All four named assertions are re-disposed and none of the four tests is deleted: `grep -c "^test(" src/mcp/vice/anno-store.test.ts` returns its pre-task value plus one (the one test this task ADDS).
    - The half-state A test asserts the orphan row SURVIVES the accepted write, and its title no longer claims the row is swept. `grep -n "swept by the next accepted write" src/mcp/vice/anno-store.test.ts` returns no line inside that test.
    - Each of the three re-stated post-revert sites asserts, against `ringHalves`' raw output rather than through `retainedRevisions`, that every retained revision appears in BOTH raw halves and that the retained list is non-empty — so the replacement cannot pass vacuously on a store whose ring reads empty.
    - Each of the three re-stated sites still asserts that no snapshot FILE survives unclaimed.
    - Every re-disposed assertion carries an in-test comment naming CR-05 and stating which guarantee it now carries.
    - `git diff --stat src/mcp/vice/anno-store.ts` shows changes confined to `reconcileSnapshotRing` and its doc comment. `snapshotDirFor`'s body, `retainedRevisions`' body, the DDL and `openStore` are byte-identical to their pre-task state.
  </acceptance_criteria>

  <reversibility rating="reversible">
    Removing a field from a module-internal return type and removing a delete
    statement change no on-disk shape, no published schema and no file layout. A
    later round can restore row-sweeping behind a real identity witness without
    migrating a single store on disk.
  </reversibility>

  <done>
    A write performed through a second spelling of the same store file cannot
    delete a pointer row. The symlink-alias route is closed end-to-end and pinned
    by a passing test that fails against the pre-task code.
  </done>
</task>

<task type="auto">
  <name>Task 2: CR-07 — the sweep's transaction gets a structural lifetime, so a throw can never leave the caller's connection in a transaction</name>

  <files>src/mcp/vice/anno-store.ts, src/mcp/vice/anno-store.test.ts</files>

  <read_first>
    - `src/mcp/vice/anno-store.ts`, `reconcileSnapshotRing` AS LEFT BY TASK 1 — read it again after task 1's edit rather than working from the pre-task shape.
    - `src/mcp/vice/anno-store.ts`, `runWriteSequence` step 9 (~:1160-1190) — the WR-02 wrap that swallows a sweep throw and makes this defect silent. It stays as it is; understand why before editing anything above it.
    - `.planning/phases/28-the-store-core/28-REVIEW.md` §CR-07 for the two verbatim reproductions and the fix sketch. Do not re-derive them.
    - `src/mcp/vice/anno-store.test.ts` :1980-2010 — the existing contention/decline test, which is the shape the new test should sit beside.
  </read_first>

  <action>
    Give `reconcileSnapshotRing` a structural transaction lifetime, per gap-2
    missing item 6.

    - Declare `orphanFiles` BEFORE the new handler so step 5 can still read it
      after the handler closes.
    - Wrap everything from the drop-set computation through
      `commitTransaction(handle.db)` in one `try`. Its `catch` must: attempt
      `handle.db.exec("rollback")` inside its own swallowing inner `try` (there
      is nothing useful to do with a second error, and reporting it would replace
      the first); then `return { droppedFiles: [], deferred: true }`. It must NOT
      rethrow — a throw from here is swallowed by step 9's WR-02 wrap anyway, and
      rethrowing would convert a committed write into a caller-visible failure,
      which prohibition 28-11 P5 forbids and which round 3 recorded as HELD.
    - Leave step 1's `begin immediate` and its own swallowing `try` OUTSIDE the
      new handler. Its failure means no transaction was ever opened, so a
      rollback there would report "no transaction is active" and obscure the real
      state.
    - Leave step 5's unlink loop OUTSIDE the new handler and AFTER the commit.
      Its position is trap 10's guarantee and must not move.
    - Do not add a second commit statement. The rollback is `exec("rollback")`,
      not a commit, so the single-commit-site invariant is untouched.
    - Update the function's `deferred` doc paragraph to state the WIDENED
      meaning: `deferred: true` now reports "this sweep changed nothing", which
      covers both another writer holding the write lock and this sweep failing
      part-way and rolling back. Record, in the same paragraph, that no second
      discriminator was added and why — the only consumer returns early
      identically in both cases, and the concealment CR-07 complained about
      (a leaked transaction making every later sweep report `deferred`) is
      removed at its source by this handler rather than labelled.
    - Deliberately NOT touched: `runWriteSequence` step 9's WR-02 swallow,
      `pruneSnapshots`' early return on `deferred`, and the unlink loop's own
      per-file swallowing `try`.

    In `src/mcp/vice/anno-store.test.ts` add two tests:
    - The rename/rename-back floor test, e.g.
      `"CR-05 (b): renaming the store FILE, writing, and renaming back leaves the pre-rename floor intact"`.
      Shape: create `<dir>/proj.annostore`, three accepted writes, record
      `oldestRetainedRevision()` and the ring filenames; close; `renameSync` the
      store file to `<dir>/other.annostore`; open the new name with no
      `workspaceRoot`, one accepted `setDataType`, close; `renameSync` back; open
      the original name and assert `oldestRetainedRevision()` equals the recorded
      pre-rename value and `revertTo` to it succeeds.
    - The no-leaked-transaction test, e.g.
      `"CR-07: a sweep that throws inside its own transaction leaves the caller's connection with NO open transaction"`.
      Shape: create the store, perform enough accepted writes that the ring holds
      files, then `chmodSync` the ring directory to writable-but-not-readable so
      the sweep's `readdirSync` throws. Skip the case with an explicit assertion
      message when `process.getuid?.() === 0`, because root ignores the mode bits
      and the construction would silently prove nothing. Then perform one
      ordinary `setDataType` and assert: it SUCCEEDS and reports the advanced
      revision (prohibition 28-11 P5 — the committed write is not converted into
      a failure); `handle.db.exec("begin immediate")` immediately afterwards
      succeeds, followed by `handle.db.exec("rollback")`; and the pointer rows are
      unchanged apart from the one this write inserted. Restore the directory
      mode in a `finally` so cleanup can remove the temp directory.
    - Neither test may assert stderr is empty — `node:sqlite` emits an
      unconditional `ExperimentalWarning` on the Node 22 line.
  </action>

  <verify>
    <automated>cd src/mcp/vice &amp;&amp; node --test anno-store.test.ts anno-durability.test.ts 2>&amp;1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - `node --test src/mcp/vice/anno-store.test.ts src/mcp/vice/anno-durability.test.ts` reports `# fail 0`.
    - Behavioural: with the ring directory mode `0o300`, one `setDataType` returns a result whose revision is the previous revision plus one, and the very next `handle.db.exec("begin immediate")` on that same handle SUCCEEDS. Against the pre-task code the same construction leaves the transaction open and that statement throws with a message reporting a transaction within a transaction.
    - Behavioural: after `mv proj.annostore other.annostore`, one accepted write through the new name, and `mv` back, `oldestRetainedRevision()` equals its pre-rename value and `revertTo` to that revision returns a handle at that revision. Against the pre-task code the same sequence gives `-1` and a refusal.
    - The root-skip guard is present in the unreadable-ring test and carries a message naming why the case cannot be constructed as root.
    - `grep -c "db.exec(\"commit\")" src/mcp/vice/anno-store.ts` still returns `1`.
    - The `reconcileSnapshotRing` doc comment contains a paragraph stating that `deferred` covers both contention and an incomplete sweep, and stating that no second discriminator was added and why.
    - `git diff --stat src/mcp/vice/anno-store.ts` for this task shows changes confined to `reconcileSnapshotRing` and its doc comment; `runWriteSequence` and `pruneSnapshots` are byte-identical to their post-task-1 state.
  </acceptance_criteria>

  <reversibility rating="reversible">
    A `try`/`catch` around an existing statement range plus a rollback changes no
    on-disk shape and no published contract; `deferred`'s widened meaning is
    documentation over a value that already existed.
  </reversibility>

  <done>
    No throw reachable inside `reconcileSnapshotRing` can leave the caller's
    connection holding a transaction, and the two behaviours that prove it are
    pinned by tests that fail against the pre-task code.
  </done>
</task>

<task type="auto">
  <name>Task 3: correct the two comments that assert a guarantee the code does not provide (prohibition 28-07 P3)</name>

  <files>src/mcp/vice/anno-store.ts, .planning/phases/28-the-store-core/28-10-SUMMARY.md</files>
  <precondition>Task 1 is complete, so `reconcileSnapshotRing` no longer deletes pointer rows. Correcting these three comments before the code they describe would leave the module asserting a guarantee it does not yet provide — the same prohibition, polarity reversed.</precondition>

  <read_first>
    - `src/mcp/vice/anno-store.ts` ~:474-493 — `snapshotDirFor`'s doc comment, specifically its paragraph beginning "THE RESIDUAL, STATED RATHER THAN CLAIMED CLOSED".
    - `src/mcp/vice/anno-store.ts` ~:1526-1540 — `revertTo` step 6's comment, specifically the sentence "Both halves are resolved here" and the two bullets below it that qualify the second clause.
    - `src/mcp/vice/anno-store.ts` ~:561-570 — `retainedRevisions`' consumer list, specifically the clause naming `reconcileSnapshotRing` as the consumer that decides "which half of a half-state to drop".
    - `.planning/phases/28-the-store-core/28-10-SUMMARY.md` line 49 (the key-decision bullet) and line 403 (the residual bullet) — both carry the same falsified claim.
    - `.planning/phases/28-the-store-core/28-VERIFICATION.md`, the `prohibition_flags` entry for 28-07 P3, for the exact wording the verifier falsified.
    - This plan's `<the_identity_decision>` section — the residuals it lists are the ones these comments must now state.
  </read_first>

  <action>
    Rewrite `snapshotDirFor`'s residual paragraph so it states the residual the
    code actually has after tasks 1 and 2. The paragraph currently claims the
    store-file-rename case is an accepted UNDER-claim in which the old ring "is
    deliberately NEVER DELETED and `retainedRevisions()` then honestly reports
    `[]`". The verifier falsified that by driving the code: the FILES were never
    deleted but the ROWS were, irreversibly, and restoring the name recovered
    nothing.

    The replacement must say, concretely and WITHOUT an unqualified recovery
    claim: a second spelling of the same store file — a symlink alias, or a
    store-file rename — names a DIFFERENT ring, so this handle publishes into a
    second ring; the first ring's files are never deleted, and since task 1 its
    pointer rows are never deleted BY THE SWEEP. Then, in the same sentence and
    never in a later one, the bound: `pruneSnapshots`' doomed loop still deletes
    every row below `currentRevision() - MAX_SNAPSHOT_REVISIONS`, so restoring
    the original name restores the floor ONLY while the wrong-spelling handle has
    not advanced past `MAX_SNAPSHOT_REVISIONS` further revisions. An unqualified
    "the rows survive, so renaming back recovers the floor" followed by a
    qualifying clause is a paragraph that contradicts itself, and a reader takes
    the unqualified half as the guarantee — that is precisely the 28-07 P3
    failure mode this task exists to correct, reproduced by the correction.
    Then state the two surviving residuals: each spelling accretes its own ring,
    so `MAX_SNAPSHOT_REVISIONS` bounds each ring but not the cross-spelling
    footprint; and prohibition 28-07 P2 remains VIOLATED in the UNDER-CLAIM
    direction under a second spelling — `retainedRevisions()` reports `[]` while
    the first ring's files sit on disk — accepted on the record, because closing
    it would mean reading a ring whose ownership this handle cannot establish,
    which is prohibition 28-10 P3 / 28-11 P4. Follow this module's header discipline: record the
    reversal as a reversal, naming what was RIGHT and stays (the location is a
    pure function of the handle, and a directory rename is not a residual at all)
    and what became FALSE, with the finding id CR-05.

    Correct `revertTo` step 6's comment at ~:1526-1533. It says "The image just
    restored carries `anno_snapshot` rows for revisions whose files an earlier
    prune removed, and it leaves every snapshot taken AFTER `revision` unclaimed
    by any row. **Both halves are resolved here**". After task 1 only the FILE
    half is resolved: the restored image's stale rows are now TOLERATED, and the
    comment as it stands would ship a guarantee the code does not provide — the
    same 28-07 P3 failure this whole round exists to close. Rewrite it to say
    that the FILE half is resolved here and the ROW half is deliberately left,
    naming CR-05 and stating that the handle still never advertises a revision it
    cannot deliver because the published floor routes through
    `retainedRevisions`, which requires both halves regardless of whether either
    half was swept. The comment's existing two-bullet qualification of the
    directory bound under `deferred` is STILL TRUE and must survive verbatim —
    extend, do not replace.

    Correct `retainedRevisions`' consumer-list clause at ~:561-565, which names
    `reconcileSnapshotRing` as the consumer that decides "which half of a
    half-state to drop". After task 1 that resolver decides only the FILE half.
    Narrow the clause to say so. Do NOT touch the two-halves argument above it or
    the function body: both are still exactly true, and the round-3 verifier
    counted them as evidence.

    In `28-10-SUMMARY.md`, correct BOTH sites — the key-decision bullet and the
    residual bullet — to the same corrected statement, each marked as a
    correction made by plan 28-13 after round-3 verification rather than as a
    silent overwrite. A SUMMARY is evidence; editing it to read as though it had
    always been right is the fact-laundering this project's own todo discipline
    forbids. Do NOT touch any other line of that SUMMARY.

    Deliberately NOT touched: `retainedRevisions`' two-halves ARGUMENT and its
    function body (still exactly true — only its consumer-list clause moves),
    `reconcileSnapshotRing`'s doc comment (tasks 1 and 2 already own it),
    `revertTo`'s steps 1-5 and its `deferred` bullets, and
    `anno-types.ts:830-833`'s falsified claim — that
    one belongs to plan 28-15, which is where its code is fixed, because a
    comment corrected in a plan that does not fix the code would be a second
    28-07 P3 violation with the polarity reversed.
  </action>

  <verify>
    <automated>cd src/mcp/vice &amp;&amp; node --test anno-store.test.ts 2>&amp;1 | tail -5 &amp;&amp; git -C ../../.. diff --stat .planning/phases/28-the-store-core/28-10-SUMMARY.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "honestly reports" src/mcp/vice/anno-store.ts` returns no line inside `snapshotDirFor`'s doc comment (the falsified phrase is gone from that paragraph).
    - `snapshotDirFor`'s doc comment names `CR-05`, carries the qualifier "by the sweep" on the row-survival claim, states the recovery bound in the SAME sentence as the recovery claim, and states both surviving residuals — the per-spelling ring accretion and the 28-07 P2 under-claim.
    - `grep -n "Both halves are resolved here" src/mcp/vice/anno-store.ts` returns nothing: `revertTo` step 6's comment no longer claims both halves are resolved, and it names `CR-05`.
    - `revertTo` step 6's existing two-bullet qualification about `deferred` and the directory bound is byte-identical to its pre-task state — extended, not replaced.
    - `retainedRevisions`' consumer list no longer describes `reconcileSnapshotRing` as deciding which half of a half-state to drop; its two-halves argument and its function body are byte-identical to their pre-task state.
    - `git diff --stat .planning/phases/28-the-store-core/28-10-SUMMARY.md` shows a non-empty diff — confirming the `.planning/` edit actually landed and was not stripped by worktree isolation.
    - `grep -n "28-13" .planning/phases/28-the-store-core/28-10-SUMMARY.md` finds the correction attribution on both corrected bullets.
    - `git diff .planning/phases/28-the-store-core/28-10-SUMMARY.md` touches exactly two bullets; no other line of the file changes.
    - `node --test src/mcp/vice/anno-store.test.ts` reports `# fail 0` — a comment-only change to the module must not move any behaviour.
  </acceptance_criteria>

  <done>
    Neither `snapshotDirFor`'s doc comment nor `28-10-SUMMARY.md` asserts a
    guarantee the code does not provide, and both record the correction as a
    correction.
  </done>
</task>

</tasks>

<threat_model>
ASVS enforcement level: **1**. Blocking severity threshold: **high**. The
realistic surface here is local and integrity-shaped, not network-shaped; web-app
categories with no reachable trust boundary in this phase are omitted rather than
padded.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP caller → `anno-store.ts` | Every store argument arrives UNVALIDATED — `vice-proxy.ts`'s `rawJsonSchemaAsStandardSchema()` returns `validate: (value) => ({ value })` by design, so store paths and revisions are attacker-shaped input |
| `anno-store.ts` → the filesystem | Snapshot ring paths, `vacuum into` targets and `rmSync`/`readdirSync` targets cross here; a second path spelling of the same inode is an ordinary, unprivileged way to move this boundary |
| this process's connection → other OS processes' connections | The store explicitly supports concurrent connections (`timeout: 5_000`, a cross-process CAS proof), so a leaked transaction is a cross-process effect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Tampering | `reconcileSnapshotRing` step 3 (`anno-store.ts`) | critical | mitigate | Task 1: the sweep abstains from the pointer-ROW direction entirely, so no path spelling — symlink alias, rename, bind-mount alias or case-folded name — can drive it into deleting a reachable revert history. Pinned by the symlink-alias test and by the source-order control's new absence clause. |
| T-28-02 | Denial of Service | `reconcileSnapshotRing`'s transaction (`anno-store.ts`) | high | mitigate | Task 2: a structural `try`/`catch` rolls the sweep's transaction back on every reachable throw, so the store's write lock cannot be held for a handle's lifetime and other writer processes cannot be locked out. Pinned behaviourally by the unreadable-ring test asserting `begin immediate` succeeds afterwards. |
| T-28-03 | Repudiation | `snapshotDirFor` doc comment, `28-10-SUMMARY.md` | medium | mitigate | Task 3: the two comments that assert a guarantee the code does not provide are corrected, and the correction is attributed rather than silently overwritten, so the record of what the store guarantees stays auditable (prohibition 28-07 P3). |
| T-28-04 | Tampering | ring directory unlink loop (`anno-store.ts` step 5) | medium | accept | Unchanged by this plan and already mitigated by 28-11: the sweep takes the store's write lock before it judges, and `SNAPSHOT_FILE_PATTERN` is anchored so a concurrent writer's staging file is invisible to it. Round 3 recorded 28-11 P3 and P4's lock mechanism as HELD. |
| T-28-05 | Elevation of privilege | store path confinement (`anno-types.ts`) | medium | transfer | Out of scope for this plan by design; owned by plan 28-15, where the code it depends on is fixed. Splitting it here would mean correcting a comment in the plan that does not fix its code. |
| T-28-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan; `package.json` and `package-lock.json` are not in `files_modified`. No new dependency is introduced, so the package-legitimacy gate has nothing to audit. |
</threat_model>

<artifacts_this_phase_produces>
Every symbol this plan creates, changes or removes.

**Changed signatures (`src/mcp/vice/anno-store.ts`)**
- `reconcileSnapshotRing(handle: AnnoStoreHandle): { droppedFiles: string[]; deferred: boolean }` — return type narrowed; the `droppedRows` field is REMOVED.

**Removed symbols (`src/mcp/vice/anno-store.ts`)**
- the `dropRow` prepared statement local inside `reconcileSnapshotRing`
- the `orphanRows` local inside `reconcileSnapshotRing`
- the `droppedRows` local inside `reconcileSnapshotRing`

**Unchanged exported symbols this plan deliberately does NOT touch**
- `snapshotDirFor`, `snapshotPathFor`, `SNAPSHOT_DIR_SUFFIX`, `retainedRevisions`, `oldestRetainedRevision`, `NO_RETAINED_REVISION`, `pruneSnapshots`, `openStore`, `closeStore`, `currentRevision`, `revertTo`, `applyWrite`, `stageSnapshot`, `commitTransaction`, `SCHEMA_VERSION`, `MAX_SNAPSHOT_REVISIONS`, the DDL.

**New tests (`src/mcp/vice/anno-store.test.ts`)**
- `CR-05: a store reached through a SYMLINK ALIAS keeps every pointer row and every snapshot file — the sweep can no longer delete a row it does not own`
- `CR-05 (b): renaming the store FILE, writing, and renaming back leaves the pre-rename floor intact`
- `CR-07: a sweep that throws inside its own transaction leaves the caller's connection with NO open transaction`

**Changed tests (`src/mcp/vice/anno-store.test.ts`)**
- the sweep source-order control — the row-delete ordering clause is replaced by an absence clause; the commit-before-unlink clause and every non-vacuity assertion survive
- the STORE-04 idempotency test and the contention/decline test — their `droppedRows` assertions become own-property-absence assertions
- `prune half-state A, the orphan ROW` — its title's "swept by the next accepted write" clause becomes "tolerated", and its two closing assertions are INVERTED to assert the orphan row survives
- the first-revert site (~:1493), the conditional second-revert site (~:1553) and the second-revert site (~:1614) — each `orphanRowRevisions(...)` deep-equal `[]` assertion is re-stated as "no revision the store advertises lacks either half", checked against `ringHalves`' raw output; each keeps its companion no-unclaimed-FILE assertion

**Changed documentation (`src/mcp/vice/anno-store.ts`)**
- `snapshotDirFor`'s residual paragraph
- `revertTo` step 6's "Both halves are resolved here" sentence
- `retainedRevisions`' consumer-list clause naming `reconcileSnapshotRing`
- `reconcileSnapshotRing`'s ORPHAN ROW bullet and its `deferred` paragraph

**Changed documentation (planning artifacts)**
- the key-decision bullet and the residual bullet in `.planning/phases/28-the-store-core/28-10-SUMMARY.md`

**No new files. No new error classes. No new on-disk shape, column, directory layout or schema version.**
</artifacts_this_phase_produces>

<verification>
- `node --test src/mcp/vice/anno-*.test.ts` reports `# fail 0`. Do not run the full-glob suite: it outlives the Bash timeout and carries an unrelated 44-failure baseline.
- The two CR-05 reproductions from `28-REVIEW.md` §CR-05 are now non-destructive when re-run: the alias route and the rename route each leave the pointer rows and the snapshot files intact, and `revertTo` to the pre-existing floor succeeds in both.
- The CR-07 production-path reproduction (ring directory writable but not readable, one ordinary `setDataType`) reports success AND leaves no open transaction.
- `grep -c "db.exec(\"commit\")" src/mcp/vice/anno-store.ts` returns `1`.
- `anno-seam.test.ts` is green: `node:sqlite` is still reached from exactly one shipped module and that module still has one commit statement.
- No test asserts stderr is empty anywhere in the diff.
</verification>

<success_criteria>
- A write through any second spelling of the same store file cannot delete a pointer row — proven behaviourally through production entry points, not structurally.
- A throw anywhere inside the sweep's transaction leaves the caller's connection with no open transaction — proven behaviourally on the production write path.
- `snapshotDirFor`'s doc comment and `28-10-SUMMARY.md` state the residual the code actually has.
- Every prohibition carried forward in `must_haves.prohibitions` is still honourable against the shipped code, and none of the items listed in the phase's "already true" set was re-opened.
</success_criteria>

<output>
Create `.planning/phases/28-the-store-core/28-13-SUMMARY.md` when done. Record the
identity decision, the rejected routes and their reasons verbatim from
`<the_identity_decision>` — the next round's verifier reads the SUMMARY for the
decision and the PLAN for the argument, and a SUMMARY that omits the rejected
routes invites round five to re-propose them.

Three further things the SUMMARY MUST carry, each because omitting it would be the
same 28-07 P3 failure in a different artifact:

  1. **The bound on the recovery claim.** Rows survive THE SWEEP; the prune's
     doomed loop still reaps them below `currentRevision() -
     MAX_SNAPSHOT_REVISIONS`. Never write the recovery claim without it.
  2. **Prohibition 28-07 P2 stays VIOLATED in the under-claim direction**, by
     design, under a second spelling — `retainedRevisions()` reports `[]` while
     the first ring's files exist. State it as an accepted, recorded residual;
     do not report this round as closing P2.
  3. **The `28-07 D11` backstop changes shape.** Its half-state-A construction
     now demonstrates that an orphan ROW is TOLERATED and inert, not that it is
     RESOLVED. Declare the change; a backstop whose meaning moved silently is how
     a green control comes to sit on top of a live defect.
</output>
