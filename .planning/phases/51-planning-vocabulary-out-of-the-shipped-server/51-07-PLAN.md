---
phase: 51-planning-vocabulary-out-of-the-shipped-server
plan: 07
type: execute
wave: 6
depends_on: ["51-06"]
files_modified:
  - src/mcp/vice/anno-store.ts
  - src/mcp/vice/anno-store.test.ts
  - src/mcp/vice/anno-types.ts
  - src/mcp/vice/anno-store-export.ts
  - src/mcp/vice/anno-derive.ts
  - src/mcp/vice/anno-confidence.ts
  - src/mcp/vice/skills-planning-vocabulary.test.ts
autonomous: true
requirements: [VOCAB-01, VOCAB-02, VOCAB-04, VOCAB-06]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The store core, its type declarations, its export path, its derivation helper and its confidence helper all scan clean and their ratchet entries are gone (VOCAB-01)."
    - "The technical-token collision in `anno-types.ts` is rewritten to plain wording rather than exempted, and no exemption mechanism was added (VOCAB-01)."
    - "The store's workspace-confinement and revert-history explanations survive at full length (VOCAB-02)."
  artifacts:
    - src/mcp/vice/anno-store.ts
    - src/mcp/vice/anno-types.ts
  key_links:
    - "`anno-types.ts`'s declared invariants -> every consumer of the store: these comments are the type system's prose half and are cited from across the annotation family."
---

<objective>
Sweep the annotation store's core, its type declarations and three helpers —
about 193 citations across `anno-store.ts`, `anno-types.ts`,
`anno-store-export.ts`, `anno-derive.ts` and `anno-confidence.ts` — to zero, and
settle the one technical-token collision in the phase.

Purpose: `anno-types.ts` carries the store's numbered invariants in prose. Those
are the most-cited comments in the annotation family and the ones a careless
sweep would compress hardest.

Output: five files at zero, five ratchet entries deleted, and the token collision
resolved by rewriting rather than by an escape hatch.

Counts are sizing context measured before planning; the ledger is the authority.

Locked decisions implemented by this plan:

- D-06: sweep plans partition by module family, not by file and not by citation category.
- D-07: recovery is tiered by identifier class, so lookup effort is spent where the ambiguity actually is.
- D-08: where nothing recovers a reason, the comment is rewritten to a weaker but true statement and the site is named in the plan's summary, never deleted.
- D-10: the technical-token collision is rewritten to plain wording and no exemption mechanism is added.

Those identifiers belong to this planning document. No replacement string this
plan writes into product source may contain one.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-01-SUMMARY.md
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md
@.planning/ENGINEERING_RULES.md
</context>

## Artifacts this phase produces

See `51-01-PLAN.md`. This plan creates no new symbol.

<tasks>

<task type="auto">
  <name>Task 1: Sweep anno-store.ts to zero</name>
  <files>src/mcp/vice/anno-store.ts, src/mcp/vice/anno-store.test.ts, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/anno-store.ts` — the whole header, then every comment block containing a citation, and every string literal containing one
    - `src/mcp/vice/anno-store.test.ts` — so you know which refusal strings are pinned before you change one
    - `.planning/ENGINEERING_RULES.md` section 21.2 — the worked bad/good rewrite pair
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Sections A, B, D and E. This file's review-finding ids are the ones whose resolution chain was walked end to end during research, so Section A's originating-phase row for it is known good
  </read_first>
  <action>
Rewrite every citation in `anno-store.ts` into the reason it stood for, splitting
comment sites from string-literal sites first and recording the split.

Apply the tier ladder: delete a bare parenthetical whose sentence already states
the reason; turn an inline citation into a clause that states it; take a sparse
line's reason from the block above it or from the phase artifacts the resolution
document names; and where nothing recovers a reason, rewrite to a weaker but TRUE
statement and NAME the site in the summary with its line number. Never delete a
comment.

This file's review-finding citations are the cheapest tier-3 lookups in the phase
because the chain has already been demonstrated: the site's own history names the
commit, the commit's subject names the phase, and the fixing plan's summary in
that phase directory states the finding in one quotable line. Check the fixing
plan's summary as well as the review document — the summary usually says what was
actually done and why, while the review states the finding tersely.

Two comment families here are the only record of their fact and must survive at
full length: why workspace confinement became the default when opening a store,
and why a single move plus a single write once destroyed the whole revert history.

Constraints: edit comment spans, and string literals only where the citation sits
in output; do not shorten a header explaining why the file exists; do not repoint
any citation at a different planning-tree path; every replacement string you write
must itself be free of planning-tree paths, phase and plan numbers, decision, gap
and requirement ids, and planning-document cross-references.

Lower this file's `RATCHET` count to the number the guard reports, read from its
failure message, and delete the entry at zero.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test skills-planning-vocabulary.test.ts anno-store.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -aE -c 'docs/phase[0-9]' anno-store.ts)" = "0"</automated>
    <fails_when>the printed count is not 0 — an evidence-document path citation survives in this file</fails_when>
  </verify>
  <acceptance_criteria>
    - `anno-store.ts` has no `RATCHET` entry and scans clean
    - `grep -ac '\.planning' src/mcp/vice/anno-store.ts` outputs `0`
    - The workspace-confinement comment and the revert-history comment are present and not shorter in substance; quote both before and after in the summary
    - The summary states how many sites were in comments and how many in string literals, and names every changed string with its pinning test
    - Every tier-4 site is named in the summary with its line number
  </acceptance_criteria>
  <done>The store core is at zero with its two irreplaceable explanations intact.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep anno-types.ts and three helpers, and rewrite the technical-token collision</name>
  <files>src/mcp/vice/anno-types.ts, src/mcp/vice/anno-store-export.ts, src/mcp/vice/anno-derive.ts, src/mcp/vice/anno-confidence.ts, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/anno-types.ts` — the whole header including the numbered invariant list, and specifically the line near 106 stating that string length counts sixteen-bit units
    - `src/mcp/vice/anno-store-export.ts`, `src/mcp/vice/anno-derive.ts`, `src/mcp/vice/anno-confidence.ts` — the comment blocks containing citations in each
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Section C, which records both technical-token collision sites, and Sections A, B, D and E
  </read_first>
  <action>
Sweep the four remaining files to zero under the same rules as Task 1.

`anno-types.ts` holds the store's numbered invariants as prose. Each is cited
from elsewhere in the family and each is the only statement of its rule. Rewriting
a citation attached to one of them almost always means deleting a parenthetical
and leaving the invariant exactly as written. Do not renumber the invariants and
do not merge them.

**The technical-token collision.** This file carries one of the phase's two
sites where a standard encoding name has the same shape as a requirement id. It
is not a citation. Rewrite the wording to say "16-bit code units", which is both
accurate and the standard phrasing for what a JavaScript string length counts.
Do NOT add an exemption mechanism of any kind: of the id prefixes appearing in
the shipped tree this is the only one that is not a declared project prefix, so
an exemption would carry a single entry, and a one-entry escape hatch has already
been withdrawn from this guard once after it whitelisted five unrelated citations
in the same file as collateral. The other collision site lives in the protocol
family and is handled by the plan that owns it.

Delete each file's `RATCHET` entry once it reports zero, then run the full
automated gate.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test skills-planning-vocabulary.test.ts anno-types.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -ac 'code units' anno-types.ts)" -ge "1"</automated>
    <fails_when>the printed count is 0 — the wording was deleted rather than rewritten, so the invariant about how comment length is measured no longer states what it measures</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -ac 'anno-store.ts\|anno-types.ts\|anno-store-export.ts\|anno-derive.ts\|anno-confidence.ts' skills-planning-vocabulary.test.ts)" -le "5"</automated>
    <fails_when>the printed count is above 5 — at least one ratchet entry for this plan's files was not deleted (at most the five frozen budget-baseline rows may remain)</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated</automated>
    <fails_when>non-zero exit, or the summary line "ℹ fail 0" is absent from the output</fails_when>
  </verify>
  <acceptance_criteria>
    - All five of this plan's files scan clean and none has a `RATCHET` entry
    - `anno-types.ts` says "16-bit code units" where it previously named the encoding, and the surrounding invariant is otherwise unchanged
    - No exemption list, allowlist or skip mechanism was added to `skills-planning-vocabulary.test.ts` by this plan: `git diff src/mcp/vice/skills-planning-vocabulary.test.ts` shows only ratchet-entry deletions
    - The numbered invariants in `anno-types.ts` are the same count and the same numbering as before the sweep
    - Every tier-4 site across the four files is named in the summary with its line number
    - `npm run test:automated` exits 0 and reports `fail 0`
  </acceptance_criteria>
  <done>Five ledger entries are gone, the token collision is resolved by plain wording rather than an escape hatch, and the whole automated gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repository → the npm tarball | These comments reach a consumer with no planning tree |
| `anno-types.ts`'s invariant prose → every consumer of the store | The type system's rules are half declaration and half comment; losing the comment half silently weakens the contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-02 | Tampering | the store's invariant prose and its two irreplaceable explanations | high | mitigate | The comment-byte budget reds on net loss; this plan additionally requires two named comments quoted before and after and the invariant count and numbering preserved |
| T-51-03 | Tampering | store runtime behaviour and refusal messages | high | mitigate | Edits confined to comment spans and to output strings whose pinning tests are updated in the same commit; `anno-store.test.ts` and `anno-types.test.ts` run in the tasks' own verify |
| T-51-11 | Elevation of Privilege | the guard's exemption surface | high | mitigate | The token collision is rewritten, never exempted; an acceptance criterion asserts the guard diff contains only ratchet-entry deletions |
| T-51-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan and none is added |
</threat_model>

<verification>
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck` exits 0
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated` exits 0 and reports `fail 0`
- Five files at zero, five ratchet entries deleted, no exemption mechanism added
</verification>

<success_criteria>
- The store's invariants read the same or better and are numbered the same
- The technical-token collision cost one wording change and zero machinery
- Every unrecoverable site is named rather than silently weakened
</success_criteria>

<output>
Create `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-07-SUMMARY.md` when done.

Record the two named comments before and after, the rewritten collision wording,
comment characters before and after per file, and every tier-4 site by line
number.
</output>
