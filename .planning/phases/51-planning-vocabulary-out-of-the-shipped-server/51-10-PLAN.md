---
phase: 51-planning-vocabulary-out-of-the-shipped-server
plan: 10
type: execute
wave: 9
depends_on: ["51-09"]
files_modified:
  - src/mcp/vice/stock-dispatch.ts
  - src/mcp/vice/stock-protocol.ts
  - src/mcp/vice/stock-derived.ts
  - src/mcp/vice/skills-planning-vocabulary.test.ts
autonomous: true
requirements: [VOCAB-01, VOCAB-02, VOCAB-04, VOCAB-06]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The dispatch seam, the wire protocol and the derived-answer module scan clean and their ratchet entries are gone (VOCAB-01)."
    - "Every wire-format fact — header sizes, byte order, the unsolicited message types and the one with a zero-length body — reads identically after the sweep (VOCAB-02)."
    - "The second technical-token collision, in the protocol module, is rewritten to plain wording rather than exempted (VOCAB-01)."
  artifacts:
    - src/mcp/vice/stock-protocol.ts
    - src/mcp/vice/stock-dispatch.ts
  key_links:
    - "The protocol module's comments -> every transport consumer: they are this project's only written specification of a wire format whose upstream documentation is wrong in at least three places."
---

<objective>
Sweep the transport family's three densest modules — about 262 citations across
`stock-dispatch.ts`, `stock-protocol.ts` and `stock-derived.ts` — to zero.

Purpose: `stock-protocol.ts` carries this project's only written specification of
the emulator's binary monitor, including several places where upstream behaviour
differs from upstream documentation. Those comments are load-bearing facts, not
narration, and the citations attached to them are almost all bare parentheticals
that can simply go.

It also carries the heaviest concentration of evidence-document path citations in
the phase — roughly eighteen of them — and the second of the phase's two
technical-token collisions.

Output: three files at zero, three ratchet entries deleted.

Counts are sizing context measured before planning; the ledger is the authority.

Locked decisions implemented by this plan:

- D-06: sweep plans partition by module family, not by file and not by citation category.
- D-07: recovery is tiered by identifier class, so lookup effort is spent where the ambiguity actually is.
- D-08: where nothing recovers a reason, the comment is rewritten to a weaker but true statement and the site is named in the plan's summary, never deleted.
- D-10: the technical-token collision is rewritten to plain wording and no exemption mechanism is added.

Those identifiers belong to this planning document. No replacement string this
plan writes into product source may contain one.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-01-SUMMARY.md
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md
@.planning/ENGINEERING_RULES.md
</context>

## Artifacts this phase produces

See `51-01-PLAN.md`. This plan creates no new symbol.

<tasks>

<task type="auto">
  <name>Task 1: Sweep stock-protocol.ts to zero without touching one wire fact</name>
  <files>src/mcp/vice/stock-protocol.ts, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/stock-protocol.ts` — the whole header, then every comment block containing a citation. Read the blocks describing the request and response header layout, the byte order, the unsolicited message types, and the one whose body is zero-length
    - `src/mcp/vice/stock-protocol.test.ts` — so you know which framing behaviours are pinned
    - `.planning/ENGINEERING_RULES.md` section 21.2 — the worked bad/good rewrite pair
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Section C for the technical-token collision, and Sections A, B, D and E
  </read_first>
  <action>
Rewrite every citation in `stock-protocol.ts` into the reason it stood for,
splitting comment sites from string-literal sites first and recording the split.

Apply the tier ladder: delete a bare parenthetical whose sentence already states
the reason; turn an inline citation into a clause that states it; take a sparse
line's reason from the block above it or from the phase artifacts the resolution
document names; and where nothing recovers a reason, rewrite to a weaker but TRUE
statement and NAME the site in the summary with its line number. Never delete a
comment.

This file's comments are a specification. Every number in them — a header size, a
field offset, an opcode, an error code, a message type, a body length — is a
measured fact about the emulator and several of them contradict the upstream
documentation. Do not paraphrase a number. Do not merge two facts into one
sentence. Where a citation is attached to a wire fact, delete the citation and
leave the fact exactly as written. The intended diff for most of this file is
subtraction of parentheticals and nothing else.

This file carries the heaviest concentration of evidence-document path citations
in the phase. Each is rewritten into the reason it stood for like any other
citation, and never repointed at the planning tree.

**The second technical-token collision.** One comment here names a text encoding
whose written form has the same shape as a requirement id, in a sentence about
not feeding the wrong kind of unit to a byte-oriented path. Rewrite the phrase to
"16-bit code units", which is accurate and standard, and change nothing else in
that sentence. Do NOT add an exemption mechanism — the first collision was
resolved the same way in an earlier plan and for the same reason.

Constraints: edit comment spans, and string literals only where the citation sits
in output; do not shorten a header explaining why the file exists; do not repoint
any citation at a different planning-tree path; every replacement string you write
must itself be free of planning-tree paths, phase and plan numbers, decision, gap
and requirement ids, and planning-document cross-references.

Lower this file's `RATCHET` count to the number the guard reports, read from its
failure message, and delete the entry at zero.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test skills-planning-vocabulary.test.ts stock-protocol.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -aE -c 'docs/phase[0-9]' stock-protocol.ts)" = "0"</automated>
    <fails_when>the printed count is not 0 — at least one of this file's evidence-document path citations survives</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test -z "$(git diff -- stock-protocol.ts | grep -a '^[-+]' | grep -avE '^(\+\+\+|---)' | grep -avE '^[-+][[:space:]]*(//|\*|/\*)')"</automated>
    <fails_when>non-zero exit — a non-comment line was changed in the module that defines the wire framing. Re-run the inner pipeline without the `test -z` wrapper to see which lines, and justify or revert each</fails_when>
  </verify>
  <acceptance_criteria>
    - `stock-protocol.ts` has no `RATCHET` entry and scans clean
    - `grep -ac '\.planning' src/mcp/vice/stock-protocol.ts` outputs `0`
    - The diff contains only comment lines, demonstrated by the fourth verify command
    - The file says "16-bit code units" where it previously named the encoding, and the rest of that sentence is unchanged
    - No number in any wire-format comment changed: the summary lists every comment block touched and confirms that no header size, field offset, opcode, error code, message type or body length was reworded
    - Every tier-4 site is named in the summary with its line number
  </acceptance_criteria>
  <done>The wire specification is at zero with every measured number intact and the token collision resolved by wording.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep stock-dispatch.ts and stock-derived.ts to zero</name>
  <files>src/mcp/vice/stock-dispatch.ts, src/mcp/vice/stock-derived.ts, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/stock-dispatch.ts` — the whole header, then every comment block containing a citation, especially those describing the module-level held-session state and why it is mutable and module-scoped
    - `src/mcp/vice/stock-derived.ts` — the comment blocks containing citations
    - `src/mcp/vice/stock-dispatch.test.ts` and `src/mcp/vice/stock-derived.test.ts` — so you know what is pinned
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Sections A, B, D and E
  </read_first>
  <action>
Sweep both files to zero under the same rules as Task 1.

`stock-dispatch.ts` is the dispatch seam and holds mutable module-level transport
state. The comments explaining why that state is module-scoped rather than
per-call, and what breaks if a second one appears, are the only record of that
decision and must survive at full length.

`stock-derived.ts` is short but dense — its citations are packed into a small
number of lines. Read each block before editing; a line with four citations on it
is usually one sentence that needs one edit, not four.

Delete each file's `RATCHET` entry once it reports zero, then run the full
automated gate.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test skills-planning-vocabulary.test.ts stock-dispatch.test.ts stock-derived.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -ac 'stock-dispatch.ts\|stock-protocol.ts\|stock-derived.ts' skills-planning-vocabulary.test.ts)" -le "3"</automated>
    <fails_when>the printed count is above 3 — at least one ratchet entry for this plan's files was not deleted (at most the three frozen budget-baseline rows may remain)</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated</automated>
    <fails_when>non-zero exit, or the summary line "ℹ fail 0" is absent from the output</fails_when>
  </verify>
  <acceptance_criteria>
    - All three of this plan's files scan clean and none has a `RATCHET` entry
    - `grep -ac '\.planning' src/mcp/vice/stock-dispatch.ts src/mcp/vice/stock-derived.ts` reports `0` for both
    - The module-level-state comment in `stock-dispatch.ts` is present and not shorter in substance; quote it before and after in the summary
    - Every tier-4 site across both files is named in the summary with its line number
    - `npm run test:automated` exits 0 and reports `fail 0`
  </acceptance_criteria>
  <done>Three ledger entries are gone and the whole automated gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repository → the npm tarball | These comments reach a consumer with no planning tree |
| this project's comments → the emulator's actual wire behaviour | These comments are the specification a maintainer trusts; upstream documentation is known wrong in several of the same places |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-02 | Tampering | the wire-format specification comments | high | mitigate | The comment-byte budget reds on net loss; this plan additionally forbids paraphrasing any number and requires the diff to contain only comment lines |
| T-51-03 | Tampering | transport runtime behaviour | high | mitigate | A comment-only-diff gate on the framing module, plus the owning test files and the full automated gate |
| T-51-11 | Elevation of Privilege | the guard's exemption surface | high | mitigate | The token collision is rewritten, never exempted |
| T-51-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan and none is added |
</threat_model>

<verification>
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck` exits 0
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated` exits 0 and reports `fail 0`
- Three files at zero, three ratchet entries deleted
- The framing module's diff contains comment lines only
</verification>

<success_criteria>
- No measured wire fact was paraphrased, merged or lost
- The second token collision cost one wording change and zero machinery
- Every unrecoverable site is named rather than silently weakened
</success_criteria>

<output>
Create `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-10-SUMMARY.md` when done.

Record the comment blocks touched in the framing module with a confirmation that
no number changed, the rewritten collision wording, comment characters before and
after per file, and every tier-4 site by line number.
</output>
