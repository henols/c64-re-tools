---
phase: 51-planning-vocabulary-out-of-the-shipped-server
plan: 09
type: execute
wave: 8
depends_on: ["51-08"]
files_modified:
  - src/mcp/vice/anno-tools.ts
  - src/mcp/vice/anno-enum-gen.ts
  - src/mcp/vice/anno-coverage.ts
  - src/mcp/vice/anno-bank.ts
  - src/mcp/vice/skills-planning-vocabulary.test.ts
autonomous: true
requirements: [VOCAB-01, VOCAB-02, VOCAB-04, VOCAB-06]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The annotation tool surface, the enum generator, the coverage gate and the bank helper all scan clean and their ratchet entries are gone (VOCAB-01)."
    - "The inverted allowlist protecting the tool surface from nested arguments is described in the same words after the sweep, and its curated list, batch assertion and depth limit are untouched (VOCAB-02)."
    - "The completeness gate's soundness asymmetry — observed execution proves code, never observing proves nothing — survives at full length (VOCAB-02)."
  artifacts:
    - src/mcp/vice/anno-tools.ts
    - src/mcp/vice/anno-coverage.ts
  key_links:
    - "`anno-tools.ts`'s allowlist comment -> the nested-argument hazard: the mechanism is tested, but only this comment says which direction the list runs and why."
---

<objective>
Sweep the annotation tool surface and three siblings — about 217 citations across
`anno-tools.ts`, `anno-enum-gen.ts`, `anno-coverage.ts` and `anno-bank.ts` — to
zero.

Purpose: `anno-tools.ts` holds a security-shaped comment. It describes an
allowlist that runs the opposite way from the deny list that used to sit beside
it, and a batch-depth limit. A reader who loses that explanation can easily
"simplify" the list back into a deny list.

Output: four files at zero, four ratchet entries deleted.

Counts are sizing context measured before planning; the ledger is the authority.

Locked decisions implemented by this plan:

- D-06: sweep plans partition by module family, not by file and not by citation category.
- D-07: recovery is tiered by identifier class, so lookup effort is spent where the ambiguity actually is.
- D-08: where nothing recovers a reason, the comment is rewritten to a weaker but true statement and the site is named in the plan's summary, never deleted.

Those identifiers belong to this planning document. No replacement string this
plan writes into product source may contain one.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-01-SUMMARY.md
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md
@.planning/ENGINEERING_RULES.md
</context>

## Artifacts this phase produces

See `51-01-PLAN.md`. This plan creates no new symbol.

<tasks>

<task type="auto">
  <name>Task 1: Sweep anno-tools.ts to zero with the allowlist's reasoning intact</name>
  <files>src/mcp/vice/anno-tools.ts, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/anno-tools.ts` — the whole header, then every comment block containing a citation, and the declarations of the curated tool list, the batch assertion and the batch depth limit with the comments attached to each
    - `src/mcp/vice/anno-tools.test.ts` — so you know what is pinned
    - `.planning/ENGINEERING_RULES.md` section 21.2 — the worked bad/good rewrite pair
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Sections A, B, D and E
  </read_first>
  <action>
Rewrite every citation in `anno-tools.ts` into the reason it stood for, splitting
comment sites from string-literal sites first and recording the split. Apply the
tier ladder: delete a bare parenthetical whose sentence already states the reason;
turn an inline citation into a clause that states it; take a sparse line's reason
from the block above it or from the phase artifacts the resolution document names;
and where nothing recovers a reason, rewrite to a weaker but TRUE statement and
NAME the site in the summary with its line number. Never delete a comment.

Three declarations in this file carry the only prose that explains a defence:
the curated tool list, the batch-shape assertion, and the maximum batch depth.
Their comments must say, after the sweep as before it, that the list is an
ALLOWLIST rather than a deny list, why that direction was chosen, and what the
depth limit is protecting against. If a citation is attached to any of them, the
correct edit is to delete the citation and leave the sentence. Do not change any
of the three declarations themselves — not their values, not their names, not
their types.

Constraints: edit comment spans, and string literals only where the citation sits
in output; do not shorten a header explaining why the file exists; do not repoint
any citation at a different planning-tree path; every replacement string you write
must itself be free of planning-tree paths, phase and plan numbers, decision, gap
and requirement ids, and planning-document cross-references.

Lower this file's `RATCHET` count to the number the guard reports, read from its
failure message, and delete the entry at zero.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test skills-planning-vocabulary.test.ts anno-tools.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test -z "$(git diff -- anno-tools.ts | grep -a '^[-+]' | grep -avE '^(\+\+\+|---)' | grep -avE '^[-+][[:space:]]*(//|\*|/\*)')"</automated>
    <fails_when>non-zero exit — a non-comment line was changed in a file whose declarations this plan forbids touching. Re-run the inner pipeline without the `test -z` wrapper to see which lines, and inspect each before proceeding</fails_when>
  </verify>
  <acceptance_criteria>
    - `anno-tools.ts` has no `RATCHET` entry and scans clean
    - `grep -ac '\.planning' src/mcp/vice/anno-tools.ts` outputs `0`
    - The curated tool list, the batch assertion and the batch depth limit are unchanged in name, value and type
    - The comment on each of the three still states that the list is an allowlist, why, and what the depth limit protects against; quote all three before and after in the summary
    - The diff contains only comment lines, demonstrated by the third verify command producing no output
    - Every tier-4 site is named in the summary with its line number
  </acceptance_criteria>
  <done>The tool surface is at zero, its three defences are untouched, and their explanations are intact.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep anno-enum-gen.ts, anno-coverage.ts and anno-bank.ts to zero</name>
  <files>src/mcp/vice/anno-enum-gen.ts, src/mcp/vice/anno-coverage.ts, src/mcp/vice/anno-bank.ts, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/anno-coverage.ts` — the whole header, then every comment block containing a citation, especially those describing the completeness gate's required inputs
    - `src/mcp/vice/anno-enum-gen.ts`, `src/mcp/vice/anno-bank.ts` — the comment blocks containing citations in each
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Sections A, B, D and E
  </read_first>
  <action>
Sweep the three files to zero under the same rules as Task 1.

`anno-coverage.ts` implements the completeness gate. Two of its comments are the
only statement of a rule that is easy to get backwards: the gate takes the
evidence-disagreement query as a REQUIRED input rather than an optional
cross-check, and observed execution proves a range is code while never having
observed a range proves nothing about it. Both must survive at full length.

`anno-enum-gen.ts` generates named constants for hardware registers. Comments
there that state a hardware fact are not citations; leave them exactly as they
are even when a citation sits beside one.

Delete each file's `RATCHET` entry once it reports zero, then run the full
automated gate.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test skills-planning-vocabulary.test.ts anno-coverage.test.ts anno-enum-gen.test.ts anno-bank.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -ac 'anno-tools.ts\|anno-enum-gen.ts\|anno-coverage.ts\|anno-bank.ts' skills-planning-vocabulary.test.ts)" -le "4"</automated>
    <fails_when>the printed count is above 4 — at least one ratchet entry for this plan's files was not deleted (at most the four frozen budget-baseline rows may remain)</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated</automated>
    <fails_when>non-zero exit, or the summary line "ℹ fail 0" is absent from the output</fails_when>
  </verify>
  <acceptance_criteria>
    - All four of this plan's files scan clean and none has a `RATCHET` entry
    - `grep -ac '\.planning' src/mcp/vice/anno-enum-gen.ts src/mcp/vice/anno-coverage.ts src/mcp/vice/anno-bank.ts` reports `0` for every file
    - The required-input comment and the soundness-asymmetry comment in `anno-coverage.ts` are present and not shorter in substance; quote both before and after in the summary
    - No hardware-fact comment in `anno-enum-gen.ts` was changed
    - Every tier-4 site across the three files is named in the summary with its line number
    - `npm run test:automated` exits 0 and reports `fail 0`
  </acceptance_criteria>
  <done>Four ledger entries are gone, the completeness gate's two easily-inverted rules are still stated, and the whole automated gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repository → the npm tarball | These comments reach a consumer with no planning tree |
| an incoming tool call → the annotation tool surface | The curated allowlist and the batch-depth limit are the defence against a nested-argument hazard |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-02 | Tampering | the allowlist and completeness-gate explanations | high | mitigate | The comment-byte budget reds on net loss; this plan additionally requires five named comments quoted before and after |
| T-51-13 | Elevation of Privilege | the curated allowlist, batch assertion and depth limit | high | mitigate | A verify command prints every non-comment diff line in `anno-tools.ts` and any output fails the task, so a declaration cannot be changed under cover of a comment sweep |
| T-51-03 | Tampering | annotation runtime behaviour | high | mitigate | Edits confined to comment spans; the four owning test files plus the full automated gate run before the plan closes |
| T-51-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan and none is added |
</threat_model>

<verification>
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck` exits 0
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated` exits 0 and reports `fail 0`
- Four files at zero, four ratchet entries deleted
- The `anno-tools.ts` diff contains comment lines only
</verification>

<success_criteria>
- The tool surface's defences are unchanged and still explained
- The completeness gate's two invertible rules are still stated in full
- Every unrecoverable site is named rather than silently weakened
</success_criteria>

<output>
Create `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-09-SUMMARY.md` when done.

Record the five named comments before and after, comment characters before and
after per file, the comment-only-diff verification result, and every tier-4 site
by line number.
</output>
