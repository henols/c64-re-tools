---
phase: 51-planning-vocabulary-out-of-the-shipped-server
plan: 04
type: execute
wave: 3
depends_on: ["51-03"]
files_modified:
  - src/mcp/vice/broker-launch.mts
  - src/mcp/vice/broker-control.mts
  - src/mcp/vice/broker-epoch.mts
  - src/mcp/vice/resources/broker-launch.mjs
  - src/mcp/vice/resources/broker-control.mjs
  - src/mcp/vice/resources/broker-epoch.mjs
  - src/mcp/vice/skills-planning-vocabulary.test.ts
autonomous: true
requirements: [VOCAB-01, VOCAB-02, VOCAB-04, VOCAB-06]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`broker-launch.mts`, `broker-control.mts` and `broker-epoch.mts` scan clean and their ratchet entries are gone (VOCAB-01)."
    - "The three generated artifacts they compile into are regenerated, byte-identical to a fresh build, and scan clean (VOCAB-01)."
    - "No comment explaining the single-owner launch guard or the control-plane protocol is shorter in substance than before (VOCAB-02)."
  artifacts:
    - src/mcp/vice/broker-launch.mts
    - src/mcp/vice/broker-control.mts
    - src/mcp/vice/broker-epoch.mts
  key_links:
    - "The launch guard's WHY comment -> a future maintainer: it is the only durable record of the outage that produced the synchronous check-and-set, and that rule is regression-tested but not self-explaining."
---

<objective>
Sweep the broker's launch and control-plane sources — about 265 citations across
`broker-launch.mts`, `broker-control.mts` and `broker-epoch.mts` — to zero, and
regenerate the three artifacts they compile into.

Purpose: this family carries the project's most load-bearing WHY comments. The
single-owner launch guard exists because a triple-launch outage killed a live
capture, and that reason lives nowhere except in this source. A sweep that turns
it into a shorter sentence would do the opposite of what this phase is for.

Output: three sources and three generated artifacts at zero, six ratchet entries
deleted.

Counts are sizing context measured before planning. The ledger frozen in plan
51-01 and lowered by plan 51-03 is the authority; the guard's failure message
prints any correction.

Locked decisions implemented by this plan:

- D-06: sweep plans partition by module family, not by file and not by citation category.
- D-07: recovery is tiered by identifier class, so lookup effort is spent where the ambiguity actually is.
- D-08: where nothing recovers a reason, the comment is rewritten to a weaker but true statement and the site is named in the plan's summary, never deleted.

Those identifiers belong to this planning document. No replacement string this
plan writes into product source may contain one.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-01-SUMMARY.md
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-03-SUMMARY.md
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md
@.planning/ENGINEERING_RULES.md
</context>

## Artifacts this phase produces

See `51-01-PLAN.md`. This plan creates no new symbol; it edits comment text and
deletes ledger entries.

<tasks>

<task type="auto">
  <name>Task 1: Sweep broker-launch.mts to zero</name>
  <files>src/mcp/vice/broker-launch.mts, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/broker-launch.mts` — the whole header, then every comment block containing a citation. Read the block, not the line
    - `.planning/ENGINEERING_RULES.md` section 21.2 — the worked bad/good rewrite pair
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Section A for this file's originating phases, Section B for the tier ladder, Section D for plan citations, Section E for genuinely dangling tokens
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-03-SUMMARY.md` — the final slack value and the rewrite patterns that worked on the densest file
  </read_first>
  <action>
Rewrite every citation in `broker-launch.mts` into the reason it stood for.

Classify each site against the tier ladder and act: a bare parenthetical beside
prose that already states the reason is deleted and nothing else changes; an
inline citation with prose on the line becomes a clause that states the reason;
a sparse line takes its reason from the block above it or from the phase
artifacts the resolution document names; and a site nothing recovers is rewritten
to a weaker but TRUE statement and NAMED in the summary with its line number,
never deleted.

Three comment families in this file need particular care, because each is the
only record of its fact:

  - the single-owner launch guard, and why the check-and-set must stay
    synchronous with nothing awaited between the check and the set;
  - why the emulator flag order matters, and what happens when it is wrong;
  - the warm-floor and instance-promotion behaviour, which several citations in
    this file point at a plan for. Those plan citations RESOLVE — the resolution
    document names the plan and summary documents and their prose can be
    paraphrased directly into the comment. Do not treat them as unrecoverable.

Constraints: edit comment spans, not code. Do not shorten a header that explains
why the file exists. Do not repoint any citation at a different planning-tree
path. Every replacement string you write must itself be free of planning-tree
paths, phase and plan numbers, decision, gap and requirement ids, and
planning-document cross-references — the guard checks what you write, not only
what you removed.

Lower this file's `RATCHET` count to the number the guard reports, read from the
guard's own failure message rather than computed by hand, and delete the entry
when it reaches zero. Leave `COMMENT_BUDGET_BASELINE` alone.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test skills-planning-vocabulary.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -aE -c 'docs/phase[0-9]' broker-launch.mts)" = "0"</automated>
    <fails_when>the printed count is not 0 — an evidence-document path citation survives in this file</fails_when>
  </verify>
  <acceptance_criteria>
    - `broker-launch.mts` has no `RATCHET` entry and scans clean
    - `git diff --numstat src/mcp/vice/broker-launch.mts` shows insertions as well as deletions; record both numbers in the summary
    - `grep -ac '\.planning' src/mcp/vice/broker-launch.mts` outputs `0`
    - The comment describing the single-owner launch guard is present and is not shorter in substance; quote the before and after text in the summary
    - Every tier-4 site is named in the summary with its line number
    - The guard is green, which means this file's comment-byte budget is green
  </acceptance_criteria>
  <done>`broker-launch.mts` is at zero, its three load-bearing comment families are intact or improved, and the guard and typecheck are green.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep broker-control.mts and broker-epoch.mts, then regenerate all three artifacts</name>
  <files>src/mcp/vice/broker-control.mts, src/mcp/vice/broker-epoch.mts, src/mcp/vice/resources/broker-launch.mjs, src/mcp/vice/resources/broker-control.mjs, src/mcp/vice/resources/broker-epoch.mjs, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/broker-control.mts` — the whole header, then every comment block containing a citation
    - `src/mcp/vice/broker-epoch.mts` — the whole file; it is short
    - `src/mcp/vice/build.ts` — `build()` and `HOST_BOUND_ARTIFACTS`
    - `src/mcp/vice/resources-sync.test.ts` — its assertion, so it is clear why the generated files are never hand-edited
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Sections A, B, D and E
  </read_first>
  <action>
Sweep `broker-control.mts` and `broker-epoch.mts` to zero under the same rules as
Task 1. `broker-control.mts` carries the control-plane protocol's framing and
timeout reasoning; those comments are the protocol's only specification and must
survive at full length.

Then run the build so `resources/broker-launch.mjs`, `resources/broker-control.mjs`
and `resources/broker-epoch.mjs` are regenerated from the corrected sources, and
commit them. Never hand-edit a generated artifact — a byte-identity guard already
asserts that directory matches a fresh build.

Delete the `RATCHET` entries for all three sources and all three generated
artifacts once each reports zero.

Close the plan by running the full automated gate.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run build</automated>
    <fails_when>non-zero exit, or the output does not report that the artifacts were written</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test resources-sync.test.ts skills-planning-vocabulary.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -ac 'broker-launch\|broker-control\|broker-epoch' skills-planning-vocabulary.test.ts)" -le "3"</automated>
    <fails_when>the printed count is above 3 — a ratchet entry for one of the three sources or their generated artifacts was not deleted (at most the three frozen budget-baseline rows may remain)</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated</automated>
    <fails_when>non-zero exit, or the summary line "ℹ fail 0" is absent from the output</fails_when>
  </verify>
  <acceptance_criteria>
    - All three sources and all three generated artifacts scan clean and have no `RATCHET` entry
    - `npm run build` followed by `node --test resources-sync.test.ts` is green, proving regeneration rather than hand editing
    - `git diff --numstat` shows changes to all three files under `resources/`
    - The control-plane framing and timeout comments in `broker-control.mts` are present and not shorter in substance
    - Every tier-4 site in both files is named in the summary with its line number
    - `npm run test:automated` exits 0 and reports `fail 0`
  </acceptance_criteria>
  <done>Six ledger entries are gone, the generated artifacts match a fresh build, and the whole automated gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repository → the npm tarball | These comments reach a consumer with no planning tree |
| `.mts` source → generated `.mjs` artifact | A hand edit on the generated side diverges silently from the source a maintainer opens |
| the broker's launch path → a live emulator session | A weakened comment here is how a future change reintroduces a launch race that destroys a capture |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-02 | Tampering | the launch-guard and control-plane WHY comments | high | mitigate | The comment-byte budget reds on net loss beyond citation characters removed; this plan additionally requires the launch-guard comment to be quoted before and after in the summary |
| T-51-03 | Tampering | broker runtime behaviour | high | mitigate | Edits confined to comment spans; typecheck and the full automated gate run before the plan closes |
| T-51-09 | Tampering | the three generated artifacts | medium | mitigate | Regenerated by the build; the byte-identity guard asserts they match a fresh build |
| T-51-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan and none is added |
</threat_model>

<verification>
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck` exits 0
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated` exits 0 and reports `fail 0`
- Three sources and three generated artifacts at zero, six ratchet entries deleted
</verification>

<success_criteria>
- The broker's load-bearing explanations read better after the sweep than before it
- No generated artifact was hand-edited
- Every unrecoverable site is named rather than silently weakened
</success_criteria>

<output>
Create `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-04-SUMMARY.md` when done.

Record the launch-guard comment before and after, comment characters before and
after per file, and every tier-4 site by line number.
</output>
