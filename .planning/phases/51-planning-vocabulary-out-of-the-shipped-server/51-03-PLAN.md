---
phase: 51-planning-vocabulary-out-of-the-shipped-server
plan: 03
type: execute
wave: 2
depends_on: ["51-01", "51-02"]
files_modified:
  - src/mcp/vice/host-tool.mts
  - src/mcp/vice/resources/host-tool.mjs
  - src/mcp/vice/skills-planning-vocabulary.test.ts
autonomous: true
requirements: [VOCAB-01, VOCAB-02, VOCAB-04, VOCAB-06]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`host-tool.mts` scans clean under the guard's own predicate and its ratchet entry is gone (VOCAB-01)."
    - "`resources/host-tool.mjs` is byte-identical to a fresh build and scans clean, so the generated image carries no citation either (VOCAB-01)."
    - "The file's comment character total did not fall by more than the citation characters removed, plus slack (VOCAB-02)."
    - "`COMMENT_BUDGET_SLACK` has its FINAL value, derived from this file's real diffs, and is frozen for the remaining plans."
  artifacts:
    - src/mcp/vice/host-tool.mts
    - src/mcp/vice/resources/host-tool.mjs
  key_links:
    - "`host-tool.mts` -> `build.ts` -> `resources/host-tool.mjs`: the generated image must be regenerated in the same commit or the byte-identity guard reds."
    - "This file's real diffs -> `COMMENT_BUDGET_SLACK`: the densest file in the phase is the only honest calibration corpus."
---

<objective>
Take `src/mcp/vice/host-tool.mts` — the single densest file in this phase, about
322 citations on about 170 comment lines across 3103 lines — from its pinned
count to zero, regenerate the artifact it compiles into, and finalise the
comment-byte budget's slack term against its real diffs.

Purpose: this is the first expansion slice off the tracer and the phase's
calibration corpus. It produces more real rewrite diffs than any other single
file, which is why the slack term is settled here rather than guessed earlier or
drifted later.

Output: one file at zero, its generated image regenerated and clean, its ratchet
entry deleted, and a frozen slack value the remaining twelve plans assert against.

The counts above are sizing context measured before planning. Do not trust them:
the ledger frozen in plan 51-01 is the authority, and the guard's failure message
prints any correction.

Locked decisions implemented by this plan:

- D-06: sweep plans partition by module family, not by file and not by citation category.
- D-07: recovery is tiered by identifier class, so lookup effort is spent where the ambiguity actually is.
- D-08: where nothing recovers a reason, the comment is rewritten to a weaker but true statement and the site is named in the plan's summary, never deleted.
- D-09: a per-file comment-character budget is the mechanical backing for the second success criterion.

Those identifiers belong to this planning document. No replacement string this
plan writes into product source may contain one.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-01-SUMMARY.md
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md
@.planning/ENGINEERING_RULES.md
</context>

## Artifacts this phase produces

See `51-01-PLAN.md` for the full list of symbols this phase creates. This plan
creates no new symbol; it edits comment text and deletes ledger entries.

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the first half of host-tool.mts's citations into the reasons they stood for</name>
  <files>src/mcp/vice/host-tool.mts, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/host-tool.mts` — the file header in full, then each comment block containing a citation. Read the block, not the line: about 43% of sites across this phase are a bare parenthetical beside prose that already states the reason, and you cannot tell which without the block
    - `.planning/ENGINEERING_RULES.md` section 21.2 — the worked bad/good rewrite pair, which is the standard for every edit in this plan
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Section A for this file's originating phases, Section B for the tier ladder, Section D for the plan-citation resolutions, Section E for the genuinely-dangling tokens
    - `src/mcp/vice/skills-planning-vocabulary.test.ts` — the `RATCHET` and `COMMENT_BUDGET_BASELINE` entries for this file, and the guard's failure-message format
  </read_first>
  <action>
Work the first half of this file's citation sites, top of file downward, to a
natural boundary you record in the summary.

For each site, classify it against the tier ladder in Section B of the resolution
document and act:

  - The citation is a bare parenthetical and the sentence around it already states
    the reason: delete the parenthetical and stop. This is the largest class.
    Leave the sentence exactly as it is.
  - The citation is inline and prose is still on the line: rewrite the clause so
    the sentence carries the reason instead of the pointer. The result must read
    as an explanation to someone who has only this package.
  - The line is sparse and the comment BLOCK above it carries the reason: fold the
    reason into the line, or delete the pointer if the block already says it two
    lines up.
  - Nothing on the line, nothing in the block, and the resolution document
    resolves the token: read the named phase artifacts and write what you find.
  - Nothing recoverable anywhere: rewrite the comment to a weaker but TRUE
    statement and NAME THE SITE in this plan's summary, with its line number and
    what was lost. Never delete the comment.

This file is the one reached by the host-tool seam and it explains, repeatedly,
why a container-side script cannot spawn a host binary directly. Those
explanations are the most valuable comments in the package and the ones most
likely to be shortened away by a fast sweep. Keep them. Where a citation is
attached to one, the correct edit is usually to delete four characters.

Hard constraints on what you may edit:

  - Edit comment spans. Do not edit code. If a citation sits inside a string
    literal that reaches a user at runtime, rewrite the string to state the rule
    rather than the reference, and update any test that pins that message in the
    same commit — name both in the summary.
  - Do not shorten a header comment that explains why the file exists. That is
    house style and section 21.2 requires it to survive. The comment-byte budget
    added in plan 51-01 reds if it does not.
  - Do not repoint any citation at a different planning-tree path. That is the
    same defect one hop along and fails this phase's fourth success criterion.
  - Do not introduce any new citation. Every replacement string you write must
    itself be free of planning-tree paths, phase and plan numbers, decision, gap
    and requirement ids, and planning-document cross-references. The guard checks
    what you write, not only what you removed.

When the half is done, lower this file's `RATCHET` count to the number the guard
reports. Do not compute it by hand and do not guess: run the guard, read the
corrected entry line out of its failure message, and paste it. Leave the
`COMMENT_BUDGET_BASELINE` entry untouched — it is frozen for the phase.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test skills-planning-vocabulary.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `host-tool.mts`'s `RATCHET` count is strictly lower than the value plan 51-01 froze, and equals the live count the guard reports
    - The guard is green, which means the comment-byte budget for this file is also green
    - `git diff --numstat src/mcp/vice/host-tool.mts` shows deletions and insertions, not deletions alone; record both numbers in the summary
    - No new citation token was introduced: `grep -ac '\.planning' src/mcp/vice/host-tool.mts` outputs `0`
    - Every tier-4 site reached in this half is named in the summary with its line number
    - The file's own header explaining why it exists is still present and is not shorter in substance
  </acceptance_criteria>
  <done>The first half of the file's citations are reasons rather than pointers, the ratchet count is lowered to the measured value, and the guard and typecheck are green.</done>
</task>

<task type="auto">
  <name>Task 2: Finish host-tool.mts to zero and regenerate its compiled image</name>
  <files>src/mcp/vice/host-tool.mts, src/mcp/vice/resources/host-tool.mjs, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/host-tool.mts` — the remaining comment blocks carrying citations, as left by Task 1
    - `src/mcp/vice/build.ts` — the `build()` entry point and `HOST_BOUND_ARTIFACTS`, to confirm which artifact this source produces
    - `src/mcp/vice/resources-sync.test.ts` — its header and its assertion, so you know exactly what byte-identity it enforces and why a hand edit to the generated file is never the answer
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — as in Task 1
  </read_first>
  <action>
Work the remaining citation sites in `host-tool.mts` to zero under the same rules
as Task 1, including the evidence-document path citations this file carries —
those are rewritten into the reason exactly like any other, never repointed at
the planning tree.

Then run the build so `resources/host-tool.mjs` is regenerated from the corrected
source, and commit the regenerated file. Never hand-edit the generated artifact:
a byte-identity guard already asserts that directory matches a fresh build, so a
hand edit is caught and pointless, and the generated image is exactly why the
scan surface includes the source in the first place — a maintainer opening a
failure wants the file they can edit, not its compiled image.

Delete `host-tool.mts`'s `RATCHET` entry and `resources/host-tool.mjs`'s
`RATCHET` entry once both report zero. Leave both files' `COMMENT_BUDGET_BASELINE`
rows in place if present; the generated artifact should have none, because the
build rewrites its comments wholesale and a byte delta there would measure the
compiler rather than an author.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run build</automated>
    <fails_when>non-zero exit, or the output does not report that the artifacts were written</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test resources-sync.test.ts skills-planning-vocabulary.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -ac 'host-tool.mts' skills-planning-vocabulary.test.ts)" = "1"</automated>
    <fails_when>the printed count is not 1 — either the ratchet entry survives (2 occurrences, ratchet plus budget baseline) or the budget baseline row was deleted too (0)</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -ac 'resources/host-tool.mjs' skills-planning-vocabulary.test.ts)" = "0"</automated>
    <fails_when>the printed count is not 0 — the generated artifact's ratchet entry was not deleted after it reached zero</fails_when>
  </verify>
  <acceptance_criteria>
    - `host-tool.mts` and `resources/host-tool.mjs` both scan clean; neither has a `RATCHET` entry
    - `npm run build` followed by `node --test resources-sync.test.ts` is green, proving the generated artifact was regenerated rather than hand-edited
    - `git diff --numstat src/mcp/vice/resources/host-tool.mjs` shows changes; the file was not left stale
    - `grep -aE -c 'docs/phase[0-9]' src/mcp/vice/host-tool.mts` outputs `0`
    - Every tier-4 site in the second half is named in the summary with its line number
  </acceptance_criteria>
  <done>Both the source and its compiled image are at zero, both ratchet entries are deleted, and the byte-identity guard is green.</done>
</task>

<task type="auto">
  <name>Task 3: Finalise COMMENT_BUDGET_SLACK against this file's real diffs</name>
  <files>src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/skills-planning-vocabulary.test.ts` — `COMMENT_BUDGET_SLACK` with its provisional value and the comment saying so, and the budget assertion that consumes it
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-01-SUMMARY.md` — the provisional value and the measured delta it came from
    - `src/mcp/vice/shipped-modules.ts` — `commentByteTotal()`, so you measure with the same function the assertion uses
  </read_first>
  <action>
Settle the slack term. It exists to absorb reformatting noise — re-wrapping a
comment block changes raw character count without losing or gaining any reason —
and nothing else. It must not be large enough to hide a gutted header.

Measure, using the same function the assertion uses: for `host-tool.mts`, the
comment characters lost between the frozen baseline and the file as it stands
now, and the citation characters removed over the same interval. The difference
between those two numbers, across the largest real diff set this phase will
produce, is the honest floor for slack. Choose a value at or modestly above the
largest positive difference you observe, state the observed number and the margin
in the code comment, and replace the word "provisional" with a statement that the
value is final and what corpus fixed it.

Sanity-check it in both directions before committing, as a scratch experiment you
revert:

  - Delete a genuine explanatory paragraph from a comment block in this file and
    confirm the budget assertion reds. If it does not, the slack is too large.
  - Restore it and confirm green.

Record both observed outcomes in the summary. Do not commit either experiment.

Then run the full automated gate. This is the first plan after the tracer, so it
is also the first proof that the widened guard survives a real sweep alongside
every other guard in the tree.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated</automated>
    <fails_when>non-zero exit, or the summary line "ℹ fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -ac 'COMMENT_BUDGET_SLACK' skills-planning-vocabulary.test.ts)" -ge "2"</automated>
    <fails_when>the printed count is below 2 — the constant is declared but never consumed by an assertion, or was removed</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -aic 'provisional' skills-planning-vocabulary.test.ts)" = "0"</automated>
    <fails_when>the printed count is not 0 — the slack constant is still marked provisional, so a later plan will not know the value is settled</fails_when>
  </verify>
  <acceptance_criteria>
    - `COMMENT_BUDGET_SLACK` carries a final value with a comment naming the observed largest difference and the margin added
    - The word "provisional" no longer appears in the guard file
    - Deleting a real explanatory paragraph from a swept file reds the budget assertion; restoring it returns to green. Both outcomes are recorded in the summary with the exact numbers the failure message printed
    - `npm run test:automated` exits 0 and reports `fail 0`
    - The summary records this file's total comment characters before and after the sweep, and the citation characters removed
  </acceptance_criteria>
  <done>The slack term is final, derived from the phase's densest real diff set, proven to still red on a genuine deletion, and the whole automated gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repository → the npm tarball | Every comment in this source and in its compiled image is read verbatim by a consumer with no planning tree |
| `.mts` source → generated `.mjs` artifact | A hand edit to the generated side silently diverges from the source a maintainer opens |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-02 | Tampering | the host-tool seam's explanatory headers | high | mitigate | The per-file comment-byte budget reds on a net loss beyond the citation characters removed; Task 3 proves it still reds by deleting a real paragraph and observing the failure |
| T-51-03 | Tampering | runtime behaviour of the host-tool seam | high | mitigate | Edits are confined to comment spans; `npm run typecheck` and the full automated gate run before the plan closes; a citation inside a runtime string is rewritten with its pinning test updated in the same commit |
| T-51-09 | Tampering | `resources/host-tool.mjs` | medium | mitigate | The artifact is regenerated by the build and the byte-identity guard asserts it matches a fresh build; hand-editing it is explicitly forbidden by this plan |
| T-51-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan and none is added; there is no install task to gate |
</threat_model>

<verification>
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck` exits 0
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated` exits 0 and reports `fail 0`
- `host-tool.mts` and `resources/host-tool.mjs` both scan clean and have no ratchet entry
- The slack term is final and still reds on a genuine comment deletion
</verification>

<success_criteria>
- The densest file in the phase is at zero with its explanations intact and, where they were thin, improved
- The generated artifact was regenerated, never hand-edited
- Every unrecoverable site is named in the summary rather than silently weakened
</success_criteria>

<output>
Create `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-03-SUMMARY.md` when done.

Record: the final `COMMENT_BUDGET_SLACK` value and its derivation; comment
characters before and after for this file; every tier-4 site by line number; and
the two slack sanity-check outcomes.
</output>
