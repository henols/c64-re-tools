---
phase: 51-planning-vocabulary-out-of-the-shipped-server
plan: 06
type: execute
wave: 5
depends_on: ["51-05"]
files_modified:
  - src/mcp/vice/anno-cli.ts
  - src/mcp/vice/anno-cli.test.ts
  - src/mcp/vice/anno-join.ts
  - src/mcp/vice/anno-symbols.ts
  - src/mcp/vice/anno-index.ts
  - src/mcp/vice/anno-details.ts
  - src/mcp/vice/skills-planning-vocabulary.test.ts
autonomous: true
requirements: [VOCAB-01, VOCAB-02, VOCAB-04, VOCAB-06]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The annotation-store command line and its join, symbol, index and detail helpers all scan clean and their ratchet entries are gone (VOCAB-01)."
    - "Every command-line help string and refusal message a user can see states its rule rather than an internal reference (VOCAB-02)."
  artifacts:
    - src/mcp/vice/anno-cli.ts
    - src/mcp/vice/anno-join.ts
  key_links:
    - "`anno-cli.ts`'s user-visible strings -> an operator's terminal: a citation there is not a comment, it is output, and rewriting it changes what a person reads."
---

<objective>
Sweep the annotation-store command line and four of its helpers — about 233
citations across `anno-cli.ts`, `anno-join.ts`, `anno-symbols.ts`,
`anno-index.ts` and `anno-details.ts` — to zero.

Purpose: `anno-cli.ts` is the densest file in the annotation family and the one
whose strings reach a person directly. It is the first place in this phase where
a citation may be sitting in output rather than in a comment.

Output: five files at zero, five ratchet entries deleted.

Counts are sizing context measured before planning; the ledger is the authority.

Locked decisions implemented by this plan:

- D-06: sweep plans partition by module family, not by file and not by citation category.
- D-07: recovery is tiered by identifier class, so lookup effort is spent where the ambiguity actually is.
- D-08: where nothing recovers a reason, the comment is rewritten to a weaker but true statement and the site is named in the plan's summary, never deleted.

Those identifiers belong to this planning document. No replacement string this
plan writes into product source may contain one.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-01-SUMMARY.md
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md
@.planning/ENGINEERING_RULES.md
</context>

## Artifacts this phase produces

See `51-01-PLAN.md`. This plan creates no new symbol.

<tasks>

<task type="auto">
  <name>Task 1: Sweep anno-cli.ts to zero, separating comments from user-visible output</name>
  <files>src/mcp/vice/anno-cli.ts, src/mcp/vice/anno-cli.test.ts, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/anno-cli.ts` — the whole header, then every comment block containing a citation, and every string literal containing one
    - `src/mcp/vice/anno-cli.test.ts` — so you know which help and refusal strings are pinned by a test before you change one
    - `.planning/ENGINEERING_RULES.md` section 21.2 — the worked bad/good rewrite pair
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Sections A, B, D and E
  </read_first>
  <action>
Before editing, split this file's citation sites into two buckets and record the
split in the summary: sites inside comment spans, and sites inside string
literals. The guard scans whole lines, so both count, but they are different
edits with different risks.

For COMMENT sites, apply the tier ladder: delete a bare parenthetical whose
sentence already states the reason; turn an inline citation into a clause stating
the reason; take a sparse line's reason from the block above it or from the phase
artifacts the resolution document names; and where nothing recovers a reason,
rewrite to a weaker but TRUE statement and NAME the site in the summary. Never
delete a comment.

For STRING sites, the string is output a person reads. Rewrite it so it states
the rule in plain words — what is refused and why, or what the option does —
rather than naming an internal reference that resolves nowhere for the reader.
Then update every test that pins the changed string, in the same commit, and name
each changed string and its pinning test in the summary. A refusal message that
gets shorter and vaguer is a regression even though the guard would accept it.

Constraints: do not shorten a header explaining why the file exists; do not
repoint any citation at a different planning-tree path; every replacement string
you write must itself be free of planning-tree paths, phase and plan numbers,
decision, gap and requirement ids, and planning-document cross-references.

Lower this file's `RATCHET` count to the number the guard reports, read from its
failure message, and delete the entry at zero.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test skills-planning-vocabulary.test.ts anno-cli.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -aE -c 'docs/phase[0-9]' anno-cli.ts)" = "0"</automated>
    <fails_when>the printed count is not 0 — an evidence-document path citation survives in this file</fails_when>
  </verify>
  <acceptance_criteria>
    - `anno-cli.ts` has no `RATCHET` entry and scans clean
    - `grep -ac '\.planning' src/mcp/vice/anno-cli.ts` outputs `0`
    - The summary states how many sites were in comments and how many in string literals
    - Every changed user-visible string is quoted before and after in the summary, with its pinning test named
    - `git diff --numstat src/mcp/vice/anno-cli.ts` shows insertions as well as deletions
    - Every tier-4 site is named in the summary with its line number
  </acceptance_criteria>
  <done>The command line is at zero, its user-visible output states rules rather than references, and every pinning test was updated in the same commit.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep anno-join.ts, anno-symbols.ts, anno-index.ts and anno-details.ts to zero</name>
  <files>src/mcp/vice/anno-join.ts, src/mcp/vice/anno-symbols.ts, src/mcp/vice/anno-index.ts, src/mcp/vice/anno-details.ts, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/anno-join.ts` — the whole header, then every comment block containing a citation
    - `src/mcp/vice/anno-symbols.ts`, `src/mcp/vice/anno-index.ts`, `src/mcp/vice/anno-details.ts` — the comment blocks containing citations in each
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Sections A, B, D and E
  </read_first>
  <action>
Sweep the four remaining files to zero under the same rules as Task 1, splitting
comment sites from string sites in each and recording the split.

`anno-join.ts` describes the query that reports agreement AND disagreement
between byte-derived facts and observed runtime facts, rather than overwriting
one with the other. That asymmetry — observed execution proves code, never
observing something proves nothing — is the reason the join is shaped the way it
is, and it must survive as a complete explanation rather than a pointer.

Delete each file's `RATCHET` entry once it reports zero, then run the full
automated gate.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test skills-planning-vocabulary.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -ac 'anno-cli.ts\|anno-join.ts\|anno-symbols.ts\|anno-index.ts\|anno-details.ts' skills-planning-vocabulary.test.ts)" -le "5"</automated>
    <fails_when>the printed count is above 5 — at least one ratchet entry for this plan's files was not deleted (at most the five frozen budget-baseline rows may remain)</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated</automated>
    <fails_when>non-zero exit, or the summary line "ℹ fail 0" is absent from the output</fails_when>
  </verify>
  <acceptance_criteria>
    - All five of this plan's files scan clean and none has a `RATCHET` entry
    - `grep -ac '\.planning' src/mcp/vice/anno-join.ts src/mcp/vice/anno-symbols.ts src/mcp/vice/anno-index.ts src/mcp/vice/anno-details.ts` reports `0` for every file
    - The soundness-asymmetry comment in `anno-join.ts` is present and not shorter in substance; quote it before and after in the summary
    - Every tier-4 site across the four files is named in the summary with its line number
    - `npm run test:automated` exits 0 and reports `fail 0`
  </acceptance_criteria>
  <done>Five ledger entries are gone and the whole automated gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repository → the npm tarball | These comments reach a consumer with no planning tree |
| shipped source → an operator's terminal | `anno-cli.ts`'s help and refusal strings are read by a person at run time, not by a maintainer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-02 | Tampering | the annotation family's explanatory headers | high | mitigate | The comment-byte budget reds on net loss beyond citation characters removed |
| T-51-03 | Tampering | user-visible command-line output | high | mitigate | String sites are separated from comment sites before any edit; every changed string is quoted before and after and its pinning test is updated in the same commit; `anno-cli.test.ts` runs in the task's own verify |
| T-51-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan and none is added |
</threat_model>

<verification>
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck` exits 0
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated` exits 0 and reports `fail 0`
- Five files at zero, five ratchet entries deleted
</verification>

<success_criteria>
- Comment sites and string sites were treated as the different edits they are
- No user-visible message became vaguer
- Every unrecoverable site is named rather than silently weakened
</success_criteria>

<output>
Create `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-06-SUMMARY.md` when done.

Record the comment/string split per file, every changed user-visible string
before and after with its pinning test, comment characters before and after per
file, and every tier-4 site by line number.
</output>
