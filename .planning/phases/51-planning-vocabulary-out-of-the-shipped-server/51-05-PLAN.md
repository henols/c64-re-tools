---
phase: 51-planning-vocabulary-out-of-the-shipped-server
plan: 05
type: execute
wave: 4
depends_on: ["51-04"]
files_modified:
  - src/mcp/vice/vice-broker.mts
  - src/mcp/vice/vice-broker-client.ts
  - src/mcp/vice/broker-state.mts
  - src/mcp/vice/broker-kill.mts
  - src/mcp/vice/resources/vice-broker.mjs
  - src/mcp/vice/resources/broker-state.mjs
  - src/mcp/vice/resources/broker-kill.mjs
  - src/mcp/vice/skills-planning-vocabulary.test.ts
autonomous: true
requirements: [VOCAB-01, VOCAB-02, VOCAB-04]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The broker daemon, its container-side client, its state store and its kill path all scan clean and their ratchet entries are gone (VOCAB-01)."
    - "The three generated artifacts are regenerated and byte-identical to a fresh build (VOCAB-01)."
    - "The comment recording that the connection itself is the lease — no separate heartbeat — survives at full length (VOCAB-02)."
  artifacts:
    - src/mcp/vice/vice-broker.mts
    - src/mcp/vice/vice-broker-client.ts
    - src/mcp/vice/broker-state.mts
    - src/mcp/vice/broker-kill.mts
  key_links:
    - "`vice-broker-client.ts` -> `vice-broker.mts`: the acquire/release/recycle contract is described from both sides and the two descriptions must stay consistent after the rewrite."
---

<objective>
Sweep the broker daemon, its container-side client, its state store and its kill
path — about 320 citations across four files — to zero, and regenerate the three
artifacts among them that the build produces.

Purpose: this family holds the lease model, the crash-supervision rules and the
incident-before-kill ordering. Each is explained in exactly one place.

Output: four sources and three generated artifacts at zero, seven ratchet entries
deleted.

Counts are sizing context measured before planning; the ledger is the authority
and the guard's failure message prints any correction.

Locked decisions implemented by this plan:

- D-06: sweep plans partition by module family, not by file and not by citation category.
- D-07: recovery is tiered by identifier class, so lookup effort is spent where the ambiguity actually is.
- D-08: where nothing recovers a reason, the comment is rewritten to a weaker but true statement and the site is named in the plan's summary, never deleted.

Those identifiers belong to this planning document. No replacement string this
plan writes into product source may contain one.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-01-SUMMARY.md
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-04-SUMMARY.md
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md
@.planning/ENGINEERING_RULES.md
</context>

## Artifacts this phase produces

See `51-01-PLAN.md`. This plan creates no new symbol.

<tasks>

<task type="auto">
  <name>Task 1: Sweep vice-broker.mts to zero</name>
  <files>src/mcp/vice/vice-broker.mts, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/vice-broker.mts` — the whole header, then every comment block containing a citation
    - `.planning/ENGINEERING_RULES.md` section 21.2 — the worked bad/good rewrite pair
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Sections A, B, D and E
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-04-SUMMARY.md` — the rewrite patterns that worked on the sibling broker sources
  </read_first>
  <action>
Rewrite every citation in `vice-broker.mts` into the reason it stood for, using
the tier ladder in the resolution document: delete a bare parenthetical whose
sentence already states the reason; turn an inline citation into a clause that
states it; take a sparse line's reason from the block above it or from the phase
artifacts the resolution document names; and where nothing recovers a reason,
rewrite to a weaker but TRUE statement and NAME the site in the summary with its
line number. Never delete a comment.

Comments needing particular care here, because each is the only record of its
fact: the lease model (the connection IS the lease, with no separate expiry or
heartbeat, and what that implies for a dropped connection); the warm-floor and
instance-promotion behaviour, whose plan citations RESOLVE — the resolution
document names the documents and their prose can be paraphrased directly; port
allocation and crash supervision; and the non-dotted alias this file mints at
startup for the tool-written root, whose whole justification is an external
tool's path handling and which must survive as a complete explanation rather than
a pointer.

Constraints: edit comment spans, not code. Do not shorten a header explaining why
the file exists. Do not repoint any citation at a different planning-tree path.
Every replacement string must itself be free of planning-tree paths, phase and
plan numbers, decision, gap and requirement ids, and planning-document
cross-references.

Lower this file's `RATCHET` count to the number the guard reports, read from its
failure message, and delete the entry at zero.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test skills-planning-vocabulary.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `vice-broker.mts` has no `RATCHET` entry and scans clean
    - `grep -ac '\.planning' src/mcp/vice/vice-broker.mts` outputs `0`
    - The lease-model comment and the alias-minting comment are present and not shorter in substance; quote both before and after in the summary
    - `git diff --numstat src/mcp/vice/vice-broker.mts` shows insertions as well as deletions
    - Every tier-4 site is named in the summary with its line number
  </acceptance_criteria>
  <done>The broker daemon source is at zero with its lease model and alias justification intact.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep vice-broker-client.ts to zero and keep both sides of the lease contract consistent</name>
  <files>src/mcp/vice/vice-broker-client.ts, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/vice-broker-client.ts` — the whole header, then every comment block containing a citation
    - `src/mcp/vice/vice-broker.mts` — as left by Task 1, specifically the lease and recycle comments, so the client-side description agrees with the daemon-side one
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Sections A, B, D and E
  </read_first>
  <action>
Rewrite every citation in `vice-broker-client.ts` under the same rules as Task 1.

This file describes the same acquire, release and recycle contract from the
container side. Where Task 1 rewrote a daemon-side comment about the same fact,
make the client-side comment agree with it rather than inventing a second
wording. Where the two sides currently disagree, say so in the summary — a
disagreement discovered during the sweep is a real finding, not a rewrite
problem, and must not be papered over by picking one at random.

Care is needed on the comments describing what a wedged emulator looks like from
the client side, and why a second connection attempt is not diagnosed as a hang.
That distinction is the reason a whole troubleshooting playbook exists, and it
must survive as a complete explanation.

Lower and then delete this file's `RATCHET` entry as in Task 1.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test skills-planning-vocabulary.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test vice-broker-client.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `vice-broker-client.ts` has no `RATCHET` entry and scans clean
    - `grep -ac '\.planning' src/mcp/vice/vice-broker-client.ts` outputs `0`
    - The comment distinguishing a wedged emulator from a second unserviced connection is present and not shorter in substance
    - Any disagreement found between the client-side and daemon-side descriptions of the same fact is reported in the summary, with both texts quoted
    - Every tier-4 site is named in the summary with its line number
  </acceptance_criteria>
  <done>The container-side client is at zero, its description of the lease contract agrees with the daemon's, and any disagreement found is reported rather than hidden.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep broker-state.mts and broker-kill.mts, regenerate, and run the gate</name>
  <files>src/mcp/vice/broker-state.mts, src/mcp/vice/broker-kill.mts, src/mcp/vice/resources/vice-broker.mjs, src/mcp/vice/resources/broker-state.mjs, src/mcp/vice/resources/broker-kill.mjs, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/broker-state.mts` — the whole file
    - `src/mcp/vice/broker-kill.mts` — the whole file, especially the comments on writing an incident record before any destructive action
    - `src/mcp/vice/build.ts` — `build()` and `HOST_BOUND_ARTIFACTS`
    - `src/mcp/vice/resources-sync.test.ts` — its assertion
  </read_first>
  <action>
Sweep `broker-state.mts` and `broker-kill.mts` to zero under the same rules.

The ordering rule in the kill path — an incident record is written BEFORE
anything is killed, so evidence survives the thing that destroyed it — must
survive as a full explanation. It is the reason the function is shaped the way it
is, and a shorter sentence would not stop anyone from reordering it.

Then run the build so `resources/vice-broker.mjs`, `resources/broker-state.mjs`
and `resources/broker-kill.mjs` are regenerated from the corrected sources, and
commit them. Never hand-edit a generated artifact.

Delete the `RATCHET` entries for the two remaining sources and all three
generated artifacts once each reports zero, then run the full automated gate.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run build</automated>
    <fails_when>non-zero exit, or the output does not report that the artifacts were written</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test resources-sync.test.ts skills-planning-vocabulary.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -ac 'vice-broker\|broker-state\|broker-kill' skills-planning-vocabulary.test.ts)" -le "4"</automated>
    <fails_when>the printed count is above 4 — a ratchet entry for one of these sources or their generated artifacts was not deleted (at most the four frozen budget-baseline rows may remain)</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated</automated>
    <fails_when>non-zero exit, or the summary line "ℹ fail 0" is absent from the output</fails_when>
  </verify>
  <acceptance_criteria>
    - All four sources and all three generated artifacts scan clean and have no `RATCHET` entry
    - `npm run build` followed by `node --test resources-sync.test.ts` is green
    - The incident-before-kill ordering comment is present and not shorter in substance; quote it before and after in the summary
    - Every tier-4 site across both files is named in the summary with its line number
    - `npm run test:automated` exits 0 and reports `fail 0`
  </acceptance_criteria>
  <done>Seven ledger entries are gone, the generated artifacts match a fresh build, and the whole automated gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repository → the npm tarball | These comments reach a consumer with no planning tree |
| `.mts` source → generated `.mjs` artifact | A hand edit on the generated side diverges silently from the source |
| the kill path → an in-flight capture | The incident-before-kill ordering is what makes a destroyed run still explainable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-02 | Tampering | the lease-model, wedge-diagnosis and incident-ordering comments | high | mitigate | The comment-byte budget reds on net loss; this plan additionally requires three named comments to be quoted before and after |
| T-51-03 | Tampering | broker runtime behaviour | high | mitigate | Edits confined to comment spans; typecheck and the full automated gate run before the plan closes |
| T-51-09 | Tampering | the three generated artifacts | medium | mitigate | Regenerated by the build; the byte-identity guard asserts they match |
| T-51-10 | Repudiation | disagreement between the two sides of the lease contract | medium | mitigate | Task 2 requires any disagreement found to be reported with both texts quoted, rather than resolved silently by picking one |
| T-51-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan and none is added |
</threat_model>

<verification>
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck` exits 0
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated` exits 0 and reports `fail 0`
- Four sources and three generated artifacts at zero, seven ratchet entries deleted
</verification>

<success_criteria>
- The broker family's explanations are complete rather than referential
- No generated artifact was hand-edited
- Every unrecoverable site, and every cross-file disagreement found, is reported
</success_criteria>

<output>
Create `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-05-SUMMARY.md` when done.

Record the three named comments before and after, comment characters before and
after per file, every tier-4 site by line number, and any client/daemon
disagreement found.
</output>
