---
phase: 51-planning-vocabulary-out-of-the-shipped-server
plan: 12
type: execute
wave: 11
depends_on: ["51-11"]
files_modified:
  - src/mcp/vice/stock-checkpoints.ts
  - src/mcp/vice/stock-cia.ts
  - src/mcp/vice/stock-timing.ts
  - src/mcp/vice/stock-execution.ts
  - src/mcp/vice/stock-memory-search.ts
  - src/mcp/vice/stock-run-until.ts
  - src/mcp/vice/stock-condition.ts
  - src/mcp/vice/stock-reproducible-run.ts
  - src/mcp/vice/skills-planning-vocabulary.test.ts
autonomous: true
requirements: [VOCAB-01, VOCAB-02, VOCAB-04, VOCAB-06]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The checkpoint, execution, timing, search, run-until, condition and reproducible-run modules all scan clean and their ratchet entries are gone (VOCAB-01)."
    - "The condition-language rules — the pseudo-register names, the absence of operator precedence, and hexadecimal-by-default integer literals — read identically after the sweep (VOCAB-02)."
    - "The single-resume-per-wait invariant is still stated in the module that holds it, and the test that proves it is untouched (VOCAB-02)."
  artifacts:
    - src/mcp/vice/stock-condition.ts
    - src/mcp/vice/stock-run-until.ts
  key_links:
    - "`stock-condition.ts`'s rules -> every checkpoint a user sets: a condition that silently parses to something always-false is the failure these comments exist to prevent."
---

<objective>
Sweep the checkpoint and execution group — about 167 citations across eight
modules — to zero.

Purpose: two facts in this group are silent-failure generators. A checkpoint
condition written with the wrong register names, or relying on operator
precedence that does not exist, parses successfully and is always false. And a
resume sent while a wait is outstanding corrupts the run. Both are explained in
comments and enforced nowhere a reader can see without them.

Output: eight files at zero, eight ratchet entries deleted.

Counts are sizing context measured before planning; the ledger is the authority.

Locked decisions implemented by this plan:

- D-06: sweep plans partition by module family, not by file and not by citation category.
- D-07: recovery is tiered by identifier class, so lookup effort is spent where the ambiguity actually is.
- D-08: where nothing recovers a reason, the comment is rewritten to a weaker but true statement and the site is named in the plan's summary, never deleted.

Those identifiers belong to this planning document. No replacement string this
plan writes into product source may contain one.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-01-SUMMARY.md
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md
@.planning/ENGINEERING_RULES.md
</context>

## Artifacts this phase produces

See `51-01-PLAN.md`. This plan creates no new symbol.

<tasks>

<task type="auto">
  <name>Task 1: Sweep stock-checkpoints.ts, stock-condition.ts and stock-run-until.ts to zero</name>
  <files>src/mcp/vice/stock-checkpoints.ts, src/mcp/vice/stock-condition.ts, src/mcp/vice/stock-run-until.ts, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/stock-condition.ts` — the whole file. Every comment describing the condition language: which pseudo-register names are accepted and which lex as something else, the absence of operator precedence and the parenthesisation it forces, and the base an unprefixed integer literal is read in
    - `src/mcp/vice/stock-run-until.ts` — the whole file, especially the comments on never sending a second resume while a wait is outstanding, and on resolving state from a response field rather than from elapsed time
    - `src/mcp/vice/stock-checkpoints.ts` — the header and every comment block containing a citation
    - `.planning/ENGINEERING_RULES.md` section 21.2 — the worked bad/good rewrite pair
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Sections A, B, D and E
  </read_first>
  <action>
Rewrite every citation in the three modules into the reason it stood for,
splitting comment sites from string-literal sites first and recording the split.

Apply the tier ladder: delete a bare parenthetical whose sentence already states
the reason; turn an inline citation into a clause that states it; take a sparse
line's reason from the block above it or from the phase artifacts the resolution
document names; and where nothing recovers a reason, rewrite to a weaker but TRUE
statement and NAME the site in the summary with its line number. Never delete a
comment.

Three comment families here are silent-failure documentation and must survive
word for word in their technical content:

  - the condition language's accepted pseudo-register names, and which
    similar-looking names lex as something else entirely;
  - the absence of operator precedence, with the worked example showing how an
    unparenthesised two-clause condition parses into something always false;
  - the base an unprefixed integer literal is read in, with its worked example.

Where a citation is attached to one of these, delete the citation and leave every
identifier, every example and every number exactly as written.

`stock-run-until.ts` holds the single-resume-per-wait invariant. Its comment is
the only place that says why the code is event-driven rather than polling, and a
unit test against a synthetic client depends on the invariant continuing to hold.
Do not change the code and do not change the test; keep the comment at full
length.

Constraints: edit comment spans, and string literals only where the citation sits
in output; do not shorten a header explaining why the file exists; do not repoint
any citation at a different planning-tree path; every replacement string you write
must itself be free of planning-tree paths, phase and plan numbers, decision, gap
and requirement ids, and planning-document cross-references.

Lower each file's `RATCHET` count to the number the guard reports and delete each
entry at zero.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test skills-planning-vocabulary.test.ts stock-condition.test.ts stock-run-until.test.ts stock-checkpoints.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test -z "$(git diff -- stock-run-until.ts stock-condition.ts | grep -a '^[-+]' | grep -avE '^(\+\+\+|---)' | grep -avE '^[-+][[:space:]]*(//|\*|/\*)')"</automated>
    <fails_when>non-zero exit — a non-comment line was changed in one of the two modules whose behaviour this plan forbids touching. Re-run the inner pipeline without the `test -z` wrapper to see which lines</fails_when>
  </verify>
  <acceptance_criteria>
    - All three modules scan clean and none has a `RATCHET` entry
    - `grep -ac '\.planning' src/mcp/vice/stock-condition.ts src/mcp/vice/stock-run-until.ts src/mcp/vice/stock-checkpoints.ts` reports `0` for every file
    - The diff for the condition and run-until modules contains only comment lines
    - The three condition-language comment families keep every identifier, example and number; the summary quotes each before and after
    - The single-resume-per-wait comment is present and not shorter in substance
    - Every tier-4 site is named in the summary with its line number
  </acceptance_criteria>
  <done>The three silent-failure explanations are intact, the two behaviour-critical modules have comment-only diffs, and all three files are at zero.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the remaining five execution and timing modules to zero</name>
  <files>src/mcp/vice/stock-execution.ts, src/mcp/vice/stock-timing.ts, src/mcp/vice/stock-cia.ts, src/mcp/vice/stock-memory-search.ts, src/mcp/vice/stock-reproducible-run.ts, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/stock-execution.ts` — the header and every comment block containing a citation, especially those on the default memory space becoming contaminated and what resets it
    - `src/mcp/vice/stock-timing.ts` — the comments on the absence of a monotonic cycle register and how absolute cycles are reconstructed
    - `src/mcp/vice/stock-cia.ts`, `src/mcp/vice/stock-memory-search.ts`, `src/mcp/vice/stock-reproducible-run.ts` — the comment blocks containing citations in each
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Sections A, B, D and E
  </read_first>
  <action>
Sweep the five modules to zero under the same rules as Task 1.

Facts here that must survive with their identifiers and numbers intact: the
default memory space is contaminated by a drive checkpoint hit and no binary
command resets it, while a specific text-channel command does; there is no
monotonic cycle register, so the readable line and cycle values are not monotonic
and absolute cycles must be reconstructed or read from the text channel's own
counter; and the processor-history request's count field is read wide but stored
narrow, so a large count wraps and must be clamped on this side.

Each of those is a measured fact with a consequence. Keep the fact, keep the
consequence, delete only the citation.

Delete each file's `RATCHET` entry once it reports zero, then run the full
automated gate.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test skills-planning-vocabulary.test.ts stock-execution.test.ts stock-timing.test.ts stock-cia.test.ts stock-memory-search.test.ts stock-reproducible-run.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -ac 'stock-checkpoints.ts\|stock-cia.ts\|stock-timing.ts\|stock-execution.ts\|stock-memory-search.ts\|stock-run-until.ts\|stock-condition.ts\|stock-reproducible-run.ts' skills-planning-vocabulary.test.ts)" -le "8"</automated>
    <fails_when>the printed count is above 8 — at least one ratchet entry for this plan's files was not deleted (at most the eight frozen budget-baseline rows may remain)</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated</automated>
    <fails_when>non-zero exit, or the summary line "ℹ fail 0" is absent from the output</fails_when>
  </verify>
  <acceptance_criteria>
    - All eight of this plan's files scan clean and none has a `RATCHET` entry
    - `grep -ac '\.planning' src/mcp/vice/stock-execution.ts src/mcp/vice/stock-timing.ts src/mcp/vice/stock-cia.ts src/mcp/vice/stock-memory-search.ts src/mcp/vice/stock-reproducible-run.ts` reports `0` for every file
    - The three named measured facts still state both the fact and its consequence; the summary quotes each before and after
    - Every tier-4 site across the five files is named in the summary with its line number
    - `npm run test:automated` exits 0 and reports `fail 0`
  </acceptance_criteria>
  <done>Eight ledger entries are gone and the whole automated gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repository → the npm tarball | These comments reach a consumer with no planning tree |
| a user-written checkpoint condition → the emulator's parser | A condition that parses but is always false fails silently; only these comments prevent writing one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-02 | Tampering | the condition-language rules and the single-resume invariant | high | mitigate | The comment-byte budget reds on net loss; this plan additionally requires comment-only diffs on the two behaviour-critical modules and every identifier, example and number preserved |
| T-51-03 | Tampering | checkpoint and execution runtime behaviour | high | mitigate | A comment-only-diff gate on the two modules, plus eight owning test files and the full automated gate |
| T-51-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan and none is added |
</threat_model>

<verification>
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck` exits 0
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated` exits 0 and reports `fail 0`
- Eight files at zero, eight ratchet entries deleted
- Comment-only diffs on the condition and run-until modules
</verification>

<success_criteria>
- Every silent-failure explanation survived with its worked examples
- The single-resume invariant and its test are untouched
- Every unrecoverable site is named rather than silently weakened
</success_criteria>

<output>
Create `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-12-SUMMARY.md` when done.

Record the condition-language comments and the three measured facts before and
after, comment characters before and after per file, and every tier-4 site by
line number.
</output>
