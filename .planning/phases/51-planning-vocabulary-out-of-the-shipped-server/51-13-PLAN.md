---
phase: 51-planning-vocabulary-out-of-the-shipped-server
plan: 13
type: execute
wave: 12
depends_on: ["51-12"]
files_modified:
  - src/mcp/vice/stock-disassemble.ts
  - src/mcp/vice/stock-address.ts
  - src/mcp/vice/stock-memory.ts
  - src/mcp/vice/stock-sprites.ts
  - src/mcp/vice/stock-machine.ts
  - src/mcp/vice/stock-symbols.ts
  - src/mcp/vice/stock-paths.ts
  - src/mcp/vice/stock-registers.ts
  - src/mcp/vice/stock-input.ts
  - src/mcp/vice/stock-vicii.ts
  - src/mcp/vice/stock-petscii.ts
  - src/mcp/vice/skills-planning-vocabulary.test.ts
autonomous: true
requirements: [VOCAB-01, VOCAB-02, VOCAB-04, VOCAB-06]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Eleven transport modules scan clean and their ratchet entries are gone (VOCAB-01)."
    - "The three resources that power-cycle the machine one call deep are still named, with the consequence stated, in the module that denies them (VOCAB-02)."
    - "The wire memory-space encoding — that it is not the internal enumeration, and which value means which unit — reads identically after the sweep (VOCAB-02)."
  artifacts:
    - src/mcp/vice/stock-machine.ts
    - src/mcp/vice/stock-address.ts
  key_links:
    - "`stock-machine.ts`'s denial list -> an operator's whole session: the comment naming the three destructive resources is the only explanation of why the tool refuses them."
---

<objective>
Sweep the remaining transport modules — about 155 citations across eleven files —
to zero.

Purpose: this group holds two facts that a maintainer would otherwise get wrong
from upstream sources alone: which wire value means which memory space, and which
three settings destroy all emulation state one call deep.

Output: eleven files at zero, eleven ratchet entries deleted.

Counts are sizing context measured before planning; the ledger is the authority.

Locked decisions implemented by this plan:

- D-06: sweep plans partition by module family, not by file and not by citation category.
- D-07: recovery is tiered by identifier class, so lookup effort is spent where the ambiguity actually is.
- D-08: where nothing recovers a reason, the comment is rewritten to a weaker but true statement and the site is named in the plan's summary, never deleted.

Those identifiers belong to this planning document. No replacement string this
plan writes into product source may contain one.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-01-SUMMARY.md
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md
@.planning/ENGINEERING_RULES.md
</context>

## Artifacts this phase produces

See `51-01-PLAN.md`. This plan creates no new symbol.

<tasks>

<task type="auto">
  <name>Task 1: Sweep stock-machine.ts, stock-address.ts, stock-memory.ts, stock-input.ts and stock-paths.ts to zero</name>
  <files>src/mcp/vice/stock-machine.ts, src/mcp/vice/stock-address.ts, src/mcp/vice/stock-memory.ts, src/mcp/vice/stock-input.ts, src/mcp/vice/stock-paths.ts, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/stock-machine.ts` — the whole header and every comment block containing a citation, especially the list of settings that power-cycle the machine and why they are denied
    - `src/mcp/vice/stock-address.ts` — the comments describing how a memory space is encoded on the wire, including which values are accepted and which are rejected
    - `src/mcp/vice/stock-memory.ts`, `src/mcp/vice/stock-input.ts`, `src/mcp/vice/stock-paths.ts` — the comment blocks containing citations in each
    - `.planning/ENGINEERING_RULES.md` section 21.2 — the worked bad/good rewrite pair
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Sections A, B, D and E
  </read_first>
  <action>
Rewrite every citation in the five modules into the reason it stood for,
splitting comment sites from string-literal sites first and recording the split.

Apply the tier ladder: delete a bare parenthetical whose sentence already states
the reason; turn an inline citation into a clause that states it; take a sparse
line's reason from the block above it or from the phase artifacts the resolution
document names; and where nothing recovers a reason, rewrite to a weaker but TRUE
statement and NAME the site in the summary with its line number. Never delete a
comment.

Two comment families must survive with every name and number intact:

  - the three settings that reach a power cycle one call deep and therefore
    destroy all emulation state, and the rule that any setting-change tool exposed
    to a model must deny them. Keep all three names and keep the consequence;
  - the wire encoding of a memory space: that the byte on the wire is NOT the
    internal enumeration, which value means the main processor, which range means
    which drive unit, and which value is rejected outright. Keep every value.

`stock-input.ts` records that text injection reaches only the keyboard buffer and
that the matrix is not recoverable at all on this emulator. That is a permanent
limitation, not a pending item; keep it stated as a limitation.

Constraints: edit comment spans, and string literals only where the citation sits
in output; do not shorten a header explaining why the file exists; do not repoint
any citation at a different planning-tree path; every replacement string you write
must itself be free of planning-tree paths, phase and plan numbers, decision, gap
and requirement ids, and planning-document cross-references.

Lower each file's `RATCHET` count to the number the guard reports and delete each
entry at zero.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test skills-planning-vocabulary.test.ts stock-machine.test.ts stock-address.test.ts stock-memory.test.ts stock-input.test.ts stock-paths.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test -z "$(git diff -- stock-machine.ts stock-address.ts | grep -a '^[-+]' | grep -avE '^(\+\+\+|---)' | grep -avE '^[-+][[:space:]]*(//|\*|/\*)')"</automated>
    <fails_when>non-zero exit — a non-comment line was changed in the denial list or the memory-space encoder. Re-run the inner pipeline without the `test -z` wrapper to see which lines</fails_when>
  </verify>
  <acceptance_criteria>
    - All five modules scan clean and none has a `RATCHET` entry
    - `grep -ac '\.planning' src/mcp/vice/stock-machine.ts src/mcp/vice/stock-address.ts src/mcp/vice/stock-memory.ts src/mcp/vice/stock-input.ts src/mcp/vice/stock-paths.ts` reports `0` for every file
    - The diff for the denial list and the memory-space encoder contains only comment lines
    - All three destructive setting names are still present in the denial comment, with the consequence stated; quote it before and after in the summary
    - Every value in the memory-space encoding comment is unchanged; quote it before and after in the summary
    - Every tier-4 site is named in the summary with its line number
  </acceptance_criteria>
  <done>The denial list and the wire encoding are documented exactly as before, and five modules are at zero.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the remaining six transport modules to zero</name>
  <files>src/mcp/vice/stock-disassemble.ts, src/mcp/vice/stock-sprites.ts, src/mcp/vice/stock-symbols.ts, src/mcp/vice/stock-registers.ts, src/mcp/vice/stock-vicii.ts, src/mcp/vice/stock-petscii.ts, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/stock-disassemble.ts`, `src/mcp/vice/stock-sprites.ts`, `src/mcp/vice/stock-symbols.ts`, `src/mcp/vice/stock-registers.ts`, `src/mcp/vice/stock-vicii.ts`, `src/mcp/vice/stock-petscii.ts` — the comment blocks containing citations in each
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Sections A, B, D and E
  </read_first>
  <action>
Sweep the six remaining modules to zero under the same rules as Task 1.

Facts here that must survive with their values intact: which chip registers are
readable and which are write-only in hardware, so a read-back is unrecoverable
rather than merely unimplemented; which register block addresses each derived
answer is computed from; and which of these answers are computed on this side
because the emulator's binary monitor has no command for them. Keep every address
and every register name.

`stock-registers.ts` carries an evidence-document path citation; rewrite it into
the reason it stood for, never repointed at the planning tree.

Delete each file's `RATCHET` entry once it reports zero, then run the full
automated gate.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test skills-planning-vocabulary.test.ts stock-disassemble.test.ts stock-sprites.test.ts stock-symbols.test.ts stock-registers.test.ts stock-vicii.test.ts stock-petscii.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -aE -c 'docs/phase[0-9]' stock-registers.ts)" = "0"</automated>
    <fails_when>the printed count is not 0 — the evidence-document path citation survives in this file</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -ac 'stock-disassemble.ts\|stock-address.ts\|stock-memory.ts\|stock-sprites.ts\|stock-machine.ts\|stock-symbols.ts\|stock-paths.ts\|stock-registers.ts\|stock-input.ts\|stock-vicii.ts\|stock-petscii.ts' skills-planning-vocabulary.test.ts)" -le "11"</automated>
    <fails_when>the printed count is above 11 — at least one ratchet entry for this plan's files was not deleted (at most the eleven frozen budget-baseline rows may remain)</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated</automated>
    <fails_when>non-zero exit, or the summary line "ℹ fail 0" is absent from the output</fails_when>
  </verify>
  <acceptance_criteria>
    - All eleven of this plan's files scan clean and none has a `RATCHET` entry
    - `grep -ac '\.planning' src/mcp/vice/stock-disassemble.ts src/mcp/vice/stock-sprites.ts src/mcp/vice/stock-symbols.ts src/mcp/vice/stock-registers.ts src/mcp/vice/stock-vicii.ts src/mcp/vice/stock-petscii.ts` reports `0` for every file
    - No register name and no register-block address changed; the summary lists every such comment touched and confirms it
    - Every tier-4 site across the six files is named in the summary with its line number
    - `npm run test:automated` exits 0 and reports `fail 0`
  </acceptance_criteria>
  <done>Eleven ledger entries are gone and the whole automated gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repository → the npm tarball | These comments reach a consumer with no planning tree |
| a setting-change request from a model → the emulator | Three settings destroy all emulation state one call deep; the denial and its explanation are the only guard a reader sees |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-02 | Tampering | the destructive-setting denial comment and the wire memory-space encoding | high | mitigate | The comment-byte budget reds on net loss; this plan additionally requires comment-only diffs on both modules and every name and value preserved and quoted |
| T-51-03 | Tampering | transport runtime behaviour | high | mitigate | A comment-only-diff gate on the two sensitive modules, plus eleven owning test files and the full automated gate |
| T-51-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan and none is added |
</threat_model>

<verification>
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck` exits 0
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated` exits 0 and reports `fail 0`
- Eleven files at zero, eleven ratchet entries deleted
</verification>

<success_criteria>
- The denial list and the wire encoding are byte-for-byte the same facts
- Hardware limitations still read as limitations, not as pending work
- Every unrecoverable site is named rather than silently weakened
</success_criteria>

<output>
Create `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-13-SUMMARY.md` when done.

Record the denial comment and the memory-space encoding before and after, the
list of register comments touched with a confirmation that no value changed,
comment characters before and after per file, and every tier-4 site by line
number.
</output>
