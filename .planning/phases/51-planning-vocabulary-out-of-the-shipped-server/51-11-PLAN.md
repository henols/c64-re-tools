---
phase: 51-planning-vocabulary-out-of-the-shipped-server
plan: 11
type: execute
wave: 10
depends_on: ["51-10"]
files_modified:
  - src/mcp/vice/text-protocol.ts
  - src/mcp/vice/text-tools.ts
  - src/mcp/vice/text-capability-probe.ts
  - src/mcp/vice/text-connect.ts
  - src/mcp/vice/stock-connect.ts
  - src/mcp/vice/stock-diagnose.ts
  - src/mcp/vice/stock-recycle.ts
  - src/mcp/vice/stock-runstate.ts
  - src/mcp/vice/stock-handler.ts
  - src/mcp/vice/container-guard.mts
  - src/mcp/vice/resources/container-guard.mjs
  - src/mcp/vice/skills-planning-vocabulary.test.ts
autonomous: true
requirements: [VOCAB-01, VOCAB-02, VOCAB-04, VOCAB-06]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The text channel's four modules, the binary connect path, the diagnosis, recycle, run-state and handler modules and the container detector all scan clean with their ratchet entries gone (VOCAB-01)."
    - "The generated container-detector artifact is regenerated and byte-identical to a fresh build (VOCAB-01)."
    - "The comments recording that the emulator's binary monitor serves exactly one client, and that a second connection is not a wedge, survive at full length (VOCAB-02)."
  artifacts:
    - src/mcp/vice/stock-diagnose.ts
    - src/mcp/vice/text-protocol.ts
    - src/mcp/vice/container-guard.mts
  key_links:
    - "`stock-diagnose.ts`'s wedge-versus-contention prose -> a troubleshooting playbook this project ships: the playbook tells a reader what to do, this comment says why."
---

<objective>
Sweep the text channel, the connection and diagnosis paths and the container
detector — about 241 citations across ten files — to zero, and regenerate the one
artifact among them that the build produces.

Purpose: this group is where the project distinguishes a genuinely wedged
emulator from one that is merely contended or stopped at a checkpoint. That
distinction is the reason several tools refuse rather than kill, and the comments
stating it are its only explanation.

Output: ten sources and one generated artifact at zero, eleven ratchet entries
deleted.

Counts are sizing context measured before planning; the ledger is the authority.

Locked decisions implemented by this plan:

- D-06: sweep plans partition by module family, not by file and not by citation category.
- D-07: recovery is tiered by identifier class, so lookup effort is spent where the ambiguity actually is.
- D-08: where nothing recovers a reason, the comment is rewritten to a weaker but true statement and the site is named in the plan's summary, never deleted.

Those identifiers belong to this planning document. No replacement string this
plan writes into product source may contain one.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-01-SUMMARY.md
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md
@.planning/ENGINEERING_RULES.md
</context>

## Artifacts this phase produces

See `51-01-PLAN.md`. This plan creates no new symbol.

<tasks>

<task type="auto">
  <name>Task 1: Sweep the four text-channel modules to zero</name>
  <files>src/mcp/vice/text-protocol.ts, src/mcp/vice/text-tools.ts, src/mcp/vice/text-capability-probe.ts, src/mcp/vice/text-connect.ts, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/text-protocol.ts` — the whole header, then every comment block containing a citation, especially the ones recording which text-monitor commands were probed live and what each returned
    - `src/mcp/vice/text-tools.ts`, `src/mcp/vice/text-capability-probe.ts`, `src/mcp/vice/text-connect.ts` — the comment blocks containing citations in each
    - `.planning/ENGINEERING_RULES.md` section 21.2 — the worked bad/good rewrite pair
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Sections A, B, D and E
  </read_first>
  <action>
Rewrite every citation in the four text-channel modules into the reason it stood
for, splitting comment sites from string-literal sites first and recording the
split per file.

Apply the tier ladder: delete a bare parenthetical whose sentence already states
the reason; turn an inline citation into a clause that states it; take a sparse
line's reason from the block above it or from the phase artifacts the resolution
document names; and where nothing recovers a reason, rewrite to a weaker but TRUE
statement and NAME the site in the summary with its line number. Never delete a
comment.

These modules carry measured facts about the text channel that contradict what a
reader would otherwise assume: that a capability absent from the binary monitor
is reachable over the text channel, that warp control works as a runtime command
even though no runtime setting exists for it, that per-command support is opt-out
at build time rather than gated on a version, and that several commands share one
guard while others carry none. Each of those is a measurement with a date. Keep
the fact and the date; delete only the citation. Where a citation is the only
thing on the line and the measurement is in the block above, delete the line.

Constraints: edit comment spans, and string literals only where the citation sits
in output; do not shorten a header explaining why the file exists; do not repoint
any citation at a different planning-tree path; every replacement string you write
must itself be free of planning-tree paths, phase and plan numbers, decision, gap
and requirement ids, and planning-document cross-references.

Lower each file's `RATCHET` count to the number the guard reports, read from its
failure message, and delete each entry at zero.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test skills-planning-vocabulary.test.ts text-protocol.test.ts text-tools.test.ts text-capability-probe.test.ts text-connect.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - All four text-channel modules scan clean and none has a `RATCHET` entry
    - `grep -ac '\.planning' src/mcp/vice/text-protocol.ts src/mcp/vice/text-tools.ts src/mcp/vice/text-capability-probe.ts src/mcp/vice/text-connect.ts` reports `0` for every file
    - Every measured fact that carried a date still carries that date; the summary lists each measurement comment touched and confirms the date survived
    - Every tier-4 site is named in the summary with its line number
  </acceptance_criteria>
  <done>The text channel's four modules are at zero with every dated measurement intact.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep stock-connect.ts, stock-diagnose.ts, stock-recycle.ts, stock-runstate.ts and stock-handler.ts to zero</name>
  <files>src/mcp/vice/stock-connect.ts, src/mcp/vice/stock-diagnose.ts, src/mcp/vice/stock-recycle.ts, src/mcp/vice/stock-runstate.ts, src/mcp/vice/stock-handler.ts, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/stock-diagnose.ts` — the whole header, then every comment block containing a citation, especially those distinguishing a wedge from contention and from a self-stop at a checkpoint
    - `src/mcp/vice/stock-connect.ts` — the whole header and every comment block containing a citation, including the evidence-document path citations
    - `src/mcp/vice/stock-recycle.ts`, `src/mcp/vice/stock-runstate.ts`, `src/mcp/vice/stock-handler.ts` — the comment blocks containing citations in each
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Sections A, B, D and E
  </read_first>
  <action>
Sweep the five modules to zero under the same rules as Task 1.

Two comment families here must survive at full length because they are the reason
several tools refuse rather than act:

  - the emulator's binary monitor serves exactly one client, so a second
    connection sits unserviced with no reply and no end-of-file, and that state is
    indistinguishable from a wedge unless you already know this;
  - a non-stopping checkpoint emits a frame per hit synchronously from inside the
    processor loop, which is why checkpoint-wait code resolves state from a
    response field rather than from elapsed time.

Where a citation is attached to either, delete the citation and leave the
explanation. Where the explanation is thin, lengthen it.

`stock-connect.ts` carries evidence-document path citations; rewrite each into the
reason it stood for, never repointed at the planning tree.

Delete each file's `RATCHET` entry once it reports zero.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test skills-planning-vocabulary.test.ts stock-connect.test.ts stock-diagnose.test.ts stock-recycle.test.ts stock-runstate.test.ts stock-handler.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -aE -c 'docs/phase[0-9]' stock-connect.ts)" = "0"</automated>
    <fails_when>the printed count is not 0 — an evidence-document path citation survives in this file</fails_when>
  </verify>
  <acceptance_criteria>
    - All five modules scan clean and none has a `RATCHET` entry
    - `grep -ac '\.planning' src/mcp/vice/stock-connect.ts src/mcp/vice/stock-diagnose.ts src/mcp/vice/stock-recycle.ts src/mcp/vice/stock-runstate.ts src/mcp/vice/stock-handler.ts` reports `0` for every file
    - The one-client comment and the synchronous-checkpoint-frame comment are present and not shorter in substance; quote both before and after in the summary
    - Every tier-4 site across the five files is named in the summary with its line number
  </acceptance_criteria>
  <done>Five more modules are at zero and the two refusal-justifying explanations are intact.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep container-guard.mts, regenerate its artifact, and run the gate</name>
  <files>src/mcp/vice/container-guard.mts, src/mcp/vice/resources/container-guard.mjs, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/container-guard.mts` — the whole file; it is short and its header carries the five detection signals
    - `src/mcp/vice/build.ts` — `build()` and `HOST_BOUND_ARTIFACTS`
    - `src/mcp/vice/resources-sync.test.ts` — its assertion
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Sections A, B, D and E
  </read_first>
  <action>
Sweep `container-guard.mts` to zero under the same rules. Its header lists the
five signals used to decide whether the process is running inside a container;
each signal is a fact about a runtime and must survive. One of its citations uses
an identifier prefix that the resolution document records as declared nowhere —
check Section E, and if it is confirmed unrecoverable, rewrite the comment to a
weaker but TRUE statement and name the site in the summary.

Then run the build so `resources/container-guard.mjs` is regenerated from the
corrected source, and commit it. Never hand-edit a generated artifact.

Delete the `RATCHET` entries for the source and the generated artifact once both
report zero, then run the full automated gate.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run build</automated>
    <fails_when>non-zero exit, or the output does not report that the artifacts were written</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test resources-sync.test.ts skills-planning-vocabulary.test.ts container-guard.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -ac 'resources/container-guard.mjs' skills-planning-vocabulary.test.ts)" = "0"</automated>
    <fails_when>the printed count is not 0 — the generated artifact's ratchet entry was not deleted after it reached zero</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated</automated>
    <fails_when>non-zero exit, or the summary line "ℹ fail 0" is absent from the output</fails_when>
  </verify>
  <acceptance_criteria>
    - All ten sources and the generated artifact scan clean; none has a `RATCHET` entry
    - `npm run build` followed by `node --test resources-sync.test.ts` is green
    - All five container-detection signals are still described in the header
    - The unrecoverable-prefix site is named in the summary with its line number and the weaker true statement that replaced it
    - `npm run test:automated` exits 0 and reports `fail 0`
  </acceptance_criteria>
  <done>Eleven ledger entries are gone, the generated artifact matches a fresh build, and the whole automated gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repository → the npm tarball | These comments reach a consumer with no planning tree |
| `.mts` source → generated `.mjs` artifact | A hand edit on the generated side diverges silently from the source |
| a stopped emulator → the operator's next action | The wedge-versus-contention distinction decides whether a tool refuses or destroys a live session |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-02 | Tampering | the one-client and synchronous-frame explanations, and the dated text-channel measurements | high | mitigate | The comment-byte budget reds on net loss; this plan additionally requires two named comments quoted before and after and every dated measurement confirmed intact |
| T-51-03 | Tampering | transport and diagnosis runtime behaviour | high | mitigate | Edits confined to comment spans; nine owning test files plus the full automated gate run before the plan closes |
| T-51-09 | Tampering | the generated container-detector artifact | medium | mitigate | Regenerated by the build; the byte-identity guard asserts it matches |
| T-51-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan and none is added |
</threat_model>

<verification>
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck` exits 0
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated` exits 0 and reports `fail 0`
- Ten sources and one generated artifact at zero, eleven ratchet entries deleted
</verification>

<success_criteria>
- Every dated measurement kept its date
- The two refusal-justifying explanations read the same or better
- Every unrecoverable site is named rather than silently weakened
</success_criteria>

<output>
Create `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-11-SUMMARY.md` when done.

Record the two named comments before and after, the list of dated measurement
comments touched, comment characters before and after per file, and every tier-4
site by line number.
</output>
