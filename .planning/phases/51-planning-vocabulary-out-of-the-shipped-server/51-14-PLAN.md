---
phase: 51-planning-vocabulary-out-of-the-shipped-server
plan: 14
type: execute
wave: 13
depends_on: ["51-13"]
files_modified:
  - src/mcp/vice/backend-detect.mts
  - src/mcp/vice/resources/backend-detect.mjs
  - src/mcp/vice/install-resources.ts
  - src/mcp/vice/repo-root.ts
  - src/mcp/vice/incident-record.ts
  - src/mcp/vice/version.ts
  - src/mcp/vice/version.test.ts
  - src/mcp/vice/THIRD-PARTY-NOTICES.md
  - src/mcp/vice/containerpath.ts
  - src/mcp/vice/hostpath.ts
  - src/mcp/vice/build.ts
  - src/mcp/vice/capture-predicate.ts
  - src/mcp/vice/evid-reconcile.ts
  - src/mcp/vice/evid-ingest.ts
  - src/mcp/vice/prg-image.ts
  - src/mcp/vice/stop-oracle.ts
  - src/mcp/vice/vsf-slice.ts
  - src/mcp/vice/block-class.ts
  - src/mcp/vice/channel-lock.ts
  - src/mcp/vice/skills-planning-vocabulary.test.ts
autonomous: true
requirements: [VOCAB-01, VOCAB-02, VOCAB-04, VOCAB-06]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Seventeen cross-cutting modules and the shipped third-party notices scan clean, and their ratchet entries are gone (VOCAB-01)."
    - "The one citation living inside a runtime error message states the rule instead, and the test pinning that message is updated in the same commit (VOCAB-02)."
    - "The third-party notices still state every licensing fact they stated before, with the internal references gone (VOCAB-02)."
    - "The generated backend-detector artifact is regenerated and byte-identical to a fresh build (VOCAB-01)."
  artifacts:
    - src/mcp/vice/version.ts
    - src/mcp/vice/THIRD-PARTY-NOTICES.md
    - src/mcp/vice/repo-root.ts
  key_links:
    - "`version.ts`'s refusal message -> a publisher reading a failed build: the message must say which rule was broken, and it currently says which internal decision defined the rule."
    - "`repo-root.ts` and the two path-translation modules -> every writer in the package: their headers are the only statement of where tool-written output goes and why."
---

<objective>
Sweep the cross-cutting modules — about 163 citations across seventeen source
files plus the shipped third-party notices — to zero, and regenerate the one
artifact among them that the build produces.

Purpose: this group contains the phase's two most delicate non-comment sites.
One citation lives inside a runtime error message a publisher reads when a build
fails. Several live inside a legal notices file whose factual content must not
change while its internal references go.

Output: seventeen sources, one notices file and one generated artifact at zero;
nineteen ratchet entries deleted.

Counts are sizing context measured before planning; the ledger is the authority.

Locked decisions implemented by this plan:

- D-06: sweep plans partition by module family, not by file and not by citation category.
- D-07: recovery is tiered by identifier class, so lookup effort is spent where the ambiguity actually is.
- D-08: where nothing recovers a reason, the comment is rewritten to a weaker but true statement and the site is named in the plan's summary, never deleted.

Those identifiers belong to this planning document. No replacement string this
plan writes into product source may contain one.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-01-SUMMARY.md
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md
@.planning/ENGINEERING_RULES.md
</context>

## Artifacts this phase produces

See `51-01-PLAN.md`. This plan creates no new symbol.

<tasks>

<task type="auto">
  <name>Task 1: Sweep version.ts, including the citation inside a runtime error message</name>
  <files>src/mcp/vice/version.ts, src/mcp/vice/version.test.ts, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/version.ts` — the whole file. Its header, the template-resolution rules stated as numbered rules in comments, and the thrown-error template literal near line 93 that names an internal decision inside the message itself
    - `src/mcp/vice/version.test.ts` — every assertion that matches on the text of a thrown message
    - `installer/bin/cli.mjs` — as left by plan 51-01, which rewrote the same rules from the other side. Make the two descriptions agree
    - `.planning/ENGINEERING_RULES.md` section 21.2 — the worked bad/good rewrite pair
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Sections A, B, D and E
  </read_first>
  <action>
Rewrite every citation in `version.ts` into the reason it stood for. Separate the
comment sites from the one site inside a thrown message and record the split.

For the COMMENT sites: this file states the version-template resolution algorithm
as numbered rules, each currently anchored to an internal decision id. The rules
themselves are the valuable content. Keep each rule's text and its number; delete
the id. Where a rule is referenced from another comment as "the rule that says
X", make that reference name what the rule says rather than where it came from.

For the THROWN MESSAGE: the refusal currently tells the reader which internal
decision defined the rule it broke. The reader is someone whose publish just
failed, and that reference resolves nowhere for them. Rewrite the message so it
states the rule that was broken — which component shapes may follow which — in
the same sentence, and update `version.test.ts` in the same commit. Name the old
message, the new message and the updated assertion in the summary. The new
message must not be shorter or vaguer than the old one; it should be more useful.

Make the surviving description in `installer/bin/cli.mjs`, already rewritten in
this phase's first plan, agree with what you write here. If they disagree, say so
in the summary rather than silently choosing one.

Constraints: do not shorten a header explaining why the file exists; do not
repoint any citation at a different planning-tree path; every replacement string
you write must itself be free of planning-tree paths, phase and plan numbers,
decision, gap and requirement ids, and planning-document cross-references.

Lower this file's `RATCHET` count to the number the guard reports and delete the
entry at zero.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test skills-planning-vocabulary.test.ts version.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -ac 'malformed template' version.ts)" -ge "1"</automated>
    <fails_when>the printed count is 0 — the refusal message was deleted rather than rewritten, so a failed publish now reports nothing</fails_when>
  </verify>
  <acceptance_criteria>
    - `version.ts` has no `RATCHET` entry and scans clean
    - The thrown refusal message still exists, names the rule that was broken, and is not shorter than the message it replaced; quote both in the summary
    - `version.test.ts` asserts on the new message text and is green
    - All numbered resolution rules keep their numbers and their text
    - The description in `installer/bin/cli.mjs` and the description here agree, or the disagreement is reported in the summary
    - Every tier-4 site is named in the summary with its line number
  </acceptance_criteria>
  <done>The version rules are stated rather than cited, the refusal message is more useful than before, and its pinning test was updated in the same commit.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the shipped third-party notices, the root and path seams, and the build module</name>
  <files>src/mcp/vice/THIRD-PARTY-NOTICES.md, src/mcp/vice/repo-root.ts, src/mcp/vice/hostpath.ts, src/mcp/vice/containerpath.ts, src/mcp/vice/build.ts, src/mcp/vice/install-resources.ts, src/mcp/vice/incident-record.ts, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/THIRD-PARTY-NOTICES.md` — the whole file, and specifically the paragraphs that state which vendored source is incorporated under which licence, and the paragraphs narrating how a dependency was evaluated
    - `src/mcp/vice/repo-root.ts` — the whole header, which states where tool-written output goes and why every writer derives from one place
    - `src/mcp/vice/hostpath.ts`, `src/mcp/vice/containerpath.ts` — the comment blocks containing citations, including the crash they both record as reproduced live
    - `src/mcp/vice/build.ts` — the two comment blocks containing citations: the generated-file banner's provenance and the constraint note near line 148
    - `src/mcp/vice/install-resources.ts`, `src/mcp/vice/incident-record.ts` — the comment blocks containing citations in each
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Sections A, B, D and E
  </read_first>
  <action>
Sweep these seven files to zero under the same rules.

**The third-party notices need a different standard of care than a source
comment.** Every licensing fact must survive exactly: which source is
incorporated, under which licence, where it lives, whether it is build-time-only
input, what is pinned by which checksum, and which dependencies are runtime ones.
What goes is the internal reference wrapped around a fact — a phase number in
parentheses beside a statement about vendored source, a requirement id beside a
checksum statement, an internal path and document name beside a statement about
how a dependency was evaluated. Rewrite each so the sentence states the fact and
its basis in plain words. Do not delete a paragraph and do not summarise two
paragraphs into one. Where a sentence's only content was the internal reference,
replace it with what that reference established, taking the content from the
phase artifacts the resolution document names.

`repo-root.ts` states where tool-written output goes and that there is exactly
one resolver. `hostpath.ts` and `containerpath.ts` record a crash that both were
written against and that was reproduced live. Those are the reason each file
exists; keep them at full length and, where the citation was carrying the only
description of the crash, write the description.

`build.ts` carries two citations: one naming the document that specified the
generated-file banner, and one naming a task that established a constraint. Both
become statements of what the banner is for and what the constraint is. Note that
this file also exports the host-bound artifact list the widened guard derives its
scan surface from — do not change that list, and do not change any code in this
file.

Delete each file's `RATCHET` entry once it reports zero.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test skills-planning-vocabulary.test.ts repo-root.test.ts containerpath.test.ts hostpath-consumers.test.ts install-resources.test.ts incident-record.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test -z "$(git diff -- build.ts | grep -a '^[-+]' | grep -avE '^(\+\+\+|---)' | grep -avE '^[-+][[:space:]]*(//|\*|/\*)')"</automated>
    <fails_when>non-zero exit — a non-comment line was changed in the module that exports the host-bound artifact list the widened guard derives from. Re-run the inner pipeline without the `test -z` wrapper to see which lines</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test build-atomic.test.ts shipped-modules.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
  </verify>
  <acceptance_criteria>
    - All seven files scan clean and none has a `RATCHET` entry
    - `grep -ac '\.planning' src/mcp/vice/THIRD-PARTY-NOTICES.md src/mcp/vice/repo-root.ts src/mcp/vice/hostpath.ts src/mcp/vice/containerpath.ts src/mcp/vice/build.ts src/mcp/vice/install-resources.ts src/mcp/vice/incident-record.ts` reports `0` for every file
    - The notices file has the same number of paragraphs as before, and every licensing fact listed in the summary is present before and after; quote each changed sentence in the summary
    - `build.ts`'s diff contains only comment lines, and its host-bound artifact list is unchanged
    - The crash description in the two path-translation modules is present and is at least as specific as before
    - Every tier-4 site across the seven files is named in the summary with its line number
  </acceptance_criteria>
  <done>The notices keep every licensing fact with no internal references left, the path and root seams keep their reasons, and the build module's diff is comments only.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep the remaining nine modules, regenerate the backend detector, and run the gate</name>
  <files>src/mcp/vice/backend-detect.mts, src/mcp/vice/resources/backend-detect.mjs, src/mcp/vice/capture-predicate.ts, src/mcp/vice/evid-reconcile.ts, src/mcp/vice/evid-ingest.ts, src/mcp/vice/prg-image.ts, src/mcp/vice/stop-oracle.ts, src/mcp/vice/vsf-slice.ts, src/mcp/vice/block-class.ts, src/mcp/vice/channel-lock.ts, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/backend-detect.mts` — the whole file; its header states what is probed and what happens when the probe finds nothing
    - `src/mcp/vice/capture-predicate.ts`, `src/mcp/vice/evid-reconcile.ts`, `src/mcp/vice/evid-ingest.ts`, `src/mcp/vice/prg-image.ts`, `src/mcp/vice/stop-oracle.ts`, `src/mcp/vice/vsf-slice.ts`, `src/mcp/vice/block-class.ts`, `src/mcp/vice/channel-lock.ts` — the comment blocks containing citations in each
    - `src/mcp/vice/build.ts` — `build()` and the host-bound artifact list
    - `src/mcp/vice/resources-sync.test.ts` — its assertion
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Sections A, B, D and E
  </read_first>
  <action>
Sweep the nine remaining modules to zero under the same rules.

`backend-detect.mts` states this project's standing rule that an external tool is
detected and never installed, and that a missing tool is refused BY NAME with the
remedy in the message. That rule is not negotiable and its statement must survive
at full length.

`block-class.ts` carries a comment describing an item as carried forward for a
future guard review. After this phase no comment may name a numbered phase, so
rewrite it as what it actually is: a named open question about guard scope, with
the question stated. Do not silently drop it — an item recorded as outstanding
must remain recorded as outstanding, just without the phase number.

`evid-ingest.ts` carries an evidence-document path citation; rewrite it into the
reason it stood for.

Then run the build so `resources/backend-detect.mjs` is regenerated, and commit
it. Never hand-edit a generated artifact.

Delete each remaining `RATCHET` entry for this plan's files once each reports
zero, then run the full automated gate.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run build</automated>
    <fails_when>non-zero exit, or the output does not report that the artifacts were written</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test resources-sync.test.ts skills-planning-vocabulary.test.ts backend-detect.test.ts capture-predicate.test.ts evid-reconcile.test.ts evid-ingest.test.ts prg-image.test.ts block-class.test.ts channel-lock.test.ts vsf-slice.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -ac 'resources/backend-detect.mjs' skills-planning-vocabulary.test.ts)" = "0"</automated>
    <fails_when>the printed count is not 0 — the generated artifact's ratchet entry was not deleted after it reached zero</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated</automated>
    <fails_when>non-zero exit, or the summary line "ℹ fail 0" is absent from the output</fails_when>
  </verify>
  <acceptance_criteria>
    - All seventeen sources, the notices file and the generated artifact scan clean; none has a `RATCHET` entry
    - `npm run build` followed by `node --test resources-sync.test.ts` is green
    - The detect-never-install rule is present in `backend-detect.mts` and not shorter in substance; quote it before and after in the summary
    - The outstanding item in `block-class.ts` is still recorded as outstanding, with the question stated and no phase number; quote it before and after in the summary
    - Every tier-4 site across the nine files is named in the summary with its line number
    - `npm run test:automated` exits 0 and reports `fail 0`
  </acceptance_criteria>
  <done>Nineteen ledger entries are gone, the generated artifact matches a fresh build, nothing recorded as outstanding was lost, and the whole automated gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repository → the npm tarball | These comments and the notices file reach a consumer with no planning tree |
| a failed publish → the publisher | The version refusal message is read at the moment something broke and must say what rule was broken |
| the notices file → a downstream licence review | Its factual content is the basis of a redistribution decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-02 | Tampering | the notices file's licensing facts and the root and path seam headers | high | mitigate | The comment-byte budget covers the source files; the notices file is additionally protected by an acceptance criterion requiring the same paragraph count and a quoted before and after for every changed sentence |
| T-51-03 | Tampering | the version refusal message and its pinning test | high | mitigate | The message is rewritten to state the rule, its test updated in the same commit, and a verify command asserts the message still exists |
| T-51-14 | Repudiation | the outstanding item in the block-class module | medium | mitigate | An acceptance criterion requires it to remain recorded as outstanding with the question stated, rather than disappearing with its phase number |
| T-51-09 | Tampering | the generated backend-detector artifact | medium | mitigate | Regenerated by the build; the byte-identity guard asserts it matches |
| T-51-15 | Elevation of Privilege | `build.ts`'s host-bound artifact list | high | mitigate | That list is what the widened guard derives its scan surface from; a comment-only-diff gate on this file prevents a sweep from narrowing the guard's own input |
| T-51-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan and none is added |
</threat_model>

<verification>
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck` exits 0
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated` exits 0 and reports `fail 0`
- Seventeen sources, one notices file and one generated artifact at zero; nineteen ratchet entries deleted
- `build.ts`'s diff contains comment lines only
</verification>

<success_criteria>
- The refusal message a publisher reads is better than it was
- Every licensing fact survived and no internal reference did
- Nothing recorded as outstanding was quietly dropped
</success_criteria>

<output>
Create `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-14-SUMMARY.md` when done.

Record the old and new refusal message with the updated assertion, every changed
sentence in the notices file, the detect-never-install rule before and after, the
rewritten outstanding item, comment characters before and after per file, and
every tier-4 site by line number.
</output>
