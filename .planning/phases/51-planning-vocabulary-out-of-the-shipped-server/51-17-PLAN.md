---
phase: 51-planning-vocabulary-out-of-the-shipped-server
plan: 17
type: execute
wave: 16
depends_on: ["51-16"]
files_modified:
  - src/mcp/vice/ghidra-project.mts
  - src/mcp/vice/host-tool-client.ts
  - src/mcp/vice/resources/ghidra-project.mjs
  - src/mcp/vice/skills-planning-vocabulary.test.ts
autonomous: true
requirements: [VOCAB-01, VOCAB-02, VOCAB-03, VOCAB-04, VOCAB-05, VOCAB-06]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The last two sources and the last generated artifact scan clean, and the ratchet ledger is EMPTY (VOCAB-01)."
    - "With the ledger empty, the guard's standing assertion IS the zero assertion: every file on the scan surface must scan clean or the build fails (VOCAB-01)."
    - "The guard's own header describes what it enforces now, with no paragraph left claiming a backlog that no longer exists (VOCAB-02)."
    - "All five comment-pinning guards are green in one run, with the one that was moved recorded as moved and the four that needed no change recorded with their reasons (VOCAB-03)."
    - "A planted citation on the new surface still reds the guard after the sweep, so the proof is not an artifact of the pre-sweep state (VOCAB-05)."
    - "No planning-tree path is cited from any file either npm tarball publishes, and none was repointed at a different planning-tree path (VOCAB-04)."
  artifacts:
    - src/mcp/vice/skills-planning-vocabulary.test.ts
    - src/mcp/vice/ghidra-project.mts
    - src/mcp/vice/host-tool-client.ts
  key_links:
    - "An empty ratchet -> the zero assertion: the guard's standing check must become success criterion 1's assertion automatically, with no second test to remember to enable."
---

<objective>
Close the phase. Sweep the last two sources, delete the final ratchet entries so
the ledger is empty, let the guard's standing assertion become the zero
assertion, rewrite the last stale paragraph of the guard's header, and verify
all six declared requirements end to end.

Purpose: a ledger that does not reach empty is a permanent exemption list wearing
a different name. This plan is what makes it temporary scaffolding instead.

Output: an empty ledger, a live zero assertion across the whole published
surface, and a recorded verification of every success criterion.

Counts are sizing context measured before planning; the ledger is the authority.

Locked decisions implemented by this plan:

- D-05: the ratchet ledger must reach empty, and its last entry is deleted by the phase's final plan.
- D-06: sweep plans partition by module family, not by file and not by citation category.
- D-07: recovery is tiered by identifier class, so lookup effort is spent where the ambiguity actually is.
- D-08: where nothing recovers a reason, the comment is rewritten to a weaker but true statement and the site is named in the plan's summary, never deleted.

Those identifiers belong to this planning document. No replacement string this
plan writes into product source may contain one.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-01-SUMMARY.md
@.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md
@.planning/ENGINEERING_RULES.md
@.planning/REQUIREMENTS.md
</context>

## Artifacts this phase produces

See `51-01-PLAN.md` for the full list. This plan adds the last new test-case name:

- `"the ratchet ledger is empty: every shipped file scans clean"`

<tasks>

<task type="auto">
  <name>Task 1: Sweep the last two sources and regenerate the last artifact</name>
  <files>src/mcp/vice/ghidra-project.mts, src/mcp/vice/host-tool-client.ts, src/mcp/vice/resources/ghidra-project.mjs, src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/ghidra-project.mts` — the whole header and every comment block containing a citation, especially those explaining the non-dotted alias, why the external tool's path handling requires it, why the link's target is relative, and why an unexpected file at the handle path is refused rather than repaired
    - `src/mcp/vice/host-tool-client.ts` — the comment blocks containing citations
    - `src/mcp/vice/build.ts` — `build()` and the host-bound artifact list
    - `src/mcp/vice/resources-sync.test.ts` — its assertion
    - `.planning/ENGINEERING_RULES.md` section 21.2 — the worked bad/good rewrite pair
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/evidence/CITATION-RESOLUTION.md` — Sections A, B, D and E
  </read_first>
  <action>
Rewrite every citation in the two remaining sources into the reason it stood for,
under the same rules every sweep plan in this phase has used: delete a bare
parenthetical whose sentence already states the reason; turn an inline citation
into a clause that states it; take a sparse line's reason from the block above it
or from the phase artifacts the resolution document names; and where nothing
recovers a reason, rewrite to a weaker but TRUE statement and NAME the site in
the summary with its line number. Never delete a comment.

`ghidra-project.mts` holds the phase's most fragile explanation. A non-dotted
alias exists because an external tool absolutises the path argument it is handed
without resolving a symbolic link, so the link satisfies three conditions at
once: no dotted segment in the path that tool receives, a location inside the
bind-mounted workspace, and a relative link target so the container can traverse
it too. That arrangement depends on the external tool never changing how it
resolves the path, and the comment says so. Every clause of it is load-bearing.
Keep it at full length, and where a citation is attached, delete only the
citation.

`host-tool-client.ts` records that an external tool is detected and refused by
name with the remedy in the message, never installed. Keep that at full length.

Then run the build so `resources/ghidra-project.mjs` is regenerated, and commit
it. Never hand-edit a generated artifact.

Delete the `RATCHET` entries for both sources and the generated artifact once
each reports zero. After this task the array should be empty.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run build</automated>
    <fails_when>non-zero exit, or the output does not report that the artifacts were written</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test resources-sync.test.ts skills-planning-vocabulary.test.ts ghidra-project.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - Both sources and the generated artifact scan clean and none has a `RATCHET` entry
    - `grep -ac '\.planning' src/mcp/vice/ghidra-project.mts src/mcp/vice/host-tool-client.ts` reports `0` for both
    - The alias explanation keeps all three conditions and the dependency on the external tool's behaviour; quote it before and after in the summary
    - The detect-never-install statement in `host-tool-client.ts` is present and not shorter in substance
    - `npm run build` followed by `node --test resources-sync.test.ts` is green
    - Every tier-4 site is named in the summary with its line number
  </acceptance_criteria>
  <done>The last two sources and the last generated artifact are at zero, and the ratchet array is empty.</done>
</task>

<task type="auto">
  <name>Task 2: Empty the ledger, make the zero assertion live, and rewrite the guard's last stale paragraph</name>
  <files>src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `src/mcp/vice/skills-planning-vocabulary.test.ts` — the whole file as the phase leaves it: the ratchet array and its assertion, the comment-byte baseline and its assertion, the slack constant, the nine categories, the three planted controls, and the header paragraphs rewritten in plan 51-01
    - `.planning/ENGINEERING_RULES.md` section 21 — all four subsections, so the rewritten header states the rule as it now stands
    - `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-01-SUMMARY.md` — the frozen ledger total and the design notes the tracer recorded
  </read_first>
  <action>
Make the ledger's emptiness structural rather than incidental.

Confirm `RATCHET` is the empty array. With it empty, the standing assertion
already says "every file on the scan surface must have zero hits" — that is the
zero assertion success criterion 1 asks for, reached with no second test to
enable and no switch to flip. Verify that by reading the assertion, and state in
the summary that this is why the ratchet was designed as an exact pin rather than
an upper bound.

Add one more test, `"the ratchet ledger is empty: every shipped file scans
clean"`, asserting the array's length is zero with a message explaining that a
non-empty ratchet is a permanent exemption list wearing a different name, and
that any future entry must be removed by the change that added it rather than
carried. Keep the ratchet machinery itself — the type, the empty array and the
assertion — so a future regression is pinned by the same mechanism instead of
someone inventing a second one.

**Keep `COMMENT_BUDGET_BASELINE` and its assertion.** Record the decision in the
file and in the summary, with the reason: the ledger that had to reach empty is
the count-pinned ratchet, because a pinned count grants a file an allowance. The
comment-byte baseline grants nothing — it only forbids a file's explanations from
shrinking below what they were minus the citation characters removed — so the
standing objection to an exemption list does not reach it, and it is the only
mechanical backing the second success criterion has. Also state what a future
maintainer should do when a file legitimately loses comment volume for an
unrelated reason: lower that file's baseline in the same change, with the reason
in the commit.

**Rewrite the last stale paragraph of the header.** Whatever text still describes
the ratchet as in progress, or describes the scan as newly widened, now describes
a finished state. Replace it with what is true: which four sources the scan
surface unions, that a generated mirror is skipped at the mirror and scanned at
its source, that the ratchet exists and is empty and why it must stay empty, and
that the comment-byte baseline stays and what it forbids. Leave every other
header paragraph alone — the history, the one content-derived exemption, the
record that there is no by-path exemption and why, and the record that a decision
id is a violation in every form.

**Re-prove non-vacuity after the sweep.** The three planted controls were written
before the tree was clean. Run them again and confirm each still reds on its
planted input and passes on its clean control. In addition, as a scratch
experiment you revert: add one citation to a real shipped module, confirm the
guard reds and names that file and that match, then remove it and confirm green.
Record both observed outcomes and the exact failure text in the summary. This is
what proves the guard is non-vacuous on a tree that is already at zero, which is
the only state it will ever run against from now on.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test skills-planning-vocabulary.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -ac 'the ratchet ledger is empty' skills-planning-vocabulary.test.ts)" = "1"</automated>
    <fails_when>the printed count is not 1 — the emptiness assertion was not added, or was added twice</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -ac 'COMMENT_BUDGET_BASELINE' skills-planning-vocabulary.test.ts)" -ge "2"</automated>
    <fails_when>the printed count is below 2 — the comment-byte baseline was deleted along with the ratchet, removing the only mechanical backing the second success criterion has</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && test "$(grep -ac 'skills-planning-vocabulary.test.ts' test-gate.mjs)" = "0"</automated>
    <fails_when>the printed count is not 0 — the guard was named in the manual-only gate file and no longer runs in the automated gate</fails_when>
  </verify>
  <acceptance_criteria>
    - `RATCHET` is an empty array and the new emptiness test is present exactly once
    - The standing assertion, with an empty ratchet, requires zero hits for every file on the scan surface; the summary quotes the assertion and explains why the exact-pin design makes this automatic
    - `COMMENT_BUDGET_BASELINE`, its assertion and the slack constant are all still present, with the retention decision and its reason written in the file
    - No header paragraph describes the ratchet as in progress or the scan as newly widened; the other header paragraphs are unchanged in substance
    - The three planted controls all still red on their planted input and pass on their clean control
    - Planting one citation in a real shipped module reds the guard and names that file and match; removing it returns to green. Both outcomes and the exact failure text are recorded in the summary, and neither experiment is committed
  </acceptance_criteria>
  <done>The ledger is empty and asserted empty, the zero assertion is live with no switch to flip, the byte baseline survives with its reason recorded, and the guard is proven non-vacuous against a clean tree.</done>
</task>

<task type="auto">
  <name>Task 3: Verify all six requirements end to end and record the evidence</name>
  <files>src/mcp/vice/skills-planning-vocabulary.test.ts</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the six declared requirement ids for this phase
    - `.planning/ROADMAP.md`, the Phase 51 section — the five success criteria the ids map to
    - `src/mcp/vice/test-gate.mjs` — the manual-only list, to confirm the guard is not on it
    - `scripts/check-npm-packages.mjs` — its header, so you know what it asserts about both published tarballs
  </read_first>
  <action>
Run the phase's closing verification and write the evidence into the summary, one
numbered block per requirement. Do not paraphrase a result; record the command
and what it printed.

  1. The scan surface reports zero. Run the guard and record the number of files
     it scanned across all four contributing sources, and that the offender list
     was empty. Confirm the guard runs inside the automated gate by confirming it
     is absent from the manual-only list.
  2. Explanations survived. Record the phase-wide comment-character totals: the
     sum of the frozen baselines against the sum measured now, and the sum of
     citation characters removed. State whether comment volume went down, stayed
     level or went up, and by how much. Then list every tier-4 site reported by
     every plan in this phase, gathered from their summaries into one table, so
     the sites where a reason could not be recovered are visible in one place
     rather than scattered across sixteen documents.
  3. The comment-pinning guards are green. Run all five in one command and record
     the result. Record which one was moved and what changed in it, and record the
     four that needed no change with the reason for each: two do not scan this
     phase's surface at all, one scans only string and template literal bodies,
     and one pins guard filenames rather than comment content.
  4. No unresolvable planning-tree path remains, and nothing was repointed.
     Record that the guard's own category for that path form reports zero across
     the surface, and confirm from the phase's summaries that no rewrite replaced
     a citation with a different planning-tree path.
  5. The planted controls are non-vacuous on the new surface. Record the results
     from Task 2, including the real-module planting experiment.
  6. The evidence-document path form is matched and at zero. Record the category's
     presence in the guard and a zero result across the surface, including the
     skills tree. State explicitly what this phase did NOT do and what remains for
     the phase that owns the relocation: moving those documents out of the
     operator's documentation tree, and the same path form in test files, skill
     scripts and the vestigial deployment copies.

Then run both package-level checks that bear on this phase's claim. Run the full
automated gate, and run the published-tarball validator, which asserts what each
npm package would actually ship. The second is what makes the phase's claim
about "what npm publishes" a measurement rather than an inference.

Finally, record in the summary the one thing a future reader most needs: that the
guard now holds three surfaces at zero, that the ratchet is gone, and that the
comment-byte baseline is the standing protection against a future sweep deleting
explanations instead of replacing them.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test skills-planning-vocabulary.test.ts comment-phase-pointers.test.ts docs-dangling-refs.test.ts hop-chain-comments.test.ts docs-absorbed-decisions.test.ts audit-integrity.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated</automated>
    <fails_when>non-zero exit, or the summary line "ℹ fail 0" is absent from the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-npm-packages.mjs</automated>
    <fails_when>non-zero exit — what at least one of the two packages would publish no longer matches what that script asserts it must contain</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && ! grep -aqE '\.planning/|docs/phase[0-9]' src/mcp/vice/vice-proxy.ts src/mcp/vice/stock-protocol.ts src/mcp/vice/anno-store.ts src/mcp/vice/host-tool.mts src/mcp/vice/tools-manifest.stock.json installer/bin/cli.mjs</automated>
    <fails_when>non-zero exit — a planning-tree path or an evidence-document path survives in one of the six highest-population files, which means the guard's surface derivation is not reaching them</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run build && node --test resources-sync.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "# fail 0" is absent from the output — a generated artifact is not byte-identical to a fresh build, so some plan in this phase edited one by hand or forgot to regenerate</fails_when>
  </verify>
  <acceptance_criteria>
    - The summary contains six numbered evidence blocks, one per declared requirement, each quoting the command run and what it printed
    - The summary contains a single consolidated table of every tier-4 site reported by every plan in this phase, with file, line and the weaker true statement that replaced the citation
    - The summary states the phase-wide comment-character totals before and after and the citation characters removed
    - `npm run test:automated` exits 0 and reports `fail 0`
    - `node scripts/check-npm-packages.mjs` exits 0
    - The five comment-pinning guards are green in one run, and the summary names the one that moved and the four that did not with their reasons
    - The summary states what this phase did NOT do about the evidence-document path form and what remains for the phase that owns the relocation
  </acceptance_criteria>
  <done>All six requirements are verified with recorded evidence, both published tarballs validate, every generated artifact matches a fresh build, and the unrecoverable sites are visible in one table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repository → the two npm tarballs | The closing verification is the only point where the claim "what npm publishes is clean" is measured against what npm would actually publish |
| an empty ratchet → a future change | With the scaffolding gone, the guard's standing assertion is the only thing preventing the population from growing back |
| an external tool's path handling → the alias arrangement | The alias explanation is the only warning that the arrangement depends on behaviour this project does not control |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-17 | Repudiation | the ratchet ledger | high | mitigate | An explicit emptiness assertion is added, with a failure message stating that a non-empty ratchet is an exemption list under another name; the machinery is retained so a future regression is pinned by the same mechanism |
| T-51-18 | Spoofing | the guard's non-vacuity on a clean tree | high | mitigate | The three planted controls are re-run after the sweep, and one real citation is planted into a real shipped module and observed to red the guard before being removed |
| T-51-02 | Tampering | the alias explanation and the detect-never-install statement | high | mitigate | The comment-byte budget survives the phase and is retained deliberately; both comments are additionally quoted before and after |
| T-51-19 | Information Disclosure | what the tarballs actually contain | medium | mitigate | The published-tarball validator is run as part of the closing verification, so the phase's central claim is measured against the packed file list rather than inferred from the repository |
| T-51-09 | Tampering | every generated artifact in the package | medium | mitigate | A final build plus the byte-identity guard confirms no plan in this phase hand-edited or failed to regenerate one |
| T-51-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this phase and none is added; the milestone's own out-of-scope table forbids one |
</threat_model>

<verification>
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck` exits 0
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated` exits 0 and reports `fail 0`
- `cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-npm-packages.mjs` exits 0
- The five comment-pinning guards are green in one run
- The ratchet array is empty and asserted empty
- A planted citation in a real shipped module reds the guard, observed and reverted
</verification>

<success_criteria>
- The ledger reached empty and is asserted empty
- The zero assertion is live with no second test to enable
- The comment-byte baseline survives with its retention reason recorded
- Every requirement has recorded evidence, and every unrecoverable site is in one table
</success_criteria>

<output>
Create `.planning/phases/51-planning-vocabulary-out-of-the-shipped-server/51-17-SUMMARY.md` when done.

This summary is the phase's record. It MUST contain: the six numbered evidence
blocks; the consolidated tier-4 site table; the phase-wide comment-character
totals; the guard's final scanned-file count; the planting experiment's exact
failure text; and the statement of what remains for the phase that owns the
evidence-document relocation.
</output>
