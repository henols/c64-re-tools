---
phase: 56-remove-shipped-modules-ts-and-its-embedded-source-scans
plan: 03
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - src/mcp/vice/block-class.test.ts
  - src/mcp/vice/anno-join.test.ts
  - src/mcp/vice/anno-overlap.test.ts
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "block-class.test.ts, anno-join.test.ts and anno-overlap.test.ts carry no import of the doomed scanning module."
    - "Each of the three files runs green on its own and loses only the named source-scanning cases."
    - "npm run typecheck exits 0 at every commit. npm run test:automated is green at the end of this plan."
    - statement: "Every removed case had source TEXT as its entire subject, confirmed by a hand-read of the case body."
      verification: backstop
    - statement: "No surviving case in the three files reads a sibling .ts or .mts and asserts on that text."
      verification: backstop
  artifacts:
    - src/mcp/vice/block-class.test.ts
    - src/mcp/vice/anno-join.test.ts
    - src/mcp/vice/anno-overlap.test.ts
    - .planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-03-SUMMARY.md
  key_links:
    - "anno-overlap.test.ts declares 21 cases statically but reports 36 TAP names, because two loop sites generate case families. The SET diff is the gate, so the gap is expected and harmless."
    - "anno-join.test.ts's setComment refusal case at line 284 is wholly behavioural. RESEARCH Q3 measured it. It must survive untouched."
---

<objective>
Remove the four source-scanning cases in `block-class.test.ts`, `anno-join.test.ts` and
`anno-overlap.test.ts`, and remove each file's import of the doomed module.

Purpose: these three files are mechanical. Each carries one or two wholly structural cases and
no measured mixed case. They are the low-risk expansion of the pattern plan 56-01 proved.

Output: three files without source-text assertions, and their removed case names for D-16.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-CONTEXT.md
@.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-RESEARCH.md
@.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-PATTERNS.md
</context>

<boundaries>
## Hard boundaries. These bind every task in this plan

NEVER `git add`, `git commit`, `git checkout` or `git restore` any of these eight paths. They are
the user's unrelated in-flight edits, modified or untracked in the working tree right now.
`.claude/settings.json`, `src/mcp/vice/anno-bank.ts`, `src/mcp/vice/anno-coverage.ts`,
`src/mcp/vice/anno-enum-gen.ts`, `src/mcp/vice/anno-tools.ts`, `docs/dissambler-workflow.md`,
`docs/vice-mcp-ideas.md`, `setup-claude-ste100.sh`.

Stage EXPLICITLY BY PATH in every commit. Never `git add -A`. Never `git add .`. Never
`git commit -a`.

COLLISION HAZARD. `src/mcp/vice/anno-coverage.ts` is untouchable.
`src/mcp/vice/anno-coverage.test.ts` is in scope for this phase. The two names differ by six
characters. `src/mcp/vice/anno-tools.ts` is untouchable.

All file deletions use `git rm`. No file is truncated or emptied to simulate deletion. This plan
removes no file.

Never run the bare `npm test` glob. It blocks forever on `vice-proxy.test.ts`. Use
`npm run test:automated`, or a per-file `node --test <file>`.

Never pipe a suite run to `tail` or `head`. That reports the pager's exit code and fakes a green
baseline. Redirect to a file. Then read `EXIT=$?` on the same line.
</boundaries>

<cut_rule>
## What qualifies for removal

IN SCOPE: a case whose SUBJECT is the source TEXT of a module. The route does not matter. It may
be the doomed scanning helpers, a raw `readFileSync` of a sibling `.ts` or `.mts`, or a
module-scope `*_SOURCE` constant built from one.

NOT IN SCOPE: a case that reads a non-source artifact, such as `package.json` parsed as JSON, a
JSON data file, a fixture, or a directory listing.

- **D-01.** Whole-case removal is the default.
- **D-02.** A case is exempt only when removal of every source-text assertion still leaves at
  least one assertion that exercises production behaviour. A setup-only remainder goes whole.
- **D-03.** An exempt case is stripped in place, byte-for-byte, and renamed to what it now proves.
- **D-04 (prohibition).** Never remove a case and re-add its behavioural half as a new case.
- **Helper rule.** A module-scope helper, constant or import goes with its cases only when every
  caller is itself being removed. grep the whole file for other callers first.
- **D-10 (prohibition).** Write NO new prose anywhere. The SUMMARY is the one exception.
- **Stale comments in test files stay stale on purpose.** Leave surviving comment prose
  byte-identical, following D-13's reasoning.
- **Stop and report. Never adjust.** If a cut trips another file's pinned assertion, that
  measurement IS the finding.
- **D-17.** Compare SETS, never counts. Never pin a case count in an assertion.
</cut_rule>

<scanner_protocol>
## The D-14 scratch scanner. It narrows the read. It never authorises a cut

Reuse `/tmp/gsd-56-scope-scan/scope-scan.mjs` from plan 56-01 if it is present. Rebuild it from
56-RESEARCH.md § Q2's algorithm if it is absent. In BOTH cases, re-run its planted-fixture proof
and observe GREEN before you trust a hit. It is never committed and never placed under
`scripts/`. Do not list it in `files_modified`.

The scanner is symbol-anchored and has two measured blind spots. So for every file do three
passes. Run the scanner. Then grep for `readFileSync` of a sibling `.ts` or `.mts` and for
module-scope `*_SOURCE` constants. Then skim every surviving case NAME for source-reading prose
such as "STRUCTURAL", "non-vacuity", "imports nothing" or "declares no module-level". Resolve
every candidate to its own paren-matched `test(` boundary before you judge it. Line-proximity
attribution was measured wrong on 8 of 10 cases.
</scanner_protocol>

<!-- planner-discipline-allow: from "./shipped-modules.ts" -->
<!-- planner-discipline-allow: codeOnly -->
<!-- planner-discipline-allow: shippedTsModules -->
<tasks>

<task type="auto">
  <name>Task 1: Cut block-class.test.ts</name>
  <files>src/mcp/vice/block-class.test.ts</files>
  <read_first>
    - src/mcp/vice/block-class.test.ts lines 1 to 40 (imports and module-scope constants), lines
      320 to 400 (the two candidate cases and any helper between them).
    - 56-RESEARCH.md § Q1 (this file's two measured hits, at lines 330 and 389).
  </read_first>
  <action>
    Capture the BEFORE TAP name set to /tmp/56-03-t1-before.set with the same command shape the
    verify block uses for the AFTER set.

    Re-run the scanner's planted-fixture proof and observe GREEN. Run the scanner over the file.
    It reports two hits. Hand-read both and remove them WHOLE under D-01, by verbatim name:
      1. block-class.ts imports nothing census-side, disassembler-side, transport-side or path-translation-side
      2. block-class.ts declares no module-level mutable binding, including a const mutable container

    Run the two blind-spot passes named in the scanner_protocol section. Hand-read any extra
    candidate and apply the cut rule. Record every extra removal with its verbatim name.

    Remove the module-scope helpers, constants and imports whose only callers were removed cases.
    grep the whole file for each identifier before removing it. Remove the doomed module import.

    Stage with git add src/mcp/vice/block-class.test.ts and commit that one path. Run
    git status --porcelain before the commit and confirm the eight untouchable paths are unstaged.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && node --test --test-reporter=tap block-class.test.ts > /tmp/56-03-t1-after.tap 2>&1; echo "EXIT=$?"; grep -aE '^ok [0-9]+ - ' /tmp/56-03-t1-after.tap | sed -E 's/^ok [0-9]+ - //' | sort > /tmp/56-03-t1-after.set; comm -23 /tmp/56-03-t1-before.set /tmp/56-03-t1-after.set > /tmp/56-03-t1-lost.txt; comm -13 /tmp/56-03-t1-before.set /tmp/56-03-t1-after.set > /tmp/56-03-t1-gained.txt; printf 'lost='; wc -l /tmp/56-03-t1-lost.txt; printf 'gained='; wc -l /tmp/56-03-t1-gained.txt; cat /tmp/56-03-t1-lost.txt</automated>
    <fails_when>EXIT is anything but 0. Or gained reports any non-zero count. Or the printed lost list holds a name the task notes did not record as an intended removal. Or the lost list is empty.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && printf 'doomed_import='; grep -ac 'from "./shipped-modules.ts"' block-class.test.ts || true; printf 'doomed_calls='; grep -av '^\s*[*/]' block-class.test.ts | grep -acE '(codeOnly|shippedTsModules|extractCommentSpans|commentByteTotal|shippedScanSurface) *\(' || true; printf 'raw_src_reads='; grep -av '^\s*[*/]' block-class.test.ts | grep -acE 'readFileSync\([^)]*"[a-z0-9.-]+\.(ts|mts)"' || true; printf 'assertions='; grep -ac 'assert\.' block-class.test.ts || true</automated>
    <fails_when>doomed_import is anything but 0. Or doomed_calls is anything but 0. Or raw_src_reads is anything but 0. Or assertions is 0, which would mean the file was left with setup and no assertions.</fails_when>
    <automated>npm --prefix "$(git rev-parse --show-toplevel)/src/mcp/vice" run typecheck > /tmp/56-03-t1-tc.txt 2>&1; echo "EXIT=$?"; tail -20 /tmp/56-03-t1-tc.txt</automated>
    <fails_when>EXIT is anything but 0, or the output names any file under src/mcp/vice.</fails_when>
  </verify>
  <acceptance_criteria>
    - The post-cut TAP name set loses only names the task notes record as intended removals, and
      gains no name.
    - `grep -ac 'from "./shipped-modules.ts"' block-class.test.ts` returns 0.
    - No surviving code line reads a sibling .ts or .mts with readFileSync.
    - `npm run typecheck` exits 0.
    - The commit stages exactly one path, src/mcp/vice/block-class.test.ts.
  </acceptance_criteria>
  <done>block-class.test.ts carries no source-scanning case and no doomed import.</done>
</task>

<task type="auto">
  <name>Task 2: Cut anno-join.test.ts</name>
  <files>src/mcp/vice/anno-join.test.ts</files>
  <read_first>
    - src/mcp/vice/anno-join.test.ts lines 1 to 40 (imports and constants), lines 275 to 300 (the
      behavioural case RESEARCH measured as a keeper), lines 300 to 360 (the helper
      importSpecifiers and the candidate case at line 334).
    - 56-RESEARCH.md § Q1 and § Q3's verdict on the case at line 284.
  </read_first>
  <action>
    Capture the BEFORE TAP name set to /tmp/56-03-t2-before.set.

    Re-run the scanner's planted-fixture proof and observe GREEN. Run the scanner over the file.
    It reports one hit, reached through the module-scope helper importSpecifiers. Hand-read the
    case and remove it WHOLE under D-01, by verbatim name:
      1. the join's module graph (anno-join.ts, memmap-lookup.ts, anno-import.ts) imports nothing under the skills tree, spawns no child process, and references no queue module

    KEEP the case at line 284, a setComment MAX_COMMENT_BYTES refusal. RESEARCH Q3 measured it as
    wholly behavioural with zero reference to any doomed symbol. It must stay byte-identical.

    Run the two blind-spot passes. Hand-read any extra candidate and apply the cut rule.

    Remove the helper importSpecifiers and any other module-scope binding whose only callers were
    removed cases. grep the whole file for each identifier first. Remove the doomed module import.

    Stage with git add src/mcp/vice/anno-join.test.ts and commit that one path. Run
    git status --porcelain before the commit and confirm the eight untouchable paths are unstaged.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && node --test --test-reporter=tap anno-join.test.ts > /tmp/56-03-t2-after.tap 2>&1; echo "EXIT=$?"; grep -aE '^ok [0-9]+ - ' /tmp/56-03-t2-after.tap | sed -E 's/^ok [0-9]+ - //' | sort > /tmp/56-03-t2-after.set; comm -23 /tmp/56-03-t2-before.set /tmp/56-03-t2-after.set > /tmp/56-03-t2-lost.txt; comm -13 /tmp/56-03-t2-before.set /tmp/56-03-t2-after.set > /tmp/56-03-t2-gained.txt; printf 'lost='; wc -l /tmp/56-03-t2-lost.txt; printf 'gained='; wc -l /tmp/56-03-t2-gained.txt; cat /tmp/56-03-t2-lost.txt</automated>
    <fails_when>EXIT is anything but 0. Or gained reports any non-zero count. Or the printed lost list holds a name the task notes did not record as an intended removal. Or the lost list is empty.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && printf 'doomed_import='; grep -ac 'from "./shipped-modules.ts"' anno-join.test.ts || true; printf 'doomed_calls='; grep -av '^\s*[*/]' anno-join.test.ts | grep -acE '(codeOnly|shippedTsModules|extractCommentSpans|commentByteTotal|shippedScanSurface) *\(' || true; printf 'helper_left='; grep -av '^\s*[*/]' anno-join.test.ts | grep -ac 'importSpecifiers' || true; printf 'assertions='; grep -ac 'assert\.' anno-join.test.ts || true</automated>
    <fails_when>doomed_import is anything but 0. Or doomed_calls is anything but 0. Or helper_left is anything but 0. Or assertions is 0.</fails_when>
    <automated>npm --prefix "$(git rev-parse --show-toplevel)/src/mcp/vice" run typecheck > /tmp/56-03-t2-tc.txt 2>&1; echo "EXIT=$?"; tail -20 /tmp/56-03-t2-tc.txt</automated>
    <fails_when>EXIT is anything but 0, or the output names any file under src/mcp/vice.</fails_when>
  </verify>
  <acceptance_criteria>
    - The post-cut TAP name set loses only names the task notes record as intended removals, and
      gains no name.
    - The setComment MAX_COMMENT_BYTES refusal case is present in the post-cut TAP set.
    - `grep -ac 'from "./shipped-modules.ts"' anno-join.test.ts` returns 0.
    - `npm run typecheck` exits 0.
    - The commit stages exactly one path, src/mcp/vice/anno-join.test.ts.
  </acceptance_criteria>
  <done>anno-join.test.ts carries no source-scanning case, no dead helper and no doomed import.</done>
</task>
<task type="auto">
  <name>Task 3: Cut anno-overlap.test.ts and close the plan with the full automated gate</name>
  <files>src/mcp/vice/anno-overlap.test.ts</files>
  <read_first>
    - src/mcp/vice/anno-overlap.test.ts lines 1 to 40 (imports and constants) and lines 1470 to
      1540 (the candidate case and its surroundings).
    - 56-RESEARCH.md § Q1 (this file's one measured hit at line 1489, and the measured note that
      this file's two loop sites generate 36 TAP names from 21 static declarations).
    - 56-CONTEXT.md § D-16 (what the SUMMARY must carry).
  </read_first>
  <action>
    Capture the BEFORE TAP name set to /tmp/56-03-t3-before.set.

    Re-run the scanner's planted-fixture proof and observe GREEN. Run the scanner over the file.
    It reports one hit. Hand-read the case and remove it WHOLE under D-01, by verbatim name:
      1. adjacency, STRUCTURAL: no coalescing, merging or splitter identifier exists anywhere in the store's code

    Expect the TAP name set to hold more names than the file has `test(` declarations. Two loop
    sites generate case families here. Neither loop body touches a doomed symbol, so no family
    needs cutting. The SET diff is the gate, so the gap is expected.

    Run the two blind-spot passes. Hand-read any extra candidate and apply the cut rule.

    Remove the module-scope bindings whose only callers were removed cases. grep the whole file
    for each identifier first. Remove the doomed module import.

    Stage with git add src/mcp/vice/anno-overlap.test.ts and commit that one path. Run
    git status --porcelain before the commit and confirm the eight untouchable paths are unstaged.

    Then run the full automated suite once for the whole plan. The measured floor, taken live on
    2026-09-14 before any Phase 56 edit, is EXIT=0 with fail 0. Compare the outcome as a SET of
    failing names, never as a count. If a file outside this plan turns red, that measurement IS
    the finding. Report it and stop. Do not adjust the tripped assertion.

    Finally write the SUMMARY. It must carry every case name removed in this plan VERBATIM,
    grouped by file, each with a one-line reason it qualified. That is D-16's deliverable and this
    plan's share of it. Counts alone are not acceptable.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && node --test --test-reporter=tap anno-overlap.test.ts > /tmp/56-03-t3-after.tap 2>&1; echo "EXIT=$?"; grep -aE '^ok [0-9]+ - ' /tmp/56-03-t3-after.tap | sed -E 's/^ok [0-9]+ - //' | sort > /tmp/56-03-t3-after.set; comm -23 /tmp/56-03-t3-before.set /tmp/56-03-t3-after.set > /tmp/56-03-t3-lost.txt; comm -13 /tmp/56-03-t3-before.set /tmp/56-03-t3-after.set > /tmp/56-03-t3-gained.txt; printf 'lost='; wc -l /tmp/56-03-t3-lost.txt; printf 'gained='; wc -l /tmp/56-03-t3-gained.txt; cat /tmp/56-03-t3-lost.txt</automated>
    <fails_when>EXIT is anything but 0. Or gained reports any non-zero count. Or the printed lost list holds a name the task notes did not record as an intended removal. Or the lost list is empty.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && printf 'doomed_import='; grep -ac 'from "./shipped-modules.ts"' anno-overlap.test.ts || true; printf 'doomed_calls='; grep -av '^\s*[*/]' anno-overlap.test.ts | grep -acE '(codeOnly|shippedTsModules|extractCommentSpans|commentByteTotal|shippedScanSurface) *\(' || true; printf 'raw_src_reads='; grep -av '^\s*[*/]' anno-overlap.test.ts | grep -acE 'readFileSync\([^)]*"[a-z0-9.-]+\.(ts|mts)"' || true</automated>
    <fails_when>doomed_import is anything but 0. Or doomed_calls is anything but 0. Or raw_src_reads is anything but 0.</fails_when>
    <automated>npm --prefix "$(git rev-parse --show-toplevel)/src/mcp/vice" run typecheck > /tmp/56-03-t3-tc.txt 2>&1; echo "EXIT=$?"; tail -20 /tmp/56-03-t3-tc.txt</automated>
    <fails_when>EXIT is anything but 0, or the output names any file under src/mcp/vice.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && npm run test:automated > /tmp/56-03-t3-gate.txt 2>&1; echo "EXIT=$?"; grep -aE '^.? ?(tests|pass|fail|skipped) [0-9]+$' /tmp/56-03-t3-gate.txt; grep -aE '^not ok |✖ ' /tmp/56-03-t3-gate.txt || true</automated>
    <fails_when>EXIT is anything but 0. Or the summary block reports a fail count other than 0. Or any failing-test line is printed. Or the summary block prints nothing, which means the grep anchors missed the reporter format and the run was not read.</fails_when>
  </verify>
  <acceptance_criteria>
    - The post-cut TAP name set loses only names the task notes record as intended removals, and
      gains no name.
    - `grep -ac 'from "./shipped-modules.ts"' anno-overlap.test.ts` returns 0.
    - `npm run typecheck` exits 0 and `npm run test:automated` exits 0 with fail 0.
    - The SUMMARY lists every case removed in this plan verbatim, grouped by file, each with a
      one-line reason.
    - The commit stages exactly one path, src/mcp/vice/anno-overlap.test.ts.
  </acceptance_criteria>
  <done>All three files in this plan are cut and committed by explicit path. The suite is green and
  the removed-name list exists in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer working tree to git index | This plan commits in a tree holding eight unrelated in-flight edits owned by the user. |

## STRIDE Threat Register

ASVS level 1. Block on high. RESEARCH assessed this phase as touching no authentication, no
session, no access control, no input validation and no cryptography surface. It removes test code
in a private developer-tool tree. That assessment is recorded honestly rather than padded. The one
genuine high-severity hazard is destructive-action, not attacker-driven.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-01 | Tampering | `git add` in every committing task | high | mitigate | Stage by explicit path only. `git add -A`, `git add .` and `git commit -a` are prohibited by the boundaries section. Confirm with `git status --porcelain` before each commit. |
| T-56-02 | Tampering | the cut, against test coverage integrity | high | mitigate | The per-file TAP name SET diff gates every task. A name may disappear only when the task notes recorded it. No name may appear. |
| T-56-SC | Tampering | npm, pip and cargo installs | low | accept | This plan runs no package-manager install and adds no dependency. RESEARCH § "Package Legitimacy Audit" records the same finding. No package-legitimacy checkpoint is owed. |
</threat_model>

<verification>
1. `npm --prefix src/mcp/vice run typecheck` exits 0.
2. `cd src/mcp/vice && npm run test:automated` exits 0 and reports fail 0.
3. `grep -c 'from "./shipped-modules.ts"' src/mcp/vice/block-class.test.ts src/mcp/vice/anno-join.test.ts src/mcp/vice/anno-overlap.test.ts` returns 0 for each file.
4. Each per-file TAP name-set diff lost only the recorded names and gained none.
5. `git log --name-only` for this plan's commits names only the three files above and the SUMMARY.
</verification>

<success_criteria>
- SC-1 progress: three more of the seventeen importing files no longer name the doomed module.
- SC-2: every non-scanning case in the three files survives, proved by the TAP name-set diffs.
- SC-3: the automated suite and typecheck both exit 0.
- SC-4: none of the three files is empty, and none is left with setup and no assertions.
</success_criteria>

## Artifacts this phase produces

This phase's net effect is DELETION. It adds no production symbol, no module and no test helper.
A drift-verification pass must not read an absent symbol as an unplanned omission.

What this plan produces:

- .planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-03-SUMMARY.md
- /tmp/56-03-t1-lost.txt, /tmp/56-03-t2-lost.txt and /tmp/56-03-t3-lost.txt, the measured TAP
  name-set diffs. These are run evidence and are not committed.

<output>
Create `.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-03-SUMMARY.md` when done.
</output>

