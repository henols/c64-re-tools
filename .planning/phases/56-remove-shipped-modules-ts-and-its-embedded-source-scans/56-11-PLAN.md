---
phase: 56-remove-shipped-modules-ts-and-its-embedded-source-scans
plan: 11
type: execute
wave: 4
depends_on: ["56-01", "56-02", "56-03", "56-04", "56-05", "56-06", "56-07", "56-08", "56-09", "56-10"]
files_modified:
  - src/mcp/vice/shipped-modules.ts
  - src/mcp/vice/shipped-modules.test.ts
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "src/mcp/vice/shipped-modules.ts no longer exists in the repository."
    - "src/mcp/vice/shipped-modules.test.ts no longer exists in the repository."
    - "No file in src/mcp/vice imports the removed module."
    - "Both removals were made with git rm. Neither file was truncated or emptied."
    - "npm run typecheck exits 0 and npm run test:automated is green with fail 0."
    - "anno-seam.test.ts's WR-25 behavioural case is present in the final TAP set."
    - "No touched test file is empty, and none is left with setup and no assertions."
    - statement: "The phase SUMMARY reconciles the test-count drop case by case, with every removed case name carried verbatim, so a silently broken file cannot hide inside the expected decrease."
      verification: backstop
    - statement: "The embedded-case removals and the whole-file removal of the doomed module's own test are reported as two separate numbers."
      verification: backstop
  artifacts:
    - .planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-11-SUMMARY.md
  key_links:
    - "Every one of the sixteen regular test files must have lost its import before this plan removes the module, or typecheck breaks immediately. That is the phase's one hard ordering constraint."
    - "The doomed module does not appear in package.json files[], so its removal owes no manifest edit. anno-seam.test.ts's surviving manifest-completeness case must still pass."
---

<objective>
Remove `shipped-modules.ts` and `shipped-modules.test.ts` together, run the phase gate, and write
the SUMMARY that reconciles the whole phase case by case.

Purpose: this is the last plan of the phase. It converts sixteen independent cuts into the one
outcome the phase exists for, and it produces the only artifact in which a reader can check
success criterion 3's claim without re-running anything.

Output: two removed files, a green suite, and a SUMMARY carrying every removed case name
verbatim.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-CONTEXT.md
@.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-RESEARCH.md
@.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-PATTERNS.md
</context>

<boundaries>
## Hard boundaries. These bind every task in this plan

NEVER `git add`, `git commit`, `git checkout` or `git restore` any of these eight paths. They are
the user's unrelated in-flight edits, modified or untracked in the working tree right now.
`.claude/settings.json`, `src/mcp/vice/anno-bank.ts`, `src/mcp/vice/anno-coverage.ts`,
`src/mcp/vice/anno-enum-gen.ts`, `src/mcp/vice/anno-tools.ts`, `docs/dissambler-workflow.md`,
`docs/vice-mcp-ideas.md`, `setup-claude-ste100.sh`.

Stage EXPLICITLY BY PATH in every commit. Never `git add -A`. Never `git add .`. Never
`git commit -a`.

Both removals use `git rm`. Neither file is truncated or emptied to simulate removal.

Never run the bare `npm test` glob. It blocks forever on `vice-proxy.test.ts`. Use
`npm run test:automated`, or a per-file `node --test <file>`.

Never pipe a suite run to `tail` or `head`. That reports the pager's exit code and fakes a green
baseline. Redirect to a file. Then read `EXIT=$?` on the same line.
</boundaries>

<non_goals>
## What this plan deliberately does NOT do

- **D-05.** Success criterion 2 stands as written. Phase 54's matching wording also stands. This
  phase owes no ROADMAP edit for either.
- **D-08.** `anno-seam.test.ts` keeps its name, though the name now refers to a removed scan. Do
  not `git mv` it.
- **D-13.** Two test files name the removed module in a comment with no import, at
  `vice-proxy.test.ts` line 3488 and `hostpath-consumers.test.ts` line 624. Both stay stale ON
  PURPOSE. The six `.planning/codebase/*.md` maps that cite the removed scanners also stay stale.
  They are regenerated by the codebase-mapping command. Do not touch any of them.
- **D-12.** A stale enforcement claim outside D-11's five named modules may survive. That is
  accepted and is not a gap to close here.
- **D-10.** Write NO new prose in the source tree. No note. No seed. No successor document. No
  replacement guard. No lint rule. No weaker assertion. The SUMMARY is the one exception, and it
  lives under `.planning/`.
- The modules left deliberately untested by the preceding rounds are NOT repaired here. The
  removed module joins that list by being removed.
- `src/mcp/vice/package.json` needs no edit. The removed module was never in `files[]`.
</non_goals>

<!-- planner-discipline-allow: from "./shipped-modules.ts" -->
<!-- planner-discipline-allow: shipped-modules.ts -->
<!-- planner-discipline-allow: codeOnly -->
<!-- planner-discipline-allow: shippedTsModules -->
<tasks>

<task type="auto">
  <name>Task 1: Confirm zero remaining importers, then git rm both files</name>
  <files>src/mcp/vice/shipped-modules.ts, src/mcp/vice/shipped-modules.test.ts</files>
  <read_first>
    - The SUMMARY of every earlier plan in this phase, at
      `.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-0*-SUMMARY.md`
      and `56-10-SUMMARY.md`. They carry the per-file removed-name lists this plan assembles.
    - 56-CONTEXT.md § D-09 (why the module's own test goes wholesale, and why its removal is
      reported separately).
  </read_first>
  <precondition>Every one of the sixteen regular test files has already lost its import of the
  doomed module, delivered by plans 56-01 through 56-10. A repository-wide grep for the import
  specifier returns only the module's own test file.</precondition>
  <reversibility rating="costly">Restoring the module and its test means recovering two whole
  files from git history and re-justifying every embedded case this phase removed across sixteen
  files. The verbatim removed-name list in the SUMMARY is what keeps this costly rather than
  one-way.</reversibility>
  <action>
    First, the preflight. Run a repository-wide grep for the import specifier across
    src/mcp/vice. Exactly one file may still carry it: shipped-modules.test.ts, the module's own
    test. If ANY other file still carries it, stop. That is a finding: an earlier plan did not
    land. Report which file and stop. Do not remove the module while a live importer exists.

    Then remove both files with git rm, in one command naming both paths explicitly:
    src/mcp/vice/shipped-modules.ts and src/mcp/vice/shipped-modules.test.ts. D-09 removes them
    together. The test holds 16 cases, all of them testing the removed module, and it is in the
    automated set.

    Neither file is truncated or emptied. git rm is the only route.

    Then commit those two paths. Run git status --porcelain before the commit and confirm the
    eight untouchable paths are unstaged.

    Record, for the SUMMARY, the number of cases the removed test file held and the fact that this
    number is reported SEPARATELY from the embedded-case removals. Success criterion 2 asks for
    both numbers.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && printf 'module_exists='; test -e shipped-modules.ts && echo yes || echo no; printf 'test_exists='; test -e shipped-modules.test.ts && echo yes || echo no; printf 'importers='; grep -rac 'from "./shipped-modules.ts"' --include='*.ts' --include='*.mts' . | grep -av ':0$' | wc -l</automated>
    <fails_when>module_exists is yes. Or test_exists is yes. Or importers is anything but 0.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)" && git show --name-status --format='%s' HEAD; printf 'untouchable_staged='; git diff --cached --name-only | grep -acE '(settings\.json|anno-bank\.ts|anno-coverage\.ts$|anno-enum-gen\.ts|anno-tools\.ts|dissambler-workflow\.md|vice-mcp-ideas\.md|setup-claude-ste100\.sh)' || true</automated>
    <fails_when>The commit's file list does not show a D status for both src/mcp/vice/shipped-modules.ts and src/mcp/vice/shipped-modules.test.ts. Or it names any other path. Or untouchable_staged is anything but 0.</fails_when>
    <automated>npm --prefix "$(git rev-parse --show-toplevel)/src/mcp/vice" run typecheck > /tmp/56-11-t1-tc.txt 2>&1; echo "EXIT=$?"; tail -20 /tmp/56-11-t1-tc.txt</automated>
    <fails_when>EXIT is anything but 0, or the output names any file under src/mcp/vice.</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -e src/mcp/vice/shipped-modules.ts` is false.
    - `test -e src/mcp/vice/shipped-modules.test.ts` is false.
    - `git show --name-status HEAD` shows status D for both paths, which proves `git rm` was used
      rather than truncation.
    - No file under src/mcp/vice imports the removed module.
    - `npm run typecheck` exits 0.
    - The commit stages exactly the two removed paths.
  </acceptance_criteria>
  <done>Both files are gone from the repository, removed with git rm, and typecheck still exits
  0.</done>
</task>

<task type="auto">
  <name>Task 2: Run the phase gate and check all five success criteria</name>
  <files>.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-11-SUMMARY.md</files>
  <read_first>
    - 56-VALIDATION.md § "Measured Baseline" and § "Manual-Only Verifications".
    - The per-plan SUMMARY files, for the removed-name lists.
  </read_first>
  <action>
    Run the full automated suite once. The measured floor, taken live on 2026-09-14 before any
    Phase 56 edit, is EXIT=0, fail 0, skipped 9. Compare the outcome as a SET of failing names,
    never as a count. If any test is red, that measurement IS the finding. Report it and stop.

    Then check the five success criteria one at a time and record the evidence for each.

    SC-1. The removed module no longer exists and nothing imports it. Evidence: the two file
    existence checks and the repository-wide importer grep from task 1.

    SC-2. No real coverage was removed as collateral. Evidence: every plan's per-file TAP name-set
    diff shows only names that plan recorded. Assemble the two numbers success criterion 2 asks
    for. Number one is the count of embedded cases removed across the sixteen surviving files.
    Number two is the count of cases in the removed test file. Report them separately, never as
    one total.

    SC-3. The suite is green and typecheck exits 0. Evidence: this task's two runs. Also state the
    test-count drop as a number, and reconcile it case by case against the assembled verbatim
    name list. A count that does not reconcile is a finding, not a rounding error.

    SC-4. No test file is left empty, and none is left with only setup and no assertions. Evidence:
    for each of the sixteen surviving files, its post-cut TAP run reports at least one case, and
    the file contains at least one assertion call.

    SC-5. The WR-25 behavioural case survives. Evidence: the named case is present in
    anno-seam.test.ts's final TAP set.

    Record each criterion's verdict and its evidence. Do not mark a criterion met on the strength
    of a green suite alone. A green suite is consistent with a silently over-broad cut, which is
    the exact failure success criterion 2 exists to catch.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && npm run test:automated > /tmp/56-11-t2-gate.txt 2>&1; echo "EXIT=$?"; grep -aE '^.? ?(tests|pass|fail|skipped) [0-9]+$' /tmp/56-11-t2-gate.txt; grep -aE '^not ok |✖ ' /tmp/56-11-t2-gate.txt || true</automated>
    <fails_when>EXIT is anything but 0. Or the summary block reports a fail count other than 0. Or any failing-test line is printed. Or the summary block prints nothing, which means the grep anchors missed the reporter format and the run was not read.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && node --test --test-reporter=tap anno-seam.test.ts > /tmp/56-11-t2-seam.tap 2>&1; echo "EXIT=$?"; grep -aE '^ok [0-9]+ - ' /tmp/56-11-t2-seam.tap | sed -E 's/^ok [0-9]+ - //'</automated>
    <fails_when>EXIT is anything but 0. Or the printed name list does not hold the WR-25 behavioural refusal case. Or it does not hold the package.json files[] completeness case. Or it holds any third name.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && for f in anno-coverage.test.ts anno-derive.test.ts anno-export-asm.test.ts anno-graphics.test.ts anno-index.test.ts anno-join.test.ts anno-overlap.test.ts anno-seam.test.ts anno-store.test.ts anno-types.test.ts block-class.test.ts capture-predicate.test.ts evid-report-keys.test.ts prg-image.test.ts stock-dispatch.test.ts vsf-slice.test.ts; do printf '%s asserts=' "$f"; grep -ac 'assert\.' "$f" || true; done</automated>
    <fails_when>Any listed file reports asserts=0, which means it was left with setup and no assertions. Or any listed file is missing, which means a file was removed that this phase keeps.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && printf 'residual_mentions='; grep -ral 'shipped-modules' --include='*.ts' --include='*.mts' --include='*.mjs' . | paste -sd' '</automated>
    <fails_when>The printed path list names any file other than ./vice-proxy.test.ts and ./hostpath-consumers.test.ts. Those two are comment-only mentions that D-13 leaves stale on purpose.</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm run test:automated` exits 0 and reports fail 0.
    - anno-seam.test.ts's final TAP set holds exactly the two D-07 survivor names.
    - Every one of the sixteen surviving files reports at least one assertion call.
    - The only remaining textual mentions of the removed module are the two comment-only ones
      D-13 names.
    - Each of SC-1 through SC-5 has a recorded verdict with its evidence.
    - The embedded-case removal count and the removed test file's case count are recorded as two
      separate numbers.
  </acceptance_criteria>
  <done>All five success criteria have a recorded verdict backed by a measured artifact, and the
  suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Write the D-16 phase SUMMARY with every removed name verbatim</name>
  <files>.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-11-SUMMARY.md</files>
  <read_first>
    - Every earlier SUMMARY in this phase directory. They carry the per-file verbatim name lists.
    - 56-CONTEXT.md § D-16, which states what the SUMMARY must carry and why a counts-only
      reconciliation was rejected.
  </read_first>
  <action>
    Write the phase SUMMARY. It is long on purpose. It is the only form in which a reader can
    check success criterion 3's claim without re-running anything.

    It must carry these sections.

    One. Every removed case name VERBATIM, grouped by file, each with a one-line reason it
    qualified. The population is roughly seventy embedded cases across sixteen files, plus the
    sixteen in the removed module's own test. A name that appears only as a count is not
    acceptable.

    Two. Every D-03 rename, with both the old name and the new name, and one line saying what the
    case now proves.

    Three. Every D-02 exemption, naming the case and the assertions that survived.

    Four. Every candidate that a hand-read CLEARED, with one line saying why the scanner or the
    line-proximity attribution was a false positive. These are as load-bearing as the removals.
    They are the record that the cut was not over-broad.

    Five. The two numbers, reported separately: embedded cases removed across the sixteen
    surviving files, and cases removed with the module's own test file.

    Six. The reconciliation. State the suite's test-count drop as a number. Reconcile it against
    the verbatim list, case by case. If the two do not agree, say so plainly and stop rather than
    round.

    Seven. The five repaired production modules from plan 56-10, with the clause removed from
    each.

    Eight. What is left stale on purpose, per D-13, and the accepted gap, per D-12.

    Nine. What is now deliberately untested as a side effect, following the precedent of the two
    preceding rounds. The removed module joins the list of modules the preceding rounds left
    untested.

    Write no new prose in the source tree. D-10 allows this SUMMARY and nothing else.

    Commit the SUMMARY by explicit path. Run git status --porcelain before the commit and confirm
    the eight untouchable paths are unstaged.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && S=.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-11-SUMMARY.md; printf 'lines='; wc -l "$S"; printf 'file_headings='; grep -ac '\.test\.ts' "$S" || true; printf 'two_numbers='; grep -aci 'separately\|two numbers' "$S" || true; printf 'cleared_section='; grep -aci 'false positive\|cleared' "$S" || true</automated>
    <fails_when>file_headings is less than 16, which means at least one touched file has no section. Or two_numbers is 0, which means the two counts were merged. Or cleared_section is 0, which means the hand-read clearances were not recorded.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)" && git show --name-only --format='%s' HEAD; printf 'untouchable_staged='; git diff --cached --name-only | grep -acE '(settings\.json|anno-bank\.ts|anno-coverage\.ts$|anno-enum-gen\.ts|anno-tools\.ts|dissambler-workflow\.md|vice-mcp-ideas\.md|setup-claude-ste100\.sh)' || true</automated>
    <fails_when>The commit's file list names any path outside .planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/. Or untouchable_staged is anything but 0.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && npm run test:automated > /tmp/56-11-t3-gate.txt 2>&1; echo "EXIT=$?"; grep -aE '^.? ?(tests|pass|fail|skipped) [0-9]+$' /tmp/56-11-t3-gate.txt; grep -aE '^not ok |✖ ' /tmp/56-11-t3-gate.txt || true</automated>
    <fails_when>EXIT is anything but 0. Or the summary block reports a fail count other than 0. Or any failing-test line is printed. Or the summary block prints nothing, which means the grep anchors missed the reporter format and the run was not read.</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY carries a section per touched file, each holding verbatim removed case names
      with a one-line reason each.
    - It records every D-03 rename with both names, and every D-02 exemption.
    - It records every hand-read clearance, so the record shows the cut was not over-broad.
    - It reports the embedded-case count and the removed test file's case count separately.
    - It reconciles the suite's test-count drop against the verbatim list.
    - It names the five repaired production modules and the clause removed from each.
    - It names what is left stale on purpose and the accepted bounded gap.
    - The commit stages only paths inside this phase's planning directory.
  </acceptance_criteria>
  <done>The phase SUMMARY exists and a reader can check success criterion 3 from it alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer working tree to git index | This plan commits in a tree holding eight unrelated in-flight edits owned by the user. |
| repository to removed file | This plan is the only one in the phase that removes a file. A removal made while a live importer exists breaks typecheck for every later reader. |

## STRIDE Threat Register

ASVS level 1. Block on high. RESEARCH assessed this phase as touching no authentication, no
session, no access control, no input validation and no cryptography surface. It removes test code
in a private developer-tool tree. That assessment is recorded honestly rather than padded. The one
genuine high-severity hazard is destructive-action, not attacker-driven.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-01 | Tampering | `git add` and `git rm` in every committing task | high | mitigate | Stage by explicit path only. Each task reads `git show --name-only` after the commit and greps `git diff --cached` for every untouchable path. |
| T-56-09 | Denial of service | removing the module while an importer survives | high | mitigate | Task 1 runs a repository-wide importer grep as a preflight and stops when any file other than the module's own test still imports it. |
| T-56-10 | Repudiation | a reconciliation that hides a broken file inside an expected decrease | high | mitigate | The SUMMARY carries every removed name verbatim and reconciles the drop case by case. A counts-only reconciliation was rejected by success criterion 3's own wording. |
| T-56-SC | Tampering | npm, pip and cargo installs | low | accept | This plan runs no package-manager install and adds no dependency. It edits no `dependencies` or `devDependencies` entry, and the removed module was never in `files[]`. RESEARCH § "Package Legitimacy Audit" records the same finding. No package-legitimacy checkpoint is owed. |
</threat_model>

<verification>
1. `test -e src/mcp/vice/shipped-modules.ts` is false and `test -e src/mcp/vice/shipped-modules.test.ts` is false.
2. `git show --name-status` for this plan's removal commit shows status D for both paths.
3. `grep -rl 'shipped-modules' src/mcp/vice --include='*.ts' --include='*.mts' --include='*.mjs'`
   names only `vice-proxy.test.ts` and `hostpath-consumers.test.ts`, the two comment-only mentions
   D-13 leaves stale.
4. `npm --prefix src/mcp/vice run typecheck` exits 0.
5. `cd src/mcp/vice && npm run test:automated` exits 0 and reports fail 0.
6. `node --test --test-reporter=tap src/mcp/vice/anno-seam.test.ts` reports exactly the two D-07
   survivor names, one of which is the WR-25 behavioural refusal.
7. The phase SUMMARY reconciles the test-count drop case by case against a verbatim name list.
</verification>

<success_criteria>
- SC-1: `src/mcp/vice/shipped-modules.ts` no longer exists and nothing imports it.
- SC-2: no real coverage was removed as collateral, proved by every plan's TAP name-set diff, with
  the embedded-case count and the whole-file count reported separately.
- SC-3: `npm run test:automated` is green and `npm run typecheck` exits 0, with the test-count
  drop stated and reconciled case by case.
- SC-4: no test file is empty, and none is left with setup and no assertions.
- SC-5: `anno-seam.test.ts`'s WR-25 behavioural case survives.
</success_criteria>

## Artifacts this phase produces

This phase's net effect is DELETION. It adds no production symbol, no module and no test helper.
A drift-verification pass must not read an absent symbol as an unplanned omission. Two files stop
existing, and sixteen files lose cases.

What this plan produces:

- .planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-11-SUMMARY.md,
  the phase-level record carrying every removed case name verbatim, both counts, the
  reconciliation, the renames, the exemptions and the hand-read clearances.
- /tmp/56-11-t2-gate.txt and /tmp/56-11-t2-seam.tap, the final gate evidence. These are run
  evidence and are not committed.

<output>
Create `.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-11-SUMMARY.md` when done.
</output>

