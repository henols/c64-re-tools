---
phase: 56-remove-shipped-modules-ts-and-its-embedded-source-scans
plan: 02
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - src/mcp/vice/anno-seam.test.ts
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "anno-seam.test.ts holds exactly two cases: the package.json files[] completeness case and the WR-25 behavioural refusal case."
    - "Both survivors are byte-identical to their pre-cut form. Nothing moved between files."
    - "anno-seam.test.ts carries no import of the doomed scanning module."
    - "The file is neither empty nor left with setup and no assertions."
    - "npm run typecheck exits 0 and npm run test:automated is green at the end of this plan."
    - statement: "Each of the 21 removed cases was hand-read against the file's own reasoning before removal, not accepted from a scanner hit."
      verification: backstop
  artifacts:
    - src/mcp/vice/anno-seam.test.ts
    - .planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-02-SUMMARY.md
  key_links:
    - "The declared list TEST_FILES_NAMING_SQLITE inside this file names anno-derive.test.ts. Plan 56-10 removes anno-derive's cases that spell that specifier. 56-10 therefore runs AFTER this plan, or its suite run turns red."
    - "The WR-25 survivor is success criterion 5. It is the only behavioural case in the file and anno-confinement.test.ts does not cover it."
---

<objective>
Cut 21 of `anno-seam.test.ts`'s 23 cases and keep the file.

Purpose: D-06 and D-07 already decided this file case by case. Twenty-one cases scan source text
or plant synthetic sources for a text scan. Two do not. Keeping the file means both survivors
stay byte-for-byte where they are, so no transcription slip is possible.

Output: a 2-case `anno-seam.test.ts` with no doomed import, and the verbatim list of the 21
removed names for D-16.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-CONTEXT.md
@.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-RESEARCH.md
@.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-PATTERNS.md
</context>

<boundaries>
## Hard boundaries. These bind every task in this plan

NEVER `git add`, `git commit`, `git checkout` or `git restore` any of these eight paths. They are
the user's unrelated in-flight edits, modified or untracked in the working tree right now.
`.claude/settings.json`, `src/mcp/vice/anno-bank.ts`, `src/mcp/vice/anno-coverage.ts`,
`src/mcp/vice/anno-enum-gen.ts`, `src/mcp/vice/anno-tools.ts`, `docs/dissambler-workflow.md`,
`docs/vice-mcp-ideas.md`, `setup-claude-ste100.sh`.

Stage EXPLICITLY BY PATH in every commit. Never `git add -A`. Never `git add .`. Never
`git commit -a`.

COLLISION HAZARD. `src/mcp/vice/anno-coverage.ts` is untouchable.
`src/mcp/vice/anno-coverage.test.ts` is in scope for this phase. The two names differ by six
characters. `src/mcp/vice/anno-tools.ts` is untouchable.

All file deletions use `git rm`. No file is truncated or emptied to simulate deletion. This plan
removes no file. D-06 keeps `anno-seam.test.ts`. D-08 also keeps its name, so do not `git mv` it.

Never run the bare `npm test` glob. It blocks forever on `vice-proxy.test.ts`. Use
`npm run test:automated`, or a per-file `node --test <file>`.

Never pipe a suite run to `tail` or `head`. That reports the pager's exit code and fakes a green
baseline. Redirect to a file. Then read `EXIT=$?` on the same line.
</boundaries>

<cut_rule>
## What qualifies for removal

IN SCOPE: a case whose SUBJECT is the source TEXT of a module. The route does not matter.

NOT IN SCOPE: a case that reads a non-source artifact, such as `package.json` parsed as JSON or a
directory listing. That distinction is exactly why line 238 survives under D-07.

- **D-01.** Whole-case removal is the default.
- **D-02.** A case is exempt only when removal of every source-text assertion still leaves at
  least one assertion that exercises production behaviour.
- **D-03.** An exempt case is stripped in place, byte-for-byte, and renamed to what it now proves.
- **D-04 (prohibition).** Never remove a case and re-add its behavioural half as a new case.
- **Helper rule.** A module-scope helper or constant goes with its cases only when every caller is
  itself being removed. grep the whole file for other callers first.
- **D-10 (prohibition).** Write NO new prose anywhere. The SUMMARY is the one exception.
- **Stale comments stay stale on purpose.** Leave surviving comment prose byte-identical.
- **Stop and report. Never adjust.** If a cut trips another file's pinned assertion, that
  measurement IS the finding.
- **D-17.** Compare SETS, never counts. Never pin a case count in an assertion.
</cut_rule>

<scanner_protocol>
## The D-14 scratch scanner. It narrows the read. It never authorises a cut

Reuse `/tmp/gsd-56-scope-scan/scope-scan.mjs` from plan 56-01 if it is present. Rebuild it from
56-RESEARCH.md § Q2's algorithm if it is absent. In BOTH cases, re-run its planted-fixture proof
and observe GREEN before you trust a hit. It is never committed and never placed under
`scripts/`. Do not list it in `files_modified`.

For THIS file the scanner is explicitly NOT authoritative. RESEARCH § Q2 measured that it finds
only 15 hits here, because one case reaches a doomed symbol through TWO levels of helper, and six
more scan text through a raw route. D-06 and D-07's locked 21-of-23 judgement is authoritative
over any fresh scan. Use the scanner only as a cross-check.
</scanner_protocol>

<!-- planner-discipline-allow: from "./shipped-modules.ts" -->
<!-- planner-discipline-allow: codeOnly -->
<!-- planner-discipline-allow: shippedTsModules -->
<tasks>

<task type="auto">
  <name>Task 1: Remove the nineteen scanning cases between line 146 and line 661</name>
  <files>src/mcp/vice/anno-seam.test.ts</files>
  <read_first>
    - src/mcp/vice/anno-seam.test.ts lines 1 to 145 (header, imports, module-scope constants and
      the three specifier-scan helpers), then lines 146 to 700 (the nineteen cases and the
      helpers between them).
    - 56-CONTEXT.md § "anno-seam.test.ts -- two survivors of twenty-three" (D-06, D-07, D-08).
    - 56-RESEARCH.md § Q2's two-level-indirection finding for this exact file.
  </read_first>
  <action>
    Capture the BEFORE TAP name set to /tmp/56-02-t1-before.set. Use the same command shape the
    verify block below uses for the AFTER set.

    Remove these nineteen cases WHOLE, by verbatim name. Hand-read each one first and confirm its
    subject is source text or a planted synthetic source for a text scan.
      1. node:sqlite is named by exactly one module of the shipped module set (STORE-07)
      2. planted violation, route (a): a single-line static import is reported by the same predicate the real scan uses
      3. planted violation, route (b): a multi-line static import whose keyword, binding and specifier land on different lines is reported
      4. planted violation, route (c): a dynamic import() is reported
      5. planted violation, route (d): process.getBuiltinModule -- the route a two-regex import detector cannot see -- is reported
      6. negative control: a source whose ONLY mention of the specifier is inside a // comment is NOT reported
      7. non-vacuity: the scanned shipped module set is non-empty and contains all three new modules
      8. the seam never touches SQLite's extension-loading surface
      9. the seam declares no module-level mutable binding, including a const bound to a mutable container
      10. no shipped module other than the seam names ANY of the declared seam-private exports
      11. the seam-private export scan is NON-VACUOUS over the code this area added: all three staging transitions are present in the seam's own stripped source
      12. idempotency: re-running the scan over an unchanged tree yields the identical one-element importer list
      13. the revision compare-and-swap is structurally intact: begin immediate, an UPDATE guarded on the current revision, and a changes count that must equal 1
      14. the seam contains exactly one commit statement, so the single planted-violation site is unique
      15. the commit-statement matcher counts all six of SQLite's spellings, so neither a synonym nor a trailing semicolon can hide a second commit site
      16. the commit-statement matcher counts a commit sharing an exec() with a preceding statement, which the whole-literal matcher counted as zero
      17. the commit-statement matcher counts neither commit-ish identifiers, nor an exec() taking a bare identifier, nor user-facing prose about a commit (28-18 P1)
      18. node:sqlite is bounded in the TEST tree too: the set of test files naming it is a DECLARED list
      19. non-vacuity of the test-tree scan: a planted test file naming the specifier IS reported by the same predicate

    Keep the case at line 238, named: package.json files[] ships every anno-* production module on
    disk and no anno-prefixed test file or test-only helper. It reads package.json as JSON, a
    directory listing, and a local three-element constant. It never scans source text. D-07 keeps
    it. It must stay byte-identical.

    Then remove the module-scope bindings whose only callers were removed cases. The measured
    candidates are the helpers namesNodeSqlite at line 117, stripForSpecifierScan at line 123,
    sqliteImporters at line 136, seamSource at line 298, commitStatements at line 436 and
    testFilesNamingSqlite at line 631, plus the constants THE_ONE_SEAM at line 28 and
    TEST_FILES_NAMING_SQLITE at line 82. Before each removal, grep the whole file for that
    identifier. Confirm the only remaining code-line matches sat inside removed cases.

    KEEP the constant NEW_SHIPPED_MODULES at line 31 and the HERE constant at line 25. The
    surviving line-238 case uses both.

    Do not touch the doomed import at line 21 in this task. Task 2 removes it after the last two
    cases go. The file will not typecheck cleanly between the two tasks only if an import becomes
    unused, which is not an error under this tsconfig, so run typecheck anyway and expect 0.

    Stage with git add src/mcp/vice/anno-seam.test.ts and commit that one path. Run
    git status --porcelain before the commit and confirm the eight untouchable paths are unstaged.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && node --test --test-reporter=tap anno-seam.test.ts > /tmp/56-02-t1-after.tap 2>&1; echo "EXIT=$?"; grep -aE '^ok [0-9]+ - ' /tmp/56-02-t1-after.tap | sed -E 's/^ok [0-9]+ - //' | sort > /tmp/56-02-t1-after.set; comm -23 /tmp/56-02-t1-before.set /tmp/56-02-t1-after.set > /tmp/56-02-t1-lost.txt; comm -13 /tmp/56-02-t1-before.set /tmp/56-02-t1-after.set > /tmp/56-02-t1-gained.txt; printf 'lost='; wc -l /tmp/56-02-t1-lost.txt; printf 'gained='; wc -l /tmp/56-02-t1-gained.txt; cat /tmp/56-02-t1-lost.txt</automated>
    <fails_when>EXIT is anything but 0. Or gained reports any non-zero count. Or the printed lost list holds a name that is not one of the nineteen named in the action. Or the lost list holds fewer than nineteen names.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && printf 'files_case='; grep -ac 'package.json files\[\] ships every anno-' anno-seam.test.ts || true; printf 'wr25_case='; grep -ac 'WR-25: the guard itself exists' anno-seam.test.ts || true; printf 'dead_helpers='; grep -av '^\s*[*/]' anno-seam.test.ts | grep -acE '(namesNodeSqlite|stripForSpecifierScan|sqliteImporters|seamSource|commitStatements|testFilesNamingSqlite|THE_ONE_SEAM|TEST_FILES_NAMING_SQLITE)' || true</automated>
    <fails_when>files_case is anything but 1. Or wr25_case is anything but 1. Or dead_helpers is anything but 0.</fails_when>
    <automated>npm --prefix "$(git rev-parse --show-toplevel)/src/mcp/vice" run typecheck > /tmp/56-02-t1-tc.txt 2>&1; echo "EXIT=$?"; tail -20 /tmp/56-02-t1-tc.txt</automated>
    <fails_when>EXIT is anything but 0, or the output names any file under src/mcp/vice.</fails_when>
  </verify>
  <acceptance_criteria>
    - The post-cut TAP name set loses exactly the nineteen names listed in the action, and no
      other name.
    - The post-cut TAP name set gains no name.
    - The two survivor names are present in the post-cut TAP set.
    - No dead helper or constant from the removal list remains in a code line.
    - `npm run typecheck` exits 0.
    - The commit stages exactly one path, src/mcp/vice/anno-seam.test.ts.
  </acceptance_criteria>
  <done>anno-seam.test.ts holds four cases: the files[] case, the two WR-25 pins, and the WR-25
  behavioural case. Its specifier-scan helpers are gone.</done>
</task>
<task type="auto">
  <name>Task 2: Remove the two WR-25 pin cases and the doomed import</name>
  <files>src/mcp/vice/anno-seam.test.ts</files>
  <read_first>
    - src/mcp/vice/anno-seam.test.ts, the region holding the two WR-25 pin cases and the WR-25
      behavioural case, plus the file's import block.
    - 56-CONTEXT.md § D-07 item 2 (why the behavioural WR-25 case survives and what it proves).
  </read_first>
  <action>
    Capture the BEFORE TAP name set to /tmp/56-02-t2-before.set.

    Remove these two cases WHOLE, by verbatim name. Both assert over the seam's stripped source.
      20. WR-25 pin: the unconfined escape is used by NO shipped module but the seam, and exactly at its enumerated module-derived opens
      21. WR-25 pin, NON-VACUITY: the scanned shipped module set is real and the seam's stripped source still contains openStore

    KEEP the case named: WR-25: the guard itself exists -- openStore refuses BEHAVIOURALLY when
    neither a workspaceRoot nor the escape is supplied. It must stay byte-identical. It is
    success criterion 5. Its own comment states it is asserted through the entry point rather than
    against source text. anno-confinement.test.ts does not cover this refusal.

    Remove the constants ESCAPE_AT_A_CALL_SITE and ENUMERATED_DERIVED_OPENS, whose only callers
    were the two removed pins. grep the file for each identifier first.

    Remove the import of the doomed scanning module, which is line 21 in the pre-cut file.

    Then re-check the import block. Keep every specifier the two survivors still use: test,
    assert, existsSync, mkdtempSync, readdirSync, readFileSync, rmSync, tmpdir, fileURLToPath,
    dirname, join, AnnoStorePathError, closeStore and openStore. Remove an import specifier only
    when no surviving code line names it.

    Leave the file's header comment block byte-identical. It is test-file prose and stays stale on
    purpose under D-13's reasoning.

    Stage with git add src/mcp/vice/anno-seam.test.ts and commit that one path. Run
    git status --porcelain before the commit and confirm the eight untouchable paths are unstaged.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && node --test --test-reporter=tap anno-seam.test.ts > /tmp/56-02-t2-after.tap 2>&1; echo "EXIT=$?"; grep -aE '^ok [0-9]+ - ' /tmp/56-02-t2-after.tap | sed -E 's/^ok [0-9]+ - //' | sort > /tmp/56-02-t2-after.set; comm -23 /tmp/56-02-t2-before.set /tmp/56-02-t2-after.set > /tmp/56-02-t2-lost.txt; comm -13 /tmp/56-02-t2-before.set /tmp/56-02-t2-after.set > /tmp/56-02-t2-gained.txt; printf 'lost='; wc -l /tmp/56-02-t2-lost.txt; printf 'gained='; wc -l /tmp/56-02-t2-gained.txt; cat /tmp/56-02-t2-after.set</automated>
    <fails_when>EXIT is anything but 0. Or gained reports any non-zero count. Or lost reports a count other than 2. Or the printed AFTER set holds anything other than the two survivor names.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && printf 'doomed_import='; grep -ac 'from "./shipped-modules.ts"' anno-seam.test.ts || true; printf 'doomed_calls='; grep -av '^\s*[*/]' anno-seam.test.ts | grep -acE '(codeOnly|shippedTsModules|extractCommentSpans|commentByteTotal|shippedScanSurface) *\(' || true; printf 'dead_consts='; grep -av '^\s*[*/]' anno-seam.test.ts | grep -acE '(ESCAPE_AT_A_CALL_SITE|ENUMERATED_DERIVED_OPENS)' || true; printf 'assertions='; grep -ac 'assert\.' anno-seam.test.ts || true</automated>
    <fails_when>doomed_import is anything but 0. Or doomed_calls is anything but 0. Or dead_consts is anything but 0. Or assertions is 0, which would mean the file was left with setup and no assertions.</fails_when>
    <automated>npm --prefix "$(git rev-parse --show-toplevel)/src/mcp/vice" run typecheck > /tmp/56-02-t2-tc.txt 2>&1; echo "EXIT=$?"; tail -20 /tmp/56-02-t2-tc.txt</automated>
    <fails_when>EXIT is anything but 0, or the output names any file under src/mcp/vice.</fails_when>
  </verify>
  <acceptance_criteria>
    - The post-cut TAP name set holds exactly two names: the package.json files[] completeness
      case and the WR-25 behavioural refusal case.
    - `git diff` for this file shows no change inside either survivor's body.
    - `grep -ac 'from "./shipped-modules.ts"' anno-seam.test.ts` returns 0.
    - The file still contains at least one `assert.` call, so it is neither empty nor setup-only.
    - `npm run typecheck` exits 0.
    - The commit stages exactly one path, src/mcp/vice/anno-seam.test.ts.
  </acceptance_criteria>
  <done>anno-seam.test.ts holds two byte-identical survivors and no doomed import.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full automated gate and record the twenty-one removed names</name>
  <files>.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-02-SUMMARY.md</files>
  <read_first>
    - /tmp/56-02-t1-lost.txt and /tmp/56-02-t2-lost.txt, the two measured name-set diffs.
    - 56-CONTEXT.md § D-16 (what the SUMMARY must carry and why counts alone were rejected).
  </read_first>
  <action>
    Run the full automated suite once. The measured floor, taken live on 2026-09-14 before any
    Phase 56 edit, is EXIT=0 with fail 0. Compare the outcome as a SET of failing names, never as
    a count.

    One cross-file coupling is already measured and must be watched here. The removed case named
    "node:sqlite is bounded in the TEST tree too: the set of test files naming it is a DECLARED
    list" was the only code-level assertion in this repository that pins one in-scope test file's
    name from another file. Removing it is what makes plan 56-10's anno-derive.test.ts cut safe.
    If any OTHER file turns red instead, that measurement IS the finding. Report it and stop. Do
    not adjust the tripped assertion.

    Then write the SUMMARY. It must list all 21 removed case names VERBATIM, each with a one-line
    reason it qualified. Group them under this file's heading. Also record, separately, that this
    plan removed no file and added no prose to the source tree.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && npm run test:automated > /tmp/56-02-t3-gate.txt 2>&1; echo "EXIT=$?"; grep -aE '^.? ?(tests|pass|fail|skipped) [0-9]+$' /tmp/56-02-t3-gate.txt; grep -aE '^not ok |✖ ' /tmp/56-02-t3-gate.txt || true</automated>
    <fails_when>EXIT is anything but 0. Or the summary block reports a fail count other than 0. Or any failing-test line is printed. Or the summary block prints nothing, which means the grep anchors missed the reporter format and the run was not read.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)" && printf 'summary_names='; grep -ac 'planted violation, route' .planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-02-SUMMARY.md || true</automated>
    <fails_when>summary_names is less than 4, which means the four planted-violation route names were not carried verbatim into the SUMMARY.</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm run test:automated` exits 0 and reports fail 0.
    - The SUMMARY holds all 21 removed case names verbatim, each with a one-line reason.
    - The SUMMARY states that anno-seam.test.ts was kept, not removed, and was not renamed.
  </acceptance_criteria>
  <done>The suite is green and the 21 removed names exist verbatim in the SUMMARY for D-16.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer working tree to git index | This plan commits in a tree holding eight unrelated in-flight edits owned by the user. |

## STRIDE Threat Register

ASVS level 1. Block on high. RESEARCH assessed this phase as touching no authentication, no
session, no access control, no input validation and no cryptography surface. It removes test code
in a private developer-tool tree. That assessment is recorded honestly rather than padded. The one
genuine high-severity hazard is destructive-action, not attacker-driven.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-01 | Tampering | `git add` in every committing task | high | mitigate | Stage by explicit path only. `git add -A`, `git add .` and `git commit -a` are prohibited by the boundaries section. Confirm with `git status --porcelain` before each commit. |
| T-56-02 | Tampering | the cut, against test coverage integrity | high | mitigate | The per-file TAP name SET diff gates both cutting tasks. The AFTER set is asserted to hold exactly the two survivor names. |
| T-56-04 | Denial of service | success criterion 5, the WR-25 behavioural refusal | high | mitigate | The survivor is asserted present by name in the AFTER set and asserted byte-identical by `git diff` review. |
| T-56-SC | Tampering | npm, pip and cargo installs | low | accept | This plan runs no package-manager install and adds no dependency. RESEARCH § "Package Legitimacy Audit" records the same finding. No package-legitimacy checkpoint is owed. |
</threat_model>

<verification>
1. `npm --prefix src/mcp/vice run typecheck` exits 0.
2. `cd src/mcp/vice && npm run test:automated` exits 0 and reports fail 0.
3. `node --test --test-reporter=tap src/mcp/vice/anno-seam.test.ts` reports exactly the two
   survivor names.
4. `grep -c 'from "./shipped-modules.ts"' src/mcp/vice/anno-seam.test.ts` returns 0.
5. `git log --name-only` for this plan's commits names only src/mcp/vice/anno-seam.test.ts and
   the SUMMARY.
</verification>

<success_criteria>
- SC-1 progress: anno-seam.test.ts no longer names the doomed module.
- SC-2: the 21 removals are exactly the cases whose subject was source text. Both survivors are
  byte-identical.
- SC-3: the automated suite and typecheck both exit 0.
- SC-4: the file is neither empty nor setup-only. It still carries assertions.
- SC-5: the WR-25 behavioural case survives byte-identically.
</success_criteria>

## Artifacts this phase produces

This phase's net effect is DELETION. It adds no production symbol, no module and no test helper.
A drift-verification pass must not read an absent symbol as an unplanned omission.

What this plan produces:

- .planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-02-SUMMARY.md
- /tmp/56-02-t1-lost.txt and /tmp/56-02-t2-lost.txt, the measured TAP name-set diffs. These are
  run evidence and are not committed.

<output>
Create `.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-02-SUMMARY.md` when done.
</output>

