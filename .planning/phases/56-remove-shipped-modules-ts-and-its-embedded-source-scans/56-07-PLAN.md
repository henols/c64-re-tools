---
phase: 56-remove-shipped-modules-ts-and-its-embedded-source-scans
plan: 07
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - src/mcp/vice/anno-store.test.ts
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "anno-store.test.ts carries no import of the doomed scanning module."
    - "No surviving case in anno-store.test.ts reads anno-store.ts as text."
    - "The two genuinely mixed cases are stripped in place, with every surviving assertion line byte-identical."
    - "The D-15 behavioural refusal case that opens a real SCHEMA_VERSION 2 store file survives untouched."
    - "npm run typecheck exits 0 at every commit. npm run test:automated is green at the end of this plan."
    - statement: "Each of the fourteen candidate cases was hand-read at its own paren-matched boundary before any removal."
      verification: backstop
    - statement: "Each D-02 exemption judgement was made by reading the case body, and the rename it produced states what the case now proves."
      verification: backstop
  artifacts:
    - src/mcp/vice/anno-store.test.ts
    - .planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-07-SUMMARY.md
  key_links:
    - "This file holds both of the phase's measured genuinely-mixed cases. D-03's strip-in-place rule is load-bearing here and nowhere else in the phase except stock-dispatch.test.ts."
    - "The case at line 5321 is wholly structural and goes whole. Its behavioural sibling is a different, earlier case at line 5284 that needs no edit. Do not confuse them."
---

<objective>
Remove `anno-store.test.ts`'s fourteen source-scanning cases in three scoped regions, strip the
two genuinely mixed cases in place, and remove the file's import of the doomed module.

Purpose: this file is 5,595 lines and carries more scanning cases than any other. Two of them are
the phase's only measured genuinely-mixed cases, where a whole-case removal would take real
behavioural coverage with it.

Output: an `anno-store.test.ts` that asserts on no source text, with its behavioural coverage
intact, and the removed case names for D-16.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-CONTEXT.md
@.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-RESEARCH.md
@.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-PATTERNS.md
</context>

<boundaries>
## Hard boundaries. These bind every task in this plan

NEVER `git add`, `git commit`, `git checkout` or `git restore` any of these eight paths. They are
the user's unrelated in-flight edits, modified or untracked in the working tree right now.
`.claude/settings.json`, `src/mcp/vice/anno-bank.ts`, `src/mcp/vice/anno-coverage.ts`,
`src/mcp/vice/anno-enum-gen.ts`, `src/mcp/vice/anno-tools.ts`, `docs/dissambler-workflow.md`,
`docs/vice-mcp-ideas.md`, `setup-claude-ste100.sh`.

Stage EXPLICITLY BY PATH in every commit. Never `git add -A`. Never `git add .`. Never
`git commit -a`.

`src/mcp/vice/anno-store.ts` is NOT edited by this plan. Plan 56-10 owns that file's four comment
repairs. This plan touches only `src/mcp/vice/anno-store.test.ts`. The two names differ by five
characters.

All file deletions use `git rm`. No file is truncated or emptied to simulate deletion. This plan
removes no file.

Never run the bare `npm test` glob. It blocks forever on `vice-proxy.test.ts`. Use
`npm run test:automated`, or a per-file `node --test <file>`.

Never pipe a suite run to `tail` or `head`. That reports the pager's exit code and fakes a green
baseline. Redirect to a file. Then read `EXIT=$?` on the same line.
</boundaries>

<cut_rule>
## What qualifies for removal

IN SCOPE: a case whose SUBJECT is the source TEXT of a module. The route does not matter. It may
be the doomed scanning helpers, a raw `readFileSync` of a sibling `.ts` or `.mts`, or a
module-scope `*_SOURCE` constant built from one.

NOT IN SCOPE: a case that reads a non-source artifact, such as `package.json` parsed as JSON, a
JSON data file, a fixture, or a directory listing.

- **D-01.** Whole-case removal is the default.
- **D-02.** A case is exempt only when removal of every source-text assertion still leaves at
  least one assertion that exercises production behaviour. A setup-only remainder goes whole. A
  remainder whose assertions all derive from scanned text goes whole. Several of this file's
  self-labelled STRUCTURAL cases call a real store function only as scaffolding for a text claim.
  Those FAIL the exemption test and go whole.
- **D-03.** An exempt case is stripped in place. Keep every surviving assertion line
  byte-for-byte. Never retype an assertion. Rename the case when the surviving name still
  promises something the stripped part proved.
- **D-04 (prohibition).** Never remove a case and re-add its behavioural half as a new case. That
  route was considered and rejected. It reaches the same end state by retyping.
- **Helper rule.** A module-scope helper, constant or import goes with its cases only when every
  caller is itself being removed. grep the whole file for other callers first.
- **D-10 (prohibition).** Write NO new prose anywhere. The SUMMARY is the one exception.
- **Stale comments in test files stay stale on purpose.** Leave surviving comment prose
  byte-identical, following D-13's reasoning.
- **Stop and report. Never adjust.** If a cut trips another file's pinned assertion, that
  measurement IS the finding.
- **D-17.** Compare SETS, never counts. Never pin a case count in an assertion.
</cut_rule>

<scanner_protocol>
## The D-14 scratch scanner. It narrows the read. It never authorises a cut

Reuse `/tmp/gsd-56-scope-scan/scope-scan.mjs` from plan 56-01 if it is present. Rebuild it from
56-RESEARCH.md § Q2's algorithm if it is absent. In BOTH cases, re-run its planted-fixture proof
and observe GREEN before you trust a hit. It is never committed and never placed under
`scripts/`. Do not list it in `files_modified`.

Scope your reading. This file is 5,595 lines. Read the cited spans plus about 50 lines of context
each. Do not read the whole file.

The scanner is symbol-anchored and has two measured blind spots. So do three passes over this
file across the three tasks. Run the scanner. Then grep for `readFileSync` of a sibling `.ts` or
`.mts` and for module-scope `*_SOURCE` constants. Then skim every surviving case NAME for
source-reading prose such as "STRUCTURAL", "SOURCE ORDER", "non-vacuity", "imports nothing" or
"declares no module-level". Resolve every candidate to its own paren-matched `test(` boundary
before you judge it. Line-proximity attribution was measured wrong on 8 of 10 cases.

This file reports 121 TAP names from 117 static `test(` declarations, because one loop site
iterates a spelling list. That loop body touches no doomed symbol and needs no cut.
</scanner_protocol>

<!-- planner-discipline-allow: from "./shipped-modules.ts" -->
<!-- planner-discipline-allow: codeOnly -->
<!-- planner-discipline-allow: shippedTsModules -->
<tasks>

<task type="auto">
  <name>Task 1: Region A, lines 865 to 2600, including the worked mixed case</name>
  <files>src/mcp/vice/anno-store.test.ts</files>
  <read_first>
    - src/mcp/vice/anno-store.test.ts lines 860 to 955 in full. This is the worked mixed case the
      phase discussion used to settle D-01 through D-03.
    - The same file at lines 1450 to 1500, 2050 to 2100, 2245 to 2300, 2510 to 2560 and 2578 to
      2640. Read about 50 lines of context around each.
    - 56-CONTEXT.md § "Specific Ideas" (the worked example, with its exact line boundaries).
    - 56-PATTERNS.md § "Edit Kind 2" (the strip-in-place shape).
  </read_first>
  <action>
    Capture the BEFORE TAP name set to /tmp/56-07-t1-before.set with the same command shape the
    verify block uses for the AFTER set.

    Re-run the scanner's planted-fixture proof and observe GREEN. Run the scanner over the file
    and keep its 14-hit list for reference across all three tasks of this plan.

    First, the mixed case. Hand-read the case named: the reserved bank field is never INTERPRETED:
    every list function returns bank null, and no line of the seam's own code branches on or
    computes with a bank value. Its first half opens a real store, writes rows and asserts every
    row has a null bank. That is genuine behavioural coverage and is EXEMPT under D-02. Its second
    half runs the doomed stripper over anno-store.ts and asserts on the stripped text. Remove the
    second half only. Keep every surviving assertion line byte-for-byte.

    Then RENAME that case. Its current name promises both halves, and the second half is gone.
    D-03 makes the rename mandatory here. The new name must state only what the case now proves.
    Record the old name and the new name in the task notes. Both belong in the SUMMARY.

    Then hand-read and remove WHOLE under D-01 these five cases, by verbatim name:
      1. WR-24, STRUCTURAL: revertTo's staging name derives from randomUUID, and all three of its cleanups route through discardSnapshot rather than a bare rmSync
      2. STORE-02 non-vacuity: the code this gap closure adds is inside the source the no-splitter structural scan reads
      3. the prune's SOURCE ORDER is the guarantee: inside pruneSnapshots the pointer-row delete precedes the unlink, asserted over the module's own stripped source
      4. the sweep's SOURCE ORDER is the guarantee too: inside reconcileSnapshotRing there is NO pointer-row delete at all, and the commit precedes the unlink
      5. the publish path's SOURCE ORDER is the durability guarantee (WR-13): stageSnapshot fsyncs after its vacuum, and publishSnapshot fsyncs the ring directory after its rename

    Several of these call pruneSnapshots or reconcileSnapshotRing as scaffolding before asserting
    on text. A call alone does not earn the D-02 exemption. The remainder must hold an assertion
    that exercises production behaviour. Apply the test by hand to each one and record the
    verdict.

    Do not remove the doomed import in this task. Task 3 removes it after the last case goes.

    Stage with git add src/mcp/vice/anno-store.test.ts and commit that one path. Run
    git status --porcelain before the commit and confirm the eight untouchable paths are unstaged.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && node --test --test-reporter=tap anno-store.test.ts > /tmp/56-07-t1-after.tap 2>&1; echo "EXIT=$?"; grep -aE '^ok [0-9]+ - ' /tmp/56-07-t1-after.tap | sed -E 's/^ok [0-9]+ - //' | sort > /tmp/56-07-t1-after.set; comm -23 /tmp/56-07-t1-before.set /tmp/56-07-t1-after.set > /tmp/56-07-t1-lost.txt; comm -13 /tmp/56-07-t1-before.set /tmp/56-07-t1-after.set > /tmp/56-07-t1-gained.txt; printf 'lost='; wc -l /tmp/56-07-t1-lost.txt; printf 'gained='; wc -l /tmp/56-07-t1-gained.txt; cat /tmp/56-07-t1-lost.txt /tmp/56-07-t1-gained.txt</automated>
    <fails_when>EXIT is anything but 0. Or the lost list holds a name the task notes did not record. Or the gained list holds anything other than the one renamed case the task notes record. Or the lost list holds fewer than the six names this task acts on.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && printf 'source_order_cases='; grep -ac "SOURCE ORDER is the guarantee" anno-store.test.ts || true; printf 'bank_null_assert='; grep -ac 'bank' anno-store.test.ts || true</automated>
    <fails_when>source_order_cases is anything but 0. Or bank_null_assert is 0, which would mean the exempt case's surviving behavioural half went with the removal.</fails_when>
    <automated>npm --prefix "$(git rev-parse --show-toplevel)/src/mcp/vice" run typecheck > /tmp/56-07-t1-tc.txt 2>&1; echo "EXIT=$?"; tail -20 /tmp/56-07-t1-tc.txt</automated>
    <fails_when>EXIT is anything but 0, or the output names any file under src/mcp/vice.</fails_when>
  </verify>
  <acceptance_criteria>
    - The five named cases are absent from the post-cut TAP set.
    - The mixed case appears in the post-cut TAP set under its NEW name.
    - `git diff` shows the mixed case's surviving assertion lines unchanged.
    - The task notes record the old name, the new name, and one D-02 verdict line per candidate.
    - `npm run typecheck` exits 0.
    - The commit stages exactly one path, src/mcp/vice/anno-store.test.ts.
  </acceptance_criteria>
  <done>Region A holds no source-text assertion. The worked mixed case is stripped in place and
  renamed.</done>
</task>

<task type="auto">
  <name>Task 2: Region B, lines 3060 to 3980, including the CR-08 exempt case</name>
  <files>src/mcp/vice/anno-store.test.ts</files>
  <read_first>
    - src/mcp/vice/anno-store.test.ts lines 3848 to 3980 in full. This is the CR-08 case, whose
      exact strip boundary PATTERNS.md quotes.
    - The same file at lines 3055 to 3110, 3215 to 3270, 3648 to 3700 and 3708 to 3760. Read about
      50 lines of context around each.
    - 56-PATTERNS.md § "Edit Kind 2" (the CR-08 boundary, read in full, with the keep and remove
      spans named).
  </read_first>
  <action>
    Capture the BEFORE TAP name set to /tmp/56-07-t2-before.set.

    First, the CR-08 exempt case. Hand-read the case named: CR-08: a retained snapshot TRUNCATED to
    zero bytes is REFUSED by name -- the live store stays byte-identical, the caller's handle still
    answers, and a later openStore succeeds. PATTERNS.md measured its boundary. Keep lines 3848 to
    3935 byte-for-byte: a real store at revision 3, a truncated snapshot, a refusal naming both
    paths, an intact live store, and a successful fresh open. Remove the tail that begins at the
    boundary comment on line 3937, which slices revertTo's body out of stripped source and asserts
    on the order of three string landmarks inside it.

    Re-read the surviving name afterwards. PATTERNS.md measured that it already describes only the
    behavioural half, so no rename is textually required. Confirm that still holds. If the
    surviving name promises anything the removed tail proved, rename it and record both names.

    Then hand-read and remove WHOLE under D-01 these four cases, by verbatim name:
      1. WR-11, STRUCTURAL: the CAS-failure refusal carries BOTH revisions, and reads the second one BEFORE the rollback
      2. WR-02 and WR-04, STRUCTURAL: the two remaining unguarded calls are inside handlers
      3. revertTo step 6, STRUCTURAL BACKSTOP: the sweep call sits inside a handler, so a future edit that reintroduces a throw cannot cost the caller a handle
      4. WR-17, STRUCTURAL: every openStore AFTER the rename in revertTo is inside a handler, and the reopen failure reports a revert that LANDED ON DISK

    Apply D-02 by hand to each one. A call to a real store function used only as scaffolding for a
    text claim does not earn the exemption. Record one verdict line per candidate.

    Stage with git add src/mcp/vice/anno-store.test.ts and commit that one path. Run
    git status --porcelain before the commit and confirm the eight untouchable paths are unstaged.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && node --test --test-reporter=tap anno-store.test.ts > /tmp/56-07-t2-after.tap 2>&1; echo "EXIT=$?"; grep -aE '^ok [0-9]+ - ' /tmp/56-07-t2-after.tap | sed -E 's/^ok [0-9]+ - //' | sort > /tmp/56-07-t2-after.set; comm -23 /tmp/56-07-t2-before.set /tmp/56-07-t2-after.set > /tmp/56-07-t2-lost.txt; comm -13 /tmp/56-07-t2-before.set /tmp/56-07-t2-after.set > /tmp/56-07-t2-gained.txt; printf 'lost='; wc -l /tmp/56-07-t2-lost.txt; printf 'gained='; wc -l /tmp/56-07-t2-gained.txt; cat /tmp/56-07-t2-lost.txt</automated>
    <fails_when>EXIT is anything but 0. Or the lost list holds a name the task notes did not record. Or the lost list holds the CR-08 case name while the task notes record no rename for it. Or the lost list holds fewer than the four whole-removal names.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && printf 'cr08_case='; grep -ac 'CR-08: a retained snapshot TRUNCATED to zero bytes is REFUSED by name' anno-store.test.ts || true; printf 'structural_left='; grep -ac 'STRUCTURAL' anno-store.test.ts || true</automated>
    <fails_when>cr08_case is 0 while the task notes record no rename for that case. Or structural_left is unchanged from its pre-task value, which would mean no STRUCTURAL case was removed.</fails_when>
    <automated>npm --prefix "$(git rev-parse --show-toplevel)/src/mcp/vice" run typecheck > /tmp/56-07-t2-tc.txt 2>&1; echo "EXIT=$?"; tail -20 /tmp/56-07-t2-tc.txt</automated>
    <fails_when>EXIT is anything but 0, or the output names any file under src/mcp/vice.</fails_when>
  </verify>
  <acceptance_criteria>
    - The four named cases are absent from the post-cut TAP set.
    - The CR-08 case is present in the post-cut TAP set, under its original name or under a
      recorded new name.
    - `git diff` shows CR-08's surviving assertion lines unchanged between the case opening and
      the removed tail's boundary comment.
    - The task notes record one D-02 verdict line per candidate.
    - `npm run typecheck` exits 0.
    - The commit stages exactly one path, src/mcp/vice/anno-store.test.ts.
  </acceptance_criteria>
  <done>Region B holds no source-text assertion. CR-08's behavioural half is intact.</done>
</task>
<task type="auto">
  <name>Task 3: Region C, the import, and the full automated gate</name>
  <files>src/mcp/vice/anno-store.test.ts</files>
  <read_first>
    - src/mcp/vice/anno-store.test.ts lines 4430 to 4540, lines 5280 to 5380, and the file's import
      block at the top. Read about 50 lines of context around each span.
    - 56-RESEARCH.md § Q3's verdict on the two D-15 cases, which explains why the one at line 5321
      goes whole and the one at line 5284 needs no edit.
    - 56-CONTEXT.md § D-16 (what the SUMMARY must carry).
  </read_first>
  <action>
    Capture the BEFORE TAP name set to /tmp/56-07-t3-before.set.

    Hand-read and remove WHOLE under D-01 these three cases, by verbatim name:
      1. WR-18, STRUCTURAL: pruneSnapshots' return value is BOUND at runWriteSequence's step-9 call site rather than discarded
      2. WR-18, STRUCTURAL: revertTo's step-6 sweep call BINDS its result and acts on it, so a reported rollback failure is not handed back as a clean handle
      3. D-15: the schema_version refusal is a SINGLE WITNESS -- exactly one comparison site, inside openStore, with no second write of anno_meta.schema_version and no migration entry point anywhere in the module

    Candidate 3 is measured as wholly structural. Every assertion in it reads anno-store.ts's own
    stripped source. It calls no real store function.

    KEEP the earlier case whose name begins: D-15: a genuine SCHEMA_VERSION 2 store file is
    REFUSED. RESEARCH Q3 measured it as the genuine behavioural D-15 proof, with no reference to
    any doomed symbol. It must stay byte-identical. The two cases share a decision prefix and are
    easy to confuse.

    Then run the two blind-spot passes over the WHOLE file. Pass one: grep for readFileSync of a
    sibling .ts or .mts and for any module-scope constant whose name ends in _SOURCE. Pass two:
    skim every surviving case NAME for source-reading prose such as "STRUCTURAL", "SOURCE ORDER",
    "non-vacuity" or "imports nothing". Hand-read every remaining candidate and apply the cut
    rule. Record each extra removal with its verbatim name.

    Remove every module-scope helper or constant whose only callers are now gone. grep the whole
    file for each identifier first. Then remove the doomed module import.

    Stage with git add src/mcp/vice/anno-store.test.ts and commit that one path. Run
    git status --porcelain before the commit and confirm the eight untouchable paths are unstaged.
    Confirm the commit does NOT name src/mcp/vice/anno-store.ts.

    Then run the full automated suite once for the whole plan. The measured floor, taken live on
    2026-09-14 before any Phase 56 edit, is EXIT=0 with fail 0. Compare the outcome as a SET of
    failing names, never as a count. If a file outside this plan turns red, that measurement IS
    the finding. Report it and stop.

    Finally write the SUMMARY. It must carry every case name removed in this plan VERBATIM, each
    with a one-line reason it qualified, plus a line per D-03 rename recording both names, plus
    one line per D-02 exemption recording what the case still proves.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && node --test --test-reporter=tap anno-store.test.ts > /tmp/56-07-t3-after.tap 2>&1; echo "EXIT=$?"; grep -aE '^ok [0-9]+ - ' /tmp/56-07-t3-after.tap | sed -E 's/^ok [0-9]+ - //' | sort > /tmp/56-07-t3-after.set; comm -23 /tmp/56-07-t3-before.set /tmp/56-07-t3-after.set > /tmp/56-07-t3-lost.txt; comm -13 /tmp/56-07-t3-before.set /tmp/56-07-t3-after.set > /tmp/56-07-t3-gained.txt; printf 'lost='; wc -l /tmp/56-07-t3-lost.txt; printf 'gained='; wc -l /tmp/56-07-t3-gained.txt; cat /tmp/56-07-t3-lost.txt</automated>
    <fails_when>EXIT is anything but 0. Or gained reports any non-zero count. Or the printed lost list holds a name the task notes did not record as an intended removal. Or the lost list is empty.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && printf 'doomed_import='; grep -ac 'from "./shipped-modules.ts"' anno-store.test.ts || true; printf 'doomed_calls='; grep -av '^\s*[*/]' anno-store.test.ts | grep -acE '(codeOnly|shippedTsModules|extractCommentSpans|commentByteTotal|shippedScanSurface) *\(' || true; printf 'raw_src_reads='; grep -av '^\s*[*/]' anno-store.test.ts | grep -acE 'readFileSync\(join\(HERE, "[a-z0-9.-]+\.(ts|mts)"' || true; printf 'd15_behavioural='; grep -ac 'D-15: a genuine SCHEMA_VERSION 2 store file is REFUSED' anno-store.test.ts || true</automated>
    <fails_when>doomed_import is anything but 0. Or doomed_calls is anything but 0. Or raw_src_reads is anything but 0. Or d15_behavioural is anything but 1.</fails_when>
    <automated>npm --prefix "$(git rev-parse --show-toplevel)/src/mcp/vice" run typecheck > /tmp/56-07-t3-tc.txt 2>&1; echo "EXIT=$?"; tail -20 /tmp/56-07-t3-tc.txt</automated>
    <fails_when>EXIT is anything but 0, or the output names any file under src/mcp/vice.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && npm run test:automated > /tmp/56-07-t3-gate.txt 2>&1; echo "EXIT=$?"; grep -aE '^.? ?(tests|pass|fail|skipped) [0-9]+$' /tmp/56-07-t3-gate.txt; grep -aE '^not ok |✖ ' /tmp/56-07-t3-gate.txt || true</automated>
    <fails_when>EXIT is anything but 0. Or the summary block reports a fail count other than 0. Or any failing-test line is printed. Or the summary block prints nothing, which means the grep anchors missed the reporter format and the run was not read.</fails_when>
  </verify>
  <acceptance_criteria>
    - The three named cases are absent from the post-cut TAP set.
    - The behavioural D-15 case is present in the post-cut TAP set.
    - No surviving code line reads anno-store.ts, or any other sibling .ts or .mts, as text.
    - `grep -ac 'from "./shipped-modules.ts"' anno-store.test.ts` returns 0.
    - `npm run typecheck` exits 0 and `npm run test:automated` exits 0 with fail 0.
    - The SUMMARY lists every case removed in this plan verbatim with a one-line reason, records
      both D-03 renames with old and new names, and records the D-02 exemptions.
    - Every commit in this plan stages exactly one path, src/mcp/vice/anno-store.test.ts.
  </acceptance_criteria>
  <done>anno-store.test.ts asserts on no source text, carries no doomed import, and the whole
  automated suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer working tree to git index | This plan commits in a tree holding eight unrelated in-flight edits owned by the user. |
| test file to the production module it reads | `anno-store.test.ts` reads `anno-store.ts` as text. The reading cases go. The read file is not edited here. |

## STRIDE Threat Register

ASVS level 1. Block on high. RESEARCH assessed this phase as touching no authentication, no
session, no access control, no input validation and no cryptography surface. It removes test code
in a private developer-tool tree. That assessment is recorded honestly rather than padded. The one
genuine high-severity hazard is destructive-action, not attacker-driven.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-01 | Tampering | `git add` in every committing task | high | mitigate | Stage by explicit path only. `anno-store.ts` differs from `anno-store.test.ts` by five characters and must never be staged by this plan. Confirm with `git status --porcelain` and `git show --name-only`. |
| T-56-02 | Tampering | the cut, against test coverage integrity | high | mitigate | The per-file TAP name SET diff gates every task. A name may disappear only when the task notes recorded it. A name may appear only as a recorded D-03 rename. |
| T-56-07 | Tampering | the D-02 judgement on this file's two mixed cases | high | mitigate | Every surviving assertion in a stripped case is kept byte-for-byte and reviewed in `git diff`. D-04 forbids the retyping route. PATTERNS.md quotes CR-08's exact strip boundary. |
| T-56-SC | Tampering | npm, pip and cargo installs | low | accept | This plan runs no package-manager install and adds no dependency. RESEARCH § "Package Legitimacy Audit" records the same finding. No package-legitimacy checkpoint is owed. |
</threat_model>

<verification>
1. `npm --prefix src/mcp/vice run typecheck` exits 0.
2. `cd src/mcp/vice && npm run test:automated` exits 0 and reports fail 0.
3. `grep -c 'from "./shipped-modules.ts"' src/mcp/vice/anno-store.test.ts` returns 0.
4. The three per-task TAP name-set diffs lost only the recorded names, and gained only the
   recorded renames.
5. `git log --name-only` for this plan's commits names only src/mcp/vice/anno-store.test.ts and
   the SUMMARY. It does not name src/mcp/vice/anno-store.ts.
</verification>

<success_criteria>
- SC-1 progress: anno-store.test.ts no longer names the doomed module.
- SC-2: every behavioural case survives, proved by the TAP name-set diffs, including both mixed
  cases' assertions kept byte-for-byte.
- SC-3: the automated suite and typecheck both exit 0.
- SC-4: the file is neither empty nor setup-only.
</success_criteria>

## Artifacts this phase produces

This phase's net effect is DELETION. It adds no production symbol, no module and no test helper.
A drift-verification pass must not read an absent symbol as an unplanned omission. The names this
plan introduces are RENAMES of existing cases under D-03, recorded in the SUMMARY with both their
old and their new form.

What this plan produces:

- .planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-07-SUMMARY.md
- /tmp/56-07-t1-lost.txt, /tmp/56-07-t2-lost.txt and /tmp/56-07-t3-lost.txt, the measured TAP
  name-set diffs. These are run evidence and are not committed.

<output>
Create `.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-07-SUMMARY.md` when done.
</output>

