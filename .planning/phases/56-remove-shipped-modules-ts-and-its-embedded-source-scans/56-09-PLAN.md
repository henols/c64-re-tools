---
phase: 56-remove-shipped-modules-ts-and-its-embedded-source-scans
plan: 09
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - src/mcp/vice/anno-coverage.test.ts
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "anno-coverage.test.ts carries no import of the doomed scanning module."
    - "No surviving case in anno-coverage.test.ts reads anno-coverage.ts, anno-cli.ts or any other sibling module as text."
    - "src/mcp/vice/anno-coverage.ts is byte-identical and unstaged at the end of this plan."
    - "npm run typecheck exits 0 at every commit. npm run test:automated is green at the end of this plan."
    - statement: "Every removed case had source TEXT as its entire subject, confirmed by a hand-read at its own paren-matched boundary."
      verification: backstop
  artifacts:
    - src/mcp/vice/anno-coverage.test.ts
    - .planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-09-SUMMARY.md
  key_links:
    - "anno-coverage.ts is on the untouchable list and anno-coverage.test.ts is in scope. The names differ by six characters. This is the phase's sharpest staging hazard."
    - "The scanner reports three hits here. The planner measured further raw-source candidates that reach a sibling module's text without any doomed symbol."
---

<objective>
Remove `anno-coverage.test.ts`'s source-scanning cases and its import of the doomed module.

Purpose: this is the third giant, at 5,016 lines. It was not flagged as a giant in the ROADMAP
census, so it gets its own plan and its own scoped read.

Output: an `anno-coverage.test.ts` that asserts on no source text, and the removed case names for
D-16.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-CONTEXT.md
@.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-RESEARCH.md
@.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-PATTERNS.md
</context>

<boundaries>
## Hard boundaries. These bind every task in this plan

NEVER `git add`, `git commit`, `git checkout` or `git restore` any of these eight paths. They are
the user's unrelated in-flight edits, modified or untracked in the working tree right now.
`.claude/settings.json`, `src/mcp/vice/anno-bank.ts`, `src/mcp/vice/anno-coverage.ts`,
`src/mcp/vice/anno-enum-gen.ts`, `src/mcp/vice/anno-tools.ts`, `docs/dissambler-workflow.md`,
`docs/vice-mcp-ideas.md`, `setup-claude-ste100.sh`.

READ THIS TWICE. `src/mcp/vice/anno-coverage.ts` is on that untouchable list. It is MODIFIED in
the working tree right now by the user. `src/mcp/vice/anno-coverage.test.ts` is this plan's only
target. The two names differ by six characters. Staging the wrong one commits the user's
unrelated in-flight work.

Stage EXPLICITLY BY PATH in every commit. Never `git add -A`. Never `git add .`. Never
`git commit -a`.

All file deletions use `git rm`. No file is truncated or emptied to simulate deletion. This plan
removes no file.

Never run the bare `npm test` glob. It blocks forever on `vice-proxy.test.ts`. Use
`npm run test:automated`, or a per-file `node --test <file>`.

Never pipe a suite run to `tail` or `head`. That reports the pager's exit code and fakes a green
baseline. Redirect to a file. Then read `EXIT=$?` on the same line.
</boundaries>

<cut_rule>
## What qualifies for removal

IN SCOPE: a case whose SUBJECT is the source TEXT of a module. The route does not matter. It may
be the doomed scanning helpers, a raw `readFileSync` of a sibling `.ts` or `.mts`, or a
module-scope `*_SOURCE` constant built from one.

NOT IN SCOPE: a case that reads a non-source artifact, such as `package.json` parsed as JSON, a
JSON data file, a fixture, or a directory listing.

- **D-01.** Whole-case removal is the default.
- **D-02.** A case is exempt only when removal of every source-text assertion still leaves at
  least one assertion that exercises production behaviour. A setup-only remainder goes whole.
- **D-03.** An exempt case is stripped in place, byte-for-byte, and renamed to what it now proves.
- **D-04 (prohibition).** Never remove a case and re-add its behavioural half as a new case.
- **Helper rule.** A module-scope helper, constant or import goes with its cases only when every
  caller is itself being removed. grep the whole file for other callers first.
- **D-10 (prohibition).** Write NO new prose anywhere. The SUMMARY is the one exception.
- **Stale comments in test files stay stale on purpose.** Leave surviving comment prose
  byte-identical, following D-13's reasoning.
- **Stop and report. Never adjust.** If a cut trips another file's pinned assertion, that
  measurement IS the finding.
- **D-17.** Compare SETS, never counts. Never pin a case count in an assertion.
</cut_rule>

<scanner_protocol>
## The D-14 scratch scanner. It narrows the read. It never authorises a cut

Reuse `/tmp/gsd-56-scope-scan/scope-scan.mjs` from plan 56-01 if it is present. Rebuild it from
56-RESEARCH.md § Q2's algorithm if it is absent. In BOTH cases, re-run its planted-fixture proof
and observe GREEN before you trust a hit. It is never committed and never placed under
`scripts/`. Do not list it in `files_modified`.

Scope your reading. This file is 5,016 lines. Read the cited spans plus about 50 lines of context
each. Do not read the whole file.

The scanner reports three hits here. The planner additionally measured raw-source reads attributed
by line proximity near three more cases. Resolve every candidate to its own paren-matched `test(`
boundary before you judge it. Line-proximity attribution was measured wrong on 8 of 10 cases
elsewhere in this phase.

This file reports 126 TAP names from 114 static `test(` declarations, because one loop site
generates a control family. That loop body touches no doomed symbol and needs no cut.
</scanner_protocol>

<!-- planner-discipline-allow: from "./shipped-modules.ts" -->
<!-- planner-discipline-allow: codeOnly -->
<!-- planner-discipline-allow: shippedTsModules -->
<tasks>

<task type="auto">
  <name>Task 1: The three scanner hits in the census-vocabulary region</name>
  <files>src/mcp/vice/anno-coverage.test.ts</files>
  <read_first>
    - src/mcp/vice/anno-coverage.test.ts lines 1 to 45 (imports and module-scope constants), and
      lines 945 to 1110. That span holds all three scanner hits and any helper between them.
    - 56-RESEARCH.md § Q1 (this file's three measured hits, at lines 957, 1040 and 1069).
  </read_first>
  <action>
    Capture the BEFORE TAP name set to /tmp/56-09-t1-before.set with the same command shape the
    verify block uses for the AFTER set.

    Re-run the scanner's planted-fixture proof and observe GREEN. Run the scanner over the file
    and keep its three-hit list for reference across both tasks of this plan.

    Hand-read and remove WHOLE under D-01 these three cases, by verbatim name:
      1. derived agreement: every member of the store's label-kind vocabulary appears as a literal in the census's source
      2. SUPPLEMENT (not the proof): the census module's source carries no production block-type literal
      3. SUPPLEMENT (WR-12): no shipped module passes CoverageOptions.blockClassifier

    Apply D-02 by hand to each. A call to a real coverage function used only as scaffolding for a
    text claim does not earn the exemption. Record one verdict line per candidate.

    Remove every module-scope helper or constant whose only callers were these cases. grep the
    whole file for each identifier first.

    Do not remove the doomed import in this task. Task 2 removes it after the last case goes.

    Stage with git add src/mcp/vice/anno-coverage.test.ts and commit that one path. The file name
    ends in .test.ts. Run git status --porcelain before the commit and confirm that
    src/mcp/vice/anno-coverage.ts is still modified and unstaged.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && node --test --test-reporter=tap anno-coverage.test.ts > /tmp/56-09-t1-after.tap 2>&1; echo "EXIT=$?"; grep -aE '^ok [0-9]+ - ' /tmp/56-09-t1-after.tap | sed -E 's/^ok [0-9]+ - //' | sort > /tmp/56-09-t1-after.set; comm -23 /tmp/56-09-t1-before.set /tmp/56-09-t1-after.set > /tmp/56-09-t1-lost.txt; comm -13 /tmp/56-09-t1-before.set /tmp/56-09-t1-after.set > /tmp/56-09-t1-gained.txt; printf 'lost='; wc -l /tmp/56-09-t1-lost.txt; printf 'gained='; wc -l /tmp/56-09-t1-gained.txt; cat /tmp/56-09-t1-lost.txt</automated>
    <fails_when>EXIT is anything but 0. Or gained reports any non-zero count. Or the lost list holds a name that is not one of the three named in the action. Or the lost list holds fewer than three names.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)" && printf 'untouchable_staged='; git diff --cached --name-only | grep -acE '(settings\.json|anno-bank\.ts|anno-coverage\.ts$|anno-enum-gen\.ts|anno-tools\.ts|dissambler-workflow\.md|vice-mcp-ideas\.md|setup-claude-ste100\.sh)' || true; git show --name-only --format='%s' HEAD</automated>
    <fails_when>untouchable_staged is anything but 0. Or the commit's file list names any path other than src/mcp/vice/anno-coverage.test.ts.</fails_when>
    <automated>npm --prefix "$(git rev-parse --show-toplevel)/src/mcp/vice" run typecheck > /tmp/56-09-t1-tc.txt 2>&1; echo "EXIT=$?"; tail -20 /tmp/56-09-t1-tc.txt</automated>
    <fails_when>EXIT is anything but 0, or the output names any file under src/mcp/vice.</fails_when>
  </verify>
  <acceptance_criteria>
    - The three named cases are absent from the post-cut TAP set, and no other name is lost.
    - The post-cut TAP name set gains no name.
    - The commit stages exactly one path, src/mcp/vice/anno-coverage.test.ts.
    - `git status --porcelain` still reports src/mcp/vice/anno-coverage.ts as modified and
      unstaged.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>The census-vocabulary scanning region is gone and the untouchable sibling file is
  untouched.</done>
</task>

<task type="auto">
  <name>Task 2: The remaining raw-source candidates, the import, and the full automated gate</name>
  <files>src/mcp/vice/anno-coverage.test.ts</files>
  <read_first>
    - src/mcp/vice/anno-coverage.test.ts lines 4025 to 4060, lines 4655 to 4700, lines 4920 to
      4960, and the file's import block.
    - 56-RESEARCH.md § "Open Questions" (why a symbol-anchored scan cannot see a raw readFileSync
      case).
    - 56-CONTEXT.md § D-16 (what the SUMMARY must carry).
  </read_first>
  <action>
    Capture the BEFORE TAP name set to /tmp/56-09-t2-before.set.

    Hand-read these three candidates at their own paren-matched boundaries and apply the cut rule.
    The planner attributed a sibling-source read to each by line proximity, which is unreliable, so
    resolve the boundary first. Names as measured:
      1. the coverage module contains no file-write call, no project-save call and no live-session import
      2. CR-05 (F, one decode two callers): anno-cli.ts's cross-reference byte source delegates to this loader rather than re-parsing
      3. the witness is PURE over its four arguments: two calls on one payload return the same answer

    Candidates 1 and 2 read as source-text subjects from their names. Candidate 3 reads as
    behavioural. Record a verdict line for each. Leave any candidate the hand-read clears, and
    record that the attribution was a false positive.

    Then run the two blind-spot passes over the WHOLE file. Pass one: grep for readFileSync of a
    sibling .ts or .mts and for any module-scope constant whose name ends in _SOURCE. Pass two:
    skim every surviving case NAME for source-reading prose such as "SUPPLEMENT", "STRUCTURAL",
    "non-vacuity", "contains no", "imports nothing" or "declares no module-level". Hand-read every
    remaining candidate and apply the cut rule. Record each extra removal with its verbatim name.

    Remove every module-scope helper or constant whose only callers are now gone. grep the whole
    file for each identifier first. Then remove the doomed module import.

    Stage with git add src/mcp/vice/anno-coverage.test.ts and commit that one path. Run
    git status --porcelain before the commit and confirm src/mcp/vice/anno-coverage.ts is still
    modified and unstaged.

    Then run the full automated suite once for the whole plan. The measured floor, taken live on
    2026-09-14 before any Phase 56 edit, is EXIT=0 with fail 0. Compare the outcome as a SET of
    failing names, never as a count. If a file outside this plan turns red, that measurement IS
    the finding. Report it and stop.

    Finally write the SUMMARY with every removed case name VERBATIM, each with a one-line reason,
    plus a line per D-03 rename and per D-02 exemption.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && node --test --test-reporter=tap anno-coverage.test.ts > /tmp/56-09-t2-after.tap 2>&1; echo "EXIT=$?"; grep -aE '^ok [0-9]+ - ' /tmp/56-09-t2-after.tap | sed -E 's/^ok [0-9]+ - //' | sort > /tmp/56-09-t2-after.set; comm -23 /tmp/56-09-t2-before.set /tmp/56-09-t2-after.set > /tmp/56-09-t2-lost.txt; comm -13 /tmp/56-09-t2-before.set /tmp/56-09-t2-after.set > /tmp/56-09-t2-gained.txt; printf 'lost='; wc -l /tmp/56-09-t2-lost.txt; printf 'gained='; wc -l /tmp/56-09-t2-gained.txt; cat /tmp/56-09-t2-lost.txt</automated>
    <fails_when>EXIT is anything but 0. Or gained reports any non-zero count. Or the printed lost list holds a name the task notes did not record as an intended removal.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && printf 'doomed_import='; grep -ac 'from "./shipped-modules.ts"' anno-coverage.test.ts || true; printf 'doomed_calls='; grep -av '^\s*[*/]' anno-coverage.test.ts | grep -acE '(codeOnly|shippedTsModules|extractCommentSpans|commentByteTotal|shippedScanSurface) *\(' || true; printf 'raw_src_reads='; grep -av '^\s*[*/]' anno-coverage.test.ts | grep -acE 'readFileSync\(join\(HERE, "[a-z0-9.-]+\.(ts|mts)"' || true; printf 'assertions='; grep -ac 'assert\.' anno-coverage.test.ts || true</automated>
    <fails_when>doomed_import is anything but 0. Or doomed_calls is anything but 0. Or raw_src_reads is anything but 0. Or assertions is 0, which would mean the file was left with setup and no assertions.</fails_when>
    <automated>npm --prefix "$(git rev-parse --show-toplevel)/src/mcp/vice" run typecheck > /tmp/56-09-t2-tc.txt 2>&1; echo "EXIT=$?"; tail -20 /tmp/56-09-t2-tc.txt</automated>
    <fails_when>EXIT is anything but 0, or the output names any file under src/mcp/vice.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && npm run test:automated > /tmp/56-09-t2-gate.txt 2>&1; echo "EXIT=$?"; grep -aE '^.? ?(tests|pass|fail|skipped) [0-9]+$' /tmp/56-09-t2-gate.txt; grep -aE '^not ok |✖ ' /tmp/56-09-t2-gate.txt || true</automated>
    <fails_when>EXIT is anything but 0. Or the summary block reports a fail count other than 0. Or any failing-test line is printed. Or the summary block prints nothing, which means the grep anchors missed the reporter format and the run was not read.</fails_when>
  </verify>
  <acceptance_criteria>
    - No surviving code line reads a sibling .ts or .mts as text.
    - `grep -ac 'from "./shipped-modules.ts"' anno-coverage.test.ts` returns 0.
    - The file still contains at least one `assert.` call.
    - `npm run typecheck` exits 0 and `npm run test:automated` exits 0 with fail 0.
    - The SUMMARY lists every case removed in this plan verbatim with a one-line reason, and
      records every false-positive candidate that was left in place.
    - Every commit in this plan stages exactly one path, src/mcp/vice/anno-coverage.test.ts.
  </acceptance_criteria>
  <done>anno-coverage.test.ts asserts on no source text, carries no doomed import, and the whole
  automated suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer working tree to git index | This plan commits in a tree holding eight unrelated in-flight edits owned by the user. One of them, anno-coverage.ts, differs from this plan's target by six characters. |

## STRIDE Threat Register

ASVS level 1. Block on high. RESEARCH assessed this phase as touching no authentication, no
session, no access control, no input validation and no cryptography surface. It removes test code
in a private developer-tool tree. That assessment is recorded honestly rather than padded. The one
genuine high-severity hazard is destructive-action, not attacker-driven.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-01 | Tampering | `git add` in every committing task | high | mitigate | Stage by explicit path only. Both tasks assert with a grep over `git diff --cached --name-only` that no untouchable path is staged, and read `git show --name-only` after the commit. |
| T-56-02 | Tampering | the cut, against test coverage integrity | high | mitigate | The per-file TAP name SET diff gates both tasks. A name may disappear only when the task notes recorded it. |
| T-56-SC | Tampering | npm, pip and cargo installs | low | accept | This plan runs no package-manager install and adds no dependency. RESEARCH § "Package Legitimacy Audit" records the same finding. No package-legitimacy checkpoint is owed. |
</threat_model>

<verification>
1. `npm --prefix src/mcp/vice run typecheck` exits 0.
2. `cd src/mcp/vice && npm run test:automated` exits 0 and reports fail 0.
3. `grep -c 'from "./shipped-modules.ts"' src/mcp/vice/anno-coverage.test.ts` returns 0.
4. Both per-task TAP name-set diffs lost only the recorded names and gained none.
5. `git log --name-only` for this plan's commits names only src/mcp/vice/anno-coverage.test.ts
   and the SUMMARY. It does not name src/mcp/vice/anno-coverage.ts.
6. `git status --porcelain` still reports src/mcp/vice/anno-coverage.ts as modified and unstaged.
</verification>

<success_criteria>
- SC-1 progress: anno-coverage.test.ts no longer names the doomed module.
- SC-2: every behavioural case survives, proved by the TAP name-set diffs.
- SC-3: the automated suite and typecheck both exit 0.
- SC-4: the file is neither empty nor setup-only.
</success_criteria>

## Artifacts this phase produces

This phase's net effect is DELETION. It adds no production symbol, no module and no test helper.
A drift-verification pass must not read an absent symbol as an unplanned omission.

What this plan produces:

- .planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-09-SUMMARY.md
- /tmp/56-09-t1-lost.txt and /tmp/56-09-t2-lost.txt, the measured TAP name-set diffs. These are
  run evidence and are not committed.

<output>
Create `.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-09-SUMMARY.md` when done.
</output>

