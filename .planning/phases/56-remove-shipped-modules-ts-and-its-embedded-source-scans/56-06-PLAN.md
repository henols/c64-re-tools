---
phase: 56-remove-shipped-modules-ts-and-its-embedded-source-scans
plan: 06
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - src/mcp/vice/stock-dispatch.test.ts
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "stock-dispatch.test.ts carries no import of the doomed scanning module."
    - "No surviving case in stock-dispatch.test.ts reads vice-proxy.ts, anno-tools.ts or stock-dispatch.ts as text."
    - "The file runs green on its own and loses only the cases the plan named or the executor recorded."
    - "The CHAN-04 concurrent-dispatch case survives untouched."
    - "npm run typecheck exits 0 at every commit. npm run test:automated is green at the end of this plan."
    - statement: "Each genuinely mixed case was judged by hand under D-02, and every surviving assertion line in it is byte-identical to its pre-cut form."
      verification: backstop
  artifacts:
    - src/mcp/vice/stock-dispatch.test.ts
    - .planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-06-SUMMARY.md
  key_links:
    - "This file holds the largest measured population of raw-source scanning cases that the symbol-anchored scanner cannot see. They reach vice-proxy.ts through two module-scope constants rather than through a doomed symbol."
    - "Removing those constants is what makes the doomed import removable, and the import removal is what makes plan 56-11's module deletion possible."
---

<objective>
Remove `stock-dispatch.test.ts`'s source-scanning cases, in three scoped regions, and remove its
import of the doomed module.

Purpose: this file is 3,278 lines and holds two distinct scanning populations. Seven cases reach
the doomed helper directly. Roughly ten more scan `vice-proxy.ts`, `anno-tools.ts` or
`stock-dispatch.ts` through module-scope constants, with no doomed symbol anywhere in the chain.
A scanner-only cut would leave the second population in place and the file would still assert on
text.

Output: a `stock-dispatch.test.ts` whose cases are all behavioural, and the removed case names
for D-16.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-CONTEXT.md
@.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-RESEARCH.md
@.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-PATTERNS.md
</context>

<boundaries>
## Hard boundaries. These bind every task in this plan

NEVER `git add`, `git commit`, `git checkout` or `git restore` any of these eight paths. They are
the user's unrelated in-flight edits, modified or untracked in the working tree right now.
`.claude/settings.json`, `src/mcp/vice/anno-bank.ts`, `src/mcp/vice/anno-coverage.ts`,
`src/mcp/vice/anno-enum-gen.ts`, `src/mcp/vice/anno-tools.ts`, `docs/dissambler-workflow.md`,
`docs/vice-mcp-ideas.md`, `setup-claude-ste100.sh`.

`src/mcp/vice/anno-tools.ts` is on that untouchable list and this file READS it as text. Remove
the reading case. Never edit the read file.

Stage EXPLICITLY BY PATH in every commit. Never `git add -A`. Never `git add .`. Never
`git commit -a`.

All file deletions use `git rm`. No file is truncated or emptied to simulate deletion. This plan
removes no file.

Never run the bare `npm test` glob. It blocks forever on `vice-proxy.test.ts`. Use
`npm run test:automated`, or a per-file `node --test <file>`. `vice-proxy.test.ts` is in
`MANUAL_ONLY_TESTS` and is out of this phase's scope, even though this file scans
`vice-proxy.ts`'s source. Do not run it and do not edit it.

Never pipe a suite run to `tail` or `head`. That reports the pager's exit code and fakes a green
baseline. Redirect to a file. Then read `EXIT=$?` on the same line.
</boundaries>

<cut_rule>
## What qualifies for removal

IN SCOPE: a case whose SUBJECT is the source TEXT of a module. The route does not matter. It may
be the doomed scanning helpers, a raw `readFileSync` of a sibling `.ts` or `.mts`, or a
module-scope `*_SOURCE` constant built from one.

NOT IN SCOPE: a case that reads a non-source artifact, such as `package.json` parsed as JSON, a
JSON data file, a fixture, or a directory listing.

- **D-01.** Whole-case removal is the default.
- **D-02.** A case is exempt only when removal of every source-text assertion still leaves at
  least one assertion that exercises production behaviour. A setup-only remainder goes whole. A
  remainder whose assertions all derive from scanned text goes whole.
- **D-03.** An exempt case is stripped in place. Keep every surviving assertion line
  byte-for-byte. Never retype an assertion. Rename the case when the surviving name still
  promises something the stripped part proved. This file holds at least one measured mixed case,
  so D-03 is load-bearing here.
- **D-04 (prohibition).** Never remove a case and re-add its behavioural half as a new case.
- **Helper rule.** A module-scope helper, constant or import goes with its cases only when every
  caller is itself being removed. grep the whole file for other callers first.
- **D-10 (prohibition).** Write NO new prose anywhere. The SUMMARY is the one exception.
- **Stale comments in test files stay stale on purpose.** Leave surviving comment prose
  byte-identical, following D-13's reasoning.
- **Stop and report. Never adjust.** If a cut trips another file's pinned assertion, that
  measurement IS the finding.
- **D-17.** Compare SETS, never counts. Never pin a case count in an assertion.
</cut_rule>

<scanner_protocol>
## The D-14 scratch scanner. It narrows the read. It never authorises a cut

Reuse `/tmp/gsd-56-scope-scan/scope-scan.mjs` from plan 56-01 if it is present. Rebuild it from
56-RESEARCH.md § Q2's algorithm if it is absent. In BOTH cases, re-run its planted-fixture proof
and observe GREEN before you trust a hit. It is never committed and never placed under
`scripts/`. Do not list it in `files_modified`.

For THIS file the scanner is necessary and NOT sufficient. It reports seven hits. The planner
measured roughly ten more scanning cases that reach source through the module-scope constants
`VICE_PROXY_SOURCE`, `VICE_PROXY_CODE_LINES` and `ANNO_TOOLS_SOURCE`, plus one direct
`readFileSync` of `stock-dispatch.ts`. Every candidate line number this plan gives came from
line-proximity attribution, so resolve each one to its own paren-matched `test(` boundary before
you judge it. Line-proximity attribution was measured wrong on 8 of 10 cases elsewhere in this
phase.

This file also reports 137 TAP names from 91 static `test(` declarations, because one loop site
iterates roughly 47 tool names. That loop body touches no doomed symbol and needs no cut. The SET
diff is the gate, so the gap is expected.
</scanner_protocol>

<!-- planner-discipline-allow: from "./shipped-modules.ts" -->
<!-- planner-discipline-allow: codeOnly -->
<!-- planner-discipline-allow: shippedTsModules -->
<tasks>

<task type="auto">
  <name>Task 1: The WR-06 region, including the one measured mixed case</name>
  <files>src/mcp/vice/stock-dispatch.test.ts</files>
  <read_first>
    - src/mcp/vice/stock-dispatch.test.ts lines 1 to 45 (imports), lines 1340 to 1410 (the WR-06
      cases), and lines 1460 to 1475 (the two module-scope source constants).
    - 56-PATTERNS.md § "Edit Kind 2" (the strip-in-place shape, with a worked boundary).
  </read_first>
  <action>
    Capture the BEFORE TAP name set to /tmp/56-06-t1-before.set with the same command shape the
    verify block uses for the AFTER set.

    Re-run the scanner's planted-fixture proof and observe GREEN. Run the scanner over the file
    and keep its hit list for reference across all three tasks of this plan.

    Hand-read the case named: WR-06: a non-connect handshake failure keeps the plain wording -- the
    binmon-host advice is not sprayed over unrelated causes. The planner read it and measured it as
    wholly behavioural, with no source-text assertion at all. If your hand-read agrees, LEAVE IT
    UNTOUCHED and record that the scanner hit was a false positive. If your hand-read disagrees,
    that measurement IS the finding. Report it and stop.

    Then hand-read the case named: WR-06: vice-proxy.ts strips the WHATWG bracket form when
    deriving the dial host, so an IPv6 URL is usable by net.connect(). The planner measured it as
    a genuine MIXED case. Its first three assertions exercise the WHATWG URL parser directly and
    are real behaviour. Its last three lines slice `buildHeldLease`'s body out of a source constant
    and assert on that text.

    Apply D-02 and D-03 to it. Keep the three URL assertions byte-for-byte. Remove the source
    slice and the assertion over it. Then RENAME the case: the surviving name promises that
    vice-proxy.ts strips the bracket form, which only the removed half proved. The new name must
    state what the case now proves, which is the WHATWG parser quirk and the strip expression
    applied to it. Record the old name and the new name in the task notes. Both belong in the
    SUMMARY.

    Never retype a surviving assertion. Never remove the case and re-add its behavioural half.
    D-04 forbids that route.

    Stage with git add src/mcp/vice/stock-dispatch.test.ts and commit that one path. Run
    git status --porcelain before the commit and confirm the eight untouchable paths are unstaged.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && node --test --test-reporter=tap stock-dispatch.test.ts > /tmp/56-06-t1-after.tap 2>&1; echo "EXIT=$?"; grep -aE '^ok [0-9]+ - ' /tmp/56-06-t1-after.tap | sed -E 's/^ok [0-9]+ - //' | sort > /tmp/56-06-t1-after.set; comm -23 /tmp/56-06-t1-before.set /tmp/56-06-t1-after.set > /tmp/56-06-t1-lost.txt; comm -13 /tmp/56-06-t1-before.set /tmp/56-06-t1-after.set > /tmp/56-06-t1-gained.txt; printf 'lost='; wc -l /tmp/56-06-t1-lost.txt; printf 'gained='; wc -l /tmp/56-06-t1-gained.txt; cat /tmp/56-06-t1-lost.txt /tmp/56-06-t1-gained.txt</automated>
    <fails_when>EXIT is anything but 0. Or the lost list holds a name the task notes did not record. Or the gained list holds anything other than the one renamed case the task notes record. Or lost and gained are both empty, which means the rename did not land.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && printf 'plain_wording_case='; grep -ac 'WR-06: a non-connect handshake failure keeps the plain wording' stock-dispatch.test.ts || true; printf 'buildheldlease_slices='; grep -av '^\s*[*/]' stock-dispatch.test.ts | grep -ac 'indexOf("function buildHeldLease(")' || true</automated>
    <fails_when>plain_wording_case is anything but 1. Or buildheldlease_slices is 2 or more, which means the WR-06 mixed case still slices the source constant.</fails_when>
    <automated>npm --prefix "$(git rev-parse --show-toplevel)/src/mcp/vice" run typecheck > /tmp/56-06-t1-tc.txt 2>&1; echo "EXIT=$?"; tail -20 /tmp/56-06-t1-tc.txt</automated>
    <fails_when>EXIT is anything but 0, or the output names any file under src/mcp/vice.</fails_when>
  </verify>
  <acceptance_criteria>
    - The wholly behavioural WR-06 handshake-wording case is present in the post-cut TAP set and
      its body is unchanged in `git diff`.
    - The mixed WR-06 case appears in the post-cut TAP set under its NEW name, and its three
      surviving URL assertions are byte-identical in `git diff`.
    - The task notes record the old name, the new name, and the D-02 judgement in one line each.
    - `npm run typecheck` exits 0.
    - The commit stages exactly one path, src/mcp/vice/stock-dispatch.test.ts.
  </acceptance_criteria>
  <done>The WR-06 region holds no source-text assertion. The mixed case is stripped in place and
  renamed.</done>
</task>

<task type="auto">
  <name>Task 2: The structure/proxy region and its two source constants</name>
  <files>src/mcp/vice/stock-dispatch.test.ts</files>
  <read_first>
    - src/mcp/vice/stock-dispatch.test.ts lines 1440 to 1700. This span holds every candidate in
      this task plus the three module-scope source constants.
    - 56-RESEARCH.md § Q1 (this file's scanner hit list).
  </read_first>
  <action>
    Capture the BEFORE TAP name set to /tmp/56-06-t2-before.set.

    Hand-read each candidate below and apply the cut rule. Every one of them was attributed by
    line proximity, so resolve its own paren-matched `test(` boundary first. Remove WHOLE under
    D-01 every case whose assertions all derive from scanned source text. Apply D-02 and D-03 to
    any case that also exercises production behaviour, and record the judgement.

    Candidates, by verbatim name:
      1. dispatch: no handler in the table ever throws -- dispatchStock always resolves to a well-formed {content,isError} result
      2. structure/proxy: vice-proxy.ts has exactly one dispatchStock CALL SITE
      3. structure/proxy: vice-proxy.ts's dispatchStock call site passes ensureBrokerLease as its LeaseProvider
      4. structure/proxy: vice-proxy.ts references manifestPathForBackend exactly once
      5. structure/proxy: vice-proxy.ts's ensureBrokerLease has at least two lease-bearing success returns
      6. structure/proxy: no code line in vice-proxy.ts pairs "stock" with "forwardToVice"
      7. structure/proxy (CR-07): vice_result_continue's runner is handleResultContinue, whose body touches no transport at all
      8. structure/proxy (plan 29-01): the anno_* loop registration's runner is runAnnoTool, whose body touches no VICE transport at all
      9. structure/proxy (CR-07): handleRecycle() and handleDiagnose() each delegate to dispatchStockFor(), with no per-backend branch left to route around
      10. structure/proxy: vice-proxy.ts CALLS resolvedBackend() exactly once
      11. structure/proxy (CR-06): buildHeldLease() threads epochFile and supervisorDir, from activeInstance() and brokerRootDir() respectively
      12. structure/proxy (CR-07): handleDiagnose and handleRecycle are each referenced by exactly one registration, via buildViceTool() directly

    Candidate 1 is the one most likely to be mixed. The planner measured it reading a source
    constant twice while also calling dispatchStock for real. Judge it under D-02 by hand.

    After the removals, remove the module-scope constants whose only callers are gone:
    VICE_PROXY_SOURCE at line 1467, VICE_PROXY_CODE_LINES at line 1468 and ANNO_TOOLS_SOURCE at
    line 1595. grep the WHOLE file for each name before removing it. If a case in task 3's region
    still uses one, leave that constant for task 3.

    Stage with git add src/mcp/vice/stock-dispatch.test.ts and commit that one path. Run
    git status --porcelain before the commit and confirm the eight untouchable paths are unstaged.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && node --test --test-reporter=tap stock-dispatch.test.ts > /tmp/56-06-t2-after.tap 2>&1; echo "EXIT=$?"; grep -aE '^ok [0-9]+ - ' /tmp/56-06-t2-after.tap | sed -E 's/^ok [0-9]+ - //' | sort > /tmp/56-06-t2-after.set; comm -23 /tmp/56-06-t2-before.set /tmp/56-06-t2-after.set > /tmp/56-06-t2-lost.txt; comm -13 /tmp/56-06-t2-before.set /tmp/56-06-t2-after.set > /tmp/56-06-t2-gained.txt; printf 'lost='; wc -l /tmp/56-06-t2-lost.txt; printf 'gained='; wc -l /tmp/56-06-t2-gained.txt; cat /tmp/56-06-t2-lost.txt</automated>
    <fails_when>EXIT is anything but 0. Or the lost list holds a name the task notes did not record as an intended removal. Or the gained list holds a name the task notes did not record as a D-03 rename. Or the lost list is empty.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && printf 'structure_proxy_left='; grep -ac 'test("structure/proxy' stock-dispatch.test.ts || true; printf 'chan04_case='; grep -ac 'CHAN-04' stock-dispatch.test.ts || true</automated>
    <fails_when>structure_proxy_left is anything but 0. Or chan04_case is 0, which means the concurrent-dispatch behavioural case was lost.</fails_when>
    <automated>npm --prefix "$(git rev-parse --show-toplevel)/src/mcp/vice" run typecheck > /tmp/56-06-t2-tc.txt 2>&1; echo "EXIT=$?"; tail -20 /tmp/56-06-t2-tc.txt</automated>
    <fails_when>EXIT is anything but 0, or the output names any file under src/mcp/vice.</fails_when>
  </verify>
  <acceptance_criteria>
    - No case name beginning "structure/proxy" remains in the file.
    - The post-cut TAP name set loses only names the task notes record, and gains only a name the
      task notes record as a D-03 rename.
    - The CHAN-04 concurrent-dispatch case is present in the post-cut TAP set.
    - `npm run typecheck` exits 0.
    - The commit stages exactly one path, src/mcp/vice/stock-dispatch.test.ts.
  </acceptance_criteria>
  <done>The structure/proxy region is gone and the source constants it fed are gone with it.</done>
</task>
<task type="auto">
  <name>Task 3: The two remaining scanning cases, the import, and the full automated gate</name>
  <files>src/mcp/vice/stock-dispatch.test.ts</files>
  <read_first>
    - src/mcp/vice/stock-dispatch.test.ts lines 2530 to 2560 (the fork-forwarding structural case
      and its own source read), lines 3230 to 3290 (the WR-13 invariant case and the helper
      nonCommentLines), and the file's import block.
    - 56-RESEARCH.md § "Open Questions" (the symbol-anchored scan cannot see a raw readFileSync
      case) and § Q3's verdict on the CHAN-04 case.
    - 56-CONTEXT.md § D-16 (what the SUMMARY must carry).
  </read_first>
  <action>
    Capture the BEFORE TAP name set to /tmp/56-06-t3-before.set.

    Hand-read and remove these two cases WHOLE under D-01, by verbatim name:
      1. structure: stock-dispatch.ts contains zero CODE references to the fork-forwarding function's name, pairing the vice-proxy.ts structural assertion above with this module's own
      2. invariant (WR-13): no shipped module hardcodes a fork-provides refusal claim

    KEEP the CHAN-04 concurrent-dispatch case. RESEARCH Q3 measured it as a real channel-lock
    refusal test with no source-text assertion.

    Then run the two blind-spot passes over the WHOLE file one more time. Pass one: grep for
    readFileSync of a sibling .ts or .mts and for any remaining module-scope constant whose name
    ends in _SOURCE. Pass two: skim every surviving case NAME for source-reading prose such as
    "structure", "STRUCTURAL", "invariant", "non-vacuity" or "imports nothing". Hand-read every
    remaining candidate and apply the cut rule. Record each extra removal with its verbatim name.

    Remove the helper nonCommentLines and any other module-scope binding whose only callers are
    now gone. grep the whole file for each identifier first. Remove the doomed module import.

    Stage with git add src/mcp/vice/stock-dispatch.test.ts and commit that one path. Run
    git status --porcelain before the commit and confirm the eight untouchable paths are unstaged.

    Then run the full automated suite once for the whole plan. The measured floor, taken live on
    2026-09-14 before any Phase 56 edit, is EXIT=0 with fail 0. Compare the outcome as a SET of
    failing names, never as a count. If a file outside this plan turns red, that measurement IS
    the finding. Report it and stop.

    Finally write the SUMMARY. It must carry every case name removed in this plan VERBATIM, each
    with a one-line reason it qualified, plus a separate line for the D-03 rename recording both
    the old and the new name.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && node --test --test-reporter=tap stock-dispatch.test.ts > /tmp/56-06-t3-after.tap 2>&1; echo "EXIT=$?"; grep -aE '^ok [0-9]+ - ' /tmp/56-06-t3-after.tap | sed -E 's/^ok [0-9]+ - //' | sort > /tmp/56-06-t3-after.set; comm -23 /tmp/56-06-t3-before.set /tmp/56-06-t3-after.set > /tmp/56-06-t3-lost.txt; comm -13 /tmp/56-06-t3-before.set /tmp/56-06-t3-after.set > /tmp/56-06-t3-gained.txt; printf 'lost='; wc -l /tmp/56-06-t3-lost.txt; printf 'gained='; wc -l /tmp/56-06-t3-gained.txt; cat /tmp/56-06-t3-lost.txt</automated>
    <fails_when>EXIT is anything but 0. Or gained reports any non-zero count. Or the printed lost list holds a name the task notes did not record as an intended removal. Or the lost list is empty.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && printf 'doomed_import='; grep -ac 'from "./shipped-modules.ts"' stock-dispatch.test.ts || true; printf 'doomed_calls='; grep -av '^\s*[*/]' stock-dispatch.test.ts | grep -acE '(codeOnly|shippedTsModules|extractCommentSpans|commentByteTotal|shippedScanSurface) *\(' || true; printf 'source_consts='; grep -acE '^const [A-Z_]+_SOURCE' stock-dispatch.test.ts || true; printf 'raw_src_reads='; grep -av '^\s*[*/]' stock-dispatch.test.ts | grep -acE 'readFileSync\([^)]*"[a-z0-9.-]+\.(ts|mts)"' || true</automated>
    <fails_when>doomed_import is anything but 0. Or doomed_calls is anything but 0. Or source_consts is anything but 0. Or raw_src_reads is anything but 0.</fails_when>
    <automated>npm --prefix "$(git rev-parse --show-toplevel)/src/mcp/vice" run typecheck > /tmp/56-06-t3-tc.txt 2>&1; echo "EXIT=$?"; tail -20 /tmp/56-06-t3-tc.txt</automated>
    <fails_when>EXIT is anything but 0, or the output names any file under src/mcp/vice.</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && npm run test:automated > /tmp/56-06-t3-gate.txt 2>&1; echo "EXIT=$?"; grep -aE '^.? ?(tests|pass|fail|skipped) [0-9]+$' /tmp/56-06-t3-gate.txt; grep -aE '^not ok |✖ ' /tmp/56-06-t3-gate.txt || true</automated>
    <fails_when>EXIT is anything but 0. Or the summary block reports a fail count other than 0. Or any failing-test line is printed. Or the summary block prints nothing, which means the grep anchors missed the reporter format and the run was not read.</fails_when>
  </verify>
  <acceptance_criteria>
    - No module-scope constant whose name ends in _SOURCE remains in the file.
    - No surviving code line reads a sibling .ts or .mts with readFileSync.
    - `grep -ac 'from "./shipped-modules.ts"' stock-dispatch.test.ts` returns 0.
    - The CHAN-04 concurrent-dispatch case is present in the post-cut TAP set.
    - `npm run typecheck` exits 0 and `npm run test:automated` exits 0 with fail 0.
    - The SUMMARY lists every case removed in this plan verbatim, each with a one-line reason, and
      records the D-03 rename with both names.
    - The commit stages exactly one path, src/mcp/vice/stock-dispatch.test.ts.
  </acceptance_criteria>
  <done>stock-dispatch.test.ts asserts on no source text anywhere, carries no doomed import, and
  the whole automated suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer working tree to git index | This plan commits in a tree holding eight unrelated in-flight edits owned by the user. |
| in-scope test file to untouchable production file | This test file READS `anno-tools.ts`, which is on the untouchable list. |

## STRIDE Threat Register

ASVS level 1. Block on high. RESEARCH assessed this phase as touching no authentication, no
session, no access control, no input validation and no cryptography surface. It removes test code
in a private developer-tool tree. That assessment is recorded honestly rather than padded. The one
genuine high-severity hazard is destructive-action, not attacker-driven.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-01 | Tampering | `git add` in every committing task | high | mitigate | Stage by explicit path only. `anno-tools.ts` is read by this test file and must never be staged. Confirm with `git status --porcelain` before each commit. |
| T-56-02 | Tampering | the cut, against test coverage integrity | high | mitigate | The per-file TAP name SET diff gates every task. A name may disappear only when the task notes recorded it. A name may appear only as a recorded D-03 rename. |
| T-56-07 | Tampering | the D-02 judgement on this file's mixed cases | high | mitigate | Every surviving assertion in a stripped case is kept byte-for-byte and reviewed in `git diff`. D-04 forbids the retyping route. |
| T-56-SC | Tampering | npm, pip and cargo installs | low | accept | This plan runs no package-manager install and adds no dependency. RESEARCH § "Package Legitimacy Audit" records the same finding. No package-legitimacy checkpoint is owed. |
</threat_model>

<verification>
1. `npm --prefix src/mcp/vice run typecheck` exits 0.
2. `cd src/mcp/vice && npm run test:automated` exits 0 and reports fail 0.
3. `grep -c 'from "./shipped-modules.ts"' src/mcp/vice/stock-dispatch.test.ts` returns 0.
4. `grep -cE '^const [A-Z_]+_SOURCE' src/mcp/vice/stock-dispatch.test.ts` returns 0.
5. The three per-task TAP name-set diffs lost only the recorded names, and gained only the one
   recorded rename.
6. `git log --name-only` for this plan's commits names only src/mcp/vice/stock-dispatch.test.ts
   and the SUMMARY.
</verification>

<success_criteria>
- SC-1 progress: stock-dispatch.test.ts no longer names the doomed module.
- SC-2: every behavioural case survives, proved by the TAP name-set diffs, including the one
  mixed case's assertions kept byte-for-byte.
- SC-3: the automated suite and typecheck both exit 0.
- SC-4: the file is neither empty nor setup-only.
</success_criteria>

## Artifacts this phase produces

This phase's net effect is DELETION. It adds no production symbol, no module and no test helper.
A drift-verification pass must not read an absent symbol as an unplanned omission. The one name
this plan does introduce is a RENAME of an existing case under D-03, recorded in the SUMMARY with
both its old and its new form.

What this plan produces:

- .planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-06-SUMMARY.md
- /tmp/56-06-t1-lost.txt, /tmp/56-06-t2-lost.txt and /tmp/56-06-t3-lost.txt, the measured TAP
  name-set diffs. These are run evidence and are not committed.

<output>
Create `.planning/phases/56-remove-shipped-modules-ts-and-its-embedded-source-scans/56-06-SUMMARY.md` when done.
</output>

