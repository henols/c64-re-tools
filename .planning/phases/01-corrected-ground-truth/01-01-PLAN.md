---
phase: 01-corrected-ground-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/phase0-binmon-findings.md
  - docs/stock-vice-parity.md
autonomous: true
requirements: [DOC-01, DOC-02]

must_haves:
  truths:
    - "A reader of docs/phase0-binmon-findings.md learns that checkpoint conditions use RL/CY, uppercase, and that LIN/CYC are rejected by the condition lexer with 0x8f"
    - "A reader of docs/stock-vice-parity.md learns the same RL/CY fact where it discusses raster/cycle-precise conditions"
    - "Neither document asserts that pause-on-demand requires a checkpoint"
    - "Neither document asserts that REGISTERS_GET cannot source a stopwatch"
    - "Neither document asserts that CPU history's compile flag is the availability risk; both name VICE >= 3.10 as the real gate"
    - "docs/phase0-binmon-findings.md section 4 names five unsolicited event types, not three"
  artifacts:
    - path: "docs/phase0-binmon-findings.md"
      provides: "Corrected normative binary-monitor protocol findings (sections 1 and 4)"
      contains: "RL"
    - path: "docs/stock-vice-parity.md"
      provides: "Corrected two-way parity gap analysis (A.3, A.5, B.1, B.4)"
      contains: "RL"
  key_links:
    - from: "docs/phase0-binmon-findings.md"
      to: "VICE source citations (monitor_binary.c:281, monitor.c:395, mon_lex.l:559-560, mon_parse.y:168, monitor.c:1597, mon_register6502.c:57)"
      via: "inline source citation on every corrected claim"
      pattern: "mon_lex\\.l|monitor_binary\\.c|mon_parse\\.y"
    - from: "docs/stock-vice-parity.md B.1"
      to: "the VICE >= 3.10 version gate"
      via: "explicit caveat naming the version"
      pattern: "3\\.10"
---

<objective>
Correct the four verified factual errors, plus the unsolicited-event undercount, in the
two normative protocol documents: `docs/phase0-binmon-findings.md` (NORMATIVE on the
binary-monitor wire format per user resolution W2) and `docs/stock-vice-parity.md`
(authoritative on chip-state read-back per resolution W1).

Purpose: every downstream phase derives protocol design from these two files. A checkpoint
condition written on `LIN` instead of `RL` fails at runtime with `0x8f` and the parser
returns no diagnostic over the socket. Phase 2's event-demux design (PROTO-03) is derived
from section 4, which currently undercounts unsolicited event types by 40%.

Output: two corrected markdown documents. No code is touched by this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-corrected-ground-truth/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct section 1 of docs/phase0-binmon-findings.md (errors c and d, plus the RL/CY omission)</name>
  <files>docs/phase0-binmon-findings.md</files>
  <read_first>
    - `docs/phase0-binmon-findings.md` — the file being modified; section 1 is lines 9-24 today
    - `.planning/phases/01-corrected-ground-truth/01-RESEARCH.md` — "Error Inventory" items (a), (c), (d). These carry the exact current wrong text and the drafted, source-cited replacement wording. Use them as the copy-source; do not re-derive from VICE source
    - `./CLAUDE.md` — Constraints section. It already states the CORRECTED facts (RL/CY, no operator precedence, hex-by-default literals, VICE >= 3.10 for CPUHISTORY_GET) in user-approved wording. This is the second copy-source
    - `.planning/todos/pending/2026-08-11-correct-phase0-binmon-findings-three-verified-errors.md` — the drafted corrections for errors 1-3 with VICE source line citations against `VICE-Team/svn-mirror @ e50d42c`
    - `.planning/ROADMAP.md` Phase 7 section and Notes — needed to avoid the overcorrection described in `<action>` below
  </read_first>
  <action>
Rewrite section 1 ("Cycle stopwatch — RESOLVED: yes, via CPU history") of
`docs/phase0-binmon-findings.md` to remove three defects and add one missing fact.

**Error (c) — remove the "cannot be a stopwatch" conclusion.** The current final sentence of
the first bullet reads "So `REGISTERS_GET` (0x31) cannot be a stopwatch." Replace it. The
corrected fact: `LIN` and `CYC` ARE present in `mon_reg_list_6510` and `REGISTERS_GET` (0x31)
does return them (`mon_register6502.c:57`). What survives is that neither is monotonic and
`CYC` is cycle-within-raster-line. Absolute cycles CAN be reconstructed without CPU history as
`cycles = frames * 19656 (PAL) + delta(LIN * 63 + CYC)`, with the frame count read from a
non-stopping exec checkpoint at `$EA31` whose `hit_count` sits at bytes 13-16 of the
`CHECKPOINT_INFO` (0x11) response. Carry the cost warning with it: every hit of a non-stopping
checkpoint fires a synchronous `CHECKPOINT_INFO` from inside the CPU loop
(`mon_breakpoint.c:439-535`), so a wide-range non-stopping checkpoint is dangerous.

**Do not overcorrect.** The reconstruction route does NOT replace the CPU-history stopwatch —
the two coexist and Phase 7 picks between them with measured socket cost (see ROADMAP Phase 7
notes and `.planning/notes/stock-vice-migration-revised-loss-ledger.md`). State the fact that
`REGISTERS_GET` can source cycle data; do not re-litigate Phase 7's routing decision and do not
write anything that reads as "CPU history is unnecessary now". That would be a fifth error.

**Error (d) — replace the VERIFY bullet's compile-flag framing.** The current bullet claims CPU
history is a compile-time feature and that a build without it is the risk. The corrected fact is
two-part. Part A: `--enable-cpuhistory` is on by default (`configure.ac:120,521`) and Debian,
Ubuntu, Homebrew and official VICE CI all build with it — the compile flag is not the risk.
Part B: the real gate is the VICE version. `e_MON_CMD_CPUHISTORY_GET = 0x86` exists only in
**VICE >= 3.10** (VICE manual section 13, "Minimum VICE version: 3.10"). Debian trixie/forky/sid
and all current Ubuntu ship 3.9, whose `monitor_binary.c` has no `0x86` case at all. Name the two
distinguishable failures: opcode absent entirely (VICE < 3.10) returns `INVALID_TYPE` (`0x83`);
opcode present but the feature genuinely disabled on a >= 3.10 build returns `CMD_FAILURE`
(`0x8f`). Detect capability via `VICE_INFO` (0x85)'s 4-byte version quad, never the SVN revision
field, which is zeros in distro builds. Keep the pointer to the probe but reframe it: the probe
records WHICH error code appears, it does not decide whether the feature was compiled in.

**Error (a) — add the missing RL/CY fact.** Section 1 currently discusses `e_Cycle`/`e_Rasterline`
and `LIN`/`CYC` but never tells the reader which tokens the CONDITION parser accepts. Add new
prose adjacent to that discussion stating: checkpoint *conditions* use the pseudo-registers `RL`
(raster line) and `CY` (cycle within line), **uppercase only** — not the `REGISTERS_GET` names
`LIN`/`CYC`, which lex as `BANKNAME` in `COND_MODE` and produce a syntax error (`0x8f`, no
diagnostic body) (`mon_lex.l:559-560`). Conditions have no operator precedence
(`mon_parse.y:168`), so `RL == $64 && CY == $14` parses as `(((RL==$64) && CY) == $14)` and is
always false — parenthesise every comparison. Bare integer literals are hex by default
(`monitor.c:1597`), so `RL == 100` means line 256, not 100. `CLAUDE.md` states this in
user-approved wording; copy it.

Keep every corrected claim source-cited inline in the same style the file already uses. Keep the
existing document voice, heading structure and table formatting.

**Gate hygiene constraint:** do not re-quote the removed wording anywhere in the file, including
in any errata or "previously stated" note. The zero-count assertions below grep the whole file
and a verbatim re-quote would silently invalidate them. Paraphrase if you must refer to what
changed.
  </action>
  <acceptance_criteria>
    - `grep -cF 'cannot be a stopwatch' docs/phase0-binmon-findings.md` outputs `0`
    - `grep -cF 'compile-time' docs/phase0-binmon-findings.md` outputs `0`
    - `grep -cE 'VICE (>=|≥) 3\.10' docs/phase0-binmon-findings.md` outputs a value >= 1
    - `grep -cF '0x83' docs/phase0-binmon-findings.md` outputs a value >= 1 AND `grep -cF '0x8f' docs/phase0-binmon-findings.md` outputs a value >= 1 (both error codes named)
    - ``grep -cF '`RL`' docs/phase0-binmon-findings.md`` outputs a value >= 1 AND ``grep -cF '`CY`' docs/phase0-binmon-findings.md`` outputs a value >= 1
    - `grep -cF 'mon_lex.l' docs/phase0-binmon-findings.md` outputs a value >= 1 (the RL/CY claim is source-cited)
    - `grep -ciE 'cpu history is (now )?(unnecessary|no longer needed|redundant)' docs/phase0-binmon-findings.md` outputs `0` (no overcorrection)
    - Section 1 still names `CPUHISTORY_GET` (0x86) as a stopwatch route: `grep -cF 'CPUHISTORY_GET' docs/phase0-binmon-findings.md` outputs a value >= 1
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; cd "$(git rev-parse --show-toplevel)"; test "$(grep -cF "cannot be a stopwatch" docs/phase0-binmon-findings.md)" = 0; test "$(grep -cF "compile-time" docs/phase0-binmon-findings.md)" = 0; test "$(grep -cE "VICE (>=|≥) 3\.10" docs/phase0-binmon-findings.md)" -ge 1; test "$(grep -cF "0x83" docs/phase0-binmon-findings.md)" -ge 1; test "$(grep -cF "0x8f" docs/phase0-binmon-findings.md)" -ge 1; test "$(grep -cF "\`RL\`" docs/phase0-binmon-findings.md)" -ge 1; test "$(grep -cF "\`CY\`" docs/phase0-binmon-findings.md)" -ge 1; test "$(grep -cF "mon_lex.l" docs/phase0-binmon-findings.md)" -ge 1; echo TASK1-OK'</automated>
  </verify>
  <done>Section 1 of `docs/phase0-binmon-findings.md` no longer contains the "cannot be a stopwatch" conclusion or the compile-flag framing, names VICE >= 3.10 as the CPUHISTORY_GET gate with the 0x83-vs-0x8f distinction, and names `RL`/`CY` as the condition-parser pseudo-registers with the precedence and hex-literal traps, each source-cited.</done>
</task>

<task type="auto">
  <name>Task 2: Correct section 4 of docs/phase0-binmon-findings.md (error b, plus the three-to-five event-type undercount)</name>
  <files>docs/phase0-binmon-findings.md</files>
  <read_first>
    - `docs/phase0-binmon-findings.md` — the file being modified; section 4 is lines 43-51 today, and "The one empirical step left" is lines 96-102
    - `.planning/phases/01-corrected-ground-truth/01-RESEARCH.md` — "Error Inventory" item (b) with the exact current text and the drafted `monitor_binary.c:281` / `monitor.c:395` correction, and "Other Repetitions" item 4 for the event-count fix
    - `./CLAUDE.md` — Constraints section, which states all five unsolicited event types and the shared-response-type hazard in user-approved wording; this is the copy-source
    - `.planning/ROADMAP.md` Phase 2 Notes — PROTO-03/PROTO-04 derive from this section; the corrected text must be consistent with what those requirements assume
  </read_first>
  <action>
Rewrite section 4 ("Pause / run model — async events, no bare pause") of
`docs/phase0-binmon-findings.md`.

**Error (b) — delete the "no pause-now opcode" claim.** The current second bullet asserts there
is no "pause now" opcode, that stopping a free-running machine on demand requires a temporary
checkpoint or opening the monitor, and calls this "the one real ergonomic wrinkle for
`vice_execution_pause`". All three statements are wrong. The corrected fact:
`monitor_check_binary()` calls `monitor_startup_trap()` on **any inbound byte**
(`monitor_binary.c:281`), and that check is invoked every vsync from `monitor_vsync_hook`
(`monitor.c:395`). A bare `PING` (0x81) therefore halts the machine within roughly one frame and
emits `STOPPED` (0x62). No temporary checkpoint is required. Add the useful corollary: if there
is no vsync because the host UI is paused or hung, the command times out, which is itself a
`vice-wedge-triage` diagnostic signal rather than a protocol failure. `EXIT` (0xaa) still resumes;
keep that sentence.

Rename the section heading so it no longer advertises the retracted claim. "Pause / run model —
async events, no bare pause" must not survive; use a heading that states the corrected model
(for example, naming that any inbound byte halts the machine).

**Event-type undercount — three becomes five.** The first bullet currently lists only `STOPPED`
(0x62), `RESUMED` (0x63) and `JAM` (0x61). Correct it to five unsolicited message types, all
arriving at request-id `0xffffffff`: `STOPPED` (0x62), `RESUMED` (0x63), `JAM` (0x61),
`CHECKPOINT_INFO` (0x11) emitted on every checkpoint hit, and `REGISTER_INFO` (0x31) emitted on
every monitor open. State the hazard explicitly, because Phase 2's PROTO-03 exists to handle it:
the last two **share a response type with a legitimate command reply**, so a demultiplexer must
key on request-id and must never resolve a pending request with an event. Add the second
correctness fact from `CLAUDE.md`: `JAM` (0x61) has a **zero-length body** —
`monitor_binary.c:384-394` computes the PC then passes `length = 0`, so no PC is sent, and every
surveyed client that assumes 2 bytes breaks on it. The existing text says `STOPPED`/`RESUMED`
carry a 2-byte PC body; keep that and contrast it with `JAM`.

Leave the "The one empirical step left" section alone in this plan — plan `01-04` rewrites it
after the probe has actually run. Do not pre-empt it.

**Gate hygiene constraint:** do not re-quote the removed wording ("no \"pause now\" opcode",
"ergonomic wrinkle") anywhere in the file, including in errata notes. The zero-count assertions
below grep the whole file.
  </action>
  <acceptance_criteria>
    - `grep -cF 'ergonomic wrinkle' docs/phase0-binmon-findings.md` outputs `0`
    - `grep -ciF 'no bare pause' docs/phase0-binmon-findings.md` outputs `0`
    - `grep -cF 'pause now' docs/phase0-binmon-findings.md` outputs `0`
    - `grep -cF 'CHECKPOINT_INFO' docs/phase0-binmon-findings.md` outputs a value >= 1 AND `grep -cF 'REGISTER_INFO' docs/phase0-binmon-findings.md` outputs a value >= 1
    - `grep -cF 'monitor_startup_trap' docs/phase0-binmon-findings.md` outputs a value >= 1 (the corrected pause claim is source-cited)
    - `grep -ciE 'zero.length body|length = 0' docs/phase0-binmon-findings.md` outputs a value >= 1 (the JAM body fact is present)
    - `grep -cF '0xffffffff' docs/phase0-binmon-findings.md` outputs a value >= 1 (event request-id still named)
    - The count of unsolicited event types stated in section 4 is five, verifiable by reading the section; all five opcodes `0x61`, `0x62`, `0x63`, `0x11`, `0x31` appear in it
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; cd "$(git rev-parse --show-toplevel)"; test "$(grep -cF "ergonomic wrinkle" docs/phase0-binmon-findings.md)" = 0; test "$(grep -ciF "no bare pause" docs/phase0-binmon-findings.md)" = 0; test "$(grep -cF "pause now" docs/phase0-binmon-findings.md)" = 0; test "$(grep -cF "CHECKPOINT_INFO" docs/phase0-binmon-findings.md)" -ge 1; test "$(grep -cF "REGISTER_INFO" docs/phase0-binmon-findings.md)" -ge 1; test "$(grep -cF "monitor_startup_trap" docs/phase0-binmon-findings.md)" -ge 1; test "$(grep -ciE "zero.length body|length = 0" docs/phase0-binmon-findings.md)" -ge 1; echo TASK2-OK'</automated>
  </verify>
  <done>Section 4 states that any inbound byte halts the machine via `monitor_startup_trap()` (no checkpoint needed), lists all five unsolicited event types with the shared-response-type hazard named, and records that `JAM` has a zero-length body.</done>
</task>

<task type="auto">
  <name>Task 3: Correct docs/stock-vice-parity.md (A.3 dissolves, A.5 premise, B.1 version gate, B.4 names RL/CY)</name>
  <files>docs/stock-vice-parity.md</files>
  <read_first>
    - `docs/stock-vice-parity.md` — the file being modified. Current locations: section A item 3 (on-demand pause) at lines 33-36, section A item 5 (`vice_cycles_stopwatch`) at lines 43-46, section B item 1 (CPU instruction-history trace) at lines 67-71, section B item 4 (raster/cycle-precise conditions) at lines 79-81
    - `.planning/phases/01-corrected-ground-truth/01-RESEARCH.md` — Error Inventory items (a), (b), (c), (d), each of which names the corresponding location in this file
    - `docs/phase0-binmon-findings.md` — the sibling document corrected in Tasks 1 and 2; the two must not contradict each other
    - `./CLAUDE.md` — Constraints section, copy-source for the RL/CY wording and the VICE >= 3.10 gate
  </read_first>
  <action>
Apply four corrections to `docs/stock-vice-parity.md`.

**Location note (the research file mislabels one of these).** `01-RESEARCH.md` refers to the
RL/CY omission as living in "section A.4". It does not. The text it quotes — "conditions can
reference the raster-line / cycle-in-line pseudo-registers" — is **section B item 4**
("Raster/cycle-precise checkpoint conditions"), in the gains list, not the losses list. Correct
B.4, not A.4. The other three location references in the research file (A.3, A.5, B.1) are
accurate.

**A.3 — on-demand pause is not a loss; remove it from the losses list.** Item 3 currently claims
there is no "stop now" opcode, that halting happens only on a checkpoint hit or a JAM, and
recommends emulating pause with a temporary checkpoint. Per `monitor_binary.c:281` and
`monitor.c:395`, any inbound byte triggers `monitor_startup_trap()` via the vsync hook, so a bare
`PING` halts the machine within roughly one frame. `vice_execution_pause` is not degraded on
stock. Delete this item from the "What can't be replicated exactly (losses)" list. The list is
explicitly numbered and ranked by impact — either renumber items 4-7 to stay contiguous, or leave
a short explicit note that the numbering is non-contiguous because item 3 was retired on
2026-08-12. Pick one and be consistent; do not leave a silently broken sequence. Also update the
section A preamble or the document's "Net:" summary if either counts the losses, so the count
still matches the list.

**A.5 — fix the stopwatch loss premise.** Item 5 (`vice_cycles_stopwatch`) currently frames the
loss as conditional on CPU history being compiled into the build. Change the condition to
"conditional on VICE >= 3.10 for the CPU-history route", and note that a second, always-available
route now exists: `LIN`/`CYC` from `REGISTERS_GET` plus a frame count, reconstructed client-side
(with the synchronous-event cost warning). Keep the surviving part of the loss, which is real:
there is no hardware stopwatch reset, so "atomic reset_and_read" becomes client-side baseline
math.

**B.1 — add the missing version gate.** Item 1 (CPU instruction-history trace, `CPUHISTORY_GET`
0x86) presents the capability unconditionally. Add the caveat: it requires **VICE >= 3.10**;
Debian and all current Ubuntu ship 3.9 and lack the opcode entirely, returning `INVALID_TYPE`
(`0x83`); a >= 3.10 build with the feature genuinely disabled returns `CMD_FAILURE` (`0x8f`);
detect via `VICE_INFO` (0x85)'s version quad, never the SVN revision. This is the milestone's
headline gain and it is unavailable on the most common `apt install vice` path — say so.

**B.4 — name the pseudo-registers.** Item 4 currently says only "conditions can reference the
raster-line / cycle-in-line pseudo-registers" without naming them. Replace with the named tokens:
`RL` (raster line) and `CY` (cycle within line), uppercase only — NOT the `REGISTERS_GET` names
`LIN`/`CYC`, which lex as `BANKNAME` in `COND_MODE` and fail with `0x8f` and no diagnostic
(`mon_lex.l:559-560`). Include the two adjacent traps: no operator precedence
(`mon_parse.y:168`), so every comparison must be parenthesised; and bare integer literals are hex
by default (`monitor.c:1597`), so `RL == 100` means line 256.

Preserve the document's existing voice, its numbered-list structure and its "Net:" framing.

**Gate hygiene constraint:** do not re-quote the removed wording ("stop now", "requires CPU
history compiled") anywhere in the file, including in errata notes. The zero-count assertions
below grep the whole file.
  </action>
  <acceptance_criteria>
    - `grep -cF 'stop now' docs/stock-vice-parity.md` outputs `0`
    - `grep -cF 'CPU history compiled' docs/stock-vice-parity.md` outputs `0`
    - ``grep -cF '`RL`' docs/stock-vice-parity.md`` outputs a value >= 1 AND ``grep -cF '`CY`' docs/stock-vice-parity.md`` outputs a value >= 1
    - `grep -cE 'VICE (>=|≥) 3\.10' docs/stock-vice-parity.md` outputs a value >= 1
    - `grep -cF 'mon_lex.l' docs/stock-vice-parity.md` outputs a value >= 1 (RL/CY claim is source-cited)
    - `grep -cF '0x83' docs/stock-vice-parity.md` outputs a value >= 1 AND `grep -cF '0x8f' docs/stock-vice-parity.md` outputs a value >= 1
    - `grep -cF 'vice_execution_pause' docs/stock-vice-parity.md` outputs `0`, or every remaining occurrence is outside the section A losses list — confirm by reading, since the tool must not appear as a loss
    - The section A numbered list is either contiguous or carries an explicit note that item 3 was retired; verify by reading the rendered list
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; cd "$(git rev-parse --show-toplevel)"; test "$(grep -cF "stop now" docs/stock-vice-parity.md)" = 0; test "$(grep -cF "CPU history compiled" docs/stock-vice-parity.md)" = 0; test "$(grep -cF "\`RL\`" docs/stock-vice-parity.md)" -ge 1; test "$(grep -cF "\`CY\`" docs/stock-vice-parity.md)" -ge 1; test "$(grep -cE "VICE (>=|≥) 3\.10" docs/stock-vice-parity.md)" -ge 1; test "$(grep -cF "mon_lex.l" docs/stock-vice-parity.md)" -ge 1; test "$(grep -cF "0x83" docs/stock-vice-parity.md)" -ge 1; test "$(grep -cF "0x8f" docs/stock-vice-parity.md)" -ge 1; echo TASK3-OK'</automated>
    <human-check>Read the corrected section A losses list end to end and confirm the numbering is coherent after item 3's removal, and that the "Net:" summary at the top still matches the list it summarises.</human-check>
  </verify>
  <done>`docs/stock-vice-parity.md` no longer lists on-demand pause as a loss, frames the stopwatch loss on VICE version rather than a compile flag, states the >= 3.10 gate on the CPU-history gain, and names `RL`/`CY` with their precedence and hex-literal traps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| normative doc -> downstream implementation | `docs/phase0-binmon-findings.md` is NORMATIVE by resolution W2. Every later phase derives protocol design from it. A wrong or overcorrected claim here propagates into runtime code with no further review gate. |

No network, user input, credential, or third-party boundary is touched. This plan edits two
markdown files.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering (integrity of ground truth) | `docs/phase0-binmon-findings.md` section 1 | mitigate | Every corrected claim must carry its VICE source citation inline (`mon_lex.l:559-560`, `monitor_binary.c:281`, `monitor.c:395`, `mon_register6502.c:57`, `mon_parse.y:168`, `monitor.c:1597`). Copy from the already-cited drafts in `01-RESEARCH.md` and the pending todo rather than re-deriving; re-derivation risks introducing a fifth error. Grep assertions in each task enforce that the citations are present. |
| T-01-02 | Tampering (overcorrection) | Error (c) fix in section 1 | mitigate | Explicit negative assertion: the file must not state that CPU history is unnecessary or redundant. Enforced by the `grep -ciE 'cpu history is (now )?(unnecessary\|no longer needed\|redundant)'` gate returning 0, plus a required cross-read of ROADMAP Phase 7, which keeps both stopwatch routes open. |
| T-01-03 | Repudiation (self-invalidating verification) | grep-based acceptance gates | mitigate | The corrected files are forbidden from re-quoting removed wording, including in errata notes, so the zero-count gates cannot be defeated by a "previously we said X" block. Stated as an explicit constraint in all three task actions. |
| T-01-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Not applicable: this plan adds no dependency and runs no package-manager install. `01-RESEARCH.md` confirms the phase introduces no new dependency. No package legitimacy checkpoint is required. |

Honest scope note: this is a documentation-correction plan with no network surface, no user
input handling and no credential handling. A truthful ASVS L1 review of it is short, and the
register above is not padded to look longer.
</threat_model>

<verification>
Run all of this plan's gates together after Task 3:

`grep -cF 'cannot be a stopwatch' docs/phase0-binmon-findings.md` -> 0
`grep -cF 'compile-time' docs/phase0-binmon-findings.md` -> 0
`grep -cF 'ergonomic wrinkle' docs/phase0-binmon-findings.md` -> 0
`grep -cF 'pause now' docs/phase0-binmon-findings.md` -> 0
`grep -lF '`RL`' docs/phase0-binmon-findings.md docs/stock-vice-parity.md` -> both files listed
`grep -cF 'stop now' docs/stock-vice-parity.md` -> 0
`grep -cF 'CPU history compiled' docs/stock-vice-parity.md` -> 0

Non-regression backstop (this plan must not touch any `.ts`/`.mts`/`.mjs` file):
`git diff --name-only | grep -cE '\.(ts|mts|mjs)$'` -> 0
</verification>

<success_criteria>
- `docs/phase0-binmon-findings.md` and `docs/stock-vice-parity.md` both name `RL`/`CY` as the condition-parser pseudo-registers, so a reader following either cannot write a condition on `LIN` and get `0x8f` (phase success criterion 1)
- Neither document asserts that pause-on-demand requires a checkpoint, that `REGISTERS_GET` cannot source a stopwatch, or that CPU history's compile flag is the availability risk; both name VICE >= 3.10 as the real gate (phase success criterion 2, doc half)
- `docs/phase0-binmon-findings.md` section 4 names five unsolicited event types and the shared-response-type hazard, so Phase 2's PROTO-03 design derives from a correct count
- All seven grep gates above pass and no source file was modified
</success_criteria>

<output>
Create `.planning/phases/01-corrected-ground-truth/01-01-SUMMARY.md` when done.
</output>
