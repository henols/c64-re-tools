---
phase: 01-corrected-ground-truth
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/mcp/vice/probe-binmon.mjs
autonomous: true
requirements: [VERIF-01, VERIF-04]

must_haves:
  truths:
    - "probe-binmon.mjs can report the api_version byte from the response header, not just assume it on send"
    - "probe-binmon.mjs can report PALETTE_GET entry count"
    - "probe-binmon.mjs can report whether an 8-byte and a 9-byte CHECKPOINT_SET are both accepted on a given build"
    - "probe-binmon.mjs can report whether a condition on RL/CY is accepted and actually fires, and whether the same condition written on LIN is rejected"
    - "probe-binmon.mjs can report whether Drive8TrueEmulation exists under that name on a given build"
    - "probe-binmon.mjs can report whether MEM_SET into drive ROM is a no-op, an error, or fatal to the emulator"
    - "probe-binmon.mjs can report whether ADVANCE_INSTRUCTIONS emits a RESUMED/STOPPED pair"
    - "probe-binmon.mjs can report one DISPLAY_GET pixel's palette index against the live $D020 border colour register"
    - "probe-binmon.mjs prints the full ordered sequence of unsolicited events observed during the session"
    - "probe-binmon.mjs --selftest verifies every wire-body builder and parser offline, with no emulator running"
  artifacts:
    - path: ".claude/mcp/vice/probe-binmon.mjs"
      provides: "Extended binary-monitor probe covering all of phase success criterion 3 and all five UNVERIFIED items"
      min_lines: 400
      contains: "PALETTE_GET"
  key_links:
    - from: "new probe checks"
      to: "the existing BinMon class and encode() helper"
      via: "mon.send(cmdType, body) — no second framing implementation"
      pattern: "mon\\.send\\("
    - from: "CONDITION_SET check"
      to: "the CHECKPOINT_INFO (0x11) unsolicited event path"
      via: "RESP_NAME extension plus hit_count read from parseCheckpointInfo"
      pattern: "CHECKPOINT_INFO"
---

<objective>
Extend `.claude/mcp/vice/probe-binmon.mjs` so that a single run answers everything phase
success criterion 3 asks for and all five items research flagged UNVERIFIED (criterion 4).

Purpose: VERIF-01 is not "run the existing script". The current script covers roughly 70% of
criterion 3 and 0% of criterion 4 — it has no `PALETTE_GET`, no `CONDITION_SET`, no
`CHECKPOINT_SET`, no `RESOURCE_GET`, no `MEM_SET`, and no pixel check. Every one of the five
UNVERIFIED items needs new probe code before any run can satisfy the criteria.

Output: one extended script, plus an offline `--selftest` mode so its wire encoders are
verifiable without an emulator. This plan does NOT run the probe — plan `01-04` does.

Scope discipline: extend the existing `BinMon` class and `encode()` helper. Do not write a
second framing implementation and do not import broker machinery; the probe connects to an
already-listening port and never launches anything.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-corrected-ground-truth/01-PATTERNS.md
@.planning/phases/01-corrected-ground-truth/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the opcode/event tables, api_version capture, and every wire-body builder and parser, behind an offline --selftest</name>
  <files>.claude/mcp/vice/probe-binmon.mjs</files>
  <read_first>
    - `.claude/mcp/vice/probe-binmon.mjs` — the file being extended, in full. Note its existing shape: `STX`/`API`/`EVENT_ID` constants (lines 23-25), `CMD`/`RESP_NAME`/`ERR_NAME` tables (lines 27-51), `parseTarget()` (53-58), `encode()` (60-68), the `BinMon` class with `_onData` demux (70-129), `newestCycleFromHistory()` (136-145) and `main()` (147-284)
    - `.planning/phases/01-corrected-ground-truth/01-PATTERNS.md` — the "Pattern Assignments" section. It contains concrete, cross-checked source for every table addition and every body builder and parser named below. Copy these; do not re-derive layouts
    - `.planning/intel/constraints.md` — `CON-wire-request-header`, `CON-wire-response-header`, `CON-command-opcode-set`, `CON-error-codes`. Every opcode used below must match `CON-command-opcode-set`
    - `./CLAUDE.md` — Conventions section: 2-space indent, double quotes, semicolons, camelCase verb-first functions, SCREAMING_SNAKE_CASE module constants, header comment explaining WHY the file exists
  </read_first>
  <action>
Extend the constant tables, capture the response header's api_version, and add the request-body
builders and response parsers every later check needs. Contracts first: no check code in this
task.

**Extend `CMD`** with the opcodes the new checks need, keeping the existing entries and the
existing alphabetical-by-value ordering: `MEM_SET` 0x02, `CHECKPOINT_GET` 0x11, `CHECKPOINT_SET`
0x12, `CHECKPOINT_DELETE` 0x13, `CONDITION_SET` 0x22, `RESOURCE_GET` 0x51, `PALETTE_GET` 0x91.
All seven are confirmed in `CON-command-opcode-set`.

**Extend `RESP_NAME`** with `0x11` -> `"CHECKPOINT_INFO"` and `0x31` -> `"REGISTER_INFO"`. Add a
comment on each recording that these response types are shared with a legitimate command reply and
that the demux keys on request-id, so this addition is display-only and must not change
`_onData`'s dispatch logic. `ERR_NAME` already carries every code the new checks need
(`OBJECT_MISSING`, `INVALID_TYPE`, `CMD_FAILURE`) — do not add to it.

**Capture the response api_version.** `_onData` currently reads `respType`, `errCode`, `reqId` and
`body` from the frame but discards `frame[1]`. Phase success criterion 3 asks for the api version
as observed, not as assumed on send. Record `frame[1]` on the `BinMon` instance (for example as
`this.observedApi`, set on the first frame seen) and include it in the object resolved by `send()`
so any check can report it. Do not change the framing, the STX resync, or the request-id demux.

**Add the request-body builders**, each a top-level `function` in the file's existing style,
camelCase and verb-first, matching the exact layouts given in `01-PATTERNS.md`:

- `memGetBody({ sidefx = 0, start, end, memspace = 0x00, bank = 0x0000 })` — 8 bytes:
  sidefx (1), start (u16LE), end (u16LE), memspace (1), bank (u16LE). Always exactly 8 bytes; the
  VICE handler dereferences the body before its length check.
- `memSetBody({ start, end, memspace, data })` — 8 + data.length bytes, same field order as
  `memGetBody` with the payload appended at offset 8.
- `checkpointSetBody({ start, end, stop = 1, enabled = 1, ops = 0x04, temporary = 1, memspace })` —
  8 bytes, or 9 when `memspace` is supplied: start (u16LE), end (u16LE), stop (1), enabled (1), ops
  (1, `0x04` = exec), temporary (1), optional memspace (1). The wire memspace byte is `0x00` for
  main and `0x01`-`0x04` for units 8-11; `0x08` is rejected.
- `conditionSetBody(checkpointNum, expr)` — checkpointNum (u32LE), exprLen (1), expr as ASCII, NOT
  NUL-terminated. Throw before encoding if the ASCII expression exceeds 255 bytes; the length field
  is a uint8 and a silently truncated frame would desync the stream. This bound is the ASVS V5
  control recorded in the threat model below.
- `resourceGetBody(name)` — nameLen (1), name as ASCII.
- `paletteGetBody()` — one byte, `use_vic` = `0x00` on x64sc.

**Add the response parsers**, same style:

- `parsePalette(body)` — count at u16LE offset 0, then per entry `itemSize` (1, expected 3)
  followed by r, g, b; advance by `1 + itemSize` per entry, never a fixed stride. Return
  `{ count, entries }`.
- `parseResource(r)` — on non-zero errCode return a missing marker. Otherwise `body[0] === 0x00`
  means string: length at `body[1]`, ASCII after. `body[0] === 0x01` means int: a **signed** int32LE
  at offset 2. Record in a comment that `OBJECT_MISSING` (0x01) is returned both for "resource does
  not exist" and for "string resource is NULL", and that the two are not distinguishable on the
  wire — this matters for interpreting the `Drive8TrueEmulation` result.
- `parseCheckpointInfo(body)` — checkpointNum (u32LE at 0), currentlyHit (`body[4] === 1`),
  startAddr (u16LE at 5), endAddr (u16LE at 7), hitCount (u32LE at 13), memspace (`body[22]`).
- `parseDisplayGet(body)` — return the geometry AND the pixel buffer. Read `infoLen` as u32LE at
  offset 0, then dw/dh/xo/yo/iw/ih as u16LE at 4/6/8/10/12/14 and bpp at 16. Compute the buffer
  length field position as `4 + infoLen` and the pixel buffer start as `4 + infoLen + 4` — do NOT
  hardcode 17 and 21. The existing check 5 hardcodes those offsets; the ROADMAP Phase 5 note
  explicitly flags that as a defect to avoid, so fix it here while the layout is being touched.

**Add a `--selftest` mode.** Intercept it before any socket work: when `process.argv` contains
`--selftest`, run offline assertions over every builder and parser above and exit 0 on success or
non-zero with a failing-assertion message. It must not open a socket. Make `parseTarget()` ignore
argv entries beginning with `--` so the existing positional `[host] [port]` usage is unaffected.
Assert at minimum: `encode()` produces STX `0x02` at byte 0, api `0x02` at byte 1, body length
u32LE at 2, request id u32LE at 6 and command type at 10; `checkpointSetBody` returns 8 bytes
without a memspace and 9 with one, with the memspace landing in byte 8 and ops defaulting to
`0x04`; `conditionSetBody` writes the checkpoint number as u32LE, the byte length at offset 4, the
ASCII expression from offset 5, and throws on a 256-byte expression; `memSetBody` and `memGetBody`
place every field at the offsets listed above; `resourceGetBody` writes the correct name length;
`paletteGetBody` is a single `0x00` byte; `parsePalette` decodes a synthesised 16-entry buffer to
count 16 with correct RGB on the first and last entry; `parseResource` decodes a synthesised string
body and a synthesised negative-int body; `parseCheckpointInfo` reads a synthesised 23-byte body's
checkpoint number and hit count; `parseDisplayGet` locates the pixel buffer correctly for a
synthesised body whose `infoLen` is deliberately NOT 13, proving the offsets are not hardcoded.

Update the file's header block comment to state the extended scope and the `--selftest` usage,
keeping the existing "WHY this file exists" convention. Keep 2-space indent, double quotes and
semicolons throughout. Add no dependency; `node:net` remains the only import.
  </action>
  <acceptance_criteria>
    - `node --check .claude/mcp/vice/probe-binmon.mjs` exits 0
    - `node .claude/mcp/vice/probe-binmon.mjs --selftest` exits 0 and prints a pass line, with no emulator running and no socket opened
    - `grep -cE '(MEM_SET|CHECKPOINT_GET|CHECKPOINT_SET|CHECKPOINT_DELETE|CONDITION_SET|RESOURCE_GET|PALETTE_GET):' .claude/mcp/vice/probe-binmon.mjs` outputs `7`
    - `grep -cF 'CHECKPOINT_INFO' .claude/mcp/vice/probe-binmon.mjs` outputs a value >= 1 AND `grep -cF 'REGISTER_INFO' .claude/mcp/vice/probe-binmon.mjs` outputs a value >= 1
    - `grep -cE 'function (memGetBody|memSetBody|checkpointSetBody|conditionSetBody|resourceGetBody|paletteGetBody|parsePalette|parseResource|parseCheckpointInfo|parseDisplayGet)' .claude/mcp/vice/probe-binmon.mjs` outputs `10`
    - `grep -c 'from "node:' .claude/mcp/vice/probe-binmon.mjs` outputs `1` (still only `node:net`; no new dependency)
    - `grep -cE 'readUInt32LE\(17\)|subarray\(21' .claude/mcp/vice/probe-binmon.mjs` outputs `0` (DISPLAY_GET buffer offsets are derived from infoLen, not hardcoded)
    - A 256-byte condition expression makes `conditionSetBody` throw; asserted inside `--selftest`
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; cd "$(git rev-parse --show-toplevel)"; F=.claude/mcp/vice/probe-binmon.mjs; node --check $F; node $F --selftest; test "$(grep -cE "(MEM_SET|CHECKPOINT_GET|CHECKPOINT_SET|CHECKPOINT_DELETE|CONDITION_SET|RESOURCE_GET|PALETTE_GET):" $F)" = 7; test "$(grep -cE "function (memGetBody|memSetBody|checkpointSetBody|conditionSetBody|resourceGetBody|paletteGetBody|parsePalette|parseResource|parseCheckpointInfo|parseDisplayGet)" $F)" = 10; test "$(grep -c "from \"node:" $F)" = 1; test "$(grep -cE "readUInt32LE\(17\)|subarray\(21" $F)" = 0; echo TASK1-OK'</automated>
  </verify>
  <done>All seven new opcodes and both new event names are in the tables, the response api_version is captured, all six body builders and all four parsers exist with the layouts from `01-PATTERNS.md`, and `--selftest` proves every one of them offline in under a second.</done>
</task>

<task type="auto">
  <name>Task 2: Add checks 7-12 — palette, pixel-vs-register, 8-vs-9-byte checkpoint, RL/CY condition, drive resource, event pair</name>
  <files>.claude/mcp/vice/probe-binmon.mjs</files>
  <read_first>
    - `.claude/mcp/vice/probe-binmon.mjs` — the file being extended, including the builders and parsers added in Task 1 and the existing numbered checks 1-6 in `main()`
    - `.planning/phases/01-corrected-ground-truth/01-PATTERNS.md` — the "Core request/response pattern, numbered-check style", "Error handling pattern" and "Async/event pattern" sections. Every new check follows the existing try/catch + numbered-log shape and must never let one check's exception abort the run
    - `.planning/phases/01-corrected-ground-truth/01-RESEARCH.md` — "The Five UNVERIFIED Items" table. Items 1, 2, 4 and 5 are answered by this task; each row's "what breaks" text is what plan `01-04` records if a check cannot conclude
    - `./CLAUDE.md` — Constraints section, specifically that condition pseudo-registers are `RL`/`CY` uppercase, that conditions have no operator precedence so every comparison must be parenthesised, that bare integer literals are hex, and that a non-stopping checkpoint on a hot address emits a synchronous frame per hit from inside the CPU loop
  </read_first>
  <action>
Append six new numbered checks to `main()`, continuing the existing numbering from 7. Every check
uses the established shape: try, `await mon.send(...)`, branch on `errCode`, log one aligned
numbered line, catch and log a FAILED line without rethrowing. Record each outcome on the existing
`results` accumulator so the verdict block can summarise it.

**Check 7 — `PALETTE_GET` entry count.** Send `CMD.PALETTE_GET` with `paletteGetBody()`. On success
parse with `parsePalette` and log the entry count and the first entry's RGB. Store the parsed
entries on `results` — check 8 needs them. This is a hard requirement of phase success criterion 3.

**Check 8 — one `DISPLAY_GET` pixel against the live border colour register.** This is the second
half of UNVERIFIED item 5. Do not hardcode a colour from memory of what VICE's defaults are;
research assumption A2 explicitly forbids it. Instead: send `CMD.MEM_GET` with
`memGetBody({ start: 0xd020, end: 0xd021, memspace: 0x00 })` and read the two returned bytes (the
response body is a u16LE length followed by the data) to get the live border and background colour
register values, masking each with `0x0f`. Then send `CMD.DISPLAY_GET` with body `[0x00, 0x00]`,
parse with `parseDisplayGet`, and sample the pixel at debug coordinates x=4, y=4 — four pixels in
from the top-left corner of the debug frame, which is unambiguously border area — using stride
`dw` (the debug width, not the inner width). Assert that the sampled palette index equals the
masked `$D020` value, and log both the index, the expected value, and the RGB that
`results.palette.entries[index]` gives. Also sample the centre of the debug frame and log its index
against the masked `$D021` value as **informational only** — the centre may land on a character
glyph and a mismatch there is not a failure. Log the observed `xo`/`yo`/`iw`/`ih` verbatim, since
the ROADMAP Phase 5 note records that these are degenerate on every VICE machine and the recorded
values are themselves useful evidence.

**Check 9 — 8-byte versus 9-byte `CHECKPOINT_SET`.** This is UNVERIFIED item 1. Send
`checkpointSetBody({ start: 0xea31, end: 0xea31, stop: 1, enabled: 0, ops: 0x04, temporary: 1 })`
with no memspace (8 bytes) and record the error code. Then send the same with `memspace: 0x00`
(9 bytes) and record that error code. Log both side by side — the question the check answers is
whether a build accepts the optional ninth byte or rejects the whole command on length. Set both
checkpoints disabled (`enabled: 0`) and temporary so neither perturbs execution, and delete both
with `CMD.CHECKPOINT_DELETE` afterwards, reading each checkpoint number out of the
`CHECKPOINT_INFO` reply via `parseCheckpointInfo`. Leaking a checkpoint here would poison every
later check.

**Check 10 — `RL`/`CY` conditions: accepted, and actually firing.** This is UNVERIFIED item 4's
answerable half, and it is the empirical proof behind DOC-02. Three parts, all logged:

  (a) **Token differential.** Create an exec checkpoint over the full range `$0000`-`$ffff` with
  `stop: 1`, `enabled: 1`, `temporary: 0` and capture its checkpoint number. Attach
  `conditionSetBody(cpNum, "(RL == $64) && (CY == $14)")` and record the error code. Then attach
  `conditionSetBody(cpNum, "(LIN == $64) && (CYC == $14)")` to the same checkpoint and record ITS
  error code. The expected and decisive result is that the first is accepted and the second fails
  with `0x8f` and an empty body. Log both codes explicitly — this single line is what makes the
  `RL`/`CY` correction empirical rather than source-inferred.

  (b) **Fire test.** Re-attach the correct-token condition, but relax it to `(RL == $64)` alone so
  a hit is reachable within one frame regardless of where in the line execution lands. The
  condition gates the hit, so a full-range exec checkpoint produces exactly one stop rather than a
  storm. Send `CMD.EXIT` to resume, sleep about 500 ms, then send `CMD.CHECKPOINT_GET` for the
  checkpoint number and read `hitCount` via `parseCheckpointInfo`. Log whether the hit count went
  above zero and which unsolicited events arrived in the meantime — expect a `CHECKPOINT_INFO`
  (0x11) and a `STOPPED` (0x62) at request-id `0xffffffff`.

  (c) **Cleanup.** Delete the checkpoint with `CMD.CHECKPOINT_DELETE`. Conditions cannot be read
  back or cleared and leak if the checkpoint survives; a leaked full-range breakpoint left armed
  would break every later check and the plan `01-04` run against the other binary.

**Check 11 — `Drive8TrueEmulation` under that exact name.** This is UNVERIFIED item 2. Send
`CMD.RESOURCE_GET` with `resourceGetBody("Drive8TrueEmulation")` and parse with `parseResource`.
Also query `"Drive8Type"` (the real gate is TDE plus a non-zero drive type) and, only if the first
returns `OBJECT_MISSING`, query the hypothetical older name `"DriveTrueEmulation"` as a fallback
probe. Log all three outcomes. Include in the log line the caveat the parser comment records:
`OBJECT_MISSING` cannot distinguish "resource does not exist" from "string resource is NULL", so a
missing result is suggestive, not conclusive, on its own.

**Check 12 — does `ADVANCE_INSTRUCTIONS` emit a `RESUMED`/`STOPPED` pair?** This answers the
ROADMAP's separately-listed probe addition and feeds phase success criterion 3's "observed
unsolicited event sequence". Snapshot `mon.events.length` immediately before sending
`CMD.ADVANCE_INSTRUCTIONS` for one instruction, sleep about 150 ms, then examine only the newly
appended slice. Log the exact ordered names in that slice and state plainly whether it is a
`RESUMED` followed by a `STOPPED`, only a `STOPPED`, or something else. Do not assert a specific
answer — the point of the check is that the answer is currently unknown.

Every check must tolerate a build that rejects the command: an unexpected error code is a
recorded result, not a crash. Follow the existing alignment style of the numbered log lines.
  </action>
  <acceptance_criteria>
    - `node --check .claude/mcp/vice/probe-binmon.mjs` exits 0 AND `node .claude/mcp/vice/probe-binmon.mjs --selftest` still exits 0
    - `grep -cE '^  // ?(7|8|9|10|11|12)\.' .claude/mcp/vice/probe-binmon.mjs` outputs `6`, or the six new checks are otherwise individually identifiable by their numbered log lines — verify with `grep -cE '"(7|8|9|10|11|12)\.' .claude/mcp/vice/probe-binmon.mjs` returning 6 or more
    - `grep -cF '(RL == $64) && (CY == $14)' .claude/mcp/vice/probe-binmon.mjs` outputs a value >= 1 (correct-token condition, fully parenthesised, hex literals)
    - `grep -cF '(LIN == $64)' .claude/mcp/vice/probe-binmon.mjs` outputs a value >= 1 (negative control present)
    - `grep -cF 'Drive8TrueEmulation' .claude/mcp/vice/probe-binmon.mjs` outputs a value >= 1 AND `grep -cF 'Drive8Type' .claude/mcp/vice/probe-binmon.mjs` outputs a value >= 1
    - `grep -cF 'CHECKPOINT_DELETE' .claude/mcp/vice/probe-binmon.mjs` outputs a value >= 2 (checks 9 and 10 both clean up)
    - `grep -cF '0xd020' .claude/mcp/vice/probe-binmon.mjs` outputs a value >= 1 (pixel check reads the live register instead of hardcoding a colour)
    - `grep -cE '0x0[0-9a-f]{5}|hardcode' .claude/mcp/vice/probe-binmon.mjs` is not used as a check; instead confirm by reading that no RGB triple is hardcoded as an expected colour anywhere in check 8
    - Every new check is individually wrapped in try/catch: reading the six checks shows no `await mon.send` outside a try block
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; cd "$(git rev-parse --show-toplevel)"; F=.claude/mcp/vice/probe-binmon.mjs; node --check $F; node $F --selftest; test "$(grep -cF "(RL == \$64) && (CY == \$14)" $F)" -ge 1; test "$(grep -cF "(LIN == \$64)" $F)" -ge 1; test "$(grep -cF "Drive8TrueEmulation" $F)" -ge 1; test "$(grep -cF "Drive8Type" $F)" -ge 1; test "$(grep -cF "CHECKPOINT_DELETE" $F)" -ge 2; test "$(grep -cF "0xd020" $F)" -ge 1; echo TASK2-OK'</automated>
  </verify>
  <done>Checks 7 through 12 exist, each independently wrapped, each logging a numbered result line; UNVERIFIED items 1, 2, 4 and 5 all have probe code; both the correct-token and the `LIN`/`CYC` negative-control conditions are sent; and every checkpoint the probe creates is deleted before the run ends.</done>
</task>

<task type="auto">
  <name>Task 3: Add check 13 (MEM_SET into drive ROM) last, crash-tolerant, and extend the verdict block with the full event sequence</name>
  <files>.claude/mcp/vice/probe-binmon.mjs</files>
  <read_first>
    - `.claude/mcp/vice/probe-binmon.mjs` — the file being extended, including checks 7-12 from Task 2 and the existing verdict block at lines 277-283 of the original
    - `.planning/phases/01-corrected-ground-truth/01-RESEARCH.md` — "The Five UNVERIFIED Items" row 3, which states this is the one probe that can crash the emulator instance and must be sequenced last against a disposable instance, and the Security Domain table's DoS row
    - `.planning/phases/01-corrected-ground-truth/01-PATTERNS.md` — the "Error handling pattern" section, specifically the note that the surrounding try/catch does not protect against a crashed process (the socket errors or closes) and that this check's catch must treat a closed socket as an informative result rather than a generic FAILED line
    - `./CLAUDE.md` — Constraints section: drive memory access requires `Drive8TrueEmulation` plus a non-zero `Drive8Type`; with TDE off, drive reads return silent zeros rather than an error, so a zero read-back is NOT evidence of a safe no-op unless check 11 confirmed TDE is on
  </read_first>
  <action>
Add the final check and make the run summarise itself for the results document.

**Check 13 — `MEM_SET` into drive ROM.** This is UNVERIFIED item 3 and the only probe that can end
the session. It must be the last check in `main()`, immediately before the `EXIT` and
`socket.end()`.

Gate it on evidence, not assumption: if check 11 did not establish that `Drive8TrueEmulation` is
on and `Drive8Type` is non-zero, log that the drive-ROM write is being skipped because the
precondition is unmet and record that as the result. With TDE off, drive reads return silent zeros
rather than an error, so an unguarded run would produce a meaningless "looks like a no-op" answer.

When the precondition holds: first read the target byte with
`memGetBody({ start: 0xc000, end: 0xc000, memspace: 0x01 })` and record it. Then send `CMD.MEM_SET`
with `memSetBody({ start: 0xc000, end: 0xc000, memspace: 0x01, data: Buffer.from([0xff]) })` and
record the error code. Then read the same byte again and compare. Log one of three conclusions
explicitly: the write was rejected with an error code (name it); the write returned OK and the
byte was unchanged, indicating a silent no-op store stub; or the write returned OK and the byte
changed, meaning drive ROM is writable through the monitor.

Its catch block must distinguish a socket error or premature close from an ordinary failure and
log that the drive-ROM write crashed or hung the target, recording that as the answer to
UNVERIFIED item 3 rather than as a probe defect. Wire memspace `0x01` is unit 8; `0x08` is the
internal enum and is rejected on the wire.

**Make the verdict block usable as evidence.** The existing `=== Phase-0 verdict ===` block prints
connect/ping, version, stopwatch and screenshot. Extend it so a single paste of the probe's stdout
carries everything phase success criterion 3 asks for and every UNVERIFIED item's answer:

- the observed api_version byte from the response header, captured in Task 1
- the VICE version as the raw 4-byte quad and as a dotted string, plus the SVN revision field with
  a note that it is zeros in distro builds and must never be used for capability detection
- the `CPUHISTORY_GET` outcome named as OK, `INVALID_TYPE` `0x83`, or `CMD_FAILURE` `0x8f`
- `DISPLAY_GET` geometry: dw, dh, xo, yo, iw, ih, bpp
- `PALETTE_GET` entry count
- the 8-byte and 9-byte `CHECKPOINT_SET` error codes
- the `RL`/`CY` condition result, the `LIN`/`CYC` negative-control result, and the fire-test hit count
- the `Drive8TrueEmulation`, `Drive8Type` and fallback-name outcomes
- the `ADVANCE_INSTRUCTIONS` event-slice result
- the drive-ROM `MEM_SET` conclusion
- **the full ordered sequence of every unsolicited event observed during the whole session**,
  printed as an arrow-joined list of names from the accumulated `mon.events`. Phase success
  criterion 3 asks for the observed unsolicited event sequence by name; this line is that answer.

The verdict block must print even when check 13 kills the connection. Ensure no throw from check 13
can escape past it — if the socket is gone, the `EXIT` send and `socket.end()` must be tolerated
and the summary still written to stdout.

Retire the trailing advice paragraph about the stopwatch being missing, which is written against
the pre-correction compile-flag framing; replace it with a line naming VICE >= 3.10 as the gate and
pointing at `docs/phase1-probe-results.md` as where the run is recorded.
  </action>
  <acceptance_criteria>
    - `node --check .claude/mcp/vice/probe-binmon.mjs` exits 0 AND `node .claude/mcp/vice/probe-binmon.mjs --selftest` exits 0
    - `grep -cF '0xc000' .claude/mcp/vice/probe-binmon.mjs` outputs a value >= 1 (drive ROM target address)
    - `grep -cF 'MEM_SET' .claude/mcp/vice/probe-binmon.mjs` outputs a value >= 2 (table entry plus the check)
    - Check 13 is the last check before `EXIT`: `grep -n 'CMD.EXIT' .claude/mcp/vice/probe-binmon.mjs` reports a line number greater than the line number of the drive-ROM `MEM_SET` send
    - `grep -ciE 'compile.time|lacks CPU history' .claude/mcp/vice/probe-binmon.mjs` outputs `0` (the pre-correction stopwatch advice is gone)
    - `grep -cF 'phase1-probe-results.md' .claude/mcp/vice/probe-binmon.mjs` outputs a value >= 1
    - `grep -ciE 'event sequence' .claude/mcp/vice/probe-binmon.mjs` outputs a value >= 1 (the ordered event list is printed in the verdict)
    - Running the probe against a port with nothing listening exits non-zero with a connect error and does not hang past the existing 4-second connect timeout
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; cd "$(git rev-parse --show-toplevel)"; F=.claude/mcp/vice/probe-binmon.mjs; node --check $F; node $F --selftest; test "$(grep -cF "0xc000" $F)" -ge 1; test "$(grep -cF "MEM_SET" $F)" -ge 2; test "$(grep -ciE "compile.time|lacks CPU history" $F)" = 0; test "$(grep -cF "phase1-probe-results.md" $F)" -ge 1; test "$(grep -ciE "event sequence" $F)" -ge 1; if node $F 127.0.0.1 6599 >/dev/null 2>&1; then echo "expected connect failure on dead port"; exit 1; fi; echo TASK3-OK'</automated>
  </verify>
  <done>Check 13 runs last, refuses to write when the TDE precondition is unmet, distinguishes rejected / silent-no-op / actually-written, and treats a dead socket as the answer rather than a defect; the verdict block reports every criterion-3 field plus the full ordered unsolicited-event sequence and survives a crashed target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe process -> emulator binary monitor (loopback TCP) | The probe opens a raw TCP connection to a locally-launched `x64sc`. VICE's binary monitor is unauthenticated and unencrypted by design; this is a pre-existing, project-wide accepted posture (deferred item QUAL-03), not something this plan changes. |
| probe-authored command bodies -> VICE command handlers | Every byte the probe sends is constructed from constants written in this file. There is no external or untrusted input anywhere in the script. |
| `MEM_SET` -> emulated drive CPU state | The one write path in the probe. It mutates emulator memory and may, per UNVERIFIED item 3, dereference a NULL store function pointer inside VICE. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Denial of Service (self-inflicted, local) | check 13, `MEM_SET` into drive ROM `$C000` (unit 8) | mitigate | Sequenced last so every other result is already on stdout; gated on check 11 having confirmed TDE plus a non-zero drive type; catch treats socket error or close as the recorded answer; verdict block must still print. Blast radius is one disposable emulator process launched by hand in plan `01-04` — no broker instance is involved. |
| T-01-08 | Denial of Service (emulator thread stall) | check 10's full-range exec checkpoint | mitigate | The checkpoint is created with `stop: 1` and gated by a condition, so at most one hit occurs before the machine halts. `CLAUDE.md` records that a **non-stopping** checkpoint on a hot address emits a synchronous frame per hit from inside the CPU loop and can stall the emulator thread — the probe must never create one. |
| T-01-09 | Tampering (malformed frame desync) | `conditionSetBody` uint8 length field (ASVS V5) | mitigate | Throw before encoding when the ASCII expression exceeds 255 bytes, so a truncated length field can never be written to the wire. Asserted in `--selftest`. |
| T-01-10 | Tampering (leaked emulator state across runs) | checkpoints created by checks 9 and 10 | mitigate | Both checks delete every checkpoint they create. Conditions cannot be read back or cleared and leak with their checkpoint; a surviving full-range armed breakpoint would corrupt the second binary's run in plan `01-04`. Enforced by the `CHECKPOINT_DELETE` count gate. |
| T-01-11 | Information disclosure (control-plane exposure) | binary-monitor TCP port | accept | VICE's binary monitor has no auth. Plan `01-04` binds it to `ip4://127.0.0.1` loopback only. Broader control-plane exposure is deferred item QUAL-03 and explicitly out of this milestone; this plan does not change that posture and must not be read as addressing it. |
| T-01-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Not applicable: the probe adds no dependency. `node:net` remains its only import, asserted by a gate in Task 1. No package legitimacy checkpoint required. |
</threat_model>

<verification>
After all three tasks, with no emulator running:

`node --check .claude/mcp/vice/probe-binmon.mjs` -> exit 0
`node .claude/mcp/vice/probe-binmon.mjs --selftest` -> exit 0, prints a pass line
`grep -c 'from "node:' .claude/mcp/vice/probe-binmon.mjs` -> 1

Non-regression backstop (this plan touches one `.mjs` dev script that is not part of the MCP
server module graph; the existing suite must be unaffected):
`cd .claude/mcp/vice && npm test` -> green
</verification>

<success_criteria>
- The probe has code for every field phase success criterion 3 names: api version, VICE version quad, `CPUHISTORY_GET` outcome with the `0x83`-vs-`0x8f` distinction, `DISPLAY_GET` geometry, `PALETTE_GET` entry count, and the observed unsolicited event sequence
- The probe has code for all five UNVERIFIED items: 9-byte `CHECKPOINT_SET` (item 1), `Drive8TrueEmulation` naming (item 2), `MEM_SET` into drive ROM (item 3), `RL`/`CY` condition acceptance and firing (item 4), and `PALETTE_GET` count plus the pixel-vs-register check (item 5)
- Plus the ROADMAP's separately-listed sixth addition: whether `ADVANCE_INSTRUCTIONS` emits a `RESUMED`/`STOPPED` pair
- `--selftest` proves every wire encoder and parser offline in under a second, so the phase has an automated verification command that does not need an emulator
- No new dependency; no second framing implementation; no broker machinery imported
</success_criteria>

<output>
Create `.planning/phases/01-corrected-ground-truth/01-03-SUMMARY.md` when done.
</output>
