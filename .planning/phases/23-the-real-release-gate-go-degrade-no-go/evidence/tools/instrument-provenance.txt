# Phase 23 — instrument provenance transcript (23-02, the tracer)
#
# Throwaway evidence, not product. Every command below was run for real and its
# stdout/stderr is reproduced immediately under a `$ <command>` line, per the
# binding transcript convention in evidence/README.md § Evidence conventions #1.
# Nothing here is reconstructed from memory and nothing is summarised in place
# of output.
#
# Working directory for cross-wave artifacts (convention #2):
#   PROBE_DIR = /home/henrik/.cache/c64-re-tools/phase23
# Never /tmp — tmpfs on this host, and it already cost the dxa+Ghidra pivot its
# dxa build. Everything that matters is copied into evidence/ before this
# session ends.
#
# Outcome lines (bare NAME: value at column 0) appear in the `## OUTCOME LINES`
# section below, and -- per SCHEMA.md § 3, which declares TOOLS.txt their home --
# identically in evidence/tools/TOOLS.txt. Sections 3 and 4 (the two headless
# Ghidra rehearsals, written by tasks 2 and 3) are appended after it.


## 1. dxa 0.1.5 — fetch, pin, verify, build

The expected digest line is written into evidence BEFORE the fetch is verified,
so the pin cannot be back-filled from whatever arrived. The pin is corroborated
only by the FreeBSD ports devel/dxa65 distinfo: upstream publishes no signature
and no checksum of its own. Recorded as a property of the artefact, not as a
supply-chain reassurance.

$ cat evidence/tools/dxa-0.1.5.tar.gz.sha256   # written before the fetch, HTTPS only
8e40ed77816581f9ad95acac2ed69a2fb2ac7850e433d19cd684193a45826799  dxa-0.1.5.tar.gz

$ curl -fsSL -o dxa-0.1.5.tar.gz https://www.floodgap.com/retrotech/xa/dists/dxa-0.1.5.tar.gz
[exit 0]

$ ls -l dxa-0.1.5.tar.gz
-rw-rw-r-- 1 henrik henrik 37987 26 aug 12.23 dxa-0.1.5.tar.gz
[exit 0]

$ sha256sum -c dxa-0.1.5.tar.gz.sha256
dxa-0.1.5.tar.gz: OK
[exit 0]

$ tar xzf dxa-0.1.5.tar.gz
[exit 0]

$ ls -l dxa-0.1.5
total 160
-rw-r--r-- 1 henrik henrik  1492 26 mar  2022 ChangeLog
-rw-r--r-- 1 henrik henrik 10152 26 mar  2022 dump.c
-rw-r--r-- 1 henrik henrik 12702 25 mar  2022 dxa.1
-rw-r--r-- 1 henrik henrik   750 25 mar  2022 INSTALL
-rw-r--r-- 1 henrik henrik  3466 29 jan  2019 label.c
-rw-r--r-- 1 henrik henrik 17576 25 mar  2022 main.c
-rw-r--r-- 1 henrik henrik   796 25 mar  2022 Makefile
-rw-r--r-- 1 henrik henrik 29718  1 nov  2006 opcodes.h
-rw-r--r-- 1 henrik henrik  6144  1 feb  2019 options.h
-rw-r--r-- 1 henrik henrik  2296  1 nov  2006 proto.h
-rw-r--r-- 1 henrik henrik 25812 25 mar  2022 scan.c
-rw-r--r-- 1 henrik henrik  8285 25 mar  2022 structures.h
-rw-r--r-- 1 henrik henrik  4028  1 nov  2006 table.c
drwxr-xr-x 2 henrik henrik  4096 26 mar  2022 tests
-rw-r--r-- 1 henrik henrik  2876  1 nov  2006 vector.c
[exit 0]

$ make
gcc -Wall -Wmissing-prototypes -O2   -c -o scan.o scan.c
gcc -Wall -Wmissing-prototypes -O2   -c -o vector.o vector.c
gcc -Wall -Wmissing-prototypes -O2   -c -o dump.o dump.c
gcc -Wall -Wmissing-prototypes -O2   -c -o table.o table.c
gcc -Wall -Wmissing-prototypes -O2   -c -o label.o label.c
gcc -Wall -Wmissing-prototypes -O2   -c -o main.o main.c
gcc  -o dxa scan.o vector.o dump.o table.o label.o main.o
[exit 0]

$ ls -l dxa
-rwxrwxr-x 1 henrik henrik 60592 26 aug 12.24 dxa
[exit 0]

The built binary is copied out of PROBE_DIR into evidence/tools/dxa so a later
wave reads a file that still exists (convention #2).

$ cp -v /home/henrik/.cache/c64-re-tools/phase23/dxa-0.1.5/dxa ./dxa
'/home/henrik/.cache/c64-re-tools/phase23/dxa-0.1.5/dxa' -> './dxa'
[exit 0]

$ sha256sum dxa
0e2bf1a5ea4433c795dbcc96089a29eb8efb6bdaad73f065a5443d31f0ec8523  dxa
[exit 0]

$ ./dxa -h
./dxa: option requires an argument -- 'h'
dxa v0.1.5 -- symbolic 65xx disassembler
Based on d65 copyright (C) 1993-4 Marko M"akel"a
Changes for dxa copyright (c) 2006-22 Cameron Kaiser

Usage: dxa [options] [filename]
[exit 1]

Property of the tool under test, recorded as such and not as a supply-chain
signal: dxa's own man page calls it alpha software.

$ grep -n -i alpha dxa.1
[exit 1]

And the tarball carries no upstream signature and no upstream checksum: the
distribution directory listing offers the tarball alone.

$ bash -c ls -a dxa-0.1.5/ | grep -Ei "sig|asc|sha|md5|sum" || echo "(no signature or checksum file inside the tarball)"
(no signature or checksum file inside the tarball)
[exit 0]

Two transcript notes, recorded rather than repaired, because a transcript is
never reconstructed afterwards (convention #1):

  a. The `grep -n -i alpha dxa.1` above exited 1 with no output. The alpha-software
     statement is NOT in the man page, as 23-02-PLAN.md's action text says; it is in
     the tarball's INSTALL file. The claim itself holds, the citation does not.
     Carried into `## RESEARCH CORRECTIONS` in evidence/fixture/fixture-baseline.txt.
  b. The `$ bash -c ...` line two commands up lost its inner quoting when the
     helper joined argv for display. The command actually run was, verbatim:
       bash -c 'ls -a dxa-0.1.5/ | grep -Ei "sig|asc|sha|md5|sum" || echo "(no signature or checksum file inside the tarball)"'
     Later `$ ` lines in this file are single commands with no shell quoting to lose.

$ grep -n -i alpha INSTALL
15:Please keep in mind this is still considered "alpha" software and there may
[exit 0]


## 2. The other two instruments — Ghidra and VICE

Ghidra is recorded by VERSION, never by install path (evidence/README.md
§ External inputs). The probe install states in its own README that it is safe
to delete and that nothing in the repo depends on that path, so recording its
location as a durable repository fact would create a dependency the install
itself disclaims. The path is shown below only as the argument of the read, and
appears on no outcome line.

$ grep -nE ^application\.(name|version|release\.name|build\.date)= application.properties
2:application.build.date=2026-Aug-17 1710 UTC
10:application.name=Ghidra
13:application.release.name=PUBLIC
17:application.version=12.1.3
[exit 0]

$ bash -c java -version 2>&1
openjdk version "21.0.12.1" 2026-08-18
OpenJDK Runtime Environment (build 21.0.12.1+1-1-deb13u1-Debian)
OpenJDK 64-Bit Server VM (build 21.0.12.1+1-1-deb13u1-Debian, mixed mode, sharing)
[exit 0]

$ /usr/local/bin/x64sc --version
x64sc (VICE 3.10)

[exit 0]

The fork is the measurement instrument for this phase, deliberately. Stock on
this host is VICE 3.9 (/usr/bin/x64sc), and 3.9 lacks CPUHISTORY_GET entirely,
lacks vice_backtrace, lacks vice_checkpoint_set_ignore_count, and auto-disables
a trace checkpoint above 20 hits per second. The D-05/D-06 inventory that later
plans build is a runtime-observation artifact, so it needs the fork. Recorded
here so no later reader assumes the gate could have been run on stock.

$ /usr/bin/x64sc --version
x64sc (VICE 3.9)

[exit 0]


## OUTCOME LINES

Value domains are SCHEMA.md § 3's, which is the frozen pre-commitment.
`DXA_TARBALL_SHA256_VERIFIED` therefore reads `yes`, not the `pass` that
23-02-PLAN.md's action text and automated verify expect; that divergence is
recorded as ACCEPTED LIMIT 1 in evidence/fixture/fixture-baseline.txt and the
evidence is not bent to satisfy the plan-side regex.

DXA_VERSION: 0.1.5
DXA_TARBALL_SHA256: 8e40ed77816581f9ad95acac2ed69a2fb2ac7850e433d19cd684193a45826799
DXA_TARBALL_SHA256_VERIFIED: yes
DXA_BINARY_SHA256: 0e2bf1a5ea4433c795dbcc96089a29eb8efb6bdaad73f065a5443d31f0ec8523
GHIDRA_VERSION: 12.1.3 PUBLIC build 2026-Aug-17
JAVA_VERSION: openjdk 21.0.12.1 2026-08-18 OpenJDK Runtime Environment build 21.0.12.1+1-1-deb13u1-Debian
VICE_BACKEND: fork 3.10


## 2. Task 1's own automated verify, run and recorded as-is

Run verbatim as 23-02-PLAN.md task 1 writes it. It FAILS, and the failure is
recorded rather than repaired: two of its greps assert values SCHEMA.md's frozen
domains forbid (`DXA_TARBALL_SHA256_VERIFIED: pass` where § 3's domain is
`yes | no`, and a bare-decimal `FIXTURE_DATA_RECOVERY_PCT` where § 3 mandates the
`<decimal> (<numerator>/<denominator>)` shape). Per the phase-wide ruling that
SCHEMA.md wins, the evidence is not bent to satisfy the regex.

Both checks are committed as scripts under evidence/tools/verify/ so a reader
re-runs exactly what ran here. The second is the first with only those domains
substituted -- see the diff below -- so its passing shows the failure is
entirely the domain divergence and nothing else.

$ diff -u /home/henrik/dev/henrik/git/c64-re-tools/.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/tools/verify/task1-verify-as-planned.sh /home/henrik/dev/henrik/git/c64-re-tools/.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/tools/verify/task1-verify-schema-domains.sh
--- /home/henrik/dev/henrik/git/c64-re-tools/.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/tools/verify/task1-verify-as-planned.sh	2026-08-26 12:38:02.424288521 +0200
+++ /home/henrik/dev/henrik/git/c64-re-tools/.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/tools/verify/task1-verify-schema-domains.sh	2026-08-26 12:38:02.428288521 +0200
@@ -1,12 +1,13 @@
 #!/usr/bin/env bash
-# 23-02-PLAN.md task 1 <verify><automated>, verbatim.
+# 23-02-PLAN.md task 1 <verify><automated>, with SCHEMA.md § 3's
+# frozen value domains substituted for the three the plan's regexes contradict.
 D=.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence
-grep -v "^#" "$D/tools/instrument-provenance.txt" | grep -qE "^DXA_TARBALL_SHA256_VERIFIED: pass$" &&
+grep -v "^#" "$D/tools/instrument-provenance.txt" | grep -qE "^DXA_TARBALL_SHA256_VERIFIED: yes$" &&
 grep -v "^#" "$D/tools/instrument-provenance.txt" | grep -qE "^DXA_TARBALL_SHA256: 8e40ed77816581f9ad95acac2ed69a2fb2ac7850e433d19cd684193a45826799$" &&
 grep -v "^#" "$D/tools/instrument-provenance.txt" | grep -qE "^DXA_BINARY_SHA256: [0-9a-f]{64}$" &&
 grep -v "^#" "$D/tools/instrument-provenance.txt" | grep -qE "^GHIDRA_VERSION: 12\.1\.3" &&
-grep -v "^#" "$D/fixture/fixture-baseline.txt" | grep -qE "^FIXTURE_REPRODUCED: (pass|fail)$" &&
-grep -v "^#" "$D/fixture/fixture-baseline.txt" | grep -qE "^FIXTURE_DATA_RECOVERY_PCT: [0-9]+\.[0-9]{2}$" &&
+grep -v "^#" "$D/fixture/fixture-baseline.txt" | grep -qE "^FIXTURE_REPRODUCED: (yes|no|could-not-run)$" &&
+grep -v "^#" "$D/fixture/fixture-baseline.txt" | grep -qE "^FIXTURE_DATA_RECOVERY_PCT: [0-9]+\.[0-9]{2} \([0-9]+/[0-9]+\)$" &&
 test -x "$D/tools/dxa" &&
 node --check "$D/dxa-listing-parse.mjs" &&
 echo TRACER-OK
[exit 1]

$ bash /home/henrik/dev/henrik/git/c64-re-tools/.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/tools/verify/task1-verify-as-planned.sh
[exit 1]

$ bash /home/henrik/dev/henrik/git/c64-re-tools/.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/tools/verify/task1-verify-schema-domains.sh
TRACER-OK
[exit 0]
