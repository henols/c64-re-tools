---
phase: 23-the-real-release-gate-go-degrade-no-go
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/DECISION-RULE.md
  - .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/SCHEMA.md
  - .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/README.md
  - .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/corpus/.gitignore
autonomous: false
requirements: [PROOF-05, PROOF-01, PROOF-02, PROOF-03, PROOF-04]
requirement_subparts:
  - "PROOF-05 — the named rules, committed before any measurement exists (D-07, one-way)"
  - "PROOF-01 — the criterion-1 measurement definitions, window and dxa flag set, fixed before the capture is seen"
  - "PROOF-02/PROOF-03 — the `not-exercised` outcome vocabulary and the D-09 pre-mapped degrade narrowing"
  - "PROOF-04 — the three-disposition audit vocabulary"
worktree: false
worktree_reason: "Config sets workflow.use_worktrees=false project-wide, and this plan's whole value is that its commit lands on the mainline before every measurement commit (D-07). A worktree branch merged later can reorder the commit relative to a measurement branch, which silently destroys the one property the phase exists to establish. Run it sequentially on the main tree."

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A first-match-wins rule R1..R9 exists in git, naming every input by a literal outcome line and a literal evidence file path, and its commit precedes every measurement commit in this phase"
    - "Every outcome-line name any later plan will emit is fixed here, before the capture exists, so no measuring plan can invent a line name that flatters its own result"
    - "`not-exercised` is a declared value distinct from `pass` for both C2_COMPUTED_DISPATCH and C3_BANK_DIVERGENCE, and no rule maps it to `go`"
    - "The corpus is modelled as a list of releases with exactly one flagged canonical, not as a scalar image with a second bolted alongside"
    - "Each numeric threshold in the rule states which side of the comparison is inclusive, and what the value exactly at the threshold does: adjudicated_fraction == 10.00 does NOT fire R3, data_recovery_pct == 50.00 does NOT fire R2 or R5, false_positives == 0 does NOT fire R4"
    - "Percentages are computed from exact integer byte ratios, the rule is evaluated on the unrounded ratio, and the displayed value is rounded half-up to two decimal places with its raw numerator and denominator byte counts printed beside it"
    - "Re-evaluating the committed rule against the same outcome lines yields the same verdict and the same fired rule id, with no step requiring judgement"
    - "Every outcome line named in the inputs table exists in its named evidence file carrying one of its declared values; a measurement that could not be completed writes `could-not-run` rather than leaving the line absent"
  artifacts:
    - path: ".planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/DECISION-RULE.md"
      provides: "The binding inputs table, R1..R9 first-match-wins rule, the D-09 pre-mapped degrade narrowing for criteria 2 and 3, and the never-a-gate declarations"
      min_lines: 90
    - path: ".planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/SCHEMA.md"
      provides: "Every outcome-line name and its value domain, the corpus release-list schema, the criterion-1 measurement definitions, the dxa flag set, the inventory schema, and the audit disposition vocabulary"
      min_lines: 120
    - path: ".planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/README.md"
      provides: "The phase-wide <evidence_conventions> block, restated as a binding document later plans cite"
      min_lines: 30
  key_links:
    - from: "evidence/DECISION-RULE.md"
      to: "evidence/SCHEMA.md"
      via: "every rule input names an outcome line that SCHEMA.md declares"
      pattern: "C[0-9]_[A-Z_]+:"
    - from: "evidence/SCHEMA.md"
      to: "docs/phase23-real-release-gate-findings.md"
      via: "the findings frontmatter keys are declared here before the document exists"
      pattern: "verdict_rule_applied"
  prohibitions:
    - "The decision rule, the inventory schema, and the criterion-1 measurement definitions must NOT be edited, refined, re-scoped or 'clarified' after the first measurement commit lands — including by a later plan that believes it is only resolving an ambiguity. If an ambiguity is found, the measuring plan records it as an ACCEPTED LIMIT in its own evidence file and the verdict document records an explicit override; the rule text itself does not move."
    - "No fact used to judge dxa or Ghidra may be produced by dxa or Ghidra. The corpus inventory, the certain-code set, the certain-data set and the dispatch-site enumeration come from VICE runtime observation only."
    - "A negative, partial or could-not-run outcome must NOT be summarised away, softened, or rewritten as a pass, and `not-exercised` must never be recorded as `pass`. A check that could not be performed is written up as fully as one that passed."
    - "regenerator2000 must NOT be executed in any form — not the binary, not `r2000-coverage.ts`, not as an oracle, baseline or screening tool. Reading `analyzer.rs` source offline is permitted and is PROOF-04's subject."
    - "Nothing under `src/` may be created or modified by any plan in this phase, and no probe script may be added to `resources/`, to `tools-manifest.json`, or to any other manifest. Probe scripts are evidence, not deliverables."
    - "No plan in this phase may write, amend or pre-empt a Phase 24, 25 or 26 plan, and the corpus image itself must never be committed to this repository — its identity is release name plus sha256."
---

<objective>
Commit the gate before the gate can be gamed. This plan writes the first-match-wins
decision rule (D-07), the corpus-inventory schema (D-05), and — per 23-RESEARCH.md's
single strongest recommendation — the criterion-1 measurement definitions, dxa flag set
and measurement window, all while the outcome of every measurement is still unknown.

Purpose: `git log` is the only proof PROOF-05 has. Once any measurement commit lands,
the rules can never again be shown to predate it. This is one-way (D-07), which is why
this plan is alone in wave 1 and why its first task is a decision checkpoint.

Output: three binding documents under `evidence/` plus the corpus ignore rule. No
measurement, no tool invocation against the corpus, no number.

**Why there is no `type="tracer"` task in this plan.** TRACER_MODE is on by default, but
D-07's ordering constraint overrides tracer shaping where they conflict: the leading item
of this phase cannot be an end-to-end slice, because an end-to-end slice *is* a
measurement and a measurement committed before the rule destroys criterion 5 permanently.
The tracer lives one wave later, in 23-02, where the whole instrument chain is driven
end-to-end against the 279-byte fixture — work D-11 requires anyway. Every measuring plan
from 23-07 on is shaped as a vertical slice of one criterion (instrument → run →
transcribe outcome line → record accepted limits), never as a horizontal layer.
</objective>

<assumption_delta_decision>
`gsd_run query assumption-delta scan 23 --json` returned `detected: true`
(`pluralization` on "another"). Independently, the operator answered during this planning
run that **two independently-cracked releases of one title** are available, which is a
singular→plural transition on the corpus noun that CONTEXT.md's D-04 framed in the
singular.

- **Now-primary noun:** `corpus.releases` — a list of release records.
- **Decision:** `promote`.
- **Rationale:** 23-RESEARCH.md Pattern 2 proposed scalar `corpus.file_sha256` /
  `corpus.capture_sha256`, and 23-VALIDATION.md's verification map inherited that scalar
  phrasing. With two releases those keys have no single correct value, and bolting a
  `corpus.file_sha256_secondary` alongside reproduces the exact
  canonical-image-centric model `recovery-schema.mjs` already exists to prevent in this
  codebase ("the registry is release-CENTRIC, never canonical-image-centric again"). The
  schema is therefore a list from the start, with the one-release case as the degenerate
  single-element list, and exactly one entry carrying `canonical: true`. This cannot be
  retrofitted after the first measurement without destroying the pre-commitment, which is
  why it is decided here and not later. The supersession of 23-VALIDATION.md's scalar
  wording is recorded in SCHEMA.md and carried into the findings document's
  `## Corrections to prior documents`.
</assumption_delta_decision>

<probe_edge_accounting>
## Spec-less probe fallback — no-silent-drop accounting

No SPEC exists for phase 23, so `## Edge Coverage` and `## Prohibitions` were both absent
and the spec-less probe fallback ran. The edge probe surfaced **9 applicable edges, all
`unresolved`**. Every one is accounted for below; none was dropped and none was
auto-dismissed. Equality holds: 9 surfaced == 7 authored into `must_haves` + 2 surfaced as
flagged assumptions.

| # | Requirement | Category | Disposition | Where it lives |
|---|-------------|----------|-------------|----------------|
| 1 | PROOF-01 | boundary | resolved (explicit) | `23-01` truth — the threshold boundary contract: exactly 10.00, 50.00, 0 and 0 do not fire their rules |
| 2 | PROOF-01 | precision | resolved (explicit) | `23-01` truth — the precision contract: exact integer ratios, rule evaluated unrounded, display half-up to 2 dp with raw counts beside it |
| 3 | PROOF-02 | unclassified | **UNRESOLVED — flagged assumption** | `23-08` `probe_edges_flagged` — the detection boundary is silent, and no pre-committed rule covers a partially-resolved dispatch |
| 4 | PROOF-03 | adjacency | resolved (explicit) | `23-09` truth — narrowest containing range wins, ties break toward the symbol-carrying entry, and equal resolutions under different bank values are not a divergence |
| 5 | PROOF-03 | empty | resolved (explicit) | `23-09` truth — the three empty shapes each write `not-exercised` with the reason stated |
| 6 | PROOF-03 | ordering | resolved (explicit) | `23-05` truth — non-decreasing cycle order, equal-cycle hits in observation order, site tables sorted by address |
| 7 | PROOF-04 | unclassified | **UNRESOLVED — flagged assumption** | `23-04` `flagged_assumptions` — the disposition boundary for a partially-replaced capability has no clean bucket |
| 8 | PROOF-05 | idempotency | resolved (explicit) | `23-01` truth — re-evaluating the committed rule over the same outcome lines yields the same verdict and fired rule, with no step requiring judgement |
| 9 | PROOF-05 | concurrency | resolved (explicit) | `23-01` truth — every named outcome line exists with a declared value; an incomplete measurement writes `could-not-run` rather than leaving the line absent |

**Prohibitions.** The §B recall pass over-produced roughly a dozen candidates and kept six,
authored **descriptor-less** into this plan's `must_haves.prohibitions` and repeated in the
directly-affected plans. Two candidates were dropped by the canon-referral rule with
breadcrumbs rather than minted: *path traversal / command injection from strings recovered
out of the corpus image* and *unsafe handling of an untrusted binary in a GUI tool* are both
canon OWASP security concerns, already covered by ASVS V5 in every plan's `<threat_model>`
(threat `T-23-04`, `T-23-03`) and by this repository's existing `hostpath.ts` /
`containerpath.ts` path seams. Nothing was auto-dismissed.
</probe_edge_accounting>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-the-real-release-gate-go-degrade-no-go/23-CONTEXT.md
@.planning/phases/23-the-real-release-gate-go-degrade-no-go/23-RESEARCH.md
@.planning/phases/23-the-real-release-gate-go-degrade-no-go/23-PATTERNS.md
@.planning/phases/23-the-real-release-gate-go-degrade-no-go/23-VALIDATION.md
</context>

<evidence_conventions>
Binding on every task in this plan **and on every other plan in phase 23**. Task 3 writes
this block into `evidence/README.md`; later plans cite it rather than restating it.

1. **Transcript convention.** Before running a command, append a line reading
   `$ <the exact command line>` to the named evidence file; then append its real stdout
   and stderr immediately below. A summary written in place of output is not evidence.
   Never reconstruct a transcript afterwards from memory.
2. **Worktree-independent probe directory.** Cross-wave working artifacts live at
   `PROBE_DIR=$HOME/.cache/c64-re-tools/phase23` — never inside the checkout, and never
   in `/tmp` (tmpfs on this host, 16 GB, cleared on reboot; the dxa+Ghidra pivot already
   lost its dxa build to it). Record the absolute `PROBE_DIR` paths in the evidence file
   so later waves can find them. Anything that matters is copied into `evidence/` before
   the session ends.
3. **Argv arrays, never shell strings.** Any spawn from a probe script uses an argv array.
   Never interpolate a path, a filename, or any string recovered from the corpus image
   into a shell command string.
4. **regenerator2000 is never executed** — not the binary, not `r2000-coverage.ts`, not as
   an oracle, a baseline or a screening tool (D-01). Reading `analyzer.rs` source offline
   is permitted and is PROOF-04's subject.
5. **Do NOT spawn a nested `claude` / `claude -p` session.** Nested sessions stall
   indefinitely in this project and the stall reports as success.
6. **Commit early.** Bank each task before starting the next; verify with `git log`.
7. **Outcome lines only.** Every rule input is a bare `NAME: value` at column 0 of a named
   evidence file. If a line is written more than once in a file, **the final occurrence
   wins** — this is stated here because Phase 9's own evidence carried a superseded early
   `INSTALLED_VERSION:` line that was only resolved 106 lines later.
8. **Single-owner file rule.** Measuring plans record `## RESEARCH CORRECTIONS` in their
   **own** evidence files. Exactly one closing plan (23-10) owns `23-RESEARCH.md` and the
   findings document, and collects every correction in one pass. Two parallel plans never
   edit one document.
9. **No product code.** `git diff --name-only <phase-base>..HEAD` must touch only
   `.planning/` and `docs/`. Nothing under `src/` is created or modified. Files under
   `src/` may be **read** (`memmap.json`, the skill scripts, `analyzer.rs` in the cargo
   registry) — reading is not modifying.
10. **The corpus image is never committed.** Identity is release name plus sha256 (D-04).
</evidence_conventions>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Confirm the one-way pre-commitment before writing it</name>
  <reversibility rating="one-way">D-07 and D-06 are both rated one-way in 23-CONTEXT.md. Once a measurement commit lands, the rule can never again be shown to predate it, and a criterion-2 result produced from a self-supplied inventory cannot be laundered into a valid one afterwards — the measurement would have to be discarded and re-run from a fresh capture. The gate is `blocking-human` because auto-selecting the first option here would decide, unattended, the thresholds the entire milestone's scope turns on.</reversibility>
  <decision>Fix the verdict thresholds, the criterion-1 measurement definitions and the inventory provenance, sight unseen, before any measurement exists</decision>
  <context>
    Three things become irreversible the moment Task 2 commits.

    1. **The verdict rule.** Thresholds are numeric and stated now: `c1_adjudicated_fraction < 10`
       degrades, `c1_data_recovery_pct < 50` degrades, `c1_false_positives > 0` degrades, and
       both of the last two together is a `no-go`.
    2. **The criterion-1 measurement definitions.** Positive class is *data*; the denominator is
       certain-data bytes inside the declared window after the cracker hold-out; the dxa flag set
       is `-g 0000 -p all-nmos6502 -d skip-scanning -t detect-internal -R <entrypoints>
       -B <datablocks> -a dump`, with `-t detect-all` run as a second invocation and both numbers
       printed.
    3. **The inventory provenance.** The corpus inventory is produced by VICE runtime observation
       and by nothing else (D-06).

    Alternatives deliberately rejected: sketching the rule now and tightening it once the numbers
    are in, which is precisely the advisory-gate failure the ROADMAP names ("a rule written after
    the measurement makes every future gate advisory"); letting dxa or Ghidra enumerate the
    dispatch sites, which is cheaper and circular, degrading criterion 2 to "Ghidra resolved the
    dispatch that Ghidra found"; and leaving the numeric thresholds unstated, which 23-PATTERNS.md
    names as an explicit anti-pattern.

    Cost of being wrong: a threshold that fits badly can only be superseded at verdict time by
    recording the override explicitly in the findings document, which weakens the pre-commitment
    it exists to provide. It cannot be silently edited.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed as specified</name>
      <pros>The rule, the definitions and the provenance are fixed while every outcome is still unknown, which is the only state in which the gate is credible</pros>
      <cons>A threshold that fits the evidence badly can only be superseded by a loud, recorded override</cons>
    </option>
    <option id="adjust">
      <name>Adjust a threshold or a flag before it is committed</name>
      <pros>The last moment at which any of these can change without weakening the pre-commitment</pros>
      <cons>Requires stating which value and why now; after Task 2 commits this option is gone</cons>
    </option>
    <option id="halt">
      <name>Halt</name>
      <pros>Correct when the corpus or an instrument is not actually available, so the rule's inputs would be guesses</pros>
      <cons>The phase stalls until the missing input exists</cons>
    </option>
  </options>
  <read_first>
    - `.planning/phases/23-the-real-release-gate-go-degrade-no-go/23-CONTEXT.md` — D-05, D-06, D-07, D-09 and their reversibility ratings
    - `.planning/phases/23-the-real-release-gate-go-degrade-no-go/23-RESEARCH.md` § *Summary* (the primary recommendation: 23-01 must commit three things, not one), § *Pitfall 8*, § *Criterion 1: the measurement definition*
    - `.planning/phases/09-the-assumption-probe-go-no-go/09-07-PLAN.md` — the `<decision_rule>` block, lines 80-136, which is the structure being copied
  </read_first>
  <acceptance_criteria>
    - The operator has selected `proceed`, `adjust` or `halt`, and the selection is recorded verbatim in the plan SUMMARY.
    - If `adjust`, the adjusted value is written into `evidence/DECISION-RULE.md` in Task 2 and the original is not carried anywhere as a shadow default.
  </acceptance_criteria>
  <resume-signal>Select: proceed, adjust (and say which value and why), or halt</resume-signal>
  <done>The decision is recorded and Task 2 may write the rule.</done>
</task>

<task type="auto">
  <name>Task 2: Write the binding decision rule and the outcome-line schema</name>
  <files>
    .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/DECISION-RULE.md,
    .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/SCHEMA.md
  </files>
  <read_first>
    - `.planning/phases/09-the-assumption-probe-go-no-go/09-07-PLAN.md` lines 80-136 — the inputs table, the first-match-wins rule, and the "never changes the verdict" device, all three of which are copied structurally
    - `docs/phase9-regenerator2000-probe-findings.md` lines 1-40 — the frontmatter shape and the `## Verdict` re-derivation section this schema must make writable
    - `.planning/phases/23-the-real-release-gate-go-degrade-no-go/23-RESEARCH.md` § *Pattern 1*, § *Pattern 2*, § *Pattern 3*, § *Pattern 4*, § *Pattern 5*, § *Criterion 1: the measurement definition*, § *Pitfall 1*, § *Pitfall 3*, § *Pitfall 4*, § *analyzer.rs capability inventory*
    - `.planning/phases/23-the-real-release-gate-go-degrade-no-go/23-VALIDATION.md` — the Per-Task Verification Map, whose `❌ W0` rows are all blocked on this file
    - `.planning/ROADMAP.md` Phase 23 entry, its five success criteria and its Notes
    - `.planning/REQUIREMENTS.md` — PROOF-01..05 and the AUTO-01..07 / GHID-04 / DXA-03 text the pre-mapped narrowing names
  </read_first>
  <action>
Write two documents. Neither runs anything; both are pure pre-commitment.

**`evidence/DECISION-RULE.md`** opens with the Phase 9 binding header ("Stated here,
before the run, so the verdict is derived and not judged. Read the inputs from the
evidence files, never from a summary's paraphrase, then evaluate the rules in order and
take the first that matches. Record which rule fired."), then an inputs table with three
columns — Input, Source line, Source file — one row per input:

`c0_corpus` from `C0_CORPUS:` in `evidence/capture/CAPTURE-SUMMARY.txt`;
`c1_adjudicated_fraction` from `C1_ADJUDICATED_FRACTION:`,
`c1_data_recovery_pct` from `C1_DATA_RECOVERY_PCT:`,
`c1_false_positives` from `C1_FALSE_POSITIVES:` — all three in
`evidence/criterion1-dxa-classification.txt`;
`c2_computed_dispatch` from `C2_COMPUTED_DISPATCH:` in
`evidence/criterion2-ghidra-dispatch.txt`;
`c3_bank_divergence` from `C3_BANK_DIVERGENCE:` in
`evidence/criterion3-bank-divergence.txt`;
`c4_unreplaced` from `C4_UNREPLACED_CAPABILITIES:` in
`evidence/criterion4-analyzer-audit.md`.

Then the rules, first match wins, each stating its verdict, its condition, and why in the
milestone's own terms:

- **R1 → `no-go`.** `c0_corpus` is not `pass`. Every criterion except 4 is measured on the
  depacked capture (D-03). Without a real release and a capture proven equivalent across
  two runs, the phase would be measuring another self-authored fixture — the exact defect
  PROOF-01 exists to remove. The milestone becomes: secure a corpus first, or re-scope
  v0.6.0 to a claim explicitly qualified as fixture-only.
- **R2 → `no-go`.** `c1_false_positives > 0` **and** `c1_data_recovery_pct < 50`. Both
  halves of dxa's error budget failed at once: it types certain-code bytes as data
  (poisoning discovery) *and* recovers less than half the certain data. Ghidra produced
  0 functions and 0 code bytes with no map on the pivot fixture, so dxa's map is not an
  optimisation but the thing that makes Ghidra work at all; wrong in both directions means
  DXA-01..03 and GHID-01..05 are not the phases they were scoped as.
- **R3 → `degrade`.** `c1_adjudicated_fraction < 10`. Below one tenth of the declared
  window, `C1_DATA_RECOVERY_PCT` is a statement about a sliver and PROOF-01's
  "re-measured against real cracked releases" is not honestly claimable at full strength.
  Narrowing authored at verdict time against the evidence (criterion 1 is not pre-mapped,
  per D-09).
- **R4 → `degrade`.** `c1_false_positives > 0`. dxa typed at least one byte the CPU was
  observed executing as data. Narrowing authored at verdict time.
- **R5 → `degrade`.** `c1_data_recovery_pct < 50`. The false-negative direction — data
  called code — is the dangerous one on the pivot's own record, and it feeds phantom code
  into every downstream stage. Narrowing authored at verdict time; AUTO-07's
  graphics-feedback containment and DXA-03's known-data exclusion are the obvious
  candidates and are named as such without being pre-bound.
- **R6 → `degrade`.** `c2_computed_dispatch` is not `resolved` — that is, `unresolved`,
  `not-exercised`, or `could-not-run`. **Pre-mapped narrowing (D-09):** AUTO-04 and
  AUTO-05 narrow to decline-to-annotate at every dispatch site whose index is not a
  compile-time constant — the join emits no target annotation there rather than a
  confident wrong one; and GHID-04's "at least one resolved computed jump" acceptance is
  qualified to "resolved where the dispatch index is a compile-time constant, recorded as
  a declared unresolved otherwise". On `not-exercised` the same narrowing applies but is
  recorded as unproven-either-way rather than as a demonstrated failure.
- **R7 → `degrade`.** `c3_bank_divergence` is not `found`. **Pre-mapped narrowing
  (D-09):** AUTO-04's per-program-point bank carry ships unvalidated against real
  path-dependent code, so its acceptance is qualified to single-`$01`-value access sites
  only; AUTO-05's decline rule may be acceptance-tested against a synthetic
  path-dependent fixture with the corpus gap recorded beside it; and Phase 26's third
  selection rule is marked unvalidated in the ROADMAP. Note the asymmetry deliberately:
  `found` does **not** fire this rule. A divergence that is found is the criterion
  answered affirmatively and is consistent with the milestone as already scoped — Phase 26
  is already required to decline where bank state is path-dependent.
- **R8 → `degrade`.** `c4_unreplaced > 0`. At least one `analyzer.rs` capability carries
  the `lost-blocking:` disposition. Narrowing authored at verdict time, naming each
  blocking capability and which Phase 25 requirement absorbs it.
- **R9 → `go`.** Everything above passed.

Then the **never-a-gate** declarations, copying Phase 9's `c1_container_cost` device
including its closing sentence so a later reader cannot mistake absence for failure. Four
inputs are measurements, not gates, and each says why: `C1_FALSE_NEGATIVES` (arithmetically
derived from `C1_DATA_RECOVERY_PCT` and the denominator; gating on it would double-count
R5); `C1_CODE_DATA_OVERLAP` (self-modifying code and jump tables legitimately produce bytes
that are both executed and DMA-fetched — a finding, reported by count, excluded from both
ratios); `C3_DIVERGENT_SITES` (no threshold on how *much* divergence is defensible before
the measurement exists); `C4_LOST_ACCEPTED` (a capability accepted as lost with its cost
stated is a decision, not a defect).

Then the **threshold boundary contract**, one line per numeric comparison, stating what
happens exactly at the threshold and one step either side: `c1_adjudicated_fraction` of
exactly `10.00` does not fire R3 (`9.99` does); `c1_data_recovery_pct` of exactly `50.00`
does not fire R2 or R5 (`49.99` does); `c1_false_positives` of exactly `0` does not fire
R2 or R4 (`1` does); `c4_unreplaced` of exactly `0` does not fire R8 (`1` does).

Then the **precision contract**: every percentage is computed as an exact integer ratio of
byte counts; the rule is evaluated against the **unrounded** ratio; the displayed value is
rounded half-up to two decimal places; and the raw numerator and denominator byte counts
are printed on their own outcome lines beside every percentage so a reader recomputes
rather than trusts.

Then the **hold-out precedence contract**: `C1_DATA_RECOVERY_PCT` and `C1_FALSE_POSITIVES`
are the cracker-held-out numbers for the canonical release and are the only criterion-1
rule inputs. `C1_DATA_RECOVERY_PCT_WITH_CRACKER` and `C1_FALSE_POSITIVES_WITH_CRACKER` are
printed beside them and never gate — the difference between the two is itself evidence
about how far a naive measurement would have been distorted. If the provenance diff cannot
produce a hold-out (`PROV_HOLDOUT: unavailable`, e.g. anchors disagree so there is no
single offset), then `C1_CRACKER_HELD_OUT: no` is written, the all-resident-bytes number
becomes the rule input, and that substitution is recorded as an `## ACCEPTED LIMIT` in the
criterion-1 evidence file. The secondary release's numbers live in their own file and never
gate.

Close with the ordering statement: this document's commit precedes every measurement
commit, `git log` is the proof, and no test guard is added (D-08's reasoning).

**`evidence/SCHEMA.md`** fixes every literal name the rest of the phase may emit. It has
seven sections.

*Corpus release list (the assumption-delta promotion).* The findings frontmatter carries
`corpus.releases` as a YAML list; each element has `release` (operator-supplied name/id),
`file_sha256`, `capture_sha256`, and `canonical` (boolean). Exactly one element carries
`canonical: true`, and that element is the one criterion 1's rule inputs are computed
over. State explicitly that this supersedes 23-RESEARCH.md Pattern 2's and
23-VALIDATION.md's scalar `corpus.file_sha256` / `corpus.capture_sha256` phrasing, that
the one-release case is the degenerate single-element list, and that the correction is
carried into the findings document's `## Corrections to prior documents`.

*Findings frontmatter keys.* `phase`, `requirement` (the five PROOF ids), `probe_date`,
`verdict` ∈ {`go`,`degrade`,`no-go`}, `verdict_rule_applied` matching `R[0-9]+`,
`corpus.releases[]` as above, `tools` with `dxa`, `ghidra` and `vice` keys, and `criteria`
carrying one key per rule input with its recorded value.

*Outcome lines.* A table of every literal line name, its value domain, and the evidence
file that owns it. All lines are bare `NAME: value` at column 0. Enumerate:
`C0_CORPUS`, `CORPUS_RELEASES`, `CORPUS_CANONICAL`, `CORPUS_FILE_SHA256`,
`CAPTURE_SHA256`, `CAPTURE_SIZE`, `CAPTURE_EQUIVALENT`, `CAPTURE_HANDOFF_PC`,
`CAPTURE_PORT01`;
`DXA_VERSION`, `DXA_TARBALL_SHA256`, `DXA_TARBALL_SHA256_VERIFIED`, `DXA_BINARY_SHA256`,
`GHIDRA_VERSION`, `JAVA_VERSION`, `VICE_BACKEND`, `FIXTURE_DATA_RECOVERY_PCT`,
`FIXTURE_FALSE_POSITIVES`, `FIXTURE_FALSE_NEGATIVES`, `FIXTURE_TOTAL_BYTES`,
`FIXTURE_REPRODUCED`;
`INV_SOURCE`, `INV_WINDOW`, `INV_WINDOW_BYTES`, `INV_DISPATCH_SITES`, `INV_DISPATCH_SITE`,
`INV_BANK_WRITE_SITES`, `INV_BANK_VALUES`, `INV_HIT_CEILING`, `INV_TERMINATION`,
`INV_CERTAIN_CODE_BYTES`, `INV_CERTAIN_DATA_BYTES`, `INV_CODE_DATA_OVERLAP`,
`INV_ADJUDICATED_FRACTION`;
`PROV_ANCHOR_OK`, `PROV_RELEASES`, `PROV_CRACKER_PATCH_BYTES`, `PROV_CRACKER_PATCH_RANGES`,
`PROV_HOLDOUT`;
`C1_WINDOW`, `C1_WINDOW_BYTES`, `C1_DXA_FLAGS`, `C1_DENOMINATOR_BYTES`,
`C1_DATA_RECOVERY_PCT`, `C1_FALSE_POSITIVES`, `C1_FALSE_NEGATIVES`,
`C1_ADJUDICATED_FRACTION`, `C1_CRACKER_HELD_OUT`, `C1_DATA_RECOVERY_PCT_WITH_CRACKER`,
`C1_FALSE_POSITIVES_WITH_CRACKER`, `C1_DETECT_INTERNAL_DATA_BYTES`,
`C1_DETECT_ALL_DATA_BYTES`, `C1_PARSE_ACCOUNTED_BYTES`, `C1_CODE_DATA_OVERLAP`;
`C2_COMPUTED_DISPATCH`, `C2_SITES_ENUMERATED`, `C2_COMPUTED_SITES`, `C2_RESOLVED_TARGET`,
`C2_TRANSCRIPT`, `C2_REFERENCE_COUNT`, `C2_CLASSIFICATION_LINES`;
`C3_BANK_DIVERGENCE`, `C3_DIVERGENT_SITES`, `C3_DIVERGENT_SITE`, `C3_MEMMAP_SHA256`;
`C4_CAPABILITIES_AUDITED`, `C4_REPLACED`, `C4_LOST_ACCEPTED`, `C4_UNREPLACED_CAPABILITIES`.
State that `C2_COMPUTED_DISPATCH` ∈ {`resolved`,`unresolved`,`not-exercised`,
`could-not-run`} and `C3_BANK_DIVERGENCE` ∈ {`found`,`not-exercised`,`could-not-run`},
that `not-exercised` is its own value distinct from any pass, and that where a line is
written more than once in a file the final occurrence is the one the rule reads.

*Criterion 1 measurement definitions.* Reproduce the four-set model as a table: window `W`
is the explicit list of `$xxxx-$yyyy` ranges committed in the inventory, derived by the
stated rule and never expressed as a percentage of the image; `C` is bytes in `W` proven
code by observed execution or by being a JSR call site or call target; `D` is bytes in `W`
proven data by VIC DMA derivation (screen matrix at `screen_base`, the 8 sprite pointers
at `screen_base+$3F8`, charset or bitmap at `charset_base`, and the sprite bitmaps those
pointers name); `U` is `W \ (C ∪ D)` and enters no ratio. Then: adjudicated fraction is
`(|C| + |D|) / |W|`; data-recovery rate is bytes in `D` that dxa typed as data over `|D|`;
false positives is the count of bytes in `C` that dxa typed as data; false negatives is the
count of bytes in `D` that dxa typed as code. `C ∩ D` is a **finding, not an error** —
reported by count on `C1_CODE_DATA_OVERLAP` and excluded from both ratios (self-modifying
code and jump tables legitimately produce such bytes). Record the fixture's own reproduced
definition beside it — positive class data, denominator true-data bytes,
100/138 = 72.46%, 0 false positives, 38/138 = 27.5% false negatives on 279 bytes — so the
D-11 side-by-side is apples-to-apples.

*Window derivation rule.* `W` is the union of the address ranges the loader was observed
writing plus the ranges observed executing, minus `$0000-$01FF` (zero page and stack),
minus `$D000-$DFFF` (I/O), minus any range the capture shows as neither written by the
loader nor executed. It is committed as an explicit range list in the inventory, before
dxa is run.

*dxa flag set (fixed here, not chosen after seeing the result).* Primary invocation:
`-g 0000` (mandatory — dxa's default load-address detection reads the first two bytes of
a flat capture as a little-endian load address, so a processor-port `$2F $37` re-bases the
image to `* = $372f` and discards roughly 50K; the warning it prints goes to stderr and
stderr is kept as evidence), `-p all-nmos6502` (the narrower instruction sets exclude
exactly the illegal opcodes crack code uses), `-d skip-scanning` (dxa's default `-d poor`
lists as much as possible as code even where illegal instructions are present, a large
code-side bias over unreferenced RAM), `-t detect-internal`, `-R` pointing at the
VICE-observed entry-point file, `-B` pointing at the data-block file excluding
`$0000-$01FF`, `$D000-$DFFF` and every range the inventory shows the loader never wrote,
and `-a dump` (every byte-emitting line is prefixed with its actual bytes, so counts are
read rather than inferred from mnemonic lengths). A second invocation, identical except
`-t detect-all`, is run and its data-byte count printed on `C1_DETECT_ALL_DATA_BYTES`
beside the primary's `C1_DETECT_INTERNAL_DATA_BYTES`; `detect-internal` is declared the
primary and is what the rule inputs are computed from. The listing parser matches
`^([0-9a-f]{4}) ((?:[0-9a-f]{2} )+)\s+(.*)$` — whitespace class, not a literal tab,
because `.word` lines are space-separated — classifies by whether the text begins `.byt`
or `.word`, and **asserts the accounted byte total equals the image size**, refusing
rather than under-counting on an unseen line shape.

*Inventory schema (D-05/D-06).* The inventory is produced by VICE runtime observation and
by nothing else; `INV_SOURCE: vice-runtime-observation` is the assertion of that. It
records: the `$01` write timeline as `(cycle, PC, value)` triples ordered by non-decreasing
cycle with equal-cycle hits kept in observation order; the hit ceiling **declared before
the run** and whether the run ended by saturation or by ceiling; the distinct `$01` values
observed; the enumerated dispatch sites, one `INV_DISPATCH_SITE:` line each carrying
address, construct kind (`jmp-indirect`, `rts-trick`, `self-modified-jmp`), the count of
distinct targets observed at that site, and the hit count — where a site is **computed**
iff it dispatches through two or more distinct values at the same program address, and a
site with one target across many hits is the easy case and is recorded as such rather than
as a pass; the certain-code and certain-data byte sets; and the window `W`. Every
divergent-site and dispatch-site table is sorted ascending by address so two runs of the
same derivation produce byte-identical tables.

*Audit disposition vocabulary (PROOF-04).* Every audited `analyzer.rs` capability carries
exactly one of `replaced-by:<what>`, `lost-accepted:<cost>`, `lost-blocking:<what it
breaks>`. `C4_UNREPLACED_CAPABILITIES` is the count of the third and is R8's input; the
audit is scoped to `analyzer.rs` only, and a capability found to live in `exporter/` or
`state/` is recorded as a scope observation for Phase 25 rather than widening this phase.

Neither document may contain a measured number from the corpus. The only numbers permitted
are the fixture's reproduced figures, the fixed thresholds, and the pinned tool hashes.
  </action>
  <verify>
    <automated>bash -c 'D=.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence; grep -qE "^- \*\*R9 → .go" "$D/DECISION-RULE.md" &amp;&amp; grep -c "^| \`c[0-9]" "$D/DECISION-RULE.md" | grep -qE "^([7-9]|[0-9]{2})$" &amp;&amp; for L in C0_CORPUS C1_DATA_RECOVERY_PCT C1_FALSE_POSITIVES C1_ADJUDICATED_FRACTION C1_WINDOW C2_COMPUTED_DISPATCH C3_BANK_DIVERGENCE C4_UNREPLACED_CAPABILITIES INV_SOURCE INV_WINDOW PROV_HOLDOUT; do grep -q "$L" "$D/SCHEMA.md" || { echo "MISSING $L"; exit 1; }; done; grep -q "not-exercised" "$D/SCHEMA.md" &amp;&amp; grep -q "corpus.releases" "$D/SCHEMA.md" &amp;&amp; grep -q "canonical" "$D/SCHEMA.md" &amp;&amp; echo SCHEMA-OK'</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/DECISION-RULE.md` contains rules `R1` through `R9`, each on its own bullet, each naming a verdict from {`no-go`, `degrade`, `go`}, and `R9` is the only `go`.
    - The inputs table has one row per rule input and every row names both an outcome line and a relative evidence-file path; no row names a document section or a summary.
    - `R6` and `R7` each contain a paragraph beginning with the pre-mapped narrowing and naming at least two of `AUTO-04`, `AUTO-05`, `GHID-04` by id.
    - A "never changes the verdict" declaration exists for each of `C1_FALSE_NEGATIVES`, `C1_CODE_DATA_OVERLAP`, `C3_DIVERGENT_SITES`, `C4_LOST_ACCEPTED`, each with its own one-sentence reason.
    - The threshold boundary contract states the exact-at-threshold behaviour for all four numeric comparisons, and the precision contract states half-up to two decimal places with the rule evaluated on the unrounded ratio.
    - `evidence/SCHEMA.md` declares every outcome-line name listed in the action, the value domains for `C2_COMPUTED_DISPATCH` and `C3_BANK_DIVERGENCE` including `not-exercised` and `could-not-run`, and the `corpus.releases` list schema with exactly one `canonical: true`.
    - `evidence/SCHEMA.md` names the dxa flag set verbatim including `-g 0000`, `-d skip-scanning`, `-t detect-internal`, `-a dump`, and states that a second `-t detect-all` invocation is run and both numbers printed.
    - Neither file contains a number measured from the corpus; `git status` shows no file added under `src/`.
    - `cd src/mcp/vice &amp;&amp; npm test` is green (regression gate; the full suite, not `test:automated`).
  </acceptance_criteria>
  <done>The rule and the schema are on disk and committed, and no measurement has been taken.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the binding evidence conventions and the corpus ignore rule, then bank the ordering proof</name>
  <files>
    .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/README.md,
    .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/corpus/.gitignore
  </files>
  <read_first>
    - `.planning/phases/09-the-assumption-probe-go-no-go/09-01-PLAN.md` — the `<evidence_conventions>` block being restated, and `09-07-PLAN.md`'s item 12 single-owner addition
    - `.planning/notes/dxa-ghidra-pivot-evidence/README.md` — the index shape: title line, a two-sentence "why this exists", then a `| File | What it is |` table where each row states both what the artifact is and what it proves
    - `.planning/phases/23-the-real-release-gate-go-degrade-no-go/23-PATTERNS.md` § *Throwaway artifacts survive tmpfs*, § *Corpus is untrusted input*
  </read_first>
  <action>
Write `evidence/README.md` carrying, verbatim, the ten-item `<evidence_conventions>` block
from this plan, under a heading that declares it binding on every plan in phase 23. Above
it put the pivot-README index shape: a title line naming the activity and the date, a
two-sentence statement of why the directory exists (working artifacts live at
`$HOME/.cache/c64-re-tools/phase23` because `/tmp` is tmpfs on this host and the pivot
already lost a dxa build to it; anything that matters is copied here before the session
ends), then a `| File | What it is | What it proves |` table pre-seeded with the artifacts
this phase will produce — `DECISION-RULE.md`, `SCHEMA.md`, `corpus/`, `tools/`,
`inventory/`, `capture/`, `criterion1-*.txt` through `criterion4-*.md` — each row stating
what the artifact proves, not merely what it is. Rows for artifacts that do not exist yet
say so; later plans fill them in.

Close the README with the external-input note: Ghidra is recorded in evidence by **version**
(`12.1.3 PUBLIC`, build `2026-Aug-17`), never by install path, because the probe install at
`~/dev/_ghidra-probe/` states in its own README that it is safe to delete and that nothing
in the repo depends on that path — it is an undeclared external input to this phase.

Write `evidence/corpus/.gitignore` so the corpus directory tracks its own metadata and
identity files while every binary image form is ignored: ignore `*.d64`, `*.prg`, `*.bin`,
`*.t64`, `*.crt`, `*.zip`, `*.gz`, and negate the ignore for `.gitignore` itself and for
`*.md` / `*.txt` / `*.json`. Put the ignore file inside the evidence tree rather than
editing the repository root `.gitignore`, because the phase's no-product-code invariant is
that `git diff --name-only` touches only `.planning/` and `docs/`, and a root-level edit
would break it for no benefit.

Then bank the ordering proof: append to `evidence/README.md` a short
`## Ordering proof` section containing the literal command
`git log --oneline -1 -- .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/DECISION-RULE.md`
followed by its real output, captured after Task 2's commit exists. This is what lets the
findings document cite the ordering without a reader running git, and it is why Pitfall 8
insists it be recorded before the first measurement.
  </action>
  <verify>
    <automated>bash -c 'D=.planning/phases/23-the-real-release-gate-go-degrade-no-go; test -f "$D/evidence/README.md" &amp;&amp; test -f "$D/evidence/corpus/.gitignore" &amp;&amp; grep -q "PROBE_DIR=\$HOME/.cache/c64-re-tools/phase23" "$D/evidence/README.md" &amp;&amp; grep -q "Ordering proof" "$D/evidence/README.md" &amp;&amp; git log --oneline --reverse -- "$D" | head -1 | grep -qiE "23-01|pre-commit|decision rule" &amp;&amp; test -z "$(git diff --name-only HEAD~3..HEAD -- src/)" &amp;&amp; echo CONVENTIONS-OK'</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/README.md` contains all ten numbered evidence conventions, including the `PROBE_DIR=$HOME/.cache/c64-re-tools/phase23` path, the final-occurrence-wins rule, and the single-owner file rule.
    - `evidence/README.md` records the Ghidra version string and does not record the Ghidra install path as a durable fact.
    - `evidence/corpus/.gitignore` ignores at least `*.d64`, `*.prg` and `*.bin`, and the repository root `.gitignore` is unmodified.
    - `git log --oneline --reverse -- <phase dir> | head -1` names this plan's first commit — nothing from any measuring plan precedes it.
    - `evidence/README.md` `## Ordering proof` section contains the literal `git log --oneline -1 --` command line and its real output, not a paraphrase.
    - `git diff --name-only` across this plan's commits touches only paths under `.planning/`.
  </acceptance_criteria>
  <done>The conventions, the ignore rule and the ordering proof are committed; measurement may now begin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner → future measuring plans | This plan's text is the only thing constraining what a later plan may claim; there is no runtime enforcement |
| git history → the verdict's credibility | Commit ordering is the sole integrity mechanism for PROOF-05 (D-08 declined a test guard) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-05 | Repudiation | `evidence/DECISION-RULE.md` vs. later measurement commits | high | mitigate | The rule commits alone in wave 1, before any measurement plan runs; `git log --oneline -1` of the rule file is banked into `evidence/README.md` in Task 3; the prohibitions block forbids post-measurement edits and requires any override to be recorded explicitly in the findings document |
| T-23-08 | Tampering | The pre-committed thresholds | high | mitigate | Thresholds are numeric and stated (10.00, 50.00, 0, 0) with an exact-at-threshold contract, so a later plan cannot re-read them favourably; Task 1 is a blocking decision checkpoint so the operator sees them before they become one-way |
| T-23-06 | Information Disclosure | `evidence/corpus/` | medium | mitigate | `evidence/corpus/.gitignore` ignores every binary image form before any image can arrive; identity is release name plus sha256 (D-04); the Ghidra install path is recorded as an external input, not as a durable repository fact |
| T-23-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing. 23-RESEARCH.md's Package Legitimacy Audit records **no npm, PyPI or crates package** in the phase; the one external artefact is a source tarball handled in 23-02, where the `T-23-SC` row is carried with its sha256 pin and both audited artefacts hold an **OK** verdict (no `[ASSUMED]`, no `[SUS]`, no `[SLOP]`), so no blocking legitimacy checkpoint is required |
</threat_model>

<artifacts_this_phase_produces>
This plan creates:

- `.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/DECISION-RULE.md` — the binding inputs table and rules `R1`..`R9`
- `.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/SCHEMA.md` — every outcome-line name and value domain, the `corpus.releases` list schema, the criterion-1 measurement definitions, the window derivation rule, the dxa flag set, the inventory schema, and the audit disposition vocabulary
- `.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/README.md` — the ten binding evidence conventions and the `## Ordering proof` section
- `.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/corpus/.gitignore` — keeps the corpus image out of the repository

Literal outcome-line names fixed here and emitted by later plans (no other name is
permitted): `C0_CORPUS`, `CORPUS_RELEASES`, `CORPUS_CANONICAL`, `CORPUS_FILE_SHA256`,
`CAPTURE_SHA256`, `CAPTURE_SIZE`, `CAPTURE_EQUIVALENT`, `CAPTURE_HANDOFF_PC`,
`CAPTURE_PORT01`, `DXA_VERSION`, `DXA_TARBALL_SHA256`, `DXA_TARBALL_SHA256_VERIFIED`,
`DXA_BINARY_SHA256`, `GHIDRA_VERSION`, `JAVA_VERSION`, `VICE_BACKEND`,
`FIXTURE_DATA_RECOVERY_PCT`, `FIXTURE_FALSE_POSITIVES`, `FIXTURE_FALSE_NEGATIVES`,
`FIXTURE_TOTAL_BYTES`, `FIXTURE_REPRODUCED`, `INV_SOURCE`, `INV_WINDOW`,
`INV_WINDOW_BYTES`, `INV_DISPATCH_SITES`, `INV_DISPATCH_SITE`, `INV_BANK_WRITE_SITES`,
`INV_BANK_VALUES`, `INV_HIT_CEILING`, `INV_TERMINATION`, `INV_CERTAIN_CODE_BYTES`,
`INV_CERTAIN_DATA_BYTES`, `INV_CODE_DATA_OVERLAP`, `INV_ADJUDICATED_FRACTION`,
`PROV_ANCHOR_OK`, `PROV_RELEASES`, `PROV_CRACKER_PATCH_BYTES`,
`PROV_CRACKER_PATCH_RANGES`, `PROV_HOLDOUT`, `C1_WINDOW`, `C1_WINDOW_BYTES`,
`C1_DXA_FLAGS`, `C1_DENOMINATOR_BYTES`, `C1_DATA_RECOVERY_PCT`, `C1_FALSE_POSITIVES`,
`C1_FALSE_NEGATIVES`, `C1_ADJUDICATED_FRACTION`, `C1_CRACKER_HELD_OUT`,
`C1_DATA_RECOVERY_PCT_WITH_CRACKER`, `C1_FALSE_POSITIVES_WITH_CRACKER`,
`C1_DETECT_INTERNAL_DATA_BYTES`, `C1_DETECT_ALL_DATA_BYTES`, `C1_PARSE_ACCOUNTED_BYTES`,
`C1_CODE_DATA_OVERLAP`, `C2_COMPUTED_DISPATCH`, `C2_SITES_ENUMERATED`,
`C2_COMPUTED_SITES`, `C2_RESOLVED_TARGET`, `C2_TRANSCRIPT`, `C2_REFERENCE_COUNT`,
`C2_CLASSIFICATION_LINES`, `C3_BANK_DIVERGENCE`, `C3_DIVERGENT_SITES`,
`C3_DIVERGENT_SITE`, `C3_MEMMAP_SHA256`, `C4_CAPABILITIES_AUDITED`, `C4_REPLACED`,
`C4_LOST_ACCEPTED`, `C4_UNREPLACED_CAPABILITIES`.

Findings-document frontmatter keys fixed here: `phase`, `requirement`, `probe_date`,
`verdict`, `verdict_rule_applied`, `corpus.releases[].release`,
`corpus.releases[].file_sha256`, `corpus.releases[].capture_sha256`,
`corpus.releases[].canonical`, `tools.dxa`, `tools.ghidra`, `tools.vice`, `criteria.*`.

No probe scripts are produced by this plan. No file under `src/` is created or modified.
</artifacts_this_phase_produces>

<verification>
- `git log --oneline --reverse -- <phase dir> | head -1` names a commit from this plan.
- `evidence/DECISION-RULE.md` and `evidence/SCHEMA.md` both exist and are committed.
- No file under `.planning/phases/23-*/evidence/criterion*` exists yet.
- `cd src/mcp/vice && npm test` green.
</verification>

<success_criteria>
The rule, the schema and the conventions are in git, no measurement has been taken, and
every literal name a later plan may emit is already fixed.
</success_criteria>

<output>
Create `.planning/phases/23-the-real-release-gate-go-degrade-no-go/23-01-SUMMARY.md` when done.
</output>
