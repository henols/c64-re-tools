---
phase: 23-the-real-release-gate-go-degrade-no-go
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/tools/instrument-provenance.txt
  - .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/tools/dxa-0.1.5.tar.gz.sha256
  - .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/tools/dxa
  - .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/fixture/fixture.a
  - .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/fixture/fixture.lbl
  - .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/fixture/fixture-baseline.txt
  - .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/dxa-listing-parse.mjs
  - .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/FlatVolatile.java
  - .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/ExportAnalysis23.java
autonomous: true
requirements: [PROOF-01, PROOF-02]
requirement_subparts:
  - "PROOF-01 — the 279-byte fixture's 72%/0-FP/28%-FN claim reproduced from source so D-11's side-by-side is apples-to-apples"
  - "PROOF-01 — dxa 0.1.5 pinned by sha256 and built, so DXA-01 (Phase 24) can vendor the same one the gate measured"
  - "PROOF-02 — the Ghidra pre-script and reference-export post-script proven working end-to-end before the real capture exists"

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "dxa 0.1.5 exists on this host, built from a tarball whose sha256 was verified before extraction, and both the tarball hash line and the built binary are inside evidence/ rather than in tmpfs"
    - "The 279-byte fixture's published 72% data-recovery / 0 false-positive / 28% false-negative claim is reproduced from source by this phase's own parser, so D-11's side-by-side compares one definition against itself"
    - "The whole instrument chain — assemble, run dxa, parse the listing, count bytes, run Ghidra headless, export references — is proven working end-to-end before any corpus byte is measured"
    - "The Ghidra volatile pre-script carves $0000-$0001 and $D000-$DFFF into their own blocks on a flat 64K import, and the block dump printed after the carve shows the remaining blocks non-volatile"
    - "The reference export enumerates every reference with no cap and prints the count, so a real release's thousands of references cannot truncate silently"
    - "Ghidra is recorded in evidence by version string, never by install path"
  artifacts:
    - path: ".planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/tools/instrument-provenance.txt"
      provides: "curl + sha256sum -c transcript, make transcript, DXA_VERSION / DXA_TARBALL_SHA256 / DXA_TARBALL_SHA256_VERIFIED / DXA_BINARY_SHA256 / GHIDRA_VERSION / JAVA_VERSION / VICE_BACKEND outcome lines"
      min_lines: 40
    - path: ".planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/fixture/fixture-baseline.txt"
      provides: "The reproduced fixture numbers: FIXTURE_DATA_RECOVERY_PCT, FIXTURE_FALSE_POSITIVES, FIXTURE_FALSE_NEGATIVES, FIXTURE_TOTAL_BYTES, FIXTURE_REPRODUCED"
      min_lines: 30
    - path: ".planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/dxa-listing-parse.mjs"
      provides: "The single dxa -a dump column parser used by every later criterion-1 computation, with the byte-total refusal assertion"
    - path: ".planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/FlatVolatile.java"
      provides: "The flat-64K volatile pre-script: split at $0002/$D000/$E000, then setVolatile on the carved $0000-$0001 and $D000-$DFFF blocks, with a block dump before and after"
    - path: ".planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/ExportAnalysis23.java"
      provides: "The uncapped ## CLASSIFICATION / ## REFERENCES export criteria 1 and 2 read, with the reference count asserted"
  key_links:
    - from: "evidence/tools/instrument-provenance.txt"
      to: "evidence/tools/dxa"
      via: "recorded sha256 of the built binary"
      pattern: "DXA_BINARY_SHA256: [0-9a-f]{64}"
    - from: "evidence/fixture/fixture-baseline.txt"
      to: "evidence/dxa-listing-parse.mjs"
      via: "the reproduced fixture numbers are produced by the same parser criterion 1 will use"
      pattern: "FIXTURE_DATA_RECOVERY_PCT: 72"
  prohibitions:
    - "A negative, partial or could-not-run outcome must NOT be summarised away, softened, or rewritten as a pass. If the fixture numbers do not reproduce, `FIXTURE_REPRODUCED: fail` is written with the observed values, and the discrepancy is carried into the findings document rather than tuned away."
    - "regenerator2000 must NOT be executed in any form — not the binary, not `r2000-coverage.ts`, not as an oracle, baseline or screening tool."
    - "Nothing under `src/` may be created or modified, and no probe script may be added to `resources/`, to `tools-manifest.json`, or to any other manifest. Probe scripts are evidence, not deliverables."
    - "The dxa tarball must NOT be extracted before its sha256 is verified, and must NOT be fetched over plain HTTP even though the FreeBSD ports MASTER_SITES records one."
---

<objective>
Provision the three instruments and prove the whole chain works end-to-end — on the
279-byte fixture, whose answer is already known — before a single corpus byte is measured.

Purpose: this is the phase's **tracer**. It wires one thin path through every layer the
phase will use (fetch → verify → build → assemble → run dxa → parse listing → count bytes
→ run Ghidra headless → carve volatile blocks → export references) against an input whose
correct output is published, so an architectural dead end surfaces here rather than after
the capture exists. It is not a throwaway rehearsal: the reproduced fixture numbers are
required evidence for D-11's side-by-side, and the parser and the two Java scripts written
here are the ones every later criterion plan uses unchanged.

Output: the built dxa pinned by hash inside `evidence/tools/`, the fixture baseline
numbers, and three probe scripts proven to run.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/README.md
@.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/SCHEMA.md
@.planning/phases/23-the-real-release-gate-go-degrade-no-go/23-RESEARCH.md
@.planning/phases/23-the-real-release-gate-go-degrade-no-go/23-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "a code/data map comes out of a binary" — the fixture path only</name>
  <files>
    .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/tools/instrument-provenance.txt,
    .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/tools/dxa-0.1.5.tar.gz.sha256,
    .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/tools/dxa,
    .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/fixture/fixture.a,
    .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/fixture/fixture.lbl,
    .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/dxa-listing-parse.mjs,
    .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/fixture/fixture-baseline.txt
  </files>
  <precondition>Network egress to `https://www.floodgap.com` is reachable, `gcc` and `make` are on `$PATH`, and `acme` is on `$PATH` (the researcher verified all four on this host this session). If the tarball cannot be fetched, halt and record `DXA_TARBALL_SHA256_VERIFIED: could-not-run` rather than substituting any other dxa build.</precondition>
  <read_first>
    - `.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/SCHEMA.md` — the dxa flag set and the outcome-line names this task must emit
    - `.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/README.md` — the ten binding evidence conventions, especially the transcript convention and `PROBE_DIR`
    - `.planning/phases/23-the-real-release-gate-go-degrade-no-go/23-RESEARCH.md` § *Standard Stack* → Installation, § *Package Legitimacy Audit*, § *Criterion 1: the measurement definition* → *The fixture's definition, recovered by reproduction*, § *Pitfall 4*, § *Code Examples* → *The `-a dump` classifier*
    - `.planning/notes/dxa-ghidra-pivot-evidence/fixture.a` and `fixture.lbl` — the 279-byte fixture source and its ground-truth symbols
    - `.planning/notes/dxa-ghidra-pivot-evidence/dxa.out` — the pivot's own dxa run, the source of the 72%/0-FP claim
    - `.planning/phases/09-the-assumption-probe-go-no-go/evidence/grammar-check.mjs` — the throwaway-probe-script header pattern: declares itself evidence, cites its real consumer by `file:line`, states why it does not import from it, and holds exactly one parser
  </read_first>
  <action>
Wire one path through every layer, end to end, against the fixture.

**Fetch and pin dxa.** Work in `PROBE_DIR=$HOME/.cache/c64-re-tools/phase23`, never `/tmp`.
Fetch `https://www.floodgap.com/retrotech/xa/dists/dxa-0.1.5.tar.gz` over HTTPS — not the
`http://` the FreeBSD ports `MASTER_SITES` records. Write the expected digest line
`8e40ed77816581f9ad95acac2ed69a2fb2ac7850e433d19cd684193a45826799  dxa-0.1.5.tar.gz` into
`evidence/tools/dxa-0.1.5.tar.gz.sha256` and verify it with `sha256sum -c -` **before**
extracting. Only then extract and build with plain `make` — no configure, no external
dependencies. Copy the built binary to `evidence/tools/dxa` and record its own sha256.
Record every command and its real output in `evidence/tools/instrument-provenance.txt` per
the transcript convention. Emit the outcome lines `DXA_VERSION`, `DXA_TARBALL_SHA256`,
`DXA_TARBALL_SHA256_VERIFIED`, `DXA_BINARY_SHA256`. Note in the file, as a property of the
tool under test rather than a supply-chain signal, that dxa's own man page describes it as
alpha software, and that the tarball carries no upstream signature and no upstream checksum
— the pin is corroborated only by the FreeBSD ports `devel/dxa65` distinfo.

**Record the other two instruments.** Read the Ghidra version from the install's own
`Ghidra/application.properties` (`application.build.date`) and emit
`GHIDRA_VERSION: 12.1.3 PUBLIC build 2026-Aug-17` — the **version**, never the install
path, because the probe install states in its own README that it is safe to delete and
that nothing in the repo depends on that path. Emit `JAVA_VERSION` from `java -version`
verbatim and `VICE_BACKEND: fork 3.10` from `/usr/local/bin/x64sc --version`, noting that
the fork is the measurement instrument because stock here is VICE 3.9, which lacks
`CPUHISTORY_GET` entirely, lacks `vice_backtrace`, lacks
`vice_checkpoint_set_ignore_count`, and auto-disables a trace checkpoint above 20 hits per
second.

**Rebuild the fixture and reproduce its numbers.** Copy
`.planning/notes/dxa-ghidra-pivot-evidence/fixture.a` and `fixture.lbl` into
`evidence/fixture/`. Assemble with `acme -f cbm -o fixture.prg -l fixture.lbl fixture.a`
— 281 bytes, being 2 header bytes plus 279. Run the built dxa with the pivot's own
published command line `-U -p all-nmos6502 -t detect-all -a dump` (this is the `.prg`
route, correct for a 279-byte file — the flat-64K flag set in SCHEMA.md is a different
invocation and is not used here).

**Write the one parser.** `evidence/dxa-listing-parse.mjs` is the single `-a dump` column
parser this phase owns; no second listing parser is written anywhere. Its header comment
declares it throwaway evidence, cites 23-RESEARCH.md § *Code Examples* → *The `-a dump`
classifier* as the shape it reproduces, and states that it deliberately does not import
from any `src/` module. It matches lines with a four-hex-digit address, one or more
two-hex-digit byte columns, then whitespace, then the text — the whitespace class matters
because `.word` lines are space-separated where instruction lines are tab-separated.
It classifies a line as data when the text begins `.byt` or `.word`, otherwise as code,
adds one address per emitted byte to the corresponding set, and **throws** if the accounted
byte total does not equal the declared image size, naming the accounted and expected counts
in the message. That refusal is the guard that turns an unseen line shape into a failure
rather than a silent undercount, and it is a free preview of DXA-02's own refusal
requirement. Take the image size as an explicit argument rather than inferring it.

**Compute and record the baseline.** Cross the parser's code and data sets against
`fixture.lbl`'s ground-truth partition of 141 code and 138 data bytes. Write
`evidence/fixture/fixture-baseline.txt` with the full transcript, then the outcome lines
`FIXTURE_TOTAL_BYTES`, `FIXTURE_DATA_RECOVERY_PCT` (expected `72.46`),
`FIXTURE_FALSE_POSITIVES` (expected `0`), `FIXTURE_FALSE_NEGATIVES` (expected `38`), and
`FIXTURE_REPRODUCED: pass` only if all three match the published figures. If any differs,
write `FIXTURE_REPRODUCED: fail`, record the observed values, and add a
`## RESEARCH CORRECTIONS` section stating what the pivot's published number actually was
versus what reproduced — the discrepancy is the finding, and 23-10 collects it. Also record
in that section whether the fixture's 141/138 partition was re-derived from `fixture.a` and
`fixture.lbl` byte by byte or taken from the pivot note, since 23-RESEARCH.md logs that as
assumption A1.
  </action>
  <verify>
    <automated>bash -c 'D=.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence; grep -v "^#" "$D/tools/instrument-provenance.txt" | grep -qE "^DXA_TARBALL_SHA256_VERIFIED: pass$" &amp;&amp; grep -v "^#" "$D/tools/instrument-provenance.txt" | grep -qE "^DXA_TARBALL_SHA256: 8e40ed77816581f9ad95acac2ed69a2fb2ac7850e433d19cd684193a45826799$" &amp;&amp; grep -v "^#" "$D/tools/instrument-provenance.txt" | grep -qE "^DXA_BINARY_SHA256: [0-9a-f]{64}$" &amp;&amp; grep -v "^#" "$D/tools/instrument-provenance.txt" | grep -qE "^GHIDRA_VERSION: 12\.1\.3" &amp;&amp; grep -v "^#" "$D/fixture/fixture-baseline.txt" | grep -qE "^FIXTURE_REPRODUCED: (pass|fail)$" &amp;&amp; grep -v "^#" "$D/fixture/fixture-baseline.txt" | grep -qE "^FIXTURE_DATA_RECOVERY_PCT: [0-9]+\.[0-9]{2}$" &amp;&amp; test -x "$D/tools/dxa" &amp;&amp; node --check "$D/dxa-listing-parse.mjs" &amp;&amp; echo TRACER-OK'</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/tools/instrument-provenance.txt` shows the `sha256sum -c -` line and its real `OK` output appearing **before** any `tar xzf` line in the transcript.
    - `DXA_TARBALL_SHA256` equals `8e40ed77816581f9ad95acac2ed69a2fb2ac7850e433d19cd684193a45826799` exactly, and `DXA_TARBALL_SHA256_VERIFIED: pass`.
    - `evidence/tools/dxa` exists, is executable, and its recorded `DXA_BINARY_SHA256` is a 64-hex string.
    - The transcript contains no `http://www.floodgap.com` fetch — only `https://`.
    - `GHIDRA_VERSION` carries the version and build date; the string `_ghidra-probe` does not appear on any outcome line.
    - `evidence/fixture/fixture-baseline.txt` carries `FIXTURE_TOTAL_BYTES: 279` and all four other `FIXTURE_*` outcome lines with numeric values.
    - `FIXTURE_REPRODUCED` is `pass` with `72.46` / `0` / `38`, or `fail` with the observed values and a `## RESEARCH CORRECTIONS` section explaining the divergence.
    - `evidence/dxa-listing-parse.mjs` parses under `node --check`, contains exactly one line-matching regular expression, and throws with both the accounted and expected byte counts in the message when the total does not match — demonstrated by a recorded run against a deliberately truncated copy of the listing.
    - `cd src/mcp/vice &amp;&amp; npm test` is green.
  </acceptance_criteria>
  <done>A binary goes in and a byte-exact code/data classification comes out, and the published fixture numbers are reproduced by this phase's own parser rather than quoted.</done>
</task>

<task type="auto">
  <name>Task 2: The flat-64K Ghidra volatile pre-script, proven on a synthetic 64K image</name>
  <files>.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/FlatVolatile.java</files>
  <read_first>
    - `.planning/notes/dxa-ghidra-pivot-evidence/BankProbe3.java` — the surrounding script structure to keep: `extends GhidraScript`, dump every existing block first, mutate, `println` every mutation, seed entry points, `analyzeAll()`, write to `getScriptArgs()[0]`
    - `.planning/phases/23-the-real-release-gate-go-degrade-no-go/23-RESEARCH.md` § *Pitfall 2* and § *Code Examples* → *Ghidra pre-script: volatile I/O on a flat 64K image (the corrected pattern)*
    - `.planning/phases/23-the-real-release-gate-go-degrade-no-go/23-PATTERNS.md` § *Ghidra pre/post-scripts under `evidence/`* — including the explicit do-not-copy-verbatim warning
    - `.planning/notes/ghidra-volatile-io-and-banking.md` — why `$0000-$0001` and `$D000-$DFFF` must be volatile before `analyzeAll()`
  </read_first>
  <action>
Write `evidence/FlatVolatile.java`, a `GhidraScript` pre-script for the flat-64K route.

Keep `BankProbe3.java`'s surrounding structure — dump every existing memory block with
name, start, end and volatility before touching anything; `println` every mutation as it
happens; dump the blocks again afterwards; seed entry points; call
`analyzeAll(currentProgram)`; write the result to `getScriptArgs()[0]`.

**Replace `makeVolatile()`'s body.** 23-CONTEXT.md's discretion note recommended reusing
`BankProbe3.java`'s `getBlock()`-first guard verbatim; 23-RESEARCH.md Pitfall 2 overturns
that, verified live. Ghidra's raw-binary loader creates exactly one block spanning
`RAM 0000-ffff`, so a `getBlock($D000)` returns that whole-image block and marking it
volatile suppresses dead-store elimination across the entire address space — with no error
and no warning, and the symptom looks like success. Instead: call `Memory.split(block,
addr)` at `$0002`, `$D000` and `$E000` first, each guarded by the block existing and not
already starting at that address, then call `setVolatile(true)` on the carved
`$0000-$0001` and `$D000-$DFFF` blocks only.

**Keep the `.prg` branch.** Where `getBlock(addr)` returns null — the `.prg` route, where
the image block does not cover I/O — fall back to `createUninitializedBlock` with
read, write and volatile set, exactly as `BankProbe3.java` does. Branch on which route is
in use rather than choosing one; the fixture rehearsal exercises the `.prg` branch and the
corpus run exercises the split branch, and both must work from one script.

**Entry points.** Accept a file of entry-point addresses as a script argument, one
four-hex-digit address per line, and for each: `disassemble`, `createFunction`, and
`addExternalEntryPoint`. Those addresses come from VICE runtime observation (D-06) and
never from dxa or Ghidra. When the file is absent or empty, print an explicit
`ENTRYPOINTS: none` line and continue — Ghidra alone on a headerless 6502 image produced
0 functions and 0 code bytes on the pivot fixture, so an empty entry-point set is a
recordable fact, not a silent no-op.

**Prove it.** Run `analyzeHeadless` against a synthetic 65536-byte image built for this
purpose (planted processor-port bytes at offset 0, a short instruction run somewhere in
the middle) with `-processor 6502:LE:16:default -loader BinaryLoader -loader-baseAddr 0x0
-noanalysis -preScript FlatVolatile.java -deleteProject`, and append the run log to
`evidence/tools/instrument-provenance.txt`. The block dump after the carve must show four
blocks, with volatility true on the `$0000-$0001` and `$D000-$DFFF` blocks and false on the
other two. Record in the transcript that the import also logs `Failed to add language
defined memory block due to conflict` for `ZERO_PAGE` and `STACK` — those are INFO lines
that a log grep for a failure keyword would trip on, and naming them here stops a later
plan misreading them as errors.
  </action>
  <verify>
    <automated>bash -c 'D=.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence; grep -q "Memory" "$D/FlatVolatile.java" &amp;&amp; grep -c "split" "$D/FlatVolatile.java" | grep -qE "^[1-9]" &amp;&amp; grep -q "createUninitializedBlock" "$D/FlatVolatile.java" &amp;&amp; grep -qE "vol=true" "$D/tools/instrument-provenance.txt" &amp;&amp; grep -cE "vol=true" "$D/tools/instrument-provenance.txt" | grep -qE "^[2-9]" &amp;&amp; echo VOLATILE-OK'</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/FlatVolatile.java` calls `Memory.split` at `$0002`, `$D000` and `$E000` before any `setVolatile` call, and each split is guarded by the block existing and not already starting at that address.
    - The script retains a `createUninitializedBlock` fallback branch for the `.prg` route where `getBlock` returns null.
    - The recorded `analyzeHeadless` run's post-carve block dump lists four blocks, exactly two of which report volatility true, and those two are the `$0000-$0001` and `$D000-$DFFF` ranges.
    - The script prints `ENTRYPOINTS: none` when handed an absent or empty entry-point file, and continues.
    - The transcript names the `ZERO_PAGE` and `STACK` conflict INFO lines explicitly, so a later log grep does not misclassify them.
    - `cd src/mcp/vice &amp;&amp; npm test` is green.
  </acceptance_criteria>
  <done>The volatile pre-script carves correctly on a flat 64K import and still works on the .prg route, proven by a recorded run.</done>
</task>

<task type="auto">
  <name>Task 3: The uncapped classification and reference export criteria 1 and 2 read</name>
  <files>.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/ExportAnalysis23.java</files>
  <read_first>
    - `.planning/notes/dxa-ghidra-pivot-evidence/ExportAnalysis.java` — the `## SECTION`-delimited export format and the `## CLASSIFICATION` per-byte loop being reused, and the `while (ri.hasNext() && n < 400)` reference cap being removed
    - `.planning/notes/dxa-ghidra-pivot-evidence/ghidra3.txt` — the fixture's own export, including the `082e -> 089a COMPUTED_JUMP` reference and the empty `## STRUCTS` section
    - `.planning/phases/23-the-real-release-gate-go-degrade-no-go/23-RESEARCH.md` § *Pitfall 5*, § *Code Examples* → *Ghidra post-script: the reference dump criterion 2 reads*
  </read_first>
  <action>
Write `evidence/ExportAnalysis23.java`, the post-script both criterion 1 and criterion 2
read. Base it on `ExportAnalysis.java`'s `## SECTION`-delimited plain-text format, one fact
per line, written to `getScriptArgs()[0]`.

Two sections are kept and both are fixed:

**`## CLASSIFICATION`** — one line per address, `<address> code|data|undef`, derived from
whether the containing code unit is an `Instruction`, a defined `Data`, or neither. On a
flat 64K image this is 65536 lines and roughly 1.5 MB, which is expected; append a
`## CLASSIFICATION_LINES <n>` line and have the script itself assert that `n` equals the
image size, failing loudly rather than exporting a short file.

**`## REFERENCES`** — every reference from the `ReferenceManager` iterator, formatted
`<from> -> <to> <referenceType>`, followed by `## REFERENCE_COUNT <n>`. The pivot's version
capped the loop at 400 iterations and truncated silently in address order, so the tail of a
real image simply vanished from the export; the fixture had 43 references and a real
release will have thousands. Remove the cap entirely. The reference kinds the join
consumes and that must survive the export are `READ`, `WRITE`, `READ_WRITE`, `DATA`,
`CONDITIONAL_JUMP`, `UNCONDITIONAL_JUMP`, `UNCONDITIONAL_CALL` and `COMPUTED_JUMP`.

Record in the file's header comment why the pivot evidence README's blanket "this is the
trap" label on `ExportAnalysis.java` is only partly right: only its `## STRUCTS` section —
the `DataTypeManager` composite query — is empty on 6502, and that section is therefore
dropped from this script; the classification and reference sections are exactly what
criteria 1 and 2 consume. Structural facts, where a later phase needs them, come from
`DecompInterface` as `Decomp.java` already demonstrates, and that is Phase 24's concern,
not this phase's.

Prove the script by re-running the fixture through `analyzeHeadless` with `FlatVolatile.java`
as the pre-script and this as the post-script, and append the run to
`evidence/tools/instrument-provenance.txt`. The fixture's export must show the
`082e -> 089a COMPUTED_JUMP` reference the pivot recorded — that reference is the immediate
`ldx #$02` easy case, and its presence here proves the export works, not that Ghidra
resolves computed dispatch. Say so in the transcript so a later reader cannot mistake this
rehearsal for criterion 2's answer.
  </action>
  <verify>
    <automated>bash -c 'D=.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence; grep -q "## REFERENCES" "$D/ExportAnalysis23.java" &amp;&amp; grep -q "## CLASSIFICATION" "$D/ExportAnalysis23.java" &amp;&amp; grep -q "REFERENCE_COUNT" "$D/ExportAnalysis23.java" &amp;&amp; grep -q "CLASSIFICATION_LINES" "$D/ExportAnalysis23.java" &amp;&amp; ! grep -qE "n *< *400" "$D/ExportAnalysis23.java" &amp;&amp; grep -q "COMPUTED_JUMP" "$D/tools/instrument-provenance.txt" &amp;&amp; echo EXPORT-OK'</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/ExportAnalysis23.java` emits `## CLASSIFICATION`, `## CLASSIFICATION_LINES <n>`, `## REFERENCES` and `## REFERENCE_COUNT <n>`, and contains no numeric iteration bound on the reference loop.
    - The script asserts the classification line count equals the image size and fails loudly when it does not.
    - The recorded fixture run's export contains the `082e -> 089a COMPUTED_JUMP` line, and the transcript states in prose that this is the immediate-index easy case and is not criterion 2's answer.
    - The header comment records that only `ExportAnalysis.java`'s `## STRUCTS` section is the documented trap, and that section is absent from this script.
    - `cd src/mcp/vice &amp;&amp; npm test` is green.
    - `git diff --name-only` across this plan's commits touches only paths under `.planning/`.
  </acceptance_criteria>
  <done>The export both measuring criteria read exists, is uncapped, self-asserts its own completeness, and has been run end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| floodgap.com → this host | An unsigned source tarball from a dormant upstream crosses here and is compiled and executed |
| Ghidra install at an undeclared external path → this phase | Version is an input to the verdict; the path is not a repository fact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-SC | Tampering | `dxa-0.1.5.tar.gz` source tarball (npm/pip/cargo equivalent) | high | mitigate | Fetch over HTTPS, never the `http://` MASTER_SITES value; verify sha256 `8e40ed77…826799` with `sha256sum -c -` **before** `tar xzf`; the pin is corroborated by the FreeBSD ports `devel/dxa65` distinfo; the verified tarball hash line and the built binary are vendored into `evidence/tools/` so the phase never re-fetches. 23-RESEARCH.md's Package Legitimacy Audit records verdict **OK** for both artefacts — no `[ASSUMED]`, no `[SUS]`, no `[SLOP]` — so no blocking human checkpoint is required |
| T-23-09 | Tampering | The built `dxa` binary drifting from what Phase 24 vendors | medium | mitigate | `DXA_BINARY_SHA256` recorded in evidence and carried into the findings document, so `DXA-01` can vendor the same build the gate measured |
| T-23-06 | Information Disclosure | Ghidra install path recorded as a durable fact | low | mitigate | Record `GHIDRA_VERSION` only; the install path is an undeclared external input and is named as such in `evidence/README.md` |
| T-23-04 | Injection | Probe scripts spawning tools with interpolated paths | high | mitigate | Argv arrays, never shell strings (evidence convention 3); `PROBE_DIR` is a fixed literal, not derived from any untrusted input; no corpus byte is touched by this plan at all |
</threat_model>

<artifacts_this_phase_produces>
This plan creates:

- `evidence/tools/instrument-provenance.txt` — fetch/verify/build transcript, the Ghidra and Java version records, and the two `analyzeHeadless` rehearsal logs. Outcome lines: `DXA_VERSION`, `DXA_TARBALL_SHA256`, `DXA_TARBALL_SHA256_VERIFIED`, `DXA_BINARY_SHA256`, `GHIDRA_VERSION`, `JAVA_VERSION`, `VICE_BACKEND`
- `evidence/tools/dxa-0.1.5.tar.gz.sha256` — the expected digest line, written before the fetch is verified
- `evidence/tools/dxa` — the built binary, vendored out of tmpfs
- `evidence/fixture/fixture.a`, `evidence/fixture/fixture.lbl` — the 279-byte fixture and its ground truth, copied in
- `evidence/fixture/fixture-baseline.txt` — outcome lines `FIXTURE_TOTAL_BYTES`, `FIXTURE_DATA_RECOVERY_PCT`, `FIXTURE_FALSE_POSITIVES`, `FIXTURE_FALSE_NEGATIVES`, `FIXTURE_REPRODUCED`
- `evidence/dxa-listing-parse.mjs` — the single `-a dump` column parser, throwaway evidence
- `evidence/FlatVolatile.java` — the flat-64K volatile pre-script, throwaway evidence
- `evidence/ExportAnalysis23.java` — the uncapped classification and reference export, throwaway evidence

No file under `src/` is created or modified. No probe script is registered in any manifest.
</artifacts_this_phase_produces>

<verification>
- All seven `evidence/tools/` and `evidence/fixture/` artifacts exist and are committed.
- The three probe scripts run without error against the fixture and the synthetic 64K image.
- `git diff --name-only` across this plan's commits touches only `.planning/`.
- `cd src/mcp/vice && npm test` green.
</verification>

<success_criteria>
Every instrument is pinned and recorded, the whole chain is proven end-to-end on an input
whose answer is published, and the fixture's own numbers are reproduced rather than quoted.
</success_criteria>

<output>
Create `.planning/phases/23-the-real-release-gate-go-degrade-no-go/23-02-SUMMARY.md` when done.
</output>
