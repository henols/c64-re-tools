---
phase: 19-absorbed-procedures-and-the-coverage-instrument
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/skills/c64-memory-mapping/SKILL.md
  - src/skills/c64-program-recon/SKILL.md
  - src/mcp/vice/skill-attribution.test.ts
  - installer/THIRD-PARTY-NOTICES.md
  - installer/package.json
  - scripts/check-npm-packages.mjs
autonomous: true
requirements: [ABS-01, ABS-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All five upstream analyze procedures are absorbed: `analyze-program` into `src/skills/routine-queue-walker/SKILL.md`, `analyze-blocks` and `analyze-symbol` into `src/skills/c64-memory-mapping/SKILL.md`, `analyze-routine` and `analyze-basic` into `src/skills/c64-program-recon/SKILL.md` — exactly the destinations the pinned manifest records"
    - "Every absorbed destination file carries its own six-field attribution block, one per absorbed source path, and `skill-attribution.test.ts`'s registry holds a row for each of the five source paths"
    - "The absorbed BASIC-token material is marked reference-only with its capability deferred, and none of its trigger phrases appear in any skill's `description:` frontmatter, so it can never fire"
    - "No absorbed step instructs a call to an `r2000_*` tool outside `CURATED_R2000_TOOLS`: `node scripts/check-skill-tool-coverage.mjs` exits 0 and its OK line reports every extracted name curated"
    - "The `@henols/c64-re-tools` tarball ships a third-party notices file, asserted against the packed file list rather than a repo path, mirroring the existing assertion for the other package"
    - "The installer's packed skill count still equals the count of `src/skills/*` directories carrying a `SKILL.md` after absorption adds no further directories"
  prohibitions:
    - requirement_id: ABS-01
      category: values
      statement: "MUST NOT ship an absorbed procedure step whose instruction cannot be carried out with the curated tool surface — a step requiring a tool this project does not expose must be rewritten or removed, never left as an aspirational instruction the reader cannot follow"
  # Edge-probe accounting (spec-less probe fallback, no-silent-drop rule):
  #   17 applicable items = 14 authored into must_haves.truths + 3 surfaced as flagged assumptions.
  # The three flagged rows are `unresolved` / `unclassified` and stay that way — an unclassified
  # row is never auto-resolved with a backstop marker and never promoted to a truth. One owner
  # each, no duplication: ABS-01 -> 19-02 (below), SURF-03 -> 19-04, ABS-04 -> 19-05.
  flagged_assumptions:
    - requirement_id: ABS-01
      source: edge-probe
      status: unresolved
      category: unclassified
      probe_accounting: "17 applicable = 14 authored + 3 flagged"
      assumption: "\"Five procedures absorbed\" is an EXACT count fixed by the requirement text and by the pinned manifest's `procedures.length` — not a growing census. The probe returned no classified edge predicate for ABS-01, so nothing here checks the count's *edge* behaviour: the registry-length assertion in Task 1(e) is a relation against `manifest.procedures.length` and nothing more, and a reviewer must read the five destinations in the manifest to judge whether the count is right rather than merely self-consistent."
  artifacts:
    - path: "src/skills/c64-memory-mapping/SKILL.md"
      provides: "Absorbed block-classification and symbol-analysis procedures, attributed"
      contains: "ATTRIBUTION"
    - path: "src/skills/c64-program-recon/SKILL.md"
      provides: "Absorbed routine-analysis procedure plus the reference-only BASIC material, attributed"
      contains: "ATTRIBUTION"
    - path: "installer/THIRD-PARTY-NOTICES.md"
      provides: "The notices document that ships inside the package carrying the absorbed prose"
      min_lines: 20
  key_links:
    - from: "installer/package.json"
      to: "installer/THIRD-PARTY-NOTICES.md"
      via: "files[] entry so the notices document is packed"
      pattern: "THIRD-PARTY-NOTICES.md"
    - from: "scripts/check-npm-packages.mjs"
      to: "installer/THIRD-PARTY-NOTICES.md"
      via: "assertion over the installer tarball's own packed file list"
      pattern: "installer: missing THIRD-PARTY-NOTICES.md"
    - from: "src/mcp/vice/skill-attribution.test.ts"
      to: "src/skills/c64-memory-mapping/SKILL.md"
      via: "registry row asserting header-vs-manifest agreement for two absorbed source paths"
      pattern: "c64-memory-mapping"
---

<!-- planner-discipline-allow: .agent/skills -->
<!-- planner-discipline-allow: r2000_toggle_splitter -->
<!-- planner-discipline-allow: r2000_undo -->
<!-- planner-discipline-allow: r2000_set_immediate_format -->
<!-- planner-discipline-allow: r2000_unpack_binary -->
<!-- planner-discipline-allow: r2000_get_disassembly_cursor -->

<objective>
Absorb the remaining four upstream analyze procedures into the two existing skills that already
own their jobs, each attributed per source path, and make the package that actually ships the
absorbed prose carry a notices document.

Purpose: plan 19-01 proved the whole absorption chain on one file. This plan repeats it four
times along the proven path, and closes the one attribution gap 19-01 deliberately left: the
`@henols/c64-re-tools` tarball is the package that carries the absorbed text to end users, and
it currently ships no notices file at all.

Output: five absorbed procedures total, five attribution blocks, one new notices document, and
one new packed-payload assertion.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-RESEARCH.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-PATTERNS.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Absorb the remaining four procedures into the two skills that own their jobs</name>
  <precondition>A local clone of https://github.com/ricardoquesada/regenerator2000 at object `493f840418f1450a342bb220c2fe3d2585dd0525` is reachable and its five `.agent/skills/r2000-analyze-*/SKILL.md` files hash to the manifest's sha256 values — plan 19-01 re-established this clone outside `/tmp`, which is RAM on this host.</precondition>
  <files>src/skills/c64-memory-mapping/SKILL.md, src/skills/c64-program-recon/SKILL.md, src/mcp/vice/skill-attribution.test.ts</files>
  <read_first>
    - `src/skills/routine-queue-walker/SKILL.md` — the shape plan 19-01 established: where the attribution block sits, how deviations are named, how a non-curated upstream call is referred to without its prefix
    - `src/mcp/vice/skill-attribution.test.ts` — the registry to extend; its header states the presence-AND-absence discipline and that later plans add rows
    - `src/skills/c64-memory-mapping/SKILL.md` — the whole file, the absorption target for two procedures; note its existing description and section structure
    - `src/skills/c64-program-recon/SKILL.md` — the whole file, the absorption target for two procedures; note that its existing description already claims "which memory regions are code versus data", which is a direct trigger fight with the upstream block-classification procedure
    - the upstream files at the pin: `.agent/skills/r2000-analyze-blocks/SKILL.md` (note lines 85, 86, 94, 158, 213-217, 223), `.agent/skills/r2000-analyze-symbol/SKILL.md` (note lines 12, 57, 58, 113), `.agent/skills/r2000-analyze-routine/SKILL.md` (note lines 20, 66, 67, 103), `.agent/skills/r2000-analyze-basic/SKILL.md` (all five of its tool references are curated)
    - `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-RESEARCH.md` sections 1.3 (the five per-call dispositions with their verbatim upstream justifications), 1.4, 1.5, 1.6 (the ABS-01 to FUT-01 reconciliation), 4.3 (the qualitative trigger fights to resolve while rewriting)
    - `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json` — `procedures[0]`, `[1]`, `[3]`, `[4]` fix the four destinations and their per-tool dispositions; `disposition_rationale` (added by 19-01) carries the justification per non-curated call
    - `src/mcp/vice/r2000-tools.ts` — `R2000_TOOL_DEFINITIONS` for every tool named in the absorbed prose, and `R2000_READ_REGION_MAX_BYTES`, since the range-read route replaces the cursor route
    - `.planning/REQUIREMENTS.md` — `FUT-01`'s exact deferral wording, and `DECOMP-01` / `BUILD-02` / `BUILD-03`, the requirements two omitted upstream calls acquire criteria from
  </read_first>
  <action>
Absorb the four remaining procedures into their manifest-fixed destinations, each as a clearly
delimited, attributed section inside the existing skill rather than a replacement of it.

(a) Into `src/skills/c64-memory-mapping/SKILL.md`, absorb `r2000-analyze-blocks` and
`r2000-analyze-symbol`. Carry the block-classification sequencing and the symbol-purpose
sequencing, including upstream's own most-dangerous-mistake rule verbatim in substance: never
disassemble a region without concrete proof it executes — a branch target, a JSR or JMP target,
a vector-table entry, or explicit user confirmation — because random data routinely disassembles
into plausible instruction sequences and that is not evidence of code. Route the cursor-based
entry step to explicit address input; upstream's own text forbids the cursor route in exactly
the situation this project is in, and this project has no editor cursor at all. Replace the
undo-on-wrong-conversion step with "set the correct type again", since the data-type setter is
idempotent over a range. Where upstream tells the reader to insert a table boundary marker
between two adjacent tables, keep the *hazard* — adjacent same-type tables merge, and the block
listing respects markers — as a named, dated limitation with a forward pointer, and refer to
the upstream call by its bare verb without the `r2000_` prefix.

(b) Into `src/skills/c64-program-recon/SKILL.md`, absorb `r2000-analyze-routine` and
`r2000-analyze-basic`. For the routine procedure: carry the read-the-range, name-it,
document-it, enum-the-registers sequencing; route the cursor step to explicit address input plus
the range read, honouring the read-region byte ceiling; refer to the low/high-byte immediate
formatting step by its bare verb, mark it as not currently exposed, and name the requirement
that supplies its criterion. For the BASIC procedure: absorb its text as a clearly headed
REFERENCE-ONLY section that states in its first line that the capability is deferred and names
the deferral's requirement ID and the reason (commercial titles captured post-loader reduce to a
one-line stub). Keep every one of its trigger phrases OUT of the file's `description:`
frontmatter, so the section can be read but never fires as a trigger. Do not add BASIC
vocabulary to any skill's description.

(c) Give each absorbed section its own six-field attribution block, immediately before the
section (not at the top of the file — one file now carries two source paths, so one block per
source path is the only honest shape). Each block names its own source path, its own sha256, the
shared commit and licence facts, this project's elected option, and the deviations specific to
that procedure.

(d) While rewriting, resolve the three known trigger fights by sharpening the *absorbed section
headings and body*, not the frontmatter: the symbol procedure versus this file's existing
address-lookup job; the block procedure versus the recon skill's existing code-versus-data
claim; and the whole-program orchestration overlap with the seventh skill. Frontmatter
description sharpening is plan 19-05's job and must not be pre-empted here — leave every
`description:` byte-identical to what plans 19-01 and prior phases left, so 19-05 measures a
stable corpus.

(e) Extend `src/mcp/vice/skill-attribution.test.ts`'s registry with one row per newly absorbed
source path, so all five upstream source paths are covered. Assert the registry's length equals
the manifest's `procedures.length`, so a forgotten row fails instead of passing silently. Add a
dedicated assertion that no skill file's `description:` frontmatter value contains any member of
a named, non-empty set of BASIC-capability trigger phrases, and assert that set's own length is
non-zero so an emptied set cannot make the check vacuous.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-skill-tool-coverage.mjs && node scripts/check-skill-fork-honesty.mjs && cd src/mcp/vice && node --experimental-strip-types --test skill-attribution.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-skill-tool-coverage.mjs` exits 0 and prints `check-skill-tool-coverage: OK`
    - `grep -c 'r2000_toggle_splitter' -r src/skills/` returns 0; the same holds for `r2000_undo`, `r2000_set_immediate_format`, `r2000_unpack_binary` and `r2000_get_disassembly_cursor`
    - `grep -rc '\.agent/skills' src/skills/` returns 0 across every file
    - `src/skills/c64-memory-mapping/SKILL.md` contains the sha256 values `3fad6193466a20fa0d2f56a7e38a740fa7218b920aa36e348bc65273c987aa1b` and `d57d9c2fdfa1c3e2f8a6384a881378ad1e3e371114c3b0b8d15ec1c71b3b4da8`
    - `src/skills/c64-program-recon/SKILL.md` contains the sha256 values `6fd26337de42b2d8f7da570ec7c5aa47072818f4cede675d8189930cadbe2730` and `8fc662ce52a1c947e0b57b92a8efb8e2f387a4cdad117de2b5300f50d44c23a2`
    - `src/skills/c64-program-recon/SKILL.md` contains the literal `FUT-01` and the word `REFERENCE-ONLY` inside the absorbed BASIC section
    - Every `description:` line under `src/skills/` is byte-identical to its state at the end of plan 19-01: `git diff 19-01's commit -- src/skills | grep -c '^[+-]description:'` returns 0
    - `node --experimental-strip-types --test skill-attribution.test.ts` exits 0 and its registry length assertion compares against `manifest.procedures.length`
    - Deleting one registry row makes `skill-attribution.test.ts` exit non-zero (demonstrate, then restore)
  </acceptance_criteria>
  <done>All five upstream procedures are absorbed at the pin, each attributed per source path, none calling a tool this project does not expose, and the deferred BASIC capability cannot fire.</done>
</task>

<task type="auto">
  <name>Task 2: Make the package that ships the absorbed prose carry a notices document</name>
  <files>installer/THIRD-PARTY-NOTICES.md, installer/package.json, scripts/check-npm-packages.mjs</files>
  <read_first>
    - `installer/package.json` — the `files[]` array (three entries: a bin directory, a skills directory, a README) and the `prepack` script that syncs the skills copy
    - `installer/scripts/sync-skills.mjs` — its header records that its exclusion rule is deliberately NOT shared with the packaging checker, so the gate can catch the copier being wrong; preserve that, do not deduplicate them
    - `scripts/check-npm-packages.mjs` — the whole file: `packFiles()` runs `npm pack --dry-run --json` (which triggers the installer's prepack sync), `assertLeanTarball()` runs from inside it, and the existing notices assertion for the other package near line 110 is the exact shape to mirror
    - `src/mcp/vice/THIRD-PARTY-NOTICES.md` — the canonical document plan 19-01 corrected; the installer document must agree with it and not restate it wrongly
    - `src/mcp/vice/package.json` — its `files[]` entry for the notices file, the precedent
    - `.gitignore` — the entry that makes `installer/skills/` a generated, untracked copy; do not attempt to commit that tree
    - `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-RESEARCH.md` section 5.3 — why per-file headers are the primary mechanism and the notices file is the complementary one
  </read_first>
  <action>
Give the installer package a notices document and make its presence a packed-payload assertion.

(a) Create `installer/THIRD-PARTY-NOTICES.md`. State that this package is MIT-licensed with the
project's copyright holder; then, in a section structured like the canonical document's
incorporated-material sections, record the regenerator2000 analysis procedures: the repository,
the full 40-hex pinned commit and its date, the five source paths, the upstream dual licence
string with its copyright line, this project's elected option, and the statement that the text
is adapted rather than verbatim with a pointer to the per-file attribution blocks that carry the
per-file digests. Add a pointer line to the canonical document in the MCP-server package for the
full third-party inventory, so the two documents do not drift into two competing inventories.
Do not restate the cc65 or ACME provenance here — neither ships in this package.

(b) Add the notices filename to `installer/package.json`'s `files[]`.

(c) Add an assertion to `scripts/check-npm-packages.mjs`'s installer block, mirroring the
existing one for the other package: read the notices filename out of the installer tarball's own
packed file list, never off the filesystem, and fail with a message naming the requirement it
serves. Add a comment beside it stating why the check reads the packed list rather than a repo
path — a repo-path check passes even when the published package omits the file, which is the
exact failure class the file's header already names.

(d) Run the packaging checker and confirm both tarballs are still lean: no committed test file,
no fixtures directory, no dependency tree, and the packed-package name set still pinned to
exactly the two known packages. `installer/skills/` is gitignored and untracked, so nothing
about it is committed — the sync runs inside the pack.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-npm-packages.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `installer/THIRD-PARTY-NOTICES.md` exists and contains the literals `MIT OR Apache-2.0`, `493f840418f1450a342bb220c2fe3d2585dd0525`, and all five upstream source paths
    - `node -e "process.exit(require('./installer/package.json').files.includes('THIRD-PARTY-NOTICES.md')?0:1)"` exits 0
    - `node scripts/check-npm-packages.mjs` exits 0 and prints no leak or missing-file message
    - Temporarily removing the notices entry from `installer/package.json`'s `files[]` makes `node scripts/check-npm-packages.mjs` exit non-zero with a message naming the installer package (demonstrate, then restore)
    - `git status --porcelain installer/skills` produces no output — the synced copy is untracked and nothing tried to commit it
    - `cd src/mcp/vice && node --experimental-strip-types --test ci-guardrails.test.mjs` exits 0
  </acceptance_criteria>
  <done>The tarball that actually carries the absorbed third-party prose ships a notices document, and its presence is asserted against the packed payload rather than a repo path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream repository at the pin → `src/skills/` | four more third-party procedure texts cross into the tree and into the published installer tarball |
| absorbed prose → Claude Code's trigger matcher | untrusted text matched by description; a deferred capability's triggers must not enter the matcher |
| `installer/` package → npm consumers | the notices document and the per-file headers are the only attribution that travels |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-07 | Repudiation | four new absorbed sections | high | mitigate | One six-field attribution block per absorbed source path, not one per file; `skill-attribution.test.ts`'s registry length is asserted equal to the manifest's procedure count, so a missing block fails rather than passing |
| T-19-08 | Repudiation | the installer tarball | high | mitigate | A notices document is added to `files[]` and its presence asserted against the packed file list, mirroring the existing assertion for the sibling package; the assertion is proven to fire by removing the entry |
| T-19-09 | Tampering | an absorbed step naming a tool this project does not expose | medium | mitigate | `scripts/check-skill-tool-coverage.mjs` fails on any non-curated `r2000_*` token anywhere under `src/skills/`; non-curated calls are referred to by bare verb and their prefixed record kept under `.planning/`, and no allowlist is added to that gate |
| T-19-10 | Elevation of Privilege | the deferred BASIC capability firing as a trigger | medium | mitigate | Its trigger phrases are kept out of every `description:`, and a named non-empty phrase set is asserted absent from every description, with the set's own length asserted non-zero |
| T-19-11 | Information Disclosure | the installer tarball's packed contents | medium | mitigate | `assertLeanTarball()` already runs from inside the one packing seam, so this package cannot skip the no-test-file / no-fixtures / no-dependency-tree checks; the packed-package name set stays pinned to two |
| T-19-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable this plan — no package-manager install task exists. `installer/package.json`'s single dependency is unchanged and remains asserted by `scripts/check-npm-packages.mjs` |

Canon-referral breadcrumb (recorded, deliberately not minted as a prohibition): archive/path
traversal inside a packed tarball is canon — covered by `/gsd-secure-phase` and by the existing
lean-tarball assertions.
</threat_model>

<verification>
- `node scripts/check-skill-tool-coverage.mjs`, `node scripts/check-skill-fork-honesty.mjs`, `node scripts/check-npm-packages.mjs` — all exit 0
- `cd src/mcp/vice && node --experimental-strip-types --test skill-attribution.test.ts ci-guardrails.test.mjs ci-suite-coverage.test.ts`
- `cd src/mcp/vice && npm test` — the full suite, never the automated subset
- `cd src/mcp/vice && node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json`
</verification>

<success_criteria>
Five upstream procedures are absorbed at one pinned commit into project-owned, attributed skill
text; no absorbed step calls a tool this project does not expose; the deferred BASIC capability
is readable but cannot fire; and the package that ships the absorbed prose carries a notices
document asserted against its own packed payload.
</success_criteria>

## Artifacts this phase produces

New files:
- `src/skills/routine-queue-walker/SKILL.md` (new, seventh skill directory)
- `installer/THIRD-PARTY-NOTICES.md`
- `src/mcp/vice/skill-attribution.test.ts`
- `src/mcp/vice/r2000-coverage.ts`
- `src/mcp/vice/r2000-coverage.test.ts`
- `src/mcp/vice/skill-description-overlap.test.ts`
- `src/mcp/vice/fixtures/coverage/` — the NC1, NC1b, NC2, NC3, NC4, NC5 synthetic project fixtures
- `scripts/lib/skill-descriptions.mjs`
- `scripts/check-skill-description-overlap.mjs`
- `src/skills/c64-program-recon/scripts/packer-finding.mjs`
- `src/skills/c64-program-recon/scripts/packer-finding.test.mjs`
- `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/evidence/measure-stdio-multiplexing.mjs`
- `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-STDIO-MULTIPLEXING-EVIDENCE.md`
- `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/evidence/coverage-reproducibility/QUESTION.md`, `ANSWER.md`, `ANSWER.sha256`, `RE-DERIVED-ANSWER.md`
- `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-DECISIONS.md`
- `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/COVERAGE.md`

New exported symbols:
- `src/mcp/vice/r2000-coverage.ts`: `COVERAGE_SCHEMA_VERSION`, `CoverageReport`, `CoverageOptions`, `StructuralCensus`, `IndirectDispatchScan`, `LabelRatio`, `CommentVacuity`, `Reproducibility`, `buildCoverageReport()`, `computeStructuralCensus()`, `scanIndirectDispatch()`, `computeLabelRatio()`, `computeCommentVacuity()`, `computeReproducibility()`, `AUTO_NAME_PREFIX_RE`, `BANNED_GENERIC_COMMENTS`, `R2000CoverageInputError`
- `scripts/lib/skill-descriptions.mjs`: `parseSkillFrontmatter()`, `splitTriggerClauses()`, `normaliseClause()`, `clauseTokens()`, `jaccard()`, `descriptionCollisions()`, `DESCRIPTION_OVERLAP_THRESHOLD`, `COLLISION_ALLOWLIST`, `FUNCTION_STOP_WORDS`, `DOMAIN_STOP_WORDS`
- `src/skills/c64-program-recon/scripts/packer-finding.mjs`: `packerFinding()`, `probeUnp64()`, `parseUnp64Stdout()`, `PACKER_VERDICTS`
- `src/mcp/vice/r2000-cli.ts`: `cmdCoverage()`, plus a `coverage` entry in the existing `VERB_OPTIONS`

New CLI surface:
- verb `vice-mcp r2000 coverage <project> [--out FILE] [--force] [--sample N]`
- skill script invocation `node src/skills/c64-program-recon/scripts/packer-finding.mjs <file>`

New env vars: `UNP64`, `UNP64_PATH`, `VICE_REQUIRE_UNP64`

Modified with new symbols:
- `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json`: `commit` widened to the full 40-hex SHA; new keys `upstream_version`, `licence`, `licence_copyright`, `elected_licence`, `resync_triggers[]`, `disposition_rationale{}`, and per-procedure `bytes`
- `scripts/lib/r2000-cli-verbs.mjs`: `R2000_CLI_VERB_FLOOR` raised from 7 to 8
- `src/mcp/vice/r2000-verb-coverage.test.ts`: `REAL_VERBS` gains `coverage`

<output>
Create `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-02-SUMMARY.md` when done
</output>
