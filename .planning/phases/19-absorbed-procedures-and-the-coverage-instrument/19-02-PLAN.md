---
phase: 19-absorbed-procedures-and-the-coverage-instrument
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified: ["src/skills/c64-program-recon", "src/skills/c64-memory-mapping", "src/skills/routine-queue-walker", "src/mcp/vice/THIRD-PARTY-NOTICES.md", "scripts/check-skill-description-overlap.mjs"]
autonomous: false
requirements: [ABS-01, ABS-02, ABS-03, ABS-04]
---
<objective>Absorb the audited procedures as project-owned, attributed skill guidance without trigger collisions or a runtime dependency on upstream skill directories.</objective>
<tasks>
<task type="auto"><name>Adapt the five procedures into shipped skills</name><files>src/skills/c64-program-recon, src/skills/c64-memory-mapping, src/skills/routine-queue-walker, src/mcp/vice/THIRD-PARTY-NOTICES.md</files><action>Use the manifest to adapt each procedure into the appropriate existing skill or the new routine-queue-walker. Add a per-file attribution header naming repository, source path, commit, licence, and adaptation boundary. Add a notice entry for incorporated procedure prose under `MIT OR Apache-2.0`; retain only project-owned execution instructions and reference the manifest rather than `.agent/skills`.</action><verify>node scripts/check-skill-tool-coverage.mjs && node scripts/check-npm-packages.mjs</verify><acceptance_criteria>All five are installed from `src/skills`, attributed, and call only curated tools.</acceptance_criteria></task>
<task type="auto"><name>Add inventory-wide trigger collision validation</name><files>scripts/check-skill-description-overlap.mjs</files><action>Parse every shipped skill frontmatter description, normalize trigger terms, and report every pairwise collision with both paths and terms. Add a deliberate collision fixture/control so an empty or self-derived comparison cannot pass. Resolve actual collisions by sharpening descriptions, never by silently excluding a skill.</action><verify>node scripts/check-skill-description-overlap.mjs</verify><acceptance_criteria>The whole shipped inventory passes and a planted duplicate description fails.</acceptance_criteria></task>
</tasks>
<threat_model>Copied prose can omit attribution or create ambiguous skill dispatch. Pin attribution fields and fail the inventory checker on collisions.</threat_model>
<verification>Run tool coverage, overlap checker, package-content validator, and focused skill tests.</verification>
<success_criteria>ABS-01 through ABS-04 are represented by installed, attributable, uniquely triggerable skills.</success_criteria>

