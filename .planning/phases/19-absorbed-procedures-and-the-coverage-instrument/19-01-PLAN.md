---
phase: 19-absorbed-procedures-and-the-coverage-instrument
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/skills/routine-queue-walker/SKILL.md
  - scripts/check-npm-packages.mjs
  - src/mcp/vice/THIRD-PARTY-NOTICES.md
  - THIRD-PARTY-NOTICES.md
  - CLAUDE.md
  - src/mcp/vice/skill-attribution.test.ts
  - src/mcp/vice/r2000-upstream-audit.test.ts
  - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json
  - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/evidence/measure-stdio-multiplexing.mjs
  - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-STDIO-MULTIPLEXING-EVIDENCE.md
autonomous: true
requirements: [ABS-01, ABS-02, ABS-04]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A seventh skill directory `src/skills/routine-queue-walker/` exists, carries a `SKILL.md` with `name` + `description` frontmatter, and mentions no `r2000_*` tool name outside `CURATED_R2000_TOOLS`"
    - "`node scripts/check-npm-packages.mjs` exits 0 with seven skills present, because the installer skill count is asserted as a relation against `src/skills/*/SKILL.md` rather than a literal"
    - "`src/mcp/vice/THIRD-PARTY-NOTICES.md` carries an `## Incorporated material` section for the regenerator2000 analysis procedures naming the repository, the full 40-hex pinned commit, the five source paths, the five sha256 digests, the dual `MIT OR Apache-2.0` licence and this project's MIT election"
    - "The absorbed file's attribution header carries all six provenance fields and its `sha256` and commit strings equal the manifest's values for the same source path, compared as lowercase-hex string equality over the raw upstream bytes"
    - "A `src/skills/*/SKILL.md` that carries no absorbed content is not required to carry an attribution header, and `skill-attribution.test.ts` FAILS (never skips) when its absorbed-file registry is empty"
    - "No file under `src/skills/` instructs the reader to read a file inside the upstream repository's excluded agent-skills directory; the guard asserts absence AND is proven to fire on a planted re-insertion"
    - "`r2000-upstream-audit.test.ts` rejects an abbreviated or floating upstream ref, requires a non-empty justification for every non-curated upstream call, and requires a named re-sync trigger"
    - "`.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-STDIO-MULTIPLEXING-EVIDENCE.md` records the measured answer to D18-16 with the executed command sequence and the raw observed output of at least one run"
    - statement: "The commit that adds the first absorbed SKILL.md also modifies `src/mcp/vice/THIRD-PARTY-NOTICES.md`, so no intermediate commit ships absorbed prose under a notices file that denies incorporation"
      verification: backstop
  prohibitions:
    - requirement_id: ABS-02
      category: transparency
      statement: "MUST NOT claim verbatim provenance for adapted text, or adapted provenance for verbatim text — the attribution header's adaptation statement and its named deviations must match what was actually changed"
    - requirement_id: ABS-02
      category: transparency
      statement: "MUST NOT ship a documentation claim that the same commit falsifies — no notices, README or CLAUDE.md sentence may assert that nothing from regenerator2000 is incorporated while incorporated prose is in the tree"
  artifacts:
    - path: "src/skills/routine-queue-walker/SKILL.md"
      provides: "The absorbed routine-queue-walking procedure, attributed, serialised, curated-tools-only"
      min_lines: 80
    - path: "src/mcp/vice/skill-attribution.test.ts"
      provides: "Frozen-registry presence/absence guard over attribution headers and upstream runtime reads"
      min_lines: 60
    - path: ".planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-STDIO-MULTIPLEXING-EVIDENCE.md"
      provides: "The D18-16 measurement of record"
      min_lines: 40
    - path: ".planning/phases/19-absorbed-procedures-and-the-coverage-instrument/evidence/measure-stdio-multiplexing.mjs"
      provides: "Self-contained, re-runnable measurement driver emitting one JSON result line"
      min_lines: 40
  key_links:
    - from: "src/skills/routine-queue-walker/SKILL.md"
      to: ".planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json"
      via: "attribution header quotes the manifest's commit and the sha256 for .agent/skills/r2000-analyze-program/SKILL.md"
      pattern: "493f840418f1450a342bb220c2fe3d2585dd0525"
    - from: "src/mcp/vice/skill-attribution.test.ts"
      to: ".planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json"
      via: "reads the manifest and asserts header-vs-manifest agreement per absorbed file"
      pattern: "upstream-procedure-manifest.json"
    - from: "scripts/check-npm-packages.mjs"
      to: "scripts/lib/skill-corpus.mjs"
      via: "topLevelSkillDirs() supplies the relation the installer skill count is asserted against"
      pattern: "topLevelSkillDirs"
---

<!-- planner-discipline-allow: 493f840 -->
<!-- planner-discipline-allow: .agent/skills -->
<!-- planner-discipline-allow: === 6 -->
<!-- planner-discipline-allow: No regenerator2000 source -->
<!-- planner-discipline-allow: rolling window -->

<objective>
Land ONE absorbed upstream procedure end to end — pinned, attributed, curated-tools-only,
packaged, and green across the whole CI guard chain — and record the measurement that
justifies the one deviation the absorption makes from upstream's own instructions.

Purpose: `r2000-analyze-program` is the procedure that forces every layer this phase touches.
It is the only one that creates a new skill directory (so it trips the installer's skill-count
pin), it is the only one that instructs the reader to read files inside upstream's excluded
agent-skills directory (three sites), and it is the only one that prescribes a seven-way
concurrent fan-out this project measured to be worthless. Proving the whole chain on this one
file — before the other four land — catches an architectural dead end after one commit
instead of after five.

Output: the seventh skill, the corrected packaging relation, the truthful notices sections,
two mechanical attribution guards, and the D18-16 evidence artifact.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-RESEARCH.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-PATTERNS.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json
@.planning/phases/18-persistent-session-and-tool-surface/18-06-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — the seventh skill lands attributed, curated, packaged, and green</name>
  <precondition>`git rev-parse --verify 493f840418f1450a342bb220c2fe3d2585dd0525` succeeds inside a local clone of https://github.com/ricardoquesada/regenerator2000, or `git clone` of that repository succeeds and the pinned object resolves — the previously-used clone lived under `/tmp`, which is RAM on this host and does not survive a reboot.</precondition>
  <reversibility rating="costly">Electing MIT from regenerator2000's dual `MIT OR Apache-2.0` for the absorbed text is a licence claim published in two npm tarballs; re-electing later means re-issuing both notices files and every per-file header.</reversibility>
  <files>src/skills/routine-queue-walker/SKILL.md, scripts/check-npm-packages.mjs, src/mcp/vice/THIRD-PARTY-NOTICES.md, THIRD-PARTY-NOTICES.md, CLAUDE.md</files>
  <read_first>
    - `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-RESEARCH.md` sections 1.1, 1.3, 1.4, 1.6, 5.1, 5.2, 5.4, 5.5 and Pitfalls 1, 2, 6, 7 — the pin corroboration, the per-call dispositions, the three upstream runtime-read sites, the attribution header shape, and the two guards that go red
    - `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-PATTERNS.md` sections "ABS-02 — attribution and notices", "The two pinned counts that go red when the 7th skill lands", and "New / modified skills"
    - `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json` — `procedures[2]` fixes the destination as `src/skills/routine-queue-walker` and lists its six upstream tool references with dispositions
    - the upstream file at the pin: `.agent/skills/r2000-analyze-program/SKILL.md` — the source being absorbed; note lines 31-36 (the entropy gate and the destructive-unpack warning), 42, 74, 78, 84, 89-93, 128, 129, 134, 140, 145
    - `src/skills/c64-program-recon/SKILL.md` lines 1-40 — the frontmatter shape, the H1, the do-not-do-the-expensive-thing-first paragraph, the script-path bash block, and the "contacts nothing" caveat this new file must mirror
    - `scripts/check-npm-packages.mjs` — the whole file: its header explains why every assertion reads the packed tarball's own file list, and names the one legitimate repo-path filesystem check; the installer block near line 231 holds the pin to change
    - `scripts/lib/skill-corpus.mjs` — `topLevelSkillDirs()` at line 90 is the relation side; do not write a second walker
    - `scripts/package.sh` lines 100-125 — the already-existing "every skill directory must carry a SKILL.md" enumeration
    - `src/mcp/vice/THIRD-PARTY-NOTICES.md` — the whole file: the cc65 `## Incorporated material` section is the structural analog; the opening no-GPL paragraph and the regenerator2000 not-incorporated section are the two claims to change
    - `THIRD-PARTY-NOTICES.md` (repo root) — the six-line pointer
    - `CLAUDE.md` "## Project Skills" table
    - `src/mcp/vice/r2000-tools.ts` — `R2000_TOOL_DEFINITIONS` and `CURATED_R2000_TOOLS`, so every tool named in the new prose is checked against the real curated list
  </read_first>
  <action>
Re-establish the upstream source, then land the absorbed procedure and every guard/document
it forces, in ONE commit.

(a) Clone or fetch https://github.com/ricardoquesada/regenerator2000 to a path OUTSIDE `/tmp`
(use a scratch directory under the repo's own ignored tree or the user's home), check out the
pinned object `493f840418f1450a342bb220c2fe3d2585dd0525`, and confirm the five
`.agent/skills/r2000-analyze-*/SKILL.md` files hash to the manifest's five `sha256` values with
`sha256sum`. Do not proceed if any digest differs — report the mismatch instead.

(b) Convert the installer skill-count assertion in `scripts/check-npm-packages.mjs` from an
exact-equality literal into a relation: import `topLevelSkillDirs` from
`./lib/skill-corpus.mjs`, compute the set of immediate subdirectories of `src/skills/` that
contain a `SKILL.md`, and assert that the installer tarball's `skills/<name>/SKILL.md` entry
count equals that set's size. Add a separate floor assertion that the computed set size is at
least 6, so a broken directory read cannot make the equality vacuously true on two zeros.
Extend the file's header comment with the reason this is the second legitimate filesystem check
against a repo path (the relation cannot be derived from a tarball listing alone), and name the
project rule it satisfies: assert relations, not counts.

(c) Create `src/skills/routine-queue-walker/SKILL.md`, absorbing
`.agent/skills/r2000-analyze-program/SKILL.md` as this project's own routine-queue-walking
playbook. Frontmatter is exactly `name: routine-queue-walker` and one `description:` carrying a
capability sentence followed by `Use when asked to ...` trigger clauses, written to be
distinct from `c64-program-recon`'s existing description (that pair is the known trigger fight:
both are whole-program orchestration). Immediately after the frontmatter, add the attribution
header as an HTML comment carrying all six fields — source repository, source path, pinned
commit (the full 40-hex form), source sha256, upstream licence with copyright holder, and this
project's elected option — plus an explicit adaptation statement and the named deviations.
Absorb the *sequencing* only: queue construction, the refresh points, the no-premature-halting
rule, the entropy gate (keep the 7.5 threshold and cite `r2000_get_binary_info`'s `entropy`
field, which is curated), and the explicit-address discipline. Carry NONE of the concurrency:
no rolling window, no slot count, no concurrent subagents — the queue is walked one item at a
time through the session seam. Replace all three instructions to read a file inside upstream's
excluded agent-skills directory with this project's own skill names and paths. Route the
cursor-based entry step to explicit address input plus `r2000_read_region`. Omit the
destructive unpack step and point instead at this project's own run-and-capture route
(`c64-ram-capture`) and at the packer finding this phase adds. Name upstream's non-curated
calls WITHOUT the `r2000_` prefix (write, for example, "upstream's `set_immediate_format`
step, which this project does not expose") — the ABS-01 gate is a plain token grep over
`src/skills/`, so a prefixed mention of a non-curated name fails CI even inside an omission
note; the prefixed record belongs in `.planning/` only. Do not add an allowlist to that gate.

(d) Correct `src/mcp/vice/THIRD-PARTY-NOTICES.md`. Add a new section, structured exactly like
the existing cc65 incorporated-material section, headed to identify the regenerator2000
analysis procedures and their dual licence: name the repository, the full pinned commit and its
date, all five source paths with their sha256 digests and byte counts, the total absorbed byte
count, the statement that the text is adapted rather than verbatim, and the licence option this
project elects (MIT — it matches this repository's own LICENSE, and the modification-notice
obligation of the other option is discharged anyway by the adaptation statement in each
per-file header). Then narrow the existing regenerator2000 not-incorporated section so its
blanket no-source-included claim applies only to source code, data tables and program output —
which remains true — and no longer to the repository as a whole. Keep the existing correction
note about the stale single-licence claim. Widen the opening enumeration so incorporated
material under the dual licence is one of the listed categories; leave the no-GPL claim
untouched, it is unaffected.

(e) Update the repo-root `THIRD-PARTY-NOTICES.md` pointer so it also names the incorporated
skill prose and points at the canonical section, rather than describing only the published
MCP package.

(f) Add a `routine-queue-walker` row to `CLAUDE.md`'s "## Project Skills" table, with the same
description text as the frontmatter and the path `src/skills/routine-queue-walker/SKILL.md`.

Do not run `installer/scripts/sync-skills.mjs` expecting to commit its output: `installer/skills/`
is gitignored and untracked, and `npm pack --dry-run` runs it via `prepack`, so the packed
tarball is the surface that gets asserted.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-skill-tool-coverage.mjs && node scripts/check-skill-fork-honesty.mjs && node scripts/check-npm-packages.mjs</automated>
    <human-check>Read `src/skills/routine-queue-walker/SKILL.md`'s `description:` next to `src/skills/c64-program-recon/SKILL.md`'s. Confirm a reader deciding "which of these two do I want" would not hesitate: one is "walk the queue of routines to closure", the other is "work out how an unknown program is structured before disassembling". If they read as the same job, the description needs sharpening.</human-check>
  </verify>
  <acceptance_criteria>
    - `src/skills/routine-queue-walker/SKILL.md` exists, its first line is `---`, and its frontmatter carries exactly the keys `name` and `description`
    - `grep -c '493f840418f1450a342bb220c2fe3d2585dd0525' src/skills/routine-queue-walker/SKILL.md` is at least 1, and the same file contains the sha256 `2d1c91bcc612c00ce71b7def08917b59ca7e495aa61f9075cbb0795e935f6955`
    - `grep -c '\.agent/skills' src/skills/routine-queue-walker/SKILL.md` returns 0
    - `grep -Eci 'rolling window|concurrent subagent|7 subagent|seven subagent' src/skills/routine-queue-walker/SKILL.md` returns 0
    - `grep -c '=== 6' scripts/check-npm-packages.mjs` returns 0, and `grep -c 'topLevelSkillDirs' scripts/check-npm-packages.mjs` is at least 1
    - `node scripts/check-npm-packages.mjs` prints no `expected 6 skills` message and exits 0
    - `node scripts/check-skill-tool-coverage.mjs` exits 0 and its OK line reports at least 7 skill directories scanned
    - `grep -c 'No regenerator2000 source' src/mcp/vice/THIRD-PARTY-NOTICES.md` returns 0
    - `grep -c 'MIT OR Apache-2.0' src/mcp/vice/THIRD-PARTY-NOTICES.md` is at least 1, and the file contains all five upstream source paths
    - `grep -c 'routine-queue-walker' CLAUDE.md` is at least 1
    - The single commit produced by this task shows both `src/skills/routine-queue-walker/SKILL.md` (added) and `src/mcp/vice/THIRD-PARTY-NOTICES.md` (modified) in `git show --stat`
  </acceptance_criteria>
  <done>A seventh skill, absorbed from a verified pinned upstream commit, ships with honest attribution and truthful notices, and every CI guard in the chain is green.</done>
</task>

<task type="auto">
  <name>Task 2: Mechanical guards — attribution headers, absent upstream runtime reads, and a sharpened audit</name>
  <files>src/mcp/vice/skill-attribution.test.ts, src/mcp/vice/r2000-upstream-audit.test.ts, .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json</files>
  <read_first>
    - `src/mcp/vice/skill-consumer-paths.test.ts` lines 1-60 — the frozen-registry discipline this new test copies: presence of the consumer form AND absence of the source form, never a substring both satisfy; content-anchored regions, never line numbers
    - `src/mcp/vice/docs-fork-decision.test.ts` lines 1-60 — the named non-empty phrase set whose own length is asserted, and the section isolator that returns null on a renamed heading
    - `src/mcp/vice/r2000-upstream-audit.test.ts` — the whole file, the extension target
    - `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json` — the shape being extended
    - `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-RESEARCH.md` sections 1.3 (the five per-call justifications, each quoting upstream verbatim with a line number), 1.7 (the two re-sync triggers), 5.4 (the six header fields)
    - `src/mcp/vice/r2000-test-gate.ts` lines 20-60 — the D-11 live-gate pattern: probe once at module load, treat spawn failure as not-available, hard-fail only under an opt-in env var
    - `scripts/lib/skill-corpus.mjs` — `walkSkills()`, for the corpus scan
  </read_first>
  <action>
Extend the manifest with the justification and licence facts it lacks, then pin both with tests.

(a) Extend `upstream-procedure-manifest.json`, additively — no existing key changes meaning:
widen `commit` to the full 40-hex SHA; add `upstream_version`, the upstream `licence` string,
`elected_licence`, a `licence_copyright` line, and per-procedure `bytes`. Add a top-level
`disposition_rationale` object keyed by the five non-curated upstream tool names, each entry
carrying its disposition, a non-empty `justification`, an `upstream_citation` naming the
upstream file and line the justification rests on, and — where the disposition is a
future-surface proposal rather than an omission — the requirement ID that supplies its
criterion. Add a `resync_triggers` array of at least two entries, each with a `trigger`
sentence and a `mechanism` sentence describing how it is checked; keep the existing
`resync_trigger` string as-is so nothing that already reads it breaks.

(b) Extend `r2000-upstream-audit.test.ts`: tighten the commit assertion to require exactly 40
lowercase hex characters, so an abbreviated or branch-shaped ref fails; assert every value in
every procedure's `tools` map is one of the three known dispositions; assert every non-curated
tool name appearing in any procedure's `tools` map has a `disposition_rationale` entry whose
`justification` and `upstream_citation` are both non-empty; assert `resync_triggers` is
non-empty and every entry has both fields; assert `elected_licence` is one of the two options
the upstream dual licence offers. Add a live-gated check in the D-11 style: when the pinned
upstream clone is reachable at a path given by an env var, re-hash the five source paths and
assert each equals the manifest's `sha256`; when it is not reachable, SKIP by default and hard
FAIL under an opt-in env var. Absence of the clone must never read as agreement.

(c) Create `src/mcp/vice/skill-attribution.test.ts` as a frozen registry, not a scan. The
registry maps each absorbed destination file to the upstream source path it was absorbed from;
seed it with the one entry this plan lands and state in the header that later plans add rows.
Assert, per row: the destination file exists; its attribution block is present and carries all
six named fields; the commit string in the block equals the manifest's `commit`; the sha256 in
the block equals the manifest entry for that source path; the adaptation statement is present.
Assert the registry's own length is non-zero, so an emptied registry fails instead of passing
vacuously. Add the absence half over the whole `src/skills/` corpus via `walkSkills()`: no file
instructs the reader to read anything inside upstream's excluded agent-skills directory. Prove
the absence half non-vacuous with a planted violation written into a temporary scratch copy of
one file's text (a string in memory, never a write into `src/skills/`), asserting the predicate
reports it. Every path helper resolves through `repoRoot({ from: HERE })`; import nothing from
`hostpath.ts` or `containerpath.ts` — the r2000-side modules are asserted absent from that
consumer set.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --experimental-strip-types --test skill-attribution.test.ts r2000-upstream-audit.test.ts && node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `node --experimental-strip-types --test skill-attribution.test.ts` exits 0 and reports at least 5 passing tests
    - `node --experimental-strip-types --test r2000-upstream-audit.test.ts` exits 0
    - Replacing the manifest's `commit` with its 7-character abbreviation makes `r2000-upstream-audit.test.ts` exit non-zero (demonstrate, then restore)
    - Emptying `skill-attribution.test.ts`'s registry array makes the suite exit non-zero (demonstrate, then restore)
    - `node -e "const m=require('./.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json'); process.exit(m.commit.length===40 && m.resync_triggers.length>=2 && Object.keys(m.disposition_rationale).length>=5 ? 0 : 1)"` exits 0
    - `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>The manifest carries a cited justification per non-curated upstream call and named re-sync triggers; two tests hold the attribution chain and the audit shut, each proven to fire.</done>
</task>

<task type="auto">
  <name>Task 3: Measure and record the stdio-multiplexing answer that closes D18-16</name>
  <precondition>`regenerator2000 --version` reports `0.9.20` (or `R2000_BIN` points at a binary that does) — the measurement is worthless against a different build, and the roadmap's deferral names measurement, not assumption.</precondition>
  <files>.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/evidence/measure-stdio-multiplexing.mjs, .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-STDIO-MULTIPLEXING-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/18-persistent-session-and-tool-surface/18-STDIN-EOF-EVIDENCE.md` — the section order and metadata block this artifact copies exactly
    - `.planning/phases/18-persistent-session-and-tool-surface/evidence/measure-stdin-eof-driver.mjs` — the driver shape: self-contained, one JSON result line, imports the real shipped `synthesizeProject()` via `pathToFileURL`, honours `R2000_BIN`, bounds its poll
    - `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-RESEARCH.md` section 6 in full — the source citation, the method, the three measured runs, and the conclusion this artifact must reproduce rather than restate
    - `.planning/phases/18-persistent-session-and-tool-surface/18-06-SUMMARY.md` — the coarse FIFO mutex decision and the recorded deferral this closes
    - `src/mcp/vice/r2000-project.ts` — `synthesizeProject()` and `R2000_SYSTEM_C64`, the real writer the driver must use rather than a hand-built project file
    - `.planning/ROADMAP.md` Phase 19 Notes — the 2026-08-24 "deferred, not rejected" note stating what evidence is required
  </read_first>
  <action>
Write and run the measurement, then record it as the artifact of record.

(a) Create `evidence/measure-stdio-multiplexing.mjs`: a self-contained driver, filename
deliberately without a test-suffix so the CI suite-coverage guard is unaffected. It imports the
real `synthesizeProject()` from `src/mcp/vice/r2000-project.ts` by file URL, synthesizes a
scratch project into a `mkdtemp` directory, spawns the binary from `R2000_BIN` (defaulting to
`regenerator2000`) with the stdio-server flag and piped stdio — the same three-pipe shape the
session opener uses — sends an initialize request, waits a fixed delay, then writes ONE burst
containing two requests back to back so both sit in the child's stdin pipe simultaneously: a
slow batched mutation and a trivial binary-info query. Timestamp every response line's arrival
relative to spawn. Print exactly one JSON line carrying the binary path, the version banner,
the inner-call count, the burst timestamp, each response's id and arrival time, the computed
wait the trivial request suffered, and a boolean verdict field. Exit non-zero with an object
carrying an explicit failure reason when the binary cannot be spawned or a response never
arrives, so a future re-run can never record an assumed result. Bound every wait explicitly;
never poll unbounded.

(b) Run it at least twice and capture the raw JSON lines.

(c) Create `19-STDIO-MULTIPLEXING-EVIDENCE.md` following the Phase 18 artifact's section order:
title naming the decision id and the thing measured; a metadata block giving the plan, date,
binary path and version output; Question; Method (numbered, explaining why writing both
requests in one burst is what isolates the child's own behaviour rather than the client's);
Exact command sequence as a shell block; Observed outcome carrying the run table and the raw
JSON of one representative run; Decision; Reproducing this measurement. In Method, cite the
upstream source alongside the measurement — the stdio loop function, the file and line range,
and the fact that the request handler is called synchronously on a mutable borrow — because the
source proof is what makes the measured answer general rather than one machine's timing. In
Decision, state the outcome plainly: the child does not multiplex, a reader-writer upgrade
would buy zero parallelism, the coarse FIFO mutex is an exact model of the child's behaviour
rather than a compromise, and read-only fan-out remains the sanctioned orchestration pattern
whose value is agent reasoning concurrency and explicitly NOT throughput at the session. State
that D18-16 is closed by measurement, not re-deferred, and name what would reopen it (an
upstream change to the stdio loop's dispatch).
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/evidence/measure-stdio-multiplexing.mjs | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s.trim().split('\n').pop());process.exit(r.ok===true?0:1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `node .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/evidence/measure-stdio-multiplexing.mjs` exits 0 and its last stdout line parses as JSON containing a version string, two response arrival timestamps, and a numeric wait for the trivial request
    - Running the same driver with `R2000_BIN=/nonexistent-binary` exits non-zero and its JSON output carries a non-empty failure reason
    - `19-STDIO-MULTIPLEXING-EVIDENCE.md` contains the headings `## Question`, `## Method`, `## Exact command sequence`, `## Observed outcome`, `## Decision`, `## Reproducing this measurement`
    - `19-STDIO-MULTIPLEXING-EVIDENCE.md` contains the literal `D18-16`, the literal `0.9.20`, and at least one fenced block holding raw JSON captured from a real run
    - `grep -Ec 'stdio\.rs' .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-STDIO-MULTIPLEXING-EVIDENCE.md` is at least 1 — the source citation accompanies the measurement
    - The driver filename does not contain `.test.`, and `cd src/mcp/vice && node --experimental-strip-types --test ci-suite-coverage.test.ts` exits 0
  </acceptance_criteria>
  <done>D18-16's deferred measurement is executed against the real binary, recorded with its command sequence and raw output, and answered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream GitHub repository → this repository | 53 KB of third-party prose crosses into `src/skills/` and into two published tarballs |
| absorbed skill prose → Claude Code's trigger matcher | untrusted text that is matched, never imported, required, evaluated or spawned |
| this repository → npm consumers | the attribution claim travels inside the tarball; a stripped or false header is a downstream licence defect |
| `regenerator2000` child process → the measurement driver | the driver reads the child's stdout as untrusted text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | the pinned upstream fetch (Task 1a) | high | mitigate | Check out an immutable 40-hex object, never a branch or tag; verify all five sha256 digests against the manifest before absorbing; the audit test tightens `commit` to exactly 40 lowercase hex so a floating ref cannot be recorded |
| T-19-02 | Repudiation | attribution headers and both notices files | high | mitigate | `skill-attribution.test.ts` asserts header-vs-manifest agreement per absorbed file and asserts its own registry is non-empty; the notices section names paths, digests and byte counts so a downstream copy's provenance is re-checkable |
| T-19-03 | Elevation of Privilege | absorbed prose under `src/skills/` | medium | mitigate | Standing rule preserved: nothing under `src/skills/` is imported, required, evaluated or spawned. `skill-attribution.test.ts` and the planted-violation proof operate on strings read from disk, never on a path handed to `import()` |
| T-19-04 | Information Disclosure | the measurement driver's scratch project | low | accept | The synthesized project holds a fixed NOP payload with no user data, is written under `mkdtemp`, and carries nothing worth disclosing |
| T-19-05 | Denial of Service | the measurement driver (Task 3) | medium | mitigate | Every wait is explicitly bounded and the driver exits non-zero with a reason rather than polling unbounded; it spawns with an argv array and never a shell string |
| T-19-06 | Spoofing | `R2000_BIN` naming an arbitrary executable | low | accept | Local developer-controlled env var on a single-user host; ASVS L1 and the binary is spawned with an argv array, never interpolated into a shell. The version banner is recorded in the artifact so a substituted binary is visible in the evidence |
| T-19-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable this plan — no package-manager install task exists. `src/mcp/vice/package.json`'s runtime dependency set is unchanged and remains asserted by `scripts/check-npm-packages.mjs`; the only new external tool anywhere in this phase is an optional OS-level oracle that is probed, never installed |

Canon-referral breadcrumbs (recorded, deliberately not minted as prohibitions): path traversal
on a caller-supplied project path is canon — covered by reusing `resolveStorePath()` and by
`/gsd-secure-phase`; prototype pollution on `JSON.parse` of a manifest or project file is canon
— covered by `/gsd-secure-phase`.
</threat_model>

<verification>
- `node scripts/check-skill-tool-coverage.mjs`, `node scripts/check-skill-fork-honesty.mjs`, `node scripts/check-npm-packages.mjs` — all exit 0
- `cd src/mcp/vice && node --experimental-strip-types --test skill-attribution.test.ts r2000-upstream-audit.test.ts ci-suite-coverage.test.ts`
- `cd src/mcp/vice && node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json`
- `cd src/mcp/vice && npm test` — the full suite, never the automated subset, which skips the manual-only list and hides CI failures. Record the pre-existing baseline failures Phase 18 already noted (`audit-integrity.test.ts`, `binmon-fixtures.test.ts`, broker tests) before attributing any red to this plan
- `node .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/evidence/measure-stdio-multiplexing.mjs`
</verification>

<success_criteria>
One absorbed procedure exists as a project-owned, attributed, curated-tools-only skill; the
packaging guard asserts a relation instead of a literal; both notices files are true; two
mechanical guards hold the attribution chain shut and are each proven to fire; and D18-16 is
answered by a recorded measurement with its command sequence and raw output.
</success_criteria>

## Artifacts this phase produces

New files:
- `src/skills/routine-queue-walker/SKILL.md` (new, seventh skill directory)
- `installer/THIRD-PARTY-NOTICES.md`
- `src/mcp/vice/skill-attribution.test.ts`
- `src/mcp/vice/r2000-coverage.ts`
- `src/mcp/vice/r2000-coverage.test.ts`
- `src/mcp/vice/skill-description-overlap.test.ts`
- `src/mcp/vice/fixtures/coverage/` — the NC1, NC1b, NC2, NC3, NC4, NC5 synthetic project fixtures
- `scripts/lib/skill-descriptions.mjs`
- `scripts/check-skill-description-overlap.mjs`
- `src/skills/c64-program-recon/scripts/packer-finding.mjs`
- `src/skills/c64-program-recon/scripts/packer-finding.test.mjs`
- `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/evidence/measure-stdio-multiplexing.mjs`
- `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-STDIO-MULTIPLEXING-EVIDENCE.md`
- `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/evidence/coverage-reproducibility/QUESTION.md`, `ANSWER.md`, `ANSWER.sha256`, `RE-DERIVED-ANSWER.md`
- `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-DECISIONS.md`
- `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/COVERAGE.md`

New exported symbols:
- `src/mcp/vice/r2000-coverage.ts`: `COVERAGE_SCHEMA_VERSION`, `CoverageReport`, `CoverageOptions`, `StructuralCensus`, `IndirectDispatchScan`, `LabelRatio`, `CommentVacuity`, `Reproducibility`, `buildCoverageReport()`, `computeStructuralCensus()`, `scanIndirectDispatch()`, `computeLabelRatio()`, `computeCommentVacuity()`, `computeReproducibility()`, `AUTO_NAME_PREFIX_RE`, `BANNED_GENERIC_COMMENTS`, `R2000CoverageInputError`
- `scripts/lib/skill-descriptions.mjs`: `parseSkillFrontmatter()`, `splitTriggerClauses()`, `normaliseClause()`, `clauseTokens()`, `jaccard()`, `descriptionCollisions()`, `DESCRIPTION_OVERLAP_THRESHOLD`, `COLLISION_ALLOWLIST`, `FUNCTION_STOP_WORDS`, `DOMAIN_STOP_WORDS`
- `src/skills/c64-program-recon/scripts/packer-finding.mjs`: `packerFinding()`, `probeUnp64()`, `parseUnp64Stdout()`, `PACKER_VERDICTS`
- `src/mcp/vice/r2000-cli.ts`: `cmdCoverage()`, plus a `coverage` entry in the existing `VERB_OPTIONS`

New CLI surface:
- verb `vice-mcp r2000 coverage <project> [--out FILE] [--force] [--sample N]`
- skill script invocation `node src/skills/c64-program-recon/scripts/packer-finding.mjs <file>`

New env vars: `UNP64`, `UNP64_PATH`, `VICE_REQUIRE_UNP64`

Modified with new symbols:
- `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json`: `commit` widened to the full 40-hex SHA; new keys `upstream_version`, `licence`, `licence_copyright`, `elected_licence`, `resync_triggers[]`, `disposition_rationale{}`, and per-procedure `bytes`
- `scripts/lib/r2000-cli-verbs.mjs`: `R2000_CLI_VERB_FLOOR` raised from 7 to 8
- `src/mcp/vice/r2000-verb-coverage.test.ts`: `REAL_VERBS` gains `coverage`

<output>
Create `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-01-SUMMARY.md` when done
</output>
