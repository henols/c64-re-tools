---
phase: 19-absorbed-procedures-and-the-coverage-instrument
plan: 19
type: execute
wave: 6
gap_closure: true
depends_on: ["19-15", "19-16", "19-17", "19-18", "19-20"]
files_modified:
  - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md
  - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/deferred-items.md
autonomous: true
requirements: [COV-01, COV-02]

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every gate this round added has a recorded planted-violation demonstration with the exact test name observed red, the observed counts and the observed values — a gate nobody has watched fail is not known to be a gate (D-07)"
    - "The round-4 validation record states, per gate, both directions: the payload the gate must decline and the payload it must still accept"
    - "WR-03's disposition is recorded as PROMOTED AND CLOSED by plan 19-20 — with the measured before/after and the authority that promoted it — so the next verification reads a closed route rather than an open human item"
    - "D-08's contingency is recorded WITHOUT being acted on: if this round's verification returns SC4 partial again, the next action is to rescope the criterion, not to run a round 5"
    - "The whole workspace suite is run ONCE in full (never the automated subset) and its result recorded, with the documented `anno-session.ts` call-timeout flake handled by its existing clearing condition rather than by widening a timeout outside this plan's fence"
  artifacts:
    - ".planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md — the consolidated round-4 section"
    - ".planning/phases/19-absorbed-procedures-and-the-coverage-instrument/deferred-items.md — the WR-03 closure note, the D-08 contingency, and this round's flake observation"
  key_links:
    - "each round-4 gate → its planted-violation row → the exact test name and counts observed — the record is traceable to a command someone ran, not to a claim"
    - "`deferred-items.md` WR-03 entry → plan 19-20's measured before/after and `19-DECISIONS.md` Decision 6 — the closure carries its own evidence and its own reversal condition"
  edge_predicates:
    - "empty: a gate with no recorded demonstration is a gap in this record, not an omission to be inferred — the section lists every gate this round added and marks any missing demonstration explicitly"
    - "ordering: the validation record is append-only and chronological; earlier rounds' rows keep their positions"
    - "idempotency: re-reading the record after the commit yields the same content; nothing in it is regenerated from a template"
    - "concurrency: this plan touches only `.planning/` files, so it cannot affect any suite it reports on"
    - "encoding: every recorded count, exit code and test name is transcribed verbatim from the command's own output, never paraphrased or rounded"
  prohibitions:
    - "No requirement checkbox in REQUIREMENTS.md is ticked and no requirement status is changed by this plan — the verifier owns that judgment, and commit `0315d9c` already reverted one premature set"
    - "No ROADMAP success criterion is edited and no REQUIREMENTS entry is rescoped — D-08 is recorded as a contingency, not executed"
    - "19-REVIEW.md is not regenerated and stays byte-unchanged; finding-id continuity is preserved"
    - "`19-VALIDATION.md` and `deferred-items.md` are EXTENDED, never replaced: zero deletions in either"
    - "A red gate is never made green by editing a ledger — if the full suite fails for a reason this round caused, that is a finding to record and fix in source, not to reword"
    - "`anno-session.ts`'s 200 ms call timeout is not widened — that file is outside this plan's fence, and its clearing condition is unchanged"
    - "WR-03 is not re-litigated here: plan 19-20 closed it, and this plan records the outcome rather than re-deciding it"
    - "No source file under `src/` is touched by this plan, so a WR-03 regression found at the gate is recorded and handed back, never patched from here"
    - "No scope-reduction language (`v1`, `for now`, `simplified`, `placeholder`, `future phase`) appears in anything this plan writes"
    - "No source file under `src/` is modified by this plan"
---

<objective>
Close the round with the record it needs to be auditable, and run the phase gate once, honestly.

Purpose: D-07 makes acceptance for this round "every new gate has been demonstrated to fail when its
control is removed", not "SC4 passes". Five plans produced planted-violation demonstrations across
seven gates; this plan consolidates them into one reviewable section, records WR-03's CLOSURE and
D-08's contingency, and runs the whole workspace suite once so the round's own claim of a green tree is
a measurement rather than an inference.

The seven gates are: the tightened push-idiom branch (19-15); the tightened zero-page-vector branch
(19-16); the pairing-consultation pin that turns D-02 from prose into an assertion (19-16); the
corpus-wide proven-set equality over composed arrangements (19-17); the (shape × route) coverage
assertion with its reachability matrix (19-18); the four source-derived route pins (19-18); and the
census decodability predicate that closes WR-03 (19-20). Derive the demonstration COUNT from the five
SUMMARY files rather than typing a number here — a consolidation that disagrees with its sources is
worse than none.

Output: the consolidated round-4 section of `19-VALIDATION.md`, the extended `deferred-items.md`, and
the recorded full-suite result.

**Requirement scope.** `ABS-01`…`ABS-04` and `SURF-03` are closed by plans 19-01…19-05 and are out of
scope for this round. `COV-02` is cited because the dispatch gate is the mechanism both requirements
read. The open requirement is `COV-01`, and this plan does not tick it — the verifier decides that.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-CONTEXT.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VERIFICATION.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-15-SUMMARY.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-16-SUMMARY.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-17-SUMMARY.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-18-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan creates no source symbol. It extends two existing `.planning/` documents:
`19-VALIDATION.md` (a new round-4 consolidated section) and `deferred-items.md` (three new entries:
the WR-03 disposition for this round, the D-08 contingency, and this round's `anno-session.ts`
flake observation).

Every symbol this round created is enumerated in plan `19-15`'s "Artifacts this phase produces"
section and repeated in each later plan's; a source-grounding pass should read that list rather than
re-deriving it here.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The consolidated round-4 validation record and the two dispositions this round did not act on</name>
  <files>.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md, .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/deferred-items.md</files>
  <read_first>
    - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md — every
      existing section, so the new one extends the established row format instead of inventing one
    - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/deferred-items.md — items
      1 through 3 and their clearing conditions, especially item 3's measured flake rate
    - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-CONTEXT.md — D-07's
      three numbered acceptance clauses, D-08, and the `<deferred>` section's WR-03 paragraph
    - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VERIFICATION.md — the
      WR-03 measurement (`reached=64`, `unreached=0`, `linearSweepDecodable=4`) and the `missing`
      list item that asks for a human decision on it
    - The four round-4 SUMMARY files, for the demonstrations each plan actually recorded
  </read_first>
  <action>
Consolidate the round-4 evidence in
`.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md`. Extend the
file; do not restructure or replace what rounds 1 through 3 recorded.

Write one table covering every gate this round added, with a row per gate: the gate, the plan that
landed it, the payload or condition it must DECLINE with the measured report values, the payload it
must still ACCEPT with its measured values, the planted violation applied, the exact test name observed
red, the observed pass/fail counts and exit code, and the confirmation of green after restoration. The
seven gates are the two tightened branches, the pairing-consultation pin, the corpus-wide proven-set
equality, the (shape × route) coverage assertion with its reachability matrix, the four source-derived
route pins, and the census decodability predicate. Read the numbers out of the five SUMMARY files and
the rows those plans already appended rather than re-running anything — a consolidation that disagrees
with its sources is worse than none, so where a number is missing from a SUMMARY, say so in the row
instead of filling it in.

Add a short paragraph stating D-07's three clauses and, for each, which row discharges it: clause 1
the (shape × route) coverage assertion's missing-coverage plant, clause 2 the corpus property's
plants against both pre-fix branches, clause 3 the FP3 fixture's measured report values.

Then record WR-03's CLOSURE and D-08's contingency in `deferred-items.md`, as new numbered items that
extend the file with zero deletions.

WR-03 — the illegal-opcode inflation route on the same number — was PROMOTED into this round by
orchestrator decision and CLOSED by plan 19-20. Record: the measurement it was promoted on (four code
bytes followed by sixty `$02` bytes reported sixty-four bytes reached, zero unreached, and a
contradicting `linearSweepDecodable` of four — the report disagreed with itself sixteen-fold); the
measured after state from 19-20's SUMMARY; that it is a DIFFERENT route from the dispatch gate, being
the census's own classification rather than `provenDispatchTargets()`; that the authority for promoting
it was the orchestrator's decision, which supersedes the deferral recorded in `19-CONTEXT.md`, and that
the context document was deliberately not edited to say so; and a pointer to `19-DECISIONS.md`
Decision 6 for the rejected alternative and the named reversal condition. Mark the item CLOSED with its
closing evidence rather than deleting it — the ledger is append-only and a closed item with its
evidence is what a later reader needs.

Record one residual explicitly rather than letting the closure imply more than it earned: 19-20's fix
brings the census to the linear sweep's existing standard, so a program legitimately executing stable
undocumented opcodes is now under-reported by BOTH figures instead of contradicting itself. That is the
safe direction and it is no longer silent, and its reversal condition is named in Decision 6.

D-08 — the contingency, RECORDED and NOT ACTED ON. If this round's verification returns SC4 partial
again, the conclusion is that the criterion is over-specified for what a heuristic disassembler can
prove, and the next action is to rescope SC4 to advisory-not-gate — not to run a round 5. Record that
this is a ONE-WAY change: it edits a ROADMAP success criterion and a REQUIREMENTS entry that Phase
20's entry conditions read, so it changes a published contract between phases and requires explicit
user confirmation before anyone acts on it. This plan writes the contingency down so a round 5 is not
planned reflexively; it does not edit ROADMAP.md, REQUIREMENTS.md or any success criterion, and it
does not tick a checkbox.
  </action>
  <verify>
    <automated>git -C /home/henrik/dev/henrik/git/c64-re-tools diff --numstat -- .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/deferred-items.md | awk '{ if ($2 != 0) { print "DELETIONS PRESENT: " $0; exit 1 } } END { print "append-only OK" }'</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --numstat` on both `19-VALIDATION.md` and `deferred-items.md` shows insertions and
      ZERO deletions.
    - `19-VALIDATION.md`'s round-4 section carries one row per gate for all seven gates, each naming
      the plan, the declined payload or condition with its measured values, the accepted payload with
      its measured values, the planted violation, the test observed red, the observed counts and exit
      code, and the green after restoration.
    - The section names D-07's three clauses and, for each, the row that discharges it.
    - The total demonstration count stated in the section is DERIVED by counting the rows the five
      SUMMARY files record, not typed from this plan.
    - `deferred-items.md` carries a WR-03 entry marked CLOSED, recording the before and after
      measurements, the authority that promoted it, the note that `19-CONTEXT.md` was deliberately not
      edited, the pointer to Decision 6, and the named under-report residual.
    - `deferred-items.md` carries a D-08 entry recording the contingency, its one-way rating, and the
      requirement for explicit user confirmation before it is acted on.
    - `git diff --name-only` lists neither `.planning/REQUIREMENTS.md`, nor `.planning/ROADMAP.md`,
      nor `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-REVIEW.md`.
    - `git diff --name-only` lists no path under `src/`.
    - `grep -c 'Gaps Found' .planning/REQUIREMENTS.md` is unchanged from before this plan ran.
  </acceptance_criteria>
  <done>
    The round's five gates each have a consolidated, source-traceable planted-violation row; D-07's
    three clauses are each mapped to a row; and WR-03 and D-08 are recorded as decisions rather than
    left as open human items, with no requirement, roadmap or review file touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: The phase gate — one full workspace suite run, recorded honestly</name>
  <files>.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md, .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/deferred-items.md</files>
  <read_first>
    - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/deferred-items.md — item 3,
      the `anno-session.ts` call-timeout flake, its measured rate and its clearing condition
    - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VERIFICATION.md — the
      Behavioural Spot-Checks table, whose command set this gate reproduces
    - src/mcp/vice/package.json — the `test` script and the `test:automated` script, so the full one
      is the one that runs
    - CLAUDE.md — the testing conventions
  </read_first>
  <action>
Run the phase gate once and record exactly what happened.

Run the FULL workspace suite: `cd src/mcp/vice && npm test`. Not `test:automated` — that script skips
a manual-only subset and has previously hidden a real CI failure, so a green from it is not evidence
about the tree. Record the total test count, pass count, fail count, exit code and duration verbatim.

Run the rest of the gate and record each result: `cd src/mcp/vice && npx tsc --noEmit`;
`cd src/mcp/vice && node --test anno-coverage.test.ts`;
`cd src/mcp/vice && node --test anno-coverage-grammar.test.ts`;
`cd src/mcp/vice && node --test docs-review-disposition.test.ts audit-integrity.test.ts
ci-suite-coverage.test.ts comment-phase-pointers.test.ts docs-dangling-refs.test.ts
docs-absorbed-decisions.test.ts docs-deferred-ledger.test.ts`;
`node scripts/check-npm-packages.mjs`; `node scripts/check-skill-tool-coverage.mjs`;
`node scripts/check-skill-description-overlap.mjs`; and
`cd src/mcp/vice && node fixtures/coverage/make-coverage-fixtures.mjs` followed by
`git status --porcelain src/mcp/vice/fixtures/coverage`, which must be empty.

If the ONLY failure in the full suite is the documented `anno-session.ts` call-timeout stub test,
handle it by its existing clearing condition and nothing else: re-run
`cd src/mcp/vice && node --test anno-session.test.ts` standalone, record both results, confirm
`git log` shows this round touched neither `anno-session.ts` nor `anno-session.test.ts`, and append
the new observation to `deferred-items.md` item 3's running tally. Do NOT widen the timeout — that
file is outside this plan's fence and the item's owner is a plan that owns it. Do not treat a green
full-suite run as proof the flake is fixed; the item stays open either way.

If any OTHER test fails, that is a finding this round caused or exposed. Record it in
`deferred-items.md` with the failing test name, the command, the output and the diagnosis, and fix it
in source if it is within this round's fence. Never make a red gate green by editing a ledger.

Append the whole gate result to the round-4 section of `19-VALIDATION.md` as a behavioural
spot-check table with one row per command: the command, the observed result, and the verdict.
  </action>
  <verify>
    <automated>cd src/mcp/vice && npx tsc --noEmit &amp;&amp; node --test anno-coverage.test.ts &amp;&amp; node --test anno-coverage-grammar.test.ts &amp;&amp; node ../../../scripts/check-npm-packages.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && npm test` has been run ONCE in full and its total/pass/fail counts, exit
      code and duration are transcribed verbatim into `19-VALIDATION.md`.
    - `cd src/mcp/vice && npx tsc --noEmit` exits 0.
    - `cd src/mcp/vice && node --test anno-coverage.test.ts` exits 0 with a test count strictly
      greater than the 71 recorded at the round-3 verification.
    - `cd src/mcp/vice && node --test anno-coverage-grammar.test.ts` exits 0.
    - `node scripts/check-npm-packages.mjs`, `node scripts/check-skill-tool-coverage.mjs` and
      `node scripts/check-skill-description-overlap.mjs` each exit 0.
    - `cd src/mcp/vice && node --test docs-review-disposition.test.ts audit-integrity.test.ts
      ci-suite-coverage.test.ts comment-phase-pointers.test.ts docs-dangling-refs.test.ts
      docs-absorbed-decisions.test.ts docs-deferred-ledger.test.ts` exits 0 — the two documentation guards
      over `19-DECISIONS.md` and the deferred ledger are included because plan 19-20 and this plan both
      extend those files.
    - Re-running the fixture generator leaves `git status --porcelain src/mcp/vice/fixtures/coverage`
      empty.
    - If the full suite reported a failure, `19-VALIDATION.md` names it, states whether it is the
      documented `anno-session.ts` flake, and records the standalone re-run result; if it is any
      other failure, `deferred-items.md` carries its diagnosis.
    - `git diff --numstat` on both `.planning/` files shows ZERO deletions, and `git diff
      --name-only` lists no path under `src/` and neither `.planning/REQUIREMENTS.md` nor
      `.planning/ROADMAP.md`.
  </acceptance_criteria>
  <done>
    The full workspace suite has been run once and recorded verbatim alongside every gate command,
    the documented flake is handled by its own clearing condition without touching the file it lives
    in, and no ledger was edited to produce a green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this round's claims → the next verification | The boundary this plan guards: a verifier reads `19-VALIDATION.md` and `deferred-items.md` and decides whether the gates are real. A record that overstates what was run is the only way this plan can cause harm. |

This plan modifies no source file, runs no network call, installs nothing, and touches no
authentication, authorization, session or persistence surface. Spoofing, Repudiation and Elevation of
Privilege have no applicable component and no rows are manufactured for them.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-19-01 | Tampering | `19-VALIDATION.md` / `deferred-items.md` | medium | mitigate | A ledger edited to produce a green verdict is the failure mode this phase has already had once. Mitigated by the append-only assertion (zero deletions, checked mechanically), by requiring every number to be transcribed verbatim from a command's output, and by the explicit prohibition on making a red gate green by rewording. |
| T-19-19-02 | Repudiation | the planted-violation records | medium | mitigate | A demonstration nobody can retrace is indistinguishable from one that never ran. Mitigated by recording the exact test name, counts and exit code per demonstration, sourced from the plan SUMMARYs that ran them. |
| T-19-19-03 | Information disclosure | the recorded command output | low | accept | Test output for this repository carries no secret; the commands are all local, read-only checks. Accepted. |
| T-19-19-SC | Tampering | npm/pip/cargo installs | low | accept | No package install and no dependency change. The package-legitimacy gate does not engage. |
</threat_model>

<verification>
- `cd src/mcp/vice && npm test` — run once, in full, result recorded verbatim.
- `cd src/mcp/vice && npx tsc --noEmit` — exit 0.
- `git diff --numstat` on both edited `.planning/` files — zero deletions.
- `git diff --name-only` — no path under `src/`, and neither `.planning/REQUIREMENTS.md` nor
  `.planning/ROADMAP.md` nor `19-REVIEW.md`.
</verification>

<success_criteria>
- All seven gates this round added have a consolidated, retraceable planted-violation record.
- D-07's three clauses are each mapped to a recorded demonstration.
- WR-03 is recorded as closed with its before/after measurements, its authority and its residual;
  D-08 is recorded as a contingency with its one-way rating and is NOT acted on.
- The full workspace suite has been run once and its result recorded verbatim, with the documented
  flake handled by its existing clearing condition.
- No requirement checkbox, roadmap criterion or review file was touched.
</success_criteria>

<output>
Create `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-19-SUMMARY.md` when
done.
</output>
