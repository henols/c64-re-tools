---
phase: 19-absorbed-procedures-and-the-coverage-instrument
plan: 16
type: execute
wave: 2
gap_closure: true
depends_on: ["19-15"]
files_modified:
  - src/mcp/vice/anno-coverage.ts
  - src/mcp/vice/anno-coverage.test.ts
  - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md
autonomous: true
requirements: [COV-01, COV-02]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The SECOND true-returning site of `hasDispatchContext()` also consults the pairing under test: the indirect jump must name the vector the TWO PAIRED LOADS were stored into, not any consecutive zero-page pair that happens to sit in the same window (COV-01, D-03)"
    - "A payload that builds a vector from its two paired loads at `$fb`/`$fc` and then jumps through an unrelated vector at `$fd`/`$fe` reports zero proven dispatch targets, where before this plan it reported a proven split table"
    - "The minimal-pair twin — the same payload with the indirect jump naming `$fb` instead of `$fd`, one operand byte different — is still PROVEN, so the tightening is a discrimination and not a refusal"
    - "`SPLIT_TABLE`, `SPLIT_TABLE_CLEAN` and `SPLIT_TABLE_INTERPOSED` all still PROVE, and `FP2` still declines"
    - "The branch-B gate is watched FAIL: with the `vectorLow` requirement reverted, the foreign-jump control reds by name (D-07)"
    - "D-02 is ENFORCED and not merely restated: a source-derived pin extracts every `return true` site in `hasDispatchContext()`'s body and asserts each site's own guard names the `pairing` parameter, so a future presence-only branch reds the suite BY NAME instead of satisfying declaration guards its own author wrote (D-02, D-05)"
    - "The pairing-consultation pin is watched FAIL: reverting EITHER branch to its presence-only form reds it by name, and its non-vacuity guard ties the extracted site count to `DISPATCH_CONTEXT_SHAPES.length`"
  artifacts:
    - "src/mcp/vice/anno-coverage.ts — `SplitOrientation.vectorLow`, derived once in `resolveSplitOrientation()` from the pairing's own two consumer stores and read by branch B"
    - "src/mcp/vice/anno-coverage.test.ts — `ZP_VECTOR_FOREIGN_JUMP` and its one-byte-different proven twin, both declared in `GATE_INTERIOR_DECLARATIONS`"
    - "src/mcp/vice/anno-coverage.test.ts — `functionBodyFromSource()`, the shared brace-matching source reader, and the pairing-consultation pin built on it"
    - ".planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md — the branch-B planted-violation row"
  key_links:
    - "`resolveSplitOrientation()` → `SplitOrientation.vectorLow` → `hasDispatchContext()` branch B — the vector address travels with the orientation that derived it, so the two cannot disagree"
    - "class-3 condition (d) `resolveSplitOrientation()` → condition (c) `hasDispatchContext()` — conditions (c) and (d) now read ONE pairing instead of two independent window scans"
    - "`hasDispatchContext()`'s source text → the pairing-consultation pin → the `pairing` parameter — the invariant is checked against the predicate's own body, so it constrains branches nobody has written yet"
  edge_predicates:
    - "adjacency: the two consumer stores must differ by exactly one; equal targets, or targets two or more apart, never form a vector, and the indirect jump must name the LOWER of the two"
    - "encoding: exact numeric equality on a zero-page address in 0..255 between the indirect jump's `operand.value` and the lower consumer store's `operand.value` — never a string or hex-text comparison"
    - "empty: a window with no zero-page store, or with a store but no indirect jump, yields no proven pairing and no throw"
    - "ordering: a jump appearing BEFORE the two loads in the window is still inside the window's collected set, and the predicate's answer does not depend on collection order"
    - "idempotency: `buildCoverageReport()` is `deepEqual` to itself over every committed fixture after this change"
    - "concurrency: the read-only-by-construction source assertion still passes"
  prohibitions:
    - "D-02 may not be left as prose: a plan in this round that only states the invariant in an objective or a must_have has not enforced it. The pin is the enforcement, and a pin nobody has watched fail is not a pin"
    - "The pairing-consultation pin may not be satisfied by a guard that reads `pairing` in a comment: the extracted text is the guard's own condition, and the pin asserts a non-zero site count before comparing"
    - "19-REVIEW.md is not regenerated and stays byte-unchanged"
    - "No requirement checkbox in REQUIREMENTS.md is ticked by this plan"
    - "The both-directions rule holds in the same commit: no tightening lands without the payload it must still ACCEPT beside the payload it must now decline"
    - "A control built from OUTSIDE the predicate it constrains may not be declared as interior"
    - "`COVERAGE_SCHEMA_VERSION` is not bumped and `COVERAGE_REPORT_KEYS` is not changed"
    - "No scope-reduction language (`v1`, `for now`, `simplified`, `placeholder`, `future phase`) appears in any comment or assertion message this plan writes"
    - "No comment this plan writes hands work to a numbered phase; narration of a past plan stays legal"
    - "No new string or template literal in `src/mcp/vice/anno-coverage.ts` names a phase number"
    - "WR-03 is not fixed here and not silently absorbed"
    - "`anno-session.ts`'s 200 ms call timeout is not widened — that file is outside this plan's fence"
    - "No runtime dependency is added to either published package"
---

<objective>
Apply D-02's invariant to the SECOND true-returning site of `hasDispatchContext()`. D-03 is not a
statement about one branch: *every* true-returning site must consult the pairing under test. The
`zeropage-vector-jumped-through` branch was fixed in round 3 to require the CONSUMER — an indirect
jump naming the lower of two consecutive zero-page store targets — but it still collects those
store targets by scanning the whole window, with no link to the two loads actually being paired. A
payload can therefore build its pairing's vector at `$fb`/`$fc`, build a second, unrelated vector at
`$fd`/`$fe`, jump through the second, and be promoted on the strength of a link that has nothing to
do with the tables being reconstructed.

Purpose: this is the same defect class as the one plan 19-15 closed, in the branch round 3 believed
it had fixed — which is the evidence that round 4's scope is the class and not the instance (D-01).
Closing it here means both true-returning sites answer the same question about the same pairing, so a
future branch has one obvious shape to follow.

Output: `vectorLow` derived once in `resolveSplitOrientation()` and read by branch B; the foreign-jump
negative control and its one-operand-byte-different proven twin; the planted-violation record; and the
pin that turns D-02 from a sentence into an assertion.

**Why the pairing-consultation pin lands HERE and not later.** D-02 is outside Claude's discretion,
and until this plan's task 3 the invariant exists only as prose in plan objectives and `must_haves`.
Every other guard this round builds — the source-pinned branch count, the reachability matrix, the
(shape × route) coverage assertion — is satisfiable by the SAME author who writes a loose branch: each
one forces that author to add a declaration, and the author adds it. That is round 3's
self-consistency hole relocated one level up and given a route label. The pin closes it by reading the
predicate's own body and asserting that every true-returning guard names the `pairing` parameter, which
a presence-only branch structurally cannot do. It lands in this plan because this is the commit where
the SECOND true-returning site starts naming `pairing` — the pin cannot be green before that, and
leaving it any later means the round's own remaining plans are unguarded by it.

**Requirement scope.** `ABS-01`…`ABS-04` and `SURF-03` are closed by plans 19-01…19-05 and are out of
scope for this round. `COV-02` is cited because the dispatch gate is the mechanism both requirements
read. The open requirement is `COV-01`.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-CONTEXT.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-15-SUMMARY.md
@CLAUDE.md
@src/mcp/vice/anno-coverage.ts
@src/mcp/vice/anno-coverage.test.ts
</context>

<artifacts_this_phase_produces>
Created by this plan:
- `src/mcp/vice/anno-coverage.ts`: `SplitOrientation.vectorLow` (a new field on the interface
  plan 19-15 introduced)
- `src/mcp/vice/anno-coverage.test.ts`: `ZP_VECTOR_FOREIGN_JUMP`, `ZP_VECTOR_OWN_JUMP`,
  `functionBodyFromSource()` (the shared brace-matching source reader, promoted out of the existing
  branch-count test; plan 19-18's four pins read it too)

Already created earlier in this round (19-15): `SplitOrientation`, `DispatchPairing`,
`hasDispatchContext()`'s `pairing` parameter, `FP3_INTERIOR`, `FP3_IMMEDIATE`,
`PUSH_IDIOM_WINDOW_EDGE`, `PUSH_IDIOM_LINKED`, the `fp3-unlinked-push-idiom` and
`fp3b-immediate-push-idiom` fixture directories and the generator symbols listed in that plan.
Still to come this round: 19-17's `anno-coverage-grammar.test.ts` (the compositional corpus, its
fragment alphabet, its arrangement enumerator and the computed `expectedProvenLink()` oracle);
19-18's `DISPATCH_GATE_ROUTES`, `PROVEN_TARGET_SOURCES`, `GateInteriorDeclaration.route`,
`GATE_ROUTE_REACHABILITY`; and 19-20's `isDecodableAsInstruction()` plus the WR-03 census controls.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The indirect jump must name the pairing's OWN vector</name>
  <files>src/mcp/vice/anno-coverage.ts, src/mcp/vice/anno-coverage.test.ts</files>
  <read_first>
    - src/mcp/vice/anno-coverage.ts — `resolveSplitOrientation()` including its `consumerOf()`
      closure and the `firstZp < secondZp` orientation decision; `hasDispatchContext()` as plan
      19-15 left it, with special attention to the `zpStores` / `indirectJumpPointers` collection
      loop and the nested pair loop beneath it; `interface SplitOrientation` and
      `interface DispatchPairing`
    - src/mcp/vice/anno-coverage.test.ts — `SPLIT_TABLE`, `SPLIT_TABLE_CLEAN`,
      `SPLIT_TABLE_INTERPOSED` and the FP2 report-level test (the payloads that must keep their
      current verdicts), `scanOf()`, `wiredCensus()`, `GATE_INTERIOR_DECLARATIONS`
    - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-CONTEXT.md — D-02, D-03
  </read_first>
  <behavior>
    - Test 1 (RED first): `ZP_VECTOR_FOREIGN_JUMP` — two same-register indexed loads whose nearest
      following stores are `$fb` and `$fc`, two further stores into `$fd` and `$fe`, then an
      indirect jump naming `$fd` — reports `splitTables` empty, `provenDispatchTargets(scan)` empty
      and `tableEntryAddresses` empty, and still reports exactly one advisory candidate so the gate
      is shown to have examined and declined it.
    - Test 2 (the both-directions half): `ZP_VECTOR_OWN_JUMP` — byte-identical except that the
      indirect jump's operand names `$fb` — is PROVEN: `splitTables.length === 1` and
      `provenDispatchTargets(scan)` deep-equals the eight addresses `$0840`…`$0847`.
    - Test 3: the two payloads differ at exactly ONE byte offset, asserted rather than claimed, so
      "the only difference is which vector is jumped through" is a checked property.
    - Test 4: `ZP_VECTOR_OWN_JUMP`'s census reaches strictly more bytes than
      `ZP_VECTOR_FOREIGN_JUMP`'s, and `ZP_VECTOR_FOREIGN_JUMP`'s equals its own prologue length,
      derived from the prologue rather than typed.
    - Test 5: `SPLIT_TABLE`, `SPLIT_TABLE_CLEAN` and `SPLIT_TABLE_INTERPOSED` keep their existing
      proven verdicts and `FP2` keeps its decline — the pre-existing controls are untouched.
  </behavior>
  <action>
Add a third field to `interface SplitOrientation`: `vectorLow: number`, the LOWER of the two
zero-page addresses the pairing's own two loads are consumed by. `resolveSplitOrientation()` already
computes both consumer addresses in `firstZp` and `secondZp` and already requires them to differ by
exactly one, so `vectorLow` is `Math.min(firstZp, secondZp)` and needs no new scan. Set it in both
arms of the existing return. Document it as the address a little-endian 6502 vector's low byte lives
at, which is the address an indirect jump through that vector names — a fact about the
construction, not a convention.

Change `hasDispatchContext()`'s `zeropage-vector-jumped-through` branch to read
`pairing.oriented.vectorLow` instead of scanning `zpStores` for a consecutive pair. The branch
becomes one membership test: the window's collected `indirectJumpPointers` must contain
`pairing.oriented.vectorLow`. Delete the `zpStores` array, its `zeroPageStoreTarget()` collection in
the window loop and the nested double loop that searched it for a pair differing by one — all three
exist only to re-derive, from the whole window and without reference to the pairing, a quantity the
orientation already knows about the two loads under test. Keep `indirectJumpPointers` and its
comment: collecting the jumps and testing membership is still the right shape.

Extend that branch's doc-comment paragraph. The existing CR-04 reasoning — that the construction of
a vector is not evidence about what consumes it — stays, because it is still true and still the
reason the branch demands a consumer. Add the second half this plan supplies: a consumer that
consumes some OTHER vector is not evidence about THIS pairing either, so the address compared is the
one the pairing's own two stores built and no other. Name the shape that defeated the previous form
(a second, unrelated consecutive zero-page pair inside the same window) so the WHY is concrete.

In `src/mcp/vice/anno-coverage.test.ts`, add `ZP_VECTOR_FOREIGN_JUMP`: a 64-byte image at `$0810`
whose prologue is `lda $0830,x` / `sta $fb` / `lda $0838,x` / `sta $fc` / `sta $fd` / `sta $fe` /
`jmp ($00fd)`, over the same table and target layout the FP3 fixtures use — eight ascending lo bytes
at `$0830`, eight `$08` hi bytes at `$0838`, sixteen `nop` bytes at `$0840`. Derive
`ZP_VECTOR_OWN_JUMP` from it by changing the single operand byte of the indirect jump, so the pair's
one-byte difference is true by construction and the assertion is about the SCAN's output. Assert
behaviors 1 through 4, each message naming the pre-fix verdict it replaces: before this change the
foreign-jump payload reported one proven split table, sixteen claimed table-entry bytes and eight
proven targets, on a link built out of a vector its two loads never wrote to.

Declare both payloads in `GATE_INTERIOR_DECLARATIONS`. `ZP_VECTOR_FOREIGN_JUMP` reaches the interior
of `zeropage-vector-jumped-through` with polarity `negative`; `ZP_VECTOR_OWN_JUMP` reaches the same
interior with polarity `positive`. Both are genuinely interior: each carries the pairing precondition
and a consecutive zero-page construction inside the window, which is that shape's declared interior,
and neither can be dismissed as outside-bracketing.
  </action>
  <verify>
    <automated>cd src/mcp/vice && node --test anno-coverage.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && node --test anno-coverage.test.ts` exits 0 with a test count strictly
      greater than the count plan 19-15 left.
    - `cd src/mcp/vice && npx tsc --noEmit` exits 0.
    - `ZP_VECTOR_FOREIGN_JUMP` reports `splitTables` deep-equal `[]`, `provenDispatchTargets(scan)`
      deep-equal `[]`, `tableEntryAddresses` deep-equal `[]`, `splitTableCandidates.length === 1`,
      and `reachedAsInstruction === 17`.
    - `ZP_VECTOR_OWN_JUMP` reports `splitTables.length === 1`, `provenDispatchTargets(scan)`
      deep-equal `[0x0840, 0x0841, 0x0842, 0x0843, 0x0844, 0x0845, 0x0846, 0x0847]`, and
      `reachedAsInstruction` strictly greater than `ZP_VECTOR_FOREIGN_JUMP`'s.
    - The two payloads differ at exactly one byte offset, asserted in the suite.
    - Branch B reads `pairing.oriented.vectorLow` and performs no window-wide search for a consecutive
      store pair — checked by reading the branch, and pinned mechanically by task 3's
      pairing-consultation assertion rather than by a source grep for the deleted identifier, which a
      comment naming the removed scan would falsely trip.
    - `resolveSplitOrientation()` returns `vectorLow` in both arms, and
      `grep -c 'vectorLow' src/mcp/vice/anno-coverage.ts` is at least 4 (the interface field, the
      two returns, and branch B's read).
    - `reportFor("fp2-zeropage-data-pointer")` still returns `dispatch.splitTables` deep-equal `[]`;
      `scanOf(SPLIT_TABLE).splitTables.length === 1`; the `SPLIT_TABLE_CLEAN` /
      `SPLIT_TABLE_INTERPOSED` deep-equality test still passes.
    - `reportFor("fp3-unlinked-push-idiom")` still returns `structural.reachedAsInstruction === 15`
      and an empty `splitTables` — plan 19-15's control is not weakened by this change.
    - `grep -c 'COVERAGE_SCHEMA_VERSION = 2' src/mcp/vice/anno-coverage.ts` prints 1.
  </acceptance_criteria>
  <reversibility rating="costly">
    Both true-returning sites now read one pairing object. Reverting to per-branch window scans means
    re-deriving in two places a quantity that is currently derived once, and re-writing the two
    interior controls declared against it.
  </reversibility>
  <done>
    Both branches of `hasDispatchContext()` consult the pairing under test, a payload that jumps
    through a foreign vector is declined at report level, its one-byte-different twin still proves,
    and every pre-existing dispatch control keeps its verdict.
  </done>
</task>

<task type="auto">
  <name>Task 2: Watch the branch-B tightening FAIL — the planted-violation demonstration</name>
  <files>.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md, src/mcp/vice/anno-coverage.ts</files>
  <read_first>
    - src/mcp/vice/anno-coverage.ts — branch B as task 1 left it
    - src/mcp/vice/anno-coverage.test.ts — the `ZP_VECTOR_FOREIGN_JUMP` assertions
    - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md — the
      round-4 section plan 19-15 opened, whose row format this task follows
    - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-CONTEXT.md — D-07
  </read_first>
  <action>
Demonstrate the new branch-B condition failing, then restore.

In the working tree only, revert branch B to the pre-fix form: re-collect zero-page store targets
across the whole window and accept when any two of them differ by exactly one and the window carries
an indirect jump naming the lower. Run `cd src/mcp/vice && node --test anno-coverage.test.ts` and
record the exact failing test name or names, the exit code, the pass/fail counts, and the values the
foreign-jump assertions print — expected to be one proven split table, sixteen claimed table-entry
bytes and eight proven targets at `$0840`…`$0847`. If what is observed differs, record the difference
as a finding rather than editing the payload to reproduce a remembered number.

Restore the tightened branch, re-run the suite, and confirm green before committing. The
demonstration leaves no trace in committed source.

Append the rows to the round-4 section of
`.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md`: the gate under
demonstration, the planted violation applied, the command, the observed red with test names and
counts, the observed numbers, the restoration and the observed green. Record `ZP_VECTOR_OWN_JUMP` as
the both-directions evidence in the same section, because a tightening whose positive control was
never run is indistinguishable from one that declines everything. Extend the file; delete nothing.
  </action>
  <verify>
    <automated>cd src/mcp/vice && node --test anno-coverage.test.ts &amp;&amp; git -C /home/henrik/dev/henrik/git/c64-re-tools diff --quiet -- src/mcp/vice/anno-coverage.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && node --test anno-coverage.test.ts` exits 0 after restoration.
    - `git diff --numstat` on `19-VALIDATION.md` shows insertions and ZERO deletions.
    - The new `19-VALIDATION.md` rows name the branch-B gate, the exact test observed red, the
      observed pass/fail counts, and the observed foreign-jump numbers under the reverted predicate.
    - A row records `ZP_VECTOR_OWN_JUMP` as executed both-directions evidence.
    - `git diff --name-only` lists neither `.planning/REQUIREMENTS.md` nor
      `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-REVIEW.md`.
  </acceptance_criteria>
  <done>
    The branch-B gate has been observed failing under a planted violation and green after
    restoration, with both observations and the positive-control evidence recorded as additions to
    `19-VALIDATION.md`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Enforce D-02 mechanically — the pairing-consultation pin, and watch it fail</name>
  <files>src/mcp/vice/anno-coverage.test.ts, src/mcp/vice/anno-coverage.ts, .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md</files>
  <read_first>
    - src/mcp/vice/anno-coverage.test.ts — the existing source-derived test "the declared shape count
      equals the number of true-returning sites in hasDispatchContext()'s own source" IN FULL: its
      brace-matching body extraction, its `signature` lookup, its three non-vacuity guards and its
      failure message. This task promotes that extraction into a named helper and adds one assertion
      built on it
    - src/mcp/vice/anno-coverage.ts — `hasDispatchContext()`'s body exactly as tasks 1 and 2 of this
      plan and plan 19-15 left it, with attention to the TEXTUAL FORM of each true-returning guard
    - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-CONTEXT.md — D-02, D-05,
      D-07
    - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md — the
      round-4 section's row format
  </read_first>
  <action>
Promote the existing branch-count test's body extraction into a named helper
`functionBodyFromSource(source, signature)` that finds the signature, brace-matches its body, and
throws a message naming the signature when the function is renamed, missing, or brace-unbalanced.
Carry over every non-vacuity guard the existing test has — a silently empty extraction would make
this pin and the four that plan 19-18 adds all pass vacuously. Re-point the existing branch-count
assertion at the helper without changing what it asserts.

Add the PAIRING-CONSULTATION PIN. Extract `hasDispatchContext()`'s body with the helper, locate every
`return true` site in it, and for each site extract its GUARD CHAIN: the whole enclosing statement at
depth 1 of the function body — the outermost `if` or `for` that contains this return — from that
statement's first character up to the `return true`. Assert that every such guard chain contains the
identifier `pairing`. A branch that returns true on the mere presence of a shape within the window
structurally cannot satisfy this, because presence is a property of the window and `pairing` is the
only handle on the two loads under test.

The depth-1 statement is the right span and a narrower one is WRONG, which is worth stating because
the obvious implementation gets it wrong. As plan 19-15 landed branch A, the `return true` sits inside
a `for` loop scanning for the `rts`, and its immediate guard reads only the opcode — the `pairing`
reference is in the enclosing `if` that tests the two pushes. An extraction that stopped at the
nearest preceding statement boundary would therefore red on correct code, and whoever hit that red
would be tempted to weaken the pin rather than widen the window. Take the depth-1 statement, and use
the same brace matching `functionBodyFromSource()` already performs so there is one notion of depth in
this file.

Guard it against vacuity in two directions, both required: the extracted site count must be non-zero,
and it must equal `DISPATCH_CONTEXT_SHAPES.length`. The first stops a rename or a failed extraction
from passing; the second ties the pin to the same declared shape set the branch-count assertion
governs, so a site that escapes extraction cannot hide behind a passing pin.

Write the failure message so it teaches: name the offending guard text verbatim, state the invariant
(a branch may return true only on a proven data-flow link from the two reconstructed table bases to
the dispatch mechanism, never on the mere presence of a shape within the window), and say that adding
a shape id, a reachability row and a negative control does not discharge it — those are all written
by the same author as the branch, which is why this pin reads the predicate instead of the
declarations.

This pin constrains the SHAPE of the code, so state that contract beside it: every true-returning
branch's depth-1 guard chain must name `pairing`, either by reading `pairing.` directly or by passing
`pairing` to a predicate call inside a condition on that chain. Nesting inside the chain is fine and
expected. What defeats the extraction is a branch that computes its verdict into a local in an EARLIER
SIBLING statement and then tests the bare local, because the guard chain then names only the local;
that is the one convention this pin imposes, and the comment must say so plainly so a future author
conforms rather than deleting the pin.

Then watch it fail, twice over — once per branch, because a pin demonstrated against only one of the
two sites is not known to constrain the other. In the working tree only, revert the
`stack-return-push-idiom` guard to its presence-only form (two `pha` bytes and an `rts` anywhere in
the window) and run `cd src/mcp/vice && node --test anno-coverage.test.ts`; record the exact failing
test name, the counts, the exit code and the guard text the message printed. Restore, confirm green.
Repeat with the `zeropage-vector-jumped-through` guard reverted to its window-wide store scan. Restore,
confirm green.

If either revert leaves the pin GREEN, that is the finding: the extraction is not reading the guard it
claims to read. Record the observation and fix the extraction rather than accepting a demonstration
that showed nothing.

Append both demonstrations to the round-4 section of `19-VALIDATION.md`, with the fields the earlier
rows use. Extend the file; delete nothing.
  </action>
  <verify>
    <automated>cd src/mcp/vice && node --test anno-coverage.test.ts &amp;&amp; git -C /home/henrik/dev/henrik/git/c64-re-tools diff --quiet -- src/mcp/vice/anno-coverage.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && node --test anno-coverage.test.ts` exits 0 after both restorations, and
      `git diff --quiet -- src/mcp/vice/anno-coverage.ts` succeeds so neither revert survived.
    - The pin extracts exactly 2 `return true` sites from `hasDispatchContext()`'s body, asserts the
      count is non-zero, asserts it equals `DISPATCH_CONTEXT_SHAPES.length`, and asserts each site's
      depth-1 GUARD CHAIN contains `pairing`.
    - The pin passes against branch A as plan 19-15 committed it, where the `return true` is nested
      inside a loop whose immediate condition tests only an opcode and the `pairing` reference lives in
      the enclosing `if` — confirming the extraction takes the depth-1 statement and not the nearest
      preceding boundary.
    - Reverting the `stack-return-push-idiom` guard to its presence-only form makes the pin fail with
      a message quoting that guard's text; the observed test name and counts are recorded.
    - Reverting the `zeropage-vector-jumped-through` guard to its window-wide store scan makes the pin
      fail with a message quoting that guard's text; the observed test name and counts are recorded.
    - `functionBodyFromSource()` throws a message naming the signature when asked for a function that
      does not exist — asserted with a deliberately absent signature, so the helper's own failure mode
      is proved rather than assumed.
    - The existing branch-count assertion still passes and still asserts the same equality after being
      re-pointed at the helper.
    - `git diff --numstat` on `19-VALIDATION.md` shows insertions and ZERO deletions.
    - `git diff --name-only` lists neither `.planning/REQUIREMENTS.md` nor
      `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-REVIEW.md`.
  </acceptance_criteria>
  <reversibility rating="costly">
    The pin imposes a shape contract on every future true-returning guard in `hasDispatchContext()`.
    Removing it later is one deletion, but every branch written against the contract in the meantime
    reads as if the contract still holds, which is the failure mode this round exists to stop.
  </reversibility>
  <done>
    D-02 is an assertion over `hasDispatchContext()`'s own source text rather than a sentence in a
    plan, its non-vacuity is tied to the declared shape count, and it has been observed failing
    against the presence-only form of BOTH true-returning sites.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.regen2000proj` file → `buildCoverageReport()` | An operator-supplied JSON document carrying a base64 payload; the only untrusted input in scope, parsed by code this plan does not change. |
| coverage JSON → Phase-20 consumer | A downstream reader trusts `dispatch.splitTables` and `provenDispatchTargets()`. The numbers' honesty is the only control. |

No network surface, no authentication, no authorization, no session, no persistence. Spoofing,
Repudiation and Elevation of Privilege have no applicable component and no rows are manufactured for
them.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-16-01 | Tampering | `hasDispatchContext()` branch B → `provenDispatchTargets()` | high | mitigate | A payload builds its pairing's vector and then jumps through a second, unrelated vector in the same window, manufacturing a proven split table and eight fabricated entry points. Mitigated by comparing the jump's operand against `vectorLow` — the address the pairing's own two consumer stores built — plus the foreign-jump interior control and the planted-violation demonstration. |
| T-19-16-02 | Information disclosure | `anno-coverage.ts` as a whole | low | accept | No filesystem write and no live-session import; the committed source-level assertion proves it and must still pass. Accepted as mitigated by construction. |
| T-19-16-03 | Denial of service | the class-3 reconstruction walk | low | accept | Bounded by `MAX_TABLE_ENTRIES` and the one derived `effectiveEnd`; this plan removes a nested loop rather than adding one. |
| T-19-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package install, no dependency change. The package-legitimacy gate does not engage. |
</threat_model>

<verification>
- `cd src/mcp/vice && node --test anno-coverage.test.ts` — exit 0.
- `cd src/mcp/vice && npx tsc --noEmit` — exit 0.
- `cd src/mcp/vice && node --test comment-phase-pointers.test.ts docs-dangling-refs.test.ts` — exit 0.
- `git status --porcelain src/mcp/vice/fixtures/coverage` — empty; this plan commits no fixture change.
- The pairing-consultation pin observed RED against the presence-only form of each of the two
  true-returning sites, and green after each restoration.
</verification>

<success_criteria>
- Both true-returning sites of `hasDispatchContext()` consult the pairing under test, and a
  source-derived pin ASSERTS that they do rather than a plan stating it.
- The pin has been watched fail against the presence-only form of each site.
- A jump through a foreign vector no longer proves a pairing; the one-byte-different twin still does.
- Every pre-existing dispatch control, including plan 19-15's FP3 pair, keeps its verdict.
- The branch-B gate has been watched fail, recorded in `19-VALIDATION.md`.
</success_criteria>

<output>
Create `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-16-SUMMARY.md` when
done.
</output>
