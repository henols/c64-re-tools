---
phase: 19-absorbed-procedures-and-the-coverage-instrument
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified: ["src/mcp/vice/r2000-coverage.ts", "src/mcp/vice/r2000-coverage.test.ts", "src/mcp/vice/r2000-cli.ts"]
autonomous: false
requirements: [COV-01, COV-02, SURF-03]
---
<objective>Build a project-owned coverage report with independent measures and controls that reject mechanically manufactured evidence.</objective>
<tasks>
<task type="tdd"><name>Implement the separate coverage measures</name><files>src/mcp/vice/r2000-coverage.ts, src/mcp/vice/r2000-coverage.test.ts, src/mcp/vice/r2000-cli.ts</files><action>Define a typed report and CLI/MCP-facing read route. Compute structural completeness from raw byte/instruction traversal independently of upstream block typing, Auto-versus-User label ratio from provenance, and sampled reproducibility from independently re-derived labels/comments. Emit these fields separately; do not calculate or display an aggregate percentage.</action><verify>node --test r2000-coverage.test.ts</verify><acceptance_criteria>Tests prove each metric independently and a report has exactly the three named top-level measures.</acceptance_criteria></task>
<task type="tdd"><name>Prove the report rejects vacuous documentation</name><files>src/mcp/vice/r2000-coverage.test.ts</files><action>Add fixtures and planted controls for all-auto labels, generic `handles data` comments, and a multi-caller label lacking cross-reference-backed documentation. Each must produce a non-clean result naming the reason; a genuine documented sample must pass. Include indexed dispatch-table traversal coverage rather than relying only on `follow_indirect_jumps`.</action><verify>node --test r2000-coverage.test.ts</verify><acceptance_criteria>The report cannot be gamed by automated labelling or generic prose, and multi-caller evidence is checked.</acceptance_criteria></task>
</tasks>
<threat_model>Coverage can be falsified by trusting the store or collapsing weak measures into one score. Use independent byte traversal, explicit provenance, and negative controls.</threat_model>
<verification>Focused coverage tests plus a live run against a previously unseen fixture.</verification>
<success_criteria>COV-01/COV-02 produce useful separate measures with demonstrated non-vacuity.</success_criteria>

