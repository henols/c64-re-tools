---
phase: 19-absorbed-procedures-and-the-coverage-instrument
plan: 14
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - .planning/todos/completed/2026-08-25-phase-19-review-cr-02-disposition.md
autonomous: true
requirements: [COV-02, COV-01]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`CR-02` — the one finding id `docs-review-disposition.test.ts` currently reports as undispositioned anywhere — carries a real disposition in a source the guard reads, recording what 19-08 narrowed, what remained after it, and where the residual is now tracked"
    - "The disposition is durable and independent of any document a later run rewrites: it lives in `.planning/todos/completed/`, the guard's source 3, and satisfies the guard's own phase-scoping predicate by naming both `19-REVIEW.md` and Phase 19 explicitly"
    - "`docs-review-disposition.test.ts` (AUDIT-01) passes standalone at the end of this plan, and `audit-integrity.test.ts` D-12-02 — which cascades from it — passes standalone too, so the two failures the re-verification recorded are gone before any wave-2 plan asserts a green tree"
    - "Finding-id continuity is preserved absolutely: `19-REVIEW.md` is byte-unchanged by this plan — not regenerated, not renumbered, not annotated"
    - "No new PENDING todo is created, so `docs-deferred-ledger.test.ts`'s two-directional invariant between `.planning/todos/pending/` and `STATE.md`'s `## Deferred Items` section is untouched and `STATE.md` needs no row"
  prohibitions:
    - requirement_id: COV-02
      category: values
      statement: "MUST NOT make a red guard green by rewriting the ledger it reads — finding ids are never renumbered and `19-REVIEW.md` is never regenerated; a disposition is a record of what was done, not a phrasing that satisfies a parser"
    - requirement_id: COV-02
      category: transparency
      statement: "MUST NOT record a finding as fixed by work that has not landed — a disposition written in wave 1 may cite only commits that already exist, and must name what is still outstanding as outstanding"
  artifacts:
    - path: ".planning/todos/completed/2026-08-25-phase-19-review-cr-02-disposition.md"
      provides: "CR-02's disposition record — guard source 3, phase-scoped, citing only commits that already exist"
  key_links:
    - from: "src/mcp/vice/docs-review-disposition.test.ts"
      to: ".planning/todos/completed/2026-08-25-phase-19-review-cr-02-disposition.md"
      via: "disposition source 3 — a todo naming the id AND identifiably about that phase, per todoMentionsPhase()"
      pattern: "19-REVIEW.md"
    - from: "src/mcp/vice/audit-integrity.test.ts"
      to: "src/mcp/vice/docs-review-disposition.test.ts"
      via: "D-12-02 cascades: no milestone audit may declare a gated status while any docs guard is red"
      pattern: "D-12-02"
---

<objective>
Take the workspace test suite from RED to GREEN in wave 1, by discharging the one finding id that
is genuinely undispositioned in every source the AUDIT-01 guard accepts: `CR-02`.

Purpose: three plans in this gap-closure run (19-10, 19-11, 19-12) gate task completion on a fully
green full suite. The tree is red for a cause none of them fixes and none of their SUMMARYs names.
Without this plan, each of those gates fails for a reason outside its own scope, and the executor
either halts the wave or reaches for the planning documents to make it green — which is the exact
tampering `19-13`'s `T-19G-13-01` forbids. Discharging CR-02 in wave 1 makes the later gates honest
rather than annotated.

Output: one dated disposition record under `.planning/todos/completed/`, and an observed green run
of both guards the re-verification named.
</objective>

<gap_closure_context>
**Source:** `19-VERIFICATION.md` gap 2 — *"The phase's own artifacts leave the workspace test suite
RED"*, `status: failed`. Confirmed at HEAD `80c544b`: `# tests 2564, # pass 2517, # fail 2`, exit 1.

**Measured at plan time, the red is narrower than the verification recorded — and it is CR-02.**
Running the guard standalone today reports exactly one undispositioned finding:

    19-REVIEW.md (19-absorbed-procedures-and-the-coverage-instrument): CR-02

The other five ids the verification named (CR-04, WR-13, WR-14, WR-15, IN-05) became
"dispositioned" the moment `19-VERIFICATION.md` was committed, because the guard's **disposition
source 2** is the phase's own `*-VERIFICATION.md` and that file's `missing:` prose quotes each id by
name. That is a disposition by accident — it records nothing about what was done and it evaporates
the next time a re-verification rewrites the file. **Replacing those five accidental dispositions
with durable ones is plan 19-13's job and is NOT discharged here.** This plan does one thing:
closes the genuine hole.

**CR-02's actual disposition, established from the artifacts and citable today** — do not restate
this from memory, re-derive it from the sources named in `<read_first>`:

- `19-REVIEW.md`'s own in-place blockquote records CR-02 as RESOLVED (NARROWED) by plan 19-08,
  commit `1706d8b`: the class-3 scan is gated, ungated pairings move to the advisory
  `splitTableCandidates` sibling, `COVERAGE_SCHEMA_VERSION` was bumped to 2, and
  `provenDispatchTargets()` became the single seam every `extraSeeds:` assignment reads.
- The same blockquote records that the defect class is **narrowed, not closed**, and that the
  residual was re-filed as the new id `CR-04` *rather than by reopening CR-02*.
- `CR-04` is being closed by plan **19-10** in this same run, and 19-10's SUMMARY is where that
  disposition lands.

That is a complete and truthful disposition for CR-02 at wave-1 time, and it depends on nothing
this run has not already committed.

**Why a completed todo rather than a SUMMARY or the review-fix ledger.** The guard accepts five
sources. Source 1 (a plan SUMMARY) would work but arrives only at plan close, after this plan's own
verification step wants to observe the guard green. Source 5 (`19-REVIEW-FIX.md`) is plan 19-13's
deliverable and writing it here would put two plans on one file. Source 3 — a
`.planning/todos/{pending,completed}/*.md` naming the id and identifiably about the phase — is the
established convention in this repository for exactly this act; the precedent is
`.planning/todos/completed/2026-08-21-phase-10-and-11-review-residual-dispositions.md`, which
disposes eleven ids the v0.3.0 audit found silent. **Completed**, not pending: a new PENDING todo
would require a matching row in `STATE.md`'s `## Deferred Items` section or
`docs-deferred-ledger.test.ts` turns red, and CR-02 is not deferred — it is disposed.

**Scope fence.** Do not modify, supersede or rewrite plans 19-01 … 19-09 or their SUMMARYs. Do not
touch `19-REVIEW.md`. Do not touch `19-REVIEW-FIX.md` (19-13 creates it). Do not touch source code —
this plan is `.planning/`-only and has zero file overlap with its wave-1 sibling 19-10, which is why
the two run in parallel.
</gap_closure_context>

<execution_notes>
**Worktree isolation must be OFF for this plan.** The single file in `files_modified` is under
`.planning/`. Worktree mode strips `.planning/` files from commits, so a plan whose entire
deliverable is a planning document silently cannot deliver under worktree isolation. Force isolation
to `none` before dispatching this plan, and do not re-query to "confirm" it — re-force it.

**This plan does NOT run the full suite as its gate, deliberately.** Its wave-1 sibling 19-10 is
editing `anno-coverage.ts` and its test file concurrently, so a full-suite run here would observe a
tree mid-flight and report a failure that belongs to neither plan. The gate here is the two named
guards run standalone — both read only `.planning/` documents and neither reads any file 19-10
touches, so their result is independent of 19-10's progress. The unconditional full-suite gate
belongs to wave 2 onward, which is exactly what this plan makes honest.

**Cite only commits that already exist.** `git cat-file -t <hash>` must return `commit` for every
hash written into the record. A disposition that cites a commit from a plan still in flight is a
prediction, not a record.

**`[skip release]` in every commit subject.** The hold's lifting condition — verbatim from 19-07's
checkpoint — is *"Phase 19 re-verification returns no gaps."* This plan does not perform that
re-verification and cannot lift the hold.
</execution_notes>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: File CR-02's disposition record in the source the guard actually reads</name>
  <reversibility rating="reversible">A new planning document under `.planning/todos/completed/`. Nothing shipped, no code touched; deleting the file restores the prior state, including the prior red guard.</reversibility>
  <files>.planning/todos/completed/2026-08-25-phase-19-review-cr-02-disposition.md</files>
  <read_first>
    - `src/mcp/vice/docs-review-disposition.test.ts` lines 1-75 — the header enumerating the five accepted disposition sources verbatim, and why source 3 exists. Read this before choosing where anything lands.
    - `src/mcp/vice/docs-review-disposition.test.ts` `todoMentionsPhase()` (around line 190) — the exact predicate a todo must satisfy to count for a phase: it names the phase's own REVIEW.md filename directly, OR it mentions the phase as a word-bounded `phase N` / `phase-N` token. Satisfy BOTH, not one, so a future edit to either half cannot silently un-scope the record.
    - `src/mcp/vice/docs-review-disposition.test.ts` `isDispositioned()` (around line 255) — the word-bounded match on the id. The id must appear as its own token, not inside a longer string.
    - `src/mcp/vice/docs-review-disposition.test.ts` `dispositionTextForPhase()` (around line 265) — which directories and filename patterns are concatenated, confirming that `.planning/todos/completed/*.md` is read and that `19-REVIEW.md` itself is not a source.
    - `src/mcp/vice/docs-deferred-ledger.test.ts` lines 1-60 and its two direction tests — direction A requires every PENDING todo to have a row in `STATE.md`'s `## Deferred Items` section; direction B requires that no COMPLETED todo is still listed there. Confirm for yourself that a new completed todo, absent from `STATE.md`, satisfies both before writing the file.
    - `.planning/todos/completed/2026-08-21-phase-10-and-11-review-residual-dispositions.md` — the established shape for this exact act: frontmatter with `created`, `source`, `severity`, a resolving-phase field, then a per-id disposition section. Follow it rather than inventing a shape.
    - `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-REVIEW.md` — CR-02's heading and its in-place RESOLVED (NARROWED) blockquote in full, which is the disposition text to carry across and re-verify, plus the CR-04 heading that records where the residual went.
    - `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-08-SUMMARY.md` — for what 19-08 actually shipped and the commit hash to cite.
  </read_first>
  <action>
Establish the facts first, then write. Do not transcribe the summary in this plan's
`<gap_closure_context>` — re-derive each claim from the file that owns it, because a disposition
assembled from a plan's prose rather than from the artifacts is the defect class AUDIT-01 exists to
catch.

Run the guard standalone before writing anything, and record its exact failure text — the id it
names and the phase directory it names. That measured line is the record's own starting evidence and
belongs in the SUMMARY.

Create `.planning/todos/completed/2026-08-25-phase-19-review-cr-02-disposition.md`. Give it
frontmatter following the precedent file's shape: the creation date, a `source` field naming
`19-REVIEW.md` and the phase, a severity, and the phase this resolves. The filename and the `source`
field between them must satisfy the guard's phase predicate; additionally write the literal string
`19-REVIEW.md` and a word-bounded `Phase 19` reference into the body, so both halves of
`todoMentionsPhase()` hold independently.

Write the disposition itself as prose with evidence, not as a table row. It must state, each with
the artifact it was checked against:

- what CR-02's finding was — the split lo/hi table scan manufacturing census coverage from ordinary
  data;
- what plan 19-08 shipped against it, at commit `1706d8b`, confirmed present with `git cat-file -t`
  and `git log` before it is written down: the gated class-3 scan, the advisory
  `splitTableCandidates` sibling, the schema version bump to 2, and `provenDispatchTargets()` as the
  single seam;
- that the defect class was **narrowed, not closed**, and that the residual was deliberately re-filed
  as the new id `CR-04` rather than by reopening CR-02 — quoting the review's own sentence saying so,
  so the reader can see the decision was recorded at the time and not invented now;
- where CR-04 stands **as of this record's date**: open, and being closed by plan 19-10 in this
  gap-closure run. Name it as outstanding. Do NOT write that CR-04 is fixed — at wave-1 time it is
  not, and a record that predicts a commit is not a record.
- one sentence stating why this file exists at all: the id had no disposition in any of the guard's
  five accepted sources, which is AUDIT-01's own defect applied to this phase.

Close with a short note that `19-REVIEW.md` was not modified in the course of writing this, and that
the id continuity the guard depends on is intact.

Do not mention any id other than `CR-02` and `CR-04` in this file. Widening it to the other four
would duplicate 19-13's ledger and create two competing records of the same dispositions — the
condition that made the original AUDIT-01 finding hard to resolve.
  </action>
  <verify>
    <automated>cd src/mcp/vice &amp;&amp; node --test docs-review-disposition.test.ts</automated>
    <automated>test -z "$(git diff --name-only -- .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-REVIEW.md)" &amp;&amp; echo REVIEW-UNTOUCHED</automated>
  </verify>
  <acceptance_criteria>
    - The guard's pre-write failure text is recorded in the SUMMARY verbatim, naming `CR-02` and the phase directory, as observed by running `node --test docs-review-disposition.test.ts` before the file was created.
    - `cd src/mcp/vice && node --test docs-review-disposition.test.ts` exits 0 with 0 failures after the file is created. Record the observed `# tests` / `# pass` / `# fail` line.
    - The record satisfies BOTH halves of `todoMentionsPhase()` independently: it contains the literal `19-REVIEW.md` and a word-bounded Phase 19 token. State how each was checked.
    - Every commit hash cited in the record resolves: `git cat-file -t <hash>` returns `commit`. List each hash and its result in the SUMMARY.
    - The record names `CR-04` as OUTSTANDING at this record's date, and nowhere describes it as fixed.
    - `git diff --name-only -- .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-REVIEW.md` is empty across this plan's whole commit range.
    - `git status --porcelain .planning/todos/pending/` is empty — no pending todo was created, so `STATE.md` needs no row.
    - `git status --porcelain .planning/STATE.md` is empty — this plan does not edit `STATE.md`.
    - No file under `src/` was modified: `git diff --name-only -- src/` is empty for this plan's commits.
  </acceptance_criteria>
  <done>`CR-02` has a real, evidence-bearing disposition in the guard's source 3; the AUDIT-01 guard passes standalone; and `19-REVIEW.md` is byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Observe the cascade resolved, so wave 2 can assert a green tree honestly</name>
  <reversibility rating="reversible">A verification and recording task. It changes no file.</reversibility>
  <files>(no files modified — verification only; results are recorded in this plan's SUMMARY)</files>
  <read_first>
    - `src/mcp/vice/audit-integrity.test.ts` around line 225 — the D-12-02 assertion ("no milestone audit declares a gated status while any docs guard is red") and its guard runner, so you can read its failure message correctly if it is still red and can tell a genuine red from the known cascade.
    - `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VERIFICATION.md`'s gap-2 `reason` block — it records that D-12-02's message names SEVEN red docs guards while six of them pass standalone. Treat that as a claim to re-test, not a fact to repeat.
    - `src/mcp/vice/package.json` `scripts` — the definitions of `test` and `test:automated`, and which files `MANUAL_ONLY_TESTS` excludes, so you can state why the subset is never the evidence.
  </read_first>
  <action>
Run each of the seven guards D-12-02's message names, standalone, and record the exit code of each:
`docs-review-disposition`, `docs-core-value-decision`, `docs-dangling-refs`, `docs-deferred-ledger`,
`docs-fork-decision`, `docs-linerefs`, `docs-anno-decisions`. Establish by measurement how many are
genuinely red now — do not carry the verification's count forward. If any of the six that were
previously green has turned red, that is new information: halt and report it rather than absorbing
it into this plan.

Then run `node --test audit-integrity.test.ts` standalone and record its exit code and, if it fails,
its message. D-12-02 cascades from the disposition guard, so with Task 1 landed it is expected to
pass; expected is not observed, so observe it.

Do NOT run the full suite here. This plan's wave-1 sibling 19-10 is editing `anno-coverage.ts` and
`anno-coverage.test.ts` concurrently, so a full-suite run would report a failure belonging to
neither plan. The two guards above read only `.planning/` documents and read no file 19-10 touches,
so their results are independent of 19-10's progress. State that reasoning in the SUMMARY, so the
absence of a full-suite gate here reads as a deliberate scoping decision rather than an omission.

Record, in the SUMMARY, the one sentence wave 2 depends on: with CR-02 discharged, the two failures
`19-VERIFICATION.md` recorded are gone, and a downstream plan asserting a green full suite is now
asserting something its own work can be responsible for.
  </action>
  <verify>
    <automated>cd src/mcp/vice &amp;&amp; node --test docs-review-disposition.test.ts &amp;&amp; node --test audit-integrity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All seven guards named in D-12-02's message were run standalone and each exit code is recorded by guard name in the SUMMARY.
    - `node --test docs-review-disposition.test.ts` exits 0 standalone.
    - `node --test audit-integrity.test.ts` exits 0 standalone, and its D-12-02 subtest is observed passing by name.
    - The SUMMARY states explicitly that the full suite was NOT run in this plan and why — concurrent wave-1 sibling 19-10 edits source files this plan does not read — and that the unconditional full-suite gate belongs to wave 2 onward.
    - The SUMMARY names finding id `CR-02` and the file that disposes it, so the disposition also exists in the guard's source 1 and does not depend on a single artifact.
    - `git status --porcelain src/` is empty — this task modifies nothing.
  </acceptance_criteria>
  <done>Both guards the re-verification named are observed green standalone, the seven-guard cascade is re-measured rather than assumed, and the scoping decision behind the absent full-suite gate is on the record.</done>
</task>

</tasks>

<threat_model>
ASVS level 1. Blocking threshold: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a red guard → the documents it reads | The guard reads planning documents that the same agent can write. The cheapest way to make it green is also the one that destroys the record, and this plan exists precisely to take the honest route instead. |
| a wave-1 disposition → work still in flight | A record written in wave 1 can cite only what has already landed. Citing a commit from a plan still executing turns a record into a prediction. |
| planning documents → future verification and milestone audit | This record is read by re-verification and by the next milestone's audit. A disposition that overstates what was done poisons every later judgement built on it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19G-14-01 | Tampering | making the guard green by editing `19-REVIEW.md` rather than disposing the finding | critical | mitigate | `19-REVIEW.md` must be byte-unchanged, asserted by an empty `git diff --name-only` over the plan's whole commit range, and stated as a prohibition. The deliverable is a new file, never an edit to the review. |
| T-19G-14-02 | Repudiation | a disposition that cites a commit which does not exist yet | high | mitigate | Every cited hash must resolve under `git cat-file -t`, listed in the SUMMARY; `CR-04` is required to be named as outstanding and forbidden from being described as fixed. |
| T-19G-14-03 | Spoofing | a record that looks phase-scoped but does not satisfy the guard's predicate | medium | mitigate | Task 1 requires BOTH halves of `todoMentionsPhase()` to hold independently, each with a stated check, so a later edit to one half cannot silently un-scope the record. |
| T-19G-14-04 | Tampering | a new pending todo turning `docs-deferred-ledger.test.ts` red | medium | mitigate | The record is filed under `completed/`, not `pending/`; acceptance criteria assert `.planning/todos/pending/` and `STATE.md` are both untouched, and the guard's two directions were read before writing. |
| T-19G-14-05 | Tampering | `.planning/` deliverable silently stripped by worktree isolation | high | mitigate | `<execution_notes>` requires isolation forced to `none` before dispatch, and re-forced rather than re-queried. The plan's entire deliverable is one `.planning/` file, so a stripped commit would deliver nothing at all. |
| T-19G-14-06 | Repudiation | a green claimed from a full-suite run polluted by a concurrent sibling plan | medium | mitigate | This plan deliberately gates on the two `.planning/`-only guards run standalone, and requires the scoping decision to be stated in the SUMMARY rather than left as a silent omission. |
| T-19G-14-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs nothing and adds no dependency; `package-lock.json` is not in `files_modified`, so no `## Package Legitimacy Audit` row is required. |
</threat_model>

<artifacts_this_phase_produces>
Symbols and files this plan CREATES (newly-created names — excluded from drift verification against
the pre-existing tree):

**New planning document:**
- `.planning/todos/completed/2026-08-25-phase-19-review-cr-02-disposition.md`

**No new source symbol. No new test. No new fixture. No shipped file. No schema change. No edit to
any existing file** — this plan adds one file and modifies none.

**Created by sibling plans in this run** (listed here so the whole run's new-symbol surface is in one
place): `DISPATCH_CONTEXT_SHAPES`, `reachesGateInterior()`, `GATE_INTERIOR_DECLARATIONS`,
`FP2_INDEXED_PROLOGUE`, `FP2_IMMEDIATE_PROLOGUE`, `FP2_CODE_SIZE`, `withZeroPageVectorData()`,
`ZEROPAGE_DATA_POINTER`, `IMMEDIATE_DATA_POINTER`, the caller-citation marker constant, the scan's
clamped effective-end local, the fixture directories `fp2-zeropage-data-pointer` and
`fp2b-immediate-data-pointer`, and `19-REVIEW-FIX.md`.
</artifacts_this_phase_produces>

<verification>
1. `cd src/mcp/vice && node --test docs-review-disposition.test.ts` — exits 0 standalone.
2. `cd src/mcp/vice && node --test audit-integrity.test.ts` — exits 0 standalone.
3. `cd src/mcp/vice && node --test docs-deferred-ledger.test.ts` — exits 0.
4. All seven guards named in D-12-02's message run standalone, each exit code recorded by name.
5. `git diff --name-only -- .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-REVIEW.md`
   is empty across the plan's commit range.
6. `git status --porcelain src/`, `git status --porcelain .planning/todos/pending/` and
   `git status --porcelain .planning/STATE.md` are all empty.
7. Every commit hash cited in the record resolves under `git cat-file -t`.
8. Every commit subject carries `[skip release]`.
</verification>

<success_criteria>
- `CR-02` has a real, evidence-bearing disposition in a source the AUDIT-01 guard reads.
- `19-REVIEW.md` is byte-unchanged and finding-id continuity is intact.
- Both guards the re-verification named pass standalone, so the two recorded failures are gone
  before any wave-2 plan asserts a green tree.
- `CR-04` is named as outstanding, never as fixed, because at wave-1 time it is.
- No pending todo, no `STATE.md` edit, no source change.
</success_criteria>

<output>
Create `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-14-SUMMARY.md`
when done. It MUST name the finding id **`CR-02`** and the file that disposes it, so the disposition
exists in the guard's source 1 as well as source 3 and does not depend on a single artifact.

Also record: the guard's verbatim pre-write failure text; its post-write
`# tests` / `# pass` / `# fail` line; the standalone exit code of each of the seven guards named in
D-12-02's message; every commit hash cited in the record with its `git cat-file -t` result; how both
halves of `todoMentionsPhase()` were checked; and the explicit statement that the full suite was not
run in this plan, with the reason.
</output>
