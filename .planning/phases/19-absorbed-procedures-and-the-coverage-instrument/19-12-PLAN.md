---
phase: 19-absorbed-procedures-and-the-coverage-instrument
plan: 12
type: execute
wave: 3
depends_on: ["19-11"]
gap_closure: true
files_modified:
  - src/mcp/vice/anno-coverage.ts
  - src/mcp/vice/anno-coverage.test.ts
autonomous: true
requirements: [COV-02, COV-01]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The multi-caller rule's label-name branch demands that a caller's name be USED AS A REFERENCE, not merely present: a comment whose only occurrence of a caller's label text is an ordinary English word in ordinary prose leaves the label counted as undocumented, and the decision about what counts as a reference is written down beside the rule rather than left implicit"
    - "The tightening has a both-directions control committed in the same change: a comment that genuinely cites its caller still clears the rule, and the well-documented control fixture still produces a CLEAN report — so the rule is not satisfied by an instrument that fails everything"
    - "The dispatch scan describes the same address space as the census it reports beside: no value in `dispatch.tableEntryAddresses`, `dispatch.discoveredTargets`, any `SplitTableFinding.targets` or any stack-return finding's targets exceeds `$FFFF`, even for a project file whose declared origin plus payload length would leave the 16-bit space"
    - "The bound is stated once and read everywhere: the scan derives a single effective end from the declared origin and length, clamped at `$10000`, and every in-image test and index computation in the scan reads that one value rather than recomputing origin-plus-length"
    - "COV-02 / encoding — the equality the dispatch gate applies is exact numeric equality on a zero-page address in the range 0..255, compared between an indirect jump's operand value and a store instruction's operand value, never a string or hex-text comparison; and the census range is the half-open byte range from the clamped origin to the clamped effective end"
    - "COV-01 / encoding — a length is a count of bytes over that same half-open range and nothing else; comment equality remains exact string equality after the four pinned normalisation steps; and auto-name prefix matching remains ASCII case-sensitive. This run changes where the range ends, never what equality means"
    - "COV-01 / concurrency — the instrument remains read-only by construction: the committed source-level assertion that `anno-coverage.ts` contains no filesystem-write call and no live-session import still passes after this run's edits, so parallel runs cannot corrupt a project and an interrupted run leaves nothing partial on disk"
  prohibitions:
    - requirement_id: COV-02
      category: transparency
      statement: "MUST NOT certify a comment as documenting its caller when the comment refers to no caller at all — a falsely-clean verdict is worse than a noisy one, because it removes the label from the sample population that would have caught it"
    - requirement_id: COV-01
      category: transparency
      statement: "MUST NOT print an address the measured machine cannot address — a report whose dispatch sub-report and whose census describe two different address spaces is misleading even when nothing crashes"
    - requirement_id: COV-02
      category: values
      statement: "MUST NOT tighten a rule without shipping the control that shows the genuine case still passes — a rule that declines everything measures nothing, and is indistinguishable from a rule that works until someone checks"
  artifacts:
    - path: "src/mcp/vice/anno-coverage.ts"
      provides: "`namesACaller()`'s reference-demanding label-name branch with its decision recorded beside it, and the single clamped effective end shared by every bound in `scanIndirectDispatch()`"
    - path: "src/mcp/vice/anno-coverage.test.ts"
      provides: "The coincidental-English-word control, its both-directions twin, and the extended 16-bit bound assertions over the dispatch sub-report"
  key_links:
    - from: "namesACaller()"
      to: "reproducibility.multiCallerUndocumented"
      via: "the multi-caller cross-reference rule decides which labels enter the reproducibility sample population"
      pattern: "multiCallerUndocumented"
    - from: "scanIndirectDispatch()'s effective end"
      to: "computeStructuralCensus()'s clamped range"
      via: "both must describe one address space, or the report contradicts itself"
      pattern: "0x10000"
---

<objective>
Close the two remaining `19-REVIEW.md` findings that gap 2 requires a real disposition for and that
are cheap to fix properly: **WR-13**, the multi-caller rule's label-name branch that an ordinary
English word satisfies by coincidence, and **IN-05**, the dispatch scan that was never given the
16-bit bound its sibling census received.

Purpose: WR-13 is the same falsely-clean class as CR-01 at a lower trigger rate — a label counted as
documented stays in `labels.kindRatio.user` and stays in the reproducibility sample, so the measure
that exists to catch it can no longer see it. IN-05 makes the report describe two different address
spaces: the census is bounded at `$10000` and its own dispatch sub-report is not.

Output: a multi-caller name branch that demands a reference rather than a coincidence, with a
both-directions control; and one clamped effective end read by every bound in the scan, with the
existing out-of-space test extended to assert it over the dispatch sub-report.
</objective>

<gap_closure_context>
**Source:** `19-VERIFICATION.md` gap 2, `missing` item 2 — *"Record a disposition for WR-13, WR-14,
WR-15 and IN-05 — fixed-and-cited, or a filed todo naming the reason."* Plan 19-11 chose
fixed-and-cited for WR-14 and WR-15; this plan chooses fixed-and-cited for WR-13 and IN-05.

**WR-13, reproduced in `19-REVIEW.md` against HEAD** (`anno-coverage.ts:1342-1343`), identical
inputs with one caller renamed:

- callers of `$0820` are `[$0012, $0034]`; the comment reads
  `"[confirmed-code] sets the mode flag before the main loop runs"`
- caller `$0012` named `caller` → `multiCallerUndocumented = { count: 1, addresses: [2080] }` — correct
- caller `$0012` named `loop` → `multiCallerUndocumented = { count: 0, addresses: [] }` — falsely clean

Nothing in the comment refers to the routine at `$0012`. The external analyser label names are routinely
ordinary English words — `loop`, `init`, `main`, `start`, `data`, `table`, `draw` — and an ordinary
description of what a routine does will contain them by accident.

The review offers two admissible fixes and asks that whichever is chosen be documented beside the
rule: demand an adjacent citation marker, or stop accepting bare names entirely and require the hex
form that CR-01's fix already anchors correctly.

**IN-05, from `19-REVIEW.md`** (`anno-coverage.ts:722` `inImage`, and the class-2/3/4 index
arithmetic that likewise reads origin-plus-length): IN-04 bounded the census at `$10000`; the scan's
own predicate was left unbounded. Values at or above `$10000` cause no crash — the census's `mark()`
filters them — but they are written into the JSON report that Phase 20 and Phase 21 consume, and
`anno-cli.ts`'s `hexAddr()` renders them as five hex digits.

**Depends on 19-11** because it edits the same regions of `anno-coverage.ts` and
`anno-coverage.test.ts`. Read `19-11-SUMMARY.md` first.

**Scope fence.** Do not modify, supersede or rewrite plans 19-01 … 19-09 or their SUMMARYs. Do not
touch `19-REVIEW.md`.
</gap_closure_context>

<execution_notes>
**Full suite, not the subset.** `cd src/mcp/vice && npm test`, never `npm run test:automated`.

**WR-13 is a judgment call with two admissible answers, and the judgment must be recorded.** Rated
`reversible` — it is a predicate in a non-exported helper with no persisted state and no published
contract, so no decision checkpoint is warranted. Take the review's *cheapest honest tightening*
(demand that the name be used as a reference) rather than removing the name branch, because removing
it would make the rule hex-only and silently reclassify every project whose comments cite callers by
name. Whichever wording of "used as a reference" you implement, write the decision and its rejected
alternative into the doc comment beside the rule, so the next reader does not have to re-derive it.

**Check the committed comment corpus before choosing the marker set.** The well-documented control
fixture's comments cite callers by HEX (`"reached from $0810 and from $0816"`), so the hex branch is
what keeps NC5 clean today. Confirm that by reading the generator's `GOOD_COMMENTS`, and make sure
the tightening does not turn any currently-clean fixture non-clean by accident. If it does, that is a
signal about the marker set, not a reason to add an exemption.

**`[skip release]` in every commit subject.** The hold's lifting condition is a no-gaps Phase 19
re-verification, which has not happened.

**Node ≥ 22.18, no build step.** Do not add a transpile step and do not move logic into a `.mts`
file.
</execution_notes>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VERIFICATION.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-REVIEW.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-11-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the multi-caller rule's name branch demand a reference, not a coincidence</name>
  <precondition>Plan 19-11 has landed and the full suite is green at its final commit — this task edits the same file and must not be layered on a red tree.</precondition>
  <reversibility rating="reversible">A predicate tightening inside one non-exported helper. No exported signature changes, no schema key changes, no persisted state. Reverting the hunk restores the prior behaviour exactly.</reversibility>
  <files>src/mcp/vice/anno-coverage.ts, src/mcp/vice/anno-coverage.test.ts</files>
  <read_first>
    - `src/mcp/vice/anno-coverage.ts` around lines 1329-1350 — `namesACaller()` in full as 19-06 left it: `escapeRegExp()`, the hex branch anchored on `` `\\$${token}(?![0-9a-f])` `` over both the bare and canonical-4-digit widths, and the label-name branch bounded by `(?<![0-9A-Za-z_])` / `(?![0-9A-Za-z_])`. Only the name branch changes.
    - `src/mcp/vice/anno-coverage.ts` — the `multiCallerUndocumented` computation and the `excludedByMultiCallerRule` list it feeds, so you can see which downstream figures a falsely-clean verdict contaminates (the kind ratio and the reproducibility sample).
    - `src/mcp/vice/anno-coverage.test.ts` lines 550-620 — the two committed anchoring controls from 19-06 (`ANCHORING: a colliding longer hex …` and the identifier-boundary control). Both must keep passing, and the new control follows their shape.
    - `src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs` — `GOOD_COMMENTS` and `UNNAMED_CALLER_COMMENTS`. `GOOD_COMMENTS`' `set_flag` entry cites its callers by hex; `UNNAMED_CALLER_COMMENTS` names none. NC5 must stay clean and NC4 must stay caught by `reproducibility`.
    - `19-REVIEW.md` WR-13 in full (lines 737-786) — the reproduction, the two admissible fixes and the control the review asks for.
    - `19-REVIEW.md` CR-01's resolution note (lines 141-160) — what 19-06 anchored and why, so this task tightens the remaining branch without loosening the fixed one.
  </read_first>
  <action>
Change only the label-name branch of `namesACaller()`. Keep the hex branch exactly as 19-06
anchored it — it is correct and is what the committed fixtures rely on.

Require that the caller's label text be used as a REFERENCE. Implement the review's cheapest honest
tightening: the identifier-bounded name must additionally carry an adjacent citation signal — the
name in backticks, the name immediately followed by a parenthesised hex address, or the name
appearing in a caller-naming context such as a `from`, `called by` or `callers:` phrase. Keep the
existing identifier boundaries on both sides so the `my_entry_pointer` / `entry_point` case that
19-06 fixed stays fixed. Build the marker set from a named module-level constant rather than inlining
literals in the regex, so the decision is inspectable and a future widening is a one-line edit in an
obvious place.

Rewrite the doc comment beside the rule to record the decision explicitly: what now counts as naming
a caller, why bare presence is not enough (the external analyser label names are routinely ordinary English
words and an ordinary description will contain them by accident), and the alternative that was
weighed and rejected — dropping the name branch entirely and requiring the hex form — with the reason
it was rejected. A future reader must not have to re-derive this.

Add the control the review asks for, plus its twin, so the change is both-directions in one commit:

- the coincidental-word control: two callers of one label, one of them named with an ordinary English
  word that appears in the comment's prose while the comment refers to no caller at all. Assert
  `multiCallerUndocumented.count` is 1 and its `addresses` names the label. State the pre-fix value
  (`count: 0`, empty addresses) in the failure message.
- the genuine-citation control: the same inputs with a comment that genuinely cites the caller by the
  chosen marker. Assert `count` is 0. Without this, the tightening would be satisfiable by a rule
  that rejects every name.
- an explicit assertion that the well-documented control fixture still produces a CLEAN report, and
  that NC4 is still caught by the `reproducibility` measure. Those two are the fixture-level
  both-directions statement.

Run the whole coverage suite and confirm no other fixture's clean/non-clean verdict moved. If one
did, treat it as information about the marker set and fix the marker set — do not add an exemption
and do not touch the fixture's expected verdict.
  </action>
  <verify>
    <automated>cd src/mcp/vice &amp;&amp; node --test anno-coverage.test.ts</automated>
    <automated>cd src/mcp/vice &amp;&amp; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - The coincidental-word control reports `multiCallerUndocumented.count === 1` with the label's address in `addresses`; its failure message names the pre-fix result `{count: 0, addresses: []}`.
    - The genuine-citation control reports `multiCallerUndocumented.count === 0`, asserted in the same commit as the tightening.
    - `coverageFindings(reportFor("nc5-well-documented")).clean` is `true` and its `findings` array is empty — the non-vacuity control is unmoved.
    - `nc4-multi-caller-unnamed` is still caught, and still by a finding whose `measure` is `reproducibility`.
    - Both 19-06 anchoring controls still pass: the colliding-longer-hex case still reports `{count: 1, …}` and the identifier-boundary case is unchanged.
    - No committed fixture's `expect_clean` or `expect_measure` value was edited — confirm with `git diff src/mcp/vice/fixtures/`, which must show no change at all from this task.
    - The doc comment beside `namesACaller()` states the chosen definition of "names a caller", the rejected alternative, and the reason for the rejection. Confirm by reading it.
    - `cd src/mcp/vice && node --test anno-coverage.test.ts` exits 0 and `npx tsc --noEmit -p tsconfig.json` exits 0.
  </acceptance_criteria>
  <done>A comment must use a caller's name as a reference before the multi-caller rule counts the label as documented; the decision and its rejected alternative are recorded beside the rule; and the genuine case, the well-documented control and NC4's catch all still behave as before.</done>
</task>

<task type="auto">
  <name>Task 2: Give the dispatch scan the same 16-bit bound the census already has, stated once</name>
  <reversibility rating="reversible">One derived local plus the replacement of every comparison that recomputes the same quantity. No exported signature or schema change.</reversibility>
  <files>src/mcp/vice/anno-coverage.ts, src/mcp/vice/anno-coverage.test.ts</files>
  <read_first>
    - `src/mcp/vice/anno-coverage.ts` `scanIndirectDispatch()` from its opening through the class-4 loop as 19-11 left it — `safeBytes`, `safeOrigin` (already clamped to `0..0xffff`), `size`, the `inImage` predicate, `wordAt()`, and every place in the class-2, class-3 and class-4 walks that compares against origin-plus-length. Enumerate them all before editing; the review names four sites and 19-11 may have moved or added one.
    - `src/mcp/vice/anno-coverage.ts` `computeStructuralCensus()` and the clamp IN-04 installed — the scan's new bound must be the SAME quantity, computed the same way, so the two describe one address space.
    - `src/mcp/vice/anno-coverage.test.ts` — the existing test named for a census whose origin plus size would leave the 16-bit space. That test is the one to extend rather than duplicate.
    - `src/mcp/vice/anno-cli.ts` `hexAddr()` — how an out-of-space value is currently rendered (five hex digits), which is what makes this user-visible rather than internal.
    - `19-REVIEW.md` IN-05 and IN-04's resolution note (lines 940-1010) — IN-04 states the threat model in one sentence: a `.regen2000proj` file the operator did not author can claim any origin and carry any length.
  </read_first>
  <action>
Derive the bound once, immediately after `size` is established: an effective end equal to the lesser
of origin-plus-length and `0x10000`. Give it a name that says what it is and a one-line comment
saying why it exists and which finding produced it.

Replace the `inImage` predicate's upper comparison with that value, and replace every remaining
comparison in the scan that recomputes origin-plus-length — the class-2 table walk's in-image test,
the class-3 target plausibility test and its index computations, and the class-4 reconstruction's
in-image test and its `discovered` guard. Do not leave one site recomputing the quantity: the point
of the fix is that the bound is stated once and read everywhere, which is this repository's
single-seam-per-concern pattern applied at function scope.

Extend the existing out-of-16-bit-space test rather than adding a parallel one. On a malformed
origin/length pair, assert that every value in `dispatch.tableEntryAddresses` is at most `$FFFF`,
that every value in `dispatch.discoveredTargets` is at most `$FFFF`, that every target of every
split-table finding is at most `$FFFF`, and that every target of every stack-return finding is at
most `$FFFF`. Assert the census's own bound is unchanged, so the two halves of the report are shown
to describe one address space rather than merely both being small.

Add the non-vacuity half: the malformed input must still produce a non-empty scan somewhere, or the
four assertions above would be satisfied by a scan that found nothing. Assert that the same payload
at a legal origin produces the findings the bounded run truncates, so the bound is shown to be doing
work rather than emptying the report.
  </action>
  <verify>
    <automated>cd src/mcp/vice &amp;&amp; node --test anno-coverage.test.ts</automated>
    <automated>cd src/mcp/vice &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - For a project whose declared origin plus payload length exceeds `$10000`, every value in `dispatch.tableEntryAddresses`, `dispatch.discoveredTargets`, every `SplitTableFinding.targets` entry and every stack-return finding's target is at most `0xffff`. All four asserted.
    - The non-vacuity half is asserted: the same payload at a legal origin produces at least one dispatch finding, so the bounded assertions are not satisfied by an empty scan.
    - The census's own bound is asserted unchanged in the same test, demonstrating that the census and its dispatch sub-report describe one address space.
    - Every comparison inside `scanIndirectDispatch()` that previously recomputed origin-plus-length now reads the single derived effective end. Confirm by reading the function end to end and listing the changed sites in the SUMMARY, with their line numbers as found.
    - `cd src/mcp/vice && npm test` — the FULL suite, never `test:automated` — reports 0 failures, UNCONDITIONALLY. Record the observed `# tests` / `# pass` / `# fail` line. The two failures `19-VERIFICATION.md` recorded (`CR-02` undispositioned, and D-12-02 cascading from it) were discharged by plan 19-14 in wave 1, so any failure observed here is this plan's to diagnose. Do NOT attempt to make the tree green by editing a planning document — that is the tampering `19-13`'s `T-19G-13-01` forbids.
    - `cd src/mcp/vice && npx tsc --noEmit -p tsconfig.json` exits 0.
    - `git diff src/mcp/vice/anno-coverage.ts` shows no change to `COVERAGE_SCHEMA_VERSION` and no change to `COVERAGE_REPORT_KEYS`.
    - `cd src/mcp/vice && node fixtures/coverage/make-coverage-fixtures.mjs` twice leaves `git status --porcelain src/mcp/vice/fixtures/coverage` empty — this plan writes no fixture.
  </acceptance_criteria>
  <done>The dispatch scan is bounded at `$10000` by a single derived value that every in-image test and index computation reads, and the report's two halves are asserted to describe one address space on a malformed input that still produces findings.</done>
</task>

</tasks>

<threat_model>
ASVS level 1. Blocking threshold: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a `.regen2000proj` file's declared origin and length → the dispatch scan | An operator does not author every image they analyse. A file can claim any origin and carry any length, and the scan must not emit addresses outside the machine's address space into a report that Phase 20 and Phase 21 consume. |
| a store comment's prose → the multi-caller documentation verdict | The comment text is authored by whoever annotated the project, including an agent working fast. A rule satisfied by an accidental word certifies documentation that does not exist, and removes the label from the sample that would have caught it. |
| the coverage report → downstream phases | Phase 20 is instructed to run this instrument throughout. Any value it prints is consumed as fact by later work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19G-12-01 | Spoofing | an ordinary English word spoofing a caller citation | high | mitigate | Task 1 demands an adjacent citation signal in the name branch, held down by a coincidental-word control and its genuine-citation twin in the same commit. |
| T-19G-12-02 | Tampering | a report carrying addresses outside the measured machine's address space | high | mitigate | Task 2 derives one clamped effective end and requires every in-image test and index computation in the scan to read it, with four bounded assertions plus a non-vacuity half. |
| T-19G-12-03 | Repudiation | a tightening that quietly makes the instrument decline everything | high | mitigate | Both tasks require a both-directions control in the same commit: the genuine citation still clears the rule, and the legal-origin run still produces the findings the bounded run truncates. |
| T-19G-12-04 | Tampering | a fixture's expected verdict edited to accommodate a rule change | medium | mitigate | An acceptance criterion requires `git diff src/mcp/vice/fixtures/` to show no change from Task 1, so a green result cannot be bought by moving the target. |
| T-19G-12-05 | Denial of service | a malformed origin/length pair driving an unbounded walk | medium | mitigate | The existing `MAX_TABLE_ENTRIES` clamp is retained and the new effective end bounds the walk earlier; 19-11's plausibility stop bounds it earlier still. |
| T-19G-12-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs nothing and adds no dependency; `package-lock.json` is not in `files_modified`, so no `## Package Legitimacy Audit` row is required. |
</threat_model>

<artifacts_this_phase_produces>
Symbols and files this plan CREATES (newly-created names — excluded from drift verification against
the pre-existing tree):

**New module-level constant** in `src/mcp/vice/anno-coverage.ts`:
- the caller-citation marker set read by `namesACaller()`'s label-name branch

**New derived local** inside `scanIndirectDispatch()` in `src/mcp/vice/anno-coverage.ts`:
- the single clamped effective end shared by every in-image test and index computation

**New tests** in `src/mcp/vice/anno-coverage.test.ts`:
- the coincidental-English-word multi-caller control
- its genuine-citation twin
- extensions to the existing out-of-16-bit-space test covering the dispatch sub-report, plus the
  non-vacuity half

**No new fixture directory. No new exported runtime symbol. No schema change** —
`COVERAGE_SCHEMA_VERSION` stays 2 and `COVERAGE_REPORT_KEYS` is untouched. **No shipped CLI flag.**
</artifacts_this_phase_produces>

<verification>
1. `cd src/mcp/vice && node --test anno-coverage.test.ts` — exits 0, 0 failures.
2. `cd src/mcp/vice && npm test` — the FULL suite, never `test:automated`. 0 failures.
3. `cd src/mcp/vice && npx tsc --noEmit -p tsconfig.json` — exits 0.
4. `git diff src/mcp/vice/fixtures/` — empty; this plan writes no fixture and edits no expected
   verdict.
5. `cd src/mcp/vice && node fixtures/coverage/make-coverage-fixtures.mjs` twice leaves
   `git status --porcelain src/mcp/vice/fixtures/coverage` empty.
6. `node scripts/check-npm-packages.mjs`, `node scripts/check-skill-tool-coverage.mjs`,
   `node scripts/check-skill-description-overlap.mjs` — all exit 0.
7. The four bounded assertions and the non-vacuity half are present and asserted.
8. `git status --porcelain .planning/REQUIREMENTS.md` is empty — this plan does not tick a box.
9. Every commit subject carries `[skip release]`.
</verification>

<success_criteria>
- A caller's label name counts only when it is used as a reference, the decision is recorded beside
  the rule with its rejected alternative, and a genuine citation still clears the rule.
- No committed fixture's expected verdict was moved to accommodate the change.
- The dispatch scan and the census describe one address space, enforced by a single derived bound
  that every site in the scan reads.
- The bound is shown to be doing work: the same payload at a legal origin still produces findings.
- The full suite is green and nothing is published.
</success_criteria>

<output>
Create `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-12-SUMMARY.md`
when done. It MUST name the finding ids **`WR-13`** and **`IN-05`** and the commits that fixed them —
this is the disposition source `docs-review-disposition.test.ts` reads.

Also record: the chosen definition of "names a caller" and the alternative rejected with its reason;
the observed pre-fix and post-fix values for the coincidental-word control; the list of
`scanIndirectDispatch()` sites changed to read the single effective end, with their line numbers as
found; the four bounded assertions' observed results; and the full-suite
`# tests` / `# pass` / `# fail` line.
</output>
