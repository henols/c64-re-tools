---
phase: 19-absorbed-procedures-and-the-coverage-instrument
plan: 13
type: execute
wave: 4
depends_on: ["19-12", "19-14"]
gap_closure: true
files_modified:
  - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-REVIEW-FIX.md
  - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/deferred-items.md
  - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md
autonomous: true
requirements: [COV-02, COV-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every finding id declared by a heading in `19-REVIEW.md` has a recorded disposition in a source the guard actually reads, and the recording is DURABLE: the ledger `19-REVIEW-FIX.md` names every one of them by id, so the guard's green does not depend on the incidental prose of a verification report that a later re-verification will rewrite"
    - "Finding-id continuity is preserved absolutely: `19-REVIEW.md` is byte-unchanged by this run — not regenerated, not renumbered, not annotated — because regenerating it orphans ids and reds the same guard again"
    - "Each of the six ids this gap-closure run is accountable for — CR-02, CR-04, WR-13, WR-14, WR-15, IN-05 — carries a disposition that names WHAT was done and WHICH plan and commit did it, and CR-04 is recorded as FIXED rather than accepted, because it is the defect SC4 turns on"
    - "The workspace test suite is GREEN on the full run: `cd src/mcp/vice && npm test` reports 0 failures and exits 0, verified with `npm test` and never with `npm run test:automated`, which skips `MANUAL_ONLY_TESTS` and would hide a CI failure"
    - "`docs-review-disposition.test.ts` (AUDIT-01) passes standalone, and `audit-integrity.test.ts`'s D-12-02 — which cascades from it — passes standalone too, so the cascade is demonstrated resolved rather than assumed"
    - "`deferred-items.md`'s two entries state the CURRENT condition rather than a superseded prediction: each records that its stated clearing condition (19-09's SUMMARY landing) came and went without clearing it, names why, and states the new clearing condition in terms this run can actually satisfy"
    - "`19-VALIDATION.md` is EXTENDED, not replaced: it gains one evidence row per plan in this second gap-closure run, each naming the command that actually ran, the file it ran against and the observed result, and every pre-existing row is byte-unchanged"
    - "`REQUIREMENTS.md` still records COV-01 and COV-02 as NOT complete and their status-table rows as `Gaps Found` at the end of this run — a requirement is re-checked by re-verification, never by the plan that fixed it"
    - statement: "COV-01 / empty — a coverage report over an empty, single-byte or otherwise degenerate payload reports real zeros in all three measures rather than NaN, an error or a missing key, and the schema's nine top-level keys are all present"
      verification: backstop
  prohibitions:
    - requirement_id: COV-02
      category: values
      statement: "MUST NOT make a red guard green by rewriting the ledger it reads — finding ids are never renumbered and `19-REVIEW.md` is never regenerated; a disposition is a record of what was done, not a phrasing that satisfies a parser"
    - requirement_id: COV-02
      category: transparency
      statement: "MUST NOT let a guard's green depend on incidental prose in a document that a later run will rewrite — a disposition that exists only because a verification report happened to quote the id is an accident, not a record"
    - requirement_id: COV-01
      category: values
      statement: "MUST NOT tick a requirement checkbox or move a status row from the plan that fixed the defect — the box is earned by re-verification, and a self-ticked box poisons every later judgement built on it"
    - requirement_id: COV-02
      category: transparency
      statement: "MUST NOT record a deferred item's clearing condition as satisfied when it is not, or leave a superseded prediction standing as though it were the current state — a ledger that understates the state is worse than no ledger, because it is trusted"
  artifacts:
    - path: ".planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-REVIEW-FIX.md"
      provides: "The durable disposition ledger for every finding id in 19-REVIEW.md — disposition source 5 of the guard's five"
    - path: ".planning/phases/19-absorbed-procedures-and-the-coverage-instrument/deferred-items.md"
      provides: "Both entries corrected to the current state, each with a clearing condition this run can satisfy"
    - path: ".planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md"
      provides: "Extended executed-evidence rows for the second gap-closure run"
  key_links:
    - from: "src/mcp/vice/docs-review-disposition.test.ts"
      to: ".planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-REVIEW-FIX.md"
      via: "disposition source 5 — the guard reads every *-REVIEW-FIX.md in the phase's own directory"
      pattern: "REVIEW-FIX"
    - from: "src/mcp/vice/audit-integrity.test.ts"
      to: "src/mcp/vice/docs-review-disposition.test.ts"
      via: "D-12-02 cascades: no milestone audit may declare a gated status while any docs guard is red"
      pattern: "D-12-02"
---

<objective>
Close GAP 2: the phase's own artifacts leave the workspace test suite RED. Record real, durable
dispositions for every `19-REVIEW.md` finding id, correct the two `deferred-items.md` entries whose
stated clearing condition came and went, extend the phase's validation record, and take the full
suite to zero failures.

Purpose: 19-07's checkpoint conditions lifting `[skip release]` on a Phase 19 re-verification that
returns no gaps. A red tree is a gap. And the red is CORRECT signal — the fix is a real disposition,
never a rewritten ledger.

Output: `19-REVIEW-FIX.md` as the phase's durable disposition ledger; two corrected deferred entries;
an extended `19-VALIDATION.md`; and a recorded, observed green full-suite run.
</objective>

<gap_closure_context>
**Source:** `19-VERIFICATION.md` gap 2 — *"The phase's own artifacts leave the workspace test suite
RED"*, `status: failed`. Confirmed on the full suite at HEAD `80c544b`: `# tests 2564, # pass 2517,
# fail 2`, exit 1, 95.8s.

**IMPORTANT — the state has DRIFTED since the verification was written, and the drift is itself the
problem this plan must fix properly.** Measured at plan time, `docs-review-disposition.test.ts` now
names exactly **one** undispositioned finding, not five:

```
19-REVIEW.md (19-absorbed-procedures-and-the-coverage-instrument): CR-02
```

The other five ids the verification named — CR-04, WR-13, WR-14, WR-15, IN-05 — became
"dispositioned" the moment `19-VERIFICATION.md` was committed, because the guard's **disposition
source 2** is the phase's own `*-VERIFICATION.md`, and the verification's `missing:` prose quotes
each id by name. That is a disposition by accident. It will evaporate the next time a
re-verification rewrites that file, and it records nothing about what was actually done.

**`CR-02` is discharged upstream, in wave 1, by plan 19-14** — a dated disposition record under
`.planning/todos/completed/`, the guard's source 3. That was pulled out of this plan deliberately:
three plans in this run (19-10, 19-11, 19-12) gate on a green full suite, and leaving the only
genuine red until wave 4 would have made every one of those gates fail for a reason outside its own
scope. So this plan no longer has to make the guard green — it has to make the guard's green
**earned**:

1. Replace the accidental dispositions with durable ones, in a source that exists because someone
   decided to write it: `19-REVIEW-FIX.md`, the guard's **source 5** and this repository's
   established convention (`02-REVIEW-FIX.md`, `07-REVIEW-FIX.md`). Discharging CR-02 early does
   **not** substitute for this — the five ids that `19-VERIFICATION.md`'s prose incidentally
   satisfied are still riding on an accident until this ledger exists.
2. Cover `CR-02` in that ledger too, **citing 19-14's record rather than re-deciding it**, so the
   phase has one place a reader can consult for every id — with the two records agreeing, not
   competing.

**The guard's five accepted disposition sources**, from `docs-review-disposition.test.ts`'s own
header, for a given (phase, id) pair — at least one of:
1. a `*-SUMMARY.md` in that phase's own directory naming the id;
2. that phase's own `*-VERIFICATION.md` naming the id;
3. a `.planning/todos/{pending,completed}/*.md` file naming the id AND identifiably about that phase;
4. a top-level `.planning/v*-MILESTONE-AUDIT.md`'s `tech_debt:` block, in the entry whose own
   `phase:` field matches, naming the id;
5. that phase's own `*-REVIEW-FIX.md`.

The guard's match is a word-bounded mention. It deliberately does not grade disposition quality — so
quality is this plan's responsibility, not the guard's.

**CRITICAL CONSTRAINT, learned the hard way in this repository:** *regenerating* `19-REVIEW.md`
orphans finding ids and reds the same guard again. Preserve id continuity — never renumber, never
regenerate the review file to make the guard green. `19-REVIEW.md` must be byte-identical before and
after this plan.

**Note on `.planning/todos/pending/`:** filing a pending todo is one of the guard's accepted sources,
but `docs-deferred-ledger.test.ts` enforces a two-directional invariant between
`.planning/todos/pending/` and `STATE.md`'s `## Deferred Items` section. A new pending todo without a
matching STATE.md row turns THAT guard red. This plan therefore records dispositions in
`19-REVIEW-FIX.md` and in the plan SUMMARYs, and files no new pending todo. If a genuine todo is
unavoidable, the STATE.md row is part of the same commit — never a follow-up.

**Scope fence.** Do not modify, supersede or rewrite plans 19-01 … 19-09 or their SUMMARYs.
</gap_closure_context>

<execution_notes>
**Worktree isolation must be OFF for this plan.** Every file in `files_modified` is under
`.planning/`. Worktree mode strips `.planning/` files from commits, so a plan whose entire
deliverable is planning documents silently cannot deliver under worktree isolation. Force isolation
to `none` before dispatching this plan, and do not re-query to "confirm" it — re-force it.

**Full suite, not the subset.** The gate is `cd src/mcp/vice && npm test`. Never
`npm run test:automated` — the automated subset skips `MANUAL_ONLY_TESTS` and would hide a CI
failure. Any acceptance criterion here that asserts a green tree names the full-suite command.

**The cascade is expected to be spurious except for one guard.** `audit-integrity.test.ts`'s D-12-02
failure message names seven red docs guards, but six of them
(`docs-core-value-decision`, `docs-dangling-refs`, `docs-deferred-ledger`, `docs-fork-decision`,
`docs-linerefs`, `docs-anno-decisions`) pass when run standalone. Verify that claim yourself by
running each standalone rather than repeating it — if one of the six is genuinely red, it is new
information and must be reported, not absorbed.

**`REQUIREMENTS.md` is READ-ONLY in this plan.** COV-01 and COV-02 stay unchecked and their status
rows stay `Gaps Found`. `git status --porcelain .planning/REQUIREMENTS.md` must be empty at the end.
The box is re-checked by re-verification, never by the run that fixed the defect.

**`[skip release]` in every commit subject.** The hold's lifting condition — verbatim from 19-07's
checkpoint — is *"Phase 19 re-verification returns no gaps."* This plan does not perform that
re-verification and therefore cannot lift the hold. Say so in the SUMMARY rather than leaving the
hold as an unowned default.
</execution_notes>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VERIFICATION.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/deferred-items.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the durable disposition ledger, without touching a single byte of 19-REVIEW.md</name>
  <precondition>Plans 19-10, 19-11, 19-12 and 19-14 have all landed with their SUMMARYs, and each SUMMARY names the finding ids it discharged; 19-14's disposition record for `CR-02` exists at `.planning/todos/completed/2026-08-25-phase-19-review-cr-02-disposition.md`. This task cites those commits and that record; it cannot invent them.</precondition>
  <reversibility rating="reversible">A new planning document. Nothing shipped, no code touched; deleting the file restores the prior state (and the prior red guard).</reversibility>
  <files>.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-REVIEW-FIX.md</files>
  <read_first>
    - `src/mcp/vice/docs-review-disposition.test.ts` lines 1-75 — the file header, which enumerates the five accepted disposition sources verbatim and explains why source 5 exists. Read this before choosing where anything lands.
    - `src/mcp/vice/docs-review-disposition.test.ts` `parseFindingIds()` and `declaredFindingIdsInHeadings()` (lines 110-150) — the exact heading regex the guard uses to derive the id set: heading levels 2 through 6, at least one space after the marker, id terminated at the first non-digit. This tells you precisely which ids exist and why.
    - `src/mcp/vice/docs-review-disposition.test.ts` `isDispositioned()` and `dispositionTextForPhase()` (lines 250-290) — the word-bounded match and the concatenation of sources. Note that `*-REVIEW-FIX.md` is included by the `/REVIEW-FIX\.md$/i` filter and that `*-REVIEW.md` itself is NOT a source.
    - `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-REVIEW.md` — every finding heading, and the existing in-place resolution blockquotes on CR-01, CR-02, WR-01, WR-02, IN-01 and IN-04. Those blockquotes are the disposition TEXT to carry across; they are not themselves a disposition source.
    - An existing `*-REVIEW-FIX.md` in this repository — find one with `ls .planning/phases/*/*-REVIEW-FIX.md` and read it, so the new file follows the established shape rather than inventing one.
    - `19-06-SUMMARY.md`, `19-07-SUMMARY.md`, `19-08-SUMMARY.md`, `19-09-SUMMARY.md`, and the four SUMMARYs from this run (19-10, 19-11, 19-12, 19-14) — for the commit hashes and the wording each plan used for the findings it discharged.
    - `.planning/todos/completed/2026-08-25-phase-19-review-cr-02-disposition.md` — plan 19-14's wave-1 disposition record for `CR-02`, in full. The ledger cites it rather than re-deriving CR-02, and the two must agree.
  </read_first>
  <action>
Derive the id set mechanically first, then write the ledger. Do not type the id list from memory and
do not copy it from the verification report — extract it from `19-REVIEW.md` using the guard's own
heading rule (a heading at level 2 through 6, at least one space, then the id, terminated at the
first non-digit), and record the derived count in the SUMMARY.

Create `19-REVIEW-FIX.md` in the phase directory. Give it frontmatter naming the phase, the review it
dispositions, the date, and the derived finding count. Open with a short statement of what the file
is: the phase's durable disposition ledger, written because a disposition that exists only because a
verification report quoted an id is an accident that the next re-verification erases.

Then a table with one row per finding id, in the order the ids appear in `19-REVIEW.md`. Each row
carries: the id; a one-line statement of the finding; the disposition, drawn from a small fixed
vocabulary (fixed, fixed-narrowed-then-superseded, rejected-with-reason, deferred-with-owner); the
plan that discharged it; and the commit hash. Where a finding was rejected rather than fixed, the
row's reason field must carry the actual reason, not the word rejected.

The six ids this gap-closure run is accountable for get a paragraph each below the table, because a
table row is too small for them:

- **CR-02** — the defect class narrowed by 19-08 and then fully closed by 19-10. Its disposition was
  already filed in wave 1 by plan 19-14
  (`.planning/todos/completed/2026-08-25-phase-19-review-cr-02-disposition.md`), which is the record
  that took the guard green. **Cite that record; do not re-decide it and do not contradict it.** The
  one thing this paragraph adds that 19-14's record could not is the outcome: 19-14 was written at
  wave-1 time and correctly named CR-04 as OUTSTANDING, so state here what became of it — closed by
  plan 19-10, with the commit — and name 19-14's record as the earlier half. Two records that agree
  and cross-reference each other are a ledger; two that were each written from scratch are a
  contradiction waiting to be found.
- **CR-04** — recorded as **FIXED**, with the plan and commit from 19-10. It must NOT be recorded as
  accepted: it is the defect SC4 turns on, and an accepted disposition here would be a scope
  reduction wearing a ledger entry's clothes.
- **WR-13**, **WR-14**, **WR-15**, **IN-05** — each recorded as fixed, with the plan (19-11 or 19-12)
  and the commit, and one sentence naming the control that now holds the fix down. A fix with no
  control is not a fix this phase accepts.

Close with a short section stating the two properties this ledger exists to hold: that `19-REVIEW.md`
was not touched, and that every id in it is accounted for here regardless of whether some other
source also happens to mention it.

Do not edit `19-REVIEW.md`. Do not renumber anything. Do not add, remove or reword a finding heading.
  </action>
  <verify>
    <automated>cd src/mcp/vice &amp;&amp; node --test docs-review-disposition.test.ts</automated>
    <automated>test -z "$(git diff --name-only -- .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-REVIEW.md)" &amp;&amp; echo REVIEW-UNTOUCHED</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && node --test docs-review-disposition.test.ts` exits 0 with 0 failures. Record the observed `# tests` / `# pass` / `# fail` line.
    - `git diff --name-only -- .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-REVIEW.md` is empty across this plan's whole commit range — the review file is byte-unchanged.
    - Set equality is demonstrated, not asserted in prose: the id set derived from `19-REVIEW.md`'s headings and the id set named in `19-REVIEW-FIX.md` are identical in both directions. Perform the comparison mechanically and paste the command and its output into the SUMMARY.
    - Independence from the accidental source is demonstrated: `CR-04`, `WR-13`, `WR-14`, `WR-15` and `IN-05` each appear in `19-REVIEW-FIX.md` and in the SUMMARY of the plan that fixed them, so the guard would stay green even if `19-VERIFICATION.md` were replaced. State how this was checked.
    - `CR-04`'s disposition is `fixed`, cites plan 19-10 and a real commit hash, and the word `accepted` does not describe it.
    - `CR-02`'s paragraph names 19-08 (narrowed) and 19-10 (closed) with their commits, AND cites plan 19-14's wave-1 record by path. Confirm the two records do not contradict each other on any fact — in particular that 19-14 named CR-04 outstanding at its date and this ledger names it closed at its own, which is agreement across time rather than contradiction. State how the comparison was made.
    - The five ids that were riding on `19-VERIFICATION.md`'s incidental prose — `CR-04`, `WR-13`, `WR-14`, `WR-15`, `IN-05` — each appear in `19-REVIEW-FIX.md`. This is the half of gap 2 that 19-14 did NOT discharge and that this plan owns.
    - Every commit hash cited in the ledger resolves: `git cat-file -t <hash>` returns `commit` for each. List them in the SUMMARY.
  </acceptance_criteria>
  <done>Every finding id in `19-REVIEW.md` has a durable, quality-bearing disposition in `19-REVIEW-FIX.md`; the review file itself is byte-unchanged; and the AUDIT-01 guard passes standalone.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the two stale deferred entries and extend the phase's validation record</name>
  <reversibility rating="reversible">Two planning documents. Nothing shipped; reverting the hunks restores the prior text.</reversibility>
  <files>.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/deferred-items.md, .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/deferred-items.md` in full (both entries) — entry 1 lists 17 ids and states "Clears when: the last gap-closure plan (19-09) lands its SUMMARY"; entry 2 states "Clears when: item 1 clears". Both predictions are now false: 19-09 landed and neither cleared, because a second review pass added five findings after it.
    - `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md` — its frontmatter, the executed-evidence table's column shape, its row count, and the flagged-assumption section 19-09 appended. The verification recorded that 19-09's diff was 85 insertions and exactly 1 deletion; this task's diff must show insertions only.
    - `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VERIFICATION.md` — the gap-2 `reason` block, which records the exact observed suite numbers and the six-of-seven cascade finding. Those are the facts the corrected entries must reflect.
    - `src/mcp/vice/docs-deferred-ledger.test.ts` lines 1-60 — its SCOPE note. It scans `STATE.md`'s `## Deferred Items` section against `.planning/todos/`, NOT this phase-local `deferred-items.md`. Confirm that yourself before editing, so you know exactly which guard this file is and is not under.
    - The three SUMMARYs from this run (19-10, 19-11, 19-12) — for the commands each actually ran and the results each actually observed. Every new validation row must name a command that a SUMMARY records having been run.
  </read_first>
  <action>
**`deferred-items.md`.** Correct both entries in place. Do not delete them and do not rewrite the
file — the historical record of what was deferred and why is the point, and a ledger that erases its
own wrong prediction teaches nothing.

For entry 1: keep the original text as the record of what was believed, and add a dated correction
block stating plainly that the stated clearing condition was reached and the condition did NOT clear;
why (a second review pass on 2026-08-25 added CR-04, WR-13, WR-14, WR-15 and IN-05 after 19-09
landed); what the guard actually reports today (measured, with the command and its output, not
quoted from the verification report); and the new clearing condition, phrased so this run can satisfy
it — the durable ledger `19-REVIEW-FIX.md` existing and the guard passing standalone.

For entry 2: the same treatment. Its clearing condition was "item 1 clears", which is still the right
dependency but was stated against a prediction that failed. Add the dated correction, record that the
seven-guard cascade was re-checked standalone and how many of the seven are genuinely red today, and
restate the clearing condition against the corrected item 1.

Both entries must end in a state where a reader can tell, without reading any other file, what is
true right now.

**`19-VALIDATION.md`.** EXTEND it. Append one evidence row per plan in this second gap-closure run —
19-10, 19-11, 19-12 and this plan — into the existing executed-evidence table, following its existing
column shape exactly. Each row names the requirement it evidences, the command that actually ran, the
file or fixture it ran against, and the observed result. Do not paraphrase a result; quote the number
that was observed. Do not add a row for a command no SUMMARY records having been run.

Add a short dated section recording what this second gap-closure run measured that the first did not:
the interior control's pre-fix and post-fix values, the class-4 negative controls' results, and the
full-suite line. Keep it to observed facts with their commands.

Delete nothing. The verification checks this file's diff for deletions, and a deletion here is
indistinguishable from a record being quietly dropped.
  </action>
  <verify>
    <automated>cd src/mcp/vice &amp;&amp; node --test docs-deferred-ledger.test.ts</automated>
    <automated>git diff --numstat -- .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --numstat` for `19-VALIDATION.md` over this plan's commit range shows a deletions count of 0. Paste the observed numstat line into the SUMMARY.
    - Both `deferred-items.md` entries retain their original text and each carries a dated correction block stating the failed prediction, the measured current condition, and a new clearing condition this run can satisfy.
    - The measured current condition in entry 1 is a real measurement taken during this task — the command and its output are pasted into the SUMMARY, and they are not copied from `19-VERIFICATION.md`.
    - Entry 2 records how many of the seven cascaded guards are genuinely red today, established by running each standalone. Name every guard run and its exit code in the SUMMARY.
    - `cd src/mcp/vice && node --test docs-deferred-ledger.test.ts` exits 0 — confirming this phase-local file is outside that guard's scope and that nothing this task did disturbed `STATE.md`'s Deferred Items invariant.
    - Every new `19-VALIDATION.md` row's command appears verbatim in one of the four SUMMARYs of this gap-closure run.
    - `git status --porcelain .planning/todos/` is empty — this plan files no pending todo, so `STATE.md`'s Deferred Items table needs no row.
  </acceptance_criteria>
  <done>Both deferred entries state the current condition with a clearing condition this run can satisfy, and the phase's validation record is extended with executed evidence and zero deletions.</done>
</task>

<task type="auto">
  <name>Task 3: Take the full suite to zero and record the gate, without ticking a box the run has not earned</name>
  <reversibility rating="reversible">A verification and recording task. It changes no source and edits no requirement state.</reversibility>
  <files>.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md</files>
  <read_first>
    - `src/mcp/vice/package.json` `scripts` — the exact definitions of `test` and `test:automated`, and which files `MANUAL_ONLY_TESTS` excludes. You must be able to state why the subset is not the gate.
    - `src/mcp/vice/audit-integrity.test.ts` around line 225 — the D-12-02 assertion and its guard runner, so you can read its failure message correctly if it is still red.
    - `.planning/REQUIREMENTS.md` — the COV-01 and COV-02 entries and their rows in the status table. Note their current state before doing anything, so you can prove at the end that nothing moved.
    - `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-07-SUMMARY.md` around lines 100-110 — the checkpoint option selected and the verbatim condition that lifts `[skip release]`. This plan restates it; it does not reinterpret it.
    - `19-VERIFICATION.md`'s `### Release Hold` section — the same condition from the verifier's side.
  </read_first>
  <action>
Run the gate and record it. Nothing here is optional and nothing here is a substitute for anything
else.

Run the FULL suite: `cd src/mcp/vice && npm test`. Record the observed `# tests`, `# pass`, `# fail`
and `# duration_ms` lines and the exit code. If it is not zero failures, do not proceed to the
recording steps — diagnose, and if the failure is outside this gap-closure run's scope, halt and
report it rather than absorbing it.

Run the two guards that gap 2 named, standalone, and record each exit code:
`node --test docs-review-disposition.test.ts` and `node --test audit-integrity.test.ts`. D-12-02
cascades from the first, so both must be observed green independently rather than inferred from the
aggregate run.

Run the typecheck and the three repository check scripts, and record each exit code:
`npx tsc --noEmit -p tsconfig.json` from `src/mcp/vice`, then `node scripts/check-npm-packages.mjs`,
`node scripts/check-skill-tool-coverage.mjs` and `node scripts/check-skill-description-overlap.mjs`
from the repository root.

Regenerate the coverage fixtures twice and confirm `git status --porcelain
src/mcp/vice/fixtures/coverage` is empty, so the determinism contract still holds after two plans
added fixtures to the generator.

Then verify — read-only — that `REQUIREMENTS.md` still records COV-01 and COV-02 as incomplete and
their status rows as `Gaps Found`, and that `git status --porcelain .planning/REQUIREMENTS.md` is
empty. If the state has drifted to complete, halt and report; do not "fix" it in either direction.

Append the gate's results to `19-VALIDATION.md` as a dated block, insertions only.

Finally, restate the release hold in this plan's SUMMARY: the option selected at 19-07's checkpoint
and the exact condition that lifts `[skip release]`, quoted verbatim from 19-07's SUMMARY, together
with the plain statement that this run does not satisfy it because the re-verification has not
happened. An unnamed hold becomes the permanent state by default; a named one does not.
  </action>
  <verify>
    <automated>cd src/mcp/vice &amp;&amp; npm test</automated>
    <automated>cd src/mcp/vice &amp;&amp; node --test docs-review-disposition.test.ts &amp;&amp; node --test audit-integrity.test.ts</automated>
    <automated>cd src/mcp/vice &amp;&amp; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && npm test` exits 0 with `# fail 0`. The observed `# tests` / `# pass` / `# fail` / `# duration_ms` lines are pasted into the SUMMARY, together with the command as run. The subset command is not used anywhere as evidence.
    - `node --test docs-review-disposition.test.ts` exits 0 standalone, and `node --test audit-integrity.test.ts` exits 0 standalone. Both exit codes recorded.
    - `npx tsc --noEmit -p tsconfig.json` exits 0; `check-npm-packages.mjs`, `check-skill-tool-coverage.mjs` and `check-skill-description-overlap.mjs` each exit 0. All four exit codes recorded.
    - Running `node fixtures/coverage/make-coverage-fixtures.mjs` twice leaves `git status --porcelain src/mcp/vice/fixtures/coverage` empty, and the fixture directory count matches the pinned `COMMITTED_CONTROL_FIXTURES` constant. Both observed values recorded.
    - `.planning/REQUIREMENTS.md` is unmodified: `git status --porcelain .planning/REQUIREMENTS.md` is empty, COV-01 and COV-02 are still unchecked, and both status rows still read `Gaps Found`. The observed line numbers and states are recorded.
    - `19-VALIDATION.md`'s diff for this task shows insertions only — a deletions count of 0 on `git diff --numstat`.
    - The SUMMARY restates 19-07's checkpoint option and the verbatim `[skip release]` lifting condition, and states that this run does not satisfy it.
    - Every commit subject in this plan carries `[skip release]`.
  </acceptance_criteria>
  <done>The full suite is observed green with its numbers recorded, both named guards pass standalone, the requirement state is verified unmoved without being edited, and the release hold's owner and named lifting condition are written down.</done>
</task>

</tasks>

<threat_model>
ASVS level 1. Blocking threshold: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning documents → future verification and milestone audit | `19-REVIEW-FIX.md`, `deferred-items.md`, `19-VALIDATION.md` and `REQUIREMENTS.md` are read by re-verification and by the next milestone's audit. A row naming a command nobody ran, or a box ticked by the run that fixed the thing, poisons every later judgement built on it. |
| a guard's derived id set → the ledger that must cover it | The guard derives ids from `19-REVIEW.md` by regex rather than from a list. A ledger typed from memory can silently miss an id that the guard sees, and a ledger built by editing the review can silently remove one the guard used to see. |
| a red guard → the temptation to edit what it reads | The guard reads planning documents that the same agent can write. The cheapest way to make it green is also the one that destroys the record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19G-13-01 | Tampering | making a red guard green by rewriting the ledger it reads | critical | mitigate | `19-REVIEW.md` must be byte-unchanged, asserted by an empty `git diff --name-only` over this plan's whole commit range, and stated as a prohibition. Task 1's ledger is a new file, never an edit to the review. |
| T-19G-13-02 | Repudiation | a disposition that exists only because a verification report quoted the id | high | mitigate | Task 1 requires each of the six ids to appear in `19-REVIEW-FIX.md` and in the fixing plan's own SUMMARY, so the guard's green survives the next re-verification rewriting `19-VERIFICATION.md`. |
| T-19G-13-03 | Spoofing | a requirement marked complete by the run that fixed it | high | mitigate | Task 3 is read-only over `REQUIREMENTS.md`; `git status --porcelain` on that path must be empty, and the task halts and reports if the state has drifted. |
| T-19G-13-04 | Tampering | `19-VALIDATION.md` rewritten rather than extended | high | mitigate | An acceptance criterion requires a deletions count of 0 on `git diff --numstat`, and every new row's command must appear verbatim in one of this run's SUMMARYs. |
| T-19G-13-05 | Tampering | `.planning/` deliverables silently stripped by worktree isolation | high | mitigate | `<execution_notes>` requires isolation forced to `none` before dispatch, and re-forced rather than re-queried. Every file in `files_modified` is under `.planning/`, so a stripped commit would deliver nothing at all. |
| T-19G-13-06 | Repudiation | a green claimed from the automated subset rather than the full suite | high | mitigate | Every green-tree acceptance criterion names `npm test` explicitly and forbids the subset as evidence; Task 3's `read_first` requires reading the two script definitions so the difference is understood rather than trusted. |
| T-19G-13-07 | Tampering | a new pending todo turning `docs-deferred-ledger.test.ts` red | medium | mitigate | This plan files no pending todo; an acceptance criterion asserts `git status --porcelain .planning/todos/` is empty, and the guard is run to confirm. |
| T-19G-13-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs nothing and adds no dependency; `package-lock.json` is not in `files_modified`, so no `## Package Legitimacy Audit` row is required. |
</threat_model>

<artifacts_this_phase_produces>
Symbols and files this plan CREATES (newly-created names — excluded from drift verification against
the pre-existing tree):

**New planning document:**
- `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-REVIEW-FIX.md`

**New sections appended to existing documents:**
- a dated correction block on each of `deferred-items.md`'s two entries
- a dated second-gap-closure evidence block in `19-VALIDATION.md`, plus one executed-evidence row
  per plan in this run
- a dated phase-gate block in `19-VALIDATION.md`

**No new source symbol. No new test. No new fixture. No shipped file. No schema change.**

**Created by sibling plans in this run** (listed here so the whole run's new-symbol surface is in one
place): `DISPATCH_CONTEXT_SHAPES`, `reachesGateInterior()`, `GATE_INTERIOR_DECLARATIONS`,
`FP2_INDEXED_PROLOGUE`, `FP2_IMMEDIATE_PROLOGUE`, `FP2_CODE_SIZE`, `withZeroPageVectorData()`,
`ZEROPAGE_DATA_POINTER`, `IMMEDIATE_DATA_POINTER`, the caller-citation marker constant, the scan's
clamped effective-end local, the fixture directories `fp2-zeropage-data-pointer` and
`fp2b-immediate-data-pointer`, and `.planning/todos/completed/2026-08-25-phase-19-review-cr-02-disposition.md`.
</artifacts_this_phase_produces>

<verification>
1. `cd src/mcp/vice && npm test` — the FULL suite, never `test:automated`. Exits 0 with `# fail 0`,
   and the observed counts are recorded.
2. `cd src/mcp/vice && node --test docs-review-disposition.test.ts` — exits 0 standalone.
3. `cd src/mcp/vice && node --test audit-integrity.test.ts` — exits 0 standalone.
4. `cd src/mcp/vice && node --test docs-deferred-ledger.test.ts` — exits 0.
5. `cd src/mcp/vice && npx tsc --noEmit -p tsconfig.json` — exits 0.
6. `node scripts/check-npm-packages.mjs`, `node scripts/check-skill-tool-coverage.mjs`,
   `node scripts/check-skill-description-overlap.mjs` — all exit 0.
7. `git diff --name-only -- .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-REVIEW.md`
   is empty across the plan's commit range.
8. `git diff --numstat -- .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md`
   shows a deletions count of 0.
9. `git status --porcelain .planning/REQUIREMENTS.md` and `git status --porcelain .planning/todos/`
   are both empty.
10. The id set derived from `19-REVIEW.md`'s headings equals the id set named in `19-REVIEW-FIX.md`,
    demonstrated mechanically with the command and output pasted into the SUMMARY.
11. Every commit subject carries `[skip release]`.
</verification>

<success_criteria>
- Every `19-REVIEW.md` finding id has a durable, quality-bearing disposition in a source the guard
  reads, and the review file is byte-unchanged.
- CR-04 is recorded as fixed and cited, never accepted.
- The full workspace suite is green, observed and recorded with its numbers, on `npm test`.
- Both named guards pass standalone, so the cascade is demonstrated resolved rather than assumed.
- Both `deferred-items.md` entries state the current condition with a clearing condition this run can
  satisfy.
- The phase's validation record is extended with executed evidence and zero deletions.
- `REQUIREMENTS.md` is unmoved, and the release hold's owner and verbatim lifting condition are
  written down.
</success_criteria>

<output>
Create `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-13-SUMMARY.md`
when done. It MUST name the finding id **`CR-02`** and the plan and commit that discharged it, and
must restate the ids **`CR-04`**, **`WR-13`**, **`WR-14`**, **`WR-15`** and **`IN-05`** with the
plans that fixed them, so the disposition survives in a plan SUMMARY as well as in the ledger.

Also record: the derived `19-REVIEW.md` finding count and the mechanical set-equality command with
its output; every commit hash cited in the ledger with its `git cat-file -t` result; the standalone
exit codes of all seven guards named in the D-12-02 cascade; the full-suite
`# tests` / `# pass` / `# fail` / `# duration_ms` lines; the `19-VALIDATION.md` numstat; the observed
`REQUIREMENTS.md` line numbers and states; the fixture directory count against the pinned constant;
and the release-hold record — the option selected at 19-07's checkpoint and the exact named condition
that lifts `[skip release]`, quoted verbatim, with the statement that this run does not satisfy it.
</output>
