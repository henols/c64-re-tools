---
phase: 19-absorbed-procedures-and-the-coverage-instrument
plan: 15
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - src/mcp/vice/anno-coverage.ts
  - src/mcp/vice/anno-coverage.test.ts
  - src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs
  - src/mcp/vice/fixtures/coverage/README.md
  - src/mcp/vice/fixtures/coverage/fp3-unlinked-push-idiom/project.regen2000proj
  - src/mcp/vice/fixtures/coverage/fp3-unlinked-push-idiom/store.json
  - src/mcp/vice/fixtures/coverage/fp3b-immediate-push-idiom/project.regen2000proj
  - src/mcp/vice/fixtures/coverage/fp3b-immediate-push-idiom/store.json
  - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md
autonomous: true
requirements: [COV-01, COV-02]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A payload whose only dispatch evidence is a `pha`/`pha`/`rts` that pushes bytes UNRELATED to the two paired indexed loads reports zero proven dispatch targets (COV-01, D-02, D-03)"
    - "The round-3 blocker payload — `lda $0830,x / sta $fb / lda $0838,x / sta $fc / pha / txa / pha / tya / rts`, 15 code bytes, no `0x6c` byte anywhere in the image — reports `dispatch.splitTables === []`, `provenDispatchTargets() === []`, `dispatch.tableEntryAddresses === []` and `classAt($0840) === \"unreached\"` (D-07.3)"
    - "The pairing is still REPORTED as one advisory candidate, so the gate demonstrably examined and DECLINED it rather than never seeing it (COV-02)"
    - "The class-3 route into `stack-return-push-idiom` is LIVE, not dead: a payload whose two paired loads are each immediately followed by a `pha` and whose `rts` follows both is still PROVEN (COV-01, both-directions)"
    - "`SPLIT_TABLE` still PROVES through the zero-page-vector shape and `STACK_RETURN` still PROVES through the class-4 pass — the tightening bought nothing by declining everything"
    - "`hasDispatchContext()`'s doc comment no longer asserts the falsified rationale that a pairing inside a class-4 window is never promoted by the class-3 pass (D-03)"
    - "The FP3 gate is watched FAIL: with the branch-A tightening reverted in the working tree, the new report-level assertion reds by name and the pre-fix numbers are reproduced (D-07)"
  artifacts:
    - "src/mcp/vice/fixtures/coverage/fp3-unlinked-push-idiom/ — the class-3 push-idiom route's first INTERIOR control, generated and committed"
    - "src/mcp/vice/fixtures/coverage/fp3b-immediate-push-idiom/ — its immediate twin, supplying a MEASURED census baseline"
    - "src/mcp/vice/anno-coverage.ts — `hasDispatchContext()` takes the pairing under test; branch A requires a proven push link"
    - "src/mcp/vice/anno-coverage.test.ts — the FP3 report-level control, the window-edge twin, the liveness positive control, `COMMITTED_CONTROL_FIXTURES` = 12"
    - ".planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md — the round-4 planted-violation row for the branch-A gate"
  key_links:
    - "class-3 pass condition (c) → `hasDispatchContext(insns, i, SPLIT_TABLE_WINDOW, pairing)` — the pairing under test now crosses the call boundary instead of being re-guessed inside"
    - "`hasDispatchContext()` branch A → `insns[firstIndex + 1]` and `insns[secondIndex + 1]` — the two `pha`s are read at the two paired loads' own successors, not counted anywhere in the window"
    - "FP3 fixture → `buildCoverageReport()` → `provenDispatchTargets()` → `computeStructuralCensus()` extraSeeds — the whole seam asserted at report level, not at scan level only"
  edge_predicates:
    - "empty: an empty byte array, a single-instruction payload, and a payload shorter than the pairing window each produce empty lists and no throw"
    - "adjacency: two zero-page store targets exactly equal, or differing by two or more, never form a vector; only a difference of exactly one counts, with the lower address as the vector's low byte"
    - "ordering: every printed address list is strictly ascending and deduped; advisory candidates appear in deterministic encounter order, so two runs over one input order identically"
    - "idempotency: `buildCoverageReport()` is `deepEqual` to itself over every committed fixture, and re-running the fixture generator twice leaves `git status --porcelain src/mcp/vice/fixtures/coverage` empty"
    - "concurrency: read-only by construction — the committed no-filesystem-write, no-live-session-import source assertion still passes"
    - "encoding: exact numeric equality on a zero-page address in 0..255 between an indirect jump's operand and a store's operand, never a string or hex-text comparison"
  prohibitions:
    - "19-REVIEW.md is not regenerated and stays byte-unchanged; finding-id continuity is preserved"
    - "No requirement checkbox in REQUIREMENTS.md is ticked by this plan, and no phase status is advanced by it"
    - "A tightening that declines everything is refused: `SPLIT_TABLE` and `STACK_RETURN` must still PROVE in the same commit as the tightening"
    - "A control built from OUTSIDE the predicate it constrains may not be declared as an interior control"
    - "`COVERAGE_SCHEMA_VERSION` is not bumped and `COVERAGE_REPORT_KEYS` is not changed — this is a predicate and control change, not a report-shape change"
    - "No scope-reduction language (`v1`, `for now`, `simplified`, `placeholder`, `future phase`) appears in any comment, fixture purpose field or assertion message this plan writes"
    - "No comment this plan writes hands work to a numbered phase (the assignment shape `comment-phase-pointers.test.ts` fails on); narration of what a past plan did stays legal"
    - "No new string or template literal in `src/mcp/vice/anno-coverage.ts` names a phase number (FLOW-02's surface)"
    - "WR-03 (the illegal-opcode inflation route) is not fixed here and not silently absorbed"
    - "`anno-session.ts`'s 200 ms call timeout is not widened — that file is outside this plan's fence"
    - "No runtime dependency is added to either published package"
---

<objective>
Close the class-3 route into `stack-return-push-idiom` at its root: a branch of
`hasDispatchContext()` may return true only on a proven data-flow link from the two reconstructed
table bases to the dispatch mechanism, never on the mere presence of a shape within the window
(D-02). This plan is the TRACER — the thinnest end-to-end slice through every layer this round
touches: the predicate, the generator, a committed fixture pair, a report-level assertion, and a
watched planted violation.

Purpose: SC4's goal qualifier ("a coverage instrument … that resists being gamed") has failed three
consecutive verifications in this one function, each time via a different shape. Rounds 1–3 were
scoped to REVIEW finding ids; this round is scoped to the defect class (D-01). The blocker payload
manufactures eight "proven" entry points and 47 of 64 bytes of code-or-table out of a 15-byte
program, silently, into the JSON Phase 20 runs under.

Output: a tightened branch A, the `fp3` / `fp3b` fixture pair, the report-level interior control,
the liveness positive control that proves the branch is not dead, and the planted-violation row in
`19-VALIDATION.md`.

**This round is six plans**, 19-15 through 19-20, run in waves 1 to 6. Plan 19-16 completes D-03 across
both true-returning sites and adds the source-derived pin that ENFORCES D-02 rather than restating it;
19-17 delivers D-06 as a composed corpus with a computed oracle; 19-18 delivers D-04 and D-05; 19-20
closes WR-03, promoted into this round by orchestrator decision; and 19-19 consolidates the record and
runs the phase gate last.

**Requirement scope.** `ABS-01`, `ABS-02`, `ABS-03`, `ABS-04` and `SURF-03` are closed by plans
19-01…19-05 and verified; they are OUT OF SCOPE for this round and no plan here touches them.
`COV-02`'s own text (vacuity detection plus the multi-caller rule) was judged earned by the round-3
verification; it is cited here only because the dispatch gate is the shared mechanism both
requirements read. The one open requirement is `COV-01`.

**The route identity decision, recorded here for the whole round.** `route` is a CLOSED enumeration
DERIVED FROM SOURCE, not an open string. Plan 19-18 introduces `DISPATCH_GATE_ROUTES` as a frozen
exported array in `anno-coverage.ts`, pinned to the module's own source text, and
`reachesGateInterior()` THROWS on a route it carries no predicate for. This plan changes no route
key; it only makes the class-3 route's PREDICATE honest, so that the route-keyed control 19-18
declares has something true to say.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-CONTEXT.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VERIFICATION.md
@CLAUDE.md
@src/mcp/vice/anno-coverage.ts
@src/mcp/vice/anno-coverage.test.ts
@src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs
</context>

<artifacts_this_phase_produces>
Symbols and paths this ROUND creates. A source-grounding pass must treat every name below as
newly created rather than as drift against an existing symbol.

**Created by this plan (19-15):**
- `src/mcp/vice/anno-coverage.ts`: `interface SplitOrientation` (the named return type of
  `resolveSplitOrientation()`); `interface DispatchPairing`; `hasDispatchContext()`'s fourth
  parameter `pairing`
- `src/mcp/vice/anno-coverage.test.ts`: `FP3_INTERIOR`, `FP3_IMMEDIATE`,
  `PUSH_IDIOM_WINDOW_EDGE`, `PUSH_IDIOM_LINKED`; `COMMITTED_CONTROL_FIXTURES` changes 10 → 12
- `src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs`: `FP3_INDEXED_PROLOGUE`,
  `FP3_IMMEDIATE_PROLOGUE`, `FP3_CODE_SIZE`, `UNLINKED_PUSH_IDIOM`, `IMMEDIATE_PUSH_IDIOM`,
  `assertNoIndirectJumpOpcode()`, `assertCarriesPushIdiom()`
- New fixture directories: `src/mcp/vice/fixtures/coverage/fp3-unlinked-push-idiom/`,
  `src/mcp/vice/fixtures/coverage/fp3b-immediate-push-idiom/`

**Created later in this round (declared here so the whole round's new-symbol set is in one place):**
- 19-16: `SplitOrientation.vectorLow`; test payloads `ZP_VECTOR_FOREIGN_JUMP` and
  `ZP_VECTOR_OWN_JUMP`; `functionBodyFromSource()`, the shared brace-matching source reader that the
  pairing-consultation pin and plan 19-18's four route pins all read
- 19-17: new file `src/mcp/vice/anno-coverage-grammar.test.ts` with `GRAMMAR_ORIGIN`,
  `PAYLOAD_SIZE`, `TABLE_LO_BASE`, `TABLE_HI_BASE`, `TARGET_BASE`, `DATA_TAIL`,
  `MAX_PROLOGUE_BYTES`, `Fragment`, `FRAGMENT_BYTES`, `CORE_COMBOS`, `ATTACHMENT_SETS`,
  `MAX_FRAGMENTS`, `MAX_CORPUS`, `MIN_CORPUS`, `MIN_PER_FAMILY`, `MIN_LINKED`,
  `enumerateArrangements()`, `renderArrangement()`, `immediateTwinOf()`, `buildCorpus()`, `CORPUS`,
  `Arrangement`, `GeneratedPayload`, `expectedProvenLink()`, `pairingsOf()`,
  `classFourWindowsOf()`, `orientationOf()`, `reconstructTargets()`, `everyTargetPlausible()`,
  `PINNED_IDIOMS`, `scanOfPayload()`, `censusOfPayload()`, `provenIds()`, `expectedIds()`
- 19-18: `DISPATCH_GATE_ROUTES` and `PROVEN_TARGET_SOURCES` (exported frozen arrays in
  `anno-coverage.ts`); `GateInteriorDeclaration.route`; `GATE_ROUTE_REACHABILITY`;
  `reachesGateInterior()`'s fourth parameter `route`
- 19-20: `isDecodableAsInstruction()` in `anno-coverage.ts`; test payloads `JAM_FILLED_IMAGE` and
  `NOP_FILLED_IMAGE`; Decision 6 in `19-DECISIONS.md`
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: The blocker payload, end to end — branch A consults the pairing under test, and the FP3 interior control proves it at report level</name>
  <files>src/mcp/vice/anno-coverage.ts, src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs, src/mcp/vice/fixtures/coverage/fp3-unlinked-push-idiom/project.regen2000proj, src/mcp/vice/fixtures/coverage/fp3-unlinked-push-idiom/store.json, src/mcp/vice/fixtures/coverage/fp3b-immediate-push-idiom/project.regen2000proj, src/mcp/vice/fixtures/coverage/fp3b-immediate-push-idiom/store.json, src/mcp/vice/anno-coverage.test.ts</files>
  <read_first>
    - src/mcp/vice/anno-coverage.ts — `hasDispatchContext()` (definition and doc comment),
      `resolveSplitOrientation()`, `DISPATCH_CONTEXT_SHAPES`, the class-4 pass (its
      `classFourWindow` claim) and the class-3 pass's conditions (a)…(e) including the single
      `hasDispatchContext(...)` call site
    - src/mcp/vice/anno-coverage.test.ts — the FP2 report-level test and its `FP2b earns its
      place` twin test (the exact shape this task copies), `reportFor()`, `payloadOf()`,
      `loadFixture()`, `scanOf()`, `wiredCensus()`, `COMMITTED_CONTROL_FIXTURES`
    - src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs — the FP2 pair's prologues,
      `withZeroPageVectorData()`, `assertDispatchesNowhere()` and the four enforced pair
      invariants (equal length, exactly 64 bytes, prologue length, identical tail)
    - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-CONTEXT.md — D-02,
      D-03, D-07.3
    - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VERIFICATION.md —
      the `gaps_remaining` entry's measured D2 and D3 numbers
  </read_first>
  <behavior>
    - Test 1 (RED first): the FP3 payload, run through `buildCoverageReport()`, reports
      `dispatch.splitTables === []`, `provenDispatchTargets(report.dispatch) === []`,
      `dispatch.tableEntryAddresses === []`, `classAt(report.structural, 0x0840) === "unreached"`
      and `structural.reachedAsInstruction <= store.code_size`.
    - Test 2: FP3 and its immediate twin report the SAME `structural.reachedAsInstruction`, and the
      twin's own value equals its declared `code_size` — so the equality is against a MEASURED
      baseline, not a remembered number.
    - Test 3: FP3 still reports exactly ONE advisory candidate
      (`dispatch.splitTableCandidates.length === 1`), so the gate examined and declined the pairing
      rather than never seeing it; the twin reports zero, because it has no indexed pair at all.
    - Test 4 (the liveness positive, both directions): a payload whose two paired loads are EACH
      immediately followed by a `pha`, with an `rts` after both, is still PROVEN —
      `scan.splitTables.length === 1`, `provenDispatchTargets(scan)` deep-equals the eight
      addresses `$0840`…`$0847`, and `structural.reachedAsInstruction` is strictly greater than the
      payload's own declared code size.
    - Test 5: `SPLIT_TABLE` still proves through the zero-page-vector shape and
      `provenDispatchTargets()` still includes `$c00d`; `STACK_RETURN` still proves through the
      class-4 pass. A tightening that declines everything measures nothing.
  </behavior>
  <action>
Give `hasDispatchContext()` the pairing under test and make its first branch demand a proven push
link, then commit the interior control that proves it at report level.

SOURCE — `src/mcp/vice/anno-coverage.ts`:

Extract the return type of `resolveSplitOrientation()` into a named `interface SplitOrientation`
with the two existing fields `loBase` and `hiBase`, and declare `interface DispatchPairing` with
`firstIndex: number`, `secondIndex: number` and `oriented: SplitOrientation`. Change
`hasDispatchContext()`'s signature to take a fourth parameter `pairing: DispatchPairing`, and change
its one call site in the class-3 pass (condition (c), the `gatedSoFar` expression) to pass
`{ firstIndex: i, secondIndex: j, oriented }` — the call already sits behind `oriented !== null`, so
the non-null orientation is available there without a new narrowing.

Replace the `stack-return-push-idiom` branch. It currently reads `insn.opcode === 0x60 && sawPha >= 2`
inside the window loop, which is satisfied by any two `pha` bytes and any `rts` anywhere in reach.
The replacement is a single check made against `pairing`, outside the collecting loop, that is true
only when ALL of: `insns[pairing.firstIndex + 1]` has opcode `0x48`; `insns[pairing.secondIndex + 1]`
has opcode `0x48`; and some instruction at an index strictly greater than `pairing.secondIndex + 1`
and strictly less than the window end has opcode `0x60`. That is the RTS-trick data flow stated as a
link: each of the two loads under test pushes the byte it just loaded, and the `rts` that consumes
the pushed pair follows both pushes. Delete `sawPha` and its increment — nothing else reads it.

Correct the doc comment. The `stack-return-push-idiom` bullet currently justifies the loose branch
by asserting that the class-4 pass runs first and claims its windows, so a pairing inside such a
window is never promoted here. That rationale is FALSIFIED and must be replaced, not preserved:
class 4 claims only its exact five-instruction shape, and when it declines — mixed registers, an
implausible target, an instruction between a load and its push — the class-3 pass reaches this branch
and promoted the pairing anyway. Write what the branch now requires and why the presence of a push
idiom is not evidence about THIS pairing, in the same voice as the existing CR-04 paragraph beneath
it. Do not delete that CR-04 paragraph; it explains the second branch.

GENERATOR — `src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs`:

Add a third control pair beside the FP2 pair, following its structure exactly. `FP3_INDEXED_PROLOGUE`
is the round-3 blocker payload verbatim: `lda $0830,x` / `sta $fb` / `lda $0838,x` / `sta $fc` / `pha`
/ `txa` / `pha` / `tya` / `rts`, fifteen bytes at `$0810`. `FP3_IMMEDIATE_PROLOGUE` is its immediate
twin — the two indexed loads become immediate loads padded with `nop` so the prologue length is
unchanged, the same zero-page vector is still constructed at `$fb`/`$fc`, and the same
`pha`/`txa`/`pha`/`tya`/`rts` tail is retained. `FP3_CODE_SIZE` is DERIVED from
`FP3_INDEXED_PROLOGUE.length`, never typed as a literal. Build both payloads with the existing
`withZeroPageVectorData()` concatenation helper, each carrying its OWN written-out data literal
(never a shared constant — a shared region would make the identical-tail invariant true by
construction): seventeen `nop` filler bytes, then eight ascending lo bytes `$40`…`$47` at `$0830`,
then eight `$08` hi bytes at `$0838`, then sixteen `nop` bytes at `$0840`, for exactly 64 bytes.
Reproduce the FP2 pair's four enforced invariants for this pair — equal length, exactly 64 bytes each,
both prologues exactly `FP3_CODE_SIZE`, byte-identical from `FP3_CODE_SIZE` onward.

`assertDispatchesNowhere()` must NOT be applied to this pair: FP3 exists precisely because it carries
`pha` bytes. Write two new generator invariants instead, so this pair's defining properties are
checked facts rather than claims in a comment. `assertNoIndirectJumpOpcode()` throws when byte `0x6c`
appears anywhere in the image, naming the offset — the property D-07.3 states, and what makes "nothing
here dispatches through these tables" true of the whole image rather than of the decoded prefix.
`assertCarriesPushIdiom()` throws unless the image carries at least two `0x48` bytes and at least one
`0x60` byte inside the fifteen-byte prologue — the interior property, so a future edit that removes the
push idiom cannot leave this pair silently bracketing the gate from the outside.

Register both fixtures in the `FIXTURES` array as `fp3-unlinked-push-idiom` (control `FP3`) and
`fp3b-immediate-push-idiom` (control `FP3b`), each with `code_size` set from `FP3_CODE_SIZE`,
`expect_clean: false`, `expect_measure: null`, and empty symbol/comment/block/cross-reference sets —
matching the FP2 pair's registration. Each `purpose` field states what the payload is, what it must
report, and the pre-fix numbers it was observed RED against (`reached=31`, `tableEntry=16`,
`splitTables=1`, eight proven targets at `$0840`…`$0847`) so the fixture carries its own evidence.

TEST — `src/mcp/vice/anno-coverage.test.ts`:

Declare `FP3_INTERIOR = "fp3-unlinked-push-idiom"` and `FP3_IMMEDIATE = "fp3b-immediate-push-idiom"`
beside the existing FP2 constants, and raise `COMMITTED_CONTROL_FIXTURES` from 10 to 12. Add the
report-level test for the FP3 pair, modelled on the FP2 test one section above it: read the bound
from the fixture's own `code_size` and assert it is a number (never type the bound into the test),
then assert the five report-level facts of behavior Test 1, the twin equality of Test 2, and the
advisory-candidate counts of Test 3, each with a message naming the pre-fix number it replaces.

Add `PUSH_IDIOM_LINKED` as an in-suite payload for behavior Test 4 — the liveness positive control.
It is a 64-byte image at `$0810` whose thirteen-byte prologue is `lda $0830,x` / `pha` / `sta $fb` /
`lda $0838,x` / `pha` / `sta $fc` / `rts`, with the same table and target layout as FP3. Each paired
load is immediately followed by its push, so the tightened branch accepts it; class 4 declines the
window because an instruction sits between the first push and the second load, so the class-3 route
is the one under test. Assert at scan level (`splitTables.length`, `provenDispatchTargets(scan)`
deep-equal to the eight addresses) plus the census RELATION that `reachedAsInstruction` exceeds the
prologue length — a relation, not a pinned count, per this project's "assert relations, not counts"
rule. This control is load-bearing: without it a tightened branch that can never fire would be dead
code wearing a sufficient-shape label, which is a worse defect than the loose branch it replaced.

Keep the existing `SPLIT_TABLE` and `STACK_RETURN` assertions passing untouched (behavior Test 5) —
they are this tightening's both-directions half and must not be relaxed to accommodate it.
  </action>
  <verify>
    <automated>cd src/mcp/vice && node fixtures/coverage/make-coverage-fixtures.mjs &amp;&amp; node --test anno-coverage.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && node --test anno-coverage.test.ts` exits 0 with a test count strictly
      greater than the 71 recorded at the round-3 verification.
    - `cd src/mcp/vice && npx tsc --noEmit` exits 0.
    - `ls -d src/mcp/vice/fixtures/coverage/*/ | wc -l` prints 12, and
      `COMMITTED_CONTROL_FIXTURES` in `anno-coverage.test.ts` is 12.
    - The FP3 fixture's own `store.json` declares `code_size: 15`, and
      `reportFor("fp3-unlinked-push-idiom")` returns `structural.reachedAsInstruction === 15`,
      `dispatch.splitTables` deep-equal `[]`, `provenDispatchTargets(...)` deep-equal `[]`,
      `dispatch.tableEntryAddresses` deep-equal `[]`, `classAt(structural, 0x0840) === "unreached"`
      and `dispatch.splitTableCandidates.length === 1`.
    - `reportFor("fp3b-immediate-push-idiom")` returns `structural.reachedAsInstruction === 15` and
      `dispatch.splitTableCandidates.length === 0`.
    - Neither committed FP3 payload contains byte `0x6c` at any offset, and each contains at least
      two `0x48` bytes and one `0x60` byte within its first fifteen bytes — enforced by the two new
      generator throws, not asserted in prose.
    - `PUSH_IDIOM_LINKED` reports `scan.splitTables.length === 1` and `provenDispatchTargets(scan)`
      deep-equal to `[0x0840, 0x0841, 0x0842, 0x0843, 0x0844, 0x0845, 0x0846, 0x0847]`.
    - `scanOf(SPLIT_TABLE).splitTables.length === 1` and `provenDispatchTargets(scanOf(SPLIT_TABLE))`
      includes `0xc00d`; the class-4 `STACK_RETURN` assertions are unchanged and pass.
    - `grep -c 'skipped outright' src/mcp/vice/anno-coverage.ts` prints 0.
    - The `stack-return-push-idiom` guard reads `pairing` in its own condition and no window-wide
      push counter survives — checked by reading the branch, and pinned mechanically by plan 19-16's
      pairing-consultation assertion rather than by a source grep for the deleted identifier, which a
      comment naming the removed counter would falsely trip.
    - Running the generator twice leaves `git status --porcelain src/mcp/vice/fixtures/coverage`
      empty.
    - `grep -c 'COVERAGE_SCHEMA_VERSION = 2' src/mcp/vice/anno-coverage.ts` prints 1 — the report
      shape did not change.
  </acceptance_criteria>
  <reversibility rating="costly">
    D-02's invariant is what every control in 19-16 through 19-18 is written against, so restating
    it later means rewriting the control mechanism rather than only the predicate. The signature
    change to `hasDispatchContext()` is local (one definition, one call site), but the interface it
    introduces is read by three later plans.
  </reversibility>
  <done>
    The round-3 blocker payload is a committed fixture that reports zero proven dispatch targets and
    a census equal to its declared code size, its immediate twin supplies the measured baseline, the
    tightened branch is demonstrably still live, and both pre-existing positive controls still prove.
  </done>
</task>

<task type="auto">
  <name>Task 2: The window-edge twin — prove FP3 sits INSIDE the predicate rather than beside it</name>
  <files>src/mcp/vice/anno-coverage.test.ts, src/mcp/vice/fixtures/coverage/README.md</files>
  <read_first>
    - src/mcp/vice/anno-coverage.test.ts — the FP3 test written in task 1, `SPLIT_TABLE_WINDOW`'s
      import and use, `withSplitTableData()` and `withOrdinaryData()` (the pre-sized-array payload
      helpers this task copies), the `reachesGateInterior()` witness and
      `GATE_INTERIOR_DECLARATIONS`
    - src/mcp/vice/anno-coverage.ts — `SPLIT_TABLE_WINDOW` and the class-3 window bound
      `Math.min(insns.length, i + 1 + SPLIT_TABLE_WINDOW)`
    - src/mcp/vice/fixtures/coverage/README.md — the three-group description this task extends
    - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VERIFICATION.md — D3's
      measured numbers (`reached=16`, `tableEntry=0`, `splitTables=0`, `proven=0`, `$0840` unreached)
  </read_first>
  <action>
Add `PUSH_IDIOM_WINDOW_EDGE` to `src/mcp/vice/anno-coverage.test.ts`: the FP3 payload with ONE
`nop` inserted before its `rts`, so the `rts` falls at instruction index nine and the pairing window
from the leading load covers indices zero through eight inclusive. Build it into a pre-sized 64-byte
array the way `withSplitTableData()` does, so the sixteen-byte prologue leaves the tables at `$0830`
and `$0838` and the targets at `$0840` exactly where FP3 has them. Derive the payload from FP3's own
prologue bytes by insertion rather than typing the instruction sequence out again, so "identical
except for where the `rts` sits" is true by construction and the assertion is about the SCAN's
output.

Assert at scan and census level that it reports nothing proven — `splitTables` empty,
`provenDispatchTargets()` empty, `tableEntryAddresses` empty, `classAt($0840) === "unreached"` — and
that `reachedAsInstruction` equals its sixteen-byte prologue length, derived from the prologue rather
than typed. State in the test's own comment why this control exists NOW that FP3 declines too: its
job has changed from discriminating the boundary to being the OUTSIDE half of the witness pair. FP3
satisfies the class-3 route's pre-gate sufficient condition and this payload does not, which is what
proves the interior predicate 19-18 declares is not a machine that returns true for everything.

Add both FP3 fixtures to `GATE_INTERIOR_DECLARATIONS`. `fp3-unlinked-push-idiom` reaches the
interior of `stack-return-push-idiom` with polarity `negative`; its note states that it carries a
same-register indexed pairing with consecutive zero-page stores and a `pha`/`pha`/`rts` that class 4
declines, which is the region no control reached before. `fp3b-immediate-push-idiom` is `OUTSIDE`
with polarity `negative`, for the reason `fp2b`'s row gives: it builds the same vector and carries the
same push idiom, but its two loads are immediate, so there is no indexed pair to rule on. The
route key these rows will carry arrives in 19-18; declare them against the existing shape key here
so the mechanical witness check covers them from this commit onward.

Update `src/mcp/vice/fixtures/coverage/README.md`: the count moves from ten to twelve and the groups
from three to four. The new group is the class-3 push-idiom route's interior control pair, described
the way the FP2 group is — what it is inside, what it must report, and why an outside-bracketing
control could not have held that property down.
  </action>
  <verify>
    <automated>cd src/mcp/vice && node --test anno-coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && node --test anno-coverage.test.ts` exits 0.
    - `PUSH_IDIOM_WINDOW_EDGE` reports `splitTables` deep-equal `[]`,
      `provenDispatchTargets(scan)` deep-equal `[]`, `tableEntryAddresses` deep-equal `[]`,
      `classAt(census, 0x0840) === "unreached"` and `reachedAsInstruction === 16`.
    - `reachesGateInterior(PUSH_IDIOM_WINDOW_EDGE, 0x0810, "stack-return-push-idiom")` returns
      `false` while `reachesGateInterior(payloadOf(FP3_INTERIOR), 0x0810,
      "stack-return-push-idiom")` returns `true` — asserted, so the interior predicate is not
      satisfied by everything.
    - `GATE_INTERIOR_DECLARATIONS` contains a row for `fp3-unlinked-push-idiom` with
      `position: "stack-return-push-idiom"` and `polarity: "negative"`, and a row for
      `fp3b-immediate-push-idiom` with the `OUTSIDE` position, and the existing "every gate-interior
      declaration is mechanically TRUE" test passes over both.
    - The README's opening count line names TWELVE fixtures in FOUR groups, and the new group is
      described the way the FP2 group is — what it is inside, what it must report, and why an
      outside-bracketing control could not have held that property down.
    - `git status --porcelain src/mcp/vice/fixtures/coverage` is empty after re-running the generator.
  </acceptance_criteria>
  <done>
    FP3's position inside the class-3 route is a mechanically-witnessed fact with a payload just
    outside it as the contrast, both fixtures are declared, and the fixture README describes twelve
    fixtures in four groups.
  </done>
</task>

<task type="auto">
  <name>Task 3: Watch the new gate FAIL — the planted-violation demonstration for the branch-A tightening</name>
  <files>.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md, src/mcp/vice/anno-coverage.ts</files>
  <read_first>
    - src/mcp/vice/anno-coverage.ts — the branch-A condition written in task 1
    - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md — the
      existing executed-evidence rows and planted-violation entries for rounds 1–3, whose format
      this task extends
    - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-CONTEXT.md — D-07
  </read_first>
  <action>
A gate nobody has watched fail is not known to be a gate (D-07). Demonstrate this one failing, then
restore.

In the working tree only, revert the branch-A condition to the pre-fix form — presence of two `pha`
bytes and an `rts` anywhere in the window, ignoring `pairing` — and run
`cd src/mcp/vice && node --test anno-coverage.test.ts`. Record the exact failing test name or names,
the exit code, the pass/fail counts, and the reported values the assertion messages print for the FP3
payload. The expected planted-violation reading is the round-3 verification's own measurement:
`reachedAsInstruction=31`, `tableEntry=16`, `splitTables=1`, eight proven targets at
`$0840`…`$0847`, and `classAt($0840) === "reached-as-instruction"`. If the observed pre-fix numbers
differ from those, record the difference as a finding — do not adjust the fixture to reproduce the
remembered number.

Restore the tightened condition with `git checkout -- src/mcp/vice/anno-coverage.ts` or by
re-applying the edit, re-run the suite, and confirm it is green again before committing. The
demonstration must leave no trace in the committed source.

Append a round-4 section to
`.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md` — EXTEND, never
replace, and delete nothing that is already there. The new rows record: the gate under
demonstration, the planted violation applied, the command run, the observed red (test names, counts,
exit code), the observed numbers, the restoration, and the observed green. Also record the liveness
positive control from task 1 as executed evidence, because "the branch is still live" is the half of
this demonstration that a decline-everything tightening would fail.
  </action>
  <verify>
    <automated>cd src/mcp/vice && node --test anno-coverage.test.ts &amp;&amp; git -C /home/henrik/dev/henrik/git/c64-re-tools diff --quiet -- src/mcp/vice/anno-coverage.ts &amp;&amp; grep -c 'planted' .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The suite is green after restoration: `cd src/mcp/vice && node --test anno-coverage.test.ts`
      exits 0.
    - `git diff --numstat` on `19-VALIDATION.md` shows insertions and ZERO deletions.
    - `19-VALIDATION.md` carries a round-4 row naming the branch-A gate, the exact test name observed
      red under the planted violation, the observed pass/fail counts, and the observed FP3 numbers
      under the reverted predicate.
    - `19-VALIDATION.md` carries a row recording the liveness positive control as executed evidence,
      so "the gate declines everything" is ruled out by measurement rather than by argument.
    - No requirement checkbox in `.planning/REQUIREMENTS.md` is changed by this plan:
      `git diff --name-only` does not list `.planning/REQUIREMENTS.md`.
    - `git diff --name-only` does not list
      `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-REVIEW.md`.
  </acceptance_criteria>
  <done>
    The branch-A gate has been observed failing under a planted violation and green after
    restoration, with both observations and the liveness evidence recorded in `19-VALIDATION.md` as
    additions only.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.regen2000proj` file → `buildCoverageReport()` | An operator-supplied JSON document carrying a base64 payload. The only untrusted input in this plan's scope, and it is parsed by already-shipped code that this plan does not change. |
| coverage JSON → Phase-20 consumer | A downstream reader trusts `structural.reachedAsInstruction`, `dispatch.tableEntryAddresses` and `provenDispatchTargets()`. Nothing authenticates that trust; the numbers' honesty IS the control. |

There is no network surface, no authentication, no authorization, no session and no persistence in
this plan. Spoofing, Repudiation and Elevation of Privilege have no applicable component here, and
rows are not manufactured for them.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-15-01 | Tampering | `hasDispatchContext()` → `provenDispatchTargets()` → `computeStructuralCensus()` extraSeeds | high | mitigate | A payload — hostile or merely ordinary — manufactures "proven" entry points out of pointer data and inflates the number Phase 20 gates on. Mitigated by requiring a proven data-flow link from the two paired loads to the push idiom (task 1), plus the committed FP3 interior control asserted at report level and the planted-violation demonstration (tasks 1 and 3). |
| T-19-15-02 | Denial of service | the table-reconstruction walks | low | accept | Both walks are bounded by `MAX_TABLE_ENTRIES` and by the one derived `effectiveEnd`, and report their own `truncated` flag. Accepted as already bounded; this plan adds no unbounded loop. |
| T-19-15-03 | Information disclosure | `anno-coverage.ts` as a whole | low | accept | The module performs no filesystem write and imports no live session — a committed source-level assertion proves it, so a parallel run cannot corrupt a project and an interrupted run leaves nothing partial on disk. Accepted as mitigated by construction; that assertion must still pass after this plan. |
| T-19-15-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package and adds no dependency to either published package. The package-legitimacy gate does not engage; `RESEARCH.md` needs no audit table for a round with no install task. |
</threat_model>

<verification>
- `cd src/mcp/vice && node --test anno-coverage.test.ts` — exit 0, test count above 71.
- `cd src/mcp/vice && npx tsc --noEmit` — exit 0.
- `cd src/mcp/vice && node fixtures/coverage/make-coverage-fixtures.mjs` twice —
  `git status --porcelain src/mcp/vice/fixtures/coverage` empty.
- `node scripts/check-npm-packages.mjs` — exit 0; the two committed fixture directories must not
  leak into either tarball.
- `cd src/mcp/vice && node --test comment-phase-pointers.test.ts docs-dangling-refs.test.ts` —
  exit 0, so no new comment hands work to a numbered phase and no shipped literal names one.
</verification>

<success_criteria>
- The round-3 blocker payload is committed as a fixture pair and reports zero proven dispatch
  targets, an empty table-entry set, and a census equal to its declared fifteen code bytes.
- The class-3 route into `stack-return-push-idiom` returns true only on a proven push link, and is
  demonstrably still live.
- Both pre-existing positive controls still prove.
- The gate has been watched fail under a planted violation, recorded in `19-VALIDATION.md`.
- `COVERAGE_SCHEMA_VERSION` is still 2 and `COVERAGE_REPORT_KEYS` is unchanged.
</success_criteria>

<output>
Create `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-15-SUMMARY.md` when
done.
</output>
