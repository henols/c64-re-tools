---
phase: 19-absorbed-procedures-and-the-coverage-instrument
plan: 09
type: execute
wave: 3
depends_on: ["19-08", "19-07"]
gap_closure: true
files_modified:
  - src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs
  - src/mcp/vice/fixtures/coverage/README.md
  - src/mcp/vice/fixtures/coverage/fp1-indexed-copy-loop/project.regen2000proj
  - src/mcp/vice/fixtures/coverage/fp1-indexed-copy-loop/store.json
  - src/mcp/vice/fixtures/coverage/fp1b-immediate-copy-loop/project.regen2000proj
  - src/mcp/vice/fixtures/coverage/fp1b-immediate-copy-loop/store.json
  - src/mcp/vice/anno-coverage.test.ts
  - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md
autonomous: true
requirements: [COV-02, COV-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A committed false-positive control fixture exists for the structural census: a program of DECLARED code size containing an ordinary two-table indexed copy loop, plus its immediate-addressing twin, and the census reports the same `reachedAsInstruction` for both — neither exceeding the declared code size"
    - "The false-positive control is generated, not hand-committed: re-running `make-coverage-fixtures.mjs` twice leaves `git status --porcelain src/mcp/vice/fixtures/coverage` empty, and the two new payloads are asserted equal in length and differing only in their code prologue by the generator itself"
    - "The committed fixture count is a deliberate pin, not a stale number: the test asserting how many control fixtures exist, the README's own count and per-fixture table, and the generator's shared-program invariant all state the new total, and the README no longer claims every fixture carries the same program"
    - "`REQUIREMENTS.md` still records COV-02 as NOT complete and its status table row as `Gaps Found` at the end of this gap-closure run — the box is re-checked by re-verification once the rule actually holds, never by the plan that fixes it"
    - "`19-VALIDATION.md` is EXTENDED, not replaced: its executed-evidence table gains one row per gap-closure plan naming the command that ran, the file it ran against, and the observed result, and its existing 52 rows are byte-unchanged"
    - "The release hold has a named lifting condition and a recorded owner: this plan's SUMMARY restates, verbatim from 19-07's SUMMARY, the option selected at 19-07's checkpoint and the exact condition that lifts `[skip release]` — so the hold cannot silently become the permanent state, and no gap-closure commit publishes anything"
  prohibitions:
    - requirement_id: COV-02
      category: values
      statement: "MUST NOT ship a documentation claim that the same commit falsifies"
    - requirement_id: COV-02
      category: values
      statement: "MUST NOT let a control fixture's incidental text stand in for an enforced rule — a false-positive control whose data happens not to trigger the heuristic proves nothing, so the fixture must be shown to have triggered it before the gate landed"
  flagged_assumptions:
    # -------------------------------------------------------------------------
    # The nine edge-probe rows belonging to requirements this gap-closure run
    # does NOT reopen. Each is surfaced, not dropped, and each names the
    # already-passing verification evidence rather than inventing new work.
    # Accounting: 8 authored into must_haves.truths across 19-06/07/08 + 9 here
    # == 17 applicable rows. See 19-06-PLAN.md for the full arithmetic.
    # -------------------------------------------------------------------------
    - requirement_id: ABS-01
      category: unclassified
      status: unresolved
      assumption: "The edge probe could not classify ABS-01 ('five procedures absorbed at a pinned commit, tool calls diffed, no `.agent/skills/` runtime dependency'). Left unresolved deliberately — not auto-resolved with a backstop marker. Standing evidence: `19-VERIFICATION.md` truth 1 (✓ VERIFIED) confirmed all five upstream sha256 digests AND byte counts against the real repository over the network at the pin, spot-checked four manifest line-number citations as exact, and recorded `check-skill-tool-coverage.mjs` exit 0 over 17 curated `anno_*` names with `grep -rn '\\.agent/skills' src/skills/` returning 0 hits. This run does not reopen it."
    - requirement_id: ABS-03
      category: adjacency
      status: unresolved
      assumption: "'When two descriptions are exactly equal or just touch, do they merge, collide, or separate?' is answered by the shipped gate rather than by new work: `skill-descriptions.mjs`'s threshold is INCLUSIVE at 0.35 and the boundary case is already pinned by an inclusive-threshold boundary test. Standing evidence: `19-VERIFICATION.md` truth 3 (✓ VERIFIED), 21/21 pairs, observed max 0.250, allowlist size 0."
    - requirement_id: ABS-03
      category: empty
      status: unresolved
      assumption: "An empty or single-skill inventory: `skill-description-overlap.test.ts` already carries an emptied-corpus and stale-allowlist control (32/32 pass). Not reopened here."
    - requirement_id: ABS-03
      category: ordering
      status: unresolved
      assumption: "Stable output order for equal-scoring pairs is already pinned by a stable-ordering test in `skill-description-overlap.test.ts`. Not reopened here."
    - requirement_id: ABS-04
      category: unclassified
      status: unresolved
      assumption: "Carried forward verbatim from `19-VALIDATION.md`'s own flagged-assumption row: the reversal-condition count is STRUCTURAL and cannot distinguish a checkable condition from a well-formed but unfalsifiable one. Left unresolved / unclassified deliberately; not auto-resolved with a backstop marker. Standing evidence: `19-VERIFICATION.md` truth 5 and artifact row for `19-DECISIONS.md` (✓ VERIFIED, five dated decisions each with a named reversal or re-sync condition, the snapshot trade's mechanism being a hash comparison against `manifest.resync_triggers`)."
    - requirement_id: COV-01
      category: encoding
      status: unresolved
      assumption: "'Whose definition of length/equality applies' for the coverage measures is already fixed and tested: byte counts are over the half-open range `[origin, origin + size)`, comment equality is exact string equality after a normalisation whose four steps are pinned individually, and auto-name prefix matching is ASCII case-sensitive. Standing evidence: `19-VERIFICATION.md` truth 4 clauses A and B (✓ PASS) and the normalisation tests in `anno-coverage.test.ts`. Plan 19-08 adds a 16-bit upper bound to the range but does not change what equality means."
    - requirement_id: COV-01
      category: concurrency
      status: unresolved
      assumption: "'If interrupted or run in parallel, what is guaranteed?' is answered structurally rather than by a concurrency test: a coverage run is read-only over a project file BY CONSTRUCTION, asserted at source level by the committed test that the module contains no `writeFileSync`, `renameSync`, `appendFileSync`, `save_project` or live-session import. Two parallel runs therefore cannot corrupt a project and an interrupted run leaves nothing partial on disk. Standing evidence: `anno-coverage.test.ts` section 11 (✓ VERIFIED)."
    - requirement_id: SURF-03
      category: unclassified
      status: unresolved
      assumption: "The edge probe could not classify SURF-03 ('packer identity surfaced as a recon finding'). Left unresolved deliberately. Standing evidence: `19-VERIFICATION.md` truth 5 (✓ VERIFIED) — `PACKER_VERDICTS` frozen to exactly four, `identifiedByOracle()` structurally the only constructor that assigns `packer` and it throws on a non-string or empty name, both `spawnSync` sites passing an argument array with `shell: false`, wired into `c64-program-recon/SKILL.md:88-89`. Note: `19-REVIEW.md` WR-08 and WR-09 are open against this file and are dispositioned DEFERRED in 19-08's `<gap_closure_context>`; they do not reopen SURF-03's verification."
    - requirement_id: SURF-03
      category: empty
      status: unresolved
      assumption: "'What is the result for empty, single-element, or null input?' is already the requirement's own core property: `unknownFinding()` throws on an empty reason and `identifiedByOracle()` throws on an empty name, so an absent oracle produces a HARD UNKNOWN rather than a guess. Standing evidence: `packer-finding.test.mjs`, 17 tests, 16 pass, 1 visible skip (the live oracle gate)."
  artifacts:
    - path: "src/mcp/vice/fixtures/coverage/fp1-indexed-copy-loop/project.regen2000proj"
      provides: "The false-positive control payload: ordinary two-table indexed copy loop plus ordinary data"
    - path: "src/mcp/vice/fixtures/coverage/fp1b-immediate-copy-loop/project.regen2000proj"
      provides: "The immediate-addressing twin, identical but for the code prologue"
    - path: "src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs"
      provides: "Per-fixture program support plus the equal-length / prologue-only-difference invariants"
      min_lines: 260
    - path: "src/mcp/vice/fixtures/coverage/README.md"
      provides: "Corrected fixture count, the two new table rows, and a corrected shared-program claim"
    - path: ".planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md"
      provides: "Extended executed-evidence rows for the gap-closure run"
  key_links:
    - from: "src/mcp/vice/anno-coverage.test.ts"
      to: "src/mcp/vice/fixtures/coverage/fp1-indexed-copy-loop/store.json"
      via: "the census assertion reads the fixture's own declared code size rather than a number typed into the test"
      pattern: "fp1-indexed-copy-loop"
    - from: "src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs"
      to: "src/mcp/vice/anno-project.ts"
      via: "synthesizeProject() writes every fixture payload, so the format is the shipped writer's own"
      pattern: "synthesizeProject"
---

<objective>
Close the last of GAP 2 (`missing` item 3): commit the false-positive control fixture the census
never had — a program of known code size containing an ordinary two-table indexed copy loop —
and assert through the real report path that the census does not inflate. Then extend the phase's
validation record and verify that `REQUIREMENTS.md` still reads honestly.

Purpose: 19-08's gate is held down by hand-built payloads inside the test file. That is the right
place for a scan-level unit control, but the property the gap names is a REPORT-level one ("the
census does NOT inflate"), and the six committed fixtures are where report-level controls live in
this project. Committing the pair also makes the "differing ONLY in addressing mode" claim a
generated, checkable fact rather than a sentence in a test comment.

The second half of this plan is bookkeeping that the gap explicitly asks for and that is easy to
get wrong in the honest direction: `19-VALIDATION.md` must be EXTENDED, not replaced, and
`REQUIREMENTS.md`'s COV-02 box must be verified as still-unchecked rather than helpfully ticked
by the plan that does the fixing.

Output: two committed fixtures, a generator that produces them deterministically, a corrected
README, one new report-level control, extended validation rows, and a verified-honest
requirements file.
</objective>

<gap_closure_context>
Source: `19-VERIFICATION.md` gap 2, `missing` items 3 and 5.

Item 3 — "Add a false-positive control fixture: a program of known code size containing an
ordinary two-table indexed copy loop, asserting the census does NOT inflate" — is Tasks 1 and 2.

Item 5 — "Re-check `REQUIREMENTS.md` line 59 — COV-02 was marked `[x]` Complete on the strength
of a rule that does not hold" — is **ALREADY DONE** and this plan VERIFIES it rather than
redoing it. Line 59 currently reads `- [ ] **COV-02**: …` (unchecked) and the requirement-status
table rows 129-135 all read `Gaps Found`. Task 3 asserts that state still holds after the fixes
and does NOT tick the box: re-checking it is re-verification's job, once the rule actually holds.
Editing it here would reproduce the original defect in miniature — a status claim written by the
same commit that would need to justify it.

**Review items folded in here:** IN-02 (fixture regeneration is host-dependent despite the
stated determinism contract) — FOLDED IN, because this plan is the one that edits the generator
and adds two fixtures to it. Task 1 re-establishes the determinism contract by running the
generator twice and asserting a clean `git status`, which is the contract the generator's own
header states.

**Review items NOT folded in:** every remaining WR-* and IN-* item's disposition is recorded in
19-07 (WR-11, WR-12) and 19-08 (WR-03, WR-04, WR-05, WR-06, WR-07, WR-08, WR-09, WR-10, IN-01,
IN-03, IN-04). None is silently dropped.

**Do not touch** `19-01-PLAN.md` … `19-05-PLAN.md` or any `*-SUMMARY.md`.
</gap_closure_context>

<execution_notes>
**Worktree isolation must be OFF for this plan.** `files_modified` includes
`.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md`. Worktree
mode strips `.planning/` files from commits, so a plan whose deliverable includes a `.planning/`
document silently cannot deliver it under worktree isolation. Force isolation to `none` before
dispatching this plan, and do not re-query to "confirm" it — re-force it.

**Release discipline, and the act this plan OWNS.** No file in this plan's set ships
(`fixtures/` is refused by `assertLeanTarball()` in both tarballs, and test files never ship), so
no publish is triggered here. Carry `[skip release]` in the commit subject anyway, for
consistency with the rest of the gap-closure run.

19-07's checkpoint approves the licence WORDING only; it does not authorise a publish and does
not lift `[skip release]`. This gap-closure run cannot honestly authorise one, because the
artifact that would prove the gaps are closed is the Phase 19 re-verification that has not
happened yet. The release is therefore a SEPARATE, LATER ACT, and **this plan owns recording it**
— as the last plan to land, Task 3 must write down the named condition that lifts the hold,
taken verbatim from 19-07's SUMMARY, so an unnamed hold cannot become the permanent state by
default. Do NOT author a release-bearing commit here, and do NOT drop `[skip release]`.

**Full suite, not the subset.** Verify with `npm test`, never `npm run test:automated` — the
automated subset skips `MANUAL_ONLY_TESTS` and would hide a CI failure.
</execution_notes>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VERIFICATION.md
@.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the false-positive control pair, and make "differs only in addressing mode" a checked property</name>
  <reversibility rating="reversible">Two new generated fixture directories plus generator changes. Nothing ships (`fixtures/` is refused by both tarballs' lean check), no published contract is touched, and deleting the directories plus reverting the generator restores the prior state exactly.</reversibility>
  <files>src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs, src/mcp/vice/fixtures/coverage/fp1-indexed-copy-loop/project.regen2000proj, src/mcp/vice/fixtures/coverage/fp1-indexed-copy-loop/store.json, src/mcp/vice/fixtures/coverage/fp1b-immediate-copy-loop/project.regen2000proj, src/mcp/vice/fixtures/coverage/fp1b-immediate-copy-loop/store.json, src/mcp/vice/fixtures/coverage/README.md</files>
  <read_first>
    - `src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs` in full (247 lines) — the header's three stated contracts (generator-not-blobs, determinism, no test suffix in the filename), the module-level `PROGRAM` with its line-by-line disassembly comment, the `PROGRAM.length !== 0x40` invariant throw, `ADDR`, `CROSS_REFERENCES`, `HONEST_BLOCKS`, `USER_NAMES`, `AUTO_NAMES`, `LABEL_TYPES`, `symbols()`, the four comment sets, the `FIXTURES` array's per-row shape, and `writeFixture()`'s use of the module-level `PROGRAM` and `ORIGIN`
    - `src/mcp/vice/fixtures/coverage/README.md` in full — the "Six committed synthetic fixtures. **Five must FAIL and one must PASS.**" opening, the per-fixture table, the `## Shape of each fixture` table (whose `project.regen2000proj` row claims "Identical across all six — only the store differs"), and the `## The one shared program` section. Three of these state a count or a sameness claim that this task falsifies if it is not updated
    - `src/mcp/vice/anno-project.ts` — `synthesizeProject(bytes, { origin })` at line 135, so you know exactly what the generator's one writer call produces
    - `src/mcp/vice/anno-coverage.test.ts` lines 95-130 and 535-560 — `loadFixture()`, `reportFor()`, the `"six control fixtures are committed"` test with its `assert.equal(dirs.length, 6, …)`, and the data-driven `for (const dir of fixtureDirs())` loop that reads `expect_clean` and `expect_measure` from every store. A new directory is pulled into that loop automatically, so its store must declare both fields
    - `src/mcp/vice/anno-coverage.test.ts` as 19-08 left it — the `ORDINARY_INDEXED_COPY` and `ORDINARY_IMMEDIATE_COPY` payloads and the code-size constant, so the committed fixtures use the same bytes as the unit control rather than a second, drifting definition
    - `19-REVIEW.md` IN-02 — the host-dependence concern in fixture regeneration, which Task 1's determinism check addresses
  </read_first>
  <action>
Extend `make-coverage-fixtures.mjs` to support a per-fixture program while keeping the shared
control program's guarantee intact.

Add two payload constants beside the existing `PROGRAM`, each with the same line-by-line
disassembly comment style, each exactly 64 bytes at `ORIGIN`:
  - `INDEXED_COPY_LOOP` — a prologue of two indexed loads through the SAME index register
    followed by `rts`, then 57 bytes of ordinary data (an ascending run, then a run of a single
    plausible colour byte, then a run of spaces). No indirect-jump opcode, no `pha`/`pha`/`rts`,
    no zero-page vector construction anywhere in it: the whole point is that nothing in this
    program dispatches through anything, and yet the pre-19-08 scan reconstructed 8 in-image
    targets out of it.
  - `IMMEDIATE_COPY_LOOP` — the same 64 bytes except that the two loads are immediate. The
    prologue must be the same LENGTH as the indexed one, so the two programs are byte-identical
    from the end of the prologue onward.

Declare `FP_CODE_SIZE` as the prologue length, once, and derive it rather than typing it twice.
Add generator-level invariant throws in the same style as the existing `PROGRAM.length !== 0x40`
check: both new payloads are exactly 64 bytes; both prologues are exactly `FP_CODE_SIZE` bytes;
and the two payloads are byte-identical from offset `FP_CODE_SIZE` onward. That last throw is
what makes "differing ONLY in addressing mode" a checked property of the generator rather than a
claim in a comment.

Give each `FIXTURES` row an optional `program` field and have `writeFixture()` use
`fixture.program ?? PROGRAM`. Also write the fixture's own `code_size` into its `store.json`
alongside the existing `size`, so the report-level assertion in Task 2 reads the declared code
size from the fixture instead of a number typed into the test.

Add the two rows:
  - `fp1-indexed-copy-loop`, control `FP1`, `program: INDEXED_COPY_LOOP`. Its `purpose` must
    state plainly that this is a CENSUS false-positive control, not a findings control: the
    property under test is that `structural.reachedAsInstruction` does not exceed `code_size`,
    and its `expect_clean` verdict is incidental. `expect_clean: false`, `expect_measure: null`
    — it has no symbols and no comments, so `labels` and `commentVacuity` will legitimately
    report unavailable, and pinning a measure here would assert something the fixture is not for.
  - `fp1b-immediate-copy-loop`, control `FP1b`, `program: IMMEDIATE_COPY_LOOP`, same shape, whose
    `purpose` states that it is the twin whose only difference is the addressing mode, and that
    it exists so the indexed fixture's census can be compared against a baseline rather than
    against a remembered number.

Give both an empty `symbols`, empty `comments`, empty `blocks` and empty `cross_references`
array — anything else would be a second variable. Keep every existing fixture row and the
existing `CROSS_REFERENCES` / `HONEST_BLOCKS` / comment sets byte-unchanged.

Update the generator's header: the "WHY A GENERATOR RATHER THAN SIX HAND-COMMITTED BLOBS"
paragraph now describes five fixtures sharing one program plus a false-positive PAIR with its
own two, and must say why the pair is generated together (their only-difference-is-addressing
relationship is the fixture, and hand-committing them would let it drift silently).

Update `README.md`: the opening count, the per-fixture table (two new rows naming what each is a
control for and which property must hold), the `## Shape of each fixture` table's
`project.regen2000proj` row (it is no longer identical across every fixture), and the
`## The one shared program` section (retitle and scope it to the five that share it, and add a
short section describing the false-positive pair and its 57 bytes of ordinary data). Every count
and sameness claim in that file must be true of this commit — the README ships nowhere, but the
prohibition this plan carries is about claims, not about tarballs.

Regenerate: `cd src/mcp/vice && node fixtures/coverage/make-coverage-fixtures.mjs`. Then run it a
SECOND time and confirm `git status --porcelain src/mcp/vice/fixtures/coverage` is empty, which is
the generator's own stated determinism contract and IN-02's concern.
  </action>
  <verify>
    <automated>cd src/mcp/vice &amp;&amp; node fixtures/coverage/make-coverage-fixtures.mjs &amp;&amp; node fixtures/coverage/make-coverage-fixtures.mjs &amp;&amp; test -z "$(git status --porcelain fixtures/coverage)" &amp;&amp; echo DETERMINISTIC</automated>
    <automated>test "$(ls -d src/mcp/vice/fixtures/coverage/*/ | wc -l)" = "8"</automated>
  </verify>
  <acceptance_criteria>
    - `ls -d src/mcp/vice/fixtures/coverage/*/ | wc -l` returns 8.
    - The generator prints a line naming 8 written fixtures, and running it twice leaves `git status --porcelain src/mcp/vice/fixtures/coverage` empty.
    - Deleting one byte from `IMMEDIATE_COPY_LOOP` makes the generator throw with a message naming the equal-length invariant; changing one byte in the DATA region of only one of the two payloads makes it throw naming the identical-tail invariant. Demonstrate both and restore. Record the observed messages in the SUMMARY.
    - `fp1-indexed-copy-loop/store.json` and `fp1b-immediate-copy-loop/store.json` each declare `control`, `purpose`, `origin`, `size`, `code_size`, `expect_clean`, `expect_measure`, and empty `symbols` / `comments` / `blocks` / `cross_references`.
    - `README.md` contains no claim that every fixture carries the same program, and its stated fixture count matches the directory count. Check by reading it, not by grep — a count can be right in one sentence and stale in another.
    - `node scripts/check-npm-packages.mjs` exits 0 and neither tarball packs anything under `fixtures/` (`assertLeanTarball()` already enforces this; confirm it is still exiting 0 after two directories were added).
  </acceptance_criteria>
  <done>Two generated, deterministic false-positive control fixtures are committed; their only-difference-is-addressing-mode relationship is enforced by the generator; and every count and sameness claim in the README and the generator header is true of this commit.</done>
</task>

<task type="auto">
  <name>Task 2: Assert at report level that the census does not inflate, and re-pin the fixture count</name>
  <reversibility rating="reversible">Test-only changes plus a deliberate edit to one pinned count.</reversibility>
  <files>src/mcp/vice/anno-coverage.test.ts</files>
  <read_first>
    - `src/mcp/vice/anno-coverage.test.ts` lines 535-600 — the `"six control fixtures are committed"` test, the data-driven `for (const dir of fixtureDirs())` control loop, the `NON-VACUITY` test, the `NC1b earns its place` test and the `NC3 earns its place` test. The last two are the pattern to follow: a named test explaining why a specific fixture earns its place in the set
    - `src/mcp/vice/anno-coverage.test.ts` lines 88-100 — the `FixtureStore` interface, which needs the new `code_size` field
    - `src/mcp/vice/anno-coverage.test.ts` as 19-08 left it — the `ORDINARY_INDEXED_COPY` / `ORDINARY_IMMEDIATE_COPY` unit controls, so the new report-level test complements them rather than duplicating them (unit: the scan declines; report: the census does not inflate)
    - `src/mcp/vice/fixtures/coverage/README.md` and the two new `store.json` files as Task 1 left them
  </read_first>
  <action>
Add `code_size` to the `FixtureStore` interface as an optional number, and update the
`"six control fixtures are committed"` test: change its expected count to 8, rename it so the
name does not carry a stale number, and extend its per-directory loop to require that any store
declaring `code_size` also declares a non-empty `purpose`.

Add one report-level test, in the controls section, named for the property rather than the
mechanism — that a false-positive control fixture exists for the census and that the census
declines to inflate on it. Assert, reading both fixtures through `reportFor()`:
  - `fp1-indexed-copy-loop`'s `structural.reachedAsInstruction` equals
    `fp1b-immediate-copy-loop`'s;
  - neither exceeds the `code_size` its own store declares;
  - the ADVISORY SPLIT-TABLE FIELD — whatever 19-08's checkpoint named it, and read by role
    rather than by a spelling typed here — carries exactly one entry for
    `fp1-indexed-copy-loop` and none for `fp1b-immediate-copy-loop`, which is the one structural
    difference between the two, stated explicitly so the pair is not merely two identical
    numbers. If 19-08's checkpoint selected `gate-only-no-shape-change`, no advisory field
    exists: assert instead that the pairing appears in NO reported target list for either
    fixture, and say so in the SUMMARY;
  - THE PROVEN-TARGET SEAM (again by role, under whatever name 19-08 gave it) contributes
    nothing for either fixture.
Put the pre-19-08 numbers in the assertion message (the indexed fixture reached 55 of 64 bytes
against 7 bytes of real code) so a future reader sees what this control defends against and can
tell a regression from a rewrite.

Add a second test in the `earns its place` idiom explaining why FP1b is committed rather than
inferred: without a twin, the indexed fixture's census number could only be compared against a
remembered value, and a remembered value is exactly what the whole gap-closure run exists to
replace.

Do not change any existing control's expectations, and do not touch the NC1-NC5 stores.
  </action>
  <verify>
    <automated>cd src/mcp/vice &amp;&amp; node --test anno-coverage.test.ts 2>&amp;1 | tail -20</automated>
    <automated>cd src/mcp/vice &amp;&amp; npm test 2>&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && node --test anno-coverage.test.ts` exits 0 with 0 failures, and the fixture-count test asserts 8.
    - The new report-level control has been observed to FAIL against the pre-19-08 instrument. Demonstrate by stashing only `anno-coverage.ts`, running the single test, and recording the observed `reachedAsInstruction` values for both fixtures (expect 55 and 7). A false-positive control that has never been red does not discharge the gap.
    - The advisory/proven assertions are read from 19-08's ACTUAL implemented names, not from a spelling assumed here: the SUMMARY states which identifiers were used for the advisory field and for the proven-target seam, and which of 19-08's checkpoint options produced them. If the option was `gate-only-no-shape-change`, the SUMMARY states that the advisory assertion was replaced by an absent-from-every-target-list assertion.
    - The data-driven control loop runs for 8 directories, and `nc5-well-documented` is still the only fixture with `expect_clean: true`.
    - `cd src/mcp/vice && npm test` — the FULL suite — exits 0.
    - `npx tsc --noEmit -p tsconfig.json` exits 0.
    - No existing NC1-NC5 store file appears in `git status --porcelain`.
  </acceptance_criteria>
  <done>The committed false-positive control pair is exercised through the real report path, the census is asserted not to exceed each fixture's declared code size, and the control was seen red before the gate landed.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the validation record, and verify REQUIREMENTS.md still reads honestly</name>
  <reversibility rating="reversible">One appended table section in a planning document, plus a read-only verification that produces no edit.</reversibility>
  <files>.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md` in full — its frontmatter (`status: complete`, `closed_by: 19-05`, `nyquist_compliant: true`), the "Every row below names a command that **actually ran**" preamble, the 52-row `## Requirement → executed evidence` table's exact column set, the flagged-assumption table near the end, and the `## Nyquist compliance` closing paragraph and its stated basis (every requirement sampled by an executed command AND a planted-violation demonstration)
    - `.planning/REQUIREMENTS.md` lines 55-62 and lines 120-140 — COV-02's checkbox line and its closing note, and the requirement-status table rows for the seven Phase 19 IDs
    - `19-VERIFICATION.md` gap 2 `missing` item 5, verbatim
    - the three gap-closure SUMMARY files (19-06, 19-07, 19-08) as they now exist — every row you add must name a command that actually ran and a result that was actually observed, taken from those SUMMARYs rather than from these plans' expectations
  </read_first>
  <action>
Extend `19-VALIDATION.md`. Do NOT rewrite it and do NOT touch its existing 52 rows.

Add a new section after the existing evidence table, headed to name the gap-closure run and
dated, using the SAME five-column shape as the existing table (Requirement, Plan(s), Command
that ran, Ran against, Observed result). Add one row per gap-closure plan's verified work, at
minimum:
  - ABS-02 / 19-07: the sha256 verification of the fetched upstream `LICENSE-MIT` and the
    extracted-block digests from all three notices files, with the observed digest.
  - ABS-02 / 19-07: the `skill-attribution.test.ts` run, with its test count, plus the observed
    failure message from the one-word-alteration planted violation.
  - COV-02 / 19-06: the report-level anchoring control, with the observed pre-fix and post-fix
    `multiCallerUndocumented` values and the pre-fix `clean` verdict.
  - COV-02 / 19-08: the immediate-versus-indexed census pair, with the observed pre-fix and
    post-fix `reachedAsInstruction` values.
  - COV-01 / 19-08: the live CLI run against a fixture after the schema bump, confirming three
    named measures and no combined figure.
  - COV-02 / 19-09: the fixture determinism check and the report-level false-positive control,
    with the observed values.

Update the frontmatter minimally and honestly: bump `closed_by` to name the last gap-closure
plan, add a `reopened` or `extended` date field recording that this file was extended by the gap
closure, and leave `nyquist_compliant` as it stands ONLY if every new row carries both an
executed command and a planted-violation demonstration — which is the basis the closing paragraph
states. If any new row lacks a demonstrated failure, say so in the closing paragraph rather than
leaving the flag unexamined.

Extend the flagged-assumption table with the nine rows recorded in this plan's frontmatter
`flagged_assumptions`, each naming the already-passing verification evidence, so the file carries
the same accounting the plans do.

Then record the RELEASE HOLD, which this plan owns. Read 19-07's SUMMARY, and copy into THIS
plan's SUMMARY, verbatim: which option was selected at 19-07's checkpoint, and the exact named
condition that lifts `[skip release]` (under the recommended option that is "Phase 19
re-verification returns no gaps"). State plainly that no gap-closure commit published anything
and that lifting the hold is a separate later act. Do NOT author a release-bearing commit, and do
NOT drop `[skip release]` from this plan's commit subject. If 19-07's SUMMARY records no named
condition, STOP and report it — an approved-but-unnamed hold is exactly the governance defect
the option text warns about.

Then VERIFY — and do not edit — `.planning/REQUIREMENTS.md`:
  - COV-02's checkbox line is still unchecked;
  - all seven Phase 19 rows in the requirement-status table still read `Gaps Found`.
Record both observations, with the line numbers you observed them at, in the SUMMARY. If either
has drifted to a completed state, STOP and report it: a requirement marked complete by the run
that fixed it is the original defect in miniature, and re-verification is what is entitled to
tick it.
  </action>
  <verify>
    <automated>grep -c '^- \[ \] \*\*COV-02\*\*' .planning/REQUIREMENTS.md</automated>
    <automated>test "$(grep -cE '^\| (SURF-03|ABS-0[1-4]|COV-0[12]) \| Phase 19 \| Gaps Found \|$' .planning/REQUIREMENTS.md)" = "7" &amp;&amp; echo SEVEN-ROWS-STILL-GAPS-FOUND</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[ \] \*\*COV-02\*\*' .planning/REQUIREMENTS.md` returns 1 — the box is still unchecked.
    - All seven Phase 19 rows in `REQUIREMENTS.md`'s requirement-status table still read `Gaps Found`; the second verify command prints `SEVEN-ROWS-STILL-GAPS-FOUND`.
    - `git status --porcelain .planning/REQUIREMENTS.md` is empty — this task made no edit to it.
    - `19-VALIDATION.md`'s existing 52 evidence rows are byte-unchanged: `git diff -- .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md` shows only additions and the minimal frontmatter change, with no deletion inside the original table.
    - Every new evidence row names a command that appears verbatim in one of the 19-06 / 19-07 / 19-08 SUMMARY files, and an observed result taken from it — not a result predicted by a plan.
    - The nine flagged assumptions from this plan's frontmatter appear in the validation file's flagged-assumption table.
    - The SUMMARY carries the release-hold record: the option selected at 19-07's checkpoint, quoted verbatim from 19-07's SUMMARY, and the exact named condition that lifts `[skip release]`. `git log --oneline` for every gap-closure commit (19-06 through 19-09) shows `[skip release]` in each subject — verify and record the count.
  </acceptance_criteria>
  <done>The phase's validation record is extended with executed evidence from the gap-closure run and carries the nine flagged assumptions, `REQUIREMENTS.md` has been verified to still record COV-02 as incomplete without this plan having touched it, and the release hold has a named lifting condition recorded against a named owner rather than an open-ended default.</done>
</task>

</tasks>

<threat_model>
ASVS level 1. Blocking threshold: `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated fixture payloads → the committed tree | The fixtures are synthetic and authored here, so the payload bytes are trusted; the boundary that matters is the one between a fixture and the claim made about it. A fixture whose properties are asserted rather than enforced is the reproduced defect class of this whole gap-closure run. |
| planning documents → future verification | `19-VALIDATION.md` and `REQUIREMENTS.md` are read by re-verification and by the next milestone's audit. A row that names a command nobody ran, or a box ticked by the run that fixed the thing, poisons every later judgement built on it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19G-09-01 | Repudiation | a false-positive fixture that passes by construction rather than by the gate declining | high | mitigate | Task 2's acceptance criteria require the control to have been observed RED against the pre-19-08 instrument, with the observed `reachedAsInstruction` values (55 and 7) recorded. Task 1's generator throws enforce the equal-length and identical-tail invariants, so the pair's stated relationship cannot drift into a fixture that never triggered the heuristic. |
| T-19G-09-02 | Tampering | `19-VALIDATION.md` rewritten rather than extended | high | mitigate | An acceptance criterion requires `git diff` on that file to show no deletion inside the original 52-row table, and every new row's command to appear verbatim in a gap-closure SUMMARY. |
| T-19G-09-03 | Spoofing | a requirement marked complete by the run that fixed it | high | mitigate | Task 3 is read-only over `REQUIREMENTS.md`; `git status --porcelain` on that path must be empty, and the task halts and reports if the state has drifted to complete. |
| T-19G-09-04 | Tampering | `.planning/` deliverables silently stripped by worktree isolation | medium | mitigate | `<execution_notes>` requires isolation forced to `none` before dispatch, and re-forced rather than re-queried. |
| T-19G-09-05 | Information disclosure | fixture payloads leaking into a published tarball | low | accept | `assertLeanTarball()` in `scripts/check-npm-packages.mjs` already refuses any packed file under `fixtures/` in both tarballs, and its continued exit 0 is an acceptance criterion. The payloads are synthetic in any case — no copyrighted image and no user data, per the milestone's proving-ground bar. |
| T-19G-09-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs nothing and adds no dependency; `package-lock.json` is not in `files_modified`. No `## Package Legitimacy Audit` row is required. |
</threat_model>

<artifacts_this_phase_produces>
Symbols and files this plan CREATES (newly-created names — excluded from drift verification
against the pre-existing tree):

**New fixture directories and files:**
- `src/mcp/vice/fixtures/coverage/fp1-indexed-copy-loop/project.regen2000proj`
- `src/mcp/vice/fixtures/coverage/fp1-indexed-copy-loop/store.json`
- `src/mcp/vice/fixtures/coverage/fp1b-immediate-copy-loop/project.regen2000proj`
- `src/mcp/vice/fixtures/coverage/fp1b-immediate-copy-loop/store.json`

**New constants** in `src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs`:
- `INDEXED_COPY_LOOP`
- `IMMEDIATE_COPY_LOOP`
- `FP_CODE_SIZE`

**New store field** (in the two new fixtures only): `code_size`.

**New optional field** on the generator's `FIXTURES` rows: `program`.

**New interface field** in `src/mcp/vice/anno-coverage.test.ts`: `FixtureStore.code_size?: number`.

**New tests** in `src/mcp/vice/anno-coverage.test.ts` (names may be refined; the property each
asserts may not):
- a report-level false-positive control asserting the census does not inflate on the indexed
  fixture relative to its immediate twin
- an `earns its place` test for `fp1b-immediate-copy-loop`
- plus a deliberate edit to the committed-fixture-count test (6 → 8, renamed to carry no stale
  number)

**New section** in `19-VALIDATION.md`: a dated gap-closure evidence table plus nine appended
flagged-assumption rows.

**No new exported runtime symbol. No shipped file. No schema change.**
</artifacts_this_phase_produces>

<verification>
1. `ls -d src/mcp/vice/fixtures/coverage/*/ | wc -l` returns 8.
2. `cd src/mcp/vice && node fixtures/coverage/make-coverage-fixtures.mjs` twice, then
   `git status --porcelain src/mcp/vice/fixtures/coverage` is empty.
3. `cd src/mcp/vice && node --test anno-coverage.test.ts` — exits 0, 0 failures.
4. `cd src/mcp/vice && npm test` — the FULL suite. 0 failures.
5. `cd src/mcp/vice && npx tsc --noEmit -p tsconfig.json` — exits 0.
6. `node scripts/check-npm-packages.mjs`, `node scripts/check-skill-tool-coverage.mjs`,
   `node scripts/check-skill-description-overlap.mjs` — all exit 0.
7. `grep -c '^- \[ \] \*\*COV-02\*\*' .planning/REQUIREMENTS.md` returns 1, and
   `git status --porcelain .planning/REQUIREMENTS.md` is empty.
8. `git diff -- .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-VALIDATION.md`
   shows additions only inside the original evidence table's line range.
9. The two generator-invariant planted violations, and the report-level control's pre-fix red
   run, are recorded in the SUMMARY with their observed messages and values.
10. Every gap-closure commit subject (19-06 … 19-09) contains `[skip release]`, and the SUMMARY
    records the option selected at 19-07's checkpoint plus the exact named condition that lifts
    the hold — verbatim from 19-07's SUMMARY.
</verification>

<success_criteria>
- A committed, generated, deterministic false-positive control pair exists for the structural
  census, and the census is asserted not to exceed each fixture's declared code size.
- The pair's "differs only in addressing mode" relationship is enforced by the generator, not
  claimed in a comment.
- Every fixture count and sameness claim in the README and the generator header is true of this
  commit.
- The phase's validation record is extended with executed evidence and the nine flagged
  assumptions, with no original row deleted.
- `REQUIREMENTS.md` still records COV-02 as incomplete, verified without being edited.
- Nothing published: every gap-closure commit carries `[skip release]`, and the release hold's
  named lifting condition and owner are written down rather than left as an open-ended default.
</success_criteria>

<output>
Create `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/19-09-SUMMARY.md`
when done. Record: the two generator-invariant planted-violation messages, the determinism check
result, the report-level control's observed pre-fix and post-fix values, the observed
`REQUIREMENTS.md` line numbers and states, and confirmation that no original `19-VALIDATION.md`
row was deleted. Also record: which identifiers 19-08's checkpoint produced for the advisory
field and the proven-target seam (and therefore what this plan actually asserted against), and
the release-hold record — the option selected at 19-07's checkpoint and the exact named condition
that lifts `[skip release]`, quoted verbatim from 19-07's SUMMARY.
</output>
