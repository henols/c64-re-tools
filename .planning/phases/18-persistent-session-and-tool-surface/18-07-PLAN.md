---
phase: 18-persistent-session-and-tool-surface
plan: 07
type: execute
wave: 5
depends_on: ["18-05", "18-06"]
files_modified:
  - src/mcp/vice/r2000-session.test.ts
  - .planning/phases/18-persistent-session-and-tool-surface/18-PHASE-GATE-EVIDENCE.md
  - .planning/phases/18-persistent-session-and-tool-surface/18-VALIDATION.md
autonomous: false
requirements: [SESS-01, SESS-02, SESS-03, SESS-04, SURF-01, SURF-02]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Handing the session-open path a project file whose use_illegal_opcodes is false yields a project whose setting reads true immediately after open (D18-35, first half)."
    - "A subsequent mutating call's own internal save does NOT revert the forced setting — the half that actually tests the before-spawn-only forcing rule rather than just the edit (D18-35, second half)."
    - "Removing the forcing call from the session-open path makes the round trip go RED — proven, not assumed."
    - "The full npm test suite is green, including every live-gated test, with regenerator2000 present so no live scenario silently skips."
    - "Both published tarballs still validate after package.json's files[] gained r2000-session.ts: no node_modules, no test files, no fixtures leaked, skills present."
    - "No regenerator2000 --mcp-server-stdio process survives the suite."
    - "Every one of this phase's six requirement ids has at least one green automated test naming it, recorded in one place."
  artifacts:
    - src/mcp/vice/r2000-session.test.ts
    - .planning/phases/18-persistent-session-and-tool-surface/18-PHASE-GATE-EVIDENCE.md
  key_links:
    - "ensureProjectSettings() (plan 18-02) <-> the session-open path's call site (plan 18-03) <-> this plan's no-revert round trip, which is the only test that exercises all three together."
    - "package.json files[]'s r2000-session.ts entry (plan 18-03) <-> scripts/check-npm-packages.mjs's tarball validation, which has not run against the new entry until this plan."
  prohibitions:
    - "A live-gated test must never be recorded green on the strength of a SKIP: a skip is acceptable only when regenerator2000 is genuinely absent, never as a substitute for running it when the binary is present."
    - "The phase gate must never be run as the automated test subset: that subset skips the manual-only suites and has hidden real CI failures in this repository before."
---

<objective>
Close the phase: prove the half of D18-35 that only a live session can prove —
that a mutating call's own internal save does **not** revert the forced
`use_illegal_opcodes` setting — and then run the phase gate for real, against a
tree where every part of this phase is landed.

Purpose: the forcing edit and the no-revert property are two different claims.
Plan 18-02 proved the edit against a pure function. Only now, with the session
holding the file across many calls, can the property Pitfall 9 item 6 actually
names be tested: a live child that has the project loaded, saving over it, must
not silently undo the correction made before it started.

Output: the D18-35 round trip as a committed test, and one evidence document
recording the phase gate's real observed output — the full suite, the coverage
script, the typecheck, both tarball validations, and the orphan-process check.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-persistent-session-and-tool-surface/18-CONTEXT.md
@.planning/phases/18-persistent-session-and-tool-surface/18-VALIDATION.md
@.planning/research/PITFALLS.md
@.planning/phases/18-persistent-session-and-tool-surface/18-02-SUMMARY.md
@.planning/phases/18-persistent-session-and-tool-surface/18-03-SUMMARY.md
@.planning/phases/18-persistent-session-and-tool-surface/18-04-SUMMARY.md
@.planning/phases/18-persistent-session-and-tool-surface/18-05-SUMMARY.md
@.planning/phases/18-persistent-session-and-tool-surface/18-06-SUMMARY.md
@src/mcp/vice/r2000-session.ts
@src/mcp/vice/r2000-project.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: The settings round trip through a live session, including the no-revert half (D18-35)</name>
  <files>src/mcp/vice/r2000-session.test.ts</files>
  <precondition>`regenerator2000 --version` reports 0.9.20 on PATH. This test's whole subject is what a real child does to the project file it has loaded; a stub cannot answer it, so a SKIP here means the claim is unproven, not satisfied.</precondition>
  <read_first>
    - `src/mcp/vice/r2000-project.ts` — `ensureProjectSettings()` and `synthesizeProject()` as landed by plan 18-02, and the forced-values convention they share.
    - `src/mcp/vice/r2000-session.ts` — the session-open path and the exact position of the `ensureProjectSettings()` call relative to `openR2000Session()`.
    - `src/mcp/vice/r2000-session.test.ts` — the whole file as landed by plans 18-03, 18-04 and 18-06: the D-11 gate block, the reset discipline, and the existing live tests' temp-project setup.
    - `.planning/research/PITFALLS.md` § "Pitfall 9" item 6 and § "Pitfall 6" — the external-edit-then-silent-revert hazard, and why `use_illegal_opcodes` is load-bearing for illegal-opcode-correct reassembly.
    - `.planning/phases/18-persistent-session-and-tool-surface/18-CONTEXT.md` — D18-32, D18-34, D18-35.
  </read_first>
  <action>
    Add a named block to `r2000-session.test.ts` implementing D18-35's round trip against the real
    binary, gated through `r2000-test-gate.ts` like every other live scenario in the file.

    Set up: synthesise a temp project from the committed `probe-illegal.prg` fixture (the same
    fixture the other live tests use — it is the only `.prg` committed anywhere in this repo and it
    deliberately contains an illegal opcode), then edit its JSON directly on disk to set
    `settings.use_illegal_opcodes` to `false`, simulating the older project D18-34 describes.

    **First half — the forcing.** Issue one `r2000_*` call through `runR2000Tool()` so the session
    opens against that project. Read the `.regen2000proj` back from disk and assert the setting now
    reads `true`.

    **Second half — the no-revert.** With that same session still live and holding the file, issue
    a mutating call (a label write) so the session performs its own internal
    `r2000_save_project` over the project. Read the file from disk again and assert the setting is
    STILL `true`. This is the half that tests the before-spawn-only forcing rule rather than the
    edit: a child that loaded the file before the correction, or one whose in-memory state carries
    the old value, would write `false` back here.

    Add a third assertion that the illegal opcode actually decodes: after the round trip, call
    `r2000_disassemble` at the fixture's own illegal-opcode address and assert the result is not
    the opaque byte-directive fallback. This is what makes the setting load-bearing rather than a
    JSON field nobody reads; without it the test proves a boolean survived, not that anything
    depended on it.

    **The planted violation.** Temporarily remove the `ensureProjectSettings()` call from
    `r2000-session.ts`'s open path, run this block, observe the first-half assertion go RED,
    restore, observe green. Transcribe the red assertion message in the SUMMARY and confirm
    `git status --porcelain src/mcp/vice/r2000-session.ts` is clean afterwards. Do not commit any
    planted state.

    Also add one negative-path live test: a project whose `settings.system` is some other machine
    string causes the session open to refuse with `R2000ProjectSettingsError`, no child is spawned
    (`__r2000SessionStateForTest().openCount` unchanged), and the project file's sha256 is
    unchanged.
  </action>
  <verify>
    <automated>cd src/mcp/vice && node --test r2000-session.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && node --test r2000-session.test.ts` exits 0 with the D18-35 block RUN (not skipped) on this host.
    - Both halves assert against the file read from disk with `readFileSync`/`JSON.parse`, never against a value returned by the session.
    - The illegal-opcode assertion exists and passes: the disassembly at the fixture's illegal-opcode address is not the opaque byte-directive fallback.
    - The system-mismatch test asserts `R2000ProjectSettingsError` by class identity, asserts `openCount` did not change, and compares two `createHash("sha256")` digests of the project file for equality.
    - The planted-violation probe produced a RED first-half assertion; its message is transcribed in the SUMMARY and the working tree is clean afterwards.
    - `cd src/mcp/vice && npm test` exits 0 (full suite).
  </acceptance_criteria>
  <done>A project re-opened with the setting off comes back corrected, stays corrected across the session's own saves, and the correction demonstrably changes what decodes.</done>
</task>

<task type="auto">
  <name>Task 2: The phase gate — run it for real and record what was observed</name>
  <files>.planning/phases/18-persistent-session-and-tool-surface/18-PHASE-GATE-EVIDENCE.md, .planning/phases/18-persistent-session-and-tool-surface/18-VALIDATION.md</files>
  <precondition>Plans 18-01 through 18-06 are all merged into the working tree. This gate is meaningless against a partial tree; confirm every plan's SUMMARY exists before running it.</precondition>
  <read_first>
    - `.planning/phases/18-persistent-session-and-tool-surface/18-VALIDATION.md` — the Test Infrastructure table, the Sampling Rate section's full-suite discipline note, the Per-Task Verification Map's six requirement rows, and the Validation Sign-Off checklist.
    - `.planning/phases/18-persistent-session-and-tool-surface/18-01-SUMMARY.md` through `18-06-SUMMARY.md` — every prior plan's recorded outcome, including the stdin-EOF measurement's verdict and the lost-update non-vacuity oracle, both of which this document reports.
    - `scripts/check-npm-packages.mjs` — what it validates about each tarball, since `package.json`'s `files[]` gained an entry in plan 18-03 and this is the first time the packaged file set is checked against it.
    - `.github/workflows/ci.yml` around lines 165-190 — the exact commands CI runs, so this gate runs the same ones rather than a local approximation.
  </read_first>
  <action>
    Run the gate, in this order, from the repository root, and capture each command's real output:

    1. `cd src/mcp/vice && npx tsc --noEmit -p tsconfig.json`
    2. `cd src/mcp/vice && npm test` — the FULL suite. Do not substitute `npm run test:automated`:
       it skips the manual-only suites and has hidden genuine CI failures in this repository before.
    3. `node scripts/check-skill-tool-coverage.mjs`
    4. `node scripts/check-npm-packages.mjs`
    5. `bash scripts/package.sh`
    6. `pgrep -f 'regenerator2000 --mcp-server-stdio'` — expected to find nothing.
    7. `git status --porcelain` — expected clean apart from this plan's own new files.

    Then write `.planning/phases/18-persistent-session-and-tool-surface/18-PHASE-GATE-EVIDENCE.md`
    containing:

    - the date, the host's `regenerator2000 --version` output, and the `node --version` output;
    - one row per command above with its exit status and the load-bearing lines of its output
      (test counts for the suite: total, passed, failed, skipped);
    - **a skipped-test census**: list every test the full suite reported as skipped, with its
      reason. For each one, state whether the skip is legitimate (a genuinely absent external
      dependency) or a live scenario that should have run on this host. A live-gated r2000 test
      appearing in this list on a host where the binary is present is a FAILURE of the gate, not a
      footnote — fix it rather than recording it;
    - **a requirement-to-evidence table** with one row per phase requirement id (SESS-01, SESS-02,
      SESS-03, SESS-04, SURF-01, SURF-02), naming for each the test file and the specific test
      title that proves it, and the plan that landed it. Every row must name a test that RAN;
    - **a planted-violation census**: every guard this phase landed that was proven non-vacuous,
      with the plan that proved it and the observed red assertion message, gathered from the six
      SUMMARYs. The list must include, at minimum, the save-discipline gate, the settings forcing,
      the lost-update lock, the one-spawn-site invariant, and the two planning-record guards;
    - **carried findings**: the stdin-EOF measurement's verdict and whether it created a follow-on,
      the lost-update non-vacuity oracle actually used and why, and anything else a prior SUMMARY
      recorded as a finding rather than a fix.

    Record honest results. If any command fails, do not adjust the command — record the failure,
    fix the cause, re-run, and record both runs.

    Then update `.planning/phases/18-persistent-session-and-tool-surface/18-VALIDATION.md`'s
    Per-Task Verification Map: replace the `TBD` task/plan/wave cells with the real plan ids and
    test commands, and flip each row's Status to green with the evidence document cited. Leave
    `status:` and `nyquist_compliant:` in its frontmatter alone — those are `/gsd-validate-phase`'s
    to set, not this plan's.
  </action>
  <verify>
    <automated>cd src/mcp/vice && npm test && cd ../../.. && node scripts/check-skill-tool-coverage.mjs && node scripts/check-npm-packages.mjs && bash scripts/package.sh && test -z "$(pgrep -f 'regenerator2000 --mcp-server-stdio')"</automated>
    <human-check>Read `.planning/phases/18-persistent-session-and-tool-surface/18-PHASE-GATE-EVIDENCE.md` and confirm three things by eye: (1) the skipped-test census contains no live-gated r2000 test, given `regenerator2000 0.9.20` is installed on this host; (2) every one of the six requirement rows names a test that actually ran, not a test that exists; (3) the stdin-EOF measurement's recorded verdict matches what you would expect from the transcript it cites, and if it created a follow-on, that follow-on is filed with an owner rather than only mentioned.</human-check>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/18-persistent-session-and-tool-surface/18-PHASE-GATE-EVIDENCE.md` exists and contains all six sections named in the action (command table, skipped-test census, requirement-to-evidence table, planted-violation census, carried findings, and the version/date header).
    - All seven gate commands are recorded with their real exit statuses, and every one is 0 (or `pgrep` finding nothing).
    - The requirement-to-evidence table has exactly six rows, one per phase requirement id, each naming a test file and a test title.
    - The planted-violation census has at least six entries, each with an observed red assertion message quoted.
    - The skipped-test census contains zero live-gated r2000 tests.
    - `18-VALIDATION.md`'s Per-Task Verification Map has no `TBD` cells remaining and every Status cell is green, with `status:` and `nyquist_compliant:` in its frontmatter unchanged.
    - `git status --porcelain` shows only this plan's intended new and modified files.
  </acceptance_criteria>
  <done>The phase gate has been run rather than described, its real output is committed, and every requirement points at a test that actually executed on this host.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a live child holding the project file → the forced settings on disk | The child's own save writes over the correction made before it started |
| this repo → the two published npm tarballs | `files[]` changed this phase; the packaged file set is the boundary consumers receive |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-03 | Tampering | the session's own save silently reverting the forced settings | high | mitigate | The before-spawn-only forcing rule (D18-32) makes the only editor run before any child exists; task 1's no-revert half is what proves it, and the planted violation is what proves the test |
| T-18-27 | Tampering | the new `files[]` entry leaking a test file or a fixture into a published tarball | medium | mitigate | `scripts/check-npm-packages.mjs` validates both tarballs' exact contents via `npm pack --dry-run --json`; task 2 runs it, and this is the first run against the changed `files[]` |
| T-18-28 | Repudiation | a live-gated test skipping silently and being counted as green | high | mitigate | The skipped-test census is a required section of the evidence document and a live r2000 skip on this host is defined as a gate FAILURE, not a footnote; the human-check re-reads it |
| T-18-29 | Denial of Service | an orphaned child surviving the test suite and holding the project file | medium | mitigate | `pgrep` for the stdio child is a gate command with an expected-empty result |
| T-18-SC | Tampering | npm/pip/cargo installs | low | accept | Not applicable — zero packages installed by this phase (`18-RESEARCH.md` § Package Legitimacy Audit); `check-npm-packages.mjs` validates what is *published*, which is a different concern and is covered by T-18-27 |
</threat_model>

<verification>
- Every command in task 2's list exits 0 (or, for `pgrep`, finds nothing), with the real output committed as evidence.
- `cd src/mcp/vice && npm test` — the FULL suite. `test:automated` is explicitly insufficient.
- The human-check in task 2 is answered before the phase is called done.
</verification>

<success_criteria>
- D18-35 is proven in both halves, with the no-revert half exercised against a real child that had the project loaded.
- The phase gate is executed, not described, and its evidence names a running test for each of the six requirements.
- Both tarballs still validate after the `files[]` change, and nothing this phase built leaks into them.
</success_criteria>

## Artifacts this phase produces

Symbols, files and ids **created** by phase 18 as a whole. Items this plan
creates are marked `[18-07]`.

**Planning ids and records**
- `D-36` — project-wide decision id, superseding `D-32`
- `.planning/ARCHITECTURE.md` → `## Architecture Change Record`, `### Rule A21`
- `.planning/phases/18-persistent-session-and-tool-surface/18-STDIN-EOF-EVIDENCE.md`
- `.planning/phases/18-persistent-session-and-tool-surface/18-PHASE-GATE-EVIDENCE.md` `[18-07]`
- `.planning/ROADMAP.md` → Phase 19 Notes entry recording D18-16's deferral and its measurement task

**New files**
- `src/mcp/vice/docs-r2000-decisions.test.ts`
- `src/mcp/vice/r2000-session.ts`, `src/mcp/vice/r2000-session.test.ts`
- a session-reuse transcript fixture under the phase evidence directory

**New exported symbols — `src/mcp/vice/r2000-mcp-client.ts`**
- `R2000Session` (interface), `openR2000Session()`
- `R2000RestartBudgetExhaustedError`, `R2000RestartBudgetExhaustedErrorOptions`
- `R2000SessionBusyError`, `R2000SessionBusyErrorOptions`

**New exported symbols — `src/mcp/vice/r2000-session.ts`**
- `runInR2000Session()`, `closeR2000SessionSync()`
- `__r2000SessionStateForTest()`, `__resetR2000SessionForTest()`
- a test-only queue-bypass toggle (D18-19's non-vacuity control)
- `DEFAULT_R2000_RESTART_BUDGET`, `DEFAULT_R2000_QUEUE_WAIT_MS`

**New exported symbols — `src/mcp/vice/r2000-project.ts`**
- `ensureProjectSettings()`, `R2000ProjectSettingsError`

**New exported symbols — `src/mcp/vice/r2000-tools.ts`**
- a test-only save-suppression toggle (D18-09 scenario 2's non-vacuity control)
- `R2000_READ_REGION_MAX_BYTES`, `R2000ReadRegionRangeError`, `composeAddressDetails()`
- tool definitions `r2000_read_region` and `r2000_get_address_details`

**New environment variables**
- `R2000_RESTART_BUDGET`, `R2000_QUEUE_WAIT_MS`, `R2000_READ_REGION_MAX_BYTES`

**Removed symbols**
- `ADDRESS_DETAILS_REFUSAL` (`r2000-tools.ts`) — retired by D-36

<output>
Create `.planning/phases/18-persistent-session-and-tool-surface/18-07-SUMMARY.md` when done.
</output>
