---
phase: 45-decomposition-to-closure-disagreement-first
plan: 10
type: execute
wave: 5
depends_on: [45-05, 45-08, 45-09]
files_modified:
  - src/mcp/vice/anno-decomp-closure.test.ts
  - docs/phase45-closure-gate.md
autonomous: true
requirements: [DECOMP-01, DECOMP-02, DECOMP-03, DECOMP-04]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All nine committed fixtures pass the completeness gate with exit 0, each run captured with its output — including the three whose green run still prints their NOT EXECUTED declaration."
    - "charset-phantom.prg's $D011 and $D018 writes export as OR-ed named constants with a decoded comment, and real ACME 0.97 assembles that export to bytes identical to the fixture image — criterion 5 demonstrated on the real store, not on a synthetic one."
    - "A committed test re-runs the whole gate offline over the committed exports — no emulator, no dxa, no Ghidra, no ACME — so the closure keeps being proven on every CI run rather than only on the day it was reached."
    - "The derived half of every committed export regenerates byte-identically: a full re-derivation sweep leaves `git status --porcelain src/mcp/vice/fixtures` empty (D-03)."
    - "The final suite comparison is a NAMED FAILURE SET compared against docs/phase45-wave0-measurements.md's baseline, taken with the broker stopped — never a count, never a pipe to tail."
  artifacts:
    - src/mcp/vice/anno-decomp-closure.test.ts
    - docs/phase45-closure-gate.md
  key_links:
    - "The nine committed .annostore.json exports -> importStoreDocument() -> a temp store -> the gate: the offline regression path that needs no external tool."
    - "anno export-asm over charset-phantom's real store -> real ACME -> a byte-diff against the fixture image: criterion 5's proof."
    - "docs/phase45-closure-gate.md -> the phase's verification: one document a reviewer can read instead of nine."
  prohibitions:
    - "Never auto-install an external tool (dxa, Ghidra, VICE, ACME, c1541, petcat) — detect, then refuse by name with the remedy in the message."
    - "Never extend anno-coverage.ts, and never derive a completeness measure from the store's block-type listing (its own trap 1)."
    - "Never re-implement evid-reconcile.ts's join, and never rename or re-derive its field names."
    - "Never print a percentage, rate, ratio or combined figure in the completeness report."
    - "Never default the disagreement input to an empty array, and never let a missing input render an empty-disagreement report."
    - "Never write a phase deliverable under the gitignored .c64-re-tools/ tree."
    - "Never write the whole-glob `npm test` as a verify command — it blocks forever on vice-proxy.test.ts."
    - "Never compare a `npm run test:automated` result by failure COUNT; compare the named failure SET against the measured baseline."
    - "Never hand-edit anno-regbits.json — it is generated-but-committed."
    - "Never widen DATA_TYPES beyond the frozen twelve."
    - "Never fabricate a symbol where the bank state is path-dependent — decline with a named reason instead."
    - "Never leave the VICE broker running when the work is done, and never launch it with setsid or nohup."
    - "Never run a plain `grep` over src/mcp/vice/ — use `grep -a` (anno-memmap-render.ts contains a NUL byte)."
---

<objective>
Close the phase: demonstrate criterion 5 on the real store, run the gate over
all nine fixtures, and leave behind a test that keeps proving it.

Two things distinguish this plan from a summary. First, criterion 5's proof
runs against `charset-phantom.prg`'s ACTUAL committed store — the enums plan
45-09 installed, exported through the renderer plan 45-05 built, assembled by
real ACME, diffed byte for byte against the fixture image. Second, the
offline regression test: every committed export is re-imported into a temp
store and re-gated with no external tool present, so the closure this phase
reached is checked on every CI run rather than being a fact about one
afternoon.

Purpose: a phase whose evidence is nine terminal transcripts decays the moment
someone edits a fixture. A phase whose evidence is a committed test does not.

Output: the criterion-5 demonstration, nine green gate runs, an offline
regression test, and one closure document a reviewer can read instead of nine.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/45-decomposition-to-closure-disagreement-first/45-CONTEXT.md
@.planning/phases/45-decomposition-to-closure-disagreement-first/45-08-SUMMARY.md
@.planning/phases/45-decomposition-to-closure-disagreement-first/45-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criterion 5, demonstrated on the real charset-phantom store under real ACME</name>
  <precondition>ACME 0.97 is on $PATH (acme-gate.ts's ACME_BIN resolves it). If it is absent, REFUSE BY NAME with the install line from README.md as the remedy and halt — never install it.</precondition>
  <read_first>
    - src/mcp/vice/anno-export-asm.ts — the OR-ed render path as plan 45-05 left it, and the export's own statement that it never assembles its own output.
    - src/mcp/vice/acme-gate.ts and src/mcp/vice/acme-verify.ts — `ACME_BIN`, `ACME_VERIFY_ARGV_FLAGS`, `verifyAcmeAssembles()` and the never-treat-a-warning-as-a-failure rule.
    - src/mcp/vice/fixtures/ghidra/charset-phantom.annostore.json — the committed export as plan 45-09 left it, especially its projectEnums and enumUsage rows.
    - src/mcp/vice/fixtures/ghidra/charset-phantom.a — the literal values written to `$D011` and `$D018`.
    - .planning/phases/45-decomposition-to-closure-disagreement-first/45-RESEARCH.md — the MEASURED ACME result for the OR-ed form.
  </read_first>
  <files>docs/phase45-closure-gate.md</files>
  <action>
Run criterion 5's proof end to end against the real artifact, and capture what
it produced.

Import src/mcp/vice/fixtures/ghidra/charset-phantom.annostore.json into a
fresh scratch store, then run `anno export-asm` over it with the fixture image.
Capture the exported source lines for the `$D011` and `$D018` writes verbatim
— they must read as OR-ed named constants followed by a decoded comment, in
D-17's shape, with the per-field constant definitions present in the header
block.

Assemble that exported source with the real `ACME_BIN` and diff the produced
bytes against src/mcp/vice/fixtures/ghidra/charset-phantom.prg. Capture ACME's
exit status and the diff result. Byte equality is the proof that the
decomposition is arithmetically correct rather than merely plausible; an ACME
warning is not a failure and must not be reported as one.

Then capture the readability half: call `anno_disassemble` over the
`$0810-$0822` range with the same store and image, and paste the listing lines
for the two register writes. Both surfaces, one decoder — that is D-16, and
this is where it is shown rather than described.

Write all of it into docs/phase45-closure-gate.md under a criterion-5 heading:
the exported lines, the header definition lines, the ACME exit status, the
byte-diff result, and the disassemble listing lines.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac ' | D018_\| | D011_' docs/phase45-closure-gate.md</automated>
    <fails_when>the count is 0 — the captured export lines must show the OR-ed form for at least one of the two registers</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac 'byte-identical\|bytes identical\|diff: equal' docs/phase45-closure-gate.md</automated>
    <fails_when>the count is 0 — the byte-diff result must be recorded, not implied</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && VICE_REQUIRE_ACME=1 node --test anno-export-asm.test.ts</automated>
    <fails_when>non-zero exit, or a line matching "^# fail [1-9]", or any "# skip" for the decomposition case while ACME is required</fails_when>
  </verify>
  <acceptance_criteria>
    - docs/phase45-closure-gate.md carries the verbatim exported source lines for the `$D011` and `$D018` writes, each showing OR-ed named constants and a decoded comment on the same line.
    - The captured header block shows one definition line per per-field constant, each appearing exactly once.
    - The document records ACME's exit status and the byte-diff outcome against charset-phantom.prg explicitly.
    - The document carries the `anno_disassemble` listing lines for the same two instructions, showing the same named constants.
    - No ACME warning was reported as a failure.
  </acceptance_criteria>
  <done>Criterion 5 is demonstrated on the real committed store: named, decomposed, commented, and proven byte-identical by a real assembler.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The offline closure regression — nine fixtures, no external tool</name>
  <read_first>
    - src/mcp/vice/anno-store-export.ts — `importStoreDocument()` and the schema, as plan 45-02 left it.
    - src/mcp/vice/anno-cli.ts — `cmdEvidDisagreements()` and `cmdDecompCompleteness()`, so the test drives the same code path a developer does.
    - src/mcp/vice/anno-store.test.ts — the temp-directory convention this repo's store tests use (never a scratch path inside the repository tree).
    - src/mcp/vice/test-gate.mjs — MANUAL_ONLY_TESTS, which this file must NOT join: it needs no emulator and must run in the automated set.
    - src/mcp/vice/fixtures/decomp-execution-manifest.json — the manifest the gate requires.
  </read_first>
  <behavior>
    - Test 1: for each of the nine committed exports, importing it into a fresh temp store reproduces every row class (a re-export is deep-equal to the committed document).
    - Test 2: for each of the nine, running the disagreement query against the imported store and then the gate exits 0.
    - Test 3: for each of the three manifest-declared non-executed fixtures, the rendered report contains its NOT EXECUTED line and the manifest's reason.
    - Test 4: the gate run for each fixture reports zero `undefined` bytes, zero survivors, zero unresolved addresses and zero unresolved disagreements.
    - Test 5: the non-vacuity control — deleting the disagreement input for one imported fixture makes that same gate run exit non-zero, inside the test, so a future change that softens the requirement reds this suite.
  </behavior>
  <files>src/mcp/vice/anno-decomp-closure.test.ts</files>
  <action>
Write the test that keeps this phase's closure true after the terminal
transcripts are gone.

For each of the nine committed `.annostore.json` exports: import it into a
fresh store in an OS temp directory (never inside the repository tree — this
project has already had an intermittent suite failure from scratch files
racing there), run the disagreement query against that store, and run the
gate. Assert exit 0 and assert each of the four zero-counts individually, by
name, rather than only asserting the exit code — an exit code tells you it
passed, the named counts tell you WHAT passed.

The whole test must run with no emulator, no dxa, no Ghidra and no ACME: every
input it needs is already committed. That is what makes it a regression rather
than a re-run.

Include the non-vacuity control as a real test case: for one fixture, invoke
the same gate with the disagreement input removed and assert it exits non-zero
naming `--disagreements`. Without that case the suite would still pass if
someone made the input optional.

Do NOT add this file to MANUAL_ONLY_TESTS. It belongs in the automated set,
and `ci-suite-coverage.test.ts` must stay green.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test anno-decomp-closure.test.ts</automated>
    <fails_when>non-zero exit, or a line matching "^# fail [1-9]", or a "# tests" count below 9 in the output</fails_when>
    <automated>grep -ac 'anno-decomp-closure.test.ts' src/mcp/vice/test-gate.mjs</automated>
    <fails_when>the count is not 0 — this file must stay in the automated set</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test ci-suite-coverage.test.ts</automated>
    <fails_when>non-zero exit, or a line matching "^# fail [1-9]" in the output (measure this after any worktree cleanup — the guard's walk descends into .claude/worktrees/agent-*/)</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck</automated>
    <fails_when>non-zero exit, or any line matching "error TS" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `node --test src/mcp/vice/anno-decomp-closure.test.ts` reports `# fail 0` with at least 9 tests — one per committed fixture at minimum.
    - Each fixture's case asserts the four zero-counts by name, not only the exit code.
    - The three non-executed fixtures' cases assert the NOT EXECUTED line and the manifest's reason text.
    - The non-vacuity control case asserts a non-zero exit and the literal `--disagreements` in the message.
    - The test spawns no emulator, resolves no external binary, and writes nothing inside the repository tree; `git status --porcelain` is clean after it runs.
    - The file is absent from MANUAL_ONLY_TESTS and `ci-suite-coverage.test.ts` is green.
  </acceptance_criteria>
  <done>The phase's closure is proven by a committed test that needs nothing but the repository, and that test cannot pass if the disagreement input stops being required.</done>
</task>

<task type="auto">
  <name>Task 3: The closure record — nine green gates, the idempotence sweep, and the suite baseline</name>
  <read_first>
    - docs/phase45-wave0-measurements.md — the recorded suite failure baseline and the frozen survivor set.
    - docs/phase45-closure-dxa-family.md and docs/phase45-closure-ghidra-family.md — the per-family green gate runs, so this document points at them rather than restating them.
    - docs/phase45-planted-control-evidence.md — the three OBSERVED RED controls.
    - .planning/ROADMAP.md — the five success criteria for this phase, which this document is organised against.
  </read_first>
  <files>docs/phase45-closure-gate.md</files>
  <action>
Write the phase's closure record: one document a reviewer reads instead of
nine, organised against the roadmap's own five success criteria, each with a
pointer to the evidence rather than a restatement of it.

Per criterion, record: what was measured, where the evidence lives (file and
section), and what the result was. Criterion 1 points at the nine green gate
runs and the planted-control document; criterion 2 at the disagreement
resolution counts and the accepted-disagreement records; criterion 3 at the
per-entry-point tables in the two family documents; criterion 4 at the
persisted `DECLINED:` records, naming bank-path-dependent.prg's both-states
record and the runtime-varying pointer declines; criterion 5 at task 1's ACME
byte-diff.

Then run the two sweeps this phase has been deferring to the end:

1. **Idempotence, all nine.** Re-derive each fixture's derived half into a
   fresh scratch store, re-export, and confirm `git status --porcelain
   src/mcp/vice/fixtures` is empty. This is D-03's claim about the derived half,
   checked once across the whole set rather than per family. Record the
   commands and the empty output. Note explicitly in the document that the
   AUTHORED half is NOT regenerated and no claim is made that it would be —
   D-03 splits provenance precisely so this sentence can be true.
2. **The suite baseline comparison.** STOP THE BROKER FIRST (a live broker
   deterministically reddens BACK-05). Run `npm run test:automated` from
   src/mcp/vice redirected to a file, reading `$?` on the same line — never
   piped to `tail`, which reports tail's own exit 0. Compare the NAMED failing
   set against docs/phase45-wave0-measurements.md's baseline and record the
   comparison as a set difference: which named failures are new, which are
   gone, which are unchanged. A count comparison is not acceptable evidence.

Finish with an honest limits section: what this phase did NOT prove. At
minimum: purpose-comment CONTENT is human-reviewed rather than mechanically
checked (only the four labels are checked); the authored half makes no
regeneration claim; and the nine fixtures are synthetic, so nothing here
establishes behaviour on real cracked code.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && git status --porcelain src/mcp/vice/fixtures</automated>
    <fails_when>any output at all after the idempotence sweep — the derived half did not reproduce byte-identically across all nine</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac 'criterion' docs/phase45-closure-gate.md</automated>
    <fails_when>the count is fewer than 5 — one section per roadmap success criterion</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -a 'test:automated' docs/phase45-closure-gate.md | grep -acv '| tail'</automated>
    <fails_when>the count is 0, or any recorded invocation in the document is piped to tail</fails_when>
    <automated>pgrep -x x64sc; echo "pgrep_exit=$?"</automated>
    <fails_when>pgrep_exit=0 — the broker must be stopped before and after the suite measurement</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac 'did NOT prove\|not prove\|Limits' docs/phase45-closure-gate.md</automated>
    <fails_when>the count is 0 — the limits section is mandatory</fails_when>
  </verify>
  <acceptance_criteria>
    - docs/phase45-closure-gate.md has one section per roadmap success criterion, each naming the evidence file and section rather than restating the evidence.
    - The idempotence sweep across all nine fixtures is recorded with its commands and an empty `git status --porcelain` output, plus the explicit sentence that the authored half makes no regeneration claim.
    - The suite comparison is recorded as a NAMED SET DIFFERENCE against the Wave 0 baseline, with the broker-stopped precondition stated and the redirect-not-pipe method visible.
    - The limits section names, at minimum, the three limits above.
    - No `x64sc` process survives the plan.
  </acceptance_criteria>
  <done>The phase has one readable closure record, the derived half is proven to regenerate across all nine fixtures, and the suite result is compared as a named set against a real baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed export -> importStoreDocument() in a test | A committed document is replayed into a store on every CI run |
| exported ACME source -> real ACME | Generated source is assembled and its bytes compared |
| the closure document -> a reviewer's belief | A summary could assert what was never measured |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-40 | Repudiation | A closure summary asserting results nobody measured | high | mitigate | Every criterion section names the evidence file and section; the offline regression test re-proves the gate mechanically on every CI run rather than trusting the document |
| T-45-41 | Tampering | The regression test passing after the disagreement input is made optional | high | mitigate | The non-vacuity control case is part of the same suite: it asserts a non-zero exit and the literal `--disagreements` when the input is removed |
| T-45-42 | Denial of Service | The offline test needing an emulator or an external tool and therefore being skipped in CI | high | mitigate | The test uses only committed exports and in-process store code; a verify asserts it is absent from MANUAL_ONLY_TESTS and that ci-suite-coverage stays green |
| T-45-43 | Spoofing | A green suite result produced by piping to tail or by a live broker | high | mitigate | The suite run is redirected to a file with `$?` read on the same line, the broker is stopped first, and the comparison is a named set difference |
| T-45-SC | Tampering | npm/pip/cargo installs | low | accept | Severity is low HERE because the attack surface is absent, not because the class is minor: no package-manager install task exists in this plan or this phase; 45-RESEARCH.md's Package Legitimacy Audit records "not applicable". ACME is detected, never installed |
</threat_model>

<verification>
- `node --test src/mcp/vice/anno-decomp-closure.test.ts` is green with at least nine cases and the non-vacuity control.
- `VICE_REQUIRE_ACME=1 node --test src/mcp/vice/anno-export-asm.test.ts` is green with the decomposition case running.
- `git status --porcelain src/mcp/vice/fixtures` is empty after the nine-fixture idempotence sweep.
- docs/phase45-closure-gate.md carries one section per success criterion, the criterion-5 capture, the set-difference suite comparison and a limits section.
- No `x64sc` process survives the plan.
</verification>

<success_criteria>
- All nine fixtures pass the gate, and a committed offline test keeps proving it.
- Criterion 5 is demonstrated on the real store under real ACME, with the OR-ed form present in the captured source.
- The derived half regenerates byte-identically across the whole fixture set.
- The phase's evidence is readable in one document and re-checkable by one command.
</success_criteria>

## Artifacts this phase produces

New test:
- src/mcp/vice/anno-decomp-closure.test.ts — the offline, tool-free closure regression over all nine committed exports

New document:
- docs/phase45-closure-gate.md — the phase closure record, organised against the five roadmap success criteria, with the criterion-5 ACME capture, the nine-fixture idempotence sweep and the named-set suite comparison

<output>
Create `.planning/phases/45-decomposition-to-closure-disagreement-first/45-10-SUMMARY.md` when done
</output>
