---
phase: 45-decomposition-to-closure-disagreement-first
plan: 04
type: execute
wave: 3
depends_on: [45-01, 45-02]
files_modified:
  - src/mcp/vice/anno-cli.ts
  - src/skills/routine-queue-walker/scripts/completeness-report.mjs
  - src/skills/routine-queue-walker/scripts/completeness-report.test.mjs
  - src/skills/routine-queue-walker/SKILL.md
  - docs/phase45-planted-control-evidence.md
autonomous: true
requirements: [DECOMP-01, DECOMP-02, DECOMP-03]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The report is a GATE, not a bulletin: a nonzero Undefined byte count, a nonzero unresolved-disagreement count, a surviving auto-name, an entry point missing any of the four purpose-comment elements, or a referenced non-hardware address that is neither named nor declined each makes the run exit non-zero."
    - "The soundness asymmetry is carried TWO ways, both required: the aggregate reuses evid-reconcile.ts's four buckets verbatim with blockCoveredNeverObservedCount on its own line against an explicit denominator and never folded into a class total; and every typed range carries typedBy of observed-executing, byte-derived or authored (D-10)."
    - "Two typed ranges that touch exactly stay two rows and are rendered as two rows; a byte where the derived classification and the execution oracle disagree is rendered as a disagreement row and is never folded into the agreement count (edge probe DECOMP-01/adjacency)."
    - "Every rendered list — fixtures, disagreement rows, per-range provenance rows, entry points, survivors, declines — is emitted in a specified stable order (ascending address, then ascending endInclusive, then name; fixtures by manifest path), so two runs over the same store produce byte-identical text (edge probe DECOMP-01/ordering, DECOMP-02/ordering, DECOMP-03/ordering)."
    - "A fixture with zero code entry points renders `0 of 0` against an explicit denominator plus, where the manifest declares it non-executed, an explicit NOT EXECUTED line naming why — never an unqualified pass (edge probe DECOMP-02/empty)."
    - "A fixture that references zero non-hardware addresses renders `0 of 0` against an explicit denominator rather than an unqualified pass (edge probe DECOMP-03/empty)."
    - "The four split layouts lo_hi_address, hi_lo_address, lo_hi_word and hi_lo_word are NAMED as `table` in the rendered output while the frozen twelve-member DATA_TYPES vocabulary is untouched (D-11)."
    - "Removing the disagreement input makes the gate FAIL, observed and recorded as a red run in docs/phase45-planted-control-evidence.md — asserting that the check exists proves nothing (D-09)."
  artifacts:
    - src/skills/routine-queue-walker/scripts/completeness-report.mjs
    - src/skills/routine-queue-walker/scripts/completeness-report.test.mjs
    - src/skills/routine-queue-walker/SKILL.md
    - docs/phase45-planted-control-evidence.md
  key_links:
    - "evid-reconcile.ts's EvidReconciliation fields -> the report's aggregate section, reused verbatim and never renamed."
    - "anno-store-export.ts's DECLINE_COMMENT_PREFIX / DISAGREEMENT_ACCEPTED_COMMENT_PREFIX / AUTHORED_PROVENANCE_COMMENT_PREFIX -> the report's decline census and typedBy derivation: one declaration, imported."
    - "The report's exit code -> routine-queue-walker's numeric stop condition (D-08): the walk stops when the gate goes green, not when the agent believes it is finished."
  prohibitions:
    - "Never auto-install an external tool (dxa, Ghidra, VICE, ACME, c1541, petcat) — detect, then refuse by name with the remedy in the message."
    - "Never extend anno-coverage.ts, and never derive a completeness measure from the store's block-type listing (its own trap 1)."
    - "Never re-implement evid-reconcile.ts's join, and never rename or re-derive its field names."
    - "Never print a percentage, rate, ratio or combined figure in the completeness report."
    - "Never default the disagreement input to an empty array, and never let a missing input render an empty-disagreement report."
    - "Never write a phase deliverable under the gitignored .c64-re-tools/ tree."
    - "Never write the whole-glob `npm test` as a verify command — it blocks forever on vice-proxy.test.ts."
    - "Never compare a `npm run test:automated` result by failure COUNT; compare the named failure SET against the measured baseline."
    - "Never hand-edit anno-regbits.json — it is generated-but-committed."
    - "Never widen DATA_TYPES beyond the frozen twelve."
    - "Never fabricate a symbol where the bank state is path-dependent — decline with a named reason instead."
    - "Never leave the VICE broker running when the work is done, and never launch it with setsid or nohup."
    - "Never run a plain `grep` over src/mcp/vice/ — use `grep -a` (anno-memmap-render.ts contains a NUL byte)."
---

<objective>
Turn plan 45-01's tracer report into the phase's actual closure bar, and make
its central claim — that it cannot pass vacuously — provable by a control
observed going RED.

Three things land: the full measure set (zero-Undefined census with D-11's
`table` naming, D-10's two soundness renderings, the entry-point and survivor
censuses, the decline census), the gate semantics (a nonzero unresolved count
EXITS NON-ZERO rather than being reported beside a pass), and the planted
control that proves the disagreement input is load-bearing.

Then the skill that owns it (D-08) is brought up to date: its candidate queue
is rebuilt from cross-references and block classification, because RESEARCH.md
§2 MEASURED that its current label-prefix scan finds nothing at all on a store
the dxa + Ghidra route derived.

Purpose: criterion 2 is this milestone's named most-dangerous gap — an oracle
that ships and never gets cashed. A gate that reports disagreements beside a
pass is exactly that failure with a nicer face.

Output: the complete report, its gate semantics, a recorded red run, and a
skill playbook whose queue actually fires.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-decomposition-to-closure-disagreement-first/45-CONTEXT.md
@.planning/phases/45-decomposition-to-closure-disagreement-first/45-RESEARCH.md
@.planning/phases/45-decomposition-to-closure-disagreement-first/45-PATTERNS.md
@.planning/phases/45-decomposition-to-closure-disagreement-first/45-01-SUMMARY.md
@.planning/phases/45-decomposition-to-closure-disagreement-first/45-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The full measure set and the gate semantics</name>
  <read_first>
    - src/mcp/vice/evid-reconcile.ts — the header's four buckets and five named traps (lines 1-73) and the `EvidReconciliation` interface (lines 124-174). Every field name is reused verbatim.
    - src/mcp/vice/anno-cli.ts — `printEvidDisagreementsReport()` (lines 1482-1508) as the rendering discipline to copy, and `cmdDecompCompleteness()` as plan 45-01 left it.
    - src/mcp/vice/anno-types.ts — `DATA_TYPES` and `SPLIT_DATA_TYPES` (lines 288-323), so the four split layouts are read from the source of truth rather than restated.
    - src/mcp/vice/anno-store-export.ts — the three comment-prefix constants and `isDeclineComment()` / `provenanceForComment()` from plan 45-02.
    - src/mcp/vice/anno-coverage.ts — the header's trap 1, quoted in the new module's own WHAT NOT TO DO so a reader sees why this is a separate instrument.
    - src/mcp/vice/fixtures/decomp-execution-manifest.json — as plan 45-01 authored it.
  </read_first>
  <behavior>
    - Test 1: a store with one `undefined`-typed byte makes the gate exit non-zero and renders that byte's address; a store with none exits zero on that measure.
    - Test 2: the aggregate section prints `blockCoveredNeverObservedCount` on its own line beside `denominator`, and the rendered text contains no `%` and no line combining two buckets into one figure.
    - Test 3: a range whose bytes carry an execute observation renders `typedBy: observed-executing`; a range whose start address carries an `AUTHORED_PROVENANCE_COMMENT_PREFIX` comment and no observation renders `authored`; a range with neither renders `byte-derived`. Precedence is asserted explicitly: a range that is BOTH observed and authored renders `observed-executing`.
    - Test 4: two ranges that touch exactly (`endInclusive + 1 === next.start`) render as two rows, never merged; and a disagreement at that boundary byte appears in the disagreements section, not in the agreement count.
    - Test 5: rendering the same answer twice produces byte-identical text, and the row order is ascending address then ascending endInclusive then name.
    - Test 6: a fixture with zero code entry points renders `0 of 0` with a denominator; the same for zero referenced non-hardware addresses; neither renders as an unqualified pass.
    - Test 7: a `lo_hi_address` range is rendered with the word `table` while `DATA_TYPES` still holds exactly twelve members.
    - Test 8: an entry point whose comment lacks any one of `function:`, `inputs:`, `outputs:`, `side effects:` makes the gate exit non-zero and is named by address.
    - Test 9: a disagreement row with no `DISAGREEMENT-ACCEPTED:` comment at its address counts as unresolved and exits non-zero; the same row with that comment counts as accepted and does not.
  </behavior>
  <files>src/mcp/vice/anno-cli.ts, src/skills/routine-queue-walker/scripts/completeness-report.mjs, src/skills/routine-queue-walker/scripts/completeness-report.test.mjs</files>
  <action>
Widen the `decomp-completeness` verb's `--json` answer and the script's
renderer to the full measure set, and give the whole thing an exit code.

VERB SIDE (src/mcp/vice/anno-cli.ts) — add to the answer:
- `rangeProvenance`: one row per typed range, `{ start, endInclusive,
  dataType, renderedType, typedBy }`. `renderedType` is `dataType` except for
  the four members of `SPLIT_DATA_TYPES`, which render as `table` (D-11) —
  read the four from `SPLIT_DATA_TYPES`, never restate them. `typedBy` is
  derived with this stated precedence, which the executor must not reorder:
  `observed-executing` when at least one execute observation falls inside the
  range; else `authored` when the range's start address carries a comment
  beginning `AUTHORED_PROVENANCE_COMMENT_PREFIX`; else `byte-derived`.
  Evidence beats inference — that is the same asymmetry criterion 1 states.
- `entryPoints`: one row per code entry point (an address that is the target
  of at least one JSR cross-reference, plus the image's own start address),
  `{ address, name, hasName, purposeElements: { function, inputs, outputs,
  sideEffects } }`. Each element is TRUE when the address's comment contains
  the corresponding literal label `function:`, `inputs:`, `outputs:`,
  `side effects:` (case-insensitive on the label only). Presence is what is
  mechanically checkable; the semantic quality of the four elements is the
  manual verification 45-VALIDATION.md already records as manual-only.
- `referencedAddresses`: `{ resolved: [...], declined: [{ address, reason }],
  unresolved: [...], denominator }`, where a declined address is one carrying
  a comment beginning `DECLINE_COMMENT_PREFIX` (imported from
  anno-store-export.ts, never re-matched locally), and a non-hardware address
  is one outside the ranges `anno-regbits.json` and memmap.json classify as
  hardware registers.
- `disagreementResolution`: for each row of the supplied disagreement input,
  `{ address, resolved, accepted, reason }`, where `accepted` is true when the
  address carries a `DISAGREEMENT_ACCEPTED_COMMENT_PREFIX` comment, and
  `unresolvedCount` beside its own `denominator`.
- Sort EVERY array by ascending address, then ascending endInclusive, then
  name. State the sort in a comment; a Map or Set iteration order must never
  reach the output.

SCRIPT SIDE (completeness-report.mjs) — render each measure under its own
heading in `printEvidDisagreementsReport()`'s discipline: disagreements first,
`denominator` beside every count, an explicit sentence per section stating
what an absence does NOT prove, no percentage, no rate, no combined figure.
Render `blockCoveredNeverObservedCount` as its own named line, never folded
into a code or data total (D-10.1). Render the NOT EXECUTED line for a
manifest-declared non-executed fixture, naming the manifest's own reason
(D-13) — that line prints in the same position a disagreement section would
occupy, so a reader cannot mistake absence of disagreements for a clean bill
of health.

GATE SEMANTICS — the script's process exit code is 0 only when ALL of:
`byteCensus.undefinedCount === 0`; `disagreementResolution.unresolvedCount ===
0`; `survivors` is empty; every `entryPoints` row has `hasName` true and all
four `purposeElements` true; `referencedAddresses.unresolved` is empty. Any
failing measure exits 1 AND names, by address, what failed. Criterion 2's own
words: a nonzero unresolved count BLOCKS rather than being reported beside a
pass.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node --test src/skills/routine-queue-walker/scripts/completeness-report.test.mjs</automated>
    <fails_when>non-zero exit, or a line matching "^# fail [1-9]", or a "# tests" count below 14 in the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck</automated>
    <fails_when>non-zero exit, or any line matching "error TS" in the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test anno-types.test.ts anno-cli.test.ts</automated>
    <fails_when>non-zero exit, or a line matching "^# fail [1-9]" beyond the named failures recorded in docs/phase45-wave0-measurements.md's baseline</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac 'lo_hi_address' src/skills/routine-queue-walker/scripts/completeness-report.mjs</automated>
    <fails_when>the count is not 0 — the four split layouts must be read from SPLIT_DATA_TYPES on the verb side, never restated in the renderer</fails_when>
  </verify>
  <acceptance_criteria>
    - The test file reports `# fail 0` with at least 14 tests, covering all nine behaviours above.
    - The rendered output of a complete synthetic answer contains zero `%` characters, and every count line contains the substring `denominator`.
    - A test asserts render determinism by rendering twice and comparing strings for exact equality.
    - A test asserts the typedBy precedence explicitly for a range that is both observed and authored.
    - `grep -ac 'blockCoveredNeverObservedCount' src/skills/routine-queue-walker/scripts/completeness-report.mjs` is at least 1, and the field name is spelled exactly as evid-reconcile.ts declares it.
    - The gate's exit code is asserted by a test for each of the five failing measures, each exiting 1 and naming the offending address.
    - `DATA_TYPES` is unchanged: `node --test src/mcp/vice/anno-types.test.ts` is green and `git status --porcelain src/mcp/vice/anno-types.ts` shows no change to the vocabulary.
  </acceptance_criteria>
  <done>The report measures everything the five success criteria name, renders it under the project's own reporting discipline, and exits non-zero when any measure fails.</done>
</task>

<task type="auto">
  <name>Task 2: The planted control, observed RED and recorded</name>
  <read_first>
    - .planning/phases/45-decomposition-to-closure-disagreement-first/45-RESEARCH.md — §8 in full (the project's planted-control convention and the concrete shape recommended for D-09).
    - src/mcp/vice/anno-verb-coverage.test.ts — lines 33-44, the never-derive-the-control-from-its-subject posture.
    - src/skills/routine-queue-walker/scripts/completeness-report.test.mjs — as task 1 left it.
    - docs/phase45-wave0-measurements.md — the format for recording a MEASURED fact in this phase.
  </read_first>
  <files>src/skills/routine-queue-walker/scripts/completeness-report.test.mjs, docs/phase45-planted-control-evidence.md</files>
  <action>
Prove D-09 by making the gate fail, and record the failure. An assertion that
the check is present proves nothing; the milestone's stated discipline is a
control observed going RED.

Run three planted controls against a REAL store (the tracer.prg store plan
45-01 derived), each one reverted immediately after it is observed:

1. **Input removed.** Invoke the gate with `--disagreements` omitted. Capture
   the exact stderr and the exit code. Expected: exit 1, the message names
   `--disagreements`, and NO report body is printed.
2. **Input emptied.** Supply a syntactically valid disagreements document whose
   `disagreements` array is empty and whose run identity matches NO row in the
   store's evid-runs table. Capture stderr and exit code. Expected: exit 1
   naming the unmatched run identity — this is the control that distinguishes
   "no disagreements" from "the query was never run", and it is the one that
   matters most.
3. **Required field deleted.** In a scratch copy of the script (never
   committed), delete the `disagreementInput` guard in
   `renderCompletenessReport()`, then run the previously-green render. Capture
   what happens. Expected: the render either throws elsewhere or produces a
   report with no disagreement section — record verbatim whichever occurs,
   then discard the scratch copy. This is what proves the required output
   field is load-bearing rather than decorative.

Write docs/phase45-planted-control-evidence.md carrying, for each control: the
exact command, the exact captured output, the exit code, and the statement that
it was reverted. Then add the two automatable controls (1 and 2) as permanent
tests in completeness-report.test.mjs, asserting the exit code and the named
string — so a future edit that quietly softens either refusal reds the suite.
Control 3 stays documentary: it is a scratch-copy experiment, and its evidence
is the recorded output, not a committed test.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node --test src/skills/routine-queue-walker/scripts/completeness-report.test.mjs</automated>
    <fails_when>non-zero exit, or a line matching "^# fail [1-9]" in the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac 'OBSERVED RED' docs/phase45-planted-control-evidence.md</automated>
    <fails_when>the count is fewer than 3 — one recorded red per planted control</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac 'exit=1' docs/phase45-planted-control-evidence.md</automated>
    <fails_when>the count is 0 — a control with no recorded exit code is an assertion, not an observation</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && git status --porcelain src/skills/routine-queue-walker/scripts</automated>
    <fails_when>any line naming a scratch or .bak copy of completeness-report.mjs — the planted edit must be reverted, not committed</fails_when>
  </verify>
  <acceptance_criteria>
    - docs/phase45-planted-control-evidence.md carries three sections, each headed with the literal string OBSERVED RED, each containing the exact command, the verbatim captured output and the exit code.
    - Control 2's recorded output names the unmatched run identity, proving that an empty disagreement array with no matching run is refused rather than rendered.
    - Each section states explicitly that the plant was reverted, and `git status --porcelain` is clean of scratch copies.
    - completeness-report.test.mjs carries permanent tests for controls 1 and 2 that assert exit code 1 and the named literal string, and those tests are NOT derived from the code path they test.
    - No control was run against a synthetic store: each records the real store path it used.
  </acceptance_criteria>
  <done>The disagreement input is proven load-bearing by three failures that were made to happen and recorded, and two of the three are pinned as permanent tests.</done>
</task>

<task type="auto">
  <name>Task 3: Rework routine-queue-walker's queue so it fires on a derived store</name>
  <read_first>
    - src/skills/routine-queue-walker/SKILL.md — the whole file, but especially Phase 2.1 (lines 80-100), Phase 3.1 (lines 128-145) and Phase 5 (lines 190-258).
    - .planning/phases/45-decomposition-to-closure-disagreement-first/45-RESEARCH.md — §2 and §7, and Open Question 1's recommendation.
    - src/mcp/vice/anno-tools.ts — the `anno_get_cross_references` and `anno_get_blocks` tool definitions (lines 674 and 859), for the exact argument names the playbook must quote.
    - docs/phase45-wave0-measurements.md — the MEASURED label population, which is the evidence this rework is based on.
  </read_first>
  <files>src/skills/routine-queue-walker/SKILL.md</files>
  <action>
Make the playbook work against the store this project actually produces.

RESEARCH.md §2 MEASURED the problem: the derivation route writes typed ranges
and cross-references and NO labels, so Phase 2.1's and Phase 3.1's
label-prefix scan (the eleven legacy prefixes from the retired external
analyser) finds nothing to do and the walk reports a clean queue on a store
where nothing has been named. That is a silent vacuous pass in the skill
itself.

Three surgical additions — not a rewrite:

1. **Phase 2.1 gains an alternate candidate source, stated first.** Build the
   routine queue from `anno_get_cross_references` and `anno_get_blocks`: an
   address that is the target of at least one JSR cross-reference AND sits
   inside a range typed `code` is a routine candidate, whether or not it
   carries a label. The existing label-prefix path stays, below it, for a
   store that DOES carry externally-imported auto-names — both paths, unioned,
   because a store may be either shape. State in one sentence that the
   label-prefix path finds nothing on a purely dxa/Ghidra-derived store, and
   cite docs/phase45-wave0-measurements.md for the measurement rather than
   asserting it.
2. **Phase 3.1 gains the same treatment for symbols:** an address referenced
   by a split lo/hi pair or by an address table, or referenced from a code
   range and not itself inside one, is a symbol candidate regardless of its
   label. Keep the existing prefix list underneath.
3. **Phase 5 gains the numeric stop condition (D-08's stated job for the new
   report).** The existing `anno coverage` invocation stays EXACTLY as it is —
   do not edit that command, do not reword its surrounding paragraph. Add
   beside it the `anno decomp-completeness` invocation plan 45-01 introduced,
   with one paragraph stating plainly that these are two different,
   non-overlapping measurements: `anno coverage` is the byte-census and
   label-ratio instrument, `anno decomp-completeness` is the disagreement-gated
   closure gate, and neither replaces the other. State the stop condition in
   one sentence: the walk is finished when `anno decomp-completeness` exits 0
   for the fixture — not when the agent believes the queue is empty.

Also add, to the "When something fails" section, the decline convention: an
address whose target is genuinely path-dependent gets a `DECLINED:` comment
naming what is unknown, and an accepted disagreement gets a
`DISAGREEMENT-ACCEPTED:` comment naming why — both through `anno_set_comment`,
both greppable, neither a new mechanism. A confident wrong label is worse than
an absent one.

Every documented `anno` invocation is validated argument by argument by
scripts/check-skill-cli-invocations.mjs, so any command text added here must
match `VERB_OPTIONS` exactly.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-skill-cli-invocations.mjs; echo "exit=$?"</automated>
    <fails_when>the printed exit is not exit=0, or the output names a mismatched argument on any routine-queue-walker invocation</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-skill-description-overlap.mjs; echo "exit=$?"</automated>
    <fails_when>the printed exit is not exit=0</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac 'anno coverage' src/skills/routine-queue-walker/SKILL.md</automated>
    <fails_when>the count is 0 — Phase 5's existing coverage invocation must survive this edit unchanged</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac 'anno_get_cross_references' src/skills/routine-queue-walker/SKILL.md</automated>
    <fails_when>the count is fewer than 2 — the xref-derived candidate source must appear in both Phase 2.1 and Phase 3.1</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-skill-tool-coverage.mjs; echo "exit=$?"</automated>
    <fails_when>the printed exit is not exit=0</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff src/skills/routine-queue-walker/SKILL.md` shows the existing `anno coverage` command line unchanged, character for character.
    - Phase 2.1 and Phase 3.1 each state the xref/block-derived candidate source ABOVE the legacy label-prefix path, and each cites docs/phase45-wave0-measurements.md for the measurement that motivated it.
    - Phase 5 states the numeric stop condition as "exits 0", not as a subjective judgement.
    - The `DECLINED:` and `DISAGREEMENT-ACCEPTED:` conventions appear in the playbook with the same literal prefixes anno-store-export.ts declares.
    - All four skill guard scripts (`check-skill-cli-invocations`, `check-skill-tool-coverage`, `check-skill-description-overlap`, `check-skill-fork-honesty`) exit 0.
  </acceptance_criteria>
  <done>The skill that owns this phase's closure work builds a queue that actually has entries on a derived store, and knows when to stop by measurement rather than by belief.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supplied disagreements document -> the gate | A caller-authored document decides whether a fixture passes |
| store comments -> the decline / accepted / authored censuses | Free-text comments carry machine-read meaning through prefixes |
| the gate's exit code -> the skill's stop condition | A process exit code is trusted as a closure signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-14 | Spoofing | An empty disagreements document with a fabricated or absent run identity | high | mitigate | The run identity must match a row the same store's evid-runs table holds; planted control 2 proves the refusal by observing it red |
| T-45-15 | Repudiation | A comment prefix typo silently dropping an address from the decline census | high | mitigate | The three prefixes are imported from anno-store-export.ts's single declaration; the renderer never re-matches a literal locally, and a test asserts the imported constant is what is matched |
| T-45-16 | Tampering | A softened refusal quietly turning the gate into a bulletin | high | mitigate | Planted controls 1 and 2 are permanent tests asserting exit code 1 and the named literal string, deliberately not derived from the code path they test |
| T-45-17 | Information disclosure | The rendered report echoing absolute host paths | low | accept | The report is a local developer artifact; the committed export carries a basename only (plan 45-02's own control) |
| T-45-SC | Tampering | npm/pip/cargo installs | low | accept | Severity is low HERE because the attack surface is absent, not because the class is minor: no package-manager install task exists in this plan or this phase; 45-RESEARCH.md's Package Legitimacy Audit records "not applicable" |
</threat_model>

<verification>
- `node --test src/skills/routine-queue-walker/scripts/completeness-report.test.mjs` is green with at least 14 tests.
- `cd src/mcp/vice && npm run typecheck` is clean.
- docs/phase45-planted-control-evidence.md carries three OBSERVED RED sections with verbatim output and exit codes.
- All four skill guard scripts exit 0.
- `DATA_TYPES` is unchanged and `anno-coverage.ts` is untouched: `git status --porcelain src/mcp/vice/anno-coverage.ts` is empty.
</verification>

<success_criteria>
- The report measures every one of the five roadmap success criteria and exits non-zero when any of them fails.
- The soundness asymmetry is rendered two ways, aggregate and per range, with `blockCoveredNeverObservedCount` never folded into anything.
- The disagreement input is proven load-bearing by three recorded red runs, two of them pinned as tests.
- The skill's candidate queue fires on a dxa/Ghidra-derived store, and its stop condition is a measured exit code.
</success_criteria>

## Artifacts this phase produces

New JSON answer fields (src/mcp/vice/anno-cli.ts, decomp-completeness --json):
- rangeProvenance, rangeProvenance[].renderedType, rangeProvenance[].typedBy
- entryPoints, entryPoints[].purposeElements.function, .inputs, .outputs, .sideEffects
- referencedAddresses.resolved, .declined, .unresolved, .denominator
- disagreementResolution, disagreementResolution[].accepted, .resolved, unresolvedCount

New provenance vocabulary (values, not identifiers):
- observed-executing, byte-derived, authored

New purpose-comment element labels (authored-comment convention):
- function:, inputs:, outputs:, side effects:

New file:
- docs/phase45-planted-control-evidence.md

<output>
Create `.planning/phases/45-decomposition-to-closure-disagreement-first/45-04-SUMMARY.md` when done
</output>
