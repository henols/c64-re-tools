---
phase: 32-the-deletion-and-the-grep-gate
plan: 09
type: execute
wave: 7
depends_on: ["32-04", "32-05", "32-08"]
files_modified:
  - .github/workflows/ci.yml
  - .planning/phases/32-the-deletion-and-the-grep-gate/COVERAGE.md
  - .planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-close-gate.md
autonomous: true
requirements: [CUT-04, CUT-06]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "[edge:CUT-06/concurrency] The close-gate run is serialized — no two legs run concurrently — and the broker state is READ and RECORDED immediately before the first leg and again after the last. Both readings appear verbatim in the evidence. A run whose two broker readings disagree is VOID and re-run from the start, never merged with partial output; an interrupted leg is re-run whole rather than having its partial output recorded."
    - "The fate guard has its own named CI step alongside the six existing ones, its own file name, and NO entry in `src/mcp/vice/package.json`'s `scripts` block (D-16, audit-gate.mjs D-12-11)."
    - "The build job's `actions/checkout@v4` sets `fetch-depth: 0`, so the pinned commits `0394cbc` and `345d5c4` are in the runner's object store — CI's default clone is shallow (depth 1) and the derivation would otherwise fail on CI."
    - "`.github/workflows/ci.yml` still carries EXACTLY ONE occurrence of the subject literal after the new step lands; the `gate-self` exemption pins it at 1 with `===`, so a step name or run line carrying it would red the removal gate on its own landing commit."
    - "The whole-glob claim is honoured as `test:automated` to a real zero PLUS `test:manual` worked file-by-file, and the union is the whole glob — the split being this repo's own documented seam at `src/mcp/vice/test-gate.mjs:95-105` and `:107-114`, not one invented for the audit."
    - "The mutation harness is invoked from NO CI step and NO package script — it is a committed phase instrument, not a CI job (D-17)."
  prohibitions:
    - statement: "MUST NOT present the close gate as green without stating, in words, that CI's whole-glob `npm test` proves the nine MANUAL_ONLY_TESTS DID NOT FAIL rather than that they exercised anything — every one reaches its default-SKIP branch on the runner. Citing the CI green alone quietly counts nine SKIPs as coverage, which is the exact shape of claim this milestone has already had to correct once (D-14)."
    - statement: "MUST NOT stop, kill, restart, or otherwise modify the developer's `vice-broker` systemd user unit, or any emulator process, at any point in the gate run. Broker state is asserted read-only and recorded; an active broker HALTS the run and is surfaced. A scripted stop was rejected because the broker runs as a systemd unit here and an audit script that tears down the developer's environment is the wrong shape (D-13)."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "fetch-depth: 0 on the build job checkout, and one new named guard step"
      contains: "fetch-depth"
    - path: ".planning/phases/32-the-deletion-and-the-grep-gate/COVERAGE.md"
      provides: "The API-coverage declaration for a phase that integrates no external API"
    - path: ".planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-close-gate.md"
      provides: "D-15's record: raw commands, raw output, broker state and the commit measured, with D-14's SKIP nuance stated in words"
      min_lines: 120
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/check-guard-fates.mjs"
      via: "the new named CI step invokes the guard directly, as its own file name"
      pattern: "check-guard-fates\\.mjs"
    - from: ".planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-close-gate.md"
      to: "src/mcp/vice/test-gate.mjs"
      via: "the automated and manual legs are defined by automatedTestFiles() and MANUAL_ONLY_TESTS, never by a second list"
      pattern: "test-gate"
---

<!-- planner-discipline-allow: regenerator2000 -->
<!-- planner-discipline-allow: check-guard-fates -->

<objective>
Wire the fate guard into CI, then re-run the phase-close gate on the settled tree and record it.

Two deliverables, in this order:

1. **`D-16`'s CI step, plus the checkout fix it needs.** The fate guard gets its own named step
   alongside the six existing `check-*.mjs` steps at `.github/workflows/ci.yml:189-235`, invoked
   directly as its own file name, and stays OUT of `src/mcp/vice/package.json`'s `scripts` block —
   `audit-gate.mjs`'s `D-12-11` states the prohibition and its reason. The step lands NOW, in the wave
   after plan 32-08 took the guard green, so nothing is ever wired into CI while red (ordering
   constraint 5 / the `4f048bb` precedent).
   The checkout fix is not optional: `.github/workflows/ci.yml:29` is a bare `- uses: actions/checkout@v4`
   with no `with:` block, so the runner's clone is SHALLOW (depth 1) and neither pinned commit is in
   its object store. A guard that shells `git ls-tree 0394cbc` fails there. `fetch-depth: 0` on the
   build job's checkout is the fix; the guard already fails closed with a message naming it.

2. **`D-12`–`D-15`'s close-gate re-run**, last, on the settled tree, with the broker asserted down and
   its state recorded beside the results. No single local command produces the whole-glob claim:
   `npm test` is `node --test '*.test.*'` over the full glob including nine `MANUAL_ONLY_TESTS`, one of
   which hangs locally and six of which want a live emulator; `test:automated` is that glob MINUS those
   nine. The split is this repo's own documented seam at `src/mcp/vice/test-gate.mjs:95-105` and
   `:107-114`, not one invented for the audit, and `test-gate.test.ts` is the single source of truth for
   which files are manual-only — do not write a second list.

`D-14`'s honesty clause is a hard requirement of the evidence, not a stylistic preference.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-CONTEXT.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-RESEARCH.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-08-SUMMARY.md
@.planning/phases/29-the-mcp-surface/29-21-SUMMARY.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: The named CI step, the full-history checkout it needs, and the API-coverage declaration</name>
  <precondition>node scripts/check-guard-fates.mjs exits 0 against the completed 60-row registry (delivered by plan 32-08) — wiring a red guard into CI is exactly what ordering constraint 5 and the 4f048bb precedent forbid</precondition>
  <reversibility rating="reversible">Both changes are single YAML edits behind no data or contract: the step can be removed and `fetch-depth` reverted with no migration.</reversibility>
  <files>.github/workflows/ci.yml, .planning/phases/32-the-deletion-and-the-grep-gate/COVERAGE.md</files>
  <read_first>
    - `.github/workflows/ci.yml` — `:25-35` (the build job's bare `actions/checkout@v4` with no `with:` block), `:126-128` ("Generate the shipped skills tree"), `:130-161` (the full-glob `npm test` step, its `VICE_REQUIRE_ACME: "1"` env and the recorded reason from run 32517575905), and `:189-236` (the six existing named `check-*.mjs` steps and the `Build installable package` step that follows). Note the three other checkout sites at `:257`, `:298` and `:348` — only the build job's needs changing
    - `src/mcp/vice/ci-suite-coverage.test.ts` — `:40-110` (the tree walk and `SKIP_DIR_NAMES`) and `CI_YAML_PATH` at `:268`, so the effect of adding a step is checked rather than assumed
    - `src/mcp/vice/package.json` — the `scripts` block, which holds exactly `test`, `test:automated`, `test:manual`, `typecheck`, `build`, `smoke`. Nothing is added to it
    - `scripts/audit-gate.mjs` — header lines 27-34, `D-12-11`: guard files are invoked directly, as their own file names, never through the broader automated-suite entry point
    - `scripts/check-no-regenerator2000.mjs` — the `gate-self` exemption class, which pins `.github/workflows/ci.yml` at exactly 1 occurrence with `===`
  </read_first>
  <action>
**A. Full history for the build job.** Add a `with:` block setting `fetch-depth: 0` to the
`actions/checkout@v4` step at `.github/workflows/ci.yml:29` — the BUILD job only. Leave the three
other checkout sites (`:257`, `:298`, `:348`) unchanged; none of them runs the fate guard. Add a short
comment in the file's existing `# Blocking:` idiom stating WHY: the fate guard derives the audited set
from two pinned commits in the object store, and a depth-1 clone does not contain them.

**B. The new named step.** Place it immediately after the `check-skill-cli-invocations.mjs` step
(currently ending at `:235`) and before `- name: Build installable package`. Match the shape of the six
existing steps exactly: a `# Blocking (CUT-04):` comment stating what breaks FOR THE USER if the gate
is absent, then `- name:` naming the requirement id, then a bare `run: node scripts/check-guard-fates.mjs`.

Suggested wording for the blocking comment, in the register the neighbouring six use: without this
gate a guard can silently drop out of the set the audit protects — the `4f048bb` failure mode, where a
milestone closed with a guard already red and nothing forced anyone to notice — and the recorded fate
for every audited member becomes unenforced prose.

**HARD NAME CONSTRAINT.** `.github/workflows/ci.yml` currently carries EXACTLY ONE occurrence of the
subject literal (the existing removal-gate step's name), and the `gate-self` exemption pins that count
with `===`. Neither the new step's `name:` nor its `run:` nor its comment may contain the literal in
any form. Refer to the audited set by role. A second occurrence reds the removal gate on this step's
own landing commit.

**C. Nothing in the package scripts block.** Do not add `check-guard-fates` or the harness to
`src/mcp/vice/package.json`'s `scripts` (`D-16`, `D-12-11`). Do not add a CI step for the harness at
all (`D-17`: it mutates files and drives the whole guard set — the wrong shape for every-push CI, and
its output is an audit measurement rather than a pass/fail contract).

**D. The API-coverage declaration.** Write
`.planning/phases/32-the-deletion-and-the-grep-gate/COVERAGE.md` containing exactly one reasoned
declaration line in the canonical form:
`No external API integration: <one-line reason>.` The reason is required. This phase integrates no
external API, SDK or service — the deterministic detector returned `detected: false` over the ROADMAP
phase section at plan time — but the phase's own vocabulary is saturated with the word "integration"
because the DELETED SUBJECT was one, so the declaration pre-empts a seal-time re-detection firing on
that vocabulary. State that reason in the line.

**E. Verify the side effects rather than assuming them.** Run `ci-suite-coverage.test.ts` after the
edit: research marks its behaviour toward a new step as NOT MEASURED. Run `check-npm-packages.mjs`
too: research marks the new `scripts/` file as only PROBABLY inert to it. Both are cheap and both are
listed as unmeasured assumptions that this task converts into measurements.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && test $(grep -ac 'regenerator2000' .github/workflows/ci.yml) -eq 1 && grep -q 'fetch-depth: 0' .github/workflows/ci.yml && grep -q 'run: node scripts/check-guard-fates.mjs' .github/workflows/ci.yml && test $(grep -c 'check-guard-fates' src/mcp/vice/package.json) -eq 0 && node scripts/check-no-regenerator2000.mjs && node scripts/check-npm-packages.mjs && cd src/mcp/vice && node --test ci-suite-coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ac 'regenerator2000' .github/workflows/ci.yml` == 1 — unchanged, so the `gate-self` exact-count pin did not move — and `node scripts/check-no-regenerator2000.mjs` exits 0.
    - `.github/workflows/ci.yml` contains `fetch-depth: 0` under the BUILD job's `actions/checkout@v4` only; the checkouts at `:257`, `:298` and `:348` are byte-identical (`git diff` shows one changed checkout hunk).
    - The new step is a bare `run: node scripts/check-guard-fates.mjs` preceded by a `# Blocking (CUT-04):` comment and a `- name:` naming the requirement id, positioned after the `check-skill-cli-invocations.mjs` step and before `- name: Build installable package`.
    - `grep -c 'check-guard-fates' src/mcp/vice/package.json` == 0 and `grep -c 'audit-mutation-harness' .github/workflows/ci.yml src/mcp/vice/package.json` == 0 — the guard is invoked as its own file name only, and the harness is in neither.
    - `cd src/mcp/vice && node --test ci-suite-coverage.test.ts` exits 0 — the new step did not break the CI-step cross-reference.
    - `node scripts/check-npm-packages.mjs` exits 0 — the new `scripts/` files are inert to the tarball validation, measured rather than assumed.
    - `.planning/phases/32-the-deletion-and-the-grep-gate/COVERAGE.md` contains a line beginning `No external API integration:` followed by a non-empty reason.
    - `node scripts/check-guard-fates.mjs` exits 0 — the step being wired in is green at the moment it is wired in.
  </acceptance_criteria>
  <done>The fate guard runs in CI as its own named step against a full-history checkout, the removal gate's `ci.yml` pin is unmoved, neither the guard nor the harness entered the package scripts block, and the API-coverage declaration is on disk.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the phase-close gate on the settled tree, both legs, with the broker asserted and recorded</name>
  <precondition>the vice-broker systemd user unit is inactive — `systemctl --user is-active vice-broker` reports `inactive` and `pgrep -af vice-broker` returns nothing. A live broker makes the BACK-05 D-G ordering test red DETERMINISTICALLY, which would leave every red ambiguous between a real failure and a live broker (D-13). Do NOT stop the unit: assert, record, and HALT if active.</precondition>
  <files>.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-close-gate.md</files>
  <read_first>
    - `src/mcp/vice/test-gate.mjs` — `:95-105` (`MANUAL_ONLY_TESTS`, nine entries, the array closing at `:105`) and `:107-114` (`automatedTestFiles()`). CONTEXT.md cites `:95-104` and `:95-131`; the measured ranges are `:95-105` and `:107-114` and the file is 135 lines — use the corrected ranges
    - `src/mcp/vice/test-gate.test.ts` — all 55 lines. It is the drift guard and the SINGLE SOURCE OF TRUTH for which files are manual-only. Do not write a second list anywhere in the evidence or the run script
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-RESEARCH.md` §7.1 (the nine files and what each needs), §7.2 (`test:automated` measured at 2920 tests / 0 fail / exit 0, broker inactive), §7.3 (all six check scripts green, `audit-gate.mjs --json` reporting 9 derived docs guards), §7.4 (the concrete recipe), §7.5 (all eight landmines)
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-02-SUMMARY.md` — whether `generate-tool-support-table.mjs` writes or only checks on direct invocation, which decides how step 4 of the recipe is run
    - `.github/workflows/ci.yml:130-161` — the full-glob `npm test` step and the recorded evidence from run 32517575905 (2026-08-21)
  </read_first>
  <action>
Work research §7.4's recipe in order, serially — no two legs concurrently — capturing every command and
its raw output as you go.

**0. The precondition, asserted and recorded (`D-13`).** Run and record verbatim:
`systemctl --user is-active vice-broker`, `pgrep -af 'vice-broker'`, `git rev-parse HEAD`,
`git status --porcelain`. If the broker is ACTIVE, HALT and surface it — do not stop it. The tree must
be clean apart from the four pre-existing untracked files
(`docs/dissambler-workflow.md`, `docs/undocumented-opcodes-ghidra.md`, `docs/vice-mcp-ideas.md`,
`skills-lock.json`); a dirty tracked tree voids the run.

**1. The automated leg.** `cd src/mcp/vice && npm run test:automated`. The measured baseline on this
tree is 2920 tests, 0 fail, exit 0, ~50s. There is NO failure baseline: the historical 44-, 7- and
5-failure figures are superseded and any non-zero failure count is a real regression to investigate,
not an accepted floor.

**2. The manual leg, file-by-file.** Derive the nine names from `test-gate.mjs`'s exported
`MANUAL_ONLY_TESTS` — read it, do not retype it — and run each under an explicit timeout, recording the
exit status per file. Run `vice-proxy.test.ts` LAST and under an explicit timeout: it hangs locally, and
a bare `npm test` over the full glob therefore hangs. `/usr/bin/x64sc` on this host is genuine unpatched
stock VICE (the fork shadows it on `PATH`), so the six live-emulator files CAN be run for real locally
rather than left as SKIPs — that is a meaningful upgrade over CI's evidence and the evidence must say
which ones actually exercised something and which reached a SKIP branch.

**3. The six CI check scripts**, each invoked directly as its own file name:
`check-npm-packages.mjs`, `check-no-regenerator2000.mjs`, `check-skill-tool-coverage.mjs`,
`check-skill-fork-honesty.mjs`, `check-skill-description-overlap.mjs`, `check-skill-cli-invocations.mjs`.
CONTEXT.md says "both `check-*.mjs` CI scripts"; there are six, and that phrasing is from the Phase 29
close where two were that phase's own. Run all six. Then run the SEVENTH, this phase's own
`check-guard-fates.mjs`.

**4. `docs/tool-support.md` byte-identical.** Run `node scripts/generate-tool-support-table.mjs`
(default root, no `--root`) then `git diff --exit-code -- docs/tool-support.md`, which must exit 0. If
plan 32-02 recorded that the script only checks rather than writes, use the mode it recorded.

**5. Every `docs-*.test.ts` guard green.** `node scripts/audit-gate.mjs --json` must report
`allowed: true`, `redGuards: []`, `structuralErrors: []`, and 9 derived docs guards matching
`EXPECTED_DOCS_GUARD_NAMES`.

**6. Typecheck.** `cd src/mcp/vice && npm run typecheck` — cheap, and it catches `.d.mts` drift from
this phase's three new declaration files.

**7. Re-assert and re-record the broker state** with the same two commands, and re-record
`git status --porcelain`. If the two broker readings disagree, the run is VOID: discard it, record that
it was voided and why, and re-run from step 0. Do not merge partial output from two runs, and do not
record a leg's partial output if it was interrupted — re-run that leg whole.

**8. Clean up.** Remove any `.planning/vice-proxy-evidence-test-*` directory this run leaked and record
what was removed. `/tmp` is a 16GB RAM tmpfs with aging disabled on this host and the suite leaks into
it; budget for it.

Capture everything raw as you go — plan 32-09's Task 3 writes it up, and a summary table of exit codes
is explicitly NOT what `D-15` asks for.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && systemctl --user is-active vice-broker | grep -qx inactive && cd src/mcp/vice && npm run test:automated && npm run typecheck && cd /home/henrik/dev/henrik/git/c64-re-tools && for s in check-npm-packages check-no-regenerator2000 check-skill-tool-coverage check-skill-fork-honesty check-skill-description-overlap check-skill-cli-invocations check-guard-fates; do node scripts/$s.mjs >/dev/null || exit 1; done && node scripts/generate-tool-support-table.mjs >/dev/null && git diff --exit-code -- docs/tool-support.md && node scripts/audit-gate.mjs --json | grep -q '"allowed":true' && echo CLOSE_GATE_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `systemctl --user is-active vice-broker` reports `inactive` and `pgrep -af vice-broker` is empty, both BEFORE the first leg and AFTER the last, and both readings are captured verbatim. Disagreeing readings void the run.
    - `cd src/mcp/vice && npm run test:automated` exits 0 with a failure count of exactly 0. Any non-zero failure count halts the task as a real regression.
    - All nine `MANUAL_ONLY_TESTS` were run individually with a recorded exit status, the nine names were read from `test-gate.mjs`'s exported array rather than retyped, and `vice-proxy.test.ts` ran last under an explicit timeout.
    - For each of the nine, the capture records whether it EXERCISED its subject or reached a default-SKIP branch — the two are not conflated.
    - All seven `check-*.mjs` scripts exit 0, each invoked directly as its own file name.
    - `node scripts/generate-tool-support-table.mjs` followed by `git diff --exit-code -- docs/tool-support.md` exits 0.
    - `node scripts/audit-gate.mjs --json` reports `allowed: true`, `redGuards: []`, `structuralErrors: []`, 9 derived docs guards.
    - `cd src/mcp/vice && npm run typecheck` exits 0.
    - `git status --porcelain` after the run shows only the four pre-existing untracked files plus this phase's own `.planning/` additions.
    - Every command's RAW output is captured for Task 3; no leg is recorded from memory or as a bare exit code.
  </acceptance_criteria>
  <done>Both legs of the whole-glob claim, all seven check scripts, the table byte-identity, the docs-guard sweep and the typecheck have been run serially on the settled tree with the broker asserted down at both ends, and every raw output is captured.</done>
</task>

<task type="auto">
  <name>Task 3: Write D-15's evidence artifact with D-14's honesty clause stated in words</name>
  <files>.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-close-gate.md</files>
  <read_first>
    - `.planning/phases/29-the-mcp-surface/29-21-SUMMARY.md` — `:176-301`, the model `D-15` names: every measurement command and its raw output, which is the record this milestone's provenance corrections were able to RE-DERIVE FROM. A summary table of exit codes is explicitly not it
    - `.github/workflows/ci.yml:130-161` — the full-glob `npm test` step, its `VICE_REQUIRE_ACME: "1"` env, and the recorded reason from run 32517575905 (2026-08-21) that all nine manual-only files reach their default-SKIP branches on the runner
    - `src/mcp/vice/test-gate.mjs:95-105` and `:107-114` — the seam that defines the two legs
    - `.planning/todos/pending/` — the BACK-05 D-G ordering todo, which is folded here as a RECORDED PRECONDITION rather than as adopted fix work; the todo stays open
    - The raw captures from Task 2
  </read_first>
  <action>
Write `evidence/32-close-gate.md` in `29-21-SUMMARY.md`'s shape.

**Head it with the run's identity:** the commit measured (`git rev-parse HEAD`), the date, the host's
broker state at both ends, and the `git status --porcelain` readings at both ends. `D-15` requires raw
commands, raw output, broker state and the commit measured — all four, not three.

**Body: one section per leg**, each carrying the command AS EXECUTED and its RAW captured output. Not a
summary, not a table of exit codes. The automated leg's full `node --test` tail (tests / suites / pass /
fail / cancelled / skipped / todo / duration_ms). Each of the nine manual-only files individually with
its own command and output. Each of the seven check scripts with its own output line, which for these
scripts carries the measured counts rather than the word OK. The table-generation diff. The
`audit-gate.mjs --json` payload. The typecheck output.

**`D-14`'s honesty clause, in its own titled section, stated in words rather than left inferable.**
Three claims, each separately:
  1. CI runs the FULL `npm test` glob at `.github/workflows/ci.yml:130-161` — deliberately, decided
     from run 32517575905 on 2026-08-21 — and it is green.
  2. All nine `MANUAL_ONLY_TESTS` reach their default-SKIP branches on the runner. **CI's green
     therefore proves they DID NOT FAIL, not that they exercised anything.** Say this sentence, or one
     that means exactly it. Citing the CI green alone quietly counts nine SKIPs as coverage, and that is
     the exact shape of claim this milestone has already had to correct once.
  3. The LOCAL legs are what add real exercise: `/usr/bin/x64sc` on this host is genuine unpatched stock
     VICE, so the six live-emulator files ran for real rather than skipping. State per file which
     actually exercised its subject and which still reached a SKIP branch — the upgrade over CI's
     evidence is only as large as the files that genuinely ran, and overstating it would be the same
     error in the other direction.
  4. The union of `test:automated` (a real zero) and `test:manual` worked file-by-file IS the whole
     glob — cite `automatedTestFiles()` at `src/mcp/vice/test-gate.mjs:107-114` and `MANUAL_ONLY_TESTS`
     at `:95-105` as the seam, and `test-gate.test.ts` as the drift guard that keeps the two sets
     partitioned with no overlap and no gap. Do not restate the nine names as a list; point at the
     guard.

**A recorded-precondition section for the folded todo.** The BACK-05 D-G ordering test fails
DETERMINISTICALLY on a live-broker host — it is not flaky. That is the recorded reason `D-13`'s
broker-down precondition exists. Record it as a known condition of this gate run, state that the todo
in `.planning/todos/pending/` STAYS OPEN, and state that fixing the ordering test is explicitly out of
this phase's scope (ROADMAP.md: this phase contains no build work by design).

**A closing state section:** the post-run broker reading, the post-run tree state, the leaked
directories removed, and — if the run was ever voided and restarted — that fact with its reason.

Every number in the document is one the reader can re-derive from a command printed beside it.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && D=.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-close-gate.md && grep -q 'is-active vice-broker' "$D" && grep -q 'test:automated' "$D" && grep -q '32517575905' "$D" && grep -q 'test-gate' "$D" && grep -q 'BACK-05' "$D" && grep -Eq 'did not fail|DID NOT FAIL' "$D" && node scripts/check-no-regenerator2000.mjs && node scripts/check-guard-fates.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The document records all four things `D-15` requires: raw commands, raw output, broker state and the commit measured — with the broker state and `git status --porcelain` captured at BOTH ends of the run.
    - Every leg carries its command as executed and its raw captured output; no leg is represented only by an exit code or a summary row.
    - The `D-14` honesty section states, in words, that CI's whole-glob green proves the nine manual-only files DID NOT FAIL rather than that they exercised anything, and cites `.github/workflows/ci.yml:130-161` and CI run 32517575905 (2026-08-21).
    - The same section states per manual-only file whether the LOCAL run exercised its subject or reached a SKIP branch — the two are never conflated in either direction.
    - The whole-glob claim is justified by pointing at `automatedTestFiles()` (`test-gate.mjs:107-114`), `MANUAL_ONLY_TESTS` (`:95-105`) and `test-gate.test.ts` as the drift guard — not by restating a second list of the nine names.
    - A recorded-precondition section states that the BACK-05 D-G ordering test reds deterministically on a live-broker host, that it is the reason `D-13`'s precondition exists, that the todo stays open, and that fixing it is out of scope.
    - The closing section records the post-run broker reading, the post-run tree state, any leaked directories removed, and any voided-and-restarted run with its reason.
    - `node scripts/check-no-regenerator2000.mjs` and `node scripts/check-guard-fates.mjs` both exit 0 with this document committed.
  </acceptance_criteria>
  <done>The close-gate run is on disk as a record a later reader can re-derive every figure from, with the SKIP nuance stated plainly in words and the folded BACK-05 precondition recorded rather than fixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow YAML -> the removal gate's exact-count pin | A step name or comment crosses into a file pinned at exactly one occurrence of the subject literal |
| host broker/emulator state -> gate results | An external process the phase does not own decides whether a red is real |
| the evidence artifact -> future readers' claims about coverage | A green recorded without the SKIP nuance becomes a coverage claim nobody can audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-38 | Tampering | the new CI step's text | high | mitigate | Neither the step `name:`, its `run:` line nor its comment may contain the subject literal; `grep -ac 'regenerator2000' .github/workflows/ci.yml` is asserted to remain exactly 1 and the removal gate is run afterwards |
| T-32-39 | Denial of Service | shallow CI clone breaking the derivation | high | mitigate | `fetch-depth: 0` on the build job's checkout, plus the guard's fail-closed `git cat-file -t` assertion naming that fix — a missing pinned object is a loud failure, never a silent degradation to a cached list |
| T-32-40 | Repudiation | nine SKIPs counted as coverage | high | mitigate | `D-14`'s honesty clause is a required, separately-asserted section stating in words that CI's green proves the nine did not fail rather than that they exercised anything, with the per-file local exercised/SKIP breakdown beside it |
| T-32-41 | Spoofing | a live broker making a red ambiguous | high | mitigate | Broker state read and recorded at both ends of the run; a HALT if active; disagreeing readings VOID the run rather than merging partial output |
| T-32-42 | Elevation | an audit script tearing down the developer's environment | medium | mitigate | Broker interaction is read-only (`systemctl --user is-active`, `pgrep -af`); no stop, kill or restart appears anywhere in the plan (`D-13`) |
| T-32-43 | Tampering | `docs/tool-support.md` mutated by the gate run itself | medium | mitigate | The generator is run at the default root and `git diff --exit-code -- docs/tool-support.md` is asserted to exit 0; plan 32-02's `--root` addition kept every planted run out of this file |
| T-32-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no packages; the only npm invocations are the repository's existing `test:automated` and `typecheck` scripts |
</threat_model>

<verification>
- `grep -ac 'regenerator2000' .github/workflows/ci.yml` == 1 and `node scripts/check-no-regenerator2000.mjs` exits 0.
- `node scripts/check-guard-fates.mjs` exits 0 with 60 rows.
- All seven `check-*.mjs` scripts exit 0, invoked directly as their own file names.
- `cd src/mcp/vice && npm run test:automated` exits 0 with 0 failures; `npm run typecheck` exits 0; `node --test ci-suite-coverage.test.ts` exits 0.
- `node scripts/generate-tool-support-table.mjs` then `git diff --exit-code -- docs/tool-support.md` exits 0.
- `node scripts/audit-gate.mjs --json` reports `allowed: true`, `redGuards: []`, `structuralErrors: []`, 9 derived docs guards.
- `systemctl --user is-active vice-broker` reports `inactive` at both ends of the gate run.
- `grep -c 'check-guard-fates\|audit-mutation-harness' src/mcp/vice/package.json` == 0.
</verification>

## Artifacts this phase produces

See the table in `32-01-PLAN.md` → "Artifacts this phase produces" — the canonical list of every
symbol, file and CI step Phase 32 creates. This plan adds the CI step
"Validate every audited guard has a recorded, non-vacuous fate (CUT-04)", `fetch-depth: 0` on the build
job's checkout, `.planning/phases/32-the-deletion-and-the-grep-gate/COVERAGE.md`, and
`.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-close-gate.md`.

<success_criteria>
- The fate guard runs in CI as its own named step, against a checkout that contains the pinned commits, and it was green at the moment it was wired in.
- The removal gate's `ci.yml` occurrence pin is unmoved and all seven check scripts are green.
- Both legs of the whole-glob claim were run serially with the broker asserted down at both ends.
- The evidence artifact carries raw commands, raw output, broker state and the commit measured — and states the SKIP nuance in words.
- The BACK-05 todo is recorded as a precondition and stays open; nothing about it was fixed here.
</success_criteria>

<output>
Create `.planning/phases/32-the-deletion-and-the-grep-gate/32-09-SUMMARY.md` when done
</output>
