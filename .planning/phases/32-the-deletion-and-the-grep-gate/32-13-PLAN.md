---
phase: 32-the-deletion-and-the-grep-gate
plan: 13
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - .planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CUT-04, CUT-06]
net_deletion: false   # pure insertion into two documents; zero deleted lines expected and
                      # asserted. cleanup-wave's predicate is `git diff --diff-filter=D
                      # --name-only` (lib/worktree-safety.cjs:759) — DELETED FILES only; none here.

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md` adjudicates every guard `CUT-04` names by NAME — all eleven of them — not only the requirement's figures. Each name is resolved by a stated, re-runnable rule against the registry's `historicalPath` set at the pinned commits — basename equality, never substring containment — and each is either CITED to its registry row by `historicalPath` and `verdict`, or EXCLUDED with its measured occurrence count at both pinned commits and the place its non-vacuity was discharged instead, or reported AMBIGUOUS with every candidate row listed and none picked. Today the document reconciles the figures and research §1.5's candidate list, and the string `docs-linerefs` occurs in it ZERO times. — TRACE: verifier truth 1 / gaps[0].missing[0]"
    - "`src/mcp/vice/docs-linerefs.test.ts` — the guard `CUT-04` names FIRST and `ROADMAP.md` SC-1 names again — has a written, measured exclusion: 0 subject occurrences at `0394cbc`, 0 at `345d5c4`, not added between the pins, and therefore outside the mechanical predicate by construction rather than by oversight; with its in-band discharge cited to plan 32-04's per-document non-vacuity floor at `docs-linerefs.test.ts:196` and its three planted-violation tests at `:361-383`. — TRACE: verifier truth 1 / gaps[0].missing[0]"
    - "A later reader can tell `we looked and it is fine` from `we never looked` for every name in the requirement's list — D-09's own test, which the verifier recorded as not met for exactly one guard. — TRACE: verifier truth 1 / gaps[0] `reason`"
    - "`CUT-04` and `CUT-06` are NOT demoted. Both keep `[x]` in `.planning/REQUIREMENTS.md` and `Complete` in its traceability table, and the reason is written beside `CUT-04` as a dated note citing the verifier's own recommendation. — TRACE: verifier Requirements Coverage table, the paragraph beginning 'On plan 32-09 flipping both to [x] / Complete'"
    - "[edge:CUT-06/empty] A name in the requirement's list that resolves to ZERO registry rows produces an explicit EXCLUDED verdict carrying its measured occurrence count and its in-band discharge — never an absent table row and never silence. A name resolving to more than one candidate row is REPORTED as ambiguous with every candidate listed, never resolved by preference. And the adjudication text names the deleted subject only inside quoted measurement commands and dated historical citations, so it points no reader at a live deleted route: `node scripts/check-no-analyser.mjs` exits 0 and all nine `docs-*.test.ts` guards stay green. — TRACE: verifier truth 5 (no regression of the ✓ VERIFIED CUT-06 criterion) / gaps[0].missing[0]"
  prohibitions:
    - statement: "MUST NOT make any guard, gate or check green by rewriting a dated record. Every existing section of `32-audited-set-reconciliation.md` stays byte-identical; the adjudication is ADDED as a new, dated section. No `*-SUMMARY.md`, no `32-VERIFICATION.md` and no other `evidence/*.md` file is touched by this plan. (This is the phase's own D-08 standard — only text that tells a reader to use, install or invoke a deleted route is corrected, everything historical stays byte-identical — carried forward, not minted here.)"
    - statement: "MUST NOT record a figure, a verdict or an exclusion that was reasoned rather than measured. Every occurrence count, every commit and every row citation written by this plan is the output of a command quoted beside it, re-run by the executor rather than transcribed from this plan. (Carried forward from D-01's derive-from-disk rule and D-02's 'every addition and removal given a reason', not minted here.)"
  artifacts:
    - path: ".planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md"
      provides: "A new §6.1 adjudicating CUT-04's eleven NAMED guards one by one, each CITED to a registry row or EXCLUDED with its measurement and its in-band discharge"
      contains: "docs-linerefs"
    - path: ".planning/REQUIREMENTS.md"
      provides: "A dated note beside CUT-04 recording that Gap 1 was closed by adjudication rather than by demoting the row, with the verifier's recommendation quoted"
      contains: "docs-linerefs"
  key_links:
    - from: ".planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md"
      to: ".planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json"
      via: "each CITED name resolves to a row by historicalPath and quotes that row's verdict"
      pattern: "guard-fates\\.json"
    - from: ".planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md"
      to: "src/mcp/vice/docs-linerefs.test.ts"
      via: "the EXCLUDED verdict cites the per-document floor and the three planted-violation tests as the in-band discharge"
      pattern: "docs-linerefs\\.test\\.ts"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md"
      via: "the dated note beside CUT-04 points at the new §6.1 as where the named list is adjudicated"
      pattern: "32-audited-set-reconciliation\\.md"
---

<!-- planner-discipline-allow: docs-linerefs -->
<!-- planner-discipline-allow: guard-fates -->
<!-- planner-discipline-allow: anno -->
<!-- planner-discipline-allow: The external analyser -->
<!-- planner-discipline-allow: EXCLUDED -->
<!-- planner-discipline-allow: CITED -->

<objective>
GAP CLOSURE, round 1. **Gap 1 — the smallest fix in this round and the most on-point.**

This is an audit phase whose central deliverable is a *record*, and the one guard its own
requirement names by name is the one guard with no record. `CUT-04`'s text opens its
"Named explicitly:" list with `docs-linerefs`; `ROADMAP.md` SC-1 names it again as one of
the three that went red by construction. It has no row in `guard-fates.json` (61 rows, none
naming it) and zero occurrences in `evidence/32-audited-set-reconciliation.md`.

It is *correctly* outside the mechanical predicate — the verifier measured
`git show 0394cbc:src/mcp/vice/docs-linerefs.test.ts | grep -aic anno` as 0, and it was not
added between the pins — but §6 of the reconciliation reconciles only against research
§1.5's candidate list, and §2 only against `CUT-04`'s FIGURES. Neither reconciles against
`CUT-04`'s NAMES. So the guard falls out of the audit and nobody wrote down that it had.

Implements **D-01** (the recorded fate lives in a committed record, not in a reader's head),
**D-02** (the audited set is reconciled against the requirement's historical list with every
addition and removal given a reason — this plan closes the half of D-02 that reconciled the
numbers but not the names), **D-09** (every member of the swept set gets a row, including
"left unchanged", so a reader can tell "we looked and it is fine" from "we never looked" —
the verifier's stated test, and the one this gap fails), and **D-10** (the widening of
`docs-linerefs.test.ts` onto `PROJECT.md`, which is where this guard's non-vacuity was
discharged in-band instead).

Purpose: the verifier's words — *"Until that exists, a later reader cannot tell 'we looked
and it is fine' from 'we never looked', which is D-09's own test."* This is a paragraph, not
code.

**Take the FIRST of the verifier's two remedies, not the second — and here is the
measurement that decides it.** `gaps[0].missing` offers either an adjudication paragraph OR
a set-D/manual registry row. The registry row is NOT viable: `checkGuardFates()` rejects a
"stranger row" — a row naming a path that is not in the mechanically derived set — at
`scripts/check-guard-fates.mjs:672`, with the message *"This direction is what stops the
registry drifting into a hand-typed second list of members."* Adding a row for
`docs-linerefs.test.ts`, which the derivation correctly does not admit, would turn the
blocking CI gate red for exactly the reason that gate exists. Re-measure this before
starting and record the measurement; do not take it from this plan.

Output: one new dated section in the reconciliation, and one dated note beside `CUT-04`.

**SCOPE FENCE.** Every truth above traces to a verifier truth number or a `gaps[].missing`
line. This plan writes NO code, touches no script, and changes no test. It does not
re-derive set A, set B or set C — those reproduce independently and the verifier
re-derived them. It does not re-open `CUT-06`, which is ✓ VERIFIED. It does not touch
`guard-fates.json`.

**Edge-coverage arithmetic for this plan.** This plan owns 1 of the round's 6 probe items:
`[edge:CUT-06/empty]`, resolved `explicit` and authored as a plain `must_haves.truths`
string above. Round total, stated in plan 32-10: 6 surfaced == 6 authored (5 explicit +
1 backstop) + 0 flagged assumptions. Nothing dropped.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-CONTEXT.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-VERIFICATION.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adjudicate every guard CUT-04 names by name — eleven names, eleven verdicts, one new dated section</name>
  <files>.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the whole `CUT-04` entry, and specifically the sentence beginning "Named explicitly:". That sentence is the input list. Read it character by character; the list is the deliverable's subject and a name misread is a name unaudited.
    - `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md` — §1 (the two pinned commits and the three predicates), §2 and §2.5 (where `CUT-04`'s FIGURES are reconciled), §4 and §4.2 (the forward map and the five renames git's heuristic misses), §6 (the exclusion table whose format §6.1 must match), §9 (the total) and §10 (how to re-derive everything). §6's table shape — Candidate, added between the commits, occurrences at the end pin, verdict — is the shape §6.1 extends.
    - `.planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json` — the row shape: `historicalPath`, `verdict`, `newSubject`, `removalTrigger`, `plant`, `guard`, `observedRed`. Read at least the `docs-dangling-refs` row in full so the citation format quotes real field values.
    - `scripts/check-guard-fates.mjs:660-690` — the stranger-row rule at `:672` and the duplicate rule that follows. This is the measurement that rules out the registry-row remedy.
    - `src/mcp/vice/docs-linerefs.test.ts` — `SCANNED_DOCS`, the per-document floor at `:196`, and the three planted-violation tests at `:361-383`. This is the in-band discharge the exclusion must cite, and it must be cited from having been read.
    - `.planning/ROADMAP.md` § Phase 32 — Success Criterion 1, which names the same three guards again.
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-VERIFICATION.md` — the Gap 1 block in full: its `reason`, its two `artifacts` rows and both `missing` lines.
  </read_first>
  <action>
Add ONE new section, `## 6.1 CUT-04's NAMED list, adjudicated name by name`, immediately
after §6 and before §7. Do not modify a single existing byte of the document. The
adjudication is an insertion; every other section stays as its author wrote it, which is
this phase's D-08 standard and this project's standing rule against fact-laundering.

**A. Open the section with why it exists.** In two or three sentences: §2 reconciled
`CUT-04`'s figures and §6 reconciled research §1.5's candidate list, but neither reconciled
`CUT-04`'s NAMES, so one named guard fell out of the audit with no recorded reason. State
the date, and state that this section was written in gap-closure round 1 after
`32-VERIFICATION.md` recorded it.

**B. Extract the eleven names mechanically from the requirement text**, and quote the
command that extracts them so the list is derived rather than transcribed. Then resolve
each name to a path with a rule that actually works on the names as written.

Most of the eleven are BARE STEMS, not filenames. A rule that requires a tracked path to
equal the token or to end with a separator plus the token — the rule §1.3 states for set C
— resolves a bare stem to ZERO paths, because no tracked path is named `docs-linerefs`.
Do not reuse that rule here. Re-measure this before writing anything: run the stem-equality
rule against `git ls-files` for one bare stem and record what it returns, so the reason
this section uses a different rule is itself a measurement rather than an assertion.

The rule to use instead, and to state in the section so a later reader can re-run it:
resolve each name against the SET OF `historicalPath` VALUES IN `guard-fates.json`, which
are the paths as they stood at the pinned commits — not against `HEAD`'s `git ls-files`.
A name matches a row when the row's `historicalPath` BASENAME is exactly the name, or
exactly the name plus one of `.test.ts`, `.test.mjs` or `.mjs`. Basename EQUALITY, never
substring containment: a substring rule would let one name claim another name's row.

Three consequences to handle explicitly rather than to smooth over:

  - A name matching ZERO rows is not an error in the rule — it is the finding. It gets an
    `EXCLUDED` verdict with its measurement, per section D.
  - A name matching MORE THAN ONE row is reported as AMBIGUOUS in its own row, with EVERY
    candidate listed and none picked. Do not resolve an ambiguity by preference; an
    ambiguity silently resolved is exactly the "name unaudited" failure this section exists
    to close. Report it and say so in the SUMMARY.
  - One name — `docs-anno-decisions` — resolves to zero paths at `HEAD` because the file
    was renamed by plan 29-05, but resolves cleanly against the pinned-commit
    `historicalPath` set. Say in that row which commit its resolution was taken at, and
    name the rename and the plan that made it.

Record, beside the table, the exact rule as a re-runnable command, so a later reader gets
the same eleven resolutions rather than having to reconstruct the rule from the results.

**C. Give every one of the eleven a row** in a table with these columns: Name as written in
`CUT-04` · Resolved path · Verdict (`CITED`, `EXCLUDED` or `AMBIGUOUS`) · Evidence.

For a `CITED` name, Evidence is the registry row's `historicalPath` and its `verdict`,
quoted from `guard-fates.json`. For an `EXCLUDED` name, Evidence is its measured occurrence
count at BOTH pinned commits, whether it was added between them, and where its non-vacuity
was discharged instead. For an `AMBIGUOUS` name, Evidence is EVERY candidate row the rule
returned, listed, with the statement that the ambiguity is reported rather than resolved.

**Every name gets its OWN row and its OWN citation.** Do not collapse two names into one
citation on a belief that they share a registry row. In particular, `tool-support-table`
and `generate-tool-support-table.mjs` look like one guard named twice and are NOT: under
basename equality the first resolves to a `*.test.mjs` guard and the second to a `scripts/`
generator, and `CUT-04`'s list is a list of GUARDS. Re-measure both, and if they do resolve
to two distinct rows, cite each row separately — collapsing them would turn two
independently audited guards into one citation, and this section exists precisely because
a name that nobody resolved is a guard that nobody audited.

The outcome is NOT pre-decided by this plan, and the executor MUST NOT steer the
measurement toward the shape described here. Whatever the resolution rule returns is the
result; a name that comes back with zero rows, with two rows, or with a row this plan did
not anticipate is a FINDING to record in the section and report in the SUMMARY, not a
number to reconcile against an expectation. The only fixed requirement is COMPLETENESS:
eleven names in, eleven rows out, each carrying a verdict backed by a re-run command.

**D. Give `docs-linerefs` a full paragraph, not just a table cell.** It must state, each
with the command that produced it:

  1. Its resolved path, `src/mcp/vice/docs-linerefs.test.ts`.
  2. Its subject-occurrence count at `0394cbc` and at `345d5c4`, measured with `grep -a`.
     The `-a` flag is not optional: a plain `grep` silently skips a file containing a NUL
     byte, and this project has already recorded one false decision from that. State that
     the `-a` was used and why, exactly as `evidence/32-document-sweep.md` does.
  3. Whether it was added between the pins, measured with the diff-filter command §6
     already quotes.
  4. The conclusion that follows: it is outside the mechanical predicate by construction,
     so it owes no registry row and cannot be given one — cite
     `scripts/check-guard-fates.mjs:672`'s stranger-row rule and quote its message, having
     re-measured that the rule is still there and still says that.
  5. Where its non-vacuity WAS discharged: plan 32-04's widening of the guard onto
     `.planning/PROJECT.md`, its per-document floor at `docs-linerefs.test.ts:196`, and its
     three planted-violation tests at `:361-383` — including the one whose own message
     states that a globally summed floor would be satisfied here while the per-document
     floor is not. Cite the plan and the line numbers, and re-measure the line numbers
     before writing them: this repository mechanically checks line citations in
     `CLAUDE.md` and `.planning/PROJECT.md`, and a citation written from memory is the
     class of defect that guard exists to catch.
  6. The explicit statement a later reader needs: **we looked, and it is fine, and here is
     what we measured.**

**E. Close the section by restating the arithmetic — the MEASURED arithmetic, not a
predicted one.** Eleven names in, eleven rows out, and the count of each verdict as the
measurement produced it. State that the derived set is unchanged at 61 — this section adds
no member, removes no member and changes no floor. State that
`node scripts/check-guard-fates.mjs` was re-run afterwards and record its exit status and
its measured line verbatim.

Note on that measured line, so a cross-check does not read as a regression: the gate today
prints `set C parsed from .planning/ROADMAP.md line 830`, while `32-VERIFICATION.md` records
line 814. `32-VERIFICATION.md` is the stale side — it is a dated record of what the gate
printed on the day it was written, and `.planning/ROADMAP.md` has grown since. Re-measure
the current value, record THAT, and do NOT edit `32-VERIFICATION.md` to agree with it. The
requirement is only that the line the gate prints AFTER this task equals the line it printed
BEFORE this task, in the same working tree.

**F. Watch what the prose itself says.** The section quotes measurement commands that
contain the deleted subject's short form. That is measurement text and dated history, both
of which `CUT-06` explicitly permits — but the document must not, in its own voice, tell a
reader to use, install or invoke anything that no longer exists. Re-read the finished
section against that test before committing, and record in the SUMMARY that you did.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools; git diff --numstat -- .planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md; grep -c 'docs-linerefs' .planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md; node scripts/check-guard-fates.mjs; echo "gate=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --numstat` for `32-audited-set-reconciliation.md` shows a DELETIONS count of exactly 0. Every existing section is byte-identical; the change is a pure insertion.
    - `grep -c 'docs-linerefs' .planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md` returns at least 6. It returns 0 at HEAD today.
    - The new section contains exactly 11 adjudication rows, one per name in `CUT-04`'s "Named explicitly:" list — no name absent, no name doubled, and no two names sharing one citation. The count is stated in the section's own closing arithmetic.
    - Every one of the 11 rows carries a verdict, and each verdict carries its own evidence in the shape that verdict owes: `CITED` quotes a `historicalPath` and a `verdict` from `guard-fates.json`; `EXCLUDED` states the measured occurrence count at both pinned commits and where non-vacuity was discharged instead; `AMBIGUOUS` lists every candidate row and states that the ambiguity is reported rather than resolved. No row is blank and no row is a bare verdict word. This criterion measures COMPLETENESS, not a predicted tally — the distribution of verdicts is whatever the resolution rule returned, and a distribution differing from this plan's expectation is a finding reported in the SUMMARY, never a reason to fail the task or to re-steer the measurement.
    - The section states the resolution rule as a re-runnable command, and re-running that command by hand reproduces the same 11 resolutions the table records.
    - Every measurement in the section is accompanied by the command that produced it, and `git show 0394cbc:src/mcp/vice/docs-linerefs.test.ts | grep -aic anno` re-run by hand returns the number the section states.
    - `node scripts/check-guard-fates.mjs` exits 0 and prints the same measured line it printed before this task, both readings taken in this task's own working tree — the derived set and all four floors are unchanged. The before-reading is the baseline, NOT the line quoted in `32-VERIFICATION.md`, which is stale by design.
    - `node scripts/check-no-analyser.mjs` exits 0.
    - Each of the nine `docs-*.test.ts` guards, run individually with `node --test`, exits 0.
    - `git status --porcelain -- .planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md` is the ONLY non-empty result among this task's paths, and `git status --porcelain -- .planning/phases/32-the-deletion-and-the-grep-gate/evidence/ .planning/phases/32-the-deletion-and-the-grep-gate/*-SUMMARY.md .planning/phases/32-the-deletion-and-the-grep-gate/32-VERIFICATION.md` lists no other file. Scoped to paths deliberately: this plan runs in wave 1 under this project's stock worktree isolation, so a whole-tree `git status` is clean here — but a path-scoped predicate stays correct even if the plan is ever run in a shared tree, and an unscoped one would misfire silently.
  </acceptance_criteria>
  <done>
Every guard `CUT-04` names by name has a written verdict backed by a re-run command. The
one it names first is no longer the one nobody wrote about: its exclusion is measured, its
reason is stated, and the place its non-vacuity was actually established is cited. D-09's
test — "we looked and it is fine" versus "we never looked" — is now answerable for all
eleven.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record the non-demotion decision beside CUT-04, where the next reader of the row will find it</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md:136` and its surrounding entry — the whole `CUT-04` block including the "Why this requirement STAYED in Phase 32" sub-bullet, so the new note matches that entry's established voice and does not repeat what is already there.
    - `.planning/REQUIREMENTS.md:246` and `:248` — the traceability rows for `CUT-04` and `CUT-06`.
    - `.planning/REQUIREMENTS.md` — the standing rule this file states about moving a checkbox, a sizing sentence and a traceability row in ONE edit. Follow it: this note does not move any of the three, and the note must say so explicitly.
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-VERIFICATION.md` — the Requirements Coverage table's `CUT-04` row and the paragraph beginning "On plan 32-09 flipping both to `[x]` / Complete". The verifier's recommendation is quoted, not paraphrased.
    - `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md` — the §6.1 Task 1 just wrote, so the note points at a section that exists.
  </read_first>
  <action>
Insert a dated note inside the `CUT-04` entry, after its existing "Why this requirement
STAYED in Phase 32" sub-bullet. Insert only; change no existing byte, no checkbox and no
traceability row.

The note records four things and nothing else:

  1. **What the verifier found**, quoted: `32-VERIFICATION.md` scored `CUT-04`
     ⚠ SATISFIED WITH ONE NAMED EXCEPTION — 61 of 61 fates recorded, 35 of 35
     machine-captured reds with green controls, the guard non-vacuous under ten independent
     registry mutations — but the requirement's own "Named explicitly:" list contained a
     guard the deliverable did not account for.
  2. **The decision taken and by whom**, quoted from the verifier's own recommendation:
     *"My recommendation is to close Gap 1 (a paragraph, not code) rather than to demote the
     row, and to record that decision beside it."* State that the recommendation was
     followed, in gap-closure round 1, on the date of this edit.
  3. **Where the closure lives**: `evidence/32-audited-set-reconciliation.md` §6.1, naming
     the section by its heading, with the one-line statement of what it establishes — all
     eleven named guards adjudicated, each cited to its registry row or excluded with a
     measured occurrence count and its in-band discharge. Quote the verdict counts from
     the section as it was written, rather than from this plan.
  4. **What was deliberately NOT done**, so a later reader does not re-litigate it: the row
     is not demoted; no registry row was added for the excluded guard, because the fate
     guard's stranger-row rule would reject it and turn the blocking CI step red; and no
     `overrides:` entry was written, because the substantive claim was never in dispute and
     recording a divergence as acceptable would defeat this requirement's own purpose.

Do not restate the audit's figures as new measurements — cite them to
`32-VERIFICATION.md`, which measured them. This note is a decision record, not a second
opinion about the numbers.

Make the edit by hand, as a scoped replacement of the smallest surrounding region that
makes the insertion unambiguous. Do not rewrite the file, do not regenerate it from any
tool, and do not run any command that re-serializes its front matter — this project has a
recorded failure where a one-field write flattened block scalars across a whole document.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools; git diff --numstat -- .planning/REQUIREMENTS.md; grep -c '\- \[x\] \*\*CUT-04\*\*' .planning/REQUIREMENTS.md; grep -c '| CUT-04 | Phase 32 | Complete |' .planning/REQUIREMENTS.md; grep -c '| CUT-06 | Phase 32 | Complete |' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --numstat` for `.planning/REQUIREMENTS.md` shows a DELETIONS count of exactly 0.
    - `grep -c '\- \[x\] \*\*CUT-04\*\*' .planning/REQUIREMENTS.md` returns 1 — the checkbox is unchanged and still checked.
    - `grep -c '| CUT-04 | Phase 32 | Complete |'` returns 1 and `grep -c '| CUT-06 | Phase 32 | Complete |'` returns 1 — neither traceability row moved.
    - The note quotes the verifier's recommendation verbatim, including the phrase "a paragraph, not code", and names `32-audited-set-reconciliation.md` §6.1 by its heading.
    - The note states all four of: the finding, the decision, where the closure lives, and what was deliberately not done.
    - `git diff --name-only` for this task lists exactly `.planning/REQUIREMENTS.md`.
    - No `*-SUMMARY.md`, no `32-VERIFICATION.md` and no `evidence/*.md` file is modified by this task.
  </acceptance_criteria>
  <done>
The next reader of `CUT-04`'s row finds, beside it, what the verifier found, what was
decided, where the closure lives, and what was deliberately not done — instead of a bare
checked box whose one named exception is only discoverable from a verification report.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requirement text → derived name list | The eleven names are extracted from prose written by a human and resolved to tracked paths; a misread name is a guard silently unaudited. |
| dated evidence record → this round's edit | Two committed documents are edited in a round whose purpose is to close a gap a verifier found — the exact situation in which fact-laundering happens. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-09 | Repudiation | `evidence/32-audited-set-reconciliation.md`, `.planning/REQUIREMENTS.md` | high | mitigate | Editing a dated record to turn a finding green destroys the record's value, and this project has caught it twice. Mitigated structurally rather than by intent: both edits are asserted to be PURE INSERTIONS with a measured deletions count of exactly 0, the acceptance criteria forbid touching any `*-SUMMARY.md`, `32-VERIFICATION.md` or other `evidence/*.md`, and every figure written must be re-measured with its command quoted beside it rather than transcribed from this plan. |
| T-32-10 | Tampering | the `grep` predicate used for the occurrence measurements | high | mitigate | A plain `grep` silently skips `src/mcp/vice/anno-memmap-render.ts`, which contains a NUL byte, and that has already produced one false decision in this project. Every occurrence measurement in this plan must use `grep -a`, and the section must state that it did and why. A census taken without it would produce a written exclusion resting on a wrong number — the precise defect this gap exists to close. |
| T-32-11 | Denial of service | the blocking CI fate gate | high | mitigate | Taking the verifier's second remedy — a manual registry row for the excluded guard — would trip the stranger-row rule at `scripts/check-guard-fates.mjs:672` and turn a blocking CI step red. The plan takes the first remedy, states the measurement that rules out the second, and requires that measurement to be re-run rather than trusted. `node scripts/check-guard-fates.mjs` exiting 0 is an acceptance criterion of Task 1. |
| T-32-SC | Tampering | package-manager installs | — | not applicable | This plan runs no package-manager install and adds no dependency. Measured: `files_modified` contains only two markdown documents. No package-legitimacy checkpoint is owed. |
</threat_model>

<verification>
1. `git diff --numstat` on both files — deletions exactly 0 on each.
2. `grep -c 'docs-linerefs'` on the reconciliation — at least 6, against a HEAD baseline of 0.
3. The eleven adjudication rows counted — one per name, none absent, none doubled, none sharing a citation — and each row's verdict paired with the evidence that verdict owes.
4. `git show 0394cbc:src/mcp/vice/docs-linerefs.test.ts | grep -aic anno` re-run — matches the written figure.
5. `node scripts/check-guard-fates.mjs` — exit 0, same measured line as before.
6. `node scripts/check-no-analyser.mjs` — exit 0.
7. The nine `docs-*.test.ts` guards, each run individually — all exit 0.
8. The three `CUT-04` / `CUT-06` checkbox and traceability greps — each returns 1.
</verification>

<success_criteria>
- Every guard `CUT-04` names by name has a written, measured verdict.
- The excluded guard's exclusion states what was measured, why the predicate cannot reach it, and where its non-vacuity was discharged instead.
- Neither requirement row was demoted, and the reason is recorded beside the row.
- Both documents changed only by insertion; no dated record was rewritten.
- The blocking CI gate is unchanged and still green.
</success_criteria>

<artifacts_this_phase_produces>
Every symbol, path and flag this ROUND creates, marked with its creating plan, so the
source-grounding pass excludes them from drift verification.

**New exported functions**
- `parseRootArg(argv, { script, booleanFlags })` — `scripts/lib/audit-root.mjs` (32-10)

**New files**
- `src/mcp/vice/audit-root-args.test.ts` (32-10, extended by 32-11 and 32-12)
- `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-gap2-root-seam-coverage.md` (32-12)
- `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-gap3-harness-correction.md` (32-14)

**New declarations**
- `parseRootArg` in `scripts/lib/audit-root.d.mts` (32-10)

**New message literals introduced as contract surface**
- `BAD ARGUMENTS --` argument-rejection prefix (32-10)
- `SPLIT READ REFUSED --` refusal prefix in four scripts (32-12)

**New document sections**
- `## 6.1 CUT-04's NAMED list, adjudicated name by name` in
  `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md`
  (32-13, this plan)
- A dated Gap-1 closure note beside `CUT-04` in `.planning/REQUIREMENTS.md` (32-13, this plan)
- A dated correction block appended to
  `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-sweep-renamed-rows.md` (32-14)

**New gitignore entries**
- `.audit-root-synth-*` (32-11)

**New CLI flags**
- None.

**New registry fields**
- None. This plan does not touch `guard-fates.json` at all. Plan 32-14 re-measures two field
  values on ONE existing row; it adds no field and no row.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/32-the-deletion-and-the-grep-gate/32-13-SUMMARY.md` when done.
Record: the eleven extracted names and the command that extracted them; each name's
resolved path and verdict; the re-measured occurrence counts at both pins for the excluded
guard; the re-measured stranger-row rule and its line number; the fate gate's exit status
and measured line before and after; both files' `git diff --numstat` deletion counts; and
the explicit statement that the finished prose was re-read against the "does it route a
reader at something deleted" test.
</output>
