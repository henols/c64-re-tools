---
phase: 32-the-deletion-and-the-grep-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/audit-root.mjs
  - scripts/lib/audit-root.d.mts
  - scripts/check-guard-fates.mjs
  - scripts/check-guard-fates.d.mts
  - scripts/audit-mutation-harness.mjs
  - src/mcp/vice/guard-fates.test.ts
  - .planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json
  - .planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-tracer-observed-red.md
  - .planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md
autonomous: true
requirements: [CUT-04]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "[edge:CUT-04/adjacency] A path that is both a set-A member's newSubject and a set-B candidate produces EXACTLY ONE registry row; the guard fails on a duplicate historicalPath and on a duplicate newSubject."
    - "[edge:CUT-04/empty] A derived set of zero members, a registry with zero rows, or a row count below the recorded floor is a HARD FAILURE, never a vacuous green; the guard's success line prints the derived-set size, the row count and the floor."
    - "[edge:CUT-04/concurrency] A harness run interrupted by SIGINT, SIGTERM, an uncaught exception or a plain process exit restores every captured original byte-for-byte, idempotently, and `git status --porcelain` returns to its pre-run value."
    - "`node scripts/audit-mutation-harness.mjs --row src/mcp/vice/r2000-verb-coverage.test.ts` runs the unplanted guard as a GREEN false-positive control and requires exit 0, then plants, runs the guard with every NODE_TEST_* key stripped from the child env, observes a NON-ZERO exit, reverts, and writes both halves' raw commands and raw output to the evidence file. A red with no green control is recorded as UNMEASURABLE, never as an observed red."
    - "`node scripts/check-guard-fates.mjs` derives 43 set-A members at 0394cbc, 15 net-new set-B members at 345d5c4, and 2 set-C members parsed from ROADMAP.md's deferred-fates note, for 60 total — every one from the git object store or a committed document, with no hand-typed list of member paths anywhere in the file."
    - "`node scripts/check-guard-fates.mjs --root <synthetic dir>` operates entirely inside that directory and refuses any --root that resolves outside the repository root."
    - "Deleting a row from the registry object, or adding a row naming a path outside the derived set, makes the guard's exported predicate report the discrepancy — proven by a test that drives the real exported predicate, never a re-implementation."
  prohibitions:
    - statement: "MUST NOT record a fate row's observed-red evidence from anything other than a machine-captured, non-zero exit status of the named guard run by the harness — no hand-written transcript, and no reuse of a guard's existing in-test non-vacuity assertion as the proof (D-04: 'the guard asserts it can fail' and 'the guard was observed failing against its new subject' are different claims)."
    - statement: "MUST NOT introduce a waiver file, an environment-variable override, a skip flag, or any other relaxation hatch into the fate guard, the harness, or the root helper (audit-gate.mjs D-12-14 — testability comes from --root only)."
  artifacts:
    - path: "scripts/check-guard-fates.mjs"
      provides: "Derive-from-disk fate guard: two-pinned-commit set derivation, per-verdict evidence rules, floors, --root"
      exports: ["deriveAuditedSet", "checkGuardFates", "SET_A_FLOOR", "SET_B_FLOOR", "SET_C_FLOOR", "TOTAL_FLOOR"]
    - path: "scripts/check-guard-fates.d.mts"
      provides: "Ambient declarations so src/mcp/vice/guard-fates.test.ts typechecks under strict mode"
      contains: "export declare"
    - path: "scripts/audit-mutation-harness.mjs"
      provides: "Plant -> run one guard -> assert non-zero -> revert loop with restore-on-exit"
      contains: "NODE_TEST_"
    - path: "scripts/lib/audit-root.mjs"
      provides: "resolveContainedRoot(): resolve a --root argument and refuse anything outside the repo"
      exports: ["resolveContainedRoot"]
    - path: "scripts/lib/audit-root.d.mts"
      provides: "Ambient declaration for the root helper"
    - path: "src/mcp/vice/guard-fates.test.ts"
      provides: "The fate guard's own non-vacuity proof (delete a row -> red; stranger row -> red; empty registry -> red)"
      min_lines: 60
    - path: ".planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json"
      provides: "The fate registry: auditCommit, auditEnd, floors, and the tracer row"
      contains: "auditCommit"
    - path: ".planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-tracer-observed-red.md"
      provides: "Raw command and raw output of the tracer member's observed red"
    - path: ".planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md"
      provides: "D-02's reconciliation of CUT-04's historical 43 against the settled tree, every addition and removal reasoned"
  key_links:
    - from: "scripts/audit-mutation-harness.mjs"
      to: ".planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json"
      via: "reads each row's plant descriptor and writes back the captured observedRed"
      pattern: "guard-fates\\.json"
    - from: "scripts/check-guard-fates.mjs"
      to: "scripts/lib/audit-root.mjs"
      via: "imports resolveContainedRoot for the --root path-containment refusal"
      pattern: "resolveContainedRoot"
    - from: "src/mcp/vice/guard-fates.test.ts"
      to: "scripts/check-guard-fates.mjs"
      via: "imports the real exported predicate and drives it against synthetic registry objects"
      pattern: "check-guard-fates\\.mjs"
---

<!-- planner-discipline-allow: regenerator2000 -->
<!-- planner-discipline-allow: NODE_TEST_ -->
<!-- planner-discipline-allow: execSync -->
<!-- planner-discipline-allow: guard-fates -->
<!-- planner-discipline-allow: r2000 -->

<objective>
TRACER SLICE. Build the whole fate-audit machine and prove it end-to-end on ONE
real audited-set member, before any expansion: registry row -> derive-from-disk
guard sees it -> harness plants that guard's violation -> guard observed
non-zero -> harness reverts cleanly -> the row's raw evidence is recorded.

Implements D-01 (registry + derive-from-disk guard), D-02 (re-derive the set and
reconcile it against the requirement's historical list), D-03 (four-field rows),
D-04 (a committed, re-runnable mutation harness produces the evidence), D-05
(typed rows, verdict decides the evidence owed) and D-06 (planted violations
against a synthetic tree via `--root` wherever available).

Purpose: prove the architecture on the agent's best early-context tokens. If the
harness cannot observe a red against a real member, every later expansion task is
built on a vacuous audit — which is precisely the failure CUT-04 exists to catch.
Output: the guard, its type declarations, its own non-vacuity test, the root
helper, the harness, a registry seeded with the tracer row, the tracer's raw
evidence, and D-02's set reconciliation.

ACCEPTED CONTEXT OVERRUN (recorded, not hidden). This plan is estimated at 105k against
the 100k smart-zone budget. Re-slicing was considered and rejected: 3 tasks and 9 files
are inside band, and the tracer's whole value is one agent carrying registry -> guard ->
harness -> observed red -> revert in a single context. Splitting it would put the
architecture proof across two contexts, which is the failure the tracer exists to avoid.

PRE-DECLARED RED (Sequencing constraint 8 discipline). `scripts/check-guard-fates.mjs`
is EXPECTED to exit non-zero from this commit until plan 32-08 completes the
registry. It is deliberately NOT wired into CI in this plan; its CI step lands in
plan 32-09, in the wave after the registry becomes complete and green. Nothing in this phase
is recorded green over it while it is red (ordering constraint 5 / the `4f048bb`
precedent). This pre-declaration is the record that the red is predicted.
</objective>

<assumption_delta_decision>
The `assumption-delta scan` detector fired `pluralization` on the term "second".
Re-read on the phase scope, the genuine identity-model question is: this phase
adds a **second** record of guard membership beside `audit-gate.mjs`'s
`EXPECTED_DOCS_GUARD_NAMES`.

- **Noun now primary:** the audited-set member, keyed on `historicalPath @ auditCommit`.
- **Decision: `promote`.** The registry's primary key is the historical path at
  the pinned commit `0394cbc`, NOT the path on the current tree. 22 of the 43
  set-A members do not exist at HEAD at all, so "file on disk now" cannot be the
  identity. `EXPECTED_DOCS_GUARD_NAMES` keeps its own, narrower job (the nine
  `docs-*.test.ts` guards at HEAD) and is NOT merged into or duplicated by this
  registry — they name different nouns for different sets.
- **Invariant test (accepted):** the guard asserts a bijection between derived-set
  members and rows in both directions, so a future rename cannot silently reintroduce
  the "path on disk now" identity without failing.
</assumption_delta_decision>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-CONTEXT.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-RESEARCH.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-PATTERNS.md
@scripts/audit-gate.mjs
@scripts/check-no-regenerator2000.mjs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: One audited-set member, end to end — derivation, registry, guard, harness, observed red, reverted</name>
  <reversibility rating="costly">D-01: the registry becomes an artifact later phases must keep fed, and its guard reds on any future guard rename. Carried forward from CONTEXT.md, not re-derived.</reversibility>
  <files>scripts/lib/audit-root.mjs, scripts/check-guard-fates.mjs, scripts/audit-mutation-harness.mjs, .planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json, .planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-tracer-observed-red.md</files>
  <read_first>
    - `scripts/audit-gate.mjs` — the WHOLE header block (D-12-07 derive-from-disk, D-12-11 own-file-name invocation, D-12-14 no-relaxation-hatch, the ASVS V5 never-shell-execute-scanned-text rule), then `DOCS_GUARD_FLOOR` at `:112` with its comment block at `:96-111`, `docsGuardFiles()` at `:171-176`, `GUARD_RUN_TIMEOUT_MS` at `:201`, `runGuardsLive()` at `:203-228` INCLUDING its recorded reason, the fail-closed error mapping at `:229-241`, and `parseArgs()` at `:1118` with the WR-03 try/catch that follows it
    - `scripts/check-no-regenerator2000.mjs` — `:114-125` (imports, module-scope root, `errors`/`need()`), `:138-146` (path composition from `SUBJECT_NEEDLE`), `:178-200` (`PLANNING_PREFIX` and `trackedFiles()`), `:217-222` (the exact-count rule), `:891-899` (the `===` assertion, both directions named), `:754-768` (the entry-point guard), and the tail report block
    - `scripts/lib/anno-cli-verbs.mjs` — `:83-86`, where `ANNO_CLI_VERB_FLOOR` is exported (this is the tracer's plant target)
    - `src/mcp/vice/anno-verb-coverage.test.ts` — `:24` (the import) and `:184-188` (the floor assertion this plant must red)
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-RESEARCH.md` §1.3 (the derivation predicate), §1.4 (the 21/16/6 forward map), §1.5 (the new-guard union), §3.1-3.3 (the harness), §4.1-4.2 (the registry schema and the guard's four checks)
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-PATTERNS.md` — the `check-guard-fates.mjs` section, HAZARD 1, HAZARD 2, and the mutation-harness section
  </read_first>
  <action>
Build the four artifacts and drive ONE member through all of them.

**A. `scripts/lib/audit-root.mjs` — the containment refusal that does not exist to copy.**
Export `resolveContainedRoot(rootArg, { repoRoot, allowExtra = [] })`. It `resolve()`s
`rootArg` (defaulting to `repoRoot`), then REFUSES — by throwing an `Error` whose message
names the offending resolved path and the containing root — any result that is not
`repoRoot` itself, not a descendant of `repoRoot`, and not a descendant of any `allowExtra`
entry. Use a path-segment-boundary comparison (`resolved === base || resolved.startsWith(base + sep)`),
never a bare `startsWith`, so `/repo-evil` cannot pass as inside `/repo`. Research §Security
cites `audit-gate.mjs:1147-1151` for this; PATTERNS measured that citation as WRONG — that
code is the WR-03 try/catch for a mistyped root and there is NO containment check anywhere
in the file. Write it new.

**B. `scripts/check-guard-fates.mjs` — the derive-from-disk fate guard.**
Module shape copied from `check-no-regenerator2000.mjs`: an `errors` array, a `need(cond, msg)`
accumulator, an `IS_ENTRY_POINT` guard so a colocated test can import the predicates without
running the scan, and a tail report block that prints the MEASURED numbers on success
(derived-set size, row count, each floor) rather than the word OK.

Pin two commits as module constants: `AUDIT_COMMIT = "0394cbc"` (the v0.7.0 open; both of
CUT-04's historical figures reproduce there) and `AUDIT_END = "345d5c4"` (the settled tree
measured at plan time). Assert BOTH objects are present with `git cat-file -t` before any
derivation, and on absence fail with a message naming `fetch-depth: 0` as the fix — CI's
`actions/checkout@v4` at `.github/workflows/ci.yml:29` has no `with:` block, so the runner's
clone is shallow (depth 1) and neither object is present until plan 32-08 lands `fetch-depth: 0`.
This assertion is fail-closed: never silently degrade to a cached list.

Derive the set as a UNION of two mechanically-derived halves, with no hand-typed member paths:
  - **Set A (43, at `AUDIT_COMMIT`)** — over `git ls-tree -r --name-only <AUDIT_COMMIT> -- src/mcp/vice scripts`:
    a path qualifies if it matches `^src/mcp/vice/.*\.test\.[a-z]+$` AND (its basename starts
    with `r2000-` OR `git show <AUDIT_COMMIT>:<path>` matches `/r2000/i`); OR it matches
    `^scripts/` AND (the path contains `r2000` OR its content at `AUDIT_COMMIT` matches `/r2000/i`).
    This yields 19 + 13 = 32 test files and 11 `scripts/` files — CUT-04's two historical
    figures, both reproduced at plan time.
  - **Set B (15 net-new, at `AUDIT_END`)** — over `git diff --name-only --diff-filter=A <AUDIT_COMMIT> <AUDIT_END> -- scripts src/mcp/vice`:
    a path qualifies if it matches `^src/mcp/vice/.*\.test\.[a-z]+$` OR `^src/mcp/vice/fixtures/planted-`
    OR `^scripts/`, AND (the path contains `r2000` OR its content at `AUDIT_END` matches `/r2000/i`).
    Then SUBTRACT every path that is already some set-A row's `newSubject` — that subtraction is
    the adjacency rule, and it is why a renamed member gets one row and not two. Measured at plan
    time: 22 raw candidates minus 7 already-claimed newSubjects = 15.
  - **Set C (2, from the ROADMAP)** — the two guard fates `ROADMAP.md` § Phase 32 explicitly DEFERS
    to this phase are pinned to the deleted subject through the twelve committed coverage fixtures
    rather than through their own text, so neither the set-A nor the set-B predicate reaches them.
    Derive them from the document that mandates them rather than typing them: read
    `.planning/ROADMAP.md`, isolate the `### Phase 32` section and within it the note beginning "Two
    guard fates were DEFERRED", extract every backticked token ending in `.ts`, `.mjs` or `.d.mts`,
    and resolve each against `git ls-files` requiring EXACTLY ONE match (the note names one by bare
    basename and one by full path). Measured at plan time this yields `src/mcp/vice/block-class.ts`
    and `src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs`. Apply the same adjacency
    subtraction against set-A `newSubject`s and set B. A parse that yields anything other than 2 is a
    hard failure naming the note — the ROADMAP is the authority, and a note reworded so the parse
    finds nothing must be loud, not silent.

Export `deriveAuditedSet({ root })` returning `{ setA, setB, setC, union }` with every array
`.sort()`ed so two runs on the same commits produce byte-identical output. Export four floors as
named constants — `SET_A_FLOOR = 43`, `SET_B_FLOOR = 15`, `SET_C_FLOOR = 2`, `TOTAL_FLOOR = 60` —
each with a comment in `DOCS_GUARD_FLOOR`'s idiom stating that a floor moves ONLY in the same commit
that changes the derivation, and naming what a silently-shrinking derivation would mean.

Export `checkGuardFates({ derived, registry })` (pure, no I/O — this is what the test drives) which
accumulates errors for:
  1. every derived member with no row, named individually;
  2. every row whose `historicalPath` is not in the derived set, named individually (the inverse
     direction — this is what stops the registry drifting into a hand-typed second list);
  3. duplicate `historicalPath` values, and duplicate non-null `newSubject` values;
  4. `derived.union.length < TOTAL_FLOOR`, `setA.length !== SET_A_FLOOR`, `setB.length !== SET_B_FLOOR`,
     `setC.length !== SET_C_FLOOR`;
  5. `registry.rows.length === 0` or `derived.union.length === 0` — an empty set or an empty
     registry is a structural failure, not a pass;
  6. the per-verdict evidence rules from D-05, verdict being one of exactly
     `re-pointed | deleted | superseded | kept-unchanged`:
       - `re-pointed`: `newSubject` non-null and exists on the working tree; `observedRed` present
         with a non-zero integer `exitStatus`, a non-empty `command`, a non-empty `excerpt`, and a
         `control` whose `exitStatus` is exactly 0 — the green false-positive control without which a
         red proves nothing;
       - `deleted`: `newSubject === null`; the `historicalPath` absent from the working tree;
         `removingCommit` present and non-empty;
       - `superseded`: `newSubject` non-null and exists; the replacement's own `observedRed` present
         with a non-zero `exitStatus`;
       - `kept-unchanged`: `newSubject === historicalPath`; the path exists; no `observedRed` owed;
         `removalTrigger` REQUIRED (this is the field the two deferred fates need).
  7. `removalTrigger` present and non-empty on every row whose verdict is not `deleted`.
Mirror `check-no-regenerator2000.mjs:891-899`'s message style: state what a HIGHER count means
and what a LOWER count means, in the message itself.

Add `--root <dir>` via a `parseArgs()` copied from `audit-gate.mjs:1118`, resolved through
`resolveContainedRoot()`, and wrap every call that can throw on a bad root in one try/catch so a
typo and a refusal are distinguishable — the WR-03 contract.

**Name discipline (HAZARD 1, hard).** `scripts/check-guard-fates.mjs`, `scripts/lib/audit-root.mjs`,
`scripts/audit-mutation-harness.mjs` and `src/mcp/vice/guard-fates.test.ts` are all inside the
removal gate's scope (`git ls-files` minus the `.planning/` prefix). The removal gate asserts EXACT
hit counts with `===` per exemption. None of these four files may contain the subject literal in any
form — not in a name, not in a comment, not in an error message, not in a sample path. Where one of
them must refer to the gate script by path, compose it the way the gate composes its own path at
`scripts/check-no-regenerator2000.mjs:139-146` (`"regenerator" + "2000"`), or refer to it by role
("the removal gate") instead of by file name. The registry JSON is exempt because
`.planning/` is excluded by PREFIX at `scripts/check-no-regenerator2000.mjs:180`.

**C. `scripts/audit-mutation-harness.mjs` — the phase instrument (D-04, D-17).**
CLI: `node scripts/audit-mutation-harness.mjs (--row <historicalPath> | --rows <path,path,...> | --all) [--root <dir>] [--out <file>]`.
Row selection is EXACTLY ONE of the three: `--row` (a single `historicalPath`), `--rows` (a
comma-separated list of `historicalPath`s — this is how a sweep plan measures only the rows it owns
instead of re-running rows an earlier wave already measured), or `--all` (every row in the registry).
Zero or more than one of the three is a usage error, never a silent full run. An entry in `--rows`
that matches no registry row is a HARD FAILURE naming the offending entry — never a silent skip — and
every run prints the number of rows it measured, so a truncated or mistyped selector is visible
rather than quietly shrinking a sweep. Match `--rows` entries against `historicalPath` only; matching
loosely against `newSubject` too would let one entry select two rows.
D-17: it is a committed instrument invoked by hand. It goes into NO CI step and NO entry in
`src/mcp/vice/package.json`'s `scripts` block (D-16 / `audit-gate.mjs` D-12-11).

Per row, in this order:
  1. `assertTreeClean()` — run `git status --porcelain` and compare against a baseline captured
     ONCE at process start. Four untracked files (`docs/dissambler-workflow.md`,
     `docs/undocumented-opcodes-ghidra.md`, `docs/vice-mcp-ideas.md`, `skills-lock.json`) are
     pre-existing on this host; the comparison must be against the captured baseline, not against
     an empty string, or every row fails its precondition.
  1b. **The green false-positive control, before any plant.** Run the row's `guard` descriptor
     UNPLANTED and require exit 0. Phase 29 proved its own gate with "four planted routes plus the
     exemption non-vacuity plant, with a green false-positive control", and the control is what makes
     a subsequent red mean something. Without it, a guard that is red for an unrelated reason — or one
     that TIMES OUT, which the fail-closed `result.error` mapping reports as status 1 — records a
     false observed red, which is precisely the vacuity CUT-04 exists to catch. If the unplanted
     control does not go green, do NOT proceed to the plant: record the row as UNMEASURABLE with the
     control's raw output and the reason, and surface it. Store the control's exit status and output
     in the row's `observedRed.control` field so the evidence carries both halves.
  2. Plant. Prefer the row's `--root` synthetic-fixture route (D-06); where the row's descriptor
     says working-tree mutation, `readFileSync` the target's ORIGINAL BYTES FIRST — with no encoding
     argument, so a NUL-carrying file round-trips byte-identically — register the restore, then write.
  3. Run only that guard, from the row's own `guard` descriptor `{ argv: string[], cwd: string }` —
     the harness spawns exactly that argv and branches on nothing. Two conventions cover most rows and
     a third is needed for type declarations, so keep the descriptor general rather than special-casing:
       - a `src/mcp/vice/*.test.*` subject: `argv: ["--test", "<basename>"]`, `cwd: "src/mcp/vice"`;
       - a `scripts/*.mjs` subject: `argv: ["scripts/<name>.mjs", ...flags]`, `cwd: "."`;
       - a `scripts/lib/*.d.mts` subject, whose only mechanical check is the typechecker:
         `argv: ["--run", "typecheck"]` executed through the npm client, `cwd: "src/mcp/vice"`.
     `cwd` is always resolved relative to the run's contained root; `argv[0]` is `process.execPath` for
     the first two and the npm client for the third. Every element is a literal from the descriptor —
     never an interpolated shell string.
     `spawnSync(<bin>, <argv>, { cwd: <resolved>, encoding: "utf8", env, timeout: 15000, killSignal: "SIGKILL" })`.
     **HAZARD 2 — copy `audit-gate.mjs:203-228`'s env strip verbatim: build `env = { ...process.env }`
     and `delete` EVERY key beginning with `NODE_TEST_` before spawning.** Without it a nested
     `node --test` switches to the IPC/v8-serialization reporter and a genuinely failing guard
     returns exit 0 with empty output — the harness would then record every planted red as a green
     and the whole audit would be vacuous, which is exactly the failure CUT-04 exists to catch.
     Copy the fail-closed `result.error` mapping at `:229-241` too, so an ETIMEDOUT is a red rather
     than a hang.
  4. Assert `result.status !== 0`. A zero status is a HARD FAILURE of the harness run, reported with
     the row, the command and the captured output — never downgraded to a warning.
  5. Record `{ command, cwd, plant, exitStatus, excerpt }` where `excerpt` is the first failing
     assertion text lifted from the captured stdout/stderr.
  6. Revert, then `assertTreeClean()` again against the same baseline.

Restore-on-exit: hold the captured originals in a module-level map; register one idempotent
`restoreAll()` (guarded by a `restored` flag, since `finally` and `exit` both fire) in a `try/finally`
AND on `process.on("exit")`, `process.on("SIGINT")`, `process.on("SIGTERM")` and
`process.on("uncaughtException")`. A crashed harness leaving a plant in the tree would corrupt this
phase's own close-gate run, which happens on that tree (D-04).

ASVS V5: every `spawnSync` receives an argv ARRAY; never set `shell: true`; never `execSync` an
interpolated string; never evaluate, `import()` or shell-execute any text read from the registry or
from a scanned file. Resolve every plant target through `resolveContainedRoot()` before writing.

**D. The registry and the tracer row.**
Write `.planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json` with top-level
`auditCommit: "0394cbc"`, `auditEnd: "345d5c4"`, `setAFloor: 43`, `setBFloor: 15`, `setCFloor: 2`,
`totalFloor: 60` (43 + 15 + 2 — the union INCLUDES set C; a registry writing 58 here contradicts
`TOTAL_FLOOR` and reds the guard for a reason that has nothing to do with the audit),
`derivationNote` (one sentence naming the two predicates), and a `rows` array containing EXACTLY ONE
row — the tracer member:
  - `historicalPath`: `src/mcp/vice/r2000-verb-coverage.test.ts`
  - `verdict`: `re-pointed`
  - `newSubject`: `src/mcp/vice/anno-verb-coverage.test.ts`
  - `removalTrigger`: the anno CLI verb table ceasing to be the skill playbooks' documented surface
  - `plant`: `{ "kind": "worktree", "file": "scripts/lib/anno-cli-verbs.mjs", "find": "ANNO_CLI_VERB_FLOOR = 3", "replace": "ANNO_CLI_VERB_FLOOR = 4" }`
  - `guard`: `{ "argv": ["--test", "anno-verb-coverage.test.ts"], "cwd": "src/mcp/vice" }`
  - `observedRed`: filled by the harness run, not by hand
Then run `node scripts/audit-mutation-harness.mjs --row src/mcp/vice/r2000-verb-coverage.test.ts`.
The plant flips the exported floor constant at `scripts/lib/anno-cli-verbs.mjs:86`, which reds
`anno-verb-coverage.test.ts:185`'s `assert.equal(ANNO_CLI_VERB_FLOOR, 3)` deterministically. If it
does NOT red, do not weaken the assertion — choose a different mutation against the SAME
`newSubject`, record in the evidence file which mutation was tried, why it did not bite, and which
one did.

**E. The evidence file.** Write `evidence/32-tracer-observed-red.md` carrying the raw command, the
raw captured stdout/stderr (not a summary), the exit status, the `git status --porcelain` reading
before and after, and the commit measured — the `29-21-SUMMARY.md` shape D-15 names.

**Canon-referral breadcrumbs (recorded, deliberately NOT minted as prohibitions):** command
injection via a scanned path and `--root` path traversal are canon security constraints covered by
`/gsd-secure-phase` and this plan's `<threat_model>`; they are mitigations, not bespoke prohibitions.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/audit-mutation-harness.mjs --row src/mcp/vice/r2000-verb-coverage.test.ts && git status --porcelain | sort | diff - <(printf '?? docs/dissambler-workflow.md\n?? docs/undocumented-opcodes-ghidra.md\n?? docs/vice-mcp-ideas.md\n?? skills-lock.json\n' | sort) >/dev/null && echo TRACER_OK</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/audit-mutation-harness.mjs --row src/mcp/vice/r2000-verb-coverage.test.ts` exits 0 (the harness succeeded) AND its stdout records the guard's own exit status as a NON-ZERO integer. A run reporting the guard's status as 0 is a failed task, not a passed one.
    - The same run records a GREEN false-positive control: the unplanted guard exited 0 before the plant was applied, and the control's status and output are stored in the row's `observedRed.control`. A red with no green control is not an observed red — a guard that is red for an unrelated reason, or one that times out, would otherwise record a false red.
    - The three selectors behave as specified: `--rows src/mcp/vice/r2000-verb-coverage.test.ts` measures exactly that one row and prints the measured-row count; `--rows <a path not in the registry>` exits non-zero naming the offending entry; and passing none of `--row`/`--rows`/`--all`, or more than one, is a usage error rather than a silent full run.
    - `grep -c 'NODE_TEST_' scripts/audit-mutation-harness.mjs` is at least 1, and the file contains no `shell: true` and no `execSync` (`grep -c 'shell: *true' scripts/audit-mutation-harness.mjs` == 0 and `grep -c 'execSync' scripts/audit-mutation-harness.mjs` == 0).
    - The four new in-scope files carry ZERO occurrences of the subject literal: for each of `scripts/check-guard-fates.mjs`, `scripts/lib/audit-root.mjs`, `scripts/audit-mutation-harness.mjs`, `src/mcp/vice/guard-fates.test.ts`, `grep -ac 'regenerator2000' <file>` == 0.
    - `node scripts/check-no-regenerator2000.mjs` exits 0 after this task's files land — proving no exact-count exemption moved.
    - `node scripts/check-guard-fates.mjs` prints `setA=43`, `setB=15`, `setC=2`, `total=60` and `rows=1`, and exits NON-ZERO naming the 59 members with no row. This red is pre-declared in `<objective>`; a green here would mean the membership check is vacuous.
    - `node scripts/check-guard-fates.mjs --root /tmp` exits non-zero with a message naming `/tmp` as outside the repository root (the containment refusal), and `node scripts/check-guard-fates.mjs --root /nonexistent-typo` exits non-zero with a diagnosable message rather than an uncaught ENOENT stack.
    - `git status --porcelain` after the harness run is byte-identical to the pre-run reading (the four pre-existing untracked files, nothing else). `git diff --stat -- scripts/lib/anno-cli-verbs.mjs` is empty.
    - `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-tracer-observed-red.md` contains the literal command string that was run, the captured output, the non-zero exit status, and the commit measured.
    - `scripts/audit-mutation-harness.mjs` appears in NO `run:` line of `.github/workflows/ci.yml` and in NO value of `src/mcp/vice/package.json`'s `scripts` object (D-17, D-16).
    - `grep -Ec 'SET_A_FLOOR|SET_B_FLOOR|SET_C_FLOOR|TOTAL_FLOOR' scripts/check-guard-fates.mjs` is at least 4, and no array of member file paths is typed literally in the file (the derivation is the only source).
  </acceptance_criteria>
  <done>One real audited-set member is proven end to end: the guard derives it from the object store, the registry names it, the harness plants against its new subject, the guard is observed exiting non-zero, the tree is restored byte-identical, and the raw evidence is on disk.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The fate guard's own non-vacuity proof and its ambient declarations</name>
  <files>scripts/check-guard-fates.d.mts, scripts/lib/audit-root.d.mts, src/mcp/vice/guard-fates.test.ts</files>
  <read_first>
    - `scripts/check-no-regenerator2000.d.mts` — the whole 14-line file; its header formula is the one to copy
    - `scripts/lib/anno-cli-verbs.d.mts` — `:1-7`, the second instance of the same convention
    - `src/mcp/vice/removal-gate.test.ts` — `:32-75`, the planted-violation test shape, especially the closing self-non-vacuity assertion
    - `scripts/check-guard-fates.mjs` — as written in Task 1, for the exact exported signatures
    - `scripts/check-no-regenerator2000.mjs` — `:149-154` ("A planted violation proved against a re-implementation of the rule proves nothing about the rule the real scan applies")
    - `src/mcp/vice/ci-suite-coverage.test.ts` — `:40-110` (the tree walk and `SKIP_DIR_NAMES`) and `CI_YAML_PATH` at `:268`, to confirm a new test colocated in `src/mcp/vice/` needs no new CI step
  </read_first>
  <behavior>
    - Given a registry whose rows exactly match the derived set: `checkGuardFates` returns zero errors.
    - Given that registry with ONE row deleted: it returns an error naming the removed member's `historicalPath`.
    - Given that registry plus a row whose `historicalPath` is not in the derived set: it returns an error naming that stranger path.
    - Given a registry with two rows sharing a `historicalPath`, or two rows sharing a non-null `newSubject`: it returns a duplicate error naming the shared value.
    - Given `rows: []` against a non-empty derived set: it returns an error, never zero errors.
    - Given a derived set of `[]`: it returns a floor error, never zero errors.
    - Given a ROADMAP note that parses to zero deferred paths: `deriveAuditedSet` fails loudly naming the note, never returning a silently smaller set C.
    - Given a `re-pointed` row with `observedRed.exitStatus === 0`: it returns an error (a zero exit is not an observed red).
    - Given a `kept-unchanged` row with no `removalTrigger`: it returns an error.
    - Self-non-vacuity: a stubbed predicate that always returns `[]` must not deep-equal the real predicate's output on the deleted-row case.
    - `resolveContainedRoot` refuses a sibling directory whose name merely shares the repo root's prefix (e.g. `<repoRoot>-evil`), proving the boundary is segment-wise and not a bare `startsWith`.
  </behavior>
  <action>
Write `src/mcp/vice/guard-fates.test.ts` importing the REAL exported predicates from
`../../../scripts/check-guard-fates.mjs` and `../../../scripts/lib/audit-root.mjs` — drive the real
rule, never a re-implementation of it, per `scripts/check-no-regenerator2000.mjs:149-154`. Build the
synthetic derived-set and registry objects in-test (they are the predicate's INPUTS, so no git access
and no fixture files are needed, and the test stays fast enough for the per-commit sampling rate).

Colocate the test in `src/mcp/vice/` rather than creating a test directory under `scripts/`, so it is
already covered by CI's existing `npm test` step and `ci-suite-coverage.test.ts` does not demand a new
CI step for a new test-containing directory. Confirm this by reading that file's `SKIP_DIR_NAMES` and
tree walk before writing.

Name discipline: this file is inside the removal gate's scope. It must carry zero occurrences of the
subject literal — refer to the removal gate by role, not by file name.

Then write the two `.d.mts` siblings using the header formula from
`scripts/check-no-regenerator2000.d.mts`: "Type declarations for X, so its colocated test (PATH)
typechecks under strict mode. CI-only helper with no runtime role, stays out of
`src/mcp/vice/package.json`'s `files[]` like its `.mjs` sibling." Declare exactly the symbols the test
imports — `deriveAuditedSet`, `checkGuardFates`, `SET_A_FLOOR`, `SET_B_FLOOR`, `SET_C_FLOOR`, `TOTAL_FLOOR`,
`resolveContainedRoot` — plus the row and derived-set interfaces. Add nothing the test does not use.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test guard-fates.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && node --test guard-fates.test.ts` exits 0 and reports at least 8 passing tests covering every case in `<behavior>`.
    - `cd src/mcp/vice && npm run typecheck` exits 0, proving the two `.d.mts` files declare everything the strict-mode test imports.
    - The test file imports from `../../../scripts/check-guard-fates.mjs`; `grep -c 'check-guard-fates.mjs' src/mcp/vice/guard-fates.test.ts` is at least 1 and the file defines no local re-implementation of the membership rule.
    - `grep -ac 'regenerator2000' src/mcp/vice/guard-fates.test.ts` == 0 and `grep -ac 'regenerator2000' scripts/check-guard-fates.d.mts` == 0.
    - `grep -c 'guard-fates' src/mcp/vice/package.json` == 0 — neither the `.d.mts` nor the guard is listed in `files[]`, matching the two existing `.d.mts` siblings.
    - `cd src/mcp/vice && node --test ci-suite-coverage.test.ts` exits 0, proving the new colocated test needed no new CI step.
    - `cd src/mcp/vice && npm run test:automated` exits 0 with 0 failures — the measured clean floor (research §7.2: 2920 tests, 0 fail). Any failure is a real regression; the historical 44/7/5 baselines are superseded.
  </acceptance_criteria>
  <done>The fate guard has its own committed non-vacuity assertion — deleting a row, adding a stranger row, emptying the registry, and a zero-exit `observedRed` each make the REAL exported predicate report the discrepancy, and the whole automated suite is still at zero failures.</done>
</task>

<task type="auto">
  <name>Task 3: D-02's audited-set reconciliation — the historical 43 against the settled tree, every addition and removal reasoned</name>
  <files>.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — `CUT-04`'s full text, specifically "32 test files (19 `r2000-`named plus 13 non-`r2000-`named that reference it) and 11 files under `scripts/`" and the eleven guards it names explicitly
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-RESEARCH.md` §1.2 (both figures reproduced, with the 19 and 13 enumerated and the 11 `scripts/` paths listed), §1.4 (the 21 same-path / 16 renamed / 6 gone forward map, and the warning that git's `-M` heuristic disagrees for five files), §1.5 (the new-guard union)
    - `.planning/phases/29-the-mcp-surface/29-21-SUMMARY.md` — `:176-259`, the commit-level record of the 15 outright removals that `deleted` rows cite
    - `.planning/phases/29-the-mcp-surface/29-VERIFICATION.md` — it enumerates the three renamed test files individually; the surviving-file predicate depends on it
    - `scripts/check-guard-fates.mjs` — as written in Task 1, so the document's predicates and the guard's are the SAME predicates, quoted not paraphrased
  </read_first>
  <action>
Write the reconciliation D-02 requires: the audited set re-derived on the settled tree AND reconciled
against `CUT-04`'s historical list, with every addition and every removal given a reason.

Structure it as:
  1. **The two pinned commits and the two predicates**, quoted from `scripts/check-guard-fates.mjs`
     rather than paraphrased, plus the exact commands a later reader re-runs
     (`git ls-tree -r --name-only 0394cbc -- src/mcp/vice scripts`, `git show <commit>:<path>`,
     `git diff --name-only --diff-filter=A 0394cbc 345d5c4 -- scripts src/mcp/vice`).
  2. **Set A reproduces CUT-04's figures exactly**: 19 `r2000-`named test files + 13
     non-`r2000-`named referencing test files = 32, plus 11 `scripts/` files = 43. Enumerate all
     43 paths. State that `.planning/research/PITFALLS.md:36` lists the SAME 13 names with
     different per-file counts, and that the difference is a counting definition (lines-with-a-hit
     versus occurrences) — say which definition this document uses, per research §1.2's flag.
  3. **Resolve CUT-04's "two `.d.mts` declarations" ambiguity in writing**: the derived set contains
     three (`r2000-cli-verbs.d.mts`, `skill-corpus.d.mts`, `skill-descriptions.d.mts`); the
     arithmetic closes either way because the count of 11 is exact.
  4. **The forward map**: 21 survive under the same path, 16 under a new name, 6 are gone
     (21+16+6 = 43), each named. Record that five of the sixteen renames are INVISIBLE to git's `-M`
     heuristic (`r2000-cli`, `r2000-enum-gen`, `r2000-memmap-render`, `r2000-tools`,
     `r2000-verb-coverage` all score `D`+`A`) and that the `r2000-` -> `anno-` name-descendant
     predicate — the one `CUT-01` and `29-VERIFICATION.md` already established — is authoritative
     over git's output. Note that an earlier research draft said 13 renamed; 16 is the measured figure.
  5. **The additions, with reasons**: the 15 net-new set-B members, each with the reason it is in
     scope (created by Phase 29 as part of the re-pointing, and matching the same subject predicate
     set A uses). Enumerate them.
  6. **The removals from research's §1.5 candidate list, with reasons**: `scripts/check-skill-cli-invocations.mjs`
     and `scripts/lib/anno-cli-invocations.d.mts` are NOT in set B because neither their path nor
     their content at `345d5c4` matches the subject predicate — a uniform, mechanical exclusion, not
     a judgement. `scripts/lib/anno-cli-invocations.mjs` IS in set B because its content matches.
     Record this asymmetry explicitly so a later reader does not read it as an omission.
  7. **The adjacency subtraction**: 22 raw set-B candidates minus 7 paths already claimed as a set-A
     row's `newSubject` = 15. Name the 7. This is why a renamed member has one row, not two.
  8. **Set C, the two deferred fates**: name both (`src/mcp/vice/block-class.ts` and
     `src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs`), quote the `ROADMAP.md` § Phase 32
     note beginning "Two guard fates were DEFERRED" that they are parsed out of, give the exact parse
     command, and state that the adjacency subtraction is applied to them too and removes neither.
  9. **The total**: 43 + 15 + 2 = 60, matching `TOTAL_FLOOR`, with each part matching `SET_A_FLOOR`,
     `SET_B_FLOOR` and `SET_C_FLOOR`. The union is 60, NOT 58 — an arithmetic that omits set C
     contradicts the guard this document is supposed to reconcile against, and plan 32-08 reads this
     file as the authoritative 43 / 15 / 2 = 60 arithmetic when it completes the registry.

Every figure carries the command that produced it. No figure is copied from CONTEXT.md or from
RESEARCH.md without re-running its command — this milestone has already had to correct figures whose
provenance did not reproduce.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && test $(git ls-tree -r --name-only 0394cbc -- src/mcp/vice scripts | wc -l) -eq 273 && grep -c '0394cbc' .planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md && grep -c '345d5c4' .planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md</automated>
  </verify>
  <acceptance_criteria>
    - The document names both pinned commits (`0394cbc`, `345d5c4`) and, for every figure it publishes, the exact shell command that produced it.
    - It enumerates all 43 set-A paths and all 15 set-B paths by name; `grep -c 'src/mcp/vice/' <doc>` is at least 50.
    - It states the counting definition used for per-file hit counts and explicitly reconciles the difference against `.planning/research/PITFALLS.md:36`'s figures rather than leaving them contradicting.
    - It names the five renames git's `-M` heuristic misses and states that the name-descendant predicate is authoritative.
    - It gives a written reason for every set-B inclusion AND for the two research-listed candidates that are excluded — no candidate is dropped silently.
    - It names the 7 paths subtracted by the adjacency rule and states 22 − 7 = 15 and 43 + 15 + 2 = 60.
    - It names both set-C members and quotes the `ROADMAP.md` § Phase 32 deferred-fates note they are parsed out of, with the parse command.
    - The four floor figures in the document (43, 15, 2, 60) are identical to `SET_A_FLOOR`, `SET_B_FLOOR`, `SET_C_FLOOR` and `TOTAL_FLOOR` in `scripts/check-guard-fates.mjs`. A document publishing 58 as the union is a failed task, not a rounding difference.
  </acceptance_criteria>
  <done>A later reader can re-derive the audited set from the document alone, and every difference between CUT-04's historical list and the settled-tree derivation carries a stated reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry JSON -> harness | Row-supplied plant descriptors (`file`, `find`, `replace`) cross into filesystem writes and subprocess argv construction |
| `--root <dir>` CLI argument -> guard/harness filesystem access | An operator-supplied path decides which tree is read and written |
| git object-store content -> guard logic | Text read from `git show` at two pinned commits crosses into the derivation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Tampering | `scripts/audit-mutation-harness.mjs` subprocess spawn | high | mitigate | Every `spawnSync` receives an argv ARRAY built from a pinned path or a directory listing; `shell: true` is never set; `execSync` on an interpolated string is never used; scanned text is never evaluated or dynamically imported (`audit-gate.mjs` T-12-03 / ASVS V5) |
| T-32-02 | Tampering | `--root` handling in `check-guard-fates.mjs` and the harness | high | mitigate | `scripts/lib/audit-root.mjs`'s `resolveContainedRoot()` resolves then refuses any path that is not the repo root or a segment-boundary descendant of it; written new, because no containment check exists in `audit-gate.mjs` to copy (PATTERNS corrected RESEARCH's citation) |
| T-32-03 | Tampering | harness working-tree mutation | high | mitigate | Capture original bytes before any write; idempotent `restoreAll()` in `try/finally` and on `exit`/`SIGINT`/`SIGTERM`/`uncaughtException`; `git status --porcelain` compared against a process-start baseline before and after every row |
| T-32-04 | Repudiation | fate row evidence | medium | mitigate | `observedRed` is written only by the harness from a captured `spawnSync` result; the guard rejects a `re-pointed`/`superseded` row whose `exitStatus` is zero, absent, or non-integer |
| T-32-05 | Denial of Service | nested `node --test` reporter switch | high | mitigate | Every `NODE_TEST_*` key stripped from the child env before spawning (`audit-gate.mjs:203-228` verbatim); a 15000ms timeout with `killSignal: "SIGKILL"` and the fail-closed `result.error` mapping so an ETIMEDOUT is a red, not a hang |
| T-32-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no packages and invokes no package manager; every module it adds uses Node builtins plus existing in-repo imports. No `## Package Legitimacy Audit` is owed because no install task exists |
</threat_model>

<verification>
- `cd src/mcp/vice && npm run test:automated` exits 0 with 0 failures.
- `cd src/mcp/vice && npm run typecheck` exits 0.
- All six existing CI check scripts still exit 0, in particular `node scripts/check-no-regenerator2000.mjs`.
- `node scripts/audit-gate.mjs --json` reports `allowed: true`, `redGuards: []`, `structuralErrors: []` and 9 derived docs guards.
- `node scripts/check-guard-fates.mjs` exits NON-ZERO with the pre-declared incomplete-registry red, printing `setA=43 setB=15 setC=2 total=60 rows=1`.
- `git status --porcelain` is unchanged from its pre-plan baseline apart from this plan's own new files.
</verification>

<success_criteria>
- One real audited-set member has been driven through registry -> guard -> harness -> plant -> observed non-zero -> revert -> recorded evidence.
- The guard derives its set from the object store at two pinned commits, with no hand-typed member list, and refuses an out-of-repo `--root`.
- The harness strips `NODE_TEST_*`, spawns with an argv array, and cannot leave the tree dirty.
- The guard carries its own committed non-vacuity proof driving the real exported predicate.
- D-02's reconciliation is on disk and every figure in it re-derives from a stated command.
</success_criteria>

## Artifacts this phase produces

Symbols and paths created by Phase 32 (excluded from drift verification — they do not exist before this phase):

| Artifact | Kind | Created by |
|---|---|---|
| `scripts/check-guard-fates.mjs` | new CI guard script | 32-01 |
| `deriveAuditedSet()`, `checkGuardFates()` | exported functions | 32-01 |
| `SET_A_FLOOR`, `SET_B_FLOOR`, `SET_C_FLOOR`, `TOTAL_FLOOR` | exported constants | 32-01 |
| `scripts/check-guard-fates.d.mts` | ambient declaration | 32-01 |
| `scripts/lib/audit-root.mjs`, `scripts/lib/audit-root.d.mts` | new helper + declaration | 32-01 |
| `resolveContainedRoot()` | exported function | 32-01 |
| `scripts/audit-mutation-harness.mjs` | new phase instrument (never a CI job) | 32-01 |
| `src/mcp/vice/guard-fates.test.ts` | new colocated non-vacuity test | 32-01 |
| `.planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json` | the fate registry | 32-01, completed 32-06/32-07 |
| `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-tracer-observed-red.md` | evidence | 32-01 |
| `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md` | evidence | 32-01 |
| `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-root-override-inventory.md` | evidence | 32-02 |
| `--root` flag on `generate-tool-support-table.mjs` and the skill-facing `check-*.mjs` | new CLI surface | 32-02 |
| `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-document-sweep.md` | evidence | 32-05 |
| `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-sweep-renamed-rows.md` | evidence | 32-06 |
| `.planning/phases/32-the-deletion-and-the-grep-gate/fixtures/` synthetic trees (never under `src/mcp/vice/fixtures/planted-` — that prefix carries an exact `prefixHits: 2` pin in the removal gate and a new file under it reds the gate on its own landing commit) | synthetic plant fixtures | 32-06, 32-07 |
| `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-sweep-same-path-rows.md` | evidence | 32-07 |
| `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-deferred-fates.md` | evidence | 32-08 |
| CI step "Validate every audited guard has a recorded, non-vacuous fate (CUT-04)" | new named CI step | 32-08 |
| `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-close-gate.md` | evidence | 32-08 |
| `.planning/phases/32-the-deletion-and-the-grep-gate/COVERAGE.md` | API-coverage declaration | 32-08 |

<output>
Create `.planning/phases/32-the-deletion-and-the-grep-gate/32-01-SUMMARY.md` when done
</output>
