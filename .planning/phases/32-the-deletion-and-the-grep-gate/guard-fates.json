{
  "auditCommit": "0394cbc",
  "auditEnd": "345d5c4",
  "setAFloor": 43,
  "setBFloor": 16,
  "setCFloor": 2,
  "totalFloor": 61,
  "derivationNote": "Set A is derived over `git ls-tree -r --full-tree --name-only 0394cbc -- src/mcp/vice scripts`: an in-scope test path whose basename carries the subject prefix or whose content at that commit mentions the subject, or any `scripts/` path whose path or content mentions it. Set B is derived over `git diff --name-only --diff-filter=A 0394cbc 345d5c4`, restricted to in-scope test paths, the planted-fixture prefix and `scripts/`, matched on the same subject predicate, then reduced by the adjacency subtraction that removes every path already claimed as a set-A member's mechanically-derived successor. Set C is parsed out of `.planning/ROADMAP.md`'s Phase 32 deferred-fates note. Every member path is derived; none is typed by hand.",
  "rows": [
    {
      "historicalPath": "src/mcp/vice/r2000-verb-coverage.test.ts",
      "verdict": "re-pointed",
      "newSubject": "src/mcp/vice/anno-verb-coverage.test.ts",
      "removalTrigger": "The anno CLI verb table ceasing to be the skill playbooks' documented surface. While the playbooks document the verbs, this guard is the only thing that fails when a verb is added to the CLI and never documented; once the playbooks stop naming verbs at all, the guard is asserting a relation nobody depends on and should be removed with the last verb reference.",
      "plant": {
        "kind": "worktree",
        "file": "scripts/lib/anno-cli-verbs.mjs",
        "find": "ANNO_CLI_VERB_FLOOR = 3",
        "replace": "ANNO_CLI_VERB_FLOOR = 4"
      },
      "guard": {
        "argv": [
          "--test",
          "anno-verb-coverage.test.ts"
        ],
        "cwd": "src/mcp/vice"
      },
      "observedRed": {
        "command": "node --test anno-verb-coverage.test.ts",
        "cwd": "src/mcp/vice",
        "plant": {
          "kind": "worktree",
          "file": "scripts/lib/anno-cli-verbs.mjs",
          "find": "ANNO_CLI_VERB_FLOOR = 3",
          "replace": "ANNO_CLI_VERB_FLOOR = 4"
        },
        "exitStatus": 1,
        "excerpt": "not ok 5 - non-vacuity floor: ANNO_CLI_VERB_FLOOR matches the measured true count and the real parse meets it\n  ---\n  duration_ms: 2.342793\n  type: 'test'\n  location: '/home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-verb-coverage.test.ts:184:1'\n  failureType: 'testCodeFailure'\n  error: |-\n    Expected values to be strictly equal:\n    \n    4 !== 3\n    \n  code: 'ERR_ASSERTION'\n  name: 'AssertionError'\n  expected: 3\n  actual: 4\n  operator: 'strictEqual'\n  stack: |-\n    TestContext.<anonymous> (file:///home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-verb-coverage.test.ts:185:10)\n    Test.runInAsyncScope (node:async_hooks:214:14)\n    Test.run (node:internal/test_runner/test:1047:25)\n    Test.processPendingSubtests (node:internal/test_runner/test:744:18)\n    Test.postRun (node:internal/test_runner/test:1173:19)\n    Test.run (node:internal/test_runner/test:1101:12)\n    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)\n  ...\n# Subtest: the CI script's live execution path: `node scripts/check-skill-tool-coverage.mjs` exits 0 with 'OK' in stdout\nnot ok 6 - the CI script's live execution path: `node scripts/check-skill-tool-coverage.mjs` exits 0 with 'OK' in stdout\n  ---\n  duration_ms: 342.722475\n  type: 'test'\n  location: '/home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-verb-coverage.test.ts:191:1'\n  failureType: 'testCodeFailure'\n  error: |-\n    expected exit 0, got 1. stderr: check-skill-tool-coverage: FAIL\n      - non-vacuity: expected at least 4 anno CLI verbs parsed from anno-cli.ts's dispatch switch, got 3 -- the parser or the switch statement itself may be broken\n    \n    \n    1 !== 0\n    \n  code: 'ERR_ASSERTION'",
        "control": {
          "command": "node --test anno-verb-coverage.test.ts",
          "cwd": "src/mcp/vice",
          "exitStatus": 0,
          "excerpt": "TAP version 13\n# Subtest: real-source parse: anno-cli.ts's dispatch switch yields exactly the 3 known verbs, never 'default'\nok 1 - real-source parse: anno-cli.ts's dispatch switch yields exactly the 3 known verbs, never 'default'\n  ---\n  duration_ms: 16.375333\n  type: 'test'\n  ...\n# Subtest: positive control: every real verb is named by at least one real skill file (the Task 1 property, restated mechanically)\nok 2 - positive control: every real verb is named by at least one real skill file (the Task 1 property, restated mechanically)\n  ---\n  duration_ms: 16.70315\n  type: 'test'\n  ...\n# Subtest: planted violation: an extra, genuinely new case is parsed and reported missing, while a real, documented verb is not\nok 3 - planted violation: an extra, genuinely new case is parsed and reported missing, while a real, documented verb is not\n  ---\n  duration_ms: 5.007724\n  type: 'test'\n  ...\n# Subtest: comment hygiene: a case hidden in a block comment or a line comment is never parsed as a verb\nok 4 - comment hygiene: a case hidden in a block comment or a line comment is never parsed as a verb\n  ---\n  duration_ms: 0.507813\n  type: 'test'\n  ...\n# Subtest: non-vacuity floor: ANNO_CLI_VERB_FLOOR matches the measured true count and the real parse meets it\nok 5 - non-vacuity floor: ANNO_CLI_VERB_FLOOR matches the measured true count and the real parse meets it\n  ---\n  duration_ms: 3.238227\n  type: 'test'\n  ...\n# Subtest: the CI script's live execution path: `node scripts/check-skill-tool-coverage.mjs` exits 0 with 'OK' in stdout\nok 6 - the CI script's live execution path: `node scripts/check-skill-tool-coverage.mjs` exits 0 with 'OK' in stdout\n  ---\n  duration_ms: 360.383644\n  type: 'test'\n  ...\n# Subtest: a verb the CLI actually dispatches is never documented as withdrawn, in either skill tree\nok 7 - a verb the CLI actually dispatches is never documented as withdrawn, in either skill tree\n  ---"
        }
      }
    },
    {
      "historicalPath": "scripts/lib/r2000-cli-verbs.mjs",
      "verdict": "re-pointed",
      "newSubject": "scripts/lib/anno-cli-verbs.mjs",
      "removalTrigger": "The skill playbooks ceasing to document CLI invocation at all. This module's `verbsMissingFromSkills()` asserts a relation between the dispatch switch's own verbs and the literal token `anno <verb>` in skill prose; once no skill file names an invocation, the relation has no right-hand side and the module -- with `scripts/check-skill-tool-coverage.mjs`'s verb half -- should be removed rather than left asserting over an empty corpus.",
      "plant": {
        "kind": "worktree",
        "file": "scripts/lib/anno-cli-verbs.mjs",
        "find": "text.includes(`anno ${verb}`)",
        "replace": "text.includes(`anno-${verb}`)"
      },
      "guard": {
        "argv": [
          "scripts/check-skill-tool-coverage.mjs"
        ],
        "cwd": "."
      },
      "note": "Plant target is the INVOCATION LITERAL in `verbsMissingFromSkills()` (`anno ${verb}` -> `anno-${verb}`), deliberately NOT the `ANNO_CLI_VERB_FLOOR` constant the tracer row (`src/mcp/vice/r2000-verb-coverage.test.ts`) already plants. The two rows therefore share a plant FILE but not a plant TARGET and not a guard: the tracer runs `node --test anno-verb-coverage.test.ts`, this row runs the live CI script `scripts/check-skill-tool-coverage.mjs`. Neither can mask the other -- the floor mutation trips an arithmetic non-vacuity assertion, this one trips the skill-coverage relation, and they fail through different code paths. Expected to trip: the verb-coverage relation, reporting every real verb as undocumented because the token the predicate matches no longer matches the token the playbooks carry.",
      "observedRed": {
        "command": "node scripts/check-skill-tool-coverage.mjs",
        "cwd": ".",
        "plant": {
          "kind": "worktree",
          "file": "scripts/lib/anno-cli-verbs.mjs",
          "find": "text.includes(`anno ${verb}`)",
          "replace": "text.includes(`anno-${verb}`)"
        },
        "exitStatus": 1,
        "excerpt": "check-skill-tool-coverage: FAIL\n  - anno coverage: parsed from anno-cli.ts's dispatch switch but named by NO skill file. Resolve by: (1) documenting it in a playbook, (2) removing the verb, or (3) recording it as a scope decision.\n  - anno export-asm: parsed from anno-cli.ts's dispatch switch but named by NO skill file. Resolve by: (1) documenting it in a playbook, (2) removing the verb, or (3) recording it as a scope decision.\n  - anno render-memmap: parsed from anno-cli.ts's dispatch switch but named by NO skill file. Resolve by: (1) documenting it in a playbook, (2) removing the verb, or (3) recording it as a scope decision.",
        "control": {
          "command": "node scripts/check-skill-tool-coverage.mjs",
          "cwd": ".",
          "exitStatus": 0,
          "excerpt": "check-skill-tool-coverage: OK -- 37 distinct vice_* names extracted from 33 files across 7 skill directories; 31 resolved as advertised on the stock manifest (38 tools total). Classified: 0 proxy-local (neither manifest), 2 proxy-local-with-stock-manifest-entry, 1 deny-listed, 2 not-a-tool-name, 6 fork-only-unrecoverable, 0 pending-later-phase. anno_*: 18 distinct names extracted, all curated (CURATED_ANNO_TOOLS has 19 entries). anno CLI verbs: 3 parsed from anno-cli.ts, 3/3 resolved (named by at least one skill file).\n\n(node:255828) ExperimentalWarning: SQLite is an experimental feature and might change at any time\n(Use `node --trace-warnings ...` to show where the warning was created)"
        }
      }
    },
    {
      "historicalPath": "scripts/lib/r2000-cli-verbs.d.mts",
      "verdict": "re-pointed",
      "newSubject": "scripts/lib/anno-cli-verbs.d.mts",
      "removalTrigger": "Its `.mjs` sibling being removed, or `src/mcp/vice/anno-verb-coverage.test.ts` ceasing to import it. This file exists only so that colocated test typechecks under `strict`; it has no runtime role and is excluded from the shipped `files[]`. With no TypeScript importer there is nothing for it to declare and it should be deleted in the same commit as its last consumer.",
      "plant": {
        "kind": "worktree",
        "file": "scripts/lib/anno-cli-verbs.d.mts",
        "find": "export declare function verbsMissingFromSkills(verbs: string[], skillTexts: string[]): string[];",
        "replace": "// planted: the verbsMissingFromSkills declaration is removed"
      },
      "guard": {
        "argv": [
          "node_modules/typescript/bin/tsc",
          "--noEmit",
          "-p",
          "tsconfig.json"
        ],
        "cwd": "src/mcp/vice"
      },
      "note": "A declaration file has NO runtime behaviour, so no `node --test` run can observe its absence -- the typechecker is the only mechanical observer that exists. The guard is therefore `npm run typecheck` in `src/mcp/vice`, and the plant removes one of the three exported declarations. Expected to trip: `src/mcp/vice/anno-verb-coverage.test.ts:24` imports `verbsMissingFromSkills` from `../../../scripts/lib/anno-cli-verbs.mjs`, which TypeScript resolves through this `.d.mts`; with the declaration gone the import has no exported member and `tsc --noEmit` errors. Note that `skipLibCheck: true` suppresses checking INSIDE declaration files but not export resolution against them, which is exactly the check this plant exercises. GUARD ARGV DEVIATION (recorded, not hidden): the harness's third documented convention, `argv: [\"--run\", \"typecheck\"]`, spawns `npm --run typecheck`, and npm 11 rejects that form (`Unknown command: \"typecheck\"`; it wants `npm run typecheck`). Measured here: the UNPLANTED control exited 1 with that message, so the row came back UNMEASURABLE rather than red -- the green-control rule doing exactly its job, refusing to let a broken invocation be recorded as evidence. This plan modifies no script, so the route around it is the harness's OTHER convention: spawn `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json`, which is byte-for-byte the command `package.json`'s `typecheck` script runs. This is a FINDING against `scripts/audit-mutation-harness.mjs` (plan 32-01): the `--run` convention is unusable as documented and is the only one of its three that had never been exercised before this row.",
      "observedRed": {
        "command": "node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json",
        "cwd": "src/mcp/vice",
        "plant": {
          "kind": "worktree",
          "file": "scripts/lib/anno-cli-verbs.d.mts",
          "find": "export declare function verbsMissingFromSkills(verbs: string[], skillTexts: string[]): string[];",
          "replace": "// planted: the verbsMissingFromSkills declaration is removed"
        },
        "exitStatus": 1,
        "excerpt": "anno-verb-coverage.test.ts(24,29): error TS2305: Module '\"../../../scripts/lib/anno-cli-verbs.mjs\"' has no exported member 'verbsMissingFromSkills'.",
        "control": {
          "command": "node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json",
          "cwd": "src/mcp/vice",
          "exitStatus": 0,
          "excerpt": ""
        }
      }
    },
    {
      "historicalPath": "src/mcp/vice/docs-r2000-decisions.test.ts",
      "verdict": "re-pointed",
      "newSubject": "src/mcp/vice/docs-absorbed-decisions.test.ts",
      "removalTrigger": "D-36 being retired from `.planning/PROJECT.md`'s Key Decisions table -- which happens if upstream issue #42 is fixed in a released version this project installs, letting the client-side composition be retired in favour of the native tool. At that point the row this guard pins no longer states a live decision and the guard should be removed with it, not re-pointed at whatever replaces it.",
      "plant": {
        "kind": "worktree",
        "file": ".planning/PROJECT.md",
        "find": "handler.rs:1894",
        "replace": "handler.rs:1895"
      },
      "guard": {
        "argv": [
          "--test",
          "docs-absorbed-decisions.test.ts"
        ],
        "cwd": "src/mcp/vice"
      },
      "note": "Plant is a single-digit change to the ONE occurrence of the cited upstream source location in `.planning/PROJECT.md` (verified: `grep -ac 'handler.rs:1894'` is 1). Expected to trip: test 4, assertion `the D-36 row does not name handler.rs:1894`. `.planning/` is outside the removal gate's scope so no exact-count pin is disturbed, but this IS a mutation of a file plan 32-03 repaired, so the harness's capture-before-write and byte-mode restore are load-bearing here; `git diff --stat -- .planning/PROJECT.md` is asserted empty after the run. Chosen over deleting `supersedes D-32` or the issue url because a one-character edit is the smallest mutation that reaches a real assertion.",
      "observedRed": {
        "command": "node --test docs-absorbed-decisions.test.ts",
        "cwd": "src/mcp/vice",
        "plant": {
          "kind": "worktree",
          "file": ".planning/PROJECT.md",
          "find": "handler.rs:1894",
          "replace": "handler.rs:1895"
        },
        "exitStatus": 1,
        "excerpt": "not ok 4 - 4. PROJECT.md's Key Decisions D-36 row states supersession of D-32, names handler.rs:1894, the upstream issue url, and a named reversal-trigger phrase\n  ---\n  duration_ms: 1.853943\n  type: 'test'\n  location: '/home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/docs-absorbed-decisions.test.ts:198:1'\n  failureType: 'testCodeFailure'\n  error: 'the D-36 row does not name handler.rs:1894'\n  code: 'ERR_ASSERTION'\n  name: 'AssertionError'\n  expected: true\n  actual: false\n  operator: '=='\n  stack: |-\n    TestContext.<anonymous> (file:///home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/docs-absorbed-decisions.test.ts:205:10)\n    Test.runInAsyncScope (node:async_hooks:214:14)\n    Test.run (node:internal/test_runner/test:1047:25)\n    Test.processPendingSubtests (node:internal/test_runner/test:744:18)\n    Test.postRun (node:internal/test_runner/test:1173:19)\n    Test.run (node:internal/test_runner/test:1101:12)\n    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)\n  ...\n# Subtest: 5. cross-document consistency: every PROJECT.md line naming D-32 also names D-36 or the word 'supersede'\nok 5 - 5. cross-document consistency: every PROJECT.md line naming D-32 also names D-36 or the word 'supersede'\n  ---\n  duration_ms: 1.148676\n  type: 'test'\n  ...\n1..5\n# tests 5\n# suites 0\n# pass 4\n# fail 1\n# cancelled 0\n# skipped 0\n# todo 0\n# duration_ms 248.628816",
        "control": {
          "command": "node --test docs-absorbed-decisions.test.ts",
          "cwd": "src/mcp/vice",
          "exitStatus": 0,
          "excerpt": "TAP version 13\n# Subtest: 1. non-vacuity: the guard's own phrase sets are non-empty and its section isolators return non-null for both documents\nok 1 - 1. non-vacuity: the guard's own phrase sets are non-empty and its section isolators return non-null for both documents\n  ---\n  duration_ms: 4.540163\n  type: 'test'\n  ...\n# Subtest: 2. ARCHITECTURE.md's Architecture Change Record names D-17/D-18, carries a date, has six numbered steps, and names both guard filenames in step 5\nok 2 - 2. ARCHITECTURE.md's Architecture Change Record names D-17/D-18, carries a date, has six numbered steps, and names both guard filenames in step 5\n  ---\n  duration_ms: 1.42465\n  type: 'test'\n  ...\n# Subtest: 3. ARCHITECTURE.md carries Rule A21 naming resolveStorePath, ChildProcess, and r2000-mcp-client.ts\nok 3 - 3. ARCHITECTURE.md carries Rule A21 naming resolveStorePath, ChildProcess, and r2000-mcp-client.ts\n  ---\n  duration_ms: 0.830046\n  type: 'test'\n  ...\n# Subtest: 4. PROJECT.md's Key Decisions D-36 row states supersession of D-32, names handler.rs:1894, the upstream issue url, and a named reversal-trigger phrase\nok 4 - 4. PROJECT.md's Key Decisions D-36 row states supersession of D-32, names handler.rs:1894, the upstream issue url, and a named reversal-trigger phrase\n  ---\n  duration_ms: 2.1164\n  type: 'test'\n  ...\n# Subtest: 5. cross-document consistency: every PROJECT.md line naming D-32 also names D-36 or the word 'supersede'\nok 5 - 5. cross-document consistency: every PROJECT.md line naming D-32 also names D-36 or the word 'supersede'\n  ---\n  duration_ms: 1.977749\n  type: 'test'\n  ...\n1..5\n# tests 5\n# suites 0\n# pass 5\n# fail 0\n# cancelled 0\n# skipped 0\n# todo 0\n# duration_ms 224.785627"
        }
      }
    },
    {
      "historicalPath": "src/mcp/vice/r2000-answer-key.test.ts",
      "verdict": "re-pointed",
      "newSubject": "src/mcp/vice/absorbed-answer-key.test.ts",
      "removalTrigger": "The phase-11 criterion-1 blind-answer experiment ceasing to be cited as evidence for the annotation store's query surface. This guard seals a one-shot historical experiment; if a later milestone supersedes that evidence with a live oracle, the seal is asserting the integrity of a record nobody reads and should be removed together with the evidence directory's citation, never silently left green.",
      "plant": {
        "kind": "worktree",
        "file": ".planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/evidence/criterion1/ANSWER.sha256",
        "find": "e64463d8",
        "replace": "e64463d9"
      },
      "guard": {
        "argv": [
          "--test",
          "absorbed-answer-key.test.ts"
        ],
        "cwd": "src/mcp/vice"
      },
      "note": "Plant is a one-hex-digit change to the SEAL, chosen precisely so it does NOT delete, move or rename anything under `.planning/phases/` -- the phase-11 evidence directory is a design input to this milestone and `--no-archive-phases` is a standing constraint. The mutated value is still 64 lowercase hex characters, so the shape assertion still passes and the failure is unambiguously the drift assertion rather than a format complaint. Expected to trip: `ANSWER.sha256 matches the sha256 recomputed from ANSWER.md's own canonical line (T-11-SEAL-DRIFT)`.",
      "observedRed": {
        "command": "node --test absorbed-answer-key.test.ts",
        "cwd": "src/mcp/vice",
        "plant": {
          "kind": "worktree",
          "file": ".planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/evidence/criterion1/ANSWER.sha256",
          "find": "e64463d8",
          "replace": "e64463d9"
        },
        "exitStatus": 1,
        "excerpt": "not ok 2 - ANSWER.sha256 matches the sha256 recomputed from ANSWER.md's own canonical line (T-11-SEAL-DRIFT)\n  ---\n  duration_ms: 3.99494\n  type: 'test'\n  location: '/home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/absorbed-answer-key.test.ts:84:1'\n  failureType: 'testCodeFailure'\n  error: |-\n    ANSWER.sha256 (e64463d9cef8fbb7699620a3c207de08a36b1189afd9700452a448d91d8c08cc) does not match the sha256 recomputed from ANSWER.md's canonical line \"label=border_bump_up confidence=probable-data blocktype=byte xrefcount=2\" (e64463d8cef8fbb7699620a3c207de08a36b1189afd9700452a448d91d8c08cc) -- the seal has drifted from the answer it seals. Re-seal by recomputing sha256 of the exact canonical line (no trailing newline) and rewriting ANSWER.sha256.\n    + actual - expected\n    \n    + 'e64463d8cef8fbb7699620a3c207de08a36b1189afd9700452a448d91d8c08cc'\n    - 'e64463d9cef8fbb7699620a3c207de08a36b1189afd9700452a448d91d8c08cc'\n    \n  code: 'ERR_ASSERTION'\n  name: 'AssertionError'\n  expected: 'e64463d9cef8fbb7699620a3c207de08a36b1189afd9700452a448d91d8c08cc'\n  actual: 'e64463d8cef8fbb7699620a3c207de08a36b1189afd9700452a448d91d8c08cc'\n  operator: 'strictEqual'\n  stack: |-\n    TestContext.<anonymous> (file:///home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/absorbed-answer-key.test.ts:89:10)\n    Test.runInAsyncScope (node:async_hooks:214:14)\n    Test.run (node:internal/test_runner/test:1047:25)\n    Test.processPendingSubtests (node:internal/test_runner/test:744:18)\n    Test.postRun (node:internal/test_runner/test:1173:19)\n    Test.run (node:internal/test_runner/test:1101:12)\n    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)\n  ...\n# Subtest: the canonical answer line matches QUESTION.md's own grammar (four lowercase key=value fields, single spaces)\nok 3 - the canonical answer line matches QUESTION.md's own grammar (four lowercase key=value fields, single spaces)\n  ---\n  duration_ms: 0.716384\n  type: 'test'\n  ...\n# Subtest: QUESTION.md does not contain the canonical answer line (T-11-LEAK)\nok 4 - QUESTION.md does not contain the canonical answer line (T-11-LEAK)\n  ---\n  duration_ms: 0.56703\n  type: 'test'\n  ...\n# Subtest: QUESTION.md does not contain the sealed answer's distinctive field values (T-11-LEAK)",
        "control": {
          "command": "node --test absorbed-answer-key.test.ts",
          "cwd": "src/mcp/vice",
          "exitStatus": 0,
          "excerpt": "TAP version 13\n# Subtest: ANSWER.sha256 is exactly 64 hex characters\nok 1 - ANSWER.sha256 is exactly 64 hex characters\n  ---\n  duration_ms: 2.888561\n  type: 'test'\n  ...\n# Subtest: ANSWER.sha256 matches the sha256 recomputed from ANSWER.md's own canonical line (T-11-SEAL-DRIFT)\nok 2 - ANSWER.sha256 matches the sha256 recomputed from ANSWER.md's own canonical line (T-11-SEAL-DRIFT)\n  ---\n  duration_ms: 1.957713\n  type: 'test'\n  ...\n# Subtest: the canonical answer line matches QUESTION.md's own grammar (four lowercase key=value fields, single spaces)\nok 3 - the canonical answer line matches QUESTION.md's own grammar (four lowercase key=value fields, single spaces)\n  ---\n  duration_ms: 0.701534\n  type: 'test'\n  ...\n# Subtest: QUESTION.md does not contain the canonical answer line (T-11-LEAK)\nok 4 - QUESTION.md does not contain the canonical answer line (T-11-LEAK)\n  ---\n  duration_ms: 0.59501\n  type: 'test'\n  ...\n# Subtest: QUESTION.md does not contain the sealed answer's distinctive field values (T-11-LEAK)\nok 5 - QUESTION.md does not contain the sealed answer's distinctive field values (T-11-LEAK)\n  ---\n  duration_ms: 0.960059\n  type: 'test'\n  ...\n# Subtest: SESSION-B-ANSWER.md's canonical line hashes to the sealed ANSWER.sha256 (criterion 1, D-26 two-session comparison)\nok 6 - SESSION-B-ANSWER.md's canonical line hashes to the sealed ANSWER.sha256 (criterion 1, D-26 two-session comparison)\n  ---\n  duration_ms: 0.630714\n  type: 'test'\n  ...\n# Subtest: SESSION-B-ANSWER.md's canonical line matches QUESTION.md's own grammar\nok 7 - SESSION-B-ANSWER.md's canonical line matches QUESTION.md's own grammar\n  ---"
        }
      }
    },
    {
      "historicalPath": "src/mcp/vice/r2000-spawn-seam.test.ts",
      "verdict": "re-pointed",
      "newSubject": "src/mcp/vice/spawn-seam.test.ts",
      "removalTrigger": "The project ceasing to spawn an emulator binary from Node at all -- for instance if backend detection moves entirely into the host launcher and `backend-detect.mts`'s `probeBackend()` is deleted. `EXPECTED_EMULATOR_SPAWN_SITES` would then be empty and the guard's own non-vacuity floor would red rather than pass vacuously, which is the signal to remove it.",
      "plant": {
        "kind": "worktree",
        "file": "src/mcp/vice/backend-detect.mts",
        "find": "spawnSync(binPath, [flag], {",
        "replace": "spawnSync(binPath, flag, {"
      },
      "guard": {
        "argv": [
          "--test",
          "spawn-seam.test.ts"
        ],
        "cwd": "src/mcp/vice"
      },
      "note": "Plant removes the argv-array brackets at the ONE real emulator spawn call, turning `spawnSync(binPath, [flag], ...)` into the bare-string form. This is the exact command-injection invariant the seam exists to hold, so the plant is the real violation rather than an arbitrary token swap. Expected to trip: `every discovered emulator spawn site uses the argv-array form and builds no shell command string` (`allCallsUseSafeForm` false) and `backend-detect.mts's own probeBackend() spawnSync(binPath, [flag] call is discovered and reports safe`.",
      "observedRed": {
        "command": "node --test spawn-seam.test.ts",
        "cwd": "src/mcp/vice",
        "plant": {
          "kind": "worktree",
          "file": "src/mcp/vice/backend-detect.mts",
          "find": "spawnSync(binPath, [flag], {",
          "replace": "spawnSync(binPath, flag, {"
        },
        "exitStatus": 1,
        "excerpt": "not ok 2 - every discovered emulator spawn site uses the argv-array form and builds no shell command string\n  ---\n  duration_ms: 157.644173\n  type: 'test'\n  location: '/home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/spawn-seam.test.ts:301:1'\n  failureType: 'testCodeFailure'\n  error: 'backend-detect.mts: not every emulator spawn call passes an argv ARRAY as its second argument (the command-injection invariant this seam exists to hold)'\n  code: 'ERR_ASSERTION'\n  name: 'AssertionError'\n  expected: true\n  actual: false\n  operator: '=='\n  stack: |-\n    TestContext.<anonymous> (file:///home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/spawn-seam.test.ts:311:12)\n    Test.runInAsyncScope (node:async_hooks:214:14)\n    Test.run (node:internal/test_runner/test:1047:25)\n    Test.processPendingSubtests (node:internal/test_runner/test:744:18)\n    Test.postRun (node:internal/test_runner/test:1173:19)\n    Test.run (node:internal/test_runner/test:1101:12)\n    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)\n  ...\n# Subtest: non-vacuity: the scanned module set is real, and at least one emulator spawn call site was discovered\nok 3 - non-vacuity: the scanned module set is real, and at least one emulator spawn call site was discovered\n  ---\n  duration_ms: 170.522171\n  type: 'test'\n  ...\n# Subtest: planted violation: a module that spawns the emulator through a shell command string is reported unsafe\nok 4 - planted violation: a module that spawns the emulator through a shell command string is reported unsafe\n  ---\n  duration_ms: 0.768215\n  type: 'test'\n  ...\n# Subtest: planted violation: a module whose emulator spawn passes an argv array reports safe\nok 5 - planted violation: a module whose emulator spawn passes an argv array reports safe\n  ---\n  duration_ms: 0.664253\n  type: 'test'\n  ...\n# Subtest: planted violation: an argv-form emulator spawn whose second argument is a bare string, not an array, is reported unsafe",
        "control": {
          "command": "node --test spawn-seam.test.ts",
          "cwd": "src/mcp/vice",
          "exitStatus": 0,
          "excerpt": "TAP version 13\n# Subtest: the discovered emulator spawn-site set equals EXPECTED_EMULATOR_SPAWN_SITES exactly, in both directions\nok 1 - the discovered emulator spawn-site set equals EXPECTED_EMULATOR_SPAWN_SITES exactly, in both directions\n  ---\n  duration_ms: 320.035271\n  type: 'test'\n  ...\n# Subtest: every discovered emulator spawn site uses the argv-array form and builds no shell command string\nok 2 - every discovered emulator spawn site uses the argv-array form and builds no shell command string\n  ---\n  duration_ms: 168.100942\n  type: 'test'\n  ...\n# Subtest: non-vacuity: the scanned module set is real, and at least one emulator spawn call site was discovered\nok 3 - non-vacuity: the scanned module set is real, and at least one emulator spawn call site was discovered\n  ---\n  duration_ms: 220.036142\n  type: 'test'\n  ...\n# Subtest: planted violation: a module that spawns the emulator through a shell command string is reported unsafe\nok 4 - planted violation: a module that spawns the emulator through a shell command string is reported unsafe\n  ---\n  duration_ms: 0.783833\n  type: 'test'\n  ...\n# Subtest: planted violation: a module whose emulator spawn passes an argv array reports safe\nok 5 - planted violation: a module whose emulator spawn passes an argv array reports safe\n  ---\n  duration_ms: 0.577835\n  type: 'test'\n  ...\n# Subtest: planted violation: an argv-form emulator spawn whose second argument is a bare string, not an array, is reported unsafe\nok 6 - planted violation: an argv-form emulator spawn whose second argument is a bare string, not an array, is reported unsafe\n  ---\n  duration_ms: 0.486762\n  type: 'test'\n  ...\n# Subtest: planted violation control: an emulator spawn mention that exists ONLY inside a block comment and a string literal is NOT reported\nok 7 - planted violation control: an emulator spawn mention that exists ONLY inside a block comment and a string literal is NOT reported\n  ---"
        }
      }
    },
    {
      "historicalPath": "src/mcp/vice/r2000-cli.test.ts",
      "verdict": "re-pointed",
      "newSubject": "src/mcp/vice/anno-cli.test.ts",
      "removalTrigger": "The `anno` CLI ceasing to be a supported entry point -- if the three surviving verbs move behind MCP tools only and `anno-cli.ts` is deleted, this guard has no subject. It must be removed with that file, never left asserting over a CLI nobody can invoke.",
      "plant": {
        "kind": "worktree",
        "file": "src/mcp/vice/anno-cli.ts",
        "find": "\"export-asm\": [\"--store\", \"--out\", \"--force\"],",
        "replace": "\"export-asmX\": [\"--store\", \"--out\", \"--force\"],"
      },
      "guard": {
        "argv": [
          "--test",
          "--test-name-pattern",
          "VERB_OPTIONS carries exactly the surviving verbs",
          "anno-cli.test.ts"
        ],
        "cwd": "src/mcp/vice"
      },
      "note": "Plant renames one key of `VERB_OPTIONS` so the map no longer carries exactly the surviving verbs. GUARD ARGV DEVIATION (recorded): the guard is scoped with `--test-name-pattern` rather than running the whole file. MEASURED REASON, not a convenience -- the full `node --test anno-cli.test.ts` takes 20596ms and the harness's `GUARD_RUN_TIMEOUT_MS` is 15000ms; a timeout is mapped to status 1, which would have recorded a TIMEOUT as an observed red while the green control timed out too and the row went UNMEASURABLE. Scoped, the run takes 2675ms. The command still names the row's `newSubject` and still runs that guard's own assertion. Expected to trip: `VERB_OPTIONS carries exactly the surviving verbs`.",
      "observedRed": {
        "command": "node --test --test-name-pattern VERB_OPTIONS carries exactly the surviving verbs anno-cli.test.ts",
        "cwd": "src/mcp/vice",
        "plant": {
          "kind": "worktree",
          "file": "src/mcp/vice/anno-cli.ts",
          "find": "\"export-asm\": [\"--store\", \"--out\", \"--force\"],",
          "replace": "\"export-asmX\": [\"--store\", \"--out\", \"--force\"],"
        },
        "exitStatus": 1,
        "excerpt": "not ok 1 - VERB_OPTIONS carries exactly the surviving verbs\n  ---\n  duration_ms: 4.828684\n  type: 'test'\n  location: '/home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-cli.test.ts:331:1'\n  failureType: 'testCodeFailure'\n  error: |-\n    Expected values to be strictly deep-equal:\n    + actual - expected\n    \n      [\n        'coverage',\n    +   'export-asmX',\n    -   'export-asm',\n        'render-memmap'\n      ]\n    \n  code: 'ERR_ASSERTION'\n  name: 'AssertionError'\n  expected:\n    0: 'coverage'\n    1: 'export-asm'\n    2: 'render-memmap'\n  actual:\n    0: 'coverage'\n    1: 'export-asmX'\n    2: 'render-memmap'\n  operator: 'deepStrictEqual'\n  stack: |-\n    TestContext.<anonymous> (file:///home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-cli.test.ts:332:10)\n    Test.runInAsyncScope (node:async_hooks:214:14)\n    Test.run (node:internal/test_runner/test:1047:25)\n    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)\n  ...\n1..1\n# tests 1\n# suites 0\n# pass 0\n# fail 1\n# cancelled 0",
        "control": {
          "command": "node --test --test-name-pattern VERB_OPTIONS carries exactly the surviving verbs anno-cli.test.ts",
          "cwd": "src/mcp/vice",
          "exitStatus": 0,
          "excerpt": "TAP version 13\n# (node:256142) ExperimentalWarning: SQLite is an experimental feature and might change at any time\n# (Use `node --trace-warnings ...` to show where the warning was created)\n# Subtest: VERB_OPTIONS carries exactly the surviving verbs\nok 1 - VERB_OPTIONS carries exactly the surviving verbs\n  ---\n  duration_ms: 5.365528\n  type: 'test'\n  ...\n1..1\n# tests 1\n# suites 0\n# pass 1\n# fail 0\n# cancelled 0\n# skipped 0\n# todo 0\n# duration_ms 2767.82162"
        }
      }
    },
    {
      "historicalPath": "src/mcp/vice/r2000-confidence.test.ts",
      "verdict": "re-pointed",
      "newSubject": "src/mcp/vice/anno-confidence.test.ts",
      "removalTrigger": "The confidence-grade vocabulary ceasing to be a closed set written down in one place -- for instance if grades become free-form user text. `CONFIDENCE_GRADES` is the ONE place the vocabulary is recorded; with no closed set there is no five-member shape to pin and the guard should be removed rather than relaxed.",
      "plant": {
        "kind": "worktree",
        "file": "src/mcp/vice/anno-confidence.ts",
        "find": "phrase: \"probable code\",",
        "replace": "phrase: \"probable-code\","
      },
      "guard": {
        "argv": [
          "--test",
          "anno-confidence.test.ts"
        ],
        "cwd": "src/mcp/vice"
      },
      "note": "Research (32-RESEARCH.md Sec 2.3) identifies this member as ALREADY carrying an in-test non-vacuity assertion, via `anno-derive`. That assertion is used here only as a MAP of where a reusable plant shape exists -- it is NOT the evidence. Per D-04 the evidence is the harness's machine-captured non-zero exit against the real subject, recorded in `observedRed` below. Plant swaps a space for a hyphen in one grade's `phrase`, the same class of violation the in-test assertion describes, applied to the REAL `CONFIDENCE_GRADES` table. Expected to trip: `CONFIDENCE_GRADES has exactly five members with the exact five phrases from the template`.",
      "observedRed": {
        "command": "node --test anno-confidence.test.ts",
        "cwd": "src/mcp/vice",
        "plant": {
          "kind": "worktree",
          "file": "src/mcp/vice/anno-confidence.ts",
          "find": "phrase: \"probable code\",",
          "replace": "phrase: \"probable-code\","
        },
        "exitStatus": 1,
        "excerpt": "not ok 1 - CONFIDENCE_GRADES has exactly five members with the exact five phrases from the template\n  ---\n  duration_ms: 8.463384\n  type: 'test'\n  location: '/home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-confidence.test.ts:20:1'\n  failureType: 'testCodeFailure'\n  error: |-\n    Expected values to be strictly deep-equal:\n    + actual - expected\n    \n      [\n        'confirmed code',\n    +   'probable-code',\n    -   'probable code',\n        'confirmed data',\n        'probable data',\n        'unknown'\n      ]\n    \n  code: 'ERR_ASSERTION'\n  name: 'AssertionError'\n  expected:\n    0: 'confirmed code'\n    1: 'probable code'\n    2: 'confirmed data'\n    3: 'probable data'\n    4: 'unknown'\n  actual:\n    0: 'confirmed code'\n    1: 'probable-code'\n    2: 'confirmed data'\n    3: 'probable data'\n    4: 'unknown'\n  operator: 'deepStrictEqual'\n  stack: |-\n    TestContext.<anonymous> (file:///home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-confidence.test.ts:23:10)\n    Test.runInAsyncScope (node:async_hooks:214:14)\n    Test.run (node:internal/test_runner/test:1047:25)\n    Test.start (node:internal/test_runner/test:944:17)\n    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)",
        "control": {
          "command": "node --test anno-confidence.test.ts",
          "cwd": "src/mcp/vice",
          "exitStatus": 0,
          "excerpt": "TAP version 13\n# Subtest: CONFIDENCE_GRADES has exactly five members with the exact five phrases from the template\nok 1 - CONFIDENCE_GRADES has exactly five members with the exact five phrases from the template\n  ---\n  duration_ms: 2.648541\n  type: 'test'\n  ...\n# Subtest: CONFIDENCE_GRADES tokens and brackets are the five canonical bracket tokens\nok 2 - CONFIDENCE_GRADES tokens and brackets are the five canonical bracket tokens\n  ---\n  duration_ms: 0.283068\n  type: 'test'\n  ...\n# Subtest: every grade round-trips through formatConfidenceComment -> parseConfidencePrefix\nok 3 - every grade round-trips through formatConfidenceComment -> parseConfidencePrefix\n  ---\n  duration_ms: 0.506804\n  type: 'test'\n  ...\n# Subtest: parseConfidencePrefix returns grade: null for a plain comment, without throwing\nok 4 - parseConfidencePrefix returns grade: null for a plain comment, without throwing\n  ---\n  duration_ms: 0.195745\n  type: 'test'\n  ...\n# Subtest: parseConfidencePrefix returns grade: null for an empty comment\nok 5 - parseConfidencePrefix returns grade: null for an empty comment\n  ---\n  duration_ms: 0.308877\n  type: 'test'\n  ...\n# Subtest: parseConfidencePrefix returns grade: null for a comment with an unclosed bracket\nok 6 - parseConfidencePrefix returns grade: null for a comment with an unclosed bracket\n  ---\n  duration_ms: 0.165805\n  type: 'test'\n  ...\n# Subtest: parseConfidencePrefix throws on a plain typo, naming the offending token and all five valid ones\nok 7 - parseConfidencePrefix throws on a plain typo, naming the offending token and all five valid ones\n  ---"
        }
      }
    },
    {
      "historicalPath": "src/mcp/vice/r2000-coverage-grammar.test.ts",
      "verdict": "re-pointed",
      "newSubject": "src/mcp/vice/anno-coverage-grammar.test.ts",
      "removalTrigger": "The synthetic 6502 grammar corpus ceasing to be the way dispatch-table detection is exercised -- if `anno-coverage.ts`'s scanner is retired or replaced by a measurement against real captured programs, the generated alphabet has nothing to prove and the guard should be deleted with the generator.",
      "plant": {
        "kind": "worktree",
        "file": "src/mcp/vice/disasm-decoder.ts",
        "find": "case \"absolute_y\": {\n        const value = b1! | (b2! << 8);",
        "replace": "case \"absolute_y\": {\n        const value = b1! | (b2! << 9);"
      },
      "guard": {
        "argv": [
          "--test",
          "anno-coverage-grammar.test.ts"
        ],
        "cwd": "src/mcp/vice"
      },
      "note": "Research (Sec 2.3) lists this member as already carrying an in-test non-vacuity assertion; that is a map, not the evidence (D-04). Plant corrupts the high-byte shift for the `absolute_y` addressing mode only (`<< 8` -> `<< 9`), inside the one case block, so every y-indexed absolute operand decodes to a wrong address. Plant file is `disasm-decoder.ts`, deliberately NOT `anno-coverage.ts` -- the sibling row `r2000-coverage.test.ts` plants there, and giving the two rows different files means neither can mask the other even though both tests import both modules. Expected to trip: `every y-indexed load the alphabet emits renders to the absolute_y opcode and never the zeropage_y one`, and the payload/decode assertions downstream of it.",
      "observedRed": {
        "command": "node --test anno-coverage-grammar.test.ts",
        "cwd": "src/mcp/vice",
        "plant": {
          "kind": "worktree",
          "file": "src/mcp/vice/disasm-decoder.ts",
          "find": "case \"absolute_y\": {\n        const value = b1! | (b2! << 8);",
          "replace": "case \"absolute_y\": {\n        const value = b1! | (b2! << 9);"
        },
        "exitStatus": 1,
        "excerpt": "not ok 15 - the set of arrangements the instrument PROVES equals exactly the set the oracle says carries a proven link\n  ---\n  duration_ms: 10.711054\n  type: 'test'\n  location: '/home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-coverage-grammar.test.ts:1964:1'\n  failureType: 'testCodeFailure'\n  error: |-\n    the instrument and the oracle disagree over 2000 composed payloads.\n      PROVEN but NOT expected (0) -- false positives of the kind that has failed this criterion three times:\n      EXPECTED but NOT proven (22) -- an over-tightening that would make the instrument measure nothing:\n        xx-hi-first/adjacent-stores#0  `lda $0838,x : sta $fc : lda $0830,x : sta $fb : jmp ($00fb)`  [oracle: LINKED by R1..R6 class-3 zeropage-vector-jumped-through]\n        xx-hi-first/adjacent-stores#6  `lda $0838,x : sta $fc : nop : nop : lda $0830,x : sta $fb : jmp ($00fb)`  [oracle: LINKED by R1..R6 class-3 zeropage-vector-jumped-through]\n        xx-hi-first/adjacent-stores-and-indirect-read#0  `lda $0838,x : sta $fc : lda $0830,x : nop : nop : sta $fb : lda ($fb),y : jmp ($00fb)`  [oracle: LINKED by R1..R6 class-3 zeropage-vector-jumped-through]\n        xx-hi-first/adjacent-stores-and-indirect-read#3  `lda $0838,x : sta $fc : lda $0830,x : sta $fb : lda ($fb),y : jmp ($00fb)`  [oracle: LINKED by R1..R6 class-3 zeropage-vector-jumped-through]\n        xx-hi-first/adjacent-stores-and-indirect-read#9  `lda $0838,x : sta $fc : lda ($fb),y : lda $0830,x : sta $fb : nop : nop : jmp ($00fb)`  [oracle: LINKED by R1..R6 class-3 zeropage-vector-jumped-through]\n        xx-hi-first/pushes-and-adjacent-stores#6  `lda $0838,x : sta $fc : lda $0830,x : pha : sta $fb : pha : jmp ($00fb)`  [oracle: LINKED by R1..R6 class-3 zeropage-vector-jumped-through]\n        xx-hi-first/two-pushes#pin0  `lda $0838,x : pha : lda $0830,x : pha : rts`  [oracle: LINKED by R2+R6 class-4 window published]\n        xx-lo-first/adjacent-stores#3  `lda $0830,x : sta $fb : lda $0838,x : sta $fc : nop : jmp ($00fb)`  [oracle: LINKED by R1..R6 class-3 zeropage-vector-jumped-through]\n        ... and 14 more\n    + actual - expected\n    \n      {\n    +   expectedNotProven: [\n    +     'xx-hi-first/adjacent-stores#0',\n    +     'xx-hi-first/adjacent-stores#6',\n    +     'xx-hi-first/adjacent-stores-and-indirect-read#0',\n    +     'xx-hi-first/adjacent-stores-and-indirect-read#3',\n    +     'xx-hi-first/adjacent-stores-and-indirect-read#9',\n    +     'xx-hi-first/pushes-and-adjacent-stores#6',\n    +     'xx-hi-first/two-pushes#pin0',\n    +     'xx-lo-first/adjacent-stores#3',\n    +     'xx-lo-first/adjacent-stores#pin1',\n    +     'xx-lo-first/adjacent-stores-and-indirect-read#6',\n    +     'xx-lo-first/four-stores-two-pairs#7',\n    +     'xx-lo-first/pushes-and-adjacent-stores#0',\n    +     'xx-lo-first/pushes-and-adjacent-stores#6',\n    +     'xx-lo-first/pushes-and-adjacent-stores#pin0',\n    +     'yy-hi-first/four-stores-two-pairs#7',\n    +     'yy-hi-first/pushes-and-adjacent-stores#3',\n    +     'yy-hi-first/two-pushes#0',",
        "control": {
          "command": "node --test anno-coverage-grammar.test.ts",
          "cwd": "src/mcp/vice",
          "exitStatus": 0,
          "excerpt": "TAP version 13\n# Subtest: the composed corpus is bounded, stratified across every family, and large enough to be a population\nok 1 - the composed corpus is bounded, stratified across every family, and large enough to be a population\n  ---\n  duration_ms: 2.944705\n  type: 'test'\n  ...\n# Subtest: every family contributed at least MIN_PER_FAMILY members, so the corpus cap is not a prefix of one family\nok 2 - every family contributed at least MIN_PER_FAMILY members, so the corpus cap is not a prefix of one family\n  ---\n  duration_ms: 1.011838\n  type: 'test'\n  ...\n# Subtest: building the corpus twice yields byte-identical payloads in identical order\nok 3 - building the corpus twice yields byte-identical payloads in identical order\n  ---\n  duration_ms: 151.762613\n  type: 'test'\n  ...\n# Subtest: every payload is exactly 64 bytes with an intact data tail, and no two indexed arrangements are byte-identical\nok 4 - every payload is exactly 64 bytes with an intact data tail, and no two indexed arrangements are byte-identical\n  ---\n  duration_ms: 33.946916\n  type: 'test'\n  ...\n# Subtest: a twin differs from its own indexed member, and two members share a twin only when they differ solely in index register\nok 5 - a twin differs from its own indexed member, and two members share a twin only when they differ solely in index register\n  ---\n  duration_ms: 25.492461\n  type: 'test'\n  ...\n# Subtest: no generated payload contains the JAM opcode at any offset\nok 6 - no generated payload contains the JAM opcode at any offset\n  ---\n  duration_ms: 25.656743\n  type: 'test'\n  ...\n# Subtest: every arrangement carries exactly one terminator and it is the last fragment\nok 7 - every arrangement carries exactly one terminator and it is the last fragment\n  ---"
        }
      }
    },
    {
      "historicalPath": "src/mcp/vice/r2000-coverage.test.ts",
      "verdict": "re-pointed",
      "newSubject": "src/mcp/vice/anno-coverage.test.ts",
      "removalTrigger": "The coverage report shape ceasing to be consumed by a second party. `COVERAGE_SCHEMA_VERSION` exists because Phase 20's sweep and Phase 21's dispatch reuse both read this shape; when no consumer outside this module reads it, the version pin is ceremony and the guard should be removed with the last consumer.",
      "plant": {
        "kind": "worktree",
        "file": "src/mcp/vice/anno-coverage.ts",
        "find": "export const COVERAGE_SCHEMA_VERSION = 2;",
        "replace": "export const COVERAGE_SCHEMA_VERSION = 3;"
      },
      "guard": {
        "argv": [
          "--test",
          "anno-coverage.test.ts"
        ],
        "cwd": "src/mcp/vice"
      },
      "note": "Research (Sec 2.3) lists this member as already carrying an in-test non-vacuity assertion; used as a map only, never as the proof (D-04). Plant bumps the schema version by one. This is the cleanest possible plant in this file because the test pins the version to a LITERAL `2` alongside the imported constant, precisely so a bump is always deliberate -- so the mutation cannot be absorbed self-consistently the way a change to `MAX_TABLE_ENTRIES` or `SPLIT_TABLE_WINDOW` would be (both are used by the walk AND by the assertion, so both sides move together and the plant would not bite). Expected to trip: the `assert.equal(COVERAGE_SCHEMA_VERSION, 2, ...)` literal pin.",
      "observedRed": {
        "command": "node --test anno-coverage.test.ts",
        "cwd": "src/mcp/vice",
        "plant": {
          "kind": "worktree",
          "file": "src/mcp/vice/anno-coverage.ts",
          "find": "export const COVERAGE_SCHEMA_VERSION = 2;",
          "replace": "export const COVERAGE_SCHEMA_VERSION = 3;"
        },
        "exitStatus": 1,
        "excerpt": "not ok 1 - the report's top-level key set is exactly the pinned set, in order, and carries the schema version\n  ---\n  duration_ms: 27.385793\n  type: 'test'\n  location: '/home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-coverage.test.ts:577:1'\n  failureType: 'testCodeFailure'\n  error: |-\n    the schema version is pinned to a LITERAL here so a bump is always deliberate. It moved 1 -> 2 in 19-08: the top-level key set above is UNCHANGED, but the `dispatch` sub-object's target vocabulary changed -- `discoveredTargets` narrowed to evidence-backed targets only, and the ungated split lo/hi pairings it used to include moved to the advisory sibling `splitTableCandidates`. A consumer reading `discoveredTargets` gets a smaller, honest set than at version 1, and this number is the only signal it gets. Accepted by a human at 19-08's decision checkpoint (option `narrow-and-add-sibling`).\n    \n    3 !== 2\n    \n  code: 'ERR_ASSERTION'\n  name: 'AssertionError'\n  expected: 2\n  actual: 3\n  operator: 'strictEqual'\n  stack: |-\n    TestContext.<anonymous> (file:///home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-coverage.test.ts:586:10)\n    Test.runInAsyncScope (node:async_hooks:214:14)\n    Test.run (node:internal/test_runner/test:1047:25)\n    Test.start (node:internal/test_runner/test:944:17)\n    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)\n  ...\n# Subtest: COV-01: no key anywhere in the report matches a combined-figure vocabulary\nok 2 - COV-01: no key anywhere in the report matches a combined-figure vocabulary\n  ---\n  duration_ms: 4.085319\n  type: 'test'\n  ...\n# Subtest: independence: rewriting every block entry to one type leaves every census byte count unchanged and moves only the divergence sub-report\nok 3 - independence: rewriting every block entry to one type leaves every census byte count unchanged and moves only the divergence sub-report\n  ---\n  duration_ms: 3.953282\n  type: 'test'\n  ...\n# Subtest: the production block-spelling list is the DERIVED union of BOTH accepted vocabularies, with no duplicates\nok 4 - the production block-spelling list is the DERIVED union of BOTH accepted vocabularies, with no duplicates\n  ---\n  duration_ms: 0.73984\n  type: 'test'",
        "control": {
          "command": "node --test anno-coverage.test.ts",
          "cwd": "src/mcp/vice",
          "exitStatus": 0,
          "excerpt": "# Subtest: RE-DERIVED-ANSWER.md's canonical line hashes to the sealed ANSWER.sha256, and a missing or empty fence FAILS rather than skips (T-19-RETROFIT / T-19-VACUOUS-CHECK)\nok 115 - RE-DERIVED-ANSWER.md's canonical line hashes to the sealed ANSWER.sha256, and a missing or empty fence FAILS rather than skips (T-19-RETROFIT / T-19-VACUOUS-CHECK)\n  ---\n  duration_ms: 0.258447\n  type: 'test'\n  ...\n# Subtest: LIVE non-vacuity: both routes are recomputed from the committed fixture and both reproduce the sealed line\nok 116 - LIVE non-vacuity: both routes are recomputed from the committed fixture and both reproduce the sealed line\n  ---\n  duration_ms: 1.530892\n  type: 'test'\n  ...\n# Subtest: CR-05 (A): a real .prg is censused -- payload decoded, origin from its own load address, non-zero byte count\nok 117 - CR-05 (A): a real .prg is censused -- payload decoded, origin from its own load address, non-zero byte count\n  ---\n  duration_ms: 1.632364\n  type: 'test'\n  ...\n# Subtest: CR-05 (B): an exactly-65536-byte flat capture with a .raw extension decodes at origin 0\nok 118 - CR-05 (B): an exactly-65536-byte flat capture with a .raw extension decodes at origin 0\n  ---\n  duration_ms: 283.91378\n  type: 'test'\n  ...\n# Subtest: CR-05 (C, WR-07): a SHORT flat .raw is refused BY NAME, never parsed as a load address plus payload\nok 119 - CR-05 (C, WR-07): a SHORT flat .raw is refused BY NAME, never parsed as a load address plus payload\n  ---\n  duration_ms: 1.923352\n  type: 'test'\n  ...\n# Subtest: CR-05 (D): the retired JSON project form still loads, so an existing project file is not broken\nok 120 - CR-05 (D): the retired JSON project form still loads, so an existing project file is not broken\n  ---\n  duration_ms: 1.988882\n  type: 'test'\n  ...\n# Subtest: CR-05 (E, agreement): loadProjectImage and anno-tools' loadImage answer identically for one .prg and one flat capture\nok 121 - CR-05 (E, agreement): loadProjectImage and anno-tools' loadImage answer identically for one .prg and one flat capture\n  ---\n  duration_ms: 15.973634"
        }
      }
    },
    {
      "historicalPath": "src/mcp/vice/r2000-d64.test.ts",
      "verdict": "re-pointed",
      "newSubject": "src/mcp/vice/anno-d64.test.ts",
      "removalTrigger": "`.d64` images ceasing to be an accepted input form for the annotation pipeline. If projects are only ever loaded from `.prg`, `anno-d64.ts` has no caller and both it and this guard should go, rather than the guard being kept green over a parser nothing reaches.",
      "plant": {
        "kind": "worktree",
        "file": "src/mcp/vice/anno-d64.ts",
        "find": "if (image.length !== 174848) {",
        "replace": "if (image.length !== 174847) {"
      },
      "guard": {
        "argv": [
          "--test",
          "anno-d64.test.ts"
        ],
        "cwd": "src/mcp/vice"
      },
      "note": "PLANT DESIGNED FROM SCRATCH -- research (Sec 2.3) records this member as having no reusable in-test non-vacuity assertion. The pinned constant broken is the plain-1541-image length `174848` in `assertPlainImage()`, the ONE place the documented input limit is enforced. Off by one, so the oversize-variant refusal at 175531 bytes still fires and the failure is unambiguously the acceptance side. Expected to trip: `assertPlainImage: passes for exactly 174848 bytes`.",
      "observedRed": {
        "command": "node --test anno-d64.test.ts",
        "cwd": "src/mcp/vice",
        "plant": {
          "kind": "worktree",
          "file": "src/mcp/vice/anno-d64.ts",
          "find": "if (image.length !== 174848) {",
          "replace": "if (image.length !== 174847) {"
        },
        "exitStatus": 1,
        "excerpt": "not ok 9 - assertPlainImage: passes for exactly 174848 bytes\n  ---\n  duration_ms: 2.203051\n  type: 'test'\n  location: '/home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-d64.test.ts:191:1'\n  failureType: 'testCodeFailure'\n  error: |-\n    Got unwanted exception.\n    Actual message: \"assertPlainImage: expected a plain 174848-byte, 35-track .d64 image with no error-info bytes, got 174848 bytes\"\n  code: 'ERR_ASSERTION'\n  name: 'AssertionError'\n  actual:\n  error: 'assertPlainImage: expected a plain 174848-byte, 35-track .d64 image with no error-info bytes, got 174848 bytes'\n  stack: |-\n    assertPlainImage (file:///home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-d64.ts:306:11)\n    file:///home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-d64.test.ts:192:29\n    getActual (node:assert:609:5)\n    Function.doesNotThrow (node:assert:777:32)\n    TestContext.<anonymous> (file:///home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-d64.test.ts:192:10)\n    Test.runInAsyncScope (node:async_hooks:214:14)\n    Test.run (node:internal/test_runner/test:1047:25)\n    Test.processPendingSubtests (node:internal/test_runner/test:744:18)\n    Test.postRun (node:internal/test_runner/test:1173:19)\n    Test.run (node:internal/test_runner/test:1101:12)\n  operator: 'doesNotThrow'\n  stack: |-\n    TestContext.<anonymous> (file:///home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-d64.test.ts:192:10)\n    Test.runInAsyncScope (node:async_hooks:214:14)\n    Test.run (node:internal/test_runner/test:1047:25)\n    Test.processPendingSubtests (node:internal/test_runner/test:744:18)\n    Test.postRun (node:internal/test_runner/test:1173:19)\n    Test.run (node:internal/test_runner/test:1101:12)\n    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)\n  ...\n# Subtest: extractEntry: a valid image truncated into the file's own sector throws naming the sector and the actual image length (WR-05)\nok 10 - extractEntry: a valid image truncated into the file's own sector throws naming the sector and the actual image length (WR-05)\n  ---\n  duration_ms: 1.693027\n  type: 'test'\n  ...",
        "control": {
          "command": "node --test anno-d64.test.ts",
          "cwd": "src/mcp/vice",
          "exitStatus": 0,
          "excerpt": "TAP version 13\n# Subtest: listEntries: returns both entry names, types, and starting track/sector\nok 1 - listEntries: returns both entry names, types, and starting track/sector\n  ---\n  duration_ms: 6.685153\n  type: 'test'\n  ...\n# Subtest: extractEntry: returns exactly the written payload bytes, including the load address, honouring the final sector's used-byte count\nok 2 - extractEntry: returns exactly the written payload bytes, including the load address, honouring the final sector's used-byte count\n  ---\n  duration_ms: 3.138132\n  type: 'test'\n  ...\n# Subtest: extractEntry: case-insensitive name match returns the same bytes\nok 3 - extractEntry: case-insensitive name match returns the same bytes\n  ---\n  duration_ms: 1.231091\n  type: 'test'\n  ...\n# Subtest: extractEntry: unknown name throws, naming both the requested name and the available entries\nok 4 - extractEntry: unknown name throws, naming both the requested name and the available entries\n  ---\n  duration_ms: 1.75822\n  type: 'test'\n  ...\n# Subtest: extractEntry: an ambiguous name (two entries sharing it) throws naming the ambiguity and returns nothing\nok 5 - extractEntry: an ambiguous name (two entries sharing it) throws naming the ambiguity and returns nothing\n  ---\n  duration_ms: 0.991654\n  type: 'test'\n  ...\n# Subtest: extractEntry: a self-referential (corrupt) chain throws naming the revisited track/sector rather than looping\nok 6 - extractEntry: a self-referential (corrupt) chain throws naming the revisited track/sector rather than looping\n  ---\n  duration_ms: 0.634504\n  type: 'test'\n  ...\n# Subtest: extractEntry: an out-of-image pointer throws naming the offending pointer rather than reading past the buffer\nok 7 - extractEntry: an out-of-image pointer throws naming the offending pointer rather than reading past the buffer\n  ---"
        }
      }
    },
    {
      "historicalPath": "src/mcp/vice/r2000-enum-gen.test.ts",
      "verdict": "re-pointed",
      "newSubject": "src/mcp/vice/anno-enum-gen.test.ts",
      "removalTrigger": "The ACME enum export ceasing to be a shipped capability. These identifiers only matter because generated source must assemble under a real ACME; if `export-asm` and the enum generator are withdrawn again (they were once already, in plan 29-07/D-14), the legality rules have no consumer and this guard should be removed with them.",
      "plant": {
        "kind": "worktree",
        "file": "src/mcp/vice/anno-enum-gen.ts",
        "find": "return `$${address.toString(16).toUpperCase().padStart(4, \"0\")}`;",
        "replace": "return `$${address.toString(16).toUpperCase().padStart(5, \"0\")}`;"
      },
      "guard": {
        "argv": [
          "--test",
          "anno-enum-gen.test.ts"
        ],
        "cwd": "src/mcp/vice"
      },
      "note": "PLANT DESIGNED FROM SCRATCH -- research (Sec 2.3) records this member as needing one. The declared shape broken is `registerKeyFor()`'s `$XXXX` format: four uppercase hex digits, zero-padded. `padStart(4` occurs TWICE in this module (lines 159 and 273), so the `find` string carries the whole `return` expression to stay unique -- the harness rejects a `find` that matches other than exactly once, which is what caught the ambiguity. Expected to trip: `registerKeyFor formats addresses as $XXXX (uppercase, 4-hex-digit)`.",
      "observedRed": {
        "command": "node --test anno-enum-gen.test.ts",
        "cwd": "src/mcp/vice",
        "plant": {
          "kind": "worktree",
          "file": "src/mcp/vice/anno-enum-gen.ts",
          "find": "return `$${address.toString(16).toUpperCase().padStart(4, \"0\")}`;",
          "replace": "return `$${address.toString(16).toUpperCase().padStart(5, \"0\")}`;"
        },
        "exitStatus": 1,
        "excerpt": "not ok 1 - variantNameFor(0xd011, 0x1b) === 'YSCROLL3_ROW25_SCREENON_TEXT' (the pinned criterion-3 target)\n  ---\n  duration_ms: 5.966576\n  type: 'test'\n  location: '/home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-enum-gen.test.ts:79:1'\n  failureType: 'testCodeFailure'\n  error: 'variantNameFor: no bit-name table entry for register 0D011 -- anno-regbits.json has no fields for this address (add an OVERRIDES entry in anno-regbits-gen.ts, or exclude it from generation).'\n  code: 'ERR_TEST_FAILURE'\n  stack: |-\n    variantNameFor (file:///home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-enum-gen.ts:190:11)\n    TestContext.<anonymous> (file:///home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-enum-gen.test.ts:80:16)\n    Test.runInAsyncScope (node:async_hooks:214:14)\n    Test.run (node:internal/test_runner/test:1047:25)\n    Test.start (node:internal/test_runner/test:944:17)\n    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)\n  ...\n# Subtest: registerKeyFor formats addresses as $XXXX (uppercase, 4-hex-digit)\nnot ok 2 - registerKeyFor formats addresses as $XXXX (uppercase, 4-hex-digit)\n  ---\n  duration_ms: 2.388233\n  type: 'test'\n  location: '/home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-enum-gen.test.ts:83:1'\n  failureType: 'testCodeFailure'\n  error: |-\n    Expected values to be strictly equal:\n    \n    '0D011' !== '$D011'\n    \n  code: 'ERR_ASSERTION'\n  name: 'AssertionError'\n  expected: '$D011'\n  actual: '0D011'\n  operator: 'strictEqual'\n  stack: |-\n    TestContext.<anonymous> (file:///home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-enum-gen.test.ts:84:10)\n    Test.runInAsyncScope (node:async_hooks:214:14)\n    Test.run (node:internal/test_runner/test:1047:25)\n    Test.processPendingSubtests (node:internal/test_runner/test:744:18)\n    Test.postRun (node:internal/test_runner/test:1173:19)\n    Test.run (node:internal/test_runner/test:1101:12)",
        "control": {
          "command": "node --test anno-enum-gen.test.ts",
          "cwd": "src/mcp/vice",
          "exitStatus": 0,
          "excerpt": "TAP version 13\n# Subtest: variantNameFor(0xd011, 0x1b) === 'YSCROLL3_ROW25_SCREENON_TEXT' (the pinned criterion-3 target)\nok 1 - variantNameFor(0xd011, 0x1b) === 'YSCROLL3_ROW25_SCREENON_TEXT' (the pinned criterion-3 target)\n  ---\n  duration_ms: 6.188277\n  type: 'test'\n  ...\n# Subtest: registerKeyFor formats addresses as $XXXX (uppercase, 4-hex-digit)\nok 2 - registerKeyFor formats addresses as $XXXX (uppercase, 4-hex-digit)\n  ---\n  duration_ms: 0.350962\n  type: 'test'\n  ...\n# Subtest: variantNameFor is injective across all 256 values for $D011\nok 3 - variantNameFor is injective across all 256 values for $D011\n  ---\n  duration_ms: 2.193512\n  type: 'test'\n  ...\n# Subtest: variantNameFor is injective across all 256 values for $D016\nok 4 - variantNameFor is injective across all 256 values for $D016\n  ---\n  duration_ms: 2.696318\n  type: 'test'\n  ...\n# Subtest: variantNameFor is injective across all 256 values for $D018\nok 5 - variantNameFor is injective across all 256 values for $D018\n  ---\n  duration_ms: 1.390069\n  type: 'test'\n  ...\n# Subtest: variantNameFor is injective across all 256 values for $D015\nok 6 - variantNameFor is injective across all 256 values for $D015\n  ---\n  duration_ms: 1.513822\n  type: 'test'\n  ...\n# Subtest: assertLegalAcmeIdentifier rejects '1BAD', 'has space', 'has-dash', '' and accepts a legal identifier\nok 7 - assertLegalAcmeIdentifier rejects '1BAD', 'has space', 'has-dash', '' and accepts a legal identifier\n  ---"
        }
      }
    },
    {
      "historicalPath": "src/mcp/vice/r2000-memmap-render.test.ts",
      "verdict": "re-pointed",
      "newSubject": "src/mcp/vice/anno-memmap-render.test.ts",
      "removalTrigger": "The rendered memory map ceasing to carry a provenance sidecar. The header parser exists so a rendered map can never claim a capture it cannot name; if provenance moves into the annotation store itself, `parseProvenanceHeader` and this guard go together.",
      "plant": {
        "kind": "worktree",
        "file": "src/mcp/vice/anno-memmap-render.ts",
        "find": "if (!/^[0-9a-fA-F]{64}$/.test(sha.trim())) {",
        "replace": "if (!/^[0-9a-fA-F]{63}$/.test(sha.trim())) {"
      },
      "guard": {
        "argv": [
          "--test",
          "anno-memmap-render.test.ts"
        ],
        "cwd": "src/mcp/vice"
      },
      "note": "PLANT DESIGNED FROM SCRATCH -- research (Sec 2.3) records this member as needing one. The pinned constant broken is the SHA-256 hex length in `parseProvenanceHeader()`'s `captureSha256` check (64 -> 63), so a genuine 64-character digest is now rejected. NUL-TAINT DISCLOSURE, as the plan requires: the plant target IS the NUL-carrying module (`anno-memmap-render.ts` carries a NUL at offset 15097 / line 315). This is safe, and the safety is a property of the harness rather than of the choice: `plant()` captures the original with `readFileSync(abs)` and NO encoding, then does the find/replace over a `latin1` round-trip, which is byte-preserving for every code unit including NUL, and `revert()` writes the captured Buffer back verbatim. The plant site (line 221) is far ahead of the NUL, and `git diff --stat` over this file is asserted empty after the run, which is the mechanical check that the round-trip really was byte-identical. Expected to trip: `parseProvenanceHeader accepts a fully-filled valid header`.",
      "observedRed": {
        "command": "node --test anno-memmap-render.test.ts",
        "cwd": "src/mcp/vice",
        "plant": {
          "kind": "worktree",
          "file": "src/mcp/vice/anno-memmap-render.ts",
          "find": "if (!/^[0-9a-fA-F]{64}$/.test(sha.trim())) {",
          "replace": "if (!/^[0-9a-fA-F]{63}$/.test(sha.trim())) {"
        },
        "exitStatus": 1,
        "excerpt": "not ok 1 - parseProvenanceHeader accepts a fully-filled valid header\n  ---\n  duration_ms: 3.690996\n  type: 'test'\n  location: '/home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-memmap-render.test.ts:65:1'\n  failureType: 'testCodeFailure'\n  error: |-\n    provenance sidecar has 1 problem(s):\n      - captureSha256: must be exactly 64 hex characters, got \"aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa\" (length 64)\n  code: 'ERR_TEST_FAILURE'\n  name: 'R2000ProvenanceHeaderError'\n  stack: |-\n    parseProvenanceHeader (file:///home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-memmap-render.ts:244:11)\n    TestContext.<anonymous> (file:///home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-memmap-render.test.ts:66:18)\n    Test.runInAsyncScope (node:async_hooks:214:14)\n    Test.run (node:internal/test_runner/test:1047:25)\n    Test.start (node:internal/test_runner/test:944:17)\n    startSubtestAfterBootstrap (node:internal/test_runner/harness:296:17)\n  ...\n# Subtest: parseProvenanceHeader accepts an optional rasterPositions array\nnot ok 2 - parseProvenanceHeader accepts an optional rasterPositions array\n  ---\n  duration_ms: 0.575642\n  type: 'test'\n  location: '/home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-memmap-render.test.ts:73:1'\n  failureType: 'testCodeFailure'\n  error: |-\n    provenance sidecar has 1 problem(s):\n      - captureSha256: must be exactly 64 hex characters, got \"aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa\" (length 64)\n  code: 'ERR_TEST_FAILURE'\n  name: 'R2000ProvenanceHeaderError'\n  stack: |-\n    parseProvenanceHeader (file:///home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-memmap-render.ts:244:11)\n    TestContext.<anonymous> (file:///home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-memmap-render.test.ts:74:18)\n    Test.runInAsyncScope (node:async_hooks:214:14)\n    Test.run (node:internal/test_runner/test:1047:25)\n    Test.processPendingSubtests (node:internal/test_runner/test:744:18)\n    Test.postRun (node:internal/test_runner/test:1173:19)\n    Test.run (node:internal/test_runner/test:1101:12)\n    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)",
        "control": {
          "command": "node --test anno-memmap-render.test.ts",
          "cwd": "src/mcp/vice",
          "exitStatus": 0,
          "excerpt": "TAP version 13\n# (node:256549) ExperimentalWarning: SQLite is an experimental feature and might change at any time\n# (Use `node --trace-warnings ...` to show where the warning was created)\n# Subtest: parseProvenanceHeader accepts a fully-filled valid header\nok 1 - parseProvenanceHeader accepts a fully-filled valid header\n  ---\n  duration_ms: 3.970977\n  type: 'test'\n  ...\n# Subtest: parseProvenanceHeader accepts an optional rasterPositions array\nok 2 - parseProvenanceHeader accepts an optional rasterPositions array\n  ---\n  duration_ms: 2.036283\n  type: 'test'\n  ...\n# Subtest: parseProvenanceHeader({}) throws listing ALL missing required keys in one message\nok 3 - parseProvenanceHeader({}) throws listing ALL missing required keys in one message\n  ---\n  duration_ms: 2.120833\n  type: 'test'\n  ...\n# Subtest: parseProvenanceHeader refuses a template-placeholder captureSha256 and videoStandard, naming both\nok 4 - parseProvenanceHeader refuses a template-placeholder captureSha256 and videoStandard, naming both\n  ---\n  duration_ms: 0.861236\n  type: 'test'\n  ...\n# Subtest: parseProvenanceHeader refuses a bad hash length\nok 5 - parseProvenanceHeader refuses a bad hash length\n  ---\n  duration_ms: 0.90262\n  type: 'test'\n  ...\n# Subtest: parseProvenanceHeader refuses a videoStandard value that is neither a placeholder nor PAL/NTSC\nok 6 - parseProvenanceHeader refuses a videoStandard value that is neither a placeholder nor PAL/NTSC\n  ---\n  duration_ms: 0.727342\n  type: 'test'\n  ...\n# Subtest: parseProvenanceHeader refuses a malformed rasterPositions"
        }
      }
    },
    {
      "historicalPath": "src/mcp/vice/r2000-regbits.test.ts",
      "verdict": "re-pointed",
      "newSubject": "src/mcp/vice/anno-regbits.test.ts",
      "removalTrigger": "`anno-regbits.json` ceasing to be generated from `memmap.json`. The whole guard is a drift relation between a generated artifact and its source; if the bit tables become hand-maintained, there is no generator to drift from and the guard must be removed rather than re-pointed at a hand-edited file it cannot regenerate.",
      "plant": {
        "kind": "worktree",
        "file": "src/mcp/vice/anno-regbits.json",
        "find": "\"memmapSha256\": \"60a517c1",
        "replace": "\"memmapSha256\": \"60a517c2"
      },
      "guard": {
        "argv": [
          "--test",
          "anno-regbits.test.ts"
        ],
        "cwd": "src/mcp/vice"
      },
      "note": "Research (Sec 2.3) lists this member as already carrying an in-test non-vacuity assertion -- the committed planted-violation test that appends a byte to a SCRATCH COPY of `memmap.json`. That is a map of where the plant shape lives, NOT the evidence (D-04): the committed test mutates a copy, whereas this row mutates the REAL generated artifact and captures the guard's own exit status. Plant changes one hex digit of the generated banner's `memmapSha256`. Expected to trip: `drift guard: the committed banner's memmapSha256 equals memmap.json's current digest`.",
      "observedRed": {
        "command": "node --test anno-regbits.test.ts",
        "cwd": "src/mcp/vice",
        "plant": {
          "kind": "worktree",
          "file": "src/mcp/vice/anno-regbits.json",
          "find": "\"memmapSha256\": \"60a517c1",
          "replace": "\"memmapSha256\": \"60a517c2"
        },
        "exitStatus": 1,
        "excerpt": "not ok 4 - drift guard: the committed banner's memmapSha256 equals memmap.json's current digest\n  ---\n  duration_ms: 6.286789\n  type: 'test'\n  location: '/home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-regbits.test.ts:58:1'\n  failureType: 'testCodeFailure'\n  error: |-\n    Expected values to be strictly equal:\n    + actual - expected\n    \n    + '60a517c2833a44e6dc1a99a949554fa39834b558371d9f1e98531499fe3642fe'\n    - '60a517c1833a44e6dc1a99a949554fa39834b558371d9f1e98531499fe3642fe'\n    \n  code: 'ERR_ASSERTION'\n  name: 'AssertionError'\n  expected: '60a517c1833a44e6dc1a99a949554fa39834b558371d9f1e98531499fe3642fe'\n  actual: '60a517c2833a44e6dc1a99a949554fa39834b558371d9f1e98531499fe3642fe'\n  operator: 'strictEqual'\n  stack: |-\n    TestContext.<anonymous> (file:///home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-regbits.test.ts:61:10)\n    Test.runInAsyncScope (node:async_hooks:214:14)\n    Test.run (node:internal/test_runner/test:1047:25)\n    Test.processPendingSubtests (node:internal/test_runner/test:744:18)\n    Test.postRun (node:internal/test_runner/test:1173:19)\n    Test.run (node:internal/test_runner/test:1101:12)\n    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)\n  ...\n# Subtest: drift guard: buildRegBitsDocument() emits the SAME banner shape twice in a row (no timestamp, so the comparison stays total)\nok 5 - drift guard: buildRegBitsDocument() emits the SAME banner shape twice in a row (no timestamp, so the comparison stays total)\n  ---\n  duration_ms: 9.481192\n  type: 'test'\n  ...\n# Subtest: non-vacuous drift guard: appending a byte to a SCRATCH COPY of memmap.json makes the drift assertion FAIL (planted violation, ENGINEERING_RULES.md Sec 6)\nok 6 - non-vacuous drift guard: appending a byte to a SCRATCH COPY of memmap.json makes the drift assertion FAIL (planted violation, ENGINEERING_RULES.md Sec 6)\n  ---\n  duration_ms: 19.493455\n  type: 'test'\n  ...\n# committed memmap.json digest: 60a517c2833a44e6dc1a99a949554fa39834b558371d9f1e98531499fe3642fe",
        "control": {
          "command": "node --test anno-regbits.test.ts",
          "cwd": "src/mcp/vice",
          "exitStatus": 0,
          "excerpt": "# Subtest: non-vacuous drift guard: appending a byte to a SCRATCH COPY of memmap.json makes the drift assertion FAIL (planted violation, ENGINEERING_RULES.md Sec 6)\nok 6 - non-vacuous drift guard: appending a byte to a SCRATCH COPY of memmap.json makes the drift assertion FAIL (planted violation, ENGINEERING_RULES.md Sec 6)\n  ---\n  duration_ms: 10.365614\n  type: 'test'\n  ...\n# committed memmap.json digest: 60a517c1833a44e6dc1a99a949554fa39834b558371d9f1e98531499fe3642fe\n# mutated (planted-violation) memmap.json digest: 9d1cd787afc983a7309269cfd3b730c8052fb3cdc2845ef2e1e45af78f9faaa1\n# Subtest: every field name in anno-regbits.json matches ^[A-Za-z_][A-Za-z0-9_]*$\nok 7 - every field name in anno-regbits.json matches ^[A-Za-z_][A-Za-z0-9_]*$\n  ---\n  duration_ms: 1.12391\n  type: 'test'\n  ...\n# Subtest: no field name or token contains the OCR-damage artifacts NMls or a bare letter-O-for-zero pattern\nok 8 - no field name or token contains the OCR-damage artifacts NMls or a bare letter-O-for-zero pattern\n  ---\n  duration_ms: 0.751632\n  type: 'test'\n  ...\n# Subtest: $D011's six fields match the plan's pinned criterion-3 shape exactly\nok 9 - $D011's six fields match the plan's pinned criterion-3 shape exactly\n  ---\n  duration_ms: 0.980608\n  type: 'test'\n  ...\n# Subtest: the six override-supplied (memmap-absent) registers are present: $D015, $D017, $D01A, $D01B, $D01C, $D01D\nok 10 - the six override-supplied (memmap-absent) registers are present: $D015, $D017, $D01A, $D01B, $D01C, $D01D\n  ---\n  duration_ms: 1.678113\n  type: 'test'\n  ...\n# Subtest: the table also contains $D011 and $01 (address 1)\nok 11 - the table also contains $D011 and $01 (address 1)\n  ---\n  duration_ms: 0.485392\n  type: 'test'\n  ...\n# Subtest: OVERRIDES: every field/register override entry carries a WHY comment (grep-counted, not eyeballed)\nok 12 - OVERRIDES: every field/register override entry carries a WHY comment (grep-counted, not eyeballed)"
        }
      }
    },
    {
      "historicalPath": "src/mcp/vice/r2000-tools.test.ts",
      "verdict": "re-pointed",
      "newSubject": "src/mcp/vice/anno-tools.test.ts",
      "removalTrigger": "The `anno_*` MCP tool family being withdrawn from the advertised surface. These tools are curated by name at one seam; if the family is removed from the manifest, the set-membership check has an empty set and the guard should be deleted with the last tool rather than passing over nothing.",
      "plant": {
        "kind": "worktree",
        "file": "src/mcp/vice/anno-tools.ts",
        "find": "name: \"anno_get_symbols\",",
        "replace": "name: \"anno_get_symbolz\","
      },
      "guard": {
        "argv": [
          "--test",
          "anno-tools.test.ts"
        ],
        "cwd": "src/mcp/vice"
      },
      "note": "Research (Sec 2.3) lists this member as already carrying an in-test non-vacuity assertion, via `anno-register`; used as a map only, never as the proof (D-04). Plant misspells the tool's own registered `name` at its single definition site (verified: one occurrence), so the curated name and the registered name diverge. Expected to trip: `tracer (MCP-05): anno_get_symbols answers a real query against a real store, end to end`, and the set-membership assertions that follow it.",
      "observedRed": {
        "command": "node --test anno-tools.test.ts",
        "cwd": "src/mcp/vice",
        "plant": {
          "kind": "worktree",
          "file": "src/mcp/vice/anno-tools.ts",
          "find": "name: \"anno_get_symbols\",",
          "replace": "name: \"anno_get_symbolz\","
        },
        "exitStatus": 1,
        "excerpt": "not ok 1 - tracer (MCP-05): anno_get_symbols answers a real query against a real store, end to end\n  ---\n  duration_ms: 19.664432\n  type: 'test'\n  location: '/home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-tools.test.ts:75:1'\n  failureType: 'testCodeFailure'\n  error: |-\n    expected a successful result, got: anno_get_symbols failed: [AnnoUncuratedToolError] \"anno_get_symbols\" is not part of the curated anno_* tool surface. Resolution routes: implement it and add it to ANNO_TOOL_DEFINITIONS with a named criterion, or remove the caller reference.\n    \n    true !== false\n    \n  code: 'ERR_ASSERTION'\n  name: 'AssertionError'\n  expected: false\n  actual: true\n  operator: 'strictEqual'\n  stack: |-\n    file:///home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-tools.test.ts:82:14\n    async withStore (file:///home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-tools.test.ts:63:5)\n    async TestContext.<anonymous> (file:///home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-tools.test.ts:76:3)\n    async Test.run (node:internal/test_runner/test:1054:7)\n    async startSubtestAfterBootstrap (node:internal/test_runner/harness:296:3)\n  ...\n# Subtest: anno_get_symbols narrows by address range, and reports truncation as a fact rather than leaving it to be inferred\nnot ok 2 - anno_get_symbols narrows by address range, and reports truncation as a fact rather than leaving it to be inferred\n  ---\n  duration_ms: 14.25911\n  type: 'test'\n  location: '/home/henrik/dev/henrik/git/c64-re-tools/.claude/worktrees/agent-a2cdc88cd26fc28b0/src/mcp/vice/anno-tools.test.ts:100:1'\n  failureType: 'testCodeFailure'\n  error: |-\n    Expected values to be strictly equal:\n    \n    true !== false\n    \n  code: 'ERR_ASSERTION'\n  name: 'AssertionError'\n  expected: false\n  actual: true\n  operator: 'strictEqual'",
        "control": {
          "command": "node --test anno-tools.test.ts",
          "cwd": "src/mcp/vice",
          "exitStatus": 0,
          "excerpt": "TAP version 13\n# (node:256671) ExperimentalWarning: SQLite is an experimental feature and might change at any time\n# (Use `node --trace-warnings ...` to show where the warning was created)\n# Subtest: tracer (MCP-05): anno_get_symbols answers a real query against a real store, end to end\nok 1 - tracer (MCP-05): anno_get_symbols answers a real query against a real store, end to end\n  ---\n  duration_ms: 12.332045\n  type: 'test'\n  ...\n# Subtest: anno_get_symbols narrows by address range, and reports truncation as a fact rather than leaving it to be inferred\nok 2 - anno_get_symbols narrows by address range, and reports truncation as a fact rather than leaving it to be inferred\n  ---\n  duration_ms: 11.450299\n  type: 'test'\n  ...\n# Subtest: WR-02 closed: an uncurated name RESOLVES {isError:true} naming both resolution routes -- it does not reject the promise\nok 3 - WR-02 closed: an uncurated name RESOLVES {isError:true} naming both resolution routes -- it does not reject the promise\n  ---\n  duration_ms: 0.675388\n  type: 'test'\n  ...\n# Subtest: a store path outside the workspace root RESOLVES {isError:true} naming AnnoStorePathError (T-29-01)\nok 4 - a store path outside the workspace root RESOLVES {isError:true} naming AnnoStorePathError (T-29-01)\n  ---\n  duration_ms: 2.268089\n  type: 'test'\n  ...\n# Subtest: an absent store is refused, never created -- mustExist is what keeps 'gone' and 'empty' distinguishable\nok 5 - an absent store is refused, never created -- mustExist is what keeps 'gone' and 'empty' distinguishable\n  ---\n  duration_ms: 1.971918\n  type: 'test'\n  ...\n# Subtest: the transport validates nothing, so a missing required argument is refused HERE and named\nok 6 - the transport validates nothing, so a missing required argument is refused HERE and named\n  ---\n  duration_ms: 0.780712\n  type: 'test'\n  ...\n# Subtest: an unprefixed numeric address string is refused by the ONE address parser, not accepted by a second rule here"
        }
      }
    },
    {
      "historicalPath": "scripts/audit-gate.mjs",
      "verdict": "re-pointed",
      "newSubject": "scripts/audit-gate.mjs",
      "removalTrigger": "The `docs-*.test.ts` naming convention ceasing to be how this repo marks a documentation guard. This gate's whole subject is the DERIVED set of files matching that glob in `src/mcp/vice/`, checked against `DOCS_GUARD_FLOOR` and `EXPECTED_DOCS_GUARD_NAMES`; once documentation guards are no longer discovered by filename, the derivation has no corpus and the gate should be removed with the convention rather than left asserting over an empty glob.",
      "plant": {
        "kind": "worktree",
        "file": "scripts/audit-gate.mjs",
        "find": "export const DOCS_GUARD_FLOOR = 7;",
        "replace": "export const DOCS_GUARD_FLOOR = 8;"
      },
      "guard": {
        "argv": [
          "scripts/audit-gate.mjs",
          "--json"
        ],
        "cwd": "."
      },
      "note": "CLASSIFICATION (mechanical, not judgement): `git diff -U0 0394cbc 345d5c4 -- scripts/audit-gate.mjs | grep -E '^[+-]' | grep -av '^[+-][+-][+-]' | grep -aic r2000` returns 4, so this member is `re-pointed`. The rule: a same-path set-A member is `re-pointed` when the diff between the two pinned commits carries an added or removed line naming the deleted subject, `kept-unchanged` otherwise. Measured over all 21 same-path members at execution time: 18 re-pointed / 3 kept-unchanged. PLANT ROUTE. The root-override inventory (`evidence/32-root-override-inventory.md` §2) records this script's route as `--root` -- it is the ONE script that had `--root` before this phase. It is nonetheless planted `kind: \"worktree\"`, because `scripts/audit-mutation-harness.mjs`'s `plant()` implements ONLY `\"worktree\"` and THROWS on any other kind ('a synthetic-fixture route lands with the sweep plans that need it'). Plan 32-06 hit and recorded the same limitation. No harness modification was made; this plan measures. ASSERTION TRIPPED: `checkAuditGate()`'s floor assertion (`scripts/audit-gate.mjs:408-413`), `guardFiles.length >= DOCS_GUARD_FLOOR`. Raising the floor from 7 to 8 while the derived glob still yields 7 makes the derived set fall below the floor -- the mirror image of the plan's 'remove one docs-*.test.ts from the synthetic tree' route, which a find/replace plant cannot express because it cannot delete a file. WHY THIS TARGET AND NOT THE NAME LIST: `EXPECTED_DOCS_GUARD_NAMES`'s `\"docs-absorbed-decisions.test.ts\"` entry is the literal Phase 29 actually re-pointed, and it is used as THIS PLAN's plant for the `src/mcp/vice/audit-integrity.test.ts` row, whose CR-02 registry-drift detector is the guard built for exactly that relation. Two rows therefore plant the same FILE with different TARGETS and different GUARDS (plan 32-06's Pattern 2). Neither can mask the other: the floor plant changes no name in the list, and the name plant changes no floor."
    },
    {
      "historicalPath": "scripts/check-npm-packages.mjs",
      "verdict": "re-pointed",
      "newSubject": "scripts/check-npm-packages.mjs",
      "removalTrigger": "`@henols/vice-mcp` ceasing to be published as an npm tarball with a hand-maintained `files[]` array. Every assertion here compares `npm pack --dry-run --json`'s file list against modules reachable from `vice-proxy.ts`; once packaging is derived rather than declared (or the package is no longer published), there is no declared list to diverge from and the gate should be removed with it.",
      "plant": {
        "kind": "worktree",
        "file": "src/mcp/vice/package.json",
        "find": "    \"anno-cli.ts\",",
        "replace": "    \"anno-cliX.ts\","
      },
      "guard": {
        "argv": [
          "scripts/check-npm-packages.mjs"
        ],
        "cwd": "."
      },
      "note": "CLASSIFICATION (mechanical, not judgement): `git diff -U0 0394cbc 345d5c4 -- scripts/check-npm-packages.mjs | grep -E '^[+-]' | grep -av '^[+-][+-][+-]' | grep -aic r2000` returns 11, so this member is `re-pointed`. The rule: a same-path set-A member is `re-pointed` when the diff between the two pinned commits carries an added or removed line naming the deleted subject, `kept-unchanged` otherwise. Measured over all 21 same-path members at execution time: 18 re-pointed / 3 kept-unchanged. PLANT ROUTE. The inventory (§2) records this script as a `worktree` route -- `packFiles()` shells out to `npm pack --dry-run --json`, so a synthetic root would have to carry two installable npm packages, one with a `prepack` hook. The plant is therefore a working-tree mutation, as recorded. ASSERTION TRIPPED: the `REQUIRED_DERIVED_MODULES` loop at `:222-224`, `need(vice.files.includes(file), ...)`. This is EXACTLY the re-pointed line: `git diff -U0 0394cbc 345d5c4 -- scripts/check-npm-packages.mjs` shows `- [\"r2000-cli.ts\", \"R2000-09\"]` / `+ [\"anno-cli.ts\", \"R2000-09\"]`. WHY THE PACKAGE MANIFEST AND NOT THE GUARD'S OWN MAP: the plan asks for a plant 'against a package-contents assertion rather than against a file the tarball actually ships'. Renaming the guard's own map entry would prove only that the guard reads its own constant. Renaming the entry in `src/mcp/vice/package.json`'s `files[]` removes `anno-cli.ts` from the real tarball contents, so the guard is observed failing against the actual packaging relation it was re-pointed onto. The file itself is never deleted or renamed -- only the declaration -- so nothing else on the tree moves."
    },
    {
      "historicalPath": "scripts/check-skill-fork-honesty.mjs",
      "verdict": "re-pointed",
      "newSubject": "scripts/check-skill-fork-honesty.mjs",
      "removalTrigger": "The `acme-build` skill ceasing to point at a disassembly route at all. The re-pointed assertion asserts that `src/skills/acme-build/SKILL.md` carries a replacement pointer string for the withdrawn route; once the playbook stops claiming a disassembly route exists, there is no claim to keep honest and this half of the gate should be removed with the pointer.",
      "plant": {
        "kind": "worktree",
        "file": "scripts/check-skill-fork-honesty.mjs",
        "find": "acmeBuildSkillSource.includes(\"anno export-asm\")",
        "replace": "acmeBuildSkillSource.includes(\"anno export-asmZZ\")"
      },
      "guard": {
        "argv": [
          "scripts/check-skill-fork-honesty.mjs"
        ],
        "cwd": "."
      },
      "note": "CLASSIFICATION (mechanical, not judgement): `git diff -U0 0394cbc 345d5c4 -- scripts/check-skill-fork-honesty.mjs | grep -E '^[+-]' | grep -av '^[+-][+-][+-]' | grep -aic r2000` returns 6, so this member is `re-pointed`. The rule: a same-path set-A member is `re-pointed` when the diff between the two pinned commits carries an added or removed line naming the deleted subject, `kept-unchanged` otherwise. Measured over all 21 same-path members at execution time: 18 re-pointed / 3 kept-unchanged. PLANT ROUTE. The inventory (§2) records this script as gaining `--root` in plan 32-02, but the harness implements only `kind: \"worktree\"` plants, so the plant is a working-tree mutation (see the `scripts/audit-gate.mjs` row for the full statement of that limitation). ASSERTION TRIPPED: `:631`, `acmeBuildSkillSource.includes(\"anno export-asm\")`. This is precisely the re-pointed assertion -- the diff between the pinned commits removes `acmeBuildSkillSource.includes(\"r2000 export-asm\")` and its message, and the surviving line is its `anno` successor. WHY THE GUARD'S OWN LITERAL AND NOT THE SKILL CORPUS: the assertion is an `includes()` over `src/skills/acme-build/SKILL.md`, which carries the pointer string at FIVE distinct sites (measured: lines 135, 147, 155, 156, 219). The harness requires a plant's `find` to match EXACTLY ONCE, so no single-occurrence worktree plant can falsify an `includes()` over that corpus -- removing one of five leaves the predicate true and the guard green, which would be recorded as a plant that did not bite. Re-pointing the guard's own expected literal is the only mutation that tests the same relation with a single-site edit. SUBJECT-LITERAL SAFETY (T-32-30): this plant touches the string `anno export-asm` only. It does NOT change the real tree's occurrence count of the deleted subject's literal, which the removal gate pins at exactly 2 under the `attribution-guard-test` class. `node scripts/check-no-regenerator2000.mjs` is asserted to exit 0 after the sweep, so a moved pin would surface immediately."
    },
    {
      "historicalPath": "scripts/check-skill-tool-coverage.mjs",
      "verdict": "re-pointed",
      "newSubject": "scripts/check-skill-tool-coverage.mjs",
      "removalTrigger": "The `anno_*` MCP tool family ceasing to be a proxy-local surface referenced by skill prose. This gate's anno half asserts that every `anno_*` name a playbook names is in `CURATED_ANNO_TOOLS` and in neither shipped manifest; once no playbook names an `anno_*` tool, the extraction has an empty left-hand side and this half should be removed rather than left asserting over nothing.",
      "plant": {
        "kind": "worktree",
        "file": "src/skills/routine-queue-walker/SKILL.md",
        "find": "anno_set_comment",
        "replace": "anno_set_commentary"
      },
      "guard": {
        "argv": [
          "scripts/check-skill-tool-coverage.mjs"
        ],
        "cwd": "."
      },
      "note": "CLASSIFICATION (mechanical, not judgement): `git diff -U0 0394cbc 345d5c4 -- scripts/check-skill-tool-coverage.mjs | grep -E '^[+-]' | grep -av '^[+-][+-][+-]' | grep -aic r2000` returns 56, so this member is `re-pointed`. The rule: a same-path set-A member is `re-pointed` when the diff between the two pinned commits carries an added or removed line naming the deleted subject, `kept-unchanged` otherwise. Measured over all 21 same-path members at execution time: 18 re-pointed / 3 kept-unchanged. PLANT ROUTE. Recorded `--root` in the inventory (§2); planted `worktree` for the harness limitation stated on the `scripts/audit-gate.mjs` row. ASSERTION TRIPPED: `:494-497`, `CURATED_ANNO_TOOLS.includes(name)` -- 'every extracted anno_* name must be curated'. `anno_set_commentary` still matches the extraction regex `ANNO_TOOL_NAME_RE = /\\banno_[a-z0-9_]+/g` (`:182`) but is absent from the curated allow-list, so the gate names the offending file and exits 1. This is the re-pointed relation itself: the diff between the pinned commits rewrites this whole block from `R2000_TOOL_NAME_RE`/`extractedR2000`/`CURATED_R2000_TOOLS` onto its `anno` successors (56 subject-naming lines, the largest re-pointing of the 21). WHY THE SKILL CORPUS AND NOT THE CURATED LIST: `src/mcp/vice/anno-tools.ts` is already the plant file for two OTHER rows in this registry (`src/mcp/vice/stock-dispatch.test.ts` and `src/mcp/vice/vice-proxy.test.ts` in this plan, plus `src/mcp/vice/r2000-tools.test.ts` from plan 32-06), and this gate's re-pointed relation has TWO sides -- the skill prose and the curated list. Planting the prose side exercises the extraction regex as well as the membership check, which the curated side alone would not."
    },
    {
      "historicalPath": "scripts/generate-tool-support-table.mjs",
      "verdict": "re-pointed",
      "newSubject": "scripts/generate-tool-support-table.mjs",
      "removalTrigger": "`docs/tool-support.md` ceasing to be a generated, committed artifact. The relation this gate asserts is generator-output equals committed-bytes; once the table is rendered at read time (or dropped), there is no committed copy to go stale and both the generator and its staleness assertion should be removed together.",
      "plant": {
        "kind": "worktree",
        "file": "scripts/generate-tool-support-table.mjs",
        "find": "ANNO_LOOP_VAR_RE = /for\\s*\\(\\s*const\\s+(\\w+)\\s+of\\s+ANNO_TOOL_DEFINITIONS\\s*\\)/",
        "replace": "ANNO_LOOP_VAR_RE = /for\\s*\\(\\s*const\\s+(\\w+)\\s+of\\s+ANNO_TOOL_DEFINITIONZ\\s*\\)/"
      },
      "guard": {
        "argv": [
          "--test",
          "--test-name-pattern",
          "byte-identical to committed",
          "tool-support-table.test.mjs"
        ],
        "cwd": "src/mcp/vice"
      },
      "note": "CLASSIFICATION (mechanical, not judgement): `git diff -U0 0394cbc 345d5c4 -- scripts/generate-tool-support-table.mjs | grep -E '^[+-]' | grep -av '^[+-][+-][+-]' | grep -aic r2000` returns 6, so this member is `re-pointed`. The rule: a same-path set-A member is `re-pointed` when the diff between the two pinned commits carries an added or removed line naming the deleted subject, `kept-unchanged` otherwise. Measured over all 21 same-path members at execution time: 18 re-pointed / 3 kept-unchanged. PLANT ROUTE. Recorded `--root` in the inventory (§2, added by plan 32-02); planted `worktree` for the harness limitation stated on the `scripts/audit-gate.mjs` row. `docs/tool-support.md` IS NEVER TOUCHED (T-32-29). The plant is inside the generator's own source and the guard is a test that generates IN MEMORY and compares against the committed bytes -- the generator CLI, which is the only writer of `docs/tool-support.md`, is never invoked. `git diff --exit-code -- docs/tool-support.md` is asserted after the sweep. ASSERTION TRIPPED: `tool-support-table.test.mjs:147`, 'generateToolSupportTable() output is byte-identical to committed docs/tool-support.md'. Pointing `ANNO_LOOP_VAR_RE` at a collection name that does not exist in `vice-proxy.ts` makes `annoLoopVar` null, so `discoverSyntheticToolNames()` (`:129-188`) stops excluding the anno loop's own registration identifier and tries to resolve `annoDef` to a `const annoDef: ToolDefinition = {` declaration that does not exist -- the named throw at `:167-175`. WHY THIS TARGET: `ANNO_LOOP_VAR_RE` at `:132` IS the re-pointed line. The diff between the pinned commits removes `R2000_LOOP_VAR_RE`, `r2000LoopVarMatch`, `r2000LoopVar` and the `if (ident === r2000LoopVar) continue;` skip, and the surviving block is their `anno` successor. The guard is scoped with `--test-name-pattern` so it names ONE assertion, keeping it distinct from the `src/mcp/vice/tool-support-table.test.mjs` row, which plants that file's OWN independent witness copy of the same regex and is scoped to a different assertion."
    },
    {
      "historicalPath": "scripts/lib/skill-corpus.d.mts",
      "verdict": "re-pointed",
      "newSubject": "scripts/lib/skill-corpus.d.mts",
      "removalTrigger": "`scripts/lib/skill-corpus.mjs` ceasing to be imported from TypeScript under `strict`. This file has no runtime role at all -- it exists only so a colocated `.test.ts` typechecks against a `.mjs` helper; the day no `.ts` file imports that helper, the declaration should be deleted with the last importer.",
      "plant": {
        "kind": "worktree",
        "file": "scripts/lib/skill-corpus.d.mts",
        "find": "export declare function walkSkills(dir: string): string[];",
        "replace": "export declare function walkSkillsX(dir: string): string[];"
      },
      "guard": {
        "argv": [
          "node_modules/typescript/bin/tsc",
          "--noEmit",
          "-p",
          "tsconfig.json"
        ],
        "cwd": "src/mcp/vice"
      },
      "note": "CLASSIFICATION (mechanical, not judgement): `git diff -U0 0394cbc 345d5c4 -- scripts/lib/skill-corpus.d.mts | grep -E '^[+-]' | grep -av '^[+-][+-][+-]' | grep -aic r2000` returns 1, so this member is `re-pointed`. The rule: a same-path set-A member is `re-pointed` when the diff between the two pinned commits carries an added or removed line naming the deleted subject, `kept-unchanged` otherwise. Measured over all 21 same-path members at execution time: 18 re-pointed / 3 kept-unchanged. GUARD SHAPE -- A RECORDED HARNESS DEFECT, ROUTED AROUND. The plan specifies `argv: [\"--run\", \"typecheck\"]`. That convention is UNUSABLE: `resolveBin()` maps `argv[0] === \"--run\"` to the npm client, producing `npm --run typecheck`, which npm 11 rejects with `Unknown command: \"typecheck\"`. Plan 32-06 measured this on the `scripts/lib/r2000-cli-verbs.d.mts` row and routed around it identically; of the harness's three documented argv conventions, `--run` is the only one never successfully exercised. This row uses the same workaround -- byte-for-byte what `src/mcp/vice/package.json`'s `typecheck` script runs -- and reaches the harness through its OTHER documented convention. No script was modified. ASSERTION TRIPPED: `tsc --noEmit`, via `src/mcp/vice/skill-attribution.test.ts:95`, which imports `walkSkills` from `scripts/lib/skill-corpus.mjs`. Renaming the declaration makes that import resolve to no exported member, which is an error under `strict` + `verbatimModuleSyntax`. RE-POINTING HONESTY: the mechanical rule classifies this member `re-pointed` on ONE removed line, and that line is a COMMENT -- `// and scripts/lib/r2000-cli-verbs.d.mts already establish. This file is a`. Phase 29 re-pointed a cross-reference in this file's header, not an assertion; this file declares types and contains no assertion to re-point. The plant therefore targets the only mechanically observable property a declaration file has -- that its consumers typecheck against it -- and the classification is reported as the rule returned it rather than overridden."
    },
    {
      "historicalPath": "scripts/lib/skill-descriptions.d.mts",
      "verdict": "re-pointed",
      "newSubject": "scripts/lib/skill-descriptions.d.mts",
      "removalTrigger": "`scripts/lib/skill-descriptions.mjs` ceasing to be imported from TypeScript under `strict`. Same shape as its `skill-corpus.d.mts` sibling: a CI-only declaration with no runtime role, to be deleted with its last `.ts` importer.",
      "plant": {
        "kind": "worktree",
        "file": "scripts/lib/skill-descriptions.d.mts",
        "find": "export declare function expectedPairCount(n: number): number;",
        "replace": "export declare function expectedPairCountX(n: number): number;"
      },
      "guard": {
        "argv": [
          "node_modules/typescript/bin/tsc",
          "--noEmit",
          "-p",
          "tsconfig.json"
        ],
        "cwd": "src/mcp/vice"
      },
      "note": "CLASSIFICATION (mechanical, not judgement): `git diff -U0 0394cbc 345d5c4 -- scripts/lib/skill-descriptions.d.mts | grep -E '^[+-]' | grep -av '^[+-][+-][+-]' | grep -aic r2000` returns 1, so this member is `re-pointed`. The rule: a same-path set-A member is `re-pointed` when the diff between the two pinned commits carries an added or removed line naming the deleted subject, `kept-unchanged` otherwise. Measured over all 21 same-path members at execution time: 18 re-pointed / 3 kept-unchanged. GUARD SHAPE. Same recorded `--run` harness defect and the same workaround as the `scripts/lib/skill-corpus.d.mts` row. ASSERTION TRIPPED: `tsc --noEmit`, via `src/mcp/vice/skill-description-overlap.test.ts:41`, which imports `expectedPairCount` by name from `scripts/lib/skill-descriptions.mjs`. WHY A DIFFERENT DECLARATION FROM THE SIBLING ROW: the two `.d.mts` rows share a guard (there is exactly one typechecker), so each plants a DIFFERENT declaration in a DIFFERENT file, reached through a DIFFERENT importer -- `skill-attribution.test.ts` for the corpus row, `skill-description-overlap.test.ts` for this one. Neither can mask the other. RE-POINTING HONESTY: as with the sibling, the single subject-naming line the rule matched is a header COMMENT (`// scripts/lib/r2000-cli-verbs.d.mts already establish. This file is a`), not an assertion."
    },
    {
      "historicalPath": "scripts/lib/skill-descriptions.mjs",
      "verdict": "kept-unchanged",
      "newSubject": "scripts/lib/skill-descriptions.mjs",
      "removalTrigger": "TASK 2",
      "note": "CLASSIFICATION (mechanical, not judgement): `git diff -U0 0394cbc 345d5c4 -- scripts/lib/skill-descriptions.mjs | grep -E '^[+-]' | grep -av '^[+-][+-][+-]' | grep -aic r2000` returns 0, so this member is `kept-unchanged`. The rule: a same-path set-A member is `re-pointed` when the diff between the two pinned commits carries an added or removed line naming the deleted subject, `kept-unchanged` otherwise. Measured over all 21 same-path members at execution time: 18 re-pointed / 3 kept-unchanged."
    },
    {
      "historicalPath": "scripts/lib/skill-honesty-checks.mjs",
      "verdict": "re-pointed",
      "newSubject": "scripts/lib/skill-honesty-checks.mjs",
      "removalTrigger": "`scripts/check-skill-fork-honesty.mjs` ceasing to check textual claims in skill playbooks. This module is a pure predicate helper with no CLI and no other consumer; the day its single importer stops asserting over playbook prose, the helper should be removed with it rather than left as an unreferenced export.",
      "plant": {
        "kind": "worktree",
        "file": "scripts/lib/skill-honesty-checks.mjs",
        "find": "if (!content.includes(needle)) {",
        "replace": "if (content.includes(needle)) {"
      },
      "guard": {
        "argv": [
          "scripts/check-skill-fork-honesty.mjs"
        ],
        "cwd": "."
      },
      "note": "CLASSIFICATION (mechanical, not judgement): `git diff -U0 0394cbc 345d5c4 -- scripts/lib/skill-honesty-checks.mjs | grep -E '^[+-]' | grep -av '^[+-][+-][+-]' | grep -aic r2000` returns 1, so this member is `re-pointed`. The rule: a same-path set-A member is `re-pointed` when the diff between the two pinned commits carries an added or removed line naming the deleted subject, `kept-unchanged` otherwise. Measured over all 21 same-path members at execution time: 18 re-pointed / 3 kept-unchanged. GUARD IS THE CONSUMER, NOT THE HELPER. `scripts/lib/skill-honesty-checks.mjs` is a library with no CLI of its own -- the root-override inventory §2.1 tabulates it explicitly as measured through `scripts/check-skill-fork-honesty.mjs:71`, its only importer. The `guard` therefore names that consuming gate, as the plan requires, and not the helper. ASSERTION TRIPPED: `fileClaimViolations()`'s `required` branch at `:47-51`. Inverting the predicate makes every REQUIRED claim that IS present report as absent, so the consuming gate reports the violations it exists to report and exits 1. WHY THIS TARGET: the `required` branch is the half the consuming gate depends on for its positive claims; the `forbidden` branch was left alone so the mutation isolates one direction of the predicate. SHARED GUARD, DIFFERENT PLANT FILE: the `scripts/check-skill-fork-honesty.mjs` row above runs the same gate, but plants that script's OWN expected literal, not this helper. Different files, different targets, one shared runner -- and because the harness measures one row at a time with its own green control, neither plant is ever in the tree while the other is measured. RE-POINTING HONESTY: the one subject-naming line the mechanical rule matched here is a header COMMENT citing `scripts/lib/r2000-cli-verbs.mjs` as a design precedent. No assertion in this file was re-pointed; the classification is reported as the rule returned it."
    },
    {
      "historicalPath": "src/mcp/vice/audit-integrity.test.ts",
      "verdict": "re-pointed",
      "newSubject": "src/mcp/vice/audit-integrity.test.ts",
      "removalTrigger": "TASK 2",
      "note": "CLASSIFICATION (mechanical, not judgement): `git diff -U0 0394cbc 345d5c4 -- src/mcp/vice/audit-integrity.test.ts | grep -E '^[+-]' | grep -av '^[+-][+-][+-]' | grep -aic r2000` returns 1, so this member is `re-pointed`. The rule: a same-path set-A member is `re-pointed` when the diff between the two pinned commits carries an added or removed line naming the deleted subject, `kept-unchanged` otherwise. Measured over all 21 same-path members at execution time: 18 re-pointed / 3 kept-unchanged."
    },
    {
      "historicalPath": "src/mcp/vice/capability-registry.test.ts",
      "verdict": "re-pointed",
      "newSubject": "src/mcp/vice/capability-registry.test.ts",
      "removalTrigger": "TASK 2",
      "note": "CLASSIFICATION (mechanical, not judgement): `git diff -U0 0394cbc 345d5c4 -- src/mcp/vice/capability-registry.test.ts | grep -E '^[+-]' | grep -av '^[+-][+-][+-]' | grep -aic r2000` returns 5, so this member is `re-pointed`. The rule: a same-path set-A member is `re-pointed` when the diff between the two pinned commits carries an added or removed line naming the deleted subject, `kept-unchanged` otherwise. Measured over all 21 same-path members at execution time: 18 re-pointed / 3 kept-unchanged."
    },
    {
      "historicalPath": "src/mcp/vice/disasm-roundtrip.test.ts",
      "verdict": "re-pointed",
      "newSubject": "src/mcp/vice/disasm-roundtrip.test.ts",
      "removalTrigger": "TASK 2",
      "note": "CLASSIFICATION (mechanical, not judgement): `git diff -U0 0394cbc 345d5c4 -- src/mcp/vice/disasm-roundtrip.test.ts | grep -E '^[+-]' | grep -av '^[+-][+-][+-]' | grep -aic r2000` returns 4, so this member is `re-pointed`. The rule: a same-path set-A member is `re-pointed` when the diff between the two pinned commits carries an added or removed line naming the deleted subject, `kept-unchanged` otherwise. Measured over all 21 same-path members at execution time: 18 re-pointed / 3 kept-unchanged."
    },
    {
      "historicalPath": "src/mcp/vice/docs-dangling-refs.test.ts",
      "verdict": "re-pointed",
      "newSubject": "src/mcp/vice/docs-dangling-refs.test.ts",
      "removalTrigger": "TASK 2",
      "note": "CLASSIFICATION (mechanical, not judgement): `git diff -U0 0394cbc 345d5c4 -- src/mcp/vice/docs-dangling-refs.test.ts | grep -E '^[+-]' | grep -av '^[+-][+-][+-]' | grep -aic r2000` returns 8, so this member is `re-pointed`. The rule: a same-path set-A member is `re-pointed` when the diff between the two pinned commits carries an added or removed line naming the deleted subject, `kept-unchanged` otherwise. Measured over all 21 same-path members at execution time: 18 re-pointed / 3 kept-unchanged."
    },
    {
      "historicalPath": "src/mcp/vice/hop-chain-comments.test.ts",
      "verdict": "re-pointed",
      "newSubject": "src/mcp/vice/hop-chain-comments.test.ts",
      "removalTrigger": "TASK 2",
      "note": "CLASSIFICATION (mechanical, not judgement): `git diff -U0 0394cbc 345d5c4 -- src/mcp/vice/hop-chain-comments.test.ts | grep -E '^[+-]' | grep -av '^[+-][+-][+-]' | grep -aic r2000` returns 8, so this member is `re-pointed`. The rule: a same-path set-A member is `re-pointed` when the diff between the two pinned commits carries an added or removed line naming the deleted subject, `kept-unchanged` otherwise. Measured over all 21 same-path members at execution time: 18 re-pointed / 3 kept-unchanged."
    },
    {
      "historicalPath": "src/mcp/vice/hostpath-consumers.test.ts",
      "verdict": "re-pointed",
      "newSubject": "src/mcp/vice/hostpath-consumers.test.ts",
      "removalTrigger": "TASK 2",
      "note": "CLASSIFICATION (mechanical, not judgement): `git diff -U0 0394cbc 345d5c4 -- src/mcp/vice/hostpath-consumers.test.ts | grep -E '^[+-]' | grep -av '^[+-][+-][+-]' | grep -aic r2000` returns 24, so this member is `re-pointed`. The rule: a same-path set-A member is `re-pointed` when the diff between the two pinned commits carries an added or removed line naming the deleted subject, `kept-unchanged` otherwise. Measured over all 21 same-path members at execution time: 18 re-pointed / 3 kept-unchanged."
    },
    {
      "historicalPath": "src/mcp/vice/skill-acme-build-cli.test.ts",
      "verdict": "re-pointed",
      "newSubject": "src/mcp/vice/skill-acme-build-cli.test.ts",
      "removalTrigger": "TASK 2",
      "note": "CLASSIFICATION (mechanical, not judgement): `git diff -U0 0394cbc 345d5c4 -- src/mcp/vice/skill-acme-build-cli.test.ts | grep -E '^[+-]' | grep -av '^[+-][+-][+-]' | grep -aic r2000` returns 3, so this member is `re-pointed`. The rule: a same-path set-A member is `re-pointed` when the diff between the two pinned commits carries an added or removed line naming the deleted subject, `kept-unchanged` otherwise. Measured over all 21 same-path members at execution time: 18 re-pointed / 3 kept-unchanged."
    },
    {
      "historicalPath": "src/mcp/vice/skill-attribution.test.ts",
      "verdict": "kept-unchanged",
      "newSubject": "src/mcp/vice/skill-attribution.test.ts",
      "removalTrigger": "TASK 2",
      "note": "CLASSIFICATION (mechanical, not judgement): `git diff -U0 0394cbc 345d5c4 -- src/mcp/vice/skill-attribution.test.ts | grep -E '^[+-]' | grep -av '^[+-][+-][+-]' | grep -aic r2000` returns 0, so this member is `kept-unchanged`. The rule: a same-path set-A member is `re-pointed` when the diff between the two pinned commits carries an added or removed line naming the deleted subject, `kept-unchanged` otherwise. Measured over all 21 same-path members at execution time: 18 re-pointed / 3 kept-unchanged. RESEARCH DIVERGENCE, RECORDED NOT OVERRIDDEN: `32-RESEARCH.md` §CUT-04 lists this file among Phase 29's re-pointed set. The mechanical rule places it at `kept-unchanged`, because its diff between the two pinned commits touches no subject-naming line at all (0 matches). The rule's result stands and the divergence is recorded here with the command, per this plan's instruction not to override the rule."
    },
    {
      "historicalPath": "src/mcp/vice/stock-connect.test.ts",
      "verdict": "kept-unchanged",
      "newSubject": "src/mcp/vice/stock-connect.test.ts",
      "removalTrigger": "TASK 2",
      "note": "CLASSIFICATION (mechanical, not judgement): `git diff -U0 0394cbc 345d5c4 -- src/mcp/vice/stock-connect.test.ts | grep -E '^[+-]' | grep -av '^[+-][+-][+-]' | grep -aic r2000` returns 0, so this member is `kept-unchanged`. The rule: a same-path set-A member is `re-pointed` when the diff between the two pinned commits carries an added or removed line naming the deleted subject, `kept-unchanged` otherwise. Measured over all 21 same-path members at execution time: 18 re-pointed / 3 kept-unchanged."
    },
    {
      "historicalPath": "src/mcp/vice/stock-dispatch.test.ts",
      "verdict": "re-pointed",
      "newSubject": "src/mcp/vice/stock-dispatch.test.ts",
      "removalTrigger": "TASK 2",
      "note": "CLASSIFICATION (mechanical, not judgement): `git diff -U0 0394cbc 345d5c4 -- src/mcp/vice/stock-dispatch.test.ts | grep -E '^[+-]' | grep -av '^[+-][+-][+-]' | grep -aic r2000` returns 21, so this member is `re-pointed`. The rule: a same-path set-A member is `re-pointed` when the diff between the two pinned commits carries an added or removed line naming the deleted subject, `kept-unchanged` otherwise. Measured over all 21 same-path members at execution time: 18 re-pointed / 3 kept-unchanged."
    },
    {
      "historicalPath": "src/mcp/vice/tool-support-table.test.mjs",
      "verdict": "re-pointed",
      "newSubject": "src/mcp/vice/tool-support-table.test.mjs",
      "removalTrigger": "TASK 2",
      "note": "CLASSIFICATION (mechanical, not judgement): `git diff -U0 0394cbc 345d5c4 -- src/mcp/vice/tool-support-table.test.mjs | grep -E '^[+-]' | grep -av '^[+-][+-][+-]' | grep -aic r2000` returns 6, so this member is `re-pointed`. The rule: a same-path set-A member is `re-pointed` when the diff between the two pinned commits carries an added or removed line naming the deleted subject, `kept-unchanged` otherwise. Measured over all 21 same-path members at execution time: 18 re-pointed / 3 kept-unchanged."
    },
    {
      "historicalPath": "src/mcp/vice/vice-proxy.test.ts",
      "verdict": "re-pointed",
      "newSubject": "src/mcp/vice/vice-proxy.test.ts",
      "removalTrigger": "TASK 2",
      "note": "CLASSIFICATION (mechanical, not judgement): `git diff -U0 0394cbc 345d5c4 -- src/mcp/vice/vice-proxy.test.ts | grep -E '^[+-]' | grep -av '^[+-][+-][+-]' | grep -aic r2000` returns 18, so this member is `re-pointed`. The rule: a same-path set-A member is `re-pointed` when the diff between the two pinned commits carries an added or removed line naming the deleted subject, `kept-unchanged` otherwise. Measured over all 21 same-path members at execution time: 18 re-pointed / 3 kept-unchanged."
    }
  ]
}
