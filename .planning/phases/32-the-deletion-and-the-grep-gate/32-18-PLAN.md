---
phase: 32-the-deletion-and-the-grep-gate
plan: 18
type: execute
wave: 3
gap_closure: true
depends_on: ["32-16", "32-17"]
files_modified:
  - src/mcp/vice/audit-root-args.test.ts
  - .planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-gap5-population-and-precondition.md
# TRANSIENT, NOT OWNED — declared here rather than in files_modified, deliberately.
# Task 2's two non-vacuity experiments temporarily modify `scripts/audit-mutation-harness.mjs`
# (owned by plan 32-19, same wave) and `scripts/audit-gate.mjs` (owned by plan 32-16, wave 2).
# Both are restored inside the task, each bounded by an empty `git diff` AND a content-hash
# match, and Task 2 ends with `git status --porcelain` listing exactly one path. They are NOT
# listed above because listing them would declare an ownership this plan does not take and
# would force a spurious wave bump on a file this plan leaves byte-identical. See flagged
# assumption 4 for the cross-plan edge with 32-19 and why it is not a race.
transient_touches:
  - scripts/audit-mutation-harness.mjs   # owned by 32-19 (same wave) — reverted, hash-checked
  - scripts/audit-gate.mjs               # owned by 32-16 (wave 2)   — reverted, hash-checked
autonomous: true
requirements: [CUT-04]
net_deletion: false   # no file removed. One predicate function and one docblock are rewritten
                      # IN PLACE and two matrix rows are added. cleanup-wave's predicate is
                      # `git diff --diff-filter=D --name-only` — DELETED FILES only; none here.

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The completeness guard derives its population from the FLAG, not from the remedy. At HEAD `src/mcp/vice/audit-root-args.test.ts:423` uses `src.includes(\"parseRootArg(\")`, which makes the population 'scripts that already use the seam' — tautologically the set the matrix covers. Measured at HEAD: `grep -l -- '--root' scripts/*.mjs` returns 8 and `grep -l 'parseRootArg(' scripts/*.mjs` returns 6, and the two invisible ones are exactly the two that were behind. After this plan a `scripts/*.mjs` whose source contains the token `--root` is a member whether or not it uses the seam. — TRACE: verifier truth 15 / gaps[1].missing[0] / CR-08"
    - "The flipped predicate is proven to BITE before it is satisfied: with the population derived from the flag and the matrix still carrying six rows, the guard goes red naming `audit-gate` and `audit-mutation-harness`. That red is captured verbatim as this plan's observed-red evidence, and it is the cheapest possible regression test for Gap 2 — it is what would have caught Gap 2 and did not. NO EXCLUSION LIST is added to keep it green; the verifier forbids that in `gaps[1].missing[1]` in those words. — TRACE: gaps[1].missing[1] / verifier Gaps Summary, Gap 5 / this phase's own planted-violation convention"
    - "The guard is then taken green by ADDING the two missing rows, so the matrix covers all EIGHT root-accepting scripts and every one of them has at least one spawned test. `gaps[1].missing[2]` stated verbatim in plan 32-11's must-have 6: 'At least one spawnSync test per --root-ised script'. That is now true of the population defined by the flag rather than of the population defined by the remedy. — TRACE: verifier truth 15 / gaps[1].missing[2] / plan 32-11 truth 6"
    - "The two docblock sentences at `:294-296` are corrected to state what the guard actually measures. They currently assert 'Six scripts accept a root', 'The table below is the whole population', and that 'a seventh root-accepting script added later fails this file by omission rather than passing unnoticed'. All three were false as measured, and the third was false in the present tense rather than hypothetically — the seventh and eighth already existed and already passed unnoticed. — TRACE: gaps[1].missing[2] / verifier truth 15 evidence"
    - "The `CR-05` precondition is DECLARED MECHANICALLY, not only in prose: the harness's matrix row is a standing spawned test that reds the moment the harness is taken off the strict parser, so the soundness of the 35 recorded observed reds no longer rests on the flag going unused. The verifier's `harden:` asks for the precondition to be declared rather than for the habit to be documented; this is the declaration. — TRACE: `coincidental_reliance_items[0].harden` / CR-05 / verifier truth 2's coincidental-reliance note"
    - "The containment asymmetry that plan 32-16 recorded as a decision is now a guarded invariant: the matrix carries `audit-gate` with a typed expectation naming it uncontained by design, alongside a two-part write-freedom assertion: a CLOSED allow-list over the DIRECT fs import surface — closed over THAT surface and no wider (measured at `audit-gate.mjs:90`: exactly `readdirSync` and `readFileSync`, nothing from `node:fs/promises`, no namespace import, no dynamic fs import), with its residual hole named rather than implied: `audit-gate.mjs:93` binds `spawnSync` from `node:child_process`, so a write by a spawned process is outside both halves and neither half claims to cover it, and a count of an ENUMERATED filesystem-write API set — `writeFileSync`, `appendFileSync`, `createWriteStream`, `mkdirSync`, `rmSync`, `rmdirSync`, `unlinkSync`, `renameSync`, `cpSync`, `copyFileSync`, `truncateSync`, `writeSync` and the `node:fs/promises` forms — taken over COMMENT-STRIPPED source and deliberately not matching the file's five `process.stderr.write(` calls. Comments are stripped because this file discusses writes in prose and plan 32-16 adds a header note whose reversal condition is a write call appearing in it, so an unfiltered count would red on prose; the set is enumerated because a `writeFileSync`-only count misses five of the listed APIs. This is the SOLE mechanical revocation of `T-32-22`, so the moment `audit-gate.mjs` gains a write, the acceptance recorded in plan 32-16 is revoked mechanically rather than by memory. — TRACE: plan 32-16's reversibility note and `T-32-22` / gaps[0].missing[1]"
    - "The population read is byte-safe and its zero results are provably real: all EIGHT scripts are asserted NUL-free, not six, and the population walk's visited `.mjs` list is cross-checked against a DIFFERENT MECHANISM — a `spawnSync` shell glob expansion, in another process, with no `isFile()` filter and no Node-side read — compared as sorted basename lists reported in both directions, so a file skipped for any reason is NAMED rather than passing unnoticed. Two `readdirSync` calls sharing the walk's filter would be an assertion that cannot disagree, which is this gap's own defect; the mechanisms are deliberately different and the assertion says so. Its non-vacuity is proven by mutating the SUBJECT — a directory named `zz-population-probe.mjs`, which the shell glob sees and `isFile()` drops — never by mutating the assertion. `anno-memmap-render.ts` in this repository carries a NUL byte and has already caused one false decision from a plain census. — TRACE: `$COVERAGE` CUT-06/empty applied to the guard's own census / the existing NUL assertion at `audit-root-args.test.ts:729`"
    - "[edge:CUT-06/adjacency] Two scripts that are exactly equal in one property and different in another do not merge in the matrix: `audit-gate` and `audit-mutation-harness` both accept `--root` and both are on the argv seam, but one is contained and one is not, and the typed expectation separates them so a single generic case cannot pass for both by accident. The out-of-repository case is asserted in OPPOSITE directions for the two. — TRACE: `$COVERAGE` CUT-06/adjacency, resolved explicit"
    - "[edge:CUT-04/ordering] The population is compared against the matrix as two SORTED lists, so the assertion's verdict does not depend on directory-listing order, and a member added to the matrix in any position is equivalent. Both sides are sorted by the same comparison. — TRACE: `$COVERAGE` CUT-04/ordering applied to the completeness assertion"
  prohibitions:
    - statement: "MUST NOT make this guard green by narrowing what it measures. Explicitly forbidden, in the verifier's own words: an exclusion list, an allow-list, an `UNMIGRATED` array, a skip, a `contained` value that means 'not checked', or any predicate that re-derives the population from anything other than the flag. The reviewer's suggested fix in `32-REVIEW.md` DOES propose an `UNMIGRATED` list; the verifier overrides it because both scripts are migrated in this round. If a future script cannot be migrated, the answer is a typed expectation with a stated reason and its own spawned test, never an absence."
    - statement: "MUST NOT record a measurement this plan did not take. The red state must be an actual captured test run with the two script names in its output, taken between the predicate flip and the matrix rows landing — not a description of what would happen. A plan that goes straight to green has not proven the guard bites, which is the entire finding."
    - statement: "MUST NOT let a guard assert in its own text something it does not measure. The docblock and the assertion move in one edit. A corrected predicate under a stale docblock is the same defect this gap is, one round later."
  artifacts:
    - path: "src/mcp/vice/audit-root-args.test.ts"
      provides: "A population derived from the --root flag, a matrix covering all eight root-accepting scripts with typed expectations and per-row extra arguments, a corrected docblock, and the write-freedom assertion that guards the containment asymmetry"
      contains: "audit-mutation-harness"
    - path: ".planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-gap5-population-and-precondition.md"
      provides: "The captured red naming both scripts, the captured green after the rows land, the two population counts before and after, and the CR-05 precondition declaration with its mechanical guard named"
      contains: "parseRootArg"
  key_links:
    - from: "src/mcp/vice/audit-root-args.test.ts"
      to: "scripts/audit-mutation-harness.mjs"
      via: "the matrix row spawns it and reds if it is ever taken off the strict parser — the standing declaration of the CR-05 precondition"
      pattern: "audit-mutation-harness"
    - from: "src/mcp/vice/audit-root-args.test.ts"
      to: "scripts/audit-gate.mjs"
      via: "the typed expectation records it as uncontained by design and the write-freedom assertion revokes that acceptance if it ever writes"
      pattern: "audit-gate"
---

<!-- planner-discipline-allow: parseRootArg -->
<!-- planner-discipline-allow: UNMIGRATED -->
<!-- planner-discipline-allow: writeFileSync -->

<objective>
GAP CLOSURE, round 2. **Gap 5 (`CR-08`) — the completeness guard whose population is its own
remedy, plus the mechanical half of the `CR-05` precondition declaration.**

**What the verifier measured.** Round 1 added this guard as the mechanism that would stop a
future root-accepting script from shipping untested; plan 32-11's must-have 6 quotes
`gaps[1].missing[2]` verbatim — "At least one spawnSync test per `--root`-ised script" — and
the completeness guard is what makes that a standing property instead of a snapshot. But
`audit-root-args.test.ts:423` derives the population with `src.includes("parseRootArg(")`,
which is the REMEDY. So the set it compares against the matrix is "scripts that already use
the seam", which is tautologically the set the matrix covers. Measured at HEAD: eight scripts
accept `--root`, six contain `parseRootArg(`, and the two invisible ones are exactly the two
that were behind.

The file's own docblock asserts the opposite, twice, and the second time in the present
tense: "The table below is the whole population", and "a seventh root-accepting script added
later fails this file by omission rather than passing unnoticed". The seventh and eighth
already existed and already passed unnoticed. This is a guard added by a phase whose success
criterion is "none passes vacuously", and it passed vacuously with respect to its own stated
purpose.

**Why the red is the point, not an inconvenience.** The verifier is explicit: deriving the
population from the flag "reds the guard immediately with `audit-gate` and
`audit-mutation-harness` named — which is the correct outcome, and the same one Gap 2 above
asks for. Do not add an exclusion list to keep it green." This plan therefore flips the
predicate FIRST, captures the red naming both scripts as evidence, and only then adds the two
rows. That ordering is this repository's planted-violation convention applied to the guard
itself: a guard that cannot be made to fail has not been re-pointed.

Note the one place this plan deliberately departs from `32-REVIEW.md`. `CR-08`'s suggested
fix proposes an `UNMIGRATED` array beside the matrix so non-migration is "an explicit, named
exception rather than an invisible one". The verifier overrides that, and this plan follows
the verifier: both scripts are migrated in this round by plans 32-16 and 32-17, so the list
would be empty, and an empty exclusion list is an invitation to fill it.

**The second thing this plan lands.** Plan 32-17 declares the `CR-05` precondition in prose
and makes it true in code. A prose declaration decays. The harness's matrix row is the
standing mechanical version: a spawned test that reds the moment the harness is taken off the
strict parser, so the soundness of the 35 recorded observed reds stops depending on nobody
using the flag. And plan 32-16's containment decision for `audit-gate` — accepted on the
measured basis that the script never writes — gets its named reversal trigger wired: an
assertion that the file contains zero write calls.

Implements **D-06** (the `--root` pattern is the only testability seam), **D-07** (the
ordering boundary for `--root` work) and **D-15** (the evidence artifact carries raw commands,
raw output, broker state and the commit measured).

Purpose: the mechanism that was supposed to prevent this gap must be able to see the thing it
was built to see.

Output: a population defined by the flag; a matrix covering all eight; a corrected docblock; a
captured red and a captured green; and the precondition declared where a future change will
trip over it.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-CONTEXT.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-VERIFICATION.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-11-SUMMARY.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-12-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The population comes from the flag, the docblock says so, and the guard is captured RED naming both scripts</name>
  <files>src/mcp/vice/audit-root-args.test.ts</files>
  <read_first>
    - `src/mcp/vice/audit-root-args.test.ts` — the WHOLE file, 866 lines. In particular: the matrix docblock at `:285-296` with all three claims this task corrects; `ContainedExpectation` and its three documented values at `:298-320`; `MATRIX` and `runScript()` at `:325-340`; the split-read contract section and its two text predicates at `:342-402`; `declaredSplitReadImports()` at `:406-411`; `scriptsUsingTheSharedParser()` at `:413-426` — the predicate being replaced; the completeness assertion at `:428-442` including its `covered.length >= 6` floor; the three per-script case loops at `:444-530`; `scriptText()` at `:370-378` with its six-script NUL claim; and the NUL assertion at `:729`.
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-VERIFICATION.md` — `gaps[1]` in full: its `truth`, its `reason` with both measured counts, its `artifacts` row and all three `missing` lines. The instruction not to add an exclusion list is `missing[1]`; read it there.
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-REVIEW.md:351-430` — `CR-08` in full, including its suggested `UNMIGRATED` fix, so the departure from it is taken knowingly rather than by not having read it.
    - `scripts/audit-gate.mjs` and `scripts/audit-mutation-harness.mjs` as plans 32-16 and 32-17 left them — their flag surfaces, their rejection prefixes and, for the harness, the ordering of containment relative to row selection. The matrix rows in Task 2 are written against those, not against this plan's description of them.
  </read_first>
  <action>
**A. Replace the population predicate.** Rename `scriptsUsingTheSharedParser()` to a name that
says what it now measures — a script that ACCEPTS a root — and change its body to test for the
`--root` token in the source rather than for a call to the seam. Keep the rest of the walk as
it is: top-level `scripts/*.mjs` only, `entry.isFile()` so `scripts/lib/` is excluded because
that is where the seam itself lives, and the basename with its extension stripped as the
member name. Read each file with the same `latin1` byte-preserving read `scriptText()` already
uses, so a NUL byte cannot silently truncate the read.

Write the reason into the function's docblock in one sentence a future reader can act on:
deriving from the flag rather than from the remedy is load-bearing, because a script that
hand-rolls the reader is the defect this file exists to catch, and keying on the remedy
excludes exactly those.

**B. Cross-check the population walk against a DIFFERENT MECHANISM, named here because the
choice of mechanism is the entire content of this assertion.** The walk reads
`readdirSync(dir, { withFileTypes: true })` and keeps entries passing
`entry.isFile() && entry.name.endsWith(".mjs")`. Comparing that against a second `readdirSync`
carrying the same filter would be an assertion that CANNOT DISAGREE — a tautological guard
added to the very file whose tautological population is this gap. The second side must
therefore not be a Node directory read at all.

Use a `spawnSync` shell listing as the independent side: spawn a shell and have IT expand
`scripts/*.mjs`, one path per line, then take basenames. That side runs in a different
process, has the glob expanded by the shell rather than by a Node filter, applies no
`isFile()` test, and performs no Node-side read — so a Node-side filter change, a read failure
or a byte that defeats a Node read moves ONE number and not the other, which is exactly the
class of skip this assertion exists to catch. `spawnSync` is already imported in this file for
`runScript()`; do not add a second spawn helper.

**The shell command is fixed, not left to the implementer, because the obvious form is the
wrong one.** Use exactly `printf '%s\n' scripts/*.mjs`. Do NOT use `ls scripts/*.mjs`:
measured in this repository against this task's own probe, a bare `ls` whose glob matches a
DIRECTORY descends into it and prints its contents under a `scripts/zz-population-probe.mjs:`
header preceded by a blank line, emitting the directory's name with a trailing colon instead
of the path itself. That defeats the probe twice over — the divergence assertion would red for
the wrong reason (a phantom empty-string member and a colon-suffixed member entering the
listing side) rather than for the reason the probe exists to demonstrate, and a reader would
record a red that does not mean what the criterion says it means. `ls -d scripts/*.mjs` also
emits the path correctly and is an acceptable equivalent; `ls` without `-d` is not.

Guard the listing side against that form being reintroduced later rather than trusting the
comment: after splitting on newlines, DROP empty entries and DROP any entry ending in `:`, and
say in the code why — both are the signature of a bare `ls` recursing into a directory member.
Handle the no-match case explicitly too, rather than letting an unexpanded glob become a
phantom member.

Compare SORTED BASENAME LISTS, not bare counts, and report the difference in BOTH directions —
present in the walk and absent from the listing, and present in the listing and absent from
the walk. A count comparison tells you a number moved; a list comparison names the file, and
this gap exists precisely because a guard reported a state without naming the members it could
not see.

Say in the assertion's own message that the two sides are deliberately different mechanisms
and that collapsing them into the same mechanism silently voids the check. An equality
produced by a census must be provably a real one in this repository: a plain census over its
sources has already produced one false decision.

**C. Widen the NUL assertion from six scripts to the whole population,** and update
`scriptText()`'s docblock claim, which currently says the measurement covers six. Drive it
from the population function rather than from a hard-coded list, so it cannot fall behind the
same way the matrix did.

**D. Correct the three docblock claims at `:285-296`.** State the measured count of
root-accepting scripts; state that the population is derived from the FLAG and why; record
that the previous predicate derived it from the remedy, that this made the two scripts that
were behind the only two it could not see, and that the docblock asserted the opposite in the
present tense while both already existed; and restate the forward-looking promise correctly —
a NINTH root-accepting script added later fails this file by omission. Date the correction.

**E. Raise the floor.** The `covered.length >= 6` assertion is a non-vacuity floor for the
matrix; it becomes eight. Keep it a floor to be raised and never lowered, and say so.

**F. Run the file now, with the matrix still at six rows, and CAPTURE THE RED.** This is the
task's evidence, not a step towards it. Record the failing assertion's full message, both
lists as the assertion prints them, and the two script names. Do not proceed to Task 2 until
that output is captured verbatim. If the run does NOT go red, that is a finding — it would
mean the predicate is not measuring what this task believes it measures — and it must be
diagnosed before anything else.

**G. Add nothing that suppresses the red.** No exclusion list, no `UNMIGRATED` array, no skip,
no conditional. The next task closes it by adding rows.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice &amp;&amp; ! node --test audit-root-args.test.ts; RED=$?; cd /home/henrik/dev/henrik/git/c64-re-tools; rmdir scripts/zz-population-probe.mjs 2&gt;/dev/null || true; test ! -e scripts/zz-population-probe.mjs &amp;&amp; test $RED -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && node --test audit-root-args.test.ts` exits NON-ZERO, and the failing test is the completeness assertion. Its full output is recorded verbatim, and both `audit-gate` and `audit-mutation-harness` appear by name in the diff the assertion prints. A green run here fails this criterion — the whole point is that the flipped predicate bites.
    - Exactly one test fails. If more than one fails, the extra failures are diagnosed and recorded before Task 2 begins; a guard flip that breaks unrelated assertions is a finding.
    - The two population counts are recorded, taken with `grep -a` in this plan's tree: `grep -a -l -- '--root' scripts/*.mjs | wc -l` returns 8, and `grep -a -l 'parseRootArg(' scripts/*.mjs | wc -l` returns 8 as well after plans 32-16 and 32-17 landed. Both numbers are recorded, and the pre-round HEAD values of 8 and 6 are recorded beside them from the verifier's report as the reason this gap exists.
    - The population function's own returned list is printed and recorded, and it contains exactly the eight expected basenames. This is the direct measurement; the `grep` counts are the independent cross-check.
    - **The cross-check's two sides are DIFFERENT MECHANISMS, verified by reading them.** The walk is `readdirSync` + `isFile()` inside this process; the independent side is a `spawnSync` shell glob expansion in another process. A second `readdirSync`, or any second side that re-applies the walk's own filter, fails this criterion outright — an assertion whose two sides cannot disagree is the same tautology this whole plan exists to remove, reintroduced one line lower.
    - **The cross-check is non-vacuous, proven by mutating the SUBJECT rather than the assertion.** Create a DIRECTORY — not a file — at `scripts/zz-population-probe.mjs`, run the test file, and confirm the cross-check goes red naming `zz-population-probe.mjs` as seen by the shell listing and NOT seen by the walk. That is exactly the divergence the assertion exists to catch: the shell glob matches an entry by name, while the walk's `isFile()` filter drops it, so a Node-side filter silently discarding something present on disk becomes a named failure instead of a smaller number nobody reads. Then remove it with `rmdir`, confirm `test ! -e scripts/zz-population-probe.mjs`, and re-run to confirm green. Record the red, the removal and the green re-run. Note that an empty directory is invisible to `git status`, so the existence check is the load-bearing cleanup proof here, not the diff.
    - **Explicitly forbidden as the non-vacuity experiment for this assertion: reducing an expected count, editing the comparison, or otherwise mutating the guard.** That proves only that the comparison executes, which was never in doubt — it does not prove the two sides are independent, which is the only thing worth proving. The experiment must change the world the guard looks at. Task 2's two experiments already follow this discipline; this one matches them.
    - **The paired control is recorded.** A real, EMPTY scratch FILE at `scripts/zz-population-probe.mjs` is seen by BOTH sides and therefore must NOT red the cross-check. Run it, confirm green, delete it, confirm `git status --porcelain` clean. This is what distinguishes a divergence detector from a pinned snapshot, and recording it stops a later reader from "correcting" the assertion into a hard-coded count. Take this control AFTER the directory probe so the two readings cannot be confused. The file must be empty — a scratch file containing the token `--root` would join the population and red a different assertion, which would not be this control.
    - The NUL assertion covers all eight scripts and is driven from the population function, not from a hard-coded list. `scriptText()`'s docblock no longer claims a six-script measurement.
    - The three corrected docblock claims are present and dated, and `grep -c 'The table below is the whole population' src/mcp/vice/audit-root-args.test.ts` returns 0. Non-vacuous: it returns 1 at HEAD.
    - `grep -a -c 'UNMIGRATED' src/mcp/vice/audit-root-args.test.ts` returns 0 — no exclusion list was introduced.
  </acceptance_criteria>
  <done>
The guard measures the root-accepting population and is currently RED, naming the two scripts
Gap 2 names, with that red captured as evidence. This is the correct intermediate state and
it is committed as such.
  </done>
</task>

<task type="auto">
  <name>Task 2: The matrix covers all eight, with typed expectations that separate the contained from the read-only, and the guard goes green</name>
  <precondition>Task 1 has been committed and its red is captured; adding the rows before capturing the red would destroy this plan's only evidence that the guard bites.</precondition>
  <files>src/mcp/vice/audit-root-args.test.ts</files>
  <read_first>
    - `src/mcp/vice/audit-root-args.test.ts` as Task 1 left it — especially the three per-script case loops, so the new rows are written against the cases that will actually run for them: Case 1 (the equals form) runs for every row; Case 2 (an out-of-repository root exits 1 with a refusal) runs for every row today; Case 3 (a contained non-default root is a split-read refusal) runs only for `refuses` rows.
    - `scripts/audit-mutation-harness.mjs` as plan 32-17 left it — its `booleanFlags` and `valueFlags` declarations, its exactly-one-selector rule, the position of containment relative to row selection, and its two rejection prefixes. A matrix row that spawns it without a selector would hit the selector rule instead of the behaviour under test.
    - `scripts/audit-gate.mjs` as plan 32-16 left it — that it is on the argv seam and deliberately not on the containment seam, its header's dated note recording that decision and its named reversal trigger, and the fact that its verdict line and its typo line are distinct from an argument rejection.
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-VERIFICATION.md` — the `coincidental_reliance_items` block, whose `harden:` this task's harness row discharges mechanically.
  </read_first>
  <action>
**A. Add a fourth `ContainedExpectation` value** for a script that accepts an uncontained root
BY DESIGN, and document it beside the existing three in the same contract-not-convenience
voice: the script performs no write, so the trust boundary containment guards does not exist
for it; its own test suite points it at out-of-repository synthetic trees for a recorded
reason; and the expectation is paired with a write-freedom assertion that revokes the
acceptance the moment a write appears.

**B. Add a per-row `extraArgs` field to `MatrixRow`,** optional, supplied in code and never
from argv — the same rule `booleanFlags` and `valueFlags` already carry. Thread it through
`runScript()`'s call sites in the case loops so a row can name the arguments its script needs
before the behaviour under test is reachable.

**C. Add the two rows.**
- `audit-mutation-harness`, contained expectation `repo-root`, with extra arguments naming a
  row selector whose value is a deliberately NON-EXISTENT row name. The selector is required
  because the harness's exactly-one-selector rule fires before the behaviour under test; the
  non-existent name is required because it makes the case FAIL-SAFE — if the containment check
  ever stopped preceding row selection, the run would still find no row to plant rather than
  sweeping the real tree from inside a unit test. State that reasoning in the row's comment; it
  is the difference between a safe test and a destructive one.
- `audit-gate`, contained expectation `uncontained-read-only`, with no extra arguments.

**D. Make Case 2 typed rather than universal.** It currently asserts that every script refuses
an out-of-repository root. That is true of seven of the eight and false of the eighth by
design. Skip `uncontained-read-only` rows in that loop and give them their own case asserting
the OPPOSITE: an existing out-of-repository directory is ACCEPTED as an argument — stderr
carries neither the containment-refusal prefix nor the argument-rejection prefix — so the run
reached the gate's own logic. Assert the two directions in two separate tests so neither can
pass for the other's reason.

**E. Add the write-freedom assertion — the sole mechanical revocation of plan 32-16's
containment acceptance (`T-32-22`), so its measurement must be named exactly rather than left
as "no write call".** It has two halves, because a deny-list over call sites is open-ended and
an allow-list over the import surface is closed, and the two fail in different directions.

*E1 — the CLOSED half, over the DIRECT fs import surface.* Assert that the names
`scripts/audit-gate.mjs` binds from `node:fs` are exactly the read-only set it has today —
measured at `audit-gate.mjs:90`, exactly `readdirSync` and `readFileSync` — and that it binds
nothing from `node:fs/promises`, takes no fs namespace import, and performs no `require` or
dynamic `import()` of an fs module. Keep that pin exactly as strict as stated; the correction
below narrows the CLAIM, never the assertion.

**Scope the claim to what is measured, and say so in the assertion's own message.** E1 is
closed over the DIRECT fs import surface and over nothing else. It is NOT closed over "any
filesystem write", and the message must not say it is. Measured in this tree while writing
this plan: `scripts/audit-gate.mjs:93` binds `spawnSync` from `node:child_process`, and a
filesystem write is reachable through a spawned process with no fs binding whatsoever — so
"a write API cannot be CALLED without first being bound" is false as a general statement about
this file, today, and must not be written. The correct, measured form is narrower: within the
DIRECT fs import surface, a write API cannot be called without first being bound, so any fs
name outside the permitted set is a failure. Write the message as that scoped statement, not
as "fails on anything new by construction" — an unqualified completeness claim is precisely
the `CR-08` shape this round exists to remove, and reproducing it one file over, in the plan
that removes it, would be self-defeating.

Say in the message that this is an allow-list over an IMPORT surface — a closed check over
that surface — and that it is the exact opposite of an exclusion list over a population, which
removes members from measurement; this plan's prohibition forbids the latter and this is not
it. The two differ in object and in direction: E1 constrains what may be bound, and removes
nothing from any population.

*E2 — the OPEN half, over comment-stripped call sites.* Count occurrences, over source with
comment lines stripped FIRST, of an ENUMERATED filesystem-write API set. Enumerate it in the
assertion rather than gesturing at it: `writeFileSync`, `appendFileSync`, `createWriteStream`,
`mkdirSync`, `rmSync`, `rmdirSync`, `unlinkSync`, `renameSync`, `cpSync`, `copyFileSync`,
`truncateSync`, `writeSync`, plus the `node:fs/promises` forms `writeFile(`, `appendFile(`,
`copyFile(`. A single-API check is measured-inadequate: it would miss five of those. The count
must be zero.

Two mechanics are load-bearing and each has a measured reason:

- **Strip comments first.** `audit-gate.mjs` discusses writes in prose throughout its header
  and its Bash-writer section (measured at HEAD: `:40`, `:44`, `:48`, `:61`, `:774`, `:779`,
  `:788`), and plan 32-16's Task 1D adds a further note whose stated reversal condition is a
  write call appearing in this file. An unstripped count can therefore red on prose — the
  self-invalidating-header shape plan 32-16 already avoids by stripping comments for its own
  containment-resolver count. Use the same form here.
- **Do NOT match `process.stderr.write(`.** Measured: this file has five, at `:1015`, `:1059`,
  `:1076`, `:1090` and `:1113`. Stdio is not a filesystem write, and a bare `.write(` pattern
  would red on a correct file, which would make the assertion worthless in the other direction.
  Anchor on the enumerated API names, never on a bare `.write(`.

Write the message so it states the CONSEQUENCE rather than the fact: a write in this file means
the containment acceptance recorded in its header and in plan 32-16's `T-32-22` is revoked, and
the script must be moved onto the containment seam. Name BOTH residual holes in the same comment
so they are known limits rather than silent ones, and name them as measured rather than as
hypotheticals. First: a write reached through a name not in the enumerated set and not bound
at import time — an `openSync` with a write flag, say — would evade E2, which is why E1's
import-surface check exists beside it. Second, and present in the file today rather than
merely conceivable: `audit-gate.mjs:93` binds `spawnSync` from `node:child_process`, so a
write performed by a spawned process is outside BOTH halves — E1 sees no fs binding because
there is none, and E2 counts no enumerated fs API because none is called. Neither half claims
to cover it. State that limit explicitly so the pair is read as a scoped instrument rather
than as a completeness proof it cannot give.

**F. Confirm the split-read contract still holds for the two new members** rather than
assuming it. Neither new script binds a `../src/` specifier statically, so neither violates
the contract; measure that through the file's existing predicates rather than by inspection,
and let the existing contract test cover them now that they are in the population.

**G. Run the file and take it green.** Then run it a second time to confirm stability — three
of the case loops spawn real gates, one of which regenerates a directory under the default
root, and a test that passes only on a cold tree is not a test.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice &amp;&amp; node --test audit-root-args.test.ts &amp;&amp; node --test audit-root-args.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && node --test audit-root-args.test.ts` exits 0 with 0 failures, on two consecutive runs. Both counts recorded; a pass that is not reproducible on the second run fails this criterion.
    - `cd src/mcp/vice && npm run typecheck` exits 0 — the widened `MatrixRow` and the fourth expectation value type-check.
    - The matrix has eight rows and the completeness assertion passes with both lists equal. The test count for the file is recorded before and after; the delta accounts for the two new rows across the case loops plus the new typed case plus the write-freedom test.
    - **The harness row is a real standing guard, proven by making it fail.** Temporarily revert `scripts/audit-mutation-harness.mjs`'s argv handling to a hand-rolled read, run the file, and confirm the harness's Case 1 goes red because the `BAD ARGUMENTS --` prefix is absent. Record the failure verbatim, then restore the file, confirm `git diff` on it is empty AND that its content hash matches the pre-experiment hash. Both bounds are required, not one: this file is owned by plan 32-19 in the same wave (see flagged assumption 4), and the empty diff is the entire reason that cross-plan edge is safe to leave undeclared as a dependency. A revert that does not reproduce the hash is a stop condition, not a rounding error. This is also what makes the `CR-05` precondition a declaration rather than a hope.
    - **The write-freedom assertion names its API set and strips comments, and the reasons are recorded.** E2's enumerated set covers at least `writeFileSync`, `appendFileSync`, `createWriteStream`, `mkdirSync`, `rmSync`, `rmdirSync`, `unlinkSync`, `renameSync`, `cpSync`, `copyFileSync`, `truncateSync`, `writeSync` and the `node:fs/promises` forms; it is counted over COMMENT-STRIPPED source; and it does not match `process.stderr.write(`. Verified three ways, each recorded: the enumerated set is read from the assertion; `audit-gate.mjs`'s five `process.stderr.write(` calls are present and the file is GREEN, proving stdio is not caught; and the count is 0 at the head of this plan's tree across the whole set. A `writeFileSync`-only check, or an unstripped count, fails this criterion — the first misses five APIs, the second reds on the header prose plan 32-16 adds.
    - **E1's closed import check is present and pinned to the measured surface:** the fs import names are exactly `{ readdirSync, readFileSync }` (measured at `audit-gate.mjs:90`), nothing is bound from `node:fs/promises`, and there is no fs namespace import and no `require`/dynamic `import()` of fs. Recorded as the state plan 32-16 already measured, re-measured here.
    - **Both halves are non-vacuous, proven by mutating the SUBJECT.** Temporarily add to `scripts/audit-gate.mjs` a real `writeFileSync` import AND a call to it — the import so E1 reds, the call so E2 reds — run the file, and confirm BOTH assertions go red, each with its consequence message. A mutation that reds only one half leaves the other unproven and does not satisfy this criterion. Then restore, confirm `git diff` on that file is empty, and confirm its content hash matches the pre-experiment hash. All four readings recorded.
    - **The two out-of-repository cases assert opposite things and neither passes for the other's reason.** The harness's case asserts exit 1 with a containment-refusal prefix naming it; `audit-gate`'s case asserts the absence of both the containment-refusal and argument-rejection prefixes. Both outputs recorded verbatim.
    - The `extraArgs` values are literals in the matrix, not read from any environment variable, file or argv. Verified by reading the row definitions.
    - `git status --porcelain` lists exactly one changed path, `src/mcp/vice/audit-root-args.test.ts`, after both temporary experiments have been reverted — and specifically NOT `scripts/audit-mutation-harness.mjs` (owned by plan 32-19, same wave) or `scripts/audit-gate.mjs` (owned by plan 32-16). Both are additionally confirmed by content hash. This is the mechanical form of flagged assumption 4; if either path appears here, stop and do not merge.
  </acceptance_criteria>
  <done>
Every one of the eight root-accepting scripts has at least one spawned test; the two that
were invisible are visible and typed; and both the `CR-05` precondition and the containment
asymmetry are pinned by assertions that have each been observed to fail.
  </done>
</task>

<task type="auto">
  <name>Task 3: The evidence record and the gates re-run under a read broker state</name>
  <precondition>Tasks 1 and 2 have been committed and `git status --porcelain` is clean apart from this task's own output.</precondition>
  <files>.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-gap5-population-and-precondition.md</files>
  <read_first>
    - `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-gap2-root-seam-coverage.md` — round 1's evidence layout for a per-invocation matrix. Match it.
    - `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-close-gate.md` §2b — the broker-state reading method and its process-name false-positive trap. Use it and cite it.
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-VERIFICATION.md` — `gaps[1]` and the `coincidental_reliance_items` block, both quoted rather than paraphrased in the record.
  </read_first>
  <action>
**A. Read the broker state** by §2b's method, record it and cite it.

**B. Write the evidence file** carrying: the commit measured; the broker state; the captured
RED from Task 1 with both script names in it; the captured GREEN from Task 2 on two
consecutive runs; the population function's returned list and the independent `grep -a`
cross-checks with their file counts; the pre-round HEAD counts of eight and six quoted from
the verifier; the two non-vacuity experiments — the reverted harness un-wiring and the
reverted write call — with their failure messages and the empty `git diff` after each; and the
two opposite out-of-repository captures.

**C. Write the `CR-05` precondition declaration in its final, findable form.** State the
precondition, the measurement that showed the recorded evidence was safe by non-use rather
than by construction, what makes it true by construction now, and — the part that makes it a
declaration rather than a note — which named test reds if it stops being true. Cite the
verifier's `harden:` wording. Cross-reference plan 32-17's prose declaration in the harness
header so the two halves are findable from each other.

**D. Record the departure from `32-REVIEW.md`'s `CR-08` fix,** naming the reviewer's
`UNMIGRATED` proposal, the verifier's override, and why the override is right here: both
scripts are migrated, so the list would be empty. A departure from a review recommendation is
recorded, not silent.

**E. Re-run the gates** and record: `node scripts/check-guard-fates.mjs`,
`node scripts/audit-gate.mjs`, `cd src/mcp/vice && npm run typecheck`, and
`cd src/mcp/vice && npm run test:automated` with its four counts beside the broker state.
**Do NOT run the whole-glob `npm test`** — it blocks indefinitely on `vice-proxy.test.ts`;
cite `evidence/32-close-gate.md` §2b rather than re-taking the decision.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools &amp;&amp; test -s .planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-gap5-population-and-precondition.md &amp;&amp; node scripts/check-guard-fates.mjs &amp;&amp; node scripts/audit-gate.mjs &amp;&amp; cd src/mcp/vice &amp;&amp; npm run test:automated</automated>
  </verify>
  <acceptance_criteria>
    - The evidence file exists and carries the captured red, the captured green on two runs, both population measurements with their file counts, and both non-vacuity experiments with their failure messages and the empty `git diff` that followed each.
    - The `CR-05` precondition declaration names the specific test that reds if the precondition stops holding, quotes the verifier's `harden:` wording, and cross-references the harness header's prose half.
    - The departure from `32-REVIEW.md`'s `CR-08` fix is recorded with the reviewer's proposal, the verifier's override and the reason.
    - `cd src/mcp/vice && npm run test:automated` exits 0 with 0 failures, four counts recorded beside the broker state.
    - `node scripts/check-guard-fates.mjs`, `node scripts/audit-gate.mjs` and `cd src/mcp/vice && npm run typecheck` all exit 0, report lines recorded verbatim.
    - The whole-glob `npm test` was NOT run and the SUMMARY says so in words, citing `evidence/32-close-gate.md` §2b.
    - `git status --porcelain` byte-identical before and after, apart from this task's evidence file.
  </acceptance_criteria>
  <done>
The guard's own non-vacuity is recorded the same way this phase records every other guard's:
by a captured red against a real subject, followed by a captured green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → guard population | The set of files the completeness guard walks decides what it can see. A predicate that reads the wrong property makes the defect it exists to catch invisible. |
| matrix row → spawned process | A matrix row's arguments are passed to a real script that can mutate the working tree. A row whose arguments reach a destructive path turns a unit test into a sweep. |
| typed expectation → accepted risk | A `contained` value records which risks are accepted for which script. A value meaning "not checked" would launder an acceptance into an absence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-29 | Repudiation | `scriptsUsingTheSharedParser()` at `src/mcp/vice/audit-root-args.test.ts:413-426` | high | mitigate | The population is derived from the remedy, so the guard is structurally incapable of seeing an unmigrated script — while its own docblock asserts the opposite in the present tense. Task 1 derives the population from the flag, corrects all three docblock claims, and captures the resulting red naming both scripts as the guard's own non-vacuity evidence. |
| T-32-30 | Denial of service | the `audit-mutation-harness` matrix row's spawned arguments | high | mitigate | A row that spawned the harness with a real selector could plant into the working tree from inside a unit test, and `--all` would sweep it. Mitigated by construction: the row's extra arguments name a deliberately non-existent row, so even a regression in the ordering of containment relative to row selection leaves nothing to plant. The reasoning is written into the row's comment so a future edit cannot innocently replace the name with a real one. |
| T-32-31 | Elevation of privilege | the fourth `ContainedExpectation` value | medium | mitigate | A typed expectation meaning "uncontained" could become a general escape hatch for any script whose test is inconvenient. Mitigated by pairing it with the two-part write-freedom assertion — a closed allow-list over the fs import surface plus an enumerated filesystem-write API count over comment-stripped source — so a script may only carry the expectation while it provably performs no write, and by both halves being proven to fail when a real import-plus-call is introduced. The enumeration and the comment-stripping are the mitigation: a `writeFileSync`-only or unstripped check would be an acceptance revoked by an instrument that cannot see the revocation, which is this phase's own subject. |
| T-32-32 | Tampering | the two temporary non-vacuity experiments | medium | mitigate | Both experiments deliberately break a live script. A forgotten revert would ship a hand-rolled parser or a stray write. Mitigated by requiring an empty `git diff` AND a matching content hash on the touched file after each, and a `git status --porcelain` listing exactly one changed path at the end of the task. One of the two files, `scripts/audit-mutation-harness.mjs`, is owned by plan 32-19 in the SAME WAVE; that edge is declared in `transient_touches` and flagged assumption 4, and the empty-diff plus hash bound is precisely what makes leaving it out of `depends_on` sound rather than merely convenient. |
| T-32-33 | Repudiation | the population census read | medium | mitigate | A file silently skipped by the walk — for a read failure, a filter change or a NUL byte — would shrink the population and re-create the invisibility this gap is. Mitigated by cross-checking the walk's visited `.mjs` list against a `spawnSync` shell glob expansion — a deliberately different mechanism, since two `readdirSync` calls sharing a filter cannot disagree — compared as sorted lists in both directions so the skipped file is named; by the byte-preserving read; and by widening the NUL assertion to the whole population and driving it from the population function. The cross-check's own non-vacuity is established by a subject mutation (a directory the shell glob sees and `isFile()` drops), not by editing the assertion. |
| T-32-SC | Tampering | package-manager installs | — | not applicable | This plan runs no package-manager install and adds no dependency. Measured: `files_modified` contains no manifest and no lockfile. No package-legitimacy checkpoint is owed. |
</threat_model>

<verification>
1. Task 1's run is RED with exactly one failing test, naming both scripts; captured verbatim.
2. The population function's returned list is the eight expected basenames; the `grep -a` counts cross-check it; the walk-vs-shell-listing cross-check compares sorted basename lists across two different mechanisms and is proven non-vacuous by the directory probe (subject mutation), with the empty-file control recorded beside it. Both probes removed and their removal verified.
3. `grep -c 'The table below is the whole population'` returns 0; `grep -a -c 'UNMIGRATED'` returns 0.
4. Task 2's run is GREEN on two consecutive runs; `npm run typecheck` exits 0.
5. The harness un-wiring experiment reds the harness's Case 1 and is reverted to an empty `git diff`.
6. The write-freedom assertion enumerates its filesystem-write API set, counts over comment-stripped source, does not match `process.stderr.write(`, and is paired with the closed fs-import allow-list. The subject mutation — a real `writeFileSync` import plus a call — reds BOTH halves, and is reverted to an empty `git diff` and a matching content hash.
7. The two out-of-repository cases assert opposite outcomes; both outputs recorded.
8. `node scripts/check-guard-fates.mjs` and `node scripts/audit-gate.mjs` — exit 0.
9. `cd src/mcp/vice && npm run test:automated` — 0 failures, broker state recorded beside it. Do NOT run the full-glob `npm test`.
</verification>

<success_criteria>
- The completeness guard's population is the root-accepting population, measured from the flag.
- The guard was observed RED naming the two scripts before it was taken green, and no exclusion list exists.
- All eight root-accepting scripts have at least one spawned test.
- The docblock states what the guard measures, and the correction records what it used to claim.
- The `CR-05` precondition and the `audit-gate` containment acceptance are each pinned by an assertion that has been observed to fail.
</success_criteria>

<artifacts_this_phase_produces>
Every symbol, path, flag and literal this ROUND creates, marked with its creating plan.

**New test-matrix vocabulary** (all 32-18, this plan, in `src/mcp/vice/audit-root-args.test.ts`)
- A population function deriving from the `--root` flag, replacing the remedy-keyed one
- A fourth `ContainedExpectation` value for a read-only script that accepts an uncontained root by design
- A per-row `extraArgs` field on `MatrixRow`
- Two new `MATRIX` rows: `audit-mutation-harness` and `audit-gate`
- A typed out-of-repository case asserting acceptance, paired with the existing refusal case
- A write-freedom assertion over `scripts/audit-gate.mjs`
- A visited-file-count assertion over the population walk

**New parser options and return fields**
- `valueFlags` and `values` on `parseRootArg()` (32-17)

**New call sites of an existing seam**
- `parseRootArg` in `scripts/audit-gate.mjs` (32-16) and `scripts/audit-mutation-harness.mjs` (32-17)

**New files**
- `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-gap4-sweep-rerun.md` (32-15)
- `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-gap2-audit-gate-seam.md` (32-16)
- `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-gap2-harness-seam.md` (32-17)
- `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-gap5-population-and-precondition.md` (32-18, this plan)
- `src/mcp/vice/audit-harness-restore.test.ts` (32-19)
- `src/mcp/vice/fixtures/harness-signal/restore-latch-driver.mjs` (32-19)
- `src/mcp/vice/fixtures/harness-signal/signal-window-driver.mjs` (32-19)

**New exported functions**
- `plant`, `restoreAll`, `pendingRestoreCount` exported from `scripts/audit-mutation-harness.mjs` (32-19)

**New message literals introduced as contract surface**
- `audit-gate: BAD ARGUMENTS --` (32-16); `audit-mutation-harness: BAD ARGUMENTS --` (32-17); the harness SKIPPED and counts lines (32-15)

**New CLI flags**
- None. This round adds no flag to any script.
</artifacts_this_phase_produces>

<flagged_assumptions>
Surfaced rather than dropped, per the no-silent-drop rule.

1. **Every `scripts/*.mjs` containing the token `--root` is assumed to actually ACCEPT it.**
   Measured at planning time: the token appears in exactly eight files and all eight are
   root-accepting. A future file that merely mentions the flag in prose would join the
   population and fail the guard by omission — which is the correct, loud outcome, and the
   correct response is a matrix row with a typed expectation, never a predicate exception.
2. **Case 3 (the split-read refusal) is assumed to remain inapplicable to both new members.**
   Neither binds a `../src/` specifier statically at planning time. Task 2 measures it through
   the file's own predicates rather than asserting it; if either does bind one, its expectation
   must become `refuses` and the scripts must gain the refusal, which would be a finding.
3. **The two temporary non-vacuity experiments modify live scripts.** They are the only way to
   prove the two new pins are not themselves vacuous — a pin that has never been seen to fail
   is exactly what this gap is. Bounded by an explicit revert, an empty `git diff` assertion
   AND a content-hash match after each, inside a single task, in an isolated worktree.
4. **One of those experiments targets a file another plan in this same wave owns, and that
   edge is declared here rather than left to be discovered at merge time.** Task 2 temporarily
   reverts `scripts/audit-mutation-harness.mjs`'s argv handling; that file is in plan 32-19's
   `files_modified`, and 32-18 and 32-19 are both wave 3 with no `depends_on` between them.
   This is deliberately NOT converted into a dependency edge, for three measured reasons.
   (a) It is not a live race: the two plans execute in separate worktrees, so neither observes
   the other's tree. (b) It produces no merge-time hunk: the experiment is reverted inside the
   task and bounded by an empty `git diff` plus a content-hash match on that exact path, so
   32-18's branch carries no change to it and cannot conflict with 32-19's. (c) Adding
   `32-19` to `depends_on` would push 32-18 to wave 4 and, worse, would mean the harness matrix
   row and the un-wiring experiment run against the harness as 32-19 left it — with new
   exports and a signal handler — rather than against the state Task 2's `read_first` and
   rejection-prefix expectations are written against. The ordering is therefore left free and
   the edge is recorded. **If the experiment's revert ever fails to produce an empty diff, this
   assumption is void and the run must stop** rather than merge — the empty-diff and hash
   checks in Task 2 are what hold this reasoning up, and they are acceptance criteria, not
   advice. Plan 32-19's frontmatter carries the reciprocal note.
</flagged_assumptions>

<output>
Create `.planning/phases/32-the-deletion-and-the-grep-gate/32-18-SUMMARY.md` when done.
Record: Task 1's red verbatim with both script names; the population function's returned list;
both `grep -a` counts and the file-count assertion's non-vacuity experiment; the three
corrected docblock claims before and after; Task 2's green on two consecutive runs with test
counts before and after; both non-vacuity experiments with their failure messages and the
empty `git diff` after each; the two opposite out-of-repository captures; the `CR-05`
declaration and the test that pins it by name; the recorded departure from `32-REVIEW.md`'s
`CR-08` fix; the broker state and how it was read; the commit measured; and
`test:automated`'s four counts.
</output>
