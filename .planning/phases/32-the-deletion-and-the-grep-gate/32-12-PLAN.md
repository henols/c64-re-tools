---
phase: 32-the-deletion-and-the-grep-gate
plan: 12
type: execute
wave: 3
gap_closure: true
depends_on: ["32-11"]
files_modified:
  - scripts/check-skill-tool-coverage.mjs
  - scripts/check-skill-fork-honesty.mjs
  - scripts/check-skill-cli-invocations.mjs
  - scripts/generate-tool-support-table.mjs
  - src/mcp/vice/audit-root-args.test.ts
  - .planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-gap2-root-seam-coverage.md
autonomous: true
requirements: [CUT-04]
net_deletion: false   # deletes the false docblock sentence and no file. cleanup-wave's
                      # predicate is `git diff --diff-filter=D --name-only`
                      # (gsd-tools.cjs:759) — DELETED FILES only. This branch deletes none.

estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No script both accepts a root override AND reads its comparison data from the default root while claiming otherwise. Each of the four affected scripts either follows the resolved root for BOTH its corpus and its comparison data, or refuses a root that is not the default root with a diagnosable message naming the identifiers that make it impossible. — TRACE: verifier truth 12 / gaps[1].missing[1]"
    - "`node scripts/generate-tool-support-table.mjs --root <an in-repository directory that is not the repository root>` exits NON-ZERO and leaves the real `docs/tool-support.md` byte-identical. Today it exits 0 and writes a table built from the SYNTHETIC manifest against the REAL registry — the hybrid the verifier recorded as ✗ FAIL. — TRACE: verifier truth 12 / gaps[1].missing[1] / verifier key-link row 'Four --root-ised gates → ../src/mcp/vice/*.ts'"
    - "The docblock sentence forbidding a path re-derived from the default root is present ONLY in files where it is true. It is deleted from the files where the verifier measured it as false, and each deletion is replaced by the true statement of what that file does instead. Today `grep -c` returns 1 in four files, three of which contradict it in the same file. — TRACE: verifier truth 12 / gaps[1].missing[1]"
    - "A contract assertion goes red the instant a future phase gives a script a root override while leaving its comparison data statically bound to the default root. It drives the real file contents, it is non-vacuous (it fails against a planted violation), and it asserts the positive side too, so it cannot pass by matching an empty set. — TRACE: gaps[1].missing[1] / the assumption-delta companion recorded in plan 32-10's `<assumption_delta_decision>`"
    - "`.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-gap2-root-seam-coverage.md` records, with raw command and raw output, each of the three commands the verifier reproduced as ✗ FAIL, before and after — so a later reader can re-run the closure rather than take it on trust. — TRACE: gaps[1].missing[2] / verifier truth 12"
  prohibitions:
    - statement: "MUST NOT introduce any relaxation hatch — no environment-variable override, no skip or force flag, no waiver file, no allow-list — while settling the split read. A root that cannot be honoured must REFUSE with a diagnosable message; it must never degrade into a silent read of the default root, and it must never be honoured 'partially'. (Carried forward from plan 32-01's prohibition 2 and `scripts/audit-gate.mjs`'s D-12-14, not minted here.)"
    - statement: "MUST NOT make a guard, gate or check green by editing a dated evidence record, a `*-SUMMARY.md`, or `32-VERIFICATION.md`. This plan writes ONE NEW evidence file and corrects no existing record. (Carried forward from the phase's D-08 standard and this project's twice-caught fact-laundering failure mode.)"
  artifacts:
    - path: "scripts/check-skill-tool-coverage.mjs"
      provides: "Split read settled: a root that is not the default root is refused, naming the two identifiers that cannot follow it"
      contains: "SPLIT READ"
    - path: "scripts/check-skill-fork-honesty.mjs"
      provides: "Split read settled: a root that is not the default root is refused, naming the identifier that cannot follow it"
      contains: "SPLIT READ"
    - path: "scripts/check-skill-cli-invocations.mjs"
      provides: "Split read settled: a root that is not the default root is refused, naming the identifier that cannot follow it"
      contains: "SPLIT READ"
    - path: "scripts/generate-tool-support-table.mjs"
      provides: "The WRITING script: a root that is not the default root is refused before any read or write, naming the two identifiers that cannot follow it"
      contains: "SPLIT READ"
    - path: "src/mcp/vice/audit-root-args.test.ts"
      provides: "Extended with the split-read refusal cases and the contract assertion that keeps the invariant from silently reverting"
      min_lines: 300
    - path: ".planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-gap2-root-seam-coverage.md"
      provides: "Raw before/after for the three verifier reproductions, the eighteen matrix cases and the four refusals"
      contains: "definitely-not-here"
  key_links:
    - from: "src/mcp/vice/audit-root-args.test.ts"
      to: "scripts/check-skill-tool-coverage.mjs"
      via: "reads the file's own text and asserts the import/refusal invariant against it, plus a planted-violation control"
      pattern: "check-skill-tool-coverage\\.mjs"
    - from: "scripts/generate-tool-support-table.mjs"
      to: "scripts/lib/audit-root.mjs"
      via: "resolves the root, then refuses it if it is not the default root, before any read or write"
      pattern: "resolveContainedRoot"
---

<!-- planner-discipline-allow: parseArgs -->
<!-- planner-discipline-allow: parseRootArg -->
<!-- planner-discipline-allow: DEFAULT_ROOT -->
<!-- planner-discipline-allow: SPLIT READ -->
<!-- planner-discipline-allow: REFUSED -->
<!-- planner-discipline-allow: r2000 -->
<!-- planner-discipline-allow: regenerator2000 -->
<!-- planner-discipline-allow: guard-fates -->
<!-- planner-discipline-allow: CAPABILITY_REGISTRY -->
<!-- planner-discipline-allow: CURATED_ANNO_TOOLS -->
<!-- planner-discipline-allow: VERB_OPTIONS -->
<!-- planner-discipline-allow: DENY_LIST -->

<objective>
GAP CLOSURE, round 1. **The second half of Gap 2: the split read.** Plans 32-10 and 32-11
made the root argument parse correctly. This plan makes it MEAN something correct.

The verifier's finding, reproduced at HEAD: four of the five root-ised gates read their
corpus from the resolved root while statically importing their comparison data from the
default root — `CAPABILITY_REGISTRY` and `CURATED_ANNO_TOOLS` in
`scripts/check-skill-tool-coverage.mjs:50-51`, `CAPABILITY_REGISTRY` in
`scripts/check-skill-fork-honesty.mjs:70`, `VERB_OPTIONS` in
`scripts/check-skill-cli-invocations.mjs:43`, and `CAPABILITY_REGISTRY` plus `DENY_LIST`
in `scripts/generate-tool-support-table.mjs:40-41`. Three of those files carry, verbatim,
a docblock sentence forbidding exactly that. Under a synthetic tree the gate compares the
synthetic corpus against the real registry — a false green inside the audit instrument
itself.

Implements **D-06** (a guard is pointed at a synthetic tree, and what it compares against
must therefore come from that tree too) and **D-07** (a root override is added where the
change is cheap and self-contained — this plan settles what "self-contained" turned out to
mean for four gates where it was not).

**The branch this plan takes, and why it is not the other one.** The verifier names two
acceptable remedies: root-parameterised dynamic imports, or an outright refusal of a
non-default root with the false claim deleted. This plan takes the **refusal**, for three
recorded reasons. First, the dynamic-import route requires the synthetic tree to carry a
working `src/mcp/vice/` module graph, at which point it is a copy of the repository rather
than a small planted corpus — which is not the thing D-06's flag exists to point at.
Second, `WR-11` measured that no caller is waiting for the capable version: the fate
registry passes a root override in 0 of 61 rows and all 35 plants are `kind: "worktree"`.
Third, refusal is the honest form of the promotion recorded in plan 32-10's
`<assumption_delta_decision>` — exactly one root governs every read in one invocation — and
it replaces a false claim with a true, tested behaviour instead of documenting the false
one. `scripts/check-skill-description-overlap.mjs` is untouched: it is the one gate that
already honours the root for both halves, its docblock claim is true, and plan 32-11 gave
it the test that proves it.

Output: four scripts whose root behaviour matches what they say about themselves, a
contract assertion that keeps it that way, and one new evidence file recording the closure
raw.

**SCOPE FENCE.** Every truth above traces to a verifier truth number or a `gaps[].missing`
line. Explicitly OUT of scope, recorded rather than silently skipped: `WR-06` (the fate
guard does not tie an observed red to its row's subject — the data is clean 35 of 35 and
the verifier calls the registry "honest today"; closing it means changing the blocking
gate's predicate and re-proving its non-vacuity, which is scope this round is not judged
against and which would grow the very surface the next verification scores), `WR-12`,
`WR-13`, `WR-14`, and broken windows #28–#32. All stay OPEN in
`.planning/todos/pending/2026-08-31-phase-32-review-twenty-five-open-findings.md`.

**Edge-coverage arithmetic for this plan.** This plan owns 0 of the round's 6 probe items;
they are owned by plans 32-10 (3), 32-11 (1), 32-13 (1) and 32-14 (1). Round total, stated
in plan 32-10: 6 surfaced == 6 authored (5 explicit + 1 backstop) + 0 flagged assumptions.
Nothing dropped here or anywhere.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-CONTEXT.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-VERIFICATION.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-11-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settle the split read — four scripts refuse a root they cannot honour, and the false docblock claim is deleted where it is false</name>
  <precondition>Plans 32-10 and 32-11 have merged: all six root-accepting scripts call the shared parser and none defines an argv reader of its own. Assert with a grep before editing, and halt if any local reader remains.</precondition>
  <files>scripts/check-skill-tool-coverage.mjs, scripts/check-skill-fork-honesty.mjs, scripts/check-skill-cli-invocations.mjs, scripts/generate-tool-support-table.mjs</files>
  <read_first>
    - `scripts/check-skill-tool-coverage.mjs` — the module docblock at `:60-70` including the sentence at `:67`; the static imports at `:50-51`; `DEFAULT_ROOT` at `:56`; `paths()` in full with its `required` list; the root resolution and its handler at `:110-125`; and the missing-input diagnostic that follows at `:127-145`, whose wording is the model for the new refusal's tone.
    - `scripts/check-skill-fork-honesty.mjs` — docblock at `:79-89` including `:86`; the static import at `:70`; `DEFAULT_ROOT` at `:75`; `paths()` in full; the resolution at `:130-145`.
    - `scripts/check-skill-cli-invocations.mjs` — docblock at `:65-75` including `:72`; the static import at `:43`; `DEFAULT_ROOT` at `:61`; `paths()` in full; the resolution at `:107-120`; and `:164`, the existing branch on whether the resolved root equals the default root, which this change must keep working.
    - `scripts/generate-tool-support-table.mjs` — the static imports at `:40-41`; `DEFAULT_ROOT`; `paths()` at `:50-70` including the docblock at `:56-62` that names an out-of-repository write as "worse than no flag at all"; and `main()` as plans 32-10 left it.
    - `scripts/check-skill-description-overlap.mjs` — `:73-97` including the sentence at `:84` and the comment at `:97` about a CLAUDE.md resolved from the default root. This file is the CONTROL: its claim is true, it is not edited, and the new contract assertion must pass on it without a refusal.
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-VERIFICATION.md` — the Gap 2 `reason` paragraph beginning "Compounding it, CR-03 is also reproduced", the `artifacts` rows for these four files, and the "Anti-Patterns Found" row whose Pattern column reads "Static import from `DEFAULT_ROOT` under a docblock forbidding exactly that".
  </read_first>
  <action>
For each of the four scripts, in this order — `scripts/generate-tool-support-table.mjs`
FIRST, because it is the one that writes and therefore the one where a wrong root has a
filesystem consequence:

**A. Refuse a resolved root that is not the default root.** Immediately after the existing
containment resolution succeeds, and BEFORE any read, any glob, any file existence check
and any write, compare the resolved root against that file's own default root. If they
differ, print to stderr the script's own base name followed by `SPLIT READ REFUSED --` and
a message that states, concretely: the resolved root; the default root; the exact
identifiers this script imports statically and the module path they come from; and the
consequence in one sentence — that honouring the root would compare a corpus read from the
resolved root against comparison data read from the default root, which is a false green
rather than a stricter check. Exit 1.

The identifiers to name, per file:
  - `scripts/check-skill-tool-coverage.mjs`: `CAPABILITY_REGISTRY` and
    `CURATED_ANNO_TOOLS`, from `../src/mcp/vice/capability-registry.ts` and
    `../src/mcp/vice/anno-tools.ts`.
  - `scripts/check-skill-fork-honesty.mjs`: `CAPABILITY_REGISTRY`, from
    `../src/mcp/vice/capability-registry.ts`.
  - `scripts/check-skill-cli-invocations.mjs`: `VERB_OPTIONS`, from
    `../src/mcp/vice/anno-cli.ts`.
  - `scripts/generate-tool-support-table.mjs`: `CAPABILITY_REGISTRY` and `DENY_LIST`, from
    `../src/mcp/vice/capability-registry.ts` and `../src/mcp/vice/vice.ts`.

Compare RESOLVED paths, not raw arguments, so that a root spelled `.`, spelled as an
absolute path, or spelled with a redundant segment is all recognised as the default root
and accepted. This is the `[edge:CUT-04/adjacency]` boundary and plan 32-11 already pins it
for the fate guard; these four must agree with it.

**B. Delete the false docblock sentence.** In `scripts/check-skill-tool-coverage.mjs:67`,
`scripts/check-skill-fork-honesty.mjs:86` and `scripts/check-skill-cli-invocations.mjs:72`
the sentence forbidding a re-derived default-root path is contradicted in the same file.
Delete it. Replace it with the true statement: what the file imports statically, that those
imports cannot follow a root override, that a non-default root is therefore refused rather
than half-honoured, and one line naming `CR-03` as the finding that produced the change so
a later reader can find the reasoning rather than re-derive it.

Measure `scripts/generate-tool-support-table.mjs` first rather than assuming: if it carries
that sentence, treat it the same way; if it does not, record the measured count in the
SUMMARY and leave its docblock's other claims alone. Do NOT delete the `paths()` docblock
at `:56-62` — that claim is about an out-of-repository write and is true, and this change
makes it truer.

**C. Do not delete the sentence from `scripts/check-skill-description-overlap.mjs`.** It is
true there. That file is not in this task's `files_modified` and must not be edited.

**D. Preserve every existing diagnostic.** The missing-input TYPO messages, the containment
refusal, the argument rejection from plan 32-11 and
`scripts/check-skill-cli-invocations.mjs:164`'s default-root branch all stay exactly as
they are. The new refusal is an additional, earlier gate — not a replacement for any of
them.

**E. Confirm the two untouched scripts really are untouched-worthy, and record the
measurement.** `scripts/check-guard-fates.mjs` and
`scripts/check-skill-description-overlap.mjs` must contain zero static imports whose
specifier reaches into `../src/`. Measure it, write the count in the SUMMARY, and if either
returns non-zero, stop and report rather than silently widening this task.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools; for s in generate-tool-support-table check-skill-tool-coverage check-skill-fork-honesty check-skill-cli-invocations; do node scripts/$s.mjs >/dev/null 2>/dev/null; echo "$s bare=$?"; done; git diff --exit-code -- docs/tool-support.md; echo "table-clean=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Each of the four scripts run bare exits with the SAME status it did before this change; record each measured status in the SUMMARY.
    - For each of the four, invoking it with a root pointing at an in-repository directory that is not the repository root exits NON-ZERO and its stderr contains `SPLIT READ REFUSED`.
    - `node scripts/generate-tool-support-table.mjs --root <in-repository non-root directory>` exits non-zero and `git diff --exit-code -- docs/tool-support.md` exits 0 afterwards. Measured baseline at HEAD: exit 0, having written a table built from the synthetic manifest against the real registry.
    - For each of the four, invoking it with a root that RESOLVES to the repository root — spelled `.`, spelled absolutely, and spelled with a redundant `./x/..` segment — exits with the same status as the unflagged run. All three spellings are asserted.
    - `grep -c 'do not re-derive a path from' scripts/check-skill-tool-coverage.mjs scripts/check-skill-fork-honesty.mjs scripts/check-skill-cli-invocations.mjs` returns 0 for all three; the same command against `scripts/check-skill-description-overlap.mjs` still returns 1.
    - `grep -c 'CR-03' scripts/check-skill-tool-coverage.mjs scripts/check-skill-fork-honesty.mjs scripts/check-skill-cli-invocations.mjs` returns at least 1 for each.
    - `grep -cE '^import .* from "\.\./src' scripts/check-guard-fates.mjs` returns 0 and the same command against `scripts/check-skill-description-overlap.mjs` returns 0.
    - `cd src/mcp/vice; npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>
No script claims a soundness property it does not have. Four scripts refuse a root whose
comparison data cannot follow it, naming the exact identifiers that make it impossible; one
script honours a root fully and says so truthfully; and the writing script can no longer be
pointed at any tree but the real one.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The contract assertion that keeps the invariant, and the raw closure evidence</name>
  <files>src/mcp/vice/audit-root-args.test.ts, .planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-gap2-root-seam-coverage.md</files>
  <read_first>
    - `src/mcp/vice/audit-root-args.test.ts` — the WHOLE file as plan 32-11 left it: the six-script table, the temporary-directory discipline, the synthetic-tree helper, and the header's note about which half of the ordering backstop this file does and does not carry.
    - `src/mcp/vice/guard-fates.test.ts` — the planted-violation convention this repository uses: a violation is planted against the REAL predicate and the test asserts the predicate reports it, never against a re-implementation. The contract assertion below follows the same rule with file text in place of a registry object.
    - The four scripts as Task 1 left them, plus `scripts/check-skill-description-overlap.mjs` and `scripts/check-guard-fates.mjs` as the two clean controls.
    - `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-tracer-observed-red.md` — the evidence-file shape this phase uses: raw command, raw output, exit status, and the state of the machine at the time.
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-VERIFICATION.md` — the "Behavioural Spot-Checks" table. Its three ✗ FAIL rows are the before-half of this evidence file.
  </read_first>
  <behavior>
    Tests to write BEFORE anything else, all driving real file text and real processes.

    Contract assertion, over all six root-accepting scripts:
    - For every script that accepts a root override AND contains a static import whose
      specifier reaches into `../src/`, that script's text must also contain the split-read
      refusal literal. Four scripts must satisfy the rule by carrying the refusal.
    - Non-vacuity in the positive direction: the count of scripts matched by the antecedent
      must be at least 4. A rule that passes because nothing matched proves nothing, and
      this is the exact failure mode `CUT-03` exists to forbid.
    - Non-vacuity in the negative direction: a planted synthetic script text carrying the
      static import and NO refusal literal must be reported by the same predicate. Drive the
      real predicate against the planted text, not a copy of the rule.
    - The two clean controls must satisfy the rule by carrying no matching import at all,
      and the test must assert that specifically — so a future edit that adds an import to
      one of them without a refusal is caught by the same assertion.

    Split-read refusal cases, added to the six-script matrix:
    - Each of the four refusing scripts, with an in-repository non-root directory: exits
      non-zero, stderr contains the refusal literal.
    - Each of the four, with a root resolving to the repository root, in all three
      spellings: same exit status as the unflagged run.
    - `check-skill-description-overlap` with the synthetic tree from plan 32-11: still reads
      the synthetic tree and does NOT refuse. This is the control that proves the new
      refusal is targeted rather than blanket.
  </behavior>
  <action>
**A. Add the contract assertion to `src/mcp/vice/audit-root-args.test.ts`.** Express the
antecedent and the consequent as two small predicates over a file's text, exported or
declared inside the test so the planted-violation case can drive the SAME predicate that
the real files are driven through. Read each real script with `readFileSync` and `latin1`,
matching the byte-level convention this phase's document sweep already uses; note that a
plain text read is sufficient here because none of these six files carries a NUL byte, and
record that as a measured fact rather than an assumption.

Write the rule as one sentence in the test's own comment, in the form a future reader can
act on: a script that accepts a root override may not statically import comparison data
from the default root unless it also refuses a non-default root. Name it as the invariant
companion to plan 32-10's `<assumption_delta_decision>`, so the two are findable from each
other.

**B. Add the split-read matrix rows** to the existing six-script table rather than a second
table. The four refusing scripts get their contained-root expectation tightened from plan
32-11's deliberately loose "non-zero plus a diagnostic" to the specific refusal literal;
`check-skill-description-overlap` keeps its synthetic-tree read; `check-guard-fates` keeps
its repository-root acceptance.

Reuse plan 32-11's synthetic-tree helper and its cleanup discipline verbatim — the
directory is created inside the repository because containment requires it, is removed in a
`finally` and in a `t.after` hook, and is covered by the `.gitignore` entry plan 32-11
added. Do not create a second helper.

**C. Write `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-gap2-root-seam-coverage.md`.**
This is a NEW file. It does not correct, rewrite or replace any existing record — no dated
evidence document, no `*-SUMMARY.md` and not `32-VERIFICATION.md` is edited by this plan.
Record, each with its raw command, its raw output and its exit status:

  1. The three commands the verifier recorded as ✗ FAIL, as they behave now. Quote the
     verifier's own before-values beside each, attributed to
     `32-VERIFICATION.md`'s "Behavioural Spot-Checks" table, so the before-half is cited
     rather than re-measured — the old behaviour no longer exists to measure.
  2. Every case in the six-script matrix: script, argv, exit status, and the first line of
     output.
  3. The four split-read refusals in full, including the identifier names each message
     prints.
  4. The two clean controls' measured import counts.
  5. The state of the machine: the commit measured, and whether a VICE broker was running.
     A live broker deterministically reds the `BACK-05` test and the `stock-broker-live`
     family, so any suite figure in this file is uninterpretable without it. Read the
     broker's state read-only; do not stop or start it.
  6. `git status --porcelain` before and after, byte-identical.

Close the file with a short section stating what the seam is now claimed to do and what it
is NOT claimed to do — specifically that four gates refuse a synthetic root rather than
supporting one, that this is the branch chosen from the verifier's two acceptable remedies,
and that `WR-11`'s observation stands: the seam still has no production caller, and the
contract assertion rather than a caller is what now keeps it honest.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice; node --test audit-root-args.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice; node --test audit-root-args.test.ts` exits 0.
    - The contract assertion's antecedent matches at least 4 of the 6 scripts, asserted explicitly, and the planted-violation case is reported by the same predicate the real files are driven through.
    - Temporarily deleting the refusal literal from any one of the four scripts makes `node --test audit-root-args.test.ts` exit non-zero. Perform this check, record the exit status and the failing assertion's name in the SUMMARY, and restore the file — leaving the tree byte-identical, confirmed by `git status --porcelain`.
    - `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-gap2-root-seam-coverage.md` exists and contains a raw command, a raw output and an exit status for each of the three verifier reproductions, all eighteen-plus matrix cases and all four refusals.
    - `git diff --name-only` for this task lists no `*-SUMMARY.md`, no `32-VERIFICATION.md` and no pre-existing `evidence/*.md` file.
    - `cd src/mcp/vice; npm run test:automated` reports 0 failures, with the broker's state recorded beside the number. Do NOT run the full-glob `npm test` — it blocks forever on `vice-proxy.test.ts` and outlives any timeout.
    - `node scripts/check-no-regenerator2000.mjs` exits 0, and every one of the nine `docs-*.test.ts` guards run individually exits 0.
    - `node scripts/check-guard-fates.mjs` exits 0 — the blocking CI gate is still green after all three Gap-2 plans.
  </acceptance_criteria>
  <done>
The invariant is enforced by a test that goes red if a future phase reintroduces the split
read, non-vacuously in both directions, and the whole Gap-2 closure is recorded raw in a
new evidence file that rewrites nothing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator argv → audited gate's comparison data | A root value selects the corpus a gate measures; the data it measures the corpus AGAINST comes from a different, statically bound location. |
| repository filesystem → generated document | `generate-tool-support-table.mjs` writes `docs/tool-support.md`, a tracked file CI compares byte-for-byte. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-07 | Spoofing | the four split-read gates | high | mitigate | A gate pointed at a synthetic corpus compares it against the REAL registry, so a planted violation in the synthetic tree is invisible and the gate reports green. That is a false attestation from the audit instrument itself — the verifier's words, "a false green inside the audit instrument". Task 1 refuses the root before any read; Task 2's contract assertion prevents it silently returning. |
| T-32-01 | Tampering | `scripts/generate-tool-support-table.mjs` root → file write | high | mitigate | Carried forward from plan 32-10 and tightened here: after Task 1 the writing script can be pointed at no tree but the real one, so the class of "wrote the wrong table" is closed by construction rather than by argument validation alone. Asserted by a byte-identity check on `docs/tool-support.md` after every refusal. |
| T-32-02 | Tampering / Elevation | `resolveContainedRoot()` lexical containment | medium | accept | `WR-04`, unchanged disposition. Containment is lexical, so an in-repository symlink pointing outward defeats it. Accepted at ASVS L1 for the reason the module's header records — resolving symlinks would make the function throw on a not-yet-created directory that several callers legitimately pass. Below the `high` blocking threshold; exploitation presupposes an attacker already able to write into the repository. Stays OPEN in the findings todo. Note that Task 1's refusal materially shrinks this threat's reach: four of the six scripts now accept only the default root, where the containment question does not arise. |
| T-32-08 | Repudiation | `checkGuardFates()`'s `redOwed()` | medium | accept | `WR-06`: the gate does not require an observed red's control to name the same command, does not tie the red to the row's subject, and does not require a plant at all, so a hand-typed observation would pass the blocking CI gate. Accepted for this round, not silently: the verifier audited the actual data and found it clean on all three counts (35 of 35 command-paired, 35 of 35 distinct excerpts, 35 of 35 with a plant), so the registry is honest today. Not mitigated here because no `gaps[].missing` line asks for it and changing the blocking gate's predicate would grow the surface the next verification scores. Tracked OPEN in the findings todo. |
| T-32-SC | Tampering | package-manager installs | — | not applicable | This plan runs no package-manager install and adds no dependency. Measured: `files_modified` contains no manifest and no lockfile. No package-legitimacy checkpoint is owed. |
</threat_model>

<verification>
1. The four bare runs, each exit status recorded against its pre-change value.
2. The four split-read refusals, with their full messages.
3. All three spellings of a root resolving to the repository root, per script.
4. `node --test audit-root-args.test.ts` — exit 0, plus the deliberate temporary-deletion check with the tree restored.
5. `cd src/mcp/vice; npm run typecheck` — exit 0.
6. `cd src/mcp/vice; npm run test:automated` — 0 failures, broker state recorded.
7. `node scripts/check-guard-fates.mjs` — exit 0.
8. `node scripts/check-no-regenerator2000.mjs` — exit 0; the nine `docs-*.test.ts` guards each exit 0.
9. `git status --porcelain` before and after — byte-identical.
</verification>

<success_criteria>
- Every root-accepting script's behaviour matches what its own docblock says about it.
- A synthetic root can no longer produce a green from a gate that would be comparing against the real registry.
- The writing script cannot be pointed at any tree but the real one.
- The invariant is held by a non-vacuous test rather than by convention.
- No existing dated record was edited.
</success_criteria>

<artifacts_this_phase_produces>
Every symbol, path and flag this ROUND creates, marked with its creating plan, so the
source-grounding pass excludes them from drift verification.

**New exported functions**
- `parseRootArg(argv, { script, booleanFlags })` — `scripts/lib/audit-root.mjs` (32-10)

**New files**
- `src/mcp/vice/audit-root-args.test.ts` (32-10; extended by 32-11 and by this plan)
- `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-gap2-root-seam-coverage.md` (32-12, this plan)
- `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-gap3-harness-correction.md` (32-14)

**New declarations**
- `parseRootArg` in `scripts/lib/audit-root.d.mts` (32-10)

**New message literals introduced as contract surface**
- `BAD ARGUMENTS --` argument-rejection prefix (32-10)
- `SPLIT READ REFUSED --` refusal prefix in four scripts (32-12, this plan)

**New document sections**
- `## 6.1 CUT-04's NAMED list, adjudicated name by name` in
  `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md` (32-13)
- A dated Gap-1 closure note beside `CUT-04` in `.planning/REQUIREMENTS.md` (32-13)
- A dated correction block appended to
  `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-sweep-renamed-rows.md` (32-14)

**New gitignore entries**
- `.audit-root-synth-*` (32-11)

**New CLI flags**
- None. This round adds no flag.

**New registry fields**
- None. Plan 32-14 re-measures two field values on ONE existing row; it adds no field and no
  row.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/32-the-deletion-and-the-grep-gate/32-12-SUMMARY.md` when done.
Record: the four bare-run exit statuses against their pre-change values; each split-read
refusal message in full; the measured docblock-sentence counts before and after; the two
clean controls' import counts; the temporary-deletion check's exit status and failing
assertion name; the broker's state at the `test:automated` run; and `git status --porcelain`
before and after.
</output>

