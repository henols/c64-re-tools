---
phase: 32-the-deletion-and-the-grep-gate
plan: 03
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - .planning/PROJECT.md
  - .planning/ARCHITECTURE.md
  - scripts/check-no-regenerator2000.mjs
autonomous: true
requirements: [CUT-06]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`.planning/PROJECT.md:311` cites `vice-proxy.ts:3050`, `:2985`, `vice-proxy.ts:1529` and `:1507` corrected to `:1505` — all four matching CLAUDE.md:26 and all four verified against `src/mcp/vice/vice-proxy.ts` in the same run."
    - "`.planning/PROJECT.md:311` names the `anno_*` family, not the deleted `r2000_*` family, matching its already-corrected twin at CLAUDE.md:26."
    - "`.planning/PROJECT.md`'s D-36 Key Decisions row names a guard file that exists on disk; `docs-r2000-decisions.test.ts` appears nowhere in the file."
    - "`.planning/ARCHITECTURE.md`'s Rule A21 no longer states a present-tense invariant about a module that does not exist; it is dated, superseded-by-the-deletion prose, and no historical sentence in the file was rewritten."
    - "The removal gate's header carries no live-tense false number: the NUL-byte offset and line are the measured current values with the measurement date, and the tree-wide occurrence totals are either re-measured with a date or explicitly attributed to plan 29-02."
    - "Every one of the 17 `r2000_`-carrying lines in `.planning/PROJECT.md` has an explicitly recorded correct-or-keep verdict — none is left un-adjudicated."
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Repaired Architecture constraint bullet and D-36 row"
      contains: "anno_*"
    - path: ".planning/ARCHITECTURE.md"
      provides: "Rule A21 converted to dated, superseded prose"
      contains: "A21"
    - path: "scripts/check-no-regenerator2000.mjs"
      provides: "Header drift repair on the NUL-byte provenance claim"
  key_links:
    - from: ".planning/PROJECT.md"
      to: "src/mcp/vice/vice-proxy.ts"
      via: "the four MCP-02 line citations in the Architecture constraint bullet"
      pattern: "vice-proxy\\.ts:[0-9]+"
    - from: ".planning/PROJECT.md"
      to: "src/mcp/vice/docs-absorbed-decisions.test.ts"
      via: "the D-36 Key Decisions row naming the guard that pins it"
      pattern: "docs-absorbed-decisions"
---

<!-- planner-discipline-allow: regenerator2000 -->
<!-- planner-discipline-allow: :2987 -->
<!-- planner-discipline-allow: :1507 -->
<!-- planner-discipline-allow: docs-r2000-decisions -->
<!-- planner-discipline-allow: 12862 -->
<!-- planner-discipline-allow: line 291 -->
<!-- planner-discipline-allow: 399 with -->
<!-- planner-discipline-allow: r2000_ -->

<objective>
`CUT-06`, part A: correct the living pointers, leave the history byte-identical, and repair the one
live-tense false number in the removal gate's own header.

Three subjects, all pre-adjudicated by research and all landing before plan 32-04 widens
`docs-linerefs.test.ts` onto `PROJECT.md`:

1. `.planning/PROJECT.md:311` — four `vice-proxy.ts` line citations stale by exactly +2, on the same
   line as a live pointer at the deleted `r2000_*` tool family. One edit discharges both `D-10` and
   `D-08`. The line already records its own defect, which is the strongest support for `D-10`.
2. `.planning/ARCHITECTURE.md`'s Rule A21 — a present-tense architectural invariant about
   `r2000-mcp-client.ts`, a module that no longer exists. Converted to dated past tense on the
   `CORE-01` / `D-36` keep-dated precedent, never deleted.
3. `scripts/check-no-regenerator2000.mjs`'s header — its rule-4 provenance block states a NUL-byte
   offset of 12862 / line 291 for `src/mcp/vice/anno-memmap-render.ts`; measured at plan time the NUL
   is at offset 15097 / line 315. The gate's own `memmap-measurement-provenance` pin at line 79 is
   still correct and passes today; only the descriptive offsets drifted.

Ordering: this plan runs in wave 2 and plan 32-04's widening runs in wave 3. Widening
`docs-linerefs.test.ts` onto an unrepaired `PROJECT.md` makes the guard red on landing, and ordering
constraint 5 / the `4f048bb` precedent then forbids recording anything green (research Pitfall 3).

`D-11` is settled and must NOT be re-derived: `CLAUDE.md` measures 0 and 0 and needs no work in this
phase; there is no `ARCHITECTURE.md` outside `.planning/`.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-CONTEXT.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-RESEARCH.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair PROJECT.md's Architecture bullet and its D-36 row — the two live pointers</name>
  <reversibility rating="costly">D-10: once plan 32-04 widens the guard onto this file, PROJECT.md reds CI whenever vice-proxy.ts shifts. That is the intended pressure, but it is new pressure on a file edited every milestone. Carried forward from CONTEXT.md, not re-derived.</reversibility>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - `.planning/PROJECT.md` line 311 in full — the Architecture constraint bullet carrying all four citations, the `r2000_*` pointer AND its own self-recorded drift history
    - `.planning/PROJECT.md` line 396 in full — the D-36 Key Decisions row, whose status cell ends "pinned by the committed guard `docs-r2000-decisions.test.ts`"
    - `CLAUDE.md` line 26 — the already-corrected twin of the same bullet; it is the target wording
    - `src/mcp/vice/vice-proxy.ts` — confirm each of the four lines by reading them: `:3050` must be the `rewriteArguments(args, name)` call, `:2985` the `forwardToVice` declaration, `:1529` the `rewriteArguments({ path: screenshotContainerPath }, ...)` call, `:1505` the `gatherWedgeEvidence` declaration
    - `src/mcp/vice/docs-absorbed-decisions.test.ts` — `:33` (`PROJECT_MD`), `:121-126` (`d36Rows` uniqueness), `:198-212` (test 4: the row must state "supersedes D-32", name `handler.rs:1894`, name `issues/42`, and carry a named reversal-trigger phrase), `:216-227` (test 5: every line naming D-32 also names D-36 or "supersede"). The edit must preserve all of these
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-RESEARCH.md` §5.3 and §6.1-6.2
  </read_first>
  <action>
Three edits to `.planning/PROJECT.md`, all scoped, none a whole-file rewrite.

**Edit 1 — the four line citations at `:311`, mechanically.** `3052` -> `3050`, `2987` -> `2985`,
`1531` -> `1529`, `1507` -> `1505`. Every one is stale by exactly +2, which is precisely the −2 shift
CLAUDE.md:26 records for plan 29-10 ("removed the retired analyser's session-close import and the
comment block above its call site"). Only two of the four are written in the full `vice-proxy.ts:N`
form (`:3052` and `:1531`); the other two are the bare `:2987` / `:1507` shorthand and are NOT matched
by `docs-linerefs.test.ts`'s `CITATION_RE`. Repair all four regardless — the guard checks two, the
reader reads four.

VERIFY each target line before writing, by reading `src/mcp/vice/vice-proxy.ts` at that line. All four
were correct at research time and at plan time, but CLAUDE.md's own standing note says these numbers
drift every phase and must be re-verified, never assumed. If a number has moved again, use the
measured value and say so in the SUMMARY.

**Edit 2 — the live pointer on the same line.** Replace the clause naming the `r2000_*` family with
the `anno_*` family, matching CLAUDE.md:26's wording exactly: the family is registered through
`buildViceTool()` and never reaches `forwardToVice()`, so the constraint is satisfied by construction
for that family rather than by an interception. Keep the parenthetical provenance and the
self-recorded drift history in the same bullet byte-identical — that text is dated and is `D-08`
class 1. This is the correction research §5.3 adjudicated as a live pointer at a deleted tool family.

**Edit 3 — the D-36 row's stale guard file name at `:396`.** Its status cell ends "pinned by the
committed guard `docs-r2000-decisions.test.ts`". That file does not exist on disk; plan 29-05 renamed
it to `docs-absorbed-decisions.test.ts` in the same commit as `scripts/audit-gate.mjs`'s registry
entry, per `D-12`. Replace the file name and change nothing else in the row. The row must still state
"supersedes D-32", name `handler.rs:1894`, name `issues/42`, and carry a reversal-trigger phrase —
`docs-absorbed-decisions.test.ts` asserts all four at `:204-212` and asserts the D-36 row is unique at
`:126`. `D-36` stays as dated history with an explicit superseded-by fate; do not delete it, do not
restate its verdict.

**Adjudicate, do not sweep.** `.planning/PROJECT.md` carries 17 lines with `r2000_` (23 occurrences)
and 46 lines with the full subject literal (51 occurrences), measured at plan time with `grep -a`.
Apply `D-08`'s rule to each of the 17 `r2000_` lines: a sentence that tells a reader to use, install or
invoke a deleted route, or that asserts in the PRESENT TENSE that a deleted surface exists, is
corrected; a sentence that is dated, or explicitly scoped to a named past milestone, phase or decision,
stays BYTE-IDENTICAL. Most will be keep — lines 24, 91, 159, 198, 223, 381, 384, 396, 465, 668, 715,
1254 and 1270 all sit inside milestone-scoped or decision-scoped records. Record the verdict for every
one of the 17 in the SUMMARY, with the rule clause that decided it; plan 32-05 rolls those verdicts
into the sweep ledger. `D-09` exists so a later reader can tell "we looked and it is fine" from "we
never looked", and CONTEXT.md names this file as exactly where a silent omission would bite.

Rewriting a dated record is the one move this project's convention treats as destroying evidence
rather than tidying it. When a line is genuinely ambiguous, KEEP it and record the ambiguity.

`.planning/` is excluded from the removal gate's scope by PREFIX at
`scripts/check-no-regenerator2000.mjs:180`, so none of these edits can move an exact-count exemption.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -aoc 'docs-r2000-decisions' .planning/PROJECT.md | grep -qx 0 && grep -aq 'vice-proxy.ts:3050' .planning/PROJECT.md && grep -aq 'vice-proxy.ts:1529' .planning/PROJECT.md && cd src/mcp/vice && node --test docs-absorbed-decisions.test.ts docs-dangling-refs.test.ts docs-linerefs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -aoc 'vice-proxy.ts:3052' .planning/PROJECT.md` == 0 and `grep -aoc 'vice-proxy.ts:1531' .planning/PROJECT.md` == 0; `grep -ac 'vice-proxy.ts:3050' .planning/PROJECT.md` >= 1 and `grep -ac 'vice-proxy.ts:1529' .planning/PROJECT.md` >= 1.
    - The bare shorthand citations on the same line read `:2985` and `:1505`; `grep -ac ':2987' .planning/PROJECT.md` == 0 and `grep -ac ':1507' .planning/PROJECT.md` == 0.
    - Reading `src/mcp/vice/vice-proxy.ts` at 3050, 2985, 1529 and 1505 yields, respectively, a `rewriteArguments(` call, an `async function forwardToVice` declaration, a `rewriteArguments(` call, and an `async function gatherWedgeEvidence` declaration.
    - `.planning/PROJECT.md:311` names the `anno_*` family and no longer names an `r2000_*` family as a currently-registered surface; the bullet's dated parenthetical and its self-recorded drift history are byte-identical to before (`git diff .planning/PROJECT.md` shows changes only inside the intended clauses).
    - `grep -ac 'docs-r2000-decisions' .planning/PROJECT.md` == 0.
    - `cd src/mcp/vice && node --test docs-absorbed-decisions.test.ts` exits 0 — all five of its tests, including the D-36 uniqueness and content assertions.
    - `cd src/mcp/vice && node --test docs-dangling-refs.test.ts docs-linerefs.test.ts` exits 0.
    - `node scripts/check-no-regenerator2000.mjs` exits 0.
    - The SUMMARY lists all 17 `r2000_`-carrying `.planning/PROJECT.md` line numbers with a `correct` or `keep` verdict and the `D-08` clause that decided each.
  </acceptance_criteria>
  <done>PROJECT.md's Architecture bullet cites four verified line numbers and the surviving tool family, its D-36 row names a guard that exists, and every `r2000_`-carrying line in the file has an adjudicated verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Convert ARCHITECTURE.md's Rule A21 to dated, superseded prose without rewriting history</name>
  <files>.planning/ARCHITECTURE.md</files>
  <read_first>
    - `.planning/ARCHITECTURE.md` lines 225-245 — Rule A21 in full: its heading, its present-tense body, and its citations of `D18-04`, `D18-03`, `D18-21`, `D18-02` and `r2000-mcp-client.ts`
    - `.planning/ARCHITECTURE.md` — the rule immediately before A21 and the "Dependency Direction" section immediately after, so the conversion keeps the document's own structural conventions
    - `.planning/ROADMAP.md` § Phase 32 criterion 2 — the sentence naming "`ARCHITECTURE.md`'s Rule A21"
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-RESEARCH.md` §5.3 — the A21 finding, the four-`ARCHITECTURE.md` inventory, and the recommendation to convert to dated past tense rather than delete
    - `.planning/PROJECT.md` → Core Value — the `CORE-01` keep-dated precedent this conversion follows (the statement left byte-identical, with a dated record of the evidence weighed and a stated reversal condition beside it)
  </read_first>
  <action>
Rule A21 states a present-tense architectural invariant — "At most one live child exists per
`vice-proxy.ts` process", "It is never spawned from a module other than `r2000-mcp-client.ts`" —
about a module that no longer exists on disk. It reads as live and is not.

Convert it to a dated record, on the `CORE-01` / `D-36` keep-dated precedent:
  - Keep the `### Rule A21` heading and its number so every existing cross-reference still resolves.
    `.planning/ROADMAP.md` § Phase 32 criterion 2 names it by number.
  - Mark it superseded in the heading or the first line: superseded by the Phase 29 deletion, with the
    date, so a reader sees the status before the body.
  - Restate the body in the PAST tense, keeping every `D18-*` citation and the `r2000-mcp-client.ts`
    reference intact — those are the provenance, and a dated record that drops its citations is worth
    less than the stale rule it replaced.
  - State what replaced it or that nothing did. A21 governed a child process whose whole integration
    was removed; say so, and name Phase 29 as the removing phase.
  - Add a reversal condition in the same idiom the rest of this project's dated decisions use, so the
    record is a decision rather than a tombstone.

Change no other rule in the file and do not renumber anything. Verify with `git diff` that the
changed hunk is confined to the A21 block.

The `.planning/research/ARCHITECTURE.md` copy also carries an A21 — leave it BYTE-IDENTICAL. It is a
research archive, dated by construction. It gets a recorded verdict row in plan 32-05's ledger, not an
edit; that distinction is exactly what `D-09` exists to make visible. Record in the SUMMARY that all
four `ARCHITECTURE.md` files live inside `.planning/` (`.planning/ARCHITECTURE.md`,
`.planning/codebase/ARCHITECTURE.md`, `.planning/research/ARCHITECTURE.md`,
`.planning/research/archive-v0.6.0/ARCHITECTURE.md`), that A21 appears in two of them, and that
ROADMAP's "Rule A21" reference resolves to `.planning/ARCHITECTURE.md` — confirming `D-11` rather than
re-deriving it.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && git diff --stat -- .planning/research/ARCHITECTURE.md | grep -q . && exit 1; grep -aq 'A21' .planning/ARCHITECTURE.md && cd src/mcp/vice && node --test docs-dangling-refs.test.ts docs-absorbed-decisions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ARCHITECTURE.md` still contains a `### Rule A21` heading (the number is unchanged and every cross-reference still resolves).
    - The A21 body contains no present-tense assertion that a live child process exists; it names Phase 29 as the removing phase and carries a date.
    - All five original citations survive in the converted block: `D18-04`, `D18-03`, `D18-21`, `D18-02` and `r2000-mcp-client.ts`.
    - `git diff -- .planning/ARCHITECTURE.md` shows changes confined to the A21 block; no other rule heading, body or number appears in the diff.
    - `git diff --exit-code -- .planning/research/ARCHITECTURE.md` exits 0 — the research copy is byte-identical.
    - `cd src/mcp/vice && node --test docs-dangling-refs.test.ts docs-absorbed-decisions.test.ts` exits 0.
    - The SUMMARY names all four `ARCHITECTURE.md` paths, states which two carry A21, and confirms there is no `ARCHITECTURE.md` outside `.planning/` (`D-11`).
  </acceptance_criteria>
  <done>Rule A21 is a dated, superseded record with its citations and its number intact, the research copy is untouched, and no other rule in the document moved.</done>
</task>

<task type="auto">
  <name>Task 3: Repair the removal gate's header drift — the NUL-byte offset and the tree-wide totals</name>
  <files>scripts/check-no-regenerator2000.mjs</files>
  <read_first>
    - `scripts/check-no-regenerator2000.mjs` — header rule 4 at `:64-75` (the NUL-byte provenance block claiming offset 12862 / line 291 and tree-wide totals of 399 with `-a` and 398 without), and the in-body comment at `:819-820` repeating `anno-memmap-render.ts:12862`
    - `scripts/check-no-regenerator2000.mjs` — the `memmap-measurement-provenance` exemption at `:414-418`, which pins the mention at exactly 1 occurrence at line 79
    - `src/mcp/vice/anno-memmap-render.ts` — line 79 (the pinned provenance comment) and the byte region around the NUL separator
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-RESEARCH.md` §5.1 — the measured NUL position and the documentation-drift flag
    - `CLAUDE.md` line 26 — the idiom this project uses for a drifting numeric citation ("treat a mismatch as drift to re-verify, not as evidence the constraint itself changed"), which the repaired header should mirror
  </read_first>
  <action>
The gate's rule-4 provenance block carries a live-tense false number. Measured at plan time:
`src/mcp/vice/anno-memmap-render.ts` is 31604 bytes with 2 NUL bytes, the first at offset **15097**,
which is **line 315**. The header says offset 12862, line 291. The gate's own
`memmap-measurement-provenance` pin at line 79 is still correct and the gate exits 0 today — only the
descriptive offsets drifted as the file grew.

Repair both sites:
  - the header block at `:64-75`
  - the in-body comment at `:819-820`, which repeats `anno-memmap-render.ts:12862`

Re-measure rather than copying the figures above: read the file's bytes and compute the first NUL's
offset and its 1-based line. Write the measured values with the measurement date, and add the drift
clause in `CLAUDE.md:26`'s idiom — the offset moves as the file grows, and a mismatch is drift to
re-verify, not evidence that rule 4 stopped applying. That is what stops this same repair being owed
again next phase without a reader knowing whether the rule changed.

The tree-wide occurrence totals in the same block ("399 with `-a` and 398 without") are stale for two
reasons: they were measured at plan 29-02, BEFORE the deletion removed most of the tree's mentions,
and they were measured over the gate's own scope (tracked files minus the `.planning/` prefix, plus
the shipped `installer/**` paths supplied by `packFiles()`), which is not the same set as a plain
`git ls-files` sweep. Measured at plan time over tracked-minus-`.planning/` alone: 141 with `-a` and
140 without. Do NOT simply substitute that pair — it is a different scope from the one the sentence
describes. Instead do one of these two, and say which:
  (a) re-measure in the gate's OWN scope by running the gate and reading the measured counts from its
      success line, then state those figures with the date and the scope definition; or
  (b) attribute the existing pair explicitly to plan 29-02 and its pre-deletion tree, so the sentence
      stops reading as a current fact.
Either is defensible; leaving a live-tense false number is not. Keep the one-hit-difference argument
intact — it is the load-bearing point of rule 4, and the provenance comment it names is still at
`src/mcp/vice/anno-memmap-render.ts:79`. Verify that line still carries the mention before relying on it.

Change no exemption, no pinned count, no scope predicate, no assertion. This is a comment-only edit to
the file's prose. The gate carries ZERO contiguous occurrences of the subject literal today (it
composes every one from `SUBJECT_NEEDLE`), so a prose edit cannot move any exact-count pin — but
confirm that by re-running the gate.

`D-07`'s principle applies: this edit to an audited guard lands in wave 2, before wave 4 measures that
guard. Do not touch this file again after this task.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -c '12862' scripts/check-no-regenerator2000.mjs | grep -qx 0 && node scripts/check-no-regenerator2000.mjs && cd src/mcp/vice && node --test removal-gate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '12862' scripts/check-no-regenerator2000.mjs` == 0 and `grep -c 'line 291' scripts/check-no-regenerator2000.mjs` == 0 — both stale sites repaired, including the in-body comment near `:819`.
    - The header states an offset and a line that reproduce: reading `src/mcp/vice/anno-memmap-render.ts`'s bytes yields a first NUL at exactly the stated offset, and that offset's 1-based line equals the stated line.
    - The header carries a measurement date and a drift clause in `CLAUDE.md:26`'s idiom, so a future mismatch reads as drift rather than as a changed rule.
    - The tree-wide occurrence sentence either carries re-measured figures WITH their scope definition and date, or is explicitly attributed to plan 29-02 and its pre-deletion tree. `grep -c '399 with' scripts/check-no-regenerator2000.mjs` == 0 unless that sentence now carries an explicit "measured at plan 29-02" attribution.
    - `git diff -- scripts/check-no-regenerator2000.mjs` touches only comment lines: no exemption id, no `hits:`/`lines:`/`prefixHits:` value, no `need(` call and no predicate appears in the diff.
    - `node scripts/check-no-regenerator2000.mjs` exits 0.
    - `cd src/mcp/vice && node --test removal-gate.test.ts` exits 0.
    - `grep -ac 'regenerator2000' scripts/check-no-regenerator2000.mjs` == 0 — the file still composes every occurrence rather than typing it.
  </acceptance_criteria>
  <done>The removal gate's header no longer states a currently-false byte offset or a currently-false tree-wide total, its pins and predicates are untouched, and it still exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edited planning documents -> guards that read them | `docs-absorbed-decisions.test.ts`, `docs-dangling-refs.test.ts` and (from wave 3) `docs-linerefs.test.ts` parse `.planning/PROJECT.md`; an edit crosses into their assertions |
| edited guard header -> the guard's own exact-count exemptions | A prose edit inside an in-scope file could move a pinned occurrence count |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-10 | Tampering | `.planning/PROJECT.md` D-36 row | high | mitigate | The edit changes only the trailing guard file name; `docs-absorbed-decisions.test.ts:198-212` asserts "supersedes D-32", `handler.rs:1894`, `issues/42` and a reversal-trigger phrase, and `:126` asserts row uniqueness — the guard is run in this task's verify, not assumed |
| T-32-11 | Repudiation | rewriting dated records while correcting live pointers | high | mitigate | `D-08`'s clause is applied per line and the verdict for every one of the 17 `r2000_` lines is recorded; `git diff` is asserted confined to the intended clauses; ambiguity resolves to KEEP |
| T-32-12 | Tampering | comment edit inside `scripts/check-no-regenerator2000.mjs` | medium | mitigate | The diff is asserted to touch comment lines only — no exemption id, pinned count, predicate or `need()` call in the hunk — and both the gate and `removal-gate.test.ts` are run afterwards |
| T-32-13 | Information disclosure | none — no secrets, credentials or PII appear in any file this plan edits | low | accept | The three files carry project documentation and a CI guard's comments; no secret material is read or written |
| T-32-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no packages and invokes no package manager |
</threat_model>

<verification>
- `cd src/mcp/vice && node --test docs-absorbed-decisions.test.ts docs-dangling-refs.test.ts docs-linerefs.test.ts removal-gate.test.ts` exits 0.
- `node scripts/check-no-regenerator2000.mjs` exits 0.
- `node scripts/audit-gate.mjs --json` reports `allowed: true`, `redGuards: []`, 9 derived docs guards.
- `cd src/mcp/vice && npm run test:automated` exits 0 with 0 failures.
- `git diff --exit-code -- .planning/research/ARCHITECTURE.md` exits 0.
</verification>

## Artifacts this phase produces

See the table in `32-01-PLAN.md` → "Artifacts this phase produces" — the canonical list of every
symbol, file and CI step Phase 32 creates. This plan creates no new symbol; it modifies
`.planning/PROJECT.md`, `.planning/ARCHITECTURE.md` and `scripts/check-no-regenerator2000.mjs` in
place.

<success_criteria>
- Every citation in `PROJECT.md`'s Architecture bullet resolves to a real `rewriteArguments()` call site or function declaration in `vice-proxy.ts`, verified in the same run.
- No living document names the deleted `r2000_*` family as a currently-registered surface, and no document names `docs-r2000-decisions.test.ts`.
- Rule A21 is dated and superseded rather than present-tense, with its citations and number intact.
- The removal gate's header carries no currently-false number, and the gate is still green.
- Every `r2000_`-carrying line in `PROJECT.md` has an explicit recorded verdict.
</success_criteria>

<output>
Create `.planning/phases/32-the-deletion-and-the-grep-gate/32-03-SUMMARY.md` when done
</output>
