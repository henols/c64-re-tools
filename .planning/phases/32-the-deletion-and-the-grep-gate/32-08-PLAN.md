---
phase: 32-the-deletion-and-the-grep-gate
plan: 08
type: execute
wave: 6
depends_on: ["32-07"]
files_modified:
  - .planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json
  - .planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-deferred-fates.md
autonomous: true
requirements: [CUT-04]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All 6 `deleted` rows carry `newSubject: null`, a proof the historical path AND its `anno-` name-descendant are both absent from the working tree, and a `removingCommit` that `git cat-file -t` resolves — the evidence D-05 requires of that verdict."
    - "All 15 set-B rows carry a verdict, a `removalTrigger` and a proof of existence; where a set-B guard's plant is cheap, an `observedRed` is recorded as STRENGTHENING and labelled as such in the row's `note`, never as an owed contract."
    - "Both deferred fates are recorded against their NEW removal triggers, not against their original ones: `block-class.ts`'s capitalised block-type arm against the twelve `project.regen2000proj` fixtures being re-spelled, and `make-coverage-fixtures.mjs`'s frozen writer against being re-pointed onto the Phase 28 store. The registry states that the two rows share one subject and discharge together."
    - "`node scripts/check-guard-fates.mjs` exits 0: 60 derived members, 60 rows, every floor met, every per-verdict evidence rule satisfied. The pre-declared red from plan 32-01 is discharged HERE, in the commit that completes the registry — not laundered by loosening a rule."
    - "No re-point of either deferred fate is performed. ROADMAP.md is explicit that this phase records the fate against the new trigger and does not perform the re-point; the twelve fixtures are left byte-identical."
  artifacts:
    - path: ".planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-deferred-fates.md"
      provides: "The two deferred guard fates, their new removal triggers, and the shared-subject relation between them"
      contains: "regen2000proj"
      min_lines: 60
  key_links:
    - from: ".planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json"
      to: ".planning/ROADMAP.md"
      via: "set C is parsed from ROADMAP § Phase 32's deferred-fates note; the two rows are the note's two subjects"
      pattern: "block-class|make-coverage-fixtures"
    - from: ".planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json"
      to: ".planning/phases/29-the-mcp-surface/29-21-SUMMARY.md"
      via: "every deleted row's removingCommit is sourced from the commit-level record at :176-259"
      pattern: "removingCommit"
---

<!-- planner-discipline-allow: regenerator2000 -->
<!-- planner-discipline-allow: r2000 -->

<objective>
Complete the registry and take the fate guard green.

Three groups remain after the two sweep plans:

1. **The 6 members gone outright** — `src/mcp/vice/r2000-launch.test.ts`, `r2000-mcp-client.test.ts`,
   `r2000-project.test.ts`, `r2000-session.test.ts`, `r2000-symbol-roundtrip.test.ts`,
   `r2000-verify.test.ts`. Verdict `deleted`; `D-05` says each owes proof of absence plus the commit
   that removed it.
2. **The 15 net-new set-B members** — guards Phase 29 created as part of the re-pointing, which the
   requirement's own text puts squarely inside `CUT-04`'s scope ("Phase 29 is the source of most of
   the guards it will audit").
3. **The two set-C deferred fates** — `src/mcp/vice/block-class.ts`'s transitional capitalised
   block-type arm, and `src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs`'s frozen writer.
   ROADMAP.md names them here so the recorded-fate criterion picks them up "instead of them sitting
   inert", and is explicit that this phase **records the fate against the NEW trigger and does not
   perform the re-point**. Both hinge on the same subject — the twelve committed
   `project.regen2000proj` coverage fixtures — so they are one decision taken twice, not two.

This plan discharges plan 32-01's PRE-DECLARED RED. `scripts/check-guard-fates.mjs` has been
deliberately red since it landed, because a registry cannot be complete before the sweep that fills
it. It goes green here, in the commit that completes the registry — by satisfying every rule, never
by loosening one. Its CI step lands in plan 32-09, the wave after, so nothing is ever wired into CI
while red (ordering constraint 5 / the `4f048bb` precedent).

**Re-derive before you trust those three lists.** Run `node scripts/check-guard-fates.mjs` FIRST and
treat its derivation as authoritative over the hand-typed enumerations above — they are a
planning-time reading of the two pinned commits and the ROADMAP note, and `D-01` makes the derivation
(and the guard's bidirectional bijection check, under which a stranger row fails) the authority. If
HEAD has moved since planning and the derivation differs, follow the derivation, record the difference
in `evidence/32-deferred-fates.md` and in the SUMMARY, and never edit a floor or a list to make the
planning-time reading win.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-CONTEXT.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-RESEARCH.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-07-SUMMARY.md
@.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md
@.planning/phases/29-the-mcp-surface/29-21-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The six deleted rows and the fifteen set-B rows</name>
  <files>.planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json</files>
  <read_first>
    - `.planning/phases/29-the-mcp-surface/29-21-SUMMARY.md` — `:176-259`, which enumerates the removals by name and is the commit-level record `D-05` asks for. Take each `removingCommit` from there, not from a fresh `git log` guess
    - `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md` — the 6 gone-outright names and the 15 net-new set-B names, both enumerated with their derivation commands
    - The set-B members, each read for its verdict and its removal trigger: `scripts/check-no-regenerator2000.mjs` and `scripts/check-no-regenerator2000.d.mts`, `scripts/lib/anno-cli-invocations.mjs`, `src/mcp/vice/acme-gate.test.ts`, `anno-index.test.ts`, `anno-seam.test.ts`, `anno-store.test.ts`, `anno-types.test.ts`, `block-class.test.ts`, `module-classification.test.ts`, `prg-image.test.ts`, `removal-gate.test.ts`, `shipped-modules.test.ts`, and the two `src/mcp/vice/fixtures/planted-removal-fixture.{ts,md}.txt`
    - `src/mcp/vice/removal-gate.test.ts` — `:32-75`, so its row's removal trigger names the condition under which the removal gate itself would legitimately retire
    - `.planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json` — the row schema established by plans 32-01, 32-06 and 32-07
  </read_first>
  <action>
**The six `deleted` rows.** For each of `src/mcp/vice/r2000-launch.test.ts`,
`r2000-mcp-client.test.ts`, `r2000-project.test.ts`, `r2000-session.test.ts`,
`r2000-symbol-roundtrip.test.ts` and `r2000-verify.test.ts`, write a row with:
  - `verdict: "deleted"`, `newSubject: null`;
  - a proof-of-absence recording BOTH that the historical path is absent from the working tree AND
    that its `r2000-` -> `anno-` name-descendant is absent (research verified none of
    `anno-launch.test.ts`, `anno-mcp-client.test.ts`, `anno-project.test.ts`, `anno-session.test.ts`,
    `anno-symbol-roundtrip.test.ts`, `anno-verify.test.ts` exists). Recording only the first would
    leave "deleted" indistinguishable from "renamed and we did not look";
  - `removingCommit`, taken from `29-21-SUMMARY.md:176-259`'s enumeration, verified resolvable with
    `git cat-file -t`;
  - no `observedRed` (`D-05`: `deleted` owes absence plus the commit, not a red);
  - `removalTrigger` is not owed on a `deleted` row — the guard does not require it — but record a
    `note` naming what the file used to assert, so a later reader knows what coverage went away.

**The fifteen set-B rows.** These guards were CREATED by Phase 29 as part of the re-pointing rather
than moved by it, so their subject never migrated. Classify each mechanically: `re-pointed` if the
guard's assertions were subsequently re-aimed at a different subject between its creation commit and
`AUDIT_END`; `kept-unchanged` otherwise, with `newSubject === historicalPath`. Expect most to be
`kept-unchanged`. For each:
  - a `removalTrigger` naming the condition under which the guard would legitimately be removed. For
    `removal-gate.test.ts` and the removal gate itself that is the gate's own retirement; for the two
    `planted-removal-fixture` files it is the gate that owns them being retired; for the `anno-*`
    store guards it is the corresponding capability being removed;
  - a proof of existence: the path is on the working tree, plus the creation commit from
    `git log --diff-filter=A --format=%h -1 -- <path>`;
  - no `observedRed` is owed on `kept-unchanged`. Where a guard's plant is cheap and its green control
    is available, an `observedRed` MAY be recorded as STRENGTHENING — the guard's schema accepts but
    does not require it on this verdict. Label it as strengthening in the `note` so a later reader
    does not read it as a contract.

Record in each set-B row's `note` the reason it is in the audited set at all: it matched the same
subject predicate set A uses, applied to the added-file delta between the two pinned commits. Where a
research-listed candidate is NOT in the set — `scripts/check-skill-cli-invocations.mjs` and
`scripts/lib/anno-cli-invocations.d.mts`, neither of whose path nor content matches the predicate —
the reconciliation document already carries the reason; do not add a row for it, and do not widen the
predicate to reach it. Widening it by hand is exactly the hand-typed second list `D-12-07` forbids.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node -e 'const r=require("./.planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json");const d=r.rows.filter(x=>x.verdict==="deleted");if(d.length!==6){console.error("deleted rows:",d.length);process.exit(1)}for(const x of d){if(x.newSubject!==null||!x.removingCommit)process.exit(1)}console.log("deleted ok",d.length,"total rows",r.rows.length)' && for c in $(node -e 'const r=require("./.planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json");console.log(r.rows.filter(x=>x.removingCommit).map(x=>x.removingCommit).join(" "))'); do git cat-file -t "$c" >/dev/null || exit 1; done && echo DELETED_AND_SETB_OK</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 6 rows carry `verdict: "deleted"`, each with `newSubject: null` and a `removingCommit` that `git cat-file -t` resolves to `commit`.
    - Each `deleted` row records absence of BOTH the historical path and its `anno-` name-descendant; for each of the six, `test -f` on both paths fails.
    - Exactly 15 rows cover the set-B members, each with a verdict, a non-empty `removalTrigger`, an existence proof including the creation commit, and a `note` stating the predicate clause that put it in the set.
    - Any `observedRed` on a `kept-unchanged` row is labelled `strengthening` in that row's `note`.
    - No row exists for `scripts/check-skill-cli-invocations.mjs` or `scripts/lib/anno-cli-invocations.d.mts`, and the derivation predicate in `scripts/check-guard-fates.mjs` is byte-identical to what plan 32-01 committed (`git diff --exit-code -- scripts/check-guard-fates.mjs` exits 0).
    - `node scripts/check-no-regenerator2000.mjs` exits 0 and `cd src/mcp/vice && npm run test:automated` exits 0 with 0 failures.
  </acceptance_criteria>
  <done>The six removals are recorded with a resolvable removing commit and a two-sided proof of absence, and every set-B guard carries a verdict, a removal trigger and a stated reason for membership.</done>
</task>

<task type="auto">
  <name>Task 2: The two deferred fates, recorded against their NEW removal triggers</name>
  <files>.planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json, .planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-deferred-fates.md</files>
  <read_first>
    - `.planning/ROADMAP.md` § Phase 32 — the note beginning "Two guard fates were DEFERRED", in full: it states both subjects, both reasons, and that this phase records the fate rather than performing the re-point
    - `src/mcp/vice/block-class.ts` — `:171` (the TRANSITIONAL comment naming the capitalised vocabulary and its absent producer), `:180` ("Deleting the two arms ..."), and `:195` (the live `block.type === "Code"` arm)
    - One of the twelve `src/mcp/vice/fixtures/coverage/*/project.regen2000proj` files — READ IT and confirm whether its block types are actually spelled `"Code"` / `"Byte"` / `"Undefined"`. Research marks this `NOT MEASURED — planner must verify` (assumption A7); if they are already lowercase, `block-class.ts`'s new trigger is already satisfied and the row's verdict changes
    - `src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs` — `:43` (the never-hand-assembled-JSON header line), `:67-97` (the FROZEN PROJECT-FILE WRITER block and the freeze instruction), `:121` (`synthesizeProject()`), `:943` (its call site writing `project.regen2000proj`)
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-RESEARCH.md` §8.1 and §8.2 — the two candidate rows with their field values
  </read_first>
  <action>
Add the two set-C rows and write the evidence document that explains them.

**Row: `src/mcp/vice/block-class.ts`.**
  - `verdict: "kept-unchanged"`, `newSubject: "src/mcp/vice/block-class.ts"`.
  - Proof its subject still exists untouched: the arm at `:195` is present, and
    `git ls-files 'src/mcp/vice/fixtures/coverage/*/project.regen2000proj'` returns exactly 12 files.
    RE-MEASURE the count; do not copy it.
  - **NEW removal trigger** — the twelve fixtures being re-spelled in the lowercase (`code`/`data`)
    vocabulary. Until then, deleting the capitalised arms silently reclassifies every fixture block as
    `data`. State explicitly that the ORIGINAL trigger — the producer's absence — is already satisfied
    and is NOT sufficient. That sentence is the whole point of `D-03`'s fourth field: without it these
    two read as unexplained survivors rather than deliberate ones.
  - `observedRed` is not owed on `kept-unchanged`.
  - **Verify research assumption A7 before writing the row.** Read one fixture and record whether its
    block types are capitalised. If they are already lowercase, the new trigger is already satisfied,
    the verdict changes, and that is a finding to surface in the SUMMARY — not something to write
    around.

**Row: `src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs`.**
  - `verdict: "kept-unchanged"`, `newSubject: "src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs"`.
  - Proof its subject still exists untouched: `synthesizeProject()` is present at `:121` and called at
    `:943`; the freeze block at `:67-97` is present. Re-verify the line numbers and record the measured
    values — this project's line citations drift every phase.
  - **NEW removal trigger** — the writer being re-pointed onto the Phase 28 annotation store. Doing so
    re-derives all twelve fixtures and changes what the census controls measure, so it is later work on
    later evidence. Until then the twelve committed `project.regen2000proj` files are the only remaining
    record of the format, as the file's own header states.
  - `observedRed` is not owed.

**The evidence document** `evidence/32-deferred-fates.md` states, with citations:
  - why the two rows share ONE subject — the twelve fixtures — and therefore discharge together, which
    is why ROADMAP calls them "one decision taken twice, not two". A later reader re-spelling the
    fixtures must know both rows fall at once;
  - the measured fixture count and the measured block-type spelling (assumption A7's resolution);
  - that the fixture filename is `project.regen2000proj` — `regen2000`, NOT the full subject literal —
    which is why twelve committed files carrying a retired producer's extension survive a gate that
    scans paths as well as content. That is a real gap in coverage and belongs in the record;
  - that NO re-point was performed by this phase, and that the twelve fixtures are byte-identical.

Do not perform either re-point. Do not remove `block-class.ts`'s capitalised arms. Do not re-point
`synthesizeProject()`. ROADMAP.md is explicit, and CONTEXT.md lists both as deferred ideas.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && test $(git ls-files 'src/mcp/vice/fixtures/coverage/*/project.regen2000proj' | wc -l) -eq 12 && git diff --exit-code -- src/mcp/vice/fixtures/coverage/ src/mcp/vice/block-class.ts && node -e 'const r=require("./.planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json");const c=r.rows.filter(x=>x.historicalPath==="src/mcp/vice/block-class.ts"||x.historicalPath==="src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs");if(c.length!==2)process.exit(1);for(const x of c){if(!x.removalTrigger||x.removalTrigger.length<40)process.exit(1)}console.log("deferred fates ok")'</automated>
  </verify>
  <acceptance_criteria>
    - `guard-fates.json` contains exactly two rows whose `historicalPath` is `src/mcp/vice/block-class.ts` or `src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs`, each with a `removalTrigger` describing the NEW trigger and explicitly stating that the original trigger is already satisfied and insufficient.
    - `git ls-files 'src/mcp/vice/fixtures/coverage/*/project.regen2000proj' | wc -l` is 12, re-measured in this task and recorded.
    - The evidence document records the measured block-type spelling of at least one fixture, resolving research assumption A7 one way or the other; if the fixtures are already lowercase, the changed verdict and the finding are surfaced in the SUMMARY.
    - The evidence document states that the two rows share one subject and discharge together, and records that `project.regen2000proj` carries `regen2000` rather than the full subject literal — the reason twelve files survive a gate that scans paths.
    - `git diff --exit-code -- src/mcp/vice/fixtures/coverage/ src/mcp/vice/block-class.ts` exits 0 — no re-point was performed and the twelve fixtures are byte-identical.
    - `src/mcp/vice/block-class.ts` still contains the `block.type === "Code"` arm; `grep -c 'block.type === "Code"' src/mcp/vice/block-class.ts` >= 1.
    - `node scripts/check-no-regenerator2000.mjs` exits 0 and `cd src/mcp/vice && npm run test:automated` exits 0 with 0 failures.
  </acceptance_criteria>
  <done>Both deferred fates are recorded against their new triggers with the original trigger explicitly named as insufficient, their shared subject is documented, and neither re-point was performed.</done>
</task>

<task type="auto">
  <name>Task 3: Complete the registry and take the fate guard green</name>
  <files>.planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json</files>
  <read_first>
    - `scripts/check-guard-fates.mjs` — every rule it enforces: the bijection in both directions, the duplicate checks, the four floors, the empty-set and empty-registry failures, and the per-verdict evidence rules
    - `.planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json` — the full registry as assembled by plans 32-01, 32-06, 32-07 and this plan's first two tasks
    - `scripts/audit-gate.mjs` — `:96-112`, the `DOCS_GUARD_FLOOR` comment block, for the discipline a floor change owes if any floor turns out to be wrong
    - `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md` — the authoritative 43 / 15 / 2 = 60 arithmetic
  </read_first>
  <action>
Run `node scripts/check-guard-fates.mjs` and drive it to exit 0 by SATISFYING every rule.

Work the guard's own error output as the checklist. Expected residue after the first two tasks:
missing rows, rows failing a per-verdict evidence rule, and any duplicate `newSubject` introduced by
two rows naming the same replacement.

**The one thing you may not do is loosen a rule to reach green.** No floor may be lowered, no
per-verdict rule relaxed, no exemption added, no waiver file, no environment override, no skip flag
(`audit-gate.mjs` `D-12-14`; this repo's gate design deliberately carries no relaxation hatches). If a
FLOOR turns out to be genuinely wrong — the derivation legitimately yields a different count because
HEAD moved — change it only with the same-commit justification `DOCS_GUARD_FLOOR`'s comment demands,
recording the re-derivation command and the reason in the same commit, and update
`evidence/32-audited-set-reconciliation.md`'s arithmetic to match. A floor moved without that record
is the failure mode this whole phase exists to make impossible.

If a row cannot satisfy its verdict's evidence rule, that is a FINDING about the guard, not a reason to
change the verdict. Surface it. Plans 32-06 and 32-07 already established the discipline: a member that
cannot be red is recorded as a possible vacuous guard, never reclassified.

When the guard exits 0, record in the SUMMARY:
  - the guard's full success line (derived-set size, row count, each floor);
  - the verdict distribution across the 60 rows;
  - that plan 32-01's PRE-DECLARED RED is discharged here, in the commit that completed the registry,
    and by satisfaction rather than by relaxation. That sentence is the audit trail ordering constraint
    5 exists for.

Then re-run the guard's own non-vacuity test and confirm it still bites against the now-complete
registry: `cd src/mcp/vice && node --test guard-fates.test.ts`. A guard that has just gone green must
still be provably able to go red.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-guard-fates.mjs && node -e 'const r=require("./.planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json");console.log("rows",r.rows.length);if(r.rows.length!==60)process.exit(1)' && cd src/mcp/vice && node --test guard-fates.test.ts && npm run test:automated</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-guard-fates.mjs` exits 0 and its success line prints `setA=43`, `setB=15`, `setC=2`, `total=60`, `rows=60` and each floor — the measured numbers, not the word OK.
    - `guard-fates.json` has exactly 60 rows with no duplicate `historicalPath` and no duplicate non-null `newSubject`.
    - Every `re-pointed` and `superseded` row carries a control-gated, machine-captured non-zero `observedRed`; every `deleted` row carries `newSubject: null` and a resolvable `removingCommit`; every `kept-unchanged` row carries `newSubject === historicalPath`, an existence proof and a `removalTrigger`.
    - `git diff --exit-code -- scripts/check-guard-fates.mjs scripts/lib/audit-root.mjs scripts/audit-mutation-harness.mjs` exits 0 unless a floor changed — and if a floor changed, the same commit carries the re-derivation command, the reason, and the matching update to `evidence/32-audited-set-reconciliation.md`.
    - `grep -Ec 'WAIVER|process\.env\.[A-Z_]*(SKIP|ALLOW|FORCE)|--skip|--force' scripts/check-guard-fates.mjs` == 0 — no relaxation hatch was added to reach green.
    - `cd src/mcp/vice && node --test guard-fates.test.ts` exits 0 — the guard is still provably able to go red now that it is green.
    - `cd src/mcp/vice && npm run test:automated` exits 0 with 0 failures; `npm run typecheck` exits 0; `node scripts/audit-gate.mjs --json` reports `allowed: true`, `redGuards: []`.
    - The SUMMARY states that plan 32-01's pre-declared red is discharged here, by satisfaction rather than by relaxation, and gives the verdict distribution across the 60 rows.
  </acceptance_criteria>
  <done>The fate registry is complete, the derive-from-disk guard exits 0 against it without a single rule having been loosened, and the guard's own non-vacuity test still bites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/ROADMAP.md` prose -> set C derivation | A committed document decides two members of the audited set; a reworded note changes the set |
| registry completeness -> the audit's central claim | A green guard asserts every audited member has a recorded, evidenced fate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-33 | Tampering | reaching green by loosening a rule | high | mitigate | No floor may be lowered, no per-verdict rule relaxed, no exemption/waiver/env-override/skip flag added; a legitimate floor change owes the same-commit re-derivation record `DOCS_GUARD_FLOOR`'s comment demands; asserted by a `git diff --exit-code` over the three guard files and a negative grep for hatch vocabulary |
| T-32-34 | Spoofing | a `deleted` row that is really a rename nobody looked for | high | mitigate | Every `deleted` row proves absence of BOTH the historical path and its `anno-` name-descendant, and carries a `removingCommit` verified resolvable with `git cat-file -t` |
| T-32-35 | Repudiation | a set-C member silently dropping out when the ROADMAP note is reworded | medium | mitigate | The set-C parse requires exactly 2 resolved paths and fails loudly naming the note otherwise; the note is never mirrored into a hand-typed array |
| T-32-36 | Tampering | performing a deferred re-point while recording its fate | medium | mitigate | `git diff --exit-code -- src/mcp/vice/fixtures/coverage/ src/mcp/vice/block-class.ts` is asserted to exit 0, and the `block.type === "Code"` arm is asserted still present |
| T-32-37 | Repudiation | strengthening evidence on a `kept-unchanged` row read as an owed contract | low | accept | Any such `observedRed` is labelled `strengthening` in the row's `note`; the guard neither requires nor rejects it, so the risk is a reader's misreading rather than a gate failure |
| T-32-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no packages and invokes no package manager |
</threat_model>

<verification>
- `node scripts/check-guard-fates.mjs` exits 0 with 60 derived members and 60 rows.
- `cd src/mcp/vice && node --test guard-fates.test.ts` exits 0.
- `cd src/mcp/vice && npm run test:automated` exits 0 with 0 failures; `npm run typecheck` exits 0.
- All six existing CI check scripts exit 0; `node scripts/audit-gate.mjs --json` reports `allowed: true`, `redGuards: []`.
- `git diff --exit-code -- src/mcp/vice/fixtures/coverage/ src/mcp/vice/block-class.ts` exits 0.
- `git status --porcelain` over tracked non-`.planning` paths is empty.
</verification>

## Artifacts this phase produces

See the table in `32-01-PLAN.md` → "Artifacts this phase produces" — the canonical list of every
symbol, file and CI step Phase 32 creates. This plan adds
`.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-deferred-fates.md` and completes
`.planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json` to 60 rows.

<success_criteria>
- Every member of the 60-member audited set has exactly one row with a recorded, typed fate.
- The six removals carry a two-sided proof of absence and a resolvable removing commit.
- Both deferred fates are recorded against their new triggers, with the original trigger named as insufficient and neither re-point performed.
- The fate guard exits 0 without a single rule having been loosened, and is still provably able to go red.
- Plan 32-01's pre-declared red is discharged by satisfaction, and the SUMMARY says so.
</success_criteria>

<output>
Create `.planning/phases/32-the-deletion-and-the-grep-gate/32-08-SUMMARY.md` when done
</output>
