---
phase: 32-the-deletion-and-the-grep-gate
plan: 05
type: execute
wave: 3
depends_on: ["32-03"]
files_modified:
  - .planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-document-sweep.md
autonomous: true
requirements: [CUT-06]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "[edge:CUT-06/adjacency] A file that qualifies through BOTH the tracked-census predicate and the named-living-document list gets EXACTLY ONE verdict row, keyed on its repo-relative path; and a document whose post-sweep bytes are identical to its pre-sweep bytes is recorded `unchanged`, never `corrected` — the two states are distinguished by `git diff --exit-code`, not by narration."
    - "[edge:CUT-06/encoding] Every published count states its unit — LINES containing a match versus OCCURRENCES of a match — and every count is produced by a byte-level scan that survives NUL bytes. `src/mcp/vice/anno-memmap-render.ts` carries a NUL at offset 15097 (line 315), so a plain `grep` yields 34 tracked non-`.planning` files where `grep -a` yields 35; the ledger uses and names the `-a` definition."
    - "Every file in the defined swept set has a row, including every file left unchanged, so a later reader can tell `we looked and it is fine` from `we never looked` (D-09)."
    - "The three discussion-time figures CONTEXT.md records are reconciled against re-measured values with the command beside each: 35 tracked non-`.planning` files (exact, with `-a`); `.planning/PROJECT.md` 46 lines / 51 occurrences of the subject literal; `.planning/PROJECT.md` 17 lines / 23 occurrences of `r2000_` against CONTEXT.md's 26 — a disagreement neither counting definition reproduces, stated as such rather than smoothed over."
    - "`CLAUDE.md` is recorded as 0 and 0 and NOT re-derived as outstanding (D-11), and the absence of any `ARCHITECTURE.md` outside `.planning/` is recorded as a measured fact with its command."
    - "`node scripts/check-no-regenerator2000.mjs` exits 0 after the sweep, and every one of the 35 tracked non-`.planning` files has an occurrence count identical to its pre-sweep count — the sweep is verdict-recording, not editing."
  prohibitions:
    - statement: "MUST NOT rewrite, reword or delete a dated historical record to make a document read as clean. Only text that tells a reader to use, install or invoke a deleted route — or asserts in the present tense that a deleted surface exists — is corrected; dated findings documents, licence and attribution notices, and milestone-scoped decision rows stay byte-identical (D-08). Rewriting a dated record is the one move this project's convention treats as destroying evidence rather than tidying it."
  artifacts:
    - path: ".planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-document-sweep.md"
      provides: "The CUT-06 sweep ledger: one verdict row per file in the defined swept set, plus the number reconciliation and the scope declaration"
      contains: "grep -a"
      min_lines: 90
  key_links:
    - from: ".planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-document-sweep.md"
      to: "scripts/check-no-regenerator2000.mjs"
      via: "every unchanged row cites the exemption class and pinned count that already protects that file"
      pattern: "exemption|pins|prefixHits"
---

<!-- planner-discipline-allow: regenerator2000 -->
<!-- planner-discipline-allow: r2000_ -->

<objective>
`CUT-06`, part B: the living-document sweep as a RECORD. `D-09` requires a verdict row for every file
in the swept set, including every file left unchanged, so a later reader can distinguish "we looked and
it is fine" from "we never looked" — the same distinction `D-01`'s registry draws for guards, applied
to documents.

Research measured the decisive fact: **every one of the 35 tracked non-`.planning` files carrying the
subject literal is already `D-08` class 1 (dated findings) or class 2 (licence / attribution /
provenance), and every one is already pinned by an exact-count exemption in the removal gate, which
exits 0 today. There are no live pointers left outside `.planning/`.** The corrective work was
confined to `.planning/` and was done in plan 32-03. This plan's deliverable is therefore the ledger,
not more edits.

That makes one corollary load-bearing: because every count is pinned with `===`, ANY edit to one of
those 35 files that changes its occurrence count reds the gate. The sweep is verdict-recording for all
35. If an edit ever becomes genuinely necessary, its pin moves in the same commit — the gate's own
stated rule: "a mention that moved moves its pin in the same commit, a mention that multiplied does not."

`D-11` is settled and must NOT be re-derived: `CLAUDE.md` measures 0 and 0; there is no
`ARCHITECTURE.md` outside `.planning/`. Record both as measured facts.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-CONTEXT.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-RESEARCH.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-03-SUMMARY.md
@scripts/check-no-regenerator2000.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the swept set, re-measure every count, and write one verdict row per file</name>
  <files>.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-document-sweep.md</files>
  <read_first>
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-RESEARCH.md` §5.1 (the headline reconciliation and the NUL-byte trap), §5.2 (the full 35-file census with per-file hit counts, `D-08` class and candidate verdict, plus the shipped-twin duplication note), §5.3 (`.planning/PROJECT.md`'s live pointers, the four `ARCHITECTURE.md` copies, and the swept-set recommendation)
    - `scripts/check-no-regenerator2000.mjs` — `SUBJECT_NEEDLE` at `:140` (the contiguous literal, composed), `PLANNING_PREFIX` at `:180` and `trackedFiles()` at `:194-200` (the scope predicate), `:217-222` (the exact-count rule), and the `EXEMPTION_CLASSES` block, so every unchanged row can cite the class and pinned count that already protects its file
    - `.planning/ROADMAP.md` § Phase 32 criterion 2 — the named document list (`install documentation`, `CLAUDE.md`'s constraint bullets and `r2000_*` clause, `PROJECT.md`'s constraints and Key Decisions rows, `ARCHITECTURE.md`'s Rule A21, `THIRD-PARTY-NOTICES.md`'s dual-licence notice which REMAINS TRUE for the retained prose, and all seven skill playbooks)
    - `src/skills/*/SKILL.md` — all seven playbooks; they are in `CUT-06`'s named set and each needs a row
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-03-SUMMARY.md` — the 17 per-line `r2000_` verdicts recorded for `.planning/PROJECT.md`, which this ledger rolls up
  </read_first>
  <action>
Write the sweep ledger. It is a record, not an edit pass — do not modify any file in the swept set from
this plan.

**1. Declare the swept set explicitly, with its predicate.** Research §5.3 leaves this open and names
it as something the planner must define. Define it as the union of:
  (a) every TRACKED file outside the `.planning/` prefix whose bytes contain the contiguous subject
      literal — the removal gate's own scope predicate (`git ls-files` minus `PLANNING_PREFIX`, plus
      the shipped `installer/**` paths supplied by `packFiles()`);
  (b) the named living documents from ROADMAP criterion 2 that predicate does not reach:
      `.planning/PROJECT.md`, `.planning/ARCHITECTURE.md`, `.planning/ROADMAP.md`,
      `.planning/REQUIREMENTS.md`, and `CLAUDE.md`;
  (c) all seven `src/skills/*/SKILL.md` playbooks, whether or not they carry the literal — ROADMAP
      criterion 2 names them as a set, so a playbook with zero mentions still owes an "we looked" row.
Write the exact commands for (a) and (c) so a later reader re-derives the set rather than trusting it.

**2. Apply the adjacency rule.** A file reachable through more than one clause gets EXACTLY ONE row,
keyed on its repo-relative path. Deduplicate before writing, and say in the ledger that you did — the
skill playbooks in particular qualify under both (a) and (c).

**3. Re-measure every count with the encoding definition stated.** Use `grep -a` (or an in-process byte
read) for every count, never a plain `grep`: `src/mcp/vice/anno-memmap-render.ts` carries a NUL byte at
offset 15097 (line 315), which makes GNU grep classify the whole file as binary and skip it, yielding 34
tracked non-`.planning` files where the correct answer is 35. State for every published number whether
it counts LINES containing a match or OCCURRENCES of a match — research found `.planning/research/PITFALLS.md`
and this project's own earlier figures disagreeing for exactly that reason.

**4. One row per file** with these columns: path, occurrence count (with `-a`), `D-08` class (1 dated
findings / 2 licence-attribution-provenance / 3 live pointer / infra), verdict
(`unchanged` | `corrected`), the plan that corrected it if any, and the removal-gate exemption class and
pinned count that protects it. Research §5.2's table is the starting point — re-measure every count
rather than copying it, and record any file whose count has moved since research.

**5. Distinguish `unchanged` from `corrected` mechanically.** A row is `corrected` only if
`git diff --exit-code <path>` against this phase's base commit is non-zero. A document whose bytes are
identical is `unchanged` even if it was read closely. Do not narrate the difference; derive it.

**6. Record the shipped-twin duplication.** `installer/skills/**` is gitignored but scanned via
`packFiles()`, and the gate pins `installer/skills/c64-program-recon/scripts/packer-finding.mjs` at 1
alongside `SKILL_ATTRIBUTION_PINS` for the shipped twins. Any edit to a `src/skills/*/SKILL.md` must be
followed by `node installer/scripts/sync-skills.mjs` before the gate is re-run —
`.github/workflows/ci.yml:126-128` does exactly this. State it in the ledger even though this plan makes
no skill edit, because the next reader who does needs it.

**7. Prove the sweep changed nothing it should not have.** After writing the ledger, run
`node scripts/check-no-regenerator2000.mjs` and confirm exit 0, and confirm every one of the 35 files'
occurrence counts is identical to its pre-sweep value.

Do not rewrite any dated record to make a document read as clean. Where a file's status is genuinely
ambiguous, record it as `unchanged` WITH the ambiguity stated — an ambiguity recorded is a decision, an
ambiguity resolved by silent editing is evidence destroyed.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && N=$(for f in $(git ls-files | grep -v '^\.planning/'); do grep -aq 'regenerator2000' "$f" && echo "$f"; done | wc -l) && test "$N" -eq 35 && node scripts/check-no-regenerator2000.mjs >/dev/null && grep -c 'grep -a' .planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-document-sweep.md</automated>
  </verify>
  <acceptance_criteria>
    - The ledger declares the swept set with the exact commands for each of its three clauses, and states that duplicates across clauses were collapsed to one row per repo-relative path.
    - Every file in the derived swept set has exactly one row: the ledger's row count equals the deduplicated set size, and no path appears twice (`awk` over the path column yields no duplicate).
    - The row count for clause (a) is 35, measured with `-a`; the ledger states that a plain `grep` yields 34 and names `src/mcp/vice/anno-memmap-render.ts` and its NUL at offset 15097 / line 315 as the cause.
    - Every published count states its unit (lines vs occurrences); no bare number appears without one.
    - All seven `src/skills/*/SKILL.md` playbooks have a row, including any with zero mentions.
    - Every `corrected` row is backed by a non-zero `git diff --exit-code <path>` against the phase base commit, and every `unchanged` row by a zero exit — the verdict is derived, not narrated.
    - Every `unchanged` row for a file in clause (a) cites the removal-gate exemption class and pinned count that protects it.
    - The ledger records the shipped-twin duplication and the `installer/scripts/sync-skills.mjs` obligation with its `.github/workflows/ci.yml:126-128` citation.
    - `node scripts/check-no-regenerator2000.mjs` exits 0, and the SUMMARY states that every one of the 35 counts is unchanged from its pre-sweep value.
  </acceptance_criteria>
  <done>Every file in a declared, re-derivable swept set carries exactly one verdict row with a mechanically-derived changed/unchanged state, and the gate is still green with every pinned count unmoved.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the discussion-time numbers and record the out-of-scope declarations</name>
  <files>.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-document-sweep.md</files>
  <read_first>
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-CONTEXT.md` `<specifics>` — the five discussion-time measurements offered "for the researcher to re-verify rather than trust"
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-RESEARCH.md` §5.1 (the reconciliation table, including the one figure that disagrees) and §5.3 (the `.planning/PROJECT.md` breakdown and the four-`ARCHITECTURE.md` inventory)
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-RESEARCH.md` Open Question 5 — where CONTEXT.md's "26 `r2000_` occurrences" came from, and its recommendation to state both measurements and cite the command
    - `CLAUDE.md` — to confirm 0 and 0 by measurement, since `D-11` forbids re-deriving it as outstanding but the ledger still owes the recorded evidence
    - `.planning/ARCHITECTURE.md`, `.planning/codebase/ARCHITECTURE.md`, `.planning/research/ARCHITECTURE.md`, `.planning/research/archive-v0.6.0/ARCHITECTURE.md` — the four copies, and which two carry Rule A21
  </read_first>
  <action>
Append two sections to the ledger.

**Section A — the number reconciliation.** For each figure CONTEXT.md recorded at discussion time, give
the re-measured value, the exact command, the counting unit, and a verdict of reconciles / disagrees:
  - "35 tracked non-`.planning` files contain the literal" — reconciles exactly, but ONLY with `-a`.
    Record 34 without, and name the cause.
  - "`.planning/PROJECT.md` has 46 mentions" — reconciles as 46 LINES; also record 51 occurrences.
  - "`.planning/PROJECT.md` has 26 `r2000_` occurrences" — **DISAGREES.** Measured 17 lines and 23
    occurrences; neither counting definition yields 26. State the delta of 3, state both measurements,
    cite the command, and treat 23 as the working figure. Do not smooth this over: `D-09`'s whole point
    is that a silent omission bites exactly here, and this milestone has already had to correct figures
    whose provenance did not reproduce. Note the plausible causes research listed (a case-insensitive
    count including `R2000_`, a superset pattern, or a measurement taken before a commit landed) as
    hypotheses, explicitly unconfirmed.
  - "`CLAUDE.md` has 0 and 0" — reconciles. Record it as confirming `D-11`, not as re-deriving it.
  - "no `ARCHITECTURE.md` outside `.planning/`" — reconciles. Record the command
    (`git ls-files | grep -i 'ARCHITECTURE\.md'`) and the four paths it returns, all inside `.planning/`,
    and which two carry Rule A21.
  - "126 `*.test.*` files in `src/mcp/vice/`" — re-measure and record.

**Section B — the out-of-scope declarations, each as a ROW rather than an omission.** `D-09` requires
that a reader can tell "we looked and it is fine" from "we never looked", so every excluded population
gets an explicit row with its reason:
  - `.planning/phases/**` — phase artifacts are dated records by construction; excluded. Give the count
    of files under that prefix carrying the literal so the exclusion has a measured size.
  - `.planning/research/**` and `.planning/research/archive-v0.6.0/**` — research archives, dated by
    construction; excluded. The `.planning/research/ARCHITECTURE.md` copy of Rule A21 is named here
    explicitly, with the verdict `unchanged — research archive, dated by construction`, and cross-linked
    to plan 32-03's decision to leave it byte-identical.
  - The remaining `.planning/` files carrying the literal — give the total (research measured 338
    `.planning` files against 373 tracked overall; re-measure both) and state which of them are in the
    swept set by name and why the rest are not.
  - The two todos reviewed and NOT folded (the `research/questions.md` corpus claim, and Phase 7
    Pitfall 5's overgeneralisation) — recorded as adjacent-to-but-outside `CUT-06`'s subject, so a later
    reader sees they were considered rather than missed.

Every number in both sections carries the command that produced it. No number is copied from CONTEXT.md
or RESEARCH.md without being re-run.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac 'r2000_' .planning/PROJECT.md && grep -ao 'r2000_' .planning/PROJECT.md | wc -l && test $(grep -ac 'regenerator2000' CLAUDE.md) -eq 0 && test $(git ls-files | grep -ic 'ARCHITECTURE\.md') -eq 4 && grep -q '23' .planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-document-sweep.md</automated>
  </verify>
  <acceptance_criteria>
    - Section A gives, for each of the six discussion-time figures, the re-measured value, the exact command, the counting unit, and a reconciles/disagrees verdict.
    - The `r2000_` figure is recorded as DISAGREEING with CONTEXT.md's 26: both 17 lines and 23 occurrences appear, the delta of 3 is stated, 23 is named as the working figure, and the hypotheses for the 26 are marked unconfirmed rather than asserted.
    - `CLAUDE.md` is recorded as 0 and 0 with its command, framed as confirming `D-11` rather than re-opening it.
    - The four `ARCHITECTURE.md` paths are listed with the command that found them; both A21-carrying copies are named; the ledger states there is none outside `.planning/`.
    - Section B carries a row for each excluded population — `.planning/phases/**`, `.planning/research/**`, the remaining `.planning/` files, and the two non-folded todos — each with a measured size where a size exists and an explicit reason.
    - `.planning/research/ARCHITECTURE.md` appears by name with the verdict `unchanged — research archive, dated by construction`, cross-referencing plan 32-03.
    - Every number in both sections is followed by the command that produced it; no figure is carried over from CONTEXT.md or RESEARCH.md unre-run.
    - `node scripts/check-no-regenerator2000.mjs` exits 0 and `cd src/mcp/vice && npm run test:automated` exits 0 with 0 failures.
  </acceptance_criteria>
  <done>Every discussion-time figure is reconciled with its command and its unit, the one that disagrees is recorded as disagreeing, and every excluded population carries an explicit reasoned row rather than being absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tracked file bytes -> census counts | File content, including NUL-tainted content, crosses into published figures a later phase re-derives from |
| the ledger's verdicts -> future readers' decisions | An `unchanged` row asserts "we looked"; a wrong one is indistinguishable from "we never looked" |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-18 | Repudiation | published census figures | high | mitigate | Every number carries the command that produced it and the counting unit (lines vs occurrences); no figure is carried over from CONTEXT.md or RESEARCH.md without being re-run; the one disagreeing figure is recorded as disagreeing rather than smoothed |
| T-32-19 | Information disclosure | NUL-tainted file silently dropped from the census | high | mitigate | Every count uses `grep -a` or an in-process byte read; the ledger names `src/mcp/vice/anno-memmap-render.ts`, its NUL at offset 15097 / line 315, and the 34-versus-35 consequence |
| T-32-20 | Tampering | an edit to a swept file moving a pinned occurrence count | high | mitigate | The sweep is verdict-recording; no file in the swept set is modified by this plan, and `node scripts/check-no-regenerator2000.mjs` is asserted to exit 0 with every count unchanged |
| T-32-21 | Repudiation | a silently excluded population reading as an omission | medium | mitigate | Every excluded population gets an explicit row with a measured size and a stated reason, so "we looked and it is out of scope" is distinguishable from "we never looked" (`D-09`) |
| T-32-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no packages and invokes no package manager; it writes one evidence document |
</threat_model>

<verification>
- `node scripts/check-no-regenerator2000.mjs` exits 0 with every pinned count unmoved.
- `cd src/mcp/vice && npm run test:automated` exits 0 with 0 failures.
- `node scripts/audit-gate.mjs --json` reports `allowed: true`, `redGuards: []`.
- `git status --porcelain` shows changes only under `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/`.
- The tracked non-`.planning` census re-measures to 35 with `-a` and 34 without.
</verification>

## Artifacts this phase produces

See the table in `32-01-PLAN.md` → "Artifacts this phase produces" — the canonical list of every
symbol, file and CI step Phase 32 creates. This plan adds
`.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-document-sweep.md`.

<success_criteria>
- Every file in a declared, re-derivable swept set has exactly one verdict row, including every file left unchanged.
- Every count states its unit and survives the NUL-byte trap.
- CONTEXT.md's one disagreeing figure is recorded as disagreeing, with both measurements and the command.
- Every excluded population is a reasoned row, not an absence.
- No dated record was rewritten and no pinned count moved.
</success_criteria>

<output>
Create `.planning/phases/32-the-deletion-and-the-grep-gate/32-05-SUMMARY.md` when done
</output>
