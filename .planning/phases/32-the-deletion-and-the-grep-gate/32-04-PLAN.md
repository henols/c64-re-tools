---
phase: 32-the-deletion-and-the-grep-gate
plan: 04
type: execute
wave: 3
depends_on: ["32-03"]
files_modified:
  - src/mcp/vice/docs-linerefs.test.ts
autonomous: true
requirements: [CUT-06]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "[edge:CUT-06/empty] A scanned document whose isolated bullet yields ZERO `vice-proxy.ts:N` citations FAILS, and the non-vacuity floor is enforced PER DOCUMENT (>= 2 each), never as a global sum across documents — a global count stays green when one document's bullet is reworded to zero citations while the other still has two, which is exactly the vacuity CUT-04 audits for. A scanned document that does not exist FAILS rather than shrinking the set silently."
    - "[edge:CUT-06/ordering] `.planning/PROJECT.md`'s citations are repaired in wave 2 (plan 32-03) and the guard is widened onto that file in wave 3 — never the reverse and never in one commit. Widening onto an unrepaired file makes the guard red on landing, and ordering constraint 5 / the `4f048bb` precedent then forbids recording anything green over it."
    - "The bullet-isolation predicate selects the line that BOTH contains `rewriteArguments()` AND matches the citation regex — not `.find()`'s first `rewriteArguments()` line. `.planning/PROJECT.md` has 6 such lines; `.find()` returns `:230`, which carries zero citations."
    - "`.planning/PROJECT.md:1319` — a dated record of PAST drift citing `:3029`/`:2964`/`:1508`/`:1484` — is NOT swept into the checked set, so correctly-preserved history cannot red the guard."
    - "`cd src/mcp/vice && node --test docs-linerefs.test.ts` exits 0 against both scanned documents, and `node scripts/audit-gate.mjs --json` still reports 9 derived docs guards with `DOCS_GUARD_FLOOR` unchanged at 7."
  artifacts:
    - path: "src/mcp/vice/docs-linerefs.test.ts"
      provides: "Line-citation guard widened from one hard-coded document to a declared document set with a per-document non-vacuity floor"
      contains: "PROJECT.md"
      min_lines: 110
  key_links:
    - from: "src/mcp/vice/docs-linerefs.test.ts"
      to: ".planning/PROJECT.md"
      via: "the widened scanned-document set, resolved through repoRoot({ from: HERE })"
      pattern: "\\.planning/PROJECT\\.md"
    - from: "src/mcp/vice/docs-linerefs.test.ts"
      to: "src/mcp/vice/vice-proxy.ts"
      via: "every cited line number is read back and asserted to be a call site or a function declaration"
      pattern: "vice-proxy\\.ts"
---

<!-- planner-discipline-allow: The external analyser -->
<!-- planner-discipline-allow: PROJECT.md -->

<objective>
`D-10`, second half: bring `.planning/PROJECT.md`'s `vice-proxy.ts` line citations under
`docs-linerefs.test.ts` so they are mechanically checked the way `CLAUDE.md`'s already are.

`PROJECT.md:311` records its own defect — the figures were "stale again at `:3029`/`:2964`/`:1508`/`:1484`
until the v0.7.0 open, when `docs-linerefs.test.ts` was found to read only CLAUDE.md and not this
copy". That sentence is the requirement.

The widening is small in lines and sharp in hazards. The guard's scanned "set" is not a set at all
today: it is one hard-coded path read twice (`:57`, `:67`), and its bullet-isolation predicate is a
`.find()` that returns the FIRST line containing `rewriteArguments()`. In `CLAUDE.md` there is exactly
one such line, so `.find()` is safe there. In `.planning/PROJECT.md` there are SIX — `:230`, `:311`,
`:375`, `:381`, `:1111`, `:1319` — and `.find()` returns `:230`, which carries zero `vice-proxy.ts:N`
citations. A naive widening therefore fails the non-vacuity test on its first run, for the wrong reason.
`:1319` is dated drift history and must never be swept in.

Ordering (research Pitfall 3, `D-07`): plan 32-03 repaired `PROJECT.md:311` in wave 2; this plan
widens in wave 3; the sweep measures `docs-linerefs` in wave 4. Never reversed, never merged.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-CONTEXT.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-RESEARCH.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-PATTERNS.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-03-SUMMARY.md
@src/mcp/vice/docs-linerefs.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen the scanned-document set and fix the bullet-isolation predicate</name>
  <precondition>.planning/PROJECT.md:311 cites vice-proxy.ts:3050 and vice-proxy.ts:1529 (repaired by plan 32-03) — widening onto the unrepaired file lands the guard red</precondition>
  <reversibility rating="costly">D-10: PROJECT.md now reds CI whenever vice-proxy.ts shifts. That is the intended pressure, but it is new pressure on a file edited every milestone. Carried forward from CONTEXT.md, not re-derived.</reversibility>
  <files>src/mcp/vice/docs-linerefs.test.ts</files>
  <read_first>
    - `src/mcp/vice/docs-linerefs.test.ts` — all 100 lines: `CITATION_RE` at `:34`, `findRewriteArgumentsBullet()` at `:41-47` (the `.find()`), `extractCitations()` at `:48-54`, the non-vacuity test at `:56-64`, the real assertion at `:67-86`, the planted-violation test at `:88-100`, and the two hard-coded `CLAUDE.md` reads at `:57` and `:67`
    - `src/mcp/vice/docs-dangling-refs.test.ts` — `:44-56` (the frozen `ALWAYS_PRESENT_NORMATIVE_DOCS` array and the per-call `normativeDocs()` derivation) and `:136` (the existence assertion whose message shape is the one to copy: a missing document FAILS rather than letting the scanned set shrink silently)
    - `.planning/PROJECT.md` at 230, 311, 375, 381, 1111 and 1319 — the six lines containing `rewriteArguments()`, so the isolation predicate is written against the real text rather than an assumption. `:1319` is dated drift history
    - `.planning/ROADMAP.md:541` — it carries `vice-proxy.ts:3216` in a NON-`rewriteArguments` context. It is not in the scanned set, but confirm the isolation predicate could not sweep it in if the set later grew
    - `src/mcp/vice/repo-root.ts` — `repoRoot({ from: HERE })`, already imported at `:28` and used at `:57`; `.planning/PROJECT.md` is reachable through it with no new path machinery
    - `scripts/audit-gate.mjs` — `EXPECTED_DOCS_GUARD_NAMES` at `:132` and `DOCS_GUARD_FLOOR` at `:112`, to confirm that widening an EXISTING `docs-*.test.ts` changes neither
  </read_first>
  <behavior>
    - Given the two-document set: every extracted citation resolves to a `vice-proxy.ts` line that is either a `rewriteArguments(` call or a function declaration.
    - Given a document with 6 lines containing `rewriteArguments()` of which one carries citations: the predicate selects the citation-carrying line, not the first match.
    - Given a document whose only `rewriteArguments()` line carries zero citations: the per-document non-vacuity test FAILS for that document.
    - Given one document with 2 citations and another with 0: the run FAILS — the floor is per document, never a sum.
    - Given a document path in the set that does not exist on disk: the run FAILS with a message telling the reader to update the declared set rather than let it shrink.
    - Given `.planning/PROJECT.md:1319`'s dated drift record (`:3029`/`:2964`/`:1508`/`:1484`): none of those four numbers is checked, so preserved history cannot red the guard.
  </behavior>
  <action>
Replace the two hard-coded `CLAUDE.md` reads at `:57` and `:67` with a declared document set and loop
the three tests over it.

**The set.** Copy `docs-dangling-refs.test.ts:44-56`'s shape: a frozen, repo-root-relative array —
`CLAUDE.md` and `.planning/PROJECT.md` — with the comment that shape carries ("paths are relative to
the repo root and are all expected to exist; a missing one FAILS rather than silently shrinking the
scanned set"). A hand-typed two-item list is correct here: this is the guard's own SUBJECT declaration,
not a guard set being derived from disk, and it is the same shape `docs-dangling-refs.test.ts` already
uses. Add the existence assertion in `docs-dangling-refs.test.ts:136`'s message shape.

**The isolation predicate.** `findRewriteArgumentsBullet()` at `:41-47` uses `lines.find(...)`, which
returns the FIRST line containing `rewriteArguments()`. Replace it with a predicate that filters to
lines containing BOTH `rewriteArguments()` AND at least one `CITATION_RE` match, then asserts EXACTLY
ONE such line per document and returns it. Measured at plan time: `CLAUDE.md` has exactly one
qualifying line (`:26`); `.planning/PROJECT.md` has six lines containing `rewriteArguments()` (`:230`,
`:311`, `:375`, `:381`, `:1111`, `:1319`) of which the citation-carrying one is `:311`. The
exactly-one assertion is what makes a future second citation-carrying bullet a loud failure rather
than a silently-ignored one.

`:1319` reads "`rewriteArguments()` citations had drifted to `:3029`/`:2964`/`:1508`/`:1484`" — those
are bare-shorthand numbers, not the full `vice-proxy.ts:N` shape `CITATION_RE` matches, so the
citation filter already excludes it. VERIFY that by reading the line rather than assuming, and record
the reasoning in a comment on the predicate — a later reader must be able to see why preserved history
is excluded by construction rather than by luck. Do NOT loosen `CITATION_RE` to match the bare
shorthand: that would sweep in `:1319`'s dated record and red the guard on correctly-preserved history.

**The non-vacuity floor.** `:56-64`'s `citations.length >= 2` must become PER DOCUMENT, asserted inside
the loop, with the document name in the failure message. Never a global sum: a sum stays green if
`PROJECT.md`'s bullet is reworded to zero citations while `CLAUDE.md` still carries two, and that is
precisely the vacuous-guard class this file's own comment names (`T-11-DOC-DRIFT`).

**Preserve the file's existing comment discipline.** Every one of the three tests carries a comment
explaining what a rewording would do; keep those and extend them to the new per-document shape.

Add nothing else. Do not change `CITATION_RE`'s shape, do not add a new scanned document beyond
`.planning/PROJECT.md`, do not touch the guard's file name (it stays a `docs-*.test.ts` already in
`audit-gate.mjs`'s derived set and in `EXPECTED_DOCS_GUARD_NAMES`, so membership does not change and
`DOCS_GUARD_FLOOR` does not move).

This file is inside the removal gate's scope; add no occurrence of the subject literal.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-linerefs.test.ts && npm run typecheck && cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/audit-gate.mjs --json | grep -q '"allowed":true'</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && node --test docs-linerefs.test.ts` exits 0, and its output names BOTH `CLAUDE.md` and `.planning/PROJECT.md` as scanned documents.
    - `grep -c 'PROJECT.md' src/mcp/vice/docs-linerefs.test.ts` >= 1 and `grep -c 'readFileSync(join(repoRoot({ from: HERE }), "CLAUDE.md")' src/mcp/vice/docs-linerefs.test.ts` == 0 — the hard-coded double read is gone.
    - `grep -c '\.find(' src/mcp/vice/docs-linerefs.test.ts` == 0 — the first-match isolation predicate is gone.
    - The file asserts EXACTLY ONE qualifying bullet per document; a temporary local edit adding a second citation-carrying `rewriteArguments()` line to `.planning/PROJECT.md` makes the test exit non-zero, and the edit is reverted before commit.
    - The non-vacuity assertion runs inside the per-document loop and its failure message names the document; `grep -c 'citations.length >= 2' src/mcp/vice/docs-linerefs.test.ts` >= 1 and no global summed count is asserted.
    - `cd src/mcp/vice && npm run typecheck` exits 0.
    - `node scripts/audit-gate.mjs --json` reports `allowed: true`, `redGuards: []`, 9 derived docs guards, and `DOCS_GUARD_FLOOR` unchanged at 7 — widening an existing guard changed no membership.
    - `grep -ac 'the external analyser' src/mcp/vice/docs-linerefs.test.ts` == 0 and `node scripts/check-no-analyser.mjs` exits 0.
  </acceptance_criteria>
  <done>`.planning/PROJECT.md`'s citations are mechanically checked alongside `CLAUDE.md`'s, the isolation predicate selects the citation-carrying bullet rather than the first match, the floor is per document, and preserved drift history is excluded by construction.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the planted-violation test to the widened predicate, and prove the empty and missing-document cases red</name>
  <files>src/mcp/vice/docs-linerefs.test.ts</files>
  <read_first>
    - `src/mcp/vice/docs-linerefs.test.ts` — as written by Task 1, plus the original planted-violation test at `:88-100` whose shape is being extended
    - `src/mcp/vice/removal-gate.test.ts` — `:58-75`, particularly the closing self-non-vacuity assertion (`stubbedAlwaysEmpty` vs the real predicate's output), which is the idiom for proving a check is not vacuous
    - `scripts/check-no-analyser.mjs` — `:149-154`, "A planted violation proved against a re-implementation of the rule proves nothing about the rule the real scan applies"
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-PATTERNS.md` — the `docs-linerefs.test.ts` section and its measured six-line finding
  </read_first>
  <action>
The original planted-violation test at `:88-100` plants a citation at `vice-proxy.ts` line 1 and asserts
the line-shape check rejects it. That still holds and stays. Add the three plants the WIDENING
introduces, each driving the real predicates written in Task 1 rather than a re-implementation of them:

  1. **Zero-citation document.** Feed the isolation-and-extraction path a synthetic document body whose
     only `rewriteArguments()` line carries no `vice-proxy.ts:N` citation, and assert the per-document
     non-vacuity check reports it. This is the case a global summed floor would hide.
  2. **Ambiguous document.** Feed it a synthetic body with TWO citation-carrying `rewriteArguments()`
     lines and assert the exactly-one assertion reports it, naming the count found. A silently-ignored
     second bullet is the drift this widening exists to catch.
  3. **Missing document.** Feed the set a repo-relative path that does not exist and assert the run
     fails with a message telling the reader to update the declared set — not a skip, not a shrunken
     set, not a pass.

Restructure Task 1's helpers so these three plants can drive them with an in-memory string plus a
declared path, with no filesystem writes and no fixture files: the predicates take text and a document
name, and the file-reading loop is the only thing that touches disk. That keeps the whole test fast
enough for the per-commit sampling rate and keeps every plant driving the REAL rule.

Close with the self-non-vacuity assertion in `removal-gate.test.ts:58-75`'s idiom: a stubbed predicate
that always reports "no problem" must not deep-equal the real predicate's output on plant 1. Without it
a future refactor that neuters the check passes all three plants silently.

Add no fixture file under `src/mcp/vice/fixtures/planted-` — that prefix carries an exact
`prefixHits: 2` pin in the removal gate, and a new file there would move it.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-linerefs.test.ts && npm run typecheck && cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-no-analyser.mjs && cd src/mcp/vice && npm run test:automated</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && node --test docs-linerefs.test.ts` exits 0 and reports at least 6 passing tests (the original three plus the three new plants).
    - Each of the three new tests drives a function imported or defined in the file's real predicate path — no test re-implements the isolation, extraction or floor logic locally.
    - The zero-citation plant asserts a failure that NAMES the offending document, proving the message is per document.
    - The ambiguous-document plant asserts the reported count, so the failure tells a reader how many bullets were found.
    - The missing-document plant asserts a message instructing the reader to update the declared set, matching `docs-dangling-refs.test.ts:136`'s shape.
    - A self-non-vacuity assertion is present: a stubbed always-clean predicate is asserted NOT to deep-equal the real predicate's output on the zero-citation plant.
    - `git status --porcelain -- src/mcp/vice/fixtures/` is empty — no new planted fixture was added under the pinned `planted-` prefix.
    - `cd src/mcp/vice && npm run test:automated` exits 0 with 0 failures, and `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>The widened guard carries its own planted proof for every new failure mode it introduced — zero citations, an ambiguous bullet, and a missing document — each driving the real rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/PROJECT.md` content -> guard assertions | Document text now decides a CI-relevant pass/fail; a reworded bullet can silence the check |
| synthetic plant strings -> the guard's predicates | In-memory test inputs cross into the real isolation/extraction/floor logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-14 | Tampering | the widened non-vacuity floor | high | mitigate | The floor is asserted PER DOCUMENT inside the loop with the document named in the message; a global sum is explicitly forbidden and is proven red by the zero-citation plant |
| T-32-15 | Spoofing | the bullet-isolation predicate | high | mitigate | The predicate requires BOTH `rewriteArguments()` and a `CITATION_RE` match and asserts exactly one qualifying line per document; the ambiguous-document plant proves a second bullet is loud rather than silently ignored |
| T-32-16 | Repudiation | dated drift history at `.planning/PROJECT.md:1319` | medium | mitigate | `CITATION_RE` is NOT loosened to the bare-shorthand form, so `:1319`'s preserved record is excluded by construction; the exclusion is recorded in a comment on the predicate rather than left to luck |
| T-32-17 | Denial of Service | a missing scanned document silently shrinking the set | medium | mitigate | An existence assertion in `docs-dangling-refs.test.ts:136`'s shape fails the run, and the missing-document plant proves it |
| T-32-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no packages and invokes no package manager; it edits one existing test file |
</threat_model>

<verification>
- `cd src/mcp/vice && node --test docs-linerefs.test.ts` exits 0 with at least 6 tests.
- `cd src/mcp/vice && npm run test:automated` exits 0 with 0 failures; `npm run typecheck` exits 0.
- `node scripts/audit-gate.mjs --json` reports `allowed: true`, `redGuards: []`, 9 derived docs guards, `DOCS_GUARD_FLOOR` still 7.
- `node scripts/check-no-analyser.mjs` exits 0.
- `git status --porcelain -- src/mcp/vice/fixtures/` is empty.
</verification>

## Artifacts this phase produces

See the table in `32-01-PLAN.md` → "Artifacts this phase produces" — the canonical list of every
symbol, file and CI step Phase 32 creates. This plan adds no new file; it replaces
`docs-linerefs.test.ts`'s `findRewriteArgumentsBullet()` with a citation-aware, exactly-one isolation
predicate and adds a frozen scanned-document array.

<success_criteria>
- `.planning/PROJECT.md`'s `vice-proxy.ts` citations are mechanically checked and green.
- The guard fails per document on zero citations, on an ambiguous second bullet, and on a missing document — each proven by a plant driving the real rule.
- Preserved drift history is excluded by construction and the exclusion is documented in the predicate.
- No guard membership changed and `DOCS_GUARD_FLOOR` did not move.
</success_criteria>

<output>
Create `.planning/phases/32-the-deletion-and-the-grep-gate/32-04-SUMMARY.md` when done
</output>
