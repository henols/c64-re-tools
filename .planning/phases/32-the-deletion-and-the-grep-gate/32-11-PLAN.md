---
phase: 32-the-deletion-and-the-grep-gate
plan: 11
type: execute
wave: 2
gap_closure: true
depends_on: ["32-10"]
files_modified:
  - scripts/check-guard-fates.mjs
  - scripts/check-skill-tool-coverage.mjs
  - scripts/check-skill-fork-honesty.mjs
  - scripts/check-skill-cli-invocations.mjs
  - scripts/check-skill-description-overlap.mjs
  - src/mcp/vice/audit-root-args.test.ts
  - .gitignore
autonomous: true
requirements: [CUT-04]
net_deletion: false   # five local argv readers are deleted as LINES inside modified files.
                      # cleanup-wave's predicate is `git diff --diff-filter=D --name-only`
                      # (lib/worktree-safety.cjs:759) — DELETED FILES only. This branch deletes no file.

estimate:
  tokens: 82000
  raw_tokens: 82000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All five remaining root-accepting scripts call the shared `parseRootArg()` and none of them defines an argv-parsing function of its own. Today six verbatim copies exist and all six carry the same defect (`IN-06`). — TRACE: gaps[1].missing[0]"
    - "`node scripts/check-guard-fates.mjs --root=/tmp` exits NON-ZERO. Measured at HEAD today: exit 0, with a full green `setA=43 setB=16 setC=2 total=61 rows=61` report — the blocking CI gate reporting success about a tree it was told not to read. — TRACE: verifier truth 12 / gaps[1].missing[0]"
    - "`node scripts/check-guard-fates.mjs --rooot /tmp` exits NON-ZERO and names the token. Measured at HEAD today: exit 0 with the same green report. — TRACE: verifier truth 12 / gaps[1].missing[0]"
    - "`node scripts/check-guard-fates.mjs --root` with no value exits NON-ZERO. Measured at HEAD today: exit 0 with the same green report. — TRACE: verifier truth 12 / gaps[1].missing[0] / [edge:CUT-04/empty]"
    - "`scripts/check-guard-fates.mjs` still accepts its existing boolean flag, and that flag is never consumed as the root's value in either order the two flags are supplied. — TRACE: gaps[1].missing[0] (the shared parser must not regress an existing accepted flag)"
    - "Every one of the six root-accepting scripts has at least one spawned-process test covering all three of: a contained root, an out-of-repository root exiting 1 with `REFUSED`, and an equals-form root exiting non-zero. — TRACE: gaps[1].missing[2], verbatim"
    - "`node scripts/check-skill-description-overlap.mjs --root <synthetic in-repository tree>` reads that tree's skills corpus and that tree's `CLAUDE.md`, not the real repository's — proven by a synthetic tree constructed so the real repository's answer and the synthetic answer differ. — TRACE: gaps[1].missing[2] ('contained root reads the synthetic tree') / verifier key-link row 'Five re-pointed scripts → lib/audit-root.mjs'"
    - statement: "[edge:CUT-04/ordering] Where two argv tokens compare equal — the same flag supplied twice — the resolution is SPECIFIED rather than positional: a repeated root flag is a hard error naming both values, so no invocation's meaning depends on which copy the parser kept. And where the derived audited set's members compare equal for ordering purposes, the fate guard's verdict does not depend on the order `deriveAuditedSet()` returns them in, nor on the order rows appear in the registry — the comparison is set-shaped, so a reordering must not change the verdict. — TRACE: [edge:CUT-04/ordering] from the deterministic edge probe, resolved backstop rather than explicit per §A because only the repeated-flag half has a defensible acceptance criterion this round; the set-order half concerns code this round does not change."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT introduce any relaxation hatch — no environment-variable override, no skip or force flag, no waiver file, no allow-list — into any of the five scripts while wiring them. A root argument that cannot be honoured must REFUSE with a diagnosable message; it must never degrade into a silent read of the default root. (Carried forward from plan 32-01's prohibition 2 and `scripts/audit-gate.mjs`'s D-12-14, not minted here.)"
    - statement: "MUST NOT record a figure, an exit status or a count in this plan's SUMMARY that was reasoned rather than measured. Every number written this round is the output of a command quoted beside it. (Carried forward from D-01's derive-from-disk rule and D-09's 'we looked and it is fine' standard, not minted here.)"
  artifacts:
    - path: "scripts/check-guard-fates.mjs"
      provides: "The blocking CI fate guard, with its local argv reader deleted, the shared strict parser wired in, and its existing boolean flag preserved"
      contains: "parseRootArg"
    - path: "scripts/check-skill-tool-coverage.mjs"
      provides: "Root-accepting skill-coverage gate wired to the shared parser"
      contains: "parseRootArg"
    - path: "scripts/check-skill-fork-honesty.mjs"
      provides: "Root-accepting fork-honesty gate wired to the shared parser"
      contains: "parseRootArg"
    - path: "scripts/check-skill-cli-invocations.mjs"
      provides: "Root-accepting CLI-invocation gate wired to the shared parser"
      contains: "parseRootArg"
    - path: "scripts/check-skill-description-overlap.mjs"
      provides: "The one gate whose comparison data already follows the root; wired to the shared parser and given the synthetic-tree read proof"
      contains: "parseRootArg"
    - path: "src/mcp/vice/audit-root-args.test.ts"
      provides: "Extended to the full six-script matrix plus the synthetic-tree read proof"
      min_lines: 200
    - path: ".gitignore"
      provides: "Ignores the in-repository synthetic-tree fixtures so a crashed test cannot dirty the tree and void the mutation harness's cleanliness assertion"
      contains: "audit-root-synth"
  key_links:
    - from: "scripts/check-guard-fates.mjs"
      to: "scripts/lib/audit-root.mjs"
      via: "imports parseRootArg alongside resolveContainedRoot; has no argv reader of its own"
      pattern: "parseRootArg"
    - from: "src/mcp/vice/audit-root-args.test.ts"
      to: "scripts/check-skill-description-overlap.mjs"
      via: "spawned against a synthetic in-repository tree whose answer differs from the real repository's"
      pattern: "check-skill-description-overlap\\.mjs"
---

<!-- planner-discipline-allow: parseArgs -->
<!-- planner-discipline-allow: parseRootArg -->
<!-- planner-discipline-allow: DEFAULT_ROOT -->
<!-- planner-discipline-allow: BAD ARGUMENTS -->
<!-- planner-discipline-allow: REFUSED -->
<!-- planner-discipline-allow: r2000 -->
<!-- planner-discipline-allow: regenerator2000 -->
<!-- planner-discipline-allow: guard-fates -->
<!-- planner-discipline-allow: audit-root-synth -->

<objective>
GAP CLOSURE, round 1. **Expansion out from plan 32-10's tracer.** The tracer proved one
shared strict parser against one consumer. This plan carries the same shape to the other
five, deletes the five remaining verbatim copies of the defective reader, and gives the
whole seam the per-script coverage `32-VERIFICATION.md` asks for by name: *"At least one
spawnSync test per `--root`-ised script: contained root reads the synthetic tree;
out-of-repo root exits 1 with REFUSED; `--root=<dir>` exits non-zero."*

The most consequential consumer here is `scripts/check-guard-fates.mjs` — the phase's own
blocking CI gate. At HEAD it answers an equals-form root with a full green report about
the real repository. That is the audit instrument itself reporting success about a tree it
was explicitly told not to read.

Implements **D-06** (a guard is pointed at a synthetic tree rather than the real working
tree being mutated) and **D-07** (guards that lack a root override get one, and the
addition lands before the sweep measures that guard). Neither decision is re-taken here;
this plan makes the mechanism D-06 assumed actually hold for the one gate whose comparison
data can follow the root.

Purpose: `IN-06` — one defect copy-pasted into six files. Patching six copies is the
symptom fix and reopens the moment a seventh script wants a root override. Extraction is
the cause fix, and `IN-07`, the duplicated refusal preamble, falls out of it for free.

Output: five wired scripts, five deleted readers, the full six-script spawned matrix, and
the first proof that a contained root actually redirects a gate's reads.

**SCOPE FENCE.** Every truth above traces to a verifier truth number or a `gaps[].missing`
line. Explicitly OUT of scope, recorded rather than silently skipped:
`scripts/audit-mutation-harness.mjs` is NOT migrated — the verifier names it as *the model
that was not reused*, it already rejects unknown arguments correctly, and it carries four
further flags with their own exactly-one-selector rule; migrating it would put the model
at risk in order to remove a duplication no finding reports. `scripts/audit-gate.mjs` is
NOT migrated either (`WR-13`), for the same reason and with the same recorded decision.
`WR-06`, `WR-12`, `WR-14` and broken windows #28–#32 stay OPEN in the existing findings
todo.

**Edge-coverage arithmetic for this plan.** This plan owns 1 of the round's 6 probe items:
`[edge:CUT-04/ordering]`, resolved `backstop` and authored as a flat-scalar
`{ statement, verification: backstop }` marker above. Backstop rather than explicit
because only its first half — the repeated-flag rule — has a defensible acceptance
criterion this round; the second half, order-independence of the fate guard's verdict, is
a property of code this round does not change and would need a measurement no
`gaps[].missing` line asks for. Round total is stated in plan 32-10: 6 surfaced ==
6 authored (5 explicit + 1 backstop) + 0 flagged assumptions.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-CONTEXT.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-VERIFICATION.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-10-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the five remaining argv readers and wire all five scripts to the shared strict parser</name>
  <precondition>Plan 32-10 has merged and `scripts/lib/audit-root.mjs` exports `parseRootArg`; assert with a grep before editing any consumer, and halt if absent.</precondition>
  <files>scripts/check-guard-fates.mjs, scripts/check-skill-tool-coverage.mjs, scripts/check-skill-fork-honesty.mjs, scripts/check-skill-cli-invocations.mjs, scripts/check-skill-description-overlap.mjs</files>
  <read_first>
    - `scripts/lib/audit-root.mjs` — the WHOLE file as plan 32-10 left it: `parseRootArg()`'s signature, its five throwing conditions, and its message-prefix convention.
    - `scripts/generate-tool-support-table.mjs` — the wiring plan 32-10 landed: the import, the call site, the handler that reports an argument rejection, and the separate handler that reports a containment refusal. This is the shape to replicate five times.
    - `scripts/check-guard-fates.mjs:826-870` — the local reader at `:836-848`, the `IS_ENTRY_POINT` constant, the call at `:854` which also destructures the boolean flag, and the refusal preamble comment that follows it.
    - `scripts/check-skill-tool-coverage.mjs:94-125` — the local reader at `:99-107` and the module-scope resolution at `:115-123`. Note this file resolves its root at MODULE scope, not inside a `main()`; the other three skill gates do the same and the wiring must respect that.
    - `scripts/check-skill-fork-honesty.mjs:112-140`, `scripts/check-skill-cli-invocations.mjs:89-118`, `scripts/check-skill-description-overlap.mjs:106-135` — the same pattern in each.
    - `scripts/check-skill-cli-invocations.mjs:164` — the one place a script branches on whether the resolved root equals the default root. Preserve that behaviour exactly; it is not part of this change.
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-VERIFICATION.md` — the Gap 2 `artifacts` list, which names each file's reader by line range.
  </read_first>
  <action>
Work the five scripts in this order — `scripts/check-guard-fates.mjs` FIRST, because it is
the blocking CI gate and any regression there must surface before the four skill gates are
touched.

**A. Delete the local argv reader outright.** Remove the whole function, not just its body,
and remove nothing else. Do not leave it unused, do not leave a thin wrapper around the
shared parser, and do not keep a second reader for a compatibility path.

**B. Import `parseRootArg` from `./lib/audit-root.mjs`** in the same import style each file
already uses for `resolveContainedRoot`.

**C. Call it with the script's own base name as the `script` option.** The names are
`check-guard-fates`, `check-skill-tool-coverage`, `check-skill-fork-honesty`,
`check-skill-cli-invocations` and `check-skill-description-overlap` — each matching the
prefix that file already prints on its refusal line, so an argument rejection and a
containment refusal read as coming from the same source.

**D. `scripts/check-guard-fates.mjs` alone declares a boolean flag.** Pass its existing
`--json` flag through `booleanFlags` and read the result's flags object wherever the old
destructured value was used. The other four declare none, so any extra token is rejected
there. Verify by hand that the boolean flag works in BOTH orderings relative to the root
flag, and that neither ordering lets it be swallowed as the root's value.

**E. Wrap the parser call in its own error handler, separate from the existing containment
handler.** Print the script's own base name, then the error message, to stderr; exit 1.
Two separate handlers, two distinct message prefixes, one exit status — the convention four
of these files already state verbatim in the comment above their containment handler
("exit with the SAME code, so they are separated by their message"). Keep every existing
containment and typo diagnostic exactly as it is.

**F. Rewrite the comment block that sat above each deleted reader.** Four of them currently
claim the same argv shape as `scripts/audit-gate.mjs`'s own reader; that claim is now false
in a new way. Replace it with: which shared function is used; that the file has no reader
of its own by design (`IN-06` — the same reader was copy-pasted verbatim into six files and
the same defect shipped six times); and that `scripts/audit-gate.mjs` was deliberately not
migrated in this round (`WR-13`) because it carries five further flags no verifier finding
asks this round to touch.

**G. Touch nothing else in these five files.** In particular do not touch the static
imports at the top of `scripts/check-skill-tool-coverage.mjs`,
`scripts/check-skill-fork-honesty.mjs` or `scripts/check-skill-cli-invocations.mjs`. The
split-read defect those cause is plan 32-12's work and must land in its own commit, after
this one, so an argument regression and a split-read regression can never be confused for
one another. This is D-07's ordering constraint applied to the auditor itself.

Finish by running each of the five bare, with no arguments at all, and confirming each
behaves exactly as it did before. Record each exit status verbatim in the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools; for s in check-guard-fates check-skill-tool-coverage check-skill-fork-honesty check-skill-cli-invocations check-skill-description-overlap; do node scripts/$s.mjs >/dev/null 2>/dev/null; echo "$s bare=$?"; node scripts/$s.mjs --root=/tmp >/dev/null 2>/dev/null; echo "$s equals=$?"; node scripts/$s.mjs --rooot /tmp >/dev/null 2>/dev/null; echo "$s typo=$?"; done</automated>
  </verify>
  <acceptance_criteria>
    - Each of the five scripts run bare exits with the SAME status it did before this change (all five exit 0 at HEAD today; record each measured status in the SUMMARY).
    - For each of the five: the equals form exits non-zero, the typo form exits non-zero, and the valueless form exits non-zero. Measured baseline at HEAD for `check-guard-fates`: all three exit 0 with a full green `setA=43 setB=16 setC=2 total=61 rows=61` report.
    - `grep -c 'function parseArgs' scripts/check-guard-fates.mjs scripts/check-skill-tool-coverage.mjs scripts/check-skill-fork-honesty.mjs scripts/check-skill-cli-invocations.mjs scripts/check-skill-description-overlap.mjs` returns 0 for every file.
    - `grep -c parseRootArg` returns at least 2 for every one of the five files.
    - `node scripts/check-guard-fates.mjs --json` still emits JSON and exits 0; `node scripts/check-guard-fates.mjs --json --root .` and `node scripts/check-guard-fates.mjs --root . --json` both exit 0 and both emit JSON.
    - `node scripts/check-guard-fates.mjs --root /a --root /b` exits non-zero and its stderr names BOTH values.
    - `cd src/mcp/vice; npm run typecheck` exits 0.
    - `git diff --diff-filter=D --name-only` over this task's commit lists no file.
  </acceptance_criteria>
  <done>
Six root-accepting scripts, one argv reader between them. Every malformed root form the
verifier reproduced is refused by all six, and the blocking CI gate no longer answers a
mistyped root with a green report about a tree it was told not to read. The static imports
that cause the split read are untouched and are plan 32-12's work.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The six-script spawned matrix, plus the first proof that a contained root actually redirects a gate's reads</name>
  <files>src/mcp/vice/audit-root-args.test.ts, .gitignore</files>
  <read_first>
    - `src/mcp/vice/audit-root-args.test.ts` — the WHOLE file as plan 32-10 left it: the helper shape, the temporary-directory discipline, and the header convention that a file inside the removal gate's scope names the deleted subject by role and never by literal.
    - `scripts/check-skill-description-overlap.mjs` — `paths()` in full, especially the comment beginning "CLAUDE.md lives HERE, in paths()", which states in its own words the split-read failure this task's synthetic-tree case exists to disprove; `:159`'s note that every path below is the resolved root's; and the checks that read `paths().skillsDir` and `paths().claudeMd`.
    - `scripts/check-guard-fates.mjs` — the derivation's dependence on a git object store at the resolved root, so the contained-root case for this script is the repository root itself rather than an empty synthetic directory.
    - `.gitignore` — the whole file, to place the new entry with the surrounding conventions.
    - `src/mcp/vice/test-gate.mjs:95-112` — `MANUAL_ONLY_TESTS` and `automatedTestFiles()`, to confirm a new colocated test file joins the automated set with no list edit, and that `test-gate.test.ts`'s drift guard is satisfied by that alone.
  </read_first>
  <behavior>
    Tests to write BEFORE wiring, all driving the REAL scripts through spawned processes.

    For EACH of the six root-accepting scripts — `generate-tool-support-table`,
    `check-guard-fates`, `check-skill-tool-coverage`, `check-skill-fork-honesty`,
    `check-skill-cli-invocations`, `check-skill-description-overlap` — three cases, which
    is `gaps[1].missing[2]` stated verbatim:
    - equals-form root: exits non-zero, stderr carries the argument-rejection prefix.
    - out-of-repository root: exits 1, stderr carries `REFUSED`.
    - contained root: behaves as the script's own contract says (see below), NOT as a
      silent read of the real tree.

    Contained-root case, per script:
    - `check-guard-fates`: the repository root itself. Exits 0 with the same report as the
      unflagged run. This is the `[edge:CUT-04/adjacency]` boundary — a root exactly equal
      to the default root is accepted, not treated as a second tree.
    - `check-skill-description-overlap`: a synthetic in-repository tree, built so its
      answer DIFFERS from the real repository's. Assert the gate's output reflects the
      synthetic corpus. This is the case that proves the redirect is real rather than
      nominal.
    - the other four (`check-skill-tool-coverage`, `check-skill-fork-honesty`,
      `check-skill-cli-invocations`, `generate-tool-support-table`): a contained root that
      is not the default root must exit NON-ZERO with a diagnostic, and must NOT produce
      one of the gate's substantive findings and must NOT exit 0. Assert exactly that
      shape — non-zero plus a diagnostic — and nothing narrower. At HEAD these four report
      a missing-input TYPO diagnostic; after plan 32-12 settles the split read they refuse
      earlier and for a different reason. Both satisfy this assertion, so the case does not
      have to be rewritten between waves. Do not pin the specific message here; plan 32-12
      pins it.

    Plus, for the repeated-flag rule: the same script invoked with the root flag twice
    exits non-zero and names both values.
  </behavior>
  <action>
**A. Extend `src/mcp/vice/audit-root-args.test.ts`** with a table-driven block over the six
script base names, so a seventh root-accepting script added later fails the file by
omission rather than passing unnoticed. Each row carries the script's path, its expected
refusal-message prefix, and its contained-root expectation.

**B. Build the synthetic tree for the description-overlap case inside the repository**, not
under the system temporary directory — containment requires it. Create it with
`mkdtempSync` against a prefix of `.audit-root-synth-` at the repository root, populate a
minimal `src/skills/` corpus and a minimal `CLAUDE.md` whose project-skills table
deliberately disagrees with the real repository's, and remove the whole tree with
`rmSync({ recursive: true, force: true })` in a `finally` AND in a `t.after` hook, so a
thrown assertion cannot leave it behind.

Construct the synthetic corpus so the two answers are DISTINGUISHABLE: if the gate reads
the real repository's `CLAUDE.md` against the synthetic skills directory, the output must
differ from the output when both halves are synthetic. A case that passes under both reads
proves nothing, and that is exactly the shape of the defect being closed. Record in the
test's comment which observable difference discriminates the two.

**C. Add `.audit-root-synth-*` to `.gitignore`**, with a one-line comment naming the
reason: a crashed test that leaves the directory behind would otherwise dirty the working
tree, and a dirty tree VOIDS the mutation harness's evidence — `scripts/audit-mutation-harness.mjs:654`
refuses to record on one. Ignoring it makes a test crash a test failure rather than a
corruption of the next harness run.

**D. Keep the file fast.** Sixteen of the eighteen matrix cases are refusals that exit
before their script does any work. Only two perform a full run — the fate guard's
repository-root case and the description-overlap synthetic case. Do not add a second full
run of any script.

**E. Do not add the file to the manual-only list.** It joins the automated set by
existing in `src/mcp/vice/`, which is what `test-gate.test.ts`'s drift guard requires.

**F. Record the `[edge:CUT-04/ordering]` backstop honestly in the file's header.** State
that the repeated-flag half is asserted here, and that the set-order half — whether the
fate guard's verdict is independent of derived-set and registry row order — is NOT asserted
by this round, is recorded as a backstop in this plan's `must_haves`, and is a property of
code this round does not change. Do not imply coverage the file does not carry.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice; node --test audit-root-args.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice; node --test audit-root-args.test.ts` exits 0 and reports at least 18 spawned-process cases plus the plan 32-10 unit cases.
    - `grep -c 'audit-root-synth' .gitignore` returns at least 1.
    - After a full run of the file, `git status --porcelain` is byte-identical to its pre-run value, and no directory matching `.audit-root-synth-*` remains at the repository root.
    - The description-overlap synthetic case asserts an output string that the real repository's corpus does NOT produce — stated explicitly in the test's own comment, and demonstrated by the SUMMARY quoting both outputs.
    - `cd src/mcp/vice; npm run typecheck` exits 0.
    - `cd src/mcp/vice; npm run test:automated` reports 0 failures. State the broker's state in the SUMMARY before trusting the number: a live VICE broker deterministically reds the BACK-05 test and the `stock-broker-live` family, which is not a flake. Read what the run prints rather than trusting its exit summary alone. Do NOT run the full-glob `npm test` — it blocks forever on `vice-proxy.test.ts` and outlives any timeout.
    - `node scripts/check-no-regenerator2000.mjs` exits 0 and `grep -aic r2000 src/mcp/vice/audit-root-args.test.ts` returns 0.
  </acceptance_criteria>
  <done>
`gaps[1].missing[2]` is satisfied literally: one spawned-process test per root-accepting
script, covering a contained root, an out-of-repository refusal and the equals form. The
contained-root case is not a formality — for the one gate whose comparison data follows
the root, the test proves the redirect changes the answer.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator argv → audited gate | A root value crosses from a human-typed command line into the variable that decides which tree five gates read. |
| test fixture → repository working tree | The synthetic-tree case creates a directory INSIDE the repository, because containment requires it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-05 | Spoofing | `scripts/check-guard-fates.mjs` under a malformed root | high | mitigate | Reproduced at HEAD: the blocking CI gate answers an equals-form root, a typo'd flag and a valueless flag with a full green report about the REAL repository. A gate that reports success about a tree it was told not to read can be made to vouch for the wrong tree. Task 1 makes all three a named refusal; Task 2 pins each with a spawned-process assertion. |
| T-32-06 | Tampering | synthetic-tree fixture left behind by a crashed test | medium | mitigate | A leftover `.audit-root-synth-*` directory dirties the working tree, and a dirty tree voids the mutation harness's evidence at `scripts/audit-mutation-harness.mjs:654`. Mitigated twice over: removal in a `finally` AND in a `t.after` hook, plus a `.gitignore` entry so a leftover cannot register as an untracked change. |
| T-32-02 | Tampering / Elevation | `resolveContainedRoot()` lexical containment | medium | accept | `WR-04`, unchanged from plan 32-10's disposition: containment is lexical, so an in-repository symlink pointing outward defeats it. Accepted at ASVS L1 for the reason the module's own header records — resolving symlinks would make the function throw on a not-yet-created directory, which several callers legitimately pass. Below the `high` blocking threshold. Stays OPEN in the findings todo. |
| T-32-SC | Tampering | package-manager installs | — | not applicable | This plan runs no package-manager install and adds no dependency. Measured: `files_modified` contains no manifest and no lockfile. No package-legitimacy checkpoint is owed. |
</threat_model>

<verification>
1. The five bare runs, each exit status recorded verbatim against its pre-change value.
2. The eighteen matrix cases, from `node --test audit-root-args.test.ts`.
3. `node scripts/check-guard-fates.mjs --json`, and both orderings of the boolean flag with the root flag.
4. `cd src/mcp/vice; npm run typecheck` — exit 0.
5. `cd src/mcp/vice; npm run test:automated` — 0 failures, broker state recorded beside the number.
6. `node scripts/check-no-regenerator2000.mjs` — exit 0.
7. `git status --porcelain` before and after the test file's full run — byte-identical.
</verification>

<success_criteria>
- One argv reader serves all six root-accepting scripts; five local copies are gone.
- Every malformed root form is refused by every one of the six.
- The blocking CI gate's unflagged and JSON paths are unchanged.
- A contained root demonstrably redirects the one gate whose data can follow it.
- No relaxation hatch was added anywhere.
</success_criteria>

<artifacts_this_phase_produces>
Every symbol, path and flag this ROUND creates. Items are marked with the plan that
creates them, so the source-grounding pass excludes them from drift verification.

**New exported functions**
- `parseRootArg(argv, { script, booleanFlags })` — `scripts/lib/audit-root.mjs` (32-10)

**New files**
- `src/mcp/vice/audit-root-args.test.ts` (32-10; extended here and by 32-12)
- `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-gap2-root-seam-coverage.md` (32-12)
- `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-gap3-harness-correction.md` (32-14)

**New declarations**
- `parseRootArg` in `scripts/lib/audit-root.d.mts` (32-10)

**New document sections**
- `## 6.1 CUT-04's NAMED list, adjudicated name by name` in
  `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md` (32-13)
- A dated Gap-1 closure note beside `CUT-04` in `.planning/REQUIREMENTS.md` (32-13)
- A dated correction block appended to
  `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-sweep-renamed-rows.md` (32-14)

**New gitignore entries**
- `.audit-root-synth-*` (32-11, this plan)

**New CLI flags**
- None. This round adds no flag; it makes the existing root flag parse strictly.

**New registry fields**
- None. Plan 32-14 re-measures the values of two fields on ONE existing row; it adds no
  field and no row.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/32-the-deletion-and-the-grep-gate/32-11-SUMMARY.md` when done.
Record: each of the five bare-run exit statuses against its pre-change value; the eighteen
matrix results; both outputs from the description-overlap discrimination case; the broker's
state at the `test:automated` run; and `git status --porcelain` before and after.
</output>
