---
phase: 32-the-deletion-and-the-grep-gate
plan: 06
type: execute
wave: 4
depends_on: ["32-02", "32-03", "32-04"]
files_modified:
  - .planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json
  - .planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-sweep-renamed-rows.md
autonomous: true
requirements: [CUT-04]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All 16 renamed set-A members carry verdict `re-pointed`, a `newSubject` that exists on disk, a `plant` descriptor, a `guard` descriptor, and an `observedRed` whose `exitStatus` is a non-zero integer captured by the harness — never hand-written."
    - "Every observed red was produced against the member's NEW subject, not against its historical one: `D-04`'s distinction between `the guard asserts it can fail` and `the guard was observed failing against its new subject` is the claim CUT-04 asks for, and existing in-test non-vacuity assertions are used only as a map of where a reusable plant exists, never as the proof."
    - "`git status --porcelain` after the full harness run is byte-identical to its pre-run baseline, and `git diff --stat` over every planted target is empty — no plant survived the run that placed it."
    - "A member whose first plant did not produce a non-zero exit is recorded with BOTH attempts and the reason, and is either red by a second plant against the same new subject or surfaced as a FINDING that the guard may be vacuous — never silently reclassified to a verdict that owes no evidence."
    - "The evidence document carries the raw command, the raw captured output, the exit status, the plant applied, the broker state and the commit measured for every row — the `29-21-SUMMARY.md` shape D-15 names, which is the record this milestone's provenance corrections were able to re-derive from."
  prohibitions:
    - statement: "MUST NOT leave the working tree mutated after a harness run. Every plant is captured before it is written and restored idempotently on normal exit, on SIGINT, on SIGTERM and on an uncaught exception; a run that cannot restore fails loudly rather than continuing. A crashed harness leaving a plant in place would corrupt this phase's own close-gate run, which happens on that tree (D-04)."
  artifacts:
    - path: ".planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json"
      provides: "16 renamed set-A rows, each with plant, guard and machine-captured observedRed"
      contains: "observedRed"
    - path: ".planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-sweep-renamed-rows.md"
      provides: "Raw command, raw output, exit status, plant and broker state per renamed row"
      min_lines: 120
  key_links:
    - from: ".planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json"
      to: "scripts/audit-mutation-harness.mjs"
      via: "each row's plant + guard descriptor is the harness's only per-row input; the harness writes observedRed back"
      pattern: "\"plant\"|\"guard\"|\"observedRed\""
---

<!-- planner-discipline-allow: regenerator2000 -->
<!-- planner-discipline-allow: r2000 -->

<objective>
The sweep, first half: the 16 set-A members that survive under a NEW NAME. Each is verdict
`re-pointed` and each owes an observed red against its new subject (`D-05`).

This is the increment `CUT-04` adds over Phase 29's per-commit individual proofs: a mechanical sweep
over the set at once, measured after the dust settles. Phase 29 proved each re-pointed guard
non-vacuous individually, at the commit that moved it. `D-04` rejects reusing those in-test
assertions as the proof here — "the guard asserts it can fail" and "the guard was observed failing
against its new subject" are different claims, and `CUT-04` asks for the second.

Wave ordering is load-bearing and already satisfied: every `--root` addition to an audited guard
landed in wave 2 (plan 32-02), `PROJECT.md`'s repair landed in wave 2 (plan 32-03), and
`docs-linerefs.test.ts`'s widening landed in wave 3 (plan 32-04). This plan is the first that
MEASURES. `D-07` forbids the auditor modifying an audited guard in the same change that measures it;
nothing in this plan modifies any guard.

The 16, from the name-descendant map `CUT-01` and `29-VERIFICATION.md` established (authoritative over
git's `-M` heuristic, which misses five of them):

| # | historical path | new subject |
|---|---|---|
| 1 | `scripts/lib/r2000-cli-verbs.mjs` | `scripts/lib/anno-cli-verbs.mjs` |
| 2 | `scripts/lib/r2000-cli-verbs.d.mts` | `scripts/lib/anno-cli-verbs.d.mts` |
| 3 | `src/mcp/vice/docs-r2000-decisions.test.ts` | `src/mcp/vice/docs-absorbed-decisions.test.ts` |
| 4 | `src/mcp/vice/r2000-answer-key.test.ts` | `src/mcp/vice/absorbed-answer-key.test.ts` |
| 5 | `src/mcp/vice/r2000-spawn-seam.test.ts` | `src/mcp/vice/spawn-seam.test.ts` |
| 6 | `src/mcp/vice/r2000-upstream-audit.test.ts` | `src/mcp/vice/anno-derivation.test.ts` |
| 7 | `src/mcp/vice/r2000-cli.test.ts` | `src/mcp/vice/anno-cli.test.ts` |
| 8 | `src/mcp/vice/r2000-confidence.test.ts` | `src/mcp/vice/anno-confidence.test.ts` |
| 9 | `src/mcp/vice/r2000-coverage-grammar.test.ts` | `src/mcp/vice/anno-coverage-grammar.test.ts` |
| 10 | `src/mcp/vice/r2000-coverage.test.ts` | `src/mcp/vice/anno-coverage.test.ts` |
| 11 | `src/mcp/vice/r2000-d64.test.ts` | `src/mcp/vice/anno-d64.test.ts` |
| 12 | `src/mcp/vice/r2000-enum-gen.test.ts` | `src/mcp/vice/anno-enum-gen.test.ts` |
| 13 | `src/mcp/vice/r2000-memmap-render.test.ts` | `src/mcp/vice/anno-memmap-render.test.ts` |
| 14 | `src/mcp/vice/r2000-regbits.test.ts` | `src/mcp/vice/anno-regbits.test.ts` |
| 15 | `src/mcp/vice/r2000-tools.test.ts` | `src/mcp/vice/anno-tools.test.ts` |
| 16 | `src/mcp/vice/r2000-verb-coverage.test.ts` | `src/mcp/vice/anno-verb-coverage.test.ts` (**already done — the tracer row, plan 32-01**) |
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-CONTEXT.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-RESEARCH.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-01-SUMMARY.md
@.planning/phases/32-the-deletion-and-the-grep-gate/32-02-SUMMARY.md
@.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-root-override-inventory.md
@.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-audited-set-reconciliation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author plant and guard descriptors for the six structurally-named renames</name>
  <precondition>.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-root-override-inventory.md exists and records a plant route per script (delivered by plan 32-02)</precondition>
  <files>.planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json</files>
  <read_first>
    - `scripts/lib/anno-cli-verbs.mjs` and `scripts/lib/anno-cli-verbs.d.mts` — rows 1 and 2. The `.d.mts` has no runtime behaviour, so its only mechanical check is the typechecker
    - `src/mcp/vice/docs-absorbed-decisions.test.ts` — row 3. Read `:33`, `:121-126`, `:198-212`, `:216-227` for the assertions a plant can trip
    - `src/mcp/vice/absorbed-answer-key.test.ts` — row 4. It reads `.planning/phases/11-*/evidence/` with no existence guard; that is the second leg of the do-not-archive-phase-directories decision, and it is what a plant must exercise WITHOUT deleting a phase directory
    - `src/mcp/vice/spawn-seam.test.ts` — row 5, re-pointed onto the emulator spawn seam
    - `src/mcp/vice/anno-derivation.test.ts` — row 6, the registry-versus-manifest non-vacuity relation
    - `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-root-override-inventory.md` — the plant route per script, and the two fixture constraints it carries forward
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-RESEARCH.md` §2.3 — which of these already carry an in-test non-vacuity assertion (a map of where a reusable plant exists, NOT the proof)
    - `.planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json` — the tracer row's shape, as the schema to follow
  </read_first>
  <action>
Add six rows to `guard-fates.json` — rows 1 through 6 of the table in `<objective>` — each with
`historicalPath`, `verdict: "re-pointed"`, `newSubject`, `removalTrigger`, a `plant` descriptor and a
`guard` descriptor. Leave `observedRed` unset; Task 3's harness run fills it.

Design each plant against the member's NEW subject by reading that file and choosing the smallest
mutation that makes a real assertion fail. Prefer a single-token change to a constant or a pinned
literal over deleting a block — a smaller plant is easier to restore and harder to get wrong. Concrete
targets to consider per row:
  - **Row 1** (`anno-cli-verbs.mjs`): the tracer already plants `ANNO_CLI_VERB_FLOOR`. Choose a
    DIFFERENT mutation for this row so the two rows are independent evidence — for example a verb name
    in the parsed table, which `anno-verb-coverage.test.ts`'s coverage relation asserts against the
    skill playbooks.
  - **Row 2** (`anno-cli-verbs.d.mts`): remove one exported declaration. Its guard descriptor is the
    typecheck shape — `argv: ["--run", "typecheck"]`, `cwd: "src/mcp/vice"` — because a declaration file
    has no runtime behaviour and the typechecker is the only thing that can observe its absence.
  - **Row 3** (`docs-absorbed-decisions.test.ts`): plant against `.planning/PROJECT.md`'s D-36 row —
    remove one of the four things `:204-212` asserts (`supersedes D-32`, `handler.rs:1894`, `issues/42`,
    a reversal-trigger phrase). `.planning/` is outside the removal gate's scope, so the plant cannot
    disturb an exact-count pin; but it IS a working-tree mutation of a file plan 32-03 just repaired, so
    the capture-before-write and restore path must be exercised carefully.
  - **Row 4** (`absorbed-answer-key.test.ts`): plant against a value the answer key asserts, NEVER by
    deleting or moving a `.planning/phases/11-*/evidence/` directory. That directory is a design input
    to this milestone and `--no-archive-phases` is a standing constraint.
  - **Row 5** (`spawn-seam.test.ts`): plant against the emulator spawn seam module it asserts over.
  - **Row 6** (`anno-derivation.test.ts`): plant against one side of the registry-versus-manifest
    relation so the relation, not a hard-coded number, is what breaks.

Route choice: use `kind: "root"` with a synthetic tree wherever the target script accepts `--root`
per the inventory; otherwise `kind: "worktree"`. Put any synthetic fixture tree under
`.planning/phases/32-the-deletion-and-the-grep-gate/fixtures/`, NOT under
`src/mcp/vice/fixtures/planted-` — that prefix carries an exact `prefixHits: 2` pin in the removal
gate and a new file there reds it, and `src/mcp/vice/fixtures/README.md` carries a single pinned
`gate-self` occurrence that a new index row would move.

Write a `note` on every row recording WHY that plant was chosen and which assertion it is expected to
trip, so a later reader can tell a deliberate minimal plant from an arbitrary one. Do not run the
harness in this task.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node -e 'const r=require("./.planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json");const rows=r.rows.filter(x=>x.verdict==="re-pointed");if(rows.length<7)process.exit(1);for(const x of rows){if(!x.plant||!x.guard||!x.newSubject||!x.removalTrigger)process.exit(1);}console.log("descriptors ok",rows.length)' && node scripts/check-no-regenerator2000.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `guard-fates.json` contains rows for all six historical paths listed as rows 1-6 in `<objective>`, each with `verdict: "re-pointed"`, a non-empty `newSubject` that exists on disk, a non-empty `removalTrigger`, a `plant` object and a `guard` object.
    - Every `plant.kind` is exactly `root` or `worktree`, and every `root`-kind plant names a fixture path under `.planning/phases/32-the-deletion-and-the-grep-gate/fixtures/`.
    - `git status --porcelain -- src/mcp/vice/fixtures/` is empty — no new file was added under the pinned `planted-` prefix, and `src/mcp/vice/fixtures/README.md` is unchanged.
    - Row 2's `guard.argv` is the typecheck invocation with `cwd: "src/mcp/vice"`, reflecting that a declaration file has no runtime observer.
    - Row 1's plant target differs from the tracer row's `ANNO_CLI_VERB_FLOOR` mutation, so the two rows are independent evidence.
    - Row 4's plant does not delete, move or rename any path under `.planning/phases/`; `git status --porcelain -- .planning/phases/11-` is empty after the task.
    - Every row carries a `note` naming the assertion the plant is expected to trip.
    - `node scripts/check-no-regenerator2000.mjs` exits 0 and `cd src/mcp/vice && npm run test:automated` exits 0 with 0 failures — authoring descriptors changed no behaviour.
  </acceptance_criteria>
  <done>Six structurally-named renamed members each carry a designed, minimal plant against their new subject, with the assertion it is expected to trip recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Author plant and guard descriptors for the nine capability-test renames</name>
  <files>.planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json</files>
  <read_first>
    - The nine new subjects, each read for a pinned constant or a relation a one-token plant can break: `src/mcp/vice/anno-cli.test.ts`, `anno-confidence.test.ts`, `anno-coverage-grammar.test.ts`, `anno-coverage.test.ts`, `anno-d64.test.ts`, `anno-enum-gen.test.ts`, `anno-memmap-render.test.ts`, `anno-regbits.test.ts`, `anno-tools.test.ts`
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-RESEARCH.md` §2.3 — of these, `anno-confidence` (via `anno-derive`), `anno-coverage`, `anno-coverage-grammar`, `anno-regbits` and `anno-tools` (via `anno-register`) already carry an in-test non-vacuity assertion and therefore already have a reusable plant shape; `anno-d64`, `anno-enum-gen` and `anno-memmap-render` need one designed from scratch
    - `src/mcp/vice/anno-memmap-render.ts` — the NUL-taint at offset 15097 / line 315. Any plant that reads or rewrites this file must be byte-safe: use `readFileSync` and a byte-preserving write, never a text tool that normalises, or the restore will not be byte-identical
    - `.planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json` — the schema as extended by Task 1
  </read_first>
  <action>
Add nine rows — rows 7 through 15 of the table in `<objective>` — in the same shape Task 1 established:
`historicalPath`, `verdict: "re-pointed"`, `newSubject`, `removalTrigger`, `plant`, `guard`, `note`.
Leave `observedRed` unset.

For the five that already carry an in-test non-vacuity assertion, read that assertion and plant the
SAME violation against the REAL subject rather than inventing a new one — research names this as the
cheapest correct route, and it keeps the plant provably relevant. Do not, however, cite the in-test
assertion as the evidence: `D-04` requires an observed red from the harness run, and the in-test
assertion is only a map of where the plant already exists in reusable form.

For `anno-d64`, `anno-enum-gen` and `anno-memmap-render`, design a plant from scratch: find a pinned
constant, a fixture-derived expectation or a declared shape the test asserts, and mutate the smallest
token that breaks it.

`anno-memmap-render` needs specific care. Its production module carries two NUL bytes, the first at
offset 15097 (line 315). If the plant target is that module, the harness's capture-and-restore must
operate on raw bytes: `readFileSync` without an encoding, and a write of the exact captured Buffer on
restore. A text-mode round-trip can normalise the NULs and leave the file changed after a "successful"
restore, which would silently corrupt this phase's own close-gate run. Prefer planting against the
TEST's own fixture or a pinned constant rather than against the NUL-tainted module, and record that
choice in the row's `note`.

Keep every plant minimal and independently restorable. Where two rows would plant against the same
file, give them different targets so a failure in one does not mask the other.

Route choice and fixture location follow Task 1: synthetic trees live under
`.planning/phases/32-the-deletion-and-the-grep-gate/fixtures/`, never under
`src/mcp/vice/fixtures/planted-`.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node -e 'const r=require("./.planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json");const rows=r.rows.filter(x=>x.verdict==="re-pointed");if(rows.length<16)process.exit(1);const t=new Set(rows.map(x=>x.plant.file));if(t.size!==rows.length)console.error("shared plant targets:",rows.length-t.size);for(const x of rows){if(!x.plant||!x.guard||!x.note)process.exit(1);}console.log("descriptors ok",rows.length)' && git status --porcelain -- src/mcp/vice/fixtures/ | wc -l | grep -qx 0 && echo DESCRIPTORS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `guard-fates.json` now contains 16 `re-pointed` rows covering every historical path in `<objective>`'s table, each with `plant`, `guard`, `newSubject`, `removalTrigger` and `note`.
    - For each of the five members research identifies as already carrying an in-test non-vacuity assertion, the row's `note` names that assertion and states that the plant reuses its violation against the real subject — and states that the in-test assertion is NOT the evidence.
    - For `anno-d64`, `anno-enum-gen` and `anno-memmap-render`, the `note` describes a plant designed from scratch and names the pinned constant, fixture expectation or declared shape it breaks.
    - The `anno-memmap-render` row's `note` records whether the plant target is the NUL-tainted module, and if it is, that capture and restore are byte-mode (`readFileSync` with no encoding, Buffer write on restore).
    - No two rows share a `plant.file` target, or where they do, the row `note` states why the two targets cannot mask each other.
    - `git status --porcelain -- src/mcp/vice/fixtures/` is empty.
    - `node scripts/check-no-regenerator2000.mjs` exits 0 and `cd src/mcp/vice && npm run test:automated` exits 0 with 0 failures.
  </acceptance_criteria>
  <done>All 16 renamed members carry a designed plant against their new subject, with the reused-versus-designed provenance recorded per row.</done>
</task>

<task type="auto">
  <name>Task 3: Run the harness over all 16 renamed rows, capture the observed reds, and record the raw evidence</name>
  <precondition>the vice-broker systemd user unit is inactive — `systemctl --user is-active vice-broker` reports `inactive` and `pgrep -af vice-broker` returns nothing; a live broker makes the BACK-05 D-G ordering test red deterministically and would make a red ambiguous (D-13). Do NOT stop the unit: assert and record only.</precondition>
  <files>.planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json, .planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-sweep-renamed-rows.md</files>
  <read_first>
    - `scripts/audit-mutation-harness.mjs` — as delivered by plan 32-01: the `NODE_TEST_*` strip, the argv-array spawn, the capture-before-write, the idempotent `restoreAll()` on `exit`/`SIGINT`/`SIGTERM`/`uncaughtException`, and the `assertTreeClean()` baseline comparison
    - `.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-tracer-observed-red.md` — the evidence shape to follow, as established by the tracer
    - `.planning/phases/29-the-mcp-surface/29-21-SUMMARY.md` — the record `D-15` names as the model: every measurement command and its raw output, which is what this milestone's provenance corrections were able to re-derive from
    - `.planning/phases/32-the-deletion-and-the-grep-gate/32-RESEARCH.md` §7.5 — the landmines, particularly the four pre-existing untracked files the tree-clean baseline must tolerate and the `/tmp` tmpfs leak
  </read_first>
  <action>
Run `node scripts/audit-mutation-harness.mjs --all` restricted to this plan's 16 rows, on a clean tree,
with the broker asserted down.

Before the run: capture `systemctl --user is-active vice-broker`, `pgrep -af vice-broker`,
`git rev-parse HEAD` and `git status --porcelain`. Record all four verbatim in the evidence document —
`D-13` makes broker-down an ASSERTED, RECORDED precondition, never a prose instruction and never a
scripted kill. The broker runs as a systemd unit on this host and an audit script that tears down the
developer's environment is the wrong shape. If the broker is active, HALT and surface it rather than
stopping it.

For every row, the harness writes back `observedRed = { command, cwd, plant, exitStatus, excerpt }`
with `exitStatus` captured from the spawn, never typed by hand. A row whose guard returns exit 0 under
its plant is a HARD FAILURE of the run, not a warning: "a guard that cannot be made to fail has not
been re-pointed."

For any row that does not red on the first plant:
  1. Record the first attempt in full — the plant, the command, the raw output and the exit 0.
  2. Design a SECOND plant against the SAME `newSubject`, mutating a different assertion, and re-run.
  3. If the second plant reds, keep both attempts in the evidence and record in the row's `note` why
     the first did not bite. A plant that did not bite is information about the guard.
  4. If no plant against the new subject produces a red, do NOT reclassify the row to a verdict that
     owes no evidence and do NOT add an exemption — this repo's gate design carries no relaxation
     hatches. Record it as an explicit FINDING: this guard may be vacuous against its new subject, with
     both attempts, and surface it in the SUMMARY as a phase-level result requiring a decision.

Write `evidence/32-sweep-renamed-rows.md` in `29-21-SUMMARY.md`'s shape: per row, the raw command as
executed, the raw captured stdout and stderr (not a summary, not a table of exit codes), the exit
status, the plant applied, and the `git status --porcelain` readings before and after. Head the document
with the broker state, the commit measured and the harness version, and close it with the post-run tree
state.

Clean up after the run: research §7.5 records twelve leaked `.planning/vice-proxy-evidence-test-*`
directories from prior suite runs and notes that `/tmp` is a 16GB RAM tmpfs with aging disabled. Remove
any directory this run leaked and record what was removed.

Do not modify any guard, any script or any document outside the registry and the evidence file. This
plan MEASURES; wave 2 and wave 3 were where modification was allowed.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node -e 'const r=require("./.planning/phases/32-the-deletion-and-the-grep-gate/guard-fates.json");const rows=r.rows.filter(x=>x.verdict==="re-pointed");let bad=rows.filter(x=>!x.observedRed||!Number.isInteger(x.observedRed.exitStatus)||x.observedRed.exitStatus===0||!x.observedRed.command||!x.observedRed.excerpt);if(bad.length){console.error("rows without a captured non-zero red:",bad.map(b=>b.historicalPath));process.exit(1)}console.log("observed reds:",rows.length)' && git status --porcelain | grep -v '^?? docs/dissambler-workflow.md$' | grep -v '^?? docs/undocumented-opcodes-ghidra.md$' | grep -v '^?? docs/vice-mcp-ideas.md$' | grep -v '^?? skills-lock.json$' | grep -v '^ M \.planning/' | grep -v '^?? \.planning/' | wc -l | grep -qx 0 && echo SWEEP_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - All 16 `re-pointed` rows carry an `observedRed` with an integer, non-zero `exitStatus`, a non-empty `command`, a non-empty `cwd`, the `plant` applied, and a non-empty `excerpt` of the failing assertion.
    - All 16 rows carry `observedRed.control.exitStatus === 0` — the unplanted guard was green before the plant was applied. A red with no green control is recorded as UNMEASURABLE with the control's raw output, never as an observed red: a guard that is red for an unrelated reason, or one whose run TIMES OUT (which the harness's fail-closed error mapping reports as status 1), would otherwise record a false red — the exact vacuity CUT-04 exists to catch.
    - Every `observedRed.command` names the row's `newSubject` (or the guard that asserts over it), never the row's `historicalPath` — the red was observed against the NEW subject.
    - `git status --porcelain` after the run, filtered to tracked non-`.planning` paths, is empty: no plant survived, and `git diff --stat` over every `plant.file` is empty.
    - The evidence document records `systemctl --user is-active vice-broker` reporting `inactive`, the `pgrep -af vice-broker` output, the measured commit from `git rev-parse HEAD`, and the pre- and post-run `git status --porcelain` readings, all verbatim.
    - The evidence document carries the raw captured stdout/stderr per row, not a summary table of exit codes.
    - Any row that needed a second plant carries BOTH attempts in the evidence and a `note` explaining why the first did not bite. Any row that could not be red at all is recorded as an explicit FINDING and surfaced in the SUMMARY — not reclassified and not exempted.
    - `cd src/mcp/vice && npm run test:automated` exits 0 with 0 failures after the run, proving the tree was restored.
    - `node scripts/check-no-regenerator2000.mjs` exits 0 and `node scripts/audit-gate.mjs --json` reports `allowed: true`, `redGuards: []`.
    - No `.planning/vice-proxy-evidence-test-*` directory created by this run remains; the SUMMARY records what was removed.
  </acceptance_criteria>
  <done>Every renamed set-A member has been observed exiting non-zero against its new subject under a recorded plant, the tree is byte-identical to its pre-run state, and the raw evidence re-derives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry plant descriptors -> filesystem writes | Row-supplied `file`/`find`/`replace` values decide what is mutated in the working tree |
| registry guard descriptors -> subprocess argv | Row-supplied `argv`/`cwd` values decide what is executed |
| harness process lifetime -> the tree the close gate later measures | An interrupted run leaves state the phase's own close gate would then measure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-22 | Tampering | plant descriptors reaching filesystem writes | high | mitigate | Every `plant.file` is resolved through `resolveContainedRoot()` before any write; original bytes are captured first; restore is idempotent and registered on `exit`/`SIGINT`/`SIGTERM`/`uncaughtException`; `git status --porcelain` is compared against the process-start baseline before and after every row |
| T-32-23 | Tampering | guard descriptors reaching subprocess argv | high | mitigate | `argv` is spawned as an ARRAY with `shell` never set; no element is an interpolated shell string; no descriptor value is evaluated or dynamically imported |
| T-32-24 | Repudiation | a planted red recorded as a green | high | mitigate | Every `NODE_TEST_*` key is stripped from the child env before spawning; a zero exit under a plant is a hard failure of the run; `exitStatus` is captured from the spawn result and the guard rejects a zero or absent value |
| T-32-25 | Tampering | byte-normalising restore of the NUL-tainted module | high | mitigate | Capture and restore for `anno-memmap-render.ts` are byte-mode (`readFileSync` with no encoding, Buffer write); the plan prefers a non-NUL plant target and requires the choice be recorded |
| T-32-26 | Elevation | tearing down the developer's broker unit | medium | mitigate | Broker state is READ ONLY (`systemctl --user is-active`, `pgrep -af`); an active broker HALTS the run rather than being stopped (`D-13`) |
| T-32-27 | Denial of Service | `/tmp` tmpfs exhaustion from leaked evidence directories | low | mitigate | Leaked `.planning/vice-proxy-evidence-test-*` directories created by this run are removed and the removal is recorded |
| T-32-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no packages and invokes no package manager; the only npm invocation is the existing `typecheck` script run as a guard |
</threat_model>

<verification>
- All 16 `re-pointed` rows carry a machine-captured, non-zero `observedRed`.
- `git status --porcelain` over tracked non-`.planning` paths is empty after the run.
- `cd src/mcp/vice && npm run test:automated` exits 0 with 0 failures; `npm run typecheck` exits 0.
- `node scripts/check-no-regenerator2000.mjs` exits 0; `node scripts/audit-gate.mjs --json` reports `allowed: true`.
- `node scripts/check-guard-fates.mjs` still exits with the pre-declared incomplete-registry red, now naming fewer missing members.
- The evidence document records broker state, commit measured, and raw output per row.
</verification>

## Artifacts this phase produces

See the table in `32-01-PLAN.md` → "Artifacts this phase produces" — the canonical list of every
symbol, file and CI step Phase 32 creates. This plan adds
`.planning/phases/32-the-deletion-and-the-grep-gate/evidence/32-sweep-renamed-rows.md` and, only where
a `root`-kind plant needs one, fixture trees under
`.planning/phases/32-the-deletion-and-the-grep-gate/fixtures/`.

<success_criteria>
- Every one of the 16 renamed members was observed exiting non-zero against its NEW subject under a recorded plant.
- No in-test non-vacuity assertion was accepted as the proof.
- The working tree is byte-identical to its pre-run state and the whole automated suite is still at zero failures.
- Every measurement re-derives from the raw command and raw output in the evidence document.
- Any member that could not be red is an explicit surfaced finding, not a reclassification and not an exemption.
</success_criteria>

<output>
Create `.planning/phases/32-the-deletion-and-the-grep-gate/32-06-SUMMARY.md` when done
</output>
