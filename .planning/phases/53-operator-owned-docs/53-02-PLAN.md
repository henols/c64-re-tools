---
phase: 53-operator-owned-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/vice/probe-binmon.mjs
  - src/mcp/vice/binmon-fixtures.ts
  - src/mcp/vice/fixtures/binmon/README.md
  - src/mcp/vice/fixtures/ghidra/README.md
  - src/mcp/vice/fixtures/dxa/README.md
  - src/mcp/vice/fixtures/petcat/README.md
  - src/mcp/vice/fixtures/export-asm/README.md
  - src/mcp/vice/fixtures/hazard-subject/hazard-subject-align-nosprite.a
  - src/mcp/vice/fixtures/hazard-subject/make-exported-edit.mjs
  - src/mcp/vice/fixtures/ghidra/charset-phantom.annostore.json
  - src/mcp/vice/fixtures/ghidra/bank-path-dependent.annostore.json
autonomous: true
requirements: [DOCS-03]

estimate:
  tokens: 60000
  raw_tokens: 120000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Every one of the 32 evidence-document path citations in the probe script, the binary-monitor fixture module and the eleven fixture-tree files is gone, replaced by the reason it stood for (roadmap criterion 4)"
    - "No replaced site names ANY path in its place — repointing at a relocated path fails criterion 4 exactly as leaving the original does (locked decision L4)"
    - "Two of the 32 sites are NOT comments but runtime strings — the probe script's own operator-facing hint text and a note field written into a generated fixture record — and both are rewritten as text a reader without the repository can act on"
    - "Three of the 32 sites sit inside fixture DATA that live tests consume, and the edits touch only the citation clause: the production-emitted decline reason quoted inside the bank-path fixture survives byte-for-byte because a live assertion matches it by regex"
    - "Each rewritten site carries more reasoning than the pointer it replaced: no file's diff removes more lines than it adds, and the plan's aggregate adds strictly more (roadmap criterion 5, locked decision L5)"
    - "`tsc --noEmit` is clean, every touched JSON file still parses, and the automated gate is green — no fixture consumer broke"
    - "NOT MOVED HERE: no document under `docs/` is relocated, created or deleted by this plan (locked decision L3 defers all moves to 53-04)"
  artifacts:
    - src/mcp/vice/probe-binmon.mjs
    - src/mcp/vice/binmon-fixtures.ts
    - src/mcp/vice/fixtures/binmon/README.md
    - src/mcp/vice/fixtures/ghidra/README.md
    - src/mcp/vice/fixtures/dxa/README.md
    - src/mcp/vice/fixtures/petcat/README.md
    - src/mcp/vice/fixtures/export-asm/README.md
    - src/mcp/vice/fixtures/hazard-subject/hazard-subject-align-nosprite.a
    - src/mcp/vice/fixtures/hazard-subject/make-exported-edit.mjs
    - src/mcp/vice/fixtures/ghidra/charset-phantom.annostore.json
    - src/mcp/vice/fixtures/ghidra/bank-path-dependent.annostore.json
  key_links:
    - "None of these eleven files is published by `src/mcp/vice/package.json`'s `files[]`, but all eleven are physically under `src/**`, which is the scope roadmap criterion 4's grep names. RESEARCH.md Q3 settles this: the criterion carries no npm-publication qualifier, and the whole fixture tree is packed into the plugin zip regardless."
    - "`charset-phantom.annostore.json` and `bank-path-dependent.annostore.json` are read by `anno-export-asm.test.ts` and `anno-decomp-closure.test.ts`; `hazard-subject-align-nosprite.a` is read by `hazard-subject-variants.test.ts`, `reassembly-gate-modified-run.test.ts` and two fixture generators. A careless edit to fixture data reds tests that have nothing to do with this phase."
---

<!-- planner-discipline-allow: docs/phase -->

<objective>
Rewrite all 32 evidence-document path citations across the live probe script, the
binary-monitor fixture module and the nine fixture-tree files that carry them, so each states
the reason it stood for instead of naming a file.

Purpose: these eleven files are the maintainer-facing half of `src/**`. They are not in
either npm tarball, but roadmap criterion 4's grep scope is `src/**` without a publication
qualifier, and the whole tree is packed into the plugin zip. Two of the sites are not
comments at all — they are strings a running program prints or writes — so leaving them
would ship a dangling reference into a program's own output.

Output: eleven modified files, zero evidence-document path citations among them, every
fixture consumer still green.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ENGINEERING_RULES.md
@.planning/phases/53-operator-owned-docs/RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite the 16 citations in the probe script and the binary-monitor fixture module</name>
  <files>src/mcp/vice/probe-binmon.mjs, src/mcp/vice/binmon-fixtures.ts</files>
  <read_first>
    RESEARCH.md section "Q6: What does a rewritten citation look like?" — three worked
    before/after pairs. The third one is directly relevant here: it shows a comment that
    cites BOTH an evidence document and this probe script, and keeps the probe-script
    reference while removing the document pointer.

    ENGINEERING_RULES section 21.2 is the rule being applied.
  </read_first>
  <action>
    Ten sites in `probe-binmon.mjs` (measured at lines 12, 13, 38, 56, 127, 277, 1231, 1879,
    2088 and 2179) and six in `binmon-fixtures.ts` (lines 8, 12, 60, 85, 153 and 154).
    Re-measure with `grep -an 'docs/phase'` over both files rather than trusting those
    numbers.

    Apply the same three-property rewrite 53-01 establishes, and read 53-01's summary first
    if it has landed so the voice matches:
    (a) state WHAT was established and AGAINST WHAT — for this pair that is almost always a
    recorded live run against a genuine unpatched emulator, or the emulator's own source;
    (b) name ZERO paths in place of the one removed (locked decision L4);
    (c) come out LONGER than what it replaced (locked decision L5).

    THREE sites need handling beyond a plain comment rewrite:

    `probe-binmon.mjs` line 2088 is NOT a comment. It is an element of an operator-facing
    hint string this script PRINTS when a capability probe comes back unsupported. A reader
    of that output has a terminal, not the repository. Rewrite it so the printed sentence
    states the operative fact — that the version floor binds the binary-monitor opcode rather
    than a build-time flag, and that the same capability is reachable by another route on an
    older build — and remove the trailing pointer entirely. Nothing asserts this string's
    text (checked: no test matches it), so the only constraint is that the printed sentence
    must stand on its own.

    `binmon-fixtures.ts` lines 8 and 12 are the file's WHY header — the sentence that says
    why the file exists at all. That reason must survive and grow, not shrink. The fact it
    carries is that the wire format is fixed independently of any live emulator and that the
    fixtures let the framing be exercised with no emulator and no display present. Write the
    mechanism; drop the pointer.

    `binmon-fixtures.ts` lines 153-154 cite a captured display geometry by its recorded
    numbers and a body layout by two document sections. The numbers are already inline in the
    comment and stay exactly as they are — they are the evidence. Replace only the pointers,
    saying what the numbers are and how they were obtained.

    Comments and one printed string only. No expression, numeric literal, exported name or
    type changes in this task.
  </action>
  <verify>
    <automated>test "$(grep -rn 'docs/phase' src/mcp/vice/probe-binmon.mjs src/mcp/vice/binmon-fixtures.ts | wc -l)" = 0 && NS="$(git diff --numstat -- src/mcp/vice/probe-binmon.mjs src/mcp/vice/binmon-fixtures.ts )" && echo "$NS" | awk '{ if ($1 < $2) { printf "FAIL shrank %s: +%s -%s\n", $3, $1, $2; bad=1 } } END { exit bad }' && npm --prefix src/mcp/vice run typecheck && node --check src/mcp/vice/probe-binmon.mjs</automated>
  </verify>
  <done>Both files carry zero evidence-document path citations, neither diff removes more lines than it adds, `tsc --noEmit` is clean and the probe script still parses under `node --check`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite the 10 citations across the five fixture README files</name>
  <files>src/mcp/vice/fixtures/binmon/README.md, src/mcp/vice/fixtures/ghidra/README.md, src/mcp/vice/fixtures/dxa/README.md, src/mcp/vice/fixtures/petcat/README.md, src/mcp/vice/fixtures/export-asm/README.md</files>
  <action>
    Ten sites: `binmon/README.md` (3, measured at lines 23, 30 and 128), `ghidra/README.md`
    (3, lines 498, 514 and 544), `dxa/README.md` (2, lines 198 and 211),
    `petcat/README.md` (1, line 131) and `export-asm/README.md` (1, line 80). Re-measure
    before editing.

    These are prose documents describing what each fixture is and how it was produced, so the
    rewrite is more natural here than in a code comment: the citation is nearly always
    standing in for a transcript of the run that produced the fixture. Write what that run
    established. Several sites say "see X for the full transcript" — a reader of the fixture
    tree cannot see it, so state the finding the transcript carried and drop the sentence
    that sends them looking.

    `binmon/README.md` line 30 names an original override record. That record's CONTENT — what
    was overridden and why — is what matters; write it out.

    Same three properties as Task 1, and the same hard rule: no path replaces the path
    (locked decision L4), and every rewrite is longer than what it replaced (locked
    decision L5). Markdown links elsewhere in these files that point at real sibling fixture
    files stay untouched.
  </action>
  <verify>
    <automated>test "$(grep -rn 'docs/phase' src/mcp/vice/fixtures/binmon/README.md src/mcp/vice/fixtures/ghidra/README.md src/mcp/vice/fixtures/dxa/README.md src/mcp/vice/fixtures/petcat/README.md src/mcp/vice/fixtures/export-asm/README.md | wc -l)" = 0 && NS="$(git diff --numstat -- src/mcp/vice/fixtures/binmon/README.md src/mcp/vice/fixtures/ghidra/README.md src/mcp/vice/fixtures/dxa/README.md src/mcp/vice/fixtures/petcat/README.md src/mcp/vice/fixtures/export-asm/README.md )" && echo "$NS" | awk '{ if ($1 < $2) { printf "FAIL shrank %s: +%s -%s\n", $3, $1, $2; bad=1 } } END { exit bad }'</automated>
  </verify>
  <done>All five fixture README files carry zero evidence-document path citations and none shrank.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Rewrite the 6 citations inside fixture data files, then gate the whole plan</name>
  <files>src/mcp/vice/fixtures/hazard-subject/hazard-subject-align-nosprite.a, src/mcp/vice/fixtures/hazard-subject/make-exported-edit.mjs, src/mcp/vice/fixtures/ghidra/charset-phantom.annostore.json, src/mcp/vice/fixtures/ghidra/bank-path-dependent.annostore.json</files>
  <action>
    Six sites, and every one of them sits inside data that a live test reads. Handle each in
    the way its consumer requires.

    `hazard-subject-align-nosprite.a` (2 sites, measured lines 15 and 27) — assembler source
    comments introduced by a semicolon, each naming the finding that justified a planted
    hazard at a specific anchor address. Comments do not affect the assembled output, and no
    test pins a hash of this file, so a plain comment rewrite is safe. Keep the anchor
    addresses exactly as written; replace only the pointer, stating what the finding at that
    anchor WAS.

    `make-exported-edit.mjs` (1 site, line 372) — NOT a comment. It is the value of a note
    field this generator writes into its output record, so the text travels into generated
    data. Nothing asserts the string (checked: no test matches it). Rewrite it so it states,
    on its own terms, that byte-identity with the modified subject was never the acceptance
    criterion and was not aimed for, and what WAS being demonstrated instead. Do not change
    the field name and do not change any other field.

    `bank-path-dependent.annostore.json` (1 site, line 85) — the citation sits inside an
    annotation `text` value, immediately after a QUOTED production-emitted decline reason.
    That quoted reason is matched by a live regex assertion in `anno-bank.test.ts` against
    what the production code emits. Edit ONLY the citation clause that follows it. The quoted
    reason itself must come through byte-for-byte; if the two are hard to separate, leave the
    quoted reason alone and rewrite around it.

    `charset-phantom.annostore.json` (2 sites, lines 3138 and 6715) — annotation `text`
    values on two function records, each citing a finding letter from a derivation transcript.
    Write the finding out: what was measured, and what the two simultaneous readings of the
    same bytes were. The structured parts of these `text` values (the `function:` /
    `inputs:` / `outputs:` / `side effects:` segments and their separators) are a parsed
    convention — keep the segment structure and the separators exactly as they are, and edit
    only inside the prose of the segment that carries the citation.

    Both JSON files must still parse and must keep their existing formatting style — edit in
    place, do not reserialize the document, and do not reorder or reindent anything.

    Then gate the whole plan: confirm zero citations across all eleven files, confirm the
    aggregate diff adds strictly more lines than it removes, and run the automated suite once
    (this plan's single test run — it is the one that proves the fixture consumers survived).
  </action>
  <verify>
    <automated>test "$(grep -rn 'docs/phase' src/mcp/vice/probe-binmon.mjs src/mcp/vice/binmon-fixtures.ts src/mcp/vice/fixtures | wc -l)" = 0 && node -e 'for (const f of ["src/mcp/vice/fixtures/ghidra/charset-phantom.annostore.json","src/mcp/vice/fixtures/ghidra/bank-path-dependent.annostore.json"]) { JSON.parse(require("fs").readFileSync(f,"utf8")); } console.log("json ok")' && grep -c 'no recovered processor-port value reaches' src/mcp/vice/fixtures/ghidra/bank-path-dependent.annostore.json && NS="$(git diff --numstat -- src/mcp/vice/probe-binmon.mjs src/mcp/vice/binmon-fixtures.ts src/mcp/vice/fixtures )" && echo "$NS" | awk '{ i+=$1; d+=$2; if ($1 < $2) { printf "FAIL shrank %s\n", $3; bad=1 } } END { if (i <= d) { printf "FAIL net shrink: +%d -%d\n", i, d; bad=1 } exit bad }' && npm --prefix src/mcp/vice run typecheck && npm --prefix src/mcp/vice run test:automated</automated>
  </verify>
  <done>All eleven files this plan owns carry zero evidence-document path citations; both annotation stores still parse and the quoted production decline reason is still present verbatim; the aggregate diff adds strictly more lines than it removes with no single file shrinking; `tsc --noEmit` is clean and `npm run test:automated` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture data -> test oracle | Two annotation stores and one assembler source are inputs to live tests. Editing them changes what those tests are measured against. |
| probe script -> operator terminal | One edited site is a string the probe PRINTS. Its reader has output, not the repository. |
| generator -> generated record | One edited site is a note value written into a produced fixture record, so the text propagates into data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-02-01 | Tampering | `bank-path-dependent.annostore.json` annotation text | high | mitigate | The citation sits beside a quoted production-emitted reason that `anno-bank.test.ts` matches by regex. Task 3 forbids touching the quoted clause and the verify gate greps for it explicitly after the edit. |
| T-53-02-02 | Tampering | `charset-phantom.annostore.json` segmented annotation text | medium | mitigate | The `function:`/`inputs:`/`outputs:`/`side effects:` segmentation is parsed. Task 3 confines edits to prose inside one segment and forbids reserialization; `npm run test:automated` exercises the consumers. |
| T-53-02-03 | Tampering | both annotation stores as JSON documents | medium | mitigate | An in-place edit can produce invalid JSON or silently reformat the file. Verify gate parses both with `JSON.parse` and the diff gate would show a whole-file reformat as a mass deletion. |
| T-53-02-04 | Information disclosure | the probe script's printed hint string | low | mitigate | The current string sends an operator to a file they may not have. Rewriting it to state the fact removes the misdirection rather than adding exposure. |
| T-53-02-05 | Repudiation | provenance of fixture derivation | medium | accept | Removing the transcript pointer removes the route back to the run that produced each fixture. Accepted on roadmap criterion 4's terms; the finding travels with the fixture where the pointer did not. |
| T-53-02-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package and adds no dependency. |
</threat_model>

<verification>
- `grep -rn 'docs/phase'` over `probe-binmon.mjs`, `binmon-fixtures.ts` and the whole `fixtures/` tree returns zero lines.
- Both annotation stores parse with `JSON.parse`, and the quoted production decline reason is still present verbatim in the bank-path store.
- `node --check src/mcp/vice/probe-binmon.mjs` exits 0.
- No file's diff removes more lines than it adds; the aggregate adds strictly more.
- `npm --prefix src/mcp/vice run typecheck` exits 0 and `npm --prefix src/mcp/vice run test:automated` is green.
- No file under `docs/` was moved, created or deleted by this plan.
</verification>

<success_criteria>
Thirty-two of the phase's eighty-three citation sites are closed across the maintainer-facing
half of `src/**`, including the two that were runtime strings rather than comments, with
every fixture consumer still green.
</success_criteria>

<output>
Create `.planning/phases/53-operator-owned-docs/53-02-SUMMARY.md` when done.
Record the per-file counts before and after, and quote in full the rewritten probe-script
hint string and the rewritten generator note field — the two sites that are not comments.
</output>
