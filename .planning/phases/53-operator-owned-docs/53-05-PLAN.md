---
phase: 53-operator-owned-docs
plan: 05
type: execute
wave: 3
depends_on: [53-04]
files_modified:
  - .planning/phases/01-corrected-ground-truth/evidence/phase0-binmon-findings.md
  - .planning/phases/01-corrected-ground-truth/evidence/phase1-probe-results.md
  - .planning/phases/02-stock-backend-connection/evidence/phase2-backend-probe-evidence.md
  - .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/phase23-real-release-gate-findings.md
  - .planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/evidence/phase33-reproducible-run-gate-findings.md
  - .planning/phases/34-the-host-tool-execution-seam/evidence/phase34-host-tool-seam-decisions.md
  - .planning/phases/36-the-sleigh-language-and-the-ghidra-harness/evidence/phase36-sleigh-language-and-harness-findings.md
  - .planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/phase39-dual-channel-coexistence-gate-findings.md
  - .planning/phases/40-the-three-preprocessing-host-tools/evidence/phase40-preprocessing-tools-decisions.md
  - .planning/phases/41-the-text-channel-its-serialization-authority-and-the-content/evidence/phase41-text-channel-live-evidence.md
  - .planning/phases/42-the-text-format-parsers-and-their-two-binary-fixtures/evidence/phase42-text-format-drift-citations.md
  - .planning/phases/43-the-runtime-evidence-layer/evidence/phase43-runtime-evidence-layer.md
  - .planning/phases/45-decomposition-to-closure-disagreement-first/evidence/phase45-closure-gate.md
  - .planning/phases/45-decomposition-to-closure-disagreement-first/evidence/phase45-closure-ghidra-family.md
  - .planning/phases/45-decomposition-to-closure-disagreement-first/evidence/phase45-derivation-execution-evidence.md
  - .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/phase49-the-reassembly-gate-findings.md
  - .planning/phases/50-equivalence-and-modifiability/evidence/phase50-ci-boundary.md
  - .planning/phases/50-equivalence-and-modifiability/evidence/phase50-equivalence-transcript.md
  - .planning/phases/50-equivalence-and-modifiability/evidence/phase50-exported-edit-findings.md
  - .planning/phases/50-equivalence-and-modifiability/evidence/phase50-exported-modifiability-transcript.md
  - .planning/phases/50-equivalence-and-modifiability/evidence/phase50-modifiability-findings.md
  - .planning/phases/50-equivalence-and-modifiability/evidence/phase50-modifiability-transcript.md
  - docs/roadmap-stock-vice.md
  - docs/stock-vice-parity.md
  - CLAUDE.md
autonomous: true
requirements: [DOCS-01]

USE_WORKTREES_FOR_PLAN: false
worktree_reason: "This plan's deliverable is edits to 22 files under `.planning/phases/**/evidence/`. A worktree-isolated run strips planning-tree files from the commit, so the plan would report success while delivering nothing — the same silent failure 23-11-PLAN.md records having been observed in this project. Dispatch sequentially on the main working tree."

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Every one of the 98 references that the relocation stranded now resolves: 87 cross-references among the relocated documents themselves, 5 in the two operator-authored documents that stayed behind, and 6 in the project's top-level instruction file"
    - "Each repaired reference names the document's real location under `.planning/phases/NN-<slug>/evidence/`, and a mechanical check opens every referenced path and confirms the file is there — a reference that does not resolve fails this plan"
    - "Repointing is CORRECT here and forbidden in `src/**`: ENGINEERING_RULES section 21.3 bans a planning-tree path from PRODUCT source because it is dead on arrival for a consumer. None of these 25 files is product source — 22 of them live inside the planning tree, and the plugin archive packs that tree, so the path resolves for every reader who can reach the citing file at all"
    - "No reasoning is deleted in the repair: this is a path substitution, so every touched file's line count is unchanged and its diff is a one-for-one replacement (roadmap criterion 5, locked decision L5)"
    - "All five live Phase 53 success criteria are demonstrated mechanically at the end: an operator-owned `docs/`, 27 documents relocated with `git mv`, the orphan decision recorded, a zero scan across `src/` and `scripts/` with nothing repointed there, and no net deletion of explanatory comments"
    - "NOT BUILT: no guard, continuous-integration check, lint rule, pre-commit hook, note, seed or todo proposing one. Criterion 6 was withdrawn on 2026-09-17 and `DOCS-04` closes as SUPERSEDED (locked decision L1)"
  artifacts:
    - .planning/phases/50-equivalence-and-modifiability/evidence/phase50-ci-boundary.md
    - .planning/phases/45-decomposition-to-closure-disagreement-first/evidence/phase45-closure-gate.md
    - docs/roadmap-stock-vice.md
    - docs/stock-vice-parity.md
    - CLAUDE.md
  key_links:
    - "The 27-row source-to-destination table recorded in `53-04-SUMMARY.md` is the substitution map this plan drives. Do not re-derive it from filenames — three of the rows are pre-roadmap orphans whose destination is not their filename number."
    - "`scripts/package.sh` builds the plugin archive with `git archive HEAD`, which packs the whole tracked tree including the planning tree. That is why a planning-tree path resolves for a plugin-archive reader and why repointing is the right repair for these 25 files and the wrong one for the 34 files under `src/**`."
---

<!-- planner-discipline-allow: docs/phase -->

<objective>
Repair the 98 references that plan 53-04's relocation stranded — 87 among the relocated
documents themselves, 5 in the two operator documents that stayed in `docs/`, and 6 in the
project's top-level instruction file — then demonstrate all five live Phase 53 success
criteria mechanically.

Purpose: 53-04 moved 27 files and left every reference to them pointing at an address that no
longer exists. That is breakage this phase caused, not new scope, and leaving it would make
roadmap criterion 2's "relocated document" a document nobody can navigate to. The repair is a
path substitution rather than a reason rewrite because, unlike the 83 sites in `src/**`, all
25 of these files are read by someone who has the planning tree.

Output: every stranded reference resolving to a real file, and a recorded pass on criteria
1 through 5.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ENGINEERING_RULES.md
@.planning/phases/53-operator-owned-docs/RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Repoint the 87 cross-references among the 22 relocated documents that carry them</name>
  <files>.planning/phases/01-corrected-ground-truth/evidence/phase0-binmon-findings.md, .planning/phases/01-corrected-ground-truth/evidence/phase1-probe-results.md, .planning/phases/02-stock-backend-connection/evidence/phase2-backend-probe-evidence.md, .planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/phase23-real-release-gate-findings.md, .planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/evidence/phase33-reproducible-run-gate-findings.md, .planning/phases/34-the-host-tool-execution-seam/evidence/phase34-host-tool-seam-decisions.md, .planning/phases/36-the-sleigh-language-and-the-ghidra-harness/evidence/phase36-sleigh-language-and-harness-findings.md, .planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/phase39-dual-channel-coexistence-gate-findings.md, .planning/phases/40-the-three-preprocessing-host-tools/evidence/phase40-preprocessing-tools-decisions.md, .planning/phases/41-the-text-channel-its-serialization-authority-and-the-content/evidence/phase41-text-channel-live-evidence.md, .planning/phases/42-the-text-format-parsers-and-their-two-binary-fixtures/evidence/phase42-text-format-drift-citations.md, .planning/phases/43-the-runtime-evidence-layer/evidence/phase43-runtime-evidence-layer.md, .planning/phases/45-decomposition-to-closure-disagreement-first/evidence/phase45-closure-gate.md, .planning/phases/45-decomposition-to-closure-disagreement-first/evidence/phase45-closure-ghidra-family.md, .planning/phases/45-decomposition-to-closure-disagreement-first/evidence/phase45-derivation-execution-evidence.md, .planning/phases/49-the-reassembly-gate-committed-before-the-phase-it-gates/evidence/phase49-the-reassembly-gate-findings.md, .planning/phases/50-equivalence-and-modifiability/evidence/phase50-ci-boundary.md, .planning/phases/50-equivalence-and-modifiability/evidence/phase50-equivalence-transcript.md, .planning/phases/50-equivalence-and-modifiability/evidence/phase50-exported-edit-findings.md, .planning/phases/50-equivalence-and-modifiability/evidence/phase50-exported-modifiability-transcript.md, .planning/phases/50-equivalence-and-modifiability/evidence/phase50-modifiability-findings.md, .planning/phases/50-equivalence-and-modifiability/evidence/phase50-modifiability-transcript.md</files>
  <precondition>All 27 documents are already at their new addresses under `.planning/phases/*/evidence/` and `docs/` holds no phase-prefixed file — plan 53-04 has landed. If any document is still in the old location, HALT: this plan repairs references to the new addresses and would write paths that do not yet resolve.</precondition>
  <action>
    Measured 2026-09-17, before the move: 87 stranded references across 22 of the 27 relocated
    documents, naming 23 distinct targets, all of which exist. Re-measure after the move
    rather than trusting those numbers.

    This is a PATH SUBSTITUTION, not a reason rewrite, and the distinction is deliberate. The
    83 sites in `src/**` had to become reasons because their reader is a consumer who has only
    the product. These 22 files live INSIDE the planning tree — anyone who can open one can
    open its neighbour — so the repair that serves that reader is a working link. Do not
    rewrite the surrounding prose, do not add or remove reasoning, and do not reflow anything.
    Every touched file must come out with exactly the line count it went in with.

    Build the substitution map from the 27-row source-to-destination table recorded in
    `53-04-SUMMARY.md`. Do NOT re-derive destinations from filenames: three rows are
    pre-roadmap orphans whose destination has nothing to do with the number in their name.

    Apply the map with a scripted in-place substitution over the 22 files — one rule per row,
    replacing the old address with the new one and changing nothing else on the line. Work
    from the longest filename to the shortest so no rule is a prefix of another, or anchor
    each rule on the trailing file extension so partial matches cannot occur.

    Then prove every reference resolves. Extract every planning-tree evidence path now present
    in the 22 files, and open each one. A path that does not resolve means a map row was wrong
    or a substitution was partial; fix the map, do not hand-patch the output.
  </action>
  <verify>
    <automated>test "$(grep -arn 'docs/phase' .planning/phases/*/evidence/phase*.md | wc -l)" = 0 && grep -aoh '\.planning/phases/[^ )`'"'"'"]*evidence/phase[a-z0-9._-]*\.md' .planning/phases/*/evidence/phase*.md | sort -u | while read -r p; do test -f "$p" || { echo "UNRESOLVED: $p"; exit 1; }; done && NS="$(git diff --numstat -- .planning/phases )" && echo "$NS" | awk '{ if ($1 != $2) { printf "FAIL line-count changed %s: +%s -%s\n", $3, $1, $2; bad=1 } } END { exit bad }'</automated>
  </verify>
  <done>The 22 relocated documents that carried cross-references now carry zero old-address references; every planning-tree evidence path they name opens to a real file; and every touched file's diff is a one-for-one line replacement, so no reasoning was added or lost.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Repair the 5 references in the two operator documents and the 6 in the top-level instruction file</name>
  <files>docs/roadmap-stock-vice.md, docs/stock-vice-parity.md, CLAUDE.md</files>
  <action>
    Eleven stranded references remain outside both `src/**` and the relocated set:
    `docs/roadmap-stock-vice.md` (2) and `docs/stock-vice-parity.md` (3) — two
    operator-authored documents that correctly STAY in `docs/` — and `CLAUDE.md` (6
    occurrences across 4 lines, measured at lines 28, 33, 37 and 43).

    Apply the same substitution map and the same discipline as Task 1: replace the address,
    change nothing else on the line, leave every line count unchanged.

    Why repointing is right here and wrong in `src/**`, stated so nobody reverses it later:
    ENGINEERING_RULES section 21.3 bans a planning-tree path from PRODUCT source on the
    measurement that such a path is dead on arrival for a consumer who installed a package.
    None of these three files is product source. `CLAUDE.md` is this repository's own working
    instruction file, read by someone with the whole checkout, and it already cites the
    engineering-rules document by path for the same reason. The two operator documents ship
    only in the plugin archive, which `scripts/package.sh` builds with `git archive HEAD` —
    that packs the whole tracked tree including the planning tree, so the repaired path
    resolves for exactly the reader who could reach the citing document in the first place.

    In `CLAUDE.md`, do not touch anything else on those four lines. Each is a dense, dated,
    measured constraint bullet whose wording has been revised repeatedly; the only change is
    the address inside the backticks. Do not reflow, do not shorten, do not "tidy" a bullet.
  </action>
  <verify>
    <automated>test "$(grep -arn 'docs/phase' docs/roadmap-stock-vice.md docs/stock-vice-parity.md CLAUDE.md | wc -l)" = 0 && grep -aoh '\.planning/phases/[^ )`'"'"'"]*evidence/phase[a-z0-9._-]*\.md' docs/roadmap-stock-vice.md docs/stock-vice-parity.md CLAUDE.md | sort -u | while read -r p; do test -f "$p" || { echo "UNRESOLVED: $p"; exit 1; }; done && NS="$(git diff --numstat -- docs/roadmap-stock-vice.md docs/stock-vice-parity.md CLAUDE.md )" && echo "$NS" | awk '{ if ($1 != $2) { printf "FAIL line-count changed %s: +%s -%s\n", $3, $1, $2; bad=1 } } END { exit bad }'</automated>
  </verify>
  <done>Both operator documents and the top-level instruction file carry zero old-address references, every planning-tree evidence path they name opens to a real file, and all three diffs are one-for-one line replacements.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Demonstrate all five live Phase 53 success criteria and close the phase</name>
  <files>.planning/phases/53-operator-owned-docs/53-05-SUMMARY.md</files>
  <action>
    Stop any running emulator broker before measuring anything — a live broker deterministically
    reds one broker test, and a suite result taken with one up proves nothing either way.

    Then produce the evidence for each of the five live criteria and record the literal command
    output in the summary. Do not paraphrase a result; paste it.

    Criterion 1 — an operator-owned documentation directory. List it. No file whose name begins
    with the phase prefix, and no subfolder inside it. Show the full listing so a reader can see
    what IS there.

    Criterion 2 — every relocated document in the phase artifact tree with its history intact.
    Count the relocated set (27) and run a history-following log on at least three of them, one
    from the oldest group, one from the middle and one from the newest, showing commits that
    predate this phase.

    Criterion 3 — the orphan destination decided and recorded. Print the Phase 53 roadmap
    section and show it naming all three pre-roadmap documents with their destinations and the
    evidence that resolved them.

    Criterion 4 — zero across `src/` and `scripts/`, with nothing repointed there. Two checks,
    both required: the old-address scan returns zero, AND a scan of the same scope for a
    planning-tree evidence path introduced by this phase returns zero. The second is the one
    that catches "the same defect one hop along", and it is the reason criterion 4 is not
    satisfied by the first check alone.

    Criterion 5 — no net deletion of explanatory comments. Resolve this phase's base commit as
    the most recent commit whose subject does NOT carry a phase-53 scope, sanity-check that it
    really is the commit before this phase began, and run a line-count diff over `src/` from
    there to the current head. Insertions must exceed deletions. Cross-check the total against
    the per-file numbers the four earlier plan summaries recorded; if the two disagree, the base
    commit was resolved wrongly — say so rather than reporting the number.

    Finally, state explicitly in the summary what this phase did NOT build: no guard, no
    continuous-integration check, no lint rule, no pre-commit hook, and no note, seed or todo
    proposing one. Criterion 6 was withdrawn on 2026-09-17 and `DOCS-04` closes as SUPERSEDED,
    not met, with the recurrence risk accepted and named in the roadmap. Do not add one to be
    helpful; the standing decisions that withdrew it forbid a successor document as explicitly
    as they forbid the guard itself.
  </action>
  <verify>
    <automated>test "$(ls docs/ | grep -c '^phase')" = 0 && test "$(find docs -mindepth 1 -type d | wc -l)" = 0 && test "$(find .planning/phases -path '*/evidence/phase*.md' -type f | wc -l)" = 27 && test "$(grep -rn 'docs/phase' src scripts | wc -l)" = 0 && test "$(grep -rn '\.planning/phases/[^ ]*evidence/phase' src scripts | wc -l)" = 0 && test "$(awk '/^### Phase 53:/,/^### Phase 54:/' .planning/ROADMAP.md | grep -c 'SUPERSEDED')" -ge 1 && npm --prefix src/mcp/vice run typecheck && npm --prefix src/mcp/vice test</automated>
    <human-check>Read the five criterion sections of the summary. Confirm each carries literal command output rather than a claim, that the criterion-5 base commit was sanity-checked rather than assumed, and that the summary states plainly that no recurrence guard was built and none is proposed.</human-check>
  </verify>
  <done>All five live criteria are demonstrated with pasted command output: `docs/` holds no phase-prefixed file and no subfolder; 27 documents sit under `.planning/phases/*/evidence/` with history-following logs proving pre-phase commits for three of them; the roadmap names all three orphans with destinations and evidence; both scans over `src/` and `scripts/` return zero, the second proving nothing was repointed; and the line-count diff over `src/` from this phase's base adds more than it removes. `tsc --noEmit` is clean and the full suite is green with no broker running. The summary states what was deliberately not built.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| citing document -> cited document | Every repaired reference is an assertion that a file is at an address. A wrong address is a silent false statement inside an evidence record. |
| repair script -> 25 files at once | A scripted substitution touches every file in one pass; a wrong map row propagates everywhere before anyone reads a single result. |
| phase gate -> project record | The criteria evidence recorded here is what a later reader and a later audit will trust about this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-05-01 | Tampering | the 98 repaired references | high | mitigate | A wrong or partial substitution writes an address that does not exist into an evidence record. Both repair tasks end by extracting every planning-tree evidence path from the files they touched and opening each one; an unresolved path fails the task. |
| T-53-05-02 | Tampering | prose surrounding a repaired reference | medium | mitigate | A scripted pass that reflows or rewrites is indistinguishable from a repair in a large diff. Both tasks require an exactly unchanged line count per file, which a reflow or a rewrite cannot satisfy. |
| T-53-05-03 | Tampering | the four dense measured bullets in the top-level instruction file | high | mitigate | Those bullets encode hard-won protocol and capability constraints and have been revised repeatedly. Task 2 confines the edit to the address inside the backticks and forbids reflowing or shortening; the line-count gate catches any structural change. |
| T-53-05-04 | Spoofing | the criterion-5 measurement | medium | mitigate | A wrongly resolved base commit produces a confident but meaningless insertions-versus-deletions number. Task 3 requires the base be sanity-checked and cross-checked against the four earlier plan summaries, and requires reporting the discrepancy rather than the number when they disagree. |
| T-53-05-05 | Spoofing | a green suite measured against a contaminated environment | medium | mitigate | Task 3 requires the emulator broker be stopped before the suite is measured, because a live one deterministically reds a broker test. |
| T-53-05-06 | Elevation of privilege | the withdrawn recurrence guard | medium | accept | Nothing mechanical prevents a future plan writing evidence into the operator's directory again. Accepted by the owner on 2026-09-17; recorded in the roadmap rather than mitigated, and Task 3 forbids adding a guard or a successor document to close it. |
| T-53-05-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package and adds no dependency. |
</threat_model>

<verification>
- No file among the 22 relocated documents, the two operator documents or the top-level instruction file carries an old-address reference.
- Every planning-tree evidence path named in those 25 files opens to a real file.
- Every touched file's diff is a one-for-one line replacement — identical line counts in and out.
- `docs/` holds no phase-prefixed file and no subfolder; exactly 27 documents sit under `.planning/phases/*/evidence/`.
- Both scans over `src/` and `scripts/` return zero: the old address form, and the planning-tree evidence path form.
- The Phase 53 roadmap section records `DOCS-04` as SUPERSEDED.
- `npm --prefix src/mcp/vice run typecheck` exits 0 and the full suite is green with no broker running.
- No guard, continuous-integration check, lint rule, pre-commit hook, note, seed or todo proposing one exists anywhere in this phase's output.
</verification>

<success_criteria>
Nothing the relocation stranded is left dangling, every repaired reference opens, and all five
live Phase 53 success criteria are demonstrated with command output rather than asserted.
</success_criteria>

<output>
Create `.planning/phases/53-operator-owned-docs/53-05-SUMMARY.md` when done.
Record the literal output for each of the five criteria, the count of references repaired per
group, and an explicit statement that no recurrence guard was built and none is proposed.
</output>
