---
phase: 53-operator-owned-docs
plan: 04
type: execute
wave: 2
depends_on: [53-01, 53-02, 53-03]
files_modified:
  - docs/phase0-binmon-findings.md
  - docs/phase1-probe-results.md
  - docs/phase2-backend-probe-evidence.md
  - docs/phase23-real-release-gate-findings.md
  - docs/phase33-reproducible-run-gate-findings.md
  - docs/phase34-host-tool-seam-decisions.md
  - docs/phase36-sleigh-language-and-harness-findings.md
  - docs/phase39-dual-channel-coexistence-gate-findings.md
  - docs/phase40-preprocessing-tools-decisions.md
  - docs/phase41-text-channel-live-evidence.md
  - docs/phase42-text-format-drift-citations.md
  - docs/phase43-instrumentation-perturbation-ab.md
  - docs/phase43-runtime-evidence-layer.md
  - docs/phase45-closure-dxa-family.md
  - docs/phase45-closure-gate.md
  - docs/phase45-closure-ghidra-family.md
  - docs/phase45-derivation-execution-evidence.md
  - docs/phase45-ghidra-derivation-evidence.md
  - docs/phase45-planted-control-evidence.md
  - docs/phase45-wave0-measurements.md
  - docs/phase49-the-reassembly-gate-findings.md
  - docs/phase50-ci-boundary.md
  - docs/phase50-equivalence-transcript.md
  - docs/phase50-exported-edit-findings.md
  - docs/phase50-exported-modifiability-transcript.md
  - docs/phase50-modifiability-findings.md
  - docs/phase50-modifiability-transcript.md
  - src/mcp/vice/phase50-findings-contract.test.ts
  - src/mcp/vice/phase50-transcript-freshness.test.ts
  - .planning/ROADMAP.md
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03, DOCS-04]

USE_WORKTREES_FOR_PLAN: false
worktree_reason: "Three independent reasons, any one of which is sufficient. (1) This plan's deliverable is 27 files moved INTO `.planning/phases/**/evidence/` plus an edit to `.planning/ROADMAP.md`; a worktree-isolated run strips planning-tree files from the commit, so the plan would report success while delivering nothing — the failure mode 23-11-PLAN.md records having been observed in this project. (2) The diff necessarily contains 27 deletions (the source side of each rename), and `cleanup-wave` refuses any branch whose diff contains a deletion, unconditionally. (3) The move and the two guard-test path constants must land in ONE commit; splitting them across an isolation boundary leaves the automated gate red in between. Dispatch this plan sequentially on the main working tree."

estimate:
  tokens: 38000
  raw_tokens: 76000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "`ls docs/` lists only operator-authored documents: no file whose name begins with the phase prefix remains, and no subfolder was created inside `docs/` to hold them — the owner rejected an evidence subfolder by name on 2026-09-13 (roadmap criterion 1, locked decision L2)"
    - "All 27 relocated documents sit at `.planning/phases/NN-<slug>/evidence/`, the convention the majority of phase directories already follow (roadmap criterion 2, DOCS-01)"
    - "Every relocation was performed with `git mv`, and `git log --follow` on a moved document returns commits that predate this phase — history survived (roadmap criterion 2, locked decision L6)"
    - "The three documents with no phase directory of their own — the binary-monitor findings, the probe results and the backend probe evidence — land at destinations decided from direct evidence and RECORDED in `.planning/ROADMAP.md`, not left implicit (roadmap criterion 3, DOCS-02)"
    - "Both phase-50 guard tests read their subject documents from the new location, changed in the SAME commit as the move, and both pass: `phase50-findings-contract.test.ts` and `phase50-transcript-freshness.test.ts` each hold a real runtime path constant, not a citation"
    - "`grep -rn` for the evidence-document path form across `src/` and `scripts/` still returns zero after the move — nothing was repointed at a relocated path to make a check pass (roadmap criterion 4, locked decision L4)"
    - "`.planning/ROADMAP.md` records `DOCS-04` as SUPERSEDED with its reason, and the phase ships NO guard, CI grep, lint rule or pre-commit hook for this path form — criterion 6 was withdrawn on 2026-09-17 and the recurrence risk is accepted and named (locked decision L1)"
    - "The full suite is green after the move, with no live emulator broker running while it is measured"
  artifacts:
    - .planning/phases/01-corrected-ground-truth/evidence/phase0-binmon-findings.md
    - .planning/phases/01-corrected-ground-truth/evidence/phase1-probe-results.md
    - .planning/phases/02-stock-backend-connection/evidence/phase2-backend-probe-evidence.md
    - .planning/phases/45-decomposition-to-closure-disagreement-first/evidence/phase45-closure-gate.md
    - .planning/phases/50-equivalence-and-modifiability/evidence/phase50-ci-boundary.md
    - src/mcp/vice/phase50-findings-contract.test.ts
    - src/mcp/vice/phase50-transcript-freshness.test.ts
    - .planning/ROADMAP.md
  key_links:
    - "The two phase-50 guard tests are the ONLY functional (non-citation) dependency on the old location in the whole repository. Their path constant and the `git mv` must be one commit: repointing first fails on an empty directory, moving first reds both guards."
    - "Repointing those two constants adds two more consumers to the set of tests that read a committed artifact by its phase-directory path — `anno-derivation.test.ts` and `host-scripts.test.ts` already do this. That set is what makes archiving phase directories at milestone close unsafe, and this plan grows it by two."
---

<!-- planner-discipline-allow: docs/phase -->

<objective>
Move all 27 relocatable evidence documents out of `docs/` into their phase directories'
`evidence/` convention with `git mv`, repoint the two guard tests that actually READ them in
the same commit, and record the orphan-destination decision and `DOCS-04`'s superseded
disposition in the roadmap.

Purpose: this is the move the whole phase exists for, and it is deliberately the LAST thing
that happens. Moving before the citations were rewritten would have left 83 sites pointing at
files that no longer exist, and "fixing" them by repointing at the new location is the same
defect one hop along — which is why 53-01, 53-02 and 53-03 all had to land first.

Output: an operator-owned `docs/`, 27 documents with their history intact in the phase
artifact tree, a green suite, and a roadmap that records what was decided.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ENGINEERING_RULES.md
@.planning/phases/53-operator-owned-docs/RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the six missing evidence directories, `git mv` all 27 documents, and repoint both guard tests in the same commit</name>
  <files>docs/phase0-binmon-findings.md, docs/phase1-probe-results.md, docs/phase2-backend-probe-evidence.md, docs/phase23-real-release-gate-findings.md, docs/phase33-reproducible-run-gate-findings.md, docs/phase34-host-tool-seam-decisions.md, docs/phase36-sleigh-language-and-harness-findings.md, docs/phase39-dual-channel-coexistence-gate-findings.md, docs/phase40-preprocessing-tools-decisions.md, docs/phase41-text-channel-live-evidence.md, docs/phase42-text-format-drift-citations.md, docs/phase43-instrumentation-perturbation-ab.md, docs/phase43-runtime-evidence-layer.md, docs/phase45-closure-dxa-family.md, docs/phase45-closure-gate.md, docs/phase45-closure-ghidra-family.md, docs/phase45-derivation-execution-evidence.md, docs/phase45-ghidra-derivation-evidence.md, docs/phase45-planted-control-evidence.md, docs/phase45-wave0-measurements.md, docs/phase49-the-reassembly-gate-findings.md, docs/phase50-ci-boundary.md, docs/phase50-equivalence-transcript.md, docs/phase50-exported-edit-findings.md, docs/phase50-exported-modifiability-transcript.md, docs/phase50-modifiability-findings.md, docs/phase50-modifiability-transcript.md, src/mcp/vice/phase50-findings-contract.test.ts, src/mcp/vice/phase50-transcript-freshness.test.ts</files>
  <precondition>A repository-wide scan of `src/` and `scripts/` for the evidence-document path form returns zero matches — the three wave-1 plans have all landed. If it returns anything, HALT and report: moving now would strand every remaining site, which is the exact failure the phase ordering exists to prevent.</precondition>
  <reversibility rating="costly">A rename is revertible with a second `git mv`, but every later reference written against the new location has to be found and reverted too, and the two guard tests would have to be repointed back in lockstep. Doing it once, correctly, in one commit is materially cheaper than undoing it.</reversibility>
  <action>
    Before starting, stop any running emulator broker. A live broker deterministically reds
    one broker test and would make the post-move suite result meaningless.

    STEP 1 — Verify the destination map against reality before moving anything. Twenty-seven
    documents map onto fourteen phase directories:

    - the binary-monitor findings and the probe results, to phase directory 01;
    - the backend probe evidence, to phase directory 02;
    - the real-release-gate findings, to phase directory 23;
    - the reproducible-run-gate findings, to phase directory 33;
    - the host-tool-seam decisions, to phase directory 34;
    - the sleigh-language and harness findings, to phase directory 36;
    - the dual-channel-coexistence-gate findings, to phase directory 39;
    - the preprocessing-tools decisions, to phase directory 40;
    - the text-channel live evidence, to phase directory 41;
    - the text-format drift citations, to phase directory 42;
    - the instrumentation perturbation record and the runtime-evidence-layer document, both
      to phase directory 43;
    - all seven documents whose names begin with the phase-45 prefix, to phase directory 45;
    - the reassembly-gate findings, to phase directory 49;
    - all six documents whose names begin with the phase-50 prefix, to phase directory 50.

    Resolve each phase directory by its number prefix under `.planning/phases/` rather than
    typing a slug from memory — several slugs are long and truncated. Assert you resolved
    exactly fourteen distinct directories and that each resolution matched exactly one
    directory, and assert the source list is exactly 27 files.

    The first three are the orphans: they predate the roadmap by a day and have no phase
    directory named after them. Their destinations are NOT derived from their filenames.
    RESEARCH.md Q1 resolved both by direct evidence — phase directory 01's plan 01-04 lists
    the first two in its own `files_modified` frontmatter, and the backend probe evidence
    document's own opening names the plan and the decision record it belongs to. VERIFY both
    before moving: open `.planning/phases/01-corrected-ground-truth/01-04-PLAN.md` and
    confirm the two filenames appear in its frontmatter, and open the backend probe evidence
    document's first dozen lines and confirm it names phase directory 02's context document.
    If either check fails, HALT and report rather than guessing.

    STEP 2 — Create the six evidence directories that do not exist yet: under phase
    directories 01, 02, 40, 41, 42 and 45. The other eight already have one. Re-check rather
    than trusting that list.

    STEP 3 — Move every document with `git mv` and nothing else. Never copy-then-delete: the
    audit chain from the run that produced a document to the document itself is what makes it
    evidence, and a copy severs it (locked decision L6, roadmap criterion 2).

    KEEP EVERY FILENAME EXACTLY AS IT IS. Do not strip the phase prefix, do not renumber, do
    not rename for tidiness. Two of the guard tests discover their subjects by filename
    prefix and by a filename pattern; a rename would break discovery silently. Criterion 2
    constrains the destination directory, not the filename.

    Create no subfolder inside `docs/` and leave nothing behind there but operator-authored
    documents (roadmap criterion 1, locked decision L2).

    STEP 4 — In the SAME commit, repoint the two guard tests. Each declares a constant
    resolving the repository's documentation directory and then reads real files from it.
    Change each to resolve the phase-50 evidence directory instead. Both read only phase-50
    documents, so both point at the same destination.

    While there, fix the surrounding prose in those two files that asserts where the
    documents live: a comment stating which other documents the old directory also holds, a
    parameter doc-comment describing the directory the transcripts are discovered in, and a
    test title claiming the old directory holds at least two transcripts. Those describe a
    location that is no longer true. Leave every temporary-directory fixture in those files
    alone — the tests that build a scratch tree to exercise failure modes are unaffected by
    this move and must keep building it the way they do.

    On the constant itself, write a comment saying WHY the path is what it is: these two
    guards read committed artifacts out of the phase artifact tree, so renaming or archiving
    that phase directory breaks them. Two sibling tests already read a committed artifact the
    same way, so this is an established shape here rather than a new one — but it does mean
    this repository now has more tests that a phase-directory rename would break, which is
    worth stating where a future reader will meet it.

    STEP 5 — Verify and commit once. The move and the two constants are one commit; there is
    no intermediate state in which both halves are green.
  </action>
  <verify>
    <automated>test "$(ls docs/ | grep -c '^phase')" = 0 && test "$(find docs -mindepth 1 -type d | wc -l)" = 0 && test "$(find .planning/phases -path '*/evidence/phase*.md' -type f | wc -l)" = 27 && H1="$(git log --follow --oneline -- .planning/phases/01-corrected-ground-truth/evidence/phase0-binmon-findings.md)" && test "$(echo "$H1" | wc -l)" -gt 1 && H2="$(git log --follow --oneline -- .planning/phases/50-equivalence-and-modifiability/evidence/phase50-ci-boundary.md)" && test "$(echo "$H2" | wc -l)" -gt 1 && test "$(grep -rn 'docs/phase' src scripts | wc -l)" = 0 && node --test src/mcp/vice/phase50-findings-contract.test.ts src/mcp/vice/phase50-transcript-freshness.test.ts && npm --prefix src/mcp/vice run typecheck && npm --prefix src/mcp/vice test</automated>
  </verify>
  <done>`docs/` holds no phase-prefixed file and no subfolder; exactly 27 documents sit under `.planning/phases/*/evidence/`; `git log --follow` returns pre-move commits for a moved document from both the oldest and the newest group; the scan of `src/` and `scripts/` is still zero; both guard tests pass against the new location; the full suite is green with no broker running; and the move plus both path constants landed as one commit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Record the orphan-destination decision and DOCS-04's superseded disposition in the roadmap</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Roadmap criterion 3 and `DOCS-02` are not satisfied by the move itself — they require the
    decision be WRITTEN DOWN. Edit the Phase 53 section of `.planning/ROADMAP.md` by hand,
    with a scoped replacement. Do NOT use any tooling verb that rewrites the roadmap or the
    state file wholesale; this project has recorded defects in several of them, and a
    whole-file write here destroys every other phase entry.

    Make exactly four changes, all inside the Phase 53 section:

    (1) Replace the note that still treats the orphan destination as open with the decision
    and its grounds: all three pre-roadmap documents were placed by the evidence of which
    plan authored them, not by the number in their filename. Name all three documents and
    their destination directories, and name the two pieces of evidence that resolved them —
    the authoring plan's own frontmatter file list for the first two, and the third
    document's own opening paragraph naming the plan and decision record it belongs to.

    (2) Mark the notes RESEARCH.md re-measured as obsolete rather than deleting them, so a
    reader meeting them in history knows which version was live: the stale-layout-tree note
    (all three directories are already gone from disk), the guard-hole note (the test file it
    names was deleted on 2026-09-14), the generated-files class (it measures zero today), and
    the dead-files class (that directory no longer exists). State the measurement date.

    (3) Replace the 2026-09-13 counts with what this phase actually measured and closed:
    27 documents relocated, 83 citation sites across 34 files rewritten, and the skills tree
    already at zero before the phase began.

    (4) Record `DOCS-04`'s disposition as SUPERSEDED, not met, pointing at the withdrawal
    already written into criterion 6 on 2026-09-17. State plainly that this phase ships no
    guard, no continuous-integration check, no lint rule and no pre-commit hook for this path
    form, and that the recurrence risk is accepted. Do not add one. Do not propose one. Do
    not file a successor note, seed or todo for one — the standing decisions that withdrew
    criterion 6 forbid a successor document as explicitly as they forbid the guard.

    Then diff the file and confirm the change is confined to the Phase 53 section. This
    project has recorded several tools that silently damage neighbouring roadmap content, so
    check the sections either side are byte-identical rather than assuming.
  </action>
  <verify>
    <automated>test "$(NS="$(git diff --numstat -- .planning/ROADMAP.md )" && echo "$NS" | awk '{print ($1 > $2)}')" = 1 && test "$(awk '/^### Phase 53:/,/^### Phase 54:/' .planning/ROADMAP.md | grep -c 'phase0-binmon-findings.md')" -ge 1 && test "$(awk '/^### Phase 53:/,/^### Phase 54:/' .planning/ROADMAP.md | grep -c 'phase1-probe-results.md')" -ge 1 && test "$(awk '/^### Phase 53:/,/^### Phase 54:/' .planning/ROADMAP.md | grep -c 'phase2-backend-probe-evidence.md')" -ge 1 && test "$(awk '/^### Phase 53:/,/^### Phase 54:/' .planning/ROADMAP.md | grep -c 'SUPERSEDED')" -ge 1 && test "$(D="$(git diff -U0 -- .planning/ROADMAP.md )" && echo "$D" | grep -c '^[+-]### Phase 5[^3]')" = 0 && test "$(D="$(git diff -U0 -- .planning/ROADMAP.md )" && echo "$D" | grep -c '^[+-]### Phase [1-4]')" = 0</automated>
    <human-check>Read the rewritten Phase 53 section end to end. Confirm it reads as a record of what was decided and why, that the obsolete notes are marked obsolete rather than deleted, and that nothing anywhere in it proposes or schedules a replacement guard.</human-check>
  </verify>
  <done>The Phase 53 roadmap section names all three orphan documents with their destinations and the evidence that resolved them, marks the four obsolete notes as obsolete with their measurement date, carries this phase's real counts, and records `DOCS-04` as SUPERSEDED with no guard proposed anywhere. The diff adds more than it removes and touches no other phase section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history -> evidence claim | These 27 documents are evidence only because their provenance is traceable. The move is the point at which that chain can be severed. |
| test -> committed artifact on disk | Two guard tests read real files. The move relocates their subjects. |
| plan -> project-wide roadmap | A whole-file write to the roadmap destroys every phase entry outside the diff window. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-04-01 | Repudiation | the 27 relocated evidence documents | high | mitigate | A copy-then-delete relocation severs the commit chain that makes each document evidence of a measured run. Task 1 mandates `git mv` and the verify gate asserts `git log --follow` returns pre-move commits for a document from both the oldest and the newest group. |
| T-53-04-02 | Denial of service | the two phase-50 guard tests | high | mitigate | Splitting the move from the path constants leaves the automated gate red between commits. Task 1 requires both in one commit and the gate runs both test files directly before the full suite. |
| T-53-04-03 | Tampering | `.planning/ROADMAP.md` | high | mitigate | A whole-file write or a roadmap-rewriting tooling verb destroys neighbouring phase entries. Task 2 forbids both by name, requires a scoped hand edit, and the verify gate asserts no other phase heading appears in the diff. |
| T-53-04-04 | Tampering | destination correctness for the three orphans | medium | mitigate | A destination guessed from the filename number would file pre-roadmap evidence under the wrong phase. Task 1 requires both evidence checks to be re-run before the move and halts on failure rather than guessing. |
| T-53-04-05 | Spoofing | a green suite measured against a contaminated environment | medium | mitigate | A live emulator broker deterministically reds one broker test, so a run made with one up proves nothing either way. Task 1 requires the broker be stopped before the suite is measured. |
| T-53-04-06 | Elevation of privilege | the withdrawn recurrence guard | medium | accept | Nothing mechanical now prevents a future plan writing evidence into the operator's directory again. Accepted explicitly by the owner on 2026-09-17: such a guard scans source text by construction, and the standing decisions of 2026-09-14 ban that shape and ban a successor. The risk is recorded in the roadmap rather than mitigated. |
| T-53-04-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package and adds no dependency. |
</threat_model>

<verification>
- `ls docs/` shows no phase-prefixed file and `find docs -mindepth 1 -type d` is empty.
- Exactly 27 documents match `.planning/phases/*/evidence/phase*.md`.
- `git log --follow` on a phase-01 document and on a phase-50 document each returns more than one commit.
- `grep -rn` for the evidence-document path form across `src/` and `scripts/` returns zero.
- Both phase-50 guard test files pass when run directly, and the full suite is green with no broker running.
- The roadmap diff is confined to the Phase 53 section, names all three orphan documents with destinations, and records `DOCS-04` as SUPERSEDED.
- No guard, CI check, lint rule, pre-commit hook, note, seed or todo proposing one was created anywhere in this plan.
</verification>

<success_criteria>
`docs/` is operator-owned; the 27 evidence documents live in the phase artifact tree with
their history intact; the only two functional readers of the old location read the new one in
the same commit; and what was decided — including what was deliberately NOT built — is
written down.
</success_criteria>

<output>
Create `.planning/phases/53-operator-owned-docs/53-04-SUMMARY.md` when done.
Record the full 27-row source-to-destination table, the `git log --follow` output proving
history survived for at least two moved documents, the before/after of both guard-test path
constants, and the full suite's pass/fail counts.
</output>
