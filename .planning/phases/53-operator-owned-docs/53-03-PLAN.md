---
phase: 53-operator-owned-docs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/vice/phase50-findings-contract.test.ts
  - src/mcp/vice/phase50-transcript-freshness.test.ts
  - src/mcp/vice/anno-tools.test.ts
  - src/mcp/vice/anno-enum-gen.test.ts
  - src/mcp/vice/anno-store-export.test.ts
  - src/mcp/vice/anno-decomp-closure.test.ts
  - src/mcp/vice/broker-launch.test.ts
  - src/mcp/vice/stock-connect.test.ts
  - src/mcp/vice/stock-protocol.test.ts
  - src/mcp/vice/reassembly-gate-modified-run.test.ts
  - src/mcp/vice/reassembly-gate-exported-edit-run.test.ts
  - src/mcp/vice/ghidra-opcode-live.test.ts
  - src/mcp/vice/text-monitor-live.test.ts
autonomous: true
requirements: [DOCS-03]

estimate:
  tokens: 55000
  raw_tokens: 110000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Every one of the 21 evidence-document path citations across the thirteen test files is gone, replaced by the reason it stood for (roadmap criterion 4)"
    - "No replaced site names ANY path in its place (locked decision L4)"
    - "Six of the 21 sites are not comments but test titles and assertion failure messages; each is rewritten so the text a failing run prints stands on its own for a reader who has only the terminal output"
    - "The two phase-50 guard tests have their COMMENT citations rewritten and their `DOCS_DIR` path constant left exactly as it is — repointing it before the documents move would red both tests immediately, and 53-04 owns that change as one atomic commit with the move"
    - "Each rewritten site carries more reasoning than the pointer it replaced: no file's diff removes more lines than it adds, and the plan's aggregate adds strictly more (roadmap criterion 5, locked decision L5)"
    - "`tsc --noEmit` is clean and `npm run test:automated` is green; the two live-emulator files this plan touches are in the manual-only set and are covered by typecheck alone"
    - "NOT MOVED HERE: no document under `docs/` is relocated, created or deleted by this plan (locked decision L3 defers all moves to 53-04)"
  artifacts:
    - src/mcp/vice/phase50-findings-contract.test.ts
    - src/mcp/vice/phase50-transcript-freshness.test.ts
    - src/mcp/vice/anno-tools.test.ts
    - src/mcp/vice/anno-enum-gen.test.ts
    - src/mcp/vice/anno-store-export.test.ts
    - src/mcp/vice/anno-decomp-closure.test.ts
    - src/mcp/vice/broker-launch.test.ts
    - src/mcp/vice/stock-connect.test.ts
    - src/mcp/vice/stock-protocol.test.ts
    - src/mcp/vice/reassembly-gate-modified-run.test.ts
    - src/mcp/vice/reassembly-gate-exported-edit-run.test.ts
    - src/mcp/vice/ghidra-opcode-live.test.ts
    - src/mcp/vice/text-monitor-live.test.ts
  key_links:
    - "`ghidra-opcode-live.test.ts` and `text-monitor-live.test.ts` are members of `test-gate.mjs`'s frozen `MANUAL_ONLY_TESTS` array, so `npm run test:automated` never runs them. For a comment-and-message edit `tsc --noEmit` is the honest gate; do not add either to the automated set to make a check pass."
    - "This plan and 53-04 both touch the two phase-50 guard test files, in that order and in different waves. 53-03 rewrites their prose; 53-04 changes their path constant in the same commit as the move."
---

<!-- planner-discipline-allow: docs/phase -->

<objective>
Rewrite all 21 evidence-document path citations across the thirteen test files that carry
them, so each states the reason it stood for instead of naming a file — including the six
that are test titles or assertion failure messages rather than comments.

Purpose: roadmap criterion 4's grep scope is `src/**`, and the test suite is the largest part
of it. Six of these sites are worse than dangling comments: they are text a FAILING test
prints, so today a red run tells a maintainer to go read a file the checkout may not contain.
Writing the reason into the message makes the failure self-explaining.

Output: thirteen modified files, zero evidence-document path citations among them, the
automated gate green and the two phase-50 guards still reading their documents from the
unchanged location that 53-04 will move.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ENGINEERING_RULES.md
@.planning/phases/53-operator-owned-docs/RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite the 6 comment citations in the two phase-50 guard tests, leaving their path constant untouched</name>
  <files>src/mcp/vice/phase50-findings-contract.test.ts, src/mcp/vice/phase50-transcript-freshness.test.ts</files>
  <read_first>
    RESEARCH.md section "Q6", including its closing paragraph on the one functional
    (non-comment) dependency. That paragraph is why this task is narrowly scoped to prose.
  </read_first>
  <action>
    Six sites: `phase50-findings-contract.test.ts` at measured lines 8, 9, 19 and 165, and
    `phase50-transcript-freshness.test.ts` at measured lines 61 and 97. Re-measure with
    `grep -an 'docs/phase'` over both files.

    HARD BOUNDARY for this task: do NOT change the `DOCS_DIR` constant in either file, and do
    not change any `docsDir` parameter, any `readdirSync` call, any filename-prefix filter,
    any transcript filename pattern, or the assertion that lists the discovered findings
    documents by name. Those are a live path dependency on documents that have not moved yet.
    Changing them now reds both tests. Plan 53-04 repoints them in the same commit as the
    move, which is the only ordering in which both halves are ever green.

    For the six prose sites, apply the phase's three-property rewrite: state what was
    established and against what; name zero paths in place of the one removed (locked
    decision L4); come out longer than what it replaced (locked decision L5).

    Two of the six need specific care:

    Line 165 is a comment labelling a contract check that immediately follows it and that
    really does read a named file at runtime. The comment must stop naming the path while
    still identifying WHICH document the check covers. Refer to it by what it is — the
    document recording where the continuous-integration boundary falls — not by where it
    lives. The code below the comment is unchanged.

    Line 97 in `phase50-transcript-freshness.test.ts` is NOT a comment. It is part of the
    remediation text this guard prints when a transcript has gone stale, telling a maintainer
    what to do about it. That instruction currently sends them to a section of a named
    document. Write the instruction itself into the message: what has to be re-run, against
    what, and what has to be committed afterwards. The reader of that text has a failing test
    run, not a file tree.
  </action>
  <verify>
    <automated>test "$(grep -rn 'docs/phase' src/mcp/vice/phase50-findings-contract.test.ts src/mcp/vice/phase50-transcript-freshness.test.ts | wc -l)" = 0 && grep -q 'const DOCS_DIR = join(REPO_ROOT, "docs")' src/mcp/vice/phase50-findings-contract.test.ts && grep -q 'const DOCS_DIR = join(REPO_ROOT, "docs")' src/mcp/vice/phase50-transcript-freshness.test.ts && npm --prefix src/mcp/vice run typecheck && node --test src/mcp/vice/phase50-findings-contract.test.ts src/mcp/vice/phase50-transcript-freshness.test.ts</automated>
  </verify>
  <done>Both phase-50 guard tests carry zero evidence-document path citations in their prose, each still declares its path constant exactly as before, and both test files pass when run directly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite the 9 citations in the annotation and reassembly test files</name>
  <files>src/mcp/vice/anno-tools.test.ts, src/mcp/vice/anno-enum-gen.test.ts, src/mcp/vice/anno-store-export.test.ts, src/mcp/vice/anno-decomp-closure.test.ts, src/mcp/vice/reassembly-gate-modified-run.test.ts, src/mcp/vice/reassembly-gate-exported-edit-run.test.ts</files>
  <action>
    Nine sites: `anno-tools.test.ts` (3, measured lines 1132, 1160 and 2906),
    `anno-enum-gen.test.ts` (1, line 164), `anno-store-export.test.ts` (1, line 474),
    `anno-decomp-closure.test.ts` (1, line 8), `reassembly-gate-modified-run.test.ts` (1,
    line 9) and `reassembly-gate-exported-edit-run.test.ts` (1, line 9). Re-measure first.

    Three of the nine are not comments and each needs its own treatment:

    `anno-enum-gen.test.ts` line 164 is a TEST TITLE. The citation sits mid-title, between the
    statement of what is confirmed absent and the regression case it names. Rewrite the title
    so it reads as one self-contained sentence: what is asserted, and what the underlying
    finding was. Keep the register addresses and the named regression case exactly as they
    are — they are what makes the title searchable.

    `anno-store-export.test.ts` line 474 is part of an assertion failure message explaining
    why a synthetic store is not an acceptable substitute for a genuinely derived one. The
    parenthetical currently names a subject and cites where its measurement lives. Keep the
    subject, drop the pointer, and say what property of that store makes it the real thing.

    `anno-tools.test.ts` line 1160 cites a document for a "disclosed gap class". A reader
    needs to know WHAT the gap class is, not where it was disclosed — write it out.

    The remaining six are ordinary comments, two of them WHY headers
    (`anno-decomp-closure.test.ts` line 8 and both reassembly-gate files at line 9). The WHY
    headers carry the reason the file exists and must survive and grow. The reassembly-gate
    pair both turn on a committed verdict recorded by an earlier gate; state the verdict and
    the rule it came from rather than where it was written down.

    Same three properties throughout, and the same hard rule: no path replaces the path
    (locked decision L4), every rewrite is longer than what it replaced (locked decision L5).
  </action>
  <verify>
    <automated>test "$(grep -rn 'docs/phase' src/mcp/vice/anno-tools.test.ts src/mcp/vice/anno-enum-gen.test.ts src/mcp/vice/anno-store-export.test.ts src/mcp/vice/anno-decomp-closure.test.ts src/mcp/vice/reassembly-gate-modified-run.test.ts src/mcp/vice/reassembly-gate-exported-edit-run.test.ts | wc -l)" = 0 && NS="$(git diff --numstat -- src/mcp/vice/anno-tools.test.ts src/mcp/vice/anno-enum-gen.test.ts src/mcp/vice/anno-store-export.test.ts src/mcp/vice/anno-decomp-closure.test.ts src/mcp/vice/reassembly-gate-modified-run.test.ts src/mcp/vice/reassembly-gate-exported-edit-run.test.ts )" && echo "$NS" | awk '{ if ($1 < $2) { printf "FAIL shrank %s: +%s -%s\n", $3, $1, $2; bad=1 } } END { exit bad }' && npm --prefix src/mcp/vice run typecheck</automated>
  </verify>
  <done>All six annotation and reassembly test files carry zero evidence-document path citations, none shrank, and `tsc --noEmit` is clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Rewrite the 6 citations in the transport, broker and live-emulator tests, then gate the whole plan</name>
  <files>src/mcp/vice/stock-connect.test.ts, src/mcp/vice/stock-protocol.test.ts, src/mcp/vice/broker-launch.test.ts, src/mcp/vice/ghidra-opcode-live.test.ts, src/mcp/vice/text-monitor-live.test.ts</files>
  <action>
    Six sites: `stock-connect.test.ts` (2, measured lines 272 and 285),
    `stock-protocol.test.ts` (1, line 1485), `broker-launch.test.ts` (2, lines 457 and 528),
    `ghidra-opcode-live.test.ts` (1, line 799) and `text-monitor-live.test.ts` (1, line 1158).
    Re-measure first.

    Two are not comments:

    `stock-connect.test.ts` line 285 is the third argument to an equality assertion — the
    message printed when the resume opcode constant does not match. Rewrite it to state what
    the opcode does and why this test pins it, rather than citing where the byte was recorded.

    `text-monitor-live.test.ts` line 1158 is part of a skip/failure message describing a named
    gap that is not reproducible in this environment. Name the gap and what makes it
    non-reproducible here; drop the pointer.

    The other four are comments. `broker-launch.test.ts` line 457 introduces a hand-built
    header layout, and line 528 explains why a probe that only pinged would be insufficient —
    both turn on a protocol fact that is stated elsewhere in this repository's own top-level
    instruction file, so write the fact.

    `ghidra-opcode-live.test.ts` and `text-monitor-live.test.ts` are frozen members of
    `test-gate.mjs`'s `MANUAL_ONLY_TESTS`, so `npm run test:automated` will not execute them.
    That is correct and must not be worked around: do not add either file to the automated
    set, and do not move it out of the manual list. For an edit that touches only comments
    and message strings, `tsc --noEmit` is the honest gate and it covers both files.

    Then gate the whole plan: confirm zero citations across all thirteen of this plan's files,
    confirm the aggregate diff adds strictly more lines than it removes, confirm both phase-50
    guards still declare their path constant unchanged, and run the automated suite once
    (this plan's single full test run).
  </action>
  <verify>
    <automated>test "$(grep -rn 'docs/phase' src/mcp/vice/phase50-findings-contract.test.ts src/mcp/vice/phase50-transcript-freshness.test.ts src/mcp/vice/anno-tools.test.ts src/mcp/vice/anno-enum-gen.test.ts src/mcp/vice/anno-store-export.test.ts src/mcp/vice/anno-decomp-closure.test.ts src/mcp/vice/broker-launch.test.ts src/mcp/vice/stock-connect.test.ts src/mcp/vice/stock-protocol.test.ts src/mcp/vice/reassembly-gate-modified-run.test.ts src/mcp/vice/reassembly-gate-exported-edit-run.test.ts src/mcp/vice/ghidra-opcode-live.test.ts src/mcp/vice/text-monitor-live.test.ts | wc -l)" = 0 && grep -q 'const DOCS_DIR = join(REPO_ROOT, "docs")' src/mcp/vice/phase50-findings-contract.test.ts && grep -q 'const DOCS_DIR = join(REPO_ROOT, "docs")' src/mcp/vice/phase50-transcript-freshness.test.ts && NS="$(git diff --numstat -- 'src/mcp/vice/*.test.ts' )" && echo "$NS" | awk '{ i+=$1; d+=$2; if ($1 < $2) { printf "FAIL shrank %s\n", $3; bad=1 } } END { if (i <= d) { printf "FAIL net shrink: +%d -%d\n", i, d; bad=1 } exit bad }' && npm --prefix src/mcp/vice run typecheck && npm --prefix src/mcp/vice run test:automated</automated>
  </verify>
  <done>All thirteen test files this plan owns carry zero evidence-document path citations; both phase-50 guards still declare their path constant unchanged; the aggregate diff adds strictly more lines than it removes with no single file shrinking; `tsc --noEmit` is clean and `npm run test:automated` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> maintainer terminal | Six edited sites are test titles or failure messages. Their reader has a red run, not the repository, so a misleading rewrite misdirects a debugging session. |
| guard test -> real files on disk | The two phase-50 guards read committed documents at runtime. Their path constant is a live dependency, not prose. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-03-01 | Denial of service | the two phase-50 guard tests | high | mitigate | Repointing `DOCS_DIR` in this wave, before the documents move, reds both guards and blocks the whole phase. Task 1 forbids it by name and the verify gate asserts the constant is still declared unchanged in both files. |
| T-53-03-02 | Tampering | test titles and assertion messages | medium | mitigate | A rewritten failure message that states a weaker or different claim than the assertion actually makes misleads every future reader of a red run. Task 2 and Task 3 require each rewritten message to state what the assertion checks, and the diff gate catches a net shortening. |
| T-53-03-03 | Tampering | `MANUAL_ONLY_TESTS` membership | medium | mitigate | The tempting shortcut for the two live-emulator files is to move them into the automated set to get a green signal. Task 3 forbids it explicitly; `test-gate.mjs` documents the array as frozen and extended only for a file needing the same treatment. |
| T-53-03-04 | Repudiation | provenance of a recorded gate verdict | medium | accept | The two reassembly-gate files cite a committed verdict by document. Removing the pointer removes the route back to it. Accepted on roadmap criterion 4's terms; the verdict and its rule travel with the test instead. |
| T-53-03-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package and adds no dependency. |
</threat_model>

<verification>
- `grep -rn 'docs/phase'` over this plan's thirteen files returns zero lines.
- Both phase-50 guard tests still declare `DOCS_DIR` exactly as they did before this plan.
- Running both phase-50 guard test files directly under `node --test` passes.
- No file's diff removes more lines than it adds; the aggregate adds strictly more.
- `npm --prefix src/mcp/vice run typecheck` exits 0 and `npm --prefix src/mcp/vice run test:automated` is green.
- `MANUAL_ONLY_TESTS` in `src/mcp/vice/test-gate.mjs` is byte-identical to its pre-plan state.
- No file under `docs/` was moved, created or deleted by this plan.
</verification>

<success_criteria>
The last twenty-one of the phase's eighty-three citation sites are closed, six of them in text
that a failing test prints, with the phase-50 guards left in the only state in which they and
the not-yet-moved documents are both correct.
</success_criteria>

<output>
Create `.planning/phases/53-operator-owned-docs/53-03-SUMMARY.md` when done.
Record the per-file counts before and after, quote the six rewritten titles and messages in
full, and state explicitly that `DOCS_DIR` was left unchanged in both phase-50 guards so
53-04's executor can rely on it.
</output>
