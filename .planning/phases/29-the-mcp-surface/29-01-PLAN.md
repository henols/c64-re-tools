---
phase: 29-the-mcp-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/vice/anno-tools.ts
  - src/mcp/vice/anno-tools.test.ts
  - src/mcp/vice/vice-proxy.ts
  - src/mcp/vice/stock-dispatch.test.ts
  - src/mcp/vice/tool-support-table.test.mjs
  - src/mcp/vice/capability-registry.test.ts
  - src/mcp/vice/hostpath-consumers.test.ts
  - scripts/generate-tool-support-table.mjs
  - src/mcp/vice/package.json
autonomous: true
requirements: [MCP-02, MCP-03, MCP-05]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A single `anno_*` verb is advertised by the running proxy and answers end to end: `runAnnoTool(\"anno_get_symbols\", {store, max_results})` opens the store, reads it, closes it in a `finally`, and returns `okText(JSON)` with `isError: false`."
    - "`BACKEND_SEAM_BYPASS_KEYS` is an ordered `deepEqual` of exactly two entries; a third registration that bypasses the backend-aware seam collides — the assertion fails naming the extra key rather than accepting a superset."
    - "If `proxyToolRegistrations()` returns an empty set the test fails on its own `registrations.length >= 5` floor rather than reporting zero bypassers as agreement."
    - "`assert.deepEqual(bypassing, BACKEND_SEAM_BYPASS_KEYS)` is order-sensitive and the `anno` entry keeps position 2; swapping the two source-order registrations fails the test."
    - "D-13: the three `ANNO_TOOL_DEFINITIONS` bounding witnesses are re-pointed independently in the registering commit, each keeping its own bounding technique and none refactored into a shared helper; the order in which they are re-pointed does not change the outcome — the generator throws by name if any one is not moved."
    - "`docs/tool-support.md` regenerates byte-identical (7,874 bytes) after the loop substitution, and the generator run against an un-re-pointed regex throws by name rather than emitting a different table."
    - "`hostpath.ts`'s production consumer set is still exactly the five declared modules, and no `anno-*` module appears in it."
    - "`vice-proxy.ts`'s net line count is unchanged by this plan, so `docs-linerefs.test.ts`'s two CLAUDE.md `rewriteArguments()` citations still land on their cited lines."
  artifacts:
    - src/mcp/vice/anno-tools.ts
    - src/mcp/vice/anno-tools.test.ts
  key_links:
    - "vice-proxy.ts:3401 loop -> buildViceTool(annoDef, ...) -> runAnnoTool -> openStore/closeStore"
    - "stock-dispatch.test.ts body-slice read path -> anno-tools.ts / runAnnoTool"
    - "generate-tool-support-table.mjs regex + two duplicate witnesses -> ANNO_TOOL_DEFINITIONS"
  prohibitions:
    - statement: "No guard may be \"re-pointed\" to a subject that cannot fail, and no exemption may be widened to silence a real reintroduction; a guard whose planted violation no longer reddens has not been re-pointed."
      status: unverified
      verification: flagged
---

<objective>
Lead the phase with one production-quality end-to-end slice: the `anno_*` tool
family is registered proxy-locally through `buildViceTool()` and one verb
answers a real query against a real store — and every guard that breaks on
**registration** moves in the same commit (`MCP-05`).

Purpose: the registration seam is this phase's architectural risk. Proving it
end-to-end on one path, in one commit, means an architectural dead end costs one
commit rather than ten. Everything else in the phase expands out from this slice.
Output: `anno-tools.ts` (the never-throw boundary and the tool table),
the two-line `vice-proxy.ts` substitution, and five re-pointed guards.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-the-mcp-surface/29-CONTEXT.md
@.planning/phases/29-the-mcp-surface/29-RESEARCH.md
@.planning/phases/29-the-mcp-surface/29-PATTERNS.md
@.planning/phases/29-the-mcp-surface/29-VALIDATION.md
</context>

## Decisions and readings adopted by this plan

- **D-05** (locked): one prefix — `anno_` for tool names, `anno-` for modules.
- **D-06** (locked): open/close per call, explicit `store` argument on every verb.
- **D-13** (locked): the registration-time guards move in the registering commit;
  the three `R2000_TOOL_DEFINITIONS` witnesses move together and **none** is
  refactored into a shared helper.
- **Discretion 2, adopted as measured:** the `{available:false, reason}` shapes
  are the four verbatim in-repo sites — the discriminated union type at
  `stock-recycle.ts:86` / `vice-proxy.ts:1461`, the frozen `[name, reason]` table
  at `stock-vicii.ts:98`, and the construction at `stock-cia.ts:494-497`. They are
  **matched, not invented**.
- **Planner correction to `29-CONTEXT.md` (N-11):** `vice-proxy.ts` carries
  **five** r2000 sites, not two — the tool import (`:194`), the session-close
  import (`:200`), the `r2000` subcommand branch (`:277-310`), the
  `closeR2000SessionSync(trigger)` call (`:3186`), and the registration loop
  (`:3401-3403`). **This plan touches only the two that are line-count-neutral**
  (`:194`, `:3401-3403`). The three that change line count are deferred to
  plan 29-10, which corrects `CLAUDE.md`'s citations in the same commit.

## Flagged assumptions and unresolved probe edges (phase-wide, no silent drops)

Phase 29 has no SPEC, so the deterministic edge probe was run over the six phase
requirements and its 19 applicable edges are resolved here rather than by a SPEC
section. **18 of the 19 are authored into `must_haves.truths` across this plan set
— 17 as explicit acceptance criteria and 1 as a structured `backstop` marker — and
1 remains unresolved and is surfaced below.** The no-silent-drop equality holds:
19 == 18 authored + 1 flagged.

| Requirement | Edge category | Where it landed |
|---|---|---|
| MCP-01 | adjacency, empty, ordering | `29-08-PLAN.md` `must_haves.truths` |
| **MCP-02** | **unclassified** | **UNRESOLVED — flagged below** |
| MCP-03 | adjacency, empty, ordering | this plan's `must_haves.truths` |
| MCP-04 | empty, encoding, concurrency | `29-06-PLAN.md` `must_haves.truths` |
| MCP-05 | adjacency, empty | `29-05-PLAN.md`; ordering in this plan |
| STORE-06 | boundary, adjacency, empty, encoding, ordering | `29-04-PLAN.md` `must_haves.truths` |
| STORE-06 | precision | `29-04-PLAN.md`, authored as `verification: backstop` |

**The one unresolved edge, stated rather than dismissed.** The probe classified
`MCP-02`'s edge as `unclassified` and emitted "review manually" as its own probe
text. It is left `unresolved` deliberately — the protocol forbids auto-resolving an
`unclassified` row with a backstop marker, and auto-dismissing it is the silent
failure the whole mechanism exists to remove. The reason it is genuinely hard to
classify: `MCP-02` is a **structural** requirement about a call path that must not
exist, and the shape taxonomy (boundaries, adjacency, encoding, ordering) has no
category for "this seam is unreachable by construction". Its verification is
therefore the body-slice assertion and the five-member importer set, both of which
this plan carries as ordinary truths — but the *edge* is not thereby resolved, and
saying so is more honest than inventing a category for it.

**Research assumptions still carried, with their disposition.** A1 (the full-glob
runtime and failure baseline, carried from project memory rather than measured) is
closed by plan 29-02's Task 1. A2 (no store outside this repository would be
stranded by the schema bump) is surfaced in plan 29-03's decision gate rather than
silently relied on. A3 (the block-type vocabulary agreement, flagged unverified) is
closed by plan 29-07's Task 1 before the census re-point depends on it. A4 (the
module-count arithmetic) is resolved by the naming policy this plan set adopts, and
plan 29-05 halts rather than lowering a floor if the measured count disagrees. A5
(whether the pack-file helper could be reused) is closed by plan 29-02 exporting it.
A6 (whether a real assembler is installed) is irrelevant to this phase and belongs
to Phase 30.

<assumption_delta_decision>
The `assumption-delta` detector fired (`kind: pluralization`, term `second`).
The genuine delta in this phase is the annotation surface losing its monopoly:
until now `r2000_*` over a rented `.regen2000proj` was the only annotation tool
family; `anno_*` over an owned store is the second.

- **Primary noun:** the *annotation surface over the owned store* (`anno_*`).
- **Decision: `promote`.** `anno_*` is promoted to the one primary annotation
  surface and `r2000_*` is not kept alongside it — the registration loop is
  **substituted**, not appended, and D-01 deletes the old family outright in
  plan 29-10. Add-alongside was explicitly rejected by the owner (D-01).
- **Invariant test encoding the generalized intent:** already present and
  strengthened here — `stock-dispatch.test.ts`'s ordered two-entry
  `BACKEND_SEAM_BYPASS_KEYS` goes red the instant a second annotation family is
  registered alongside, and plan 29-02's grep gate goes red if the old one
  returns.
</assumption_delta_decision>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "the store answers an agent through the MCP surface" — one verb only</name>
  <files>src/mcp/vice/anno-tools.ts, src/mcp/vice/anno-tools.test.ts, src/mcp/vice/vice-proxy.ts, src/mcp/vice/stock-dispatch.test.ts, scripts/generate-tool-support-table.mjs, src/mcp/vice/tool-support-table.test.mjs, src/mcp/vice/capability-registry.test.ts, src/mcp/vice/package.json</files>
  <read_first>
    - src/mcp/vice/r2000-tools.ts — READ BEFORE IT IS DELETED. Lines 1-9 and 52-60 (the three-part header convention), 110-148 (`R2000ToolDefinition`'s index signature, `ToolCallResult`, `isPlainObject`, `okText`, `errText`), 196-232 (the read-at-call-time cap pattern), 763-838 (`assertCuratedBatch`, D-33), 854-874 (`assertCuratedTool`, the set-membership-first gate and its two-route throw text), 1141-1213 (the runner and its named-by-class catch, plus WR-02 recorded at 772-774).
    - src/mcp/vice/vice-proxy.ts — lines 186-210 (the imports being substituted), 3224-3237 (`rawJsonSchemaAsStandardSchema`'s `validate: (value) => ({ value })`), 3263-3276 (`buildViceTool`), 3378-3405 (the registration comment block and the loop).
    - src/mcp/vice/stock-dispatch.test.ts — line 45 (the `CURATED_R2000_TOOLS` import, which is OUTSIDE the re-pointing block below and is the whole reason it is cited separately) and lines 1486-1600 (all five re-pointing sites). The import at :45 is the file's only route to the retired set; once the manifest-absence loop at :1564-1575 iterates `CURATED_ANNO_TOOLS`, that import is unused-but-still-valid, `tsconfig.json` sets no `noUnusedLocals`, so it type-checks green here and only breaks at plan 29-10 when its subject is deleted. Remove it in this task.
    - src/mcp/vice/anno-store.ts — `openStore` (413), `closeStore` (550), `currentRevision` (555), `listLabels` (2842), and the `schema_version` refusal at 521-523.
    - src/mcp/vice/anno-types.ts — `SCHEMA_VERSION` (154), `storePathWithinWorkspace` (1140), `parseStoreAddress` (864), `AnnoStoreError` (477) and its subclasses.
    - src/mcp/vice/stock-recycle.ts:75-140 and src/mcp/vice/stock-cia.ts:85-124,480-510 — the `{available:false, reason}` convention, verbatim.
    - scripts/generate-tool-support-table.mjs — lines 93-146 (the prose at :95, the regex at :107, the three-witness prohibition at :133-141).
    - src/mcp/vice/tool-support-table.test.mjs:55-90 and src/mcp/vice/capability-registry.test.ts:145-180 — the two duplicate witnesses and their own "do not collapse into a shared helper" comments.
    - .planning/phases/29-the-mcp-surface/29-PATTERNS.md — the header-comment convention and the wire-shape declarations to copy with identifiers substituted.
  </read_first>
  <action>
Create `src/mcp/vice/anno-tools.ts` as the ONE authoritative place for the curated
`anno_*` tool surface, carrying the project's three-part header (what this file is
the one authoritative place for; WHY it exists, naming D-05/D-06/MCP-02; WHAT NOT
TO DO, naming D-33 and WR-02 by id).

Export `AnnoToolDefinition` with `name`, `description`, `inputSchema`
(`{ type: "object"; properties: Record<string, unknown>; required?: string[] }`)
and the load-bearing `[key: string]: unknown` index signature that makes
`buildViceTool()` accept it directly. Export `ANNO_TOOL_DEFINITIONS` holding
**exactly one** definition for this slice: `anno_get_symbols`, with required
arguments `store` and `max_results` and an optional `start_address` /
`end_address` pair. Derive `CURATED_ANNO_TOOLS` from `ANNO_TOOL_DEFINITIONS`'s
`name` values — never a second hand-typed list. Add module-private `isPlainObject`,
`okText` and `errText` returning `ToolCallResult` (`{content:[{type:"text",text}], isError}`).

Add `AnnoUncuratedToolError` extending `AnnoStoreError` (never a bare `Error`),
carrying `toolName` and, where applicable, `batchIndex`, with a message that names
both resolution routes in the wording of `assertCuratedTool`'s own throw: implement
it and add it to `ANNO_TOOL_DEFINITIONS` with a named criterion, or remove the
caller reference. Export `assertAnnoTool(name, args)` whose FIRST check is
`CURATED_ANNO_TOOLS` set membership.

Export `runAnnoTool(name, args): Promise<ToolCallResult>` as the never-throw
boundary. It differs deliberately from `runR2000Tool` in one respect: `assertAnnoTool`
is called INSIDE the `try`, which closes WR-02 (recorded at `r2000-tools.ts:772-774`
as out of scope for that plan) so a refusal resolves `{isError:true}` instead of
rejecting the promise. Resolve the `store` argument with `storePathWithinWorkspace()`
against `repoRoot()`; open with `openStore(path, { workspaceRoot: repoRoot(), mustExist: true })`;
wrap the dispatch in `try { ... } finally { closeStore(handle); }` so D-06's
close happens on every path; catch everything and return
`errText(`${name} failed: [${errName}] ${errMessage}`)` naming the error class,
matching `r2000-tools.ts:1205-1213` verbatim in wording.

Substitute `vice-proxy.ts:194` IN PLACE, one line for one line, importing
`ANNO_TOOL_DEFINITIONS` and `runAnnoTool` from `./anno-tools.ts`. Substitute
`vice-proxy.ts:3401-3403` IN PLACE, three lines for three lines, with loop
variable `annoDef` — a distinct name is load-bearing, per the comment at
`:3393-3400`, because `proxyToolRegistrations()` keys registrations by their raw
captured text. Keep the comment block at `:3386-3400` and rewrite its wording:
the family is backend-independent because it reaches a proxy-local SQLite store
and never VICE, not because a child process is a separate host. **Add no lines
and remove none** — `docs-linerefs.test.ts` checks CLAUDE.md's cited
`vice-proxy.ts` line numbers and any net drift above `:3052` reds it.

In the SAME commit, re-point every guard that breaks on registration:
`stock-dispatch.test.ts:1505`'s second `BACKEND_SEAM_BYPASS_KEYS` entry to
`"annoDef.name"` keeping position 2 (the `deepEqual` at `:1519-1531` is
order-sensitive); the `registrations.length >= 5` message at `:1507-1517`;
the body-slice block at `:1547-1562` — its `readFileSync` path to `anno-tools.ts`,
its `indexOf` needle to `export async function runAnnoTool(`, and its failure
message to name the `anno_*` family's backend-independence; and the
manifest-absence loop at `:1564-1575` to iterate `CURATED_ANNO_TOOLS` with its own
non-vacuity guard intact. Re-point `scripts/generate-tool-support-table.mjs`'s
`R2000_LOOP_VAR_RE` at `:107` and the prose naming the array at `:95` to
`ANNO_TOOL_DEFINITIONS`; re-point `tool-support-table.test.mjs:66`'s witness and
`capability-registry.test.ts:161`'s witness, and correct that file's stale comment
at `:154` — the array it describes held 19 entries, not 17. Each of the three
witnesses keeps its own distinct bounding technique (character-offset search,
brace-depth counting, line-oriented scanning); do not extract a shared helper, per
all three files' own headers.

Add `"anno-tools.ts"` to `src/mcp/vice/package.json`'s `files[]`.

Write `src/mcp/vice/anno-tools.test.ts` with the end-to-end tracer case: create a
temporary store under a temporary workspace root, write one label through
`setLabel()`, call `runAnnoTool("anno_get_symbols", ...)`, and assert the returned
`ToolCallResult` has `isError: false` and a JSON body containing that label.
Assert structurally that `anno-tools.ts` holds no module-level mutable store handle
and that every `openStore(` in it is followed by a `closeStore(` inside a `finally`.
  </action>
  <verify>
    <automated>cd src/mcp/vice && node --test anno-tools.test.ts stock-dispatch.test.ts tool-support-table.test.mjs capability-registry.test.ts hostpath-consumers.test.ts docs-linerefs.test.ts module-classification.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/mcp/vice/anno-tools.ts` exists and contains `export const ANNO_TOOL_DEFINITIONS`, `export const CURATED_ANNO_TOOLS`, `export function assertAnnoTool(` and `export async function runAnnoTool(`.
    - `src/mcp/vice/anno-tools.ts` contains `class AnnoUncuratedToolError extends AnnoStoreError` and contains no `throw new Error(`.
    - `grep -c 'closeStore(' src/mcp/vice/anno-tools.ts` is greater than 0 and every `openStore(` occurrence is inside a function whose body also contains `finally`.
    - `src/mcp/vice/vice-proxy.ts` contains `for (const annoDef of ANNO_TOOL_DEFINITIONS)` and contains no `R2000_TOOL_DEFINITIONS`.
    - `git diff --numstat HEAD -- src/mcp/vice/vice-proxy.ts` reports equal added and deleted line counts (the substitution is line-count-neutral).
    - `src/mcp/vice/stock-dispatch.test.ts` contains `"annoDef.name"` as the second element of `BACKEND_SEAM_BYPASS_KEYS` and contains `export async function runAnnoTool(` as the body-slice needle.
    - `grep -c 'from "./r2000-tools.ts"' src/mcp/vice/stock-dispatch.test.ts` reports `0`, and the same file's retired-set identifier count — measured with comment lines excluded, `grep -v '^[[:space:]]*//' src/mcp/vice/stock-dispatch.test.ts | grep -c CURATED_R2000_TOOLS` — also reports `0`. The import at `:45` is gone along with its use sites, so no unused-but-valid reference to the retired set survives this wave to break at plan 29-10, which does not own this file. Do not record the removal in a farewell comment naming the retired identifier inside this file: the comment-excluded form above is what makes that survivable rather than self-reddening, and a bare count over the unfiltered file would be.
    - `scripts/generate-tool-support-table.mjs`, `src/mcp/vice/tool-support-table.test.mjs` and `src/mcp/vice/capability-registry.test.ts` each contain `ANNO_TOOL_DEFINITIONS`, and the three regex declarations remain three separate declarations (no shared import between them).
    - `cd src/mcp/vice && node --test anno-tools.test.ts stock-dispatch.test.ts tool-support-table.test.mjs capability-registry.test.ts hostpath-consumers.test.ts docs-linerefs.test.ts` exits 0.
    - `node scripts/check-npm-packages.mjs` exits 0.
  </acceptance_criteria>
  <done>One `anno_*` verb is registered through `buildViceTool()` and answers a real query against a real store end to end, with every registration-time guard moved in the same commit, and the whole 1.24 s structural-guard set green.</done>
  <reversibility rating="costly">The `r2000_*` family leaves the advertised tool surface here and five guards fan out to follow it; the modules themselves survive until plan 29-10, so this commit alone is revertible, but the guard re-points are not free to undo.</reversibility>
</task>

<task type="auto">
  <name>Task 2: The generator's negative control — byte-identity proven, and a missed witness throws rather than differs</name>
  <files>src/mcp/vice/tool-support-table.test.mjs, docs/tool-support.md</files>
  <read_first>
    - scripts/generate-tool-support-table.mjs — lines 93-160, especially `discoverSyntheticToolNames`'s "could not resolve synthetic tool registration identifier" throw at :137-146.
    - src/mcp/vice/tool-support-table.test.mjs — the whole file; its header states why three independent bounding witnesses exist.
    - docs/tool-support.md — the committed table (7,874 bytes as measured 2026-08-29).
    - .planning/phases/29-the-mcp-surface/29-RESEARCH.md — the executed byte-identity proof under "Verified: `docs/tool-support.md` regenerates byte-identical".
  </read_first>
  <action>
Add a test to `tool-support-table.test.mjs` that is the recorded negative control
for `MCP-03`'s "neither manifest nor `docs/tool-support.md` gains an entry": run
`discoverSyntheticToolNames` against a synthetic proxy source that registers
`tools[annoDef.name]` from a loop over an array named something the regex does not
match, and assert the call **throws** with a message naming the unresolved
identifier — never returns a different name set. This encodes the WR-08 bounding
property that the generator cannot emit a wrong table, only throw.

Then assert positively that the regenerated table equals the committed
`docs/tool-support.md` byte for byte, and that `discoverSyntheticToolNames` over
the real proxy source still returns exactly `vice_diagnose`, `vice_recycle`,
`vice_result_continue`. Do not regenerate and commit a changed
`docs/tool-support.md` — a diff here means the substitution was not
identifier-only and must be investigated, not accepted.
  </action>
  <verify>
    <automated>cd src/mcp/vice && node --test tool-support-table.test.mjs && git diff --exit-code -- ../../../docs/tool-support.md</automated>
  </verify>
  <acceptance_criteria>
    - `src/mcp/vice/tool-support-table.test.mjs` contains a test asserting `discoverSyntheticToolNames` throws on an un-re-pointed loop-variable regex.
    - `cd src/mcp/vice && node --test tool-support-table.test.mjs` exits 0.
    - `git diff --exit-code -- docs/tool-support.md` exits 0 (the committed table is unchanged by the substitution).
    - The regenerated table's byte length equals the committed file's byte length.
  </acceptance_criteria>
  <done>`docs/tool-support.md` is proven byte-identical after the substitution, and a witness left un-re-pointed is proven to throw by name rather than silently emit a different table.</done>
</task>

<task type="auto">
  <name>Task 3: MCP-02 by construction — the hostpath consumer set stays exactly five</name>
  <files>src/mcp/vice/hostpath-consumers.test.ts</files>
  <read_first>
    - src/mcp/vice/hostpath-consumers.test.ts — lines 115-235, especially `EXPECTED_IMPORTERS` at :143 (the exact-five `deepEqual` plus length assertion) and the named-absence test at :156-171.
    - ./CLAUDE.md — the derived-tool path-translation constraint bullet and its two `rewriteArguments()` line citations.
    - src/mcp/vice/anno-tools.ts — as written by Task 1.
  </read_first>
  <action>
Extend `hostpath-consumers.test.ts`'s named-absence test — the one that already
lists `stock-disassemble.ts`, `disasm-*.ts`, `stock-symbols.ts` and friends with
the message "must not import hostpath.ts, whether or not it exists yet" — to name
every module this phase adds: `anno-tools.ts`, `anno-derive.ts`, `anno-details.ts`,
`anno-register.ts` and `anno-cli.ts`. The named-absence form is deliberate: it
asserts the constraint for files that do not exist yet, so a later plan cannot
introduce the import unnoticed.

Leave `EXPECTED_IMPORTERS` and its `assert.equal(importers.length, 5)` byte-identical.
That five-member `deepEqual` **is** `MCP-02`'s "no new module imports `hostpath.ts`"
and is what makes CLAUDE.md's derived-tool constraint satisfied by construction for
this family — there is no interception to forget because `rewriteArguments()` is
unreachable from a runner that never touches the host-path seam.

Do not touch `R2000_MODULE_FLOOR` or the `INT-01` positive control in this plan;
they break on the **rename**, not on registration, and plan 29-04 owns them.
  </action>
  <verify>
    <automated>cd src/mcp/vice && node --test hostpath-consumers.test.ts docs-linerefs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/mcp/vice/hostpath-consumers.test.ts` names `anno-tools.ts`, `anno-derive.ts`, `anno-details.ts`, `anno-register.ts` and `anno-cli.ts` in its named-absence list.
    - `src/mcp/vice/hostpath-consumers.test.ts` still contains `assert.equal(importers.length, 5)` and its `EXPECTED_IMPORTERS` array is unchanged.
    - `cd src/mcp/vice && node --test hostpath-consumers.test.ts docs-linerefs.test.ts` exits 0.
  </acceptance_criteria>
  <done>Every module this phase adds is named as forbidden from the host-path seam before it exists, and the five-member consumer set is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client (the LLM) → `runAnnoTool` | Every argument is LLM-supplied; `vice-proxy.ts:3230`'s `validate: (value) => ({ value })` means the transport validates nothing |
| `runAnnoTool` → filesystem | The `store` argument is a path chosen by the caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Tampering / Information Disclosure | the `store` path argument to `runAnnoTool` | high | mitigate | Resolve every path through `storePathWithinWorkspace()` (`anno-types.ts:1140`) against `repoRoot()`; never `resolve()` + `startsWith`. WR-01's finding is carried: containment is enforced against the deepest existing ancestor's realpath. |
| T-29-02 | Elevation of Privilege | `assertAnnoTool`'s allow-list | high | mitigate | Set membership in `CURATED_ANNO_TOOLS` is the FIRST statement of the gate, and `CURATED_ANNO_TOOLS` is derived from `ANNO_TOOL_DEFINITIONS` rather than hand-typed, so a name cannot be curated in one place and absent from the other. |
| T-29-03 | Denial of Service | an unclosed store handle | medium | mitigate | `closeStore` in a `finally` on every path (D-06); asserted structurally in `anno-tools.test.ts`. |
| T-29-04 | Repudiation | a failure that surfaces as a silent success | high | mitigate | The runner's catch names the error class (`[${errName}]`) rather than collapsing to a string, matching `r2000-tools.ts:1205-1213`. |
| T-29-05 | Information Disclosure | error text on the wire | low | accept | `vice-proxy.ts:3480-3488` renders a thrown error as `e.message` only; the `anno-types.ts` family already puts the offending value in the message, and the workspace root is not secret in a local stdio server. |
| T-29-SC | Tampering | npm/pip/cargo installs | high | accept | This phase installs no external package (`29-RESEARCH.md` § Package Legitimacy Audit: not applicable, no packages added). No install task exists, so no legitimacy checkpoint is owed. |
</threat_model>

<verification>
- `cd src/mcp/vice && node --test anno-tools.test.ts stock-dispatch.test.ts tool-support-table.test.mjs capability-registry.test.ts module-classification.test.ts hostpath-consumers.test.ts docs-linerefs.test.ts` — green (~1.3 s).
- `cd src/mcp/vice && npm run typecheck` — clean.
- `node scripts/check-npm-packages.mjs`, `node scripts/check-skill-tool-coverage.mjs`, `node scripts/check-skill-fork-honesty.mjs`, `node scripts/audit-gate.mjs` — all green.
- `git diff --exit-code -- docs/tool-support.md` — no diff.
</verification>

<success_criteria>
- One `anno_*` verb is advertised and answers end to end against a real store.
- The registering commit carries all five registration-time guard re-points.
- `docs/tool-support.md` is byte-identical and a missed witness throws by name.
- `hostpath.ts`'s consumer set is still exactly five and `vice-proxy.ts`'s line count is unchanged.
</success_criteria>

## Artifacts this phase produces

New symbols and files created by **this plan** (excluded from drift verification —
they do not exist before this plan runs):

- Files: `src/mcp/vice/anno-tools.ts`, `src/mcp/vice/anno-tools.test.ts`
- Types: `AnnoToolDefinition`, `ToolCallResult` (module-private re-declaration)
- Constants: `ANNO_TOOL_DEFINITIONS`, `CURATED_ANNO_TOOLS`
- Functions: `runAnnoTool`, `assertAnnoTool`, `okText`, `errText`, `isPlainObject`
- Error classes: `AnnoUncuratedToolError`
- Tool names: `anno_get_symbols`
- Identifiers introduced into existing files: the `annoDef` loop variable in `vice-proxy.ts`, and the `"annoDef.name"` entry in `BACKEND_SEAM_BYPASS_KEYS`

The full phase-wide artifact list is in `29-08-PLAN.md`'s "Artifacts this phase
produces" section, which is the phase's canonical roll-up.

<output>
Create `.planning/phases/29-the-mcp-surface/29-01-SUMMARY.md` when done.
</output>
