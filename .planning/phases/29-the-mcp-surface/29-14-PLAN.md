---
phase: 29-the-mcp-surface
plan: 14
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/vice/anno-cli.ts
  - src/mcp/vice/anno-memmap-render.ts
  - src/mcp/vice/anno-cli-path-consumers.test.ts
  - src/mcp/vice/anno-cli.test.ts
  - src/mcp/vice/module-classification.ts
autonomous: true
gap_closure: true
requirements: [REPOINT-01]
user_setup: []

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "CLAUDE.md's standing constraint: EVERY caller-supplied path the `anno` CLI accepts -- the two positionals, `--store`, `--provenance` and `--out` on both verbs -- is confined through `storePathWithinWorkspace()` against `repoRoot()` before it reaches any `readFileSync` or `writeFileSync`"
    - "`anno render-memmap <store> --provenance <path outside the workspace>` refuses naming the confinement, and the refusal text contains NONE of the target file's bytes"
    - "`anno render-memmap <store> --provenance <ok> --out <existing file>` refuses to overwrite unless `--force` is passed, and `--force` is an accepted option of that verb"
    - "A closed-consumer-set test enumerates every caller-supplied path argument the CLI accepts and fails when one of them reaches a filesystem call without passing through the confinement seam -- so the next unconfined argument fails a test rather than a review"
    - "The three headers that assert confinement as a maintained property (`anno-cli.ts`'s file header, `cmdCoverage`'s doc, `refuseOverwrite()`'s doc) each describe what the code now actually does, and `anno-memmap-render.ts`'s `provenancePath` field is documented rather than silent"
    - "MCP-02 stays satisfied by construction: no `anno-*` module imports `hostpath.ts`, and the confinement used here is the annotation family's OWN seam"
    - "`anno-cli.ts` is a LINE-PINNED file: `module-classification.ts` cites `:63` for `renderMemoryMap`, `:68` for `buildCoverageReport` and, in prose, `:169` for `checkAcceptedOptions`. This plan adds header prose above all three and an option entry above the third, so all three citations move -- and all three are corrected in the SAME task as the edit that moved them, with `module-classification.test.ts` green as an acceptance criterion"
  artifacts:
    - path: "src/mcp/vice/anno-cli.ts"
      provides: "Both verbs' output and sidecar paths confined at the CLI boundary, `--force` on `render-memmap`, and headers that match the code"
      contains: "storePathWithinWorkspace"
    - path: "src/mcp/vice/anno-cli-path-consumers.test.ts"
      provides: "The closed-consumer-set assertion over the CLI's caller-supplied path arguments, modelled on `hostpath-consumers.test.ts`, with a non-vacuity floor and a planted-violation positive control"
      contains: "CLI_PATH_ARGUMENTS"
    - path: "src/mcp/vice/module-classification.ts"
      provides: "The three `anno-cli.ts` citations -- two structured `basis.consumers[].line` values and one prose `path:NN` -- re-measured against the post-edit file so DIRECTION 9 and DIRECTION 9b stay green"
      contains: "checkAcceptedOptions"
    - path: "src/mcp/vice/anno-memmap-render.ts"
      provides: "A `provenancePath` field documented with the same confinement statement `storePath` carries, and a sidecar parse failure that names the failure without echoing file bytes"
      contains: "provenancePath"
  key_links:
    - from: "src/mcp/vice/module-classification.ts (the anno-cli.ts consumer citations)"
      to: "src/mcp/vice/anno-cli.ts (lines 63, 68 and 169)"
      via: "the same task that shifts a cited line re-measures the citation, so the guarded record never lags the file it points into"
      pattern: "checkAcceptedOptions"
    - from: "src/mcp/vice/anno-cli.ts (cmdRenderMemmap, cmdCoverage)"
      to: "src/mcp/vice/anno-types.ts (storePathWithinWorkspace)"
      via: "every caller-supplied path resolved through the one seam before any filesystem call"
      pattern: "storePathWithinWorkspace"
    - from: "src/mcp/vice/anno-cli-path-consumers.test.ts"
      to: "src/mcp/vice/anno-cli.ts"
      via: "the test derives the CLI's option surface from VERB_OPTIONS rather than a hand-typed list, so a new path-shaped flag joins the audit automatically"
      pattern: "VERB_OPTIONS"
  prohibitions:
    - statement: "Never write, keep or restore a comment that asserts a guarantee the code does not provide. A header naming a maintained property is a written warrant for the next maintainer not to check; when the property and the prose disagree, the prose is the more dangerous half."
---

<objective>
Close verification gap 3 — the reproduced confinement escape on the `anno` CLI's
caller-supplied paths, and the three headers that assert in writing the property
the code does not keep.

Reproduced on this working tree by the verifier, on arguments that are
LLM-composed by design (the shipped playbooks invoke this CLI through Bash):

- **`--out` on `render-memmap` escaped the workspace root entirely and silently
  overwrote a pre-existing file.** `--force` is not in that verb's option set at
  all, and the verb never reaches `refuseOverwrite()`.
- **`--provenance` is an arbitrary-file-read oracle with content disclosure.**
  Pointed at a file outside the repo, it echoed that file's opening bytes back
  in the error message, because the sidecar parse failure interpolates Node's
  own parse error and that error carries a content snippet.
- **`--out` on `coverage`** takes the same escape: it is checked for overwrite
  and then written, both against the raw caller string.

The seam itself is excellent — fifteen thorough tests of
`storePathWithinWorkspace()` — and its CONSUMER SET is unenumerated. That
asymmetry is the whole mechanism of the failure, and it is the thing worth
fixing rather than the four call sites alone. `hostpath-consumers.test.ts` does
exactly this for the host-path seam and is the model.

Purpose: this is a violation of CLAUDE.md's standing architectural constraint
that any host-facing or caller-supplied path goes through one confinement seam,
on the delivery path REPOINT-01's absorbed procedures are documented to invoke.
Output: both verbs confined on every argument, `--force` where the safety check
needs it, a closed-consumer-set test, and three headers that tell the truth.

**Scope note.** The confinement used here is `storePathWithinWorkspace()` from
`anno-types.ts` — the annotation family's OWN seam. Do NOT reach for
`hostpath.ts`: no `anno-*` module imports it, that absence is asserted
structurally by `hostpath-consumers.test.ts` and by a body-slice assertion in
`stock-dispatch.test.ts`, and it is how MCP-02 is satisfied by construction.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-the-mcp-surface/29-VERIFICATION.md
@.planning/phases/29-the-mcp-surface/29-REVIEW.md
@.planning/phases/29-the-mcp-surface/29-07-SUMMARY.md
@.planning/phases/29-the-mcp-surface/29-12-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Confine every caller-supplied path on both verbs, and give `render-memmap` the overwrite refusal it documents elsewhere</name>
  <files>src/mcp/vice/anno-cli.ts, src/mcp/vice/anno-cli.test.ts, src/mcp/vice/module-classification.ts</files>
  <read_first>
    - **`src/mcp/vice/module-classification.ts` — read this BEFORE you touch `anno-cli.ts`.** It is a mechanically-guarded record of symbols and line citations pointing into this repo's modules, and three of them point into the file you are about to edit: the structured consumer `{ path: "src/mcp/vice/anno-cli.ts", symbol: "renderMemoryMap", line: 63 }`, the structured consumer `{ path: "src/mcp/vice/anno-cli.ts", symbol: "buildCoverageReport", line: 68 }`, and the prose citation `` `checkAcceptedOptions` (`anno-cli.ts:169`) `` in the header's glue-module extractables survey. Every step below inserts lines above at least one of them.
    - `src/mcp/vice/module-classification.test.ts` lines 262-288 (**DIRECTION 9**) — the structured-citation guard: for every consumer carrying a `line`, it asserts that line of that file CONTAINS the cited symbol. Its own comment records that three citations had already drifted "because earlier work in the same phase inserted lines above them" — which is precisely what this task does.
    - `src/mcp/vice/module-classification.test.ts` lines 289-336 and the DIRECTION 9b tests around line 568 — the PROSE-citation guard added for WR-06: every `path:NN` in `module-classification.ts`'s own source must resolve to a real file and a real, non-blank line, plus symbol containment where the prose names a symbol on the same line. `anno-cli.ts:169` is one of six prose citations it checks.
    - `src/mcp/vice/anno-cli.ts` lines 30-60 — the file header's "Never grow a second path validator" paragraph. It is one of the three headers this task must make true; read it before changing anything so you write the code the header already promised.
    - `src/mcp/vice/anno-cli.ts` lines 148-200 — `VERB_OPTIONS` (the frozen per-verb option set, exported) and `refuseOverwrite()` with its doc claiming uniformity across every verb that writes an output file.
    - `src/mcp/vice/anno-cli.ts` lines 250-360 — `cmdRenderMemmap()` end to end: the argument parse, the confinement of the positional store, the sidecar existence check, the output-path default, the `--check` branch, and the write.
    - `src/mcp/vice/anno-cli.ts` lines 690-850 — `cmdCoverage()` end to end: its doc's "NO SECOND PATH VALIDATOR" property, the two confined path resolutions, the overwrite check, and the report write.
    - `src/mcp/vice/anno-types.ts` around line 1192 — `storePathWithinWorkspace()`'s signature, what it returns (the REALPATH, not the input) and the `AnnoStorePathError` it throws. Every call site must use the RETURNED path downstream, never the raw argument.
    - `src/mcp/vice/anno-confinement.test.ts` lines 1-115 — the seam's own control set and the temp-workspace scaffolding helpers. Reuse that scaffolding shape for the new CLI-level cases rather than inventing a second one.
    - `src/mcp/vice/anno-cli.test.ts` — the existing verb-level cases and how they invoke the CLI in-process.
  </read_first>
  <behavior>
    All driven through the CLI entry function in-process, against a real temp workspace with a real store, a real sidecar and a sibling directory OUTSIDE the workspace root:
    - Test A (currently RED): `render-memmap <store> --provenance <sidecar> --out <path outside the workspace>` returns a non-zero exit code, prints a refusal naming the confinement, and creates NOTHING at the outside path.
    - Test B (currently RED): `render-memmap <store> --provenance <file outside the workspace>` returns non-zero and refuses; the combined stdout+stderr contains NONE of that file's contents. Plant a file whose first bytes are a distinctive token and assert that token is absent from the output — that is the content-disclosure property, and asserting only the exit code would not see it.
    - Test C (currently RED): `render-memmap <store> --provenance <sidecar> --out <existing file inside the workspace>` returns non-zero, refuses naming the existing file, and leaves that file's bytes unchanged.
    - Test D (currently RED): the same invocation with `--force` succeeds and overwrites — the over-refusal control, without which C is satisfied by a verb that can never write.
    - Test E (currently RED): `coverage <image> --store <store> --out <path outside the workspace>` returns non-zero, refuses, and creates nothing at the outside path.
    - Test F (must stay GREEN): an `--out` inside the workspace still writes on both verbs, and `render-memmap --check` against an in-workspace rendered file still reports in-sync / drifted / missing correctly.
    - Test G (must stay GREEN): the default output path — the one derived beside the store when `--out` is omitted — still resolves and still writes.
  </behavior>
  <action>
    In `src/mcp/vice/anno-cli.ts`:

    1. `VERB_OPTIONS`: add the force flag to `render-memmap`'s accepted set.
       It already exists on `coverage`, and `refuseOverwrite()`'s doc already
       claims the safety is uniform across every verb that writes an output
       file; today `render-memmap` writes one and has neither the flag nor the
       check.

    2. `cmdRenderMemmap()`'s argument parser: parse the force flag alongside
       the existing `--check`, in the same boolean shape `cmdCoverage`'s parser
       already uses. Thread it into the command function.

    3. `cmdRenderMemmap()`: after the workspace root is resolved and inside the
       SAME `try` that already confines the positional store, resolve the
       sidecar argument through `storePathWithinWorkspace()` too, and use the
       returned realpath for the existence check and for every downstream call
       — the render call, and the drift-check call. The raw caller string must
       not survive past that block. Confining it here also confines it for
       `anno-memmap-render.ts`, which reads it without a check of its own.

    4. `cmdRenderMemmap()`: resolve the output path through
       `storePathWithinWorkspace()` as well. Do it AFTER the default is applied,
       so the derived default and a caller-supplied value are confined by the
       same rule rather than the default being trusted because it was derived.
       Then, before the write and only on the non-check branch, call
       `refuseOverwrite()` with the confined output path, the parsed force flag
       and this verb's label, returning a non-zero exit code when it refuses.
       Use the confined path for the write and for the success line, so the
       message names the file that was actually written.

    5. `cmdCoverage()`: resolve the output argument through
       `storePathWithinWorkspace()` in the same `try` that already confines the
       positional and the store, and use the returned path for the overwrite
       check, for the write, and for the "wrote" line. Today all three use the
       raw caller string.

    6. Correct the three headers so each describes what the code now does, and
       add to each the concrete incident that motivated it in this file's
       established voice — CR-02 for the escaping and silently-overwriting
       output path, CR-03 for the sidecar read oracle. Name the seam once and
       state that the CONSUMER SET, not only the predicate, is now under test,
       citing the new test file by name. A header that asserts a property must
       point at the mechanism that keeps it.

    In `src/mcp/vice/anno-cli.test.ts`, add the seven cases from `<behavior>`
    under a heading naming CR-02, CR-03 and WR-08.

    7. **`anno-cli.ts` is LINE-PINNED, and steps 1 and 6 both move the pins.**
       `module-classification.ts` cites this file three times: `:63` for
       `renderMemoryMap` and `:68` for `buildCoverageReport` as structured
       `basis.consumers[].line` values, and `anno-cli.ts:169` for
       `checkAcceptedOptions` in the header's extractables-survey prose. Step 6
       adds prose in the 30-60 header region, which moves all three; step 1 adds
       an entry to `VERB_OPTIONS` around line 153, which moves the third again.

       Re-measure and correct all three IN THIS TASK, after the code edits are
       final. Measure, do not arithmetic: run
       `grep -n 'renderMemoryMap\|buildCoverageReport' src/mcp/vice/anno-cli.ts | head`
       and `grep -n 'export function checkAcceptedOptions' src/mcp/vice/anno-cli.ts`
       against the finished file and write the observed numbers in. Then run
       `cd src/mcp/vice && node --test module-classification.test.ts` and read
       DIRECTION 9's and DIRECTION 9b's output; both name the offending citation
       and quote the line they landed on, so a wrong number is diagnosed rather
       than guessed at.

       Correct the citation, never the code, and never the guard. Do NOT make
       the edit line-neutral to dodge this: a line-neutral edit is a constraint
       the next maintainer cannot see and will unknowingly break, whereas an
       updated citation plus a green guard in the verify is checkable forever.
       Do NOT delete a citation to silence a failure — DIRECTION 9's and 9b's
       non-vacuity tests are relations rather than counts, so a deleted citation
       passes silently and costs the record exactly the coverage it exists for.
       Change nothing else in `module-classification.ts`: no verdict, no
       rationale, no fate, no requirement anchor.
  </action>
  <verify>
    <automated>cd src/mcp/vice && node --test anno-cli.test.ts anno-confinement.test.ts module-classification.test.ts 2>&1 | tail -20 && npm run typecheck</automated>
    Expected: all three test files green including the seven new cases, typecheck clean. `anno-confinement.test.ts`'s fifteen predicate tests must be UNCHANGED and still green — they are the untouched regression surface for this change. `module-classification.test.ts` green is the proof that step 7's three citations landed: it is NOT in `29-BASELINE.md`'s failing set, so reddening it is a regression by the baseline's own rule.
  </verify>
  <acceptance_criteria>
    - `anno render-memmap <store> --provenance /etc/passwd` exits non-zero and its combined output contains none of that file's bytes.
    - `anno render-memmap <store> --provenance <ok> --out <outside-workspace path>` exits non-zero and creates nothing at that path.
    - `anno render-memmap <store> --provenance <ok> --out <existing in-workspace file>` exits non-zero without `--force` and succeeds with it.
    - `anno coverage <image> --store <store> --out <outside-workspace path>` exits non-zero and creates nothing at that path.
    - `cd src/mcp/vice && grep -c 'storePathWithinWorkspace' anno-cli.ts` prints at least `5`.
    - `cd src/mcp/vice && node --test anno-cli.test.ts anno-confinement.test.ts` exits 0, with `anno-confinement.test.ts` unmodified.
    - `cd src/mcp/vice && node --test module-classification.test.ts` exits 0 — DIRECTION 9 and DIRECTION 9b both green against the post-edit `anno-cli.ts`.
    - `module-classification.ts`'s two structured `anno-cli.ts` citations name lines that contain `renderMemoryMap` and `buildCoverageReport` respectively, and its prose `anno-cli.ts:NN` citation names a line containing `checkAcceptedOptions`. Record the before and after line numbers for all three in the summary.
    - `git diff src/mcp/vice/module-classification.ts` shows changes to three line numbers and nothing else — no verdict, rationale, fate or requirement anchor moved.
  </acceptance_criteria>
  <reversibility rating="reversible">Argument-handling changes inside two command functions plus one entry added to a frozen option map; no on-disk format, no exported signature change.</reversibility>
  <done>Every caller-supplied path on both verbs is resolved through the one seam before any filesystem call; `render-memmap` refuses to overwrite without `--force`; and the three headers describe the code rather than an intention.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The sidecar parse failure names the failure without echoing the file's bytes, and `provenancePath` stops being documented by silence</name>
  <files>src/mcp/vice/anno-memmap-render.ts, src/mcp/vice/anno-cli.test.ts</files>
  <read_first>
    - `src/mcp/vice/anno-memmap-render.ts` lines 305-370 — `RenderMemoryMapOptions` (note that `storePath`'s confinement is documented in four lines and `provenancePath` one line below carries no comment at all — that silence is what the review names as the mechanism), and `renderMemoryMap()`'s sidecar read and parse.
    - `src/mcp/vice/anno-memmap-render.ts` lines 503-560 — `CheckRenderedMemoryMapOptions` and `checkRenderedMemoryMap()`, which reads `renderedPath` raw and re-enters `renderMemoryMap()`. Both of its caller-supplied paths are confined by task 1 at the CLI boundary; this task records that fact where the fields are declared.
    - **This file carries a literal NUL byte, so GNU grep classifies it as binary.** A plain `grep` over it prints nothing and exits 1. Use `grep -a` for every search and every verify gate against this file. A census that forgets this silently skips the file and has already produced one wrong decision in this repo.
    - `src/mcp/vice/anno-cli.ts` — the confinement you added in task 1, so the comments you write here name the caller that performs it rather than claiming this module performs it.
  </read_first>
  <behavior>
    - A sidecar file whose contents are not JSON produces a failure message that names the path and states the file is not valid JSON, and that contains NONE of the file's bytes. Assert with a planted distinctive token in the file's opening bytes.
    - A sidecar that IS valid JSON but is not a valid provenance header still fails with the existing structured message from the header parser — this task must not blur a schema failure into a syntax failure.
    - A valid sidecar still renders — the over-refusal control.
  </behavior>
  <action>
    In `src/mcp/vice/anno-memmap-render.ts`:

    1. Document `provenancePath` on `RenderMemoryMapOptions` with the same
       statement `storePath` carries one line above: the CALLER confines it
       through the one seam before this module is entered, this module performs
       no confinement of its own, and it must never be given a path that has
       not been through that seam. Name `anno-cli.ts` as the confining caller
       and CR-03 as the finding that the silence caused. Do the same for
       `provenancePath` and `renderedPath` on `CheckRenderedMemoryMapOptions`,
       which are the same two arguments one layer over.

    2. In `renderMemoryMap()`, the sidecar syntax-failure branch currently
       interpolates the underlying parse error into the thrown message, and
       that error's text carries a snippet of the file's own content. Stop
       interpolating it. Throw a message that names the sidecar path and states
       that it is not valid JSON, and — where the underlying error exposes one
       — the byte OFFSET at which parsing failed, which is a position and not
       content. Add a comment recording why: this argument reaches the module
       from an agent-composed Bash invocation, so an error that quotes the
       file's bytes turns a read refusal into a content-disclosure oracle
       (CR-03).

       Leave the sidecar READ-failure branch as it is: it reports an errno-class
       failure and carries no file content.

    3. Leave `parseProvenanceHeader()`'s structured schema failures untouched.
       A schema failure names FIELDS the caller supplied and is not a
       disclosure route; blurring it into the syntax failure would lose the
       diagnostic the verb depends on.

    In `src/mcp/vice/anno-cli.test.ts`, add the three cases from `<behavior>`
    beside task 1's, so the disclosure property is asserted at the CLI boundary
    the agent actually uses.
  </action>
  <verify>
    <automated>cd src/mcp/vice && node --test anno-cli.test.ts anno-memmap-render.test.ts 2>&1 | tail -20 && grep -ac 'provenancePath' anno-memmap-render.ts</automated>
    Expected: both files green; the final grep (note `-a`) prints a count of at least 9. If it prints nothing and exits 1, you omitted `-a` and the gate did not run.
  </verify>
  <acceptance_criteria>
    - A non-JSON sidecar whose first bytes are a distinctive planted token produces a failure whose text does not contain that token.
    - The failure text still names the sidecar path and still says the file is not valid JSON.
    - A structurally valid but schema-invalid sidecar still fails through the header parser with its existing message.
    - A valid sidecar still renders successfully.
    - `cd src/mcp/vice && grep -a -c 'provenancePath' anno-memmap-render.ts` succeeds (exit 0) and prints a non-zero count.
    - `cd src/mcp/vice && node --test anno-memmap-render.test.ts` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">One thrown message reworded and three doc comments added; no signature or control-flow change.</reversibility>
  <done>The sidecar read can no longer be used to read out any file's opening bytes, and every caller-supplied path field on this module's two option interfaces states who confines it.</done>
</task>

<task type="auto">
  <name>Task 3: The closed-consumer-set assertion — the next unconfined CLI argument fails a test, not a review</name>
  <files>src/mcp/vice/anno-cli-path-consumers.test.ts</files>
  <read_first>
    - `src/mcp/vice/hostpath-consumers.test.ts` lines 1-60 and 100-200 — **this is the source-of-truth analog for this task.** Read it for: the file-header statement of what the closed set IS and why widening it is a reviewed decision; the comment-stripping discipline (a real import statement matched after comments are stripped, never a bare substring count against raw text, because headers and string literals in this repo mention the very names being policed); the `readdirSync`-plus-stable-prefix derivation; the hand-pinned non-vacuity floor with its explicit prohibition on deriving the floor from disk; and the planted-violation test that proves the predicate can go red.
    - `src/mcp/vice/anno-cli.ts` — `VERB_OPTIONS` (exported, frozen) and the two command functions, so the argument inventory is derived from the CLI's own declared option surface rather than hand-typed.
    - `src/mcp/vice/anno-confinement.test.ts` lines 1-60 — that file's own stated reason for being SEPARATE from `anno-store.test.ts`: keeping the existing pins as an untouched regression rather than as assertions the change could quietly adjust. The same reasoning is why this task adds a new file instead of extending that one — the predicate and its consumer set are different subjects, and conflating them is precisely how CR-02 and CR-03 shipped past a green suite.
    - `scripts/lib/anno-cli-verbs.mjs` — `stripComments()`, a single-pass character scanner rather than a regex, with a recorded reason. If comment stripping is needed here, mirror that discipline; do not hand-roll a regex stripper.
  </read_first>
  <action>
    Create `src/mcp/vice/anno-cli-path-consumers.test.ts`. It is the CONSUMER-SET
    counterpart to `anno-confinement.test.ts`'s PREDICATE coverage, and its file
    header must say so in those terms, naming CR-02 and CR-03 as the two
    findings that fell through the gap between the two.

    It asserts four things:

    1. **The inventory is declared and complete.** A hand-pinned constant —
       name it `CLI_PATH_ARGUMENTS` — enumerates every caller-supplied path
       argument the CLI accepts, as a verb plus an argument identifier plus
       whether it is a positional or a flag: the store positional and the
       sidecar and output flags on the render verb, and the image positional,
       the store flag and the output flag on the coverage verb. Assert that
       every path-shaped FLAG named in this constant is present in the real
       `VERB_OPTIONS` for that verb, imported from `anno-cli.ts` — so an
       inventory entry naming an option the CLI does not have fails here rather
       than sitting inert.

    2. **The inventory has not gone stale underneath the CLI.** For each verb,
       assert that every entry of the real `VERB_OPTIONS` that is not an
       explicitly-listed non-path option (the check flag, the force flag, the
       sample flag) IS named in `CLI_PATH_ARGUMENTS`. This is the direction
       that catches the actual failure mode: a new path-shaped flag added to
       the CLI without a confinement call reds HERE, by name, rather than
       being reviewed. Keep the non-path exclusion list explicit and short, so
       adding to it is a visible decision.

    3. **Every declared consumer routes through the seam.** Read
       `anno-cli.ts`'s own source, strip comments with the same single-pass
       discipline `scripts/lib/anno-cli-verbs.mjs` uses, and assert that the
       count of `storePathWithinWorkspace(` call sites in the stripped source
       is at least the number of entries in `CLI_PATH_ARGUMENTS`. Comment
       stripping is mandatory here and not optional hygiene: this file's own
       header, and `anno-cli.ts`'s, both mention the seam by name in prose, so
       an unfiltered count would pass by counting the very comments that
       described the problem.

    4. **Two positive controls, so the predicate can go red.**
       (a) A planted synthetic source in the shape of a command function that
       takes an output argument and writes it WITHOUT a seam call is reported
       by the same predicate the real scan uses — the planted-violation shape
       `hostpath-consumers.test.ts` uses for the same purpose.
       (b) A non-vacuity floor: assert `CLI_PATH_ARGUMENTS` has at least six
       entries, hand-pinned as an integer literal with a comment stating the
       measurement and forbidding its derivation from disk, for the same reason
       that file records — a floor computed from the thing it guards can never
       fail.

    Add the whole-file rationale in this repo's established header voice: WHY
    the file exists (the seam was proven correct and its callers were
    unenumerated, which is the exact hole two blockers fell through), what it
    is the one authoritative place for (the CLI's caller-supplied path argument
    inventory), and what NOT to do (never add an entry here without adding the
    confinement call; never derive the floor from disk; never widen the
    non-path exclusion list to silence a failure).

    Do NOT modify `anno-confinement.test.ts`. Its fifteen predicate tests are
    the untouched regression surface for this change.
  </action>
  <verify>
    <automated>cd src/mcp/vice && node --test anno-cli-path-consumers.test.ts anno-confinement.test.ts 2>&1 | tail -15 && npm run typecheck && node --test hostpath-consumers.test.ts 2>&1 | tail -5</automated>
    Expected: the new file green; `anno-confinement.test.ts` green and unmodified; typecheck clean; `hostpath-consumers.test.ts` green — this last one confirms the new TEST file did not disturb `ANNO_MODULE_FLOOR`, which counts production modules only and must be unchanged by this plan.
  </verify>
  <acceptance_criteria>
    - `src/mcp/vice/anno-cli-path-consumers.test.ts` exists and exports/declares `CLI_PATH_ARGUMENTS` with at least six entries.
    - Removing any one `storePathWithinWorkspace(` call from `anno-cli.ts` makes this test file fail — demonstrate this once by hand, revert, and record the observed failure text in the summary.
    - Adding a path-shaped flag to `VERB_OPTIONS` without an inventory entry makes this test file fail — demonstrate, revert, record.
    - `anno-confinement.test.ts` has zero diff in this plan.
    - `cd src/mcp/vice && node --test hostpath-consumers.test.ts` exits 0 with `ANNO_MODULE_FLOOR` unchanged at its current pinned value.
  </acceptance_criteria>
  <reversibility rating="reversible">A new test file; deleting it restores the prior state exactly.</reversibility>
  <done>The CLI's caller-supplied path arguments are a declared, closed, mechanically-checked set with a floor and two positive controls, in the shape the host-path seam has used since Phase 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-composed Bash invocation → `anno` CLI arguments | Untrusted, LLM-composed path strings cross into `readFileSync` and `writeFileSync`. The shipped playbooks tell the agent to compose these; this is the designed route, not an abuse of one. |
| workspace root → the host filesystem | The confinement seam is the only thing standing between a mistyped or manipulated argument and any file the process can reach |
| CLI error text → the agent's context | Bytes interpolated into a refusal become readable by whatever composed the invocation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-14-01 | Information Disclosure | `render-memmap --provenance` | **high** | mitigate | **The attacker is a compromised or merely mistaken agent composing a Bash invocation; the asset is any file outside the workspace root the process can read.** Today the argument reaches `readFileSync` raw and the JSON parse failure echoes the file's opening bytes back, so the flag is a read oracle with content disclosure. Mitigated on both halves: task 1 resolves the argument through `storePathWithinWorkspace()` before any read, so a path outside the root never reaches the filesystem; task 2 stops interpolating the parse error, so even an in-workspace unreadable file discloses a position and not content. Task 3 keeps the mitigation from silently regressing by putting the argument in a declared, mechanically-checked consumer set. |
| T-29-14-02 | Tampering | `render-memmap --out`, `coverage --out` | **high** | mitigate | Same attacker, asset is any file outside the workspace root the process can WRITE — the verifier reproduced a silent destruction of a pre-existing file's contents. Mitigated by confining both output arguments through the same seam and by routing the render verb through `refuseOverwrite()` with a real `--force` opt-in, so an in-workspace overwrite is deliberate and an out-of-workspace one is impossible. The closed-consumer-set test is the anti-regression mechanism. |
| T-29-14-03 | Tampering | `checkRenderedMemoryMap`'s rendered-file path | medium | mitigate | The drift-check branch reads the output path too. It is confined by the same CLI-boundary resolution as the write path, so both branches use one confined value rather than two rules. |
| T-29-14-04 | Repudiation | headers asserting an unkept guarantee | **high** | mitigate | A comment naming a maintained property is a written warrant for the next maintainer not to check — the verifier's stated reason this is a BLOCKER rather than a warning. All three headers are corrected to describe the code, and each now names the mechanism (the new consumer-set test) rather than only the intention. |
| T-29-14-05 | Elevation of Privilege | reaching for `hostpath.ts` to fix a path problem | medium | mitigate | Explicitly out of bounds: no `anno-*` module may import `hostpath.ts`, asserted by `hostpath-consumers.test.ts` and by a body-slice assertion in `stock-dispatch.test.ts`. The fix uses the annotation family's own seam, and both assertions are re-run in `<verification>`. |
| T-29-14-06 | Denial of Service | symlink cycles / dangling links in a confined path | low | accept | Already mitigated by the seam itself and covered by `anno-confinement.test.ts` tests 10 and 15 (kernel hop bound). This plan adds consumers to that seam and does not change it. |
| T-29-14-07 | Repudiation | `module-classification.ts`'s pinned citations into `anno-cli.ts` | medium | mitigate | A guarded record whose citations drift becomes a record that points a reader at the wrong line while a green suite says otherwise — the same class of written-warrant failure as T-29-14-04, one level up. This plan inserts lines above all three `anno-cli.ts` citations, so task 1 step 7 re-measures and corrects them in the same task, and `module-classification.test.ts` is in the task's `<automated>` block and in two acceptance criteria. The alternative — a line-neutral edit — is rejected on the record: an invisible constraint is not a mitigation. |
| T-29-14-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install in this plan; no dependency added or changed. |

**ASVS L1 disposition:** every `high` threat above is `mitigate`, with the mitigation named as a specific code change and each paired with a mechanism that keeps it from regressing. No `high` finding is accepted or transferred.
</threat_model>

<verification>
1. `cd src/mcp/vice && node --test anno-cli.test.ts anno-memmap-render.test.ts anno-cli-path-consumers.test.ts anno-confinement.test.ts` — all green.
2. `cd src/mcp/vice && node --test module-classification.test.ts` — green. This carries DIRECTION 9 (structured `basis.consumers[].line` containment) and DIRECTION 9b (prose `path:NN` resolution and containment). It is **absent from `29-BASELINE.md`'s failing set**, so a red here is a regression attributable to this plan, not a pre-existing condition. Task 1 step 7 is the only thing standing between this plan's header edits and that red.
3. `cd src/mcp/vice && node --test hostpath-consumers.test.ts stock-dispatch.test.ts` — green. These carry MCP-02: no `anno-*` module imports `hostpath.ts`, and `runAnnoTool`'s body contains none of the three forwarding symbols. This plan must not disturb either.
4. `cd src/mcp/vice && npm run typecheck` — clean.
5. `cd src/mcp/vice && npm run test:automated` — compare the failing-file SET against `29-BASELINE.md`, never a count against zero.
6. `node scripts/check-npm-packages.mjs` — exit 0. The new file is a test file and must NOT enter `package.json`'s `files[]`; this gate proves no test file leaked into either tarball. Its assertions are RELATIONAL (no `node_modules/`, no `*.test.*`, no fixtures; skill count derived from `src/skills/`), not pinned integer file counts, so adding `module-classification.ts` to this plan's `files_modified` — a file already tracked and already unpublished — moves nothing it measures.
7. `node scripts/check-no-regenerator2000.mjs` — exit 0, temporary allow-list still **0 entries**.
8. `node scripts/audit-gate.mjs` — exit 0. Create no new REVIEW-shaped artifact in this plan.
9. `node scripts/generate-tool-support-table.mjs` then `md5sum docs/tool-support.md` — unchanged at `bb47448…`. This plan edits `anno-cli.ts` file headers and adds a `--force` CLI flag; **neither is an MCP tool description**, and the table is generated from the two manifests, so it MUST regenerate byte-identical. That is exactly why asserting it is worth the two seconds: a cheap check whose expected answer is "no change" is the one that catches an unintended change. If the md5 moves, something in this plan reached a manifest and the regeneration is in scope for the task that did it. (Matches item 6 of plan 29-13 and item 5 of plan 29-16 — all three plans assert the same digest so no single plan can move it unnoticed.)
10. Every grep against `src/mcp/vice/anno-memmap-render.ts` uses `grep -a`. That file carries a literal NUL byte and a plain grep silently reports nothing while exiting 1.
11. **Wave 1 is `29-13`, `29-14` and `29-17`, and the three `files_modified` sets are disjoint** — re-verified after `module-classification.ts` was added here. 29-13 owns `anno-tools.ts` / `anno-tools.test.ts` (and needs no citation update: `module-classification.ts`'s only mention of `anno-tools.ts` is a path-only `supersededBy` with no line number). 29-17 owns `.planning/REQUIREMENTS.md` alone, which nothing cites by line. This plan owns `anno-cli.ts`, `anno-memmap-render.ts`, `anno-cli.test.ts`, `anno-cli-path-consumers.test.ts` and `module-classification.ts`. Plans **29-15 (now wave 2)** and **29-16 (now wave 3)** must each edit `module-classification.ts` as well — 29-15 for its two `c64-program-recon/SKILL.md` prose citations, 29-16 for the same three `anno-cli.ts` citations this plan corrects, which its own import changes move again — so they were pushed out of wave 1 rather than left to collide with this plan in parallel worktrees.
</verification>

<success_criteria>
- All six caller-supplied path arguments across the two verbs resolve through `storePathWithinWorkspace()` before any filesystem call.
- `--force` exists on `render-memmap` and the verb refuses an existing output file without it.
- A sidecar pointed outside the workspace refuses without disclosing bytes; a sidecar inside the workspace that is not JSON refuses without disclosing bytes.
- `anno-cli-path-consumers.test.ts` exists, has a hand-pinned floor, and was observed red on two separate hand-planted violations then reverted.
- `anno-confinement.test.ts` is byte-unchanged.
- `module-classification.test.ts` is green: all three of its `anno-cli.ts` citations were re-measured against the post-edit file in the same task that moved them.
- The gates in `<verification>` exit 0, `md5sum docs/tool-support.md` is unchanged at `bb47448…`, and the automated-arm failing-file set is no worse than the baseline's.
</success_criteria>

<output>
Create `.planning/phases/29-the-mcp-surface/29-14-SUMMARY.md` when done.
Record: the before/after output of the two reproduced escapes (the out-of-workspace
write and the content-disclosing read), the exact refusal text now produced by each,
the two planted violations observed red against `anno-cli-path-consumers.test.ts`
with their failure messages, confirmation that `anno-confinement.test.ts` has
zero diff, the before and after line numbers of all three `module-classification.ts`
citations into `anno-cli.ts` with the commands used to measure them, and the
observed `md5sum docs/tool-support.md`.
</output>

## Artifacts this phase produces

| Kind | Name | File |
|---|---|---|
| file | `anno-cli-path-consumers.test.ts` — the CLI path-argument consumer-set test | `src/mcp/vice/anno-cli-path-consumers.test.ts` |
| constant | `CLI_PATH_ARGUMENTS` — the declared inventory of caller-supplied path arguments | `src/mcp/vice/anno-cli-path-consumers.test.ts` |
| CLI flag | `--force` on the `render-memmap` verb | `src/mcp/vice/anno-cli.ts` (`VERB_OPTIONS`) |
| test | the out-of-workspace `--out` refusal cases on both verbs (CR-02) | `src/mcp/vice/anno-cli.test.ts` |
| test | the out-of-workspace `--provenance` refusal case (CR-03) | `src/mcp/vice/anno-cli.test.ts` |
| test | the content-disclosure absence case with a planted token (CR-03) | `src/mcp/vice/anno-cli.test.ts` |
| test | the overwrite-refusal case and its `--force` over-refusal control (CR-02/WR-08) | `src/mcp/vice/anno-cli.test.ts` |
| test | the in-workspace write and default-output-path controls | `src/mcp/vice/anno-cli.test.ts` |

No new production module. `ANNO_MODULE_FLOOR` is unchanged: it counts
`anno-*.ts` production modules and excludes `*.test.*`.

`src/mcp/vice/module-classification.ts` is edited but produces no artifact: task 1
step 7 re-measures three existing line citations and changes nothing else. It is
not an `anno-*.ts` module, so `ANNO_MODULE_FLOOR` is unaffected by its presence in
`files_modified`; it adds no file to either tarball, and `check-npm-packages.mjs`
asserts relations rather than pinned file counts, so no count moves.
