---
phase: 29-the-mcp-surface
plan: 11
type: execute
wave: 8
depends_on: ["29-10"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - scripts/check-no-regenerator2000.mjs
autonomous: true
requirements: [MCP-02, MCP-05, STORE-06]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No sentence in the roadmap or the requirements still describes a plan that was abandoned: the coexistence clauses that D-01 falsified are edited, not left standing beside the outcome that contradicts them."
    - "Phases 30 to 32 are NARROWED, not renumbered: every requirement still maps to exactly one phase, and the requirements moved into this phase are moved in the traceability table rather than duplicated."
    - "The ordering constraint that said the removal follows the re-pointing is RESTATED as an intra-phase constraint of this phase rather than deleted — it is the reason the removal gate was built before the deletion commit, and deleting it would erase the reason."
    - "The withdrawal of the symbol round trip appears in the requirements record beside the requirements it affects, with its return condition named, and the phase that restores it names where the carried-forward fixtures live."
    - "The removal gate's temporary allow-list block is empty and the gate asserts its own emptiness, so a later phase cannot quietly re-add an entry to it."
    - "The block reaches empty by every entry being DISCHARGED by the plan that cited it, never by an entry being deleted here for lack of an owner and never by one being converted into a permanent exemption for a statement that is false after the deletion — that conversion is the widening this phase's standing prohibition forbids, and it would make emptiness a bookkeeping fact instead of a real one."
    - "Every phase requirement of this phase has a status in the traceability table that matches what was actually delivered, and no requirement is marked complete whose own evidence says otherwise."
    - "`CUT-04` is the one cut requirement this phase does NOT pull forward, and that is recorded as an explicit decision with its reason — it stays in Phase 32 as a retrospective vacuity audit over the guards this phase re-pointed — rather than left as an unexplained residue in a Phase 32 reduced to two requirements."
  artifacts:
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
  key_links:
    - "ROADMAP per-phase Requirements lines <-> REQUIREMENTS.md traceability table (exactly one phase per requirement)"
    - "the intra-phase ordering constraint <-> why plan 29-02 ran before plan 29-10"
  prohibitions:
    - statement: "The record must not be left describing a plan that was abandoned: a criterion or clause falsified by an owner decision is edited with the decision cited, never left standing beside the outcome that contradicts it and never silently deleted."
      status: unverified
      verification: flagged
---

<objective>
Make the record match what happened: edit the criteria and clauses D-01 falsified,
narrow the phases this one consumed, and close the gate's last temporary entry.

Purpose: `29-CONTEXT.md`'s in-scope item 7. D-01 explicitly says the record is not
left describing a plan that was abandoned. Two of this phase's own requirements
carry sentences that are now false by decision, three later phases shrank, and one
ordering constraint became an intra-phase constraint rather than an inter-phase one.
Output: an edited roadmap and requirements record, and a removal gate with no
temporary allow-list left.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/29-the-mcp-surface/29-CONTEXT.md
@.planning/phases/29-the-mcp-surface/29-RESEARCH.md
@.planning/phases/29-the-mcp-surface/29-10-SUMMARY.md
</context>

## Decisions and readings adopted by this plan

- **Discretion 5, adopted as research recommends: NARROW, do not renumber.** Phase
  numbers are cited from tracked, guarded files — one guard reads a phase-11
  evidence directory with no existence guard, the manifest audit resolves a
  phase-19 directory as a literal path, and a third guard exists specifically to
  police phase pointers. The traceability table maps every requirement to exactly
  one phase, so a renumber rewrites every row while a narrow rewrites only the rows
  whose phase actually moved. Phase 30 is untouched by D-01 apart from one note.
  Renumbering is strictly more expensive and buys nothing a requirements-line edit
  does not.
- **Ordering constraint 3 is restated, not deleted.** It said the removal follows
  the skill re-pointing, which was an inter-phase constraint between 31 and 32.
  Both are now inside this phase, so it becomes an **intra-phase** ordering
  constraint and must be recorded as one — it is the reason the removal gate was
  built and observed biting before the deletion commit, and deleting the constraint
  would delete the reason.
- **Worktree isolation must be off for this plan.** It edits planning documents;
  worktree mode strips those files from commits (`execute-plan.md:532`, and the
  worktree prompt at `execute-phase.md:707` forbids executors from touching them),
  so the plan would appear to succeed while delivering nothing. The project runs
  with worktrees ENABLED, so the orchestrator must force the stock per-plan
  carve-out for this plan only — set `USE_WORKTREES_FOR_PLAN=false` in the
  per-plan worktree gate, exactly as a submodule-touching plan gets. The gate
  re-records the plan-scoped isolation sentinel itself; do not disable worktrees
  project-wide.

<tasks>

<task type="auto">
  <name>Task 1: Edit the roadmap — the falsified criterion, the narrowed phases, the restated constraint</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md § "Phase 29" — criterion 2 in full, and the notes list.
    - .planning/ROADMAP.md § "Phase 30" — its five criteria and its notes, especially the ordering-constraint note and the note about re-recording both transcripts from real assembler output.
    - .planning/ROADMAP.md § "Phase 31" — its five criteria, of which the first three are consumed by this phase and the last two are not.
    - .planning/ROADMAP.md § "Phase 32" — its five criteria and its "Depends on" line, and which halves of criterion 5 are consumed.
    - .planning/ROADMAP.md § "Sequencing Rationale (v0.7.0)" — ordering constraints 2, 3, 4 and 5.
    - .planning/phases/29-the-mcp-surface/29-10-SUMMARY.md — what was actually deleted, so the edited criteria describe the outcome rather than the plan.
  </read_first>
  <precondition>This plan is dispatched with `USE_WORKTREES_FOR_PLAN=false` (the stock per-plan worktree carve-out), so it runs on the main working tree. A worktree-isolated run strips `.planning/` files from the commit, so the plan would report success while delivering nothing. Verify the dispatch was non-isolated, NOT that the project-level `use_worktrees` is false — it is true.</precondition>
  <action>
Edit the roadmap with scoped replacements only — never rewrite the whole file, which
would destroy every phase entry outside the edit window.

Replace this phase's criterion 2. Its current text says both families are
registered and callable at the phase's close and calls that the point of the phase.
That was superseded by the owner's decision before planning began. Write the
outcome instead: the new family is registered and the retired one is deleted in the
same phase, with the ordering that made that safe named — the gate built and
observed biting first, the skills re-pointed before the removal, and the guards
moved with the change that broke each. Cite D-01 by id so a reader can find the
decision rather than infer it.

Add one sentence to Phase 30's notes recording that the two pinned transcripts were
carried forward, naming the directory they now live in and repeating that they are
the shape to reproduce rather than content to assert against — Phase 30's own note
already says both must be re-recorded from real assembler output, so the two
statements must agree.

Narrow Phase 31: drop the criteria this phase discharged and keep the ones it did
not — the attribution chain, the trigger-description rewrite and its collision
re-check, and the manifest's own re-sync trigger. Update its goal sentence so it
describes what is left rather than what it was. Narrow Phase 32 the same way: drop
the criteria this phase discharged, keep the recorded-fate criterion and the
living-document half of its last criterion, and update its "Depends on" line, which
currently names a phase whose content moved.

Restate ordering constraint 3 rather than deleting it: record that it became an
intra-phase constraint of this phase, that it is why the removal gate was built and
observed biting before the deletion commit, and that the `4f048bb` precedent is what
it exists against. Update constraints 2 and 4 to say where their subjects now live.

Add to Phase 32's notes the two guard fates this phase deferred rather than
discharged, so its recorded-fate criterion picks them up instead of them sitting
inert: the transitional block-type vocabulary arm whose removal trigger changed, and
any guard whose fate plan 29-10's summary records as deferred.

**Record why `CUT-04` stays behind, in the same edit.** This phase pulls `CUT-01`,
`CUT-02`, `CUT-03` and `CUT-05` forward and leaves `CUT-04` — *every guard and CI
script pinned to the deleted subject has a recorded fate and none passes
vacuously* — in Phase 32. That is deliberate, and left unstated it reads as an
oversight in a phase reduced to two requirements. State the reason in Phase 32's
notes: `CUT-04` is a **retrospective vacuity audit**, and its subject only exists
once the re-pointing has happened. This phase re-points a large guard set —
the module floor, the CLI verb floor, the skill-coverage floor, the registry's
non-vacuity relation, the docs-decisions pair and the fork-honesty assertion — and
each is proven non-vacuous *individually*, at the commit that moved it, by this
phase's own prohibition. What `CUT-04` adds is the **mechanical sweep over the
whole set at once**, measured after the dust settles, which is a different check
and one that cannot be run before the last guard has moved. Keeping it in Phase 32
is therefore the only place it can be honest. Say that, and name this phase as the
source of most of the guards it will audit, so the Phase 32 planner knows the scope
grew rather than shrank.
  </action>
  <verify>
    <automated>cd src/mcp/vice && node --test docs-dangling-refs.test.ts docs-deferred-ledger.test.ts docs-review-disposition.test.ts docs-absorbed-decisions.test.ts docs-linerefs.test.ts docs-fork-decision.test.ts docs-core-value-decision.test.ts comment-phase-pointers.test.ts && cd ../../.. && node scripts/audit-gate.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` § Phase 29 criterion 2 describes the deletion outcome and cites D-01 by id, and contains no claim that both families are registered at the phase's close.
    - `.planning/ROADMAP.md` § Phase 30's notes name the directory the carried-forward transcripts live in and state they are the shape rather than the content.
    - `.planning/ROADMAP.md` § Phase 31 and § Phase 32 each contain only the criteria this phase did not discharge, and Phase 32's "Depends on" line names a phase that still owns what it depends on.
    - `.planning/ROADMAP.md` § "Sequencing Rationale (v0.7.0)" contains a restated ordering constraint recording that the removal-follows-re-pointing rule became an intra-phase constraint of this phase, and names the `4f048bb` precedent.
    - `.planning/ROADMAP.md` § Phase 32's notes state why `CUT-04` was not pulled forward — that it is a retrospective vacuity audit whose subject only exists once every guard has moved — and name this phase as the source of most of the guards it audits.
    - `.planning/ROADMAP.md` § Phase 32's `**Requirements**` line names `CUT-04` and `CUT-06` and no requirement this phase carried out.
    - The per-phase Progress table and every other phase entry outside the edited sections are byte-identical (verifiable in the diff as scoped edits).
    - `cd src/mcp/vice && node --test docs-dangling-refs.test.ts docs-deferred-ledger.test.ts docs-review-disposition.test.ts docs-absorbed-decisions.test.ts docs-linerefs.test.ts docs-fork-decision.test.ts docs-core-value-decision.test.ts comment-phase-pointers.test.ts` exits 0.
    - `node scripts/audit-gate.mjs` exits 0.
  </acceptance_criteria>
  <done>The roadmap describes what happened and what is left, with the ordering constraint that made it safe restated rather than erased.</done>
</task>

<task type="auto">
  <name>Task 2: Edit the requirements — the coexistence clauses, the traceability moves, the withdrawal</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — the six requirement lines this phase carries, and specifically the two whose closing sentences assert that both families coexist and that nothing is deleted to make them pass.
    - .planning/REQUIREMENTS.md § Traceability — the table mapping every requirement to exactly one phase, and its own statement that the mapping is the roadmap's per-phase requirements lines.
    - .planning/REQUIREMENTS.md — the requirement whose text names the symbol round trip as a validated capability implemented by a module this phase renamed.
    - .planning/phases/29-the-mcp-surface/29-07-SUMMARY.md — the withdrawal note's exact wording in the project record, so the two agree rather than differ.
    - .planning/phases/29-the-mcp-surface/29-10-SUMMARY.md — what was delivered, so no status claims more than the evidence.
  </read_first>
  <precondition>Worktree isolation is disabled for this plan's execution, for the same reason as Task 1.</precondition>
  <action>
Edit with scoped replacements only.

Rewrite the two closing sentences D-01 falsified. Each currently asserts that the
two families coexist at this point and that nothing is deleted to make the guards
pass. Replace each with the decision and its consequence, citing D-01 by id: the
old family is deleted in this phase, the guards were moved in the commit that broke
each of them, and the coexistence the clause assumed did not happen. Leave the rest
of each requirement untouched — the first sentence of the guard requirement, which
lists the guards, survives unedited and the decision record says so explicitly.

Move the requirements this phase discharged in the **traceability table**, not by
duplicating them: the two procedure re-pointing requirements and the four cut
requirements this phase carried out — `CUT-01`, `CUT-02`, `CUT-03` and `CUT-05` —
now map to this phase; the ones it did not carry out stay where they are.
`CUT-04` and `CUT-06` stay mapped to Phase 32, and `CUT-04`'s retention is the one
that needs a reason rather than an omission: it is a retrospective vacuity audit
over the whole re-pointed guard set, and its subject does not exist until the last
guard has moved, which is this phase's close. Record that reason beside the
requirement itself as well as in the roadmap note, so a reader who checks only the
requirements record finds it too. Every requirement must still map to exactly one phase, and
the table's own statement that the mapping follows the roadmap's per-phase lines
must remain true after the edit — so make the roadmap edit in Task 1 and this one
agree.

Add the withdrawal record beside the requirement whose text names the symbol round
trip. State that the export and import route was withdrawn in this phase, that the
two validated requirements it implements therefore have no route for the duration of
the next phase, that this is a temporary loss of a validated capability rather than
a completed one, and where it returns. Use the same wording as the note plan 29-07
put in the project record, so a reader comparing the two finds one statement rather
than two.

Set each of this phase's six requirement statuses to what the evidence supports and
nothing more. If any requirement's own evidence says it was not fully met, record it
as not met with the reason — a status that claims more than the evidence is the
failure the audit gate exists against.
  </action>
  <verify>
    <automated>cd src/mcp/vice && node --test docs-dangling-refs.test.ts docs-absorbed-decisions.test.ts docs-deferred-ledger.test.ts && cd ../../.. && node scripts/audit-gate.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Neither requirement contains a sentence asserting that both families coexist or that nothing is deleted to make the guards pass, and each carries a citation to D-01.
    - The guard-list sentence of the guard requirement is unchanged.
    - Every requirement in the traceability table maps to exactly one phase, and the phase named for each requirement matches the roadmap's per-phase requirements line for that phase.
    - `CUT-01`, `CUT-02`, `CUT-03` and `CUT-05` map to this phase; `CUT-04` and `CUT-06` still map to Phase 32.
    - `CUT-04` carries a recorded reason for staying in Phase 32 — a retrospective vacuity audit whose subject only exists once every guard has moved — and that reason is stated in both `.planning/REQUIREMENTS.md` and the roadmap's Phase 32 notes, in agreement.
    - The requirement naming the symbol round trip carries a withdrawal record naming both requirement ids, the temporary nature of the loss, and the phase it returns in, with wording matching the project record.
    - Each of this phase's six requirement statuses is supported by a named summary, and any unmet requirement is recorded as unmet with a reason.
    - `node scripts/audit-gate.mjs` exits 0.
  </acceptance_criteria>
  <done>The requirements record matches the decisions and the deliveries, with every requirement still owned by exactly one phase.</done>
</task>

<task type="auto">
  <name>Task 3: Close the gate's last temporary entry and run the phase sweep</name>
  <files>scripts/check-no-regenerator2000.mjs</files>
  <read_first>
    - scripts/check-no-regenerator2000.mjs — the temporary allow-list block and its dated header, which states that the block must be empty at the phase's close and that this plan asserts it.
    - .planning/phases/29-the-mcp-surface/29-BASELINE.md — the recorded pre-phase failing-file set.
    - .planning/phases/29-the-mcp-surface/29-VALIDATION.md — the phase-gate sampling row: the full glob with the broker stopped, all CI scripts, the generated table byte-identical, and every documentation guard green.
  </read_first>
  <precondition>The VICE broker is not running. A live broker makes one backend test fail deterministically, which would contaminate the comparison against the recorded baseline.</precondition>
  <action>
Remove the last entries from the temporary allow-list and replace the block with an
assertion that it is empty, keeping the dated header as the record of what it was
for and when it existed.

Before emptying it, walk the block once and confirm every remaining entry names
**this** plan. An entry citing an earlier plan is an orphan — its owner has already
run and did not discharge it — and the correct response is to stop and report,
never to delete it here and never to move it into the permanent exemption set. Plan
29-05's reachability rule exists to make that state unreachable (an entry may cite
only a plan carrying both the gate and the entry's own file), so an orphan found
here means the rule was violated upstream and the file it names still says something
about a route that no longer exists. An empty block with an assertion is a guard against a later
phase quietly re-adding an entry; a deleted block is an invitation to reintroduce
one.

Run the phase sweep as the validation contract defines it. Stop the broker. Run the
full-glob suite in the background and collect it when it exits. Run the automated
set, the typecheck, and all five CI scripts. Confirm the generated tool-support
table is byte-identical. Confirm every documentation guard is green.

Compare the full-glob failing-file **set** against the recorded baseline set, name
every difference, and explain each one. A name that disappeared is an improvement to
account for — several are expected, since this phase deleted tests — and a name that
appeared is a regression that must be fixed before the phase closes rather than
carried into verification.

Record the closing measurements in the summary so the next phase has a baseline of
its own: the failing-file set, the wall time, the removal gate's scanned-file count
and per-class breakdown, and the new module and tool-name floors.
  </action>
  <verify>
    <automated>node scripts/check-no-regenerator2000.mjs && node scripts/check-skill-tool-coverage.mjs && node scripts/check-skill-fork-honesty.mjs && node scripts/check-npm-packages.mjs && node scripts/audit-gate.mjs && cd src/mcp/vice && npm run test:automated && npm run typecheck && git diff --exit-code -- ../../../docs/tool-support.md</automated>
    <human-check>The full-glob suite was run with the broker stopped and in the background; its failing-file set is compared against `29-BASELINE.md` in `29-11-SUMMARY.md` with every difference named and explained, and no new failing file appears.</human-check>
  </verify>
  <acceptance_criteria>
    - `scripts/check-no-regenerator2000.mjs`'s temporary allow-list is empty and the script asserts its emptiness, with the dated header retained.
    - Every entry the block held when this plan started cited this plan; the summary records the walk, and any entry citing an earlier plan halted this plan rather than being deleted or converted to a permanent exemption.
    - No permanent exemption was added by this plan.
    - Adding any entry to the emptied allow-list makes the gate exit non-zero — observed and recorded, then reverted.
    - All five CI scripts exit 0.
    - `cd src/mcp/vice && npm run test:automated` and `npm run typecheck` exit 0.
    - `git diff --exit-code -- docs/tool-support.md` exits 0.
    - `29-11-SUMMARY.md` records the closing failing-file set, wall time, gate scanned-file count and per-class breakdown, and the new module and tool-name floors.
    - `git status --porcelain` is empty.
  </acceptance_criteria>
  <done>The gate carries no temporary exception, the suite matches its recorded baseline with every difference explained, and the phase's closing measurements are written down.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the planning record → every later phase | A phase that reads a falsified criterion plans against a world that does not exist |
| the temporary allow-list → the removal gate | An allow-list that outlives its reason is an exemption nobody decided |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-46 | Repudiation | a record left describing an abandoned plan | high | mitigate | Every falsified criterion and clause is edited with the decision cited by id, and the documentation guards are run as an acceptance criterion. |
| T-29-47 | Tampering | a whole-file rewrite destroying phase entries outside the edit window | high | mitigate | Scoped replacements only, with an acceptance criterion that every entry outside the edited sections is byte-identical in the diff. |
| T-29-48 | Elevation of Privilege | a temporary allow-list quietly re-populated later | medium | mitigate | The block is emptied and its emptiness asserted, with the assertion itself observed to fire before being reverted. |
| T-29-49 | Repudiation | a requirement status claiming more than its evidence | high | mitigate | Each status must cite the summary that evidences it, and an unmet requirement is recorded as unmet with a reason rather than flipped to complete. |
| T-29-50 | Repudiation | a regression carried into verification | high | mitigate | The failing-file set is compared name by name against the recorded baseline, and a new name must be fixed before the phase closes. |
| T-29-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed by this plan. |
</threat_model>

<verification>
- All five CI scripts green; `npm run test:automated` and `npm run typecheck` green.
- Full-glob suite, broker stopped, failing-file set compared to `29-BASELINE.md` with every difference explained.
- `docs/tool-support.md` byte-identical; every documentation guard green.
- The removal gate's temporary allow-list is empty and its emptiness is asserted.
</verification>

<success_criteria>
- No sentence in the roadmap or requirements still describes the abandoned plan.
- Phases 30 to 32 are narrowed, every requirement owned by exactly one phase.
- The intra-phase ordering constraint is recorded as the reason the gate came first.
- The removal gate carries no temporary exception and the suite matches its baseline.
</success_criteria>

## Artifacts this phase produces

New records created by **this plan**:

- The restated intra-phase ordering constraint in the sequencing rationale
- The withdrawal record beside the symbol-round-trip requirement
- The emptied-and-asserted temporary allow-list block in the removal gate
- The closing measurements recorded in `29-11-SUMMARY.md`: failing-file set, wall time, gate scanned-file count and per-class breakdown, and the new module and tool-name floors

<output>
Create `.planning/phases/29-the-mcp-surface/29-11-SUMMARY.md` when done.
</output>
