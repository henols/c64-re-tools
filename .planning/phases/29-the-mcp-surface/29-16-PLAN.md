---
phase: 29-the-mcp-surface
plan: 16
type: execute
wave: 3
depends_on: [29-14, 29-15]
files_modified:
  - src/mcp/vice/anno-coverage.ts
  - src/mcp/vice/anno-cli.ts
  - src/mcp/vice/anno-coverage.test.ts
  - src/mcp/vice/anno-cli.test.ts
  - src/mcp/vice/prg-image.ts
  - src/mcp/vice/module-classification.ts
  - src/skills/routine-queue-walker/SKILL.md
  - scripts/check-skill-cli-invocations.mjs
  - scripts/lib/anno-cli-invocations.mjs
  - src/mcp/vice/anno-cli-invocations.test.ts
  - scripts/lib/anno-cli-verbs.mjs
  - scripts/check-npm-packages.mjs
  - .github/workflows/ci.yml
  - .planning/phases/29-the-mcp-surface/29-VERIFICATION.md
autonomous: true
gap_closure: true
requirements: [REPOINT-01, REPOINT-02]
user_setup: []

estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "REPOINT-01 / ROADMAP criterion 2: `anno coverage game.prg --store game.annostore` -- the ONLY measurement instruction `routine-queue-walker` has -- runs to exit 0 against a real image and a real store, instead of printing a report of zeros and exiting 1"
    - "REPOINT-01 probe/encoding: the coverage verb dispatches its positional by file EXTENSION before any byte-length check, so a short flat capture is refused by name rather than parsed as a .prg with an origin read backwards out of its own payload"
    - "One decode, two callers: the census's payload load and the cross-reference byte source are the SAME function, so the two halves of one report can never describe different programs -- the property `projectImage()`'s own comment already claimed"
    - "REPOINT-01/REPOINT-02 structural: a CI gate ARGUMENT-CHECKS every documented `anno <verb>` invocation in both skill trees -- every flag against the verb's real option set and every positional against the file kinds that verb accepts -- so a dead command fails a gate rather than a review"
    - "REPOINT-01 probe/adjacency: an invocation whose positional extension exactly matches an accepted kind passes the gate, and one differing by extension alone fails it -- proven by a planted violation observed red and reverted"
    - "REPOINT-01 probe/empty: a skill file set from which zero invocations are extracted fails the gate's non-vacuity floor loudly rather than passing as 'nothing to check'"
    - "WR-14 (reduced, decision recorded in `<wr14_scope_decision>`): no USER-FACING string and no stale CROSS-REFERENCE on the `anno` CLI path still names the retired subcommand vocabulary, and every floor value and count those sites touch is unchanged. The two INTERNAL identifier renames -- the exported entry function and the test-hatch environment variable -- are deliberately NOT taken in this round and stay recorded warnings"
    - "`anno-cli.ts` is a LINE-PINNED file and this plan moves its pins a SECOND time, after plan 29-14 already corrected them: `module-classification.ts` cites it at `:63` for `renderMemoryMap`, `:68` for `buildCoverageReport` and, in prose, at `:169` for `checkAcceptedOptions`. Task 1's import changes move the third; all three are re-measured in the same task, with `module-classification.test.ts` green as an acceptance criterion"
    - "WR-07: `prg-image.ts`'s reachability rationale names an import that actually exists, so the stated reason it is in the published file list is true"
    - "Symmetry with 29-14 (`T-29-14-01`): the coverage path's JSON-SYNTAX failure no longer interpolates the parse error's text into its reason, so the two sibling verbs give the same treatment to the same content-disclosure shape in the same round -- a caller-supplied path is answered with a position, never with the file's own bytes"
    - "The CI gate this plan writes is DECLARED IN `.github/workflows/ci.yml` beside the FIVE `node scripts/check-*` gate steps already declared there, taking that count to six, so it runs on every push rather than only existing on disk"
    - "WR-14's partial discharge is recorded in `29-VERIFICATION.md`'s WR-14 row itself -- the artifact a later reader opens to find out what is still open -- rather than only in a plan file and a summary"
    - statement: "Re-running `anno coverage` twice against the same store and image returns the same report figures and leaves the store byte-identical -- the verb opens read-only and writes nothing"
      verification: backstop
    - statement: "installer/skills/ is regenerated from src/skills/ by prepack/sync-skills before the invocation gate scans it, so the gate reads the shipped text rather than a stale copy"
      verification: backstop
  artifacts:
    - path: "src/mcp/vice/anno-coverage.ts"
      provides: "An exported image loader that dispatches the two live image forms by extension before any length check, with the retired JSON project form kept only as a trailing legacy branch"
      contains: "loadProjectImage"
    - path: "scripts/check-skill-cli-invocations.mjs"
      provides: "The CI gate that argument-checks every documented anno CLI invocation in both trees, with a non-vacuity floor"
      contains: "ANNO_INVOCATION_FLOOR"
    - path: "scripts/lib/anno-cli-invocations.mjs"
      provides: "The extraction and checking predicates, importable by both the CI gate and its committed test -- the one-definition-two-callers shape anno-cli-verbs.mjs already uses"
      contains: "parseDocumentedInvocations"
    - path: "src/mcp/vice/anno-cli-invocations.test.ts"
      provides: "The committed non-vacuity and planted-violation proof for the invocation gate's predicates"
      contains: "parseDocumentedInvocations"
    - path: "src/mcp/vice/module-classification.ts"
      provides: "The three `anno-cli.ts` citations re-measured against this plan's import changes, and the two `anno-coverage.ts` citations confirmed unmoved"
      contains: "checkAcceptedOptions"
    - path: ".github/workflows/ci.yml"
      provides: "The new gate declared as the SIXTH CI-step gate, beside the five node-scripts-check steps already declared there, so the invocation gate runs rather than merely existing"
      contains: "check-skill-cli-invocations"
    - path: ".planning/phases/29-the-mcp-surface/29-VERIFICATION.md"
      provides: "The WR-14 row annotated in place with which sites shipped and which remain open, so the partial discharge is durable rather than summary-only"
      contains: "WR-14"
  key_links:
    - from: "src/mcp/vice/anno-cli.ts (projectImage)"
      to: "src/mcp/vice/anno-coverage.ts (loadProjectImage)"
      via: "a direct import, replacing the second hand-rolled decode -- one definition, two callers"
      pattern: "loadProjectImage"
    - from: "scripts/check-skill-cli-invocations.mjs"
      to: "src/mcp/vice/anno-cli.ts (VERB_OPTIONS)"
      via: "the gate imports the CLI's own frozen option map rather than a hand-typed flag list"
      pattern: "VERB_OPTIONS"
    - from: "src/mcp/vice/anno-cli-invocations.test.ts"
      to: "scripts/lib/anno-cli-invocations.mjs"
      via: "the test calls the predicates in isolation, so it does not re-run the live gate on import"
      pattern: "parseDocumentedInvocations"
    - from: ".github/workflows/ci.yml"
      to: "scripts/check-skill-cli-invocations.mjs"
      via: "a CI step declared beside the five existing node-scripts-check gate steps -- a gate that is not declared here does not run"
      pattern: "check-skill-cli-invocations"
    - from: "src/mcp/vice/module-classification.ts (the anno-cli.ts and anno-coverage.ts citations)"
      to: "src/mcp/vice/anno-cli.ts, src/mcp/vice/anno-coverage.ts"
      via: "the same task that changes an import block re-measures every citation that block sits above"
      pattern: "checkAcceptedOptions"
---

<objective>
Close the second half of verification gap 2 (CR-05) and the structural blind spot
that let both halves ship, plus the two warnings that live in the same files.

**CR-05.** `anno coverage game.prg --store game.annostore` — the ONLY measurement
instruction `routine-queue-walker` has, and the delivery path for one of
REPOINT-01's five absorbed procedures — prints a full report of zeros and exits
1. The positional is loaded by two functions that between them understand exactly
one format: JSON carrying a gzip-then-base64 payload, whose only producer was
DELETED in this same phase. Unlike CR-04 there is no correct spelling to move to,
so this one needs a production change.

**The structural reason both halves shipped green.**
`check-skill-tool-coverage.mjs` resolves tool and verb NAMES and never an
invocation's arguments. A name-only floor cannot see a dead command. That gate is
correct at what it does; what is missing is a sibling that checks the arguments.

**Folded in, and each is genuinely in a file this plan already opens:**
**WR-07** — `prg-image.ts`'s stated reachability reason names an import
`anno-cli.ts` does not have, and this plan changes that file's imports again, so
leaving it would compound a false rationale. **WR-14, in part** — the mechanical
rename left user-facing strings and cross-references on the retired vocabulary,
including the message a user gets for an unknown verb. Three of its five sites
are taken here; the two internal identifier renames are dropped from this round
on a recorded decision. See `<wr14_scope_decision>` below.

**One symmetry defect, corrected rather than shipped.** Plan 29-14 classifies the
`render-memmap --provenance` sidecar's JSON-syntax failure as `T-29-14-01`, a
**high** Information Disclosure threat, and stops it interpolating the parse
error's text — because that text carries a snippet of the file's own bytes, which
turns a caller-supplied path into a read oracle. `anno-coverage.ts:2129` is the
same shape byte-for-byte on the sibling verb:
`` `${projectPath} is not valid JSON -- ${err instanceof Error ? err.message : String(err)}` ``.
An earlier draft of task 1 said to keep it. That would have shipped opposite
treatments of one defect class, on two sibling verbs, in one round, with no stated
reason — and the asymmetry argument runs the other way: `coverage`'s `<project>`
positional is equally caller-supplied and equally LLM-composed. It is confined by
`storePathWithinWorkspace()` at `anno-cli.ts:770`, which is why the residual
severity is **medium** rather than 29-14's **high** — the oracle cannot leave the
workspace — but an in-workspace content echo is still a content echo. Task 1 step
1 now applies 29-14's own treatment to that one branch. See `T-29-16-06`.

**Explicitly NOT folded in:** WR-13 (`check-no-analyser.mjs`'s
`exemptionFor()` short-circuit). It is a real latent defect, it is correct today
only by array order, and it is in the phase's own removal gate — but that gate is
green tree-wide with an empty allow-list and touching its exemption walk in a
gap-closure round risks the one mechanism every other plan here is verified
against. It stays a recorded warning. Also not folded in: WR-01, WR-02, WR-03,
WR-04, WR-05, WR-06, WR-11, WR-12 (no gap plan opens their files), and WR-09
(deferred to Phase 30 by the verification itself).

Purpose: REPOINT-01 and REPOINT-02 are recorded `Gaps Found`; this plan closes
the half that needs code, and builds the gate that makes the next re-pointing
checkable.
Output: a live measurement command, one decode with two callers, an
argument-checking CI gate — **declared in `.github/workflows/ci.yml`, so it runs
rather than merely exists** — with a committed planted-violation proof, and a
vocabulary that matches the surface.

<assumption_delta_decision>
**Signal:** the deterministic `assumption-delta` scan fired `detected: true` on
one `pluralization` signal for the term "second", in ROADMAP Phase 29's notes —
*"a cached index inside the store creates a **second** truth that can disagree
with the block table"*.

**Primary noun:** *truth* — specifically, how many authoritative sources exist
for a cross-reference or search answer.

**Decision: `no-change`.**

**Rationale:** assessed against this phase's scope rather than accepted on the
prose match. The sentence is an anti-caching PROHIBITION attached to STORE-06
(derive on every query, never cache on disk), not a transition from a
one-to-one to a one-to-many identity model. STORE-06 was verified SATISFIED with
its byte-identity control green, it is not in the gap-closure requirement set,
and nothing in these five plans touches `anno-derive.ts`'s caching posture. The
word "second" there counts truths in order to forbid the second one, which is
the opposite of a pluralization the data model must absorb. Recorded here rather
than dropped so the assessment is auditable.

*(Worth noting that this plan moves in the same direction the sentence argues
for: task 1 collapses two independent payload decodes into one, removing a
second truth about which bytes a coverage report describes.)*
</assumption_delta_decision>

<wr14_scope_decision>
**Decision, 2026-08-30: WR-14 is taken in part. Sites 2, 4 and 5 ship in this
plan; sites 1 and 3 — the two internal identifier renames — are dropped from
this gap round and stay recorded warnings.** Recorded as a decision with its
reason rather than removed silently, per this project's standing rule.

**What changed since the fold-in was decided.** The original plan asserted of the
exported entry function: *"The consumer set is one dynamic import in
`vice-proxy.ts`; the typecheck plus the package gate's import-closure walk prove
there is no other."* **That sentence was false, and it is replaced here by the
measured set rather than left standing.** Measured on this tree with
`grep -rn 'runAnnoCli' --include='*.ts' --include='*.mjs' .`:

| Site | Count | Note |
|---|---|---|
| `src/mcp/vice/anno-cli.ts` | 1 declaration + 2 header prose | the rename target |
| `src/mcp/vice/vice-proxy.ts:307` | 1 | the dynamic import — the only consumer the old sentence knew about |
| **`src/mcp/vice/anno-cli.test.ts`** | **1 import + ~26 call sites** (`:36`, `:206`, `:256` … `:1010`) | **the whole reason the sentence was wrong.** The rename does not typecheck without it. |
| `src/mcp/vice/module-classification.ts:674` and `:676` | 2 | a **guarded record**: `:674` cites `vice-proxy.ts:307` for the symbol, and DIRECTION 9 asserts that line contains it. Renaming at the call site while leaving the record reds `module-classification.test.ts` |
| `scripts/check-npm-packages.mjs:237` | 1 | a historical sentence — site 5, already handled as past-tense |

**Why that changes the answer.** A one-consumer rename inside a plan that already
carries CR-05 and a new CI gate is cheap. A ~26-call-site test rename plus a
guarded-record update, in a **wave-3** plan carrying gap 2b, is not — and it buys
nothing a requirement depends on. WR-14 is a warning; no gap, no ROADMAP criterion
and no requirement in this round rests on it. This project carries a recorded
failure mode of gap-closure rounds growing their own scope, and this is exactly
its shape: a warning volunteered as a courtesy, discovered to be four times its
estimated size, absorbed into the round's largest plan.

**Why sites 1 and 3 go together and not separately.** Renaming the test-hatch
environment variable while leaving the exported function on the old vocabulary
produces a *half*-renamed surface — the state WR-14 objects to in the first
place. They are one decision.

**What is gained, beyond the work not done.** Dropping both removes
`src/mcp/vice/vice-proxy.ts` and `src/mcp/vice/vice-proxy.test.ts` from this plan
entirely. That retires the line-drift exposure against `CLAUDE.md`'s MCP-02
pinned citations and `docs-linerefs.test.ts` (formerly `T-29-16-08`), and removes
the need to hand-search a `MANUAL_ONLY_TESTS` file the automated arm never runs.
It is a reduction in risk, not only in effort.

**What is kept, and why each earns its place.** Site 2 is the only *user-facing*
half of WR-14 — a user typing an unrecognised verb is answered by a subcommand
token that no longer dispatches — and it is a message-text edit in a file this
plan already opens. Sites 4 and 5 are broken cross-references (one citing an
identifier and value that no longer exist, one naming three deleted modules as
though present); both are prose in files this plan already opens, both cost
nothing, and neither touches a pinned line, a floor or a count.

**Where the remainder lives.** Sites 1 and 3 stay recorded warnings in
`29-VERIFICATION.md`'s anti-pattern table. Their correct home is a pass that owns
`anno-cli.test.ts` as its subject and can take the rename and the guarded-record
update together — not a gap-closure round whose subject is four reproduced
defects.
</wr14_scope_decision>
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-the-mcp-surface/29-VERIFICATION.md
@.planning/phases/29-the-mcp-surface/29-REVIEW.md
@.planning/phases/29-the-mcp-surface/29-07-SUMMARY.md
@.planning/phases/29-the-mcp-surface/29-09-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: One decode, two callers — the coverage verb accepts the image forms the surface already reads, and its only documented invocation runs</name>
  <files>src/mcp/vice/anno-coverage.ts, src/mcp/vice/anno-cli.ts, src/mcp/vice/anno-coverage.test.ts, src/mcp/vice/anno-cli.test.ts, src/mcp/vice/prg-image.ts, src/mcp/vice/module-classification.ts, src/skills/routine-queue-walker/SKILL.md</files>
  <read_first>
    - **`src/mcp/vice/module-classification.ts` — read this BEFORE you change an import block.** It is a mechanically-guarded record of symbols and line citations, and FIVE of them point into files this task edits: structured `{ path: "src/mcp/vice/anno-cli.ts", symbol: "renderMemoryMap", line: 63 }` and `{ ..., symbol: "buildCoverageReport", line: 68 }`; the prose citation `` `checkAcceptedOptions` (`anno-cli.ts:169`) ``; structured `{ path: "src/mcp/vice/anno-coverage.ts", symbol: "CONFIDENCE_GRADES", line: 144 }`; and the prose citation `anno-coverage.ts:1392` for `AUTO_NAME_PREFIX_RE`, which appears TWICE. Locate them with `grep -n 'anno-cli.ts\|anno-coverage.ts' src/mcp/vice/module-classification.ts`. **Plan 29-14 already re-measured the three `anno-cli.ts` citations once; this task moves them again**, so read their CURRENT values off the file rather than the numbers written above.
    - `src/mcp/vice/module-classification.test.ts` — **DIRECTION 9** (lines 262-288: for every consumer carrying a `line`, that line must CONTAIN the cited symbol) and **DIRECTION 9b** (lines 289-336 plus its tests: every `path:NN` in `module-classification.ts`'s own prose must resolve to a real file and a real, non-blank line, with symbol containment where the prose names one on the same line). DIRECTION 9's own comment records that three citations had already drifted "because earlier work in the same phase inserted lines above them" — the same mechanism as this task.
    - `src/mcp/vice/anno-cli.test.ts` — the existing `coverage` verb cases, in particular the absent-store refusal and the absent-project-file refusal (around `:386` and `:400`), which drive the loader this task replaces. Both assert refusal messages; widening the accepted input set can change which branch they reach. Read them before editing the loader, and adjust only what the change genuinely invalidates.
    - `src/mcp/vice/anno-coverage.ts` lines 2095-2160 — `LoadedProject`, `loadProject()` in full (it reads the file as UTF-8 text, JSON-parses it, and pulls a base64 payload out of one field), and how `buildCoverageReport()` consumes it into `report.project.payloadDecoded` and `report.project.reason`. Note that the whole-file UTF-8 read is itself a blocker for binary input.
    - `src/mcp/vice/anno-cli.ts` lines 527-556 — `projectImage()`, the SECOND decode. Read its doc comment carefully: it already states the property this task makes structural — that it is deliberately not a second byte source, because handing the cross-reference derivation bytes from somewhere else would let the two halves of one report disagree about which program they describe. Today it is exactly the second byte source it says it must not be.
    - `src/mcp/vice/anno-tools.ts` lines 1741-1775 — `loadImage()`. **This is the source-of-truth analog for the dispatch ORDER**: extension first (`.raw`/`.bin` → flat), then a non-`.prg` file of exactly 65536 bytes → flat, then `.prg` → load-address-plus-payload. Copy that order; do not re-derive it.
    - `src/mcp/vice/prg-image.ts` lines 1-100 — the whole header, and `parsePrg()` / `flatImageOrigin()` / `decodeRawData()`. Three constraints in that header bind this task: never give a function there a filesystem PATH parameter; never move the dispatch ORDER discipline into it; and never relax or reword either input refusal, because the message texts are a user-visible contract with a test asserting one of them. The reachability rationale at lines 30-36 is WR-07 and is corrected by this task.
    - `src/skills/routine-queue-walker/SKILL.md` lines 234-256 — the Phase 5 section: the invocation, the two-paths paragraph, and the `--out`/`--force`/`--sample` paragraph. The invocation line already names a `.prg`; it becomes correct when this task lands, so the skill edit here is a dated note, not a command change.
    - `src/mcp/vice/anno-coverage.test.ts` — the existing report-shape cases and fixture construction, so the new cases reuse them.
  </read_first>
  <behavior>
    - Test A (currently RED): `buildCoverageReport()` over a real `.prg` fixture reports `payloadDecoded: true`, the load address from the file's first two bytes as `origin`, and a non-zero censused byte count. Today it reports `payloadDecoded: false` with a not-valid-JSON reason.
    - Test B (currently RED): the same over an exactly-65536-byte flat capture with a `.raw` extension reports `payloadDecoded: true` with `origin` 0.
    - Test C (the WR-07 incident, must be REFUSED not silently parsed): a SHORT flat capture with a `.raw` extension — well under 65536 bytes — is refused by name with the flat-capture message, NOT fallen through to the load-address parser. This is the concrete incident `prg-image.ts`'s header records: a 4096-byte `.raw` whose first two bytes became an origin read backwards out of its own payload. Extension dispatch runs before any length check precisely so this refusal stays reachable.
    - Test D (the legacy form still works): the retired JSON-with-payload form still loads, so a user with an existing project file is not broken by this change.
    - Test E (agreement, the property that was never asserted): for the same `.prg` fixture, the origin and body bytes the coverage loader produces are identical to those `anno-tools.ts`'s `loadImage()` produces. The two views of "what is an image" must agree, and only a test can say so.
    - Test F (one decode, two callers): the CLI's cross-reference byte source and the census's payload are the SAME loaded values — assert that the CLI-side helper delegates rather than re-parsing, by checking that a file the loader refuses yields no cross-references AND `payloadDecoded:false` in the same run.
    - Test G (read-only, re-runnable): running the verb twice against the same store and image produces the same report figures and leaves the store file byte-identical.
    - Test H (the disclosure symmetry with 29-14): a file whose opening bytes are a distinctive planted token and which is not valid JSON produces `payloadDecoded:false` whose reason does NOT contain that token, while still naming the path and still saying the file is not valid JSON. This is the mirror of 29-14's sidecar case; asserting the token's ABSENCE and the diagnosis's PRESENCE in one case makes the property under test the redaction rather than the refusal.
  </behavior>
  <action>
    <!-- planner-discipline-allow: runAnnoCli -->

    In `src/mcp/vice/anno-coverage.ts`:

    1. Replace `loadProject()`'s body with a dispatch that reads the file as
       BYTES once, then decides by the same order `anno-tools.ts`'s `loadImage()`
       uses: a `.raw` or `.bin` extension goes to the flat-capture origin
       function; any non-`.prg` file of exactly 65536 bytes goes to the same
       place; a `.prg` goes to the load-address parser. Only if none of those
       matched does it fall through to the retired form — decode the bytes as
       UTF-8, JSON-parse, and read the base64 payload field, exactly as today.

       Keep the legacy branch's reason strings as they are, with **one
       deliberate exception (the `T-29-14-01` symmetry, `T-29-16-06`)**: the
       JSON-SYNTAX failure — the one that today appends the caught parse error's
       own text after the path — must stop interpolating that error. This is the
       same treatment plan 29-14 task 2 applies to the sidecar syntax failure on
       the sibling verb, and for the same reason: the caught error's text carries
       a snippet of the file's opening bytes, so a caller-supplied path becomes a
       content-disclosure oracle. Replace it with a reason that names the path,
       states that it is not valid JSON, and — where the underlying error exposes
       one — carries the byte OFFSET at which parsing failed, which is a position
       and not content. Add a comment recording why, naming CR-03 as the finding
       that established the shape and 29-14 as the sibling fix, so the next
       reader does not restore the interpolation as a diagnostic improvement.

       Two neighbouring interpolations in the same function are deliberately NOT
       changed, mirroring 29-14's own scoping. The unreadable-PATH throw reports
       an errno-class failure and carries no file content — 29-14 leaves the
       equivalent read-failure branch alone for exactly this reason. The
       payload-decode failure reports a library-class base64/inflate failure over
       a field the caller did not author; **read it before you decide**, and if
       its underlying error text can carry a fragment of the file's own bytes,
       give it the same treatment and say so in the summary; if it reports a
       library-class failure only, leave it and record which you found. Do not
       widen this beyond the one branch on a guess.

       Every OTHER legacy reason string stays byte-identical: a caller with an
       old project file must get the same diagnosis it gets now.

       Extension dispatch runs BEFORE any length check, and a comment must say
       why with the incident named: a short flat capture that fell through to
       the load-address parser silently bootstrapped with an origin read
       backwards out of its own payload bytes and exited zero. That ordering is
       a contract, not a style choice.

       The failure of an extension-dispatched branch is a `payloadDecoded:false`
       with the underlying refusal text as the reason — not a throw. The
       existing caller-contract throw stays for an unreadable PATH only.

    2. EXPORT the loader under the name `loadProjectImage`, keeping
       `LoadedProject` as its return type. Give it a doc comment stating that it
       is the ONE definition of how the coverage verb turns a path into bytes
       plus an origin, that it has exactly two callers (the census here and the
       CLI's cross-reference byte source), and that a third hand-rolled decode
       anywhere is the defect this export exists to remove.

    In `src/mcp/vice/anno-cli.ts`:

    3. Replace `projectImage()`'s body with a call to the imported
       `loadProjectImage`, mapping its result onto the `{origin, bytes}`-or-null
       shape the existing caller expects (null when the payload did not decode
       or is empty). Keep the function and its doc comment — but rewrite the
       comment's claim into a statement of fact: the two halves of one report now
       CANNOT describe different programs, because they call one function, rather
       than must not, because a reader is trusted to keep two in step.

    4. Drop the now-unused import from the byte-layout module if nothing else in
       this file uses it, and let the typecheck prove the closed consumer set.

    In `src/mcp/vice/prg-image.ts`:

    5. Correct the reachability rationale (WR-07). It currently says the CLI
       imports the load-address parser and the flat-capture origin function from
       there; the CLI imports neither after this task. Restate it against the
       importers that actually exist — `anno-tools.ts` statically, and
       `anno-coverage.ts` — and prefer naming the STATIC importer, which is a
       stronger reachability claim than the dynamic-import route the old sentence
       depended on. Change nothing else in this file: no function gains a path
       parameter, no refusal message is reworded, and no dispatch order moves in.

    In `src/skills/routine-queue-walker/SKILL.md`:

    6. The Phase 5 invocation line already names a `.prg` and becomes correct
       when this task lands, so do not change the command. Add a dated note,
       `2026-08-30`, beside it in this repo's established withdrawal-note shape,
       recording that the verb's positional is a program IMAGE — a `.prg`, or an
       exactly-65536-byte flat capture with a `.raw`/`.bin` extension — that the
       intermediate project-file format this verb previously required has no
       producer left in this repo, and that dispatch is by extension so a short
       flat capture is refused rather than misread. Also correct the
       `--out`/`--force`/`--sample` paragraph if it names the positional by the
       retired vocabulary.

    In `src/mcp/vice/anno-coverage.test.ts`, add the eight cases from
    `<behavior>` under a heading naming CR-05, REPOINT-01, WR-07 and the
    `T-29-14-01` symmetry.

    In `src/mcp/vice/anno-cli.test.ts`:

    7. The existing `coverage` cases around `:386` and `:400` drive the loader
       this task replaces. Re-run them FIRST and see what the change does to
       them. The absent-store case must still refuse by name and must still
       leave no store behind — that behaviour is upstream of the loader and must
       not move. The absent-project-file case must still refuse before the store
       is opened. If widening the accepted input set changes which message
       either case reaches, update the assertion to the message the code now
       produces AND record in the summary which assertion changed and why. Do
       not delete a case: a deleted assertion is indistinguishable from one that
       was never written. Add no new coverage cases here — the eight new cases
       belong in `anno-coverage.test.ts` beside the loader.

    In `src/mcp/vice/module-classification.ts`:

    8. **Re-measure every citation this task's edits could have moved, after the
       code edits are final.** Three definitely move and two are asserted not to:

       - `anno-cli.ts` `:63` (`renderMemoryMap`), `:68` (`buildCoverageReport`)
         and prose `:169` (`checkAcceptedOptions`). Step 3 adds a
         `loadProjectImage` import and step 4 removes the byte-layout-module
         import; the removal is BELOW the first two and ABOVE the third, so the
         prose citation moves even if the structured pair does not.
       - `anno-coverage.ts` `:144` (`CONFIDENCE_GRADES`) and prose `:1392`
         (`AUTO_NAME_PREFIX_RE`, cited twice). Both sit far above the loader you
         are replacing, so both should be UNMOVED — **prefer keeping them that
         way**: `anno-coverage.ts` already imports the byte-layout module on the
         line directly above `:144`, so take the additional symbols you need by
         extending that EXISTING import in place rather than adding a line above
         it. That is line-neutral by construction here, unlike in `anno-cli.ts`
         where the header genuinely grows.

       Measure, do not compute: `grep -n` for each symbol against the finished
       files and write the observed numbers in. Then run `cd src/mcp/vice &&
       node --test module-classification.test.ts`; DIRECTION 9 and 9b both name
       the offending citation and quote the line they landed on.

       Correct the citation, never the code, and never the guard. Do not delete
       a citation to silence a failure — both directions' non-vacuity assertions
       are relations rather than counts, so a deletion passes silently and costs
       the record exactly the coverage it exists for. Change nothing else in this
       file: no verdict, no rationale, no fate, no requirement anchor. In
       particular, leave the `runAnnoCli` consumer entries at `:674` and `:676`
       ALONE — the rename that would have required touching them is dropped from
       this plan on the decision recorded in `<wr14_scope_decision>`.
  </action>
  <verify>
    <automated>cd src/mcp/vice && node --test anno-coverage.test.ts anno-cli.test.ts prg-image.test.ts anno-tools.test.ts module-classification.test.ts 2>&1 | tail -20 && npm run typecheck</automated>
    Expected: all five test files green including the eight new cases, typecheck clean. `anno-tools.test.ts` is in the list because test E asserts agreement with `loadImage()`; if that verb's behaviour moved, this is where it shows. `module-classification.test.ts` is in the list because step 8 re-measures five citations into two files this task edits; it is **absent from `29-BASELINE.md`'s failing set**, so reddening it is a regression by the baseline's own rule.
  </verify>
  <acceptance_criteria>
    - `node src/mcp/vice/vice-proxy.ts anno coverage <a real .prg> --store <a real .annostore>` exits **0** and prints a report whose payload line reads decoded, where it previously printed a report of zeros with an UNAVAILABLE payload and exited 1. Record the full transcript in the summary.
    - The same command against a 4096-byte `.raw` file exits non-zero with the flat-capture refusal message, and NOT with an origin read out of the payload's first two bytes.
    - The same command against a legacy JSON project file still exits 0.
    - `cd src/mcp/vice && grep -c 'loadProjectImage' anno-cli.ts anno-coverage.ts` reports a non-zero count in both files.
    - A not-valid-JSON file whose first bytes are a distinctive planted token produces a report whose reason text does not contain that token, and which still names the path and still says the file is not valid JSON. Record the exact before and after reason strings — this is the `T-29-14-01` symmetry, and the before string is the one 29-14 removed from the render path in the same round.
    - Record which of that function's other two error interpolations were inspected and left as they are, with the one-line reason for each, so the scoping is legible rather than assumed.
    - `cd src/mcp/vice && npm run typecheck` exits 0 — proving the dropped import has no remaining consumer.
    - `src/skills/routine-queue-walker/SKILL.md` carries a 2026-08-30 note naming the accepted image forms.
    - `node scripts/check-npm-packages.mjs` exits 0 — the import-closure walk must still resolve every reachable module against the published file list after the import changes.
    - `cd src/mcp/vice && node --test module-classification.test.ts` exits 0. Record the before and after line numbers of the three `anno-cli.ts` citations, and confirm the two `anno-coverage.ts` citations are UNMOVED by showing that the new byte-layout symbols were taken by extending the existing import line rather than adding one above it.
    - `git diff src/mcp/vice/module-classification.ts` shows changed line numbers and nothing else — no verdict, rationale, fate or requirement anchor moved, and the `:674` / `:676` consumer entries are untouched.
    - `cd src/mcp/vice && node --test anno-cli.test.ts` exits 0 with no case deleted; any assertion changed by the widened input set is named in the summary with its before and after text.
  </acceptance_criteria>
  <reversibility rating="costly">Widening the coverage verb's accepted input set is a published-surface promise: once the playbooks and any consuming project depend on passing a `.prg` directly, withdrawing it breaks a documented instruction rather than an internal call. Flagged rather than gated — it is the fix the verification asked for, the alternative it offered (a withdrawal note leaving the skill with no measurement instruction at all) is worse for REPOINT-01, and the legacy form is retained so nothing existing breaks.</reversibility>
  <done>The only measurement instruction `routine-queue-walker` has runs to exit 0; the census and the cross-reference derivation share one decode; the short-flat-capture refusal is reachable and pinned; and `prg-image.ts`'s stated reason for being shipped is true again.</done>
</task>

<task type="auto">
  <name>Task 2: The invocation gate — argument-check every documented `anno` command, because a name-only floor cannot see a dead command</name>
  <files>scripts/lib/anno-cli-invocations.mjs, scripts/check-skill-cli-invocations.mjs, src/mcp/vice/anno-cli-invocations.test.ts, .github/workflows/ci.yml</files>
  <read_first>
    - `scripts/lib/anno-cli-verbs.mjs` in full (177 lines) — **this is the source-of-truth analog.** Read it for: why the logic lives in `scripts/lib/` and not in the CI script (the CI script executes its whole check at import time, so a test importing it would re-run the live gate instead of calling predicates in isolation); why it must stay out of the MCP package's published file list; `stripComments()` as a single-pass character scanner rather than a regex, with the recorded reason; `switchVerbBody()`'s brace-depth scoping and its rule that `null` means "nothing to parse", never "zero verbs"; and `ANNO_CLI_VERB_FLOOR`'s long comment distinguishing a REPLACEMENT over a new subject from a lowering.
    - `scripts/check-skill-tool-coverage.mjs` lines 480-540 — the existing CLI-verb section: how it collects the skill file set, how `need()` accumulates errors, the non-vacuity floor it asserts, and the final report line's shape. The new gate mirrors this structure and reports in the same voice.
    - `scripts/check-skill-tool-coverage.mjs` lines 1-50 — its standing rule that skill content is untrusted input that is MATCHED, never executed: it never imports, requires, evals or spawns anything from the skill tree. The new gate inherits that rule without exception.
    - `src/mcp/vice/anno-cli.ts` — `VERB_OPTIONS` (exported and frozen) and both command functions' argument parsers, so the gate's flag check reads the CLI's own declaration.
    - `src/mcp/vice/anno-tools.ts` lines 1741-1775 and `src/mcp/vice/anno-coverage.ts` (`loadProjectImage`, after task 1) — the accepted image extensions, so the positional check names the kinds the code actually reads.
    - `src/mcp/vice/anno-verb-coverage.test.ts` — the committed non-vacuity and planted-violation proof for the VERB predicates. The new test file is its counterpart for the INVOCATION predicates and follows its shape.
    - **`.github/workflows/ci.yml` lines 165-206 — this is where the FIVE existing `node scripts/check-*` gate steps are DECLARED, and it is the only place any of them is declared.** Measured on this tree: `check-npm-packages.mjs` at `:170`, `check-no-analyser.mjs` at `:180`, `check-skill-tool-coverage.mjs` at `:187`, `check-skill-fork-honesty.mjs` at `:195`, `check-skill-description-overlap.mjs` at `:205` — plus `bash scripts/package.sh` at `:208`, which the new step must precede. Read the comment above each: every one states, in one or two sentences, the user-visible failure it exists against. The new step carries the same shape. (These line numbers are a reading aid measured at planning time, not a pin — `ci.yml` is not a line-guarded file. Locate the block with `grep -n 'run: node scripts/check-' .github/workflows/ci.yml`.)
  </read_first>
  <action>
    Create `scripts/lib/anno-cli-invocations.mjs` holding the predicates, and
    `scripts/check-skill-cli-invocations.mjs` as the thin CI entry point that
    runs them and reports. One definition, two callers — the same split
    `anno-cli-verbs.mjs` already uses and for the same recorded reason.

    **`scripts/lib/anno-cli-invocations.mjs`** exports three things:

    1. `parseDocumentedInvocations(text)` — extracts every documented `anno`
       CLI invocation from one skill file's text and returns a structured record
       per invocation: the verb, the positional arguments in order, and the
       flags with their values. Scope the extraction to FENCED CODE BLOCKS only,
       so a mention of a verb in prose is not mistaken for an invocation; the two
       documented routes (the `npx` one and the in-repo `node` one) both end in
       the same subcommand token followed by a verb, so match from that token
       rightward and ignore whatever launcher precedes it. Return `null` — never
       an empty array — if the text contains no fenced blocks at all, so a
       caller must treat "nothing to parse" as distinct from "zero invocations",
       the same discipline `switchVerbBody()` records.

    2. `checkInvocation(invocation, verbOptions, positionalKinds)` — returns an
       array of human-readable problems, empty when the invocation is sound. It
       checks two things and only two: every flag is in that verb's real
       accepted option set, and every positional's file extension is one of the
       kinds declared for that verb's slot. Both inputs are passed IN rather
       than imported here, so this module stays a pure predicate with no
       first-party TypeScript import and no filesystem access.

    3. `ANNO_INVOCATION_FLOOR` — a hand-pinned integer, the measured count of
       documented invocations across the skill tree at authoring time, with the
       comment discipline `ANNO_CLI_VERB_FLOOR` establishes: state what was
       measured and how, state that it is a floor that rises when invocations
       are added, and forbid deriving it from disk, because a floor computed
       from the thing it guards can never fail.

    Give the module the header this repo's `scripts/lib/` modules carry: why it
    exists (the phase's own coverage gate resolves NAMES and never an
    invocation's ARGUMENTS, which is the structural reason two documented
    commands shipped dead while the gate was green — CR-04 and CR-05), what it
    is the one authoritative place for, and what not to do (never execute skill
    content; never hand-type the option set that `VERB_OPTIONS` already
    declares; never lower the floor to make a run pass).

    **`scripts/check-skill-cli-invocations.mjs`** is the CI entry point:

    4. Collect the skill file set the same way `check-skill-tool-coverage.mjs`
       does, over BOTH trees — the canonical `src/skills/` and the generated
       `installer/skills/` — and run the installer sync first so the shipped
       text scanned is current rather than stale. Import `VERB_OPTIONS` from
       `anno-cli.ts` (Node's native type-stripping resolves the TypeScript
       module with no build step, exactly as the sibling gate already imports
       `capability-registry.ts`). Declare the per-verb positional kinds in ONE
       frozen map in this file: the render verb's single positional is an
       annotation store, and the coverage verb's single positional is a program
       image whose accepted extensions are the ones task 1 made the loader
       dispatch. Give that map a comment pointing at the two functions that
       define the truth it mirrors, so a future format addition has a named
       place to land.

    5. Assert the non-vacuity floor BEFORE reporting any per-invocation result,
       so a broken extractor fails loudly rather than reporting a clean run over
       zero invocations. Then report every problem `checkInvocation` returns,
       naming the file, the verb and the offending argument, and exit non-zero
       if there are any. Finish with an OK line in the sibling gate's voice,
       reporting how many invocations were extracted from how many files across
       both trees and how many verbs they covered.

    6. **Declare it as a CI step in `.github/workflows/ci.yml`**, in the block
       where the five existing `node scripts/check-*` gate steps are declared
       and nowhere else — that file IS the wiring, and a gate that lives only on
       disk never runs. Place the new step immediately after the
       `check-skill-description-overlap.mjs` step and before
       `bash scripts/package.sh`, as `run: node scripts/check-skill-cli-invocations.mjs`,
       with a `name:` naming the requirement it serves and a comment above it in
       the voice the other five use: one or two sentences stating the
       user-visible failure it exists against — a documented command whose
       ARGUMENTS are wrong runs, fails for the user, and passes every existing
       gate, because the sibling gate resolves NAMES and never arguments. That
       is the structural reason CR-04 and CR-05 both shipped green.

       Do not add an `npm ci` step and do not give it a `working-directory`:
       like the five beside it, it runs from the repository root against a bare
       checkout. Change no other step, no trigger, no matrix and no permissions
       block in that file.

    **`src/mcp/vice/anno-cli-invocations.test.ts`** is the committed proof:

    7. Non-vacuity: `parseDocumentedInvocations` over the real
       `routine-queue-walker` playbook returns at least one invocation naming
       the coverage verb, and over the real `c64-program-recon` playbook returns
       at least one naming the render verb. A parser that silently returns
       nothing must fail here.
    8. Planted violations, four of them, each a synthetic text rather than an
       edit to a real skill file: an invocation with a flag the verb does not
       accept; an invocation whose positional carries an extension outside the
       declared kinds — this is CR-04's exact shape and it must be reported by
       name; an invocation of a verb the CLI does not have; and an invocation
       inside PROSE rather than a fenced block, which must NOT be reported,
       because a gate that fires on prose is one nobody can keep green.
    9. The `null`-versus-empty discipline: a text with no fenced blocks returns
       `null`, and a text with a fenced block containing no invocation returns
       an empty array. Assert both, because conflating them is how a broken
       extractor reads as a clean tree.
    10. The adjacency control: an invocation whose positional extension exactly
        matches an accepted kind passes, and the SAME invocation differing only
        in that extension fails. Asserting both in one test makes the property
        under test the discrimination rather than the refusal.
  </action>
  <verify>
    <automated>node scripts/check-skill-cli-invocations.mjs && test "$(grep -c 'run: node scripts/check-' .github/workflows/ci.yml)" -eq 6 && grep -q 'check-skill-cli-invocations' .github/workflows/ci.yml && cd src/mcp/vice && node --test anno-cli-invocations.test.ts 2>&1 | tail -15</automated>
    Expected: the gate exits 0 over the corrected tree and reports a non-zero invocation count; the CI file declares six `node scripts/check-*` steps where it declared five, one of them the new gate; the test file green. **The two `ci.yml` checks are in this block deliberately** — step 6's wiring is otherwise provable only by reading, and a gate that exists on disk and never runs satisfies every other check in this task while delivering none of this plan's CI truth. That is B-1, and it is the same defect class as the round-1 blocker in a different file.
  </verify>
  <acceptance_criteria>
    - `node scripts/check-skill-cli-invocations.mjs` exits 0 and its report line names a non-zero extracted-invocation count across both trees.
    - **The gate is wired, not merely written:** `grep -c 'check-skill-cli-invocations' .github/workflows/ci.yml` prints a count of at least 1, and `grep -c 'run: node scripts/check-' .github/workflows/ci.yml` prints **6** where it printed 5 before. Record both numbers. Without this the plan's own `must_haves` truth about a CI gate is undelivered while every other criterion still passes — the failure mode this criterion exists against.
    - `grep -n 'run: node scripts/check-\|run: bash scripts/package.sh' .github/workflows/ci.yml` shows the new step declared AFTER `check-skill-description-overlap.mjs` and BEFORE `package.sh`. Quote the output in the summary.
    - `python3 -c "import sys,yaml;yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0 — the workflow still parses as YAML after the insertion. If PyYAML is unavailable, use any YAML parser on hand and say which; do not skip the check, because a mis-indented step silently disables a job rather than failing loudly.
    - Reverting plan 29-15's positional correction in `src/skills/c64-program-recon/SKILL.md` makes this gate exit non-zero, naming the file and the offending positional. Demonstrate once by hand, revert, and record the observed failure text in the summary — this is the proof the gate would have caught CR-04.
    - Reverting task 1's loader change makes the coverage invocation's positional kind declaration and the loader disagree; record which check fires and what it says.
    - Adding a flag to a documented invocation that is not in that verb's `VERB_OPTIONS` makes the gate exit non-zero. Demonstrate, revert, record.
    - `cd src/mcp/vice && node --test anno-cli-invocations.test.ts` exits 0 with at least ten cases.
    - `node scripts/check-npm-packages.mjs` exits 0 — the two new `scripts/` files must stay OUT of the MCP package's published file list, exactly as `scripts/lib/anno-cli-verbs.mjs` does, while remaining tracked by git so the release archive includes them.
  </acceptance_criteria>
  <reversibility rating="reversible">Two new script files and one new test file; deleting them restores the prior state exactly.</reversibility>
  <done>Every documented `anno` CLI invocation in both trees is argument-checked by a gate with a non-vacuity floor and four planted-violation controls, so the next dead command fails CI rather than a review.</done>
</task>

<task type="auto">
  <name>Task 3: WR-14 in part — the user-facing vocabulary matches the surface and the two stale cross-references point at things that exist</name>
  <files>src/mcp/vice/anno-cli.ts, scripts/lib/anno-cli-verbs.mjs, scripts/check-npm-packages.mjs, .planning/phases/29-the-mcp-surface/29-VERIFICATION.md</files>
  <read_first>
    - **`<wr14_scope_decision>` in this plan's `<objective>` — read it FIRST.** It records, with its measurement, why sites 1 and 3 (the exported entry function and the test-hatch environment variable) are NOT taken in this round. Do not reinstate them here on the grounds that they look small; they were measured and they are not.
    - `.planning/phases/29-the-mcp-surface/29-REVIEW.md` § WR-14 — the finding lists all five sites and prescribes a specific treatment for the last one that differs from the other four. Read it before editing; a half-renamed historical citation is unusable in both directions, which is why that site is marked past-tense rather than renamed.
    - `src/mcp/vice/anno-cli.ts` lines 44-50, 160-170, and the exported entry function with the two unknown-verb messages near the end of the file. You are editing the two MESSAGE strings only — the function keeps its current name.
    - `scripts/lib/anno-cli-verbs.mjs` lines 55-70 — the parenthetical citing a precedent floor by an identifier and a value that have both changed. `check-skill-tool-coverage.mjs` around line 478 carries the real current identifier and value; read it so the corrected citation points at something a reader can find.
    - `scripts/check-npm-packages.mjs` lines 228-245 — the historical sentence mixing renamed and deleted module names.
    - `scripts/check-no-analyser.mjs` — confirm what the removal gate's subject actually is before editing any of these strings, so you know which of them it polices and which it does not.
    - `.planning/phases/29-the-mcp-surface/29-VERIFICATION.md` — find the WR-14 row in the anti-pattern table with `grep -n 'WR-14' .planning/phases/29-the-mcp-surface/29-VERIFICATION.md`, and read the table's column shape before writing into it. You are annotating one existing row in place, not adding a row and not restructuring the table.
  </read_first>
  <action>
    **Three sites, not five.** Sites 1 and 3 — the exported entry function and
    the test-hatch environment variable — are dropped from this round on the
    decision recorded in `<wr14_scope_decision>`, which states the measured
    consumer set that changed the answer. Rename nothing; edit message text and
    citation prose only, and change no floor value and no count.

    2. `src/mcp/vice/anno-cli.ts` — the unknown-verb message and the last-resort
       catch both prefix their output with the retired subcommand token. A user
       typing an unrecognised verb is answered by a subcommand that no longer
       dispatches. This is the only *user-facing* site in WR-14 and it is why the
       finding is worth taking at all. Change both prefixes to the current
       subcommand token, leaving the rest of each message byte-identical. Touch
       only the string literals: the enclosing function keeps its current name,
       so no consumer, no test and no record entry moves.

    4. `scripts/lib/anno-cli-verbs.mjs` — the parenthetical cites a precedent
       floor by an identifier and a value that have both since changed, so a
       reader following the cross-reference finds nothing. Repoint it at the
       real current identifier and value in `check-skill-tool-coverage.mjs`,
       keeping the paragraph's argument intact: that guard is over a DIFFERENT
       subject (tool names, not CLI verbs) and the two numbers never move
       together. Do not change `ANNO_CLI_VERB_FLOOR` itself.

    5. `scripts/check-npm-packages.mjs` — the historical sentence names five
       modules in one breath, of which three no longer exist. Do NOT half-rename
       it: mark it explicitly past-tense, stating that it records the state at
       the time the finding was closed and naming the modules by their
       PRE-deletion names, so the citation is followable in both directions.
       Change no assertion, no floor and no count in this file. Leave the entry
       function's name inside that sentence exactly as it stands: it is a
       historical citation, the function still carries that name on this tree,
       and the sentence is followable in both directions as written.

    Finally, record the reduced scope **in `29-VERIFICATION.md`'s WR-14 row
    itself** — that file is in this plan's `files_modified` precisely so the row
    can be annotated in place, and it is the artifact a later reader opens to
    find out what is still open. The summary is a second copy, not the record.
    Annotate the existing row (do not add a row, do not restructure the table)
    with one sentence stating that WR-14 sites 2, 4 and 5 shipped in plan 29-16,
    that sites 1 and 3 — the exported entry function and the test-hatch
    environment variable — remain open, and the measured reason: a ~26-call-site
    test rename plus a guarded-record update in `module-classification.ts`, which
    `<wr14_scope_decision>` records in full. Point the sentence at
    `<wr14_scope_decision>` by plan id so the reason is reachable from the row.

    Commit that edit with this task's own commit. It is a `.planning/` file that
    is NOT `STATE.md` or `ROADMAP.md`, so the worktree carve-out does not apply
    to it and the plan-metadata commit does not pick it up either — an
    uncommitted edit here would be discarded with the worktree, which is the
    failure mode this instruction exists against. A warning that is partly
    discharged and silently left at "warning" is indistinguishable from one
    nobody looked at.

    Do **not** route this through `state.add-decision`: that verb has a recorded
    corruption failure mode on this project's `STATE.md`, and the decision is
    already recorded in `<wr14_scope_decision>` above.
  </action>
  <verify>
    <automated>cd src/mcp/vice && npm run typecheck && node --test docs-linerefs.test.ts anno-verb-coverage.test.ts module-classification.test.ts 2>&1 | tail -10 && cd ../../.. && node scripts/check-npm-packages.mjs && node scripts/check-skill-tool-coverage.mjs && node scripts/check-no-analyser.mjs</automated>
    Expected: typecheck clean, `docs-linerefs.test.ts` green, `module-classification.test.ts` green, and the three gates unchanged in their reported counts. `docs-linerefs.test.ts` and `module-classification.test.ts` should be green **trivially** for this task — it edits no line-pinned file — and that is the point: after dropping sites 1 and 3 this task no longer touches `vice-proxy.ts` at all, so the line-drift exposure it used to carry is gone rather than managed.
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && npm run typecheck` exits 0.
    - **The anti-creep guard, pinned against this PLAN'S BASE rather than the working tree.** `src/mcp/vice/vice-proxy.ts` and `src/mcp/vice/vice-proxy.test.ts` must be untouched by any commit this plan makes; a bare `git diff --stat` compares the working tree to the index and is therefore empty unconditionally once a task commits, which is every time this criterion would actually be read. Measure it with the plan-base spelling `execute-plan` itself uses:

      ```bash
      PLAN_BASE=$(git log --oneline --grep='(29-16):' --reverse | head -1 | cut -d' ' -f1)
      test -n "$PLAN_BASE" || { echo 'no 29-16 commit yet -- criterion not dischargeable, re-run after the commit'; exit 1; }
      git diff --stat "${PLAN_BASE}^..HEAD" -- src/mcp/vice/vice-proxy.ts src/mcp/vice/vice-proxy.test.ts
      git status --porcelain -- src/mcp/vice/vice-proxy.ts src/mcp/vice/vice-proxy.test.ts
      ```

      Both commands must print NOTHING. The first covers everything this plan has committed (tasks 1-3, since `${PLAN_BASE}^` is the parent of its first commit); the second covers staged and unstaged work not yet committed, which the first cannot see. Run this AFTER task 3's commit, and record both outputs. An empty working-tree diff taken after a commit does not discharge this criterion. A non-empty result from either means WR-14 sites 1 or 3 crept back in against the recorded decision.
    - `cd src/mcp/vice && node --test docs-linerefs.test.ts module-classification.test.ts` exits 0.
    - `node scripts/check-no-analyser.mjs` exits 0 with the temporary allow-list still reporting **0 entries across 0 entries**, and its permanent-exemption count unchanged from the pre-task value. Record both numbers before and after.
    - `node scripts/check-skill-tool-coverage.mjs` exits 0 and its reported CLI-verb count is unchanged at 2.
    - `node scripts/check-npm-packages.mjs` exits 0 with unchanged file counts for both tarballs.
    - Invoking the CLI with an unrecognised verb prints a message prefixed with the current subcommand token. Record the exact before and after strings.
    - `grep -n 'WR-14' .planning/phases/29-the-mcp-surface/29-VERIFICATION.md` shows the annotated row naming sites 2, 4 and 5 as shipped in plan 29-16 and sites 1 and 3 as open with the measured reason, and `git log --oneline -1 -- .planning/phases/29-the-mcp-surface/29-VERIFICATION.md` shows a commit from this plan — the annotation must be COMMITTED, not left in the working tree. Quote the row's before and after text.
    - `git diff <that commit>^..<that commit> -- .planning/phases/29-the-mcp-surface/29-VERIFICATION.md` shows ONE row annotated and nothing else: no row added, no row removed, no verdict, score or gap disposition changed. This plan annotates the gap contract; it does not re-score it.
    - The summary states which WR-14 sites shipped (2, 4, 5) and which remain open (1, 3) with the reason, so the warning's partial discharge is legible rather than silent — as a second copy of the durable record above, not as a substitute for it.
  </acceptance_criteria>
  <reversibility rating="reversible">Message-text and citation-prose edits across three files, each mechanically revertable; no identifier, behaviour, floor, count or pinned line number changes.</reversibility>
  <done>No USER-FACING string on the `anno` CLI path still names the retired subcommand vocabulary, the two stale cross-references point at things that exist, every floor and count is untouched, and the two internal identifier renames are recorded as deliberately deferred rather than forgotten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-composed Bash invocation → `anno coverage` positional | An LLM-composed path crosses into a byte decode that infers a load address from file content |
| skill file text → the invocation gate | Skill content is untrusted input that is MATCHED, never executed — the standing rule the sibling gate declares |
| documented playbook step → an agent's execution | A documented command is executed literally; a wrong one costs a failed invocation and a wrong diagnosis |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-16-01 | Spoofing | the coverage report's origin | **high** | mitigate | The recorded incident: a short flat capture fell through to the load-address parser, whose first two bytes became the origin, so a truncated capture reported a complete-looking measurement with every downstream address wrong and exit 0. Mitigated by dispatching on EXTENSION before any length check — the order copied from `loadImage()` rather than re-derived — which keeps the flat-capture refusal reachable, and pinned by test C. |
| T-29-16-02 | Tampering | two independent payload decodes | **high** | mitigate | Two decodes over one path let the census half and the cross-reference half of one report describe different programs. Collapsed into one exported function with two callers, and pinned by test F, so the property `projectImage()`'s comment already claimed becomes structural. |
| T-29-16-03 | Elevation of Privilege | the invocation gate reading skill text | **high** | mitigate | Skill content is untrusted. The gate never imports, requires, evals or spawns anything from either skill tree — it reads text and regex-matches it, inheriting the sibling gate's standing rule verbatim. The predicates live in a pure module with no filesystem access and no first-party TypeScript import, so the executable surface exposed to skill text is a regex. |
| T-29-16-04 | Denial of Service | `parseDocumentedInvocations` regex over skill text | medium | mitigate | The extraction is scoped to fenced blocks and matches from a fixed subcommand token rightward, with no nested quantifier over unbounded input. Skill files are first-party and bounded; a catastrophic-backtracking shape is avoided by construction rather than by a timeout. |
| T-29-16-05 | Tampering | a gate that passes over zero invocations | **high** | mitigate | A broken extractor reporting a clean run is the exact failure mode this gate exists against. Mitigated by a hand-pinned non-vacuity floor asserted BEFORE any per-invocation result, by the `null`-versus-empty discipline, and by four planted-violation controls including one that must NOT fire. |
| T-29-16-06 | Information Disclosure | the coverage verb's file read | medium | mitigate | **The `T-29-14-01` symmetry, taken rather than deferred.** `anno-coverage.ts`'s JSON-syntax branch interpolates the caught parse error's text, which carries a snippet of the file's opening bytes — byte-for-byte the shape 29-14 classifies as **high** on the sibling verb and removes from the render path in this same round. The positional IS confined by `storePathWithinWorkspace()` at `anno-cli.ts:770` before this loader sees it, so the oracle cannot leave the workspace and the residual severity is **medium** rather than **high**; an in-workspace content echo is still a content echo. Mitigated by task 1 step 1 applying 29-14's own treatment to that one branch — name the path and the byte offset, never the content — pinned by behaviour test H, which asserts a planted token's ABSENCE and the diagnosis's PRESENCE in one case. The unreadable-PATH throw and the payload-decode failure are left as errno- and library-class failures, mirroring 29-14's scoping, with the latter re-read before it is left. |
| T-29-16-07 | Repudiation | half-renamed historical citations | medium | mitigate | A citation renamed halfway is unusable in both directions. The historical sentence in the package gate is marked past-tense with pre-deletion names rather than renamed, on the review's own prescription. |
| T-29-16-08 | Tampering | line drift in `module-classification.ts`'s citations | medium | mitigate | Five citations point into `anno-cli.ts` and `anno-coverage.ts`, and `module-classification.test.ts` DIRECTION 9 / 9b assert each lands on a line containing its symbol. Task 1's import changes move three of them — **for the second time in this round**, after plan 29-14 already corrected them once. Mitigated by re-measuring all five in the same task (step 8), by taking `anno-coverage.ts`'s new symbols through the EXISTING import line so the two citations there stay put by construction, and by putting `module-classification.test.ts` in task 1's `<automated>` block, two acceptance criteria and `<verification>`. |
| T-29-16-09 | Tampering | line drift in `vice-proxy.ts` | low | accept | **Retired by scope reduction, not by mitigation.** `CLAUDE.md`'s MCP-02 bullet pins `vice-proxy.ts` line numbers that `docs-linerefs.test.ts` checks mechanically, and the original task 3 edited that file. Dropping WR-14 sites 1 and 3 (`<wr14_scope_decision>`) removes `vice-proxy.ts` and `vice-proxy.test.ts` from this plan entirely, so there is no edit left to drift them. Accepted rather than mitigated because the exposure no longer exists; the acceptance criterion is an empty **plan-base-pinned** diff (`${PLAN_BASE}^..HEAD`) plus an empty `git status --porcelain`, for both files, which fails loudly if the scope creeps back. A bare working-tree `git diff` was rejected for this criterion: it is empty unconditionally once a task commits, so it would have gone green whether or not the scope crept. |
| T-29-16-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install in this plan. Two new files land under `scripts/`, both first-party, and neither enters the MCP package's published file list. |

**ASVS L1 disposition:** every `high` threat above is `mitigate`, each with a named code-level mechanism and a paired anti-regression control. None is accepted or transferred.
</threat_model>

<verification>
1. `cd src/mcp/vice && node --test anno-coverage.test.ts anno-cli.test.ts prg-image.test.ts anno-tools.test.ts anno-cli-invocations.test.ts anno-verb-coverage.test.ts docs-linerefs.test.ts module-classification.test.ts` — all green. `module-classification.test.ts` carries DIRECTION 9 and 9b over the five citations task 1 step 8 re-measures; it is **absent from `29-BASELINE.md`'s failing set**, so a red is a regression attributable to this plan.
2. `cd src/mcp/vice && npm run typecheck` — clean.
3. `cd src/mcp/vice && npm run test:automated` — compare the failing-file SET against `29-BASELINE.md`, never a count against zero. The phase closed with an EMPTY failing set on the automated arm; a new file name in it is a regression attributable to this plan.
4. All **seven** single-command gates exit 0: `check-no-analyser.mjs`, `check-skill-tool-coverage.mjs`, **`check-skill-cli-invocations.mjs` (new)**, `check-npm-packages.mjs`, `check-skill-fork-honesty.mjs`, `check-skill-description-overlap.mjs`, `audit-gate.mjs`. **Six of those seven must be DECLARED as CI steps in `.github/workflows/ci.yml` once this plan lands — five are today** (`audit-gate.mjs` is a GSD-side gate and is never a CI step), the sixth being the new one, declared by task 2 step 6. `grep -c 'run: node scripts/check-' .github/workflows/ci.yml` prints **6** after this plan where it printed **5** before. Exiting 0 locally and never running in CI is the B-1 failure mode: the `must_haves` truth about a CI gate is satisfied only when the gate is declared in that file.
5. `node scripts/generate-tool-support-table.mjs` then `md5sum docs/tool-support.md` — unchanged at `bb47448…`. No `anno_*` tool description changes in this plan, so the table must be byte-identical.
6. The removal gate's temporary allow-list stays **empty**. Nothing in this plan may add an entry; task 3 touches strings the gate does not police, which is why it can be done without one. `.github/workflows/ci.yml` is a tracked file that gate walks and it already contains the subject needle at `:180` (the `check-no-analyser.mjs` step's own name), covered today with no temporary entry; the step task 2 adds introduces no new occurrence of the needle, so the allow-list stays at 0. Re-run the gate after the `ci.yml` edit and record the entry count rather than assuming it.
7. Create no new REVIEW-shaped artifact: `docs-review-disposition.test.ts` reds on undispositioned finding ids and cascades into `audit-gate.mjs`.
8. `npm test` (the full glob) is NOT run: ~660 s, and it blocks forever on `vice-proxy.test.ts` with no emulator present. **This plan no longer edits that file** — dropping WR-14 sites 1 and 3 removed it and `vice-proxy.ts` from the plan — so the "find the sites by search because the automated arm never runs this file" hazard the earlier draft carried is gone. Prove it with the plan-base-pinned form in task 3's acceptance criteria — `git diff --stat "${PLAN_BASE}^..HEAD" -- <both files>` and `git status --porcelain -- <both files>`, both empty — not a bare working-tree `git diff`, which is empty unconditionally after a commit and would pass whether or not either file moved.
9. Every grep against `src/mcp/vice/anno-memmap-render.ts` uses `grep -a` (literal NUL byte; a plain grep reports nothing and exits 1). This plan does not edit that file, but a tree-wide census in the summary would silently skip it.
10. **Wave 3 contains this plan alone**, so no `files_modified` overlap is possible within the wave. Wave 1 is 29-13 / 29-14 / 29-17; wave 2 is 29-15 alone. Wave 1's three sets are disjoint, re-measured: 29-13 owns `anno-tools.ts` + `anno-tools.test.ts`, 29-14 owns `anno-cli.ts`, `anno-memmap-render.ts`, `anno-cli-path-consumers.test.ts`, `anno-cli.test.ts` and `module-classification.ts`, 29-17 owns `.planning/REQUIREMENTS.md` alone. The serialisation exists because 29-14, 29-15 and this plan must each edit `src/mcp/vice/module-classification.ts`, and same-wave plans run in parallel worktrees off one base. `depends_on: [29-14, 29-15]` was already correct and is unchanged; adding `anno-cli.test.ts` here therefore creates no new conflict with 29-14, which owns it in wave 1.

    **The two files added to `files_modified` in the final revision re-wave nothing, confirmed rather than assumed.** `.github/workflows/ci.yml` appears in no other plan in this round (`grep -l 'ci\.yml' 29-1{3,4,5,7}-PLAN.md` is empty), and `.planning/phases/29-the-mcp-surface/29-VERIFICATION.md` likewise — 29-17's only `.planning/` file is `REQUIREMENTS.md`. Both are therefore 29-16-only, this plan is alone in its wave, and no wave assignment or `depends_on` entry changes. Neither file lives under `src/mcp/vice/`, so `ANNO_MODULE_FLOOR` and every shipped-module count are untouched.
11. **`check-npm-packages.mjs` moves no count.** Its assertions are relational — no `node_modules/`, no `*.test.*`, no fixtures in either tarball, and a skill count derived from `src/skills/` — not pinned integer file totals. This plan adds two `scripts/` files (excluded from the MCP package's `files[]`, as `scripts/lib/anno-cli-verbs.mjs` already is) and one `*.test.ts` (excluded by the test-file assertion), and `module-classification.ts` was already tracked and already unpublished. `.github/workflows/ci.yml` and `.planning/phases/29-the-mcp-surface/29-VERIFICATION.md` are both EDITS to already-tracked files that neither tarball ships — `.github/` is outside both packages' `files[]` and `.planning/` is excluded by prefix, the same exclusion `29-15-e2e.sh` relies on. Nothing the gate measures changes.
</verification>

<success_criteria>
- `anno coverage <a real .prg> --store <a real .annostore>` exits 0 with a decoded payload, where it previously printed zeros and exited 1.
- A short flat `.raw` capture is refused by name rather than parsed as a load address plus payload.
- The census and the cross-reference derivation call one loader, pinned by a test, and that loader agrees with `anno-tools.ts`'s `loadImage()` on a shared fixture.
- `scripts/check-skill-cli-invocations.mjs` exists, exits 0 over the corrected tree, was observed red on three separate planted violations then reverted, **and is declared as a CI step in `.github/workflows/ci.yml`** — `grep -c 'run: node scripts/check-' .github/workflows/ci.yml` prints 6 where it printed 5. A gate that exists and never runs delivers none of the truth it was written for.
- The coverage path's JSON-syntax failure no longer echoes the parse error's text, so the `T-29-14-01` content-disclosure shape gets one treatment across both sibling verbs in one round, pinned by behaviour test H.
- No USER-FACING string on the CLI path names the retired subcommand vocabulary and the two stale cross-references resolve, with every floor, count and pinned line number unchanged. The two internal identifier renames are recorded as deferred in `<wr14_scope_decision>`, not silently dropped.
- The plan-base-pinned diff and `git status --porcelain` for `src/mcp/vice/vice-proxy.ts` and `src/mcp/vice/vice-proxy.test.ts` are both empty, measured after the commit rather than against the working tree.
- `29-VERIFICATION.md`'s WR-14 row is annotated in place and committed, naming sites 2/4/5 shipped and 1/3 open with the measured reason.
- `module-classification.test.ts` is green, with the three `anno-cli.ts` citations re-measured and the two `anno-coverage.ts` citations shown unmoved.
- All seven single-command gates exit 0 and the automated-arm failing-file set is no worse than the baseline's.
</success_criteria>

<output>
Create `.planning/phases/29-the-mcp-surface/29-16-SUMMARY.md` when done.
Record: the full before/after transcript of the coverage invocation; the refusal
text for the short flat capture; the three planted violations observed red against
the new gate with their exact failure messages; the new gate's OK report line; the
before/after unknown-verb message; the before/after reason string for the coverage
path's JSON-syntax failure and which of that function's other two interpolations
were left and why; the two `ci.yml` grep counts proving the new gate is declared
(`check-skill-cli-invocations` present, `run: node scripts/check-` at 6) and the
ordered gate list showing where it landed; the WR-14 row's before and after text
with the commit that carries it; the removal gate's temporary allow-list count and
permanent-exemption count before and after task 3; the empty plan-base-pinned diff
and empty `git status --porcelain` for `vice-proxy.ts` and `vice-proxy.test.ts`; the before/after line numbers of the three
`module-classification.ts` citations into `anno-cli.ts` and the evidence that the
two `anno-coverage.ts` citations did not move; which `anno-cli.test.ts` assertions
task 1 changed and why; which WR-14 sites shipped and which remain open with the
reason; and the observed `md5sum docs/tool-support.md`.
</output>

## Artifacts this phase produces

| Kind | Name | File |
|---|---|---|
| function (exported) | `loadProjectImage` — the one coverage-path image loader | `src/mcp/vice/anno-coverage.ts` |
| file | the invocation-checking CI gate | `scripts/check-skill-cli-invocations.mjs` |
| file | the invocation predicates module | `scripts/lib/anno-cli-invocations.mjs` |
| function (exported) | `parseDocumentedInvocations` | `scripts/lib/anno-cli-invocations.mjs` |
| function (exported) | `checkInvocation` | `scripts/lib/anno-cli-invocations.mjs` |
| constant (exported) | `ANNO_INVOCATION_FLOOR` | `scripts/lib/anno-cli-invocations.mjs` |
| file | the committed non-vacuity and planted-violation proof for the invocation predicates | `src/mcp/vice/anno-cli-invocations.test.ts` |
| documentation | the 2026-08-30 note naming the coverage verb's accepted image forms | `src/skills/routine-queue-walker/SKILL.md` |
| test | the eight coverage-loader cases (CR-05, WR-07, REPOINT-01, `T-29-14-01` symmetry) | `src/mcp/vice/anno-coverage.test.ts` |
| CI step | the new gate declared as the sixth CI-step gate, beside the five already there (the seventh single-command gate overall; `audit-gate.mjs` is GSD-side and is not a CI step) | `.github/workflows/ci.yml` |
| record | the WR-14 row annotated in place with which sites shipped and which remain open | `.planning/phases/29-the-mcp-surface/29-VERIFICATION.md` |

**No renamed function and no renamed environment variable.** WR-14 sites 1 and 3
were dropped from this plan on the decision recorded in `<wr14_scope_decision>`;
the exported entry function and the CLI stdout-fill test hatch keep their current
names, and `src/mcp/vice/vice-proxy.ts` and `src/mcp/vice/vice-proxy.test.ts` are
not edited at all.

`ANNO_MODULE_FLOOR` in `hostpath-consumers.test.ts` is UNCHANGED: this plan adds no
`anno-*.ts` production module, and `module-classification.ts` is not one either.
The two new `scripts/` files must stay out of `src/mcp/vice/package.json`'s
`files[]`, exactly as `scripts/lib/anno-cli-verbs.mjs` does, while remaining
tracked by git so the release archive includes them.
`check-npm-packages.mjs` asserts relations rather than pinned file counts, so
none of this moves a count.

