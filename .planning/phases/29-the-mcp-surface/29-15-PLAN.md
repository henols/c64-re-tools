---
phase: 29-the-mcp-surface
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - src/skills/c64-program-recon/SKILL.md
  - src/skills/c64-program-recon/templates/memory-map.template.md
autonomous: true
gap_closure: true
requirements: [REPOINT-01, REPOINT-02]
user_setup: []

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "REPOINT-01 / ROADMAP criterion 2: the four documented `anno render-memmap` invocations in the canonical skill tree name a store the verb can actually open, and the command runs end to end against a real store and a real sidecar"
    - "REPOINT-02: the shipped `installer/skills/` twin carries the same corrected invocations, regenerated from the canonical tree by prepack/sync-skills rather than hand-edited -- proven by running the sync and reading the shipped copy"
    - "The dated note that asserts in the PRESENT TENSE that this verb still reads the pre-store project file is DELETED, and what replaces it states what plan 29-12 actually shipped in the same phase"
    - "REPOINT-01 probe/ordering: BOTH documented invocation lines in each of the two files -- the npx route and the in-repo node route -- are corrected, not only the first; the two routes name the same input"
    - "REPOINT-01 probe/adjacency: the copy-forward template and the playbook name the same store filename, so a project scaffolded from the template and a reader following the playbook end up on one route rather than two that differ by a filename"
    - statement: "installer/skills/ is regenerated from src/skills/ by prepack/sync-skills before any gate scans it, so the shipped twin can never be checked against a stale copy -- a hand-edit to the installer tree alone is therefore not independently covered"
      verification: backstop
  artifacts:
    - path: "src/skills/c64-program-recon/SKILL.md"
      provides: "Two runnable render-memmap invocations and a dated note that records what the verb now reads instead of asserting the behaviour that was replaced"
      contains: "annostore"
    - path: "src/skills/c64-program-recon/templates/memory-map.template.md"
      provides: "The copy-forward template's two invocations, corrected at the source that propagates into every new project"
      contains: "annostore"
  key_links:
    - from: "src/skills/c64-program-recon/SKILL.md"
      to: "installer/skills/c64-program-recon/SKILL.md"
      via: "installer/scripts/sync-skills.mjs, run by prepack -- the canonical tree is the only edited source and the shipped tree is generated"
      pattern: "annostore"
    - from: "src/skills/c64-program-recon/templates/memory-map.template.md"
      to: "src/mcp/vice/anno-cli.ts (cmdRenderMemmap)"
      via: "the documented invocation's positional argument is a path the store opener accepts"
      pattern: "render-memmap"
---

<objective>
Close the first half of verification gap 2 (CR-04): the `render-memmap`
invocation documented in the shipped playbooks cannot run, in BOTH skill trees,
and the dated note beside it asserts in the present tense the exact behaviour
this same phase replaced.

Four call sites, two canonical files and their two generated twins, all naming a
pre-store project filename that the store opener plan 29-12 gave this verb
refuses with `not an annotation store (file is not a database)` and exit 1. One
of the two files is `templates/memory-map.template.md` — **the template an agent
copies into every new project** — so the dead route propagates rather than
sitting still.

The note is the sharper half. `29-VERIFICATION.md`'s decision-coverage section
puts it plainly: `D-17` (the `render-memmap` rebuild) is the most-cited decision
in this phase at five summaries, so the phase KNEW it had rebuilt the verb and
shipped a playbook asserting the opposite in the present tense. Deleting the
note and replacing it with what is now true is the deliverable, not a tidy-up
beside one.

Purpose: ROADMAP criterion 2 claims both skill trees were re-pointed onto verbs
that exist, **proven against the shipped copy**, before the removal. That clause
is false today, and REPOINT-01 and REPOINT-02 are recorded `Gaps Found` because
of it.
Output: four corrected invocations, one deleted falsehood replaced by a dated
record, and the shipped twin proven correct by regenerating it and reading it.

**Scope note, stated so it is a decision rather than an omission.** This plan
does NOT touch `src/skills/routine-queue-walker/SKILL.md`. Its Phase-5
measurement command is the other half of gap 2 (CR-05), its input format's only
producer was deleted in this same phase, and its correct spelling depends on a
production change to the coverage loader. That work is plan 29-16, which runs in
wave 2 and owns that file. Splitting the two halves this way keeps the skill
edits here from being blocked on a code change, and keeps 29-16's skill edit
next to the code that makes it true.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-the-mcp-surface/29-VERIFICATION.md
@.planning/phases/29-the-mcp-surface/29-09-SUMMARY.md
@.planning/phases/29-the-mcp-surface/29-12-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Re-point all four invocations and delete the falsified note, proven by running the command end to end</name>
  <files>src/skills/c64-program-recon/SKILL.md, src/skills/c64-program-recon/templates/memory-map.template.md</files>
  <read_first>
    - `src/skills/c64-program-recon/SKILL.md` lines 240-275 — the "Generate the memory map; do not hand-author it" section, the two invocation lines at :255-256, the `--check` paragraph, and the dated note at :263-267 that must be deleted. Read the whole section, not the lines: the note's claim and the invocation's argument are one statement made twice.
    - `src/skills/c64-program-recon/templates/memory-map.template.md` lines 1-30 — the "Run the generator once findings are in the store" block at :11-12 and the `--check` paragraph beneath it, plus the sentence about the store being canonical. Note that the same file's opening paragraph still names the retired tool-name prefix for the store-writing tools; correct that too, since it is one sentence away and would otherwise leave the template pointing at a tool family that no longer exists.
    - `src/mcp/vice/anno-cli.ts` — `cmdRenderMemmap()` and the `USAGE` string's `render-memmap` entry. The playbook must document the verb the code actually implements: the positional is an EXISTING annotation store, the sidecar is required by name, and the verb creates neither.
    - `.planning/phases/29-the-mcp-surface/29-12-SUMMARY.md` — what plan 29-12 actually shipped when it rebuilt this verb onto the Phase 28 store (D-17). The replacement note must state that, not a reconstruction of it.
    - `src/skills/c64-program-recon/SKILL.md` around line 161 — the existing sentence that already tells the reader to name a store path on every mutating call. The corrected invocations must use the same filename convention that sentence uses, so the playbook is internally consistent.
    - `src/skills/acme-build/SKILL.md` lines 137-138 and 194 — the dated withdrawal notice CUT-05 landed. That is this repo's established shape for a dated note that records a route's fate; the replacement note in task 1 follows it.
  </read_first>
  <action>
    <!-- planner-discipline-allow: game.regen2000proj -->
    <!-- planner-discipline-allow: regen2000proj -->

    In `src/skills/c64-program-recon/SKILL.md`:

    1. Both invocation lines in the fenced block (the `npx -y @henols/vice-mcp`
       route and the in-repo `node .../vice-proxy.ts` route) pass a positional
       named `game.regen2000proj`. Replace that positional on BOTH lines with
       `game.annostore`, the store filename this same playbook already uses in
       its mutating-call guidance. Correct both lines, not the first: they are
       two routes to the same command and a reader on either one must succeed.

    2. DELETE the dated note that follows. It asserts, in the present tense,
       that this verb still reads the pre-store project file, and that the route
       which used to create those project files is withdrawn so the verb runs
       only against a project file you already have. Every clause of it is false
       as of plan 29-12, which rebuilt this verb onto the Phase 28 annotation
       store inside this same phase on `D-17`'s authority.

       Replace it with a dated note, `2026-08-30`, in the shape
       `acme-build/SKILL.md`'s withdrawal notice uses, recording three things:
       that this verb reads the annotation store directly as of Phase 29 plan
       29-12 (`D-17`); that the pre-store project file it used to read has no
       producer left in this repo, so there is no path back to the old spelling;
       and that the earlier note asserting the old behaviour was WRONG when it
       shipped and is deleted rather than amended. Naming the correction as a
       correction is the point — a silently-swapped sentence leaves the next
       reader unable to tell which of the two dated claims to believe.

    3. Leave the `--check` paragraph and the generated-file-banner paragraph
       intact. Both are true of the rebuilt verb and neither is in scope.

    In `src/skills/c64-program-recon/templates/memory-map.template.md`:

    4. Make the same positional correction on BOTH invocation lines in the
       generator block.

    5. The opening paragraph describes the store as written through tools named
       with the retired prefix. Update it to the current `anno_*` family name so
       a project scaffolded from this template is pointed at tools that exist.
       Keep the sentence's meaning — the store is canonical and this file is the
       schema for the one input the generator needs beyond it — and change only
       the family name.

    Do NOT edit anything under `installer/skills/`. That tree is gitignored and
    GENERATED: `installer/scripts/sync-skills.mjs` regenerates it from
    `src/skills/` on `prepack` and on the `sync-skills` script. A hand-edit
    there is not independently covered by any gate (the removal gate runs the
    sync before it scans, so it would be overwritten before it was measured) and
    would drift the moment the canonical tree changed. Task 2 runs the sync and
    proves the twin.
  </action>
  <verify>
    <automated>grep -rc 'annostore' src/skills/c64-program-recon/SKILL.md src/skills/c64-program-recon/templates/memory-map.template.md && ! grep -rq 'regen2000proj' src/skills/ && node scripts/check-skill-tool-coverage.mjs</automated>
    Expected: a non-zero count in both files, no occurrence of the retired project-file extension anywhere under `src/skills/`, and the coverage gate exit 0.
  </verify>
  <acceptance_criteria>
    - `grep -rn 'regen2000proj' src/skills/` produces no output and exits 1.
    - Both files name `game.annostore` on both of their invocation lines — four corrected sites in total.
    - Running the documented command end to end against a real temp store and a real sidecar exits 0 and prints the row count, `[unknown]` count and render digest. Record the exact command and its output in the summary; this is the proof the invocation is live, and it is the check the phase's own name-only gate could not perform.
    - `grep -n '2026-08-30' src/skills/c64-program-recon/SKILL.md` shows the replacement note, and the sentence asserting that the verb still reads the pre-store project file is absent from the file.
    - `node scripts/check-skill-tool-coverage.mjs` exits 0.
    - `node scripts/check-skill-description-overlap.mjs` exits 0 — the note edit must not push this skill's description past the 0.35 pairwise threshold.
  </acceptance_criteria>
  <reversibility rating="reversible">Documentation edits in two tracked Markdown files.</reversibility>
  <done>The four documented `render-memmap` invocations run, the copy-forward template no longer propagates a dead route into new projects, and the falsified dated note is deleted and replaced by a dated record of what is now true.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the SHIPPED copy — regenerate the installer tree and read it, rather than asserting the sync</name>
  <files>src/skills/c64-program-recon/SKILL.md</files>
  <read_first>
    - `installer/package.json` lines 45-55 — the `sync-skills` and `prepack` scripts, both running `node scripts/sync-skills.mjs`.
    - `installer/scripts/sync-skills.mjs` — what it copies and from where, so the summary can state the mechanism rather than the belief.
    - `.gitignore` lines 39-44 — the comment recording that `installer/skills/` is generated on pack from the canonical tree and is never committed.
    - `.planning/phases/29-the-mcp-surface/29-VERIFICATION.md` — the planted-violation table row noting that a hand-edit directly in `installer/skills/` produced exit 0 from the removal gate BECAUSE the gate runs the sync before scanning. That row is why this task exists as a separate, explicit proof step rather than an assumption inside task 1.
    - `scripts/check-npm-packages.mjs` — what it asserts about the `@henols/c64-re-tools` tarball (file count and skill count), so you know what its exit 0 does and does not prove.
  </read_first>
  <action>
    REPOINT-02's clause is "proven against the SHIPPED copy". Prove it by
    regenerating and reading, not by trusting the sync.

    1. Run the installer's skill sync from the repository root, then read the
       two regenerated files under `installer/skills/c64-program-recon/` and
       confirm each carries the corrected positional on both of its invocation
       lines and, for the playbook, the replacement dated note. Record the exact
       sync command and the exact lines read in the summary — the verifier's own
       finding was that the shipped twin carried the dead invocation VERBATIM,
       so an assertion that the sync works is not the evidence being asked for.

    2. Run `node scripts/check-npm-packages.mjs` and record its reported file
       and skill counts for both tarballs. Note in the summary what this gate
       does prove (the shipped tarball contains the skills and leaks no test
       files or fixtures) and what it does not (it does not read an invocation's
       arguments — that is 29-16's new gate).

    3. Add a short maintainer note to `src/skills/c64-program-recon/SKILL.md`,
       placed with the existing generated-artifact guidance rather than as a new
       top-level section, recording that this playbook has a generated twin
       under the installer package, that the twin is regenerated from this file
       and must never be edited directly, and that a change here is only shipped
       once the sync has run. This is the one durable artifact of this task; the
       rest is evidence for the summary.

    Do not commit anything under `installer/skills/` — it is gitignored by
    design and committing it would convert a generated tree into a second
    hand-maintained source, which is the failure mode the ignore rule exists
    against.
  </action>
  <verify>
    <automated>npm --prefix installer run sync-skills && grep -c 'annostore' installer/skills/c64-program-recon/SKILL.md installer/skills/c64-program-recon/templates/memory-map.template.md && ! grep -rq 'regen2000proj' installer/skills/ && node scripts/check-npm-packages.mjs</automated>
    Expected: the sync succeeds, both regenerated files report a non-zero count, the retired project-file extension is absent from the whole installer tree, and the package gate exits 0. `git status --porcelain installer/skills/` must additionally report NOTHING — the tree is ignored and must stay uncommitted.
  </verify>
  <acceptance_criteria>
    - `npm --prefix installer run sync-skills` exits 0.
    - `grep -rn 'regen2000proj' installer/skills/` produces no output and exits 1.
    - Both regenerated installer files name `game.annostore` on both invocation lines, and the regenerated playbook carries the 2026-08-30 replacement note — read and quoted in the summary, not asserted.
    - `node scripts/check-npm-packages.mjs` exits 0, and its reported skill count is 7.
    - `git status --porcelain installer/skills/` produces no output.
    - `src/skills/c64-program-recon/SKILL.md` carries the maintainer note naming the generated twin and forbidding direct edits to it.
  </acceptance_criteria>
  <reversibility rating="reversible">One documentation note added; the installer tree is regenerated output and is not committed.</reversibility>
  <done>The shipped copy is proven correct by regenerating it and reading it, the package gate is green, and the canonical playbook records that its twin is generated so the next editor does not repair the wrong tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped playbook prose → an agent's Bash invocation | The playbook is the instruction an agent follows literally; a documented command is executed, not read |
| canonical skill tree → generated installer tree | A one-way generation boundary; the shipped artifact is derived and must never become a second source |
| copy-forward template → every new project | The template propagates whatever it says into projects this repo will never see again |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-15-01 | Spoofing | the falsified dated note | **high** | mitigate | A dated note asserting in the present tense a behaviour that was replaced in the same phase impersonates a current record. An agent reading it concludes the failure it just hit is expected, and works around a bug rather than reporting it. Mitigated by DELETING the note — not amending it — and replacing it with a dated record that names the correction as a correction, so a reader can tell which of two dated claims to believe. |
| T-29-15-02 | Tampering | `templates/memory-map.template.md` | **high** | mitigate | This template is copied into every new project, so a wrong route here is written into repositories with no path back. Corrected at the source, and the correction is proven by running the documented command end to end rather than by reading it. |
| T-29-15-03 | Repudiation | hand-editing the generated installer tree | medium | mitigate | Explicitly forbidden by this plan, with the mechanism named: the removal gate runs the sync before scanning, so a hand-edit there is overwritten before it is measured and is therefore not independently covered. The canonical tree is the only edited source and task 2 proves the twin by regenerating it. |
| T-29-15-04 | Information Disclosure | skill prose | low | accept | These files carry no credentials, no host paths and no environment values. The edits are a filename and a dated note. |
| T-29-15-05 | Denial of Service | a documented command that always fails | medium | mitigate | The measurable form of this gap: a playbook step that cannot run costs every agent that follows it a failed invocation and a diagnosis. Closed by making the invocation live and by proving it end to end; the structural anti-regression mechanism (argument-checking every documented invocation) is plan 29-16's third task. |
| T-29-15-SC | Tampering | npm/pip/cargo installs | low | accept | `npm --prefix installer run sync-skills` runs an existing first-party script from a committed lockfile-free package; it installs nothing and adds no dependency. No package-legitimacy audit is triggered. |
</threat_model>

<verification>
1. `node scripts/check-skill-tool-coverage.mjs` — exit 0. Note what its greenness does and does not mean: it resolves tool and verb NAMES and never an invocation's arguments. That is the structural reason this gap passed the phase's own gate, and closing it is plan 29-16 task 3.
2. `node scripts/check-skill-fork-honesty.mjs` — exit 0. The note edits sit in a file this gate polices for fork-only claims.
3. `node scripts/check-skill-description-overlap.mjs` — exit 0, max pairwise score still below the 0.35 threshold.
4. `node scripts/check-npm-packages.mjs` — exit 0, 7 skills.
5. `node scripts/check-no-regenerator2000.mjs` — exit 0, temporary allow-list still **0 entries**. The gate runs the installer sync itself before scanning, so it also re-proves the twin.
6. `cd src/mcp/vice && npm run test:automated` — compare the failing-file SET against `29-BASELINE.md`.
7. `node scripts/audit-gate.mjs` — exit 0. Create no new REVIEW-shaped artifact.
8. The end-to-end run of the documented command is the load-bearing check in this plan and is NOT substitutable by any of the gates above. Run it, record the command and the output.
</verification>

<success_criteria>
- No occurrence of the retired project-file extension remains anywhere under `src/skills/` or `installer/skills/`.
- The documented `render-memmap` command runs to exit 0 against a real store and a real sidecar, with the transcript recorded.
- The falsified dated note is gone; a 2026-08-30 note in its place records what plan 29-12 shipped and that the earlier note was wrong when it shipped.
- The copy-forward template names the current tool family and the correct store filename on both routes.
- The shipped installer twin is regenerated and read, and the five gates in `<verification>` exit 0.
</success_criteria>

<output>
Create `.planning/phases/29-the-mcp-surface/29-15-SUMMARY.md` when done.
Record: the four corrected call sites by file and line, the deleted note's text
and the replacement's text, the exact end-to-end command run and its output, the
exact sync command and the regenerated installer lines read back, and the
reported counts from `check-npm-packages.mjs`.
</output>

## Artifacts this phase produces

| Kind | Name | File |
|---|---|---|
| documentation | the 2026-08-30 dated note recording the `render-memmap` rebuild onto the store (replaces the falsified note) | `src/skills/c64-program-recon/SKILL.md` |
| documentation | the maintainer note naming the generated `installer/skills/` twin and forbidding direct edits | `src/skills/c64-program-recon/SKILL.md` |

No new symbol, no new file path, no new CLI flag. Every change is a correction to
existing prose in two tracked Markdown files; the two `installer/skills/` twins are
regenerated output and are not tracked.
