---
phase: 29-the-mcp-surface
plan: 21
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [CUT-01, MCP-04]
user_setup: []

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "TRACE: truth 6 (`CUT-01` -- the anno surface's sizing claim is stated in figures that re-derive from the record's own predicate) — every figure in the `CUT-01` entry is one the executor MEASURED during this plan, at a NAMED commit, under the predicate the entry already documents; no figure is transcribed from `29-VERIFICATION.md` on trust"
    - "TRACE: truth 6 / gaps[1].missing[0] — the sentence is corrected to the figures that re-derive, in the SAME edit that touches the traceability row, and it NAMES the commit the surviving figure is anchored to so the next reader re-derives it rather than believing it"
    - "TRACE: truth 6 / gaps[1].artifacts[0] — the entry no longer asserts as measured a figure the same entry records as non-reproducing; `:128` and `:132` agree after this plan, so a reader can act on both halves"
    - "TRACE: prohibitions[2] (29-17) — `CUT-01`'s checkbox, sizing sentence, narrative paragraph and traceability row all move in ONE edit; moving the row alone is the failure the audit gate exists against"
    - "The substantive `CUT-01` claim is UNCHANGED and is not in dispute: zero `anno-*.ts` files remain under `src/mcp/vice/` and the removal gate is green tree-wide with an EMPTY temporary allow-list. Only the numerals move"
    - "No `overrides:` entry is written accepting 15,957/10,066 as a decided-but-unreproducing figure — the owner's 2026-08-30 decision was 'correct the text to the measured figures', and an override would defeat the requirement's own stated purpose"
    - "MCP-04 is promoted on the VERIFICATION VERDICT and on nothing else: `29-VERIFICATION.md` § Requirements Coverage scores it ✓ SATISFIED (was BLOCKED). Its checkbox, its traceability row, its per-phase status-table row AND that row's evidence cell all move in one edit, and the authorising sentence is quoted rather than paraphrased"
    - "Superseded paragraphs are SUPERSEDED WITH A DATE, never deleted — this file's convention, already visible at `:253` superseding `:255` and at `:257` superseding what came before it. The convention is ASSERTED POSITIVELY, not merely not-violated: the kept paragraphs are grep-proven to STILL CARRY the superseded numerals after the edit, while the live entry is grep-proven to carry none. A whole-file absence check would force the executor to choose between gutting a kept paragraph and failing a criterion, and either outcome re-opens gap 2"
    - "CUT-01 probe/empty (verification: explicit) — a selection that returns ZERO files fails the measurement loudly rather than reporting a total of 0, and a selected file that is zero lines long contributes 0 while still counting as a file; the pre-phase selection must return exactly the 35 files the entry names or the measurement stops"
    - "CUT-01 probe/adjacency (verification: explicit) — the pre-phase set and the surviving set are related by NAME DESCENDANCE, not path equality; the descendant map is asserted INJECTIVE (no two pre-phase files map to one survivor, no survivor claimed twice), and every pre-phase file with NO descendant at the anchor is enumerated as fully removed rather than silently dropped"
    - "CUT-01 probe/encoding (verification: explicit) — 'lines' has ONE definition across the subtraction: `git show <commit>:<path> | wc -l` at BOTH ends, which counts newline characters, so a file whose last line lacks a trailing newline contributes one fewer than its visible line count — accepted deliberately because the identical rule applies at both ends"
    - "CUT-01 probe/ordering (verification: explicit) — the summed total is independent of the order the selected paths are iterated, re-derived a second time with the list sorted differently and asserted equal"
    - "CUT-01 probe/concurrency (verification: explicit) — the measurement reads only the git object store at named commits, so it is unaffected by the working tree; re-running it with the tree dirty returns identical figures"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "A `CUT-01` entry whose every numeral was measured during this plan at a named commit under the predicate the entry documents, and an `MCP-04` promotion taken on the re-verification verdict across all four of its sites"
      contains: "CUT-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md (CUT-01 checkbox and sizing sentence)"
      to: ".planning/REQUIREMENTS.md (the CUT-01 traceability row)"
      via: "one edit, per the file's standing rule and 29-17's own prohibition"
      pattern: "CUT-01"
    - from: ".planning/REQUIREMENTS.md (MCP-04, all four sites)"
      to: ".planning/phases/29-the-mcp-surface/29-VERIFICATION.md (§ Requirements Coverage, MCP-04 row)"
      via: "the promotion is taken on the verification verdict, quoted, and on nothing else"
      pattern: "MCP-04"
    - from: "the CUT-01 sizing sentence"
      to: "a named git commit"
      via: "the sentence names the anchor commit, so the figure re-derives for the next reader instead of drifting with HEAD"
      pattern: "f16d0b1"
  prohibitions:
    - statement: "29-13: Never answer a question the surface cannot answer with a number that reads like an answer (a zero, an empty list or a clamped range where the honest answer is 'that is not a question I can answer about this image')."
      status: honored_on_the_mcp_surface_flagged_at_the_cli
      verification: judgment
    - statement: "29-14: Never write, keep or restore a comment that asserts a guarantee the code does not provide."
      status: violated
      verification: judgment
    - statement: "29-17: Never move a requirement's status row without changing the sentence that row scores."
      status: honored_literally_flagged_in_substance
      verification: judgment
---

<objective>
Gap 2, and one stale row. **This plan touches no code.**

**Gap 2 — `CUT-01`.** `.planning/REQUIREMENTS.md:128` states as **measured** *"a net
**10,066** lines … the remaining **15,957** lines surviving under new names"*.
`:132` — the **same entry** — records that the surviving end does **not**
re-derive and yields 19,714. The traceability row at `:242` has already been
demoted to `Gaps Found` by the verification pass. A reader cannot act on both
halves.

The verifier measured independently, under the requirement's **own** documented
predicate, and swept **every** commit from `8f21d77` to `d30b63e`: the surviving
figure is never 15,957 at any of them, so this is **not** commit drift. Two
alternative set-definitions miss in both directions (all `anno-*.ts` at `d30b63e`
= 36,905 over 36 files; content-survival across the 20 rename pairs = 11,611).

**The substantive claim is confirmed and is NOT in dispute**: zero `anno-*.ts`
remain under `src/mcp/vice/`, and the removal gate is green tree-wide with an
**empty** temporary allow-list. What fails is that a requirement whose whole
stated purpose is *"its numbers are the measured ones"* carries two numbers the
same entry admits do not reproduce, under a checkbox that says otherwise.

**Third item — `MCP-04`'s row is stale.** `29-VERIFICATION.md` § Requirements
Coverage scores **MCP-04 ✓ SATISFIED (was BLOCKED)**, but `REQUIREMENTS.md:232`
still reads `Gaps Found`, `:99`'s checkbox is still unticked, and `:266`'s
per-phase status row still quotes the superseded BLOCKED verdict. `STATE.md`'s
`last_activity_desc` says so in terms: *"a promotion left for the next gap plan
rather than taken from the orchestrator seat."* `:270` is the paragraph that sets
the rule — *"The row moves back UP only on a **re-verification verdict**"* — and
that verdict now exists.

Purpose: `CUT-01` is recorded ✗ BLOCKED and `MCP-04`'s record disagrees with its
own verifier. Output: an entry whose every numeral was measured during this plan
at a named commit, and a promotion taken on a quoted verdict.

<decided_remedy>
**The remedy for gap 2 is DECIDED by the owner and is not re-opened here.**
`29-VERIFICATION.md` § Human Verification Required put two options: correct the
sentence to figures that re-derive, or record an explicit `overrides:` entry
accepting 15,957/10,066 as a decided-but-unreproducing figure.

**Decision: correct the sentence to the figures that re-derive.** No `overrides:`
entry is to be written.

**Rationale, recorded because it is the second time this decision has been
implemented.** The owner's 2026-08-30 decision already chose *"correct the text to
the measured figures"*. Plan 29-17 executed that decision faithfully — it moved
the checkbox, the sentence, the paragraph and the row together, honouring its own
prohibition to the letter — but wrote a **surviving** figure that then failed to
reproduce, because it kept the verifier's as-measured number rather than
re-measuring. Finishing the same decision therefore means using figures that
**do** re-derive. An override would record the divergence as acceptable, which
defeats `CUT-01`'s own stated purpose: that its numbers are the measured ones.

**Reversibility:** `reversible` — a documentation edit in a file whose convention
is supersede-with-a-date, so the prior state stays legible in the same file and in
git history.
</decided_remedy>

<measurement_discipline>
**RE-MEASURE. Do not copy the verifier's numbers into the requirement on trust.**
This is the entire point of `CUT-01`, and it is the specific failure 29-17 made:
it transcribed a figure rather than deriving one. Task 1 runs the commands and
writes **whatever they return** — including if what they return disagrees with
both 15,957 and 19,714.

**If the measured figure disagrees with the verifier's 19,714, write the measured
one and say so prominently in the summary.** A third figure appearing is a
finding, not a licence to reach for whichever number is most convenient. It is
also not a reason to stop: the requirement asks for a figure that re-derives, and
a figure this plan derived and documented re-derives by construction.

**Anchoring the surviving figure to a named commit is part of the fix, not a
detail.** The verifier measured 19,714 at both `f16d0b1` and `d30b63e` but
**20,960** at today's HEAD — because the surviving `anno-*` files keep growing
with ordinary post-deletion work. A HEAD-anchored figure is therefore not
re-derivable by a later reader **by construction**, which is exactly the disease.

**The anchor is `f16d0b1`, and it is not changed.** The entry at `:130` already
documents `f16d0b1` as *"the phase HEAD this correction was written against"* and
already documents the predicate taken at it. Keeping the anchor means **only the
figure moves**, not the predicate — which is the smallest honest edit and leaves
the entry's provenance paragraph structurally intact. The sentence at `:128` must
**name** that commit so a later reader re-derives rather than believes.
</measurement_discipline>

<scope_verification>
Verified against `.planning/ROADMAP.md` § Phase 29 **Success Criteria** — exactly
**five** numbered criteria (SC-1..SC-5). `29-VERIFICATION.md`'s six observable
truths are those five plus **`CUT-01`**, which appears verbatim on the roadmap's
own `**Requirements**:` line for Phase 29 and has its own entry in
`.planning/REQUIREMENTS.md:128`. **Truth 6 is a declared phase requirement, not a
must-have lifted from a gap plan's frontmatter.** The denominator did not grow
between round 1 and round 2 — same six truths, 4/6 both times, a different four.
This is not the self-generated-scope loop, and it was checked rather than assumed.
</scope_verification>

<stopping_rule>
**Dated expectation, 2026-08-30.** If a **round-3** verification again finds new
defects in **plan-derived** truths while all five ROADMAP success criteria plus
`CUT-01` read verified, **the phase seals on the contract with its residuals
stated** rather than running a round 4.

**The residuals to state at seal — enumerated with their count so the two can be
checked against each other and against the record.** They are **every id the
disposition table in `29-VERIFICATION.md` § Code Review Disposition marks
OPEN-AS-WARNING**, which is **eleven**: WR-02, WR-03, WR-04, WR-06, WR-07, WR-08,
WR-09, WR-10, WR-11, WR-12, WR-13 — plus the three deferred ones (WR-14 → Phase
30, WR-15/WR-16 → on record). If the count and the list ever disagree, the table
is right and this sentence is wrong.

**WR-02 is the one id on that list this round partly works, and it is stated in
its unworked remainder rather than silently included or excluded.** Plan 29-20
corrects the three headers' claims about `anno-cli-path-consumers.test.ts` —
the 29-14 prohibition is recorded `violated` on them — but deliberately does
**not** give that test per-argument association, and 29-20 names that residual in
the headers themselves. The other ten are left exactly as dispositioned; this
round works none of them, and nothing here is planned for any deferred finding
either. Naming an id in this list states what is left over, never what to go and
do.

Sealing with stated residuals is a decision; discovering them later is not.
</stopping_rule>

<probe_provenance>
**Spec-less probe fallback fired (step 7.95): Phase 29 has no `29-SPEC.md`
(`EDGE_ABSENT=1`, `PROHIB_ABSENT=1`).**

**Edge probe scoping — a visible decision, recorded rather than silently taken.**
The deterministic edge probe was scoped to the three requirements this round
actually plans against (`REPOINT-01`, `REPOINT-02`, `CUT-01`) rather than all
twelve phase requirement IDs, deliberately, so it does not manufacture must-haves
for the nine requirements already verified SATISFIED — which is precisely the
scope growth this round is fenced against. As delivered:
`coverage.applicable = 11`, `resolved = 0`, `unresolved = 11`.

**This plan authors ALL FIVE `CUT-01` items, every one `explicit`.** They are not
decoration here — a line-counting predicate is exactly the kind of measurement
that goes wrong at its edges, and two of them (`empty` and `adjacency`) name the
specific ways a set-difference measurement silently produces a plausible wrong
number: a selection that returns nothing and totals 0, and a descendant map that
double-counts or drops. Both are asserted in task 1.

Across the round: 11 surfaced → 11 authored (9 explicit, 2 backstop), 0 dropped,
0 left unresolved. The no-silent-drop equality holds.

**Prohibition probe — carried forward, none minted.**
`29-VERIFICATION.md`'s `prohibitions:` frontmatter block contains **three**
entries (29-13, 29-14, 29-17), counted mechanically. The orchestrator's brief said
four; that is a **miscount**, reported here rather than resolved by inventing a
fourth prohibition to fill the slot. All three are carried verbatim into
`must_haves.prohibitions` with their recorded `status` and `verification`,
**descriptor-less** (no `check_*` scalar) per the fallback's `--auto` divergence,
so each disposes flagged-unverified and never auto-dismisses. **The 29-17
prohibition is this plan's to honour in substance as well as in letter** — its
recorded status is `honored_literally_flagged_in_substance`, flagged precisely
because 29-17 moved everything together but around a figure that did not
reproduce.
</probe_provenance>
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-the-mcp-surface/29-VERIFICATION.md
@.planning/phases/29-the-mcp-surface/29-17-SUMMARY.md
@.planning/phases/29-the-mcp-surface/29-10-SUMMARY.md
</context>

## Artifacts this phase produces

No source symbol is created. The artifacts are edits to one file, enumerated so a
verifier can check each site individually rather than trusting a diff summary.

| Kind | Site | Where | Note |
|---|---|---|---|
| Checkbox | `- [ ] **CUT-01**` → `- [x]` | `.planning/REQUIREMENTS.md:128` | Moves only in the same edit as the sentence, the paragraph and the row. |
| Sizing sentence (rewritten) | the `net N lines` / `remaining M lines` clause | `.planning/REQUIREMENTS.md:128` | Figures MEASURED by this plan; the sentence **names the anchor commit**. |
| Provenance paragraph (corrected) | the re-derivation-status paragraph | `.planning/REQUIREMENTS.md:132` | Must agree with `:128` after this plan; today it contradicts it, and its `18,728 at d30b63e` clause disagrees with the verifier's 19,714 and is itself re-measured. |
| Traceability row | `\| CUT-01 \| Phase 29 \| Gaps Found \|` → `Complete` | `.planning/REQUIREMENTS.md:242` | Same edit. |
| Narrative paragraph (NEW, dated) | supersedes `:253` | `.planning/REQUIREMENTS.md` ~`:253` | `:253`, `:255` and `:257` are all KEPT — this file supersedes with a date, it does not delete. |
| Checkbox | `- [ ] **MCP-04**` → `- [x]` | `.planning/REQUIREMENTS.md:99` | On the re-verification verdict. |
| Traceability row | `\| MCP-04 \| Phase 29 \| Gaps Found \|` → `Complete` | `.planning/REQUIREMENTS.md:232` | Same edit. |
| Per-phase status row + evidence cell | `\| \`MCP-04\` \| Gaps Found \| …BLOCKED quote… \|` | `.planning/REQUIREMENTS.md:266` | Both the status **and** the evidence cell move; leaving the superseded BLOCKED quote under a `Complete` status recreates the exact disagreement `:270` was written to end. |
| Narrative paragraph (NEW, dated) | supersedes `:270` | `.planning/REQUIREMENTS.md` ~`:270` | `:270` is KEPT. |
| NOT produced | an `overrides:` entry in `29-VERIFICATION.md` | — | Ruled out; see `<decided_remedy>`. |
| NOT produced | any change to `STATE.md` or `ROADMAP.md` | — | Not this plan's; the orchestrator owns the ROADMAP gap-closure narrative on a `--gaps` run. |
| NOT produced | a status move for any requirement other than `CUT-01` and `MCP-04` | — | `REPOINT-01`/`REPOINT-02` stay `Gaps Found` until a re-verification says otherwise. |

<tasks>

<task type="auto">
  <name>Task 1: Measure it, then write what the measurement returned — CUT-01's four sites moved in one edit</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md:128-141` — the whole `CUT-01` entry: the sizing sentence at `:128`, the **Provenance** paragraph at `:130` (which documents the two commits, the file-selection predicate at each end, and the single counting command), and the **Re-derivation status** paragraph at `:132`. Read all three before touching any of them; `:130` is the predicate this task must execute rather than re-invent.
    - `.planning/phases/29-the-mcp-surface/29-VERIFICATION.md` — the `gaps:` entry for truth 6 **in full**: the `reason` (which states the verifier's predicate verbatim and every figure it produced), `artifacts[0]`, and both `missing[]` lines. Then § Gaps Summary's paragraph beginning *"The fifth gap is not closed"*, and § Human Verification Required item 1 including the **suggested override block** — read that block so you can see concretely what this plan is deciding **not** to write.
    - `.planning/phases/29-the-mcp-surface/29-17-SUMMARY.md` — `key-decisions` in full. It records that 29-17 **kept the verifier's 15,957 even though re-derivation yielded 19,714**, on the reasoning that "an executor's re-measurement is a finding rather than a licence to substitute". That reasoning was defensible for 29-17 and is **superseded here**: this plan is authorised by a verification verdict that scored the resulting entry ✗ FAILED, so re-measuring and substituting is exactly its instruction. Say so in the summary rather than silently contradicting a prior plan.
    - `.planning/phases/29-the-mcp-surface/29-10-SUMMARY.md` — the plan that performed the deletion, for what was actually removed.
    - `.planning/REQUIREMENTS.md:249-258` — the four narrative paragraphs around the traceability table. Note the shape: `:253` is dated and says it supersedes `:255`, and `:257` explains that the paragraph it replaces "was correct when it was written, and is superseded rather than wrong". **That is this file's convention and it is mandatory: supersede with a date, never delete.**
    - `.planning/REQUIREMENTS.md:242` — the `CUT-01` traceability row.
    - This plan's `<measurement_discipline>` and `<decided_remedy>` blocks.
  </read_first>
  <action>
    <!-- planner-discipline-allow: 15,957 -->
    <!-- planner-discipline-allow: 10,066 -->

    **Step 1 — measure the pre-phase end at `8f21d77`.**

    Select with `git ls-tree -r --name-only 8f21d77 -- src/mcp/vice` filtered by the
    regular expression `^src/mcp/vice/anno-.*\.ts$`. Assert the selection is
    **non-empty** and that its cardinality is **35**; if it is not 35, stop and
    report — the entry names 35 and a different count means the predicate is not
    the one documented. Sum `git show 8f21d77:<path> | wc -l` over the selection.
    Split the same selection into test (`.test.ts` suffix) and non-test and record
    both subtotals; they must add to the total.

    Record the three numbers you get. The entry claims 26,023 = 10,035 non-test +
    15,988 test; the verifier confirmed those re-derive exactly. **Confirm it
    yourself; do not assume it.**

    **Step 2 — measure the surviving end at `f16d0b1`.**

    Derive the surviving set mechanically from the step-1 selection: for each
    `src/mcp/vice/anno-<rest>` produce the name-descendant
    `src/mcp/vice/anno-<rest>`, then add the three renamed test files
    `29-VERIFICATION.md` enumerates individually — `absorbed-answer-key.test.ts`,
    `spawn-seam.test.ts` and `docs-absorbed-decisions.test.ts`, all under
    `src/mcp/vice/`. Test each candidate's existence at the anchor with
    `git cat-file -e f16d0b1:<path>` before counting it, and sum
    `git show f16d0b1:<path> | wc -l` over those that exist.

    Four assertions, each of which is one of the edge predicates in
    `must_haves.truths` and each of which must be run and its result recorded:

    - **adjacency / injectivity:** no two pre-phase paths map to one descendant,
      and no descendant is counted twice. Assert the descendant list has no
      duplicates.
    - **adjacency / no silent drop:** enumerate by name every pre-phase file whose
      descendant does **not** exist at the anchor. Those are the fully-removed
      files; list them in the summary. A file dropped from the surviving set
      without being named is how a wrong number looks plausible.
    - **empty:** a zero-line file contributes 0 and still counts as a file; a
      selection returning zero files is a hard stop, never a total of 0.
    - **ordering:** re-derive the same total with the path list sorted in a
      different order and assert the two totals are equal.

    Then run the **concurrency** check: touch a file in the working tree so it is
    dirty, re-run both measurements, and assert identical figures — `git show` at
    a named commit reads the object store and is unaffected by the tree. Revert
    the touch.

    **Step 3 — cross-check, but do NOT write the cross-checks into the entry.**

    Run the same surviving-end predicate at `d30b63e` and at `HEAD`, and record
    both in the summary. `REQUIREMENTS.md:132` currently claims *"the same
    predicate yields 18,728 at `d30b63e`"* while `29-VERIFICATION.md` measured
    **19,714** there — the two records disagree, and step 3 is what settles it.
    Whichever your measurement returns, `:132` must be corrected to it.

    **Step 4 — write the four sites, in ONE edit.**

    Compute net removal as pre-phase total minus surviving total, and rewrite:

    - `:128`'s sizing sentence with your measured net figure and your measured
      surviving figure, and **name the anchor commit `f16d0b1` in the sentence
      itself** so the surviving figure re-derives for the next reader instead of
      drifting with HEAD. Say in the sentence, briefly, why it is anchored: the
      surviving `anno-*` files keep growing with ordinary work, so a HEAD-relative
      figure is not re-derivable by construction.
    - `:130`'s provenance paragraph — leave the predicate and the counting command
      **unchanged**; they are correct and are what you executed. Add only what your
      measurement adds, e.g. the count of fully-removed pre-phase files.
    - `:132`'s re-derivation-status paragraph — rewrite it to say that **both** ends
      now re-derive under the predicate above, with your figures, and correct the
      `d30b63e` clause to what step 3 measured. **Delete the sentence that keeps
      15,957 as the verifier's as-measured figure**: that sentence is the defect.
      Do **not** restate 15,957 or 10,066 anywhere as a live figure — record only
      that the previously-recorded surviving and net figures were superseded on
      2026-08-30 by this measurement, and point at `29-VERIFICATION.md` and this
      plan's summary for the superseded numerals. (Restating falsified numerals in
      a record whose purpose is that its numbers are the measured ones is the same
      mistake one level out; 29-17 made the same call and it was right.)

      **"Anywhere" in the sentence above means anywhere in the LIVE `CUT-01`
      entry — the block from the `CUT-01` bullet down to the `CUT-02` bullet,
      which is exactly what acceptance criterion 3 extracts by text range.** It
      does **not** mean anywhere in the file. The dated superseded paragraphs
      below the traceability table quote both numerals as the record of what was
      superseded, and that is the convention working rather than a violation of
      it. Criterion 3 asserts their survival positively. Do not edit them to
      satisfy a misread of this instruction — doing so would re-open gap 2 from
      the other side.
    - `:128`'s checkbox `- [ ]` → `- [x]`.
    - `:242`'s traceability row `Gaps Found` → `Complete`.

    Add a **new dated paragraph (2026-08-30)** immediately above `:253` recording:
    that the row moves back UP on this measurement; the predicate and the anchor
    commit; that the substantive claim — zero `anno-*.ts` under `src/mcp/vice/`,
    the removal gate green with an empty allow-list — was never in dispute and is
    unchanged; and that no `overrides:` entry was written, with the reason from
    `<decided_remedy>`. **Keep `:253`, `:255` and `:257` intact.** Three stacked
    dated paragraphs is the correct shape for this file, and a reader meeting them
    chronologically can tell which is live.

    **Step 5 — do not touch anything else.** Not `REPOINT-01`, not `REPOINT-02`,
    not `STORE-03` (recorded in `deferred-items.md` as a Phase-28 matter), not
    `STATE.md`, not `ROADMAP.md`. Do **not** use `gsd query requirements.*`
    mutation verbs for any of this — they operate at row granularity, which is the
    exact failure the 29-17 prohibition names. Hand-edit the file.
  </action>
  <verify>
    <automated>node scripts/audit-gate.mjs && npm --prefix src/mcp/vice run test:automated</automated>
  </verify>
  <acceptance_criteria>
    1. **The measurement is quoted as executed commands with their output in the summary**, for both ends: the exact `git ls-tree`, the filter, the file count, the `git show … | wc -l` summation, and the test/non-test split. A summary that states figures without the commands that produced them fails this criterion outright — that is the shape gap 2 exists to correct.
    2. **The written figures equal the measured figures**, checked mechanically: extract the net and surviving numerals from `.planning/REQUIREMENTS.md:128` and compare them to the measurement output. Quote both sides.
    3. **The superseded numerals are gone from the LIVE `CUT-01` entry, and are still PRESENT in the kept superseded paragraphs.** This criterion is deliberately SCOPED rather than whole-file, because a whole-file absence check cannot be satisfied without gutting a paragraph step 4 orders kept — measured live on 2026-08-30, the surviving-line numeral occurs on **4** lines of this file and the net-removal numeral on **3**, and two of those sites are the dated superseded paragraphs. Assert three things, each anchored to **text** rather than a line number, because this task edits the file and shifts the numbering:
       - **Absence in the live entry.** Extract the live entry by text range and check it:
         `awk '/^- \[.\] \*\*CUT-01\*\*/{f=1} f&&/^- \[.\] \*\*CUT-02\*\*/{f=0} f' .planning/REQUIREMENTS.md | grep -c -e '15,957' -e '10,066'`
         returns **0**. Run this BEFORE the edit too and record the value: it returns **2** on the pre-fix tree (measured 2026-08-30), and a post-fix 0 means nothing without that before-value. If the before-value is not 2, the extraction range is wrong — stop and re-derive it rather than trusting a 0.
       - **Presence in the move-up paragraph** (the one at ~`:255`, kept intact by step 4):
         `grep 'MOVES UP to `Complete` on the OWNER' .planning/REQUIREMENTS.md | grep -c '15,957'` returns ≥ 1, and the same pipeline ending `grep -c '10,066'` returns ≥ 1. This is the supersede-don't-delete convention **asserted**, not merely not-violated.
       - **Presence in the superseded-rather-than-wrong paragraph** (the one at ~`:257`, also kept):
         `grep 'superseded rather than wrong' .planning/REQUIREMENTS.md | grep -c '15,957'` returns ≥ 1. **That paragraph carries the surviving-line numeral only — it does not contain the net-removal numeral and never did** (measured 2026-08-30). Do not add it to manufacture a symmetry that was never there; an assertion demanding it would be exactly the unsatisfiable-criterion defect this round exists to close.
    4. `grep -n 'f16d0b1' .planning/REQUIREMENTS.md` shows the anchor commit named **inside the sizing sentence at `:128`**, not only in the provenance paragraph.
    5. `git diff --stat` for this task shows exactly **one** changed file, and `git diff .planning/REQUIREMENTS.md` shows the checkbox, the sizing sentence, the provenance paragraphs, the traceability row and the new dated paragraph all in the **same** diff — the 29-17 prohibition, honoured in substance this time because the figures reproduce.
    6. `grep -c 'MOVES BACK DOWN' .planning/REQUIREMENTS.md` returns ≥ 1 — the superseded dated paragraphs survive.
    7. Independent re-derivation: after writing, re-run **both** measurements from a clean shell and confirm the figures in the file reproduce. Quote the second run.
    8. `node scripts/audit-gate.mjs` exits **0**, and `node scripts/check-no-analyser.mjs` exits **0** with an empty temporary allow-list — the substantive claim this entry scores is re-observed rather than assumed.
  </acceptance_criteria>
  <reversibility rating="reversible">A documentation edit in a file whose convention preserves the superseded text with a date; git history holds the rest.</reversibility>
  <done>Every numeral in the `CUT-01` entry was produced by a command this plan ran, at a commit the sentence names, and the checkbox, the sentence, the provenance paragraphs and the traceability row moved together.</done>
</task>

<task type="auto">
  <name>Task 2: MCP-04 promoted on the verification verdict — all four of its sites, one edit</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/29-the-mcp-surface/29-VERIFICATION.md` § Requirements Coverage, the **MCP-04** row: `✓ SATISFIED (was BLOCKED)` with the evidence *"Both defects re-executed and closed; named tests exist for each (`anno-tools.test.ts:823`, `:846`, `:1352`, `:1409`)"*. Then § Behavioural Spot-Checks — the four rows marked `✓ PASS (was FAIL)` that belong to MCP-04 (the out-of-image disassemble refusal, the in-image answer, the nested batch at depth 2, the depth cap). **This is the authorising verdict and it must be quoted, not paraphrased.**
    - `.planning/REQUIREMENTS.md:99` — the `MCP-04` requirement text and its unticked checkbox.
    - `.planning/REQUIREMENTS.md:232` — the traceability row.
    - `.planning/REQUIREMENTS.md:259-268` — the header *"This phase's six requirement statuses, each with the summary that evidences it"* and the table, especially `:266`'s `MCP-04` row whose evidence cell **quotes the superseded BLOCKED verdict in full**. Both the status cell and the evidence cell must move; a `Complete` status beside a quoted BLOCKED verdict recreates exactly the disagreement `:270` was written to end.
    - `.planning/REQUIREMENTS.md:270` — the paragraph recording the earlier DOWNWARD correction and setting the rule: *"The row moves back UP only on a **re-verification verdict** — never on the completion of the gap-closure plans that address `CR-01` and `CR-06`, and never on an executor's reading of its own work."* **That rule is being satisfied, not bypassed.** Keep the paragraph; supersede it with a dated one.
    - `.planning/STATE.md`'s `last_activity_desc` — the sentence *"MCP-04 is scored SATISFIED by the verifier but its row still reads Gaps Found — a promotion left for the next gap plan rather than taken from the orchestrator seat."* This task is that plan. Read it so the summary can record that the promotion was taken in the right seat.
  </read_first>
  <action>
    Move **all four** `MCP-04` sites in one edit:

    1. `:99` — tick the checkbox `- [ ]` → `- [x]`. Leave the requirement's own
       text unchanged: nothing about what `MCP-04` demands has changed, only
       whether it is met.
    2. `:232` — traceability row `Gaps Found` → `Complete`.
    3. `:266` — the per-phase status row's status cell → `Complete`, **and** its
       evidence cell rewritten. Replace the quoted BLOCKED verdict with the
       authorising one, quoted from `29-VERIFICATION.md` § Requirements Coverage,
       naming the two closed findings (**CR-01** as it appeared in the earlier
       review — the out-of-image refusal — and **CR-06**, the nested batch) and the
       four named tests. State that the verifier **re-executed** both rather than
       reading the fix, because that is the property that makes the promotion
       trustworthy. Keep the row's existing convention of naming the evidencing
       summaries.
    4. Add a **new dated paragraph (2026-08-30)** immediately above `:270`
       recording: that the row moves **UP** on the re-verification verdict of
       2026-08-30T15:20Z; that this is exactly the condition `:270` named; which
       two findings closed and that each was re-executed; and that the promotion
       was taken by a gap-closure plan rather than from the orchestrator seat, as
       `STATE.md` records it was left to be. **Keep `:270` intact** — supersede
       with a date, do not delete.

    **A caution that is the whole reason this task is separate from task 1.**
    Promote `MCP-04` and **nothing else**. `REPOINT-01` and `REPOINT-02` are
    recorded ⚠️ PARTIAL and stay `Gaps Found` until a re-verification says
    otherwise — plans 29-18, 29-19 and 29-20 addressing their gaps is **not** a
    verdict, and this project has a recorded incident of a tool flipping every
    phase requirement to `Complete` on its own reading. Check the diff for any row
    you did not intend to touch before committing.

    Do **not** use `gsd query requirements.*` mutation verbs. Hand-edit.
  </action>
  <verify>
    <automated>node scripts/audit-gate.mjs && npm --prefix src/mcp/vice run test:automated</automated>
  </verify>
  <acceptance_criteria>
    1. `grep -n 'MCP-04' .planning/REQUIREMENTS.md` returns **five** kinds of hit and each is consistent: a ticked checkbox at `:99`, a `Complete` traceability row, a `Complete` per-phase status row whose evidence cell quotes the SATISFIED verdict, the preserved `:270` paragraph, and the new dated paragraph. Quote the full grep output.
    2. `grep -c 'BLOCKED' .planning/REQUIREMENTS.md` — every remaining occurrence sits inside a **dated, superseded** paragraph, never inside a live status cell. Enumerate each hit with its line and say which paragraph it belongs to.
    3. `git diff .planning/REQUIREMENTS.md` for this task shows changes to `MCP-04`'s four sites and to **no other requirement's** status row. Enumerate every changed row in the summary by requirement id — an unintended flip is the recorded failure mode this criterion exists against.
    4. `node scripts/audit-gate.mjs` exits **0**.
    5. `npm --prefix src/mcp/vice run test:automated` — report pass/fail counts and the failing-file set by name. `docs-review-disposition.test.ts` must be green (7/7); the two suite assertions that keyed on undispositioned review ids are already discharged by `29-VERIFICATION.md` and must stay discharged.
    6. `grep -c 'corrected DOWNWARD on 2026-08-30' .planning/REQUIREMENTS.md` returns ≥ 1 — the superseded paragraph survives.
  </acceptance_criteria>
  <reversibility rating="reversible">A status promotion in a supersede-with-a-date document; revertible in one commit.</reversibility>
  <done>`MCP-04` reads `Complete` in every place the file answers the question, each moved in one edit, on a quoted re-verification verdict and on nothing else.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verifier's report → the requirement record | A verdict crosses into a durable status. A record that promotes on anything less than a verdict is a self-assessment wearing a verdict's clothes. |
| Requirement record → a later reader (Phase 32's `CUT-04`, the milestone audit) | Numbers written here are consumed by later audits that will not re-measure them. |
| Prior measurement → this measurement | A transcribed figure looks identical to a derived one in the finished text. Only the process distinguishes them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-21-01 | Spoofing | the `CUT-01` sizing sentence | high | mitigate | A transcribed figure presented as a measurement is a forged measurement, and it is exactly what shipped in 29-17. Mitigated by task 1 running the commands and criterion 1 requiring the commands **and their output** in the summary, plus criterion 7's independent re-derivation from a clean shell. |
| T-29-21-02 | Tampering | the `MCP-04` status rows | high | mitigate | Promoting a requirement on an executor's reading of its own work corrupts the record's meaning. Mitigated by quoting the verifier's authorising sentence (task 2 step 3) and by criterion 3 enumerating every changed row so an unintended flip cannot pass — this project has a recorded incident of exactly that. |
| T-29-21-03 | Repudiation | the superseded narrative paragraphs | medium | mitigate | Deleting a superseded paragraph erases which claim was live when, and this file's whole integrity story is that chronology. Criteria 6 (both tasks) assert survival by grep. |
| T-29-21-04 | Information Disclosure | — | low | accept | No secret, credential or user datum is read or written; the plan reads git metadata and one planning document. Accepted at ASVS L1 with nothing to mitigate. |
| T-29-21-05 | Denial of Service | `node scripts/audit-gate.mjs` | medium | mitigate | A malformed edit can red the docs/audit gate and cascade into the suite (`docs-review-disposition.test.ts`, the milestone-audit assertion). Mitigated by running the gate **and** `test:automated` in both tasks' `<verify>` rather than at the end of the plan. |
| T-29-21-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package-manager install task exists in this plan** — it touches one Markdown file and runs `git`, `node` and `npm run` against already-installed dependencies. No `[ASSUMED]`/`[SUS]` package row exists to clear, so no blocking human checkpoint is owed. If execution discovers an install is needed, **stop** and raise it. |

No `high` threat is dispositioned `mitigate` without a named verification, satisfying `security_block_on: high`.
</threat_model>

<verification>
- Both measurements re-run from a clean shell reproduce the figures written into `.planning/REQUIREMENTS.md`.
- The live `CUT-01` entry, extracted by text range, contains neither superseded numeral (0), having contained both (2) before the edit — while the dated superseded paragraphs at ~`:255` and ~`:257` still carry theirs. Scoped deliberately: a whole-file absence check would require gutting a paragraph the plan orders kept, and would re-open gap 2 either way.
- `f16d0b1` is named inside the sizing sentence.
- `MCP-04` reads `Complete` at all four sites, with the superseded BLOCKED quote replaced.
- Every superseded dated paragraph survives (`MOVES BACK DOWN`, `corrected DOWNWARD on 2026-08-30`).
- `node scripts/audit-gate.mjs` exits 0.
- `node scripts/check-no-analyser.mjs` exits 0 with an empty temporary allow-list.
- `npm --prefix src/mcp/vice run test:automated` — failing-file set reported by name; `docs-review-disposition.test.ts` 7/7.
- `git diff --name-only` across the whole plan lists exactly `.planning/REQUIREMENTS.md`.

## Regression guard for the four ✓ VERIFIED criteria

This plan changes no code and no gate. SC-1, SC-3, SC-4 and SC-5 cannot be
disturbed by it, and the removal gate is re-run above to say so rather than assume
it. The nine SATISFIED requirements other than `MCP-04` keep their rows untouched,
asserted by task 2 criterion 3's row-by-row enumeration.
</verification>

<success_criteria>
- Every numeral in `CUT-01` was produced by a command this plan ran, at a commit the sentence names.
- `:128` and `:132` agree; a reader can act on both halves.
- No `overrides:` entry was written.
- `MCP-04` is `Complete` everywhere the file answers that question, on a quoted verdict.
- No other requirement's status moved.
- Every superseded paragraph survives with its date.
</success_criteria>

<output>
Create `.planning/phases/29-the-mcp-surface/29-21-SUMMARY.md` when done.

Record explicitly: every measurement command and its raw output for both ends and
both cross-check commits; the file counts, the test/non-test split, the
fully-removed file list by name, the injectivity result, the different-order
re-derivation, and the dirty-tree re-run; whether the measured figures agree with
the verifier's 19,714 and — if not — the discrepancy stated prominently; the
before/after value of every edited site by line; the row-by-row enumeration of
what changed in the traceability table; and the `test:automated` failing-file set
by name.
</output>
