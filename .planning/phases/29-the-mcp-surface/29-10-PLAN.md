---
phase: 29-the-mcp-surface
plan: 10
type: execute
wave: 7
depends_on: ["29-02", "29-09", "29-12"]
files_modified:
  # >> THE DELETION SET, spelled out path by path. 29-05's own manifest header
  # states the rule this obeys: a path absent from a manifest is exactly how a
  # file survives a worktree-isolated merge. `use_worktrees` is now TRUE, so
  # that IS merge-fatal: an unlisted deletion would not survive the merge, and it
  # also leaves BOTH the undo manifest and the wave-overlap guard blind to it.
  # NOTE: stock `worktree-safety.cjs`'s cleanup-wave check blocks any branch whose diff
  # contains a deletion, unconditionally — this plan's branch will build and
  # commit cleanly but `cleanup-wave` will refuse it, so merge it by hand.
  # This plan's own
  # acceptance criterion is that none of these exists on disk afterwards.
  - src/mcp/vice/anno-launch.ts
  - src/mcp/vice/anno-launch.test.ts
  - src/mcp/vice/anno-mcp-client.ts
  - src/mcp/vice/anno-mcp-client.test.ts
  - src/mcp/vice/anno-project.ts
  - src/mcp/vice/anno-project.test.ts
  - src/mcp/vice/anno-session.ts
  - src/mcp/vice/anno-session.test.ts
  - src/mcp/vice/anno-tools.ts
  - src/mcp/vice/anno-tools.test.ts
  # D-16's two, deleted rather than renamed on the authority of their own
  # registry `note` fields. `anno-test-gate.ts` has no co-located test.
  - src/mcp/vice/anno-test-gate.ts
  - src/mcp/vice/anno-verify.ts
  - src/mcp/vice/anno-verify.test.ts
  # D-11's recorded-choice deletion, named here rather than referred to as
  # "the round-trip test": the live symbol round trip, whose subject plan
  # 29-07 already withdrew. It is NOT the symbols module's co-located test and
  # was deliberately excluded from 29-05's rename sweep for that reason.
  - src/mcp/vice/anno-symbol-roundtrip.test.ts
  # >> SURVIVORS this plan must edit BECAUSE it deletes what they import or
  # what their gate entry describes. Each is here under 29-05's reachability
  # rule (a temporary allow-list entry may cite only a plan carrying both the
  # gate and the entry's own file) or because its import list dies here.
  - src/mcp/vice/anno-symbols.ts
  - src/mcp/vice/anno-enum-gen.ts
  - src/mcp/vice/anno-enum-gen.test.ts
  - src/mcp/vice/anno-memmap-render.test.ts
  - src/mcp/vice/anno-coverage.ts
  - src/mcp/vice/anno-cli.ts
  - src/mcp/vice/anno-cli.test.ts
  - src/mcp/vice/spawn-seam.test.ts
  - src/mcp/vice/vice-proxy.test.ts
  - src/mcp/vice/module-classification.ts
  - src/mcp/vice/vice-proxy.ts
  - src/mcp/vice/package.json
  - src/mcp/vice/docs-linerefs.test.ts
  - CLAUDE.md
  - scripts/check-no-analyser.mjs
  - .planning/phases/29-the-mcp-surface/fixtures/README.md
  # >> THE FIVE IMPORTERS THE FIRST SWEEP MISSED, each with a stated fate in
  # Task 2. The first sweep measured only `src/mcp/vice/*.test.ts` and the two
  # capability modules; these were found by widening to EVERY tracked file
  # under `git ls-files` minus `.planning/`. Four are invisible to at least one
  # of the three mechanisms that would otherwise catch them, which is why the
  # miss was silent rather than loud:
  #   - make-coverage-fixtures.mjs is invisible to ALL THREE — `tsconfig.json`
  #     compiles only `**/*.ts` and `**/*.mts`, `node --test '*.test.*'` never
  #     loads it, and `grep -ac the external analyser` on it is 0 because it names the
  #     PATH and not the word. Its dependency was nevertheless already recorded:
  #     `module-classification.ts:598` cites it as a surviving consumer of
  #     `anno-project.ts`, so the registry D-03 makes authoritative for exactly
  #     this question had it all along.
  #   - check-skill-tool-coverage.mjs is `.mjs`, so typecheck and the test
  #     runner are both blind; only running the script catches it. Plan 29-09
  #     Task 3 re-points it at wave 5 and asserts the retired import is gone —
  #     it is named here as the wave-7 safety net, not as a second owner.
  #   - anno-memmap-render.ts's import of the retired runner IS USED, and the
  #     first sweep recorded it as unused. That was measured with a plain
  #     `grep`, which is blind to this file (NUL byte at offset 12862), so the
  #     import at `:69` and its use at `:106` inside `queryAnnoJson()` were
  #     both invisible. `renderMemoryMap()` calls that helper three times at
  #     `:353-355` and `checkRenderedMemoryMap()` reaches it too, so the verb
  #     drives the child. That falsified D-14's premise and produced D-17:
  #     plan 29-12 rebuilds the verb onto the Phase 28 store at wave 6 and
  #     removes the import as part of that rebuild. It is named here as the
  #     wave-7 safety net, not as a second owner — the same posture this list
  #     already takes for check-skill-tool-coverage.mjs.
  #   - anno-derivation.test.ts's import IS used, by the upstream-integrity
  #     half plan 29-08 is explicitly required to leave unchanged.
  #   - stock-dispatch.test.ts is removed at wave 1 by plan 29-01; it is named
  #     here as a safety net only. It appears in no other wave-7 plan — 29-10
  #     is the sole plan in wave 7 — so none of these five can create a
  #     same-wave `files_modified` overlap. (This plan moved from wave 6 to
  #     wave 7 when D-17 added plan 29-12 at wave 6; the two memmap entries
  #     above are discharged there and survive here only as safety nets.)
  - src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs
  - scripts/check-skill-tool-coverage.mjs
  - src/mcp/vice/anno-memmap-render.ts
  - src/mcp/vice/anno-derivation.test.ts
  - src/mcp/vice/stock-dispatch.test.ts
autonomous: false
requirements: [CUT-01, MCP-02]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "The deletion set comes from the classification registry entry by entry, never from a prefix sweep, and every deleted module's registry entry carries a `deleted` fate whose closure the registry's own guard checks against disk."
    - "Every deleted file leaves the package's file list in the same commit — a file list naming a file that is not on disk makes the packaging check fail, and an unproven packaging gate is a vacuous one."
    - "The two pinned false-pass transcripts the registry names as the thing that must survive are moved forward as Phase 30 fixtures, with their provenance and Phase 30's obligation to re-record them from real assembler output written down beside them."
    - "The two `rewriteArguments()` line citations in the project instructions are corrected in the SAME commit that changes the line count above them, and the guard that checks them is green afterwards — a mismatch is treated as drift to re-verify, which is what the instructions themselves say to do."
    - "The removal gate's temporary allow-list is empty of deletion-bound entries after this commit, and the gate is green over the tree without them."
    - "The deletion of the live symbol round-trip test — `anno-symbol-roundtrip.test.ts`, named rather than referred to — is an explicit recorded choice with its subject's withdrawal already recorded, never a side effect of a prefix sweep."
    - "D-01 is satisfied literally: after this plan no test anywhere drives a real analyser child, and no test file retains an env-gated block, an availability-gate assertion or an import whose subject this plan deleted — the directive's own words are `never be included in any tests`, and a gated test whose subject is gone is still a test that includes it."
    - "Every surviving file whose import list names a module this plan deletes is edited in the SAME commit as the deletion, where 'every' is measured tree-wide over `git ls-files` minus `.planning/` rather than over one directory. Typecheck and the automated suite catch the `.ts` importers but are structurally blind to `.mjs`, and the removal gate greps the word rather than the path, so the tree-wide two-arm sweep — not those three — is what makes an unlisted importer impossible to miss silently."
    - "Every temporary allow-list entry citing this plan names a file this plan carries in `files_modified`, so each can actually be discharged here rather than surviving to the phase close as an orphan."
    - "The two capability modules whose routes died keep their knowledge and lose only their routes: the functions that reached the deleted glue are removed, the modules type-check, and what Phase 30 rebuilds is named in each module's header."
  artifacts:
    - CLAUDE.md
    - .planning/phases/29-the-mcp-surface/fixtures/README.md
  key_links:
    - "module-classification.ts deleted fates -> the discharge-closure relation the registry's guard checks"
    - "package.json files[] <-> the files on disk (check-npm-packages.mjs fails on a stale entry)"
    - "CLAUDE.md's rewriteArguments() citations <-> vice-proxy.ts line numbers <-> docs-linerefs.test.ts"
  prohibitions:
    - statement: "No guard may be \"re-pointed\" to a subject that cannot fail, and no exemption may be widened to silence a real reintroduction; a guard whose planted violation no longer reddens has not been re-pointed."
      status: unverified
      verification: flagged
---

<objective>
Delete the integration. The gate is already standing and already observed biting;
the skills already point somewhere real; the guards have already moved.

Purpose: D-01, taken as a one-way decision by the owner. This plan is deliberately
last-but-one and deliberately gated: the `4f048bb` precedent is a milestone closed
over an already-red guard with nothing forcing anyone to notice, and every
precondition that precedent asks for has been discharged by plans 29-02, 29-05,
29-07 and 29-09 before this one runs.
Output: the glue gone, the record of what it was kept, and CI green against the
recorded baseline.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-the-mcp-surface/29-CONTEXT.md
@.planning/phases/29-the-mcp-surface/29-RESEARCH.md
@.planning/phases/29-the-mcp-surface/29-BASELINE.md
@.planning/phases/29-the-mcp-surface/29-02-SUMMARY.md
@.planning/phases/29-the-mcp-surface/29-09-SUMMARY.md
</context>

## Decisions and readings adopted by this plan

- **D-01** (locked, user decision): the integration is deleted in this phase, not
  unregistered and not deferred. The owner's directive, recorded as data: the usage
  is decided to be removed, and if it cannot be removed right now everything about
  it must be disabled and never included in any tests. Presented with
  unregister-and-quarantine as the recommended option, the owner chose deletion.
- **D-02** (locked): the deletion set is the glue — every module the registry
  verdicts `glue` — while the CLI was renamed and kept.
- **D-16** (agent-resolved): two further modules are deleted rather than renamed,
  on the authority of their own registry `note` fields. **The consequence this plan
  must carry:** the verify module's two pinned false-pass transcripts are what the
  registry names as the thing that must survive. They ride forward as Phase 30
  fixtures; this plan moves them rather than letting them go with the module.
- **D-14's consequence, already recorded:** the two capability modules that
  implemented the symbol round trip and the enum generation survive under their new
  names with their **routes** removed and their **knowledge** kept. Their live
  round-trip test is deleted here, as an explicit choice, because its subject is
  withdrawn — and the withdrawal itself was already written into the project record
  by plan 29-07, so this deletion discharges a recorded decision rather than making
  a silent one.
- **Planner correction to `29-CONTEXT.md` (N-11), acted on here:** the proxy carries
  five references to the retired integration, not two. Plan 29-01 substituted the
  two that were line-count-neutral and plan 29-09 substituted the subcommand token
  in place; this plan removes the remaining two, which **does** change the line
  count above the cited lines. `CLAUDE.md`'s two `rewriteArguments()` citations are
  therefore corrected in this same commit — the instructions themselves say a
  mismatch is drift to re-verify, not evidence the constraint changed.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: One-way decision gate — the deletion, and what cannot be bought back</name>
  <read_first>
    - .planning/phases/29-the-mcp-surface/29-CONTEXT.md — D-01 and D-02 in full, including the reversibility notes recorded against each.
    - .planning/phases/29-the-mcp-surface/29-02-SUMMARY.md — the seven-step observed-red transcript, which is this gate's evidence that the gate exists and bites.
    - .planning/phases/29-the-mcp-surface/29-09-SUMMARY.md — the proof that both skill trees point somewhere real, including the shipped copy.
    - .planning/phases/29-the-mcp-surface/29-BASELINE.md — the recorded pre-phase failing-file set this plan's CI sweep is compared against.
    - .planning/ROADMAP.md § "Sequencing Rationale (v0.7.0)" — ordering constraints 2, 3 and 5, and which of them this plan is the discharge of.
  </read_first>
  <decision>Execute D-01's one-way call: delete the retired integration's glue modules and their tests, so this project stops depending on the external analyser at all.</decision>
  <context>
    D-01 is already a locked owner decision; this gate exists because
    `REVERSIBILITY_GATES` requires a one-way move to be confirmed immediately
    before it is executed, and because the `4f048bb` precedent is a milestone
    closed over an already-red guard with nothing forcing anyone to notice.

    **What is spent here:** the safety property the roadmap's own criterion 2 was
    written to buy — that the replacement is demonstrably ready before anything is
    removed. Restoring the route later means restoring a deleted external
    integration and its pinned crate version, and that demonstration cannot be
    re-run after the fact.

    **The preconditions, each to be cited to the summary that evidences it rather
    than asserted.** The removal gate exists and was observed biting on four
    planted routes plus the exemption non-vacuity plant, with a green
    false-positive control (plan 29-02). Both skill trees point at verbs that exist
    or say plainly that a verb is withdrawn, proven against the shipped copy rather
    than the tracked one (plan 29-09). Every guard that breaks on the rename moved
    with the rename (plan 29-05). Every guard that breaks on registration moved in
    the registering commit (plan 29-01). The pre-phase failing-file set is recorded,
    so "green" has a defined meaning (plan 29-02).

    **What is deliberately NOT in this phase:** the recorded fate of every guard and
    CI script pinned to the subject, and every living document naming it as a
    required prerequisite beyond the ones this phase touches, remain Phase 32's
    requirements. Narrowing that phase rather than emptying it is plan 29-11's
    subject.
  </context>
  <options>
    <option id="delete-now">
      <name>Delete the glue in this phase — the owner's recorded choice (D-01, D-02, D-16)</name>
      <pros>The external dependency is gone at this phase's close; the record stops describing a plan that was abandoned; the gate that guards the removal has already been observed biting, which is the ordering the `4f048bb` precedent demands.</pros>
      <cons>One-way: the demonstration criterion 2 asked for cannot be re-run, and restoring the route means restoring a deleted external integration and its pinned dependency.</cons>
    </option>
    <option id="unregister-and-quarantine">
      <name>Keep the files, unregister the family and gate its tests off</name>
      <pros>Reversible; the structural guards stay live against a real subject.</pros>
      <cons>Rejected by the owner at decision time, whose directive was that if it cannot be removed now, everything about it must be disabled and never included in any tests. This option leaves it present.</cons>
    </option>
    <option id="halt-and-report">
      <name>Halt, because a precondition above is not evidenced</name>
      <pros>Prevents exactly the failure the `4f048bb` precedent records: a removal landing over a guard that was never proven to bite.</pros>
      <cons>The phase stops one plan short of its goal and the deletion moves to a follow-up.</cons>
    </option>
  </options>
  <resume-signal>Reply "confirmed" to proceed with the deletion, or name a precondition that is not evidenced in its summary to halt.</resume-signal>
  <verify>
    <human-check>The owner has confirmed the deletion, and the confirmation is recorded in `29-10-SUMMARY.md` with its date and with the five preconditions each cited to the summary that evidences it.</human-check>
  </verify>
  <acceptance_criteria>
    - `29-10-SUMMARY.md` records the confirmation, its date, and one citation per precondition to the plan summary that evidences it.
    - No file has been deleted by this task.
  </acceptance_criteria>
  <done>The one-way call is confirmed against evidence rather than intention, or the phase stops here.</done>
  <reversibility rating="one-way">The external integration and its pinned dependency go; the "replacement demonstrably ready before anything is removed" property cannot be re-bought after the fact.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Delete the glue, keep the knowledge, and carry the transcripts forward</name>
  <files>src/mcp/vice/anno-symbols.ts, src/mcp/vice/anno-enum-gen.ts, src/mcp/vice/anno-enum-gen.test.ts, src/mcp/vice/anno-memmap-render.ts, src/mcp/vice/anno-memmap-render.test.ts, src/mcp/vice/anno-coverage.ts, src/mcp/vice/anno-cli.ts, src/mcp/vice/anno-cli.test.ts, src/mcp/vice/anno-derivation.test.ts, src/mcp/vice/spawn-seam.test.ts, src/mcp/vice/stock-dispatch.test.ts, src/mcp/vice/vice-proxy.test.ts, src/mcp/vice/module-classification.ts, src/mcp/vice/package.json, src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs, scripts/check-skill-tool-coverage.mjs, .planning/phases/29-the-mcp-surface/fixtures/README.md</files>
  <read_first>
    - src/mcp/vice/module-classification.ts — every entry still carrying an `in-enumeration` scope. That set, entry by entry, is the deletion set; confirm it against the verdicts and the two contested notes before removing anything.
    - src/mcp/vice/anno-symbols.ts — its import list and the functions that reach the deleted glue: the label export path, the label import path and their session use. What survives is the label-file format knowledge and the naming vocabulary; what goes is the route.
    - src/mcp/vice/anno-enum-gen.ts — the same, for the enum generation path and its tool round trips.
    - The verify module and its test — the two pinned transcripts, the honest pass and the false-pass trap, and the recorded verbatim text of each.
    - .planning/ROADMAP.md § Phase 30 — its note that both transcripts must be re-recorded from real assembler output rather than from the deleted producer's, which is what the carried-forward copies are the shape for and not the content of.
    - src/mcp/vice/package.json:10-80 — every file-list entry naming a module about to be deleted.
    - src/mcp/vice/test-gate.mjs:90-115 — how the automated set is derived, so the deleted tests leave it correctly. Measured at plan time: `MANUAL_ONLY_TESTS` names none of the files this plan deletes, so the frozen nine is expected to stay nine — confirm rather than assume.
    - The import line of every surviving file that still names a module this plan deletes. **Re-measured tree-wide at plan time over `git ls-files` minus `.planning/`, not over `src/mcp/vice/*.test.ts` — that narrower scan is what produced the earlier four-file figure, and it missed five.** The tree-wide scan finds **25** importing files. **11** are this plan's own deletions, leaving **14 survivors**: the memmap-render test (three such imports) and the memmap-render *module* (one, and it IS used — see D-17; discharged upstream by plan 29-12 at wave 6, so this plan expects to find nothing to do), the enum-gen test (two) and the enum-gen module (one), the CLI test and the CLI module, the spawn-seam guard (five, plus two more inside planted-source string literals it builds for its own violation plant), the symbols module (three), the derivation guard (one, and it IS used), the stock-dispatch guard (its import at `:45`), the proxy's own manual-only test (the curated-tool set from the deleted tool module), the skill-coverage script, the coverage-fixture generator, and the proxy itself. Thirteen are named in this task's `<files>`; the fourteenth is the proxy, which Task 3 owns. Read each import line before editing, and treat a survivor that is NOT in this list as a measurement that has drifted — stop and report rather than editing a file this plan does not name.
    - src/mcp/vice/anno-coverage.ts — the four needle mentions 29-05 bucketed by line, and in particular `DIVERGENCE_NOTE`, which is a runtime user-facing string rather than a comment.
    - src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs — its header (what each of the four fixture groups asserts and why a generator rather than twelve hand-committed blobs), the determinism contract stated there, and the one call site that reaches the deleted project module. Measured at plan time: the module-level import is at `:63` and the single call is at `:860`, which writes `project.regen2000proj`.
    - src/mcp/vice/anno-project.ts — `synthesizeProject()` only, and only far enough to reproduce its output exactly: it gzips the payload, base64s it, and emits `{origin, raw_data_base64, blocks: [], settings: {use_illegal_opcodes: true, system}}` as `JSON.stringify` with no whitespace, no timestamp and no host-dependent value. READ IT BEFORE IT IS DELETED — after this commit the only remaining record of the format is the twelve committed files themselves.
    - src/mcp/vice/anno-memmap-render.ts — read it with `grep -a` or `sed -n`, NEVER a plain `grep`; the NUL byte at offset 12862 is what hid this file's import from the first sweep. Expect to find NO import of a deleted module: plan 29-12 removed it at wave 6 while rebuilding `renderMemoryMap()` onto the store (D-17). This plan carries the file as a safety net only. If the import is still present, the correct reading is that 29-12's criterion did not hold — fix it here and say so in the summary. Do NOT treat this as a line-only edit if you do find it: the import is USED at `:106`, and removing it without re-pointing the three calls at `:353-355` breaks the verb.
    - src/mcp/vice/anno-derivation.test.ts — the upstream-integrity half that plan 29-08 was required to leave unchanged, and the assertion inside it that compares the manifest's `curated` disposition against the retired curated set. That comparison is this file's only use of the deleted module, and it is a real use, not a stray import.
    - src/mcp/vice/anno-tools.ts — `CURATED_ANNO_TOOLS`, the surviving set the derivation guard's integrity half is re-pointed onto, and the name mapping plan 29-08 added, which is what makes the re-point meaning-preserving rather than a rename.
    - .planning/phases/29-the-mcp-surface/29-05-SUMMARY.md — the recorded 25-row reconciliation and its per-mention split for the two mixed files, so this plan discharges exactly the entries that cite it.
  </read_first>
  <action>
Derive the deletion set from the registry, entry by entry: every entry the registry
verdicts as glue, plus the two whose own notes say the verdict must not be read as a
claim of survival. Confirm the count before removing anything; if it disagrees with
what the registry says, stop and report, because the registry is the authority.

Before deleting the verify module, copy its two pinned transcripts into
`.planning/phases/29-the-mcp-surface/fixtures/` and write a README beside them
recording three things: what each transcript is (the honest pass, and the trap whose
aggregate line reads as a full pass while the assembler never ran), where it came
from, and Phase 30's obligation to re-record both from real assembler output rather
than inherit these. These are the **shape** Phase 30 must reproduce, not the content
it may reuse; say so in the README, or a later reader will treat them as fixtures to
assert against. They live under the planning tree deliberately: that prefix is
outside the removal gate's scope by construction, so carrying them forward needs no
exemption.

Delete the glue modules and their co-located tests. Delete
`anno-symbol-roundtrip.test.ts` as an explicit recorded choice — name it, rather
than calling it "the round-trip test", because D-11 requires this deletion be a
recorded choice and a choice recorded only by description is not one. Its subject
is withdrawn, the withdrawal is already in the project record, and this deletion
discharges that decision rather than making a new one. It is not the symbols
module's co-located test, which is why plan 29-05 deliberately did not sweep it up.
Record the choice in the summary in those terms.

**D-01's second clause, discharged here rather than left standing.** The owner's
directive has two halves, and the phase has so far honoured only the first: *the
usage is removed, and if it cannot be removed right now everything about it must
be disabled and **never be included in any tests***. Deleting the glue satisfies
the first half. It does not satisfy the second, because four surviving test files
still carry live, environment-gated blocks whose subject is a real child process
this plan is deleting — and one of them asserts the availability of that child
outright, ungated, so it runs on every suite invocation. A gated test whose
subject no longer exists is still a test that includes it, and it fails at load
time regardless, because its imports are gone.

So, in the same commit as the deletion, and for each surviving file this task's
`<files>` lists:

- Remove every import naming a module this plan deletes, and remove the test
  bodies that exist only to exercise it. Where a whole gated block's only subject
  was the deleted child, the block goes — do not leave it skipped, do not leave it
  behind an environment variable, and do not convert it to a `todo`. Any of those
  leaves it "included in tests", which is the exact wording the directive rules
  out.
- The memmap-render test's ungated availability-gate assertion is deleted
  outright, and the fact of its deletion — with D-01 and D-11 both named as its
  authority — goes in the summary. **This is now the ONLY part of that file this
  plan removes.** Its three `gated:` render tests are no longer deleted here:
  D-17 gave them a substrate, so plan 29-12 converted them at wave 6 into ungated
  tests over a real Phase 28 store, keeping their assertions and dropping only the
  child. D-01 asks for the dependency to go, not the coverage. Its file's **unit**
  half (the provenance sidecar schema, the cell escaping, the renderer version)
  has no such dependency and stays exactly as it is; keep that half and say in the
  file's header that the
  gated half is gone and why, so the next reader does not look for it.
- The spawn-seam guard is the delicate one: its subject is the **discipline** that
  no shipped module spawns a child touching the emulator, and that discipline
  outlives its substrate. Re-point its module-set derivation and its planted-source
  string literals onto a subject that still exists so the plant still reddens.
  A guard whose planted violation stops reddening has not been re-pointed — this
  phase's standing prohibition — so re-run its plant and record the output.
- Where removing a block would empty a file entirely, say so and stop rather than
  deleting the file: this task's deletion set comes from the registry, and a test
  file emptied as a side effect is not in it.

**The five importers the narrow scan could not see, each with a decided fate.**
None of these is a `src/mcp/vice/*.test.ts` file, which is why the first
measurement missed all five. Four of them are invisible to at least one of the
three mechanisms that would otherwise make the miss loud, so each needs a fate
written down rather than left to typecheck to discover:

- **The coverage-fixture generator** (`fixtures/coverage/make-coverage-fixtures.mjs`)
  is invisible to all three: it is `.mjs`, so `tsconfig.json`'s `**/*.ts` /
  `**/*.mts` include never compiles it; it carries no test suffix by deliberate
  design (its own header explains that a `*.test.mjs` here would register an
  uncovered suite directory), so `node --test '*.test.*'` never loads it; and the
  removal gate greps for the WORD, which this file never uses — it names only the
  path. **Its fate is: retire the dependency, freeze the emitted artifact, keep
  the generator.** Replace the deleted module's import and its single call with a
  module-private writer inside this file that emits the same JSON the deleted
  function emitted — gzip the payload, base64 it, and write `origin`,
  `raw_data_base64`, an empty `blocks` array and the two forced settings, in that
  key order, stringified with no whitespace. **Do NOT re-point this generator onto
  the Phase 28 store.** The twelve fixtures' consumers read the analyser's project
  file, so re-pointing would re-derive all twelve committed fixtures and change
  what the census controls measure; that is Phase 30's work, on Phase 30's
  evidence, and the registry entry for the deleted module already says why ("once
  the store is this project's own, there is no such file to synthesise"). Keep
  every one of the generator's checked negatives — the equal-length and
  identical-tail throws, `assertDispatchesNowhere()`, `assertNoIndirectJumpOpcode()`
  and `assertCarriesPushIdiom()`. Those throws, not the project file, are the
  reason this is a generator, and the fixtures stay reproducible because of them.
  Record the freeze in the file's header: what left, that the twelve fixtures are
  now the only record of the format, and that Phase 30 re-points the writer when
  the store-native project file lands.
- **The skill-coverage script** (`scripts/check-skill-tool-coverage.mjs`) is `.mjs`
  too, so typecheck and the test runner are both blind to it and only RUNNING it
  catches the dangling import. Plan 29-09 Task 3 re-points it at wave 5 and
  asserts the retired import is gone. It is named here as a **safety net, not a
  second owner**: expect to find nothing to do, and if the retired import is still
  present, the correct reading is that plan 29-09's criterion did not hold — fix it
  here and say so in the summary rather than treating it as new work.
- **The memmap-render module** (`anno-memmap-render.ts`) imported the deleted
  runner and **called it** — three times, from `renderMemoryMap()`. The first
  sweep recorded that import as unused because a plain `grep` cannot read this
  file (NUL byte at offset 12862). D-17 records the correction and the owner's
  choice: plan 29-12 rebuilds the verb onto the Phase 28 store at wave 6, so by
  the time this plan runs the import is already gone and this file needs **no
  edit at all**. Expect a clean `git diff` on it. It is carried here as a safety
  net, exactly as `check-skill-tool-coverage.mjs` is. The module keeps its
  renderer, its provenance-sidecar schema and its store-only address-row rule.
  **The comment at `:79` stays**, the
  measurement-provenance citation recording that this renderer's three query result
  shapes were measured live against a real `external-analyser-core-0.9.20` child. That
  is a **permanent** gate exemption pinned at exactly 1 (plan 29-05 row 11; authored
  at wave 1 by plan 29-02, re-pointed onto this path by 29-05 in the `git mv`
  commit), so no plan discharges it and this one must not "tidy" it away — removing
  it reds the gate's exact-count assertion for that exemption class. Note that the
  import line being deleted does not itself contain the needle, so this edit is
  count-neutral and half 2 of 29-05's ownership rule is satisfied by construction.
- **The derivation guard** (`anno-derivation.test.ts`) is the one real re-point in
  this group. Its upstream-integrity half asserts that the Phase 19 manifest's
  `curated` disposition agrees with the retired curated set — plan 29-08 was
  explicitly required to leave that half unchanged, which is correct for wave 4 and
  is exactly why the repair lands here instead. Re-point that assertion onto the
  surviving curated set, carrying the upstream-to-surface name mapping plan 29-08
  added so the comparison keeps its meaning: it must still fail when the manifest
  and the surface disagree. Do not weaken it to a presence check, and do not delete
  it — a guard removed because its subject was renamed is the drift it exists to
  catch. Re-run it with one manifest disposition flipped, observe it red, revert.
- **The stock-dispatch guard** (`stock-dispatch.test.ts`) has its retired import
  removed at wave 1 by plan 29-01, which asserts its absence there. It is named
  here as a safety net on the same terms as the skill-coverage script.

Confirm afterwards that `MANUAL_ONLY_TESTS` in `test-gate.mjs` still names the
same nine files; measured at plan time it names none of this plan's deletions, so
a change there means something was removed that should not have been.

Remove from the two surviving capability modules exactly the functions that reached
the deleted glue, and nothing else. Keep the label-file format knowledge, the
naming vocabulary and the enum-generation heuristics — that is what Phase 30
rebuilds around, and re-deriving it there would be the loss the classification
registry exists to prevent. Rewrite each module's header to say plainly what left
and what stayed, and to name Phase 30 as where the route returns.

Update every deleted entry in the registry to the discharged scope with a deleted
fate carrying a reason, so the registry's discharge-closure relation can check each
one against disk. Remove every file-list entry naming a deleted module **in this
same commit** — an entry naming a file that is not on disk makes the packaging check
fail, which is that gate working, and npm would otherwise ignore it silently.
  </action>
  <verify>
    <automated>cd src/mcp/vice && npm run typecheck && npm run test:automated && cd ../../.. && node scripts/check-npm-packages.mjs && node scripts/audit-gate.mjs</automated>
  </verify>
  <acceptance_criteria>
    - None of the deleted modules or their tests exists on disk, and each one's registry entry carries a discharged scope with a deleted fate and a reason.
    - `cd src/mcp/vice && node --test module-classification.test.ts` exits 0, including the discharge-closure relation over both fate kinds.
    - `.planning/phases/29-the-mcp-surface/fixtures/` contains both transcripts and a README stating their provenance and Phase 30's obligation to re-record them.
    - `src/mcp/vice/anno-symbols.ts` and `src/mcp/vice/anno-enum-gen.ts` import no deleted module, type-check, and each carries a header paragraph naming what left, what stayed and where the route returns.
    - `src/mcp/vice/package.json` `files[]` names no deleted file.
    - `src/mcp/vice/anno-symbol-roundtrip.test.ts` does not exist, and `29-10-SUMMARY.md` names that exact path as a recorded choice citing D-11, not as "the round-trip test".
    - `grep -arn` over `src/mcp/vice/*.test.ts` finds no import of any module this plan deleted — `-a` per the phase-level binary-safety note in plan 29-02's Task 2, so a NUL byte in a test file cannot turn an unread file into an apparent pass — and no test file contains an availability-gate assertion or an environment-gated block whose subject is the deleted child. A skipped block counts as present and fails this criterion.
    - **The tree-wide dangling-import sweep, both arms, over EVERY tracked file — not over the rename set and not over `src/mcp/vice/*.test.ts`.** Neither the typecheck (`tsconfig.json` compiles only `**/*.ts` and `**/*.mts`) nor the removal gate (it greps for the word, not the path) reaches `.mjs`, so this criterion is the only mechanism that sees the whole tree. Define the scan once and run it twice:
      - **Arm 1, the assertion.** With `DEAD='anno-(launch|mcp-client|project|session|tools|test-gate|verify|symbol-roundtrip)\.(ts|mts|mjs|js)'`, the command `git ls-files -z ':!.planning/' | xargs -0 grep -lE "(from|require\(|import\()[[:space:]]*[\"'][^\"']*${DEAD}[\"']"` prints **nothing** and the pipeline's `grep` exits non-zero (no matches). Measured at plan time on the pre-deletion tree this same command prints **25** paths — 11 of this plan's own deletions and the 14 survivors named in `<read_first>` — so it is known to be capable of printing, which is what makes an empty result evidence rather than silence.
      - **Arm 2, the non-vacuity control, run in the SAME shell.** The identical command with `DEAD` replaced by `anno-store\.(ts|mts|mjs|js)` prints **at least one** path. A zero here means the scanner stopped reaching the tree — a broken pathspec, a broken quote, an empty `git ls-files` — and Arm 1's emptiness proves nothing. **If Arm 2 is empty, this criterion FAILS even when Arm 1 is empty.** Record both arms' output in the summary; an Arm 1 result reported without its Arm 2 count is not this criterion being met.
    - `src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs` imports no deleted module, and running `cd src/mcp/vice && node fixtures/coverage/make-coverage-fixtures.mjs` twice leaves `git status --porcelain src/mcp/vice/fixtures/coverage` **empty** — the generator's own determinism contract, which here does double duty as the proof that the inlined writer reproduces the deleted function byte for byte. A single changed byte in any of the twelve `project.regen2000proj` files fails this.
    - `src/mcp/vice/anno-memmap-render.ts` imports no deleted module, and `git diff` on it in this commit is **EMPTY** — plan 29-12 already discharged the import at wave 6 as part of the D-17 rebuild, so there is nothing left here to remove. This plan's standing constraint on the files whose imports it *does* remove — that `git diff` on each shows the removed import line and nothing else — therefore does not reach this file, because this plan removes no import from it. The constraint is quoted here rather than cited by line on purpose: this phase has been bitten three times by line references that drifted under their own commits. The permanent exemption comment at `:79` is present and `grep -ac` on the file reports `1`, the count 29-05 row 11 pinned; `-a` is required because the file's NUL byte at offset 12862 makes a plain `grep -c` print nothing at all. A NON-empty diff here means 29-12's criterion did not hold — fix it and record that in the summary rather than accepting it silently.
    - `src/mcp/vice/anno-derivation.test.ts`'s upstream-integrity half still compares the manifest's `curated` disposition against a live curated set and still fails when they disagree — observed red with one disposition flipped, then reverted, with the output recorded in the summary. A version that only checks presence, or that was deleted, fails this criterion.
    - `src/mcp/vice/anno-memmap-render.test.ts` retains its provenance-sidecar and cell-escaping unit tests AND the three render tests plan 29-12 converted to ungated store-backed tests at wave 6, contains no reference to the deleted child, and its header records that the availability-gate assertion was removed under D-01 with D-11 named and that the gated render half was converted rather than deleted under D-17. A version of this file with the three render tests DELETED fails this criterion — D-01 required the child to go, not the assertions.
    - `src/mcp/vice/spawn-seam.test.ts`'s planted violation was re-run after the re-point, observed non-zero, reverted, and its output recorded in the summary.
    - `src/mcp/vice/test-gate.mjs`'s `MANUAL_ONLY_TESTS` still names the same nine files as before this plan.
    - `node scripts/check-npm-packages.mjs` exits 0.
    - `cd src/mcp/vice && npm run test:automated` exits 0 and `npm run typecheck` exits 0.
    - `cd src/mcp/vice && node --test test-gate.test.ts` exits 0 (the manual-only set is still the exact declared nine).
  </acceptance_criteria>
  <done>The glue is gone, no test anywhere still includes its subject, the two capability modules kept their knowledge, the transcripts rode forward, and the packaging gate proves the file list matches disk.</done>
  <reversibility rating="one-way">Gated by Task 1.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Remove the proxy's last two references, and correct the line citations in the same commit</name>
  <files>src/mcp/vice/vice-proxy.ts, CLAUDE.md, src/mcp/vice/docs-linerefs.test.ts</files>
  <read_first>
    - src/mcp/vice/vice-proxy.ts — the session-close import near the top of the import block, and the recycle region that calls it, together with the comment block above that call explaining why the region also closes a live session.
    - src/mcp/vice/docs-linerefs.test.ts:37-99 — how it reads the project instructions' bullet, extracts every cited proxy line number, and asserts each cited line still contains the expected call or a function keyword.
    - ./CLAUDE.md — the derived-tool path-translation bullet, both of its cited line numbers, and the sentence stating that these citations drift between phases and that a mismatch is drift to re-verify.
    - .planning/phases/29-the-mcp-surface/29-RESEARCH.md — Pitfall 4, which is exactly this hazard.
  </read_first>
  <action>
Remove the session-close import and the call site in the recycle region, together
with the comment block that exists only to explain that call. Do not pad the file
with filler comments to preserve the line count — padding hides the drift rather
than recording it, and the project instructions already say what to do instead.

Then, in this same commit, re-measure both cited line numbers in the project
instructions against the file as it now stands and correct them. Re-run the guard
that checks them. If either cited line no longer lands on what the guard expects,
the citation is wrong, not the constraint — re-measure and fix the citation, exactly
as the instructions say.

Record in the summary the before and after line numbers for both citations, so the
next phase's re-verification has a starting point rather than a search.
  </action>
  <verify>
    <automated>cd src/mcp/vice && node --test docs-linerefs.test.ts docs-dangling-refs.test.ts vice-proxy.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/mcp/vice/vice-proxy.ts` contains no import from and no call into any deleted module.
    - `CLAUDE.md`'s derived-tool bullet cites two proxy line numbers, and each cited line in the current file contains what the guard expects.
    - `cd src/mcp/vice && node --test docs-linerefs.test.ts` exits 0.
    - `29-10-SUMMARY.md` records the before and after values of both citations.
    - `cd src/mcp/vice && node --test vice-proxy.test.ts` exits 0 or is recorded as matching its pre-phase baseline status from `29-BASELINE.md`.
  </acceptance_criteria>
  <done>The proxy carries no reference to the retired integration, and the line citations that drifted because of it were corrected in the same change.</done>
</task>

<task type="auto">
  <name>Task 4: Empty the temporary allow-list and prove the gate is green without it</name>
  <files>scripts/check-no-analyser.mjs</files>
  <read_first>
    - scripts/check-no-analyser.mjs — the temporary allow-list block, its dated header, and the assertion that every entry names a file on disk and a plan of this phase.
    - .planning/phases/29-the-mcp-surface/29-02-SUMMARY.md — the seven-step transcript, so the plants can be re-run against the post-deletion tree rather than only the pre-deletion one.
    - .planning/phases/29-the-mcp-surface/29-BASELINE.md — the recorded failing-file set to compare the full sweep against.
  </read_first>
  <action>
Remove from the temporary allow-list every entry this plan discharged, leaving only
those plan 29-11 owns. Run the gate and confirm it is green over the tree without
them — this is the first moment the gate is doing its real job rather than being
held off, and it is the point of having built it first.

Work from plan 29-05's recorded reconciliation rather than from the allow-list
alone, because the two must agree. Every entry citing **this** plan names a file
this plan carries, by 29-05's reachability rule, and Task 2 has already removed or
rewritten the text each entry covered. Two of them are not deletions of comment
prose and must be checked by reading, not by counting: `anno-coverage.ts`'s
divergence note is a **runtime, user-facing string** that Task 2 rewrites rather
than removes, so confirm the rewrite names no deleted producer and no tool surface
that no longer exists; and `anno-coverage.ts`'s remaining two mentions are
**permanent** — the rejected-circular-design paragraph that is the reason the module
has its shape, and a verbatim past-defect reproduction — so that file keeps a
permanent exemption at the lowered count and must NOT have its entry removed
wholesale. Lowering a permanent exemption's count to zero because a temporary entry
on the same file was discharged is the widening this phase's standing prohibition
forbids, one file at a time.

Re-run two of the plants against the post-deletion tree: one in a surviving module
and one in a shipped skill file, each observed non-zero and reverted. The plants
were proven against the pre-deletion tree in plan 29-02; re-running two of them here
proves the gate did not become vacuous when its subject disappeared, which is the
specific failure mode a removal gate has.

Then run the full sweep and compare against the recorded baseline: the whole-glob
suite with the broker stopped and in the background, the automated set, the
typecheck, and all five CI scripts. Compare the failing-file **set** against
`29-BASELINE.md`'s recorded set. A name that disappeared is an improvement to
explain; a name that appeared is a regression to fix before this plan closes.
  </action>
  <verify>
    <automated>node scripts/check-no-analyser.mjs && node scripts/check-skill-tool-coverage.mjs && node scripts/check-skill-fork-honesty.mjs && node scripts/check-npm-packages.mjs && node scripts/audit-gate.mjs && cd src/mcp/vice && npm run test:automated && npm run typecheck</automated>
    <human-check>The full-glob suite was run with the broker stopped, in the background, and its failing-file set compared against `29-BASELINE.md`'s recorded set; every difference is explained in `29-10-SUMMARY.md`. Two plants were re-run against the post-deletion tree, each observed non-zero and reverted, with the output recorded.</human-check>
  </verify>
  <acceptance_criteria>
    - `scripts/check-no-analyser.mjs`'s temporary allow-list contains only entries naming plan 29-11.
    - No temporary allow-list entry names this plan; each of the ones 29-05 and 29-07 left citing it is gone, and the summary lists them by file with the before and after counts.
    - `src/mcp/vice/anno-coverage.ts` still carries a permanent exemption at its re-measured count, and the summary states which of its mentions survive and why — a file whose permanent exemption disappeared alongside its temporary entry fails this criterion.
    - `node scripts/check-no-analyser.mjs` exits 0.
    - All five CI scripts exit 0.
    - `cd src/mcp/vice && npm run test:automated` and `npm run typecheck` exit 0.
    - `29-10-SUMMARY.md` records the post-deletion plant outputs and the full-glob failing-file set diff against the baseline, with every difference explained.
    - `git status --porcelain` is empty after the plants are reverted.
  </acceptance_criteria>
  <done>The gate is green over a tree with no allow-list for the deleted subject, still bites when planted against, and the suite matches its recorded baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the registry → the deletion set | A deletion driven by a name rather than by the record is the failure D-03 exists against |
| the published tarball → users | A file list naming a deleted file, or a shipped skill pointing nowhere, reaches users directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-41 | Repudiation | a removal gate that becomes vacuous once its subject is gone | high | mitigate | Two plants are re-run against the post-deletion tree and observed non-zero, not merely against the pre-deletion tree. |
| T-29-42 | Repudiation | a capability lost as a side effect rather than by decision | high | mitigate | The deletion set is derived from the registry entry by entry; the round-trip test's deletion discharges an already-recorded withdrawal; the transcripts the registry names as must-survive are moved forward before their module goes. |
| T-29-43 | Tampering | a stale file-list entry shipping or failing silently | medium | mitigate | Entries are removed in the same commit and the packaging check is an acceptance criterion; npm ignores a stale entry, so an unproven packaging gate is a vacuous one. |
| T-29-44 | Repudiation | a documentation citation silently drifting past its subject | medium | mitigate | Both cited line numbers are re-measured and corrected in the same commit as the change that moved them, and the guard that checks them is an acceptance criterion. |
| T-29-46 | Repudiation | a test left gated-off rather than removed, so its subject is still "included in tests" | high | mitigate | D-01's second clause is discharged explicitly: every gated block whose subject this plan deletes is removed, not skipped and not converted to a `todo`, and the acceptance criterion counts a skipped block as present. |
| T-29-47 | Denial of Service | a surviving file importing a deleted module | high | mitigate | Importers are measured **tree-wide** over `git ls-files` minus `.planning/`, not over `src/mcp/vice/*.test.ts`; all 14 survivors that scan finds are named in this plan's `files_modified` and edited in the deletion commit. Because typecheck compiles only `**/*.ts` / `**/*.mts` and the removal gate greps the word rather than the path, **neither reaches `.mjs`**, so the listing is backed by a two-arm tree-wide sweep whose non-vacuity control must print before its emptiness assertion counts. |
| T-29-48 | Repudiation | a mitigation whose stated text is broader than what the plan does | high | mitigate | T-29-47's earlier text claimed every measured importer was listed while the measurement behind it was scoped to one directory; the claim is now scoped to the scan that backs it, and the scan is an acceptance criterion with a non-vacuity arm rather than a plan-time assertion. A mitigation that is false is worse than one that is absent. |
| T-29-45 | Denial of Service | a phase closing over an already-red guard | high | mitigate | The full sweep is compared against a recorded failing-file set rather than against zero, and every difference must be explained before the plan closes — the `4f048bb` failure mode, closed by construction. |
| T-29-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed by this plan; one external binary stops being required, which removes a dependency rather than adding one. |
</threat_model>

<verification>
- `cd src/mcp/vice && npm run test:automated` and `npm run typecheck` — green.
- `node scripts/check-no-analyser.mjs`, `check-skill-tool-coverage.mjs`, `check-skill-fork-honesty.mjs`, `check-npm-packages.mjs`, `audit-gate.mjs` — all green.
- Full-glob suite, broker stopped, compared against `29-BASELINE.md`'s failing-file set with every difference explained.
- Two plants re-run against the post-deletion tree, observed non-zero, reverted.
</verification>

<success_criteria>
- The glue is deleted, driven by the registry and recorded in it.
- The knowledge the registry names as must-survive survived: two capability modules keep theirs, and the two transcripts rode forward.
- No guard is red, no citation is stale, and the removal gate is green without a single allow-list entry for the deleted subject.
</success_criteria>

## Artifacts this phase produces

New files created by **this plan**:

- `.planning/phases/29-the-mcp-surface/fixtures/` — the two carried-forward transcripts and their README
- New registry values: the `deleted` fate kind populated for every glue entry

Everything else this plan does is a removal or a correction; the phase-wide artifact
roll-up is in `29-08-PLAN.md`.

<output>
Create `.planning/phases/29-the-mcp-surface/29-10-SUMMARY.md` when done.
</output>
