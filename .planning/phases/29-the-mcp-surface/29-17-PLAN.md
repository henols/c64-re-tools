---
phase: 29-the-mcp-surface
plan: 17
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [CUT-01]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CUT-01's sizing sentence names the MEASURED figures -- 26,023 lines pre-phase, 10,066 net removed, 15,957 surviving -- and no longer the three falsified ones"
    - "CUT-01's row and checkbox move to Complete in the SAME edit that rewrites the sentence they score; the row is never adjusted while the sentence stands"
    - "The narrative paragraph explaining why CUT-01 is Partial does not survive the flip asserting the opposite; it is replaced by the owner's dated decision"
    - "CUT-01 probe/empty: the corrected sentence carries its own provenance -- which two commits, which file set, which counting command -- so a later reader can re-derive every figure rather than take it on trust"
    - statement: "CUT-01 probe/encoding: 'lines' has ONE definition across both measurements -- the same file-selection predicate and the same counting command run at both commits -- and that definition is stated in the record rather than implied"
      verification: backstop
    - "The record repair is scoped: CUT-01's row is the only status that moves, and MCP-04's stale Complete in the per-phase status table is corrected DOWN to agree with the verification, never up"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "A CUT-01 entry whose checkbox, traceability row, sizing sentence and narrative paragraph all agree with each other and with the measurement, plus a per-phase status table that agrees with the traceability table"
      contains: "26,023"
  key_links:
    - from: ".planning/REQUIREMENTS.md (CUT-01 checkbox and sizing sentence)"
      to: ".planning/REQUIREMENTS.md (CUT-01 traceability row)"
      via: "one edit moves both, so the row can never be adjusted without the sentence it scores"
      pattern: "CUT-01"
    - from: ".planning/REQUIREMENTS.md (CUT-01 sizing sentence)"
      to: "the git history at 8f21d77 and at HEAD"
      via: "a stated provenance -- the two commits, the file-selection predicate and the counting command -- so every figure is re-derivable"
      pattern: "8f21d77"
  prohibitions:
    - statement: "Never move a requirement's status row without changing the sentence that row scores. A row and its sentence disagreeing is not a bookkeeping lag; it is a claim the record makes twice and answers differently, and it is the specific failure the audit gate exists against."
---

<objective>
Close verification gap 5 — `CUT-01`'s three falsified sizing figures — by
implementing the owner's decision of **2026-08-30**.

The verifier measured the requirement for the first time, which is the
measurement `29-10-SUMMARY.md` and `REQUIREMENTS.md` both said nobody had taken.
The substantive claim holds and was independently confirmed: zero `anno-*.ts`
files remain under `src/mcp/vice/`, and the removal gate is green tree-wide with
an **empty** temporary allow-list. But all three stated figures are wrong, in the
same direction:

| Figure | Stated in `CUT-01` | Measured |
|---|---|---|
| Pre-phase surface | 25,759 lines (10,102 non-test + 15,657 test) | **26,023 lines / 35 files** (10,035 non-test + 15,988 test) at `8f21d77` |
| Net removed | ~12.4k | **10,066** |
| Surviving under new names | ~12.9k | **15,957** at HEAD |

The verification's `human_verification` item asked the owner for one of two
things: correct the text to the measured figures and mark it Complete, or accept
the shortfall and leave it Partial with a recorded reason. **The owner decided on
2026-08-30: correct the text, then mark it Complete.**

This is a **record-of-intent** edit, already decided, so it carries no fresh
checkpoint. What it must not become is the failure the verifier warned about in
the same sentence: *do not adjust the row without changing the sentence — that is
the failure the audit gate exists against.*

Purpose: `CUT-01` is the sixth of the six truths the phase was scored on, and it
is the only one that needs no code.
Output: a `CUT-01` entry whose four places — checkbox, sizing sentence,
traceability row and narrative paragraph — all say the same thing, and say it
with provenance a later reader can re-derive.

**Scope, stated so nothing is silently in or out.** This plan edits exactly one
file and moves exactly one requirement status UP. `MCP-04`, `REPOINT-01` and
`REPOINT-02` stay `Gaps Found`: they are closed by plans 29-13 through 29-16,
and per this repo's standing rule a row moves up only on a verification verdict,
never on an executor's reading of its own work. Task 2's one correction moves a
status DOWN, into agreement with the verification that already scored it.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-the-mcp-surface/29-VERIFICATION.md
@.planning/phases/29-the-mcp-surface/29-10-SUMMARY.md
@.planning/phases/29-the-mcp-surface/29-11-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive the figures, then rewrite the sentence and move the row in ONE edit</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` line 128 — `CUT-01`'s checkbox and its full sizing sentence, including the trailing clause about correcting v0.6.0's figures. That trailing clause is part of the claim and moves with the rest.
    - `.planning/REQUIREMENTS.md` line 238 — the traceability row.
    - `.planning/REQUIREMENTS.md` line 249 — the narrative paragraph beginning with the assertion that `CUT-01` is `Partial` and the reason is its own evidence. Read it in full: it quotes `29-10-SUMMARY.md` and states that a verification pass re-measuring the two figures is what moves the row. That pass has now happened, which is why this paragraph must be rewritten rather than left standing beside a `Complete` row.
    - `.planning/phases/29-the-mcp-surface/29-VERIFICATION.md` — the `CUT-01` gap entry in the YAML `gaps:` block, truth 6 in the Observable Truths table, the `human_verification` item, and the `CUT-01` row in Requirements Coverage. These four places carry the measurement and the exact figures; the record must agree with them.
    - `.planning/phases/29-the-mcp-surface/29-11-SUMMARY.md` — how plan 29-11 worded the `Partial` record. This edit supersedes that wording, and the summary should say so rather than leaving two records that disagree.
    - `.planning/REQUIREMENTS.md` lines 245-252 — the surrounding record-discipline paragraphs, so the new paragraph is written in the same voice: a status is stated with the evidence that authorises it and never as a self-assessment.
  </read_first>
  <action>
    FIRST, re-derive rather than transcribe. The three figures are an owner
    decision about intended scope, but their PROVENANCE is a measurement, and a
    record whose numbers cannot be re-derived is the condition this whole gap is
    about. From a clean tree, measure both ends:

    - The pre-phase surface at commit `8f21d77`: the set of files under
      `src/mcp/vice/` matching the retired module prefix, and their total line
      count, split into test and non-test. Read the blobs at that commit rather
      than the working tree.
    - The surviving set at HEAD: the descendants of that surface under their new
      names, which the verification enumerates as the annotation-family modules
      plus the three renamed test files it names individually.

    Record the exact commands and their exact output in the summary. If a figure
    you derive differs from the verification's, **do not substitute your own
    number silently** — the owner decided on those specific figures. Record the
    discrepancy in the summary, use the verifier's figure in the requirement
    text, and note the divergence so the next reader can adjudicate it. A
    measurement that disagrees with the record is a finding, not a correction to
    apply on the spot.

    THEN make the edit — checkbox, sentence and row together, in one change:

    1. Rewrite `CUT-01`'s sizing sentence to the measured figures: a pre-phase
       surface of 26,023 lines across 35 files, split into its non-test and test
       halves; 10,066 lines net removed; 15,957 lines surviving under new names.
       Keep the sentence's SHAPE — it is still a sizing claim about the same
       subject — and keep its trailing clause about superseding v0.6.0's
       19,181, restating it against the new numbers.

    2. Append the provenance INTO that same entry, not into a footnote: name the
       two commits measured (`8f21d77` for the pre-phase end and the phase's
       HEAD for the surviving end), the file-selection predicate used at each,
       and the counting command. State that both ends were counted the same way,
       so "lines" has one definition across the subtraction rather than two that
       happen to be compared. This is the half that makes the figures
       re-derivable instead of merely stated.

    3. Flip the checkbox to checked in the same edit.

    4. Move the traceability row from `Partial` to `Complete`.

    5. Replace the narrative paragraph. It currently explains why the row is
       `Partial` and states that a verification pass re-measuring the figures is
       what moves it. Write in its place a paragraph that records: that the pass
       happened and what it measured; that all three original figures were
       falsified in the same direction; that the OWNER decided on **2026-08-30**
       to correct the text and mark the requirement Complete rather than accept
       the shortfall and hold it Partial; and that the substantive claim was
       independently verified — zero `anno-*.ts` under `src/mcp/vice/`, the
       removal gate green tree-wide with an empty temporary allow-list. Say
       plainly that the earlier paragraph's reasoning was correct at the time and
       is superseded by the measurement, so a reader meeting both in the history
       can tell which one is live.

    Use scoped edits. Do NOT rewrite `.planning/REQUIREMENTS.md` wholesale: it
    carries twenty-eight requirements and several screens of record-discipline
    prose that nothing here touches, and a whole-file rewrite destroys every
    entry outside the diff window.
  </action>
  <verify>
    <automated>grep -c '26,023' .planning/REQUIREMENTS.md && grep -c '10,066' .planning/REQUIREMENTS.md && grep -c '15,957' .planning/REQUIREMENTS.md && grep -c '8f21d77' .planning/REQUIREMENTS.md && ! grep -q '12.4k' .planning/REQUIREMENTS.md && node scripts/audit-gate.mjs</automated>
    Expected: each of the four measured/provenance tokens present at least once, the falsified figure absent, and the audit gate exit 0.
  </verify>
  <acceptance_criteria>
    - `grep -n 'CUT-01' .planning/REQUIREMENTS.md` shows the checkbox checked, the traceability row reading `Complete`, and the narrative paragraph naming the 2026-08-30 owner decision.
    - The sizing sentence contains 26,023, 10,066 and 15,957, and contains none of 25,759, 10,102, 15,657 or the two approximate figures.
    - The entry names both measured commits and the counting command, so every figure is re-derivable from the record alone.
    - `git diff .planning/REQUIREMENTS.md` shows the checkbox, the sentence, the row and the paragraph all changed in this one commit — never the row alone.
    - `node scripts/audit-gate.mjs` exits 0.
    - `cd src/mcp/vice && node --test docs-dangling-refs.test.ts audit-integrity.test.ts` exits 0.
  </acceptance_criteria>
  <reversibility rating="costly">A record-of-intent edit: it changes what the project claims it set out to do, and a later reversal would need the same weight of decision that authorised it. **The one-way half is already discharged** — the owner made this decision on 2026-08-30 in response to the verification's explicit two-option `human_verification` item, so no fresh checkpoint is inserted; implement it directly and cite that decision. The measurement itself is re-derivable from two named commits, so the figures are auditable even if the decision is later revisited.</reversibility>
  <done>`CUT-01`'s checkbox, sizing sentence, traceability row and narrative paragraph all say the same thing, the figures are the measured ones, and every figure names the commit and command it came from.</done>
</task>

<task type="auto">
  <name>Task 2: One record repair in the same file — the per-phase status table must not assert what the verification refuted</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 253-260 — the six-row table introduced as *"This phase's six requirement statuses, each with the summary that evidences it. No status claims more than its evidence, and none is a self-assessment of the plan that wrote this line."* It is presented as current fact, not as a dated snapshot, which is why a stale row in it is a live claim.
    - `.planning/REQUIREMENTS.md` lines 225-243 — the traceability table, which already reads `Gaps Found` for `MCP-04`, `REPOINT-01` and `REPOINT-02`.
    - `.planning/phases/29-the-mcp-surface/29-VERIFICATION.md` — the Requirements Coverage table's `MCP-04` row, scored `✗ BLOCKED`, and its stated reason: CR-01 delivers exactly the plausible-looking zero the requirement names, and CR-06 makes the batch's documented nesting unusable with a message contradicting the tool's own description. Those are the authorising sentences for this correction.
    - `git show e87650e --stat` and its message — the commit that reverted premature `Complete` statuses after the gaps were found. It corrected the traceability table and did not reach this second table, which is why the two disagree today.
  </read_first>
  <action>
    The six-row per-phase status table still reads `MCP-04 | Complete`, citing
    two summaries as evidence for explicit addressing, idempotent edits, the
    depth-capped batch and refusal by name — while the traceability table
    eighteen lines above reads `Gaps Found` for the same requirement, and the
    verification scored it BLOCKED on two independently reproduced defects. The
    revert commit corrected one table and missed this one.

    Correct the `MCP-04` row DOWN to agree with the traceability table and the
    verification. Replace its evidence cell with the verification's own reason
    rather than a paraphrase: which two review findings block it, and that the
    three clauses which DO hold (explicit addressing, idempotence, per-item batch
    status) hold — so the row records a partial truth precisely rather than
    flattening it to a bare status. Name `29-VERIFICATION.md` as the authority,
    in the same voice the paragraphs below the table already use for the Phase 28
    rows.

    Add one sentence under the table recording why the correction was needed: the
    two tables disagreed, one of them was a live claim the verification had
    already refuted, and a record that answers the same question twice with
    different answers is the condition the audit gate exists against. Note that
    the row moves back up only on a re-verification verdict, never on the
    completion of the gap-closure plans that address it.

    **Boundaries, non-negotiable.** Move no other row in either table. Do not
    touch `REPOINT-01` or `REPOINT-02` — the six-row table does not carry them,
    and the traceability table already reads `Gaps Found` for both. Do not move
    any row UP. Do not edit any Phase 28 row, including `STORE-03`, whose
    contradiction is a logged deferred item routed to a Phase 28 pass. Diff the
    file before committing and confirm the change set is exactly `CUT-01`'s four
    places from task 1 plus this one row and its accompanying sentence.
  </action>
  <verify>
    <automated>git diff --stat .planning/REQUIREMENTS.md && grep -n 'MCP-04' .planning/REQUIREMENTS.md && node scripts/audit-gate.mjs</automated>
    Expected: exactly one file changed; both `MCP-04` occurrences agreeing on the same status; the audit gate exit 0.
  </verify>
  <acceptance_criteria>
    - Both `MCP-04` rows in `.planning/REQUIREMENTS.md` state the same status, and that status is the one `29-VERIFICATION.md` scored.
    - The corrected row's evidence cell names CR-01 and CR-06 and names the three clauses that do hold.
    - No requirement status other than `CUT-01` moves up anywhere in the file.
    - No Phase 28 row is edited.
    - `node scripts/audit-gate.mjs` exits 0 and `cd src/mcp/vice && node --test audit-integrity.test.ts docs-dangling-refs.test.ts` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">A single table cell corrected downward into agreement with an existing verification verdict.</reversibility>
  <done>The two tables in `REQUIREMENTS.md` give one answer for `MCP-04`, and that answer is the verification's.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the project record → a future reader or auditor | `REQUIREMENTS.md` is the authority a milestone audit and every later phase reads; a claim here is acted on without re-derivation |
| a measured figure → the claim it supports | Numbers copied without provenance cannot be distinguished from numbers invented |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-17-01 | Repudiation | a status row moved without its sentence | **high** | mitigate | This is the specific failure the verifier named in the same breath as the decision, and the specific failure the audit gate exists against. Mitigated by making the checkbox, the sentence, the row and the paragraph a single edit, with an acceptance criterion that the diff shows all four changed together and never the row alone. |
| T-29-17-02 | Spoofing | figures stated without provenance | **high** | mitigate | A number with no derivation impersonates a measurement. Mitigated by writing the two commits, the file-selection predicate and the counting command INTO the requirement entry, and by re-deriving both ends before the edit rather than transcribing the report. |
| T-29-17-03 | Tampering | a re-derivation quietly overriding the owner's decision | medium | mitigate | If a re-derived figure disagrees with the verification's, the executor records the discrepancy and uses the verifier's figure, because the owner decided on those specific numbers. A disagreement is surfaced as a finding, never applied on the spot. |
| T-29-17-04 | Elevation of Privilege | a gap-closure plan promoting the requirements it is closing | **high** | mitigate | `MCP-04`, `REPOINT-01` and `REPOINT-02` are explicitly out of bounds for upward movement; a row moves up only on a verification verdict. The one status this plan moves up is `CUT-01`, on the owner's recorded decision; the one it moves down is a correction into agreement with a verdict that already exists. Both directions are bounded by acceptance criteria that name the rows. |
| T-29-17-05 | Tampering | a whole-file rewrite of the record | medium | mitigate | Scoped edits only. The file carries twenty-eight requirements and several screens of prose outside this change; a whole-file write destroys every entry outside the diff window. |
| T-29-17-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install; no code changes at all in this plan. |

**ASVS L1 disposition:** every `high` threat is `mitigate`, each paired with an acceptance criterion that makes the mitigation checkable in the diff.
</threat_model>

<verification>
1. `node scripts/audit-gate.mjs` — exit 0.
2. `cd src/mcp/vice && node --test audit-integrity.test.ts docs-dangling-refs.test.ts docs-review-disposition.test.ts` — green. The disposition guard reds on undispositioned review-finding ids; this plan adds no finding ids and must create no new REVIEW-shaped artifact.
3. `cd src/mcp/vice && npm run test:automated` — compare the failing-file SET against `29-BASELINE.md`, never a count against zero.
4. `git diff --stat` — exactly one file changed, `.planning/REQUIREMENTS.md`.
5. This plan touches neither `.planning/STATE.md` nor `.planning/ROADMAP.md`, so the standing per-plan worktree carve-out for those two files does not apply here; `REQUIREMENTS.md` is explicitly unaffected by it.
6. **Wave 1 is `29-13`, `29-14` and `29-17`, and the three `files_modified` sets are disjoint.** This plan owns `.planning/REQUIREMENTS.md` alone; 29-13 owns the `anno-tools` pair; 29-14 owns the `anno-cli` / `anno-memmap-render` / `module-classification` set. Nothing in this round cites `.planning/REQUIREMENTS.md` by line — `src/mcp/vice/module-classification.ts`'s guarded citations point only into `src/mcp/vice/*.ts`, `scripts/*.mjs` and two `SKILL.md` files — so this plan needs no citation maintenance and takes no additional file. Plans 29-15 (wave 2) and 29-16 (wave 3) were moved out of wave 1 because they, like 29-14, must edit `module-classification.ts`; that reshuffle does not touch this plan.
7. Do NOT invoke any tooling that rewrites requirement statuses in bulk. A phase-completion or requirement-revert helper marks every phase requirement at once and would flip the three rows this plan is required to leave alone. Every edit here is a scoped, hand-made replacement, diffed before commit.
</verification>

<success_criteria>
- `CUT-01` reads `Complete` in both its checkbox and its traceability row, and its sizing sentence names 26,023 / 10,066 / 15,957 with the commits and command they were derived from.
- The narrative paragraph explaining the `Partial` status is replaced by one recording the measurement, the falsification, and the owner's 2026-08-30 decision.
- `MCP-04`'s two rows agree, at the status the verification scored.
- No other requirement status moves in either direction.
- The audit gate and the three record guards exit 0, and the automated-arm failing-file set is no worse than the baseline's.
</success_criteria>

<output>
Create `.planning/phases/29-the-mcp-surface/29-17-SUMMARY.md` when done.
Record: the exact commands used to re-derive both ends of the measurement and
their exact output; any discrepancy between a re-derived figure and the
verification's, stated rather than reconciled; the before and after text of
`CUT-01`'s sizing sentence and of its narrative paragraph; the before and after
`MCP-04` row; and the full `git diff --stat`.
</output>

## Artifacts this phase produces

| Kind | Name | File |
|---|---|---|
| record | `CUT-01`'s corrected sizing sentence with its measurement provenance (commits, file-selection predicate, counting command) | `.planning/REQUIREMENTS.md` |
| record | the 2026-08-30 owner-decision paragraph replacing the `Partial` rationale | `.planning/REQUIREMENTS.md` |
| record | the corrected `MCP-04` row in the per-phase status table, plus the sentence recording why the two tables had disagreed | `.planning/REQUIREMENTS.md` |

No code, no symbol, no file path, no CLI flag. This plan changes one tracked
Markdown file and nothing else.
