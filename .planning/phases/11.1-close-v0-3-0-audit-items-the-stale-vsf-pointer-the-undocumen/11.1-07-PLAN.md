---
phase: 11.1-close-v0-3-0-audit-items-the-stale-vsf-pointer-the-undocumen
plan: 07
type: execute
wave: 3
depends_on: ["11.1-01", "11.1-02", "11.1-03", "11.1-04", "11.1-05", "11.1-06"]
files_modified:
  - .planning/todos/completed/2026-08-21-phase-10-and-11-review-residual-dispositions.md
  - .planning/todos/pending/2026-08-21-migrate-hand-copied-acme-gates-to-r2000-test-gate.md
  - .planning/STATE.md
  - .claude/mcp/vice/docs-deferred-ledger.test.ts
  - .claude/mcp/vice/docs-review-disposition.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every finding the v0.3.0 audit listed as having no disposition anywhere -- WR-09, WR-10, WR-11, WR-12 and IN-01 through IN-07 -- has an explicit recorded disposition naming either the plan that fixed it or the reason it is deferred (AUDIT-01, D-11.1-04)"
    - "STATE.md's Deferred Items reflects the todo tree as it actually stands after this phase: no completed todo is listed as Pending, and every pending todo appears (AUDIT-04)"
    - "STATE.md's Operator Next Steps describes what the operator should do now, not v0.2.0-close text (AUDIT-05)"
    - "A committed guard fails if STATE.md's Deferred Items and .planning/todos/pending/ ever disagree again -- the list that stopped growing, held by derivation instead of by memory"
    - "A committed guard fails if ANY REVIEW.md finding id anywhere in .planning/phases/ has no recorded disposition -- AUDIT-01's defect (silence, not error) expressed as an assertion, so this phase's guard-first principle applies to its own closing plan"
  artifacts:
    - path: ".planning/todos/completed/2026-08-21-phase-10-and-11-review-residual-dispositions.md"
      provides: "The disposition ledger: one entry per undispositioned finding, each fixed-by-plan or deferred-with-reason"
      contains: "WR-12"
    - path: ".planning/STATE.md"
      provides: "Accurate Deferred Items and current Operator Next Steps"
      contains: "Deferred Items"
    - path: ".claude/mcp/vice/docs-review-disposition.test.ts"
      provides: "Completeness guard over the disposition ledger: every REVIEW.md finding id must be accounted for somewhere, derived rather than listed"
      contains: "planted-violation"
    - path: ".claude/mcp/vice/docs-deferred-ledger.test.ts"
      provides: "Two-directional derivation guard between STATE.md's Deferred Items and the todo tree, with a planted violation"
      contains: "planted-violation"
  key_links:
    - from: ".claude/mcp/vice/docs-deferred-ledger.test.ts"
      to: ".planning/todos/pending/"
      via: "readdirSync-derived pending set compared against STATE.md's parsed Deferred Items rows"
      pattern: "todos/pending"
---

<objective>
The paperwork, and it lands last on purpose.

**AUDIT-01** is not "four findings are unfixed" — it is that **WR-09, WR-10, WR-11, WR-12 and
IN-01 through IN-07 appear in no todo, no STATE entry and no SUMMARY**. The Phase 10
residual-findings todo enumerated WR-02 through WR-07 without saying why the rest were excluded.
Silence is the defect this closes: by the time this plan runs, plans 11.1-01 through 11.1-06 have
fixed most of them, and the remainder need a stated reason, not an absence.

**AUDIT-04 / AUDIT-05** must land last because **this phase changes what is deferred**
(D-11.1-07). The final deferred set has to be accurate at the moment `/gsd-complete-milestone`
reads it. STATE.md today carries v0.2.0's 13-item inheritance verbatim: two todos listed Pending
are in `.planning/todos/completed/`, and none of v0.3.0's five new pending todos appear. Its
Operator Next Steps still says `R2000-16`'s probe "can run before the milestone is opened" — that
probe ran, and the milestone is nearly closed.

Because a hand-maintained list that stopped growing produced four of this phase's findings, the
deferred set gets a derivation guard rather than a promise.

Purpose: the milestone-close command reads this file. An inaccurate deferred set propagates into
the next milestone's inheritance, which is exactly how v0.2.0's list arrived here stale.

Output: a disposition ledger with no silent exclusions, an accurate STATE.md, and a guard that
keeps it accurate.

**`use_worktrees` is false for this phase** (11.1-CONTEXT, Environment): this plan's deliverables
are `.planning/` content, and worktree mode strips those files from commits, so a worktree run
silently cannot deliver them. Confirm you are on the main checkout before starting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.1-close-v0-3-0-audit-items-the-stale-vsf-pointer-the-undocumen/11.1-CONTEXT.md
@.planning/v0.3.0-MILESTONE-AUDIT.md
@.planning/STATE.md
@.planning/todos/completed/2026-08-20-r2000-review-residual-findings.md
@.planning/ENGINEERING_RULES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The disposition ledger — every undispositioned finding gets an entry, fixed or deferred with a reason</name>
  <files>.planning/todos/completed/2026-08-21-phase-10-and-11-review-residual-dispositions.md, .planning/todos/pending/2026-08-21-migrate-hand-copied-acme-gates-to-r2000-test-gate.md</files>
  <read_first>
    - `.planning/todos/completed/2026-08-20-r2000-review-residual-findings.md` in full — the
      established format for this document class: frontmatter (`created`, `source`, `severity`,
      `resolves_phase`), a per-finding body with file:line and repro, a "Verified CLEAN by the same
      review" section so facts are not re-derived, and a `## Resolution` section naming the closing
      plan per finding. This ledger is its sibling; follow it exactly.
    - `.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-REVIEW.md`
      lines 455-635 — WR-09 through WR-12 and IN-01 through IN-07, each with its exact wording,
      file:line and suggested fix. Quote each finding's own words; never paraphrase a finding into
      something easier to close.
    - `.planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/11-REVIEW.md`
      lines 233-245 — Phase 11's IN-02, dispositioned by plan 11.1-04.
    - `.planning/todos/pending/2026-08-21-migrate-hand-copied-acme-gates-to-r2000-test-gate.md` —
      IN-07's existing home. It already names `r2000-cli.test.ts`'s hand-copied
      `probeR2000()`/`SKIP_REASON` and states why the migration was deferred; its `files:` list is
      missing `r2000-project.test.ts`, which also still carries a copy.
    - The SUMMARY files of plans 11.1-01 through 11.1-06 — every "fixed by plan" claim in this
      ledger must cite a SUMMARY that exists and actually says so. Anything those SUMMARYs reported
      as newly surfaced must appear in this ledger too.
  </read_first>
  <action>
Create `.planning/todos/completed/2026-08-21-phase-10-and-11-review-residual-dispositions.md`.

Body: one entry per finding — WR-09, WR-10, WR-11, WR-12, IN-01, IN-02, IN-03, IN-04, IN-05,
IN-06, IN-07 from `10-REVIEW.md`, plus IN-02 from `11-REVIEW.md`. Label each by its review so the
two IN-02s are distinguishable. Each entry states the finding in its review's own words with its
file:line, then its disposition, which must be exactly one of:

- **fixed** — naming the plan and task (`11.1-0N` Task M) and the SUMMARY that records it;
- **deferred** — naming the todo file it now lives in and the reason, in the register the
  residual-findings todo uses ("deliberately deferred, not overlooked", with the real reason).

Expected mapping as of this plan set — **verify each against the SUMMARYs rather than assuming**,
and correct the ledger if a plan closed something differently: WR-09 → 11.1-06 T1;
WR-10 → 11.1-04 T1; WR-11 → 11.1-05 T1; WR-12 → 11.1-06 T3; IN-01 → 11.1-05 T3;
IN-02 (Phase 10) → 11.1-03 T2; IN-03 → 11.1-05 T2; IN-04 → 11.1-04 T1; IN-05 → 11.1-05 T1;
IN-06 → 11.1-06 T2; IN-02 (Phase 11) → 11.1-04 T3.

**IN-07 is the one deferral.** State it plainly: plan 11-01 created `r2000-test-gate.ts` and six of
the eight r2000/ACME-gated test files now import it; two hand-copied gates remain
(`r2000-cli.test.ts`, `r2000-project.test.ts`); converting them is behaviour-preserving churn on
files whose gate semantics are load-bearing for Phase 11's already-verified criterion-3 evidence
(`11-VERIFICATION.md` cites `r2000-cli.test.ts`'s test 35 by name) — the reason the existing
migration todo already gives. Amend that todo: add `.claude/mcp/vice/r2000-project.test.ts` to its
`files:` list and one line to its "What to do" naming the third copy, so IN-07's home is complete
rather than approximately right.

Add a "Found while dispositioning" section for anything the 11.1 SUMMARYs surfaced that is not one
of the enumerated findings. Two are already known from planning and must appear:

- `stock-cia.ts` and `stock-dispatch.ts` carry header comments handing work to numbered phases that
  have since completed (`is Phase 8's business`, and a Phase 7 routing note). They are comments, not
  user-facing strings, so plan 11.1-01's guard does not cover them **by design** — a
  comment-scoped guard would be invalidated by the very commits that quote a defect while fixing it.
  Disposition: file as a pending todo with that reason, or record as accepted debt. Decide, and say
  which. Leaving it unstated is the exact defect AUDIT-01 is.
- `r2000-cli.test.ts:87-92` duplicates `writeChain()`'s used-byte formula (noted by plan 11.1-06 T3),
  with the reason the independent literal pin lives in `r2000-d64.test.ts` instead.

Close with a `## Resolution` section in the residual-findings todo's style (the ledger lands in
`completed/`): every enumerated finding now has a disposition; name the one deferral and where it
lives; then state the closing rule — **"no disposition anywhere" was the defect; a future review's
findings are either fixed with a named plan or filed with a stated reason.** Include the arithmetic:
`10-REVIEW.md` reported 2 Critical / 12 Warning / 7 Info = 21 findings; account for all 21 by naming
where each was closed (CR-01, CR-02, WR-01 at phase close-out; WR-02 through WR-07 by the
residual-findings todo and plans 11-01/11-02; WR-08 — check its state and state it; the rest here).
If the arithmetic does not reach 21, say so explicitly rather than rounding.
  </action>
  <verify>
    <automated>grep -c 'WR-09' .planning/todos/completed/2026-08-21-phase-10-and-11-review-residual-dispositions.md</automated>
  </verify>
  <acceptance_criteria>
    - The ledger file exists in `.planning/todos/completed/` and every one of WR-09, WR-10, WR-11, WR-12, IN-01, IN-02, IN-03, IN-04, IN-05, IN-06, IN-07 appears in it by ID (verify with one grep per ID; a single combined grep can pass on a subset)
    - Every entry marked **fixed** cites a plan, a task and a SUMMARY file that exists on disk (verify each cited SUMMARY path with `test -f`)
    - IN-07's deferral names `.planning/todos/pending/2026-08-21-migrate-hand-copied-acme-gates-to-r2000-test-gate.md`, and that file's `files:` list now includes `r2000-project.test.ts`
    - The "Found while dispositioning" section states a decision (todo filed, or accepted debt) for the `stock-cia.ts`/`stock-dispatch.ts` stale phase comments — not merely a description
    - The 21-finding arithmetic is stated and reaches 21, or explicitly names the shortfall
    - No finding's wording was softened relative to `10-REVIEW.md`: for each entry, the quoted claim still names the same file and the same defect (spot-check recorded in the SUMMARY)
  </acceptance_criteria>
  <done>Nothing from either review is closed by silence: every finding names its plan or its reason.</done>
</task>

<task type="auto">
  <name>Task 2: STATE.md's Deferred Items and Operator Next Steps, accurate as of this phase's close</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - `.planning/STATE.md` lines 260-330 — § Deferred Items (the 13-row v0.2.0 inheritance table,
      its "Not counted above" paragraph, its "Also carried, not blocking" paragraph, and the
      § "Carried forward from earlier closes" table), § Session Continuity and § Operator Next Steps.
    - `.planning/STATE.md` lines 1-45 — frontmatter (`progress`, `last_activity`) and § Current
      Position, so the numbers you write agree with the file's own frontmatter.
    - `ls .planning/todos/pending/` and `ls .planning/todos/completed/` — the ground truth. There
      are 15 pending todos as of planning, five opened during v0.3.0, and two currently listed as
      Pending in STATE.md now live in `completed/`.
    - `.planning/v0.3.0-MILESTONE-AUDIT.md` frontmatter and § Milestone-level bookkeeping — AUDIT-04
      and AUDIT-05's exact claims, which this task closes.
    - The ledger written in Task 1 — its one deferral belongs in the refreshed Deferred Items.
  </read_first>
  <action>
**Deferred Items.** Rebuild the table from the todo tree as it actually is, after this phase's
plans. Every file in `.planning/todos/pending/` gets exactly one row, with the same
`Category | Item | Priority | Status` columns the table already uses and the `Item` cell holding the
todo's filename stem (the guard in Task 3 matches on that, so keep it exact). Remove the two rows
whose todos are in `completed/`
(`2026-08-17-document-second-binmon-client-as-a-wedge-lookalike`,
`2026-08-19-acme-build-scaffold-library-missing-on-both-provisioning-routes`) and say in one line
where they went, so the change reads as a correction rather than a deletion. Keep the `uat_gap` row
and the two carried-forward tables — they are not todo files and the guard must not require them to
be.

Update the table's heading sentence: it currently says "13 items acknowledged and deferred at the
v0.2.0 milestone close on 2026-08-19", which is now a historical statement about a different set.
**Do not rewrite that history** — this repo's convention (and this phase's scope fence) is that a
superseded statement stays beside its correction, marked superseded. Keep the v0.2.0 sentence,
marked as the inherited set with its date, and add the current statement beneath it: the count as of
Phase 11.1's close, how many are v0.3.0-opened, and that the set is now derived from
`.planning/todos/pending/` and guarded by `docs-deferred-ledger.test.ts`.

Also add the items this phase itself deferred: IN-07's migration (already a pending todo, so it
arrives via the derivation) and anything Task 1's "Found while dispositioning" section filed as a
todo. If Task 1 recorded something as accepted debt rather than a todo, it belongs in the "Also
carried, not blocking" prose, not the table — put it there.

**Operator Next Steps.** Replace the stale v0.2.0-close text. It must say what is true now: this
phase closed the v0.3.0 audit items; the two Phase 10 coverage gaps are separate commands to run
next — `/gsd-validate-phase 10` (the `10-VALIDATION.md` ledger is a pre-execution snapshot; the
validation itself happened) and `/gsd-secure-phase 10` (no `10-SECURITY.md` exists despite Phase 10
introducing the untrusted-input byte parsers) — and they are deliberately NOT plans in this phase
(D-11.1-08) because they write phase-10 artifacts. Then the milestone close itself. Delete the
`R2000-16` line: that probe ran and its verdict is recorded.

Keep § Session Continuity's mechanical fields alone (the tooling writes them) and make sure any
number you state agrees with the frontmatter — if the frontmatter is itself stale, say so in the
SUMMARY rather than silently editing a field this plan does not own.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && node --test docs-deferred-ledger.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every file in `.planning/todos/pending/` has exactly one row in STATE.md's Deferred Items table, and no row names a file that lives in `.planning/todos/completed/` (asserted by Task 3's guard)
    - The count stated in the table's heading equals the number of pending todos, and the v0.2.0 sentence survives marked as the inherited set (superseded-beside-original, not overwritten)
    - § Operator Next Steps names `/gsd-validate-phase 10` and `/gsd-secure-phase 10`, states they are separate commands per D-11.1-08, and no longer contains the string `can run before the milestone is opened`
    - `grep -c 'R2000-16' .planning/STATE.md` shows the stale probe line is gone from § Operator Next Steps (other, historical mentions elsewhere in the file may legitimately remain — check which you are counting and say so)
    - No success criterion or historical claim anywhere in STATE.md was reworded to match an outcome; every correction sits beside its original, marked superseded (spot-check recorded in the SUMMARY)
  </acceptance_criteria>
  <done>The deferred set and the operator's next actions are accurate at the moment the milestone-close command will read them.</done>
</task>

<task type="auto">
  <name>Task 3: A derivation guard so the deferred set cannot go stale again</name>
  <files>.claude/mcp/vice/docs-deferred-ledger.test.ts</files>
  <read_first>
    - `.claude/mcp/vice/docs-dangling-refs.test.ts` in full — the established shape for a
      `.planning/`-facing guard in this directory: `repoRoot({ from: HERE })` for path resolution,
      an existence assertion on every scanned path so the set cannot shrink silently, a non-vacuity
      test, and a committed planted-violation test. Mirror it. (Plan 11.1-01 also edits that file;
      this is a NEW file, so there is no conflict.)
    - `.planning/STATE.md` § Deferred Items as Task 2 leaves it — the exact table shape the parser
      must read.
    - `.claude/mcp/vice/repo-root.ts` — `repoRoot()`'s contract, and its documented behaviour under
      a `.claude/worktrees/` checkout (`repo-root.test.ts` is known to fail only there; do not
      re-create that sensitivity in this new file — resolve from `HERE` the way
      `docs-dangling-refs.test.ts` does).
    - `.claude/mcp/vice/package.json` — the `node --test '*.test.*'` glob (runs this file in CI) and
      `files[]` (a `*.test.ts` is correctly absent; this file verifies planning documents, not
      shipped runtime behaviour — state that in its header, as `docs-dangling-refs.test.ts` does).
  </read_first>
  <action>
Create `.claude/mcp/vice/docs-deferred-ledger.test.ts` with a header stating why it exists: AUDIT-04
— STATE.md's Deferred Items was v0.2.0's inheritance verbatim, listing two completed todos as
Pending and omitting five v0.3.0 ones, for the same structural reason four other findings in this
phase exist (a hand-maintained list stopped growing). This file makes the list derived.

Four tests:

1. **Every pending todo appears.** `readdirSync('.planning/todos/pending/')` filtered to `*.md`,
   each filename stem asserted present in STATE.md's Deferred Items section text. Failure message
   names the missing stems and says to add a row.
2. **No completed todo is listed as Pending.** For each `*.md` in `.planning/todos/completed/`,
   assert its stem does NOT appear in the Deferred Items section. Failure message names the stem and
   says the todo is complete on disk.
3. **Non-vacuity.** Assert the Deferred Items section was actually located and is non-empty (parse
   from the `## Deferred Items` heading to the next `## ` heading), that the pending set has at
   least 10 entries, and that the completed set has at least 5 — a broken path or a renamed heading
   must FAIL, not pass by finding nothing. Include a positive control: assert one specific known
   pending stem (`2026-08-20-vsf-as-a-bootstrap-input`) is found by test 1's own matcher.
4. **Planted violation.** Run both predicates against synthetic inputs: a Deferred Items text
   missing a known pending stem is reported by predicate 1; a Deferred Items text containing a known
   completed stem is reported by predicate 2; and the real, corrected text is reported by neither.
   Extract each predicate into exactly one named function used by both the real tests and this one.

Scope the section parse narrowly and say why in a comment: only § Deferred Items is scanned, because
the rest of STATE.md legitimately mentions completed todos while describing their closure — the same
false-positive reasoning `docs-dangling-refs.test.ts` records for `.planning/phases/**`.

Do not assert on the `uat_gap` row or the carried-forward tables: they are not todo files, and a
guard that demands they match a directory listing would be unsatisfiable.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && node --test docs-deferred-ledger.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd .claude/mcp/vice && node --test docs-deferred-ledger.test.ts` exits 0 with 4 or more tests
    - Demonstrated in the SUMMARY: deleting one pending todo's row from STATE.md makes the test FAIL naming that stem (transcript recorded, revert); re-adding a completed todo's stem also makes it FAIL naming that stem (transcript recorded, revert)
    - The non-vacuity test asserts the section was located, the floors (>= 10 pending, >= 5 completed) hold, and the positive-control stem is found
    - The planted-violation test asserts both predicates on synthetic inputs and that the real text is reported by neither
    - Each predicate has exactly one definition (grep for its `function` line returns 1) and is called by both the real-source and planted-violation tests
    - `node scripts/check-npm-packages.mjs` exits 0 — the new file is a `*.test.ts` and correctly absent from `files[]`
  </acceptance_criteria>
  <done>STATE.md's deferred set is now derived from the todo tree by a guard that has been observed failing in both directions.</done>
</task>


<task type="auto">
  <name>Task 4: Guard the ledger itself -- the completeness check that AUDIT-01 needed and nobody wrote</name>
  <files>.claude/mcp/vice/docs-review-disposition.test.ts</files>
  <read_first>
    - `.claude/mcp/vice/docs-deferred-ledger.test.ts` as Task 3 leaves it -- same directory, same
      `repoRoot({ from: HERE })` idiom, same planted-violation discipline. Mirror its shape; do not
      extend it (it owns a different invariant, and one guard per invariant is this repo's pattern).
    - `.planning/phases/09-the-assumption-probe-go-no-go/09-REVIEW.md`,
      `.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-REVIEW.md`,
      `.planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/11-REVIEW.md` --
      the finding-heading shape the parser must read (`### WR-09: ...`, `### IN-03: ...`).
  </read_first>

  **Why this task exists.** The v0.3.0 audit's AUDIT-01 finding was not "these four warnings are
  wrong". It was **"these four warnings have no disposition anywhere"** -- silence, not error. Every
  other generalizable finding in this phase is held by a self-deriving mechanical guard. The
  disposition ledger Task 1 writes is the one artifact assembled by prose instruction ("read every
  SUMMARY and cross-reference the callouts"), which means the AUDIT-01 defect can recur *inside the
  plan that exists to close AUDIT-01*. The phase's own principle -- guard-first, not fix-first --
  is not applied to itself without this task. Flagged by the plan checker as this phase's single
  highest process risk; this task is the answer to it.

  **Build a guard that derives the finding set rather than listing it.** Parse every
  `.planning/phases/*/[0-9]*-REVIEW.md` for finding headings matching `^### (WR|IN|CR)-\d+:` and
  collect `(phase, id)` pairs. For each pair, assert the id has a recorded disposition in at least
  one of:
    - a `.planning/todos/{pending,completed}/*.md` file naming it,
    - `.planning/v*-MILESTONE-AUDIT.md`'s `tech_debt` block,
    - that phase's own `*-VERIFICATION.md` residual-findings section,
    - a `*-SUMMARY.md` in that phase's directory.

  A finding with no mention in any of those is **undispositioned**, and the guard fails naming it.
  This is exactly AUDIT-01's defect expressed as an assertion.

  **Non-vacuity, three ways -- all committed, none transcript-only:**
    1. **Positive control on the parser.** Assert the derived pair set contains known-present
       anchors (`10-REVIEW.md` -> `WR-09`, `11-REVIEW.md` -> `IN-02`, `09-REVIEW.md` -> `WR-01`)
       and that the total parsed count clears a floor of **>= 100**. A parser that silently matches
       nothing fails outright -- the same trap plan 11.1-01 found in the string-literal extractor.

       **Measured before planning, do not re-derive from scratch:** the guard scans *every* phase,
       not just this milestone's, because a finding id going silent is the same defect whichever
       milestone wrote it. Actual counts on disk at 2026-08-21: 108 findings across 11 REVIEW.md
       files (01:8, 02:25, 03:0, 04:4, 05:0, 07:20, 08:16, 08.2:2, 09:5, 10:21, 11:7). Of those,
       **8 are undispositioned today** -- and 2 of the 8 (`10-REVIEW.md`'s `IN-05` and `IN-06`) are
       closed by this very phase (plans 11.1-05 T1 and 11.1-06 T2), so they will be dispositioned
       by the time this task runs.
    2. **Planted violation.** A fixture REVIEW.md carrying a synthetic `### WR-99:` heading that no
       disposition source mentions; assert the checker reports it undispositioned. Commit the
       fixture.
    3. **Planted false-negative.** The same synthetic id WITH a disposition fixture present; assert
       the checker passes. This pins that the guard reads the disposition sources rather than just
       counting headings.

  **Scope fence.** The guard reports; it does not rewrite. It must not require a *particular*
  disposition wording, only that the id is accounted for somewhere -- otherwise it becomes a
  prose-matching test that fights every future reviewer's phrasing. Record that limit in the file
  header, the way plan 11.1-01 records its comment-scope limit rather than papering over it.

  **The six v0.2.0 stragglers -- disposition them, do not exempt them.** After this phase's own
  fixes, the guard's remaining failures will be exactly six inherited findings:
  `02-REVIEW.md` -> `IN-05`, and `07-REVIEW.md` -> `WR-15, WR-16, WR-17, WR-18, WR-19`. These are
  v0.2.0's leftovers: real review findings that were never fixed, never filed, and never named in
  that milestone's audit (whose `tech_debt` block enumerates 16 ids, not these). Give each a
  one-line disposition in the Task 1 ledger -- read the finding, then either mark it still-open with
  a reason or record it as superseded/moot if the code has since changed. Do **not** add a
  milestone-scoped exemption to make the guard green: an exemption would reintroduce exactly the
  "silence, not error" defect one layer up, and the whole point of this phase is that nothing gets
  closed by being unmentioned. Six lines of honest disposition is the cheaper and truer fix.

  **Interaction with Task 1.** Run this guard AFTER Task 1's ledger lands. If it fails, Task 1's
  ledger is incomplete -- that is the guard doing its job, and the ledger gets the missing entries
  before this task closes. Record in the SUMMARY how many findings the guard caught that the prose
  pass had missed; that number is the evidence for whether the process risk was real.

  <verify>
    cd .claude/mcp/vice && node --test docs-review-disposition.test.ts
  </verify>
</task>

</tasks>

<verification>
Per-plan regression gate — all must hold:

```
cd .claude/mcp/vice && VICE_REQUIRE_R2000=1 VICE_REQUIRE_ACME=1 npm test
cd .claude/mcp/vice && npm run typecheck
node scripts/check-npm-packages.mjs
node scripts/check-skill-fork-honesty.mjs
node scripts/check-skill-tool-coverage.mjs
cd .claude/mcp/vice && node --test docs-linerefs.test.ts
```

Baseline: 2086 tests / 0 failures plus every test waves 1-2 added; `tsc --noEmit` clean; all four
scripts exit 0. Record the final total in the SUMMARY — it is the number the phase's VERIFICATION
will cite.

No threat model section: this plan edits planning documents and adds one test file. It introduces no
input handling, no spawn path and no shipped surface. The one security-relevant fact it records is
that Phase 10 has no `10-SECURITY.md` — dispositioned as `/gsd-secure-phase 10`, a separate command
per D-11.1-08, and named in § Operator Next Steps by Task 2.
</verification>

<success_criteria>
- Every enumerated Phase 10 and Phase 11 review finding has a recorded disposition; the one deferral
  names its todo and its reason; the 21-finding arithmetic is stated.
- STATE.md's Deferred Items matches `.planning/todos/pending/` exactly, in both directions.
- STATE.md's Operator Next Steps names the two Phase 10 commands and drops the stale `R2000-16` line.
- The derivation guard has been observed failing in both directions.
- The ledger-completeness guard derives >= 100 findings, reports zero undispositioned after Task 1
  (including the six inherited v0.2.0 findings, dispositioned rather than exempted),
  and has been observed failing on both a planted undispositioned id and passing on a planted
  dispositioned one.
- Full suite, typecheck and all four CI scripts green.
</success_criteria>

<verification_map>
| Task | Item closed | Test Type | Automated Command |
|------|-------------|-----------|-------------------|
| 11.1-07-T1 | AUDIT-01 (D-11.1-04) | document + per-ID grep + cited-SUMMARY existence checks | `grep -c 'WR-09' .planning/todos/completed/2026-08-21-phase-10-and-11-review-residual-dispositions.md` |
| 11.1-07-T2 | AUDIT-04, AUDIT-05 (D-11.1-07) | document, checked by the Task 3 guard | `cd .claude/mcp/vice && node --test docs-deferred-ledger.test.ts` |
| 11.1-07-T3 | AUDIT-04 guard half | derivation guard + planted violation | `cd .claude/mcp/vice && node --test docs-deferred-ledger.test.ts` |
| 11.1-07-T4 | AUDIT-01 guard half (the ledger's own completeness) | derived finding set + positive control + two planted violations | `cd .claude/mcp/vice && node --test docs-review-disposition.test.ts` |
</verification_map>

<output>
Create `.planning/phases/11.1-close-v0-3-0-audit-items-the-stale-vsf-pointer-the-undocumen/11.1-07-SUMMARY.md` when done.
</output>
</content>
