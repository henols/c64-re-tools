---
phase: 11.1-close-v0-3-0-audit-items-the-stale-vsf-pointer-the-undocumen
verified: 2026-08-21T11:33:55Z
status: passed
score: 11/11 must-haves verified
overrides_applied: 0
---

# Phase 11.1: Close v0.3.0 Audit Items — Verification Report

**Phase Goal:** Every finding the v0.3.0 milestone audit recorded is either fixed or formally
dispositioned, and each one that could drift back is held by a mechanical guard rather than by
a promise.

**Verified:** 2026-08-21T11:33:55Z
**Status:** passed
**Re-verification:** No — initial verification

## Method

This verification did not trust SUMMARY.md prose. For every guard this phase claims is
"non-vacuous," a **real mutation** was planted directly against the live source tree (never a
throwaway/inline synthetic-only claim), the corresponding test or CI script was run against the
mutated tree, the failure was inspected to confirm it names the actual violation, and the
mutation was reverted with `diff`/`git show` byte-identity confirmed before moving on. The repo
was left in the exact clean state it started in (`git status` clean throughout; a single
detached `git worktree` used to check pre-plan history was removed afterward).

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | FLOW-02 closed at all three sites (`r2000-cli.ts` USAGE + bootstrap refusal, `r2000-project.ts` comment); none names a phase | ✓ VERIFIED | `grep -c "Phase 11's job"` → 0 in both files; both `r2000-cli.ts` sites name `2026-08-20-vsf-as-a-bootstrap-input.md`; live CLI transcript confirmed |
| 2 | FLOW-02 guard genuinely fails on a phase-number literal anywhere in shipped `.claude/mcp/vice/` source | ✓ VERIFIED | Planted `"...Phase 42's job..."` literal into `r2000-verify.ts` → `docs-dangling-refs.test.ts` failed naming the file and literal; reverted, byte-identical |
| 3 | FLOW-01: all 7 CLI verbs (incl. `gen-enums`/`export-lbl`/`import-lbl`) named by ≥1 skill file; coverage guard parses verbs from `r2000-cli.ts`'s dispatch switch, not a hard-coded list | ✓ VERIFIED | `grep -rl` confirms all three verbs documented; planted an 8th `"ghost-verb"` case in a scratch copy of `r2000-cli.ts` → `check-skill-tool-coverage.mjs` failed naming it; reverted, byte-identical |
| 4 | INT-01: `hostpath-consumers.test.ts`'s r2000 absence list is `readdirSync`-derived with a ≥14 floor; old hard-coded 10-name array gone | ✓ VERIFIED | `grep -c '"r2000-launch.ts",'` → 0; `readdirSync`/`topLevelProductionModules`/`R2000_MODULE_FLOOR=14` present; planted a real `import { hostPath } from "./hostpath.ts"` into `r2000-symbols.ts` → test failed naming it; reverted, byte-identical. Multi-line import shape on `stock-paths.ts` reproduced as still-detected |
| 5 | INT-02: `r2000-launch.ts`'s header no longer falsely claims exclusivity; a guard asserts every regenerator2000 spawn site calls `assertNoViceFlag` first | ✓ VERIFIED | `grep -c 'ONE authoritative place...'` → 0; header names `r2000-mcp-client.ts`; planted removal of `assertNoViceFlag(argv)` from `r2000-mcp-client.ts:330` → `r2000-spawn-seam.test.ts` failed naming the file; reverted, byte-identical |
| 6 | WR-09/WR-10/WR-11/WR-12, IN-01…IN-07 (10-REVIEW.md), Phase 11 IN-02 each fixed or dispositioned with cited evidence | ✓ VERIFIED | All 12 findings traced to a real commit + SUMMARY (all commit hashes confirmed via `git log`); WR-09 (`c74617e`), WR-10 (`b915913`), WR-11 (`94010dd`), WR-12 (`d9c339e`), IN-01 (`d5e01a8`), IN-02/10 (`95b128f`), IN-03 (`c3f5b75`), IN-04 (`b915913`), IN-05 (`94010dd`), IN-06 (`03e71e9`), Phase-11 IN-02 (`0ff3440`); each behaviour independently spot-checked live (see per-plan sections below) |
| 7 | AUDIT-04/-05: STATE.md's Deferred Items matches `.planning/todos/pending/` in both directions; Operator Next Steps is current | ✓ VERIFIED | `ls .planning/todos/pending/*.md \| wc -l` → 18, matches STATE.md's stated count exactly; Operator Next Steps names `/gsd-validate-phase 10` + `/gsd-secure-phase 10` + `/gsd-complete-milestone v0.3.0`; the stale "can run before the milestone is opened" `R2000-16` sentence is gone from that section |
| 8 | A committed guard fails if STATE.md's Deferred Items and the todo tree ever disagree again | ✓ VERIFIED | Deleted a pending todo's row → `docs-deferred-ledger.test.ts` failed naming the stem (direction A); re-added a completed todo's stem as Pending → failed naming it (direction B); both reverted, byte-identical |
| 9 | A committed guard fails if any REVIEW.md finding anywhere has no recorded disposition, with no milestone-scoped exemption | ✓ VERIFIED | Independently re-derived 108 findings by direct grep across all 11 `*-REVIEW.md` files (01:8 02:25 03:0 04:4 05:0 07:20 08:16 08.2:2 09:5 10:21 11:7 = 108, exact match); planted a real `### WR-999:` heading into the live `10-REVIEW.md` → `docs-review-disposition.test.ts` failed naming it; reverted, byte-identical. No `exempt`/`allowlist`/`skip` token found anywhere in the guard's source |
| 10 | The ledger-completeness guard's own numbers: 27 undispositioned before Task 1 (not the 8 predicted), 0 after | ✓ VERIFIED | Checked out baseline commit `28ed0d1` (pre-11.1-07) into a detached worktree, copied today's guard file + fixtures into it, ran it: reported **exactly 27** undispositioned findings, matching the SUMMARY's claim precisely (Phase01×6, Phase02×4, Phase08×9, Phase09×1, plus 7 in Phase10/11) — not the plan's pre-measured 8. Guard passes 4/4 (zero undispositioned) against the current tree. Worktree removed afterward, main tree confirmed clean |
| 11 | Scope fences held: no `.vsf` bootstrap, Phase 4 disassembler family untouched, `installer/skills/` untouched, every new non-test `.claude/mcp/vice/` module is in `package.json` `files[]`, no criterion reworded in place | ✓ VERIFIED | `git diff --stat` across the phase's full commit range (`2277885^..8338fea`) touches zero `disasm-*.ts`/`stock-disassemble.ts`/`disasm-roundtrip.test.ts` files and zero `installer/skills/` files; the only non-test `.claude/mcp/vice/` files modified (`r2000-cli.ts`, `r2000-launch.ts`, `r2000-project.ts`, `r2000-symbols.ts`, `vice-proxy.ts`) were already in `files[]`; new production logic (`scripts/lib/r2000-cli-verbs.mjs`, `scripts/lib/skill-honesty-checks.mjs`) deliberately lives outside that tree; STATE.md's v0.2.0 sentence is kept, marked superseded, beside the current one — not overwritten |

**Score:** 11/11 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.claude/mcp/vice/docs-dangling-refs.test.ts` | shipped-string phase-pointer guard | ✓ VERIFIED | 8/8 tests pass; genuine mutation-kill confirmed |
| `.claude/mcp/vice/r2000-cli.ts`, `r2000-cli.test.ts` | FLOW-02 sites reworded + retargeted test | ✓ VERIFIED | live transcript + tests pass |
| `scripts/lib/r2000-cli-verbs.mjs`, `.claude/mcp/vice/r2000-verb-coverage.test.ts` | verb parser + coverage gate | ✓ VERIFIED | 6/6 tests; live CI-script mutation-kill confirmed |
| `.claude/skills/c64-memory-mapping/SKILL.md`, `c64-program-recon/SKILL.md` | gen-enums / export-lbl / import-lbl documented as one loop | ✓ VERIFIED | grep confirms all three verbs named; round-trip subsection present |
| `.claude/mcp/vice/hostpath-consumers.test.ts` | self-deriving r2000 absence list + widened import detection | ✓ VERIFIED | 22/22 tests; two independent mutation-kills confirmed |
| `.claude/mcp/vice/r2000-launch.ts`, `r2000-spawn-seam.test.ts` | bounded spawn + spawn-seam invariant | ✓ VERIFIED | live slow-child timeout test passes; mutation-kill on `r2000-mcp-client.ts` confirmed |
| `.claude/mcp/vice/r2000-symbols.ts`, `r2000-symbol-roundtrip.test.ts` | `regenerateAndReload()` library-only + biconditional guard | ✓ VERIFIED | mutation-kill (scratch caller added) confirmed |
| `scripts/lib/skill-honesty-checks.mjs`, `.claude/mcp/vice/skill-honesty-checks.test.ts` | WR-11/IN-05/IN-03 predicates | ✓ VERIFIED | 12/12 tests; three live mutation-kills confirmed (WR-11 bidirectional, IN-03 both directions) |
| `.claude/mcp/vice/vice-proxy.ts`, `vice-proxy.test.ts` | drained/bounded exit (IN-01) | ✓ VERIFIED | live mutation (drain removed) reproduced exact truncation regression (146176/512000 bytes); CLAUDE.md's 4 line citations resolve correctly against current source |
| `.claude/mcp/vice/r2000-cli.ts` (WR-09/IN-06), `r2000-d64.test.ts` (WR-12) | never-throw contract + option refusal + literal fixtures | ✓ VERIFIED | structural-guard mutation-kill confirmed; live `verify --out` refusal confirmed; WR-12 joint-mutation reproduced the exact tautology contrast (literal fixtures fail, `writeChain()`-based round trip still passes) |
| `.planning/todos/completed/2026-08-21-phase-10-and-11-review-residual-dispositions.md` | disposition ledger | ✓ VERIFIED | all 12 findings present with cited plan/task/commit; WR-08 and `stock-connect.ts` IN-05 claims spot-checked live and confirmed still genuinely open |
| `.claude/mcp/vice/docs-deferred-ledger.test.ts`, `docs-review-disposition.test.ts` | derivation + completeness guards | ✓ VERIFIED | both mutation-killed in both directions against real files |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `docs-dangling-refs.test.ts` | `package.json` `files[]` | derived shipped-module set | ✓ WIRED | confirmed scanning 58 modules, existence-asserted |
| `check-skill-tool-coverage.mjs` | `r2000-cli.ts` | `parseR2000CliVerbs()` reads dispatch switch | ✓ WIRED | live mutation-kill confirms it reads real source, not a cached list |
| `hostpath-consumers.test.ts` | `.planning/../` r2000 modules | `readdirSync`-derived | ✓ WIRED | confirmed 14-module floor and derivation |
| `r2000-spawn-seam.test.ts` | `r2000-mcp-client.ts` | structural `assertNoViceFlag` ordering check | ✓ WIRED | mutation-kill confirms real ordering check, not a name-only presence check |
| `docs-deferred-ledger.test.ts` | `.planning/todos/pending/` | `readdirSync`-derived pending set vs. STATE.md | ✓ WIRED | both directions mutation-killed |

### Requirements Coverage

No new or reopened requirements (`requirements: []` in every plan, confirmed). All 12 in-scope
`R2000-*` requirements remain satisfied — `check-skill-tool-coverage.mjs` and the full test suite
both confirm no regression to any of them.

### Anti-Patterns Found

None blocking. No `TBD`/`FIXME`/`XXX` unreferenced markers found in files this phase modified.
Deliberate, honestly-recorded residual debt (see Judgement below):

| Item | Severity | Disposition |
|------|----------|-------------|
| `VICE_TEST_R2000_CLI_STDOUT_FILL_BYTES` test-only env hatch on `vice-proxy.ts`'s shipped path | ℹ️ Info | Recorded in the ledger as accepted debt; gated behind a name no real caller would set; inert; inspected — cannot execute code or read arbitrary files |
| `r2000-project.ts`'s FLOW-02 comment site permanently outside the string-literal guard | ℹ️ Info | Recorded as a deliberate, documented, permanent guard-scope limit (both in `11.1-01-SUMMARY.md` and the ledger) |
| `r2000-cli.test.ts:87-92` duplicates `writeChain()`'s used-byte formula | ℹ️ Info | Recorded as accepted debt with a stated reason (the independent pin lives in `r2000-d64.test.ts`) |
| WR-08 (10-REVIEW.md), `stock-cia.ts`/`stock-dispatch.ts` stale phase pointers, Phase 08's WR-04..WR-12, `02-REVIEW.md`'s IN-05 | ⚠️ Warning (pre-existing debt, correctly filed) | All filed as new pending todos or recorded as accepted/superseded debt with a stated reason — genuine dispositions, not silence |

### Human Verification Required

None. Every claim in this phase was either mechanically checkable (tests, CI scripts, structural
guards) or spot-checkable via a live CLI/file-content check, and all were checked directly against
the codebase rather than trusted from SUMMARY prose.

### Gaps Summary

No gaps. Every guard this phase claims is non-vacuous was independently mutation-tested against the
real, live source tree (not merely re-run against its own committed fixtures) and observed failing
for the correct, specific reason, then reverted with byte-identity confirmed. The phase's central,
hardest self-check — the ledger-completeness guard's own arithmetic — was independently re-derived
from a clean pre-plan worktree and matched exactly (108 findings total; 27 undispositioned before
Task 1, considerably more than the plan's 8-item prediction; 0 after). No milestone-scoped exemption
or allowlist exists in that guard. All scope fences (no `.vsf` support, Phase 4 disassembler family,
`installer/skills/`, `package.json` `files[]`, no criterion reworded in place) held under direct
`git diff --stat` inspection across the phase's full commit range. The full regression suite
(2192 tests / 2157 pass / 0 fail / 30 skipped / 5 todo), `tsc --noEmit`, and all four CI scripts plus
`docs-linerefs.test.ts` all reproduced exactly the numbers the final SUMMARY claims.

## Residual Debt — Honest Judgement

- **`VICE_TEST_R2000_CLI_STDOUT_FILL_BYTES`** — a legitimate deviation. Neither of the plan's two
  named payload-generation routes could deterministically clear 128 KiB against the real installed
  `regenerator2000 0.9.20`; the hatch is narrow, inert unless explicitly set, never surfaces in
  `--help`, and is openly recorded in both the plan-05 SUMMARY and the disposition ledger. This is a
  disclosed trade-off, not a buried one.
- **The phase-pointer guard's comment-scope limit** — a deliberate, correctly-reasoned boundary
  (a comment-scoped guard would be invalidated by its own fix commits, which legitimately quote the
  old wording). Documented in three places (guard header, 11.1-01-SUMMARY.md, the ledger). This is
  an honest disposition, not a gap disguised as a feature.
- **The newly-filed pending todos** (WR-08, Phase 08's WR-04..WR-12, the two stale phase-pointer
  comments, `02-REVIEW.md`'s IN-05) — all are *filed with a genuinely stated reason* and, where
  checkable, independently reproduced live during this verification (WR-08's silent `--out`
  swallowing, and `stock-connect.ts`'s `stockConnect:`-prefixed message on `stockReconnect()`'s
  throw, were both confirmed to still be live defects). These are legitimate dispositions under this
  phase's own stated rule ("fixed with a named plan, or filed with a stated reason") — not
  "dispositioned" used as a euphemism for "left undone." The phase does not claim these are fixed;
  it claims they are now visible, which is verifiably true.

Overall: this phase's central thesis — that fixes are held by mechanical guards rather than by
promise — held up under adversarial, hands-on mutation testing at every guard site checked. Nothing
found here contradicts the SUMMARYs' claims.

---
*Verified: 2026-08-21T11:33:55Z*
*Verifier: Claude (gsd-verifier)*
