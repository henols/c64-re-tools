---
phase: 08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift
plan: 05
type: execute
wave: 3
depends_on: ["08.1-02", "08.1-04"]
files_modified:
  - .planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-CONSISTENCY-READ.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "A single reader working only from REQUIREMENTS.md, ROADMAP.md and STATE.md derives the same phase-completion and requirement-coverage picture the 2026-08-19 audit derived"
    - "Every number claimed in more than one of the three documents holds the same value in all of them"
    - "No document claims the milestone is complete while Phase 8.1 is in flight, and none claims Phase 8.1 is unnecessary"
    - "All seven D-item assertions pass together in one run, after both tracks have landed"
  artifacts:
    - path: ".planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-CONSISTENCY-READ.md"
      provides: "A cross-document reconciliation table naming every shared claim, its value in each file, and the verdict"
      contains: "AGREE"
  key_links:
    - from: ".planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-CONSISTENCY-READ.md"
      to: ".planning/v0.2.0-MILESTONE-AUDIT.md"
      via: "each reconciled claim is checked against the audit's own derivation"
      pattern: "MILESTONE-AUDIT"
---

<objective>
Close criterion 4: prove that a fresh reader of `REQUIREMENTS.md`, `ROADMAP.md` and `STATE.md`
derives the same picture the 2026-08-19 audit derived, and that no document contradicts another.

Purpose: the seven mechanical D-item fixes are necessary but not sufficient. The interaction
effects are what a per-item grep cannot see — a corrected total in one file that a second file
still derives differently, or a completion claim that only became false once the walkthrough
resolved. This plan is the single cross-document read-through the validation contract names as
criterion 4's manual verification, plus the one run in which all seven D-item assertions are
green at the same time.

Output: `08.1-CONSISTENCY-READ.md`, plus any residual corrections it turns up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v0.2.0-MILESTONE-AUDIT.md
@.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-VALIDATION.md
@.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-02-SUMMARY.md
@.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-04-SUMMARY.md
</context>

<canonical_numbers>
The values every one of the three documents must agree on after plans 08.1-01 and 08.1-02 land.
Any file disagreeing with one of these is a residual to fix in Task 2.

| Claim | Value |
|-------|-------|
| v0.2.0 requirements defined | 68 |
| Whole requirements cut 2026-08-17 | 17 |
| Additional partial halves cut | 4 (DIRECT-06 detach; DERIV-01 fill; DERIV-05, DERIV-06 `*_set_state`) |
| In scope | 51 |
| Complete | 51 |
| Open | 0 |
| Phase slots in the milestone | 9 (1-5, 6 cut, 7, 8, 8.1) |
| Phases complete | 7 |
| Progress | 78% |
| Plans complete | 76 of 76 |
| Phase 6 | Cut 2026-08-17 |
| Phase 7 | Complete 2026-08-18 |
| Phase 8 | Complete 2026-08-18 |
| Phase 8.1 | in flight; the milestone's last phase |
| Fork manifest tools | 62 |
| Stock manifest tools | 38 as of 2026-08-19 (26 as-of the 2026-08-17 cut) |
| Tools the six skills call | 29 |
| Of those: already working / buildable / impossible | 16 / 10 / 3 |
| Fork tools no skill calls | 33 |
| Fork-exclusive *capabilities* | 2 (this counts capabilities, not tools — correct at two) |
</canonical_numbers>

<tasks>

<task type="auto">
  <name>Task 1: Read the three documents in one pass and reconcile every shared claim</name>
  <files>.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-CONSISTENCY-READ.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the traceability table, the coverage block, the
      "Open requirements per phase" paragraph, and "### The cut criterion", in one pass.
    - `.planning/ROADMAP.md` — the milestone list, the Overview, the phase checkbox list
      (lines ~92-102), the Phase 7 / Phase 8 / Phase 8.1 sections, "Cut from scope", and the
      Progress section including its table and the "Remaining scope" / "Remaining work is not
      requirement work" paragraphs.
    - `.planning/STATE.md` — frontmatter and body together, including "Current Position", the
      cut-scope paragraph, "Performance Metrics", and "Blockers/Concerns".
    - `.planning/v0.2.0-MILESTONE-AUDIT.md` sections 2.1, 2.2, 2.3, 6, 7 and 9 — the picture the
      audit derived, which criterion 4 says a fresh reader must now re-derive.
    - `.planning/phases/08.1-*/08.1-VALIDATION.md` "Manual-Only Verifications", criterion 4 row —
      the binding test instruction for this task, including its explicit statement that the target
      is **7 of 9 phases complete with 8.1 in flight**, not the audit's now-stale "7/7, 100%".
    - `.planning/phases/08-capability-honesty-and-the-install-story/08-HUMAN-UAT.md` as updated by
      plan 08.1-04 — a `failed` outcome there may make a completion claim elsewhere false.
    - The `<canonical_numbers>` table above.
  </read_first>
  <action>
    Read all three planning documents in one pass, then write
    `08.1-CONSISTENCY-READ.md` containing a reconciliation table with one row per shared claim.
    Columns: `Claim`, `REQUIREMENTS.md (value @ line)`, `ROADMAP.md (value @ line)`,
    `STATE.md (value @ line)`, `Audit says`, `Verdict`. `Verdict` is exactly `AGREE` or
    `CONFLICT` — no hedged third value. Where a document legitimately does not carry a claim,
    write `n/a`, which never produces a CONFLICT on its own.

    Cover at minimum every row of the `<canonical_numbers>` table, plus these interaction checks
    that no single D-item grep can catch:
    - Does any file still describe the milestone as complete, finished, or ready to tag while
      Phase 8.1 is in flight?
    - Does STATE.md's frontmatter `percent`, its body progress bar, and ROADMAP.md's Progress
      table all imply the same completed-phase count?
    - Does REQUIREMENTS.md's "Open requirements per phase" paragraph sum to the coverage block's
      open total?
    - Does every phase marked complete in ROADMAP.md's checkbox list carry the same date as that
      phase's row in ROADMAP.md's own Progress table, and the same status as that phase's
      `*-VERIFICATION.md` frontmatter?
    - Does any file still name a requirement as owned by a phase that a different file records as
      cut or reassigned (the D-2 class of error)?
    - After plan 08.1-04, does any file still describe the install-to-RAM-capture walkthrough as
      pending, unrun, or blocked?
    - Do the as-of annotations added by plans 08.1-01 and 08.1-02 all carry a date, so a later
      reader can tell a dated historical figure from a live one?

    Finish the document with a "## Verdict" section: one sentence stating whether a fresh reader
    derives the audit's picture, and an explicit `CONFLICTS: <n>` line. If `n` is 0, criterion 4
    holds and Task 2 has nothing to do. If `n` is greater than 0, list each conflict as
    `RESIDUAL-<n>` with the file, the line, the two disagreeing values, and which one is correct
    per `<canonical_numbers>` — Task 2 fixes them.

    Do not fix anything in this task. Reading and fixing in the same pass is how a reader
    rationalises a contradiction instead of recording it.
  </action>
  <acceptance_criteria>
    - `.planning/phases/08.1-*/08.1-CONSISTENCY-READ.md` exists and is non-empty.
    - `grep -cE '^CONFLICTS: [0-9]+$' .planning/phases/08.1-*/08.1-CONSISTENCY-READ.md` returns 1.
    - The reconciliation table has at least 19 data rows: `awk '/^\| Claim/,0' .planning/phases/08.1-*/08.1-CONSISTENCY-READ.md | grep -cE '^\|' ` returns 21 or more (header, separator, and 19+ rows).
    - Every table row's verdict cell is `AGREE`, `CONFLICT` or `n/a`: `grep -oE 'AGREE|CONFLICT' .planning/phases/08.1-*/08.1-CONSISTENCY-READ.md | wc -l` returns 19 or more.
    - `grep -cE '^RESIDUAL-[0-9]+' .planning/phases/08.1-*/08.1-CONSISTENCY-READ.md` equals the number in the `CONFLICTS:` line.
    - `git diff --stat .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md` shows no change made by THIS task (the read is non-destructive).
  </acceptance_criteria>
  <verify>
    <automated>grep -qE '^CONFLICTS: [0-9]+$' .planning/phases/08.1-*/08.1-CONSISTENCY-READ.md &amp;&amp; test "$(grep -oE 'AGREE|CONFLICT' .planning/phases/08.1-*/08.1-CONSISTENCY-READ.md | wc -l)" -ge 19</automated>
    <human-check>Criterion 4 is a holistic reading judgment. The table proves the mechanical half; a human skim of the three documents end to end is the strictly stronger check and can be appended to this file if performed.</human-check>
  </verify>
  <done>A reconciliation table covers every claim shared by two or more of the three documents, each with a per-file value and line, an AGREE/CONFLICT verdict, and an explicit conflict count; no document was edited during the read.</done>
</task>

<task type="auto">
  <name>Task 2: Fix every recorded residual and prove all seven D-items green together</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-CONSISTENCY-READ.md</files>
  <read_first>
    - `.planning/phases/08.1-*/08.1-CONSISTENCY-READ.md` — the `RESIDUAL-<n>` list produced by
      Task 1, which is this task's entire work list.
    - `.planning/phases/08.1-*/08.1-d-checklist.sh` and `08.1-d-checklist-baseline.txt` — the
      seven-item harness and its recorded RED baseline.
    - The `<canonical_numbers>` table above — the authority for which of two disagreeing values
      is correct.
    - `.planning/phases/08.1-*/08.1-RESEARCH.md` section "Architecture Patterns" Pattern 2 — the
      as-of annotation convention to use for a dated historical figure rather than a silent
      renumber.
  </read_first>
  <action>
    For each `RESIDUAL-<n>` recorded in Task 1, apply the minimal correction that makes the
    disagreeing files agree on the `<canonical_numbers>` value. Two rules govern which side moves:
    - A **live** statement (a progress figure, a current-position line, a milestone Overview
      sentence, a Progress table cell) is rewritten to the canonical value.
    - A **dated historical** statement (a phase's success criteria, a dated scoping note, a
      dated comparison paragraph) keeps its text and gains an appended
      `*(As of <date>: ...)*` or `*(Corrected <date>, during Phase 8.1: ...)*` parenthetical, per
      the convention this ROADMAP already uses.
    Never resolve a conflict by deleting the claim from one file — that hides the disagreement
    rather than settling it.

    Do not remove the `# Milestone v0.2.0: Switchable stock-VICE backend` heading from
    ROADMAP.md; `extractCurrentMilestone()` depends on it.

    Do not invent a requirement ID. This phase introduces none, and every plan's `requirements:`
    field is deliberately empty.

    Then run `bash .planning/phases/08.1-*/08.1-d-checklist.sh` once, with both tracks landed, and
    confirm every one of D-1 through D-7 reports PASS and the script exits 0. If any item is FAIL
    at this point, fix the document it names — the assertions were proven to fire in plan
    08.1-01's recorded RED baseline, so a FAIL here is a real regression, not a bad assertion.

    Append to `08.1-CONSISTENCY-READ.md` a "## Residuals fixed" section listing each residual, the
    edit made, and the file and line, followed by the literal final output of the D-checklist run.
    If Task 1 found zero conflicts, write that section anyway stating `none` and still append the
    green checklist output — the run is the evidence, and its absence is indistinguishable from
    not having done it.
  </action>
  <acceptance_criteria>
    - `bash .planning/phases/08.1-*/08.1-d-checklist.sh; echo $?` exits 0.
    - `bash .planning/phases/08.1-*/08.1-d-checklist.sh | grep -c '^FAIL D-'` returns 0.
    - `bash .planning/phases/08.1-*/08.1-d-checklist.sh | grep -c '^PASS D-'` returns 7 or more.
    - `grep -cF '## Residuals fixed' .planning/phases/08.1-*/08.1-CONSISTENCY-READ.md` returns 1.
    - `grep -c 'PASS D-' .planning/phases/08.1-*/08.1-CONSISTENCY-READ.md` returns 7 or more (the green run is recorded, not just performed).
    - `grep -cE '^# Milestone v0\.2\.0: ' .planning/ROADMAP.md` returns 1.
    - `grep -c '100%' .planning/STATE.md` returns 0 and `grep -c '| Pending |' .planning/REQUIREMENTS.md` returns 0 (the two headline drift claims stay closed).
    - `git status --porcelain .claude .github scripts docs README.md installer` is empty.
    - `git rev-parse origin/main` is unchanged from the value recorded in `08.1-WALKTHROUGH-SETUP.md`, and `git tag --points-at HEAD | wc -l` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>bash .planning/phases/08.1-*/08.1-d-checklist.sh &amp;&amp; test "$(bash .planning/phases/08.1-*/08.1-d-checklist.sh | grep -c '^FAIL D-')" -eq 0 &amp;&amp; test "$(git tag --points-at HEAD | wc -l)" -eq 0</automated>
  </verify>
  <done>Every recorded residual is fixed with the live-versus-historical rule applied; all seven D-items pass in one run with exit code 0; the green run's literal output is recorded in the consistency read.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan reads three Markdown planning documents, writes one Markdown evidence document, and applies text corrections. No process boundary, network endpoint, untrusted input, credential, or runtime code path is created, modified, or crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-12 | Repudiation | `08.1-CONSISTENCY-READ.md` | mitigate | The reconciliation table records a per-file value AND line number for every shared claim, plus the literal D-checklist output, so criterion 4's verdict is re-derivable by a later auditor rather than being an unbacked assertion. An explicit `CONFLICTS: <n>` line prevents a silent "looked fine to me". |
| T-08.1-13 | Tampering | `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md` | mitigate | Task 1 is read-only and asserted so by a `git diff --stat` acceptance criterion; only Task 2 edits, and only against the residual list Task 1 recorded. Splitting read from fix prevents a reader rationalising a contradiction into agreement while editing. |
| T-08.1-14 | Elevation of Privilege | release path (`git push` / `npm publish`) | mitigate | LD-1 forbids push, tag and publish; the repo's CI auto-publishes on merge to `main`. Enforced mechanically by acceptance criteria on `git rev-parse origin/main` and `git tag --points-at HEAD`. |
| T-08.1-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install occurs. `08.1-RESEARCH.md` section "Package Legitimacy Audit" records N/A for this phase; no `[ASSUMED]`/`[SUS]` package exists to gate. |

**Justified assessment: no new threats.** This plan ships no code and touches no runtime module;
`capability-registry.ts` is untouched. ASVS L1 has no applicable control for a documentation
consistency review with no input surface.
</threat_model>

<verification>
1. `bash .planning/phases/08.1-*/08.1-d-checklist.sh` exits 0 with seven PASS lines and no FAIL.
2. `08.1-CONSISTENCY-READ.md` contains a reconciliation table, an explicit `CONFLICTS: <n>` line,
   a matching count of `RESIDUAL-<n>` entries, a "## Residuals fixed" section, and the literal
   green checklist output.
3. `grep -cE '^status: (passed|failed)$' .planning/phases/08-capability-honesty-and-the-install-story/08-HUMAN-UAT.md`
   returns 1 and no planning document still describes that walkthrough as pending.
4. `git status --porcelain .claude .github scripts docs README.md installer` is empty;
   `git rev-parse origin/main` unchanged; `git tag --points-at HEAD` empty.
</verification>

<success_criteria>
- A cross-document reconciliation table exists covering every claim shared by two or more of
  REQUIREMENTS.md, ROADMAP.md and STATE.md, each with a per-file value, a line reference, and an
  AGREE/CONFLICT verdict
- `CONFLICTS: 0` after Task 2, with every residual's fix recorded
- All seven D-item assertions pass in one run, exit code 0, with the output recorded as evidence
- No document describes the milestone as complete while Phase 8.1 is in flight, and none
  describes the walkthrough as pending
- The `# Milestone v0.2.0:` heading survives
- No REQ-ID invented; no push to `origin`, no tag, no npm publish
</success_criteria>

<output>
Create `.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-05-SUMMARY.md` when done.
</output>
