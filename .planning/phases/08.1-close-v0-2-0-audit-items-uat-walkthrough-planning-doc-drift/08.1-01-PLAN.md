---
phase: 08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-d-checklist.sh
  - .planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-d-checklist-baseline.txt
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "A single command reports pass/fail for each of the seven audit §7 drift items D-1..D-7"
    - "No row in REQUIREMENTS.md's traceability table still reads `Pending` for a requirement that was cut on 2026-08-17"
    - "REQUIREMENTS.md's DIRECT-06 traceability row no longer attributes disk detach to Phase 7"
    - "REQUIREMENTS.md's coverage block reads 51 in scope / 51 complete / 0 open"
    - "REQUIREMENTS.md's cut-criterion arithmetic sums: 16 native + 10 buildable + 3 impossible = 29 skill-called tools, leaving 33 of the fork's 62 uncalled"
  artifacts:
    - path: ".planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-d-checklist.sh"
      provides: "Throwaway seven-item D-1..D-7 assertion harness (Wave 0 gap; NOT wired into CI)"
      contains: "D-1"
    - path: ".planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-d-checklist-baseline.txt"
      provides: "Recorded RED baseline proving the assertions actually fire before the fixes"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected traceability rows, coverage block, and cut-criterion arithmetic"
      contains: "Cut 2026-08-17"
  key_links:
    - from: ".planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-d-checklist.sh"
      to: ".planning/REQUIREMENTS.md"
      via: "file-scoped grep assertions for D-1, D-2, D-3, D-6, D-7"
      pattern: "REQUIREMENTS\\.md"
---

<objective>
Build the Wave 0 verification harness for this phase's mechanical half, prove it fires RED
against the current repo, then close the four audit §7 drift items that live in
`.planning/REQUIREMENTS.md` (D-1, D-2, D-3, and REQUIREMENTS.md's share of D-6 and D-7).

Purpose: criterion 3 of the phase says all seven D-items are corrected. A checklist that was
never seen to fail proves nothing, so the harness is written and run RED first. REQUIREMENTS.md
is the single largest concentration of drift and is owned exclusively by this plan so it can run
in parallel with the walkthrough track.

Output: `08.1-d-checklist.sh`, a recorded RED baseline, and a corrected `.planning/REQUIREMENTS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v0.2.0-MILESTONE-AUDIT.md
@.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-RESEARCH.md
@.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-VALIDATION.md
</context>

<canonical_numbers>
These are the ONLY correct values for the D-6 arithmetic. They are stated here once so the
same numbers land identically in every file that carries them. Do not re-derive them; do not
copy them from another planning document (STATE.md carries the corrected impossible-count `3`
but still carries the stale total `28` — it is NOT a safe template, contrary to what the
research summary implies).

- Fork manifest `tools-manifest.json`: **62** tools (unchanged).
- Stock manifest `tools-manifest.stock.json`: **38** tools as of 2026-08-19 (was 26 at the
  2026-08-17 cut).
- The six shipped skills call **29** tools.
- **16** of those already worked on stock (Phase 3 delivered them).
- **10** were buildable and missing (8 in Phase 5, 2 in Phase 7).
- **3** are provably impossible on stock: `vice_sid_get_state`, `vice_keyboard_matrix`,
  `vice_keyboard_restore`.
- 16 + 10 + 3 = 29. ✓
- The fork's other **33** tools are called by no skill (62 − 29 = 33). The figure **34** is the
  stale value that pairs with the stale total 28; it must move wherever 28 moves.
- ROADMAP.md:22's phrase "the fork's **two** exclusive *capabilities*" counts *capabilities*
  (SID register read-back; raw keyboard-matrix/RESTORE control), not tools. It stays **two**.
  Do not touch it.
</canonical_numbers>

<tasks>

<task type="auto">
  <name>Task 1: Write the throwaway D-1..D-7 checklist and record its RED baseline</name>
  <files>
    .planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-d-checklist.sh,
    .planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-d-checklist-baseline.txt
  </files>
  <read_first>
    - `.planning/v0.2.0-MILESTONE-AUDIT.md` § "## 7. Documentation Drift" — the D-1..D-7 table
      that defines the seven items
    - `.planning/phases/08.1-.../08.1-VALIDATION.md` § "Wave 0 Requirements" — this task IS that
      Wave 0 item
    - `.planning/phases/08.1-.../08.1-RESEARCH.md` § "Code Examples" and § "Don't Hand-Roll" —
      the assertion style to mirror, and the explicit instruction not to build a generic
      markdown contradiction checker
    - The `<canonical_numbers>` block above
  </read_first>
  <action>
    Create `08.1-d-checklist.sh` under this phase's directory: a plain `#!/usr/bin/env bash`
    script with `set -uo pipefail` (NOT `set -e` — every assertion must run so the operator sees
    the full picture), that runs the assertions below, prints one `PASS D-n: <label>` or
    `FAIL D-n: <label> (expected <x>, got <y>)` line per assertion, and exits 1 if any failed.

    Hard constraints on the script:
    - It is a THROWAWAY. Do not add it to `.github/workflows/ci.yml`, to
      `.claude/mcp/vice/test-gate.mjs`, to `package.json` scripts, or to any of the three
      repo-root guardrail scripts. It lives only under this phase's directory as evidence.
    - Every assertion is scoped to an explicitly named file. Never use `grep -r` over
      `.planning/`, because the script's own text contains the very strings it searches for and
      would self-satisfy every assertion.
    - Use `grep -cF` for fixed strings containing markdown emphasis asterisks or pipes, and
      `grep -cE` where a real alternation is needed. Compare counts with `-eq`.

    The assertions, by item:

    D-1 — `.planning/REQUIREMENTS.md`: count of the fixed string `| Pending |` is 0.
    D-2 — `.planning/REQUIREMENTS.md`: count of `DIRECT-06 | Phase 3 (attach) / Phase 7 (detach)`
      is 0, AND count of `| DIRECT-06 | Phase 3 | Complete (attach); detach CUT 2026-08-17` is 1.
    D-3 — `.planning/REQUIREMENTS.md`: count of `47 — 39 already complete` is 0, AND count of
      `**In scope**: 51 — **51 complete, 0 open**` is 1, AND count of `Mapped to phases: 51` is 1.
    D-4 — `.planning/ROADMAP.md`: count of the regex `^- \[ \] \*\*Phase 7` is 0, AND count of
      the fixed string `NOT complete)` is 0. (Note: the fixed string `"NOT complete" text`
      legitimately survives at ROADMAP.md line ~498 inside Phase 8.1's own criterion 3, which
      quotes the drift item. Assert on `NOT complete)` — with the closing parenthesis — so the
      quotation is not counted.)
    D-5 — `.planning/STATE.md`: count of `71%` is 0, AND count of `] 78%` is 1, AND count of
      `Total plans completed: 76` is 1, AND count of `Phase 08 is the last phase` is 0, AND
      count of the regex `^\| 07 \| 18 \|` is 1.
    D-6 — arithmetic self-consistency across all three docs:
      count of `The skills call 28 tools` in `.planning/ROADMAP.md` is 0;
      count of `The skills call **28** tools` in `.planning/REQUIREMENTS.md` is 0;
      count of `six skills call 28 tools` in `.planning/STATE.md` is 0;
      count of `34 tools` is 0 in EACH of the three files;
      count of `**3** are provably impossible` in `.planning/REQUIREMENTS.md` is 1;
      count of `two exclusive` in `.planning/ROADMAP.md` is 1 (deliberately preserved — this
      assertion guards against an over-eager two→three sweep).
    D-7 — `.planning/ROADMAP.md`: count of `38` on the same line as `tools-manifest.stock.json`
      or of the fixed string `ships **38** tools` is 1, AND count of `26 tools` is >= 1 (the
      historical figure is annotated as-of-cut, never deleted). Same two assertions against
      `.planning/REQUIREMENTS.md`.

    Then run it: `bash .planning/phases/08.1-.../08.1-d-checklist.sh > .planning/phases/08.1-.../08.1-d-checklist-baseline.txt 2>&1 || true`
    and confirm by reading the baseline file that D-1 through D-7 are ALL reported FAIL. Any
    item reporting PASS before a single fix has landed means the assertion is written wrong (it
    is matching nothing, or matching the wrong thing) — fix the assertion, not the document,
    and re-record the baseline. Prepend one line to the baseline file naming the commit sha the
    baseline was taken at (`git rev-parse HEAD`).
  </action>
  <acceptance_criteria>
    - `bash .planning/phases/08.1-*/08.1-d-checklist.sh; echo $?` prints a non-zero exit code
      at this point in the plan (RED).
    - `grep -c '^FAIL D-' .planning/phases/08.1-*/08.1-d-checklist-baseline.txt` returns 7 or
      more (one line per failing item; composite items may emit more than one line).
    - `grep -c '^PASS D-' .planning/phases/08.1-*/08.1-d-checklist-baseline.txt` returns 0.
    - `grep -c 'grep -r' .planning/phases/08.1-*/08.1-d-checklist.sh` returns 0.
    - `grep -c '08.1-d-checklist' .github/workflows/ci.yml .claude/mcp/vice/package.json` returns
      0 for both files (the harness is not wired into CI).
    - `git status --porcelain .claude .github scripts docs README.md` is empty (no source file
      touched by this task).
  </acceptance_criteria>
  <verify>
    <automated>bash .planning/phases/08.1-*/08.1-d-checklist.sh; test $? -ne 0 &amp;&amp; grep -c '^FAIL D-' .planning/phases/08.1-*/08.1-d-checklist-baseline.txt</automated>
  </verify>
  <done>The checklist exists, is not wired into CI, and its recorded baseline shows all seven D-items FAIL against the pre-fix repo.</done>
</task>

<task type="auto">
  <name>Task 2: Close D-1 and D-2 in REQUIREMENTS.md's traceability table</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 150-232 — the status-vocabulary legend (line ~155) and
      the full traceability table. Read the legend first: it explains what `Pending`, `Partial`
      and `Complete` mean, and it is prose ABOUT the vocabulary, not a table row. It must not be
      edited.
    - `.planning/REQUIREMENTS.md` line 44 — `DIRECT-06`'s own requirement text, which already
      says the detach half is "**Detach CUT 2026-08-17**" and "No longer owned by Phase 7". The
      traceability row is what contradicts it.
    - `.planning/v0.2.0-MILESTONE-AUDIT.md` §7 rows D-1 and D-2 — the audit's prescribed fixes.
    - `.planning/ROADMAP.md` § "Cut from scope (v0.2.0, 2026-08-17)" — the authoritative list of
      what was cut and why, which the 17 rows must agree with.
  </read_first>
  <action>
    D-1 — change the status cell from `Pending` to `Cut 2026-08-17` in exactly these 17
    traceability rows (currently at lines 190, 191, 203-216, and 230):
    `DERIV-02`, `DERIV-03`, `SHOT-01`, `SHOT-02`, `SHOT-03`, `SHOT-04`, `SHOT-05`, `GAIN-01`,
    `GAIN-02`, `GAIN-03`, `GAIN-04`, `GAIN-05`, `GAIN-06`, `GAIN-07`, `GAIN-08`, `GAIN-09`,
    `VERIF-03`. Each becomes e.g. `| DERIV-02 | Phase 5 | Cut 2026-08-17 |`. Leave each row's
    phase cell untouched. Do not add any other row, and do not touch the vocabulary legend at
    line ~155 even though it contains the word `Pending`.

    D-2 — replace the whole `DIRECT-06` traceability row (line 185), which currently reads
    `| DIRECT-06 | Phase 3 (attach) / Phase 7 (detach) | Partial — attach complete; detach deferred, no stock opcode |`,
    with the audit's prescribed reconciliation:
    `| DIRECT-06 | Phase 3 | Complete (attach); detach CUT 2026-08-17 — no stock detach opcode, no skill calls `vice_disk_detach` |`.
    The point of the change is that Phase 7 must stop appearing as the owner of detach: Phase 7
    is complete and never owned it after the cut.

    If the `Cut 2026-08-17` value is not already in the status legend at line ~155, add it there
    as one more vocabulary entry, worded to match the existing entries' style, so the table's
    new status value is defined rather than novel.
  </action>
  <acceptance_criteria>
    - `grep -c '| Pending |' .planning/REQUIREMENTS.md` returns 0 (was 17).
    - `grep -c '| Cut 2026-08-17 |' .planning/REQUIREMENTS.md` returns 17.
    - `grep -cF 'DIRECT-06 | Phase 3 (attach) / Phase 7 (detach)' .planning/REQUIREMENTS.md` returns 0.
    - `grep -cF '| DIRECT-06 | Phase 3 | Complete (attach); detach CUT 2026-08-17' .planning/REQUIREMENTS.md` returns 1.
    - `grep -c '^| DERIV-01 | Phase 5 | Complete |' .planning/REQUIREMENTS.md` returns 1 (an
      untouched control row — proves the edit was targeted, not a blanket sweep).
    - `bash .planning/phases/08.1-*/08.1-d-checklist.sh 2>&amp;1 | grep -E '^(PASS|FAIL) D-(1|2):'`
      shows PASS for both D-1 and D-2.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c '| Pending |' .planning/REQUIREMENTS.md)" -eq 0 &amp;&amp; test "$(grep -c '| Cut 2026-08-17 |' .planning/REQUIREMENTS.md)" -eq 17</automated>
  </verify>
  <done>All 17 cut requirements read `Cut 2026-08-17`; DIRECT-06's row attributes detach to the 2026-08-17 cut rather than to Phase 7; D-1 and D-2 report PASS on the checklist.</done>
</task>

<task type="auto">
  <name>Task 3: Close D-3, and REQUIREMENTS.md's share of D-6 and D-7</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 233-285 — the coverage block, the "Open requirements per
      phase" paragraph, and the whole "### The cut criterion" section. Read all of it in one
      pass; the numbers interlock.
    - `.planning/v0.2.0-MILESTONE-AUDIT.md` §7 rows D-3, D-6, D-7, and §2 (the audit's own
      51/51/0 derivation).
    - `.planning/phases/08.1-.../08.1-RESEARCH.md` § "Common Pitfalls" Pitfall 2 and
      § "Anti-Patterns to Avoid" — the arithmetic trap this task exists to not fall into.
    - The `<canonical_numbers>` block in this plan.
    - `.planning/phases/08-capability-honesty-and-the-install-story/08-VERIFICATION.md`
      frontmatter — confirms `BACK-05`/`DIST-01`/`DIST-02`/`DIST-03`/`SKILL-01` are complete,
      which is why "Phase 8: 5 open" is stale.
  </read_first>
  <action>
    D-3 — rewrite the coverage bullet list (currently lines ~233-247) to these values, keeping
    the existing bullet style:
    - `v0.2.0 requirements defined: 68` (unchanged).
    - The `**Cut**` bullet currently reads `21` and lumps four *halves* in with whole
      requirements. Restate it as **17 whole requirements cut** — `DERIV-02`, `DERIV-03`,
      `SHOT-01`..`SHOT-05`, `GAIN-01`..`GAIN-09`, `VERIF-03` — **plus 4 partial halves cut from
      otherwise-retained requirements** (the `DIRECT-06` detach half and the `fill` /
      `*_set_state` halves of `DERIV-01`, `DERIV-05`, `DERIV-06`), explicitly noting that halves
      are not counted as cut requirements because their parent requirement is still in scope and
      still complete.
    - Replace the whole `**In scope**: 47 — 39 already complete, **8 open**` bullet, including
      its long inline `*(revised 2026-08-18, quick task 260818-obc: ...)*` parenthetical, with
      exactly: `**In scope**: 51 — **51 complete, 0 open**` followed by a correction
      parenthetical in this file's established style, stating that the previous
      "47 in scope — 39 complete, 8 open" counted the four cut halves as whole cut requirements
      and predated Phase 8's completion, that 68 − 17 = 51, and that this was corrected
      2026-08-19 by Phase 8.1 per audit §7 D-3 and audit §2's independent recount. Preserve the
      substance of the 260818-obc note (TIME-04 moved from open to complete) inside that
      parenthetical rather than deleting the history.
    - `Mapped to phases: 51 · Unmapped: 0 ✓` (was 47).
    Then fix the "Open requirements per phase" paragraph that follows: its final clause
    `Phase 8: 5 (BACK-05, DIST-01, DIST-02, DIST-03, SKILL-01)` is now false. Change it to
    `Phase 8: **0** — `BACK-05`, `DIST-01`, `DIST-02`, `DIST-03` and `SKILL-01` are all Complete
    (2026-08-18, `08-VERIFICATION.md` 4/4)`. After this, every per-phase figure in that
    paragraph must read 0 or "cut", which is what makes the block's own total of 0 open
    internally derivable rather than asserted.

    D-6 — in "### The cut criterion" (lines ~262-281), apply the `<canonical_numbers>` values.
    Concretely: `The skills call **28** tools.` becomes `The skills call **29** tools.`;
    `- **2** are provably impossible on stock — ...` becomes `- **3** are provably impossible on
    stock — ` naming all three, `vice_sid_get_state` (SID registers are write-only in hardware),
    `vice_keyboard_matrix` (`read_ciapb()` recomputes from `keyarr` on every read) and
    `vice_keyboard_restore` (`KEYBOARD_FEED` injects buffer text only and cannot pulse
    RESTORE/NMI), keeping the existing "These need `BACK-05` + `SKILL-01` + `DIST-02`, i.e.
    honesty, not code" sentence; and `- The fork's other **34** tools are called by no skill.`
    becomes `- The fork's other **33** tools are called by no skill.` Leave the `**16**` and
    `**10**` bullets' counts alone — they are correct — but if the `**10**` bullet's prose says
    "two impossible" anywhere, fix that too. Add a one-sentence correction note in this file's
    established parenthetical style recording that the count was two until plan 05-08's
    skill-vs-manifest sweep found `vice_keyboard_restore`, and that 16 + 10 + 3 = 29.

    D-7 — line ~266-267 reads `actual `vice_*` usage against `tools-manifest.json` (62 tools)
    and \n`tools-manifest.stock.json` (26 tools):`. Do NOT renumber 26 → 38 silently. Follow this
    project's established as-of-annotation convention (see ROADMAP.md:307 and ROADMAP.md's
    Phase 4 note for the precedent): keep `(26 tools)` and append a parenthetical immediately
    after the colon stating that both counts are as-of the 2026-08-17 cut, that
    `tools-manifest.stock.json` ships **38** tools as of 2026-08-19 after Phases 5 and 7 added
    twelve, that the fork's 62 is unchanged, and that the cut arithmetic below is unaffected
    because scope is set by what the skills call rather than by manifest size.

    Do not touch anything below the `## v0.3.0 Requirements` heading — those are proposed,
    uncounted, and out of this phase's scope.
  </action>
  <acceptance_criteria>
    - `grep -cF 'In scope**: 51 — **51 complete, 0 open**' .planning/REQUIREMENTS.md` returns 1.
    - `grep -cF '47 — 39 already complete' .planning/REQUIREMENTS.md` returns 0.
    - `grep -cF 'Mapped to phases: 51' .planning/REQUIREMENTS.md` returns 1.
    - `grep -cE 'Phase 8: ?5 \(' .planning/REQUIREMENTS.md` returns 0.
    - `grep -cF 'The skills call **29** tools' .planning/REQUIREMENTS.md` returns 1.
    - `grep -cF 'The skills call **28** tools' .planning/REQUIREMENTS.md` returns 0.
    - `grep -cF '**3** are provably impossible' .planning/REQUIREMENTS.md` returns 1.
    - `grep -c 'vice_keyboard_restore' .planning/REQUIREMENTS.md` returns 1 or more.
    - `grep -c '34 tools' .planning/REQUIREMENTS.md` returns 0; `grep -cF "other **33** tools" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c '26 tools' .planning/REQUIREMENTS.md` returns 1 or more AND `grep -cF 'ships **38** tools' .planning/REQUIREMENTS.md` returns 1 (annotated, not renumbered).
    - `grep -c '^- \[ \] \*\*R2000-' .planning/REQUIREMENTS.md` is unchanged from its pre-task
      value (the v0.3.0 section was not touched).
    - `bash .planning/phases/08.1-*/08.1-d-checklist.sh 2>&amp;1 | grep -cE '^FAIL D-(1|2|3):'` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -cF 'In scope**: 51 — **51 complete, 0 open**' .planning/REQUIREMENTS.md)" -eq 1 &amp;&amp; test "$(grep -cF 'The skills call **29** tools' .planning/REQUIREMENTS.md)" -eq 1 &amp;&amp; test "$(grep -c '34 tools' .planning/REQUIREMENTS.md)" -eq 0</automated>
  </verify>
  <done>REQUIREMENTS.md's coverage reads 51/51/0 with every per-phase open count at 0 or cut; the cut-criterion arithmetic sums to 29 with 3 impossible and 33 uncalled; the 26-tool manifest figure is annotated as-of-cut with the current 38 stated. D-1, D-2, D-3 all PASS on the checklist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan edits Markdown planning documents and adds one throwaway local shell script. No process boundary, no network endpoint, no untrusted input, no credential, and no runtime code path is created, modified, or crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-01 | Tampering | `08.1-d-checklist.sh` (new local shell script) | mitigate | The script is read-only over three named files: it uses `grep` with file-scoped arguments and performs no writes, no network calls, no `eval`, and no execution of file content. Acceptance criteria assert it is absent from `.github/workflows/ci.yml` and `package.json`, so it cannot become an executed CI step. |
| T-08.1-02 | Information Disclosure | `.planning/REQUIREMENTS.md` | accept | The file is already public in a public repository and contains no secrets, credentials, hostnames, or PII. The edits correct counts and status labels only. |
| T-08.1-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install occurs in this plan. `.planning/phases/08.1-.../08.1-RESEARCH.md` § "Package Legitimacy Audit" records N/A for this phase: no new external package is added. No `[ASSUMED]`/`[SUS]` package exists to gate. |

**Justified assessment: no new threats.** This phase ships no code. `capability-registry.ts`,
`vice.ts`, `vice-proxy.ts` and every other runtime module are untouched (asserted by
`git status --porcelain .claude` being empty in Task 1's acceptance criteria). ASVS L1 has no
applicable control for a documentation correction with no input surface.
</threat_model>

<verification>
1. `bash .planning/phases/08.1-*/08.1-d-checklist.sh` reports PASS for D-1, D-2 and D-3.
   (D-4..D-7 remain FAIL — they are plan 08.1-02's ownership, except the REQUIREMENTS.md halves
   of D-6/D-7, which this plan closes and which the checklist reports per-file.)
2. `git status --porcelain .claude .github scripts docs README.md installer` is empty.
3. `git diff --stat .planning/ROADMAP.md .planning/STATE.md` shows no change introduced by this
   plan (those files belong to plan 08.1-02; their pre-existing uncommitted edits are untouched).
</verification>

<success_criteria>
- `grep -c '| Pending |' .planning/REQUIREMENTS.md` == 0 and `grep -c '| Cut 2026-08-17 |'` == 17
- `grep -cF 'In scope**: 51 — **51 complete, 0 open**' .planning/REQUIREMENTS.md` == 1
- `grep -cF 'The skills call **29** tools' .planning/REQUIREMENTS.md` == 1 and `grep -c '34 tools'` == 0
- `grep -cF 'ships **38** tools' .planning/REQUIREMENTS.md` == 1 with `26 tools` still present
- `08.1-d-checklist.sh` exists, is not referenced by CI, and its recorded baseline shows the
  pre-fix RED state
- No push to `origin`, no tag, no npm publish performed by this plan
</success_criteria>

<output>
Create `.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-01-SUMMARY.md` when done.
</output>
