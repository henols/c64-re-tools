---
phase: 08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift
plan: 03
subsystem: testing
tags: [uat, acme, c1541, d64, vice-mcp, stock-backend, walkthrough]

# Dependency graph
requires:
  - phase: 08-capability-honesty-and-the-install-story
    provides: "The stock VICE backend, VICE_BACKEND=stock, and the un-run second half of 08-HUMAN-UAT.md Test 1"
provides:
  - "08.1-WALKTHROUGH-SETUP.md: pinned artifact-under-test identity, install-route probe, corrected/widened FINDING-A1, a library-free capture target proven against c64-ram-capture's own d64 parser, and a fully wired scratch project"
  - "A throwaway .prg/.d64 under /tmp/gsd-08.1-walkthrough/artifact/ (walkthrough.d64, 174848 bytes, one non-suspicious PRG entry)"
  - "A scratch project at /tmp/gsd-08.1-walkthrough/scratch-project/ with six skills, .mcp.json pointing at local HEAD with VICE_BACKEND=stock, and a working RELEASES.json registry"
affects: [08.1-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Library-free ACME source as a substitute for acme-build's scaffold when the ACME standard library is absent from both the bare-binary and apt provisioning routes"
    - "Direct-installer (route A) fallback for a scratch project instead of claude plugin marketplace add/install, to avoid writing into machine-global, cross-session plugin state"

key-files:
  created:
    - .planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-WALKTHROUGH-SETUP.md
    - /tmp/gsd-08.1-walkthrough/artifact/walkthrough.a (library-free, not committed)
    - /tmp/gsd-08.1-walkthrough/artifact/walkthrough.prg (not committed)
    - /tmp/gsd-08.1-walkthrough/artifact/walkthrough.d64 (not committed)
    - /tmp/gsd-08.1-walkthrough/scratch-project/.mcp.json (not committed)
    - /tmp/gsd-08.1-walkthrough/scratch-project/recovery/RELEASES.json (not committed)
    - /tmp/gsd-08.1-walkthrough/scratch-project/disks/walkthrough.d64 (not committed)
  modified: []

key-decisions:
  - "Human decision: produce the capture target from a minimal, library-free ACME source instead of installing an ACME standard library or hand-editing acme-build/template.a."
  - "FINDING-A1 corrected and widened: the missing cbm/c64/* library gap is not local-machine-only -- the Debian apt acme package (the same one CI installs) ships no such library either, so CI's own environment has the identical, silently-uncaught gap."
  - "Route A (local installer) used for the scratch project instead of route B (claude plugin marketplace add/install), despite Task 1's local_path_marketplace_supported: yes verdict, because route B writes into machine-global ~/.claude/plugins/ state shared with the live orchestrating session -- an unacceptable, non-reversible side effect for a throwaway fixture. LD-1 already authorises this fallback."

requirements-completed: []

# Metrics
duration: ~25min (this continuation session; Task 1 ran in a prior session)
completed: 2026-08-19
---

# Phase 08.1 Plan 03: Walkthrough Setup Summary

**Built a library-free C64 capture target proven against c64-ram-capture's own d64 parser, and wired a scratch project to local HEAD with VICE_BACKEND=stock, after correcting FINDING-A1 to show the acme-build scaffold's missing library gap also breaks CI's own provisioning route.**

## Performance

- **Duration:** ~25 min (this continuation; Task 1 was executed in a prior session, paused at a human-action checkpoint, then resumed here)
- **Completed:** 2026-08-19T09:13Z
- **Tasks:** 3/3 complete (Task 1 in prior session; Tasks 2-3 in this session)
- **Files modified:** 1 tracked file (`08.1-WALKTHROUGH-SETUP.md`, across 3 commits); 8 throwaway files under `/tmp/gsd-08.1-walkthrough/` (none committed)

## Accomplishments
- Corrected and widened FINDING-A1 with first-hand evidence: downloaded and extracted the Debian trixie `acme` apt package (`apt-get download` + `dpkg-deb -x`, no `sudo`, no system install) and confirmed it contains no `cbm/c64/*.a` library — only `usr/bin/acme`, `usr/bin/toacme`, docs, and a man page. This is the same package `.github/workflows/ci.yml:58` installs, so CI has the identical gap, uncaught because `disasm-roundtrip.test.ts` never exercises the scaffold.
- Authored a new, self-contained, library-free `walkthrough.a` (no `!source` includes; hand-rolled `$d020`/`$d021`/`$ffd2` constants) per the human's explicit decision, built it with the skill's own driver, and wrapped it into a standard 174848-byte `.d64` with stock `/usr/bin/c1541`.
- Proved the artifact with `c64-ram-capture`'s own `d64-parse.mjs` (directory, `--json`, and `bam` subcommands) — one entry, `suspicious: false`, `chain_error: null` — not eyeballed.
- Stood up `/tmp/gsd-08.1-walkthrough/scratch-project/` (`git init`'d, six skills installed via the local installer, `.mcp.json` hand-overridden to point at this checkout's `vice-proxy.ts` with `VICE_BACKEND=stock`, no `npx`) and a working `RELEASES.json` registry listing the `walkthru` release, proved via `releases.mjs list`.
- Documented, as a named deviation, why `claude plugin marketplace add`/`install` (route B) was not used for the scratch project despite Task 1's `yes` verdict: that command persists into machine-global `~/.claude/plugins/` state shared with the live orchestrating session, which this executor judged an unacceptable side effect for a throwaway fixture — LD-1 already authorises the route A fallback taken instead.

## Task Commits

Task 1 was completed in a prior executor session (see `<completed_tasks>` in the continuation prompt) plus two documentation-only commits recording the blocker:

1. **Task 1: Pin the artifact under test and probe the install routes** - `ded90f2` (docs, prior session)
   - `ee5dda1` - docs: record FINDING-A1 (initial, blocked)
   - `c342aaa` - docs: record blocker for human decision

This session:

2. **Task 2: Assemble a throwaway capture target and prove the skill's own parser accepts it** - `580229e` (docs)
3. **Task 3: Stand up the scratch project and wire it to local HEAD with VICE_BACKEND=stock** - `1e782c5` (docs)

**Plan metadata:** (this commit, made after this summary)

## Files Created/Modified
- `.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-WALKTHROUGH-SETUP.md` - artifact identity, corrected FINDING-A1, capture-target proof, scratch-project wiring record, all deviations (repo-tracked, the only file this plan commits besides this summary)
- `/tmp/gsd-08.1-walkthrough/artifact/walkthrough.a` - library-free C64 source (77-byte `.prg` when built), NOT committed
- `/tmp/gsd-08.1-walkthrough/artifact/walkthrough.prg`, `walkthrough.d64` - built capture target, NOT committed
- `/tmp/gsd-08.1-walkthrough/scratch-project/.mcp.json` - vice MCP entry pointing at local checkout, `VICE_BACKEND=stock`, NOT committed
- `/tmp/gsd-08.1-walkthrough/scratch-project/.envrc` - two-process `VICE_BACKEND` note, NOT committed
- `/tmp/gsd-08.1-walkthrough/scratch-project/recovery/RELEASES.json`, `disks/walkthrough.d64` - release registry, NOT committed

## Decisions Made
- **Human decision (authoritative, quoted in the plan continuation prompt):** produce the capture target from a minimal, library-free ACME source rather than installing a third-party ACME standard library or hand-editing `acme-build/template.a`. Rationale honored: criterion 1 tests `c64-ram-capture`, not `acme-build`, so the substitution costs nothing in criterion-1 evidence quality, and installing an upstream stdlib was explicitly rejected as needlessly invasive.
- **This executor's own judgment call:** use route A (local installer) for the scratch project instead of route B (`claude plugin marketplace add`/`install`), despite Task 1's `local_path_marketplace_supported: yes` verdict. Verified first-hand that marketplace registration persists into `~/.claude/plugins/known_marketplaces.json` and `installed_plugins.json` — machine-global, not project-scoped even with `--scope project` — and this executor is itself a `claude` process sharing that state with the live orchestrating session. Writing there for a throwaway fixture was judged an unacceptable, non-reversible side effect; LD-1's route-A fallback was already authorised for exactly this situation, so no new permission was needed, only recording.

## Deviations from Plan

### Auto-fixed / Human-directed Issues

**1. [Human decision, honored] Library-free capture target instead of the `acme-build` scaffold**
- **Found during:** Task 2 (blocked in a prior session; resumed here per the human's decision)
- **Issue:** `acme-build/template.a` `!source`s a `cbm/c64/*.a` library absent from this machine's ACME installation (both the bare binary on `$PATH` and the Debian apt package).
- **Fix:** Authored a new, self-contained `.a` with no library includes, built it with the skill's own unmodified driver.
- **Files modified:** `/tmp/gsd-08.1-walkthrough/artifact/walkthrough.a/.prg/.d64` (not committed), `08.1-WALKTHROUGH-SETUP.md` (deviation recorded explicitly).
- **Verification:** Build succeeded (77-byte `.prg`, `$0801` load address confirmed via `od`); `.d64` is exactly 174848 bytes; `c64-ram-capture`'s own `d64-parse.mjs` (`directory`, `--json`, `bam`) all accept it with `suspicious: false`.
- **Committed in:** `580229e`

**2. [Rule 4-adjacent judgment call, documented not asked] Route A used for scratch project, not route B**
- **Found during:** Task 3
- **Issue:** `claude plugin marketplace add`/`install` (route B, which Task 1 verified is supported in principle) writes into machine-global `~/.claude/plugins/` state shared across all sessions on this machine, including the live orchestrating session.
- **Fix:** Used README route A instead (`node installer/bin/cli.mjs <scratch-project>`), then hand-applied the LD-1 local-HEAD override to `.mcp.json`. This substitution was already authorised by the plan's own LD-1 fallback clause; no unauthorised deviation.
- **Files modified:** `/tmp/gsd-08.1-walkthrough/scratch-project/.mcp.json` (not committed), `08.1-WALKTHROUGH-SETUP.md` (deviation recorded with before/after JSON).
- **Verification:** `.mcp.json`'s `mcpServers.vice.args[0]` ends in `/.claude/mcp/vice/vice-proxy.ts` and exists on disk; `env.VICE_BACKEND === "stock"`; `args` does not contain `"npx"`.
- **Committed in:** `1e782c5`

**3. [Undocumented fields, surfaced as findings per Task 3's own instruction] FINDING-B1, FINDING-B2**
- **Found during:** Task 3
- **Issue:** `RELEASES.json`'s schema (top-level `schema_version`/`schema_notes`, per-release `id`/`canonical`/`disk_image`/`dumps` with `dumps` required as a present array) and `project-paths.mjs`'s `.git`-marker project-root requirement are both undocumented in `c64-ram-capture/SKILL.md` or `README.md` — learned by reading `releases.mjs`/`project-paths.mjs` source directly.
- **Fix:** N/A (these are findings about missing documentation, not bugs to fix in this plan) — recorded verbatim in `08.1-WALKTHROUGH-SETUP.md` § 3 as `FINDING-B1` and `FINDING-B2`, per Task 3's explicit instruction to surface every undocumented field this way.
- **Files modified:** `08.1-WALKTHROUGH-SETUP.md` only.
- **Committed in:** `1e782c5`

---

**Total deviations:** 1 human-directed substitution (honored as instructed), 1 judgment-call substitution (documented, already authorised by LD-1), 2 documentation-gap findings (no fix required by this plan).
**Impact on plan:** No scope creep. The judgment-call substitution (route A vs. route B) trades a small amount of route-B verification fidelity for avoiding an irreversible side effect on shared machine state; this is recorded prominently so plan 08.1-04 does not assume route B was exercised.

## Issues Encountered
None beyond what is documented above as deviations. The earlier blocker (ACME library missing) was resolved by the human's decision before this session began.

## User Setup Required
None — no external service configuration required. (The human decision that unblocked this session was already made and is recorded in the continuation prompt and in `08.1-WALKTHROUGH-SETUP.md` § 2.)

## Next Phase Readiness

Plan 08.1-04 can now drive the actual criterion-1 UAT walkthrough:
- `08.1-WALKTHROUGH-SETUP.md` names the exact artifact under test (`0e6e913e...`, local `HEAD`), with both documented install routes' distance from it measured.
- A real, capturable C64 disk image exists at `/tmp/gsd-08.1-walkthrough/artifact/walkthrough.d64`, proven against `c64-ram-capture`'s own parser.
- A scratch project at `/tmp/gsd-08.1-walkthrough/scratch-project/` is wired with all six skills, a `vice` MCP entry pointing at local `HEAD` with `VICE_BACKEND=stock`, and a `RELEASES.json` registry listing `walkthru`.
- Plan 08.1-04 should quote, as the run's stated limitations: (a) the tested artifact is local `HEAD`, not a published release (LD-1), (b) the capture target came from a library-free hand-rolled source rather than the documented `acme-build` scaffold (FINDING-A1, corrected/widened), and (c) the scratch project was wired via route A + a hand-edit rather than the plugin-marketplace route, for the machine-global-state reason recorded in § 3.
- No blockers remain for 08.1-04 to proceed.

---
*Phase: 08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift*
*Completed: 2026-08-19*

## Self-Check: PASSED

All claimed files exist on disk and all claimed commits (`ded90f2`, `ee5dda1`, `c342aaa`, `580229e`, `1e782c5`) are present in `git log --oneline --all`.
