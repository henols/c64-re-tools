---
phase: 08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-WALKTHROUGH-SETUP.md
  - /tmp/gsd-08.1-walkthrough/artifact/walkthrough.a
  - /tmp/gsd-08.1-walkthrough/artifact/walkthrough.prg
  - /tmp/gsd-08.1-walkthrough/artifact/walkthrough.d64
  - /tmp/gsd-08.1-walkthrough/scratch-project/.mcp.json
  - /tmp/gsd-08.1-walkthrough/scratch-project/.claude/skills/
  - /tmp/gsd-08.1-walkthrough/scratch-project/recovery/RELEASES.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "The exact artifact criterion 1 will test is named by 40-character commit sha, and its distance from both documented install routes is recorded as a number"
    - "A throwaway C64 program exists that the c64-ram-capture skill's own d64 parser accepts, and no part of it is committed to the repository"
    - "A scratch project outside the repository has the six skills installed and a vice MCP entry that resolves to the local checkout with VICE_BACKEND=stock"
    - "The release registry c64-ram-capture's write-set requires exists in the scratch project and lists the throwaway release"
  artifacts:
    - path: ".planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-WALKTHROUGH-SETUP.md"
      provides: "Artifact identity, install-route probe verdict, scratch-project wiring record, and every deviation from README's documented route"
      contains: "tested_artifact_sha"
    - path: "/tmp/gsd-08.1-walkthrough/artifact/walkthrough.d64"
      provides: "A capture target the skill's documented disk-first procedure can drive"
    - path: "/tmp/gsd-08.1-walkthrough/scratch-project/.mcp.json"
      provides: "A vice MCP server entry pointing at the local checkout with VICE_BACKEND=stock"
  key_links:
    - from: "/tmp/gsd-08.1-walkthrough/scratch-project/.mcp.json"
      to: ".claude/mcp/vice/vice-proxy.ts"
      via: "absolute path in the vice server entry's args"
      pattern: "vice-proxy\\.ts"
    - from: "/tmp/gsd-08.1-walkthrough/scratch-project/recovery/RELEASES.json"
      to: "/tmp/gsd-08.1-walkthrough/artifact/walkthrough.d64"
      via: "release entry disk_image field"
      pattern: "walkthrough\\.d64"
---

<objective>
Build everything the criterion-1 walkthrough needs, and record honestly what is being tested.

Purpose: `08-HUMAN-UAT.md` Test 1's second half has never been run. Before it can be run, three
things must exist that do not exist today: an unambiguous statement of WHICH artifact is under
test (research found neither documented install route reaches the code with the stock backend in
it), a C64 program to capture (this repo ships no `.prg`/`.d64` fixture), and a project outside
this repository with the plugin installed. This plan produces all three and writes down every
place it deviates from README's documented route, so plan 08.1-04 can record the run's
limitations without inventing them after the fact.

Output: `08.1-WALKTHROUGH-SETUP.md`, a throwaway `.prg`/`.d64` under `/tmp`, and a wired scratch
project.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-RESEARCH.md
@.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-VALIDATION.md
@.planning/phases/08-capability-honesty-and-the-install-story/08-HUMAN-UAT.md
@README.md
@.claude/skills/c64-ram-capture/SKILL.md
@.claude/skills/acme-build/SKILL.md
</context>

<locked_decisions>
Two decisions were made by the user on 2026-08-19, before planning. They are NOT open questions
and must not be re-litigated by the executor.

**LD-1 — the install route is the local checkout at current `HEAD`.**
- NO task in this phase may `git push`, create a tag, or run `npm publish`. Pushing `main`
  auto-publishes via CI; that is explicitly out of scope for this phase.
- The walkthrough installs from the local checkout: a local-path plugin marketplace if Claude
  Code supports it (UNVERIFIED — Task 1 probes it), otherwise direct `.mcp.json` wiring in a
  scratch project pointing at the local checkout.
- The UAT record must state plainly that the tested artifact was local `HEAD` at a named commit
  sha, not a published release, and must name that as the run's stated limitation.

**LD-2 — the run is agent-driven and labelled as such.**
- A live Claude Code session (not a script) performs the install, sets `VICE_BACKEND=stock`, and
  drives `c64-ram-capture` end to end against genuine stock VICE.
- The UAT result records `driven_by: agent-proxy`. An agent session satisfies the mechanism
  `why_human` names (no *script* can substitute for a live session); a human witness is strictly
  stronger evidence and was not obtained. Both facts get said plainly in the record.
- The result must be `pass` or `fail` — never `pending`.
</locked_decisions>

<environment_facts>
Verified during planning on this machine. Re-confirm each before relying on it — the research's
"valid until" note is short.

- `/usr/bin/x64sc` is genuine unpatched stock VICE 3.9. `/usr/local/bin/x64sc` is the fork build
  and SHADOWS bare `x64sc` on `PATH`. Always invoke stock by absolute path.
- `-default` must precede `-binarymonitor` on the stock launch line or the monitor never binds.
- `/usr/bin/c1541` (stock VICE's disk-image tool) is present, so a `.d64` can be built locally.
  `/usr/local/bin/c1541` is the fork's; prefer `/usr/bin/c1541` for consistency.
- ACME 0.97 "Zem" is at `~/.local/bin/acme`.
- `claude` is on `PATH` and supports `-p` / `--print` headless mode.
- This machine is NOT inside a container (`/.dockerenv` absent, `/proc/1/cgroup` is
  `0::/init.scope`), so the MCP server and the host broker share one environment. README's "Two
  processes read it" still applies; record that they coincide here rather than assuming it away.
- `origin/main` is far behind local `HEAD`, and the last npm publish of `@henols/vice-mcp`
  predates Phases 7 and 8. Task 1 re-measures both rather than trusting these numbers.
- Scratch root for this phase: `/tmp/gsd-08.1-walkthrough/`. Nothing produced under it is
  committed.
</environment_facts>

<tasks>

<task type="auto">
  <name>Task 1: Pin the artifact under test and probe the install routes</name>
  <files>.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-WALKTHROUGH-SETUP.md</files>
  <read_first>
    - `README.md` lines 18-62 — the two documented install routes, A (`npx @henols/c64-re-tools`)
      and B (`/plugin marketplace add henols/c64-re-tools`). This is the prose criterion 1 says a
      person must be able to install from.
    - `.planning/phases/08.1-.../08.1-RESEARCH.md` section "Architecture Patterns" (the
      documented-route-versus-actual-artifact diagram) and "Common Pitfalls" Pitfall 4 — the
      trap of running `npx @henols/c64-re-tools` today, watching it succeed, and calling it
      proof while never reaching the `VICE_BACKEND=stock` code path at all.
    - `installer/bin/cli.mjs` lines 1-95 — in particular `viceServerEntry(vendor)`, which wires
      `npx -y @henols/vice-mcp@<MCP_VERSION>`. This is why route A alone cannot reach local
      `HEAD` even when run from the local checkout.
    - The `<locked_decisions>` block above.
  </read_first>
  <action>
    Measure, do not assume. Run and capture the literal output of each of:
    - `git rev-parse HEAD` and `git rev-parse --short HEAD`
    - `git status --porcelain` (the tested tree's dirty files, if any — a dirty tree is itself a
      limitation and must be recorded)
    - `git rev-list --count origin/main..HEAD` and `git log origin/main -1 --format='%H %ad %s' --date=iso`
    - `npm view @henols/vice-mcp version --json` and `npm view @henols/vice-mcp time.modified --json`
    - `node --version`, `/usr/bin/x64sc --version`, `/usr/bin/c1541 --help 2>&1 | head -3`,
      `acme --version`
    - `grep -n 'MCP_VERSION' installer/bin/cli.mjs` — the version route A would pin

    Probe LD-1's preferred route: does this Claude Code build support adding a plugin marketplace
    from a local filesystem path? Run `claude plugin --help 2>&1` and `claude --help 2>&1`, and
    search the output for a marketplace subcommand and for any statement about local paths. Do
    NOT run `/plugin marketplace add henols/c64-re-tools` against the remote — that would install
    the stale `origin/main` and is exactly Pitfall 4. Record a verdict of `yes` or `no` with the
    literal help text that justifies it. `no` is a perfectly good answer; the fallback (direct
    `.mcp.json` wiring, Task 3) is already authorised by LD-1.

    Write `08.1-WALKTHROUGH-SETUP.md` with a YAML frontmatter block carrying at minimum these
    keys — exact key names, because plan 08.1-04 reads them:
    `tested_artifact_sha` (40 chars), `tested_artifact_short_sha`, `tested_tree_dirty` (true or
    false), `origin_behind_by` (integer), `origin_main_sha`, `npm_latest_vice_mcp` (version
    string), `npm_latest_published` (ISO timestamp), `installer_pinned_mcp_version`,
    `local_path_marketplace_supported` (yes or no), `stock_vice_path`, `stock_vice_version`,
    `node_version`, `acme_version`, `driven_by: agent-proxy`.

    Then a body section "## 1. What is actually under test" stating in plain prose: the artifact
    is the local working checkout at `<sha>`; neither documented install route reaches it today
    (route A resolves to `@henols/vice-mcp@<npm version>` published `<timestamp>`, which predates
    Phases 7 and 8; route B resolves to `origin/main` at `<sha>`, `<N>` commits behind); per LD-1
    no push, tag, or publish will be performed to close that gap; and therefore the run's stated
    limitation is that it exercises local `HEAD`, not a published release.
  </action>
  <acceptance_criteria>
    - `.planning/phases/08.1-*/08.1-WALKTHROUGH-SETUP.md` exists.
    - `grep -cE '^tested_artifact_sha: [0-9a-f]{40}$' .planning/phases/08.1-*/08.1-WALKTHROUGH-SETUP.md` returns 1.
    - `grep -cE '^origin_behind_by: [0-9]+$' .planning/phases/08.1-*/08.1-WALKTHROUGH-SETUP.md` returns 1.
    - `grep -cE '^local_path_marketplace_supported: (yes|no)$' .planning/phases/08.1-*/08.1-WALKTHROUGH-SETUP.md` returns 1.
    - `grep -cE '^npm_latest_vice_mcp: ' .planning/phases/08.1-*/08.1-WALKTHROUGH-SETUP.md` returns 1.
    - `grep -cE '^driven_by: agent-proxy$' .planning/phases/08.1-*/08.1-WALKTHROUGH-SETUP.md` returns 1.
    - `grep -cF 'not a published release' .planning/phases/08.1-*/08.1-WALKTHROUGH-SETUP.md` returns 1 or more.
    - The tested sha in the file equals the live one: `test "$(grep -oE '[0-9a-f]{40}' .planning/phases/08.1-*/08.1-WALKTHROUGH-SETUP.md | head -1)" = "$(git rev-parse HEAD)"` succeeds.
    - `git log --oneline origin/main..HEAD | wc -l` has NOT changed from the recorded
      `origin_behind_by` value, i.e. nothing was pushed: `git rev-parse origin/main` still equals
      the recorded `origin_main_sha`.
    - `git tag --points-at HEAD | wc -l` returns 0 (no tag created).
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -oE '[0-9a-f]{40}' .planning/phases/08.1-*/08.1-WALKTHROUGH-SETUP.md | head -1)" = "$(git rev-parse HEAD)" &amp;&amp; grep -qE '^local_path_marketplace_supported: (yes|no)$' .planning/phases/08.1-*/08.1-WALKTHROUGH-SETUP.md &amp;&amp; test "$(git tag --points-at HEAD | wc -l)" -eq 0</automated>
  </verify>
  <done>The artifact under test is pinned by sha with both documented routes' distance from it measured; the local-path marketplace question has a yes/no verdict backed by literal help output; nothing was pushed, tagged or published.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble a throwaway capture target and prove the skill's own parser accepts it</name>
  <files>
    /tmp/gsd-08.1-walkthrough/artifact/walkthrough.a,
    /tmp/gsd-08.1-walkthrough/artifact/walkthrough.prg,
    /tmp/gsd-08.1-walkthrough/artifact/walkthrough.d64,
    .planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-WALKTHROUGH-SETUP.md
  </files>
  <read_first>
    - `.claude/skills/acme-build/template.a` — the scaffold to copy. It has no `!to` directive on
      purpose (the driver passes `-o`), starts at `* = $0801`, and carries a BASIC `SYS` stub so
      `LOAD"*",8,1` + `RUN` reaches the code.
    - `.claude/skills/acme-build/SKILL.md` — the documented build invocation and the ACME library
      probe order.
    - `.claude/skills/c64-ram-capture/SKILL.md` section "Read the disk first" — `d64-parse.mjs`'s
      `directory` and `bam` subcommands and the `suspicious` / `suspicious_reasons` fields the
      `--json` output carries.
    - `.planning/phases/08.1-.../08.1-RESEARCH.md` section "Don't Hand-Roll", the
      "Producing a test program to capture" row — no committed fixture; assemble ad hoc, keep out
      of git.
  </read_first>
  <action>
    Create `/tmp/gsd-08.1-walkthrough/artifact/`. Copy `.claude/skills/acme-build/template.a`
    there as `walkthrough.a` unmodified — an unmodified scaffold is the point: it proves the
    shipped template assembles, and it keeps the capture target trivially reproducible.

    Build it with the skill's own driver:
    `node .claude/skills/acme-build/scripts/acme.mjs build /tmp/gsd-08.1-walkthrough/artifact/walkthrough.a`.
    Capture the driver's full stdout/stderr. If the build fails, that is a real finding about the
    `acme-build` skill — record it as `FINDING-A<n>` in the setup document and stop; do not
    hand-edit the template to make it build.

    Wrap the `.prg` into a `.d64` so the walkthrough can follow `c64-ram-capture`'s documented
    disk-first order (`d64-parse directory` → `vice_disk_attach` → `vice_autostart`) rather than
    an undocumented bare-`.prg` shortcut. Use stock VICE's own tool by absolute path:
    `/usr/bin/c1541 -format "walkthru,01" d64 /tmp/gsd-08.1-walkthrough/artifact/walkthrough.d64 -write /tmp/gsd-08.1-walkthrough/artifact/walkthrough.prg walkthru`.
    Read `/usr/bin/c1541 --help` first and adapt the argument order to what this build actually
    accepts; record the exact command line that worked in the setup document.

    Prove the artifact with the capture skill's OWN parser, not by eyeballing:
    - `node .claude/skills/c64-ram-capture/scripts/d64-parse.mjs directory --image /tmp/gsd-08.1-walkthrough/artifact/walkthrough.d64`
    - the same with `--json`, and confirm the single entry has `suspicious: false` and a null
      `chain_error`
    - `node .claude/skills/c64-ram-capture/scripts/d64-parse.mjs bam --image /tmp/gsd-08.1-walkthrough/artifact/walkthrough.d64`

    Confirm the `.prg` carries the expected `$0801` load address: the first two bytes must be
    `01 08` (little-endian). Use `od -An -tx1 -N2` or equivalent.

    Append a "## 2. Capture target" section to `08.1-WALKTHROUGH-SETUP.md` recording the absolute
    paths, the `.prg` byte size, its sha256, the exact `c1541` command line used, and the literal
    `d64-parse` outputs.

    Nothing produced by this task may enter the repository. Do not create a `fixtures/`,
    `disks/` or `recovery/` directory inside the checkout.
  </action>
  <acceptance_criteria>
    - `test -s /tmp/gsd-08.1-walkthrough/artifact/walkthrough.prg` succeeds.
    - `od -An -tx1 -N2 /tmp/gsd-08.1-walkthrough/artifact/walkthrough.prg | tr -d ' '` outputs `0108`.
    - `test "$(stat -c%s /tmp/gsd-08.1-walkthrough/artifact/walkthrough.d64)" -eq 174848` (a standard 35-track `.d64`).
    - `node .claude/skills/c64-ram-capture/scripts/d64-parse.mjs directory --image /tmp/gsd-08.1-walkthrough/artifact/walkthrough.d64 --json` exits 0 and its JSON contains exactly one entry with `"suspicious": false`.
    - `git status --porcelain` shows no new or modified path under `.claude/`, `docs/`,
      `scripts/`, `recovery/`, `disks/` or `fixtures/`.
    - `grep -cF '## 2. Capture target' .planning/phases/08.1-*/08.1-WALKTHROUGH-SETUP.md` returns 1.
    - `grep -cE 'sha256|SHA-256' .planning/phases/08.1-*/08.1-WALKTHROUGH-SETUP.md` returns 1 or more.
  </acceptance_criteria>
  <verify>
    <automated>test "$(od -An -tx1 -N2 /tmp/gsd-08.1-walkthrough/artifact/walkthrough.prg | tr -d ' \n')" = "0108" &amp;&amp; node .claude/skills/c64-ram-capture/scripts/d64-parse.mjs directory --image /tmp/gsd-08.1-walkthrough/artifact/walkthrough.d64 --json | grep -c '"suspicious": *false'</automated>
  </verify>
  <done>An unmodified `acme-build` template assembles to a `$0801` `.prg`, is wrapped in a `.d64` that the capture skill's own parser reads as one non-suspicious entry, and nothing entered the repository.</done>
</task>

<task type="auto">
  <name>Task 3: Stand up the scratch project and wire it to local HEAD with VICE_BACKEND=stock</name>
  <files>
    /tmp/gsd-08.1-walkthrough/scratch-project/.mcp.json,
    /tmp/gsd-08.1-walkthrough/scratch-project/.claude/skills/,
    /tmp/gsd-08.1-walkthrough/scratch-project/recovery/RELEASES.json,
    .planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-WALKTHROUGH-SETUP.md
  </files>
  <read_first>
    - `README.md` section "Installing VICE, and choosing a backend", especially "Choosing the
      backend" and "**Two processes read it, each from its own environment**" — the walkthrough
      must follow this prose, and any place it is insufficient is a finding.
    - `installer/bin/cli.mjs` in full — `parseArgs`, `installSkills`, `wireMcp`,
      `viceServerEntry`, `vendorInstall`. Note that `wireMcp` writes an entry invoking
      `npx -y @henols/vice-mcp@<MCP_VERSION>`, i.e. the stale published server.
    - `.mcp.json` at the repo root — the shape of a local-checkout vice entry
      (`command: node`, `args: ["${CLAUDE_PLUGIN_ROOT}/.claude/mcp/vice/vice-proxy.ts"]`,
      `timeout: 150000`, `env.MASTRA_TELEMETRY_DISABLED: "1"`).
    - `.claude/skills/c64-ram-capture/scripts/project-paths.mjs` — `projectRoot()` walks UP for a
      `.git` marker or honours `C64RE_PROJECT_ROOT`; `dataRoot()` defaults to
      `<project root>/recovery` or `C64RE_DATA_DIR`; `registryFile()` defaults to
      `<dataRoot>/RELEASES.json` or `C64RE_REGISTRY`.
    - `.claude/skills/c64-ram-capture/scripts/releases.mjs` — `loadRegistry()`, `release(id)`,
      `assertKnownRelease`, `releaseDir`, and the `schema_notes` / `schema_version` / `releases`
      top-level shape it expects.
    - The `<locked_decisions>` and `<environment_facts>` blocks above.
  </read_first>
  <action>
    Create `/tmp/gsd-08.1-walkthrough/scratch-project/` and run `git init` inside it — required,
    because `project-paths.mjs`'s `projectRoot()` walks up for a `.git` marker and would
    otherwise resolve the capture toolkit's data root to the wrong place or throw.

    Install by the route Task 1's probe selected:
    - If `local_path_marketplace_supported: yes`, follow README route B against the LOCAL
      checkout path (never `henols/c64-re-tools`, which resolves to stale `origin/main`), and
      record the literal commands.
    - Otherwise (LD-1's authorised fallback), reproduce README route A faithfully from the local
      checkout by running the LOCAL installer:
      `node <repo-abs>/installer/bin/cli.mjs /tmp/gsd-08.1-walkthrough/scratch-project`.
      Capture its full output. This copies the six skills and writes a `vice` entry — but the
      entry it writes invokes the stale published `@henols/vice-mcp`. That is the gap LD-1
      answers.

    Then apply the LD-1 local-`HEAD` override to `/tmp/gsd-08.1-walkthrough/scratch-project/.mcp.json`,
    editing ONLY the `mcpServers.vice` entry:
    - `command`: `node`
    - `args`: a single-element array holding the ABSOLUTE path to
      `<repo-abs>/.claude/mcp/vice/vice-proxy.ts` (no `${CLAUDE_PLUGIN_ROOT}` — that variable is
      only defined in plugin mode)
    - `timeout`: `150000`
    - `env`: `MASTRA_TELEMETRY_DISABLED` = `"1"`, `VICE_BACKEND` = `"stock"`,
      `CLAUDE_PROJECT_DIR` = the scratch project's absolute path
    Record the before/after of that entry verbatim in the setup document. This override IS the
    run's headline limitation and plan 08.1-04 will quote it.

    Satisfy README's "Two processes read it" instruction for the host broker half as well: write
    an `/tmp/gsd-08.1-walkthrough/scratch-project/.envrc`-style shell snippet (or an explicit
    exported-variable note) setting `VICE_BACKEND=stock` for whatever shell launches the broker,
    and record that on THIS machine both processes share one environment because it is not
    containerised — state that as an observed fact, not as a reason to skip the step.

    Create the release registry the capture toolkit needs. `write-set --release <id>` fails on an
    unknown id, and a fresh project has no registry. Read `releases.mjs` and `project-paths.mjs`
    for the exact required shape, then create
    `/tmp/gsd-08.1-walkthrough/scratch-project/recovery/RELEASES.json` with `schema_version`,
    `schema_notes`, and one `releases[]` entry whose `id` is `walkthru` and whose `disk_image`
    points at the throwaway `.d64` from Task 2 (copy the `.d64` into the scratch project first if
    the registry's `disk_image` must be project-relative — `disksRoot()` defaults to the project
    root). Prove it with `node <scratch>/.claude/skills/c64-ram-capture/scripts/releases.mjs list`.

    Every field you had to supply that is NOT documented in `c64-ram-capture/SKILL.md` or
    `README.md` is a finding: record each as `FINDING-B<n>` in the setup document with the field
    name, where you had to read the source to learn it, and one sentence on what documentation
    would have prevented it. Do not smooth these over — surfacing them is the point of the UAT.

    Do not run `npm publish`, `git push`, or `git tag` in this task or any other.
  </action>
  <acceptance_criteria>
    - `test -d /tmp/gsd-08.1-walkthrough/scratch-project/.git` succeeds.
    - `ls /tmp/gsd-08.1-walkthrough/scratch-project/.claude/skills | wc -l` returns 6.
    - `test -f /tmp/gsd-08.1-walkthrough/scratch-project/.claude/skills/c64-ram-capture/scripts/dump-artifacts.mjs` succeeds.
    - `node -e 'const c=require("/tmp/gsd-08.1-walkthrough/scratch-project/.mcp.json");console.log(c.mcpServers.vice.args[0])'` prints an absolute path ending in `/.claude/mcp/vice/vice-proxy.ts` that exists on disk.
    - `node -e 'const c=require("/tmp/gsd-08.1-walkthrough/scratch-project/.mcp.json");console.log(c.mcpServers.vice.env.VICE_BACKEND)'` prints `stock`.
    - `node -e 'const c=require("/tmp/gsd-08.1-walkthrough/scratch-project/.mcp.json");console.log(JSON.stringify(c.mcpServers.vice.args).includes("npx"))'` prints `false` (the stale published-server wiring was replaced).
    - `node /tmp/gsd-08.1-walkthrough/scratch-project/.claude/skills/c64-ram-capture/scripts/releases.mjs list` exits 0 and its output contains `walkthru`.
    - `grep -cE '^FINDING-B[0-9]+' .planning/phases/08.1-*/08.1-WALKTHROUGH-SETUP.md` returns 0 or more, and the document contains a `## 3. Scratch project and its deviations from the documented route` heading either listing findings or stating explicitly that none were needed.
    - `git status --porcelain` in the repo shows changes only under `.planning/`.
    - `git rev-parse origin/main` still equals the `origin_main_sha` recorded in Task 1, and `git tag --points-at HEAD | wc -l` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>node -e 'const c=require("/tmp/gsd-08.1-walkthrough/scratch-project/.mcp.json");const a=c.mcpServers.vice.args[0];if(!a.endsWith("/.claude/mcp/vice/vice-proxy.ts"))process.exit(1);if(c.mcpServers.vice.env.VICE_BACKEND!=="stock")process.exit(1);require("node:fs").accessSync(a)' &amp;&amp; node /tmp/gsd-08.1-walkthrough/scratch-project/.claude/skills/c64-ram-capture/scripts/releases.mjs list | grep -c walkthru</automated>
  </verify>
  <done>A scratch project outside the repo has the six skills, a `vice` MCP entry resolving to the local checkout's `vice-proxy.ts` with `VICE_BACKEND=stock`, a working release registry listing `walkthru`, and every deviation from README's documented route recorded as a numbered finding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo checkout -> scratch project at `/tmp/gsd-08.1-walkthrough/` | Files are copied out of the repository into a world-readable temp tree. Nothing flows back in. |
| scratch project `.mcp.json` -> local `vice-proxy.ts` | A configuration file names an absolute path to code that a Claude Code session will execute in plan 08.1-04. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-05 | Tampering | `/tmp/gsd-08.1-walkthrough/scratch-project/.mcp.json` | mitigate | The `args[0]` path is asserted by an acceptance criterion to end in `/.claude/mcp/vice/vice-proxy.ts` and to exist on disk, and asserted not to contain `npx` — so the entry cannot silently resolve to a fetched-at-runtime package. The executed code is this repository's own checkout at a recorded sha. |
| T-08.1-06 | Information Disclosure | `/tmp/gsd-08.1-walkthrough/` (world-readable temp tree) | accept | Nothing written there is secret: an ACME template copied verbatim from the public repo, its assembled `.prg`/`.d64`, the six public skills, and a config file containing only `VICE_BACKEND=stock` and absolute paths. No credential, token, or API key is written. |
| T-08.1-07 | Elevation of Privilege | published-artifact release path (`git push` / `npm publish`) | mitigate | LD-1 forbids push, tag and publish. Two acceptance criteria enforce it mechanically: `git rev-parse origin/main` must be unchanged, and `git tag --points-at HEAD` must be empty. The repo's CI auto-publishes on merge to `main`, so not pushing is the control. |
| T-08.1-SC | Tampering | npm/pip/cargo installs | mitigate | The only package-manager activity permitted is `npm view` (read-only metadata) and, if route A's installer is run, this project's own already-published packages. No third-party package is introduced. `08.1-RESEARCH.md` section "Package Legitimacy Audit" records N/A for this phase, so there is no `[ASSUMED]`/`[SUS]` entry to gate. If the executor finds itself about to install a package not owned by this project, stop and report instead. |

**Assessment: no new attack surface.** No code is written, no auth path, input validator, or
cryptographic routine is touched, and `capability-registry.ts` remains untouched. The two
non-trivial rows above are process controls over a release path, not new application surface.
</threat_model>

<verification>
1. `.planning/phases/08.1-*/08.1-WALKTHROUGH-SETUP.md` exists with all frontmatter keys named in
   Task 1's acceptance criteria, plus sections 1, 2 and 3.
2. `git status --porcelain` in the repo shows changes only under `.planning/`.
3. `git rev-parse origin/main` is unchanged from the value recorded in Task 1, and
   `git tag --points-at HEAD` is empty.
4. `node /tmp/gsd-08.1-walkthrough/scratch-project/.claude/skills/c64-ram-capture/scripts/releases.mjs list`
   exits 0 and names `walkthru`.
</verification>

<success_criteria>
- The commit sha under test is recorded and equals `git rev-parse HEAD`
- Both documented install routes' distance from that sha is recorded as a measured number
- `local_path_marketplace_supported` has a `yes`/`no` verdict backed by literal help output
- A `$0801` `.prg` and a 174848-byte `.d64` exist under `/tmp`, and `d64-parse.mjs` reads exactly
  one non-suspicious directory entry
- The scratch project has six skills, a `vice` entry pointing at the local checkout with
  `VICE_BACKEND=stock` and no `npx`, and a registry listing `walkthru`
- No file produced by this plan entered the repository outside `.planning/`
- No push to `origin`, no tag, no npm publish
</success_criteria>

<output>
Create `.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-03-SUMMARY.md` when done.
</output>
