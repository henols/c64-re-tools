---
phase: 08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift
plan: 04
subsystem: testing
tags: [uat, stock-vice, binary-monitor, broker, drive8type, honesty, vice-mcp]

# Dependency graph
requires:
  - phase: 08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift
    plan: 03
    provides: "A proven library-free .d64 capture target and a scratch project wired to local HEAD with VICE_BACKEND=stock"
provides:
  - "08.1-WALKTHROUGH-EVIDENCE.md: the literal transcript, backend proof, and root-cause diagnosis of the one criterion-1 walkthrough that had never been run"
  - "08-HUMAN-UAT.md Test 1 resolved: failed (never pending), driven_by: agent-proxy, tested_artifact_sha named"
  - "A confirmed, reproducible defect (FINDING-C1): the broker launches stock x64sc with Drive8Type=0, blocking every disk-based c64-ram-capture walkthrough against genuine stock VICE"
affects: [08.1-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Headless `claude -p --permission-mode bypassPermissions` as the preferred (and, here, sufficient) driving rung for an unattended UAT walkthrough"
    - "Read-only diagnosis via VICE's -remotemonitor text-monitor port (resourceget/resourceset/load), used to root-cause a binary-monitor-observed failure without contending with the single-client binary-monitor constraint"

key-files:
  created:
    - .planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-WALKTHROUGH-EVIDENCE.md
  modified:
    - .planning/phases/08-capability-honesty-and-the-install-story/08-HUMAN-UAT.md
    - .planning/phases/08-capability-honesty-and-the-install-story/08-VERIFICATION.md
    - .planning/phases/08-capability-honesty-and-the-install-story/08-VALIDATION.md

key-decisions:
  - "Recorded the walkthrough's real outcome (fail) rather than adjusting the procedure or fixing broker-launch.mts to force a pass -- the outcome vocabulary's own anti-gaming rule ('if the documented procedure does not work, that is the finding') was honored literally."
  - "Corrected the driving session's own incorrect self-diagnosis ('missing 1541 ROMs') in the open rather than silently replacing it, per the phase's established correction convention (see 08.1-03-SUMMARY.md's FINDING-A1 precedent)."
  - "Used the idle -remotemonitor text-monitor port for read-only root-cause diagnosis after the driving session's own MCP connection had already closed, rather than opening a second binary-monitor client -- avoided the exact DoS/wedge-lookalike hazard T-08.1-10 names."

requirements-completed: []

# Metrics
duration: ~90min
completed: 2026-08-19
---

# Phase 08.1 Plan 04: Criterion-1 UAT Walkthrough Summary

**Drove `c64-ram-capture` end to end via a headless Claude Code session against genuine `/usr/bin/x64sc`; the capture failed on a confirmed, reproducible defect (stock x64sc boots with `Drive8Type=0` and no MCP tool can fix it) rather than completing, and `08-HUMAN-UAT.md` Test 1 now reads `failed`, never `pending`.**

## Performance

- **Duration:** ~90 min
- **Started:** 2026-08-19T09:00Z (approx, continuing plan 08.1-03's scratch project)
- **Completed:** 2026-08-19T10:42Z
- **Tasks:** 2/2 complete
- **Files modified:** 4 (1 created, 3 modified)

## Accomplishments
- Ran the walkthrough that had never been run: a headless `claude -p --permission-mode bypassPermissions` session (rung 1, no fallback needed) drove `c64-ram-capture`'s documented procedure end to end from the scratch project against genuine `/usr/bin/x64sc`, confirmed via three independent sources (broker log, `epoch.json`, live `ps` argv) rather than trusting `vice_ping`'s own misleading `resolvedBinaryPath` field.
- Found, root-caused, and confirmed the fix for a real, load-bearing defect: this project's own broker launches stock `x64sc` with `Drive8Type=0` (NONE) by default, so no drive answers unit 8 and `LOAD"*",8,1` correctly returns `?DEVICE NOT PRESENT ERROR` — not a wedge, not a bad disk image, not a fork/stock backend mismatch. Confirmed live with a disposable standalone probe: `-drive8type 1541` at launch lets the monitor's own `load "walkthru" 8` succeed for exactly the expected 75 bytes.
- Corrected the driving session's own incorrect self-diagnosis ("missing 1541 drive ROMs") in the open — the launch log actually shows the 1541 ROM loading successfully — per this phase's established "correct, don't silently erase" convention.
- Found and recorded a second, independent defect: `vice_ping`'s `resolvedBinaryPath` field is a one-time, static PATH probe done at MCP-server-process startup, unrelated to which binary the broker's on-demand pool actually leases — it reports the fork's path even when the backend and the actually-leased instance are genuinely stock.
- Closed `08-HUMAN-UAT.md` Test 1 (`pending` since 2026-08-18) as `failed`, carrying `driven_by: agent-proxy`, the pinned local-checkout sha, `tested_artifact_route: local-checkout-HEAD`, and all six required prose statements (what was tested, who drove it, how it was wired, what the backend was, the capture result, every finding) — while preserving the prior install-only-half record and its `why_human` block unedited.
- Pointed `08-VERIFICATION.md`'s `human_verification` entry and truth 3's row at the Phase 8.1 resolution instead of duplicating the record, and left `08-VALIDATION.md`'s criterion-2 keys (`status: audited`, `nyquist_compliant: true`) untouched while recording `manual_only_open_note` naming the defect (`manual_only_open` stays `1` since the outcome was a failure, not a resolution).

## Task Commits

1. **Task 1: Drive c64-ram-capture end to end against genuine stock VICE and capture the transcript** - `af2599f` (docs)
2. **Task 2: Record the result in 08-HUMAN-UAT.md and point Phase 8's other artifacts at it** - `d7dbe4e` (docs)

**Plan metadata:** (this commit, made after this summary)

## Files Created/Modified
- `.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-WALKTHROUGH-EVIDENCE.md` - literal transcript, backend proof (ping/diagnose + argv cross-check), step-by-step output, failure section with exact command + stderr, and four `FINDING-C` entries
- `.planning/phases/08-capability-honesty-and-the-install-story/08-HUMAN-UAT.md` - Test 1 resolved to `failed`, `driven_by: agent-proxy`, sha/route recorded, prior install-half preserved
- `.planning/phases/08-capability-honesty-and-the-install-story/08-VERIFICATION.md` - `human_verification` entry and truth 3's row now point at the Phase 8.1 resolution
- `.planning/phases/08-capability-honesty-and-the-install-story/08-VALIDATION.md` - `manual_only_open_note` added; `manual_only_open` (still `1`), `status: audited` and `nyquist_compliant: true` untouched

## Decisions Made
- **This executor's own judgment call:** did not attempt to fix `broker-launch.mts` (the confirmed root cause) and re-run to force a `pass`. The plan's own outcome vocabulary explicitly forbids "adjusting the procedure mid-run to manufacture a pass," and fixing the broker's launch-argument construction is an architectural change to production code, out of scope for a documentation/evidence-recording plan (Rule 4 territory — left as `FINDING-C1` for its own follow-up).
- **This executor's own judgment call:** used the already-idle `-remotemonitor` text-monitor port (a separate port from the binary monitor the walkthrough used) for read-only root-cause diagnosis, only after confirming no live binary-monitor client remained connected — avoiding the exact single-client contention/wedge-lookalike hazard T-08.1-10 names, while still getting a definitive, source-confirmed root cause instead of guessing from log text alone.
- **This executor's own judgment call:** corrected the driving session's "missing ROMs" self-diagnosis in the open rather than silently overwriting it, consistent with 08.1-03's own precedent for handling a wrong initial finding.

## Deviations from Plan

### Auto-fixed / Judgment-call Issues

**1. [Judgment call, documented not asked] Reused a pre-existing broker/scratch-project state left over from an interrupted prior attempt**
- **Found during:** Task 1 setup
- **Issue:** The scratch project already had a running broker (pid 40843) and two warm `x64sc` instances, plus a `walkthrough-prompt.txt`, left over from a prior session that had set up the walkthrough prompt but never produced a transcript.
- **Fix:** Verified the broker's heartbeat was fresh and its supervisor state pointed at the correct scratch project before reusing it; ran a smoke test first (list available `vice_*` tools) to confirm the MCP wiring actually worked before committing to the full run.
- **Files modified:** None (runtime state only, under `/tmp`).
- **Verification:** Smoke test returned the expected 37-tool stock surface; the full run then completed end to end.

**2. [Rule 1-adjacent, documented] Corrected the driving session's own diagnosis**
- **Found during:** Task 1, post-run investigation
- **Issue:** The headless session's final report claimed "VICE was launched without 1541 drive ROMs" as the root cause — incorrect; the ROM loaded successfully per the launch log.
- **Fix:** Investigated further via the idle text-monitor port, found the true root cause (`Drive8Type=0`), and recorded both the original (wrong) claim and the corrected finding in `08.1-WALKTHROUGH-EVIDENCE.md`, explicitly superseding rather than deleting.
- **Files modified:** `08.1-WALKTHROUGH-EVIDENCE.md`.
- **Committed in:** `af2599f`.

**3. [Scope boundary respected] Did not fix the confirmed `Drive8Type` defect**
- **Found during:** Task 1
- **Issue:** `broker-launch.mts`'s stock `buildViceArgs()` never sets a drive type, and `stock-machine.ts`'s `vice_disk_attach`/`vice_autostart` handlers never resource-set one either, so every stock walkthrough will hit this same block until fixed.
- **Fix:** Not fixed by this plan — recorded as `FINDING-C1` in the evidence file and referenced from `08-HUMAN-UAT.md`/`08-VALIDATION.md`, left to size its own follow-up work, per this plan's explicit scope boundary against editing source to force a pass.
- **Files modified:** None (finding only).

---

**Total deviations:** 1 reused-state judgment call (verified safe before use), 1 self-correction of a wrong sub-agent diagnosis, 1 explicit non-fix (scope boundary respected).
**Impact on plan:** None — the plan's actual job (run the walkthrough, record what happened, resolve the pending UAT entry honestly) is complete. The confirmed defect (`FINDING-C1`) is real, load-bearing, and blocks every future disk-based stock-VICE walkthrough until a follow-up phase fixes the broker's launch arguments or the stock handlers' resource-setting.

## Issues Encountered
None beyond what is documented above as deviations/findings.

## User Setup Required
None.

## Next Phase Readiness

Plan 08.1-05 (if any further drift-closing work remains) can build on this plan's record:
- `08-HUMAN-UAT.md` Test 1 is closed (`failed`, not `pending`), with the exact defect and its confirmed fix named.
- `FINDING-C1` (broker's `Drive8Type=0` default) is a concrete, actionable, reproducible item for a future phase that wants an actual passing stock-VICE capture — the fix path is understood and verified (`-drive8type <N>` at launch, or a `resourceset` before `AUTOSTART` in `stock-machine.ts`'s handlers).
- No blockers remain for closing out Phase 8.1's remaining audit items.

---
*Phase: 08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift*
*Completed: 2026-08-19*

## Self-Check: PASSED

All claimed files exist on disk and both claimed commits (`af2599f`, `d7dbe4e`) are present in `git log --oneline --all`.
