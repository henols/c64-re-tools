---
phase: 08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift
plan: 02
type: execute
wave: 2
depends_on: ["08.1-01"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-criterion-2-evidence.txt
autonomous: true
requirements: []
must_haves:
  truths:
    - "ROADMAP.md's Phase 7 entry is checked and its text agrees with the same file's own progress table and with 07-VERIFICATION.md"
    - "ROADMAP.md's milestone overview arithmetic sums: 16 + 10 + 3 = 29 skill-called tools, 33 of the fork's 62 uncalled"
    - "ROADMAP.md's dated historical notes carry appended as-of corrections rather than rewritten history"
    - "STATE.md's body figures agree with its own frontmatter (7 of 9 phases, 78%) and do not claim the milestone is finished"
    - "Phase 8's 08-VALIDATION.md is confirmed to read status: audited / nyquist_compliant: true, so no phase in the milestone is non-compliant"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Corrected Phase 7 entry, corrected milestone arithmetic, as-of annotations for D-6/D-7 historical prose"
      contains: "Phase 7: Cycle Timing and Wedge Triage"
    - path: ".planning/STATE.md"
      provides: "Body figures reconciled with frontmatter and with Phase 8.1's insertion"
      contains: "78%"
    - path: ".planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-criterion-2-evidence.txt"
      provides: "Recorded proof that criterion 2 was already satisfied by commit 4306338, verified rather than re-authored"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/07-cycle-timing-and-wedge-triage/07-VERIFICATION.md"
      via: "Phase 7 completion claim must match the verification report's status"
      pattern: "Phase 7"
    - from: ".planning/STATE.md"
      to: ".planning/ROADMAP.md"
      via: "progress percentage and phase count must match the roadmap's progress table"
      pattern: "78%"
---

<objective>
Close the remaining audit §7 drift items — D-4 (ROADMAP.md's Phase 7 entry), D-5 (STATE.md's
figures), and ROADMAP.md's and STATE.md's shares of D-6 and D-7 — and record the evidence that
criterion 2 was already satisfied before this phase started.

Purpose: ROADMAP.md currently contradicts its own progress table about Phase 7, and STATE.md
contradicts its own frontmatter about progress. Both would start the next audit from a false
picture. This plan runs after 08.1-01 so that the D-6 arithmetic correction is applied by one
executor holding the whole picture, rather than split across parallel agents.

Output: corrected `.planning/ROADMAP.md` and `.planning/STATE.md`, plus a recorded criterion-2
evidence file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v0.2.0-MILESTONE-AUDIT.md
@.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-RESEARCH.md
@.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-01-SUMMARY.md
</context>

<canonical_numbers>
Identical to plan 08.1-01's block — restated so this executor does not have to fetch them, and
so the two plans cannot drift apart.

- Fork manifest `tools-manifest.json`: **62** tools (unchanged).
- Stock manifest `tools-manifest.stock.json`: **38** as of 2026-08-19 (was 26 at the 2026-08-17 cut).
- The six shipped skills call **29** tools = **16** already-working + **10** buildable + **3** impossible.
- The 3 impossible: `vice_sid_get_state`, `vice_keyboard_matrix`, `vice_keyboard_restore`.
- The fork's other **33** tools are called by no skill (62 - 29 = 33). **34** is the stale value.
- ROADMAP.md's phrase "the fork's **two** exclusive *capabilities*" (line ~22) counts
  *capabilities*, not tools, and stays **two**. Do not touch it.

WARNING - `.planning/STATE.md` is NOT a safe template for this arithmetic. It carries the
corrected impossible-count (`3`) but still carries the stale total (`28`) and the stale
uncalled-count (`34`), so 16 + 10 + 3 does not equal 28 inside STATE.md today. That
self-inconsistency is part of what this plan fixes.
</canonical_numbers>

<correction_convention>
This ROADMAP.md already has an established convention for a count that has moved since a dated
record was written: keep the original sentence and append an italic parenthetical correction.
The precedent is ROADMAP.md line ~307 (Phase 5 criterion 5), which reads
`*(Corrected 2026-08-17, during Phase 5 planning: the count was **two**. ...)*`.

Apply the convention this way:
- **Live, current statements** - the milestone Overview, the Progress section, the Phase 7
  checkbox line - are REWRITTEN to be true today.
- **Dated historical records** - a phase's success criteria, a phase's scoping Notes bullet, a
  v0.3.0 comparison paragraph written on a named date - keep their original text and gain an
  appended `*(Corrected 2026-08-19, during Phase 8.1: ...)*` or `*(As of 2026-08-19: ...)*`
  parenthetical.
</correction_convention>

<tasks>

<task type="auto">
  <name>Task 1: Close D-4, and ROADMAP.md's share of D-6 and D-7</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` - read lines 1-45 (milestone list and Overview), 92-102 (the phase
      checkbox list), 296-355 (Phase 5's section, for its Notes bullet and its criterion-5
      correction precedent), 377-392 (Phase 7's section), 488-510 (Phase 8.1's own section),
      539-566 (the Progress section and its table), and 578-596 (the v0.3.0 comparison prose).
      Read the CURRENT file - it has uncommitted working-tree edits from the Phase 8.1 insertion
      that already added the milestone heading and the Phase 8.1 section, so parts of the audit's
      description are already stale.
    - `.planning/phases/07-cycle-timing-and-wedge-triage/07-VERIFICATION.md` frontmatter - it
      reads `status: verified` and `score: 4/4 truths fully verified; the one residual
      human-verification item is closed with a real live transcript (quick task 260818-obc)`.
      This is the authority the Phase 7 checkbox line must agree with.
    - `.planning/STATE.md` section "Quick Tasks Completed" - rows `260818-nh5` (closed the Phase
      07 UAT gap and the manual-only gate hole) and `260818-obc` (live-proved the broker-mediated
      `monitor_held_elsewhere` verdict, closing TIME-04). These closed the two residuals the
      stale Phase 7 text still describes as open.
    - `.planning/v0.2.0-MILESTONE-AUDIT.md` section 7, rows D-4, D-6, D-7.
    - The `<canonical_numbers>` and `<correction_convention>` blocks above.
  </read_first>
  <action>
    D-4 - line ~98 is the Phase 7 entry in the checkbox list. It is `- [ ]`, and its parenthetical
    ends "...the broker-mediated monitor_held_elsewhere verdict is unit-proven only, see
    07-HUMAN-UAT.md. Code review found 1 blocker introduced by the CR-01 fix, see 07-REVIEW.md.
    NOT complete)". Both halves are now false: the `monitor_held_elsewhere` verdict was
    live-proven by quick task 260818-obc, and the CR-01 review blocker was fixed by quick task
    260818-nh5. Change the checkbox to `- [x]` and replace the stale tail so the entry ends
    `(completed 2026-08-18)`, matching the format of the Phase 1/2/3/4/5/8 entries directly above
    and below it, and matching the Progress table's own
    `| 7. Cycle Timing and Wedge Triage | 18/18 | Complete   | 2026-08-18 |` row. Retain the
    substantive plan-count history ("18/18 plans executed: 10 on 2026-08-18 + 8 gap-closure plans
    07-11..07-18 in 3 waves") and replace the human-needed / NOT-complete tail with a factual
    close-out naming `07-VERIFICATION.md` (`status: verified`, 4/4) and the two quick tasks that
    closed the residuals. Do NOT edit line ~498, which quotes the phrase `"NOT complete" text`
    inside Phase 8.1's own criterion 3 - that is a quotation of the drift item, not the drift
    itself. The checklist asserts on `NOT complete)` with the closing parenthesis precisely so
    that quotation survives.

    D-6 - lines ~36-39 are the milestone Overview's live statement of the finish line:
    "The skills call 28 tools; 16 already work on stock, 10 are buildable and are Phases 5 and 7,
    and 2 are impossible and are Phase 8's job to route honestly. The fork's other 34 tools are
    called by no skill and are not a gap." This is a live statement - REWRITE it with the
    `<canonical_numbers>` values: 29 called, 16 already working, 10 buildable, **3** impossible,
    and the fork's other **33** uncalled. Name the three impossible tools inline
    (`vice_sid_get_state`, `vice_keyboard_matrix`, `vice_keyboard_restore`) so the count is
    checkable rather than asserted.

    Then handle the three dated historical occurrences by annotation, not rewrite:
    - Line ~350 (Phase 5's Notes bullet, "the skills call 28 tools; 16 already work on stock, 12
      do not, and 2 of those 12 are impossible"): append a `*(Corrected 2026-08-19, during Phase
      8.1: ...)*` parenthetical stating the impossible count is three not two, that plan 05-08's
      sweep found `vice_keyboard_restore`, that the measured totals move with it to 29 called /
      16 working / 13 not working / 3 impossible, and that the "8 tools in this phase and 2 in
      Phase 7" split is unchanged.
    - Lines ~559-561 ("**Remaining scope:** 14 open requirements across 3 phases ... plus the
      capability-honesty work for the 2 that cannot be built"): a dated statement from the
      2026-08-17 cut that now also contradicts the paragraph immediately below it. Label it
      as-of the cut and append an `*(As of 2026-08-19: **0 open** ...)*` parenthetical stating
      that all 51 in-scope requirements are satisfied with evidence, that "the 2 that cannot be
      built" is **3**, and pointing at the paragraph below and at REQUIREMENTS.md's coverage
      block.
    - Line ~22's "the fork's **two** exclusive capabilities": LEAVE UNCHANGED. It counts
      capabilities (SID register read-back; raw keyboard-matrix/RESTORE control), and two is
      correct for capabilities even though three tools map onto them. The checklist asserts this
      phrase still exists, as a guard against an over-eager two-to-three sweep.

    D-7 - lines ~588-589 in the v0.3.0 comparison prose read "stock advertises 26 tools against
    the fork's 62, and Phase 5 is that gap". This is a dated 2026-08-17 comparison. Keep it and
    append an `*(As of 2026-08-19 ...)*` parenthetical stating that `tools-manifest.stock.json`
    ships **38** tools, that 26 was the figure at the 2026-08-17 cut before Phases 5 and 7 added
    twelve, and that the fork's 62 is unchanged.

    Finally, line ~498 - Phase 8.1's own criterion 3 - contains a prescription this phase's
    research proved stale: "STATE.md is synced to 7/7 and 100%". Leaving it would make this
    phase's own roadmap entry contradict the STATE.md this phase produces. Append, in the same
    correction style, a `*(Corrected 2026-08-19, during Phase 8.1 planning: ...)*` parenthetical
    stating that the audit's "sync STATE.md to 7/7, 100%" was written before Phase 8.1 was
    inserted, that with 8.1 in flight the truthful target is **7 of 9 phases complete, 78%**, and
    that forcing 100% would be a new false claim of exactly the kind this phase exists to remove.
    Do not otherwise alter the criterion's wording; it is the phase's own decision record.

    Do not remove the `# Milestone v0.2.0: Switchable stock-VICE backend` heading - it is
    load-bearing for `extractCurrentMilestone()` (see Phase 8.1's own note at line ~509).
  </action>
  <acceptance_criteria>
    - `grep -cE '^- \[ \] \*\*Phase 7' .planning/ROADMAP.md` returns 0.
    - `grep -cE '^- \[x\] \*\*Phase 7' .planning/ROADMAP.md` returns 1.
    - `grep -cF 'NOT complete)' .planning/ROADMAP.md` returns 0.
    - `grep -cF '"NOT complete" text' .planning/ROADMAP.md` returns 1 (the quotation inside Phase 8.1's criterion 3 survives).
    - `grep -cF 'The skills call 28 tools' .planning/ROADMAP.md` returns 0.
    - `grep -cF 'call 29 tools' .planning/ROADMAP.md` returns 1 or more.
    - `grep -c '34 tools' .planning/ROADMAP.md` returns 0; `grep -c '33 tools' .planning/ROADMAP.md` returns 1 or more.
    - `grep -cF 'two exclusive' .planning/ROADMAP.md` returns 1 (deliberately preserved).
    - `grep -cF 'ships **38** tools' .planning/ROADMAP.md` returns 1 AND `grep -c '26 tools' .planning/ROADMAP.md` returns 1 or more (annotated, not renumbered).
    - `grep -cE '^# Milestone v0\.2\.0: ' .planning/ROADMAP.md` returns 1 (heading preserved).
    - `grep -cF 'Corrected 2026-08-19' .planning/ROADMAP.md` returns 3 or more.
    - `bash .planning/phases/08.1-*/08.1-d-checklist.sh 2>&amp;1 | grep -cE '^FAIL D-(4|7):'` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -cE '^- \[x\] \*\*Phase 7' .planning/ROADMAP.md)" -eq 1 &amp;&amp; test "$(grep -cF 'NOT complete)' .planning/ROADMAP.md)" -eq 0 &amp;&amp; test "$(grep -c '34 tools' .planning/ROADMAP.md)" -eq 0 &amp;&amp; test "$(grep -cF 'two exclusive' .planning/ROADMAP.md)" -eq 1</automated>
  </verify>
  <done>ROADMAP.md's Phase 7 entry is `[x]` and agrees with the progress table and 07-VERIFICATION.md; the live milestone arithmetic reads 29/16/10/3/33; the three dated historical figures carry appended as-of corrections; the milestone heading and the "two exclusive capabilities" phrase are intact.</done>
</task>

<task type="auto">
  <name>Task 2: Close D-5, and STATE.md's share of D-6</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - `.planning/STATE.md` in full - the CURRENT file, which has uncommitted working-tree edits
      from the Phase 8.1 insertion. Its frontmatter already reads `total_phases: 9`,
      `completed_phases: 7`, `total_plans: 76`, `completed_plans: 76`, `percent: 78`, and its
      "Current Position" block already names Phase 08.1. Those are already correct - the drift is
      entirely in the BODY, which still contradicts them.
    - `.planning/v0.2.0-MILESTONE-AUDIT.md` section 7, row D-5 - note its prescribed fix ("sync
      to 7/7, 100%") predates Phase 8.1's insertion and is itself stale; do not apply it
      literally.
    - `.planning/phases/08.1-.../08.1-RESEARCH.md` section "Anti-Patterns to Avoid", third bullet
      - why forcing 100% would be a new false claim.
    - `.planning/ROADMAP.md` section "Progress" table - the authority for which phases are
      complete.
    - The `<canonical_numbers>` block above, including its WARNING about STATE.md.
  </read_first>
  <action>
    Do NOT edit the frontmatter - `total_phases: 9`, `completed_phases: 7`, `total_plans: 76`,
    `completed_plans: 76`, `percent: 78` are already correct (7 of 9 phase slots complete, with
    Phase 8.1 in flight and cut Phase 6 retained in the denominator). Every body edit below
    brings the body into line with that frontmatter, not the other way round.

    D-5, five concrete body corrections:

    1. The ASCII progress bar reads `Progress: [███████░░░] 71%`. It contradicts `percent: 78`.
       Change it to `Progress: [████████░░] 78%` - eight filled blocks of ten, matching 78%.
    2. The "Current Position" block's trailing sentence reads "Phase 08 is the last phase of
       v0.2.0." That is now false - ROADMAP.md's Progress section states Phase 8.1 "is the last
       phase of this milestone". Change it to name Phase 8.1 as the last phase, and state that
       Phase 8.1 closes the audit's unwitnessed claim and its seven stale documents rather than
       shipping a feature.
    3. The same block has a dangling fragment beginning "Complete, UAT 12/12, all 20 code-review
       findings fixed, verification's single human_verification item live-proven." with no
       subject - it reads as if it describes the current position when it describes Phase 08.
       Give it its subject by prefixing it with `Phase 08:`.
    4. "## Performance Metrics" states `Total plans completed: 58`, contradicting the
       frontmatter's `completed_plans: 76`. The cause is mechanical: the "By Phase" table below
       it omits Phase 07 entirely (4 + 10 + 18 + 7 + 13 + 6 = 58; the missing 18 is Phase 07).
       Insert the row `| 07 | 18 | - | - |` between the `05` and `08` rows, and change
       `Total plans completed: 58` to `Total plans completed: 76`. 4 + 10 + 18 + 7 + 13 + 18 + 6
       = 76.
    5. Confirm no other body sentence claims the milestone is complete or that a phase other than
       8.1 is last. The `status:` frontmatter key reads `ready_to_plan`, which is correct for a
       phase being planned - leave it; the execute-phase workflow owns that key.

    D-6 - the "**Scope was cut on 2026-08-17.**" paragraph reads "The six skills call 28 tools --
    16 already work on stock, 10 are buildable (8 in Phase 5, 2 in Phase 7), and 3 are provably
    impossible ... The fork's other 34 tools are called by no skill." This is the arithmetic trap
    in its purest form: the impossible count is already correct at 3, but 16 + 10 + 3 = 29, not
    28, and 62 - 29 = 33, not 34. Change `28 tools` to `29 tools` and `other 34 tools` to
    `other 33 tools`. Leave the three named impossible tools and the parenthetical about plan
    05-08 finding `vice_keyboard_restore` exactly as they are - they are already right.

    Also in that paragraph: "See ROADMAP.md 'Cut from scope' and REQUIREMENTS.md for the 21
    CUT-marked items." Plan 08.1-01 restated that as 17 whole requirements plus 4 partial halves.
    Change `the 21 CUT-marked items` to `the 17 CUT-marked requirements and 4 cut halves` so
    STATE.md agrees with the REQUIREMENTS.md coverage block it points at.
  </action>
  <acceptance_criteria>
    - `grep -c '71%' .planning/STATE.md` returns 0; `grep -cF '] 78%' .planning/STATE.md` returns 1.
    - `grep -cF 'Phase 08 is the last phase' .planning/STATE.md` returns 0.
    - `grep -cE '^Phase 08: Complete, UAT 12/12' .planning/STATE.md` returns 1.
    - `grep -cF 'Total plans completed: 76' .planning/STATE.md` returns 1; `grep -cF 'Total plans completed: 58' .planning/STATE.md` returns 0.
    - `grep -cE '^\| 07 \| 18 \|' .planning/STATE.md` returns 1.
    - `grep -cF 'call 29 tools' .planning/STATE.md` returns 1; `grep -cF 'call 28 tools' .planning/STATE.md` returns 0.
    - `grep -c '34 tools' .planning/STATE.md` returns 0; `grep -c '33 tools' .planning/STATE.md` returns 1.
    - `grep -cE '^  percent: 78$' .planning/STATE.md` returns 1 and `grep -cE '^  completed_phases: 7$' .planning/STATE.md` returns 1 (frontmatter untouched).
    - `grep -c '100%' .planning/STATE.md` returns 0 (the audit's stale prescription was not applied).
    - `bash .planning/phases/08.1-*/08.1-d-checklist.sh 2>&amp;1 | grep -cE '^FAIL D-(5|6):'` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c '71%' .planning/STATE.md)" -eq 0 &amp;&amp; test "$(grep -cF 'Total plans completed: 76' .planning/STATE.md)" -eq 1 &amp;&amp; test "$(grep -c '34 tools' .planning/STATE.md)" -eq 0 &amp;&amp; test "$(grep -c '100%' .planning/STATE.md)" -eq 0</automated>
  </verify>
  <done>STATE.md's body agrees with its own frontmatter (7 of 9 phases, 78%, 76 plans), names Phase 8.1 as the milestone's last phase, and its cut-scope arithmetic reads 29/16/10/3/33. D-5 and D-6 PASS on the checklist.</done>
</task>

<task type="auto">
  <name>Task 3: Verify criterion 2 rather than re-author it, and record the evidence</name>
  <files>.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-criterion-2-evidence.txt</files>
  <read_first>
    - `.planning/phases/08-capability-honesty-and-the-install-story/08-VALIDATION.md` frontmatter
      (lines 1-11) - read it directly rather than trusting the audit's description of it.
    - `.planning/v0.2.0-MILESTONE-AUDIT.md` - its own frontmatter key `nyquist.partial_phases`
      and section 2.2, which describe Phase 8 as non-compliant. That description is stale.
    - `.planning/phases/08.1-.../08.1-RESEARCH.md` section "Common Pitfalls", Pitfall 1 -
      "Assuming the audit's tech_debt list is still 100% current", and its explicit warning that
      a task titled "flip 08-VALIDATION.md to audited" is planning against stale information.
    - `.planning/phases/08.1-.../08.1-VALIDATION.md` "Per-Task Verification Map", criterion 2 row
      - marked "already passes - verify, do not re-do".
  </read_first>
  <action>
    This is a VERIFICATION task, not a fix task. Commit `4306338` ("docs(phase-08): update
    validation strategy after Nyquist audit") already flipped `08-VALIDATION.md` from
    `status: draft` / `nyquist_compliant: false` to `status: audited` / `nyquist_compliant: true`
    with `wave_0_complete: true` and `manual_only_open: 1`. Do not edit that file in this plan.
    Its `manual_only_open` counter is plan 08.1-04's business, because it tracks the walkthrough.

    Produce `08.1-criterion-2-evidence.txt` under this phase's directory containing, in this
    order:
    1. The literal output of
       `grep -nE '^(phase|slug|status|nyquist_compliant|wave_0_complete|created|audited|manual_only_open):' .planning/phases/08-capability-honesty-and-the-install-story/08-VALIDATION.md`
    2. The literal output of
       `git log -1 --format='%H %ad %s' --date=iso -- .planning/phases/08-capability-honesty-and-the-install-story/08-VALIDATION.md`
       - which must name commit `4306338` (or a later commit that did not regress the two keys).
    3. A per-phase sweep proving no OTHER phase in this milestone is non-compliant: the literal
       output of `grep -H -E '^(status|nyquist_compliant):' .planning/phases/*/[0-9]*-VALIDATION.md`.
       Every `*-VALIDATION.md` in the milestone must read `nyquist_compliant: true` EXCEPT this
       phase's own `08.1-VALIDATION.md`, which is legitimately still `draft`/`false` because
       Phase 8.1 has not been executed yet - note that exception explicitly in the file so a
       later reader does not mistake it for a regression.
    4. One plain-English verdict line stating whether criterion 2 holds, and stating that it was
       satisfied by a prior commit rather than by any edit made in Phase 8.1.

    If, contrary to the research, `08-VALIDATION.md` does NOT read `status: audited` /
    `nyquist_compliant: true` at execution time, do not silently fix it: record the actual values
    in the evidence file, stop, and report it in the plan SUMMARY as a discovered regression so
    the orchestrator can decide whether it needs its own work.
  </action>
  <acceptance_criteria>
    - `grep -cE '^status: audited$' .planning/phases/08-capability-honesty-and-the-install-story/08-VALIDATION.md` returns 1.
    - `grep -cE '^nyquist_compliant: true$' .planning/phases/08-capability-honesty-and-the-install-story/08-VALIDATION.md` returns 1.
    - `git diff --stat .planning/phases/08-capability-honesty-and-the-install-story/08-VALIDATION.md` shows no change made by this plan.
    - `.planning/phases/08.1-*/08.1-criterion-2-evidence.txt` exists and `grep -c '4306338' .planning/phases/08.1-*/08.1-criterion-2-evidence.txt` returns 1 or more.
    - `grep -cE 'nyquist_compliant: false' .planning/phases/08.1-*/08.1-criterion-2-evidence.txt` returns 1 (the recorded, explained exception for `08.1-VALIDATION.md`) and the file contains prose naming that exception.
    - `grep -cE '^(HOLDS|DOES NOT HOLD)' .planning/phases/08.1-*/08.1-criterion-2-evidence.txt` returns 1 (an unambiguous verdict line).
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -cE '^nyquist_compliant: true$' .planning/phases/08-capability-honesty-and-the-install-story/08-VALIDATION.md)" -eq 1 &amp;&amp; test -s .planning/phases/08.1-*/08.1-criterion-2-evidence.txt</automated>
  </verify>
  <done>Criterion 2 is proven already-satisfied with recorded evidence naming the prior commit; `08-VALIDATION.md` was read, not edited; the one legitimately non-compliant validation file (this phase's own) is named as an expected exception.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan edits two Markdown planning documents and writes one plain-text evidence file. No process boundary, network endpoint, untrusted input, credential, or runtime code path is created, modified, or crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-03 | Repudiation | `08.1-criterion-2-evidence.txt` | mitigate | The evidence file records literal command output plus the originating commit sha (`4306338`) rather than a human assertion, so the criterion-2 claim is independently re-derivable by any later auditor from `git log` alone. |
| T-08.1-04 | Information Disclosure | `.planning/ROADMAP.md`, `.planning/STATE.md` | accept | Both are already public in a public repository and contain no secrets, credentials, hostnames, or PII. The edits correct counts, checkbox state and progress figures only. |
| T-08.1-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install occurs in this plan. `08.1-RESEARCH.md` section "Package Legitimacy Audit" records N/A for this phase; no new external package is added, so there is no `[ASSUMED]`/`[SUS]` package to gate. |

**Justified assessment: no new threats.** This plan ships no code. `capability-registry.ts`,
`vice.ts`, `vice-proxy.ts` and every other runtime module are untouched. ASVS L1 has no
applicable control for a documentation correction with no input surface.
</threat_model>

<verification>
1. `bash .planning/phases/08.1-*/08.1-d-checklist.sh` exits 0 with PASS for all seven items
   D-1..D-7 (D-1..D-3 closed by plan 08.1-01, D-4..D-7 closed here).
2. `git status --porcelain .claude .github scripts docs README.md installer` is empty - no source
   file was touched.
3. `git diff --stat .planning/phases/08-capability-honesty-and-the-install-story/` shows no change
   from this plan (Phase 8's own artifacts belong to plan 08.1-04).
4. `grep -cE '^# Milestone v0\.2\.0: ' .planning/ROADMAP.md` returns 1.
</verification>

<success_criteria>
- All seven D-item assertions in `08.1-d-checklist.sh` report PASS; the script exits 0
- ROADMAP.md's Phase 7 entry is `[x]` with no `NOT complete)` text remaining
- ROADMAP.md and STATE.md both carry the 29 / 16 / 10 / 3 / 33 arithmetic; no `34 tools` remains
  in either file
- STATE.md's body reads 78% and 76 plans, matching its own frontmatter; no `100%` claim exists
- `08-VALIDATION.md` is confirmed `status: audited` / `nyquist_compliant: true` and unmodified
- No push to `origin`, no tag, no npm publish performed by this plan
</success_criteria>

<output>
Create `.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-02-SUMMARY.md` when done.
</output>
