---
phase: 13-external-verification
plan: 05
type: execute
wave: 3
depends_on: [13-01, 13-02, 13-04]
files_modified:
  - docs/phase2-backend-probe-evidence.md
  - docs/stock-vice-parity.md
  - .planning/todos/pending/2026-08-13-re-record-binmon-fixtures-against-real-stock-vice.md
  - .planning/todos/completed/2026-08-13-re-record-binmon-fixtures-against-real-stock-vice.md
  - .planning/todos/pending/2026-08-13-confirm-help-discriminator-against-real-vice-binaries.md
  - .planning/todos/completed/2026-08-13-confirm-help-discriminator-against-real-vice-binaries.md
  - .planning/todos/pending/2026-08-14-probe-phase3-assumed-wire-details.md
  - .planning/todos/pending/2026-08-22-cpuhistory-get-sidecars-mislabel-the-fork-as-stock.md
  - .planning/STATE.md
autonomous: true
requirements: [EXTV-01, EXTV-02, EXTV-03]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`docs/phase2-backend-probe-evidence.md` §1's synthetic-fixture override is recorded as CLOSED, citing the re-recorded fixtures and `13-CAPTURE-TRANSCRIPT.md`, and §2's discriminator verdict no longer reads OPEN — it reads a resolved verdict citing the two committed transcripts and the recorded live runs (D-13-03)."
    - "`docs/stock-vice-parity.md` §A item 7 gains a one-sentence caveat that a wrong implementation detail among the probed assumptions produces a silently wrong answer rather than a licensed divergence, scoped to a sentence and not a rewrite."
    - "The two closed todos are moved to `.planning/todos/completed/` with a Resolution section naming the commits, the artifacts and the phase requirement each closed."
    - "`2026-08-14-probe-phase3-assumed-wire-details.md` stays in `.planning/todos/pending/` with A4 as its only remaining item, its A1/A2/A3/A5 sections rewritten to point at the recorded verdicts rather than restating them as open (D-13-05)."
    - "`STATE.md`'s `## Deferred Items` section is updated so that every remaining pending todo stem appears in it and no completed todo stem appears in it anywhere — including in prose, because the guard's second direction is a substring check over that section in isolation."
    - "Every finding this phase surfaced but deliberately did not fix is a filed todo in `.planning/todos/pending/`, with a row in `STATE.md`'s Deferred Items — including the two `cpuhistory-get*` sidecars whose recorded backend kind contradicts the binary they name, and any advertised-tool-contract finding handed over by plan 13-04."
    - "No requirement in this phase is recorded as closed on the strength of a document edit alone — each closure cites the artifact or transcript that proves it."
  artifacts:
    - .planning/todos/completed/2026-08-13-re-record-binmon-fixtures-against-real-stock-vice.md
    - .planning/todos/completed/2026-08-13-confirm-help-discriminator-against-real-vice-binaries.md
    - .planning/todos/pending/2026-08-22-cpuhistory-get-sidecars-mislabel-the-fork-as-stock.md
  key_links:
    - "`docs-deferred-ledger.test.ts` is the two-directional guard tying `.planning/todos/pending/` to `STATE.md`'s Deferred Items section — moving a todo without editing that section turns the guard red."
    - "`docs/phase2-backend-probe-evidence.md` is the single document carrying both of this phase's retired verdicts; its §1 and §2 edits are the closing record for EXTV-01 and EXTV-02 respectively."
    - "The A4-only remainder in the probe-debt todo is what keeps D-13-05's exclusion visible after the phase seals, rather than losing it in a closed todo."
  prohibitions:
    - statement: "MUST NOT close a todo, flip a verdict or shrink the deferred ledger on the strength of a document edit alone — each closure must cite the committed artifact, transcript or test that proves it, because a ledger that can be shrunk by editing prose is the failure this milestone exists to stop."
      status: unverified
      verification: judgment
    - statement: "MUST NOT absorb a surfaced-but-unfixed finding silently — every one becomes a filed todo with a ledger row, never a sentence in a summary that no later session reads."
      status: unverified
      verification: judgment
---

<objective>
Close the phase's record: flip both retired verdicts in
`docs/phase2-backend-probe-evidence.md`, add the parity-doc caveat, close the two
finished todos, trim the third to its one remaining out-of-scope item, file the
findings this phase surfaced but did not fix, and update `STATE.md`'s deferred
ledger so its guard stays green.

Purpose: two of this milestone's carried items are only genuinely closed when the
documents that recorded them as open say so, and the ledger that counts them is
mechanically tied to the todo tree. A phase that fixes the code and leaves the
record stale reproduces the exact defect the milestone exists to end.

Output: two resolved verdicts, one scoped parity caveat, two closed todos, one
trimmed-to-A4 todo, at least one newly filed todo, and a `STATE.md` Deferred
Items section that passes its two-directional guard.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-external-verification/13-CONTEXT.md
@.planning/phases/13-external-verification/13-RESEARCH.md
@.planning/phases/13-external-verification/13-CAPTURE-TRANSCRIPT.md
@.planning/phases/13-external-verification/13-HELP-DISCRIMINATOR-EVIDENCE.md
@.planning/phases/13-external-verification/13-PROBE-RESULTS.md
@.planning/phases/13-external-verification/13-04-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols and paths created by this plan (new, not pre-existing):

- `.planning/todos/completed/2026-08-13-re-record-binmon-fixtures-against-real-stock-vice.md` — moved file with a new Resolution section.
- `.planning/todos/completed/2026-08-13-confirm-help-discriminator-against-real-vice-binaries.md` — moved file with a new Resolution section.
- `.planning/todos/pending/2026-08-22-cpuhistory-get-sidecars-mislabel-the-fork-as-stock.md` — new file.
- Zero or more further new files under `.planning/todos/pending/` for
  advertised-tool-contract findings handed over by plan 13-04 (exact filenames
  chosen at execution time from the finding's own subject).

No new exported functions, classes, decorators, CLI flags or dataclass fields
are created by this plan. Two files are `git mv`-moved rather than deleted; this
plan runs with worktree isolation off, so those moves land in its own commits.

<tasks>

<task type="auto">
  <name>Task 1: Flip both retired verdicts in the evidence doc and add the parity caveat</name>
  <files>docs/phase2-backend-probe-evidence.md, docs/stock-vice-parity.md</files>
  <read_first>
    - `docs/phase2-backend-probe-evidence.md` — the whole file: §1's D-19 override record for the three synthetic fixtures and §2's discriminator section with its OPEN verdict paragraph.
    - `.planning/phases/13-external-verification/13-CAPTURE-TRANSCRIPT.md` — the artifact §1's closure cites: the resolved binary, the version, the per-frame decode and the terminator-frame verdict.
    - `.planning/phases/13-external-verification/13-HELP-DISCRIMINATOR-EVIDENCE.md` — the artifact §2's closure cites: the enumerated binaries, the `probeBackend()` verdicts, the `resolvedBackend()` cache round-trip, and the honest "unexercised on this host" finding about the fallback ladder.
    - `docs/stock-vice-parity.md` around line 155-175 — §A item 7's heading and body, the licensed-divergence register that gains a one-sentence caveat.
    - `.planning/phases/13-external-verification/13-PROBE-RESULTS.md` — the per-assumption verdicts the parity caveat refers to.
    - `.planning/phases/13-external-verification/13-CONTEXT.md` § D-13-01 — the accepted consequence to record honestly in §1: a fork-recorded fixture is real evidence for the protocol, but describes a binary the project may stop shipping.
  </read_first>
  <action>
Rewrite `docs/phase2-backend-probe-evidence.md` §1 from an open override record
into a closed one. Keep the full historical account of what was synthesized and
which spec section each field came from — that is the archaeology D-13-06 relies
on now that the original bytes live only in git history — and add a clearly
headed closure statement naming: the phase requirement it closes, the resolved
binary path and kind and version the re-capture ran against, the transcript that
records the decode, and the outcome of the two readings the synthesis had guessed
(the event order and the terminator frame). Record D-13-01's accepted consequence
explicitly and in the reader's interest: the fixtures are real evidence for the
protocol, captured from whichever build resolved first in `PATH`, and if the fork
backend is later retired these fixtures describe a binary the project no longer
ships. State the stock-versus-fork byte-drift question as deliberately out of
scope rather than as unexamined.

Rewrite §2's verdict. It currently reads as an explicitly open question that
claims neither success nor failure for `--help` introspection. Replace that with
a resolved verdict citing: the two committed transcripts by path, the
`probeBackend()` result per real binary, the `resolvedBackend()` cache round-trip
including the zero-re-probe second call, and the committed real-hardware
regression block that pins the classification. Record the third finding honestly
rather than as a confirmation: both real builds exit zero with non-empty `--help`
output, so the fallback ladder's later branches are never reached on this host
and are therefore unexercised, not verified. Keep §2's original statement of what
was assumed, so a reader can see what changed.

Add one sentence to `docs/stock-vice-parity.md` §A item 7. That item registers
divergences licensed by design; a wrong implementation detail among the probed
assumptions is a different animal — it produces a silently wrong answer, not a
known divergence — and the document does not currently warn about it. Add that
warning, name where the per-assumption verdicts live, and keep it to a sentence.
Do not restructure the item or the register.

Nothing in this task closes a todo or edits the ledger; that is Tasks 2 and 3.
Every claim added here cites an artifact by path — a verdict that cites only
itself is the thing this milestone exists to stop.
  </action>
  <acceptance_criteria>
    - `grep -n 'OPEN, not resolved either way' docs/phase2-backend-probe-evidence.md` returns nothing.
    - `docs/phase2-backend-probe-evidence.md` §1 contains a closure statement naming `EXTV-01` and citing `13-CAPTURE-TRANSCRIPT.md` by path.
    - `docs/phase2-backend-probe-evidence.md` §2 contains a resolved verdict naming `EXTV-02` and citing both committed transcript paths under `fixtures/backend-detect/` and `13-HELP-DISCRIMINATOR-EVIDENCE.md` by path.
    - `docs/phase2-backend-probe-evidence.md` §2 states that the `-help`/`-?` fallback branches are unexercised on this host rather than verified.
    - `docs/phase2-backend-probe-evidence.md` §1 states that stock-versus-fork byte drift was deliberately out of scope and records the accepted consequence about a fork-recorded fixture.
    - `docs/stock-vice-parity.md` §A item 7 contains a sentence distinguishing a wrong implementation detail from a licensed divergence and naming where the per-assumption verdicts are recorded, and `git diff --numstat docs/stock-vice-parity.md` shows at most 4 added lines.
    - `cd .claude/mcp/vice && node --test docs-dangling-refs.test.ts docs-linerefs.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/mcp/vice && node --test docs-dangling-refs.test.ts docs-linerefs.test.ts && node -e 'const s=require("node:fs").readFileSync("../../../docs/phase2-backend-probe-evidence.md","utf8");if(/OPEN, not resolved either way/.test(s))throw new Error("section 2 verdict still open");for(const c of ["EXTV-01","EXTV-02","13-CAPTURE-TRANSCRIPT.md","13-HELP-DISCRIMINATOR-EVIDENCE.md","fixtures/backend-detect/"])if(!s.includes(c))throw new Error("evidence doc does not cite "+c);console.log("both verdicts resolved and cited")'</automated>
  </verify>
  <done>Both retired verdicts in the evidence document are resolved with artifact citations, the fallback-ladder finding is recorded honestly as unexercised, D-13-01's accepted consequence is written down for a later reader, and the parity register carries a one-sentence silently-wrong-answer caveat.</done>
</task>

<task type="auto">
  <name>Task 2: Close the two finished todos, trim the third to A4 only, and file every surfaced-but-unfixed finding</name>
  <files>.planning/todos/pending/2026-08-13-re-record-binmon-fixtures-against-real-stock-vice.md, .planning/todos/completed/2026-08-13-re-record-binmon-fixtures-against-real-stock-vice.md, .planning/todos/pending/2026-08-13-confirm-help-discriminator-against-real-vice-binaries.md, .planning/todos/completed/2026-08-13-confirm-help-discriminator-against-real-vice-binaries.md, .planning/todos/pending/2026-08-14-probe-phase3-assumed-wire-details.md, .planning/todos/pending/2026-08-22-cpuhistory-get-sidecars-mislabel-the-fork-as-stock.md</files>
  <read_first>
    - `.planning/todos/pending/2026-08-13-re-record-binmon-fixtures-against-real-stock-vice.md` — its six numbered acceptance steps, each of which the Resolution section must answer.
    - `.planning/todos/pending/2026-08-13-confirm-help-discriminator-against-real-vice-binaries.md` — its five numbered acceptance steps and its three enumerated assumed sub-claims.
    - `.planning/todos/pending/2026-08-14-probe-phase3-assumed-wire-details.md` — the whole file: the five per-assumption sections, the seven-step acceptance check, and A4's own section which is the only part that stays open.
    - `.planning/todos/completed/` — pick any two existing entries and read their Resolution sections for the established format, so this plan matches it rather than inventing one.
    - `.planning/phases/13-external-verification/13-CAPTURE-TRANSCRIPT.md`, `13-HELP-DISCRIMINATOR-EVIDENCE.md` and `13-PROBE-RESULTS.md` — the evidence each Resolution cites.
    - `.planning/phases/13-external-verification/13-04-SUMMARY.md` — the label-edit inventory, any contradicted-assumption corrections, and any advertised-tool-contract finding handed over for filing here.
    - `.planning/phases/13-external-verification/13-RESEARCH.md` § Open Questions item 1 — the two `cpuhistory-get*` sidecars whose recorded backend kind contradicts the binary they name, whose recommendation is to file rather than fix.
  </read_first>
  <action>
Move the two finished todos with `git mv` from `.planning/todos/pending/` to
`.planning/todos/completed/`, keeping their filenames and their original bodies
intact, and append a Resolution section to each in the format the existing
completed entries use. Each Resolution names: the phase requirement it closes,
the plans that closed it, the commits, the artifacts by path, and a one-line
answer per numbered acceptance step in that todo's own check — six answers for
the fixtures todo, five for the discriminator todo. Where a step's honest answer
is that something turned out to be unexercised or moot rather than confirmed, say
that; a Resolution that upgrades an honest non-answer into a pass is the defect
this phase is closing.

Edit `2026-08-14-probe-phase3-assumed-wire-details.md` in place and leave it in
`.planning/todos/pending/`. Rewrite its A1, A2, A3 and A5 sections to point at
the recorded verdict for each, so a reader is not re-reading four open questions
that have answers; where a verdict was inconclusive, say so and say what would
settle it. Rewrite the acceptance check so only A4's item remains open, and state
plainly at the top that A4 is the only remaining item, that it was deliberately
excluded, and why: probing it means arming a non-stopping checkpoint on a hot
address, whose hit frame is emitted synchronously from inside the emulator's CPU
loop and can stall the emulator thread. Update the front matter so it no longer
claims this phase resolves it. Do not delete this file and do not move it.

File `.planning/todos/pending/2026-08-22-cpuhistory-get-sidecars-mislabel-the-fork-as-stock.md`
in the established todo format: what is wrong (two committed real-capture
sidecars record a backend kind that contradicts the binary path in the same
field), why it was not fixed here (it sits outside the three fixtures the
fixture-provenance decision scoped, and a cheap fix is still a scope decision
that should be visible rather than absorbed), what a fix would touch, and how to
verify it. Name the affected files by path.

File one further todo per advertised-tool-contract finding handed over by plan
13-04, if there were any. Each names the contract, the observation that
contradicts it, the file and function that would change, and the reason it was
not changed in a verification phase.

Do not touch any other file in `.planning/todos/pending/`. In particular leave
`2026-08-20-vsf-as-a-bootstrap-input.md` alone — a committed guard uses it as a
positive control and moving it would break that control for reasons unrelated to
this phase.
  </action>
  <acceptance_criteria>
    - `ls .planning/todos/completed/ | grep -c 're-record-binmon-fixtures-against-real-stock-vice'` is 1 and `ls .planning/todos/pending/ | grep -c 're-record-binmon-fixtures-against-real-stock-vice'` is 0.
    - `ls .planning/todos/completed/ | grep -c 'confirm-help-discriminator-against-real-vice-binaries'` is 1 and the same stem count in `pending/` is 0.
    - `git log --diff-filter=R --name-status -1` (or `git status`) shows both files as renames, not as a delete plus an add.
    - Each moved file contains a `Resolution` heading citing at least one commit hash, at least one artifact path under `.planning/phases/13-external-verification/`, and one answer line per numbered acceptance step in that todo (6 and 5 respectively).
    - `.planning/todos/pending/2026-08-14-probe-phase3-assumed-wire-details.md` still exists, states that A4 is its only remaining item, and names the CPU-loop stall reason.
    - `.planning/todos/pending/2026-08-22-cpuhistory-get-sidecars-mislabel-the-fork-as-stock.md` exists and names both affected sidecar paths.
    - `ls .planning/todos/pending/*.md | wc -l` is at least 10 — the guard's own floor.
    - `ls .planning/todos/pending/ | grep -c '2026-08-20-vsf-as-a-bootstrap-input'` is 1.
  </acceptance_criteria>
  <verify>
    <automated>node -e 'const {readdirSync,readFileSync}=require("node:fs");const P=".planning/todos/pending",C=".planning/todos/completed";const p=readdirSync(P),c=readdirSync(C);const closed=["2026-08-13-re-record-binmon-fixtures-against-real-stock-vice.md","2026-08-13-confirm-help-discriminator-against-real-vice-binaries.md"];for(const f of closed){if(p.includes(f))throw new Error(f+" still pending");if(!c.includes(f))throw new Error(f+" not in completed");if(!/^#+ *Resolution/m.test(readFileSync(C+"/"+f,"utf8")))throw new Error(f+" has no Resolution section");}if(!p.includes("2026-08-14-probe-phase3-assumed-wire-details.md"))throw new Error("A4 todo must stay pending");if(!p.includes("2026-08-20-vsf-as-a-bootstrap-input.md"))throw new Error("guard positive-control todo was moved");if(!p.some(f=>/cpuhistory-get-sidecars-mislabel/.test(f)))throw new Error("mislabel todo not filed");if(p.length<10)throw new Error("pending floor breached: "+p.length);console.log("todo tree closed correctly: "+p.length+" pending, "+c.length+" completed")'</automated>
  </verify>
  <done>Both finished todos are `git mv`-moved to completed with Resolution sections answering every numbered acceptance step honestly; the probe-debt todo stays pending trimmed to A4 with the exclusion reason written down; every surfaced-but-unfixed finding is a filed todo; the guard's positive-control todo is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md's deferred ledger so its two-directional guard stays green</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - `.claude/mcp/vice/docs-deferred-ledger.test.ts` — the whole file. Direction A requires every pending todo stem to appear somewhere in the `## Deferred Items` section; direction B requires no completed todo stem to appear anywhere in that section, as a substring, including inside prose; the non-vacuity test pins the heading name, a non-empty section, a pending floor of 10, a completed floor of 5, and a named positive-control stem.
    - `.planning/STATE.md` `## Deferred Items` — the whole section: its narrative paragraphs, its table, and the `Carried forward from earlier closes` sub-table. Note which stems the prose paragraphs name, not just the table rows.
    - `.planning/STATE.md` `### Pending Todos` — the hand-written count sentence, outside the guarded section, which is stale after this plan.
    - `.planning/STATE.md` `## Current Position` and `### Decisions` — where this phase's outcome is recorded.
    - `.planning/phases/13-external-verification/13-01-SUMMARY.md`, `13-02-SUMMARY.md`, `13-03-SUMMARY.md`, `13-04-SUMMARY.md` — the per-plan outcomes summarised into the decision entries.
  </read_first>
  <action>
Update `STATE.md`'s `## Deferred Items` section so both directions of its guard
hold after Task 2's moves.

Direction B is a substring check over the section text in isolation, so removing
the two closed todos' table rows is not enough — their filename stems must not
appear anywhere in that section, including inside the explanatory paragraphs
above the table. Search the section for both stems and remove or reword every
occurrence. Where a paragraph's narrative genuinely needs to describe the
closure, move that sentence out of this section — the sections around it are not
scanned, and the guard's own header comment explains that this narrow scope is
what keeps it satisfiable.

Direction A requires a row for every stem still in `.planning/todos/pending/`,
including the newly filed one(s) from Task 2. Add a row per new todo in the
table's existing column shape, with a one-line status saying why it is deferred.

Update the section's own count and narrative so they describe the post-phase
truth: how many items remain, that two came off because this phase closed them
against real binaries, and that the probe-debt item remains with A4 as its only
open part. Keep the section's existing framing that the table is derived from the
todo tree rather than hand-maintained.

Then update the unguarded parts of `STATE.md` for honesty, since nothing forces
them: correct the `### Pending Todos` count sentence to the new number; set
`## Current Position` to reflect this phase's completion state and name the
artifacts a next session should read; and add one `### Decisions` entry per plan
in this phase recording what was actually found — the three re-recorded fixtures
and whether the terminator reading and the event order held, the discriminator's
confirmed verdicts and the honestly-unexercised fallback ladder, the four
per-assumption probe verdicts, and which labels came off. Write findings, not
intentions: a decision entry that says what was attempted rather than what was
observed is worthless to the next session.

Update the frontmatter's `last_updated` and `last_activity` fields. Do not invent
progress numbers: if a plan-count or percentage field would need a value this
task cannot derive from the phase's own artifacts, leave it to the workflow's own
state tooling rather than typing a figure, and never pin a computed figure as an
assertion anywhere.
  </action>
  <acceptance_criteria>
    - `cd .claude/mcp/vice && node --test docs-deferred-ledger.test.ts` exits 0, with all three of its tests passing.
    - Neither closed todo's filename stem appears anywhere between `## Deferred Items` and the next `## ` heading in `.planning/STATE.md`.
    - Every filename stem in `.planning/todos/pending/` appears somewhere in that same section.
    - The `## Deferred Items` heading is unchanged in name, and the section is non-empty.
    - `.planning/STATE.md`'s `### Pending Todos` count sentence equals the actual file count in `.planning/todos/pending/`.
    - `.planning/STATE.md` `### Decisions` contains at least one new entry per plan in this phase, each stating an observed outcome rather than an intention.
    - `cd .claude/mcp/vice && node --test docs-deferred-ledger.test.ts docs-dangling-refs.test.ts docs-review-disposition.test.ts docs-linerefs.test.ts` exits 0.
    - `cd .claude/mcp/vice && npm run test:automated` exits 0 and `npm test` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/mcp/vice && node --test docs-deferred-ledger.test.ts docs-dangling-refs.test.ts docs-review-disposition.test.ts docs-linerefs.test.ts && npm run test:automated</automated>
  </verify>
  <done>`STATE.md`'s Deferred Items section satisfies both directions of its guard against the post-phase todo tree, the unguarded count and position narrative match reality, and the decision log records what each plan observed rather than what it set out to do.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase artifacts → normative documents | a transcript becomes a claim that future sessions treat as settled |
| todo tree → `STATE.md`'s deferred ledger | a mechanically guarded relationship that a prose edit can break in either direction |
| this phase's findings → the next milestone | anything not filed as a todo is lost, because nothing else carries it forward |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-12 | Repudiation | verdict flips in `docs/phase2-backend-probe-evidence.md` | high | mitigate | Every closure statement cites the committed artifact or transcript that proves it by path, and the acceptance criteria grep for those citations — a verdict that cites only itself cannot pass. |
| T-13-13 | Repudiation | shrinking the deferred ledger | high | mitigate | The ledger row set is reconciled against the real `.planning/todos/pending/` directory and enforced in both directions by a committed guard, which is run as this plan's own verification; a row cannot be dropped without a corresponding file move. |
| T-13-14 | Tampering | silently absorbed findings | medium | mitigate | Every surfaced-but-unfixed finding becomes a filed todo with a ledger row, including the two mislabelled sidecars and any advertised-tool-contract finding handed over by plan 13-04; the acceptance criteria check the files exist by stem. |
| T-13-15 | Denial of Service | breaking the ledger guard's positive control | low | mitigate | The guard names one pending todo as its positive control; this plan explicitly leaves that file alone and asserts it is still pending, so the guard cannot be silently disarmed by an unrelated move. |
| T-13-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages; `13-RESEARCH.md`'s Package Legitimacy Audit records the phase as not applicable. |

Canon referral: none applicable — this plan edits documents and moves files, and
introduces no new input-handling, authentication or data-retention surface.
</threat_model>

<verification>
- `cd .claude/mcp/vice && node --test docs-deferred-ledger.test.ts docs-dangling-refs.test.ts docs-review-disposition.test.ts docs-linerefs.test.ts` — all four green.
- `cd .claude/mcp/vice && npm run test:automated` and `npm test` — green.
- `grep -n 'OPEN, not resolved either way' docs/phase2-backend-probe-evidence.md` — no match.
- `git status` shows both closed todos as renames, not delete-plus-add.
- Every stem in `.planning/todos/pending/` appears in `STATE.md`'s Deferred Items section, and neither closed stem appears there.
</verification>

<success_criteria>
1. `docs/phase2-backend-probe-evidence.md` §1 and §2 both carry resolved verdicts that cite the committed artifacts by path, with the fallback-ladder finding recorded as unexercised rather than confirmed.
2. `docs/stock-vice-parity.md` §A item 7 warns, in one sentence, that a wrong probed implementation detail is a silently wrong answer rather than a licensed divergence.
3. The two finished todos are moved to completed with Resolution sections answering every numbered acceptance step honestly.
4. The probe-debt todo stays pending with A4 as its only remaining item and the exclusion reason written down.
5. Every finding this phase surfaced but did not fix is a filed todo with a ledger row.
6. `STATE.md`'s Deferred Items section passes its two-directional guard against the post-phase todo tree, and the unguarded count and position narrative match reality.
</success_criteria>

<output>
Create `.planning/phases/13-external-verification/13-05-SUMMARY.md` when done.
</output>
