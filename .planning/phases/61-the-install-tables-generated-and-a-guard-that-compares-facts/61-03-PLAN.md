---
phase: 61-the-install-tables-generated-and-a-guard-that-compares-facts
plan: 03
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - README.md
  - docs/phase58-declaration-provenance.md
autonomous: true
requirements: [GEN-01]
estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "No sentence in README.md attributes a VICE version floor, a shipped-version number or a gate verdict to a column of the install table, because no such column exists after generation (D-07, GEN-01)."
    - "The Windows guidance under the install table no longer directs a reader to a version number the generated table does not carry (D-07)."
    - "`src/mcp/vice/phase58-citation-ledger.test.ts` is green with every README citation's line range and anchor matching the rewritten text, in the ledger block, the document body and the YAML frontmatter together (D-08)."
    - "`docs/phase58-declaration-provenance.md` states plainly that the 27 `source` values naming a README line record where each string came from BEFORE generation, and that following one after generation lands on the generator's own output (D-09)."
    - "`docs/phase58-declaration-provenance.md` records the regeneration procedure by name, so ENGINEERING_RULES.md §11's four steps have a written home."
    - "`src/mcp/vice/prerequisites.json` is byte-unchanged (D-02) — the circularity is written down, never repaired in the declaration."
  prohibitions:
    - statement: "A known-false claim must never be left standing in documentation because correcting it is inconvenient or because a guard does not happen to check it."
      status: flagged-unverified
      verification: none
    - statement: "A historical provenance record must never be rewritten into a claim about the present; where generation makes an old citation point at output, that fact is recorded rather than the record edited away."
      status: flagged-unverified
      verification: none
  artifacts:
    - README.md
    - docs/phase58-declaration-provenance.md
  key_links:
    - "README.md's rewritten prose -> the anchor strings in docs/phase58-declaration-provenance.md's Citation ledger -> phase58-citation-ledger.test.ts"
    - "prerequisites.json's 27 README-line source values -> the generated README region they now describe -> the circularity section in the provenance document"
---

<objective>
Close the gap the generated tables open: three pieces of prose that cite columns
which no longer exist, and a provenance record that has quietly become circular.

Plan 61-01 dropped the two version columns because the declaration carries no VICE
version data. Three neighbouring passages were written against those columns — one
introduces them, one points a Windows reader at a version number they carried, and
one attributes the 3.10 floor to them outright. The third goes false the moment the
column is gone. Phase 58 refused to leave a known-false claim standing one section
further down, and the same refusal applies here (D-07).

This plan also writes down what the generation changed about provenance: the 27
`source` values in the declaration that name a README line now cite the
declaration's own output (D-09), and one remedy string is known to render
incorrectly for a reason this phase is forbidden to fix.

Purpose: the half of `GEN-01` that is about nothing hand-maintained being left
standing beside the generated section — including prose that only makes sense if
the hand-maintained columns are still there.

Output: three rewritten passages in `README.md`, a re-anchored citation ledger,
and two new sections in `docs/phase58-declaration-provenance.md`.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-the-install-tables-generated-and-a-guard-that-compares-facts/61-CONTEXT.md
@.planning/phases/61-the-install-tables-generated-and-a-guard-that-compares-facts/61-RESEARCH.md
@.planning/phases/61-the-install-tables-generated-and-a-guard-that-compares-facts/61-PATTERNS.md
@.planning/phases/61-the-install-tables-generated-and-a-guard-that-compares-facts/61-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the three passages the dropped version columns leave false</name>

  <files>README.md, docs/phase58-declaration-provenance.md</files>

  <read_first>
    - `README.md` as plan 61-01 left it — read the whole "Installing VICE" section and the "VICE version compatibility" subsection that follows it, so the three passages are located by their content rather than by a line number that has already moved once.
    - `docs/phase58-declaration-provenance.md` lines 1-60 — the YAML frontmatter's `location` and `amended_at` values, both of which carry a README citation, and "Case one: the VICE version gate", whose narrative quotes the README lead-in being rewritten here.
    - `docs/phase58-declaration-provenance.md` lines 368-405 — the Citation ledger block and the three entries whose citations and anchors this task must keep true.
    - `src/mcp/vice/phase58-citation-ledger.test.ts` lines 140-236 — the completeness and no-orphans relations: a citation in the body with no ledger entry fails, and a ledger entry with no body occurrence fails, so a citation string must change in both places at once.
    - `.planning/REQUIREMENTS.md` around line 86 — the measured claim the provenance document records as the winning side of the version-gate reversal, so the rewritten prose states the same thing the project already decided.
  </read_first>

  <action>
Rewrite three passages in `README.md`, then bring the citation ledger back to true in the same commit.

The first passage is the lead-in immediately above the generated VICE table, under the "Which VICE you get, per package manager" heading. It currently introduces the two columns the generated table no longer has. Rewrite it so it introduces only what survives: an ecosystem id, the platforms that ecosystem covers, and the command a user types. It must not assert a VICE version floor as load-bearing — `.planning/REQUIREMENTS.md`'s measured row, and the provenance document's own Case one, already settled that the floor binds an opcode no shipped tool calls. STRONGLY PREFERRED: keep the passage's first sentence — the one dating the live check of each ecosystem — intact and unmoved within the paragraph, because it is the anchor string of a citation-ledger entry and keeping it reduces this edit to a line-number change. If your rewrite genuinely cannot keep it, you have taken on three extra obligations and must discharge all three: re-anchor that ledger entry to a literal substring the NEW text contains, update Case one's surrounding prose so it no longer presents deleted README text as the document's current reading, and re-run the ledger test until green.

The second passage is the sentence under the table about Windows package availability. It points the reader at a specific version of the official downloads, a number the generated table no longer carries. Rewrite it to say what remains true — that no package-manager route to VICE exists on Windows, and the alternatives are the official download named in the table row above it or a source build — without naming a version number the declaration does not hold.

The third passage is inside the "VICE version compatibility" subsection: the clause that attributes the 3.10 floor to a column of the table above it. That clause goes outright false the moment the column is gone. Rewrite the sentence so the 3.10 floor is stated as a fact about the `CPUHISTORY_GET` opcode on its own terms, citing `.planning/REQUIREMENTS.md` as the measured source exactly as the surrounding text already does, with no reference to any table carrying it. Leave the subsection's opening sentence — the one stating that no shipped tool in this project refuses on a VICE version — byte-unchanged: it is a ledger anchor and it is still true.

Do not touch either generated region. The markers wrap the table alone (D-06); if this task changes anything between a start and end marker, it has edited generated output by hand and must be redone.

Then repair the citation ledger in `docs/phase58-declaration-provenance.md`. Every README citation's line range must match where its anchor now lives, and each citation string must be changed identically in the Citation ledger JSON block, in the document body, and in the YAML frontmatter's `location` and `amended_at` values — the guard fails a body citation with no ledger entry and a ledger entry with no body occurrence, so a one-sided edit reds it. Where a rewritten passage no longer contains its old anchor, choose a new anchor that is a literal substring of the new text and that would not survive a later rewrite of a different sentence. Run the ledger test until green; treat a red run as the guard working.
  </action>

  <reversibility rating="costly">D-07's hand rewrite is once-only: moving the marker boundary later would make the generator author English prose and would move the ledger anchors a second time.</reversibility>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && printf 'stale_table_attribution='; grep -av '^<!--' README.md | grep -ac 'named in the table above' || true; printf 'stale_version_pointer='; grep -av '^<!--' README.md | grep -ac 'zips above' || true; printf 'ledger_anchor_still_present='; grep -ac 'No shipped tool in this project refuses on a VICE version.' README.md || true</automated>
    <fails_when>stale_table_attribution= or stale_version_pointer= is followed by any value other than 0, or ledger_anchor_still_present= is followed by 0</fails_when>

    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && node --test --test-reporter=tap phase58-citation-ledger.test.ts prereq-readme-gen.test.ts prerequisites.test.ts > /tmp/61-03-t1-unit.tap 2>&1; echo "EXIT=$?"; grep -aE '^# (tests|pass|fail) ' /tmp/61-03-t1-unit.tap; grep -aE '^not ok ' /tmp/61-03-t1-unit.tap || true</automated>
    <fails_when>EXIT= is followed by a non-zero value, or the `# fail ` summary line reports anything other than 0, or any `not ok ` line is printed</fails_when>

    <automated>cd "$(git rev-parse --show-toplevel)" && npm --prefix src/mcp/vice run generate:readme > /tmp/61-03-t1-gen.txt 2>&1; echo "GEN_EXIT=$?"; printf 'generated_regions_untouched='; git diff --quiet -- README.md && echo yes || echo no; git diff --stat -- README.md</automated>
    <fails_when>GEN_EXIT= is followed by a non-zero value, or generated_regions_untouched=no (regenerating changed README.md, meaning a generated region was hand-edited or the prose edit strayed inside the markers)</fails_when>
  </verify>

  <acceptance_criteria>
    - `README.md` contains no sentence attributing a version floor, a shipped-version number or a gate verdict to a column of the install table.
    - The Windows guidance sentence names no VICE version number.
    - The "VICE version compatibility" subsection still opens with the sentence stating that no shipped tool in this project refuses on a VICE version, byte-unchanged.
    - Regenerating with `npm --prefix src/mcp/vice run generate:readme` leaves `README.md` unchanged, proving no edit strayed inside a marker pair.
    - `node --test phase58-citation-ledger.test.ts` passes.
    - Every README citation string in `docs/phase58-declaration-provenance.md` appears with the SAME text in the ledger block and in the body or frontmatter — verified by the guard's completeness and no-orphans relations, not by eye.
    - `git status --porcelain -- src/mcp/vice/prerequisites.json` prints nothing.
  </acceptance_criteria>

  <done>No passage in README.md describes a table column that no longer exists, the 3.10 floor is stated on its own terms rather than as a table citation, and the citation ledger is green in the same commit that moved the lines.</done>
</task>

<task type="auto">
  <name>Task 2: Record what generation changed about provenance</name>

  <files>docs/phase58-declaration-provenance.md</files>

  <read_first>
    - `docs/phase58-declaration-provenance.md` lines 190-240 — "Case five: one ecosystem under two platforms" and the "Authoring decisions" heading that follows it. Case five is the shape to extend: a level-2 heading and one plain-prose paragraph stating a fact and saying what a future consumer must not try to "fix".
    - `docs/phase58-declaration-provenance.md` lines 368-405 — the Citation ledger, so any `file:line` citation added by the new sections gets a matching entry with a verified anchor.
    - `src/mcp/vice/prerequisites.json` — the `source` values naming a README line, and the `acme-lib` record's `universal` remedy text.
    - `.planning/ENGINEERING_RULES.md` §11 — the four-step change procedure the regeneration note must satisfy.
    - `.planning/phases/61-the-install-tables-generated-and-a-guard-that-compares-facts/61-CONTEXT.md` § "The 27 circular citations" — D-09's exact wording, including both rejected alternatives.
  </read_first>

  <action>
Add three records to `docs/phase58-declaration-provenance.md`, each as a level-2 section placed before the Citation ledger (which must stay the document's final section), each following Case five's shape: a heading, then plain prose stating the fact and naming what a future reader must not try to repair.

First, the circular citations (D-09). State that 27 `source` values in `src/mcp/vice/prerequisites.json` name a README line; that after this phase those README lines are generated FROM the declaration, so a record whose provenance is `carried` now cites its own output; that this breaks nothing today because the ledger guard reads documents and never the JSON's `source` field; and that the claim has become historical — those values record where each string came from BEFORE generation, and following one afterwards lands on output rather than on original authorship. State both rejected alternatives and why: a commit-pinned citation syntax nothing else in this repo uses, and re-pointing `source` at the generator, which would destroy the historical record that is the whole reason a `carried` entry carries a `source` at all. State that the declaration is unchanged, which is the owner decision D-02 and not a matter for a later agent to revisit.

Second, the one remedy string that is known to render incorrectly. The `acme-lib` record's `universal` remedy text contains an angle-bracketed placeholder naming the directory that holds the ACME library's marker file. A generated cell is the declaration's text character for character (D-05), so the placeholder reaches the README as written — and a markdown renderer reads it as an HTML tag and drops it, so the rendered cell displays without the placeholder even though the committed bytes are correct. Record this as what the phase's own cross-cutting constraint calls it: an authoring defect in the declaration surfaced late by generation, not a formatting decision to be taken in the generator. State that both available repairs are foreclosed here — escaping the cell is foreclosed by D-05, and rewording the declaration is foreclosed by D-02 — and that closing it belongs to a future phase that is allowed to edit the declaration. Name the record and the field so the future phase does not have to rediscover them.

Third, the regeneration procedure. State the four steps `ENGINEERING_RULES.md` §11 requires — update the declaration, regenerate the artifact, run the drift guard, verify the delta is expected — mapped onto this generator by name: the source of truth is `src/mcp/vice/prerequisites.json`, the regeneration command is the `generate:readme` npm script, the drift guard is `src/mcp/vice/prereq-readme-gen.test.ts`, and the expected delta is confined to the two marker-delimited regions of `README.md`. Add the consequence a maintainer will hit: regenerating shifts `README.md` line numbers, which reds the citation-ledger guard, and that is the ledger doing its job — the entries get re-anchored in the same commit, never routed around.

Any `file:line` citation these sections introduce must be added to the Citation ledger block with an anchor that is a literal substring of the cited range, because the guard fails both a body citation with no ledger entry and a ledger entry with no body occurrence. If a fact reads better without a line pin, write it without one — a citation that must track a moving file forever is a liability, and this document already carries the ones it needs.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/src/mcp/vice" && node --test --test-reporter=tap phase58-citation-ledger.test.ts > /tmp/61-03-t2-ledger.tap 2>&1; echo "EXIT=$?"; grep -aE '^# (tests|pass|fail) ' /tmp/61-03-t2-ledger.tap; grep -aE '^not ok ' /tmp/61-03-t2-ledger.tap || true</automated>
    <fails_when>EXIT= is followed by a non-zero value, or the `# fail ` summary line reports anything other than 0, or any `not ok ` line is printed</fails_when>

    <automated>cd "$(git rev-parse --show-toplevel)" && printf 'new_sections='; grep -ac '^## ' docs/phase58-declaration-provenance.md; printf 'ledger_is_last_section='; tail -n +"$(grep -an '^## Citation ledger' docs/phase58-declaration-provenance.md | tail -1 | cut -d: -f1)" docs/phase58-declaration-provenance.md | grep -ac '^## ' ; printf 'acme_lib_recorded='; grep -ac 'acme-lib' docs/phase58-declaration-provenance.md || true; printf 'regen_command_recorded='; grep -ac 'generate:readme' docs/phase58-declaration-provenance.md || true</automated>
    <fails_when>ledger_is_last_section= is followed by any value other than 1 (a section was added after the Citation ledger), or acme_lib_recorded= or regen_command_recorded= is followed by 0</fails_when>

    <automated>cd "$(git rev-parse --show-toplevel)" && printf 'declaration_unchanged:\n'; git status --porcelain -- src/mcp/vice/prerequisites.json src/mcp/vice/resources/prerequisites.json; printf 'readme_unchanged_by_this_task='; git diff --quiet HEAD -- README.md && echo yes || echo no</automated>
    <fails_when>any line is printed under declaration_unchanged:, or readme_unchanged_by_this_task=no</fails_when>
  </verify>

  <acceptance_criteria>
    - Three new level-2 sections exist in `docs/phase58-declaration-provenance.md`, all positioned before the `## Citation ledger` heading, which remains the document's final section.
    - The circularity section names the count of affected `source` values, states that nothing audits them today, names both rejected alternatives, and states that the declaration is unchanged under D-02.
    - The rendering-defect section names the `acme-lib` record and the field carrying the placeholder, states why escaping and rewording are both foreclosed here, and hands the fix to a future phase.
    - The regeneration section names `src/mcp/vice/prerequisites.json`, the `generate:readme` script, `src/mcp/vice/prereq-readme-gen.test.ts`, and the two marker-delimited regions, and states the citation-ledger consequence.
    - `node --test phase58-citation-ledger.test.ts` passes, so every citation added by these sections has a matching ledger entry with a resolving anchor.
    - `git status --porcelain -- src/mcp/vice/prerequisites.json` prints nothing.
    - `README.md` is unchanged by this task.
  </acceptance_criteria>

  <done>The provenance document states plainly that the declaration's README-line citations have become historical, records the one remedy string that renders wrong and why this phase may not fix it, and names the exact four-step regeneration procedure a maintainer follows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `README.md` prose → a reader's shell | Install guidance a human reads and types. The only place a documented string becomes an executed command. |
| `docs/phase58-declaration-provenance.md` → `phase58-citation-ledger.test.ts` | A machine-read ledger block whose entries are re-verified against the live cited files on every run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-08 | Repudiation | Documentation asserting a version floor or gate verdict the project has measured to be non-binding | high | mitigate | The three rewritten passages state the floor as a fact about an opcode no shipped tool calls, citing the measured row in `.planning/REQUIREMENTS.md`, matching the provenance document's own recorded reversal. Negative checks assert the stale attributions are gone. |
| T-61-09 | Tampering | A one-sided citation edit disarming the ledger guard | medium | mitigate | The guard's completeness and no-orphans relations fail a body citation with no ledger entry and a ledger entry with no body occurrence; the task requires each citation string to change in the block, the body and the frontmatter together, and the ledger test to be green in the same commit. |
| T-61-10 | Information Disclosure | A remedy cell rendering without its placeholder, so a reader types an incomplete command | medium | accept | Both repairs are foreclosed by locked decisions (D-05 forbids escaping, D-02 freezes the declaration). The defect is recorded by name in the provenance document and handed to a future phase, rather than silently reworded to fit the renderer. |
| T-61-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan; it edits two markdown files. `61-RESEARCH.md` § Package Legitimacy Audit records zero packages proposed. |
</threat_model>

<verification>
- `cd src/mcp/vice && node --test phase58-citation-ledger.test.ts` is green.
- `cd src/mcp/vice && npm run test:automated` is green, compared as a failing SET against the pre-plan baseline.
- `npm --prefix src/mcp/vice run generate:readme` leaves `README.md` unchanged, proving no hand edit strayed inside a generated region.
- `git status --porcelain -- src/mcp/vice/prerequisites.json src/mcp/vice/resources/prerequisites.json` prints nothing.
- No file is deleted by this plan.
</verification>

<success_criteria>
- Nothing hand-maintained is left standing beside the generated section, including prose that only made sense while the hand-maintained columns existed (`GEN-01`).
- The citation ledger is true against the rewritten README, re-anchored in the same commit that moved the lines (D-08).
- The circularity, the known rendering defect and the regeneration procedure are all written down where a maintainer will find them (D-09, ENGINEERING_RULES.md §11).
</success_criteria>

<output>
Create `.planning/phases/61-the-install-tables-generated-and-a-guard-that-compares-facts/61-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Symbols, files and strings created by Phase 61 that this plan references or adds
(exclude from drift verification against the pre-phase tree):

**Files created by plan 61-01 and named here**
- `src/mcp/vice/prereq-readme-gen.ts`
- `src/mcp/vice/prereq-readme-gen.test.ts`

**New npm script named here**
- `generate:readme` in `src/mcp/vice/package.json`

**New marker strings in `README.md` named here**
- `<!-- prereq-gen:vice-ecosystems:start -->` / `<!-- prereq-gen:vice-ecosystems:end -->`
- `<!-- prereq-gen:prerequisite-overview:start -->` / `<!-- prereq-gen:prerequisite-overview:end -->`

**New sections this plan adds to `docs/phase58-declaration-provenance.md`**
- a circular-citation record (D-09)
- a known-rendering-defect record for the `acme-lib` remedy
- a regeneration-procedure record mapping ENGINEERING_RULES.md §11's four steps onto this generator

**New citation-ledger entries**
- whatever `file:line` citations the three new sections introduce, each with a re-verified anchor

## Flagged Assumptions

1. **The preferred rewrite keeps one sentence unmoved to spare a ledger anchor.**
   That is an editorial constraint chosen for guard economy, not for prose quality.
   If the executor judges the sentence genuinely wrong after the version columns
   go, the plan permits replacing it — at the cost of three extra obligations,
   stated in the task. A reviewer should confirm the executor took the path it
   claims to have taken.
2. **Two YAML frontmatter citations in the provenance document are historical
   records of what Phase 58 did**, and this plan re-points their line numbers
   rather than de-pinning them, because the ledger guard's no-orphans relation
   fails a body citation with no ledger entry. De-pinning them — writing the
   historical statement without a line number — is the alternative, and is
   arguably more honest about a record of the past; it was not taken because it
   changes a Phase 58 artifact's shape for a Phase 61 convenience.
3. **The `acme-lib` rendering defect is accepted, not fixed.** It is the one place
   in this phase where the generated documentation is known to display something
   other than what the user should type — the exact failure the phase's
   cross-cutting constraint warns about. Both repairs are foreclosed by locked
   decisions. This is raised to the owner in the planning handback as a gap for a
   future phase, and a reviewer should decide whether it is tolerable to ship the
   milestone with it open.
