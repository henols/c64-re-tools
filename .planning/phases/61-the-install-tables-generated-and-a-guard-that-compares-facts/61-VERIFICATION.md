---
phase: 61-the-install-tables-generated-and-a-guard-that-compares-facts
verified: 2026-09-19T00:00:00Z
status: human_needed
score: 4/4 roadmap success criteria verified; 1 plan-level must-have needs human confirmation
behavior_unverified: 0
overrides_applied: 0
covered_files:
  - ".planning/REQUIREMENTS.md"
  - ".planning/phases/61-the-install-tables-generated-and-a-guard-that-compares-facts/61-01-PLAN.md"
  - ".planning/phases/61-the-install-tables-generated-and-a-guard-that-compares-facts/61-01-SUMMARY.md"
  - ".planning/phases/61-the-install-tables-generated-and-a-guard-that-compares-facts/61-02-PLAN.md"
  - ".planning/phases/61-the-install-tables-generated-and-a-guard-that-compares-facts/61-02-SUMMARY.md"
  - ".planning/phases/61-the-install-tables-generated-and-a-guard-that-compares-facts/61-03-PLAN.md"
  - ".planning/phases/61-the-install-tables-generated-and-a-guard-that-compares-facts/61-03-SUMMARY.md"
  - "README.md"
  - "docs/phase58-declaration-provenance.md"
  - "src/mcp/vice/package.json"
  - "src/mcp/vice/prereq-readme-gen.test.ts"
  - "src/mcp/vice/prereq-readme-gen.ts"
  - "src/mcp/vice/prerequisites.test.ts"
covered_digest: "v1:sha256:824b59b35f72db8e195a766997dd332e29bca664778bf68c85b8fab135422124"
human_verification:
  - test: "Decide whether the plan-level must-have 'phase58-citation-ledger.test.ts is green after every commit that moves a README.md line (D-08)' is satisfied given the test FILE's overall exit code is 1."
    expected: "A decision on record: either (a) accept that the substantive D-08 concern (every README.md-pointing ledger entry resolves correctly) is fully met and the file-level red is an out-of-scope pre-existing issue, or (b) require a gap-closure plan before treating GEN-01/GEN-02/GEN-03 as fully closed."
    why_human: "This is a wording/scope judgment, not a fact grep can resolve. The literal must-have text says the FILE is green; it is not (2 of 11 tests fail, exit code 1). But every failure is independently confirmed to be a `.planning/REQUIREMENTS.md` or `.planning/ROADMAP.md` line-drift or dropped-anchor issue caused by Phase 61's own planning commits growing those files (plus one anchor whose supporting sentence was deleted when the doctor phase was dropped) -- none of the 5 failures touch README.md, and no plan in this phase declared `.planning/ROADMAP.md` or `.planning/REQUIREMENTS.md` in `files_modified`. The executor itself flagged this exact ambiguity as `human_judgment: true` in both 61-01-SUMMARY.md and 61-03-SUMMARY.md rather than silently claiming pass."
---

# Phase 61: The Install Tables Generated, and a Guard That Compares Facts Verification Report

**Phase Goal:** `README.md`'s per-platform install tables stop being maintained by hand. They
are generated from the declaration, and a build-failing guard catches divergence between the
two by comparing parsed records — never bytes.

**Verified:** 2026-09-19
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

All four roadmap success criteria were independently re-proven by behavior in this
verification pass — not accepted from SUMMARY.md narrative. Where a claim was
behavioral (does the guard actually fail? does reflow actually stay green?), a fresh
scratch-tree script was written and executed against the real `auditGeneratedReadme()`
export, independent of the plan's own committed test cases.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The tables are generated and the hand-kept ones are gone (`GEN-01`). | ✓ VERIFIED | Both `<!-- prereq-gen:vice-ecosystems:... -->` and `<!-- prereq-gen:prerequisite-overview:... -->` regions exist in `README.md`, each with a "Generated by ... do not hand-edit" banner. Negative greps for the old hand-maintained table's signatures (`Clears the 3.10 gate`, `^| Debian 13`) return 0 hits. `npm --prefix src/mcp/vice run generate:readme` run twice produced byte-identical output and `git diff --quiet -- README.md` succeeds (no drift from HEAD). |
| 2 | The guard compares facts, and fails the build when they differ; no byte comparison anywhere (`GEN-02`). | ✓ VERIFIED | Read `auditGeneratedReadme()`/`parseGeneratedRegion()` source directly: both sides parse into `{ecosystem, platforms, text}` / row-cell records and compare fields, never whole-string/byte equality. `grep -a` for `sha256`, `Buffer.compare`, `deepStrictEqual`-on-rendered-text found nothing in either file. Independently planted a divergence in a fresh `mkdtempSync(tmpdir())` scratch copy of the real declaration+README (not the plan's own fixture) — mutating `x64sc`'s `debian-trixie` remedy text and leaving the README stale — and confirmed `auditGeneratedReadme()` returned exactly one failure naming `debian-trixie`, the mismatched text, and the regen command. |
| 3 | The guard is watched failing before it is trusted; the planted control is committed, not described (`GEN-03`). | ✓ VERIFIED | `src/mcp/vice/prereq-readme-gen.test.ts` contains 5 committed planted-divergence cases (changed ecosystem remedy, changed universal remedy, removed record, emptied region, removed marker) — confirmed present as real `test()` calls, not just SUMMARY prose, by running the file directly (`node --test`: 20/20 green, including all 5 by name). Independently re-ran two of the five shapes myself in a throwaway script against the real files (a changed remedy text, and a stripped end-marker / emptied-region pair) and got matching non-empty, correctly-named failures both times. |
| 4 | A change that alters no fact leaves the guard green (roadmap criterion 4). | ✓ VERIFIED | Independently constructed a scratch README with the ecosystem region's 8 rows reversed, every cell repadded with extra spaces, and a blank line inserted after the header separator — `auditGeneratedReadme()` returned `[]` (0 failures). This reproduces, from a script I wrote myself rather than the committed test file, the same tolerance the committed `prereq-readme-gen.test.ts` cases assert. |
| 5 (plan-level, D-08) | `phase58-citation-ledger.test.ts` is green after every commit that moves a README.md line. | ⚠️ Ambiguous — see Human Verification | The test FILE's overall exit code is 1 (2 of 11 subtests fail). Every one of the 5 individual anchor failures inside those 2 subtests was independently confirmed to cite `.planning/REQUIREMENTS.md` or `.planning/ROADMAP.md` — never `README.md` — and to be caused by Phase 61's own planning commits growing those files (4 cases) or by a deliberately-dropped doctor-phase sentence (1 case, commit `67a0d810`). No README.md-citing ledger entry fails. |

**Score:** 4/4 roadmap success criteria independently verified true by behavior. 1 additional
plan-level must-have (D-08, literal wording) is ambiguous between "substantively true" and
"file exit code red" and is routed to human judgment rather than silently marked either way.

### Cross-Cutting Constraints

| Constraint | Status | Evidence |
|---|---|---|
| Generator never runs/spawns/implies a remedy command on the user's behalf | ✓ VERIFIED | `grep -a` for `exec`/`spawn`/`child_process` in `prereq-readme-gen.ts` found only the module-header comment stating the prohibition; no such call exists in the module. |
| Generating tables must not change what a user is told to type | ✓ VERIFIED | D-05 (character-for-character cell rendering) is enforced structurally — `renderEcosystemTable`/`renderOverviewTable` interpolate `text` with no escaping/wrapping — and proven by the committed round-trip tests (`parseGeneratedRegion(renderEcosystemTable(deriveEcosystemRows(decl)))` deepEqual to the derived rows, including the debian-trixie em dash), independently re-run and green. |
| D-02: `src/mcp/vice/prerequisites.json` byte-unchanged by this phase | ✓ VERIFIED | `git diff 85e627a7..HEAD -- src/mcp/vice/prerequisites.json` empty (measured by orchestrator); independently re-confirmed via `git status --porcelain -- src/mcp/vice/prerequisites.json` (clean) at verification time. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/mcp/vice/prereq-readme-gen.ts` | Generator module: derive/render/splice functions | ✓ VERIFIED | Exists, plain `.ts`, exports all constants/functions the plans specify (`REGENERATE_COMMAND`, `PLATFORM_LABELS`, `deriveEcosystemRows`, `deriveOverviewRows`, `renderEcosystemTable`, `renderOverviewTable`, `spliceRegion`, `writeGeneratedRegions`, etc.). Absent from both `build.ts`'s `HOST_BOUND_ARTIFACTS`/`HOST_BOUND_DATA_FILES` and from `package.json`'s `files[]` (confirmed by grep), matching D-13. |
| `src/mcp/vice/prereq-readme-gen.test.ts` | Colocated drift guard | ✓ VERIFIED | Exists, exports `parseGeneratedRegion`/`auditGeneratedReadme`, 20 test cases, `node --test`: 20/20 pass (re-run independently, not just cited from SUMMARY). |
| `README.md`'s generated regions | Two marker-delimited, generated regions | ✓ VERIFIED | Both present, 8 rows each, idempotent regeneration confirmed. |
| `docs/phase58-declaration-provenance.md` new sections | Circularity (D-09), rendering defect, regeneration procedure | ✓ VERIFIED | All three level-2 sections present, positioned before `## Citation ledger` (which remains the final section — confirmed via `grep -n '^## '`), each substantively stating what the plan required (read in full). |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `src/mcp/vice/prerequisites.json` | `README.md`'s two generated regions | `deriveEcosystemRows`/`deriveOverviewRows` → `render*Table` → `spliceRegion` | ✓ WIRED | Confirmed by running `generate:readme` and observing the committed README's rows exactly match the declaration's records field-for-field. |
| `prereq-readme-gen.ts`'s derive functions | `prereq-readme-gen.test.ts`'s guard | Shared-derivation import (D-10) | ✓ WIRED | Guard file imports and calls the generator's own `deriveEcosystemRows`/`deriveOverviewRows`/`renderEcosystemTable`/`renderOverviewTable` rather than re-implementing row expectations (confirmed by reading the guard's audit functions). |
| `README.md` line positions | `docs/phase58-declaration-provenance.md`'s Citation ledger | `phase58-citation-ledger.test.ts` | ✓ WIRED for README.md entries; ⚠️ see D-08 above for the file's other (unrelated) entries | Zero README.md-citing ledger-entry failures found in an independent run of the full ledger test. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Generator is idempotent | `npm --prefix src/mcp/vice run generate:readme` (x2), compare sha256 | Identical both runs; `git diff --quiet -- README.md` succeeds | ✓ PASS |
| Guard fails on a self-planted divergence (independent of committed fixtures) | Custom script: mutate `x64sc`/`debian-trixie` text in a `mkdtempSync` copy, call `auditGeneratedReadme` | 1 failure, correctly named | ✓ PASS |
| Guard tolerates reflow (independent of committed fixtures) | Custom script: reverse row order + repad + insert blank line in a `mkdtempSync` copy | 0 failures | ✓ PASS |
| Guard cannot be disarmed by emptying a region or stripping a marker | Custom script: empty ecosystem region's data rows; separately, strip the end marker | Both report a named failure, neither throws | ✓ PASS |
| No byte/hash comparison hides in generator or guard | `grep -a` for `sha256`, `Buffer.compare`, whole-string `deepStrictEqual` on rendered markdown | None found | ✓ PASS |
| Committed guard test suite | `node --test prereq-readme-gen.test.ts` | 20/20 pass | ✓ PASS |
| Full workspace suite (single run) | `npm --prefix src/mcp/vice test` | 4051 tests, 3968 pass, 2 fail, 81 skipped, exit 1 | ⚠️ 2 pre-existing failures, both `.planning/`-only citation-ledger anchors — see D-08 discussion above |
| Typecheck | `npm --prefix src/mcp/vice run typecheck` | exit 0 | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| `GEN-01` | 61-01, 61-03 | Tables generated from declaration, no hand-maintained copy left | ✓ SATISFIED | Both generated regions present and idempotent; 61-03's prose rewrite removed all stale table-attribution sentences (verified via negative grep). `REQUIREMENTS.md` marks it Complete, consistent with the evidence. |
| `GEN-02` | 61-01, 61-02 | Build-failing guard compares parsed records, never bytes; tolerant of reflow | ✓ SATISFIED | Record-based comparison confirmed by source read and independent behavioral test; reflow tolerance independently reproduced. |
| `GEN-03` | 61-02 | Guard proven non-vacuous by a committed planted divergence | ✓ SATISFIED | 5 committed cases; 2 reproduced independently outside the committed fixtures with matching results. |

No orphaned requirements: `.planning/REQUIREMENTS.md` maps only `GEN-01`/`GEN-02`/`GEN-03` to
Phase 61, and all three are claimed by at least one of the three plans' `requirements` fields.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `src/mcp/vice/prereq-readme-gen.ts` | 243 | A raw NUL byte (`0x00`), not a space, sits inside the `(ecosystem, text)` dedup-key template literal — confirmed independently: `readFileSync(f).indexOf(0)` returns `10247`, `file(1)` reports the module as `data`, plain `grep` (no `-a`) returns zero matches. | ⚠️ Warning | Silently adds a *third* file to this project's own documented "NUL byte hides a file from grep" hazard list (already true of `anno-memmap-render.ts` and `prerequisites.test.ts`). Does not affect current behavior (a NUL byte works as a separator) but is a real maintainability hazard. Already filed as WR-01 in `61-REVIEW.md`; not independently re-fixed here since it was in scope for that gate, not this one. |
| `src/mcp/vice/prereq-readme-gen.ts` (audit consumer: `prereq-readme-gen.test.ts:186-191`) | 229-256 / 186-191 | `deriveEcosystemRows` dedupes by `(ecosystem, text)`, but the audit's `expectedByEco`/`actualByEco` maps key by `ecosystem` alone, so two rows sharing an ecosystem id with *different* text would silently collapse to whichever occurs last on each side. | ⚠️ Warning (latent, not live) | Confirmed the committed `x64sc` declaration has no such collision today, so nothing is red now. This is a real gap in the guard's totality against a hypothetical future declaration shape, already filed as WR-02 in `61-REVIEW.md`. |
| `src/mcp/vice/prereq-readme-gen.ts` | 262-267, 301-310 | No structural check rejects a remedy `text` containing a literal `|` or newline, which would misalign a rendered table row on both the generator and parser side identically. | ℹ️ Info | No current remedy value triggers this (measured: longest is 157 chars, none contain `|`/`\n`). Already filed as IN-01 in `61-REVIEW.md`. |

None of the three findings above are TBD/FIXME/XXX debt markers, and none is a blocking
anti-pattern under the decision tree (they are code-review Warnings/Info already filed and
disclosed in `61-REVIEW.md`, not newly discovered gaps). They do not change the status
determination on their own.

## Human Verification Required

### 1. Confirm the D-08 must-have's intended reading against the actual citation-ledger failure

**Test:** Read the plan-level must-have in `61-01-PLAN.md`: "`src/mcp/vice/phase58-citation-ledger.test.ts` is green after every commit that moves a README.md line (D-08)." Then run `node --test src/mcp/vice/phase58-citation-ledger.test.ts` and inspect the two failing subtests.

**Expected:** A decision on record — either the substantive reading (every README.md-pointing ledger entry resolves, which is independently confirmed true) satisfies D-08 and the file's other red assertions are an accepted pre-existing/out-of-scope condition, or a gap-closure plan is required to re-anchor the 4 drifted `.planning/ROADMAP.md`/`.planning/REQUIREMENTS.md` citations and resolve the 1 deleted anchor before the phase is considered fully closed.

**Why human:** This is a scope/wording judgment the executor itself could not resolve and explicitly flagged twice (`human_judgment: true` in both `61-01-SUMMARY.md` and `61-03-SUMMARY.md`) rather than silently claiming pass or fail. The underlying facts are settled (5 failures, all `.planning/`-only, none touching README.md, none introduced by this phase's own `files_modified`), but whether the must-have's literal "green" wording is a hard gate or a proxy for "the README.md-specific concern D-08 exists to protect" is not something grep can decide.

## Gaps Summary

No gap blocks the phase goal. All four ROADMAP.md success criteria for Phase 61 were
independently re-proven true by direct behavioral testing against the real declaration and
README — not merely accepted from SUMMARY.md narrative. `GEN-01`, `GEN-02`, and `GEN-03` are
satisfied. `D-02`'s freeze on `prerequisites.json` held throughout.

One item is routed to human judgment rather than auto-passed: a plan-level must-have's
literal wording ("the citation-ledger test file is green") is not met at the file-exit-code
level, because 2 of its 11 subtests fail on 5 anchors that all cite `.planning/ROADMAP.md`
or `.planning/REQUIREMENTS.md` — never `README.md` — and all five are attributable to Phase
61's own planning-document growth (4) or a deliberately dropped doctor-phase sentence (1),
not to any change any of the three plans made under their declared `files_modified`. This is
a pre-existing, already-disclosed condition (documented identically in all three plan
SUMMARYs), not a regression this verification discovered. It has no bearing on whether
README's install tables are generated and guarded, which is the phase's actual goal — but
because the literal must-have text is not met, it is surfaced for a human decision rather
than silently resolved either way.

Three code-review findings (a NUL byte hiding a file from plain grep, a latent
same-ecosystem-different-text audit-key collision, and no `|`/newline validation on remedy
text) are real and already filed in `61-REVIEW.md`; none is live against the committed data
today and none blocks the roadmap criteria.

---

_Verified: 2026-09-19_
_Verifier: Claude (gsd-verifier)_
