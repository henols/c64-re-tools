---
phase: 11-annotation-store-enums-and-the-symbol-round-trip
plan: 03
type: execute
wave: 1
depends_on: []
worktree: false
worktree_reason: "This plan's deliverables include .planning/ROADMAP.md, .planning/REQUIREMENTS.md, a Phase 10 CONTEXT.md edit and two todo-file moves. Worktree isolation strips ROADMAP.md and STATE.md from executor commits (precedent: plans 09-08 and 10-09), so under worktree isolation this plan reports success while delivering nothing. It also performs two intended `git mv` deletions out of .planning/todos/pending/, which the cleanup wave blocks on when they arrive from a worktree branch. Run it sequentially on the main tree."
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-CONTEXT.md
  - .planning/todos/pending/2026-08-20-vsf-as-a-bootstrap-input.md
  - .planning/todos/completed/2026-08-20-npm-closure-walk-blind-to-dynamic-imports.md
  - .planning/todos/completed/2026-08-20-r2000-review-residual-findings.md
  - README.md
  - CLAUDE.md
  - .claude/mcp/vice/stock-symbols.ts
  - .claude/mcp/vice/docs-linerefs.test.ts
autonomous: true
requirements: [R2000-13, R2000-14]
must_haves:
  truths:
    - "D-34: no document in this repo points at Phase 11 as `.vsf`'s home; the deferral is a backlog item carrying the three real reasons instead of a dangling forward reference"
    - "D-35: `stock-symbols.ts`'s header states `--export_lbl` compatibility as VERIFIED, scoped to regenerator2000 0.9.20 and the `probe-illegal.prg` fixture — and no wider"
    - "D-22: `R2000-13`'s wording no longer credits `--dump-enum-files` as an install path; the install path named is `r2000_create_project_enum`"
    - "D-15/D-19: the README's one-project limit is narrowed to the `--mcp-server` HTTP route, because this project's route (`--mcp-server-stdio`, D-16) binds no port at all"
    - "CLAUDE.md's `rewriteArguments()` line citations match the source, and a test now fails when they drift again"
  artifacts:
    - path: ".planning/todos/pending/2026-08-20-vsf-as-a-bootstrap-input.md"
      provides: "The `.vsf` deferral as a backlog item with its three real reasons"
      contains: "machine-type"
    - path: ".claude/mcp/vice/stock-symbols.ts"
      provides: "A verified, explicitly scoped --export_lbl compatibility note"
      contains: "al C:0810 .init_screen"
    - path: ".claude/mcp/vice/docs-linerefs.test.ts"
      provides: "A drift guard binding CLAUDE.md's cited line numbers to the real call sites"
      contains: "rewriteArguments"
  key_links:
    - from: "CLAUDE.md"
      to: ".claude/mcp/vice/vice-proxy.ts"
      via: "cited line numbers for the two rewriteArguments() call sites, now mechanically checked"
      pattern: "vice-proxy\\.ts:2943"
---

<objective>
Deliver the four scope corrections this phase owes as artifacts rather than notes, and convert
one of them from a recurring manual chore into a test.

Four corrections, each with its own reason:

1. **D-34 — `.vsf` is out of Phase 11, and three documents currently say otherwise.** Phase 10's
   D-03 deferred `.vsf` to "Phase 11's `c64-ram-capture` extension, `R2000-14`/`R2000-15`", but
   those two requirements are about symbols, not snapshots. The deferral points at a home that
   does not exist.
2. **D-35 — `stock-symbols.ts` carries a "STATED ASSUMPTION, NOT A VERIFIED FACT" note that is
   now false.** `--export_lbl` was run and emitted `al C:0810 .init_screen`, matching that file's
   own parser regex exactly. The note must become a verified claim, scoped to 0.9.20 and this
   fixture — the same scoping caveat ROADMAP.md already applies to Phase 9's criterion 3(3).
3. **D-22 — `R2000-13`'s own wording miscredits `--dump-enum-files`.** That flag writes the three
   built-in enums to a directory and exits (`assets.rs:445-468`); it is how you learn the TOML
   shape, not an install path. Plan 11-06 implements the real install path and must not inherit a
   requirement that describes a different mechanism.
4. **D-15/D-19 — the documented one-project limit is wider than the truth.** D-16 chose
   `--mcp-server-stdio`, which binds no port; the limit applies only to the `--mcp-server`
   HTTP-on-3000 route. CONTEXT.md's own instruction is to NARROW the documented limitation, not
   to build detection for it.

Plus the drift CLAUDE.md's own bullet asks to be re-verified: `rewriteArguments()`'s two call
sites are at `vice-proxy.ts:2943` (inside `forwardToVice()`, which starts at `:2878`) and
`vice-proxy.ts:1422` (inside `gatherWedgeEvidence()`, starting at `:1398`) — both drifted from the
cited `2889`/`1368`, independently re-verified twice.

Purpose: a dangling forward reference and a stale "unverified" note both send the next session
looking for something that is not there.

Output: corrected ROADMAP/REQUIREMENTS/CONTEXT/README/CLAUDE prose, a real backlog item for
`.vsf`, the two folded todos moved to `completed/`, and a drift guard for the line citations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/11-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-34 — remove the `.vsf` dangling forward reference at all three sites and file it as backlog</name>
  <files>.planning/ROADMAP.md, .planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-CONTEXT.md, .planning/todos/pending/2026-08-20-vsf-as-a-bootstrap-input.md</files>
  <read_first>
    - `.planning/ROADMAP.md` line 187 (the standing constraint "Prefer `.vsf` over `.raw` for anything leaving the emulator"), line 291 (Phase 10 criterion 3's parenthetical naming "Phase 11's `c64-ram-capture` extension, `R2000-14`/`R2000-15`"), lines 337-342 (Phase 9's own `.vsf` machine-type amendment), and line 389 (the `R2000-08` cut-table row that also says "Prefer `.vsf`")
    - `.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-CONTEXT.md` — its Deferred Ideas section's `.vsf` entry (D-03)
    - `docs/phase9-regenerator2000-probe-findings.md` § Accepted limits entry 2 — the machine-type unreliability finding, the evidence the backlog item cites
    - `.planning/todos/pending/2026-08-20-warp-over-resource-set-refuted-on-stock-3-10.md` — an existing todo file, for the frontmatter/section shape to match
  </read_first>
  <action>
Edit all three sites so none of them names Phase 11 as `.vsf`'s home:

- ROADMAP.md line 291 (Phase 10 criterion 3's parenthetical): replace the trailing clause
  "its home is Phase 11's `c64-ram-capture` extension, `R2000-14`/`R2000-15`" with a statement
  that no `R2000-*` requirement covers `.vsf` as a bootstrap input, that Phase 11 confirmed this
  (D-34), and that it is filed as backlog at
  `.planning/todos/pending/2026-08-20-vsf-as-a-bootstrap-input.md`.
- ROADMAP.md line 187 (the standing constraint): keep "prefer `.vsf` over `.raw` for anything
  leaving the emulator" as the emulator-output rule it is, and add the qualifier that it does
  **not** extend to regenerator2000's input set — the D-01 synthesis route never hands
  regenerator2000 a container format, so `.prg`/`.d64`/flat-64K is the whole supported input set.
  Cross-reference the Phase 9 machine-type finding.
- ROADMAP.md line 389 (`R2000-08`'s cut-table row): same qualifier in one clause, so the table
  does not contradict the constraint it summarises.
- `10-CONTEXT.md`'s Deferred Ideas `.vsf` entry: append that Phase 11's D-34 resolved the pointer
  and where it now lives. Do not rewrite D-03's original reasoning — it was correct about
  deferring, only wrong about the destination.

Create `.planning/todos/pending/2026-08-20-vsf-as-a-bootstrap-input.md` matching the existing
todo files' shape, recording the three real reasons: (a) no `R2000-*` requirement covers it —
`R2000-14`/`R2000-15` are about symbols; (b) Phase 9 found `.vsf` machine-type auto-detection
unreliable (traced to a coincidental default, findings doc § Accepted limits entry 2), while RAM
content and start address remain reliable; (c) the D-01 synthesis route builds the project file
in Node and never hands regenerator2000 a container format, so parsing VICE snapshots ourselves
would be new work. State what would change the decision: a consumer who has only `.vsf` captures
and cannot re-capture as `.raw`.
  </action>
  <verify>
    <automated>test -f .planning/todos/pending/2026-08-20-vsf-as-a-bootstrap-input.md && ! grep -rn "Phase 11's \`c64-ram-capture\` extension" .planning/ROADMAP.md .planning/phases/10-*/10-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "vsf" .planning/ROADMAP.md | grep -c "Phase 11"` returns 0
    - The new todo file exists and contains all three reasons (grep finds `machine-type`, `R2000-14`, and `synthes`)
    - ROADMAP.md line ~187's constraint still says to prefer `.vsf` for emulator OUTPUT and now explicitly excludes regenerator2000's input set
    - `10-CONTEXT.md`'s `.vsf` deferred-ideas entry names D-34 and the new todo path
  </acceptance_criteria>
  <done>Nothing in `.planning/` or `docs/` points a future session at Phase 11 for `.vsf`.</done>
</task>

<task type="auto">
  <name>Task 2: D-35, D-22 and the D-15 narrowing — three prose claims made true</name>
  <files>.claude/mcp/vice/stock-symbols.ts, .planning/REQUIREMENTS.md, README.md</files>
  <read_first>
    - `.claude/mcp/vice/stock-symbols.ts` lines 20-90 — the header's STATED-ASSUMPTION block (26-33), the never-a-second-resolver-holder rule, the replace-not-merge rule (40-42), `VICE_LABEL_LINE_RE` at line 75, and the three ceilings at 79-81
    - `.planning/REQUIREMENTS.md` lines 92-98 — `R2000-13`'s wording, including the `--dump-enum-files` claim
    - `README.md` lines 198-206 — the "one-project-per-network-namespace limit is stated, not detected" paragraph
    - `.planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/11-CONTEXT.md` — D-35's measured evidence and D-22's correction wording; the `assets.rs:445-468` citation for what `--dump-enum-files` actually does
    - `docs/phase9-regenerator2000-probe-findings.md` § Accepted limits entry 3 — the scoping caveat to reuse verbatim in shape
  </read_first>
  <action>
**D-35 (`stock-symbols.ts`).** Replace the "STATED ASSUMPTION, NOT A VERIFIED FACT" sentence and
its consequence clause with a verified claim that states: regenerator2000 0.9.20's `--export_lbl`
was run and emitted `al C:0810 .init_screen`, which matches this module's own
`VICE_LABEL_LINE_RE` (`/^al\s+C:([0-9a-fA-F]{1,4})\s+\.(\S+)/`) exactly; the claim is scoped to
**regenerator2000 0.9.20 and the `probe-illegal.prg`-derived fixture**, not to all inputs
forever — the same caveat ROADMAP.md applies to Phase 9's criterion 3(3) `pass`. Keep the
skip-unrecognised-lines behaviour and say why it is still right under a verified format (a
future version, a hand-edited file, or a different exporter can still produce lines this parser
should ignore rather than reject the whole file). Do NOT touch the replace-not-merge rule or the
ceilings — plan 11-07 depends on both, unchanged.

**D-22 (`REQUIREMENTS.md` `R2000-13`).** Rewrite the sentence crediting regenerator2000 with
"the enum mechanism and `--dump-enum-files`" so it names the real install path,
`r2000_create_project_enum` (project-level, per D-21), and states that `--dump-enum-files` only
writes the three built-in enums (`vic_ii_colors`, `vic_colors`, `petscii_shifted`) to a directory
and exits, making it a TOML-shape discovery tool. Keep the requirement's substance — `memmap.json`
holds the bit tables, regenerator2000 holds the enum mechanism, neither side can produce this
alone — and keep its ID and checkbox state untouched.

**D-15/D-19 narrowing (`README.md`).** Rewrite the one-project-limit paragraph: the hardcoded
port belongs to regenerator2000's `--mcp-server` HTTP route; this project drives
`--mcp-server-stdio` (D-16), which binds no port and spawns one short-lived child per call
(D-17), so two projects in one network namespace are not in conflict through this project's
route. Keep the limit documented for anyone running r2000's HTTP MCP server directly, and keep
the "stated, not detected" posture (`R2000-04` is permanently folded). Do not claim the upstream
limit was fixed — it was sidestepped.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && node --test stock-symbols.test.ts 2>/dev/null || node --test 'stock-symbols*.test.*'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "STATED ASSUMPTION" .claude/mcp/vice/stock-symbols.ts` returns 0
    - `stock-symbols.ts` contains the literal `al C:0810 .init_screen` and the literal `0.9.20`, and its verified claim mentions the fixture rather than "all inputs"
    - `grep -c "MAX_SYMBOLS\|MAX_LABEL_FILE_LINES\|MAX_LABEL_FILE_BYTES" .claude/mcp/vice/stock-symbols.ts` is unchanged from before the edit (record both counts)
    - `grep -n "dump-enum-files" .planning/REQUIREMENTS.md` shows the corrected sentence, and `R2000-13`'s line contains `r2000_create_project_enum`
    - `README.md`'s one-project paragraph contains both `--mcp-server-stdio` and `--mcp-server`, and states that this project's route binds no port
    - `cd .claude/mcp/vice && npm run test:automated` green (the header edit must not break the module's tests)
  </acceptance_criteria>
  <done>Three prose claims that were wrong, unverified or wider than the evidence are now exactly as strong as the measurements behind them.</done>
</task>

<task type="auto">
  <name>Task 3: CLAUDE.md's drifted line citations, a drift guard for them, and the two folded todos closed</name>
  <files>CLAUDE.md, .claude/mcp/vice/docs-linerefs.test.ts, .planning/todos/completed/2026-08-20-npm-closure-walk-blind-to-dynamic-imports.md, .planning/todos/completed/2026-08-20-r2000-review-residual-findings.md</files>
  <read_first>
    - `CLAUDE.md`'s Architecture bullet citing `vice-proxy.ts:2889` and `vice-proxy.ts:1368` — including its own instruction to treat a mismatch as drift to re-verify
    - `.claude/mcp/vice/vice-proxy.ts` — confirm by grep, at execution time, the current line of each `rewriteArguments(` CALL (not the declaration at ~1900) and of the two enclosing function declarations `function forwardToVice(` and `function gatherWedgeEvidence(`
    - `.claude/mcp/vice/hostpath-consumers.test.ts` — the in-repo precedent for a test that reads source text and asserts a structural property, including how it locates repo-root files
    - `.planning/todos/pending/2026-08-20-npm-closure-walk-blind-to-dynamic-imports.md` and `.planning/todos/pending/2026-08-20-r2000-review-residual-findings.md` — the two folded todos; the second lists WR-02..WR-07
  </read_first>
  <action>
Update CLAUDE.md's bullet to the re-verified numbers: the `forwardToVice()` call site is
`vice-proxy.ts:2943` (function starts at `:2878`) and the `gatherWedgeEvidence()` call site is
`vice-proxy.ts:1422` (function starts at `:1398`). Verify both by grep before writing them — if
the file has moved again, write what you measure, not what this plan says. Add one clause: the
`r2000_*` family (Phase 11) is registered through `buildViceTool()` and never reaches
`forwardToVice()`, so neither call site is reachable from it and the constraint is satisfied by
construction for that family, not by an interception.

Create `.claude/mcp/vice/docs-linerefs.test.ts`: read `CLAUDE.md` from the repo root and
`vice-proxy.ts`, extract every `vice-proxy.ts:<N>` citation from the `rewriteArguments` bullet,
and assert that each cited line of `vice-proxy.ts` actually contains `rewriteArguments(` or the
enclosing `function` declaration named alongside it. Assert at least two citations were extracted
(non-vacuity: a bullet reworded so the regex matches nothing must FAIL, not pass silently). Header
comment: this test exists because the same two numbers drifted between Phase 10 and Phase 11 and
were re-verified by hand twice; a citation the repo can check is cheaper than a convention that
asks each phase to re-check it. Keep the module out of `package.json`'s `files[]`.

Close the two folded todos: `git mv` each from `.planning/todos/pending/` to
`.planning/todos/completed/` and append to each a resolution section naming the plan that closed
it — folded todo 1 by `11-02` (Task 3, dynamic-import closure walk), folded todo 2's WR-02 by
`11-01` (Task 1), WR-04 by `11-01` (Task 2), WR-03/05/06/07 by `11-02` (Tasks 1-3). If any WR
item is still open at execution time, do NOT move that todo — leave it pending with the closed
items struck through and say so in the SUMMARY.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && node --test docs-linerefs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --test docs-linerefs.test.ts` exits 0
    - Rewording CLAUDE.md's bullet to remove one citation makes the test FAIL (planted-violation transcript in the SUMMARY, change reverted)
    - `grep -c "2889\|1368" CLAUDE.md` returns 0
    - `CLAUDE.md` cites `vice-proxy.ts:2943` and `vice-proxy.ts:1422`, and each cited line of `vice-proxy.ts` contains `rewriteArguments(`
    - `CLAUDE.md`'s bullet states the `r2000_*` family satisfies the constraint by construction via `buildViceTool()`
    - Both folded-todo files are under `.planning/todos/completed/` with a resolution section naming the closing plan
    - `docs-linerefs.test.ts` is absent from `.claude/mcp/vice/package.json`'s `files[]`
  </acceptance_criteria>
  <done>The citation drift this phase inherited is corrected once and mechanically prevented from recurring, and both folded todos are closed with attribution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed documentation → a future session's decisions | A dangling forward reference or an over-wide verified claim is a false statement acted on later; this is the same class as a vacuous test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-DOC-OVERCLAIM | Spoofing | `stock-symbols.ts` header's `--export_lbl` claim | mitigate | Task 2: the verified claim is scoped to 0.9.20 and the named fixture; the skip-unrecognised-lines behaviour is retained with its reason |
| T-11-DOC-DANGLE | Repudiation | ROADMAP/CONTEXT `.vsf` pointers | mitigate | Task 1: all three sites corrected, backlog item carries the three real reasons and what would reverse the decision |
| T-11-DOC-DRIFT | Repudiation | CLAUDE.md's `vice-proxy.ts` line citations | mitigate | Task 3: numbers re-measured at execution time and pinned by `docs-linerefs.test.ts`, observed failing when a citation is removed |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package is installed by this plan |

No new code path, network surface or external input is introduced by this plan, so the ASVS L1
input-validation, session and authentication chapters have no applicable control here — stated
explicitly rather than padded.
</threat_model>

<verification>
- `cd .claude/mcp/vice && npm run test:automated` green (the standing per-phase BACK-02 gate) and `npm run typecheck` clean.
- `node scripts/check-npm-packages.mjs` green (the README and CLAUDE.md edits must not disturb it).
- The `docs-linerefs.test.ts` planted-violation transcript recorded in the SUMMARY.
- The SUMMARY explicitly confirms `ROADMAP.md`, `REQUIREMENTS.md`, the `10-CONTEXT.md` edit and
  both todo moves are present in the commit — this plan ran with `worktree: false` precisely
  because worktree mode would have stripped them.
</verification>

<success_criteria>
- D-34, D-35, D-22's wording correction and the D-15/D-19 narrowing are all landed.
- CLAUDE.md's citations match the source and a test enforces that.
- Both folded todos are in `.planning/todos/completed/` with attribution.
</success_criteria>

<verification_map>
| Task | Requirement | Threat Ref | Test Type | Automated Command |
|------|-------------|------------|-----------|-------------------|
| 11-03-T1 | R2000-13 (D-34 scope correction) | T-11-DOC-DANGLE | doc assertion (grep gate) | `! grep -rn "Phase 11" .planning/ROADMAP.md \| grep vsf` |
| 11-03-T2 | R2000-14 (D-35), R2000-13 (D-22 wording) | T-11-DOC-OVERCLAIM | doc assertion + existing suite | `cd .claude/mcp/vice && npm test` |
| 11-03-T3 | R2000-10 (folded todos), R2000-14 | T-11-DOC-DRIFT | unit + planted violation | `node --test docs-linerefs.test.ts` |
</verification_map>

<output>
Create `.planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/11-03-SUMMARY.md` when done.
</output>
