---
phase: 11-annotation-store-enums-and-the-symbol-round-trip
plan: 09
type: execute
wave: 5
depends_on: ["11-07"]
files_modified:
  - .planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/evidence/criterion1/SESSION-B-ANSWER.md
  - .claude/mcp/vice/r2000-answer-key.test.ts
autonomous: true
requirements: [R2000-10, R2000-11]
must_haves:
  truths:
    - "D-26 session B: a separate execution context answers the committed question using ONLY `r2000_*` queries against the committed store — criterion 1's actual claim, sampled"
    - "The answer is recorded together with the exact tool calls that produced it, so the derivation is inspectable rather than asserted"
    - "The result is checked mechanically against the sealed key, and a mismatch is reported as a criterion failure rather than repaired"
    - "No file from session A other than the store and the question was read, and the SUMMARY lists every file that was read"
  artifacts:
    - path: ".planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/evidence/criterion1/SESSION-B-ANSWER.md"
      provides: "The canonical answer line, the exact r2000_* calls that derived it, and the list of files read"
      contains: "canonical answer"
  key_links:
    - from: "evidence/criterion1/SESSION-B-ANSWER.md"
      to: "evidence/criterion1/ANSWER.sha256"
      via: "sha256 of the canonical answer line, compared by r2000-answer-key.test.ts"
      pattern: "ANSWER\\.sha256"
---

<objective>
Session B of criterion 1's two-session evidence: answer a question you have not seen the answer to,
from a store you did not write, using only the query surface.

**This plan is deliberately starved of context.** You are the "later session" criterion 1 is about.
Your permitted inputs are exactly two files:

- `.planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/evidence/criterion1/QUESTION.md`
- `.planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/evidence/criterion1/recon-subject.regen2000proj`
  (read only through `r2000_*` tool calls, never as text)

**You must not read**, and the SUMMARY must state that you did not:
`evidence/criterion1/ANSWER.md`, `evidence/criterion1/ANSWER.sha256` (before the comparison step),
`evidence/criterion1/SESSION-A-TRANSCRIPT.md`, `evidence/criterion1/fixture/recon-subject.a`,
`evidence/criterion1/fixture/recon-subject.prg`, `11-07-PLAN.md`, `11-07-SUMMARY.md`, or plan
11-07's commit messages and diffs. Do not `cat` the `.regen2000proj` either — reading the store as
raw JSON is re-deriving from a file, not querying, and it defeats the point.

If the question turns out to be unanswerable from the store, that is a **finding about the store**,
not a licence to widen your inputs. Record what was missing and stop.

Purpose: plan 11-05's tests prove the query API returns correct rows. Only this plan samples
criterion 1's real claim — that a later session, blind to the first session's prose, gets the answer
out of the store.

Output: `SESSION-B-ANSWER.md` and a mechanical pass/fail against the sealed key.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/evidence/criterion1/QUESTION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: answer the question from the store, recording every call</name>
  <files>.planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/evidence/criterion1/SESSION-B-ANSWER.md</files>
  <read_first>
    - `.planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/evidence/criterion1/QUESTION.md` — the question, the permitted inputs, and the canonical answer grammar with its worked example
    - `.claude/mcp/vice/r2000-tools.ts` — the curated tool names and their argument shapes, so you can form valid calls (this is API documentation, not session-A prose)
    - `.claude/mcp/vice/r2000-cli.ts` `USAGE` — how to reach the surface if you drive it from the shell rather than as MCP tools
  </read_first>
  <action>
Answer `QUESTION.md` using only `r2000_*` queries against
`evidence/criterion1/recon-subject.regen2000proj`. Expect to need `r2000_get_symbols`,
`r2000_get_comments`, `r2000_get_blocks` and `r2000_get_cross_references`; use
`r2000_search_disassembly` with an EXPLICIT `max_results` if you search (the child defaults it to
50, which truncates silently).

Write `SESSION-B-ANSWER.md` containing, in this order:
1. **The canonical answer line**, byte-exact per `QUESTION.md`'s grammar, inside a fenced block
   marked so a test can extract it unambiguously (use the same marker convention `ANSWER.md`
   documents, which `QUESTION.md` names).
2. **The derivation**: for each field of the answer, the exact tool call (name plus arguments) and
   the exact response fragment the value came from. A field with no call behind it is not an answer.
3. **The files read**: an explicit list of every file you opened, including this plan and
   `QUESTION.md`. This list is the evidence that the session boundary held.
4. **Anything the store could not answer**, if applicable, with the call that came back empty.

Do not guess a field to complete the line. An incomplete answer with the missing field named is
better evidence than a complete one with a guess in it — and a guessed field that happens to hash
correctly would destroy the value of the whole exercise.
  </action>
  <verify>
    <automated>test -s .planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/evidence/criterion1/SESSION-B-ANSWER.md && grep -q "canonical answer" .planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/evidence/criterion1/SESSION-B-ANSWER.md</automated>
  </verify>
  <acceptance_criteria>
    - `SESSION-B-ANSWER.md` exists and contains a fenced canonical answer line matching `QUESTION.md`'s grammar (validate it against the grammar's regex and say so)
    - Every answer field has a named tool call and a quoted response fragment behind it
    - The files-read list is present and contains none of the forbidden files
    - No `cat`/`Read` of the `.regen2000proj` as text appears anywhere in the work
    - No nested `claude -p` invocation was used
  </acceptance_criteria>
  <done>The question is answered from the store, and the derivation is inspectable call by call.</done>
</task>

<task type="auto">
  <name>Task 2: compare against the sealed key and record the verdict honestly</name>
  <files>.claude/mcp/vice/r2000-answer-key.test.ts, .planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/evidence/criterion1/SESSION-B-ANSWER.md</files>
  <read_first>
    - `.claude/mcp/vice/r2000-answer-key.test.ts` (from plan 11-07) — how it extracts the canonical line and recomputes the seal; extend that file rather than adding a second seal test
    - `.planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/evidence/criterion1/QUESTION.md` — the exact canonicalisation rules (case, separators, trailing newline) the hash is over
  </read_first>
  <action>
Extend `r2000-answer-key.test.ts` with a test that extracts the canonical line from
`SESSION-B-ANSWER.md`, applies `QUESTION.md`'s canonicalisation rules, computes its sha256 and
asserts it equals `ANSWER.sha256`. Keep plan 11-07's existing assertions intact. Add a
non-vacuity control: the test must fail if `SESSION-B-ANSWER.md` is missing or its fenced canonical
block is empty, rather than skipping.

Then record the verdict:
- **If the hashes match**, criterion 1's two-session evidence is complete. Append a short verdict
  section to `SESSION-B-ANSWER.md` stating the match, the two hashes, and the number of `r2000_*`
  calls used.
- **If they do not match**, do NOT edit `ANSWER.md`, `ANSWER.sha256` or `QUESTION.md`, and do not
  reformulate your answer to fit. Append a verdict section recording the mismatch, your canonical
  line, both hashes, and your assessment of which of the three possible causes applies: (a) the
  answer is wrong, so the store does not support the claim; (b) the answer is right but the
  canonicalisation differs, so `QUESTION.md`'s grammar is underspecified; (c) session A's key is
  wrong. Then report the criterion as NOT demonstrated in the SUMMARY and stop. A mismatch is a
  real result; retro-fitting the question or the key would be exactly the "make verification green
  by weakening the verifier" failure ENGINEERING_RULES.md §5 forbids.

Only after the verdict is written may you read `ANSWER.md` — and only if the hashes mismatched and
you are diagnosing cause (b) or (c). If you do, say so in the SUMMARY, because it changes what the
evidence proves.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && node --test r2000-answer-key.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --test r2000-answer-key.test.ts` exits 0, and the run includes both plan 11-07's seal assertions and the new session-B comparison
    - Deleting `SESSION-B-ANSWER.md`'s fenced block makes the test FAIL, not skip (transcript in the SUMMARY, change reverted)
    - `SESSION-B-ANSWER.md` ends with a verdict section containing both hashes
    - On a mismatch: `ANSWER.md`, `ANSWER.sha256` and `QUESTION.md` are unmodified (`git diff --exit-code` on those three paths is clean) and the SUMMARY reports the criterion as NOT demonstrated with the cause assessment
    - The SUMMARY states which files were read and whether `ANSWER.md` was read
  </acceptance_criteria>
  <done>The two-session claim is settled by a hash comparison, in whichever direction the evidence actually points.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session A's artifacts → this session's context | The evidential value of the whole criterion rests on this boundary; the mitigation is a sealed key plus a recorded files-read list, not an instruction alone |
| a failing comparison → the temptation to adjust the question or key | The classic "make it green by weakening the verifier" failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-CONTEXT-BLEED | Information Disclosure | this plan's context and reading | mitigate | Objective names the two permitted inputs and the eight forbidden ones; Task 1 requires a files-read list; the plan's own `<context>` block loads only `QUESTION.md` |
| T-11-RETROFIT | Tampering | `ANSWER.md` / `ANSWER.sha256` / `QUESTION.md` | mitigate | Task 2: a mismatch must be reported, not repaired; a `git diff --exit-code` criterion on those three paths enforces it |
| T-11-VACUOUS-CHECK | Repudiation | the comparison test | mitigate | Task 2: a missing or empty answer block must FAIL rather than skip, observed failing |
| T-11-NESTED-SESSION | Denial of Service | a nested headless `claude -p` | mitigate | Structural: this plan IS the second session; nested invocation is prohibited and asserted |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installed (phase-wide audit: no new package) |
</threat_model>

<verification>
- `cd .claude/mcp/vice && npm run test:automated` green (the standing per-phase BACK-02 gate) and `npm run typecheck` clean.
- `git diff --exit-code` clean for `ANSWER.md`, `ANSWER.sha256` and `QUESTION.md`.
- The SUMMARY lists every file read and states the verdict and both hashes.
</verification>

<success_criteria>
- `SESSION-B-ANSWER.md` records a canonical answer with a per-field derivation and a files-read list.
- The seal comparison runs in CI and its result is recorded either way.
- No session-A prose was read, and the SUMMARY says so.
</success_criteria>

<verification_map>
| Task | Requirement | Threat Ref | Test Type | Automated Command |
|------|-------------|------------|-----------|-------------------|
| 11-09-T1 | R2000-10, R2000-11 (criterion 1, session B) | T-11-CONTEXT-BLEED | human-witnessed transcript (recorded artifact) | `grep -q "canonical answer" …/SESSION-B-ANSWER.md` |
| 11-09-T2 | R2000-10 | T-11-RETROFIT, T-11-VACUOUS-CHECK | unit (seal comparison) | `node --test r2000-answer-key.test.ts` |
</verification_map>

<output>
Create `.planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/11-09-SUMMARY.md` when done.
</output>
