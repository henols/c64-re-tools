---
phase: 11-annotation-store-enums-and-the-symbol-round-trip
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/mcp/vice/r2000-d64.ts
  - .claude/mcp/vice/r2000-d64.test.ts
  - .claude/mcp/vice/r2000-cli.ts
  - .claude/mcp/vice/r2000-cli.test.ts
  - scripts/check-skill-fork-honesty.mjs
  - scripts/check-npm-packages.mjs
autonomous: true
requirements: [R2000-10]
must_haves:
  truths:
    - "WR-05: a truncated `.d64` throws a named error naming the sector, the needed byte range and the actual image length — it never silently short-reads a partial payload into a synthesized project"
    - "WR-06: a `$00`-padded directory name is printed exactly as it can be typed back, so `--entry <name>` from the printed listing always resolves"
    - "WR-07: a flat `.raw`/`.bin` capture that is not exactly 65536 bytes is refused by name, never reinterpreted as a `.prg` with its first two bytes eaten as a load address"
    - "WR-03: the `disasm` deletion-pin exemption applies only to the check it was written for, and is bounded to exactly one occurrence"
    - "Folded todo 1: a module reachable from `vice-proxy.ts` only through `await import(\"./x.ts\")` can no longer fall out of `files[]` unnoticed"
  artifacts:
    - path: ".claude/mcp/vice/r2000-d64.ts"
      provides: "Buffer-bounded sector reads inside the walk, and padding-consistent name decoding"
      contains: "sectorSlice"
    - path: ".claude/mcp/vice/r2000-cli.ts"
      provides: "Extension-dispatched flat-capture branch so flatImageOrigin()'s size check is reachable"
      contains: "flatImageOrigin"
    - path: "scripts/check-npm-packages.mjs"
      provides: "A closure walk that follows dynamic imports"
      contains: "import\\s*\\("
  key_links:
    - from: "scripts/check-npm-packages.mjs"
      to: ".claude/mcp/vice/r2000-cli.ts"
      via: "the transitive closure walk now traversing vice-proxy.ts's `await import(\"./r2000-cli.ts\")`"
      pattern: "r2000-cli"
---

<objective>
Close the four residual Phase 10 review findings that this phase's own deliverables sit on top
of, and make the packaging closure walk see the dynamic-import seam this phase widens.

Classification against `<hard_requirements>`'s prerequisite-vs-cleanup question:

- **WR-03 is a prerequisite.** Plan 11-09 edits skill prose, and `check-skill-fork-honesty.mjs`
  is one of the two CI gates over that prose. Its `evidence: "disasm"` exemption currently
  short-circuits all three checks on any line carrying that literal and is unbounded, so the
  gate protecting this phase's skill edits can be sidestepped by a single comment.
- **Folded todo 1 is a prerequisite.** Plans 11-04..11-08 add six modules reachable from
  `vice-proxy.ts` only through `await import(…)`. The closure walk is static-import-only, so a
  missing `files[]` entry ships a tarball that throws `ERR_MODULE_NOT_FOUND` on both npm routes.
- **WR-05/06/07 are independent cleanup, waved here because they own `r2000-cli.ts`.** Nothing
  in this phase's curated MCP surface walks a `.d64` (D-18 excludes it), so 10-REVIEW.md's
  "Phase 11 plans an MCP surface over exactly these functions" does not hold. They are landed in
  wave 1 anyway because plans 11-06/11-07/11-08 each add a verb to `r2000-cli.ts`, and fixing
  WR-07 first avoids editing that file around three unfixed silent-guess paths.

Purpose: every one of these is a silent-wrong-answer defect in a path this phase extends.

Output: bounded `.d64` reads, a refusing flat-capture branch, a scoped-and-counted honesty
exemption, and a closure walk that follows `await import("./x.ts")`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/11-CONTEXT.md
@.planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/11-PATTERNS.md
@.planning/ENGINEERING_RULES.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-05 + WR-06 — bound every `.d64` sector read, and decode names the way the walker defines padding</name>
  <files>.claude/mcp/vice/r2000-d64.ts, .claude/mcp/vice/r2000-d64.test.ts</files>
  <read_first>
    - `.claude/mcp/vice/r2000-d64.ts` in full — `isInImage()` at 79-83, the two `image.subarray(off, off + 256)` reads at ~225 and ~241, the `Math.max(2, …)` clamp at ~237, `petsciiName()` at 93-97, `isEmptySlot()` at 148-151, and `assertPlainImage()` at 262-268
    - `.claude/mcp/vice/r2000-d64.test.ts` — the existing fixtures, all of which pad names with `Buffer.alloc(16, 0xa0)`, which is why WR-06 is untested
    - `.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-REVIEW.md` lines 310-386 — WR-05's and WR-06's demonstrated silent failures, with the exact reproductions
    - `.claude/mcp/vice/r2000-cli.ts` lines ~150-215 — the one current caller of `assertPlainImage()`, so the change does not double-throw
  </read_first>
  <action>
Add an internal `sectorSlice(image, track, sector, what)` helper that computes
`tsToOffset(track, sector)` and throws
`` `${what}: sector T/S needs bytes A..B but the image is only N bytes -- truncated or non-plain image` ``
(real values interpolated) when `off + 256 > image.length`, otherwise returns the 256-byte
subarray. Use it at BOTH read sites (the directory walk and the file-chain walk), so the bound
check is part of the walk rather than the separate opt-in `assertPlainImage()` — keep
`assertPlainImage()` exported and unchanged for its existing caller.

Replace the `Math.max(2, usedByte)` clamp with a throw naming the sector and the observed
`usedByte` when `usedByte < 2`: a zero-length payload currently surfaces downstream as the
confusing `parsePrg: input is 0 byte(s)`.

Fix `petsciiName()` to strip trailing `0x00` as well as `0xa0`, matching `isEmptySlot()`'s own
definition of padding, and say in a comment that the two functions must agree because a name the
listing prints must be a name `--entry` accepts.

Add fixtures to `r2000-d64.test.ts`: (a) a valid image truncated 100 bytes into the file's own
sector, asserting `extractEntry()` THROWS and the message contains the image's real byte length
(the reviewer measured a silent 98-byte read where 254 were claimed); (b) a directory entry whose
name is `$00`-padded, asserting `listEntries()` returns the bare name and `extractEntry(name)`
resolves with that same string; (c) a final sector whose `usedByte` is 0, asserting a throw
naming the sector.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && node --test r2000-d64.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --test r2000-d64.test.ts` exits 0
    - `grep -c 'function sectorSlice' r2000-d64.ts` returns 1 and `grep -c 'image.subarray(off, off + 256)' r2000-d64.ts` returns 0
    - A truncated image causes `extractEntry()` to throw a message containing both the sector as `T/S` and the literal image length
    - `petsciiName(Buffer.from([...bytes, 0x00, 0x00]))` returns the name with no embedded NUL, and the returned string round-trips through `extractEntry()`
    - `grep -c 'Math.max(2' r2000-d64.ts` returns 0
  </acceptance_criteria>
  <done>No `.d64` input produces a partial payload, an unselectable name, or a zero-length extraction without a named error.</done>
</task>

<task type="auto">
  <name>Task 2: WR-07 — dispatch flat captures by extension so the size check is reachable</name>
  <files>.claude/mcp/vice/r2000-cli.ts, .claude/mcp/vice/r2000-cli.test.ts</files>
  <read_first>
    - `.claude/mcp/vice/r2000-cli.ts` lines ~150-215 — the input-dispatch chain, in particular the `bytes.length === 65536` branch selection at ~200-210 and the `USAGE` text at ~59-70 that advertises `.raw`/`.bin`
    - `.claude/mcp/vice/r2000-project.ts` — `flatImageOrigin()` and the named error it throws for a non-65536 length, which is currently unreachable
    - `.planning/phases/10-adoption-boundaries-automated-bootstrap-and-the-removal/10-REVIEW.md` lines 386-421 — WR-07's live reproduction (a 4096-byte `capture.raw` bootstrapped at origin `$62c5`, exit 0)
  </read_first>
  <action>
Add an explicit `ext === ".raw" || ext === ".bin"` branch BEFORE the
`bytes.length === 65536` branch. In it, call `flatImageOrigin(bytes)` inside a try/catch and on
throw print `bootstrap: <message>` to stderr and return exit code 1, so the size check that
exists precisely to name the actual length actually runs. Keep the existing
`bytes.length === 65536` branch for extension-less flat captures. Extend the header's
what-NOT-to-do block with the WR-07 incident: dispatching a flat capture by content length means
a truncated capture is reinterpreted as a `.prg` and every downstream address is wrong with no
diagnostic — the exact "silently guess" behaviour D-02 forbids.

Add tests: a 4096-byte `capture.raw` returns a non-zero exit code and stderr containing both the
actual length (`4096`) and `65536`; a genuine 65536-byte `capture.raw` still bootstraps
successfully; a `.prg` whose length happens to be 4096 still bootstraps as a `.prg`.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && node --test r2000-cli.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --test r2000-cli.test.ts` exits 0
    - A 4096-byte `.raw` input yields a non-zero exit code and stderr containing `4096` and `65536`
    - A 65536-byte `.raw` input still writes a `.regen2000proj`
    - `r2000-cli.ts` contains a `".raw"` / `".bin"` extension branch that calls `flatImageOrigin`
    - The header names WR-07
  </acceptance_criteria>
  <done>No flat capture of the wrong size is silently reinterpreted.</done>
</task>

<task type="auto">
  <name>Task 3: WR-03's scoped exemption and folded todo 1's dynamic-import closure walk</name>
  <files>scripts/check-skill-fork-honesty.mjs, scripts/check-npm-packages.mjs</files>
  <read_first>
    - `scripts/check-skill-fork-honesty.mjs` lines 330-400 — `DISASM_LINE_EXEMPTION` at 350, the `continue` at 358, and the three checks (`toacme`, `cmdDisasm`, `\bdisasm\b`) it currently short-circuits
    - `scripts/check-npm-packages.mjs` lines 85-145 — `REQUIRED_DERIVED_MODULES`, the `need()` loop at ~103, and the transitive-closure walk with its static-only regex `/^\s*import\s[^;]*?from\s+"(\.\/[^"]+)"/gm` at ~127
    - `.claude/mcp/vice/vice-proxy.ts` line 218 — `const { runR2000Cli } = await import("./r2000-cli.ts");`, the one dynamic import today
    - `.claude/mcp/vice/package.json` `files[]`
    - `.planning/todos/pending/2026-08-20-npm-closure-walk-blind-to-dynamic-imports.md` — the todo's own two fix options
  </read_first>
  <action>
**WR-03.** Move the exemption inside the `\bdisasm\b` check only — evaluate the `toacme` and
`cmdDisasm` checks before it, so a line reading
`// see acme.mjs cmdDisasm / toacme, evidence: "disasm"` now fails. Count exemption uses in an
`exemptionHits` counter and add a final `need(exemptionHits === 1, …)` whose message says a
second occurrence means the exemption is being used to hide a reintroduction. Update the
surrounding header comment: the current claim that scoping "to the LINE, not the file" keeps a
reintroduction elsewhere in that file caught is true per-line and silent about the same line,
which is where the hole was.

**Folded todo 1.** Widen the closure walk to also traverse dynamic imports by matching
`` /import\s*\(\s*"(\.\/[^"]+)"\s*\)/g `` in addition to the existing static pattern (keep both;
do not replace the static one). Both matches feed the same `stack`/`listed` check, so a
dynamically-imported module missing from `files[]` now produces the same
`is imported by … but is not in the published tarball` failure. Add `["r2000-cli.ts", "R2000-09"]`
to `REQUIRED_DERIVED_MODULES` so the one dynamic entry point known today is also pinned by name
rather than only by traversal. Extend the walk's header comment with folded todo 1's reason:
`vice-proxy.ts:218` reaches the whole r2000 family through `await import()`, so before this fix
none of the five existing r2000 modules were traversed at all and `files[]` was correct only by
hand.

Demonstrate both fixes non-vacuously (ENGINEERING_RULES.md §6) and record the transcripts in the
SUMMARY without committing the planted states: (a) add the combined
`cmdDisasm` + `evidence: "disasm"` line to a scratch copy of a skill file and confirm
`node scripts/check-skill-fork-honesty.mjs` fails; (b) temporarily remove `r2000-cli.ts` from
`.claude/mcp/vice/package.json`'s `files[]` and confirm `node scripts/check-npm-packages.mjs`
fails naming it.
  </action>
  <verify>
    <automated>node scripts/check-skill-fork-honesty.mjs && node scripts/check-npm-packages.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-skill-fork-honesty.mjs` exits 0 and its output reports a non-zero number of files scanned
    - `node scripts/check-npm-packages.mjs` exits 0 and its closure line reports MORE modules than before this task (the r2000 family is now traversed) — record both counts in the SUMMARY
    - `check-skill-fork-honesty.mjs` contains `exemptionHits` and a `need(exemptionHits === 1` assertion
    - `check-npm-packages.mjs` contains a dynamic-import regex matching `import\s*\(` and `REQUIRED_DERIVED_MODULES` includes `r2000-cli.ts`
    - With `r2000-cli.ts` removed from `files[]`, `check-npm-packages.mjs` fails with a message naming `r2000-cli.ts` (transcript in the SUMMARY, change reverted)
  </acceptance_criteria>
  <done>Both CI gates this phase leans on have been observed failing under a planted violation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a user-supplied `.d64`/`.raw` file → `r2000-d64.ts` / `r2000-cli.ts` | Untrusted, possibly truncated or malformed binary drives offsets and a synthesized project's origin |
| skill Markdown/`.mjs` prose → `check-skill-fork-honesty.mjs` | Untrusted text that is matched, never executed; a bypass lets a false capability claim ship |
| `package.json` `files[]` → the published tarball | A missing entry ships a package that throws at runtime on both npm install routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-D64-TRUNC | Tampering | `r2000-d64.ts` `listEntries()`/`extractEntry()` | mitigate | Task 1: `sectorSlice()` bounds every read against `image.length` and throws naming the sector and the real length; truncated-image fixture pins it |
| T-11-D64-NAME | Spoofing | `r2000-d64.ts` `petsciiName()` | mitigate | Task 1: strip `0x00` as padding so a printed name equals a selectable name; `$00`-padded fixture pins it |
| T-11-RAW-GUESS | Tampering | `r2000-cli.ts` flat-capture dispatch | mitigate | Task 2: extension-dispatched branch makes `flatImageOrigin()`'s named refusal reachable; 4096-byte `.raw` fixture pins it |
| T-11-HONESTY-BYPASS | Repudiation | `check-skill-fork-honesty.mjs` exemption | mitigate | Task 3: exemption scoped to the `\bdisasm\b` check and bounded to exactly one hit; planted combined line observed failing |
| T-11-PKG-CLOSURE | Denial of Service | `check-npm-packages.mjs` closure walk | mitigate | Task 3: dynamic-import traversal plus a named `REQUIRED_DERIVED_MODULES` entry; observed failing with `r2000-cli.ts` removed from `files[]` |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package is installed by this plan (RESEARCH.md Package Legitimacy Audit: no new package for the phase) |
</threat_model>

<verification>
- `cd .claude/mcp/vice && npm run test:automated` green (the standing per-phase BACK-02 gate) and `npm run typecheck` clean.
- `node scripts/check-npm-packages.mjs` green, with a closure-module count strictly greater than
  the pre-task count (recorded).
- `node scripts/check-skill-fork-honesty.mjs` and `node scripts/check-skill-tool-coverage.mjs`
  both green (the second is untouched here but must not regress).
- Both planted-violation transcripts recorded in the SUMMARY, neither committed.
</verification>

<success_criteria>
- WR-03, WR-05, WR-06 and WR-07 are each fixed and pinned by a fixture or assertion that fails
  without the fix.
- The closure walk traverses `await import("./x.ts")`.
- Folded todo 1 is closed; the todo file's disposition is handled by plan 11-03.
</success_criteria>

<verification_map>
| Task | Requirement | Threat Ref | Test Type | Automated Command |
|------|-------------|------------|-----------|-------------------|
| 11-02-T1 | R2000-10 (folded todo 2, WR-05/06) | T-11-D64-TRUNC, T-11-D64-NAME | unit, synthetic-image fixtures | `node --test r2000-d64.test.ts` |
| 11-02-T2 | R2000-10 (folded todo 2, WR-07) | T-11-RAW-GUESS | unit, real-file fixtures | `node --test r2000-cli.test.ts` |
| 11-02-T3 | R2000-10 (folded todo 1, WR-03) | T-11-HONESTY-BYPASS, T-11-PKG-CLOSURE | packaging / CI script + planted violation | `node scripts/check-npm-packages.mjs` |
</verification_map>

<output>
Create `.planning/phases/11-annotation-store-enums-and-the-symbol-round-trip/11-02-SUMMARY.md` when done.
</output>
