---
phase: 11-annotation-store-enums-and-the-symbol-round-trip
plan: 12
subsystem: docs
tags: [regenerator2000, r2000, skills, ci-gates, memory-map, confidence-grading, installer]

# Dependency graph
requires:
  - phase: 11 (plan 11-05)
    provides: "r2000-tools.ts's 17 curated r2000_* tool definitions and CURATED_R2000_TOOLS"
  - phase: 11 (plan 11-10)
    provides: "r2000-confidence.ts's D-25 grade parser and r2000-memmap-render.ts's ProvenanceHeader/render-memmap CLI verb"
  - phase: 11 (plan 11-06)
    provides: "gen-enums / r2000-regbits-gen.ts, the register-enum generator memmap.json feeds"
provides:
  - "c64-program-recon's SKILL.md rewritten to write findings through r2000_* tools and generate the memory map instead of hand-authoring it"
  - "memory-map.template.md reshaped from empty fill-in rows into the render-memmap pointer, the provenance sidecar schema with a fully-filled example, and the unchanged confidence vocabulary"
  - "c64-memory-mapping and c64-ram-capture SKILL.md sections naming what now consumes their output (the enum generator; the memory map's provenance sidecar)"
  - "check-skill-tool-coverage.mjs's r2000_* extraction now has a real non-vacuity floor (>=10) plus a render-memmap presence assertion"
  - "Confirmed correction to the phase's 'mandatory second site' framing: installer/skills/ is gitignored and generated by prepack, not a second file to hand-edit"
affects: [12-final-verification-and-milestone-close]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Skill prose documents a generator + its input schema rather than inviting hand-authoring of a generated artifact (D-24)"
    - "A CI coverage gate's non-vacuity floor is derived from an exact, comment-documented count rather than a round guess"

key-files:
  created: []
  modified:
    - .claude/skills/c64-program-recon/SKILL.md
    - .claude/skills/c64-program-recon/templates/memory-map.template.md
    - .claude/skills/c64-memory-mapping/SKILL.md
    - .claude/skills/c64-ram-capture/SKILL.md
    - scripts/check-skill-tool-coverage.mjs

key-decisions:
  - "The phase's 'mandatory second site' framing is wrong: /installer/skills/ is gitignored (.gitignore:43) and regenerated by installer's own prepack script; there is no second file to hand-edit, only a generated copy to verify (npm run sync-skills + check-npm-packages.mjs)"
  - "The r2000_* non-vacuity floor is pinned at exactly 10, the count plan 11-12 itself introduced, with the ten names listed in-comment so a future phase raising it has a documented baseline"
  - "r2000_get_address_details is never named literally in skill prose (even to explain its exclusion) because check-skill-tool-coverage.mjs's extraction would otherwise flag it as an uncurated reference; the exclusion is described without the tool's own name"

patterns-established:
  - "A CI floor assertion's comment states its own provenance (which plan, which exact names) so a future maintainer knows whether to raise it or investigate a regression"

requirements-completed: [R2000-10, R2000-11, R2000-13]

# Metrics
duration: ~30min
completed: 2026-08-21
---

# Phase 11 Plan 12: Store-first playbooks and a real r2000_* coverage floor Summary

**c64-program-recon now writes findings through r2000_* tools and generates the memory map via `render-memmap`, with the template reshaped into a provenance-sidecar schema and check-skill-tool-coverage.mjs given a real non-vacuity floor over the r2000_* references it checks.**

## Performance

- **Duration:** ~30 min
- **Tasks:** 3
- **Files modified:** 5

## Accomplishments

- `c64-program-recon/SKILL.md` gained a "Writing findings into the annotation store" section: bootstrap the store, write with `r2000_set_label_name`/`r2000_set_comment`/`r2000_set_data_type`/`r2000_add_scope`/`r2000_batch_execute`, grade with the five D-25 bracket tokens (typo fails loudly, quoted verbatim from `r2000-confidence.ts`), query with `r2000_get_symbols`/`r2000_get_comments`/`r2000_get_blocks`/`r2000_get_cross_references`/`r2000_search_disassembly` (`max_results` required, no silent 50-row truncation), then generate the map with `render-memmap [--check]`.
- `memory-map.template.md` is no longer a fill-in-the-rows document: it now documents the generator command (both invocation forms), the full `ProvenanceHeader` sidecar schema with a source for every key and one fully-filled, placeholder-free example, and the unchanged five-phrase confidence vocabulary with its do-not-promote rule verbatim.
- `c64-memory-mapping/SKILL.md` names `r2000-regbits-gen.ts`/`r2000-regbits.json`, the digest-pinning to `memmap.json`, and the honest 29-of-959 gap (six sprite-plane registers supplied by the generator's override table instead).
- `c64-ram-capture/SKILL.md` names `captureSha256` as the field it feeds into the memory map's provenance sidecar, and states plainly that this skill does not emit the sidecar itself.
- `check-skill-tool-coverage.mjs` gained a real non-vacuity floor (`extractedR2000.size >= 10`, the exact count this plan introduced) and a `render-memmap`-presence assertion, replacing the "no floor asserted yet" comment left by plan 11-05.

## Task Commits

1. **Task 1: c64-program-recon becomes store-first, and the template stops inviting hand-authoring** - `921bceb` (docs)
2. **Task 2: c64-memory-mapping and c64-ram-capture say what they now feed** - `0d42c10` (docs)
3. **Task 3: the r2000 reference floor, and proof the installer's generated copy picks the edits up** - `88515cb` (test)

**Plan metadata:** committed as part of this SUMMARY commit (see below).

## Files Created/Modified

- `.claude/skills/c64-program-recon/SKILL.md` - Adds the store-first annotation section; updates the References table row for the template.
- `.claude/skills/c64-program-recon/templates/memory-map.template.md` - Fully reshaped: generator pointer, provenance sidecar schema + filled example, confidence vocabulary.
- `.claude/skills/c64-memory-mapping/SKILL.md` - Adds a "Feeding the enum generator" section naming the generator, the digest pin, and the 29-of-959 gap.
- `.claude/skills/c64-ram-capture/SKILL.md` - Adds a "Feeding the memory map's provenance sidecar" section.
- `scripts/check-skill-tool-coverage.mjs` - Adds the r2000_* non-vacuity floor and the render-memmap presence assertion; updates the stale "plan 11-09" comment.

## Decisions Made

- Corrected the phase's "mandatory second site" framing (per the plan's own objective note): `/installer/skills/` is gitignored and regenerated by `installer`'s `prepack` script (`node scripts/sync-skills.mjs`), so there is no second file to hand-edit for this plan's skill changes — only a generated copy to verify. Verified all four ways the plan asked for (see "Installer generated-copy verification" below).
- Pinned the r2000_* coverage floor at exactly 10 (not a round number) — the true count this plan's own edits introduced, with the ten names enumerated in the assertion's comment so a future addition knows whether to raise the floor or investigate a regression.
- Never wrote the literal string `r2000_get_address_details` in any skill file, even while describing why it is excluded (D-32) — `check-skill-tool-coverage.mjs`'s r2000_* extraction regex matches any `r2000_*` token found in skill prose and would otherwise flag it as an uncurated reference (it is deliberately absent from `CURATED_R2000_TOOLS`). Described the exclusion without naming the tool.

## Deviations from Plan

None — plan executed exactly as written. All acceptance criteria across the three tasks were verified directly (see below); no Rule 1-4 auto-fixes were needed.

## Issues Encountered

None.

## Verification Evidence

**Task 1 acceptance criteria** (all verified):
- `node scripts/check-skill-tool-coverage.mjs && node scripts/check-skill-fork-honesty.mjs` — both exit 0.
- Every `r2000_*` name in `SKILL.md`/`memory-map.template.md` is in `CURATED_R2000_TOOLS` (10 distinct names extracted, all curated); `r2000_get_address_details` appears nowhere in either file (grepped, zero matches).
- `SKILL.md` names all five D-25 tokens exactly as `r2000-confidence.ts` spells them (`[confirmed-code]`, `[probable-code]`, `[confirmed-data]`, `[probable-data]`, `[unknown]`) and states a typo "fails loudly".
- `SKILL.md`'s `r2000_search_disassembly` guidance names `max_results` as required and states the 50-row default.
- `memory-map.template.md` contains `render-memmap` (2 occurrences), has no empty table row, and its example sidecar JSON block contains zero `<...>`-delimited placeholders.
- `memory-map.template.md` still contains all five confidence phrases and the do-not-promote sentence verbatim.

**Task 2 acceptance criteria** (all verified):
- Both prose gates exit 0.
- `c64-memory-mapping/SKILL.md` names `r2000-regbits-gen.ts`, `r2000-regbits.json`, the "29 of ... 959" figure, and all six override-supplied registers (`$D015`, `$D017`, `$D01A`, `$D01B`-`$D01D`).
- `c64-ram-capture/SKILL.md` names `captureSha256`, states the sidecar is hand-authored and validated by the renderer, and states plainly this skill does not emit it.
- Neither file overclaims — `check-skill-fork-honesty.mjs` passed with no new findings.

**Task 3 acceptance criteria** (all verified):
- All three scripts (`check-skill-tool-coverage.mjs`, `check-skill-fork-honesty.mjs`, `check-npm-packages.mjs`) exit 0.
- `check-skill-tool-coverage.mjs` contains the `extractedR2000.size >= 10` floor with a provenance comment, and a `render-memmap`-presence assertion.
- **Planted-violation transcript** (both changes reverted immediately after, confirmed by a clean `git diff`):
  - Removing the sole `r2000_add_scope` reference from `SKILL.md` dropped the extracted count from 10 to 9 and the gate failed: `non-vacuity: expected at least 10 distinct r2000_* names extracted from .claude/skills/, got 9 -- the extraction regex or plan 11-12's skill edits may have regressed`.
  - Corrupting one of two `r2000_set_comment` mentions in `memory-map.template.md` (to `r2000_set_KOMMENT`) did **not** trip the floor (the mangled token still matched the extraction regex up to the underscore boundary, `r2000_set_`, keeping the count at 10 while adding a spurious uncurated name) — instead it failed the *existing* curated-set check: `r2000_set_: referenced by ... but NOT in CURATED_R2000_TOOLS`. This confirms the floor and the curated-set check catch two genuinely different regressions, not the same one twice.
- **Installer generated-copy verification** (all four results):
  1. `npm run sync-skills` (invoked transitively via `check-npm-packages.mjs`'s own `prepack` script) copied the edited files into `installer/skills/`; grepping `installer/skills/c64-program-recon/SKILL.md` and its template copy for `render-memmap` confirmed the new text is present in both.
  2. `git status --porcelain installer/skills` produced no output, and `git check-ignore -v installer/skills/c64-program-recon/SKILL.md` confirmed the match is `.gitignore:43: /installer/skills/` — genuinely ignored, not merely untracked.
  3. `check-npm-packages.mjs` reported 6 skills with a `SKILL.md`, unchanged.
  4. This confirms the correction to the "mandatory second site" framing: no second file needs a matching edit; only the generated copy needed verifying, which it was.

**Standing phase-wide gates** (run after all three tasks, from a fresh `npm ci` in `.claude/mcp/vice` since this worktree started with no `node_modules`):
- `npx tsc --noEmit` — clean, no errors.
- `node test-gate.mjs` (the `test:automated` BACK-02 gate) — 1928 pass / 1 fail / 5 todo / 1934 total. The one failure (`repo-root.test.ts`'s "path agreement (D-3, D-6...)" assertion) is the documented pre-existing worktree-only artifact named in this plan's own prior-wave context — it fails because this worktree's checkout path lives under `.claude/worktrees/...`, and passes on the main tree. Not caused by this plan's changes (none of them touch `repo-root.ts` or its test).
- `node --test r2000-confidence.test.ts r2000-memmap-render.test.ts r2000-regbits.test.ts r2000-tools.test.ts` — 61/61 pass, including the live-gated criterion-2 test against a real `regenerator2000` child.
- `evidence/criterion1/**` — confirmed byte-unmodified (`git diff --stat` against this plan's own commit range shows zero changes under that path).

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

This was the last plan of Phase 11 (wave 7, depends on 11-05/11-06/11-10, all merged). All three of this plan's requirements (`R2000-10`, `R2000-11`, `R2000-13`) are satisfied by store-first playbooks and a generated memory map. No blockers identified for phase close; the orchestrator owns the final STATE.md/ROADMAP.md wave-completion update per this plan's worktree instructions.

## Self-Check: PASSED

- All 5 modified/created files confirmed present on disk (FOUND).
- All 4 commits (`921bceb`, `0d42c10`, `88515cb`, and this SUMMARY's own `b0ec97e`) confirmed present in `git log`.

---
*Phase: 11-annotation-store-enums-and-the-symbol-round-trip*
*Completed: 2026-08-21*
