---
phase: 08-capability-honesty-and-the-install-story
plan: 06
type: execute
wave: 3
depends_on: [08-01, 08-03]
files_modified:
  - scripts/check-skill-tool-coverage.mjs
  - docs/stock-vice-parity.md
autonomous: true
requirements: [DIST-01, SKILL-01]

must_haves:
  truths:
    - "The capability data exists in exactly one place: check-skill-tool-coverage.mjs consumes the registry rather than holding its own copy."
    - "The three fork-only tools that skills actually reference are pinned by set equality, so a fourth bare reference or a missing one fails a script."
    - "No document in the repo still says a capability is deferred to a phase that has already closed without it, or claims a parity harness is coming."
    - "A reader of the parity narrative is pointed at the generated support table for the mechanical per-tool answer."
  artifacts:
    - path: "scripts/check-skill-tool-coverage.mjs"
      provides: "FORK_ONLY_UNRECOVERABLE derived from CAPABILITY_REGISTRY instead of a second literal copy"
      contains: "capability-registry.ts"
    - path: "docs/stock-vice-parity.md"
      provides: "Four corrected claims plus a pointer to the generated support table"
      contains: "tool-support.md"
  key_links:
    - from: "scripts/check-skill-tool-coverage.mjs"
      to: ".claude/mcp/vice/capability-registry.ts"
      via: "import replacing the literal FORK_ONLY_UNRECOVERABLE array"
      pattern: "CAPABILITY_REGISTRY"
    - from: "docs/stock-vice-parity.md"
      to: "docs/tool-support.md"
      via: "a prose pointer replacing the deleted parity-harness claim"
      pattern: "tool-support\\.md"
---

<objective>
Finish the honesty work the support table cannot do by itself: **one copy of the capability data**, and
**no false forward-looking claims left in the narrative**.

`scripts/check-skill-tool-coverage.mjs` still holds a second, literal copy of three registry entries
in its `FORK_ONLY_UNRECOVERABLE` array. Its own header comment (lines 9-12) says this phase is
expected to reuse that extraction rather than re-derive it, and `CLAUDE.md` names "re-deriving a
cross-cutting seam locally" as a documented anti-pattern. **D-E** forbids the phase ending with two
copies.

`docs/stock-vice-parity.md` carries four claims that are now provably false -- three because later
scope decisions narrowed or cut the phase they point at, one because it promises `SKILL-01` will cover
something `SKILL-01`'s requirement text does not mention. Research Pitfall 5 is exactly this failure
class: prose written forward-looking and never revisited.

Purpose: after this plan, the capability data has one home and the narrative says only true things.
Output: a consolidated lint and a corrected parity document.
</objective>

<source_audit>
| Source | Item | Covered by |
|--------|------|------------|
| GOAL | Criterion 4: derived rather than maintained by hand -- which is only true if no second hand-maintained copy survives | Task 1 |
| GOAL | Criterion 2's supporting mechanism: the three skill-referenced unrecoverable tools are pinned mechanically | Task 1 |
| REQ | DIST-01 (the derived inventory has one data source) | Task 1 |
| REQ | SKILL-01 (the parity doc's claim about what SKILL-01 covers must match SKILL-01's actual text) | Task 2 |
| RESEARCH | "Don't Hand-Roll": reason text comes from `docs/stock-vice-parity.md` and the existing array, never re-derived | Task 1 |
| RESEARCH | Pitfall 5 and State of the Art "Deprecated/outdated": stale `deferred to Phase 7` prose | Task 2 |
| RESEARCH | Open Question 1, resolved in favour of the direct import (verified this session under Node 22.22, no flag) | Task 1 |
| PATTERNS | `scripts/check-skill-tool-coverage.mjs` is its own analog -- the array becomes a projection, the surrounding assertions stay | Task 1 |
| CONTEXT | D-E: one source of truth; a second hand-maintained copy is forbidden | Task 1 |
| CONTEXT | D-C: `VERIF-03`'s parity harness is out of scope, so the doc must stop promising one | Task 2 |
| CLAUDE.md | Anti-pattern: re-deriving a cross-cutting seam locally | Task 1 |
| CLAUDE.md | Drift note: treat a line-number mismatch as drift to re-verify, not as evidence the claim changed | Task 2 |

**Deliberately NOT in this plan:**
- Building anything the corrected prose describes as absent. `vice_joystick_tap` stays unbuilt on
  stock, `vice_disk_detach` stays cut, and no parity harness is written (**D-C**).
- Answer-shape drift between the two backends. Task 2 records it as an open concern with no mechanical
  check, which is the truth; it is not `SKILL-01`'s scope and nothing in this phase covers it.
- Any change to `scripts/check-skill-fork-honesty.mjs` (plans 08-04 and 08-05 own that file) or to
  `scripts/generate-tool-support-table.mjs` and `docs/tool-support.md` (plan 08-03 owns those).
</source_audit>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/08-capability-honesty-and-the-install-story/08-RESEARCH.md
@.planning/phases/08-capability-honesty-and-the-install-story/08-PATTERNS.md
@.planning/phases/08-capability-honesty-and-the-install-story/08-01-SUMMARY.md
@.planning/phases/08-capability-honesty-and-the-install-story/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate check-skill-tool-coverage.mjs onto the registry (D-E)</name>
  <read_first>
    - `scripts/check-skill-tool-coverage.mjs` in full (about 348 lines). In particular: the header
      comment lines 1-24 (the reuse mandate for this phase, and the never-import-skill-content rule);
      the `FORK_ONLY_UNRECOVERABLE` literal array (expected around lines 171-184 -- **re-verify, this
      has already drifted once from the research doc's cited 141-159**); the assertion block that
      requires each reason to name both `BACK-05` and `SKILL-01` (expected around lines 256-264); the
      `PENDING_LATER_PHASE` drift guard; the liveness assertion that every allowlisted name is
      referenced by some skill file (expected around lines 275-283); and `allowlistedNames`, which
      feeds the core coverage count.
    - `.claude/mcp/vice/capability-registry.ts` -- `CAPABILITY_REGISTRY`, and the note in plan
      08-01's SUMMARY that registry reasons deliberately carry **no** planning identifiers.
  </read_first>
  <files>scripts/check-skill-tool-coverage.mjs</files>
  <action>
Replace the literal `FORK_ONLY_UNRECOVERABLE` array with a projection of the registry: import
`CAPABILITY_REGISTRY` from `../.claude/mcp/vice/capability-registry.ts` and derive the same
`[name, reason]` pair shape by filtering to entries whose `category` is the hardware category and
whose `providedBy` is `fork`. Keep the constant's name so every downstream assertion and the final
category-count report keep working unchanged.

One assertion must change, and it is the reason this task is not a mechanical find-and-replace: the
existing check requires every `FORK_ONLY_UNRECOVERABLE` reason to contain both `BACK-05` and
`SKILL-01`. Registry reasons are user-facing refusal text and deliberately carry no planning
identifiers (plan 08-01 asserts their absence). Replace that assertion with checks that hold against
the registry: the reason is non-empty and at least 40 characters; the entry's `category` is the
hardware category; and `providedBy` is `fork`. Update the surrounding comment to say the routing
annotation moved out of the reason text when the registry became the source of truth, and that
`capability-registry.ts` is now where a reason is edited.

The liveness assertion needs adjusting too. Today it requires every `FORK_ONLY_UNRECOVERABLE` entry
to be referenced by some skill file, but the registry's hardware set has 6 members while only 3 are
referenced by any skill (`vice_sid_get_state`, `vice_keyboard_matrix`, `vice_keyboard_restore` --
confirmed by grep this session; `vice_keyboard_chord`, `vice_keyboard_key_press` and
`vice_keyboard_key_release` appear in no skill). Replace it with a **set-equality** assertion: the
subset of hardware fork-only names actually referenced by a skill file must equal exactly those
three names. That is strictly stronger than the old check -- it pins ROADMAP Phase 5 criterion 5's
corrected three-tool exception list mechanically, and it fails in both directions (a new bare
reference appearing, or one of the three disappearing).

Leave every other assertion untouched, in particular the `!stockNames.has(name)` shrink-by-failing
drift guard, which must keep failing the day a later phase implements one of these on stock.

Update the header comment: the script now imports one first-party TypeScript module from
`.claude/mcp/vice/`, which does not weaken its standing rule -- it still never imports, requires,
evaluates or spawns anything from `.claude/skills/`; skill content remains matched, never executed.
State that explicitly so the rule is not read as broken.
  </action>
  <verify>
    <automated>node scripts/check-skill-tool-coverage.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-skill-tool-coverage.mjs` exits 0 and its final category-count line reports 6 fork-only-unrecoverable entries (up from 3, because the registry's hardware set is complete).
    - `grep -c 'capability-registry.ts' scripts/check-skill-tool-coverage.mjs` is at least 1.
    - `grep -c 'SID \$D400' scripts/check-skill-tool-coverage.mjs` is 0 -- the literal seed reason strings are gone from this file, so no second copy of the capability data survives anywhere.
    - `grep -rn 'KEYBOARD_FEED (0x72) injects PETSCII buffer text only' scripts/ .claude/mcp/vice/*.ts` returns hits in `capability-registry.ts` only.
    - `grep -c 'BACK-05' scripts/check-skill-tool-coverage.mjs` may be non-zero only in comments: `grep -vE '^\s*(//|\*)' scripts/check-skill-tool-coverage.mjs | grep -c 'BACK-05'` is 0.
    - Transient proof: adding a fourth bare skill reference (e.g. writing `vice_keyboard_chord` into a scratch copy of a skill file) makes the new set-equality assertion fail naming that tool; removing one of the three makes it fail the other way. Revert, and record both messages in the SUMMARY.
    - `grep -rn 'never import()s, require()s, eval()s or spawns anything from .claude/skills' scripts/check-skill-tool-coverage.mjs` still matches -- the untrusted-content rule is restated, not deleted.
  </acceptance_criteria>
  <done>The coverage script derives its unrecoverable set from the registry, its reason assertion no longer demands planning identifiers, and the three skill-referenced hardware tools are pinned by set equality.</done>
</task>

<task type="auto">
  <name>Task 2: Purge the four stale forward-looking claims from docs/stock-vice-parity.md</name>
  <read_first>
    - `docs/stock-vice-parity.md` -- the four regions named below, plus enough surrounding text to
      keep each correction consistent with its paragraph. **Re-verify every line number; treat a
      mismatch as drift to re-verify, not as evidence the claim changed** (CLAUDE.md's own note).
    - `.planning/ROADMAP.md` -- the "Cut from scope (v0.2.0, 2026-08-17)" table (the authoritative list
      of what was cut and why) and the Phase 7 section's Notes (which record that Phase 7 was narrowed
      to exactly two tools plus `vice_recycle`).
    - `.planning/phases/07-cycle-timing-and-wedge-triage/07-VERIFICATION.md` -- what Phase 7 actually
      shipped, so a correction does not overstate or understate it.
    - `.planning/REQUIREMENTS.md` line 110 -- `SKILL-01`'s actual text, which is about naming the stock
      route or the fork requirement, and says nothing about answer-shape drift.
  </read_first>
  <files>docs/stock-vice-parity.md</files>
  <action>
Correct four claims that are now factually wrong. Each is the same failure class this phase exists to
prevent -- prose written forward-looking and never revisited after a later scope decision. Do not
rewrite the document; change only what is untrue, and keep each correction to the same paragraph so
the surrounding narrative and its evidence/confidence framing survive.

1. **`vice_joystick_tap` "deferred to Phase 7"** (expected around lines 195-203). Phase 7 closed
   having built exactly `vice_cycles_stopwatch`, `vice_run_until` and a stock `vice_recycle` evidence
   gatherer; it never built this. Restate it as simply not built on stock, with the same underlying
   reason the paragraph already gives (a tap needs the machine to run for a measured interval, which
   collides with the no-unrequested-resume policy), and keep the accurate part that
   `vice_joystick_set` covers the hold/release/centre workflow. Name no future phase.

2. **`vice_disk_detach` "ships in Phase 7 through the text monitor"** (expected around lines
   204-207). It was **cut from scope on 2026-08-17** -- no skill calls it, stock has no detach opcode,
   and re-attaching a different image covers the workflow. Restate it as cut, citing the ROADMAP's
   "Cut from scope (v0.2.0, 2026-08-17)" section the way the neighbouring `vice_disk_read_sector` and
   `vice_machine_config_*` entries already do. Name no future phase.

3. **"Phase 8's parity harness must expect these on stock only"** (expected around line 261, in the
   two-stock-only-tools bullet). There is no parity harness: `VERIF-03` is cut (**D-C**), because
   byte-identical parity is an explicit non-goal in `PROJECT.md`. Replace the clause with a pointer to
   the generated `docs/tool-support.md`, which is where those two tools' stock-only status is now
   recorded mechanically.

4. **"`SKILL-01` (Phase 8) must cover answer-shape drift, not only capability gaps"** (expected
   around line 168). `SKILL-01`'s actual requirement text is about naming the stock route or stating
   the fork requirement at the point of use; answer-shape drift is not in its scope and is not built
   in this phase. Restate the sentence to say what is true: the skills' playbooks name the fork
   requirement at each fork-only call site (plan 08-04's work), and answer-shape drift between the two
   backends remains **documented here only, with no mechanical check** -- so it is an open concern
   rather than something a landed requirement covers. Do not promise a future phase.

While in this file, also check the bullet around lines 244-247 that says the ROADMAP's criterion text
is "not amended by this correction -- that is a developer decision, flagged here for Phase 8
planning." That flag is now resolved: `ROADMAP.md`'s Phase 5 criterion 5 already carries a dated
parenthetical naming all three unrecoverable tools. Update the sentence to record that the decision
was taken and where, rather than leaving an open flag pointing at a phase that is now executing.
  </action>
  <verify>
    <automated>node -e 'const s=require("node:fs").readFileSync("docs/stock-vice-parity.md","utf8"); const bad=[/deferred to Phase 7/,/ships in Phase 7/,/parity harness/,/must cover answer-shape drift/,/flagged here for Phase 8 planning/]; const hits=bad.filter(r=>r.test(s)).map(String); if(hits.length){console.error("stale claims remain:",hits);process.exit(1)} if(!/tool-support\.md/.test(s)){console.error("no pointer to the generated table");process.exit(1)} console.log("parity doc clean");'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'deferred to Phase 7' docs/stock-vice-parity.md` is 0.
    - `grep -c 'ships in Phase 7' docs/stock-vice-parity.md` is 0.
    - `grep -c 'parity harness' docs/stock-vice-parity.md` is 0.
    - `grep -c 'must cover answer-shape drift' docs/stock-vice-parity.md` is 0.
    - `grep -c 'flagged here for Phase 8 planning' docs/stock-vice-parity.md` is 0.
    - `grep -c 'tool-support.md' docs/stock-vice-parity.md` is at least 1 -- the stock-only bullet now points at the generated table.
    - `grep -c 'Cut from scope (v0.2.0, 2026-08-17)' docs/stock-vice-parity.md` increased by at least 1 versus `git show HEAD:docs/stock-vice-parity.md`, i.e. the `vice_disk_detach` correction cites the same authority its neighbours do.
    - The document's section headings are unchanged: `diff <(git show HEAD:docs/stock-vice-parity.md | grep '^#') <(grep '^#' docs/stock-vice-parity.md)` produces no output.
    - `node scripts/check-skill-tool-coverage.mjs` still exits 0.
  </acceptance_criteria>
  <done>Four provably false forward-looking claims are gone, the resolved developer-decision flag is recorded as resolved, the stock-only bullet points at the generated table, and no heading moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.claude/skills/**` file content -> `check-skill-tool-coverage.mjs` | Skill files are untrusted input: walked, read and regex-matched, never executed |
| `.claude/mcp/vice/capability-registry.ts` -> a repo-root lint script | A new cross-boundary import: the script now loads first-party TypeScript from the packaged tree |
| `docs/stock-vice-parity.md` -> a reader deciding what a backend can do | The narrative is consulted when the generated table's one-line note is not enough; a false claim there misleads exactly the reader who is looking hardest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | Elevation of Privilege | executing untrusted content while linting | mitigate | Task 1 preserves `check-skill-tool-coverage.mjs`'s standing rule verbatim and restates it in the header: nothing under `.claude/skills/` is ever imported, required, evaluated or spawned. The one new import is first-party, in-repo, and load-bearing for D-E; its acceptance greps for the restated rule so the rule cannot be silently dropped. |
| T-08-06-02 | Tampering | the consolidation quietly weakening an existing assertion | mitigate | Task 1 changes exactly one assertion (the reason-must-name-BACK-05-and-SKILL-01 check, which cannot hold against user-facing registry text) and replaces it with three stricter checks plus a set-equality liveness check. The `!stockNames.has(name)` shrink-by-failing drift guard is explicitly preserved, and the acceptance requires the final category-count line to report 6 rather than 3. |
| T-08-06-03 | Repudiation | a fourth bare skill reference appearing unnoticed | mitigate | The set-equality liveness assertion fails in both directions, proven by two transient edits in the acceptance criteria. |
| T-08-06-04 | Spoofing | corrected prose overstating what Phase 7 shipped | mitigate | Task 2's `read_first` requires reading `07-VERIFICATION.md` and the ROADMAP's cut table before rewording, and its acceptance pins the heading set unchanged so a correction cannot become a rewrite. |
| T-08-06-05 | Information Disclosure | the parity narrative exposing more than the public repo already does | accept | Every corrected sentence describes tools and scope decisions already public in `ROADMAP.md`, `REQUIREMENTS.md` and the two shipped manifests. No new information class is introduced. |
| T-08-06-SC | Tampering | npm/pip/cargo installs | mitigate | No package is installed in any ecosystem. No runtime dependency changes, and neither `package.json` nor `check-npm-packages.mjs` is touched -- that script is run as a regression check only. |
</threat_model>

<verification>
- `node scripts/check-skill-tool-coverage.mjs` exits 0 and reports 6 fork-only-unrecoverable entries.
- `node scripts/check-skill-fork-honesty.mjs` exits 0 (regression; plan 08-04 owns that script).
- `node scripts/check-npm-packages.mjs` exits 0 (regression only; not edited here).
- `node scripts/generate-tool-support-table.mjs` then `git diff --exit-code -- docs/tool-support.md` is clean (regression: the consolidation must not change the generated table).
- `cd .claude/mcp/vice && node --test capability-registry.test.ts tool-support-table.test.mjs` passes.
- `grep -rn 'deferred to Phase' docs/` returns nothing.

</verification>

<success_criteria>
- Exactly one copy of the capability data exists in the repo after this plan.
- The coverage script reports 6 hardware fork-only entries and pins the 3 skill-referenced ones by set equality.
- No document still defers a capability to a phase that closed without it, and none claims a parity harness exists.
- The parity narrative points at the generated table for the mechanical per-tool answer.
</success_criteria>

<output>
Create `.planning/phases/08-capability-honesty-and-the-install-story/08-06-SUMMARY.md` when done.
Record: the confirmed current line numbers of `FORK_ONLY_UNRECOVERABLE` and of the four corrected
parity-doc regions, with any drift noted; the two transient set-equality failure messages from Task 1;
the final `check-skill-tool-coverage.mjs` category-count line verbatim; and the before/after text of
each of the four corrected parity-doc claims, so a later reader can audit the correction rather than
trusting it.
</output>
