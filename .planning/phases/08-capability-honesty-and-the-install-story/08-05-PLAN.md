---
phase: 08-capability-honesty-and-the-install-story
plan: 05
type: execute
wave: 3
depends_on: [08-03, 08-04]
files_modified:
  - README.md
  - scripts/check-skill-fork-honesty.mjs
  - .github/workflows/ci.yml
  - .planning/phases/08-capability-honesty-and-the-install-story/08-HUMAN-UAT.md
autonomous: false
requirements: [DIST-02, DIST-03]

must_haves:
  truths:
    - "A new user reading only README.md learns which VICE to install, from which package manager, what version that gives them, and what that version costs them."
    - "The reader learns the backend is chosen by one config value, what each backend can and cannot do, and that SID read-back and the matrix keyboard require the fork."
    - "The reader is pointed at the generated per-backend support table for the full per-tool answer."
    - "Any smoke-test command the docs tell a user to paste has been run against a real stock VICE before publication."
    - "README.md contains no claim about this repository that is false."
    - "Every new check this phase added runs as a blocking CI step."
  artifacts:
    - path: "README.md"
      provides: "A VICE-install and backend-selection section, a per-ecosystem version table, and a link to docs/tool-support.md"
      contains: "VICE_BACKEND"
      min_lines: 160
    - path: "scripts/check-skill-fork-honesty.mjs"
      provides: "README presence assertions for VICE_BACKEND and the two named fork-only tools"
      contains: "README.md"
    - path: ".github/workflows/ci.yml"
      provides: "A blocking step running check-skill-fork-honesty.mjs beside the existing coverage step"
      contains: "check-skill-fork-honesty.mjs"
    - path: ".planning/phases/08-capability-honesty-and-the-install-story/08-HUMAN-UAT.md"
      provides: "The phase's one manual verification item -- a human install walkthrough on a clean box"
      contains: "apt install vice"
  key_links:
    - from: "README.md"
      to: "docs/tool-support.md"
      via: "a relative markdown link from the new backend section and an entry in the Layout tree"
      pattern: "docs/tool-support\\.md"
    - from: "scripts/check-skill-fork-honesty.mjs"
      to: "README.md"
      via: "literal-substring presence assertions"
      pattern: "VICE_BACKEND"
    - from: ".github/workflows/ci.yml"
      to: "scripts/check-skill-fork-honesty.mjs"
      via: "a run step in the build job, immediately after the existing skill-coverage step"
      pattern: "check-skill-fork-honesty"
---

<objective>
Close criterion 3: **a user installs the plugin and a working VICE from a package manager by following
the documentation**, and is told the backend choice and its consequences before hitting them.

Confirmed this session: none of the three README files in this repo mentions installing VICE itself,
the `VICE_BACKEND` config value, or the fork/stock tradeoff. This is new content, not an edit. The
root README's `### Dependencies` section covers only npm dependencies, and its opening bullet claims
the MCP server offers "screenshots" without qualification -- true on the fork, false on stock.

Two further honesty debts are paid here because they sit in the file this plan edits:
`README.md:123-126` asserts two "repo-wide documentation guardrail tests" (`skill-docs.test.ts`,
`vice-mcp-selector-docs.test.ts`) exist; repo-wide grep confirms neither file exists anywhere in this
repository. A documentation-honesty phase cannot ship leaving a false claim in the file it is editing.

Purpose: the install story a stranger can follow, stated once, mechanically pinned.
Output: a README section, three presence assertions, one CI step, one human-UAT record.
</objective>

<source_audit>
| Source | Item | Covered by |
|--------|------|------------|
| GOAL | Criterion 3: a user installs the plugin and a working VICE from a package manager by following the documentation, with the backend choice and its consequences stated | Tasks 1, 3 |
| GOAL | Criterion 4's readability half: the derived table must be reachable by a reader | Task 1 (link from README) |
| REQ | DIST-02 (what VICE you need, where to get it, what differs per version, including the fork-required SID/matrix exceptions) | Task 1 |
| REQ | DIST-03 (plugin plus stock VICE from a package manager is sufficient to drive the emulator) | Tasks 1, 3 |
| RESEARCH | Environment Availability -- seven ecosystems checked live, with the 3.10 gate marked per ecosystem | Task 1 |
| RESEARCH | Pitfall 4: `-default` must precede `-binarymonitor`, MEDIUM confidence, from project memory, not re-verified in research | Task 1 (verify live before publishing a paste-able command) |
| RESEARCH | Adjacent finding: `README.md:123-126`'s ghost guardrail-test claim | Task 1 |
| RESEARCH | Assumption A2 (Fedora via RPM Fusion Non-Free, single-source, MEDIUM) and A3 (Flatpak unresolved) | Task 1 (mark confidence, or omit) |
| PATTERNS | `README.md`'s existing `## Install` section as the structural analog for a sibling `##` section; the `## Layout` tree block as where the new doc gets a line | Task 1 |
| PATTERNS | `.github/workflows/ci.yml` lines 81-89 -- the two existing lint steps to sit beside | Task 2 |
| PATTERNS | `check-skill-fork-honesty.mjs`'s README presence-check snippet | Task 2 |
| CONTEXT | D-B: regenerator2000 is named nowhere in the install docs; v0.3.0 is proposed, not opened, and it is accepted that v0.3.0 will re-touch these docs | Task 1 |
| CONTEXT | D-H: the generated table lives in `docs/`, is linked from README, is not added to the npm tarballs; confirm `check-npm-packages.mjs` still passes unchanged | Task 1 |
| CONTEXT | D-I: Flatpak and Snap are out of scope and must not be claimed to work -- omit, or mark explicitly unverified. Do not assert | Task 1 |
| CLAUDE.md | Dependency: `CPUHISTORY_GET` needs VICE >= 3.10; Debian trixie/forky/sid and all current Ubuntu ship 3.9 | Task 1 |
| CLAUDE.md | Concurrency: stock's binary monitor serves exactly one client, and a second connect is indistinguishable from a wedge | Task 1 |
| CLAUDE.md | Capability: there is no runtime `WarpMode` resource -- warp control is launch-time only | Task 1 (only if a launch flag is documented) |
| VALIDATION | Wave 0 row 5: the README presence assertion, foldable into the fork-honesty lint | Task 2 |
| VALIDATION | The one genuinely manual item -- criterion 3's human install walkthrough | Task 3 |

**Deliberately NOT in this plan:**
- regenerator2000, in any form (**D-B**). The ROADMAP's suggestion to pull `R2000-16`'s probe forward
  was considered and declined; the rework cost of v0.3.0 re-touching this section is accepted.
- Flatpak and Snap as recommended install paths (**D-I**).
- `installer/README.md` and `.claude/mcp/vice/README.md`. The root `README.md` is the one entry point a
  new user reaches from GitHub and from both npm package pages; duplicating the install story into
  three files creates three copies to drift. If the executor judges a cross-link from either package
  README is needed, record it as a finding rather than making a silent edit.
- Writing the two guardrail test suites `README.md:123-126` falsely claims exist. The claim is deleted,
  not made true -- the scope fence says prefer deleting over building unrequested tests.
- Adding `docs/tool-support.md` to any `files[]` array (**D-H**).
</source_audit>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/08-capability-honesty-and-the-install-story/08-RESEARCH.md
@.planning/phases/08-capability-honesty-and-the-install-story/08-PATTERNS.md
@.planning/phases/08-capability-honesty-and-the-install-story/08-03-SUMMARY.md
@.planning/phases/08-capability-honesty-and-the-install-story/08-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the VICE-install and backend-selection section in README.md, and delete two false claims</name>
  <read_first>
    - `README.md` in full (about 130 lines). Specifically: the opening bullet list (lines 6-17), whose
      first bullet claims "screenshots" among the MCP server's tools without qualifying the backend;
      the `## Install` section (lines 19-60) with its `### A.` / `### B.` / `### Dependencies`
      substructure, which the new section sits beside as a sibling `##`; the `## Layout` tree block
      (lines 72-91), which needs one line for the new generated doc; and lines 123-126's ghost
      guardrail-test claim.
    - `docs/tool-support.md` -- the generated table produced by plan 08-03, so the README's link and its
      one-line description match what the reader will actually find, including its derived counts.
    - `.planning/phases/08-capability-honesty-and-the-install-story/08-RESEARCH.md` § Environment
      Availability -- the seven-ecosystem version table with per-ecosystem confidence, plus the two
      "Missing dependencies with fallback" paragraphs describing what a sub-3.10 VICE costs.
    - `CLAUDE.md` -- the Dependency constraint on `CPUHISTORY_GET` >= 3.10; the Concurrency constraint
      that stock's binary monitor serves exactly one client; the Capability constraint that warp is
      launch-time only.
    - `.claude/mcp/vice/backend-detect.mts` -- how `VICE_BACKEND` is read (the override branch returning
      immediately with `source: "override"`, spawning nothing) and what happens when it is unset (probe,
      then cache, then a documented fall back to `fork`). The README must describe the real default.
    - `.claude/mcp/vice/broker-launch.mts` -- `buildViceArgs()`, to confirm which flags this project's
      own launcher passes. Research verified it never emits `-default`; re-confirm, so the README does
      not contradict the shipped launcher.
    - `.mcp.json` and `installer/bin/cli.mjs` -- where a user actually sets an env var for this server,
      so the instruction for setting `VICE_BACKEND` names a real location rather than a plausible one.
  </read_first>
  <files>README.md</files>
  <action>
Add one new sibling `##` section to `README.md`, placed directly after `## Install` so a reader meets
it before `## How it locates the project`. It answers a stranger's four questions, in this order.

**(a) Which VICE, and where from.** State plainly that the emulator is not bundled and runs on the
host, and that two backends exist: **stock upstream VICE**, driven through its binary monitor and
installable from any package manager, and a **custom non-upstream fork** exposing an HTTP endpoint,
which must be built from source. Say which one a new user should pick (stock) and why.

**(b) A per-ecosystem version table.** Reproduce `08-RESEARCH.md` § Environment Availability as a
markdown table with columns: ecosystem, install command, version it currently ships, and whether that
clears the >= 3.10 gate. All figures were verified live on 2026-08-18 -- date-stamp the table, since
distro versions move. Rows and the honesty each needs: Debian 13 trixie and Debian forky ship 3.9
(below the gate); Ubuntu 25.10 multiverse ships 3.9; Arch `extra/vice` ships 3.10 (clears it); Fedora
is **not in the base repositories at all** and needs RPM Fusion Non-Free, where 3.10 is available --
mark this row single-source rather than asserting it flatly (research Assumption A2); Alpine has 3.10
in `edge`/`testing` only, in no stable release branch; Homebrew ships 3.10 on macOS and Linux (clears
it); and the official Windows download page still offers only 3.9 zips even though 3.10 was released
-- state that explicitly, because "official equals latest" is the assumption that breaks there. Note
that no MSYS2 package exists, so Windows users have the SourceForge zips or a source build.

**Flatpak and Snap (D-I):** either omit both entirely, or include them marked **unverified** with one
sentence saying this project has not confirmed whether their sandboxing permits reaching the binary
monitor on `127.0.0.1`. Do not assert either way. Prefer omission plus a single caveat sentence naming
them, since a table row reads as an endorsement.

**(c) What a sub-3.10 VICE costs.** One short paragraph: the `CPUHISTORY_GET` opcode arrived in VICE
3.10, so on 3.9 the exact cycle stopwatch degrades to an honest within-one-frame approximation. Say
this is already-shipped graceful degradation, not a failure -- everything else works. Do not cite
requirement or phase identifiers in user-facing prose.

**(d) Choosing the backend, and its consequences.** State that the backend is selected by the single
config value `VICE_BACKEND`, set to `stock` or `fork` in the MCP server's environment, and name the
concrete place a user sets it (the `env` block of the `vice` server entry in `.mcp.json`). Describe
what happens when it is unset, matching `backend-detect.mts`'s actual behaviour rather than guessing.
Then state the consequences honestly:
- the two backends deliberately advertise different tool lists, and a tool advertised on both keeps
  the same name and argument shape;
- calling a tool the active backend does not advertise returns an error naming the tool, the reason,
  and which backend provides it -- describe the behaviour, do not cite a requirement ID;
- **`vice_sid_get_state` and `vice_keyboard_matrix` require the fork** and are unrecoverable on stock
  (SID `$D400`-`$D418` is write-only in hardware and the binary monitor has no SID command; the raw
  keyboard matrix is not readable, and stock's `KEYBOARD_FEED` injects KERNAL-buffer text only). Both
  literal tool names must appear -- Task 2 asserts their presence;
- a link to `docs/tool-support.md` for the full per-tool answer, described as generated from the
  shipped manifests rather than maintained by hand.

**(e) A verify-your-install command, only if it has been run.** Research Pitfall 4 says `-default`
must precede `-binarymonitor` or the monitor never binds -- MEDIUM confidence, from project memory,
not independently reproduced. This project's own launcher never emits `-default`. So: **run the
command live against a real stock VICE before publishing it.** A genuine unpatched stock binary is at
`/usr/bin/x64sc` (3.9) and another at `/usr/local/bin/x64sc` (3.10); the fork shadows `x64sc` on
`PATH`, so invoke by absolute path. Publish the exact command you ran and observed to bind, and
nothing else. If the command includes `-default` at all, `-default` must come first. Also warn that
stock's binary monitor serves **exactly one client**: a second connection sits unserviced with no
reply and no EOF, indistinguishable from a hang -- so a user must not leave a hand-run monitor session
open while the plugin is also driving the emulator. If live verification is impossible in the
execution environment, do **not** publish a command; say instead that the plugin's own launcher
handles the flags, and record the omission as a finding.

**Two false claims to delete or correct:**
1. Lines 123-126's assertion that `skill-docs.test.ts` and `vice-mcp-selector-docs.test.ts` exist "in
   the originating project". Neither exists anywhere in this repository, and every other in-tree
   reference to those filenames is itself a comment documenting them as a ghost reference from a
   predecessor project. **Delete the claim.** Confirm with a repo-wide grep first and record the grep
   result in the SUMMARY. Do not write the two test suites -- deleting a false sentence is in scope;
   building unrequested tests is not.
2. The opening bullet's unqualified "screenshots". Client-side screenshots were cut from v0.2.0 scope
   and `vice_display_screenshot` is fork-only. Qualify the bullet so it does not promise a stock user
   a capability the stock backend does not advertise -- either name the capability with its backend, or
   drop the word and let the linked support table carry the per-tool truth.

Finally, add one line for `docs/tool-support.md` to the `## Layout` tree block, with a description
matching its generated nature.

**D-B**: the string `regenerator2000` must not appear anywhere in `README.md`.
  </action>
  <verify>
    <automated>grep -q 'VICE_BACKEND' README.md; grep -q 'vice_sid_get_state' README.md; grep -q 'vice_keyboard_matrix' README.md; grep -q 'docs/tool-support.md' README.md; grep -q '3\.10' README.md; test 0 -eq "$(grep -c 'regenerator2000' README.md)"; test 0 -eq "$(grep -c 'skill-docs.test.ts' README.md)"; test 0 -eq "$(grep -c 'vice-mcp-selector-docs.test.ts' README.md)"; node scripts/check-npm-packages.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains all of the literal strings `VICE_BACKEND`, `vice_sid_get_state`, `vice_keyboard_matrix`, `docs/tool-support.md` and `3.10`.
    - `grep -c 'regenerator2000' README.md` is 0; `grep -c 'skill-docs.test.ts' README.md` is 0; `grep -c 'vice-mcp-selector-docs.test.ts' README.md` is 0.
    - `grep -c '^## ' README.md` is exactly one greater than `git show HEAD:README.md | grep -c '^## '` -- one new top-level section, no restructuring of the existing ones.
    - The version table has one row per ecosystem named in the action, each with a version string and an explicit clears/does-not-clear marker for the 3.10 gate; `grep -c '3\.9' README.md` is at least 3 (Debian, Ubuntu, official Windows).
    - The Fedora row carries an explicit single-source or unverified qualifier -- `grep -iE 'RPM Fusion' README.md` matches, and the same line or the sentence adjacent to it contains a confidence qualifier.
    - Flatpak/Snap: either `grep -ciE 'flatpak|snap' README.md` is 0, or every match sits on a line that also contains the word `unverified`.
    - `node scripts/check-npm-packages.mjs` exits 0 **with no change to that script or to either `package.json`** (D-H): `git diff --name-only` does not list `scripts/check-npm-packages.mjs`, `.claude/mcp/vice/package.json` or `installer/package.json`.
    - If a launch command is published: the exact command in the README was executed against an absolute-path stock binary and observed to bind its monitor port; the SUMMARY carries the command, the binary path, the version, and the observed evidence. If `-default` appears anywhere in the command, its index precedes `-binarymonitor`'s. If no command is published, the SUMMARY records why.
    - `grep -icE 'one client|single client' README.md` is at least 1 -- the single-client warning is present.
    - `git diff --name-only` lists only `README.md` for this task.
  </acceptance_criteria>
  <done>README carries a dated per-ecosystem VICE version table, the `VICE_BACKEND` switch with its honest consequences, the two named fork-only tools, a link to the generated table, a live-verified command or an explicit omission, and no false claim about this repository.</done>
</task>

<task type="auto">
  <name>Task 2: Add README presence assertions to the lint and wire it into CI</name>
  <read_first>
    - `scripts/check-skill-fork-honesty.mjs` -- the script plan 08-04 created: its `ROOT` derivation,
      its `need()`/`errors` accumulator, and its final report-and-exit block, so the new assertions
      join the same accumulator rather than exiting early on the first failure.
    - `README.md` -- the section written in Task 1, so each asserted substring is one that actually
      exists and is load-bearing rather than incidental.
    - `.github/workflows/ci.yml` -- the `build` job (starts around line 26) and the two existing lint
      steps: `Validate npm package contents` running `check-npm-packages.mjs` (around lines 81-82) and
      `Validate skill tool coverage against the stock manifest` running `check-skill-tool-coverage.mjs`
      (around lines 84-89, with its explanatory blocking comment). The new step goes immediately after
      the coverage step and before `Build installable package`. Note both use a repo-root invocation
      with no `working-directory`. **Re-verify these line numbers.**
  </read_first>
  <files>scripts/check-skill-fork-honesty.mjs, .github/workflows/ci.yml</files>
  <action>
Add README presence assertions to `scripts/check-skill-fork-honesty.mjs`, using its existing `need()`
accumulator so a failure is reported alongside any skill-prose failure rather than short-circuiting.
Read `README.md` once with `readFileSync` and assert it contains each of these literal substrings,
with a failure message naming what a reader loses if it is missing:
- `VICE_BACKEND` -- without it a reader cannot select a backend at all;
- `vice_sid_get_state` and `vice_keyboard_matrix` -- the two tools the phase goal names explicitly as
  fork-required, which a stock user must be warned about before they design a method around them;
- `docs/tool-support.md` -- the reader's route to the full per-tool answer;
- `3.10` -- the version gate, without which the reader cannot tell what an `apt install` gives them.

Add the inverse assertions too, so the lint also catches a regression in the other direction: assert
`README.md` does **not** contain `regenerator2000` (**D-B**), `skill-docs.test.ts`, or
`vice-mcp-selector-docs.test.ts` (the two ghost filenames Task 1 deleted). Each failure message must
say why the string is forbidden.

Extend the script's header comment to record that it now covers two surfaces -- playbook prose and
README claims -- and why they share one script: both are documentation-honesty checks over
first-party prose, and one CI step is cheaper to keep green than two.

Then add the CI step to `.github/workflows/ci.yml`, in the `build` job, immediately after the existing
`check-skill-tool-coverage.mjs` step and before `Build installable package`. Match the existing steps
exactly in shape: a `- name:` line and a `run: node scripts/...` line, no `working-directory`. Give it
a step name naming what it protects (skill playbook and README backend-honesty) and precede it with a
short blocking-rationale comment in the same style as the coverage step's, saying what a user
experiences if it regresses: a playbook that names a tool the active backend does not advertise, or a
README that no longer tells them which VICE to install.

Do not reorder or modify either existing step, and do not add a second workflow file.
  </action>
  <verify>
    <automated>node scripts/check-skill-fork-honesty.mjs; grep -q 'check-skill-fork-honesty.mjs' .github/workflows/ci.yml; node -e "const s=require('node:fs').readFileSync('.github/workflows/ci.yml','utf8');const a=s.indexOf('check-skill-tool-coverage.mjs');const b=s.indexOf('check-skill-fork-honesty.mjs');const c=s.indexOf('scripts/package.sh');if(!(a>0&&b>a&&c>b)){console.error('step order wrong',{a,b,c});process.exit(1)}console.log('ci step order ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-skill-fork-honesty.mjs` exits 0.
    - `grep -c 'check-skill-fork-honesty.mjs' .github/workflows/ci.yml` is exactly 1.
    - In `.github/workflows/ci.yml`, the byte offset of `check-skill-tool-coverage.mjs` is less than that of `check-skill-fork-honesty.mjs`, which is less than that of `scripts/package.sh` -- the new step sits between the existing coverage step and the package build (asserted by the verify command).
    - `grep -c 'check-skill-tool-coverage.mjs' .github/workflows/ci.yml` is still exactly 1 and `grep -c 'check-npm-packages.mjs' .github/workflows/ci.yml` is still exactly 1 -- no existing step was duplicated or removed.
    - The workflow still parses: `node -e "require('node:fs').readFileSync('.github/workflows/ci.yml','utf8')"` succeeds and `git diff .github/workflows/ci.yml` shows only added lines (no deletions, no reindentation).
    - Transient proof: deleting the substring `VICE_BACKEND` from `README.md` makes `node scripts/check-skill-fork-honesty.mjs` exit non-zero naming that string; adding the literal `regenerator2000` to `README.md` makes it exit non-zero naming D-B's rule. Revert both, and record both messages in the SUMMARY.
    - `node scripts/check-skill-tool-coverage.mjs` exits 0 -- the sibling script is unaffected.
  </acceptance_criteria>
  <done>The lint asserts five required and three forbidden README substrings, exits 0 against the shipped README, and runs as a blocking CI step positioned between the existing coverage check and the package build.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Record and run the phase's one manual verification -- the human install walkthrough</name>
  <read_first>
    - `.planning/phases/07-cycle-timing-and-wedge-triage/07-HUMAN-UAT.md` -- the exact frontmatter keys
      (`status`, `phase`, `source`, `started`, `updated`, `resolved_by`) and the body structure
      (`## Current Test`, `## Tests`, then a numbered test with `expected:`, `why_human:` and
      `result:` fields). Copy this shape; it is the house format.
    - `.planning/phases/08-capability-honesty-and-the-install-story/08-VALIDATION.md` § Manual-Only
      Verifications -- the single row defining this item, including its stated test instructions.
    - `README.md` -- the section written in Task 1. The walkthrough follows the README as written, not
      the executor's memory of what it should say.
  </read_first>
  <files>.planning/phases/08-capability-honesty-and-the-install-story/08-HUMAN-UAT.md</files>
  <what-built>
Plans 08-01 through 08-05 delivered: a capability registry and a runtime refusal naming the tool, the
reason and the providing backend; a generated, drift-guarded per-backend support table; four annotated
fork-only call sites in the playbooks plus a section-scoped honesty lint; and the README install story
with a per-ecosystem VICE version table and the `VICE_BACKEND` switch. Every mechanical check is green
in CI.

What no script can prove is criterion 3's actual claim: that a human, following only the prose, on a
machine this project does not control, ends up with a working setup.
  </what-built>
  <action>
First create `.planning/phases/08-capability-honesty-and-the-install-story/08-HUMAN-UAT.md`, following
`07-HUMAN-UAT.md`'s format exactly: the same frontmatter keys with `status: pending` and
`source: [08-VALIDATION.md]`, a `## Current Test` block naming the open item, and a `## Tests` section
with one numbered test carrying `expected:`, `why_human:` and `result:` fields.

The test, stated so someone other than the author can run it: on a clean or containerised
Debian or Ubuntu box, follow **only** `README.md` -- install VICE from the package manager, install the
plugin by whichever of the two documented routes the tester picks, select the stock backend, and then
run `c64-ram-capture`'s documented entry-point procedure end to end. Record the VICE version the
package manager actually delivered, whether the README's stated version for that ecosystem matched,
and whether any step required knowledge not present in the README.

`why_human:` must say why: it is a claim about a human successfully following prose on a machine this
project does not control. The mechanical presence and table checks are a strong proxy, not proof.

Then attempt the run. If the execution environment permits it (a container or a spare box), run it and
fill in `result:` with the transcript, the delivered VICE version, and every place the README was
insufficient. If it does not permit it, leave `status: pending` with `result:` empty and say plainly
what is blocking -- an honest pending item is the correct outcome, and Phase 7 shipped exactly this
shape before its own item was later closed by a real run.
  </action>
  <how-to-verify>
1. Confirm the file exists at
   `.planning/phases/08-capability-honesty-and-the-install-story/08-HUMAN-UAT.md` and its frontmatter
   keys match `07-HUMAN-UAT.md`'s.
2. Read the numbered test. Ask: could someone who has never seen this repo run it from that text
   alone? If not, it needs more detail.
3. If the executor ran it, read the `result:` transcript. Confirm the delivered VICE version is
   recorded and that it matches what README's table claims for that ecosystem. A mismatch is a README
   correction, not a passing result.
4. If the executor did not run it, confirm `status: pending` and that the blocker is stated concretely
   rather than as "not possible".
5. Decide whether the phase closes with this item pending (acceptable -- it is tracked) or whether you
   want the walkthrough run before `/gsd-verify-work`.
  </how-to-verify>
  <verify>
    <human-check>Read 08-HUMAN-UAT.md and confirm the test is runnable by a stranger, and that any recorded VICE version matches README's table for that ecosystem.</human-check>
    <automated>test -f .planning/phases/08-capability-honesty-and-the-install-story/08-HUMAN-UAT.md; grep -q 'apt install vice' .planning/phases/08-capability-honesty-and-the-install-story/08-HUMAN-UAT.md; grep -q 'why_human' .planning/phases/08-capability-honesty-and-the-install-story/08-HUMAN-UAT.md; grep -q 'c64-ram-capture' .planning/phases/08-capability-honesty-and-the-install-story/08-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/08-capability-honesty-and-the-install-story/08-HUMAN-UAT.md` exists and contains the literal strings `apt install vice`, `why_human`, `c64-ram-capture` and `VICE_BACKEND`.
    - Its frontmatter key set is identical to `07-HUMAN-UAT.md`'s: `diff <(sed -n '2,8p' .planning/phases/07-cycle-timing-and-wedge-triage/07-HUMAN-UAT.md | cut -d: -f1) <(sed -n '2,8p' .planning/phases/08-capability-honesty-and-the-install-story/08-HUMAN-UAT.md | cut -d: -f1)` produces no output.
    - Exactly one numbered test is present, and it carries all three of `expected:`, `why_human:` and `result:`.
    - `status` is either `pending` with a concrete named blocker in the body, or `complete` with a non-empty `result:` transcript recording the delivered VICE version.
    - If a run happened and the delivered VICE version differs from README's claim for that ecosystem, a README correction is recorded as a follow-up finding in the SUMMARY rather than left implicit.
  </acceptance_criteria>
  <resume-signal>Type "approved" to close the phase with this item as recorded, or describe what the walkthrough must additionally cover.</resume-signal>
  <done>The phase's one manual item is recorded in the house format, runnable by a stranger, and either executed with a transcript or left honestly pending with a named blocker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published README prose -> a stranger's shell | Any command the README tells a user to paste executes on their machine with their privileges |
| README's version and availability claims -> a user's install decision | A wrong claim sends them to a package that does not exist, or to a version whose limits they were not told about |
| `.github/workflows/ci.yml` -> the CI runner | A workflow step runs arbitrary code on every push; a badly-placed or malformed step can skip a gate or break the job |
| `README.md` file content -> `check-skill-fork-honesty.mjs` | First-party prose read as data, matched by substring, never executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Tampering | a paste-able launch command that silently fails to bind the monitor | mitigate | Task 1(e) forbids publishing any command that has not been run live against a real stock binary by absolute path, requires the SUMMARY to carry the command, binary path, version and observed evidence, and requires `-default` to precede `-binarymonitor` if present at all. If live verification is impossible, no command is published. This closes research Pitfall 4 at MEDIUM confidence by promoting it to a measurement. |
| T-08-05-02 | Spoofing | README claiming a capability the active backend lacks | mitigate | Task 1 fixes the unqualified "screenshots" bullet and links the mechanically-generated `docs/tool-support.md` as the per-tool authority. Task 2 asserts the two fork-required tool names are named. |
| T-08-05-03 | Repudiation | README drifting back into false claims | mitigate | Task 2 adds five required and three forbidden substring assertions to a CI-blocking script, with transient proofs in both directions in the acceptance criteria. |
| T-08-05-04 | Information Disclosure | leaking a local absolute path, hostname or environment value into public install prose | mitigate | Task 1 restricts published commands to what was live-verified and requires the SUMMARY (a planning artifact) to carry the machine-specific detail. The two absolute binary paths named in the action are for the executor's verification run, not for publication; the README's own command must be one a stranger can run. |
| T-08-05-05 | Denial of Service | advising a user into the single-client monitor trap | mitigate | Task 1(e) requires the single-client warning, and its acceptance greps for it. `CLAUDE.md`'s Concurrency constraint says a second connection is indistinguishable from a wedge, so omitting this warning manufactures support requests. |
| T-08-05-06 | Elevation of Privilege | a malformed or misplaced CI step weakening the gate set | mitigate | Task 2 asserts the new step's byte offset falls strictly between the existing coverage step and the package build, that both existing steps still appear exactly once, and that the diff is additions only with no reindentation. |
| T-08-05-07 | Injection | README content reaching an interpreter via the lint | mitigate | The lint `readFileSync`s `README.md` and performs substring containment only -- no regex construction from file content, no `eval`, no spawn. Inherited from plan 08-04's script discipline, which its own acceptance greps for. |
| T-08-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package is installed in any ecosystem by this plan. Task 1's acceptance requires `check-npm-packages.mjs` to pass **unchanged**, with neither `package.json` modified, so the generated doc does not leak into a tarball (D-H). The README's `apt`/`brew`/`pacman` commands are instructions to a human, not executed by this repo; each version claim is dated and sourced, and the two MEDIUM-confidence rows (Fedora, Flatpak) are marked or omitted rather than asserted. |
</threat_model>

<verification>
- `node scripts/check-skill-fork-honesty.mjs` exits 0.
- `node scripts/check-skill-tool-coverage.mjs` exits 0.
- `node scripts/check-npm-packages.mjs` exits 0, with neither `package.json` nor that script modified.
- `node scripts/generate-tool-support-table.mjs` then `git diff --exit-code -- docs/tool-support.md` is clean.
- `cd .claude/mcp/vice && node test-gate.mjs` exits 0.
- `cd .claude/mcp/vice && npx tsc --noEmit -p tsconfig.json` exits 0.
- `bash scripts/package.sh` succeeds (the CI step that follows the new one).
- `grep -c 'regenerator2000' README.md` is 0.

</verification>

<success_criteria>
- A stranger reading only README.md can install VICE, pick a backend, and know what that backend cannot do.
- Every version and availability claim is dated, and the two that research could not confirm are marked or omitted rather than asserted.
- Any published command was run live before publication, or no command is published.
- README contains no false claim about this repository, and a regression in either direction fails CI.
- The phase's one manual item is recorded in the house format.
</success_criteria>

<output>
Create `.planning/phases/08-capability-honesty-and-the-install-story/08-05-SUMMARY.md` when done.
Record: the exact launch command published (or why none was), with the binary path, version and
observed binding evidence; the repo-wide grep result confirming the two ghost test filenames are
absent before deletion; how the "screenshots" bullet was requalified; whether Flatpak/Snap were
omitted or marked; the two transient lint failure messages from Task 2; the re-verified `ci.yml` line
numbers with any drift; and the `08-HUMAN-UAT.md` status with its blocker if pending. Also record any
README-versus-reality mismatch found during the walkthrough as a follow-up finding.
</output>
