---
phase: 33-the-reproducible-run-protocol-and-the-capture-substrate-go-d
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/33-CONTEXT.md
  - .planning/STATE.md
autonomous: true
requirements: [CAP-01, CAP-02]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A reader of 33-CONTEXT.md alone gets the TRUE C64MEM body length (65543 minimum / 65555 at snapshot minor 1) and the TRUE port-normalisation source fields (dir_read and data_read, in the 3-byte suffix), not the falsified ones — the amendment carries the measured counter-value beside the superseded text (D-21, D-24)"
    - "33-CONTEXT.md no longer asserts that compare.mjs's volatile/drift/divergence rules are the shape CAP-02 should keep — the range-shaped allow-list and the one-bit-drift-passes rule are named as incompatible with CAP-02 and with D-25's control"
    - "The two completed phase-32-review todos are gone from STATE.md's Deferred Items table, so `docs-deferred-ledger.test.ts` and the `audit-integrity.test.ts` cascade go green and the measured suite baseline moves from 5 failing tests in 3 files to 2 failing tests in anno-register.test.ts alone"
    - "The remaining 2 failures have their root cause recorded in STATE.md as an open, out-of-phase concern (four v0.7.0 requirement ids dropped by the v0.8.0 REQUIREMENTS rewrite), so a later reader does not mistake them for Phase 33 regressions"
  artifacts:
    - path: ".planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/33-CONTEXT.md"
      provides: "D-21, D-24 and the compare.mjs code-insight claim amended with dated riders carrying the measured counter-values"
      contains: "AMENDED"
    - path: ".planning/STATE.md"
      provides: "A Deferred Items table matching the pending-todo tree in both directions, plus the recorded out-of-phase requirement-id concern"
  key_links:
    - from: ".planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/33-CONTEXT.md"
      to: "src/mcp/vice/vsf-slice.ts"
      via: "33-04 implements the amended D-21 length rule; the plan text and CONTEXT.md now agree"
      pattern: "65543"
    - from: ".planning/STATE.md"
      to: "src/mcp/vice/docs-deferred-ledger.test.ts"
      via: "the two-directional Deferred Items <-> pending-todo-tree invariant"
      pattern: "Deferred Items"
  prohibitions:
    - statement: "MUST NOT re-derive or restate the suite baseline to match whatever the tree happens to produce at the moment a criterion is checked — the baseline is a recorded measurement with named root causes, and a plan that quietly adopts a new number has converted a known-red into a moving target"
      status: resolved
      verification: judgment
---

<objective>
Reconcile the three working decisions this phase's research falsified, narrow the
one whose scope it corrected (`D-15`), and repair one of the two root causes behind
the suite's known-red baseline — all **before** any code in this phase depends on
them.

Purpose: `33-RESEARCH.md` § *Decisions This Research Falsifies* measured three
CONTEXT.md claims false against genuine stock VICE 3.9 on this host. A plan that
silently implements the corrected behaviour while `33-CONTEXT.md` still asserts
the falsified one leaves the next reader with two contradictory sources of truth —
and one of the three (`D-21`'s `4 + 65536` body assertion) would refuse **every**
real snapshot, losing the gate's most easily-earned `go` input to an arithmetic
error. Separately, two completed todos still listed `Pending` in `STATE.md` red
three of the five failing tests in the measured baseline, and repairing them here
gives every later plan in this phase a tighter number to compare against.

Output: an amended `33-CONTEXT.md` whose D-21, D-24 and `compare.mjs` claims carry
dated riders with their measured counter-values, whose `D-15` carries a fourth
rider scoping its argv byte-identity claim to the fork branch and the `profile`
field, and a `STATE.md` whose Deferred Items table matches the pending-todo tree in
both directions.

**Why `D-15` is in this plan.** It is the only decision in `33-CONTEXT.md` that no
plan cited, and its byte-identity claim is measured wrong for the stock branch by
`33-05`. Leaving it unamended is the exact failure mode this plan exists to
prevent: a reader arriving at the decision text alone would act on a superseded
number. `33-05` and `33-06` now cite `D-15` by id on the implementation side, so
the decision record, the research and the plans agree.

**Worktree carve-out:** this plan delivers `.planning/STATE.md` content, so it
takes the stock per-plan `USE_WORKTREES_FOR_PLAN=false` carve-out. It writes
nothing under `evidence/`, so it cannot disturb `33-01`'s ordering proof.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/33-CONTEXT.md
@.planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/33-RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Amend the three falsified claims in 33-CONTEXT.md and narrow D-15's byte-identity claim, each beside its measured counter-value</name>
  <read_first>
    - .planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/33-CONTEXT.md — the `<decisions>` block (D-21, D-24 and D-15 verbatim) and the `<code_context>` `### Reusable Assets` bullet naming `compare.mjs`
    - .planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/33-RESEARCH.md — § "Decisions This Research Falsifies" (BOTH tables: the three-row falsification table and the two-row scope-correction table below it, whose first row is the one echoed by `D-15`), § "Measured Evidence" M4 and M5, and § "Common Pitfalls" P3 and P6
    - .planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/33-05-PLAN.md — its `<objective>`'s "Note on scope" paragraph, which is the wording the `D-15` rider must agree with rather than restate differently
    - src/skills/c64-ram-capture/scripts/compare.mjs — its own header rules block and the `VOLATILE` span table, so the amendment names what is actually there
  </read_first>
  <files>.planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/33-CONTEXT.md</files>
  <action>
Edit `33-CONTEXT.md` in place. Do not rewrite history: leave each falsified
sentence readable and append a dated rider immediately after it, in the same
register the project uses elsewhere for superseded prose. Each rider opens
`**AMENDED 2026-09-02** (33-RESEARCH.md § Decisions This Research Falsifies):`
and carries the measured counter-value, so a reader who arrives at the decision
text alone cannot act on the superseded number.

**Amendment 1 — D-24, the port normalisation.** The decision says the
`$0000`/`$0001` overlay is normalised "by substituting the snapshot's own 4-byte
port/PLA prefix over RAM `$0000`/`$0001`". The rider must state: the 4-byte prefix
is `(pport.data, pport.dir, EXROM, GAME)` — data first, address-swapped relative
to the decision text — and the CPU-visible values are neither of those. The CPU
sees `pport.dir_read` at `$0000` and `pport.data_read` at `$0001`, and those two
fields live in the **3-byte suffix immediately after the RAM array**. Give the
measured triple: `prefix=[231,47,0,0]` against `suffix3=[39,55,47]`, with the live
register read on the same snapshot giving `$00=47 $01=55`. Name the consequence:
a prefix-over-RAM copy writes 231 to `$0000` where the CPU sees 47 — wrong by 176,
silently, at exactly the two addresses the normalisation exists to fix. State the
corrected rule as the byte offsets a reader can check: `$0001` takes
`body[RAM_OFFSET + RAM_SIZE + 1]` and `$0000` takes `body[RAM_OFFSET + RAM_SIZE + 2]`,
with `RAM_OFFSET = 4` and `RAM_SIZE = 65536`. The rest of D-24 stands unchanged:
normalise in code, in one place, and explicitly not by spending two allow-list
slots.

**Amendment 2 — D-21, the body-length assertion.** The decision says the slicer
"asserts the module body is exactly `4 + 65536` bytes, refusing otherwise". The
rider must state: `4 + 65536` is 65540 and the measured `C64MEM` body on this host
is **65555**, because VICE writes three more port bytes and two DWORD falloff
clocks plus four more state bytes after the RAM array —
`4 + 65536 + 3 + 4 + 4 + 4 = 65555` at snapshot minor 1, and
`4 + 65536 + 3 = 65543` at minor 0, which the reader also accepts. So the assertion
becomes `body.length >= 65543`, refusing below it and recording which minor
produced which length. Name the cost of not fixing it: the slicer would refuse
every real snapshot, and `SLICER: failed` would fire `R1 → no-go` on an arithmetic
error. Add the second half of the same amendment: the prototype at
`.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/vsf-ram-extract.mjs`
carries `first_module_offset = 37`, which is stale — the real value is **58** on
3.9, because a second magic block (`"VICE Version\x1a"` 13 bytes + 4 version bytes
+ a 4-byte SVN dword) follows the machine name. State that the prototype survives
the stale offset only through a byte-by-byte resync fallback, that such a scan can
lock onto a false module-name string inside 64 KB of RAM data, and that the
corrected rule is a **strict walk** that refuses on the first malformed module
header — the resync path reintroduces D-21's own stated worst failure mode by the
recovery route rather than by the offset.

**Amendment 3 — the `compare.mjs` claim in `<code_context>`.** The
`### Reusable Assets` bullet says `compare.mjs`'s "existing volatile / drift /
divergence classification is the shape `CAP-02`'s verdict should keep". The rider
must state: its volatile set is four **ranges** covering 4866 addresses and its
drift rule lets any single-bit difference pass anywhere, both of which `CAP-02`
forbids — it requires an enumerated list, never a range. Name the concrete
consequence for `D-25`: a control that plants a one-bit difference against an
inherited predicate PASSES, so the fail-ability guard proves nothing. State what
IS inheritable: the reporting vocabulary, the `digest` verb, the
header-states-the-rules convention, the `hex4`/`hex2`/`bin8`/`popcount` helpers
and the exact-size load refusal. State what is not: the four ranges and the
drift-passes rule. Add the simplification the snapshot route buys: `$D000-$DFFF`
is volatile only on the transcription route, because `vice_memory_read` samples
live I/O while the `.vsf` `C64MEM` array is `mem_ram[]` — RAM under I/O, not the
register image — so on the snapshot route that 4096-address exclusion disappears
entirely and the capture-record template's standing note about it must be
re-grounded rather than copied.

**Amendment 4 — D-15, the argv byte-identity claim.** This one is a **narrowing of
scope**, not a falsification, and the rider must say so in its first clause — the
research's own second table classifies it that way, and overstating it would
falsify a decision that is mostly right. `D-15` says an absent `profile` produces
argv "byte-identical to today's, which keeps the three whole-argv
`assert.deepEqual` assertions in `broker-launch.test.ts` green". The rider must
state: there are **five** stock whole-argv `assert.deepEqual(args, …)` assertions,
not three (lines 1775, 1789, 1907, 1919, 1929), and `REPRO-01`'s determinism block
is *unconditional on stock*, so all five change — and they change with `profile`
absent, because it is the determinism block and not the profile that moves them.
The rider must contain the phrase `five stock whole-argv` so the reconciliation
check can see it. Name what survives: byte-identity holds in full only on the
**fork** branch, whose argv this phase leaves untouched, and on the stock branch
only the `profile` half survives — an absent profile adds no flag. Name what
`D-15` gets exactly right and this phase preserves: `-default` stays at argv index
0 ahead of `-binarymonitor`, the three *ordering* assertions do survive additions,
and the field really is additive and absent by default. Give the consequence of
not writing this down: a reader arriving at `D-15` alone would treat five changed
whole-argv assertions in `33-05` as an unplanned regression rather than as the
measured cost of `REPRO-01`. Point the rider at `33-05`'s objective, which carries
the same narrowing on the implementation side, and at `33-06`, which implements
`D-15` and `D-16` together.

Finally, add a short `### Research Reconciliation` subsection at the end of the
`<decisions>` block listing the four amendments with one line each, so the
amendments are discoverable without reading every decision. Distinguish the three
falsifications (`D-21`, `D-24`, the `compare.mjs` claim) from the one scope
narrowing (`D-15`) in that index. Also record there, in one line each, the two
ROADMAP-note claims this phase treats as **superseded by measurement**: the note
calling the `broker-launch.test.ts` whole-argv assertions "avoidable" — the same
claim `D-15` echoes, so cross-reference Amendment 4 rather than writing the
counter-value twice — and the note stating the clean floor for `test:automated` is
0 (measured `EXIT=1`, 5 failing tests in 3 files, from two root causes this phase
did not create).
  </action>
  <acceptance_criteria>
    - `33-CONTEXT.md` contains the literal strings `65543`, `65555`, `dir_read`, `data_read` and `58`, none of which appear in it today
    - Every one of the four amendments opens with `**AMENDED 2026-09-02**` and names `33-RESEARCH.md` as its source
    - `D-15` carries a rider naming five stock whole-argv assertions and scoping byte-identity to the fork branch and the `profile` field, labelled a scope narrowing rather than a falsification
    - The original D-21, D-24 and D-15 sentences are still present and readable — the amendment appends, it does not delete
    - A `### Research Reconciliation` subsection exists and names all four amendments — three falsifications plus the `D-15` narrowing — plus the two superseded ROADMAP-note claims
    - `33-CONTEXT.md` states the measured `test:automated` baseline as 5 failing tests in 3 files and does not claim a clean floor of 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && C=.planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/33-CONTEXT.md && for s in 65543 65555 dir_read data_read 'Research Reconciliation' 'AMENDED 2026-09-02' 'five stock whole-argv'; do grep -aq "$s" "$C" || { echo "missing: $s"; exit 1; }; done && test "$(grep -ac 'AMENDED 2026-09-02' "$C")" -ge 4 && grep -aq 'first_module_offset' "$C" && echo RECONCILED_OK</automated>
    <fails_when>non-zero exit, `missing: &lt;string&gt;` on stdout, or `RECONCILED_OK` absent from stdout — the count check reds if fewer than four amendment riders were written, and a `missing: five stock whole-argv` line is the specific signal that the `D-15` rider was skipped</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && C=.planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/33-CONTEXT.md && grep -aq 'asserts the module body is exactly' "$C" && grep -aq '4-byte port/PLA' "$C" && grep -aq 'volatile / drift' "$C" && grep -aq "byte-identical to today's" "$C" && echo ORIGINALS_INTACT</automated>
    <fails_when>non-zero exit, or `ORIGINALS_INTACT` absent from stdout — which happens if the amendment deleted a falsified or narrowed sentence instead of appending a rider beside it</fails_when>
  </verify>
  <done>D-21, D-24, the `compare.mjs` code-insight claim and D-15 each carry a dated amendment rider with its measured counter-value — the first three as falsifications, D-15 as a scope narrowing to the fork branch and the `profile` field; the superseded text is still readable; a `### Research Reconciliation` subsection indexes all four plus the two superseded ROADMAP-note claims.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the two stale Deferred Items rows and record the residual failures as an out-of-phase concern</name>
  <read_first>
    - .planning/STATE.md — the `## Deferred Items` table (the 12-row `| Category | Item | Priority | Status |` table) and the paragraph above it describing the two prior stale-row incidents
    - src/mcp/vice/docs-deferred-ledger.test.ts — the two-directional invariant, its scope statement (only the `## Deferred Items` section is scanned), and the `AUDIT-04 direction B` assertion
    - .planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/33-RESEARCH.md — § "Common Pitfalls" P8 (the five failures, three files, two root causes) and § "Open Questions" Q3
  </read_first>
  <files>.planning/STATE.md</files>
  <action>
Two rows in `STATE.md`'s `## Deferred Items` table name todos that have already
moved to `.planning/todos/completed/`:
`2026-08-31-phase-32-review-twenty-five-open-findings` and
`2026-09-01-phase-32-review-round-4-nine-open-findings`. Both are the `audit`
category, both `blocker`, both listed `Pending`. Delete **those two table rows and
nothing else** — do not touch the ten remaining rows, do not reorder them, and do
not reformat the table.

Editing risk to avoid, from this project's own record: a broad
search-and-replace over `STATE.md` for a bare status word has previously deleted
standalone `NONE`/`none` tokens elsewhere in the file and inverted unrelated
sentences. Address the two rows by their unique todo stems, and diff `STATE.md`
after the edit — the diff must be exactly two deleted lines plus the additions
Task 2 makes below.

Then record the residual failures so the next reader cannot mistake them for a
Phase 33 regression. In the paragraph that already precedes the Deferred Items
table (the one describing the two earlier stale-row incidents), append a dated
sentence: measured 2026-09-02 with the broker inactive, `npm run test:automated`
in `src/mcp/vice` exited 1 with 5 failing tests across 3 files; three of them
(`docs-deferred-ledger.test.ts` x2 and the `audit-integrity.test.ts` cascade at
`no milestone audit declares a gated status while any docs guard is red`) had
these two rows as their single root cause and are closed by this edit; the
remaining two (`anno-register.test.ts`'s `DIRECTION 5 (basis integrity)` and its
`planted violation (the negative control)`) have a different, **out-of-phase**
root cause — `STORE-01`, `STORE-04`, `STORE-06` and `MCP-04` are cited by the anno
tool register but are no longer declared in `.planning/REQUIREMENTS.md`, which was
rewritten for v0.8.0 and dropped the v0.7.0 ids. State that reconciling those four
ids is deliberately **not** in Phase 33's scope (they are v0.7.0 store ids and the
fix may legitimately be a carried-ids section in `REQUIREMENTS.md` rather than a
code change), and that the expected `test:automated` baseline for the remainder of
Phase 33 is therefore **2 failing tests in `anno-register.test.ts` alone**.

Do not file a new todo for the four requirement ids in this plan — adding a row to
the Deferred Items table without a matching file under `.planning/todos/pending/`
reds the same guard this task is repairing, in the other direction.
  </action>
  <acceptance_criteria>
    - `STATE.md`'s `## Deferred Items` table has 10 data rows, and neither `phase-32-review-twenty-five-open-findings` nor `phase-32-review-round-4-nine-open-findings` appears in that section
    - Every remaining row's todo stem still has a matching file under `.planning/todos/pending/`, and every file under `.planning/todos/pending/` still has a matching row
    - `cd src/mcp/vice && node --test docs-deferred-ledger.test.ts` reports `fail 0`
    - `cd src/mcp/vice && node --test audit-integrity.test.ts` reports `fail 0`
    - `git diff -- .planning/STATE.md` shows exactly two deleted table lines; no other line is deleted
    - The paragraph above the table names the measured 5-in-3 baseline, the three failures this edit closes, and the two that remain with their out-of-phase root cause
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -aq 'phase-32-review-round-4-nine-open-findings' .planning/STATE.md && { echo "stale row still present"; exit 1; }; grep -aq 'phase-32-review-twenty-five-open-findings' .planning/STATE.md && { echo "stale row still present"; exit 1; }; test "$(git diff --numstat -- .planning/STATE.md | awk '{print $2}')" -eq 2 && echo LEDGER_ROWS_OK</automated>
    <fails_when>non-zero exit, `stale row still present` on stdout, or `LEDGER_ROWS_OK` absent from stdout — the numstat check reds if more or fewer than two lines were deleted from STATE.md</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-deferred-ledger.test.ts audit-integrity.test.ts</automated>
    <fails_when>non-zero exit, or `fail 0` absent from the summary lines</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated 2>&1 | tail -20</automated>
    <fails_when>more than 2 failing tests in the summary, or any failing test in a file other than `anno-register.test.ts`. The measured pre-plan baseline is 5 failing tests in 3 files, so a non-zero exit alone is NOT the failure signal here — the signal is a count above 2 or a fourth file name appearing.</fails_when>
  </verify>
  <done>The two stale Deferred Items rows are gone, the ledger guard and the audit-integrity cascade are green, `test:automated` is down to 2 failing tests in `anno-register.test.ts` alone, and STATE.md records the remaining root cause as an out-of-phase concern with the new expected baseline stated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research measurement → the phase's own decision record | A falsified decision text that survives into execution becomes the authority a later reader acts on |
| `STATE.md` prose → the suite's two-directional ledger guard | A blunt edit to this file has twice inverted unrelated sentences in this project |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-14 | Tampering | `33-CONTEXT.md` decision text | high | mitigate | Amend by appending a dated rider beside the superseded sentence, never by deleting it; Task 1's second verify asserts the original D-21 and D-24 sentences are still greppable, so a silent rewrite reds. |
| T-33-15 | Tampering | `.planning/STATE.md` | high | mitigate | Address the two rows by their unique todo stems, never by a bare status token (this project's own record: a broad status replace deleted standalone `NONE` tokens and inverted unrelated sentences). Task 2's verify pins the deletion count at exactly 2 via `git diff --numstat`. |
| T-33-16 | Repudiation | the recorded suite baseline | medium | mitigate | The new expected count (2 failing tests in `anno-register.test.ts`) is written into `STATE.md` with the root cause named, and every later plan's `fails_when` states it. The residual failure is recorded as out-of-phase rather than absorbed or hidden. |
| T-33-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan or anywhere in this phase — `33-RESEARCH.md` § *Package Legitimacy Audit* records "Not applicable to this phase". Accepted because the attack surface is absent; any later plan proposing an install must run the legitimacy gate protocol first. |
</threat_model>

## Artifacts this phase produces

**Created by THIS plan:** no new files and no new symbols — this plan amends
`.planning/phases/33-…/33-CONTEXT.md` and `.planning/STATE.md` in place. New
identifier text it introduces into prose: `65543`, `65555`, `MIN_C64MEM_BODY_LEN`,
`V01_C64MEM_BODY_LEN`, `dir_read`, `data_read`, `first_module_offset = 58`.

**Created elsewhere in Phase 33 (also new — not pre-existing drift):**
`src/mcp/vice/vsf-slice.ts` (`SNAPSHOT_MAGIC`, `SNAPSHOT_MACHINE_NAME_LEN`,
`SNAPSHOT_VERSION_MAGIC`, `FIRST_MODULE_OFFSET`, `MODULE_HEADER_LEN`,
`RAM_OFFSET`, `RAM_SIZE`, `MIN_C64MEM_BODY_LEN`, `V01_C64MEM_BODY_LEN`,
`VsfSliceError`, `listSnapshotModules()`, `sliceC64Mem()`, `SnapshotModule`,
`C64MemSlice`) · `capture-predicate.ts` (`TRANSIENT_ALLOW_LIST_CAP`,
`CaptureComparisonError`, `normalisePorts()`, `compareCaptures()`,
`parseAllowList()`, `argvDigest()`, `TransientAllowList`, `CaptureComparison`) ·
`stop-oracle.ts` (`ORACLE_TERMS`, `StopOracleError`, `compareStopIdentity()`,
`StopIdentity`, `StopIdentityComparison`) · `stock-reproducible-run.ts`
(`runReproducible()`, `REPRODUCIBLE_RUN_REQUIRED_SIBLINGS`,
`ReproducibleRunOutcome`) · `broker-launch.mts` additions
(`STOCK_DETERMINISM_SEED`, `STOCK_DETERMINISM_FLAGS`, `LaunchProfile`,
`buildViceArgs`'s optional `profile`) · `broker-control.mts`
(`normaliseLaunchProfile()`) · `broker-state.mts` (`InstanceRecord.profile`) ·
`vice-broker.mts` (`profileEligible()`) · `stock-run-until.ts` (`RUN_UNTIL_KEYS`
extended with `reproducible` and `frame_anchor`) · tests `vsf-slice.test.ts`,
`capture-predicate.test.ts`, `capture-seam.test.ts`,
`stock-reproducible-run.test.ts` · fixtures `src/mcp/vice/fixtures/vsf/` · skill
scripts `vsf-slice.mjs` (verbs `slice`, `digest`) and `derive-transients.mjs`
(verbs `derive`, `check`) · `src/skills/c64-ram-capture/transients/` ·
`docs/phase33-reproducible-run-gate-findings.md` ·
`.planning/phases/33-…/evidence/*`

<verification>
1. `33-CONTEXT.md` greps clean for all four corrected values (`65543`, `65555`,
   `dir_read`, `data_read`) and for `58` as the first module offset.
2. Four `**AMENDED 2026-09-02**` riders exist — `D-21`, `D-24`, the `compare.mjs`
   claim and `D-15` — and all four superseded sentences are still greppable,
   including `D-15`'s "byte-identical to today's".
3. `git diff --numstat -- .planning/STATE.md` shows exactly 2 deletions.
4. `node --test docs-deferred-ledger.test.ts audit-integrity.test.ts` reports
   `fail 0`.
5. **Wave gate for wave 1, and this plan's own deliverable measurement**
   (~90 s): `cd src/mcp/vice && npm run test:automated` at 2 failing tests, both
   in `anno-register.test.ts`. This is the one plan in the phase where the full
   suite legitimately runs at task scope rather than only at wave scope: the
   suite result is not a regression check here, it *is* the deliverable. Task 2
   exists to move the number, the phase-open baseline of **5 failing tests in 3
   files** is what it moves from, and 2-in-1 is the figure every later wave gate
   compares against — so it has to be measured in the plan that produces it. A
   non-zero exit alone is NOT the failure signal; the signal is a count above 2
   or a third file name appearing. Every other plan in this phase keeps the
   targeted `node --test` per task and defers the suite to its wave gate, per
   `33-VALIDATION.md` § Sampling Rate.
</verification>

<success_criteria>
- No source of truth in this phase still asserts a falsified constant.
- The suite's known-red set is down from 5 tests in 3 files to 2 tests in 1 file,
  with the residual root cause recorded as out-of-phase.
- The Deferred Items table matches the pending-todo tree in both directions.
</success_criteria>

<output>
Create `.planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/33-02-SUMMARY.md` when done
</output>