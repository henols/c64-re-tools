---
phase: 58-one-declaration-four-places-that-can-no-longer-disagree
plan: 02
type: execute
wave: 2
depends_on: ["58-01"]
files_modified:
  - docs/phase58-declaration-provenance.md
  - README.md
autonomous: true
requirements: [DECL-01, DECL-04]

estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "`docs/phase58-declaration-provenance.md` exists and records, for every case where two in-tree sources disagreed about the same fact, which one the declaration followed and why — satisfying the second half of criterion 5 in the place a human reads, not in prose inside the JSON (DECL-01, D-04)"
    - "The VICE 3.10 disagreement is recorded as a worked example: `README.md:96-97` frames the gate as load-bearing, `.planning/REQUIREMENTS.md:88` records that no shipped tool refuses on it, REQUIREMENTS.md wins as the later evidence, and the doc says so by name (DECL-01, D-05)"
    - "The reuse of the VICE install table's own text as the `c1541` and `petcat` remedy is recorded as a deliberate authoring decision with its cause — one OS package installs all three binaries — and not left to read as copy-paste (DECL-01, RESEARCH Pitfall 2)"
    - "The scope of the one `measured` grade is recorded precisely: which runner label executes it, which single ecosystem key carries it, and that no sibling ecosystem is promoted to `measured` by sharing a package manager (DECL-01, RESEARCH Pitfall 4)"
    - "The two Node floors are recorded as two different facts: the record's `versionFloor` is the one a shipped code path refuses below, and the installer package's lower declaration is the doctor's own start floor and deliberately does not become a second field (DECL-04, D-14)"
    - "`README.md`'s prose about what an older VICE costs no longer claims a consequence this project has since measured false; it states that no shipped tool carries a VICE version dependency and cites the measured evidence (DECL-04, D-10, D-11)"
    - "`README.md`'s eight-row install table is byte-unchanged — that section is generated in Phase 62 and a hand-edit here would be overwritten (D-11)"
    - "Every `file:line` citation the provenance doc makes names a file that exists in the repository (DECL-01, criterion 5)"
    - "No claim in either deliverable asserts a version number, runner image version, or install outcome that was not read off a real artifact — an unobserved figure is named as unobserved rather than filled in (D-03)"
  artifacts:
    - docs/phase58-declaration-provenance.md
    - README.md
  key_links:
    - "`docs/phase58-declaration-provenance.md` to `src/mcp/vice/prerequisites.json` — the JSON's `provenance` tag is what a test asserts on; this doc is what a human reads. Neither restates the other, and a case recorded in one and absent from the other is the gap this pairing exists to close"
    - "`README.md`'s rewritten prose to `.planning/REQUIREMENTS.md:85,88` — the measured evidence behind the correction, and the same fact the provenance doc's first worked case documents. Both must say the same thing"
    - "`README.md:99-108` to Phase 62's `GEN-01` — the table is generated there, so it is the one part of this section Phase 58 must leave untouched"
  prohibitions:
    - statement: "MUST NOT hand-edit the eight-row VICE install table. Phase 62 generates that section from the declaration, so an edit here is overwritten and the effort is worse than wasted — it creates a third description for one milestone."
      status: required
      verification: flagged-unverified
    - statement: "MUST NOT edit `CLAUDE.md` or `.planning/PROJECT.md`. The stale ACME-prefix citation in the constraints list is a real defect and an explicitly deferred one; `CLAUDE.md`'s constraint list is mirrored from PROJECT.md, so a one-sided edit is wiped on the next regeneration and would look like the defect was closed."
      status: required
      verification: flagged-unverified
    - statement: "MUST NOT replace a false claim with a remembered one. Every factual statement in the rewritten prose cites the artifact it was read from. Swapping one unsourced assertion for another reproduces the exact defect being corrected."
      status: required
      verification: flagged-unverified
    - statement: "MUST NOT write a specific runner image version, tool version number, or install outcome that was not read off a real run log or a real file. Where the figure is genuinely unobserved, the doc says it is unobserved and names where it would be read from."
      status: required
      verification: flagged-unverified
    - statement: "MUST NOT quote a source in the provenance doc in reworded form. A quotation is the source's own words; a paraphrase is labelled a paraphrase."
      status: required
      verification: flagged-unverified
---

<objective>
Write down which source won, and why, everywhere two of them disagreed — and correct the one
piece of README prose that is still asserting the loser.

The JSON tag is what a test asserts on. This is the half a human reads: the cases where the
tree contradicted itself, the reuse decisions that would otherwise read as copy-paste, and the
figures this project has genuinely measured versus the ones it has only been told.

Purpose: satisfy criterion 5's second half — "records which was chosen and why" — in the place
it belongs, and stop a known-false claim standing for four more phases in a milestone whose
whole subject is the user's first hour.
Output: `docs/phase58-declaration-provenance.md`, and a corrected `README.md` section with its
generated table byte-untouched.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/58-one-declaration-four-places-that-can-no-longer-disagree/58-CONTEXT.md
@.planning/phases/58-one-declaration-four-places-that-can-no-longer-disagree/58-RESEARCH.md
@.planning/phases/58-one-declaration-four-places-that-can-no-longer-disagree/58-PATTERNS.md
@.planning/phases/58-one-declaration-four-places-that-can-no-longer-disagree/58-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The provenance record — every case where two sources disagreed, and which won</name>
  <precondition>`src/mcp/vice/prerequisites.json` exists and carries all eight records with their
  final `provenance` and `source` values. This doc records the choices that file actually made;
  written against a half-populated file it would document choices that were never taken.</precondition>
  <files>docs/phase58-declaration-provenance.md</files>
  <read_first>
    - `src/mcp/vice/prerequisites.json` — the committed declaration, in full. Every case below is recorded against what this file actually says, not against what the plan said it would say.
    - `.planning/phases/58-one-declaration-four-places-that-can-no-longer-disagree/58-01-SUMMARY.md` — what Plan 01 actually did, including any authoring choice it had to make that this plan did not anticipate. Those choices belong in this doc too.
    - `docs/stock-hard-losses.md` — the structural and tonal model: a plain prose header stating why the doc exists and that its outcomes are settled rather than hedged, then one section per case. No hedging, no "may", no "probably".
    - `docs/phase40-preprocessing-tools-decisions.md:1-25` — the frontmatter shape and the "statements reversed" pattern, for the cases where this doc records that an existing claim was overturned.
    - `README.md:94-97` — the sentence framing the 3.10 gate as load-bearing, and the "Checked live against each ecosystem on 2026-08-18" dating. This is the losing side of the first worked case; quote it, do not paraphrase it.
    - `.planning/REQUIREMENTS.md:85-90` — the Out of Scope row "Reporting a VICE, ACME, Ghidra or dxa version number" and its reason. This is the winning side and the later evidence.
    - `src/mcp/vice/host-tool.mts:1545-1565` and `:1625-1640` — the c1541 and petcat refusals, which carry no remedy text. Their emptiness is the cause of the second worked case and should be quoted as evidence.
    - `src/mcp/vice/host-tool.mts:2273-2311` — `findSiblingBinary()`, the structural reason those two binaries share one remedy.
    - `.github/workflows/ci.yml:17-18` and `:45-81` — the runner label and the ACME install step. Note the comment at lines 48-50 distinguishes a package-name verification from an observed install; that distinction is the third worked case.
    - `src/mcp/vice/package.json:101-103` and `installer/package.json:15-17` — the two Node floors, both real, both different facts.
    - `src/mcp/vice/host-tool.mts:2205` and `:2231-2245` — `ACME_LIB_MARKER` and `findAcmeLib()`'s four probe prefixes plus `$ACME`. These belong in this doc and nowhere in the JSON; record them here so the prefix list has a documented home outside the function body.
    - `README.md:101-108`, and specifically the Homebrew row's "macOS + Linux" scope — the cause of one ecosystem key appearing under two platform keys.
  </read_first>
  <action>
Create `docs/phase58-declaration-provenance.md`. Open with frontmatter in the style of
`docs/phase40-preprocessing-tools-decisions.md`: the phase slug, a decision id of
`prerequisite-declaration-provenance`, the date, and the CONTEXT.md decision ids this doc
discharges and relates to. Follow it with a plain prose header, in the register
`docs/stock-hard-losses.md` uses, stating what this file is the one authoritative place for:
the reasoning behind every provenance grade in `src/mcp/vice/prerequisites.json`, and every case
where two places in this tree said different things about the same fact.

Then one section per case. Each section quotes both sides in their own words with a
`file:line` citation, names the winner, and says why that side won — not merely that it did.

Case one, the VICE version gate. `README.md:96-97` frames the 3.10 floor as load-bearing.
`.planning/REQUIREMENTS.md:88` records that no shipped tool refuses on it, because the tool that
would have is served over the text channel instead, and that the floor binds an opcode no
shipped tool calls. REQUIREMENTS.md wins on recency and on being the measured side. State the
consequence: the declaration carries no VICE version data of any kind, which is what lets the
one-version-floor rule hold literally rather than by argument, and which is why the README prose
outside the generated table is corrected in this same phase.

Case two, the shared remedy. The c1541 and petcat refusals carry no remedy text at all — quote
one of them to show it names only the paths it tried. There is therefore nothing to literally
carry from the refusal, and the only remedy for those two binaries anywhere in the tree is the
VICE install table, written for a third binary. State the cause — installing the VICE package
installs all three — state that their remedy is the table's own text reused rather than freshly
worded, and state what guards the reuse: the three records' remedy texts are asserted
byte-identical to one another, so they cannot drift apart silently.

Case three, the scope of the one measured grade. Name the runner label the ACME install step
runs on and the single ecosystem key that carries the measured grade. State explicitly that a
sibling ecosystem carrying the same command text is not promoted to measured by sharing a
package manager, and name the CI comment that records a package-name verification, which is a
different claim from an observed install. If a real CI run log is available, record the runner
image version it resolved to. If none is, say the figure is unobserved and name where a reader
would read it — do not fill in a version number from memory.

Case four, the two Node floors. Both `src/mcp/vice/package.json` and `installer/package.json`
declare one, they differ, and both are correct about different things. State which one the
record's version-floor field carries and why — a shipped code path genuinely refuses below it —
and state that the installer's lower number is the doctor's own start floor, the number the
Node-18 CI proof pins and the number Phase 61's doctor cell will pin again, and that it
deliberately does not become a second field. This is the case that keeps the
one-version-floor rule from being read as "the only Node number in the project".

Case five, one ecosystem under two platforms. The Homebrew row's own scope covers both macOS
and Linux, so that ecosystem entry appears under two platform keys with byte-identical text and
source. Record it so a reader does not treat the duplication as an error.

Then a decisions section recording the authoring choices this phase made that were left open
before it: whether the file carries a schema version and what value; the shape dxa's record
uses and why a single platform-independent entry rather than the same string repeated three
times; how often the version-floor guard's planted-violation case runs; why remedy text carries
no markdown code-span formatting even though one of its sources does, and which later phase
re-adds it; whether the order of records in the file is significant and whether the order of
ecosystem entries within a platform is; and which tool-and-platform combinations have no remedy
entry at all, with the rule that an absent key is the schema's answer for "no known remedy" and
is preferred to a guessed one.

Close with the ACME library probe prefixes. `findAcmeLib()` is now their only home in the tree;
record the list and the marker file it probes for here, with its `file:line`, so the list has a
documented home outside a function body. Note in one line that `CLAUDE.md`'s constraint list
still cites the skill file for these and that correcting it is deferred, so a later reader does
not treat this doc as having closed that.
  </action>
  <acceptance_criteria>
    - `docs/phase58-declaration-provenance.md` exists, opens with YAML frontmatter carrying a `phase` key naming this phase and a `date`, and contains at least six second-level sections.
    - The doc contains the strings `README.md:96`, `REQUIREMENTS.md:88`, `host-tool.mts`, `ci.yml` and `installer/package.json` — every worked case names its sources by path.
    - Every `path:line` citation in the doc resolves: splitting each citation on its last colon yields a path that exists in the repository.
    - The doc names, for the measured grade, both the runner label and the single ecosystem key that carries it, and contains a sentence stating that no sibling ecosystem is promoted by association.
    - The doc states which of the two Node floors the record carries, and states that the other is the doctor's start floor and is not a second field.
    - The doc records all of: the schema-version choice and its value, dxa's remedy shape, the cadence of the planted-violation case, the markdown-formatting decision for remedy text, and the ordering contract for records and for ecosystem entries.
    - The doc lists the ACME library probe prefixes and the marker file, with the `file:line` of the function they come from, and states in one line that the `CLAUDE.md` correction is deferred.
    - For every remedy entry in `src/mcp/vice/prerequisites.json` whose `provenance` is `authored`, the doc names the page it came from and what that page said.
    - No sentence in the doc asserts a runner image version, tool version, or install outcome that is not attributed to a named artifact; where a figure is unobserved the doc says so.
    - `git status --porcelain` shows no modification to `CLAUDE.md` or `.planning/PROJECT.md`.
  </acceptance_criteria>
  <verify>
    <automated>R="$(git rev-parse --show-toplevel)" && cd "$R" && D=docs/phase58-declaration-provenance.md; test -f "$D" || { echo "DOC_MISSING"; exit 1; }; printf 'frontmatter='; head -1 "$D"; printf 'sections='; grep -ac '^## ' "$D"; printf 'named_sources='; for s in 'README.md:96' 'REQUIREMENTS.md:88' 'host-tool.mts' 'ci.yml' 'installer/package.json'; do grep -aq "$s" "$D" && printf 'ok ' || printf "MISSING:%s " "$s"; done; echo</automated>
    <fails_when>the output contains DOC_MISSING, or sections is below 6, or the named_sources line contains any MISSING: entry, or the frontmatter line is not three dashes</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -aoE '[A-Za-z0-9_./-]+[.](md|mts|ts|mjs|json|sh|yml):[0-9]+' docs/phase58-declaration-provenance.md > /tmp/58-02-cites-raw.txt; sed -E 's/:[0-9]+$//' /tmp/58-02-cites-raw.txt | sort -u > /tmp/58-02-cites.txt; printf 'cited='; wc -l < /tmp/58-02-cites.txt; U=0; while read -r c; do [ -e "$c" ] || { echo "MISSING_PATH:$c"; U=1; }; done < /tmp/58-02-cites.txt; echo "unresolved_flag=$U"</automated>
    <fails_when>cited is 0, or any MISSING_PATH line is printed, or unresolved_flag is 1</fails_when>
    <automated>R="$(git rev-parse --show-toplevel)" && cd "$R" && git status --porcelain -- CLAUDE.md .planning/PROJECT.md src/mcp/vice/prerequisites.json src/mcp/vice/resources > /tmp/58-02-t1-untouchable.txt; echo "GIT_STATUS_EXIT=$?"; printf 'untouchable_modified='; grep -ac . /tmp/58-02-t1-untouchable.txt || true</automated>
    <fails_when>GIT_STATUS_EXIT is not 0, or untouchable_modified is not 0</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e '
const fs=require("fs");
const doc=fs.readFileSync("docs/phase58-declaration-provenance.md","utf8");
const d=JSON.parse(fs.readFileSync("src/mcp/vice/prerequisites.json","utf8"));
const urls=new Set();
for(const r of Object.values(d.tools)){ const rem=r.remedies?r.remedies:{}; for(const arr of Object.values(rem)) for(const e of arr) if(e.provenance==="authored") urls.add(e.source); }
console.log("authored_sources="+urls.size);
console.log("undocumented_authored="+JSON.stringify([...urls].filter(u=>!doc.includes(u))));
console.log("tools_unmentioned="+JSON.stringify(Object.keys(d.tools).filter(id=>!doc.includes(id))));
'; echo "EXIT=$?"</automated>
    <fails_when>non-zero EXIT, or undocumented_authored is not [], or tools_unmentioned is not []</fails_when>
  </verify>
  <done>Every case where two places in this tree said different things about the same fact is
  written down with both sides quoted, the winner named and the reason given. Every citation
  resolves. Every authored remedy in the declaration has its upstream page named here. Nothing
  outside this one new file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the README claim the project has since measured false</name>
  <files>README.md</files>
  <read_first>
    - `README.md:88-126` — the whole install section, read before editing anything in it. Three parts matter and only one of them is in scope: the dated lead-in and the eight-row table (lines 94-108) are generated in Phase 62 and are out of scope; the MSYS2 and Flatpak notes (110-115) are untouched; the section at 117-123 is the one this task rewrites.
    - `.planning/REQUIREMENTS.md:85-90` — the Out of Scope row that is the measured evidence for the correction, including the reason naming the text channel.
    - `docs/phase58-declaration-provenance.md` — Task 1's first worked case. The README correction and that case must state the same fact; write the README prose against what the doc actually says rather than re-deriving it.
    - `src/mcp/vice/prerequisites.json` — confirm for yourself that the declaration carries no VICE version data before the README asserts it carries none.
  </read_first>
  <action>
Rewrite the section at `README.md:117-123`, heading included. The current heading frames the
question as what an older VICE costs, which presupposes it costs something; the current body
then asserts a specific degradation as a consequence of the version. This project has since
recorded the opposite, and nothing regenerates this section, so it stays false unless this phase
owns it.

The replacement states, with its evidence cited: that no shipped tool in this project refuses on
a VICE version; that the capability the old framing hung on is served over the text channel
regardless of version; that the version floor the old framing named binds an opcode no shipped
tool calls; and that the prerequisite declaration consequently carries no VICE version data of
any kind — not a floor and not a dated observation. Cite `.planning/REQUIREMENTS.md` for the
measured claim and name `src/mcp/vice/prerequisites.json` for the second. Keep the section short
and keep it in the README's existing register. Choose a heading that does not presuppose a cost.

Do not touch anything above line 116 or below line 124. The dated lead-in at 94-97 and the
eight-row table at 99-108 are Phase 62's generated section — a hand-edit there is overwritten,
and for one milestone it would create a third description of the same thing. The MSYS2 and
Flatpak notes are unrelated and stay exactly as they are.

Do not edit `CLAUDE.md` or `.planning/PROJECT.md`, even though the former's ACME-prefix citation
is stale and this phase's provenance doc says so. That correction is explicitly deferred and
needs to touch both files at once.
  </action>
  <acceptance_criteria>
    - The eight-row install table is byte-identical to its state at HEAD: extracting from the table header row through the final ecosystem row, before and after, produces no diff.
    - The dated lead-in sentence at `README.md:94-97` is unchanged.
    - In the region between the final table row and the "Capabilities with no route on stock" heading, the approximation claim the old prose made no longer appears.
    - That same region contains the name of the text-channel command that serves the capability, and the phrase naming the text channel.
    - That same region cites `REQUIREMENTS.md` and names `src/mcp/vice/prerequisites.json`.
    - The rewritten heading does not presuppose that an older VICE costs anything.
    - `git diff HEAD -- README.md` shows changed lines only within the rewritten section; no added or removed line begins with a markdown table pipe.
    - `git status --porcelain` shows no modification to `CLAUDE.md`, `.planning/PROJECT.md`, or any file under `src/mcp/vice/`.
  </acceptance_criteria>
  <verify>
    <automated>R="$(git rev-parse --show-toplevel)" && cd "$R" && git show HEAD:README.md > /tmp/58-02-readme-head.md; echo "GIT_SHOW_EXIT=$?"; wc -l < /tmp/58-02-readme-head.md</automated>
    <fails_when>GIT_SHOW_EXIT is not 0, or the line count is 0</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)" && sed -n '/^| Ecosystem |/,/^| Official Windows/p' /tmp/58-02-readme-head.md > /tmp/58-02-table-before.txt; sed -n '/^| Ecosystem |/,/^| Official Windows/p' README.md > /tmp/58-02-table-after.txt; printf 'before_rows='; wc -l < /tmp/58-02-table-before.txt; printf 'after_rows='; wc -l < /tmp/58-02-table-after.txt; diff /tmp/58-02-table-before.txt /tmp/58-02-table-after.txt; echo "TABLE_DIFF_EXIT=$?"</automated>
    <fails_when>TABLE_DIFF_EXIT is not 0, or before_rows is 0, or after_rows differs from before_rows</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)" && sed -n '/^| Official Windows downloads/,/^### Capabilities with no route on stock/p' README.md > /tmp/58-02-region.txt; printf 'region_lines='; wc -l < /tmp/58-02-region.txt; printf 'stale_approximation='; grep -ac 'within-one-frame' /tmp/58-02-region.txt || true; printf 'text_channel='; grep -ac 'text channel' /tmp/58-02-region.txt || true; printf 'chis='; grep -ac 'chis' /tmp/58-02-region.txt || true; printf 'requirements_cite='; grep -ac 'REQUIREMENTS.md' /tmp/58-02-region.txt || true; printf 'declaration_named='; grep -ac 'prerequisites.json' /tmp/58-02-region.txt || true</automated>
    <fails_when>region_lines is below 5, or stale_approximation is not 0, or text_channel is 0, or chis is 0, or requirements_cite is 0, or declaration_named is 0</fails_when>
    <automated>R="$(git rev-parse --show-toplevel)" && cd "$R" && git diff HEAD -- README.md > /tmp/58-02-readme.diff; echo "GIT_DIFF_EXIT=$?"; git status --porcelain -- CLAUDE.md .planning/PROJECT.md src/mcp/vice > /tmp/58-02-untouchable.txt; echo "GIT_STATUS_EXIT=$?"</automated>
    <fails_when>GIT_DIFF_EXIT is not 0, or GIT_STATUS_EXIT is not 0</fails_when>
    <automated>printf 'table_rows_touched='; grep -acE '^[+-][|] ' /tmp/58-02-readme.diff || true; printf 'lines_changed='; grep -acE '^[+-][^+-]' /tmp/58-02-readme.diff || true; printf 'other_files_touched='; grep -ac . /tmp/58-02-untouchable.txt || true; echo '--- changed lines ---'; grep -aE '^[+-][^+-]' /tmp/58-02-readme.diff | sed -n '1,60p'</automated>
    <fails_when>table_rows_touched is not 0, or lines_changed is 0, or other_files_touched is not 0, or the changed-lines listing contains the dated lead-in sentence about checking ecosystems live</fails_when>
    <human-check>Read the rewritten section against `.planning/REQUIREMENTS.md:85-90` and against the provenance doc's first worked case. Confirm all three say the same thing, that the new heading does not presuppose a cost, and that nothing in the new prose is a figure nobody measured.</human-check>
  </verify>
  <done>The README no longer asserts a consequence this project has measured false. It states
  that no shipped tool carries a VICE version dependency, cites the evidence, and names the
  declaration as carrying no version data. The generated table above it is byte-unchanged and
  Phase 62 will still own it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository claim → reader's decision | README prose and a docs file are what a user and a future agent act on. A false claim here is acted on with no signal that it is false, because nothing tests prose. |
| Phase 58 hand-edit → Phase 62 generated output | The install table is hand-maintained today and generated later. An edit made now to a region that will be regenerated is silently discarded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-58-05 | Spoofing | `docs/phase58-declaration-provenance.md` citations | medium | mitigate | A citation that does not resolve makes an unsourced claim look sourced, which defeats the entire provenance grading the JSON carries. Mitigation: every `path:line` in the doc is extracted and its path part resolved against the filesystem; any unresolved path fails the task. Line numbers are deliberately not pinned, because a line-pinned assertion is brittle and gets disabled rather than maintained. |
| T-58-06 | Tampering | `README.md:99-108` | medium | mitigate | An edit to the section Phase 62 generates is silently discarded there, leaving a third description of the same remedies alive for one milestone. Mitigation: the table region is extracted from HEAD and from the working tree and diffed; any difference fails the task, and a second gate counts changed markdown table rows in the diff and requires zero. |
| T-58-07 | Repudiation | the rewritten README prose | medium | mitigate | Replacing one unsourced assertion with another reproduces the defect while appearing to fix it, and leaves nothing a later reader can check the claim against. Mitigation: the rewritten region must cite `REQUIREMENTS.md` and name the declaration, both asserted by grep, and a human-check confirms the three statements of the same fact agree. |
| T-58-08 | Information Disclosure | none applicable | low | accept | Neither deliverable handles input, secrets, credentials or user data. No authentication, session, access-control or cryptography surface is touched — recorded rather than omitted, per `58-RESEARCH.md` § Security Domain, which marks V2, V3, V4 and V6 as not applicable to this phase. |
| T-58-SC | Tampering | npm/pip/cargo installs | low | accept | No package is added to any dependency list and no install task exists in this plan. The Package Legitimacy Gate has nothing to check, recorded as not applicable in `58-RESEARCH.md` rather than skipped silently. |
</threat_model>

<assumption_delta_decision>
Recorded in full in `58-01-PLAN.md`. Summary, because this plan carries the human-readable half
of the same transition: the generalization is `chosen` — a remedy string that used to be derived
from whichever of four scattered places a reader reached is now chosen by one declaration record.
The primary noun is the declaration record; the scattered literals are demoted to cited sources.
The decision is **add-alongside** for exactly one milestone, because `DECL-03` (Phase 60) and
`GEN-01`/`GEN-02` (Phase 62) own the promote. This plan is where that debt is made visible to a
human rather than only to a test: the provenance doc states which source won each disagreement,
and the README correction removes the one place where the losing side is still being asserted to
a user.
</assumption_delta_decision>

## Flagged Planner Assumptions

The spec-less edge probe's seven items are accounted for in `58-01-PLAN.md`: four authored into
`must_haves.truths` there with explicit acceptance criteria, three surfaced there as unresolved
flagged assumptions (DECL-02, DECL-04, DECL-05, all `category: unclassified`). None is repeated
here and none is dropped.

One assumption is specific to this plan: **the provenance doc's citations are checked for path
resolution, not for honesty.** A citation can point at a real file and still not say what the doc
claims it says. `58-VALIDATION.md` already records that as a manual verification — provenance is
a human judgement about whether a quoted source supports the claim, and no assertion can check
that. The human-check in Task 2 covers the README correction; the doc as a whole is covered by
the phase-level manual verification.

## Artifacts this phase produces

New files:
- `docs/phase58-declaration-provenance.md`

New identifiers and section names in `docs/phase58-declaration-provenance.md`:
- frontmatter keys `phase`, `decision`, `date`, `decision_ids`, `related_decisions`
- frontmatter value `prerequisite-declaration-provenance`
- one second-level section per worked case: the VICE version gate, the shared c1541/petcat
  remedy, the scope of the measured grade, the two Node floors, one ecosystem under two
  platforms, the authoring decisions, and the ACME library probe prefixes

Modified regions:
- `README.md` — the section between the install table and the "Capabilities with no route on
  stock" heading is replaced, heading included. The table at `README.md:99-108` and the dated
  lead-in at `README.md:94-97` are byte-unchanged.

No new exported function, JSON field, CLI flag, CI job or test case is created by this plan.

<verification>
- `docs/phase58-declaration-provenance.md` exists with frontmatter and at least six worked-case sections.
- Every `path:line` citation in the provenance doc resolves to a file that exists.
- Every `authored` remedy source in `src/mcp/vice/prerequisites.json` is named in the doc, and every tool id in the declaration is mentioned in the doc.
- The README install table is byte-identical to HEAD; `git diff HEAD -- README.md` contains no added or removed markdown table row.
- The rewritten README region no longer carries the old approximation claim and does cite `REQUIREMENTS.md` and name `src/mcp/vice/prerequisites.json`.
- `CLAUDE.md`, `.planning/PROJECT.md` and everything under `src/mcp/vice/` are unmodified by this plan.
</verification>

<success_criteria>
- Every disagreement between two in-tree sources that the declaration had to settle is written down with both sides quoted, the winner named and the reason given.
- Every authoring choice left open before this phase — schema version, dxa's remedy shape, guard cadence, markdown formatting, ordering, absent remedies — is recorded with its reasoning.
- The README no longer asserts a consequence of an older VICE that this project has measured false, and cites what it was measured against.
- The section Phase 62 generates is byte-untouched.
</success_criteria>

<output>
Create `.planning/phases/58-one-declaration-four-places-that-can-no-longer-disagree/58-02-SUMMARY.md` when done.
</output>