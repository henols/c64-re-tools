---
phase: 27-shared-seams-extracted
plan: 05
type: execute
wave: 3
depends_on: [27-01, 27-02, 27-03, 27-04]
files_modified:
  - src/mcp/vice/module-classification.ts
  - src/mcp/vice/module-classification.test.ts
autonomous: true
requirements: [SEAM-02]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every module and data file matching the registry's declared in-enumeration scope on disk has a registry entry; a module added later with no entry makes module-classification.test.ts FAIL, naming the unclassified file (SEAM-02 probe: empty — and the same loop is what stops the record going stale after later phases rearrange the tree)"
    - "The non-vacuity guard is a DERIVED relation, never a pinned total: the enforcing test asserts the on-disk in-enumeration count is at least the number of in-enumeration registry entries, so a broken or empty glob fails loudly while a correct tree that legitimately gains or loses a module stays green (SEAM-02 probe: boundary, one step either side)"
    - "No entry's basis rests on the name prefix: the enforcing test rejects any basis text naming a prefix as a justification, and every cited consumer path is asserted to exist on disk — which is what makes 'no classification cites a name prefix' checkable rather than promised"
    - "Two entries can never name the same module, and every out-of-enumeration entry is excluded from the disk-completeness loop rather than colliding with it (SEAM-02 probe: adjacency)"
    - "Registry module values and on-disk filenames are compared as exact strings including the extension, so the generated data file is matched by its full filename and never by a stem (SEAM-02 probe: encoding)"
    - "Every assertion is order-independent: entries are looked up by module key, never by array index, proven by re-running the same predicates over a reversed copy of the registry (SEAM-02 probe: ordering)"
    - "consumers[].line is optional and advisory; where present, the enforcing test asserts the cited line of the cited file contains the cited symbol rather than trusting the number, so a drift is reported instead of silently misleading a later phase (SEAM-02 probe: precision)"
    - "The full-glob suite is green with zero anno modules deleted, evidenced by the named command, the stated broker state, the pass/fail/skip counts, and a deletion query returning zero"
  prohibitions:
    - "MUST NOT justify any module's capability-or-glue verdict by its name prefix, and MUST NOT leave an in-scope module, a deliberate scope exclusion, or a contested verdict unrecorded. A later phase acts on this record under deletion pressure, and to that reader an unstated exclusion is indistinguishable from an oversight."
    - "MUST NOT claim the phase's green run from the narrowed automated gate, or from a host with a live emulator broker, or state the result without naming the exact command, the broker state and the pass/fail/skip counts. A green that silently omits nine test files, or a red that cannot be attributed to a known environmental condition, is a claim the reader cannot check."
  artifacts:
    - path: "src/mcp/vice/module-classification.ts"
      provides: "The committed capability-or-glue classification for every anno-named module, with its basis and its declared scope exclusions"
      exports: ["ModuleVerdict", "ModuleClassificationEntry", "MODULE_CLASSIFICATION", "classificationFor"]
      min_lines: 180
    - path: "src/mcp/vice/module-classification.test.ts"
      provides: "The enforcing guard: completeness, no orphans, basis integrity, the prefix prohibition, verdict/extractables coherence, a derived non-vacuity relation, and a planted violation"
      min_lines: 130
  key_links:
    - from: "src/mcp/vice/module-classification.test.ts"
      to: "src/mcp/vice/module-classification.ts"
      via: "named import of the registry const, then relation assertions in both directions against a disk enumeration"
      pattern: 'from "\./module-classification\.ts"'
    - from: "src/mcp/vice/module-classification.ts"
      to: "src/mcp/vice/"
      via: "every entry's cited consumer path, asserted to exist on disk by the enforcing test"
      pattern: 'consumers'
---

<objective>
Record a committed capability-or-glue classification for every `anno`-named
module, derived from what each module does rather than from its name, and make it
impossible for a module to go unclassified — then close the phase by proving the
whole extraction was a move rather than a change (SEAM-02, D-06..D-09, and
criterion 4).

Purpose: a prefix-driven deletion in a later phase needs a record it can act on,
and the record is only trustworthy if it exists **before** any deletion. A prose
document goes stale with nothing failing; a data file with an enumerating test
fails the moment a module slips in unclassified.
Output: a typed registry, its enforcing guard, and the phase's green-run evidence.
Zero `anno` modules deleted.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-shared-seams-extracted/27-CONTEXT.md
@.planning/phases/27-shared-seams-extracted/27-RESEARCH.md
@.planning/phases/27-shared-seams-extracted/27-PATTERNS.md
@.planning/phases/27-shared-seams-extracted/27-01-SUMMARY.md
@.planning/phases/27-shared-seams-extracted/27-02-SUMMARY.md
@.planning/phases/27-shared-seams-extracted/27-03-SUMMARY.md
@.planning/phases/27-shared-seams-extracted/27-04-SUMMARY.md
</context>

<research_corrections_applied>
RESEARCH.md overrides CONTEXT.md on every matter of fact. What binds this plan,
re-verified against the tree at `6c1f569` while planning:

- **This plan is LAST on purpose, and its entries must cite the post-extraction
  tree.** The three preceding plans change the facts every entry rests on: the
  gate module's consumer list changes once the ACME half leaves; the census gains
  a named store boundary; and the project builder's extractable obligation is
  discharged into a new shipped module. A registry written first would be stale
  inside its own phase — the one thing criterion 2 says must not happen.
- **The discriminator D-07's wording invites getting wrong.** "Has a
  non-`anno`-prefixed consumer" is **not** the test. Measured: three glue modules
  have prominent non-`anno` consumers (the tool surface and the session closer are
  imported by the stdio entry point at `vice-proxy.ts:194` and `:200`, the CLI at
  `:309` plus four `scripts/` files), and **two** capability modules have no
  non-`anno` consumer at all. Read D-07 as: **a consumer that survives the
  substrate swap, or a requirement id.** The entry point survives; *its imports of
  the glue modules do not*.
- **Registry name must NOT match its own glob.** Naming it after the prefix would
  make the enumerating test demand an entry for the registry itself. It also must
  not be `capability-registry.*`, which is taken by the per-backend delta.
  Resolved: `module-classification.ts`, re-verified free while planning.
- **Format resolved: a TypeScript module with a typed const**, not JSON. The
  directory is covered by a `strict` tsconfig with `noEmit`, so a typed const gets
  shape-checking from the typecheck script for free — a second, independent guard
  against a malformed entry at zero cost. A JSON file would need the enforcing
  test to hand-validate every field.
- **Scope resolved: the sixteen non-test modules plus the generated data file;
  test files excluded; two `scripts/lib/` files carried as data with an explicit
  out-of-enumeration marker (Open Question OQ-4, resolved here).** Counts
  re-measured on disk while planning: sixteen `anno-*.ts` non-test modules, one
  `anno-*.json` data file (which is in the shipped `files[]`), nineteen
  `anno-*.test.ts` files, and two tracked `scripts/lib/` files. The data file is
  included **deliberately** rather than filtered out incidentally, because it
  matches the glob and an unfiltered enumeration would demand an entry for it
  regardless of intent. Test files are excluded because a test file's fate follows
  its module's, and a separate committed drift guard already ensures every
  on-disk test file lands in exactly one of the automated or manual sets, so none
  can go unaccounted for. The two `scripts/lib/` files are named explicitly by
  `CUT-04` as guards with a fate to record, so they are carried as **data** with an
  out-of-enumeration marker while the test's *enumeration* stays inside the module
  directory — cheap, honest, and it means a later phase reading the registry finds
  them. A third name a reader will expect and the glob does not match
  (`docs-absorbed-decisions.test.ts`, which begins `docs-`) is recorded in the same
  exclusion list.
- **OQ-2 resolved: record the verify module as `capability`, with its basis
  written as the discipline rather than the route.** The tension is real and must
  not be smoothed over: criterion 2 names that module among the ten provably not
  deletable by prefix, while `EXPORT-01` states verbatim that the existing
  `--verify` seam "invokes the external analyser and parses ITS transcript. Only the
  discipline survives." Criterion 2 is the phase's own binding text and a registry
  should not overrule it — so the verdict is `capability`, and the basis cites the
  surviving discipline (`acmeVerdict()`'s never-trust-the-exit-code parse, at
  `anno-verify.ts:116`, described at `anno-cli.ts:83` and `:623` as deriving
  **only** from the parsed result line) with requirement ids `EXPORT-01` and
  `EXPORT-03`. **Flag the tension in the entry itself** so a later phase reads a
  basis it can act on rather than a verdict it will contest.
- **The `glue-with-extractable` verdict's one instance is discharged by `27-03`.**
  The project builder held three pure, prefix-free functions; all three moved out.
  Its verdict is therefore plain `glue` at the time this registry is written, and
  the third verdict may end this phase with zero instances. That is a correct
  outcome, not a vacuous one — but it must be **written down** in the header, and
  each remaining glue module must be **surveyed** for extractables rather than
  assumed to have none.
- **`consumers[].line` is a measured drift liability.** RESEARCH's own audit found
  two of CONTEXT's citations had drifted by two lines. Cite path plus symbol; make
  the line optional; and where present, assert the cited line **contains** the
  cited symbol rather than pinning the number — the in-repo precedent for that
  check is `docs-linerefs.test.ts:66-83`.
- **The standing hazard, and how this plan answers it better than a literal
  floor.** CONTEXT.md forbids a pinned total, and this project's scar is a guard
  that pinned per-milestone totals and goes red on a correct tree. The established
  in-repo analog uses a hard-coded `>=` floor with a "RAISED, never lowered"
  comment. That pattern is right about relations-over-equality but a literal
  growing floor would go red in a later phase when modules are legitimately
  deleted. **This plan therefore derives the floor from the registry itself** —
  the on-disk in-enumeration count must be at least the number of in-enumeration
  entries — which catches a broken or empty glob and self-adjusts as a later phase
  removes a module together with its entry. Record that reasoning beside the
  assertion, and cite the analog it supersedes.
</research_corrections_applied>

<assumption_delta_decision>
**Scope of this plan: `no-change`.**

The detector fired for Phase 27 on three prose signals. Within this plan's scope
nothing is generalised: the registry records a judgement about files that already
exist, and introduces no identity, key or vocabulary that anything else consumes
at runtime. The nearest candidate — the three-valued verdict union — is a
*classification* vocabulary invented here for the first time, not a singular
assumption losing its monopoly.

The phase's one genuine identity-model transition (the block-class vocabulary) is
resolved as **promote** in `27-02-PLAN.md`.
</assumption_delta_decision>

<probe_accounting>
Spec-less probe fallback (no SPEC.md exists for this phase). The deterministic
edge probe surfaced **12** applicable edges across SEAM-01/02/03, all
`unresolved`, with resolution deferred to the planner.

**This plan owns the 6 SEAM-02 rows.** All six resolved `explicit`:

| Probe row | Resolution | Authored as |
|---|---|---|
| SEAM-02 / empty ("result for empty, single-element, or null input") | resolved, explicit — read as "what does the enforcing test do when the enumeration glob matches nothing, and what happens to a module with no entry" | `must_haves.truths` rows 1 and 2 |
| SEAM-02 / boundary ("what happens exactly at each threshold, and one step either side") | resolved, explicit — the derived relation between the on-disk count and the entry count; one module more on disk than entries fails completeness, one entry more than files on disk fails no-orphans, and a glob returning fewer than the entry count fails the relation | `must_haves.truths` row 2 |
| SEAM-02 / adjacency ("when two things are exactly equal or just touch, do they merge, collide, or separate") | resolved, explicit — two entries naming the same module, and an out-of-enumeration entry meeting the disk-completeness loop | `must_haves.truths` row 4 |
| SEAM-02 / encoding ("whose definition of length/equality applies") | resolved, explicit — exact string comparison including the file extension, so the generated data file is matched by full filename and never by a stem | `must_haves.truths` row 5 |
| SEAM-02 / ordering ("when elements compare equal, is output order specified and stable") | resolved, explicit — every assertion looks entries up by module key, proven by re-running the same predicates over a reversed copy | `must_haves.truths` row 6 |
| SEAM-02 / precision ("where can precision loss or tie-breaking occur") | resolved, explicit — the only numeric field is the advisory line citation; it is optional and, where present, is verified by containment rather than trusted | `must_haves.truths` row 7 |

That is **six** rows, and the count matters: the coverage report lists `boundary`,
`adjacency`, `empty`, `encoding`, `ordering` and `precision` under SEAM-02.
Re-counted from the report while planning, the split across the 12 rows is
SEAM-01 **2**, SEAM-02 **6**, SEAM-03 **4** — not the even 2 / 5 / 5 an eye-count
suggests. `27-02-PLAN.md` authors the four SEAM-03 rows (plus one goal-backward
truth it marks as not a probe row), and `27-01-PLAN.md` the two SEAM-01 rows, so
the phase-level equality holds: **12 probe rows in → 12 authored, 0 flagged, 0
dropped.** Recorded here rather than silently rounded, because a miscount is
exactly the kind of silent drop the equality check exists to catch.

**Prohibition recall (§B, in-prompt).** Stage 1 asked, of SEAM-02 and of
criterion 4: *what could this silently become that the author would not want, but
the requirement does not forbid?* Raw candidates included several routine-
engineering items (must not throw on an empty registry, must not mutate the
array, must not pin a total) which Stage 2 dropped as owned by the edge probe and
by this plan's own truths. **Two kept**, both transparency/integrity-of-the-record
class, authored **descriptor-less** under `must_haves.prohibitions`: the record
must not be justified by the very prefix it exists to stop trusting nor leave an
exclusion or a contested verdict unstated; and the phase's green run must not be
claimed from the narrowed gate, from a live-broker host, or without naming the
command, the broker state and the counts.

Canon-referral breadcrumbs, dropped not minted: supply-chain integrity of the
published tarball and generic secure-configuration concerns are canon — covered
by `/gsd-secure-phase` and by the tarball validator that `27-02` and `27-03`
already run; not minted here.
</probe_accounting>

<artifacts_this_phase_produces>
| New path | Symbols | New name? |
|---|---|---|
| `src/mcp/vice/module-classification.ts` | `ModuleVerdict` (type), `ModuleClassificationEntry` (interface), `MODULE_CLASSIFICATION` (const), `classificationFor` (function) | **Yes** — all genuinely new |
| `src/mcp/vice/module-classification.test.ts` | new test titles only | new file |

No new CLI flag, env var, tool name, wire name, file format or stored record.
`package.json` is not modified by this plan — the registry is bookkeeping, not
runtime, and must stay out of the shipped `files[]`.

**Phase-wide artifact list, for the record** (each new path, with whether the
symbol names are new or moved): `acme-gate.ts` and `acme-gate.test.ts` (`27-01`,
five symbols moved with byte-identical names); `block-class.ts` and
`block-class.test.ts` (`27-02`, three new names plus one renamed moved type);
`prg-image.ts` and `prg-image.test.ts` (`27-03`, three symbols moved with
byte-identical names); `shipped-modules.ts` and `shipped-modules.test.ts`
(`27-04`, two symbols moved with byte-identical names); and this plan's two files.
`package.json` `files[]` gains exactly two entries across the phase —
`block-class.ts` and `prg-image.ts` — and gains none of the three test-only
modules.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write the classification registry against the post-extraction tree</name>
  <precondition>The four preceding plans are committed, so the tree this registry cites is the post-extraction one: `src/mcp/vice/acme-gate.ts`, `block-class.ts`, `prg-image.ts` and `shipped-modules.ts` all exist on disk, and `cd src/mcp/vice && npm run typecheck` exits 0.</precondition>
  <reversibility rating="costly">A later phase's deletion decisions read this file instead of re-deriving each verdict under deletion pressure, so replacing it later means re-establishing trust in a record written after deletions began — which is the one thing criterion 2 says cannot be done.</reversibility>
  <files>src/mcp/vice/module-classification.ts</files>
  <read_first>
    - `src/mcp/vice/capability-registry.ts:1-90` — the exemplar: a long header with `WHY THIS FILE EXISTS`, `WHAT NOT TO DO` and an `EXCLUDED, DELIBERATELY` section, then an exported union type, an exported entry interface with per-field doc comments, and an exported `readonly Entry[]` const with `// --- group ---` section banners.
    - `src/mcp/vice/fork-deleted-tools.ts` — the whole file (21 lines): the shortest complete instance of the same shape, and the canonical statement of why such a module is a plain `.ts` and never a `.test.ts`.
    - `.planning/phases/27-shared-seams-extracted/27-RESEARCH.md` → `## SEAM-02 Ground Truth` → the per-module consumer map with file:line citations and the requirement ids each module serves. **This is the raw material for every entry's basis.** Read the `†` convention: a marked consumer is itself prefix-named and does not survive the substrate swap.
    - `.planning/phases/27-shared-seams-extracted/27-RESEARCH.md` → `## Registry Scope Gaps` and `## Glue-vs-Capability Discriminator`.
    - `.planning/REQUIREMENTS.md:68-70` — SEAM-01/02/03 verbatim; `:100-104` — the ACME-export requirements whose text creates the OQ-2 tension; `:127-133` — `CUT-04`, which names the two `scripts/lib/` files.
    - `.planning/ROADMAP.md` → Phase 27 → success criterion 2, which names the ten capability modules explicitly.
    - `src/mcp/vice/anno-verify.ts:25-35` and `:110-125`, plus `src/mcp/vice/anno-cli.ts:80-90` and `:618-630` — the surviving-discipline basis for the contested verdict.
  </read_first>
  <action>
Create `src/mcp/vice/module-classification.ts`: the committed record of which
prefix-named modules are capabilities and which are glue, with each verdict's
basis stated in citable terms.

**Name.** `module-classification.ts`, confirmed free. It must not carry the
prefix in its own name — the enumerating test walks that glob and would demand an
entry for the registry itself — and must not reuse the per-backend delta
registry's name.

**Shape.** Copy `capability-registry.ts`'s structure exactly: an exported
`ModuleVerdict` union of the three verdicts (capability, glue,
glue-with-extractable); an exported `ModuleClassificationEntry` interface whose
every field carries a doc comment stating what it must and must not contain; an
exported `MODULE_CLASSIFICATION` typed as `readonly ModuleClassificationEntry[]`
with `// --- group ---` section banners; and a small exported `classificationFor`
lookup by module name so the enforcing test and any later reader use one accessor
rather than re-deriving a find.

**Entry fields.** `module` — the exact filename including its extension, because
the completeness loop compares exact strings and one entry is a `.json` data
file. `verdict`. `basis` — an object holding `consumers` (each a `path` plus the
`symbol` imported, with `line` **optional and advisory**) and `requirements` (the
requirement ids the module serves). `extractables` — the specific prefix-free
symbols that must move out before the module may be deleted, non-empty **only**
for the third verdict. `scope` — either in-enumeration or out-of-enumeration, so
the two `scripts/lib/` entries are carried as data without the test reaching
outside the module directory. And a `note` for anything a later reader must know,
including a contested verdict.

**The discriminator, written into the interface's own doc comments (D-07).**
Each entry's basis is surviving consumers plus requirement ids — never the name
prefix, which is the whole of D-07 and the checkable half of criterion 2.
Capability
means the module has a consumer that survives the substrate swap **or** a
requirement it implements. It does **not** mean "has a consumer whose name lacks
the prefix": three glue modules have prominent unprefixed consumers whose *imports
of them* do not survive, and two capability modules have no unprefixed consumer at
all and rest on a requirement id instead. Say both of those out loud in the doc
comment, with the concrete file:line citations, because that is the trap the
requirement's own wording sets.

**Entries.** One per in-enumeration item — the sixteen non-test modules and the
generated data file — plus the two out-of-enumeration `scripts/lib/` files.
Populate every basis from RESEARCH's consumer map, **re-checked against the
post-extraction tree**, and cite path plus symbol. Grouped:

*Capability.* The ten modules criterion 2 names. (The verdict union carries all
three values per D-08 — capability, glue, and the third one naming the symbols
that must move out before deletion.) Their bases, each from the
consumer map rather than invented: the ACME-identifier module rests on the
typed-label-prefix requirement, its only unprefixed-surviving basis; the
confidence module supplies the grade tokens the census reads before falling
through to block class; the census itself rests on the two coverage requirements
and now on its named store boundary; the `.d64` reader is pure C64 format
knowledge with no analyser dependency; the enum generator on its export lineage;
the memory-map renderer on skill-facing rendering; the register-bit generator on
the fact that it produces the shipped data file; the symbol module on the two ✓
Validated round-trip requirements it *implements*; and the gate module on
SEAM-01, with a note recording that its ACME half now lives in `acme-gate.ts`
and that ten importers remain. The tenth is the contested one below.

*The contested verdict, flagged rather than smoothed.* Record the verify module as
`capability`, and write its basis as the **discipline**, not the route: the
never-trust-the-exit-code parse at `anno-verify.ts:116`, described at
`anno-cli.ts:83` and `:623` as deriving only from the parsed result line, with
requirement ids from the ACME-export family. Then use the `note` field to state
the tension explicitly: the export requirement says the existing seam invokes the
analyser and parses **its** transcript, so only the discipline survives, while
criterion 2 names this module as not deletable by prefix. A later phase must read
a basis it can act on, not a verdict it will argue with.

*Glue.* The six remaining modules: the CLI, the launcher, the MCP client, the
project builder, the session lifecycle and the tool surface. Each basis states
what makes it glue in terms of what it does — spawning the analyser binary,
speaking its protocol, building its project file, owning its session, exposing its
tool surface — and names the surviving-consumer question honestly where an
unprefixed consumer exists but its import does not survive.

*The project builder specifically.* Its verdict is plain `glue` **now**, because
`27-03` moved all three of its prefix-free functions into `prg-image.ts`. Record
that discharge in its `note`, naming the new module, so a reader does not conclude
the obligation was overlooked. Then **survey each of the other five glue modules
for remaining extractables** — a pure, prefix-free symbol with a consumer that
survives — and record the survey result in the header. If any is found, its
verdict becomes the third one and its `extractables` names the symbols. Do not
assume the survey finds nothing; run it and write down what it found.

**The header.** Three-part single-seam shape plus, critically, an
`EXCLUDED, DELIBERATELY` section stating every scope decision, because an unstated
exclusion is the failure this record exists to stop: the nineteen prefix-named
test files (a test file's fate follows its module's, and a separate committed
drift guard already ensures every on-disk test file lands in exactly one of the
automated or manual sets); `docs-absorbed-decisions.test.ts`, which a reader will
expect to be covered and which the glob does not match because its name begins
differently; and the two `scripts/lib/` files, which are carried as **data** with
the out-of-enumeration marker while the test's enumeration stays inside the module
directory. Also state in the header: that the third verdict may currently have
zero instances and why; the survey result for the glue modules; that `line`
citations are advisory because a line-number audit of this phase's own source
documents found two drifted by two; and that no basis may cite the name prefix as
a justification, with the enforcing test named as the mechanism.

No `[ASSUMED]` label. No `hostpath.ts` import. No phase number inside a string
literal, and check the phase-pointer guard's pattern families before writing any
"recorded in" sentence. This module is a plain `.ts`, never a `.test.ts`, for the
recorded reason that importing a test module for its exports re-runs every test it
registers.
  </action>
  <verify>
    <automated>cd src/mcp/vice && npm run typecheck && node --test docs-dangling-refs.test.ts comment-phase-pointers.test.ts hop-chain-comments.test.ts assumption-label-discipline.test.ts hostpath-consumers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^export (type|interface|const|function) (ModuleVerdict|ModuleClassificationEntry|MODULE_CLASSIFICATION|classificationFor)' src/mcp/vice/module-classification.ts` returns `4`
    - `grep -c 'glue-with-extractable' src/mcp/vice/module-classification.ts` returns at least `2` — the third verdict is in the union and its current-instance status is stated in the header
    - `grep -c 'module: "' src/mcp/vice/module-classification.ts` returns **at least** `19` — the sixteen non-test `anno-*.ts` modules, the generated data file, and the two out-of-enumeration `scripts/lib/` files. Stated as a **floor**, matching its siblings in this block (`>= 2`, `>= 3`), for two reasons: `grep -c` counts matching *lines* rather than entries, so an exact `19` is brittle against formatting alone; and the exact total is Task 2's enforcing test's job, where it is **derived** from the on-disk glob rather than pinned (a pinned `assert.equal(x.length, N)` is forbidden there by name — the `audit-integrity.test.ts` scar). The floor is still a real check: fewer than 19 entries means a module has no verdict, which is the failure this criterion exists to catch, and Task 2's no-orphans relation closes the upper side. Arithmetic re-measured against the tree while revising: `ls src/mcp/vice/anno-*.ts | grep -v '\.test\.ts$' | wc -l` = 16, plus `anno-regbits.json`, plus `scripts/lib/anno-cli-verbs.mjs` and `.d.mts` = 19. This phase adds four new modules (`acme-gate.ts`, `block-class.ts`, `prg-image.ts`, `shipped-modules.ts`), none of which matches `anno-*`, so the 16 does not move.
    - `grep -c 'anno-regbits.json' src/mcp/vice/module-classification.ts` returns at least `1` — the data file is included by full filename, deliberately, not filtered out incidentally
    - `grep -c 'scripts/lib/anno-cli-verbs' src/mcp/vice/module-classification.ts` returns at least `2` and `grep -c 'out-of-enumeration' src/mcp/vice/module-classification.ts` returns at least `3` — both are carried as data with an explicit marker
    - `grep -c 'docs-absorbed-decisions.test.ts' src/mcp/vice/module-classification.ts` returns at least `1` — the anno-named file the glob does not match is named in the exclusion list rather than left for a reader to notice
    - `grep -c 'EXCLUDED, DELIBERATELY' src/mcp/vice/module-classification.ts` returns `1`
    - `grep -c 'acmeVerdict' src/mcp/vice/module-classification.ts` returns at least `1` and the verify module's entry carries a non-empty `note` naming the tension between criterion 2 and the ACME-export requirement text
    - `grep -c 'prg-image.ts' src/mcp/vice/module-classification.ts` returns at least `1` — the project builder's entry records where its three extractables went
    - `grep -c 'acme-gate.ts' src/mcp/vice/module-classification.ts` returns at least `1` — the gate module's entry records where its ACME half went
    - `grep -c 'block-class.ts' src/mcp/vice/module-classification.ts` returns at least `1` — the census's entry records its named store boundary
    - Every entry with verdict `capability` has a non-empty `basis.requirements` **or** at least one `basis.consumers` entry whose path is not prefix-named; the ten modules criterion 2 names all appear with verdict `capability`
    - `grep -ciE '(prefix|because of its name)' src/mcp/vice/module-classification.ts` matches only header prose and doc comments, and **no** occurrence sits inside a `basis` object — confirmed by reading the diff, and enforced mechanically by Task 2's Direction 4
    - `grep -cE 'from "\./(hostpath|containerpath)' src/mcp/vice/module-classification.ts` returns `0`, `grep -c 'node:test' src/mcp/vice/module-classification.ts` returns `0`, and `grep -c 'ASSUMED' src/mcp/vice/module-classification.ts` returns `0`
    - `git diff --stat -- src/mcp/vice/package.json` is empty — the registry is bookkeeping and must not enter the shipped list
    - `cd src/mcp/vice && npm run typecheck` exits 0 — the typed const is the format's own free shape check, so a malformed entry cannot land
    - The header records the glue-module extractables **survey result** as a statement of what was found, not as an assumption
  </acceptance_criteria>
  <done>`src/mcp/vice/module-classification.ts` holds one entry per in-enumeration item plus the two out-of-enumeration files, every basis populated from the post-extraction tree with path-plus-symbol citations and optional advisory lines, the ten criterion-2 modules recorded as capabilities, the contested verdict flagged with its tension stated, the project builder's discharged extractables recorded, every scope exclusion written into an `EXCLUDED, DELIBERATELY` section, the glue-module survey result stated, and the typecheck clean. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the enforcing guard — relations, never counts, with a planted violation</name>
  <files>src/mcp/vice/module-classification.test.ts</files>
  <behavior>
    - Direction 1 (completeness): for every in-enumeration path found on disk, an entry exists. The failure message names the unclassified file, so a module added later cannot slip in unclassified.
    - Direction 2 (no orphans): for every in-enumeration entry, its module exists on disk. Catches an entry left behind after a rename or a deletion.
    - Direction 3 (basis integrity): every entry's `basis` is non-empty; every `basis.consumers[].path` exists on disk; every `basis.requirements[]` matches a known requirement-id shape.
    - Direction 4 (the prefix prohibition — criterion 2's checkable half): no `basis` field's text names the prefix as a justification.
    - Direction 5 (verdict/extractables coherence): `extractables` is non-empty **if and only if** the verdict is the third one.
    - Direction 6 (the derived non-vacuity relation): the on-disk in-enumeration count is at least the number of in-enumeration entries, so a broken or empty glob fails loudly rather than making Direction 1 vacuously true — and the relation self-adjusts when a later phase removes a module together with its entry.
    - Direction 7 (adjacency): no two entries name the same module; out-of-enumeration entries are excluded from Direction 1's loop.
    - Direction 8 (ordering): re-running Directions 1-5 over a reversed copy of the registry yields identical results — every lookup is by module key, never by array index.
    - Direction 9 (advisory line citations): for every `basis.consumers[]` entry that carries a `line`, that line of that file contains the cited symbol.
    - Planted violation: the **same predicates the real scan uses**, run against synthetic entries — one with an empty basis, one citing a nonexistent consumer path, one whose basis names the prefix as its justification, one duplicating another entry's module, and one with the third verdict but no extractables — each reported. Plus a synthetic clean entry that is not reported.
    - Structural: `package.json`'s `files[]` does not include `module-classification.ts`.
  </behavior>
  <read_first>
    - `src/mcp/vice/hostpath-consumers.test.ts:120-135` — `topLevelProductionModules()`, the `readdirSync` enumeration idiom that excludes `*.test.*`; and `:175-232` for the floor, the non-vacuity guard before an absence loop, and the planted-violation shape whose message says that if the bad case is not reported, the real assertion cannot catch a real violation.
    - `src/mcp/vice/capability-registry.test.ts:16-25` and `:115-160` — the standard test import block and the mechanical bidirectional completeness test, whose header records that a hard-coded expected set was the previous defect.
    - `src/mcp/vice/docs-linerefs.test.ts:60-90` — the precedent for asserting that a cited line **contains** the cited symbol rather than pinning the number.
    - `src/mcp/vice/anno-verify.test.ts:187-194` — the mechanical `files[]`-absence shape (read for shape; do NOT edit that file).
    - `src/mcp/vice/module-classification.ts` — the registry as Task 1 left it, for the exact field names.
  </read_first>
  <action>
Create `src/mcp/vice/module-classification.test.ts`: the guard that turns the
record from a document into an obligation. Implement the nine directions plus the
planted violation and the structural absence check.

**Extract each direction's rule into a named predicate function, then use those
same functions in both the real scan and the planted-violation test.** This is
the property that makes the guard trustworthy and it is already the established
shape in this suite: a planted violation that re-implements the rule proves
nothing about the rule the real scan applies.

**Enumerate from disk, filtered explicitly.** Reuse the `readdirSync`-based
top-level-module idiom rather than a second directory walk, filter to the
registry's declared in-enumeration scope — prefix-named `.ts` files excluding
`*.test.*`, plus the prefix-named `.json` data file — and make that filter
**explicit** rather than incidental, because an unfiltered glob would demand an
entry for a data file by accident rather than by decision. Compare filenames as
exact strings including the extension.

**Direction 6 is the one to get right, and it must not be a pinned total.**
Derive the threshold from the registry: assert the on-disk in-enumeration count is
at least the number of in-enumeration entries. Write the reasoning beside the
assertion: an equality on a literal number goes red on a correct tree the moment a
module is added or extracted — this project already carries that scar in a guard
that pinned per-milestone totals — and even the established growing-floor variant
would go red in a later phase when modules are legitimately deleted. A derived
relation catches a broken or empty glob and survives both directions of legitimate
change. Cite the growing-floor analog as the pattern this supersedes and why, so a
later reader does not "restore" the literal floor. Put this guard **before** any
loop whose emptiness would make it vacuous.

**Direction 4** is criterion 2's only mechanically checkable half. Scan each
entry's `basis` — every consumer path, every symbol, every requirement id, and
any free text the entry's basis holds — and reject a justification that names the
prefix as the reason. Do not scan the entry's `note`: a note legitimately
discusses the prefix hazard, and a guard that cannot tell a justification from a
caveat would force the record to go silent about exactly the thing it exists to
document. Say that distinction in the test's own comment.

**Direction 9** must not trust a number it did not check. For each consumer entry
carrying an optional line, read that file, take that line, and assert it contains
the cited symbol. If it does not, fail with a message naming the file, the line,
the symbol and the drift — the same shape the doc-line-reference guard already
uses. This is the mitigation for a measured liability, not a hypothetical one.

**Direction 8** re-runs the earlier predicates over a reversed copy of the entry
array and asserts the same verdicts. If any assertion depends on array position,
this fails.

Use the repo's standard test import block, and `HERE` derived the usual way. Do
**not** add this file to the manual-only set: new test files are auto-discovered
into the automated set, which is correct. Do not extend any existing guard test to
carry this registry's assertions — a non-prefixed module's only structural guard
must not live inside a prefix-named test file, which is the hazard this whole
phase removes.
  </action>
  <verify>
    <automated>cd src/mcp/vice && npm run typecheck && node --test module-classification.test.ts test-gate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && node --test module-classification.test.ts` exits 0 with at least 11 passing tests and 0 failures
    - <!-- planner-discipline-allow: assert.equal( -->
      `grep -cE 'assert\.equal\([A-Za-z]+\.length, *[0-9]+\)' src/mcp/vice/module-classification.test.ts` returns `0` — no pinned total anywhere, which is the standing hazard this test must not reproduce
    - `grep -c 'readdirSync' src/mcp/vice/module-classification.test.ts` returns at least `1` — the in-scope set is derived from disk, never a hand-typed array
    - `grep -cE '>=' src/mcp/vice/module-classification.test.ts` returns at least `1` and the non-vacuity guard's threshold is computed from the registry rather than written as a literal — confirmed by reading the assertion
    - Direction 1 fires on a real absence: create an empty `src/mcp/vice/anno-planted-probe.ts`, observe `node --test module-classification.test.ts` RED with a message naming that file, delete the file, observe green again, and record all three observations in the SUMMARY
    - Direction 6 fires on a broken glob: temporarily narrow the enumeration filter so it returns fewer paths than there are in-enumeration entries, observe RED, restore, record both observations
    - The planted-violation test drives the **same named predicate functions** the real scan calls — confirmed by grep: each predicate name appears at least twice in the file, once in the real scan and once in the planted test
    - The planted-violation test covers all five synthetic bad cases (empty basis, nonexistent consumer path, prefix-as-justification, duplicate module, third verdict with no extractables) plus one clean case that is not reported
    - `grep -c 'note' src/mcp/vice/module-classification.test.ts` returns at least `1` and the test's own comment states that a `note` is deliberately exempt from Direction 4 so the record can discuss the prefix hazard it exists to document
    - `node -e 'const f=require("./src/mcp/vice/package.json").files; process.exit(f.includes("module-classification.ts")?1:0)'` exits 0 — the registry is absent from the shipped list, and the test asserts it
    - `git diff --stat -- src/mcp/vice/anno-verify.test.ts src/mcp/vice/hostpath-consumers.test.ts` is empty — no existing guard was extended
    - `cd src/mcp/vice && node --test test-gate.test.ts` exits 0 with no manual-only-set edit
    - `cd src/mcp/vice && npm run typecheck` exits 0
  </acceptance_criteria>
  <done>`module-classification.test.ts` implements all nine directions with the non-vacuity threshold derived from the registry rather than pinned, no equality assertion on any count, the in-scope set enumerated from disk with an explicit filter, order-independence proven over a reversed copy, advisory line citations verified by containment, and a planted-violation test driving the same named predicates as the real scan across five bad cases and one clean one. Two break-and-restore probes — a planted unclassified module and a narrowed glob — have been observed RED and restored. No existing guard was extended. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the phase was a move rather than a change — the full-glob green run and its evidence</name>
  <precondition>No VICE broker process is running on this host: `pgrep -f vice-broker` finds nothing. A live broker makes the backend-ordering test in `vice-proxy.test.ts` fail deterministically — a recorded environmental condition with an open todo, a pre-existing red, and out of scope for this phase. A suite result taken on a live-broker host is uninterpretable, not merely noisy.</precondition>
  <files>.planning/phases/27-shared-seams-extracted/27-05-SUMMARY.md</files>
  <read_first>
    - `src/mcp/vice/package.json` — the `test` script, so the command named in the evidence is the script's real definition rather than a remembered one.
    - `src/mcp/vice/test-gate.mjs:90-110` — the nine manual-only entries the narrowed gate skips, so the evidence can state what would have been omitted had the wrong command been used.
    - `src/mcp/vice/ci-guardrails.test.mjs:325-345` and `:380-392` — the two committed guards asserting CI still runs the full glob and that the script is not redefined; this task's run is the local counterpart of what they protect.
    - `.planning/todos/pending/2026-08-26-back-05-test-fails-deterministically-on-a-live-broker-host.md` — the recorded environmental condition the evidence must name.
    - `.planning/phases/27-shared-seams-extracted/27-01-SUMMARY.md`, `27-02-SUMMARY.md`, `27-03-SUMMARY.md`, `27-04-SUMMARY.md` — the four preceding plans' recorded probe observations and command results, which this evidence consolidates rather than re-derives.
  </read_first>
  <action>
Close the phase by producing criterion 4's evidence. This task writes no source
code; its deliverable is a set of observations recorded in the plan SUMMARY, each
backed by a command rather than a claim.

**First, assert the environment (D-17).** Confirm no broker process is running.
D-17 is explicit that the evidence must name the exact command and state the
broker was stopped, and this is not optional hygiene: the backend-ordering test in the stdio-proxy suite reddens
deterministically on a live-broker host, it is a pre-existing condition with an
open todo, and fixing it is broker behaviour with no requirement in this phase.
Evidence that does not state the broker was stopped cannot be interpreted, because
a red there would mean nothing either way.

**Then run, in this order, and record each result:**

1. The typecheck. This phase's characteristic failure mode is a half-done import
   split across a dozen files, and the typechecker catches that class before the
   suite does.
2. The **full-glob** suite from the module directory — the `test` script's own
   definition, which runs every file matching the test glob. **Not** the narrowed
   automated gate: it skips nine files including the only end-to-end wire proof in
   the suite, and a committed CI guard exists specifically to stop that
   substitution happening upstream. Record the exact command verbatim, and record
   the pass, fail and skip counts, because a skip count that *dropped* is as much
   a signal as a failure.
3. The tarball validator, which sees this phase's two `files[]` additions and
   validates both published tarballs' exact contents.
4. The zero-deletions query: ask git for the files deleted between the phase's
   base commit and the current head, filtered to prefix-named paths under the
   module directory and the scripts directory, and confirm the count is zero.
   This is criterion 4's actual substance — demonstrably a move rather than a
   change — and it is the one part of the criterion that is trivially mechanical,
   so it must be a command, not a sentence.
5. A floor check that the prefix-named non-test module family is still intact on
   disk: count the prefix-named `.ts` files excluding test files and confirm it is
   at least sixteen. State it as a floor, never an equality — a pinned total is
   the standing hazard, and this phase's own directory gained four files.

**Then write the evidence into the SUMMARY**, with all four mandatory elements
present and none inferred: the exact command; the sentence that the broker was
stopped, with the reason it matters; the pass/fail/skip counts; and the
zero-deletions result as command output. Add the typecheck and the tarball
validator as two further lines. If any suite is red, **record it red with its
name and its attribution** rather than re-running until it passes or narrowing the
command — an honest red plus its cause is evidence; a green from a different
command is not.

**Finally, consolidate the phase's observed-RED probes** into one list, drawn from
the four preceding SUMMARYs rather than re-run: the gate's env-guard removal, the
input-validator's loosened minimum, the census's left-behind comparison site, the
enumerator's removed existence assertion, and this plan's planted unclassified
module and narrowed glob. Each was observed red and restored. That list is what
makes the phase's guards demonstrably capable of failing, which is this project's
acceptance bar.
  </action>
  <verify>
    <automated>if pgrep -f 'vice-broker' >/dev/null; then echo "FAIL: a VICE broker is running - the backend-ordering test reddens deterministically; stop it before trusting this run" >&2; exit 1; fi; cd src/mcp/vice && npm run typecheck && npm test && cd ../../.. && node scripts/check-npm-packages.mjs && test "$(git diff --diff-filter=D --name-only 6c1f569..HEAD -- src/mcp/vice scripts | grep -c anno)" = "0" && test "$(ls src/mcp/vice/anno-*.ts | grep -vc '\.test\.ts$')" -ge 16</automated>
  </verify>
  <acceptance_criteria>
    - `pgrep -f 'vice-broker'` finds no process, and the SUMMARY states the broker was stopped and why it matters
    - The SUMMARY names the full-glob command verbatim as `package.json`'s `test` script defines it, and states explicitly that the narrowed automated gate was **not** used, naming the nine files it would have skipped
    - `cd src/mcp/vice && npm test` exits 0, and the SUMMARY records the pass, fail and skip counts as reported by the runner
    - `cd src/mcp/vice && npm run typecheck` exits 0 and the SUMMARY records it
    - `node scripts/check-npm-packages.mjs` exits 0 and the SUMMARY records its closure-walk output line
    - <!-- planner-discipline-allow: anno -->
      `test "$(git diff --diff-filter=D --name-only 6c1f569..HEAD -- src/mcp/vice scripts | grep -c anno)" = "0"` exits 0, and the SUMMARY records the command and its output rather than the claim. Wrapped in `test "$(…)"` to match this plan's own `<automated>` gate: the bare pipeline prints `0` but *exits 1*, because `grep -c` returns no-match status even when its count is the count we want.
    - `ls src/mcp/vice/anno-*.ts | grep -vc '\.test\.ts$'` returns at least `16`, recorded as a floor and never as an equality
    - The SUMMARY lists exactly two `package.json` `files[]` additions for the whole phase and confirms none of the three test-only new modules is among them: `node -e 'const f=require("./src/mcp/vice/package.json").files; const bad=["acme-gate.ts","module-classification.ts","shipped-modules.ts"].filter(n=>f.includes(n)); process.exit(bad.length?1:0)'` exits 0
    - The SUMMARY consolidates all six observed-RED probe results from this phase, each naming what was broken, that it went red, and that it was restored
    - The SUMMARY states which requirement ids this phase closes (SEAM-01, SEAM-02, SEAM-03) and, for each, the artifact that evidences it
    - If any suite is red, the SUMMARY names the failing test, states whether it is a pre-existing environmental condition, and does **not** present a narrowed command's green in its place
  </acceptance_criteria>
  <done>The full-glob suite, the typecheck and the tarball validator are all green with the broker confirmed stopped; the deletion query returns zero and the prefix-named module family is intact at or above its floor; the SUMMARY carries all four mandatory evidence elements plus the typecheck and validator lines, the two-entry `files[]` accounting, the six consolidated observed-RED probes, and the per-requirement artifact mapping. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this phase's record → a later phase's deletion decisions | The registry is consumed by a phase that will delete ~12k lines. A wrong or incomplete entry there is acted on, not reviewed. This is the boundary the whole plan exists to harden. |
| on-disk module set → the enforcing test's scanned set | If the enumeration glob silently narrows, every completeness assertion passes vacuously and an unclassified module ships. |
| a suite result → a phase-completion claim | The choice of test command decides what "green" means. Two commands exist and one omits nine files. |
| `package.json` `files[]` → published npm tarball | The registry must stay out; the phase's two genuine additions must be exactly two. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-05-01 | Repudiation | A verdict recorded on a basis the record did not actually use — in particular the name prefix, which is the one justification criterion 2 forbids | high | mitigate | Direction 4 scans every `basis` field and rejects a prefix-as-justification, with `note` deliberately exempt so the record can still discuss the hazard; the planted-violation test drives the **same** predicate against a synthetic prefix-justified entry; and the contested verdict is flagged in its own entry rather than presented as settled. |
| T-27-05-02 | Tampering | A module added later slipping in unclassified, or an entry left behind after a rename, so the record silently stops matching the tree | high | mitigate | Directions 1 and 2 assert the relation in both directions from a disk enumeration; a real planted unclassified module is created, observed RED with the file named, and removed. |
| T-27-05-03 | Tampering | The enumeration glob narrowing silently, making every completeness assertion vacuously true | high | mitigate | Direction 6's non-vacuity threshold is **derived from the registry** rather than pinned, placed before every loop; the narrowed-glob case is exercised once and observed RED. A pinned literal was explicitly rejected: it goes red on a correct tree, which is this project's recorded scar. |
| T-27-05-04 | Repudiation | The phase's green claimed from the narrowed automated gate, or taken on a live-broker host, or stated without the counts — a green that omits nine files or a red nobody can attribute | high | mitigate | The verify gate refuses to run while a broker process exists; the full-glob command is named verbatim from the script's own definition; the pass/fail/skip counts are recorded; a red is recorded red with its attribution rather than replaced by a narrower command's green. Two committed CI guards protect the same substitution upstream. |
| T-27-05-05 | Information Disclosure | The registry leaking into the published npm tarball, or one of the phase's three test-only modules doing so | medium | mitigate | A mechanical `files[]`-absence assertion in the registry's own test, plus a phase-level accounting criterion asserting exactly two additions and naming the three modules that must be absent. |
| T-27-05-06 | Tampering | An advisory line citation drifting, so a later phase reads a number that points at the wrong code | medium | mitigate | Lines are optional and, where present, verified by asserting the cited line **contains** the cited symbol — the established in-repo shape — rather than trusted. The liability is measured, not hypothetical: an audit of this phase's own source documents found two citations drifted by two lines. |
| T-27-05-SC | Tampering | npm / pip / cargo package installation | low | accept | **No package-manager install task exists in this plan or this phase.** This plan does not modify `package.json` at all, asserted by a criterion; the phase's only `package.json` diff is two `files[]` entries, with `dependencies` and `devDependencies` byte-identical. RESEARCH.md's Package Legitimacy Audit records zero external packages, so there is nothing to audit and the dependency bar is not triggered. |
</threat_model>

<verification>
Per task, from `src/mcp/vice`:
- `npm run typecheck` — the typed const's free shape check, and the split-import detector.
- `node --test module-classification.test.ts` — sub-30-second feedback.

Plan gate (this is also the **phase** gate, since this plan is last):
- No broker process running (`pgrep -f vice-broker` finds nothing).
- `cd src/mcp/vice && npm run typecheck` exits 0.
- `cd src/mcp/vice && npm test` — the **full glob** — exits 0, with pass/fail/skip counts recorded.
- `node scripts/check-npm-packages.mjs` exits 0.
- <!-- planner-discipline-allow: anno -->
  `test "$(git diff --diff-filter=D --name-only 6c1f569..HEAD -- src/mcp/vice scripts | grep -c anno)" = "0"` exits 0 — the wrapped form, matching the `<automated>` gate above; the bare pipeline prints `0` but exits 1.
- `ls src/mcp/vice/anno-*.ts | grep -vc '\.test\.ts$'` returns at least `16` (a floor, never an equality).

**Forbidden as evidence, at the phase level:** `npm run test:automated`. It runs
the narrowed gate, which skips nine files including the only end-to-end wire proof
in the suite. A green from it is not this phase's green.
</verification>

<success_criteria>
- `src/mcp/vice/module-classification.ts` holds one entry per in-enumeration item plus the two out-of-enumeration files, each with a basis of cited consumers and requirement ids, and none justified by the name prefix.
- The ten modules criterion 2 names are recorded as capabilities; the contested one carries its tension in a `note`; the project builder's discharged extractables are recorded; every scope exclusion is written into an `EXCLUDED, DELIBERATELY` section; the glue-module extractables survey result is stated.
- `module-classification.test.ts` fails when a module has no entry, when an entry has no module, when a basis is empty or cites a nonexistent path, when a basis names the prefix as its justification, when a verdict and its extractables disagree, when two entries name one module, and when the enumeration glob narrows — each proven by a planted violation driving the same predicate as the real scan.
- No pinned total appears anywhere in the guard; the non-vacuity threshold is derived from the registry.
- Two break-and-restore probes in this plan, and four inherited from the preceding plans, are recorded as observed RED and restored.
- The registry is absent from `files[]`; the phase's `files[]` diff is exactly two entries and includes none of the three test-only new modules.
- The full-glob suite, the typecheck and the tarball validator are green with the broker confirmed stopped, and the deletion query returns zero.
- SEAM-01, SEAM-02 and SEAM-03 each have a named artifact evidencing them.
</success_criteria>

<output>
Create `.planning/phases/27-shared-seams-extracted/27-05-SUMMARY.md` when done.
It carries criterion 4's evidence and must state, without inference: the exact
full-glob command; that the broker was stopped and why that matters; the
pass/fail/skip counts; the zero-deletions command and its output; the typecheck
and tarball-validator results; the two-entry `files[]` accounting with the three
test-only modules confirmed absent; the OQ-2 and OQ-4 decisions with their
reasons; the glue-module extractables survey result; the consolidated list of six
observed-RED probes; and the per-requirement artifact mapping for SEAM-01,
SEAM-02 and SEAM-03.
</output>
