---
phase: 27-shared-seams-extracted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/vice/acme-gate.ts
  - src/mcp/vice/acme-gate.test.ts
  - src/mcp/vice/r2000-test-gate.ts
  - src/mcp/vice/disasm-roundtrip.test.ts
  - src/mcp/vice/skill-acme-build-cli.test.ts
  - src/mcp/vice/r2000-cli.test.ts
  - src/mcp/vice/r2000-answer-key.test.ts
autonomous: true
requirements: [SEAM-01]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "With VICE_REQUIRE_ACME=1 and ACME_BIN pointed at a nonexistent path, a child `node --test` run of a probe file importing src/mcp/vice/acme-gate.ts by absolute path exits NON-ZERO and its combined stdout+stderr names the gate's own assertion message — observed by a committed test, not by a transcript (SEAM-01 probe: empty/null input)"
    - "With VICE_REQUIRE_ACME unset and the same nonexistent ACME_BIN, that identical child run exits ZERO (a named skip, not a failure) — the non-vacuity control that proves the FAIL came from the gate and not from a broken import path or a missing binary"
    - "acme-gate.ts resolves the binary name from process.env.ACME_BIN with the same fallback default the moved body used, matches its probe banner with the same case-insensitive /acme/i test over combined stdout+stderr, and reads both env vars by byte-identical names — so .github/workflows/ci.yml:140's VICE_REQUIRE_ACME binding still reaches the gate with no ci.yml code edit (SEAM-01 probe: encoding / whose definition of equality applies)"
    - "src/mcp/vice/r2000-test-gate.ts retains the regenerator2000 half and all ten of its importers, and holds no ACME symbol, no ACME prose and no re-export of the moved module"
    - "All four ACME-importing test files import the five symbols from ./acme-gate.ts; r2000-cli.test.ts's single mixed statement is SPLIT into two, one per source module, keeping R2000_AVAILABLE / skipReasonFor / assertR2000RequiredIfEnvSet on the regenerator2000 gate"
  prohibitions:
    - "MUST NOT leave any route by which the ACME hard-FAIL degrades into a silent skip: no re-export shim in the module the half leaves, no rename of ACME_BIN or VICE_REQUIRE_ACME, and no removal or weakening of the committed observation of the FAIL. A gate that reports green whether or not it works is worse than no gate, and CI is green either way."
  artifacts:
    - path: "src/mcp/vice/acme-gate.ts"
      provides: "The ONE ACME-availability seam under a non-r2000 name"
      exports: ["ACME_BIN", "probeAcme", "ACME_AVAILABLE", "acmeSkipReasonFor", "assertAcmeRequiredIfEnvSet"]
      min_lines: 60
    - path: "src/mcp/vice/acme-gate.test.ts"
      provides: "Criterion 1's observed hard-FAIL, its non-vacuity control, and the files[]-absence assertion"
      contains: "mkdtempSync"
      min_lines: 80
  key_links:
    - from: "src/mcp/vice/disasm-roundtrip.test.ts"
      to: "src/mcp/vice/acme-gate.ts"
      via: "named import of ACME_BIN / acmeSkipReasonFor / assertAcmeRequiredIfEnvSet"
      pattern: 'from "\./acme-gate\.ts"'
    - from: "src/mcp/vice/skill-acme-build-cli.test.ts"
      to: "src/mcp/vice/acme-gate.ts"
      via: "named import of the same three symbols"
      pattern: 'from "\./acme-gate\.ts"'
    - from: "src/mcp/vice/r2000-cli.test.ts"
      to: "src/mcp/vice/acme-gate.ts"
      via: "split import — ACME_AVAILABLE / assertAcmeRequiredIfEnvSet only"
      pattern: 'from "\./acme-gate\.ts"'
    - from: "src/mcp/vice/r2000-answer-key.test.ts"
      to: "src/mcp/vice/acme-gate.ts"
      via: "named import of the same three symbols"
      pattern: 'from "\./acme-gate\.ts"'
    - from: ".github/workflows/ci.yml"
      to: "src/mcp/vice/acme-gate.ts"
      via: "env-var name binding only (VICE_REQUIRE_ACME at :140) — never a module path, so no ci.yml code edit"
      pattern: "VICE_REQUIRE_ACME"
---

<objective>
Move the ACME availability gate out from under the `r2000-` prefix into
`src/mcp/vice/acme-gate.ts` under its own name, repoint all four importing test
files, and prove by a committed child-process observation that a missing ACME
still makes the suite **FAIL** rather than skip (SEAM-01, D-01..D-05).

Purpose: a prefix-driven deletion in Phase 32 currently takes the ACME hard-FAIL
gate with it, and that loss is silent — CI stays green whether the gate works or
not. This plan removes the gate from the prefix's reach and makes its FAIL
observable forever.
Output: a new non-`r2000` seam module, its own test file carrying criterion 1's
proof, and four repointed importers. Zero `r2000` modules deleted.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-shared-seams-extracted/27-CONTEXT.md
@.planning/phases/27-shared-seams-extracted/27-RESEARCH.md
@.planning/phases/27-shared-seams-extracted/27-PATTERNS.md
</context>

<research_corrections_applied>
RESEARCH.md overrides CONTEXT.md on every matter of fact. Corrections that bind
this plan, each re-verified against the tree at `6c1f569` while planning:

- **C-2** — `r2000-test-gate.ts` keeps **ten** importers, not nine, because
  `r2000-cli.test.ts` imports *both* halves. Its single import statement
  (`:22-28`) must be **SPLIT into two**, never rewritten. It imports
  `ACME_AVAILABLE` and `assertAcmeRequiredIfEnvSet` only — **not** `ACME_BIN`
  and **not** `acmeSkipReasonFor`, so a criterion asserting "all four files
  import `ACME_BIN`" would be false.
- **C-5** — `ACME_AVAILABLE` is at `r2000-test-gate.ts:134`, not `:132`.
  Re-verified at `:134` while planning. The substance of D-05 is unaffected: it
  is still a module-load `const`, so no in-process test can re-probe it.
- **ci.yml audit (D-03 CONFIRMED)** — `.github/workflows/ci.yml` binds
  **env-var names only**. `ACME_BIN` does not appear in it at all;
  `VICE_REQUIRE_ACME: "1"` is at `:140` followed by `run: npm test` at `:141`.
  **No `ci.yml` edit is required or permitted.** SEAM-01's "repointed in the same
  commit" clause is satisfied by confirming the names did not move, not by a code
  change. Inventing a `ci.yml` change here is inventing work; renaming either env
  var is the one-way failure D-03 warns about.
- **hostpath-consumers.test.ts:204 resolved** — `acme-gate.ts` does **not**
  belong in that list. The assertion is membership in `r2000ProductionModules()`,
  a glob over `r2000-*.ts`; `acme-gate.ts` can never match it and adding it
  **breaks** the test. `R2000_MODULE_FLOOR = 14` against 16 on disk still holds
  because this plan adds and removes zero `r2000-*.ts` modules. **No edit.**
- **r2000-verify.test.ts:187 stays exactly as written** (D-04) — it is still
  correct about the module it names. Extending it would put a non-`r2000`
  module's only structural guard inside an `r2000-*` test file, re-creating the
  prefix hazard this phase removes.
</research_corrections_applied>

<reversibility_note>
**The structural validator's `one-way`-without-a-checkpoint warning on Task 1 is
expected and dispositioned, not an oversight.** `27-CONTEXT.md` D-03 records the
owner already choosing this exact option — `ACME_BIN`, `VICE_REQUIRE_ACME` and
`assertAcmeRequiredIfEnvSet` keep byte-identical names — so the door has been
walked through deliberately. The reversibility taxonomy's own anti-pattern list
forbids gating a decision the phase CONTEXT already records the human making:
keep the rating for the record, do not insert a `checkpoint:decision` to re-ask.
The plan therefore stays `autonomous: true`, and the rating survives on the task
so a later phase can see which door was walked through and why.
</reversibility_note>

<assumption_delta_decision>
**Scope of this plan: `no-change`.**

The `assumption-delta` detector fired for Phase 27 on three prose signals
("a second, silent failure mode", "also extractable here", "not optional"). Read
against this plan's scope, none is a singular→plural transition in the design:
the gate had exactly one implementation before the move and has exactly one
after it, under a different filename. No identity model is generalised here — the
five symbol names are byte-identical by D-03, and `ci.yml` still binds them by
the same names.

The phase's one genuine identity-model transition (the block-class vocabulary) is
answered in `27-02-PLAN.md`, where it is resolved as **promote**. This is a
recorded decision, not an omission.
</assumption_delta_decision>

<probe_accounting>
Spec-less probe fallback (no SPEC.md exists for this phase). The deterministic
edge probe surfaced **12** applicable edges across SEAM-01/02/03, all
`unresolved`, and resolution was deferred to the planner.

**This plan owns the 2 SEAM-01 rows.** Both resolved `explicit`:

| Probe row | Resolution | Authored as |
|---|---|---|
| SEAM-01 / empty ("result for empty, single-element, or null input") | resolved, verification: explicit — read as "what does the gate do when `ACME_BIN` is unset, when it points at a nonexistent path, and when `VICE_REQUIRE_ACME` is unset" | `must_haves.truths` rows 1, 2 and (for the unset-`ACME_BIN` default) row 3 |
| SEAM-01 / encoding ("whose definition of length/equality applies") | resolved, verification: explicit — read as "which string-equality definition governs the moved gate's banner match and its env-var name reads" | `must_haves.truths` row 3 |

2 surfaced → 2 authored, 0 flagged, 0 dropped. The remaining 10 rows are
accounted for in `27-02-PLAN.md` (**4** × SEAM-03: boundary, precision,
idempotency, concurrency) and `27-05-PLAN.md` (**6** × SEAM-02: boundary,
adjacency, empty, encoding, ordering, precision). Recounted from the coverage
report while planning — the split is 2 / 6 / 4, not 2 / 5 / 5. Phase-level
equality is 12 in / 12 accounted.

**Prohibition recall (§B, in-prompt).** Stage 1 over-produced for SEAM-01;
Stage 2 dropped the routine-engineering candidates (must not throw on a spawn
error, must not mutate `process.env`, must not leak a child process — all owned
by the edge probe or ordinary review). One kept, authored **descriptor-less**
under `must_haves.prohibitions`. Canon-referral breadcrumb, dropped not minted:
command injection via an env-supplied binary name reaching a subprocess is canon
— covered by `/gsd-secure-phase` and recorded as `T-27-01-01` in
`<threat_model>` below; not minted as a prohibition.
</probe_accounting>

<artifacts_this_phase_produces>
Symbols and paths this plan creates. Every symbol name below is **unchanged**
from its current definition — only its file changes:

| New path | Symbols | New name? |
|---|---|---|
| `src/mcp/vice/acme-gate.ts` | `ACME_BIN`, `probeAcme`, `ACME_AVAILABLE`, `acmeSkipReasonFor`, `assertAcmeRequiredIfEnvSet` | **No** — byte-identical names, moved (D-03) |
| `src/mcp/vice/acme-gate.test.ts` | new test titles only; no exported symbols | new file |

Removed from `src/mcp/vice/r2000-test-gate.ts`: the same five symbols and the
ACME rationale banner (`:97-166`). No new CLI flag, env var, tool name, wire
name, file format or stored record is introduced by this plan.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "the ACME gate stands under its own name" — one complete symbol move</name>
  <precondition>The ACME cross-assembler resolves on PATH (`command -v acme` succeeds and prints a path) — the repointed importers' positive half needs the real oracle, and without it every ACME-gated test skips and the repoint is unproven rather than proven.</precondition>
  <reversibility rating="one-way">Renaming `ACME_BIN` or `VICE_REQUIRE_ACME` silently converts CI's hard-FAIL into a skip with a green run either way; keeping both names byte-identical is the decision, and CONTEXT.md D-03 already records the owner choosing exactly this, so the rating is carried for the record and no checkpoint re-asks it.</reversibility>
  <files>src/mcp/vice/acme-gate.ts, src/mcp/vice/r2000-test-gate.ts, src/mcp/vice/disasm-roundtrip.test.ts, src/mcp/vice/skill-acme-build-cli.test.ts, src/mcp/vice/r2000-cli.test.ts, src/mcp/vice/r2000-answer-key.test.ts</files>
  <read_first>
    - `src/mcp/vice/r2000-test-gate.ts` — the whole file (166 lines). `:1-33` is the header whose TEST-ONLY and non-`*.test.*`-name paragraphs travel to the new module; `:34` is the `node:child_process` import both halves use; `:97-112` is the ACME rationale banner that travels with the code; `:116`, `:122`, `:134`, `:143`, `:158` are the five symbol definitions.
    - `src/mcp/vice/disasm-roundtrip.test.ts:50-70` — importer 1 (`:57`) plus the prose block at `:60-65` that names the old seam.
    - `src/mcp/vice/skill-acme-build-cli.test.ts:13-18` and `:45-49` — importer 2 (`:47`) plus its header prose.
    - `src/mcp/vice/r2000-cli.test.ts:20-32` — importer 3, the **mixed** statement spanning `:22-28`.
    - `src/mcp/vice/r2000-answer-key.test.ts:215-235` — importer 4, the statement opening `import {` at `:227`.
    - `src/mcp/vice/fork-deleted-tools.ts` — the whole file (21 lines). Its closing paragraph is this repo's canonical statement of why a shared helper is a plain `.ts` and never a `.test.ts`; reproduce that reasoning in the new header.
    - `src/mcp/vice/r2000-verify.test.ts:173-196` — the `files[]`-absence assertion shape (read for shape; do NOT edit this file).
    - `.planning/codebase/CONVENTIONS.md` — the `<domain>-<role>.ts` naming rule and the real-extension relative-import rule.
  </read_first>
  <action>
Create `src/mcp/vice/acme-gate.ts` and move the ACME half of the gate into it
verbatim, then repoint every consumer and delete the half from the module it
came from. This is one commit: the module, its origin's deletion, and all four
importers.

**The new module.** Name it `acme-gate.ts` per D-02 — `test-gate.ts` is
unavailable (`test-gate.mjs`, `test-gate.d.mts` and `test-gate.test.ts` already
occupy that stem in the same directory and gate *which tests run*, a different
job), and `acme-probe.ts` was rejected because `vice-probe.ts` already means
"liveness probe of a running thing". Confirmed free: no file matching that stem
exists in `src/mcp/vice/`.

Move these five exports with their doc comments byte-identically, preserving
declaration order: `ACME_BIN`, `probeAcme`, `ACME_AVAILABLE`,
`acmeSkipReasonFor`, `assertAcmeRequiredIfEnvSet`. Do not rename, retype,
reorder the internals, or change a single message string — `ACME_BIN` and
`VICE_REQUIRE_ACME` are read from `process.env` by exact name and CI binds those
names (D-03). `probeAcme()` must keep its argv-array `spawnSync` form, its
`--version`-then-`--help` fallback ladder, its 10-second timeout, its
combined-`stdout`+`stderr` banner read, its case-insensitive banner test, and
its never-throw contract. Add `import { spawnSync } from "node:child_process";`
as the new module's own copy — the origin module keeps its own, because its
regenerator2000 probe still uses it.

**The header.** Follow the three-part single-seam shape this repo uses
(`stock-condition.ts` is the best exemplar): a one-line "the ONE place that…"
statement, a `WHY THIS FILE EXISTS` paragraph, and an explicit
`WHAT NOT TO DO` list. Carry across, reworded for the new home: the divergence
narrative from the origin's ACME banner (three hand-copied probes converged
here; one copy would get its timeout changed, another its regex loosened, and
nobody would notice); the TEST-ONLY paragraph, with its `files[]`-absence
attribution changed to name `acme-gate.test.ts` (D-04); and the paragraph
explaining that the filename deliberately does not match the `*.test.*` glob the
`test` script runs, because it is imported BY test files and must never be
collected as one. Add to `WHAT NOT TO DO`: never rename either env var (a rename
converts CI's hard-FAIL into a skip, green either way), and never re-export
these symbols from the module they left (a shim leaves the deletion hazard
intact, which is the failure this requirement exists to remove).

Do **not** write any `[ASSUMED]` label into the header —
`assumption-label-discipline.test.ts` scans authored non-test `.ts` directly
under `src/mcp/vice/` and demands a matching Assumptions Log row. The moved code
contains none today. Do not import `hostpath.ts` or `containerpath.ts`:
`hostpath-consumers.test.ts` asserts the hostpath consumer set is exactly five
named modules, and this module has no reason to be a sixth.

**The origin module.** Delete the ACME banner and all five definitions from
`src/mcp/vice/r2000-test-gate.ts` (the contiguous block from the banner comment
at `:97` to end of file at `:166`). Leave the regenerator2000 half and the
module's own `:1-33` header **intact**: measured at `6c1f569`, that header does
not narrate two halves — its opening line is "the ONE place the D-11
regenerator2000 availability gate is implemented", which is already true of the
surviving half — so **no header edit is expected**. If a sentence claiming both
gates is nevertheless found, narrow it to the regenerator2000 gate and stop
there: do **not** add a pointer naming the new module, because this task's own
`grep -ci acme … returns 0` criterion is the shim detector and forbids any ACME
text in this file, a courtesy pointer included. The sibling-seam pointer belongs
in `acme-gate.ts`'s own header, which this task writes. Add **no** re-export
(D-01: a shim was rejected by name because with one in place a prefix deletion
still breaks four test files). Every ACME mention leaves the file.

**The four importers.** Three are single-line rewrites: change the module
specifier only, keeping the imported symbol list exactly as it is, in
`disasm-roundtrip.test.ts:57`, `skill-acme-build-cli.test.ts:47`, and
`r2000-answer-key.test.ts:227-231`. The fourth is a **SPLIT, not a rewrite**
(Correction C-2): `r2000-cli.test.ts:22-28` is one statement importing both
halves. Replace it with two statements side by side — one keeping
`R2000_AVAILABLE`, `skipReasonFor` and `assertR2000RequiredIfEnvSet` from the
regenerator2000 gate, one taking `ACME_AVAILABLE` and
`assertAcmeRequiredIfEnvSet` from the new module. That file imports neither
`ACME_BIN` nor `acmeSkipReasonFor`; do not add them. `r2000-spawn-seam.test.ts:53`
is the in-repo template for the target shape: one statement per source module,
real `.ts` extension, side by side.

Do **not** touch `.github/workflows/ci.yml` (D-03: it binds env-var names only,
verified line by line), `src/mcp/vice/r2000-verify.test.ts` (D-04), or
`src/mcp/vice/hostpath-consumers.test.ts` (adding the new module there breaks its
`r2000-*` glob membership assertion).
  </action>
  <verify>
    <automated>cd src/mcp/vice && npm run typecheck && node --test disasm-roundtrip.test.ts skill-acme-build-cli.test.ts r2000-cli.test.ts r2000-answer-key.test.ts r2000-verify.test.ts hostpath-consumers.test.ts assumption-label-discipline.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/mcp/vice/acme-gate.ts` exists and `grep -cE '^export (const|function) (ACME_BIN|probeAcme|ACME_AVAILABLE|acmeSkipReasonFor|assertAcmeRequiredIfEnvSet)' src/mcp/vice/acme-gate.ts` returns `5`
    - `grep -c 'spawnSync(ACME_BIN, \["--version"\]' src/mcp/vice/acme-gate.ts` returns `1` — the argv-array form is preserved, never a shell string
    - `grep -cE '(execSync|execFileSync|shell: *true)' src/mcp/vice/acme-gate.ts` returns `0`
    - `grep -c 'process.env.ACME_BIN' src/mcp/vice/acme-gate.ts` returns `1` and `grep -c 'VICE_REQUIRE_ACME' src/mcp/vice/acme-gate.ts` returns at least `1` — both env vars read by byte-identical name
    - <!-- planner-discipline-allow: acme -->
      `grep -ci acme src/mcp/vice/r2000-test-gate.ts` returns `0` — the half is gone AND no re-export shim remains (a shim would mention it)
    - `grep -rc 'from "./r2000-test-gate.ts"' src/mcp/vice/*.test.ts | grep -c ':1$'` returns `10` — the ten remaining importers, one statement each (Correction C-2's corrected count)
    - `grep -c 'from "./acme-gate.ts"' src/mcp/vice/disasm-roundtrip.test.ts src/mcp/vice/skill-acme-build-cli.test.ts src/mcp/vice/r2000-cli.test.ts src/mcp/vice/r2000-answer-key.test.ts` reports `1` for each of the four files
    - `grep -c 'from "./r2000-test-gate.ts"' src/mcp/vice/r2000-cli.test.ts` returns `1` — the split kept the regenerator2000 half in place rather than replacing it
    - <!-- planner-discipline-allow: ACME_BIN -->
      <!-- planner-discipline-allow: acmeSkipReasonFor -->
      **Neither `ACME_BIN` nor `acmeSkipReasonFor` appears in either gate import *statement* in `r2000-cli.test.ts`** — scoped to the statements, because a file-wide grep can never return `0` here: the file carries **2** occurrences today, both in the prose block at `:679-680`, which Task 3 rewrites while being told to preserve exactly those symbol claims.
      `perl -0777 -ne 'while (/import[^;]*?from "\.\/(acme-gate|r2000-test-gate)\.ts";/gs) { print "$&\n" if $& =~ /ACME_BIN|acmeSkipReasonFor/ }' src/mcp/vice/r2000-cli.test.ts`
      prints nothing. This is a "must not appear" guard, so its value is that it *can* fire — **verified by a planted probe while planning**: inserting `ACME_BIN,` into the brace list makes the identical command print. `tsc --noEmit` would not catch that on its own, because `src/mcp/vice/tsconfig.json` sets `strict` but **not** `noUnusedLocals`, so an unused import binding compiles clean. Measured at `6c1f569`: prints nothing (the single mixed statement at `:22-28` imports `ACME_AVAILABLE` and `assertAcmeRequiredIfEnvSet` only), and it must still print nothing after the split. Read it by its printed output, not by its exit status.
    - `grep -c 'hostpath' src/mcp/vice/acme-gate.ts` returns `0`
    - `grep -c 'ASSUMED' src/mcp/vice/acme-gate.ts` returns `0`
    - `git diff --stat -- .github/workflows/ci.yml` is empty — no ci.yml change
    - `git diff --stat -- src/mcp/vice/r2000-verify.test.ts src/mcp/vice/hostpath-consumers.test.ts` is empty
    - `cd src/mcp/vice && npm run typecheck` exits 0 — the mechanical completeness check for a half-done split
    - `cd src/mcp/vice && node --test disasm-roundtrip.test.ts skill-acme-build-cli.test.ts r2000-cli.test.ts r2000-answer-key.test.ts` exits 0 with the ACME-gated tests **running** (not skipping), because real ACME 0.97 is on PATH per this task's precondition
  </acceptance_criteria>
  <done>The five ACME symbols stand in `src/mcp/vice/acme-gate.ts` under byte-identical names, no ACME text or re-export survives in the module they left, all four importers resolve against the new module with `r2000-cli.test.ts`'s statement split rather than rewritten, `tsc --noEmit` is clean, and the four importers' suites pass with their ACME-gated tests actually executing. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create acme-gate.test.ts — the observed hard-FAIL, its non-vacuity control, and the files[] absence</name>
  <files>src/mcp/vice/acme-gate.test.ts</files>
  <behavior>
    - Test 1 (the FAIL): a child `node --test` run over a probe file that calls the gate's assert helper, spawned with `VICE_REQUIRE_ACME=1` and `ACME_BIN` set to a path that does not exist, exits with a **non-zero** status.
    - Test 2 (same run, sharper): that child's combined stdout+stderr contains the gate's own refusal wording, proving the non-zero exit came from the gate's assertion and not from a module-resolution error, a syntax error or a missing file.
    - Test 3 (the non-vacuity control): the identical child run with `VICE_REQUIRE_ACME` **absent** from the environment and the same nonexistent `ACME_BIN` exits **zero** — a named skip, not a failure. Both directions, or neither proves anything.
    - Test 4 (structural): `package.json`'s `files[]` array does not include `acme-gate.ts`.
    - Test 5 (default resolution): with `ACME_BIN` absent, the gate's binary name falls back to the same default the moved body used.
  </behavior>
  <action>
Create `src/mcp/vice/acme-gate.test.ts`. It carries criterion 1's proof and the
new module's `files[]`-absence guard (D-04, D-05). Do **not** extend
`r2000-verify.test.ts` — putting a non-`r2000` module's only structural guard
inside an `r2000-*` test file re-creates the exact prefix-deletion hazard this
phase removes.

**Why it must be a child process.** `ACME_AVAILABLE` is a module-load `const`
(verified at `r2000-test-gate.ts:134`, and it keeps that shape after the move),
so assigning `process.env.ACME_BIN` inside a test cannot affect it — the module
is already loaded. No in-process test can prove criterion 1. The in-repo
precedent to copy is `r2000-launch.test.ts:497-512`, which spawns
`process.execPath` with an absolute-path import built by `JSON.stringify(join(HERE, …))`
and an `env` override on the spawn.

**Avoid the two recorded traps.** First, the child must NOT be pointed at this
suite or at this file — that recurses forever. Second, the probe file must NOT
be written inside `src/mcp/vice/`, because `node --test '*.test.*'` would collect
it on the next run and, if ever left behind, break
`test-gate.test.ts`'s "every on-disk `*.test.*` file lands in exactly one of the
automated/manual sets" assertion. So: `mkdtempSync` under `tmpdir()`, write one
small `probe.test.mjs` there whose whole body is a single `test()` calling the
gate's assert helper with `node:assert/strict`, import the gate by **absolute
path** (Node's type-stripping resolves the `.ts` regardless of the child's cwd),
and `rmSync(dir, { recursive: true, force: true })` in a `finally`. The
`finally` is not optional. Recorded host fact: `/tmp` is a RAM-backed tmpfs with
aging disabled, so a leaked probe dir is leaked memory.

**Spawn shape.** `spawnSync(process.execPath, ["--test", probePath], { encoding: "utf8", timeout: 30_000, env: { … } })`.
For the FAIL direction set `VICE_REQUIRE_ACME` to `"1"` and `ACME_BIN` to a path
inside the temp dir that was never created. For the control direction **delete**
`VICE_REQUIRE_ACME` from the copied env (do not set it to an empty string —
assert the gate's actual unset behaviour) and keep the same nonexistent
`ACME_BIN`.

**Assert three things in the FAIL direction, not one.** Assert `status !== 0`
— never `status === 1`, because TAP runner exit codes are not a contract worth
pinning. Assert the combined output matches the gate's own refusal wording, read
from the moved `assertAcmeRequiredIfEnvSet` body rather than retyped from memory
(a typo'd import path otherwise produces a passing test for the wrong reason).
Then assert the control direction exits zero. This two-direction shape is the
same one recorded when the gate was first built: env set plus nonexistent binary
gave one failure and one skip; env unset with the same binary gave zero failures
and one skip.

**Cost, stated rather than smuggled.** Two child processes per suite run, roughly
one to two seconds. D-05 budgeted one; the control direction earns the second,
because a FAIL with no paired control cannot distinguish "the gate fired" from
"the harness broke".

For the `files[]` assertion, copy the shape at `r2000-verify.test.ts:187-194`:
read and parse `package.json` from `HERE`, assert `files` is an array, then
assert membership of the new module's filename is `false` with a message saying
it is test-only and must never ship in the published tarball. Read `files[]`
directly here rather than through any shared enumerator — this is a
single-membership check, not a module enumeration.

Use the repo's standard test import block: `test` from `node:test`, `assert` from
`node:assert/strict`, `HERE` derived from `dirname(fileURLToPath(import.meta.url))`.
Do **not** add this file to `MANUAL_ONLY_TESTS` — `test-gate.test.ts`
auto-discovers new `*.test.ts` files into the automated set, which is correct.
  </action>
  <verify>
    <automated>cd src/mcp/vice && npm run typecheck && node --test acme-gate.test.ts test-gate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && node --test acme-gate.test.ts` exits 0 with at least 5 passing tests and 0 failures
    - `grep -c 'mkdtempSync' src/mcp/vice/acme-gate.test.ts` returns at least `1` and `grep -c 'rmSync' src/mcp/vice/acme-gate.test.ts` returns at least `1`
    - `grep -c 'finally' src/mcp/vice/acme-gate.test.ts` returns at least `1` — the temp dir is removed on the failure path too
    - `grep -cE 'status *(!==|===) *1\b' src/mcp/vice/acme-gate.test.ts` returns `0` — the exit code is asserted non-zero, never pinned to 1
    - `grep -c 'tmpdir()' src/mcp/vice/acme-gate.test.ts` returns at least `1` and `ls src/mcp/vice/probe.test.mjs` fails with "No such file" — the probe file never lands in the module directory
    - Deleting the `VICE_REQUIRE_ACME` guard from the moved `assertAcmeRequiredIfEnvSet` body makes `node --test acme-gate.test.ts` go RED (run this break-and-restore probe once, restore the body, and record both observations in the SUMMARY) — a gate that cannot be made to fail has not been written
    - `cd src/mcp/vice && node --test test-gate.test.ts` exits 0 — the new test file lands cleanly in the auto-discovered automated set, with no `MANUAL_ONLY_TESTS` edit
    - `grep -c 'acme-gate.ts' src/mcp/vice/package.json` returns `0` and the test's own `files[]`-absence assertion passes
  </acceptance_criteria>
  <done>`node --test acme-gate.test.ts` proves, in both directions, that a nonexistent ACME binary under `VICE_REQUIRE_ACME=1` makes a run FAIL and that the same binary with the env var unset makes it skip; the new module's `files[]` absence is asserted mechanically; the temp probe file is created outside the module directory and removed in a `finally`; and one break-and-restore probe has been observed RED. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: Correct every comment that still names the old module as the ACME seam</name>
  <files>src/mcp/vice/disasm-roundtrip.test.ts, src/mcp/vice/skill-acme-build-cli.test.ts, src/mcp/vice/r2000-cli.test.ts, src/mcp/vice/r2000-answer-key.test.ts, src/mcp/vice/r2000-test-gate.ts</files>
  <read_first>
    - `src/mcp/vice/disasm-roundtrip.test.ts:58-66` — the gate prose block naming the old seam and the migration todo.
    - `src/mcp/vice/skill-acme-build-cli.test.ts:13-18` — the header paragraph naming the old seam by path.
    - `src/mcp/vice/r2000-cli.test.ts:677-686` — the ACME-gate prose block naming the old seam and its bounded-probe rationale.
    - `src/mcp/vice/r2000-answer-key.test.ts:218-224` — the CI prose block naming the old seam as "the sanctioned seam".
    - `src/mcp/vice/r2000-test-gate.ts:1-40` — the surviving header, to confirm no sentence still claims the file holds two gates.
    - `.github/workflows/ci.yml:104-145` — read to CONFIRM (not to edit) that `:114`'s comment names a test file rather than the gate module and that `:140`'s env binding is unchanged.
  </read_first>
  <action>
Four comment blocks and possibly one header sentence become factually false the
moment Task 1 lands, and this project's recorded preference is to rewrite the
record rather than route around it. Correct them in this commit.

In each of `disasm-roundtrip.test.ts`, `skill-acme-build-cli.test.ts`,
`r2000-cli.test.ts` and `r2000-answer-key.test.ts`, the prose that names the
shared ACME seam by its old module path must name `acme-gate.ts` instead.
Preserve every other claim each block makes — `disasm-roundtrip.test.ts`
originated the `ACME_BIN`/`VICE_REQUIRE_ACME` convention and says so;
`skill-acme-build-cli.test.ts` asserts the seam's value is used rather than a
second default and says so; `r2000-cli.test.ts` records that the seam's probe
passes a bounded 10-second timeout where its own former local copy passed none;
`r2000-answer-key.test.ts` records that CI installs ACME and sets
`VICE_REQUIRE_ACME`, so a missing binary hard-FAILs there. Keep the pointer to
the migration todo that recorded why the gate was consolidated in the first
place — that record is still true and is now the prehistory of this move.

Then re-read the surviving header of the module the half left, and expect to
change nothing. Measured at `6c1f569`, `r2000-test-gate.ts:1-33` describes
**one** gate — "the ONE place the D-11 regenerator2000 availability gate is
implemented" — so there is no two-halves sentence to narrow, and this step is a
confirmation, not an edit. If a sentence claiming both gates is nevertheless
found, narrow it to the regenerator2000 gate and stop there. **Do not add a
sentence pointing at `acme-gate.ts` as the sibling seam**, tempting as it is:
Task 1's `grep -ci acme src/mcp/vice/r2000-test-gate.ts` returns `0` criterion is
this plan's shim detector, and it makes no exception for a courtesy pointer — any
ACME text in that file, prose included, fails it. That is the intended trade:
the detector's value is that it needs no judgement about which mentions are
benign. The reader arriving at the old name is served from the other end
instead — `acme-gate.ts`'s own header names where the half came from, and the
four importers' corrected prose names `acme-gate.ts` as the seam.

Finally, record the `ci.yml` binding audit as a stated finding rather than an
assumed one, in the plan SUMMARY: `.github/workflows/ci.yml` contains no
occurrence of `ACME_BIN` at all; `VICE_REQUIRE_ACME: "1"` sits at `:140`
immediately above `run: npm test` at `:141`; `:114`'s comment names
`disasm-roundtrip.test.ts`, which is still correct after this plan. Therefore
SEAM-01's "repointed in the same commit" clause is discharged by confirming the
env-var names did not move — not by a code change — and `ci.yml` is left
byte-identical.

Do not introduce a `Phase 27` string into any of these comment blocks:
`docs-dangling-refs.test.ts` forbids a phase number inside a string literal in a
shipped module, and `comment-phase-pointers.test.ts` scans for dangling
phase-comment assignments. Refer to work by requirement id or by filename, which
is this repo's established form.
  </action>
  <verify>
    <automated>cd src/mcp/vice && npm run typecheck && node --test docs-dangling-refs.test.ts comment-phase-pointers.test.ts hop-chain-comments.test.ts docs-linerefs.test.ts ci-guardrails.test.mjs disasm-roundtrip.test.ts skill-acme-build-cli.test.ts r2000-cli.test.ts r2000-answer-key.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'acme-gate.ts' src/mcp/vice/disasm-roundtrip.test.ts src/mcp/vice/skill-acme-build-cli.test.ts src/mcp/vice/r2000-cli.test.ts src/mcp/vice/r2000-answer-key.test.ts` reports at least `2` for each file (the import plus the corrected prose)
    - <!-- planner-discipline-allow: r2000-test-gate.ts -->
      `grep -c 'r2000-test-gate.ts' src/mcp/vice/disasm-roundtrip.test.ts src/mcp/vice/skill-acme-build-cli.test.ts src/mcp/vice/r2000-answer-key.test.ts` reports `0` for each — none of the three still names the old module anywhere
    - `grep -c 'ACME_BIN/VICE_REQUIRE_ACME' src/mcp/vice/disasm-roundtrip.test.ts` returns at least `1` — the origination claim survives the rewrite
    - `grep -c '10s spawnSync timeout' src/mcp/vice/r2000-cli.test.ts` returns at least `1` — the bounded-probe rationale survives
    - `grep -cE 'Phase +[0-9]' src/mcp/vice/disasm-roundtrip.test.ts src/mcp/vice/skill-acme-build-cli.test.ts src/mcp/vice/r2000-cli.test.ts src/mcp/vice/r2000-answer-key.test.ts` reports `0` new occurrences relative to `git show HEAD~1:` for the same files
    - `git diff --stat -- .github/workflows/ci.yml` is empty, and `grep -c 'ACME_BIN' .github/workflows/ci.yml` returns `0`
    - `grep -n 'VICE_REQUIRE_ACME' .github/workflows/ci.yml` reports line `140`
    - `cd src/mcp/vice && node --test docs-dangling-refs.test.ts comment-phase-pointers.test.ts hop-chain-comments.test.ts ci-guardrails.test.mjs` exits 0
    - The SUMMARY states the ci.yml binding audit result in the three parts above and states that no ci.yml edit was made
  </acceptance_criteria>
  <done>No comment anywhere in the four importers still names the old module as the ACME seam, every other claim those blocks made survives intact, the surviving header no longer describes a module holding two gates, the four comment guards and the CI guardrail tests are green, and `ci.yml` is byte-identical with its binding audit recorded as a stated finding. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI environment → test harness | `.github/workflows/ci.yml:140` sets `VICE_REQUIRE_ACME`; the harness reads it by name. A rename on either side silently converts a hard-FAIL into a skip, and both sides stay green. |
| `process.env.ACME_BIN` → `spawnSync` argv | An externally-controlled binary **name** reaches a subprocess launch. Untrusted-ish input crosses into process execution here. |
| `package.json` `files[]` → published npm tarball | Anything listed ships to consumers of `@henols/vice-mcp`. A test-only module leaking in enlarges the published surface. |
| temp filesystem → child `node --test` | The new test writes an executable module to a temp dir and runs it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01-01 | Tampering | `probeAcme()` in `acme-gate.ts` — env-supplied binary name reaching `spawnSync` | medium | mitigate | Move the body byte-identically so the argv-array form (`spawnSync(bin, ["--version"], …)`) is preserved; never a shell string, never `execSync`/`execFileSync`, never `shell: true`. Asserted by two greps in Task 1. This is the same discipline recorded as `T-04-06-01` when the probe was first written. |
| T-27-01-02 | Repudiation | The ACME hard-FAIL gate degrading into a silent skip during or after the move | high | mitigate | Criterion 1's committed child-process test **plus** its non-vacuity control (Task 2), so a run that skips instead of failing is caught by a test rather than inferred from a green CI log. `ACME_BIN` / `VICE_REQUIRE_ACME` keep byte-identical names (D-03), and a break-and-restore probe is observed RED once. |
| T-27-01-03 | Information Disclosure | Test-only `acme-gate.ts` leaking into the published npm tarball | medium | mitigate | Mechanical `files[]`-absence assertion in `acme-gate.test.ts`, copied from the established `r2000-verify.test.ts:187-194` shape (D-04). Enforced on every suite run, not asserted once. |
| T-27-01-04 | Tampering | The child probe file written to a predictable path, or left inside the module directory where the next run collects it | low | mitigate | `mkdtempSync` under `tmpdir()` (unpredictable name, outside `src/mcp/vice/`) plus `rmSync(…, { recursive: true, force: true })` in a `finally`. Asserted by grep and by `test-gate.test.ts` staying green. |
| T-27-01-05 | Spoofing | A banner-matching probe accepting a non-ACME binary that happens to print the substring | low | accept | Pre-existing property of the moved code, unchanged by this plan; the probe gates *whether an external-oracle test may skip*, never a security decision, and the phase's contract is that behaviour is preserved rather than improved. Recorded so it is a decision rather than an oversight. |
| T-27-01-SC | Tampering | npm / pip / cargo package installation | low | accept | **No package-manager install task exists in this plan or this phase.** `src/mcp/vice/package.json` `dependencies` and `devDependencies` are unchanged; the only `package.json` diff anywhere in the phase is `files[]` entries, and this plan changes `package.json` not at all. RESEARCH.md's Package Legitimacy Audit records the same: zero external packages, so `ENGINEERING_RULES.md` §4's dependency bar is not triggered and there is nothing to audit. |
</threat_model>

<verification>
Per task, from `src/mcp/vice`:
- `npm run typecheck` — the mechanical detector for a half-done import split.
- `node --test <the affected files>` — sub-30-second feedback.

Plan gate, from `src/mcp/vice`:
- `npm run typecheck` exits 0.
- `node --test acme-gate.test.ts disasm-roundtrip.test.ts skill-acme-build-cli.test.ts r2000-cli.test.ts r2000-answer-key.test.ts r2000-verify.test.ts hostpath-consumers.test.ts test-gate.test.ts docs-dangling-refs.test.ts comment-phase-pointers.test.ts hop-chain-comments.test.ts assumption-label-discipline.test.ts ci-guardrails.test.mjs` exits 0.
- <!-- planner-discipline-allow: r2000 -->
  `test "$(git diff --diff-filter=D --name-only 6c1f569..HEAD -- src/mcp/vice scripts | grep -c r2000)" = "0"` exits 0 — wrapped in `test "$(…)"` deliberately: the bare pipeline *prints* `0` but *exits 1*, because `grep -c` returns no-match status even when its count is the count we want, so chaining the bare form with `&&` produces a false red on a correct tree; nothing was deleted.

**Forbidden as evidence:** `npm run test:automated`. It runs the narrowed gate
that skips the nine `MANUAL_ONLY_TESTS` entries, including the only end-to-end
wire proof in the suite. The whole-glob `npm test` for the phase is run in
`27-05-PLAN.md`; this plan's gate is the targeted file list above.
</verification>

<success_criteria>
- The five ACME symbols are exported from `src/mcp/vice/acme-gate.ts` under byte-identical names, and no ACME text or re-export survives in the module they left.
- All four importers resolve against the new module, with `r2000-cli.test.ts`'s mixed statement split into two rather than rewritten.
- A committed child-process test observes a non-zero exit and the gate's own refusal wording under `VICE_REQUIRE_ACME=1` with a nonexistent `ACME_BIN`, and observes a zero exit for the same binary with the env var unset.
- `acme-gate.ts` is absent from `package.json` `files[]`, asserted mechanically in its own test file.
- `.github/workflows/ci.yml` is byte-identical, with its env-var-only binding recorded as a stated finding.
- No `r2000` module is deleted or renamed; `hostpath-consumers.test.ts` and `r2000-verify.test.ts` are untouched and green.
</success_criteria>

<output>
Create `.planning/phases/27-shared-seams-extracted/27-01-SUMMARY.md` when done.
Record in it: the ci.yml binding audit's three parts; the break-and-restore probe
observation (RED, then restored); and the exact commands run with their results.
</output>
