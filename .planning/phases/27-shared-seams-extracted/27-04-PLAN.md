---
phase: 27-shared-seams-extracted
plan: 04
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - src/mcp/vice/shipped-modules.ts
  - src/mcp/vice/shipped-modules.test.ts
  - src/mcp/vice/docs-dangling-refs.test.ts
  - src/mcp/vice/r2000-spawn-seam.test.ts
  - src/mcp/vice/stock-dispatch.test.ts
  - src/mcp/vice/comment-phase-pointers.test.ts
  - src/mcp/vice/hop-chain-comments.test.ts
autonomous: true
requirements: [SEAM-02]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "shippedTsModules() exists once in the tree, in src/mcp/vice/shipped-modules.ts, and all four guard test files that held a hand-copy now import it — the four copies are deleted, not left beside an import"
    - "The extracted helper cannot silently return a short list: a files[] entry naming a file that does not exist on disk makes it throw a named error rather than shrink the scanned set, proven by a planted stale entry"
    - "codeOnly() exists once, in the same shared helper, and still strips both comment text and string/template-literal bodies — the property that makes r2000-spawn-seam.test.ts's spawn-site scan trustworthy"
    - "shipped-modules.ts is absent from package.json's files[] (test-only) and its filename does not match the *.test.* glob the test script runs, so it is imported by test files and never collected as one"
    - "Both recorded statements of the no-import-between-sibling-guard-tests convention are rewritten in this plan's commits, so the record states the convention's scope once and does not contradict itself"
  prohibitions: []
  artifacts:
    - path: "src/mcp/vice/shipped-modules.ts"
      provides: "The ONE files[]-derived shipped-module enumerator and the ONE full comment-and-string-literal stripper, both test-only"
      exports: ["shippedTsModules", "codeOnly"]
      min_lines: 70
    - path: "src/mcp/vice/shipped-modules.test.ts"
      provides: "The files[]-absence assertion, the stale-entry planted violation, and the stripper's template-literal cases"
      min_lines: 55
  key_links:
    - from: "src/mcp/vice/docs-dangling-refs.test.ts"
      to: "src/mcp/vice/shipped-modules.ts"
      via: "named import replacing its local copy — the canonical body's original home"
      pattern: 'from "\./shipped-modules\.ts"'
    - from: "src/mcp/vice/r2000-spawn-seam.test.ts"
      to: "src/mcp/vice/shipped-modules.ts"
      via: "named import of both helpers, replacing two local copies"
      pattern: 'from "\./shipped-modules\.ts"'
    - from: "src/mcp/vice/stock-dispatch.test.ts"
      to: "src/mcp/vice/shipped-modules.ts"
      via: "named import replacing its local copy; its own separate line-oriented stripper is deliberately left alone"
      pattern: 'from "\./shipped-modules\.ts"'
    - from: "src/mcp/vice/comment-phase-pointers.test.ts"
      to: "src/mcp/vice/shipped-modules.ts"
      via: "named import replacing its local copy, together with the rewrite of the convention comment that forbade exactly this"
      pattern: 'from "\./shipped-modules\.ts"'
---

<objective>
Extract the four hand-copied `shippedTsModules()` enumerators and the single
`codeOnly()` stripper into one test-only shared helper,
`src/mcp/vice/shipped-modules.ts`, repoint every copy, and rewrite **both**
recorded statements of the convention that previously argued for the duplication
(D-15, D-16, and Correction C-4).

Purpose: four identical copies of a `package.json`-reading module enumerator is
exactly the divergence hazard the gate module's own header was written to stop —
one copy gets its filter changed, another loses its existence assertion, and
nobody notices because each guard still passes. And `codeOnly()`, the only
stripper in the tree that correctly blanks template-literal bodies, currently
lives inside a file a later phase deletes.
Output: one shared helper with its own test file, four deleted copies, and a
record that states the convention's scope once rather than twice with different
wording. Zero `r2000` modules deleted.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-shared-seams-extracted/27-CONTEXT.md
@.planning/phases/27-shared-seams-extracted/27-RESEARCH.md
@.planning/phases/27-shared-seams-extracted/27-PATTERNS.md
@.planning/phases/27-shared-seams-extracted/27-01-SUMMARY.md
</context>

<research_corrections_applied>
RESEARCH.md overrides CONTEXT.md on every matter of fact. What binds this plan,
re-verified against the tree at `6c1f569` while planning:

- **C-3 — there is exactly ONE `codeOnly()`, not two, and one of the two sites
  the roadmap cites deliberately declines it.** Measured: a single definition at
  `r2000-spawn-seam.test.ts:65`, consumed at `:276` and `:493` in that same file
  and nowhere else. The two `stock-dispatch.test.ts` lines the roadmap names are
  **prose** — `:2889` is a header comment, and `:2919` is a doc comment that
  explicitly explains why it uses a simpler line-oriented stripper
  (`nonCommentLines()` at `:2925`) instead, because the literal it searches for is
  itself a string and blanking string content would make it unobservable.
  **Consequence: the divergence argument D-15 makes applies to
  `shippedTsModules()` alone.** `codeOnly()` has no divergence hazard because it
  has no second copy.
  **Decision (RESEARCH Open Question OQ-1, resolved here): extract `codeOnly()`
  too, on a *survival* rationale, not a divergence one.** Its only home is an
  `r2000-*.test.ts` file a prefix deletion removes, so today it dies with that
  file; it is the only implementation in the tree that correctly strips
  template-literal bodies, which is real and non-obvious logic that was
  measured into existence after a regex extractor missed a real violation; and it
  costs one function move with one consuming file, the cheapest repoint in the
  phase. **State the survival rationale explicitly in the helper's header** so a
  later reader does not look for the divergence that motivated its sibling.
  **And record the deliberate limit:** the comment-extractor family is out of
  scope — the three byte-identical local consts in `disasm-decoder.test.ts:308`,
  `disasm-renderer.test.ts:346` and `disasm-opcodes.test.ts:394`, the regex
  variant at `r2000-tools.test.ts:201`, `stock-dispatch.test.ts:2925`'s
  `nonCommentLines()`, and `hop-chain-comments.test.ts`'s comment extractor. Each
  does a different job and `r2000-tools.test.ts:193-201` states in code why.
  This is a recorded decision, not a partial repoint by omission.
- **C-4 — the convention is recorded in TWO places, and clarifying one leaves the
  record self-contradictory.** `comment-phase-pointers.test.ts:53-59` is the
  statement D-16 names, and it is exact. A second, independently-worded statement
  exists at `hop-chain-comments.test.ts:46-50`, re-verified verbatim while
  planning. The second is worded **more broadly** — "each guard test owns its own
  scan end-to-end", with no "between sibling guard tests" qualifier — and ends
  with a positive instruction to keep duplicating: "Keep the copy verbatim so a
  future reader can diff the two." **Both must be rewritten in this plan.**
  Note that `hop-chain-comments.test.ts` duplicates the *comment extractor*, not
  the module enumerator, so nothing there is repointed — only its comment.
- **All four `shippedTsModules()` definitions re-verified** at
  `docs-dangling-refs.test.ts:353` (the canonical body),
  `r2000-spawn-seam.test.ts:176`, `stock-dispatch.test.ts:2902` and
  `comment-phase-pointers.test.ts:400`. Call-site counts measured while planning:
  3, 7, 4 and 7 respectively — every one must resolve after the copies are
  deleted, and `tsc --noEmit` is the check.
- **The existence assertion inside the enumerator is load-bearing.**
  `docs-dangling-refs.test.ts:344-352` records the reason: a `files[]` entry that
  does not exist on disk must FAIL rather than let the scanned set silently
  shrink. Extracted, the helper can no longer borrow a caller's `assert`
  implicitly — it must take one or throw its own named error. It **must not** be
  able to return a short list quietly.
- **The rationale travels with the code, and gains a second example.**
  `r2000-spawn-seam.test.ts:155-175`'s doc comment explains why the set is
  `files[]`-derived rather than `readdirSync`-derived, using the regenerator2000
  gate module as the motivating test-only-module-that-spawns-a-real-binary
  example. After `27-01`, `acme-gate.ts` is a **second** instance of exactly that
  shape, so the moved comment should name both.
</research_corrections_applied>

<assumption_delta_decision>
**Scope of this plan: `no-change`.**

The detector fired for Phase 27 on three prose signals. There is a *superficial*
plural transition here — four copies of one helper become one — but that is
de-duplication, not a generalisation of an identity model: the helper's inputs,
outputs and semantics are unchanged, and no caller gains or loses a variant. The
one thing genuinely being generalised is a **convention statement**, and this
plan narrows rather than widens it: "no guard test imports another guard test"
stays in force; importing a neutral non-test helper module is recorded as a
different thing, with the precedent already in the tree
(`r2000-spawn-seam.test.ts:53` already imports a neutral non-test helper exactly
that way). That narrowing is recorded in both places the convention is written
down, which is the whole of Task 3.

The phase's one genuine identity-model transition (the block-class vocabulary) is
resolved as **promote** in `27-02-PLAN.md`.
</assumption_delta_decision>

<probe_accounting>
Spec-less probe fallback (no SPEC.md exists for this phase). The deterministic
edge probe surfaced **12** applicable edges, all `unresolved`: 2 × SEAM-01,
**6** × SEAM-02, **4** × SEAM-03 (recounted from the coverage report while
planning — not 2 / 5 / 5).

**This plan owns none of the 12 rows.** Its requirement id is SEAM-02, whose six
probe rows are authored in `27-05-PLAN.md`, where the classification record and
its enforcing test are built and where every one of those rows has a concrete
referent. Authoring copies here would double-count against the no-silent-drop
equality check (12 in / 12 accounted), which is the property that must hold. This
plan's `must_haves.truths` are derived goal-backward from its own deliverable.

**Prohibition recall (§B, in-prompt).** Stage 1 over-produced; Stage 2 dropped
every candidate. The strongest raw candidates — "must not let the shared helper
return a short list", "must not leave a copy beside the import", "must not
consolidate strippers that do different jobs" — are routine engineering or
already-locked design, owned by this plan's `must_haves.truths` and
`<threat_model>`. The phase's kept prohibitions live in `27-01-PLAN.md` and
`27-05-PLAN.md`. `prohibitions: []` is the correct precision outcome, not an
omission.
</probe_accounting>

<artifacts_this_phase_produces>
| New path | Symbols | New name? |
|---|---|---|
| `src/mcp/vice/shipped-modules.ts` | `shippedTsModules`, `codeOnly` | **No** — byte-identical names, moved from `docs-dangling-refs.test.ts:353` and `r2000-spawn-seam.test.ts:65` |
| `src/mcp/vice/shipped-modules.test.ts` | new test titles only | new file |

**Name resolution recorded.** `shipped-modules.ts` is the one name RESEARCH.md
left open. Its constraint set is fully determined: a plain `.ts` (never matching
`*.test.*`, or the `test` script would collect it), absent from `files[]`
(test-only), and free in `src/mcp/vice/`. Collision check run while planning —
`ls src/mcp/vice/ | grep -iE '^(shipped|module)'` returns nothing, and no file
named `shipped-modules.*` exists. Chosen over `files-modules.ts` and
`module-set.ts` because the exported enumerator's own name is
`shippedTsModules()` and the filename should read as its home.

Removed: four local `shippedTsModules()` definitions and one local `codeOnly()`.
No new CLI flag, env var, tool name, wire name or stored record.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shipped-modules.ts with both helpers, and its own test file</name>
  <files>src/mcp/vice/shipped-modules.ts, src/mcp/vice/shipped-modules.test.ts</files>
  <behavior>
    - Test 1: `shippedTsModules()` returns every `files[]` entry ending `.ts` or `.mts`, and nothing else — no `.json`, no `.md`, no directory entry.
    - Test 2 (the load-bearing one): given a `files[]` naming an entry that does not exist on disk, the helper **throws** a named error whose message says to update `files[]` rather than letting the scanned set shrink silently. It must not return a short list.
    - Test 3: a `files[]` with no `.ts`/`.mts` entries returns an empty array without throwing — the empty case is distinguishable from the missing-file case.
    - Test 4: `codeOnly()` blanks the body of a single-quoted, a double-quoted and a **template-literal** string, so a forbidden token hidden inside any of the three is not reported.
    - Test 5: `codeOnly()` blanks `//` line comments, `/* */` block comments including a same-line open-and-close, and an unterminated block comment continuing across lines.
    - Test 6: `codeOnly()` leaves real code identifiers and call syntax intact, so a genuine occurrence still matches after stripping.
    - Test 7 (structural): `package.json`'s `files[]` does **not** include `shipped-modules.ts`.
    - Test 8 (structural): the module's filename does not match the `*.test.*` glob, and the module registers no `node:test` `test(...)` call at import time.
  </behavior>
  <read_first>
    - `src/mcp/vice/docs-dangling-refs.test.ts:344-365` — the canonical enumerator body at `:353` and, above it, the doc comment recording why the existence assertion is load-bearing.
    - `src/mcp/vice/r2000-spawn-seam.test.ts:20-80` — the header's stripper rationale at `:34` and the `codeOnly()` definition at `:65`, a character state machine.
    - `src/mcp/vice/r2000-spawn-seam.test.ts:155-178` — the doc comment explaining the `files[]`-derived-not-`readdirSync`-derived choice, which travels with the enumerator and gains a second example.
    - `src/mcp/vice/fork-deleted-tools.ts` — the whole file (21 lines). Its closing paragraph is this repo's canonical statement of why a shared helper is a plain `.ts` and never a `.test.ts`: importing a `.test.ts` module for its exports also re-runs every top-level `test(...)` call it registers. Reproduce that reasoning.
    - `src/mcp/vice/r2000-test-gate.ts:25-33` — the TEST-ONLY and non-`*.test.*`-name paragraphs, the second exemplar of the same two rules.
    - `src/mcp/vice/r2000-verify.test.ts:187-194` — the mechanical `files[]`-absence assertion shape (read for shape; do NOT edit that file).
    - `src/mcp/vice/hostpath-consumers.test.ts:218-232` — the planted-violation shape: run the **same predicate the real scan uses** against a synthetic violation and a synthetic clean case.
  </read_first>
  <action>
Create `src/mcp/vice/shipped-modules.ts` holding both test-only helpers, and
`src/mcp/vice/shipped-modules.test.ts` proving them.

**Name.** `shipped-modules.ts`, confirmed free. It is a plain `.ts` on purpose:
the `test` script runs `node --test '*.test.*'`, so a helper matching that glob
would be collected as a test file, and — separately — importing a `.test.ts`
module for its exports re-runs every top-level `test(...)` call it registers as
an import side effect, silently duplicating that file's execution inside whichever
file imports it. Both rules are already recorded in the tree; state them in this
module's header too.

**`shippedTsModules()`.** Move the canonical body from
`docs-dangling-refs.test.ts:353-362` verbatim: read and parse `package.json` from
the module directory, filter `files[]` to entries ending `.ts` or `.mts`, then —
for each surviving entry — assert it exists on disk before returning. The
existence check is the point of the function, not a nicety: without it, a stale
`files[]` entry makes every consuming guard scan a quietly shorter set and pass.
Because the helper no longer sits inside a test file, it cannot borrow the
caller's `assert` implicitly. Give it one of two shapes and keep it consistent:
either take the assertion library as a parameter, the way the gate's own
`assertAcmeRequiredIfEnvSet` already does, or throw its own named `Error`. Do
**not** let it return a short list under any circumstance, and keep the failure
message's existing wording — "names X but it does not exist on disk; update
`files[]` rather than letting the scanned set shrink silently" — because that
sentence is the whole lesson.

Move the `files[]`-derived-rather-than-`readdirSync`-derived rationale from
`r2000-spawn-seam.test.ts:155-175` into this module's doc comment for the
function, and extend it: the broader directory listing also catches genuinely
test-only helpers that merely fail to end in `.test.ts` by name, and there are
now **two** modules of exactly that shape — the regenerator2000 gate, and the
ACME gate `27-01` created. Naming both is the point; naming one leaves the reader
thinking it is a special case.

**`codeOnly()`.** Move the character state machine from
`r2000-spawn-seam.test.ts:65` verbatim. It strips comment text **and**
string/template-literal bodies. Do not simplify it into a regex or a line filter:
the tree already contains five simpler strippers, and this one exists because a
regex extractor was measured to miss a real violation sitting inside a template
literal. Carry its rationale comment across, and add the **survival** reason for
its extraction, stated plainly: it has exactly one consuming file today and
therefore no divergence hazard, but that file is one a later phase deletes, so
without this move the tree's only correct template-literal-aware stripper goes
with it.

**Header.** Three-part single-seam shape. State that both helpers are TEST-ONLY
and must never appear in `files[]` nor be imported by a production module, with
this module's own test file named as the mechanical enforcement. State the
non-`*.test.*`-name rule and the import-side-effect reason. `WHAT NOT TO DO`:
never add a fifth copy of the enumerator anywhere; never remove the existence
assertion; and never fold in the comment-extractor family — list the six sites by
filename and line and say each does a different job, with a pointer to
`r2000-tools.test.ts:193-201`, which states in code why comment-only stripping is
the right tool when the literal being searched for is itself a string. That list
is what makes the partial consolidation a recorded decision rather than an
oversight.

No `[ASSUMED]` label — `assumption-label-discipline.test.ts` scans authored
non-test `.ts` directly under `src/mcp/vice/`. No `hostpath.ts` or
`containerpath.ts` import. Refer to work by requirement id or filename, never by
a phase number, and never put a phase number inside a string literal.

**The test file** covers the eight behaviours above. For tests 1-3, build
synthetic `files[]` inputs rather than depending on the real `package.json`'s
current contents — a test that asserts against the live array would go red every
time the array legitimately changes, which is precisely the pinned-total failure
this project has already been bitten by. Drive the helper's own predicate against
the synthetic inputs, using the planted-violation shape at
`hostpath-consumers.test.ts:218-232`: the same code path the real scan uses,
exercised against one known-bad and one known-good case, with the failure message
saying that if the bad case does not throw, the real guard cannot catch a real
violation. For test 7 copy the `r2000-verify.test.ts:187-194` shape and assert
absence.
  </action>
  <verify>
    <automated>cd src/mcp/vice && npm run typecheck && node --test shipped-modules.test.ts test-gate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^export function (shippedTsModules|codeOnly)' src/mcp/vice/shipped-modules.ts` returns `2`
    - `grep -c 'update files\[\] rather than letting the scanned set shrink silently' src/mcp/vice/shipped-modules.ts` returns `1` — the failure message moved byte-identically
    - `grep -cE '/\\\.\(ts\|mts\)\$/' src/mcp/vice/shipped-modules.ts` returns at least `1` — the extension filter moved unchanged
    - `grep -c 'acme-gate.ts' src/mcp/vice/shipped-modules.ts` returns at least `1` — the moved rationale names the second instance of the unshipped-spawn-site shape
    - `grep -c 'r2000-tools.test.ts' src/mcp/vice/shipped-modules.ts` returns at least `1` — the deliberate out-of-scope list points at the in-code statement of why the comment-extractor family stays separate
    - `grep -cE '(disasm-decoder|disasm-renderer|disasm-opcodes|nonCommentLines|hop-chain-comments)' src/mcp/vice/shipped-modules.ts` returns at least `4` — the out-of-scope sites are named, not merely alluded to
    - `grep -cE 'from "\./(hostpath|containerpath)' src/mcp/vice/shipped-modules.ts` returns `0` and `grep -c 'ASSUMED' src/mcp/vice/shipped-modules.ts` returns `0`
    - `grep -c 'node:test' src/mcp/vice/shipped-modules.ts` returns `0` — the helper registers no test at import time
    - `cd src/mcp/vice && node --test shipped-modules.test.ts` exits 0 with at least 8 passing tests and 0 failures
    - `node -e 'const f=require("./src/mcp/vice/package.json").files; process.exit(f.includes("shipped-modules.ts")?1:0)'` exits 0 — the helper is absent from `files[]`
    - `git diff --stat -- src/mcp/vice/package.json` is empty — this plan changes `package.json` not at all
    - **Planted-violation observation:** the stale-`files[]`-entry test is confirmed to fail when the helper's existence assertion is removed. Delete the assertion, observe `node --test shipped-modules.test.ts` RED, restore it, and record both observations in the SUMMARY.
    - `cd src/mcp/vice && node --test test-gate.test.ts` exits 0 with no `MANUAL_ONLY_TESTS` edit
  </acceptance_criteria>
  <done>`src/mcp/vice/shipped-modules.ts` exports both helpers with byte-identical names, bodies and failure wording; the enumerator cannot return a short list and its rationale names both unshipped-spawn-site modules; the stripper carries its survival rationale and an explicit list of the six deliberately-excluded comment extractors; the module is absent from `files[]`, registers no test at import, and its eight tests pass with the existence assertion observed load-bearing. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint all four enumerator copies and both stripper consumers, deleting each local copy</name>
  <files>src/mcp/vice/docs-dangling-refs.test.ts, src/mcp/vice/r2000-spawn-seam.test.ts, src/mcp/vice/stock-dispatch.test.ts, src/mcp/vice/comment-phase-pointers.test.ts</files>
  <read_first>
    - `src/mcp/vice/docs-dangling-refs.test.ts:340-375` — the copy at `:353`, its doc comment, and its three call sites.
    - `src/mcp/vice/r2000-spawn-seam.test.ts:45-80` — the import block at `:50-53` (the target import shape) and the stripper at `:65`.
    - `src/mcp/vice/r2000-spawn-seam.test.ts:150-200` and `:270-300` and `:485-500` — the enumerator at `:176`, its seven call sites, and the stripper's two consumers at `:276` and `:493`.
    - `src/mcp/vice/stock-dispatch.test.ts:2885-2960` — the copy at `:2902`, its four call sites, and — immediately below — `nonCommentLines()` at `:2925` with the doc comment explaining why it is NOT the full stripper. That function stays.
    - `src/mcp/vice/comment-phase-pointers.test.ts:390-450` — the copy at `:400` and its seven call sites.
    - `src/mcp/vice/shipped-modules.ts` — the helper as Task 1 left it, for the exact exported signatures.
  </read_first>
  <action>
Delete each local copy and replace it with an import of the shared helper. Six
deletions across four files: four enumerator copies, one stripper definition, and
— in the file that held both — a single import statement covering the pair.

In each of the four files, add one `import { … } from "./shipped-modules.ts";`
statement in the existing import block (real `.ts` extension, one statement per
source module, side by side — `r2000-spawn-seam.test.ts:50-53` is the target
shape and is already in the tree), then **delete the local function body
entirely**. Do not leave a local wrapper that forwards to the import: the phase's
recorded principle is no shims and no partial repoints, because a half-move
leaves the divergence hazard in place while looking finished.

Where the local copy carried a doc comment that is now duplicated in the shared
helper, delete the duplicated rationale and leave a one-line pointer naming the
helper — except for the copy in the file the canonical body came from, whose doc
comment recorded the existence-assertion lesson: keep a one-sentence trace of
that lesson at the call site so the reader of that guard still learns why the
scanned set cannot shrink, and let the full statement live in the helper.

If the extracted enumerator takes an assertion library as a parameter, every one
of the twenty-one call sites across the four files must pass it; if it throws its
own error, none may. Whichever shape Task 1 chose, apply it uniformly — a mixed
convention across four guard files is the divergence this whole task exists to
end. Call-site counts measured while planning, so nothing is missed: three in
`docs-dangling-refs.test.ts`, seven in `r2000-spawn-seam.test.ts`, four in
`stock-dispatch.test.ts`, seven in `comment-phase-pointers.test.ts`.

In `r2000-spawn-seam.test.ts`, the same import also takes the stripper, and both
of its consumers stay exactly as written — only the definition leaves. That
file's header at `:34` explains why full stripping is needed there rather than a
comment filter; keep that explanation and point it at the helper.

**Leave `nonCommentLines()` in `stock-dispatch.test.ts:2925` completely alone.**
Its own doc comment records the reasoning: it is line-oriented on purpose, because
that file searches for literals that are themselves strings, so blanking string
content would make them unobservable. Consolidating it would delete a measured
decision. Do not add it to the shared helper and do not repoint its callers.

Each guard's behaviour must be identical after the repoint. The mechanical checks
are `tsc --noEmit` for a missed call site and each guard's own suite for a changed
scanned set.
  </action>
  <verify>
    <automated>cd src/mcp/vice && npm run typecheck && node --test shipped-modules.test.ts docs-dangling-refs.test.ts r2000-spawn-seam.test.ts stock-dispatch.test.ts comment-phase-pointers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - <!-- planner-discipline-allow: shippedTsModules -->
      <!-- planner-discipline-allow: codeOnly -->
      `grep -rcE '^function (shippedTsModules|codeOnly)' src/mcp/vice/*.test.ts | grep -vc ':0$'` returns `0` — no local definition of either helper survives anywhere in the tree
    - `grep -c 'from "./shipped-modules.ts"' src/mcp/vice/docs-dangling-refs.test.ts src/mcp/vice/r2000-spawn-seam.test.ts src/mcp/vice/stock-dispatch.test.ts src/mcp/vice/comment-phase-pointers.test.ts` reports `1` for each of the four files
    - `grep -c 'shippedTsModules(' src/mcp/vice/docs-dangling-refs.test.ts` returns `3`, `src/mcp/vice/r2000-spawn-seam.test.ts` returns `7`, `src/mcp/vice/stock-dispatch.test.ts` returns `4`, `src/mcp/vice/comment-phase-pointers.test.ts` returns `7` — every measured call site resolved, none dropped and none added
    - `grep -c 'codeOnly(' src/mcp/vice/r2000-spawn-seam.test.ts` returns at least `2` — both consumers survive; only the definition left
    - `grep -c '^function nonCommentLines' src/mcp/vice/stock-dispatch.test.ts` returns `1` — the deliberately-separate line stripper is untouched
    - `git diff -U0 -- src/mcp/vice/stock-dispatch.test.ts | grep -c 'nonCommentLines'` returns `0`
    - `cd src/mcp/vice && npm run typecheck` exits 0
    - `cd src/mcp/vice && node --test docs-dangling-refs.test.ts r2000-spawn-seam.test.ts stock-dispatch.test.ts comment-phase-pointers.test.ts` exits 0, and for each of the four the pass count is identical to `git stash`-baseline — record both counts in the SUMMARY so a silently shrunken scanned set is visible rather than inferred
    - **Scanned-set equivalence check, recorded:** for one of the four guards, print the enumerator's returned array before and after the repoint and confirm they are element-for-element identical; record the array length in the SUMMARY
  </acceptance_criteria>
  <done>Neither helper has a local definition anywhere in the tree; all four guard files import the shared helper with every measured call site resolved and none added or dropped; the deliberately-separate line stripper is untouched; `tsc --noEmit` is clean; all four guards pass with pass counts matched against their pre-repoint baseline and one scanned-set equivalence check recorded. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite both recorded statements of the sharing convention, so the record does not contradict itself</name>
  <files>src/mcp/vice/comment-phase-pointers.test.ts, src/mcp/vice/hop-chain-comments.test.ts, src/mcp/vice/r2000-spawn-seam.test.ts</files>
  <read_first>
    - `src/mcp/vice/comment-phase-pointers.test.ts:50-62` — the statement D-16 names: it records the copy as a deliberate independence choice and cites "this codebase's established 'no import between sibling guard tests' convention".
    - `src/mcp/vice/hop-chain-comments.test.ts:44-52` — the second, more broadly worded statement: "each guard test owns its own scan end-to-end", with no sibling qualifier, ending with "Keep the copy verbatim so a future reader can diff the two."
    - `src/mcp/vice/hop-chain-comments.test.ts:250-278` — its own scan set and floor assertion, to confirm nothing there is repointed by this phase.
    - `src/mcp/vice/r2000-spawn-seam.test.ts:45-58` — the pre-existing import of a neutral non-test helper at `:53`, which is the precedent the narrowed convention rests on.
    - `src/mcp/vice/r2000-spawn-seam.test.ts:150-178` — the enumerator doc comment as Task 2 left it.
  </read_first>
  <action>
Two comment blocks now argue against a change that has just landed beside them.
This project's recorded preference when a written decision blocks a change is to
rewrite the record in the same commit rather than route around it, so both are
corrected here — and both, not one, because clarifying only the first leaves the
second asserting the unqualified version.

**In `comment-phase-pointers.test.ts:53-59`**, replace the "kept as a second,
independent copy rather than an import" paragraph with a statement of the
convention's **scope**: a guard test must not import **another guard test**,
because importing a `*.test.ts` module for its exports also re-runs every
top-level test it registers; importing a neutral, non-test helper module is a
different thing and is permitted, with the precedent already in the tree at
`r2000-spawn-seam.test.ts:53`. Then say that this file's module enumerator now
comes from the shared helper for that reason. Keep the block's factual content
about what the enumerator does — `files[]` filtered to `.ts`/`.mts` with an
existence assertion so the scanned set cannot shrink silently.

**In `hop-chain-comments.test.ts:46-50`**, apply the same narrowing to the more
broadly worded statement, and remove the positive instruction to keep
duplicating — "keep the copy verbatim so a future reader can diff the two" is
exactly the sentence that would send a later reader to re-copy a helper that now
has one home. Replace it with the narrowed convention and one sentence stating
what **this** file still duplicates deliberately: its comment extractor, which is
a different helper from the module enumerator, is not consolidated by this phase,
and the reason is that the comment-extractor family does several different jobs
(the full stripper blanks string bodies; the line filters must not, because the
literals they search for are themselves strings). Point at
`r2000-tools.test.ts:193-201`, which states that reasoning in code. Nothing in
this file is repointed — the change is comment text only.

**In `r2000-spawn-seam.test.ts`**, finish the enumerator doc comment Task 2
left: confirm it names both unshipped-modules-that-spawn-a-real-binary instances
(the regenerator2000 gate and the ACME gate `27-01` created) as the reason the
scanned set is `files[]`-derived rather than directory-derived, and that it points
at the shared helper as the single home rather than describing a local copy.

Do not introduce a phase number into any of these comments, and do not put one
inside a string literal — `docs-dangling-refs.test.ts` and
`comment-phase-pointers.test.ts` are themselves the guards for that, and this
task edits one of them. Refer to work by requirement id or filename.
  </action>
  <verify>
    <automated>cd src/mcp/vice && npm run typecheck && node --test comment-phase-pointers.test.ts hop-chain-comments.test.ts r2000-spawn-seam.test.ts docs-dangling-refs.test.ts stock-dispatch.test.ts shipped-modules.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Kept as a second, independent copy' src/mcp/vice/comment-phase-pointers.test.ts src/mcp/vice/hop-chain-comments.test.ts` reports `0` for both files — neither still records the copy as a deliberate independence choice
    - `grep -c 'Keep the copy verbatim so a future reader can diff the two' src/mcp/vice/hop-chain-comments.test.ts` returns `0` — the positive instruction to keep duplicating is gone
    - `grep -c 'another guard test' src/mcp/vice/comment-phase-pointers.test.ts src/mcp/vice/hop-chain-comments.test.ts` reports at least `1` for both — the narrowed scope is stated in both places
    - `grep -c 'r2000-spawn-seam.test.ts' src/mcp/vice/comment-phase-pointers.test.ts` returns at least `1` — the permitted-import precedent is cited rather than asserted
    - `grep -c 'r2000-tools.test.ts' src/mcp/vice/hop-chain-comments.test.ts` returns at least `1` — the reason its own comment extractor stays duplicated is cited in code
    - `git diff -U0 -- src/mcp/vice/hop-chain-comments.test.ts | grep -cE '^[-+]\s*(assert|const|let|function|test\()' ` returns `0` — comment text only, no code change in that file
    - `grep -c 'acme-gate.ts' src/mcp/vice/r2000-spawn-seam.test.ts` returns at least `1`
    - `grep -cE 'Phase +[0-9]' src/mcp/vice/comment-phase-pointers.test.ts src/mcp/vice/hop-chain-comments.test.ts src/mcp/vice/r2000-spawn-seam.test.ts` reports no increase relative to `git show HEAD~1:` for the same files
    - `cd src/mcp/vice && node --test comment-phase-pointers.test.ts hop-chain-comments.test.ts docs-dangling-refs.test.ts` exits 0
  </acceptance_criteria>
  <done>Both recorded statements of the sharing convention state the same narrowed scope — no guard test imports another guard test, importing a neutral non-test helper is permitted, with the in-tree precedent cited — the instruction to keep duplicating is removed, the one duplication this phase deliberately keeps is named with its in-code reason, the enumerator's doc comment names both unshipped-spawn-site modules, and no code changed in the comment-only file. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `package.json` `files[]` → every guard's scanned set | Four committed guards derive *what they scan* from this array. A helper that returns a short list silently narrows four guards at once, and each keeps passing. |
| `package.json` `files[]` → published npm tarball | The shared helper must stay **out**: it is test-only, and shipping it would enlarge the published surface. |
| source text → structural guard predicates | `codeOnly()` decides what a guard can see. Under-stripping produces false positives; over-stripping produces false negatives that look like a clean tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-04-01 | Tampering | The shared enumerator losing its existence assertion in the move, silently shrinking the scanned set of four guards at once | high | mitigate | The helper takes an assertion library or throws its own named error and **cannot** return a short list; a planted stale-`files[]`-entry test drives the same predicate the real scan uses; the assertion is deleted once and the test observed RED before being restored; and a scanned-set equivalence check compares the returned array element-for-element across the repoint. |
| T-27-04-02 | Information Disclosure | The test-only helper leaking into the published npm tarball | medium | mitigate | Mechanical `files[]`-absence assertion in its own test file, copied from the established shape, plus a criterion that this plan changes `package.json` not at all. |
| T-27-04-03 | Tampering | `codeOnly()` being "simplified" into a regex or line filter during the move, so a violation hidden in a template literal becomes invisible | high | mitigate | The character state machine moves verbatim; committed unit tests cover single-quoted, double-quoted and template-literal bodies plus the three comment shapes; and the header records that a regex extractor was measured to miss a real violation of exactly this kind. |
| T-27-04-04 | Repudiation | A helper imported into a guard test that also registers tests, silently duplicating that file's test execution | medium | mitigate | The helper is a plain `.ts` that imports nothing from `node:test` (asserted by grep) and whose filename does not match the runner's glob; the reason is stated in its header and is already recorded twice in the tree. |
| T-27-04-05 | Repudiation | The record contradicting the code — one convention statement narrowed, the other still instructing a reader to keep duplicating | medium | mitigate | Both statements are rewritten in Task 3, asserted by greps in both directions (the old wording absent, the narrowed scope present, in both files). This is Correction C-4 and it is the failure D-16 exists to avoid. |
| T-27-04-SC | Tampering | npm / pip / cargo package installation | low | accept | **No package-manager install task exists in this plan or this phase.** This plan does not touch `package.json` at all, asserted by a criterion. RESEARCH.md's Package Legitimacy Audit records zero external packages, so there is nothing to audit. |
</threat_model>

<verification>
Per task, from `src/mcp/vice`:
- `npm run typecheck` — the detector for a missed call site among the twenty-one.
- `node --test <the affected guards>` — sub-30-second feedback, with pass counts compared against a pre-repoint baseline.

Plan gate:
- `cd src/mcp/vice && npm run typecheck` exits 0.
- `cd src/mcp/vice && node --test shipped-modules.test.ts docs-dangling-refs.test.ts r2000-spawn-seam.test.ts stock-dispatch.test.ts comment-phase-pointers.test.ts hop-chain-comments.test.ts test-gate.test.ts assumption-label-discipline.test.ts hostpath-consumers.test.ts docs-linerefs.test.ts` exits 0.
- `git diff --stat -- src/mcp/vice/package.json` is empty.
- <!-- planner-discipline-allow: r2000 -->
  `git diff --diff-filter=D --name-only 6c1f569..HEAD -- src/mcp/vice scripts | grep -c r2000` returns `0`.

**Forbidden as evidence:** `npm run test:automated`. The whole-glob run for the
phase is in `27-05-PLAN.md`.
</verification>

<success_criteria>
- Neither `shippedTsModules()` nor `codeOnly()` has a local definition anywhere in the tree; both live in `src/mcp/vice/shipped-modules.ts`.
- All four guard files import the shared helper, with every measured call site resolved and none added or dropped, and pass counts matched against a pre-repoint baseline.
- The helper cannot return a short list — proven by a planted stale `files[]` entry and by one observed RED after the existence assertion was removed.
- The stripper still blanks template-literal bodies, covered by committed unit tests.
- The helper is absent from `files[]`, registers no test at import time, and this plan changes `package.json` not at all.
- Both recorded statements of the sharing convention state the same narrowed scope; the instruction to keep duplicating is removed; the one duplication deliberately kept is named with its in-code reason.
- `nonCommentLines()` and the three `disasm-*` local comment filters are untouched, and their exclusion is written into the helper's header.
- No `r2000` module is deleted or renamed.
</success_criteria>

<output>
Create `.planning/phases/27-shared-seams-extracted/27-04-SUMMARY.md` when done.
Record in it: the OQ-1 decision (extract the stripper on a survival rationale,
with the comment-extractor family explicitly out of scope) and its reason; the
existence-assertion break-and-restore observation (RED, then restored); the four
guards' pass counts before and after the repoint; the scanned-set array length
from the equivalence check; and the exact commands run with their results.
</output>
