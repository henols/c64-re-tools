---
phase: 27-shared-seams-extracted
plan: 03
type: execute
wave: 2
depends_on: [27-01, 27-02]
files_modified:
  - src/mcp/vice/prg-image.ts
  - src/mcp/vice/prg-image.test.ts
  - src/mcp/vice/r2000-project.ts
  - src/mcp/vice/r2000-project.test.ts
  - src/mcp/vice/r2000-cli.ts
  - src/mcp/vice/r2000-coverage.ts
  - src/mcp/vice/r2000-coverage.test.ts
  - src/mcp/vice/r2000-symbol-roundtrip.test.ts
  - src/mcp/vice/r2000-verify.test.ts
  - src/mcp/vice/r2000-tools.test.ts
  - src/mcp/vice/r2000-mcp-client.test.ts
  - src/mcp/vice/r2000-d64.test.ts
  - src/mcp/vice/r2000-d64.ts
  - src/mcp/vice/r2000-cli.test.ts
  - src/mcp/vice/package.json
autonomous: true
requirements: [SEAM-02, SEAM-03]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "parsePrg, flatImageOrigin and decodeRawData are exported from src/mcp/vice/prg-image.ts under byte-identical names with byte-identical refusal messages, and r2000-project.ts exports none of them"
    - "The coverage census's only import from the module that drives the regenerator2000 binary is gone: r2000-coverage.ts and r2000-coverage.test.ts take decodeRawData from prg-image.ts, so a prefix-driven deletion of that glue module can no longer break the census (Correction C-1, resolved as OQ-3 option 2 — move it)"
    - "prg-image.ts is in package.json's files[] and scripts/check-npm-packages.mjs validates both tarballs, because r2000-cli.ts — itself reachable from vice-proxy.ts's import closure — imports the moved symbols"
    - "parsePrg refuses an input shorter than 3 bytes and flatImageOrigin refuses any length other than 65536, both with their original messages naming the observed length; and r2000-cli.ts's extension-before-length dispatch order — which is what makes an oversized flat capture hit the flat-image refusal instead of falling through — is unchanged"
    - "Every one of the nine consumer sites resolves: the six mixed import statements are SPLIT rather than rewritten, the two decodeRawData-only statements are rewritten, and r2000-d64.test.ts's dynamic path constant points at the new module"
  prohibitions: []
  artifacts:
    - path: "src/mcp/vice/prg-image.ts"
      provides: "Pure C64 byte-layout parsing: .prg load-address split, flat-64K origin, and the gzip+base64 payload inverse"
      exports: ["parsePrg", "flatImageOrigin", "decodeRawData"]
      min_lines: 55
    - path: "src/mcp/vice/prg-image.test.ts"
      provides: "The four relocated unit tests plus the payload round trip — the input-refusal regression for the two validators"
      min_lines: 45
  key_links:
    - from: "src/mcp/vice/r2000-cli.ts"
      to: "src/mcp/vice/prg-image.ts"
      via: "named import split out of the project-builder import; the shipped consumer that forces the files[] entry"
      pattern: 'from "\./prg-image\.ts"'
    - from: "src/mcp/vice/r2000-coverage.ts"
      to: "src/mcp/vice/prg-image.ts"
      via: "decodeRawData — the census dependency that made a glue-module deletion break a capability (C-1)"
      pattern: 'from "\./prg-image\.ts"'
    - from: "src/mcp/vice/package.json"
      to: "src/mcp/vice/prg-image.ts"
      via: "files[] entry — required by the transitive-closure walk from vice-proxy.ts"
      pattern: '"prg-image\.ts"'
---

<objective>
Move `parsePrg`, `flatImageOrigin` and `decodeRawData` out of
`r2000-project.ts` into a new shipped `src/mcp/vice/prg-image.ts`, and repoint
all nine consumer sites (D-14, and Correction C-1 resolved as move-it).

Purpose: these three functions are pure C64 byte-layout knowledge with no
dependency on the analyser this milestone deletes. They are the concrete instance
behind `r2000-project.ts`'s `glue-with-extractable` verdict — D-08's third verdict
exists precisely to name the symbols that must move out before a module may be
deleted, and this plan discharges that named obligation — and one of them —
`decodeRawData` — is statically imported by the coverage census, which criterion 2
names as provably not deletable by prefix. Extracting now discharges the
obligation immediately instead of leaving Phase 32 to discover it under deletion
pressure.
Output: a new shipped module, its unit tests, nine repointed consumers, and a
validated tarball. Zero `r2000` modules deleted.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-shared-seams-extracted/27-CONTEXT.md
@.planning/phases/27-shared-seams-extracted/27-RESEARCH.md
@.planning/phases/27-shared-seams-extracted/27-PATTERNS.md
@.planning/phases/27-shared-seams-extracted/27-02-SUMMARY.md
</context>

<research_corrections_applied>
RESEARCH.md overrides CONTEXT.md on every matter of fact. What binds this plan,
re-verified against the tree at `6c1f569` while planning:

- **C-1, the highest-impact correction in the phase, and its resolution.**
  `27-CONTEXT.md`'s `<canonical_refs>` states that `decodeRawData` "is
  r2000-payload-specific and does **not** move". Both halves of that
  parenthetical fail against the code: the body is two generic calls
  (base64-decode then gunzip) and only its doc comment ties it to the analyser's
  payload field; and `r2000-coverage.ts:132` — a criterion-2 capability module —
  **statically imports it** from a module D-08 classifies as
  `glue-with-extractable`. Re-verified at `:132` while planning, with
  `r2000-coverage.test.ts:64` importing it too. Following CONTEXT verbatim would
  leave a prefix-driven deletion able to break the coverage census: the precise
  failure mode SEAM-02 exists to prevent.
  **Decision (RESEARCH Open Question OQ-3, resolved here): move it (option 2).**
  It is the same shape of pure byte-level helper as its two siblings, it already
  has a non-glue consumer, and D-14's own rationale — discharge the obligation
  now rather than under deletion pressure — applies with equal force. Cost: a
  `node:zlib` import on the new module and two more sites to repoint. The
  alternative (record it as a named extractable and leave it in place for five
  phases) was rejected because it leaves the census's dependency on a
  to-be-deleted module live.
- **Every mixed import is a SPLIT, not a rewrite.** Measured: six of the nine
  sites import a moving symbol *together with* a staying one, so "rewrite the
  import line" understates the work and produces either a typecheck error or a
  silently disabled test. Two sites import `decodeRawData` alone and are genuine
  rewrites. One is a dynamic, non-literal specifier.
- **Correction to RESEARCH's own consumer table, found while planning.**
  RESEARCH lists `r2000-cli.test.ts` among the `SPLIT` candidates by implication;
  measured, its statement at `:21` imports **only** `synthesizeProject`, so it
  needs **no import change** — only comment accuracy at `:383`, `:384`, `:392`,
  `:974`, `:1245`, `:1253`, `:1259`. **Five** further files
  (`r2000-enum-gen.test.ts`, `r2000-spawn-seam.test.ts:52`,
  `r2000-session.test.ts`, `r2000-session.ts`, `r2000-memmap-render.test.ts`)
  import only staying symbols and are **not touched at all** — five, not six:
  re-counted against the tree while revising, and the enumeration above is
  exhaustive. Full arithmetic, so no later criterion has to re-derive it: **14**
  files import the old module today; **2** drop it entirely (the census pair);
  **5** are splits that keep their statement (`r2000-cli.ts` in Task 1, four test
  files in Task 3); **1** (`r2000-project.test.ts`) keeps a narrowed statement;
  **1** (`r2000-cli.test.ts`) is edited for comment accuracy only and keeps its
  statement unchanged; and these **5** are untouched. 2 + 5 + 1 + 1 + 5 = 14.
- **The check-order discipline is NOT inside the moved functions.** RESEARCH's
  security section says the ordering of the two refusals is deliberate and must
  not be reordered. Measured precisely: the order lives in **`r2000-cli.ts`'s
  dispatch logic** (`:24-31` explains it; `:477-491` implements it as
  extension-based dispatch *before* the length check), not inside `parsePrg` or
  `flatImageOrigin`. So the discipline is preserved by changing only
  `r2000-cli.ts`'s **import statement** and touching none of its control flow.
  A plan that "preserved the order" by editing the moved bodies would be editing
  the wrong file.
- **`r2000-d64.test.ts`'s dynamic import is a stale worktree workaround, and the
  code is still correct.** Its comment at `:370-375` justifies a non-literal
  specifier by an isolated worktree; `use_worktrees` is `false`. Repoint the path
  constant and update the comment. Do **not** remove the pattern — it is harmless
  and the code is right either way.
- **`gunzipSync` must leave `r2000-project.ts`'s import.** Verified: `:83` imports
  `gzipSync` and `gunzipSync` together, and `gunzipSync` is used **only** by the
  moving function. `gzipSync` stays with the project builder.
</research_corrections_applied>

<assumption_delta_decision>
**Scope of this plan: `no-change`.**

The detector fired for Phase 27 on three prose signals. Within this plan's scope
there is no singular→plural transition: three pure functions change file, keeping
byte-identical names, signatures and refusal messages. Nothing is generalised —
no vocabulary, no identity, no primary key. `decodeRawData`'s doc comment is
reworded to stop naming one particular payload field as its only purpose, which
is a *documentation* correction to a function that was already generic, not a
promotion of an identity model.

The phase's one genuine identity-model transition (the block-class vocabulary) is
resolved as **promote** in `27-02-PLAN.md`.
</assumption_delta_decision>

<probe_accounting>
Spec-less probe fallback (no SPEC.md exists for this phase). The deterministic
edge probe surfaced **12** applicable edges, all `unresolved`, distributed by
requirement id: 2 × SEAM-01, **6** × SEAM-02, **4** × SEAM-03 (recounted from the
coverage report while planning — not 2 / 5 / 5).

**This plan owns none of the 12 rows.** Its requirement ids are SEAM-02 and
SEAM-03, but the probe rows for both are authored where the requirement's own
observable behaviour is built — SEAM-03's four in `27-02-PLAN.md`, SEAM-02's six
in `27-05-PLAN.md`. Authoring a copy here would double-count against the
no-silent-drop equality check (12 in / 12 accounted), which is the one thing that
must hold. This plan's `must_haves.truths` are derived goal-backward from its own
deliverable instead, and its contribution to the two requirements is stated in
the objective.

**Prohibition recall (§B, in-prompt).** Stage 1 over-produced; Stage 2 dropped
every candidate. The strongest raw candidates — "must not silently loosen a
refusal", "must not reorder the two validators", "must not leave a re-export
behind" — are routine engineering and locked design, owned by this plan's
acceptance criteria and `<threat_model>` rather than by a values/safety
prohibition. Canon-referral breadcrumb, dropped not minted: a supply-chain change
to the published tarball is canon — covered by `/gsd-secure-phase` and by
`scripts/check-npm-packages.mjs`, recorded as `T-27-03-04` below; not minted.
`prohibitions: []` is the correct precision outcome, not an omission.
</probe_accounting>

<artifacts_this_phase_produces>
| New path | Symbols | New name? |
|---|---|---|
| `src/mcp/vice/prg-image.ts` | `parsePrg`, `flatImageOrigin`, `decodeRawData` | **No** — byte-identical names, moved out of `r2000-project.ts:171`, `:188`, `:202` |
| `src/mcp/vice/prg-image.test.ts` | new test titles only | new file (four tests relocated, plus a payload round trip) |

Removed from `src/mcp/vice/r2000-project.ts`: those three exports and the
`gunzipSync` half of its `node:zlib` import. Changed in `package.json`: `files[]`
gains `prg-image.ts`. No new CLI flag, env var, tool name, wire name, file format
or stored record.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create prg-image.ts, remove the three functions from the project builder, and repoint both production consumers</name>
  <files>src/mcp/vice/prg-image.ts, src/mcp/vice/r2000-project.ts, src/mcp/vice/r2000-cli.ts, src/mcp/vice/r2000-coverage.ts, src/mcp/vice/package.json</files>
  <behavior>
    - Test 1 (asserted in Task 2's file, but the behaviour is this task's contract): `parsePrg` on a 5-byte input returns the little-endian load address from the first two bytes and the remaining bytes as the body.
    - Test 2: `parsePrg` throws on inputs of 2, 1 and 0 bytes, with a message naming the observed byte count and the 3-byte minimum.
    - Test 3: `flatImageOrigin` returns 0 for exactly 65536 bytes.
    - Test 4: `flatImageOrigin` throws for 65535 and for 0 bytes, with a message naming the observed length.
    - Test 5: `decodeRawData` is the exact inverse of gzip-then-base64 for a non-trivial byte sequence.
  </behavior>
  <read_first>
    - `src/mcp/vice/r2000-project.ts:1-30` — the module's own header, for the shape and for the "performs no filesystem or network I/O" claim that must remain true of both files after the split.
    - `src/mcp/vice/r2000-project.ts:80-90` — the import block; `:83` imports `gzipSync` and `gunzipSync` together.
    - `src/mcp/vice/r2000-project.ts:164-205` — the three functions with their doc comments: `parsePrg` `:171`, `flatImageOrigin` `:188`, `decodeRawData` `:202`.
    - `src/mcp/vice/r2000-d64.ts:1-25` — the analog header for a shipped, pure C64-format module. It already reasons about the `files[]` transitive-closure rule that forces this new module into `files[]`; reproduce that reasoning.
    - `src/mcp/vice/r2000-cli.ts:20-35` and `:470-500` — the extension-before-length dispatch rationale and its implementation. Read to CONFIRM the control flow is untouched; only `:56`'s import statement changes.
    - `src/mcp/vice/r2000-cli.ts:50-80` — the import block, for the split at `:56`.
    - `src/mcp/vice/r2000-coverage.ts:128-140` — the import block; `:132` imports `decodeRawData` alone, so this one is a rewrite.
    - `src/mcp/vice/package.json` — the `files[]` array, and the `block-class.ts` entry `27-02` added, so the new entry reads as one more addition beside it.
    - `scripts/check-npm-packages.mjs:207-260` — the transitive-closure walk, so the reason for the `files[]` entry is understood rather than taken on faith.
  </read_first>
  <action>
Create `src/mcp/vice/prg-image.ts` holding the three pure byte-layout functions,
delete them from the project builder, and repoint the two production consumers.
Name confirmed free in `src/mcp/vice/`.

**Move all three bodies with their doc comments byte-identically**, preserving
declaration order: `parsePrg`, then `flatImageOrigin`, then `decodeRawData`. Do
not touch a single message string — `parsePrg`'s refusal names the observed byte
count and the 3-byte minimum, and `flatImageOrigin`'s names the observed length
and the exact 65536 requirement. Those messages are the user-visible contract:
`r2000-cli.ts` prefixes them and `r2000-cli.test.ts:1245` asserts that a `.d64`
entry rejection never leaks the parser's own name, so a reworded message would
break a test for a reason that looks unrelated. Do not add validation, do not
relax a threshold, do not reorder the checks inside either function.

Reword exactly one thing: `decodeRawData`'s doc comment currently describes
itself as the inverse of one particular analyser payload field's encoding. Restate
it as what it is — base64-decode then gunzip, the inverse of a gzip+base64
payload encoding — and keep its second sentence about being exported so tests can
prove the payload round-trips exactly rather than asserting against an opaque
blob. This is the one place CONTEXT.md was factually wrong (C-1) and the comment
is what made it look right.

Add `import { gunzipSync } from "node:zlib";` to the new module. Then **remove
`gunzipSync` from `r2000-project.ts:83`'s import**, leaving `gzipSync` alone —
verified that the project builder itself uses only `gzipSync`. An unused import
left behind is exactly the kind of residue that makes criterion 4's "demonstrably
a move" harder to read.

**Header for the new module.** Use the three-part single-seam shape. State that
it holds pure C64 file-layout knowledge with no dependency on any external
analyser; that it performs no filesystem and no network I/O (callers read and
write bytes themselves), matching the claim the module it came from makes; and —
copying `r2000-d64.ts`'s reasoning — that it **must** be listed in `files[]`
because it is reachable from the published entry point's import closure through
`r2000-cli.ts`, and the tarball validator's closure walk fails the pack the
moment a reachable module sits outside the listed set. `WHAT NOT TO DO`: never
take a filesystem path (these functions take byte arrays, which is what keeps
path traversal out of scope entirely); never relax either refusal, naming why —
a truncated flat capture that fell through to the `.prg` parser once
"bootstrapped" with a load address read out of its own payload bytes and exited
zero with every downstream address wrong and no diagnostic.

No `[ASSUMED]` label. No phase number inside a **string literal** — this module
ships and `docs-dangling-refs.test.ts` forbids that; check
`comment-phase-pointers.test.ts`'s pattern families before writing any "moved
here" sentence. No `hostpath.ts` or `containerpath.ts` import.

**Delete the three exports** from `r2000-project.ts`, leaving `synthesizeProject`,
`ensureProjectSettings`, `R2000ProjectSettingsError`, `R2000_SYSTEM_C64` and the
rest untouched. Add **no** re-export: the phase's recorded principle is no shims
and no partial repoints, because a half-move leaves the hazard in place while
looking finished.

**Repoint the two production consumers.** `r2000-cli.ts:56` is a **SPLIT** —
keep `synthesizeProject` on the project builder and take `parsePrg` and
`flatImageOrigin` from the new module in a second statement beside it. Change
nothing else in that file: its dispatch order is the discipline, and it lives in
control flow this task does not enter. `r2000-coverage.ts:132` imports
`decodeRawData` alone, so it is a straight **rewrite** of the specifier.

**Add `"prg-image.ts"` to `package.json`'s `files[]`** and run the tarball
validator. Change nothing else in `package.json`.
  </action>
  <verify>
    <automated>cd src/mcp/vice && npm run typecheck && node --test r2000-coverage.test.ts r2000-coverage-grammar.test.ts && node ../../../scripts/check-npm-packages.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^export function (parsePrg|flatImageOrigin|decodeRawData)' src/mcp/vice/prg-image.ts` returns `3`
    - <!-- planner-discipline-allow: parsePrg -->
      <!-- planner-discipline-allow: flatImageOrigin -->
      <!-- planner-discipline-allow: decodeRawData -->
      `grep -cE '^export function (parsePrg|flatImageOrigin|decodeRawData)' src/mcp/vice/r2000-project.ts` returns `0`
    - <!-- planner-discipline-allow: gunzipSync -->
      `grep -c 'gunzipSync' src/mcp/vice/r2000-project.ts` returns `0` and `grep -c 'gzipSync' src/mcp/vice/r2000-project.ts` returns at least `1`
    - `grep -c 'a .prg needs at least 3 bytes' src/mcp/vice/prg-image.ts` returns `1` and `grep -c 'must be exactly 65536 bytes' src/mcp/vice/prg-image.ts` returns `1` — both refusal messages moved byte-identically
    - `git diff -- src/mcp/vice/r2000-cli.ts | grep -cE '^[-+]' ` shows changes confined to the import region: `git diff -U0 -- src/mcp/vice/r2000-cli.ts | grep -cE '^[-+](origin|body|  )' ` returns `0` — no control-flow change, so the extension-before-length dispatch order is provably untouched
    - `grep -c 'from "./prg-image.ts"' src/mcp/vice/r2000-cli.ts` returns `1` and `grep -c 'from "./r2000-project.ts"' src/mcp/vice/r2000-cli.ts` returns `1` — a split, not a replacement
    - `grep -c 'from "./prg-image.ts"' src/mcp/vice/r2000-coverage.ts` returns `1` and `grep -c 'from "./r2000-project.ts"' src/mcp/vice/r2000-coverage.ts` returns `0` — the census no longer imports from the glue module at all
    - `grep -cE 'from "\./(hostpath|containerpath)' src/mcp/vice/prg-image.ts` returns `0` and `grep -c 'ASSUMED' src/mcp/vice/prg-image.ts` returns `0`
    - `node -e 'const f=require("./src/mcp/vice/package.json").files; process.exit(f.includes("prg-image.ts")?0:1)'` exits 0
    - `node scripts/check-npm-packages.mjs` exits 0 and reports the transitive closure from the published entry point clean
    - `git diff -- src/mcp/vice/package.json` shows exactly one added line and no change to `dependencies` or `devDependencies`
    - `cd src/mcp/vice && npm run typecheck` exits 0
    - `cd src/mcp/vice && node --test docs-dangling-refs.test.ts comment-phase-pointers.test.ts hop-chain-comments.test.ts assumption-label-discipline.test.ts r2000-spawn-seam.test.ts stock-dispatch.test.ts` exits 0 — every guard that begins scanning the new shipped module stays green
  </acceptance_criteria>
  <done>The three functions are exported from `src/mcp/vice/prg-image.ts` with byte-identical names and refusal messages, are gone from the project builder along with its now-unused `gunzipSync` import, and both production consumers resolve — `r2000-cli.ts` by a split with its control flow provably untouched, the census by a rewrite that removes its last import from the glue module. `prg-image.ts` is in `files[]`, the tarball validates, and every `files[]`-scanning guard is green. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create prg-image.test.ts by relocating the four unit tests, and split the project builder's own test import</name>
  <files>src/mcp/vice/prg-image.test.ts, src/mcp/vice/r2000-project.test.ts</files>
  <behavior>
    - The four tests currently at `r2000-project.test.ts:110-128` move verbatim (titles and assertions unchanged) into the new file, now importing from the new module: the load-address extraction, the short-input refusals matching `/3 bytes/`, the exact-65536 success, and the wrong-length refusals matching the observed length.
    - One added test: `decodeRawData` returns exactly the original bytes for a gzip-then-base64 round trip of a non-trivial sequence — the round-trip property its own doc comment claims it exists to allow.
    - One added test: the new module performs no filesystem or network I/O — its source contains no write, no read-file, no spawn and no fetch call.
  </behavior>
  <read_first>
    - `src/mcp/vice/r2000-project.test.ts:44-60` — the import block; the multi-line statement opens at `:51` and closes at `:59`, and lists three moving symbols among six.
    - `src/mcp/vice/r2000-project.test.ts:106-130` — the four tests that relocate, verbatim.
    - `src/mcp/vice/r2000-project.test.ts:130-150` — what follows them, so the excision leaves the surrounding structure intact.
    - `src/mcp/vice/capability-registry.test.ts:16-25` — the repo's standard test import block and the `HERE` idiom.
    - `src/mcp/vice/prg-image.ts` — the module under test as Task 1 left it.
  </read_first>
  <action>
Create `src/mcp/vice/prg-image.test.ts` and relocate the four existing unit tests
into it, then split the project builder's own test import.

**Relocate, do not rewrite.** Move the four tests verbatim — same titles, same
assertions, same fixtures, same regex matchers (`/3 bytes/` and the
observed-length matchers). Their value is that they are the committed regression
for the two input validators; a reworded assertion is a new test with an old
test's authority. Change only the import specifier.

Add the round-trip test for `decodeRawData`: gzip and base64-encode a non-trivial
byte sequence, decode it back through the function, and assert byte equality.
Its doc comment says it is exported so tests can prove exactly this rather than
asserting against an opaque blob, and no such test exists today — the property
was claimed and never checked.

Add the no-I/O structural test: read the new module's own source and assert it
contains no write, read-file, spawn or fetch call. This is the mechanical form of
the header's own claim, and it is the same shape as the source-level read-only
assertion the coverage suite already carries.

Use the repo's standard test import block — `test` from `node:test`, `assert`
from `node:assert/strict`, `HERE` from `dirname(fileURLToPath(import.meta.url))`
— and real `.ts` extensions on the relative import. Do **not** add this file to
`MANUAL_ONLY_TESTS`: `test-gate.test.ts` auto-discovers new `*.test.*` files into
the automated set, which is correct.

**Then edit `r2000-project.test.ts`.** Excise the four relocated tests, and
**SPLIT** its multi-line import: keep `R2000_SYSTEM_C64`, `synthesizeProject`,
`ensureProjectSettings` and `R2000ProjectSettingsError` on the project builder,
and take whichever of the three moved symbols the file's *remaining* tests still
use from the new module — if the excision leaves none of them used, drop them
from the import entirely rather than importing an unused binding. Determine that
by reading what remains, not by assuming. Leave every other test in that file
untouched, including its regenerator2000-gated suite and its `skipReasonFor`
call.
  </action>
  <verify>
    <automated>cd src/mcp/vice && npm run typecheck && node --test prg-image.test.ts r2000-project.test.ts test-gate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && node --test prg-image.test.ts` exits 0 with at least 6 passing tests and 0 failures
    - The four relocated test titles appear verbatim in `prg-image.test.ts` and no longer appear in `r2000-project.test.ts`: `grep -c 'parsePrg: extracts a little-endian load address' src/mcp/vice/prg-image.test.ts` returns `1` and the same grep against `src/mcp/vice/r2000-project.test.ts` returns `0`
    - `grep -c '/3 bytes/' src/mcp/vice/prg-image.test.ts` returns at least `1` — the refusal matcher moved unchanged
    - `grep -c 'from "./prg-image.ts"' src/mcp/vice/prg-image.test.ts` returns `1`
    - `cd src/mcp/vice && node --test r2000-project.test.ts` exits 0 — the excision left the surrounding structure intact
    - `cd src/mcp/vice && npm run typecheck` exits 0 — no unused or dangling binding survives the split
    - `cd src/mcp/vice && node --test test-gate.test.ts` exits 0 with no `MANUAL_ONLY_TESTS` edit — the new test file lands in the auto-discovered automated set
    - **Break-and-restore probe, observed:** loosen `parsePrg`'s minimum-length check by one byte; `node --test prg-image.test.ts` goes RED on the short-input refusal test; restore the check and record both observations in the SUMMARY
  </acceptance_criteria>
  <done>`prg-image.test.ts` carries the four relocated tests verbatim plus the payload round trip and the no-I/O structural check, all green; the four tests no longer exist in the project builder's test file and that file still passes; its multi-line import is split with no unused binding; and one break-and-restore probe has been observed RED and restored. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: Repoint the remaining six consumers, the dynamic path constant, and every comment that names the old home</name>
  <files>src/mcp/vice/r2000-symbol-roundtrip.test.ts, src/mcp/vice/r2000-verify.test.ts, src/mcp/vice/r2000-tools.test.ts, src/mcp/vice/r2000-mcp-client.test.ts, src/mcp/vice/r2000-coverage.test.ts, src/mcp/vice/r2000-d64.test.ts, src/mcp/vice/r2000-d64.ts, src/mcp/vice/r2000-cli.test.ts</files>
  <read_first>
    - `src/mcp/vice/r2000-symbol-roundtrip.test.ts:45-52` — its import at `:49` mixes a moving and a staying symbol.
    - `src/mcp/vice/r2000-verify.test.ts:35-42` — its import at `:38` mixes.
    - `src/mcp/vice/r2000-tools.test.ts:33-40` — its import at `:36` mixes.
    - `src/mcp/vice/r2000-mcp-client.test.ts:95-102` — its import at `:98` mixes.
    - `src/mcp/vice/r2000-coverage.test.ts:60-68` — its import at `:64` takes `decodeRawData` alone: a rewrite.
    - `src/mcp/vice/r2000-d64.test.ts:355-385` — the path constant at `:359`, the existence check at `:360`, the worktree-workaround comment at `:370-375`, and the dynamic import at `:376`.
    - `src/mcp/vice/r2000-d64.ts:212-222` and `:265-275` — two doc/inline comments naming the old module as the parser's home.
    - `src/mcp/vice/r2000-cli.test.ts:380-395`, `:970-978`, `:1240-1280` — comment and assertion-message prose naming the parser; note `:1273` deliberately asserts the parser's name never reaches stderr, which must keep working.
  </read_first>
  <action>
Eight files, each a single-statement import split, a path-constant repoint, or a
comment-text correction. No logic changes anywhere in this task, and
`tsc --noEmit` is the completeness check for the whole set.

**Four splits.** In `r2000-symbol-roundtrip.test.ts:49`,
`r2000-verify.test.ts:38`, `r2000-tools.test.ts:36` and
`r2000-mcp-client.test.ts:98`, each statement imports a moving symbol together
with `synthesizeProject`. Replace each with two statements side by side, one per
source module, real `.ts` extension —
`r2000-spawn-seam.test.ts:50-53` is the target shape. Do **not** collapse either
statement, and do not reorder the symbol lists.

**One rewrite.** `r2000-coverage.test.ts:64` imports `decodeRawData` alone;
change only the specifier. Note this file was also edited by `27-02`; re-read its
current import block rather than working from the line number.

**One dynamic path constant.** In `r2000-d64.test.ts`, the constant at `:359`
joins the module directory to the old filename and the dynamic import at `:376`
resolves it. Point the constant at the new module's filename, rename the constant
and its sibling availability flag to match the new target, and update the
inline-typed shape at `:376-378` if it names the module. Then correct the
comment at `:370-375`: its stated reason — that a literal specifier would fail
the typechecker in an isolated worktree — no longer applies, because worktrees are
off. Keep the non-literal pattern; it is harmless and the code is correct either
way. Say in the comment that the pattern is retained deliberately and that its
original worktree rationale has lapsed, rather than deleting the explanation and
leaving an unexplained indirection.

**Two comment-accuracy files.** In `r2000-d64.ts`, the doc comment at `:215-218`
says the whole point of the module is to hand bytes straight to the parser "in
`r2000-project.ts`" — correct the module name, keep the claim. The inline comment
near `:268-271` quotes the parser's own refusal text as an example of a confusing
downstream message; keep the quoted text byte-identical (it is the real message)
and correct any module attribution around it. In `r2000-cli.test.ts`, correct
every comment that attributes the parser or the flat-image function to the old
module while preserving each comment's actual claim — in particular the block at
`:383-392` explaining why an oversized flat capture must hit the flat-image
refusal instead of falling through, and the note at `:1253-1259` about the
one-byte-short payload. Do **not** touch the assertion at `:1273` that the
parser's name never reaches stderr: that is a real behavioural assertion, it
still holds, and its regex must stay as written.

Do not introduce a phase number into any of these comments — refer to work by
requirement id or filename, which is this repo's established form.
  </action>
  <verify>
    <automated>cd src/mcp/vice && npm run typecheck && node --test prg-image.test.ts r2000-symbol-roundtrip.test.ts r2000-verify.test.ts r2000-tools.test.ts r2000-mcp-client.test.ts r2000-coverage.test.ts r2000-d64.test.ts r2000-cli.test.ts docs-dangling-refs.test.ts comment-phase-pointers.test.ts hop-chain-comments.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from "./prg-image.ts"' src/mcp/vice/r2000-symbol-roundtrip.test.ts src/mcp/vice/r2000-verify.test.ts src/mcp/vice/r2000-tools.test.ts src/mcp/vice/r2000-mcp-client.test.ts src/mcp/vice/r2000-coverage.test.ts` reports `1` for each of the five files
    - `grep -c 'from "./r2000-project.ts"' src/mcp/vice/r2000-symbol-roundtrip.test.ts src/mcp/vice/r2000-verify.test.ts src/mcp/vice/r2000-tools.test.ts src/mcp/vice/r2000-mcp-client.test.ts` reports `1` for each of the four split files — the staying symbol kept its own statement
    - `grep -c 'from "./r2000-project.ts"' src/mcp/vice/r2000-coverage.test.ts` returns `0`
    - <!-- planner-discipline-allow: r2000-project.ts -->
      **The importer set lost exactly the two census files and nothing else.** Stated as a relation against the phase's base commit rather than as a pinned total, because a pinned total is satisfiable by *deleting* a legitimate import:
      `diff <(git grep -l 'from "./r2000-project.ts"' 6c1f569 -- src/mcp/vice | sed 's|^[^:]*:||' | sort) <(git grep -l 'from "./r2000-project.ts"' -- src/mcp/vice | sort) | grep '^[<>]'`
      prints exactly two lines, both `<`, naming `src/mcp/vice/r2000-coverage.test.ts` and `src/mcp/vice/r2000-coverage.ts`. **Read this criterion by its printed lines, never by its exit status** — the trailing `grep` exits 1 when it matches nothing, so the exit code is about `grep`, not about the tree. Measured at `6c1f569` while planning: **14** files import that module, one statement each. Two drop it entirely (the census pair); **five are SPLITS that KEEP their statement by design** (`r2000-cli.ts`, `r2000-symbol-roundtrip.test.ts`, `r2000-verify.test.ts`, `r2000-tools.test.ts`, `r2000-mcp-client.test.ts`); `r2000-project.test.ts` keeps its statement narrowed to the staying symbols; and five are not touched by this plan at all. A `>` line means a file *gained* the import; any third `<` line means a legitimate import was deleted instead of split — the harmful resolution this criterion exists to catch. A count-based form cannot tell those two failures apart, which is why this one is set-shaped.
    - <!-- planner-discipline-allow: r2000-project.ts -->
      <!-- planner-discipline-allow: parsePrg -->
      <!-- planner-discipline-allow: flatImageOrigin -->
      <!-- planner-discipline-allow: decodeRawData -->
      **No surviving `from "./r2000-project.ts"` import *statement* names a moved symbol in its brace list.** Scoped to the statement, never to the file: `r2000-cli.test.ts` keeps that import while deliberately naming the parser in the `doesNotMatch(stderr, …)` assertion at `:1273`, and `r2000-d64.ts` quotes the parser's real refusal text verbatim — both are correct and a file-wide grep would red on them forever.
      `for f in $(grep -rl 'from "./r2000-project.ts"' src/mcp/vice/); do perl -0777 -ne 'while (/import[^;]*?from "\.\/r2000-project\.ts";/gs) { print "$ARGV\n" if $& =~ /parsePrg|flatImageOrigin|decodeRawData/ }' "$f"; done`
      prints nothing. **Non-vacuity, measured at `6c1f569`:** the identical command prints eight files today (`r2000-symbol-roundtrip.test.ts`, `r2000-project.test.ts`, `r2000-verify.test.ts`, `r2000-tools.test.ts`, `r2000-mcp-client.test.ts`, `r2000-coverage.ts`, `r2000-coverage.test.ts`, `r2000-cli.ts`) — including the multi-line statement in `r2000-project.test.ts`, which a line-scoped grep would miss. `npm run typecheck` is the paired backstop that closes the one loophole a "must not appear" check leaves: the three symbols are no longer exported from that module, and each split file's staying symbol is still *used*, so deleting a whole statement to reach green is a hard compile error rather than a shortcut.
    - `grep -c 'prg-image.ts' src/mcp/vice/r2000-d64.test.ts` returns at least `1` and `cd src/mcp/vice && node --test r2000-d64.test.ts` exits 0 with the dynamic-import test running rather than skipping (its availability flag resolves against a file that exists)
    - `grep -c 'prg-image.ts' src/mcp/vice/r2000-d64.ts` returns at least `1` and `grep -c 'r2000-project.ts' src/mcp/vice/r2000-d64.ts` returns `0`
    - `grep -c 'parsePrg: input is 0 byte(s)' src/mcp/vice/r2000-d64.ts` returns `1` — the quoted real refusal text is byte-identical
    - `grep -c 'doesNotMatch(stderr, /parsePrg/)' src/mcp/vice/r2000-cli.test.ts` returns `1` — the behavioural assertion is untouched
    - `git diff -U0 -- src/mcp/vice/r2000-cli.test.ts | grep -cE '^[-+]\s*(assert|const|let|await|test\()' ` returns `0` — the changes in that file are comment text only
    - `cd src/mcp/vice && npm run typecheck` exits 0
    - `cd src/mcp/vice && node --test docs-dangling-refs.test.ts comment-phase-pointers.test.ts hop-chain-comments.test.ts` exits 0
  </acceptance_criteria>
  <done>All nine consumer sites resolve — this task's eight edits being four splits, one rewrite, one dynamic path constant and two comment-accuracy files, on top of Task 1's split and rewrite — every claim each comment made survives, the parser-name-never-in-stderr assertion is untouched, the five files importing only staying symbols were not edited, `tsc --noEmit` is clean, and the three comment guards are green. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied image bytes → parser | `.prg`, `.d64`-extracted and flat-64K byte arrays reach the two validators. Both are input-validation refusals, and both are the *only* thing standing between a truncated capture and a silently wrong load address. |
| `package.json` `files[]` → published npm tarball | `prg-image.ts` newly ships to consumers of `@henols/vice-mcp`. |
| glue module → capability module (internal) | The census statically imports one of the moving functions. Until this plan lands, a prefix deletion of the glue module breaks a capability with no announcement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-03-01 | Tampering | The two input-validation refusals (`parsePrg`'s 3-byte minimum, `flatImageOrigin`'s exact-65536 requirement) being reworded, relaxed or reordered during the move | high | mitigate | Bodies and message strings moved byte-identically, asserted by two literal greps; the four existing unit tests relocate **verbatim** as the regression rather than being rewritten; and one break-and-restore probe against the minimum-length check is observed RED. Recorded finding: the *ordering* discipline lives in `r2000-cli.ts`'s extension-before-length dispatch, not inside either function, so it is preserved by a criterion asserting that file's control flow is untouched. |
| T-27-03-02 | Repudiation | A capability module left depending on a module a prefix deletion removes, so the loss does not announce itself | high | mitigate | This is Correction C-1, and this plan is the mitigation: the census's last import from the glue module is removed, asserted by a grep returning zero. Resolving it the other way (record and defer) was considered and rejected in `<research_corrections_applied>`. |
| T-27-03-03 | Information Disclosure | The new module enlarging the published tarball surface | medium | mitigate | The addition is forced by the closure walk, not chosen. The module is pure: byte arrays in, values out — no filesystem, no subprocess, no network, no path handling, no env read, asserted by a structural no-I/O test and by the absence of a host-path import. `scripts/check-npm-packages.mjs` is **run**, not assumed. |
| T-27-03-04 | Tampering | A supply-chain change to the published tarball going unnoticed | medium | mitigate | Canon security, referred not minted as a prohibition: the tarball validator compares both published tarballs' exact contents via a dry-run pack, and this plan runs it after the `files[]` change. `/gsd-secure-phase` owns the canon side. |
| T-27-03-05 | Tampering | Path traversal via an image argument | low | accept | Not reachable: all three moved functions take `Uint8Array` or a base64 string, never a path. Path resolution stays entirely with the CLI, which this plan does not modify. Recorded so the absence is a decision rather than an omission. |
| T-27-03-06 | Denial of Service | A hostile gzip payload expanding without bound through the moved decoder | low | accept | Unchanged from the code being moved — same call, same absence of a size cap, same single caller shape. The input reaches it only from an analyser project file the operator supplied. Recorded rather than silently inherited; adding a cap would be a behaviour change, which this phase forbids. |
| T-27-03-SC | Tampering | npm / pip / cargo package installation | low | accept | **No package-manager install task exists in this plan or this phase.** `dependencies` and `devDependencies` are byte-identical; the only `package.json` diff is one `files[]` entry, asserted by a criterion. RESEARCH.md's Package Legitimacy Audit records zero external packages, so there is nothing to audit. |
</threat_model>

<verification>
Per task, from `src/mcp/vice`:
- `npm run typecheck` — the completeness check for six split imports.
- `node --test <the affected files>` — sub-30-second feedback.
- After the `files[]` change: `node scripts/check-npm-packages.mjs` from the repo root.

Plan gate:
- `cd src/mcp/vice && npm run typecheck` exits 0.
- `cd src/mcp/vice && node --test prg-image.test.ts r2000-project.test.ts r2000-symbol-roundtrip.test.ts r2000-verify.test.ts r2000-tools.test.ts r2000-mcp-client.test.ts r2000-coverage.test.ts r2000-coverage-grammar.test.ts r2000-d64.test.ts r2000-cli.test.ts test-gate.test.ts docs-dangling-refs.test.ts comment-phase-pointers.test.ts hop-chain-comments.test.ts assumption-label-discipline.test.ts r2000-spawn-seam.test.ts stock-dispatch.test.ts hostpath-consumers.test.ts` exits 0.
- `node scripts/check-npm-packages.mjs` exits 0.
- <!-- planner-discipline-allow: r2000 -->
  `test "$(git diff --diff-filter=D --name-only 6c1f569..HEAD -- src/mcp/vice scripts | grep -c r2000)" = "0"` exits 0 — wrapped in `test "$(…)"` deliberately: the bare pipeline *prints* `0` but *exits 1*, because `grep -c` returns no-match status even when its count is the count we want, so chaining the bare form with `&&` produces a false red on a correct tree.

**Forbidden as evidence:** `npm run test:automated`. The whole-glob run for the
phase is in `27-05-PLAN.md`.
</verification>

<success_criteria>
- `src/mcp/vice/prg-image.ts` exports the three functions with byte-identical names and refusal messages, takes no path, performs no I/O, and is in `files[]` with the tarball validated.
- `r2000-project.ts` exports none of the three and carries no unused `node:zlib` binding.
- The coverage census imports nothing from the glue module — Correction C-1 discharged, with the decision to move rather than defer recorded.
- All nine consumer sites resolve; the six mixed statements are split, not rewritten; `tsc --noEmit` is clean.
- The four input-validator unit tests exist verbatim in the new test file, plus the payload round trip and the no-I/O structural check, with one break-and-restore probe observed RED.
- `r2000-cli.ts`'s extension-before-length dispatch order is provably untouched.
- Every comment naming the old module as the parser's home is corrected while keeping its claim; the parser-name-never-in-stderr assertion is untouched.
- No `r2000` module is deleted or renamed.
</success_criteria>

<output>
Create `.planning/phases/27-shared-seams-extracted/27-03-SUMMARY.md` when done.
Record in it: the OQ-3 decision (move `decodeRawData`, not record-and-defer) with
its reason; the break-and-restore probe observation (RED, then restored); the
`check-npm-packages.mjs` closure-walk output line; the corrected count of files
that needed no import change; and the exact commands run with their results.
</output>
