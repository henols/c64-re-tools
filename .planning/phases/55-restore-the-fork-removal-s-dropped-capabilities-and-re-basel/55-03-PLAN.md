---
phase: 55-restore-the-fork-removal-s-dropped-capabilities-and-re-basel
plan: 03
type: execute
wave: 2
depends_on: ["55-01", "55-02"]
files_modified:
  - src/mcp/vice/vice-proxy.test.ts
autonomous: true
requirements: [PROXY-03, PROXY-04]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every test asserting the replaced-machine / fresh-session replacement mechanism is deleted, and the mechanism is confirmed absent from shipped code before any of them is removed (PROXY-03)"
    - "Every test driving the proxy's own recycle implementation is deleted, each one's behaviour first located in `stock-recycle.test.ts` or `incident-record.test.ts` by name (PROXY-03)"
    - "Every test driving the proxy's own diagnose implementation is deleted, each one's behaviour first located in `stock-diagnose.test.ts` by name (PROXY-03)"
    - "Every checkpoint-arming seam-annotation test is deleted under the owner's settled disposition — retired, no successor owed (PROXY-03)"
    - "Every test asserting a construction-time deny list is deleted or re-pointed, on the measured fact that no deny list exists in shipped code (PROXY-04)"
    - "No test is disabled with `skip` anywhere in this plan; the file's `skip` count does not rise (PROXY-03)"
    - "Every deletion's commit message names what was removed, why it no longer has a subject, and the successor suite that covers the behaviour — or states that the owner settled that no successor is owed (PROXY-03)"
  artifacts:
    - src/mcp/vice/vice-proxy.test.ts
  key_links:
    - "each deleted recycle test to its named counterpart in `stock-recycle.test.ts` / `incident-record.test.ts` — the successor must be located BEFORE the deletion, not asserted afterwards"
    - "each deleted diagnose test to its named counterpart in `stock-diagnose.test.ts`"
    - "`DIAGNOSE_VERDICTS` to `STOCK_DIAGNOSE_VERDICTS` in `stock-diagnose.ts` — the frozen five-member verdict vocabulary survives under a new name and is already pinned by its own suite"
    - "the two surviving fallback tests near the end of the file to the deleted deny-list ordering test — the surviving pair is what `ci-guardrails.test.mjs` cites as this file's wire proof, so they must not be swept up"
  prohibitions:
    - statement: "No test is removed by deleting a line range. Every removal is located by its test NAME and cut at its own boundaries, because every one of these regions has interleaved survivors."
      status: required
      verification: flagged-unverified
    - statement: "No test is disabled with `skip`, `todo`, a commented-out body, or a renamed-to-nothing stub. A retired test is deleted."
      status: required
      verification: flagged-unverified
    - statement: "No test whose behaviour still exists in shipped code and is covered nowhere else is deleted. When the successor cannot be located by name, the test is left in place and reported, not removed."
      status: required
      verification: flagged-unverified
    - statement: "The two surviving unknown-tool fallback tests near the end of the file are NOT deleted. They pass, they assert live behaviour, and an external guard cites this file for exactly that proof."
      status: required
      verification: flagged-unverified
    - statement: "The test named `vice_diagnose appears in tools/list alongside the other synthetic tools` is NOT deleted. It passes and asserts a live registration."
      status: required
      verification: flagged-unverified
    - statement: "A helper is deleted only after a grep shows every one of its consumers is gone. A helper with a surviving consumer stays, even if it looks fork-era."
      status: required
      verification: flagged-unverified
---

<objective>
Delete the fork-era regions of `vice-proxy.test.ts` — every assertion whose subject `96ef711f` removed — with each deletion's reason and named successor in its own commit message.

The file's header declares its harness: a real spawned proxy child driven against an in-process HTTP stand-in for a host VICE server. `96ef711f` deleted the forwarding function that was that stand-in's only consumer. Everything downstream of it is dead by construction — one shape, not dozens of independent bugs.

MEASURED at `6e4e9a0b`, over shipped (non-test) sources under `src/mcp/vice/`: `SEAM_HAZARDS` 0 hits, `DENY_LIST` 0 hits, `epochDriftMessage` 0 hits, `detectCheckpointArmingHazard` 0 hits, `runCycleBracket` 0 hits. `handleGrantedInstanceUnreachable` has 5 hits and every one is inside a comment — one of them is the record of its own deletion. These mechanisms do not exist.

Retiring a test means DELETING it with its reason in the commit message. A skipped test is a standing lie about coverage, and so is a passing test that asserts nothing.

Purpose: remove roughly 2,500 lines of assertions about mechanisms that are gone, without losing a single assertion about a mechanism that is not.
Output: a `vice-proxy.test.ts` whose every remaining test has a live subject, and a commit trail that says where each removed assertion's coverage went.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-restore-the-fork-removal-s-dropped-capabilities-and-re-basel/55-01-SUMMARY.md
@.planning/phases/55-restore-the-fork-removal-s-dropped-capabilities-and-re-basel/55-02-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 0: Re-census the file and record the pre-deletion baseline</name>
  <files>src/mcp/vice/vice-proxy.test.ts</files>
  <read_first>
    - `src/mcp/vice/vice-proxy.test.ts` — the file header and the environment gate near the top, so you know which cases are gated before you interpret a skip.
  </read_first>
  <action>
Before deleting anything, take your own census. Every line number recorded anywhere in this phase's planning material is an anchor to navigate by, not a coordinate to cut at — the file has already shifted once and will shift again with every deletion in this plan.

Run the file in full, with output redirected to a scratch file outside the repository tree and the exit code read on the same line. Never pipe a test command into `tail` or `head` — the pipe reports the pager's exit code, not the runner's. Record the summary counts and the complete list of test names with their result.

Separately record: the count of top-level `test(` declarations, the count of `skip` usages, and the file's total line count. These three numbers are the invariants every later task in this plan compares against.

Stop the broker before you measure. A live broker instance changes at least one case's result in this file deterministically — it is not a flake, and a census taken beside one is the wrong baseline.
  </action>
  <acceptance_criteria>
    - A full-file run's summary counts are recorded in the SUMMARY, taken at this plan's own starting commit rather than copied from any earlier census.
    - The top-level test count, the `skip` count and the line count are recorded.
    - No broker process was running during the measurement.
  </acceptance_criteria>
  <verify>
    <automated>pgrep -af 'vice-broker|x64sc' > /tmp/55-03-t0-procs.txt 2>&1; echo "BROKER_LINES=$(wc -l < /tmp/55-03-t0-procs.txt)"; cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test vice-proxy.test.ts > /tmp/55-03-t0-census.txt 2>&1; echo "EXIT=$?"; grep -E '^# (tests|pass|fail|cancelled|skipped)' /tmp/55-03-t0-census.txt; printf 'toplevel='; grep -ac '^test(' vice-proxy.test.ts; printf 'skips='; grep -ac 'test.skip\|{ skip:\|, { skip' vice-proxy.test.ts; printf 'lines='; wc -l < vice-proxy.test.ts</automated>
    <fails_when>`BROKER_LINES` is greater than 0 — a live broker or emulator was running and the census is not a clean baseline. Or the run produced no `# tests` line at all, meaning it did not terminate. `EXIT=` being non-zero is EXPECTED here; this task records the red baseline rather than fixing it.</fails_when>
  </verify>
  <done>A clean, self-taken census exists with the summary counts, the full name-and-result list, and the three file invariants recorded.</done>
</task>

<task type="auto">
  <name>Task 1: Delete the replaced-machine region and the proxy-local recycle region</name>
  <files>src/mcp/vice/vice-proxy.test.ts</files>
  <read_first>
    - `src/mcp/vice/vice-proxy.test.ts` — the replaced-machine block, whose members are (by name, in file order): `D-13: a dead granted instance costs exactly one replacement acquisition...`, `D-13: a replacement that lands back on the SAME port...`, `D-13: the epoch baseline after a replacement is re-based...`, `D-13/D-14: a replacement the broker itself refuses (at_capacity)...`, `D-13: two consecutive calls against a still-unreachable replacement...`, `structural: the replaced-machine report is built from the existing voided-run vocabulary (epochDriftMessage)...`, `D-14: the broker connection itself gone -- with a replacement listener available...`, `D-14: the broker connection itself gone -- with nothing available to reconnect to...`, `D-14: two consecutive calls after the connection dropped...`, `structural: within handleGrantedInstanceUnreachable()...`, `structural: the fresh-session replacement branch performs no release over the dead session...`, and `structural: the source records broker death as an accepted, knowing regression...`. Read the section banner comments above and between them; several carry reasoning that must be quoted in the commit message rather than discarded silently.
    - `src/mcp/vice/vice-proxy.test.ts` — the recycle block, whose members are (by name, in file order): `vice_recycle: a missing or empty reason...`, `vice_recycle: no broker lease held yet for this session is refused...`, `vice_recycle: the incident record -- with its evidence section already complete -- exists on disk before the recycle request reaches the broker`, `vice_recycle: an ack whose kill stage is the escalated one...`, `vice_recycle: an ack with a refusal...`, `vice_recycle: an ack that never arrives before the deadline...`, `vice_recycle: the broker dropping the connection mid-recycle...`, `vice_recycle: after a confirmed recycle, a subsequent forwarded call succeeds...`, `vice_recycle: a confirmed kill whose epoch file never advances within the poll deadline persists epoch_after as null...`, `vice_recycle: a healthy capture produces a full evidence object...`, `vice_recycle: a rejected screenshot capture...`, `vice_recycle: a rejected checkpoint enumeration...`, `vice_recycle: a stand-in that rejects every read...`, `vice_recycle: a rejected snapshot attempt...`, `vice_recycle: an unanswered snapshot call...`, `vice_recycle: two recycles at the same port and epoch produce two distinct record files...`, and `structural: within handleRecycle(), the record write appears before the request write...`.
    - `src/mcp/vice/vice-proxy.test.ts` — the INTERLEAVED SURVIVOR inside that block: `vice_recycle: with the endpoint override set returns a well-formed error result naming that no broker is in the loop, writes no request`. It PASSES today and its behaviour is live. It stays. Read it so you can cut around it rather than through it.
    - `src/mcp/vice/stock-recycle.test.ts` — the whole test-name list, and Plan 55-02's newly added epoch cases. This is where you locate each recycle test's successor BEFORE deleting it.
    - `src/mcp/vice/incident-record.test.ts` — its test-name list, for the record-rendering and finalise-round-trip successors.
    - `.planning/phases/55-restore-the-fork-removal-s-dropped-capabilities-and-re-basel/55-02-SUMMARY.md` — confirmation that the epoch producer's own coverage landed before its proxy-side witness is removed.
  </read_first>
  <action>
Remove the two largest fork-era blocks, in two commits, each cutting test by test.

**Before any deletion, confirm the mechanism is absent from shipped code.** Grep the non-test sources under `src/mcp/vice/` for the replaced-machine vocabulary and record what you find. Comment-only survivors are expected — one of them is the record of the deletion itself — but an executable hit means the mechanism partly survived and the block is not fully dead; stop and report rather than delete.

**Commit one — the replaced-machine block.** Delete the twelve tests named above, plus the banner comments that introduce only them, plus any helper whose every consumer is inside the block (grep each candidate helper before removing it; a helper with a surviving consumer stays). There is no interleaved survivor in this block.

In the commit message, state what the mechanism was in plain terms — the proxy adopting a replacement instance after its granted one stopped answering, and reporting the swap in the voided-run vocabulary — and state that its entry point, its message builders and its shared vocabulary are all gone from shipped code, with the grep result that proves it. State that no successor suite is owed for the mechanism itself, and name what DOES still cover the surviving broker-session behaviour: the unreachable-shape, never-cache and control-plane-unreachable cases that remain green in this same file, plus `vice-broker-client.test.ts`.

**Commit two — the proxy-local recycle block.** For EACH of the seventeen tests named above, locate its successor in `stock-recycle.test.ts` or `incident-record.test.ts` by name, first. Write the pairing down as you go; the commit message carries it. The strongest pairings to expect: the record-exists-before-the-request case pairs with `handleRecycleStock: the incident record exists on disk, with a complete evidence section, at the MOMENT the recycle RPC is invoked`; the four unavailable-evidence cases pair with the `gatherStockWedgeEvidence` family; the refusal, deadline and broker-gone cases pair with the three named outcome tests; the epoch-never-advances case pairs with the stall case Plan 55-02 added; and the ordering guard pairs with the record-before-request case.

If any one of the seventeen has NO successor you can name, do not delete it. Leave it in place, report it in the SUMMARY as an uncovered behaviour, and let the phase decide. That rule is the whole reason this task pairs before it cuts.

Cut AROUND the endpoint-override survivor. It passes, it asserts live behaviour, and it must still pass after this commit.

In the commit message, give the pairing list — deleted name to successor name — and state that the recycle implementation moved out of the proxy into its own module and is covered there against injected stubs rather than against a deleted HTTP stand-in.

After each commit, re-run the file and record the new summary counts and the new top-level test count. The count must fall by exactly the number of tests that commit removed. A larger drop means a survivor was swept up.
  </action>
  <reversibility rating="costly">Deletes roughly 1,500 lines of assertions. Restorable from git, but the harness they drive was deleted in a prior phase, so restoring them restores red tests, not coverage.</reversibility>
  <acceptance_criteria>
    - Two commits, each naming what it removed, why the subject is gone, and the successor coverage or the absence of any owed successor.
    - The pairing list — every deleted recycle test to its named successor — is recorded in the commit message and the SUMMARY.
    - The test named `vice_recycle: with the endpoint override set returns a well-formed error result naming that no broker is in the loop, writes no request` is still present and still passes.
    - The file's `skip` count did not rise.
    - The top-level test count fell by exactly the number deleted, verified after each commit.
    - `stock-recycle.test.ts` and `incident-record.test.ts` are byte-unchanged by this task.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && printf 'live_replaced_machine='; grep -rn 'handleGrantedInstanceUnreachable\|epochDriftMessage' --include='*.ts' --include='*.mts' . | grep -v '\.test\.' | grep -av '^\S*: *[*/]' | wc -l</automated>
    <fails_when>The count is greater than 0 after filtering comment lines — an executable reference survives, the mechanism is not fully dead, and the block must not be deleted until that is explained.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && printf 'toplevel='; grep -ac '^test(' vice-proxy.test.ts; printf 'skips='; grep -ac 'test.skip\|{ skip:\|, { skip' vice-proxy.test.ts; printf 'endpoint_override_survivor='; grep -ac 'with the endpoint override set returns a well-formed error result' vice-proxy.test.ts; printf 'lines='; wc -l < vice-proxy.test.ts</automated>
    <fails_when>`endpoint_override_survivor=` is anything other than 1 — the interleaved survivor was swept up. Or `skips=` rose above the Task 0 baseline — a test was disabled instead of deleted. Or `toplevel=` did not fall by the number recorded in the commit messages.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test stock-recycle.test.ts incident-record.test.ts > /tmp/55-03-t1-succ.txt 2>&1; echo "EXIT=$?"; grep -E '^# (tests|pass|fail)' /tmp/55-03-t1-succ.txt; cd /home/henrik/dev/henrik/git/c64-re-tools && git diff --stat HEAD -- src/mcp/vice/stock-recycle.test.ts src/mcp/vice/incident-record.test.ts</automated>
    <fails_when>`# fail` is anything other than 0 — the successor suites must be green at the moment their predecessors are retired, or the pairing is a promise rather than a fact. Or `git diff --stat` shows either successor file changed by this task.</fails_when>
  </verify>
  <done>Both blocks are gone in two reasoned commits; every removed recycle test is paired to a named, currently-green successor; the interleaved survivor still passes; nothing was skipped.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the proxy-local diagnose region and the checkpoint-arming seam region</name>
  <files>src/mcp/vice/vice-proxy.test.ts</files>
  <read_first>
    - `src/mcp/vice/vice-proxy.test.ts` — the diagnose block, whose members are (by name, in file order): `diagnose: a stopping checkpoint at the current PC is a checkpoint trap, and no bracket is run`, `diagnose: a stopping checkpoint at the resolved live IRQ handler is a checkpoint trap`, `diagnose: the trap report names the vector pair, the $01 value, and that remediation is not guaranteed`, `diagnose: does not fire on disabled, non-stopping, or address-mismatched checkpoints`, `diagnose: a restarted epoch is reported with both epoch values...`, `diagnose: the live IRQ handler resolver is called fresh on every diagnose call, never cached`, `diagnose: a ping-says-running, counter-frozen stand-in is wedged...`, `diagnose: a ping-says-not-running, counter-advancing stand-in is live...`, `diagnose: a byte-identical register read with an advancing counter is stale_read_path`, `diagnose: a below-baseline non-zero rate (six thousand cycles/s)...`, `structural: runCycleBracket has exactly one definition...`, and `structural: DIAGNOSE_VERDICTS is a frozen five-member array...`.
    - `src/mcp/vice/vice-proxy.test.ts` — the INTERLEAVED SURVIVOR inside that block: `vice_diagnose appears in tools/list alongside the other synthetic tools`. It PASSES and asserts a live registration. It stays.
    - `src/mcp/vice/vice-proxy.test.ts` — the VACUOUS PASSER inside that block: `diagnose: does not fire on disabled, non-stopping, or address-mismatched checkpoints`. It passes because the annotation walk it guards no longer exists, so it can no longer fail. It is listed above as a deletion for exactly that reason; treat it as part of the block, not as a survivor.
    - `src/mcp/vice/vice-proxy.test.ts` — the checkpoint-arming seam block, whose members are (by name, in file order): `seam: arming a stopping exec checkpoint is forwarded and annotated, never refused`, `seam: the annotated result's error flag is false and the host payload is intact`, `seam: a continue-only checkpoint, a disabled one and a non-exec watchpoint draw no annotation`, `seam: a checkpoint-add call the host rejects is returned as an error, with no annotation appended`, `seam: the annotation makes no additional forwarded calls`, `seam: a repeat arm at the same address is suppressed to a pointer, and an epoch change clears it`, `structural: every SEAM_HAZARDS entry has a detector, an annotation and a test`, `structural: the refusal set and the annotation set are disjoint`, `synthetic second entry ('test-fixture-synthetic-entry') is detected and annotated through the same SEAM_HAZARDS walk...`, and `structural: SEAM_HAZARDS's checkpoint-arming detector and renderer never route through isErrorText or make a forwarded call`. Two of these currently PASS; both pass vacuously, for the same reason, and both are in the block.
    - `src/mcp/vice/vice-proxy.test.ts` — the two tests immediately AFTER the seam block, whose names begin `BACK-05/FORKRM-05:`. Both pass, both assert the live unknown-tool fallback, and an external guard cites this file for exactly that proof. They are NOT in the block. Read them so the cut stops before them.
    - `src/mcp/vice/stock-diagnose.test.ts` — the full test-name list. This is where each diagnose test's successor is located by name.
    - `src/mcp/vice/stock-diagnose.ts` — `STOCK_DIAGNOSE_VERDICTS`, its freeze, its derived type and the comment block above it. This is the live successor of the deleted frozen-vocabulary guard.
    - `src/skills/vice-wedge-triage/SKILL.md` — the checkpoint-arming guidance that survives as user-facing advice. Read it so the seam block's commit message can state accurately where the guidance went.
  </read_first>
  <action>
Remove the two remaining fork-era blocks, in two commits, cutting test by test around two survivors.

**Before any deletion, confirm both mechanisms are absent from shipped code.** Grep the non-test sources under `src/mcp/vice/` for the seam-hazard table, the checkpoint-arming detector and the cycle-bracket helper, and record what you find. An executable hit means the mechanism partly survived; stop and report.

**Commit one — the diagnose block.** Delete the twelve tests named above. For each, locate its successor in `stock-diagnose.test.ts` by name first and write the pairing down; that suite carries 65 tests against the surviving implementation. The frozen-vocabulary guard's successor is the one that pins the verdict list in `stock-diagnose.ts` — the vocabulary survives under a new name and is already covered there, so this is a rename-and-move, not a loss. Where a deleted test has no namable successor, leave it in place and report it rather than deleting it.

Cut AROUND the tools/list registration survivor. `vice_diagnose` is still a registered synthetic tool and that test proves it.

In the commit message, give the pairing list and state that diagnose's implementation moved out of the proxy into its own module, covered there against injected stubs.

**Commit two — the checkpoint-arming seam block.** This one is different and the difference belongs in the commit message. The owner settled its disposition on 2026-09-13: retire its tests, no successor suite owed, and no accepted-loss entry in `docs/stock-hard-losses.md`. The reasons recorded were that the removal was intentional and named as fork-only in the deletion commit; that the mechanism was structurally moot, carrying one production entry whose only caller was the fork's own pre-flight, with no generic-dispatch surface surviving for a data-driven walk to hang off; that it was never a hardware capability, so the accepted-loss ledger is the wrong home for it; and that its user-facing guidance survives in `src/skills/vice-wedge-triage/SKILL.md`. Put that reasoning in the commit message in your own words, as a consumer-readable statement, and name the skill file as where the guidance lives now.

Delete all ten, including the two that currently pass. Both pass because there is nothing left to detect; a test that cannot fail is not coverage. Stop the cut before the two unknown-tool fallback tests that follow.

After each commit, re-run the file and confirm the top-level test count fell by exactly the number that commit removed, and that the two fallback tests and the diagnose registration test still pass.
  </action>
  <reversibility rating="costly">Deletes roughly 950 lines. Restorable from git; the seam block's retirement is an owner decision already taken and recorded, not one this task makes.</reversibility>
  <acceptance_criteria>
    - Two commits, each naming what it removed and why; the diagnose one carries a deleted-to-successor pairing list, the seam one carries the settled retirement reasoning and names the surviving guidance file.
    - The test named `vice_diagnose appears in tools/list alongside the other synthetic tools` is still present and still passes.
    - Both tests whose names begin `BACK-05/FORKRM-05:` are still present and still pass.
    - The file's `skip` count did not rise.
    - `stock-diagnose.test.ts` is byte-unchanged by this task and still reports 0 failures.
    - No entry was added to `docs/stock-hard-losses.md` — the owner settled that none is owed.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && printf 'live_seam_mechanism='; grep -rn 'SEAM_HAZARDS\|detectCheckpointArmingHazard\|runCycleBracket' --include='*.ts' --include='*.mts' . | grep -v '\.test\.' | wc -l</automated>
    <fails_when>The count is greater than 0 — an executable reference survives and the block is not fully dead.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && printf 'diagnose_registration_survivor='; grep -ac 'vice_diagnose appears in tools/list alongside the other synthetic tools' vice-proxy.test.ts; printf 'fallback_survivors='; grep -ac 'BACK-05/FORKRM-05' vice-proxy.test.ts; printf 'skips='; grep -ac 'test.skip\|{ skip:\|, { skip' vice-proxy.test.ts; printf 'toplevel='; grep -ac '^test(' vice-proxy.test.ts</automated>
    <fails_when>`diagnose_registration_survivor=` is anything other than 1, or `fallback_survivors=` is fewer than 2 — a survivor was swept up. Or `skips=` rose above the Task 0 baseline.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test stock-diagnose.test.ts > /tmp/55-03-t2-succ.txt 2>&1; echo "EXIT=$?"; grep -E '^# (tests|pass|fail)' /tmp/55-03-t2-succ.txt; cd /home/henrik/dev/henrik/git/c64-re-tools && git status --porcelain docs/stock-hard-losses.md</automated>
    <fails_when>`# fail` is anything other than 0 — the successor suite must be green when its predecessors retire. Or `git status --porcelain` prints a line for the accepted-loss ledger, meaning an entry was added that the owner settled is not owed.</fails_when>
  </verify>
  <done>Both blocks are gone in two reasoned commits; the diagnose pairings are recorded; the seam retirement carries its settled reasoning and names the surviving guidance; all three interleaved survivors still pass; nothing was skipped and no accepted-loss entry was written.</done>
</task>

<task type="auto">
  <name>Task 3: Dispose of the deny-list fragments and the remaining vacuous passers</name>
  <files>src/mcp/vice/vice-proxy.test.ts</files>
  <read_first>
    - `src/mcp/vice/vice-proxy.test.ts` — the deny-list fragments, scattered rather than contiguous, by name: `vice_disk_list is refused at tools/call with no request made`, `tools_list is refused at tools/call with no request made`, `vice_disk_list is absent from tools/list`, `structural: the construction-time tools registry itself filters DENY_LIST, not merely tools/list's wire output`, `vice_disk_list is still absent from tools/list and still refused at tools/call, with both synthetic tools present`, `tools_call carrying a nested vice_disk_list argument is now refused before any request reaches the stand-in host (closes the gap the prior test proved)`, `tools_call is refused at tools/call with no request made, even with a benign nested name`, `initialize is refused at tools/call with no request made`, `notifications_initialized is refused at tools/call with no request made`, and `BACK-05 (D-G ordering, observed at the wire): DENY_LIST still wins over a capability refusal for tools_call, and the synthetic tools are never refused`.
    - `src/mcp/vice/vice-proxy.test.ts` — the two `BACK-05/FORKRM-05:` tests. They pass, they assert the live unknown-tool fallback byte-for-byte, and they are the successor this task's commit message names. They are NOT deleted.
    - `src/mcp/vice/vice-proxy.ts` — the unknown-name branch of the tools/call override and its comment, which states in its own words that with one backend an unrecognised name is simply unknown and no per-capability refusal lookup remains. This is the shipped fact the disposition rests on.
    - `src/mcp/vice/tools-manifest.stock.json` — confirm for yourself whether the advertised manifest names the disk-list tool at all. If it does not, a test asserting that name's absence from `tools/list` cannot fail, which is what makes it vacuous rather than merely redundant.
    - `src/mcp/vice/ci-guardrails.test.mjs` — the tests that cite this file by name as a wire proof, so you know which names an external guard depends on before removing anything.
  </read_first>
  <action>
Apply one decision ladder to each named test above, and record the verdict per test.

MEASURED at `6e4e9a0b`: `grep -c 'DENY_LIST' src/mcp/vice/vice-proxy.ts` returns 0, and the advertised stock manifest does not name the disk-list tool. There is no deny list in shipped code. Re-measure both facts yourself before acting — that is what makes this a disposition rather than an inheritance.

The ladder, in order, for each test:

1. **Does the behaviour the test names still exist in shipped code?** If yes, this test is a rewrite and belongs to Plan 55-04, not here. Move it there by reporting it in the SUMMARY and leaving it untouched.
2. **Can this test fail?** A test asserting that a name is absent from a manifest that no longer contains it, or that a name is refused when every unregistered name is refused by a single fallback, cannot fail regardless of what the code does. That is the vacuity test, and it applies equally to the ones that currently pass and the ones that currently fail.
3. **Is the surviving property covered elsewhere by name?** For the refusal property, the two unknown-tool fallback tests cover it: one for a former capability name, one for a genuine typo, both asserting the identical fallback byte-for-byte.
4. **Delete, with the verdict in the commit message.** If a test reaches this rung with no namable successor and a property that is still real, do NOT delete it — leave it and report it.

Expect the disk-list and method-name refusal tests to reach rung 4. Their original value was that a general argument-rewriting pass ran before dispatch, so a forbidden name nested inside another call's arguments could slip through; the deny list existed to close exactly that. No generic-dispatch surface survives, so the nesting hazard has no route and the refusal itself now comes from one fallback that the surviving pair already pins. Say that in the commit message, in consumer-readable terms, and name the surviving pair.

Then sweep the orphans. With the four blocks and these fragments gone, grep for every helper, constant and import the file still declares and check each one has a consumer. Delete only those with none — including the in-process HTTP stand-in builders, if Plan 55-01's rewrite left them with no caller. A helper with even one surviving consumer stays, even if it looks fork-era. Remove any import that no longer resolves to a used symbol.

Finally, re-run the file and record the summary counts and the top-level test count. Compare against Task 0's baseline and account for the whole difference: every removed test is either in one of this plan's four commits' named lists or in this task's verdict table. An unaccounted difference means something was removed without a reason, which is the failure this accounting exists to catch.
  </action>
  <acceptance_criteria>
    - A verdict table — test name, rung reached, disposition, successor named — is recorded in the commit message and the SUMMARY for all ten named tests.
    - Any test the ladder routes to Plan 55-04 is left untouched and reported.
    - Both `BACK-05/FORKRM-05:` tests are still present and still pass.
    - Every helper and import removed had zero remaining consumers, shown by grep.
    - The file's `skip` count is unchanged from the Task 0 baseline.
    - The whole difference between Task 0's top-level test count and this task's is accounted for by the named lists across this plan's commits.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && printf 'denylist_in_shipped='; grep -rac 'DENY_LIST' vice-proxy.ts; printf 'disklist_in_manifest='; grep -ac 'vice_disk_list' tools-manifest.stock.json</automated>
    <fails_when>`denylist_in_shipped=` is anything other than 0, or `disklist_in_manifest=` is anything other than 0 — INVERTED polarity on both: a non-zero count means the mechanism or the advertised name still exists and the vacuity verdict is wrong.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && printf 'fallback_survivors='; grep -ac 'BACK-05/FORKRM-05' vice-proxy.test.ts; printf 'skips='; grep -ac 'test.skip\|{ skip:\|, { skip' vice-proxy.test.ts; printf 'toplevel='; grep -ac '^test(' vice-proxy.test.ts; printf 'lines='; wc -l < vice-proxy.test.ts</automated>
    <fails_when>`fallback_survivors=` is fewer than 2, or `skips=` differs from the Task 0 baseline, or `toplevel=` differs from Task 0's by an amount the commit messages do not account for.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test vice-proxy.test.ts > /tmp/55-03-t3-final.txt 2>&1; echo "EXIT=$?"; grep -E '^# (tests|pass|fail|cancelled|skipped)' /tmp/55-03-t3-final.txt</automated>
    <fails_when>`# skipped` rose above Task 0's value, or `# cancelled` is greater than 0 (something stopped terminating), or `# fail` did not fall by at least 50 from Task 0's baseline. `# fail` reaching 0 is NOT expected at this plan — Plans 55-04 and 55-05 own the remaining failures.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated > /tmp/55-03-t3-gate.txt 2>&1; echo "EXIT=$?"; grep -E '^# (tests|pass|fail|skipped)' /tmp/55-03-t3-gate.txt</automated>
    <fails_when>`# fail` is anything other than 0, or `# skipped` is anything other than 9. This plan touches one manual-only file and must not move the automated gate at all beyond Plan 55-02's already-explained delta.</fails_when>
  </verify>
  <done>Every deny-list fragment and vacuous passer has a recorded verdict; the surviving fallback pair is intact; orphaned helpers are gone with grep evidence; the whole test-count difference is accounted for; the automated gate is unmoved.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → future reader | A deleted assertion removes a claim about the system; the commit message is the only record of why it was safe to remove |
| test suite → external guard | `ci-guardrails.test.mjs` cites this file by name as a wire proof; removing the cited tests silently breaks a guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-11 | Repudiation | coverage removed without a successor | critical | mitigate | Every deletion pairs to a named, currently-green successor BEFORE it is cut, and the pairing is in the commit message. A test with no namable successor is left in place and reported. |
| T-55-12 | Repudiation | a test disabled rather than deleted | high | mitigate | The `skip` count is recorded at Task 0 and compared after every task; a rise is a failure. A retired test is deleted, never silenced. |
| T-55-13 | Tampering | a survivor swept up by a range delete | high | mitigate | Every region has interleaved survivors and every cut is by test name. Three named survivors are asserted present and passing after each task. |
| T-55-14 | Denial of Service | breaking an external guard's cited proof | medium | mitigate | The two unknown-tool fallback tests are named as prohibited deletions and asserted present in three separate verify steps. |
| T-55-15 | Tampering | a helper removed while still consumed | medium | mitigate | A helper is deleted only after a grep shows zero remaining consumers; a typecheck-free test file will not catch this, so the grep is the gate. |
| T-55-SC | Tampering | package-manager installs | n/a | accept | This plan runs no `npm install` / `pip install` / `cargo add` task and adds no dependency. No package-legitimacy checkpoint is owed. |
</threat_model>

<verification>
- A self-taken census exists from this plan's starting commit, and the whole difference between it and the end state is accounted for by the commits' named lists.
- Each of the four block deletions is its own commit naming what went, why the subject is gone, and where the coverage lives now.
- `stock-recycle.test.ts`, `incident-record.test.ts` and `stock-diagnose.test.ts` are byte-unchanged by this plan and all report 0 failures.
- The `skip` count in `vice-proxy.test.ts` is identical before and after this plan.
- `npm run test:automated` reports 0 failures and 9 skips. Run it with output redirected and the exit code read on the same line; never pipe it into `tail` or `head`.
</verification>

<success_criteria>
- Roughly 2,500 lines of assertions about deleted mechanisms are gone, cut test by test rather than by line range.
- Every removed test either names a green successor or carries a recorded reason no successor is owed.
- Every named survivor — the endpoint-override case, the diagnose registration case, and both unknown-tool fallback cases — is still present and still passing.
- Nothing was skipped, commented out, or stubbed.
- The remaining failures in the file are exactly the rewrite set and the no-harness set, which later plans own.
</success_criteria>

<output>
Create `.planning/phases/55-restore-the-fork-removal-s-dropped-capabilities-and-re-basel/55-03-SUMMARY.md` when done. Record: Task 0's full census, the four pairing/verdict lists, the top-level test count and `skip` count after each commit, the list of helpers removed with their zero-consumer evidence, and any test the ladder routed to Plan 55-04 instead of deleting.
</output>

