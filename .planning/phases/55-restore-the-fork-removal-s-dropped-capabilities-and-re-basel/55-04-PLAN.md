---
phase: 55-restore-the-fork-removal-s-dropped-capabilities-and-re-basel
plan: 04
type: execute
wave: 3
depends_on: ["55-03"]
files_modified:
  - src/mcp/vice/vice-proxy.test.ts
autonomous: true
requirements: [PROXY-05]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The grant-containerization seam and both of its safety nets are asserted against the code that ships, using the idiom the one already-passing containerize test proves works (PROXY-05)"
    - "The network-call module-set guard names the module set that actually contains a network call today, measured rather than inherited (PROXY-05)"
    - "The launcher-message guard's stale call-site floor is replaced by a measured figure, and its two subcommand assertions — the guard's real subject — are preserved verbatim (PROXY-05)"
    - "`tools/list`'s manifest-exactness test asserts name set, order, schema and cap stamp against the manifest as it ships, with no clause about a deny list that no longer exists (PROXY-05)"
    - "The synthetic-tool registration guard asserts the synthetic names actually registered today (PROXY-05)"
    - "Each epoch-drift test either drives a live route or is retired against a named, currently-green successor in `stock-connect.test.ts` (PROXY-05)"
  artifacts:
    - src/mcp/vice/vice-proxy.test.ts
  key_links:
    - "`containerizeGrant()` in `vice-proxy.ts` to the three containerize tests — the seam is live and has an already-passing witness in the same file; the rewrite copies that witness's idiom rather than inventing one"
    - "the network-call guard's expected module set to the actual on-disk module set — two of the three names it expects were deleted files"
    - "`stock-connect.test.ts`'s restart-error coverage to the two epoch-drift tests — the candidate successor the ladder checks first"
  prohibitions:
    - statement: "No guard is weakened to make it pass. A structural guard whose expectation is stale gets a re-measured expectation, never a loosened one — replacing an equality with a permissive floor, or deleting an assertion rather than re-pointing it, is the failure mode this plan exists to avoid."
      status: required
      verification: flagged-unverified
    - statement: "The two subcommand assertions inside the launcher-message guard are preserved verbatim. They are the guard's actual subject; only the call-site count beside them is stale."
      status: required
      verification: flagged-unverified
    - statement: "No test is disabled with `skip`. The file's `skip` count does not rise."
      status: required
      verification: flagged-unverified
    - statement: "The deleted HTTP stand-in harness is not resurrected, and no test-only environment hatch is added to shipped code."
      status: required
      verification: flagged-unverified
    - statement: "A test the ladder routes to retirement is deleted with its successor named in the commit message, never left in place as a skip and never silently weakened into vacuity."
      status: required
      verification: flagged-unverified
---

<objective>
Re-point the tests whose behaviour survived the fork removal but whose expectations did not.

These are not retirements. Each one names a mechanism that still ships — grant containerization, the network-call module boundary, the launcher-message guard, the manifest-exactness contract, synthetic tool registration — and each fails because it encodes a coordinate the fork removal moved, not because its subject is gone. MEASURED at `6e4e9a0b`: the network-call guard expects the module set `broker-launch.mts`, `vice-probe.ts`, `vice.ts`, and neither of the last two exists on disk. `containerizeGrant()` is live in `vice-proxy.ts`, and the containerize test that adopts an already-container-shaped grant passes today — so the rewrite idiom is already proven inside this same file.

Weakening a guard to make it green is the failure this plan must not commit. A stale expectation gets re-measured against what ships; it does not get relaxed into something that can no longer fail.

Purpose: keep every guard whose subject is alive, pointed at where that subject now lives.
Output: a rewritten surviving-behaviour set, each test asserting the post-fork-removal reality.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-restore-the-fork-removal-s-dropped-capabilities-and-re-basel/55-03-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Re-point the grant-containerization trio</name>
  <files>src/mcp/vice/vice-proxy.test.ts</files>
  <read_first>
    - `src/mcp/vice/vice-proxy.ts` — `containerizeGrant()` in full, from its declaration through its port validation, its loopback rewrite, its host-path translation of the epoch file, its two refusal branches and the single stderr line it logs. Read the comment block above it, which states why the seam exists and which fields it touches.
    - `src/mcp/vice/vice-proxy.ts` — the one call site that hands a grant to `containerizeGrant()`, and the validation that follows it before the grant is stashed.
    - `src/mcp/vice/vice-proxy.test.ts` — the test named `containerize: an already-container-shaped grant (tmpdir VICE_POOL_DIR) is adopted byte-identical, reported as unchanged on stderr`. It PASSES today. Read it whole: it drives a real control listener, issues a grant, and asserts on the proxy's stderr rather than on a forwarded call's result. That is the idiom the other three need.
    - `src/mcp/vice/vice-proxy.test.ts` — the three failing tests, by name: `containerize: a loopback grant url is rewritten so the forwarded call actually reaches a stub bound off loopback`, `containerize safety net: a grant whose epoch_file translates outside the workspace is refused, falling back to the port-derived path`, and `containerize safety net: a grant whose url port disagrees with the granted port is refused, falling back to the port-derived url`. Read each whole, and note exactly which assertion depends on a forwarded call completing.
    - `src/mcp/vice/vice-proxy.test.ts` — the environment-gated containerize case, `containerize: a host-rooted grant epoch_file is rewritten so epoch drift is actually detected, and the translation line names all three fields`. It is gated, not failing. Leave its gate alone in this task; Plan 55-06 owns that decision.
    - `src/mcp/vice/hostpath.ts` and `src/mcp/vice/containerpath.ts` — the translation functions the seam calls, so an assertion about a refusal names the right reason.
  </read_first>
  <action>
Re-point the three failing containerize tests at the seam's own observable output instead of at a forwarded call's result.

The seam is live and unchanged. What broke is the witness: each of these three proved its point by making a call and watching where it landed, and the forwarding path that made the call is gone. The already-passing sibling shows the replacement witness — the seam logs exactly one stderr line per adoption, naming what it changed and what it left alone, and that line is observable from a spawned proxy with no forwarded call at all.

For the loopback-rewrite test, assert that a grant whose url names a loopback address is adopted with a rewritten url, by reading the seam's own stderr line and by asserting on whatever the proxy subsequently reports about the instance it holds. The property is that a container-side proxy does not keep a host-loopback url it cannot reach; prove that from the adoption record rather than by round-tripping a request to a stub bound off loopback.

For the two safety nets, the property in each case is a REFUSAL plus a specific fallback: an epoch-file path that translates outside the workspace is refused and the port-derived path is used instead; a url whose port disagrees with the granted port is refused and the port-derived url is used instead. Both refusals are decisions the seam makes and logs. Assert the refusal and the fallback value from the seam's own output, and assert that the grant the proxy ends up holding carries the fallback rather than the refused value.

Keep each test's NAME as close to its current one as the rewrite allows — these names are how a future reader finds the property — and update only the clause that describes the retired witness. Where a test's comment explains why the safety net exists, keep the reasoning and correct only the mechanism description.

If, after reading the seam, one of the three turns out to assert a property the seam genuinely no longer has, do not force a rewrite: retire it with its reason and its successor named, and say so in the SUMMARY. That is a legitimate outcome of the same ladder, but it must be a finding, not a convenience.
  </action>
  <acceptance_criteria>
    - All three named tests pass, or any one of them is retired with a recorded reason and a named successor.
    - The already-passing `already-container-shaped grant` test is unmodified and still passes.
    - The environment-gated containerize case's gate is untouched.
    - No test asserts through a forwarded call to an HTTP stand-in.
    - The file's `skip` count did not rise.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test --test-name-pattern 'containerize' vice-proxy.test.ts > /tmp/55-04-t1.txt 2>&1; echo "EXIT=$?"; grep -E '^# (tests|pass|fail|skipped)' /tmp/55-04-t1.txt</automated>
    <fails_when>`# fail` is anything other than 0. `# skipped` above 1 means a gate was added rather than a test fixed — exactly one containerize case is environment-gated and this task does not change that.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && printf 'containerize_live='; grep -ac 'function containerizeGrant' vice-proxy.ts; printf 'skips='; grep -ac 'test.skip\|{ skip:\|, { skip' vice-proxy.test.ts</automated>
    <fails_when>`containerize_live=` is anything other than 1 — the seam these tests assert on is not where the rewrite assumes. Or `skips=` rose above the value Plan 55-03 recorded.</fails_when>
  </verify>
  <done>The containerization seam and both safety nets are asserted from the seam's own observable output; the passing sibling is untouched; nothing was skipped.</done>
</task>

<task type="auto">
  <name>Task 2: Re-measure the three structural guards whose coordinates moved</name>
  <files>src/mcp/vice/vice-proxy.test.ts</files>
  <read_first>
    - `src/mcp/vice/vice-proxy.test.ts` — the test named `structural: the set of source files under src/mcp/vice/ containing a network-call construct is exactly broker-launch.mts, vice-probe.ts and vice.ts`. Read it whole, including the construct patterns it scans for, the file-set filter it applies, and the failure message it prints. Its expected set is a three-name literal; two of those names are files that no longer exist.
    - `src/mcp/vice/vice-proxy.test.ts` — the test named `structural: no message quotes the launcher with a subcommand -- vice-launcher.sh accepts none`. Read it whole. Its subject is the two negative assertions at the end, which forbid appending a bare subcommand token to the launcher path in any agent-visible message. The call-site floor above them exists only as a non-vacuity control, and that floor is what fails now.
    - `src/mcp/vice/vice-proxy.test.ts` — the test named `structural: vice-proxy.ts defines exactly one host-path helper (brokerHostPath), and supervisorHostPath is gone entirely`. It PASSES. Read it so you can tell which of the two tests owns which assertion before touching either.
    - `src/mcp/vice/vice-proxy.test.ts` — the test named `structural: neither synthetic tool name appears in tools-manifest.stock.json, and both appear in a live tools/list response`. Read it whole and work out which half fails: the manifest-absence half or the live-presence half, and for which name.
    - `src/mcp/vice/vice-proxy.ts` — the three synthetic tool definitions registered outside the manifest loop, and the registration lines for each. Count them yourself; the test's expectation predates at least one registration change.
    - `src/mcp/vice/vice-proxy.ts` — every call site of the launcher-path helper, so the re-measured floor is a figure you counted rather than one you adjusted until it passed.
    - `src/mcp/vice/shipped-modules.ts` and `src/mcp/vice/package.json`'s `files[]` — the project already derives a shipped-module set from the package manifest rather than from a directory listing, and a sibling guard uses it. Read both before deciding how the network-call guard should enumerate its candidate files.
  </read_first>
  <action>
Re-measure each guard's expectation against the tree as it is, and record the measurement beside the change.

**The network-call module set.** Run the guard's own scan yourself and record which files it finds. Replace the three-name literal with the set you measured. Two of the expected names are deleted files, so a set-equality assertion that names them can never pass; the assertion must stay a set EQUALITY in both directions — a permissive subset check would let a new, unguarded network call appear without failing, which is the whole property this guard holds. Where the guard enumerates candidate files by reading the directory, consider whether the project's existing shipped-module derivation is the more honest source, as a sibling guard already argues; adopt it only if the guard's own subject is the shipped set rather than the on-disk set, and say which you chose and why in the commit message.

**The launcher-message guard.** Count the helper's call sites in the shape the guard matches, and replace the stale floor with the figure you counted. Keep it a non-vacuity control — its job is to fail if the scan ever matches nothing — so express it as a floor at or just below the measured count rather than as an exact equality that will red on the next unrelated message edit. Preserve the two negative assertions verbatim; they are the subject and they still hold. Update the comment above them only where it describes a count that changed.

**The synthetic-tool registration guard.** Determine which half fails and why. If the file registers a different number of synthetic tools than the test assumes, or one of the assumed names now resolves through the manifest rather than beside it, re-point the expectation at what is registered and derive the name list from the shipped definitions rather than hand-typing it, so the guard cannot drift the same way twice. Keep both halves: absence from the advertised manifest and presence in a live response are two different claims and both matter.

For each of the three, the commit message carries: the old expectation, the measured new one, and the command you ran to measure it. A guard changed without a recorded measurement is indistinguishable from a guard relaxed to pass.

Do not touch the host-path helper test that passes. If, while counting call sites, you find its own floor is also stale in a way that makes it unfalsifiable, say so in the SUMMARY and tighten it in the same commit with the same discipline — but only on your own measurement.
  </action>
  <acceptance_criteria>
    - All three named guards pass, each with its expectation derived from a recorded measurement.
    - The network-call guard is still a two-directional set equality; it was not relaxed to a subset or a containment check.
    - The launcher guard's two negative assertions are byte-identical to their pre-task form.
    - The synthetic-tool guard still asserts both halves — manifest absence and live presence — and derives its name list from the shipped definitions.
    - Each guard's commit message records the old expectation, the new one, and the measuring command.
    - The file's `skip` count did not rise.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test --test-name-pattern 'network-call construct|quotes the launcher with a subcommand|synthetic tool name|exactly one host-path helper' vice-proxy.test.ts > /tmp/55-04-t2.txt 2>&1; echo "EXIT=$?"; grep -E '^# (tests|pass|fail|skipped)' /tmp/55-04-t2.txt</automated>
    <fails_when>`# fail` is anything other than 0, or `# skipped` is anything other than 0, or `# pass` is fewer than 4.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && printf 'deleted_names_still_expected='; grep -ac 'vice-probe.ts\|"vice.ts"' vice-proxy.test.ts; printf 'subcommand_assertions='; grep -ac "may append a bare 'start' subcommand" vice-proxy.test.ts; printf 'skips='; grep -ac 'test.skip\|{ skip:\|, { skip' vice-proxy.test.ts</automated>
    <fails_when>`deleted_names_still_expected=` is greater than 0 — INVERTED polarity: the guard still names files that do not exist. `subcommand_assertions=` is anything other than 2 — the guard's real subject was altered or lost. `skips=` rose above the Plan 55-03 value.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && ls src/mcp/vice/vice-probe.ts src/mcp/vice/vice.ts 2>&1 | grep -c 'No such file'</automated>
    <fails_when>The count is anything other than 2. Both files must be absent; if either exists, the premise behind the re-measured module set is wrong and the whole task needs re-grounding.</fails_when>
  </verify>
  <done>All three guards assert measured expectations, none was relaxed, the launcher guard's subject is verbatim intact, and every change carries its measuring command in the commit message.</done>
</task>

<task type="auto">
  <name>Task 3: Re-point the manifest-exactness contract and dispose of the two epoch-drift tests</name>
  <files>src/mcp/vice/vice-proxy.test.ts</files>
  <read_first>
    - `src/mcp/vice/vice-proxy.test.ts` — the test named `tools/list's full output matches the manifest exactly (name set, order, schema, _meta cap) except for DENY_LIST's deliberate absence`. Read it whole, and determine which of its four clauses actually fails. The name it carries claims a deny-list exception; the mechanism behind that clause does not exist, and the observed failure is on ORDERING, not on the exception.
    - `src/mcp/vice/vice-proxy.ts` — the registration order: the manifest loop first, then the continuation tool, then the two synthetic handlers, then the annotation family. `tools/list` is answered by the library's own handler over the registry this file builds, so the wire order follows registration order, not manifest order.
    - `src/mcp/vice/vice-proxy.test.ts` — the test named `tools/list declares the same cap it enforces`, which passes and asserts the cap stamp. Plan 55-01 depends on it staying green; do not fold it into this one.
    - `src/mcp/vice/vice-proxy.test.ts` — the two epoch-drift tests, by name: `epoch drift is reported loudly and not cached` and `a missing epoch file is not a restart`. Read each whole and identify exactly what each needs in order to observe a drift.
    - `src/mcp/vice/stock-connect.test.ts` — its restart-error coverage. MEASURED at `6e4e9a0b`: this file carries 13 references to the machine-restarted error type. This is the candidate successor the ladder checks first.
    - `src/mcp/vice/stock-connect.ts` — where the drift guard actually lives now, and the dependency-injected epoch reader it uses.
  </read_first>
  <action>
Settle the manifest contract, then apply the ladder to the two epoch-drift tests.

**The manifest-exactness contract.** Work out which clause fails before changing anything, and say so in the commit message. Then rewrite the test to assert the contract that is actually owed:

- Every manifest name appears in the `tools/list` response.
- The response additionally carries the proxy-local tools this file registers beside the manifest — the continuation tool, the two synthetic handlers and the annotation family — and the test enumerates them from the shipped definitions rather than hand-typing them.
- Each manifest entry's schema is deep-equal to the manifest's own raw schema.
- Every entry carries the cap stamp.

On ordering: decide, and write the decision into the test. If the wire order is a real contract a client can depend on, assert the order the registration seam produces and say in a comment that registration order IS the wire order and why. If it is not a contract, drop the ordering assertion and say explicitly in the same comment that ordering is deliberately unasserted because nothing depends on it — an unasserted property stated as a decision is honest; one silently dropped is not. Do not leave the test asserting an order it no longer produces.

Drop the deny-list exception clause and the phrase from the test's name, since there is no deny list. Keep the name otherwise recognisable.

**The two epoch-drift tests.** Apply the same ladder Plan 55-03 used. The drift guard itself is live and moved into the connection module, which has its own suite. For each test, locate a named counterpart there first. If one exists, retire the proxy-side test with that counterpart named in the commit message. If the property is genuinely proxy-side — something about how the proxy reports drift to its client rather than how the connection detects it — and no counterpart exists, then it is a rewrite, and it belongs to Plan 55-05's harness work rather than here: leave it and report it.

Do not delete either one on the assumption that drift is covered somewhere. Name the counterpart or hand the test forward.
  </action>
  <acceptance_criteria>
    - The manifest-exactness test passes, carries no deny-list clause in its body or its name, and enumerates the proxy-local additions from shipped definitions rather than a hand-typed list.
    - The ordering decision is stated explicitly in a comment inside the test, whichever way it went.
    - `tools/list declares the same cap it enforces` is unmodified and still passes.
    - Each epoch-drift test is either passing, retired with a named counterpart in the commit message, or explicitly handed to Plan 55-05 and reported.
    - The file's `skip` count did not rise.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test --test-name-pattern 'matches the manifest exactly|declares the same cap it enforces|epoch drift|missing epoch file' vice-proxy.test.ts > /tmp/55-04-t3.txt 2>&1; echo "EXIT=$?"; grep -E '^# (tests|pass|fail|skipped)' /tmp/55-04-t3.txt</automated>
    <fails_when>`# fail` is anything other than 0, unless a failing name is one this task explicitly handed to Plan 55-05 and recorded as such in the SUMMARY. `# skipped` above 0 is always a failure here.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && printf 'denylist_clause='; grep -ac "DENY_LIST's deliberate absence" vice-proxy.test.ts; printf 'skips='; grep -ac 'test.skip\|{ skip:\|, { skip' vice-proxy.test.ts</automated>
    <fails_when>`denylist_clause=` is greater than 0 — INVERTED polarity: the exception clause naming a mechanism that does not exist is still present. `skips=` rose above the Plan 55-03 value.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test stock-connect.test.ts > /tmp/55-04-t3b.txt 2>&1; echo "EXIT=$?"; grep -E '^# (tests|pass|fail)' /tmp/55-04-t3b.txt</automated>
    <fails_when>`# fail` is anything other than 0 — a counterpart named as a successor must be green at the moment its predecessor retires.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated > /tmp/55-04-t3-gate.txt 2>&1; echo "EXIT=$?"; grep -E '^# (tests|pass|fail|skipped)' /tmp/55-04-t3-gate.txt</automated>
    <fails_when>`# fail` is anything other than 0, or `# skipped` is anything other than 9.</fails_when>
  </verify>
  <done>The manifest contract asserts what ships with an explicit ordering decision; the cap test is untouched; both epoch-drift tests are passing, retired against a named green counterpart, or handed forward on the record.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| broker grant → proxy | An untrusted-shaped grant record crosses into the proxy and is rewritten before any of its fields are used to reach a host |
| shipped module set → network | The network-call guard is the boundary check on which modules may open a socket at all |
| advertised manifest → MCP client | `tools/list` is a contract; the exactness test is what keeps the advertised surface and the registered surface in agreement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-16 | Spoofing | a grant url pointing somewhere unreachable or unintended | high | mitigate | The two safety-net tests assert the seam REFUSES a port-mismatched url and a workspace-escaping epoch path and falls back to the port-derived values; the rewrite keeps both refusals as the asserted property. |
| T-55-17 | Elevation of Privilege | an epoch-file path escaping the workspace | high | mitigate | The workspace-escape safety net is one of the three tests re-pointed in Task 1; its refusal branch is the assertion, not a side effect. |
| T-55-18 | Tampering | a new unguarded network call appearing | high | mitigate | The module-set guard stays a two-directional set equality; the plan explicitly forbids relaxing it to a subset or containment check, which would let a second spawn-or-dial site appear silently. |
| T-55-19 | Spoofing | an agent-visible message quoting a launcher invocation the launcher rejects | medium | mitigate | The two negative subcommand assertions are preserved verbatim; only the non-vacuity floor beside them is re-measured. |
| T-55-20 | Repudiation | advertised tool surface diverging from the registered one | medium | mitigate | The manifest-exactness test is re-pointed rather than retired, enumerates proxy-local additions from shipped definitions, and states its ordering decision explicitly. |
| T-55-SC | Tampering | package-manager installs | n/a | accept | This plan runs no `npm install` / `pip install` / `cargo add` task and adds no dependency. No package-legitimacy checkpoint is owed. |
</threat_model>

<verification>
- Every named test in this plan passes, or is retired against a named green counterpart, or is explicitly handed to Plan 55-05 and recorded.
- No guard was relaxed: the module-set equality is still two-directional, the subcommand assertions are byte-identical, and the manifest test still asserts name set, schema and cap stamp.
- `stock-connect.test.ts` reports 0 failures.
- `npm run test:automated` reports 0 failures and 9 skips. Run it with output redirected and the exit code read on the same line; never pipe it into `tail` or `head`.
- The `skip` count in `vice-proxy.test.ts` is unchanged from Plan 55-03's recorded value.
</verification>

<success_criteria>
- The containerization seam and both safety nets are proven from the seam's own observable output, with no forwarded call.
- Three structural guards carry measured expectations and a recorded measuring command each.
- The manifest-exactness contract matches what ships and states its ordering decision out loud.
- The two epoch-drift tests are resolved by the ladder, not by assumption.
- Nothing was skipped, weakened, or made unfalsifiable.
</success_criteria>

<output>
Create `.planning/phases/55-restore-the-fork-removal-s-dropped-capabilities-and-re-basel/55-04-SUMMARY.md` when done. Record: the witness idiom chosen for the containerize trio, each guard's old and new expectation with its measuring command, the ordering decision and its reason, and the ladder verdict for each epoch-drift test including anything handed to Plan 55-05.
</output>

