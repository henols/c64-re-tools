---
phase: 55-restore-the-fork-removal-s-dropped-capabilities-and-re-basel
plan: 06
type: execute
wave: 5
depends_on: ["55-05"]
files_modified:
  - src/mcp/vice/vice-proxy.test.ts
  - src/mcp/vice/vice-proxy-ping.test.ts
  - src/mcp/vice/test-gate.mjs
autonomous: true
requirements: [PROXY-06]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No file in the tree still claims `vice-proxy.test.ts` hangs or must never be run — the claim is false and has been since the unsettleable waits were bounded (PROXY-06)"
    - "`vice-proxy.test.ts`'s own header describes the harness it actually uses, not the deleted stand-in server (PROXY-06)"
    - "The environment gate's comment carries figures measured in this phase, and no longer cites a workflow block that was removed or defers to a re-baseline that has happened (PROXY-06)"
    - "`test-gate.mjs`'s disposition for this file states the real, measured reason it stays manual-only, not the hang that no longer occurs (PROXY-06)"
    - "Each remaining environment-gated case is re-measured and either ungated or kept with a reason taken from that measurement (PROXY-06)"
    - "`npm test` — the FULL `*.test.*` glob CI runs — exits 0 from `src/mcp/vice`, or every remaining failure is named, attributed to a file other than `vice-proxy.test.ts`, and reported (PROXY-06)"
    - "`npm run test:automated` reports 0 failures and 9 skips, with `vice-proxy.test.ts` still outside that set (PROXY-06)"
  artifacts:
    - src/mcp/vice/vice-proxy.test.ts
    - src/mcp/vice/vice-proxy-ping.test.ts
    - src/mcp/vice/test-gate.mjs
  key_links:
    - "`test-gate.mjs`'s `MANUAL_ONLY_TESTS` to `test-gate.test.ts`'s twelve-file equality and to `ci-guardrails.test.mjs`'s non-vacuity control — three guards read this one list, and two of them assert this file's membership in it"
    - "`ci-guardrails.test.mjs`'s full-glob assertion to CI's Test step — the reason CI runs the bare glob at all is so this file reaches CI despite being outside the narrowed gate"
    - "`ci-guardrails.test.mjs`'s environment-gate ledger to `vice-proxy.test.ts`'s gate — the ledger asserts the set of files using that idiom is exactly this one file, and fails in both directions"
  prohibitions:
    - statement: "`vice-proxy.test.ts` is NOT removed from `MANUAL_ONLY_TESTS`. The phase's protected baseline is that the narrowed automated gate does not gain this file; promotion is explicitly out of scope and is recorded as such, not done quietly."
      status: required
      verification: flagged-unverified
    - statement: "No figure is written into a comment that was not measured in this phase. Replacing one stale number with another remembered number reproduces the exact defect being fixed."
      status: required
      verification: flagged-unverified
    - statement: "An environment-gated case is ungated only on a measurement showing it PASSES when it runs. A gate is never removed to reduce the skip count."
      status: required
      verification: flagged-unverified
    - statement: "A failure in a file other than `vice-proxy.test.ts` is REPORTED by name, never fixed opportunistically inside this plan. That is a separate finding with a separate scope."
      status: required
      verification: flagged-unverified
    - statement: "No test command is piped into `tail` or `head`. The pipe reports the pager's exit code, which has already faked a green baseline in this repository once."
      status: required
      verification: flagged-unverified
---

<objective>
Correct every stale claim the tree makes about `vice-proxy.test.ts`, then prove the full glob CI actually runs is green.

The file's content settled in Plans 55-01 through 55-05. Its ledger did not, and the ledger is wrong in four places at once:

- `vice-proxy-ping.test.ts` opens by asserting, in prose, that its sibling HANGS and must never be run. MEASURED: the file terminates, in about two minutes sixteen seconds, and has since an earlier quick task bounded its unsettleable waits.
- `vice-proxy.test.ts`'s own header describes its harness as a spawned child driven against an in-process stand-in for a host server. The stand-in's only consumer was deleted a phase ago.
- The environment gate's comment hard-codes a pass/fail figure taken before this phase and defers the situation to a wholesale re-baseline of the file — which is this phase.
- `test-gate.mjs` disposes this file as manual-only on the grounds that it hangs. That reason is false; a true one exists and should replace it rather than the entry being quietly dropped.

Three guards read that disposition list, two of them asserting this file's membership. The phase's protected baseline keeps the file out of the narrowed automated gate, so this plan replaces the false reason rather than the membership.

Purpose: a ledger that describes the file truthfully, and a measured green run of the glob CI actually executes.
Output: four corrected claims, a re-measured gate decision, and the phase's terminal proof.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-restore-the-fork-removal-s-dropped-capabilities-and-re-basel/55-05-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Re-measure the environment-gated cases and decide each on that measurement</name>
  <files>src/mcp/vice/vice-proxy.test.ts</files>
  <read_first>
    - `src/mcp/vice/vice-proxy.test.ts` — the environment gate near the top of the file: the boolean derived from the two workspace variables, the skip-reason string beside it, and the whole comment block above them. Note that the comment already carries one correction — the workflow block it used to cite was removed — and that the skip-reason string was updated with it. What remains stale is the hard-coded pass/fail figure and the deferral to a re-baseline.
    - `src/mcp/vice/vice-proxy.test.ts` — every case still gated on that boolean, by name. Plan 55-03 deleted at least one of them along with the recycle block; establish the current set yourself rather than inheriting a count.
    - `src/mcp/vice/containerpath.ts` — the module-scope caching of the workspace root that the gate's comment names as the reason these cases cannot be converted to inject the signal into a child process. Confirm that reason still holds; it is the load-bearing half of the gate's justification.
    - `src/mcp/vice/ci-guardrails.test.mjs` — the test asserting that the set of files using this gate idiom is exactly this one file, and that it fails in BOTH directions. Read it before touching the gate.
    - `.planning/phases/55-restore-the-fork-removal-s-dropped-capabilities-and-re-basel/55-05-SUMMARY.md` — the four end-state numbers from the clean run.
  </read_first>
  <action>
Run the remaining gated cases and decide each on what you observe, not on what the comment says.

Run the whole file twice: once as it runs by default, and once with both workspace variables set in the running process's own environment. Redirect both runs to scratch files outside the repository tree and read the exit code on the same line. Record both summary counts and, for the second run, the per-case result of every gated case by name.

Then, per case:

- **It passes when it runs.** Remove its gate. The gate existed because these cases were measured failing under the only environment that runs them; a case that now passes no longer needs one, and keeping it would be a skip standing in for a pass.
- **It fails when it runs.** Keep the gate and rewrite the reason from your measurement: what it asserts, why it fails under the current code, and what it would take to make it run unattended. If the failure is because its subject was deleted, that is not a gate decision — it is a retirement, and it belongs to the same ladder the earlier plans used, applied here with its successor named.
- **It is the last one gated.** If removing a gate would empty the set, stop: the external ledger asserts that the set of files using this idiom is exactly this file, in both directions, and an emptied gate would red it. Either keep one gated case with its measured reason, or remove the idiom and the ledger entry together in one commit. Decide, and say which in the commit message.

Then correct the gate's comment. Replace the hard-coded figure with the one you just measured, dated. Remove the sentence deferring the situation to a wholesale re-baseline, because that re-baseline is this phase — say instead what the current state is. Keep the explanation of why these cases cannot be converted to inject the signal into a child process, since that reason is structural and still holds; confirm it against the module first.

Write no number you did not measure in this run.
  </action>
  <acceptance_criteria>
    - Both runs' summary counts are recorded, with the per-case result of every gated case from the second run.
    - Every gated case is either ungated because it was measured passing, kept with a measured reason, or retired with a named successor.
    - The gate's comment carries only figures measured in this phase, dated, and no longer defers to a re-baseline.
    - The external ledger asserting this file is the sole user of the gate idiom still passes, or was changed in the same commit with its reason.
    - `ci-guardrails.test.mjs` reports 0 failures.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test vice-proxy.test.ts > /tmp/55-06-t1-default.txt 2>&1; echo "DEFAULT_EXIT=$?"; grep -E '^# (tests|pass|fail|skipped)' /tmp/55-06-t1-default.txt</automated>
    <fails_when>`# fail` is anything other than 0 — Plan 55-05 was meant to leave the file green in its default environment, so a failure here means an earlier plan's end state regressed or was never reached.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && CONTAINER_WORKSPACE_PATH=/workspace HOST_WORKSPACE_PATH=/home/henrik/dev/henrik/git/c64-re-tools node --test vice-proxy.test.ts > /tmp/55-06-t1-gated.txt 2>&1; echo "GATED_EXIT=$?"; grep -E '^# (tests|pass|fail|skipped)' /tmp/55-06-t1-gated.txt</automated>
    <fails_when>`# fail` is greater than the number of cases this task's record keeps gated with a measured failing reason. If every gated case was ungated, `# fail` must be 0 and `# skipped` must be 0.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test ci-guardrails.test.mjs test-gate.test.ts > /tmp/55-06-t1-guards.txt 2>&1; echo "EXIT=$?"; grep -E '^# (tests|pass|fail)' /tmp/55-06-t1-guards.txt</automated>
    <fails_when>`# fail` is anything other than 0 — the gate-idiom ledger and the disposition-list guard both read this file and must stay green through the change.</fails_when>
  </verify>
  <done>Every gated case is decided on a fresh measurement; the gate comment carries only this phase's dated figures; the external ledger is green or changed with its reason in the same commit.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the three stale ledger claims about this file</name>
  <files>src/mcp/vice/vice-proxy.test.ts, src/mcp/vice/vice-proxy-ping.test.ts, src/mcp/vice/test-gate.mjs</files>
  <read_first>
    - `src/mcp/vice/vice-proxy-ping.test.ts` — its opening comment, which describes itself as a focused, non-hanging sibling to a file that HANGS and must never be run, and points at that file's own header and the disposition list as corroboration. Read the rest of the comment too: everything after that first claim is still true and must survive the correction.
    - `src/mcp/vice/vice-proxy.test.ts` — the file header, including the harness description and the several coverage notes below it. Work out which notes still describe live structure and which describe deleted mechanisms.
    - `src/mcp/vice/test-gate.mjs` — the whole leading comment block, including the sentence stating why the file exists, the per-entry rationale paragraphs in the order they were added, the standing rule about mirror assertions for manual-only suites, and the frozen list itself. Note that the block's opening sentence gives a count of files that depend on manual host setup, and that this file is not one of them for that reason.
    - `src/mcp/vice/test-gate.test.ts` — the equality assertion naming twelve dispositioned files, and the two coverage assertions around it.
    - `src/mcp/vice/ci-guardrails.test.mjs` — the full-glob assertion and the non-vacuity control beside it, both of which assert this file's membership in the manual-only list and its absence from the narrowed set.
    - `.planning/phases/55-restore-the-fork-removal-s-dropped-capabilities-and-re-basel/55-05-SUMMARY.md` and this plan's Task 1 record — the measured numbers every corrected claim must use.
  </read_first>
  <action>
Replace three false claims with measured ones, keeping every surviving claim around them intact.

**The sibling's opening claim.** Rewrite the first sentence so it stops asserting the other file hangs. State what is true: the sibling terminates, it costs roughly the measured wall-clock time per run, and it is dispositioned manual-only for that cost rather than for any host dependency. Keep the rest of the comment verbatim — the explanation of why this focused file exists, and the qualification it pins, are both still correct and are not this task's subject.

**The harness header.** Rewrite the description of what this file drives. It spawns a real proxy child and speaks JSON-RPC to it over stdio; what it no longer does is dial an in-process stand-in for a host server, because the forwarding path that reached one was deleted. Say what the successful-call route is now, in one sentence, so the next reader does not have to derive it. Then go through the coverage notes below the header and delete any that describe a mechanism this phase removed, keeping any that describe live structure — the cleanup-signal note about how child processes are killed in each case is the clearest example of one that survives.

**The disposition rationale.** In the disposition list's comment block, the reason recorded for this file is that it hangs outside a container. Replace it with the measured reason: the file terminates, takes roughly the measured time per run and spawns a child process per case, which is why it stays outside the fast narrowed gate while still reaching CI through the bare glob. State explicitly that it is NOT excluded for a host dependency, since every neighbouring entry is, and a reader scanning the block will otherwise assume this one is too. Then check the block's opening sentence, which counts how many files depend on manual host setup — if that count included this file, correct it and say what the corrected number counts.

Do NOT remove this file from the frozen list. Three guards read that list and two assert this file's membership; the phase's protected baseline is that the narrowed gate does not gain this file. Record in the commit message that promotion was considered and deliberately not done, with the reason — a two-minute-plus, child-process-per-case suite belongs outside a gate meant to run on every edit — so the next reader finds a decision rather than an omission.

All three files ship or are read by consumers of the shipped package: no planning-tree path, no phase or plan number, no bare decision or gap identifier in anything you write.
  </action>
  <acceptance_criteria>
    - No file in the tree claims `vice-proxy.test.ts` hangs or must never be run.
    - The harness header describes the spawned-child stdio surface and names the current successful-call route; coverage notes describing deleted mechanisms are gone and those describing live structure remain.
    - The disposition rationale states the measured time cost and the child-process-per-case shape, and says explicitly that the exclusion is not for a host dependency.
    - `vice-proxy.test.ts` is still a member of the frozen manual-only list, and the commit message records that promotion was considered and declined with its reason.
    - `test-gate.test.ts` and `ci-guardrails.test.mjs` both report 0 failures.
    - None of the three files gained planning vocabulary.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && printf 'hang_claims='; grep -raiE 'HANGS and must never be run|which HANGS' src/mcp/vice/ --include='*.ts' --include='*.mts' --include='*.mjs' | wc -l</automated>
    <fails_when>The count is greater than 0 — INVERTED polarity: any surviving claim that the file hangs is the failure this task exists to remove.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && printf 'still_manual_only='; grep -ac '"vice-proxy.test.ts"' test-gate.mjs; printf 'planningrefs='; grep -aEc '\.planning/|/gsd-[a-z]|ROADMAP\.md|REQUIREMENTS\.md' test-gate.mjs vice-proxy-ping.test.ts | paste -sd' '</automated>
    <fails_when>`still_manual_only=` is 0 — the file was removed from the frozen list, which the phase's protected baseline forbids. Any per-file `planningrefs=` count other than 0 — INVERTED polarity.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test test-gate.test.ts ci-guardrails.test.mjs vice-proxy-ping.test.ts > /tmp/55-06-t2.txt 2>&1; echo "EXIT=$?"; grep -E '^# (tests|pass|fail)' /tmp/55-06-t2.txt</automated>
    <fails_when>`# fail` is anything other than 0.</fails_when>
  </verify>
  <done>All three stale claims carry measured replacements; every surviving claim around them is intact; the frozen list is unchanged and the declined promotion is on the record; the guards reading all of it are green.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the full glob CI runs is green, and protect the narrowed gate's baseline</name>
  <files>src/mcp/vice/vice-proxy.test.ts</files>
  <read_first>
    - `.github/workflows/ci.yml` — the Test step, so the command you prove against is the command CI runs, character for character, rather than one you assume.
    - `src/mcp/vice/package.json` — the `test` and `test:automated` scripts.
    - `src/mcp/vice/ci-guardrails.test.mjs` — the assertion that CI's Test step still runs the bare glob and carries no continue-on-error, and the non-vacuity control beside it.
    - `.planning/phases/55-restore-the-fork-removal-s-dropped-capabilities-and-re-basel/55-05-SUMMARY.md` — the four end-state numbers, for comparison.
  </read_first>
  <action>
Run what CI runs, and report the result as a set rather than a count.

Stop the broker first. A live broker instance deterministically reddens at least one case in this tree; a green claim measured beside one is worthless, and so is a red one.

Run the exact full-glob command from the package's own `test` script, from the package directory, with output redirected to a scratch file outside the repository tree and the exit code read on the SAME line. Do not pipe it into a pager. Give it a generous timeout — the glob includes twelve manual-only files, most of which are default-skip but one of which is this two-minute suite — and if it does not terminate, record where it stopped and treat that as the finding rather than retrying blind.

From the output, extract the SET of failing test names with their owning file, not just the summary count. Compare that set against `vice-proxy.test.ts`'s own known state. Three outcomes:

- **Empty set.** The phase goal is met. Record the summary counts, the wall-clock duration and the command, verbatim.
- **Non-empty, and every member belongs to `vice-proxy.test.ts`.** An earlier plan's end state regressed or was never reached. Name the failures and stop; this is not something to patch here.
- **Non-empty, and members belong to other files.** Those are OUT of this plan's scope. Name every one by file and test name, state what it asserts, and report it as a finding for the phase's verification to dispose of. Do not fix them opportunistically — a file this plan never planned to touch is a separate scope with its own successor questions.

Then protect the narrowed gate. Run it the same way and confirm 0 failures and 9 skips, with a test count whose whole difference from 4407 is accounted for by the cases Plan 55-02 added to the recycle suite. Confirm `vice-proxy.test.ts` is still absent from that set. Record both numbers.

Finally, run the build check the project's own configuration names, so the phase does not ship a green test run over a broken typecheck.
  </action>
  <acceptance_criteria>
    - No broker or emulator process was running during any measurement.
    - The full-glob run's command, exit code, summary counts and wall-clock duration are recorded verbatim, taken from a redirected run with the exit code read on the same line.
    - The failing set is reported as names-with-files, never as a bare count.
    - Any failure outside `vice-proxy.test.ts` is named and reported as a finding, not fixed.
    - `npm run test:automated` reports 0 failures and 9 skips, with the count delta fully attributed.
    - `vice-proxy.test.ts` is absent from the narrowed automated set.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pgrep -af 'vice-broker|x64sc' > /tmp/55-06-t3-procs.txt 2>&1; echo "BROKER_LINES=$(wc -l < /tmp/55-06-t3-procs.txt)"</automated>
    <fails_when>`BROKER_LINES` is greater than 0 — stop the broker before measuring anything below; a live one changes at least one result deterministically.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm test > /tmp/55-06-t3-fullglob.txt 2>&1; echo "FULLGLOB_EXIT=$?"; grep -E '^# (tests|suites|pass|fail|cancelled|skipped|todo|duration_ms)' /tmp/55-06-t3-fullglob.txt; echo '--- failing set ---'; grep -E '^not ok |^✖ ' /tmp/55-06-t3-fullglob.txt | sort -u</automated>
    <fails_when>`FULLGLOB_EXIT=` is non-zero, or the failing set is non-empty. If the failing set contains only names from files other than `vice-proxy.test.ts`, that is still a failure of the phase goal, but it is reported as a finding rather than patched inside this plan. No `# tests` line at all means the run did not terminate — record where it stopped.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated > /tmp/55-06-t3-gate.txt 2>&1; echo "GATE_EXIT=$?"; grep -E '^# (tests|pass|fail|skipped)' /tmp/55-06-t3-gate.txt; printf 'file_in_narrowed_set='; node -e 'import("./test-gate.mjs").then(m=>console.log(m.automatedTestFiles(process.cwd()).includes("vice-proxy.test.ts")))'</automated>
    <fails_when>`GATE_EXIT=` is non-zero, `# fail` is anything other than 0, `# skipped` is anything other than 9, or `file_in_narrowed_set=` prints `true` — the phase's protected baseline is that this file stays outside the narrowed gate.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck > /tmp/55-06-t3-tc.txt 2>&1; echo "EXIT=$?"; tail -5 /tmp/55-06-t3-tc.txt</automated>
    <fails_when>`EXIT=` is non-zero.</fails_when>
    <human-check>The developer has seen the full-glob run's verbatim summary and failing set, and has confirmed that the recorded reason `vice-proxy.test.ts` stays manual-only — a two-minute-plus suite spawning a child process per case, not a host dependency — is the disposition they want, rather than promoting it into the narrowed gate.</human-check>
  </verify>
  <done>The full glob CI runs is green from a clean, broker-free, redirected run whose command and numbers are recorded verbatim; any out-of-scope failure is named as a finding; the narrowed gate is 0 failures and 9 skips with this file still outside it; typecheck is clean.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger comment → future maintainer | A false claim in a comment is acted on as fact; the hang claim has already kept a whole suite unexamined for multiple phases |
| disposition list → CI | Membership in the manual-only list decides which suite the narrowed gate runs; three guards read it and two assert this file's membership |
| local measurement → phase verdict | The phase's terminal claim rests on one command's exit code, which a pipe into a pager has already falsified once in this repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-26 | Repudiation | a false claim that the file hangs | high | mitigate | Removed in Task 2 and asserted absent tree-wide by a verify step with inverted polarity, so any surviving copy fails. |
| T-55-27 | Repudiation | a remembered figure written as a measured one | high | mitigate | Every number in a corrected comment comes from a run performed in this plan, dated. The prohibition is explicit and the acceptance criteria require both runs' outputs to be recorded. |
| T-55-28 | Spoofing | a green claim produced by a pipe's exit code | critical | mitigate | Every test command redirects and reads the exit code on the same line; piping into a pager is prohibited by name, having already faked a green baseline here once. |
| T-55-29 | Spoofing | a green claim measured beside a live broker | high | mitigate | A process check runs before every measurement and fails the step if a broker or emulator is running. |
| T-55-30 | Tampering | the narrowed gate silently gaining this file | medium | mitigate | A verify step asserts the file is still outside the derived automated set and still inside the frozen list; the prohibition forbids promotion and requires the declined decision to be recorded. |
| T-55-31 | Denial of Service | an unbounded full-glob run | medium | mitigate | The run is given a generous timeout and non-termination is recorded as the finding rather than retried blind; the glob's one long suite is named so the duration is expected rather than surprising. |
| T-55-SC | Tampering | package-manager installs | n/a | accept | This plan runs no `npm install` / `pip install` / `cargo add` task and adds no dependency. No package-legitimacy checkpoint is owed. |
</threat_model>

<verification>
- No file in the tree claims `vice-proxy.test.ts` hangs or must never be run.
- Every figure in a corrected comment was measured in this plan and is dated.
- `test-gate.test.ts`, `ci-guardrails.test.mjs` and `vice-proxy-ping.test.ts` all report 0 failures.
- `npm test` from `src/mcp/vice` exits 0 with an empty failing set, measured with no broker running, redirected, with the exit code read on the same line.
- `npm run test:automated` reports 0 failures and 9 skips, with `vice-proxy.test.ts` outside the derived set.
- `npm run typecheck` exits 0.
</verification>

<success_criteria>
- The ledger describes the file as it is: terminating, costly, manual-only for cost rather than for host dependence.
- Every remaining environment gate is justified by a measurement taken in this phase.
- The full `*.test.*` glob CI executes is green, proven by a recorded command and exit code.
- The narrowed automated gate is untouched in shape and green in result.
- Nothing outside this plan's files was fixed opportunistically; anything found there is named as a finding.
</success_criteria>

<output>
Create `.planning/phases/55-restore-the-fork-removal-s-dropped-capabilities-and-re-basel/55-06-SUMMARY.md` when done. Record: both gated-case runs with per-case results, the three corrected claims with their before and after text, the declined-promotion decision and its reason, the full-glob command with its verbatim summary counts, duration and exit code, the failing set (empty or named-with-files), and the narrowed gate's numbers with the count delta attributed.
</output>

