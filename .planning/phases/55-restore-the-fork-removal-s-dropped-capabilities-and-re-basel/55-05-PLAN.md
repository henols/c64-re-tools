---
phase: 55-restore-the-fork-removal-s-dropped-capabilities-and-re-basel
plan: 05
type: execute
wave: 4
depends_on: ["55-04"]
files_modified:
  - src/mcp/vice/vice-proxy.test.ts
autonomous: true
requirements: [PROXY-05]

estimate:
  tokens: 135000
  raw_tokens: 135000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of the seven tests that needs a SUCCESSFUL tool call is resolved by a recorded ladder verdict — rewritten against a live route, ported to a named successor suite, or retired with its successor named (PROXY-05)"
    - "A proxy that answers a real tool call end to end is proven in this file again, so the file's leading tracer is a tracer and not a placeholder (PROXY-05)"
    - "Any test retired here names a currently-green successor; none is retired on the assumption that its property is covered somewhere (PROXY-05)"
    - "Anything Plan 55-04 handed forward is resolved here, not carried further (PROXY-05)"
    - "No test is disabled with `skip`; the file's `skip` count does not rise (PROXY-03)"
  artifacts:
    - src/mcp/vice/vice-proxy.test.ts
  key_links:
    - "the annotation family's proxy-local route to the tracer — a real registered tool that answers over stdio with no emulator, no broker and no stand-in server, MEASURED live at planning time"
    - "the surviving broker-side siblings (`broker three states`, `broker never-cache`, `control-plane unreachable`) to their host-side counterparts — the broker-level property may already be the whole surviving property"
    - "`stock-dispatch.test.ts` and `vice-proxy-ping.test.ts` to any test whose property is really about dispatch rather than about the stdio surface — both drive dispatch in-process and are the natural port target"
  prohibitions:
    - statement: "A binary-monitor stand-in is built ONLY if the ladder demands it for a specific named test. It is not built speculatively, and the frame encoders in `binmon-fixtures.ts` are not mistaken for one — they encode frames, they do not serve a connection."
      status: required
      verification: flagged-unverified
    - statement: "No test is retired on the belief that its property is covered elsewhere. The successor is located by NAME and shown green before the predecessor is cut."
      status: required
      verification: flagged-unverified
    - statement: "No test is disabled with `skip`, `todo` or a commented-out body."
      status: required
      verification: flagged-unverified
    - statement: "The deleted HTTP stand-in is not resurrected, and no test-only environment hatch is added to shipped code to make a test easier to write."
      status: required
      verification: flagged-unverified
    - statement: "A stand-in built here spawns no emulator and opens no connection to anything outside the test process. This file stays in the manual-only set for cost, not for host dependence, and must not acquire one."
      status: required
      verification: flagged-unverified
---

<objective>
Resolve the seven tests that cannot pass without a tool call SUCCEEDING, plus anything Plan 55-04 handed forward.

These are the residue: `tracer: one real tool call round-trips end to end`, `never-throw: malformed and hostile input is answered, not fatal`, `never-cache: host down then up succeeds without a restart`, `three states: each unreachable shape gets its own message and fix`, `path translation: a lexical .. cannot escape the workspace, and one that resolves back inside still translates`, `with an explicit endpoint override set, the control listener receives no connection at all`, and `fixed-port unreachable is unchanged: no lease held still produces the 01.1 never-started message naming the surviving launcher`.

The cluster was scoped as having no available harness. Two facts narrow that:

MEASURED LIVE 2026-09-13, planning time: a spawned `vice-proxy.ts` child, given `CLAUDE_PROJECT_DIR` pointed at a temporary workspace holding a seeded annotation store, answered `tools/call` for `anno_get_symbols` with `isError: false` and a 23,290-character payload. No emulator, no broker, no stand-in server. A real registered tool DOES round-trip end to end through the stdio surface today.

MEASURED at `6e4e9a0b`, in this same file: `broker three states: each broker-absent shape gets its own message and fix`, `broker never-cache: absent-then-alive-and-granted succeeds on the SAME process, no restart` and `control-plane unreachable: a fresh heartbeat but a dead connect names the address and port, never the heartbeat-age wording` all PASS. For at least two of the seven, a broker-level sibling asserting the same shape is already green — which makes "is the surviving property already covered?" a real question rather than a formality.

Purpose: finish the file without either faking coverage or building infrastructure nobody asked for.
Output: seven recorded verdicts, a working tracer, and a stand-in only if a named test genuinely required one.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-restore-the-fork-removal-s-dropped-capabilities-and-re-basel/55-04-SUMMARY.md
</context>

<tasks>
<task type="tracer">
  <name>Task 1: Make the file's leading tracer a real end-to-end round trip again</name>
  <files>src/mcp/vice/vice-proxy.test.ts</files>
  <read_first>
    - `src/mcp/vice/vice-proxy.test.ts` — the test named `tracer: one real tool call round-trips end to end`, whole, including the comment block above it. It is the first test in the file and it declares what the file is for.
    - `src/mcp/vice/vice-proxy.test.ts` — the file header, which describes the harness as a spawned child driven against an in-process stand-in for a host server. Plan 55-06 owns correcting that header; read it here so the tracer's own comment does not contradict what 55-06 will write.
    - `src/mcp/vice/vice-proxy.test.ts` — whatever helper Plan 55-01 added for seeding a temporary workspace and store for a spawned child. Reuse it; do not add a second.
    - `src/mcp/vice/vice-proxy.ts` — the tools/call override and the four registration families it serves, so the tracer's comment can say accurately which layers the round trip actually crosses.
    - `src/mcp/vice/stock-broker-live.test.ts` — the manual-only suite that drives a real broker and a real emulator end to end. This is the named home of the broker-mediated round trip, which the tracer here deliberately does NOT reproduce.
  </read_first>
  <action>
Rewrite the file's first test so it again proves, in one case, that a real tool call crosses every layer this file is responsible for.

The layers are: stdio JSON-RPC framing in, the tools/call override, the registry the file builds at construction time, a registered tool's own runner, the result-shape check, the size-cap wrap Plan 55-01 restored, and stdio JSON-RPC framing out. A call to a proxy-local annotation verb against a seeded temporary store crosses all of them with no emulator, no broker and no stand-in server — MEASURED live at planning time, and reproduced by Plan 55-01's own proof.

Drive exactly one such call: `initialize`, then one `tools/call`, then assert a well-formed JSON-RPC response carrying a success result whose payload is the seeded content. Keep it ONE path — this is the tracer, not the coverage.

Then write the honesty into the test's own comment, in the file's voice: this proves the stdio surface and the registry, and it deliberately does not prove the broker-mediated route, whose end-to-end proof lives in the manual-only live suite that spawns a real broker and a real emulator. Name that suite. A tracer that quietly narrows its claim is worse than one that states its boundary.

Adjust the test's NAME only enough to stay true. If it keeps claiming a scope it no longer covers, it becomes the same kind of standing lie this phase exists to remove.

Reuse Plan 55-01's seeding helper. If that helper is shaped for a cap-driven payload size rather than for a small deterministic one, widen it rather than adding a second helper beside it.
  </action>
  <acceptance_criteria>
    - The tracer test passes.
    - It drives exactly one `tools/call` and asserts a success result with the seeded payload.
    - Its comment names what it proves and what it does not, and names the live suite that covers the broker-mediated route.
    - No second seeding helper was added beside Plan 55-01's.
    - The file's `skip` count did not rise.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test --test-name-pattern 'tracer' vice-proxy.test.ts > /tmp/55-05-t1.txt 2>&1; echo "EXIT=$?"; grep -E '^# (tests|pass|fail|skipped)' /tmp/55-05-t1.txt</automated>
    <fails_when>`# fail` is anything other than 0, or `# pass` is 0 — a name-pattern that matches nothing reports zero tests and exits clean, which is not a pass.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && printf 'live_suite_named='; grep -ac 'stock-broker-live.test.ts' vice-proxy.test.ts; printf 'skips='; grep -ac 'test.skip\|{ skip:\|, { skip' vice-proxy.test.ts</automated>
    <fails_when>`live_suite_named=` is 0 — the tracer's boundary statement does not name where the route it excludes is actually proven. `skips=` rose above the Plan 55-04 value.</fails_when>
  </verify>
  <done>The file's first test is a real one-path round trip again, passing, with its own boundary stated and the excluded route's proof named.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the ladder to the remaining six singles and anything handed forward</name>
  <files>src/mcp/vice/vice-proxy.test.ts</files>
  <read_first>
    - `src/mcp/vice/vice-proxy.test.ts` — each of the six, whole, by name: `never-throw: malformed and hostile input is answered, not fatal`, `never-cache: host down then up succeeds without a restart`, `three states: each unreachable shape gets its own message and fix`, `path translation: a lexical .. cannot escape the workspace, and one that resolves back inside still translates`, `with an explicit endpoint override set, the control listener receives no connection at all`, and `fixed-port unreachable is unchanged: no lease held still produces the 01.1 never-started message naming the surviving launcher`. For each, identify precisely which assertion requires a successful call and which do not — several may be mostly rewritable with one dependent assertion.
    - `src/mcp/vice/vice-proxy.test.ts` — the three PASSING broker-side siblings: `broker three states: each broker-absent shape gets its own message and fix`, `broker never-cache: absent-then-alive-and-granted succeeds on the SAME process, no restart`, and `control-plane unreachable: a fresh heartbeat but a dead connect names the address and port, never the heartbeat-age wording`. Read all three. For the never-cache and three-states pair, work out whether the host-side variant still names a distinct property or whether the broker-side one is now the whole surviving property.
    - `src/mcp/vice/vice-proxy.test.ts` — `never-throw: a notification draws no response` and `never-throw: a broken stdout pipe does not kill the process`, both PASSING. They are the never-throw family's surviving members and show what the hostile-input case can assert without a successful call.
    - `src/mcp/vice/vice-proxy.ts` — the tools/call override's unknown-name branch, its shape check and its catch arm; the endpoint-override handling; and the message the file produces when no lease is held. These are the live subjects the rewrites aim at.
    - `src/mcp/vice/stock-dispatch.test.ts` and `src/mcp/vice/vice-proxy-ping.test.ts` — both drive dispatch IN-PROCESS rather than through a spawned proxy. If a test's property turns out to be about dispatch rather than about the stdio surface, these are the port targets.
    - `src/mcp/vice/anno-confinement.test.ts` — the suite that covers workspace confinement for path arguments, the candidate successor for the path-translation case.
    - `src/mcp/vice/binmon-fixtures.ts` — read enough of it to confirm for yourself what it is. It encodes and decodes protocol frames; it does not serve a connection. Do not plan around it as though it were a server.
    - `.planning/phases/55-restore-the-fork-removal-s-dropped-capabilities-and-re-basel/55-04-SUMMARY.md` — the list of anything Plan 55-04 handed forward.
  </read_first>
  <action>
Take each of the six, plus anything handed forward, through one ladder, and record the rung and the verdict for every one.

The ladder:

1. **Can the assertion be made without a successful call?** The never-throw family's two surviving members show the shape: a malformed request draws a well-formed error, a notification draws nothing, a broken pipe does not kill the process. None needs a success. If the case's real property is that the proxy ANSWERS rather than dies, rewrite it to assert that against malformed and hostile input plus one well-formed call that succeeds through the annotation route.
2. **Can it be made with the proven proxy-local route?** A real registered tool answering end to end is available now. The endpoint-override case, the fixed-port case and the path-translation case may all be expressible with a call that reaches a real runner.
3. **Is the property now the broker-side sibling's property?** For the never-cache and three-states pair, the host-side and broker-side variants may have collapsed into one. If they have, retire the host-side one naming the broker-side one as its successor, and say in the commit message what distinction was lost and why it no longer exists.
4. **Does it belong in a different suite?** A property about dispatch belongs where dispatch is driven in-process; a property about workspace confinement belongs where confinement is already covered. Port it — move the assertion, do not duplicate it — and name the destination in the commit message.
5. **Does it need a binary-monitor stand-in behind the spawned child?** Only reach this rung if rungs 1-4 all fail for a specific named test. If you do reach it, build the smallest thing that serves the one case: a listener inside the test process speaking the framed protocol, scripted per case, spawning nothing and dialing nothing outside the test process. Frame encoders already exist and can be used to build its replies; they are not themselves a server. Do not generalise it beyond the cases that forced it.
6. **Retire, with a named green successor.** Only if no rung above applies and the property is genuinely covered elsewhere by name.

If a test reaches the bottom with a real, uncovered property and no reachable harness, LEAVE IT and report it. An uncovered property on the record is a finding; a deleted test is a lost one.

Do not build the stand-in unless rung 5 is actually reached for a named test. Record, per test, the rung that resolved it — the SUMMARY's verdict table is how the phase proves nothing was resolved by convenience.
  </action>
  <reversibility rating="costly">If rung 5 is reached, a protocol stand-in becomes a maintained fixture in this file. Building it speculatively is the cost this ladder exists to avoid; building it when a named test demands it is the right call.</reversibility>
  <acceptance_criteria>
    - A verdict table covering all six plus anything handed forward — test name, rung reached, action taken, successor or destination named — is in the SUMMARY and the commit messages.
    - Every test in the table is passing, ported, retired with a named green successor, or explicitly reported as an uncovered property left in place.
    - A protocol stand-in exists only if the table shows at least one test resolved at rung 5, and if it exists it spawns no process and opens no connection outside the test process.
    - Any test ported to another suite was MOVED, not copied — the assertion exists in exactly one place afterwards.
    - The file's `skip` count did not rise.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test vice-proxy.test.ts > /tmp/55-05-t2.txt 2>&1; echo "EXIT=$?"; grep -E '^# (tests|pass|fail|cancelled|skipped)' /tmp/55-05-t2.txt</automated>
    <fails_when>`# cancelled` is greater than 0 — something stopped terminating. `# skipped` rose above the Plan 55-04 value. `# fail` is greater than the number of tests the verdict table records as deliberately-left uncovered properties; if the table records none, `# fail` must be 0.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test stock-dispatch.test.ts vice-proxy-ping.test.ts anno-confinement.test.ts > /tmp/55-05-t2b.txt 2>&1; echo "EXIT=$?"; grep -E '^# (tests|pass|fail)' /tmp/55-05-t2b.txt</automated>
    <fails_when>`# fail` is anything other than 0 — a port target must be green after receiving a moved assertion.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && printf 'spawns_in_testfile='; grep -ac 'spawnSync(\|spawn(' vice-proxy.test.ts</automated>
    <fails_when>This count rose beyond the pre-task value by more than the spawned-proxy calls the verdict table accounts for. A stand-in that spawns anything is not the bounded in-process listener rung 5 authorises; record the pre-task value before changing the file.</fails_when>
  </verify>
  <done>Every single has a recorded rung and verdict; the file's remaining failures are exactly the uncovered properties the table names, or zero; nothing was skipped and no stand-in was built without a named test demanding it.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the file against its own accounting and the automated gate</name>
  <files>src/mcp/vice/vice-proxy.test.ts</files>
  <read_first>
    - `.planning/phases/55-restore-the-fork-removal-s-dropped-capabilities-and-re-basel/55-03-SUMMARY.md` — Task 0's census and the four deletion lists.
    - `.planning/phases/55-restore-the-fork-removal-s-dropped-capabilities-and-re-basel/55-04-SUMMARY.md` — the rewrite verdicts and anything handed forward.
    - `src/mcp/vice/vice-proxy.test.ts` — every helper, constant and import the file still declares, so the orphan sweep is over the current state rather than a remembered one.
  </read_first>
  <action>
Close the books on the file's content before Plan 55-06 touches its ledger.

Reconcile the numbers. Take Plan 55-03's Task 0 census as the origin. Every test that is no longer present must appear in exactly one of: a deletion list from 55-03, a retirement verdict from 55-04, a retirement or port verdict from this plan's Task 2. Every test that is present and failing must appear in this plan's verdict table as a deliberately-left uncovered property. Anything that fits neither is unaccounted; find out why before continuing.

Sweep the orphans again. Deletions across four plans leave helpers, constants and imports with no consumers. Grep each candidate, delete only those with zero, and leave anything with even one consumer. Then confirm every remaining import resolves to a symbol the file actually uses.

Re-run the file clean and record the summary counts, the top-level test count, the `skip` count and the line count. These four numbers are what Plan 55-06's ledger repair writes down, so they must be measured here rather than estimated there.

Stop the broker before measuring, and never pipe the run into a pager — redirect and read the exit code on the same line.

Leave the file's header comment, the environment gate's comment and the manual-only disposition alone. Those are Plan 55-06's, deliberately: the content has to settle before the ledger describing it can be written truthfully.
  </action>
  <acceptance_criteria>
    - A reconciliation table in the SUMMARY accounts for every test present at 55-03's census and absent now, and for every test present and failing now.
    - Zero unaccounted differences.
    - Every helper and import removed had zero consumers, shown by grep; nothing with a consumer was removed.
    - The four end-state numbers — summary counts, top-level test count, `skip` count, line count — are recorded from a clean run with no broker present.
    - The file header, the environment gate comment and the manual-only disposition are byte-unchanged by this plan.
  </acceptance_criteria>
  <verify>
    <automated>pgrep -af 'vice-broker|x64sc' > /tmp/55-05-t3-procs.txt 2>&1; echo "BROKER_LINES=$(wc -l < /tmp/55-05-t3-procs.txt)"; cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test vice-proxy.test.ts > /tmp/55-05-t3.txt 2>&1; echo "EXIT=$?"; grep -E '^# (tests|pass|fail|cancelled|skipped|duration)' /tmp/55-05-t3.txt; printf 'toplevel='; grep -ac '^test(' vice-proxy.test.ts; printf 'skips='; grep -ac 'test.skip\|{ skip:\|, { skip' vice-proxy.test.ts; printf 'lines='; wc -l < vice-proxy.test.ts</automated>
    <fails_when>`BROKER_LINES` is greater than 0 — the measurement is not clean. `# cancelled` is greater than 0. `# skipped` differs from Plan 55-03's Task 0 value by more than the gated cases this phase's plans explicitly removed. `# fail` is greater than the count of uncovered properties named in Task 2's verdict table.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && git diff 55-03-start -- src/mcp/vice/vice-proxy.test.ts 2>/dev/null | head -0; printf 'header_unchanged='; git diff HEAD~1 -- src/mcp/vice/vice-proxy.test.ts | grep -cE '^[-+].*(HANGS|122 tests|MANUAL_ONLY|in-process node:http stand-in)'</automated>
    <fails_when>`header_unchanged=` is greater than 0 — this plan touched ledger prose that belongs to Plan 55-06, which would split one correction across two plans and leave the intermediate state describing the file wrongly.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated > /tmp/55-05-t3-gate.txt 2>&1; echo "EXIT=$?"; grep -E '^# (tests|pass|fail|skipped)' /tmp/55-05-t3-gate.txt</automated>
    <fails_when>`# fail` is anything other than 0, or `# skipped` is anything other than 9.</fails_when>
  </verify>
  <done>Every difference from the origin census is accounted for, orphans are swept with grep evidence, the four end-state numbers are recorded from a clean run, and the ledger prose is untouched and ready for Plan 55-06.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → proxy stdio | Malformed and hostile input crosses here; the never-throw family is the check that it is answered rather than fatal |
| test process → anything outside it | A stand-in built at rung 5 must not cross this boundary; a test file that spawns an emulator or dials a host acquires a manual dependency this file does not have |
| workspace root → path arguments | A path argument that resolves outside the workspace must be refused; the path-translation case is that check's witness |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-21 | Denial of Service | the stdio surface under hostile input | high | mitigate | The never-throw case is resolved at rung 1 or 2, keeping a live assertion that malformed and hostile input draws a well-formed answer rather than killing the process. The two surviving never-throw members stay green throughout. |
| T-55-22 | Elevation of Privilege | a path argument escaping the workspace | high | mitigate | The path-translation case is rewritten against a live route or PORTED to the confinement suite — never retired on the assumption that confinement is covered. The ladder requires the successor be named and shown green. |
| T-55-23 | Repudiation | a property lost to a convenient verdict | high | mitigate | Every one of the seven carries a recorded rung and verdict; Task 3 reconciles the whole file against the origin census with zero unaccounted differences permitted. |
| T-55-24 | Denial of Service | a stand-in acquiring a host dependency | medium | mitigate | Rung 5 authorises only an in-process listener that spawns nothing and dials nothing outside the test process; a verify step counts spawn calls against a recorded pre-task value. |
| T-55-25 | Tampering | an assertion duplicated rather than moved on a port | low | mitigate | The ladder requires a port to MOVE the assertion; the acceptance criteria require it to exist in exactly one place afterwards. |
| T-55-SC | Tampering | package-manager installs | n/a | accept | This plan runs no `npm install` / `pip install` / `cargo add` task and adds no dependency. No package-legitimacy checkpoint is owed. |
</threat_model>

<verification>
- The file's leading tracer passes and states its own boundary, naming the live suite that proves the route it excludes.
- All seven singles plus anything handed forward carry a recorded rung and verdict.
- `stock-dispatch.test.ts`, `vice-proxy-ping.test.ts` and `anno-confinement.test.ts` report 0 failures after any port.
- The reconciliation table has zero unaccounted differences from Plan 55-03's origin census.
- `npm run test:automated` reports 0 failures and 9 skips. Run it with output redirected and the exit code read on the same line; never pipe it into `tail` or `head`.
</verification>

<success_criteria>
- A real tool call round-trips end to end through this file again.
- Nothing was retired without a named, green successor, and nothing was resolved by convenience.
- A protocol stand-in exists only if a named test forced one, and it carries no host dependency.
- The file's content is fully reconciled against its origin census, ready for its ledger to be written truthfully.
</success_criteria>

<output>
Create `.planning/phases/55-restore-the-fork-removal-s-dropped-capabilities-and-re-basel/55-05-SUMMARY.md` when done. Record: the verdict table for all seven plus anything handed forward, whether rung 5 was reached and for which test, the reconciliation table, and the four end-state numbers from the clean run.
</output>

