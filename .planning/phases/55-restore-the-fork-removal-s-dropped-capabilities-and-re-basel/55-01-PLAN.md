---
phase: 55-restore-the-fork-removal-s-dropped-capabilities-and-re-basel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/vice/vice-proxy.ts
  - src/mcp/vice/anno-tools.ts
  - src/mcp/vice/vice-proxy.test.ts
autonomous: true
requirements: [PROXY-01]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A successful `tools/call` result longer than `OUTPUT_CHAR_CAP` comes back over the wire as TWO content items — the first a pure payload chunk, the second a marker naming the exact `vice_result_continue` call to make (PROXY-01)"
    - "`vice_result_continue` returns a real chunk for a token the proxy just issued, instead of only ever returning its unknown-or-expired refusal (PROXY-01)"
    - "Concatenating the payload chunks of a chunked sequence reproduces the original serialised text byte-for-byte, with no truncation branch anywhere on the path (PROXY-01)"
    - "The number `_meta[\"anthropic/maxResultSizeChars\"]` advertises and the number actually used as the chunk boundary are one read of one constant (PROXY-01)"
    - "A result at or under the cap is unchanged: exactly one content item, nothing appended (PROXY-01)"
    - "An `isError: true` result is never chunked, and `vice_result_continue`'s own two-item result is never re-chunked (PROXY-01)"
  artifacts:
    - src/mcp/vice/vice-proxy.ts
    - src/mcp/vice/vice-proxy.test.ts
  key_links:
    - "`server.getServer().setRequestHandler(CallToolRequestSchema, ...)`'s success branch in `vice-proxy.ts` — the ONE choke point every registered tool's result passes through, and therefore the only place the cap can be enforced once rather than per-family"
    - "`buildViceTool()`'s `_meta` stamp to `OUTPUT_CHAR_CAP` — the advertisement; enforcement must read the SAME constant, never a second copy"
    - "`CONTINUATION_STORE` to `handleResultContinue()` — the store the wiring populates and the tool that drains it"
  prohibitions:
    - statement: "No truncation branch is introduced. An oversized payload is split and served in full or it is not handled at all — silently shortening a payload is the failure mode the continuation mechanism exists to prevent."
      status: required
      verification: flagged-unverified
    - statement: "`OUTPUT_CHAR_CAP` is not duplicated. The enforcement site reads the existing constant; a second cap definition anywhere is a defect even if the two numbers happen to agree."
      status: required
      verification: flagged-unverified
    - statement: "The four chunking tests are REWRITTEN, never deleted or skipped. They are the only surviving evidence this regression exists."
      status: required
      verification: flagged-unverified
    - statement: "The deleted HTTP stand-in harness is NOT resurrected. `startBigPayloadServer()` and `VICE_MCP_URL`-driven forwarding are gone; the rewrite uses a surviving route."
      status: required
      verification: flagged-unverified
    - statement: "No new test-only environment hatch is added to shipped code to make the rewrite easier. A real registered tool producing a real oversized payload is already reachable."
      status: required
      verification: flagged-unverified
---

<objective>
Re-wire result chunking into the surviving dispatch path, so the shipped server enforces the result-size ceiling it advertises, and rewrite the four tests that prove it against a harness that still exists.

`96ef711f` deleted `forwardToVice()`, the only caller of `wrapPossiblyChunked()`. The function survived; its call site did not. MEASURED at `fd91b4b2` and re-confirmed at `6e4e9a0b`: `grep -rn wrapPossiblyChunked --include=*.ts --include=*.mts src/` returns the definition plus three comments and ZERO call sites.

MEASURED LIVE 2026-09-13, planning time, through a real spawned `vice-proxy.ts` child with `VICE_MAX_RESULT_CHARS=200`: a `tools/call` for `anno_get_symbols` against a seeded store returned `isError: false`, **one** content item, **23,290 characters**. A 23KB payload came back under a 200-character advertised cap, unchunked. That is the regression, reproduced at the wire, and the same run is the harness the rewritten tests use.

Purpose: a client that trusts the advertised `anthropic/maxResultSizeChars` currently gets something else, and `vice_result_continue` is a registered tool that can only refuse.
Output: one enforcement site at the proxy's single tools/call choke point; four rewritten tests driving it end to end; two now-false comments corrected.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/todos/pending/2026-09-13-result-chunking-orphaned-by-the-fork-removal.md
@src/mcp/vice/anno-tools.test.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Wire the cap into the one surviving tools/call choke point, end to end</name>
  <files>src/mcp/vice/vice-proxy.ts</files>
  <read_first>
    - `src/mcp/vice/vice-proxy.ts` — `OUTPUT_CHAR_CAP` (declared around `:416`, an IIFE reading `VICE_MAX_RESULT_CHARS` with a 500000 default) and the comment block above it, which states in its own words that the ceiling a caller is TOLD about and the ceiling actually enforced must be the same single number.
    - `src/mcp/vice/vice-proxy.ts` — the `ContinuationEntry` interface, `CONTINUATION_STORE`, `MAX_CONTINUATIONS`, `nextContinuationToken()`, `chunkMarkerText()`, `wrapPossiblyChunked()` (around `:958`) and `handleResultContinue()` (around `:989`). Read the whole block: `wrapPossiblyChunked()` already implements the split, the bounded store and the eviction; this task adds its call site, it does not rewrite it.
    - `src/mcp/vice/vice-proxy.ts` — the `ErrorTextResult` / `OkTextResult` / `ToolCallResult` declarations around `:620-637`, and `toolCallResultToWire()` / `isToolCallResult()` around `:1503-1514`.
    - `src/mcp/vice/vice-proxy.ts` — the `server.getServer().setRequestHandler(CallToolRequestSchema, async (request) => {...})` override near the end of the file (around `:1625-1654`), including its unknown-name fallback, its shape check, its `return toolCallResultToWire(raw);` success branch and its catch arm. This override is the ONE place every registered tool's result passes through.
    - `src/mcp/vice/vice-proxy.ts` — `buildViceTool()` around `:1522-1535`, specifically the `_meta` merge that stamps `"anthropic/maxResultSizeChars": OUTPUT_CHAR_CAP` on every tool.
    - `src/mcp/vice/vice-proxy.ts` — the registration block around `:1559-1608`: the manifest loop, the `RESULT_CONTINUE_TOOL` registration, the two synthetic handlers, and the `anno_*` loop. All four families flow through the same override.
  </read_first>
  <action>
Give `wrapPossiblyChunked()` a call site again, at the single choke point every tool result already crosses.

The enforcement point is the `CallToolRequestSchema` override's success branch — the line that currently hands `raw` straight to `toolCallResultToWire()`. That override is reached by the manifest loop's stock-dispatched tools, by `vice_recycle` and `vice_diagnose`, by the `anno_*` family and by `vice_result_continue` alike, so wiring here restores the capability for every family at once, from one line, reading one constant. There is no second dispatch surface to also patch — `vice-proxy.ts`'s own record of the fork removal states that no generic-dispatch surface survives.

Apply the wrap under exactly these conditions, and derive them from the data rather than from the tool name:

- Only when the result is a success. An `isError: true` result carries a refusal or a diagnostic, not a payload, and must reach the client unchanged.
- Only when the result carries exactly ONE content item. This single condition keeps `vice_result_continue`'s own output out of the wrap — it always returns a payload item plus a marker item — without naming that tool anywhere, so a future second multi-item producer is covered by the same rule. A result already carrying a marker must never be wrapped a second time.
- Only when that one item is a text item whose `text` is a string.

Every other result shape passes through to `toolCallResultToWire()` untouched.

Keep `wrapPossiblyChunked()`'s own body as it stands. It already returns the input unchanged when the text is at or under the cap, already splits on `OUTPUT_CHAR_CAP` boundaries with no truncation branch, already bounds `CONTINUATION_STORE` at `MAX_CONTINUATIONS` with oldest-first eviction, and already emits the first chunk as a pure payload item so reassembly is plain concatenation. Do not re-implement any of that at the call site, and do not add a second cap constant — read `OUTPUT_CHAR_CAP` through the existing function, which is what makes the advertised number and the enforced number one read of one definition.

Write the WHY at the new call site in this file's established comment voice: state that the ceiling is stamped on every tool's `_meta` by the registration helper and so has to be honoured for every tool, that this override is the only place all four registration families converge, and that a previous edit left the split function defined with no caller for the whole life of one release — naming the observable consequence (a registered continuation tool that could only refuse, and an oversized payload delivered whole under a much smaller declared ceiling) rather than citing where that was recorded. Planning vocabulary stays out of this file; it ships verbatim to npm.

Then prove it end to end, in this task, before writing any test file. Spawn a real `vice-proxy.ts` child with `CLAUDE_PROJECT_DIR` pointed at a temporary workspace holding a seeded `project.annostore` and `VICE_MAX_RESULT_CHARS` set small, drive `initialize` then `tools/call` for `anno_get_symbols`, and confirm the response carries two content items where it previously carried one; then call `vice_result_continue` with the token from the marker and confirm a real chunk comes back. Seed the store with `openStore()` / `setLabel()` / `closeStore()` from `anno-store.ts` — the idiom `anno-tools.test.ts`'s own `withStore()` helper already uses. Keep this proof as a scratch script outside the repository tree; Task 2 turns it into the committed tests.
  </action>
  <reversibility rating="reversible">A single call site at one choke point, reading an existing constant and calling an existing function. Revertable by deleting the wrap.</reversibility>
  <acceptance_criteria>
    - `wrapPossiblyChunked` has at least one real call site in `vice-proxy.ts` — the file's occurrence count for that identifier, with comment lines filtered out, is at least 2 (the definition plus the call).
    - The call site is inside the `CallToolRequestSchema` override, not inside `buildViceTool()` and not inside any per-family registration loop.
    - `OUTPUT_CHAR_CAP` is still declared exactly once in the tree.
    - The live proof above showed a two-item response for an over-cap payload and a one-item response for an under-cap payload from the same spawned proxy.
    - The live proof above showed `vice_result_continue` returning a chunk, not the unknown-or-expired refusal, for a token the same proxy had just issued.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck > /tmp/55-01-t1-typecheck.txt 2>&1; echo "EXIT=$?"; tail -20 /tmp/55-01-t1-typecheck.txt</automated>
    <fails_when>`EXIT=` is non-zero. A `TS2345` at the new call site means the narrowing of the single content item to a string payload was skipped.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && printf 'callsites='; grep -av '^\s*[*/]' vice-proxy.ts | grep -ac 'wrapPossiblyChunked'; printf 'capdefs='; grep -rac 'const OUTPUT_CHAR_CAP' vice-proxy.ts</automated>
    <fails_when>`callsites=` is less than 2 — the definition alone, with no caller, is the exact state this task exists to end. Or `capdefs=` is anything other than 1, meaning a second cap constant was introduced.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test vice-proxy-ping.test.ts stock-dispatch.test.ts anno-tools.test.ts > /tmp/55-01-t1-siblings.txt 2>&1; echo "EXIT=$?"; grep -E '^# (pass|fail)' /tmp/55-01-t1-siblings.txt</automated>
    <fails_when>`EXIT=` is non-zero, or `# fail` is anything other than 0. These three files drive the surviving dispatch and annotation surfaces directly; a regression here means the wrap changed a result shape it should have passed through.</fails_when>
  </verify>
  <done>`wrapPossiblyChunked()` has a real caller at the one tools/call choke point; a spawned proxy chunks an over-cap payload and leaves an under-cap one alone; `vice_result_continue` returns a real chunk; typecheck and the three sibling suites are clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the four chunking tests against a harness that still exists</name>
  <files>src/mcp/vice/vice-proxy.test.ts</files>
  <read_first>
    - `src/mcp/vice/vice-proxy.test.ts` — the four tests to rewrite, located by NAME (line numbers in any earlier census are stale): `an oversized result is recoverable in full across continuations`, `an exhausted continuation token fails loudly`, `an unknown continuation token (never issued by this proxy) fails loudly, not silently or opaquely`, and `the _meta cap stamp and the actual chunk boundary never drift apart`. Read each one whole, including the comment block above the fourth, which explains why the two halves are tied together with a single cap value.
    - `src/mcp/vice/vice-proxy.test.ts` — `startBigPayloadServer()` and every other helper the four tests reach. These build an in-process `node:http` stand-in that the deleted forwarding path used to dial; nothing reaches them any more.
    - `src/mcp/vice/vice-proxy.test.ts` — `startProxy()` and the JSON-RPC send/collect helpers, which survive and are what the rewrite drives.
    - `src/mcp/vice/vice-proxy.test.ts` — the test named `tools/list declares the same cap it enforces`, which PASSES today. It is the advertisement half of the pair; the rewrite must leave it untouched and still green.
    - `src/mcp/vice/anno-tools.test.ts` — the `withStore()` helper (temp workspace, `openStore()` / seed / `closeStore()`, `CLAUDE_PROJECT_DIR` set and restored) and the tracer test immediately below it. This is the seeding idiom to reuse.
    - `src/mcp/vice/anno-store.ts` — `openStore()`, `closeStore()` and `setLabel()` signatures.
  </read_first>
  <action>
Point the four surviving chunking tests at a route that still exists, preserving what each one asserts.

The harness they used is gone: an in-process HTTP stand-in reached through `VICE_MCP_URL`, whose only consumer was the deleted forwarding function. Do not resurrect it and do not add a test-only hatch to the shipped file. A real registered tool already produces a real oversized payload with no emulator, no broker and no stand-in server — MEASURED live at planning time: a spawned proxy with `CLAUDE_PROJECT_DIR` pointed at a temporary workspace holding a `project.annostore` seeded with 300 labels answered `tools/call` for `anno_get_symbols` with a 23,290-character single-item success. With `VICE_MAX_RESULT_CHARS` set to a small number in the child's environment, that payload crosses the cap many times over, deterministically, on every platform.

Build ONE local helper in this file that yields a seeded temporary workspace and its store path, modelled on `anno-tools.test.ts`'s `withStore()` but adapted for a spawned child: the child reads `CLAUDE_PROJECT_DIR` from its own environment, so this helper passes the path into `startProxy()`'s env rather than mutating the parent process. Have it take the label count so each test picks a payload size proportionate to the cap it sets.

Rewrite each test to assert the same property it always asserted, now over this route:

- **Recoverable in full across continuations.** Issue the oversized call; assert the response carries a payload item plus a marker item; extract the token from the marker; drive `vice_result_continue` until the final chunk is reported; assert the concatenation of the payload items equals the single-item text the SAME proxy returns for the SAME call with a cap large enough not to split it. Comparing against a second, unchunked run of the same query is what makes this a byte-exactness assertion rather than a length check, and it needs no fixture.
- **An exhausted continuation token fails loudly.** Drive a sequence to its final chunk, then call `vice_result_continue` once more with the same token; assert an error result whose text names the token and tells the caller to re-issue a narrower original call.
- **An unknown continuation token fails loudly.** Same assertion for a token this proxy never issued. This one is already close to correct — it never needed a payload — so change only what the dead harness forced.
- **The cap stamp and the chunk boundary never drift apart.** Keep its structure exactly: ONE cap value, set once in the child's environment, read back from the `tools/list` `_meta` entry AND measured as the observed boundary of the first payload chunk. Assert the two numbers are equal. The comment above it explains why this test exists beside the two that exercise each half separately with different values; keep that comment and update only the parts that describe the retired harness.

Where a test's own comment describes the stand-in server or the forwarding path, rewrite it to describe the route actually used, and state plainly that the chunking call site was absent for a period and this test is what would have caught it. Keep the `vice_memory_read` targeting out of the rewrite — that name only ever mattered to the stand-in's request matcher.

Do not delete any of the four. Do not mark any of them `skip`.
  </action>
  <acceptance_criteria>
    - All four tests are present by name, none is `skip`ped, and all four pass.
    - `tools/list declares the same cap it enforces` is unmodified and still passes.
    - The file no longer reaches `startBigPayloadServer()` from any of the four (whether the helper itself survives for other callers is Plan 55-03's question, not this task's).
    - The byte-exactness assertion compares a reassembled chunked payload against an unchunked run of the same query, not against a hand-written expected string.
    - No shipped file gained a test-only environment variable for this task.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test --test-name-pattern 'continuation|oversized|cap stamp|declares the same cap' vice-proxy.test.ts > /tmp/55-01-t2.txt 2>&1; echo "EXIT=$?"; grep -E '^# (tests|pass|fail|skipped)' /tmp/55-01-t2.txt</automated>
    <fails_when>`# fail` is anything other than 0, `# skipped` is anything other than 0, or `# pass` is fewer than 5 — four rewritten tests plus the untouched advertisement test. A `# pass` of 5 with `# fail` 0 is the success shape.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && printf 'skips='; grep -ac 'test.skip\|{ skip:' vice-proxy.test.ts; printf 'four='; grep -ac 'an oversized result is recoverable in full across continuations\|an exhausted continuation token fails loudly\|an unknown continuation token\|the _meta cap stamp and the actual chunk boundary never drift apart' vice-proxy.test.ts</automated>
    <fails_when>`four=` is less than 4 — one of the four was deleted rather than rewritten. `skips=` rising above its pre-task value means a test was disabled instead of fixed; record the pre-task value in the SUMMARY before changing the file.</fails_when>
  </verify>
  <done>The four chunking tests drive the restored capability over a live route and pass; the advertisement test is untouched and green; nothing was skipped or deleted.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the two comments that describe chunking as it was, not as it is</name>
  <files>src/mcp/vice/anno-tools.ts, src/mcp/vice/vice-proxy.ts</files>
  <read_first>
    - `src/mcp/vice/anno-tools.ts` around `:1725-1745` — the size-cap comment block for the annotation read/disassemble family. It states that the registration helper calls each runner directly and that this family's results therefore never pass through the split, which is why the per-verb result cap is described as the only bound there is. Task 1 changes the first half of that claim.
    - `src/mcp/vice/vice-proxy.ts` around `:628-637` — the doc comment on the shared success-or-failure result interface, which lists the handlers that share the shape and names one function that no longer exists in this file.
    - `src/mcp/vice/vice-proxy.ts` — the comment block above `wrapPossiblyChunked()`, to confirm which parts are now true again and need no change.
  </read_first>
  <action>
Bring two comment blocks back into agreement with the code, without weakening either.

In the annotation family's size-cap block, the statement that this surface is never split was true while the split had no caller and stops being true the moment Task 1 lands. Correct that sentence to say what now happens: the family's results cross the proxy's single tools/call choke point like every other tool's, so an over-cap result is split across a continuation sequence rather than delivered whole. Then keep the rest of the block intact and say why the per-verb cap is still load-bearing rather than redundant — the client's own inline-response ceiling was measured far below the proxy's default character cap, so a result that never trips the proxy's split can still be far too large to be useful, and the second mitigation (a required result limit with the true total returned beside the truncated list) remains the bound that matters for this family. Do not delete the measurement or the reasoning; only the false clause changes.

In the shared-result-shape doc comment, replace the name of the retired tools/call handler with the override that actually answers tools/call now, so the list of things sharing the shape is a list a reader can find.

Both files ship verbatim to consumers. Keep planning vocabulary out of them — no planning-tree path, no phase or plan number, no bare decision or gap identifier. State the reason in terms a consumer can act on.
  </action>
  <acceptance_criteria>
    - The annotation family's size-cap block describes splitting as something that now happens on this surface, and still carries the measured client-ceiling reasoning and the required-result-limit mitigation.
    - The shared-result-shape doc comment names only functions that exist in `vice-proxy.ts`.
    - Neither file gained a planning-tree path, a `/gsd-` command name, a phase or plan number, or a bare `D-NN` / `G-NN-N` identifier.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck > /tmp/55-01-t3.txt 2>&1; echo "EXIT=$?"; tail -5 /tmp/55-01-t3.txt</automated>
    <fails_when>`EXIT=` is non-zero.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && printf 'planningrefs='; grep -aEc '\.planning/|/gsd-[a-z]|ROADMAP\.md|REQUIREMENTS\.md' src/mcp/vice/anno-tools.ts src/mcp/vice/vice-proxy.ts | paste -sd' '</automated>
    <fails_when>Either per-file count is anything other than `0` — INVERTED polarity: a non-zero count is the failure. These two files ship to consumers who have no planning tree.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test anno-tools.test.ts > /tmp/55-01-t3b.txt 2>&1; echo "EXIT=$?"; grep -E '^# (pass|fail)' /tmp/55-01-t3b.txt</automated>
    <fails_when>`# fail` is anything other than 0. This suite contains source-scanning assertions over `anno-tools.ts` and will catch a comment edit that breaks one.</fails_when>
  </verify>
  <done>Both comment blocks describe the code as it is; the annotation suite is green; typecheck is clean; neither shipped file carries planning vocabulary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → proxy stdio | Tool names and arguments arrive untrusted over stdin; the tools/call override is the first code that inspects them |
| proxy → MCP client | Result payloads leave over stdout; this plan changes their shape and adds a server-held token to that stream |
| continuation token → in-process store | A client-supplied token is used as a `Map` key into server-held payload chunks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-01 | Denial of Service | `CONTINUATION_STORE` growth | medium | mitigate | The store is already bounded at `MAX_CONTINUATIONS` with oldest-first eviction, and a completed sequence deletes its own entry. Task 1 adds a caller and must not raise or remove that bound; the acceptance criteria forbid a second cap constant. |
| T-55-02 | Information Disclosure | continuation token reaching a chunk | low | mitigate | Tokens are server-minted from pid, wall clock and a monotonic counter, never client-chosen, and the store is per-process with no cross-session persistence. `handleResultContinue()` refuses an unknown key rather than constructing a result from it; the rewritten unknown-token test pins that refusal. |
| T-55-03 | Tampering | payload integrity across a split | high | mitigate | The first content item is the pure payload chunk with no marker text mixed in, and there is no truncation branch. The rewritten round-trip test asserts byte-exactness by comparing the reassembly against an unchunked run of the same query. |
| T-55-04 | Denial of Service | oversized result delivered whole | medium | mitigate | This is the regression the plan closes: a 23,290-character result was MEASURED crossing the wire under a 200-character advertised cap. Task 1 restores enforcement at the one choke point. |
| T-55-05 | Repudiation | advertised contract diverging from behaviour | medium | mitigate | The advertisement and the enforcement read one constant; the cap-stamp test asserts the two numbers are equal under a single cap value. |
| T-55-SC | Tampering | package-manager installs | n/a | accept | This plan runs no `npm install` / `pip install` / `cargo add` task and adds no dependency. No package-legitimacy checkpoint is owed. |
</threat_model>

<verification>
- `npm run typecheck` exits 0 from `src/mcp/vice`.
- `node --test vice-proxy-ping.test.ts stock-dispatch.test.ts anno-tools.test.ts` reports `# fail 0`.
- The four chunking tests plus the advertisement test pass under a name-pattern run of `vice-proxy.test.ts`.
- `npm run test:automated` still reports 4407 tests, 0 failures, 9 skips — the phase baseline, unchanged by this plan. Run it with output redirected and the exit code read on the same line; never pipe it into `tail` or `head`.
</verification>

<success_criteria>
- A spawned proxy splits an over-cap successful result into a payload item plus a marker item, and leaves an at-or-under-cap result as a single item.
- `vice_result_continue` returns real chunks for a token the same proxy issued, and refuses an unknown or exhausted one by name.
- Reassembling a chunked sequence reproduces the unchunked text byte-for-byte.
- The advertised cap and the enforced boundary are one read of one constant.
- No test was skipped or deleted to reach any of the above.
</success_criteria>

<output>
Create `.planning/phases/55-restore-the-fork-removal-s-dropped-capabilities-and-re-basel/55-01-SUMMARY.md` when done. Record: the pre-task `skip` count in `vice-proxy.test.ts`, the exact enforcement condition chosen at the choke point, and the observed content-item counts for the over-cap and under-cap runs.
</output>
