---
phase: 46-the-lossless-export-invariant-and-the-provenance-carry
plan: 06
type: execute
wave: 4
depends_on: [46-05]
files_modified:
  - src/mcp/vice/anno-export-asm.test.ts
autonomous: true
requirements: [BUILD-07]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A planted-control fixture carrying a range a plausible heuristic would want to drop — ledger-classified `CRACKER-PATCH`, cracktro-shaped printable text, and carrying no label or comment referencing it — exports with that range present byte for byte: its bytes appear in `result.expectedBytes` and its block appears in `result.blocks`."
    - "The control was OBSERVED RED first, against a deliberately-filtering variant of the exporter that lives only in the test file: the variant's output is asserted to be MISSING the range, in the same test run, before the real exporter's green result is asserted. A control that has never been red proves nothing."
    - "The test carrying the control records, in a comment matching `channel-lock.test.ts:108-118`'s shape, the specific broken variant that was tried, the wrong output it produces, and that it was actually run and observed to fail."
    - "A structural guard reads `anno-export-asm.ts`'s own source through `codeOnly()` — the project's own comment-and-string-literal stripper — and asserts the block-construction `.map()` is present in its exact unconditional form, so the guard cannot be satisfied or invalidated by the module's own prose."
    - "The structural guard is itself proven non-vacuous: the same predicates applied to an in-memory copy of the source with the forbidden shape inserted REPORT that copy, in the same test run."
    - "A behavioural companion asserts `result.blocks.length` equals the row count `listRanges()` returns, over a fixture that carries every `Verdict` value the ledger vocabulary has and at least two distinct `Confidence` tiers — so \"no verdict value changes what is emitted\" is measured across the vocabulary, not on one example."
    - "The deliberately-filtering variant is absent from the shipped module: a test asserts its identifier does not appear in `anno-export-asm.ts`'s source, and `anno-export-asm.test.ts` remains absent from `package.json`'s `files[]`."
  prohibitions:
    - "No verdict, confidence or kind VALUE may change what the exporter emits. There must be no conditional, comparison, switch or filter anywhere between the store read and the emitted block array whose condition inspects one of those fields' contents."
    - "A green planted control must not be trusted without a recorded red observation against a genuinely filtering variant, and that variant must never ship in `anno-export-asm.ts` or any other module in `package.json`'s `files[]`."
  artifacts:
    - src/mcp/vice/anno-export-asm.test.ts
  key_links:
    - "`codeOnly()` from `shipped-modules.ts` -> the structural guard: the ONE comment-and-string-literal stripper in this tree, and the reason the guard can safely negative-assert about a module whose own header discusses the forbidden shape at length."
    - "the filtering variant -> the same fixture -> the real `exportAsm()`: both halves run over ONE fixture in ONE test, so the red and the green are about the same input and the control cannot be red for an unrelated reason."
---

<objective>
Turn BUILD-07 from an invariant that happens to hold into one that cannot silently stop
holding: a planted control observed failing before it is trusted, and a structural guard
that reds a named test the moment any code between the store read and the emitted blocks
starts branching on a provenance value.

Purpose: `.planning/research/ARCHITECTURE.md:366-371` records that `exportAsm()` has
**zero** filtering logic today, so the invariant already holds vacuously. The risk is
prospective — a future contributor adding a provenance-based skip inside the same function
that now, after plan 46-01, finally has provenance values in scope. Criterion 1's own text
names the requirement precisely: "The control is observed failing first, against a
deliberately-filtering variant of the exporter, before its green result is trusted: a
control that has never been red proves nothing."
Output: the planted control with its recorded red observation, the structural guard with its
own non-vacuity proof, and the never-shipped assertion for the variant.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-the-lossless-export-invariant-and-the-provenance-carry/46-RESEARCH.md
@.planning/phases/46-the-lossless-export-invariant-and-the-provenance-carry/46-PATTERNS.md
@.planning/phases/46-the-lossless-export-invariant-and-the-provenance-carry/46-01-SUMMARY.md
@.planning/phases/46-the-lossless-export-invariant-and-the-provenance-carry/46-05-SUMMARY.md
</context>

Assumptions A1-A4, the four hard scope fences, the measured five-name test baseline, the
"Artifacts this phase produces" inventory and the probe accounting table are recorded once
in `46-01-PLAN.md`. They bind this plan identically.

Fence 4 is this plan's subject and its sharpest constraint: the deliberately-filtering
exporter variant is **test-only and must never ship**. It lives in
`anno-export-asm.test.ts`, which is not in `package.json`'s `files[]`, and Task 1 asserts
its absence from the shipped module rather than assuming it.

This plan runs LAST in the phase deliberately. The structural guard pins the shape of
`anno-export-asm.ts` after both plan 46-01's provenance carry and plan 46-05's exclusion
emission have landed. Written earlier it would pin a shape a later plan then changed, and a
guard that has to be edited by the next plan is a guard nobody trusts.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The planted control — observed RED against a filtering variant, then trusted GREEN</name>

  <files>src/mcp/vice/anno-export-asm.test.ts</files>

  <read_first>
    - `src/mcp/vice/anno-export-asm.test.ts:1-70` — the file header in full, especially the sentence that both planted-violation tests exist to show the brackets genuinely bite, because "an assertion nobody has ever seen fail is indistinguishable from an assertion that cannot fail". That is this task's whole thesis, already written in this file.
    - `src/mcp/vice/anno-export-asm.test.ts:435-500` — `PLANTED VIOLATION 1` and `PLANTED VIOLATION 2` in full. Note the shape: the PAIRED direction first (the unmodified export assembles cleanly, so a red below cannot be a broken fixture), then ONE documented substitution, then an assertion that the substitution actually changed the text, then the consequence. Reproduce that order.
    - `src/mcp/vice/channel-lock.test.ts:108-118` — **the exact comment template criterion 1 requires.** Read it verbatim. It names the specific broken variant tried (`queue.pop()` instead of `queue.shift()`), states the wrong output that variant produces (`["op-3", "op-2", "op-1"]`), and records that the test was run by hand against that variant and observed to fail with that exact wrong result — "confirming the assertion below actually distinguishes the two disciplines rather than merely reading whichever order happens to fall out." Copy that structure exactly, substituting this phase's variant and its wrong output.
    - `src/mcp/vice/anno-bank.test.ts:1-10` — the file-header-level declaration of "written first, observed red before the implementation existed", for the section banner's voice.
    - `src/mcp/vice/anno-coverage.test.ts:915-925` — the non-vacuity assertion shape, which the fixture needs in three separate directions here.
    - `src/mcp/vice/anno-export-asm.ts:824-905` — `exportAsm()`'s opening through the block `.map()`, since the variant is a local re-implementation of exactly this stretch and nothing more.
    - `src/mcp/vice/anno-export-asm.test.ts` — the fixture helper as plan 46-05 left it (with its `exclusions` field), the ledger-fixture helper from plan 46-01, and `verifyExport()`.
    - `src/skills/c64-provenance-diff/SKILL.md:106-130` — the five kinds and the three verdicts, so the planted range's ledger row uses the real vocabulary (`cracktro` kind, `CRACKER-PATCH` verdict) rather than an invented one.
    - `src/mcp/vice/package.json` — the `files[]` array, to confirm `anno-export-asm.test.ts` is absent from it.
  </read_first>

  <behavior>
    Write each as a failing assertion first, observe it fail, then implement.

    **The fixture** — a store whose middle range is, on three independent counts, exactly
    what a plausible heuristic would reach for:
    - its ledger row carries verdict `CRACKER-PATCH` and kind `cracktro`;
    - its bytes are a run of printable PETSCII that reads as crack-credit text;
    - no label, comment, enum usage or cross-reference anywhere in the store references it.
    Three non-vacuity assertions, before anything else: the ledger really classifies it
    `CRACKER-PATCH`; the bytes really are a printable run; and the store really holds zero
    references to any address in it. Without all three, "the heuristic would have wanted to
    drop it" is an assertion about a fixture that never tempted anything.

    **The RED half, asserted first and in the same test**
    - The test-only filtering variant, run over that fixture, produces a source and a
      block list from which the planted range is MISSING: its start is absent from the
      variant's block starts, and its bytes are absent from the variant's expected-byte span.
    - Assert the variant's block count is exactly one fewer than the store's range count —
      so the variant is proven to have dropped exactly one range, not to have failed for some
      unrelated reason.

    **The GREEN half, asserted second, over the same fixture**
    - The real, shipped `exportAsm()` over that same fixture emits a block whose `start`
      equals the planted range's start and whose `endExclusive` equals its inclusive end plus
      one.
    - The planted range's exact bytes appear at the right offset in `result.expectedBytes`.
    - The planted range's block carries a `; PROVENANCE LEDGER:` line naming `CRACKER-PATCH`
      — it is annotated, not merely retained.
    - `result.blocks.length` equals the store's range count — nothing was dropped.
    - `verifyExport(result)` returns `outcome: "ok"` with `byteDiff.equal === true`.

    **The never-shipped half**
    - The filtering variant's identifier does not appear in `anno-export-asm.ts`'s source.
    - `anno-export-asm.test.ts` is absent from `package.json`'s `files[]`.
  </behavior>

  <action>
Add one section to `anno-export-asm.test.ts` under a banner comment in the file's style,
declaring — in `anno-bank.test.ts:1-10`'s voice — that this section's assertions were
written first and observed to fail before the control was trusted.

**The variant.** Write a module-local function named `exportAsmWithVerdictFilter` that takes
the same option bag the real `exportAsm()` takes plus the ledger, opens the store, reads the
ranges, and builds its block list with a `.filter()` that drops any range whose overlapping
ledger row carries the verdict the filter is keyed on — then returns the same
`{ source, expectedBytes, blocks }` shape by whatever minimal means makes the assertions
below meaningful. It exists for exactly one purpose: to demonstrate that the assertions in
the green half can distinguish a lossless exporter from a filtering one. Keep it as small as
that purpose requires — it does not need to emit valid ACME, only to produce a block list
and an expected-byte span that the same assertions can be run against.

Put a doc-comment on it saying, unambiguously and in the first sentence, that this function
is TEST-ONLY and must never be copied into `anno-export-asm.ts` or any module in
`package.json`'s `files[]`; that it is the negative control BUILD-07 criterion 1 requires;
and that its existence in this file is the reason the green assertions below mean something.

**The control test.** One test, both halves, one fixture. Order matters and should mirror
the file's existing planted-violation tests: fixture and its three non-vacuity assertions;
then the RED half; then the GREEN half. Both halves in one test so the red and the green are
provably about the same input — split across two tests, a fixture divergence would make the
control pass while measuring nothing.

**The recorded observation.** Immediately above the green half's first assertion, write a
comment in `channel-lock.test.ts:108-118`'s exact structure. It must name: the specific
broken variant tried (a block-construction `.filter()` keyed on the overlapping ledger row's
verdict, in place of the unconditional `.map()`); the wrong output that variant produces
(one fewer block than the store has ranges, the planted range's start absent from the block
starts, and its bytes absent from the expected-byte span); and that the variant was actually
run and observed to fail with that exact wrong result — in this case run mechanically, in
this same test, in the red half directly above, rather than by hand. Say that explicitly:
this control's red observation is CONTINUOUS rather than historical, which is stronger than
`channel-lock.test.ts`'s authoring-time hand run, and a later reader should know which of
the two they are looking at.

**The never-shipped assertions.** Two small tests: one `readFileSync`es
`anno-export-asm.ts` and asserts the variant's identifier is absent from it; the other reads
`package.json` and asserts `files[]` does not include `anno-export-asm.test.ts`. The second
follows the same idiom `acme-gate.ts`'s own absence test already uses in this directory.
  </action>

  <precondition>ACME 0.97 is resolvable — `command -v acme` succeeds, or `ACME_BIN` points at it — so the green half's round-trip assertion runs instead of skipping through `acme-gate.ts`'s `SKIP_REASON`. MEASURED present at `/home/henrik/.local/bin/acme` on 2026-09-11.</precondition>

  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test anno-export-asm.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "ℹ fail 0" is absent, or the summary line "ℹ skipped 0" is absent (a non-zero skip count means ACME went missing and the green half's round trip proved nothing)</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck</automated>
    <fails_when>non-zero exit, or any line matching "error TS" in the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-npm-packages.mjs; echo "exit=$?"</automated>
    <fails_when>the printed line is not "exit=0"</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac 'DISCRIMINATING POWER' src/mcp/vice/anno-export-asm.test.ts</automated>
    <fails_when>the count is 0 — the recorded-observation comment is missing</fails_when>
  </verify>

  <acceptance_criteria>
    - One test contains BOTH the red half and the green half over ONE fixture, in that order, with the three fixture non-vacuity assertions preceding both.
    - The three non-vacuity assertions each assert a distinct property: the ledger row's verdict is `CRACKER-PATCH`; the planted bytes are a printable run; the store holds zero labels, comments, enum usages or cross-references at any address inside the planted range.
    - The red half asserts the variant's block count is exactly one fewer than the store's range count, that the planted start is absent from the variant's block starts, and that the planted bytes are absent from its expected-byte span — three assertions, not one.
    - The green half asserts the planted block is present with the exact `start` and `endExclusive`, that its bytes appear at the right offset in `result.expectedBytes`, that its block carries a `; PROVENANCE LEDGER:` line naming `CRACKER-PATCH`, that `result.blocks.length` equals the store's range count, and that `verifyExport(result)` gives `outcome === "ok"` with `byteDiff.equal === true`.
    - A comment immediately above the green half's first assertion follows `channel-lock.test.ts:108-118`'s structure and names all three required elements: the variant tried, the wrong output it produces, and that it was run and observed to fail. It also states that this control's red observation is continuous rather than a historical hand run.
    - `exportAsmWithVerdictFilter`'s doc-comment states in its first sentence that it is test-only and must never be copied into any module in `package.json`'s `files[]`.
    - A test asserts `exportAsmWithVerdictFilter`'s identifier is absent from `anno-export-asm.ts`'s source.
    - A test asserts `package.json`'s `files[]` does not include `anno-export-asm.test.ts`.
    - `node scripts/check-npm-packages.mjs` prints `exit=0`, and `node --test anno-export-asm.test.ts` reports `ℹ fail 0` and `ℹ skipped 0`.
  </acceptance_criteria>

  <done>The planted control is red-then-green in one test over one fixture: a range that is `CRACKER-PATCH`-classified, cracktro-shaped and entirely unreferenced is demonstrably dropped by a filtering variant and demonstrably present byte for byte in the real exporter's output, with the discriminating-power observation recorded in the file and the variant proven absent from everything that ships.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The structural guard — and the proof that the guard itself can bite</name>

  <files>src/mcp/vice/anno-export-asm.test.ts</files>

  <read_first>
    - `src/mcp/vice/anno-export-asm.test.ts:860-885` — the WR-07 structural guard: `readFileSync` the module's own source, `assert.match` on an exact construct, with a failure message naming the rule. This is the template's skeleton.
    - `src/mcp/vice/shipped-modules.ts:1-60` — the header explaining why `codeOnly()` lives there and what it handles: `//` and `/* */` comments, all three literal shapes, nested interpolation to any depth, escapes, and regular-expression literals. Read the MEASURED incident it records — a regex body containing a backtick opened a phantom literal frame that cut `anno-coverage.ts` from 2329 lines to 1107 and hid seven exported functions from the guard reading them. That is why the guard below must use this function and not a hand-rolled strip.
    - `src/mcp/vice/shipped-modules.ts:200-230` — `codeOnly()`'s signature and its `keepLiteralBodies` parameter, so the call passes the right argument for a construct-shape scan.
    - `src/mcp/vice/anno-export-asm.ts:857-895` — the block-construction stretch as plans 46-01 and 46-05 leave it: `sortedRanges`, then `const blocks: ExportBlock[] = sortedRanges.map((row) => ({ ... }));`. The positive assertion pins this.
    - `src/mcp/vice/module-classification.test.ts` — grep for the Direction tests' non-vacuity guards, for the discipline of proving a predicate reports a synthetic violation before trusting its silence on the real tree.
    - `src/mcp/vice/anno-store.ts` — `listRanges()`, so the behavioural companion compares against the store's own row count rather than against a number the fixture hard-codes.
    - `src/skills/c64-provenance-diff/SKILL.md:106-130` — the complete verdict and confidence vocabulary, so the companion fixture covers all three verdicts and at least two confidence tiers rather than one example.
  </read_first>

  <behavior>
    **The structural guard**
    - Read `anno-export-asm.ts`'s source, pass it through `codeOnly()`, and assert POSITIVELY
      that the block-construction `.map()` is present in its exact unconditional form.
      Positive, not negative, as the primary assertion: it pins the one right shape rather
      than enumerating wrong ones.
    - Assert NEGATIVELY, over the same stripped text, that no `.filter()` is applied to the
      range list or the block list, and that no conditional between the store read and the
      block array inspects a verdict, confidence or kind field's contents.

    **The guard's own non-vacuity — the load-bearing half**
    - Take the stripped source as a string, insert the forbidden shape into an in-memory copy,
      and assert the SAME predicates REPORT that copy. Assert the insertion actually changed
      the string first.
    - Do this once per predicate, each as its own test whose name begins `guard non-vacuity:`.
      A guard with three predicates and one non-vacuity proof has two predicates nobody has
      ever seen fire.
    - Assert `codeOnly()`'s own output is well-formed on this input — non-empty, and strictly
      shorter than the raw source — so a stripper bug that returned an empty string could not
      make every negative assertion pass for the wrong reason. This is the same self-check
      `ci-suite-coverage.test.ts` applies to its own text extraction, and for the same reason.
      Name this test with the same `guard non-vacuity:` prefix — it is the fifth proof.

    **The behavioural companion, across the vocabulary**
    - A fixture whose ledger carries all three verdicts (`ORIGINAL`, `CRACKER-PATCH`,
      `UNKNOWN`) and at least two distinct confidence tiers, over a store with one range per
      verdict plus one recorded exclusion.
    - `result.blocks.length` equals `listRanges()`'s row count for that store.
    - Every block carries at least one `; PROVENANCE LEDGER:` line — asserted per block, not
      as a total, so a block annotated twice cannot cover for a block annotated zero times.
    - The set of verdicts appearing anywhere in `result.source` equals the set of verdicts in
      the ledger. Nothing was filtered out and nothing was invented.
    - Non-vacuity: assert the fixture really covers all three verdicts and two confidence
      tiers before asserting anything about the output.
  </behavior>

  <action>
Add the guard and its companion to `anno-export-asm.test.ts`, under their own banner
comment.

**Use `codeOnly()`.** Import it from `./shipped-modules.ts` and pass
`anno-export-asm.ts`'s source through it before any matching. This is not optional polish
and the reason belongs in a comment above the call: this module's own header, its marker
constants' doc-comments and plan 46-05's `EXCLUSION_MARKER_PREFIX` rationale all DISCUSS the
forbidden shape at length, in prose. A negative grep over the raw file would therefore match
the module's own explanation of why it must not do the thing — a self-invalidating gate, and
one whose failure would look exactly like a real regression. `codeOnly()` is this tree's ONE
comment-and-string-literal stripper, and the incident recorded in its own header is why a
hand-rolled strip is the wrong answer.

**Primary assertion, positive.** `assert.match` the stripped text against a pattern pinning
the block-construction `.map()` in its exact current form, with a failure message naming the
rule in this project's voice: the block array is built 1:1 from the sorted range list and
must stay that way, because `BUILD-07` requires the export path be lossless by default and
no range be dropped, filtered or omitted on the tool's own judgement.

**Secondary assertions, negative, over the same stripped text.** One per forbidden shape: a
`.filter()` applied to the range list; a `.filter()` applied to the block list; and a
conditional whose test reads one of the three provenance field names. Give each its own
`assert.doesNotMatch` with its own failure message naming what a contributor has just done
and which requirement it violates.

**The guard's non-vacuity, one proof per predicate.** For each of the four assertions above,
build an in-memory mutated copy of the stripped text with the corresponding violation
inserted, assert the insertion changed the string, and assert the predicate reports the
mutated copy. Write these as their own tests, named so the run output shows four separate
proofs. State in a comment that this is the `module-classification.test.ts` Direction-guard
discipline applied to this guard: a predicate that has never fired is indistinguishable from
one that cannot.

Add the `codeOnly()` self-check as its own assertion — output non-empty and strictly shorter
than the raw source — with the `ci-suite-coverage.test.ts` reasoning cited: an extraction
that silently returned nothing would make every subsequent negative assertion pass.

**The behavioural companion.** Build the all-verdicts fixture, assert its three non-vacuity
properties, then assert the four output properties from `<behavior>`. Compare
`result.blocks.length` against `listRanges()`'s live row count rather than a hard-coded
number, so the assertion stays true if the fixture changes — and note in a comment that this
is the same "never hardcode a static list where the source table can drive the assertions"
rule this file's own header already states for the data-type and opcode suites.

Assert the verdict SET equality in both directions: every ledger verdict appears in the
source, and every verdict-looking token in the source came from the ledger. One direction
alone would miss an invented verdict; the other alone would miss a dropped one.
  </action>

  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test anno-export-asm.test.ts</automated>
    <fails_when>non-zero exit, or the summary line "ℹ fail 0" is absent, or the summary line "ℹ skipped 0" is absent</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck</automated>
    <fails_when>non-zero exit, or any line matching "error TS" in the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac 'codeOnly' src/mcp/vice/anno-export-asm.test.ts</automated>
    <fails_when>the count is 0 — the guard is matching raw source and is self-invalidating</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test anno-export-asm.test.ts 2>&1 | grep -ac 'guard non-vacuity:'</automated>
    <fails_when>the count is below 5 — fewer than the four per-predicate proofs plus the codeOnly self-check ran. The `guard ` prefix is load-bearing: MEASURED 2026-09-11, the bare word `non-vacuity` already matches 2 lines of this file's output, and plans 46-01, 46-02 and 46-05 each add more, so a bare-word threshold would be met without a single guard proof existing.</fails_when>
  </verify>

  <acceptance_criteria>
    - The structural guard imports `codeOnly` from `./shipped-modules.ts` and passes `anno-export-asm.ts`'s source through it before any `assert.match` or `assert.doesNotMatch`, with a comment stating that a raw-source grep would match the module's own prose about the forbidden shape.
    - One positive assertion pins the block-construction `.map()` in its exact current form, with a failure message naming `BUILD-07` and the 1:1 rule.
    - Three negative assertions exist — range-list filter, block-list filter, and a conditional reading a provenance field — each with its own failure message naming what the contributor did and which requirement it violates.
    - Four separately-named tests whose names begin `guard non-vacuity:` exist, one per predicate, each asserting its mutation changed the string AND that the predicate reports the mutated copy; the `codeOnly()` self-check carries the same prefix as the fifth.
    - An assertion checks `codeOnly()`'s output on this input is non-empty and strictly shorter than the raw source, with the reason stated.
    - The behavioural companion's fixture covers all three verdicts and at least two confidence tiers, with a preceding non-vacuity assertion of exactly that.
    - `result.blocks.length` is compared against `listRanges()`'s live row count, not a hard-coded number.
    - Every block is asserted individually to carry at least one provenance line — the assertion is per block, not a total.
    - Verdict set equality is asserted in both directions.
    - `node --test anno-export-asm.test.ts` reports `ℹ fail 0` and `ℹ skipped 0`, and the run output contains at least five lines matching `guard non-vacuity:`.
  </acceptance_criteria>

  <done>A named structural test reds the moment anything between the store read and the emitted block array starts branching on a provenance value; each of its four predicates has been observed firing against a synthetic violation; the stripper it depends on is self-checked; and a behavioural companion measures "no verdict value changes what is emitted" across the whole verdict vocabulary rather than on one example.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a future contributor's edit -> `anno-export-asm.ts`'s emission path | The only boundary this plan defends. After plan 46-01, provenance values are in scope inside `exportAsm()` for the first time, which is exactly the temptation `.planning/research/ARCHITECTURE.md:366-371` names. |
| the test-only filtering variant -> the shipped tarball | A negative control is a working filtering exporter. Its escape into a shipped module would be the very defect it exists to detect. |
| `anno-export-asm.ts`'s own prose -> a negative grep over it | The module documents the forbidden shape at length. A guard reading raw source would match that documentation and fail on the comment rather than on a regression. |

ASVS level 1. Only **V5 Input Validation** applies to this phase, per `46-RESEARCH.md`
§ Security Domain; this plan adds no new input parsing and no new emission path, so its
contribution is to keep the three threats' mitigations from silently regressing.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-01 | Tampering | ledger and exclusion free text -> emitted ACME source | high | mitigate | Mitigation lands in plans 46-01 and 46-05 (the two `assertExportableCommentText()` call sites). This plan adds no fourth emission path. Its contribution is the behavioural companion, which exports across the whole verdict vocabulary and would surface an emission path that had grown its own unvalidated text route. |
| T-46-02 | Information Disclosure | refusal messages | medium | mitigate | Mitigation lands in plans 46-02, 46-03 and 46-05. This plan adds no new refusal. |
| T-46-03 | Tampering / Repudiation | a provenance value silently changing what is emitted | medium | mitigate | Mitigated in this plan, Task 2 — this is the threat the structural guard exists for. A verdict-keyed conditional is how a tampered or hand-edited ledger row would gain the power to remove a range from the artefact, and the guard makes that edit red a named test. Task 2's four per-predicate non-vacuity proofs are what make the guard's silence meaningful. |
| T-46-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this phase; `46-RESEARCH.md` § Package Legitimacy Audit records zero new packages. Recorded rather than omitted. |
</threat_model>

<verification>
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck` — clean.
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test anno-export-asm.test.ts` — `ℹ fail 0` and `ℹ skipped 0`.
- `cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-npm-packages.mjs; echo "exit=$?"` — `exit=0`, confirming neither the variant nor the test file reaches the tarball.
- `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated` — the failing test NAME set is exactly the five names recorded in `46-01-PLAN.md` § "Measured baseline for this phase". A sixth or different name is a regression. Never compare the count; never pipe through `tail`.
- Manual read, once, at the end of the phase: `git log --oneline` for phase 46 shows the red half and the green half of the planted control landing in the same commit. A green-only commit followed by a red-adding commit would satisfy every automated check above while inverting the criterion's own ordering requirement.
</verification>

<success_criteria>
- A planted range that is `CRACKER-PATCH`-classified, cracktro-shaped and wholly unreferenced survives export byte for byte, and is annotated rather than merely retained.
- The control was observed red first, mechanically and in the same test run, against a deliberately-filtering variant — with the discriminating-power observation recorded in the file in `channel-lock.test.ts`'s structure.
- The filtering variant exists only in the test file, and its absence from the shipped module and from `files[]` is asserted rather than assumed.
- A structural test reads the module's own source through `codeOnly()` and reds on any conditional between the store read and the emitted blocks that inspects a provenance value.
- Each of the guard's four predicates has been observed reporting a synthetic violation, and the stripper it relies on is self-checked against returning nothing.
- "No verdict value changes what is emitted" is measured across all three verdicts and two confidence tiers, with per-block annotation asserted individually.
</success_criteria>

<output>
Create `.planning/phases/46-the-lossless-export-invariant-and-the-provenance-carry/46-06-SUMMARY.md` when done
</output>
