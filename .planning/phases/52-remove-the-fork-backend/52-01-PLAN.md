---
phase: 52-remove-the-fork-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/stock-hard-losses.md
  - .planning/PROJECT.md
  - src/mcp/vice/docs-fork-decision.test.ts
autonomous: false
requirements: [FORKRM-02, FORKRM-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`.planning/PROJECT.md`'s single `FORK-01` Key Decisions row states REVERSED, carries the reversal's ISO date, names the two grounds that collapsed, and cites `docs/stock-hard-losses.md` (FORKRM-02)"
    - "`.planning/PROJECT.md`'s `### Out of Scope` fork-backend bullet no longer reads as 'retained as the default hedge' and still cites `FORK-01` (FORKRM-02)"
    - "`docs/stock-hard-losses.md` exists and records SID read-back, matrix keyboard and RESTORE/NMI as ACCEPTED, dated, each with the hardware-level reason migrated verbatim from `capability-registry.ts`'s six `hardware`-category entries (FORKRM-03)"
    - "`src/mcp/vice/docs-fork-decision.test.ts` still runs and still pins the FORK-01 row — rewritten to assert the reversal, never deleted (FORKRM-02)"
    - "`cd src/mcp/vice && npm run test:automated` failure SET is unchanged from the pre-plan floor after this plan lands"
  artifacts:
    - docs/stock-hard-losses.md
    - src/mcp/vice/docs-fork-decision.test.ts
    - .planning/PROJECT.md
  key_links:
    - "`docs-fork-decision.test.ts` → `.planning/PROJECT.md`'s `## Key Decisions` FORK-01 row (read at test time, byte-exact substring assertions)"
    - "`docs-fork-decision.test.ts` → `docs/stock-hard-losses.md` (new existence + non-vacuity assertion; the acceptance record must be reachable from the decision, not a floating file)"
    - "`.planning/PROJECT.md` `### Out of Scope` fork bullet → the FORK-01 row (cross-citation, asserted by test 6)"
  prohibitions:
    - statement: "`docs-fork-decision.test.ts` is NOT deleted, and its non-vacuity self-checks (named phrase set asserted non-empty, ≥20 Key Decisions data rows) are NOT weakened or removed"
      status: required
      verification: flagged-unverified
    - statement: "`.planning/milestones/v0.4.0-REQUIREMENTS.md` is NOT edited — it is an archived historical snapshot, not a live record"
      status: required
      verification: flagged-unverified
    - statement: "No source file under `src/mcp/vice/` other than `docs-fork-decision.test.ts` is touched by this plan"
      status: required
      verification: flagged-unverified
    - statement: "`docs/stock-hard-losses.md` contains no `.planning/*` path, no phase number, and no bare `D-NN`/`G-NN-N` id — it is a `docs/`-rooted product file that shipped skill text and README will cite"
      status: required
      verification: flagged-unverified
---

<objective>
Land the decision reversal and its guard together, as one independently-green vertical cut, before any code is deleted.

This is the phase's **tracer**: the thinnest complete path through every layer this phase touches — the decision record (`.planning/PROJECT.md`), the permanent-acceptance record that gives the reversal its cost (`docs/stock-hard-losses.md`), and the committed guard that pins both (`docs-fork-decision.test.ts`) — wired end to end and verified green. `docs-fork-decision.test.ts` reds on ANY edit to the `FORK-01` row, so record and guard cannot land separately; a code-first phase would sit red throughout and give no signal about its own deletions.

Purpose: every later plan in this phase deletes code against a green baseline, and every later plan that rewrites shipped prose needs one canonical `docs/`-rooted location to point a reader at.
Output: `docs/stock-hard-losses.md` (new), an amended `FORK-01` Key Decisions row and `### Out of Scope` bullet, and a rewritten (not deleted) `docs-fork-decision.test.ts`.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/52-remove-the-fork-backend/52-RESEARCH.md
@.planning/notes/fork-removal-reversal-basis.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint: confirm the FORK-01 reversal before any record or code changes</name>
  <files>none — decision gate; no file is written by this task</files>
  <decision>Reverse FORK-01, the 2026-08-22 human blocking-checkpoint decision to RETAIN the barryw/vice-mcp fork backend, and accept SID read-back, matrix keyboard and RESTORE/NMI as permanent losses rather than hedged ones?</decision>
  <options>
    A) Proceed with the reversal. Plans 52-01 through 52-10 run as written: the decision record is amended, the acceptance record is created, and roughly 2,500 lines across 30+ files are deleted in later waves.
    B) Halt the phase and keep the fork backend. No later plan in this phase is valid without the reversal, so execution stops here rather than partially applying it.
  </options>
  <resume-signal>The user replies A or B. Any other reply is treated as not-yet-answered; ask again rather than assuming A.</resume-signal>
  <read_first>
    - `.planning/notes/fork-removal-reversal-basis.md` in full — the owner scope call of 2026-09-11 and the two grounds that collapsed.
    - `.planning/PROJECT.md` line 711 (the `FORK-01` Key Decisions row as it stands) and line 398 (the v0.8.0 close audit sentence that is ground 1).
    - `.planning/ROADMAP.md` `### Phase 52` — the goal and the seven success criteria this reversal authorises.
  </read_first>
  <action>
PAUSE and put the decision to the user before writing anything.

**The decision:** `FORK-01` was decided **retain** by a human at a blocking checkpoint on 2026-08-22. This phase reverses it. Everything downstream — nine further plans deleting roughly 2,500 lines across 30+ files, a deleted transport, a deleted manifest, a deleted security control and two rewritten project-instruction constraints — is predicated on this one row changing.

**Why it is a one-way door:** the row edit itself reverts cleanly, but nothing after it does. Once the fork transport, its manifest and its probe are deleted, re-deciding `retain` is not a document edit — it is a restoration of a backend the owner reports never worked, against an external fork this repository does not own.

**The basis, stated so the user can disagree with it:** ground 1 — the hedge was never exercised, recorded in writing by the project's own v0.8.0 close audit; ground 2 — the near-zero-carrying-cost premise is withdrawn by the owner's direct experience. Ground 1 is a measured record, ground 2 is an owner statement.

**What acceptance costs:** SID read-back, matrix keyboard and RESTORE/NMI become permanently accepted losses rather than hedged ones. Plan 52-01 Task 1 records exactly that in `docs/stock-hard-losses.md`.

Present: (a) proceed with the reversal as planned; (b) halt the phase and keep the fork backend. Do not write any file until the user answers. If the answer is (b), stop the phase — no later plan in this phase is valid without this decision.
  </action>
  <acceptance_criteria>
    - The user's answer is recorded verbatim in the plan SUMMARY, with its date.
    - No file was modified before the answer was received.
    - If the answer is (b), execution halts and no subsequent task in this phase runs.
  </acceptance_criteria>
  <verify>
    <human-check>The user has explicitly confirmed the reversal of the 2026-08-22 `retain` decision, understanding that the three hard losses become permanently accepted.</human-check>
  </verify>
  <done>The user has answered; the answer and its date are recorded; no file was written before the answer.</done>
</task>

<task type="tracer">
  <name>Task 1: End-to-end "the fork decision is reversed and still pinned" — record, cost and guard in one cut</name>
  <files>docs/stock-hard-losses.md, .planning/PROJECT.md, src/mcp/vice/docs-fork-decision.test.ts</files>
  <read_first>
    - `.planning/PROJECT.md` lines 430-445 (the `### Out of Scope` fork-backend bullet at :438) and lines 705-715 (the single `FORK-01` Key Decisions row at :711). Read both in full before editing — the row is one very long table cell.
    - `src/mcp/vice/docs-fork-decision.test.ts` in full (181 lines) — all six assertions, the `REVERSAL_PHRASES` const, `keyDecisionsSection()`, `keyDecisionsDataRows()`, `findForkRow()`, `findForkBackendBullet()`.
    - `src/mcp/vice/capability-registry.ts` lines 85-140 (the `KEYBOARD_ALTERNATIVE` const and the start of `CAPABILITY_REGISTRY`) and the six entries whose `category` is `"hardware"`: `vice_sid_get_state`, `vice_keyboard_matrix`, `vice_keyboard_restore`, `vice_keyboard_chord`, `vice_keyboard_key_press`, `vice_keyboard_key_release`. Their `reason` and `alternative` strings are the source text for the new doc — copy them, do not re-derive them.
    - `.planning/notes/fork-removal-reversal-basis.md` in full — the two collapsed grounds and the owner scope call.
    - `.planning/PROJECT.md` lines 430-440 for the existing `### Out of Scope` bullet STYLE (dated, names the technical basis, states a reversal trigger) — `docs/stock-hard-losses.md` matches that shape.
  </read_first>
  <action>
Create `docs/stock-hard-losses.md` and amend `.planning/PROJECT.md`, then rewrite `src/mcp/vice/docs-fork-decision.test.ts` — all three in this one task, because the guard reds the instant the row changes.

**(a) `docs/stock-hard-losses.md` (new).** A permanent-acceptance record, dated with the execution date, one section per loss: `## SID read-back`, `## Matrix keyboard`, `## RESTORE / NMI`. Each section states the loss as ACCEPTED (not hedged, not deferred), gives the hardware-level reason **migrated verbatim** from the matching `capability-registry.ts` `"hardware"` entry's `reason` field, names the affected tool names (`vice_sid_get_state`; `vice_keyboard_matrix`, `vice_keyboard_chord`, `vice_keyboard_key_press`, `vice_keyboard_key_release`; `vice_keyboard_restore`), and — where the registry carries an `alternative` (the `KEYBOARD_ALTERNATIVE` text) — states the alternative in the same breath. Open the file with a short preamble naming the basis: the fork backend was removed on the reversal date, so these three are permanent properties of the only backend rather than gaps a second backend covered; the v0.8.0 close already recorded that the hedge was never exercised. Per FORKRM-03 this file is what a reader hits before asking why a capability is missing, so state what is unavailable and why, never merely that a tool is absent. Planning vocabulary is forbidden here — no `.planning/*` path, no phase number, no bare decision id; this file ships as a `docs/`-rooted citation target for README and skill prose.

**(b) `.planning/PROJECT.md`'s `FORK-01` Key Decisions row (:711).** Rewrite the row in place, keeping it a single table row so test 2's "exactly one row" holds. The rewritten row must state: the decision is **REVERSED** as of the execution date (ISO `YYYY-MM-DD`); the original 2026-08-22 `retain` verdict and that it was taken by a human at a blocking checkpoint; the two grounds that collapsed — the hedge was never exercised (the project's own v0.8.0 close audit already recorded this in writing) and the near-zero-carrying-cost premise was withdrawn by the owner, who reports the fork never worked; that the `KEYBOARD_MATRIX_SET` upstream reversal trigger is now **moot** (a landed opcode would close a stock gap directly, it would not restore a removed backend) — the literal upper-case string `KEYBOARD_MATRIX_SET` must appear in the row, test 4 is case-sensitive; that the three hard losses are now accepted, citing `docs/stock-hard-losses.md` by that exact path; and a stated re-reversal condition in the guard's own vocabulary (use the literal phrase `reversal criteria` or `reverses if`, or whichever phrase set the rewritten test 5 names — see (d)) saying the reversal reverses only on an explicit owner decision.

**(c) `.planning/PROJECT.md`'s `### Out of Scope` fork-backend bullet (:438).** The bullet's bolded subject must still match `/^-\s+\*\*[^*]*fork backend[^*]*\*\*/` (test 6 isolates it that way) and must still contain the literal `FORK-01`. Rewrite its body: the fork backend has been removed as of the reversal date; the hedge rationale is withdrawn; what is now out of scope is *re-adding* a second backend. Keep the pointer to the Key Decisions row as the fuller record, and add the `docs/stock-hard-losses.md` pointer.

**(d) `src/mcp/vice/docs-fork-decision.test.ts` — rewrite, do not delete.** Keep the file's structure and its own "WHY THIS EXISTS" header discipline, updating the header to narrate the reversal (the row it pins is now a reversal record, and the drift it guards against is a reader concluding the fork is still supported). Keep tests 1, 2, 3, 4 and 6 structurally identical — the ≥20-row non-vacuity floor, "exactly one FORK-01 row", the ISO-date match, the case-sensitive `KEYBOARD_MATRIX_SET` literal, and the Out-of-Scope cross-citation are all still the right invariants. Repoint test 5: replace the `REVERSAL_PHRASES` const with a renamed, still-named, still-non-empty set covering the reversal vocabulary the row actually uses, and keep test 1's assertion that the set is non-empty so an emptied set cannot make test 5 vacuous. Add one new test (test 7): the row cites `docs/stock-hard-losses.md`, that file exists at the repo root relative path, and it is non-vacuous — assert it contains all three of `SID`, `RESTORE` and the literal `KEYBOARD_MATRIX_SET`-family tool name `vice_keyboard_matrix`, and that its byte length clears a floor (use `> 1000`), so an emptied stub cannot pass.

Add nothing to `package.json`'s `files[]` — this test verifies planning-facing documentation and is deliberately outside the tarball, exactly as its existing header states.
  </action>
  <reversibility rating="one-way">Reverses a `retain` decision a human took at a blocking checkpoint on 2026-08-22. Re-deciding `retain` after the code is deleted is not a document edit — it is a restoration of ~2,500 deleted lines across 30+ files. Everything downstream in this phase is predicated on this row.</reversibility>
  <acceptance_criteria>
    - `git ls-files -- docs/stock-hard-losses.md` is non-empty after the commit (the file is tracked, not scratch).
    - `grep -ac 'REVERSED' .planning/PROJECT.md` is ≥ 1 and the hit is inside the FORK-01 row.
    - The FORK-01 row simultaneously contains: an ISO `YYYY-MM-DD` date, the literal upper-case `KEYBOARD_MATRIX_SET`, the literal `docs/stock-hard-losses.md`, and at least one member of the rewritten test's named phrase set.
    - `docs/stock-hard-losses.md` contains zero matches for `.planning/`, zero matches for `Phase 52`, and zero bare `D-` decision ids.
    - `docs-fork-decision.test.ts` still declares 7 `test(` blocks (6 repointed + 1 new) and still exports no runtime symbol into `package.json`'s `files[]`.
    - The whole automated suite's failure SET is identical to the pre-task baseline captured in the same shell.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-fork-decision.test.ts; echo "EXIT=$?"</automated>
    <fails_when>`EXIT=` is any value other than 0, or the TAP output contains any line beginning `not ok`, or the summary reports fewer than 7 `# pass` + `# fail` tests combined (a silently-dropped assertion).</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac 'REVERSED' .planning/PROJECT.md; grep -ac 'docs/stock-hard-losses.md' .planning/PROJECT.md; grep -ac '\.planning/' docs/stock-hard-losses.md; echo "done"</automated>
    <fails_when>Either of the first two counts is `0` (the reversal or its cost pointer never landed in PROJECT.md), or the third count is anything other than `0` (planning vocabulary leaked into a shipped `docs/` file). Note the polarity: for the third grep a non-zero COUNT is the failure and `grep` exiting 1 on no-match is the success.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated > /tmp/floor-52-01.log 2>&1; echo "EXIT=$?"; grep -a '^not ok' /tmp/floor-52-01.log | sed 's/[0-9]\+ - //' | sort</automated>
    <fails_when>The sorted `not ok` SET differs from the pre-task baseline set captured the same way — a new member is a regression even if the total count went down, and a disappeared member is a signal to re-check, not to celebrate. The documented floor is 3 (`anno-import.test.ts` ×1, `anno-register.test.ts` ×2); `audit-root-args.test.ts` and `text-protocol.test.ts` members are the two known flakes and are not regressions. Do NOT pipe this command into `tail` — the pipe's exit code would mask a real failure.</fails_when>
  </verify>
  <done>`docs/stock-hard-losses.md` exists and records the three losses as accepted with their verbatim hardware reasons; the `FORK-01` row and the `### Out of Scope` bullet state the reversal with its date, its two grounds and its cost pointer; `docs-fork-decision.test.ts` is rewritten (not deleted) and green; the suite's failure set is unchanged from the pre-task floor.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the three remaining live `FORK-01 = retain` readings in PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - `.planning/PROJECT.md` lines 183-189 (the citation at :186), lines 246-252 (:251), lines 395-400 (:398), lines 1754-1762 (:1758), lines 1770-1778 (:1774). Read each in its surrounding paragraph so historical records are distinguished from present-tense claims.
    - The `FORK-01` row as amended by Task 1 — the wording there is the canonical statement these citations must not contradict.
  </read_first>
  <action>
Sweep the five non-row `FORK-01` citations in `.planning/PROJECT.md` and settle each by decision, not by blanket edit.

`:251` and `:398` are **dated milestone-close records** (v0.4.0 and v0.8.0 respectively) and stay verbatim — a historical record correctly describing what was true at its own date is not drift. `:398` in particular is load-bearing evidence for the reversal (it is ground 1) and must not be softened.

`:186` cites the FORK-01 row only as an example of a manually-tracked trigger. Re-read it against the amended row: if the amended row still states its own re-reversal condition (it does, per Task 1), this citation remains true and stays unchanged. Record in the SUMMARY that it was checked and deliberately left.

`:1758` reads present-tense — *"The first is coupled to `FORK-01` — if it lands upstream, one of the three reasons to keep the fork backend disappears."* Append a dated reversal note to that numbered item stating that as of the reversal date the fork backend is removed, so a landed upstream `KEYBOARD_MATRIX_SET` opcode would close a stock gap for everyone rather than affecting a retain/remove choice that no longer exists. Do not delete the upstream-contribution item itself — it is still a real, still-unlanded contribution.

`:1774` — the `### Fork Backend Follow-on (3, FORK-01 = retain)` line inside the v0.4.0-close follow-on inventory. This is a historical inventory, so keep the `= retain` reading of what v0.4.0 recorded and append a dated superseded-by note naming the reversal date, so a reader scanning the inventory cannot mistake it for the current disposition.

Do **not** touch `.planning/milestones/v0.4.0-REQUIREMENTS.md` or any file under `.planning/phases/` — archived snapshots are out of scope by construction.
  </action>
  <reversibility rating="reversible">Prose annotations on a planning document; a git revert restores them exactly.</reversibility>
  <acceptance_criteria>
    - `grep -ac 'FORK-01' .planning/PROJECT.md` still returns the same count as before this task plus zero or more (citations are annotated, never deleted).
    - The lines at (formerly) `:1758` and `:1774` each contain the reversal's ISO date.
    - `git diff --name-only` after this task lists exactly `.planning/PROJECT.md` and nothing under `.planning/milestones/` or `.planning/phases/`.
    - `docs-fork-decision.test.ts` is still green — test 2's "exactly one FORK-01 row" must not have been broken by an annotation that accidentally starts with `| `.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-fork-decision.test.ts; echo "EXIT=$?"</automated>
    <fails_when>`EXIT=` is non-zero, or any `not ok` line appears — in particular a test-2 failure reading "found 2" means an annotation was written as a new table row instead of inline prose.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && git diff --name-only -- .planning/milestones .planning/phases | wc -l</automated>
    <fails_when>Output is anything other than `0` — an archived snapshot was edited, which this plan's prohibitions forbid.</fails_when>
  </verify>
  <done>Every live `FORK-01` citation in `.planning/PROJECT.md` either states the reversal or is an explicitly dated historical record carrying a superseded-by note; no archived milestone or phase artifact was touched.</done>
</task>

</tasks>

<assumption_delta_decision>
- **Noun now primary:** `backend` — and the decision is that it ceases to be a noun in this project's model at all. There is one emulator transport (stock VICE's binary monitor plus its text channel), so "which backend" stops being a question a caller, a manifest, a tool definition or a document can ask.
- **Decision:** `promote`. Stock is promoted from "one of two backends" to *the* transport; the fork is demoted out of the model entirely rather than retained as a variant. This matches ROADMAP success criterion 1 verbatim — *"One backend means no backend selection."*
- **Rationale:** this phase is a plural→singular collapse, the inverse of the case the assumption-delta checkpoint usually catches. The detector fired on a single `pluralization` cue (the word "also" in a prose sentence about skill duplication) and is a false positive on its own terms; the checkpoint is recorded rather than skipped because the contract says to act on `detected`, and the honest answer happens to be the fragment's own recommended move.
- **Companion invariant test:** already required by ROADMAP criterion 2 — the rewritten `src/mcp/vice/docs-fork-decision.test.ts` (this plan, Task 1) goes red the instant a future edit reintroduces the two-backend assumption into the decision record. A second invariant test is NOT added; plan 52-10 adds the complementary source-level fork-absence gate.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → plugin consumer | `docs/stock-hard-losses.md` ships to every reader who installs the plugin; it becomes the authoritative statement of what the tool cannot do |
| decision record → future maintainer | `.planning/PROJECT.md`'s Key Decisions row is the only durable record of *why* a backend was removed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-01 | Repudiation | `.planning/PROJECT.md` `FORK-01` row | high | mitigate | The reversal of a human blocking-checkpoint decision is recorded with its ISO date, both collapsed grounds, and the owner's scope call — and pinned by the rewritten `docs-fork-decision.test.ts`, so a later silent edit fails CI rather than erasing the trail |
| T-52-02 | Information Disclosure | `docs/stock-hard-losses.md` | medium | mitigate | A reader must not be told a capability is merely "not implemented yet" when it is unrecoverable in hardware; the reasons are migrated verbatim from the measured registry text rather than re-summarised, and test 7 asserts the file is non-vacuous |
| T-52-03 | Tampering | `docs-fork-decision.test.ts` | high | mitigate | A guard rewritten into vacuity is worse than a deleted one; the ≥20-row floor, the non-empty named phrase set, and the new ≥1000-byte acceptance-record floor are all retained/added as explicit non-vacuity assertions |
| T-52-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package and adds no dependency; `.planning/config.json`'s search providers are all `false`. No package-legitimacy audit is required because there is no install task |
</threat_model>

## Artifacts this phase produces

**Created by this plan**
- `docs/stock-hard-losses.md` — new `docs/`-rooted permanent-acceptance record for SID read-back, matrix keyboard and RESTORE/NMI. This is the canonical citation target every later plan (README, skills, the rewritten honesty gate) points at.
- `docs-fork-decision.test.ts` test 7 — new assertion binding the FORK-01 row to the acceptance record's existence and non-vacuity.
- A renamed, non-empty reversal-phrase set inside `docs-fork-decision.test.ts` (replacing `REVERSAL_PHRASES`).

**Removed / superseded by this plan**
- The `retain` reading of `.planning/PROJECT.md`'s `FORK-01` Key Decisions row (rewritten in place, row not removed).
- The hedge rationale in `.planning/PROJECT.md`'s `### Out of Scope` fork-backend bullet (rewritten in place, bullet not removed).
- `docs-fork-decision.test.ts`'s `REVERSAL_PHRASES` const name and test 5's retain-era phrase set (repointed, not dropped).

**Explicitly NOT touched by this plan**
- `.planning/milestones/v0.4.0-REQUIREMENTS.md`, anything under `.planning/phases/` — archived snapshots.
- Any file under `src/mcp/vice/` other than `docs-fork-decision.test.ts`.
- `CLAUDE.md` — its two falsified constraint bullets are plan 52-09's deliverable, because `docs-constraints-sync.test.ts` requires CLAUDE.md and `.planning/PROJECT.md`'s `## Constraints` list to stay byte-identical and they must move together.

<verification>
The rewritten `docs-fork-decision.test.ts` runs green; `docs/stock-hard-losses.md` is tracked, non-vacuous and free of planning vocabulary; the automated suite's failure SET is identical to the pre-plan floor.
</verification>

<success_criteria>
- ROADMAP criterion 2 is satisfiable from this plan alone: the `FORK-01` row and the `### Out of Scope` bullet state the reversal with its date and basis, and the guard was rewritten rather than deleted.
- ROADMAP criterion 3's record EXISTS (its propagation into skill text is plan 52-08's work; its README pointer is plan 52-09's).
- Every later plan in this phase now has a green baseline to delete against.
</success_criteria>

<output>
Create `.planning/phases/52-remove-the-fork-backend/52-01-SUMMARY.md` when done. Record in it: the exact reversal date written into the row, the final name of the renamed phrase set (later plans do not need it, but the SUMMARY is the record), and the fact that `.planning/PROJECT.md:186` was checked and deliberately left unchanged.
</output>
