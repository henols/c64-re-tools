---
phase: 52-remove-the-fork-backend
plan: 04
type: execute
wave: 3
depends_on: ["52-03"]
files_modified:
  - src/mcp/vice/vice-proxy.ts
  - src/mcp/vice/stock-derived.ts
  - src/mcp/vice/stock-paths.ts
  - src/mcp/vice/vice-probe.ts
  - src/mcp/vice/vice-probe.test.ts
  - src/mcp/vice/docs-linerefs.test.ts
  - src/mcp/vice/package.json
  - CLAUDE.md
  - .planning/PROJECT.md
autonomous: false
requirements: [FORKRM-01]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`buildBackendAwareTool()` is gone from `vice-proxy.ts` and its three call sites register stock dispatch directly through `buildViceTool()` (FORKRM-01)"
    - "`forwardToVice()`, `gatherWedgeEvidence()`, `gatherCheckpointTrapEvidence()`, `gatherBracketEvidence()` and the fork arms of `handleRecycle()`/`handleDiagnose()` are deleted from `vice-proxy.ts` (FORKRM-01)"
    - "`rewriteArguments()` and every helper that exists only to serve it are deleted from `vice-proxy.ts` (FORKRM-01)"
    - "`src/mcp/vice/vice-probe.ts` and `src/mcp/vice/vice-probe.test.ts` are deleted and the `vice-probe.ts` entry is removed from `package.json`'s `files[]` (FORKRM-01)"
    - "The `rewriteArguments()`/`forwardToVice()` Architecture constraint is removed from BOTH `CLAUDE.md` and `.planning/PROJECT.md`'s `## Constraints` list, byte-identically, and `docs-linerefs.test.ts` is deleted because the invariant it pins no longer exists"
    - statement: "Stock's own path translation is untouched — `stock-paths.ts`'s `withEmulatorSidePath()` and `STOCK_EMULATOR_SIDE_PATH_TOOLS` still translate emulator-side paths, and `hostpath.ts`/`containerpath.ts` are unchanged"
      verification: backstop
  artifacts:
    - src/mcp/vice/vice-proxy.ts
    - CLAUDE.md
    - .planning/PROJECT.md
  key_links:
    - "`buildViceTool()` (`vice-proxy.ts:3300`) to `stockDispatch.dispatchStock(...)` — the surviving registration path, which every advertised tool now uses"
    - "`CLAUDE.md` `## Constraints` list to `.planning/PROJECT.md` `## Constraints` (line 633) — `docs-constraints-sync.test.ts` asserts the two copies are BYTE-IDENTICAL, so both must change in one commit"
    - "`docs-linerefs.test.ts` `SCANNED_DOCS` to the `rewriteArguments()` Architecture bullet in both scanned documents — the guard's whole subject disappears with the bullet"
  prohibitions:
    - statement: "`stock-paths.ts`'s `withEmulatorSidePath()` and `STOCK_EMULATOR_SIDE_PATH_TOOLS` are NOT merged with, refactored into, or replaced by anything from the deleted fork-side translator"
      status: required
      verification: flagged-unverified
    - statement: "`hostpath.ts` and `containerpath.ts` are not modified — only the fork-side caller dies, the seam is load-bearing for stock"
      status: required
      verification: flagged-unverified
    - statement: "No new single-entry-point generic dispatcher is introduced to replace `forwardToVice()` — there is no surviving generic-dispatch surface, and creating one would re-open the nested-argument hazard `DENY_LIST` existed to close"
      status: required
      verification: flagged-unverified
    - statement: "The `anno_*` family's proxy-local registration through `buildViceTool()` is unchanged"
      status: required
      verification: flagged-unverified
    - statement: "`docs-linerefs.test.ts` is NOT left in the tree with an emptied or narrowed `SCANNED_DOCS` — a citation guard with nothing to cite fails its own non-vacuity assertion (`SCANNED_DOCS.length >= 2`), so it is deleted outright"
      status: required
      verification: flagged-unverified
---

<objective>
Delete the large fork-only region inside `vice-proxy.ts`, together with the documentation constraint and the guard whose premise it falsifies.

This region is named in no ROADMAP note. Once `buildBackendAwareTool()`'s fork arm goes, `forwardToVice()` (from `vice-proxy.ts:3035`), the three evidence gatherers (`gatherWedgeEvidence()` from `:1505`, `gatherCheckpointTrapEvidence()` from `:1117`, `gatherBracketEvidence()` from `:1298`), the fork arms of `handleRecycle()` (`:800`) and `handleDiagnose()` (`:1385`), and `rewriteArguments()` (`:2013`) are all unreachable. `vice-probe.ts` (278 lines) goes with them — its only non-test consumer is a call inside `forwardToVice()`.

**This is the one case where a `docs-linerefs.test.ts` mismatch is NOT drift.** `CLAUDE.md` normally says to treat a line-number mismatch as drift to re-verify and never as evidence the constraint changed. That instruction exists because these citations drifted twice before. Here both cited call sites die, so the hazard the bullet describes — a derived tool sitting behind `call()` receiving host-translated paths — cannot occur: `call()` and `forwardToVice()` are both gone. The bullet is obsolete, not mis-numbered.

Purpose: remove the biggest undisclosed block of fork-only code, and retire the documentation and guard that describe a hazard that ceases to exist.
Output: a `vice-proxy.ts` with one dispatch path; `vice-probe.ts` gone; the Architecture constraint and `docs-linerefs.test.ts` retired by decision.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-remove-the-fork-backend/52-RESEARCH.md
@.planning/phases/52-remove-the-fork-backend/52-PATTERNS.md
@src/mcp/vice/stock-paths.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint: confirm deleting the vice-proxy fork region and retiring its Architecture constraint</name>
  <files>none — decision gate; no file is written by this task</files>
  <decision>Delete the fork-only region of vice-proxy.ts (forwardToVice, the three evidence gatherers, rewriteArguments and its helpers, the two handler fork arms, vice-probe.ts), AND retire the rewriteArguments/forwardToVice Architecture constraint together with docs-linerefs.test.ts — deliberately treating a line-citation mismatch as obsolescence rather than as the drift CLAUDE.md normally says it is?</decision>
  <options>
    A) Proceed with both the code deletion and the constraint/guard retirement, as Tasks 1-3 describe.
    B) Proceed with the code deletion only; keep the Architecture constraint and docs-linerefs.test.ts. This leaves the suite red and leaves a project-instruction constraint describing code that does not exist — skip Task 3 and record the red guard by name.
    C) Halt. Do not delete the fork region.
  </options>
  <resume-signal>The user replies A, B or C. Any other reply is treated as not-yet-answered; ask again rather than assuming A.</resume-signal>
  <read_first>
    - `src/mcp/vice/vice-proxy.ts` — the regions this plan deletes, located by declaration text: `forwardToVice`, `gatherWedgeEvidence`, `gatherCheckpointTrapEvidence`, `gatherBracketEvidence`, `rewriteArguments`, and the fork arms of `handleRecycle`/`handleDiagnose`.
    - `CLAUDE.md` line 26 — the Architecture constraint and its four line citations.
    - `src/mcp/vice/stock-paths.ts` lines 1-30 — the independent confirmation that stock never calls `rewriteArguments()`.
  </read_first>
  <action>
PAUSE and put two coupled one-way decisions to the user before deleting anything.

**Decision A — delete roughly a thousand lines of fork-only proxy code** (`forwardToVice()`, three evidence gatherers, `rewriteArguments()` and its helpers, two handler fork arms, and `vice-probe.ts`). One-way: the external dependency this code speaks to is itself being removed from the project.

**Decision B — retire the `rewriteArguments()`/`forwardToVice()` Architecture constraint and delete `docs-linerefs.test.ts`.** This deliberately contradicts `CLAUDE.md`'s own standing instruction to treat a line-number mismatch as drift to re-verify and never as evidence the constraint changed. That instruction is right for every other case and wrong here: both cited call sites die with Decision A, so the hazard the bullet names cannot occur. The guard cannot be repointed the way `docs-fork-decision.test.ts` was, because its subject does not survive in any form and its own `SCANNED_DOCS.length >= 2` assertion forbids narrowing it to nothing.

Present: (a) proceed with both; (b) proceed with A but keep the constraint and the guard — note this leaves the suite red and the constraint describing code that does not exist; (c) halt. Do not delete anything until the user answers.
  </action>
  <acceptance_criteria>
    - The user's answer is recorded verbatim in the plan SUMMARY, with its date.
    - No file was modified before the answer was received.
    - If (b) is chosen, the plan's Task 3 is skipped and the resulting red guard is recorded by name in the SUMMARY rather than worked around.
  </acceptance_criteria>
  <verify>
    <human-check>The user has confirmed both the fork-region deletion and the deliberate retirement of a standing CLAUDE.md constraint, understanding that the second contradicts that file's own drift instruction for this one case.</human-check>
  </verify>
  <done>The user has answered; the answer and its date are recorded; nothing was deleted before the answer.</done>
</task>

<task type="auto">
  <name>Task 1: Collapse buildBackendAwareTool() into direct stock registration</name>
  <files>src/mcp/vice/vice-proxy.ts, src/mcp/vice/stock-derived.ts</files>
  <read_first>
    - `src/mcp/vice/vice-proxy.ts` lines 3290-3440 — `buildViceTool()` (`:3300`), the comment block at `:3338` explaining that the closed-over run function is chosen by `ACTIVE_BACKEND` once, `buildBackendAwareTool()` (`:3378`) including its `ACTIVE_BACKEND.backend === "fork" ? ... : ...` ternary at `:3379-3387`, the manifest registration loop (call site at `:3397`, and the `DENY_LIST` skip at `:3396`), and the two named call sites at `:3413` (`RECYCLE_TOOL`) and `:3414` (`DIAGNOSE_TOOL`).
    - `src/mcp/vice/stock-dispatch.ts` — `dispatchStock()`'s signature and `resolveAdvertisedToolDefinition()`'s, so the collapsed registration passes the right arguments.
    - `src/mcp/vice/stock-derived.ts` — it carries a doc-comment reference to `buildBackendAwareTool`; that comment needs updating, not just the code.
    - `src/mcp/vice/vice-proxy.ts` around `:3536` — the ready-log line's `ACTIVE_BACKEND.backend === "fork" ? ... : ...` ternary, which must collapse to the stock message.
  </read_first>
  <action>
Delete `buildBackendAwareTool()` at `vice-proxy.ts:3378` and collapse its three call sites.

The function's body is a single ternary on `ACTIVE_BACKEND.backend === "fork"`. One arm survives: the stock arm, which routes to `stockDispatch.dispatchStock(...)`. Replace each of the three call sites — the manifest registration loop at `:3397`, `RECYCLE_TOOL` at `:3413` and `DIAGNOSE_TOOL` at `:3414` — with a direct `buildViceTool(...)` call wired to the stock run function, preserving each site's existing tool-definition resolution (`stockDispatch.resolveAdvertisedToolDefinition(...)`) and its existing handler (`handleRecycle`, `handleDiagnose`). The `forkRun` parameter and every argument supplying it disappear.

Delete the `resolvedBinaryPath` / `resolvedBinaryPathIsResolved` fields the fork arm set at `:3384-3385` only if they are unreachable from the stock arm; read the stock arm first. `vice_ping`'s `resolvedBinaryPath` is served from `stock-dispatch.ts`'s `handlePing()`, so the proxy-side copies here may be fork-only — confirm before deleting, and record what you found.

Collapse the ready-log ternary near `:3536` to the stock message unconditionally.

Update the comment block at `:3338` (it explains a per-backend dispatch choice that no longer exists) and `stock-derived.ts`'s doc-comment reference to `buildBackendAwareTool`. Both are shipped files, so cite only `docs/`-rooted paths. Keep the *lesson* where one survives — the comment convention in this repo is to name what not to do and why — but do not leave a comment describing a function that no longer exists.

Leave `ACTIVE_BACKEND` itself, the `DENY_LIST` skip at `:3396`, and every `forwardToVice()` reference in place for now; Task 2 and plans 52-05/52-06 remove those. Typecheck after this task: `forwardToVice()` will now be unreferenced by the registration seam, which is exactly the signal Task 2 acts on.
  </action>
  <reversibility rating="costly">Removing the per-backend registration seam is the structural change the whole phase turns on. Restorable from git, but every later plan in this phase is written against its absence.</reversibility>
  <acceptance_criteria>
    - `grep -ac 'buildBackendAwareTool' src/mcp/vice/vice-proxy.ts` returns `0`.
    - `grep -ac 'buildBackendAwareTool' src/mcp/vice/stock-derived.ts` returns `0`.
    - `grep -ac 'backend === "fork"' src/mcp/vice/vice-proxy.ts` is strictly lower than before this task, and no remaining hit is inside the tool-registration seam.
    - `buildViceTool` is still declared once in `vice-proxy.ts` and is now the only registration helper.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck; echo "EXIT=$?"</automated>
    <fails_when>`EXIT=` is non-zero, or any `TS6133` (declared but never read) diagnostic names `forwardToVice` in a way the build treats as an error rather than a hint — a real type error here means the stock arm was mis-wired.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac 'buildBackendAwareTool' src/mcp/vice/vice-proxy.ts; grep -ac 'buildBackendAwareTool' src/mcp/vice/stock-derived.ts; grep -ac 'buildViceTool' src/mcp/vice/vice-proxy.ts</automated>
    <fails_when>Either of the first two counts is anything other than `0` — INVERTED polarity: a non-zero count is the failure and `grep` exiting 1 with `0` is the success. Or the third count is `0`, meaning the surviving registration helper was deleted by mistake.</fails_when>
  </verify>
  <done>Every advertised tool registers through `buildViceTool()` calling stock dispatch directly; `buildBackendAwareTool` appears nowhere in the tree; typecheck is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Delete forwardToVice(), the three evidence gatherers, the fork arms of recycle/diagnose, rewriteArguments() and vice-probe.ts</name>
  <files>src/mcp/vice/vice-proxy.ts, src/mcp/vice/vice-probe.ts, src/mcp/vice/vice-probe.test.ts, src/mcp/vice/stock-paths.ts, src/mcp/vice/package.json</files>
  <read_first>
    - `src/mcp/vice/vice-proxy.ts`: read each region before deleting it, in this order — `handleRecycle` (`:789-900`, note the deliberate `const ... = async function handleRecycle(args)` declaration form and the structural oracle at `:793` that keys off it), `gatherCheckpointTrapEvidence` (`:1117-1300`), `gatherBracketEvidence` (`:1298-1390`), `handleDiagnose` (`:1385-1505`), `gatherWedgeEvidence` (`:1505-1600`), `rewriteArguments` (`:2013-2120`), `forwardToVice` (`:3035-3140`). Line numbers are pre-Task-1 and will have shifted; locate by declaration text, never by number alone.
    - `src/mcp/vice/vice-proxy.ts` line 101 — the `import { probeInstance, ... } from "./vice-probe.ts"` statement, and the `probeInstance` call inside `forwardToVice()` around `:3062`.
    - `src/mcp/vice/stock-paths.ts` lines 1-30 — its header states, in its own words, that stock never calls `rewriteArguments()` and has its own parallel mechanism. This is the independent confirmation that `rewriteArguments()` is fork-only. Read it so the deletion is grounded, not assumed.
    - `src/mcp/vice/stock-diagnose.ts` and `src/mcp/vice/stock-recycle.ts` — the stock counterparts of the two handlers whose fork arms are being removed. Confirm what survives before deleting what does not.
    - The output of `grep -rn 'rewriteArguments\|pathArgsFor\|rewritePathsIn\|PathResolution\|PathOutOfWorkspaceError\|PathTranslationError\|probeInstance\|forwardToVice\|gatherWedgeEvidence\|gatherCheckpointTrapEvidence\|gatherBracketEvidence' --include='*.ts' --include='*.mts' --include='*.mjs' src/ scripts/` — the real consumer set. Delete a helper only after confirming its remaining consumers are all inside the region being deleted.
    - `src/mcp/vice/package.json` `files[]` (the `vice-probe.ts` entry) and `scripts/check-npm-packages.mjs` lines 160-215.
  </read_first>
  <action>
Delete the fork-only region of `vice-proxy.ts` and the module it alone consumed.

**Order matters within this task**, because each deletion makes the next one provably unreferenced. Delete in this sequence, typechecking between steps:

1. `forwardToVice()` — now unreferenced after Task 1. Its deletion orphans the `probeInstance` import.
2. `gatherWedgeEvidence()`, `gatherCheckpointTrapEvidence()`, `gatherBracketEvidence()` — confirm each is called only from `handleRecycle`/`handleDiagnose`'s fork arms or from each other, then delete.
3. The fork arms of `handleRecycle()` and `handleDiagnose()`. These two handlers SURVIVE — they are registered by Task 1's collapsed sites — so this is an arm removal, not a function deletion. Preserve `handleRecycle`'s `const ... = async function handleRecycle(args)` declaration form exactly; a structural oracle at `vice-proxy.ts:793` does `indexOf("async function handleRecycle(args)")` and will red if the form changes.
4. `rewriteArguments()` and, individually, each helper that now has no remaining consumer: `pathArgsFor`, `rewritePathsIn`, `PathResolution`, `PathOutOfWorkspaceError`, `PathTranslationError`. Grep for each helper's remaining consumers BEFORE deleting it — `PathOutOfWorkspaceError` in particular has a plausible non-fork consumer in the container-path seam, so confirm rather than assume. Any helper with a surviving consumer stays.
5. The `import ... from "./vice-probe.ts"` statement at `:101`, then `src/mcp/vice/vice-probe.ts` and `src/mcp/vice/vice-probe.test.ts` as whole files, then the `"vice-probe.ts"` entry in `src/mcp/vice/package.json`'s `files[]`.

Where a deleted call site carried a `WHAT NOT TO DO` comment whose lesson survives its code — `stock-paths.ts:19-24`'s "Never call `rewriteArguments()` from a stock handler" is the named example — do not simply delete the comment. Rewrite it to state the surviving lesson in the abstract: stock's emulator-side path translation is its own mechanism and must not be replaced by, or merged with, a general argument-rewriting pass applied before dispatch. Naming a function that no longer exists would leave a dangling reference; dropping the lesson would discard the reason the mechanism is shaped the way it is.

Do NOT introduce any replacement for `forwardToVice()`. There is no surviving generic-dispatch surface, and building one would re-open the nested-argument hazard that `DENY_LIST` existed to close.
  </action>
  <reversibility rating="one-way">Deletes roughly a thousand lines of transport, evidence-gathering and path-rewriting logic whose only external dependency — the fork's HTTP MCP server — is itself being removed from the project. Restoring it would mean restoring a backend the owner has recorded as never having worked. Treat as one-way.</reversibility>
  <acceptance_criteria>
    - `grep -rac 'forwardToVice' src/mcp/vice/vice-proxy.ts` returns `0`.
    - `grep -rac 'rewriteArguments' src/mcp/vice/vice-proxy.ts` returns `0`, and `grep -ra 'rewriteArguments' src/mcp/vice/stock-paths.ts` finds no reference to it as a live function (only the rewritten abstract lesson, if any).
    - `grep -rac 'gatherWedgeEvidence\|gatherCheckpointTrapEvidence\|gatherBracketEvidence' src/mcp/vice/vice-proxy.ts` returns `0`.
    - `test ! -f src/mcp/vice/vice-probe.ts` and `test ! -f src/mcp/vice/vice-probe.test.ts` both succeed; `files[]` no longer lists `vice-probe.ts`.
    - `grep -ac 'async function handleRecycle(args)' src/mcp/vice/vice-proxy.ts` is still `1` — the declaration form the structural oracle keys off is intact.
    - `handleRecycle` and `handleDiagnose` are still declared and still registered.
    - `npm run typecheck` exits 0 and `node scripts/check-npm-packages.mjs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck; echo "EXIT=$?"</automated>
    <fails_when>`EXIT=` is non-zero. A `TS2307` naming `./vice-probe.ts` means the import survived the module deletion; a `TS2304` naming a deleted helper means something outside the region still consumed it and the pre-delete grep was skipped.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && for s in forwardToVice rewriteArguments gatherWedgeEvidence gatherCheckpointTrapEvidence gatherBracketEvidence probeInstance; do printf '%s=' "$s"; grep -ac "$s" src/mcp/vice/vice-proxy.ts; done; printf 'handleRecycleForm='; grep -ac 'async function handleRecycle(args)' src/mcp/vice/vice-proxy.ts</automated>
    <fails_when>Any of the six `<symbol>=` lines reports a count other than `0` — INVERTED polarity: for a deletion, a non-zero grep COUNT is the failure and `grep` exiting 1 on no-match is the success. Separately, `handleRecycleForm=` reporting anything other than `1` means the surviving handler's declaration form was changed and the structural oracle at `vice-proxy.ts:793` will red.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && test ! -f src/mcp/vice/vice-probe.ts; echo "PROBE_ABSENT_EXIT=$?"; node scripts/check-npm-packages.mjs; echo "exit=$?"</automated>
    <fails_when>`PROBE_ABSENT_EXIT=` is non-zero (note the inverted form: `test ! -f` exits 0 when the file is absent, which is success), or `exit=` is non-zero, or the output contains `check-npm-packages: FAIL`.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated > /tmp/floor-52-04.log 2>&1; echo "EXIT=$?"; grep -a '^not ok' /tmp/floor-52-04.log | sed 's/[0-9]\+ - //' | sort</automated>
    <fails_when>The sorted `not ok` SET gains a member absent from the pre-plan baseline set, OTHER than a `docs-linerefs.test.ts` member — that one is expected here and is resolved by Task 3 in this same plan. Compare the SET, never the count. Do not pipe into `tail`.</fails_when>
  </verify>
  <done>`vice-proxy.ts` has one dispatch path and no fork transport, evidence-gathering or argument-rewriting code; `vice-probe.ts` and its test are gone with their `files[]` entry; `handleRecycle`/`handleDiagnose` survive with their stock arms and `handleRecycle`'s declaration form intact; typecheck and the package check are green.</done>
</task>

<task type="auto">
  <name>Task 3: Retire the rewriteArguments()/forwardToVice() Architecture constraint and its line-reference guard</name>
  <files>CLAUDE.md, .planning/PROJECT.md, src/mcp/vice/docs-linerefs.test.ts</files>
  <read_first>
    - `CLAUDE.md` line 26 — the Architecture constraint bullet in full, including all four `vice-proxy.ts:<N>` / `:<N>` citations.
    - `.planning/PROJECT.md` `## Constraints` (heading at line 633) — the byte-identical copy of the same bullet.
    - `src/mcp/vice/docs-constraints-sync.test.ts` lines 1-60 and its eight `test(` blocks — it asserts the two Constraints lists are byte-identical, in the same order, and reports a bullet present in only one copy in BOTH directions. This is why the two edits cannot be split across plans.
    - `src/mcp/vice/docs-linerefs.test.ts` lines 40-100 (`SCANNED_DOCS`, `CITATION_RE`) and lines 249-440 (the five real checks) — enough to confirm the guard has no subject left once the bullet goes, and that its own `SCANNED_DOCS.length >= 2` non-vacuity assertion makes a narrowed-to-nothing version impossible.
    - The result of `grep -rn 'docs-linerefs' --include='*.ts' --include='*.mjs' --include='*.md' src/ scripts/ .github/` — confirm nothing else depends on the guard's existence before deleting it.
  </read_first>
  <action>
Remove the Architecture constraint from both copies of the Constraints list and delete `src/mcp/vice/docs-linerefs.test.ts`.

**The decision, recorded with its basis.** This guard exists because those citations drifted twice, and `CLAUDE.md` instructs a reader to treat a mismatch as drift to re-verify and never as evidence the constraint changed. That instruction is correct for every other case and wrong for this one: Task 2 deleted `rewriteArguments()` and both of its call sites, and the hazard the bullet names — a derived tool sitting behind `call()` receiving host-translated paths and acting on them inside the container — cannot occur once `call()` and `forwardToVice()` are both gone. The failure mode here is obsolescence, not drift. `docs-linerefs.test.ts` cannot be repointed the way `docs-fork-decision.test.ts` was, because `docs-fork-decision.test.ts`'s subject (the `FORK-01` row) survives in amended form while this guard's subject (a `rewriteArguments()` call to cite) does not exist at all. A guard narrowed to zero documents fails its own `SCANNED_DOCS.length >= 2` assertion, so "keep it but scope it down" is not available. Delete it.

**The edit.** Remove the Architecture bullet from `CLAUDE.md`'s Constraints list AND from `.planning/PROJECT.md`'s `## Constraints` list, in the same commit, producing byte-identical lists in the same order. `docs-constraints-sync.test.ts` reports a bullet present in only one copy in both directions, so an edit to one copy alone reds the suite.

Do not replace the removed bullet with a "this used to matter" note inside the Constraints list — a constraint list states what is true now. The obsolescence record belongs in the SUMMARY and, if a durable home is wanted, in `docs/stock-hard-losses.md`'s sibling prose is NOT the right place either; leave it in the SUMMARY.

Then delete `src/mcp/vice/docs-linerefs.test.ts` as a whole file. It is deliberately outside `package.json`'s `files[]` (it verifies planning-facing documentation), so no `files[]` edit is needed — confirm that before assuming it.

While in `CLAUDE.md`'s Constraints list, leave the Compatibility bullet (line 25) and the Testing bullet naming `vice-sync.ts` (line 49) ALONE. Both are also falsified by this phase, but plan 52-09 owns them — splitting them across plans would mean two different plans editing the same two byte-synced files.
  </action>
  <reversibility rating="costly">Removes a committed guard whose protection has no surviving subject. Restorable from git, but re-adding it later would require re-creating the code it cites, so in practice this is not coming back.</reversibility>
  <acceptance_criteria>
    - `grep -ac 'rewriteArguments' CLAUDE.md` returns `0` and `grep -ac 'rewriteArguments' .planning/PROJECT.md` returns `0` for the `## Constraints` section (historical narrative elsewhere in PROJECT.md that dates its own drift record may remain — state in the SUMMARY which occurrences were left and why).
    - `grep -ac 'vice-proxy.ts:' CLAUDE.md` returns `0`.
    - `test ! -f src/mcp/vice/docs-linerefs.test.ts` succeeds.
    - `node --test docs-constraints-sync.test.ts` exits 0 — the two Constraints lists are byte-identical after the edit.
    - `grep -ac 'docs-linerefs' src/mcp/vice/package.json` returns `0` (it was never in `files[]`, so this should already hold).
    - The automated suite's failure SET contains no `docs-linerefs.test.ts` member and no `docs-constraints-sync.test.ts` member.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-constraints-sync.test.ts; echo "EXIT=$?"</automated>
    <fails_when>`EXIT=` is non-zero, or any `not ok` line appears. A failure naming "a bullet present in only one copy" means only one of the two Constraints lists was edited; a failure naming "a reordering" means the bullet was moved rather than removed.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac 'rewriteArguments' CLAUDE.md; grep -ac 'vice-proxy.ts:' CLAUDE.md; test ! -f src/mcp/vice/docs-linerefs.test.ts; echo "GUARD_ABSENT_EXIT=$?"</automated>
    <fails_when>Either grep count is anything other than `0` — INVERTED polarity: a non-zero count is the failure and `grep` exiting 1 with `0` is the success. Or `GUARD_ABSENT_EXIT=` is non-zero, meaning `docs-linerefs.test.ts` still exists (`test ! -f` exits 0 when the file is absent, which is the passing case).</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated > /tmp/floor-52-04c.log 2>&1; echo "EXIT=$?"; grep -a '^not ok' /tmp/floor-52-04c.log | sed 's/[0-9]\+ - //' | sort</automated>
    <fails_when>The sorted `not ok` SET gains any member absent from the pre-plan baseline set. At this point the transient `docs-linerefs.test.ts` failure from Task 2 must be GONE (the file no longer exists); if it is still listed, the guard was not deleted. Compare the SET, never the count. Do not pipe into `tail`.</fails_when>
  </verify>
  <done>Both copies of the Constraints list have lost the Architecture bullet byte-identically, `docs-linerefs.test.ts` is deleted with its basis recorded, `docs-constraints-sync.test.ts` is green, and the suite's failure set is back to the pre-plan floor.</done>
</task>

</tasks>

<assumption_delta_decision>
Recorded once for this phase in plan `52-01`: primary noun `backend`, decision `promote`, companion invariant test already required by criterion 2. This plan removes a per-backend branch rather than adding a variant, which is the `promote` decision being carried out in code.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → tool dispatch | every `tools/call` crosses here; before this plan it could take one of two paths, after it takes one |
| container → host filesystem | argument path rewriting crossed this boundary; the fork-side rewriter is deleted, the stock-side one survives untouched |
| repo → session context | `CLAUDE.md` is loaded into every session; a constraint there is acted on as current fact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-10 | Elevation of Privilege | the deleted `forwardToVice()` generic-dispatch surface | high | mitigate | No replacement single-entry-point dispatcher is introduced. The nested-argument confused-deputy shape that `DENY_LIST` existed to close is removed by removing the surface, not by adding a new gate. This plan's prohibitions forbid re-creating one, and plan 52-10's grep gate makes the absence checkable |
| T-52-11 | Tampering | `stock-paths.ts` path translation | high | mitigate | `withEmulatorSidePath()` / `STOCK_EMULATOR_SIDE_PATH_TOOLS` and the `hostpath.ts`/`containerpath.ts` seam are named in this plan's prohibitions as untouched; only the fork-side caller dies. The surviving lesson is rewritten in the abstract rather than deleted with its code |
| T-52-12 | Information Disclosure | `CLAUDE.md` Architecture constraint | high | mitigate | A constraint describing a hazard that no longer exists misleads every future session into defending a boundary that is gone, while the real one (`stock-paths.ts`'s own mechanism) goes unnamed. The bullet is removed from both byte-synced copies in one commit, gated by `docs-constraints-sync.test.ts` |
| T-52-13 | Repudiation | `docs-linerefs.test.ts` deletion | medium | mitigate | The guard is deleted by a recorded decision with its basis (subject obsolete, not drifted; `SCANNED_DOCS.length >= 2` forbids a narrowed version), written into the SUMMARY rather than left as an unexplained removal |
| T-52-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan |
</threat_model>

## Artifacts this phase produces

**Created by this plan**
- Nothing new. This plan is entirely removal plus two comment rewrites.
- One rewritten abstract lesson comment in `stock-paths.ts` (and any sibling carrying the same "never call the fork-side rewriter" warning), restating the surviving rule without naming a deleted function.

**Removed by this plan**
- `vice-proxy.ts`: `buildBackendAwareTool()`, `forwardToVice()`, `gatherWedgeEvidence()`, `gatherCheckpointTrapEvidence()`, `gatherBracketEvidence()`, `rewriteArguments()` and its now-unreferenced helpers (`pathArgsFor`, `rewritePathsIn`, `PathResolution`, and — only if confirmed unreferenced — `PathOutOfWorkspaceError`, `PathTranslationError`), the fork arms of `handleRecycle()` and `handleDiagnose()`, the `./vice-probe.ts` import, and the backend ternary in the ready-log line.
- `src/mcp/vice/vice-probe.ts` (278 lines) and `src/mcp/vice/vice-probe.test.ts`, plus the `"vice-probe.ts"` entry in `package.json`'s `files[]`.
- `src/mcp/vice/docs-linerefs.test.ts` (584 lines) — whole file, by decision.
- The `rewriteArguments()`/`forwardToVice()` Architecture constraint bullet from BOTH `CLAUDE.md` and `.planning/PROJECT.md`'s `## Constraints` list, with all four line citations.
- `stock-derived.ts`'s doc-comment reference to `buildBackendAwareTool`.

**Explicitly NOT touched by this plan**
- `stock-paths.ts`'s `withEmulatorSidePath()` / `STOCK_EMULATOR_SIDE_PATH_TOOLS` behaviour; `hostpath.ts`; `containerpath.ts`.
- `anno-tools.ts` in any respect.
- `CLAUDE.md`'s Compatibility bullet and its `vice-sync.ts` Testing bullet — plan 52-09 owns both, because both copies must move together.
- `vice.ts`, `refresh-manifest.ts`, `tools-manifest.json`, `capability-registry.ts`, `backend-detect.mts` — later plans.

<verification>
`npm run typecheck` clean; `node scripts/check-npm-packages.mjs` exits 0; `node --test docs-constraints-sync.test.ts` green; the automated suite's failure SET equals the pre-plan floor with no `docs-linerefs` member remaining.
</verification>

<success_criteria>
- `vice-proxy.ts` has exactly one dispatch path and no fork-era transport, evidence or path-rewriting code.
- The documentation constraint and the guard that pinned it are retired by an explicit, recorded decision rather than renumbered.
- Stock's own path translation is demonstrably untouched.
</success_criteria>

<output>
Create `.planning/phases/52-remove-the-fork-backend/52-04-SUMMARY.md` when done. Record: which of `PathOutOfWorkspaceError` / `PathTranslationError` were found to have surviving consumers and were therefore kept; whether the `resolvedBinaryPath` proxy-side fields were fork-only or shared; the exact wording of the rewritten `stock-paths.ts` lesson comment; and the basis for deleting `docs-linerefs.test.ts` (subject obsolete, not drifted) so a later reader does not mistake it for a guard quietly switched off.
</output>
