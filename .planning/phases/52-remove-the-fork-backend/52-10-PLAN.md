---
phase: 52-remove-the-fork-backend
plan: 10
type: execute
wave: 8
depends_on: ["52-09"]
files_modified:
  - src/mcp/vice/docs-fork-absence.test.ts
  - src/mcp/vice/package.json
autonomous: false
requirements: [FORKRM-01, FORKRM-04, FORKRM-06, FORKRM-07]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A new committed guard `src/mcp/vice/docs-fork-absence.test.ts` goes red the instant fork vocabulary, a second manifest, a backend selector or a replacement denylist is reintroduced anywhere in the tree (FORKRM-01, FORKRM-04, FORKRM-06)"
    - "The guard is non-vacuous in both directions: it carries planted-violation cases proving each of its checks can fail, and a scanned-file-count floor proving its walk is not scanning nothing"
    - "`npm run test:automated` is green at the documented failure-set floor, measured with no live broker running (FORKRM-07)"
    - "`npm run typecheck`, `node scripts/check-npm-packages.mjs`, `node scripts/check-skill-capability-honesty.mjs`, `node scripts/check-skill-tool-coverage.mjs`, `node scripts/check-skill-cli-invocations.mjs` and `npm run smoke` all exit 0"
    - statement: "A human has read the nine rewritten skill sites and confirmed each states what is permanently unavailable and why, rather than merely omitting a tool — a grep can prove a phrase is absent but not that the replacement sentence is true and useful"
      verification: backstop
  artifacts:
    - src/mcp/vice/docs-fork-absence.test.ts
  key_links:
    - "`docs-fork-absence.test.ts` to `shipped-modules.ts` — the scanned set is derived from `package.json`'s `files[]` rather than a raw directory listing, matching the precedent `spawn-seam.test.ts` already set in this repo"
    - "`docs-fork-absence.test.ts` to `docs/stock-hard-losses.md` — the one file allowed to name the removed backend, and the one whose presence the guard requires"
  prohibitions:
    - statement: "The new guard is NOT added to `package.json`'s `files[]` — like the other `docs-*.test.ts` guards it verifies the repository, not runtime behaviour shipped in the tarball"
      status: required
      verification: flagged-unverified
    - statement: "No pre-existing floor failure (`anno-import.test.ts`, `anno-register.test.ts`) is 'fixed' as a side effect of this phase — they are unrelated stale requirement-id citations and fixing them here would hide whether this phase regressed anything"
      status: required
      verification: flagged-unverified
    - statement: "The guard does not scan `.planning/**` — that tree legitimately records the removal in detail and scanning it would produce false positives, after which the guard gets switched off rather than obeyed"
      status: required
      verification: flagged-unverified
    - statement: "The suite floor is reported as a failure SET, never as a count — a run with the same count and a different member is a regression"
      status: required
      verification: flagged-unverified
---

<objective>
Prove the removal stays removed, and close the phase at the documented floor.

This is the phase's closing gate. It does two things the earlier plans could not: it adds the mechanical proof that the deletion does not creep back — the house pattern this repo already established as "The Deletion and the Grep Gate", where every deletion wave is paired with a grep-based check that the deleted thing stays deleted — and it runs the full verification battery with the failure SET compared, not the count.

It also carries the phase's one human check. A grep can prove the phrase "requires the fork" is absent from the skills; it cannot prove the sentence that replaced it is true and useful to a reader. `workflow.human_verify_mode` is `end-of-phase`, so that check is a `<human-check>` inside this plan's verification rather than a blocking checkpoint task.

Purpose: land criterion 7 and make criteria 1, 4 and 6 regression-proof.
Output: `docs-fork-absence.test.ts` (new guard), a green battery, a recorded floor.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-remove-the-fork-backend/52-RESEARCH.md
@.planning/phases/52-remove-the-fork-backend/52-VALIDATION.md
@src/mcp/vice/docs-fork-decision.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs-fork-absence.test.ts, the guard that keeps the deletion deleted</name>
  <files>src/mcp/vice/docs-fork-absence.test.ts</files>
  <read_first>
    - `src/mcp/vice/docs-fork-decision.test.ts` as rewritten by plan 52-01 — the shape to follow: a `WHY THIS EXISTS` header naming the failure class in actionable terms, named non-empty constant sets asserted non-empty in a non-vacuity test, predicates that RETURN findings so planted-violation tests can drive the real rule, and an explicit statement that the file is kept out of `package.json`'s `files[]`.
    - `src/mcp/vice/docs-dangling-refs.test.ts` lines 1-60 — its scope reasoning, in particular why it deliberately excludes `.planning/phases/**` (historical records legitimately quote the wrong wording while describing its removal, and a guard that false-positives gets switched off rather than obeyed). Apply the same reasoning.
    - `src/mcp/vice/shipped-modules.ts` — `shippedTsModules()` / the `files[]`-derived module set, and why a raw directory listing is the wrong basis (it catches non-shipped files like `acme-gate.ts`).
    - `src/mcp/vice/spawn-seam.test.ts` as left by plan 52-06 — the two-directional set-equality pattern and the planted-violation discipline.
    - `docs/stock-hard-losses.md` — the one file that legitimately names the removed backend.
  </read_first>
  <action>
Create `src/mcp/vice/docs-fork-absence.test.ts`, a committed guard that goes red if the removal is undone.

Give it the project's guard header shape: a `WHY THIS EXISTS` paragraph naming the failure class in terms a reader can act on — a backend was removed, its transport and manifest deleted and its documentation rewritten; the way that comes back is not a deliberate restore but a single reintroduced conditional, a second manifest file, or a README sentence describing a switch — followed by a statement that this file verifies the repository rather than runtime behaviour and is deliberately kept out of `package.json`'s `files[]`.

Implement these checks, each with a named, non-empty constant set:

1. **No fork vocabulary in the shipped surface.** Walk the shipped TypeScript module set derived from `package.json`'s `files[]` via `shipped-modules.ts` (not a raw directory listing — the precedent `spawn-seam.test.ts` set), plus every `.md` under `src/skills/`, plus `README.md` and `CLAUDE.md`. Assert none contains `VICE_BACKEND`, `buildBackendAwareTool`, `probeBackend`, `DENY_LIST`, `denyListRefusalMessage`, `capabilityRefusalMessage`, `CAPABILITY_REGISTRY`, `forwardToVice` or `rewriteArguments`. Read every file with a NUL-tolerant read and match case-sensitively on these identifiers; a plain text read that stops at a NUL byte silently skips `anno-memmap-render.ts`, which has already produced one wrong decision in this repository.
2. **No second manifest.** Assert `src/mcp/vice/tools-manifest.stock.json` exists and that no sibling file matching `tools-manifest*.json` other than that one exists.
3. **No deleted module reappears.** Assert each of `vice.ts`, `vice-sync.ts`, `vice-probe.ts`, `refresh-manifest.ts`, `capability-registry.ts`, `fork-deleted-tools.ts` is absent from `src/mcp/vice/`, and that none appears in `package.json`'s `files[]`.
4. **The acceptance record exists and is reachable.** Assert `docs/stock-hard-losses.md` exists, is non-vacuous (a byte-length floor), names all three losses, and is cited by `README.md`. This is the one file allowed to name the removed backend — exempt it from check 1 explicitly and by path, not by a substring that could accidentally exempt others.
5. **Non-vacuity, in both directions.** Assert the scanned file count clears a floor (derive it from the real count and leave headroom), assert each forbidden-identifier set is non-empty, and add planted-violation tests that drive the REAL predicates against in-memory document bodies — a body containing `VICE_BACKEND` must be reported, a body without it must not, and an exempt-path body containing it must not. No fixture files, no filesystem writes, and no second implementation of the rule.

**Scope exclusion, stated in the file's own comment:** the guard does not scan `.planning/**`. That tree records this removal in detail and legitimately quotes every removed identifier while describing its removal; scanning it would produce false positives, and a guard that false-positives gets switched off rather than obeyed. Keeping the scope narrow keeps it trusted.

**Comment-text discipline:** do not write any forbidden identifier in this file's own prose comments in a form the walk would match — the file must not fail itself. If naming one is unavoidable for explanation, the file is its own exempt path; state that exemption explicitly in the code rather than relying on it accidentally.

Do NOT add this file to `package.json`'s `files[]`.
  </action>
  <reversibility rating="reversible">A new test file. If its checks prove too broad in practice they can be narrowed; nothing depends on it existing.</reversibility>
  <acceptance_criteria>
    - `src/mcp/vice/docs-fork-absence.test.ts` exists, is tracked by git, and is NOT in `package.json`'s `files[]`.
    - `node --test docs-fork-absence.test.ts` exits 0.
    - The file declares at least three planted-violation tests, and each drives the real predicate rather than a copy of the rule.
    - It asserts a scanned-file-count floor and a non-empty forbidden-identifier set, so it cannot pass by scanning nothing.
    - `node -e "const{automatedTestFiles}=await import('./test-gate.mjs');console.log(automatedTestFiles(process.cwd()).includes('docs-fork-absence.test.ts'))" --input-type=module` prints `true` — the guard runs in the automated gate.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-fork-absence.test.ts; echo "EXIT=$?"</automated>
    <fails_when>`EXIT=` is non-zero, or any `not ok` line appears, or the TAP summary reports fewer than 8 tests total — a guard with fewer checks than this plan specifies has silently dropped one.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node -e "const{automatedTestFiles}=await import('./test-gate.mjs');console.log('inGate='+automatedTestFiles(process.cwd()).includes('docs-fork-absence.test.ts'))" --input-type=module; node -e "const p=require('./package.json');console.log('inFiles='+p.files.includes('docs-fork-absence.test.ts'))"</automated>
    <fails_when>`inGate=` prints `false` — the guard exists but never runs, which is indistinguishable from not having written it. Or `inFiles=` prints `true` — a repository-verification guard leaked into the published tarball, which every sibling `docs-*.test.ts` deliberately avoids.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && git ls-files -- src/mcp/vice/docs-fork-absence.test.ts | wc -l</automated>
    <fails_when>Output is `0` — the guard was written but never staged, so it exists only in the working tree and would vanish on a fresh clone.</fails_when>
  </verify>
  <done>A committed, automated, non-vacuous guard fails the moment fork vocabulary, a second manifest, a deleted module or a missing acceptance record reappears; it does not scan `.planning/**` and does not ship in the tarball.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full verification battery and record the failure SET as the phase floor</name>
  <files>none — verification-only task; it runs the battery and records results, modifying no repository file</files>
  <read_first>
    - `.planning/phases/52-remove-the-fork-backend/52-06-SUMMARY.md` — the re-baselined reference failure SET every plan since has compared against.
    - `.planning/phases/52-remove-the-fork-backend/52-VALIDATION.md` — the three named hazards: `npm test` on the full glob HANGS (never propose it), piping the run reports the pipe's exit code and fakes a green baseline, and a live broker reddens one test deterministically.
    - `src/mcp/vice/test-gate.mjs` — `MANUAL_ONLY_TESTS` as left by plans 52-05 and 52-06, and `automatedTestFiles()`.
  </read_first>
  <precondition>No vice broker and no `x64sc` process is running: `ps aux | grep -iE 'vice-broker|x64sc' | grep -v grep` produces no output. A live broker reddens one test deterministically — not as a flake — and every floor comparison in this plan would be measured against a corrupted baseline.</precondition>
  <action>
Run the full verification battery and record the result as the phase's closing floor.

Run each of these and capture its exit code on the SAME LINE as the command (with `;`, never a pipe — piping reports the pipe's exit status and has already faked a green baseline in this repository once):

- `npm run typecheck` in `src/mcp/vice`
- `npm run test:automated` in `src/mcp/vice`, redirected to a log file
- `npm run smoke` in `src/mcp/vice`
- `node scripts/check-npm-packages.mjs`
- `node scripts/check-skill-capability-honesty.mjs`
- `node scripts/check-skill-tool-coverage.mjs`
- `node scripts/check-skill-cli-invocations.mjs`

Never run `npm test` on the full glob — it blocks forever on `vice-proxy.test.ts`, which is why that file is dispositioned manual-only. `npm run test:automated` is the gate.

From the suite log, extract the sorted set of `not ok` test identifiers and compare it as a SET against the documented floor: 3 members — one in `anno-import.test.ts` and two in `anno-register.test.ts`, all stale requirement-id citations unrelated to this phase — plus up to 2 known flakes that sit OUTSIDE those files, `audit-root-args.test.ts` (a `zz-scratch-*` ENOENT scan race) and `text-protocol.test.ts` (a 0ms timing control). A run with the same count but a different member is a regression; a run with a higher count whose extra members are exactly the two named flakes is not.

Do NOT fix the three floor failures. They are pre-existing, unrelated, and fixing them here would destroy the only signal that says whether this phase regressed anything. Record them, do not repair them.

If the set contains any member that is neither a floor member nor a named flake, stop and report it rather than closing the phase.
  </action>
  <reversibility rating="reversible">A measurement run. It changes nothing; it only records.</reversibility>
  <acceptance_criteria>
    - `ps aux | grep -iE 'vice-broker|x64sc' | grep -v grep` produced no output before any measurement was taken.
    - All seven battery commands exited 0, except `npm run test:automated`, whose non-zero exit is expected at the documented floor.
    - The suite's sorted `not ok` SET contains only the 3 documented floor members plus, at most, the 2 named flakes.
    - No member of the set is in a file this phase touched.
    - The exact set is recorded verbatim in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && ps aux | grep -iE 'vice-broker|x64sc' | grep -vc grep</automated>
    <fails_when>Output is greater than `0` — a live broker or emulator is running and will deterministically redden one test, so every measurement below would be against a corrupted baseline. Stop the process and re-run rather than recording the result.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck; echo "typecheck=$?"; npm run smoke > /tmp/smoke-52-10.log 2>&1; echo "smoke=$?"; cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-npm-packages.mjs > /tmp/pkg-52-10.log 2>&1; echo "pkg=$?"; node scripts/check-skill-capability-honesty.mjs > /tmp/honesty-52-10.log 2>&1; echo "honesty=$?"; node scripts/check-skill-tool-coverage.mjs > /tmp/cov-52-10.log 2>&1; echo "coverage=$?"; node scripts/check-skill-cli-invocations.mjs > /tmp/cli-52-10.log 2>&1; echo "cli=$?"</automated>
    <fails_when>Any of the six `<name>=` lines reports a non-zero value. Each corresponds to a named gate: `typecheck` to the build, `smoke` to the stdio server answering `tools/list` from the surviving manifest, `pkg` to the published tarball's contents, `honesty` to the inverted documentation gate, `coverage` to skills naming only tools that exist, `cli` to documented CLI invocations resolving.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated > /tmp/floor-52-10.log 2>&1; echo "SUITE_EXIT=$?"; echo "--- failure SET ---"; grep -a '^not ok' /tmp/floor-52-10.log | sed 's/[0-9]\+ - //' | sort; echo "--- end ---"</automated>
    <fails_when>The sorted SET between the two markers contains any member that is neither one of the 3 documented floor members (one in `anno-import.test.ts`, two in `anno-register.test.ts`) nor one of the 2 named flakes (`audit-root-args.test.ts`, `text-protocol.test.ts`). Judge by SET membership, never by count — a same-count run with a swapped member is a regression, and a higher-count run whose extras are the named flakes is not. `SUITE_EXIT=` being non-zero is EXPECTED at the documented floor and is not itself the failure signal. Do not pipe this command into `tail`.</fails_when>
  </verify>
  <done>The battery is green on all six named gates, the suite sits at the documented failure-set floor with no member in a file this phase touched, the measurement was taken with no live broker, and the exact set is recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the seven success criteria against the tree and close the requirement rows</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/ROADMAP.md` `### Phase 52` — the seven success criteria, verbatim.
    - `.planning/REQUIREMENTS.md`'s `### Fork-Backend Removal (Phase 52)` subsection and its seven `FORKRM-*` Traceability rows, as written by plan 52-02.
    - Every `52-*-SUMMARY.md` written so far — the recorded decisions, the knowingly-deferred items and the measured counts each plan reports.
  </read_first>
  <action>
Walk the seven ROADMAP success criteria one at a time and record, for each, the mechanical evidence that it holds — then tick the matching `FORKRM-*` requirement.

For each criterion, cite a runnable check rather than a prose claim: criterion 1 by the `docs-fork-absence.test.ts` identifier walk plus a zero count for `"fork"` across `src/mcp/vice/*.ts` and `*.mts`; criterion 2 by `docs-fork-decision.test.ts` green; criterion 3 by `docs/stock-hard-losses.md` existing, being cited from README and skills, and `check-skill-capability-honesty.mjs` green; criterion 4 by zero `DENY_LIST` references plus `anno-tools.test.ts` green plus the comment-only diff on `anno-tools.ts` recorded in plan 52-05's SUMMARY; criterion 5 by `capability-registry.ts` absent and the recorded decision with its rejected alternative; criterion 6 by one `tools-manifest*.json` file and `check-npm-packages.mjs` green; criterion 7 by Task 2's failure SET.

Then update `.planning/REQUIREMENTS.md`: change each `FORKRM-NN` requirement bullet from `- [ ]` to `- [x]` and each Traceability row's status from `Pending` to `Complete`, but ONLY for criteria whose evidence actually holds. If any criterion's evidence does not hold, leave its requirement unticked and its row `Pending`, and say so plainly — a requirement marked Complete over its own contrary evidence is the failure mode this reconciliation exists to prevent.

Do not touch any other requirement's checkbox or status, and do not touch `.planning/ROADMAP.md`'s Progress table or its phase checkbox — the orchestrator owns those.
  </action>
  <reversibility rating="reversible">A reconciliation write across one planning document; the evidence it records is reproducible from the commands above.</reversibility>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains seven `FORKRM-*` entries whose checkbox state matches the evidence recorded for their criterion — no entry is ticked whose evidence does not hold.
    - Every `FORKRM-NN` Traceability row's status is `Complete` or `Pending`, matching its bullet's checkbox.
    - `grep -ac 'FORKRM-0[1-7]' .planning/REQUIREMENTS.md` is unchanged from plan 52-02's count — rows were updated, not added or removed.
    - No `DECOMP-*`, `BUILD-*` or `EQUIV-*` checkbox or status changed.
    - `node --test docs-fork-decision.test.ts` still exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && printf 'forkrm_total='; grep -ac 'FORKRM-0[1-7]' .planning/REQUIREMENTS.md; printf 'forkrm_pending_rows='; grep -ac '| FORKRM-0[1-7] | Phase 52 | Pending |' .planning/REQUIREMENTS.md; printf 'forkrm_complete_rows='; grep -ac '| FORKRM-0[1-7] | Phase 52 | Complete |' .planning/REQUIREMENTS.md</automated>
    <fails_when>`forkrm_total=` differs from the value plan 52-02's SUMMARY recorded (rows were added or lost rather than updated), or the sum of `forkrm_pending_rows=` and `forkrm_complete_rows=` is not exactly `7` (a row's status text was mangled and now matches neither pattern).</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && printf 'fork_literal='; grep -ra '"fork"' src/mcp/vice --include='*.ts' --include='*.mts' | wc -l; printf 'denylist='; grep -rac 'DENY_LIST' src/mcp/vice --include='*.ts' --include='*.mts' | awk -F: '{s+=$2} END {print s+0}'; printf 'manifests='; ls src/mcp/vice/tools-manifest*.json | wc -l</automated>
    <fails_when>`fork_literal=` or `denylist=` is greater than `0` — INVERTED polarity: for these deletions a non-zero count is the failure and `0` is the success, so criteria 1 and 4 do not hold and their requirements must stay unticked. `manifests=` being anything other than `1` fails criterion 6: `0` means the surviving manifest was lost, `2` or more means a second manifest exists.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-fork-decision.test.ts docs-fork-absence.test.ts docs-constraints-sync.test.ts; echo "EXIT=$?"</automated>
    <fails_when>`EXIT=` is non-zero or any `not ok` line appears — the three guards that pin this phase's outcome must all be green before any requirement is ticked.</fails_when>
  </verify>
  <done>Every one of the seven criteria has a cited runnable check recorded against it; each `FORKRM-*` requirement's checkbox and Traceability status match that evidence and nothing else; no unrelated requirement changed.</done>
</task>

</tasks>

<assumption_delta_decision>
Recorded once for this phase in plan `52-01`: primary noun `backend`, decision `promote`, companion invariant test already required by criterion 2. This plan adds the complementary source-level half of that invariant — `docs-fork-absence.test.ts` goes red if a future phase reintroduces the singular-to-plural assumption in code, manifests or shipped prose, where `docs-fork-decision.test.ts` covers it in the decision record.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future phase → this tree | the only thing standing between a removed backend and its accidental reintroduction is a committed, running, non-vacuous guard |
| measurement → conclusion | a suite run measured against a corrupted baseline (live broker, piped exit code, counted instead of set-compared) produces a confident wrong answer |
| grep → human judgement | a grep proves a phrase is absent; it cannot prove the sentence that replaced it is true |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-36 | Tampering | reintroduction of fork vocabulary or a second manifest | high | mitigate | `docs-fork-absence.test.ts` walks the `files[]`-derived shipped module set plus the skills tree, README and CLAUDE.md for nine forbidden identifiers, asserts exactly one manifest, asserts six deleted modules stay absent, and carries planted-violation tests driving the real predicates so the guard cannot pass vacuously |
| T-52-37 | Information Disclosure | a NUL byte hiding a file from the walk | medium | mitigate | The guard reads NUL-tolerantly; a plain text read stops at the NUL in `anno-memmap-render.ts` and silently skips it, which has already produced one wrong decision in this repository |
| T-52-38 | Tampering | a guard that scans nothing | high | mitigate | A scanned-file-count floor, non-empty assertions on every identifier set, and three planted-violation tests — a lint that finds nothing passes everything |
| T-52-39 | Repudiation | the closing floor measurement | high | mitigate | The precondition requires no live broker; every command captures its exit code on the same line rather than through a pipe; the comparison is a failure SET, never a count; and the three pre-existing floor failures are explicitly not repaired, so the signal about this phase's own regressions survives |
| T-52-40 | Information Disclosure | skill prose that omits rather than states | high | mitigate | A `<human-check>` in this plan's verification requires a human to read all nine rewritten sites and confirm each states what is permanently unavailable and why — the one thing no grep can establish |
| T-52-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan |
</threat_model>

## Artifacts this phase produces

**Created by this plan**
- `src/mcp/vice/docs-fork-absence.test.ts` — NEW guard. Asserts: nine forbidden identifiers absent from the shipped module set, the skills tree, README and CLAUDE.md; exactly one `tools-manifest*.json`; six deleted modules absent from disk and from `files[]`; `docs/stock-hard-losses.md` present, non-vacuous, naming all three losses and cited from README; plus a scanned-file-count floor, non-empty identifier sets, and three planted-violation tests. Deliberately excluded from `package.json`'s `files[]` and deliberately not scanning `.planning/**`.
- The phase's recorded closing failure SET and battery results.
- Ticked `FORKRM-01..07` checkboxes and `Complete` Traceability statuses, each backed by a cited runnable check.

**Removed by this plan**
- Nothing. This plan adds a guard and records evidence; it deletes no code, no document and no test.

**Explicitly NOT touched by this plan**
- The three pre-existing floor failures in `anno-import.test.ts` and `anno-register.test.ts` — unrelated stale requirement-id citations, deliberately left so the floor keeps its meaning.
- `.planning/ROADMAP.md`'s Progress table and phase checkbox — the orchestrator owns those.
- Any `DECOMP-*`, `BUILD-*` or `EQUIV-*` requirement.
- `.planning/**` as a scan target for the new guard.

<verification>
`node --test docs-fork-absence.test.ts docs-fork-decision.test.ts docs-constraints-sync.test.ts` green; `npm run typecheck`, `npm run smoke`, `check-npm-packages.mjs`, `check-skill-capability-honesty.mjs`, `check-skill-tool-coverage.mjs` and `check-skill-cli-invocations.mjs` all exit 0; `npm run test:automated` sits at the documented failure SET with no member in a file this phase touched, measured with no live broker.

<human-check>
Read all nine rewritten fork-routing sites (listed by file and line in plan 52-08's SUMMARY) and confirm each one states what is permanently unavailable and WHY, rather than merely omitting a tool. A grep can prove "requires the fork" is absent; it cannot prove the replacement sentence is true and useful to a reader designing a method.

Then read `.planning/PROJECT.md`'s amended `FORK-01` row, its `### Out of Scope` fork bullet, and `.planning/todos/completed/2026-08-20-fully-remove-the-forked-vice-mcp-backend.md` together, and confirm no one of the three contradicts the others — in particular that the superseded `retain` disposition does not still read as the current answer.
</human-check>
</verification>

<success_criteria>
- Criterion 7 is satisfied with the failure SET recorded, not a count.
- Criteria 1, 4 and 6 are regression-proof: a committed, running, non-vacuous guard fails the moment any of them is undone.
- Every `FORKRM-*` requirement's status matches its own cited evidence, and none is marked Complete over contrary evidence.
</success_criteria>

<output>
Create `.planning/phases/52-remove-the-fork-backend/52-10-SUMMARY.md` when done. Record: the verbatim closing failure SET with each member attributed to the floor or to a named flake; the six battery exit codes; the scanned-file count the new guard reported; the per-criterion evidence table; any `FORKRM-*` left unticked with the reason; and the outcome of both human checks.
</output>
