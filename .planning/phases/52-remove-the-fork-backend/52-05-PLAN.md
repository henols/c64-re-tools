---
phase: 52-remove-the-fork-backend
plan: 05
type: execute
wave: 4
depends_on: ["52-04"]
files_modified:
  - src/mcp/vice/vice.ts
  - src/mcp/vice/vice.test.ts
  - src/mcp/vice/vice-proxy.ts
  - src/mcp/vice/refresh-manifest.ts
  - src/mcp/vice/refresh-manifest.test.ts
  - src/mcp/vice/tools-manifest.json
  - src/mcp/vice/manifest-arg-compat.test.ts
  - src/mcp/vice/fork-manifest-surface.test.ts
  - src/mcp/vice/fork-deleted-tools.ts
  - src/mcp/vice/fork-live.test.ts
  - src/mcp/vice/test-gate.mjs
  - src/mcp/vice/anno-tools.ts
  - src/mcp/vice/anno-tools.test.ts
  - src/mcp/vice/smoke.mjs
  - src/mcp/vice/README.md
  - src/mcp/vice/package.json
  - src/mcp/vice/shipped-modules.ts
  - src/mcp/vice/acme-gate.ts
  - scripts/check-skill-tool-coverage.mjs
autonomous: false
requirements: [FORKRM-01, FORKRM-04, FORKRM-06]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`src/mcp/vice/vice.ts` is deleted whole, and with it `DENY_LIST`, `denyListRefusalMessage()`, `rpc()`, `ensureInitialized()`, `withReconnect()`, `call()`/`callTool`, `serverInfo()` and the session-identity apparatus (`SessionInfo`, `beginSession`, `sessionReconnects`, `lastToolCall`, `assertSameMachine`, `CallFn`) (FORKRM-01, FORKRM-04)"
    - "`src/mcp/vice/tools-manifest.json` and `src/mcp/vice/refresh-manifest.ts` are deleted; `tools-manifest.stock.json` is the only manifest and nothing regenerates a manifest from a live host (FORKRM-06)"
    - "Every consumer that read the fork manifest — `check-skill-tool-coverage.mjs`, `anno-tools.test.ts`, `smoke.mjs`, `src/mcp/vice/README.md` and the `files[]` entry — is updated in the same wave so neither the suite nor CI is left red"
    - "`anno-tools.ts`'s `CURATED_ANNO_TOOLS`, `assertAnnoBatch` and `ANNO_MAX_BATCH_DEPTH` are UNCHANGED as code; only its two `DENY_LIST` comment citations are rewritten, and `anno-tools.test.ts` is still green (FORKRM-04)"
    - statement: "No replacement denylist, allowlist or per-tool special case is introduced to stand in for `DENY_LIST` — with the fork's generic meta-tool surface gone there is nothing left to filter"
      verification: backstop
  artifacts:
    - src/mcp/vice/package.json
    - src/mcp/vice/anno-tools.ts
    - scripts/check-skill-tool-coverage.mjs
  key_links:
    - "`package.json` `files[]` to `scripts/check-npm-packages.mjs:165` — that line hard-asserts `vice.files.includes(\"tools-manifest.json\")` and must be repointed in plan 52-07, which owns that script; this plan removes the `files[]` entry, so the two plans are coupled across waves and the SUMMARY must say so"
    - "`scripts/check-skill-tool-coverage.mjs:123-124` and `:304` to `tools-manifest.json` — a CI-blocking script, not a `node --test` test, so the automated suite stays green while CI would go red; it must be updated here"
    - "`anno-tools.ts:81` and `:2010` to `DENY_LIST` — comment-only citations explaining WHY the inverted allowlist is shaped as it is; rewritten in the abstract, never deleted"
  prohibitions:
    - statement: "`anno-tools.ts`'s `CURATED_ANNO_TOOLS`, `assertAnnoBatch()` and `ANNO_MAX_BATCH_DEPTH` are not modified — every changed line in that file is a comment line"
      status: required
      verification: flagged-unverified
    - statement: "No new denylist, no per-tool special-case filter, and no replacement for `serverInfo()`'s discovery filtering is introduced anywhere"
      status: required
      verification: flagged-unverified
    - statement: "`tools-manifest.stock.json` is NOT edited, renamed, regenerated or reformatted — it survives byte-identical and simply becomes the only manifest"
      status: required
      verification: flagged-unverified
    - statement: "`.planning/notes/deny-list-is-a-fork-artifact.md`'s instruction is honoured: the `anno-tools.ts` citations are rewritten to describe the precedent in the abstract, not removed"
      status: required
      verification: flagged-unverified
---

<objective>
Delete the fork transport and the fork manifest, and unwind every consumer that read either.

`vice.ts` is now reduced to its fork half by plan 52-03's split, and `vice-proxy.ts` stopped calling it in plan 52-04. `DENY_LIST` dies with it: all five entries are fork artifacts, four are the fork HTTP server's own protocol methods advertised as callable tools, and the fifth (`vice_disk_list`) is in NEITHER manifest and has been guarding a tool that exists on no backend. The nested-argument hazard is not gone — only this instance of it is — which is why `anno-tools.ts`'s inverted allowlist is fenced and only its explanatory citations move.

Research named four fork-manifest consumers. A fresh census finds more: `scripts/check-skill-tool-coverage.mjs` (a CI-blocking script that reads the fork manifest directly at `:123-124` and `:304`, so the automated suite would stay green while CI went red), `anno-tools.test.ts:570`, `smoke.mjs`, `src/mcp/vice/README.md:42`, plus two whole-file casualties the research did not list at all: `fork-manifest-surface.test.ts` and `fork-deleted-tools.ts`.

Purpose: land criterion 6 and the code half of criterion 4 without leaving either the suite or CI red.
Output: one manifest, no fork transport, no `DENY_LIST`, `anno-tools.ts` fenced.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-remove-the-fork-backend/52-RESEARCH.md
@.planning/notes/deny-list-is-a-fork-artifact.md
@src/mcp/vice/vice.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint: confirm deleting the fork transport, DENY_LIST and the fork manifest</name>
  <files>none — decision gate; no file is written by this task</files>
  <decision>Delete vice.ts whole — taking DENY_LIST and denyListRefusalMessage(), the one refusal this tree calls a security control, with it — and delete tools-manifest.json and refresh-manifest.ts, after which nothing in the project can regenerate the fork's tool surface?</decision>
  <options>
    A) Proceed with both deletions. The nested-argument hazard class stays guarded by inversion in anno-tools.ts, byte-identical, with its explanatory citations rewritten rather than dropped.
    B) Halt. Keep the fork transport and its manifest.
  </options>
  <resume-signal>The user replies A or B. Any other reply is treated as not-yet-answered; ask again rather than assuming A.</resume-signal>
  <read_first>
    - `.planning/notes/deny-list-is-a-fork-artifact.md` in full — the per-entry measurement showing all five `DENY_LIST` names are fork artifacts and that `vice_disk_list` is in neither manifest.
    - `src/mcp/vice/vice.ts` as left by plan 52-03 — the fork transport half and the session-identity apparatus.
    - `src/mcp/vice/anno-tools.ts` around lines 81 and 2010 — the two citations naming `DENY_LIST` as the precedent the surviving inverted allowlist inverted.
  </read_first>
  <action>
PAUSE and put two one-way deletions to the user before touching anything.

**Decision A — delete `vice.ts` whole, taking `DENY_LIST` and `denyListRefusalMessage()` with it.** `DENY_LIST` is described in this tree as the only refusal that is a security control. It is being removed because its target surface is removed in the same wave — the four meta-tool entries exist only in the fork manifest and are dispatched only through the fork transport, and the fifth is in neither manifest. The residual hazard class is NOT dismissed: `anno_batch_execute` has the same nested-argument shape, is live and backend-independent, and stays guarded by inversion (`CURATED_ANNO_TOOLS`, `assertAnnoBatch()`, `ANNO_MAX_BATCH_DEPTH`), byte-identical, with its explanatory citations rewritten rather than deleted.

**Decision B — delete `tools-manifest.json` and `refresh-manifest.ts`.** After this, nothing in the project can regenerate the fork's snapshotted tool surface, because the transport that served it is gone.

One residual risk to disclose: research verified by grep only — not by tracing every call graph — that no stock path calls `beginSession()`/`assertSameMachine()`. Task 1 carries a blocking precondition re-running that check and halting if it finds one.

Present: (a) proceed with both; (b) halt. Do not delete anything until the user answers.
  </action>
  <acceptance_criteria>
    - The user's answer is recorded verbatim in the plan SUMMARY, with its date.
    - No file was modified before the answer was received.
    - The SUMMARY records that the surviving nested-argument guard in `anno-tools.ts` was explicitly discussed, not assumed.
  </acceptance_criteria>
  <verify>
    <human-check>The user has confirmed deleting the fork transport together with the one refusal this tree calls a security control, understanding that the hazard class survives and stays guarded by inversion elsewhere.</human-check>
  </verify>
  <done>The user has answered; the answer and its date are recorded; nothing was deleted before the answer.</done>
</task>

<task type="auto">
  <name>Task 1: Delete vice.ts, its test, and DENY_LIST's remaining consumers</name>
  <files>src/mcp/vice/vice.ts, src/mcp/vice/vice.test.ts, src/mcp/vice/vice-proxy.ts, src/mcp/vice/fork-live.test.ts, src/mcp/vice/fork-deleted-tools.ts, src/mcp/vice/fork-manifest-surface.test.ts, src/mcp/vice/test-gate.mjs, src/mcp/vice/shipped-modules.ts, src/mcp/vice/acme-gate.ts, src/mcp/vice/package.json</files>
  <read_first>
    - `src/mcp/vice/vice.ts` as it stands after plan 52-03's split — the surviving fork half. The symbols to go: `DEFAULT_ENDPOINT` (`:41`), `DEFAULT_TIMEOUT_MS` (`:42`), `DENY_LIST` (`:201`), `denyListRefusalMessage()` (`:229`), `RpcOptions` (`:295`), `rpc()`, `ensureInitialized()`, `withReconnect()`, `SessionInfo` (`:485`), `beginSession()` (`:566`), `sessionReconnects()` (`:575`), `lastToolCall()` (`:581`), `CallFn` (`:585`), `AssertSameMachineOptions` (`:587`), `assertSameMachine()` (`:618`), `call()` (`:697`), `callTool` (`:737`), `ServerInfoPayload` (`:746`), `serverInfo()` (`:766`).
    - `src/mcp/vice/vice-proxy.ts` — the remaining `./vice.ts` import statement plan 52-03 left explicit, the `DENY_LIST` manifest-registration skip (`vice-proxy.ts:3396` pre-52-04), and the `CallToolRequestSchema` override guard (`vice-proxy.ts:3465` pre-52-04). Locate both by text; line numbers shifted in plan 52-04.
    - `src/mcp/vice/stock-connect.ts` — its header explains stock's own reconnect/identity mechanism. Read it to confirm stock does not depend on the session-identity apparatus.
    - The output of `grep -rn 'beginSession\|assertSameMachine\|sessionReconnects\|lastToolCall\|denyListRefusalMessage\|DENY_LIST' --include='*.ts' --include='*.mts' --include='*.mjs' src/ scripts/` — the real consumer set. Research verified by grep only, not by tracing every call graph, and rated this MEDIUM risk (assumption A5): a stock path secretly calling `beginSession()`/`assertSameMachine()` would mean deleting a live reconnect-safety check.
    - `src/mcp/vice/vice.test.ts` (187 lines) — confirm every test in it targets a symbol being deleted before deleting the file.
  </read_first>
  <precondition>`grep -rn 'beginSession(\|assertSameMachine(' --include='*.ts' --include='*.mts' src/ ` returns hits only inside `vice.ts` and `vice.test.ts`. If any `stock-*.ts` or `text-*.ts` module calls either, the session-identity apparatus is NOT dead fork-era code and this task must halt and escalate rather than delete a live reconnect-safety check.</precondition>
  <action>
Delete `src/mcp/vice/vice.ts` and `src/mcp/vice/vice.test.ts` as whole files, remove the `"vice.ts"` entry from `src/mcp/vice/package.json`'s `files[]`, and unwind the two remaining `DENY_LIST` consumers in `vice-proxy.ts`.

In `vice-proxy.ts`: delete the remaining `import ... from "./vice.ts"` statement (plan 52-03 left it as one explicit statement precisely so this is a single-statement removal); delete the `if (DENY_LIST.includes(def.name)) continue;` skip in the manifest-registration loop; delete the `CallToolRequestSchema` override guard that consulted `denyListRefusalMessage()`. A comment near the registration seam records that the four meta-tool names were added to `DENY_LIST` so one existing filter would cover them rather than adding per-tool special cases — honour its warning: removing the array must not reintroduce special cases in its place. With the fork's manifest gone there is nothing left to filter, so the correct replacement is nothing at all.

Also delete `src/mcp/vice/fork-live.test.ts` (a live-fork-binary test, manual-only) and remove its `"fork-live.test.ts"` entry from `test-gate.mjs`'s `MANUAL_ONLY_TESTS` array. That array's doc comment says "The exact thirteen test files dispositioned as manual-only" and calls itself frozen — update the count word in the comment to match the new length, and do not add a parallel list. Check whether any test asserts the array's length; if one does, update it in the same edit.

Then delete `src/mcp/vice/fork-deleted-tools.ts` and `src/mcp/vice/fork-manifest-surface.test.ts` — `fork-deleted-tools.ts`'s only importers are `fork-live.test.ts` and `fork-manifest-surface.test.ts`, both deleted here, and `fork-manifest-surface.test.ts`'s entire subject is the fork manifest. Two files carry comment references to `fork-deleted-tools.ts` as an example (`shipped-modules.ts:76` and `acme-gate.ts:40`); rewrite those comments so they do not name a deleted file, preserving whatever pattern each was illustrating.
  </action>
  <reversibility rating="one-way">Deletes the fork's entire HTTP/JSON-RPC transport and the security control built for it, and removes a shipped module from the published tarball. The surface it served no longer exists in the project; this does not come back.</reversibility>
  <acceptance_criteria>
    - `test ! -f src/mcp/vice/vice.ts`, `test ! -f src/mcp/vice/vice.test.ts`, `test ! -f src/mcp/vice/fork-live.test.ts`, `test ! -f src/mcp/vice/fork-deleted-tools.ts` and `test ! -f src/mcp/vice/fork-manifest-surface.test.ts` all succeed.
    - `grep -rac 'DENY_LIST' src/mcp/vice/vice-proxy.ts` returns `0`.
    - `grep -rac 'denyListRefusalMessage' src/mcp/vice --include='*.ts'` returns `0`.
    - `grep -ac 'fork-live.test.ts' src/mcp/vice/test-gate.mjs` returns `0`, and the `MANUAL_ONLY_TESTS` doc comment's count word matches the array's new length.
    - `grep -rac 'fork-deleted-tools' src/mcp/vice --include='*.ts'` returns `0`.
    - `package.json`'s `files[]` no longer lists `vice.ts`.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck; echo "EXIT=$?"</automated>
    <fails_when>`EXIT=` is non-zero. A `TS2307` naming `./vice.ts` means an importer was missed; a `TS2304` naming `DENY_LIST` or `call` means a consumer outside the grepped set survived.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && for f in vice.ts vice.test.ts fork-live.test.ts fork-deleted-tools.ts fork-manifest-surface.test.ts; do printf '%s absent=' "$f"; test ! -f "src/mcp/vice/$f" && echo yes || echo NO; done; printf 'denylist_refs='; grep -rac 'DENY_LIST' src/mcp/vice/vice-proxy.ts; printf 'files_has_vice='; node -e "const p=require('./src/mcp/vice/package.json');console.log(p.files.includes('vice.ts'))"</automated>
    <fails_when>Any `absent=` line reads `NO`, or `denylist_refs=` is anything other than `0` (INVERTED polarity — a non-zero grep count is the failure here), or `files_has_vice=` prints `true`.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node -e "const{automatedTestFiles,MANUAL_ONLY_TESTS}=await import('./test-gate.mjs');console.log('manual='+MANUAL_ONLY_TESTS.length);console.log('forklive='+MANUAL_ONLY_TESTS.includes('fork-live.test.ts'));console.log('vicetest='+automatedTestFiles(process.cwd()).includes('vice.test.ts'))" --input-type=module</automated>
    <fails_when>`forklive=` prints `true` (the deleted file is still dispositioned manual-only) or `vicetest=` prints `true` (the automated gate still expects a deleted test file). `manual=` is informational and must match the count word in the array's doc comment.</fails_when>
  </verify>
  <done>`vice.ts`, `vice.test.ts` and the three fork-manifest-surface files are gone with their `files[]` and `MANUAL_ONLY_TESTS` entries; `DENY_LIST` and `denyListRefusalMessage()` have no remaining reference in code; nothing replaced them; typecheck is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the fork manifest and every consumer that reads it</name>
  <files>src/mcp/vice/tools-manifest.json, src/mcp/vice/refresh-manifest.ts, src/mcp/vice/refresh-manifest.test.ts, src/mcp/vice/manifest-arg-compat.test.ts, src/mcp/vice/anno-tools.test.ts, src/mcp/vice/smoke.mjs, src/mcp/vice/README.md, src/mcp/vice/package.json, scripts/check-skill-tool-coverage.mjs</files>
  <read_first>
    - `src/mcp/vice/refresh-manifest.ts` (124 lines) and `src/mcp/vice/refresh-manifest.test.ts` (265 lines) — confirm the whole point of the module is regenerating the fork manifest from a live fork host.
    - `src/mcp/vice/manifest-arg-compat.test.ts` lines 1-60 and `:194-195` — its premise is a two-manifest comparison; read enough to confirm there is no single-manifest assertion worth preserving.
    - `scripts/check-skill-tool-coverage.mjs` lines 115-135 and lines 295-370 — the manifest path list at `:123-124`, the `forkManifest`/`forkNames` derivation at `:304-306`, and the classification blocks (`PROXY_LOCAL_WITH_STOCK_MANIFEST_ENTRY`, the `DENY_LIST`-referencing classification near `:350`, and the "present in neither manifest" assertions) that are all expressed in two-manifest terms.
    - `src/mcp/vice/anno-tools.test.ts` lines 565-580 — the test asserting `anno_exclude_range`/`anno_include_range` are absent from BOTH manifests.
    - `src/mcp/vice/smoke.mjs` lines 1-20 — it asserts `tools/list` is answered from `tools-manifest.json`; `npm run smoke` is a CI step.
    - `src/mcp/vice/README.md` line 42 and `src/mcp/vice/package.json` `files[]` line 100.
    - `src/mcp/vice/capability-registry.test.ts` line 148 and `src/mcp/vice/stock-dispatch.test.ts` lines 56, 116, 141-142, 211-215 — these read the fork manifest too, but belong to plans 52-06/52-07. Note in the SUMMARY which of them you had to touch here to keep the suite green versus which you left.
  </read_first>
  <action>
Delete `src/mcp/vice/tools-manifest.json` (1223 lines), `src/mcp/vice/refresh-manifest.ts` and `src/mcp/vice/refresh-manifest.test.ts`, and `src/mcp/vice/manifest-arg-compat.test.ts` — then repair every remaining reader in the same task, because a deleted manifest with a live reader is a red tree.

`manifest-arg-compat.test.ts` is a whole-file deletion, not a rewrite: its entire premise is backward-compatibility between two manifests, and with one manifest there is nothing to compare. Note that `CLAUDE.md`'s Compatibility constraint names this file as what pins the argument-shape half of that constraint; the constraint itself is removed by plan 52-09, and this plan's SUMMARY must flag the coupling so 52-09 does not leave a constraint citing a deleted guard.

Remove `"tools-manifest.json"` from `src/mcp/vice/package.json`'s `files[]`. `scripts/check-npm-packages.mjs:165` hard-asserts that entry exists — that script is plan 52-07's to edit, and 52-07 is a later wave, so `node scripts/check-npm-packages.mjs` will be red between this plan and that one. This is a known, bounded, cross-wave coupling: record it explicitly in the SUMMARY and do NOT "fix" it by leaving the `files[]` entry in place (which would ship a deleted file) or by editing `check-npm-packages.mjs` here (which would collide with 52-07's own edits to the same file).

`scripts/check-skill-tool-coverage.mjs` reads the fork manifest at `:123-124` and `:304`. This is a CI-blocking script, not a `node --test` test, so the automated suite would stay green while CI went red — update it here. Narrow it to the single stock manifest: drop `forkManifest`/`forkNames` and every set operation defined against them, and rewrite each classification whose text is phrased as a two-manifest fact ("present in neither manifest by design", the fork-vs-stock contrasts) to a single-manifest statement. Preserve every non-vacuity assertion the script carries — a coverage gate that stops finding anything passes everything.

`anno-tools.test.ts:569-578` asserts two tool names are absent from both manifests. Narrow it to the stock manifest only, keeping the assertion (the `anno_*` family is proxy-local by design and must stay out of the manifest). This is the ONLY change to that file in this task; the source `anno-tools.ts` is Task 3's.

`smoke.mjs` and `src/mcp/vice/README.md:42` both name `tools-manifest.json` as the file `tools/list` is answered from. Repoint both to `tools-manifest.stock.json`. `src/mcp/vice/README.md` ships in the tarball, so keep it free of planning vocabulary.

Do not touch `tools-manifest.stock.json` itself — it survives byte-identical and simply becomes the only manifest.
  </action>
  <reversibility rating="one-way">Deletes the fork's snapshotted 62-tool surface and its sole regenerator. Nothing in the project can produce that file again once the transport that served it is gone.</reversibility>
  <acceptance_criteria>
    - `test ! -f src/mcp/vice/tools-manifest.json`, `test ! -f src/mcp/vice/refresh-manifest.ts`, `test ! -f src/mcp/vice/refresh-manifest.test.ts` and `test ! -f src/mcp/vice/manifest-arg-compat.test.ts` all succeed.
    - `git diff --numstat -- src/mcp/vice/tools-manifest.stock.json` produces no output — the surviving manifest is byte-identical.
    - No file under `src/` or `scripts/` (excluding this phase's planning artifacts) contains the string `tools-manifest.json` other than as part of `tools-manifest.stock.json`.
    - `node scripts/check-skill-tool-coverage.mjs` exits 0 and its success line still reports a non-zero number of skills and tools scanned.
    - `node --test anno-tools.test.ts` exits 0 with the `anno_exclude_range`/`anno_include_range` absence test still present.
    - `npm run smoke` exits 0.
    - `package.json`'s `files[]` lists `tools-manifest.stock.json` and not `tools-manifest.json`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ra 'tools-manifest\.json' src/ scripts/ --include='*.ts' --include='*.mts' --include='*.mjs' --include='*.json' --include='*.md' | grep -v 'tools-manifest\.stock\.json' | wc -l</automated>
    <fails_when>Output is anything other than `0` — INVERTED polarity: a non-zero COUNT of surviving references is the failure, and an empty result is the success. Any surviving reference names a file that no longer exists.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && test ! -f src/mcp/vice/tools-manifest.json && echo "MANIFEST_GONE" || echo "MANIFEST_PRESENT"; git diff --numstat -- src/mcp/vice/tools-manifest.stock.json | wc -l</automated>
    <fails_when>The first line prints `MANIFEST_PRESENT`, or the second line prints anything other than `0` (the surviving stock manifest was edited, reformatted or regenerated when it was supposed to be untouched).</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-skill-tool-coverage.mjs; echo "coverage_exit=$?"; cd src/mcp/vice && npm run smoke > /tmp/smoke-52-05.log 2>&1; echo "smoke_exit=$?"; tail -5 /tmp/smoke-52-05.log</automated>
    <fails_when>`coverage_exit=` or `smoke_exit=` is non-zero, or the coverage script's success line reports zero skills or zero tools scanned (a gate narrowed into vacuity passes everything and is worse than a red one).</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated > /tmp/floor-52-05.log 2>&1; echo "EXIT=$?"; grep -a '^not ok' /tmp/floor-52-05.log | sed 's/[0-9]\+ - //' | sort</automated>
    <fails_when>The sorted `not ok` SET gains any member absent from the pre-plan baseline set. Members that vanish because `refresh-manifest.test.ts`, `manifest-arg-compat.test.ts`, `fork-manifest-surface.test.ts` or `vice.test.ts` no longer exist are expected. `tool-support-table.test.mjs` and `capability-registry.test.ts` members appearing here are EXPECTED and bounded — both read the fork manifest and are plan 52-07's to repair; note them in the SUMMARY rather than fixing them here. Any other new member is a regression. Do not pipe into `tail`.</fails_when>
  </verify>
  <done>One manifest remains, byte-identical; the regenerator, the two-manifest guard and the fork-manifest surface tests are gone; every remaining reader reads the stock manifest; CI's coverage and smoke steps are green; the only new suite failures are the two files plan 52-07 owns, recorded by name.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite anno-tools.ts's two DENY_LIST citations, leaving its guards byte-identical</name>
  <files>src/mcp/vice/anno-tools.ts</files>
  <read_first>
    - `src/mcp/vice/anno-tools.ts` around line 81 and around line 2010 — the two `DENY_LIST` comment citations, and enough surrounding context to see what each one is explaining.
    - `src/mcp/vice/anno-tools.ts`'s declarations of `CURATED_ANNO_TOOLS`, `assertAnnoBatch()` and `ANNO_MAX_BATCH_DEPTH` — read them so you can confirm afterwards that not one of their lines changed.
    - `.planning/notes/deny-list-is-a-fork-artifact.md` in full — its closing instruction is explicit: the array goes with the fork, the pattern stays, and anyone removing `DENY_LIST` must leave these guards untouched and should rewrite the citation rather than delete it, since it is the reason that guard is shaped the way it is.
  </read_first>
  <action>
Rewrite the two `DENY_LIST` comment citations in `src/mcp/vice/anno-tools.ts` (at `:81` and `:2010`) so each describes the precedent in the abstract instead of naming a symbol that no longer exists.

The surviving lesson, which both comments must still convey: `anno_batch_execute` executes several `anno_*` calls from a nested list, so an outer-name check on `anno_batch_execute` says nothing about what is inside — the same confused-deputy shape an earlier generic meta-tool surface in this project had, which was closed at the time with a denylist of forbidden outer names. That denylist's target surface no longer exists, but the hazard class does, which is why this guard is an ALLOWLIST (`CURATED_ANNO_TOOLS`) rather than a denylist, and why it is paired with `assertAnnoBatch()` and a depth bound (`ANNO_MAX_BATCH_DEPTH`). State that without naming `DENY_LIST`, `vice.ts` or the fork — this file ships verbatim to npm, so it must carry no planning vocabulary and no dangling reference to a deleted symbol.

Change nothing else in this file. `CURATED_ANNO_TOOLS`, `assertAnnoBatch()` and `ANNO_MAX_BATCH_DEPTH` are criterion 4's fenced set: every changed line in `anno-tools.ts` must be a comment line, and the acceptance check below is mechanical about it.
  </action>
  <reversibility rating="reversible">Two comment rewrites in one file; `git revert` restores them exactly, and the mechanical comment-only check makes an accidental code change impossible to miss.</reversibility>
  <acceptance_criteria>
    - `grep -ac 'DENY_LIST' src/mcp/vice/anno-tools.ts` returns `0`.
    - Every changed line in `anno-tools.ts` is a comment line: the count of non-comment changed lines is `0`.
    - `grep -ac 'CURATED_ANNO_TOOLS' src/mcp/vice/anno-tools.ts`, `grep -ac 'assertAnnoBatch' src/mcp/vice/anno-tools.ts` and `grep -ac 'ANNO_MAX_BATCH_DEPTH' src/mcp/vice/anno-tools.ts` are each unchanged from before this task.
    - `node --test anno-tools.test.ts` exits 0.
    - `grep -ac '\.planning/' src/mcp/vice/anno-tools.ts` returns `0` — the rewritten comments introduced no planning vocabulary into a shipped file.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && git diff -U0 -- src/mcp/vice/anno-tools.ts | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -vcE '^[+-][[:space:]]*(//|\*|/\*)'</automated>
    <fails_when>Output is anything other than `0`. Each counted line is a changed NON-comment line in `anno-tools.ts`, which means criterion 4's fenced set was modified. Note the polarity: a non-zero count is the failure and an all-comment diff yields `0`.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && printf 'denylist='; grep -ac 'DENY_LIST' src/mcp/vice/anno-tools.ts; printf 'planning='; grep -ac '\.planning/' src/mcp/vice/anno-tools.ts; printf 'curated='; grep -ac 'CURATED_ANNO_TOOLS' src/mcp/vice/anno-tools.ts</automated>
    <fails_when>`denylist=` or `planning=` reports anything other than `0` (INVERTED polarity — non-zero is the failure), or `curated=` reports `0`, which would mean the allowlist itself was removed.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test anno-tools.test.ts; echo "EXIT=$?"</automated>
    <fails_when>`EXIT=` is non-zero or any `not ok` line appears — the inverted allowlist's behaviour changed, which criterion 4 forbids.</fails_when>
  </verify>
  <done>`anno-tools.ts` names no deleted symbol, carries the surviving lesson in the abstract, and has not one non-comment line changed; its test is green.</done>
</task>

</tasks>

<assumption_delta_decision>
Recorded once for this phase in plan `52-01`: primary noun `backend`, decision `promote`, companion invariant test already required by criterion 2. This plan collapses the two-manifest model to one, which is the same `promote` decision applied to the manifest axis.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → tool name gate | `DENY_LIST` was the outer-name gate on a generic meta-tool surface; removing it removes the gate AND the surface together |
| MCP client → `anno_batch_execute` nested list | the SAME confused-deputy shape, still live, still backend-independent, guarded separately by inversion |
| repo → npm consumer | the published tarball's `files[]` decides what a consumer downloads and can execute |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-14 | Elevation of Privilege | `DENY_LIST` removal | high | mitigate | The control is removed only because its target surface is removed in the same wave: all four meta-tool entries exist only in the fork manifest (deleted in Task 2) and are dispatched only through the fork transport (deleted in Task 1); the fifth entry is in neither manifest. The residual hazard class is explicitly NOT dismissed — `anno-tools.ts`'s inverted allowlist stays byte-identical (Task 3's mechanical comment-only check) and its rewritten comments state the hazard in the abstract so a future contributor cannot lose the reason |
| T-52-15 | Elevation of Privilege | any replacement filter | high | mitigate | This plan's prohibitions forbid introducing a replacement denylist or a per-tool special case; the removal-seam comment's own warning against per-tool special cases is honoured by adding nothing |
| T-52-16 | Tampering | `tools-manifest.stock.json` | medium | mitigate | The surviving manifest must not be regenerated, reformatted or edited while its sibling is deleted; a `git diff --numstat` producing no output is an acceptance criterion |
| T-52-17 | Denial of Service | `scripts/check-skill-tool-coverage.mjs` | medium | mitigate | A CI-blocking script reading a deleted file would break every build; it is narrowed to one manifest in the same task, and its non-vacuity assertions are preserved so it cannot pass by finding nothing |
| T-52-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan |
</threat_model>

## Artifacts this phase produces

**Created by this plan**
- Nothing new. This plan is removal plus targeted repair of the readers.
- Two rewritten abstract-precedent comments in `anno-tools.ts`, and rewritten comments in `shipped-modules.ts` and `acme-gate.ts` that no longer name `fork-deleted-tools.ts`.

**Removed by this plan**
- `src/mcp/vice/vice.ts` (fork HTTP/JSON-RPC transport) and `src/mcp/vice/vice.test.ts`.
- Symbols: `DENY_LIST`, `denyListRefusalMessage()`, `DEFAULT_ENDPOINT`, `DEFAULT_TIMEOUT_MS`, `rpc()`, `ensureInitialized()`, `withReconnect()`, `call()`, `callTool`, `serverInfo()`, `RpcOptions`, `ServerInfoPayload`, `SessionInfo`, `BeginSessionOptions`, `beginSession()`, `sessionReconnects()`, `lastToolCall()`, `CallFn`, `AssertSameMachineOptions`, `assertSameMachine()`.
- `src/mcp/vice/tools-manifest.json` (1223 lines), `src/mcp/vice/refresh-manifest.ts`, `src/mcp/vice/refresh-manifest.test.ts`.
- `src/mcp/vice/manifest-arg-compat.test.ts` (two-manifest premise), `src/mcp/vice/fork-manifest-surface.test.ts`, `src/mcp/vice/fork-deleted-tools.ts` (and its exported `DELIBERATELY_DELETED_FORK_TOOLS`), `src/mcp/vice/fork-live.test.ts`.
- The `"fork-live.test.ts"` entry in `test-gate.mjs`'s `MANUAL_ONLY_TESTS`, and the `"vice.ts"` / `"tools-manifest.json"` entries in `package.json`'s `files[]`.
- `vice-proxy.ts`'s `DENY_LIST` manifest-registration skip and its `CallToolRequestSchema` override guard.
- The fork half of `scripts/check-skill-tool-coverage.mjs` (`forkManifest`, `forkNames`, and every two-manifest classification phrasing).

**Explicitly NOT touched by this plan**
- `src/mcp/vice/tools-manifest.stock.json` — byte-identical, now the only manifest.
- `anno-tools.ts`'s `CURATED_ANNO_TOOLS`, `assertAnnoBatch()`, `ANNO_MAX_BATCH_DEPTH` — comment-only diff, mechanically checked.
- `scripts/check-npm-packages.mjs` — plan 52-07 owns it; `:165`'s `tools-manifest.json` assertion is knowingly left red for one wave and the coupling is recorded.
- `CLAUDE.md`'s Compatibility bullet naming `manifest-arg-compat.test.ts` — plan 52-09 owns it (both byte-synced copies must move together).
- `capability-registry.ts`, `capability-registry.test.ts`, `tool-support-table.test.mjs`, `stock-dispatch.test.ts` — plans 52-06/52-07.

<verification>
`npm run typecheck` clean; `node scripts/check-skill-tool-coverage.mjs` and `npm run smoke` exit 0; `node --test anno-tools.test.ts` green; `anno-tools.ts`'s diff is comment-only; the suite's new failure members are exactly the fork-manifest readers plan 52-07 owns, listed by name in the SUMMARY.
</verification>

<success_criteria>
- Criterion 6 is satisfied: `tools-manifest.stock.json` is the only manifest, `refresh-manifest.ts` and `tools-manifest.json` are gone, and nothing regenerates a manifest from a live host.
- Criterion 4's code half is satisfied: `DENY_LIST` and `denyListRefusalMessage()` are gone with their consumers, nothing replaced them, and `anno-tools.ts`'s guards are provably unchanged.
</success_criteria>

<output>
Create `.planning/phases/52-remove-the-fork-backend/52-05-SUMMARY.md` when done. Record: the precondition grep's actual result for `beginSession`/`assertSameMachine` (assumption A5 was grep-only and rated MEDIUM risk); the exact list of suite failures knowingly left for plan 52-07; the `check-npm-packages.mjs:165` cross-wave coupling; the `CLAUDE.md` Compatibility-bullet coupling to plan 52-09 now that `manifest-arg-compat.test.ts` is deleted; and the new `MANUAL_ONLY_TESTS` length so the array's doc-comment count word stays honest.
</output>
