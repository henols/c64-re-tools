---
phase: 52-remove-the-fork-backend
plan: 13
type: execute
wave: 11
depends_on: ["52-12"]
files_modified:
  - .planning/PROJECT.md
  - .planning/codebase/CONCERNS.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/TESTING.md
autonomous: true
gap_closure: true
requirements: [FORKRM-02, FORKRM-03, FORKRM-07]
user_setup: []

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "FORKRM-03: no location in `.planning/PROJECT.md` still states, as the CURRENT answer, that the hardware capability with no route on stock is served by routing the work to the removed backend — the two decision rows outside the already-reversed pair read as superseded, with their supersession dated"
    - "FORKRM-02: the reversal's paper trail is internally consistent — the already-amended decision row, the out-of-scope bullet and the two rows this plan corrects all describe the same disposition, and no reader hitting any one of them first is told a different answer"
    - "The four codebase documents that no marker in the every-session document names as a source each carry a dated note saying their backend content is superseded and by what — so a planning agent reading one of them does not act on a description of a transport that no longer exists"
    - "Edge FORKRM-03/empty — a superseded row is REWORDED to state what is true now plus what it superseded, never blanked. An emptied cell in a decision log reads as 'no decision was made here', which is a different and worse claim than the false one it replaced"
    - "Edge FORKRM-03/encoding — every census command in this plan uses a NUL-tolerant read: shell greps pass `-a`, because one source file in this tree carries a NUL byte that makes a plain grep silently skip it, which has already produced one wrong decision in this project"
    - "Edge FORKRM-07/adjacency — the suite result is compared as a SET, never a count. A run whose failure count exactly equals the recorded floor but whose membership differs by one is a regression, and a count comparison merges those two cases into one indistinguishable answer"
    - "Edge FORKRM-07/empty — a suite run that executed no tests, or a gate whose file list came back empty, is reported as a failure and never as a green floor: the run's total test count is asserted against a floor alongside its failure set"
    - "Edge FORKRM-07/ordering — the extracted failure identifiers are sorted before comparison, so two runs over an unchanged tree produce byte-identical lists and any diff is a real change rather than scheduler order"
    - "FORKRM-07: `npm run test:automated` sits at the documented failure SET, measured with no broker and no emulator process running, and with the exit code read on the same line as the command rather than through a pipe"
    - statement: "A human has confirmed that the two reworded decision rows read as superseded history rather than as current guidance, and that a reader landing on either one first reaches the same disposition as a reader landing on the already-amended row"
      verification: backstop
  artifacts:
    - .planning/PROJECT.md
    - .planning/codebase/CONCERNS.md
    - .planning/codebase/INTEGRATIONS.md
    - .planning/codebase/STRUCTURE.md
    - .planning/codebase/TESTING.md
  key_links:
    - "The two reworded decision rows to the already-amended reversal row and its out-of-scope bullet — three locations a reader can land on first, which must agree; the verification report found the original three-way consistency read had not covered these two"
    - "The four annotated documents to a future codebase-mapping run — the note says what is superseded and points at the record, so the annotation is a bridge until a mapping refreshes them rather than a substitute for one"
  prohibitions:
    - statement: "No pre-existing member of the documented failure floor is repaired in this plan. They are unrelated to this round, and repairing one destroys the only signal that says whether this round regressed anything"
      status: required
      verification: flagged-unverified
    - statement: "The four documents that no marker names as a source are ANNOTATED, not deleted and not rewritten. Deleting a dated snapshot destroys the record of what the codebase looked like; rewriting four documents no projection reads would be a mapping run performed by hand, with every inference risk of one and none of its tree-derived grounding"
      status: required
      verification: flagged-unverified
    - statement: "The suite is never invoked on the full test glob, and the one manual-only file that hangs is never invoked directly either. Both routes reach the same hang, and the second is the one an executor reaches for when told to check a single file"
      status: required
      verification: flagged-unverified
    - statement: "No suite measurement is taken while a broker or emulator process is live. One test reddens deterministically under that condition — not as a flake — and every floor comparison in this plan would then be made against a corrupted baseline"
      status: required
      verification: flagged-unverified
---

<objective>
Close the two residues the verification report named and left open, and measure
the round.

The first is a decision-record residue: two rows in the project document outside
the already-amended reversal pair still describe the hardware capability with no
route on stock as being served by routing the work to the removed backend. The
report rated this optional and lower priority, and it is smaller than the
every-session document was — but it is the same class, it directly contradicts the
requirement that says those losses stop being routed to the removed backend, and
the report's own note is that the earlier three-way consistency read did not cover
these two rows. This plan takes it rather than leaving it, and records that as a
decision.

The second is scope discipline about the four codebase documents that plan 52-12
deliberately did not touch. No marker in the every-session document names any of
them as a source, so none can push a falsehood back over a correction — but all
four describe the removed backend as live, and a planning agent reads them. The
decision recorded here is to annotate rather than rewrite, and the reason matters
more than the outcome.

The third task is the round's measurement: the battery, with the suite compared as
a failure SET.

Purpose: leave no location a reader naturally hits still stating the superseded
answer, and prove the round regressed nothing.
Output: two reworded decision rows, four dated supersession notes, and a recorded
failure set.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-remove-the-fork-backend/52-VERIFICATION.md
@.planning/phases/52-remove-the-fork-backend/52-VALIDATION.md
@.planning/phases/52-remove-the-fork-backend/52-12-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reword the two decision rows that still state the superseded routing as the current answer</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - `.planning/PROJECT.md` around line 437 — the `### Out of Scope` bullet on the client-side write-shadowing mitigation, which states the capability is served by routing the work to the removed backend.
    - `.planning/PROJECT.md` around line 694 — the Key Decisions row making the same claim in the "Good" column, and the adjacent already-reversed rows two lines below it, which show the amendment style this document already uses.
    - `.planning/PROJECT.md`'s already-amended reversal row and its `### Out of Scope` bullet on re-adding a second backend — the exact wording, date format and supersession phrasing this document settled on earlier in this phase. Match it; do not invent a second amendment style.
    - `docs/stock-hard-losses.md` — the record these rows must point a reader at, and the section naming this specific loss.
    - `src/mcp/vice/docs-fork-decision.test.ts` — what this guard actually pins, so the rewording can be checked against it rather than guessed at. The guard is scoped to the reversal row and the out-of-scope bullet; confirm by reading whether it constrains the two rows this task edits.
  </read_first>
  <action>
Reword the two rows so that a reader landing on either one first reaches the same
disposition as a reader landing on the already-amended reversal row.

Both rows currently assert, in the present tense, that the capability the project
cannot reach on stock is covered because the work routes to the removed backend.
That was true when written and is false now. The correction is not deletion: this
is a decision LOG, and the record of what was decided and why is the document's
purpose. Reword each to state what is true now, what it supersedes, and when the
supersession happened — matching the amendment style the reversal row already uses,
including its date format.

The substance to state, confirmed against the acceptance record before writing it:
the loss is permanent rather than routed, it is accepted rather than mitigated, and
the reason it cannot be reached on stock is a hardware property rather than an
unbuilt feature. Point the reader at the acceptance record by its real path.

Do not blank either cell. An emptied cell in a decision log reads as "no decision
was made here", which is a different and worse claim than the false one it
replaced — and it removes the trail that makes a reversal auditable.

Then confirm the three-way consistency the verification report found unverified:
read the already-amended reversal row, its out-of-scope bullet, and these two
reworded rows together, and confirm no one of the four tells a reader a different
current answer than the others. Record what was compared, not merely that it was
compared.

Leave every other row in the decision table alone. Several carry their own
historical annotations about unrelated reversals; those are correct records of
correct history and are not this task's subject.
  </action>
  <reversibility rating="reversible">Two reworded rows in one tracked planning document; the prior text is one revert away and the rows are historical annotations, not live configuration.</reversibility>
  <acceptance_criteria>
    - `grep -ac 'routes to the fork' .planning/PROJECT.md` returns `0` and `grep -ac 'routes SID work to the fork' .planning/PROJECT.md` returns `0`.
    - Both edited rows still exist as rows: the document's total line count is unchanged or higher, and neither row's descriptive cell is empty.
    - Both edited rows cite `docs/stock-hard-losses.md`.
    - Both edited rows carry a supersession date in the same format the already-amended reversal row uses.
    - `node --test docs-fork-decision.test.ts` exits 0 — the guard that pins the reversal is undisturbed.
    - `node --test docs-constraints-sync.test.ts` exits 0 — the `## Constraints` bullet block in this document is byte-identical to its projected copy and was not touched.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && F=.planning/PROJECT.md; printf 'routed_a='; grep -ac 'routes to the fork' $F; printf 'routed_b='; grep -ac 'routes SID work to the fork' $F; printf 'losses_cited='; grep -ac 'stock-hard-losses.md' $F; printf 'lines='; wc -l < $F</automated>
    <fails_when>INVERTED polarity on the first two: a non-zero count is the failure. `routed_a=` or `routed_b=` above `0` means a decision record still states the superseded routing as the current answer. `losses_cited=` below `3` — the document cited the acceptance record twice before this task and both edited rows must now cite it too. `lines=` below its pre-task value — a row was deleted rather than reworded, which removes the trail that makes the reversal auditable.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && printf 'EMPTY_CELLS='; grep -acE '^\|[^|]*\|[[:space:]]*\|' .planning/PROJECT.md</automated>
    <fails_when>Any increase over the value measured before this task — a table row was left with a blank cell, which in a decision log asserts that no decision was made rather than that one was superseded. Measure the baseline first, on the same line, before editing.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-fork-decision.test.ts docs-constraints-sync.test.ts; echo "EXIT=$?"</automated>
    <fails_when>`EXIT=` is non-zero or any `not ok` line appears — either the guard pinning the reversal was disturbed, or the byte-identical bullet block in this document diverged from its projected copy.</fails_when>
  </verify>
  <done>No location in the project document states the superseded routing as the current answer; both reworded rows state the permanent loss, cite the acceptance record and carry a supersession date in the document's own style; and the four-way consistency read the verification report found missing is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate the four codebase documents no projection reads, and record why annotation rather than rewrite</name>
  <files>.planning/codebase/CONCERNS.md, .planning/codebase/INTEGRATIONS.md, .planning/codebase/STRUCTURE.md, .planning/codebase/TESTING.md</files>
  <read_first>
    - `CLAUDE.md`'s marker comments — confirm directly that none of these four documents is named as the source of any marker block, which is the fact this task's whole scope decision rests on. Do not take it from this plan's word.
    - The first twenty lines of each of the four documents — their existing header conventions, including the refresh markers two of them already carry and the supersession note one of them already carries for an earlier audit. The annotation added here must match the convention already in the file, not introduce a fifth style.
    - `.planning/phases/52-remove-the-fork-backend/52-12-SUMMARY.md` — the dated correction note wording plan 52-12 settled on for the three projection sources. This task's note serves a different purpose and must read differently, but the date and the record it points at are the same.
    - `docs/stock-hard-losses.md` — the record the annotation points at.
  </read_first>
  <action>
Add a dated supersession note to the header of each of these four documents, and
record the decision behind doing that rather than rewriting them.

**The note.** In each document's header, beside whatever refresh or analysis-date
marker it already carries, state: that its description of the emulator backend,
that backend's transport, its manifest, its probe, its per-backend selection and
its capability table is SUPERSEDED as of this date; that one emulator target
remains and the capabilities that have no route on it are recorded in
`docs/stock-hard-losses.md`; and that the rest of the document remains a snapshot
of its stated analysis date and has not been re-verified. Match each file's
existing header convention — one already carries a supersession note for an earlier
audit and that is the shape to follow.

Be specific about WHICH content is superseded. A vague note saying the document may
be out of date is worse than none: it invites a reader to discount everything,
including the parts that are still the best record available.

**The decision, recorded in the SUMMARY with its rejected alternatives.** These
four are annotated rather than rewritten because none is named as a source by any
marker in the every-session document, so none can push a falsehood back over a
correction — that is the specific hazard plan 52-12 existed to close, and it does
not apply here. Rewriting four dated snapshots by hand would be a codebase-mapping
run performed without any of a mapping run's tree-derived grounding, carrying every
inference risk and none of the benefit. Deleting them would destroy the record of
what the codebase looked like at its analysis date, which is the only thing a dated
snapshot is for. Annotation is the option that leaves the record intact, warns the
reader precisely, and does not manufacture claims.

State the rejected alternatives explicitly, so a later reader can tell this was a
judgement rather than an omission.

Do not correct any individual claim inside these four documents. Partial correction
is the worst of the three options: it produces a document that is authoritative in
some paragraphs and stale in others with nothing marking the boundary, and the
header note above becomes a lie the moment part of the body is fresher than it
says.
  </action>
  <reversibility rating="reversible">Four header notes. Removing them restores the previous state exactly, and a later mapping run supersedes them with regenerated content.</reversibility>
  <acceptance_criteria>
    - Each of the four documents contains a supersession note in its first 30 lines naming `docs/stock-hard-losses.md`.
    - Each note names the specific superseded subject matter rather than asserting the document is generally out of date.
    - No line outside each document's first 30 lines changed: a diff restricted to line 31 onward is empty for all four files.
    - No marker in `CLAUDE.md` names any of these four documents, confirmed by command rather than assumed.
    - The SUMMARY records the annotate-versus-rewrite-versus-delete decision with both rejected alternatives and the reason each was rejected.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && for f in CONCERNS INTEGRATIONS STRUCTURE TESTING; do printf '%s_note=' "$f"; head -30 .planning/codebase/$f.md | grep -ac 'stock-hard-losses.md'; done</automated>
    <fails_when>Any counter prints `0` — that document carries no supersession note in its header, so a planning agent reading it still finds a description of a removed transport with nothing marking it superseded.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && printf 'BODY_CHANGED:'; for f in CONCERNS INTEGRATIONS STRUCTURE TESTING; do n=$(git diff -U0 -- .planning/codebase/$f.md | grep -acE '^@@ -([3-9][0-9]|[0-9]{3,})' ); [ "$n" = "0" ] || printf ' %s' "$f"; done; echo; printf 'NAMED_AS_SOURCE:'; for f in CONCERNS INTEGRATIONS STRUCTURE TESTING; do grep -aq "GSD:.*source:.*$f" CLAUDE.md && printf ' %s' "$f"; done; echo</automated>
    <fails_when>Any filename printed after `BODY_CHANGED:` — that document was edited below its header, which this task prohibits: partial correction produces a document authoritative in some paragraphs and stale in others with nothing marking the boundary. Any filename printed after `NAMED_AS_SOURCE:` — the scope decision this whole task rests on is wrong for that file, and it belongs in plan 52-12's correction set rather than here; stop and report rather than annotating it.</fails_when>
  </verify>
  <done>Each of the four documents warns a reader precisely which of its content is superseded and points at the record; no body line changed; and the decision to annotate rather than rewrite or delete is recorded with both rejected alternatives.</done>
</task>

<task type="auto">
  <name>Task 3: Run the closing battery and record the failure SET for this gap round</name>
  <files>none — measurement-only task; it runs the battery and records results, modifying no repository file</files>
  <read_first>
    - `.planning/phases/52-remove-the-fork-backend/52-VALIDATION.md` — the three named hazards, all measured, all of which have previously cost a wrong conclusion in this repository: the full test glob hangs; piping the run reports the pipe's exit status and fakes a green baseline; a live broker reddens one test deterministically.
    - `.planning/phases/52-remove-the-fork-backend/52-VERIFICATION.md`'s truth 7 row — the seven-member failure set that verification measured, named member by member. That is this round's reference set.
    - `.planning/phases/52-remove-the-fork-backend/52-10-SUMMARY.md` — the closing set the previous plan recorded, and the two named flakes that sit outside the floor files.
    - `src/mcp/vice/test-gate.mjs` — `automatedTestFiles()` and the manual-only list, the authority on which files the gate runs.
  </read_first>
  <precondition>No broker and no emulator process is running: `ps aux | grep -iE 'vice-broker|x64sc' | grep -v grep` produces no output. One test reddens deterministically under a live broker — not as a flake — so every comparison in this task would otherwise be made against a corrupted baseline.</precondition>
  <action>
Measure the round and compare the suite as a SET.

Run each command below and capture its exit code on the SAME LINE as the command,
using a semicolon and never a pipe. Piping reports the pipe's exit status and has
already faked a green baseline in this repository once:

- the typecheck, in the server package
- the automated test gate, in the server package, redirected to a log file
- the smoke run, in the server package
- the published-package check, the capability-honesty check, the skill tool-coverage
  check and the documented CLI-invocation check, from the repository root

Never invoke the full test glob — it blocks forever on the one manual-only proxy
test file — and never invoke that file directly either, because both routes reach
the same hang and the second is the one an executor reaches for when told to check
a single file.

**Compare the suite as a SET.** Extract the failing test identifiers from the log,
strip their ordinals, and sort them. The reference set is the seven members the
verification report named. Compare membership, not size, and apply these three
readings:

- A member present in the run but absent from the reference set is a REGRESSION.
  Stop and report it; do not close the round.
- A member absent from the run but present in the reference set is a SHRINK, and is
  not a failure. One reference member — the review-disposition check — was recorded
  as expected to go green once the verification report was committed, which has
  since happened, so a six-member run whose missing member is that one is the
  predicted outcome rather than an anomaly.
- The two known flakes that sit outside the floor files may appear or not. Their
  presence is not a regression and their absence is not an improvement.

Note explicitly that this gap round writes new plan and summary documents, which is
exactly the input two of the reference members read. Re-measure those two rather
than assuming their prior state carried over.

**Assert the run was not empty.** Record the total test count alongside the failure
set and check it against a floor — the verification report measured just over four
thousand. A gate that returned an empty file list, or a run that executed nothing,
produces zero failures and reads as a perfect green; that is the single most
dangerous false pass available here.

Do NOT repair any floor member. They are pre-existing and unrelated, and repairing
one destroys the only signal that says whether this round regressed anything.

Finally, re-run the four document guards this round depends on, and record the
four-file projection census plan 52-12 established, to confirm nothing regressed
between that plan's wave and this one.
  </action>
  <reversibility rating="reversible">A measurement run. It changes nothing; it only records.</reversibility>
  <acceptance_criteria>
    - The process check produced no output before any measurement was taken.
    - All battery commands exited 0 except the automated test gate, whose non-zero exit is expected at the documented floor.
    - The suite's sorted failure SET contains no member outside the seven the verification report named, plus at most the two named flakes.
    - The suite's total test count is at least 4000 — a run that executed nothing cannot be reported as green.
    - No member of the failure set is in a file this gap round touched.
    - The four document guards exit 0 and the four-file projection census returns zero.
    - The exact failure set, the total test count and every battery exit code are recorded verbatim in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && ps aux | grep -iE 'vice-broker|x64sc' | grep -vc grep</automated>
    <fails_when>Output is greater than `0` — a broker or emulator is running and will deterministically redden one test, corrupting every measurement below. Stop that process and re-run rather than recording the result.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck; echo "typecheck=$?"; npm run smoke > /tmp/smoke-52-13.log 2>&1; echo "smoke=$?"; cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-npm-packages.mjs > /tmp/pkg-52-13.log 2>&1; echo "pkg=$?"; node scripts/check-skill-capability-honesty.mjs > /tmp/honesty-52-13.log 2>&1; echo "honesty=$?"; node scripts/check-skill-tool-coverage.mjs > /tmp/cov-52-13.log 2>&1; echo "coverage=$?"; node scripts/check-skill-cli-invocations.mjs > /tmp/cli-52-13.log 2>&1; echo "cli=$?"</automated>
    <fails_when>Any of the six `name=` lines reports a non-zero value. Each names a gate: the typecheck covers the build, the smoke run covers the server answering its tool list from the surviving manifest, and the four scripts cover the published tarball contents, the documentation-honesty gate, skills naming only tools that exist, and documented CLI invocations resolving.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated > /tmp/floor-52-13.log 2>&1; echo "SUITE_EXIT=$?"; printf 'TOTAL='; grep -aoE '^# tests [0-9]+' /tmp/floor-52-13.log | grep -aoE '[0-9]+'; echo "--- failure SET ---"; grep -a '^not ok' /tmp/floor-52-13.log | sed 's/[0-9]\+ - //' | sort; echo "--- end ---"</automated>
    <fails_when>The sorted set between the two markers contains any member that is neither one of the seven the verification report named nor one of the two known flakes — that is a regression introduced by this round. A member MISSING from the reference set is not a failure; the review-disposition member was predicted to go green. `TOTAL=` below `4000` — the run executed almost nothing and its empty failure set would read as a perfect green, which is the most dangerous false pass available here. `SUITE_EXIT=` being non-zero is EXPECTED at the documented floor and is not itself the failure signal. Judge by SET membership, never by count, and do not pipe this command into a pager.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-fork-absence.test.ts docs-fork-decision.test.ts docs-constraints-sync.test.ts docs-linerefs.test.ts; echo "GUARDS=$?"; cd /home/henrik/dev/henrik/git/c64-re-tools && printf 'PROJECTION_CENSUS='; grep -acE '(^|[^A-Za-z0-9_.-])(vice\.ts|vice-sync\.ts|vice-probe\.ts|refresh-manifest\.ts|capability-registry\.ts|fork-deleted-tools\.ts)' CLAUDE.md .planning/codebase/ARCHITECTURE.md .planning/codebase/STACK.md .planning/codebase/CONVENTIONS.md | awk -F: '{s+=$2} END {print s+0}'</automated>
    <fails_when>`GUARDS=` is non-zero or any `not ok` line appears — a guard this round depends on regressed between waves. `PROJECTION_CENSUS=` prints anything other than `0` — INVERTED polarity: the every-session document and at least one document its own markers name as a source disagree again about whether a deleted module is live, meaning a later wave undid an earlier one.</fails_when>
  </verify>
  <done>The battery is green on all six named gates; the suite sits at or below the documented failure SET with no member outside it and a total test count proving the run was not empty; the four document guards are green and the projection census is zero; and the exact set is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| decision log → reader | a reader lands on whichever row they hit first; if four locations describe one disposition and two of them state the superseded answer, the document's correctness depends on reading order |
| dated snapshot → planning agent | four documents describe a removed transport as live and nothing marks them superseded; an agent reads one and plans against it |
| measurement → conclusion | a suite measured with a live broker, through a pipe, or compared by count instead of set membership produces a confident wrong answer about whether this round regressed anything |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-50 | Spoofing | the project document's decision log | medium | mitigate | Both rows stating the superseded routing as the current answer are reworded to state the permanent loss, cite the acceptance record and carry a supersession date in the document's own style; a four-way consistency read across every location a reader can land on first is recorded rather than assumed |
| T-52-51 | Repudiation | an emptied decision cell | medium | mitigate | An empty-cell counter measured before and after, plus an explicit prohibition: a blank cell in a decision log asserts that no decision was made, which is a worse claim than the false one it replaced |
| T-52-52 | Spoofing | the four unannotated snapshots | medium | mitigate | Each gains a header note naming the specific superseded subject matter and pointing at the acceptance record; the note is deliberately specific rather than a general staleness warning, which would invite a reader to discount the parts that are still the best record available |
| T-52-53 | Repudiation | the closing measurement | high | mitigate | A precondition requiring no live broker; every exit code captured on the same line rather than through a pipe; the suite compared as a sorted SET with the shrink case explicitly distinguished from the regression case; a total-test-count floor so an empty run cannot read as green; and no floor member repaired |
| T-52-54 | Tampering | a later wave undoing an earlier one | high | mitigate | The four-file projection census from plan 52-12 is re-run in this plan's final wave alongside the four document guards, so a regression between waves is caught rather than assumed absent |
| T-52-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan. The battery runs existing scripts from the committed tree; nothing is fetched, built on demand, or installed |
</threat_model>

## Artifacts this phase produces

**Modified by this plan**
- `.planning/PROJECT.md` — two decision-log locations reworded from stating the superseded routing as current to stating the permanent, accepted loss with a supersession date and a citation to the acceptance record.
- `.planning/codebase/CONCERNS.md`, `INTEGRATIONS.md`, `STRUCTURE.md`, `TESTING.md` — a dated supersession note added to each header, naming the specific superseded subject matter. No body line changed in any of the four.

**Created by this plan**
- The round's recorded closing failure SET, its total test count, and the six battery exit codes.
- The recorded annotate-versus-rewrite-versus-delete decision for the four unprojected documents, with both rejected alternatives and the reason each was rejected.
- The recorded four-way consistency read across every location a reader can land on first for the reversal's disposition — the check the verification report found had not covered these two rows.

**Removed by this plan**
- Nothing. No file, no row, no test and no section is deleted. Both stale rows are reworded in place precisely so the decision trail survives.

**Explicitly NOT touched by this plan**
- Any member of the documented suite failure floor. Repairing one destroys the only signal that says whether this round regressed anything.
- Any body line of the four annotated documents. Partial correction would produce a document authoritative in some paragraphs and stale in others, with the header note becoming a lie the moment part of the body is fresher than it claims.
- Any other row in the project document's decision table. Several carry their own annotations about unrelated reversals; those are correct records of correct history.
- `CLAUDE.md` and the three projection-source documents — corrected in plans 52-11 and 52-12; this plan only censuses them.
- The three warnings and one informational finding the verification report dispositioned as deferred or accepted.

## Flagged planner assumptions (spec-less probe fallback)

**Accounting for this plan: 6 probe-surfaced items for the requirements it owns → 5
authored into `must_haves.truths` + 1 surfaced as a flagged assumption below.
5 + 1 = 6.**

**Aggregate across this gap round: 12 probe-surfaced items → 8 authored as explicit
truths (3 in plan 52-11, 5 here) + 4 surfaced as flagged assumptions (2 in 52-11,
1 in 52-12, 1 here). 8 + 4 = 12.** No item was dismissed and none was silently
dropped.

| Requirement | Category | Probe | Disposition |
|-------------|----------|-------|-------------|
| FORKRM-02 | unclassified | No edge category matched this requirement's shape | **Unresolved, flagged.** FORKRM-02 is already SATISFIED: the reversal row, the out-of-scope bullet and the guard that pins them are all in place and green. This plan's bearing on it is a consistency repair at two adjacent rows the original three-way read did not cover, which is authored as a plain truth above. The probe found no data-shape edge to mint a predicate from, and manufacturing one would be work with no requirement behind it |

**Canon-referral drops (prohibition probe, one-line breadcrumb each, not minted):**
path traversal, injection, and generic compliance — none has a reading in a plan
that reworks two markdown rows, adds four header notes and runs an existing test
battery over the committed tree.

<verification>
No location in the project document states the superseded routing as the current answer; both reworded rows cite the acceptance record and carry a supersession date; the four unprojected documents each carry a header supersession note with no body line changed; all six battery gates exit 0; the suite sits at or below the seven-member reference failure SET with no member outside it and a total test count above the empty-run floor; and the four document guards plus the four-file projection census confirm no earlier wave was undone.

<human-check>
Read the two reworded decision rows alongside the already-amended reversal row and
its out-of-scope bullet — all four locations together. Confirm that each reads as
superseded history rather than as current guidance, and that a reader landing on
any one of the four first reaches the same disposition. This is the read the
verification report found had not been done for these two rows, and it is a reading
judgement rather than a string match.

Then read the four header supersession notes and judge whether each one tells a
reader precisely which content to distrust, rather than inviting them to discount
the whole document. A note that is too broad costs the reader the parts that are
still the best record available; a note that is too narrow leaves the stale
transport description reading as current.
</human-check>
</verification>

<success_criteria>
- No document a reader naturally reaches still states, as the current answer, that a permanently lost capability is served by routing work to the removed backend.
- Every codebase snapshot that describes the removed backend either has been corrected or says in its own header which of its content is superseded and by what.
- The suite regressed nothing: its failure set contains no member outside the reference set, measured with no live broker and compared as a set.
- The projection census established in the previous wave still returns zero.
</success_criteria>

<output>
Create `.planning/phases/52-remove-the-fork-backend/52-13-SUMMARY.md` when done. Record: both reworded rows verbatim, before and after; the four-way consistency read naming each location compared; the annotate-versus-rewrite-versus-delete decision with both rejected alternatives; the four header notes verbatim; the closing failure SET with every member attributed to the reference set or to a named flake, and any reference member MISSING from the run called out as the predicted shrink; the total test count; the six battery exit codes; the projection census result; and the outcome of both human checks.
</output>



