---
phase: 52-remove-the-fork-backend
plan: 11
type: execute
wave: 9
depends_on: ["52-10"]
files_modified:
  - src/mcp/vice/docs-fork-absence.test.ts
  - CLAUDE.md
  - .planning/codebase/ARCHITECTURE.md
autonomous: true
gap_closure: true
requirements: [FORKRM-01, FORKRM-04, FORKRM-06]
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "FORKRM-01 / FORKRM-06: a boundary-aware census of the six deleted module filenames across the whole of `CLAUDE.md` returns zero matching lines — the document loaded into every session names only modules that exist on disk"
    - "FORKRM-01: `CLAUDE.md`'s Key Dependencies and Platform Requirements sections describe stock upstream VICE as the only emulator target, and name no non-upstream launch flag as a requirement"
    - "FORKRM-04: `CLAUDE.md` describes no refusal mechanism that was removed with the fork transport — its Layers and Component Responsibilities prose names only refusal/allowlist mechanisms that exist on disk"
    - "FORKRM-06: `CLAUDE.md` describes the advertised tool surface as an offline read of the one committed manifest snapshot, and names nothing that regenerates a manifest from a live host"
    - "`src/mcp/vice/vice-errors.ts:158` is cited as `ViceError`'s home in the Error Handling convention, and `vice-errors.ts` as `MachineRestartedError`'s — both confirmed against the file before the citation is written"
    - "Edge FORKRM-01/adjacency — a filename that merely TOUCHES a forbidden one as a substring is separated from a real citation, not merged with it: the predicate reports `vice.ts` for the path `src/mcp/vice/vice.ts` and reports nothing for `device.ts`, proven by a committed planted-violation pair rather than by inspection"
    - "Edge FORKRM-01/empty — the prose scan cannot pass vacuously in either direction: its scanned-prose-file count clears a floor derived from the real count, and its deleted-filename set is asserted non-empty. An emptied token set or an empty walk is a guard that agrees with everything, and is the single most important predicate in this round"
    - "Edge FORKRM-01/ordering — the predicate returns its findings sorted and de-duplicated, so two runs over an unchanged corpus produce byte-identical offender lists and any diff between runs means a real change rather than iteration order"
    - "`docs-constraints-sync.test.ts` stays green: the `## Constraints` bullet block is still byte-identical between `CLAUDE.md` and `.planning/PROJECT.md`, because this plan does not touch it"
    - statement: "A human has read the rewritten architecture prose and confirmed each replacement sentence states what the system DOES today, rather than merely deleting the false sentence and leaving a reader with a gap where an explanation used to be"
      verification: backstop
  artifacts:
    - src/mcp/vice/docs-fork-absence.test.ts
    - CLAUDE.md
    - .planning/codebase/ARCHITECTURE.md
  key_links:
    - "`docs-fork-absence.test.ts` to its prose population (`CLAUDE.md`, `README.md`, every `src/skills/**/*.md`) — the same population test 3 and test 4 already scan for forbidden identifiers, now also scanned for deleted filenames cited as live components. Reusing the population rather than minting a second one keeps one answer to 'what does this guard cover'"
    - "`CLAUDE.md`'s `<!-- GSD:architecture-start source:ARCHITECTURE.md -->` marker to `.planning/codebase/ARCHITECTURE.md` — the marker DECLARES a provenance relationship. Correcting only the projected copy leaves the declared source able to overwrite the correction, which is exactly the failure `docs-constraints-sync.test.ts`'s own header records for the PROJECT.md pair"
    - "`docs-fork-absence.test.ts` to `shipped-modules.ts` — `codeOnly()` and `shippedTsModules()` stay the basis for the CODE half of the guard; the new prose half deliberately does not use `codeOnly()`, because in prose the literal mention IS the violation"
  prohibitions:
    - statement: "The guard is not widened into a form that false-positives on correct text. A guard that reds a correct tree gets switched off rather than obeyed — the failure mode `docs-dangling-refs.test.ts`'s own header already records. Two measured traps: a naive substring test for the shortest deleted filename also matches `device.ts`, and a bare scan for the removed backend's name also matches the Debian codename `forky` that this document legitimately carries"
      status: required
      verification: flagged-unverified
    - statement: "Every false sentence is CORRECTED to what the system does today, never merely deleted. A document that describes less than the system does is a second defect, not a fix — and the deletion-shaped edit is the one an executor reaches for under time pressure"
      status: required
      verification: flagged-unverified
    - statement: "The `### Constraints` bullet block inside the first marker block of `CLAUDE.md` is not edited. It is held byte-identical against its source document by a committed guard, and an edit here without the matching edit there reds that guard in the same commit"
      status: required
      verification: flagged-unverified
    - statement: "The guard's scanned population does not grow to include the planning tree. That tree records this removal in detail and legitimately quotes every removed name while describing its removal; scanning it produces false positives, after which the guard gets switched off rather than obeyed"
      status: required
      verification: flagged-unverified
    - statement: "No planning vocabulary enters `CLAUDE.md`. It is a product file: no planning-tree path, no slash-command name, no phase or plan number, no bare decision or gap id, no cross-reference to a requirements or roadmap document. The one legal citation form is a decision id that resolves outside the planning tree, cited with its document named"
      status: required
      verification: flagged-unverified
    - statement: "No architecture claim is written that was not first read off the tree. Replacing a false sentence with a confident-sounding invented one is strictly worse than the original defect, because the next reader has no signal that it needs checking"
      status: required
      verification: flagged-unverified
---

<objective>
Correct the false architecture prose in the one document this project loads into
every working session, and give that class of falsehood a mechanical detector.

The phase's seven numbered success criteria are all met. Its Goal sentence has a
fourth clause the criteria do not enumerate — "the decision records retaining it
are gone" — and `CLAUDE.md` fails it. Twelve sentences across its Technology
Stack, Conventions, Architecture, Platform Requirements and Error Handling
sections still describe deleted modules as live components with live
responsibilities, and still describe the emulator dependency as a non-upstream
patched build.

The reason this survived every guard in the phase is precise and worth stating,
because it dictates the shape of the fix: `docs-fork-absence.test.ts` scans for
nine forbidden CODE IDENTIFIERS as literal strings, and separately checks that six
DELETED MODULE FILENAMES have not reappeared **on disk or in the published file
list**. It never checks whether those filenames appear in PROSE. A table row
naming a deleted file as the owner of a live responsibility therefore passes
untouched. The detector and the defect are one gap apart.

Purpose: make the every-session document true, and make the next occurrence of
this failure class fail a test instead of surviving a phase.
Output: a widened guard with a prose-citation check, a corrected `CLAUDE.md`, and
the tracer's matching correction in the document `CLAUDE.md` names as its source.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-remove-the-fork-backend/52-VERIFICATION.md
@.planning/phases/52-remove-the-fork-backend/52-VALIDATION.md
@src/mcp/vice/docs-fork-absence.test.ts
@src/mcp/vice/docs-constraints-sync.test.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — one false claim, corrected in both copies, with the detector that proves the class</name>
  <files>src/mcp/vice/docs-fork-absence.test.ts, CLAUDE.md, .planning/codebase/ARCHITECTURE.md</files>
  <read_first>
    - `src/mcp/vice/docs-fork-absence.test.ts` in full — in particular its `DELETED_MODULES` constant, `findForbiddenIdentifiers()` / `scanTextForForbiddenIdentifiers()` (predicates that RETURN findings so planted tests drive the real rule), `skillMarkdownFiles()`, `EXEMPT_RELATIVE_PATHS`, and the numbered test layout. The new checks extend this file's existing conventions; they do not introduce a second style.
    - `.planning/phases/52-remove-the-fork-backend/52-VERIFICATION.md` — the `gaps` block in the frontmatter, which names the falsifiable lines and states exactly why the existing guard does not see them.
    - `CLAUDE.md` lines 166-267 — the `## Architecture` marker block, and specifically its Component Responsibilities table. Note the opening marker on line 166 declares a source document.
    - `.planning/codebase/ARCHITECTURE.md` lines 80-120 — the Component Responsibilities table this block is projected from, which carries the same claim in its own words.
    - `src/mcp/vice/stock-connect.ts` (`stockConnect`, `stockReconnect`, `stockDisconnect`) and `src/mcp/vice/stock-protocol.ts` (the framing constants and `CommandType`) — read enough of each to state truthfully what owns the transport today before writing any sentence that claims it.
  </read_first>
  <action>
Cut one thin path through all three layers this plan touches — the detector, the
projected document, and the document the projection declares as its source — using
the single most falsifiable claim in the gap: the Component Responsibilities row
that assigns the transport seam to a module that is not on disk.

**Layer 1 — the detector.** In `src/mcp/vice/docs-fork-absence.test.ts`, add a
prose-citation predicate alongside the existing identifier predicates, following
the same discipline they already follow (a named non-empty constant set, a pure
function that RETURNS findings, no second implementation of the rule):

- A module-level regex built from the existing `DELETED_MODULES` set, matched
  **boundary-aware**: a match requires the character before the filename to be
  absent (start of line) or to be something other than a letter, digit,
  underscore, hyphen or dot. Escape the dot in each filename. This boundary is not
  decoration — a naive substring test for the shortest entry in that set also
  matches the real, current filename `device.ts`, which is a measured false
  positive and precisely the thing that gets a guard switched off.
- `findDeletedModuleCitations(text: string): string[]`, returning the matched
  filenames **sorted and de-duplicated** so two runs over an unchanged corpus
  produce byte-identical output and any diff between runs means a real change.
- A path-exempting wrapper mirroring `scanTextForForbiddenIdentifiers()`, applying
  the existing `EXEMPT_RELATIVE_PATHS` by exact path first.

Add the planted-violation pair that makes the boundary real rather than claimed:
an in-memory body citing the deleted transport module IS reported, and an
in-memory body containing only `device.ts` reports nothing. Drive the REAL
predicate in both — no fixture files, no second copy of the rule.

Extend the existing non-vacuity test (test 1) to assert the citation regex's
source string is non-empty, so an emptied pattern cannot make the new checks
vacuously pass.

Do **not** wire the predicate over the real files in this task. Eleven further
false sentences remain in the projected document, so a real-corpus scan would be
red for reasons this task has not yet fixed, and a red that is expected teaches an
executor to ignore it. Task 3 turns the scan on, after Task 2 has made it
satisfiable.

**Layer 2 — the projected document.** In `CLAUDE.md`'s Component Responsibilities
table, replace the transport-seam row. The true statement to write, after
confirming it against the two modules named in `<read_first>`: the one place that
speaks VICE's binary monitor is `src/mcp/vice/stock-connect.ts`, which owns
session setup, reconnect and teardown, over the length-prefixed binary framing
defined in `src/mcp/vice/stock-protocol.ts`; the refusal mechanism the old row
attributed to this seam does not exist and its clause is dropped rather than
reworded. Delete the adjacent liveness-probe row outright: the module it names is
gone and no module replaced it, so the honest edit is to remove the row, not to
re-point it at an unrelated file.

**Layer 3 — the declared source.** Make the same correction in
`.planning/codebase/ARCHITECTURE.md`'s own Component Responsibilities table, in
its own wording. The marker that opens `CLAUDE.md`'s architecture block names that
file as this block's source. Correcting only the projected copy leaves a document
that a regeneration can push back over the correction — the exact failure the
`## Constraints` byte-identity guard exists in this repository to prevent, one
level up. Correcting the source row here is what makes this a vertical cut rather
than two half-fixes.

Leave every other row and section of both documents alone; Task 2 and plan 52-12
take them.
  </action>
  <reversibility rating="reversible">A new test predicate and two table rows. The predicate can be narrowed or moved to a sibling file later; nothing imports it and no behaviour depends on it.</reversibility>
  <acceptance_criteria>
    - `node --test docs-fork-absence.test.ts` exits 0, and its TAP summary reports strictly more tests than the 13 the file carried before this task.
    - The planted-violation pair exists and both directions assert: a body citing the deleted transport module returns exactly that filename; a body containing only `device.ts` returns an empty array.
    - `node -e` driving the exported-or-local citation predicate against the string `device.ts` prints an empty result, and against `src/mcp/vice/vice.ts` prints a non-empty result. <!-- planner-discipline-allow: vice.ts -->
    - `grep -ac 'Liveness probe' CLAUDE.md` returns `0`.
    - `.planning/codebase/ARCHITECTURE.md` and `CLAUDE.md` each contain a Component Responsibilities row naming `stock-connect.ts`, and neither contains a transport-seam row naming a file absent from `src/mcp/vice/`.
    - `node --test docs-constraints-sync.test.ts` exits 0 — the byte-identical `## Constraints` block was not disturbed.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-fork-absence.test.ts docs-constraints-sync.test.ts; echo "EXIT=$?"</automated>
    <fails_when>`EXIT=` is non-zero, or any `not ok` line appears, or the combined TAP summary reports fewer than 20 tests — the two files carried 13 and 7 tests before this task, so a lower total means a check was dropped rather than added.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && printf 'claude_seam='; grep -ac 'stock-connect.ts' CLAUDE.md; printf 'arch_seam='; grep -ac 'stock-connect.ts' .planning/codebase/ARCHITECTURE.md; printf 'claude_probe_row='; grep -ac 'Liveness probe' CLAUDE.md</automated>
    <fails_when>`claude_seam=` or `arch_seam=` prints `0` — the corrected row was not written into that copy, so the two documents disagree and the declared source can still overwrite the projection. `claude_probe_row=` prints anything other than `0` — the row naming a module that no longer exists is still present.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck; echo "typecheck=$?"</automated>
    <fails_when>`typecheck=` is non-zero — the new predicate does not compile under the project's strict, `verbatimModuleSyntax` configuration.</fails_when>
  </verify>
  <done>A boundary-aware deleted-filename citation predicate exists, is committed, carries a planted-violation pair proving it separates a real citation from a substring neighbour, and one false claim is corrected identically in the projected document and in the document that projection declares as its source.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the remaining eleven false claims across CLAUDE.md's generated sections</name>
  <files>CLAUDE.md</files>
  <read_first>
    - `CLAUDE.md` lines 53-267 in full — the three generated marker blocks. Every claim this task corrects lives here; nothing above line 52 is in scope.
    - `.planning/phases/52-remove-the-fork-backend/52-VERIFICATION.md` — the per-line `artifacts` list in the `gaps` block, which quotes each false sentence verbatim so the executor can confirm it is editing the right one.
    - `src/mcp/vice/vice-errors.ts` lines 150-200 — confirm the class declarations and their line numbers before writing either citation. The previous citation in this document was wrong in both file AND line, so copying a line number without reading it is how this defect was made.
    - `src/mcp/vice/stock-dispatch.ts` — `manifestPathForBackend()`, `resolveAdvertisedToolDefinition()`, `ensureStockSession()`, `clearHeldStockSession()`, `withStockSession()`, `withDerivedTool()`. This is the module that actually holds the mutable session state and resolves the advertised surface.
    - `src/mcp/vice/repo-root.ts` — `repoRoot()`, `toolsDir()`, `supervisorDir()`, for the bounded ride-along below.
    - `CLAUDE.md` line 98's constraint bullet on the single tool-written root — the in-document authority for that ride-along. Read it; do not edit it.
  </read_first>
  <action>
Correct every remaining false claim in the three generated blocks. Eleven sites,
each replaced with what the tree actually does — not deleted, not softened, not
left as a gap where an explanation used to be.

**Technology Stack block.** The colocated-test-file example names a module that
is not on disk and then admits it has no test file; replace it with a real
colocated pair that exists, such as `stock-dispatch.ts` and its sibling test. The
Key Dependencies entry for the emulator binary describes it as a non-upstream
patched build exposing a proprietary server flag, calls it the load-bearing
dependency of the whole tool surface, and points at a migration plan for work that
has already shipped — replace the whole entry with the truth: `x64sc` is stock
upstream VICE, any unpatched build from a package manager, driven over its binary
monitor and its text channel; the three capabilities that have no route on stock
are recorded in `docs/stock-hard-losses.md`. The Platform Requirements line
repeating the patched-build claim gets the same correction in one sentence.

**Conventions block.** The Error Handling entry cites the base error class at a
file and line that are both wrong. Replace with the confirmed location read in
`<read_first>`, and correct the sibling citation for the restart error type in the
Architecture block's Error Handling section the same way.

**Architecture block.** Four sites:
- The Component Responsibilities row for manifest regeneration describes a module
  that regenerates the advertised surface from a live host. Nothing does that any
  more, and the requirement this phase closed forbids it. Replace the row with the
  true one: the advertised tool surface is a committed snapshot,
  `src/mcp/vice/tools-manifest.stock.json`, read offline at `tools/list`, resolved
  through `stock-dispatch.ts`.
- The Layers entry describing the server layer's contents names a refusal
  mechanism that was removed with the transport. State what the layer contains
  now instead: the wire protocol, the binary-monitor transport and the dispatch
  seam.
- The Data Flow entry and the Architectural Constraints entry both attribute
  mutable module-level transport state to a deleted module. Attribute it to the
  module that actually holds it, confirmed by reading the session-clearing
  function named in `<read_first>`.
- The Entry Points list carries a standalone script entry for the manifest
  regenerator. Remove the entry; there is no replacement script, and inventing one
  would be worse than the original defect.

**Bounded ride-along, named as such so it is not mistaken for scope growth.**
Three adjacent sentences in the same Architecture block still name the previous
tool-written locations — the old supervisor directory in the repo-root
responsibility row and again in the Data Flow section, and the old deployed-script
directory in the resource-deployment row. This document's own constraint bullet
already states the single tool-written root that superseded all three in a
documented clean break with no dual-read. Correct the three sentences to the
current root and its subdirectories while rewriting the surrounding prose. This is
a consistency repair against an authority already inside this file, costing three
lines inside prose already being edited — it is not a second investigation, and no
fourth site is in scope.

**Two hard constraints on how this edit is made.** Do not touch the bullet list
inside the first marker block: it is held byte-identical against its source
document by a committed guard, and editing one copy without the other reds that
guard in the same commit. And do not introduce planning vocabulary — this file
ships as the project's instruction document and has no planning tree behind it;
cite `docs/stock-hard-losses.md` by its real path where a reason is needed, and
cite nothing that resolves only inside the planning tree.

Confirm every replacement identifier against the tree before writing it. A
confident-sounding invented sentence is strictly worse than the false one it
replaces, because the next reader has no signal that it needs checking.
  </action>
  <reversibility rating="reversible">Prose corrections in one tracked document, recoverable from git in a single revert.</reversibility>
  <acceptance_criteria>
    - `grep -acE '(^|[^A-Za-z0-9_.-])(vice\.ts|vice-sync\.ts|vice-probe\.ts|refresh-manifest\.ts|capability-registry\.ts|fork-deleted-tools\.ts)' CLAUDE.md` returns `0`.
    - `grep -acE '(^|[^A-Za-z0-9_.-])tools-manifest\.json' CLAUDE.md` returns `0`, while `grep -ac 'tools-manifest.stock.json' CLAUDE.md` returns at least `1` — the surviving manifest is still named.
    - `grep -ac 'mcpserver' CLAUDE.md` returns `0`.
    - `grep -ac 'custom/patched' CLAUDE.md` returns `0`.
    - `grep -ac 'vice-errors.ts' CLAUDE.md` returns at least `2` — both error-class citations were re-pointed, not just one.
    - `grep -ac 'vice-supervisor' CLAUDE.md` returns `0` and `grep -ac '.c64-re-tools' CLAUDE.md` returns at least `2` — the ride-along landed.
    - `node --test docs-constraints-sync.test.ts` exits 0 — the byte-identical bullet block is untouched.
    - `CLAUDE.md` contains no planning-tree path, no slash-command name, and no phase or plan number outside the pre-existing execution-isolation section that already carries the project's own policy text.
    - Line count of `CLAUDE.md` did not drop by more than 6 — a large shrink means sentences were deleted rather than corrected.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && printf 'deleted_files='; grep -acE '(^|[^A-Za-z0-9_.-])(vice\.ts|vice-sync\.ts|vice-probe\.ts|refresh-manifest\.ts|capability-registry\.ts|fork-deleted-tools\.ts)' CLAUDE.md; printf 'old_manifest='; grep -acE '(^|[^A-Za-z0-9_.-])tools-manifest\.json' CLAUDE.md; printf 'stock_manifest='; grep -ac 'tools-manifest.stock.json' CLAUDE.md; printf 'fork_flag='; grep -ac 'mcpserver' CLAUDE.md; printf 'patched='; grep -ac 'custom/patched' CLAUDE.md</automated>
    <fails_when>INVERTED polarity on the first, second, fourth and fifth counters: for these removals a non-zero count is the failure and `0` is the success. `deleted_files=` above `0` means a module absent from disk is still named as a live component. `old_manifest=` or `fork_flag=` or `patched=` above `0` means a claim this phase falsified survives. `stock_manifest=` printing `0` is the opposite failure — the surviving manifest was deleted from the prose along with the false ones, leaving the document describing less than the system does.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && printf 'errors_cited='; grep -ac 'vice-errors.ts' CLAUDE.md; printf 'old_supervisor='; grep -ac 'vice-supervisor' CLAUDE.md; printf 'tools_root='; grep -ac 'c64-re-tools' CLAUDE.md; printf 'lines='; wc -l < CLAUDE.md</automated>
    <fails_when>`errors_cited=` is below `2` — one of the two error-class citations was left pointing at a deleted file. `old_supervisor=` is above `0` — the superseded tool-written location survives. `tools_root=` is below `2`. `lines=` is below `328` — `CLAUDE.md` had 334 lines before this task and a drop beyond six means sentences were deleted rather than corrected, which this plan prohibits.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-constraints-sync.test.ts docs-linerefs.test.ts; echo "EXIT=$?"</automated>
    <fails_when>`EXIT=` is non-zero or any `not ok` line appears. The first guard reds if the byte-identical bullet block was disturbed; the second reds if a line-number citation this document carries no longer resolves to what it claims.</fails_when>
  </verify>
  <done>Every false architecture, stack, platform and error-handling claim in `CLAUDE.md` states what the tree does today; the surviving manifest, the real error-class locations and the real transport modules are all named; the byte-identical bullet block and the line-reference guard are both still green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the citation predicate over the real prose corpus, with a floor that proves it scanned something</name>
  <files>src/mcp/vice/docs-fork-absence.test.ts</files>
  <read_first>
    - `src/mcp/vice/docs-fork-absence.test.ts` as left by Task 1 — the new predicate, and tests 3 and 4, whose prose population (`README.md`, `CLAUDE.md`, and every markdown file under the skills tree) this task reuses rather than minting a second one.
    - `src/mcp/vice/shipped-modules.ts` — `shippedTsModules()` and `codeOnly()`. Read enough to state in the file's own comment WHY the new prose check deliberately does not use `codeOnly()`.
    - The header of `src/mcp/vice/docs-dangling-refs.test.ts` — its recorded reasoning about a guard that false-positives being switched off rather than obeyed, which is the standard this task's scope decision is held to.
  </read_first>
  <action>
Turn the predicate on over the real corpus, and make it impossible for the check
to pass by scanning nothing.

Add a numbered test that walks the same prose population tests 3 and 4 already
walk — the project instruction file, the readme, and every markdown file under the
skills tree — applies the path-exempting citation wrapper from Task 1 to each, and
asserts the collected offenders list is empty. Report offenders as
`path: filename, filename` exactly as the sibling checks do, so one failure message
names every site at once rather than one per run.

Add a second forbidden-phrase set for stale transport vocabulary that is
distinctive enough to be safe: the removed backend's launch flag, and the removed
manifest's filename matched boundary-aware so the surviving `.stock.` variant
cannot trip it. Do **not** add the removed backend's bare name to any scanned set —
this repository's instruction file legitimately carries a Debian release codename
containing that word as a substring, so a bare scan for it is permanently red on a
correct tree, which is the exact failure this file's own header warns against.

Extend the non-vacuity test with the `empty` predicate that matters most here: an
absence guard whose scanned set is empty, or whose token list is empty, passes
vacuously and protects nothing. Assert the prose population count clears a floor
derived from the real count with headroom — the real count today is twenty files —
and assert both new constant sets are non-empty.

Record the scope decision in the file's own comment, because the verification
report left the choice open and the next reader deserves the reason rather than
the outcome: the prose citation check lives in this file rather than in a sibling
guard, because it scans the same population, exempts by the same path list, and
answers the same question — has the removal come back — so splitting it across two
files would produce two partial answers to one question and two places to forget
to widen. State also that the check applies to prose only and deliberately not to
the shipped TypeScript modules, which carry intentional historical comments naming
removed modules while warning a future reader not to reintroduce them; in code
those comments are the documentation, in prose the mention is the claim.

State the scanned-population boundary explicitly, extending the exclusion the file
already documents: the planning tree stays out. It records this removal in detail
and legitimately quotes every removed name while describing its removal.
  </action>
  <reversibility rating="reversible">Two new checks in an existing guard. Narrowing or removing them restores the previous behaviour exactly.</reversibility>
  <acceptance_criteria>
    - `node --test docs-fork-absence.test.ts` exits 0 with no `not ok` line.
    - The file asserts a prose-population floor of at least 18 and both new constant sets are asserted non-empty, so the check cannot pass by scanning nothing.
    - Temporarily appending a line citing a deleted module to `README.md` makes the new check RED, and reverting it makes it green again — observed, then reverted, with the working tree left clean.
    - The guard runs in the automated gate: `automatedTestFiles()` still reports the file as included.
    - The file's comment states the widen-vs-sibling scope decision and its reason, the prose-only boundary and its reason, and the continued planning-tree exclusion.
    - `git status --porcelain` is clean of any leftover planted violation before the task is considered done.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-fork-absence.test.ts; echo "EXIT=$?"</automated>
    <fails_when>`EXIT=` is non-zero, any `not ok` line appears, or the TAP summary reports fewer than 16 tests — the file carried 13 before this plan and this plan adds a citation check, a phrase check and at least two planted-violation cases, so a lower total means one was dropped.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && printf 'PLANTED: '; printf '\nSee `src/mcp/vice/refresh-manifest.ts` for the live regeneration path.\n' >> README.md; (cd src/mcp/vice && node --test docs-fork-absence.test.ts > /tmp/planted-52-11.log 2>&1); echo "planted_exit=$?"; git checkout -- README.md; printf 'REVERTED: '; (cd src/mcp/vice && node --test docs-fork-absence.test.ts > /tmp/reverted-52-11.log 2>&1); echo "reverted_exit=$?"; printf 'tree_clean='; git status --porcelain -- README.md | wc -l</automated>
    <fails_when>`planted_exit=` is `0` — the guard stayed green while a deleted module was cited as a live regeneration path in scanned prose, which means the check is not actually wired to the corpus and protects nothing. Or `reverted_exit=` is non-zero — the guard is red on a correct tree. Or `tree_clean=` is anything but `0` — the planted violation was left behind in the working tree.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node -e "const{automatedTestFiles}=await import('./test-gate.mjs');console.log('inGate='+automatedTestFiles(process.cwd()).includes('docs-fork-absence.test.ts'))" --input-type=module; node -e "const p=require('./package.json');console.log('inFiles='+p.files.includes('docs-fork-absence.test.ts'))"</automated>
    <fails_when>`inGate=` prints `false` — the widened guard exists but never runs, which is indistinguishable from not having widened it. `inFiles=` prints `true` — a repository-verification guard leaked into the published tarball, which every sibling guard in this directory deliberately avoids.</fails_when>
  </verify>
  <done>The citation and phrase checks run against the real prose corpus, are proven red on a planted violation and green on the reverted tree, cannot pass by scanning nothing, and carry their own recorded scope reasoning.</done>
</task>

</tasks>

<assumption_delta_decision>
**Noun now primary:** backend — as the type `ViceBackend`, already narrowed by this
phase to the single literal `"stock"`.

**Decision: `no-change`.**

The detector fired on one `pluralization` signal, the bare word "also" inside the
sentence *"which also records that textual duplication across the nine skills is
12"*. That is a lexical false positive with no relation to an identity model: the
word carries no cardinality claim about any noun in this phase.

The promote/demote this signal would normally prompt already happened, in this
phase's own earlier plans, when the backend type collapsed from a two-literal
union to one literal. This gap round introduces no second case of anything — it
corrects prose and widens one guard. Recorded here so the next reader does not
re-litigate the same false positive.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated document → its declared source | `CLAUDE.md`'s architecture block declares a source file. Correcting only the projected copy leaves the declared source able to push the falsehood back over the correction |
| stale prose → every future session | this document is loaded as project instructions in every session; a false sentence here is acted on, not merely read |
| guard scope → guard trust | a guard that reds a correct tree gets switched off rather than obeyed, which is strictly worse than not having written it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-41 | Spoofing | `CLAUDE.md`'s architecture prose | high | mitigate | Every deleted module named as a live component is replaced with the module that actually owns the responsibility, confirmed by reading it; a boundary-aware census of all six deleted filenames must return zero lines |
| T-52-42 | Tampering | the widened guard passing vacuously | high | mitigate | A prose-population floor derived from the real count with headroom, non-empty assertions on both new constant sets, and a planted violation observed RED against the real corpus then reverted — the `empty` edge, and the single most important predicate in this round |
| T-52-43 | Tampering | the guard false-positiving on correct text | high | mitigate | Boundary-aware matching with a committed planted-violation control proving `device.ts` is not reported; the removed backend's bare name deliberately excluded from every scanned set because a Debian release codename in this document contains it as a substring |
| T-52-44 | Repudiation | a correction that deletes rather than corrects | medium | mitigate | A line-count floor on `CLAUDE.md` in Task 2's verify, a positive assertion that the surviving manifest is still named, and an explicit prohibition against deletion-shaped edits |
| T-52-45 | Tampering | the byte-identical bullet block | high | mitigate | `docs-constraints-sync.test.ts` is run as a verify command in two of the three tasks; the plan touches no file inside that block's region |
| T-52-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan. No package-manager command runs, and no external tool is fetched or built |
</threat_model>

## Artifacts this phase produces

**Created by this plan**
- `findDeletedModuleCitations(text)` — new pure predicate in `src/mcp/vice/docs-fork-absence.test.ts`. Boundary-aware, sorted, de-duplicated. Returns findings rather than asserting, so planted-violation tests drive the real rule.
- A path-exempting wrapper for the above, mirroring the existing `scanTextForForbiddenIdentifiers()` and reusing `EXEMPT_RELATIVE_PATHS`.
- A module-level regex constant built from the existing `DELETED_MODULES` set, dot-escaped and boundary-anchored.
- A second forbidden-phrase constant set for stale transport vocabulary (the removed launch flag; the removed manifest filename, boundary-matched so the surviving `.stock.` variant cannot trip it).
- Two new numbered checks in `docs-fork-absence.test.ts` (prose citations; stale transport phrases) plus at least two planted-violation cases and an extended non-vacuity test carrying a prose-population floor.
- A recorded scope decision in that file's own comment: widen-in-place rather than sibling guard, and why; prose-only rather than code-and-prose, and why.

**Modified by this plan**
- `CLAUDE.md` — twelve false claims corrected across the Technology Stack, Conventions and Architecture blocks, plus three bounded ride-along corrections to superseded tool-written locations.
- `.planning/codebase/ARCHITECTURE.md` — the tracer's matching correction to one Component Responsibilities row.

**Removed by this plan**
- Two `CLAUDE.md` rows whose subject no longer exists and has no replacement: the liveness-probe component row, and the manifest-regeneration entry-point list item. Both removed rather than re-pointed, because inventing a replacement would be worse than the original defect.

**Explicitly NOT touched by this plan**
- The `### Constraints` bullet block inside `CLAUDE.md`'s first marker block — held byte-identical against its source document by a committed guard.
- `.planning/codebase/STACK.md`, `CONVENTIONS.md` and the rest of `ARCHITECTURE.md` — plan 52-12.
- `.planning/PROJECT.md` — plan 52-13.
- The documented suite failure floor. No floor member is repaired here.
- The three warnings and one informational finding the verification report dispositioned as deferred or accepted.

## Flagged planner assumptions (spec-less probe fallback)

This phase has no SPEC, so the edge report was produced by the deterministic probe
over the requirement texts. **Accounting for this plan: 5 probe-surfaced items for
the requirements it owns → 3 authored into `must_haves.truths` + 2 surfaced as
flagged assumptions below. 3 + 2 = 5.**

| Requirement | Category | Probe | Disposition |
|-------------|----------|-------|-------------|
| FORKRM-04 | unclassified | No edge category matched this requirement's shape | **Unresolved, flagged.** FORKRM-04 is already SATISFIED and its subject — a removed refusal mechanism and an untouched allowlist — is code this round does not touch. This plan's only bearing on it is that `CLAUDE.md` must stop describing the removed refusal as a live responsibility, which is covered by a plain truth above. No edge predicate is minted; inventing one would be work with no requirement behind it |
| FORKRM-06 | unclassified | No edge category matched this requirement's shape | **Unresolved, flagged.** Same reading: the single-manifest requirement is SATISFIED in the tree, and this plan's bearing is documentary only — the prose must describe an offline snapshot read rather than a live regeneration. Covered by a plain truth above; no edge predicate minted |

**Canon-referral drops (prohibition probe, one-line breadcrumb each, not minted):**
path traversal in the guard's file walk, and content injection through scanned
markdown — both canon security classes owned by code review, and both inert here
since the walk reads a fixed, repository-relative population and never executes
what it reads.

<verification>
`node --test docs-fork-absence.test.ts docs-constraints-sync.test.ts docs-linerefs.test.ts docs-fork-decision.test.ts` green; `npm run typecheck` exits 0; a boundary-aware census of the six deleted module filenames over `CLAUDE.md` returns zero lines while `tools-manifest.stock.json`, `vice-errors.ts` and `stock-connect.ts` are all still named; the planted README violation was observed RED and the revert observed GREEN with a clean working tree.

<human-check>
Read the rewritten Technology Stack, Conventions and Architecture blocks of
`CLAUDE.md` end to end and judge whether each replacement sentence states what the
system DOES — naming a module that exists and the responsibility it actually owns
— rather than merely removing the false sentence. A census can prove a deleted
filename is absent; it cannot prove the sentence that replaced it is true, or that
a reader who needed that explanation still gets one.

Pay particular attention to the two rows this plan deletes outright rather than
re-points, and confirm that deleting them is honest — that no surviving module
took over the responsibility and was simply missed.
</human-check>
</verification>

<success_criteria>
- The document loaded into every session describes only modules that exist, and the emulator dependency it names is stock upstream VICE.
- A deleted filename cited as a live component in scanned prose now fails a committed, automated, non-vacuous test — proven by a planted violation observed red, not asserted.
- The correction exists in both the projected document and the document that projection declares as its source, so a regeneration cannot silently undo it.
- No guard that was green before this plan is red after it.
</success_criteria>

<output>
Create `.planning/phases/52-remove-the-fork-backend/52-11-SUMMARY.md` when done. Record: the exact list of `CLAUDE.md` sites corrected with their before/after claim; every replacement identifier and the file it was confirmed against; the scanned prose population count the guard reported and the floor it was given; the planted-violation exit codes in both directions; the widen-vs-sibling scope decision as recorded in the guard's comment; and the outcome of the human check.
</output>


