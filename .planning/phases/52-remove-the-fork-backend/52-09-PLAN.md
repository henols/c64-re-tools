---
phase: 52-remove-the-fork-backend
plan: 09
type: execute
wave: 7
depends_on: ["52-07", "52-08"]
files_modified:
  - scripts/check-skill-capability-honesty.mjs
  - scripts/check-skill-fork-honesty.mjs
  - .github/workflows/ci.yml
  - src/mcp/vice/ci-guardrails.test.mjs
  - README.md
  - docs/stock-vice-parity.md
  - CLAUDE.md
  - .planning/PROJECT.md
autonomous: true
requirements: [FORKRM-03, FORKRM-05]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The documentation-honesty CI gate is INVERTED and renamed, not deleted and not replaced by a manual grep: it now asserts that no shipped prose claims a backend switch or a fork-only route, and that `docs/stock-hard-losses.md` is reachable from `README.md` (FORKRM-03)"
    - "`README.md` no longer names `VICE_BACKEND`, no longer describes a two-backend selection, no longer points at the deleted `docs/tool-support.md`, and does point at `docs/stock-hard-losses.md`"
    - "`docs/stock-vice-parity.md` no longer reads as a live comparison against a second backend"
    - "`CLAUDE.md`'s Compatibility bullet and its `vice-sync.ts` Testing bullet are rewritten/removed, and `.planning/PROJECT.md`'s `## Constraints` list is changed BYTE-IDENTICALLY in the same commit"
    - "`.github/workflows/ci.yml` and `src/mcp/vice/ci-guardrails.test.mjs` both name the renamed gate, and the CI step still runs and still blocks"
    - statement: "The renamed gate keeps every non-vacuity floor it had — a documentation-honesty lint that finds nothing passes everything, and that is worse than the stale assertions it replaced"
      verification: backstop
  artifacts:
    - scripts/check-skill-capability-honesty.mjs
    - README.md
    - CLAUDE.md
  key_links:
    - "`scripts/check-skill-capability-honesty.mjs` to `README.md` — the gate's required-string set is what keeps `docs/stock-hard-losses.md` reachable from the first surface a reader sees"
    - "`CLAUDE.md` `## Constraints` to `.planning/PROJECT.md` `## Constraints` (line 633) — `docs-constraints-sync.test.ts` asserts BYTE-IDENTICAL lists in the same order, in both directions"
    - "`.github/workflows/ci.yml:215` and `src/mcp/vice/ci-guardrails.test.mjs:72` to the gate's filename — a rename must move all three together or CI silently stops running the gate"
  prohibitions:
    - statement: "The honesty gate is NOT deleted and is NOT replaced by a manual grep — inverting it is the mitigation for the stale-documentation threat, and a deleted gate re-opens it"
      status: required
      verification: flagged-unverified
    - statement: "No non-vacuity floor is dropped during the inversion — every `need()` that existed to stop the lint passing by finding nothing is replaced by an equivalent floor against the new subject"
      status: required
      verification: flagged-unverified
    - statement: "`CLAUDE.md` and `.planning/PROJECT.md`'s `## Constraints` lists are never edited in different commits or by different plans — they must stay byte-identical and in the same order"
      status: required
      verification: flagged-unverified
    - statement: "`docs/stock-vice-parity.md`'s historical, dated measurement records and its legitimate `(Phase N, REQ-ID)` attributions are not rewritten — only present-tense two-backend claims are"
      status: required
      verification: flagged-unverified
    - statement: "No skill file under `src/skills/` is edited by this plan — plan 52-08 owns them and they are already done"
      status: required
      verification: flagged-unverified
---

<objective>
Invert the documentation-honesty gate, and clear the last shipped prose that describes a two-backend world.

`scripts/check-skill-fork-honesty.mjs` (769 lines) appears in no ROADMAP note, no basis note and no todo, and **every one of its assertions is falsified by this phase's own goal**. It derives its policed `FORK_ONLY_NAMES` set from the now-deleted `capability-registry.ts` (`providedBy === "fork"`), hard-asserts `README.md` contains the literal `VICE_BACKEND`, `vice_sid_get_state` and `vice_keyboard_matrix`, and carries non-vacuity floors of at least 20 fork-only names and at least 8 fork mentions across skills. Plan 52-07 deleted the registry it imports and plan 52-08 removed the mentions it counts, so it is red on arrival here — knowingly and by design.

Its own header records the precedent for what to do: when a required string's subject changed shape once before, the assertion was **re-pointed, not dropped**. The obligation that survives is "shipped prose must not claim a capability route that does not exist". That obligation is stronger now, not weaker, because this phase just removed a backend.

The file is also renamed. A script called `check-skill-fork-honesty.mjs` sitting in a repo with no fork is itself the stale-documentation threat it exists to catch.

Purpose: land criterion 3's gate half and clear the remaining product prose.
Output: an inverted, renamed, still-blocking gate; a README, a parity doc and a Constraints list that describe one backend.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-remove-the-fork-backend/52-RESEARCH.md
@docs/stock-hard-losses.md
@scripts/check-skill-fork-honesty.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invert and rename the documentation-honesty gate, and move its CI wiring with it</name>
  <files>scripts/check-skill-capability-honesty.mjs, scripts/check-skill-fork-honesty.mjs, .github/workflows/ci.yml, src/mcp/vice/ci-guardrails.test.mjs</files>
  <read_first>
    - `scripts/check-skill-fork-honesty.mjs` in full (769 lines) — especially its header (lines 1-70, including the "THE SIXTH REQUIRED STRING WAS RE-POINTED, NOT DROPPED" precedent at `:18-26`), `paths()` (`:123`), the `FORK_ONLY_NAMES` derivation from `CAPABILITY_REGISTRY` (`:287-292`), `splitSections()`/`splitParagraphs()` (`:319`, `:372`), the README assertions (`:477-522`, both `REQUIRED_README_SUBSTRINGS` and `FORBIDDEN_README_SUBSTRINGS`), the parity-doc assertions (`:536` onward), `SKILL_FILE_CLAIMS` (`:587`), the ANNO-05 deletion-pin walk, and the non-vacuity controls block (`:728-755`).
    - `scripts/check-npm-packages.mjs`, `scripts/check-skill-cli-invocations.mjs`, `scripts/check-skill-tool-coverage.mjs` — sibling shape: shebang, a WHY-comment naming the failure class, a small pure predicate, then an assert-and-exit-nonzero body. The renamed gate keeps that shape.
    - `.github/workflows/ci.yml` around line 215 — the `node scripts/check-skill-fork-honesty.mjs` step.
    - `src/mcp/vice/ci-guardrails.test.mjs` around line 72 — the frozen list of scripts it asserts CI runs, and its own non-vacuity assertions.
    - `docs/stock-hard-losses.md` — the new required-string target.
    - `src/skills/**` as rewritten by plan 52-08 — the gate must be written against what the skills now say, not against what they said.
  </read_first>
  <action>
Rename `scripts/check-skill-fork-honesty.mjs` to `scripts/check-skill-capability-honesty.mjs` (`git mv`, so history follows) and invert its assertions in place.

**Why rename**, recorded as a decision: the surviving obligation is "shipped prose must not claim a capability route that does not exist", and the filename must name that obligation rather than a backend that no longer exists. The alternative — keep the filename, invert the contents — was considered and rejected: a file called `check-skill-fork-honesty.mjs` in a repo with no fork is itself an instance of the stale-documentation class this gate guards against. The rename costs two edits (`ci.yml`, `ci-guardrails.test.mjs`) and both are done in this task.

**Go assertion by assertion**, deciding for each whether the underlying concern survives, dies, or inverts:

- `FORK_ONLY_NAMES`, derived from `CAPABILITY_REGISTRY`: the registry is gone. The surviving concern is that the three permanently-unavailable capabilities are named honestly where they are mentioned. Replace the registry derivation with a literal, named, non-empty set of the six affected tool names (`vice_sid_get_state`, `vice_keyboard_matrix`, `vice_keyboard_restore`, `vice_keyboard_chord`, `vice_keyboard_key_press`, `vice_keyboard_key_release`) and assert that wherever a skill file names one, the same section states it is permanently unavailable and cites `docs/stock-hard-losses.md`. Keep the section-scoped proximity rule the current script already implements — it is the mechanism that made the old assertion meaningful.
- The `totalForkMentions >= 8` floor: its subject is gone. Replace it with an equivalent floor against the NEW subject — a minimum count of permanent-limitation annotations found across the skills tree — so the walk still cannot pass by finding nothing. Derive the floor from what plan 52-08 actually produced, not from a guessed number.
- The `FORK_ONLY_NAMES.size >= 20` floor: replace with a floor on the new literal set's size, asserted against the set's actual length so an emptied set fails.
- `REQUIRED_README_SUBSTRINGS`: `VICE_BACKEND` DIES (there is no switch). `docs/tool-support.md` DIES (plan 52-07 retired it) and is REPLACED by `docs/stock-hard-losses.md` — this is the re-pointing the header's own precedent describes. `vice_sid_get_state` and `vice_keyboard_matrix` SURVIVE, but their `whatIsLost` reasons invert: a reader must be told these are permanently unavailable, not that they "require the fork". `3.10` and `the external analyser` are unrelated to this phase and survive unchanged.
- `FORBIDDEN_README_SUBSTRINGS`: the two ghost-test-file entries survive unchanged. ADD `VICE_BACKEND` as a newly forbidden string, so a future edit cannot reintroduce a backend switch into the README.
- `REQUIRED_PARITY_SUBSTRINGS` / `FORBIDDEN_PARITY_SUBSTRINGS`: read each against `docs/stock-vice-parity.md` as Task 2 leaves it. The `docs/tool-support.md` required string must be repointed or dropped; decide per string and say why.
- `SKILL_FILE_CLAIMS`, the ANNO-05 deletion-pin walk, and the `acme export-asm` pointer assertion: unrelated to this phase. Keep them all, unchanged.
- `topLevelDirs.length >= 6` and the two positive controls (`tool-selection.md`, `control-flow.md`): keep the directory floor. Re-verify the two positive controls against plan 52-08's rewrites — if a control file no longer carries a classifiable annotation, repoint the control at a file that does rather than deleting the control.

Rewrite the header to narrate the inversion in the same style: what changed, why the obligation survived its subject, and what a future contributor must not do (delete an assertion whose subject changed instead of re-pointing it).

Then update `.github/workflows/ci.yml`'s step to run the new filename, and `src/mcp/vice/ci-guardrails.test.mjs`'s frozen script list to name it. Both must move in this same commit — a rename that leaves CI invoking a missing file turns a blocking gate into a silent no-op.
  </action>
  <reversibility rating="costly">Rewrites a 769-line CI gate and renames it across three files. Reversible from git, but the rewritten assertions encode this phase's outcome, so reverting them would mean reverting the phase.</reversibility>
  <acceptance_criteria>
    - `test -f scripts/check-skill-capability-honesty.mjs` succeeds and `test ! -f scripts/check-skill-fork-honesty.mjs` succeeds.
    - `git log --follow --oneline -1 -- scripts/check-skill-capability-honesty.mjs` shows history was preserved (the rename used `git mv`).
    - `grep -ac 'capability-registry\|CAPABILITY_REGISTRY' scripts/check-skill-capability-honesty.mjs` returns `0`.
    - The script still contains at least four `need(` non-vacuity controls, including a floor on the literal tool-name set's size and a floor on the count of permanent-limitation annotations found.
    - `grep -ac 'check-skill-capability-honesty' .github/workflows/ci.yml` is at least 1 and `grep -ac 'check-skill-fork-honesty' .github/workflows/ci.yml` returns `0`; the same holds for `src/mcp/vice/ci-guardrails.test.mjs`.
    - `node scripts/check-skill-capability-honesty.mjs` exits 0 and its success line reports non-zero counts for skills scanned and names policed.
    - `node --test ci-guardrails.test.mjs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && test -f scripts/check-skill-capability-honesty.mjs && echo "NEW_PRESENT" || echo "NEW_MISSING"; test ! -f scripts/check-skill-fork-honesty.mjs && echo "OLD_GONE" || echo "OLD_PRESENT"; printf 'registry_refs='; grep -ac 'capability-registry\|CAPABILITY_REGISTRY' scripts/check-skill-capability-honesty.mjs; printf 'nonvacuity_needs='; grep -ac 'non-vacuity' scripts/check-skill-capability-honesty.mjs</automated>
    <fails_when>The first line prints `NEW_MISSING`, or the second prints `OLD_PRESENT`, or `registry_refs=` is greater than `0` (INVERTED polarity — it still imports a deleted module), or `nonvacuity_needs=` is less than `4` (floors were dropped during the inversion, so the lint can pass by finding nothing).</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && printf 'ci_new='; grep -ac 'check-skill-capability-honesty' .github/workflows/ci.yml; printf 'ci_old='; grep -ac 'check-skill-fork-honesty' .github/workflows/ci.yml; printf 'guard_new='; grep -ac 'check-skill-capability-honesty' src/mcp/vice/ci-guardrails.test.mjs; printf 'guard_old='; grep -ac 'check-skill-fork-honesty' src/mcp/vice/ci-guardrails.test.mjs</automated>
    <fails_when>`ci_new=` or `guard_new=` is `0` — CI no longer runs the gate, or the guard no longer asserts that it does, either of which turns a blocking check into a silent no-op. Or `ci_old=` or `guard_old=` is greater than `0` (INVERTED polarity) — a stale reference to the old filename remains and CI would fail on a missing file.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-skill-capability-honesty.mjs; echo "gate_exit=$?"; cd src/mcp/vice && node --test ci-guardrails.test.mjs; echo "guard_exit=$?"</automated>
    <fails_when>Either exit value is non-zero, or the gate's output begins `check-skill-capability-honesty: FAIL`, or its success line reports zero skill directories, zero policed names or zero annotations found — a gate that finds nothing passes everything and is worse than the stale one it replaced.</fails_when>
  </verify>
  <done>The gate is renamed with history preserved, inverted assertion by assertion with every non-vacuity floor replaced against the new subject, wired into CI and into `ci-guardrails.test.mjs` under its new name, and green.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md and docs/stock-vice-parity.md for a single backend</name>
  <files>README.md, docs/stock-vice-parity.md</files>
  <read_first>
    - `README.md` lines 5-15 (the feature list, including the "fork backend only, see below" qualifier at `:10`), lines 95-115 (the "A custom, non-upstream fork" bullet at `:98` and the distro table), lines 130-145 (the `VICE_BACKEND` selection paragraph at `:135-139`, including the indeterminate-probe fallback sentence), lines 165-190 (the "require the fork" capability note at `:171` and the absolute-path note at `:183` about a fork build shadowing stock on `$PATH`).
    - `docs/stock-vice-parity.md` in full — 28 fork mentions. Distinguish present-tense comparison claims from dated historical measurement records and from legitimate `(Phase N, REQ-ID)` attributions, which the honesty gate's own comments say are correct attributions and must not be swept.
    - `docs/stock-hard-losses.md` — the citation target README must reach.
    - `scripts/check-skill-capability-honesty.mjs` as rewritten by Task 1 — its required and forbidden README/parity strings are the contract this task must satisfy.
  </read_first>
  <action>
Rewrite `README.md` and `docs/stock-vice-parity.md` so neither describes a second backend.

**`README.md`.** Remove the `VICE_BACKEND` selection paragraph entirely — there is nothing to select, no probe to run and no indeterminate fallback. Remove the "A custom, non-upstream fork" bullet and the `fork backend only, see below` qualifier from the feature list; the features those qualified are the ones now recorded as permanently unavailable. Rewrite the "`vice_sid_get_state` and `vice_keyboard_matrix` require the fork" note as a permanent-limitation note citing `docs/stock-hard-losses.md`, keeping both tool names visible so a reader planning a method still finds the warning. Replace the `docs/tool-support.md` pointer (that document was retired) with a `docs/stock-hard-losses.md` pointer. Keep the distro/version table and the `3.10` version-gate discussion — they are about stock and are unaffected.

The absolute-path note at `:183` says stock is invoked by absolute path "since a fork build shadows it on `$PATH`". That warning is still operationally true on a machine where someone once installed a patched build, but it must not be phrased as a supported-backend fact. Restate it as a `$PATH`-shadowing hazard about a non-stock build, without describing that build as a backend this project supports.

**`docs/stock-vice-parity.md`.** Do NOT rewrite its dated historical measurement records or its `(Phase N, REQ-ID)` attributions — those are correct records of what was measured when, and the honesty gate's own comments say so. DO rewrite every present-tense claim that frames the document as a live comparison against a second backend: its framing (a parity document implies two things to compare), any "the fork does X, stock does Y" table row that is now a single-column fact, and any forward-looking statement that a capability "routes to the fork". Where a row's only content was the comparison, collapse it to the stock fact. Add a short dated note at the top recording that the fork backend was removed and that the document is retained for its measured stock findings, so a reader knows why a document named "parity" survives.

Both files must satisfy the rewritten gate's required and forbidden string sets. Run the gate after editing, not before.
  </action>
  <reversibility rating="reversible">Prose rewrites in two product documents, with the rewritten gate as the mechanical check.</reversibility>
  <acceptance_criteria>
    - `grep -ac 'VICE_BACKEND' README.md` returns `0`.
    - `grep -ac 'docs/tool-support.md' README.md` returns `0` and `grep -ac 'docs/stock-hard-losses.md' README.md` is at least 1.
    - `grep -ac 'vice_sid_get_state' README.md` and `grep -ac 'vice_keyboard_matrix' README.md` are each at least 1 — the capabilities are still named, now as permanent limitations.
    - `grep -aic 'fork' README.md` returns `0`, or every surviving hit is the `$PATH`-shadowing hazard restated without calling it a backend; name any survivor in the SUMMARY.
    - `docs/stock-vice-parity.md` carries a dated top note recording the removal, and every surviving `fork` mention is inside a dated historical record.
    - `node scripts/check-skill-capability-honesty.mjs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && printf 'readme_vice_backend='; grep -ac 'VICE_BACKEND' README.md; printf 'readme_tool_support='; grep -ac 'docs/tool-support.md' README.md; printf 'readme_losses='; grep -ac 'docs/stock-hard-losses.md' README.md; printf 'readme_sid='; grep -ac 'vice_sid_get_state' README.md; printf 'readme_matrix='; grep -ac 'vice_keyboard_matrix' README.md</automated>
    <fails_when>`readme_vice_backend=` or `readme_tool_support=` is greater than `0` — INVERTED polarity: a non-zero count means README still names a removed switch or a deleted document. OR `readme_losses=`, `readme_sid=` or `readme_matrix=` is `0` — normal polarity here: a zero means the acceptance record is unreachable from README, or a permanently-unavailable capability was silently dropped instead of stated.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && printf 'readme_fork='; grep -aic 'fork' README.md; printf 'parity_note='; grep -ac 'removed' docs/stock-vice-parity.md; node scripts/check-skill-capability-honesty.mjs; echo "gate_exit=$?"</automated>
    <fails_when>`gate_exit=` is non-zero or the gate prints a `FAIL` line. `readme_fork=` greater than `0` is a failure UNLESS every surviving hit is the restated `$PATH`-shadowing hazard, which the executor must list by line in the SUMMARY — an unexplained survivor is a failure. `parity_note=` of `0` means the parity document has no dated note explaining why a comparison document survives with nothing to compare against.</fails_when>
  </verify>
  <done>README describes one backend, reaches `docs/stock-hard-losses.md`, and still names every permanently-unavailable capability; the parity document keeps its measured history behind a dated removal note and makes no live two-backend claim; the rewritten gate passes against both.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the two remaining falsified CLAUDE.md constraints, byte-identically in PROJECT.md</name>
  <files>CLAUDE.md, .planning/PROJECT.md</files>
  <read_first>
    - `CLAUDE.md` line 25 — the Compatibility bullet in full: it states the stdio MCP surface is "trimmed per backend", that stock advertises only the tools it implements, that a tool advertised on both keeps a backward-compatible argument shape, that the fork's list is unchanged from v0.1.x, that a skill written against the full fork surface breaks rather than degrades, and that the argument-shape half is pinned by `manifest-arg-compat.test.ts` (which plan 52-05 deleted).
    - `CLAUDE.md` line 49 — the Testing bullet naming `vice-sync.ts`'s checkpoint-wait invariants (plan 52-03 deleted that file).
    - `CLAUDE.md` lines 11-17 — the `## Project` prose describing the fork as the only working backend and the milestone as "adds a second backend". This is OUTSIDE the Constraints marker block, so it is not byte-synced, but it is equally false.
    - `.planning/PROJECT.md` `## Constraints` (heading at line 633) — the byte-identical copies of both bullets.
    - `src/mcp/vice/docs-constraints-sync.test.ts` — its byte-equality rule and its planted-violation tests, so the edit is made to satisfy the real rule rather than a summary of it.
    - The rest of `CLAUDE.md`'s Constraints list — several Protocol and Capability bullets mention "binary monitor only" qualifications that are about stock's two channels, NOT about the fork. Read before editing so a stock-versus-stock qualification is not swept away with the fork ones.
  </read_first>
  <action>
Rewrite the two falsified constraint bullets in `CLAUDE.md` AND in `.planning/PROJECT.md`'s `## Constraints` list, byte-identically and in the same order, in one commit.

**The Compatibility bullet.** Its premise — a surface trimmed per backend, with a compatibility contract between two tool lists — is gone. What SURVIVES and must be restated: the stdio MCP surface advertises only the tools this project implements, and a skill written against a tool the server does not advertise breaks rather than degrades, so a playbook must name a real route or state a limitation. That rule (the project's `SKILL-01` reasoning) is the load-bearing half and it is now enforced by the renamed gate from Task 1, not by `manifest-arg-compat.test.ts`. Replace the deleted guard's citation with the renamed gate's filename. Remove every clause about a second list, a per-backend trim, or a cross-manifest argument-shape contract.

**The Testing bullet.** `vice-sync.ts` no longer exists, so the constraint about its checkpoint-wait functions goes with it — deliberately and by decision, recorded in the SUMMARY. Before removing it, check whether either documented invariant ("exactly one resume per wait", "poll on `hit_count`, never on paused state") is honoured by any SURVIVING module; if one is, restate the bullet against that module rather than deleting the rule along with its former home. If neither is, remove the bullet.

**The `## Project` prose at `CLAUDE.md:11-17`** is outside the Constraints marker block and therefore not byte-synced — but it describes the fork as the backend everything works against and this milestone as "adds a second backend". Rewrite it to describe what the project is now: an MCP server driving stock upstream VICE through its binary monitor and text channel, with no second backend and no patched build required. Check whether `.planning/PROJECT.md` carries a parallel passage; if it does, update it too, but note that only the Constraints list is under the byte-identity rule.

Make both Constraints edits in ONE commit. `docs-constraints-sync.test.ts` reports a bullet present in only one copy in both directions and reports a reordering rather than accepting a set match, so a split edit or a reordered list reds the suite.
  </action>
  <reversibility rating="costly">Removes a project-instruction constraint that every future session loads. Restorable from git, but the constraint describes a world that no longer exists, so restoring it would reintroduce the stale-documentation hazard rather than a protection.</reversibility>
  <acceptance_criteria>
    - `grep -ac 'manifest-arg-compat' CLAUDE.md` and `grep -ac 'manifest-arg-compat' .planning/PROJECT.md` both return `0`.
    - `grep -ac 'vice-sync' CLAUDE.md` and `grep -ac 'vice-sync' .planning/PROJECT.md` both return `0`.
    - `grep -aic 'fork' CLAUDE.md` returns `0`.
    - `CLAUDE.md`'s Constraints list still states the surviving break-rather-than-degrade rule and cites `scripts/check-skill-capability-honesty.mjs`.
    - `node --test docs-constraints-sync.test.ts` exits 0 — the two lists are byte-identical, in the same order.
    - `node --test docs-fork-decision.test.ts` still exits 0 — the PROJECT.md edits did not disturb the reversal row or the Out of Scope bullet.
    - The automated suite's failure SET matches plan 52-06's re-baselined reference floor.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-constraints-sync.test.ts docs-fork-decision.test.ts; echo "EXIT=$?"</automated>
    <fails_when>`EXIT=` is non-zero or any `not ok` line appears. A `docs-constraints-sync` failure naming "a bullet present in only one copy" means only one of the two lists was edited; one naming "a reordering" means bullets were moved rather than removed; a `docs-fork-decision` failure means the PROJECT.md edit disturbed the reversal row.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && printf 'claude_fork='; grep -aic 'fork' CLAUDE.md; printf 'claude_manifestcompat='; grep -ac 'manifest-arg-compat' CLAUDE.md; printf 'project_manifestcompat='; grep -ac 'manifest-arg-compat' .planning/PROJECT.md; printf 'claude_vicesync='; grep -ac 'vice-sync' CLAUDE.md; printf 'claude_newgate='; grep -ac 'check-skill-capability-honesty' CLAUDE.md</automated>
    <fails_when>Any of the first four counts is greater than `0` — INVERTED polarity: each names a deleted file or a removed backend, so a non-zero count is the failure. `claude_newgate=` of `0` is a failure in the normal direction: the surviving break-rather-than-degrade rule lost its enforcement citation and became unpinned prose.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated > /tmp/floor-52-09.log 2>&1; echo "EXIT=$?"; grep -a '^not ok' /tmp/floor-52-09.log | sed 's/[0-9]\+ - //' | sort</automated>
    <fails_when>The sorted `not ok` SET gains any member absent from plan 52-06's re-baselined reference floor. Compare the SET, never the count. Do not pipe into `tail` — the pipe's exit code would report success regardless.</fails_when>
  </verify>
  <done>Both falsified constraints are resolved in both byte-identical copies in one commit; the surviving break-rather-than-degrade rule is restated and re-pinned to the renamed gate; `CLAUDE.md` names no fork; the project description matches what the project now is.</done>
</task>

</tasks>

<assumption_delta_decision>
Recorded once for this phase in plan `52-01`: primary noun `backend`, decision `promote`, companion invariant test already required by criterion 2. This plan is where the `promote` decision reaches the project's own instruction files — `CLAUDE.md` stops describing a two-backend surface, and the honesty gate is re-pointed from "is the fork requirement stated?" to "is the permanent limitation stated?".
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → first-time reader | `README.md` is the first surface; a wrong install or configuration instruction there is acted on before anything else is read |
| repo → every future session | `CLAUDE.md` is loaded into every session and its constraints are treated as current fact |
| CI → merge | the honesty gate is a blocking step; a gate that silently stops running is worse than one that never existed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-31 | Information Disclosure | `scripts/check-skill-fork-honesty.mjs` left un-rewritten | high | mitigate | This gate IS the mitigation for the stale-documentation threat. Leaving it asserting fork-era claims — or deleting it — re-opens the whole class. It is inverted and re-pointed, following its own recorded precedent, with every non-vacuity floor replaced against the new subject |
| T-52-32 | Spoofing | the gate's rename | high | mitigate | A rename that moves the file but not `.github/workflows/ci.yml` and `ci-guardrails.test.mjs` turns a blocking CI step into a silent no-op or a hard CI error. All three move in one commit and both directions are asserted by grep |
| T-52-33 | Information Disclosure | `README.md`'s `VICE_BACKEND` instructions | high | mitigate | A configuration instruction for a variable nothing reads sends a user to fix a problem that cannot be fixed there. It is removed and additionally added to the gate's FORBIDDEN set so it cannot come back |
| T-52-34 | Information Disclosure | `CLAUDE.md` Compatibility and Testing bullets | high | mitigate | Both name deleted files (`manifest-arg-compat.test.ts`, `vice-sync.ts`) as active guards. Both copies are rewritten byte-identically in one commit under `docs-constraints-sync.test.ts`, and the surviving rule is re-pinned to the renamed gate rather than left citing nothing |
| T-52-35 | Repudiation | `docs/stock-vice-parity.md` historical records | medium | mitigate | Dated measurement records and `(Phase N, REQ-ID)` attributions are explicitly fenced from the rewrite; only present-tense comparison claims change, and a dated top note records why a parity document survives |
| T-52-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan |
</threat_model>

## Artifacts this phase produces

**Created by this plan**
- `scripts/check-skill-capability-honesty.mjs` — the renamed, inverted documentation-honesty gate. New assertions: a literal non-empty set of the six permanently-unavailable tool names; a section-scoped rule that naming one requires stating its permanent unavailability and citing `docs/stock-hard-losses.md`; a minimum-annotation-count floor replacing the old fork-mention floor; `docs/stock-hard-losses.md` as a REQUIRED README string; `VICE_BACKEND` as a newly FORBIDDEN README string.
- A dated removal note at the top of `docs/stock-vice-parity.md` explaining why a parity document survives with nothing to compare against.
- A rewritten Compatibility constraint (in both byte-identical copies) restating the break-rather-than-degrade rule and citing the renamed gate.

**Removed by this plan**
- `scripts/check-skill-fork-honesty.mjs` as a filename (renamed via `git mv`, history preserved), together with: the `CAPABILITY_REGISTRY`-derived `FORK_ONLY_NAMES` set, the `totalForkMentions >= 8` floor, the `FORK_ONLY_NAMES.size >= 20` floor, and the `VICE_BACKEND` / `docs/tool-support.md` required-README strings.
- `README.md`: the `VICE_BACKEND` selection paragraph, the "A custom, non-upstream fork" bullet, the `fork backend only` feature qualifier, the `docs/tool-support.md` pointer, and the fork framing of the `$PATH`-shadowing note.
- `docs/stock-vice-parity.md`: every present-tense two-backend comparison claim.
- `CLAUDE.md` and `.planning/PROJECT.md` `## Constraints`: the per-backend-trim clauses of the Compatibility bullet and its `manifest-arg-compat.test.ts` citation; the `vice-sync.ts` Testing bullet (unless one of its invariants is honoured by a surviving module, in which case the bullet is restated against that module).
- `CLAUDE.md`'s `## Project` prose describing the fork as the working backend and this milestone as adding a second one.

**Explicitly NOT touched by this plan**
- Anything under `src/skills/` — plan 52-08 owns it and it is already done.
- `docs/stock-vice-parity.md`'s dated measurement records and `(Phase N, REQ-ID)` attributions.
- `SKILL_FILE_CLAIMS`, the ANNO-05 deletion-pin walk, the `anno export-asm` pointer assertion, the two ghost-test-file forbidden strings, the `3.10` and external-analyser required strings — all unrelated to this phase and kept unchanged in the renamed gate.
- The Protocol and Capability constraint bullets whose "binary monitor only" qualifications are about stock's two channels, not about the fork.

<verification>
`node scripts/check-skill-capability-honesty.mjs` exits 0 with non-vacuous counts; CI and `ci-guardrails.test.mjs` both name the new filename and neither names the old one; `README.md` names no backend switch and reaches `docs/stock-hard-losses.md`; `docs-constraints-sync.test.ts` and `docs-fork-decision.test.ts` are green; the suite's failure SET matches the reference floor.
</verification>

<success_criteria>
- Criterion 3's gate half is satisfied: the documentation-honesty check is inverted rather than deleted, and it now enforces the permanent-limitation statements plan 52-08 wrote.
- No shipped or project-instruction prose describes a backend that does not exist.
</success_criteria>

<output>
Create `.planning/phases/52-remove-the-fork-backend/52-09-SUMMARY.md` when done. Record: the per-assertion disposition table for the gate's inversion (survives / dies / inverts, with the reason for each); the new non-vacuity floor values and how they were derived from plan 52-08's actual output; any surviving `fork` occurrence in `README.md` with its line and justification; whether either `vice-sync.ts` Testing invariant was found honoured by a surviving module; and confirmation that the two Constraints lists were edited in one commit.
</output>
