---
phase: 52-remove-the-fork-backend
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/todos/completed/2026-08-20-fully-remove-the-forked-vice-mcp-backend.md
autonomous: true
requirements: [FORKRM-01, FORKRM-02, FORKRM-03, FORKRM-04, FORKRM-05, FORKRM-06, FORKRM-07]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`.planning/REQUIREMENTS.md` declares `FORKRM-01` through `FORKRM-07`, one per ROADMAP Phase 52 success criterion, in a fresh id namespace that collides with neither `FORK-01`/`FORK-02` (historical, PROJECT.md) nor the v1.0.0 `DECOMP-*`/`BUILD-*`/`EQUIV-*` set"
    - "`.planning/REQUIREMENTS.md`'s `## Traceability` table maps every one of `FORKRM-01..07` to Phase 52, and its Coverage block's totals are recomputed rather than left stale"
    - "`.planning/todos/completed/2026-08-20-fully-remove-the-forked-vice-mcp-backend.md` no longer reads as if `retain` is the current answer — its disposition is marked superseded with the reversal date"
    - statement: "Nothing in `.planning/REQUIREMENTS.md`'s existing v1.0.0 requirement text, Departures table, Future Requirements or Out of Scope table is reworded by this plan"
      verification: backstop
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/todos/completed/2026-08-20-fully-remove-the-forked-vice-mcp-backend.md
  key_links:
    - "`.planning/REQUIREMENTS.md` `## Traceability` to `.planning/ROADMAP.md` `### Phase 52` (the 1:1 mapping the Traceability section asserts must hold for every requirement)"
    - "each `FORKRM-NN` to the ROADMAP Phase 52 success criterion numbered `NN` (the mapping is deliberately positional so the paper trail is checkable by reading two documents side by side)"
  prohibitions:
    - statement: "`FORK-01` and `FORK-02` are NOT renumbered, NOT copied into `.planning/REQUIREMENTS.md`, and NOT deleted from `.planning/PROJECT.md` — they are historical decision-table ids amended in place by plan 52-01"
      status: required
      verification: flagged-unverified
    - statement: "`.planning/milestones/v0.4.0-REQUIREMENTS.md` is NOT edited"
      status: required
      verification: flagged-unverified
    - statement: "No existing v1.0.0 requirement (`DECOMP-*`, `BUILD-*`, `EQUIV-*`) has its text, checkbox state or Traceability row changed"
      status: required
      verification: flagged-unverified
    - statement: "The completed todo file is NOT deleted and NOT moved back to `pending/` — its historical disposition is annotated, not erased"
      status: required
      verification: flagged-unverified
---

<objective>
Mint this phase's requirement IDs and close the one record that still reads as though `retain` is the live answer.

The ROADMAP's `**Requirements**` line for Phase 52 is literally `TBD — declare at planning time`, and criterion 2 makes declaring them part of the phase's paper trail rather than bookkeeping: a decision reversal with no requirement id is a reversal nothing can be traced against.

Purpose: give every later plan in this phase a requirement id to cite, and stop the superseded `retain` todo from reading as current.
Output: `FORKRM-01..07` declared and traced in `.planning/REQUIREMENTS.md`; the superseded todo annotated.

**Recorded visible skip — spec-less probe fallback.** This phase has no SPEC file and its `phase_req_ids` was `TBD`, so there was no Edge Coverage or Prohibitions section to lift and no requirement text to derive probe predicates from. **No probe-derived predicates were generated for Phase 52.** Every `must_haves` block across this phase's ten plans is derived instead from the seven ROADMAP success criteria plus the measured inventory in `52-RESEARCH.md`. This is recorded here, visibly, rather than dropped silently.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/52-remove-the-fork-backend/52-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare FORKRM-01..07 and extend the Traceability table</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` in full (175 lines) — especially the three subsections under `## v1.0.0 Requirements`, the `## Traceability` table, and the Coverage block asserting "15 total / Mapped to phases: 15 / Unmapped: 0" and the prose "Six phases, 45-50".
    - `.planning/ROADMAP.md` lines 1275-1300 — the seven Phase 52 success criteria, in order. The `FORKRM-NN` numbering is positional against them.
  </read_first>
  <action>
Add a new subsection to `.planning/REQUIREMENTS.md` and extend its Traceability table. Do not reword anything that already exists.

**New subsection.** Under `## v1.0.0 Requirements`, after the existing `### Equivalence and Modifiability` subsection, add `### Fork-Backend Removal (Phase 52)` with seven unchecked requirement bullets in the file's existing `- [ ] **ID**: text` shape, one per ROADMAP success criterion, positionally numbered:

- `FORKRM-01` — `VICE_BACKEND`, `probeBackend()`, `resolvedBackend()`'s fork branch and `buildBackendAwareTool()` are gone, every `backend === "fork"` test branch is removed rather than skipped, and no module imports a fork transport. One backend means no backend selection.
- `FORKRM-02` — `.planning/PROJECT.md`'s `FORK-01` row and its `### Out of Scope` fork bullet state the REVERSAL with its date and basis, and `docs-fork-decision.test.ts` is rewritten to pin the new decision rather than deleted.
- `FORKRM-03` — SID read-back, matrix keyboard and RESTORE/NMI are recorded as ACCEPTED, dated, with their evidence, in `docs/stock-hard-losses.md`, and they stop being routed to the fork anywhere in shipped skill text.
- `FORKRM-04` — `DENY_LIST` and `denyListRefusalMessage()` are gone along with their consumers, and `anno-tools.ts`'s inverted allowlist (`CURATED_ANNO_TOOLS`, `assertAnnoBatch`, `ANNO_MAX_BATCH_DEPTH`) is unchanged and still guarded.
- `FORKRM-05` — `capability-registry.ts` is resolved by a recorded decision, not left half-referenced.
- `FORKRM-06` — `tools-manifest.stock.json` is the only manifest; `refresh-manifest.ts` and `tools-manifest.json` are gone and nothing regenerates a manifest from a live host.
- `FORKRM-07` — `npm run test:automated` is green at the documented failure-set floor, with every fork-conditional test branch removed rather than skipped.

Keep each bullet's wording traceable to its criterion; do not soften, merge or drop any of the seven.

**Traceability.** Append seven rows to the `## Traceability` table in the form `| FORKRM-NN | Phase 52 | Pending |`, preserving the table's existing column order and pipe spacing exactly. Then update the Coverage block: totals become 22 requirements, 22 mapped, 0 unmapped. Amend the "Six phases, 45-50" sentence to name the phases that now carry requirements (45-50 plus 52), and add one short paragraph stating why `FORKRM-*` is a separate id namespace: `FORK-01` and `FORK-02` are Key Decisions rows in `.planning/PROJECT.md` (with an archived mirror in `.planning/milestones/v0.4.0-REQUIREMENTS.md`), not milestone requirements, so reusing those ids here would collide a decision id with a requirement id; a fresh namespace keeps the 1:1 requirement-to-phase invariant the section asserts.

Phase 51 also carries a `TBD` requirements line. Do NOT declare or renumber anything for Phase 51 — that is a different phase's planning job, and touching it would make this plan's Coverage totals wrong the moment 51 is planned. State this explicitly in the new paragraph so a later reader does not read 22/22 as a claim that every roadmap phase is mapped.
  </action>
  <reversibility rating="reversible">Requirement ids are a naming choice inside one planning document; renaming the namespace later is a find-and-replace across this phase's plans and one table.</reversibility>
  <acceptance_criteria>
    - `grep -ac 'FORKRM-0[1-7]' .planning/REQUIREMENTS.md` is at least 14 (seven requirement bullets plus seven Traceability rows).
    - Each of `FORKRM-01` through `FORKRM-07` appears at least twice: once as a `- [ ] **FORKRM-NN**:` bullet and once as a `| FORKRM-NN | Phase 52 | Pending |` row.
    - `grep -ac 'DECOMP-01' .planning/REQUIREMENTS.md` and `grep -ac 'BUILD-01' .planning/REQUIREMENTS.md` are unchanged from before the task — no existing requirement was disturbed.
    - The Coverage block reads 22 total / 22 mapped / 0 unmapped and the accompanying prose names Phase 52 and explicitly excludes Phase 51 from the claim.
    - `grep -ac 'FORK-0[12]' .planning/REQUIREMENTS.md` returns `0` — the historical decision ids were not copied in.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && for n in 01 02 03 04 05 06 07; do printf 'FORKRM-%s=' "$n"; grep -ac "FORKRM-$n" .planning/REQUIREMENTS.md; done; printf 'legacy='; grep -ac 'FORK-0[12]' .planning/REQUIREMENTS.md; echo "done"</automated>
    <fails_when>Any `FORKRM-NN=` line reports a count below 2 (a requirement was declared but never traced, or traced but never declared), or the `legacy=` line reports anything other than `0`. Note the polarity on `legacy=`: a NON-ZERO count is the failure and `grep` exiting 1 with `0` is the success.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -a 'requirements: 22 total\|Mapped to phases: 22\|Unmapped: 0' .planning/REQUIREMENTS.md | wc -l</automated>
    <fails_when>Output is less than `3` — the Coverage block was left with the stale 15/15 totals, which is exactly the drift the Traceability section's own 1:1 claim depends on not happening.</fails_when>
  </verify>
  <done>Seven `FORKRM-*` requirements are declared with text traceable 1:1 to the seven ROADMAP criteria, all seven are mapped to Phase 52 in the Traceability table, the Coverage totals are recomputed, and the namespace choice is stated in prose.</done>
</task>

<task type="auto">
  <name>Task 2: Mark the superseded `fully-remove-the-forked-vice-mcp-backend` todo as superseded</name>
  <files>.planning/todos/completed/2026-08-20-fully-remove-the-forked-vice-mcp-backend.md</files>
  <read_first>
    - `.planning/todos/completed/2026-08-20-fully-remove-the-forked-vice-mcp-backend.md` in full — the whole point is that its recorded `retain` disposition currently reads as the project's live answer to this exact question.
    - `.planning/notes/fork-removal-reversal-basis.md` — the two grounds and the owner scope call, so the annotation states a basis rather than just a date.
  </read_first>
  <action>
Annotate `.planning/todos/completed/2026-08-20-fully-remove-the-forked-vice-mcp-backend.md` in place. Keep every existing line — this is a completed-todo record, and rewriting its history would be the same failure mode the reversal is being documented against.

Add a clearly delimited block at the TOP of the file (above the existing body, so a reader hits it first) stating: this todo's `retain` disposition, recorded in plan 14-05, is **SUPERSEDED** as of the reversal date; the fork backend was removed; the current answer is the opposite of what the body below records; and the live record is `.planning/PROJECT.md`'s `FORK-01` Key Decisions row with the accepted-losses record at `docs/stock-hard-losses.md`. If the file carries YAML front matter with a `status` or `disposition` key, set it to `superseded` and leave the original value quoted inside the annotation block rather than silently overwritten.

Do not move the file out of `completed/`, do not create a new pending todo, and do not delete anything.
  </action>
  <reversibility rating="reversible">An annotation block at the head of one archived todo; `git revert` restores the file exactly.</reversibility>
  <acceptance_criteria>
    - `grep -ac 'SUPERSEDED' .planning/todos/completed/2026-08-20-fully-remove-the-forked-vice-mcp-backend.md` is at least 1.
    - The same file contains the literal `docs/stock-hard-losses.md` and an ISO `YYYY-MM-DD` reversal date.
    - `git diff --numstat -- .planning/todos/completed/2026-08-20-fully-remove-the-forked-vice-mcp-backend.md` shows deletions of `0` except for a front-matter `status`/`disposition` line, if one existed — the body is additive-only.
    - `ls .planning/todos/pending/ | grep -c fork` returns `0`: no new pending todo was created.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac 'SUPERSEDED' .planning/todos/completed/2026-08-20-fully-remove-the-forked-vice-mcp-backend.md; grep -ac 'docs/stock-hard-losses.md' .planning/todos/completed/2026-08-20-fully-remove-the-forked-vice-mcp-backend.md; ls .planning/todos/pending/ | grep -c fork; echo "done"</automated>
    <fails_when>Either of the first two counts is `0` (the supersession or its pointer never landed). The third count has INVERTED polarity — anything other than `0` is the failure, meaning a new pending fork todo was created when the instruction was to annotate the completed one.</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && git diff --numstat -- .planning/todos/completed/2026-08-20-fully-remove-the-forked-vice-mcp-backend.md; echo "EXIT=$?"</automated>
    <fails_when>The deletions column (the second number) is greater than `1` — the annotation removed existing body lines instead of adding to them. A single deletion is tolerated only when it is a front-matter `status`/`disposition` key being reset, and the executor must name which line it was in the SUMMARY.</fails_when>
  </verify>
  <done>The completed todo opens with a dated SUPERSEDED block naming the reversal, pointing at the live record and the accepted-losses doc; its original body is intact; no new pending todo exists.</done>
</task>

</tasks>

<assumption_delta_decision>
Recorded once for this phase in plan `52-01`. Summary as it applies here: the primary noun is `backend`, the decision is `promote` (stock becomes the only transport and "backend" leaves the model), and the companion invariant test is the rewritten `docs-fork-decision.test.ts` from plan 52-01 plus the source-level fork-absence gate in plan 52-10. Nothing in this plan adds a second identity model.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record → future planner | `.planning/REQUIREMENTS.md`'s Traceability table is the machine-readable claim that every requirement is owned by exactly one phase |
| archived todo → future reader | a `completed/` todo is read as a settled answer; a stale one silently re-answers a question that has since been re-decided |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-04 | Repudiation | `.planning/todos/completed/2026-08-20-fully-remove-the-forked-vice-mcp-backend.md` | medium | mitigate | The superseded `retain` disposition is annotated in place with the reversal date and a pointer to the live record, rather than deleted — the original answer stays auditable while ceasing to read as current |
| T-52-05 | Spoofing | `FORKRM-*` id namespace | low | mitigate | A fresh namespace is minted so a `FORKRM-NN` requirement can never be mistaken for the historical `FORK-NN` decision id; the Coverage prose states the reason |
| T-52-06 | Tampering | `.planning/REQUIREMENTS.md` Coverage totals | medium | mitigate | The totals are recomputed (22/22/0) in the same edit that adds the rows, and the prose explicitly scopes the claim to exclude the still-unplanned Phase 51 so a later reader does not act on a false "everything is mapped" |
| T-52-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan; no legitimacy audit applies |
</threat_model>

## Artifacts this phase produces

**Created by this plan**
- Requirement ids `FORKRM-01`, `FORKRM-02`, `FORKRM-03`, `FORKRM-04`, `FORKRM-05`, `FORKRM-06`, `FORKRM-07` — new symbols, new namespace, positionally 1:1 with the ROADMAP's seven Phase 52 success criteria.
- `.planning/REQUIREMENTS.md` section `### Fork-Backend Removal (Phase 52)` — new subsection.
- Seven new `## Traceability` rows mapping `FORKRM-01..07` to Phase 52, plus recomputed Coverage totals (22 total / 22 mapped / 0 unmapped) and a new namespace-rationale paragraph.

**Removed / superseded by this plan**
- The live `retain` reading of `.planning/todos/completed/2026-08-20-fully-remove-the-forked-vice-mcp-backend.md` (annotated as SUPERSEDED; the record itself is preserved).
- The stale `15 total / 15 mapped` Coverage figures and the "Six phases, 45-50" scope sentence (both recomputed/amended in place).

**Explicitly NOT touched by this plan**
- `FORK-01` / `FORK-02` as ids — they remain `.planning/PROJECT.md` Key Decisions entries, amended in place by plan 52-01 and never migrated into `REQUIREMENTS.md`.
- `.planning/milestones/v0.4.0-REQUIREMENTS.md` — archived snapshot.
- Phase 51's `TBD` requirements line — a different phase's planning job.
- Every existing `DECOMP-*`, `BUILD-*`, `EQUIV-*` and `FUT-*` requirement.

<verification>
All seven `FORKRM-*` ids are declared and traced; the Coverage block is internally consistent; the superseded todo carries a dated SUPERSEDED block; no archived snapshot and no pre-existing requirement was altered.
</verification>

<success_criteria>
- Every requirement id this phase cites (across all ten plans' `requirements` frontmatter) exists in `.planning/REQUIREMENTS.md` and maps to Phase 52.
- The paper trail criterion 2 demands is complete on the requirements side: a reader can go ROADMAP criterion → `FORKRM-NN` → Traceability row → Phase 52 without a gap.
</success_criteria>

<output>
Create `.planning/phases/52-remove-the-fork-backend/52-02-SUMMARY.md` when done. Record the exact seven requirement-id spellings used (later plans' frontmatter must match), and note whether the completed todo had front matter that needed its `status`/`disposition` key reset.
</output>
