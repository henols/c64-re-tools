---
phase: 40-the-three-preprocessing-host-tools
plan: 11
type: execute
wave: 4
depends_on: [40-10]
gap_closure: true
gap_ids: [G-40-1]
requirements: [PREP-05]
autonomous: true
worktree: false
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/todos/pending/2026-09-08-guard-ghidra-symlink-project-location.md
  - .planning/todos/completed/2026-09-08-guard-ghidra-symlink-project-location.md

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "PREP-05 reads Complete in both places it is recorded, and its own text no longer names ghidra.analyze as an open deviation -- because it is not one any more"
    - "The three automated-suite failures caused by an un-ledgered pending todo are GONE, because that todo is closed with a real Resolution section rather than papered over with a ledger row"
    - "Phase 40's plan list, plan count and Progress row all state 11 plans, matching what is on disk"
    - "STATE.md's exemption decision entry carries a dated superseding clause, and a new entry records what replaced it -- the wrong belief stays visible next to its correction"
    - "STATE.md's Deferred Items ledger count equals the real file count in .planning/todos/pending/, recomputed rather than adjusted by arithmetic"
  artifacts:
    - ".planning/REQUIREMENTS.md -- PREP-05 checked, its mapping row Complete, its parenthetical corrected"
    - ".planning/ROADMAP.md -- Phase 40's requirements line, plan checkboxes, plan count and Progress row"
    - ".planning/STATE.md -- the superseded decision entry, a new resolution entry, and a recomputed Deferred Items count"
    - ".planning/todos/completed/2026-09-08-guard-ghidra-symlink-project-location.md -- moved from pending with a Resolution section"
  key_links:
    - "closing the guard todo -> docs-deferred-ledger.test.ts going green: the three current failures are caused by that todo having no STATE.md row; closing it is the correct fix, adding a row for work already done is not"
    - "PREP-05's status -> the ROADMAP Phase 40 requirements line: the requirement is currently mapped in REQUIREMENTS.md but ABSENT from the ROADMAP phase entry, so one of the two is wrong regardless of the gap"
---

<objective>
Land the bookkeeping for gap G-40-1: mark `PREP-05` satisfied, record the corrected
decision in project state, bring Phase 40's plan accounting in line with what is on
disk, and close the live-Ghidra guard todo that plans 40-08 and 40-09 unblocked.

Purpose: three of the six failures in the current automated suite are caused by a
pending todo with no ledger row -- filed by the debug session that produced this gap's
diagnosis, and explicitly blocked on this gap landing. Closing it is what makes the
suite honest again. And `PREP-05` is the requirement this entire gap-closure round
exists to satisfy; leaving it Pending would leave the milestone's own accounting
claiming the work is undone.

Output: five bookkeeping files consistent with the code, and an automated suite back at
its documented three-failure floor.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# What actually landed across the three preceding plans -- the source for every claim
# this plan records. Read all three before writing a single bookkeeping line.
@.planning/phases/40-the-three-preprocessing-host-tools/40-08-SUMMARY.md
@.planning/phases/40-the-three-preprocessing-host-tools/40-09-SUMMARY.md
@.planning/phases/40-the-three-preprocessing-host-tools/40-10-SUMMARY.md

# The todo being closed, and the measured semantics its Resolution section must cite.
@.planning/todos/pending/2026-09-08-guard-ghidra-symlink-project-location.md
@.planning/notes/ghidra-dot-path-check-semantics.md
</context>

<execution_constraints>
This plan runs with `worktree: false` -- it is set in the frontmatter above and is not
optional. Its deliverable IS `.planning/STATE.md` and `.planning/ROADMAP.md` content,
and the per-plan carve-out exists because worktree executors may not touch those two
files and the worktree commit path strips them. `REQUIREMENTS.md` is unaffected by that
rule but travels with the same plan.

MAKE EVERY EDIT BY HAND, with scoped `Edit` calls, and DIFF after each one. Do not
reach for a planning-state helper verb for any of it. This project has recorded
specific, repeatedly-observed corruptions from those verbs, each of which would land
squarely in this plan's file set:
  - a state-update verb guts the Current Position prose;
  - an add-decision verb rejects some paths outright;
  - an add-blocker verb deletes standalone negations anywhere in the file, inverting
    unrelated sentences;
  - a frontmatter-set verb re-serialises the YAML and empties every block scalar;
  - a roadmap plan-progress verb bumps the count but leaves the trailing prose stale AND
    mangles the Progress row's pipe spacing;
  - a dependency-annotation verb runs the last success criterion into the following
    bold heading;
  - a phase-complete verb marks every phase requirement Complete even when its own note
    says otherwise, and overwrites a just-finished phase's position with a stale one.
If you nonetheless invoke any `gsd` tooling, invoke it through the absolute
`node .../gsd-core/bin/gsd-tools.cjs` path -- a bare launcher silently no-ops in a
subagent, exiting 0 with no output and no write -- and diff the file afterwards
regardless of what it reported.

Do NOT mark Phase 40 complete, and do not touch its `Status` field. Gap closure feeds
back into `/gsd-verify-work`, which owns the phase's status and reconciles gap `G-40-1`
from the existence of these plans' summaries.
</execution_constraints>

<interface_context>
MEASURED at plan time (2026-09-08). Re-verify every line number and every count.

- `.planning/REQUIREMENTS.md:59` -- `PREP-05`, checkbox `[ ]`. Its parenthetical states
  "The single deviation is `ghidra.analyze`, whose runs root is computed as
  `join(repoRoot, "tools", ...)`" and then already records the corrected measurement and
  points at gap `G-40-1`. So the measurement half is right and the deviation half is now
  stale.
- `.planning/REQUIREMENTS.md:142` -- `| PREP-05 | Phase 40 | Pending |`.
- `.planning/REQUIREMENTS.md:147,149,152,156` -- count prose that is ALREADY internally
  inconsistent, from before this gap: `:147` says "v0.9.0 requirements: 20 total",
  `:152` says "20 mapped after `PREP-03` was removed", `:156` says "19 ids mapped, 19
  unique". MEASURED: 20 requirement checkbox lines (including the struck `PREP-03`) and
  21 mapping-table rows. `.planning/ROADMAP.md:16` independently says "19/19
  requirements mapped".
- `.planning/ROADMAP.md:930` -- Phase 40's `**Requirements**:` line lists
  `PREP-01, PREP-02, ~~PREP-03~~, PREP-04` and does NOT list `PREP-05`, even though
  `REQUIREMENTS.md:142` maps `PREP-05` to Phase 40. One of the two is wrong
  independently of this gap.
- `.planning/ROADMAP.md:938-942` -- `**Plans**: 7/7 plans executed in 7 waves`, plus
  prose about why the phase is fully sequential.
- `.planning/ROADMAP.md:944-971` -- the wave-grouped plan list, currently Waves 1-7 with
  all seven boxes ticked. Waves 8-11 and the four new plans are added by the planner
  before execution; verify they are present and correctly shaped rather than re-adding
  them.
- `.planning/ROADMAP.md:1226` -- the Progress table row:
  `| 40. The Three Preprocessing Host Tools | v0.9.0 | 7/7 | In Progress | - |`.
  `.planning/ROADMAP.md:1171-1176` documents that a consumer splits on the
  `## Progress` heading and reads column 1, so the row's SHAPE matters as much as its
  content -- preserve the pipe spacing exactly.
- `.planning/STATE.md:1111` -- the decision entry recording the exemption ("stay at
  tools/ghidra-runs/ ... Verified directly").
- `.planning/STATE.md:209-220` -- the Deferred Items ledger prose. It states "7 open"
  and says the count is computed directly from the file count in
  `.planning/todos/pending/` rather than by subtraction from a prior figure. MEASURED
  RIGHT NOW: that directory holds EIGHT files -- the guard todo was filed after this
  prose was written -- so the stated count is currently false, and closing the guard
  todo restores it to seven.

The three failures this plan removes, MEASURED at plan time:
`npm run test:automated` reports `tests 3584 / pass 3567 / fail 6`. Three are the
documented `anno-register.test.ts` floor. The other three are:
  - `docs-deferred-ledger.test.ts` -- "every pending todo has a row in STATE.md's
    Deferred Items section (AUDIT-04, direction A)";
  - `docs-deferred-ledger.test.ts` -- "planted violation: both predicates fire on
    synthetic input, and the real, corrected text is reported by neither", which fails
    on its real-text half;
  - a milestone-audit cascade -- "no milestone audit declares a gated status while any
    docs guard is red (D-12-02)".
All three name `2026-09-08-guard-ghidra-symlink-project-location` as the un-ledgered
stem. `docs-deferred-ledger.test.ts`'s predicates are `missingPendingStems()` (every
pending stem must appear in the section) and `wronglyListedCompletedStems()` (no
completed stem may appear there as Pending). CLOSING the todo satisfies both, because a
stem that was never listed and is no longer pending is invisible to each. Adding a
ledger row for work that is being finished in this same round would be the wrong fix --
it would record as deferred something that is not.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: PREP-05 satisfied, and Phase 40's plan accounting matching what is on disk</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    .planning/REQUIREMENTS.md (the PREP block around :54-59, the mapping table around
    :138-142, and the count prose around :145-160)
    .planning/ROADMAP.md (Phase 40's entry at :918-975 and the Progress row at :1226 --
    two ranges, one read each)
  </read_first>
  <action>
    In `REQUIREMENTS.md`, tick `PREP-05`'s checkbox and set its mapping-table row to
    Complete. Then correct its parenthetical: the clause naming `ghidra.analyze` as the
    single open deviation is now stale. Replace it with what is true -- the runs root is
    computed under the one root and Ghidra is handed a broker-minted, verified,
    relative-target alias handle; cite the plans that landed it. KEEP the measured
    sentence about `getAbsolutePath()`/`getCanonicalPath()` and the pointer to the note;
    those were already right and are the reason this closed. Add the closing date.

    Then reconcile the count prose -- carefully, because it is ALREADY inconsistent with
    itself and that inconsistency predates this gap. Derive each number mechanically
    from the files rather than adjusting the printed figures: count the requirement
    checkbox lines, count the mapping-table rows, and count how many of each are struck
    through. Then either correct the stale prose so every stated number matches a
    measurement, or -- if reconciling requires a judgment this plan has no basis to make
    (for example whether a withdrawn requirement counts toward "total") -- state the
    discrepancy explicitly with the measurements beside it and leave the judgment to the
    owner. DISCLOSE, do not silently pick a number: this project has already been bitten
    by census figures asserted rather than derived, and an audit that reads a
    confidently-wrong total is worse than one that reads an honest question.

    In `ROADMAP.md`, Phase 40's entry. The planner already added Waves 8-11, the four
    plan lines (unticked), the seven-of-eleven executed count over eleven waves, and the
    `**Gap closure — G-40-1**` paragraph. Verify each is present and correctly shaped
    rather than re-adding it -- a duplicated gap paragraph is a recorded failure mode of
    this exact step. What remains:
      - Add `PREP-05` to the `**Requirements**:` line. It is mapped to Phase 40 in
        `REQUIREMENTS.md` but missing here, which is a real inconsistency independent of
        this gap. Note the date it was added to scope, matching how the line already
        annotates `PREP-03`'s removal.
      - Tick the four plan checkboxes, 40-08 through 40-11, once each plan's summary
        exists on disk. Verify that by listing the summaries, not by assumption.
      - Change the executed count from seven of eleven to eleven of eleven. Leave the
        wave count and the sequential-ordering prose alone; both are already correct.
      - Update the Progress table row from seven of seven to eleven of eleven. Preserve
        the pipe spacing exactly -- a consumer splits on the `## Progress` heading and
        parses column 1, and this project has recorded a helper verb mangling that row's
        spacing. Leave the Status column alone; `/gsd-verify-work` owns it.

    Diff `ROADMAP.md` after every edit. Two specific corruptions have been observed in
    this exact file: a dependency-annotation verb running the last success criterion
    directly into the following bold heading, and the plan-progress verb's pipe-spacing
    damage. Check the boundary between the last success criterion and the `**Plans**`
    line specifically.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && test "$(grep -ac '^- \[x\] \*\*PREP-05\*\*' .planning/REQUIREMENTS.md)" -eq 1 && test "$(grep -ac '| PREP-05 | Phase 40 | Pending |' .planning/REQUIREMENTS.md)" -eq 0 && test "$(grep -ac '| PREP-05 | Phase 40 | Complete |' .planning/REQUIREMENTS.md)" -eq 1 && test "$(grep -ac 'getCanonicalPath' .planning/REQUIREMENTS.md)" -ge 1 && test "$(grep -ac 'PREP-05' .planning/ROADMAP.md)" -ge 1 && for p in 08 09 10 11; do test "$(grep -ac "^- \[x\] 40-$p-PLAN\.md" .planning/ROADMAP.md)" -eq 1 || { echo "PLAN 40-$p NOT TICKED EXACTLY ONCE"; exit 1; }; done && test "$(grep -ac '^- \[ \] 40-' .planning/ROADMAP.md)" -eq 0 && test "$(grep -ac '11/11 plans executed in 11 waves' .planning/ROADMAP.md)" -eq 1 && test "$(grep -ac '7/11 plans executed' .planning/ROADMAP.md)" -eq 0 && test "$(grep -ac '^\*\*Gap closure — .G-40-1' .planning/ROADMAP.md)" -eq 1 && test "$(grep -ac '| 40\. The Three Preprocessing Host Tools | v0.9.0 | 11/11 | ' .planning/ROADMAP.md)" -eq 1 && awk '/^## Progress/{p=1} p&&/^\| 40\./{n=split($0,a,"|"); exit !(n==7)}' .planning/ROADMAP.md && echo BOOKKEEPING_OK</automated>
  </verify>
  <done>
    `PREP-05` is checked and its mapping row reads Complete, with the stale
    single-deviation clause replaced and the measured `getCanonicalPath()` sentence
    retained. `ROADMAP.md`'s Phase 40 entry lists `PREP-05`, has all eleven plan boxes
    ticked, states eleven plans in rewritten (not merely renumbered) prose, names the
    four plans as `G-40-1` gap closure, and its Progress row reads eleven of eleven with
    its pipe structure intact. The requirement count prose either matches measurement or
    states the discrepancy with the measurements beside it. Phase 40's Status field is
    untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: The corrected decision in project state, and the guard todo closed -- which is what takes the suite back to its floor</name>
  <files>.planning/STATE.md, .planning/todos/pending/2026-09-08-guard-ghidra-symlink-project-location.md, .planning/todos/completed/2026-09-08-guard-ghidra-symlink-project-location.md</files>
  <read_first>
    .planning/STATE.md (the Deferred Items ledger prose at :205-221 and the decision
    entry at :1111 with a few lines either side -- two ranges, one read each; the file
    is ~2100 lines, do not read it whole)
    .planning/todos/pending/2026-09-08-guard-ghidra-symlink-project-location.md (whole
    file -- short)
    one file from .planning/todos/completed/ (for the `## Resolution` section
    convention 40-07 established)
  </read_first>
  <action>
    In `STATE.md`, amend the exemption decision entry in place. Keep the original
    sentence -- a decision log records what was decided and believed at a date, and this
    project's own guard documentation treats historical records as evidence rather than
    as drafts. Append a dated superseding clause naming gap `G-40-1`, stating the
    `getAbsolutePath()` mechanism, naming the superseded method (this project's own
    dot-segment predicate run against a synthetic string, which observes the project and
    not Ghidra), and citing
    `.planning/notes/ghidra-dot-path-check-semantics.md`.

    Then add ONE new decision entry for the resolution, in the file's existing
    `- [Phase 40]: ...` style, recording: the runs root now resolves under the one root;
    Ghidra is handed a non-dotted alias symlink with a relative target; the broker mints
    it at startup and the resolver re-asserts it as an idempotent precondition; an
    unverified handle refuses by name rather than being mkdir-ed through; and the
    fragility is covered by a live opt-in guard. One entry, not five -- this log is read
    linearly.

    Do NOT rewrite the Current Position block or any surrounding prose. Edit only the
    two regions named above, with scoped replacements, and diff after each.

    Then close the guard todo. `git mv` it from `.planning/todos/pending/` to
    `.planning/todos/completed/`, keeping the filename identical, and add a
    `## Resolution` section in the convention the completed directory already uses.
    That section must record:
      - WHERE the guard landed and under which plan, and that it is opt-in and
        default-SKIP in the manual-only live suite;
      - that it has TWO halves -- a positive half proving a real import plus analysis
        succeeds through the handle with the database physically under the dotted root,
        and a control half proving the same Ghidra still refuses a literal dotted
        location -- and why the control is what stops the guard being vacuous;
      - that neither half matches Ghidra's stderr text, per this todo's own prohibition;
      - the installed Ghidra version it was proved against, from plan 40-09's recorded
        transcript;
      - that the todo's blocking condition (gap `G-40-1` landing first) is discharged,
        naming the plans.

    Finally, reconcile the Deferred Items ledger prose. Recompute the open count from
    the actual file count in `.planning/todos/pending/` -- the prose itself insists the
    number is derived that way rather than by subtraction, so derive it. MEASURED at plan
    time: that directory held eight files while the prose said seven, because the guard
    todo was filed after the prose was written; closing it restores agreement. Add one
    clause recording that this todo was filed and closed within this round, so the
    narrative accounts for the round trip instead of silently reading correct again.

    Do NOT add a ledger row for the guard todo. It is being closed, not deferred, and
    the guard that currently fails is satisfied by its absence from the pending set.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && test ! -e .planning/todos/pending/2026-09-08-guard-ghidra-symlink-project-location.md && test -e .planning/todos/completed/2026-09-08-guard-ghidra-symlink-project-location.md && test "$(grep -ac '^## Resolution' .planning/todos/completed/2026-09-08-guard-ghidra-symlink-project-location.md)" -eq 1 && test "$(grep -ac '12.1.3' .planning/todos/completed/2026-09-08-guard-ghidra-symlink-project-location.md)" -ge 1 && N=$(ls .planning/todos/pending/*.md | wc -l) && test "$N" -eq 7 && test "$(grep -ac "reads \*\*$N open\*\*" .planning/STATE.md)" -eq 1 && test "$(grep -ac 'G-40-1' .planning/STATE.md)" -ge 1 && test "$(grep -ac 'getAbsolutePath' .planning/STATE.md)" -ge 1 && test "$(grep -ac 'ghidra-dot-path-check-semantics' .planning/STATE.md)" -ge 1 && test "$(grep -ac 'guard-ghidra-symlink-project-location' .planning/STATE.md)" -eq 0 && cd src/mcp/vice && node --test docs-deferred-ledger.test.ts 2>&1 | grep -aE '^. (tests|pass|fail) ' && node --test docs-deferred-ledger.test.ts 2>&1 | grep -aq '^. fail 0'</automated>
  </verify>
  <done>
    The guard todo lives in `.planning/todos/completed/` with a single `## Resolution`
    section naming both guard halves, the prohibition it honours, the Ghidra version it
    was proved against, and the discharged blocking condition. The pending directory
    holds seven files and `STATE.md`'s ledger states that same derived number with a
    clause accounting for the file-and-close round trip, and carries NO ledger row for
    the closed todo. The exemption decision entry keeps its original sentence plus a
    dated superseding clause naming `G-40-1`, the mechanism and the superseded method;
    one new entry records the resolution. `docs-deferred-ledger.test.ts` reports
    `fail 0`. The Current Position block is unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning bookkeeping -> milestone audits and requirement gates | Automated guards and audits read these files as authoritative; a wrong status here silently passes or fails a gate |
| helper tooling -> `.planning/*.md` | Recorded, repeatedly-observed corruptions from state/roadmap mutation verbs, several of which target exactly this file set |
| historical decision log -> future planners | The log is the evidence trail that made this gap diagnosable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-11-01 | Tampering | a mutation verb corrupting STATE.md or ROADMAP.md | high | mitigate | `<execution_constraints>` names seven specific recorded corruptions and requires hand `Edit` calls with a diff after each. Task 1's gate independently checks the Progress row's pipe structure with `awk`, the exact defect one of those verbs is known to cause |
| T-40-11-02 | Repudiation | rewriting the exemption decision entry | high | mitigate | Task 2 keeps the original sentence and appends a dated superseding clause. The wrong belief and its correction stay legible side by side; the log is what proved the claim was never measured |
| T-40-11-03 | Spoofing | satisfying a docs guard the wrong way | high | mitigate | The ledger guard is satisfied by CLOSING the todo, never by adding a row for work being finished in this same round. Task 2 forbids the row and its gate asserts the stem is absent from STATE.md, so a row cannot be quietly added to make the test green |
| T-40-11-04 | Tampering | asserting a requirement count instead of deriving it | medium | mitigate | Task 1 requires every number to come from a measurement and permits DISCLOSING an unreconcilable discrepancy rather than picking a figure. The count prose was already self-inconsistent before this gap; inventing a confident total would hide that from the next audit |
| T-40-11-05 | Elevation of privilege | marking the phase complete from a gap-closure plan | medium | mitigate | `<execution_constraints>` forbids touching Phase 40's Status field and names `/gsd-verify-work` as its owner. A recorded defect has a phase-complete verb flipping unmet requirements to Complete; nothing here may invoke it |
| T-40-11-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install task exists in this plan; it touches only Markdown. The package-legitimacy gate is not reached |
</threat_model>

<verification>
1. `git diff` on `.planning/STATE.md` -- confined to the two named regions plus the new
   decision entry. Specifically confirm no standalone negation elsewhere in the file was
   altered and the Current Position block is byte-identical: both are recorded
   corruptions from this file's mutation verbs.
2. `git diff` on `.planning/ROADMAP.md` -- confined to Phase 40's entry and the Progress
   row. Confirm the boundary between the last success criterion and the `**Plans**` line
   still has its blank line.
3. `cd src/mcp/vice && node --test docs-deferred-ledger.test.ts docs-dangling-refs.test.ts
   docs-review-disposition.test.ts docs-linerefs.test.ts` -- `fail 0`.
4. `npm run test:automated` -- expected to land at THREE failures, all in
   `anno-register.test.ts`: the documented floor. That is a DROP of three from the
   plan-time baseline of six, and it is this plan's most direct observable outcome.
   Do not assert zero.
   PRECONDITION: no VICE broker running (a live broker reddens the BACK-05 test
   deterministically -- a known state, not a flake). Re-run once before concluding:
   `audit-root-args.test.ts` carries a documented intermittent scratch-file race that can
   add a transient fourth.
5. `ls .planning/todos/pending/*.md | wc -l` equals the number STATE.md's ledger states.
6. Phase 40's `Status` field in the Progress table is unchanged from before this plan.
</verification>

<success_criteria>
- `PREP-05` reads Complete in both places, with its stale deviation clause replaced and
  its measured sentence kept.
- Phase 40's requirements line, eleven plan checkboxes, plan count prose and Progress
  row all agree with what is on disk, with the Progress row's pipe structure intact.
- The exemption decision entry carries a dated superseding clause; one new entry records
  the resolution; the Current Position block is untouched.
- The guard todo is in `completed/` with a Resolution section citing both guard halves
  and the proved Ghidra version.
- `npm run test:automated` reports three failures, all `anno-register.test.ts` -- down
  three from the recorded baseline.
- Phase 40's status is left for `/gsd-verify-work`.
</success_criteria>

<output>
Create `.planning/phases/40-the-three-preprocessing-host-tools/40-11-SUMMARY.md` when done.

Record explicitly:
- The `npm run test:automated` numbers before and after, and the confirmation that the
  three removed failures were the ledger ones.
- The derived requirement counts, and either the reconciliation or the disclosed
  discrepancy with its measurements.
- The `git diff --stat` for `STATE.md` and `ROADMAP.md`, as evidence the edits stayed
  scoped.
- Confirmation that Phase 40's Status field was not touched.
</output>
