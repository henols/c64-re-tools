---
phase: 40-the-three-preprocessing-host-tools
plan: 05
type: execute
wave: 5
depends_on: ["40-04"]
files_modified:
  - src/skills/c64-ram-capture/SKILL.md
  - src/skills/c64-program-recon/SKILL.md
  - src/skills/c64-disk-access/SKILL.md
  - src/skills/c64-petcat/SKILL.md
  - CLAUDE.md
  - scripts/check-skill-tool-coverage.mjs
  - scripts/lib/skill-descriptions.mjs
  - src/mcp/vice/skill-honesty-checks.test.ts
  - src/mcp/vice/dxa-seam.test.ts
autonomous: true
requirements: [PREP-01, PREP-02]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every skill's description is TRUE of what that skill now owns: the RAM-capture skill no longer claims disk-image structure work, and the recon skill points at the two new skills for stub work instead of describing it."
    - "The pairwise description-collision list over the whole nine-skill inventory is empty, with no allowlist entry of any kind — handled by making the descriptions accurate, not by tuning wording around a threshold (D-19)."
    - "Every row of the project-skills table in the project instructions file is byte-identical to its skill's frontmatter description, and there are nine rows."
    - "No first-party prose still asserts a skill count that a one-command directory listing contradicts. The real post-phase total is NINE — seven pre-existing plus two new — not the eight the phase context's arithmetic states; the discrepancy is recorded rather than silently carried."
    - "No test writes a fixed-name scratch artifact into a directory another concurrently-running test file's walk observes; the intermittent multi-failure suite run this caused stops being a false signal (D-27)."
  artifacts:
    - src/skills/c64-ram-capture/SKILL.md
    - src/skills/c64-program-recon/SKILL.md
    - src/mcp/vice/skill-honesty-checks.test.ts
  key_links:
    - "each `SKILL.md` frontmatter description → the project-skills table row → the pairwise-overlap scorer; all three must agree byte-for-byte."
    - "the scratch artifact written inside a walked skill directory → the CI gate spawned by a concurrently-running test file → the intermittent failure."
  prohibitions:
    - statement: "No allowlist entry is added to the description-collision check; a collision is fixed by making the descriptions true, never by suppressing the report (D-19)."
      status: active
      verification: flagged-unverified
    - statement: "No skill description is tuned to sit just under a similarity threshold while remaining inaccurate."
      status: active
      verification: flagged-unverified
    - statement: "No first-party prose states a skill count that a directory listing contradicts."
      status: active
      verification: flagged-unverified
    - statement: "Neither new skill declares a backend posture — they name no emulator tool at all, so the documentation-honesty gate is silent by construction and needs no declaration (D-20)."
      status: active
      verification: flagged-unverified
    - statement: "This plan does not delete either replaced module or its test — the deletions land last, in 40-06, on a branch merged by hand (D-36 constraint 2)."
      status: active
      verification: flagged-unverified
    - statement: "No move of tests into a separate tree — this phase adds tests in the existing colocated convention and must not be the wedge for changing it."
      status: active
      verification: flagged-unverified
---

<!-- Literals below appear verbatim in verify greps AND in plan prose; the
     markers exempt exactly these literals from the comment-text discipline
     gate. They are file paths, table pins and document claims, not code the
     executor is being told to write into a comment. -->
<!-- planner-discipline-allow: six skills -->
<!-- planner-discipline-allow: d64-parse -->

<objective>
Re-cut the skill surface so every description is true of what its skill now owns,
and take the carried scratch-fixture fix that has been deferred twice.

Purpose: two new skills landed in 40-02 and 40-03. The RAM-capture skill's
description still claims disk-image work it no longer owns, and the recon skill's
still describes stub work the conversion skill now owns — both would collide with
the new descriptions. `D-19` settles how: make the descriptions ACTUALLY TRUE
rather than tune wording around a similarity threshold. The carried scratch fix
rides here because this is the phase that touches skill-script trees, which is
the eligibility the roadmap itself named; it has been carried twice, and a third
carry means a fourth.

Output: four re-cut descriptions, a nine-row project-skills table, corrected
first-party count prose, and no test writing a fixed-name artifact into a walked
tree.
</objective>

<skill_count_correction>
The phase context's `D-17` states the skill count goes "six → eight". A directory
listing of the skills tree returns **seven** pre-existing skills
(`acme-build`, `c64-memory-mapping`, `c64-program-recon`, `c64-provenance-diff`,
`c64-ram-capture`, `routine-queue-walker`, `vice-wedge-triage`). With the two
added by 40-02 and 40-03 the real post-phase total is **NINE**.

Nothing is blocked by this: the count assertions in the overlap check and the
packaging check are relation-based floors, not magic numbers. But two first-party
scripts carry literal "six skills" prose that is ALREADY stale today at seven,
and this plan is the natural place to fix it. State nine — recomputed from the
tree, not carried from the context's arithmetic — in every artifact and commit
message.
</skill_count_correction>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-the-three-preprocessing-host-tools/40-CONTEXT.md
@.planning/phases/40-the-three-preprocessing-host-tools/40-RESEARCH.md
@.planning/phases/40-the-three-preprocessing-host-tools/40-02-SUMMARY.md
@.planning/phases/40-the-three-preprocessing-host-tools/40-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-cut the two overlapping descriptions so they are true, and point the recon skill at the new owners</name>
  <files>src/skills/c64-ram-capture/SKILL.md, src/skills/c64-program-recon/SKILL.md, src/skills/c64-disk-access/SKILL.md, src/skills/c64-petcat/SKILL.md, CLAUDE.md</files>
  <read_first>
    - src/skills/c64-ram-capture/SKILL.md (the whole file; its frontmatter description, the tool-path preamble at :18, the disk-structure prose at :62 and :81, and the script inventory row at :422 — all four cite the module being replaced)
    - src/skills/c64-program-recon/SKILL.md (the whole file; find its "before disassembling anything" step, which is where the pointer goes)
    - src/skills/c64-disk-access/SKILL.md and src/skills/c64-petcat/SKILL.md (as committed by 40-02 and 40-03 — the descriptions the two re-cuts must not collide with)
    - src/mcp/vice/skill-description-overlap.test.ts:280-360 (the pairwise scorer, the real-inventory false-positive control, the empty-allowlist assertion, and the byte-identical project-instructions comparison)
    - CLAUDE.md:269-281 (the project-skills table between its markers)
    - .planning/phases/40-the-three-preprocessing-host-tools/40-CONTEXT.md (D-17, D-19 — the ownership split and the make-it-true instruction)
  </read_first>
  <action>
    Rewrite the RAM-capture skill's frontmatter description so it claims only what
    it still owns — capturing a running machine's full memory as a verified flat
    image, proving two captures equivalent, and depacking by running — and drop
    every disk-image structure claim from it. Then remove the four in-body
    citations of the module being replaced: the tool-path preamble line, the two
    prose paragraphs describing what that module answers about a disk image, and
    the script-inventory row. In their place put a single short pointer to the new
    disk-access skill as the owner of disk-image structure, phrased so it does not
    re-describe that skill's capabilities (a re-description would recreate the
    collision this task exists to remove). Leave the module file itself alone —
    the deletion is 40-06's, on a branch merged by hand.

    Rewrite the recon skill's frontmatter description so the stub half becomes a
    pointer rather than a capability: it still owns working out how an unknown
    program is structured at runtime, but the "what does the loader stub do and
    where does it hand over" step now names the conversion skill as the owner
    instead of describing the work. Add the pointer at the "before disassembling
    anything" step in the body, naming both new skills — disk structure before
    spending a disassembler on an image, and the stub's handover point or its
    named decline.

    If the pairwise scorer still reports any collision after these edits, fix it
    by narrowing whichever description is making a claim that is not true of its
    own skill — never by adding an allowlist entry, and never by rewording around
    the threshold while leaving the claim inaccurate. The allowlist must stay
    empty; a committed assertion checks that.

    Update the project-skills table in the project instructions file so it has
    nine rows and every row is byte-identical to its skill's frontmatter
    description. Both new rows were added by 40-02 and 40-03; the two re-cut rows
    change here.
  </action>
  <acceptance_criteria>
    - The RAM-capture skill's frontmatter description contains no disk-image structure claim.
    - `grep -ac 'd64-parse' src/skills/c64-ram-capture/SKILL.md` is 0 — all four citations are gone.
    - The recon skill's body names both new skills at its before-disassembling step.
    - The project-skills table has exactly nine rows.
    - The pairwise description-collision list is empty and the allowlist is empty.
    - Every table row is byte-identical to its skill's frontmatter description.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test skill-description-overlap.test.ts 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, or a "# fail" line whose count is anything other than 0</fails_when>
    <automated>grep -ac 'd64-parse' src/skills/c64-ram-capture/SKILL.md</automated>
    <fails_when>prints anything other than 0 — a surviving citation means the skill still claims a module it no longer owns and that 40-06 is about to delete</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && awk '/GSD:skills-start/,/GSD:skills-end/' CLAUDE.md | grep -ac '^| [a-z0-9-]* |'</automated>
    <fails_when>prints anything other than 9 — the table must carry one row per skill directory in the tree</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && ls -1 src/skills | wc -l</automated>
    <fails_when>prints anything other than 9 — the tree and the table must agree</fails_when>
  </verify>
  <done>Four descriptions are true of what their skills own, the recon skill points rather than describes, the table has nine agreeing rows, and no collision is suppressed by an allowlist.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the stale first-party skill-count prose</name>
  <files>scripts/check-skill-tool-coverage.mjs, scripts/lib/skill-descriptions.mjs</files>
  <read_first>
    - scripts/check-skill-tool-coverage.mjs:1-20 (the header prose stating a skill count)
    - scripts/lib/skill-descriptions.mjs:60-80 (the doc comment stating a skill count)
    - src/mcp/vice/skill-description-overlap.test.ts:300-360 (the relation-based floors, so the executor can see nothing pins an exact number)
  </read_first>
  <action>
    Both files carry a literal count in their prose that a directory listing
    already contradicts today, before this phase adds anything. Replace each with
    wording that states the relation rather than a number — that the check runs
    over every skill directory carrying a skill definition file, derived from the
    tree at run time — so the prose cannot go stale again the next time a skill is
    added or removed. Where the surrounding sentence genuinely needs a magnitude,
    give the current figure with the date it was true, following the project's own
    dated-rider convention, rather than a bare number.

    Do not change either script's behaviour. Both already derive their inventory
    from the tree; only the prose describing them is wrong.
  </action>
  <acceptance_criteria>
    - Neither file contains a bare skill-count claim that a directory listing contradicts.
    - Both scripts still exit 0 against the current tree, with unchanged behaviour.
    - Any magnitude that remains carries the date it was measured.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -aci 'six skills' scripts/check-skill-tool-coverage.mjs scripts/lib/skill-descriptions.mjs</automated>
    <fails_when>any line ending in a count other than 0 — a surviving literal claim is stale prose the tree already contradicts</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-skill-tool-coverage.mjs >/dev/null 2>&1; echo "COVERAGE_EXIT=$?"</automated>
    <fails_when>a COVERAGE_EXIT line carrying anything other than 0 — the prose edit must not change behaviour</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-skill-fork-honesty.mjs >/dev/null 2>&1; echo "HONESTY_EXIT=$?"</automated>
    <fails_when>a HONESTY_EXIT line carrying anything other than 0</fails_when>
  </verify>
  <done>Neither script's prose states a count the tree contradicts, and both still exit 0 with unchanged behaviour.</done>
</task>

<task type="auto">
  <name>Task 3: Take the carried scratch-fixture fix at both named sites</name>
  <files>src/mcp/vice/skill-honesty-checks.test.ts, src/mcp/vice/dxa-seam.test.ts</files>
  <read_first>
    - src/mcp/vice/skill-honesty-checks.test.ts:90-135 (the helper that writes a FIXED-name scratch file directly into a walked skill directory, its comment explaining why a system temp directory would not work, and the three cases that call it with three different fixed names)
    - src/mcp/vice/dxa-seam.test.ts:190-232 (the helper that plants a throwaway executable at the first location the binary probe checks, and its cleanup)
    - src/mcp/vice/dxa-live.test.ts:195-210 and :275-285 (the unique-temp-directory idiom already used correctly elsewhere in this same tree)
    - src/mcp/vice/audit-root-args.test.ts:1035-1060 (a third site that already creates a uniquely-named scratch tree inside the repository — the shape the first site should converge on)
    - .planning/RETROSPECTIVE.md (the entry naming both sites, the concurrency cause, and that no pass has owned the fix)
    - .planning/phases/40-the-three-preprocessing-host-tools/40-RESEARCH.md (Pitfall 8 — the exact remedy shape and the caveat that a system temp directory is the WRONG shape for the first site, because the scanner under test must still walk over the artifact)
  </read_first>
  <action>
    **First site.** The helper writes a fixed-name scratch file directly into a
    walked skill directory and removes it in a `finally`. Node's test runner
    executes test FILES concurrently, so another file's spawned scanner can
    observe that artifact mid-write or mid-delete. Replace the fixed name with a
    uniquely-named scratch DIRECTORY created inside that same walked skill
    directory — the unique-directory idiom already used correctly twice elsewhere
    in this tree — and write the scratch file inside it. The artifact must stay
    INSIDE the walked tree: a system temp directory would never be seen by the
    scanner under test at all, which is the whole point of the helper, and is the
    single most likely wrong fix. Remove the whole scratch directory recursively
    in the cleanup, and use the test runner's own after-hook so cleanup still runs
    when an assertion throws. Update the helper's comment to say why the artifact
    is inside the tree AND why its name is unique per invocation, naming the
    concurrent-scanner race as the cause.

    **Second site.** The helper plants a throwaway executable at the FIRST
    location the production binary probe checks, so its path is dictated by
    production code and a unique name may not be available. Re-read the probe's
    candidate list and decide: if the probe's first candidate can be redirected
    for the duration of the test without changing production behaviour, apply the
    same unique-directory idiom. If it cannot, do NOT force an inapplicable fix —
    instead record the assessment in a comment at the site, stating which
    directory walks observe the planted subtree and which do not, why the planted
    file's name does not match the generated-file filter the sync check applies,
    and what would have to change for the unique-directory idiom to become
    available. Either way, make the cleanup run on an assertion failure as well as
    on success, and remove only the planted subtree.

    Record in the plan summary which of the two branches was taken at the second
    site and on what evidence. This item has been carried twice; leaving it
    half-assessed is how it gets carried a third time.
  </action>
  <acceptance_criteria>
    - The first site creates a uniquely-named scratch directory per invocation inside the walked skill directory; no fixed-name scratch artifact remains in the helper.
    - The scratch artifact is NOT written to a system temp directory — the scanner under test must still walk over it, and a case proves the scanner still sees it (the deliberately-violating case still fails the gate).
    - Both sites clean up on an assertion failure as well as on success, leaving no scratch artifact behind.
    - The second site either uses the unique-directory idiom or carries a comment recording the assessment, naming what observes the planted subtree and what would make the idiom available.
    - Running the two touched test files twenty times in a row produces no failure.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && for i in $(seq 1 20); do node --test skill-honesty-checks.test.ts dxa-seam.test.ts audit-root-args.test.ts >/dev/null 2>&1 || { echo "RUN_$i_FAILED"; exit 1; }; done; echo REPEAT20_OK</automated>
    <fails_when>the literal REPEAT20_OK line is absent, or any RUN_n_FAILED line — an intermittent failure across twenty runs is the race this task closes, not a flake to retry</fails_when>
    <automated>node --test src/mcp/vice/skill-honesty-checks.test.ts >/dev/null 2>&1; git status --porcelain src/skills src/mcp/vice/resources | wc -l</automated>
    <fails_when>prints anything other than 0 — a scratch artifact survived the run and is now sitting untracked inside a walked tree</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated 2>&1 | tail -30</automated>
    <fails_when>a "# fail" line whose count exceeds 2, or any failing test outside `anno-register.test.ts` — 2 failing in that one file is this repository's known floor as of 2026-09-03. Stop any running VICE broker first: a live broker deterministically reddens the BACK-05 D-G ordering test.</fails_when>
  </verify>
  <done>Neither site writes a fixed-name scratch artifact into a walked tree, both clean up on failure, twenty consecutive runs are clean, and the second site's branch is recorded with its evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped skill prose → the user | A description is a discovery surface; an untrue one routes a user to the wrong tool. |
| test scratch artifact → a concurrently-running CI gate | A deliberately-violating artifact planted inside a scanned tree is observable by another process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-05-01 | Spoofing | a skill description claiming capability it does not own | medium | mitigate | Descriptions are re-cut to be true rather than tuned around a threshold; the pairwise scorer with an empty allowlist is the committed check. |
| T-40-05-02 | Repudiation | an intermittent multi-failure suite run treated as a regression | medium | mitigate | The fixed-name scratch artifact is replaced with a uniquely-named per-invocation directory; twenty consecutive clean runs is the acceptance bar, so the false signal stops masking real regressions. |
| T-40-05-03 | Tampering | a deliberately-violating scratch artifact left behind after a failed assertion | medium | mitigate | Cleanup moves to the runner's after-hook so it runs on failure too, and a committed check asserts the working tree is clean after a run. |
| T-40-05-04 | Information Disclosure | first-party prose stating a false magnitude | low | mitigate | Counts become relation statements, or carry the date they were measured. |
| T-40-05-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this phase. |
</threat_model>

## Artifacts this phase produces

Created or changed by **this plan** (no new symbols; this plan is prose and test
hygiene):

- Re-cut frontmatter descriptions for the RAM-capture and recon skills
- A pointer section in the recon skill's before-disassembling step naming the two
  new skills
- A nine-row project-skills table in the project instructions file
- A uniquely-named per-invocation scratch directory inside the walked skill
  directory, replacing three fixed-name scratch files

The full phase-wide new-symbol set is listed in `40-01-PLAN.md`'s own
"Artifacts this phase produces" section.

<verification>
- `node --test skill-description-overlap.test.ts skill-honesty-checks.test.ts dxa-seam.test.ts audit-root-args.test.ts` reports `# fail 0`.
- Twenty consecutive runs of the two touched test files plus the victim file are clean.
- `node scripts/check-skill-tool-coverage.mjs` and `node scripts/check-skill-fork-honesty.mjs` both exit 0.
- The skills tree and the project-skills table both have nine entries.
- The full automated suite is at or below its known floor.
</verification>

<success_criteria>
- Every description is true of what its skill owns, with an empty collision list and an empty allowlist.
- The recon skill points at the new owners rather than describing their work.
- No first-party prose states a skill count the tree contradicts; the real total is stated as nine.
- Neither carried scratch site writes a fixed-name artifact into a walked tree, and the second site's assessment is recorded rather than left open for a third carry.
</success_criteria>

<output>
Create `.planning/phases/40-the-three-preprocessing-host-tools/40-05-SUMMARY.md` when done.
</output>
