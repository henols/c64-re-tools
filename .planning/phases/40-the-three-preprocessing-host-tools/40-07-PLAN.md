---
phase: 40-the-three-preprocessing-host-tools
plan: 07
type: execute
wave: 7
depends_on: ["40-06"]
worktree: false
worktree_reason: "This plan's deliverables ARE .planning/ROADMAP.md, .planning/REQUIREMENTS.md and .planning/STATE.md content plus three todo-file moves. Worktree executors may not touch ROADMAP.md or STATE.md and execute-plan strips both from the commit, so under isolation this plan cannot deliver at all — silently. Dispatch it with USE_WORKTREES_FOR_PLAN=false, sequentially on the main tree, and let it own its own tracking writes. REQUIREMENTS.md is not covered by that carve-out, but the two amendments belong in one commit (D-36 constraint 3)."
files_modified:
  - docs/phase40-preprocessing-tools-decisions.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/todos/pending/2026-09-03-wr-03-host-tool-never-throws-contract-has-two-holes.md
  - .planning/todos/pending/2026-09-07-consolidate-all-tool-written-files-under-c64-re-tools.md
  - .planning/todos/pending/2026-09-07-installer-must-self-ignore-its-deployed-tools-in-the-consumer-repo.md
autonomous: true
requirements: [PREP-03, PREP-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The disk-parser supersession is recorded as a decision with its measured reasoning, and the two locked statements it reverses are amended IN PLACE with dated riders naming what changed and on what evidence — never silently deleted (D-04, D-29)."
    - "The excluded cartridge requirement's line stays in the requirements document, struck as removed with a dated rider naming the 2026-09-08 discussion and the owner decision, and a row is added to the excluded table. Deleting it outright would leave the coverage claims as unexplained edits rather than dated corrections (D-31)."
    - "After the strike the coverage block reads exactly 19 total, 19 mapped, 0 unmapped, and the traceability table carries 19 live rows with the removed id's row struck in place rather than deleted (PREP-03 · boundary edge, D-31)."
    - "The struck requirement line and the new excluded-table row are two SEPARATE records; the pre-existing excluded row that justified itself by reference to the live requirement is amended with its own dated rider rather than merged with the new one (PREP-03 · adjacency edge)."
    - "The removed id still occurs in both planning documents after the strike — the amendment is in place, never a deletion — and every remaining occurrence sits inside a struck line, a dated rider, or the excluded table. A zero-occurrence result is a failure, not a pass (PREP-03 · empty edge)."
    - "No row is re-sorted: the traceability table's order is unchanged apart from the struck row, and the new excluded-table row is appended last (PREP-03 · ordering edge)."
    - "Every requirements-mapped claim in the roadmap is corrected from the old total to 19 with a dated rider; none is left reading the old total and none is silently deleted. The plan-count claims elsewhere in the same document that coincidentally carry the same numerals are NOT touched (PREP-03 · precision edge)."
    - "Each folded todo's file move and its ledger row removal land in the SAME commit — the ledger guard fails in both directions and a half-finished bookkeeping edit reds the suite (D-28)."
  artifacts:
    - docs/phase40-preprocessing-tools-decisions.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  key_links:
    - "each todo file under the completed tree ↔ its row in the state document's deferred-items ledger; the guard fails in both directions, so the two moves must be one commit."
    - "the roadmap's requirements-mapped claims ↔ the requirements document's coverage block; both must read the same corrected total."
  prohibitions:
    - statement: "No requirement line, excluded row, or coverage claim is deleted outright — every correction is an in-place amendment with a dated rider (D-29, D-31)."
      status: active
      verification: flagged-unverified
    - statement: "No todo file is moved without removing its ledger row in the same commit, and no ledger row is removed without moving its file in the same commit (D-28)."
      status: active
      verification: flagged-unverified
    - statement: "The vicerc scratch-directory reaping todo is NOT folded — it belongs to a broker lifecycle change this phase never opens (D-28), and no retention policy is invented here (D-35)."
      status: active
      verification: flagged-unverified
    - statement: "No cartridge requirement is reinstated, restated as deferred-to-a-named-phase, or given a successor id — it left the roadmap by owner direction and returns, if ever, as a new requirement (D-30)."
      status: active
      verification: flagged-unverified
    - statement: "No plan-count figure elsewhere in the roadmap is edited while correcting the requirements-mapped figures, even where the numerals coincide."
      status: active
      verification: flagged-unverified
---

<!-- Literals below appear verbatim in verify greps AND in plan prose; the
     markers exempt exactly these literals from the comment-text discipline
     gate. They are file paths, table pins and document claims, not code the
     executor is being told to write into a comment. -->
<!-- planner-discipline-allow: PREP-03 -->
<!-- planner-discipline-allow: 20/20 requirements mapped -->
<!-- planner-discipline-allow: 19/19 requirements mapped -->
<!-- planner-discipline-allow: 20/20 plans -->
<!-- planner-discipline-allow: 19 total -->
<!-- planner-discipline-allow: Mapped to phases: 19 -->
<!-- planner-discipline-allow: 2026-09-08 -->

<objective>
Close the phase's record: write the supersession decision down with its measured
reasoning, amend the four locked statements it and the cartridge removal reverse,
correct every dependent count, and fold the three todos this phase resolved.

Purpose: `PREP-03`'s entire remaining deliverable is this bookkeeping. The
cartridge requirement was removed by owner direction on 2026-09-08 and nothing is
built for it — but its line, its traceability row, the excluded row that depended
on it, and three requirements-mapped claims all still describe a live
requirement. `D-29` and `D-31` fix them the way this project always fixes a
reversed decision: in place, with a dated rider, following the eleven amended ids
in the previous milestone's requirements record.

Output: one decisions document, four amended statements, corrected counts, three
folded todos with their ledger rows removed in the same commits, and the phase's
decisions recorded in the project state.
</objective>

<dispatch_note>
**Dispatch this plan with `USE_WORKTREES_FOR_PLAN=false`.** Its deliverables are
planning-tree content that worktree executors may not touch and that the commit
step strips — under isolation it would report success while delivering nothing.
Two operational details for whoever dispatches it: the per-plan isolation
sentinel and the guard that reads it disagree about the plan identifier format,
so confirm the carve-out actually took effect rather than assuming; and the
carve-out is a stock per-plan mechanism, not a reason to disable isolation for
the phase.
</dispatch_note>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/40-the-three-preprocessing-host-tools/40-CONTEXT.md
@.planning/phases/40-the-three-preprocessing-host-tools/40-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the supersession decision down with its measured reasoning</name>
  <files>docs/phase40-preprocessing-tools-decisions.md</files>
  <read_first>
    - docs/phase39-dual-channel-coexistence-gate-findings.md (the machine-readable-verdict-in-a-findings-document shape this one follows: frontmatter carrying the decision fields, then the derivation in prose)
    - .planning/phases/40-the-three-preprocessing-host-tools/40-CONTEXT.md (D-04, D-05, D-06, D-07, D-08, D-25, D-29 — the decision, its three accepted costs, and the measured evidence that made it the right call)
    - .planning/phases/40-the-three-preprocessing-host-tools/40-02-SUMMARY.md, 40-04-SUMMARY.md, 40-06-SUMMARY.md (what was actually built and deleted, so the document records outcomes rather than intentions)
    - .planning/REQUIREMENTS.md:160-176 (the notes naming this as one of two decisions to be reached and recorded, not features to be built)
  </read_first>
  <action>
    Create `docs/phase40-preprocessing-tools-decisions.md` carrying the
    supersession decision and its derivation, in the same
    machine-readable-verdict shape the previous phase's findings document uses:
    frontmatter with the decision, its date, the decision ids it implements and
    the two statements it reverses; then the prose.

    Record the evidence that made it the right call, as measured rather than
    asserted: the allocation map the utility returns is PER SECTOR where the
    replaced parser had only per-track free counts, and the entry verb returns a
    first track/sector plus the raw entry bytes plus the next-directory pointer —
    strictly richer inputs than the parser computed, which is why the detector
    port made signature 3 sharper rather than merely equivalent.

    Record the three costs, stated and accepted rather than discovered: the
    replaced parser worked whether or not the emulator was up and the seam route
    on the container route does not; the fakery detector had no equivalent and had
    to be ported; and the duplicate module's blast radius reached three live
    tests, a registration with a pinned symbol-and-line reference, a packaged file
    set, a packaged-import set and four prose citations, one of which a committed
    test asserted the presence of.

    Record the acknowledged circularity of the synthetic fixture — the tool under
    test built it — and name where the mitigating assertion against a real,
    independently-produced image lives.

    Record the two deliberate divergences future readers will otherwise re-derive:
    version probing was dropped by owner decision even though the roadmap's own
    notes ask for it, and no mutating verb ships even though mutating verbs were
    used once, outside the seam, to author the fixture.

    Record the cartridge removal as a separate, dated section: what was removed,
    by whose direction, on what date, and that it left the roadmap rather than
    being deferred to a named phase — so a reader who finds the struck requirement
    line has one place to go for why.
  </action>
  <acceptance_criteria>
    - `docs/phase40-preprocessing-tools-decisions.md` exists, is git-tracked, and carries frontmatter naming the decision and the date.
    - It names both reversed statements and where each was amended.
    - It states all three accepted costs and the acknowledged fixture circularity with the location of the mitigating assertion.
    - It states both deliberate divergences.
    - It carries a dated cartridge-removal section naming the owner direction.
  </acceptance_criteria>
  <verify>
    <automated>git ls-files -- docs/phase40-preprocessing-tools-decisions.md | wc -l</automated>
    <fails_when>prints 0 — the decisions document was not committed</fails_when>
    <automated>grep -ac '2026-09-08' docs/phase40-preprocessing-tools-decisions.md</automated>
    <fails_when>prints 0 — the record carries no date, so a later reader cannot tell when the reversal was taken</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-dangling-refs.test.ts docs-linerefs.test.ts 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, or a "# fail" line whose count is anything other than 0 — a new document naming a phase number in a shipped string literal, or citing a line reference that has drifted, reds these</fails_when>
  </verify>
  <done>The decision, its measured evidence, its three accepted costs, its circularity, its two divergences and the cartridge removal are all recorded in one dated document.</done>
</task>

<task type="auto">
  <name>Task 2: Amend the four locked statements in place and correct every dependent count</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md:916-941 (this phase's entry: the goal sentence, success criterion 1's deferred-on-the-record clause, success criterion 3, success criterion 4's three-tool phrasing, the requirements line, and the notes bullet asking for version probing)
    - .planning/ROADMAP.md:16 and :537 and :1282 (the three requirements-mapped claims; re-verify these line numbers against the file rather than trusting them — they were measured on 2026-09-08)
    - .planning/ROADMAP.md:363 and :1166 (plan-count rows that carry the same numerals for a DIFFERENT quantity — do not touch these)
    - .planning/REQUIREMENTS.md:54-57 (the four requirement lines), :108-109 (the two excluded rows: the bank-qualified-addressing row that justifies itself by reference to the removed id, and the parser-supersession row this phase reverses), :135-138 (the traceability rows), :143-149 (the coverage block and the mapped-and-cross-checked paragraph)
    - .planning/milestones/v0.8.0-REQUIREMENTS.md (the eleven amended ids — the in-place dated-rider precedent both amendments follow)
    - docs/phase40-preprocessing-tools-decisions.md (as committed by Task 1 — every rider points at it)
  </read_first>
  <action>
    Amend, never delete. Every correction below is an in-place edit carrying a
    dated rider that says what changed and on what evidence, pointing at the
    decisions document Task 1 committed. That is this project's own precedent from
    the previous milestone's eleven corrected ids, and it is why the coverage
    figures read as dated corrections rather than unexplained edits.

    **In the roadmap.** Amend this phase's goal sentence so it describes the two
    tools actually built rather than three. Amend success criterion 1: strike the
    clause asserting the parser is untouched and undeprecated and that the
    supersession is deferred on the record, and replace it with a dated rider
    stating the supersession was reached and executed in this phase, on what
    measured evidence, and where the decision is recorded. Amend success criterion
    3 in place: strike it as removed with a dated rider naming the owner direction
    and the date — do not renumber the surviving criteria, because a renumbering
    turns every external reference to criterion 4 into a silent mis-citation.
    Amend success criterion 4's three-tool phrasing to name the two tools that
    ship. Amend the requirements line so the removed id is shown as struck rather
    than dropped. Amend the notes bullet asking for a version probe with a dated
    rider recording that the owner dropped the version half on 2026-09-08, so a
    later planner reading the bullet directly does not re-add it.

    **The three requirements-mapped claims.** Correct each to the new total with a
    dated rider. Locate them by searching for the claim's own wording rather than
    by line number, and verify before editing that each match is genuinely about
    requirements and not about a plan count — two rows elsewhere in the same
    document carry the same numerals for a different quantity and must not be
    touched.

    **In the requirements document.** Strike the removed requirement's line in
    place with a dated rider naming the 2026-09-08 discussion and the owner
    decision. Append a new row to the excluded table recording the removal, and
    amend the pre-existing bank-qualified-addressing row separately with its own
    dated rider, because that row currently justifies itself by reference to a
    requirement that is no longer live — two records, not one merged row. Amend
    the parser-supersession excluded row in place with a dated rider recording
    that it is reversed by this phase and where the decision lives. Strike the
    removed id's traceability row in place, leaving every other row in its
    existing order. Correct the coverage block to 19 total, 19 mapped, 0 unmapped,
    and amend the mapped-and-cross-checked paragraph so its per-phase breakdown
    names the surviving ids for this phase.

    Re-run the mechanical cross-check the coverage paragraph claims: count the ids
    in the roadmap's per-phase requirements lines, count the live rows in the
    traceability table, and confirm both equal 19 with no duplicates, no orphans
    and no extras. If they disagree, fix the documents rather than the claim.
  </action>
  <acceptance_criteria>
    - The coverage block reads 19 total, 19 mapped, 0 unmapped.
    - The traceability table has 19 live rows plus the removed id's row struck in place; no row has been re-sorted.
    - The removed id still occurs in both planning documents, and every occurrence is inside a struck line, a dated rider, or the excluded table.
    - The excluded table gained exactly one new row, appended last, and the bank-qualified-addressing row carries its own separate dated rider.
    - No requirements-mapped claim still reads the old total; the two plan-count rows carrying the same numerals are unchanged.
    - The roadmap's success criteria are NOT renumbered.
    - The version-probe notes bullet carries a dated rider recording that it was dropped.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac 'PREP-03' .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
    <fails_when>any line ending in a count of 0 — the amendment is in place, so the id must still occur in both documents; a zero means it was deleted outright instead of struck</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac '19 total' .planning/REQUIREMENTS.md; grep -ac 'Mapped to phases: 19' .planning/REQUIREMENTS.md</automated>
    <fails_when>either command prints 0 — the coverage block was not corrected to the post-strike figures</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac '20/20 requirements mapped' .planning/ROADMAP.md</automated>
    <fails_when>prints anything other than 0 — a surviving uncorrected requirements-mapped claim</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac '19/19 requirements mapped' .planning/ROADMAP.md</automated>
    <fails_when>prints fewer than 2 — the corrected claim must appear wherever the old one did</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac '20/20 plans' .planning/ROADMAP.md</automated>
    <fails_when>prints anything other than 2 — the two plan-count rows carry the same numerals for a different quantity and must be left untouched; a lower count means one was edited by mistake</fails_when>
  </verify>
  <done>Four locked statements are amended in place with dated riders, the coverage figures read 19 across both documents, no row is re-sorted or renumbered, and the plan-count rows are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Fold the three resolved todos and record the phase's decisions in the project state</name>
  <files>.planning/todos/pending/2026-09-03-wr-03-host-tool-never-throws-contract-has-two-holes.md, .planning/todos/pending/2026-09-07-consolidate-all-tool-written-files-under-c64-re-tools.md, .planning/todos/pending/2026-09-07-installer-must-self-ignore-its-deployed-tools-in-the-consumer-repo.md, .planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (the current-position block, and the deferred-items ledger table and the paragraph above it stating the open count)
    - .planning/todos/pending/2026-09-03-wr-03-host-tool-never-throws-contract-has-two-holes.md (the todo resolved by 40-01)
    - .planning/todos/pending/2026-09-07-consolidate-all-tool-written-files-under-c64-re-tools.md (resolved by 40-01)
    - .planning/todos/pending/2026-09-07-installer-must-self-ignore-its-deployed-tools-in-the-consumer-repo.md (resolved by 40-01 task 3)
    - .planning/todos/completed/ (any one completed todo, for the resolution-section shape each moved file must gain)
    - src/mcp/vice/docs-deferred-ledger.test.ts (the guard that fails in BOTH directions — a moved file with a surviving ledger row, and a removed row with a file still pending)
    - .planning/phases/40-the-three-preprocessing-host-tools/40-CONTEXT.md (D-28 — which three are folded, which one is not, and why)
  </read_first>
  <action>
    Move each of the three resolved todo files from the pending tree to the
    completed tree with `git mv`, and in the SAME commit remove that todo's row
    from the deferred-items ledger table. The ledger guard fails in both
    directions, so a move without a row removal and a row removal without a move
    are each a red suite; doing them in one commit is the only shape that passes.
    Do one todo per commit if that is clearer, but never split a single todo's two
    halves across commits.

    Add a resolution section to each moved file following the existing shape in
    the completed tree: what closed it, which plan, and what the reader should
    look at — for the never-throws todo, the two guarded sites and the committed
    regression case; for the path consolidation, the one owning function and the
    collapsed ignore stanza; for the installer self-ignore, that the consumer now
    gets exactly one ignorable directory so the enumeration it asked for is moot.

    Update the paragraph above the ledger that states how many pending todos it
    reads, recomputing the number from the pending tree rather than by
    subtraction, and say in the same sentence which three closed and in which
    phase.

    Do NOT fold the vicerc scratch-directory reaping todo. It belongs to a broker
    lifecycle change this phase never opens, and this phase deliberately invented
    no retention policy for per-run directories — the single root is what makes
    deleting them a one-line answer.

    Update the state document's current-position block to this phase and its
    status, and add the phase's decisions to the decision record: the supersession
    and the two deletions; the clean-break path consolidation with no
    back-compatibility; the six per-capability tool ids; the positive-shape oracle
    living in the seam with exit status recorded and never consulted; the
    deliberate divergence dropping version probing; the named-decline verdict
    shape; and the corrected skill total of nine. Keep each entry short and point
    at the decisions document rather than restating its reasoning.

    After every state-document write, diff the file. The tracking verbs used
    around this workflow are known to reflow prose, orphan multi-line blocks,
    truncate names at a parenthesis, and reset counters; a write that damaged
    unrelated content must be repaired in the same commit rather than left for a
    later reader to find.
  </action>
  <acceptance_criteria>
    - The three todo files are under the completed tree and absent from the pending tree, each with a resolution section.
    - No ledger row names any of the three, and every remaining ledger row has a matching file in the pending tree.
    - The stated pending count equals the number of files in the pending tree.
    - The vicerc scratch-directory reaping todo is still pending with its ledger row intact.
    - The current-position block names this phase, and the decision record carries the phase's decisions.
    - The state document's unrelated content is unchanged apart from the intended edits.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-deferred-ledger.test.ts 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, or a "# fail" line whose count is anything other than 0 — this guard fails in both directions, so either half of a todo fold left undone reds it</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && ls -1 .planning/todos/pending/*.md | wc -l</automated>
    <fails_when>prints anything other than 7 — ten were pending and exactly three are folded; a different count means a todo was folded that should not have been, or one of the three was missed</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && ls -1 .planning/todos/pending/ | grep -c 'reap-vicerc-scratch-dirs'</automated>
    <fails_when>prints anything other than 1 — the scratch-directory reaping todo must stay pending; it was reviewed and deliberately not folded</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated 2>&1 | tail -30</automated>
    <fails_when>a "# fail" line whose count exceeds 2, or any failing test outside `anno-register.test.ts` — 2 failing in that one file is this repository's known floor as of 2026-09-03. Stop any running VICE broker first: a live broker deterministically reddens the BACK-05 D-G ordering test.</fails_when>
  </verify>
  <done>Three todos are folded with each file move and its ledger row removal in one commit, the stated count matches the tree, the one deliberately-not-folded todo is intact, and the state document records the phase's decisions without collateral damage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a reversed locked decision → a later reader | An unexplained edit to a locked statement is indistinguishable from an error; a dated rider is what makes it a decision. |
| a bookkeeping half-edit → the two-directional ledger guard | Moving a file without moving its row, or the reverse, reds the suite. |
| an automated tracking write → unrelated document content | The tracking verbs used around this workflow are known to damage neighbouring prose. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-07-01 | Repudiation | a locked statement changed without a record | high | mitigate | Every correction is an in-place amendment with a dated rider pointing at the decisions document; a committed check asserts the removed id still occurs rather than having been deleted. |
| T-40-07-02 | Tampering | a coverage claim corrected by editing the wrong figure | medium | mitigate | The plan names the two rows carrying the same numerals for a different quantity and pins their count in a verify command, so editing one by mistake is caught. |
| T-40-07-03 | Repudiation | a half-finished todo fold | medium | mitigate | The ledger guard fails in both directions and is run as a verify command; the plan requires each todo's two halves in one commit. |
| T-40-07-04 | Tampering | collateral damage from an automated tracking write | medium | mitigate | The plan requires diffing the state document after every write and repairing damage in the same commit. |
| T-40-07-05 | Repudiation | this plan silently delivering nothing under worktree isolation | high | mitigate | The plan carries the worktree carve-out in its own frontmatter with the reason recorded, and the verify commands assert the strings are present in the committed files rather than that the task ran. |
| T-40-07-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this phase. |
</threat_model>

## Artifacts this phase produces

Created by **this plan**:

- `docs/phase40-preprocessing-tools-decisions.md` — new dated decisions document
- Dated riders on: the roadmap's phase goal sentence, its success criteria 1, 3
  and 4, its requirements line, its version-probe notes bullet, its three
  requirements-mapped claims; and the requirements document's removed-requirement
  line, its two affected excluded rows, its traceability row and its coverage
  block
- One new excluded-table row
- Three todo files relocated to the completed tree with resolution sections
- The phase's decision entries in the project state document

The full phase-wide new-symbol set is listed in `40-01-PLAN.md`'s own
"Artifacts this phase produces" section.

<verification>
- The decisions document exists, is dated, and names both reversed statements.
- Coverage reads 19 total, 19 mapped, 0 unmapped in the requirements document, and every requirements-mapped claim in the roadmap agrees.
- The removed id still occurs in both documents, struck rather than deleted; the two plan-count rows are untouched.
- `node --test docs-deferred-ledger.test.ts docs-dangling-refs.test.ts docs-linerefs.test.ts` reports `# fail 0`.
- Seven todos remain pending, including the one deliberately not folded.
- The full automated suite is at or below its known floor.
</verification>

<success_criteria>
- The supersession and the cartridge removal are recorded once, with dates and evidence.
- Every locked statement they reverse is amended in place, never deleted, and nothing is renumbered or re-sorted.
- Every dependent count is corrected and cross-checked mechanically rather than claimed.
- Three todos are folded correctly in both directions; the fourth candidate is deliberately left pending.
</success_criteria>

<output>
Create `.planning/phases/40-the-three-preprocessing-host-tools/40-07-SUMMARY.md` when done.
</output>
