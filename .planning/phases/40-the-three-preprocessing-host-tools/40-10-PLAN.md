---
phase: 40-the-three-preprocessing-host-tools
plan: 10
type: execute
wave: 3
depends_on: [40-09]
gap_closure: true
gap_ids: [G-40-1]
requirements: [PREP-05]
autonomous: true
files_modified:
  - src/mcp/vice/host-tool.mts
  - src/mcp/vice/resources/host-tool.mjs
  - src/mcp/vice/repo-root.ts
  - src/mcp/vice/repo-root.test.ts
  - CLAUDE.md
  - docs/phase34-host-tool-seam-decisions.md
  - .planning/phases/34-the-host-tool-execution-seam/34-03-PLAN.md
  - .planning/phases/34-the-host-tool-execution-seam/34-03-SUMMARY.md
  - .planning/phases/40-the-three-preprocessing-host-tools/40-01-SUMMARY.md
  - .planning/phases/40-the-three-preprocessing-host-tools/40-UAT.md
  - .planning/todos/completed/2026-09-07-consolidate-all-tool-written-files-under-c64-re-tools.md

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No document in this repository still records the Ghidra location split as a hard external-tool constraint, because it is not one -- MEASURED: the dot rule binds the absolutized path argument, not where the bytes live"
    - "repo-root.ts's toolsDir() doc comment states two things that are TRUE and checkable, replacing two that were independently false"
    - "The number of non-comment occurrences of the tool-written root literal is stated exactly, enumerated by file and reason, and mechanically gated -- so the next drift is a test failure instead of a false comment"
    - ".gitignore's dead tracked-tooling justification and A-07's ONLY-placement claim are both gone from the forward-looking record"
    - "Historical execution records are SUPERSEDED with dated notes, never falsified -- a future reader can still see what was believed, when, and on what evidence"
  artifacts:
    - "src/mcp/vice/repo-root.ts -- a corrected toolsDir() doc comment with an enumerated, gated occurrence census"
    - "src/mcp/vice/repo-root.test.ts -- the census gate, with a planted-violation control"
    - "src/mcp/vice/host-tool.mts + resources/host-tool.mjs -- the runs-root convention comment corrected, source and rebuilt artifact committed together"
    - "CLAUDE.md -- the D-33 bullet's exception clause replaced by the alias-handle description"
    - "docs/phase34-host-tool-seam-decisions.md -- A-07 amended in place with a dated superseding note"
    - "four historical planning records plus one completed-todo addendum, each carrying a dated superseding note pointing at the measured-semantics note"
  key_links:
    - "every corrected claim -> .planning/notes/ghidra-dot-path-check-semantics.md: one corrective source, cited by path from each site, so the five corrections cannot drift apart from each other"
    - "repo-root.ts's enumerated census -> repo-root.test.ts's gate: the mechanism that stops this specific comment going false a second time"
---

<objective>
Correct the record. Gap G-40-1's root cause was not a bad path -- it was a claim that
was never measured, written down as external fact, mirrored into five documents, and
then consulted twice by two different phases as though it were settled. Plans 40-08 and
40-09 fixed the code. This plan fixes the thing that made the code wrong.

Purpose: a wrong path costs one plan. A wrong recorded reason costs every future plan
that reads it -- this one cost two phases and a UAT round. The diagnosis enumerated
every overstated sentence; this plan removes each one, and puts a mechanical gate under
the single claim that is both load-bearing and easy to re-break.

Output: five forward-looking documents saying what is true, four historical records
carrying dated superseding notes rather than rewrites, and one new census gate.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# THE corrective source. Every sentence this plan writes must trace to it. Read it
# before editing anything, and cite it by path from each corrected site.
@.planning/notes/ghidra-dot-path-check-semantics.md

# The enumeration of every wrong claim, with its file and the specific inference that
# produced it. Read the `root_cause`, `artifacts` and `missing` blocks.
@.planning/phases/40-the-three-preprocessing-host-tools/40-UAT.md
@.planning/debug/ghidra-run-dir-outside-one-root.md

# What actually landed, so the corrections describe the real code rather than the plan.
@.planning/phases/40-the-three-preprocessing-host-tools/40-08-SUMMARY.md
@.planning/phases/40-the-three-preprocessing-host-tools/40-09-SUMMARY.md

# Already correct and usable as-is -- do NOT edit these two, cite them.
@.planning/seeds/broker-owned-tool-output-paths.md
</context>

<interface_context>
MEASURED at plan time (2026-09-08). Re-verify every line number and every count before
editing -- plans 40-08 and 40-09 have already moved some of this.

Sites to correct, with what is wrong at each:

- `src/mcp/vice/repo-root.ts:185-207` -- the `toolsDir()` doc comment. TWO
  independently false load-bearing claims:
  (a) `:191` lists "the Ghidra runs directory" among six writers that "now all resolve
      their subdirectory through THIS function". It never did -- and after plan 40-08 it
      resolves through `ghidraRunsRoot()` in the host-bound `ghidra-project.mts`, which
      joins the segments directly because a host-bound module cannot import this
      container-side resolver (the documented module-cycle rule).
  (b) `:193-195` claims the tool-written root literal "therefore has exactly one
      non-comment occurrence in this codebase: the line below". MEASURED FALSE at plan
      time: `install-resources.ts:101`, `vice-broker.mts:131`, `host-tool.mts:2934`,
      plus `repo-root.ts:206` itself. Plan 40-08 adds a fifth in `ghidra-project.mts`.
      Re-count with `grep -a` against the post-40-09 tree; do not copy this list.

- `src/mcp/vice/host-tool.mts:2930-2933` -- the `runs/oracle` scratch-dir comment cites
  "`ghidra-project.mts`'s runs root" as using the same convention. That was false when
  written (it used the vestigial top-level directory) and is TRUE after plan 40-08 --
  but the sentence must now say WHICH convention, because the Ghidra root reaches the
  same physical place through an alias handle and a reader needs to know that.
  `host-tool.mts` is host-bound: `build.ts:42-53` lists `host-tool.mjs` in
  `HOST_BOUND_ARTIFACTS`, the emitted file RETAINS comments, so a comment-only edit
  still requires `npm run build` and a committed `resources/host-tool.mjs`.

- `CLAUDE.md:97` -- the Configuration section's D-33 bullet. Its final sentence records
  the Ghidra location as a single documented exception that stays outside the one root.
  That whole clause goes; the acceptance gate greps for its opening words being absent.
  DANGER: `docs-linerefs.test.ts:151` asserts EXACTLY ONE line in `CLAUDE.md` that both
  mentions `rewriteArguments()` and cites `vice-proxy.ts:<N>`. That is the Architecture
  bullet, NOT this one. Do not touch it, and do not introduce a `vice-proxy.ts:<N>`
  citation into any line mentioning `rewriteArguments()`.

- `docs/phase34-host-tool-seam-decisions.md:147` -- assumption A-07, whose Rationale
  column calls the vestigial location "the only placement satisfying both Finding 2's
  every-segment dot rule and the requirement that a result path be translatable by
  `containerPath()`". A symlinked handle was never in that candidate set, so "only" is
  true of the set considered and false of the option space. This is a LIVE decision
  register that planners consult, so it gets an amendment, not a deletion.

- `.planning/phases/34-the-host-tool-execution-seam/34-03-PLAN.md:89-94` -- planner
  assumption A-07 stated inline.
- `.planning/phases/34-the-host-tool-execution-seam/34-03-SUMMARY.md:11,46,104,131,137,152-157`
  -- the execution record of what 34-03 built and why.
- `.planning/phases/40-the-three-preprocessing-host-tools/40-01-SUMMARY.md:12,60,61,99,107,111,137,159,167,175,176,178,219,252`
  -- the D-33 execution record, including the `human_judgment: true` D5 rationale and
  the not-met acceptance criterion. Re-grep; this list is from plan time.
- `.planning/phases/40-the-three-preprocessing-host-tools/40-UAT.md:52-57` -- its own
  `measured_this_session` block carries the superseded "does NOT canonicalise" framing.
  It DOES absolutize; the block's own later lines already say so, so the earlier
  sentence contradicts the later one inside one field.
- `.planning/todos/completed/2026-09-07-consolidate-all-tool-written-files-under-c64-re-tools.md:103-126,145-146`
  -- the "`runs/ghidra/` is NOT movable" addendum. Note `:124-125` already anticipated
  the fix ("a project-scoped ... symlinked INTO ...").

NOT in scope, verified at plan time:
- `.planning/phases/34-the-host-tool-execution-seam/34-VERIFICATION.md` -- the UAT's
  correction list names it, but its eight Ghidra mentions carry no claim about the
  location being unavoidable (`:40` says "the no-dot project-path refusal enforced in
  code", which is still TRUE). Re-verify with `grep -a`; if that holds, record that this
  file needed no change and why, rather than editing it to look busy.
- `.planning/seeds/broker-owned-tool-output-paths.md` and
  `.planning/REQUIREMENTS.md`'s `PREP-05` -- already state the corrected semantics. Cite
  them; do not edit them. `PREP-05`'s status bookkeeping belongs to plan 40-11.
- `.planning/debug/ghidra-run-dir-outside-one-root.md` -- the diagnosis itself. Its
  `prefilled_measured_evidence` block quotes the superseded framing on purpose, and its
  Evidence entries correct it in place. It is the record OF the correction; leave it.
</interface_context>

<correction_policy>
Two different kinds of document are in scope and they get OPPOSITE treatment. Getting
this backwards is itself a defect.

**FORWARD-LOOKING documents describe the current state and are read for guidance.**
Rewrite them outright to say what is true: `repo-root.ts`, `host-tool.mts`, `CLAUDE.md`,
`docs/phase34-host-tool-seam-decisions.md`.

**HISTORICAL execution records describe what was believed and done at a past date.**
Add a dated superseding note; do NOT rewrite what they recorded. This is the project's
own established convention, stated in `docs-dangling-refs.test.ts:19-25`: executed-phase
artifacts under `.planning/phases/**` "are historical records that legitimately quote the
wrong wording while describing its removal", and are deliberately excluded from that
guard's scan for exactly that reason. Falsifying them would destroy the evidence trail
that made this gap diagnosable at all -- `40-01-SUMMARY.md`'s `verification: []` plus
`human_judgment: true` on D5 is what proved the claim was never measured.

Every note, in either category, must:
- carry the date `2026-09-08` and name gap `G-40-1`;
- cite `.planning/notes/ghidra-dot-path-check-semantics.md` by path as the measured
  source;
- state the MECHANISM (`ProjectLocator` calls `getAbsolutePath()`, never
  `getCanonicalPath()`, so the check absolutizes but does not resolve symlinks), not
  merely the conclusion. A conclusion without its mechanism is what got re-derived
  wrongly twice already;
- name the superseded METHOD where one existed -- running this project's own
  `hasDotPrefixedSegment()` against a synthetic string, which observes the project and
  not Ghidra. A reader who knows only the wrong answer can reconstruct it; a reader who
  knows the wrong answer AND why the method could not have produced a right one cannot.
</correction_policy>

<tasks>

<task type="auto">
  <name>Task 1: The two source comments, and a gate under the census claim so it cannot go false again</name>
  <files>src/mcp/vice/repo-root.ts, src/mcp/vice/repo-root.test.ts, src/mcp/vice/host-tool.mts, src/mcp/vice/resources/host-tool.mjs</files>
  <read_first>
    src/mcp/vice/repo-root.ts (lines 180-215 -- the `toolsDir()` comment and the
    functions around it)
    src/mcp/vice/repo-root.test.ts (lines 150-260 -- the existing assertion style and
    how it reaches the launcher script; this is where the census gate goes)
    src/mcp/vice/host-tool.mts (lines 2915-2940 -- the scratch-dir comment only; do not
    read the whole 3058-line file)
  </read_first>
  <behavior>
    - Test 1: the census gate counts non-comment occurrences of the tool-written root
      literal across the module tree and asserts the count equals the number
      `repo-root.ts`'s own comment states, reading that number OUT of the comment rather
      than duplicating it in the test. A comment that says one number while the tree
      holds another is the exact defect being fixed, so the test must compare the two
      rather than pin a third.
    - Test 2: the gate enumerates WHICH files hold those occurrences and asserts that
      set equals the set the comment enumerates, so adding a sixth writer without
      updating the comment fails by name rather than by an off-by-one.
    - Test 3: planted-violation control. A synthetic source with an extra occurrence, and
      a synthetic comment naming a file that holds none, are each reported by the same
      predicate; the real tree and the real comment are reported by neither.
    - Test 4: the gate excludes comment lines and test files, and uses a NUL-tolerant
      read, so a source file with an embedded NUL byte cannot be silently skipped.
  </behavior>
  <action>
    Correct `repo-root.ts`'s `toolsDir()` doc comment. Both false claims go; state
    instead:
      - How many writers resolve their subdirectory through this function, and that the
        Ghidra runs root is NOT one of them: it is computed by `ghidraRunsRoot()` in the
        host-bound `ghidra-project.mts`, which joins the segments directly because a
        host-bound module cannot import this container-side resolver. Say that the two
        must stay equal, and that it is the same arrangement `install-resources.ts`,
        `vice-broker.mts` and `host-tool.mts`'s oracle scratch dir already use for the
        same reason -- so a reader sees a documented pattern, not four exceptions.
      - The EXACT number of non-comment occurrences of the literal, ENUMERATED by file
        with the one-clause reason each exists. Re-count against the post-40-09 tree
        with `grep -a`; do not copy plan-time numbers. Then say that a test gates this
        enumeration, and name the test, so the next reader knows the number is checked
        rather than asserted.
      - One sentence on the alias handle: the non-dotted sibling of this root, a symlink
        with a relative target, minted host-side, existing so a tool that refuses a
        dot-prefixed segment in the path it is HANDED can still write into this one root.
        Cite the measured-semantics note by path.

    Add the census gate to `repo-root.test.ts`. Parse the count and the file list out of
    the comment; measure the tree; compare. Implement it as a predicate returning its
    findings, then assert over the findings, so the planted-violation control exercises
    the same code path -- the return-don't-assert shape `docs-linerefs.test.ts:32-38`
    documents. Read files as bytes or with a NUL-tolerant reader: this repository
    contains `anno-memmap-render.ts` with an embedded NUL that a naive text scan skips,
    and a census that silently skips a file has already produced one wrong decision in
    this project.

    Correct `host-tool.mts`'s scratch-dir comment. Its claim about which convention the
    Ghidra runs root follows is now satisfiable -- say precisely how: the same two
    trailing segments beneath the same root, reached through the alias handle rather than
    directly, so the physical location matches while the path handed to Ghidra does not
    contain a dot-prefixed segment. Do not restate the whole mechanism here; cite the
    note and `ghidra-project.mts`.

    Then rebuild and COMMIT: `npm run build` in `src/mcp/vice`, staging
    `resources/host-tool.mjs` in the same commit. The emitted artifact retains comments,
    so this comment-only edit changes it and `resources-sync.test.ts` reds CI otherwise.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck 2>&1 | tail -2 && npm run build >/dev/null 2>&1 && test "$(grep -ac 'exactly one non-comment occurrence' repo-root.ts)" -eq 0 && node --test repo-root.test.ts resources-sync.test.ts 2>&1 | grep -aE '^. (tests|pass|fail) ' && node --test repo-root.test.ts resources-sync.test.ts 2>&1 | grep -aq '^. fail 0' && git status --porcelain -- resources/host-tool.mjs | grep -aq . && echo ARTIFACT_CHANGED_OK</automated>
  </verify>
  <done>
    `repo-root.ts`'s `toolsDir()` comment no longer claims a single non-comment
    occurrence and no longer claims the Ghidra runs root resolves through it. It states
    an exact enumerated census that `repo-root.test.ts` mechanically compares against the
    real tree, with a planted-violation control proving the predicate fires.
    `host-tool.mts`'s convention comment names the alias arrangement precisely.
    Typecheck clean, `repo-root.test.ts` and `resources-sync.test.ts` at `fail 0`, and
    `resources/host-tool.mjs` rebuilt and staged with its source.
  </done>
</task>

<task type="auto">
  <name>Task 2: The two forward-looking documents a future planner will actually read</name>
  <files>CLAUDE.md, docs/phase34-host-tool-seam-decisions.md</files>
  <read_first>
    CLAUDE.md (the Configuration section's D-33 bullet at :97, and the Architecture
    bullet carrying the `rewriteArguments()` citations -- read the second one only to be
    certain you do not touch it)
    docs/phase34-host-tool-seam-decisions.md (the A-07 row at :147 and the two rows
    around it, to match the table's amendment style)
    src/mcp/vice/docs-linerefs.test.ts (lines 110-200 -- understand the exactly-one
    qualifying-line assertion before editing CLAUDE.md)
  </read_first>
  <action>
    In `CLAUDE.md`'s D-33 Configuration bullet, replace the closing exception clause.
    The new text must say:
      - Every tool-written file now lands under the one root, INCLUDING Ghidra per-run
        project data -- there is no exception left.
      - Ghidra is handed a path through a non-dotted alias: a sibling symlink at the repo
        root whose target is the relative dotted name, minted host-side by the broker at
        startup and re-asserted as an idempotent precondition by the resolver, verified
        on every mint and refused by name rather than repaired.
      - WHY the alias is needed and why it suffices: the refusal binds the absolutized
        path argument (`ProjectLocator` calls `getAbsolutePath()`, never
        `getCanonicalPath()`), so it absolutizes but does not resolve symlinks. Cite
        `.planning/notes/ghidra-dot-path-check-semantics.md`.
      - The three conditions the location satisfies at once, since any future reader
        tempted by a simpler location needs all three: no dotted or bare-dot segment in
        the absolutized path; inside the bind-mounted workspace, because
        `containerPath()` throws on a host path outside it; and a RELATIVE link target,
        because the container itself traverses the link to read the run log.
      - One clause noting the fragility and pointing at the live guard plan 40-09 added,
        so the next reader knows the risk is tracked rather than unnoticed.

    Say explicitly that the previously recorded justification was an overstatement and
    name the superseded method. This bullet is read by every agent that opens this repo;
    it is the single highest-leverage place to stop the wrong conclusion being
    re-derived.

    Do NOT touch the Architecture bullet or any line mentioning `rewriteArguments()`.
    `docs-linerefs.test.ts` asserts exactly one line in this file both mentions that
    function and cites `vice-proxy.ts:<N>`; a second such line, or the loss of the
    existing one, reds the automated suite. Run that test after editing.

    Also re-check the four line-number citations that bullet carries for
    `rewriteArguments()` and `forwardToVice()`. `docs-linerefs.test.ts` mechanically
    verifies the two `rewriteArguments()` ones; the others are checked by hand each
    phase. If plans 40-08 or 40-09 shifted `vice-proxy.ts`, update them and record the
    delta and its cause in the summary, exactly as the bullet's own parenthetical does
    for the previous two shifts. If nothing moved, say so -- a silent no-change and an
    unchecked no-change are indistinguishable to the next reader.

    In `docs/phase34-host-tool-seam-decisions.md`, amend the A-07 row IN PLACE, matching
    the dated-amendment style rows A-15 and A-16 already use. State: the Assumption
    column's location is superseded as of 2026-09-08 by gap `G-40-1`; the Rationale's
    "only placement" was true of the candidate set considered -- existing real
    directories already in the tree -- and false of the option space, because a
    symlinked HANDLE was never a candidate; and the Reversibility column's cost estimate
    was accurate, which is worth leaving visible. Name where the location now is, and
    cite the measured-semantics note. Do not delete the original row text; a planner
    reading this register needs to see what was assumed alongside what replaced it.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && test "$(grep -ac 'One documented exception' CLAUDE.md)" -eq 0 && test "$(grep -ac 'getCanonicalPath' CLAUDE.md)" -ge 1 && test "$(grep -ac 'ghidra-dot-path-check-semantics' CLAUDE.md docs/phase34-host-tool-seam-decisions.md | grep -av ':0$' | wc -l)" -eq 2 && test "$(grep -ac 'only placement satisfying' docs/phase34-host-tool-seam-decisions.md)" -ge 1 && test "$(grep -ac '2026-09-08' docs/phase34-host-tool-seam-decisions.md)" -ge 1 && cd src/mcp/vice && node --test docs-linerefs.test.ts docs-dangling-refs.test.ts 2>&1 | grep -aE '^. (tests|pass|fail) ' && node --test docs-linerefs.test.ts docs-dangling-refs.test.ts 2>&1 | grep -aq '^. fail 0'</automated>
  </verify>
  <done>
    `CLAUDE.md`'s D-33 bullet records no remaining exception, names the mechanism
    including `getCanonicalPath()`, lists all three conditions, cites the
    measured-semantics note, and points at the live guard. The `rewriteArguments()` line
    is untouched and `docs-linerefs.test.ts` passes. A-07 is amended in place with a
    dated note that keeps the original claim visible beside its correction, and
    `docs-dangling-refs.test.ts` passes -- every path cited by the new text exists.
  </done>
</task>

<task type="auto">
  <name>Task 3: Four historical records and one addendum, superseded with dated notes rather than rewritten</name>
  <files>.planning/phases/34-the-host-tool-execution-seam/34-03-PLAN.md, .planning/phases/34-the-host-tool-execution-seam/34-03-SUMMARY.md, .planning/phases/40-the-three-preprocessing-host-tools/40-01-SUMMARY.md, .planning/phases/40-the-three-preprocessing-host-tools/40-UAT.md, .planning/todos/completed/2026-09-07-consolidate-all-tool-written-files-under-c64-re-tools.md</files>
  <read_first>
    src/mcp/vice/docs-dangling-refs.test.ts (lines 18-30 -- the project's own statement
    of why historical phase artifacts are treated differently; this task's whole policy
    rests on it)
    .planning/todos/completed/2026-09-07-consolidate-all-tool-written-files-under-c64-re-tools.md
    (lines 100-150 -- the existing addendum's style and its :124-125 anticipation of
    this exact fix)
  </read_first>
  <action>
    Re-grep each file with `grep -a` before editing; the plan-time line lists in
    `<interface_context>` are from before plans 40-08 and 40-09 ran.

    Apply the `<correction_policy>` HISTORICAL treatment to each. One superseding note
    per file, placed where a reader encounters the wrong claim -- not buried at the
    bottom of a long document. Where a file states the claim in several places, one note
    plus a short inline pointer at each other site is better than five copies of the same
    paragraph.

    `34-03-PLAN.md` -- the inline planner assumption A-07. Note that its candidate set
    excluded a symlinked handle, so its "only placement" conclusion followed validly from
    an incomplete option space. That distinction matters: the planner did not reason
    badly, it enumerated badly, and those two failures have different remedies.

    `34-03-SUMMARY.md` -- the execution record. Its `.gitignore` reasoning is now
    doubly superseded: the location moved, AND the tracked-tooling justification it
    recorded for keeping the directory un-ignored was already false when written. Say
    both, and say which one plan 40-08 removed.

    `40-01-SUMMARY.md` -- the D-33 record, and the highest-value note in this task. It
    must state: the claim promoted to "a hard external-tool constraint, not a preference"
    was produced by running this project's own `hasDotPrefixedSegment()` against a
    synthetic string, corroborated by this project's own pinned test literal -- neither of
    which observes Ghidra; that the summary's own D5 already carried `verification: []`
    and `human_judgment: true`, which is a contemporaneous admission the claim was never
    measured and is the artefact that made the gap diagnosable; and that the one
    acceptance criterion it recorded as NOT met is now genuinely met, closed by plan
    40-08 rather than waived. Do not soften the first point -- the mechanism by which a
    self-referential check became external-tool evidence is the transferable lesson here,
    and it is worth more than the path change.

    The completed consolidation todo -- add a SECOND dated addendum superseding the
    first. Note that the first addendum's `:124-125` already floated the alias handle as
    a possible future reunification and that this is that reunification, landed. Say what
    the sketch's `runs/ghidra/` line now means in practice: physically under the one root,
    reached through the alias.

    `40-UAT.md` -- a DIFFERENT and much smaller job than the other four, because its
    substance is already correct. MEASURED at plan time: its `measured_this_session`
    block was already refined during the debug session and already states the
    absolutize-but-not-canonicalise mechanism; the only remaining occurrence of the
    superseded wording in that file is inside the `missing` list, where it appears as an
    item still to be corrected. So two of that list's sub-items are now STALE and will
    send a future gap-closure round chasing work that is already done or was never
    needed:
      - the item naming this UAT's own canonicalisation wording -- already applied;
      - the item naming `34-VERIFICATION.md` -- verified above as needing no change.
    Mark both sub-items as already-satisfied in place, dated, with one clause each
    saying why. Do NOT touch `status`, `result`, `severity`, `truth`, or the gap's
    structure: `/gsd-verify-work` reconciles the gap's resolution from the existence of
    these plans' summaries, and a hand-edited status collides with that.

    Every note in the other four files gets the date, the gap id, the citation, the
    mechanism and the superseded method, per `<correction_policy>`. Open each note with
    the literal marker `SUPERSEDED 2026-09-08` so the acceptance gate can prove a note
    was actually added rather than matching text the file already contained -- MEASURED
    at plan time: that marker appears nowhere in the repository, and none of the five
    files contains the word at all, so the gate cannot pass vacuously.

    Do not delete any original sentence in any of these five files.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && for f in .planning/phases/34-the-host-tool-execution-seam/34-03-PLAN.md .planning/phases/34-the-host-tool-execution-seam/34-03-SUMMARY.md .planning/phases/40-the-three-preprocessing-host-tools/40-01-SUMMARY.md .planning/todos/completed/2026-09-07-consolidate-all-tool-written-files-under-c64-re-tools.md; do test "$(grep -ac 'SUPERSEDED 2026-09-08' "$f")" -ge 1 || { echo "MISSING MARKER: $f"; exit 1; }; test "$(grep -ac 'G-40-1' "$f")" -ge 1 || { echo "MISSING GAP ID: $f"; exit 1; }; test "$(grep -ac 'ghidra-dot-path-check-semantics' "$f")" -ge 1 || { echo "MISSING CITATION: $f"; exit 1; }; test "$(grep -ac 'getCanonicalPath\|getAbsolutePath' "$f")" -ge 1 || { echo "MISSING MECHANISM: $f"; exit 1; }; done && test "$(grep -ac 'hasDotPrefixedSegment' .planning/phases/40-the-three-preprocessing-host-tools/40-01-SUMMARY.md)" -ge 1 && test "$(grep -ac 'already-satisfied\|already applied\|already satisfied' .planning/phases/40-the-three-preprocessing-host-tools/40-UAT.md)" -ge 1 && test "$(grep -ac '^status: diagnosed$' .planning/phases/40-the-three-preprocessing-host-tools/40-UAT.md)" -eq 1 && test "$(grep -ac '^  status: failed$' .planning/phases/40-the-three-preprocessing-host-tools/40-UAT.md)" -eq 1 && test "$(grep -ac '^  severity: major$' .planning/phases/40-the-three-preprocessing-host-tools/40-UAT.md)" -eq 1 && echo NOTES_OK && cd src/mcp/vice && node --test docs-dangling-refs.test.ts docs-review-disposition.test.ts 2>&1 | grep -aE '^. (tests|pass|fail) ' && node --test docs-dangling-refs.test.ts docs-review-disposition.test.ts 2>&1 | grep -aq '^. fail 0'</automated>
  </verify>
  <done>
    The four historical records each carry a `SUPERSEDED 2026-09-08` note naming gap
    `G-40-1`, citing the measured-semantics note by path, and stating the
    `getAbsolutePath()` mechanism -- a marker measured as absent from the repository
    beforehand, so the gate cannot have passed vacuously. No original sentence was
    deleted from any of the five files. `40-01-SUMMARY.md`'s note names the superseded
    method explicitly and records its not-met criterion as closed rather than waived.
    `40-UAT.md`'s two stale correction sub-items are marked already-satisfied with a
    reason each, and its gap status field is byte-identical to the previous commit's.
    `docs-dangling-refs.test.ts` and `docs-review-disposition.test.ts` are at `fail 0`;
    the `docs-deferred-ledger.test.ts` failures remain at their pre-plan count, because
    the un-ledgered pending todo they flag is plan 40-11's to close.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository documentation -> every future agent session | `CLAUDE.md` and the decision registers are read as authoritative by every planner and executor that opens this repo |
| a source comment -> a future maintainer | A comment stating a checkable fact is trusted without re-checking, which is precisely how this gap survived two phases |
| historical record -> audit trail | The evidence that made this gap diagnosable lives in artifacts this plan edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-10-01 | Repudiation | rewriting historical execution records | high | mitigate | `<correction_policy>` forbids deleting any original sentence in the five historical files, and task 3's gate requires the superseding note to coexist with the original text. The audit trail (`verification: []` plus `human_judgment: true` on D5) is what made the gap diagnosable and must survive intact |
| T-40-10-02 | Tampering | `repo-root.ts`'s census claim going false again | high | mitigate | Task 1 adds a gate that reads the count and the file list OUT of the comment and compares both against the real tree, with a planted-violation control. The claim can no longer be false without a test failure -- unlike the two claims it replaces |
| T-40-10-03 | Information disclosure | a corrected claim without its mechanism | medium | mitigate | Every note must state the `getAbsolutePath()` mechanism and name the superseded method, gated by task 3's verify. A conclusion without its mechanism was re-derived wrongly twice; a reader who knows why the old method could not have worked cannot repeat it |
| T-40-10-04 | Denial of service | breaking `docs-linerefs.test.ts` via `CLAUDE.md` | medium | mitigate | Task 2 states the exactly-one-qualifying-line constraint, forbids touching the `rewriteArguments()` line, and runs that test in its own verify. The four line-number citations are re-checked and any delta recorded with its cause |
| T-40-10-05 | Tampering | a census that silently skips a file | medium | mitigate | Task 1 test 4 requires a NUL-tolerant read. This repository contains a source file with an embedded NUL that a naive text scan skips, and that has already produced one wrong decision here |
| T-40-10-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install task exists in this plan; no dependency is added and nothing external is fetched or built. The package-legitimacy gate is not reached |
</threat_model>

<verification>
1. `cd src/mcp/vice && npm run typecheck` -- exits 0.
2. `node --test repo-root.test.ts resources-sync.test.ts docs-linerefs.test.ts
   docs-dangling-refs.test.ts docs-review-disposition.test.ts` -- `fail 0`.
3. `npm run test:automated` -- must not exceed the baseline plan 40-09 recorded. The
   `docs-deferred-ledger.test.ts` failures caused by the un-ledgered pending todo are
   still expected here; plan 40-11 closes them.
   PRECONDITION: no VICE broker running (a live broker reddens the BACK-05 test
   deterministically -- a known state, not a flake). Re-run once before concluding:
   `audit-root-args.test.ts` carries a documented intermittent scratch-file race.
4. `git show --name-only HEAD` for task 1's commit includes both `host-tool.mts` and
   `resources/host-tool.mjs`.
5. Repo-wide sweep: `git grep -an 'hard external-tool constraint'` returns hits ONLY
   inside a superseding note, the diagnosis, or the seed -- never as a live claim. Record
   the actual output in the summary rather than asserting it.

Merge note: this plan's diff removes lines from `repo-root.ts`, `host-tool.mts` and
`CLAUDE.md`. Per this project's recorded constraint, `cleanup-wave` refuses any branch
whose diff contains a deletion, unconditionally. Verify the removal scope against the
actions above and merge the branch by hand; the refusal is expected and is not a failure
signal.
</verification>

<success_criteria>
- No forward-looking document records the Ghidra location split as an unavoidable
  external-tool constraint, and none records a remaining exception to D-33.
- `repo-root.ts`'s two false claims are replaced by an exact enumerated census that a
  test compares against the real tree, with a planted-violation control.
- `CLAUDE.md`'s D-33 bullet names the mechanism, all three conditions, the alias handle,
  the live guard, and the superseded method -- with `docs-linerefs.test.ts` still green.
- A-07 is amended in place with its original claim still visible.
- Five historical records carry dated superseding notes and lost no original sentence.
- `resources/host-tool.mjs` is committed alongside its source.
</success_criteria>

<output>
Create `.planning/phases/40-the-three-preprocessing-host-tools/40-10-SUMMARY.md` when done.

Record explicitly:
- The re-measured non-comment occurrence count and its enumerated file list, as the
  census gate now reads them.
- Whether `34-VERIFICATION.md` in fact needed no change, with the `grep -a` output that
  settled it.
- Whether any `vice-proxy.ts` line-number citation in `CLAUDE.md` shifted, the delta,
  and its cause -- or an explicit statement that none did and that this was checked.
- The final `git grep -an 'hard external-tool constraint'` output.
- The `npm run test:automated` numbers with the known failures itemised by cause.
</output>
