---
phase: 31-procedure-re-pointing
plan: 04
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - src/mcp/vice/skill-attribution.test.ts
  - .github/workflows/ci.yml
  - .planning/phases/31-procedure-re-pointing/31-REVIEW-FIX.md
autonomous: true
requirements: [REPOINT-03, REPOINT-04]
requirement_subparts:
  - "REPOINT-03 — the committed assertion that makes '10 instances across two trees, each carrying two naming lines' durable must actually score BOTH trees where it runs unattended (gap 1)"
  - "REPOINT-03 — the assertion's documented `grep -rx` byte-equality must be true at block boundaries, not only on interior lines (gap 2)"
  - "REPOINT-04 — no regression: the manifest, its `omit` disposition and `anno-derivation.test.ts` stay exactly as verified (truths 3, 4, 7); this plan does not touch them"

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "TRACE: truth 5 — A committed assertion scores criterion 1's sentence across BOTH trees, durably: at the moment the assertion runs in CI the shipped tree EXISTS, because a workflow step ordered before the `Test` step materialises it through the one existing producer (`installer/scripts/sync-skills.mjs`, the same command `installer`'s `prepack` runs — not a second sync path)"
    - "TRACE: quoted gaps[0].missing[0] — \"Either materialise `installer/skills/` before the `Test` step in CI, or make the shipped-tree skip LOUD — a named `t.skip()`/recorded reason rather than a bare `continue` — so a run that scored only one tree cannot be read as having scored two.\" BOTH branches are taken, not either: the CI step materialises the tree, AND every unscored root is recorded with a named reason and emitted as a `t.diagnostic()` line naming the root, with an unconditional summary diagnostic reporting how many of the declared roots were scored"
    - "TRACE: quoted gaps[0].missing[1] — \"Tighten `skippableEmptyRoot()` so a shipped root that EXISTS but holds no `SKILL.md` is a failure rather than a skip (code review WR-01): today `fileCount === 0` skips regardless of whether the directory is absent or merely empty.\" The skip is keyed on ABSENCE (and on not being a CI run), never on emptiness, and every branch is asserted directly from injected inputs rather than from whichever tree happens to exist on the machine running the suite"
    - "TRACE: truth 6 — The two naming lines are compared with `grep -rx` semantics — exact whole-line byte equality, only a trailing `\\r` tolerated — and the claim is now true AS WRITTEN at both block boundaries, because the predicate can only ever receive whole physical lines: its parameter is `readonly string[]`, and EVERY call site in the file is asserted to hand it either an element of an `attributionBlockLines()` result or an explicit array literal — never an `attributionBlocks()` capture, and never a `split(\"\\n\")` of one, which is the cheap typecheck-satisfying route that would reinstate the fragment-bearing input while still passing on today's tree"
    - "TRACE: quoted gaps[1].missing[0] — \"Feed the predicate whole lines — anchor the extractor to line boundaries, or drop the block's first and last fragment before comparing — so the `grep -rx` claim in the comment is true as written.\" The line-anchored route is taken (not the fragment-dropping one), so a naming line jammed onto a boundary line is reported as an OFFENDER rather than silently discarded"
    - "TRACE: quoted gaps[1].missing[1] — \"Add a boundary plant to the adjacent proof test (marker and naming line on one physical line) so the strictness claim covers the two positions the existing interior plants cannot reach.\" Both boundary positions are planted in memory — head: the `ATTRIBUTION (ABS-02)` marker sharing a physical line with the adapted line; tail: the source-repository line sharing a physical line with the closing `-->` — and each is proven to score 0 for the affected line, each paired with a one-newline control that scores 1 so neither plant can be vacuous"
    - "TRACE: truth 5 + truth 6, via `31-VERIFICATION.md` § \"Note on the review-disposition guard\" (\"clearing it belongs to whatever plan actually fixes them\") — `docs-review-disposition.test.ts` is GREEN at the end of this round from a DURABLE source: `.planning/phases/31-procedure-re-pointing/31-REVIEW-FIX.md` names every finding id in `31-REVIEW.md`, and its cascade `audit-integrity.test.ts` is green too. No disposition for this round lives only in a `VERIFICATION.md`"
    - "TRACE: truth 7 — no pin, needle or scope predicate in `scripts/check-no-analyser.mjs` moves: the subject-token count in `src/mcp/vice/skill-attribution.test.ts` is still exactly 12, the count in `.github/workflows/ci.yml` is still exactly 1, the gate still prints `attribution-guard-test 14` and `skill-attribution-headers 24`, and `node scripts/check-no-analyser.mjs` still exits 0"
    - "TRACE: probe REPOINT-03/adjacency — When the `ATTRIBUTION (ABS-02)` marker and a naming line occupy the SAME physical line they COLLIDE, they do not merge: the whole-line predicate reports that naming line as absent (count 0) and the block is listed as an offender. Symmetrically for the closing `-->` sharing a line with the source-repository line. Asserted by the two boundary plants"
    - "TRACE: probe REPOINT-03/empty — The empty-input classification is explicit and three-valued rather than one boolean over a file count: a zero-file SOURCE root is always a failure; a zero-file SHIPPED root that EXISTS on disk is a failure (an interrupted or partial sync is not an absent tree); a zero-file SHIPPED root that is ABSENT is skippable only outside CI, and only with a recorded reason"
    - "TRACE: probe REPOINT-03/encoding — The unit of equality is BYTES over a whole physical line: `===` against the two constants, with a single trailing `\\r` stripped so a CRLF checkout scores identically, and no trim, no case folding, no Unicode normalisation and no substring match. The compared value is a whole line from the file, never a regex capture group whose ends can be line fragments"
    - "TRACE: probe REPOINT-03/ordering — Every verdict is order-independent. The offender list is compared with `deepEqual` against the empty array and the per-root tallies are multiset counts, so the pass/fail result is unchanged under any order `walkSkills()` returns files in, and no assertion depends on that order"
    - { statement: "TRACE: probe REPOINT-03/concurrency — Nothing added is concurrent or interruptible in a way that can produce a false pass; the guard is a synchronous read-and-compare that spawns nothing and writes nothing, every plant is an in-memory string, and the one writer (the CI sync step) is a distinct workflow step that GitHub Actions runs to completion before the `Test` step. The step-ordering half is asserted on the workflow SOURCE, because the runner's sequencing cannot be executed locally", verification: backstop }
  artifacts:
    - path: "src/mcp/vice/skill-attribution.test.ts"
      provides: "The line-anchored block extractor, the three-valued empty-root verdict with its recorded reason, the loud unscored-root diagnostic, the two boundary plants, and the floor derived from the manifest instead of a literal"
      contains: "emptyRootVerdict"
    - path: ".github/workflows/ci.yml"
      provides: "A step that materialises the generated, gitignored shipped skills tree through the existing producer, ordered before the `Test` step"
      contains: "sync-skills.mjs"
    - path: ".planning/phases/31-procedure-re-pointing/31-REVIEW-FIX.md"
      provides: "The durable disposition record for all 11 findings in 31-REVIEW.md, including the reasoned decision on WR-03 and WR-06 and the named deferral triggers for the findings not fixed"
      contains: "CR-01"
  key_links:
    - "CI step `Generate the shipped skills tree (scored by skill-attribution.test.ts)` -> `installer/scripts/sync-skills.mjs` -> `installer/skills/` -> `SKILL_ATTRIBUTION_SHIPPED_ROOT`: this is the whole chain that makes the two-tree half of the guard reachable where the gate actually gates. Break any link and the guard silently halves"
    - "`emptyRootVerdict()` -> `existsSync(root)` and `process.env.CI`, passed in as EXPLICIT ARGUMENTS rather than read inside: that is what lets all four branches be asserted deterministically on any machine, instead of only the branch the local tree happens to exercise"
    - "`attributionBlockLines()` -> `namingLineCountsIn(lines)`: the predicate's parameter is an array of whole physical lines, so a capture-group fragment is not merely unlikely to reach it, it cannot be expressed. The link is only closed if EVERY ONE of the four call sites is re-pointed — the per-file scoring read, the unmutated control, both plants, and the negative case — because `attributionBlocks(...)[0].split(\"\\n\")` typechecks against the new signature and passes on today's tree (all 10 real blocks have empty boundary fragments), which would leave the claim false while every other criterion read green"
    - "`attributionBlockLines()` block count <-> `attributionBlocks()` block count: asserted equal per scanned file, so the file's two extractors cannot drift on where a block begins and ends. The regex one keeps its existing job (isolating a block's CONTENT to match a registry row); the line one owns byte-exactness"
    - "`ABS02_BLOCKS_PER_TREE_FLOOR` -> `manifest.procedures.length`: derived, so absorbing a sixth procedure raises the floor with it instead of leaving a stale literal that can never ratchet"
    - "`31-REVIEW-FIX.md` -> `docs-review-disposition.test.ts` disposition source 5 (`that phase's own *-REVIEW-FIX.md`) -> `audit-integrity.test.ts`'s docs-guard cascade: the guard's red is correct signal today, and this is the only durable route to clearing it"
  prohibitions:
    - "CARRIED FORWARD from 31-02: No new test file is created — every assertion goes inside `src/mcp/vice/skill-attribution.test.ts`"
    - "CARRIED FORWARD from 31-02: No file is named `docs-*.test.ts` — it would join `audit-gate.mjs`'s globbed guard set and red `audit-integrity.test.ts` until `EXPECTED_DOCS_GUARD_NAMES` and `DOCS_GUARD_FLOOR` moved in the same commit"
    - "CARRIED FORWARD from 31-02: No count is compared against the phase's measured total as a numeric literal — relations plus a floor only, and the floor itself is derived"
    - "CARRIED FORWARD from 31-02: No assertion pins the relative position, ordering or adjacency of the TWO NAMING LINES. (The boundary plants assert about the MARKER versus a naming line and about the closing `-->` versus a naming line — a different pair, and the one gap 2 requires. The two naming lines' own relative position stays unpinned, because `c64-program-recon/SKILL.md`'s second block separates them.)"
    - "CARRIED FORWARD from 31-02, with its one stated carve-out: No file under `src/skills/**` is edited, and no planted violation is ever written into either tree — every plant lives in memory as a string. `installer/skills/**` is produced ONLY by the existing `sync-skills.mjs` producer, never hand-edited, and the local absent-tree reproduction must restore it byte-identically"
    - "CARRIED FORWARD from 31-02: `hostpath.ts` and `containerpath.ts` are not imported; repo root comes from the file's existing `repoRoot({ from: HERE })`"
    - "CARRIED FORWARD from 31-02: Nothing under `src/skills/` is `import()`ed, `require()`d, `eval()`ed or spawned — skill content is untrusted first-party prose, read as a string and matched"
    - "CARRIED FORWARD from 31-02: The frozen `ABSORBED_FILES` registry is not replaced with a corpus scan for files containing an attribution block — that check is circular"
    - "CARRIED FORWARD from 31-02: No second corpus WALKER is derived; the file's already-imported `walkSkills` is used. (The line-anchored block extractor is not a walker and not a fresh block regex — see the note in task 2 for why the existing capture-based extractor structurally cannot express the whole-line claim.)"
    - "CARRIED FORWARD from 31-02: `routine-queue-walker/SKILL.md`'s `description:` is not rewritten — it is final, and rewriting it re-triggers ABS-03 and breaks CLAUDE.md's byte-identity assertion"
    - "CARRIED FORWARD from 31-02: No attempt is made to drive `grep -rail 'the external analyser' installer/skills` toward zero — unsatisfiable by design"
    - "CARRIED FORWARD from 31-02: The bare `npm test` full glob is never used as a verification command"
    - "MINTED, demanded by gaps[0].missing[0] (\"make the shipped-tree skip LOUD … so a run that scored only one tree cannot be read as having scored two\"): No skip is silent. A declared root that goes unscored must be recorded with a named, non-empty reason AND emitted as a test diagnostic. A bare `continue` with no record is forbidden"
    - "MINTED, demanded by gaps[1].missing[0] (\"Feed the predicate whole lines\"): The naming-line predicate is never handed a regex capture group, nor any string whose first or last `split(\"\\n\")` element can be a line fragment"
    - "MINTED, demanded by the plan-checker's `key_links_planned` WARNING on revision iteration 1: No `attributionBlocks(` result — and nothing derived from one, in particular no `attributionBlocks(...)[n].split(\"\\n\")` — is ever passed to `namingLineCountsIn(`. Every argument at every call site is an element of an `attributionBlockLines()` result or an explicit array literal. This is the STRONGER of the two mechanisms guarding truth 6 in intent, but the load-bearing one is task 2's grep-shaped source acceptance criterion, because that one is mechanically checkable; this prohibition records WHY the criterion exists so a later editor cannot satisfy the type and reopen the hole"
    - "MINTED, demanded by the scope of gap 1 plus `31-VERIFICATION.md` Deviations #1: No new literal spelling of the subject token is added to `src/mcp/vice/skill-attribution.test.ts` or to `.github/workflows/ci.yml`. Every plant and every comment is built from the derived constants or from subject-free text, because the removal gate's per-path pins for those two files (12 and 1) are pins no plan in this phase may move"
    - "MINTED, demanded by the verification report's scope note (\"Scope is two files\"): `upstream-procedure-manifest.json`, `scripts/check-no-analyser.mjs`, `.planning/STATE.md`, `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` are NOT edited by this plan. `REPOINT-03`/`REPOINT-04` stay `Pending` until a re-verification verdict clears them"
---

<objective>
Close the two durability gaps `31-VERIFICATION.md` found in the guard plan 31-02 committed. Both
ROADMAP success criteria are already VERIFIED by independent direct measurement; nothing here
re-measures them.

Gap 1: the guard reads two skill trees, but in CI the second tree does not exist when the guard
runs, so it silently scores one tree and reports a clean pass with zero skips. Root cause: the
generated, gitignored `installer/skills/` is materialised only at the `Validate npm package
contents` step, which runs *after* the `Test` step. Symptom-only fixes (renaming the skip,
special-casing CI) are forbidden — the tree must exist where the assertion runs, and any
unscored root must announce itself.

Gap 2: the guard documents `grep -rx` whole-line byte equality but feeds the predicate a regex
capture group whose first and last `split("\n")` elements are line FRAGMENTS, so the claim is
false at both block boundaries. Latent today (all 10 real blocks have empty boundary fragments)
but it is a false-ACCEPT hole plus a code comment the tree falsifies — this project's own named
antipattern. Root cause: the predicate's input, not its comparison.

Purpose: until these land, criterion 1's two-tree half rests on one verification report's
measurement rather than on a standing guard, which is exactly the state the ROADMAP note says
`REPOINT-03` must not be recorded complete in.

Output: a workflow step that materialises the shipped tree before the suite runs; a three-valued
empty-root verdict with a recorded reason and a loud diagnostic; a line-anchored block extractor
that makes the whole-line claim true by construction; two boundary plants with controls; and a
durable `31-REVIEW-FIX.md` disposition record that leaves `docs-review-disposition.test.ts` and
its `audit-integrity.test.ts` cascade green.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/31-procedure-re-pointing/31-VERIFICATION.md
@.planning/phases/31-procedure-re-pointing/31-REVIEW.md
@.planning/phases/31-procedure-re-pointing/31-PATTERNS.md
@.planning/phases/31-procedure-re-pointing/31-VALIDATION.md
@.planning/phases/31-procedure-re-pointing/31-02-PLAN.md
</context>

Two literals below must appear verbatim in an `<action>` body because the instruction is to
DELETE exactly that text, and naming it by concept would leave the executor guessing which aside
and which lookup. Both are also negative-grepped in the acceptance criteria, so they are
allowlisted for the comment-text-discipline gate:

<!-- planner-discipline-allow: the header enumerates five -->
<!-- planner-discipline-allow: nameAt -->

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — an unscored shipped tree is impossible to mistake for a scored one</name>

  <files>.github/workflows/ci.yml, src/mcp/vice/skill-attribution.test.ts</files>

  <precondition>
    `installer/skills/` exists on the local machine (7 skill directories, produced by
    `installer/scripts/sync-skills.mjs`), and no VICE broker is running
    (`systemctl --user is-active vice-broker` reads `inactive`) — a live broker reddens the
    BACK-05 test deterministically and would make every suite result untrustworthy.
  </precondition>

  <reversibility rating="reversible">
    Both halves are a single-commit revert: one workflow step and one predicate's shape. Nothing
    is deleted, no data is migrated, no published artifact changes.
  </reversibility>

  <read_first>
    - `.github/workflows/ci.yml` — the file being modified. Read the whole `Test` step including
      its long `env:` comment (it explains why CI uses the wide `npm test` glob) and read down to
      the `Validate npm package contents` and `Validate the removed integration stays removed`
      steps, so the new step's placement relative to all three is seen rather than assumed
    - `src/mcp/vice/skill-attribution.test.ts` — the file being modified. Read the header block
      (lines 1-75: the five-ways non-vacuity doctrine, the `WHAT NOT TO DO` list), the imports
      (lines 76-84 — note `existsSync` is ALREADY imported, so no new import is needed), the
      doctrine comment beside the new constants (lines 240-290), the constants and the two
      predicates (lines 290-380: `ABS02_UPSTREAM_NAME`, `ABS02_ADAPTED_LINE`,
      `ABS02_SOURCE_REPOSITORY_LINE`, `SKILL_ATTRIBUTION_ROOTS`,
      `SKILL_ATTRIBUTION_SOURCE_ROOT`, `SKILL_ATTRIBUTION_SHIPPED_ROOT`,
      `ABS02_BLOCKS_PER_TREE_FLOOR`, `namingLineCountsIn`, `skippableEmptyRoot`), and the two
      new tests (lines 764-1000: the scoring test with its three branch assertions at the top,
      its per-root loop, and the plant test)
    - `installer/scripts/sync-skills.mjs` and `installer/package.json` — the producer and the
      `prepack` hook that invokes it. Confirm for yourself that `prepack` runs exactly
      `node scripts/sync-skills.mjs` with `installer` as cwd, so the new CI step reuses that one
      producer rather than introducing a second sync path
    - `src/mcp/vice/ci-suite-coverage.test.ts` — the guard that PARSES `.github/workflows/ci.yml`,
      so it is what will red on a malformed insertion. Read `STEP_SPLIT_RE` (line 175 — steps are
      six-space-indented `- ` list items), `extractBuildJobBlock()` (line 126, sliced on
      two-space-indented top-level job keys), `FROZEN_REGISTRY` (line 161) and `runsExactly()`
      (line 217): the `installer` entry is proven only by a step that pairs
      `working-directory: installer` with a `run:` line whose command is EXACTLY `npm test`, so the
      new step must not perturb that pairing. Measured before this task: 10 tests, 0 fail. Read
      only; this file is not edited
    - `scripts/check-no-analyser.mjs` — read `EXEMPTION_CLASSES` (around lines 365-405).
      Two per-path pins constrain this task: `.github/workflows/ci.yml` at **1** and
      `src/mcp/vice/skill-attribution.test.ts` at **12**. Read only; this file is not edited
    - `src/mcp/vice/anno-verb-coverage.test.ts` lines 215-232 and 385-410 — the other both-trees
      root pair in this directory and its fresh-clone skip. This is the control flow 31-02
      copied; note what it does and does NOT record when it skips
    - `.planning/phases/31-procedure-re-pointing/31-VERIFICATION.md` — gap 1's `reason`,
      `artifacts` and both `missing` lines, plus § "Scope note for any gap-closure round"
    - `.planning/phases/31-procedure-re-pointing/31-REVIEW.md` — `CR-01` and `WR-01`
  </read_first>

  <action>
    Two halves of one path, and both are required — the verification report's `missing[0]` offers
    "either/or" and this task takes both, because the CI step alone still leaves a half-failed
    sync reading as absence and the loud skip alone still leaves CI scoring one tree.

    HALF A — the workflow. Insert one new step into `.github/workflows/ci.yml` immediately before
    the existing `- name: Test` step (currently at line 110), named `Generate the shipped skills
    tree (scored by skill-attribution.test.ts)`, with `working-directory: installer` and
    `run: node scripts/sync-skills.mjs`. That command is exactly what `installer`'s `prepack`
    hook runs, so this reuses the single existing producer rather than adding a second sync path.
    Indent it exactly as its siblings do — a SIX-SPACE-indented `- name:` list item, with its
    `working-directory:` and `run:` keys at eight spaces — because `ci-suite-coverage.test.ts`
    slices the `build` job textually and splits it into steps on that exact marker
    (`STEP_SPLIT_RE`, line 175) in order to pair each step's `working-directory:` with its own
    `run:`. A mis-indented step merges into its neighbour's chunk, and the neighbour here is the
    `Test` step whose `working-directory: src/mcp/vice` / `run: npm test` pairing is one of that
    guard's two frozen registry proofs. Do not give the new step a `run:` line of `npm test`, and
    do not reorder it past `Test the installer`, whose own `working-directory: installer` +
    exact-`npm test` pairing is the other frozen proof.
    Above it write a YAML comment recording: that `installer/skills/` is generated and gitignored
    (`git ls-files installer/skills` returns 0); that the only producer is
    `installer/scripts/sync-skills.mjs`, also invoked by `prepack`; that the step must stay
    ORDERED BEFORE `Test` because `check-npm-packages.mjs` further down regenerates the tree via
    `packFiles()` and is too late to help; and the specific past defect being closed — the
    two-tree ABS-02 naming-line guard scored one tree while reporting a clean pass with zero
    skips. The comment and the step name must contain ZERO occurrences of the subject token: the
    removal gate pins this workflow file at exactly 1 occurrence and that one is the
    `check-no-…mjs` invocation at the bottom of the job.

    HALF B — the verdict. Replace `skippableEmptyRoot(root, fileCount)` with a three-valued,
    fully-injected verdict function `emptyRootVerdict(root, fileCount, probe)` returning a new
    `EmptyRootVerdict` interface of two readonly fields, `skippable: boolean` and
    `reason: string`. `probe` is an explicit object of two booleans, `rootExists` and `ci` —
    passed in by the call site, never read from `existsSync`/`process.env` inside the function,
    because that injection is what makes every branch assertable on any machine. Branch order and
    reasons, each reason a specific non-empty sentence:
      1. `fileCount > 0` — not skippable; the root was scanned.
      2. `root !== SKILL_ATTRIBUTION_SHIPPED_ROOT` — not skippable; the source tree is never
         optional and a source traversal that shrank to zero is a failure.
      3. `probe.ci` — not skippable; CI materialises the shipped tree in an explicit step, so an
         absent tree there means that step did not run, and letting the skip fire in CI is what
         made the two-tree half vacuous exactly where it gates.
      4. `probe.rootExists` — not skippable; the shipped root EXISTS but yields no `SKILL.md`,
         which is an interrupted or partial sync, not an un-run one. This is the WR-01 tightening
         the verification report's `missing[1]` names, and it is why the skip is keyed on absence
         rather than on emptiness.
      5. otherwise — skippable; a fresh clone has never run the installer's `prepack`, so the
         generated tree legitimately does not exist yet, and skipping is safe only because the
         shipped tree is a pure copy of the source tree that was just scanned in full.
    Its doc comment must record that it replaces `skippableEmptyRoot()` — the name
    `31-VERIFICATION.md` gap 1 cites — and why the shape changed from a boolean to a verdict
    carrying its reason.

    Rework the scoring test to take the test context: change its callback to accept `t`. Inside
    the per-root loop, call `emptyRootVerdict(root, files.length, { rootExists: existsSync(root),
    ci: Boolean(process.env.CI) })`. When the verdict is skippable, push a
    `relative(ROOT, root)`-prefixed entry carrying the verdict's reason onto a new
    `unscoredRoots` array, emit `t.diagnostic()` naming the unscored root and its reason with the
    prefix `ABS-02 naming lines:`, and only then `continue`. When it is not skippable, use the
    verdict's reason as the failure message on the existing `files.length > 0` assertion.

    After the loop, emit one UNCONDITIONAL `t.diagnostic()` summary line, same prefix, reporting
    how many of the declared roots were scored, out of how many were declared, and naming the
    scored roots — so a one-tree run says so in its own output instead of looking identical to a
    two-tree run. Then assert as a RELATION (no literals) that `totals.size +
    unscoredRoots.length` equals `SKILL_ATTRIBUTION_ROOTS.length`, so a root that is neither
    scored nor recorded as skipped is a failure rather than a silence.

    Replace the three existing branch assertions at the top of the scoring test with FIVE, one
    per branch above, each built from the injected `probe` object so none of them depends on
    whether the shipped tree happens to exist on the machine running the suite: absent shipped
    root outside CI is skippable; absent shipped root under CI is NOT; EXISTING but empty shipped
    root is NOT; zero-file source root is NOT; non-empty shipped root is NOT. Add one more
    assertion that every one of those five verdicts carries a non-empty `reason`, so a reason
    cannot rot to the empty string and take the diagnostic's meaning with it.

    Finally, correct the file header's own two false claims, which are the reader-facing half of
    this gap: change `NON-VACUITY, five ways` to `six ways` and add item 6 describing the
    per-block two-naming-line scoring across BOTH trees, stating that this is the one guard in
    the file that reads the shipped tree as well as the source tree and that the workflow step
    named above is what makes the shipped half reachable in CI; then delete the now-redundant
    "the header enumerates five; this is the sixth" aside in the body. Add nothing to the header
    that spells the subject token.
  </action>

  <acceptance_criteria>
    - Source: `.github/workflows/ci.yml` contains a step named `Generate the shipped skills tree (scored by skill-attribution.test.ts)` with `working-directory: installer` and `run: node scripts/sync-skills.mjs`.
    - Source ordering: the new step's line number is LOWER than the line of `- name: Test`, and lower than the line of `- name: Validate npm package contents`. Prove it with `grep -n 'name: Generate the shipped skills tree\|name: Test$\|name: Validate npm package contents' .github/workflows/ci.yml` and read the three line numbers in order.
    - Guard that PARSES the edited workflow: `cd src/mcp/vice && node --test ci-suite-coverage.test.ts` exits 0 with `# fail 0` (10 tests, the count measured before this task). This is the only check that reads `.github/workflows/ci.yml` as structure: it must still slice the `build` job to a non-empty block strictly shorter than the file, still find every walked suite directory registered AND proven by a step, and both planted-removal tests (`Test the installer`, `Test the skills`) must still bite. A mis-indented inserted step, or one that perturbs the `working-directory: installer` + exact-`npm test` pairing, shows up here rather than two commits later.
    - Pin held: `grep -c 'the external analyser' .github/workflows/ci.yml` returns `1`.
    - Pin held: `grep -o 'the external analyser' src/mcp/vice/skill-attribution.test.ts | wc -l` returns `12`.
    - Source: `src/mcp/vice/skill-attribution.test.ts` contains `function emptyRootVerdict(` and `interface EmptyRootVerdict`, and `grep -c 'skippableEmptyRoot' src/mcp/vice/skill-attribution.test.ts` returns a count greater than 0 only inside comment prose (the old function is gone; its name survives only where the doc comment records the replacement). Check with `grep -n 'skippableEmptyRoot' src/mcp/vice/skill-attribution.test.ts` and confirm every hit is on a comment line.
    - Source: the header reads `NON-VACUITY, six ways` and enumerates an item 6; `grep -c 'the header enumerates five' src/mcp/vice/skill-attribution.test.ts` returns `0`.
    - Behaviour, local, tree present: `cd src/mcp/vice && node --test skill-attribution.test.ts` exits 0, and its output carries a diagnostic line beginning `ABS-02 naming lines:` that reports 2 of 2 roots scored.
    - Behaviour, CI branch, tree present: `cd src/mcp/vice && CI=1 node --test skill-attribution.test.ts` exits 0 and reports the same 2 of 2.
    - Behaviour, CI branch, tree ABSENT — the reproduction that proves the gap is closed: move `installer/skills` aside (`mv installer/skills /tmp/claude-31-04-shipped-skills`), then `cd src/mcp/vice && CI=1 node --test --test-name-pattern 'both skill trees carry the ABS-02 naming lines' skill-attribution.test.ts` must exit NON-ZERO with `# fail 1`, and the failure message must name the shipped root. Then, with `CI` unset, the same command must exit 0 AND emit an `ABS-02 naming lines:` diagnostic naming the shipped root as unscored with its reason (the loud skip — not a silent pass). Restore with `mv /tmp/claude-31-04-shipped-skills installer/skills` and prove the restore is byte-identical: `node installer/scripts/sync-skills.mjs` then `diff -r src/skills installer/skills` shows no differences in the three absorbed playbooks (`c64-memory-mapping/SKILL.md`, `c64-program-recon/SKILL.md`, `routine-queue-walker/SKILL.md`).
    - Behaviour, present-but-empty shipped root (WR-01): with `installer/skills` moved aside, create an empty `installer/skills/` directory and run the named test with `CI` unset — it must exit NON-ZERO, not skip. Remove the empty directory and restore as above.
    - Gate: `node scripts/check-no-analyser.mjs` exits 0 and its output still reports `attribution-guard-test 14` and `skill-attribution-headers 24`.
    - Typecheck: `cd src/mcp/vice && npm run typecheck` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>cd src/mcp/vice && npm run typecheck && node --test skill-attribution.test.ts && CI=1 node --test skill-attribution.test.ts && node --test ci-suite-coverage.test.ts && cd .. && cd ../.. && node scripts/check-no-analyser.mjs && grep -c 'the external analyser' .github/workflows/ci.yml</automated>
  </verify>

  <done>
    A CI step ordered before `Test` materialises the shipped tree through the one existing
    producer; the empty-root verdict refuses to skip in CI and refuses to skip a root that
    exists; every unscored root is recorded with a named reason and announced by a diagnostic; an
    unconditional summary diagnostic reports the scored-roots count so a one-tree run cannot read
    as two; the five branch assertions and the reason-non-emptiness assertion hold without
    depending on the local tree; the file header no longer under-counts its own guards;
    `ci-suite-coverage.test.ts` — the one guard that parses the edited workflow — is still
    `# fail 0`; and both removal-gate per-path pins (12 and 1) are unmoved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make the `grep -rx` claim true by construction — whole physical lines, plus the two boundary plants</name>

  <files>src/mcp/vice/skill-attribution.test.ts</files>

  <precondition>
    Task 1 is committed and `cd src/mcp/vice && node --test skill-attribution.test.ts` exits 0 —
    this task changes the same two tests task 1 reworked, so it must start from that shape.
  </precondition>

  <reversibility rating="reversible">
    A signature change and one added extractor inside a single test file; revert is one commit.
    No production code, no published artifact, no data.
  </reversibility>

  <read_first>
    - `src/mcp/vice/skill-attribution.test.ts` — the file being modified, at the shape task 1 left
      it. Read `attributionBlocks()` (line ~192) and every one of its six call sites
      (`grep -n 'attributionBlocks(' src/mcp/vice/skill-attribution.test.ts`), so you can see
      which call sites must keep the capture-based extractor and which must move. Then run
      `grep -n 'namingLineCountsIn(' src/mcp/vice/skill-attribution.test.ts` and read EVERY hit
      before editing anything: the retyping below forces all of them to change, and two of them
      (the unmutated control at lines 933-936 and the negative case at line 997) are the ones this
      task's own claim fails on if they are converted the cheap way rather than the way the action
      names
    - `.planning/phases/31-procedure-re-pointing/31-VERIFICATION.md` — gap 2's `reason` (the two
      reproduced boundary plants and their `grep -cx` counter-measurements), its `artifacts` entry
      naming the two comment sites, and both `missing` lines. Also read the Anti-Patterns table
      rows for `ABS02_BLOCKS_PER_TREE_FLOOR` (WR-03) and `charAt(nameAt).toUpperCase()` (WR-06)
    - `.planning/phases/31-procedure-re-pointing/31-REVIEW.md` — `CR-02`, `WR-03`, `WR-06`
    - `.planning/phases/31-procedure-re-pointing/31-PATTERNS.md` § "Don't hand-roll" — it says to
      use `attributionBlocks()` instead of a fresh block regex. Read it and read the note in this
      task's action for why a line-anchored extractor is not the thing that rule forbids
    - `scripts/check-no-analyser.mjs` — `EXEMPTION_CLASSES`' `attribution-guard-test` pin
      of 12 for this path. Read only; not edited
  </read_first>

  <action>
    Fix the predicate's INPUT, not its comparison — the comparison is already exact. Four changes,
    all inside `src/mcp/vice/skill-attribution.test.ts`.

    First, add `attributionBlockLines(text: string): string[][]` — a line-anchored extractor
    returning, per ABS-02 block, the array of the file's own WHOLE PHYSICAL LINES from the line
    carrying the `ATTRIBUTION (ABS-02)` marker through the line carrying that block's closing
    `-->`, inclusive. Split the text on newline once and scan linewise: when no block is open and
    a line contains the marker, open at that line and, if the same line also contains `-->` at or
    after the marker's end, close the block on that same line (a single-line block is a real
    shape; leaving it open would swallow every following line up to the next `-->` anywhere in the
    file); when a block is open and a line contains `-->`, close inclusive of that line. Its doc
    comment must state why this exists BESIDE `attributionBlocks()` rather than replacing it:
    `attributionBlocks()` returns the regex capture `m[1]`, which begins mid-line after the marker
    and ends mid-line before `-->`, so its first and last `split("\n")` elements are line
    FRAGMENTS — which is exactly why the documented `grep -rx` equivalence was false at both block
    boundaries. The capture-based extractor keeps its own job (isolating a block's CONTENT to
    match a registry row by the upstream file it names); the line-based one owns byte-exactness.
    This is not the "fresh block regex" the pattern map forbids: it anchors on the same
    `ATTRIBUTION (ABS-02)` marker and the same `-->` terminator, and the two are asserted to agree
    on block count (below).

    Second, change `namingLineCountsIn` to take `readonly string[]` — an array of whole lines —
    instead of a string, so a capture-group fragment cannot be passed to it even by accident.
    Keep the body's comparison unchanged: strip a single trailing `\r` per line, then `===`
    against the two constants; no trim, no case folding, no normalisation, no substring. Update
    the `grep -rx` doc comment so it is now true as written, and record in it that the parameter
    type is what makes it true rather than a promise in prose. Update the corresponding claim in
    the doctrine block above `ABS02_ADAPTED_LINE` the same way (the verification report names both
    comment sites).

    Third, at the scoring test's per-file call site, replace the `attributionBlocks(...)` read
    with `attributionBlockLines(...)` and pass each block's line array straight to the predicate.
    Add a per-file assertion that `attributionBlockLines(text).length` equals
    `attributionBlocks(text).length`, with a message naming the file and both counts, so the two
    extractors cannot drift on where a block begins and ends. In the same pass, apply the WR-03
    fix: retype `ABS02_BLOCKS_PER_TREE_FLOOR` as a `number` DERIVED from
    `manifest.procedures.length` rather than the literal `5`, and update its doc comment to record
    that a derived floor ratchets with the manifest while a literal cannot — the existing
    positive-floor assertion already guards a bad derivation.

    Fourth, extend the plant test with the two boundary plants gap 2's `missing[1]` requires,
    keeping both in memory and building every string from the existing derived constants and the
    subject-free marker literal — no new spelling of the subject token, because the removal gate
    pins this path at exactly 12 occurrences.
      HEAD PLANT: a synthetic block whose opening line carries the `ATTRIBUTION (ABS-02)` marker
      with `ABS02_ADAPTED_LINE` jammed onto the same physical line, followed by
      `ABS02_SOURCE_REPOSITORY_LINE` on its own line and a closing `-->` line. Run it through
      `attributionBlockLines()` and assert the predicate scores adapted 0, repository 1.
      HEAD CONTROL: the same text with a single newline inserted between the marker and
      `ABS02_ADAPTED_LINE`, asserted to score adapted 1, repository 1 — so the plant differs from
      a correct block by exactly one newline and cannot be vacuous.
      TAIL PLANT: a synthetic block whose closing `-->` shares its physical line with
      `ABS02_SOURCE_REPOSITORY_LINE`, asserted to score adapted 1, repository 0.
      TAIL CONTROL: the same text with a newline before the `-->`, asserted to score adapted 1,
      repository 1.
    Also assert each synthetic text yields exactly one block from `attributionBlockLines()`, so a
    plant that failed to parse cannot pass by scoring nothing.

    Then apply the WR-06 fix to PLANT A, which is in the same test: build the one-character
    mutation from the constant's own fixed prefix — lower-case the first character of
    `ABS02_ADAPTED_LINE` — instead of upper-casing the first character of the derived upstream
    name. The existing length and exactly-one-differing-position assertions stay and still hold;
    the `nameAt` lookup and its guard assertion go. Record in the comment that the mutation no
    longer depends on how the derived upstream name happens to be spelled, so a legitimate
    manifest change to a name beginning with a digit or a hyphen cannot red this test on a correct
    tree.

    Finally — and this is the change that decides whether the whole-line claim is true by
    CONSTRUCTION or merely by luck — re-point EVERY remaining `namingLineCountsIn(` call site, not
    only the ones the four changes above happen to pass through. Retyping the parameter to
    `readonly string[]` makes typecheck force each site to change, and the cheapest change at two
    of them is `attributionBlocks(...)[0].split("\n")`, which typechecks, passes on today's tree
    (all 10 real blocks have empty boundary fragments — `31-VERIFICATION.md:56`) and silently
    reinstates exactly the fragment-bearing input this task exists to remove. That route is
    forbidden; use the forms named here:
      THE UNMUTATED CONTROL, currently lines 933-936 — `const clean =
      attributionBlocks(readDestination(ABSORBED_FILES[0]))[0]` feeding
      `namingLineCountsIn(clean)`. `clean` must come from
      `attributionBlockLines(readDestination(ABSORBED_FILES[0]))[0]`, so it is a `string[]` of
      whole lines directly. NOT `attributionBlocks(...)[0].split("\n")`. Its two existing
      assertions (the block-exists `assert.ok` and the score-one-of-each `deepEqual`) stay; the
      first now reads as a non-empty array rather than a non-empty string.
      THE NEGATIVE CASE, currently line 997 — `namingLineCountsIn("nothing to see here")`, which
      proves the predicate does not fire on innocent text. It becomes an explicit ARRAY LITERAL of
      whole lines (`["nothing to see here"]`), never a `.split("\n")` of a string, and it keeps
      scoring `{ adapted: 0, repository: 0 }`.
      PLANTS A AND B, currently lines 970 and 988 — both operate on the block STRING via
      `String.replace`. Rework them to operate on `clean`'s line ARRAY (map the matching line to
      its mutated form) and keep an assertion that the anchor line was actually found, so a plant
      whose anchor drifted cannot pass by mutating nothing.
    When this is done, nothing anywhere in the file passes an `attributionBlocks(` result — or
    anything derived from one — to `namingLineCountsIn(`; the acceptance criterion below is the
    grep that proves it.
  </action>

  <acceptance_criteria>
    - Source: `src/mcp/vice/skill-attribution.test.ts` contains `function attributionBlockLines(` and its signature returns `string[][]`.
    - Source: `namingLineCountsIn` takes an array — `grep -n 'function namingLineCountsIn' src/mcp/vice/skill-attribution.test.ts` shows a `readonly string[]` parameter.
    - Source: `grep -n 'ABS02_BLOCKS_PER_TREE_FLOOR' src/mcp/vice/skill-attribution.test.ts` shows the declaration deriving from `manifest.procedures.length`, and `grep -c 'ABS02_BLOCKS_PER_TREE_FLOOR = 5' src/mcp/vice/skill-attribution.test.ts` returns `0`.
    - Source: `grep -c 'nameAt' src/mcp/vice/skill-attribution.test.ts` returns `0`.
    - Source, the call-site closure that makes truth 6 true by construction — this is the criterion the plan-checker's `key_links_planned` WARNING demanded, so do not treat it as a formality: run `grep -n 'namingLineCountsIn(' src/mcp/vice/skill-attribution.test.ts` and read EVERY line of the output. It must show the declaration plus exactly one call site each for the per-file scoring read, the unmutated control (was lines 933-936), plant A (was 970), plant B (was 988) and the negative case (was 997) — and every one of those arguments must be either an element of an `attributionBlockLines()` result (or a `string[]` local bound directly from one) or an explicit bracketed array literal. No argument may be an `attributionBlocks(` result, nor a per-newline decomposition of one, nor a bare string.
    - Source, the cross-check on the other extractor: `grep -n 'attributionBlocks(' src/mcp/vice/skill-attribution.test.ts` output contains only the declaration, doc-comment mentions, `attributionBlockFor()`'s filter, the registry-row read, and the new per-file block-count equality assertion added by this task. Read the two greps side by side and confirm their hit sets are DISJOINT by line number: a line number appearing in both means a capture is still feeding the predicate, which is the hole this task exists to close.
    - Behaviour, the boundary plants bite: `cd src/mcp/vice && node --test --test-name-pattern 'planted' skill-attribution.test.ts` exits 0 with `# fail 0`, and the file now contains four boundary assertions (head plant, head control, tail plant, tail control). Prove non-vacuity by temporarily inverting ONE expected boundary count in a scratch copy under the scratchpad directory (never in the tracked file), running it, and confirming it fails; discard the scratch copy.
    - Behaviour, the whole-line route is live and the real corpus still passes: `cd src/mcp/vice && node --test skill-attribution.test.ts` exits 0 with `# fail 0`, and its `ABS-02 naming lines:` summary diagnostic still reports 2 of 2 roots scored.
    - Behaviour, extractor agreement holds on the real corpus: the per-file block-count equality assertion added in this task passes for all three absorbed playbooks in both trees (implied by the green run above; the assertion's failure message names the file and both counts).
    - Pin held: `grep -o 'the external analyser' src/mcp/vice/skill-attribution.test.ts | wc -l` returns `12`.
    - Gate: `node scripts/check-no-analyser.mjs` exits 0 and still reports `attribution-guard-test 14` and `skill-attribution-headers 24`.
    - Typecheck: `cd src/mcp/vice && npm run typecheck` exits 0.
    - Regression: `cd src/mcp/vice && node --test skill-attribution.test.ts anno-derivation.test.ts skill-description-overlap.test.ts removal-gate.test.ts` exits 0 with `# fail 0`.
  </acceptance_criteria>

  <verify>
    <automated>cd src/mcp/vice && npm run typecheck && node --test skill-attribution.test.ts anno-derivation.test.ts skill-description-overlap.test.ts removal-gate.test.ts && cd ../../.. && node scripts/check-no-analyser.mjs && test "$(grep -o 'the external analyser' src/mcp/vice/skill-attribution.test.ts | wc -l)" = "12"</automated>
  </verify>

  <done>
    The naming-line predicate can only receive whole physical lines — at EVERY call site, each of
    which now hands it an `attributionBlockLines()` element or an array literal, so no
    fragment-bearing route survives anywhere in the file — and its documented `grep -rx`
    equivalence is therefore true at both block boundaries by construction rather than by
    promise; both
    boundary positions the interior plants could not reach are planted with controls and proven to
    bite; the two in-file extractors are asserted to agree on block count; the per-tree floor is
    derived from the manifest rather than written as a literal; PLANT A no longer depends on the
    derived upstream name's casing; and the subject-token pin of 12 is unmoved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record the durable disposition for all 11 review findings, and leave both disposition guards green</name>

  <files>.planning/phases/31-procedure-re-pointing/31-REVIEW-FIX.md</files>

  <precondition>
    Tasks 1 and 2 are committed, so the fixed/not-fixed column of the disposition record states
    what the tree actually shows rather than what was intended.
  </precondition>

  <reversibility rating="reversible">
    A planning record. Every deferral it states carries a named trigger, so a later phase can
    reverse any of them by fixing the finding and appending to the record.
  </reversibility>

  <read_first>
    - `.planning/phases/31-procedure-re-pointing/31-REVIEW.md` — every finding, in full: `CR-01`,
      `CR-02`, `WR-01` through `WR-06`, `IN-01` through `IN-03`. The record must name all eleven
      ids; a finding whose id appears nowhere is the exact silence the guard exists to catch
    - `src/mcp/vice/docs-review-disposition.test.ts` — read the header's five disposition sources
      and the `*-REVIEW-FIX.md` filename handling (around lines 150-160 and 265-285), so the file
      you write is a source the guard actually recognises
    - `.planning/phases/30-acme-export-and-the-real-acme-oracle/30-REVIEW-FIX.md` — the
      established shape in this repository: YAML frontmatter (`phase`, `fixed_at`, `review_path`,
      `iteration`, `findings_in_scope`, `fixed`, `skipped`, `status`), then a `# Phase N: Code
      Review Fix Report` heading, a Verification section naming the exact commands and their
      measured outputs, then a per-finding record. Follow it
    - `.planning/phases/31-procedure-re-pointing/31-VERIFICATION.md` § "Note on the
      review-disposition guard" (why a disposition written into a `VERIFICATION.md` is
      non-durable) and Deviations #1, #2, #3, #4, #5, #6
  </read_first>

  <action>
    Write `.planning/phases/31-procedure-re-pointing/31-REVIEW-FIX.md` following
    `30-REVIEW-FIX.md`'s shape: frontmatter with `phase: 31-procedure-re-pointing`, `review_path`
    pointing at `31-REVIEW.md`, `iteration: 1`, `findings_in_scope: 11`, and `fixed`/`skipped`
    counts that add to 11, with a `status` that honestly reflects a partial fix rather than
    `all_fixed`.

    Record every one of the eleven ids with a verdict and a reason. The verdicts, decided on the
    merits and not to be softened:
      `CR-01` — FIXED by task 1. State both halves (the CI step and the loud, absence-keyed
        verdict) and cite the reproduction: with the shipped tree absent and `CI=1` the named test
        fails; with `CI` unset it emits a named diagnostic instead of a silent pass.
      `CR-02` — FIXED by task 2. State the root cause (a mid-line-anchored capture fed to a
        whole-line predicate) and the fix (the predicate's parameter type is now an array of whole
        lines), and cite the four boundary assertions.
      `WR-01` — FIXED by task 1, as gap 1's `missing[1]`. The skip is keyed on absence, and an
        existing-but-empty shipped root now fails. Note explicitly what is NOT fixed: `walkSkills`
        still swallows `EACCES` inside `scripts/lib/skill-corpus.mjs`, which this round did not
        touch; that residual is closed for the shipped root specifically by the absence check, and
        the underlying swallow is left to whoever owns that shared module.
      `WR-02` — DEFERRED, with a named trigger. The two extractors were made to agree on block
        COUNT by an assertion inside `skill-attribution.test.ts`, but they were not consolidated
        into a shared module, because that requires editing
        `scripts/check-no-analyser.mjs`'s block extractor and this round is fenced to two
        files by the verification report's own scope note. Trigger: Phase 32's `CUT-04`
        retrospective audit of the re-pointed guard set, which is where the gate's own
        non-vacuity is re-measured.
      `WR-03` — FIXED by task 2. The floor derives from `manifest.procedures.length`. Say why it
        was fixed rather than accepted: it is the same defect class this project has already been
        bitten by twice (a pinned count that grows per milestone going red on a correct tree), and
        it sat in the same file as the comment disclaiming it.
      `WR-04` — ACCEPTED as a residual, with a named trigger. Anchoring the removal gate's prose
        on the stable requirement id instead of a ROADMAP criterion ordinal requires editing
        `scripts/check-no-analyser.mjs`, which this round may not touch. Trigger: the next
        `D-NN` renumbering that moves the ordinal, or Phase 32's audit of that gate.
      `WR-05` — FIXED by task 1. The header now reads six ways, enumerates the two-tree departure
        and names the workflow step the shipped half depends on, and the body's contradicting
        aside is gone.
      `WR-06` — FIXED by task 2. The plant mutates the constant's own fixed prefix, so a
        legitimate manifest change to an upstream name beginning with a digit or a hyphen can no
        longer red this test on a correct tree. Say why fixed rather than accepted: a guard that
        goes red on a correct tree is the failure mode this file's own doctrine block was written
        against.
      `IN-01` — ACCEPTED, no change. Cosmetic coercion consistency on the derivation line; the
        loud `endsWith` assertion already reddens on a non-string or separator-bearing value, and
        editing the derivation line for tidiness risks the one constant the whole guard is built
        from.
      `IN-02` — ACCEPTED, no change; same class as `WR-02` and deferred with it to the same
        trigger.
      `IN-03` — ACCEPTED, no change. The reviewer's own verdict is "no change required now"; the
        note belongs to `WR-11`'s resolution, and the record should say that the four call sites
        exist so `WR-11` covers them together.

    Include a Verification section naming the exact commands run and their measured outputs, at
    minimum: `cd src/mcp/vice && npm run typecheck`; `cd src/mcp/vice && node --test
    skill-attribution.test.ts` (and the `CI=1` variant); the absent-tree and present-but-empty
    reproductions from task 1; `cd src/mcp/vice && npm run test:automated` with the VICE broker
    confirmed inactive; `node scripts/check-no-analyser.mjs`; and the two subject-token
    counts (12 for the test file, 1 for the workflow). Record the `test:automated` failure LIST,
    not just the exit code — that command carries its own baseline and can exit 1 on a clean
    tree, so the honest statement is which files failed, and the clean floor for this project is
    0 failures.

    State in the record that this round did NOT touch `upstream-procedure-manifest.json`,
    `scripts/check-no-analyser.mjs`, `.planning/STATE.md`, `.planning/ROADMAP.md` or
    `.planning/REQUIREMENTS.md`, and that `REPOINT-03`/`REPOINT-04` remain `Pending` pending a
    re-verification verdict — `REPOINT-04` was scored SATISFIED on the merits by
    `31-VERIFICATION.md` and moves with `REPOINT-03` under the standing four-sites-one-edit rule,
    not ahead of it.

    Do NOT file anything under `.planning/todos/pending/`. A pending todo needs a matching
    `STATE.md` Deferred Items row in the same commit, `STATE.md` is out of this plan's scope, and
    a mismatch reddens `docs-deferred-ledger.test.ts` and the `audit-integrity` cascade. Every
    deferral above is recorded in this file with its trigger instead.
  </action>

  <acceptance_criteria>
    - File exists: `.planning/phases/31-procedure-re-pointing/31-REVIEW-FIX.md`, with YAML frontmatter carrying `phase`, `fixed_at`, `review_path`, `iteration`, `findings_in_scope`, `fixed`, `skipped`, `status`.
    - Coverage: every one of the eleven ids appears in the file. Verify with `for id in CR-01 CR-02 WR-01 WR-02 WR-03 WR-04 WR-05 WR-06 IN-01 IN-02 IN-03; do printf '%s ' "$id"; grep -c "$id" .planning/phases/31-procedure-re-pointing/31-REVIEW-FIX.md; done` — every count is at least 1.
    - Guard green: `cd src/mcp/vice && node --test docs-review-disposition.test.ts` exits 0 with `# fail 0` (it reads `# fail 1` before this file exists).
    - Cascade green: `cd src/mcp/vice && node --test audit-integrity.test.ts` exits 0 with `# fail 0` (it reads `# fail 1` before this file exists, at the subtest `no milestone audit declares a gated status while any docs guard is red (D-12-02)`).
    - Full suite: `cd src/mcp/vice && npm run test:automated` reports `# fail 0`, with `systemctl --user is-active vice-broker` reading `inactive` beforehand. Record the number of tests and the failure list in the file; do not trust the exit code alone.
    - REPOINT-04 non-regression: `git status --porcelain .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json scripts/check-no-analyser.mjs .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md` is EMPTY, and `cd src/mcp/vice && node --test anno-derivation.test.ts` exits 0 with `# fail 0`.
    - No pending todo was filed: `git status --porcelain .planning/todos/pending/` is empty.
    - Gate ladder: `node scripts/check-no-analyser.mjs`, `node scripts/check-npm-packages.mjs`, `node scripts/check-skill-description-overlap.mjs`, `node scripts/check-skill-tool-coverage.mjs` and `node scripts/check-skill-fork-honesty.mjs` all exit 0.
  </acceptance_criteria>

  <verify>
    <automated>cd src/mcp/vice && node --test docs-review-disposition.test.ts audit-integrity.test.ts anno-derivation.test.ts && cd ../../.. && node scripts/check-no-analyser.mjs && node scripts/check-npm-packages.mjs && test -z "$(git status --porcelain .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md scripts/check-no-analyser.mjs)"</automated>
  </verify>

  <done>
    All eleven `31-REVIEW.md` findings carry a recorded disposition in a durable source the
    disposition guard recognises; `WR-03` and `WR-06` are decided on their merits with the
    decision written down; every deferral names its trigger; `docs-review-disposition.test.ts` and
    its `audit-integrity.test.ts` cascade are both green; `test:automated` reports 0 failures with
    the broker inactive; and nothing outside this plan's three files changed.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
Every symbol, name and path this round CREATES. Newly-created symbols are not drift and must not
be flagged for acknowledgement against the pre-existing tree.

**`src/mcp/vice/skill-attribution.test.ts` — new symbols**

| Symbol | Kind | Created by |
| --- | --- | --- |
| `EmptyRootVerdict` | interface, two readonly fields `skippable: boolean` / `reason: string` | task 1 |
| `emptyRootVerdict(root, fileCount, probe)` | function — replaces `skippableEmptyRoot(root, fileCount)`, which is REMOVED | task 1 |
| `unscoredRoots` | local array in the scoring test, the recorded-reason ledger behind the loud skip | task 1 |
| `ABS-02 naming lines:` | diagnostic message prefix emitted via `t.diagnostic()` | task 1 |
| `attributionBlockLines(text)` | function returning `string[][]` — whole physical lines per block; coexists with the pre-existing `attributionBlocks()` | task 2 |
| `namingLineCountsIn(lines: readonly string[])` | CHANGED signature — was `(text: string)` | task 2 |
| `ABS02_BLOCKS_PER_TREE_FLOOR: number` | CHANGED — derived from `manifest.procedures.length`, was the literal `5` | task 2 |
| four boundary assertions (head plant, head control, tail plant, tail control) | assertions inside the pre-existing plant test | task 2 |
| `NON-VACUITY, six ways` + header item 6 | header prose — was `five ways` | task 1 |

**`.github/workflows/ci.yml` — new step**

| Name | Kind | Created by |
| --- | --- | --- |
| `Generate the shipped skills tree (scored by skill-attribution.test.ts)` | workflow step, `working-directory: installer`, `run: node scripts/sync-skills.mjs`, ordered before `Test` | task 1 |

**New files**

| Path | Kind | Created by |
| --- | --- | --- |
| `.planning/phases/31-procedure-re-pointing/31-REVIEW-FIX.md` | disposition record, source 5 for `docs-review-disposition.test.ts` | task 3 |

**Removed**

| Symbol | Note |
| --- | --- |
| `skippableEmptyRoot()` | replaced by `emptyRootVerdict()`; the name survives only in the replacement's doc comment so `31-VERIFICATION.md` gap 1's citation still resolves |
| `nameAt` | the PLANT A index lookup and its guard assertion, removed by the `WR-06` fix |
</artifacts_this_phase_produces>

<flagged_assumptions>
Probe items that were NOT authored into `must_haves` — surfaced here rather than dropped.

**`REPOINT-04` / `unclassified` — status: `unresolved`.** The edge probe produced no category for
this requirement (`{"requirement_id":"REPOINT-04","category":"unclassified","status":"unresolved"}`),
and an `unclassified` row is never auto-resolved with a backstop marker.

Assumption being flagged, for a human to confirm or reject: **`REPOINT-04` needs no new predicate
from this round.** `31-VERIFICATION.md` scores it ✓ SATISFIED on the merits — truths 3, 4 and 7
are VERIFIED by direct measurement (one commit `7adcbaf`, one file, 9 insertions / 8 deletions,
`omit` intact with `requirement_id: STORE-04`, all four retired tokens at zero, every new citation
resolving, `anno-derivation.test.ts` green) — and both gaps are scoped to `REPOINT-03`'s guard.
This plan therefore covers `REPOINT-04` only as a NON-REGRESSION assertion (task 3: the manifest
path shows no working-tree change and `anno-derivation.test.ts` stays green). If that reading is
wrong, the correct response is a new predicate for `REPOINT-04`, not a wider edit to the two files
this round is fenced to.

Probe-item accounting, so no drop is silent: **6 items surfaced by the probe = 5 authored into
`must_haves.truths`** (`REPOINT-03` / adjacency, empty, encoding, ordering as plain explicit
strings; `REPOINT-03` / concurrency as a `{ statement, verification: backstop }` marker) **+ 1
surfaced here as a flagged assumption** (`REPOINT-04` / unclassified). No item was dismissed.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill prose -> test process | `src/skills/**` and `installer/skills/**` content is untrusted first-party prose. It crosses into the test process as STRINGS via `readFileSync`; it is never imported, required, evaluated or spawned |
| repository -> GitHub Actions runner | the new workflow step executes a repository-controlled script (`installer/scripts/sync-skills.mjs`) with write access to the runner's workspace, including an `rm -rf` of `installer/skills/` |
| planning record -> CI verdict | `.planning/phases/**` documents feed `docs-review-disposition.test.ts` and `audit-integrity.test.ts`, so a planning-file edit can change a CI verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Repudiation | `skill-attribution.test.ts` two-tree guard in CI | high | mitigate | This IS gap 1: a green run that scored one tree is an unfalsifiable record. Task 1 makes the record explicit — `emptyRootVerdict()` refuses to skip under CI, every unscored root carries a named reason, an unconditional `t.diagnostic()` reports scored-of-declared, and a `totals.size + unscoredRoots.length === roots.length` relation makes an unrecorded root a failure |
| T-31-02 | Tampering | the two ABS-02 naming lines in shipped skill prose | high | mitigate | This IS gap 2: a naming line jammed onto a block-boundary line passed as byte-identical. Task 2 makes the predicate's parameter an array of whole physical lines, so the tamper is reported as an offender; both boundary positions are planted with controls |
| T-31-03 | Tampering | `installer/scripts/sync-skills.mjs` invoked by the new CI step | low | mitigate | No new producer and no new writer: the step runs the exact command `installer`'s `prepack` already runs, the destination is derived inside that script from its own module path, and the plan forbids hand-editing `installer/skills/**`. The step adds no secret and no network access to the job |
| T-31-04 | Elevation of privilege | skill prose reaching the test process | low | mitigate | Carried-forward prohibition: nothing under `src/skills/` is `import()`ed, `require()`d, `eval()`ed or spawned; every plant is an in-memory string, never written into either tree |
| T-31-05 | Tampering | `.planning/**` as a CI verdict input | medium | accept | Accepted with a stated compensating control: the disposition guard is deliberately weak ("mentioned in a recognised source") because a guard that grades prose gets switched off. Task 3's record is a durable source, and its own acceptance criteria re-run both guards plus the whole gate ladder so a record that merely silences a red is caught by the surrounding assertions |
| T-31-SC | Tampering | npm/pip/cargo installs | — | accept | Not engaged: this plan adds no dependency and runs no package-manager install. `package.json` and `package-lock.json` are untouched in all three tasks, so no package-legitimacy checkpoint applies. If an executor finds itself needing an install, that is a scope breach — stop and report rather than installing |

ASVS level 1; blocking threshold `high`. Both `high` threats are the plan's two deliverables, so
the blocking threshold is satisfied by the tasks themselves rather than by a separate mitigation.
</threat_model>

<verification>
Run in this order, from the repository root unless stated. Stop the VICE broker first
(`systemctl --user is-active vice-broker` must read `inactive`) — a live broker reddens the
BACK-05 test deterministically and would make every result below untrustworthy.

1. `cd src/mcp/vice && npm run typecheck` — exit 0.
2. `cd src/mcp/vice && node --test skill-attribution.test.ts` — `# fail 0`, and the output carries
   an `ABS-02 naming lines:` diagnostic reporting 2 of 2 roots scored.
3. `cd src/mcp/vice && CI=1 node --test skill-attribution.test.ts` — `# fail 0`, same 2 of 2. Then
   `cd src/mcp/vice && node --test ci-suite-coverage.test.ts` — `# fail 0` (10 tests): the only
   guard that reads the edited `.github/workflows/ci.yml` as STRUCTURE, so a mis-indented or
   proof-perturbing step insertion is caught here and not two commits later.
4. The absent-tree reproduction (task 1's acceptance criteria): shipped tree relocated, `CI=1`
   fails with the shipped root named; `CI` unset emits the loud skip diagnostic and exits 0; tree
   restored and `diff -r` clean across the three absorbed playbooks.
5. The present-but-empty reproduction: an empty `installer/skills/` fails even with `CI` unset.
6. `cd src/mcp/vice && node --test docs-review-disposition.test.ts audit-integrity.test.ts` —
   `# fail 0` on both (each reads `# fail 1` before `31-REVIEW-FIX.md` exists).
7. `cd src/mcp/vice && npm run test:automated` — `# fail 0`. Read the failure LIST, never the exit
   code alone: this command carries its own baseline and skips `MANUAL_ONLY_TESTS`, and the clean
   floor for this project is 0 failures.
8. Gate ladder, all exit 0: `node scripts/check-no-analyser.mjs` (also still printing
   `attribution-guard-test 14` and `skill-attribution-headers 24`),
   `node scripts/check-npm-packages.mjs`, `node scripts/check-skill-description-overlap.mjs`,
   `node scripts/check-skill-tool-coverage.mjs`, `node scripts/check-skill-fork-honesty.mjs`.
9. Pins: `grep -o 'the external analyser' src/mcp/vice/skill-attribution.test.ts | wc -l` = `12`;
   `grep -c 'the external analyser' .github/workflows/ci.yml` = `1`.
10. Scope: `git status --porcelain` names only the three files in `files_modified` plus this
    plan's SUMMARY.

Never use the bare `npm test` full glob locally — it blocks forever on `vice-proxy.test.ts`.
</verification>

<success_criteria>
- `installer/skills/` exists at the moment `skill-attribution.test.ts` runs in CI, because a step
  ordered before `Test` produced it through the one existing producer — and that step is proven
  well-formed by `ci-suite-coverage.test.ts`, the one guard that parses `ci.yml` as structure,
  which is still `# fail 0` with both of its planted-removal tests biting.
- No root can go unscored silently: absence outside CI is the only skippable case, it is recorded
  with a named reason and announced by a diagnostic, an existing-but-empty shipped root fails, a
  CI run never skips, and a root that is neither scored nor recorded fails a relation assertion.
- The naming-line predicate cannot be handed a line fragment at ANY of its call sites — each
  argument is an `attributionBlockLines()` element or an array literal, and no `attributionBlocks(`
  result reaches it — so its `grep -rx` claim is true as written; both block-boundary positions are
  planted with controls and proven to bite.
- The per-tree floor derives from `manifest.procedures.length`; PLANT A does not depend on the
  derived upstream name's casing.
- All eleven `31-REVIEW.md` findings carry a disposition in `31-REVIEW-FIX.md`;
  `docs-review-disposition.test.ts` and `audit-integrity.test.ts` are green.
- `test:automated` reports 0 failures with the broker inactive; the whole gate ladder exits 0.
- Both removal-gate per-path pins are unmoved (12 and 1) and no pin, needle or scope predicate in
  `scripts/check-no-analyser.mjs` was edited.
- Nothing outside `src/mcp/vice/skill-attribution.test.ts`, `.github/workflows/ci.yml` and
  `.planning/phases/31-procedure-re-pointing/31-REVIEW-FIX.md` changed.
</success_criteria>

<notes>
**The stopping rule, set in advance and recorded here so the next reader does not start round 2 by
reflex.** If a re-verification of this round again finds new defects ONLY in plan-derived truths —
not in either of the two ROADMAP success criteria, both of which `31-VERIFICATION.md` already
VERIFIED by independent direct measurement — the phase SEALS on the contract with its residuals
stated, rather than running another gap-closure round. This project has a recorded failure mode
where the verifier lifts must-haves out of the gap-closure plans' own frontmatter, so each round
adds the scope it is next judged against and the "same" truth keeps failing for a new cause. The
residuals this round knowingly leaves are named in `31-REVIEW-FIX.md` with triggers: `WR-02` and
`IN-02` (extractor and notices-list consolidation, deferred to Phase 32's `CUT-04` audit of the
re-pointed guard set), `WR-04` (the removal gate's ROADMAP-ordinal citation), `IN-01`, `IN-03`, and
`walkSkills`' `EACCES` swallow inside `scripts/lib/skill-corpus.mjs`.

**Deliberately out of scope, restated so an executor does not drift into it.** The ABS-02 chain,
the 10 instances and the cross-tree byte-identity are VERIFIED and are not re-measured.
`routine-queue-walker/SKILL.md`'s `description:` is final. `upstream-procedure-manifest.json`,
`scripts/check-no-analyser.mjs`, `.planning/STATE.md`'s stale phase-level counters (left to
`phase.complete`), `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` are not edited.
`REPOINT-03`/`REPOINT-04` stay `Pending`. `packer-finding.mjs`'s recorded provenance value stays
settled. The GSD isolation-guard identifier mismatch (`31-VERIFICATION.md` Deviations #6) is
tooling, not this phase's; nothing under the vendored `.claude/gsd-core/` tree may be edited and
`.claude/gsd-local-patches/` must stay empty.

**Two counting facts an executor will otherwise re-litigate.** The removal gate's printed
`attribution-guard-test 14` is the CROSS-FILE AGGREGATE (12 for `skill-attribution.test.ts` plus 2
for `check-skill-fork-honesty.mjs`), not a figure conflicting with 12; `31-VERIFICATION.md`
Deviations #2 records the reconciliation and withdraws `31-03-SUMMARY.md:327`'s parenthetical. And
`31-02`'s declared `contains: "Adapted from the external analyser."` is an ACCEPTED deviation, not a gap:
the literal would raise this file's pinned subject count from 12 to 13 and red a gate no plan in
this phase may move, and the derivation from `manifest.repository` was verified to evaluate to
exactly that string.
</notes>

<output>
Create `.planning/phases/31-procedure-re-pointing/31-04-SUMMARY.md` when done.
</output>
