---
phase: 31-procedure-re-pointing
plan: 03
type: execute
wave: 3
depends_on: [31-01, 31-02]
files_modified:
  - scripts/check-no-regenerator2000.mjs
  - .planning/STATE.md
autonomous: true
requirements: [REPOINT-03, REPOINT-04]
requirement_subparts:
  - "REPOINT-03 — the living citations of the attribution criterion name the criterion that exists after D-01's narrowing"
  - "REPOINT-04 — the four judgements this phase took on the snapshot record are written down where a later reader will find them"
worktree: false
worktree_reason: "This plan delivers `.planning/STATE.md` content — one stale citation correction and a dated Decisions entry. Worktree executors may not touch STATE.md and execute-plan strips it from the commit, so under isolation this plan would report success while delivering nothing. Run it sequentially on the main tree with USE_WORKTREES_FOR_PLAN=false."

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No living guard or state record points a reader at a Phase 31 success criterion that `D-01` removed: the two citations in `scripts/check-no-regenerator2000.mjs` and the one in `.planning/STATE.md` name the criterion the attribution clause actually is after the narrowing (REPOINT-03)"
    - "Correcting an exemption's `why` string changed the gate's prose and nothing else: `removal-gate.test.ts` still drives the real exported predicates green over its four planted evasion routes, and the gate still reports `skill-attribution-headers 24` on its six unchanged per-file pins (REPOINT-03)"
    - "`.planning/STATE.md` carries a dated `- [Phase 31]:` Decisions entry recording all four judgements this phase took — the inferred `STORE-04` id, the date-don't-delete disposition of retired ids and superseded phase numbers, the deliberate absence of a permanent zero-token gate over the manifest's prose, and the deliberate non-promotion of the two requirement statuses (REPOINT-04)"
    - "`REPOINT-03` and `REPOINT-04` still read `Pending` in `.planning/REQUIREMENTS.md` when this plan closes, and STATE.md records that as a decision rather than an omission: a status row does not move ahead of the re-verification verdict that scores it (REPOINT-03, REPOINT-04)"
    - "Historical records are left alone: `29-09-SUMMARY.md` and this phase's own RESEARCH and PATTERNS files still quote the pre-narrowing criterion number, because an executed-phase artifact legitimately quotes the wording it describes (REPOINT-03)"
  artifacts:
    - path: "scripts/check-no-regenerator2000.mjs"
      provides: "The `SKILL_ATTRIBUTION_PINS` doc comment and the `skill-attribution-headers` exemption `why` string, both citing the criterion that exists"
      contains: "two naming lines byte-identical"
    - path: ".planning/STATE.md"
      provides: "The corrected Phase 29 decision line plus a dated Phase 31 Decisions entry recording this phase's four judgements"
      contains: "[Phase 31]"
  key_links:
    - "`scripts/check-no-regenerator2000.mjs`'s `skill-attribution-headers` exemption -> the ROADMAP Phase 31 criterion it cites as its reason to exist. The exemption is permanent precisely because a criterion requires the headers to survive; a citation pointing at a criterion that was removed reads as an exemption with no live justification"
    - "STATE.md's Decisions log -> the next planner's reading of what this phase decided and why. It is the only place the inferred `STORE-04` id and the non-promotion are recorded as decisions with reversal conditions"
  prohibitions:
    - "No entry of `SKILL_ATTRIBUTION_PINS` is edited, and neither `isInsideSkillAttributionBlock` nor `SUBJECT_NEEDLE` nor any exemption's scope, path or hit count is touched — only the two prose strings"
    - "The removal gate's `.planning/` prefix scope exclusion is not widened, and `docs-dangling-refs.test.ts`'s `.planning/phases/**` exclusion is not widened"
    - "`REPOINT-03` and `REPOINT-04` are not flipped to `[x]` or `Complete` in `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` or `.planning/STATE.md` by this plan"
    - "No executed-phase artifact under `.planning/phases/29-*/` is edited"
    - "`.planning/phases/31-procedure-re-pointing/31-RESEARCH.md` and `31-PATTERNS.md` are not edited — they quote the stale citation while describing its correction, which is the same exemption executed-phase artifacts get"
    - "STATE.md's frontmatter counters, Current Position block, Performance Metrics tables and Deferred Items ledger are not rewritten — a Decisions entry is appended and one citation corrected, nothing else"
    - "No `gsd` state, roadmap or requirements mutation verb is trusted blind: diff the file after every call. Project memory records `state.*` verbs flattening Current-Position prose, `state.add-blocker` deleting standalone NONE tokens, `phase.complete` flipping unmet requirements, and `roadmap.update-plan-progress` leaving stale trailing prose"
    - "No file under `src/skills/**` or `installer/skills/**` is edited, and `src/mcp/vice/anno-tools.ts` is not edited"
    - "The bare `npm test` full glob is never used as a verification command"
---

<objective>
Correct the three living citations of a Phase 31 success criterion that `D-01` removed, and
write down the four judgements this phase took so a later reader finds them as decisions rather
than reconstructing them from a diff.

Purpose: the removal gate's `skill-attribution-headers` exemption is **permanent**, and its
stated reason to exist is that a ROADMAP criterion requires the attribution headers to survive
with their two naming lines byte-identical. `D-01` narrowed Phase 31 from four-plus criteria to
two, so that citation now points at a criterion number that does not exist — an exemption whose
justification cannot be looked up is the shape of an exemption nobody can audit. Separately,
this phase inferred one requirement id, chose to date rather than delete four retired tokens,
declined to build a permanent gate, and declined to promote its own requirement statuses. All
four are decisions; none is currently recorded anywhere a planner reads.

Output: two prose corrections in `scripts/check-no-regenerator2000.mjs`, one in
`.planning/STATE.md`, and one dated `- [Phase 31]:` Decisions entry. No status flips.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-procedure-re-pointing/31-RESEARCH.md
@.planning/phases/31-procedure-re-pointing/31-PATTERNS.md
@.planning/phases/31-procedure-re-pointing/31-01-SUMMARY.md
@.planning/phases/31-procedure-re-pointing/31-02-SUMMARY.md
</context>

<!-- planner-discipline-allow: Phase 31 criterion 4 -->

<tasks>

<task type="auto">
  <name>Task 1: Re-point the three living citations of the removed criterion number — one commit</name>

  <files>scripts/check-no-regenerator2000.mjs, .planning/STATE.md</files>

  <read_first>
    - `scripts/check-no-regenerator2000.mjs` lines 280-300 (the `SKILL_ATTRIBUTION_PINS` doc
      comment, first citation) and lines 512-535 (the `skill-attribution-headers` exemption
      object and its `why` string, second citation) — the two sites, and the surrounding prose
      whose register the correction must keep
    - `scripts/check-no-regenerator2000.mjs` lines 340-365 (the six per-file `blocks`/`hits`
      pins) and lines 980-1000 (the bidirectional pin loop) — read so it is clear these are
      **not** being touched: a lower block count means an attribution was deleted, a higher one
      means the exemption widened, and both must keep failing
    - `.planning/STATE.md` — the `- [Phase 29]:` decision line carrying the third citation, in
      the Decisions section. Locate it with `grep -n` rather than by line number; the file is
      long and its line numbers drift
    - `.planning/ROADMAP.md` § "### Phase 31: Procedure Re-pointing" — the two Success Criteria
      as they now read after `D-01`, so the replacement number is read off the roadmap rather
      than guessed
    - `src/mcp/vice/removal-gate.test.ts` (header plus the list of what it imports) — read to
      confirm what this edit is inert to: it drives the real exported predicates, and a `why`
      string is not one of them
    - `.planning/phases/31-procedure-re-pointing/31-PATTERNS.md` § "`scripts/check-no-regenerator2000.mjs` (config, exemption prose)"
  </read_first>

  <action>
Three prose sites, one commit. Nothing executable changes.

Each site cites the ROADMAP Phase 31 criterion that requires the ABS-02 attribution headers to
survive the re-pointing with their two naming lines byte-identical, and each names the wrong
ordinal: `D-01` narrowed the phase from four-plus criteria down to two, and the attribution
clause is now the **first** of the two. Change the ordinal from four to one at all three sites,
and at each one add a short dated parenthetical recording that the phase was narrowed by `D-01`
on 2026-08-30 and the criterion renumbered — so a reader who remembers the old number can see
the move instead of suspecting a typo. Keep every other word of the surrounding sentence: the
citation's *content* ("required to survive the re-pointing with their two naming lines
byte-identical") is correct and is the exemption's whole justification.

Site 1 — `scripts/check-no-regenerator2000.mjs`, the `SKILL_ATTRIBUTION_PINS` doc comment.
Site 2 — `scripts/check-no-regenerator2000.mjs`, the `skill-attribution-headers` exemption's
`why` string. Mind the string concatenation: the sentence is split across `+`-joined fragments,
so the replacement must not break the JavaScript or lose a leading/trailing space at a seam.
Site 3 — `.planning/STATE.md`, inside the `- [Phase 29]:` decision line that records the
headers moving to a BLOCK-scoped permanent exemption. Correct the ordinal in place; do not
rewrite the line's substance and do not re-date it, because it records a Phase 29 decision.

Leave every other occurrence in the repository alone. `29-09-SUMMARY.md` is an executed-phase
artifact and this phase's own `31-RESEARCH.md` / `31-PATTERNS.md` quote the stale citation while
describing its correction — all three legitimately carry the wording they describe, which is the
same reasoning `docs-dangling-refs.test.ts` states for not scanning `.planning/phases/**`.

Do not touch `SKILL_ATTRIBUTION_PINS`' six entries, `isInsideSkillAttributionBlock`,
`SUBJECT_NEEDLE`, any exemption's `id`, scope, paths or hit counts, or the `.planning/` prefix
in the scope predicate. Editing a `why` string is inert to `removal-gate.test.ts`, which drives
only the real exported predicates; editing any of those other names is not.

Commit once, message `docs(31-03): re-point the three living citations of the narrowed Phase 31
criterion`.
  </action>

  <verify>
    <automated>node scripts/check-no-regenerator2000.mjs</automated>
    <automated>cd src/mcp/vice && node --test removal-gate.test.ts</automated>
    <automated>cd src/mcp/vice && npm run typecheck && npm run test:automated</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'Phase 31 criterion 4' scripts/check-no-regenerator2000.mjs` → `0`
    - `grep -c 'Phase 31 criterion 4' .planning/STATE.md` → `0`
    - `grep -c 'Phase 31 criterion 1' scripts/check-no-regenerator2000.mjs` → `2`
    - `grep -c 'Phase 31 criterion 1' .planning/STATE.md` → `1`
    - `grep -c 'two naming lines byte-identical' scripts/check-no-regenerator2000.mjs` → `2` (the
      citation's content is preserved at both sites, not just its number)
    - `grep -c 'D-01' scripts/check-no-regenerator2000.mjs` → at least `2` (the dated
      renumbering parenthetical is present at both sites)
    - `grep -c 'Phase 31 criterion 4' .planning/phases/29-the-mcp-surface/29-09-SUMMARY.md` → `1`
      (unchanged — the historical record still quotes the wording it describes)
    - `node -e "process.exit(0)" && node --check scripts/check-no-regenerator2000.mjs` — the file
      is still syntactically valid JavaScript after editing a concatenated string
    - `node scripts/check-no-regenerator2000.mjs` exits 0 and reports
      `skill-attribution-headers 24` and an **empty** temporary allow-list
    - `git diff -U0 scripts/check-no-regenerator2000.mjs` shows changes only inside a comment
      block and inside a `why:` string — no changed line contains `SKILL_ATTRIBUTION_PINS`
      entries, `blocks:`, `hits:`, `SUBJECT_NEEDLE`, `isInsideSkillAttributionBlock` or
      `git ls-files`
    - `cd src/mcp/vice && node --test removal-gate.test.ts` exits 0 with `# fail 0`
    - `cd src/mcp/vice && npm run typecheck` exits 0 and `npm run test:automated` exits 0 with
      `# fail 0`. Confirm `systemctl --user is-active vice-broker` reads `inactive` FIRST
    - `git show --stat HEAD` lists exactly two paths: `scripts/check-no-regenerator2000.mjs` and
      `.planning/STATE.md`
  </acceptance_criteria>

  <reversibility rating="reversible">Three prose corrections in two files; `git revert` restores the prior text exactly and no executable predicate was involved.</reversibility>

  <done>
No living guard or state record cites the removed criterion ordinal; both removal-gate sites and
the STATE.md decision line name criterion 1 with a dated renumbering parenthetical; the
citation's content is preserved verbatim at both gate sites; the gate, `removal-gate.test.ts`,
the typecheck and the full automated suite are green; and the historical records are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record this phase's four judgements as a dated STATE.md Decisions entry — and record the non-promotion as a decision</name>

  <files>.planning/STATE.md</files>

  <read_first>
    - `.planning/STATE.md` § "### Decisions" — read the surrounding entries, especially the
      `- [Phase 29]:` and `- [Phase 30]:` lines, so the new entry matches their shape: the
      bracketed phase tag, the decision stated first, then the evidence and the reversal
      condition in the same sentence rather than as a separate note
    - `.planning/phases/31-procedure-re-pointing/31-01-SUMMARY.md` and `31-02-SUMMARY.md` — the
      measured outputs the entry cites. Quote numbers from these, never from the plans
    - `.planning/REQUIREMENTS.md` — the two `REPOINT-03` / `REPOINT-04` checkboxes and their
      traceability rows, read to confirm they are **left** at `Pending`; and the promotion
      paragraph that states the standing rule (*"a status row must not move ahead of the
      re-verification verdict that scores it"*) and the four-sites-one-edit rule, which is the
      rule this task deliberately does not trigger
    - `.planning/phases/31-procedure-re-pointing/31-RESEARCH.md` § "Assumptions Log" (A1, A2, A3)
      and § "Open Questions" 1-4 — the four judgements, in the words of the document that
      raised them
    - `.planning/phases/31-procedure-re-pointing/31-VALIDATION.md` § "Wave 0 Requirements" — the
      optional second item, whose recorded fate is judgement 3
  </read_first>

  <action>
Append one `- [Phase 31]:` entry to `.planning/STATE.md`'s Decisions section, in the same shape
as the neighbouring `- [Phase 29]:` / `- [Phase 30]:` lines. Change nothing else in the file:
not the frontmatter, not Current Position, not the Performance Metrics tables, not the Deferred
Items ledger. If a `gsd` state verb is used to write it, diff the file immediately afterwards
and revert any collateral change — project memory records these verbs flattening Current
Position prose, deleting standalone `NONE` tokens, and resetting plan counters.

The entry records four judgements. Each needs its decision, its evidence, and the condition
that would reverse it — that is what makes it a record rather than a note:

  1. **`STORE-04` is the id recorded on `r2000_undo`'s `omit` disposition, and it is INFERRED.**
     No document says "the criterion for `r2000_undo` is `STORE-04`" in those words. It is the
     strongly-indicated id: decision `D2`'s closing sentence is that `STORE-04` is scoped to
     what a planted-violation test can actually prove, and `STORE-04` is the only requirement
     whose text contains "an edit can be reverted". The **direction** — the omission stands — is
     not inferred: it is verified three independent ways (`D2`, the Out-of-Scope row, and the
     live surface carrying no undo route under any spelling across 19 tool definitions), and the
     manifest's justification now carries that reasoning in prose rather than leaning on the id.
     Reversal condition: a later record naming a different criterion; the fix is a one-token
     edit and the justification prose survives it unchanged.
  2. **Retired requirement ids and superseded phase numbers stay in the manifest as dated facts;
     only live routes were re-pointed.** The retired `SURF-*` ids and the superseded phase
     numbers are facts about what was decided at the Phase 19 pin, and a decision with its
     reasoning deleted is indistinguishable from an oversight a year later. Reversal condition:
     a successor requirement id existing for one of them, at which point that one becomes a
     route and gets re-pointed.
  3. **No permanent gate was built over the manifest's prose, deliberately, and this is the
     recorded fate of the optional Wave 0 item.** The zero-hit sweep over the retired tokens is
     a plan-time acceptance criterion, not a committed assertion, for two reasons that agree:
     `anno-derivation.test.ts` is this phase's *verifier* and editing it would make the record
     and its checker move together; and a permanent zero-hit assertion would be **wrong** under
     this phase's own governing rule, because a dated past-tense sentence may legitimately name
     a retired module in a record whose whole purpose is to be a dated snapshot. Reversal
     condition: the same class of prose drift recurring — a second occurrence turns "no
     mechanical reader" from a tolerated gap into a demonstrated one, and the right instrument
     is then a curated allow-list of dated mentions rather than a bare zero.
  4. **`REPOINT-03` and `REPOINT-04` were NOT promoted by this phase's plans, and that is a
     decision.** Criterion 1 was measured true on disk before any plan ran, so the temptation to
     flip the rows was real. They stay `Pending` because this project's standing rule is that a
     status row does not move ahead of the re-verification verdict that scores it, and because
     the evidence this phase produces is a committed assertion whose whole point is that
     `/gsd-verify-work` can re-run it rather than read a claim about it. Reversal condition: the
     Phase 31 verification verdict; at that point the checkboxes, the two traceability rows and
     the promotion paragraph move in ONE edit, per this file's four-sites-one-edit rule.

Cite the measured evidence with real numbers taken from the two SUMMARYs: the manifest's four
zero-hit greps, the `r2000_undo` key order and `requirement_id` value, the per-tree
blocks/adapted/repository counts, and the ABS-03 runner's OK line figures. Do not restate a
number the SUMMARYs do not contain.

Commit once, message `docs(31-03): record Phase 31's four judgements in STATE.md`.
  </action>

  <verify>
    <automated>node .claude/gsd-core/bin/gsd-tools.cjs query frontmatter.validate .planning/STATE.md --schema state</automated>
    <automated>cd src/mcp/vice && npm run test:automated</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^- \[Phase 31\]:' .planning/STATE.md` → at least `1`
    - The new entry names all four judgements: `grep -c 'STORE-04' .planning/STATE.md` → at least
      `1`; and the entry text contains the substrings `INFERRED` (or `inferred`), `dated`,
      `Wave 0` (or `no permanent gate`), and `Pending`
    - `grep -c 'REPOINT-03' .planning/REQUIREMENTS.md` is unchanged from its pre-task value, and
      `grep -c '| REPOINT-03 | Phase 31 | Pending |' .planning/REQUIREMENTS.md` → `1` and
      `grep -c '| REPOINT-04 | Phase 31 | Pending |' .planning/REQUIREMENTS.md` → `1` (the two
      rows are still Pending — the non-promotion is real, not merely asserted)
    - `grep -c '\- \[ \] \*\*REPOINT-03\*\*' .planning/REQUIREMENTS.md` → `1` and the same for
      `REPOINT-04` (the checkboxes are unticked)
    - `git status --porcelain .planning/REQUIREMENTS.md .planning/ROADMAP.md` → empty (this task
      touches neither)
    - `git diff -U0 .planning/STATE.md` for this task shows **only added lines** in the Decisions
      section — zero deletions and zero changes anywhere else in the file. Any other hunk is
      collateral damage from a state verb and must be reverted before committing
    - `diff <(git show HEAD~1:.planning/STATE.md | sed -n '1,20p') <(sed -n '1,20p' .planning/STATE.md)` → empty (the frontmatter is byte-identical)
    - `node .claude/gsd-core/bin/gsd-tools.cjs query frontmatter.validate .planning/STATE.md --schema state` reports `valid: true`
    - `cd src/mcp/vice && npm run test:automated` exits 0 with `# fail 0`, broker `inactive`
    - `git show --stat HEAD` lists exactly one path: `.planning/STATE.md`
  </acceptance_criteria>

  <reversibility rating="reversible">An appended log entry in a planning record; reverting it removes the entry and nothing depends on it.</reversibility>

  <done>
STATE.md carries one dated `- [Phase 31]:` entry recording the inferred `STORE-04` id, the
date-don't-delete disposition, the deliberate absence of a permanent prose gate with its
reversal condition, and the deliberate non-promotion of the two requirement statuses — each with
evidence quoted from the two SUMMARYs and each with a named reversal condition. Both requirement
rows still read `Pending`. Nothing else in STATE.md changed, and REQUIREMENTS.md and ROADMAP.md
are untouched.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates **no** new symbol, constant, function, test, file, env var, dependency or MCP
verb. Its deliverables are three prose corrections and one appended log entry.

For the phase as a whole: plan `31-01` creates the JSON key
`disposition_rationale.r2000_undo.requirement_id`; plan `31-02` creates two constants for the
naming lines, the `SKILL_ATTRIBUTION_ROOTS` tuple and its two aliases, the
`ABS02_BLOCKS_PER_TREE_FLOOR` floor, the `namingLineCountsIn()` and `skippableEmptyRoot()`
helpers, and two named tests. Those tables live in `31-01-PLAN.md` and `31-02-PLAN.md`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Removal-gate exemption prose -> a future auditor | An exemption is permanent because a criterion justifies it. The `why` string is the only place that justification is written; a citation that cannot be looked up is an exemption nobody can audit |
| Planning record -> the next planner | STATE.md's Decisions log is read as fact by every later phase. An unrecorded judgement is indistinguishable from an accident |
| GSD tooling -> `.planning/*.md` | The state and roadmap mutation verbs rewrite whole sections and have a recorded history of collateral edits in this repository. Every write across this boundary is diffed |

## STRIDE Threat Register

ASVS L1, `security_block_on: high`. RESEARCH § Security Domain measured the surface as
essentially nil. **No new mitigations are required; the obligation is not to weaken an existing
one.**

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-14 | Tampering | The removal gate, while editing its prose | high | mitigate | The edit is confined to a comment block and a `why:` string. A `git diff -U0` acceptance criterion asserts no changed line contains `SKILL_ATTRIBUTION_PINS` entries, `blocks:`, `hits:`, `SUBJECT_NEEDLE`, `isInsideSkillAttributionBlock` or the scope query; `node --check` asserts the concatenated string was not broken; `removal-gate.test.ts` re-drives the four planted evasion routes; the gate must still report `skill-attribution-headers 24` |
| T-31-15 | Repudiation | An exemption whose justification cannot be looked up | high | mitigate | Both gate citations are re-pointed at the criterion that exists, with the citation's content preserved verbatim (asserted by a `grep -c` of the content clause, not just of the ordinal) and a dated renumbering parenthetical so the move is visible rather than silent |
| T-31-16 | Repudiation | A requirement status claiming more than its evidence | high | mitigate | The plan does not promote either row, and asserts they are still `Pending` mechanically. The promotion is left to the re-verification verdict, and the non-promotion is recorded as a decision with a named reversal condition so it cannot later read as an oversight |
| T-31-17 | Tampering | Collateral damage from a GSD state mutation verb | medium | mitigate | Every write to STATE.md is diffed; the acceptance criteria require zero deletions anywhere in the file and a byte-identical frontmatter, so a flattened Current-Position block or a reset counter fails the task rather than shipping |
| T-31-18 | Repudiation | A historical record edited to match the present | medium | mitigate | `29-09-SUMMARY.md` is asserted **unchanged** by a positive `grep -c` on the old wording. An executed-phase artifact quoting the wording it describes is correct, and rewriting it would destroy the record of the narrowing |
| T-31-19 | Information disclosure | Planning-record content | low | accept | The records contain requirement ids, phase numbers and file paths. Nothing secret exists to disclose |
| T-31-SC | Tampering | npm / pip / cargo installs | low | accept | **No package-manager install task exists in this phase.** RESEARCH § "Package Legitimacy Audit" records the gate as not applicable: every tool is a Node built-in or a first-party committed file. No `[ASSUMED]`, `[SUS]` or `[SLOP]` verdict exists, so no legitimacy checkpoint is owed |
</threat_model>

<verification>
Run from the repository root unless a command says otherwise.

1. `systemctl --user is-active vice-broker` reads `inactive`. First, always.
2. `node --check scripts/check-no-regenerator2000.mjs` — still valid JavaScript.
3. `node scripts/check-no-regenerator2000.mjs` — exit 0, `skill-attribution-headers 24`, empty
   temporary allow-list.
4. `cd src/mcp/vice && node --test removal-gate.test.ts` — exit 0, `# fail 0`.
5. The zero-hit and positive-hit greps in both tasks' acceptance criteria, including the one
   that asserts `29-09-SUMMARY.md` is **unchanged**.
6. `git diff -U0` review of both files — comment/string-only in the gate, additions-only in
   STATE.md.
7. `cd src/mcp/vice && npm run typecheck` — exit 0.
8. `cd src/mcp/vice && npm run test:automated` — exit 0, `# fail 0`. **Never** the bare
   `npm test` full glob.
9. `git status --porcelain .planning/REQUIREMENTS.md .planning/ROADMAP.md src/skills installer/skills` — empty.
</verification>

<success_criteria>
- Zero hits for the removed criterion ordinal in `scripts/check-no-regenerator2000.mjs` and
  `.planning/STATE.md`; two and one hits respectively for the criterion that exists; the
  citation's content clause still present at both gate sites; a dated renumbering parenthetical
  at both.
- `29-09-SUMMARY.md`, `31-RESEARCH.md` and `31-PATTERNS.md` unchanged.
- The gate's executable surface provably untouched: `node --check` clean, `git diff -U0`
  comment/string-only, `removal-gate.test.ts` green, `skill-attribution-headers 24`.
- One dated `- [Phase 31]:` Decisions entry covering all four judgements, each with evidence
  quoted from the two SUMMARYs and each with a named reversal condition.
- `REPOINT-03` and `REPOINT-04` still `Pending`, checkboxes unticked, REQUIREMENTS.md and
  ROADMAP.md untouched by this plan.
- STATE.md diff is additions-only with a byte-identical frontmatter.
- Typecheck and the full automated suite green at zero failures with the broker stopped.
</success_criteria>

<output>
Create `.planning/phases/31-procedure-re-pointing/31-03-SUMMARY.md` when done. It must record
both commit shas with their `git show --stat` output, the before/after grep counts for all three
citation sites, the verbatim `git diff -U0 .planning/STATE.md` hunk header list (proving
additions-only), and the two `Pending` rows quoted from `REQUIREMENTS.md` as they stand at the
close.
</output>
