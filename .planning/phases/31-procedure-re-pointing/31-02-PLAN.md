---
phase: 31-procedure-re-pointing
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - src/mcp/vice/skill-attribution.test.ts
autonomous: true
requirements: [REPOINT-03]
requirement_subparts:
  - "REPOINT-03 — 10 instances across two trees, each carrying two naming lines, byte-identical"
  - "REPOINT-03 — the ABS-02 attribution chain survives the code's deletion (scored, not assumed)"

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A committed assertion — not a grep run once — scores `REPOINT-03`'s exact sentence: every ABS-02 attribution block in BOTH skill trees carries both naming lines, byte-identically, and the two trees carry equal numbers of them"
    - "The assertion cannot rot into a no-op: the two naming-line constants are asserted non-empty, each root's block count carries a floor, and a planted one-character mutation is proven to make it fail"
    - "The assertion does not go red the day a sixth procedure is absorbed: every count is compared as a relation (source root vs shipped root, per-block adapted vs repository) plus a floor, never against the phase's measured total as a literal"
    - "Adjacency edge: the assertion passes with `src/skills/c64-program-recon/SKILL.md` in the corpus, whose second block orders `Adapted from regenerator2000.` (:571) and the `Source repository:` line (:577) NON-adjacently with `Source path:` between them — nothing pins their relative position"
    - "Empty edge: a root that yields zero files is classified explicitly — skippable when it is the generated, gitignored shipped tree (a fresh clone has never run `prepack`), and a hard failure when it is the source tree. Both branches are asserted directly, so a corpus that silently shrank to zero cannot pass"
    - "Encoding edge: the two naming lines are compared as exact byte strings — including the two-space leading indent on the `Source repository:` line — with only a trailing `\\r` tolerated for CRLF. No trim, no case folding, no Unicode normalisation, no substring match"
    - "Ordering edge: every verdict is a multiset property. The assertion's result is unchanged under any corpus-walk order and no assertion depends on the order `walkSkills()` returns files in"
    - "Concurrency edge: nothing in this plan is concurrent or interruptible — the new test is a synchronous single-process read plus assertions, it spawns no process, opens no socket, writes no file, and imports nothing outside `node:test`, `node:assert/strict`, `node:fs`, `node:path` and the two helpers the file already imports"
    - "`ABS-03`'s pairwise trigger-collision check still passes over all seven skill descriptions, and CLAUDE.md's project-skills table is still byte-identical to every `description:` — measured, with the numbers recorded (REPOINT-03)"
  artifacts:
    - path: "src/mcp/vice/skill-attribution.test.ts"
      provides: "Two new tests scoring the two-naming-lines claim across both skill trees, plus the named constants that pin the two lines"
      contains: "Adapted from regenerator2000."
  key_links:
    - "`SKILL_ATTRIBUTION_ROOTS` -> both `src/skills/` and `installer/skills/`: the file is `src/skills`-only by design, and this one test departs from that scope because the requirement is about two trees. The departure must be stated at the point of use"
    - "the two naming-line constants -> `scripts/check-no-regenerator2000.mjs`'s `SKILL_ATTRIBUTION_PINS`: the gate pins blocks and hits per file in both trees; this test pins the LINES. Neither replaces the other and both must stay green"
    - "`attributionBlocks()` (already in this file) -> the same `ATTRIBUTION (ABS-02)` marker the removal gate's `skillAttributionBlocks()` anchors on — the shared anchor is the pattern; no second regex is derived"
  prohibitions:
    - "No new test file is created — the assertion goes inside `src/mcp/vice/skill-attribution.test.ts`"
    - "No file is named `docs-*.test.ts` — it would join `audit-gate.mjs`'s globbed guard set and red `audit-integrity.test.ts` until `EXPECTED_DOCS_GUARD_NAMES` and `DOCS_GUARD_FLOOR` moved in the same commit"
    - "No count is compared against the phase's measured total as a numeric literal"
    - "No assertion pins the relative position, ordering or adjacency of the two naming lines"
    - "No file under `src/skills/**` or `installer/skills/**` is edited, and nothing is ever written into either tree — every planted violation lives in memory as a string"
    - "`hostpath.ts` and `containerpath.ts` are not imported; repo root comes from the file's existing `repoRoot({ from: HERE })`"
    - "Nothing under `src/skills/` is `import()`ed, `require()`d, `eval()`ed or spawned — skill content is untrusted first-party prose, read as a string and matched"
    - "The frozen `ABSORBED_FILES` registry is not replaced with a corpus scan for files containing an attribution block — that check is circular"
    - "No second corpus walker is derived; the file's already-imported `walkSkills` is used"
    - "`routine-queue-walker/SKILL.md`'s `description:` is not rewritten — it is final, and rewriting it re-triggers ABS-03 and breaks CLAUDE.md's byte-identity assertion"
    - "No attempt is made to drive `grep -rail 'regenerator2000' installer/skills` toward zero — unsatisfiable by design"
    - "The bare `npm test` full glob is never used as a verification command"
---

<objective>
Close the one genuine instrument gap in criterion 1: no assertion anywhere in the repository
scores `REPOINT-03`'s exact sentence — *"10 instances across two trees, each with its two
naming lines byte-identical"*. Today that claim is covered only by proxy (the removal gate's
per-file block and hit pins, plus `diff -r`), and a proxy is not the sentence.

Purpose: criterion 1 is **already true on disk** — measured this session at 10 blocks, 10
`Adapted from…` lines, 10 `Source repository:` lines across both trees, all three absorbed
playbooks byte-identical between them. A recorded grep is evidence for one commit; a committed
assertion is evidence forever. This project's own doctrine is registry-and-assertion, not
measurement-in-prose.

This is `REPOINT-03`, which after `D-01`'s narrowing is the **first** of Phase 31's two success
criteria. `D-01` also moved the re-pointing itself into Phase 29 plan 29-09, which is why
`routine-queue-walker/SKILL.md:3`'s `description:` is already final and is scored here, not
rewritten.

Output: two new tests inside `src/mcp/vice/skill-attribution.test.ts` — the scoring assertion
and its in-memory planted-violation proof — plus the named constants that pin the two lines.
No other file changes.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-procedure-re-pointing/31-RESEARCH.md
@.planning/phases/31-procedure-re-pointing/31-PATTERNS.md
@.planning/phases/31-procedure-re-pointing/31-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Score the two-naming-lines claim across both skill trees — relations plus a floor, no adjacency, no literal total</name>

  <files>src/mcp/vice/skill-attribution.test.ts</files>

  <read_first>
    - `src/mcp/vice/skill-attribution.test.ts` — the file being modified. Read the header block
      (lines 1-120: the five-ways non-vacuity doctrine, the "WHAT NOT TO DO" list, the
      `hostpath.ts`/`containerpath.ts` and never-execute prohibitions), the constants and
      helpers (lines 80-200: `ROOT`, `SKILLS_DIR`, `MANIFEST_PATH`, `REQUIRED_HEADER_FIELDS`,
      `attributionBlocks()`), and the two tests whose shape this task copies
      (lines 530-625: constant-set non-vacuity -> walk -> floor -> `deepEqual(offenders, [])`,
      and the in-memory plant)
    - `src/mcp/vice/anno-verb-coverage.test.ts` lines 215-232 (the **only** existing both-trees
      root pair in `src/mcp/vice/` and its doc comment — paraphrase that comment) and lines
      385-410 (the fresh-clone skip plus the per-root non-vacuity floor — copy this control
      flow exactly; it is why the guard does not red on a clone that never ran `prepack`)
    - `scripts/lib/skill-corpus.mjs` lines 30-62 (`walkSkills()` — note that `walk()` swallows
      the `readdirSync` failure and returns an empty array for a missing directory, which is
      what makes the fresh-clone branch reachable without an `existsSync` probe)
    - `scripts/check-no-regenerator2000.mjs` lines 285-300 and 345-365 (the
      `SKILL_ATTRIBUTION_PINS` doc comment and the six per-file `blocks`/`hits` pins) — the
      companion guard that pins blocks and hits in both trees while this test pins the lines.
      **Read only; plan 31-03 owns the one edit to this file**
    - `src/skills/routine-queue-walker/SKILL.md` lines 1-25 and
      `src/skills/c64-program-recon/SKILL.md` lines 560-590 — one adjacent block and the one
      NON-adjacent block, so the shape being asserted is seen rather than assumed
    - `.planning/phases/31-procedure-re-pointing/31-PATTERNS.md` § "`src/mcp/vice/skill-attribution.test.ts` (test, transform) — ONE new test" (analogs A-D, verbatim)
    - `.planning/phases/31-procedure-re-pointing/31-RESEARCH.md` § "Finding 1", § "Pattern 3", § "Pattern 4", and Pitfalls 3 and 4
  </read_first>

  <action>
Extend `src/mcp/vice/skill-attribution.test.ts` in place. Add constants, two small pure
helpers and ONE new test. Create no new file.

Constants, each with a doc comment saying what it is and why it is a constant:
  - `ABS02_ADAPTED_LINE` — the exact line `Adapted from regenerator2000.`
  - `ABS02_SOURCE_REPOSITORY_LINE` — the exact line whose text is two spaces, then
    `Source repository: `, then the upstream URL already held in `manifest.repository`. The
    two-space leading indent is part of the string; do not trim it and do not reconstruct the
    URL by hand — build the constant from `manifest.repository` so the one URL in the record
    stays the one URL, exactly as the existing header tests do with `manifest.repository`.
  - `SKILL_ATTRIBUTION_ROOTS` — a two-element `as const` tuple: the file's existing
    `SKILLS_DIR` first, then `join(ROOT, "installer", "skills")`. Paraphrase
    `anno-verb-coverage.test.ts`'s doc comment: the shipped tree is GENERATED and GITIGNORED
    yet SHIPPED, `git ls-files installer/skills` returns 0, so any gate scoped to tracked files
    is structurally blind to the copy users actually receive.
  - `SKILL_ATTRIBUTION_SOURCE_ROOT` / `SKILL_ATTRIBUTION_SHIPPED_ROOT` — named aliases for
    element 0 and element 1, so the skip branch reads by name rather than by index.
  - `ABS02_BLOCKS_PER_TREE_FLOOR` — the floor, value five. A floor, never an equality.

State explicitly, in a comment above `SKILL_ATTRIBUTION_ROOTS`, that this file is
`src/skills`-only by design and that this ONE test departs from that scope because the
requirement it scores is about two trees. Do not change `SKILLS_DIR` or re-scope any existing
test.

Helper 1 — `namingLineCountsIn(text)`: returns `{ adapted, repository }`, counting **exact
line matches** of the two constants. Split the text on newlines, strip a single trailing `\r`
from each line so a CRLF checkout behaves identically, and compare with `===`. No trim, no
`toLowerCase()`, no `includes()`. This is `grep -rx` semantics, and byte-identity is the claim
being scored — a substring match would pass on a line with trailing whitespace, which is
precisely the drift the assertion exists to catch.

Helper 2 — `skippableEmptyRoot(root, fileCount)`: returns `true` only when `fileCount` is zero
AND `root` is `SKILL_ATTRIBUTION_SHIPPED_ROOT`. A zero-file source root is never skippable.
Make this a named predicate rather than an inline condition so the empty-input edge can be
asserted directly instead of only being exercised incidentally.

The new test — name it so it reads as the requirement's own sentence, e.g.
`"both skill trees carry the ABS-02 naming lines byte-identically, in equal numbers"`:
  1. Non-vacuity for the constants themselves: assert both are non-empty, assert
     `ABS02_SOURCE_REPOSITORY_LINE` starts with two spaces and contains `manifest.repository`,
     and assert `ABS02_BLOCKS_PER_TREE_FLOOR` is positive. An emptied constant must FAIL rather
     than make every comparison below trivially true.
  2. Assert both branches of the empty-root predicate directly: shipped root with zero files is
     skippable; source root with zero files is not.
  3. For each root in `SKILL_ATTRIBUTION_ROOTS`: get the SKILL.md set with the already-imported
     `walkSkills(root)` filtered on `SKILL.md` (derive no second walker — the WR-12 lesson);
     if `skippableEmptyRoot(root, files.length)` then `continue`, carrying the same comment
     `anno-verb-coverage.test.ts` uses (a fresh clone has never run the installer's `prepack`,
     so the generated tree legitimately does not exist yet, and skipping it is safe ONLY
     because it is a pure copy of the source tree which was just scanned in full); otherwise
     assert `files.length > 0` so a traversal that silently shrank to zero fails.
  4. Per file, per block: run the file's existing `attributionBlocks()` — do NOT write a second
     regex — and for each block assert `namingLineCountsIn(block)` deep-equals
     `{ adapted: 1, repository: 1 }`. Per block, not per file: that is what makes the claim
     "each block carries its two naming lines" rather than "the file has some". Collect any
     offender as `<repo-relative path>#<block index>: adapted=<n> repository=<n>` and finish
     with `assert.deepEqual(offenders, [], ...)` in this file's house style.
  5. Accumulate per root: `blocks`, `adapted`, `repository`. Then assert, as relations and a
     floor and nothing else:
       - per root, `adapted === blocks` and `repository === blocks` (the two lines travel
         together, one of each per block);
       - per root, `blocks >= ABS02_BLOCKS_PER_TREE_FLOOR`;
       - when both roots were scanned, the source root's `{blocks, adapted, repository}`
         deep-equals the shipped root's — this is the "across two trees" half, and it is a
         relation between two independently-maintained trees, which is the only shape that
         does not go red the day a sixth procedure is absorbed.
     Do not compare any count against the phase's measured total as a numeric literal. The
     registry's own precedent is `ABSORBED_FILES.length === manifest.procedures.length` with
     the comment "Deliberately not a literal 5", and
     `check-skill-description-overlap.mjs` records that this project already paid once for an
     exact census pin.
  6. Assert nothing about where the two lines sit relative to each other. One block orders them
     with another field between; an adjacency or ordering assertion goes red on a correct tree.

Add a short comment above the new test recording that it is rot-guard number six for this file
and that its planted-violation proof is the next test (task 2), so the file's header doctrine
and its body stay in agreement.
  </action>

  <verify>
    <automated>cd src/mcp/vice && node --test skill-attribution.test.ts</automated>
    <automated>cd src/mcp/vice && npm run typecheck</automated>
  </verify>

  <acceptance_criteria>
    - `cd src/mcp/vice && node --test skill-attribution.test.ts` exits 0 with `# fail 0`, and its
      `# tests` line is strictly greater than the pre-change count recorded in the SUMMARY
    - `cd src/mcp/vice && npm run typecheck` exits 0
    - `grep -c '^test(' src/mcp/vice/skill-attribution.test.ts` is exactly one greater than the
      pre-change value of `12` after this task (the second new test lands in task 2)
    - `grep -c 'installer' src/mcp/vice/skill-attribution.test.ts` → at least `1`, and
      `grep -c 'SKILL_ATTRIBUTION_ROOTS' src/mcp/vice/skill-attribution.test.ts` → at least `3`
      (declaration, the two aliases or the loop)
    - `! grep -Eq 'assert\.(equal|strictEqual|deepEqual)\([^;]*,\s*10\s*[,)]' src/mcp/vice/skill-attribution.test.ts` — no assertion compares anything to the phase's measured total as a literal
    - `! grep -Ev '^[[:space:]]*(//|\*|/\*)' src/mcp/vice/skill-attribution.test.ts | grep -Eq '^[[:space:]]*import[^;]*(hostpath|containerpath)'`
      exits 0 (the whole command is one line, so it copy-pastes as written). Neither seam is
      **imported**: comment lines are filtered out first and only the executable `import` form is
      matched. An any-occurrence grep is the wrong instrument here — this file's own header states
      the prohibition in prose at lines 70-71, so an unfiltered pattern matches the doctrine and
      the only lever toward green would be deleting the very rule the criterion exists to enforce.
      **Measured on the unmodified file 2026-08-31: already exits 0. This is a PRESERVE criterion
      — true before this task, and it must still be true after.** Its truth value is not beyond
      the task's reach: adding an `import` of either seam flips it, which is exactly the failure
      mode being guarded. Positive control run 2026-08-31: the same pipeline over a file whose
      first line imports `hostPath` from `./hostpath.ts` exits 1
    - `! grep -Ev '^[[:space:]]*(//|\*|/\*)' src/mcp/vice/skill-attribution.test.ts | grep -Eq 'child_process|\b(spawn|spawnSync|execFile|execFileSync|eval)\(|\b(require|import)[[:space:]]*\('`
      exits 0 (one line, copy-pasteable as written). Nothing under `src/skills/` is dynamically
      imported, required, evaluated or spawned; skill prose is read as a string and matched only.
      Comment lines are filtered out first and every alternative matches an **executable form** —
      a call site, a dynamic-import call, or a `node:child_process` import specifier — never a
      mention. That filtering is load-bearing: the header's prohibition sentence at line 73 spells
      the evaluate-and-require verbs verbatim, so the unfiltered pattern is FALSE on the untouched
      file and unsatisfiable except by deleting the doctrine. **Measured on the unmodified file
      2026-08-31: already exits 0. PRESERVE criterion — true before, must still be true after.**
      A new dynamic import, a `spawnSync` call or a `node:child_process` import flips it. Positive
      control run 2026-08-31: the same pipeline over a file whose first line imports `spawnSync`
      from `node:child_process` exits 1
    - `! grep -Eq 'writeFileSync|mkdirSync|rmSync|renameSync' src/mcp/vice/skill-attribution.test.ts` — no test writes into either skill tree
    - `git status --porcelain src/skills installer/skills` → empty after the test run (the working tree is not dirtied)
    - The test's own body proves the empty edge: `grep -c 'skippableEmptyRoot' src/mcp/vice/skill-attribution.test.ts` → at least `3` (definition plus both asserted branches plus the loop)
    - The test passes with `src/skills/c64-program-recon/SKILL.md` in the corpus. Record in the
      SUMMARY that `grep -n 'Adapted from regenerator2000.' src/skills/c64-program-recon/SKILL.md`
      and `grep -n 'Source repository' src/skills/c64-program-recon/SKILL.md` report the two
      lines at different, non-consecutive line numbers — the adjacency edge, scored
    - `ls src/mcp/vice/docs-*.test.ts` lists exactly the pre-existing set (no new file, and
      certainly none with that prefix)
  </acceptance_criteria>

  <done>
`skill-attribution.test.ts` carries one new test that walks both skill trees, asserts each
ABS-02 block contains exactly one of each byte-exact naming line, compares the two trees'
counts as a relation with a per-tree floor, classifies a zero-file root explicitly, and pins no
literal total and no adjacency. The file's own suite is green and `tsc --noEmit` is clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove the new predicate bites — an in-memory one-character mutation, and the ABS-03 measurement recorded</name>

  <files>src/mcp/vice/skill-attribution.test.ts</files>

  <read_first>
    - `src/mcp/vice/skill-attribution.test.ts` — the state task 1 left it in: the new constants,
      `namingLineCountsIn()`, `skippableEmptyRoot()` and the new test
    - `src/mcp/vice/skill-attribution.test.ts` lines 604-619 (`"the absence predicate bites on a
      planted re-insertion"`) and lines 590-603 (`"the BASIC trigger-phrase predicate bites on a
      planted description"`) — the two existing plant tests. Copy their shape, including the
      mandatory `assert.notEqual(planted, clean, "the plant anchor was not found -- this proof
      would otherwise be vacuous")` guard
    - `.planning/phases/31-procedure-re-pointing/31-PATTERNS.md` § "Analog D — the in-memory
      planted violation"
    - `.planning/phases/31-procedure-re-pointing/31-RESEARCH.md` § "Pattern 4: Non-vacuity, five
      ways" and § "Finding 2" / § "Finding 3" (the ABS-03 instrument pair and its measured
      output line)
  </read_first>

  <action>
Add ONE more test to `src/mcp/vice/skill-attribution.test.ts`, then take and record the ABS-03
measurement. No other file changes.

The plant test — name it so its job is unambiguous, e.g. `"the naming-line predicate bites on a
planted one-character mutation"`:
  - Read a real absorbed playbook's text through the file's existing destination reader (the
    same route the existing plant test uses via `ABSORBED_FILES[0]`), take its first block with
    `attributionBlocks()`, and assert the clean block already scores one of each line — if it
    does not, the proof below would be vacuous and must fail loudly.
  - Build the plant IN MEMORY by capitalising exactly one character of the adapted line inside
    that block string, so the planted line differs from the constant by a single byte and by
    nothing else. Guard the plant with the house-style
    `assert.notEqual(planted, clean, "the plant anchor was not found -- this proof would
    otherwise be vacuous")`.
  - Assert `namingLineCountsIn(planted)` reports `adapted: 0` while `repository` is unchanged at
    `1` — i.e. the predicate detects a one-byte drift AND the two counts become unequal, which
    is exactly the relation task 1's test compares. A `includes()`-style or case-folding
    predicate would pass this plant, so this test is what proves the byte-exactness is real.
  - Do a second, mirror plant on the `Source repository:` line: strip one of its two leading
    spaces and assert `repository: 0` with `adapted` unchanged. Indentation is part of the
    string and this is the assertion that says so.
  - Nothing is written under `src/skills/` or `installer/skills/`. A plant-and-revert against
    the real tree would leave the working tree dirty between runs, and this file's header
    records that reasoning already.
  - Also assert the predicate does NOT fire on innocent text (mirroring the existing
    forbidden-claim test's final `deepEqual`), so "no offenders" in task 1 means something:
    `namingLineCountsIn("nothing to see here")` reports zero for both.

Then, without editing anything, take the ABS-03 measurement criterion 1's second clause needs
and record it verbatim in the SUMMARY: run the trigger-collision runner and the test that
drives its shared predicates. The expected shape of the runner's OK line is seven skills
scanned, twenty-one pairs compared, an observed maximum score below the threshold, an allowlist
of size zero, and CLAUDE.md's project-skills table reporting seven rows all byte-identical to
their SKILL.md. Record the actual numbers, not the expected ones. Do not touch any
`description:`, and do not touch CLAUDE.md — `routine-queue-walker`'s description was already
rewritten substantively in plan 29-09 and is final; this step scores it, it does not change it.

Commit the two tasks' work as one or two commits as the executor prefers, but the plant test
must not land in a commit without the assertion it proves.
  </action>

  <verify>
    <automated>cd src/mcp/vice && node --test skill-attribution.test.ts</automated>
    <automated>node scripts/check-skill-description-overlap.mjs</automated>
    <automated>cd src/mcp/vice && node --test skill-description-overlap.test.ts</automated>
    <automated>cd src/mcp/vice && npm run typecheck && npm run test:automated</automated>
  </verify>

  <acceptance_criteria>
    - `cd src/mcp/vice && node --test skill-attribution.test.ts` exits 0 with `# fail 0`
    - `grep -c '^test(' src/mcp/vice/skill-attribution.test.ts` → `14` (the pre-change `12` plus
      one test from task 1 and one from this task)
    - `grep -c 'the plant anchor was not found' src/mcp/vice/skill-attribution.test.ts` → at
      least `2` (the pre-existing plant guard plus the new one)
    - `node scripts/check-skill-description-overlap.mjs` exits 0. Its OK line, recorded verbatim
      in the SUMMARY, reports `7 skills scanned`, `21 pairs compared`, an observed maximum score
      strictly below the reported threshold, `allowlist size 0`, and `7 rows, all byte-identical`
    - `cd src/mcp/vice && node --test skill-description-overlap.test.ts` exits 0 with `# fail 0`,
      including its final live-execution control
    - `node scripts/check-no-regenerator2000.mjs` exits 0 and still reports
      `skill-attribution-headers 24`
    - `node scripts/check-npm-packages.mjs` exits 0 — the shipped twin tree is proven against the
      packed file list, never with `git status`
    - `cd src/mcp/vice && npm run typecheck` exits 0 and `npm run test:automated` exits 0 with
      `# fail 0`. Confirm `systemctl --user is-active vice-broker` reads `inactive` FIRST
    - `git status --porcelain src/skills installer/skills CLAUDE.md` → empty (no description, no
      skill file and no CLAUDE.md row was touched)
    - `git diff --name-only` for this plan's commits lists exactly
      `src/mcp/vice/skill-attribution.test.ts`
  </acceptance_criteria>

  <reversibility rating="reversible">Two additive test cases in one existing file; reverting them removes an assertion and restores the prior green state with no downstream consumer.</reversibility>

  <done>
The naming-line predicate is proven to bite on a one-byte mutation of each of the two lines and
proven not to fire on innocent text; `skill-attribution.test.ts` has fourteen top-level tests
and is green; the ABS-03 runner and its companion test are both green with their numbers
recorded verbatim; and no skill file, description or CLAUDE.md row was touched.
  </done>
</task>

</tasks>

<edge_coverage_accounting>
Spec-less probe fallback, §C lift. Five of the six probe-surfaced items belong to `REPOINT-03`
and are authored here; the sixth (`REPOINT-04`, category `unclassified`) stays `unresolved` and
is surfaced as a flagged assumption in `31-01-PLAN.md`. **6 surfaced = 5 authored here + 1
flagged there.** No item was dismissed and none was dropped.

| Probe | Category | Resolution | Where |
|---|---|---|---|
| Do equal/touching things merge, collide or separate? | adjacency | **resolved (explicit)** — the two naming lines are NON-adjacent in one block; nothing pins their relative position, and the corpus containing that block is the proof | `must_haves.truths` (adjacency edge) |
| What is the result for empty / single / null input? | empty | **resolved (explicit)** — a zero-file root is classified by a named predicate: skippable when shipped-and-generated, hard failure when source. Both branches asserted directly | `must_haves.truths` (empty edge) + task 1 step 2 |
| Whose definition of length/equality applies? | encoding | **resolved (explicit)** — bytes. Exact line equality including the two-space indent, one trailing `\r` tolerated; no trim, fold or normalisation. Task 2's second plant is the proof | `must_haves.truths` (encoding edge) |
| When elements compare equal, is output order specified and stable? | ordering | **resolved (explicit)** — every verdict is a multiset property; no assertion depends on walk order | `must_haves.truths` (ordering edge) |
| If interrupted or run in parallel, what is guaranteed? | concurrency | **resolved (explicit)** — nothing here is concurrent or interruptible; the assertion set is a synchronous single-process read with no spawn, socket or write. Asserted mechanically by the three negative greps in task 1's criteria | `must_haves.truths` (concurrency edge) |
| unclassified — review manually | unclassified (`REPOINT-04`) | **left `unresolved`**, surfaced as a flagged assumption. Never auto-resolved with a backstop, never dropped | `31-01-PLAN.md` § `flagged_assumptions` |
</edge_coverage_accounting>

<artifacts_this_phase_produces>
Symbols this plan creates. A source-grounding drift pass should treat every row as new rather
than as an unrecognised reference.

| Artifact | Kind | Where |
|---|---|---|
| `ABS02_ADAPTED_LINE` | new constant | `src/mcp/vice/skill-attribution.test.ts` |
| `ABS02_SOURCE_REPOSITORY_LINE` | new constant | `src/mcp/vice/skill-attribution.test.ts` |
| `SKILL_ATTRIBUTION_ROOTS` | new constant (two-element `as const` tuple) | `src/mcp/vice/skill-attribution.test.ts` |
| `SKILL_ATTRIBUTION_SOURCE_ROOT` | new constant alias | `src/mcp/vice/skill-attribution.test.ts` |
| `SKILL_ATTRIBUTION_SHIPPED_ROOT` | new constant alias | `src/mcp/vice/skill-attribution.test.ts` |
| `ABS02_BLOCKS_PER_TREE_FLOOR` | new constant (the floor, value five) | `src/mcp/vice/skill-attribution.test.ts` |
| `namingLineCountsIn(text)` | new module-private helper | `src/mcp/vice/skill-attribution.test.ts` |
| `skippableEmptyRoot(root, fileCount)` | new module-private predicate | `src/mcp/vice/skill-attribution.test.ts` |
| `"both skill trees carry the ABS-02 naming lines byte-identically, in equal numbers"` | new test | `src/mcp/vice/skill-attribution.test.ts` |
| `"the naming-line predicate bites on a planted one-character mutation"` | new test | `src/mcp/vice/skill-attribution.test.ts` |

No new file, module, env var, dependency or MCP verb. Names are indicative of intent; an
executor may adjust a name for local consistency as long as the doc comment still says what the
constant is and the acceptance criteria's greps are updated in the same edit.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `src/skills/**` and `installer/skills/**` prose -> the checker | Skill content is untrusted first-party prose. It crosses into the test as a **string** and is matched; it is never imported, required, evaluated or spawned |
| `installer/skills/**` (generated, gitignored) -> the published tarball | The copy users receive is not the copy `git ls-files` sees. A guard scoped to tracked files is structurally blind to it, which is why this test adds the second root |
| Attribution header -> upstream licence obligation | The ABS-02 block is where the MIT/Apache-2.0 attribution obligation is discharged in-file. Weakening its assertions weakens a licence obligation |

## STRIDE Threat Register

ASVS L1, `security_block_on: high`. RESEARCH § Security Domain measured the surface as
essentially nil. **No new mitigations are required; the obligation is not to weaken an existing
one.**

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-08 | Repudiation | The ABS-02 attribution chain in the **shipped** tarball | high | mitigate | The new test adds `installer/skills/` as a second root, so the copy users receive is scored directly rather than by proxy. Existing mitigations kept intact and re-run in `<verify>`: `skill-attribution.test.ts`'s six-field/digest checks, the removal gate's bidirectional block+hit pins in both trees, and `check-npm-packages.mjs` against the packed file list |
| T-31-09 | Tampering | The new assertion itself, rotting into a no-op | high | mitigate | Five-ways non-vacuity, matching this file's own doctrine: constants asserted non-empty; per-root floor; a zero-file source root is a hard failure; a one-byte plant proven to bite on each of the two lines; and the predicate proven not to fire on innocent text |
| T-31-10 | Tampering | A byte-exact claim implemented as a substring or case-folded match | medium | mitigate | Task 2's two mirror plants — a single capitalised character, and one stripped leading space — fail under an exact-equality predicate and pass under a loose one, so the predicate's strictness is itself asserted |
| T-31-11 | Elevation of privilege | Untrusted prose executed by a checker | medium | mitigate | Read-and-match only. Enforced mechanically by two comment-filtered negative greps in task 1's acceptance criteria, each matching an **executable form** rather than any occurrence: a `node:child_process` import specifier or a `spawn`/`spawnSync`/`execFile`/`execFileSync`/`eval`/`require`/dynamic-`import` call site, and an `import` statement naming either path seam. Both are measured clean on the unmodified file and carry a planted positive control, so the mitigation claim is checkable rather than asserted — an any-occurrence grep would instead have scored this file's own header prohibitions (lines 70-71, 73) as the violation |
| T-31-12 | Tampering | A working tree left dirty by a plant-and-revert | low | mitigate | Every plant is an in-memory string. Asserted by the negative greps for `writeFileSync`, `mkdirSync`, `rmSync`, `renameSync` and by a `git status --porcelain` criterion over both skill trees |
| T-31-13 | Denial of service | A census pin that reds a correct tree | medium | accept | Mitigated by design (relations plus a floor, never a literal total) rather than by a control. Residual risk accepted: the per-tree floor of five would need lowering if a procedure were ever *removed*, which no requirement contemplates |
| T-31-SC | Tampering | npm / pip / cargo installs | low | accept | **No package-manager install task exists in this phase.** RESEARCH § "Package Legitimacy Audit" records the gate as not applicable: every tool is a Node built-in or a first-party committed file. No `[ASSUMED]`, `[SUS]` or `[SLOP]` verdict exists, so no legitimacy checkpoint is owed |
</threat_model>

<verification>
Run from the repository root unless a command says otherwise.

1. `systemctl --user is-active vice-broker` reads `inactive`. First, always.
2. `cd src/mcp/vice && node --test skill-attribution.test.ts` — exit 0, `# fail 0`, fourteen
   top-level tests.
3. `cd src/mcp/vice && npm run typecheck` — exit 0.
4. `node scripts/check-skill-description-overlap.mjs` — exit 0; record the OK line verbatim.
5. `cd src/mcp/vice && node --test skill-description-overlap.test.ts` — exit 0, `# fail 0`.
6. `node scripts/check-no-regenerator2000.mjs` — exit 0, `skill-attribution-headers 24`.
7. `node scripts/check-npm-packages.mjs` — exit 0.
8. `cd src/mcp/vice && npm run test:automated` — exit 0, `# fail 0`. **Never** the bare
   `npm test` full glob.
9. `git status --porcelain src/skills installer/skills CLAUDE.md` — empty.
</verification>

<success_criteria>
- One new scoring test and one new plant test in `src/mcp/vice/skill-attribution.test.ts`,
  fourteen top-level tests total, green.
- Both skill trees walked; a zero-file root classified by a named predicate with both branches
  asserted; every count a relation or a floor; no literal total; no adjacency assertion.
- Byte-exactness proven by two one-byte plants, and the predicate proven not to fire on innocent
  text.
- ABS-03's runner and its companion test green, with the seven-skill / twenty-one-pair / max
  score / allowlist-zero / seven-rows numbers recorded verbatim.
- The removal gate and `check-npm-packages.mjs` still green; no skill file, description or
  CLAUDE.md row touched; `tsc --noEmit` clean; full automated suite at zero failures with the
  broker stopped.
</success_criteria>

<output>
Create `.planning/phases/31-procedure-re-pointing/31-02-SUMMARY.md` when done. It must record
the pre-change and post-change `# tests` counts, the verbatim OK line from
`check-skill-description-overlap.mjs`, the two non-consecutive line numbers of the naming lines
in `c64-program-recon/SKILL.md` (the adjacency edge, scored), and the four measured counts
(blocks / adapted / repository, per tree).
</output>
