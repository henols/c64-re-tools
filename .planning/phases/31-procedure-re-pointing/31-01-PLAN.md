---
phase: 31-procedure-re-pointing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json
autonomous: true
requirements: [REPOINT-04]
requirement_subparts:
  - "REPOINT-04 — the manifest is updated in the same commit that changes what it describes (its own third re-sync trigger)"
  - "REPOINT-04 — r2000_undo's `omit` disposition gains the criterion v0.7.0 supplies, without reversing the omission"

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "A reader following the manifest's THIRD re-sync trigger lands on modules that exist: it names `CURATED_ANNO_TOOLS` and `src/mcp/vice/anno-tools.ts`, not the pair deleted in Phase 29 (REPOINT-04)"
    - "A reader following the manifest's FIRST re-sync trigger lands on the test that actually performs the live-gated re-hash: `anno-derivation.test.ts`, with `R2000_UPSTREAM_CLONE` spelled byte-identically (REPOINT-04)"
    - "`disposition_rationale.r2000_undo` reads `\"disposition\": \"omit\"` and carries `\"requirement_id\": \"STORE-04\"`, and its justification states that the criterion ARRIVED and was discharged at the store layer by `revertTo(handle, revision)` per decision `D2` — so the omission is a decided omission, not a reversed one (REPOINT-04)"
    - "No sentence in the manifest points a future reader at a module, symbol or test file that Phase 29 deleted or renamed: `grep -c` over the four retired tokens returns 0 (REPOINT-04)"
    - "Every stale reference is re-pointed onto a surviving subject or turned into a dated past-tense fact — no justification, citation or trigger loses the decision it records (REPOINT-04)"
    - "`node --test anno-derivation.test.ts skill-attribution.test.ts` reports 0 failures after the edit: the manifest schema survives, every non-curated call still carries a justification and a line-numbered citation, and the manifest-to-surface agreement holds in BOTH directions (REPOINT-04)"
    - "The whole manifest change lands in exactly ONE commit whose `git show --stat` lists the manifest, so no intermediate commit exists in which the manifest half-describes the surface (REPOINT-04)"
  artifacts:
    - path: ".planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json"
      provides: "The re-synced upstream snapshot record — three re-sync triggers whose mechanisms name live subjects, and five disposition rationales one of which now carries STORE-04"
      contains: "STORE-04"
  key_links:
    - "manifest `disposition_rationale` -> `ANNO_TOOL_DEFINITIONS` in `src/mcp/vice/anno-tools.ts`, compared in both directions by `derivationVerdict()` in `anno-derivation.test.ts` — this is the link that makes a reversed omission fail rather than pass"
    - "manifest `procedures[].sha256` / `commit` -> the ABS-02 attribution headers, compared by `skill-attribution.test.ts` (procedure count, upstream-path SET, per-row commit and digest) — untouched by this plan and asserted still equal after it"
    - "manifest third re-sync trigger -> `CURATED_ANNO_TOOLS` in `src/mcp/vice/anno-tools.ts` — the instruction REPOINT-04 turns on, and the only link in the record with no mechanical reader"
  prohibitions:
    - "`disposition_rationale.r2000_undo.disposition` must not change from `omit`"
    - "No `anno_undo`, `undo` or `anno_revert` verb is added anywhere, and `src/mcp/vice/anno-tools.ts` is not edited"
    - "No `resync_triggers` entry is deleted — a stale mechanism is re-pointed, never dropped"
    - "No `sha256`, `commit`, `commit_date`, `upstream_version`, `licence`, `licence_copyright` or `elected_licence` value is re-computed or edited"
    - "The 40-lowercase-hex `commit` pattern is not loosened, and no integrity pin is recalculated to make a check pass"
    - "The manifest file is not moved or renamed — two tests resolve it by identical relative path"
    - "`R2000_UPSTREAM_CLONE` and `VICE_REQUIRE_R2000_UPSTREAM` keep their exact spellings"
    - "No diff-inspecting same-commit gate is built — the criteria do not ask for one and the state invariant already covers the failure mode"
    - "`src/mcp/vice/anno-derivation.test.ts` is not edited — it is this plan's verifier, not its target"
    - "No file under `src/skills/**` or `installer/skills/**` is edited"
    - "No executed-phase artifact under `.planning/phases/29-*/` is edited — those are historical records that legitimately quote retired wording"
    - "The bare `npm test` full glob is never used as a verification command"
    - "The manifest diff must not delete more lines than it adds — a retired token is re-pointed or dated, never deleted together with the fact it recorded"
---

<objective>
Re-sync `upstream-procedure-manifest.json` — Phase 19's dated upstream snapshot record — so
that no sentence in it points a reader at something Phase 29 deleted, and so that
`r2000_undo`'s `omit` disposition carries the criterion v0.7.0 supplied for it without
reversing the omission.

Purpose: this record's own third re-sync trigger is the instruction `REPOINT-04` turns on, and
that trigger's mechanism currently names `CURATED_R2000_TOOLS` and a module that no longer
exists. The manifest lives under `.planning/`, which the removal gate excludes **by prefix**
and which `docs-dangling-refs.test.ts` deliberately does not scan, so **the drift is invisible
to every gate in this repository** — it survived all of Phase 29. There is no mechanical
reader for the manifest's prose; this plan is that reader.

This is `REPOINT-04`, which after `D-01`'s narrowing is one of only two success criteria left in
Phase 31 — the re-pointing itself landed in Phase 29 plan 29-09 and must not be redone here.

Output: one commit, one file. The manifest with seven live stale references re-pointed or
dated, and `"requirement_id": "STORE-04"` added to `r2000_undo`.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-procedure-re-pointing/31-RESEARCH.md
@.planning/phases/31-procedure-re-pointing/31-PATTERNS.md
@.planning/phases/31-procedure-re-pointing/31-VALIDATION.md
</context>

<!-- planner-discipline-allow: r2000-tools -->
<!-- planner-discipline-allow: r2000-session -->
<!-- planner-discipline-allow: r2000-upstream-audit -->
<!-- planner-discipline-allow: CURATED_R2000_TOOLS -->

<tasks>

<task type="tracer">
  <name>Task 1: Re-sync the whole manifest end-to-end — one file, one commit, verified by its own two readers</name>

  <files>.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json</files>

  <read_first>
    - `.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json` (all 66 lines — the file being modified; read it whole before touching anything, the line numbers below drift)
    - `src/mcp/vice/anno-derivation.test.ts` lines 1-100 (the header's env-var prohibition and the manifest resolve), 140-225 (the disposition/justification/citation assertions and the `requirement_id` shape gate), 300-430 (`annoNameFor()`, `derivationVerdict()`, `MEASURED_OMITTED_MINIMUM`) — this is the validator whose assertions the edit must satisfy; **read it, do not edit it**
    - `src/mcp/vice/anno-tools.ts` lines 1-100 (the `CURATED_ANNO_TOOLS` derivation comment) and lines 340-370, 420-435 (the optional `base_revision` compare-and-swap validator and its argument declaration) — the two live subjects this edit re-points onto. **Read only; this file is not edited by any plan in this phase**
    - `src/mcp/vice/anno-store.ts` lines 2450-2470 (`revertTo(handle, revision)`) — the route that discharged `STORE-04`, cited by the new justification
    - `.planning/REQUIREMENTS.md` lines 36-55 (decision `D2`), line 85 (`STORE-04`), line 83 (`STORE-02`), lines 178-205 (the Future/Out-of-Scope tables) — the direction of the undo decision, from the record rather than from inference
    - `scripts/check-no-regenerator2000.mjs` lines 490-510 (the `surviving-provenance` exemption's `why` string) — the exact prose register to imitate for a dated past-tense fact. **Read only**
    - `.planning/phases/31-procedure-re-pointing/31-RESEARCH.md` § "Finding 4", § "Finding 5", § "Finding 6", § "Finding 8" (the delta table), § "Common Pitfalls" 1, 2, 5, 6
    - `.planning/phases/31-procedure-re-pointing/31-PATTERNS.md` § "`upstream-procedure-manifest.json` (config, snapshot record)" (the field shape, the key order, the prose register, the seven tokens to sweep)
  </read_first>

  <action>
Edit the manifest only. Eight deltas, all in this one task and one commit, because the record
must never exist in a committed state where half of it describes the surface.

Apply the phase's governing rule to every one of them: **a live instruction that a future
reader would follow gets RE-POINTED onto the surviving subject; a past-tense fact about a
decision gets KEPT and DATED, with the live token retired.** A diff that deletes more lines
than it adds means facts were thrown away with tokens — that is the failure, not the fix.

D1 — trigger 1's mechanism (currently line 13). The performer sentence names
`r2000-upstream-audit.test.ts`. Re-point it to `anno-derivation.test.ts`, which is the same
test renamed by plan 29-05. Leave `R2000_UPSTREAM_CLONE` byte-identical — it is a name CI
binds by, and renaming it silently converts a hard-gated check into a skipped one. Add a short
dated parenthetical recording the rename, e.g. "(renamed from the pre-Phase-29 audit test in
plan 29-05)", so the record shows the move rather than pretending the name never changed.

D2 — trigger 3's mechanism (currently line 21). This is the single most important edit in the
phase: it is the instruction `REPOINT-04` invokes, and both of its named anchors are gone.
Replace `CURATED_R2000_TOOLS` with `CURATED_ANNO_TOOLS` and `r2000-tools.ts` with
`anno-tools.ts` (spell it `src/mcp/vice/anno-tools.ts` so the pointer is unambiguous). The
sentence's meaning is unchanged — the guard is re-pointed onto the surviving surface, exactly
as plan 29-10 re-pointed the same comparison one document over in `anno-derivation.test.ts`.
Keep the "in the same commit" clause verbatim in substance: it is the clause this phase obeys.

D3 — `disposition_rationale.r2000_undo` (currently lines 31-36). Three changes to this entry
and no others:
  (a) `"disposition"` stays `"omit"`. The criterion arriving is what makes the omission a
      *decided* one; it does not reverse it. Flipping it to `"curated"` goes red at
      `anno-derivation.test.ts`'s `assert.equal(disposition === "curated", curated, ...)`
      because the live surface carries no undo route under any spelling — 19
      `ANNO_TOOL_DEFINITIONS`, and `anno_undo` / `undo` / `anno_revert` all absent.
  (b) Add `"requirement_id": "STORE-04"` in the key slot the two sibling entries use:
      after `upstream_citation`, before `sites`. Full key order for this entry becomes
      `disposition`, `justification`, `upstream_citation`, `requirement_id`, `sites`.
      (`also_required_by` exists on `r2000_toggle_splitter` only and is not added here.)
      `STORE-04` matches the optional field's shape gate `/^[A-Z][A-Z0-9]*-\d+$/`.
  (c) Rewrite `justification` to state the direction explicitly. Keep the first sentence's
      fact — `r2000_set_data_type` is idempotent over a range, so "undo the wrong conversion
      and redo it" collapses to "set it correctly". Then replace the sentence that cites the
      deleted tool registry with a dated past-tense form (that module recorded undo/redo as
      earning no place under this surface's discipline, and it was deleted in Phase 29;
      `src/mcp/vice/anno-tools.ts` carries no equivalent record, so this is a fact and not a
      route). Then add the new, load-bearing sentence: the criterion v0.7.0 supplies is
      `STORE-04`, it ARRIVED and was DISCHARGED at the store layer by whole-store
      snapshot/restore — `revertTo(handle, revision)` in `src/mcp/vice/anno-store.ts` —
      deliberately not exposed as a surface verb, per decision `D2`, which also records that
      a per-edit inverse-command undo journal is permanently Out of Scope. Close by naming
      what changed about the record: the omission is now a decided omission with a named
      criterion rather than an unexamined one. Leave `upstream_citation` and `sites`
      byte-identical.

D4 — the three other justifications that cite the deleted tool registry (currently lines 27,
33 and 47). **Do not blindly re-point these onto `anno-tools.ts`** — verified against the tree
this session, that module records none of the three things the manifest attributes to its
predecessor: no `HELD` marker, no `D18-26`, no "no criterion in this phase" note. Re-pointing
would manufacture a false citation, which is worse than the stale one. Convert each to a dated
past-tense fact naming the retired analyser's tool registry (deleted in Phase 29) as the thing
that recorded it, and state that the surviving `src/mcp/vice/anno-tools.ts` carries no
equivalent record. Line 33's is the same sentence handled under D3(c) — do it once there.

D5 — line 47's concurrency sentence. It says a mutating addition "must go through the
`r2000-session.ts` FIFO mutex". No module of that name exists. Re-point it onto the discipline
that actually applies: the Phase 28 store is a single writer, and a caller serialises against
a concurrent edit with the optional `base_revision` compare-and-swap argument validated in
`src/mcp/vice/anno-tools.ts` (declared alongside the other tool arguments; its validator
refuses a non-integer by name). Keep the fact that the upstream implementation is mutating.

D6 — the retired requirement ids and phase numbers: `SURF-03` (trigger 2's mechanism, line 17;
and `r2000_unpack_binary`'s justification, line 54), `SURF-01` (lines 27 and 39), and the
phase numbers `Phase 20/21` (line 39) and `Phase 21` (line 47). These are **facts about what
was decided at the Phase 19 pin**, not routes. Keep every one and add a short dated
parenthetical retiring the live token — e.g. that `SURF-01`/`SURF-03` are Phase 19 requirement
ids from a retired family and do not resolve against the current `REQUIREMENTS.md`, and that
the phase numbers are from a superseded roadmap. Do not delete them: a decision with its
reasoning deleted is indistinguishable from an oversight a year later, which is
`anno-derivation.test.ts`'s own stated reason for demanding justifications at all.

D7 — `r2000_toggle_splitter`'s justification (line 39) predicts a systematic over-merge bias
that cannot now occur. Add one dated sentence: `STORE-02` closed that blocker **by
construction** — ranges are stored as ranges and never merged on adjacency, so no splitter
concept is needed and none is introduced. Keep `"requirement_id": "DECOMP-01"` and
`"also_required_by": ["BUILD-02"]` exactly as they are: both ids are still live requirements,
re-mapped from v0.7.0 to v0.9.0 on 2026-08-26.

D8 — sweep and prove. After the edits, grep the manifest for each of the four retired tokens
and require zero hits, because this plan is the only reader the manifest's prose has: the
schema test asserts merely that each `mechanism` and `justification` is a non-empty string and
that each `upstream_citation` matches `/:\d+/`, so a stale sentence satisfies every assertion
in the repository. A re-point diff that touched only trigger 3 is the shape of this failure.

Keep the file valid JSON with its existing 2-space indentation and its existing single-line
`procedures` entries untouched. Commit exactly once, message
`docs(31-01): re-sync the upstream procedure manifest onto surviving subjects`, then record the
commit sha in the SUMMARY together with the `git show --stat` output.
  </action>

  <verify>
    <automated>cd src/mcp/vice && node --test anno-derivation.test.ts skill-attribution.test.ts</automated>
    <automated>node -e "JSON.parse(require('fs').readFileSync('.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json','utf8'))"</automated>
    <automated>node scripts/check-no-regenerator2000.mjs</automated>
    <automated>cd src/mcp/vice && npm run test:automated</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'r2000-tools' .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json` → `0`
    - `grep -c 'r2000-session' .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json` → `0`
    - `grep -c 'r2000-upstream-audit' .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json` → `0`
    - `grep -c 'CURATED_R2000_TOOLS' .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json` → `0`
    - `grep -c 'CURATED_ANNO_TOOLS' <manifest>` → `1` and `grep -c 'anno-tools.ts' <manifest>` → at least `2` (trigger 3's re-point plus at least one dated-fact sentence)
    - `grep -c 'anno-derivation.test.ts' <manifest>` → at least `1`, and `grep -c 'R2000_UPSTREAM_CLONE' <manifest>` → `1` (the env var name is unchanged)
    - `node -e "const m=require('./.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json'); const u=m.disposition_rationale.r2000_undo; console.log(u.disposition, u.requirement_id, Object.keys(u).join(','))"` prints `omit STORE-04 disposition,justification,upstream_citation,requirement_id,sites`
    - `grep -c 'STORE-04' .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json` → at least `1`; `grep -c 'revertTo' .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json` → at least `1`; `grep -cE '\bD2\b' .planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json` → at least `1` (word-boundary ERE, single-quoted, with **no
      backtick inside the pattern** — the earlier form embedded a backticked spelling inside a
      single-quoted shell string and would not have survived the shell; this one matches the
      quoted, backticked and bare spellings of the decision id alike). All three report `0` on
      the unmodified manifest, measured 2026-08-31 — each is false-before / true-after
    - `node -e "const m=require('./.planning/phases/19-absorbed-procedures-and-the-coverage-instrument/upstream-procedure-manifest.json'); console.log(m.resync_triggers.length, m.procedures.length, m.commit, m.upstream_version, m.licence, m.elected_licence)"` prints `3 5 493f840418f1450a342bb220c2fe3d2585dd0525 0.9.20 MIT OR Apache-2.0 MIT` — unchanged pins, unchanged trigger count
    - `git show --stat HEAD` for the commit lists exactly one changed path, the manifest, and `git show --numstat HEAD` shows insertions >= deletions (facts were re-pointed or dated, not deleted)
    - `cd src/mcp/vice && node --test anno-derivation.test.ts skill-attribution.test.ts` exits 0 and reports `# fail 0` (1 skip is expected and correct — the live-gated upstream re-hash SKIPs when `R2000_UPSTREAM_CLONE` is unset)
    - `node scripts/check-no-regenerator2000.mjs` exits 0 and still reports `skill-attribution-headers 24`
    - `cd src/mcp/vice && npm run test:automated` exits 0 with `# fail 0`. Confirm `systemctl --user is-active vice-broker` reads `inactive` FIRST — a live broker deterministically reddens the BACK-05 test and that is not a flake
    - `git status --porcelain src/skills installer/skills src/mcp/vice/anno-tools.ts src/mcp/vice/anno-derivation.test.ts` → empty (no prohibited file was touched)
  </acceptance_criteria>

  <reversibility rating="reversible">A JSON record edit in one commit with no consumer outside two tests; `git revert` restores it exactly and nothing downstream has read it in between.</reversibility>

  <done>
The manifest names no deleted module, symbol or test file; trigger 3's mechanism points at
`CURATED_ANNO_TOOLS` and `src/mcp/vice/anno-tools.ts`; `r2000_undo` reads `omit` with
`requirement_id` `STORE-04` and a justification that states the criterion arrived and was
answered at the store layer; every retired id and phase number survives as a dated fact; and
the whole change is one commit whose `git show --stat` lists the manifest alone.
  </done>
</task>

</tasks>

<flagged_assumptions>
Surfaced rather than resolved. Both are recorded so a later reader knows they were seen and
decided, not missed.

- **`REPOINT-04` / edge-probe category `unclassified` — LEFT `unresolved`, deliberately.** The
  deterministic probe engine could not classify one item against `REPOINT-04` ("unclassified —
  review manually"). It is **not** auto-resolved with a backstop and **not** dropped: this
  requirement's own edge surface is *prose in a record with no mechanical reader*, which no
  probe category (adjacency, empty, encoding, ordering, concurrency) names. The plan's answer
  is the D8 sweep — the plan itself acts as the reader — and the residual risk is that a stale
  reference of a **shape not in the four-token grep set** survives. A human reading of the
  66-line file at review time is the only coverage for that, and this bullet is the record that
  it is uncovered mechanically.
- **`STORE-04` is an inferred id, not a quoted one (RESEARCH assumption A1).** No document says
  "the criterion for `r2000_undo` is `STORE-04`" in those words. It is the strongly-indicated
  id — `D2`'s closing sentence is *"`STORE-04` is scoped to what a planted-violation test can
  actually prove"*, and `STORE-04` is the only requirement whose text contains *"an edit can be
  reverted"*. The **direction** of the decision (the omission stands) is verified three
  independent ways and is not an assumption. If the id is wrong the fix is a one-token edit, so
  the plan proceeds rather than blocking — but the justification prose must carry the reasoning
  in words, not lean on the id alone.
</flagged_assumptions>

<assumption_delta_decision>
The `workflow.assumption_delta` detector fired at `plan:pre` with a `pluralization` signal on
the term `alongside`, matched inside the ROADMAP Phase 31 Note that reads *"An earlier version
of this note forecast that it would come back alongside the ACME export oracle; the ACME export
route did return on 2026-08-31 as `anno export-asm`, but the work that rebuilt it covered that
route only, so no phase currently owns the symbol round trip's return."*

- **Noun now primary:** `the ACME export route` — **singular**, and it is a *withdrawn-then-
  partially-returned* route, not a set. Exactly one route came back (`anno export-asm`). The
  three siblings that were withdrawn with it (`export-lbl`, `import-lbl`, `gen-enums`) did
  **not** return and are unowned.
- **Decision: `no-change`.** This is a false positive for this phase's scope. The lexical hit
  is on prose whose whole purpose is to *correct a withdrawn forecast* — the sentence records
  that a predicted plural ("the round trip returns alongside the oracle") did **not** happen
  and that the noun stayed singular. No artifact in Phase 31's scope has a singular→plural
  transition: the manifest edit touches one file, `r2000_undo` is one entry, `requirement_id`
  is a single scalar (its plural sibling `also_required_by` already exists for the genuinely
  multi-valued case and is deliberately **not** added here), and the naming-line assertion in
  plan 31-02 already treats its subject as a fixed pair of two lines, not a growing set.
- **What would force a later `promote`:** a phase taking ownership of the symbol round trip's
  return, i.e. `export-lbl` / `import-lbl` shipping so that "the export route" becomes "the
  export routes". At that point `PROJECT.md`'s and `REQUIREMENTS.md`'s dated withdrawal notes
  and this ROADMAP Note all become plural-shaped and should be promoted together, in one edit.
- **Optional (not required by this plan):** an invariant test asserting that
  `disposition_rationale` entries use `requirement_id` for the single case and
  `also_required_by` for the additional ones — i.e. that no entry carries a comma-separated id
  list inside `requirement_id`. Suggested, not planned; the existing shape gate
  `/^[A-Z][A-Z0-9]*-\d+$/` already makes a comma-separated value fail, which is most of the
  value at zero cost.
</assumption_delta_decision>

<artifacts_this_phase_produces>
Symbols and keys this plan creates (so a source-grounding drift pass does not flag them as
unrecognised):

| Artifact | Kind | Where |
|---|---|---|
| `disposition_rationale.r2000_undo.requirement_id` (value `"STORE-04"`) | new JSON key on an existing entry | `.planning/phases/19-.../upstream-procedure-manifest.json` |

No new file, module, function, constant, test, env var or dependency is created by this plan.
Phase-wide new symbols are listed in `31-02-PLAN.md` (the naming-line assertion's constants and
test names); plan `31-03` creates none.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/` snapshot record -> test-tier validator | The manifest is JSON parsed by two tests. It is first-party but **unreviewed by any gate** (removal gate excludes the `.planning/` prefix; `docs-dangling-refs.test.ts` excludes `.planning/phases/**`), so its prose crosses into a reader's hands with no mechanical check |
| Manifest disposition record -> live MCP surface | The only bidirectional mechanical link in the phase: `derivationVerdict()` compares dispositions against `ANNO_TOOL_DEFINITIONS`. A record edit that disagrees with the surface must fail, not pass |
| Upstream integrity pins -> absorbed skill prose | Five `sha256` values and one 40-hex `commit` are compared by string equality against the ABS-02 headers. This plan must not move either side |

## STRIDE Threat Register

ASVS L1, `security_block_on: high`. RESEARCH § Security Domain measured the surface as
essentially nil — no runtime code path, no dependency, no network call, no input, no
credential. **No new mitigations are required; the obligation is not to weaken an existing
one.**

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Repudiation | The `r2000_undo` justification | medium | mitigate | The rewritten justification must state the criterion's arrival AND the omission's survival in the same paragraph. A justification that records only "omitted" after `STORE-04` exists is a record that cannot be distinguished from an oversight — the exact defect `anno-derivation.test.ts` demands justifications against. Acceptance criteria assert `STORE-04`, `revertTo` and `D2` all appear |
| T-31-02 | Tampering | `disposition_rationale.r2000_undo.disposition` | high | mitigate | Flipping `omit` -> `curated` is a quietly reversed decision. Mechanically caught by `anno-derivation.test.ts`'s `assert.equal(disposition === "curated", curated, ...)` and by `derivationVerdict()`'s omit-spelling scan (all four omit verbs, two spellings each, plus `MEASURED_OMITTED_MINIMUM = 4`). Both are in this task's `<verify>`; a prohibition forbids the flip outright |
| T-31-03 | Tampering | The five `sha256` pins and the 40-hex `commit` | high | mitigate | Pins are committed constants compared by string equality against the ABS-02 headers. Prohibitions forbid re-computing any pin and forbid loosening the commit pattern back to a 7-40 hex form (plan 19-01 demonstrated that abbreviation failure deliberately). An acceptance criterion prints the pins and asserts they are unchanged |
| T-31-04 | Repudiation | A retired requirement id or phase number deleted rather than dated | medium | mitigate | D6/D7 keep every retired id and add a dated parenthetical. The `git show --numstat` insertions >= deletions criterion is the mechanical shape of "facts were not thrown away with tokens" |
| T-31-05 | Tampering | `R2000_UPSTREAM_CLONE` / `VICE_REQUIRE_R2000_UPSTREAM` | high | mitigate | An env var is a name CI binds by; renaming one silently converts a hard-gated check into a skipped one. Prohibited outright, and an acceptance criterion asserts the clone var still appears exactly once in the manifest |
| T-31-06 | Information disclosure | Manifest content | low | accept | The record contains a public upstream URL, a public commit sha, public licence strings and digests of public files. Nothing secret exists to disclose |
| T-31-07 | Elevation of privilege | Untrusted prose executed by a checker | low | accept | No plan in this phase executes, imports or spawns anything under `src/skills/`; this plan executes nothing at all. `anno-derivation.test.ts` reads the manifest with `JSON.parse`, never `eval` |
| T-31-SC | Tampering | npm / pip / cargo installs | low | accept | **No package-manager install task exists in this phase.** RESEARCH § "Package Legitimacy Audit" records the gate as not applicable: every tool used is a Node built-in or a first-party file already committed. No `[ASSUMED]`, `[SUS]` or `[SLOP]` verdict exists, so no legitimacy checkpoint is owed |
</threat_model>

<verification>
Run from the repository root unless a command says otherwise.

1. `systemctl --user is-active vice-broker` reads `inactive`. Do this **first**; a live broker
   deterministically reddens the BACK-05 test and no suite result is trustworthy until it is
   stopped.
2. `node -e "JSON.parse(...)"` on the manifest — still valid JSON.
3. `cd src/mcp/vice && node --test anno-derivation.test.ts skill-attribution.test.ts` — exit 0,
   `# fail 0`, one expected skip.
4. `node scripts/check-no-regenerator2000.mjs` — exit 0, `skill-attribution-headers 24`.
5. `cd src/mcp/vice && npm run typecheck` — exit 0 (no TypeScript changed, so this must be a
   no-op; a red here means something outside this plan's scope was touched).
6. `cd src/mcp/vice && npm run test:automated` — exit 0, `# fail 0`. **Never** the bare
   `npm test` full glob: it blocks forever on `vice-proxy.test.ts`.
7. The four zero-hit greps in `<acceptance_criteria>`.
8. `git show --stat` and `git show --numstat` on the single commit.
</verification>

<success_criteria>
- The manifest carries zero hits for all four retired tokens and at least one live pointer at
  `CURATED_ANNO_TOOLS`, `src/mcp/vice/anno-tools.ts` and `anno-derivation.test.ts`.
- `r2000_undo` reads `omit` + `requirement_id: STORE-04`, with the five keys in the documented
  order, and a justification naming `STORE-04`, `revertTo` and `D2`.
- Every retired requirement id and phase number is still present, each with a dated
  parenthetical; `git show --numstat` shows insertions >= deletions.
- All three pinned integrity values, the trigger count (3) and the procedure count (5) are
  unchanged.
- `anno-derivation.test.ts`, `skill-attribution.test.ts`, the removal gate and
  `npm run test:automated` are all green with the broker stopped.
- Exactly one commit, listing exactly the manifest.
</success_criteria>

<output>
Create `.planning/phases/31-procedure-re-pointing/31-01-SUMMARY.md` when done. It must record
the commit sha, the verbatim `git show --stat` output, the four zero-hit grep results, and the
`# fail 0` lines from the two test runs.
</output>
