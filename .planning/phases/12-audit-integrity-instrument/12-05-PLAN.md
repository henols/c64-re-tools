---
phase: 12-audit-integrity-instrument
plan: 05
type: execute
wave: 5
depends_on: ["12-04"]
worktree: false
gap_closure: true
files_modified:
  - scripts/audit-gate.mjs
  - .claude/mcp/vice/audit-integrity.test.ts
  - .planning/phases/12-audit-integrity-instrument/12-GATE-PROOF.md
autonomous: true
requirements: [GATE-01]

estimate:
  tokens: 82000
  raw_tokens: 82000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The `--hook` Bash/text detection path is bounded: no input length or shape makes target detection super-linear, so an ordinary large `sed -i`/`perl -i` Bash call cannot hang the live PreToolUse hook (closes CR-01; restores plan 12-02's D-12-14 scoped truth)"
    - "A single-line Bash append of a gated status into a milestone audit — `echo`, `printf`, `tee -a`, and a one-line `sed -i` substitution — is detected and refused, not only the multi-line heredoc form (closes CR-03; restores plan 12-02's D-12-04 truth)"
    - "The gated-status value set used by the new unanchored Bash scan is derived from the same single `GATED_STATUSES` definition `isGatedStatus()` uses — there is no second list of gated status words (D-12-12, D-12-01)"
    - "`status: gaps_found` is still never detected as gated by any of the new or changed scans, and an Edit that downgrades a gated status is still never blocked (D-12-13)"
    - "The file's own header and per-function comments describe the detection contract the code now actually implements — no comment claims coverage the code does not deliver, and the newly-bounded in-place window is recorded as an accepted limitation rather than left implied (D-12-15's diagnosability premise)"
    - "No relaxation hatch was introduced by any of these fixes: no waiver file, no environment-variable read, no skip flag (D-12-14)"
  artifacts:
    - path: "scripts/audit-gate.mjs"
      provides: "Bounded, non-backtracking milestone-audit token locator plus an unanchored Bash gated-status scan replacing the two super-linear regexes and the line-anchored Bash status check"
      contains: "auditTokenOffsets"
    - path: ".claude/mcp/vice/audit-integrity.test.ts"
      provides: "Committed reproductions of CR-01 (measured wall-clock ceiling through the real --hook CLI) and CR-03 (the four single-line Bash write shapes)"
      contains: "--hook"
    - path: ".planning/phases/12-audit-integrity-instrument/12-GATE-PROOF.md"
      provides: "An appended note recording that the Bash detection contract the transcript describes was amended by gap closure, so the artifact does not keep asserting a superseded limitation shape"
      contains: "Detection contract amended by gap closure"
  key_links:
    - from: ".claude/mcp/vice/audit-integrity.test.ts"
      to: "scripts/audit-gate.mjs --hook"
      via: "spawnSync with the payload built as a JS string and written to the child's stdin — never assembled as a real shell command"
      pattern: "--hook"
    - from: "the new unanchored Bash gated-status scan"
      to: "GATED_STATUSES / isGatedStatus()"
      via: "the scan's alternation is built from the same frozen value set, at module load"
      pattern: "GATED_STATUSES"
  prohibitions:
    - "Must not introduce any relaxation hatch as a remedy for the denial-of-service or the crash — no waiver file, no environment variable, no `--allow`/`--skip` flag, no 'bail out and exit 0 if this is taking too long' branch. D-12-14 is the premise of the whole phase; a hatch added while fixing a hang reproduces exactly the failure mode 4f048bb demonstrated."
    - "Must not convert the fail-closed contract into fail-open to make a defect disappear. An in-scope call whose evaluation fails internally must still refuse (exit 2); silently exiting 0 to avoid a crash would turn a loud bug into an invisible one."
    - "Must not widen the interceptor beyond the milestone-audit gated-status write it was built to police, and must never obstruct honest bad news: a write declaring `status: gaps_found`, or an Edit downgrading a gated status to a non-gated one, must pass. A control that starts refusing writes it was not built to refuse gets deleted, and then it gates nothing."
    - "Must not make the red reproduction go away by weakening or deleting an assertion. Each of the three defects is closed by changing `scripts/audit-gate.mjs`, never by relaxing the test that proves the defect."
---

<objective>
Close CR-01 (catastrophic regex backtracking, a live denial of service against
every Bash tool call in this repo) and CR-03 (the single-line Bash append
bypass) in `scripts/audit-gate.mjs`'s `--hook` detection path, and make the
file's own comments true again.

Purpose: plan 12-02 claimed two must-have truths its code does not deliver —
"the hook detects a gated status written through Bash: heredoc, append and tee
shapes" (D-12-04) and "a bug in the gate cannot brick unrelated Write/Edit/Bash
calls" (D-12-14, scoped). `12-VERIFICATION.md` independently reproduced both
failures. The hook is wired live to `Write|Edit|Bash` in the committed
`.claude/settings.json`, so CR-01 is not a latent risk — every Bash call in
every session in this repo already runs the super-linear regex.

Output: a bounded, non-backtracking target locator shared by all three text-scan
call sites; an unanchored gated-status scan for Bash command text derived from
the one `GATED_STATUSES` definition; committed reproductions of both defects in
`audit-integrity.test.ts`; reconciled header comments; and a note appended to
`12-GATE-PROOF.md` so the recorded transcript stops describing a superseded
detection contract.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-audit-integrity-instrument/12-CONTEXT.md
@.planning/phases/12-audit-integrity-instrument/12-VERIFICATION.md
@.planning/phases/12-audit-integrity-instrument/12-REVIEW.md
@.planning/phases/12-audit-integrity-instrument/12-02-SUMMARY.md
</context>

<measured_starting_state>
Measured on this tree on 2026-08-21, immediately before writing this plan. Do
not assume any of it still holds — re-measure at execution time — but do not
plan against the opposite of it either.

- `node scripts/audit-gate.mjs` returns `audit-gate: OK -- 4 docs guards green,
  6 milestone audits scanned, 4 declaring a gated status`, exit 0. The real tree
  is **green right now** (`docs-review-disposition.test.ts` passes: 12-VERIFICATION.md
  itself is one of the five disposition sources that guard recognises, and it
  names CR-01/CR-02/CR-03/WR-01..WR-04). `12-VERIFICATION.md`'s description of a
  red real tree is stale.
- `cd .claude/mcp/vice && node --test audit-integrity.test.ts` → 27 tests,
  27 pass, 0 fail, ~4.2 s.
- Consequence for acceptance criteria: **never** write a criterion that expects
  the real tree to refuse. Every refusal assertion goes through a synthetic
  `--root` tree (`buildSyntheticTree()` in the test file, D-12-14's only
  sanctioned testability route — there is deliberately no waiver file and no
  env var).
- Reproduced pre-fix timings, measured by importing the committed script and
  calling its own exported `extractHookTarget`/`isHookInScope`:
  Bash `sed -i ` + 100,000 filler chars = **7,050 ms**; an unrecognised-shape
  `tool_input` carrying one 100,000-char string leaf = **5,353 ms**. Both grow
  quadratically. The prototype design in Task 1 measured **≤ 1 ms** on the same
  two inputs and **4.7 ms** on a 10 MiB non-matching string.
- Reproduced CR-03 pre-fix: `isHookInScope("Bash", {command}, extraction)`
  returns `false` for all four of the single-line append shapes named in Task 2,
  not just the one named in the verification report.
</measured_starting_state>

<hazards>
**You can block your own tool calls.** `scripts/audit-gate.mjs --hook` is wired
live on matcher `Write|Edit|Bash` in the committed `.claude/settings.json`. After
Task 2 lands, ANY Bash command string that carries both (a) a write indicator
adjacent to — or a `sed -i`/`perl -i` within 4096 characters before — a
`*MILESTONE-AUDIT*.md` token, and (b) a gated status token anywhere in the
command, is in scope. It refuses only when a docs guard is red, and the tree is
green as measured above, but do not rely on that.

Rules, same as the ones `audit-integrity.test.ts`'s hook-mode describe block
already carries at its head:
- Build every hook payload as a JS string or object **inside the test file** and
  hand it to the child via `spawnSync`'s `input` option. Never assemble a payload
  on a real Bash command line, never pipe one with `printf ... |`.
- Edit files with the Write and Edit tools. Never `sed -i`, never a heredoc, for
  any file in this plan.
- If you need a one-off CLI reproduction outside the test file, write the payload
  to a scratch file first with the Write tool (a scratch filename that does not
  contain `MILESTONE-AUDIT` is out of scope by construction) and redirect it into
  the gate with `<`.

**Do not spawn a nested headless `claude -p` / `claude --print` session** for any
part of this plan. It stalls the executor in this project, and nothing in this
plan needs one.
</hazards>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bounded, non-backtracking milestone-audit token locator — one end-to-end path through detection, CLI, proof suite and header (CR-01)</name>
  <reversibility rating="costly">Replaces the detection primitive that three call sites, the committed hook tests, the file header and 12-GATE-PROOF.md's accepted-limitation prose all describe; undoing it means re-editing all four.</reversibility>
  <files>scripts/audit-gate.mjs, .claude/mcp/vice/audit-integrity.test.ts</files>
  <read_first>
    - `scripts/audit-gate.mjs` in full, and in particular: the WHAT NOT TO DO and HOOK MODE header blocks (`:16-71`), `MILESTONE_AUDIT_TOKEN_RE` (`:483-487`), the two Bash regexes and `bashTargetsMilestoneAudit()` (`:513-537`), `isHookInScope()` (`:539-558`), `rawTextIndicatesScope()` (`:560-575`). Line numbers are from the pre-fix file and will drift as you edit — treat a mismatch as drift, re-locate by identifier.
    - `.claude/mcp/vice/audit-integrity.test.ts` in full. It pins the contract this fix must not break: 27 tests, and specifically the hook-mode describe block's `runHook()` helper, `buildSyntheticTree()`, `RED_GUARD_NAME`, `MILESTONE_AUDIT_REL_PATH`, and the CRITICAL HAZARD comment above the describe block that forbids assembling payloads on a real shell command line.
    - `.planning/phases/12-audit-integrity-instrument/12-REVIEW.md` § CR-01 — the reviewer's measured backtracking table and the two fix shapes they proposed.
    - `.planning/phases/12-audit-integrity-instrument/12-VERIFICATION.md` gap 2 — the `missing:` list this task closes.
  </read_first>
  <behavior>
    Through the real `--hook` CLI, payloads built inside the test file:
    - A `Bash` payload whose `command` is `sed -i ` followed by 100,000 filler characters and no milestone-audit token: exit 0, and the whole `runHook` subprocess round trip completes in under 2000 ms wall clock. Pre-fix the regex evaluation alone measured 7,050 ms.
    - A `Write` payload whose `tool_input` carries none of the recognised keys and one 100,000-character string leaf with no milestone-audit token: exit 0, under 2000 ms. Pre-fix measured 5,353 ms.
    - Precision unchanged on the shapes already pinned: `echo x | tee -a <audit>` and `dd of=<audit>` and `> <audit>` still detected as targeting the audit; `grep 'status: passed' <audit> > /tmp/out` still NOT detected (its redirect target is not the audit file); the existing heredoc test still exits 2 against a red synthetic tree.
    - A `sed -i` whose script argument is ~3,000 characters and whose target is the audit file is still detected (the bounded in-place window is 4096, not a few hundred).
  </behavior>
  <action>
Replace the two super-linear Bash regexes and the whole-text token regex with one
bounded locator, then wire all three call sites to it. Keep every exported name
in the file exactly as it is (`docsGuardFiles`, `runGuardsLive`, `frontmatterStatus`,
`isGatedStatus`, `milestoneAuditFiles`, `checkAuditGate`, `writtenDeclaresGatedStatus`,
`extractHookTarget`, `isHookInScope`, `DOCS_GUARD_FLOOR`, `EXPECTED_DOCS_GUARD_NAMES`) —
`.claude/settings.json` and `audit-integrity.test.ts` are the only consumers and
both drive the CLI, but plan 12-02's header contract says these names must not be
renamed or re-derived.

Add, near the existing Bash-detection block:

- `const AUDIT_TOKEN = "MILESTONE-AUDIT";` — the literal used with
  `String.prototype.indexOf`, which is linear and cannot backtrack. This literal
  already exists three times in the file as part of larger regex sources; this
  becomes the one place it is defined for scanning purposes.
- `const MAX_AUDIT_TOKEN_SCANS = 64;` — the cap on how many token occurrences a
  single text is examined at. Bounds the worst case to a constant number of
  bounded-window regex evaluations.
- `const AUDIT_TOKEN_TAIL_WINDOW = 256;` and `AUDIT_TOKEN_TAIL_RE`, a
  start-anchored pattern matching a run of characters that are not whitespace,
  quote, or angle bracket, ending in a literal `.md`. Applied ONLY to the 256
  characters following an occurrence, so its input is bounded by construction.
- `const BASH_WRITER_WINDOW = 512;` and `BASH_WRITER_TAIL_RE`, an END-anchored
  pattern for the redirect/writer adjacency the existing `BASH_ADJACENT_WRITE_RE`
  expresses — the same alternation of one-or-two `>`, `tee` with optional `-a`,
  and `dd of=`, followed by optional whitespace, an optional quote and a run of
  non-whitespace non-quote characters, anchored at end of string. Applied ONLY to
  the 512 characters preceding an occurrence.
- `const BASH_INPLACE_WINDOW = 4096;` and `BASH_INPLACE_PRESENCE_RE`, matching a
  start-of-string-or-whitespace boundary, then `sed` or `perl`, then whitespace,
  then `-i`. No trailing wildcard, no bridge to the target: the target is located
  separately. Applied ONLY to the 4096 characters preceding an occurrence.
- `function auditTokenOffsets(text)` — a loop over `text.indexOf(AUDIT_TOKEN, i)`
  collecting at most `MAX_AUDIT_TOKEN_SCANS` offsets. No regex, no recursion.
- `function textNamesMilestoneAudit(text)` — true when any offset's 256-character
  right window matches `AUDIT_TOKEN_TAIL_RE`. This replaces
  `MILESTONE_AUDIT_TOKEN_RE` entirely.

Rewrite `bashTargetsMilestoneAudit(command)` to iterate `auditTokenOffsets`, skip
any offset whose right window is not a `.md`-terminated token, and return true on
the first offset whose left window satisfies either `BASH_WRITER_TAIL_RE` (512
chars) or `BASH_INPLACE_PRESENCE_RE` (4096 chars). Delete `BASH_ADJACENT_WRITE_RE`
and `BASH_INPLACE_EDIT_RE`; nothing else may reference them.
<!-- planner-discipline-allow: BASH_INPLACE_EDIT_RE -->
<!-- planner-discipline-allow: MILESTONE_AUDIT_TOKEN_RE -->

Repoint the two remaining users of the deleted whole-text regex at
`textNamesMilestoneAudit`: `isHookInScope()`'s unrecognised-shape branch, and
`rawTextIndicatesScope()`. Behaviour there is otherwise unchanged in this task.

Why this shape rather than the reviewer's "cap the scanned window with
`.slice(0, 4096)`" suggestion: a global input cap is a bypass — put the write
past character 4096 and the gate never sees it. Locating the literal first with
`indexOf` and then examining bounded windows around each hit keeps full-length
coverage of the command while giving every regex a bounded input. State that
reasoning in the code comment; a future maintainer reading only the reviewer's
note would otherwise "simplify" this back into a global cap.

Record the one precision trade this makes, as an accepted limitation in the
comment on `bashTargetsMilestoneAudit` alongside the existing T-12-02 paragraph:
an in-place edit whose script argument exceeds 4096 characters before its target
filename is no longer detected. The pre-fix regex bridged that distance without
bound, which is exactly what made it super-linear. Measured: a 3,000-character
`sed -i` script is still detected; realistic in-place edits are far below the
window.

Then add the two ceiling tests described in `<behavior>` to the hook-mode
describe block of `audit-integrity.test.ts`, measuring around the existing
`runHook()` call with `Date.now()` or `process.hrtime.bigint()`. Name them so the
defect they close is greppable, e.g. "hook mode: a large benign `sed -i` command
is evaluated in bounded time (CR-01, regex backtracking)" and "hook mode: a large
unrecognised-shape payload is evaluated in bounded time (CR-01)". Build the filler
with `"x".repeat(100000)` inside the test file. Assert exit 0 AND the elapsed
ceiling in the same test, so a regression that makes it slow again fails loudly
rather than merely slowly.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice &amp;&amp; node --test audit-integrity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd .claude/mcp/vice && node --test audit-integrity.test.ts` reports `# fail 0` and a test count of at least 29 (the pre-existing 27 plus the two new ceiling tests).
    - The two new test names appear in that run's TAP output and both pass.
    - Fail-first proof, recorded in the SUMMARY: with the new bounded locator reverted (or with the new tests run against `git stash`-ed source), the `sed -i` ceiling test fails by exceeding 2000 ms — expect roughly 7 s of wall clock for the 100,000-char payload, per the pre-fix measurement. Do not skip this step; a ceiling test that was never seen red proves nothing.
    - `grep -v '^\s*//' scripts/audit-gate.mjs | grep -c 'BASH_INPLACE_EDIT_RE'` outputs `0` (comment-stripped, so the header may still name the removed constant, which this repo's convention wants).
    - `grep -v '^\s*//' scripts/audit-gate.mjs | grep -c 'MILESTONE_AUDIT_TOKEN_RE'` outputs `0`.
    - `grep -c 'auditTokenOffsets' scripts/audit-gate.mjs` outputs at least `3` (the definition plus its two or more call sites).
    - `grep -v '^\s*//' scripts/audit-gate.mjs | grep -c 'process.env'` outputs `1` — the only environment read in the file remains `runGuardsLive()`'s `NODE_TEST_*` strip. No hatch was added.
    - `node scripts/audit-gate.mjs --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);console.log(j.guardFiles.length, j.allowed)})"` still prints `4` and a boolean — the check-mode JSON contract is untouched by this task.
  </acceptance_criteria>
  <done>The `--hook` Bash and unrecognised-shape detection paths evaluate in bounded time on any input length, the two ceiling tests are committed and green, both super-linear regexes are gone from the code, and the file's comment on the bounded in-place window states the accepted trade.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unanchored gated-status scan for Bash command text — close the single-line append bypass (CR-03)</name>
  <files>scripts/audit-gate.mjs, .claude/mcp/vice/audit-integrity.test.ts</files>
  <read_first>
    - `scripts/audit-gate.mjs` as left by Task 1, specifically `GATED_STATUSES`/`isGatedStatus()` (`:163-174` pre-fix), `writtenDeclaresGatedStatus()` (`:489-511` pre-fix) and `isHookInScope()`'s three branches.
    - `.claude/mcp/vice/audit-integrity.test.ts` — the heredoc test ("hook mode: a Bash heredoc writing a gated status into a milestone audit is blocked (D-12-04)"), the grep-redirect negative test, and the D-12-13 downgrade test. All three must still pass unchanged.
    - `.planning/phases/12-audit-integrity-instrument/12-REVIEW.md` § CR-03 — the reviewer's exact reproduction and their proposed fix.
    - `.planning/phases/12-audit-integrity-instrument/12-CONTEXT.md` § Blast radius — D-12-12 (both `passed` and `tech_debt` gated), D-12-13 (`gaps_found` never gated, honest bad news never obstructed).
  </read_first>
  <behavior>
    Against a synthetic tree with a red guard, through the real `--hook` CLI, payloads built inside the test file. All four of these currently exit 0 and must exit 2:
    - `Bash` with a one-line `echo` append of a gated status line redirected with two chevrons into `.planning/v9.9.9-MILESTONE-AUDIT.md`.
    - The same via a one-line `printf` append.
    - The same piped into `tee -a` on that path.
    - A one-line `sed -i` substitution that writes a gated status value into that path.
    And these must still exit 0 (no new false positives):
    - The same four shapes with `status: gaps_found` instead of a gated value (D-12-13).
    - A read-only `grep` of a gated status out of the audit file, redirected to a non-audit path (the existing pinned test).
    - A one-line append of a gated status to a path that is not a milestone audit.
    Against an all-green synthetic tree, every one of the four blocked shapes exits 0 — scope is established but the guard verdict allows.
  </behavior>
  <action>
Add a module-level unanchored gated-status scan and use it for Bash command text
and for the unrecognised-shape fallback, while leaving the line-anchored
`writtenDeclaresGatedStatus()` in place and unchanged for structured document
content.

Build the pattern from the existing `GATED_STATUSES` set at module load, not from
a second hardcoded word list — D-12-12's value set is defined in exactly one
place and must stay that way. Shape: an alternation of the set's values, preceded
by `status:`, optional whitespace and an optional single or double quote, and
preceded in turn by either start-of-string or a character that is not a letter,
digit or underscore (so `mystatus: passed` does not match), with a word boundary
at the end and the whole pattern case-insensitive. Name it
`BASH_GATED_STATUS_RE` and expose it through
`function declaresGatedStatusUnanchored(text)`.

Change `isHookInScope()`'s `Bash` branch to pair `bashTargetsMilestoneAudit(command)`
with `declaresGatedStatusUnanchored(command)` instead of
`writtenDeclaresGatedStatus(command)`. Change the unrecognised-shape branch to
accept EITHER scan on the joined leaves — line-anchored or unanchored — because a
renamed `command` field is precisely the T-12-08 scenario that fallback exists
for, and in that case the only signal available is a one-line command string.
Change `rawTextIndicatesScope()` the same way, after its existing escape decoding.

Leave the structured `Write`/`Edit` branch on the line-anchored scan. That is
deliberate and must be stated in the comment: a milestone-audit document's own
prose legitimately contains the words `status: passed` while its frontmatter
declares something else — `12-VERIFICATION.md` in this very phase directory is an
example — and the line-anchored scan is what keeps T-12-04's measured
false-positive trap closed for document content. A Bash command line is not
multi-line frontmatter and must not be scanned as if it were; that mismatched
assumption between two checks evaluated on the same string is the whole of CR-03.

Update the comment on `writtenDeclaresGatedStatus()`: it currently says it
tolerates leading whitespace "because a heredoc body inside a shell command ...
is not column-anchored", which reads as though it covers shell commands. It does
not, and that stale claim is part of the defect. Say plainly which path each scan
serves.

Then add the seven tests from `<behavior>` to the hook-mode describe block, named
so the defect is greppable (e.g. "hook mode: a one-line Bash echo append of a
gated status into a milestone audit is blocked (CR-03, D-12-04)"). Reuse
`buildSyntheticTree()`, `runHook()`, `MILESTONE_AUDIT_REL_PATH` and
`RED_GUARD_NAME`; do not add a second tree builder or a second hook spawner.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice &amp;&amp; node --test audit-integrity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd .claude/mcp/vice && node --test audit-integrity.test.ts` reports `# fail 0` and at least 36 tests.
    - Fail-first proof recorded in the SUMMARY: the four blocked-shape tests fail (exit 0 observed where exit 2 is asserted) against the pre-Task-2 source. This is the verifier's own reproduction, so it must be seen red once.
    - The three pre-existing Bash/D-12-13 tests named in `<read_first>` still pass, unmodified — confirm by `git diff` showing no edits inside their bodies.
    - `grep -c 'GATED_STATUSES' scripts/audit-gate.mjs` outputs at least `3` (the definition, `isGatedStatus`, and the new pattern construction) — proving the new scan derives from the one value set rather than a second list.
    - `grep -v '^\s*//' scripts/audit-gate.mjs | grep -c '"passed"'` outputs `1` — exactly the `GATED_STATUSES` definition line, unchanged (measured as 1 before this plan ran). A second literal spelling of a gated status value anywhere in the code means the new scan built its own word list instead of deriving from the one set, which is the D-12-12 violation this criterion exists to catch.
    - A negative control through the CLI: a payload declaring `status: gaps_found` in every one of the four Bash shapes exits 0 against a red synthetic tree (asserted by the tests, and stated in the SUMMARY as the D-12-13 check).
  </acceptance_criteria>
  <done>All four single-line Bash write shapes carrying a gated status into a milestone audit are refused with exit 2 against a red tree and allowed against a green one; `gaps_found` and the read-only grep shape are still never blocked; the gated-status value set still exists in exactly one place.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the dead Bash pathish push (WR-04) and reconcile every comment that now describes the code wrongly</name>
  <files>scripts/audit-gate.mjs, .planning/phases/12-audit-integrity-instrument/12-GATE-PROOF.md</files>
  <read_first>
    - `scripts/audit-gate.mjs` as left by Task 2 — in particular the `HOOK_PATH_KEYS` comment (`:369-373` pre-fix), `extractHookTarget()`'s Bash push (`:453-455` pre-fix), the file header's HOOK MODE paragraph (`:46-71` pre-fix) and the exported-surface list at the end of that header.
    - `.planning/phases/12-audit-integrity-instrument/12-REVIEW.md` § WR-04 — why the push is dead and what the reviewer offered as the alternative.
    - `.planning/phases/12-audit-integrity-instrument/12-GATE-PROOF.md`, the closing "The Bash-mode scan is a heuristic" paragraph — the prose whose description of the detection contract Tasks 1 and 2 superseded.
    - `.planning/phases/12-audit-integrity-instrument/12-CONTEXT.md` § D-12-15 — why the diagnosability of this file is load-bearing.
  </read_first>
  <action>
Delete the `toolName === "Bash"` push into `pathish` inside `extractHookTarget()`.
`isHookInScope()` special-cases `Bash` and returns before the generic
`pathish.some(isMilestoneAuditPath)` check ever runs, so the push is unreachable
in the only caller. Then fix the `HOOK_PATH_KEYS` comment, which currently states
that `command` is added for `Bash` specifically inside `extractHookTarget` — after
this deletion that sentence is false. Replace it with what is now true: the Bash
command string reaches scope determination through `isHookInScope()`'s own Bash
branch, not through the path list.

Then sweep the file for every remaining comment that Tasks 1 and 2 falsified, and
correct each one. At minimum:
- The header's HOOK MODE paragraph describes target extraction and the fail-open
  boundary; it must now name the bounded locator and say that no input length
  makes detection super-linear, because that property is the whole of CR-01's fix
  and a future maintainer "simplifying" the windows back into one bridging
  pattern is the regression to prevent.
- The header's WHAT NOT TO DO list gains one entry: do not reintroduce a single
  pattern that bridges a writer token to a target token across unbounded
  intervening text, and do not replace the bounded windows with a global input
  cap (a cap is a bypass — the write goes past the cap). Name CR-01 and the
  measured numbers, in the same style the `runGuardsLive()` comment already uses
  for the `NODE_TEST_*` incident: the WHY is the incident, not the rule.
- The header's exported-surface list must still match the actual exports.
- `collectStringLeaves()`'s comment explains that it joins with a real newline
  rather than `JSON.stringify` "because `JSON.stringify` escapes newlines ...
  which would defeat `writtenDeclaresGatedStatus()`'s line-anchored scan". After
  Task 2 the fallback also runs an unanchored scan; keep the newline-join reason
  (it is still correct for the line-anchored half) but stop implying the
  line-anchored scan is the only one.

Finally, append a short section to `12-GATE-PROOF.md` — with the Write or Edit
tool, never a heredoc and never `sed -i` — headed
`## Detection contract amended by gap closure` and dated. State: what the
transcript's closing paragraph described (a writer token immediately or
eventually preceding a milestone-audit token, unbounded), what replaced it (a
bounded left window of 512 characters for writer adjacency and 4096 for in-place
edits, located by a linear literal scan), why (CR-01, with the measured pre-fix
and post-fix numbers), and that T-12-02's base64/`python -c` limitation is
carried forward unchanged. Do not rewrite the original paragraph — that document
is a historical transcript; the amendment is additive and dated so a later reader
sees both the claim and its correction.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice &amp;&amp; node --test audit-integrity.test.ts &amp;&amp; node --test docs-linerefs.test.ts docs-dangling-refs.test.ts docs-deferred-ledger.test.ts docs-review-disposition.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pathish.push' scripts/audit-gate.mjs` outputs `1` (only the `HOOK_PATH_KEYS` loop pushes; the Bash push is gone).
    - `cd .claude/mcp/vice && node --test audit-integrity.test.ts` still reports `# fail 0` — deleting the dead push changed no behaviour.
    - All four docs guards still pass: the second half of the verify command reports `# fail 0`. Appending to a file under `.planning/phases/**` must not red any guard; confirm rather than assume.
    - `node scripts/audit-gate.mjs` prints `audit-gate: OK` and exits 0 on the real tree.
    - `grep -c 'CR-01' scripts/audit-gate.mjs` outputs at least `1` — the header records the incident that motivated the bounded windows, in this file's established WHY-this-exists style.
    - `grep -c 'Detection contract amended by gap closure' .planning/phases/12-audit-integrity-instrument/12-GATE-PROOF.md` outputs `1`.
    - No comment in the file asserts that a Bash command's gated status is found by a line-anchored scan: `grep -n 'line-anchored' scripts/audit-gate.mjs` output is reviewed line by line in the SUMMARY, and each surviving occurrence is shown to be about document content, not about Bash command text.
  </acceptance_criteria>
  <done>The dead push is gone, every comment in `scripts/audit-gate.mjs` describes what the code now does, the header records CR-01 as the incident that motivated the bounded windows, and `12-GATE-PROOF.md` carries a dated amendment instead of a superseded claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code tool call → `--hook` stdin | Every `Write`/`Edit`/`Bash` tool call in every session in this repo crosses here. The payload is attacker-shaped in the weak sense that it is arbitrary length and arbitrary structure, and untrusted in the strong sense that a compromised or merely confused agent controls it. |
| scanned command text → regex engine | The one place where input length and shape determine CPU time. CR-01 lives exactly here. |
| the gate's own failure → the developer's session | A hang or crash in this file degrades every tool call in the session. The blast radius of a bug here is the whole session, not the audit. |
| detection precision → the developer's trust | A control that refuses writes it was not built to refuse gets deleted, after which it gates nothing. Over-refusal is a real threat to the control, not a safe default. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-16 | Denial of Service | `bashTargetsMilestoneAudit()` regex evaluation on `tool_input.command` | critical | mitigate | Task 1: locate the target literal with `indexOf` (linear, non-backtracking), then apply only end/start-anchored patterns to fixed 512-, 4096- and 256-character windows, at most 64 occurrences per text. Measured worst case 0.46 ms at 263 KB and 4.7 ms at 10 MiB, against 7,050 ms at 100 KB pre-fix. Pinned by a committed wall-clock ceiling test that was seen red first. |
| T-12-17 | Denial of Service | the unrecognised-shape fallback and `rawTextIndicatesScope()` scanning up to the 10 MiB stdin cap | high | mitigate | Same bounded locator replaces the whole-text token regex at both sites. This instance is worse than the one the review named — measured 19.5 s at 200 KB and quadratic beyond — and is reachable from any malformed-JSON payload; it is in scope because it falsifies the same D-12-14 truth by the same mechanism. Pinned by the second ceiling test. |
| T-12-18 | Tampering | gate bypass via a single-line Bash write shape | high | mitigate | Task 2: an unanchored gated-status scan for command text, built from the one `GATED_STATUSES` set. Four bypass shapes (echo, printf, tee -a, one-line sed -i) pinned by committed tests, each seen red first. |
| T-12-19 | Tampering | base64 / `python -c` / runtime-assembled write target or status text | medium | accept | Carried forward unchanged from T-12-02. Accepted because this function matches literal command TEXT and never shell semantics, and because Layer 1 (`checkAuditGate()` re-reading the committed file under `npm test` and CI, where every merge to `main` auto-publishes) catches the landed write regardless of how the shell produced it. The hook is a live deterrent, not the enforcement boundary. Stated plainly in the code comment, in `12-GATE-PROOF.md`, and in this plan rather than implied away. |
| T-12-20 | Tampering | an in-place edit whose script argument exceeds the 4096-character left window | low | accept | New, introduced by Task 1's bound. Accepted as strictly better than the alternative: the unbounded bridge it replaces is the denial of service. A 3,000-character `sed -i` script is still detected (measured); realistic in-place edits are far below the window; and Layer 1 still catches the landed write. Recorded in the code comment and in the `12-GATE-PROOF.md` amendment. |
| T-12-21 | Denial of Service | over-refusal — the control blocking writes it was not built to police | medium | mitigate | Every widening in this plan is paired with a negative control: `gaps_found` in all four Bash shapes, the read-only grep-redirect shape, a gated append to a non-audit path, and the D-12-13 downgrade Edit all asserted to exit 0. The structured `Write`/`Edit` content path keeps the line-anchored scan precisely to preserve T-12-04's measured prose false-positive defence. |
| T-12-22 | Elevation of Privilege | a relaxation hatch introduced while fixing a hang | high | mitigate | Prohibited in `must_haves.prohibitions` and gated by a comment-stripped grep asserting the file still performs exactly one environment read (`runGuardsLive()`'s `NODE_TEST_*` strip). D-12-14 has no hatch by design; a "skip if slow" branch would be one. |
| T-12-03 | Tampering / Elevation of Privilege | guard-file argv and payload handling | high | mitigate | Unchanged and re-asserted: nothing from hook stdin reaches the `node --test` argv, and no scanned text is ever `eval`'d, `import()`ed, `require()`d or shell-executed. The new locator uses `indexOf` and bounded `RegExp.test` only. |
| T-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero package installs in this plan. No `## Package Legitimacy Audit` is required because no package-manager install task exists; `node --test` and the Node standard library only. |
</threat_model>

<flagged_assumptions>
Surfaced, not resolved. Neither is authored into `must_haves`.

1. **Edge-probe row, GATE-01 — `unclassified` / `unresolved`.** The deterministic
   edge probe classified this phase's single requirement as `unclassified` and
   left it `unresolved`. Per the spec-less fallback protocol an `unclassified`
   row is never auto-resolved with a backstop marker and never silently dropped,
   so it is surfaced here as a flagged planner assumption: the probe's eight-category
   data/behaviour-shape taxonomy has no purchase on a requirement whose subject is
   an enforcement *control* rather than a data transformation, and no edge
   predicate was derived from it. Coverage in this plan comes from the reproduced
   gap set instead. One item surfaced, zero authored, one flagged — the
   no-silent-drop equality holds.

2. **The api-coverage detector's single signal is a false positive.** It fired on
   the noun `mcp` from the file path `.claude/mcp/vice/audit-integrity.test.ts`
   plus the verb `wiring`, where "wiring" means a Claude Code `PreToolUse` hook
   entry in `.claude/settings.json` and `mcp` is a directory name. I confirmed
   this by re-reading the phase scope and by grepping: nothing in this phase
   touches the `vice` MCP tool surface, no external API, SDK, or network service
   is called, and the only cross-process contracts are Node's own test runner and
   Claude Code's local hook stdin/exit-code convention. The reasoned declaration
   is written to `COVERAGE.md` in this phase directory.
</flagged_assumptions>

## Artifacts this phase produces

New or renamed symbols, files and test names introduced by this plan. Newly
created symbols are not drift.

**`scripts/audit-gate.mjs` — new module-level constants:** `AUDIT_TOKEN`,
`MAX_AUDIT_TOKEN_SCANS`, `AUDIT_TOKEN_TAIL_WINDOW`, `AUDIT_TOKEN_TAIL_RE`,
`BASH_WRITER_WINDOW`, `BASH_WRITER_TAIL_RE`, `BASH_INPLACE_WINDOW`,
`BASH_INPLACE_PRESENCE_RE`, `BASH_GATED_STATUS_RE`.

**`scripts/audit-gate.mjs` — new functions (module-private):**
`auditTokenOffsets(text)`, `textNamesMilestoneAudit(text)`,
`declaresGatedStatusUnanchored(text)`.

**`scripts/audit-gate.mjs` — removed:** `BASH_ADJACENT_WRITE_RE`,
`BASH_INPLACE_EDIT_RE`, `MILESTONE_AUDIT_TOKEN_RE`, and the `toolName === "Bash"`
push into `pathish` inside `extractHookTarget()`.

**`scripts/audit-gate.mjs` — rewritten body, same name and signature:**
`bashTargetsMilestoneAudit(command)`.

**Exports:** unchanged. No export is added, removed, or renamed by this plan.

**New test names in `.claude/mcp/vice/audit-integrity.test.ts`** (final wording
is the executor's, these are the required subjects): a bounded-time `sed -i`
ceiling test and a bounded-time unrecognised-shape ceiling test (Task 1); four
blocked single-line Bash shapes — echo append, printf append, `tee -a`, one-line
`sed -i` — plus their `gaps_found` negative controls and a gated append to a
non-audit path (Task 2).

**New file paths:** none. `COVERAGE.md` in this phase directory is written by the
planner, not by this plan.

**New CLI flags:** none. `--root`, `--json` and `--hook` are the complete set and
stay that way (D-12-14).

<verification>
1. `cd .claude/mcp/vice && node --test audit-integrity.test.ts` — `# fail 0`, at
   least 36 tests.
2. `cd .claude/mcp/vice && node --test docs-linerefs.test.ts docs-dangling-refs.test.ts docs-deferred-ledger.test.ts docs-review-disposition.test.ts` — `# fail 0`.
3. `node scripts/audit-gate.mjs` — `audit-gate: OK`, exit 0.
4. `cd .claude/mcp/vice && npm run typecheck` — clean. The script is `.mjs` and
   untypechecked, but the test file is TypeScript and is not.
5. Each of the three defect reproductions was seen red before its fix and green
   after, with the transition recorded in the SUMMARY.
</verification>

<success_criteria>
- CR-01 closed: no input length or shape makes `--hook` target detection
  super-linear; proven by two committed ceiling tests that were seen red first.
- CR-03 closed: all four single-line Bash write shapes carrying a gated status
  into a milestone audit are refused; proven by committed tests that were seen
  red first.
- WR-04 closed: the dead push is gone and the comment that described it is
  corrected.
- D-12-12/D-12-13 preserved: one gated-status value set, `gaps_found` never
  gated, honest downgrades never blocked, each asserted by a negative control.
- D-12-14 preserved: no waiver file, no environment-variable hatch, no skip flag;
  exactly one environment read remains in the file.
- Every comment in `scripts/audit-gate.mjs` describes the code as it now is.
</success_criteria>

<output>
Create `.planning/phases/12-audit-integrity-instrument/12-05-SUMMARY.md` when
done. It must name CR-01, CR-03 and WR-04 explicitly (those identifiers are how
`docs-review-disposition.test.ts` recognises a Phase 12 finding as dispositioned),
record the red-then-green transition for each of the three reproductions with the
measured numbers, and state T-12-19 and T-12-20 as accepted limitations rather
than implying the hook is now complete.
</output>
