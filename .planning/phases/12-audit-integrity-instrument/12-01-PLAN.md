---
phase: 12-audit-integrity-instrument
plan: 01
type: execute
wave: 1
depends_on: []
worktree: false
files_modified:
  - scripts/audit-gate.mjs
  - .claude/mcp/vice/audit-integrity.test.ts
autonomous: true
requirements: [GATE-01]

must_haves:
  truths:
    - "A single committed executable script, `scripts/audit-gate.mjs`, answers both 'is any docs guard red right now?' and 'does this text declare a gated status?' — the logic exists in exactly one place (D-12-01)"
    - "The guard set is derived from the tree by globbing `docs-*.test.ts` in `.claude/mcp/vice/`, never hand-listed (D-12-07), and carries a `>= 4` floor plus an assertion that the four current names are present (D-12-08)"
    - "The guards are re-run live in a subprocess on every invocation, never read from a recorded artifact (D-12-10), and are invoked as the guard files directly — never `npm test` / `npm run test:automated` (D-12-11)"
    - "`status: passed` and `status: tech_debt` are both gated (D-12-12); `status: gaps_found` is never gated (D-12-13)"
    - "There is no waiver file and no env var that relaxes the gate; testability comes from a `--root` directory parameter instead (D-12-14)"
    - "A refusal names which guard is red, quotes its failing assertion text, and states the two legitimate routes — fix the documents, or change/retire the guard in a commit (D-12-15)"
    - "A committed planted-violation / planted-false-negative pair proves the gate bites on a deliberately-red synthetic tree and clears on a green one (D-12-16)"
    - "The gate's own test is named `audit-integrity.test.ts`, outside the `docs-*` glob, so the gate can never spawn itself recursively (D-12-09)"
    - "Layer 1 runs from a clean checkout under `npm test` and CI with zero setup (D-12-02)"
  artifacts:
    - path: "scripts/audit-gate.mjs"
      provides: "The single check point — guard-set derivation, live guard run, milestone-audit discovery, frontmatter-only status parse, refusal message, CLI contract"
      contains: "4f048bb"
      min_lines: 180
    - path: ".claude/mcp/vice/audit-integrity.test.ts"
      provides: "Layer 1: clean-checkout enforcement plus the planted-violation/false-negative pair"
      min_lines: 150
  key_links:
    - from: ".claude/mcp/vice/audit-integrity.test.ts"
      to: "scripts/audit-gate.mjs"
      via: "spawnSync of the script's CLI with --root and --json"
      pattern: "audit-gate\\.mjs"
    - from: "scripts/audit-gate.mjs"
      to: ".claude/mcp/vice/docs-*.test.ts"
      via: "readdirSync-derived glob + spawnSync(node, ['--test', ...guards])"
      pattern: "docs-.*\\\\.test\\\\.ts"
---

<objective>
Build the single check point (`scripts/audit-gate.mjs`) and Layer 1
(`.claude/mcp/vice/audit-integrity.test.ts`), covering everything except the
`PreToolUse` hook mode (plan 12-02) and the settings wiring (plan 12-03).

Purpose: `4f048bb` closed v0.3.0 with `docs-review-disposition.test.ts` already
red and nothing stopped it. GATE-01 requires that be mechanically impossible,
not documented. This plan produces the code the other three plans attach to.

Output: `scripts/audit-gate.mjs` (check mode + exported directory-parameterised
functions) and `.claude/mcp/vice/audit-integrity.test.ts` (real-tree assertions
plus the committed planted pair).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-audit-integrity-instrument/12-CONTEXT.md
@.planning/phases/12-audit-integrity-instrument/12-RESEARCH.md
@.planning/phases/12-audit-integrity-instrument/12-PATTERNS.md
@.planning/phases/12-audit-integrity-instrument/12-VALIDATION.md
@CLAUDE.md
</context>

<interfaces>
The CLI and module contract every later plan and both enforcement layers depend
on. Implement exactly these names; plan 12-02 extends the same file with
`--hook` and MUST NOT rename or re-derive any of them.

Exported functions (all directory-parameterised, no globals, no env reads):

- `docsGuardFiles(viceDir)` -> `string[]` — basenames matching
  `/^docs-.*\.test\.ts$/` in `viceDir`, sorted. Non-recursive.
- `DOCS_GUARD_FLOOR` -> `4`
- `EXPECTED_DOCS_GUARD_NAMES` -> frozen array of the four current names:
  `docs-linerefs.test.ts`, `docs-dangling-refs.test.ts`,
  `docs-deferred-ledger.test.ts`, `docs-review-disposition.test.ts`
- `runGuardsLive(viceDir, files)` -> `{ status: number, stdout: string, stderr: string }`
- `frontmatterStatus(markdown)` -> `string | null`
- `isGatedStatus(value)` -> `boolean`
- `milestoneAuditFiles(planningDir)` -> `string[]` (absolute paths, sorted)
- `checkAuditGate({ viceDir, planningDir })` -> `{ allowed, redGuards, guardOutput, gatedAudits, structuralErrors, reason }`

CLI contract (`node scripts/audit-gate.mjs [--root <dir>] [--json]`):
- `--root <dir>` defaults to the repo root resolved from the script's own
  location (`dirname(fileURLToPath(import.meta.url))` joined with `..`).
- exit `0` = allowed. exit `1` = refused or structurally broken.
- human mode: one `audit-gate: OK — ...` line on stdout when allowed; an
  `audit-gate: REFUSED` block on stderr when not.
- `--json` prints exactly one JSON object on stdout (both outcomes) with keys
  `allowed`, `redGuards`, `gatedAudits`, `guardFiles`, `auditFiles`,
  `statusCounts`, `structuralErrors`, `reason`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build scripts/audit-gate.mjs — the single check point</name>
  <files>scripts/audit-gate.mjs</files>
  <read_first>
    - `.claude/mcp/vice/test-gate.mjs` — the spawn/interpret shape (`automatedTestFiles(dir)` at :82-88, `runNodeTest()` at :90-98, the `import.meta.url === \`file://${process.argv[1]}\`` direct-invocation guard at :108) and its header-comment convention
    - `scripts/check-skill-tool-coverage.mjs` — the `need()`/`errors[]` accumulate-then-report exit contract (:56-59 and the report block near :520-548) and the enumerated "Resolve by: (1)... (2)..." refusal-message style near :395-399
    - `.claude/mcp/vice/docs-review-disposition.test.ts:140-190` — `topLevelMilestoneAuditFiles()` and `extractTechDebtBlock()`; the column-zero line-scan technique and the header comment explaining why a `/m`-anchored lookahead regex truncated to 43 characters on this exact document shape
    - `.claude/mcp/vice/hostpath-consumers.test.ts:182-197` — the derived-set-plus-floor idiom (`R2000_MODULE_FLOOR`)
    - `.planning/milestones/v0.3.0-MILESTONE-AUDIT.md` and `.planning/milestones/v0.2.0-MILESTONE-AUDIT.md` — the real frontmatter shape and the prose `status:` occurrences that a whole-document scan would false-positive on
    - `.planning/phases/12-audit-integrity-instrument/12-RESEARCH.md` § Common Pitfalls (Pitfall 1) and § Security Domain
  </read_first>
  <action>
Create `scripts/audit-gate.mjs` with a `#!/usr/bin/env node` shebang and a long
structured header comment in this repo's house voice: a `WHY THIS FILE EXISTS:`
paragraph naming `4f048bb` (the v0.3.0 close that recorded `status: passed`
while `docs-review-disposition.test.ts` was already red) and GATE-01; a
`WHAT NOT TO DO:` paragraph stating that this is the ONE place the gate's logic
lives per D-12-01 — no second hand-typed guard list, no waiver file, no env-var
hatch (D-12-14), and no reachability into `npm test` / `npm run test:automated`
(D-12-11, which also keeps this phase from silently settling the deliberately-
unfolded todo `2026-08-13-reconcile-ci-test-command-with-narrowed-gate.md`); and
a `SCOPE FENCE:` paragraph stating that it gates only the milestone audit's
frontmatter status line, never phase `VERIFICATION.md`, never
`/gsd-complete-milestone`, and never `status: gaps_found`.

Import only `node:fs`, `node:path`, `node:url`, `node:child_process`. No new
dependency. Never `eval`, `import()`, `require()` or shell-execute any scanned
text (ASVS V5, threat T-12-03).

Implement the exported names exactly as given in this plan's `<interfaces>`
block:

`docsGuardFiles(viceDir)` — `readdirSync(viceDir)` filtered by
`/^docs-.*\.test\.ts$/`, `.sort()`. Non-recursive by construction. D-12-07.

`DOCS_GUARD_FLOOR = 4` and a frozen `EXPECTED_DOCS_GUARD_NAMES` array holding
the four current guard basenames. D-12-08.

`runGuardsLive(viceDir, files)` — `spawnSync(process.execPath, ["--test",
...files], { cwd: viceDir, encoding: "utf8" })`. Argv array, never a shell
string. Return `{ status: result.status ?? 1, stdout: result.stdout ?? "",
stderr: result.stderr ?? "" }`; if `result.error` is set, return status 1 with
the error message folded into `stderr`. Diverge deliberately from
`test-gate.mjs`'s `stdio: "inherit"` and say so in a comment: D-12-15's refusal
message needs the captured assertion text, which inherited stdio does not give
the caller.

`frontmatterStatus(markdown)` — column-zero, frontmatter-only line scan,
adapted from `extractTechDebtBlock()`. Strip a leading BOM and all `\r`. The
first line must be exactly `---`; scan forward until the next line that is
exactly `---`; within that block find the first line matching
`/^status:\s*(.*)$/`; trim the value and strip a single pair of surrounding
single or double quotes. Return `null` if there is no frontmatter block or no
`status:` key inside it. Do NOT use a whole-document regex — this is the
measured false-positive trap (9 prose `status:` occurrences in
`v0.2.0-MILESTONE-AUDIT.md`, 4 in `v0.3.0-MILESTONE-AUDIT.md`, exactly 1
frontmatter key each).

`isGatedStatus(value)` — case-insensitive compare of the trimmed value against
the frozen set `{ "passed", "tech_debt" }`. `gaps_found` and every other value
return `false`. D-12-12 / D-12-13. Add an inline comment recording that gating
`tech_debt` is deliberately stricter than GATE-01's literal wording because both
statuses route to `/gsd-complete-milestone`.

`milestoneAuditFiles(planningDir)` — recursive walk of `planningDir` returning
absolute paths whose basename matches `/MILESTONE-AUDIT/` and ends `.md`,
sorted. Recursive, not top-level-only: D-12-02 says "no `*MILESTONE-AUDIT*.md`
under `.planning/`", the live audit-milestone workflow writes
`.planning/v{version}-MILESTONE-AUDIT.md` at top level, and every closed round
is archived under `.planning/milestones/`. Skip any directory whose name starts
with `.` and skip symlinked directories (`lstatSync().isSymbolicLink()`) so the
walk cannot be redirected outside the tree.

`checkAuditGate({ viceDir, planningDir })` — accumulate `structuralErrors` via a
`need()`-style helper: guard count must be `>= DOCS_GUARD_FLOOR`, and every name
in `EXPECTED_DOCS_GUARD_NAMES` must be present in the derived list. Compute
`gatedAudits` as `[{ file, status }]` for every discovered audit whose
`frontmatterStatus` is gated. Only run the guards when there is at least one
gated audit OR the caller is in CLI mode (always run in CLI mode so `--json`
reports real guard state); derive `redGuards` from a non-zero guard exit status.
Return `allowed: structuralErrors.length === 0 && !(guardsRed && gatedAudits.length > 0)`.
Build `reason` per D-12-15 with three labelled parts: (a) which guard file(s)
are red — parse the TAP output for `not ok` lines and `# Subtest:` / file names
so the specific guard is named, falling back to the whole guard list when the
parse finds nothing; (b) the captured failing assertion text (the guard
subprocess's own stdout/stderr, truncated to a bounded length such as 4000
characters with an explicit truncation marker); (c) the two legitimate routes,
worded as an enumerated list: fix the documents the guard checks, or change or
retire the guard in a commit — and state explicitly that there is no waiver file
and no env var.

`main()` — parse `--root <dir>` and `--json` from `process.argv`. Resolve the
default root from the script's own location. Derive `viceDir` as
`<root>/.claude/mcp/vice` and `planningDir` as `<root>/.planning`. In `--json`
mode print exactly one `JSON.stringify` object on stdout and exit 0/1. In human
mode print a single `audit-gate: OK — N docs guards green, M milestone audits
scanned, K declaring a gated status` line on stdout when allowed, or an
`audit-gate: REFUSED` block on stderr with the three D-12-15 parts and one `- `
bullet per offending audit file, then exit 1. Structural errors print
`audit-gate: FAIL` with one `- ` bullet each and exit 1. Guard the whole of
`main()` behind the same `import.meta.url === \`file://${process.argv[1]}\``
check `test-gate.mjs:108` uses, so importing the module never runs it.

Do not add hook-mode handling in this task; plan 12-02 owns `--hook`.
  </action>
  <acceptance_criteria>
    - `test -x scripts/audit-gate.mjs` succeeds (file is executable) and its first line is exactly `#!/usr/bin/env node`
    - `grep -c '4f048bb' scripts/audit-gate.mjs` returns `>= 1` (the dated incident is named in the header, per this repo's header convention)
    - `node scripts/audit-gate.mjs --json` exits 0 today (all four guards are green) and its stdout parses as one JSON object
    - That JSON object reports `guardFiles` of length exactly 4 containing all of `docs-linerefs.test.ts`, `docs-dangling-refs.test.ts`, `docs-deferred-ledger.test.ts`, `docs-review-disposition.test.ts`
    - That JSON object reports `auditFiles` of length `>= 6` and `statusCounts` with `passed: 1`, `tech_debt: 3`, `gaps_found: 2` — the measured ground truth of the six committed `*MILESTONE-AUDIT*.md` files
    - That JSON object reports `gatedAudits` of length exactly 4 (the one `passed` plus the three `tech_debt`), and none of the two `gaps_found` files appears in it
    - `node -e` importing the module and calling `frontmatterStatus` on the full text of `.planning/milestones/v0.2.0-MILESTONE-AUDIT.md` returns exactly `tech_debt` — not any of the 8 other `status:` occurrences in that file
    - `grep -nE 'npm (run )?test' scripts/audit-gate.mjs | grep -v '^[0-9]*://' | wc -l` returns 0 outside comment lines — the gate never shells `npm test` or `npm run test:automated` (D-12-11)
    - `grep -niE 'waiver|AUDIT_GATE_SKIP|process\.env\.[A-Z_]*(SKIP|OVERRIDE|BYPASS|FORCE)' scripts/audit-gate.mjs` returns no match outside the header's prose stating that no such hatch exists (D-12-14)
    - `grep -nE '\beval\(|\brequire\(|\bimport\(' scripts/audit-gate.mjs` returns no match (ASVS V5 / T-12-03)
    - `spawnSync` is called with an argv array and never with `{ shell: true }`: `grep -n 'shell: *true' scripts/audit-gate.mjs` returns no match
    - `node --test .claude/mcp/vice/docs-linerefs.test.ts` still passes — this task did not disturb any guard
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/audit-gate.mjs --json | node -e "let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{const j=JSON.parse(d);if(!j.allowed)throw new Error('expected allowed');if(j.guardFiles.length!==4)throw new Error('guard floor');if(j.gatedAudits.length!==4)throw new Error('gated count '+j.gatedAudits.length);if(j.statusCounts.gaps_found!==2)throw new Error('gaps_found count');console.log('audit-gate contract OK')})"</automated>
  </verify>
  <done>`scripts/audit-gate.mjs` exists, is executable, exports the eight names in `<interfaces>`, exits 0 against the real tree today, and its `--json` output matches the measured ground truth (4 guards, 6+ audits, 4 gated, 2 `gaps_found` ungated).</done>
</task>

<task type="auto">
  <name>Task 2: Build .claude/mcp/vice/audit-integrity.test.ts — Layer 1 plus the planted pair</name>
  <files>.claude/mcp/vice/audit-integrity.test.ts</files>
  <read_first>
    - `scripts/audit-gate.mjs` — the file produced by Task 1; its CLI contract is what this test drives
    - `.claude/mcp/vice/docs-review-disposition.test.ts:280-340` — the house planted-violation / planted-false-negative pair and its naming convention
    - `.claude/mcp/vice/hostpath-consumers.test.ts:182-197` — the derived-set-plus-floor assertion shape
    - `.claude/mcp/vice/install-resources.test.ts:60-100` — the `mkdtempSync(join(tmpdir(), "..."))` synthetic-tree idiom and its cleanup
    - `.claude/mcp/vice/test-gate.test.ts:16-29` — the exact-membership `assert.deepEqual` shape for a frozen name list
    - `.claude/mcp/vice/docs-linerefs.test.ts:22-45` — the `node:test` / `node:assert/strict` / `repoRoot({ from: HERE })` header idiom
    - `.claude/mcp/vice/tsconfig.json` — `allowJs: false`, which is why this test must NOT `import` the `.mjs` script
  </read_first>
  <action>
Create `.claude/mcp/vice/audit-integrity.test.ts`. Name it exactly this — never
`docs-audit-integrity.test.ts`. A `docs-` prefix would make this file a member
of `docsGuardFiles()`'s own glob and the gate would spawn itself recursively
(D-12-09, hard correctness constraint).

Header comment in house voice: `WHY THIS EXISTS:` naming `4f048bb` and GATE-01;
`WHAT NOT TO DO:` naming the recursion hazard (never rename this file into the
`docs-` prefix) and the second-list hazard (never re-derive the guard glob here
— read it from `audit-gate.mjs`'s `--json` output); `SCOPE FENCE:` this is Layer
1, the clean-checkout backstop; it does not test the Claude Code hook runtime,
which is manual by nature (same class as `vice-sync.ts`'s deliberately-untested
checkpoint waits, CLAUDE.md § Testing).

Drive `scripts/audit-gate.mjs` as a **subprocess**, not as an import. A direct
`import` of `../../../scripts/audit-gate.mjs` from TypeScript fails
`npm run typecheck` with TS7016 ("Could not find a declaration file") because
`tsconfig.json` sets `allowJs: false` — this was measured, not assumed. Driving
the CLI instead also means the test exercises the exact contract both the hook
and a human operator use, which is what criterion 3 asks for. Add a helper
`runGate(rootDir)` that does `spawnSync(process.execPath, [GATE, "--root",
rootDir, "--json"], { encoding: "utf8" })` where `GATE` is
`join(ROOT, "scripts", "audit-gate.mjs")` and `ROOT` is `repoRoot({ from: HERE })`,
returning `{ status, json: JSON.parse(stdout), stderr }`.

Write these tests:

1. "the gate script exists and is executable" — `existsSync(GATE)` and the
   file's mode has an owner-execute bit. This is criterion 3's file-level pin:
   the check point is code, not a checklist.

2. "this test file is NOT a member of the guard set it audits (D-12-09
   anti-recursion)" — assert the real-tree `guardFiles` array from `runGate(ROOT)`
   does not contain `audit-integrity.test.ts`, and separately assert
   `basename(fileURLToPath(import.meta.url))` does not start with `docs-`.

3. "the docs guard set is derived from disk with a non-vacuity floor (D-12-07 /
   D-12-08)" — from `runGate(ROOT).json`, assert `guardFiles.length >= 4` with a
   message explaining that an empty or broken glob must fail loudly here rather
   than let the gate report green forever, then `assert.deepEqual([...guardFiles].sort(),
   [the four names].sort())`.

4. "no milestone audit declares a gated status while any docs guard is red
   (D-12-02)" — `runGate(ROOT)`, assert `status === 0` and `json.allowed === true`,
   with a failure message that embeds `json.reason` verbatim so a red run tells
   the reader which guard broke and how to fix it.

5. "the frontmatter scan reads only the frontmatter, not prose (T-12-04)" —
   from the same real-tree `--json` run, assert `auditFiles.length >= 6`,
   `statusCounts.passed === 1`, `statusCounts.tech_debt === 3`,
   `statusCounts.gaps_found === 2`, and `gatedAudits.length === 4`. Comment that
   `v0.2.0-MILESTONE-AUDIT.md` contains 9 `status:` occurrences and
   `v0.3.0-MILESTONE-AUDIT.md` 4, exactly one frontmatter key each, so these
   counts are the direct proof that the scan is frontmatter-only.

6. "planted violation: a synthetic tree with a red guard and an audit declaring
   `status: passed` is refused (D-12-16)" — build the tree with `mkdtempSync`
   (see the synthetic-tree rules below), run `runGate(tmpRoot)`, assert
   `status === 1`, `json.allowed === false`, `json.redGuards.length >= 1`, and
   that `json.reason` contains all three D-12-15 parts: the offending guard's
   basename, a substring of the planted assertion's own failure text, and the
   phrase naming the change-or-retire-in-a-commit route.

7. "planted false-negative: the same synthetic audit with all guards green is
   allowed (D-12-16)" — identical tree, but the planted guard file asserts a
   truth. Assert `status === 0` and `json.allowed === true`. This is what stops a
   gate that always refuses from passing test 6.

8. "`status: gaps_found` is never gated even with a red guard (D-12-13)" —
   synthetic tree with the red guard and an audit whose frontmatter status is
   `gaps_found`. Assert `status === 0` and `json.allowed === true`. Honest bad
   news must never be obstructed.

9. "`status: tech_debt` is gated (D-12-12)" — synthetic tree with the red guard
   and an audit whose frontmatter status is `tech_debt`. Assert refusal.

10. "a prose-only occurrence of a gated status does not trigger the gate
    (T-12-04, the measured false-positive trap)" — synthetic tree with the red
    guard and an audit whose frontmatter status is `gaps_found` but whose BODY
    contains a backticked `status: passed` line quoting another document. Assert
    `status === 0` / allowed.

11. "a synthetic tree below the guard floor is a structural failure, not a
    silent pass (D-12-08)" — synthetic tree with only one planted guard file and
    a gated audit. Assert `status === 1` and that
    `json.structuralErrors.length >= 1` mentioning the floor. A derived set that
    matches almost nothing must fail loudly.

Synthetic-tree rules (all planted-tree helpers):
- Build under `mkdtempSync(join(tmpdir(), "audit-gate-planted-"))`, never inside
  the repo. Deliberately diverging from `12-PATTERNS.md`'s committed-fixture
  recommendation, and record the reason in a comment: a committed file literally
  named `docs-*.test.ts` that always fails is exactly the leak this phase must
  not create — it would be one recursive-glob or one `node --test '*.test.*'`
  change away from reding CI permanently. A `mkdtempSync` tree lives outside the
  repo entirely and is structurally incapable of joining the real `docs-*` glob.
  `mkdtempSync` is already this repo's established synthetic-tree idiom
  (`install-resources.test.ts`).
- Each tree needs `<tmp>/.claude/mcp/vice/` holding four planted
  `docs-*.test.ts` files (so the floor is satisfied) named to mirror the four
  real names, plus `<tmp>/.planning/` holding the synthetic audit.
- The planted guard bodies are two-line `node:test` files using
  `node:assert/strict`; the red one asserts a deliberate falsehood, the green
  ones assert a truth.
- Register cleanup with `rmSync(root, { recursive: true, force: true })` so no
  temp tree survives a failing assertion; use `t.after(...)` or a `try/finally`
  so cleanup runs even when the assertion throws.
  </action>
  <acceptance_criteria>
    - `cd .claude/mcp/vice && node --test audit-integrity.test.ts` exits 0 with `# fail 0`
    - That run reports `# pass 11` or more (the eleven tests above)
    - `node --test audit-integrity.test.ts -t "planted violation"` passes, and `-t "planted false-negative"` passes
    - `cd .claude/mcp/vice && npm run typecheck` exits 0 — proving the test does not `import` the `.mjs` script (TS7016 would fire if it did)
    - `grep -c 'import.*scripts/audit-gate.mjs' .claude/mcp/vice/audit-integrity.test.ts` returns 0
    - `ls .claude/mcp/vice/ | grep -c '^docs-audit-integrity'` returns 0 — D-12-09 respected
    - `node -e "const {readdirSync}=require('node:fs');const g=readdirSync('.claude/mcp/vice').filter(f=>/^docs-.*\.test\.ts$/.test(f));if(g.includes('audit-integrity.test.ts'))throw new Error('recursion');console.log('no recursion, '+g.length+' guards')"` prints `no recursion, 4 guards`
    - After the full run, `ls /tmp | grep -c '^audit-gate-planted-'` returns 0 — no synthetic tree leaked
    - `cd .claude/mcp/vice && npm test` exits 0 (full suite still green)
    - `node scripts/check-npm-packages.mjs` exits 0 — neither new file leaked into either npm tarball
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/.claude/mcp/vice && node --test audit-integrity.test.ts && npm run typecheck && cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-npm-packages.mjs</automated>
  </verify>
  <done>`audit-integrity.test.ts` exists, passes 11+ tests including the planted violation/false-negative pair, typechecks, leaves no temp trees, and keeps `check-npm-packages.mjs` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → `audit-gate.mjs` | Markdown frontmatter and guard file names read off disk; treated as untrusted text that is matched, never executed |
| `audit-gate.mjs` → `node --test` subprocess | The only place this plan spawns a process; the argv is built exclusively from `readdirSync` over a code-controlled directory |
| CLI argv → `audit-gate.mjs` | `--root` is operator-supplied and selects the scanned tree; it never selects which files count as guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering / Elevation of Privilege | `runGuardsLive()`'s `spawnSync` argv | mitigate | Guard file list comes only from `readdirSync(viceDir)` filtered by `/^docs-.*\.test\.ts$/`; never from stdin, argv, or scanned document text. `spawnSync` uses an argv array, never `{ shell: true }`. Verified by the acceptance greps for `shell: *true` and by test 3's derived-set assertion. |
| T-12-04 | Spoofing / Tampering | `frontmatterStatus()` | mitigate | Column-zero frontmatter-only line scan bounded by the first two `---` lines; BOM and `\r` stripped; surrounding quotes stripped; case-insensitive value compare. Proven by real-tree test 5 (`passed: 1`, `tech_debt: 3`, `gaps_found: 2` against 9 and 4 prose occurrences) and synthetic test 10. |
| T-12-06 | Tampering | `milestoneAuditFiles()` recursive walk | mitigate | Skips dot-directories and refuses to descend symlinked directories (`lstatSync().isSymbolicLink()`), so a symlink planted under `.planning/` cannot redirect the scan outside the tree or into an unbounded loop. |
| T-12-07 | Repudiation | the derived guard glob | mitigate | `>= 4` floor plus exact-membership assertion on the four current names (D-12-08); a glob that silently matches nothing becomes a structural failure (exit 1), never a vacuous green. Test 11 proves it. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this phase; `12-RESEARCH.md` § Package Legitimacy Audit records the gate protocol as not triggered. No new dependency is added by either file. |
</threat_model>

<verification>
- `cd .claude/mcp/vice && npm test` exits 0
- `cd .claude/mcp/vice && npm run typecheck` exits 0
- `node scripts/audit-gate.mjs` exits 0 and prints one `audit-gate: OK` line
- `node scripts/check-npm-packages.mjs` exits 0
- The four existing guards remain untouched: `git diff --quiet .claude/mcp/vice/docs-linerefs.test.ts .claude/mcp/vice/docs-dangling-refs.test.ts .claude/mcp/vice/docs-deferred-ledger.test.ts .claude/mcp/vice/docs-review-disposition.test.ts`
</verification>

<success_criteria>
- `scripts/audit-gate.mjs` is the only place the gate's logic lives (D-12-01), names `4f048bb` in its header, and has no waiver file or env-var hatch (D-12-14)
- The guard set is derived (D-12-07) with a `>= 4` floor and exact-name assertion (D-12-08), re-run live in a subprocess (D-12-10), invoked as the guard files directly (D-12-11)
- `passed` and `tech_debt` gate; `gaps_found` never does (D-12-12 / D-12-13) — each proven by a committed test
- Refusal text carries the red guard, its assertion output, and the two legitimate routes (D-12-15)
- The committed planted violation/false-negative pair passes (D-12-16), and the gate's own test cannot join its own guard set (D-12-09)
- Layer 1 runs under `npm test` from a clean checkout with zero setup (D-12-02)
</success_criteria>

<output>
Create `.planning/phases/12-audit-integrity-instrument/12-01-SUMMARY.md` when done.
Record in it: the `--json` ground-truth numbers observed against the real tree,
and the deliberate divergence from `12-PATTERNS.md`'s committed-fixture
recommendation in favour of `mkdtempSync` (with the leak reason).
</output>
