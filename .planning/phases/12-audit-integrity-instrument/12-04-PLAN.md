---
phase: 12-audit-integrity-instrument
plan: 04
type: execute
wave: 4
depends_on: ["12-03"]
worktree: false
files_modified:
  - .planning/phases/12-audit-integrity-instrument/12-GATE-PROOF.md
autonomous: true
requirements: [GATE-01]

must_haves:
  truths:
    - "A guard deliberately turned red on the REAL repository tree is shown, in a committed transcript, blocking the audit-`passed` path — success criterion 1 (D-12-17)"
    - "The same mechanism, against the same real tree with all four guards genuinely green, allows the gated status — success criterion 2"
    - "The guard reddened for the proof is `docs-linerefs.test.ts`, reddened by a one-digit change to a `vice-proxy.ts:<N>` citation in CLAUDE.md, because reverting it is a one-character undo with zero risk to product code (D-12-18)"
    - "The transcript records the revert as explicitly as the plant, with the guard's own green output shown afterwards, so a reader can tell the tree was not left dirty (D-12-20)"
    - "The transcript lives as a phase artifact under `.planning/phases/12-audit-integrity-instrument/`, not under `docs/`, which is reserved for durable cross-milestone findings (D-12-19)"
    - "Both gated statuses are exercised against the real tree — `passed` and `tech_debt` — and `gaps_found` is shown passing through unobstructed (D-12-12, D-12-13)"
    - "The committed planted pair in `audit-integrity.test.ts` is shown still green after the revert, so the permanent proof and the one-time proof agree (D-12-16)"
  artifacts:
    - path: ".planning/phases/12-audit-integrity-instrument/12-GATE-PROOF.md"
      provides: "The one-time real-tree red/green plant-and-revert transcript satisfying criteria 1 and 2"
      min_lines: 90
      contains: "docs-linerefs.test.ts"
  key_links:
    - from: ".planning/phases/12-audit-integrity-instrument/12-GATE-PROOF.md"
      to: "scripts/audit-gate.mjs"
      via: "verbatim captured output of the gate invoked against the real tree, red then green"
      pattern: "audit-gate"
    - from: "CLAUDE.md"
      to: "docs-linerefs.test.ts"
      via: "the planted-then-reverted vice-proxy.ts:<N> citation digit"
      pattern: "vice-proxy\\.ts:"
---

<objective>
Produce the one-time real-tree evidence that GATE-01's criteria 1 and 2 ask for:
plant a red guard on the actual repository, capture the mechanism refusing the
audit-`passed` path, revert, and capture it allowing again — all in one committed
phase artifact.

Purpose: the committed planted pair in `audit-integrity.test.ts` proves the gate
keeps working against synthetic trees. Criteria 1 and 2 additionally ask for a
transcript of the real mechanism against the real tree. This plan produces it,
and leaves the tree byte-identical to how it started.

Output: `.planning/phases/12-audit-integrity-instrument/12-GATE-PROOF.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-audit-integrity-instrument/12-CONTEXT.md
@.planning/phases/12-audit-integrity-instrument/12-PATTERNS.md
@.planning/phases/12-audit-integrity-instrument/12-01-SUMMARY.md
@.planning/phases/12-audit-integrity-instrument/12-02-SUMMARY.md
@.planning/phases/12-audit-integrity-instrument/12-03-SUMMARY.md
@CLAUDE.md
</context>

<hazards>
Read before touching anything.

1. **Do not write this artifact with a Bash heredoc.** Its body contains both a
   `*MILESTONE-AUDIT*` filename token and the literal `status: passed`. Once plan
   12-03's hook is live, a Bash command carrying both is exactly what the gate
   blocks — the executor would block its own tool call. Use the Write tool. A
   `Write` whose `file_path` is `12-GATE-PROOF.md` is out of the gate's scope
   (the path is not a milestone audit) and passes cleanly. This also matches
   CLAUDE.md's standing rule against heredoc file writes.
2. **Never commit the planted CLAUDE.md.** The plant makes `npm test` red. Revert
   before any `git add`, and verify with `git diff --quiet CLAUDE.md`.
3. **Expect Layer 1 to go red during the plant.** `audit-integrity.test.ts`
   asserts that no milestone audit declares a gated status while a guard is red,
   and four archived audits under `.planning/milestones/` declare `passed` or
   `tech_debt`. Its red run IS part of the evidence — capture it rather than
   treating it as a defect.
4. **Do not spawn a nested headless `claude -p` session** to exercise the hook.
   The deterministic hook proof is the piped `--hook` payload path already pinned
   by plan 12-02's tests; the live in-session proof is the end-of-phase
   `<human-check>` below.
</hazards>

<tasks>

<task type="auto">
  <name>Task 1: Run the real-tree plant-and-revert and capture every output</name>
  <files>.planning/phases/12-audit-integrity-instrument/12-GATE-PROOF.md</files>
  <read_first>
    - `CLAUDE.md:26` — the Architecture bullet containing the citations `vice-proxy.ts:3029`, `:2964`, `:1508` and `:1484`. The digit to change is the `3029` in the `rewriteArguments() runs at vice-proxy.ts:3029` clause.
    - `.claude/mcp/vice/docs-linerefs.test.ts` — the guard being reddened; read its three tests so the transcript can quote the right assertion by name
    - `scripts/audit-gate.mjs` — the mechanism whose output is being captured
    - `.planning/milestones/v0.3.0-MILESTONE-AUDIT.md:1-12` and `.planning/milestones/v0.2.0-MILESTONE-AUDIT.md:1-14` — the real frontmatter the refusal will name (`status: passed` and `status: tech_debt` respectively)
    - `.planning/phases/08.1-close-v0-2-0-audit-items-uat-walkthrough-planning-doc-drift/08.1-WALKTHROUGH-EVIDENCE.md:1-80` — the evidence-artifact frontmatter and the fenced `$ command` / literal-output transcript convention to copy
  </read_first>
  <action>
Run the sequence below on the real repository working tree, capturing every
command and its literal output as you go. Do not paraphrase any output.

Step 0 — baseline. Run `git status --porcelain` and confirm a clean tree. Run
`node scripts/audit-gate.mjs` and capture the `audit-gate: OK` line. Run
`cd .claude/mcp/vice && node --test docs-linerefs.test.ts` and capture its
`# pass` / `# fail` counts. This is the green-before state.

Step 1 — the plant (D-12-18). Change exactly one digit in CLAUDE.md line 26:
`vice-proxy.ts:3029` becomes `vice-proxy.ts:3030`. Nothing else in the file
changes. Capture `git diff --stat CLAUDE.md` and the one-line `git diff`
hunk to prove the change is a single character.

Step 2 — the guard is genuinely red. Run
`cd .claude/mcp/vice && node --test docs-linerefs.test.ts` and capture its full
failing output, including the assertion message naming the bad citation. This is
the guard's own voice, which D-12-15 requires the refusal to quote.

Step 3 — the mechanism refuses (criterion 1). Run `node scripts/audit-gate.mjs`
from the repo root and capture the complete `audit-gate: REFUSED` block and its
exit code. Confirm the block names all three D-12-15 parts: the red guard
(`docs-linerefs.test.ts`), its failing assertion text, and the two legitimate
routes. Confirm it lists the offending audit files, which will include
`.planning/milestones/v0.3.0-MILESTONE-AUDIT.md` (`status: passed`) and the three
`tech_debt` files — real-tree evidence for both gated statuses (D-12-12). Also
run `node scripts/audit-gate.mjs --json` and capture the object, so the
transcript carries a machine-readable form of the same verdict.

Step 4 — `gaps_found` is untouched (D-12-13). From the same `--json` capture,
point out that the two `gaps_found` round files appear in `auditFiles` and are
absent from `gatedAudits` even while the guard is red. Quote the relevant lines.

Step 5 — Layer 1 red. Run `cd .claude/mcp/vice && node --test audit-integrity.test.ts`
and capture the failure of the "no milestone audit declares a gated status while
any docs guard is red" test, including the embedded reason. Note that the
planted-violation and planted-false-negative tests still pass, because they run
against synthetic trees and are independent of the real repo's state.

Step 6 — the hook path refuses too. Run
`node --test audit-integrity.test.ts -t "hook mode"` and capture the result.
These are the deterministic hook-contract proofs; they use synthetic roots, so
they stay green. Then, to show the hook refusing against the REAL tree while it
is red, run the `--hook` mode with a payload supplied through a small Node
one-liner that builds the JSON object in JavaScript and pipes it to the gate
with `--root` pointed at the repo root. Build the payload in Node, never as a
literal Bash string — see this plan's hazard 1. Capture the exit code (`2`) and
the stderr block.

Step 7 — the revert (D-12-20). Change `vice-proxy.ts:3030` back to
`vice-proxy.ts:3029`. Capture `git diff --stat CLAUDE.md` showing no change and
`git status --porcelain` showing a clean tree.

Step 8 — green again (criterion 2). Re-run, capturing each in full:
`cd .claude/mcp/vice && node --test docs-linerefs.test.ts` (green, with its own
output shown — D-12-20 requires the guard's output after the revert, not merely
an assertion that it passed); `node scripts/audit-gate.mjs` (the `audit-gate: OK`
line and exit 0); `cd .claude/mcp/vice && npm test` (full suite green);
`node scripts/check-npm-packages.mjs` (green).

Now write `.planning/phases/12-audit-integrity-instrument/12-GATE-PROOF.md`
using the Write tool, following `08.1-WALKTHROUGH-EVIDENCE.md`'s shape.
Frontmatter must carry at least: `outcome: red-then-green`, `guard:
docs-linerefs.test.ts`, `planted_change` describing the one-digit CLAUDE.md
citation edit, `reverted: true`, `criteria: [1, 2]`, `date`, and the git sha the
proof was run against. Body sections, in order: Baseline; The plant; Guard red;
Mechanism refuses (criterion 1); `gaps_found` unobstructed; Layer 1 red; Hook
path refuses; The revert; Green again (criterion 2); and a closing "What this
does and does not prove" section.

That closing section must state plainly, in prose:
  - which file and line the check point lives at, so criterion 3 has its
    citation — `scripts/audit-gate.mjs`, naming the function and line number of
    `checkAuditGate()` as it stands at the recorded sha, and the
    `.claude/settings.json` hook entry that calls it;
  - that gating `status: tech_debt` alongside `passed` is deliberately stricter
    than GATE-01's literal wording, with D-12-12's reason (both statuses route to
    `/gsd-complete-milestone`), so a future milestone audit does not read it as
    scope drift;
  - that the Bash-mode scan is a content-and-adjacency heuristic and is evadable
    by a base64 or `python -c` payload that assembles the text at runtime
    (threat T-12-02), and that Layer 1 — which re-reads the actual committed file
    — is the unevadable enforcement point. Do not imply the hook is bulletproof;
  - that the live in-session hook block is verified separately by the
    end-of-phase human check recorded in this plan's `<verify>` block, because
    Claude Code loads hook configuration at session start and this session
    predates the wiring.
  </action>
  <acceptance_criteria>
    - `.planning/phases/12-audit-integrity-instrument/12-GATE-PROOF.md` exists, is at least 90 lines, and opens with YAML frontmatter containing `outcome: red-then-green`, `guard: docs-linerefs.test.ts`, `reverted: true` and a git sha
    - The body contains at least 8 fenced transcript blocks and at least 8 lines beginning with `$ ` (the captured commands)
    - The body contains the literal string `audit-gate: REFUSED` and the literal string `audit-gate: OK`, in that order
    - The refusal transcript names `docs-linerefs.test.ts`, quotes at least one line of that guard's own failing assertion output, and contains the two legitimate routes
    - The refusal transcript names `.planning/milestones/v0.3.0-MILESTONE-AUDIT.md` and at least one `tech_debt` audit file
    - The document shows the two `gaps_found` round files present in `auditFiles` and absent from `gatedAudits`
    - The document shows an exit code of `2` from a `--hook` invocation against the real tree while red
    - Sections for both the plant and the revert exist, and the post-revert section shows the guard's own green output, not a bare claim: `grep -c 'pass 3' 12-GATE-PROOF.md` returns `>= 1` (docs-linerefs has three tests)
    - The closing section names `scripts/audit-gate.mjs` with a function name and line number (criterion 3's citation), states the `tech_debt` strictness decision, and states the Bash-evasion limitation
    - `git diff --quiet CLAUDE.md` — the tree is byte-identical to the pre-plant state
    - `git status --porcelain CLAUDE.md` is empty
    - `cd .claude/mcp/vice && npm test` exits 0
    - `node scripts/audit-gate.mjs` exits 0
    - `node scripts/check-npm-packages.mjs` exits 0
    - No Bash command in this plan's shell history wrote the artifact: `grep -c '<<' 12-GATE-PROOF.md` may be non-zero inside quoted transcripts, but the file was created with the Write tool (record this in the SUMMARY)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && P=.planning/phases/12-audit-integrity-instrument/12-GATE-PROOF.md && test -f "$P" && test "$(wc -l < "$P")" -ge 90 && grep -q 'audit-gate: REFUSED' "$P" && grep -q 'audit-gate: OK' "$P" && grep -q 'docs-linerefs.test.ts' "$P" && grep -q 'reverted: true' "$P" && git diff --quiet CLAUDE.md && node scripts/audit-gate.mjs && cd .claude/mcp/vice && npm test && echo "gate proof recorded, tree clean, suite green"</automated>
    <human-check>
      After this phase completes, restart the Claude Code session (or run `/hooks`
      and approve the newly committed project hook) so the `PreToolUse` entry in
      the now-committed `.claude/settings.json` is loaded. Then, in that session:
      (1) plant the same one-digit CLAUDE.md change so `docs-linerefs.test.ts` is
      red; (2) attempt, with the Write tool, to create
      `.planning/v9.9.9-MILESTONE-AUDIT.md` whose frontmatter declares
      `status: passed`; (3) attempt the same via the Edit tool on an existing
      milestone-audit file; (4) attempt the same via a Bash heredoc. All three
      must be refused with the gate's stderr message; capture the refusal text.
      Then revert the CLAUDE.md digit and confirm the same Write now succeeds
      (delete the scratch file afterwards). Append the observed results to
      `12-GATE-PROOF.md` under a "Live in-session hook block" section, and confirm
      `git status --porcelain` is clean apart from that appended section.
    </human-check>
  </verify>
  <done>`12-GATE-PROOF.md` is committed, carries the full red-then-green real-tree transcript with the revert shown as explicitly as the plant, cites the check point by file and line, and the working tree is byte-identical to its pre-plant state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planted repo state → committed history | A deliberately-broken CLAUDE.md exists on disk for the duration of the proof; committing it would ship a red guard |
| transcript content → the live hook | The artifact's own text contains a milestone-audit token and a gated status, which is the exact shape the hook blocks |
| one-time evidence → standing guarantee | A transcript proves the mechanism worked once; it does not prove it keeps working |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-13 | Tampering | the planted CLAUDE.md citation | mitigate | The plant is a single digit in a documentation citation, chosen precisely because reverting is a one-character undo with zero risk to product code (D-12-18). Reversion is verified mechanically by `git diff --quiet CLAUDE.md` in the task's own automated verify, and the acceptance criteria refuse a non-empty `git status --porcelain CLAUDE.md`. `docs-review-disposition.test.ts` and `docs-deferred-ledger.test.ts` are deliberately NOT the reddened guards, since they require moving a todo out of the tree or editing STATE.md, which other derivations read. |
| T-12-14 | Denial of Service | the executor blocking its own tool call | mitigate | Hazard 1 forbids writing the artifact with a Bash heredoc and forbids literal gated-status payload strings on any Bash command line; payloads are constructed inside Node, and the artifact is written with the Write tool, whose `file_path` is out of the gate's scope. |
| T-12-15 | Repudiation | one-time transcript as the only proof | mitigate | The transcript is explicitly the secondary proof. The primary, permanent proof is the committed planted-violation / planted-false-negative pair in `audit-integrity.test.ts` (D-12-16), whose continued green state is captured in Step 8 and asserted by `npm test`. The closing section says which proof carries which guarantee. |
| T-12-02 | Tampering | Bash-mode regex evasion | accept | Carried forward from plan 12-02 unchanged. The closing "What this does and does not prove" section must state the limitation plainly rather than implying the hook is bulletproof. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan. |
</threat_model>

<verification>
- `git diff --quiet CLAUDE.md` and `git status --porcelain` shows nothing beyond the new artifact
- `cd .claude/mcp/vice && npm test` exits 0
- `cd .claude/mcp/vice && npm run typecheck` exits 0
- `node scripts/audit-gate.mjs` exits 0
- `node scripts/check-npm-packages.mjs` exits 0
- `12-GATE-PROOF.md` lives under `.planning/phases/12-audit-integrity-instrument/`, and nothing was added under `docs/` (D-12-19): `git status --porcelain docs/` is empty
</verification>

<success_criteria>
- Criterion 1 satisfied: a committed transcript shows the mechanism refusing the audit-`passed` path on the real tree while `docs-linerefs.test.ts` is red (D-12-17, D-12-18)
- Criterion 2 satisfied: the same transcript shows the mechanism allowing it after the revert, with the guard's own green output (D-12-20)
- Criterion 3 satisfied: the transcript cites `scripts/audit-gate.mjs` by function and line, plus the `.claude/settings.json` entry that calls it
- The transcript is a phase artifact, not a `docs/` finding (D-12-19)
- The permanent planted pair (D-12-16) is shown still green, and the tree is left byte-identical
- The `tech_debt` strictness decision (D-12-12) and the Bash-evasion limitation (T-12-02) are both stated plainly for the milestone audit to read
</success_criteria>

<output>
Create `.planning/phases/12-audit-integrity-instrument/12-04-SUMMARY.md` when done.
Record in it, per D-12-12's instruction: that the gate refuses `status: tech_debt`
as well as `status: passed`, deliberately stricter than GATE-01's literal
wording, and why — so the milestone audit does not read it as scope drift. Also
record the plainly-stated Bash-regex evasion limitation, and confirm that
`2026-08-13-reconcile-ci-test-command-with-narrowed-gate.md` was left unresolved
and CI's test command unchanged.
</output>
