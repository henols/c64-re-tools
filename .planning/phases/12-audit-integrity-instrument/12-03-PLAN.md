---
phase: 12-audit-integrity-instrument
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
worktree: false
files_modified:
  - .claude/settings.json
  - .claude/settings.local.json
  - .gitignore
  - .claude/mcp/vice/audit-integrity.test.ts
autonomous: true
requirements: [GATE-01]

must_haves:
  truths:
    - "A committed `.claude/settings.json` declares a `PreToolUse` hook whose matcher covers `Write`, `Edit` AND `Bash`, pointing at `scripts/audit-gate.mjs --hook` (D-12-03, D-12-04)"
    - "The committed `.claude/settings.json` contains a `hooks` key and nothing else — no permissions, no absolute machine paths (D-12-05)"
    - "The machine-specific permission allowlist and `additionalDirectories` moved into `.claude/settings.local.json`, merged with its existing content rather than overwriting it, and stay uncommitted (D-12-05)"
    - "`.gitignore` stops ignoring `.claude/settings.json`, starts ignoring `.claude/settings.local.json`, and its comment records the split and why — honouring the original machine-specific rationale rather than overriding it (D-12-05)"
    - "No committed git hook and no `core.hooksPath` setup step is introduced; Layer 1 remains the clean-checkout guarantee (D-12-06)"
    - "Layer 1 asserts the hook wiring itself, so deleting or narrowing the hook block reds the test suite and is visible in a commit (D-12-02, D-12-14)"
  artifacts:
    - path: ".claude/settings.json"
      provides: "Committed, hooks-only PreToolUse wiring for the audit gate"
      contains: "audit-gate.mjs"
    - path: ".gitignore"
      provides: "The amended ignore rule and the recorded split rationale"
      contains: ".claude/settings.local.json"
    - path: ".claude/mcp/vice/audit-integrity.test.ts"
      provides: "Wiring assertions that make the hook block itself guarded"
      contains: "PreToolUse"
  key_links:
    - from: ".claude/settings.json"
      to: "scripts/audit-gate.mjs"
      via: "PreToolUse hooks[].command shelling the script with --hook"
      pattern: "audit-gate\\.mjs.*--hook"
    - from: ".claude/mcp/vice/audit-integrity.test.ts"
      to: ".claude/settings.json"
      via: "reads and asserts the committed hook block's matcher and command"
      pattern: "settings\\.json"
---

<objective>
Wire Layer 2 into the repo: commit a hooks-only `.claude/settings.json`, relocate
the machine-specific permissions into `.claude/settings.local.json`, amend
`.gitignore` to record the split, and make Layer 1 guard the wiring so removing
it cannot be silent.

Purpose: D-12-03's in-session refusal is inert until Claude Code is told about
the hook. D-12-05 requires that be done by splitting the settings file rather
than un-ignoring a file full of absolute worktree paths.

Output: a committed hooks-only settings file, an updated local settings file, an
amended `.gitignore` block, and three new wiring assertions in
`audit-integrity.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-audit-integrity-instrument/12-CONTEXT.md
@.planning/phases/12-audit-integrity-instrument/12-RESEARCH.md
@.planning/phases/12-audit-integrity-instrument/12-PATTERNS.md
@.planning/phases/12-audit-integrity-instrument/12-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split .claude/settings.json into a committed hooks-only file and an ignored local file</name>
  <files>.claude/settings.json, .claude/settings.local.json, .gitignore</files>
  <read_first>
    - `.claude/settings.json` — the current on-disk file: a single `permissions` key holding ~35 `allow` entries and three `additionalDirectories` absolute worktree paths under this machine's home directory. Every byte of this is the machine-specific content D-12-05 says must move.
    - `.claude/settings.local.json` — already exists and already holds unrelated content: a `permissions.allow` array of four entries and `disabledMcpjsonServers: ["mastra","vice"]`. This must be MERGED into, never overwritten.
    - `.gitignore:49-55` — the `/.vscode/` block and the `/.claude/settings.json` block with its four-line rationale comment; and the `/tools/` block at :16-24 and `/installer/skills/` block at :40-44 for the house comment voice (explain WHY, point at the alternative location)
    - `.claude-plugin/plugin.json:24-35` — the repo's only existing committed hook declaration (`hooks.SessionStart`), the structural precedent for a hook block
    - `$HOME/.claude/settings.json` § `hooks.PreToolUse` — four working entries on this host showing the real `{ matcher, hooks: [{ type, command, timeout }] }` shape
    - `.planning/phases/12-audit-integrity-instrument/12-RESEARCH.md` § Common Pitfalls Pitfall 4 — the committed-absolute-paths redline
  </read_first>
  <action>
Perform the split in this order so nothing is lost.

1. Read both settings files in full. Merge the entire `permissions` object from
   `.claude/settings.json` — both `allow` and `additionalDirectories` — into
   `.claude/settings.local.json`'s existing `permissions` object. Union the
   `allow` arrays (deduplicate exact string matches, preserve the existing four
   entries), and carry `additionalDirectories` across verbatim. Leave
   `disabledMcpjsonServers` untouched. Claude Code merges the two files, so
   behaviour is preserved.

2. Replace `.claude/settings.json` entirely with a JSON object containing
   exactly one top-level key, `hooks`. Under it, a `PreToolUse` array with one
   entry whose `matcher` is the string `Write|Edit|Bash` and whose `hooks` array
   holds one `{ "type": "command", "command": ..., "timeout": 30 }` entry. The
   command shells the single check point directly with no wrapper file
   (D-12-01): node, then the script path built from `${CLAUDE_PROJECT_DIR}` and
   `scripts/audit-gate.mjs`, quoted so a space in the project path is safe, then
   the `--hook` flag. Use a bare `node` rather than an absolute interpreter path
   — an absolute nvm path is exactly the machine-specific content this split
   exists to keep out. `MultiEdit` and `NotebookEdit` are not in the matcher:
   `audit-gate.mjs` recognises them if they ever arrive, but adding untested
   matcher names here would be a claim the phase has not verified. Record that
   in the SUMMARY.

   Redline before staging: the committed file must contain no absolute path, no
   home directory, no `permissions` key, and no `additionalDirectories` key.

3. Amend `.gitignore`'s `:51-55` block. Change the ignored path from
   `/.claude/settings.json` to `/.claude/settings.local.json`, and rewrite the
   comment in the same voice as the surrounding blocks to record: that
   `.claude/settings.json` is now committed and carries the audit-gate
   `PreToolUse` hook only (GATE-01, phase 12); that the machine-specific
   permission allowlist and `additionalDirectories` — which hold absolute
   worktree paths from earlier sessions — moved to `.claude/settings.local.json`,
   which Claude Code merges over the committed file; and that this split honours
   the original rationale (machine-specific permission content must not be
   committed) rather than overriding it. Note in the comment that
   `.claude/settings.local.json` is also covered by many users' global gitignore
   and that the repo-level rule here makes the guarantee local and explicit.

4. Because `.claude/settings.json` is currently ignored, git will not pick it up
   until the `.gitignore` amendment is staged. Stage the `.gitignore` change
   first, then `git add .claude/settings.json`, and confirm with
   `git status --porcelain` that the settings file is staged as a new file.
   Confirm with `git check-ignore -v .claude/settings.local.json` that the local
   half is still ignored, and that `git status --porcelain .claude/settings.local.json`
   shows nothing.

Do not create `scripts/githooks/pre-commit` and do not touch `core.hooksPath` —
D-12-06 rejected that route because per-clone git config is not committed and
reintroduces the clean-checkout hole. Do not change `.github/workflows/ci.yml`:
Layer 1 rides the existing `Test` step, and the question of which command CI runs
is the deliberately-unfolded todo `2026-08-13-reconcile-ci-test-command-with-narrowed-gate.md`.
  </action>
  <acceptance_criteria>
    - `node -e "const s=require('./.claude/settings.json');const k=Object.keys(s);if(k.length!==1||k[0]!=='hooks')throw new Error('not hooks-only: '+k);console.log('hooks-only OK')"` prints `hooks-only OK`
    - `grep -c '/home/' .claude/settings.json` returns 0 and `grep -ci 'permissions\|additionalDirectories' .claude/settings.json` returns 0
    - `node -e "const h=require('./.claude/settings.json').hooks.PreToolUse[0];for(const t of ['Write','Edit','Bash'])if(!h.matcher.split('|').includes(t))throw new Error('matcher missing '+t);if(!h.hooks[0].command.includes('audit-gate.mjs')||!h.hooks[0].command.includes('--hook'))throw new Error('command wrong');console.log('matcher+command OK')"` prints `matcher+command OK`
    - `.claude/settings.local.json` still parses as JSON, still contains `disabledMcpjsonServers` with both `mastra` and `vice`, and its `permissions.allow` array length is at least the sum of the two source arrays minus exact duplicates
    - `node -e "const l=require('./.claude/settings.local.json');if(!l.permissions.additionalDirectories||l.permissions.additionalDirectories.length!==3)throw new Error('additionalDirectories not carried across');console.log('local merge OK')"` prints `local merge OK`
    - `git check-ignore -v .claude/settings.local.json` reports a match; `git check-ignore .claude/settings.json` reports no match (exit 1)
    - `git status --porcelain .claude/settings.json` shows the file staged or modified — it is no longer invisible to git
    - `grep -v '^#' .gitignore | grep -c '/.claude/settings.json'` returns 0 — the whole-file ignore is gone from the rules (comment lines excluded so the explanatory prose does not self-invalidate the check)
    - `grep -v '^#' .gitignore | grep -c '/.claude/settings.local.json'` returns 1
    - `sed -n '/settings.local.json/,+0p' .gitignore` is preceded by a comment block of at least 4 lines: `awk '/^\/\.claude\/settings\.local\.json$/{print NR}' .gitignore` gives a line number N, and lines N-5..N-1 include at least four lines starting with `#`
    - No `scripts/githooks/` directory was created: `test ! -d scripts/githooks`
    - `git diff --quiet .github/workflows/ci.yml` — CI's test command is untouched (scope fence)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node -e "const s=require('./.claude/settings.json');const k=Object.keys(s);if(k.length!==1||k[0]!=='hooks')throw new Error('not hooks-only');const h=s.hooks.PreToolUse[0];for(const t of ['Write','Edit','Bash'])if(!h.matcher.split('|').includes(t))throw new Error('matcher missing '+t);if(!h.hooks[0].command.includes('--hook'))throw new Error('no --hook');const l=require('./.claude/settings.local.json');if(!l.disabledMcpjsonServers.includes('vice'))throw new Error('local content lost');if(l.permissions.additionalDirectories.length!==3)throw new Error('dirs lost');console.log('settings split OK')" && test ! -d scripts/githooks && git diff --quiet .github/workflows/ci.yml && echo "scope fence intact"</automated>
  </verify>
  <done>`.claude/settings.json` is committed, hooks-only, absolute-path-free, and declares a `Write|Edit|Bash` PreToolUse hook calling `audit-gate.mjs --hook`; `.claude/settings.local.json` holds the merged machine-specific permissions and remains ignored; `.gitignore` records the split with a four-line rationale.</done>
</task>

<task type="auto">
  <name>Task 2: Make Layer 1 guard the hook wiring itself</name>
  <files>.claude/mcp/vice/audit-integrity.test.ts</files>
  <read_first>
    - `.claude/mcp/vice/audit-integrity.test.ts` — the file being extended; reuse `ROOT` from `repoRoot({ from: HERE })`
    - `.claude/settings.json` — the file being asserted against, as written by Task 1
    - `.claude/mcp/vice/test-gate.test.ts:16-29` — the exact-membership assertion shape for a frozen expected set
    - `.claude/mcp/vice/hostpath-consumers.test.ts:182-197` — the non-vacuity-floor convention these assertions follow
  </read_first>
  <action>
Add a wiring test group to `.claude/mcp/vice/audit-integrity.test.ts`. Its
purpose is threat T-12-09: without it, the entire Layer 2 wiring can be deleted
in a commit nobody notices, and the only signal would be silence. With it,
deleting or narrowing the hook block reds `npm test` and CI — which is exactly
D-12-14's doctrine that the legitimate route to disable a guard is a visible
commit, not an invisible omission.

Read `<ROOT>/.claude/settings.json` with `readFileSync` and `JSON.parse`, and
assert:

1. "the committed settings file exists and is hooks-only (D-12-05)" — the file
   exists, parses, its top-level key set is exactly `["hooks"]`, and its raw
   text contains neither a `/home/` substring nor the strings `permissions` or
   `additionalDirectories`. The absolute-path assertion is the Pitfall 4
   redline made mechanical.

2. "a PreToolUse entry wires the audit gate over Write, Edit and Bash (D-12-03 /
   D-12-04)" — locate the `PreToolUse` entry whose command mentions
   `audit-gate.mjs`; assert it exists, that its `matcher` split on `|` contains
   all three of `Write`, `Edit` and `Bash`, that the command also contains
   `--hook`, and that a numeric `timeout` is present. Give the `Bash` assertion
   its own failure message naming the heredoc bypass, so a future maintainer who
   narrows the matcher reads why it was wide.

3. "the wired script path resolves to the real single check point (D-12-01)" —
   substitute the literal `${CLAUDE_PROJECT_DIR}` token in the command with
   `ROOT`, extract the resulting `.mjs` path, and assert `existsSync` on it and
   that it ends with `scripts/audit-gate.mjs`. This is what catches a rename of
   the script that leaves the hook pointing at nothing.

4. "the ignore rules match the committed split (D-12-05)" — read `<ROOT>/.gitignore`,
   strip comment lines beginning with `#`, and assert that the remaining rules
   contain `/.claude/settings.local.json` and do NOT contain a bare
   `/.claude/settings.json`. Stripping comments first is required: the amended
   comment block names both paths in prose, and an unfiltered count would make
   the assertion self-invalidating.

5. "no second enforcement route was introduced (D-12-06)" — assert that
   `<ROOT>/scripts/githooks` does not exist, with a message citing D-12-06's
   reason (per-clone `core.hooksPath` is not committed and reintroduces the
   clean-checkout hole Layer 1 exists to close).
  </action>
  <acceptance_criteria>
    - `cd .claude/mcp/vice && node --test audit-integrity.test.ts` exits 0 with `# fail 0` and `# pass 25` or more
    - `node --test audit-integrity.test.ts -t "PreToolUse"` runs at least one test and passes
    - The wiring tests are proven non-vacuous: with `.claude/settings.json` temporarily moved aside, `node --test audit-integrity.test.ts` exits non-zero naming the missing settings file; restore it and it passes again. Record both outputs in the task's commit message or the plan SUMMARY.
    - The matcher test is proven non-vacuous: with the matcher temporarily narrowed to `Write|Edit`, the run fails with the heredoc-bypass message; restore and it passes. Record both outputs.
    - After both non-vacuity probes, `git diff --quiet .claude/settings.json` — the file is byte-identical to Task 1's version
    - `cd .claude/mcp/vice && npm run typecheck` exits 0
    - `cd .claude/mcp/vice && npm test` exits 0
    - `node scripts/audit-gate.mjs` exits 0
    - `node scripts/check-npm-packages.mjs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/.claude/mcp/vice && node --test audit-integrity.test.ts && npm run typecheck && npm test && cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/audit-gate.mjs && node scripts/check-npm-packages.mjs && git diff --quiet .claude/settings.json && echo "wiring guarded and clean"</automated>
  </verify>
  <done>Five wiring assertions pass, each proven non-vacuous by a temporary break-and-restore, and `.claude/settings.json` is left byte-identical afterwards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local machine state → committed repo | `.claude/settings.json` currently holds absolute home-directory paths; committing it unfiltered would leak machine layout into every clone |
| committed hook wiring → future commits | The wiring is a plain JSON file any commit can delete or narrow |
| `.gitignore` rules → the committed/ignored boundary | A wrong rule either leaks machine-specific permissions or keeps the hook wiring invisible to git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Repudiation | `.claude/settings.json` hook block | mitigate | Layer 1 asserts the block exists, that its matcher covers `Write`, `Edit` and `Bash`, that its command names `audit-gate.mjs --hook`, and that the referenced script path resolves. Deleting or narrowing the wiring reds `npm test` and CI, so the only route to disabling it is a visible commit — D-12-14's doctrine. Each assertion is proven non-vacuous by a temporary break-and-restore. |
| T-12-10 | Information Disclosure | committing `.claude/settings.json` | mitigate | The committed file is hooks-only by construction; a mechanical assertion rejects any `/home/` substring, any `permissions` key and any `additionalDirectories` key in its raw text (Pitfall 4's redline). The machine-specific half moves to `.claude/settings.local.json`, which the amended `.gitignore` keeps out of the repo, verified with `git check-ignore -v`. |
| T-12-11 | Tampering | the `.gitignore` amendment | mitigate | The ignore-rule assertion strips `#` comment lines before matching, so the amended rationale prose naming both paths cannot make the check pass or fail spuriously. Both directions are asserted: the local file ignored, the committed file not. |
| T-12-12 | Elevation of Privilege | hook `command` string | mitigate | The command uses a bare `node` plus a `${CLAUDE_PROJECT_DIR}`-relative, quoted script path — no absolute interpreter path, no shell pipeline, no interpolation of any tool payload. A numeric `timeout` bounds the hook's wall clock so a stuck gate cannot stall the session indefinitely (pairs with T-12-01's stdin bound). |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan. |
</threat_model>

<verification>
- `cd .claude/mcp/vice && npm test` exits 0
- `cd .claude/mcp/vice && npm run typecheck` exits 0
- `node scripts/check-npm-packages.mjs` exits 0
- `git check-ignore .claude/settings.json` exits 1 (no longer ignored); `git check-ignore -v .claude/settings.local.json` matches
- `git diff --quiet .github/workflows/ci.yml` and `test ! -d scripts/githooks` — scope fences intact
- `git status --porcelain .claude/settings.local.json` is empty (the local half never enters the commit)
</verification>

<success_criteria>
- A committed, hooks-only `.claude/settings.json` wires `Write|Edit|Bash` to `audit-gate.mjs --hook` with a bounded timeout (D-12-03, D-12-04, D-12-05)
- The machine-specific permissions and `additionalDirectories` live in `.claude/settings.local.json`, merged with its pre-existing content and still ignored (D-12-05)
- `.gitignore`'s amended block records the split and honours the original rationale (D-12-05)
- No git-hook route and no `core.hooksPath` step was introduced (D-12-06)
- Layer 1 now guards the wiring, each assertion proven non-vacuous (D-12-02, D-12-14)
- CI's test command and the reconcile-CI-command todo are untouched (scope fence)
</success_criteria>

<output>
Create `.planning/phases/12-audit-integrity-instrument/12-03-SUMMARY.md` when done.
Record in it: the non-vacuity break-and-restore outputs for the wiring
assertions, and the decision to keep `MultiEdit`/`NotebookEdit` out of the
matcher despite the script recognising them.
</output>
