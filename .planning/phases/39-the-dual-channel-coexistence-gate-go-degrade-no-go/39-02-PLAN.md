---
phase: 39-the-dual-channel-coexistence-gate-go-degrade-no-go
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-cycle-timing-and-wedge-triage/07-RESEARCH.md
  - CLAUDE.md
  - .planning/notes/stock-vice-migration-revised-loss-ledger.md
  - .planning/STATE.md
  - .planning/todos/pending/2026-08-28-phase-7-pitfall-5-overgeneralizes-text-monitor-unreachability.md
  - .planning/todos/completed/2026-08-28-phase-7-pitfall-5-overgeneralizes-text-monitor-unreachability.md
autonomous: true
requirements: [CHAN-01]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CHAN-01: the premise this phase's whole measurement rests on — that the text monitor is reachable over TCP via `-remotemonitor` and is neither the binary port nor the interactive console — is stated correctly in the artifact that got it wrong, so the next reader cannot repeat the inference that left the port unclaimed for eight phases"
    - "The narrow, correct claim survives the correction: there is no execute-monitor-command-text opcode in the binary monitor's command enum on any VICE version, so the text monitor genuinely cannot be driven over the binary port"
    - "Phase 7's decision is not rewritten — only its stated reason; the record says plainly that the decision was taken on a false premise even if the outcome stands"
    - "Three CLAUDE.md constraints that are true of the binary monitor and have a text-channel remedy carry a scoping clause rather than being deleted or left unqualified"
    - "CHAN-01 (edge: ordering): the todo's file move and its STATE.md Deferred Items row removal land in the SAME commit, because `docs-deferred-ledger.test.ts` fails in both directions — a moved file with a surviving row and a removed row with a surviving file both red the suite"
    - "The historical v0.7.0 audit-acknowledgement row for this todo is left in place: it records what was deferred at that close and is not a pending-tree assertion"
    - statement: "No new failing test appears beyond the recorded `test:automated` baseline of 2 failing tests in `anno-register.test.ts` — asserted as a relation against the baseline this run observes, never as an absolute count of zero"
      verification: backstop
  artifacts:
    - path: ".planning/todos/completed/2026-08-28-phase-7-pitfall-5-overgeneralizes-text-monitor-unreachability.md"
      provides: "The resolved todo, moved out of the pending tree in the same commit as its ledger row"
      contains: "resolves_phase: 39"
    - path: ".planning/phases/07-cycle-timing-and-wedge-triage/07-RESEARCH.md"
      provides: "The corrected Pitfall 5 and Alternatives Considered row, naming `-remotemonitor` and `monitor_network.c` explicitly"
      contains: "monitor_network.c"
  key_links:
    - from: ".planning/todos/completed/2026-08-28-phase-7-pitfall-5-overgeneralizes-text-monitor-unreachability.md"
      to: ".planning/STATE.md"
      via: "the `## Deferred Items` row for this stem is removed in the same commit as the file move; the two-directional guard checks both directions"
      pattern: "Deferred Items"
  prohibitions:
    - statement: "MUST NOT delete or weaken any of the three CLAUDE.md constraints being scoped — each is literally true of the binary monitor, and a deleted constraint is a lost fact rather than a corrected one"
      status: resolved
      verification: judgment
    - statement: "MUST NOT rewrite Phase 7's decision to reject the text route; only its stated reason is corrected, and the record says the decision was taken on a false premise even where the outcome stands"
      status: resolved
      verification: judgment
    - statement: "MUST NOT remove the historical v0.7.0 audit-acknowledgement row for this todo from STATE.md — it records what was deferred at that milestone's close and is not an assertion about the pending tree"
      status: resolved
      verification: judgment
---

<objective>
Resolve the one folded todo (`resolves_phase: 39`): correct the artifact that
overgeneralized "text monitor unreachable" into the claim that left the
`-remotemonitor` port opened-and-never-dialed since Phase 3, scope the three
CLAUDE.md constraints that are true of the binary monitor only, and close the todo
through the two-directional ledger guard.

Purpose: this phase is the first thing in the project's history to actually dial
that port, so it produces exactly the measured evidence the correction needs. The
correction is also load-bearing for the phase itself — `39-RESEARCH.md` records
that `07-RESEARCH.md`'s Pitfall 5 "is what Phase 7 acted on", and a planner or
executor reading it mid-phase would conclude the channel this gate measures cannot
be reached at all.

Output: a corrected `07-RESEARCH.md`, three scoped CLAUDE.md constraints, a
cross-referenced loss-ledger note, and the todo moved to `completed/` with its
`STATE.md` Deferred Items row removed in the **same** commit.

**This plan writes nothing under the phase evidence directory**, so it cannot
disturb `39-01`'s ordering proof — that proof counts commits touching
`evidence/`, not commits. It runs concurrently with `39-01` in wave 1 for exactly
the reason Phase 33's `33-02` did.

**Execution note:** this plan delivers `.planning/STATE.md` content, so it takes
the stock per-plan carve-out `USE_WORKTREES_FOR_PLAN=false` (worktree executors
may not touch STATE.md, and the commit strips it). That also side-steps the
deletion half of the file move: `cleanup-wave` refuses any branch whose diff
contains a deletion, and a `git mv` reads as delete-plus-add.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/39-CONTEXT.md
@.planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/39-RESEARCH.md
@.planning/todos/pending/2026-08-28-phase-7-pitfall-5-overgeneralizes-text-monitor-unreachability.md
@.planning/notes/text-monitor-channel-live-probe.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the overgeneralized claim where it was made, and scope the three constraints it distorted</name>
  <reversibility rating="reversible">All four edits are prose in planning artifacts and project instructions; reverting is a single revert with no dependent state.</reversibility>
  <read_first>
    - .planning/todos/pending/2026-08-28-phase-7-pitfall-5-overgeneralizes-text-monitor-unreachability.md — the four required edits and the named files, verbatim
    - .planning/phases/07-cycle-timing-and-wedge-triage/07-RESEARCH.md — locate the Alternatives Considered table row and the Pitfall 5 section before editing either; both must be found, not guessed at
    - .planning/notes/text-monitor-channel-live-probe.md — the observed evidence the correction cites: the exact launch command, the confirmed command table, and the three CLAUDE.md constraints it flags
    - .planning/notes/stock-vice-migration-revised-loss-ledger.md — Loss 5, route 2, which already had this right and which the correction should cross-reference rather than restate
    - CLAUDE.md — the Constraints section, specifically the three bullets on `CPUHISTORY_GET` requiring VICE >= 3.10, the absent runtime `WarpMode` resource, and `default_memspace` contamination having no direct remedy
    - src/mcp/vice/docs-linerefs.test.ts (lines 60-135 and 250-270) — confirm `SCANNED_DOCS` is exactly `CLAUDE.md` and `.planning/PROJECT.md` and that what it pins is the two `rewriteArguments()` citations, so a Constraints-section edit does not disturb it
  </read_first>
  <files>.planning/phases/07-cycle-timing-and-wedge-triage/07-RESEARCH.md, CLAUDE.md, .planning/notes/stock-vice-migration-revised-loss-ledger.md</files>
  <action>
Make four prose edits. Every one of them cites observed evidence from
`.planning/notes/text-monitor-channel-live-probe.md` (probed live 2026-08-27
against `/usr/bin/x64sc`, genuine unpatched stock VICE 3.9) rather than asserting
a documentation claim.

**Edit 1 — `07-RESEARCH.md`, the Alternatives Considered table row.** Rewrite the
row so it keeps the narrow claim and drops the generalization. The narrow claim
that must survive, stated in these terms: `monitor_binary.c`'s
`enum t_binary_command` has no execute-monitor-command-text opcode of any kind, on
any VICE version, so a monitor command genuinely cannot be invoked over the
**binary** port. The generalization that must go: that this makes the text monitor
reachable only through the interactive console. Name the third route explicitly so
the inference cannot be repeated — `-remotemonitor` / `monitor_network.c`, a TCP
listener that is neither the binary port nor the console, and which this repo's own
broker has appended to every stock launch since Phase 3 (`broker-launch.mts`'s
`-remotemonitor -remotemonitoraddress ip4://<host>:<port>` block, with the
allocated port recorded in the instance record by `broker-state.mts`).

**Edit 2 — `07-RESEARCH.md`, Pitfall 5.** Same correction, same two halves.
Additionally: do **not** rewrite Phase 7's decision. Rejecting the text route for
the cycle bracket may still have been right on its merits — a second channel, its
own ownership question, halt-on-command semantics — so correct the stated reason
and add a sentence saying plainly that the decision was taken on a false premise
even though the outcome may stand. Mark the corrected passage so a reader can see
it is a later correction rather than the original text, and cite this phase
(Phase 39) and the live-probe note as the source. State the consequence the todo
names: believing the channel unreachable is what left `chis` CPU history with
per-entry cycle counts, `memmapshow`/`memmapzap` execute-access mapping, the
`prof` profiler, `bt` backtrace, `io` semantic register decode, `warp on`/`warp
off` and `device c:` recorded as unavailable on stock when all of them are
confirmed working on 3.9.

**Edit 3 — `CLAUDE.md`, three Constraints bullets.** Add a scoping clause to each;
delete nothing and weaken nothing, because each bullet is literally true as
written of the binary monitor. Use the same clause shape on all three so the
result is greppable, beginning each added clause with the literal words
`binary monitor only` and naming the text-channel remedy and its measured date:

  - The `CPUHISTORY_GET` (0x86) requires-VICE-3.10 bullet gains a clause noting
    that the version floor is on the **opcode**, `binary monitor only` — the
    *capability* is not gated, because `chis` returned CPU history with per-entry
    cycle counts over the text channel on genuine stock 3.9 (MEASURED 2026-08-27).
    Note the polarity distinction `PARSE-04` depends on: text-command tracing and
    profiling support is opt-**out** at build time, the opposite of a version
    floor, and the affected commands do not share a single guard.
  - The no-runtime-`WarpMode`-resource bullet gains a clause noting the claim is
    about the **resource**, `binary monitor only`; `warp on` / `warp off` is a
    monitor *command* and works at runtime over the text channel (MEASURED
    2026-08-27, and it reports its own state).
  - The `default_memspace`-contamination-has-no-direct-remedy bullet gains a
    clause noting the absence of a remedy is `binary monitor only`; `device c:`
    over the text channel resets the default device (MEASURED 2026-08-27). Note
    that `device c` without the colon is a syntax error and the accepted spelling
    carries it.

Do not touch CLAUDE.md's Architecture bullet or either of its
`rewriteArguments()` line citations — `docs-linerefs.test.ts` pins both
mechanically, and a stray reflow of that bullet reds it.

**Edit 4 — `.planning/notes/stock-vice-migration-revised-loss-ledger.md`.** Loss 5,
route 2 already stated the correct fact ("text monitor's real `stopwatch`/`sw` …
usable because `-binarymonitor` and `-remotemonitor` **coexist**"). Add a short
cross-reference recording that this correct finding was contradicted by
`07-RESEARCH.md`'s Pitfall 5 with nothing catching it, that Pitfall 5 is what
Phase 7 acted on, and that Phase 39 is where the port was first dialed and the
correction landed. Point at `07-RESEARCH.md` and at the live-probe note by path.

Finally, re-check the sibling pending todo
`2026-08-26-run-vice-headless-and-in-warp-mode-when-the-run-allows-it` as the
todo instructs, and record the finding **in this plan's SUMMARY only** — that
todo's own file and its ledger row are left exactly as they are. Its argument that
warp must be a launch-time, per-backend, warm-instance-eligibility dimension rests
on the second constraint above, and runtime `warp on` may remove warp from it
entirely, leaving headless as the only genuine launch-mode dimension. Note whether
that is so; do not act on it, and do not open or close anything.
  </action>
  <acceptance_criteria>
    - `07-RESEARCH.md` contains the literal string `monitor_network.c` and the literal string `-remotemonitor`, in both the Alternatives Considered row and the Pitfall 5 section
    - `07-RESEARCH.md` still contains the narrow surviving claim, identifiable by the literal string `t_binary_command`
    - `07-RESEARCH.md` carries a visible correction marker and a sentence stating the decision was taken on a false premise
    - `CLAUDE.md` contains at least three occurrences of the literal clause opener `binary monitor only`, one in each of the three named constraint bullets
    - `CLAUDE.md`'s two `rewriteArguments()` citations are untouched: `node --test docs-linerefs.test.ts` reports `fail 0`
    - `.planning/notes/stock-vice-migration-revised-loss-ledger.md` cross-references `07-RESEARCH.md` by path
    - Neither the CLAUDE.md constraint count nor any constraint's original assertion is reduced — all three bullets keep their existing text and gain a clause
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && R=.planning/phases/07-cycle-timing-and-wedge-triage/07-RESEARCH.md && grep -ac 'monitor_network.c' "$R" && grep -ac -- '-remotemonitor' "$R" && grep -ac 't_binary_command' "$R" && test "$(grep -ac 'binary monitor only' CLAUDE.md)" -ge 3 && grep -ac 'stock-vice-migration-revised-loss-ledger' .planning/notes/stock-vice-migration-revised-loss-ledger.md >/dev/null; grep -ac '07-RESEARCH.md' .planning/notes/stock-vice-migration-revised-loss-ledger.md && echo CORRECTION_SHAPE_OK</automated>
    <fails_when>non-zero exit, or `CORRECTION_SHAPE_OK` absent from stdout — which happens when `07-RESEARCH.md` does not name `monitor_network.c` / `-remotemonitor` / `t_binary_command`, when fewer than three `binary monitor only` clauses are present in CLAUDE.md, or when the loss-ledger note does not cite `07-RESEARCH.md`</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-linerefs.test.ts docs-dangling-refs.test.ts 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, or the summary line does not report `fail 0` — a reflowed Architecture bullet or a dangling path introduced by these edits reds one of the two</fails_when>
  </verify>
  <done>`07-RESEARCH.md`'s Alternatives row and Pitfall 5 keep the narrow binary-port claim and name `-remotemonitor` / `monitor_network.c` as the third route; Phase 7's decision is intact with its stated reason corrected and the false premise recorded; all three CLAUDE.md constraints carry a `binary monitor only` scoping clause with their original text unchanged; the loss-ledger note cross-references the contradiction; `docs-linerefs.test.ts` and `docs-dangling-refs.test.ts` both report `fail 0`.</done>
</task>

<task type="auto">
  <name>Task 2: Close the todo through the two-directional ledger guard in one commit</name>
  <reversibility rating="reversible">A single revert restores both the file location and the ledger row; the guard checks the pair, so they revert together.</reversibility>
  <precondition>`.planning/todos/pending/2026-08-28-phase-7-pitfall-5-overgeneralizes-text-monitor-unreachability.md` still exists in the pending tree and `.planning/STATE.md`'s `## Deferred Items` table still carries the row whose Item cell is that filename stem.</precondition>
  <read_first>
    - src/mcp/vice/docs-deferred-ledger.test.ts (whole file, ~200 lines) — the exact invariant: it scans ONLY `STATE.md`'s `## Deferred Items` section, keys rows on the todo's filename stem matched as its own table cell (`| <stem> |`), and checks both directions; it explicitly does NOT check the "Carried forward from earlier closes" or audit-acknowledgement tables
    - .planning/STATE.md (lines 1678-1700) — the `## Deferred Items` table and the row to remove: Category `docs`, Item `2026-08-28-phase-7-pitfall-5-overgeneralizes-text-monitor-unreachability`, Priority `major`, Status `Pending`
    - .planning/STATE.md (lines 1825-1845) — the v0.7.0 audit-acknowledgement table, which carries a row for the same stem WITH a `.md` extension; that row is history and stays
    - .planning/todos/pending/2026-08-28-phase-7-pitfall-5-overgeneralizes-text-monitor-unreachability.md — the file being moved, including its `resolves_phase: 39` frontmatter
  </read_first>
  <files>.planning/todos/completed/2026-08-28-phase-7-pitfall-5-overgeneralizes-text-monitor-unreachability.md, .planning/STATE.md</files>
  <action>
Close the todo in a single commit, because `docs-deferred-ledger.test.ts` fails in
**both** directions: a file moved to `completed/` while its row survives in
`## Deferred Items` reds direction B, and a row removed while the file is still in
`pending/` reds direction A.

Move the file with `git mv` from `.planning/todos/pending/` to
`.planning/todos/completed/`, keeping the filename byte-identical. Append a short
`## Resolution` section to the moved file recording: resolved by Phase 39 plan
`39-02`, the date, the four edits made (naming each file by path), and that the
sibling warp/headless todo was re-checked and deliberately left open with the
finding recorded in `39-02`'s SUMMARY.

Then remove exactly one row from `.planning/STATE.md`'s `## Deferred Items` table
— the row whose Item cell is
`2026-08-28-phase-7-pitfall-5-overgeneralizes-text-monitor-unreachability` (no
`.md` extension; the ledger keys on the stem). Leave every other row in that table
untouched, and leave the v0.7.0 audit-acknowledgement table's row for the same
stem **in place**: that row records what was deferred at that milestone's close, it
carries the `.md` extension and a `(presence-only)` status, and the ledger guard
does not read it. Removing it would falsify a historical record and is not what
resolving the todo means.

Update the parenthetical count sentence immediately below the Deferred Items table
if it states a row count that the removal makes wrong; if it states only filing
dates, leave it alone. Read it before editing rather than assuming its shape.

Edit `STATE.md` with a scoped, line-targeted edit. Do not rewrite the file, do not
run a `state.*` mutation verb for this, and diff the result before committing —
the `state.add-blocker` and `state.update` verbs are recorded in this project's
operating notes as corrupting nearby prose.

Stage both paths — the `git mv` pair and `STATE.md` — and commit them together.
Then run the guard and confirm `fail 0` in both directions, and run
`npm run test:automated` and record the observed count and failing file names in
the SUMMARY as a relation against the recorded baseline of 2 failing tests in
`anno-register.test.ts` alone. Do not write a zero-failure claim.
  </action>
  <acceptance_criteria>
    - `.planning/todos/pending/` no longer contains the stem, and `.planning/todos/completed/` contains it with a byte-identical filename
    - The moved file carries a `## Resolution` section naming Phase 39 plan `39-02` and the four edited paths
    - `.planning/STATE.md`'s `## Deferred Items` table has exactly zero rows whose Item cell is the stem
    - `.planning/STATE.md`'s v0.7.0 audit-acknowledgement table still carries its row for the same stem with the `.md` extension
    - `node --test docs-deferred-ledger.test.ts` reports `fail 0`
    - The file move and the row removal are in the same commit — `git show --name-only` on that commit lists both the completed path and `.planning/STATE.md`
    - `npm run test:automated` shows no failing test outside `anno-register.test.ts`, and the observed count is recorded in the SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && S=2026-08-28-phase-7-pitfall-5-overgeneralizes-text-monitor-unreachability && test ! -e ".planning/todos/pending/$S.md" && test -e ".planning/todos/completed/$S.md" && grep -ac 'Resolution' ".planning/todos/completed/$S.md" && DEF=$(sed -n '/^## Deferred Items$/,/^## /p' .planning/STATE.md | grep -ac "| $S |" || true) && test "$DEF" -eq 0 && test "$(grep -ac "| $S.md |" .planning/STATE.md)" -ge 1 && echo LEDGER_MOVE_OK</automated>
    <fails_when>non-zero exit, or `LEDGER_MOVE_OK` absent from stdout — which happens when the file is still in `pending/`, absent from `completed/`, carries no Resolution section, still has a Deferred Items row, or when the historical audit-acknowledgement row was deleted along with it</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-deferred-ledger.test.ts 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, or the summary line does not report `fail 0` — the guard reds in direction B when a completed todo still has a Deferred Items row and in direction A when a pending todo has none</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated 2>&1 | tail -30</automated>
    <fails_when>any failing test reported in a file other than `anno-register.test.ts`, or a total failure count above the 2 this tree's recorded baseline carries — the relation is what is asserted, never an absolute count of zero</fails_when>
    <human-check>The corrected Pitfall 5 no longer leaves the console-exclusivity generalization standing unqualified anywhere in `07-RESEARCH.md`, and Phase 7's decision itself is unchanged.</human-check>
  </verify>
  <done>The todo is in `.planning/todos/completed/` with a Resolution section, its `## Deferred Items` row is gone, the historical v0.7.0 audit row is intact, both landed in one commit, `docs-deferred-ledger.test.ts` reports `fail 0`, and `test:automated` shows no failure outside `anno-register.test.ts`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a planning artifact's stated fact → a later phase's decision | `07-RESEARCH.md`'s Pitfall 5 is what Phase 7 acted on; a wrong fact in a planning artifact crosses into eight phases of behaviour with nothing checking it |
| project instructions (`CLAUDE.md`) → every agent in every session | A constraint deleted rather than scoped removes a true fact from every future agent's context |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-06 | Tampering | `CLAUDE.md` Constraints section | high | mitigate | All three bullets keep their original assertion verbatim and gain a clause beginning `binary monitor only`; the verify asserts at least three such clauses exist AND that `docs-linerefs.test.ts` still reports `fail 0`, so a reflow of the pinned Architecture bullet reds the task rather than shipping silently. |
| T-39-07 | Repudiation | `07-RESEARCH.md` | medium | mitigate | The correction carries a visible marker naming Phase 39 and cites the live-probe note by path, so a later reader can tell corrected text from original text; Phase 7's decision is preserved and the false premise recorded explicitly rather than the record being quietly rewritten to look right all along. |
| T-39-08 | Tampering | `.planning/STATE.md` | high | mitigate | Scoped line-targeted edit only, no whole-file write and no `state.*` mutation verb (both recorded in this project's operating notes as corrupting nearby prose); the verify asserts the `## Deferred Items` row is gone AND the historical audit-acknowledgement row survives, so an over-broad deletion reds the task. |
| T-39-09 | Repudiation | the two-directional ledger guard | medium | mitigate | The file move and the row removal are staged and committed together; `docs-deferred-ledger.test.ts` is run after the commit and its `fail 0` is the evidence, in both directions. |
| T-39-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan or anywhere in this phase — `39-RESEARCH.md` § *Package Legitimacy Audit* records "Not applicable — this phase installs no external package". Accepted because the attack surface is absent, not tolerated. |
</threat_model>

## Artifacts this phase produces

Symbols and files created by **Phase 39 as a whole** (listed in every plan so the
source-grounding pass can exclude newly-created names from drift verification).

**New files — planning/evidence** (all under
`.planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/`)
`DECISION-RULE.md`, `SCHEMA.md`, `README.md`, `totality-walk.mjs`,
`39-totality.md`, `probe-harness.mjs`, `textmon-probe-client.mjs`,
`idle-coexist-probe.mjs`, `39-idle-coexist.md`, `foreign-halt-probe.mjs`,
`39-foreign-halt.md`, `cross-channel-resume-probe.mjs`,
`39-cross-channel-resume.md`, `concurrent-inflight-probe.mjs`,
`39-concurrent-inflight.md`, `hitcount-invariant-probe.mjs`,
`39-hitcount-invariant.md`, `disconnect-recovery-probe.mjs`,
`textmon-kill-victim.mjs`, `39-disconnect-recovery.md`,
`text-single-client-probe.mjs`, `39-text-single-client.md`,
`fixture-capture.mjs`, `39-fixture-batch.md`

**New files — source and docs**
`src/mcp/vice/textmon-fixtures.ts`, `src/mcp/vice/textmon-fixtures.test.ts`,
`src/mcp/vice/fixtures/textmon/README.md`,
`src/mcp/vice/fixtures/textmon/*.txt` + `*.json` sidecar pairs,
`docs/phase39-dual-channel-coexistence-gate-findings.md`

**New outcome-line key names (gate inputs)**
`IDLE_COEXIST`, `FOREIGN_HALT_VISIBILITY`, `CONCURRENT_INFLIGHT`,
`CROSS_CHANNEL_RESUME`, `DISCONNECT_RECOVERY`, `HITCOUNT_INVARIANT_HOLDS`,
`TEXT_SINGLE_CLIENT`

**New outcome-line key names (recorded facts, never gate inputs)**
`IDLE_TEXT_CLIENT_HALTS`, `REMOTEMONITOR_FLAG_ORDER`,
`TEXT_PROMPT_LITERAL_CONFIRMED`, `TEXT_BIND_BUDGET_MS_MAX`,
`TEXT_SECOND_CONNECT_OBSERVATION`, `CONCURRENT_WRITE_GAP_MS`, `FIXTURE_COUNT`,
`FIXTURE_BINARIES`, `FIXTURE_ENCODING`, `FIXTURE_DIVERGENCE`,
`FIXTURE_UNSUPPORTED`, `TOTAL_TUPLES`, `TOTALITY`, `TSC_INDEPENDENCE`,
`COULD_NOT_RUN_EMITTABLE`, `RULE_HIT_R1` .. `RULE_HIT_R15`, `BROKER_STATE`,
`TEST_AUTOMATED_BASELINE`, `TEST_AUTOMATED_BASELINE_FILES`, `VICE_BINARY`,
`VICE_VERSION_OBSERVED`

**New verdict-document frontmatter fields**
`phase`, `requirement`, `probe_date`, `verdict`, `verdict_rule_applied`,
`inputs.idle_coexist`, `inputs.foreign_halt_visibility`,
`inputs.concurrent_inflight`, `inputs.cross_channel_resume`,
`inputs.disconnect_recovery`, `inputs.hitcount_invariant_holds`,
`inputs.text_single_client`

**New symbols — `evidence/totality-walk.mjs`**
`DOMAINS`, `RULES`, `crossProduct()`

**New symbols — `evidence/probe-harness.mjs`**
`VICE_STOCK`, `VICE_FORK`, `PROBE_DIR`, `viceKind()`, `viceVersion()`,
`systemdBrokerState()`, `aliveX64sc()`, `preflight()`, `allocPorts()`,
`buildProbeArgs()`, `spawnVice()`, `reapAll()`, `connectWithRetry()`,
`pingReady()`, `armNonStoppingExec()`, `checkpointHitCount()`,
`testAutomatedBaseline()`, `log()`, `sleep()`

**New symbols — `evidence/textmon-probe-client.mjs`**
`PROMPT_RE`, `connectTextMonitor()`, `awaitBanner()`, `sendAndAwaitPrompt()`,
`closeTextMonitor()`

**New symbols — `src/mcp/vice/textmon-fixtures.ts`**
`REQUIRED_PROVENANCE_KEYS`, `TEXTMON_FIXTURE_DIR`, `MissingTextFixtureError`,
`listTextFixtures()`, `loadTextFixture()`, type `TextFixture`,
type `LoadTextFixtureOptions`, type `MissingTextFixtureErrorOptions`

**Modified existing files (no new symbols)**
`.planning/phases/07-cycle-timing-and-wedge-triage/07-RESEARCH.md`, `CLAUDE.md`,
`.planning/notes/stock-vice-migration-revised-loss-ledger.md`,
`.planning/STATE.md`, `.planning/ROADMAP.md`, and the move of
`.planning/todos/pending/2026-08-28-phase-7-pitfall-5-overgeneralizes-text-monitor-unreachability.md`
to `.planning/todos/completed/`

<verification>
- `07-RESEARCH.md` names `-remotemonitor` and `monitor_network.c` in both corrected locations, and still carries the surviving narrow claim
- `CLAUDE.md` carries three `binary monitor only` scoping clauses with all original constraint text intact
- `node --test docs-linerefs.test.ts docs-dangling-refs.test.ts docs-deferred-ledger.test.ts` reports `fail 0`
- The todo is in `completed/`, its `## Deferred Items` row is gone, and the historical v0.7.0 audit row survives — all in one commit
- `npm run test:automated` shows no failing test outside `anno-register.test.ts`
</verification>

<success_criteria>
The premise this phase measures is stated correctly in the artifact that got it
wrong; the three constraints it distorted are scoped rather than deleted; and the
todo is closed through a guard that fails in both directions rather than by a
half-finished bookkeeping edit.
</success_criteria>

<output>
Create `.planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/39-02-SUMMARY.md` when done
</output>