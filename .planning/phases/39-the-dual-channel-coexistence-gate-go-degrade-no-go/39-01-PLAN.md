---
phase: 39-the-dual-channel-coexistence-gate-go-degrade-no-go
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/DECISION-RULE.md
  - .planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/SCHEMA.md
  - .planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/README.md
  - .planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/totality-walk.mjs
  - .planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/39-totality.md
autonomous: false
requirements: [CHAN-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CHAN-01 (D-01): the decision rules exist as a committed file whose adding commit is the ONLY commit reachable from itself that touches the phase evidence directory — provable with `git rev-list`, no test guard"
    - "CHAN-01 (D-03): the verdict field's accepted values are exactly `go` / `degrade` / `no-go`; `could-not-run` has no antecedent to fire on, and every one of the seven inputs carries an explicit `not-taken` member instead"
    - "CHAN-01 (D-02): the seven gate inputs each have exactly one declared outcome-line name, one frozen value domain and one declared source file, so no measuring plan can invent a line name or a definition that flatters its own result"
    - "CHAN-01 (D-04): totality is discharged by a committed executable walk that enumerates all 3,888 tuples, asserts exactly one antecedent per tuple, and emits the tuple count plus a per-rule hit histogram as column-0 outcome lines"
    - "CHAN-01 (D-08): `TEXT_SINGLE_CLIENT` appears in no rule antecedent, proved mechanically rather than promised — varying only that input never changes which rule fires, over all 1,296 tuple groups"
    - "CHAN-01 (D-10, D-11): the `degrade` narrowing for `DISCONNECT_RECOVERY` and `HITCOUNT_INVARIANT_HOLDS` is written down while both answers are still unknown, and the other five inputs are declared to narrow at verdict time and labelled as such"
    - "CHAN-01 (edge: boundary): the rule set resolves to exactly one of `go` / `degrade` / `no-go` at every rule-order boundary across all 3,888 tuples of the seven declared domains, including the all-worst tuple and the all-`not-taken` tuple — asserted by the executable walk, not by prose"
    - "CHAN-01 (edge: adjacency): rules are first-match-wins and the verdict document records WHICH rule id fired, so two rules can never both be \"the first match\" for one input tuple; the walk fails loudly on any tuple matching more than one antecedent"
    - "CHAN-01 (edge: empty): an absent gate-input line is declared an incomplete phase rather than a pass, a default or a defensible state, and `not-taken` is declared an explicitly written value that no plan may reach by omission"
    - "CHAN-01 (edge: encoding): a match is defined as a bare `NAME: value` at column 0 of the single declared source file, final occurrence wins, one declared source file per line — so \"the recorded value\" has one byte-level definition rather than a reader's judgement"
    - "CHAN-01 (edge: ordering): the rules are a numbered ordered list evaluated in written order by the walk script, and the pre-commitment's own ordering is proved by commit order banked as real `git log` / `git rev-list` output"
    - statement: "No gate input is numeric, so no rounding, overflow or tie-breaking contract applies to the verdict; the one recorded number that could tempt a threshold (the concurrent-write overlap gap in ms) is declared Never a gate"
      verification: backstop
  artifacts:
    - path: ".planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/DECISION-RULE.md"
      provides: "The seven named inputs, rules R1..R15 first-match-wins, the D-10/D-11 pre-mapped degrade narrowings, the three-shape implication table, the Never a gate declarations, and the frozen-after-first-measurement status statement"
      contains: "first match wins"
    - path: ".planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/SCHEMA.md"
      provides: "Every outcome-line name, its complete value domain, its single declared source file, and the derivation rule that produces its value from raw measurement"
      contains: "IDLE_COEXIST"
    - path: ".planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/README.md"
      provides: "The artifact table, the numbered evidence conventions binding on every plan in the phase, and the banked ordering proof"
      contains: "Ordering proof"
    - path: ".planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/totality-walk.mjs"
      provides: "The executable totality walk over the 3,888-tuple cross-product, asserting exactly one antecedent per tuple and emitting the histogram"
      contains: "TOTAL_TUPLES"
    - path: ".planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/39-totality.md"
      provides: "The walk's real transcript plus its column-0 outcome lines, banked in the same commit as the rules"
      contains: "TOTALITY:"
  key_links:
    - from: ".planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/SCHEMA.md"
      to: ".planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/39-idle-coexist.md"
      via: "the declared outcome line `IDLE_COEXIST:` at column 0, written by 39-03"
      pattern: "IDLE_COEXIST:"
    - from: ".planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/DECISION-RULE.md"
      to: "docs/phase39-dual-channel-coexistence-gate-findings.md"
      via: "39-08 reproduces the fired rule verbatim and walks the seven recorded values through it"
      pattern: "verdict_rule_applied"
    - from: ".planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/totality-walk.mjs"
      to: ".planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/DECISION-RULE.md"
      via: "the RULES array mirrors the numbered rule list 1:1 in the same written order"
      pattern: "RULES"
  prohibitions:
    - statement: "MUST NOT edit, refine, re-scope, relax or \"clarify\" any rule, value domain or derivation in this plan's files after the first measurement commit lands — including by a later plan that believes it is only resolving an ambiguity; an ambiguity found later is recorded as an ACCEPTED LIMIT in the measuring plan's own evidence file and as an explicit override in the findings document"
      status: resolved
      verification: judgment
    - statement: "MUST NOT author or revise the pre-mapped degrade narrowing for DISCONNECT_RECOVERY or HITCOUNT_INVARIANT_HOLDS once either value is known, and MUST NOT present a verdict-time narrowing for the other five inputs as though it were a pre-commitment"
      status: resolved
      verification: judgment
    - statement: "MUST NOT declare any eighth gate input, and MUST NOT promote a recorded non-gating fact into a rule antecedent"
      status: resolved
      verification: judgment
---

<objective>
Commit the binding go / degrade / no-go decision rules for `CHAN-01`, the frozen
outcome-line schema, the evidence conventions and the **executable** totality
walk — **before any measurement in this phase exists**, so the verdict is derived
rather than judged.

Purpose: this is the fourth time this project makes an assumption probe a phase
rather than a criterion, and the first three all fired (Phase 9's `R4` →
`degrade`, Phase 23's `R1` → `no-go` with five of eleven plans deliberately never
dispatched, Phase 33's `R6` → `degrade`). The distinguishing property this time is
the inverse of Phase 33's: **every** input is corpus-free, so the abstention risk
is not a missing corpus but an **untakeable experiment**, and `D-03`'s explicit
`not-taken` member is what absorbs it.

Output: five committed files under the phase evidence directory, landed as **one**
commit — the rules, the schema, the conventions plus banked ordering proof, the
totality-walk script, and the walk's own recorded output. This plan touches
**nothing else** (`D-01`): git history is the whole mechanism, and a plan that also
shipped code would make the ordering claim ambiguous.

**Ordering contract:** every measuring plan in this phase depends on this one.
`39-02` runs concurrently in wave 1 but writes nothing under `evidence/`, so it
cannot disturb the proof — the proof counts commits touching `evidence/`, not
commits.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/39-CONTEXT.md
@.planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/39-RESEARCH.md
@.planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/39-PATTERNS.md
</context>

<assumption_delta_decision>
**Signal:** `pluralization`, matched token `"Second,"` in ROADMAP criterion 3.

**The noun at stake:** the monitor **channel**. Since Phase 0 there has been
exactly one channel (binary), and `channel` has existed only as a bookkeeping
discriminator in comments and instance records. This milestone adds a second
(text), and `CHAN-04` requires both to pass through a single serialization
authority — so the live question is whether `channel` gets **promoted** to the
primary identity every halt-taking operation is keyed by, or stays a
discriminator alongside a binary-primary model.

**Decision: `no-change`, for this phase only.** The matched token is a prose
ordinal, not an architectural transition, but the substance behind it is real —
and it is explicitly **not this phase's to answer**. `D-01`/`D-13` and the ROADMAP
Notes forbid building any production module here; the identity model is selected
by the verdict this phase records, and its owner is **Phase 41**, whose choice
between the in-process mutex, the broker lease and the connect-gate *is* the
promote-vs-add-alongside decision in concrete form. Recording `promote` or
`add-alongside` here would pre-empt the gate.

**Not accepted debt** — nothing is being deferred that this phase could have
shipped. The companion invariant test is out of scope by `D-19`, which caps this
phase at exactly one new corpus-free automated test.
</assumption_delta_decision>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the rule text, the rule ordering and the three flagged items before they are frozen</name>
  <decision>Freeze rules `R1`..`R15`, the seven input names with their value domains, and the two pre-mapped `degrade` narrowings exactly as drafted in Task 2's action — or adjust a named value, domain or rule position before the commit lands.</decision>
  <context>
    This is the phase's one genuine one-way door. The rules' entire value is that
    their commit **precedes** every measurement commit they score; once a
    measurement has been committed, the rules can never again be shown to predate
    it, and every future gate in this project becomes advisory. There is no
    migration and no revision path — only an explicitly recorded override, which
    weakens exactly the pre-commitment it exists to provide.

    What is NOT being re-asked: the seven-input set (D-02), the no-abstain
    assertion (D-03), the executable-walk requirement (D-04), the verdict document
    and its frontmatter shape (D-05), the ROADMAP+STATE binding with no test guard
    (D-06), the three-file evidence layout (D-07), and `TEXT_SINGLE_CLIENT` gating
    nothing (D-08) are all recorded in 39-CONTEXT.md as settled by the owner.

    What IS being confirmed is the part no source artifact contains yet — the
    **rule text and ordering** — plus the three items 39-CONTEXT.md itself names as
    most worth a second look before `39-01` is committed:

    1. **`D-09`'s `not-taken` → `no-go` arm.** 39-CONTEXT.md states plainly that
       this is the one place it extended the owner's answer rather than recording
       it: the owner chose "idle coexistence fails" as the sole `no-go`, and the
       untakeable case was reasoned out afterwards. As drafted, `R2` makes
       `IDLE_COEXIST: not-taken` a `no-go` on the stated ground that an unmeasured
       coexistence precondition cannot license a design that assumes coexistence,
       and that `IDLE_COEXIST` is the cheapest experiment and a precondition for
       the other four. **If that reasoning is wrong, a host hiccup cancels the
       concurrent design.** Research assumption `A1` is exactly such a hiccup:
       nothing has ever dialed the `-remotemonitor` port, and if `-remotemonitor`
       turns out to need the same `-default`-first ordering discipline the binary
       monitor does and the probe gets it wrong, every experiment reports
       `not-taken` for a reason unrelated to real incompatibility.
    2. **`D-02`'s seven value domains.** They are indicative in 39-CONTEXT.md and
       become frozen the moment this commit lands. Task 2's action states them
       verbatim.
    3. **`D-11`'s choice of which two inputs get pre-mapped narrowing.** Getting
       this wrong means a `degrade` whose narrowing was authored after the numbers
       were visible — the exact failure mode the gate exists to prevent.

    Phase 23 took this identical gate at its own `23-01` Task 1 and recorded the
    selection `proceed`; Phase 33 did the same at `33-01` Task 1.

    This checkpoint is `blocking`. It must not be self-answered: an autonomous
    executor that approves its own pre-commitment has produced no pre-commitment.
  </context>
  <options>
    <option id="proceed">
      <name>proceed — freeze R1..R15 exactly as drafted</name>
      <pros>`IDLE_COEXIST` is the sole `no-go` trigger exactly as the owner chose (D-09), and it fires through two separately numbered rules so the record distinguishes a measured corruption from an untakeable experiment. `TEXT_SINGLE_CLIENT` appears in no antecedent, and the walk proves that mechanically rather than promising it. `go` is reachable only at all six best values, so it can never be reached by an absence. `R15` carries no antecedent, which is what makes `could-not-run` structurally unemittable rather than merely discouraged.</pros>
      <cons>`R2` makes an untakeable idle-coexistence experiment a `no-go`, which means a probe defect — a wrong `-remotemonitor` flag position, a wrong prompt terminator, an under-budgeted connect retry — reaches the same verdict as a measured incompatibility. The mitigation is procedural, not structural: the probe records `REMOTEMONITOR_FLAG_ORDER:`, `TEXT_PROMPT_LITERAL_CONFIRMED:` and `TEXT_BIND_BUDGET_MS_MAX:` as facts so a reader can tell the two apart, but the verdict is the same either way.</cons>
    </option>
    <option id="soften-r2">
      <name>soften — make `IDLE_COEXIST: not-taken` a `degrade` instead of a `no-go`</name>
      <pros>A host or probe limitation can no longer cancel the concurrent design. Keeps `no-go` for the one case the owner actually named: a measured corruption of an idle read.</pros>
      <cons>Contradicts the reasoning 39-CONTEXT.md recorded under D-09, and licenses a `degrade` verdict — which selects a serialization shape for Phase 41 to build — off a coexistence precondition nobody ever observed. All three surviving shapes assume the idle case is safe; only the connect-gate survives its being unsafe, and `degrade` does not select the connect-gate.</cons>
    </option>
    <option id="adjust">
      <name>adjust — change a named value, domain, derivation or rule position, then freeze</name>
      <pros>Any concern with a specific rule, domain member or derivation is fixed while the rules are still revisable at zero cost.</pros>
      <cons>Requires naming the exact change; a vague "make it stricter" cannot be committed as a rule.</cons>
    </option>
  </options>
  <resume-signal>Reply `proceed`, `soften-r2`, or `adjust: &lt;the exact value, domain, derivation or rule position to change&gt;`. The selection is transcribed verbatim into DECISION-RULE.md's `**Decision checkpoint.**` paragraph.</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: The rules, the schema, the conventions and the executable walk — authored, run, committed as ONE commit, and the ordering proved end to end</name>
  <reversibility rating="one-way">Once a measurement commit lands under `evidence/`, no later commit can make these rules predate it; ordering is the entire mechanism and there is no revision path, only a recorded override that weakens the pre-commitment (D-01, D-03).</reversibility>
  <read_first>
    - .planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/evidence/DECISION-RULE.md — the precedent this mirrors: the frozen-status header, the Inputs table, "Rules, first match wins", the pre-mapped-narrowing paragraphs, `## Totality`, `## Never a gate`, and the `**Decision checkpoint.**` paragraph
    - .planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/evidence/SCHEMA.md — §1 outcome-line conventions (copy verbatim, retargeted from five gate inputs to seven) and §2's per-input derivation-rule shape
    - .planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/evidence/README.md — the artifact table's three-column discipline, the seven numbered evidence conventions, and the `## Ordering proof` section shape with its real banked git output
    - docs/phase33-reproducible-run-gate-findings.md (frontmatter, lines 1-30) — the exact frontmatter key order D-05 mirrors: `phase`, `requirement`, `probe_date`, `verdict`, `verdict_rule_applied`, `inputs:` with one commented citation per value
    - .planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/39-CONTEXT.md — D-01 through D-20 verbatim, with the D-02 seven-input table and the three flagged second-look items
    - .planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/39-RESEARCH.md — §Summary, Pattern 3 (ordering-proof commands), Pattern 4 (the 3,888 arithmetic and the walk skeleton), §Common Pitfalls 1-6, §Assumptions Log A1-A5, §Open Questions 1-3
    - src/mcp/vice/vice-sync.ts (lines 20-45 and 210-280) — the two documented invariants, read for the invariant TEXT only; this file is not edited by this phase and is not the seam any of the three shapes extends
    - src/mcp/vice/stock-protocol.ts (lines 95-130) — the `CommandType` map, so SCHEMA.md's derivations name real opcodes (`MemoryGet` 0x01, `CheckpointGet` 0x11, `CheckpointSet` 0x12, `AdvanceInstructions` 0x71, `Ping` 0x81, `Exit` 0xaa)
  </read_first>
  <files>.planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/DECISION-RULE.md, .planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/SCHEMA.md, .planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/README.md, .planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/totality-walk.mjs, .planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/39-totality.md</files>
  <action>
Create the phase evidence directory, author all five files, run the walk, paste
its real output into `39-totality.md`, then stage and commit all five in a
**single** commit. One commit is load-bearing: the ordering proof asserts that
exactly one commit reachable from the rules commit touches `evidence/`, which a
follow-up commit would break.

Do the work in this order: draft the rules, write the walk against them, run the
walk, fix the RULES array or the rule text if the walk reports a tuple matching
zero or more than one antecedent, re-run until clean, bank the output, then
commit once.

**File 1 — `evidence/DECISION-RULE.md`.** Mirror Phase 33's structure.

Opening block, same register: "Binding. Stated here, before the run, so the
verdict is derived and not judged." Read the inputs from the evidence files, never
from a SUMMARY's paraphrase; evaluate the rules in written order; take the first
that matches; record which rule fired. Then a `**Status: pre-commitment,
frozen.**` paragraph: nothing in this file may be edited, refined, re-scoped or
clarified once the first measurement commit lands, including by a later plan that
believes it is only resolving an ambiguity — such an ambiguity is recorded as an
`## ACCEPTED LIMIT` in the measuring plan's own evidence file and as an explicit
override in the findings document, and the rule text does not move. Then a
`**Decision checkpoint.**` paragraph transcribing Task 1's selection verbatim.

`## Inputs` — the seven inputs (D-02), each with its outcome line and declared
source file, pointing at `SCHEMA.md` for the domains and derivations:

| Input | Domain | Declared source file |
|---|---|---|
| `IDLE_COEXIST` | clean / corrupts / not-taken | `evidence/39-idle-coexist.md` |
| `FOREIGN_HALT_VISIBILITY` | visible / invisible / corrupts / not-taken | `evidence/39-foreign-halt.md` |
| `CONCURRENT_INFLIGHT` | clean / degraded / corrupts / not-taken | `evidence/39-concurrent-inflight.md` |
| `CROSS_CHANNEL_RESUME` | clean / corrupts / not-taken | `evidence/39-cross-channel-resume.md` |
| `DISCONNECT_RECOVERY` | recovers / leaves-halted / not-taken | `evidence/39-disconnect-recovery.md` |
| `HITCOUNT_INVARIANT_HOLDS` | holds / breaks / not-taken | `evidence/39-hitcount-invariant.md` |
| `TEXT_SINGLE_CLIENT` | single / multi / not-taken | `evidence/39-text-single-client.md` |

Then state, in the same register Phase 33 used: every input above MUST exist in
its named file carrying one of the values `SCHEMA.md` declares for it; where a
line appears more than once the final occurrence wins; an absent line is not a
pass, not a default and not a defensible state — it is an incomplete phase.

Then a `## The distinguishing property of this gate` section stating the
**inversion** of Phase 33's, explicitly rather than by omission: Phase 23's gate
could return `could-not-run` because every input needed a corpus; Phase 33
removed that by making four of five inputs corpus-free; here **all seven are
corpus-free** (no cracked release is involved anywhere), so the abstention risk is
not a missing corpus but an **untakeable experiment**, absorbed by `not-taken` as
an input value. State the check this places on every rule: if a rule can be
reached only when every input has a measured value, the rule is wrong.

`## Rules, first match wins` — exactly these fifteen, in this order, each with the
reasoning paragraph that makes it re-derivable:

- **R1 → `no-go`.** `IDLE_COEXIST` is `corrupts`. A non-halting read on the binary
  channel returning wrong data, erroring or desyncing merely because a silent text
  client is connected is unfixable by discipline: the in-process mutex, the
  broker-level lease and the connect-gate all assume the idle case is safe, and
  only the connect-gate — the two channels time-sharing and never coexisting live
  — survives. Sole `no-go` trigger per D-09.
- **R2 → `no-go`.** `IDLE_COEXIST` is `not-taken`. Same verdict by a different
  rule and a different stated reason (D-09): an unmeasured coexistence
  precondition cannot license a design that *assumes* coexistence, and because
  `IDLE_COEXIST` is the cheapest experiment and a precondition for the other four,
  its being untakeable means none of them were taken either. Numbered separately
  from `R1` so the record distinguishes a measured corruption from an untakeable
  experiment.
- **R3 → `degrade`.** `IDLE_COEXIST` is `clean` **and** `FOREIGN_HALT_VISIBILITY`
  is `corrupts`. Narrowing authored at verdict time against the evidence, Phase 9
  style, and labelled as such.
- **R4 → `degrade`.** `IDLE_COEXIST` is `clean` **and** `FOREIGN_HALT_VISIBILITY`
  is `invisible`. Verdict-time narrowing.
- **R5 → `degrade`.** `IDLE_COEXIST` is `clean` **and** `FOREIGN_HALT_VISIBILITY`
  is `not-taken`. Verdict-time narrowing.
- **R6 → `degrade`.** `IDLE_COEXIST` is `clean`, `FOREIGN_HALT_VISIBILITY` is
  `visible` **and** `CONCURRENT_INFLIGHT` is `corrupts`. Verdict-time narrowing.
- **R7 → `degrade`.** `IDLE_COEXIST` is `clean`, `FOREIGN_HALT_VISIBILITY` is
  `visible` **and** `CONCURRENT_INFLIGHT` is `degraded`. Verdict-time narrowing.
- **R8 → `degrade`.** `IDLE_COEXIST` is `clean`, `FOREIGN_HALT_VISIBILITY` is
  `visible` **and** `CONCURRENT_INFLIGHT` is `not-taken`. Verdict-time narrowing.
- **R9 → `degrade`.** `IDLE_COEXIST` is `clean`, `FOREIGN_HALT_VISIBILITY` is
  `visible`, `CONCURRENT_INFLIGHT` is `clean` **and** `CROSS_CHANNEL_RESUME` is
  `corrupts`. Verdict-time narrowing.
- **R10 → `degrade`.** `IDLE_COEXIST` is `clean`, `FOREIGN_HALT_VISIBILITY` is
  `visible`, `CONCURRENT_INFLIGHT` is `clean` **and** `CROSS_CHANNEL_RESUME` is
  `not-taken`. Verdict-time narrowing.
- **R11 → `degrade`.** `IDLE_COEXIST` is `clean`, `FOREIGN_HALT_VISIBILITY` is
  `visible`, `CONCURRENT_INFLIGHT` is `clean`, `CROSS_CHANNEL_RESUME` is `clean`
  **and** `DISCONNECT_RECOVERY` is `leaves-halted`. **Pre-mapped
  narrowing (D-10):** a killed text client leaving the machine permanently halted
  and indistinguishable from a genuine wedge is a **missing mechanism**, not a
  proof of incompatibility — the binary side's "connection close IS the release"
  (`broker-control.mts` ~line 522) has no text-channel analogue, and only the
  broker outlives a killed client. So this outcome points **at** the broker-lease
  shape rather than away from coexistence, and the fix — a broker-held
  halt-authority lease released when the holding connection dies — becomes named
  Phase 41 scope in the verdict, discovered here rather than at Phase 41's gate.
- **R12 → `degrade`.** `IDLE_COEXIST` is `clean`, `FOREIGN_HALT_VISIBILITY` is
  `visible`, `CONCURRENT_INFLIGHT` is `clean`, `CROSS_CHANNEL_RESUME` is `clean`
  **and** `DISCONNECT_RECOVERY` is `not-taken`. Verdict-time narrowing.
- **R13 → `degrade`.** `IDLE_COEXIST` is `clean`, `FOREIGN_HALT_VISIBILITY` is
  `visible`, `CONCURRENT_INFLIGHT` is `clean`, `CROSS_CHANNEL_RESUME` is `clean`,
  `DISCONNECT_RECOVERY` is `recovers` **and** `HITCOUNT_INVARIANT_HOLDS` is
  `breaks`. **Pre-mapped
  narrowing (D-11):** every stock module that upholds "poll on `hit_count`, never
  on paused state" natively — `stock-checkpoints.ts`, `stock-run-until.ts`,
  `stock-reproducible-run.ts`, `stock-diagnose.ts` — must gain foreign-halt
  discrimination, and Phase 41's selected shape must supply it. Pre-mapped
  precisely because this is where an after-the-fact author would be most tempted
  to declare the existing invariant already sufficient.
- **R14 → `degrade`.** `IDLE_COEXIST` is `clean`, `FOREIGN_HALT_VISIBILITY` is
  `visible`, `CONCURRENT_INFLIGHT` is `clean`, `CROSS_CHANNEL_RESUME` is `clean`,
  `DISCONNECT_RECOVERY` is `recovers` **and** `HITCOUNT_INVARIANT_HOLDS` is
  `not-taken`. Verdict-time narrowing.
- **R15 → `go`.** Reached only when `IDLE_COEXIST: clean`,
  `FOREIGN_HALT_VISIBILITY: visible`, `CONCURRENT_INFLIGHT: clean`,
  `CROSS_CHANNEL_RESUME: clean`, `DISCONNECT_RECOVERY: recovers` and
  `HITCOUNT_INVARIANT_HOLDS: holds`, at **any** value of `TEXT_SINGLE_CLIENT`.
  `R15` has **no antecedent** — it is the exhaustive default, which is what makes
  `could-not-run` structurally unemittable (D-03) rather than merely discouraged.

Immediately after the list, state the **disjointness design** and why the
antecedents are conditioned rather than bare. Each of `R3`..`R14` explicitly
requires every input **earlier in the reading order** to hold its best value, so
`R1`..`R14` are pairwise mutually exclusive: no tuple can satisfy two of them, and
`R15` fires exactly when none of them does. First-match-wins is therefore a
statement about how a reader proceeds, not the mechanism that disambiguates an
overlap — which is what lets the walk assert the strong property `D-04` asks for
("exactly one antecedent matches each") instead of the weak one ("at least one").
State the concrete consequence a reader can check: without the conditioning, a
tuple carrying both `DISCONNECT_RECOVERY: leaves-halted` and
`HITCOUNT_INVARIANT_HOLDS: breaks` would satisfy two antecedents at once and the
record would not say which narrowing applied; with it, `R11` fires, `R13` does not,
and the second finding is carried in the evidence and in the findings document's
per-input sections rather than in the rule id. Say that plainly, because it is the
one place the rule set gives a reader less than the evidence does.

Then close that gap with a binding sentence, in its own short
`## Narrowings are not scoped to the fired rule` subsection: **both pre-mapped
narrowings apply whenever their own input carries its triggering value,
independently of which rule id fired.** The rule id records which antecedent came
first in the reading order; it does not record which narrowings are in force. So a
verdict reached through `R11` still carries `R13`'s pre-mapped narrowing if
`HITCOUNT_INVARIANT_HOLDS` reads `breaks`, and the findings document is required to
state every narrowing whose triggering value was recorded. Without this sentence
the conditioning would silently discard a finding, which is the opposite of what
the conditioning is for.

Then `## Totality` — state the arithmetic and point at the executable walk rather
than tabulating by hand: seven inputs with domains of size 3, 4, 4, 3, 3, 3, 3
give 3 x 4 x 4 x 3 x 3 x 3 x 3 = **3,888** tuples, roughly 36 times Phase 33's
108, which D-04 records as already at the limit of what a reader can check in
prose. Name `totality-walk.mjs` as the proof and `39-totality.md` as its recorded
output, and state the expected histogram so a reader can check the walk rather
than trust it: R1 1296, R2 1296, R3 324, R4 324, R5 324, R6 81, R7 81, R8 81,
R9 27, R10 27, R11 9, R12 9, R13 3, R14 3, R15 3 — summing to 3,888. Note the
load-bearing consequence of `RULE_HIT_R15: 3`: the `go` branch is reached at all
three values of `TEXT_SINGLE_CLIENT`, which is D-08 proved by arithmetic.

Then `## The three shapes the verdict selects` — the ROADMAP criterion 4 table,
reproduced here so the verdict document does not have to invent it: `go` → an
in-process async mutex, both channels connected for the session's lifetime, the
`channel` discriminator kept for bookkeeping only; `degrade` → a broker-level
cross-channel halt-authority lease, moving correctness from one process's
in-memory mutex to the broker, at the cost of a round trip per halting call;
`no-go` → a connect-gate in which opening one channel requires releasing the
other's claim, the two time-sharing and never coexisting live. State that a
`no-go` does not kill the runtime-evidence layer: it makes Phase 43's capture step
**scheduled** rather than concurrent, and Phase 43 is written to survive it.

Then `## Never a gate` — declared here so a later plan cannot promote them:
`TEXT_SINGLE_CLIENT` (D-08 — it constrains Phase 41's connection management to one
text client per instance, the same rule the broker already enforces for the
binary monitor, but says nothing about whether one text and one binary client can
coexist, which is the only question this gate asks); the VICE version (D-15 —
provenance, never an input; the baseline is stock 3.9 and a run landing on
something later invalidates nothing); the second binary's outcomes (recorded as
facts beside the gate, and a divergence between the two binaries recorded loudly
rather than averaged); the `test:automated` failure count (a recorded baseline
every transcript states, never a threshold anything passes); the concurrent-write
overlap gap in milliseconds (recorded as a measurement caveat so a reader can
judge how tight the overlap was, never compared against a threshold); and
`IDLE_TEXT_CLIENT_HALTS` (a recorded fact shaping Phase 41's connection
management, in no antecedent).

**File 2 — `evidence/SCHEMA.md`.** Same opening register as Phase 33's: binding,
committed before any measurement exists, every literal name the rest of the phase
may emit is fixed here together with the derivation rule that produces its value
— a measuring plan needing a name not declared below has found a gap in the
pre-commitment, records it as an `## ACCEPTED LIMIT` in its own evidence file, and
does not invent a name or edit this file.

`## 1. Outcome-line conventions` — copy Phase 33's §1 **verbatim** per D-07, with
one substantive edit: retarget "the five gate inputs" to "the seven gate inputs".
Keep bare `NAME: value` at column 0, never indented, never inside a fenced block a
reader would take for sample output, never inside a table cell; final occurrence
wins; one declared source file per line; absence rules; transcripts appended,
never reconstructed.

`## 2. The seven gate inputs` — the table from `DECISION-RULE.md` § Inputs, plus a
`Corpus-free` column reading `yes` for all seven, and the sentence that this is
the inversion of Phase 33's distinguishing property.

`## 3. Derivations` — one subsection per input. These are the definitions a
measuring plan may not soften:

- `IDLE_COEXIST`. With the text client connected and having issued **no** command
  since its banner: `clean` iff three consecutive non-halting `MemoryGet` (0x01,
  `sidefx=false`) reads of the KERNAL ROM window `$E000-$E0FF` on the binary
  channel each return a 256-byte payload byte-identical to the same read taken
  with **no** text client connected, with zero desync bytes and zero duplicate
  replies on the binary client and no unsolicited frame at request-id
  `0xffffffff` arriving during the window. A ROM window is specified rather than
  RAM because it is invariant under execution, so byte-identity is a valid
  predicate. `corrupts` iff any read errors, times out, returns a differing
  payload, or the binary client counts a desync byte, a duplicate reply, or a
  pending request resolved by an event. `not-taken` iff the text port never
  accepted a connection, or the binary channel could not be established, with the
  reason named.
- `FOREIGN_HALT_VISIBILITY`. With a non-stopping (`stop_when_hit=false`) exec
  checkpoint armed at the IRQ frame anchor `$EA31` on the binary channel and a
  halting `memmapshow` issued on the text channel: `visible` iff at least one of
  (a) an unsolicited `STOPPED` (0x62) frame arrives on the binary channel at
  request-id `0xffffffff`, or (b) two `CheckpointGet` (0x11) reads bracketing the
  halt window show `hit_count` unchanged while the same bracket taken without a
  foreign halt shows it advancing — record which. `invisible` iff neither signal
  appears and `hit_count` advances across the halt window exactly as it does
  without one. `corrupts` iff the binary client desyncs, a pending request is
  resolved by an event, a request times out, or `hit_count` decreases.
  `not-taken` iff the checkpoint could not be armed or `memmapshow` produced no
  framed response.
- `CONCURRENT_INFLIGHT`. Both channel writes issued before either is awaited
  (`Promise.all` over `AdvanceInstructions` (0x71, count=1) on the binary channel
  and `prof flat 5` on the text channel, after `prof on` and a resumed run
  window), repeated three times: `clean` iff all six replies arrive within the
  stated 15 s budget, each matched to its own request id, with zero desync bytes
  and zero duplicate replies. `degraded` iff every reply eventually arrives but at
  least one exceeded the budget, needed a retry, or the binary client counted
  desync bytes it recovered from. `corrupts` iff a reply is lost, matched to the
  wrong request id, a pending request is resolved by an unsolicited event, or the
  machine jams. `not-taken` iff the overlap could not be produced at all. The
  wall-clock gap between the two `write()` calls is recorded as
  `CONCURRENT_WRITE_GAP_MS:` in every case, so a reader judges how tight the
  overlap really was — it is a caveat, never a threshold.
- `CROSS_CHANNEL_RESUME`. Halt from the text channel, then read registers over the
  binary channel, then resume from the **binary** channel with `Exit` (0xaa):
  `clean` iff the register read succeeds while halted, the resume is accepted, and
  the machine is observed running again — confirmed by the text channel's own
  `sw` counter having advanced between a reading taken before the resume and one
  taken at least one second after it. `corrupts` iff the register read returns an
  error or an implausible frame, the resume is rejected, the machine does not
  resume, or the binary client desyncs. `not-taken` iff no text-side halt could be
  established. The mirror direction (halt on binary, read and resume from text via
  `x`) is run and recorded in the same file as a second transcript; a disagreement
  between the two directions is recorded loudly and resolves to the worse value.
- `DISCONNECT_RECOVERY`. A separate child process holds a text-side halt and is
  `SIGKILL`ed by the parent — a separate process rather than a same-process
  `socket.destroy()` so no client-side cleanup runs at all, which is the literal
  reading of "SIGKILL the text client". `recovers` iff within a stated 60 s budget
  the machine is observed running again from the binary channel (a non-stopping
  checkpoint's `hit_count` advances) with **no** further action.
  `leaves-halted` iff `hit_count` does not advance within the budget; record
  separately whether a fresh text connection followed by `x` restores it, since
  that is the mechanism `R11`'s narrowing names. `not-taken` iff the victim could
  not establish a halt.
- `HITCOUNT_INVARIANT_HOLDS`. With a non-stopping exec checkpoint armed at `$EA31`
  on the binary channel, a foreign halt induced from the text channel and released
  with `x`: `holds` iff throughout, (a) no pending binary request is resolved by an
  unsolicited event, (b) `hit_count` is monotonically non-decreasing across every
  read, and (c) a wait keyed on the checkpoint's own `hit_count` — never on paused
  state — is **not** satisfied by the foreign halt and completes exactly once when
  the client's own stop arrives. `breaks` iff a foreign `STOPPED` satisfies a wait
  that was waiting for the client's own stop, or `hit_count` decreases or resets,
  or a pending request is resolved by an event. `not-taken` iff the checkpoint
  could not be armed or no foreign halt could be induced.
- `TEXT_SINGLE_CLIENT`. With client A connected and served (banner plus one
  successful command), a second TCP connection B is opened to the same text port
  under a stated **60 s** budget, and exactly one of three observations is
  recorded as `TEXT_SECOND_CONNECT_OBSERVATION:` —
  `accepted-and-served` (B receives a banner and a command reply) maps to `multi`;
  `accepted-then-silent` (connect succeeds, no banner, no EOF within the budget)
  maps to `single`; `refused-outright` (connection refused or reset) maps to
  `single`. **A timeout on B is `single` via `accepted-then-silent`, never
  `not-taken`** — the second `connect()` to a single-client server is documented
  as indistinguishable from a wedge, and recording `not-taken` because a short
  timeout expired would launder the real answer into silence. `not-taken` is
  reachable only if client A itself could not be established.

`## 4. Recorded facts that are not gate inputs` — declared here so a measuring
plan cannot mint them and a later plan cannot promote them. For each: the line
name, its domain, and its single declared source file.
`IDLE_TEXT_CLIENT_HALTS: yes | no | not-taken` (whether the machine is still
running while a text client is connected and silent, derived from a non-stopping
checkpoint's `hit_count` advancing over a one-second window with the text client
connected and silent) and `REMOTEMONITOR_FLAG_ORDER: default-first-assumed |
default-first-measured-required | order-irrelevant-measured` and
`TEXT_PROMPT_LITERAL_CONFIRMED: yes | no` and `TEXT_BIND_BUDGET_MS_MAX:
<integer>` — all four in `evidence/39-idle-coexist.md`;
`TEXT_SECOND_CONNECT_OBSERVATION:` in `evidence/39-text-single-client.md`;
`CONCURRENT_WRITE_GAP_MS: <integer>` in `evidence/39-concurrent-inflight.md`;
`FIXTURE_COUNT: <integer>`, `FIXTURE_BINARIES: <integer>`,
`FIXTURE_ENCODING: ascii-7bit | has-high-bytes`,
`FIXTURE_DIVERGENCE: none | <comma-separated commands>` and
`FIXTURE_UNSUPPORTED: none | <comma-separated commands>` in
`evidence/39-fixture-batch.md`;
`TOTAL_TUPLES: <integer>`, `TOTALITY: holds | violated`,
`TSC_INDEPENDENCE: holds | violated`, `COULD_NOT_RUN_EMITTABLE: no | yes` and
`RULE_HIT_R<N>: <integer>` for N = 1..15 in `evidence/39-totality.md`. Plus the
three lines every evidence file in this phase carries: `BROKER_STATE:`,
`TEST_AUTOMATED_BASELINE:` and `VICE_BINARY:` / `VICE_VERSION_OBSERVED:`.

`## 5. The findings-document frontmatter shape` — fix the keys and their order now,
mirroring `docs/phase33-reproducible-run-gate-findings.md`: `phase`,
`requirement`, `probe_date`, `verdict` (domain exactly `go` / `degrade` /
`no-go` — `could-not-run` is not in this field's domain, and that is structural
rather than a promise), `verdict_rule_applied` (`R<N>`), then `inputs:` carrying
all seven `D-02` inputs as lower-snake-case keys in the `DECISION-RULE.md`
§ Inputs row order, each with a trailing comment citing the evidence file and line
its value was transcribed from. Declare the incremental-population rule: the
`inputs:` block is populated as measurements land, a not-yet-transcribed input
carries the literal placeholder `not-yet-transcribed`, that placeholder is **not**
a member of any input's value domain, and the `verdict:` and
`verdict_rule_applied:` keys are **absent** until all seven inputs carry real
domain members — a findings document with the verdict key present alongside any
placeholder is an incomplete phase, exactly as an absent outcome line is.

**File 3 — `evidence/README.md`.** A `## Files` table with Phase 33's three-column
discipline (File / Owner / What it is / What it proves), one row per artifact this
phase will produce under `evidence/` — the five files of this commit, then
`probe-harness.mjs`, `textmon-probe-client.mjs`, the seven probe scripts,
`textmon-kill-victim.mjs`, `fixture-capture.mjs`, the seven `39-*.md` outcome
files, `39-fixture-batch.md`, and `docs/phase39-dual-channel-coexistence-gate-findings.md`
outside the directory — with the owning plan named in each row.

Then `## Evidence conventions`, numbered, binding on every plan in phase 39:

1. **Transcript convention.** Before running a command, append a line reading
   `$ <the exact command line>` to the named evidence file; then append its real
   stdout and stderr immediately below. A summary written in place of output is
   not evidence, and a transcript is never reconstructed afterwards from memory.
2. **Worktree-independent probe directory.** Cross-wave working artifacts live at
   `PROBE_DIR=$HOME/.cache/c64-re-tools/phase39` — never inside the checkout, and
   never under `/tmp`, which is tmpfs on this host (16 GB, emptied only on reboot)
   and has already cost this project a build. Record absolute `PROBE_DIR` paths so
   a later wave can find the artifact, and copy anything that matters into
   `evidence/` before the session ends.
3. **Broker stopped, recorded (D-16).** Every live run in this phase is taken with
   the VICE broker **stopped** and with no other `x64sc` process alive, and each
   transcript records `BROKER_STATE: inactive` alongside the
   `TEST_AUTOMATED_BASELINE:` line it was taken against. A live broker reddens the
   `BACK-05` assertion deterministically, so a measurement taken against a
   live-broker run reads a false baseline and every number in it is suspect. Such a
   measurement is **discarded and re-run, never repaired.** Verify before the run
   with `systemctl --user is-active vice-broker` (expect `inactive`) and
   `pgrep -af x64sc` (expect no output), and paste both. Every probe additionally
   refuses **in code** rather than by habit.
4. **Baseline stated, never re-derived.** The measured `test:automated` baseline on
   this tree is **2 failing tests in `anno-register.test.ts` alone**, from a cause
   no phase in this milestone created. Every transcript records the count **it
   observed** and the failing file names, in the form
   `TEST_AUTOMATED_BASELINE: tests <n> / pass <n> / fail <n>` plus
   `TEST_AUTOMATED_BASELINE_FILES: <names>`. No transcript writes "clean" or "0
   failures", and no plan adopts a `test:automated` failure count as an acceptance
   criterion — the count is a recorded baseline and never a gate. `npm test` is not
   used at all in this phase: the whole-glob run does not terminate unaided. Use
   `npm run test:automated` or a single-file `node --test <file>`.
5. **The binary is resolved by absolute path, and which one answered is recorded.**
   `/usr/bin/x64sc` is genuine unpatched stock 3.9 and is the gate baseline (D-15);
   the fork 3.10 at `/usr/local/bin/x64sc` shadows it on bare `PATH`. Every probe
   resolves by absolute path, never by bare name, and records `VICE_BINARY:` with
   the `stock:`/`fork:` kind derived from the resolved path plus
   `VICE_VERSION_OBSERVED:` taken from the binary's own `--version` output. The
   VICE version is provenance and never a gate input.
6. **Voided runs are recorded, not discarded.** A run that produced no usable
   measurement is written into the evidence file **with its reason**. A phase that
   quietly drops its unhelpful runs is selecting its evidence.
7. **Final occurrence wins.** Where an outcome line appears more than once in one
   file, the last occurrence is the value.
8. **Values are transcribed, never remembered.** Every value in the findings
   document comes from a literal outcome line at column 0 of a named evidence
   file, cited by path. Nothing is taken from a plan SUMMARY's paraphrase — a
   numeric or enumerated result with no transcript behind it is a restatement
   wearing a measurement's clothes.
9. **No production module of Phase 41 is written by any plan in this phase.** Not
   the in-process mutex, not the broker lease, not the connect-gate, not in stub
   form; not `CHAN-02`'s port surfacing, `CHAN-03`'s reliable framing,
   `CHAN-04`'s serialization authority, `CHAN-05`'s contention verdict, or any
   `PARSE-*` parser; and no text-monitor client module under `src/`. The text
   client this phase builds lives in this directory, is crude by design, and does
   not survive the phase (D-13).

Then `## Ordering proof` — bank it as two facts here, immediately before staging,
so the findings document can cite the ordering without a reader running git.
**Fact one:** nothing under `evidence/` exists anywhere in history at the moment
these rules are authored — paste the real output of
`git log --oneline -- <evidence>` (expected: no output) and
`git rev-list --count HEAD -- <evidence>` (expected: `0`), naming the `HEAD` sha
they were taken at. **Fact two:** the five files of this plan are the entire
evidence tree — paste the real output of `ls <evidence>` taken immediately before
staging.

**File 4 — `evidence/totality-walk.mjs`.** An executable Node script, no
dependencies beyond builtins, carrying the project's structured header convention
(WHY THIS FILE EXISTS / WHAT IT IS THE ONE PLACE FOR / WHAT NOT TO DO), whose
"what not to do" names the specific temptation: the `RULES` array mirrors
`DECISION-RULE.md`'s numbered list 1:1 in the same written order and is never
"simplified", reordered, or extended with a rule the prose does not carry.

It declares `DOMAINS` as the seven inputs in `DECISION-RULE.md` § Inputs row order
with their frozen domains; generates the full cross-product; declares `RULES` as an
ordered array of `{ id, verdict, test(tuple) }` mirroring `R1`..`R15`, with `R15`'s
`test` returning `true` unconditionally (no antecedent); and for every tuple
computes **all** matching antecedents, not just the first — which is what makes the
strong property checkable rather than assumed. The assertion, stated precisely:
every tuple matches **exactly one** of `R1`..`R14`, or **none** of them and then
`R15`. The script throws, naming the offending tuple and the ids it matched, when a
tuple matches two or more of `R1`..`R14`. This is a real risk rather than a
formality: an antecedent written bare rather than conditioned on the earlier inputs
overlaps its siblings, and the tuple carrying both `DISCONNECT_RECOVERY:
leaves-halted` and `HITCOUNT_INVARIANT_HOLDS: breaks` is the shortest witness. Add
that tuple to the script as a named self-check so the disjointness guarantee has a
regression case rather than only a global assertion.

It then emits, each as a bare `NAME: value` line at column 0 of stdout:
`TOTAL_TUPLES: <n>`; `RULE_HIT_R1:` .. `RULE_HIT_R15:` with each rule's count;
`TOTALITY: holds` when every tuple resolved to exactly one rule;
`COULD_NOT_RUN_EMITTABLE: no` when every rule's verdict is a member of
`{go, degrade, no-go}` and `R15` carries no antecedent — computed, not asserted;
and `TSC_INDEPENDENCE: holds` when, for every one of the 1,296 tuple groups that
differ only in `TEXT_SINGLE_CLIENT`, all three members fire the **same** rule id —
which is `D-08` proved mechanically rather than promised. Non-zero exit on any
violation.

**File 5 — `evidence/39-totality.md`.** The walk's record: the `$ node
totality-walk.mjs` transcript with its real stdout in a fenced block, then the
same outcome lines **restated bare at column 0 outside the fence** (the fenced
copy is a transcript; the bare copy is the recorded value, per `SCHEMA.md` §1's
"never inside a fenced block a reader would take for sample output"). Include the
run's `BROKER_STATE:` and `TEST_AUTOMATED_BASELINE:` lines even though the walk
needs no emulator, so the file matches every other evidence file's shape, and note
explicitly that no emulator was involved.

Finally: stage all five paths and commit them in **one** commit. Do not commit
anything else in that commit.
  </action>
  <acceptance_criteria>
    - `evidence/DECISION-RULE.md` contains the literal string `first match wins`, carries all fifteen rule headings `R1` through `R15`, and has exactly one rule whose verdict is `go`
    - `evidence/DECISION-RULE.md` names all seven inputs `IDLE_COEXIST`, `FOREIGN_HALT_VISIBILITY`, `CONCURRENT_INFLIGHT`, `CROSS_CHANNEL_RESUME`, `DISCONNECT_RECOVERY`, `HITCOUNT_INVARIANT_HOLDS`, `TEXT_SINGLE_CLIENT`
    - `evidence/DECISION-RULE.md` carries a `## Totality` section stating the tuple count 3888, a `## The three shapes the verdict selects` section, and a `## Never a gate` section naming `TEXT_SINGLE_CLIENT`, the VICE version, the `test:automated` failure count and the concurrent-write overlap gap
    - `evidence/DECISION-RULE.md` transcribes Task 1's selection verbatim in a `**Decision checkpoint.**` paragraph
    - `evidence/SCHEMA.md` declares a value domain AND a derivation rule for each of the seven gate inputs, names exactly one source file per line, and carries the findings-frontmatter key order plus the `not-yet-transcribed` placeholder rule
    - `evidence/README.md` contains a `## Ordering proof` section carrying real `git log` / `git rev-list` / `ls` output, not a placeholder, and nine numbered evidence conventions
    - `node evidence/totality-walk.mjs` exits 0 and prints `TOTAL_TUPLES: 3888`, `TOTALITY: holds`, `TSC_INDEPENDENCE: holds`, `COULD_NOT_RUN_EMITTABLE: no` and `RULE_HIT_R15: 3`
    - `evidence/39-totality.md` carries each of those five lines as a whole line at column 0, outside any fenced block
    - All five files land in ONE commit; `git rev-list --count <rules-sha> -- <evidence-dir>` is `1`
    - The commit that adds `DECISION-RULE.md` touches no path outside the evidence directory (D-01)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && E=.planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence && test "$(ls "$E" | wc -l)" -eq 5 && RULES=$(git log --diff-filter=A --format=%H -- "$E/DECISION-RULE.md" | tail -1) && test -n "$RULES" && test "$(git rev-list --count "$RULES" -- "$E")" -eq 1 && OUTSIDE=$(git show --name-only --format= "$RULES" | grep -av "^$E/" | grep -c . || true) && test "$OUTSIDE" -eq 0 && echo "GATE_ORDER_OK rules=$RULES"</automated>
    <fails_when>non-zero exit, or `GATE_ORDER_OK` absent from stdout — which happens when the evidence directory holds other than five files, when DECISION-RULE.md has no adding commit, when any commit reachable from the rules commit other than itself touched the evidence directory, or when the rules commit also touched a path outside it</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && E=.planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence && grep -ac 'first match wins' "$E/DECISION-RULE.md" && for r in R1 R2 R3 R4 R5 R6 R7 R8 R9 R10 R11 R12 R13 R14 R15; do grep -aq "\*\*$r " "$E/DECISION-RULE.md" || { echo "missing rule $r"; exit 1; }; done && for i in IDLE_COEXIST FOREIGN_HALT_VISIBILITY CONCURRENT_INFLIGHT CROSS_CHANNEL_RESUME DISCONNECT_RECOVERY HITCOUNT_INVARIANT_HOLDS TEXT_SINGLE_CLIENT; do grep -aq "$i" "$E/DECISION-RULE.md" || { echo "missing input $i in DECISION-RULE.md"; exit 1; }; grep -aq "$i" "$E/SCHEMA.md" || { echo "missing input $i in SCHEMA.md"; exit 1; }; done && grep -aq 'Never a gate' "$E/DECISION-RULE.md" && grep -aq '3888\|3,888' "$E/DECISION-RULE.md" && grep -aq 'not-yet-transcribed' "$E/SCHEMA.md" && grep -aq 'Ordering proof' "$E/README.md" && echo RULES_SHAPE_OK</automated>
    <fails_when>non-zero exit, `missing rule R&lt;N&gt;` or `missing input &lt;NAME&gt; in &lt;file&gt;` on stdout, or `RULES_SHAPE_OK` absent from stdout</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && E=.planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence && P="$HOME/.cache/c64-re-tools/phase39" && mkdir -p "$P" && node "$E/totality-walk.mjs" > "$P/walk.out" 2>&1 && grep -acE '^TOTAL_TUPLES: 3888$' "$P/walk.out" && grep -acE '^TOTALITY: holds$' "$P/walk.out" && grep -acE '^TSC_INDEPENDENCE: holds$' "$P/walk.out" && grep -acE '^COULD_NOT_RUN_EMITTABLE: no$' "$P/walk.out" && grep -acE '^RULE_HIT_R15: 3$' "$P/walk.out" && echo WALK_OK</automated>
    <fails_when>non-zero exit — the walk throws and exits non-zero on any tuple matching zero rules or more than one of R1..R14 — or `WALK_OK` absent from stdout, or any of the five asserted lines missing from the walk's own output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && E=.planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence && for L in 'TOTAL_TUPLES: 3888' 'TOTALITY: holds' 'TSC_INDEPENDENCE: holds' 'COULD_NOT_RUN_EMITTABLE: no' 'RULE_HIT_R15: 3'; do grep -qaxF -- "$L" "$E/39-totality.md" || { echo "missing banked line: $L"; exit 1; }; done && echo BANKED_OK</automated>
    <fails_when>non-zero exit, `missing banked line: &lt;line&gt;` on stdout, or `BANKED_OK` absent — `grep -x` requires a whole-line match, so an indented or table-cell copy of an outcome line does not satisfy it</fails_when>
  </verify>
  <done>The rules, the frozen schema, the evidence conventions, the executable totality walk and the walk's recorded output are committed as ONE commit; `git rev-list --count <rules-sha> -- evidence/` is 1 and that commit touches nothing outside `evidence/`; the walk exits 0 reporting 3,888 tuples, totality holding, `TEXT_SINGLE_CLIENT` independence holding and `could-not-run` unemittable; the ordering proof is banked with real git output; Task 1's selection is transcribed verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this phase's measurements → the recorded verdict | The only thing standing between a measurement and the verdict it produces is a rule written before the measurement existed |
| git history → the pre-commitment claim | The claim "the rules predate every measurement" is asserted by commit ordering and by nothing else (D-01, D-06 declined a test guard) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Repudiation | `evidence/DECISION-RULE.md` | high | mitigate | Single-commit landing plus the banked `## Ordering proof` (real `git log`, `git rev-list --count` and `ls` output). Task 2's verify asserts `git rev-list --count <rules-sha> -- evidence/` is exactly 1 AND that the rules commit touched no path outside `evidence/`, so an evidence file landing at or before the rules commit reds this plan rather than passing silently. |
| T-39-02 | Tampering | `evidence/DECISION-RULE.md` / `SCHEMA.md` after the first measurement | high | mitigate | The frozen-status paragraph names the specific failure mode (a later plan "only resolving an ambiguity") and routes ambiguities to an `## ACCEPTED LIMIT` in the measuring plan's own file plus an explicit override in the findings document. Plan `39-08` re-runs the same `git rev-list` proof after every measurement has landed. |
| T-39-03 | Repudiation | the verdict's derivability | high | mitigate | `totality-walk.mjs` enumerates all 3,888 tuples and fails loudly on a tuple matching zero or multiple antecedents, so totality is executable rather than asserted; `R15` carries no antecedent, and `COULD_NOT_RUN_EMITTABLE: no` is computed from the rule set rather than promised. `TSC_INDEPENDENCE: holds` proves D-08 mechanically. |
| T-39-04 | Tampering | the pre-mapped `degrade` narrowings | medium | mitigate | `R11` and `R13` carry their narrowing text in this commit, before either value exists; the other five rules are labelled verdict-time narrowings so a reader weighs them differently. A revision after the fact would have to appear as an explicit override in the findings document. |
| T-39-05 | Elevation of Privilege | the direct-spawn trust boundary (D-12) | medium | mitigate | Declared in this plan's `README.md` conventions 3 and 5 as a phase-wide rule before any probe exists: the probe spawns `x64sc` directly and therefore inherits none of the broker's guards, so the conventions require preflight refusal in code, absolute-path binary resolution, and `BROKER_STATE:` / `VICE_BINARY:` recorded in every transcript. The measuring plans carry the enforcing threat entry; this plan carries the declaration. |
| T-39-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan or anywhere in this phase — `39-RESEARCH.md` § *Package Legitimacy Audit* records "Not applicable — this phase installs no external package", and the container-side server's zero-third-party-runtime-dependency posture is a documented project constraint. Accepted because the attack surface is absent, not tolerated. Any later plan proposing an install must run the legitimacy gate protocol first and justify the departure. |
</threat_model>

## Artifacts this phase produces

Symbols and files created by **Phase 39 as a whole** (listed in every plan so the
source-grounding pass can exclude newly-created names from drift verification).

**New files — planning/evidence** (all under
`.planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/`)
`DECISION-RULE.md`, `SCHEMA.md`, `README.md`, `totality-walk.mjs`,
`39-totality.md`, `probe-harness.mjs`, `textmon-probe-client.mjs`,
`idle-coexist-probe.mjs`, `39-idle-coexist.md`, `foreign-halt-probe.mjs`,
`39-foreign-halt.md`, `cross-channel-resume-probe.mjs`,
`39-cross-channel-resume.md`, `concurrent-inflight-probe.mjs`,
`39-concurrent-inflight.md`, `hitcount-invariant-probe.mjs`,
`39-hitcount-invariant.md`, `disconnect-recovery-probe.mjs`,
`textmon-kill-victim.mjs`, `39-disconnect-recovery.md`,
`text-single-client-probe.mjs`, `39-text-single-client.md`,
`fixture-capture.mjs`, `39-fixture-batch.md`

**New files — source and docs**
`src/mcp/vice/textmon-fixtures.ts`, `src/mcp/vice/textmon-fixtures.test.ts`,
`src/mcp/vice/fixtures/textmon/README.md`,
`src/mcp/vice/fixtures/textmon/*.txt` + `*.json` sidecar pairs (one pair per
captured command per binary, plus the unsupported-command refusal captures),
`docs/phase39-dual-channel-coexistence-gate-findings.md`

**New outcome-line key names (gate inputs)**
`IDLE_COEXIST`, `FOREIGN_HALT_VISIBILITY`, `CONCURRENT_INFLIGHT`,
`CROSS_CHANNEL_RESUME`, `DISCONNECT_RECOVERY`, `HITCOUNT_INVARIANT_HOLDS`,
`TEXT_SINGLE_CLIENT`

**New outcome-line key names (recorded facts, never gate inputs)**
`IDLE_TEXT_CLIENT_HALTS`, `REMOTEMONITOR_FLAG_ORDER`,
`TEXT_PROMPT_LITERAL_CONFIRMED`, `TEXT_BIND_BUDGET_MS_MAX`,
`TEXT_SECOND_CONNECT_OBSERVATION`, `CONCURRENT_WRITE_GAP_MS`, `FIXTURE_COUNT`,
`FIXTURE_BINARIES`, `FIXTURE_ENCODING`, `FIXTURE_DIVERGENCE`,
`FIXTURE_UNSUPPORTED`, `TOTAL_TUPLES`, `TOTALITY`, `TSC_INDEPENDENCE`,
`COULD_NOT_RUN_EMITTABLE`, `RULE_HIT_R1` .. `RULE_HIT_R15`, `BROKER_STATE`,
`TEST_AUTOMATED_BASELINE`, `TEST_AUTOMATED_BASELINE_FILES`, `VICE_BINARY`,
`VICE_VERSION_OBSERVED`

**New verdict-document frontmatter fields**
`phase`, `requirement`, `probe_date`, `verdict`, `verdict_rule_applied`,
`inputs.idle_coexist`, `inputs.foreign_halt_visibility`,
`inputs.concurrent_inflight`, `inputs.cross_channel_resume`,
`inputs.disconnect_recovery`, `inputs.hitcount_invariant_holds`,
`inputs.text_single_client`

**New symbols — `evidence/totality-walk.mjs`**
`DOMAINS`, `RULES`, `crossProduct()`

**New symbols — `evidence/probe-harness.mjs`**
`VICE_STOCK`, `VICE_FORK`, `PROBE_DIR`, `viceKind()`, `viceVersion()`,
`systemdBrokerState()`, `aliveX64sc()`, `preflight()`, `allocPorts()`,
`buildProbeArgs()`, `spawnVice()`, `reapAll()`, `connectWithRetry()`,
`pingReady()`, `armNonStoppingExec()`, `checkpointHitCount()`,
`testAutomatedBaseline()`, `log()`, `sleep()`

**New symbols — `evidence/textmon-probe-client.mjs`**
`PROMPT_RE`, `connectTextMonitor()`, `awaitBanner()`, `sendAndAwaitPrompt()`,
`closeTextMonitor()`

**New symbols — `src/mcp/vice/textmon-fixtures.ts`**
`REQUIRED_PROVENANCE_KEYS`, `TEXTMON_FIXTURE_DIR`, `MissingTextFixtureError`,
`listTextFixtures()`, `loadTextFixture()`, type `TextFixture`,
type `LoadTextFixtureOptions`, type `MissingTextFixtureErrorOptions`

**Modified existing files (no new symbols)**
`.planning/phases/07-cycle-timing-and-wedge-triage/07-RESEARCH.md`, `CLAUDE.md`,
`.planning/notes/stock-vice-migration-revised-loss-ledger.md`,
`.planning/STATE.md`, `.planning/ROADMAP.md`, and the move of
`.planning/todos/pending/2026-08-28-phase-7-pitfall-5-overgeneralizes-text-monitor-unreachability.md`
to `.planning/todos/completed/`

<verification>
- `node .planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/totality-walk.mjs` exits 0 and reports 3,888 tuples with totality holding
- `git rev-list --count <rules-sha> -- <evidence-dir>` is `1`, and the rules commit touches nothing outside `evidence/`
- All seven gate-input names, their frozen domains and their derivations are declared in `SCHEMA.md`, one declared source file each
- `## Never a gate` names `TEXT_SINGLE_CLIENT`, the VICE version, the `test:automated` failure count and the overlap gap
</verification>

<success_criteria>
The pre-commitment exists in git before any measurement does, is provable with
`git log` alone, and its totality is discharged by a script a reader can re-run
rather than by a table a reader must trust.
</success_criteria>

<output>
Create `.planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/39-01-SUMMARY.md` when done
</output>