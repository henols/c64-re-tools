---
phase: 41-the-text-channel-its-serialization-authority-and-the-content
plan: 06
type: execute
wave: 4
depends_on: [41-02, 41-04]
files_modified:
  - src/mcp/vice/text-tools.ts
  - src/mcp/vice/text-tools.test.ts
  - src/mcp/vice/text-protocol.ts
  - src/mcp/vice/stock-dispatch.ts
  - src/mcp/vice/stock-dispatch.test.ts
  - src/mcp/vice/tools-manifest.stock.json
  - src/mcp/vice/capability-registry.ts
  - src/mcp/vice/capability-registry.test.ts
  - src/mcp/vice/package.json
  - src/mcp/vice/text-monitor-live.test.ts
  - docs/tool-support.md
  - docs/phase41-text-channel-live-evidence.md
  - CLAUDE.md
autonomous: true
requirements: [CHAN-03, CHAN-05]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A user's tool call issues a text-monitor command and gets its complete response back, through an allowlisted typed verb with no free-text command field (CHAN-03, D-01)."
    - "Exactly two text-channel tools reach `tools/list`: `vice_device_console` and `vice_warp_set`. The five Phase-42 parse-target commands are reachable in-process through the allowlist and are NOT advertised as tools (D-02)."
    - "`device c:` is an explicit tool, not auto-healing on the stepping path — no text round trip and no lock acquisition is added to any binary-side stepping call (D-03)."
    - "The `default_memspace` remedy is exercised, not merely made available: a live test contaminates it and shows `device c:` restoring main-CPU stepping (criterion 5)."
    - "`warp on` / `warp off` is re-probed live while the channel is open, and its result recorded."
    - "All three narrowed `CLAUDE.md` constraints gain this phase's own evidence citation beside the existing probe citation; none is deleted and none is weakened (D-04)."
    - "The absent runtime `WarpMode` resource stays a real and separate fact from `warp on` being a working monitor command."
    - "`capability-registry.ts` no longer asserts that this project does not dial the text channel — it does, as of this phase."
    - "The two new tools are stock-only: absent from the fork manifest, present in the stock manifest, and carried in the capability registry's divergence set."
    - "`docs/tool-support.md` is regenerated rather than hand-edited."
  artifacts:
    - path: "src/mcp/vice/text-tools.ts"
      provides: "The two allowlisted text-channel tool handlers, each holding the text channel lock for its whole logical operation"
      contains: "export async function handleDeviceConsole"
    - path: "docs/phase41-text-channel-live-evidence.md"
      provides: "This phase's own live evidence record: the device c: contamination-and-remedy run, the warp on/off re-probe, and the chis-on-3.9 citation, each with binary, version, date and command"
      min_lines: 40
    - path: "CLAUDE.md"
      provides: "The three narrowed constraints re-cited to this phase's evidence"
      contains: "phase41-text-channel-live-evidence"
  key_links:
    - from: "src/mcp/vice/stock-dispatch.ts"
      to: "src/mcp/vice/text-tools.ts"
      via: "STOCK_DISPATCH_TABLE registers vice_device_console and vice_warp_set through the one existing adapter"
      pattern: "handleDeviceConsole|handleWarpSet"
    - from: "src/mcp/vice/text-tools.ts"
      to: "src/mcp/vice/text-protocol.ts"
      via: "each handler takes the text channel's halt authority through withTextChannelLock and issues one allowlisted verb"
      pattern: "withTextChannelLock"
    - from: "src/mcp/vice/tools-manifest.stock.json"
      to: "docs/tool-support.md"
      via: "the support table is regenerated from the two manifests plus capability-registry.ts, never hand-edited"
      pattern: "vice_device_console|vice_warp_set"
    - from: "docs/phase41-text-channel-live-evidence.md"
      to: "CLAUDE.md"
      via: "each of the three narrowed constraints cites this document beside its existing exploration-probe citation"
      pattern: "phase41-text-channel-live-evidence"
  prohibitions:
    - statement: "The text channel must not expose a free-text or arbitrary monitor-command tool or parameter to any caller — every shipped tool is a typed verb whose parameters are constrained, and no tool argument is ever concatenated into a monitor command line unvalidated (D-01)."
      status: resolved
      verification: test
    - statement: "A tool must not ship returning a blob it cannot interpret — the five Phase-42 parse-target commands stay behind the internal allowlist until their owning parser lands (D-02)."
      status: resolved
      verification: test
    - statement: "A re-cited constraint must not be deleted, weakened, or have its scoping clause widened — each of the three gains this phase's evidence beside the existing citation, and each remains literally true as written (D-04)."
      status: resolved
      verification: judgment
    - statement: "Shipped documentation must not continue to assert that this project does not dial the text-monitor channel."
      status: resolved
      verification: test
---

<objective>
Ship the two remedy tools, exercise the `default_memspace` remedy live, and
re-anchor the three narrowed `CLAUDE.md` constraints to this phase's own evidence
rather than to an exploration probe.

Purpose: criterion 2 asks for a *tool call* that reaches the text monitor — not
just a module that can. Criterion 5 asks for the remedy to be **exercised, not
merely made available**, and for each of the three narrowed constraints to gain
a scoping clause rather than a deletion. All three clauses already exist
textually, but all three cite the `/gsd-explore` live probe of 2026-08-27 rather
than a phase's evidence discipline; Phase 39 independently re-confirmed only the
`chis` one. This plan closes that gap with the channel open.

Output: `vice_device_console` and `vice_warp_set`; the five Phase-42 commands
held behind the internal allowlist; a live evidence record; three re-cited
constraints; and `capability-registry.ts`'s now-false claim about this project
not dialing the channel corrected.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-the-text-channel-its-serialization-authority-and-the-content/41-CONTEXT.md
@.planning/phases/41-the-text-channel-its-serialization-authority-and-the-content/41-PATTERNS.md
@.planning/phases/41-the-text-channel-its-serialization-authority-and-the-content/41-02-SUMMARY.md
@.planning/phases/41-the-text-channel-its-serialization-authority-and-the-content/41-04-SUMMARY.md

Deliberately NOT `@`-included: `.planning/PROJECT.md`, `.planning/ROADMAP.md`
and `.planning/STATE.md` (202KB / 147KB / 335KB). Every line-number citation in
the phase artifacts is a 2026-09-08 snapshot — resolve each seam by symbol name.
</context>

## Artifacts this phase produces

New files (this plan):

- `src/mcp/vice/text-tools.ts`
- `src/mcp/vice/text-tools.test.ts`
- `docs/phase41-text-channel-live-evidence.md`

New exported symbols (this plan):

- `handleDeviceConsole`, `handleWarpSet` in `text-tools.ts`

New tool names (this plan): `vice_device_console`, `vice_warp_set`

Already produced by earlier plans: `TextMonitorClient`, `PROMPT_RE`,
`TEXT_COMMAND_ALLOWLIST`, `TEXT_MAX_BUFFERED_LEN`, `TEXT_QUIESCENCE_MS`,
`TextFramingError`, `isAllowlistedTextCommand`, `TextConnectSession`,
`textConnect`, `textDisconnect`, `AcquireGrant.remote_monitor_port`,
`HeldLease.remoteMonitorPort` (41-01); `MonitorChannel`, `MONITOR_CHANNELS`,
`ChannelLockHolder`, `ChannelLockHandle`, `ChannelLockTimeoutError`,
`acquireChannelLock`, `tryAcquireChannelLock`, `currentChannelLockHolder`,
`channelLockRefusalMessage`, `CHANNEL_LOCK_ACQUIRE_TIMEOUT_MS`,
`resetChannelLockForTests`, `withTextChannelLock` (41-02);
`InstanceRecord.monitorClients`, `ClaimMonitorOptions.channel` (41-03);
`StockChannelContention`, `channelContentionFor`, `evidence.channelContention`
(41-04); `promoteLaunchingInstances`, `PromoteLaunchingInstancesDeps` (41-05).

<tasks>

<task type="auto">
  <name>Task 1: Ship the two remedy tools, and keep the other five behind the allowlist</name>
  <reversibility rating="costly">D-01's `one-way` rating is about the door this task deliberately does NOT walk through — a published raw-command tool could never be narrowed without breaking every caller. What is published here is the narrow typed-verb shape instead (no parameters, and one boolean), whose undo is coordinated rather than a migration: widening the allowlist later is additive, and retiring either tool touches the manifest, the dispatch table, the capability registry, `STOCK_ONLY_TOOLS` and the generated support table together. No checkpoint is inserted because CONTEXT.md records the owner choosing this exact shape (D-01), so the door is already walked through; the rating is kept for the record.</reversibility>
  <files>src/mcp/vice/text-tools.ts, src/mcp/vice/text-tools.test.ts, src/mcp/vice/text-protocol.ts, src/mcp/vice/stock-dispatch.ts, src/mcp/vice/stock-dispatch.test.ts, src/mcp/vice/tools-manifest.stock.json, src/mcp/vice/capability-registry.ts, src/mcp/vice/capability-registry.test.ts, src/mcp/vice/package.json, docs/tool-support.md</files>
  <read_first>
    - `src/mcp/vice/text-protocol.ts` and `src/mcp/vice/text-connect.ts` — as
      written by plans 41-01 and 41-02: `TEXT_COMMAND_ALLOWLIST`,
      `isAllowlistedTextCommand`, `TextMonitorClient.command()`'s
      lock-held refusal, `withTextChannelLock`, `textConnect`/`textDisconnect`.
    - `src/mcp/vice/stock-dispatch.ts` — `STOCK_DISPATCH_TABLE`,
      `withStockSession()`, `withDerivedTool()` and its `needsSession` flag,
      `withChannelLockHeld()` as added by plan 41-02, `StockSessionHandler` /
      `StockHandler` / `DerivedPureHandler` shapes, `convertWireError()`,
      `dispatchStock()`'s single-miss refusal through
      `capabilityRefusalMessage()`, and the header's two standing prohibitions
      (never fall through to the fork's HTTP-forwarding path; never acquire a
      broker lease here). Both stand.
    - `src/mcp/vice/stock-handler.ts` — the shared result builders every stock
      handler answers through (`stockAnswer`, `isErrorText` and siblings), so the
      two new handlers use them rather than a third result shape.
    - `src/mcp/vice/stock-dispatch.test.ts` — the `STOCK_ONLY_TOOLS` set, the
      `PROXY_LOCAL_TOOLS` set, and the D-03 name-coverage test that asserts every
      non-stock-only, non-proxy-local stock tool has a fork counterpart. Two new
      stock-only names must join `STOCK_ONLY_TOOLS` or that test fails.
    - `src/mcp/vice/capability-registry.ts` — `CapabilityCategory`, the
      `stock-only-gain` entries and their `providedBy: "stock"` shape, and the
      `vice_machine_config_set` entry whose reason text asserts that runtime warp
      toggling exists only as a text-monitor command in a channel this project
      does not dial. That last clause is false as of this phase.
    - `src/mcp/vice/capability-registry.test.ts` — the mechanical-completeness
      test asserting the registry's name set equals the manifest-derived
      divergence set. Two new stock-only tools need two registry entries or it
      fails.
    - `src/mcp/vice/tools-manifest.stock.json` — an existing simple tool entry
      (`vice_registers_available` is a good model for a stock-only one) for the
      `inputSchema` / `outputSchema` conventions, and `stock-schema-check.ts`'s
      supported subset.
    - `src/mcp/vice/tool-support-table.test.mjs` — the byte-identity drift guard
      for `docs/tool-support.md`, and `scripts/generate-tool-support-table.mjs`,
      the generator that must be re-run rather than the doc hand-edited.
    - `src/mcp/vice/package.json` — the `files[]` array.
  </read_first>
  <action>
Publish exactly two typed verbs, and prove the other five stay unpublished.

NEW `src/mcp/vice/text-tools.ts`. Header comment stating it is the ONE place a
text-channel tool handler lives; that every handler here takes the text
channel's halt authority through `withTextChannelLock` for its whole logical
operation and never issues a bare `command()`; and that no handler here accepts
a caller-supplied command string, now or later — the generic
remote-execution seam this project rejected on the record for `host_tool` would
be worse here, over a channel that is unauthenticated and can read and write host
files. Exports:

- `handleDeviceConsole(args, session, deps)` — takes NO arguments at all
  (`inputSchema` is an empty object with `additionalProperties: false`). It
  issues the single allowlisted verb `device c:` — the colon is required; the
  spelling without it is a syntax error the monitor rejects — inside
  `withTextChannelLock("vice_device_console", …)`, and answers with the framed
  response plus a statement that the default device was reset to the main CPU.
- `handleWarpSet(args, session, deps)` — takes exactly one parameter,
  `enabled: boolean`, typed as a boolean and never as a string, so nothing a
  caller supplies is concatenated into the command line. It selects the
  allowlisted verb `warp on` or `warp off` by branch, issues it inside
  `withTextChannelLock("vice_warp_set", …)`, and answers with the framed
  response — which, per the existing measurement, reports warp's own state, so
  the answer carries the observed state rather than an assumption that the write
  took. Its description states the fact this tool does NOT change: there is no
  runtime `WarpMode` *resource* on stock; this is a monitor *command*, and warp
  requested at launch time is a separate mechanism.

Both handlers need a live text session. Obtain it through `textConnect()` using
the coordinates already on the lease — never by dialing a raw host/port and
never by opening a second broker lease. If the session lifecycle established in
plan 41-01 holds one text session for the session's lifetime, reuse that held
session; do not add a second acquisition path. State at the call site which of
the two it is.

`text-protocol.ts`: confirm `TEXT_COMMAND_ALLOWLIST` already carries all eight
verbs, and add the D-02 rationale to its comment if it is not already there — the
five parse-target verbs (`memmapshow`, `prof flat`, `chis`, `bt`, `io`) are
reachable in-process, and therefore covered by this phase's framing controls, but
are deliberately NOT advertised as tools until their owning parser lands. A tool
never ships returning a blob it cannot interpret.

`stock-dispatch.ts`: register the two handlers in `STOCK_DISPATCH_TABLE` through
the existing adapter — `withStockSession` for both, so each goes through the one
adapter every table entry goes through, and each therefore also takes the
binary-side lock for its session preamble before its own text hold. If holding
both channels' authority in one call is not what plan 41-02's integration
produces, use `withDerivedTool({ needsSession: true })` instead and state at the
call site which adapter was chosen and why — never a third adapter and never a
direct table entry that bypasses both.

`tools-manifest.stock.json`: add the two entries. `vice_device_console`'s
`inputSchema` is `{ type: "object", additionalProperties: false }` with no
properties. `vice_warp_set`'s `inputSchema` has exactly one property, `enabled`,
typed `boolean`, listed in `required`, with `additionalProperties: false`. Both
`outputSchema`s stay inside `stock-schema-check.ts`'s supported subset. Both
descriptions state that the tool reaches VICE's text monitor over the
`-remotemonitor` channel and that it halts the machine for the duration of the
command, exactly as a binary-monitor command does.

`stock-dispatch.test.ts`: add both names to `STOCK_ONLY_TOOLS`. Add a structural
case proving D-01 mechanically: walk every tool entry in
`tools-manifest.stock.json` and assert none has an input property whose name or
description indicates a free-text monitor command, and specifically that
`vice_warp_set`'s only property is a boolean. Add a second structural case
proving D-02: assert the five parse-target verb strings appear in
`TEXT_COMMAND_ALLOWLIST` and that none of them appears as a tool `name` in the
stock manifest.

`capability-registry.ts`: add two `stock-only-gain` entries, `providedBy: "stock"`,
one per new tool, each with a reason in the register the existing two use — the
fork's custom HTTP API has no equivalent, and this is the native text-monitor
command. Then correct the `vice_machine_config_set` entry's reason: its clause
asserting that runtime warp toggling lives in a channel this project does not
dial is now false. Rewrite that clause to say the channel IS dialed as of this
phase and that runtime warp toggling is available through `vice_warp_set`, while
keeping the entry's actual claim intact and unweakened — stock still has no
runtime `WarpMode` resource, still measured `err=0x01 OBJECT_MISSING` over
`RESOURCE_GET`, and warp requested at launch time is still a separate mechanism.
Do not change the entry's `category` or `providedBy`.

`capability-registry.test.ts`: the mechanical-completeness test should now pass
with the two added entries; add an assertion that the corrected
`vice_machine_config_set` reason no longer contains a claim that the channel is
undialed, derived by reading the registry entry at test time.

NEW `src/mcp/vice/text-tools.test.ts`: unit cases with a stubbed text client —
`handleDeviceConsole` issues exactly the one allowlisted verb and nothing else;
`handleWarpSet(true)` and `handleWarpSet(false)` select the two verbs by branch;
`handleWarpSet` refuses a non-boolean `enabled` by name without writing to the
socket; both handlers hold the text channel lock for the duration (observed via
`currentChannelLockHolder()` from inside the stubbed client) and release it on
both the success and the throw path; and a `ChannelLockTimeoutError` surfaces as
refusal text byte-identical to `channelLockRefusalMessage()`'s output rather than
being re-worded.

Add `text-tools.ts` to `package.json`'s `files[]`. Keep every string and template
literal in it free of a phase number. Finally regenerate the support table with
`node scripts/generate-tool-support-table.mjs` and commit `docs/tool-support.md`
— never hand-edit it; it is byte-identity guarded.
  </action>
  <acceptance_criteria>
    - `src/mcp/vice/text-tools.ts` contains `export async function handleDeviceConsole` and `export async function handleWarpSet`.
    - `tools-manifest.stock.json` contains exactly two new tool names, `vice_device_console` and `vice_warp_set`, and its tool count rises from 38 to 40.
    - `vice_device_console`'s `inputSchema` has no `properties` key and sets `additionalProperties: false`.
    - `vice_warp_set`'s `inputSchema` has exactly one property, `enabled`, of `type: "boolean"`, listed in `required`, with `additionalProperties: false`.
    - No tool entry in `tools-manifest.stock.json` has a property of `type: "string"` that is passed to the text monitor as a command — asserted by the structural case, not by reading.
    - The five parse-target verbs are present in `TEXT_COMMAND_ALLOWLIST` and absent from every tool `name` in `tools-manifest.stock.json`.
    - `STOCK_ONLY_TOOLS` in `stock-dispatch.test.ts` contains both new names, and the D-03 name-coverage test is green.
    - `capability-registry.ts` has two new `stock-only-gain` entries with `providedBy: "stock"`.
    - `! grep -aq 'a channel this project does not dial' src/mcp/vice/capability-registry.ts` succeeds.
    - `capability-registry.ts` still contains `OBJECT_MISSING` and still states that warp on stock is requested at launch time — the corrected clause narrows nothing.
    - `docs/tool-support.md` is byte-identical to the generator's output, and the generator was re-run rather than the doc edited.
    - `package.json`'s `files[]` includes `text-tools.ts`.
    - Behavior: `handleWarpSet` refuses a non-boolean `enabled` by name with no byte written to the socket.
    - Behavior: both handlers hold the text-channel lock for the duration and release it on both the success and the throw path.
    - Behavior: a `ChannelLockTimeoutError` reaching either handler surfaces as refusal text byte-identical to `channelLockRefusalMessage()`'s output.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test text-tools.test.ts stock-dispatch.test.ts capability-registry.test.ts manifest-arg-compat.test.ts stock-schema-check.test.ts 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, or the summary block does not report `fail 0`</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/generate-tool-support-table.mjs && node --test src/mcp/vice/tool-support-table.test.mjs 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, or the summary block does not report `fail 0` — a red here means `docs/tool-support.md` was hand-edited or the generator was not re-run</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && ! grep -aq 'a channel this project does not dial' src/mcp/vice/capability-registry.ts</automated>
    <fails_when>non-zero exit, meaning the registry still asserts the text channel is undialed</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, or any line matching `error TS` appears in the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated 2>&1 | tail -30</automated>
    <fails_when>non-zero exit, or any failing test outside `anno-register.test.ts`'s documented 2-failure baseline</fails_when>
  </verify>
  <done>Two typed text-channel verbs reach `tools/list`, the five parse-target commands stay behind the allowlist with a structural test proving it, and the registry's now-false claim about the undialed channel is corrected.</done>
</task>

<task type="auto">
  <name>Task 2: Exercise the remedy live, and re-cite the three narrowed constraints (D-04)</name>
  <files>docs/phase41-text-channel-live-evidence.md, CLAUDE.md, src/mcp/vice/text-monitor-live.test.ts</files>
  <read_first>
    - `CLAUDE.md` — the three narrowed Constraints bullets in full: the
      `default_memspace` / `device c:` one, the runtime `WarpMode` resource one,
      and the `CPUHISTORY_GET` >= 3.10 one. Read each completely, including its
      existing parenthetical scoping clause and its `MEASURED 2026-08-27`
      citation, so the edit adds a citation rather than restating the claim.
    - `docs/phase39-dual-channel-coexistence-gate-findings.md` —
      § *Recorded facts that gate nothing*, in particular the `chis`-on-3.9
      fixture finding, which is the independent Phase-39 citation the third
      bullet gains beside the probe's.
    - `src/mcp/vice/docs-linerefs.test.ts` — what it actually checks: the
      `vice-proxy.ts:<N>` citations in `CLAUDE.md`'s Architecture bullet. Read
      it so the edits to the three Constraints bullets do not disturb those
      citations, and so any line reference this task writes is verified against
      the source in the same commit.
    - `src/mcp/vice/text-monitor-live.test.ts` — as extended by plans 41-01,
      41-02 and 41-04.
    - `src/mcp/vice/text-tools.ts` — as written by Task 1.
    - `.planning/phases/41-the-text-channel-its-serialization-authority-and-the-content/41-04-SUMMARY.md`
      — the contention run's recorded verbatim answer, for the evidence
      document's cross-reference.
  </read_first>
  <action>
Take the measurements with the channel open, write them down, and point the three
constraints at them.

**The live runs**, all against genuine stock `/usr/bin/x64sc` (VICE 3.9) with the
broker stopped, added to `src/mcp/vice/text-monitor-live.test.ts`:

1. **The `default_memspace` contamination and remedy** — criterion 5's
   "exercised, not merely made available". Contaminate `default_memspace` by
   arming a drive checkpoint and letting it hit, then confirm main-CPU stepping
   is broken (a stepping call steps the drive CPU, or a bank-qualified condition
   fails outright — assert whichever the run actually shows, and record which).
   Then issue `device c:` over the text channel and confirm main-CPU stepping is
   restored. If a drive checkpoint hit cannot be produced on this host, the test
   must SKIP with a named reason rather than assert a contamination it did not
   observe — and that skip is a real gap to record in the evidence document and
   in the SUMMARY, not a silent pass. Do not simulate the contamination.
2. **The `warp on` / `warp off` re-probe** — issue each through
   `vice_warp_set` while the channel is open and record the verbatim response,
   including whatever state it reports about itself.
3. Assert, in both cases, that no binary-side stepping call acquired the text
   channel or made a text round trip — D-03's requirement that `device c:` is an
   explicit tool and not auto-healing on the stepping path. Observe it from the
   text client's own command counter before and after a binary stepping call,
   not from source inspection.

**The evidence document.** NEW `docs/phase41-text-channel-live-evidence.md`,
recording each run with: the resolved absolute binary path, the reported VICE
version, the date, the exact command line used, and the verbatim observed output.
State plainly for each claim what was and was not measured. Include a
cross-reference to `41-04-SUMMARY.md`'s contention run. Where run 1 skipped, say
so in the same register — a named gap, with what would be needed to close it.

**The three re-citations in `CLAUDE.md`** (D-04). Each bullet gains this phase's
citation **beside** the existing one; none is deleted, none is weakened, and no
existing clause is widened:

- The `default_memspace` bullet's scoping clause keeps its
  `MEASURED 2026-08-27` probe citation and gains a citation to this phase's own
  live contamination-and-remedy evidence in
  `docs/phase41-text-channel-live-evidence.md`. If run 1 skipped, the citation
  says exactly that — the remedy is confirmed available and the contamination
  path was not reproducible on this host — rather than claiming an unperformed
  measurement.
- The `WarpMode` bullet's scoping clause keeps its probe citation and gains this
  phase's re-probe citation. The bullet's primary claim is untouched: there is
  still no runtime `WarpMode` resource, and warp control on the stock backend is
  still launch-time. The clause continues to distinguish the absent *resource*
  from the working *command* — that distinction is the whole point and must
  survive the edit verbatim in substance.
- The `CPUHISTORY_GET` bullet's scoping clause keeps its probe citation and gains
  **Phase 39's** independent citation beside it (the `chis`-on-3.9 fixture
  finding, which Phase 39 re-confirmed on its own evidence), plus this phase's
  document as the collected record. The version floor on the *opcode* is
  unchanged, and the `PARSE-04` polarity distinction it carries is unchanged.

Do not renumber, reorder or merge the three bullets, and do not touch
`CLAUDE.md`'s Architecture bullet or its `vice-proxy.ts:<N>` citations — those are
mechanically checked, and any line reference elsewhere in this edit must be
verified against the source in the same commit.
  </action>
  <acceptance_criteria>
    - `docs/phase41-text-channel-live-evidence.md` exists and records, for each run: the absolute binary path, the reported VICE version, the date, the exact command, and the verbatim output.
    - `grep -c 'phase41-text-channel-live-evidence' CLAUDE.md` returns 3 — one citation added to each of the three narrowed bullets.
    - `CLAUDE.md` still contains all three original scoping clauses: `grep -c 'binary monitor only' CLAUDE.md` returns at least 3.
    - `CLAUDE.md` still contains `MEASURED 2026-08-27` at least three times — no existing citation was replaced.
    - `CLAUDE.md` still states that there is no runtime `WarpMode` resource and that warp control on the stock backend must be launch-time — neither claim removed or hedged.
    - `CLAUDE.md`'s `CPUHISTORY_GET` bullet cites `docs/phase39-dual-channel-coexistence-gate-findings.md` beside the probe citation.
    - `CLAUDE.md`'s Architecture bullet and its `vice-proxy.ts:<N>` citations are unmodified by this task.
    - Behavior (live): `device c:` issued through `vice_device_console` restores main-CPU stepping after an observed `default_memspace` contamination — or the test SKIPS with a named reason and that gap is recorded in both the evidence document and the SUMMARY.
    - Behavior (live): `vice_warp_set` with `enabled` true and false each return a response, recorded verbatim.
    - Behavior (live): a binary-side stepping call increments no text-channel command counter — proving `device c:` is not auto-healing on the stepping path.
  </acceptance_criteria>
  <precondition>No broker daemon and no leftover `x64sc` process is running (`pgrep -fa vice-broker`, `pgrep -fa x64sc` both empty). `/usr/bin/x64sc` exists and is genuine unpatched stock VICE 3.9, reached by absolute path because the fork shadows it on `$PATH`. Launch flag order is load-bearing: `-default` must precede `-binarymonitor` and `-remotemonitor`, or the monitor never binds.</precondition>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && VICE_LIVE_STOCK_BIN=/usr/bin/x64sc node --test text-monitor-live.test.ts 2>&1 | tail -40</automated>
    <fails_when>non-zero exit, or the summary does not report `fail 0`, or `skipped` is greater than 1 (at most the drive-contamination case may skip, and that skip must be named in the output and recorded as a gap; any other skip means the env gate was unsatisfied and nothing was measured)</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -c 'phase41-text-channel-live-evidence' CLAUDE.md</automated>
    <fails_when>output is not 3 — fewer means a narrowed bullet was left citing only the exploration probe, more means a citation was added outside the three bullets</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -c 'binary monitor only' CLAUDE.md</automated>
    <fails_when>output is less than 3, meaning a scoping clause was deleted rather than re-cited</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-linerefs.test.ts docs-dangling-refs.test.ts docs-absorbed-decisions.test.ts 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, or the summary block does not report `fail 0` — a red here means a cited line number in `CLAUDE.md` drifted or a dangling reference was introduced</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated 2>&1 | tail -30</automated>
    <fails_when>non-zero exit, or any failing test outside `anno-register.test.ts`'s documented 2-failure baseline</fails_when>
  </verify>
  <done>The `default_memspace` remedy is exercised live (or its gap named), `warp on`/`warp off` is re-probed with the channel open, the evidence is written down with binary, version, date and command, and all three narrowed constraints cite this phase's evidence beside the probe's — none deleted, none weakened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool arguments → the text-monitor command line | The only shipped parameter is a boolean; a string parameter here would be a command-injection surface over an unauthenticated channel that can read and write host files |
| a published tool's argument shape → every future caller | A published shape cannot be narrowed without breaking callers |
| a re-cited constraint → every later phase that reads it | A weakened or deleted constraint licenses work the measurement does not support |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-25 | Tampering / Elevation of Privilege | `vice_warp_set`'s `enabled` parameter | high | mitigate | The parameter is typed `boolean` with `additionalProperties: false`, and the handler selects one of two literal allowlisted verbs by branch — no caller-supplied value is ever concatenated into the command line. A structural test walks every stock manifest entry and asserts no string property is passed to the text monitor as a command. Canon breadcrumb: generic command injection is `/gsd-secure-phase` canon and is not minted as a separate prohibition; the bespoke prohibition here is against the *seam*, not the injection. |
| T-41-26 | Elevation of Privilege | the published tool surface | high | mitigate | D-01's typed-verb shape is what is published: two tools, one with no parameters and one with a boolean. No free-text command field exists at any point, so the one-way door being walked through is the narrow shape rather than the wide one. |
| T-41-27 | Information Disclosure | the five unadvertised parse-target verbs | medium | mitigate | They stay reachable in-process only, and a structural test asserts none appears as a tool name in the stock manifest. A tool never ships returning a blob it cannot interpret (D-02), so nothing hands a caller raw text this project cannot yet parse. |
| T-41-28 | Denial of Service | `device c:` on the binary stepping path | medium | mitigate | D-03 keeps it an explicit tool: no text round trip and no lock acquisition is added to `ADVANCE_INSTRUCTIONS` or `EXECUTE_UNTIL_RETURN`, asserted live by observing the text client's command counter across a binary stepping call. Auto-healing would defend a route nothing currently opens, at the cost of the hottest binary path. |
| T-41-29 | Repudiation | the three re-cited constraints and the evidence document | medium | mitigate | Four grep-shaped gates prove three citations were added, no scoping clause was deleted, no existing `MEASURED` citation was replaced, and the mechanically-checked Architecture citations are untouched. A skipped live run is recorded as a named gap rather than cited as a measurement. |
| T-41-SC | Tampering | npm/pip/cargo installs | low | accept | Zero dependencies added; `41-RESEARCH.md` § *Package Legitimacy Audit* records the phase as not applicable and no install task exists. |
</threat_model>

<verification>
- `npm run typecheck` clean.
- `text-tools.test.ts`, `stock-dispatch.test.ts`, `capability-registry.test.ts`, `manifest-arg-compat.test.ts`, `stock-schema-check.test.ts` green.
- `docs/tool-support.md` regenerated and byte-identical to the generator's output; `tool-support-table.test.mjs` green.
- `docs-linerefs.test.ts`, `docs-dangling-refs.test.ts`, `docs-absorbed-decisions.test.ts` green after the `CLAUDE.md` edits.
- `npm run test:automated` green against the documented 2-failure baseline.
- `text-monitor-live.test.ts` run by hand with the broker stopped, against `/usr/bin/x64sc`, with at most the drive-contamination case skipping and that skip recorded as a gap.
- Four grep gates green on `CLAUDE.md` and `capability-registry.ts`.
</verification>

<success_criteria>
- A user's tool call reaches VICE's text monitor and gets a complete framed response back, through a typed verb with no free-text command field.
- Exactly two text-channel tools ship; the five parse-target commands stay behind the internal allowlist with a structural test proving it.
- The `default_memspace` remedy is exercised live, or its gap is named honestly in both the evidence document and the SUMMARY.
- All three narrowed `CLAUDE.md` constraints cite this phase's evidence beside the exploration probe's; none is deleted and none is weakened (D-04).
</success_criteria>

<output>
Create `.planning/phases/41-the-text-channel-its-serialization-authority-and-the-content/41-06-SUMMARY.md` when done.
</output>
