## Criterion 3(4) -- pane captures from `regenerator2000 --mcp-server r2000-probe.vsf`
## under tmux (session r2000probe06), plan 09-06 Task 2.
##
## Three captures, in the order they happened. This is PRIMARY evidence for the
## criterion, not a formality -- the "Import Context Setup" modal (capture 1) is
## the exact surface that shows what regenerator2000 derived from the .vsf,
## before any human/automation action changes it.
##
## SUPERSEDED NOTICE: this whole first snapshot (`r2000-probe.vsf`) was found,
## during Task 3, to NOT actually carry the fixture's $c000 bytes -- a
## cross-connection session finding recorded in full in
## criterion4-vsf-load.txt's `## CORRECTION` section. Captures 1-3 below are
## kept as a real, honest record of what regenerator2000 does with a .vsf
## whose origin/entry-point ARE genuinely carried (both fields below are
## correct), even though this particular snapshot's memory content was not.
## The CORRECTED run (session r2000probe06v2, against `r2000-probe-v2.vsf`)
## is captured separately below as CAPTURES 4-6, and THAT is what Task 3's
## R2000_DERIVED/COMPARISON is scored against.

=== CAPTURE 1: immediately after launch, "Import Context Setup" modal up ===

 File  Edit  Jump  Search  View  Debugger  Help
[Disassembly pane showing raw .byte fallback around $E5CA-$E5F2 -- the
snapshot's own KERNAL-ROM-range bytes at the live PC, not yet disassembled as
instructions; Hex Dump pane showing $E350-$E610 screencode-shifted bytes]

                   ┌ Import Context Setup ─────────────────────────────────┐
                   │                                                        │
                   │System: < Commodore 64 >                                │
                   │                                                        │
                   │Origin Address: $0000                                   │
                   │Entry Point Address: $E5CF                              │
                   │                                                        │
                   │[X] Disassemble from Entry Point Address                │
                   │                                                        │
                   │  < Confirm >        < Cancel >                         │
                   │                                                        │
                   └────────────────────────────────────────────────────────┘

 MCP Server active on http://127.0.0.1:3000/mcp
 Commodore 64 | 64tass | Undefined: $0000-$FFFF | Cursor: $E5CF | File: "r2000-probe.vsf"

**What this modal states, verbatim, before any Confirm/Cancel action:**
- System: "Commodore 64" (a value, not blank -- see criterion4-vsf-load.txt's
  R2000_DERIVED section for whether this is genuinely derived from the
  snapshot's own "C64SC" machine-name field or a coincidental default)
- Origin Address: "$0000" (matches `file_io.rs`'s fixed `self.origin =
  Addr::ZERO` for every .vsf load, source-confirmed in Task 1)
- Entry Point Address: "$E5CF" -- matches, byte-for-byte, the MAINCPU PC this
  plan's own direct parse of the .vsf file found in Task 1 ($e5cf). This is
  the snapshot's start address, genuinely carried through.
- Status bar bottom-right, BEFORE Confirm: "Undefined: $0000-$FFFF" -- the
  entire 64K is Undefined block type at this instant, confirming the full
  memory image loaded (not merely a small blob), matching `size: 65536` this
  plan's Task 2 MCP query later confirmed via `r2000_get_binary_info`.

=== CAPTURE 2: after Alt+S, Save-As dialog open with pre-filled filename ===

[Disassembly pane now showing real mnemonics from $E5CF onward -- "start"
label placed at $E5CF, "brk"/".byte $ff ; Invalid or partial instruction"/
"bpl b_E5F5" visible -- confirming the modal's Confirm action (Enter, sent
between captures 1 and 2) ran flow analysis from the stated Entry Point.
Status bar changed from "Undefined: $0000-$FFFF" to "Context applied", then
to "Commodore 64 | 64tass | Code: $E5CF-$EADB | Cursor: $E5CF |
File: "r2000-probe.vsf"" after the modal closed.]

                                       ┌ Filename ──────────────────────────┐
                                       │r2000-probe          .regen2000proj │
                                       └─────────────────────────────────────┘

 Enter Project filename
 Commodore 64 | 64tass | Code: $E5CF-$EADB | Cursor: $E5CF | File: "r2000-probe.vsf"

SAVE_AS_DEFAULT_FILENAME: r2000-probe.regen2000proj -- pre-filled exactly as
`<stem>.regen2000proj`, matching plan 09-03's already-confirmed A2. Accepted
as-is (matches the target filename this plan needed).

Full raw capture (200x50, all panes) kept at
$HOME/.cache/c64-re-tools/phase9/06-pane-after-alt-s.txt for anyone who wants
the untrimmed version; the excerpt above is the load-bearing part, not a
retype -- every character quoted above appears verbatim in that raw capture.

=== CAPTURE 3: after Enter, project saved ===

 Project saved: r2000-probe.regen2000proj
 Commodore 64 | 64tass | Code: $E5CF-$EADB | Cursor: $E5CF | File: "r2000-probe.vsf"

$ ls -la $HOME/.cache/c64-re-tools/phase9/r2000-probe.regen2000proj
-rw-rw-r-- 1 henrik henrik 4654 20 aug 10.59 /home/henrik/.cache/c64-re-tools/phase9/r2000-probe.regen2000proj

VSF_PROJECT_FILE: /home/henrik/.cache/c64-re-tools/phase9/r2000-probe.regen2000proj

Full raw capture kept at $HOME/.cache/c64-re-tools/phase9/06-pane-after-enter.txt.

## No refusal occurred at any step -- the snapshot was accepted, the modal
## showed real (if partly coincidental, per the load transcript) values, and
## the bootstrap produced a working project file with zero human intervention.

## ===========================================================================
## CORRECTED RUN -- against r2000-probe-v2.vsf (session r2000probe06v2),
## the snapshot independently confirmed to carry the fixture bytes at $c000.
## This is what Task 3's R2000_DERIVED/COMPARISON is scored against.
## ===========================================================================

=== CAPTURE 4: immediately after launch, "Import Context Setup" modal up ===

 File  Edit  Jump  Search  View  Debugger  Help
[Disassembly pane showing raw .byte fallback from $E5CF onward; Hex Dump pane
showing $E360+ screencode-shifted bytes -- same uninitialised-RAM pattern as
before in this unrelated region, expected since only $c000 was written]

                   ┌ Import Context Setup ─────────────────────────────────┐
                   │                                                        │
                   │System: < Commodore 64 >                                │
                   │                                                        │
                   │Origin Address: $0000                                   │
                   │Entry Point Address: $E5D4                              │
                   │                                                        │
                   │[X] Disassemble from Entry Point Address                │
                   │                                                        │
                   │  < Confirm >        < Cancel >                         │
                   │                                                        │
                   └────────────────────────────────────────────────────────┘

**Entry Point Address: $E5D4 -- matches this plan's own direct parse of
`r2000-probe-v2.vsf`'s MAINCPU module PC exactly ($e5d4, confirmed in
criterion4-vsf-load.txt's CORRECTION section). Origin Address: $0000, System:
"Commodore 64" -- same values and same provenance caveat on System as
before (coincidental default, not a genuine machine_name derivation, per the
`file_io.rs`/`dialog_import_context.rs` source trace already established).**

Full raw capture kept at $HOME/.cache/c64-re-tools/phase9/06-pane-v2-initial.txt.

=== CAPTURE 5: after Enter (Confirm) then Alt+S, Save-As dialog open ===

[Status bar: "Context applied", then "Commodore 64 | 64tass |
Code: $E5D4-$E6CC | Cursor: $E5D4 | File: \"r2000-probe-v2.vsf\"" -- flow
analysis ran from the stated Entry Point Address, same mechanism as before.]

                                       ┌ Filename ──────────────────────────┐
                                       │r2000-probe-v2        .regen2000proj │
                                       └─────────────────────────────────────┘

 Enter Project filename
 Commodore 64 | 64tass | Code: $E5D4-$E6CC | Cursor: $E5D4 | File: "r2000-probe-v2.vsf"

SAVE_AS_DEFAULT_FILENAME (corrected run): r2000-probe-v2.regen2000proj --
again exactly `<stem>.regen2000proj`. Accepted as-is.

Full raw capture kept at $HOME/.cache/c64-re-tools/phase9/06-pane-v2-after-alt-s.txt.

=== CAPTURE 6: after Enter, project saved ===

 Project saved: r2000-probe-v2.regen2000proj
 Commodore 64 | 64tass | Code: $E5D4-$E6CC | Cursor: $E5D4 | File: "r2000-probe-v2.vsf"

$ ls -la $HOME/.cache/c64-re-tools/phase9/r2000-probe-v2.regen2000proj
-rw-rw-r-- 1 henrik henrik 4727 20 aug 11.13 /home/henrik/.cache/c64-re-tools/phase9/r2000-probe-v2.regen2000proj

VSF_PROJECT_FILE (corrected, superseding the earlier one): /home/henrik/.cache/c64-re-tools/phase9/r2000-probe-v2.regen2000proj

**Live MCP queries against the corrected run, same two tools as before:**

$ node $PROBE_DIR/mcp-harness.mjs r2000_get_binary_info '{}'
CALL_TOOL_RESULT:
{ "content": [ { "type": "text", "text": "{\n  \"description\": \"\",\n  \"entropy\": 1.255272388458252,\n  \"filename\": \"r2000-probe-v2.vsf\",\n  \"may_contain_undocumented_opcodes\": false,\n  \"origin\": 0,\n  \"size\": 65536,\n  \"system\": \"Commodore 64\"\n}" } ] }

$ node $PROBE_DIR/mcp-harness.mjs r2000_get_disassembly_cursor '{}'
CALL_TOOL_RESULT:
{ "content": [ { "type": "text", "text": "$E5D4" } ] }

Both confirm the modal exactly: origin 0, system "Commodore 64", cursor/entry
point $E5D4. No drift between the modal and the live MCP surface, same as
the first (superseded) run.

Full raw capture kept at $HOME/.cache/c64-re-tools/phase9/06-pane-v2-after-enter.txt.

**Teardown, proven:**

$ tmux kill-session -t r2000probe06v2
kill_exit=0
$ tmux has-session -t r2000probe06v2
no server running on /tmp/tmux-1000/default
exit=1
$ ss -ltn | grep ':3000'; echo "grep-exit=$?"
grep-exit=1
$ pgrep -af 'regenerator2000 --mcp-server'
(no match -- process gone)
