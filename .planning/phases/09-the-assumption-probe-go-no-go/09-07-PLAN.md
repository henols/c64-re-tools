---
phase: 09-the-assumption-probe-go-no-go
plan: 07
type: execute
wave: 4
depends_on: [09-02, 09-04, 09-05, 09-06]
files_modified:
  - docs/phase9-regenerator2000-probe-findings.md
  - .planning/phases/09-the-assumption-probe-go-no-go/09-RESEARCH.md
  - .planning/phases/09-the-assumption-probe-go-no-go/09-VALIDATION.md
autonomous: true
requirements: [R2000-16]
requirement_subparts: ["R2000-16 criterion 4 — every answer recorded in the repo as evidence a later session can re-read, every failure an accepted limit naming what it breaks", "R2000-16 criterion 5 — a machine-readable go/no-go verdict", "covers R2000-16(1)(2)(3)(4)(5) by consolidation"]

must_haves:
  truths:
    - "All five criteria have a recorded outcome in one durable document a later session can re-read without knowing which evidence file to open"
    - "Every failure and every could-not-run is recorded as an accepted limit naming what it breaks"
    - "A machine-readable verdict of proceed, degrade or reconsider exists at a stated location, derived from a decision rule written down before the run rather than judged at the time"
    - "Every research claim the transcripts contradicted has been corrected in 09-RESEARCH.md"
  artifacts:
    - path: "docs/phase9-regenerator2000-probe-findings.md"
      provides: "The durable, normative findings document with the verdict in its frontmatter"
      min_lines: 90
      contains: "verdict:"
    - path: ".planning/phases/09-the-assumption-probe-go-no-go/09-VALIDATION.md"
      provides: "Signed-off validation contract with nyquist_compliant: true"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "docs/phase9-regenerator2000-probe-findings.md"
      to: ".planning/phases/09-the-assumption-probe-go-no-go/evidence/"
      via: "every criterion section cites its evidence file by relative path"
      pattern: "evidence/criterion"
    - from: "docs/phase9-regenerator2000-probe-findings.md frontmatter"
      to: "Phase 10's planner"
      via: "the `verdict:` key, which is the gate Phase 10 reads"
      pattern: "^verdict: (proceed|degrade|reconsider)$"
---

<objective>
Answer criteria 4 and 5 — the two that make this phase a gate rather than an exercise.

**Criterion 4:** every answer is recorded in the repo as evidence a later session can re-read,
and every failure is recorded as an **accepted limit naming what it breaks**. The `evidence/`
transcripts already hold the raw observations; this plan consolidates them into one durable,
normative document, in the shape this repo already uses for probe evidence
(`docs/phase1-probe-results.md`, `docs/phase2-backend-probe-evidence.md`).

**Criterion 5 is the most important artifact in the phase.** A go/no-go verdict naming which
of three routes the milestone takes: **proceed** as scoped; **degrade** — bootstrap becomes a
documented one-time interactive step and Phase 10/11 scope is amended; or **reconsider** the
milestone. It must be **structural, not prose**, because Phase 10's planner reads it as a
gate — so it is a frontmatter key, and it is derived from the decision rule written out below
rather than from a judgement made at run time.

Purpose: the ROADMAP's own note — "this phase is the gate, and its verdict is an artifact, not
a judgement held in a head." No Phase 10 or Phase 11 plan may be written before this document
exists.

Output: `docs/phase9-regenerator2000-probe-findings.md`; corrections applied to
`09-RESEARCH.md`; `09-VALIDATION.md` signed off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-the-assumption-probe-go-no-go/09-RESEARCH.md
@.planning/phases/09-the-assumption-probe-go-no-go/09-VALIDATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<decision_rule>
**Binding. Stated here, before the run, so the verdict is derived and not judged.** Read the
seven inputs from the evidence files (never from a summary's paraphrase), then evaluate the
rules **in order** and take the first that matches. Record which rule fired.

Inputs, each read from the named outcome line:

| Input | Source line | Source file |
|---|---|---|
| `c1_build` | `INSTALLED_VERSION:` present → `pass`; recorded install failure → `could-not-run` | `evidence/criterion1-install-and-version.txt` |
| `c1_container_cost` | both `*_BYTES` lines numeric → `measured`; either `could-not-run` → `could-not-run` | `evidence/criterion1-container-toolchain-cost.txt` |
| `c2a_pty_tolerance` | `PTY_TOLERANCE:` | `evidence/criterion2-pty-transcript.txt` |
| `c2b_bootstrap_automatable` | `BOOTSTRAP_AUTOMATABLE:` | `evidence/criterion2-pty-transcript.txt` |
| `c3_2_reassembly` | `REASSEMBLY:` | `evidence/criterion3-reassembly.txt` |
| `c3_3_export_lbl` | `EXPORT_LBL:` | `evidence/criterion3-export-lbl.txt` |
| `c3_4_vsf_load` | `VSF_LOAD:` | `evidence/criterion4-vsf-load.txt` |

Rules, first match wins:

- **R1 → `reconsider`.** `c1_build` is not `pass`. D-R2 makes regenerator2000 a *required*
  prerequisite, not an optional accelerator; a build that will not install on a host meeting
  the documented toolchain floor cannot be a required prerequisite, and no other criterion is
  answerable without it.
- **R2 → `reconsider`.** `c2a_pty_tolerance` is not `pass`. This is the ROADMAP's own
  reconsider condition: a regenerator2000 that cannot run under automation at all puts the
  annotation store out of a skill's reach, and with it this milestone's thesis. Note the
  asymmetry deliberately: it is **2a**, not 2b, that triggers this.
- **R3 → `degrade`.** `c2b_bootstrap_automatable` is not `pass` (2a having passed). This is
  exactly the ROADMAP's degrade branch: bootstrap becomes a documented one-time interactive
  step, every affected playbook says so at its point of use, and `R2000-09` delivers
  documentation rather than automation.
- **R4 → `degrade`.** Any of `c3_2_reassembly`, `c3_3_export_lbl`, `c3_4_vsf_load` is not
  `pass` (including `partial` and `could-not-run`). The milestone proceeds, with named
  amendments: a non-pass on 3(2) means Phase 10 criterion 4's deletion of `acme-build`'s
  `disasm` verb is **not earned** and `R2000-06` must establish it first; a non-pass on 3(3)
  means Phase 11's symbol round trip carries a conversion step or a documented gap; a non-pass
  on 3(4) means the ROADMAP's standing "prefer `.vsf` over `.raw`" constraint is unsupported
  as worded and Phase 10 criterion 3 must say so.
- **R5 → `proceed`.** Everything above passed.

**`c1_container_cost` never changes the verdict.** It is a measurement, not a gate, and the
milestone defined no cost threshold — inventing one here would be the planner making a scope
decision it has no authority to make. A `could-not-run` on it is recorded as an accepted limit
on `R2000-03`'s install documentation ("the toolchain cost is unmeasured and must be
documented as unknown rather than estimated") and nothing more. Say this explicitly in the
document so a later reader cannot mistake its absence for a failure.
</decision_rule>

<evidence_conventions>
Plan 09-01's `<evidence_conventions>` block remains binding. One addition specific to this
plan:

12. **This plan is the single owner of `09-RESEARCH.md`.** Plans 09-01 through 09-06 were
    forbidden from editing it and instead recorded `## RESEARCH CORRECTIONS` sections in their
    own evidence files, so that three parallel wave-3 plans could not collide on one file.
    Task 3 collects them all and applies them in one pass.
</evidence_conventions>

<tasks>

<task type="auto">
  <name>Task 1: Write the durable findings document covering all five criteria</name>
  <files>docs/phase9-regenerator2000-probe-findings.md</files>
  <read_first>
    - `docs/phase1-probe-results.md` — the shape to match: a header block with run date, host and builds tested; a summary table; then one section per item, each transcribed from raw output rather than from expectation; then the raw captures. Match this. Do not invent a new document shape
    - `docs/phase2-backend-probe-evidence.md` — the shape this repo uses when a check **could not be performed**: the override is named, the reason is named, what was affected is tabulated, and what was done instead is stated. This is the model for every `could-not-run` section
    - all six evidence files under `.planning/phases/09-the-assumption-probe-go-no-go/evidence/` — the actual findings; read the outcome lines from the files, never from a summary's paraphrase
    - `.planning/ROADMAP.md` § `### Phase 9` — the five success criteria in their own words, and the Notes
    - `.planning/REQUIREMENTS.md` § `R2000-16` — the five sub-assumptions verbatim
    - `.planning/phases/09-the-assumption-probe-go-no-go/09-VALIDATION.md` § Evidence Integrity Rules and § Validation Sign-Off — the checklist this document has to satisfy
  </read_first>
  <action>
    Create `docs/phase9-regenerator2000-probe-findings.md`.

    Open with YAML frontmatter. **This is a deliberate, single departure from the convention
    set by `docs/phase1-probe-results.md` and `docs/phase2-backend-probe-evidence.md`, neither
    of which has frontmatter** — criterion 5 requires the verdict to be machine-readable so
    Phase 10's planner can read it as a gate, and a prose sentence is not that. Say so in one
    sentence immediately under the frontmatter, so the departure reads as intentional. The
    frontmatter keys, exactly:
    `phase`, `requirement`, `probe_date`, `regenerator2000_version`, `verdict`,
    `verdict_rule_applied`, and a `criteria:` map with the seven keys named in the
    `<decision_rule>` block above. Leave `verdict` and `verdict_rule_applied` set to the
    literal `TBD` in this task — Task 2 fills them from the rule. Fill the seven `criteria:`
    values now, each copied from its source outcome line.

    Then the body, mirroring `phase1-probe-results.md`'s structure:
    - **Run date, host, and build tested.** Include the installed version verbatim from
      `INSTALLED_VERSION:`, the resolved crate version, and the correction that the crate is
      licensed **`MIT OR Apache-2.0`**, not solely Apache-2.0 as
      `.planning/notes/regenerator2000-integration.md` and `REQUIREMENTS.md` `R2000-03` both
      state. Note explicitly that Phase 10's `THIRD-PARTY-NOTICES.md` must record both.
    - **Summary table**, one row per criterion: the criterion, its `R2000-16` sub-part, the
      outcome, and the evidence file. Every cell transcribed from an outcome line — none
      filled from expectation. If a cell would be empty, report exactly what happened instead,
      following `phase1-probe-results.md`'s own precedent for that.
    - **One section per criterion**, each with: what was asked, the literal command(s) run, the
      observed result quoted from the transcript, and the outcome. For criterion 2, keep 2a and
      2b as separate subsections and state why the distinction matters to the verdict. For
      criterion 1(5), report **both** numbers — build-toolchain cost and shipped-image cost —
      and state that they are absolute sizes with no baseline to diff against, because this
      repo has no existing devcontainer image.
    - **An `## Accepted limits` section.** One entry per failure and per `could-not-run`,
      collected from the `## ACCEPTED LIMIT` blocks the evidence files already carry. Each
      entry must name **what it breaks**, in the words the consuming phase needs — which
      requirement, which phase criterion, which playbook. This section is criterion 4's actual
      deliverable; an empty section is only valid if every criterion passed, and if so say that
      explicitly rather than omitting the heading.
    - **A `## Corrections to prior documents` section**, listing every contradiction the
      transcripts found, with the observed evidence beside the corrected claim. Include the
      license correction above, and any upstream `file:line` drift. Per `CLAUDE.md`'s
      convention, a `file:line` mismatch is drift to re-verify, not evidence the underlying
      constraint changed — say which it was in each case.
    - **A `## Reproducing this` section**, listing the evidence files by relative path and the
      commands to re-run, so criterion 4's "a later session can re-read" is literally true.

    Do not summarise away a negative. `docs/phase2-backend-probe-evidence.md` is the precedent:
    a check that could not be performed is written up as fully as one that passed.
  </action>
  <verify>
    <automated>test -f docs/phase9-regenerator2000-probe-findings.md &amp;&amp; grep -q '^verdict: ' docs/phase9-regenerator2000-probe-findings.md &amp;&amp; grep -q '^verdict_rule_applied: ' docs/phase9-regenerator2000-probe-findings.md &amp;&amp; grep -cE '^  c(1|2a|2b|3_2|3_3|3_4)' docs/phase9-regenerator2000-probe-findings.md | awk '$1 &gt;= 7 {exit 0} {exit 1}' &amp;&amp; grep -q 'Accepted limits' docs/phase9-regenerator2000-probe-findings.md &amp;&amp; grep -q 'Corrections to prior documents' docs/phase9-regenerator2000-probe-findings.md &amp;&amp; grep -c 'evidence/criterion' docs/phase9-regenerator2000-probe-findings.md | awk '$1 &gt;= 5 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `docs/phase9-regenerator2000-probe-findings.md` exists with YAML frontmatter carrying `phase`, `requirement`, `probe_date`, `regenerator2000_version`, `verdict`, `verdict_rule_applied` and a `criteria:` map with all seven keys populated from their source outcome lines
    - One sentence under the frontmatter states that the frontmatter is a deliberate departure from the two existing probe documents, and why
    - The summary table has one row per criterion with its `R2000-16` sub-part, its outcome, and its evidence file path, every cell transcribed from an outcome line
    - A per-criterion section exists for each of (1)(2)(3)(4)(5), with criterion 2 split into 2a and 2b subsections and criterion 1(5) reporting **both** container numbers plus the no-baseline statement
    - An `## Accepted limits` section names, per failure and per `could-not-run`, what it breaks and which requirement, phase criterion or playbook is affected — or states explicitly that every criterion passed
    - A `## Corrections to prior documents` section records the `MIT OR Apache-2.0` license correction against `R2000-03`, plus every transcript-versus-research contradiction, distinguishing `file:line` drift from a changed constraint
    - A `## Reproducing this` section lists every evidence file by relative path and the commands to re-run
    - At least five distinct `evidence/criterion*` paths are cited in the document
    - `verdict:` and `verdict_rule_applied:` are still the literal `TBD` at the end of this task
  </acceptance_criteria>
  <done>One durable document records all five criteria with their real outcomes, their accepted limits and their reproduction steps, in the shape this repo already uses.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the decision rule and set the machine-readable verdict</name>
  <files>docs/phase9-regenerator2000-probe-findings.md</files>
  <read_first>
    - `docs/phase9-regenerator2000-probe-findings.md` — the seven `criteria:` values Task 1 populated
    - this plan's `<decision_rule>` block — the rule, verbatim; do not restate it from memory
    - `.planning/ROADMAP.md` § `### Phase 9` criterion 5 — the three routes in the ROADMAP's own words, which the document must quote rather than paraphrase
    - `.planning/ROADMAP.md` § `### Phase 10` and `### Phase 11` — the criteria the amendments attach to, so each amendment names a real target
  </read_first>
  <action>
    Evaluate the `<decision_rule>` block against the seven `criteria:` values, in order, first
    match wins. Set the frontmatter `verdict:` to exactly one of `proceed`, `degrade`,
    `reconsider`, and `verdict_rule_applied:` to exactly one of `R1`, `R2`, `R3`, `R4`, `R5`.

    Add a `## Verdict` section as the document's **first body section**, before the summary
    table, since it is what a later reader is looking for. It must contain:
    - The verdict, and the rule that fired, and the specific input value that made it fire.
    - The full decision rule reproduced verbatim from this plan, so the document is
      self-contained and a later reader can check the derivation without opening the plan.
    - The ROADMAP's own words for the route taken, quoted.
    - **Where Phase 10's planner looks**, stated as an unambiguous instruction:
      `docs/phase9-regenerator2000-probe-findings.md`, frontmatter key `verdict`, with the
      `criteria:` map giving the per-item detail. Add the sentence that no Phase 10 or Phase 11
      plan may be written before reading it — `R2000-16`'s own wording is "before any further
      plan is written".
    - On `degrade`, a `### Scope amendments` subsection listing each amendment against the
      specific target it changes: which Phase 10 or Phase 11 criterion, which requirement ID,
      and which playbook has to name the limitation at its point of use. Derive these from the
      `## Accepted limits` entries, one amendment per limit — do not merge them.
    - On `reconsider`, a `### What is in question` subsection stating plainly which part of the
      milestone thesis the failure undermines and what a decision would need to be made about,
      without pre-empting that decision. The planner's job here is to hand the developer a
      clean decision, not to take it.
    - On `proceed`, an explicit sentence that all five criteria passed and no scope amendment
      is implied — so a later reader does not have to infer the absence of amendments.

    Do not soften a `reconsider` into a `degrade`, and do not upgrade a `partial` to a `pass`.
    The rule is deterministic precisely so this cannot happen at write time.
  </action>
  <verify>
    <automated>grep -qE '^verdict: (proceed|degrade|reconsider)$' docs/phase9-regenerator2000-probe-findings.md &amp;&amp; grep -qE '^verdict_rule_applied: R[1-5]$' docs/phase9-regenerator2000-probe-findings.md &amp;&amp; grep -q '## Verdict' docs/phase9-regenerator2000-probe-findings.md &amp;&amp; grep -q 'frontmatter key' docs/phase9-regenerator2000-probe-findings.md &amp;&amp; ! grep -q '^verdict: TBD' docs/phase9-regenerator2000-probe-findings.md</automated>
  </verify>
  <acceptance_criteria>
    - `verdict:` is exactly one of `proceed`, `degrade`, `reconsider` and `verdict_rule_applied:` is exactly one of `R1`-`R5`
    - The verdict is consistent with the seven `criteria:` values under a first-match-wins evaluation of the decision rule — a reader can re-derive it mechanically from the document alone
    - `## Verdict` is the first body section and reproduces the full decision rule verbatim
    - The document states, in words a planner cannot misread, that Phase 10's gate is this file's frontmatter `verdict` key, and that no Phase 10/11 plan is written before reading it
    - On `degrade`, every `## Accepted limits` entry has a matching amendment naming a specific Phase 10/11 criterion, requirement ID or playbook — no merged or generic amendments
    - On `reconsider`, a `### What is in question` subsection frames the decision without taking it
    - On `proceed`, an explicit sentence records that no scope amendment is implied
  </acceptance_criteria>
  <done>The verdict is set as a machine-readable frontmatter key, derived mechanically from the stated rule, with its consequences named against specific downstream targets.</done>
</task>

<task type="auto">
  <name>Task 3: Apply every research correction and sign off the validation contract</name>
  <files>
    .planning/phases/09-the-assumption-probe-go-no-go/09-RESEARCH.md,
    .planning/phases/09-the-assumption-probe-go-no-go/09-VALIDATION.md
  </files>
  <read_first>
    - every `## RESEARCH CORRECTIONS` section across the six evidence files — the complete correction set, collected in one place for the first time
    - `.planning/phases/09-the-assumption-probe-go-no-go/09-RESEARCH.md` — the claims to correct, especially `## Assumptions Log` A1-A4, `## Open Questions` 1-3, and the `## Standard Stack` license and version claims
    - `.planning/phases/09-the-assumption-probe-go-no-go/09-VALIDATION.md` § `## Per-Task Verification Map` and § `## Validation Sign-Off` — the status cells and checkboxes to complete
    - `docs/phase9-regenerator2000-probe-findings.md` — the outcomes to reconcile against
    - `.planning/phases/09-the-assumption-probe-go-no-go/09-VALIDATION.md` § Evidence Integrity Rule 2 — the transcript outranks the research
  </read_first>
  <action>
    **`09-RESEARCH.md`.** Apply every correction the evidence files recorded. Do not rewrite the
    document; annotate it so a later reader sees both the original claim and what was observed.
    For each correction, edit the relevant line or table row and mark it, e.g.
    `**[CORRECTED 2026-xx-xx by Phase 9 probe: …observed…]**`, citing the evidence file. At
    minimum, resolve these four, which the probe was designed to settle:
    - `## Assumptions Log` A1 — whether `M-s` registered as Alt+S under the pty actually used
    - `## Assumptions Log` A2 — the Save-As dialog's real default filename, from
      `SAVE_AS_DEFAULT_FILENAME:`
    - `## Assumptions Log` A3 / `## Open Questions` 3 — the real `use_illegal_opcodes`
      disposition, from `ILLEGAL_OPCODE_MODE:`
    - `## Assumptions Log` A4 — the base-image tag that actually resolved
    Also close `## Open Questions` 1 and 2 with the observed answers from
    `SAVE_PROJECT_POST_BOOTSTRAP:` and `SAVE_AS_DEFAULT_FILENAME:`, and update the
    `## Metadata` confidence breakdown where an observation replaced an inference. Where an
    upstream `file:line` no longer matched, mark it as drift re-verified rather than as a
    changed constraint, per `CLAUDE.md`'s convention. If a criterion was `could-not-run`, mark
    the corresponding research claim `**[UNVERIFIED — probe could not run: …]**` rather than
    leaving it reading as confirmed.

    **`09-VALIDATION.md`.** Fill in the `## Per-Task Verification Map` status column with the
    real outcome symbol per row and the plan/task that produced it. Then complete the
    `## Validation Sign-Off` checklist honestly — a box that is not true stays unchecked with a
    one-line reason beside it, which is more useful than a full sheet of ticks. Set the
    frontmatter: `status: complete`, `wave_0_complete: true`, and `nyquist_compliant: true`
    only if every criterion has a real recorded outcome (a `could-not-run` with its blocker and
    unanswered set **counts** as a real recorded outcome — the standard is honesty, not
    universal success). If any criterion has no recorded outcome at all, leave
    `nyquist_compliant: false` and say which one is missing.

    Do **not** touch `.planning/STATE.md` or `.planning/ROADMAP.md` in this plan. Plan 09-08
    owns those two files, because a plan whose deliverable is their content has to run outside
    worktree isolation or the edits are silently stripped from the commit.
  </action>
  <verify>
    <automated>grep -q 'nyquist_compliant:' .planning/phases/09-the-assumption-probe-go-no-go/09-VALIDATION.md &amp;&amp; grep -qE '^(status|wave_0_complete):' .planning/phases/09-the-assumption-probe-go-no-go/09-VALIDATION.md &amp;&amp; grep -qE 'CORRECTED|UNVERIFIED' .planning/phases/09-the-assumption-probe-go-no-go/09-RESEARCH.md &amp;&amp; test -z "$(git diff --name-only -- .planning/STATE.md .planning/ROADMAP.md)"</automated>
  </verify>
  <acceptance_criteria>
    - `09-RESEARCH.md` carries an inline correction or `[UNVERIFIED — probe could not run: …]` marker for each of Assumptions Log A1, A2, A3 and A4, each citing the evidence file that settled or blocked it
    - `## Open Questions` 1 and 2 are closed with the observed answers, quoting the outcome lines
    - Any upstream `file:line` mismatch is marked as drift re-verified, not as a changed constraint
    - `09-VALIDATION.md`'s `## Per-Task Verification Map` status column is filled with real outcomes and the producing plan/task per row
    - `## Validation Sign-Off` boxes are checked only where true; every unchecked box has a one-line reason
    - `nyquist_compliant: true` is set only if every criterion has a recorded outcome, counting `could-not-run` with its blocker as a recorded outcome; otherwise it stays `false` and names the gap
    - `.planning/STATE.md` and `.planning/ROADMAP.md` are untouched by this plan
    - Nothing under `.claude/mcp/vice/` was modified
  </acceptance_criteria>
  <done>Every claim the probe contradicted is corrected at its source, and the validation contract is signed off honestly rather than universally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence transcripts → normative document | Raw observations cross into a document later phases treat as normative; a transcription error here propagates as fact |
| findings frontmatter → Phase 10's planner | A machine-readable verdict crosses a phase boundary and gates real scope decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07-A | Repudiation | A verdict chosen by judgement rather than derived, or a `reconsider` softened to a `degrade` | mitigate | The decision rule is written into this plan before execution, reproduced verbatim into the document, and evaluated first-match-wins from seven named outcome lines read out of the evidence files. Acceptance criteria require the verdict be mechanically re-derivable from the document alone |
| T-09-07-B | Tampering | Summarising a negative away, or filling a table cell from expectation | mitigate | Every cell must be transcribed from an outcome line; `docs/phase2-backend-probe-evidence.md` is cited as the precedent for writing up an unperformable check as fully as a passing one. The `## Accepted limits` section is a required heading even when empty |
| T-09-07-C | Spoofing | A stale `09-RESEARCH.md` claim continuing to read as confirmed after the probe contradicted or could not test it | mitigate | Task 3 requires an inline `[CORRECTED …]` or `[UNVERIFIED — probe could not run: …]` marker on each of A1-A4 and Open Questions 1-3, citing the evidence file |
| T-09-07-D | Information Disclosure | Host paths and tool inventory published in a repository document | accept | Same disposition as `docs/phase1-probe-results.md`, which already records host `uname` and binary paths. No credentials are handled |
| T-09-07-E | Elevation of Privilege | — | N/A | This plan writes documents only; it runs no third-party binary, binds no port, and launches no emulator |
</threat_model>

<verification>
- `docs/phase9-regenerator2000-probe-findings.md` exists with a `verdict:` of `proceed`, `degrade` or `reconsider` and a `verdict_rule_applied:` of `R1`-`R5`
- All five criteria have a section and a summary-table row, each traceable to an evidence file
- `## Accepted limits` names what each failure breaks, against a specific requirement, criterion or playbook
- `09-RESEARCH.md` corrections applied inline with evidence citations
- `09-VALIDATION.md` signed off, frontmatter updated
- `.planning/STATE.md` and `.planning/ROADMAP.md` untouched
- `git diff --stat` shows no change under `.claude/mcp/vice/`
</verification>

<success_criteria>
Criteria 4 and 5 are satisfied: one durable document a later session can re-read holds every
answer including every failure as an accepted limit naming what it breaks, and a
machine-readable verdict sits at a stated location, derived from a rule written down before
the run.
</success_criteria>

<output>
Create `.planning/phases/09-the-assumption-probe-go-no-go/09-07-SUMMARY.md` when done.
State the verdict and the rule that fired in the summary's first line.
</output>
