---
phase: 43-the-runtime-evidence-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/vice/text-protocol.ts
  - src/mcp/vice/text-protocol.test.ts
  - .planning/phases/43-the-runtime-evidence-layer/evidence/evid06-instrumentation-ab.mjs
  - docs/phase43-instrumentation-perturbation-ab.md
autonomous: true
requirements: [EVID-06]
user_setup: []

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`memmapzap` is dialable through the shipped `TextMonitorClient.command()` against genuine stock VICE at /usr/bin/x64sc, and a `memmapshow` read back afterwards parses through the shipped `parseAccessMap()` with `ok: true`."
    - "The EVID-06 pass/fail rule is written in this plan and in the A/B script's own header BEFORE any measurement is taken, and the recorded verdict is derived from `compareCaptures()`'s own `verdict` field rather than judged."
    - "The A/B runs a control-of-the-control (condition A against a second independent condition A run at the same depth) and refuses the verdict as `not-exercised` unless that control compares `equivalent` first."
    - "`docs/phase43-instrumentation-perturbation-ab.md` records the verdict as exactly one of three tokens -- `no-perturbation`, `perturbation`, `not-exercised` -- with the two depths, the empty allow-list, the resolved absolute binary path and the release sha256 beside it."
    - "A `not-exercised` outcome is recorded as a shortfall and selects the `perturbation` labelling branch as the conservative default; it is never recorded as a pass."
    - "The A/B changes exactly two things between its conditions -- one `memmapzap` dial and one `memmapshow` dial -- and never a second binary, a second seed, or a second release."
  prohibitions:
    - "An instrumented run's evidence must never be presented as interchangeable with a frame-exact run's without the label this measurement's verdict requires."
  artifacts:
    - src/mcp/vice/text-protocol.ts
    - .planning/phases/43-the-runtime-evidence-layer/evidence/evid06-instrumentation-ab.mjs
    - docs/phase43-instrumentation-perturbation-ab.md
  key_links:
    - "TEXT_COMMAND_ALLOWLIST to TextMonitorClient.command(): an unlisted verb is refused before a byte reaches the socket, so the allowlist entry is what makes the shipped dial reachable at all."
    - "compareCaptures() unmodified to the recorded verdict: the A/B derives its answer from the shipped predicate, never from a second comparison written in the script."
    - "This plan's verdict to plan 43-02's DDL column set: the schema is written after the measurement, never retrofitted onto a shipped key."
---

<objective>
Answer EVID-06 before the evidence table exists: does recording runtime
evidence during v0.8.0's S3 anchor-counted AUTOSTART sequence perturb the
frame-exact, byte-identical capture that sequence produces through anchor
hit 50?

Purpose: this phase keys its rows by a reproducibility that instrumenting the
run may itself destroy. No documentation source answers it -- only an A/B at
the existing anchor sequence does. Taking the measurement first is what keeps
the schema consequence from becoming a retrofit onto a shipped key.

Output: `memmapzap` reachable through the shipped text client; a one-off A/B
evidence script; and a findings document carrying the verdict, its two depths,
and the decision the verdict selects.

## PASS/FAIL RULE -- FIXED 2026-09-10, BEFORE ANY MEASUREMENT IS TAKEN

Fixed inputs, identical in both conditions: binary `/usr/bin/x64sc` (genuine
unpatched stock, VICE 3.9 -- the fork at `/usr/local/bin/x64sc` shadows a bare
`x64sc` and is never launched here); release
`.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/corpus/danish.d64`,
sha256 `1a9d294e07f9593ba59d878423d157bacfe6c6902d3a52ef6ac96512a15fb6c5`;
argv from `buildProbeArgs()` including `STOCK_DETERMINISM_FLAGS` unmodified,
so the same `-seed`; zero pre-protocol jitter in every run; both monitor
sockets connected in both conditions.

Two conditions, at each of two anchor hit depths N in {10, 50}:

* **Condition A, the control.** The S3 sequence -- connect, arm a
  non-temporary Exec checkpoint at `$ea31` with `stop: true` while halted,
  `AUTOSTART` with `runAfter: true` and `fileIndex: 0` on the release, count
  `CHECKPOINT_INFO` hits to N with exactly one resume per observed hit,
  `REGISTERS_GET`, `DUMP`. No `RESET` anywhere. The text socket is connected
  and its banner drained, and **nothing is dialed over it**.
* **Condition B, instrumented.** Byte-identical to A except two dials over
  the text channel: `memmapzap` during the halted pre-`AUTOSTART` window, and
  `memmapshow` after the anchor stop at hit N and before `DUMP`.

Both captures are sliced to flat 64K through the shipped `vsf-slice.mjs` CLI,
port-normalised through `normalisePorts()` from each snapshot's own
`dirRead`/`dataRead`, and compared with the shipped, unmodified
`compareCaptures(a, b, [])` -- an **EMPTY** allow-list, because plan 33-03
measured a pre-load frame-anchored stop as comparing equivalent with an empty
allow-list at hits 1, 10 and 50.

* **`no-perturbation`** iff, at BOTH depths, `compareCaptures().verdict` is
  `"equivalent"`.
* **`perturbation`** iff at EITHER depth `compareCaptures().verdict` is
  `"not-equivalent"`.
* **`not-exercised`** iff a required input is absent, either condition fails
  to reach the anchor stop at either depth, or the control-of-the-control
  does not itself compare `equivalent`. `not-exercised` is NOT a pass: it
  forbids any claim of `no-perturbation` and selects the `perturbation`
  labelling branch as the conservative default.

The control-of-the-control is mandatory and runs first. Without it a
`not-equivalent` A-versus-B result cannot be distinguished from baseline
nondeterminism, and this repository's discipline is that a red with no green
beside it is not a proof.

## FLAGGED ASSUMPTION -- the A/B does not isolate a toggle, and cannot

EVID-06's wording is "turning the instrumentation on". VICE has no such
toggle: `monitor_memmap_store()` records continuously and unconditionally once
the binary carries `FEATURE_CPUMEMHISTORY` -- the same build flag
`memmapshow` and `chis` already require, and which the connected binary must
already have for this phase to work at all -- and `memmapzap` is a full clear,
not a pause. The recording therefore cannot be switched off on one arm.

What this A/B measures is the observable difference this project can actually
cause: the two extra text-monitor dials the evidence layer will issue on every
instrumented run. That is narrower than the requirement's literal wording and
is stated here rather than quietly substituted. Launching two
differently-configured binaries to reach the literal reading is explicitly
rejected -- the binary's own identity is part of the run-identity composite,
so that experiment moves two variables at once and answers a different
question.

Consequence for the verdict's authority, stated up front: a `no-perturbation`
verdict licenses only the claim that this project's own instrumentation dials
do not perturb frame-exactness at N of 50 or below. It does not license a
claim about `FEATURE_CPUMEMHISTORY` being absent, which no run in this phase
can produce.

## FLAGGED ASSUMPTIONS from the edge-coverage probe (unclassified rows)

The deterministic edge probe returned `unclassified / unresolved` for
EVID-01, EVID-04, EVID-05 and EVID-06 and they are surfaced here rather than
dropped. EVID-06's is the one this plan owns:

* **EVID-06 (unclassified, unresolved):** the requirement names a variable
  ("turning the instrumentation on") that the platform does not expose as a
  variable. The substituted measurement is stated above with its narrowed
  authority. Reviewed manually; not auto-resolved.

The other three are carried by the plans that own them: EVID-01 in 43-02,
EVID-04 in 43-04, EVID-05 in 43-03.
</objective>

<assumption_delta_decision>
**The noun under question.** EVID-01 forbids minting a second notion of "the
same run" -- run identity reuses v0.8.0's `(image sha256, argv digest, seed)`
composite. EVID-06's verdict can force exactly the pluralization that
forbids: if instrumentation perturbs frame-exactness, instrumented and
frame-exact runs must be separated and labelled as such in the schema and in
every rendering, never quietly conflated. That is a second class of run
sitting beside the first, and whether it exists is decided by the measurement
this plan takes.

The **decision rule** is recorded here rather than a pre-committed outcome,
because the decision is contingent on a measurement not yet taken:

| Verdict | Primary noun | Decision | What the schema carries |
|---|---|---|---|
| `no-perturbation` | the run, identified by `(image sha256, argv digest, seed)`, unchanged | `no-change` | The bare triple. No `run_class` column exists. The old identity keeps its monopoly because nothing pluralized it. |
| `perturbation` | the observed run, identified by `(image sha256, argv digest, seed, run_class)` where `run_class` is one of `frame-exact` or `instrumented` | `promote` | The generalized four-part composite is primary; the v0.8.0 triple is demoted to a projection of it, never kept alongside as a second scheme. Every read verb and every rendering surface carries `run_class`. |
| `not-exercised` | as `perturbation` | `promote` | The conservative branch. A shortfall never buys the narrower schema. |

**Why `promote` and never `add-alongside`.** Adding an `instrumented` flag
beside a still-primary bare triple leaves two answers to "is this the same
run", which is precisely the failure EVID-01 exists to prevent. Promoting the
four-part composite to primary leaves exactly one answer, of which the triple
is a projection.

**Accepted debt if the decision is `no-change`:** the schema carries no run
class, so a later phase that discovers a different perturbation source -- a
different VICE version, a different dial -- faces the one-way schema bump
this phase's own EVID-02 checkpoint weighs. That is the cost of not inventing
a column for a phenomenon this measurement did not observe, and it is named
rather than hidden.

**Companion invariant, suggested and not required by this plan:** plan 43-06's
read verbs should carry a contract test asserting that every evidence row
round-trips through the one run-identity path for every supported run class,
so a future phase reintroducing the singular assumption goes red immediately.
</assumption_delta_decision>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-the-runtime-evidence-layer/43-RESEARCH.md
@.planning/phases/43-the-runtime-evidence-layer/43-PATTERNS.md
@.planning/phases/43-the-runtime-evidence-layer/43-VALIDATION.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Dial memmapzap end to end against genuine stock VICE through the shipped client</name>
  <precondition>`/usr/bin/x64sc` exists and `/usr/bin/x64sc --version` reports VICE 3.9; `systemctl --user is-active vice-broker` reports `inactive`; `pgrep -x x64sc` returns nothing.</precondition>
  <reversibility rating="costly">A shipped allowlist entry widens the closed set of verbs this project may send over the text channel; removing it later means removing a tool surface a skill may already cite.</reversibility>
  <files>src/mcp/vice/text-protocol.ts, src/mcp/vice/text-protocol.test.ts, .planning/phases/43-the-runtime-evidence-layer/evidence/evid06-instrumentation-ab.mjs</files>
  <read_first>
    - src/mcp/vice/text-protocol.ts (lines 74-205 for `PROMPT_RE`, `TEXT_COMMAND_ALLOWLIST`, `TEXT_COMMAND_PARAM_SPECS`, `buildTextCommand`, `isAllowlistedTextCommand`; lines 460-600 for `TextMonitorClient`'s `connect` and `command`)
    - src/mcp/vice/text-protocol.test.ts (lines 108-133, the allowlist membership test and its file-touching-verb regex)
    - src/mcp/vice/textmon-memmap.ts (lines 32-96: the no-data-from-absence header clause, `AccessFlags`, `AccessMapEntry`, `AccessMap`, `parseAccessMap`)
    - .planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/probe-harness.mjs (`VICE_STOCK`, `preflight`, `allocPorts`, `buildProbeArgs`, `spawnVice`, `connectWithRetry`, `reapAll`, `PROBE_DIR`)
  </read_first>
  <action>
Add the eleventh entry `memmapzap` to `TEXT_COMMAND_ALLOWLIST` in
`src/mcp/vice/text-protocol.ts`. Extend that constant's own doc comment with a
paragraph in the register the `prof on` / `prof off` paragraph already uses:
a conscious, measured widening; what it is for (clearing VICE's accumulated
memory-access map so a measurement bracket starts from nothing); and why the
sibling verb `memmapsave` is deliberately NOT added -- it writes a file, and
this allowlist's own membership test refuses any entry matching the pattern
that spells the words load or save. Do NOT add an entry to
`TEXT_COMMAND_PARAM_SPECS`: `memmapzap` takes no parameter and keeps dialing
its bare frozen literal.

Update the membership test in `src/mcp/vice/text-protocol.test.ts`: the
`expected` array gains `memmapzap`, and the test title's word "ten" becomes
"eleven". Add a comment above the `expected` array recording that the eleventh
entry landed in plan 43-01 for EVID-06's measurement and EVID-05's bracket
reset, and that the file-writing sibling was rejected.

Then write the tracer as the opening block of a new evidence script at
`.planning/phases/43-the-runtime-evidence-layer/evidence/evid06-instrumentation-ab.mjs`,
runnable on its own behind a `--tracer` argv flag so this task has a real
end-to-end verify before the full A/B exists. That block must:

  * import `probe-harness.mjs` from phase 39's evidence directory by relative
    path and use its `preflight`, `allocPorts`, `buildProbeArgs`, `spawnVice`,
    `connectWithRetry` and `reapAll` -- never re-derive binary resolution,
    port allocation, spawn/reap or the refusal check;
  * dynamically import `text-protocol.ts` from `src/mcp/vice/` and use the
    SHIPPED `TextMonitorClient` (`connect`, then `command`) -- never
    phase 39's raw `textmon-probe-client.mjs`, because proving the shipped
    allowlisted path is the point of this slice;
  * dial `memmapzap`, then dial `memmapshow`, then parse the second reply
    through the shipped `parseAccessMap` from `textmon-memmap.ts`;
  * print on stdout the four lines `TRACER_ZAP_OK true|false`,
    `TRACER_SHOW_PARSED true|false`, `TRACER_ENTRIES n` where n is
    `parsed.value.entries.length`, and `TRACER_EXECUTE_ENTRIES n` counting
    entries whose `ram.execute` or `rom.execute` or `io.execute` is true;
  * exit non-zero on any refusal, and reap the emulator in a `finally`.

Every artifact this script produces goes under
`$HOME/.cache/c64-re-tools/phase43/43-01/`. Never the system temp directory
(a 16 GB RAM tmpfs whose aging is disabled on this host) and never inside the
checkout.

Record the observed `TRACER_ENTRIES` and `TRACER_EXECUTE_ENTRIES` numbers in
the plan SUMMARY as measured values. Do not assert a specific count anywhere;
assert the relation the acceptance criteria name.
  </action>
  <acceptance_criteria>
    - `grep -c '"memmapzap"' src/mcp/vice/text-protocol.ts` is at least 1, and `node -e` printing `TEXT_COMMAND_ALLOWLIST.length` after a dynamic import reports 11
    - `grep -n 'memmapsave' src/mcp/vice/text-protocol.ts` finds the name only inside a comment explaining its rejection, never inside the allowlist array
    - `node --test src/mcp/vice/text-protocol.test.ts` exits 0, with the membership test title containing the word "eleven"
    - The tracer run prints `TRACER_ZAP_OK true` and `TRACER_SHOW_PARSED true`
    - The tracer run prints a `TRACER_ENTRIES` value strictly greater than 0 (a sparse map is legal, an unparseable reply is not)
    - The script exits 0 and `pgrep -x x64sc` returns nothing afterwards
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test text-protocol.test.ts</automated>
    <fails_when>non-zero exit, or the allowlist membership test reports a deepEqual mismatch naming memmapzap</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck</automated>
    <fails_when>non-zero exit, or any line matching "error TS" in the output</fails_when>
    <automated>mkdir -p "$HOME/.cache/c64-re-tools/phase43/43-01" && cd /home/henrik/dev/henrik/git/c64-re-tools && node .planning/phases/43-the-runtime-evidence-layer/evidence/evid06-instrumentation-ab.mjs --tracer > "$HOME/.cache/c64-re-tools/phase43/43-01/tracer.log" 2>&1; echo "exit=$?"; grep -aE '^TRACER_' "$HOME/.cache/c64-re-tools/phase43/43-01/tracer.log"</automated>
    <fails_when>the printed exit is not `exit=0`, or `TRACER_ZAP_OK true` is absent, or `TRACER_SHOW_PARSED true` is absent, or `TRACER_ENTRIES 0` appears</fails_when>
  </verify>
  <done>`memmapzap` is on the shipped allowlist, its membership test names eleven verbs, and one live run against genuine stock VICE 3.9 dials it through the shipped client and parses a `memmapshow` reply back with a non-zero entry count.</done>
</task>

<task type="auto">
  <name>Task 2: Run the A/B at two depths with its control-of-the-control</name>
  <precondition>The release `.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/corpus/danish.d64` exists and digests to `1a9d294e07f9593ba59d878423d157bacfe6c6902d3a52ef6ac96512a15fb6c5`; `systemctl --user is-active vice-broker` reports `inactive`; `pgrep -x x64sc` returns nothing.</precondition>
  <files>.planning/phases/43-the-runtime-evidence-layer/evidence/evid06-instrumentation-ab.mjs</files>
  <read_first>
    - .planning/phases/43-the-runtime-evidence-layer/evidence/evid06-instrumentation-ab.mjs (the tracer block from Task 1)
    - .planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/evidence/capture-pair.mjs (lines 30-90 for the header's reuse rules and the reason `snapshotPathFor()` is avoided; lines 525-700 for the S3 sequence, `RELEASE_SHA256_EXPECTED`, `DUMP` and the slice/normalise/compare chain; lines 755-790 for the allow-list handling)
    - .planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/evidence/frame-anchor-probe.mjs (lines 1-70 for the header register; lines 120-280 for `preflight`, `buildArgv`, `connectWithRetry`, `pingReady`, `registerIds`, `readRegisters`, `armAnchor`, `anchorHitCount`, `sliceThroughShippedCli`)
    - src/mcp/vice/capture-predicate.ts (lines 185-240 for `CaptureComparison`; lines 380-545 for `normalisePorts`, `compareCaptures`, `formatComparison`; lines 565-597 for `argvDigest`)
    - .planning/phases/39-the-dual-channel-coexistence-gate-go-degrade-no-go/evidence/probe-harness.mjs (`buildProbeArgs`, `armStoppingExec`, `checkpointHitCount`, `resumeExecution`, `awaitFirstCheckpointHit`)
  </read_first>
  <action>
Complete `evid06-instrumentation-ab.mjs` into the full A/B. Write its header
first, in `frame-anchor-probe.mjs`'s register, stating in prose before any
code: the pass/fail rule verbatim from this plan's `<objective>`; the two
conditions; the empty allow-list and why (plan 33-03's measured
equivalence at hits 1, 10 and 50); the mandatory control-of-the-control and
why a red without a green beside it proves nothing; and the flagged
assumption that no instrumentation toggle exists in VICE.

The script takes four runs per depth and runs depths N of 10 and 50:
`A1`, `A2` (the control-of-the-control pair), `B1`, and re-uses `A1` as
B's counterpart. Every run is a fresh `spawnVice()` with a fresh
`XDG_CONFIG_HOME` under `$HOME/.cache/c64-re-tools/phase43/43-01/xdg/<label>`,
removed and recreated per run, with `DISPLAY` and `WAYLAND_DISPLAY` deleted
from the child environment.

Per run, in this exact order, with no `RESET` anywhere:
connect the binary monitor with retry; `pingReady`; resolve register ids and
refuse by name if `PC`, `LIN` or `CYC` is absent; connect the text monitor
with retry and drain its banner; for condition B only, dial `memmapzap`;
arm a non-temporary Exec checkpoint at `$ea31` with `stop: true` while halted;
send `AUTOSTART` with `runAfter: true`, `fileIndex: 0` and the release
filename; assert the checkpoint survived AUTOSTART's power cycle and refuse by
name if it did not; count hits to N polling on the checkpoint's own
`hitCount` and never on paused state, sending exactly one resume per observed
hit; `REGISTERS_GET`; for condition B only, dial `memmapshow` and parse it
through `parseAccessMap`, refusing on `ok: false`; `DUMP` to
`<cache>/<label>.vsf` with `saveRoms: false` and `saveDisks: false`; slice
through the shipped `vsf-slice.mjs` CLI; port-normalise via `normalisePorts`
from that snapshot's own `dirRead` and `dataRead`.

Then, per depth, call the shipped `compareCaptures(a, b, [])` twice -- once
for `A1` against `A2` and once for `A1` against `B1` -- and print, per
comparison, `AB_CONTROL_VERDICT`, `AB_TREATMENT_VERDICT`,
`AB_DIFFERING_COUNT`, `AB_ALLOWED_COUNT` and `formatComparison(cmp, 20)`.

Derive the verdict in code from the rule, never by hand: print one line
`DERIVED_EVID06_VERDICT no-perturbation|perturbation|not-exercised` and, when
the verdict is not `no-perturbation`, a second line
`DERIVED_EVID06_CAUSE <the observed reason>`. Also print
`DERIVED_ASSUMPTION_DELTA_DECISION no-change|promote` from the same rule.

Emit the whole transcript to stdout and also append it to
`$HOME/.cache/c64-re-tools/phase43/43-01/ab-transcript.log`. Delete the four
`.vsf` files and the sliced `.bin` files in a `finally` once the comparison
has been taken, keeping only the `.norm.bin` sha256 values in the transcript;
this host's cache directory is not swept automatically and each snapshot is
large.

Reap in a `finally` and sweep any surviving `x64sc` afterwards, exactly as
the two prior evidence scripts do.
  </action>
  <acceptance_criteria>
    - The script's header text contains the three verdict tokens `no-perturbation`, `perturbation` and `not-exercised` above the first line of executable code
    - `grep -c 'compareCaptures' <script>` is at least 1 and `grep -c 'RESET' <script>` counts no occurrence outside a comment (a census filtered with `grep -v` on comment lines)
    - The run prints exactly one `DERIVED_EVID06_VERDICT` line whose value is one of the three tokens
    - The run prints exactly one `DERIVED_ASSUMPTION_DELTA_DECISION` line whose value is `no-change` or `promote`
    - `AB_CONTROL_VERDICT equivalent` appears for both depths, or the derived verdict is `not-exercised` naming the control as the cause
    - `pgrep -x x64sc` returns nothing after the run
    - No `.vsf` file remains under `$HOME/.cache/c64-re-tools/phase43/43-01/`
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node .planning/phases/43-the-runtime-evidence-layer/evidence/evid06-instrumentation-ab.mjs > "$HOME/.cache/c64-re-tools/phase43/43-01/ab-run.log" 2>&1; echo "exit=$?"; grep -aE '^(AB_CONTROL_VERDICT|AB_TREATMENT_VERDICT|AB_DIFFERING_COUNT|DERIVED_EVID06_VERDICT|DERIVED_EVID06_CAUSE|DERIVED_ASSUMPTION_DELTA_DECISION)' "$HOME/.cache/c64-re-tools/phase43/43-01/ab-run.log"</automated>
    <fails_when>the printed exit is not `exit=0`, or no `DERIVED_EVID06_VERDICT` line appears, or its value is not one of `no-perturbation` / `perturbation` / `not-exercised`, or more than one such line appears</fails_when>
    <automated>pgrep -x x64sc; echo "pgrep_exit=$?"; find "$HOME/.cache/c64-re-tools/phase43/43-01" -name '*.vsf' -print</automated>
    <fails_when>`pgrep_exit=0` (a surviving emulator), or any `.vsf` path is printed</fails_when>
  </verify>
  <done>The A/B has been run at both depths with its control-of-the-control passing first, and one derived verdict token plus its assumption-delta decision is on the transcript.</done>
</task>

<task type="auto">
  <name>Task 3: Record the verdict and the decision it selects in the findings document</name>
  <files>docs/phase43-instrumentation-perturbation-ab.md, .planning/phases/43-the-runtime-evidence-layer/43-RESEARCH.md</files>
  <read_first>
    - $HOME/.cache/c64-re-tools/phase43/43-01/ab-run.log (the transcript Task 2 produced)
    - docs/phase41-text-channel-live-evidence.md (the register and heading shape for a live-evidence findings document in this repository)
    - docs/phase39-dual-channel-coexistence-gate-findings.md (how a recorded verdict, its rule and its accepted limits are laid out)
    - .planning/phases/43-the-runtime-evidence-layer/43-01-PLAN.md (this plan's PASS/FAIL RULE and assumption_delta_decision blocks, quoted verbatim into the document)
  </read_first>
  <action>
Create `docs/phase43-instrumentation-perturbation-ab.md` with these sections,
in this order:

  1. `## The rule, fixed before the measurement` -- the PASS/FAIL RULE from
     this plan, quoted, with the date it was fixed.
  2. `## What was actually run` -- the resolved absolute binary path and its
     `--version` output; the release path and its sha256; the argv including
     the determinism block; the two depths; the four run labels; the empty
     allow-list and the citation for why an empty one is correct at N of 50
     or below.
  3. `## The control-of-the-control` -- the `AB_CONTROL_VERDICT` result at
     each depth, and the statement that a treatment result would have been
     uninterpretable without it.
  4. `## VERDICT` -- exactly one of `no-perturbation`, `perturbation`,
     `not-exercised`, with the `AB_DIFFERING_COUNT` figures at both depths
     beside it and, when not `no-perturbation`, the recorded cause.
  5. `## What this verdict does and does not license` -- the flagged
     assumption from this plan, restated: no instrumentation toggle exists,
     so the measured variable is this project's two dials and not the
     recording itself; the verdict says nothing about a build without
     `FEATURE_CPUMEMHISTORY`.
  6. `## The decision this verdict selects` -- the assumption-delta table
     from this plan with the selected row marked, the resulting run-identity
     column set spelled out as a concrete column list, and the sentence that
     plan 43-02 writes the DDL from this row and from nowhere else.

If the verdict is `not-exercised`, section 4 states it as a shortfall with
what was searched and at what depth -- never a clean bill of health -- and
section 6 marks the `promote` row as selected by the conservative default.

Add a one-line pointer to this document from
`.planning/phases/43-the-runtime-evidence-layer/43-RESEARCH.md`'s
`## Open Questions` item 2, recording that the hit-count question is now
answered by measurement. Do not restate the verdict there.

Do not write the string `Phase 43` inside any string or template literal in
any file under `src/mcp/vice/` -- the FLOW-02 guard in
`docs-dangling-refs.test.ts` refuses a shipped literal naming a phase number.
Comments may narrate a plan id.
  </action>
  <acceptance_criteria>
    - `docs/phase43-instrumentation-perturbation-ab.md` exists and contains all six `## ` headings named in the action
    - `grep -acE '^## VERDICT' docs/phase43-instrumentation-perturbation-ab.md` is 1
    - The document contains exactly one of the three verdict tokens under the VERDICT heading, and that token equals the `DERIVED_EVID06_VERDICT` value in the transcript
    - The document names `/usr/bin/x64sc` and the release sha256 `1a9d294e07f9593ba59d878423d157bacfe6c6902d3a52ef6ac96512a15fb6c5` verbatim
    - The document's decision section marks exactly one row of the assumption-delta table as selected
    - `node --test src/mcp/vice/docs-dangling-refs.test.ts src/mcp/vice/docs-linerefs.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && test -f docs/phase43-instrumentation-perturbation-ab.md && grep -acE '^## VERDICT' docs/phase43-instrumentation-perturbation-ab.md && grep -ac '1a9d294e07f9593ba59d878423d157bacfe6c6902d3a52ef6ac96512a15fb6c5' docs/phase43-instrumentation-perturbation-ab.md</automated>
    <fails_when>non-zero exit, or the first grep count is not 1, or the sha256 grep count is 0</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-dangling-refs.test.ts docs-linerefs.test.ts</automated>
    <fails_when>non-zero exit, or a FLOW-02 violation naming a phase number inside a shipped string literal</fails_when>
  </verify>
  <done>The verdict, its rule, its two depths, its accepted narrowing and the schema decision it selects are recorded in one findings document, and the repo's documentation guards stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP caller to text-monitor socket | A caller-chosen command string would reach VICE's own monitor parser, where `load`/`save` verbs touch the filesystem with the emulator's privileges. |
| VICE text-monitor reply to `parseAccessMap()` | Untrusted, version-drifting text crosses into a parser whose output becomes durable evidence in later plans. |
| Evidence script to host process table | The A/B spawns a real emulator and must not orphan it, and must not run beside a broker-managed instance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-01 | Tampering | `TEXT_COMMAND_ALLOWLIST` widening in `text-protocol.ts` | high | mitigate | Add exactly the frozen literal `memmapzap`, no `TEXT_COMMAND_PARAM_SPECS` entry, no free-text slot; the existing membership test's load/save regex keeps the file-writing sibling `memmapsave` out, and Task 1's acceptance criteria assert the array length is 11 and that `memmapsave` appears only in a comment. |
| T-43-02 | Tampering | `parseAccessMap()` consumption in the A/B script | high | mitigate | The script checks the discriminated `ok` field and refuses by name on `ok: false`; it never absorbs a partial or best-effort map. |
| T-43-03 | Denial of service | orphaned `x64sc` processes from the A/B | medium | mitigate | `preflight()` throws rather than warns when the broker unit is active or another `x64sc` is alive; `reapAll()` runs on `exit`, `SIGINT` and `SIGTERM`; Task 2's verify asserts `pgrep -x x64sc` is empty afterwards. |
| T-43-04 | Information disclosure | snapshot bytes entering the checkout | medium | mitigate | Every `.vsf`, `.bin` and log lands under `$HOME/.cache/c64-re-tools/phase43/43-01/`, outside the checkout and never in the system temp tmpfs, and the `.vsf` files are deleted once the comparison is taken. |
| T-43-05 | Repudiation | a verdict chosen after seeing the numbers | high | mitigate | The pass/fail rule is fixed in this plan and in the script header before any run; the verdict line is computed in code from `compareCaptures().verdict` and printed as `DERIVED_EVID06_VERDICT`; the mandatory control-of-the-control gates it. |
| T-43-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no external package. `43-RESEARCH.md` records its Package Legitimacy Audit as not applicable, so there is no install task to gate and no `[ASSUMED]`/`[SUS]` row to verify. |
</threat_model>

<verification>
1. `cd src/mcp/vice && node --test text-protocol.test.ts` exits 0 with the membership test naming eleven verbs.
2. `cd src/mcp/vice && npm run typecheck` exits 0.
3. The tracer run prints `TRACER_ZAP_OK true`, `TRACER_SHOW_PARSED true` and a non-zero `TRACER_ENTRIES`.
4. The A/B run prints exactly one `DERIVED_EVID06_VERDICT` line and one `DERIVED_ASSUMPTION_DELTA_DECISION` line.
5. `docs/phase43-instrumentation-perturbation-ab.md` carries all six headings, the verdict token, and the selected assumption-delta row.
6. `cd src/mcp/vice && node --test docs-dangling-refs.test.ts docs-linerefs.test.ts` exits 0.
7. `pgrep -x x64sc` is empty and no `.vsf` remains under the plan's cache directory.
</verification>

<success_criteria>
- EVID-06 is answered by a measurement taken against a rule fixed before it, with its control-of-the-control passing first.
- The verdict is recorded as one of three tokens, and `not-exercised` is recorded as a shortfall rather than a pass.
- The schema consequence is written down as a selected decision row that plan 43-02 reads, so no later plan retrofits a column onto a shipped key.
- `memmapzap` is dialable through the shipped text client, proven live against genuine stock VICE 3.9.
</success_criteria>

## Artifacts this phase produces

New symbols and paths introduced by THIS plan (excluded from drift verification because they do not exist yet):

- `src/mcp/vice/text-protocol.ts`: new `TEXT_COMMAND_ALLOWLIST` member `"memmapzap"` (eleventh entry)
- `.planning/phases/43-the-runtime-evidence-layer/evidence/evid06-instrumentation-ab.mjs` (new file)
- `docs/phase43-instrumentation-perturbation-ab.md` (new file)
- Transcript tokens emitted by the A/B script: `TRACER_ZAP_OK`, `TRACER_SHOW_PARSED`, `TRACER_ENTRIES`, `TRACER_EXECUTE_ENTRIES`, `AB_CONTROL_VERDICT`, `AB_TREATMENT_VERDICT`, `AB_DIFFERING_COUNT`, `AB_ALLOWED_COUNT`, `DERIVED_EVID06_VERDICT`, `DERIVED_EVID06_CAUSE`, `DERIVED_ASSUMPTION_DELTA_DECISION`
- Verdict vocabulary: `no-perturbation`, `perturbation`, `not-exercised`
- Run-class vocabulary reserved by the decision rule (materialised only on the `promote` branch, in plan 43-02): `frame-exact`, `instrumented`

Symbols consumed unchanged, not produced: `TextMonitorClient`, `parseAccessMap`, `compareCaptures`, `normalisePorts`, `argvDigest`, `STOCK_DETERMINISM_FLAGS`, `buildProbeArgs`, `preflight`, `spawnVice`, `reapAll`.

<output>
Create `.planning/phases/43-the-runtime-evidence-layer/43-01-SUMMARY.md` when done.
</output>