---
phase: 44-proof-04-the-independent-external-check
plan: 02
type: execute
wave: 2
depends_on: [44-01]
files_modified:
  - .planning/phases/44-proof-04-the-independent-external-check/evidence/proof04-run-a-hit50.md
  - .planning/phases/44-proof-04-the-independent-external-check/evidence/proof04-run-b-narrowed.md
autonomous: true
requirements: [PROOF-04]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Run A is taken at anchor hit target 50 -- inside the region `EVID-06` licensed -- and its transcript records `ORACLE_DEPTH_REACHED` with `ORACLE_DEPTH_LABEL frame-exact-region`, together with the `PROOF04_*` outcome-line block the committed rule names."
    - "Run B is taken at a deeper stated anchor hit target and its transcript records `ORACLE_DEPTH_LABEL narrowed` together with the `EVID-06` licensing boundary and the hit-50 / hit-75 frame-exactness limit stated beside itself."
    - "Neither run replaces the other: both transcripts are committed, both carry their own verdict derived by the committed rule, and neither is presented as superseding the other's depth claim."
    - "Every run attempted is recorded, including any that refused -- a refused run is a transcript with `PROOF04_VERDICT not-exercised` and the refusal's own message, never an absence."
    - "A run reaching fewer anchor hits than its target is recorded as a shortfall at the depth actually reached, with the denominator named -- never as a clean bill of health and never as a missing measurement."
    - "Each transcript records `ORACLE_BROKER_STATE inactive` from before the run and an explicit post-run confirmation that no `x64sc` process survived, so no run's numbers rest on a contended machine."
    - "Each transcript records the run's own `ORACLE_SPAWN_ARGV`, `ORACLE_SEED` and `ORACLE_ARGV_DIGEST`, so its run identity is the real launch identity rather than a synthesized one."
    - "No transcript contains the full access map or the full disagreement row list; `PROOF04_DISAGREEMENT_HEAD` caps the addresses shown at 32 and the full rows stay in the uncommitted JSON artifact, cited by sha256."
    - "The subject artifact is produced once and reused by both runs, and both transcripts cite the same `SUBJECT_ARTIFACT_SHA256` -- so the two runs differ in exactly one respect, the anchor depth."
  prohibitions:
    - statement: "A run that produced an inconvenient or unfavourable number must never be silently discarded and re-taken until a better one appears; every run attempted in this phase is recorded with its label, its reached depth and its verdict, including the ones that refused."
      status: unverified
      verification: judgment
    - statement: "The committed derivation rule must never be amended after a measurement has been taken; if an amendment is unavoidable, both the original rule and the amendment are recorded with the date and the reason, and every number taken under each is labelled with which rule produced it."
      status: unverified
      verification: judgment
    - statement: "`not-exercised` must never be selected as a convenience to avoid reporting a computable but unwelcome count; it is reachable only through the input-absent, preflight-refusal, never-reached-anchor and parse-refusal conditions the committed rule names."
      status: unverified
      verification: judgment
  artifacts:
    - .planning/phases/44-proof-04-the-independent-external-check/evidence/proof04-run-a-hit50.md
    - .planning/phases/44-proof-04-the-independent-external-check/evidence/proof04-run-b-narrowed.md
  key_links:
    - "Run A's reached depth to its depth label: the label is derived mechanically against the threshold 50 by the oracle producer, never chosen in prose while writing the transcript."
    - "Run B's `narrowed` label to `EVID-06`'s own licensing sentence and to `S3`'s measured hit-50 / hit-75 boundary: the narrowing is quoted from the measured sources beside the number, not asserted."
    - "Both transcripts to the single `SUBJECT_ARTIFACT_SHA256` from plan 44-01: one subject classification, two oracle depths, so the only variable between the runs is coverage."
---

<objective>
Take the measurement. Two runs against genuine stock VICE 3.9 over the same
release `PROOF-01` measured, at two anchor depths, each joined through the
shipped reconciler and each recorded with its own verdict under the rule plan
44-01 committed before any of this existed.

Purpose: plan 44-01 proved the path works at depth 10. Depth is the whole
question of coverage here, and coverage is what decides whether the count is
informative. dxa classified only 67 of 45072 covered addresses as code, so
every address the emulator is observed executing inside `$0801-$B810` outside
those 67 is a false positive -- but only if the run got far enough for the
release to be resident and running. A shallow run inside `EVID-06`'s licensed
region may report zero disagreements for want of coverage rather than for want
of false positives, and reporting that as a clean bill of health is precisely
the failure Success Criterion 3 forbids. So: one run inside the licensed
region, one deeper run labelled `narrowed`, both recorded, neither replacing
the other (planner decision D-P2, plan 44-01).

Output: two committed transcripts, each carrying its run's full outcome-line
block, its depth, its depth label, its broker state before and after, and its
verdict.

## WHAT MAY AND MAY NOT BE CLAIMED, PER RUN

Restated from plan 44-01's committed rule; the transcripts cite it rather than
re-deriving it.

- At an `ORACLE_DEPTH_REACHED` of 50 or below the label is
  `frame-exact-region`. `EVID-06`'s `no-perturbation` verdict licenses the
  claim that this project's own instrumentation dials do not perturb
  frame-exactness at anchor hit depths of 50 or below, and `S3` is measured
  frame-exact and byte-identical to hit 50.
- Above 50 the label is `narrowed`. `EVID-06`'s authority does not extend
  there; `S3` is measured diverging from hit 75 because `AUTOSTART`'s power
  cycle does not reset the absolute emulated clock. Any comparison against
  v0.8.0's frame-exact captures is stated as narrowed rather than assumed. The
  disagreement count itself is not weakened by the label -- an observed
  execute is an observed execute -- but the run's reproducibility claim is.
- A reached depth below the target narrows the depth claim and nothing else.
  The verdict still follows the committed rule; only `ORACLE_DEPTH_REACHED` is
  what the transcript states.

## ONE CAVEAT THIS PHASE MUST STATE RATHER THAN DISCOVER LATER

The subject classifies the release's file bytes as loaded at `$0801`. The
oracle observes whatever occupied an address at the moment the CPU executed
it. On a cracked release those are not always the same byte: a loader or
depacker that writes over its own region, or over the region it just
decompressed, makes an observed execute at address A evidence that A held code
*at that moment*, not that the original file byte at A was code. This does not
weaken the false-positive count as a statement about the byte-derived tier's
classification being wrong at A -- an address the static tier called `data`
and the machine executed is a genuine miss either way -- but it does bound any
reading of the count as "dxa mis-parsed these specific file bytes". Both
transcripts carry this as `PROOF04_SELF_MODIFICATION_CAVEAT`, stated as a
limit rather than smoothed away, per ENGINEERING_RULES.md section 8.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ENGINEERING_RULES.md
@.planning/phases/44-proof-04-the-independent-external-check/44-RESEARCH.md
@.planning/phases/44-proof-04-the-independent-external-check/44-PATTERNS.md
@.planning/phases/44-proof-04-the-independent-external-check/44-VALIDATION.md
@.planning/phases/44-proof-04-the-independent-external-check/44-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run A -- the licensed run at anchor hit target 50, inside EVID-06's proven region</name>
  <precondition>`systemctl --user is-active vice-broker` reports `inactive` and `pgrep -x x64sc` returns nothing; `.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/corpus/danish.d64` exists and digests to `1a9d294e07f9593ba59d878423d157bacfe6c6902d3a52ef6ac96512a15fb6c5`; `/usr/bin/x64sc --version` reports VICE 3.9.</precondition>
  <files>.planning/phases/44-proof-04-the-independent-external-check/evidence/proof04-run-a-hit50.md</files>
  <read_first>
    - `.planning/phases/44-proof-04-the-independent-external-check/evidence/SCHEMA.md` (the committed derivation rule and the full outcome-line declaration table -- the transcript's outcome-line block must name exactly these)
    - `.planning/phases/44-proof-04-the-independent-external-check/evidence/README.md` (the transcript convention, the `PROBE_DIR` rule, the broker-recorded rule, the no-binaries-committed rule)
    - `.planning/phases/44-proof-04-the-independent-external-check/evidence/proof04-oracle-memmap.mjs` (its argv handling, its numbered S3 sequence, and its short-run and parse-refusal branches)
    - `.planning/phases/44-proof-04-the-independent-external-check/evidence/proof04-reconcile.mjs` (its argv handling and the exact set of lines it prints)
    - `.planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof01-dxa-real-release.md` (the transcript shape: the quoted command, the quoted output, then the bare authoritative outcome-line block, and the separately-named weaknesses section)
    - `docs/phase43-instrumentation-perturbation-ab.md` (lines 125-160: `EVID-06`'s measurement and the exact sentence bounding what its verdict licenses -- quote it, do not paraphrase it)
    - `.planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/evidence/33-autostart-sequencing.md` (line 236: the `S3` row and its frame-exact-to-hit-50 / diverging-from-hit-75 wording)
  </read_first>
  <action>
Confirm and record the pre-run state first: `systemctl --user is-active
vice-broker` and `pgrep -x x64sc`. If the broker is anything but `inactive`,
stop it and re-check before running -- a live broker deterministically reds
`vice-proxy.test.ts`'s BACK-05 case and would invalidate the suite reading
plan 44-03 records, and a direct-spawn run must never race a broker-managed
instance for a port. The oracle producer's own `preflight()` refuses rather
than warns, so a stale broker surfaces as a refusal, not as a silently
contended run.

Produce the subject artifact once, if plan 44-01's is no longer on disk:
`proof04-subject-dxa.mjs --out
$HOME/.cache/c64-re-tools/phase44/subject/subject-dxa.json`. Record its
`SUBJECT_ARTIFACT_SHA256`; both runs in this plan cite that one value, which
is what makes anchor depth the only variable between them.

Take Run A: `proof04-oracle-memmap.mjs --depth 50 --label run-a-hit50 --out
$HOME/.cache/c64-re-tools/phase44/run-a-hit50/oracle-memmap.json`, redirecting
stdout and stderr to a log under
`$HOME/.cache/c64-re-tools/phase44/run-a-hit50/` and reading `$?` on the same
line as the command. Then join: `proof04-reconcile.mjs --subject <the subject
artifact> --oracle <run A's artifact> --label run-a-hit50 --out
$HOME/.cache/c64-re-tools/phase44/run-a-hit50/reconcile.json`, redirected the
same way.

Do not re-run either script to get a different number. If a run refuses, that
refusal is the measurement for this attempt: record the attempt, its refusal
message and the resulting verdict, then diagnose the cause before attempting
again, and record every attempt. A refusal that names a contended emulator is
contention, not a wedge -- the `vice-wedge-triage` playbook's verdict set
covers the distinction, and recycling a healthy instance is the wrong move. An
anchor that never fires is a short run, recorded at the depth reached, not a
hang to be worked around by arming more checkpoints.

Then write `evidence/proof04-run-a-hit50.md` following the Phase 38 transcript
shape:

  * A header naming what this run is, its fixed inputs (the absolute binary
    path, the release and entry digests, the seed, the argv digest), and a
    pointer to `SCHEMA.md` as the sole home of the rule that produced its
    verdict.
  * `BROKER_STATE: inactive` recorded from before the run, and the post-run
    `pgrep -x x64sc` result, both quoted from the actual command output.
  * The three commands quoted verbatim with their output, the access map and
    the full disagreement row list excluded -- cite the artifact JSON by path
    and sha256 instead.
  * The bare authoritative outcome-line block: every `SUBJECT_*`, `ORACLE_*`
    and `PROOF04_*` line `SCHEMA.md` declares, transcribed from the run,
    final occurrence wins.
  * A short section stating what this run's depth licenses, quoting
    `EVID-06`'s own licensing sentence and `S3`'s hit-50 / hit-75 wording from
    the two sources named in `read_first`. Quote them; do not paraphrase.
  * `PROOF04_SELF_MODIFICATION_CAVEAT`, stated in the terms this plan's
    objective gives.
  * If `ORACLE_SHORT_RUN` is `true`, a section stating the depth reached, that
    the depth claim is narrowed to that depth, and that the count is over what
    was reached with `PROOF04_DENOMINATOR` named.
  * If `PROOF04_VERDICT` is `unresolved`, a section stating the shortfall in
    the committed rule's own terms -- `0 of PROOF04_DENOMINATOR` covered
    addresses observed -- and stating explicitly that this is not a clean bill
    of health and that the never-observed population is a count and not a
    class.

Record the observed numbers in the plan SUMMARY as measured values. Assert no
specific count anywhere in the transcript's own prose beyond transcribing what
the run printed.
  </action>
  <acceptance_criteria>
    - `evidence/proof04-run-a-hit50.md` exists and contains exactly one `PROOF04_VERDICT` outcome line whose value is one of `resolved`, `unresolved`, `not-exercised`
    - The transcript contains `ORACLE_DEPTH_TARGET 50` and exactly one `ORACLE_DEPTH_REACHED` line
    - The transcript contains `ORACLE_DEPTH_LABEL frame-exact-region` if and only if its recorded `ORACLE_DEPTH_REACHED` value is 50 or below
    - The transcript contains `BROKER_STATE: inactive` and a quoted post-run `pgrep -x x64sc` result showing no surviving process
    - The transcript contains one `SUBJECT_ARTIFACT_SHA256` line and one `ORACLE_ARTIFACT_SHA256` line, each a 64-character lowercase hex value
    - The transcript contains `PROOF04_BUCKET_IDENTITY_OK true`, `PROOF04_POSITIVE_CLASS code` and `PROOF04_TIER runtime-observed`
    - The transcript contains `PROOF04_SELF_MODIFICATION_CAVEAT`
    - The transcript quotes `EVID-06`'s licensing sentence and the `S3` hit-50 / hit-75 wording verbatim, each with its source path
    - The transcript contains no `.vsf` path, no full access map, and no more than 32 disagreement addresses
    - No sentence in the transcript pairs the never-observed bucket with a data conclusion
    - `git status --porcelain` shows no `.d64`, `.prg`, `.bin`, `.vsf` or `.json` file added anywhere under the phase directory
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && F=.planning/phases/44-proof-04-the-independent-external-check/evidence/proof04-run-a-hit50.md && test -f "$F" || { echo "missing transcript"; exit 1; }; echo -n "verdict_lines="; grep -acE '^PROOF04_VERDICT (resolved|unresolved|not-exercised)$' "$F"; echo -n "depth_target="; grep -ac 'ORACLE_DEPTH_TARGET 50' "$F"; echo -n "depth_reached="; grep -acE '^ORACLE_DEPTH_REACHED [0-9]+$' "$F"; echo -n "identity_ok="; grep -ac 'PROOF04_BUCKET_IDENTITY_OK true' "$F"; echo -n "caveat="; grep -ac 'PROOF04_SELF_MODIFICATION_CAVEAT' "$F"; echo -n "broker="; grep -ac 'BROKER_STATE: inactive' "$F"; echo RUN_A_CENSUS_DONE</automated>
    <fails_when>`RUN_A_CENSUS_DONE` absent, `missing transcript` printed, `verdict_lines=` any value other than 1, `depth_reached=` any value other than 1, or any of `depth_target=` / `identity_ok=` / `caveat=` / `broker=` reading 0</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && F=.planning/phases/44-proof-04-the-independent-external-check/evidence/proof04-run-a-hit50.md && R=$(grep -aoE '^ORACLE_DEPTH_REACHED [0-9]+$' "$F" | tail -1 | awk '{print $2}'); L=$(grep -aoE '^ORACLE_DEPTH_LABEL [a-z-]+$' "$F" | tail -1 | awk '{print $2}'); echo "reached=$R label=$L"; test -n "$R" && test -n "$L" || { echo LABEL_UNDERIVABLE; exit 1; }; if [ "$R" -le 50 ]; then test "$L" = "frame-exact-region" || { echo LABEL_MISMATCH; exit 1; }; else test "$L" = "narrowed" || { echo LABEL_MISMATCH; exit 1; }; fi; echo LABEL_AGREES</automated>
    <fails_when>`LABEL_AGREES` absent, or `LABEL_MISMATCH` or `LABEL_UNDERIVABLE` printed, or either side of the printed `reached=` / `label=` pair is empty</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && F=.planning/phases/44-proof-04-the-independent-external-check/evidence/proof04-run-a-hit50.md && echo -n "data_conclusions="; grep -aic 'confirmed data' "$F" || true; echo -n "vsf_paths="; grep -ac '\.vsf' "$F" || true; echo -n "sha_lines="; grep -acE '(SUBJECT|ORACLE)_ARTIFACT_SHA256 [0-9a-f]{64}$' "$F"; echo RUN_A_HYGIENE_DONE</automated>
    <fails_when>`RUN_A_HYGIENE_DONE` absent, `data_conclusions=` reading anything other than 0, `vsf_paths=` reading anything other than 0, or `sha_lines=` reading fewer than 2</fails_when>
    <automated>pgrep -x x64sc; echo "pgrep_exit=$?"; cd /home/henrik/dev/henrik/git/c64-re-tools && git status --porcelain .planning/phases/44-proof-04-the-independent-external-check/ | grep -aE '\.(d64|prg|bin|vsf|json)$'; echo "binary_leak_grep_exit=$?"</automated>
    <fails_when>`pgrep_exit=0` (an x64sc PID survived the run), or `binary_leak_grep_exit=0` (grep matched, meaning a binary or JSON artifact was added under the phase directory)</fails_when>
  </verify>
  <done>One licensed run at anchor hit target 50 is taken against genuine stock VICE 3.9 and committed as a transcript carrying its reached depth, its mechanically-derived depth label, its broker state before and after, its two artifact digests, its full outcome-line block and a verdict drawn from the committed vocabulary.</done>
</task>

<task type="auto">
  <name>Task 2: Run B -- the deeper run, explicitly narrowed, for the coverage the licensed depth cannot reach</name>
  <precondition>`systemctl --user is-active vice-broker` reports `inactive` and `pgrep -x x64sc` returns nothing; the subject artifact from Task 1 exists at `$HOME/.cache/c64-re-tools/phase44/subject/subject-dxa.json` and its recorded `SUBJECT_ARTIFACT_SHA256` matches the value Task 1's transcript cites.</precondition>
  <files>.planning/phases/44-proof-04-the-independent-external-check/evidence/proof04-run-b-narrowed.md</files>
  <read_first>
    - `.planning/phases/44-proof-04-the-independent-external-check/evidence/proof04-run-a-hit50.md` (Task 1's transcript -- its `SUBJECT_ARTIFACT_SHA256`, its reached depth, and the coverage it actually achieved, which is what decides whether the deeper run is informative)
    - `.planning/phases/44-proof-04-the-independent-external-check/evidence/SCHEMA.md` (the committed rule and the outcome-line declarations)
    - `.planning/phases/44-proof-04-the-independent-external-check/evidence/README.md` (the transcript and broker conventions)
    - `.planning/phases/44-proof-04-the-independent-external-check/evidence/proof04-oracle-memmap.mjs` (its `--depth` handling, its `countHits` budget, and its short-run branch)
    - `docs/phase43-instrumentation-perturbation-ab.md` (lines 125-160: the sentence bounding what `EVID-06`'s verdict licenses)
    - `src/skills/vice-wedge-triage/SKILL.md` (the verdict set -- so a short or contended run is diagnosed as contention or as a stopped-at-your-own-checkpoint state rather than as a wedge to be recycled)
  </read_first>
  <action>
Take a second run at a deeper anchor depth, so the measurement is not limited
to the pre-load region. Target `--depth 3000` -- roughly sixty seconds of
emulated PAL frames past `AUTOSTART` -- with a `countHits` budget generous
enough that the target is not cut short by the budget rather than by the
machine; if the shipped default budget in `proof04-oracle-memmap.mjs` is
smaller than the wall-clock a 3000-hit walk needs on this host, raise the
budget for this invocation only through the script's existing option surface
and record the budget used as part of the quoted command. Do not add a new
flag to the script for this.

Confirm and record the pre-run broker and process state exactly as Task 1
did. Then run `proof04-oracle-memmap.mjs --depth 3000 --label run-b-narrowed
--out $HOME/.cache/c64-re-tools/phase44/run-b-narrowed/oracle-memmap.json`,
redirected to a log under that same directory with `$?` read on the same line,
and join it against the SAME subject artifact Task 1 cited -- never a
re-produced one, because the whole point of reusing it is that anchor depth is
the only variable between the two runs.

Expect a short run to be a real possibility and treat it as one. The oracle
producer records `ORACLE_SHORT_RUN true` and continues to the `memmapshow`
read rather than throwing, so a run that reaches, say, 900 of 3000 hits still
produces a joined count at depth 900. That is the required outcome, not a
failure: record `ORACLE_DEPTH_REACHED` as the authoritative depth and state
the count over what was reached with `PROOF04_DENOMINATOR` named. Do not arm
additional checkpoints to widen coverage -- a non-stopping checkpoint emits a
frame per hit synchronously from inside the CPU loop and can stall the
emulator thread, and `memmapshow`'s access map already accumulates
continuously and unconditionally, so a sweep buys no coverage it was not
already going to report.

Then write `evidence/proof04-run-b-narrowed.md` in the same transcript shape
Task 1 used, with these additions:

  * A prominent section, not a footnote, stating that this run's depth label
    is `narrowed` and what that costs: `EVID-06`'s `no-perturbation` verdict
    licenses claims only at anchor hit depths of 50 or below, quoted verbatim
    with its source path; `S3` is measured frame-exact and byte-identical to
    hit 50 and diverging from hit 75 because `AUTOSTART`'s power cycle does
    not reset the absolute emulated clock; therefore this run is labelled
    instrumented and any comparison against v0.8.0's frame-exact captures is
    narrowed rather than assumed. State also what the label does NOT weaken:
    an observed execute at an address is still an observed execute, so the
    disagreement count stands as a count -- it is the run's reproducibility
    claim that is narrowed, not the observation.
  * A one-line statement that this run does not replace Run A and Run A does
    not replace this one: the two are recorded beside each other, at their two
    depths, with their two labels.
  * `PROOF04_SELF_MODIFICATION_CAVEAT`, in the same terms.
  * The same shortfall and `unresolved` sections Task 1 defines, if either
    condition holds.

If the deeper run cannot be taken at all -- the release never becomes resident,
the anchor never fires past the pre-load region, or the budget cannot cover the
walk on this host -- record `PROOF04_VERDICT not-exercised` for Run B together
with the target attempted, the depth actually reached and what was searched, and
leave Run A's number as the phase's only measurement. That is a legitimate
outcome of this plan and must not be presented as a failure, nor worked around
by relaxing the committed rule.
  </action>
  <acceptance_criteria>
    - `evidence/proof04-run-b-narrowed.md` exists and contains exactly one `PROOF04_VERDICT` outcome line whose value is one of `resolved`, `unresolved`, `not-exercised`
    - The transcript contains `ORACLE_DEPTH_TARGET 3000` and exactly one `ORACLE_DEPTH_REACHED` line
    - The transcript's `ORACLE_DEPTH_LABEL` value agrees with its recorded `ORACLE_DEPTH_REACHED` against the threshold 50 -- `narrowed` above it, `frame-exact-region` at or below
    - When the label is `narrowed`, the transcript quotes `EVID-06`'s licensing sentence with its source path, states the hit-50 / hit-75 boundary, and states that any comparison against v0.8.0's frame-exact captures is narrowed
    - The transcript cites the same `SUBJECT_ARTIFACT_SHA256` value that `proof04-run-a-hit50.md` cites
    - The transcript states in one sentence that neither run replaces the other
    - The transcript contains `PROOF04_BUCKET_IDENTITY_OK true` and `PROOF04_SELF_MODIFICATION_CAVEAT`
    - The transcript records `BROKER_STATE: inactive` from before the run and a quoted post-run `pgrep -x x64sc` result showing no surviving process
    - No sentence in the transcript pairs the never-observed bucket with a data conclusion
    - Exactly one Exec checkpoint was armed in this run, at the frame anchor; the transcript's quoted output shows one `ARMED` line and no additional checkpoint arming
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && F=.planning/phases/44-proof-04-the-independent-external-check/evidence/proof04-run-b-narrowed.md && test -f "$F" || { echo "missing transcript"; exit 1; }; echo -n "verdict_lines="; grep -acE '^PROOF04_VERDICT (resolved|unresolved|not-exercised)$' "$F"; echo -n "depth_target="; grep -ac 'ORACLE_DEPTH_TARGET 3000' "$F"; echo -n "depth_reached="; grep -acE '^ORACLE_DEPTH_REACHED [0-9]+$' "$F"; echo -n "caveat="; grep -ac 'PROOF04_SELF_MODIFICATION_CAVEAT' "$F"; echo -n "broker="; grep -ac 'BROKER_STATE: inactive' "$F"; echo RUN_B_CENSUS_DONE</automated>
    <fails_when>`RUN_B_CENSUS_DONE` absent, `missing transcript` printed, `verdict_lines=` any value other than 1, `depth_reached=` any value other than 1, or either of `depth_target=` / `caveat=` / `broker=` reading 0</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && F=.planning/phases/44-proof-04-the-independent-external-check/evidence/proof04-run-b-narrowed.md && R=$(grep -aoE '^ORACLE_DEPTH_REACHED [0-9]+$' "$F" | tail -1 | awk '{print $2}'); L=$(grep -aoE '^ORACLE_DEPTH_LABEL [a-z-]+$' "$F" | tail -1 | awk '{print $2}'); echo "reached=$R label=$L"; test -n "$R" && test -n "$L" || { echo LABEL_UNDERIVABLE; exit 1; }; if [ "$R" -le 50 ]; then test "$L" = "frame-exact-region" || { echo LABEL_MISMATCH; exit 1; }; else test "$L" = "narrowed" && grep -aq 'hit 75' "$F" && grep -aq 'narrowed' "$F" || { echo NARROWING_ABSENT; exit 1; }; fi; echo LABEL_AGREES</automated>
    <fails_when>`LABEL_AGREES` absent, or any of `LABEL_MISMATCH` / `LABEL_UNDERIVABLE` / `NARROWING_ABSENT` printed, or either side of the printed `reached=` / `label=` pair is empty</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && E=.planning/phases/44-proof-04-the-independent-external-check/evidence && A=$(grep -aoE '^SUBJECT_ARTIFACT_SHA256 [0-9a-f]{64}$' "$E/proof04-run-a-hit50.md" | tail -1 | awk '{print $2}'); B=$(grep -aoE '^SUBJECT_ARTIFACT_SHA256 [0-9a-f]{64}$' "$E/proof04-run-b-narrowed.md" | tail -1 | awk '{print $2}'); echo "run_a=$A run_b=$B"; test -n "$A" && test -n "$B" && test "$A" = "$B" && echo SUBJECT_SHARED || { echo SUBJECT_DIVERGED; exit 1; }</automated>
    <fails_when>`SUBJECT_SHARED` absent, `SUBJECT_DIVERGED` printed, or either side of the printed `run_a=` / `run_b=` pair is empty</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && F=.planning/phases/44-proof-04-the-independent-external-check/evidence/proof04-run-b-narrowed.md && echo -n "data_conclusions="; grep -aic 'confirmed data' "$F" || true; echo -n "armed_lines="; grep -ac 'ARMED' "$F" || true; echo RUN_B_HYGIENE_DONE</automated>
    <fails_when>`RUN_B_HYGIENE_DONE` absent, `data_conclusions=` reading anything other than 0, or `armed_lines=` reading anything other than 1</fails_when>
    <automated>pgrep -x x64sc; echo "pgrep_exit=$?"; cd /home/henrik/dev/henrik/git/c64-re-tools && git status --porcelain .planning/phases/44-proof-04-the-independent-external-check/ | grep -aE '\.(d64|prg|bin|vsf|json)$'; echo "binary_leak_grep_exit=$?"</automated>
    <fails_when>`pgrep_exit=0` (an x64sc PID survived the run), or `binary_leak_grep_exit=0` (grep matched, meaning a binary or JSON artifact was added under the phase directory)</fails_when>
  </verify>
  <done>A second run at a deeper stated depth is committed beside Run A, carrying the `narrowed` label with `EVID-06`'s licensing sentence and the hit-50 / hit-75 boundary quoted beside it, citing the same subject artifact so depth is the only variable -- or, where the deeper run could not be taken, `not-exercised` recorded with the target attempted and the depth reached.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VICE text-monitor reply to `parseAccessMap()` | Version-drifting untrusted text becomes the oracle side of a published count; a silently-absorbed drift would publish a fabricated zero. |
| host process table to the direct-spawn run | Two live emulator launches must not race a broker-managed instance nor orphan a process into the next run's preflight. |
| the run transcripts to every later reader | Once committed, these two files are the authoritative numbers; a discarded run absent from them is unrecoverable from the record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-01 | Tampering | corpus and extracted-entry identity at read time | high | mitigate | Both runs re-assert the release digest `1a9d294e07f9593ba59d878423d157bacfe6c6902d3a52ef6ac96512a15fb6c5` independently, and both transcripts cite the one `SUBJECT_ARTIFACT_SHA256` from the single subject production so a re-derived subject cannot slip between them. Inherited from plan 44-01's producer, asserted again per run. |
| T-44-02 | Spoofing | emulator binary resolution | high | mitigate | Every run resolves `/usr/bin/x64sc` by absolute path through the harness's `viceKind()` refusal, and each transcript records `ORACLE_VICE_BINARY` and `ORACLE_VICE_VERSION` from a live probe, so a fork measurement cannot be recorded as a stock one. Inherited unchanged. |
| T-44-03 | Tampering (data integrity) | `parseAccessMap()` refusal handling | high | mitigate | A refusal writes no artifact, prints `ORACLE_PARSE_REFUSAL` with the refusal's own code and offending line, exits non-zero, and routes to `not-exercised` by the committed rule. Inherited from plan 44-01; each transcript records the value even when it is `none`. |
| T-44-04 | Repudiation | a discarded or re-taken run vanishing from the record | high | mitigate | This plan's first prohibition forbids result-shopping and requires every attempt to be recorded with its label, reached depth and verdict; the transcripts are the audit trail, and both tasks' acceptance criteria require exactly one verdict line per committed run so a second silent attempt cannot hide inside one file. |
| T-44-05 | Repudiation | a verdict or a limit softened while writing the prose | high | mitigate | The verdict is transcribed from the reconcile driver's printed `PROOF04_VERDICT`, not judged in prose; the depth label is derived mechanically by the oracle producer and cross-checked against the reached depth by this plan's own verify gate; the `narrowed` narrowing is quoted verbatim from `docs/phase43-instrumentation-perturbation-ab.md` and the `S3` sweep rather than paraphrased. |
| T-44-06 | Denial of service | orphaned `x64sc` processes across two runs | medium | mitigate | `preflight()` throws on a non-inactive broker or a live `x64sc`; `reapAll()` runs in a `finally`; both tasks' verify gates assert `pgrep -x x64sc` prints nothing afterwards, and each transcript records the post-run result. Inherited unchanged. |
| T-44-07 | Denial of service | a checkpoint sweep stalling the emulator thread | medium | mitigate | Exactly one stopping Exec checkpoint is armed, at the frame anchor; Task 2's acceptance criteria assert a single `ARMED` line. A non-stopping checkpoint would emit a frame per hit synchronously from inside the CPU loop, and `memmapshow` already accumulates continuously, so no sweep is needed. |
| T-44-08 | Information disclosure | operator-supplied corpus bytes or snapshot data entering the checkout | medium | mitigate | Every artifact and log lands under `$HOME/.cache/c64-re-tools/phase44/`, outside the checkout and never in the system temp tmpfs; identity travels as name plus sha256 (D-P4); both tasks' verify gates grep `git status --porcelain` for an added `.d64`, `.prg`, `.bin`, `.vsf` or `.json` under the phase directory and fail when one appears. |
| T-44-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no external package and adds no dependency. `44-RESEARCH.md` records its Package Legitimacy Audit as not applicable, so there is no install task to gate and no `[ASSUMED]` or `[SUS]` row to verify. |
</threat_model>

<verification>
1. Both transcripts exist, each with exactly one `PROOF04_VERDICT` line drawn from the committed three-token vocabulary.
2. Each transcript's `ORACLE_DEPTH_LABEL` agrees with its own `ORACLE_DEPTH_REACHED` against the threshold 50, checked mechanically rather than read.
3. Run B's transcript, when labelled `narrowed`, quotes `EVID-06`'s licensing sentence with its source path and states the hit-50 / hit-75 boundary.
4. Both transcripts cite the same `SUBJECT_ARTIFACT_SHA256`.
5. Both transcripts carry `PROOF04_BUCKET_IDENTITY_OK true` and `PROOF04_SELF_MODIFICATION_CAVEAT`.
6. Both transcripts record `BROKER_STATE: inactive` before the run and a quoted post-run `pgrep -x x64sc` showing nothing survived.
7. Neither transcript pairs the never-observed bucket with a data conclusion.
8. `pgrep -x x64sc` prints nothing and no binary or JSON artifact was added under the phase directory.
</verification>

<success_criteria>
- A false-positive count exists as a measured number on real cracked code, with its denominator and its positive class, at a stated anchor depth -- or `not-exercised` is recorded with what was searched and at what depth.
- The licensed run and the narrowed run stand beside each other with their two labels, and neither is presented as superseding the other.
- A short run is recorded as a shortfall at the depth reached, never as a clean bill of health and never as a missing measurement.
- Every run attempted is in the record, including any that refused.
- No run's numbers rest on a contended machine, and no run left an emulator behind.
</success_criteria>

## Artifacts this phase produces

New symbols and paths introduced by THIS plan (excluded from drift verification because they do not exist yet):

- `.planning/phases/44-proof-04-the-independent-external-check/evidence/proof04-run-a-hit50.md` (new file)
- `.planning/phases/44-proof-04-the-independent-external-check/evidence/proof04-run-b-narrowed.md` (new file)
- Run labels introduced as artifact-directory names and `--label` values: `run-a-hit50`, `run-b-narrowed`
- Transcript token introduced by this plan: `PROOF04_SELF_MODIFICATION_CAVEAT`
- Transcript header field introduced by this plan: `BROKER_STATE:`
- Verify-gate marker tokens: `RUN_A_CENSUS_DONE`, `RUN_A_HYGIENE_DONE`, `RUN_B_CENSUS_DONE`, `RUN_B_HYGIENE_DONE`, `LABEL_AGREES`, `LABEL_MISMATCH`, `LABEL_UNDERIVABLE`, `NARROWING_ABSENT`, `SUBJECT_SHARED`, `SUBJECT_DIVERGED`

Symbols consumed unchanged, not produced: every `SUBJECT_*`, `ORACLE_*` and `PROOF04_*` outcome-line name declared by plan 44-01's `SCHEMA.md`; the three scripts and the test plan 44-01 created; `preflight`, `reapAll`, `viceKind`, `parseAccessMap`, `ingestAccessMap`, `reconcileObservedExecution`.

<!-- planner-discipline-allow: confirmed data -->

<output>
Create `.planning/phases/44-proof-04-the-independent-external-check/44-02-SUMMARY.md` when done.
</output>