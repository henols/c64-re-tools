---
phase: 30-acme-export-and-the-real-acme-oracle
plan: 04
type: execute
wave: 3
depends_on: ["30-02", "30-03"]
files_modified:
  - src/mcp/vice/anno-export-asm.ts
  - src/mcp/vice/anno-export-asm.test.ts
  - src/mcp/vice/fixtures/export-asm/README.md
  - src/mcp/vice/fixtures/export-asm/smc.a
  - src/mcp/vice/fixtures/export-asm/smc.prg
  - src/mcp/vice/fixtures/export-asm/make-export-asm-fixtures.mjs
autonomous: true
requirements: [EXPORT-02, EXPORT-03]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A fixture that GENUINELY self-modifies — an `inc` whose absolute target is the operand byte of an earlier `lda #` — exports with its write target named by a `=*+$01` mid-instruction label and reassembles BYTE-IDENTICALLY."
    - "Placement is proved load-bearing, not decorative: moving the same label line to AFTER its host instruction makes the byte-diff fail while ACME still exits 0, because the label then names the NEXT instruction's operand."
    - "A mid-instruction label at an address below `$0100` is REFUSED by name rather than emitted, because a forward reference to it would widen an operand and shift every byte after it."
    - "The eleven typed prefixes are derived from `AUTO_NAME_PREFIX_RE` itself — the exporter imports it and the test parses its alternation — so `routine-queue-walker`'s backlog signal still recognises `p_`, `j_`, `s_`, `b_`, `r_` and `zpf_`, and a five-prefix reimplementation would fail the structural scan."
    - "`L_` is absent from the prefix set and stays absent, and prefix matching is ASCII case-sensitive so `S_0820` does not match `s_`."
    - "Every one of the `acmeExpressible: false` opcodes exports as a `!byte` directive carrying all of its bytes with a naming comment, never as an invented mnemonic, and the whole 256-opcode export reassembles byte-identically — driven from the `OPCODES` table itself, never from a hand-written list."
    - "An enum renders on the IMMEDIATE operand only; the exporter refuses an enum bound to a non-immediate operand by name, and the wrong-operand emission is proved to change both the bytes and the instruction length — the mutated source is REFUSED at the source-text boundary with nothing about the run reading as a pass, its `exitStatus` observed and the rule that produced the verdict named, rather than an exit status pinned in advance."
    - "An enum whose variant value exceeds `$ff` on an immediate operand is refused by the exporter, and real ACME independently refuses it with `Number does not fit in 8 bits.` and exit 1."
    - "The store REFUSES a duplicate label name bound to a different address, asserted against the store's own refusal wording read out of its source; and with the duplicate reproduced at the source-text boundary, real ACME itself reports `Symbol already defined.` and exits 1 — the external oracle confirming the internal one."
    - "An empty label set, an empty enum set and a single-element range each export and reassemble byte-identically."
  artifacts:
    - "src/mcp/vice/fixtures/export-asm/smc.prg — a committed image that genuinely self-modifies"
    - "src/mcp/vice/fixtures/export-asm/smc.a — the ACME source that produced it"
    - "src/mcp/vice/fixtures/export-asm/make-export-asm-fixtures.mjs — the regenerator"
    - "src/mcp/vice/fixtures/export-asm/README.md — provenance and regeneration instructions"
  key_links:
    - "anno-export-asm.ts imports AUTO_NAME_PREFIX_RE from anno-coverage.ts — the FIRST cross-module production importer of that constant; anno-types.ts:93-99 forbids restating the prefixes by name"
    - "the 256-opcode export is driven from disasm-opcodes.ts's OPCODES table, so a future correction to that table is automatically re-verified"
    - "the duplicate-label ACME observation is produced at the source-text boundary because anno_label.name carries a `unique` DDL constraint and the store cannot hold two rows with one name"
  prohibitions:
    - "Correctness must never be claimed from a string match on the exporter's own output — this project shipped 14 wrong opcode-table entries that were internally verified, caught only by a real assembler."
    - "An annotation the exporter cannot express must be refused loudly and by name, never silently dropped while the export reports success."
    - "The oracle must never be satisfiable by the exporter's own output."
    - "No verb, flag or capability outside USER-D-01's locked scope may be absorbed into this phase."
---

<scope_disposition>
**Advisory, recorded rather than acted on.** This plan's `estimate.tokens` of 115000 is 1.15x the
100000-token smart-zone budget and is the highest in the phase — but it carries
`confidence: low` on a sample count of 0, so it is an uncalibrated projection, and on the
structural thresholds (3 tasks, 6 files, one concern) it is healthy. **No re-slice.** The full
plan-set disposition is recorded once, in `30-01-PLAN.md` § `<scope_disposition>`.

**If this plan overruns during execution, Task 3 is the seam.** It alone builds a 256-opcode image,
iterates `OPCODES` twice and runs the duplicate-symbol oracle; split it out as `30-04b` on an
observed overrun, not on this projection.
</scope_disposition>

<objective>
Make both carried idioms **load-bearing in the reassembly** rather than merely emitted, and let
a real assembler confirm the store's own refusals.

Purpose: criterion 3 is explicit that emitting the `=*+$01` idiom is not the criterion —
reassembling byte-identically on a fixture that *actually contains self-modifying code* is. And
criterion 5 asks for the shape this project's own record demands: an internally-verified opcode
table still shipped 14 wrong entries, caught only by running the output through a real
assembler.

Output: a committed self-modifying fixture that round-trips, the eleven prefixes wired to their
one home with a structural scan that fails a five-prefix copy, enum substitution confined to the
immediate operand, the full `acmeExpressible: false` set proved to go out as `!byte`, and real
ACME reporting the duplicate symbol the store already refuses.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/30-acme-export-and-the-real-acme-oracle/30-RESEARCH.md
@.planning/phases/30-acme-export-and-the-real-acme-oracle/30-PATTERNS.md
@.planning/phases/30-acme-export-and-the-real-acme-oracle/30-VALIDATION.md
@.planning/phases/30-acme-export-and-the-real-acme-oracle/30-02-SUMMARY.md
@.planning/phases/30-acme-export-and-the-real-acme-oracle/30-03-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols introduced by **this plan**:

| Kind | Name | File |
|---|---|---|
| function | `midInstructionLabelLine(name, offset): string` (module-private) | `anno-export-asm.ts` |
| field | `ExportAsmResult.autoNamedSymbolCount` | `anno-export-asm.ts` |
| field | `ExportAsmResult.enumSubstitutionCount` | `anno-export-asm.ts` |
| field | `ExportAsmResult.midInstructionLabelCount` | `anno-export-asm.ts` |
| new dir | `src/mcp/vice/fixtures/export-asm/` | — |
| script | `make-export-asm-fixtures.mjs` | `src/mcp/vice/fixtures/export-asm/` |

`ExportAsmOptions` and `exportAsm()`'s signature are **unchanged** — plan 30-05's CLI verb binds
to them. New result fields are additive only.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The `=*+$01` idiom on a fixture that genuinely self-modifies, with placement proved load-bearing</name>
  <files>src/mcp/vice/anno-export-asm.ts, src/mcp/vice/anno-export-asm.test.ts, src/mcp/vice/fixtures/export-asm/README.md, src/mcp/vice/fixtures/export-asm/smc.a, src/mcp/vice/fixtures/export-asm/smc.prg, src/mcp/vice/fixtures/export-asm/make-export-asm-fixtures.mjs</files>
  <read_first>
    - `src/mcp/vice/anno-export-asm.ts` as plan 30-03 left it
    - `.planning/phases/30-acme-export-and-the-real-acme-oracle/30-RESEARCH.md` § Architecture Patterns 1 — BOTH measured directions, the golden witness's compact spelling, and the note that ACME exits 0 either way; § Code Examples 4; § Common Pitfalls 4
    - `.planning/notes/dxa-ghidra-pivot-evidence/anno.asm:19-42, 51, 81, 135, 145, 202-205` — the EXTERNAL LABELS header block and all **six** live `=*+$01` labels (the ROADMAP note says four; RESEARCH.md corrected it to six)
    - `src/mcp/vice/fixtures/coverage/make-coverage-fixtures.mjs` — the committed-fixture regenerator precedent, its banner and its provenance discipline
    - `src/mcp/vice/fixtures/backend-detect/README.md` — the provenance table and Regenerating section shape
    - `src/mcp/vice/disasm-decoder.ts:61-84` — `DecodedOperand` and `Instruction`, for deciding when an address falls strictly inside an instruction
  </read_first>
  <behavior>
    - `smc.prg` assembles from `smc.a` to exactly the committed bytes, and those bytes contain an `inc` whose absolute operand is the address of an earlier `lda #`'s immediate byte — the program genuinely modifies itself.
    - A store with a code range covering the whole fixture and a label named `smc_operand` at the mid-instruction address exports source in which `smc_operand =*+$01` sits on its own line IMMEDIATELY BEFORE the `lda #$00` line, and `inc smc_operand` renders substituted.
    - That export verifies `outcome === "ok"` with `byteDiff.equal === true` against real ACME.
    - NEGATIVE CONTROL: moving the `smc_operand =*+$01` line to immediately AFTER its host instruction yields `outcome === "failed"`, `exitStatus === 0`, and `byteDiff.firstDifferingOffset` at the `inc` operand's low byte.
    - A mid-instruction label at an address below `$0100` makes `exportAsm()` throw with a message naming the address and the widening hazard.
    - `midInstructionLabelCount` equals the number of `=*+$NN` lines emitted.
  </behavior>
  <action>
**Part A — the fixture.**

Create `src/mcp/vice/fixtures/export-asm/`:

- `smc.a` — an ACME source that genuinely self-modifies. Shape it so the self-modification is
  unmistakable and the mid-instruction address is unambiguous:
  `!cpu 6510`, `* = $0801`, `smc_operand = *+$01`, then `lda #$00`, `inc smc_operand`,
  `sta $d020`, `jmp $0801`. The `inc` targets `$0802`, which is the `lda`'s immediate operand
  byte. Carry a header comment saying, in words, that this program modifies its own `lda`
  operand every pass and that a fixture merely *containing the idiom* would not satisfy
  criterion 3.
- `smc.prg` — the committed assembled bytes, `-f cbm` so the two-byte load address is present.
- `make-export-asm-fixtures.mjs` — the regenerator, in `make-coverage-fixtures.mjs`'s style:
  a `#!/usr/bin/env node` shebang, a banner comment naming what it regenerates and when to
  re-run it, an argv-array `spawnSync` of `ACME_BIN`, and a refusal to run if the assembler is
  unavailable (it prints the reason and exits non-zero rather than writing a partial fixture).
- `README.md` — the provenance table (File | Produced by | Assembler | Host | Captured at |
  Requirement anchor), the "these are real captures, not synthesized" paragraph adapted, a
  Regenerating section naming the exact command, and a "what this fixture proves" section stating
  the criterion-3 distinction above.

**Part B — the insertion rule in `anno-export-asm.ts`.**

Add a module-private `midInstructionLabelLine(name: string, offset: number): string` returning
the golden witness's compact spelling: `${name} =*+$${offset.toString(16).padStart(2, "0")}`.

In the code-block emitter, for each decoded instruction, find every store label whose address
satisfies `address > instr.address && address < instr.address + instr.length` and emit its
`=*+$NN` line **immediately before** that instruction's own rendered line — never after. Put the
measured reason in a comment directly above the loop: placing it after names the *next*
instruction's operand, and ACME exits 0 either way, so only a byte-diff distinguishes the two.

A mid-instruction label whose address is below `$0100` is REFUSED:
`exportAsm: label "<name>" names address $00XX inside an instruction, and a mid-instruction label below $0100 cannot be emitted -- a forward reference to it widens the operand that reads it and shifts every byte after it (measured: ACME reports only a Warning and exits 0).`
State in the same comment that this is the one hole `disasm-renderer.ts`'s `+2` width force does
not already close.

A mid-instruction label must NOT also appear in the header definition block — it is defined
inline. Exclude it there, and add `midInstructionLabelCount` to `ExportAsmResult`.

**Part C — the tests in `anno-export-asm.test.ts`.**

Every verification in this plan goes through plan 30-03's local helpers rather than calling
`verifyAcmeAssembles()` directly. Plan 30-03 left `verifyExport(result)` as the file's single call
site; this plan needs to verify **mutated source text** against an unmutated export's expectations,
so generalise rather than duplicate:

- Add `verifyExportText(result: ExportAsmResult, source: string): AcmeVerifyResult` — it holds the
  file's one `verifyAcmeAssembles()` call and passes
  `{ source, expectedBytes: result.expectedBytes, expectedSegments: result.blocks }`.
- Rewrite `verifyExport(result)` to `return verifyExportText(result, result.source)` — a one-line
  delegation, so the call site does not multiply and every existing 30-03 test keeps its meaning.

Both paths therefore supply the REQUIRED `expectedSegments` (D30-06) and no test in this file can
obtain a verdict with the unanimity rule unrun. Plan 30-03's comment-stripped
`grep -c 'verifyAcmeAssembles('` criterion over the test file must still return `1` when this plan
is done.

- A fixture-integrity test (no ACME needed): `smc.prg` exists, its load address is `$0801`, and
  its body contains the `inc` opcode whose operand equals the `lda`'s immediate byte address —
  asserted from the bytes, so the fixture cannot silently stop self-modifying.
- A regenerator-agreement test (`{ skip: SKIP_REASON }`): re-assemble `smc.a` in a temp directory
  and assert the result is byte-equal to the committed `smc.prg`, so the two cannot drift.
- The round-trip test (`{ skip: SKIP_REASON }`): build the store over `smc.prg`, export, and
  assert the emitted line order (`smc_operand =*+$01` on the line immediately preceding the
  `lda #$00` line) and `outcome === "ok"` with `byteDiff.equal === true`.
- The NEGATIVE CONTROL (`{ skip: SKIP_REASON }`): apply one documented string move to the export
  text — the `=*+$01` line goes from before its host to after it — and verify again. Assert
  `outcome === "failed"`, `exitStatus === 0`, and the `firstDifferingOffset`. The assertion
  message must state that ACME accepted both and only the byte-diff told them apart.
- The zero-page refusal test: a store with a mid-instruction label below `$0100` makes
  `exportAsm()` throw, asserted on the message prefix and the address.
  </action>
  <verify>
    <automated>cd src/mcp/vice && set -o pipefail && VICE_REQUIRE_ACME=1 node --test anno-export-asm.test.ts && npm run test:automated 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `src/mcp/vice/fixtures/export-asm/smc.prg` exists and its first two bytes are `01 08`.
    - The fixture-integrity test proves from the BYTES that an `inc` targets an earlier `lda`'s immediate operand — a test that only checks for the presence of `=*+$01` in the emitted text is a defect, not a criterion.
    - The round trip asserts `outcome === "ok"` AND `byteDiff.equal === true`; "ACME exits 0" is nowhere used as the proof.
    - The negative control is **observed**: paste `outcome`, `exitStatus`, and `byteDiff.firstDifferingOffset` for the moved-label run into the SUMMARY, showing exit 0 with a failed diff.
    - `cd src/mcp/vice && VICE_REQUIRE_ACME=1 node --test anno-export-asm.test.ts` exits 0.
    - Source assertion: `grep -v '^\s*//' src/mcp/vice/anno-export-asm.ts | grep -v '^\s*\*' | grep -c '=\*+\$'` returns at least `1` and every occurrence is inside `midInstructionLabelLine`.
    - `cd src/mcp/vice && npm run test:automated` reports **0 failures**.
  </acceptance_criteria>
  <done>A fixture that really self-modifies exports with its write target named by a mid-instruction label and reassembles byte-identically, and the wrong placement is observed producing different bytes at exit 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The eleven typed prefixes from their one home, and enums on the immediate operand only</name>
  <files>src/mcp/vice/anno-export-asm.ts, src/mcp/vice/anno-export-asm.test.ts</files>
  <read_first>
    - `src/mcp/vice/anno-coverage.ts:1380-1450` — `AUTO_NAME_PREFIX_RE` at `:1393`, the recorded reason `L_` is deliberately absent at `:1382-1391`, and the in-module consumer at `:1442`
    - `src/mcp/vice/anno-coverage.test.ts:39` and `:1400-1430` — the existing assertions that all eleven example names match, that `L_` is absent, and that matching is ASCII case-sensitive
    - `src/mcp/vice/anno-types.ts:93-99` — the paragraph forbidding a restatement of the prefixes by name
    - `src/mcp/vice/module-classification.ts:149-155` — the record that EXPORT-02's prefix clause anchors the census entry
    - `src/skills/routine-queue-walker/SKILL.md:128-181, 220-226` — the prefixes as documented vocabulary, the consumer that breaks silently
    - `src/mcp/vice/anno-store.ts:3258` and `:3389` — `listProjectEnums()` / `listEnumUsage()` signatures and row shapes
    - `.planning/phases/30-acme-export-and-the-real-acme-oracle/30-RESEARCH.md` § Common Pitfalls 8 and § Code Examples 5 (all three measured enum cases)
    - `src/mcp/vice/anno-cli-path-consumers.test.ts:106-158` and `:422-479` — the quote-aware comment stripper, the ONE-predicate discipline, and the two-direction planted controls including the comment-only control
  </read_first>
  <behavior>
    - `anno-export-asm.ts` imports `AUTO_NAME_PREFIX_RE` from `./anno-coverage.ts` and uses it to mark auto-generated names; the module contains no second prefix list.
    - A structural scan over the exporter's comment-stripped source reports a planted five-prefix regex as a violation and reports the real source as clean — both directions asserted.
    - A store with one label per prefix, the eleven derived by parsing the regex's own alternation, exports all eleven names into the source and verifies `ok`.
    - `L_0801` is not counted as auto-generated; `S_0820` is not counted (ASCII case-sensitive).
    - `autoNamedSymbolCount` equals eleven for that store.
    - An enum bound to an instruction whose operand role is `immediate` renders as `#<variant>` with the variant defined in the header, and reassembles byte-identically to the plain `#$01` form.
    - An enum bound to an address whose instruction operand is NOT immediate makes `exportAsm()` throw with a message naming the address and the operand role.
    - An enum variant value above `$ff` on an immediate operand makes `exportAsm()` throw; real ACME independently refuses the equivalent source with `Number does not fit in 8 bits.` and exit 1.
    - WRONG-OPERAND CONTROL: hand-moving an enum symbol onto a following `sta` in the export text changes both the bytes and the instruction length, and the mutated source is REFUSED — `outcome === "failed"`, with nothing about the run reading as a pass. `exitStatus` is observed and recorded, never pinned: 30-03's per-block `!if * != $YYYY` bracket can fire first and make ACME exit 1, and the assertion names whichever rule produced the verdict.
  </behavior>
  <action>
**Part A — wire the prefixes to their one home.**

`anno-export-asm.ts` gains `import { AUTO_NAME_PREFIX_RE } from "./anno-coverage.ts";` — this is
the **first cross-module production importer** of that constant, so establish the pattern rather
than looking for one. Its genuine job: the exporter marks every symbol definition whose name
matches as auto-generated, by appending a fixed trailing comment to that definition line, and
counts them into `ExportAsmResult.autoNamedSymbolCount`.

State the reason in a comment above the import, naming the consumer: `routine-queue-walker`
reads the typed prefixes as documented vocabulary and never imports the regex, so the coupling is
by convention and would break silently; marking them in the export is what keeps the backlog
visible in the generated source. Add a WHAT-NOT-TO-DO bullet naming `anno-types.ts:93-99`'s
prohibition on restating the eleven.

**Part B — the structural scan, with both controls.**

In `anno-export-asm.test.ts`, add the three-piece structural test:

1. A quote-aware `stripComments(src)` of the shape `anno-cli-path-consumers.test.ts:106-143` uses.
2. **The ONE predicate** — a single function, called by the real scan and by both controls, that
   answers "does this source restate the auto-name prefixes rather than import them". Define it as:
   the stripped source contains an import binding for `AUTO_NAME_PREFIX_RE`, **and** contains no
   regex literal whose body carries two or more of the prefix tokens parsed out of
   `AUTO_NAME_PREFIX_RE`'s own alternation.
3. Three directions: the real source is clean; a planted five-prefix regex literal is reported;
   and a comment-only source carrying the same text is **not** reported — the control that stops
   the scan degrading into a substring search that passes by counting its own prose.

Derive the eleven prefix tokens by parsing `AUTO_NAME_PREFIX_RE.source`'s alternation in the test.
Assert the parsed count is exactly `11`, that `L_` is not among them, and that a store label per
parsed prefix round-trips. **Never** write the eleven names as an array literal in the test.

**Part C — enum substitution, immediate operand only.**

In `anno-export-asm.ts`, read `listProjectEnums(handle)` and `listEnumUsage(handle)` alongside the
existing reads, inside the same single-handle `try`/`finally`. For each usage row:

- Locate the decoded instruction at that address. If there is none, or its `operand.role` is not
  `immediate`, REFUSE:
  `exportAsm: enum "<enumName>" is bound to $XXXX, whose operand role is "<role>" -- an enum renders on the IMMEDIATE operand only. Emitting it on any other operand changes both the bytes and the instruction length while ACME exits 0 (measured: `sta` on a symbol below $0100 encodes as zeropage, 2 bytes instead of 3).`
- Look up the variant whose key equals the operand's value. If none, refuse by name. If the
  variant's value exceeds `$ff`, refuse by name, with a comment recording that real ACME's own
  refusal is `Number does not fit in 8 bits.` at exit 1 — the exporter refuses first so the
  message names the store row rather than a temp file line number.
- Emit the variant definition in the header block as `<enumName>_<VARIANT> = $XX` (two hex digits,
  per D30-05's width rule) and render the operand as `#<enumName>_<VARIANT>` by extending the
  `symbolFor`-shaped injection the code path already uses. Count into
  `ExportAsmResult.enumSubstitutionCount`.

Every variant name goes through `assertLegalAcmeIdentifier()` before it can reach the source.

**Part D — the enum tests.**

- Byte-identity: an enum on an immediate operand verifies `ok`, and the emitted bytes equal those
  of the same program exported without the enum — asserted by exporting both and comparing
  `expectedBytes` plus both verdicts.
- The wrong-operand REFUSAL: `exportAsm()` throws, asserted on the message and the role.
- The wrong-operand CONTROL (`{ skip: SKIP_REASON }`): take a valid export and hand-move the enum
  symbol onto a following `sta`, then verify through `verifyExportText()`. Assert
  `outcome === "failed"`, with a message stating which instrument caught it.
  **Observe `exitStatus` rather than asserting it blind, and record what you saw in the SUMMARY.**
  Two refusals are in play here and they disagree about the exit code: an enum symbol below `$0100`
  moved onto a `sta` turns a 3-byte absolute into a 2-byte zeropage, and plan 30-03's per-block
  end assertion (`!if * != $YYYY`) then fires and ACME exits **1** — a stronger catch than the
  byte-diff, not a weaker one, since it refuses to write an output file at all. Assert
  `exitStatus === 0` **only if that is what the run actually returns**; if it returns 1, assert 1 and
  record in the SUMMARY that the `*` bracket pre-empted the byte-diff on this vector, naming which
  rule produced the verdict. What is load-bearing and asserted unconditionally is that the mutation
  is REFUSED and that nothing about it reads as a pass. Do not silence the `*` assertion to
  manufacture an exit-0 observation — that would disable the instrument criterion 4 asks for in
  order to demonstrate a different one.
- The over-`$ff` pair: `exportAsm()` throws, and a hand-built source with the same shape makes
  real ACME exit 1 with its own message — the external oracle agreeing with the internal one.
  </action>
  <verify>
    <automated>cd src/mcp/vice && set -o pipefail && VICE_REQUIRE_ACME=1 node --test anno-export-asm.test.ts && npm run test:automated 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -v '^\s*//' src/mcp/vice/anno-export-asm.ts | grep -v '^\s*\*' | grep -c 'AUTO_NAME_PREFIX_RE'` returns at least `2` (the import and at least one use).
    - Source assertion: `grep -v '^\s*//' src/mcp/vice/anno-export-asm.ts | grep -v '^\s*\*' | grep -cE 'zpf_\|f_\|zpa_'` returns `0` — no restated prefix alternation.
    - The structural test asserts all three directions (real clean, planted five-prefix flagged, comment-only not flagged).
    - The prefix count parsed from `AUTO_NAME_PREFIX_RE.source` asserts to exactly `11`, and the test file contains no array literal listing the eleven names.
    - All eleven prefixed labels appear in the exported source and the export verifies `outcome === "ok"`.
    - The wrong-operand control is **observed**: paste `outcome`, the observed `exitStatus`, the rule that produced the verdict, and the diff offset if the run produced one, into the SUMMARY.
    - `cd src/mcp/vice && VICE_REQUIRE_ACME=1 node --test anno-export-asm.test.ts` exits 0 and `npm run test:automated` reports **0 failures**.
  </acceptance_criteria>
  <done>The exporter reads the eleven prefixes from their one home and a five-prefix copy is provably caught; enums render on the immediate operand only, with the wrong-operand case refused by the exporter and, at the source-text boundary, refused again — by the byte-diff or by 30-03's per-block `*` assertion, whichever fires first — with the observed exit status and the winning rule recorded rather than pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Every inexpressible opcode goes out as `!byte`, and real ACME confirms the store's duplicate-label refusal</name>
  <files>src/mcp/vice/anno-export-asm.ts, src/mcp/vice/anno-export-asm.test.ts</files>
  <read_first>
    - `src/mcp/vice/disasm-opcodes.ts:1-210` — the `OPCODES` table, the `acmeExpressible` / `illegal` flags, the 221/35 split, and the header's record of the **14 wrong entries** corrected by a real-ACME round trip
    - `src/mcp/vice/disasm-roundtrip.test.ts` — Suite C in full: how it lays out a byte image covering every opcode with correctly-sized operand filler so the linear decode stays aligned, and its never-hardcode-a-static-list rule at `:33-37`
    - `src/mcp/vice/disasm-renderer.ts:112-117` and `:246-253` — the fixed note vocabulary and the `!byte` substitution's exact rendered form
    - `src/mcp/vice/anno-store.ts:266` (the `name text not null unique` DDL on `anno_label`) and `:2820-2842` (`setLabel()` and its refusal wording, verbatim)
    - `.planning/phases/30-acme-export-and-the-real-acme-oracle/30-RESEARCH.md` § Common Pitfalls 9 (both measured ACME duplicate-symbol spellings) and § Architecture Patterns 2
    - `src/mcp/vice/acme-gate.test.ts:66-82` — the wording-read-from-source non-vacuity guard
  </read_first>
  <behavior>
    - An image laid out so that every one of the 256 opcodes decodes at a known address, exported as one code range, reassembles BYTE-IDENTICALLY through the real-ACME verdict.
    - For every `OPCODES` entry with `acmeExpressible: false`, the line at that address is a `!byte` directive carrying all of the instruction's bytes with the mnemonic and the note text in a trailing comment — asserted for all 35, iterated from the table.
    - No `acmeExpressible: false` entry's mnemonic appears as an ACME instruction anywhere in the exported source.
    - `unexpressibleCount` equals the number of `acmeExpressible: false` entries the layout covered.
    - `setLabel()` refuses a name already bound to a different address, and the refusal message matched in the test is read out of `anno-store.ts`'s own source rather than retyped.
    - With the duplicate reproduced at the source-text boundary — one symbol definition line duplicated with a different address in an otherwise valid export — real ACME exits **1** and its stderr carries `Symbol already defined.`
    - The paired direction: the same export without the duplication assembles and verifies `ok`.
  </behavior>
  <action>
**Part A — the 256-opcode export.**

In `anno-export-asm.test.ts`, add a table-driven suite modelled on `disasm-roundtrip.test.ts`'s
Suite C. Build ONE image whose bytes are, for each opcode value `0x00..0xff` in order, the opcode
byte followed by `OPCODES[b].length - 1` filler operand bytes chosen so nothing decodes as a
branch out of range. Record each opcode's start address as the layout is built. Write the image as
a `.prg` in a temp directory, build a store with ONE code range covering the whole payload, export
once, and verify once — one ACME invocation, not 256, so the file stays inside the 45-second
feedback budget.

Then iterate `OPCODES` itself (never a hand-written list) and assert per entry:

- for `acmeExpressible: false`: the source line at that entry's recorded address starts with the
  `!byte` directive, carries every one of the instruction's bytes, and carries the trailing comment
  with the mnemonic and the note text from `disasm-renderer.ts`'s fixed vocabulary;
- for `acmeExpressible: true`: the line is a mnemonic line, not a `!byte` directive;
- for every entry: the entry's mnemonic does not appear as an ACME instruction on any `!byte` line.

Assert the single verdict is `outcome === "ok"` with `byteDiff.equal === true`, and record in the
assertion message that this is the control that caught 14 wrong entries in this table's own
history.

Add a WHAT-NOT-TO-DO bullet to the test file's header naming the static-list prohibition, in the
same voice as `disasm-roundtrip.test.ts:33-37`.

**Part B — the duplicate-label oracle, both sides.**

Record the reading explicitly, in a comment above the tests, refining RESEARCH.md assumption A5:
`anno_label.name` carries a `unique` DDL constraint, so the store cannot hold two rows with one
name at all and a store-level plant cannot reach ACME. The ACME-side observation is therefore
produced at the **source-text boundary**, which is the only place the duplicate can exist. State
that this is a refinement of A5, not a departure from criterion 5 — criterion 5 asks for the
external oracle to confirm the internal one, and it does.

Tests:

1. **The internal refusal.** Using the `acme-gate.test.ts:66-82` technique, read
   `anno-store.ts` from disk and assert the refusal wording the test matches on really appears
   there (so a rewording cannot make the test pass for the wrong reason). Then call `setLabel()`
   with a name already bound to a different address and assert it throws with that wording. Assert
   the paired direction too: rebinding the SAME name to the SAME address is not refused, so a
   guard that refuses everything is distinguishable from one that works.
2. **The external oracle** (`{ skip: SKIP_REASON }`). Take a valid export, duplicate one symbol
   definition line with a different address, assemble it directly with the raw helper, and assert
   exit `1` and that the parsed diagnostics carry ACME's own `Symbol already defined.` message in
   the `--msvc` spelling. Assert the un-duplicated source verifies `ok` in the same test, so the
   red is one line of difference and nothing else.
  </action>
  <verify>
    <automated>cd src/mcp/vice && set -o pipefail && VICE_REQUIRE_ACME=1 node --test anno-export-asm.test.ts && npm run test:automated 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - The 256-opcode export verifies `outcome === "ok"` with `byteDiff.equal === true` in a single ACME invocation, and `node --test anno-export-asm.test.ts` completes in under 60 seconds.
    - The per-entry assertions iterate `OPCODES` — source assertion: `grep -v '^\s*//' src/mcp/vice/anno-export-asm.test.ts | grep -c 'OPCODES'` returns at least `2`, and the file contains no array literal enumerating unassemblable mnemonics.
    - `unexpressibleCount` for the 256-opcode export equals the count of `acmeExpressible: false` entries computed from the table in the same test (expected 35 against the current table, but computed, never pinned).
    - The duplicate-symbol observation is **real**: paste ACME's exit status and its verbatim stderr line into the SUMMARY.
    - The store refusal test matches a wording string that a source read proves is present in `anno-store.ts`.
    - `cd src/mcp/vice && VICE_REQUIRE_ACME=1 node --test anno-export-asm.test.ts` exits 0 and `npm run test:automated` reports **0 failures**.
  </acceptance_criteria>
  <done>Every opcode ACME cannot express leaves as `!byte` with a naming comment and the whole 256-opcode export reassembles byte-identically; the store's duplicate-label refusal is confirmed by real ACME reporting the same duplicate at the source-text boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store label / enum-variant names → generated ACME source | Stored names become assembler symbols |
| image bytes → decoded instructions → directives | Untrusted byte content drives the decoder and the emitter |
| committed fixture bytes → test assertions | A fixture that stops proving its property degrades every test over it |
| ACME stderr → duplicate-symbol confirmation | Untrusted-shaped text confirms an internal refusal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-05 | Tampering | label and enum-variant names → ACME source | high | mitigate | Every emitted name, including generated `<enum>_<VARIANT>` names, passes `assertLegalAcmeIdentifier()` — REJECT, never sanitise |
| T-30-11 | Spoofing | a length-changing substitution | high | mitigate | The zero-page mid-instruction refusal (Task 1) plus the per-block `*` assertions from plan 30-03, with the byte-diff as the undeniable control |
| T-30-12 | Spoofing | an enum on the wrong operand | high | mitigate | The exporter refuses a non-immediate operand by name; the wrong-operand emission is observed being REFUSED at the source-text boundary — by the byte-diff, or by 30-03's per-block `*` assertion if that fires first — with the observed exit status recorded |
| T-30-13 | Repudiation | a fixture that stops self-modifying | medium | mitigate | The fixture-integrity test proves the property FROM THE BYTES, and the regenerator-agreement test pins `smc.a` to `smc.prg` |
| T-30-14 | Spoofing | a vacuous structural scan | high | mitigate | The prefix scan has one shared predicate and three directions, including the comment-only control that stops it degrading into a substring search |
| T-30-07 | Denial of Service | temp dirs on RAM-backed `/tmp` | low | mitigate | One `mkdtempSync` per test file, removed with `rmSync(..., { recursive: true, force: true })` in an `after()` |
| T-30-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no packages; RESEARCH.md § Package Legitimacy Audit records "not applicable" |
</threat_model>

<verification>
- `cd src/mcp/vice && npm run typecheck` exits 0
- `cd src/mcp/vice && VICE_REQUIRE_ACME=1 node --test anno-export-asm.test.ts` exits 0
- `cd src/mcp/vice && npm run test:automated` reports 0 failures (stop any running VICE broker first)
- `node src/mcp/vice/fixtures/export-asm/make-export-asm-fixtures.mjs` reproduces `smc.prg` byte-identically
</verification>

<success_criteria>
- The `=*+$01` idiom is load-bearing: proved on a fixture that genuinely self-modifies, with the wrong placement observed failing at exit 0
- Eleven prefixes, read from `AUTO_NAME_PREFIX_RE`, with a five-prefix copy provably caught
- Enums on the immediate operand only, with the wrong-operand case refused and independently caught
- Every `acmeExpressible: false` opcode exports as `!byte` and the 256-opcode export reassembles byte-identically
- Real ACME reports the duplicate symbol the store already refuses
</success_criteria>

<output>
Create `.planning/phases/30-acme-export-and-the-real-acme-oracle/30-04-SUMMARY.md` when done.
Record the raw negative-control results (SMC placement, enum wrong-operand) and ACME's verbatim
duplicate-symbol line.
</output>
