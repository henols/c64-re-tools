---
phase: 30-acme-export-and-the-real-acme-oracle
plan: 05
type: execute
wave: 4
depends_on: ["30-03", "30-04"]
files_modified:
  - src/mcp/vice/anno-cli.ts
  - src/mcp/vice/anno-cli.test.ts
  - src/mcp/vice/anno-cli-path-consumers.test.ts
  - src/mcp/vice/anno-verb-coverage.test.ts
  - src/mcp/vice/package.json
  - scripts/lib/anno-cli-verbs.mjs
autonomous: true
requirements: [EXPORT-01]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`anno export-asm <image> --store FILE [--out FILE] [--force]` writes ACME source from an existing annotation store and an existing image, and exits 0."
    - "Every caller-supplied path on the new verb — `<image>`, `--store` and `--out` — goes through `storePathWithinWorkspace()`, and the REALPATH it returns is what reaches the filesystem; the raw caller string is dead from that line on."
    - "Confinement happens BEFORE any filesystem probe, `existsSync` included, because a stat is itself an oracle; and the derived `--out` default is applied FIRST and confined AFTER, so a path this verb computed is confined by the same rule as one a caller supplied."
    - "A path argument outside the workspace root is refused with one line and exit 1 — no file is created, replaced or read outside the root."
    - "The verb refuses to overwrite an existing `--out` unless `--force` is passed, checked against the CONFINED path."
    - "The CLI no longer describes itself as having exactly two verbs: the header block, the `--help` synopsis and the unknown-verb message all name three."
    - "`ANNO_CLI_VERB_FLOOR` reads 3 and `anno-verb-coverage.test.ts`'s exact equality assertion reads 3, moved in the same commit."
    - "`CLI_PATH_ARGUMENT_FLOOR` reads the new measured true count as a hand-pinned integer literal, never derived from `CLI_PATH_ARGUMENTS.length` or from disk."
    - "`anno-export-asm.ts` is listed in `package.json`'s `files[]` in the same commit that makes it reachable from `vice-proxy.ts` through `anno-cli.ts`, and `acme-verify.ts` is NOT listed."
    - "The verb prints nothing that reads as an assembly verification: it writes source and says so, because USER-D-02 keeps the ACME oracle test-only and this command never invokes an assembler."
    - "Running the verb twice over an unchanged store and image writes byte-identical output; the second run is refused without `--force` and produces the same bytes with it."
  artifacts:
    - "src/mcp/vice/anno-cli.ts — the `export-asm` verb, five coordinated parts"
    - "scripts/lib/anno-cli-verbs.mjs — ANNO_CLI_VERB_FLOOR raised to 3, doc-comment corrected"
    - "src/mcp/vice/package.json — files[] gains anno-export-asm.ts"
  key_links:
    - "anno-cli-path-consumers.test.ts derives the flag half from VERB_OPTIONS and the positional half from the two-space-indented --help synopsis line, in BOTH directions — three edits must land together or it reds by name"
    - "checkAcceptedOptions() enforces the closed option set for every verb from VERB_OPTIONS; a flag listed but unread is the defect that map exists against"
    - "check-npm-packages.mjs walks the static and dynamic import closure from vice-proxy.ts and fails the pack when a reachable module is missing from files[]"
  prohibitions:
    - "A skipped assembler must never be presented to a human as a pass on any surface — and neither must an unverified export: this verb writes source and must not imply it was assembled."
    - "An annotation the exporter cannot express must be refused loudly and by name, never silently dropped while the command reports success."
    - "No verb, flag or capability outside USER-D-01's locked scope may be absorbed — `gen-enums`, `export-lbl` and `import-lbl` do not land here."
    - "A dated withdrawal notice must be re-pointed or discharged with evidence, never deleted."
---

<objective>
Land `anno export-asm` as a CLI verb — the shape RESEARCH.md recommends and D30-02 adopted —
as **one atomic change** covering every part the pattern map found moves together: the verb
itself in five coordinated pieces, the path-argument inventory and its hand-pinned floor, the
verb floor on both sides of an exact equality, the `files[]` closure, and the CLI's own
self-description, which currently says it has exactly two verbs and names both.

Purpose: `anno export-asm` is named as withdrawn-returning in six shipped files across two
trees, and `scripts/check-skill-fork-honesty.mjs:533` asserts one of them contains that exact
literal. The verb has to exist before those notices can be discharged, and every guard pinned to
the two-verb surface has to move in the commit that makes it three.

Output: a working, confined, non-overwriting CLI verb whose own guards were all raised in the
same commit, and which says plainly that it has not assembled anything.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/30-acme-export-and-the-real-acme-oracle/30-RESEARCH.md
@.planning/phases/30-acme-export-and-the-real-acme-oracle/30-PATTERNS.md
@.planning/phases/30-acme-export-and-the-real-acme-oracle/30-04-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols introduced by **this plan**:

| Kind | Name | File |
|---|---|---|
| CLI verb | `anno export-asm` | `anno-cli.ts` |
| CLI positional | `<image>` (on `export-asm`) | `anno-cli.ts` |
| CLI flags | `--store FILE`, `--out FILE`, `--force` (on `export-asm`) | `anno-cli.ts` |
| function | `cmdExportAsm(rest: string[]): Promise<number>` (module-private) | `anno-cli.ts` |
| function | `parseExportAsmArgs(rest: string[])` (module-private) | `anno-cli.ts` |
| `VERB_OPTIONS` key | `"export-asm"` | `anno-cli.ts` |
| constant value | `ANNO_CLI_VERB_FLOOR = 3` | `scripts/lib/anno-cli-verbs.mjs` |
| constant value | `CLI_PATH_ARGUMENT_FLOOR` raised to the new measured count | `anno-cli-path-consumers.test.ts` |
| `files[]` entry | `anno-export-asm.ts` | `src/mcp/vice/package.json` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The `export-asm` verb — all five coordinated parts of anno-cli.ts in one change</name>
  <files>src/mcp/vice/anno-cli.ts</files>
  <read_first>
    - `src/mcp/vice/anno-cli.ts` — the FULL file, and specifically: the header at `:1-120` including the TWO VERBS block at `:10-30` and the never-use-the-RAW-caller-string rule at `:83-87`; `USAGE` at `:130-200`; `VERB_OPTIONS` at `:222-225` and `checkAcceptedOptions()` below it; `parseRenderMemmapArgs()` at `:301-338`; `cmdRenderMemmap()` at `:419-524` including the confinement chain at `:419-471`, the CR-02 derived-default comment, and `refuseOverwrite()` usage at `:498`; the dispatch switch and its `default:` case at `:1055-1090`
    - `src/mcp/vice/anno-types.ts:1192-1203` — `storePathWithinWorkspace()` and what it returns
    - `src/mcp/vice/anno-export-asm.ts` — `ExportAsmOptions`, `ExportAsmResult` and `exportAsm()` as plans 30-01/30-03/30-04 left them
    - `.planning/phases/30-acme-export-and-the-real-acme-oracle/30-PATTERNS.md` § `src/mcp/vice/anno-cli.ts` — all five parts with their code excerpts, and the `default:`-message warning
    - `.planning/phases/30-acme-export-and-the-real-acme-oracle/30-RESEARCH.md` § Security Domain — the three live escapes Phase 29 found on exactly this shape
  </read_first>
  <behavior>
    - `anno export-asm <image> --store FILE` writes ACME source to `<image-stem>.a` in the STORE's directory and exits 0, printing one summary line.
    - `--out FILE` overrides the destination; the destination is confined whether it was supplied or derived.
    - An existing destination is refused without `--force` and replaced with it.
    - A `--store`, `<image>` or `--out` path resolving outside the workspace root is refused with one line and exit 1, and nothing is read or written at that path.
    - A missing `--store` is refused by name; `--store` with no value, or with a flag-shaped value, is refused by its own missing-value message.
    - An unknown `--`-shaped token is refused by `checkAcceptedOptions()` before the verb runs.
    - A nonexistent store is refused rather than created; a nonexistent image is refused.
    - `anno badverb` prints an unknown-verb message naming **three** verbs.
    - The summary line states that the output has not been assembled, and no word in the verb's output implies verification.
  </behavior>
  <action>
Add `export-asm` to `anno-cli.ts` as one coherent change. All five parts, plus the two
self-description repairs, land together.

**1. `VERB_OPTIONS`.** Add `"export-asm": ["--store", "--out", "--force"]`. Only options the
verb's own code actually reads — a flag listed but unread is the defect that map exists against.

**2. The `--help` synopsis.** Add a block to `USAGE`, beginning at exactly **two** leading spaces
(the inventory test's regex anchors on `^ {2}<verb>\b`), reading:
`export-asm <image> --store FILE [--out FILE] [--force]`
followed by an indented description that states, in the file's existing voice:
`<image>` supplies the payload bytes and the load origin while `--store` names the annotation
store holding the ranges, labels, comments and enums — two separate files on purpose, and neither
is derived from the other; the default `--out` is the image's basename with a `.a` extension, in
the STORE's own directory, and that derived default goes through the same confinement seam as a
caller-supplied one; an existing destination is refused unless `--force` is passed; the verb
requires an EXISTING store and an EXISTING image and creates neither. Add one explicit sentence
naming what this verb does **not** do: it writes ACME source and does not assemble it. The
correctness claim for this output is settled by the phase's real-ACME byte-diff oracle in the test
suite, which is deliberately test-only, so nothing this command prints may read as a verification
result.

**3. `parseExportAsmArgs(rest)`.** Copy `parseRenderMemmapArgs()`'s closed-set shape exactly:
`--store` and `--out` each take a value and set their own `*MissingValue` field when the value is
absent or itself flag-shaped; `--force` is a boolean; any other `--`-shaped token sets
`unknownOption ??= a`; anything else joins `positional`. Refuse when `positional.length !== 1` and
when `--store` is absent, each with its own named message.

**4. `cmdExportAsm(rest)`.** Follow `cmdRenderMemmap()`'s chain, in this order and for the reasons
its own comments give:
- `const workspaceRoot = repoRoot();`
- Confine `<image>` and `--store` through `storePathWithinWorkspace()` **before** any `existsSync`
  probe — a stat is itself an oracle — each inside its own `try`/`catch` printing
  `export-asm: ${errMsg(err)}` and returning 1.
- Then `existsSync` each confined path, refusing a missing store with a message in the
  render-memmap voice (refusing to CREATE one, because "the annotations are gone" and "there are
  no annotations" must not read the same) and refusing a missing image by name.
- Apply the `--out` default FIRST — the image's basename with its extension replaced by `.a`,
  joined onto `dirname(storePath)` — and confine the **result**, so the derived path and a
  caller-supplied one are confined by the same rule (CR-02).
- Use the realpaths the seam returned from that point on. The raw caller strings are dead.
- `refuseOverwrite(outPath, force, "export-asm")` on the writing branch, against the CONFINED
  path.
- Call `exportAsm({ storePath, imagePath, workspaceRoot })`, then `writeFileSync(outPath, result.source)`
  inside a `try` printing `export-asm: could not write ${outPath}: ${errMsg(err)}` and returning 1
  rather than throwing.
- Print one summary line naming the confined `outPath` and the result's counts — blocks, symbols,
  auto-named symbols, unexpressible instructions, mid-instruction labels, enum substitutions —
  followed by a second line stating plainly that the file has not been assembled by this command.

**5. Dispatch.** Add `case "export-asm": return await cmdExportAsm(rest);` and rewrite the
`default:` case's message so it names **three** verbs. That string currently hardcodes a
two-verb claim and names both; leaving it makes the CLI misdescribe itself.

**Self-description repair, same commit.** Rewrite the header block at `:10-30` that reads as a
two-verb declaration. It must now record: the surface is three verbs; `export-asm` returned in
this phase as a rebuild over the annotation store behind a real-ACME byte-diff oracle, not as
restored code; and — per USER-D-01 — `gen-enums`, `export-lbl` and `import-lbl` did **not** return
with it and are not owned by this phase. Do not delete the paragraph recording what went and why;
edit it so it stays true. Leave the exact fate of those three verbs to plan 30-06, which owns the
tree-wide re-pointing, and say so in one line so the two edits do not contradict each other.

Do not import `acme-verify.ts` here — it is test-only (USER-D-02) and a shipped module importing
it would drag it into the `files[]` closure.
  </action>
  <verify>
    <automated>cd src/mcp/vice && set -o pipefail && npm run typecheck && node --test anno-cli.test.ts && npm run test:automated 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `node src/mcp/vice/vice-proxy.ts anno export-asm --help` (or the equivalent `anno` entry) prints a synopsis line for `export-asm` beginning at exactly two spaces.
    - End-to-end: with a store and a `.prg` inside the repo, `anno export-asm <image> --store <store>` exits 0 and writes a file whose first line is `!cpu 6510`.
    - Re-running the same command without `--force` exits non-zero and does not modify the file; with `--force` it exits 0 and writes byte-identical bytes.
    - A `--store` path pointing outside the workspace root exits 1 with a one-line refusal, and `ls` shows no file created at that path.
    - `--store` with no value exits 1 with its own missing-value message; `--nonsense` exits 1 through `checkAcceptedOptions()`.
    - Source assertion: `grep -v '^\s*//' src/mcp/vice/anno-cli.ts | grep -v '^\s*\*' | grep -c 'storePathWithinWorkspace('` returns at least `9` — the six pre-existing call sites plus three new ones.
    - Source assertion: `grep -c 'this CLI has exactly two' src/mcp/vice/anno-cli.ts` returns `0`.
    - Source assertion: `grep -v '^\s*//' src/mcp/vice/anno-cli.ts | grep -v '^\s*\*' | grep -c 'acme-verify'` returns `0`.
    - The verb's printed output contains no word implying assembly verification — check the two printed lines by eye against the prohibition and record the exact strings in the SUMMARY.
    - `cd src/mcp/vice && npm run typecheck` exits 0; `npm run test:automated` reports **0 failures**.
  </acceptance_criteria>
  <done>`anno export-asm` exists, confines all three of its paths through the one seam and uses the realpaths, refuses an overwrite without `--force`, names three verbs everywhere the CLI describes itself, and states that it has not assembled its own output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Raise every floor the new verb moves, in the same commit</name>
  <files>src/mcp/vice/anno-cli-path-consumers.test.ts, src/mcp/vice/anno-verb-coverage.test.ts, scripts/lib/anno-cli-verbs.mjs</files>
  <read_first>
    - `src/mcp/vice/anno-cli-path-consumers.test.ts` — `CLI_PATH_ARGUMENTS` at `:161-212`, `NON_PATH_OPTIONS` at `:195`, `CLI_PATH_ARGUMENT_FLOOR` at `:211` and the never-derive-it rule at `:66-72`, the every-`VERB_OPTIONS`-key-is-covered assertion at `:245-262`, the stale-exclusion assertion at `:264+`, and the synopsis parser at `:365-385`
    - `scripts/lib/anno-cli-verbs.mjs` — the full file: the header at `:16`, the floor's doc paragraph at `:47-70`, and `ANNO_CLI_VERB_FLOOR` at `:73`
    - `src/mcp/vice/anno-verb-coverage.test.ts` — `REAL_VERBS` at `:46`, its doc at `:40-45`, and the exact-equality floor assertion at `:177-181`
    - `.planning/phases/30-acme-export-and-the-real-acme-oracle/30-PATTERNS.md` § Shared Patterns 3 and 4
  </read_first>
  <behavior>
    - `CLI_PATH_ARGUMENTS` gains three rows: `{ verb: "export-asm", argument: "<image>", kind: "positional" }`, `{ verb: "export-asm", argument: "--store", kind: "flag" }`, `{ verb: "export-asm", argument: "--out", kind: "flag" }`.
    - `CLI_PATH_ARGUMENT_FLOOR` reads `9` as a hand-pinned integer literal.
    - The inventory test's both-directions assertion passes: every `VERB_OPTIONS` key is covered, and no inventory row names an argument the surface does not accept.
    - `--force` needs no new entry — it is already in `NON_PATH_OPTIONS`, and the stale-exclusion assertion still passes because `export-asm` accepts it.
    - `ANNO_CLI_VERB_FLOOR` reads `3` and `assert.equal(ANNO_CLI_VERB_FLOOR, 3)` reads `3`; raising only one side reds.
    - `REAL_VERBS` in `anno-verb-coverage.test.ts` gains `"export-asm"` and the parsed verb list from `anno-cli.ts` matches it.
    - `node scripts/check-skill-tool-coverage.mjs` still exits 0 with `OK` in stdout.
  </behavior>
  <action>
Three coordinated edits, all in this task so none can land alone.

**A. `anno-cli-path-consumers.test.ts`.** Add the three `CLI_PATH_ARGUMENTS` rows above and raise
`CLI_PATH_ARGUMENT_FLOOR` from 6 to the new measured true count. Keep it a **hand-pinned integer
literal** — the file's own rule at `:66-72` says deriving it from `CLI_PATH_ARGUMENTS.length` or
from disk would make it unfailable. Do **not** widen `NON_PATH_OPTIONS` to silence anything; if
something reds, the fix is in the verb, not in the exclusion list.

**B. `scripts/lib/anno-cli-verbs.mjs`.** Raise `ANNO_CLI_VERB_FLOOR` from 2 to 3. Then correct the
doc paragraph that raises it — it currently records the previous floor of 8, names the six removed
verbs, and states that three of them "return in **Phase 30** as rebuilds". Per **USER-D-01** that
sentence becomes false the moment this phase closes. Rewrite it so it records what actually
happened: `export-asm` returned in this phase, rebuilt over the annotation store behind a
real-ACME byte-diff oracle, which is what raises this floor from 2 to 3; and `gen-enums`,
`export-lbl` and `import-lbl` did **not** return with it — no Phase 30 requirement or success
criterion covers any of them, and **no phase currently owns them**. Say that in those words rather
than inventing a later phase number. Keep the paragraph's original argument (a replacement over a
new verb set, not a lowering) and its per-verb-raise obligation intact — only the false forecast
moves. Also fix the header line at `:16`, which carries the same forecast in shorter form.

**C. `anno-verb-coverage.test.ts`.** Add `"export-asm"` to `REAL_VERBS`, change
`assert.equal(ANNO_CLI_VERB_FLOOR, 2)` to `3`, and update the doc paragraph at `:40-45` that
points at "the Phase 30 event that raises it again" — that event has now happened, so the comment
records it in the past tense and names this phase's plan. Both the constant and its exact-equality
assertion move in this same commit; the equality is `assert.equal`, not `>=`, so raising one side
alone reds.

Before committing, re-run the two guards that scan shipped modules and will now see
`anno-export-asm.ts` for the first time once Task 3 lands it in `files[]`, so a comment written in
plans 30-01/30-03/30-04 cannot red a guard at the wave boundary.
  </action>
  <verify>
    <automated>cd src/mcp/vice && set -o pipefail && node --test anno-cli-path-consumers.test.ts anno-verb-coverage.test.ts && node ../../../scripts/check-skill-tool-coverage.mjs && npm run test:automated 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export const ANNO_CLI_VERB_FLOOR = 3;' scripts/lib/anno-cli-verbs.mjs` returns `1`.
    - `grep -c 'assert.equal(ANNO_CLI_VERB_FLOOR, 3)' src/mcp/vice/anno-verb-coverage.test.ts` returns `1`.
    - `grep -cE 'CLI_PATH_ARGUMENT_FLOOR = [0-9]+;' src/mcp/vice/anno-cli-path-consumers.test.ts` returns `1` and the pinned value equals the new true count; the pinned value is a bare integer literal, not an expression derived from the inventory array's own length or from disk (read the line and paste it into the SUMMARY).
    - `grep -v '^\s*//' scripts/lib/anno-cli-verbs.mjs | grep -v '^\s*\*' | grep -c 'Phase 30'` returns `0`, and the unfiltered file contains no forecast that any verb returns in a numbered phase (read the rewritten paragraph by hand and paste it into the SUMMARY).
    - `cd src/mcp/vice && node --test anno-cli-path-consumers.test.ts anno-verb-coverage.test.ts` exits 0.
    - `node scripts/check-skill-tool-coverage.mjs` exits 0 and prints `OK`.
    - Non-vacuity, **observed once and reverted**: with `ANNO_CLI_VERB_FLOOR` left at 2, `anno-verb-coverage.test.ts` fails. Record the failure text in the SUMMARY.
    - `cd src/mcp/vice && npm run test:automated` reports **0 failures**.
  </acceptance_criteria>
  <done>Every floor the third verb moves is raised on both of its sides in one commit, the path-argument inventory covers all three new arguments, and the false "returns in Phase 30" forecast in the floor's own doc is replaced by what happened.</done>
</task>

<task type="auto">
  <name>Task 3: Ship the exporter — files[] closure, packaging guards, and the CLI end-to-end test</name>
  <files>src/mcp/vice/package.json, src/mcp/vice/anno-cli.test.ts</files>
  <read_first>
    - `src/mcp/vice/package.json` — the `files[]` array in full, noting that every module the exporter imports is already listed
    - `scripts/check-npm-packages.mjs:230-270` — the closure walk and what it fails on
    - `src/mcp/vice/acme-gate.test.ts` — the assertion that `acme-gate.ts` is ABSENT from `files[]`, which is the mirror this plan must add for `acme-verify.ts`
    - `src/mcp/vice/anno-cli.test.ts` — how existing verbs are driven end to end in this file, and its temp-directory discipline
    - `src/mcp/vice/shipped-modules.ts` — `shippedTsModules()` / `codeOnly()`, the one home for the scanned module set
    - `src/mcp/vice/comment-phase-pointers.test.ts:234-300` — the seven assignment-shape pattern families, so a comment written in an earlier plan cannot trip one
    - `src/mcp/vice/docs-dangling-refs.test.ts` — FLOW-02's literal-only scope
  </read_first>
  <behavior>
    - `package.json`'s `files[]` contains `anno-export-asm.ts` and does not contain `acme-verify.ts`.
    - `node scripts/check-npm-packages.mjs` exits 0 — the closure walk from `vice-proxy.ts` reaches `anno-export-asm.ts` through `anno-cli.ts` and finds it listed.
    - A new test asserts `acme-verify.ts` is ABSENT from `files[]`, mirroring the existing assertion for `acme-gate.ts`, with a message naming USER-D-02's reason.
    - `comment-phase-pointers.test.ts` and `docs-dangling-refs.test.ts` both pass with `anno-export-asm.ts` newly inside their scanned set.
    - An end-to-end CLI test drives `export-asm` in a temp directory and asserts the written file's first line and the summary line's shape.
  </behavior>
  <action>
**A. `package.json`.** Add `"anno-export-asm.ts"` to `files[]`, placed beside the other `anno-*`
entries. Do **not** add `acme-verify.ts` — USER-D-02 keeps it test-only, which is what leaves
`spawn-seam.test.ts`'s scan set and `check-npm-packages.mjs`'s closure walk untouched and avoids a
second copy of the `ACME_BIN` env-var name.

**B. The absence assertion.** Add a test in `anno-cli.test.ts` (or beside the existing one in
`acme-verify.test.ts`, whichever keeps the assertion next to its subject) that reads `package.json`
and asserts `acme-verify.ts` is absent from `files[]`, with a failure message naming the reason:
a shipped verify module would have to resolve `ACME_BIN` itself rather than import the gate, which
`acme-gate.ts` forbids by name. Mirror the wording style of the existing `acme-gate.ts` absence
assertion rather than inventing a new one.

**C. The guards that newly see the exporter.** Run `comment-phase-pointers.test.ts` and
`docs-dangling-refs.test.ts` now that `anno-export-asm.ts` is in `files[]` and therefore in
`shippedTsModules()`. If either reds on a comment written in an earlier plan of this phase, fix
the **comment**, not the guard: the assignment-shape families flag a comment that hands pending or
future work to a numbered phase, and a shipped module's comment must narrate rather than assign.
Record any comment you had to reword in the SUMMARY, with the family that flagged it.

**D. The end-to-end CLI test.** In `anno-cli.test.ts`, add a test that builds a store and a `.prg`
inside a `mkdtempSync` directory **under the repo root** (so the confinement seam accepts it),
invokes the verb through the same entry the other verbs' tests use, and asserts: exit 0; the
output file exists at the derived default path beside the store; its first line is `!cpu 6510`;
the summary line names the confined output path; and the second printed line states the file was
not assembled. Add the refusal cases as their own named tests: a path outside the workspace root,
a missing store, an existing destination without `--force`. Remove the temp directory with
`rmSync(..., { recursive: true, force: true })` in a `finally`.
  </action>
  <verify>
    <automated>cd src/mcp/vice && set -o pipefail && node --test anno-cli.test.ts && node --test comment-phase-pointers.test.ts docs-dangling-refs.test.ts && node ../../../scripts/check-npm-packages.mjs && npm run test:automated 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const f=require('./src/mcp/vice/package.json').files; process.exit(f.includes('anno-export-asm.ts') && !f.includes('acme-verify.ts') ? 0 : 1)"` exits 0.
    - `node scripts/check-npm-packages.mjs` exits 0.
    - `cd src/mcp/vice && node --test comment-phase-pointers.test.ts docs-dangling-refs.test.ts` exits 0.
    - The end-to-end CLI test asserts the written file's first line is exactly `!cpu 6510` and asserts on the summary line's text.
    - The three refusal cases each exit non-zero with their own named message, asserted individually.
    - `cd src/mcp/vice && npm run test:automated` reports **0 failures**, and `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>The exporter ships, the verify module deliberately does not and a test proves it, the packaging closure walk passes, and the two guards that newly scan the exporter are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → filesystem | `<image>`, `--store` and `--out` are caller-supplied paths |
| derived default path → filesystem | A path this verb computes still reaches `writeFileSync` |
| store/image content → error messages | A parse failure must not quote the file's own bytes |
| `files[]` closure → published tarball | A reachable module missing from `files[]` breaks the pack; a test-only module present in it changes which guards fire |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-02 | Tampering | `export-asm --out` | high | mitigate | `storePathWithinWorkspace()` on the derived default and on any caller-supplied value, applied AFTER the default and BEFORE any write; `refuseOverwrite()` against the confined path. Phase 29 found three live escapes of exactly this shape, one silently replacing a pre-existing file outside the workspace root at exit 0 |
| T-30-15 | Information Disclosure | `export-asm <image>` / `--store` | high | mitigate | Both confined BEFORE any `existsSync` probe, because a stat is itself an oracle; refusal messages name the path and never the file's content |
| T-30-03 | Information Disclosure | error messages | medium | mitigate | Never interpolate a read file's own bytes or an underlying parser message (CR-03) |
| T-30-16 | Spoofing | an unverified export presented as verified | high | mitigate | The verb prints an explicit not-assembled line and no verification-shaped wording; an acceptance criterion checks the exact printed strings |
| T-30-17 | Tampering | a new path argument bypassing the inventory | medium | mitigate | `anno-cli-path-consumers.test.ts` derives the flag half from `VERB_OPTIONS` and the positional half from the synopsis, in both directions, with a hand-pinned floor raised in the same commit |
| T-30-07 | Denial of Service | temp dirs on RAM-backed `/tmp` | low | mitigate | `rmSync(..., { recursive: true, force: true })` in a `finally` in the CLI test |
| T-30-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no packages; it only edits `files[]`, which lists modules already in this repository. RESEARCH.md § Package Legitimacy Audit records "not applicable" |
</threat_model>

<verification>
- `cd src/mcp/vice && npm run typecheck` exits 0
- `cd src/mcp/vice && npm run test:automated` reports 0 failures (stop any running VICE broker first)
- `node scripts/check-npm-packages.mjs` exits 0
- `node scripts/check-skill-tool-coverage.mjs` exits 0
- `node scripts/check-skill-cli-invocations.mjs` exits 0
</verification>

<success_criteria>
- `anno export-asm` works end to end, confines all three paths through the one seam, and refuses an overwrite
- Every floor the third verb moves is raised on both sides in one commit, and the verb-floor red was observed before the fix
- The exporter is in `files[]`, the verify module is provably not, and the packaging walk passes
- Nothing the verb prints reads as a verification result
</success_criteria>

<output>
Create `.planning/phases/30-acme-export-and-the-real-acme-oracle/30-05-SUMMARY.md` when done.
Record the verb's exact printed lines, the observed verb-floor failure text, and any comment
reworded to satisfy `comment-phase-pointers.test.ts`.
</output>
