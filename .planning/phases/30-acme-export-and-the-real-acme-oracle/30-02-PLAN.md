---
phase: 30-acme-export-and-the-real-acme-oracle
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - src/mcp/vice/acme-verify.ts
  - src/mcp/vice/acme-verify.test.ts
  - .planning/phases/30-acme-export-and-the-real-acme-oracle/fixtures/README.md
  - .planning/phases/30-acme-export-and-the-real-acme-oracle/fixtures/verify-honest-pass.txt
  - .planning/phases/30-acme-export-and-the-real-acme-oracle/fixtures/verify-false-pass-trap.txt
autonomous: true
requirements: [EXPORT-01, EXPORT-03]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "MANDATORY RED 1: with `ACME_BIN` pointed at a path that does not exist, export verification reports skipped-or-failed and never a pass — observed in a child process, because `ACME_BIN` and `ACME_AVAILABLE` are module-load constants."
    - "MANDATORY RED 1, non-vacuity control: the same shared predicate, driven with the historical truthiness-of-exit-status classifier instead of the real one, reports the missing assembler as a pass — so the guard is provably able to bite."
    - "MANDATORY RED 1, paired direction: the same child run with `VICE_REQUIRE_ACME` DELETED from the environment (not set to an empty string) exits zero — without it, a non-zero exit cannot be distinguished from a broken harness."
    - "MANDATORY RED 2: a deliberately corrupted export byte makes the byte-diff fail while ACME itself still exits 0 — `outcome === \"failed\"`, `byteDiff.equal === false`, and `exitStatus === 0` in the same recorded result."
    - "Running the verify primitive twice on the same input yields the same outcome: a failing second run reports no-output-file rather than reading the first run's bytes, and real ACME's measured stale-output behaviour is pinned by its own reproduction on this host (RESEARCH.md Pitfall 1)."
    - "Two verify invocations cannot share an output path; the temp directory is removed in a `finally` on the failure path too, so an interrupted run leaves nothing a later verdict can read."
    - "Each of the five verdict rules is exercised by its own named test against its own named pure helper, rather than only through the end-to-end path."
    - "The byte-diff's equality is octet equality via `Buffer.compare`, and `firstDifferingOffset` is a byte offset, not a character index."
    - "Both pinned transcripts are re-recorded from real ACME 0.97 output produced by THIS phase's producer, and neither is byte-equal to its Phase 29 counterpart."
    - "No test in this phase reads the Phase 29 fixtures directory."
  artifacts:
    - ".planning/phases/30-acme-export-and-the-real-acme-oracle/fixtures/verify-honest-pass.txt — re-recorded honest pass"
    - ".planning/phases/30-acme-export-and-the-real-acme-oracle/fixtures/verify-false-pass-trap.txt — the trap provoked against the NEW route"
    - ".planning/phases/30-acme-export-and-the-real-acme-oracle/fixtures/README.md — provenance (ACME 0.97 \"Zem\", this host, capture date) and the read-for-shape-only statement"
  key_links:
    - "acme-verify.test.ts's child harness deletes NODE_TEST_CONTEXT before spawning node --test, or the child refuses to run any file and exits ZERO"
    - "the child harness spawns process.execPath, never a bare node binary name (WR-20)"
    - "the shared predicate is one function called by both the real path and the planted control — the 11-01 discipline"
  prohibitions:
    - "The oracle must never be satisfiable by the exporter's own output — a verdict derived from re-rendering, re-parsing or substring-matching the exported text is a self-check wearing an oracle's clothes."
    - "A skipped assembler must never be presented to a human as a pass on any surface — not in a return value, not in a printed summary line, not in an exit code."
    - "A mandatory red must be OBSERVED, not asserted from a plan's own reading: the red run's actual output belongs in the SUMMARY, and a red that was never seen fail is not evidence."
    - "A withdrawal notice or a carried obligation must be discharged with evidence, never deleted."
---

<objective>
Earn EXPORT-01's **two mandatory reds against the new producer**, prove each of the five
verdict rules independently, and re-record both pinned transcripts from real ACME 0.97
output.

Purpose: the previous evidence for both directions was earned against a producer this
milestone deleted, and the ROADMAP is explicit that it does not transfer. This plan is where
the phase's central claim — *the verdict is a byte-diff, never an exit code, and a skipped
assembler is a third outcome* — stops being a design statement and becomes an observed one.

Output: a hardened `acme-verify.ts` whose five rules are named pure helpers, an
`acme-verify.test.ts` carrying both reds with their non-vacuity controls, and a fixtures
directory whose transcripts came out of a real assembler this week.
</objective>

<scope_disposition>
**Advisory, recorded rather than acted on.** `estimate.tokens` of 105000 is 1.05x the
100000-token smart-zone budget, on `confidence: low` with a sample count of 0 — an uncalibrated
projection. Structurally the plan is 3 tasks over 5 files with one concern. **No re-slice.** The
full plan-set disposition is recorded once, in `30-01-PLAN.md` § `<scope_disposition>`.
</scope_disposition>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/30-acme-export-and-the-real-acme-oracle/30-RESEARCH.md
@.planning/phases/30-acme-export-and-the-real-acme-oracle/30-PATTERNS.md
@.planning/phases/30-acme-export-and-the-real-acme-oracle/30-VALIDATION.md
@.planning/phases/30-acme-export-and-the-real-acme-oracle/30-01-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols introduced by **this plan**:

| Kind | Name | File |
|---|---|---|
| type | `SpawnClassification` = `"unavailable" \| "ran"` | `acme-verify.ts` |
| type | `SpawnClassifier` | `acme-verify.ts` |
| function | `classifySpawn(r): SpawnClassification` | `acme-verify.ts` |
| function | `missingAssemblerIsNeverAPass(classify: SpawnClassifier): boolean` | `acme-verify.ts` |
| interface | `AcmeSegmentLine` | `acme-verify.ts` |
| interface | `AcmeDiagnostic` | `acme-verify.ts` |
| function | `parseAcmeResultLines(stdout: string): AcmeSegmentLine[]` | `acme-verify.ts` |
| function | `parseAcmeAggregateLines(stdout: string): string[]` | `acme-verify.ts` |
| function | `parseAcmeDiagnostics(stderr: string): AcmeDiagnostic[]` | `acme-verify.ts` |
| function | `firstResultLineDisagreement(parsed, expectedSegments): string \| undefined` | `acme-verify.ts` |
| function | `refuseOnCompetingAggregates(aggregateLines: readonly string[]): string \| undefined` | `acme-verify.ts` |
| new dir | `.planning/phases/30-acme-export-and-the-real-acme-oracle/fixtures/` | — |

<same_wave_contract>
**This plan runs in wave 2 alongside 30-03, and the two are independent by contract, not by luck.**
They share no file: this plan owns `acme-verify.ts` / `acme-verify.test.ts` / the phase fixtures
directory, and 30-03 owns `anno-export-asm.ts` / `anno-export-asm.test.ts` / `anno-types.ts` /
`anno-types.test.ts`. 30-03 *calls* `verifyAcmeAssembles()`, so this plan is bound by two rules that
make that call safe to make concurrently:

1. **`verifyAcmeAssembles()`'s public signature is FROZEN at what plan 30-01 published** —
   `(options: AcmeVerifyOptions) => AcmeVerifyResult`, with `AcmeVerifyOptions`, `AcmeVerifyResult`
   and `AcmeByteDiff` unchanged. Task 2 Part A extracts helpers **out of its body** and rewrites the
   body to call them; it does not rename, retype or newly-require anything a caller passes or reads.
   New exports are additive.
2. **`ACME_VERIFY_ARGV_FLAGS` is not touched by this plan** — it is 30-01's const, and 30-03 Task 2
   relies on `--strict-segments` already being in it.

If either rule has to break, this plan stops and the orchestrator re-waves 30-03 behind it. Silently
changing the surface under a same-wave sibling is how a red surfaces as an intermittent,
unattributable failure in `anno-export-asm.test.ts`.
</same_wave_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MANDATORY RED 1 — a missing assembler is never a pass, with its non-vacuity control</name>
  <files>src/mcp/vice/acme-verify.ts, src/mcp/vice/acme-verify.test.ts</files>
  <read_first>
    - `src/mcp/vice/acme-verify.ts` and `src/mcp/vice/acme-verify.test.ts` as plan 30-01 left them
    - `src/mcp/vice/acme-gate.test.ts` (full) — `runGateProbe()` at `:94-160` verbatim, the two-directions rule at `:11-26`, the temp-probe-location rule at `:28-34`, the memoised single spawn at `:134-142`, the wording-read-from-source guard at `:66-82`
    - `src/mcp/vice/anno-cli-path-consumers.test.ts:106-158` and `:422-479` — the one-shared-predicate discipline and the two-direction planted controls, including the comment-only control that stops a scan degrading into a substring search
    - `src/mcp/vice/anno-cli-path-consumers.test.ts:490-500` — the 2026-08-30 census that removed the last bare-`node` spawn sites (WR-20)
    - `.planning/phases/30-acme-export-and-the-real-acme-oracle/30-RESEARCH.md` § Common Pitfalls 2 (the full four-row measured table) and § Code Examples 6
    - `.planning/ROADMAP.md` § Phase 30 success criterion 2 and the note recording the false pass verbatim
  </read_first>
  <behavior>
    - `classifySpawn({ error: <ENOENT>, status: null })` returns `"unavailable"`; `classifySpawn({ status: 0 })` returns `"ran"`; `classifySpawn({ status: 1 })` returns `"ran"`.
    - `missingAssemblerIsNeverAPass(classifySpawn)` returns `true`.
    - `missingAssemblerIsNeverAPass(SHORTCUT_CLASSIFIER)` — the locally-defined planted violation using the historical truthiness form — returns `false`.
    - A child `node --test` run of a generated probe, with `ACME_BIN` set to a nonexistent path and `VICE_REQUIRE_ACME=1`, exits **non-zero** and its combined output contains the gate's own refusal wording read out of `acme-gate.ts`.
    - The same child run with `VICE_REQUIRE_ACME` deleted from the environment exits **zero**.
    - `verifyAcmeAssembles()` driven with a spawn result of the missing-binary shape returns `outcome === "skipped"`, and never `"ok"`.
  </behavior>
  <action>
**Part A — extract the spawn classification into one shared predicate in `acme-verify.ts`.**

Add these exports, moving Task-30-01's inline logic rather than duplicating it:

- `export type SpawnClassification = "unavailable" | "ran";`
- `export type SpawnClassifier = (r: { error?: unknown; status: number | null }) => SpawnClassification;`
- `export const classifySpawn: SpawnClassifier` — returns `"unavailable"` when
  `r.error !== undefined || r.status === null`, otherwise `"ran"`. Its JSDoc carries
  RESEARCH.md Pitfall 2's measured four-row table in prose: a nonexistent binary, an absolute
  nonexistent path and a non-executable file all yield `status: null` with an `error`, and the
  historical truthiness form evaluates true for every one of them — which is how a missing
  assembler read as a pass.
- `export function missingAssemblerIsNeverAPass(classify: SpawnClassifier): boolean` — **the ONE
  predicate**. It applies `classify` to each of the three measured missing-binary spawn shapes
  and returns true only when every one is classified `"unavailable"`. Its JSDoc states, in the
  `anno-cli-path-consumers.test.ts` idiom, that both the real check and the planted-violation
  control call this same function so there is exactly one definition of the property.

`verifyAcmeAssembles()` now calls `classifySpawn(r)` and maps `"unavailable"` to
`outcome: "skipped"`. Nothing else in the module reads `r.status` to decide an outcome.

**Part B — the in-process control pair in `acme-verify.test.ts`.**

Add a test named for the property, not for the mechanism, that asserts BOTH directions:
`missingAssemblerIsNeverAPass(classifySpawn)` is `true`, and
`missingAssemblerIsNeverAPass(SHORTCUT_CLASSIFIER)` is `false`, where `SHORTCUT_CLASSIFIER` is a
locally-defined function whose body implements the historical rule — the one that decides
availability from the truthiness of the exit status rather than from the presence of a spawn
error. Give `SHORTCUT_CLASSIFIER` a JSDoc naming it a deliberate planted violation and quoting
the recorded false pass from the ROADMAP note. The failure message on the second assertion must
say that a control which only ever refuses is indistinguishable from one that works.

**Part C — the child-process red, both directions.**

Add `runVerifyProbe(requireAcme: boolean): { status: number | null; output: string }`, modelled
on `acme-gate.test.ts:94-160` and carrying its reasoning in this file's own voice:

- `mkdtempSync(join(tmpdir(), "acme-verify-red-"))`, removed with
  `rmSync(dir, { recursive: true, force: true })` in a `finally` — the failure path included.
- Write a generated probe `*.test.mjs` into that directory (**never** into the module directory,
  which would break `test-gate.test.ts`'s exactly-one-set assertion). The probe imports
  `assertAcmeRequiredIfEnvSet` from `acme-gate.ts` by absolute path and `verifyAcmeAssembles`
  from `acme-verify.ts` by absolute path, and registers two tests: the gate assertion, and an
  assertion that `verifyAcmeAssembles()` over a trivial source returns an outcome that is not
  `"ok"`. That call passes the trivial source's own single `expectedSegments` entry and its matching
  `expectedBytes` — `expectedSegments` is REQUIRED (D30-06), and a probe that could not compile
  would exit non-zero for the wrong reason and read as the gate firing. It passes **no** `acmeBin`:
  the whole point of this probe is the module-load-constant `ACME_BIN`, which only a child process
  can move.
- Env: `{ ...process.env, ACME_BIN: join(dir, "definitely-not-acme") }`; set
  `VICE_REQUIRE_ACME: "1"` when `requireAcme`, otherwise **`delete env.VICE_REQUIRE_ACME`**
  (deleting, not emptying — an empty string is still a set variable to the gate).
  **`delete env.NODE_TEST_CONTEXT`**, with the measured-trap comment carried verbatim in
  substance: Node sets it in every process it runs a test file in, and a child `node --test` that
  inherits it refuses to run any file, prints a recursion notice and exits ZERO — which would
  make the FAIL direction report zero on a perfectly working gate and the control direction pass
  vacuously.
- `spawnSync(process.execPath, ["--test", probePath], { encoding: "utf8", timeout: 30_000, env })`
  — `process.execPath`, never a bare binary name (WR-20).
- Memoise the failing run in a lazily-computed cache so several assertions share ONE child spawn
  (`acme-gate.test.ts:134-142`, WR-11).

Add the wording-read-from-source non-vacuity guard: read `acme-gate.ts` from disk and assert the
refusal prefix the child's output is matched against really appears there, so a typo'd import
path (which also exits non-zero) cannot be mistaken for the gate firing.

State this file's total child-process count and wall-clock cost in the header, the way
`acme-gate.test.ts:36-40` does.
  </action>
  <verify>
    <automated>cd src/mcp/vice && set -o pipefail && node --test acme-verify.test.ts && npm run test:automated 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && node --test acme-verify.test.ts` exits 0 with zero failures.
    - The FAIL direction is **observed**: the memoised child run's recorded `status` is non-zero, and its output contains `acme-gate.ts`'s refusal prefix as read from disk. Paste the raw child output into the SUMMARY.
    - The paired control is **observed**: the same probe with `VICE_REQUIRE_ACME` deleted exits `0`. Paste that too.
    - `missingAssemblerIsNeverAPass(classifySpawn) === true` and `missingAssemblerIsNeverAPass(SHORTCUT_CLASSIFIER) === false` both assert in the same test.
    - Source assertion: `grep -v '^\s*//' src/mcp/vice/acme-verify.test.ts | grep -c 'delete env.NODE_TEST_CONTEXT'` returns at least `1`.
    - Source assertion: `grep -v '^\s*//' src/mcp/vice/acme-verify.test.ts | grep -cE 'spawnSync\(\s*"node"' ` returns `0` — `process.execPath` only.
    - Source assertion: `grep -v '^\s*//' src/mcp/vice/acme-verify.ts | grep -v '^\s*\*' | grep -c 'r\.status\b'` returns at most `2` (the `classifySpawn` body and the single `exitStatus` assignment), proving no third site reads the exit status.
    - `cd src/mcp/vice && npm run test:automated` reports **0 failures**.
  </acceptance_criteria>
  <done>With a bogus `ACME_BIN` the route reports skipped-or-failed and never a pass; the same predicate driven with the historical exit-status shortcut reports a pass, proving the guard can bite; and the paired zero-exit control proves the harness itself works.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MANDATORY RED 2 — a corrupted byte fails the diff while ACME exits 0; plus the five verdict rules and the stale-output trap</name>
  <files>src/mcp/vice/acme-verify.ts, src/mcp/vice/acme-verify.test.ts</files>
  <read_first>
    - `src/mcp/vice/acme-verify.ts` as Task 1 left it
    - `.planning/phases/30-acme-export-and-the-real-acme-oracle/30-RESEARCH.md` § Common Pitfalls 1, 3, 7, 10; § Code Examples 2 (the six rules) and 7 (real ACME output, verbatim, per case)
    - `src/skills/acme-build/scripts/acme.mjs` — the `MSVC` diagnostic regex, reusable verbatim
    - `.planning/phases/30-acme-export-and-the-real-acme-oracle/30-VALIDATION.md` § Per-Task Verification Map, rows 3-5
  </read_first>
  <behavior>
    - Corrupting one operand byte of the tracer export's `expectedBytes` yields `outcome === "failed"`, `byteDiff.equal === false`, `byteDiff.firstDifferingOffset` equal to the corrupted index, and `exitStatus === 0` in the SAME result object.
    - `parseAcmeResultLines()` over the measured `-v2` stdout returns one record per `Segment size is …` line with `start` and `endExclusive` parsed from the hex extents.
    - `parseAcmeAggregateLines()` returns the `Saving …` lines and nothing else.
    - `refuseOnCompetingAggregates(["Saving 16 …", "Saving 32 …"])` returns a reason string; with a single line or none it returns `undefined`.
    - `firstResultLineDisagreement()` returns a reason naming the FIRST index whose parsed extents differ from the expected segment, and `undefined` when every pair agrees.
    - `parseAcmeDiagnostics()` classifies the measured `Warning` lines as warnings and the measured `Error` / `Serious error` lines as errors; a source assembling with only warnings yields `outcome === "ok"`.
    - Real ACME leaves a pre-existing output file untouched when it fails: after a failing run against a path holding known bytes, exit is 1 and the file still holds those bytes.
    - A verification whose source carries a failing `!if * != … { !error … }` assertion yields `outcome === "failed"` with a reason naming the absent output file — never `"ok"` from a previous run's bytes.
  </behavior>
  <action>
**Part A — name the five rules as pure exported helpers in `acme-verify.ts`.**

Extract from `verifyAcmeAssembles()`'s body (do not duplicate) and export:

- `export interface AcmeSegmentLine { start: number; endExclusive: number; size: number; raw: string }`
- `export interface AcmeDiagnostic { file: string; line: number; severity: "Error" | "Warning" | "Serious error"; zone: string; message: string; raw: string }`
- `export function parseAcmeResultLines(stdout: string): AcmeSegmentLine[]`
- `export function parseAcmeAggregateLines(stdout: string): string[]`
- `export function parseAcmeDiagnostics(stderr: string): AcmeDiagnostic[]` — using the same MSVC
  shape `src/skills/acme-build/scripts/acme.mjs` carries, with a comment recording that this is
  the second implementation of that regex for the same separate-packages reason the argv list is
  (see plan 30-01's `<assumption_delta_decision>`)
- `export function refuseOnCompetingAggregates(aggregateLines: readonly string[]): string | undefined`
- `export function firstResultLineDisagreement(parsed: readonly AcmeSegmentLine[], expected: readonly { start: number; endExclusive: number }[]): string | undefined`

Each helper's JSDoc names which of the five surviving verdict rules it implements, and
`parseAcmeAggregateLines`' JSDoc states that the aggregate is recorded and never trusted — it is
the structural twin of the line that lied in the carried false-pass trap.

`verifyAcmeAssembles()` is rewritten to call these helpers in the D30-03 order from plan 30-01.

**Part B — MANDATORY RED 2, in `acme-verify.test.ts`.**

One test, `{ skip: SKIP_REASON }`, that reuses the tracer scenario's store and export (factor the
tracer's setup into a local `buildTracerExport(dir)` helper so both tests share one definition):

1. Run the honest verification once and assert `outcome === "ok"` — the control, so the red below
   is a change of one byte and nothing else.
2. Copy `expectedBytes` and flip one operand byte (RESEARCH.md Pitfall 3 measured `lda #$00`
   versus `lda #$01`: both assemble, both exit 0, bytes differ at offset 4).
3. Verify again with the corrupted `expectedBytes`. Assert, in one test body:
   `outcome === "failed"`; `byteDiff.equal === false`;
   `byteDiff.firstDifferingOffset` equals the index that was flipped;
   and **`exitStatus === 0`**. The assertion message on the last one must state that ACME reported
   success and the byte-diff caught it anyway — that is the whole content of the red.

**Part C — the stale-output trap (RESEARCH.md Pitfall 1, the fourth false-pass vector).**

Two tests:

1. A direct reproduction pinning real ACME's measured behaviour on this host: write known bytes
   into a file inside a temp directory, assemble a source with a duplicate symbol assignment to
   that same output path with `-f plain --msvc`, assert exit is `1`, assert the diagnostics carry
   ACME's own duplicate-symbol message, and assert the file **still** holds the known bytes. Its
   name states the property: ACME does not truncate its output file on failure.
2. A behavioural test of the module's defence: verify a source whose `!if * != … { !error … }`
   assertion deliberately fails. Assert `outcome === "failed"` and — the load-bearing half —
   **`byteDiff === null`**, which is what proves no bytes were read at all and therefore that no
   previous run's product could have been reported as a match. Assert `reason` quotes ACME's own
   `!error` text, which is the rule that fires first under 30-01's stated reason precedence
   (diagnostics before the absent-output-file rule); do **not** assert on the absent-output-file
   wording here, which that precedence means this case never reaches. Run it twice in sequence,
   honest-then-failing, and assert the second run is still `"failed"` with `byteDiff === null` —
   the first run's `equal: true` diff must not survive into it.

**Part D — one named test per verdict rule**, driving the pure helpers directly with the verbatim
real-ACME output recorded in RESEARCH.md § Code Examples 7. Do not paraphrase those strings —
copy them.

**Part E — the never-a-string-match structural test (EXPORT-03).** Read `acme-verify.ts` from
disk, strip comments with a quote-aware stripper, and assert the stripped source contains no
substring-containment call whose receiver is the caller-supplied source text. Pair it with a
planted-violation control in the same test — a synthetic source string that DOES contain such a
call — asserting the predicate reports it, so the scan is provably capable of catching a
violation. Add the comment-only control (`anno-cli-path-consumers.test.ts:460-479`) so the scan
cannot degrade into a substring search that passes by counting its own prose.
  </action>
  <verify>
    <automated>cd src/mcp/vice && set -o pipefail && VICE_REQUIRE_ACME=1 node --test acme-verify.test.ts && npm run test:automated 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && VICE_REQUIRE_ACME=1 node --test acme-verify.test.ts` exits 0 with zero failures.
    - The corrupted-byte test asserts `exitStatus === 0` in the same body as `byteDiff.equal === false`. Paste the recorded `AcmeVerifyResult` for the red run into the SUMMARY.
    - The stale-output reproduction records real ACME's exit `1` and the unchanged file bytes; paste both into the SUMMARY.
    - Five named tests exist, one per verdict rule, each asserting against its own pure helper — a source grep for `test("` in the file finds all five by name.
    - The Part E structural test asserts both directions (real source clean, planted source flagged, comment-only source not flagged).
    - **Signature freeze, per `<same_wave_contract>`:** `git diff` on `acme-verify.ts` shows no change to `verifyAcmeAssembles()`'s parameter or return type, and no field of `AcmeVerifyOptions`, `AcmeVerifyResult` or `AcmeByteDiff` was renamed, retyped or newly required. `ACME_VERIFY_ARGV_FLAGS`' members are byte-identical to plan 30-01's. Same-wave sibling 30-03 calls this function while this plan is running.
    - `cd src/mcp/vice && npm run typecheck` exits 0 and `npm run test:automated` reports **0 failures**.
  </acceptance_criteria>
  <done>A corrupted byte is caught by the diff while ACME reports success; each of the five verdict rules has its own test against its own named helper; and the stale-output vector is both reproduced against real ACME and closed by the module.</done>
</task>

<task type="auto">
  <name>Task 3: Re-record both pinned transcripts from real ACME output, with provenance</name>
  <files>.planning/phases/30-acme-export-and-the-real-acme-oracle/fixtures/README.md, .planning/phases/30-acme-export-and-the-real-acme-oracle/fixtures/verify-honest-pass.txt, .planning/phases/30-acme-export-and-the-real-acme-oracle/fixtures/verify-false-pass-trap.txt, src/mcp/vice/acme-verify.test.ts</files>
  <read_first>
    - `.planning/phases/29-the-mcp-surface/fixtures/README.md` and both `verify-*.txt` files there — **for SHAPE ONLY**: the "what each file is" table, the DO / DO-NOT block, and the obligation this task discharges. Their bytes are the retired producer's and are never asserted against
    - `src/mcp/vice/fixtures/backend-detect/README.md` — the "these are real captures, not synthesized" paragraph, the provenance table shape, the Regenerating section, and the known-sources-of-non-determinism section
    - `.planning/ROADMAP.md` § Phase 30, the re-record note in full
    - `src/mcp/vice/acme-verify.test.ts` as Tasks 1 and 2 left it
  </read_first>
  <action>
Create the fixtures directory and its three files, then add the test that keeps them honest.

**`verify-honest-pass.txt`** — the real transcript of an honest pass **through this phase's own
producer**. Capture, without trimming, re-wrapping, redacting or editing after capture:
the exact ACME argv used (with the temp paths replaced by stable placeholders, and the
substitution declared in the README); ACME's combined stdout and stderr byte-for-byte; and the
resulting `AcmeVerifyResult` rendered field by field — `outcome`, `exitStatus`,
`acmeResultLines`, `aggregateLines`, `diagnostics`, `byteDiff`, `reason`. Add a short leading
block naming the source scenario (the tracer store, its one code range, its image bytes) so a
later reader can rebuild it.

**`verify-false-pass-trap.txt`** — the trap **provoked against the NEW route**, not narrated.
Capture three sections, each with real output:
(1) the missing-assembler spawn: the `ACME_BIN` value used, `status: null`, the spawn error code,
what the historical truthiness classifier answers for that shape, and what `classifySpawn()`
answers;
(2) the stale-output vector: the pre-existing file's bytes, ACME's real exit status and its real
diagnostic line, and the file's bytes afterwards — unchanged;
(3) the exit-zero-wrong-byte vector: ACME's exit `0`, its own result lines, and the byte-diff's
`firstDifferingOffset`.
Each section ends with one line stating which verdict rule refuses it.

**`README.md`** — combining both analogs:
- The "these are real captures, not synthesized" paragraph, adapted: name the exact invocation
  that produced each transcript and state that nothing was trimmed, re-wrapped, redacted or
  edited after capture beyond the declared temp-path placeholder substitution.
- A provenance table with columns: File | Producer | Assembler | Host | Captured at | Requirement
  anchor. Producer is this phase's `anno-export-asm.ts` + `acme-verify.ts`; Assembler is
  `ACME 0.97 "Zem", 31 Jan 2021` at its resolved path; anchors are `EXPORT-01` and `EXPORT-03`.
- A DO / DO-NOT block in the Phase 29 README's voice, answering its obligation **by name**:
  state that the Phase 29 transcripts were read for shape only and are never asserted against,
  because a green test over the retired producer's bytes would prove the new parser can read the
  deleted tool's format — the evidence this phase does not need and must not claim to have.
- A **Regenerating** section: the exact command to re-run, what to update alongside, and the
  instruction not to hand-edit the `.txt` content.
- A **Known sources of non-determinism** section: the temp-path placeholder substitution, and
  ACME's own version banner.

**The keeping-honest test**, added to `acme-verify.test.ts`:
- both `.txt` files and the `README.md` exist;
- the README names the assembler release string and a capture date;
- `parseAcmeResultLines()` applied to the honest-pass transcript's captured stdout section
  returns the same records the transcript records under `acmeResultLines` — so the parser and its
  own evidence agree;
- **the non-vacuity control:** each new `.txt` is **not** byte-equal to the same-named file under
  the Phase 29 fixtures directory, asserted with a message stating that byte-equality would mean
  the file was copied rather than re-recorded;
- a source scan over every `*.test.*` file in `src/mcp/vice/` finds **zero** references to the
  Phase 29 fixtures directory path, asserted with a message naming the re-pin hazard.

Compute the Phase 29 fixtures path in the test from `repoRoot()` plus a joined relative path, so
the assertion has one place to update if those files ever move.
  </action>
  <verify>
    <automated>cd src/mcp/vice && set -o pipefail && node --test acme-verify.test.ts && npm run test:automated 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - All three fixture files exist under `.planning/phases/30-acme-export-and-the-real-acme-oracle/fixtures/`.
    - `cmp -s .planning/phases/30-acme-export-and-the-real-acme-oracle/fixtures/verify-honest-pass.txt .planning/phases/29-the-mcp-surface/fixtures/verify-honest-pass.txt` exits **non-zero**, and the same for `verify-false-pass-trap.txt` — proof of a genuine re-record.
    - `grep -rl '29-the-mcp-surface/fixtures' src/mcp/vice/*.test.ts` returns **only** `src/mcp/vice/acme-verify.test.ts` (which names it solely to assert non-equality), and no test asserts a transcript's content against those bytes.
    - The README's provenance table names `ACME 0.97 "Zem"` and a capture date, and its DO-NOT block answers the Phase 29 README's obligation by name.
    - `cd src/mcp/vice && node --test acme-verify.test.ts` exits 0.
    - `cd src/mcp/vice && npm run test:automated` reports **0 failures**.
  </acceptance_criteria>
  <done>Both pinned transcripts are this phase's own real-ACME output, their provenance is recorded, and a test proves they were re-recorded rather than copied and that nothing in this phase asserts against the retired producer's bytes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ACME_BIN` env → child process launch | The red harness deliberately points it at a nonexistent path |
| child `node --test` env → runner behaviour | An inherited `NODE_TEST_CONTEXT` silently neuters the child |
| ACME stdout/stderr → verdict | Untrusted-shaped text parsed into a pass/fail decision |
| recorded transcript → future assertion | A fixture can re-pin a rebuilt parser to a retired producer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Elevation of Privilege | red-harness spawns | high | mitigate | `spawnSync(process.execPath, argvArray, { env })` and `spawnSync(ACME_BIN, argvArray)`; no shell string anywhere in the harness |
| T-30-04 | Spoofing | the byte-diff subject | high | mitigate | Task 2 Part C reproduces ACME's stale-output behaviour against real ACME and asserts the module refuses to read a prior run's product |
| T-30-07 | Denial of Service | temp dirs on RAM-backed `/tmp` | low | mitigate | Every `mkdtempSync` in this plan is paired with `rmSync(..., { recursive: true, force: true })` in a `finally`, failure path included |
| T-30-08 | Repudiation | evidence provenance | medium | mitigate | The fixtures README records producer, assembler release, host and capture date; the non-vacuity test proves the transcripts were re-recorded rather than copied |
| T-30-09 | Spoofing | a vacuous mandatory red | high | mitigate | The paired zero-exit control and the wording-read-from-source guard make "the gate fired" distinguishable from "the harness broke" |
| T-30-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no packages; RESEARCH.md § Package Legitimacy Audit records "not applicable" |
</threat_model>

<verification>
- `cd src/mcp/vice && npm run typecheck` exits 0
- `cd src/mcp/vice && VICE_REQUIRE_ACME=1 node --test acme-verify.test.ts` exits 0
- `cd src/mcp/vice && npm run test:automated` reports 0 failures (stop any running VICE broker first)
- Both mandatory reds are **pasted into the SUMMARY as raw output**, not summarised
</verification>

<success_criteria>
- Mandatory red 1 observed, with its non-vacuity control and its paired zero-exit direction
- Mandatory red 2 observed: `outcome: "failed"`, `byteDiff.equal: false`, `exitStatus: 0` in one result
- The five verdict rules each have a named test against a named pure helper
- Both transcripts re-recorded from real ACME 0.97, provably not copies
</success_criteria>

<output>
Create `.planning/phases/30-acme-export-and-the-real-acme-oracle/30-02-SUMMARY.md` when done.
It must contain the raw child-process output for both directions of red 1, the raw
`AcmeVerifyResult` for red 2, and the raw stale-output reproduction.
</output>
