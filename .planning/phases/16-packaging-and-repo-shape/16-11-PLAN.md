---
phase: 16-packaging-and-repo-shape
plan: 11
type: execute
wave: 3
depends_on: ["16-08", "16-09", "16-10"]
gap_closure: true
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/16-packaging-and-repo-shape/16-GAP-CLOSURE-DECISIONS.md
autonomous: true
requirements: [PKG-04]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`.planning/REQUIREMENTS.md`'s PKG-04 entry is checked and its Traceability row reads `Complete`, matching the disposition PROJECT.md's Key Decisions table has carried since 2026-08-22 and `16-PKG04-EVIDENCE.md` grounds in file:line citations and a live observed bind."
    - "REQUIREMENTS.md's PKG-04 checkbox state and its Traceability row agree: a checked entry with a `Pending` row, or an unchecked entry with a `Complete` row, is an internally inconsistent ledger and fails a mechanical check. The same agreement holds for PKG-01, PKG-02 and PKG-03."
    - "PKG-01's entry carries a closure note naming the gap-closure plans that made its tarball-correctness sub-clause true, so a reader who finds the verification report's `⚠️ PARTIAL` verdict can trace how it was closed rather than inferring it."
    - "Every item this gap-closure round deliberately decided to LEAVE unchanged is recorded with its reason in `16-GAP-CLOSURE-DECISIONS.md` — none is left undecided, and none is silently dropped."
    - "The two `unclassified` rows the spec-less edge probe surfaced for PKG-02 and PKG-03 are recorded as explicit flagged assumptions, still `unresolved`, never auto-resolved with a backstop and never dismissed."
  prohibitions:
    - "MUST NOT close this gap by weakening the requirement text that exposed it. Flipping a checkbox is only legitimate when the requirement's own words are already true; rewording PKG-01 or PKG-04 to fit what shipped is forbidden."
    - "MUST NOT edit `.planning/STATE.md` or `.planning/ROADMAP.md` — the orchestrator owns both, and a plan that writes them races it."
    - "MUST NOT mark DEBT-04 or CORE-01 as anything other than what they are today: both belong to Phase 17 and are outside this phase entirely."
    - "MUST NOT record a decided-and-left item without its reason. An entry that says only 'left alone' reintroduces the undecided state this plan exists to close."
  artifacts:
    - .planning/phases/16-packaging-and-repo-shape/16-GAP-CLOSURE-DECISIONS.md
  key_links:
    - "`16-02-SUMMARY.md` explicitly deferred PKG-04's checkbox flip to 'phase 16's close-out bookkeeping', and no plan 16-03..16-07 performed it. Phase 15 had a designated closer (`15-12`) in exactly this shape; this plan is Phase 16's. The verification report records the omission as `requirements_bookkeeping_note` in its frontmatter — a WARNING, not a gap — precisely because the underlying disposition is independently verified TRUE."
    - "`PROJECT.md` → Key Decisions carries the dated PKG-04 accepted-risk row (bind address, rejected narrow branch, compensating control, residual risk, reversal condition) and `16-PKG04-EVIDENCE.md` grounds every claim. The checkbox flip records an already-verified fact; it does not decide anything new, which is why no checkpoint gates it."
    - "`.planning/REQUIREMENTS.md`'s `Control-Plane Bind Follow-on (PKG-04, decided accept, Phase 16 plan 16-02)` section already names the smart-bind alternative as owned follow-on work for a future milestone. Marking PKG-04 complete does not close that follow-on and must not be worded as though it does."
    - "This plan writes only under `.planning/`. Worktree isolation strips `.planning/` files from a plan's commits, so this plan must execute on the main checkout — if the executor is running in an isolated worktree, it cannot deliver its artifacts and must stop and say so rather than reporting success."
---

<objective>
Close Phase 16's record. Three things are true in the tree and false in the ledger, and one
set of decisions this gap-closure round made deliberately has nowhere to live yet.

PKG-04's disposition is fully verified — PROJECT.md's Key Decisions table carries the dated
accepted-risk row and `16-PKG04-EVIDENCE.md` grounds every claim in file:line citations and
a live observed bind — while `.planning/REQUIREMENTS.md` still shows it unchecked and
`Pending`. `16-02-SUMMARY.md` deferred that flip to "phase 16's close-out bookkeeping" and
no plan performed it. PKG-01 is checked and its tarball-correctness sub-clause was FALSE
until plans 16-08 and 16-10 landed, so it needs a closure note a reader can follow rather
than a silent `[x]`.

And this gap-closure round made several deliberate decisions to leave things alone — three
classes of path literal that plan 16-04 classified as source-tree self-references and that
this round did not reverse, plus two probe rows it could not classify. Left unrecorded,
each of those is exactly the undecided state that produced this phase's gaps in the first
place.

Purpose: the requirement ledger is what the next milestone reads. A ledger that disagrees
with the tree is the same failure class as a comment that names a directory that is not
there — and this phase has now been bitten by that class twice.

Output: a consistent requirement ledger and one recorded decision register.

<tracer_exemption>
This plan has no `type="tracer"` task and that is deliberate, not an omission. A tracer is
the thinnest path threaded through every layer a phase will modify; this plan modifies one
layer — the planning record — and produces two documentation artifacts with no code, no
transport, no generated output and no stack to thread. Forcing a tracer label onto a
single-artifact ledger edit would make the label mean nothing. Every other plan in this
gap-closure round (16-08, 16-09, 16-10) leads with a real tracer.
</tracer_exemption>
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-packaging-and-repo-shape/16-VERIFICATION.md
@.planning/phases/16-packaging-and-repo-shape/16-REVIEW.md
@.planning/phases/16-packaging-and-repo-shape/16-PKG04-EVIDENCE.md
@.planning/phases/16-packaging-and-repo-shape/16-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The requirement ledger agrees with the verified tree</name>
  <reversibility rating="reversible">Two ledger lines and two closure notes in one planning document; reverting is a single-hunk revert with no code, artifact or published contract involved.</reversibility>
  <precondition>Plans 16-08, 16-09 and 16-10 are committed and their verification commands green — this task records their outcome, so running it against an unfinished tree would write a false ledger.</precondition>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 200-210 — the `Packaging and Repo Shape` section with all four PKG entries and their current checkbox states (PKG-01/02/03 checked, PKG-04 unchecked at :207).
    - `.planning/REQUIREMENTS.md` lines 240-265 — the `Control-Plane Bind Follow-on (PKG-04, decided accept, Phase 16 plan 16-02)` section, so the closure note does not imply that owned follow-on is closed too.
    - `.planning/REQUIREMENTS.md` lines 340-368 — the Traceability table (PKG-04's row reads `Pending` at :358) and the Coverage block below it. Confirm for yourself whether the Coverage block counts completions; if it counts only mapping, it needs no edit.
    - `.planning/REQUIREMENTS.md` lines 160-200 — the DEBT and GATE entries, several of which carry blockquoted closure notes added when they were completed. Reuse that established note shape rather than inventing one.
    - `.planning/PROJECT.md` → Key Decisions — the dated PKG-04 accepted-risk row. Read it so the closure note cites the record rather than restating its reasoning.
    - `.planning/phases/16-packaging-and-repo-shape/16-VERIFICATION.md` — the frontmatter `requirements_bookkeeping_note` and the Requirements Coverage table's PKG-01 `⚠️ PARTIAL` and PKG-04 rows. These are the two verdicts the closure notes must let a reader reconcile.
    - `.planning/phases/16-packaging-and-repo-shape/16-02-SUMMARY.md` — the sentence deferring the flip to close-out bookkeeping, so the note can cite where the deferral was recorded.
  </read_first>
  <action>
Make `.planning/REQUIREMENTS.md` agree with what is verified, using only scoped edits — never a whole-file rewrite.

Flip PKG-04's entry to checked. Immediately below it, add a blockquoted closure note in the same shape the completed DEBT and GATE entries already use. The note must state: that the disposition is `accept`, not `narrow`; that PROJECT.md's Key Decisions table carries the dated row and `16-PKG04-EVIDENCE.md` grounds it in file:line citations plus a live observed bind; that plan 16-02 recorded the disposition and deferred only this bookkeeping to close-out, which plan 16-11 performed; and — explicitly — that the `Control-Plane Bind Follow-on` section further down remains open owned follow-on work that this completion does not close. Do not restate the risk rationale; cite where it lives.

Change PKG-04's Traceability row from `Pending` to `Complete`. Change no other row.

Add a blockquoted closure note under PKG-01's entry, again in the established shape. It must state: that the relocation and merge halves were verified TRUE at phase verification; that the tarball-correctness sub-clause ("no tests, no fixtures leaked") was FALSE for the `@henols/c64-re-tools` package at that point, with the four leaked test files named as a class rather than individually; that plan 16-08 fixed the copy seam and promoted the leak assertion into the single packing seam so both packages are checked by the same code path; that plan 16-10 closed the consumer-installed-path regression the same sweep introduced; and that the phase verification report's `⚠️ PARTIAL` verdict for PKG-01 refers to the pre-closure state. Keep it to the note; do not edit PKG-01's requirement sentence.

If — and only if — the Coverage block below the Traceability table carries a completion count rather than a mapping count, update it to match. Read it first and record in the SUMMARY which it is; do not invent a count.

Then check the ledger's internal consistency mechanically before committing: for each of PKG-01..PKG-04, the checkbox state and the Traceability row must agree. Run that check as a command, not by eye, and record its output.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node -e 'const s=require("fs").readFileSync(".planning/REQUIREMENTS.md","utf8");const ids=["PKG-01","PKG-02","PKG-03","PKG-04"];for(const id of ids){const box=new RegExp("^- \\[( |x)\\] \\*\\*"+id+"\\*\\*","m").exec(s);if(!box)throw new Error("no checkbox entry for "+id);const row=new RegExp("^\\| "+id+" \\| \\d+ \\| (\\w+) \\|","m").exec(s);if(!row)throw new Error("no traceability row for "+id);const checked=box[1]==="x";const complete=row[1]==="Complete";if(checked!==complete)throw new Error(id+": checkbox checked="+checked+" but traceability says "+row[1]);}console.log("REQUIREMENTS.md: all four PKG entries internally consistent")'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[x\] \*\*PKG-04\*\*' .planning/REQUIREMENTS.md` returns 1, and `grep -c '^- \[ \] \*\*PKG-04\*\*' .planning/REQUIREMENTS.md` returns 0.
    - `grep -cE '^\| PKG-04 \| 16 \| Complete \|' .planning/REQUIREMENTS.md` returns 1, and `grep -cE '^\| PKG-04 \| 16 \| Pending \|' .planning/REQUIREMENTS.md` returns 0.
    - The consistency check in `<verify>` exits 0 and its output is recorded in the SUMMARY.
    - PKG-04's closure note cites its evidence rather than restating it: the note text contains both `16-PKG04-EVIDENCE.md` and `PROJECT.md`, and contains the word `accept`.
    - PKG-04's closure note explicitly preserves the open follow-on: the note text names `Control-Plane Bind Follow-on`.
    - PKG-01's closure note names both closing plans: `grep -c '16-08' .planning/REQUIREMENTS.md` returns at least 1 and `grep -c '16-10' .planning/REQUIREMENTS.md` returns at least 1.
    - No requirement sentence was reworded: `git diff -U0 .planning/REQUIREMENTS.md | grep -E '^-' | grep -v '^--' | grep -cE '^\-- \[( |x)\] \*\*PKG-0[123]\*\*'` returns 0, and the only removed lines are PKG-04's unchecked entry and its `Pending` traceability row.
    - No other requirement family is touched: `git diff -U0 .planning/REQUIREMENTS.md | grep -E '^[+-]' | grep -v '^[+-][+-]' | grep -cE 'DEBT-|CORE-|GATE-|FORK-|EXTV-'` returns 0.
    - Only `.planning/REQUIREMENTS.md` is modified by this task: `git status --porcelain -- .planning/STATE.md .planning/ROADMAP.md` prints nothing.
    - The commit for this task exists and contains the file: `git log -1 --name-only --format= | grep -c 'REQUIREMENTS.md'` returns 1. If the file is absent from the commit, the executor is running in worktree isolation and must stop and report rather than declaring success.
  </acceptance_criteria>
  <done>PKG-04 is checked and `Complete` with a closure note citing PROJECT.md and the evidence document and explicitly preserving the open follow-on; PKG-01 carries a closure note naming plans 16-08 and 16-10; all four PKG checkbox states agree with their Traceability rows under a mechanical check; and no requirement sentence and no other requirement family was touched.</done>
</task>

<task type="auto">
  <name>Task 2: Every deliberate leave-alone and every unclassified probe row is recorded with its reason</name>
  <reversibility rating="reversible">A new planning document; deleting it restores the prior state, though it would also restore the undecided state this task closes.</reversibility>
  <files>.planning/phases/16-packaging-and-repo-shape/16-GAP-CLOSURE-DECISIONS.md</files>
  <read_first>
    - `.planning/phases/16-packaging-and-repo-shape/16-REVIEW.md` section `IN-01` — the informational finding about the synthetic scratch-directory segment names, which plan 16-09 acted on. Read it so this register records the outcome rather than re-opening the question.
    - `.planning/phases/16-packaging-and-repo-shape/16-REVIEW.md` section `WR-03` — the reuse-advice finding and its closing sentence asking whether one guard could cover the whole class, so the register can record which sites the class guard covers and which were deliberately excluded.
    - Commit `7295e20`'s message in full (`git show -s 7295e20`) — its declared three categories of deliberate leftover and its statement that the six skills' own SKILL.md and script self-references were reclassified as source-tree references. This is the classification this round did NOT reverse, and the register must say so in those terms.
    - The three leave-alone site classes, read directly so the register cites real lines: the repo-root quick-reference assignments near the top of `src/skills/c64-memory-mapping/SKILL.md`, `src/skills/c64-program-recon/SKILL.md`, `src/skills/c64-provenance-diff/SKILL.md` and `src/skills/c64-ram-capture/SKILL.md` (each labelled `from the repo root` or `relative to the repo root`); the `HERE` self-location comment beside `SCAN_DIRS` in `src/skills/c64-provenance-diff/scripts/recovery-schema.mjs`; and the install-location narration in the header of `src/skills/c64-ram-capture/scripts/project-paths.mjs`.
    - `src/mcp/vice/repo-root.test.ts` lines 133-145 — the deliberate pre-move-shape assertion plan 16-09 left untouched, so the register records why a stale-looking segment list there is correct.
    - `.planning/phases/16-packaging-and-repo-shape/16-08-PLAN.md` and `16-09-PLAN.md` and `16-10-PLAN.md` — their `## Flagged Assumptions` sections and the `<assumption_delta_decision>` block in 16-08, so the register consolidates rather than contradicts them.
  </read_first>
  <action>
Create `.planning/phases/16-packaging-and-repo-shape/16-GAP-CLOSURE-DECISIONS.md` as this round's decision register. Give it the same frontmatter shape the phase's other evidence documents use (phase, slug, created date).

Write four sections.

**Section one — decided and left unchanged, with reasons.** One row or subsection per item, each naming the file, what it says today, and the reason it stays. Cover all five:

- The repo-root quick-reference assignments in four `SKILL.md` playbooks. Record that commit `7295e20` classified them as source-tree self-references, that they are labelled as repo-root-relative in the playbook text itself, that on a plugin install the correct form would be plugin-root-relative and on an installer install it would be skills-directory-relative, and that making them dual-route the way the same files already do for the MCP CLI commands is a fresh documentation-design decision rather than gap closure. Name the owner: whichever milestone next revisits skill playbook routing. State what would force it: a report of an agent in a consumer project following one of those assignments and failing.
- `recovery-schema.mjs`'s `HERE` self-location comment. Record that it labels this repository's own location, that the comment directly above it already acknowledges both trees, and that the code beside it is location-agnostic by construction — so the label is accurate in the tree it describes.
- `project-paths.mjs`'s install-location narration. Record that its sentence exists to say the install location varies, that the named example is now this repository's layout rather than a consumer's, and that this is a wording preference with no functional consequence because the module resolves by walking up for a repository marker and never counts hops.
- `repo-root.test.ts`'s deliberate pre-relocation-shape assertion. Record that it exists to pin the hop count as a property of depth rather than of a directory name, that plan 16-04 added it for exactly that reason, and that it is therefore correct as written and out of scope for any path sweep.
- The synthetic scratch-directory segment names the review's `IN-01` raised. Record the opposite outcome: this round did NOT leave them, plan 16-09 renamed them to match the shape the adjacent comment claims to mirror, and the depth — the only load-bearing property — is unchanged. `IN-01` asked for an explicit decision either way; this is it.

**Section two — the spec-less probe's flagged assumptions.** This phase has no SPEC.md, so both the edge-coverage and prohibition sections were absent and the fallback probe ran over PKG-01..PKG-04. Record the outcome verbatim in the terms the probe uses: four items surfaced; the two `concurrency` rows (PKG-01, PKG-04) were resolved with explicit acceptance criteria and authored as plain truths in plans 16-08 and 16-11 respectively; the two `unclassified` rows (PKG-02, PKG-03) remain `unresolved` and are carried as flagged assumptions in plans 16-08 and 16-09. State the no-silent-drop equality explicitly: four surfaced equals two authored plus two flagged. State that nothing was auto-dismissed and nothing was resolved with a backstop.

**Section three — the recalled prohibitions.** List the three kept prohibitions this round authored into the plans' `must_haves.prohibitions` blocks (do not weaken the gate that exposed a gap; do not ship a consumer-facing path that does not exist in an install; do not present an unexecuted committed suite as coverage), naming which plans carry each. Then record the canon-referral breadcrumb: path-traversal and arbitrary-write exposure in the installer's copy and merge routines is canon security work owned by the security review and the per-plan threat models, and was deliberately not minted as a bespoke prohibition.

**Section four — new sites this round found that the two gap inputs did not name.** Record them as findings with their provenance, so the record shows what came from the verification report, what came from the code review, and what came from re-verifying the tree during planning: the `renderLoading` consumer-path literal in `watch-loads.mjs`; the `; Build:` instruction in `template.a` that `acme.mjs new` copies verbatim into a user's file; the stale canonical-source comment in `.gitignore`; the self-contradictory scratch-shape comment in `r2000-regbits.test.ts`; and the four skill test suites plus one installer suite that no CI step executed. For each, name the plan that closed it.

Every entry in every section carries a reason. An entry that says only that something was left alone is not acceptable — it recreates the undecided state this document exists to close.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node -e 'const s=require("fs").readFileSync(".planning/phases/16-packaging-and-repo-shape/16-GAP-CLOSURE-DECISIONS.md","utf8");const need=["recovery-schema.mjs","project-paths.mjs","repo-root.test.ts","r2000-regbits.test.ts","watch-loads.mjs","template.a",".gitignore","unresolved","unclassified","canon"];const missing=need.filter(n=>!s.includes(n));if(missing.length)throw new Error("decision register does not name: "+missing.join(", "));if(!/four/i.test(s))throw new Error("no-silent-drop equality not stated");console.log("decision register covers all required entries")'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/16-packaging-and-repo-shape/16-GAP-CLOSURE-DECISIONS.md` exits 0 and the file has frontmatter with `phase`, `slug` and `created`.
    - All four sections are present and named: the file contains headings for the decided-and-left register, the probe's flagged assumptions, the recalled prohibitions, and the newly-found sites.
    - Every leave-alone entry names a file and a reason: the file names `recovery-schema.mjs`, `project-paths.mjs`, `repo-root.test.ts`, and at least two of the four `SKILL.md` playbooks by path, and no entry consists of a bare statement that something was left alone. Assert the reason requirement by checking that each named file appears within 400 characters of the word `because` or `reason` in the document.
    - The `IN-01` outcome is recorded as the renamed-not-left decision: the file names `r2000-regbits.test.ts` and plan `16-09`, and states that the scratch depth is unchanged.
    - The no-silent-drop equality is stated as an equality, not implied: the file states that four probe items surfaced and accounts for all four, two authored and two flagged.
    - The two `unclassified` rows are recorded as still `unresolved`: the file contains both `unclassified` and `unresolved`, and does not claim either row was dismissed or resolved with a backstop.
    - The canon-referral breadcrumb is present: the file names path traversal or arbitrary write as canon security work owned elsewhere and states it was not minted as a bespoke prohibition.
    - Each of the five newly-found sites names the plan that closed it: `grep -cE '16-(08|09|10)' .planning/phases/16-packaging-and-repo-shape/16-GAP-CLOSURE-DECISIONS.md` returns at least 5.
    - No source file is modified by this task: `git status --porcelain -- src scripts installer .github` prints nothing.
    - The commit for this task contains the new document: `git log -1 --name-only --format= | grep -c '16-GAP-CLOSURE-DECISIONS.md'` returns 1. If the file is absent from the commit, the executor is running in worktree isolation and must stop and report rather than declaring success.
  </acceptance_criteria>
  <done>A committed decision register records every item this round deliberately left unchanged with its reason and its owner, the review's `IN-01` outcome as a decision rather than an open question, the probe's four surfaced items with the no-silent-drop equality stated and the two unclassified rows still flagged as unresolved, the three recalled prohibitions with their canon-referral breadcrumb, and the five sites this round found that neither gap input named.</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol / artifact | Kind | File |
|---|---|---|
| PKG-04 entry flipped to checked plus its blockquoted closure note | ledger edit | `.planning/REQUIREMENTS.md` |
| PKG-04 Traceability row `Pending` → `Complete` | ledger edit | `.planning/REQUIREMENTS.md` |
| PKG-01 blockquoted closure note naming plans 16-08 and 16-10 | ledger edit | `.planning/REQUIREMENTS.md` |
| `16-GAP-CLOSURE-DECISIONS.md` — decided-and-left register, probe flagged assumptions, recalled prohibitions with canon breadcrumb, newly-found sites | new planning document | new file |

This plan produces no code symbol, no test, no CI step and no shipped file.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the requirement ledger → the next milestone's planning | `REQUIREMENTS.md` is read as the authoritative statement of what is done; a ledger that disagrees with the tree misdirects every later decision built on it. |
| a completion mark → an open follow-on | Marking PKG-04 complete sits next to an explicitly open follow-on section; a note that blurs the two would silently close owned work. |
| the executor's isolation mode → the planning directory | Worktree isolation strips `.planning/` from a plan's commits, so an executor could report success having delivered nothing. |

## STRIDE Threat Register

ASVS level 1 (opportunistic); blocking threshold: `high`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-46 | Repudiation | `.planning/REQUIREMENTS.md` — a completion mark whose evidence a later reader cannot trace | medium | mitigate | Both closure notes cite their record rather than restating it: PKG-04's names PROJECT.md's Key Decisions row and `16-PKG04-EVIDENCE.md`; PKG-01's names plans 16-08 and 16-10 and states that the verification report's partial verdict describes the pre-closure state. Both citations are acceptance criteria. |
| T-16-47 | Tampering | a checkbox flipped by rewording the requirement it belongs to | high | mitigate | A diff-scoped criterion asserts no PKG requirement sentence was removed or reworded and that the only removed lines are PKG-04's unchecked entry and its `Pending` row; a second criterion asserts no other requirement family appears in the diff. |
| T-16-48 | Spoofing | a completion mark read as closing the open control-plane-bind follow-on | medium | mitigate | PKG-04's closure note explicitly names the `Control-Plane Bind Follow-on` section and states it remains open owned work; asserted as an acceptance criterion. |
| T-16-49 | Repudiation | an executor in worktree isolation reporting success while `.planning/` edits were stripped from its commits | high | mitigate | Both tasks carry a criterion that inspects the last commit's file list for the artifact and instructs the executor to stop and report rather than declare success if it is absent. |
| T-16-50 | Information Disclosure | the decision register quoting evidence text into a document with a wider audience | low | accept | Every item recorded is already in a committed planning artifact or a committed source comment in this repository; the register cites and summarises rather than importing new material, and this repository's planning tree is already part of the public repo. |
| T-16-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task exists in this plan, and this plan touches no `package.json`, no lockfile and no dependency. No `[ASSUMED]`/`[SUS]` package is introduced, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
Run from the repository root:

1. The ledger consistency check from Task 1's `<verify>` — exit 0 for all four PKG entries.
2. `grep -c '^- \[x\] \*\*PKG-04\*\*' .planning/REQUIREMENTS.md` — returns 1.
3. `grep -cE '^\| PKG-04 \| 16 \| Complete \|' .planning/REQUIREMENTS.md` — returns 1.
4. The decision-register coverage check from Task 2's `<verify>` — exit 0.
5. `git status --porcelain -- src scripts installer .github .planning/STATE.md .planning/ROADMAP.md` — prints nothing.
6. `git log -2 --name-only --format=%s` — shows both artifacts inside this plan's commits; an absent `.planning/` path means worktree isolation stripped them and the plan did not deliver.
7. Regression check that this plan changed no behaviour: `cd src/mcp/vice && VICE_REQUIRE_ACME=1 npm test` reports `# fail 0`, and `node scripts/check-npm-packages.mjs`, `node scripts/check-skill-tool-coverage.mjs`, `node scripts/check-skill-fork-honesty.mjs` and `bash scripts/package.sh` all exit 0.
</verification>

<success_criteria>
- PKG-04 is checked and `Complete` with a closure note citing PROJECT.md and `16-PKG04-EVIDENCE.md` and explicitly preserving the open control-plane-bind follow-on.
- PKG-01 carries a closure note naming the two plans that made its tarball-correctness sub-clause true.
- All four PKG checkbox states agree with their Traceability rows under a mechanical check.
- No requirement sentence was reworded and no other requirement family was touched.
- Every item this round deliberately left unchanged is recorded with its reason and its owner; the review's `IN-01` has an explicit recorded outcome.
- The probe's four surfaced items are fully accounted for — two authored, two flagged `unresolved` — with the equality stated, nothing dismissed and nothing backstopped.
- `.planning/STATE.md` and `.planning/ROADMAP.md` are untouched.
</success_criteria>

<output>
Create `.planning/phases/16-packaging-and-repo-shape/16-11-SUMMARY.md` when done.
</output>
