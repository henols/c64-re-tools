---
phase: 16-packaging-and-repo-shape
plan: 09
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/mcp/vice/r2000-symbol-roundtrip.test.ts
  - src/mcp/vice/r2000-regbits.test.ts
  - src/mcp/vice/hop-chain-comments.test.ts
  - src/mcp/vice/fixtures/planted-hop-chain-fixture.ts.txt
  - .gitignore
autonomous: true
requirements: [PKG-03]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Both hop-count comments in `src/mcp/vice/r2000-symbol-roundtrip.test.ts` (at :55 and :467) describe a directory chain that exists on disk: each intermediate segment they name is a real ancestor of `src/mcp/vice`, and the chain terminates at the repository root reached by the three `\"..\"` hops the adjacent code actually performs."
    - "A comment line that mixes a current path root with a pre-move path root inside a chain that terminates at the repository root fails `src/mcp/vice/hop-chain-comments.test.ts` — the half-swept-chain defect class cannot land again silently."
    - "The guard is calibrated against the real corpus, not a synthetic one: measured at plan time exactly three comment lines across `src/mcp/vice/*.ts` and `*.mts` contain both a chain arrow and the phrase naming the repository root, of which one (`r2000-answer-key.test.ts:34`) is legitimate and stays green as an in-tree negative control, and two are the violations this plan fixes."
    - "`src/mcp/vice/r2000-regbits.test.ts`'s two synthetic scratch trees use segment names that match the shape its own adjacent comment claims to mirror, and both tests still pass — the scratch depth (three segments below the scratch root) is unchanged, which is the only load-bearing property."
    - "`.gitignore`'s comment above the ignored generated-copy entry names the directory `installer/scripts/sync-skills.mjs` actually reads today."
    - "The deliberate pre-move-shape assertion in `src/mcp/vice/repo-root.test.ts` (the test pinning the hop count as a property of depth rather than of one directory name) is untouched — `git diff --quiet -- src/mcp/vice/repo-root.test.ts` holds after this plan."
  prohibitions:
    - "MUST NOT close this gap by weakening the gate, the assertion or the requirement text that exposed it — the fix belongs in the artifact, never in the check that reports it. Adding a per-file exemption to the new guard so a stale chain can stay is forbidden; the exemption mechanism this guard deliberately does not have is what keeps it trusted."
    - "MUST NOT rewrite historical narration into a false present tense. Comments that describe a PAST move (`moved from X to Y`, `the old shape`, `what phase 16-04 did`, `pre-move`) are accurate history and are load-bearing in this codebase; only chains that claim to describe the CURRENT resolution path are in scope."
    - "MUST NOT change any code in `r2000-regbits.test.ts` other than the two scratch-directory segment lists and the comments describing them: the scratch depth, the copied generator, the planted-byte digest comparison and the cleanup must all behave identically."
    - "MUST NOT add the new fixture to `src/mcp/vice/package.json`'s `files[]`, and MUST NOT give it a `.ts` or `.mts` extension — the guard's own scanned set would then pick it up and the guard would fail on its own fixture."
  artifacts:
    - src/mcp/vice/hop-chain-comments.test.ts
    - src/mcp/vice/fixtures/planted-hop-chain-fixture.ts.txt
  key_links:
    - "`src/mcp/vice/r2000-symbol-roundtrip.test.ts:55` sits directly above a `join(HERE, \"..\", \"..\", \"..\", …)` fixture path and `:467` directly above `const REPO_ROOT = join(HERE, \"..\", \"..\", \"..\")`. The code is correct — three hops is right both before and after the move, because both shapes are three segments below the root — so the ONLY defect is the comment, and only its intermediate segment names."
    - "`src/mcp/vice/repo-root.test.ts:133-140` deliberately asserts the PRE-MOVE three-segment shape still resolves, to pin the hop count as a property of depth rather than of a directory name. Its `join(root, ...)` segment list and its narration at :107-119 are correct history added by plan 16-04 and are explicitly out of scope. The new guard's per-line matching keeps them green: line :117 carries the chain arrow but not the repository-root phrase, and line :116 carries the phrase but not the arrow."
    - "`src/mcp/vice/r2000-regbits.test.ts`'s scratch trees exist so the copied generator's `MEMMAP_PATH` formula — `join(HERE, \"..\", \"..\", \"..\", \"src\", \"skills\", \"c64-memory-mapping\", \"memmap.json\")` in `r2000-regbits-gen.ts:62` — resolves against the sibling scratch `src/skills` tree. Three hops up from a three-segment scratch path lands on the scratch root either way, so renaming the segments is behaviour-neutral; the depth is the contract, the names are the documentation."
    - "`src/mcp/vice/fixtures/planted-phase-pointer-fixture.ts.txt` plus `comment-phase-pointers.test.ts:505-517,611` is the established pattern for a committed positive fixture whose extension deliberately keeps it outside the scanned set. The new fixture follows it exactly, which turns the bite proof from a one-time recorded plant into a permanent test."
---

<objective>
Fix the two self-contradictory relocation comments the code review found (WR-02, WR-03's
sibling class) and the third one it did not, then make the whole class mechanical.

`src/mcp/vice/r2000-symbol-roundtrip.test.ts:55` and `:467` both describe the chain from
the module directory to the repository root using the NEW starting segment spliced onto
the OLD intermediate segments — a chain that exists on disk in neither the pre-move nor
the post-move tree. `src/mcp/vice/r2000-regbits.test.ts:86-92` and `:218-221` claim to
"mirror the real repo shape" while naming the pre-move shape and building scratch
directories to match it. `.gitignore:41` names a canonical skills source that has not
existed since plan 16-01.

In a codebase whose `repo-root.ts` carries paragraph-length headers precisely because a
wrong hop-count comment once sent a reader to a directory that was not there, these are
real regressions, not nitpicks — and the code is correct in all three places, so nothing
but a comment-reading human or agent is harmed, which is exactly why no test caught them.

Purpose: PKG-03 is "orphaned planning references in source comments are removed or
repointed, guarded against reintroduction". Phase 16 built that guard for orphaned PHASE
pointers. A stale PATH chain in a comment is the same requirement's other half, and it has
no guard at all.

Output: three corrected comment sites, one behaviour-neutral scratch-segment rename, and a
calibrated guard with a committed positive fixture.

<tracer_note>
Task 1 is this plan's tracer: the guard is written first, demonstrated RED against the two
real violations in the real tree, the violations are fixed, and it goes GREEN — one class
of defect threaded end-to-end from detector to corpus to fix in a single commit. It is
production-quality and final; the fixture makes the bite proof permanent rather than a
one-time capture.
</tracer_note>
</objective>

## Flagged Assumptions (spec-less probe fallback)

This phase has no SPEC.md, so the deterministic edge probe ran over PKG-01..PKG-04 and
its `unclassified` rows are carried here as explicit assumptions rather than dropped:

- **PKG-03 / `unclassified` / `unresolved`.** The probe could not classify "orphaned
  planning references in source comments are removed or repointed, guarded against
  reintroduction" into an edge category and returned
  `probe: "unclassified — review manually"`. Assumption this plan proceeds on: PKG-03's
  phase-pointer half is verified TRUE with a recorded plant-and-revert
  (`comment-phase-pointers.test.ts`, 16/16, `16-PKG03-GATE-PROOF.md`), and the residual
  exposure is the **path-reference** half of the same sentence — a comment that points at
  a directory that no longer exists is orphaned on the same grounds as one that points at
  a cut phase. This plan treats that reading as in scope. The row stays `unresolved`: it
  is not auto-resolved with a backstop and is not silently dismissed. If a reviewer reads
  PKG-03 as phase-pointers only, this plan's guard is additive rather than required.

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-packaging-and-repo-shape/16-REVIEW.md
@.planning/phases/16-packaging-and-repo-shape/16-PKG03-GATE-PROOF.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end — a guard that fails on a half-swept repo-root chain, demonstrated red on the two real sites, then green</name>
  <reversibility rating="reversible">A new guard test plus a committed fixture plus two comment lines; deleting all three restores the prior state with no other consumer affected.</reversibility>
  <files>src/mcp/vice/hop-chain-comments.test.ts, src/mcp/vice/fixtures/planted-hop-chain-fixture.ts.txt, src/mcp/vice/r2000-symbol-roundtrip.test.ts</files>
  <behavior>
    - Given the corpus as committed today, the guard reports exactly two violations, both in `r2000-symbol-roundtrip.test.ts`, at the two line numbers where the chain comments sit.
    - Given the corpus after the two comments are corrected, the guard reports zero violations.
    - Given `r2000-answer-key.test.ts:34` — a chain comment that legitimately routes through the repository root with no pre-move segment — the guard does not flag it. This is a real in-tree negative control, not a synthetic one.
    - Given `repo-root.test.ts:107-119` — historical narration that names both roots on one line with a chain arrow, but no repository-root phrase on that same line — the guard does not flag it.
    - Given the committed positive fixture, the guard flags it, so the detector's teeth are asserted on every run rather than at one recorded moment.
    - Given a scan that finds fewer than three chain-shaped comment lines in the whole corpus, the guard fails as vacuous rather than passing.
    - Given a scanned file set smaller than the real module directory, the guard fails rather than silently shrinking.
  </behavior>
  <read_first>
    - `src/mcp/vice/r2000-symbol-roundtrip.test.ts` lines 45-70 and lines 455-480 — both comment sites and the code immediately below each: a `join(HERE, "..", "..", "..", …)` fixture path at the first, and `const REPO_ROOT = join(HERE, "..", "..", "..")` followed by `SKILLS_DIR`/`SCRIPTS_DIR` at the second. Confirm for yourself that the hop count is correct and only the comment is wrong.
    - `src/mcp/vice/r2000-answer-key.test.ts` lines 28-40 — the legitimate chain comment that must stay green. Read it before writing the pattern so the pattern is calibrated against it rather than tuned after a false positive.
    - `src/mcp/vice/repo-root.test.ts` lines 98-145 — the whole narration block plus the deliberate pre-move-shape test. This is the strongest false-positive candidate in the tree and it is explicitly out of scope; the guard must leave it green without an exemption.
    - `src/mcp/vice/comment-phase-pointers.test.ts` lines 1-70 (the WHY header and the extractor-choice rationale), lines 86-180 (`extractCommentSpans`, the character-state-machine extractor), lines 181-230 (`buildLineIndex`, `lineNumberFor`, `commentPhaseLines` — the per-line matching discipline and why per-span matching was measured wrong), lines 394-411 (`shippedTsModules()` and its existence assertion), and lines 500-615 (the fixture-driven positive control and the assertion that the fixture's extension keeps it out of the scanned set).
    - `src/mcp/vice/docs-dangling-refs.test.ts` lines 1-60 — the sibling guard's header style and the "a missing one FAILS rather than silently shrinking the scanned set" rule.
    - `src/mcp/vice/fixtures/planted-phase-pointer-fixture.ts.txt` — the whole file, as the shape to mirror for a committed positive fixture.
    - `src/mcp/vice/repo-root.ts` — the exported `repoRoot()` signature, and its header, which is the canonical example of why this comment class is treated as load-bearing here.
  </read_first>
  <action>
Write the guard first, prove it fires on the real tree, then fix the two sites — all in one commit, so the tree is green at every commit and the RED is captured as evidence rather than left in history.

**Step 1 — the guard.** Create `src/mcp/vice/hop-chain-comments.test.ts`.

Open with a WHY header in this codebase's established style, naming the defect concretely: the code review found both hop-count comments in `r2000-symbol-roundtrip.test.ts` describing a directory chain whose starting segment came from after this phase's relocation and whose intermediate segments came from before it — a chain that exists on disk in neither tree — while the three-hop code below each was correct the whole time, so no test could see it. Record the calibration in the header as a measurement, not a claim: across `src/mcp/vice`'s `*.ts` and `*.mts` files, exactly three comment lines contain both a chain arrow and the phrase naming the repository root; one of them (`r2000-answer-key.test.ts:34`) is legitimate and is the in-tree negative control; the other two are the violations. Also record what the guard deliberately does NOT do: it does not flag historical narration, because a comment describing a past move is accurate and load-bearing in this codebase, and per-line matching (not per-span) is what keeps `repo-root.test.ts`'s two-root narration line green without an exemption.

Scan set: every `*.ts` and `*.mts` file in the module directory, enumerated with `readdirSync(HERE)` — NOT `package.json`'s `files[]`, because the two violations live in test files which are deliberately unshipped. Follow `vice-proxy.test.ts`'s precedent for a `readdirSync(HERE)`-derived scan. Assert the enumerated set is at least fifty files, so the scan cannot silently shrink.

Extraction: reuse the comment-span-then-physical-lines discipline `comment-phase-pointers.test.ts` documents. You may copy its `extractCommentSpans`, `buildLineIndex` and `lineNumberFor` helpers into this file rather than importing them — this codebase's established convention is that each guard test owns its own scan end-to-end, and `comment-phase-pointers.test.ts`'s own header records that decision for the same helpers. Keep the copy verbatim so a future reader can diff the two.

Detection: a comment line is a violation when all three hold on that single physical line — it contains the chain arrow; it contains the phrase naming the repository root; and it contains at least one path segment rooted at this project's pre-relocation configuration directory. Define the third condition as a single frozen constant so there is exactly one place a future editor can read it. Report each violation as file plus line number plus the offending line text, and make the failure message tell the reader to correct the segment names, not to add an exemption.

Non-vacuity, each as its own named test: the scan enumerated at least fifty files; the extractor found at least three chain-shaped comment lines across the corpus (a total below that means the arrow-or-phrase pattern stopped matching and the guard has quietly gone blind); `r2000-answer-key.test.ts`'s legitimate chain line is among the lines the extractor saw AND is not among the violations; and the committed fixture below is flagged.

**Step 2 — the committed positive fixture.** Create `src/mcp/vice/fixtures/planted-hop-chain-fixture.ts.txt` holding a small, clearly-labelled snippet whose comment carries a half-swept chain of exactly the shape the guard must catch, plus a second comment that is legitimate historical narration naming both roots on one line without the repository-root phrase, so the fixture exercises both the positive and the near-miss in one file. Add a first line stating that the file is a deliberately-planted fixture, is not real source, and must keep a non-source extension. Add a guard test asserting the fixture path exists, that the guard's detector reports at least one violation against its contents, that it reports zero violations against the fixture's legitimate-narration line alone, and that the fixture's extension is neither `.ts` nor `.mts` so the `readdirSync` scan can never pick it up.

**Step 3 — demonstrate RED, then fix.** With the guard written and the two comments still untouched, run `cd src/mcp/vice && node --test hop-chain-comments.test.ts` and capture the failing output verbatim into the SUMMARY. It must name `r2000-symbol-roundtrip.test.ts` twice, with the two line numbers. A green run here means the pattern is wrong — fix the pattern, not the demonstration.

Then correct both comment lines in `src/mcp/vice/r2000-symbol-roundtrip.test.ts` so each names the real ancestors of the module directory, ending at the repository root: the module directory, then its parent, then its grandparent, then the root — matching the three `".."` hops in the code directly below. Change nothing else in that file, and change no code anywhere.

Re-run the guard (green), then run the wider gates: `cd src/mcp/vice && npm run typecheck`, `node --test r2000-symbol-roundtrip.test.ts`, and `VICE_REQUIRE_ACME=1 npm test`. Commit the guard, the fixture and the two corrected comments together.
  </action>
  <precondition>A real ACME assembler is reachable (`acme --version` prints its banner, or `$ACME_BIN` names one) — `VICE_REQUIRE_ACME=1` turns its absence into a hard failure rather than a skip.</precondition>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck && node --test hop-chain-comments.test.ts r2000-symbol-roundtrip.test.ts r2000-answer-key.test.ts repo-root.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The RED demonstration is recorded in the SUMMARY verbatim: with the guard added and the comments untouched, `cd src/mcp/vice && node --test hop-chain-comments.test.ts` exits non-zero and its output names `r2000-symbol-roundtrip.test.ts` at both line 55 and line 467. A recorded exit code of 0 means the guard is vacuous and the task is not done.
    - `cd src/mcp/vice && node --test hop-chain-comments.test.ts` exits 0 after the fix, reporting `# fail 0` and at least 5 tests.
    - Both corrected lines are present verbatim: `grep -c '^// `src/mcp/vice` -> `src/mcp` -> `src` -> repo root\.$' src/mcp/vice/r2000-symbol-roundtrip.test.ts` returns exactly 2.
    - The pre-move segment is gone from that file's chain comments: `grep -c -- '-> `.claude/mcp`' src/mcp/vice/r2000-symbol-roundtrip.test.ts` returns 0. <!-- planner-discipline-allow: -> `.claude/mcp` -->
    - Presence and absence are asserted together, never by a substring both forms satisfy: the two criteria above are both required, and neither may be replaced by a bare `grep -c 'repo root'`.
    - No code changed in the edited test file: `git diff -U0 src/mcp/vice/r2000-symbol-roundtrip.test.ts | grep -E '^[+-]' | grep -v '^[+-][+-]' | grep -vcE '^[+-]\s*//'` returns 0.
    - The in-tree negative control stays green and is asserted, not assumed: the guard has a test whose name names `r2000-answer-key.test.ts`, and it passes.
    - The out-of-scope file is untouched: `git diff --quiet -- src/mcp/vice/repo-root.test.ts` exits 0, and `cd src/mcp/vice && node --test repo-root.test.ts` reports 6 tests with `# fail 0`.
    - The fixture is committed and outside the scanned set: `test -f src/mcp/vice/fixtures/planted-hop-chain-fixture.ts.txt` exits 0, and `node -e 'const p="src/mcp/vice/fixtures/planted-hop-chain-fixture.ts.txt";if(/\.(ts|mts)$/.test(p))throw new Error("fixture extension would be scanned")'` exits 0.
    - The fixture is not shipped: `node -e 'const f=require("./src/mcp/vice/package.json").files;if(f.includes("fixtures")||f.some(x=>String(x).startsWith("fixtures")))throw new Error("fixtures added to files[]")'` exits 0, and `node scripts/check-npm-packages.mjs` exits 0.
    - Non-vacuity floors are real assertions in the file: `grep -c 'at least' src/mcp/vice/hop-chain-comments.test.ts` returns at least 2.
    - `cd src/mcp/vice && npm run typecheck` exits 0; `cd src/mcp/vice && VICE_REQUIRE_ACME=1 npm test` reports `# fail 0` and at least 2356 tests.
    - `git status --porcelain` shows no leftover scratch file from the demonstration.
  </acceptance_criteria>
  <done>A half-swept repository-root chain in a source comment fails `hop-chain-comments.test.ts`; the two real violations were captured RED before being fixed, both corrected lines name only real ancestors of the module directory, the in-tree negative control and the deliberate pre-move-shape test both stay green without any exemption, and a committed fixture keeps the detector's teeth asserted on every run.</done>
</task>

<task type="auto">
  <name>Task 2: The scratch trees and the ignore-file comment name the shape they claim to mirror</name>
  <reversibility rating="reversible">Two segment lists in one test file plus two comments plus one ignore-file comment line; behaviour-neutral by construction and revertible in a single hunk each.</reversibility>
  <files>src/mcp/vice/r2000-regbits.test.ts, .gitignore</files>
  <read_first>
    - `src/mcp/vice/r2000-regbits.test.ts` lines 80-125 — the whole drift-guard test: the comment block claiming to mirror the real repository shape, the scratch `skillsDir` built from a `src`/`skills` segment list, the scratch `mcpDir` built from a segment list that names the pre-move shape without its leading dot, the real `memmap.json` read three levels up, the planted-byte mutation, the copied generator, the dynamic import with a cache-busting query, the digest comparison, and the `rmSync` cleanup.
    - `src/mcp/vice/r2000-regbits.test.ts` lines 210-235 — the second, non-vacuity test that repeats the same scratch shape and refers back to the first test's comment for the reason.
    - `src/mcp/vice/r2000-regbits-gen.ts` lines 50-70 — `MEMMAP_PATH` and the comment above it recording that the skills tree moved to three levels up. This formula is what makes the scratch depth load-bearing and the segment names purely documentary; read it so the rename can be argued behaviour-neutral rather than asserted to be.
    - `.gitignore` lines 38-44 — the ignored generated-copy entry and the two comment lines above it, one of which names a canonical source directory that no longer exists.
    - `installer/scripts/sync-skills.mjs` lines 1-16 — `SRC` at :14, so the `.gitignore` comment can name the directory the script actually reads.
  </read_first>
  <action>
Make the two synthetic scratch trees and the ignore-file comment name the shape they claim to describe. Nothing here changes behaviour; all three are documentation defects in places this codebase treats documentation as load-bearing.

In `src/mcp/vice/r2000-regbits.test.ts`, change the scratch MCP-directory segment list in BOTH tests so its three segments match the module directory's real segments today — the same three segments the sibling scratch skills tree already uses for its own root. The depth must stay exactly three segments below the scratch root, because the copied generator resolves `memmap.json` three levels up from wherever it is imported from; state that reason in the comment you are fixing, so the next reader knows the depth is the contract and the names are the documentation.

Then fix the comment above the first scratch tree so it no longer claims to mirror "the real repo shape" while naming the pre-relocation shape. Keep the existing sentence about the generator's own hop-count formula, keep the parenthetical recording that plan 16-01 changed the hop count from two to three, and make the shape it names the current one. Fix the second test's back-reference comment the same way. Do not touch the planted-byte mutation, the digest comparison, the copied generator, the dynamic import, or the cleanup.

In `.gitignore`, rewrite the comment line that names the canonical skills source so it names the directory `installer/scripts/sync-skills.mjs` reads today. Leave the ignored path entry itself and the surrounding entries unchanged.

Run the affected suites plus the full gates and confirm the digest-drift and non-vacuity tests still pass — a rename that changed the resolved `memmap.json` path would surface here as a read failure or an unexpectedly equal digest, so a green run is the behaviour-neutrality proof.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck && node --test r2000-regbits.test.ts hop-chain-comments.test.ts && cd /home/henrik/dev/henrik/git/c64-re-tools && git check-ignore -q installer/skills && echo "ignore entry intact"</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/mcp/vice && node --test r2000-regbits.test.ts` exits 0 with `# fail 0`, and its two diagnostic lines reporting the committed and the planted-violation digests are both present and unequal — the drift guard is still non-vacuous after the rename.
    - The scratch segment lists no longer name the pre-move shape: `grep -c '"claude", *"mcp", *"vice"' src/mcp/vice/r2000-regbits.test.ts` returns 0. <!-- planner-discipline-allow: "claude", "mcp", "vice" -->
    - The scratch segment lists name the current shape, twice: `grep -c '"src", *"mcp", *"vice"' src/mcp/vice/r2000-regbits.test.ts` returns exactly 2.
    - The scratch depth is unchanged: `node -e 'const s=require("fs").readFileSync("src/mcp/vice/r2000-regbits.test.ts","utf8");const m=[...s.matchAll(/path\.join\(\s*tmpDir\s*,\s*"src"\s*,\s*"mcp"\s*,\s*"vice"\s*\)/g)];if(m.length!==2)throw new Error("expected exactly 2 three-segment scratch mcp dirs, found "+m.length)'` exits 0.
    - Only comments and the two segment lists changed: `git diff -U0 src/mcp/vice/r2000-regbits.test.ts | grep -E '^[+-]' | grep -v '^[+-][+-]' | grep -vE '^[+-]\s*//' | grep -vcE '"src"|"claude"'` returns 0.
    - The corrected comment names the current shape: `grep -c 'src/mcp/vice' src/mcp/vice/r2000-regbits.test.ts` returns at least 2, and the pre-move directory literal is gone from the file's comments: `grep -c -- '`.claude/mcp/vice`' src/mcp/vice/r2000-regbits.test.ts` returns 0. <!-- planner-discipline-allow: `.claude/mcp/vice` -->
    - `.gitignore`'s comment names the current source: `grep -c 'src/skills' .gitignore` returns at least 1, and `grep -c -- '.claude/skills' .gitignore` returns 0. <!-- planner-discipline-allow: .claude/skills -->
    - The ignore rule itself is unchanged and still effective: `git check-ignore -q installer/skills` exits 0, and `git diff -U0 .gitignore | grep -E '^\+' | grep -v '^\+\+' | grep -vc '^\+#'` returns 0 — every added line is a comment.
    - The new guard from Task 1 stays green over the edited files: `cd src/mcp/vice && node --test hop-chain-comments.test.ts` exits 0.
    - `cd src/mcp/vice && npm run typecheck` exits 0; `cd src/mcp/vice && VICE_REQUIRE_ACME=1 npm test` reports `# fail 0` and at least 2356 tests.
    - `node scripts/check-npm-packages.mjs`, `node scripts/check-skill-tool-coverage.mjs`, `node scripts/check-skill-fork-honesty.mjs` and `bash scripts/package.sh` all exit 0.
  </acceptance_criteria>
  <done>Both synthetic scratch trees use the module directory's real segment names at unchanged depth, their comments describe the shape they actually mirror, `.gitignore`'s comment names the source `sync-skills.mjs` reads, the drift guard's digest comparison still proves non-vacuous, and only comments and the two segment lists appear in the diffs.</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol / artifact | Kind | File |
|---|---|---|
| `src/mcp/vice/hop-chain-comments.test.ts` | new guard test file | new file |
| the frozen pre-relocation-root constant the detector matches on | new module-level constant inside the guard | `src/mcp/vice/hop-chain-comments.test.ts` |
| `extractCommentSpans` / `buildLineIndex` / `lineNumberFor` | verbatim copies of the sibling guard's helpers (per this codebase's "each guard owns its scan" convention) | `src/mcp/vice/hop-chain-comments.test.ts` |
| four non-vacuity tests (file-count floor, chain-line floor, in-tree negative control, fixture positive control) | new tests | `src/mcp/vice/hop-chain-comments.test.ts` |
| `src/mcp/vice/fixtures/planted-hop-chain-fixture.ts.txt` | new committed positive fixture (deliberately non-source extension) | new file |
| the two corrected hop-chain comment lines | comment edits | `src/mcp/vice/r2000-symbol-roundtrip.test.ts` |
| the two renamed scratch MCP-directory segment lists plus their two comments | segment-list + comment edits | `src/mcp/vice/r2000-regbits.test.ts` |
| the corrected canonical-source comment | comment edit | `.gitignore` |

No new dependency, npm script, `package.json` field, CI step or shipped file is introduced by this plan.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a source comment → a future reader or agent acting on it | A comment naming a directory that does not exist sends a reader or an autonomous agent to a wrong path; in this codebase hop-count comments are the documented control against a silent-wrong-directory failure. |
| the guard's verdict → the reviewer trusting it | A pattern that matches nothing is a green gate that measures nothing. |
| the test suite → the filesystem (scratch trees under the system temp directory) | Both edited tests create and delete real directories outside the repository. |

## STRIDE Threat Register

ASVS level 1 (opportunistic); blocking threshold: `high`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-36 | Spoofing | `r2000-symbol-roundtrip.test.ts:55,467` — a comment asserting a resolution chain that exists in no tree | medium | mitigate | Task 1 corrects both lines to name only real ancestors of the module directory, and the new guard makes the class mechanical with a committed positive fixture so the detector's teeth are asserted on every run. |
| T-16-37 | Spoofing | a new guard whose pattern silently stops matching (a vacuous green) | high | mitigate | Four non-vacuity tests: a fifty-file scan floor, a three-chain-line corpus floor, a real in-tree negative control (`r2000-answer-key.test.ts:34`) asserted seen-but-not-flagged, and a committed positive fixture asserted flagged. The RED capture against the two real violations is an acceptance criterion, so the guard cannot be accepted without having fired. |
| T-16-38 | Tampering | `r2000-regbits.test.ts`'s scratch-tree rename silently changing where the copied generator resolves `memmap.json` | medium | mitigate | The depth stays three segments below the scratch root, which is what `r2000-regbits-gen.ts:62`'s three-hop formula consumes; the digest-drift and non-vacuity tests are re-run and their two unequal diagnostic digests are an acceptance criterion, so a changed resolution surfaces as a failure rather than a silent pass. A diff-scoped criterion proves only comments and the two segment lists changed. |
| T-16-39 | Denial of Service | scratch directories left behind under the system temp directory by a failed run | low | accept | Pre-existing behaviour of these two tests, unchanged by this plan: both already create their scratch tree with `mkdtempSync` under the OS temp directory and remove it with `rmSync`. The rename does not add, move or widen any filesystem write, and the paths stay inside the process's own `mkdtemp` root. |
| T-16-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task exists in this plan; no dependency is added to either package. `check-npm-packages.mjs`'s existing assertion that the vice-mcp runtime dependency set is exactly `@mastra/mcp` plus `@mastra/core` is preserved and re-run as an acceptance criterion. No `[ASSUMED]`/`[SUS]` package is introduced, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
Run from `src/mcp/vice` unless stated otherwise:

1. `node --test hop-chain-comments.test.ts` — exit 0, at least 5 tests, `# fail 0`.
2. `node --test r2000-symbol-roundtrip.test.ts r2000-answer-key.test.ts repo-root.test.ts r2000-regbits.test.ts` — exit 0, `# fail 0`; `repo-root.test.ts` reports 6 tests.
3. `npm run typecheck` — exit 0.
4. `VICE_REQUIRE_ACME=1 npm test` — `# fail 0`, at least 2356 tests.
5. From the repository root: `git diff --quiet -- src/mcp/vice/repo-root.test.ts` exits 0.
6. From the repository root: `git check-ignore -q installer/skills` exits 0.
7. From the repository root: `node scripts/check-npm-packages.mjs`, `node scripts/check-skill-tool-coverage.mjs`, `node scripts/check-skill-fork-honesty.mjs`, `bash scripts/package.sh` — all exit 0.
8. The RED capture for Task 1 and the two unequal digest diagnostics for Task 2 are recorded in the SUMMARY.
</verification>

<success_criteria>
- Every hop-chain comment in the module directory names only real ancestors, and a half-swept chain now fails a committed guard.
- The guard was demonstrated RED against both real violations before being accepted, and its teeth stay asserted by a committed fixture rather than by a one-time capture.
- The in-tree negative control and the deliberate pre-move-shape test in `repo-root.test.ts` both stay green with no exemption added.
- The scratch-tree rename is proven behaviour-neutral by the drift guard's own unequal digests plus a diff-scoped criterion.
- The measured baseline is not regressed: full glob `# fail 0`, typecheck exit 0, all four repo-root validators exit 0.
</success_criteria>

<output>
Create `.planning/phases/16-packaging-and-repo-shape/16-09-SUMMARY.md` when done.
</output>
