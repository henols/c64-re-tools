---
phase: 42-the-text-format-parsers-and-their-two-binary-fixtures
plan: 14
type: execute
wave: 8
depends_on: [42-11, 42-12, 42-13]
gap_closure: true
files_modified:
  - docs/phase42-text-format-drift-citations.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [PARSE-01, PARSE-02, PARSE-03, PARSE-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The round's evidence lives in the phase's own citation record, one block per closed gap, each naming what changed, the refusal code or symbol it introduced, and the command whose output proves it."
    - "The final automated-gate figure is recorded with its failing file names beside the count, measured in an environment proven clean of brokers and emulators first -- a bare count with no environment claim is not a reading."
    - "All five formats are re-proven against a live genuine-stock reply AFTER the parser and handler changes, not only before them, and the run's teardown is verified by the corrected method."
    - "Every item this round deliberately did not close is restated as still open -- the profiler start capability, the two manual-only verifications, the RAM-execute hardware evidence, and the behavioural half of the decimal-separator finding -- so a green round cannot be read as closing them."
    - "`PARSE-03` and `PARSE-04` are returned to Complete in REQUIREMENTS.md only after the criteria that demoted them are re-measured green in this plan's own verification, and the two IDs that were never demoted are left exactly as they are."
    - "`parseAccessMap` refuses an empty or whitespace-only payload by name rather than decoding it as a zero-entry access map -- an empty reply and a real capture recording no accesses are different facts."
    - "The access-map parser decides header and glyph equality by exact byte-for-byte string comparison over the decoded text, never a case-folded or normalized one, so a header VICE respells is drift and refuses."
  artifacts:
    - path: "docs/phase42-text-format-drift-citations.md"
      provides: "The gap-closure evidence section: one block per closed gap, the final measured gate figure with its failing file names, the re-run live evidence, and the restated open items"
      contains: "MEASURED"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PARSE-03 and PARSE-04 returned to Complete in both the checkbox list and the traceability table, on measured evidence"
      contains: "PARSE-03"
  key_links:
    - from: "docs/phase42-text-format-drift-citations.md"
      to: ".planning/REQUIREMENTS.md"
      via: "the requirement restoration cites the same measured commands the record carries, so the status flip has evidence behind it rather than a claim"
      pattern: "PARSE-04"
  prohibitions:
    - statement: "A requirement must never be returned to Complete on the strength of a plan having run -- the criteria that demoted it are re-measured in this plan's own verification first, and a criterion that does not come back green leaves its requirement Pending."
      status: unverified
    - statement: "A claim must never be labelled measured without the run behind it -- every figure carries its command, its environment claim, and for a live claim the binary, the version and the date."
      status: unverified
    - statement: "A gap-closure round must never record as closed anything it did not close -- every deliberately-excluded item is restated as open in the same document that records the closures, so a green round cannot be misread as a clean bill of health."
      status: unverified
---

<objective>
Record what this round measured, and restore the two requirement IDs the verification demoted —
in that order, with the restoration conditional on the measurement.

Four gaps were closed across plans 42-10 through 42-13: the unchecked cast that let a drifted
`io` reply return `ok: true` with absent required fields; the sprite-table requirement that made
every non-VIC-II address refuse with a misleading reason; the cold-profiler reply that read as a
parser defect; the computed binary-identity disagreement that reached nobody; and the live
harness whose teardown claim could not observe the process it spawned.

Purpose: leave the phase's record honest in both directions — what closed, and what did not.
Output: an evidence section in the phase's own citation record, and two requirement IDs
returned to Complete on measured grounds.
</objective>

<worktree_carve_out>
This plan writes `.planning/REQUIREMENTS.md`, which is **not** covered by the worktree carve-out
— only `.planning/STATE.md` and `.planning/ROADMAP.md` are, and this plan touches neither. It
therefore runs with worktree isolation ON as normal. No deletions, so `cleanup-wave` will not
refuse this branch.
</worktree_carve_out>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-the-text-format-parsers-and-their-two-binary-fixtures/42-VERIFICATION.md
@.planning/phases/42-the-text-format-parsers-and-their-two-binary-fixtures/42-10-SUMMARY.md
@.planning/phases/42-the-text-format-parsers-and-their-two-binary-fixtures/42-11-SUMMARY.md
@.planning/phases/42-the-text-format-parsers-and-their-two-binary-fixtures/42-12-SUMMARY.md
@.planning/phases/42-the-text-format-parsers-and-their-two-binary-fixtures/42-13-SUMMARY.md

Deliberately NOT `@`-included: `.planning/PROJECT.md`, `.planning/ROADMAP.md`,
`.planning/STATE.md`, `42-RESEARCH.md`.
</context>

<plan_decisions>
**The record is appended to the phase's existing document, not given a new one.** Plan 42-06
created `docs/phase42-text-format-drift-citations.md` and 42-09 appended its live section to it.
A third document would split the phase's evidence across files that a later reader has no reason
to know about.

**The requirement restoration is the last task, and it is conditional.** The verification
addendum demoted `PARSE-03` (named directly by the blocker) and `PARSE-04` (on the same evidence
plus the cold-profiler message), and deliberately did NOT use the blunt all-IDs revert.
`PARSE-01` and `PARSE-02` stayed Complete throughout and are not touched. The restoration
happens only after this plan's own verification block re-measures the criteria that caused the
demotion — the drift refusal for all five formats, and a capability answer that never reads as a
defect here. If either comes back red, the corresponding ID stays Pending and the plan says so
in its SUMMARY rather than flipping it anyway.

**The live suite is re-run here, after the changes, not merely inherited from 42-12.** Plan
42-12 ran it in an earlier wave of this round; plans 42-11 and 42-13 then changed the io parser's section handling, the
profile parser's recognition order and all five handlers' answer shape. A live reading taken
before those changes is not a reading of the tree this round ships. The re-run also exercises
42-12's corrected teardown method against the final tree.

**Out of scope stays out of scope, and is restated rather than dropped.** Four items were
excluded by the round's own boundary and one more by a task-level decision: the underlying
inability to start VICE's profiler from this tree; the two manual-only verifications (no
genuinely disabled-cpuhistory build on this host, and the register command's two degradation
strings still source-traced); the RAM-execute hardware evidence, declared-accepted scope from
this phase's first plan; and the behavioural half of the decimal-separator finding. The
validation artifact's own draft status is likewise left for its own command. Naming them in the
closing record is what stops the next reader inferring closure from a green round.

**Canon-referral drop (prohibition probe, stage 2):** OWASP/injection-class constraints are
owned by the project's security canon and by `buildTextCommand()`. Not minted here.
</plan_decisions>

<probe_edges>
This plan authors the last two of the eleven surfaced edges into `must_haves.truths`, both
resolved-explicit: **PARSE-01/empty** (an empty or whitespace-only access-map reply is refused by
name, never decoded as a zero-entry map) and **PARSE-01/encoding** (header and glyph equality is
byte-for-byte over the decoded text, never case-folded or normalized).

**Round-level tally, as the no-silent-drop rule requires:** 11 applicable, 11 authored, 0
backstop markers, 0 unresolved, 0 dismissed. Distribution — 42-10 five (PARSE-03/empty,
PARSE-03/encoding, PARSE-03/ordering, PARSE-02/idempotency, PARSE-02/concurrency), 42-11 one
(PARSE-03/adjacency), 42-12 none (its subject is process teardown, stated explicitly there),
42-13 three (PARSE-04/empty, PARSE-04/adjacency, PARSE-04/ordering), 42-14 two (PARSE-01/empty,
PARSE-01/encoding). Every one is a plain `must_haves.truths` string; none needed a backstop
marker and none was left unresolved.
</probe_edges>

<tasks>

<task type="auto">
  <name>Task 1: The round's evidence, measured on the final tree and recorded with what it does not cover</name>
  <precondition>No `vice-broker` and no `x64sc` process is alive before this task runs — `pgrep -af '[v]ice-broker|[x]64sc'` prints nothing. A live broker deterministically reddens an unrelated ordering assertion, and every figure this task records would be void.</precondition>
  <files>docs/phase42-text-format-drift-citations.md</files>
  <read_first>
    - docs/phase42-text-format-drift-citations.md — in full: its block structure, its MEASURED and source-traced labelling convention, Block 9's cold-profiler finding, and the live-evidence section 42-09 appended
    - .planning/phases/42-the-text-format-parsers-and-their-two-binary-fixtures/42-10-SUMMARY.md, 42-11-SUMMARY.md, 42-12-SUMMARY.md, 42-13-SUMMARY.md — the verbatim refusal messages, manifest check outputs, teardown observations and gate figures each plan recorded, which this record transcribes rather than re-derives
    - .planning/phases/42-the-text-format-parsers-and-their-two-binary-fixtures/42-VERIFICATION.md — the two frontmatter gaps, the Anti-Patterns table, and the orchestrator addendum in full, including the self-resolved disposition finding this round must NOT claim credit for
    - .planning/phases/42-the-text-format-parsers-and-their-two-binary-fixtures/42-VALIDATION.md — the manual-only verifications table, whose two rows must be restated as still standing
    - .planning/WINDOWS.md — the open entry for `vice_profile_flat`'s missing profiler start, so the record states it as open in its own terms
    - src/mcp/vice/textmon-memmap.ts — `parseAccessMap`'s empty-response refusal and its header comparison, for the two access-map facts this plan's must-haves assert
  </read_first>
  <action>
    Append a gap-closure section to the phase's existing citation record. Keep the document's own
    block structure and its labelling convention: a claim carrying a run is labelled MEASURED and
    names the command; a claim read from source stays labelled source-traced.

    Write one block per closed gap, five blocks. Each names: the finding id from the code review,
    what the defect was in one sentence, what changed, the new symbol or refusal code it
    introduced, and the command whose output proves it — transcribing the verbatim messages the
    four SUMMARYs recorded rather than paraphrasing them. The five are: the unchecked decoded-state
    cast and its completeness gate; the unconditional sprite-table requirement and its chip gate;
    the cold-profiler reply and its named state; the computed-and-dropped identity disagreement
    and its renderer; and the live harness's teardown assertion and its planted control.

    Write one block for the final automated-gate reading. Record the command, the fact that
    `pgrep -af '[v]ice-broker|[x]64sc'` printed nothing immediately before it, the total test count,
    the pass count, the fail count, AND the names of the failing files. State that the figure is
    the documented pre-existing floor and that it is a floor, not a target — a run reporting zero
    would mean the gate was not the gate. Do not record a count without its failing file names;
    a bare number was exactly what made an earlier reading unusable.

    Write one block for the live re-run. Dial all five formats against genuine stock VICE at the
    pinned absolute path, after this round's parser and handler changes, and record for each: the
    command, the measured shape of the result, and the capability verdict. Record the binary path,
    the version string the run reported, and the date. Record the teardown outcome using the
    corrected method — the broker child's pid, the scratch-scoped sweep result, and the scratch
    directory's removal — not a service-unit status.

    Write one closing block titled so it cannot be mistaken for a closure list: the items this
    round did NOT close, each stated as still open with one line of why. It must name all five:
    the inability of any tool in this tree to start VICE's profiler, still recorded as an open
    window; the two manual-only verifications from the phase's validation record, still standing;
    the RAM-execute hardware evidence, still zero over the searched denominator and covered only
    by a declared-synthetic case; the behavioural half of the decimal-separator finding,
    deliberately not taken with its documentation half recorded instead; and the phase validation
    artifact's own draft status, which belongs to its own command and was not folded in here.

    Add one sentence, in that same closing block, stating what this round did NOT need to do: the
    verification's second frontmatter gap — undispositioned review findings — was self-resolved by
    the verification report itself naming all five ids, re-measured green before this round began,
    and no plan here chased it. State it so a later reader does not re-open it as an oversight.

    Change nothing already in the document. The four source-traced labels and the earlier live
    blocks stay exactly as written.
  </action>
  <verify>
    <automated>pgrep -af '[v]ice-broker|[x]64sc' ; echo "PRECHECK_EXIT=$?"</automated>
    <fails_when>any process line is printed, or PRECHECK_EXIT is 0 — a match means a broker or emulator is alive and every figure recorded now is void</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test textmon-memmap.test.ts textmon-cpuhistory.test.ts textmon-backtrace.test.ts textmon-profile.test.ts textmon-registers.test.ts textmon-seam.test.ts text-capability-probe.test.ts text-protocol.test.ts text-tools.test.ts 2>&1 | tail -25</automated>
    <fails_when>the summary reports a non-zero "fail" count, or the reported "tests" total is below 246 — the phase's own sweep must be green and must not have shrunk</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck > /tmp/gsd-42-14-tc.txt 2>&1; echo "EXIT=$?"; tail -20 /tmp/gsd-42-14-tc.txt</automated>
    <fails_when>the printed EXIT is not 0, or any line contains "error TS"</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated > /tmp/gsd-42-14-gate.txt 2>&1; echo "EXIT=$?"; grep -aE '^[^A-Za-z0-9]*(tests|suites|pass|fail|cancelled|skipped) [0-9]+$' /tmp/gsd-42-14-gate.txt; sed -n '/failing tests:/,$p' /tmp/gsd-42-14-gate.txt | grep -aoE '[A-Za-z0-9_.-]+\.test\.[a-z]+' | sort -u</automated>
    <fails_when>the printed fail count is greater than 3, or the printed list of failing files contains any name other than `anno-import.test.ts` and `anno-register.test.ts` — the floor is 3, never 0</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && VICE_LIVE_STOCK_BIN=/usr/bin/x64sc node --test text-monitor-live.test.ts > /tmp/gsd-42-14-live.txt 2>&1; echo "EXIT=$?"; grep -aE '^[^A-Za-z0-9]*(tests|suites|pass|fail|cancelled|skipped) [0-9]+$' /tmp/gsd-42-14-live.txt; tail -25 /tmp/gsd-42-14-live.txt</automated>
    <fails_when>the printed EXIT is not 0, or the printed fail count is not 0, or the printed skipped count is not 0 — a skipped case means the opt-in guard rejected the pinned binary and nothing was re-measured on the final tree</fails_when>
    <automated>pgrep -af '[v]ice-broker|[x]64sc' ; echo "POSTCHECK_EXIT=$?"; ls -d /tmp/text-monitor-live-* 2>/dev/null; echo "SCRATCH_LS_EXIT=$?"</automated>
    <fails_when>any process line is printed, or POSTCHECK_EXIT is 0, or a scratch directory path is printed, or SCRATCH_LS_EXIT is 0</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac 'source-traced' docs/phase42-text-format-drift-citations.md</automated>
    <fails_when>the printed count is less than 4 — the four source-traced labels the earlier plans wrote must survive this append unchanged</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && for t in incomplete-decoded-state unsupported-chip profiling-not-started textCapabilityIdentityWarning; do printf '%s=' "$t"; grep -ac "$t" docs/phase42-text-format-drift-citations.md; done</automated>
    <fails_when>any printed count is 0 — a closed gap has no evidence block naming the symbol or refusal code it introduced</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-dangling-refs.test.ts docs-linerefs.test.ts comment-phase-pointers.test.ts shipped-modules.test.ts 2>&1 | tail -20</automated>
    <fails_when>the summary reports a non-zero "fail" count — the appended record names a path that does not resolve or a citation that no longer holds</fails_when>
  </verify>
  <acceptance_criteria>
    - The record carries five gap blocks, one per closed finding, each naming the finding id, what changed, the symbol or refusal code introduced, and the proving command.
    - Each of `incomplete-decoded-state`, `unsupported-chip`, `profiling-not-started` and `textCapabilityIdentityWarning` appears at least once in the document.
    - The gate block records command, clean-environment claim, total, pass, fail AND the failing file names, and states the figure is a floor rather than a target.
    - The live block records all five formats with their commands and measured shapes, the binary path, the reported version string and the date, and the teardown outcome expressed as broker pid, scratch-scoped sweep result and scratch-directory removal.
    - The closing block names all five not-closed items as still open, each with one line of why.
    - The closing block states that the undispositioned-findings gap was self-resolved before this round and that no plan chased it.
    - The four pre-existing `source-traced` labels are still present.
    - The phase's nine-file sweep reports `fail 0` with at least 246 tests; `npm run typecheck` exits 0; `npm run test:automated` reports at most 3 failures, all `anno-*`.
    - The live suite exits 0 with `fail 0` and zero skipped, and the environment is clean of brokers, emulators and scratch directories afterwards.
    - `node --test docs-dangling-refs.test.ts docs-linerefs.test.ts comment-phase-pointers.test.ts shipped-modules.test.ts` reports `fail 0`.
  </acceptance_criteria>
  <done>The phase's own record states what this round measured on the tree it ships, and states — in the same place — every thing it deliberately left open.</done>
</task>

<task type="auto">
  <name>Task 2: PARSE-03 and PARSE-04 returned to Complete, on the evidence Task 1 measured</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — the `PARSE-01` through `PARSE-04` checkbox entries and the traceability table rows for all four, noting which two currently read `[ ]` / `Pending` and which two read `[x]` / `Complete`
    - .planning/phases/42-the-text-format-parsers-and-their-two-binary-fixtures/42-VERIFICATION.md — the addendum's "Requirement scoping applied" section, which states exactly why each of the two was demoted and on what evidence
    - docs/phase42-text-format-drift-citations.md — as Task 1 left it: the gap blocks and the measured figures this restoration cites
    - the console output of Task 1's own verification commands — the measured results this task's decision rests on
  </read_first>
  <action>
    Flip the two demoted requirement IDs back, and only those two, and only if Task 1's
    verification came back green.

    First state the decision explicitly against the two demotion causes. `PARSE-03` was demoted
    because a drifted `io` format did not fail loudly; the criterion is met when a dropped,
    renamed and empty decoded-prose block each refuse by name and the other four formats' existing
    controls still pass. `PARSE-04` was demoted on that same evidence plus the cold-profiler
    message reading as a parse error; the criterion is met when the cold state is a named state,
    the identity disagreement reaches the caller, and neither reads as a defect in this project.
    Confirm both against Task 1's measured output before editing anything. If either is not met,
    leave that ID at `Pending`, make no edit for it, and say so plainly in the SUMMARY — a status
    flip on the strength of a plan having run is exactly what the round's own prohibition forbids.

    Then make the edit as a scoped replacement, never a whole-file rewrite. Change the `PARSE-03`
    and `PARSE-04` checkbox markers from unchecked to checked, leaving each requirement's prose
    byte-identical. Change their two traceability-table rows from `Pending` to `Complete`, keeping
    the row's existing pipe spacing exactly as the surrounding rows have it. Touch nothing else:
    `PARSE-01` and `PARSE-02` were never demoted and their entries and rows stay exactly as they
    are, and no other requirement family in the file is edited.

    Do not touch `.planning/ROADMAP.md` and do not touch `.planning/STATE.md` in this task — both
    are outside this plan's declared files and the first is subject to a worktree restriction this
    plan does not carry. Do not invoke any requirement-status helper that operates on a whole
    phase: the demotion was deliberately scoped to two IDs and the restoration must be scoped the
    same way.

    Re-read the file after editing and confirm the coverage counts and any per-family tallies the
    file carries still add up, repairing only what your own edit changed.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -aE '^\- \[[ x]\] \*\*PARSE-0[1-4]\*\*' .planning/REQUIREMENTS.md | sed 's/\(^.\{18\}\).*/\1/'</automated>
    <fails_when>fewer than 4 lines are printed, or any printed line shows an unchecked marker — all four PARSE ids must read as checked once the restoration is made</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -aE '^\| PARSE-0[1-4] \|' .planning/REQUIREMENTS.md</automated>
    <fails_when>fewer than 4 rows are printed, or any printed row contains "Pending", or any row's spacing differs from the pattern `| PARSE-0N | Phase 42 | Complete |`</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && git diff --stat -- .planning/REQUIREMENTS.md</automated>
    <fails_when>the printed stat names any file other than `.planning/REQUIREMENTS.md`, or reports more than 6 changed lines — a scoped four-line flip cannot legitimately rewrite more</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && git status --porcelain | grep -aE '^( M|M ) \.planning/(ROADMAP|STATE)\.md' ; echo "ROADMAP_STATE_TOUCHED=$?"</automated>
    <fails_when>ROADMAP_STATE_TOUCHED is 0 — this plan modified ROADMAP.md or STATE.md, neither of which it declares</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-review-disposition.test.ts 2>&1 | tail -15</automated>
    <fails_when>the summary reports a non-zero "fail" count — a review finding lost its disposition, or the phase directory changed in a way the disposition scan reads as regression</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated > /tmp/gsd-42-14-gate2.txt 2>&1; echo "EXIT=$?"; grep -aE '^[^A-Za-z0-9]*(tests|suites|pass|fail|cancelled|skipped) [0-9]+$' /tmp/gsd-42-14-gate2.txt; sed -n '/failing tests:/,$p' /tmp/gsd-42-14-gate2.txt | grep -aoE '[A-Za-z0-9_.-]+\.test\.[a-z]+' | sort -u</automated>
    <fails_when>the printed fail count is greater than 3, or the printed list of failing files contains any name other than `anno-import.test.ts` and `anno-register.test.ts` — the floor is 3, never 0</fails_when>
  </verify>
  <acceptance_criteria>
    - The `PARSE-03` and `PARSE-04` checkbox entries read as checked; their prose text is byte-identical to before the edit.
    - The `PARSE-03` and `PARSE-04` traceability rows read `Complete` and keep the same pipe spacing as the `PARSE-01` and `PARSE-02` rows.
    - The `PARSE-01` and `PARSE-02` entries and rows are unchanged.
    - `git diff --stat` for this task names only `.planning/REQUIREMENTS.md` and reports at most 6 changed lines.
    - Neither `.planning/ROADMAP.md` nor `.planning/STATE.md` was modified.
    - The SUMMARY states, for each of the two IDs, the specific measured result that justified the flip — or, if a criterion came back red, that the ID was deliberately left `Pending` and why.
    - `node --test docs-review-disposition.test.ts` reports `fail 0`.
    - `npm run test:automated` reports at most 3 failures, all in `anno-import.test.ts` / `anno-register.test.ts`.
  </acceptance_criteria>
  <done>The two IDs the verification demoted are back at Complete because the criteria that demoted them were re-measured green, and the record says which measurement bought which flip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a measured figure → this project's own record | A number written here becomes a claim later phases and later audits rely on without re-measuring. |
| live emulator run → the developer's host | A real broker daemon and a real emulator are spawned on the machine running this plan. |
| this plan → the requirements ledger | A status flip here is what a later milestone audit reads as evidence a requirement is met. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-49 | Repudiation (a status claim without the evidence behind it) | the `PARSE-03` / `PARSE-04` restoration | high | mitigate | The flip is the last task, gated on Task 1's own re-measurement of the two demotion causes, scoped to exactly the two demoted ids, and bounded by a diff-size check. A criterion that comes back red leaves its id `Pending`. |
| T-42-50 | Tampering (of the project's own record) | a measured label attached to an unmeasured claim | high | mitigate | Every figure carries its command and its clean-environment claim; the gate block records failing file names beside the count; the live block records binary, version and date; a check asserts the four pre-existing source-traced labels survive the append. |
| T-42-51 | Repudiation | a green round misread as a clean bill of health | high | mitigate | The closing block names all five deliberately-excluded items as still open, and a criterion requires each to be present. The self-resolved disposition gap is named as self-resolved rather than claimed. |
| T-42-52 | Denial of Service (self-inflicted) | the live re-run leaking a broker, an emulator or a scratch directory | high | mitigate | Pre- and post-run checks cover both process names and the scratch directories, using the corrected method plan 42-12 landed, and the run itself asserts teardown internally. |
| T-42-53 | Information Disclosure | host binary paths in a committed record | low | accept | The record names this host's VICE binary paths, already named throughout this repository's documentation and fixture sidecars. No credential, port or token. |
| T-42-SC | Tampering | npm/pip/cargo installs | low | accept | No package install. The emulator is used at an explicitly pinned absolute path and is never installed, fetched or built by any task here. |
</threat_model>

<verification>
1. `pgrep -af '[v]ice-broker|[x]64sc'` prints nothing, before and after the live re-run; no `/tmp/text-monitor-live-*` survives.
2. The phase's nine-file sweep — `fail 0`, at least 246 tests.
3. `npm run typecheck` — exit 0, no `error TS`.
4. `VICE_LIVE_STOCK_BIN=/usr/bin/x64sc node --test text-monitor-live.test.ts` — exit 0, `fail 0`, zero skipped.
5. `node --test docs-dangling-refs.test.ts docs-linerefs.test.ts comment-phase-pointers.test.ts shipped-modules.test.ts docs-review-disposition.test.ts` — `fail 0`.
6. `npm run test:automated` — at most 3 failures, all `anno-*`. Never assert 0; the floor is 3.
7. `git status --porcelain` shows no modification to `.planning/ROADMAP.md` or `.planning/STATE.md`.
8. Never run bare `npm test`.
</verification>

<success_criteria>
- Every closed gap has an evidence block naming the symbol or code it introduced and the command that proves it.
- Every deliberately-excluded item is restated as open in the same document.
- The final gate figure is recorded with its failing file names and its clean-environment claim.
- All five formats are re-proven live on the final tree, with teardown verified by the corrected method.
- `PARSE-03` and `PARSE-04` are Complete on measured grounds, and nothing else in the ledger moved.
</success_criteria>

## Artifacts this phase produces

Round-level list, covering all five gap-closure plans (42-10 through 42-14):

**New exported symbols**
- `REQUIRED_IO_DECODED_KEYS` — `src/mcp/vice/textmon-registers.ts` (42-10)
- `PROFILING_NOT_STARTED_TEXT` — `src/mcp/vice/textmon-profile.ts` (42-13)
- `textCapabilityIdentityWarning(identity, brokerIdentity)` — `src/mcp/vice/text-capability-probe.ts` (42-13)

**New non-exported module constants**
- `VIC_II_CHIP_NAME` — `src/mcp/vice/textmon-registers.ts` (42-11)

**New refusal codes (closed unions)**
- `"incomplete-decoded-state"` — `IoRegistersRefusalCode` (42-10)
- `"unsupported-chip"` — `IoRegistersRefusalCode` (42-11)
- `"profiling-not-started"` — `FlatProfileRefusalCode` (42-13)

**New published output property**
- `identityWarning` — optional string on the `outputSchema` of `vice_memmap_show`,
  `vice_cpu_history`, `vice_profile_flat`, `vice_backtrace`, `vice_io_registers`; in no
  `required` list (42-13)

**New test-harness symbols**
- `HarnessReport.brokerPid`, `HarnessReport.strayPidsMatchingScratch`,
  `HarnessReport.scratchDirRemoved`, a command-line pid-matching helper, and a boolean return on
  `stopBroker()` — `src/mcp/vice/text-monitor-live.test.ts` (42-12)

**New declared seam consumer**
- `text-tools.test.ts` as an import consumer of the flat-profile owner in
  `src/mcp/vice/textmon-seam.test.ts` (42-13)

**New document section**
- The gap-closure evidence section of `docs/phase42-text-format-drift-citations.md` (42-14)

**No new files anywhere in the round. No deletions anywhere in the round.**

<output>
Create `.planning/phases/42-the-text-format-parsers-and-their-two-binary-fixtures/42-14-SUMMARY.md` when done.
Record in it: the final `npm run test:automated` figure with its failing file names and the
clean-environment check that preceded it; the live re-run's per-format measured values with
binary, version and date; the teardown observation in the corrected three-part form; for each of
`PARSE-03` and `PARSE-04` the specific measured result that justified the flip (or the reason it
was left `Pending`); and the verbatim list of items restated as still open.
</output>
