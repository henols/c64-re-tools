---
phase: 42-the-text-format-parsers-and-their-two-binary-fixtures
plan: 16
type: execute
wave: 10
depends_on: [42-15]
gap_closure: true
files_modified:
  - src/mcp/vice/text-monitor-live.test.ts
  - docs/phase42-text-format-drift-citations.md
  - .planning/REQUIREMENTS.md
  - .planning/todos/pending/2026-09-09-io-degradation-probe-cached-under-a-binary-wide-key.md
  - .planning/todos/completed/2026-09-09-io-degradation-probe-cached-under-a-binary-wide-key.md
  - .planning/STATE.md
autonomous: true
requirements: [PARSE-04]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`io` is dialed against TWO different addresses inside ONE live session against genuine stock VICE, on the post-fix tree, and the second answer is proven derived from the second reply -- the verdict for the second dial reports it was not served from cache and carries the second reply's own text."
    - "The round's evidence lives in the phase's own citation record: one block naming the finding id, what the defect was, what changed, every symbol it introduced, the remedy NOT chosen and what that one would have cost, and the commands whose output proves it."
    - "The final automated-gate figure is recorded with its failing file names beside the count, measured in an environment proven clean of brokers and emulators first -- a bare count with no environment claim is not a reading."
    - "Every item this round deliberately did not close is restated as still open -- the profiler start capability, the two manual-only verifications, the RAM-execute hardware evidence, the behavioural half of the decimal-separator finding, and the phase validation artifact's own draft status -- so a green round cannot be read as closing them."
    - "`PARSE-04` returns to Complete in REQUIREMENTS.md only after the criterion that demoted it is re-measured green in this plan's own verification; `PARSE-01`, `PARSE-02` and `PARSE-03` were not demoted this round and are left byte-identical."
    - "The CR-02 disposition record moves from pending to completed carrying its own resolution section, and STATE.md's Deferred Items ledger row and its open-count prose move with it in the same commit, so the two-directional ledger guard is green in both directions rather than green by accident."
    - "The live run leaves no broker, no emulator and no scratch directory behind, asserted by the corrected three-part teardown method -- broker pid gone, scratch-scoped stray sweep empty, scratch directory removed."
  artifacts:
    - path: "src/mcp/vice/text-monitor-live.test.ts"
      provides: "The live two-address `io` control inside the existing five-format session: a second address dialed against the same resolved identity, proven judged on its own reply"
      contains: "fromCache"
    - path: "docs/phase42-text-format-drift-citations.md"
      provides: "The round-2 evidence section: the CR-02 closure, the live two-address measurement, the final gate figure with failing file names, and the restated open items"
      contains: "MEASURED"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PARSE-04 returned to Complete in both the checkbox list and the traceability table, on measured evidence"
      contains: "PARSE-04"
    - path: ".planning/todos/completed/2026-09-09-io-degradation-probe-cached-under-a-binary-wide-key.md"
      provides: "The closed disposition record with its own resolution section naming the remedy chosen and the commands that proved it"
      contains: "Resolution"
    - path: ".planning/STATE.md"
      provides: "The Deferred Items ledger with the CR-02 row removed and the open-count prose corrected in the same edit"
      contains: "Deferred Items"
  key_links:
    - from: "docs/phase42-text-format-drift-citations.md"
      to: ".planning/REQUIREMENTS.md"
      via: "the requirement restoration cites the same measured commands the record carries, so the status flip has evidence behind it rather than a claim"
      pattern: "PARSE-04"
    - from: ".planning/todos/completed/2026-09-09-io-degradation-probe-cached-under-a-binary-wide-key.md"
      to: ".planning/STATE.md"
      via: "the two-directional deferred-ledger guard: a completed todo must have no Pending row, and every pending todo must have one"
      pattern: "io-degradation-probe-cached-under-a-binary-wide-key"
    - from: "src/mcp/vice/text-monitor-live.test.ts"
      to: "src/mcp/vice/text-capability-probe.ts"
      via: "the live control asks the probe entry point for a verdict on a SECOND io address under the identity the first dial already probed, and asserts the answer was not served from cache"
      pattern: "probeTextCapability"
  prohibitions:
    - statement: "A requirement must never be returned to Complete on the strength of a plan having run -- the criterion that demoted it is re-measured in this plan's own verification first, and a criterion that does not come back green leaves its requirement Pending and says so."
      status: unverified
    - statement: "A claim must never be labelled measured without the run behind it -- every figure carries its command, its environment claim, and for a live claim the binary, the version and the date."
      status: unverified
    - statement: "A gap-closure round must never record as closed anything it did not close -- every deliberately-excluded item is restated as open in the same document that records the closure, so a green round cannot be misread as a clean bill of health."
      status: unverified
    - statement: "A live emulator run must never be left running -- a stray broker reddens an unrelated gate deterministically for every later run on this host, and a reading taken beside one is discarded rather than recorded."
      status: unverified
    - statement: "An external tool must never be installed to make a run possible -- a missing binary is refused by name with the remedy in the message, and the user installs it."
      status: unverified
---

<objective>
Prove the CR-02 fix against a real emulator, record what was measured, and only then return
`PARSE-04` to Complete and close its disposition record.

Plan 42-15 changed which response `io`'s chip-degradation classification reads. That is a
behavioural change to what reaches the caller, so it earns live evidence on this host's genuine
stock VICE rather than an argument from unit tests: two different addresses dialed inside one
session, against one resolved binary identity, each judged on its own reply.

Purpose: leave the phase's record honest in both directions — what this round closed, and what
it deliberately did not.
Output: a live two-address control, an evidence section, one requirement returned to Complete on
measured grounds, and a disposition record closed together with its ledger row.
</objective>

<worktree_carve_out>
**This plan carries the stock per-plan carve-out `USE_WORKTREES_FOR_PLAN=false`** — it delivers
`.planning/STATE.md` content (the Deferred Items ledger row and its open-count prose), which
worktree executors may not touch and which the worktree commit path strips. It runs on `main`
with isolation OFF, the way `42-06` did for its ROADMAP deliverable.

A second consequence follows from the same carve-out and is deliberate: Task 3 moves a file out
of `.planning/todos/pending/`, which appears in a diff as a deletion, and `cleanup-wave` refuses
any branch whose diff contains one. With no worktree branch to merge, there is no `cleanup-wave`
step to refuse it. Do not work around this by leaving the todo in place and annotating it — the
two-directional ledger guard reads the directory, not the file's contents.
</worktree_carve_out>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-the-text-format-parsers-and-their-two-binary-fixtures/42-VERIFICATION.md
@.planning/phases/42-the-text-format-parsers-and-their-two-binary-fixtures/42-15-SUMMARY.md
@.planning/todos/pending/2026-09-09-io-degradation-probe-cached-under-a-binary-wide-key.md

Deliberately NOT `@`-included: `.planning/PROJECT.md`, `.planning/ROADMAP.md`, `42-RESEARCH.md`.
`.planning/STATE.md` is edited by Task 3 but is not `@`-included — it is very large, and Task 3
reads only the two regions it edits.
</context>

<plan_decisions>
**The live proof is added to the EXISTING five-format session, not given a new test.** That case
already opens a broker, acquires a lease, opens the text channel, dials `io $d020`, and then
probes all five commands against the real resolved identity — precisely the state in which CR-02
is reachable. A second live test would cost another cold emulator launch and another teardown
window for evidence the existing session can carry, and every extra launch is another chance to
leave a stray behind.

**The live assertion is discriminating by construction.** The existing capability loop probes
`io` first; the new second-address dial then asks the same entry point for a verdict on a
different reply. On the tree as it stood before 42-15 that call returns the FIRST reply's verdict
and reports it served from cache — so this assertion goes red against the pre-fix tree, live, and
is not merely a restatement of the unit control.

**The record is appended to the phase's existing document, not given a new one.** Plan 42-06
created `docs/phase42-text-format-drift-citations.md`, 42-09 appended its live section and 42-14
its round-1 gap-closure section. A third document would split the phase's evidence across files
a later reader has no reason to know about.

**The requirement restoration is conditional and scoped to one id.** The verification demoted
`PARSE-04` alone; `PARSE-01`, `PARSE-02` and `PARSE-03` are Complete and were not demoted this
round. The blunt whole-phase requirement revert is not used, in either direction. If Task 1's
re-measurement does not come back green, `PARSE-04` stays `Pending`, no edit is made, and the
SUMMARY says why.

**The todo is closed, not merely annotated.** `42-VERIFICATION.md` records the disposition as
correct and the process guards as honestly green — but the disposition was "open, deferred", and
this round removes the reason for that. Leaving the file in `pending/` after fixing the defect
would make the ledger assert a debt that no longer exists.

**Canon-referral drop (prohibition probe, stage 2):** OWASP/injection-class constraints on the
text-monitor command path are owned by the project's security canon and by `buildTextCommand()`.
Not minted here.
</plan_decisions>

<probe_edges>
The round-level edge accounting lives in `42-15`'s `<probe_edges>` block: **12 applicable, 5
authored into `42-15`'s `must_haves.truths`, 7 surfaced as flagged assumptions naming the
already-verified plan that covers each, 0 backstop markers, 0 left `unresolved`, 0 auto-dismissed,
0 silent drops.** This plan authors none of its own — its subject is evidence, requirement state
and ledger hygiene, not parser behaviour — and that is stated here rather than left for a reader
to infer from an absent section.
</probe_edges>

<tasks>

<task type="auto">
  <name>Task 1: The fix proven live against two addresses in one session, and the round's evidence recorded</name>
  <precondition>No `vice-broker` and no `x64sc` process is alive before this task runs — `pgrep -af '[v]ice-broker|[x]64sc'` prints nothing. A live broker reddens `BACK-05` deterministically and every figure this task records would be void. Separately, `/usr/bin/x64sc` must exist and be executable on this host — it is genuine unpatched stock VICE, and the absolute path is what makes the run genuine because the fork shadows the bare name on `PATH`. If it is absent, stop and report it by name with the remedy; never install it.</precondition>
  <files>src/mcp/vice/text-monitor-live.test.ts, docs/phase42-text-format-drift-citations.md</files>
  <read_first>
    - src/mcp/vice/text-monitor-live.test.ts — the module header's opt-in and teardown paragraphs; `SKIP_REASON` and `VICE_LIVE_STOCK_BIN_ENV`; `withBrokerHarness()` and the `HarnessReport` fields `brokerPid`, `strayPidsMatchingScratch`, `scratchDirRemoved`; and, in full, the five-format case containing the `io $d020` dial and the capability loop that probes all five commands against `identity`
    - src/mcp/vice/text-capability-probe.ts — as 42-15 left it: `NEVER_CACHED_COMMANDS`, `textCapabilityVerdictFor()`, `probeTextCapability()`'s cache-read and in-flight skips, and `TextCapabilityVerdict.fromCache`
    - src/mcp/vice/text-protocol.ts — `buildTextCommand()`'s `io` spec: the accepted address bound and the canonical rendering, so the second address chosen is one the builder actually accepts
    - src/mcp/vice/textmon-registers.ts — `parseIoRegisters()`'s chip gate and its `unsupported-chip` refusal, so the second address's expected outcome is known before it is dialed rather than discovered
    - docs/phase42-text-format-drift-citations.md — in full: its block structure, its MEASURED versus source-traced labelling convention, and the round-1 gap-closure section 42-14 appended
    - .planning/phases/42-the-text-format-parsers-and-their-two-binary-fixtures/42-15-SUMMARY.md — the verbatim control-failure texts, the rendered refusal texts and the gate figures 42-15 recorded, which this record transcribes rather than re-derives
    - .planning/phases/42-the-text-format-parsers-and-their-two-binary-fixtures/42-VERIFICATION.md — the `gaps:` block (the one failed truth and its `missing[]` list) and the `deferred:` block (the five accepted-scope items this record must restate as still open)
  </read_first>
  <action>
    Extend the existing live five-format case with the two-address `io` control, then record the
    round's evidence.

    In `text-monitor-live.test.ts`, inside that same case and after the capability loop that
    already probes all five commands against `identity`, dial `io` a SECOND time at a DIFFERENT
    address. Choose the address from `buildTextCommand("io", ...)`'s own accepted bound and from a
    different chip than the first dial's `$d020` — a CIA1 address such as `0xdc00` is the natural
    second, because 42-11's chip gate gives it a known, distinguishable outcome. Assert
    `buildTextCommand` succeeded before issuing anything, and issue the command through
    `withTextChannelLock` exactly as the first `io` dial does.

    Then make the discriminating assertions. Call `probeTextCapability({ command: "io", identity,
    dial: async () => secondResponse })` and assert two things about the verdict it returns:
    `fromCache` is `false`, and `response` is strictly equal to the SECOND reply, not the first.
    Assert alongside them that the first and second replies are not equal to each other, so the
    case cannot pass vacuously on a host where both addresses happen to answer identically — if
    they ARE equal, fail with a message saying the second address was not discriminating and
    naming both replies, rather than silently asserting nothing. Also assert that
    `textCapabilityVerdictFor({ command: "io", response: secondResponse, identity })` reports the
    same outcome as the probe's verdict, so the two entry points are shown to agree live rather
    than only in the unit tests.

    Parse the second reply with `parseIoRegisters` and record what it produced — a decoded section
    or a named refusal code — without asserting WHICH: this control's point is that the answer is
    derived from this reply, not that a particular chip decodes. Log a MEASURED line in the file's
    existing `console.log` style carrying both commands as built, the binary path, the second
    verdict's `fromCache` value, and the second parse's outcome (the chip name on success, the
    refusal code on refusal). Add a short comment above the block naming what it is a control for
    — one call's evidence answering another call's question — without naming a phase number.

    Leave the case's existing teardown assertions exactly as they are, and add no new
    `withBrokerHarness` invocation anywhere in the file.

    Then append the round-2 gap-closure section to `docs/phase42-text-format-drift-citations.md`,
    keeping the document's own block structure and labelling convention: a claim carrying a run is
    labelled MEASURED and names the command; a claim read from source stays labelled source-traced.

    Write one block for CR-02. It must name: the finding id; the defect in one sentence (a
    per-address, per-call classification served from a cache whose entries are properties of a
    binary file); both reachable wrong answers it produced; the remedy chosen and, explicitly, the
    remedy NOT chosen with what that one would have cost; every symbol the fix introduced
    (`textCapabilityVerdictFor`, `TextCapabilityVerdictForOptions`, `NEVER_CACHED_COMMANDS`); and
    the commands whose output proves it. Transcribe the verbatim control-failure texts from
    `42-15-SUMMARY.md` rather than paraphrasing them, and state for each planted control that it
    was observed red against the pre-change tree.

    Write one block for the live re-measurement. Record the binary path, the version string the
    run reported, the date, both `io` commands as built, the second verdict's `fromCache` value,
    the second parse's outcome, and the teardown outcome in the corrected three-part form (broker
    pid, scratch-scoped stray sweep result, scratch-directory removal) — never a service-unit
    status.

    Write one block for the final automated-gate reading. Record the command, the fact that
    `pgrep -af '[v]ice-broker|[x]64sc'` printed nothing immediately before it, the total, the pass
    count, the fail count AND the names of the failing files. State that the figure is the
    documented pre-existing floor and that it is a floor, not a target — a run reporting zero
    would mean the gate was not the gate.

    Write one closing block, titled so it cannot be mistaken for a closure list: the items this
    round did NOT close, each stated as still open with one line of why. Name all five from the
    verification's `deferred:` block — the inability of any tool in this tree to start VICE's
    profiler; the two manual-only verifications (no `--disable-cpuhistory` build on this host, and
    the two degradation strings still source-traced rather than live-observed); the RAM-execute
    hardware evidence; the behavioural half of the decimal-separator finding; and the phase
    validation artifact's own draft status. Add one sentence stating that round 1's five closures
    were re-verified against source by `42-VERIFICATION.md` and are NOT re-claimed by this round.

    Change nothing already in the document. Every existing source-traced label and every earlier
    block stays exactly as written.
  </action>
  <verify>
    <automated>pgrep -af '[v]ice-broker|[x]64sc' ; echo "PRECHECK_EXIT=$?"</automated>
    <fails_when>any process line is printed, or PRECHECK_EXIT is 0 — a match means a broker or emulator is alive and every figure recorded now is void; stop it, then re-run this block from the top</fails_when>
    <automated>test -x /usr/bin/x64sc; echo "STOCK_BIN_EXEC_EXIT=$?"</automated>
    <fails_when>STOCK_BIN_EXEC_EXIT is not 0 — the pinned genuine-stock binary is absent or not executable; report it by name with the per-distro install line README.md already carries, and stop, never installing it</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck > /tmp/gsd-42-16-tc.txt 2>&1; echo "EXIT=$?"; tail -20 /tmp/gsd-42-16-tc.txt</automated>
    <fails_when>the printed EXIT is not 0, or any line contains "error TS"</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && VICE_LIVE_STOCK_BIN=/usr/bin/x64sc node --test text-monitor-live.test.ts > /tmp/gsd-42-16-live.txt 2>&1; echo "EXIT=$?"; grep -aE '^[^A-Za-z0-9]*(tests|suites|pass|fail|cancelled|skipped) [0-9]+$' /tmp/gsd-42-16-live.txt; grep -a 'MEASURED' /tmp/gsd-42-16-live.txt | tail -20</automated>
    <fails_when>the printed EXIT is not 0, or the printed fail count is not 0, or the printed skipped count is not 0 — a skipped case means the opt-in guard rejected the pinned binary and nothing was measured on the post-fix tree</fails_when>
    <automated>grep -ac 'fromCache' /tmp/gsd-42-16-live.txt</automated>
    <fails_when>the printed count is 0 — the new two-address control's MEASURED line never reached the log, so the control did not run even though the suite reported green</fails_when>
    <automated>pgrep -af '[v]ice-broker|[x]64sc' ; echo "POSTCHECK_EXIT=$?"; ls -d /tmp/text-monitor-live-* 2>/dev/null; echo "SCRATCH_LS_EXIT=$?"</automated>
    <fails_when>any process line is printed, or POSTCHECK_EXIT is 0, or a scratch directory path is printed, or SCRATCH_LS_EXIT is 0</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated > /tmp/gsd-42-16-gate.txt 2>&1; echo "EXIT=$?"; grep -aE '^[^A-Za-z0-9]*(tests|suites|pass|fail|cancelled|skipped) [0-9]+$' /tmp/gsd-42-16-gate.txt; sed -n '/failing tests:/,$p' /tmp/gsd-42-16-gate.txt | grep -aoE '[A-Za-z0-9_.-]+\.test\.[a-z]+' | sort -u</automated>
    <fails_when>the printed fail count is greater than 3, or the printed list of failing files contains any name other than `anno-import.test.ts` and `anno-register.test.ts` — the floor is 3, never 0</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && for t in CR-02 textCapabilityVerdictFor NEVER_CACHED_COMMANDS MEASURED; do printf '%s=' "$t"; grep -ac "$t" docs/phase42-text-format-drift-citations.md; done</automated>
    <fails_when>any printed count is 0 — the evidence section does not name the finding, does not name one of the symbols the fix introduced, or carries no measured claim</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac 'source-traced' docs/phase42-text-format-drift-citations.md</automated>
    <fails_when>the printed count is less than 4 — the pre-existing source-traced labels must survive this append unchanged</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-dangling-refs.test.ts docs-linerefs.test.ts comment-phase-pointers.test.ts shipped-modules.test.ts 2>&1 | tail -20</automated>
    <fails_when>the summary reports a non-zero "fail" count — the appended record names a path that does not resolve, a citation that no longer holds, or a comment added live names a phase number</fails_when>
  </verify>
  <acceptance_criteria>
    - `text-monitor-live.test.ts` dials `io` at a second, different address inside the SAME existing five-format case, with no new `withBrokerHarness` invocation added to the file.
    - The control asserts the second verdict's `fromCache` is `false` and its `response` strictly equals the second reply.
    - The control asserts the two replies differ from each other and fails with a message naming both if they do not — it cannot pass vacuously.
    - The control asserts `textCapabilityVerdictFor` and `probeTextCapability` report the same outcome for the second reply.
    - A MEASURED log line carries both built commands, the binary path, the second verdict's `fromCache` value and the second parse's outcome; `grep -ac 'fromCache'` over the captured live log is non-zero.
    - The live suite exits 0 with `fail 0` and zero skipped, and `pgrep`/scratch checks are clean both before and after.
    - The evidence section carries a CR-02 block naming the finding id, both wrong answers, the remedy chosen, the remedy NOT chosen with its cost, all three introduced symbols, and the proving commands — with 42-15's control-failure texts transcribed verbatim.
    - It carries a live block with binary path, reported version string, date, both commands, the `fromCache` value, the second parse outcome, and the three-part teardown observation.
    - It carries a gate block with command, clean-environment claim, total, pass, fail AND the failing file names, stating the figure is a floor rather than a target.
    - The closing block names all five not-closed items as still open, each with one line of why, and states that round 1's closures are not re-claimed here.
    - The pre-existing `source-traced` labels are still present; nothing already in the document was changed.
    - `npm run typecheck` exits 0; `npm run test:automated` reports at most 3 failures, all `anno-*`.
    - `node --test docs-dangling-refs.test.ts docs-linerefs.test.ts comment-phase-pointers.test.ts shipped-modules.test.ts` reports `fail 0`.
  </acceptance_criteria>
  <done>The fix is proven against a real emulator over two addresses in one session, and the phase's own record states what this round measured and, in the same place, everything it deliberately left open.</done>
</task>

<task type="auto">
  <name>Task 2: PARSE-04 returned to Complete, on the evidence Task 1 measured</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — the `PARSE-01` through `PARSE-04` checkbox entries (lines around 38-41) and the traceability-table rows for all four (lines around 128-131), noting which one currently reads `[ ]` / `Pending` and which three read `[x]` / `Complete`
    - .planning/phases/42-the-text-format-parsers-and-their-two-binary-fixtures/42-VERIFICATION.md — the Requirements Coverage table's `PARSE-04` row, which states exactly why it was demoted and on what evidence
    - docs/phase42-text-format-drift-citations.md — as Task 1 left it: the CR-02 block, the live block and the gate block this restoration cites
    - the console output of Task 1's own verification commands — the measured results this task's decision rests on
  </read_first>
  <action>
    Flip exactly one requirement id back, and only if Task 1's verification came back green.

    First state the decision explicitly against the demotion cause. `PARSE-04` was demoted because
    `io`'s capability answer could be served from a different call's evidence, producing either a
    real dump silently discarded behind a false refusal or a legitimate chip fact wrapped in a
    parse-failure message. The criterion is met when: both of those directions are reproduced by a
    control that was red before the fix and is green after it; `io` provably occupies no cache
    entry through write, read or in-flight memo; an empty reply still produces its own named
    indeterminate state; and a live two-address session shows the second answer derived from the
    second reply. Confirm every one of those against Task 1's and 42-15's measured output before
    editing anything. If any is not met, leave `PARSE-04` at `Pending`, make no edit, and say so
    plainly in the SUMMARY — a status flip on the strength of a plan having run is exactly what
    this round's own prohibition forbids.

    Then make the edit as a scoped replacement, never a whole-file rewrite. Change the `PARSE-04`
    checkbox marker from unchecked to checked, leaving its requirement prose byte-identical.
    Change its traceability-table row from `Pending` to `Complete`, keeping the row's pipe spacing
    exactly as the `PARSE-01`..`PARSE-03` rows have it. Touch nothing else: those three were not
    demoted this round, their entries and rows stay exactly as they are, and no other requirement
    family in the file is edited.

    Do not touch `.planning/ROADMAP.md` in this task — the round's ROADMAP bullets were written at
    plan time and this task's declared file set does not include it. Do not invoke any
    requirement-status helper that operates on a whole phase: the demotion was deliberately scoped
    to one id and the restoration must be scoped the same way.

    Re-read the file after editing and confirm any coverage counts or per-family tallies it
    carries still add up, repairing only what your own edit changed.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -aE '^\- \[[ x]\] \*\*PARSE-0[1-4]\*\*' .planning/REQUIREMENTS.md | cut -c1-18</automated>
    <fails_when>fewer than 4 lines are printed, or any printed line shows an unchecked marker — all four PARSE ids must read as checked once the restoration is made</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -aE '^\| PARSE-0[1-4] \|' .planning/REQUIREMENTS.md</automated>
    <fails_when>fewer than 4 rows are printed, or any printed row contains "Pending", or any row's spacing differs from the pattern `| PARSE-0N | Phase 42 | Complete |`</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && git diff --stat -- .planning/REQUIREMENTS.md</automated>
    <fails_when>the printed stat names any file other than `.planning/REQUIREMENTS.md`, or reports more than 4 changed lines — a scoped two-line flip cannot legitimately rewrite more</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && git status --porcelain -- .planning/ROADMAP.md; echo "ROADMAP_STATUS_LINES=$(git status --porcelain -- .planning/ROADMAP.md | wc -l)"</automated>
    <fails_when>ROADMAP_STATUS_LINES is not 0 — this task modified `.planning/ROADMAP.md`, which it does not declare</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-review-disposition.test.ts 2>&1 | tail -15</automated>
    <fails_when>the summary reports a non-zero "fail" count — a review finding lost its disposition</fails_when>
  </verify>
  <acceptance_criteria>
    - The `PARSE-04` checkbox entry reads as checked; its prose text is byte-identical to before the edit.
    - The `PARSE-04` traceability row reads `Complete` and keeps the same pipe spacing as the `PARSE-01`..`PARSE-03` rows.
    - The `PARSE-01`, `PARSE-02` and `PARSE-03` entries and rows are unchanged.
    - `git diff --stat` for this task names only `.planning/REQUIREMENTS.md` and reports at most 4 changed lines.
    - `.planning/ROADMAP.md` shows no modification.
    - The SUMMARY states, item by item, the specific measured result that justified the flip — or, if any criterion came back red, that `PARSE-04` was deliberately left `Pending` and why.
    - `node --test docs-review-disposition.test.ts` reports `fail 0`.
  </acceptance_criteria>
  <done>The one id the verification demoted is back at Complete because the criterion that demoted it was re-measured green, and the record says which measurement bought the flip.</done>
</task>

<task type="auto">
  <name>Task 3: The CR-02 disposition closed, with its ledger row and open count moving in the same commit</name>
  <files>.planning/todos/pending/2026-09-09-io-degradation-probe-cached-under-a-binary-wide-key.md, .planning/todos/completed/2026-09-09-io-degradation-probe-cached-under-a-binary-wide-key.md, .planning/STATE.md</files>
  <read_first>
    - .planning/todos/pending/2026-09-09-io-degradation-probe-cached-under-a-binary-wide-key.md — in full: its frontmatter (`created`, `title`, `area`, `severity`, `files`), its `## Problem` section, its `## Fix sketch` and its `## Why not fixed now` section
    - one existing closed todo under `.planning/todos/completed/` that carries a `## Resolution` section — read it for the section's placement, heading level and the shape of what it records, so this closure matches the convention rather than inventing one
    - src/mcp/vice/docs-deferred-ledger.test.ts — in full: `deferredItemsSection()`'s heading-boundary regex, `stemHasOwnTableCell()`, the two directional predicates and the non-vacuity floors, so the edit is made against what the guard actually reads rather than against an assumption about it
    - .planning/STATE.md — ONLY the two regions this task edits: the `## Deferred Items` table (the row whose stem is `2026-09-09-io-degradation-probe-cached-under-a-binary-wide-key`, near line 1866) and the prose paragraph above it that states how many pending todos the ledger reads (near line 210). Do not read or rewrite the rest of the file.
    - docs/phase42-text-format-drift-citations.md — as Task 1 left it, for the commands and symbol names the resolution section cites
  </read_first>
  <action>
    Close the disposition record and move its ledger row in one commit, so neither direction of the
    two-directional guard is ever momentarily red.

    Move the todo file from `.planning/todos/pending/` to `.planning/todos/completed/` with `git
    mv`, keeping the filename byte-identical — the guard matches on the filename stem, and a
    renamed stem would break the correspondence this edit exists to preserve.

    Append a `## Resolution` section to the moved file, matching the heading level and shape of the
    existing closed todos. It must record: the date; the plans that closed it (`42-15` for the fix,
    this plan for the evidence); the remedy chosen and, in one sentence, the remedy not chosen and
    what it would have cost; the symbols introduced (`textCapabilityVerdictFor`,
    `TextCapabilityVerdictForOptions`, `NEVER_CACHED_COMMANDS`); the named tests that now cover
    both failure directions plus the concurrency and never-cached cases; the live two-address
    measurement with its binary and date; and a pointer to the evidence block in
    `docs/phase42-text-format-drift-citations.md`. Change nothing already in the file — the
    `## Problem`, `## Fix sketch` and `## Why not fixed now` sections stay exactly as written,
    including the last one, which is now historical rather than current and is correct as history.

    Edit `.planning/STATE.md` in exactly two scoped places, never a whole-file rewrite.

    First, remove the Deferred Items table row whose Item cell is the moved stem. Remove the whole
    row line and nothing adjacent; leave the header row, the separator row and every other row
    byte-identical, including their pipe spacing.

    Second, correct the prose paragraph above that table which states the ledger's open count. It
    currently states a figure computed from the number of files under `.planning/todos/pending/`;
    recompute that number from the directory after the move and write the new figure, adjusting
    only the number and any clause that depends on it. Do not restate the paragraph's history, do
    not add a new sentence about this round, and do not renumber or reflow the rest of it.

    Do not use a frontmatter-setting helper on `STATE.md` — a whole-field re-serialization
    flattens block scalars elsewhere in the file. Do not use a state-mutating helper verb for
    either edit; both are scoped text replacements. Diff the file after editing and confirm the
    change set is exactly the removed row plus the count clause.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && ls .planning/todos/pending/ | grep -c 'io-degradation-probe-cached-under-a-binary-wide-key'</automated>
    <fails_when>the printed count is not 0 — the todo is still filed as pending</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && ls .planning/todos/completed/ | grep -c 'io-degradation-probe-cached-under-a-binary-wide-key'</automated>
    <fails_when>the printed count is not 1 — the todo did not land in the completed tree under its original stem</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac '^## Resolution' .planning/todos/completed/2026-09-09-io-degradation-probe-cached-under-a-binary-wide-key.md</automated>
    <fails_when>the printed count is 0 — the closed record carries no resolution section</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && for t in textCapabilityVerdictFor NEVER_CACHED_COMMANDS phase42-text-format-drift-citations; do printf '%s=' "$t"; grep -ac "$t" .planning/todos/completed/2026-09-09-io-degradation-probe-cached-under-a-binary-wide-key.md; done</automated>
    <fails_when>any printed count is 0 — the resolution does not name a symbol the fix introduced, or does not point at the evidence record</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac 'io-degradation-probe-cached-under-a-binary-wide-key' .planning/STATE.md</automated>
    <fails_when>the printed count is not 0 — a Deferred Items row (or any other mention) still names the closed stem in STATE.md, which reds the ledger guard's direction B</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && echo "PENDING_FILES=$(ls .planning/todos/pending/ | wc -l)"; grep -an 'pending todos' .planning/STATE.md | head -3</automated>
    <fails_when>the number written in the printed STATE.md prose line does not equal the printed PENDING_FILES value — the ledger's open-count prose disagrees with the directory it claims to be computed from</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && git diff --stat -- .planning/STATE.md</automated>
    <fails_when>the printed stat reports more than 6 changed lines for `.planning/STATE.md` — a scoped row removal plus a count clause cannot legitimately rewrite more</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-deferred-ledger.test.ts docs-review-disposition.test.ts 2>&1 | tail -20</automated>
    <fails_when>the summary reports a non-zero "fail" count — either ledger direction is red, or a review finding lost its disposition source</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated > /tmp/gsd-42-16-gate2.txt 2>&1; echo "EXIT=$?"; grep -aE '^[^A-Za-z0-9]*(tests|suites|pass|fail|cancelled|skipped) [0-9]+$' /tmp/gsd-42-16-gate2.txt; sed -n '/failing tests:/,$p' /tmp/gsd-42-16-gate2.txt | grep -aoE '[A-Za-z0-9_.-]+\.test\.[a-z]+' | sort -u</automated>
    <fails_when>the printed fail count is greater than 3, or the printed list of failing files contains any name other than `anno-import.test.ts` and `anno-register.test.ts` — the floor is 3, never 0</fails_when>
  </verify>
  <acceptance_criteria>
    - The todo file exists under `.planning/todos/completed/` with its original filename stem and no longer exists under `.planning/todos/pending/`.
    - The moved file carries a `## Resolution` section naming the date, the two plans, the remedy chosen, the remedy not chosen with its cost, all three introduced symbols, the named covering tests, the live measurement, and a pointer to the evidence record.
    - The file's pre-existing `## Problem`, `## Fix sketch` and `## Why not fixed now` sections are byte-identical to before the move.
    - `.planning/STATE.md` contains no occurrence of the closed stem.
    - The open-count figure in STATE.md's Deferred Items prose equals the file count under `.planning/todos/pending/`.
    - `git diff --stat -- .planning/STATE.md` reports at most 6 changed lines, and every other Deferred Items row keeps its original pipe spacing.
    - `node --test docs-deferred-ledger.test.ts docs-review-disposition.test.ts` reports `fail 0`.
    - `npm run test:automated` reports at most 3 failures, all in `anno-import.test.ts` / `anno-register.test.ts`.
    - The move and both STATE.md edits land in the same commit.
  </acceptance_criteria>
  <done>The CR-02 debt is closed on disk and in the ledger together, and the two-directional guard is green because both directions are true, not because one of them was never checked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a measured figure → this project's own record | A number written here becomes a claim later phases and later audits rely on without re-measuring. |
| live emulator run → the developer's host | A real broker daemon and a real emulator are spawned on the machine running this plan. |
| this plan → the requirements ledger and the debt ledger | A status flip and a closed todo are what a later milestone audit reads as evidence a requirement is met and a debt is gone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-60 | Repudiation (a status claim without the evidence behind it) | the `PARSE-04` restoration | high | mitigate | The flip is gated on Task 1's own re-measurement of the demotion cause, enumerated item by item, scoped to exactly one id, and bounded by a diff-size check. A criterion that comes back red leaves the id `Pending` and the SUMMARY says why. |
| T-42-61 | Tampering (of the project's own record) | a measured label attached to an unmeasured claim | high | mitigate | Every figure carries its command and its clean-environment claim; the gate block records failing file names beside the count; the live block records binary, version and date; a check asserts the pre-existing source-traced labels survive the append. |
| T-42-62 | Repudiation | a green round misread as a clean bill of health | high | mitigate | The closing block names all five deliberately-excluded items as still open, an acceptance criterion requires each to be present, and the block states that round 1's closures are not re-claimed here. |
| T-42-63 | Denial of Service (self-inflicted) | the live run leaking a broker, an emulator or a scratch directory | high | mitigate | Pre- and post-run checks cover both process names and the scratch directories, the run itself asserts teardown internally through the corrected three-part method, and no new harness invocation is added. |
| T-42-64 | Tampering | a debt ledger that disagrees with the disposition tree | medium | mitigate | The move and both STATE.md edits land in one commit, the closed stem is asserted absent from STATE.md, the open count is asserted equal to the directory count, and both ledger directions are re-run. |
| T-42-65 | Information Disclosure | host binary paths in a committed record | low | accept | The record names this host's VICE binary path, already named throughout this repository's documentation and fixture sidecars. No credential, port or token. |
| T-42-SC | Tampering | npm/pip/cargo installs | low | accept | No package install. The emulator is used at an explicitly pinned absolute path and is never installed, fetched or built by any task here; a missing binary is refused by name with the remedy in the message. |
</threat_model>

<verification>
1. `pgrep -af '[v]ice-broker|[x]64sc'` prints nothing before and after the live run; no `/tmp/text-monitor-live-*` survives.
2. `test -x /usr/bin/x64sc` exits 0 — the pinned genuine-stock binary is present. If not, refuse by name with the remedy; never install it.
3. `VICE_LIVE_STOCK_BIN=/usr/bin/x64sc node --test text-monitor-live.test.ts` — exit 0, `fail 0`, zero skipped, and the captured log carries the new `fromCache` MEASURED line.
4. `npm run typecheck` — exit 0, no `error TS`.
5. `node --test docs-dangling-refs.test.ts docs-linerefs.test.ts comment-phase-pointers.test.ts shipped-modules.test.ts docs-review-disposition.test.ts docs-deferred-ledger.test.ts` — `fail 0`.
6. `npm run test:automated` — at most 3 failures, all `anno-*`. Never assert 0; the floor is 3.
7. Never run bare `npm test`. Never pipe an `npm test` invocation through `| tail` as the whole command; redirect to a file and read `$?` on the same line.
8. `.planning/ROADMAP.md` is unmodified by any task in this plan.
</verification>

<success_criteria>
- The fix is proven live on genuine stock VICE over two addresses in one session, and the second answer is shown derived from the second reply.
- The evidence block names the finding, the remedy chosen, the remedy rejected, every introduced symbol, and the commands that prove it.
- Every deliberately-excluded item is restated as open in the same document.
- `PARSE-04` is Complete on measured grounds, and nothing else in the requirements ledger moved.
- The CR-02 debt is closed on disk and in STATE.md's ledger together, with both guard directions green.
</success_criteria>

## Artifacts this phase produces

Round-level list, covering both round-2 plans (42-15 and 42-16):

**New exported symbols**
- `textCapabilityVerdictFor(options)` — `src/mcp/vice/text-capability-probe.ts` (42-15)
- `TextCapabilityVerdictForOptions` (interface) — `src/mcp/vice/text-capability-probe.ts` (42-15)
- `NEVER_CACHED_COMMANDS` (frozen const) — `src/mcp/vice/text-capability-probe.ts` (42-15)

**New test cases**
- `src/mcp/vice/text-capability-probe.test.ts` (42-15) — two double-call/two-different-responses
  controls, the never-cached case with its discriminating `memmapshow` pair, the concurrent-`io`
  case, the `TEXT_CAPABILITY_COMMANDS` cacheability-invariant case, and the byte-equality /
  first-non-empty-line encoding case.
- `src/mcp/vice/text-tools.test.ts` (42-15) — the direction-(a) control, the direction-(b)
  control, and the resolved-identity empty-reply case.
- `src/mcp/vice/text-monitor-live.test.ts` (42-16) — the live two-address `io` control, added
  inside the existing five-format case.

**New document sections**
- The round-2 gap-closure evidence section of `docs/phase42-text-format-drift-citations.md` (42-16)
- The `## Resolution` section of
  `.planning/todos/completed/2026-09-09-io-degradation-probe-cached-under-a-binary-wide-key.md` (42-16)

**Moved file**
- `.planning/todos/pending/2026-09-09-io-degradation-probe-cached-under-a-binary-wide-key.md` →
  `.planning/todos/completed/…` (same stem) (42-16)

**No new refusal codes, no new rendered wording, no new published output property, no
`inputSchema` change, no new source files, no deletions other than the todo move above.**

<output>
Create `.planning/phases/42-the-text-format-parsers-and-their-two-binary-fixtures/42-16-SUMMARY.md` when done.
Record in it: the live run's two `io` commands as built, the second verdict's `fromCache` value,
the second parse's outcome, the binary path, the reported version string and the date; the
teardown observation in the corrected three-part form; the `pgrep -af '[v]ice-broker|[x]64sc'`
output taken immediately before and after the live run, verbatim, including the empty case; the
final `npm run test:automated` figure with its failing file names; the item-by-item measured
result that justified the `PARSE-04` flip (or the reason it was left `Pending`); the pending-todo
count before and after the move with the STATE.md figure written beside it; and the verbatim list
of items restated as still open.
</output>
