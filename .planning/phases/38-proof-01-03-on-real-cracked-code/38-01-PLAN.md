---
phase: 38-proof-01-03-on-real-cracked-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/SCHEMA.md
  - .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/README.md
  - .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof01-dxa-real-release.mjs
  - .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof01-dxa-real-release.md
  - src/mcp/vice/dxa-proof01-compare.ts
  - src/mcp/vice/dxa-proof01-compare.test.ts
autonomous: true
requirements: [PROOF-01, PROOF-02, PROOF-03]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The outcome-line vocabulary for all three proofs is committed in evidence/SCHEMA.md before any measurement in this phase is taken, and every PROOF01_/PROOF02_/PROOF03_ name a later plan emits is declared there with its value domain and its single declared source file."
    - "PROOF-01's real-release data-recovery rate reads as a formatPercent() NN.NN (numerator/denominator) string carrying its denominator, with PROOF01_POSITIVE_CLASS naming the class it measures, in evidence/proof01-dxa-real-release.md."
    - "The real-release numbers and the source-derived fixture figures FIXTURE_DATA_RECOVERY_PCT, FIXTURE_FALSE_POSITIVES and FIXTURE_REPRODUCED appear in the same evidence file as separately labelled adjacent blocks, and the pivot's published figures appear only in a third block whose non-reproduction cause is labelled a hypothesis."
    - "PROOF01_FALSE_POSITIVES reads the string structurally-uncomputable with its reason naming certainCode.size == 0 on the byte-derived tier -- it is never a bare integer (D-03)."
    - "Two named weaknesses are recorded separately in the PROOF-01 evidence file: PROOF01_WEAKNESS_NO_EXTERNAL_CHECK (no independent external oracle, memmapshow stated absent) and PROOF01_WEAKNESS_UNCOMPUTABLE_FP (the ground-truth design limit) -- neither absorbed into the other (D-03)."
    - "The comparator's recovery line prints formatPercent()'s NN.NN (numerator/denominator) shape when the ground-truth denominator is greater than 0, and a named refusal string naming the zero denominator when it is 0 -- never 0.00, NaN or 100.00. [edge: PROOF-01 boundary]"
    - "A ground-truth certain-data address that dxa reported in its unclassified overlap map is counted in neither the recovered numerator nor any false-positive tally, and its count is reported on its own PROOF01_UNCLASSIFIED_OVERLAP outcome line rather than folded into either class. [edge: PROOF-01 adjacency]"
    - "With an empty ground-truth certain-data set the comparator emits a named refusal for the recovery line instead of a rate, and with a 0-or-1-byte image partitionByteDerived()'s own size-naming throw propagates rather than being swallowed. [edge: PROOF-01 empty]"
    - "The comparator's report emits its outcome lines in one fixed declared order and prints every address list sorted ascending numerically, so two runs over identical inputs produce byte-identical output. [edge: PROOF-01 ordering]"
    - "The comparator imports formatPercent() from dxa-partition.ts and declares no rounding of its own, so the half-up-at-two-decimals contract with the raw fraction always printed alongside is the one dxa-partition.ts already ships. [edge: PROOF-01 precision]"
    - "The test:automated floor is measured with the broker stopped at Wave 0 and recorded as TEST_AUTOMATED_BASELINE in evidence/README.md, and every later suite result in this phase is compared against that recorded relation rather than against a literal count."
  artifacts:
    - .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/SCHEMA.md
    - .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/README.md
    - .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof01-dxa-real-release.mjs
    - .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof01-dxa-real-release.md
    - src/mcp/vice/dxa-proof01-compare.ts
    - src/mcp/vice/dxa-proof01-compare.test.ts
  key_links:
    - "evidence/proof01-dxa-real-release.mjs imports runDxaDisassemble() from src/mcp/vice/dxa-run.ts -- the Phase 34 host-tool seam -- and never reaches the dxa binary any other way."
    - "evidence/proof01-dxa-real-release.mjs imports partitionByteDerived() from src/mcp/vice/dxa-partition.ts for ground truth and listEntries()/extractEntry() from src/mcp/vice/anno-d64.ts for corpus access -- no private re-implementation of either."
    - "src/mcp/vice/dxa-proof01-compare.ts imports formatPercent() from src/mcp/vice/dxa-partition.ts and takes a DumpListingMap (dxa-listing.ts's shape) plus a ByteDerivedPartition as plain data -- it neither spawns anything nor reads the filesystem."
    - "Every PROOF01_ line emitted into evidence/proof01-dxa-real-release.md is declared in evidence/SCHEMA.md, which was committed in Task 1 before Task 2 ran."
  prohibitions:
    - "Never report PROOF-01's false-positive count as 0, or as any bare integer, when the byte-derived ground-truth tier asserts no address is code."
    - "Never widen or supplement the byte-derived ground-truth tier with a convention guess in order to make a false-positive count computable."
    - "Never replace the source-derived fixture figures with the real-release figures, and never present the pivot's published figures as reproduced."
    - "Never absorb either of PROOF-01's two named weaknesses into the other, into a footnote, or into silence."
    - "Never silently substitute a synthetic or stand-in image when a corpus binary is absent or digests to an unexpected value."
    - "Never reconstruct an evidence transcript from memory after the fact instead of appending the command's real output as it ran."
---

<objective>
Commit this phase's outcome-line vocabulary and evidence conventions BEFORE any
measurement exists, then take PROOF-01's number end to end on the named real
cracked release and record it beside the fixture figures.

Purpose: this milestone exists because a headline figure was published that did
not reproduce. The pre-committed schema is what stops a measuring plan from
minting a favourable line name, and the tracer proves the whole measurement rail
-- gitignored operator corpus, sha256 identity assert, the Phase 34 host-tool
seam, the byte-derived ground truth, the new comparator, the committed evidence
format -- works on one real number before three more plans build on it.

Output: `evidence/SCHEMA.md`, `evidence/README.md`, the shipped, tested
comparator module `src/mcp/vice/dxa-proof01-compare.ts` with its hermetic
colocated test, the repeatable driver `evidence/proof01-dxa-real-release.mjs`,
and the PROOF-01 evidence record `evidence/proof01-dxa-real-release.md`.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-proof-01-03-on-real-cracked-code/38-RESEARCH.md
@.planning/phases/38-proof-01-03-on-real-cracked-code/38-PATTERNS.md
@.planning/phases/38-proof-01-03-on-real-cracked-code/38-VALIDATION.md
</context>

## Locked decisions this plan implements

- **D-02** — the PROOF-01 comparator lands as a shipped module under
  `src/mcp/vice/` with its own colocated `node --test` test file and hermetic
  fixtures, NOT as a one-off evidence script. Implemented by Task 2.
  Reversibility: `costly` — moving it out later means moving a module plus its
  test off the shipped surface and re-checking `test-gate.mjs`'s governing
  lists; nothing about it is irreversible.
- **D-03** — PROOF-01's structurally-uncomputable false-positive count gets its
  own separately named weakness, distinct from the already-disclosed absent
  external oracle. Implemented by Task 2's two `PROOF01_WEAKNESS_*` lines.
  Reversibility: `reversible` — prose in an evidence file.
- **D-05** — `danish.d64`'s `BRUCE LEE   (DC)` (sha256
  `1a9d294e07f9593ba59d878423d157bacfe6c6902d3a52ef6ac96512a15fb6c5`) is
  PROOF-01's primary named binary; the `saeger.d64` cross-check is optional
  strengthening and is deliberately NOT taken in this plan. Implemented by
  Task 2. Reversibility: `reversible`.
- The frozen-vocabulary discipline in Task 1's `SCHEMA.md` (a measuring plan may
  not invent or edit a line name) is rated **`costly`**, not one-way: Phase 33's
  own schema convention supplies the escape hatch — a plan needing an undeclared
  name records an `## ACCEPTED LIMIT` in its own evidence file rather than
  editing the schema.

## Artifacts this phase produces

New symbols and paths created by this plan (none of these exist in the tree
today; treat every name below as newly minted, not as drift from an existing
symbol):

**New files**

- `src/mcp/vice/dxa-proof01-compare.ts`
- `src/mcp/vice/dxa-proof01-compare.test.ts`
- `.planning/phases/38-proof-01-03-on-real-cracked-code/evidence/SCHEMA.md`
- `.planning/phases/38-proof-01-03-on-real-cracked-code/evidence/README.md`
- `.planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof01-dxa-real-release.mjs`
- `.planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof01-dxa-real-release.md`

**New exported symbols in `src/mcp/vice/dxa-proof01-compare.ts`**

- `Proof01ComparisonInput` (interface)
- `Proof01Comparison` (interface)
- `compareByteDerivedRecovery` (function)
- `renderProof01Report` (function)
- `PROOF01_FALSE_POSITIVES_REFUSAL` (const string)

**New evidence outcome-line key names declared by this plan's `SCHEMA.md`**

`PROOF01_RELEASE_ID`, `PROOF01_RELEASE_SHA256`, `PROOF01_ENTRY_NAME`,
`PROOF01_ENTRY_SHA256`, `PROOF01_ENTRY_BYTES`, `PROOF01_ENTRYPOINTS`,
`PROOF01_GROUND_TRUTH_TIER`, `PROOF01_POSITIVE_CLASS`,
`PROOF01_DATA_RECOVERY_PCT`, `PROOF01_FALSE_POSITIVES`,
`PROOF01_UNCLASSIFIED_OVERLAP`, `PROOF01_DXA_CODE_ADDRESSES`,
`PROOF01_DXA_DATA_ADDRESSES`, `PROOF01_WEAKNESS_NO_EXTERNAL_CHECK`,
`PROOF01_WEAKNESS_UNCOMPUTABLE_FP`, `PROOF01_CAPTURE_PAIR`,
`FIXTURE_DATA_RECOVERY_PCT`, `FIXTURE_FALSE_POSITIVES`, `FIXTURE_REPRODUCED`,
`PIVOT_PUBLISHED_DATA_RECOVERY_PCT`, `PIVOT_PUBLISHED_FALSE_POSITIVES`,
`PIVOT_REPRODUCTION_STATUS`, `PIVOT_NON_REPRODUCTION_CAUSE`,
`PROOF02_SITES_METHOD`, `PROOF02_CIRCULARITY_GUARD`,
`PROOF02_LOADER_IMAGE_SHA256`, `PROOF02_LOADER_ENTRYPOINTS`,
`PROOF02_LOADER_SITES_ENUMERATED`, `PROOF02_LOADER_SITES_COMPUTED_INDEX`,
`PROOF02_LOADER_SITES_IMMEDIATE_INDEX`, `PROOF02_LOADER_COMPUTED_DISPATCH`,
`PROOF02_LOADER_SEARCH_DEPTH`, `PROOF02_DEPACKED_SITES_ENUMERATED`,
`PROOF02_DEPACKED_SITES_COMPUTED_INDEX`,
`PROOF02_DEPACKED_SITES_IMMEDIATE_INDEX`,
`PROOF02_DEPACKED_COMPUTED_DISPATCH`, `PROOF02_DEPACK_PROGRESS`,
`PROOF02_CAPTURE_OBTAINED`, `PROOF02_CAPTURE_IMAGE_SHA256`,
`PROOF02_CAPTURE_ORACLE_TERMS`, `PROOF02_CAPTURE_FRAME_TERM`,
`PROOF02_SITES_ENUMERATED`, `PROOF02_SEARCH_DEPTH`,
`PROOF02_COMPUTED_DISPATCH`, `PROOF03_FIXTURE`,
`PROOF03_FIXTURE_PRG_SHA256`, `PROOF03_EXPORT_SHA256`,
`PROOF03_TWO_BANK_STATES`,
`PROOF03_FORWARD_CARRY_WRONG_AT`, `PROOF03_AGREEING_VALUES_BRANCH`,
`PROOF03_DISAGREEING_VALUES_BRANCH`, `PROOF03_CONSTWRITES_ABSENT_BRANCH`,
`PROOF03_CONSTWRITES_EMPTY_BRANCH`, `PROOF03_ORDERING`,
`PROOF03_CAPTURE_PAIR`, `BROKER_STATE`, `TEST_AUTOMATED_BASELINE`.

<tasks>

<task type="auto">
  <name>Task 1: Commit the outcome-line vocabulary and evidence conventions before any measurement exists</name>
  <files>.planning/phases/38-proof-01-03-on-real-cracked-code/evidence/SCHEMA.md, .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/README.md</files>
  <read_first>
    - `.planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/evidence/SCHEMA.md` — the structural analog: the "Binding. Committed before any measurement exists." opening, the four outcome-line conventions (column 0, final occurrence wins, one declared source file per line, transcripts appended never reconstructed), and the `Line | Domain | Corpus-free | Declared source file` table shape.
    - `.planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/evidence/README.md` — the structural analog: the "Nothing binary lands here" disclosure, the `File | Owner | What it is | What it proves` table (rows for files later plans own are promises this table makes), and the numbered "Evidence conventions" section including the broker-stopped rule.
    - `.planning/phases/38-proof-01-03-on-real-cracked-code/38-RESEARCH.md` — § "Validation Architecture" / § "Wave 0 Gaps" for the seeded line names, and § "Common Pitfalls" 2 and 6 for the scratch-directory and gitignored-corpus rules the README must bind.
    - `.planning/phases/38-proof-01-03-on-real-cracked-code/38-PATTERNS.md` — § for `evidence/README.md` and `SCHEMA.md`, which names the exact sections to copy.
  </read_first>
  <action>
Create the phase evidence directory and its two governing documents. Nothing in
this task measures anything; its whole value is that it lands first.

Write `evidence/SCHEMA.md` opening with the binding statement that it is
committed before any measurement exists and that a measuring plan needing an
undeclared name records an `## ACCEPTED LIMIT` in its own evidence file rather
than editing this one. Reproduce Phase 33 SCHEMA.md's four outcome-line
conventions verbatim in substance: bare `NAME: value` at column 0, never
indented and never inside a fenced block or a table cell; final occurrence wins;
exactly one declared source file per line name; transcripts appended, never
reconstructed.

Then declare, in `Line | Domain | Declared source file | Absence` tables grouped
one table per proof, every name listed under "Artifacts this phase produces"
above. Bind these domains explicitly:

- `PROOF01_FALSE_POSITIVES` — domain is the single literal
  `structurally-uncomputable` followed by a parenthesised reason; an integer is
  NOT in this line's domain, per D-03 and `dxa-partition.ts:462-464`.
- `PROOF01_DATA_RECOVERY_PCT`, `FIXTURE_DATA_RECOVERY_PCT`,
  `PIVOT_PUBLISHED_DATA_RECOVERY_PCT` — domain is `formatPercent()`'s
  `NN.NN (numerator/denominator)` shape; a bare percentage with no fraction is
  not in the domain.
- `PROOF01_POSITIVE_CLASS`, `PROOF01_GROUND_TRUTH_TIER` — `data`, and
  `byte-derived` or `source-derived` respectively.
- `PROOF02_COMPUTED_DISPATCH`, `PROOF02_LOADER_COMPUTED_DISPATCH`,
  `PROOF02_DEPACKED_COMPUTED_DISPATCH` — domain is exactly
  `resolved` / `unresolved` / `not-exercised`. Record in the table, as a stated
  rule rather than an aside, that Phase 23's `could-not-run` spelling is not in
  this domain because the corpus now exists, and that `pass` is not in it either
  because a searched-and-empty corpus is `not-exercised`.
- `PROOF02_SEARCH_DEPTH`, `PROOF02_LOADER_SEARCH_DEPTH` — a free-text depth
  statement naming which entry points and which image the search covered;
  absence is not permitted alongside any `PROOF02_*_COMPUTED_DISPATCH` line.
- `PROOF02_CAPTURE_ORACLE_TERMS` and `PROOF02_CAPTURE_FRAME_TERM` — required in
  any file reporting a capture, carrying `GATE-01`'s `D-04` narrowing: the
  two-term `(PC, hit_count)` stop oracle with the frame term `(LIN, CYC)`
  recorded but not asserted. Declare a companion rule that a file reporting NO
  capture writes `PROOF0N_CAPTURE_PAIR: none` instead, so the narrowing is never
  merely absent.
- `PROOF03_*` — the branch-name lines take `annotated` / `declined` /
  `skipped-in-image` / `skipped-no-entry` (`JoinDecision.outcome`'s own
  vocabulary, `anno-join.ts:199-205`); `PROOF03_ORDERING` takes a stated
  ordering rule.
- `BROKER_STATE` — `inactive` or `active`; a measurement recorded against
  `active` is discarded and re-run, never repaired.
- `TEST_AUTOMATED_BASELINE` — the measured floor as
  `tests N / pass N / fail N`, declared as a recorded baseline and explicitly
  never a gate.

Write `evidence/README.md` with: the opening paragraph naming
`PROBE_DIR=$HOME/.cache/c64-re-tools/phase38` as this phase's worktree-independent
working directory (never inside the checkout, never `/tmp`, which is tmpfs on
this host); the "Nothing binary lands here" disclosure (no corpus image, no
`.vsf`, no flat 64K capture is ever committed — identity travels as name plus
sha256); the `File | Owner | What it is | What it proves` table with one row per
artifact this phase produces, including rows owned by plans `38-02`, `38-03` and
`38-04` as promises; and a numbered "Evidence conventions" section binding on
every plan in the phase covering: (1) the `$ <exact command>` transcript
convention with real stdout appended; (2) `PROBE_DIR`; (3) broker stopped and
recorded, verified with `systemctl --user is-active vice-broker` and
`pgrep -af x64sc` with both outputs pasted; (4) the corpus-resolution rule —
the corpus `.d64` images are gitignored operator-supplied material, absent from
a fresh clone and absent from a git worktree, so every script resolves them from
`$C64_CORPUS_DIR` if set, else `<repo-root>/.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/corpus/`,
and fails loudly naming BOTH attempted absolute paths when neither holds the
file, and asserts sha256 before reading a byte; (5) the recorded
`TEST_AUTOMATED_BASELINE`, stated once here and cited thereafter, never
re-derived and never a gate; (6) voided runs recorded, not discarded.

Measure the floor for convention (5) now, with the broker stopped, and write the
observed `tests / pass / fail` triple into `README.md` as the
`TEST_AUTOMATED_BASELINE` line together with the `$ <command>` transcript that
produced it. Record whatever the run reports — the floor is a recorded baseline,
not a target, and a number differing from Phase 37's closing baseline is a fact
to record rather than a discrepancy to fix.
  </action>
  <acceptance_criteria>
    - `evidence/SCHEMA.md` exists and its first non-heading line states it is binding and committed before any measurement exists.
    - Every outcome-line name listed under "Artifacts this phase produces" appears at column 0 or inside a schema table row in `evidence/SCHEMA.md`, each with a declared value domain and exactly one declared source file. Count them from that list rather than from a number pinned here.
    - `evidence/SCHEMA.md` states that `PROOF01_FALSE_POSITIVES`' domain is the literal `structurally-uncomputable` with a reason, and that an integer is not in that domain.
    - `evidence/SCHEMA.md` states that the `PROOF02_*_COMPUTED_DISPATCH` domain is exactly `resolved` / `unresolved` / `not-exercised`, and that neither Phase 23's `could-not-run` spelling nor `pass` is a member.
    - `evidence/README.md` contains a `File | Owner | What it is | What it proves` table with a row for each of `SCHEMA.md`, `README.md`, `proof01-dxa-real-release.mjs`, `proof01-dxa-real-release.md`, `proof02-enumerate-sites.mjs`, `proof02-loader-stage.md`, `proof02-depacked-capture.md`, `proof02-computed-dispatch.md`, `proof03-bank-boundary.mjs` and `proof03-bank-boundary.md`.
    - `evidence/README.md` contains a `TEST_AUTOMATED_BASELINE:` line at column 0 whose value reads `tests <N> / pass <N> / fail <N>`, immediately preceded by the `$ <command>` transcript that produced it, and a `BROKER_STATE: inactive` line with the two verification command outputs pasted.
    - `evidence/README.md` names both corpus-resolution paths as absolute strings and states that a script fails loudly naming both when neither holds the file.
    - `git status --porcelain` shows no untracked binary artifact under `evidence/`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac '^PROOF0[123]_\|^FIXTURE_\|^PIVOT_\|^BROKER_STATE\|^TEST_AUTOMATED_BASELINE\|`PROOF0[123]_\|`FIXTURE_\|`PIVOT_' .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/SCHEMA.md</automated>
    <fails_when>a count below 40, or non-zero exit meaning grep matched nothing (the schema declared fewer line names than the phase will emit)</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac 'structurally-uncomputable' .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/SCHEMA.md</automated>
    <fails_when>output is 0 or the command exits non-zero — the false-positive line's domain was not declared</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac 'not-exercised' .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/SCHEMA.md</automated>
    <fails_when>output is 0 or the command exits non-zero — the PROOF-02 domain was not declared</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -acE '^TEST_AUTOMATED_BASELINE: tests [0-9]+ / pass [0-9]+ / fail [0-9]+$' .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/README.md</automated>
    <fails_when>output is 0 or the command exits non-zero — the floor was not measured, or was written in a shape later plans cannot read</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && git status --porcelain .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/ | grep -v '\.md$' | wc -l</automated>
    <fails_when>output is any number other than 0 — a non-markdown artifact landed under evidence/</fails_when>
  </verify>
  <done>`evidence/SCHEMA.md` and `evidence/README.md` are committed, the full outcome-line vocabulary for all three proofs is declared with domains and source files, and the `test:automated` floor is recorded with its transcript and a `BROKER_STATE: inactive` line. No measurement has been taken yet.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: End-to-end PROOF-01 -- one real number from the named release, through the comparator, into the evidence format</name>
  <files>src/mcp/vice/dxa-proof01-compare.ts, src/mcp/vice/dxa-proof01-compare.test.ts, .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof01-dxa-real-release.mjs, .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof01-dxa-real-release.md</files>
  <precondition>`danish.d64` is readable at `$C64_CORPUS_DIR/danish.d64` or at `<repo-root>/.planning/phases/23-the-real-release-gate-go-degrade-no-go/evidence/corpus/danish.d64`, and digests to `1a9d294e07f9593ba59d878423d157bacfe6c6902d3a52ef6ac96512a15fb6c5`. Under a git worktree the second path is absent because `*.d64` is gitignored — assert this before running and halt with both attempted absolute paths named if neither holds it.</precondition>
  <read_first>
    - `src/mcp/vice/dxa-partition.ts` — read `formatPercent()` (lines 128-156, the half-up rounding contract and the zero-denominator refusal), `ByteDerivedInput`/`ByteDerivedPartition` (lines 448-492, including the `certainCode` is ALWAYS empty doc comment at 462-464), `renderByteDerivedReport()`'s stated-refusal pattern (lines 557-580), `renderPartitionReport()`'s both-tiers-always-named shape (594-636), and the `main(argv)` + entry-point-guard CLI convention (639-689).
    - `src/mcp/vice/dxa-listing.ts` — `DumpListingMap` (lines 113-154): the `code`, `data`, `unclassified` and `covered` members and the rule that an overlapping address is removed from whichever class set held it.
    - `src/mcp/vice/dxa-run.ts` — `DxaRunArgs`, `DxaRunOptions` (the `repoRoot` override), `DxaRunResult`, and `runDxaDisassemble()`'s workspace confinement.
    - `src/mcp/vice/anno-d64.ts` — `listEntries()` and `extractEntry()` signatures.
    - `src/mcp/vice/dxa-partition.test.ts` — the HERMETIC header framing, the `HERE`/`FIXTURES` constants, and the `test("taskN: <what>, <source of the expected number>")` naming convention.
    - `.planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/evidence/capture-pair.mjs` — lines 1-100, the "IT DRIVES THE SHIPPED SEAMS, NOT PRIVATE COPIES OF THEM" header section and the sha256-identity-before-launch convention.
    - `.planning/phases/38-proof-01-03-on-real-cracked-code/evidence/SCHEMA.md` and `README.md` — written in Task 1; they are binding on this task.
    - `docs/phase23-real-release-gate-findings.md` lines 440-500 — the beside-the-fixture table framing and the exact fixture figures.
  </read_first>
  <behavior>
    - Given a `certainData` set of 3 addresses of which dxa's `data` set holds 2, `compareByteDerivedRecovery()` reports `recovered: 2`, `denominator: 3` and a rate string of `66.67 (2/3)`.
    - Given a `certainData` address that appears in dxa's `unclassified` map, that address counts toward neither `recovered` nor any false-positive tally, and appears in the returned `unclassifiedOverlap` count.
    - Given an empty `certainData` set, `renderProof01Report()` emits a named refusal for the recovery line naming the zero denominator, and does not emit `0.00`, `NaN` or `100.00`.
    - `renderProof01Report()` always emits `PROOF01_FALSE_POSITIVES` with `PROOF01_FALSE_POSITIVES_REFUSAL`'s value, for every input, including inputs where dxa classified addresses as code.
    - Two calls to `renderProof01Report()` with identical inputs return byte-identical strings, and every address list inside is sorted ascending numerically.
    - `compareByteDerivedRecovery()` never reads the filesystem and never starts a process: the test asserts the module's source declares no import of Node's child-process or filesystem modules.
  </behavior>
  <action>
Write the tests first, then the module, then the driver, then take the
measurement. The whole rail is one slice: gitignored corpus, sha256 identity,
`anno-d64.ts` extraction, `runDxaDisassemble()` through the Phase 34 host-tool
seam, `partitionByteDerived()` ground truth, the new comparator, one committed
evidence file in Task 1's format.

**`src/mcp/vice/dxa-proof01-compare.ts`.** A pure transform, no I/O, no process
spawn. Header comment in this project's WHY-first style stating that this module
is precisely the caller `dxa-partition.ts`'s `T-35-13` anticipates — the one
place that names both tiers and joins them — and that it exists because PROOF-01
needs dxa's own claim measured against an independent ground truth. Quote
`dxa-partition.ts:462-464`'s always-empty doc comment in the header, since the
false-positive line depends on it. Export:

- `Proof01ComparisonInput` — `{ listing: DumpListingMap; groundTruth: ByteDerivedPartition }`, both as plain data the caller already fetched.
- `Proof01Comparison` — `{ denominator, recovered, missed, unclassifiedOverlap, recoveredAddresses, missedAddresses, overlapAddresses, positiveClass, tier }`, with every address array sorted ascending numerically.
- `compareByteDerivedRecovery(input: Proof01ComparisonInput): Proof01Comparison` — the join. `denominator` is `groundTruth.certainCode.size + groundTruth.certainData.size`, mirroring `dxa-partition.ts`'s denominator discipline, never the image size. An address is `recovered` when it is in `certainData` and in `listing.data`; `missed` when it is in `certainData` and not in `listing.data` and not in `listing.unclassified`; `unclassifiedOverlap` when it is in `certainData` and in `listing.unclassified` — a third bucket, counted in neither of the other two.
- `PROOF01_FALSE_POSITIVES_REFUSAL` — the exact string this project prints in place of an integer, of the shape `structurally-uncomputable (certainCode.size is 0 on the byte-derived tier -- see dxa-partition.ts:462-464)`.
- `renderProof01Report(comparison, meta): string` — the outcome-line block. It emits `PROOF01_GROUND_TRUTH_TIER`, `PROOF01_POSITIVE_CLASS`, `PROOF01_DATA_RECOVERY_PCT`, `PROOF01_FALSE_POSITIVES`, `PROOF01_UNCLASSIFIED_OVERLAP`, `PROOF01_DXA_CODE_ADDRESSES` and `PROOF01_DXA_DATA_ADDRESSES` in that fixed order. The rate goes through `formatPercent()` imported from `dxa-partition.ts` — never a local rounding helper — and when `denominator` is 0 the line reads a named refusal naming the zero denominator instead, following `renderByteDerivedReport()`'s stated-refusal pattern rather than printing a number. `PROOF01_FALSE_POSITIVES` is `PROOF01_FALSE_POSITIVES_REFUSAL` unconditionally.

Add the `main(argv)` plus entry-point guard so the module is both importable and
directly runnable, matching `dxa-partition.ts:639-689`.

**`src/mcp/vice/dxa-proof01-compare.test.ts`.** Hermetic: no dxa, no Ghidra, no
corpus, no network — a synthetic `DumpListingMap`-shaped object literal and a
`ByteDerivedPartition` built either by hand or from the already-committed
`fixtures/dxa/basic-stub.prg` via `partitionByteDerived()`. Cover every case in
the behavior block above, plus a source assertion over the module's own text
that it declares no filesystem or child-process import and no local rounding
helper. Use `grep -a`-equivalent reading (read the file as bytes, not as a
grep-filtered stream) so a NUL byte cannot hide a line from the assertion. Keep
the file automated — do not add it to `test-gate.mjs`'s `MANUAL_ONLY_TESTS`.

**`.planning/.../evidence/proof01-dxa-real-release.mjs`.** The repeatable driver.
Header modelled on `capture-pair.mjs`'s: an "IT DRIVES THE SHIPPED SEAMS"
section listing what is imported rather than re-authored — `anno-d64.ts`'s
`listEntries()`/`extractEntry()` for corpus access, `dxa-run.ts`'s
`runDxaDisassemble()` for the dxa invocation, `dxa-partition.ts`'s
`partitionByteDerived()` for ground truth, `dxa-proof01-compare.ts`'s
`compareByteDerivedRecovery()`/`renderProof01Report()` for the join. It must not
import Node's child-process module: the dxa invocation belongs to
`runDxaDisassemble()`, which owns the host-tool seam. Its steps:

1. Resolve the corpus per `README.md` convention (4): `$C64_CORPUS_DIR` else the
   repo-relative Phase 23 corpus path, failing loudly with both absolute paths
   named. Assert `danish.d64` digests to
   `1a9d294e07f9593ba59d878423d157bacfe6c6902d3a52ef6ac96512a15fb6c5` before
   reading a byte, and refuse outright otherwise.
2. `listEntries()` then `extractEntry()` for `BRUCE LEE   (DC)`; assert the
   extracted bytes digest to
   `331fe97e73f8c77624242b4da3029efcb9199ffa184d6cd95220a60311efdaf4` and record
   the length.
3. Create a `mkdtempSync` scratch workspace OUTSIDE the checkout under
   `PROBE_DIR=$HOME/.cache/c64-re-tools/phase38`, write the extracted `.prg` and
   an entrypoints file containing `0819` (the extracted image's own `10 SYS2073`
   BASIC line target, established by Phase 35) there, and pass that directory as
   `opts.repoRoot` to `runDxaDisassemble({ image, imageKind: "prg", entrypointsPath })`.
   Tear the scratch dir down in a `finally`. Passing an absolute path outside the
   declared root is refused by the seam by design.
4. `partitionByteDerived({ bytes, isPrg: true })` over the same extracted bytes,
   independently.
5. `compareByteDerivedRecovery()` then `renderProof01Report()`, printing the
   outcome-line block to stdout so the transcript convention captures it.

**`.planning/.../evidence/proof01-dxa-real-release.md`.** Run the driver, append
its `$ <command>` line and real stdout, and record — in this order, as three
separately labelled adjacent blocks, never merged into one series:

- **Block A, the real release.** `PROOF01_RELEASE_ID: danish`,
  `PROOF01_RELEASE_SHA256`, `PROOF01_ENTRY_NAME: BRUCE LEE   (DC)`,
  `PROOF01_ENTRY_SHA256`, `PROOF01_ENTRY_BYTES`, `PROOF01_ENTRYPOINTS: $0819`,
  then the report block the driver printed. Whatever numbers come out are the
  numbers recorded.
- **Block B, the fixture figures, unchanged and not replaced.**
  `FIXTURE_DATA_RECOVERY_PCT: 72.39 (97/134)`, `FIXTURE_FALSE_POSITIVES: 3`,
  `FIXTURE_REPRODUCED: no`, each cited to `docs/phase23-real-release-gate-findings.md`.
- **Block C, the pivot's published figures, beside — never as ground truth.**
  `PIVOT_PUBLISHED_DATA_RECOVERY_PCT: 72.46 (100/138)`,
  `PIVOT_PUBLISHED_FALSE_POSITIVES: 0`,
  `PIVOT_REPRODUCTION_STATUS: not-reproduced`, and
  `PIVOT_NON_REPRODUCTION_CAUSE:` whose value begins with the word `hypothesis`
  and then states the 141/138 partition was not source-derivable and flattered
  dxa exactly where the headline lived. This is written as a hypothesis, not as a
  finding.

Then two separate named weaknesses (D-03), each its own line and its own
paragraph — neither folded into the other:

- `PROOF01_WEAKNESS_NO_EXTERNAL_CHECK:` — the absent oracle. `memmapshow` is
  stated absent by owner decision 2026-09-02, excluded twice over. State the
  reversal condition so a future reader can tell whether it has been met: a
  binary-monitor-reachable execution oracle, or a decision to open the text
  channel.
- `PROOF01_WEAKNESS_UNCOMPUTABLE_FP:` — the ground-truth design limit. The
  byte-derived tier decides exactly two facts and neither is ever code, so a
  false-positive count has no denominator to exist against. This is a limit of
  the ground truth's design, not of the oracle's availability, and the two are
  different limitations.

Close with `PROOF01_CAPTURE_PAIR: none` (this measurement reports no capture
pair, so `GATE-01`'s two-term narrowing has nothing to attach to here, stated
rather than left absent), `BROKER_STATE: inactive` with both verification
outputs pasted, and the `TEST_AUTOMATED_BASELINE` cited from `README.md` rather
than re-derived.

State plainly, in prose beside Block A, that the real-release denominator is the
BASIC loader stub only and is therefore a structurally different and much
narrower fact than the fixture's — not a smaller sample of the same thing.
`danish.d64` is the primary named binary per D-05; the `saeger.d64` cross-check
is optional strengthening and is not taken here.
  </action>
  <acceptance_criteria>
    - `node --test dxa-proof01-compare.test.ts` passes with a `# fail 0` line, and the file is absent from `test-gate.mjs`'s `MANUAL_ONLY_TESTS`.
    - `npm run typecheck` reports no `error TS` line.
    - `src/mcp/vice/dxa-proof01-compare.ts` imports `formatPercent` from `./dxa-partition.ts` and contains no `toFixed` call.
    - `evidence/proof01-dxa-real-release.md` contains a line matching `^PROOF01_DATA_RECOVERY_PCT: [0-9]+\.[0-9]{2} \([0-9]+/[0-9]+\)$` — the value itself is whatever was measured.
    - `evidence/proof01-dxa-real-release.md` contains a `PROOF01_FALSE_POSITIVES:` line whose value contains `structurally-uncomputable`.
    - `evidence/proof01-dxa-real-release.md` contains all three of `FIXTURE_DATA_RECOVERY_PCT: 72.39 (97/134)`, `FIXTURE_FALSE_POSITIVES: 3` and `FIXTURE_REPRODUCED: no` at column 0.
    - `evidence/proof01-dxa-real-release.md` contains a `PIVOT_NON_REPRODUCTION_CAUSE:` line whose value starts with `hypothesis`.
    - `evidence/proof01-dxa-real-release.md` contains both `PROOF01_WEAKNESS_NO_EXTERNAL_CHECK:` and `PROOF01_WEAKNESS_UNCOMPUTABLE_FP:` as two distinct column-0 lines.
    - `evidence/proof01-dxa-real-release.md` contains `PROOF01_RELEASE_SHA256: 1a9d294e07f9593ba59d878423d157bacfe6c6902d3a52ef6ac96512a15fb6c5`.
    - Running the driver twice produces byte-identical outcome-line blocks.
    - `git status --porcelain` shows no untracked `.prg`, `.rep`, `.gpr` or `.bin` anywhere in the checkout after the run.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test dxa-proof01-compare.test.ts 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, or a `# fail` line with a count greater than 0 in the summary</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck</automated>
    <fails_when>non-zero exit, or any line containing `error TS` in the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && grep -ac 'formatPercent' dxa-proof01-compare.ts</automated>
    <fails_when>output is 0 or non-zero exit — the comparator did not import the shared rate formatter</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && grep -av '^[[:space:]]*[/*]' dxa-proof01-compare.ts | grep -c 'toFixed' || true</automated>
    <fails_when>output is any number other than 0 — a local rounding path was introduced alongside the imported one. Comment lines are filtered out first (`grep -av '^[[:space:]]*[/*]'`) precisely because `dxa-partition.ts`'s own `formatPercent()` doc comment names that API while forbidding it, and a copied header must not self-invalidate the gate</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -acE '^PROOF01_DATA_RECOVERY_PCT: [0-9]+\.[0-9]{2} \([0-9]+/[0-9]+\)$' .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof01-dxa-real-release.md</automated>
    <fails_when>output is 0 or non-zero exit — the rate is absent, or was written without its denominator</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -acE '^PROOF01_FALSE_POSITIVES: structurally-uncomputable' .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof01-dxa-real-release.md</automated>
    <fails_when>output is 0 or non-zero exit — the false-positive line is absent or carries a value outside its declared domain</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -acE '^(FIXTURE_DATA_RECOVERY_PCT: 72\.39 \(97/134\)|FIXTURE_FALSE_POSITIVES: 3|FIXTURE_REPRODUCED: no|PIVOT_NON_REPRODUCTION_CAUSE: hypothesis|PROOF01_WEAKNESS_NO_EXTERNAL_CHECK:|PROOF01_WEAKNESS_UNCOMPUTABLE_FP:|PROOF01_RELEASE_SHA256: 1a9d294e07f9593ba59d878423d157bacfe6c6902d3a52ef6ac96512a15fb6c5)' .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof01-dxa-real-release.md</automated>
    <fails_when>a count below 7, or non-zero exit — one of the fixture figures, the hypothesis label, a named weakness or the release digest is missing</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -al 'node:child_process' .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof01-dxa-real-release.mjs src/mcp/vice/dxa-proof01-compare.ts | wc -l</automated>
    <fails_when>output is any number other than 0 — a new process-spawn site was introduced outside the host-tool seam (T-38-01)</fails_when>
    <!-- planner-discipline-allow: prg -->
    <!-- planner-discipline-allow: rep -->
    <!-- planner-discipline-allow: gpr -->
    <!-- planner-discipline-allow: bin -->
    <!-- planner-discipline-allow: vsf -->
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && git status --porcelain | grep -cE '\.(prg|rep|gpr|bin|vsf)$' || true</automated>
    <fails_when>output is any number other than 0 — scratch binary material landed inside the checkout (RESEARCH.md Pitfall 2). The grep target is `git status` output, which cannot contain plan text, so the file-extension literals here carry no self-invalidation risk</fails_when>
    <human-check>Read `evidence/proof01-dxa-real-release.md` end to end and confirm the prose frames Block A as beside Block B rather than instead of it, that the pivot figures in Block C are never presented as reproduced, and that the two named weaknesses read as two distinct limitations rather than one restated. The greps prove the keys are present; only a reader can confirm the framing is honest.</human-check>
  </verify>
  <done>The comparator ships with green hermetic tests and a clean typecheck; the driver reproduces the measurement from a committed script; `evidence/proof01-dxa-real-release.md` carries the real-release numbers with their denominator and positive class, the fixture figures unchanged beside them, the pivot figures beside those with a hypothesis-labelled cause, and two separately named weaknesses. Whatever the measured rate turned out to be, it is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator corpus -> evidence driver | Untrusted binary corpus material (`danish.d64`, gitignored and operator-supplied) crosses into a measurement script here |
| evidence driver -> host-tool seam -> dxa | The only sanctioned route from container-side/analysis code to a host binary; argv is typed and allowlisted, never constructed from corpus content |
| evidence driver -> scratch workspace | Every extracted/generated byte is confined to a `mkdtempSync` root outside the checkout; `confineToWorkspace()` refuses anything resolving outside it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-01 | Tampering | `evidence/proof01-dxa-real-release.mjs`, `src/mcp/vice/dxa-proof01-compare.ts` | high | mitigate | Both reach dxa only through `runDxaDisassemble()`. Task 2's verify greps both new files for an import of Node's child-process module and requires zero hits. SEAM-05's own gate (`scripts/check-no-skill-external-spawn.mjs`) covers `src/skills/` and the packed tarball; it is not narrowed or disabled for this phase, and this task's grep is the complementary check for the two file classes SEAM-05's scope does not reach. |
| T-38-02 | Tampering | corpus file identity at read time | high | mitigate | The driver asserts `danish.d64`'s sha256 equals `1a9d294e07f9593ba59d878423d157bacfe6c6902d3a52ef6ac96512a15fb6c5` and the extracted entry's sha256 equals `331fe97e73f8c77624242b4da3029efcb9199ffa184d6cd95220a60311efdaf4` before any measurement reads a byte, and refuses outright otherwise — `capture-pair.mjs`'s own convention. Both digests are recorded as outcome lines in the evidence file. |
| T-38-03 | Denial of Service | dxa invoked on crafted or corrupted corpus bytes | low | accept | dxa 0.1.5 is host-installed and digest-pinned; this project does not patch it. The corpus is a fixed, digest-asserted pair of images, not attacker-supplied at runtime. Accepted as out of scope to newly mitigate, per RESEARCH.md § Security Domain. |
| T-38-04 | Information Disclosure | scratch workspace leaking binary material into the checkout | medium | mitigate | Scratch root is `mkdtempSync` under `PROBE_DIR=$HOME/.cache/c64-re-tools/phase38`, outside the checkout entirely, torn down in a `finally`; Task 2's verify runs `git status --porcelain` for stray `.prg`/`.rep`/`.gpr`/`.bin`/`.vsf` and requires zero. |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install occurs anywhere in this phase. RESEARCH.md § Package Legitimacy Audit records this as not applicable: dxa 0.1.5 was vendored and audited in Phase 35, Ghidra 12.1.3 was declared a host prerequisite in Phase 34/36. No `[ASSUMED]`/`[SUS]` package exists to gate. |
</threat_model>

<verification>
- `cd src/mcp/vice && node --test dxa-proof01-compare.test.ts` — green, `# fail 0`.
- `cd src/mcp/vice && npm run typecheck` — no `error TS` line.
- `cd src/mcp/vice && pgrep -f vice-broker >/dev/null && echo BROKER_RUNNING || npm run test:automated 2>&1 | tail -20` — the fail count is at or below the `TEST_AUTOMATED_BASELINE` recorded in `evidence/README.md`. If the output reads `BROKER_RUNNING`, the suite was not measured: stop the broker and re-run.
- Every outcome-line grep in Task 1 and Task 2 passes.
- `git status --porcelain` is free of binary scratch artifacts.
</verification>

<success_criteria>
- The outcome-line vocabulary for all three proofs is committed before any measurement was taken, and no later plan needs to invent a name.
- PROOF-01's data-recovery rate exists as a number against the named binary `BRUCE LEE   (DC)` from `danish.d64`, carrying its denominator and positive class.
- The false-positive count is recorded as structurally uncomputable with its reason, never as `0`.
- Real-release, fixture and pivot figures sit in three separately labelled adjacent blocks in one file, with the pivot's non-reproduction cause labelled a hypothesis.
- Two named weaknesses are recorded separately.
- The comparator is a shipped, typechecked, hermetically tested module (D-02).
</success_criteria>

<output>
Create `.planning/phases/38-proof-01-03-on-real-cracked-code/38-01-SUMMARY.md` when done.
</output>
