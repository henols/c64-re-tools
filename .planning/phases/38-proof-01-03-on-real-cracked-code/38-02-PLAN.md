---
phase: 38-proof-01-03-on-real-cracked-code
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof03-bank-boundary.mjs
  - .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof03-bank-boundary.md
autonomous: true
requirements: [PROOF-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The point where a single forward-carried $01 value stops being correct is established by observation, in both directions, and recorded whichever way each came out: direction 1 is one address annotating differently under two determinate bank states, direction 2 is the disagreeing-values program point where forward-carrying produces a confident specific label while the committed code declines."
    - "Phase 38's PROOF-03 record is a fresh, self-contained transcript produced by re-running the measurement under this phase's own evidence conventions, citing Phase 37's 37-06 files rather than standing on them (D-04) -- a reader can verify PROOF-03's claim without opening a Phase 37 file."
    - "The measurement runs against the already-committed fixture src/mcp/vice/fixtures/ghidra/bank-path-dependent.a / .prg and its already-committed real Ghidra export src/mcp/vice/fixtures/ghidra/export-bank-path-dependent.txt, whose sha256 digests are recorded in the evidence file, and it modifies neither."
    - "Every mutation used to observe a control red lives only inside a mkdtempSync scratch tree outside the checkout, and git status --porcelain over src/mcp/vice/anno-join.ts and src/mcp/vice/anno-bank.ts reports zero lines both before the scratch work and after teardown."
    - "At a program point reached by two const-write facts carrying the SAME $01 value the join takes one recorded JoinDecision.outcome branch, and at a point reached by two DIFFERENT values it takes another whose reason names both values; both branches are recorded as PROOF03_AGREEING_VALUES_BRANCH and PROOF03_DISAGREEING_VALUES_BRANCH rather than only the interesting one. [edge: PROOF-03 adjacency]"
    - "The record states, quoting runMemmapJoin()'s own returned JoinDecision.outcome and reason, what a bank-conditional address does under each of the three constWrites shapes -- absent (undefined, the bank machinery is a complete no-op), empty ([], activated with zero facts) and non-empty -- with no branch omitted. [edge: PROOF-03 empty]"
    - "PROOF03_ORDERING states the ordering rule the committed code uses where reaching values or decisions compare equal, and the driver run twice over unchanged inputs produces byte-identical outcome-line blocks. [edge: PROOF-03 ordering]"
    - "PROOF03_CAPTURE_PAIR reads none, stating rather than leaving absent that this measurement reports no capture pair and so carries no two-term (PC, hit_count) oracle narrowing."
  artifacts:
    - .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof03-bank-boundary.mjs
    - .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof03-bank-boundary.md
  key_links:
    - "evidence/proof03-bank-boundary.mjs imports decodeBankState()/resolveBankedRegion()/isBankConditionalAddress() from src/mcp/vice/anno-bank.ts and runMemmapJoin() from src/mcp/vice/anno-join.ts -- never a private re-implementation of the $01 bit arithmetic."
    - "evidence/proof03-bank-boundary.mjs feeds parseGhidraExport()/parseConstWrites() from src/mcp/vice/anno-import.ts over the committed real export src/mcp/vice/fixtures/ghidra/export-bank-path-dependent.txt -- the export text is never hand-parsed."
    - "Every PROOF03_ line emitted is declared in evidence/SCHEMA.md, committed by plan 38-01 before this measurement ran."
  prohibitions:
    - "Never present Phase 37's committed 37-06 evidence as this phase's own measurement without re-running it and producing a fresh transcript."
    - "Never record only the direction that came out favourably; both directions are recorded whichever way each came out."
    - "Never reconstruct an evidence transcript from memory after the fact instead of appending the command's real output as it ran."
    - "Never let a mutation written to observe a control red survive outside the scratch tree, and never repair a red observation into a green one by editing the committed module."
---

<objective>
Establish, by observation rather than assumption and in both directions, the
point where a single forward-carried `$01` value stops being correct — against
the committed fixture that banks ROM in and out — and record the result whichever
way it comes out, in a transcript that stands on its own.

Purpose: `PROOF-03` is the highest-risk item on the pivot's own record and was
recorded unmeasured in both directions. The substance exists in Phase 37's
`37-06` controls, which were badged `AUTO-04`/`AUTO-05` and never as `PROOF-03`.
D-04 settles that this phase re-runs the measurement under its own conventions
rather than citing Phase 37 by reference, so `PROOF-03`'s claim is verifiable
from one file.

Output: the repeatable driver `evidence/proof03-bank-boundary.mjs` and the
self-contained record `evidence/proof03-bank-boundary.md`.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-proof-01-03-on-real-cracked-code/38-RESEARCH.md
@.planning/phases/38-proof-01-03-on-real-cracked-code/38-PATTERNS.md
@.planning/phases/38-proof-01-03-on-real-cracked-code/38-01-SUMMARY.md
</context>

## Locked decisions this plan implements

- **D-04** — PROOF-03 re-runs Phase 37's `37-06` measurement under Phase 38's own
  evidence conventions, producing a fresh, self-contained transcript rather than
  citing Phase 37's committed files by reference. Implemented by both tasks.
  Reversibility: `reversible` — the fixture, the capture and the modules are all
  already in place, so re-running costs one task and citing instead would cost
  nothing to switch back to.

## Artifacts this phase produces

New symbols and paths created by this plan (none exist in the tree today):

**New files**

- `.planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof03-bank-boundary.mjs`
- `.planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof03-bank-boundary.md`

**New outcome-line key names emitted by this plan** (all declared in
`evidence/SCHEMA.md` by plan `38-01`)

`PROOF03_FIXTURE`, `PROOF03_FIXTURE_PRG_SHA256`, `PROOF03_EXPORT_SHA256`,
`PROOF03_TWO_BANK_STATES`, `PROOF03_FORWARD_CARRY_WRONG_AT`,
`PROOF03_AGREEING_VALUES_BRANCH`, `PROOF03_DISAGREEING_VALUES_BRANCH`,
`PROOF03_CONSTWRITES_ABSENT_BRANCH`, `PROOF03_CONSTWRITES_EMPTY_BRANCH`,
`PROOF03_ORDERING`, `PROOF03_CAPTURE_PAIR`, `BROKER_STATE`.

No new exported source symbol: every module this plan touches is read and driven,
never modified.

<tasks>

<task type="auto">
  <name>Task 1: Build the repeatable PROOF-03 driver -- both directions, three constWrites shapes, scratch-tree mutation</name>
  <files>.planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof03-bank-boundary.mjs</files>
  <precondition>`src/mcp/vice/fixtures/ghidra/export-bank-path-dependent.txt` and `src/mcp/vice/fixtures/ghidra/bank-path-dependent.prg` are present and git-tracked in the working tree — they are Phase 37 committed fixtures, so a worktree checkout carries them; assert both exist before running and halt naming the missing path.</precondition>
  <read_first>
    - `src/mcp/vice/fixtures/ghidra/bank-path-dependent.a` — the whole fixture, especially lines 9-11 (the self-correction: `$34`'s bit 2 IS set, contrary to a mislabelled comment in sibling `bank.a`) and the `probe` routine reached twice from `$0815` under `$01=$34` and from `$081c` under `$01=$33`.
    - `src/mcp/vice/fixtures/ghidra/export-bank-path-dependent.txt` — the real committed `analyzeHeadless` export this driver parses; read its `## CONST_WRITES` and `## REFERENCES` sections.
    - `src/mcp/vice/anno-bank.ts` — `BankedRegion`, `BankState`, `decodeBankState()` (line 90), `isBankConditionalAddress()` (108), `resolveBankedRegion()` (122), `regionAdmitsEntry()` (167).
    - `src/mcp/vice/anno-join.ts` — `JoinDecision` (199-205, the four `outcome` values and the rule that a skip or decline always carries a non-empty `reason`), `RunMemmapJoinArgs.constWrites` (206-228, the doc comment distinguishing `undefined` from `[]`), `JoinCounts` (165-174), and `runMemmapJoin()`'s ascending-address-order contract (233-252).
    - `src/mcp/vice/anno-import.ts` — `parseGhidraExport()` (146), `parseConstWrites()` (323), `CONST_WRITE_WATCHED_ADDRESSES` (288, which includes `0x0001`).
    - `.planning/phases/37-the-importer-and-the-automatic-annotation-join/evidence/37-06-bank-decode-bypass-red.md` — the direction-1 technique and its recorded result.
    - `.planning/phases/37-the-importer-and-the-automatic-annotation-join/evidence/37-06-path-dependent-decline-red.md` — the direction-2 technique: `putXref()` wiring, the scratch tree of a mutated module plus re-export shims for unmutated siblings, and the `git status --porcelain` cleanliness check at lines 112-117.
    - `.planning/phases/38-proof-01-03-on-real-cracked-code/evidence/SCHEMA.md` and `README.md` — binding conventions from plan `38-01`.
    - `src/mcp/vice/anno-bank.test.ts` — the 18 existing cases including the 2 planted violations, so the driver asserts something the suite does not already assert.
  </read_first>
  <action>
Write `evidence/proof03-bank-boundary.mjs`: a single repeatable driver that
produces every PROOF-03 number by driving the shipped modules. Header modelled
on `capture-pair.mjs`'s "IT DRIVES THE SHIPPED SEAMS, NOT PRIVATE COPIES OF
THEM" section, listing what it imports rather than re-authors: `anno-bank.ts`'s
`decodeBankState()`/`resolveBankedRegion()`/`isBankConditionalAddress()`,
`anno-join.ts`'s `runMemmapJoin()`, `anno-import.ts`'s `parseGhidraExport()`/
`parseConstWrites()`. No `$01` bit arithmetic and no export-format parsing may
be written in this file. It must not import Node's child-process module: the
mutated-module observation is done by writing a scratch tree and dynamically
importing from it in-process, exactly as `37-06` did.

Give it subcommands so each half is separately re-runnable and the transcript
shows which produced which number:

- `direction1` — the two-bank-states observation. Parse the committed export,
  take the two recovered `$01` const-write facts (`$34` written at `$0815`,
  `$33` written at `$081c`), and for the shared program point resolve the
  annotation under each. Print the two labels verbatim and whether they are
  equal by direct string comparison. Also print `decodeBankState($34)` and
  `decodeBankState($33)` field by field, so the bit-2 fact is visible in the
  transcript rather than asserted in prose.
- `direction2` — the forward-carry observation. Wire BOTH const-write facts to
  reach the SAME address (`$D020`) via `putXref()`, the disagreeing-values case.
  Run `runMemmapJoin()` twice: once against the committed, unmutated
  `anno-join.ts`, and once against a scratch copy whose reaching-values step is
  replaced by a forward-carry that takes the first (ascending) reaching value.
  Build the scratch tree with `mkdtempSync` under
  `PROBE_DIR=$HOME/.cache/c64-re-tools/phase38`, holding the mutated module plus
  re-export shims for its unmutated siblings, and tear it down in a `finally`.
  Print, for each run, the address's `JoinDecision.outcome`, its `reason`, its
  `label` when annotated, and `counts.declined`. Print `git status --porcelain`
  over `src/mcp/vice/anno-join.ts` and `src/mcp/vice/anno-bank.ts` before and
  after, so the cleanliness claim is observed rather than asserted.
- `branches` — the three-shapes and agreeing-values sweep, which `37-06` did not
  cover and which this phase adds. For one bank-conditional address, run
  `runMemmapJoin()` four times and print the returned `JoinDecision.outcome` and
  `reason` for each: (a) `constWrites` absent entirely (`undefined` — the doc
  comment says the bank-state machinery is a complete no-op); (b) `constWrites`
  an empty array (activated with zero facts); (c) two facts carrying the SAME
  `$01` value reaching the address; (d) two facts carrying DIFFERENT values
  reaching it. Print the ordering the returned `decisions` array is in, and the
  order in which any decline reason names the reaching values.

Every subcommand asserts the fixture identity first: sha256 of
`fixtures/ghidra/bank-path-dependent.prg` and of
`fixtures/ghidra/export-bank-path-dependent.txt`, printed as
`PROOF03_FIXTURE_PRG_SHA256` and `PROOF03_EXPORT_SHA256` before any measurement.
Output is deterministic: fixed line order, address lists sorted ascending
numerically, so two runs over unchanged inputs are byte-identical.

Do not repeat `bank.a`'s bit-2 error anywhere in this file's comments: `$34` has
bit 2 SET; the RAM outcome holds because the RAM case depends only on bits 1-0.
  </action>
  <acceptance_criteria>
    - `node evidence/proof03-bank-boundary.mjs direction1` exits 0 and prints two labels plus an explicit equal/not-equal verdict.
    - `node evidence/proof03-bank-boundary.mjs direction2` exits 0 and prints, for both the committed and the mutated run, an outcome, a reason and a `counts.declined` value.
    - `node evidence/proof03-bank-boundary.mjs branches` exits 0 and prints four labelled outcome/reason pairs, one per `constWrites` shape.
    - Every subcommand prints `PROOF03_FIXTURE_PRG_SHA256:` and `PROOF03_EXPORT_SHA256:` before its first measurement line.
    - `src/mcp/vice/anno-join.ts` and `src/mcp/vice/anno-bank.ts` are unmodified after any subcommand runs.
    - The driver's source declares no import of Node's child-process module and no local `$01` bit arithmetic.
    - Two successive runs of the same subcommand produce byte-identical stdout.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof03-bank-boundary.mjs direction1 > /tmp/p3d1a.txt 2>&1 && node .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof03-bank-boundary.mjs direction1 > /tmp/p3d1b.txt 2>&1 && cmp /tmp/p3d1a.txt /tmp/p3d1b.txt && echo DETERMINISTIC_OK</automated>
    <fails_when>non-zero exit, or the output does not end with `DETERMINISTIC_OK` (a `cmp` differ line means the driver's output is not stable across runs)</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof03-bank-boundary.mjs branches 2>&1 | grep -ac 'CONSTWRITES_ABSENT\|CONSTWRITES_EMPTY\|AGREEING_VALUES\|DISAGREEING_VALUES'</automated>
    <fails_when>a count below 4, or non-zero exit — one of the four `constWrites` shapes was not exercised or not labelled</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && git status --porcelain src/mcp/vice/anno-join.ts src/mcp/vice/anno-bank.ts | wc -l</automated>
    <fails_when>output is any number other than 0 — a scratch mutation leaked into the committed tree</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -al 'node:child_process' .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof03-bank-boundary.mjs | wc -l</automated>
    <fails_when>output is any number other than 0 — the driver reached outside its process instead of importing the scratch tree in-process</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test anno-bank.test.ts anno-join.test.ts 2>&1 | tail -20</automated>
    <fails_when>non-zero exit, or a `# fail` line with a count greater than 0 — the committed bank/join modules were disturbed</fails_when>
  </verify>
  <done>`evidence/proof03-bank-boundary.mjs` exists, runs all three subcommands green and deterministically, asserts both fixture digests before measuring, leaves the committed modules untouched, and covers a branch sweep Phase 37 did not.</done>
</task>

<task type="auto">
  <name>Task 2: Record PROOF-03 in both directions, whichever way each came out</name>
  <files>.planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof03-bank-boundary.md</files>
  <read_first>
    - `.planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof03-bank-boundary.mjs` — the driver written in Task 1, so the recorded commands match its real subcommands.
    - `.planning/phases/38-proof-01-03-on-real-cracked-code/evidence/SCHEMA.md` — the declared `PROOF03_*` domains this file must honour.
    - `.planning/phases/38-proof-01-03-on-real-cracked-code/evidence/README.md` — the numbered evidence conventions, especially the transcript rule and the broker-stopped rule.
    - `.planning/phases/37-the-importer-and-the-automatic-annotation-join/evidence/37-06-bank-decode-bypass-red.md` and `37-06-path-dependent-decline-red.md` — the document structure to mirror (Part 1: the thing measured, with the real excerpt; Part 2: the scratch tree and the red observation), and the prior results to CITE as prior art rather than to restate as this phase's own.
    - `src/mcp/vice/fixtures/ghidra/bank-path-dependent.a` lines 9-11 — the bit-2 self-correction, so it is carried forward rather than contradicted.
  </read_first>
  <action>
Run each of the driver's three subcommands with the broker stopped, appending
the `$ <exact command>` line and its real stdout to
`evidence/proof03-bank-boundary.md` as it runs. Never reconstruct output
afterwards.

Structure the file as Phase 37's `37-06` records do — Part 1 states what is
being measured with the real fixture excerpt, Part 2 shows the scratch tree and
the observation — but written so a reader never has to open a Phase 37 file to
verify PROOF-03's claim (D-04). Cite `37-06-bank-decode-bypass-red.md` and
`37-06-path-dependent-decline-red.md` explicitly as prior art, and state
plainly that they were badged `AUTO-04`/`AUTO-05` and that this record is the
Phase-38 measurement, freshly taken.

Record these column-0 outcome lines, with whatever values the runs produced:

- `PROOF03_FIXTURE: src/mcp/vice/fixtures/ghidra/bank-path-dependent.a`,
  plus `PROOF03_FIXTURE_PRG_SHA256` and `PROOF03_EXPORT_SHA256` from the driver.
- `PROOF03_TWO_BANK_STATES:` — `differ`, `identical` or `not-found` per the
  declared domain, with the two labels quoted verbatim in the prose beside it and
  the address named. Direction 1.
- `PROOF03_FORWARD_CARRY_WRONG_AT:` — the program-point address at which
  forward-carrying a single `$01` value produced a confident specific label while
  the committed code declined, or the stated reason no such point was observed.
  Beside it, in prose, name what the mutated run's label was and what the
  committed run's decline reason named, so "wrong" is shown rather than asserted.
  Direction 2.
- `PROOF03_AGREEING_VALUES_BRANCH:` and `PROOF03_DISAGREEING_VALUES_BRANCH:` —
  each one of `JoinDecision.outcome`'s own four values, from the `branches` run.
- `PROOF03_CONSTWRITES_ABSENT_BRANCH:` and `PROOF03_CONSTWRITES_EMPTY_BRANCH:` —
  likewise, so the absent-versus-empty distinction `anno-join.ts`'s own doc
  comment draws is recorded as an observation rather than quoted as a claim.
- `PROOF03_ORDERING:` — the ordering rule observed: the order `runMemmapJoin()`
  returns its decisions in, and the order a decline reason names the reaching
  values in.
- `PROOF03_CAPTURE_PAIR: none` — this measurement reports no capture pair, so
  `GATE-01`'s two-term `(PC, hit_count)` narrowing has nothing to attach to here.
  Stated rather than left absent.
- `BROKER_STATE: inactive`, with the outputs of
  `systemctl --user is-active vice-broker` and `pgrep -af x64sc` pasted, and the
  `TEST_AUTOMATED_BASELINE` cited from `evidence/README.md` rather than
  re-derived.

Close with a short "What this does and does not establish" section: state which
addresses and which bank states were covered, that the fixture is a synthetic
ACME program built for this purpose and not a real cracked release, and that
nothing here establishes whether `danish.d64`'s `BRUCE LEE   (DC)` itself ever
writes `$01` more than once — an open question, named rather than glossed. Carry
forward the fixture's bit-2 self-correction: `$34` has bit 2 set, and the RAM
outcome holds because the RAM case depends only on bits 1-0.

If either direction comes out differently from Phase 37's prior observation,
that is the result: record it, name the difference, and do not adjust the driver
to reproduce the older number.
  </action>
  <acceptance_criteria>
    - `evidence/proof03-bank-boundary.md` contains, at column 0, all of: `PROOF03_FIXTURE:`, `PROOF03_FIXTURE_PRG_SHA256:`, `PROOF03_EXPORT_SHA256:`, `PROOF03_TWO_BANK_STATES:`, `PROOF03_FORWARD_CARRY_WRONG_AT:`, `PROOF03_AGREEING_VALUES_BRANCH:`, `PROOF03_DISAGREEING_VALUES_BRANCH:`, `PROOF03_CONSTWRITES_ABSENT_BRANCH:`, `PROOF03_CONSTWRITES_EMPTY_BRANCH:`, `PROOF03_ORDERING:`, `PROOF03_CAPTURE_PAIR: none`, `BROKER_STATE: inactive`.
    - `PROOF03_TWO_BANK_STATES:`'s value is one of `differ`, `identical`, `not-found` — the declared domain, not free text.
    - `PROOF03_AGREEING_VALUES_BRANCH:` and `PROOF03_DISAGREEING_VALUES_BRANCH:` each carry one of `annotated`, `declined`, `skipped-in-image`, `skipped-no-entry`.
    - The file contains at least three `$ ` transcript lines naming the driver's three subcommands, each followed by real output.
    - The file cites both `37-06-bank-decode-bypass-red.md` and `37-06-path-dependent-decline-red.md` by filename.
    - The file contains a section stating what the measurement does not establish, naming the fixture's synthetic nature and the open question about the real release.
    - `git status --porcelain src/mcp/vice/anno-join.ts src/mcp/vice/anno-bank.ts` reports zero lines.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -acE '^(PROOF03_FIXTURE|PROOF03_FIXTURE_PRG_SHA256|PROOF03_EXPORT_SHA256|PROOF03_TWO_BANK_STATES|PROOF03_FORWARD_CARRY_WRONG_AT|PROOF03_AGREEING_VALUES_BRANCH|PROOF03_DISAGREEING_VALUES_BRANCH|PROOF03_CONSTWRITES_ABSENT_BRANCH|PROOF03_CONSTWRITES_EMPTY_BRANCH|PROOF03_ORDERING|PROOF03_CAPTURE_PAIR|BROKER_STATE): ' .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof03-bank-boundary.md</automated>
    <fails_when>a count below 12, or non-zero exit — an outcome line is missing or was written indented rather than at column 0</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -acE '^PROOF03_TWO_BANK_STATES: (differ|identical|not-found)$' .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof03-bank-boundary.md</automated>
    <fails_when>output is 0 or non-zero exit — direction 1's verdict is absent or carries a value outside its declared domain</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -acE '^(PROOF03_AGREEING_VALUES_BRANCH|PROOF03_DISAGREEING_VALUES_BRANCH|PROOF03_CONSTWRITES_ABSENT_BRANCH|PROOF03_CONSTWRITES_EMPTY_BRANCH): (annotated|declined|skipped-in-image|skipped-no-entry)$' .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof03-bank-boundary.md</automated>
    <fails_when>a count below 4, or non-zero exit — a branch was omitted or recorded outside `JoinDecision.outcome`'s vocabulary</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac '37-06-bank-decode-bypass-red.md\|37-06-path-dependent-decline-red.md' .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof03-bank-boundary.md</automated>
    <fails_when>a count below 2, or non-zero exit — the prior art was not cited</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -ac '^\$ node .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof03-bank-boundary.mjs' .planning/phases/38-proof-01-03-on-real-cracked-code/evidence/proof03-bank-boundary.md</automated>
    <fails_when>a count below 3, or non-zero exit — fewer than the three subcommand transcripts were appended</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && pgrep -f vice-broker >/dev/null && echo BROKER_RUNNING || npm run test:automated 2>&1 | tail -20</automated>
    <fails_when>the output reads `BROKER_RUNNING` (the suite was not measured — stop the broker and re-run), or the `# fail` count exceeds the `TEST_AUTOMATED_BASELINE` recorded in `evidence/README.md`</fails_when>
    <human-check>Read `evidence/proof03-bank-boundary.md` end to end and confirm it stands alone: that a reader who has never opened a Phase 37 file can see both directions established from the transcripts in this one document, and that the "does not establish" section is honest about the fixture being synthetic.</human-check>
  </verify>
  <done>`evidence/proof03-bank-boundary.md` records both directions of the `$01` boundary from freshly-run transcripts, all four `constWrites` branches, the ordering rule, the two fixture digests, and an explicit statement of what the measurement does not establish. Whatever each direction turned out to be, it is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture -> driver | The real `analyzeHeadless` export text crosses into a parser here; it is committed, git-tracked and digest-asserted, not operator-supplied |
| scratch tree -> committed tree | A deliberately mutated copy of a shipped module exists during the run; the boundary is that it must never cross back |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-01 | Tampering | `evidence/proof03-bank-boundary.mjs` | high | mitigate | The driver imports the shipped `anno-bank.ts`/`anno-join.ts`/`anno-import.ts` and reaches no host binary at all. Task 1's verify greps the new script for an import of Node's child-process module and requires zero hits. SEAM-05's own gate is neither narrowed nor disabled. |
| T-38-05 | Tampering | `src/mcp/vice/anno-join.ts`, `src/mcp/vice/anno-bank.ts` | high | mitigate | The forward-carry mutation lives only inside a `mkdtempSync` scratch tree under `PROBE_DIR`, torn down in a `finally`; `git status --porcelain` over both modules is printed by the driver before and after and asserted to report zero lines by Task 1 and Task 2 verifies. This is `37-06`'s own guard, reused rather than re-invented. |
| T-38-02 | Tampering | fixture identity at read time | medium | mitigate | Every subcommand asserts and prints the sha256 of `fixtures/ghidra/bank-path-dependent.prg` and `fixtures/ghidra/export-bank-path-dependent.txt` before its first measurement line, and both digests are recorded as outcome lines. Severity is medium rather than high because both files are git-tracked, so a substitution is also visible as a diff. |
| T-38-03 | Denial of Service | external tools | low | accept | This plan runs no external tool: no dxa, no Ghidra, no emulator. Nothing to bound. |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install occurs in this plan or anywhere in this phase; RESEARCH.md § Package Legitimacy Audit records this as not applicable. |
</threat_model>

<verification>
- All three driver subcommands run green, deterministically, with the broker stopped.
- Every `PROOF03_*` outcome-line grep passes and every domain-constrained value is in its declared domain.
- `git status --porcelain src/mcp/vice/anno-join.ts src/mcp/vice/anno-bank.ts` reports zero lines.
- `cd src/mcp/vice && pgrep -f vice-broker >/dev/null && echo BROKER_RUNNING || npm run test:automated 2>&1 | tail -20` — fail count at or below the recorded baseline.
</verification>

<success_criteria>
- The point where a forward-carried `$01` value stops being correct is established by observation in both directions and recorded whichever way each came out.
- The record stands alone: PROOF-03's claim is verifiable from `evidence/proof03-bank-boundary.md` without opening a Phase 37 file (D-04).
- All four `constWrites` branches, including the absent-versus-empty distinction, are recorded as observations.
- No mutation survives outside the scratch tree.
</success_criteria>

<output>
Create `.planning/phases/38-proof-01-03-on-real-cracked-code/38-02-SUMMARY.md` when done.
</output>
