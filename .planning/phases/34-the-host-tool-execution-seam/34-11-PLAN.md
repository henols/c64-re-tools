---
phase: 34-the-host-tool-execution-seam
plan: 11
type: execute
wave: 2
depends_on: ["34-10"]
files_modified:
  - docs/phase34-host-tool-seam-decisions.md
  - .planning/todos/pending/2026-09-03-cr-05-resolveworkspacepath-is-symlink-blind.md
  - .planning/todos/completed/2026-09-03-cr-05-resolveworkspacepath-is-symlink-blind.md
  - .planning/STATE.md
autonomous: true
gap_closure: true
closes_gaps: [3]
closes_findings: [CR-05]
requirements: [SEAM-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`docs/phase34-host-tool-seam-decisions.md` gains an APPENDED, dated correction recording CR-05: what was wrong (the lexical check), what changed (both sides walked, the real path returned), what did NOT change (the seven-key allowlist, SEAM-02's literal property, the JVM binding and its reversal condition), and what is still NOT closed (the check-then-open window, the Unicode-normalisation divergence, and the three carried warnings) — with the original text left legible above it."
    - "The two NEW recorded assumptions `A-15` (the walk is duplicated deliberately because `host-tool.mts` is host-bound and cannot import a container-side `.ts`, and the equivalence test is the price) and `A-16` (the seam returns the real path, and the container-translation consequence is a recorded limit rather than a widened `hostpath.ts` consumer set) are in the decision record's own `A-NN` table convention, marked as added this round rather than backdated."
    - "The gate ran BEFORE any bookkeeping edit, and its measured output is quoted in the SUMMARY — a record that runs ahead of its gate is the exact failure `34-VERIFICATION.md` recorded for SEAM-02 in the first place."
    - "The CR-05 todo is in `.planning/todos/completed/` with a Resolution section naming the closing plan, the commits, the two new assumptions, and the residuals the fix does NOT close; and its stale `host-tool.mts` line citations are re-derived against the post-fix source rather than carried forward wrong."
    - "`.planning/STATE.md`'s Deferred Items table no longer carries a Pending row for the CR-05 todo, and `docs-deferred-ledger.test.ts` is green in BOTH directions — every pending todo has a row, and no completed todo is still listed as Pending — in the SAME commit as the file move, so the two-directional guard never observes a half state."
    - "`.planning/STATE.md`'s stated open-pending figure agrees with its own Deferred Items table row count after the removal — the discipline that file's own prose records as having gone stale twice before and as not itself guarded."
    - "WR-03's ledger row stays Pending and its todo file stays in `.planning/todos/pending/` — closing CR-05 is not closing WR-03, and the two share only a file."
    - "SEAM-02's marking in `.planning/REQUIREMENTS.md` is untouched, in both directions: the verifier ruled in writing that its literal text is MET and is not reverted on CR-05's account."
    - statement: "CR-05 remains dispositioned after the todo moves: the review-disposition guard reads both the pending and the completed todo trees plus the phase's own SUMMARYs, so the finding does not become undispositioned by being closed."
      verification: backstop
  artifacts:
    - path: "docs/phase34-host-tool-seam-decisions.md"
      provides: "an appended dated CR-05 correction and the `A-15`/`A-16` rows, leaving Part 1 and the 34-09 correction intact"
      contains: "A-16"
    - path: ".planning/todos/completed/2026-09-03-cr-05-resolveworkspacepath-is-symlink-blind.md"
      provides: "the closed todo with a Resolution section and re-derived line citations"
    - path: ".planning/STATE.md"
      provides: "a Deferred Items table with the CR-05 row removed and a stated open-pending figure that matches its own row count"
  key_links:
    - "`.planning/todos/pending/` ↔ `.planning/STATE.md`'s `## Deferred Items` table — `docs-deferred-ledger.test.ts` asserts BOTH directions, so the file move and the row removal must land in one commit or the guard reds on whichever half arrives first."
    - "`34-REVIEW.md`'s CR-05 id → its disposition sources — the guard accepts a `*-SUMMARY.md` in the phase directory naming the id, or a todo in EITHER `.planning/todos/{pending,completed}/` naming it; the moved todo and `34-10-SUMMARY.md`/`34-11-SUMMARY.md` each satisfy it independently, which is why the move does not undispositon the finding."
    - "`docs/phase34-host-tool-seam-decisions.md` is this phase's substitute for the CONTEXT.md that never existed, so `A-15` and `A-16` belong in its `A-NN` table rather than only in a plan — a decision recorded only in a plan is a decision the next phase reads last."
  prohibitions:
    - "MUST NOT edit the record or the ledger before the gate has actually run and passed. The marking follows the evidence; a bookkeeping edit made on the strength of a plan being written is the failure this phase already recorded once."
    - "MUST NOT let a ledger row and its todo file disagree, in either direction or even briefly. The two-directional guard exists because a list that stopped deriving from ground truth is how this project lost track of its own debt once already."
    - "MUST NOT present a residual as closed. The check-then-open window, the normalisation divergence and the three carried warnings are named in the correction as still open, and the correction is APPENDED so the original record stays legible rather than being rewritten into agreement with the present."
    - "MUST NOT rewrite what was already recorded. A correction that edits the original leaves no evidence of what was believed before, which is the whole reason this document's own convention is to append."
---

<objective>
Bring the record and the debt ledger into agreement with the code `34-10` just changed — last, and
only after a gate has actually run.

Purpose: `34-VERIFICATION.md` scored CR-05 as a phase-blocking gap partly BECAUSE "its only
disposition today is a pending todo filed by the review process itself" and "a todo tracks a
finding; it does not discharge it". Closing the code without closing the record leaves
`.planning/STATE.md` carrying a `blocker | Pending` row for a defect that no longer exists, and
leaves this phase's own decision document — the substitute for the CONTEXT.md that never existed —
silent about the two assumptions `34-10` had to make.

Output: an appended, dated CR-05 correction plus the `A-15`/`A-16` rows in
`docs/phase34-host-tool-seam-decisions.md`; the todo moved to `.planning/todos/completed/` with a
Resolution section and re-derived line citations; and the Deferred Items row removed with the
stated open figure corrected — the last two in ONE commit, because the guard over them reads both
directions.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-the-host-tool-execution-seam/34-VERIFICATION.md
@.planning/phases/34-the-host-tool-execution-seam/34-REVIEW.md
@.planning/phases/34-the-host-tool-execution-seam/34-10-PLAN.md
@.planning/todos/pending/2026-09-03-cr-05-resolveworkspacepath-is-symlink-blind.md
@docs/phase34-host-tool-seam-decisions.md
</context>

## Isolation carve-out — this plan MUST run with `USE_WORKTREES_FOR_PLAN=false`

Two independent, stock reasons, both recorded in `CLAUDE.md` § "GSD Execution Isolation":

1. **This plan delivers `.planning/STATE.md` content.** A worktree executor may not touch
   `.planning/STATE.md` or `ROADMAP.md`, and the per-plan commit strips them — so a worktree run
   would silently DROP the ledger-row removal while landing the todo file move, leaving
   `docs-deferred-ledger.test.ts` red in direction B with no local diff to explain it. The stock
   carve-out for a plan delivering STATE.md content is `USE_WORKTREES_FOR_PLAN=false`.
2. **Task 2's deliverable includes a deletion.** Moving the todo out of `.planning/todos/pending/`
   is a `git rm` on that path, and `cleanup-wave` refuses any branch whose diff contains a deletion,
   unconditionally. Running this plan outside a worktree removes the branch that would have to be
   merged by hand.

`.planning/REQUIREMENTS.md` is NOT touched by this plan and is unaffected by the STATE.md
restriction anyway. `.planning/ROADMAP.md` is NOT touched by this plan — its plan list for this
round belongs to the orchestrator.

## Scope fence

- **WR-01, WR-02 and WR-03 are all still open, and this plan names them so rather than closing
  them.** WR-03 is the sharpest trap: it is a NEW warning from the same review, filed the same day,
  against the same file, at severity minor, and `34-VERIFICATION.md` explicitly did not score it.
  Its ledger row (`.planning/STATE.md`, immediately below CR-05's) stays Pending and its todo stays
  in `.planning/todos/pending/`. Removing the wrong row is the failure mode here.
- **No SEAM-02 re-scoring.** `.planning/REQUIREMENTS.md` is not edited. The verifier ruled in
  writing that SEAM-02's literal text is MET and is not being reverted on CR-05's account.
- **No edge-probe rows are owned by this plan.** All four are `34-10`'s; this plan authors no new
  predicates and re-runs no probe section.

## Traps this plan will otherwise hit

- **`state.*` tooling verbs mangle this file.** Do not use them. `state.update` guts the Current
  Position prose, `state.add-blocker` deletes standalone `NONE`/`none` tokens anywhere in the file
  (inverting unrelated sentences), and `frontmatter.set` re-serialises the YAML and empties every
  `>-` block scalar. Make both `.planning/STATE.md` edits with scoped `Edit` calls on the exact
  lines, and `git diff .planning/STATE.md` before committing.
- **`docs/phase34-host-tool-seam-decisions.md` is APPEND-ONLY for corrections**, per its own
  convention and `34-09`'s precedent. The `A-NN` table is the one place a row is added rather than
  appended; adding `A-15`/`A-16` as new rows at the end of that table is an addition, not a rewrite.
  `git diff` for this file must show additions only outside that table.
- **`npm run test:automated`'s floor is 2 failures in 1 file** (`anno-register.test.ts`, measured
  2026-09-03) — not zero. And **stop any running VICE broker first**: a live broker deterministically
  reds the BACK-05 case, which would make the gate's verdict about the broker rather than about this
  round.
- **`npm test` (the full glob) hangs forever** on `vice-proxy.test.ts`. Never write it.
- **`grep` needs `-a` on this tree** — `src/mcp/vice/anno-memmap-render.ts` contains a NUL byte.
- **The todo's own `files:` line citations go stale.** `34-10` adds roughly forty lines above
  `resolveWorkspacePath()`, so `host-tool.mts:370-386`, `:780-843` and `:1013-1017` all shift.
  Re-derive them against the post-fix source in the Resolution section rather than carrying them
  forward wrong — this project has had stale citations twice and its own convention is to treat a
  mismatch as drift to re-verify.

<tasks>

<task type="auto">
  <name>Task 1: The decision record gains a dated CR-05 correction and the two assumptions `34-10` had to make</name>
  <precondition>Plan `34-10` is merged and `cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test host-tool.test.ts resources-sync.test.ts` is green — the record describes what actually landed, and describing an unmerged change is how a record starts disagreeing with its code.</precondition>
  <files>docs/phase34-host-tool-seam-decisions.md</files>
  <read_first>
    - docs/phase34-host-tool-seam-decisions.md — Part 1 in full (the recorded `JVM_BINDING`, `JVM_STARTUP_MS_RANGE`, `JVM_STARTUP_MS_OBSERVED`, `JVM_REVERSAL_CONDITION`, and the "what `34-RESEARCH.md` could NOT answer" block whose honesty convention this correction follows); the `A-01`..`A-14` table at lines 141-154, specifically its four columns and the Reversibility-column phrasing (`Reversible — …` / `Costly — …`) that `A-15`/`A-16` must match; and the whole appended `34-09` correction at the end of the file — its four-section shape (what was wrong / what changed / what did NOT change / what is still unmeasured) is the shape this correction reuses
    - .planning/phases/34-the-host-tool-execution-seam/34-VERIFICATION.md — the `gaps:` frontmatter in full, the truth-3 row of the Observable Truths table, and the HOLLOW key-link row, which are the three places the record must answer
    - .planning/phases/34-the-host-tool-execution-seam/34-REVIEW.md — § CR-05 in full (the quoted lexical body, the blast-radius paragraph naming which two of the seven keys are write/read destinations, and the named fix), plus § WR-01, § WR-02 and § WR-03
    - .planning/phases/34-the-host-tool-execution-seam/34-10-PLAN.md — the `A-15` and `A-16` sections and the `<threat_model>`, so the record's wording matches the plan's rather than paraphrasing it into something slightly different
    - .planning/phases/34-the-host-tool-execution-seam/34-10-SUMMARY.md — what actually landed, including any flagged assumption or divergence recorded there; the correction describes what was done, not what was planned
  </read_first>
  <action>
Append a dated correction section to `docs/phase34-host-tool-seam-decisions.md`, below the existing
`34-09` correction — append, never rewrite. Reuse that correction's own four-section shape, in this
order:

  - **What was wrong.** `resolveWorkspacePath()` — the function this phase's own module header calls
    "the ONLY place a wire-supplied path becomes a real path" — enforced the workspace boundary with
    `path.resolve()` plus a `startsWith(rootAbs + sep)` string check and never consulted the
    filesystem. State that all seven keys in `HOST_TOOL_PATH_ARG_KEYS` inherited it and that two of
    them are write/read destinations (`acme.build`'s `outDir`, `oracle.run`'s `source`), with
    `ghidra.analyze`'s `preScript`/`postScript` selecting a script `analyzeHeadless` executes. Cite
    `34-VERIFICATION.md`'s gap 3 and `34-REVIEW.md`'s CR-05 by name, and record that the verifier
    reproduced the escape LIVE (a `mkdtemp` workspace, a `mkdtemp` external directory, a real
    `symlinkSync`, and a `writeFileSync` at the accepted path landing outside the root) rather than
    accepting it from source reading. Record the aggravating fact the verifier named: this bug class
    had already been found and fixed once in this codebase, in `anno-types.ts`'s
    `storePathWithinWorkspace()`.
  - **What changed.** Both the workspace root and the candidate path now go through one
    ancestor-realpath walk before the separator-appended comparison, and the walked candidate — the
    real path — is what `ok: true` returns. Name the walk's four behaviours briefly (walk up to the
    nearest existing path ENTRY, answer from the walk's own position when the filesystem root is
    reached, hop a dangling link against the LINK'S directory under a bound equal to Linux's
    `MAXSYMLINKS`, otherwise `realpathSync` and rejoin the tail) and state that every filesystem
    failure becomes a `{ ok: false, message }` refusal rather than a throw, so the module's own
    never-throw contract was not widened by adding filesystem access. Name the evidence: live
    planted-symlink refusals for a read key and a write key asserting the outside directory's own
    listing, the discriminating follow/dangling-inside/symlinked-root cases that redden an
    over-broad "refuse every symlink" fix, and a cross-implementation equivalence table against
    `storePathWithinWorkspace()`.
  - **What did NOT change.** The typed per-tool allowlist, the seven declared path keys and their
    census total, `buildHostToolArgv()`'s no-argv-passthrough property, and SEAM-02's `Complete`
    marking in `.planning/REQUIREMENTS.md` — the verifier ruled in writing that SEAM-02's literal
    text is met and is not reverted on CR-05's account, and this round did not re-score it in either
    direction. The `JVM_BINDING: per-invocation` decision, its measurements and its `N=20`/`P=30%`
    reversal condition stand verbatim; nothing in this round touched a budget.
  - **What is still NOT closed.** State each plainly, in the same register as Part 1's own A2/A3
    entries: the check-then-open window between the confinement decision and the child process's own
    open (no honest fix at this layer — the child is a third-party binary handed a path string, so
    there is no descriptor-based route to making the check and the open one operation, and no test
    added this round may be read as covering it); the byte-wise comparison that applies no Unicode
    normalisation, so two spellings differing only in normalisation form are two distinct paths here;
    and the three carried warnings — `WR-01` (the spawn-gate detector is evadable by aliasing the
    spawn function), `WR-02` (no admission control / concurrent-JVM ceiling) and `WR-03`
    (`runOracleRun()`'s unguarded `mkdirSync` and the CLI entry point's missing `.catch()`, filed as
    a pending todo, severity minor, still open and deliberately not folded into this round).

Then add exactly two new rows to the `A-01`..`A-14` table, matching its four-column shape and its
Reversibility-column phrasing:

  - `A-15` — the walk is implemented LOCALLY in `host-tool.mts` rather than imported from
    `anno-types.ts`, and the anti-drift mechanism is an equivalence test rather than a shared symbol.
    The rationale column states the structural block: `host-tool.mts` is host-bound and compiles into
    `resources/host-tool.mjs`, which runs on a bare host Node with no type-stripping; every `.mts`
    here imports only `node:*` builtins and sibling compiled `.mjs`; `anno-types.ts` is a
    container-side `.ts` that itself imports `./vice.ts` and `./disasm-opcodes.ts`, neither host-bound.
    Note that the equivalence test compares the two PUBLIC seams rather than a private symbol, so
    `anno-types.ts`'s export surface is unchanged. Reversibility: costly — collapsing the duplication
    later means touching `build.ts`'s `HOST_BOUND_ARTIFACTS`, `tsconfig.build.json`'s `include[]`,
    both consumers and the equivalence test in one commit.
  - `A-16` — the seam returns the REAL path, and the container-translation consequence is a recorded
    limit rather than a widened `hostpath.ts` consumer set. The rationale column states why the real
    path is the only honest return (it is exactly what gets spawned, so returning a lexical join
    while confining on a realpath hands the caller a string the filesystem then reinterprets), names
    the consequence (`containerPath()` throws for a host path matching no member of
    `hostRootCandidates()`, so on a host whose workspace root is itself reached through a symlink the
    response path becomes untranslatable on the container route), names the pre-existing mitigation
    (`HOST_WORKSPACE_PATH` naming the real root), and states why the fix is NOT to realpath
    `hostRootCandidates()` (`hostpath-consumers.test.ts`'s own header makes widening its five-member
    consumer set a REVIEWED DECISION, not a mechanical repair). Reversibility: costly.

Mark both rows as added in this round — dated, and pointing at `34-10-PLAN.md` — rather than
presenting them as if they had been recorded alongside `A-01`..`A-14`. A backdated assumption is a
record that lies about when it was known.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && git diff --numstat docs/phase34-host-tool-seam-decisions.md</automated>
    <fails_when>the deletions column is anything other than 0 — a non-zero deletion count means the correction rewrote existing text instead of appending, which is the one thing this document's convention forbids</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -a -c '^| A-1[56] |' docs/phase34-host-tool-seam-decisions.md</automated>
    <fails_when>the printed count is anything other than 2 — a 0 or 1 means a row is missing, a 3 or more means a duplicate row was introduced</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && grep -a -o -E 'CR-05|WR-01|WR-02|WR-03|MAXSYMLINKS' docs/phase34-host-tool-seam-decisions.md | sort -u | tr '\n' ' '</automated>
    <fails_when>the printed list is missing any one of `CR-05`, `WR-01`, `WR-02`, `WR-03` or `MAXSYMLINKS` — a missing `WR-0n` means a carried warning was silently dropped from the still-not-closed section, and a missing `MAXSYMLINKS` means the hop bound's provenance was not recorded</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-deferred-ledger.test.ts docs-review-disposition.test.ts</automated>
    <fails_when>non-zero exit, or `fail 0` absent — this task touches no todo and no ledger row, so both guards must be exactly as green as they were before it; a red here means the record edit strayed out of its own file</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff --numstat docs/phase34-host-tool-seam-decisions.md` reports 0 deletions — the correction is appended and the `A-15`/`A-16` rows are added, with nothing rewritten.
    - The appended correction has all four sections, in order, and each is answerable: what was wrong (citing gap 3 and CR-05 by name, and recording that the verifier reproduced it live), what changed (both sides walked, the real path returned, the never-throw contract preserved, and the evidence named), what did NOT change (the allowlist, the census total, the no-argv-passthrough property, SEAM-02's marking, and the JVM binding with its reversal condition), and what is still NOT closed (the check-then-open window, the normalisation divergence, and WR-01/WR-02/WR-03 each named).
    - Exactly two new `A-NN` rows exist, `A-15` and `A-16`, each with the table's four columns and a Reversibility column reading `Costly — …`.
    - Both new rows are marked as added in this round and point at `34-10-PLAN.md`; neither is presented as contemporaneous with `A-01`..`A-14`.
    - `.planning/REQUIREMENTS.md`, `.planning/STATE.md`, `.planning/ROADMAP.md` and every file under `.planning/todos/` are unmodified by this task: `git diff --name-only` for it lists exactly `docs/phase34-host-tool-seam-decisions.md`.
  </acceptance_criteria>
  <reversibility rating="reversible">An appended document section and two table rows, revertible with a single commit revert; nothing reads this document mechanically.</reversibility>
  <done>This phase's decision record — its substitute for the CONTEXT.md that never existed — describes the seam that now exists, records the two assumptions the fix rested on as newly made rather than backdated, and names what the fix does not close.</done>
</task>

<task type="auto">
  <name>Task 2: The gate runs, then the debt ledger and the todo tree agree with the code — in one commit</name>
  <precondition>No VICE broker process is running (`pgrep -af 'resources/vice-broker.mjs'` prints nothing) — a live broker deterministically reds the BACK-05 case in the suite this task's gate reads, which would make the gate's verdict about the broker rather than about this round.</precondition>
  <files>.planning/todos/pending/2026-09-03-cr-05-resolveworkspacepath-is-symlink-blind.md, .planning/todos/completed/2026-09-03-cr-05-resolveworkspacepath-is-symlink-blind.md, .planning/STATE.md</files>
  <read_first>
    - .planning/todos/pending/2026-09-03-cr-05-resolveworkspacepath-is-symlink-blind.md — in full, including its frontmatter `files:` list (whose `host-tool.mts` line citations `34-10` has shifted) and its three Constraints, each of which the Resolution must answer
    - .planning/todos/completed/2026-09-01-phase-32-review-round-4-nine-open-findings.md — the completed-todo convention: the frontmatter keys a closed todo carries (`resolves_phase`, `audit_acknowledged`) and how a Resolution section is written against finding ids
    - .planning/STATE.md — the `## Deferred Items` table (the CR-05 row and the WR-03 row directly below it, whose Item cells hold the todo filename stems); the paragraph above it stating the derived open-pending figure; and the prose near the table stating that the figure "is derived from, and must always equal, `## Deferred Items`'s table row count below — a discipline that has gone stale twice before and is not itself guarded"
    - src/mcp/vice/docs-deferred-ledger.test.ts — both direction tests and the non-vacuity floor, so it is clear that direction A keys on the pending directory and direction B on the completed directory, and that the Item cell must match as its OWN Markdown table cell
    - src/mcp/vice/docs-review-disposition.test.ts — the five disposition sources listed in its header, specifically that a todo in EITHER `pending` or `completed` naming the id satisfies it, which is why moving the file does not undispositon CR-05
    - .planning/phases/34-the-host-tool-execution-seam/34-10-SUMMARY.md — the commits, the measured suite results, and any recorded divergence; the Resolution cites these rather than restating the plan
  </read_first>
  <action>
Run the gate FIRST, before touching any todo or any ledger row, and quote its output in the SUMMARY.
The gate is the four commands in this task's `<verify>` block: the module-family suites, the two
store-side suites, the typecheck plus the two repo-level gates, and the full automated suite compared
against its measured 2-in-1-file floor. If any of them is red, STOP and fix rather than editing the
ledger — a marking that runs ahead of its gate is the exact failure `34-VERIFICATION.md` recorded for
SEAM-02 in the first place, and this task exists partly to not repeat it.

Then, in ONE commit — because `docs-deferred-ledger.test.ts` asserts both directions and would red on
whichever half arrived first:

  - `git mv` the CR-05 todo from `.planning/todos/pending/` to `.planning/todos/completed/`, keeping
    the filename stem byte-identical (the ledger guard and the disposition guard both key on it).
  - Append a `## Resolution` section to the moved file. It states: the closing plan (`34-10`) and
    plan `34-11` for the record; the commits from `34-10-SUMMARY.md`; that the fix took the LOCAL-walk
    route rather than the todo's own preferred "export and reuse `realpathOfNearestExisting()`" —
    naming the structural reason (`host-tool.mts` is host-bound and compiles to a `.mjs` that cannot
    import a container-side `.ts`) and pointing at `A-15`; how each of the todo's three Constraints
    was answered (the regenerated `resources/host-tool.mjs` committed and `resources-sync.test.ts`
    green; the `34-07`/`34-08` argv and census assertions re-proven unedited against a real-path
    return; and SEAM-02 deliberately NOT re-scored, per the verifier's own written ruling); and the
    residuals the fix does NOT close (the check-then-open window, the Unicode-normalisation
    divergence, and WR-03 as a separate still-open todo against the same file). Re-derive the
    `host-tool.mts` line citations in the frontmatter `files:` list against the post-fix source —
    `34-10` inserts roughly forty lines above `resolveWorkspacePath()`, so every one of the three
    ranges has moved — and note in the Resolution that they were re-derived rather than carried
    forward.
  - In `.planning/STATE.md`, delete the ONE Deferred Items table row whose Item cell is the CR-05
    todo's stem, with a scoped `Edit` on that exact line. Leave the WR-03 row directly below it
    untouched — it is still Pending and that is correct.
  - In the same `.planning/STATE.md` edit, correct the stated open-pending figure in the paragraph
    above the table so it equals the table's own row count after the removal. Do not restate the
    figure anywhere else and do not touch the suppressed/acknowledged sections or the
    carried-forward table, neither of which `docs-deferred-ledger.test.ts` scans.

Use scoped `Edit` calls for `.planning/STATE.md`, never a whole-file write and never the `state.*` or
`frontmatter.*` tooling verbs — `state.update` guts the Current Position prose, `state.add-blocker`
deletes standalone `NONE`/`none` tokens anywhere in the file, and `frontmatter.set` re-serialises the
YAML and empties every `>-` block scalar. Run `git diff .planning/STATE.md` and confirm it shows
exactly two changed lines before committing.

Touch `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` not at all.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node build.ts && node --test host-tool.test.ts host-tool-transport.test.ts ghidra-project.test.ts resources-sync.test.ts hostpath-consumers.test.ts spawn-seam.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports anything other than `fail 0`, or any case reports `skipped` other than the ACME-gated cases that already skip at HEAD — the gate must be green BEFORE the ledger edit, and a red here means the ledger edit does not happen at all</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test docs-deferred-ledger.test.ts docs-review-disposition.test.ts anno-confinement.test.ts anno-seam.test.ts</automated>
    <fails_when>non-zero exit, or `fail 0` absent — direction A reds if a pending todo lost its row, direction B reds if the moved todo is still listed as Pending, and the disposition guard reds if CR-05 became undispositioned by the move</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck && cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-npm-packages.mjs && node scripts/check-no-skill-external-spawn.mjs</automated>
    <fails_when>non-zero exit at any stage, or any `error TS` line, or the package check reporting an unexpected or missing file in either tarball, or the spawn gate's output missing its OK line</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run test:automated 2>&1 | tail -20</automated>
    <fails_when>the reported `fail` count exceeds 2, or the failing file is anything other than `anno-register.test.ts`, or the run does not reach a summary block at all — anything above the measured 2-in-1-file floor means the round regressed something, and the specific failing files must be named in the SUMMARY rather than absorbed into the floor</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && ls .planning/todos/pending/ | grep -c 'cr-05-resolveworkspacepath'; ls .planning/todos/completed/ | grep -c 'cr-05-resolveworkspacepath'; ls .planning/todos/pending/ | grep -c 'wr-03-host-tool-never-throws'</automated>
    <fails_when>the three printed counts are anything other than `0`, `1`, `1` in that order — a `1 0 1` means the move did not happen, and a third value of `0` means WR-03's todo was moved too, which is the wrong row and the trap this task names</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && git diff --numstat .planning/STATE.md</automated>
    <fails_when>the deletions column exceeds 4 or the insertions column exceeds 3 — the intended change is one removed ledger row plus the one or two wrapped prose lines carrying the open-pending figure; anything larger means a `state.*` verb or a whole-file write reflowed the document, which this project has been burned by</fails_when>
    <human-check>Read the appended Resolution section in the moved todo and confirm it reads as a closure rather than a claim: it names the plan and the commits, states that the LOCAL-walk route was taken instead of the todo's own preferred reuse route and why, answers all three of the todo's Constraints, and lists the residuals the fix does not close (the check-then-open window, the normalisation divergence, and WR-03) without softening any of them.</human-check>
  </verify>
  <acceptance_criteria>
    - The gate ran BEFORE any todo or ledger edit, and its output — including the measured `test:automated` failure count and the name of every failing file — is quoted in the SUMMARY.
    - `.planning/todos/pending/` no longer holds the CR-05 todo, `.planning/todos/completed/` holds it under the byte-identical filename stem, and the WR-03 todo is still in `.planning/todos/pending/`.
    - The moved todo carries a `## Resolution` section naming plan `34-10`, plan `34-11`, the commits from `34-10-SUMMARY.md`, the `A-15` route decision, an answer to each of its three Constraints, and the three residuals.
    - The moved todo's frontmatter `files:` line citations for `src/mcp/vice/host-tool.mts` are re-derived against the post-fix source, and the Resolution says they were re-derived.
    - `.planning/STATE.md`'s Deferred Items table no longer contains a row whose Item cell is the CR-05 stem, still contains the WR-03 row, and its stated open-pending figure equals the table's own row count.
    - `node --test docs-deferred-ledger.test.ts docs-review-disposition.test.ts` is green — both ledger directions and the disposition guard.
    - The file move and the `.planning/STATE.md` edit are in ONE commit: `git show --stat HEAD` for that commit lists the pending path, the completed path and `.planning/STATE.md` together.
    - `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` do not appear in `git diff --name-only` for this plan, and SEAM-02's marking is unchanged.
  </acceptance_criteria>
  <reversibility rating="reversible">A file move, an appended Resolution and two ledger lines; the gate that justifies them is re-runnable and the whole task reverts with one commit revert.</reversibility>
  <done>The debt ledger, the todo tree and the code agree — and they agree because a gate ran and passed, not because a plan said they would.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the code's actual state → the record that describes it | A record edited ahead of its gate is indistinguishable, to every later reader, from one the gate justified |
| `.planning/todos/pending/` → `.planning/STATE.md`'s Deferred Items table | A derived list and its ground truth; the two-directional guard is the only thing forcing them to agree |
| a closed finding → its disposition sources | Closing a finding must not make it undispositioned; the guard reads both todo trees and the phase SUMMARYs, which is why the move is safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-56 | Repudiation | A `blocker \| Pending` ledger row surviving a closed defect, or being removed while the defect survives | high | mitigate | The row removal and the file move land in one commit, both directions of `docs-deferred-ledger.test.ts` are asserted after it, and the whole task is gated on a recorded gate run |
| T-34-57 | Repudiation | A record edited ahead of the gate that justifies it | high | mitigate | The gate is the first action in Task 2, its output is quoted in the SUMMARY, and Task 1 carries a precondition asserting that `34-10` is merged and green |
| T-34-58 | Tampering | The WRONG ledger row removed — WR-03's sits directly below CR-05's, same category, same date, same file | medium | mitigate | A verify command asserts the three-way count `0/1/1` across the two todo directories, so removing or moving WR-03 reds it by construction |
| T-34-59 | Repudiation | A correction rewriting the original record into agreement with the present, leaving no evidence of what was believed before | medium | mitigate | The correction is APPENDED, and a `git diff --numstat` gate asserts zero deletions on the decision document |
| T-34-60 | Tampering | A `state.*` or `frontmatter.*` tooling verb reflowing `.planning/STATE.md` — measured behaviours: Current Position prose gutted, standalone `NONE` tokens deleted, `>-` block scalars emptied | medium | mitigate | Scoped `Edit` calls only, and a `git diff --numstat` bound on the changed-line count |
| T-34-61 | Repudiation | A residual presented as closed, so a later reader inherits a guarantee that was never made | medium | mitigate | All three residuals are named in the correction and again in the Resolution, and a `grep` gate asserts `WR-01`, `WR-02`, `WR-03` and `MAXSYMLINKS` all appear in the record |
| T-34-SC | Tampering | npm / pip / cargo installs | low | accept | No package-manager install task exists in this plan; `34-RESEARCH.md` § Package Legitimacy Audit records the audit as not applicable to this phase |
</threat_model>

<verification>
- `node build.ts && node --test host-tool.test.ts host-tool-transport.test.ts ghidra-project.test.ts resources-sync.test.ts hostpath-consumers.test.ts spawn-seam.test.ts` — `fail 0`, run BEFORE any bookkeeping edit.
- `node --test docs-deferred-ledger.test.ts docs-review-disposition.test.ts anno-confinement.test.ts anno-seam.test.ts` — `fail 0`, both ledger directions and the disposition guard.
- `npm run typecheck`, `node scripts/check-npm-packages.mjs`, `node scripts/check-no-skill-external-spawn.mjs` — all clean.
- `npm run test:automated` with no broker running — failing count no higher than the measured 2-in-1-file floor (`anno-register.test.ts`), with every failing file named in the SUMMARY.
- `git diff --numstat docs/phase34-host-tool-seam-decisions.md` — zero deletions.
- `git diff --numstat .planning/STATE.md` — at most 3 insertions and 4 deletions.
- The three-way todo count across `pending`/`completed` prints `0`, `1`, `1`.
</verification>

<success_criteria>
- The decision record describes the seam that now exists, records `A-15` and `A-16` as newly made rather than backdated, and names the check-then-open window, the normalisation divergence and all three carried warnings as still open.
- The CR-05 todo is closed on disk with a Resolution that answers its own three Constraints and re-derives its stale line citations.
- `.planning/STATE.md` carries no Pending row for a defect that no longer exists, still carries WR-03's, and its stated open figure matches its own table.
- Both directions of the ledger guard and the review-disposition guard are green, and the move plus the row removal are one commit.
- Every bookkeeping edit is downstream of a gate whose output is recorded — not of a plan having been written.
</success_criteria>

## Artifacts this phase produces

- Files modified (all git-tracked, verified with `git ls-files`):
  `docs/phase34-host-tool-seam-decisions.md`, `.planning/STATE.md`, and the
  `.planning/todos/pending/` → `.planning/todos/completed/` move of
  `2026-09-03-cr-05-resolveworkspacepath-is-symlink-blind.md`
- New document section: a dated CR-05 correction appended to
  `docs/phase34-host-tool-seam-decisions.md`, in the four-section shape the `34-09` correction
  established
- Two new rows in that document's `A-NN` decision table: `A-15` (the walk is local, the duplication
  is deliberate, the equivalence test is the price) and `A-16` (the seam returns the real path, and
  the container-translation consequence is a recorded limit) — both rated `Costly` and both marked
  as added this round
- New section in the moved todo: `## Resolution`, plus re-derived `src/mcp/vice/host-tool.mts` line
  citations in its frontmatter `files:` list
- Ledger change: one Deferred Items row removed from `.planning/STATE.md` and the stated
  open-pending figure corrected to match the table's own row count
- Deliberately NOT changed: `.planning/REQUIREMENTS.md` (SEAM-02's marking, in either direction),
  `.planning/ROADMAP.md`, WR-03's todo and its ledger row, and every source file under
  `src/` — this plan writes no code
- No new export, no new test, no new module, no `package.json` change, no `build.ts` change, no new
  environment variable

<output>
Create `.planning/phases/34-the-host-tool-execution-seam/34-11-SUMMARY.md` when done
</output>
