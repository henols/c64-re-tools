---
phase: 34-the-host-tool-execution-seam
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/vice/host-tool.mts
  - src/mcp/vice/host-tool.test.ts
  - src/mcp/vice/ghidra-project.mts
  - src/mcp/vice/ghidra-project.test.ts
  - src/mcp/vice/resources/host-tool.mjs
  - src/mcp/vice/resources/ghidra-project.mjs
  - src/skills/acme-build/SKILL.md
autonomous: true
gap_closure: true
closes_gaps: [1]
closes_findings: [CR-02, CR-03]
requirements: [SEAM-02]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every `acme.build` `includes` entry is resolved through `resolveWorkspacePath()` before it reaches ACME's include-search-path argv, and an entry that escapes the workspace root refuses the WHOLE request — the argv never carries a raw wire string (SEAM-02, CR-03)."
    - "Every `ghidra.analyze` `preScript`/`postScript` value is resolved through `resolveWorkspacePath()` before it reaches `analyzeHeadless`'s argv, and an escaping or absolute value refuses the whole request (SEAM-02, CR-02)."
    - "`host-tool.mts`'s own header claim that `ghidra.analyze`'s accepted keys flow through `resolveWorkspacePath()` before reaching argv is TRUE of the code as written — the comment and the code agree (CR-02)."
    - "`buildHostToolArgv()` reads every path it emits from its resolved-paths parameter, never from `request.args` — proven behaviourally: no argv element equals the raw workspace-relative string a caller supplied for an include or a pre/post script."
    - "A path that resolves EXACTLY to the workspace root is accepted, and a sibling directory whose name merely begins with the workspace root's name is refused — the boundary check separates rather than merges at the touching case (SEAM-02, edge: adjacency)."
    - "`includes: []` and an absent `includes` key both build a valid argv carrying no include flag at all, and an empty-string entry is refused by the existing narrowing rather than silently skipped (SEAM-02, edge: empty)."
    - "Two `includes` entries that resolve to the same absolute directory both appear in the argv, in caller-given order, with no dedupe and no reordering — output order is specified and stable (SEAM-02, edge: ordering)."
    - "`buildAnalyzeHeadlessArgv()` independently refuses a `preScript`/`postScript` carrying a parent-directory segment, so the rule holds for a caller that bypassed the executor — while a bare Ghidra script NAME stays accepted, exactly as the dot-segment rule is already enforced in two layers (SEAM-02/SEAM-04)."
  artifacts:
    - path: "src/mcp/vice/host-tool.mts"
      provides: "`includePaths` on the acme.build resolved-paths shape, `preScriptPath`/`postScriptPath` on the ghidra.analyze resolved-paths shape, and the resolution sites that fill them"
      contains: "resolveWorkspacePath"
    - path: "src/mcp/vice/ghidra-project.mts"
      provides: "the independent parent-directory-segment refusal for preScript/postScript in buildAnalyzeHeadlessArgv()"
    - path: "src/mcp/vice/host-tool.test.ts"
      provides: "refusal cases for an escaping include and an escaping/absolute pre/post script, the adjacency/empty/ordering edge cases, and an end-to-end refusal observed over the real control-plane route"
    - path: "src/mcp/vice/resources/host-tool.mjs"
      provides: "the regenerated host-bound artifact the broker and the test suite actually load"
    - path: "src/mcp/vice/resources/ghidra-project.mjs"
      provides: "the regenerated sibling artifact host-tool.mjs value-imports"
  key_links:
    - "`runHostTool()` resolves EVERY path-bearing wire field and hands only resolved absolute paths to `buildHostToolArgv()`; `buildHostToolArgv()` reads paths exclusively from that parameter, so there is one boundary-check site for every tool rather than one per field."
    - "`host-tool.mts` reaches the pre/post-script second layer through its existing value import of `./ghidra-project.mjs` — the rule is added to the file that already owns argv construction for Ghidra, never copied into the executor."
    - "`node build.ts` regenerates BOTH `resources/host-tool.mjs` and `resources/ghidra-project.mjs`; `host-tool.test.ts` loads the executor as the committed artifact, so an unrebuilt commit fails `resources-sync.test.ts` and tests the old code."
  prohibitions:
    - "MUST NOT degrade a refusal into a partial success: a request carrying one out-of-workspace path argument is refused whole, never executed with the offending entry silently dropped and the rest of the build proceeding."
    - "MUST NOT leave a header comment asserting a security invariant the code does not enforce — a claim about a boundary check is only written where the check exists, because a comment that lies about a boundary is what let this defect survive a review."
    - "MUST NOT widen the workspace boundary to make an existing documented invocation keep working — the boundary is the requirement, and a newly-refused invocation is documented instead."
---

<objective>
Close the two argv-passthrough paths that reach a child process's argv unresolved:
`acme.build`'s `includes` array (CR-03) and `ghidra.analyze`'s `preScript`/`postScript`
(CR-02). Both are wire-supplied strings that today reach a host-side child process's
argument list with no workspace-boundary check, while `source`/`outDir`/`importPath` three
lines away go through `resolveWorkspacePath()`.

Purpose: ROADMAP Success Criterion 1's second half — "a typed per-tool allowlist with **no
argv passthrough anywhere**" — is scored FAILED in `34-VERIFICATION.md`, and
`host-tool.mts:16-19` states the same invariant in its own words ("argv is built ENTIRELY
from typed fields... never from a raw wire array or a raw wire string"). Two of the three
confirmed violations are here. The third — `oracle.probe`'s `command` — is plan `34-08`,
because it changes a wire contract and carries its own client-side migration.

Output: one resolution site per path-bearing field, a resolved-paths parameter that is the
only source of paths inside `buildHostToolArgv()`, a second independent layer in
`ghidra-project.mts`, a corrected header comment, and a test per refusal — none of which
exist today.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-the-host-tool-execution-seam/34-VERIFICATION.md
@.planning/phases/34-the-host-tool-execution-seam/34-REVIEW.md
@.planning/phases/34-the-host-tool-execution-seam/34-PATTERNS.md
@.planning/phases/34-the-host-tool-execution-seam/34-01-SUMMARY.md
@.planning/phases/34-the-host-tool-execution-seam/34-03-SUMMARY.md
@docs/phase34-host-tool-seam-decisions.md
</context>

## Why this is a closure round, not a re-plan

`34-01`..`34-06` executed and their SUMMARYs are the record. This plan and its two siblings
(`34-08`, `34-09`) are additive: they change named lines in modules those plans created and
add cases to the suites those plans created. Nothing here supersedes or renumbers them.

Four of the six verified truths and five of the seven requirements are discharged — do not
re-prove them. Where an edge below is already covered by settled work, it is recorded as a
flagged assumption naming the plan or evidence file that discharged it.

## Multi-source coverage audit (whole closure round: 34-07, 34-08, 34-09)

```
SOURCE    | ID       | Feature / requirement                                                | Plan  | Status
--------- | -------- | -------------------------------------------------------------------- | ----- | ---------
GOAL      | SC1      | "typed per-tool allowlist with no argv passthrough anywhere"          | 07,08 | COVERED
GOAL      | goal     | Ghidra reached through the seam (a run that can COMPLETE, not only refuse) | 09 | COVERED
REQ       | SEAM-01  | Typed control op before the lease path, no lease consumed             | —     | DISCHARGED (verifier: verified; 34-01)
REQ       | SEAM-02  | Typed per-tool allowlist, no argv passthrough                        | 07,08 | COVERED
REQ       | SEAM-03  | path + digest + length through containerpath.ts                      | —     | DISCHARGED (verifier: verified; 34-01/34-02)
REQ       | SEAM-04  | Per-run Ghidra dir, -deleteProject, dot-refusal in code               | 09    | COVERED (completion question only; refusal claim discharged)
REQ       | SEAM-05  | Migrations + whole-tree spawn gate, npm-pack scope                    | —     | DISCHARGED (verifier: verified; 34-04/34-05)
REQ       | SEAM-06  | Second prefix floor inside the closed-consumer discipline             | —     | DISCHARGED (verifier: verified; 34-06)
REQ       | SEAM-07  | JVM lifetime binding recorded with measurement + reversal condition   | 09    | COVERED (usability of the recorded binding only)
RESEARCH  | Find. 2  | Ghidra refuses a dot-prefixed path after 12-16 s of JVM startup       | —     | DISCHARGED (34-03, evidence/34-ghidra-dotpath.md)
RESEARCH  | A2       | Whether -process batch framing amortises JVM startup (UNMEASURED)     | NONE  | EXCLUDED — RESEARCH.md marks it out of this phase's scope
RESEARCH  | A3       | GitHub Actions cost of installing Ghidra + a JDK (no published figure) | NONE | EXCLUDED — RESEARCH.md records no number exists to inherit
CONTEXT   | JVM_BINDING | per-invocation, with measurement and reversal condition            | 09    | COVERED (record corrected where it describes a route that cannot complete)
CONTEXT   | A-02     | One log line per attempted invocation, naming tool + exit + elapsed   | 09    | COVERED (extended with the applied budget)
CONTEXT   | A-03     | resolveWorkspacePath() is the ONE place a wire path becomes a real path | 07,08 | COVERED
CONTEXT   | A-06     | The dot-segment rule and Ghidra argv live in ghidra-project.mts only  | 07    | COVERED
REVIEW    | CR-01    | oracle.probe's `command` spawns an arbitrary host file                | 08    | COVERED
REVIEW    | CR-02    | preScript/postScript reach argv unvalidated                           | 07    | COVERED
REVIEW    | CR-03    | includes reach -I unresolved                                          | 07    | COVERED
REVIEW    | CR-04    | Both timeout budgets shorter than Ghidra's own startup cost           | 09    | COVERED
REVIEW    | WR-01    | Spawn-gate detector evadable by aliasing the spawn function           | NONE  | DEFERRED — see the deferrals block below
REVIEW    | WR-02    | host_tool has no admission control / concurrent-JVM ceiling          | NONE  | DEFERRED — see the deferrals block below
```

No row is `⚠ MISSING`. The two `NONE` rows are the verifier's own explicitly-not-scored
warnings, deferred visibly below rather than dropped.

## Explicit deferrals (visible, not silent — do NOT convert these into must_haves)

- **WR-01** (`scripts/check-no-skill-external-spawn.mjs:270-304` — the call-site detector
  matches only a literal `spawnSync`/`spawn`/`execFileSync`/... identifier adjacent to the
  opening parenthesis, so an aliased call escapes it). `34-VERIFICATION.md` records it as a
  warning and did NOT score it as a gap. No plan in this round edits that script, so it is
  not folded in. Full statement and suggested fix: `34-REVIEW.md` § WR-01.
- **WR-02** (`host_tool` has no admission control: unbounded concurrent host-side children,
  including JVMs). Also recorded as a warning, not a gap. Plan `34-09` raises
  `ghidra.analyze`'s per-invocation budget, which makes a concurrency ceiling more valuable,
  not less — recorded here so the next phase inherits a visible item rather than a surprise.
  Full statement: `34-REVIEW.md` § WR-02.

## Edge-probe ledger (spec-less fallback — 16 rows, 16 dispositions, no silent drops)

`34` has no `*-SPEC.md`, so the deterministic edge probe ran over `SEAM-01`..`SEAM-07`
(`applicable: 16, resolved: 0, unresolved: 16`). Every row is disposed below. Rows owned by
a requirement this phase already discharged are recorded as flagged assumptions naming the
plan or evidence that discharged them — that is the no-silent-drop rule satisfied without
re-implementing settled work.

| # | Req | Category | Disposition | Where |
|---|-----|----------|-------------|-------|
| 1 | SEAM-01 | concurrency | flagged assumption — DISCHARGED by `34-01`'s live overlapping-request spy case in `host-tool.test.ts` (two concurrent `host_tool` requests both resolve, distinct output paths, all seven VICE-callback spies at zero) | — |
| 2 | SEAM-02 | adjacency | truth (explicit) — the exactly-at-the-root and prefix-sibling boundary cases | this plan |
| 3 | SEAM-02 | empty | truth (explicit) — empty/absent `includes`, empty-string entry | this plan |
| 4 | SEAM-02 | ordering | truth (explicit) — equal-resolving includes keep caller order, no dedupe | this plan |
| 5 | SEAM-03 | boundary | flagged assumption — DISCHARGED by `evidence/34-transport-cap.md`'s exact-byte controls (`65536` open, `65537` destroyed) and `host-tool-transport.test.ts` | — |
| 6 | SEAM-03 | empty | flagged assumption — DISCHARGED by `34-01`'s zero-byte-output case (`byteLength: 0` plus the sha256 of the empty byte string, never an omitted entry) | — |
| 7 | SEAM-03 | encoding | flagged assumption — DISCHARGED by `34-01`'s multi-byte-UTF-8 case (`byteLength` equals `statSync`'s byte count, not a character count) | — |
| 8 | SEAM-03 | precision | flagged assumption — DISCHARGED by the same case: the only numeric in the response shape is an integer byte count from `statSync`, and the digest is hex text — no floating point, no rounding contract to state | — |
| 9 | SEAM-03 | concurrency | flagged assumption — DISCHARGED by `34-01`'s two-overlapping-requests case (distinct output paths) | — |
| 10 | SEAM-04 | idempotency | flagged assumption — DISCHARGED by `resolveGhidraProject()`'s refuse-on-reuse plus its `ghidra-project.test.ts` case | — |
| 11 | SEAM-04 | concurrency | truth (backstop) — concurrent invocations under the raised budget share no timer state | `34-09` |
| 12 | SEAM-05 | unclassified | **UNRESOLVED — flagged assumption, carried forward.** The probe could not classify this row ("unclassified — review manually") and this round does not invent a category for it. `SEAM-05` itself is discharged (verifier: verified — the gate is wired into CI at `ci.yml:200`, exits 0 on the real tree, 18/18 gate cases including three planted-violation shapes). The nearest genuinely-open item on that surface is **WR-01**, deferred above. | — |
| 13 | SEAM-06 | adjacency | flagged assumption — DISCHARGED by `34-06`'s `HOST_TOOL_FAMILY_FLOOR` pinned over a `host-tool`/`ghidra`/`dxa` prefix union that is disjoint from the `anno-` family | — |
| 14 | SEAM-06 | empty | flagged assumption — DISCHARGED by `34-06`'s synthetic empty-directory control, which is how that floor's capacity to fail is proven | — |
| 15 | SEAM-06 | ordering | flagged assumption — DISCHARGED by `34-06`'s set-comparison over the enumerated family (22/22), which is order-insensitive by construction | — |
| 16 | SEAM-07 | concurrency | truth (explicit) — two overlapping slow invocations both complete on their own deadlines | `34-09` |

Prohibition recall (§B) ran in-prompt for this plan; canon items were dropped with a
breadcrumb rather than minted: *path traversal and arbitrary host-file read are canon —
covered by `/gsd-secure-phase` and this plan's own `<threat_model>`; not minted as
prohibitions.* The three kept items are in `must_haves.prohibitions` above, authored
descriptor-less.

## Traps this plan will otherwise hit (all measured, not guessed)

- `host-tool.mts` and `ghidra-project.mts` are **host-bound** `.mts` modules with committed
  `resources/*.mjs` counterparts. `host-tool.test.ts` loads the executor as the BUILT
  artifact (`await import(new URL("./resources/host-tool.mjs", …))`) after calling `build()`,
  so an unrebuilt commit both reds `resources-sync.test.ts` and silently tests old code.
  Run `node build.ts` and commit both regenerated `.mjs` files in the same commit.
- Do **not** add an export to `vice-broker-client.ts`: `vice-broker-client.test.ts:1208`
  pins that module's export list by exact set equality.
- Do **not** write the tokens `binPath`, `viceBin`, `VICE_BIN` or `x64sc` in any shipped
  file — `spawn-seam.test.ts` classifies a bare identifier match as an emulator spawn site.
- `npm test` (full glob) hangs forever on `vice-proxy.test.ts`. Use `npm run test:automated`,
  whose green floor is **5 failures in 3 files** (measured 2026-09-02), not zero. Stop any
  live VICE broker first — a running broker deterministically reds the BACK-05 case.
- `anno-memmap-render.ts` contains a NUL byte, so any repo-wide census grep needs `grep -a`.
- Nothing in this plan goes near `forwardToVice()`, so `docs-linerefs.test.ts`'s four
  citations (`:3050` / `:2985` / `:1529` / `:1505`) are untouched. If a change here ever
  does move them, repairing the citations and the test is part of that change.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — one escaping `acme.build` include, refused across the real control-plane route</name>
  <files>src/mcp/vice/host-tool.mts, src/mcp/vice/host-tool.test.ts, src/mcp/vice/resources/host-tool.mjs, src/skills/acme-build/SKILL.md</files>
  <read_first>
    - src/mcp/vice/host-tool.mts (the whole file, but specifically: the header invariant block at :13-46; `HOST_TOOL_ARG_KEYS` at :101-110; `resolveWorkspacePath()` at :318-333 including its two refusal messages verbatim; `ResolvedAcmeBuildPaths` at :340-343; `buildHostToolArgv()`'s acme branch at :366-403 — the include loop is line :400; and `runHostTool()`'s acme resolution block at :655-670, which is the CORRECT pattern this task extends: `source` and `outDir` are each resolved and only the resolved value is passed on)
    - src/mcp/vice/host-tool.test.ts (`withTempDir`, `withFakeAcme`, `writeFakeAcme`, `startListenerWithSpies`, `assertAllSpiesEmpty`, and the two existing END-TO-END control-plane cases at :385-446 — the harness this task reuses rather than re-invents; also the `SKIP_REASON` ACME gate at :38 and how the existing e2e cases opt into it)
    - src/mcp/vice/host-tool-client.ts (`hostToolOverControlPlane()`'s signature and its resolve-on-tool-refusal / reject-on-transport-failure split, documented at :92-101 — a refusal must RESOLVE, so the new e2e case asserts on `response.ok === false`, never on a thrown error)
    - src/mcp/vice/resources/host-tool.mjs (the committed artifact, to confirm it is generated and banner-marked and must not be hand-edited)
    - src/skills/acme-build/scripts/acme.mjs (:203 and :320 — how `-I` reaches the seam as `args.includes`, so the documented CLI contract change is stated accurately)
    - src/skills/acme-build/SKILL.md (:22-23, the options line naming `-I DIR`, and :195-208, the Phase 34 route paragraph)
  </read_first>
  <behavior>
    - A request `{ tool: "acme.build", args: { source: "a.a", includes: ["../outside"] } }` is refused: the response is `{ ok: false, message }`, the message carries the workspace-escape wording `resolveWorkspacePath()` already produces, and the log spy recorded NOTHING (the module logs one line per ATTEMPTED invocation, so an empty log proves no child was spawned).
    - The same request with an ABSOLUTE include is refused with the not-absolute wording.
    - A request whose includes are `["inc", "inc2"]`, both existing inside the temp workspace, produces an argv in which each include flag is followed by the ABSOLUTE resolved directory, and no argv element equals the bare string `inc` or `inc2`.
    - Include order is the caller's: `["b", "a"]` yields the resolved `b` directory before the resolved `a` directory. Two entries that resolve to the SAME absolute directory both appear — no dedupe, no reordering.
    - `includes: []` and an absent `includes` key both produce an argv with no include flag at all; an empty-string entry (`[""]`) is refused by the existing narrowing with the array-of-strings message, not silently skipped.
    - Adjacency: an include of `.` (which resolves exactly TO the workspace root) is ACCEPTED, and a relative include that resolves to a SIBLING directory whose name merely begins with the workspace root's own name (built as `../<basename-of-root>-evil`) is REFUSED — touching and merging are different outcomes.
    - END TO END over the real control-plane route: with the real `startControlListener()` and the real `hostToolOverControlPlane()` client, the escaping-include request resolves with `ok: false` at the container-side caller and all seven VICE-callback spies stay at zero calls. This case must NOT be gated on the ACME availability skip — nothing spawns, so nothing needs ACME installed.
  </behavior>
  <action>
Extend `ResolvedAcmeBuildPaths` in `src/mcp/vice/host-tool.mts` with a third field,
`includePaths: string[]`, and change `buildHostToolArgv()`'s `acme.build` branch to emit one
include flag per entry of `resolved.includePaths` instead of reading the wire array. After
this change the acme branch reads NO path from `request.args` — it reads `format`, `setpc`,
`defines` and `noReport` (non-path scalars) from the narrowed args, and every path from the
resolved-paths parameter. That is the file's own documented contract at :334-339 made true
for this branch.

In `runHostTool()`'s `acme.build` block, immediately after the existing `source` and
`outDir` resolutions, resolve each entry of `request.args.includes ?? []` through
`resolveWorkspacePath(repoRootAbs, entry)` into a local array, and on the FIRST refusal
return that refusal's own `{ ok: false, message }` unchanged — the whole request fails, the
offending entry is never dropped and the remaining entries are never resolved. Pass the
array on as `includePaths`. An absent or empty `includes` yields an empty array, which the
argv builder emits as no flags at all.

Update the header comment block so the acme.build description matches: the file's WHAT NOT
TO DO list already states that `resolveWorkspacePath()` is the only place a wire-supplied
path becomes a real path (T-34-03) — add the include-search-path argument to the set of
fields that statement covers, so a future reader can tell from the header that this field is
inside the guarantee, not outside it.

Then extend `src/mcp/vice/host-tool.test.ts` with a case per `<behavior>` bullet. Build the
positive cases with `withTempDir` plus `mkdirSync` for the include directories, and drive
the refusal cases through `runHostTool` with a `log` spy so the "no child was spawned"
assertion is the empty spy rather than a claim. Build the END-TO-END case on
`startListenerWithSpies` + `hostToolOverControlPlane` exactly as the two existing e2e cases
do — same `brokerJsonPath(stateDir)` scaffolding, same `listener.server.close()` in
`finally` — but WITHOUT the `skip: SKIP_REASON` option, and assert `assertAllSpiesEmpty`.

Finally, in `src/skills/acme-build/SKILL.md`, state on the options line (or immediately
below it) that the include directory is resolved relative to the project root the host
broker was launched with, and that an absolute or escaping directory is refused by the seam
rather than passed to the assembler. This is a documented contract change: an absolute
include used to reach ACME and now does not. Keep the existing Phase 34 route paragraph's
claim about `$ACME` intact — that is the host-side library probe, a different mechanism from
`-I`.

Run `node build.ts` and commit the regenerated `src/mcp/vice/resources/host-tool.mjs` in the
same commit as the source change.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node build.ts &amp;&amp; node --test host-tool.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports anything other than `fail 0`, or the reported `pass` count is below 38 (31 existing cases plus at least 7 new ones), or any case reports `skipped`</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test resources-sync.test.ts</automated>
    <fails_when>non-zero exit, or `fail 0` absent — a red here means the committed artifact drifted from the `.mts` source, i.e. `node build.ts` was not run or its output was not committed</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck</automated>
    <fails_when>non-zero exit, or any `error TS` line in the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && grep -a -v '^\s*[/*]' host-tool.mts | grep -c 'includePaths'</automated>
    <fails_when>the printed count is below 3 — the field must appear in the resolved-paths type, at the argv-builder read site, and at the resolution site; a count of 0-2 means one of the three is missing</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && node scripts/check-skill-cli-invocations.mjs</automated>
    <fails_when>non-zero exit, or the output does not begin `check-skill-cli-invocations: OK` — the SKILL.md edit must not break a documented invocation</fails_when>
  </verify>
  <acceptance_criteria>
    - A request whose `includes` contains `../outside` is refused with a message containing `escapes the workspace root`, and the `log` spy passed via `deps.log` recorded zero lines.
    - A request whose `includes` contains an absolute path is refused with a message containing `must be relative to the workspace root, not absolute`.
    - For an accepted in-workspace include, the argv element following the include flag is an absolute path (starts with the platform separator) and equals `resolvePath(tempRoot, "inc")`; no argv element equals the bare string `inc`.
    - `["b","a"]` produces the resolved `b` path at a lower argv index than the resolved `a` path; `["inc","inc"]` produces two include flags, not one.
    - `includes: []` and an absent `includes` both produce argv arrays that are deepEqual to each other.
    - `includes: [""]` is refused with the existing array-of-strings message from `normaliseHostToolRequest`.
    - An include of `.` is accepted; an include built as `../<basename(tempRoot)>-evil` is refused with the escape message.
    - The END-TO-END case resolves (never throws) with `response.ok === false`, and `assertAllSpiesEmpty(spies)` passes; the case carries no `skip` option.
    - `git diff --stat` for this task includes `src/mcp/vice/resources/host-tool.mjs`.
    - `src/skills/acme-build/SKILL.md` states the include directory is workspace-relative: `grep -c 'workspace-relative\|relative to the project root' src/skills/acme-build/SKILL.md` is at least 1.
  </acceptance_criteria>
  <reversibility rating="costly">Refusing an absolute `-I` narrows a documented CLI option's accepted input, so a user's existing absolute-include invocation starts failing; reversing it means re-widening the very boundary SEAM-02 exists to hold, which is why the fix is paired with a SKILL.md statement rather than a compatibility shim.</reversibility>
  <done>An escaping include refuses the whole request end to end across the real control-plane route, an accepted include reaches the assembler as a resolved absolute path, and the argv builder no longer reads any path from the wire request for this tool.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `ghidra.analyze`'s `preScript`/`postScript` resolved, the false header comment corrected, and the rule enforced in a second layer</name>
  <files>src/mcp/vice/host-tool.mts, src/mcp/vice/ghidra-project.mts, src/mcp/vice/host-tool.test.ts, src/mcp/vice/ghidra-project.test.ts, src/mcp/vice/resources/host-tool.mjs, src/mcp/vice/resources/ghidra-project.mjs</files>
  <read_first>
    - src/mcp/vice/host-tool.mts (`HOST_TOOL_ARG_KEYS`'s doc comment at :96-100 — the claim that is FALSE today and must become true; `ResolvedGhidraAnalyzePaths` at :345-355; `buildHostToolArgv()`'s ghidra branch at :405-445, specifically the two `argvInput` assignments at :434-441; and `runHostTool()`'s ghidra block at :671-687, where `importPath` is resolved correctly and the two script fields are not resolved at all)
    - src/mcp/vice/ghidra-project.mts (`BUILD_ANALYZE_HEADLESS_ARGV_KEYS` at :235-245; `buildAnalyzeHeadlessArgv()` at :262-307, including the INDEPENDENT dot-segment re-check at :293-300 and its own comment explaining why the rule is re-run for a caller that skipped the resolver — that is the precedent this task follows; and `hasDotPrefixedSegment()`'s result shape)
    - src/mcp/vice/ghidra-project.test.ts (:242-255 — the existing case that passes a BARE script name and must stay green, which is why the second layer refuses a parent-directory segment rather than requiring an absolute path)
    - src/mcp/vice/host-tool.test.ts (`withFakeGhidraHome` at :461-477 and the five existing `ghidra.analyze` cases at :478-536 — extend that block, do not start a second suite)
    - .planning/phases/34-the-host-tool-execution-seam/34-REVIEW.md (§ CR-02 in full, including the quoted header comment and the named fix)
  </read_first>
  <behavior>
    - `runHostTool` on a well-formed `ghidra.analyze` request whose `preScript` is `../../etc/evil.java` is refused with the workspace-escape message, and the log spy recorded nothing.
    - The same with an ABSOLUTE `postScript` is refused with the not-absolute message.
    - An in-workspace `preScript` (a file written inside the temp workspace) reaches the built argv as the ABSOLUTE resolved path; no argv element equals the bare relative string the caller supplied.
    - `buildHostToolArgv()` reads the two script values from its resolved-paths parameter: called directly with a resolved-paths object carrying the script paths and a `request` whose `args` carry DIFFERENT script strings, the argv contains the resolved-parameter values and not the `args` values.
    - `buildAnalyzeHeadlessArgv()` independently refuses a `preScript` or `postScript` containing a parent-directory segment, naming the offending field, even when `projectLocation` is clean — so the rule holds for a caller that bypassed the executor entirely.
    - `buildAnalyzeHeadlessArgv()` still accepts a bare Ghidra script NAME (the existing `Pre.java` / `Post.java` case is unchanged and green) and still accepts an absolute in-workspace path.
    - Every case runs with no Ghidra installation present (`withFakeGhidraHome` only writes a fake launcher file; nothing is spawned by an argv-construction case).
  </behavior>
  <action>
In `src/mcp/vice/ghidra-project.mts`, add an independent second-layer check inside
`buildAnalyzeHeadlessArgv()`: after the existing type checks and alongside the existing
independent dot-segment re-check on `projectLocation`, refuse when `preScript` or
`postScript` contains a parent-directory path segment, with a message naming which field
carried it and stating that a script argument is either a bare Ghidra script name or a path
already bounded inside the workspace. Reuse the module's existing per-segment splitting
approach rather than a substring test, so a name that merely CONTAINS two dots is not
misjudged. Do not require absoluteness: a bare script name is Ghidra's own documented form
for these flags and `ghidra-project.test.ts:242` already pins it.

In `src/mcp/vice/host-tool.mts`:

  - Extend `ResolvedGhidraAnalyzePaths` with two optional fields, `preScriptPath?: string`
    and `postScriptPath?: string`.
  - In `buildHostToolArgv()`'s ghidra branch, fill `argvInput`'s two script fields from
    those resolved fields instead of from `request.args`. After this change the ghidra
    branch, like the acme branch in Task 1, reads no path from the wire request.
  - In `runHostTool()`'s ghidra block, resolve each present script field through
    `resolveWorkspacePath(repoRootAbs, value)` — the SAME call `importPath` one line above
    already uses — returning the refusal unchanged on failure, and pass the resolved values
    into `buildHostToolArgv()`'s resolved-paths argument.
  - Correct the doc comment on `HOST_TOOL_ARG_KEYS` (:96-100) so it describes what the code
    now does. Today it claims all four `ghidra.analyze` keys flow through
    `resolveWorkspacePath()`/`resolveGhidraProject()` before reaching argv, which was false
    for the two script fields — that false claim is part of the defect, because it is what a
    reader would have trusted instead of reading the branch. State the mechanism per field:
    `runId` through the per-run project resolver, `importPath`/`preScript`/`postScript`
    through the workspace-boundary resolver, and add that the argv builder re-checks the
    script fields independently.

Then extend `src/mcp/vice/ghidra-project.test.ts` with the two second-layer refusal cases
plus a case asserting the bare-name form is still accepted, and extend
`src/mcp/vice/host-tool.test.ts`'s existing `ghidra.analyze` block with the escape, absolute,
resolved-absolute-in-argv, and resolved-parameter-precedence cases from `<behavior>`.

Run `node build.ts` and commit BOTH regenerated artifacts —
`src/mcp/vice/resources/host-tool.mjs` and `src/mcp/vice/resources/ghidra-project.mjs` — in
the same commit as the source change.

Record one flagged assumption in the SUMMARY: whether real `analyzeHeadless` locates a
script given as an absolute in-workspace path (as opposed to a bare name resolved against
its own script directories) is NOT proven by this round — no test and no transcript
exercises a real Ghidra run with a script flag. The boundary check is what the gap requires
and what this task delivers; if a later phase needs the absolute form to actually load, the
mechanism to reach for is Ghidra's own script-path flag pointing at a resolved in-workspace
directory, and that is a functional change with its own live evidence, not a boundary change.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node build.ts &amp;&amp; node --test host-tool.test.ts ghidra-project.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports anything other than `fail 0`, or the combined `pass` count is below 73 (38 from Task 1 plus 28 existing ghidra-project cases plus at least 7 new ones), or any case reports `skipped`</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test resources-sync.test.ts</automated>
    <fails_when>non-zero exit, or `fail 0` absent — both regenerated artifacts must be committed</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck</automated>
    <fails_when>non-zero exit, or any `error TS` line in the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && grep -a -v '^\s*[/*]' host-tool.mts | grep -c 'preScriptPath\|postScriptPath'</automated>
    <fails_when>the printed count is below 4 — two fields declared on the resolved-paths type, read at the argv-builder site, and written at the resolution site; below 4 means one leg is missing</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && env -u GHIDRA_HOME -u GHIDRA_INSTALL_DIR node --test ghidra-project.test.ts</automated>
    <fails_when>non-zero exit, or `fail 0` absent, or any case reports `skipped` — the pure module's suite must pass with no Ghidra pointed at and must not skip its way to green</fails_when>
  </verify>
  <acceptance_criteria>
    - `runHostTool` refuses a `preScript` of `../../etc/evil.java` with a message containing `escapes the workspace root`, and the `deps.log` spy recorded zero lines.
    - `runHostTool` refuses an absolute `postScript` with a message containing `must be relative to the workspace root, not absolute`.
    - For an accepted in-workspace `preScript`, the argv element following the pre-script flag is an absolute path equal to `resolvePath(tempRoot, <relative>)`, and no argv element equals that relative string.
    - Called directly with resolved script paths AND differing `args` script strings, `buildHostToolArgv()`'s argv contains the resolved values and contains neither `args` value.
    - `buildAnalyzeHeadlessArgv({ …clean projectLocation…, preScript: "../x.java" })` returns `ok: false` with a message naming the pre-script field; the same for the post-script field.
    - `buildAnalyzeHeadlessArgv` with `preScript: "Pre.java"` / `postScript: "Post.java"` still returns `ok: true` with the two flags appended in the existing documented order — the pre-existing case at `ghidra-project.test.ts:242` is unmodified and green.
    - The doc comment on `HOST_TOOL_ARG_KEYS` names a resolution mechanism for each of the four accepted keys, and the file's ghidra resolution site exists: `grep -a -c 'resolveWorkspacePath' src/mcp/vice/host-tool.mts` is at least 6 (declaration, acme source, acme outDir, acme includes, ghidra importPath, ghidra scripts).
    - `git diff --stat` for this task includes both `src/mcp/vice/resources/host-tool.mjs` and `src/mcp/vice/resources/ghidra-project.mjs`.
  </acceptance_criteria>
  <reversibility rating="reversible">Resolving two optional fields through an existing boundary check and adding an independent re-check are both local, additive changes with one in-tree caller; the corrected comment is documentation of the code as written.</reversibility>
  <done>Both script fields reach `analyzeHeadless`'s argv only as workspace-bounded absolute paths, the module's header no longer claims a check it does not perform, and the rule is enforced in two layers exactly as the dot-segment rule already is.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container-side caller → broker control socket | The untrusted principal. Every `host_tool` argument value originates here, and the caller can also write files into the shared bind-mounted workspace |
| broker control op → host child process argv | Privilege boundary: a container-side string becomes an argument to a process running on the host, outside any container |
| host child process → host filesystem outside the workspace | The boundary `resolveWorkspacePath()` exists to hold: an argument that names a directory or file outside the workspace lets a host-side tool read it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-30 | Information Disclosure | `acme.build`'s include-search-path argument (`host-tool.mts:400`) | high | mitigate | Every entry resolved through `resolveWorkspacePath()` at one site; the whole request refused on the first escape; the argv builder reads includes only from the resolved-paths parameter, so a future caller cannot reintroduce the raw array |
| T-34-31 | Elevation of Privilege | `ghidra.analyze`'s `preScript`/`postScript` reaching `analyzeHeadless`'s argv (`host-tool.mts:434-441`) | high | mitigate | Both fields resolved through `resolveWorkspacePath()` before argv; `buildAnalyzeHeadlessArgv()` independently refuses a parent-directory segment so the rule survives a caller that skipped the executor |
| T-34-32 | Tampering | A header comment asserting a boundary check the code does not perform (`host-tool.mts:96-100`) | medium | mitigate | The comment is corrected to describe the mechanism per accepted key; the acceptance criteria assert the resolution sites exist rather than trusting the prose |
| T-34-33 | Tampering | Partial-success degradation — one escaping entry dropped while the rest of the request executes | high | mitigate | The first refusal returns unchanged and the request never reaches a spawn; asserted by an empty `deps.log` spy, since the module logs exactly one line per attempted invocation |
| T-34-34 | Information Disclosure | Boundary check merging at the touching case (a sibling path whose name is a prefix of the workspace root) | medium | mitigate | `resolveWorkspacePath()`'s existing separator-anchored comparison is pinned by an adjacency case that builds exactly that sibling and asserts refusal |
| T-34-05 | Denial of Service | host-tool child process | medium | accept | Unchanged by this plan and already mitigated by the per-invocation timeout from `34-01`; the budget's own defect is `34-09`'s scope (CR-04) |
| T-34-SC | Tampering | npm / pip / cargo installs | low | accept | No package-manager install task exists in this plan; `34-RESEARCH.md` § Package Legitimacy Audit records the audit as not applicable to this phase |
</threat_model>

<verification>
- `node build.ts && node --test host-tool.test.ts ghidra-project.test.ts` — `fail 0`, no skipped cases, at least 73 passing.
- `node --test resources-sync.test.ts` — `fail 0`; both regenerated `.mjs` artifacts committed.
- `npm run typecheck` — clean.
- `node scripts/check-skill-cli-invocations.mjs` and `node scripts/check-no-skill-external-spawn.mjs` — both exit 0.
- `npm run test:automated` with no VICE broker running — failing-test count no higher than the measured 5-in-3 floor.
</verification>

<success_criteria>
- No path-bearing `acme.build` or `ghidra.analyze` wire field reaches a child process's argv without passing `resolveWorkspacePath()`, proven by a refusal case per field rather than by reading the source.
- The argv builder reads every path it emits from its resolved-paths parameter, proven by a case where the resolved parameter and the wire args disagree.
- The module's own header claim about `ghidra.analyze`'s keys is true of the code.
- The adjacency, empty and ordering edges of the include list have named cases that can fail.
- The pre-existing 31 `host-tool.test.ts` and 28 `ghidra-project.test.ts` cases stay green.
</success_criteria>

## Artifacts this phase produces

- Files modified (all git-tracked, no gitignored mirrors): `src/mcp/vice/host-tool.mts`,
  `src/mcp/vice/ghidra-project.mts`, `src/mcp/vice/host-tool.test.ts`,
  `src/mcp/vice/ghidra-project.test.ts`, `src/skills/acme-build/SKILL.md`, and the two
  generated-but-committed artifacts `src/mcp/vice/resources/host-tool.mjs` and
  `src/mcp/vice/resources/ghidra-project.mjs`
- New type fields: `ResolvedAcmeBuildPaths.includePaths`,
  `ResolvedGhidraAnalyzePaths.preScriptPath`, `ResolvedGhidraAnalyzePaths.postScriptPath`
- New behaviour in existing exported functions (no new exports): `buildHostToolArgv()` reads
  include and script paths from its resolved-paths parameter; `runHostTool()` resolves the
  include array and both script fields; `buildAnalyzeHeadlessArgv()` refuses a
  parent-directory segment in either script field
- New test case names (all in existing suites): the escaping-include refusal, the
  absolute-include refusal, the resolved-absolute-include argv assertion, the
  include-order/no-dedupe assertion, the empty/absent-include equivalence, the
  exactly-at-root and prefix-sibling adjacency pair, the END-TO-END escaping-include refusal
  over the real control-plane route, the escaping/absolute pre-and-post-script refusals, the
  resolved-parameter-precedence assertion, and the two second-layer refusals plus the
  bare-name acceptance in `ghidra-project.test.ts`
- No new files, no new modules, no `package.json` `files[]` change, no `build.ts`
  `HOST_BOUND_ARTIFACTS` change, no new environment variable
- Documented contract change: an `acme.build` include directory is workspace-relative; an
  absolute or escaping value is refused by the seam

<output>
Create `.planning/phases/34-the-host-tool-execution-seam/34-07-SUMMARY.md` when done
</output>
