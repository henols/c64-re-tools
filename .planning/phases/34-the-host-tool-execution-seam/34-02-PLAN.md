---
phase: 34-the-host-tool-execution-seam
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - src/mcp/vice/host-tool-transport.test.ts
  - .planning/phases/34-the-host-tool-execution-seam/evidence/64k-cap-probe.mjs
  - .planning/phases/34-the-host-tool-execution-seam/evidence/34-transport-cap.md
autonomous: true
requirements: [SEAM-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "An inline line larger than the control plane's 64 KiB cap produces a bare disconnect: `close` fires with `hadError=false` and the client receives zero bytes — no error frame, no partial JSON — observed against the real `startControlListener()` and recorded as a red transcript (SEAM-03)."
    - "The boundary is exact and both sides of it are observed: a line of exactly 65536 bytes is NOT destroyed, and a line of 65537 bytes IS destroyed (SEAM-03, edge: boundary — what happens exactly at each threshold and one step either side)."
    - "The cap counts UTF-8 BYTES, not characters: a line whose character count is below 65536 but whose UTF-8 byte length is above it is destroyed (SEAM-03, edge: encoding — whose definition of length applies)."
    - "A `host_tool` response carries no byte-payload field at any result size — a two-line diagnostic and a megabyte export both cross as `{ path, sha256, byteLength }` (SEAM-03)."
    - "Two concurrent `host_tool` invocations return distinct per-invocation output paths, and each response's sha256 matches its own file on disk (SEAM-03, edge: concurrency)."
    - "A zero-byte result crosses as `byteLength: 0` with the sha256 of the empty byte string, never as an omitted or null entry (SEAM-03, edge: empty)."
    - statement: "`byteLength` is an exact integer and the sha256 digest — not the length — is the result's identity; no rounding or overflow contract is asserted for a result beyond `Number.MAX_SAFE_INTEGER` bytes, which this project cannot produce"
      verification: backstop
  artifacts:
    - src/mcp/vice/host-tool-transport.test.ts
    - .planning/phases/34-the-host-tool-execution-seam/evidence/64k-cap-probe.mjs
    - .planning/phases/34-the-host-tool-execution-seam/evidence/34-transport-cap.md
  key_links:
    - "The cap under test is `broker-control.mts`'s own `MAX_LINE_BYTES` at :266 and the `socket.destroy()` at :474-480 inside `attachControlProtocol()`'s data handler — the test imports `startControlListener` from the real module and never re-derives the number."
    - "The committed probe script and the committed test assert the SAME observation; the transcript is the human-readable artifact and the test is its automated counterpart, so neither can go stale without the other going red."
  prohibitions:
    - "No inline byte payload on a host-tool response, at any result size."
    - "No host-tool output written outside the bind-mounted workspace tree."
    - "The cap constant is never copied into the test as a literal — it is read from the module under test, so a change to the module cannot leave a stale expectation passing."
---

<objective>
Make the transport contract an observation rather than a claim. The 64 KiB line cap is
observed red against the real listener — a bare disconnect with no error frame — at the
exact boundary and one step either side, and the result-by-reference shape is asserted to
hold at every size including zero.

Purpose: SEAM-03's own words are that the >64 KiB inline disconnect is "READ-IN-SOURCE, not
yet observed; observing it is a cheap red control for this phase", and ROADMAP success
criterion 2 wants it "recorded as a transcript, red". This is what turns "return paths, not
payloads" from a preference into a constraint the machine enforces.
Output: `host-tool-transport.test.ts`, a committed probe script, and a recorded transcript.
</objective>

<execution_context>
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/workflows/execute-plan.md
@/home/henrik/dev/henrik/git/c64-re-tools/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-the-host-tool-execution-seam/34-RESEARCH.md
@.planning/phases/34-the-host-tool-execution-seam/34-PATTERNS.md
@.planning/phases/34-the-host-tool-execution-seam/34-01-SUMMARY.md
</context>

Planner assumption **A-05**: the cap cases live in a NEW file
`src/mcp/vice/host-tool-transport.test.ts` rather than extending
`broker-control.test.ts`. `34-RESEARCH.md` offers both; a new file is chosen so this plan
and plan 34-01 do not both edit `broker-control.test.ts` and therefore can run in
different waves without a merge conflict. `test-gate.mjs`'s `automatedTestFiles()` is a
directory listing minus `MANUAL_ONLY_TESTS`, so a new `*.test.ts` in this directory joins
`npm run test:automated` with no registration step, and `ci-suite-coverage.test.ts` already
covers this directory, so no CI step is needed either.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The 64 KiB cap observed red at the boundary, as a committed test</name>
  <files>src/mcp/vice/host-tool-transport.test.ts</files>
  <precondition>No VICE broker daemon is running — a live broker deterministically reds the `BACK-05` case in the wider suite. `pgrep -af 'vice-broker'` returns nothing before any suite result is trusted.</precondition>
  <read_first>
    - src/mcp/vice/broker-control.mts (`MAX_LINE_BYTES` at :266; the data handler and its `socket.destroy()` at :474-480; `startControlListener()`'s signature and the shape of its required options object)
    - src/mcp/vice/broker-control.test.ts (a live-socket case, for the scratch-listener setup and teardown shape — reuse it rather than deriving a second one)
    - src/mcp/vice/host-tool.mts (as written by plan 34-01 — `runHostTool` and its response shape)
    - src/mcp/vice/host-tool.test.ts (as written by plan 34-01 — the synthetic `broker.json` scratch-directory setup and the artifact-import cast, both reused here)
    - .planning/phases/34-the-host-tool-execution-seam/34-RESEARCH.md (Finding 1 — the exact transcript this session already observed: `PROBE_LINE_BYTES=70050`, `PROBE_CLOSE hadError=false gotData=false`)
  </read_first>
  <behavior>
    - A single JSON line of 70050 bytes with NO trailing newline, sent to a real `startControlListener()`: the socket emits `close` with `hadError === false` and the client received zero bytes total.
    - A line of exactly `MAX_LINE_BYTES` bytes (read from the module, not written as a literal) with no trailing newline is NOT destroyed: the connection stays open for at least a short bounded wait and no `close` fires.
    - A line of `MAX_LINE_BYTES + 1` bytes with no trailing newline IS destroyed, with `hadError === false` and zero bytes received.
    - A line whose JavaScript `.length` is below `MAX_LINE_BYTES` but whose `Buffer.byteLength(line, "utf8")` is above it — built from multi-byte UTF-8 characters — IS destroyed. The cap is bytes, not characters.
    - The destroyed cases receive zero bytes of ANY kind: no `error` frame, no `bad_request` frame, no partial JSON.
    - A `host_tool` response object has no field whose value is a `Buffer`, a base64 payload or the tool's produced bytes: every result crosses as `{ path, sha256, byteLength }`, asserted by enumerating the response's own keys.
    - A zero-byte produced output yields `byteLength: 0` and the sha256 of the empty byte string.
    - Two `host_tool` requests issued without awaiting the first both resolve, name distinct `path` values, and each response's `sha256` equals a freshly computed digest of its own file.
  </behavior>
  <action>
Create `src/mcp/vice/host-tool-transport.test.ts` covering every case in `<behavior>`.

Read the cap from the module under test, never as a literal in this file: import
`MAX_LINE_BYTES` if plan 34-01 left it exported, and if it is not exported, derive it by
reading `broker-control.mts`'s source text and parsing the single `const MAX_LINE_BYTES = `
declaration — then assert the parsed value is a positive integer so a parse failure is loud
rather than silently zero. Either way the number appears in this file exactly zero times as
a hand-written constant. The one literal this file may carry is `70050`, and only because it
is the byte count `34-RESEARCH.md` Finding 1 actually observed and this file reproduces that
observation exactly.

For the cap cases, `startControlListener()` on a scratch port with a stub options object,
then a plain `node:net` client: `connect`, `write` the oversized line WITHOUT a trailing
newline, register `data` and `close` handlers, and assert on `hadError` plus a
bytes-received counter. Use a bounded timer (a few hundred ms) as the "connection stayed
open" observation for the at-exactly-the-cap case, and `unref()` it as
`acquireOverControlPlane()` does with its own timer, so a hung case cannot wedge the runner.
Always `destroy()` the client and `close()` the listener in a `finally`, so a failing case
cannot leave a port bound for the next one.

For the result-shape cases, reuse plan 34-01's scratch-listener + synthetic `broker.json`
setup and its artifact-import cast. Assert the NEGATIVE by enumerating keys rather than by
grepping source: walk the response object recursively and assert no value is a `Buffer`,
no value is a string longer than a stated diagnostic bound other than `stderrTail`, and the
key set of each result entry is exactly `path`, `sha256`, `byteLength`. That is a check that
fails when someone adds a payload field, which a source grep would not.

For the concurrency case, issue two requests whose typed arguments produce different output
stems, do not await the first before issuing the second, then `Promise.all` and assert the
two `path` values differ and each `sha256` matches a digest computed from disk.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && node --test host-tool-transport.test.ts</automated>
    <fails_when>non-zero exit, or the summary block reports any value other than `fail 0`, or the reported `pass` count is below 8</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && npm run typecheck</automated>
    <fails_when>non-zero exit, or any `error TS` line in the output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools/src/mcp/vice && grep -a -v '^\s*//' host-tool-transport.test.ts | grep -c '65536' </automated>
    <fails_when>the printed count is anything other than 0 — the cap must be read from the module, never written as a literal in this file</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/mcp/vice/host-tool-transport.test.ts` exists and is tracked: `git ls-files -- src/mcp/vice/host-tool-transport.test.ts` is non-empty.
    - The suite has at least 8 cases and reports `fail 0`.
    - The file contains no hand-written `65536` on a code line (comments excluded).
    - The file asserts on `hadError` at least twice: `grep -a -c 'hadError' src/mcp/vice/host-tool-transport.test.ts` is at least 2.
    - The file reproduces the researched byte count: `grep -a -c '70050' src/mcp/vice/host-tool-transport.test.ts` is at least 1.
    - The file imports the real listener rather than a stub: `grep -a -c 'startControlListener' src/mcp/vice/host-tool-transport.test.ts` is at least 1 and `grep -a -c 'from "./broker-control.mts"' src/mcp/vice/host-tool-transport.test.ts` is at least 1.
    - The result-shape case asserts the result key set exactly: the file contains a `deepEqual` over a sorted key list equal to `["byteLength","path","sha256"]`.
    - `src/mcp/vice/host-tool-transport.test.ts` is absent from `package.json`'s `files[]` and from `MANUAL_ONLY_TESTS` in `test-gate.mjs`, so it ships nowhere and runs in `test:automated`.
  </acceptance_criteria>
  <done>The cap is a passing test that fails if the cap moves, at the exact boundary and one step either side, in bytes not characters — and the result-by-reference shape is asserted by key enumeration rather than by comment.</done>
</task>

<task type="auto">
  <name>Task 2: The committed probe and its recorded red transcript</name>
  <files>.planning/phases/34-the-host-tool-execution-seam/evidence/64k-cap-probe.mjs, .planning/phases/34-the-host-tool-execution-seam/evidence/34-transport-cap.md</files>
  <read_first>
    - .planning/phases/34-the-host-tool-execution-seam/34-RESEARCH.md (Finding 1 in full — the transcript format to reproduce, including the exact marker lines `PROBE_LISTENING`, `PROBE_CONNECTED`, `PROBE_LINE_BYTES`, `PROBE_CLOSE`)
    - .planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/evidence/autostart-probe.mjs (the house shape for a committed evidence probe script: standalone, `node --check`-clean, marker lines on stdout)
    - .planning/phases/33-the-reproducible-run-protocol-and-the-capture-substrate-go-d/evidence/33-wallclock-control.md (the house shape for a recorded control transcript: a machine-greppable verdict line plus the raw command output)
    - src/mcp/vice/host-tool-transport.test.ts (as written by Task 1 — the transcript must record the same observation the test asserts)
  </read_first>
  <action>
Write `evidence/64k-cap-probe.mjs`: a standalone script that imports `startControlListener`
from `src/mcp/vice/broker-control.mts` (relative path from the evidence directory, no
copy of the framing, token or dispatch logic), binds a scratch port, dials it with a plain
`node:net` client, writes one JSON line padded to 70050 bytes with no trailing newline, and
prints the four marker lines `PROBE_LISTENING port=<n>`, `PROBE_CONNECTED`,
`PROBE_LINE_BYTES=<n>` and `PROBE_CLOSE hadError=<bool> gotData=<bool>` before exiting. Then
run the same probe twice more, once at exactly the cap and once one byte above it, printing
`PROBE_BOUNDARY_AT_CAP hadError=<bool> gotData=<bool> closed=<bool>` and
`PROBE_BOUNDARY_OVER_CAP hadError=<bool> gotData=<bool> closed=<bool>`. Keep it under
roughly a hundred lines, needing no VICE instance, no broker process and no network beyond
loopback, and exiting non-zero if any marker could not be produced.

Then run it and write `evidence/34-transport-cap.md`, containing:

  - A single machine-greppable verdict line `INLINE_OVER_CAP_CONTROL: red` or
    `INLINE_OVER_CAP_CONTROL: not-red`, and a second `BOUNDARY_CONTROL: exact` or
    `BOUNDARY_CONTROL: not-exact`. Write whichever the run actually produced. A
    `not-red`/`not-exact` result is a finding to record and escalate, never a reason to
    edit the file until it reads red.
  - The verbatim probe output, prefixed with the exact command line that produced it, in
    the `33-wallclock-control.md` style.
  - A line `TEST_COUNTERPART:` naming `src/mcp/vice/host-tool-transport.test.ts` and the
    specific case names in it that assert the same observation, so the transcript and its
    automated counterpart point at each other.
  - A line `BROKER_STATE: inactive` (or the measured state), because the wider suite's
    `BACK-05` case is deterministically red under a live broker.
  - A short paragraph stating WHY this makes SEAM-03 a contract rather than a workaround:
    Ghidra exports run to megabytes, the cap is 64 KiB, and the failure mode is a silent
    bare disconnect with no error frame — so a payload route would fail invisibly at scale,
    which is the reasoning ROADMAP criterion 2 asks to be demonstrated rather than asserted.
  </action>
  <verify>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && E=.planning/phases/34-the-host-tool-execution-seam/evidence && node --check "$E/64k-cap-probe.mjs" && node "$E/64k-cap-probe.mjs" > /tmp/probe-out.txt && grep -q 'PROBE_CLOSE hadError=false gotData=false' /tmp/probe-out.txt && grep -q 'PROBE_BOUNDARY_AT_CAP' /tmp/probe-out.txt && grep -q 'PROBE_BOUNDARY_OVER_CAP' /tmp/probe-out.txt && echo PROBE_RUNS_RED</automated>
    <fails_when>non-zero exit, or `PROBE_RUNS_RED` absent from stdout, or `node --check` reports a syntax error, or a marker line is missing from the probe output</fails_when>
    <automated>cd /home/henrik/dev/henrik/git/c64-re-tools && E=.planning/phases/34-the-host-tool-execution-seam/evidence && F="$E/34-transport-cap.md" && grep -Eq '^INLINE_OVER_CAP_CONTROL: (red|not-red)$' "$F" && grep -Eq '^BOUNDARY_CONTROL: (exact|not-exact)$' "$F" && grep -q 'TEST_COUNTERPART:' "$F" && grep -q 'BROKER_STATE:' "$F" && grep -q 'host-tool-transport.test.ts' "$F" && grep -q 'PROBE_CLOSE' "$F" && echo TRANSCRIPT_RECORDED</automated>
    <fails_when>non-zero exit, or `TRANSCRIPT_RECORDED` absent from stdout — a missing verdict line, a missing `TEST_COUNTERPART:` pointer, or a transcript with no raw probe output all fail here</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/64k-cap-probe.mjs` exists, passes `node --check`, and imports `startControlListener` from the real module: `grep -a -c 'broker-control.mts' evidence/64k-cap-probe.mjs` is at least 1.
    - The probe carries no second copy of the framing, token or dispatch logic: it contains no `MAX_LINE_BYTES` declaration of its own and no `timingSafeEqual` call.
    - `evidence/34-transport-cap.md` carries an `INLINE_OVER_CAP_CONTROL:` line whose value is exactly `red` or `not-red`, and a `BOUNDARY_CONTROL:` line whose value is exactly `exact` or `not-exact`, each recording what the run produced.
    - The transcript contains the verbatim `PROBE_CLOSE hadError=… gotData=…` line and the command that produced it.
    - The transcript's `TEST_COUNTERPART:` line names `src/mcp/vice/host-tool-transport.test.ts` and at least one case name from it.
    - The transcript states, in prose, why the megabyte-scale Ghidra export makes the file route forced rather than preferred.
  </acceptance_criteria>
  <done>The over-cap disconnect is a committed, re-runnable probe plus a recorded transcript whose verdict line was written from what the run produced, and the transcript and its automated counterpart name each other.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container-side caller → broker control socket | Untrusted wire input crosses here; this plan's whole subject is what the socket does with an oversized one |
| host filesystem output → container-side reader | Path-translation boundary; this plan asserts nothing but a reference ever crosses it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-08 | Information Disclosure | over-cap inline result on the control socket | medium | mitigate | The cap is observed red as a bare disconnect with zero response bytes, at the exact boundary and one step either side, in a committed test plus a committed probe — so a payload route cannot be adopted later under the belief that an error frame would surface it |
| T-34-13 | Denial of Service | the cap test's own scratch listener | low | mitigate | Bounded, `unref()`ed timers and `finally`-block `destroy()`/`close()` so a failing case cannot leave a bound port or a hung socket for the next case |
| T-34-10 | Tampering | result substituted between write and read | low | mitigate | Each result entry's key set is asserted to be exactly `path`, `sha256`, `byteLength`, so the digest a reader would verify against is structurally present on every result |
| T-34-SC | Tampering | npm / pip / cargo installs | low | accept | No package-manager install task exists in this plan; `34-RESEARCH.md` § Package Legitimacy Audit records the audit as not applicable |
</threat_model>

<verification>
- `node --test host-tool-transport.test.ts` — `fail 0`, at least 8 cases.
- `npm run typecheck` clean.
- `node --check evidence/64k-cap-probe.mjs` clean, and the probe run emits all five marker lines.
- `evidence/34-transport-cap.md` carries both verdict lines, the raw transcript and the `TEST_COUNTERPART:` pointer.
- `npm run test:automated` failing-test count no higher than the BEFORE baseline in `evidence/34-guard-dispositions.md`, with a stopped broker.
</verification>

<success_criteria>
- The over-cap inline return is observed as a bare disconnect with `hadError=false` and zero response bytes, recorded as a transcript, and asserted by a committed test.
- The boundary is exact: 65536 bytes survives, 65537 does not, and the count is bytes rather than characters.
- No `host_tool` response carries a byte payload at any size, asserted by key enumeration.
</success_criteria>

## Artifacts this phase produces

- Files: `src/mcp/vice/host-tool-transport.test.ts`,
  `.planning/phases/34-the-host-tool-execution-seam/evidence/64k-cap-probe.mjs`,
  `.planning/phases/34-the-host-tool-execution-seam/evidence/34-transport-cap.md`
- Probe marker names: `PROBE_LISTENING`, `PROBE_CONNECTED`, `PROBE_LINE_BYTES`,
  `PROBE_CLOSE`, `PROBE_BOUNDARY_AT_CAP`, `PROBE_BOUNDARY_OVER_CAP`
- Transcript verdict keys: `INLINE_OVER_CAP_CONTROL`, `BOUNDARY_CONTROL`,
  `TEST_COUNTERPART`, `BROKER_STATE`

<output>
Create `.planning/phases/34-the-host-tool-execution-seam/34-02-SUMMARY.md` when done
</output>