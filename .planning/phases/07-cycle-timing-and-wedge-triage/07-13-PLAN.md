---
phase: 07-cycle-timing-and-wedge-triage
plan: 13
type: execute
wave: 2
depends_on: [07-11, 07-12, 07-15]
files_modified:
  - .claude/mcp/vice/stock-live.test.ts
autonomous: true
gap_closure: true
closes_gaps: [1, 3, 4]
closes_findings: [CR-01]
requirements: [TIME-01, TIME-03, TIME-04]

must_haves:
  truths:
    - "A stock connect handshake against genuine, unmodified VICE 3.10 returns a session with a resolved cpuHistory capability -- proven live, from this tree's own stockConnect(), not from a fixture."
    - "The same handshake against genuine, unmodified VICE 3.9 still returns a session and still resolves cpuHistory to \"absent\"."
    - "A user can measure elapsed cycles over a real operation on a real VICE 3.10 build through the real dispatchStock() seam, and the answer is exact rather than zero or fabricated."
    - "With the single-client binary monitor already held by another client, vice_diagnose answers within its own bound instead of hanging."
  artifacts:
    - path: ".claude/mcp/vice/stock-live.test.ts"
      provides: "Opt-in live proofs of stockConnect() on both real binaries, the Route A stopwatch on 3.10, and bounded diagnosis under second-client contention"
      contains: "VICE_LIVE_STOCK_BIN_310"
  key_links:
    - from: ".claude/mcp/vice/stock-live.test.ts"
      to: ".claude/mcp/vice/stock-connect.ts: stockConnect()"
      via: "direct call against a real spawned emulator, no stubbed connect"
      pattern: "stockConnect\\("
    - from: ".claude/mcp/vice/stock-live.test.ts"
      to: ".claude/mcp/vice/stock-dispatch.ts: dispatchStock()"
      via: "vice_cycles_stopwatch dispatched against a real VICE 3.10 instance"
      pattern: "vice_cycles_stopwatch"
---

<objective>
Turn **Gap 1's** fix from "unit-tested" into "live-proven", and close the two live proofs
07-VALIDATION.md's Manual-Only table and 07-VERIFICATION.md's `human_verification` list leave
outstanding for the connect and contention paths.

07-VERIFICATION.md's own live reproduction is the baseline this plan must invert: against genuine
`/usr/local/bin/x64sc` (VICE 3.10), `stockConnect()` failed with
`StockFramingError | response type 0x86 body is 52 byte(s), needs at least 65`; against genuine
`/usr/bin/x64sc` (VICE 3.9) it succeeded with `{"cpuHistory":"absent"}`. After 07-11 and 07-12 both
builds must connect, and the 3.10 build must resolve the capability to `"available"` and actually
measure a bracket.

This plan exists because this phase's central failure was a **shared wrong assumption between the
synthetic fixture and the code** — the exact failure mode Phase 2's and Phase 5's post-mortems warned
about and the reason `deferred-items.md` exists. Unit tests over fixtures cannot close it. These
proofs run against both real binaries present on this machine.

Purpose: prove the four gap truths against real emulators, permanently and repeatably.
Output: three new opt-in live tests in the existing manual-only `stock-live.test.ts`.
</objective>

<source_audit>
| Source | Item | Covered by |
|--------|------|------------|
| GAP | Gap 1 — the live reproduction that must now invert on both binaries | Task 1 |
| GAP | Gap 1 — "Route A actually decodes, not just fails safely" (end-to-end, through `dispatchStock()`) | Task 2 |
| GAP | Gap 3 / Gap 4 — the bounded-diagnosis-under-contention behaviour a triage agent depends on | Task 3 |
| VALIDATION | Manual-Only row "Route A stopwatch on a >= 3.10 build" — the row blocking `nyquist_compliant: true` | Task 2 |
| VERIFICATION | `human_verification` item 2 — "a second binary-monitor client ... confirm monitor_held_elsewhere is returned within its bound rather than hanging" (the broker-mediated half is recorded honestly, not faked) | Task 3 |
| REQ | TIME-01, TIME-03 (exact vs honest measurement), TIME-04 (bounded diagnosis) | Tasks 1-3 |
| MEMORY | Launch flag order: `-default` must precede `-binarymonitor` or the monitor never binds | Tasks 1-3 |

Not in this plan: the `checkpoint_trap`/`wedged`/`restarted` live verdicts (07-17); the manifest and
tool-advertisement work (07-16); all documentation corrections (07-18).
</source_audit>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/07-cycle-timing-and-wedge-triage/07-VERIFICATION.md
@.planning/phases/07-cycle-timing-and-wedge-triage/07-VALIDATION.md
@.claude/mcp/vice/stock-live.test.ts
@.planning/phases/07-cycle-timing-and-wedge-triage/07-11-SUMMARY.md
@.planning/phases/07-cycle-timing-and-wedge-triage/07-12-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove stockConnect() completes on both real binaries, with the right capability on each</name>
  <read_first>
    - `.claude/mcp/vice/stock-live.test.ts` — the whole file. Note specifically: the `SKIP_REASON` gate and why every test must pass it through node:test's `{ skip }` option rather than early-returning; the `freeEphemeralPort()` and `connectWithRetry()` helpers; the `before()`/`after()` pair that owns ONE emulator for the file and stubs `deps.connect` with a hand-built session whose `capabilities.cpuHistory` is hardcoded `"absent"`; the `XDG_CONFIG_HOME` scratch-dir mitigation for the shared-`vicerc` version dialog; the 127.0.0.1-only binding rule (T-03-16-01); and the file's registration in `test-gate.mjs`'s `MANUAL_ONLY_TESTS`.
    - `.claude/mcp/vice/stock-connect.ts` — `stockConnect()`'s signature, its `StockConnectDeps` shape (`binPath`, `supervisorDir`, `epochPath`, the injectable capability-record and epoch functions), and its seven-step body, so the live call supplies real coordinates and a stub `brokerControl` exactly the way 07-VERIFICATION.md's own probe did.
    - `.planning/phases/07-cycle-timing-and-wedge-triage/07-11-SUMMARY.md` and `07-12-SUMMARY.md` — what the two upstream plans actually landed, including the observed `viceVersion` quads.
    - `.planning/phases/07-cycle-timing-and-wedge-triage/07-VALIDATION.md` — the Manual-Only preamble naming both binary paths and the flag-order gotcha.
  </read_first>
  <files>.claude/mcp/vice/stock-live.test.ts</files>
  <action>
Add a second-instance helper and two live connect proofs. Do **not** change the existing
`before()`/`after()` pair or its spawn arguments — the phase-3 and phase-5 tests in this file depend
on that fixture exactly as it is.

**(a) The helper.** Add `withOwnStockInstance(binPath, fn)`: allocates a port with the existing
`freeEphemeralPort()`, `mkdtempSync()`s its own `XDG_CONFIG_HOME` scratch dir, spawns
`binPath` with argv `["-default", "-binarymonitor", "-binarymonitoraddress", "ip4://127.0.0.1:<port>"]`
— `-default` **first**, because the monitor never binds otherwise, and the existing `before()`'s
argv (which omits it) must not be taken as the pattern here — awaits readiness with the same bounded
retry idiom `connectWithRetry()` uses, invokes `fn({ port, binPath })`, and then in a `finally`
disconnects anything it opened, `SIGKILL`s the child, waits bounded for `exit`, and `rmSync`s the
scratch dir. Teardown must run even when `fn` throws: that is the leaked-resource failure mode this
file's header already names.

**(b) Two resolved-binary constants.** Add `VICE_LIVE_STOCK_BIN_39` (default `/usr/bin/x64sc`,
genuine unpatched stock VICE 3.9) and `VICE_LIVE_STOCK_BIN_310` (default `/usr/local/bin/x64sc`,
genuine VICE 3.10), each overridable by an env var of the same name, each with its own
`existsSync()` skip reason in the same voice as the existing `SKIP_REASON`. A test whose binary is
absent must SKIP, never fail and never silently pass.

**(c) The 3.9 proof.** Inside `withOwnStockInstance(VICE_LIVE_STOCK_BIN_39, ...)`, call the real
`stockConnect()` — a real `ViceMonitorClient`, the live host/port, a stub `brokerControl` whose
`claimMonitor`/`releaseMonitor` resolve `{ ok: true }`, `deps: {}` so no capability cache is
consulted or written. Assert: the promise **resolves**; the returned session has a `client`;
`session.capabilities.cpuHistory === "absent"`; `session.versionQuad` starts with `3.9`. Then send
one `CommandType.Ping` through the returned session to prove the session is actually usable, not
just constructed.

**(d) The 3.10 proof — the inversion of the verification's live failure.** Same shape against
`VICE_LIVE_STOCK_BIN_310`. Assert: the promise **resolves** (the whole point — it rejected before
07-11); `session.versionQuad` starts with `3.10`; and
`session.capabilities.cpuHistory === "available"`. Name CR-01 and quote the previously-observed
failure string `response type 0x86 body is 52 byte(s), needs at least 65` in the test title or a
comment, so the regression is recognisable by grep.
If the capability resolves to `"absent"` instead of `"available"`, that means 07-12's parser still
cannot decode the real reply: let the test FAIL, capture the actual bytes and the actual error, and
report it as a finding. Do not relax the assertion to `"absent"` to make the test pass — that is
exactly the accommodation that produced this gap.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && VICE_LIVE_STOCK_BIN=/usr/bin/x64sc VICE_LIVE_STOCK_BIN_39=/usr/bin/x64sc VICE_LIVE_STOCK_BIN_310=/usr/local/bin/x64sc node --test stock-live.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'VICE_LIVE_STOCK_BIN_310' .claude/mcp/vice/stock-live.test.ts` is >= 2.
    - `grep -c '"-default"' .claude/mcp/vice/stock-live.test.ts` is >= 1 and appears before `"-binarymonitor"` in the same argv array.
    - `grep -c 'stockConnect(' .claude/mcp/vice/stock-live.test.ts` is >= 2.
    - Behavior (live, 3.9): the test asserting `cpuHistory === "absent"` and a `3.9` version quad passes against `/usr/bin/x64sc`.
    - Behavior (live, 3.10): the test asserting `stockConnect()` resolves with `cpuHistory === "available"` and a `3.10` version quad passes against `/usr/local/bin/x64sc`.
    - Default-skip preserved: `node --test stock-live.test.ts` with no `VICE_LIVE_*` env vars set reports SKIPs, not failures.
    - `pgrep -f x64sc` returns nothing after the run.
  </acceptance_criteria>
  <done>Both real binaries connect through this tree's own `stockConnect()`, each resolving the capability its build actually supports, with a helper that cannot leak an emulator process.</done>
</task>

<task type="auto">
  <name>Task 2: Measure a real bracket on genuine VICE 3.10 through the real dispatchStock() seam</name>
  <read_first>
    - `.claude/mcp/vice/stock-live.test.ts` — the version from Task 1, and the existing tests' pattern of dispatching through `dispatchStock()` with `liveDeps()` and reading the payload via `parseOkPayload()`.
    - `.claude/mcp/vice/stock-timing.ts` — `handleCyclesStopwatch()`'s three actions (`reset`, `read`, `reset_and_read`), `readCycleBaseline()`'s route selection off `session.capabilities.cpuHistory`, the Route A `delta < 0n` guard, and the `stopwatchBaselines` map keyed on `session.targetId`.
    - `.claude/mcp/vice/stock-dispatch.ts` — `dispatchStock()`'s signature and `StockDispatchDeps`, so the 3.10 instance is reachable through the same seam `vice-proxy.ts` uses (the test must not call the handler directly).
    - `.claude/mcp/vice/stock-execution.ts` — the `vice_execution_run` handler, since the bracket needs a real resume between reset and read.
    - `.planning/phases/07-cycle-timing-and-wedge-triage/07-VALIDATION.md` — the Manual-Only row "Route A stopwatch on a >= 3.10 build", which this task converts from OUTSTANDING to a recorded PASS.
  </read_first>
  <files>.claude/mcp/vice/stock-live.test.ts</files>
  <action>
Add a live Route A stopwatch proof against `VICE_LIVE_STOCK_BIN_310`, dispatched through the real
`dispatchStock()` seam with a `StockDispatchDeps` whose `connect` returns the **real**
`stockConnect()` session from Task 1's helper — not the file's hardcoded
`capabilities: { cpuHistory: "absent" }` stub session, which would silently select Route B and prove
nothing.

The bracket must have zero socket traffic during the wait, per 07-CONTEXT.md's own rule that
`runCycleBracket()`'s ping-polling must not be ported: every inbound byte halts the machine on stock.
Sequence:
  1. `vice_cycles_stopwatch { action: "reset" }` -> assert `route === "cpu_history"` (this is the
     assertion that proves Route A was actually selected, and it can only hold if 07-12's parser
     decodes a real reply).
  2. `vice_execution_run` -> one resume.
  3. A real wall-clock wait with **no calls at all** — 500ms is enough and keeps the test fast.
  4. `vice_cycles_stopwatch { action: "read" }` -> assert `route === "cpu_history"`,
     `measurable === true`, `exactness === "exact"`, `cyclesExact` is a decimal string parsing to a
     positive BigInt, and `cycles` is a positive number.
  5. Sanity-bound the figure rather than asserting an exact value: a PAL C64 runs ~985,000 cycles/s,
     so assert the measured cycles fall inside a generous band (at least ~100,000 and at most
     ~5,000,000 for a 500ms wait) and print the observed figure. State in a comment that the band
     exists to catch a fabricated or wrongly-scaled number, not to pin timing.
  6. Assert `cycles` is never `0` and the payload carries no `cycles` key when `measurable` is false
     — the TIME-03 honesty rule, asserted positively here.
Additionally assert the anti-fabrication guard end-to-end: call `action: "read"` a second time
immediately (no resume, no wait) and assert the answer is either a small non-negative figure or an
explicit `measurable: false` with a `reason` — never a negative number and never a fabricated
large one.
Record the observed cycle figures in the SUMMARY.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && VICE_LIVE_STOCK_BIN=/usr/bin/x64sc VICE_LIVE_STOCK_BIN_310=/usr/local/bin/x64sc node --test stock-live.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'vice_cycles_stopwatch' .claude/mcp/vice/stock-live.test.ts` is >= 3.
    - Behavior (live, 3.10): `route === "cpu_history"` on both the reset and the read.
    - Behavior (live, 3.10): `measurable === true`, `exactness === "exact"`, `BigInt(cyclesExact) > 0n`, and the measured `cycles` fall inside the documented sanity band.
    - Behavior (live, 3.10): no assertion in the test tolerates `cycles === 0`.
    - Source assertion: the test does not reuse the file's `before()` stub session — `grep -c 'cpuHistory: "absent"' .claude/mcp/vice/stock-live.test.ts` remains 1 (the existing `before()` occurrence only).
    - `pgrep -f x64sc` returns nothing after the run.
  </acceptance_criteria>
  <done>Route A is proven live: a real bracket over a real VICE 3.10 instance yields an exact, plausible, non-zero cycle count through the same dispatch seam the proxy uses.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the diagnostician stays bounded when a second client holds the monitor</name>
  <read_first>
    - `.claude/mcp/vice/stock-live.test.ts` — the version from Tasks 1 and 2, and its raw `net` import (already present for `freeEphemeralPort()`), which is what the second client should use.
    - `.claude/mcp/vice/stock-diagnose.ts` — `handleDiagnoseStock()`'s `Promise.race` against `diagnoseSessionTimeoutMs()` (~lines 566-604), and the `diagnosis_unavailable` outcome plan 07-15 added, including its exact message prefix and the `monitor_acquisition_timeout` reason class.
    - `.planning/phases/07-cycle-timing-and-wedge-triage/07-15-SUMMARY.md` — the exact prefix format and reason-class names to assert on.
    - `CLAUDE.md` — the Concurrency constraint: stock VICE's binary monitor services exactly one client, and a second `connect()` sits unserviced with no reply and no EOF, indistinguishable from a wedge. This is why the bound is the behaviour under test.
    - `.planning/phases/07-cycle-timing-and-wedge-triage/07-VERIFICATION.md` — `human_verification` item 2, for what this task does and does not close.
  </read_first>
  <files>.claude/mcp/vice/stock-live.test.ts</files>
  <action>
Add a live contention proof against `VICE_LIVE_STOCK_BIN_39` (3.9 is deliberate: it isolates the
contention behaviour from anything version-gated, matching what 07-VERIFICATION.md's
`human_verification` item asks for).

Setup inside `withOwnStockInstance`: open a **first** raw `net` socket to the instance's monitor port
and keep it open and silent — that socket is the "other client" holding the single-client monitor.
Then set `VICE_STOCK_DIAGNOSE_SESSION_TIMEOUT_MS` to a small value (1500) for the duration of the
test and restore it afterwards, so the test is fast and the bound is unambiguous.

Dispatch `vice_diagnose` through `dispatchStock()` with a `deps.connect` that performs a **real**
second connect against the same port (this is the connect that will sit unserviced), and measure
elapsed wall-clock time around the call.

Assert:
  - the call **settles** — it must not hang; if it does not settle, the test must fail on its own
    node:test timeout rather than being wrapped in a hand-rolled forever-wait;
  - elapsed time is less than roughly 3x the configured bound (assert `< 5000` ms for a 1500ms
    bound) — the property under test is boundedness, not precision;
  - the answer is either the `monitor_held_elsewhere` verdict (if the broker-level ownership error is
    what surfaced) **or** `isError: true` with text matching
    `/^vice_diagnose: diagnosis_unavailable \(monitor_acquisition_timeout\)/`. Accept both, and
    assert on the union — which of the two occurs depends on whether the contention is detected at
    the broker claim or at the socket, and both are correct, documented outcomes. Print which one
    occurred.
  - the answer is **not** the `live` verdict, and not a raw protocol string.
Close the holding socket in a `finally`.

State explicitly in a comment, and repeat it in the SUMMARY, what this test does NOT prove: the
**broker-mediated** `monitor_held_elsewhere` path via a real `claimMonitor()` from a second
broker-managed session still requires the host broker control plane running two real sessions, which
this file's dispatch-level harness does not stand up. That half stays recorded as unit-proven only —
do not let this test's title or the docs imply otherwise.
  </action>
  <verify>
    <automated>cd .claude/mcp/vice && VICE_LIVE_STOCK_BIN=/usr/bin/x64sc VICE_LIVE_STOCK_BIN_39=/usr/bin/x64sc VICE_LIVE_STOCK_BIN_310=/usr/local/bin/x64sc node --test stock-live.test.ts && node test-gate.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'VICE_STOCK_DIAGNOSE_SESSION_TIMEOUT_MS' .claude/mcp/vice/stock-live.test.ts` is >= 2 (set and restore).
    - `grep -c 'monitor_acquisition_timeout' .claude/mcp/vice/stock-live.test.ts` is >= 1.
    - Behavior (live): the diagnose call settles in under 5000ms with a 1500ms bound, and the observed outcome is printed.
    - Behavior (live): the answer is not the `live` verdict.
    - The test's comment and the SUMMARY both state that the broker-mediated `monitor_held_elsewhere` path remains unit-proven only.
    - `node test-gate.mjs` still excludes `stock-live.test.ts` (it stays manual-only) and shows no new failures beyond the documented pre-existing worktree-path failure.
    - `pgrep -f x64sc` returns nothing after the run; no leaked holding socket.
  </acceptance_criteria>
  <done>A real second client on the single-client monitor makes `vice_diagnose` answer within its bound, with the outcome recorded and the remaining unit-only half stated honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spawned emulator's binary monitor port -> test process | The binary monitor is unauthenticated full machine control; binding scope is a real security property |
| `VICE_LIVE_STOCK_BIN*` env vars -> `spawn()` | Operator-supplied executable paths, passed as argv (never through a shell) |
| shared `~/.config/vice/vicerc` -> spawned emulator | Two builds share it; touching it would corrupt the operator's real config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13-01 | Elevation of Privilege | monitor socket binding | mitigate | Every spawn binds `ip4://127.0.0.1:<port>` only, never `0.0.0.0` — the same rule this file's header already states as T-03-16-01. |
| T-07-13-02 | Denial of Service | leaked emulator processes / leaked sockets | mitigate | `withOwnStockInstance()` tears down in a `finally` (disconnect, SIGKILL, bounded exit wait, scratch-dir removal); the holding socket in Task 3 is closed in a `finally`; `pgrep -f x64sc` is an acceptance criterion. |
| T-07-13-03 | Tampering | operator's shared `vicerc` | mitigate | Each instance gets its own `mkdtempSync()` `XDG_CONFIG_HOME`; the real config is never read or written. |
| T-07-13-04 | Spoofing | testing the wrong binary | mitigate | Both binaries are named by absolute path and asserted via `existsSync()` plus a `versionQuad` prefix assertion (`3.9` / `3.10`), so a `PATH` shadow (the fork build shadows `x64sc`) cannot silently substitute. |
| T-07-13-05 | Repudiation | a failing live assertion being relaxed to pass | mitigate | Task 1 forbids relaxing the `"available"` assertion to `"absent"`; Task 2 forbids tolerating `cycles === 0`; Task 3 forbids a hand-rolled forever-wait. A failure is a finding to report. |
| T-07-13-06 | Elevation of Privilege | resource-set surface | mitigate | These tests send only reads plus `vice_execution_run`; no `RESOURCE_SET` path is exercised, so CLAUDE.md's three power-cycling resources stay unreachable. |
| T-07-13-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; no new dependency permitted. |
</threat_model>

<verification>
- `cd .claude/mcp/vice && VICE_LIVE_STOCK_BIN=/usr/bin/x64sc VICE_LIVE_STOCK_BIN_39=/usr/bin/x64sc VICE_LIVE_STOCK_BIN_310=/usr/local/bin/x64sc node --test stock-live.test.ts` passes with no SKIPs for the three new tests.
- `cd .claude/mcp/vice && node --test stock-live.test.ts` (no env vars) reports SKIPs, no failures.
- `cd .claude/mcp/vice && npx tsc --noEmit -p tsconfig.json` exits 0.
- `cd .claude/mcp/vice && node test-gate.mjs` shows only the documented pre-existing worktree-path failure and still excludes `stock-live.test.ts`.
- `pgrep -f x64sc` returns nothing.
</verification>

<success_criteria>
- `stockConnect()` resolves against both real binaries, with `"absent"` on 3.9 and `"available"` on 3.10.
- A real 500ms bracket on 3.10 yields an exact, non-zero, plausible cycle count via `route: "cpu_history"`.
- `vice_diagnose` under real second-client contention settles well inside its bound with a documented outcome.
- The remaining unit-only half of the contention proof is stated, not glossed.
</success_criteria>

<output>
Create `.planning/phases/07-cycle-timing-and-wedge-triage/07-13-SUMMARY.md` when done. Record: both
observed `versionQuad` values and capabilities, the measured cycle figures and the sanity band used,
which of the two accepted contention outcomes occurred and the measured elapsed time, and the exact
statement of what remains unit-proven only (input for 07-18's `07-VALIDATION.md` and `SKILL.md`
corrections).
</output>
