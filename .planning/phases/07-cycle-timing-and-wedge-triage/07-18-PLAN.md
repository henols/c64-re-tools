---
phase: 07-cycle-timing-and-wedge-triage
plan: 18
type: execute
wave: 3
depends_on: [07-11, 07-12, 07-13, 07-14, 07-15, 07-16, 07-17]
files_modified:
  - docs/stock-vice-parity.md
  - .claude/skills/vice-wedge-triage/SKILL.md
  - .claude/skills/c64-program-recon/references/tool-selection.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/07-cycle-timing-and-wedge-triage/07-VALIDATION.md
  - .planning/phases/07-cycle-timing-and-wedge-triage/deferred-items.md
autonomous: true
gap_closure: true
closes_gaps: [1, 2, 3, 4]
closes_findings: [CR-01, WR-13]
requirements: [TIME-01, TIME-02, TIME-03, TIME-04]

must_haves:
  truths:
    - "No document in this repository claims a live confirmation that this phase's own verification disproved."
    - "Every remaining unproven behaviour is named as unproven, with what it would take to prove it."
    - "vice-wedge-triage's opening move has a documented, actionable response for every outcome vice_diagnose can produce on stock, including the non-verdict one."
    - "REQUIREMENTS.md's TIME-01..TIME-04 markings match the evidence the gap-closure SUMMARYs actually record."
  artifacts:
    - path: "docs/stock-vice-parity.md"
      provides: "Corrected §A capability-probe record, the re-derived CPUHISTORY_GET layout, and the diagnosis_unavailable behaviour"
      contains: "diagnosis_unavailable"
    - path: ".claude/skills/vice-wedge-triage/SKILL.md"
      provides: "A triage response for diagnosis_unavailable, corrected provenance grades, and the machineHalted/reached-unknown behaviours"
      contains: "diagnosis_unavailable"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TIME-01..TIME-04 status lines that match the recorded evidence"
    - path: ".planning/phases/07-cycle-timing-and-wedge-triage/07-VALIDATION.md"
      provides: "Manual-Only rows updated with the live results this batch produced, and an evidence-backed nyquist_compliant value"
  key_links:
    - from: ".claude/skills/vice-wedge-triage/SKILL.md"
      to: ".claude/mcp/vice/stock-diagnose.ts: diagnosis_unavailable"
      via: "a triage-table row naming the outcome and its correct response"
      pattern: "diagnosis_unavailable"
---

<objective>
Close **Gap 4** and the documentation-integrity halves of Gaps 1-3: bring every claim in this
repository into line with what the gap-closure batch actually proved, and give
`vice-wedge-triage`'s opening move an actionable response for every outcome — including the
non-verdict one.

Three specific falsehoods are on record and must go:
- `docs/stock-vice-parity.md:342-352` claims the CPU-history capability *"now actually resolves to
  `\"available\"` where the build supports it, live-confirmed against the fork's own genuine VICE
  3.10.0.0 build."* 07-VERIFICATION.md graded this a **documentation-integrity BLOCKER**: at the time
  of writing, the connect failed before any capability value existed.
- `.claude/skills/vice-wedge-triage/SKILL.md` lines ~90 and ~143 claim the live/reached/timeout
  behaviours were *"live-confirmed against genuine stock VICE 3.9 and VICE 3.10"* — for behaviours
  whose connect prerequisite was proven broken on 3.10.
- `.planning/REQUIREMENTS.md` marks all four `TIME-*` rows **Complete**, which 07-VERIFICATION.md
  states plainly are "not justified by the current codebase state".

This plan runs **last** because every correction must be sourced from what plans 07-11 through 07-17
actually recorded — not from what they intended. Where a proof came back outstanding (07-17 permits a
recorded skip for the `wedged` induction; 07-13 records the broker-mediated
`monitor_held_elsewhere` half as unit-only), that outcome is written down as outstanding. Writing a
green claim over an outstanding row is the exact failure this whole gap-closure batch exists to
correct.

Purpose: leave the documentation trustworthy enough that the next reader does not have to re-derive
the truth from a live emulator.
Output: six corrected documents, each claim traceable to a named SUMMARY.
</objective>

<source_audit>
| Source | Item | Covered by |
|--------|------|------------|
| GAP | Gap 1 — "docs/stock-vice-parity.md:349-352's false live-confirmed claim must be corrected or removed" | Task 1 |
| GAP | Gap 1 — "REQUIREMENTS.md's TIME-01 'Complete' marking must be reverted" | Task 3 |
| GAP | Gap 4 — "SKILL.md lines 90, 143 claim live confirmation ... which this verification contradicts" | Task 2 |
| GAP | Gap 4 — "make sure the skill's opening move yields a triage-actionable answer on a >= 3.10 build" | Task 2 |
| GAP | Gap 2 / Gap 3 — the new `machineHalted`, reached-unknown and `machinePausedSource` behaviours must reach the skill and the parity doc | Tasks 1, 2 |
| VERIFICATION | "all four 'Complete' markings in REQUIREMENTS.md are not justified by the current codebase state" | Task 3 |
| VALIDATION | The Manual-Only table's one OUTSTANDING and two PARTIAL rows, and `nyquist_compliant: false` with its named blocker | Task 3 |
| DEFERRED | `deferred-items.md`'s 07-10 entry, whose recommended follow-up plan this batch is | Task 3 |
| CONTEXT | D-03 — the missing-verdict divergence must be recorded in `docs/stock-vice-parity.md`; `diagnosis_unavailable` is an outcome, not a sixth verdict, and must be documented as such | Tasks 1, 2 |
| CONTEXT | Deferred Ideas — Phase 8's `SKILL-01` sweep across ALL skills stays out of scope; only claims this phase itself falsified are corrected | Task 2 |
| REQ | TIME-01, TIME-02, TIME-03, TIME-04 | Tasks 1-3 |

Bounded exception to the "vice-wedge-triage only" scope fence: `.claude/skills/c64-program-recon/references/tool-selection.md:38`
asserts "**`vice_run_until` has no working timeout**", which Phase 7's own D-02 falsified on stock.
That is a claim this phase invalidated, not a Phase-8 sweep item, so one backend-qualified correction
is in scope. Nothing else in that skill is touched.
</source_audit>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cycle-timing-and-wedge-triage/07-VERIFICATION.md
@.planning/phases/07-cycle-timing-and-wedge-triage/07-CONTEXT.md
@docs/stock-vice-parity.md
@.claude/skills/vice-wedge-triage/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct docs/stock-vice-parity.md's false claim and record what is now actually established</name>
  <read_first>
    - `docs/stock-vice-parity.md` — §A in full, especially the `vice_diagnose` D-03 missing-verdict entry, the `vice_recycle` no-screenshot entry (D-01), and the "stock correctness gain (Phase 7, `probeCpuHistory()`'s `count=0` fix)" paragraph at lines 342-352 that carries the false claim.
    - `.planning/phases/07-cycle-timing-and-wedge-triage/07-VERIFICATION.md` — gap 1's `docs/stock-vice-parity.md` artifact row, which grades this a documentation-integrity blocker.
    - `.planning/phases/07-cycle-timing-and-wedge-triage/07-11-SUMMARY.md`, `07-12-SUMMARY.md`, `07-13-SUMMARY.md` — the actual outcomes: the error-class-to-capability mapping, the re-derived layout, and the live results on both binaries with their observed version quads and cycle figures.
    - `.planning/phases/07-cycle-timing-and-wedge-triage/07-14-SUMMARY.md`, `07-15-SUMMARY.md`, `07-16-SUMMARY.md` — the new answer fields and the advertised-schema fix.
    - `.planning/phases/07-cycle-timing-and-wedge-triage/07-CONTEXT.md` — D-01 and D-03, which this document is the designated home for.
  </read_first>
  <files>docs/stock-vice-parity.md</files>
  <action>
Rewrite the §A "stock correctness gain (Phase 7, `probeCpuHistory()`'s `count=0` fix)" paragraph
(currently lines 342-352) and extend §A with what this batch established. Every claim must be
attributable to a named plan SUMMARY, and every "live-confirmed" phrase must name the binary, its
version quad, and the plan that ran it — a bare "live-confirmed" is what produced this defect.

Required content:
  1. **The corrected history**, in order and without hiding the regression: `count=0` was malformed
     and real VICE rejects it with `InvalidParameter` (0x81); 07-01 fixed that by probing with
     `count=1`; that fix exposed a worse failure on builds which *support* the opcode — the real
     reply could not be decoded, and the resulting `StockFramingError` was not classified, so the
     **entire stock handshake failed on any genuine VICE >= 3.10** (CR-01, live-reproduced twice
     independently). Say plainly that the previous version of this paragraph claimed the opposite and
     was wrong.
  2. **The two fixes**, cited to their plans: a decode failure is now a capability answer and can
     never fail the handshake (07-11), and the per-entry layout was re-derived from
     `monitor_binary.c`'s `monitor_binary_process_cpuhistory()` plus committed real captures (07-12).
     Record the actual layout compactly — `count(u32LE)`, then per entry a size byte whose value is
     the byte count of everything after it, a `write_registers()` block, `cycle(u64LE)`, a constant
     `instruction_length` byte, and that many instruction data bytes — and note that
     `instruction_length` is a hardcoded 4 in VICE and is not a decoded instruction size.
  3. **The live status**, taken verbatim from 07-13's SUMMARY: the observed capability on each binary,
     the observed version quads, and the measured cycle figures. If 07-13 recorded anything short of a
     pass, say so here rather than rounding up.
  4. **A new §A entry for the `diagnosis_unavailable` outcome** (07-15): stock's `vice_diagnose` can
     answer a classified non-verdict outcome when no verdict could be established. State that this is
     **not** a sixth verdict — D-03's set is still exactly five — that it means the machine's state is
     unknown, and that it is never on its own grounds to recycle. List the reason classes.
  5. **A new §A entry for `vice_run_until`'s stock answer shape** (07-14): a timed-out call leaves the
     machine halted and says so via `machineHalted`, naming `vice_execution_run` as the resume; a
     cleanup race is resolved from the program counter or reported unresolved rather than asserted as
     not-reached. Note that `reached` is therefore no longer unconditionally present on stock.
  6. **A note that the advertised stock schema now reaches `tools/list`** (07-16, WR-07) — previously
     the fork's synthetic definitions shadowed the stock manifest entries, so an agent reading the
     schema on stock saw the fork's verdict vocabulary including `stale_read_path`.
Leave §B's CPU-history-tracing entry as it is: tracing (`GAIN-*`) is still unbuilt and still cut.
  </action>
  <verify>
    <automated>grep -c 'live-confirmed against the fork' docs/stock-vice-parity.md; grep -c 'diagnosis_unavailable' docs/stock-vice-parity.md; grep -c 'machineHalted' docs/stock-vice-parity.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'so the capability now actually resolves to' docs/stock-vice-parity.md` is 0 — the false sentence is gone.
    - `grep -c 'diagnosis_unavailable' docs/stock-vice-parity.md` is >= 1, and the text states it is not a sixth verdict.
    - `grep -c 'machineHalted' docs/stock-vice-parity.md` is >= 1.
    - `grep -c 'stale_read_path' docs/stock-vice-parity.md` is >= 1 (D-03's divergence record is preserved, not deleted along with the correction).
    - Every occurrence of "live-confirmed" in the sections this task touched is followed within the same sentence by a binary path and a version quad — verify by reading each occurrence and listing them in the SUMMARY.
    - Every new claim cites a plan number (`07-11` .. `07-17`).
  </acceptance_criteria>
  <done>The parity doc's capability-probe history is accurate, the re-derived layout is recorded, and the three new stock behaviours are documented with attributable evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Give the skill an actionable response for every stock outcome, and correct its provenance grades</name>
  <read_first>
    - `.claude/skills/vice-wedge-triage/SKILL.md` — the whole file: the backend note at lines 20-30 naming both verdict sets, "The order" (lines 32-50), the verdict-response table, the `vice_run_until` paragraph at lines ~82-92 with its "**Confidence: HIGH on stock** — live-confirmed" claim, the manual-fallback section, and the Provenance table at lines ~130-150 including the row that already honestly says `checkpoint_trap`, `wedged`, `restarted` and `monitor_held_elsewhere` were "not yet exercised against a real emulator".
    - `.claude/skills/c64-program-recon/references/tool-selection.md` — line 38's "**`vice_run_until` has no working timeout**" claim and line 22's "five-state verdict" row.
    - `.planning/phases/07-cycle-timing-and-wedge-triage/07-15-SUMMARY.md` — the `diagnosis_unavailable` reason classes and the exact message prefix an agent will see.
    - `.planning/phases/07-cycle-timing-and-wedge-triage/07-13-SUMMARY.md` and `07-17-SUMMARY.md` — which live proofs actually landed, which were skipped and why, and both stated honest limits (the test-performed rather than broker-supervised respawn; the unit-only broker-mediated `monitor_held_elsewhere`).
    - `.planning/phases/07-cycle-timing-and-wedge-triage/07-14-SUMMARY.md` — the `machineHalted` / reached-unknown behaviour an agent must now expect.
    - `.planning/phases/07-cycle-timing-and-wedge-triage/07-CONTEXT.md` — the Deferred Ideas fence: Phase 8 owns the `SKILL-01` sweep; this skill is touched only as far as criterion 4 requires.
  </read_first>
  <files>.claude/skills/vice-wedge-triage/SKILL.md, .claude/skills/c64-program-recon/references/tool-selection.md</files>
  <action>
**(a) `vice-wedge-triage/SKILL.md`.**
  1. **Add a `diagnosis_unavailable` row to the verdict-response table**, marked clearly as a
     non-verdict outcome rather than a sixth verdict. The response it prescribes: the machine's state
     is unknown, so do **not** recycle on this answer alone; read the reason class in the message
     (which begins `vice_diagnose: diagnosis_unavailable (<reason>)`) and act on it — retry once for
     `connection_lost`/`request_timeout`, wait for the other client for
     `monitor_acquisition_timeout`, and for `protocol_decode_failure` report it as a tool defect
     (this build answered a frame the client cannot decode) and fall back to the manual cycle
     bracket. List all reason classes 07-15 shipped.
  2. **Correct the `vice_run_until` paragraph's confidence claim** (line ~90) so it states exactly
     what 07-13 and the phase's earlier live pass proved, naming the binaries and version quads, and
     add the two new behaviours: a timed-out call leaves the machine halted and reports
     `machineHalted` with `vice_execution_run` as the resume, and a cleanup race is resolved from the
     program counter or reported unresolved — so `reached` may be absent, and an absent `reached` is
     not "false".
  3. **Correct the Provenance table's stock row** (line ~143) so each behaviour carries the grade the
     recorded evidence supports: which verdicts are now live-proven (from 07-17), which remain
     unit-proven only, that the respawn behind the `restarted` proof was test-performed rather than
     broker-supervised, and that the broker-mediated `monitor_held_elsewhere` path is still unit-only
     (07-13). If 07-17 recorded a skip for `wedged`, that row must say so. Keep the file's existing
     `Evidence:` / `Confidence:` grading vocabulary; promote a grade only where a SUMMARY records the
     evidence.
  4. **Update the backend note** (lines 20-30) to state that the advertised schema an agent reads on
     stock is now the stock manifest's own (07-16), so the instruction "read the tool's own schema for
     the exact contract on whichever backend is active" is finally sound — it was not, and say so.
  5. Add `machinePaused` / `machinePausedSource` to whatever part of the file tells the reader how to
     decide whether to resume: `observed` means the run state was seen, `structural` means it was
     inferred from the fact that every stock read halts the machine, `no_session` means no claim is
     being made.
  Do **not** expand into Phase 8's `SKILL-01` sweep: no new sections about install docs, the
  manifest-derived support table, or `BACK-05`'s runtime capability error.

**(b) `c64-program-recon/references/tool-selection.md` — one bounded correction.** Line 38's
"**`vice_run_until` has no working timeout.**" is false on stock as of Phase 7's D-02. Rewrite it
backend-qualified: on the fork it still has no bound and an unreachable address is unbounded and
indistinguishable from a wedge; on stock, `timeout_ms` (default 30000, ceiling 600000) bounds the wait
and a timed-out answer says the machine is left halted. Point at `vice-wedge-triage` for the triage
judgement rather than restating it. Change nothing else in this file — line 22's "five-state verdict"
row may gain a parenthetical noting the two backends' verdict sets differ, and that is the only other
permitted edit.
  </action>
  <verify>
    <automated>grep -c 'diagnosis_unavailable' .claude/skills/vice-wedge-triage/SKILL.md && grep -c 'machineHalted' .claude/skills/vice-wedge-triage/SKILL.md && grep -c 'has no working timeout' .claude/skills/c64-program-recon/references/tool-selection.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'diagnosis_unavailable' .claude/skills/vice-wedge-triage/SKILL.md` is >= 2 (the table row plus the reason-class guidance).
    - `grep -c 'machineHalted' .claude/skills/vice-wedge-triage/SKILL.md` is >= 1 and `grep -c 'machinePausedSource' ...` is >= 1.
    - `grep -c 'stale_read_path' .claude/skills/vice-wedge-triage/SKILL.md` is >= 1 — the fork/stock verdict-set contrast is preserved.
    - Source assertion: the skill's verdict-response table still lists exactly D-03's five verdicts as verdicts, with `diagnosis_unavailable` marked as a non-verdict outcome.
    - `grep -c 'has no working timeout' .claude/skills/c64-program-recon/references/tool-selection.md` is 0 for the unqualified claim; the replacement text names both backends.
    - Every "live-confirmed" / "Confidence: HIGH" claim about stock in the edited sections names the binary and version quad and cites the plan that produced the evidence — list them in the SUMMARY.
    - `git diff --stat .claude/skills/` shows changes to exactly two files.
  </acceptance_criteria>
  <done>An agent following the skill's opening move has a correct response for every outcome including the non-verdict one, and no confidence grade exceeds its recorded evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Re-mark REQUIREMENTS.md, update 07-VALIDATION.md's Manual-Only table, and close out deferred-items.md</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the Timing checklist at lines 91-94, the status table rows at lines 208-211, the coverage summary at lines ~225-235 (including "Phase 7: 4 (`TIME-01`..`TIME-04`)" and the in-scope/open counts), and the status vocabulary the rest of the file uses: only `Complete`, `Complete (plan-id)` and `Pending` appear today, so any new value must be introduced deliberately and consistently in both the checklist and the table.
    - `.planning/phases/07-cycle-timing-and-wedge-triage/07-VALIDATION.md` — the Per-Task Verification Map, the Wave 0 Requirements list with its CPUHISTORY_GET caveat, the five Manual-Only Verifications rows (one OUTSTANDING, two PARTIAL), the Validation Sign-Off checkboxes, and the `nyquist_compliant: false` frontmatter with its named blocker.
    - `.planning/phases/07-cycle-timing-and-wedge-triage/deferred-items.md` — both entries: the pre-existing worktree-path test failure (still deferred, unrelated) and the 07-10 Route A live decode mismatch (this batch's subject).
    - Every gap-closure SUMMARY that exists: `07-11-SUMMARY.md` through `07-17-SUMMARY.md`. These are the only admissible evidence for a status change.
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/phases/07-cycle-timing-and-wedge-triage/07-VALIDATION.md, .planning/phases/07-cycle-timing-and-wedge-triage/deferred-items.md</files>
  <action>
**(a) `.planning/REQUIREMENTS.md`.** Re-mark `TIME-01`..`TIME-04` from the SUMMARY evidence, not from
intent. Use `Complete (07-NN, 07-NN)` naming the plans whose SUMMARYs carry the evidence for any row
that is genuinely complete and live-proven. For any row whose live proof came back skipped or
outstanding, do **not** write `Complete`: use `Partial (07-NN)` in the status table with a one-line
reason, mark the checklist item `[ ]` rather than `[x]`, and add a short note naming what is missing.
Introduce `Partial` consistently in both the checklist and the table if you need it — it is a new
status value for this file, so state its meaning where the file's other conventions are described.
Then reconcile the derived numbers so the document is internally consistent: the "Open requirements
per phase" line's Phase 7 count and the in-scope/open totals in the coverage summary must match the
markings you just made. An inconsistent count is the same class of defect as the false claim this plan
is fixing.

**(b) `07-VALIDATION.md`.** Update the Manual-Only Verifications table with this batch's real results:
  - "Route A stopwatch on a >= 3.10 build" — the row that blocked `nyquist_compliant`. Replace its
    OUTSTANDING content with 07-13's recorded result (capability, version quad, measured cycles) or,
    if 07-13 did not pass, with the actual observed failure. Cite 07-12 for the re-derived layout and
    07-13 for the live proof.
  - "`vice_diagnose` against a real checkpoint trap and a real wedge" — replace the PARTIAL content
    with 07-17's per-verdict results, including any recorded skip and its reason.
  - "Second-client contention does not itself hang the diagnostician" — replace the PARTIAL content
    with 07-13 Task 3's measured elapsed time and observed outcome, and keep the honest statement that
    the broker-mediated `monitor_held_elsewhere` verdict remains unit-proven only.
  - Add rows for the two behaviours this batch introduced that only a real emulator can prove:
    `stockConnect()` on both binaries (07-13 Task 1), and the `restarted` verdict after a real
    kill-and-relaunch (07-17 Task 3).
  Update the Wave 0 Requirements list's CPUHISTORY_GET caveat to point at the re-derived layout and the
  committed real captures instead of describing the assumed layout as unproven.
  Update the Validation Sign-Off checkboxes and `nyquist_compliant` **from the rows, not toward a
  desired value**: set `nyquist_compliant: true` only if every Manual-Only row now records a full
  pass. If any row is still short, leave it `false` and name the new blocker — the standing instruction
  this document already carries (T-07-19).

**(c) `deferred-items.md`.** Add a resolution note under the 07-10 Route A entry: state that this
gap-closure batch re-derived the layout from `monitor_binary.c` and committed real captures (07-12),
guarded the probe so a decode failure can never fail the handshake (07-11), and live-verified the
result (07-13) — with the observed outcome. Do not delete the original entry: it is the record of how
the defect was found and why fixtures alone could not catch it. If anything from that entry remains
open, say what. Leave the pre-existing worktree-path failure entry untouched — it is still deferred and
still unrelated.
  </action>
  <verify>
  <verify>
    <automated>cd .claude/mcp/vice && node test-gate.mjs; cd /home/henrik/dev/henrik/git/c64-re-tools && grep -n 'TIME-0' .planning/REQUIREMENTS.md && grep -n 'nyquist_compliant' .planning/phases/07-cycle-timing-and-wedge-triage/07-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Every `TIME-0N` row's status in `.planning/REQUIREMENTS.md`'s table names at least one gap-closure plan id (`07-11` .. `07-17`) or is explicitly `Partial` with a reason.
    - Consistency: the "Open requirements per phase" line's Phase 7 count equals the number of `TIME-*` checklist items still `[ ]`, and the coverage summary's in-scope/open totals are arithmetically consistent with the table. Show the arithmetic in the SUMMARY.
    - `grep -c 'OUTSTANDING' .planning/phases/07-cycle-timing-and-wedge-triage/07-VALIDATION.md` is 0 **or** every remaining occurrence names a new, current blocker attributable to a gap-closure SUMMARY (not the old CPUHISTORY_GET one).
    - `grep -c 'deferred-items.md' .planning/phases/07-cycle-timing-and-wedge-triage/07-VALIDATION.md` still resolves to a real pointer, and the Manual-Only table's Route A row cites `07-12` and `07-13`.
    - `nyquist_compliant`'s value matches the table: `true` only if no Manual-Only row is short of a full pass. State the value chosen and the evidence for it in the SUMMARY.
    - `deferred-items.md` retains its original 07-10 entry text and gains a resolution note citing 07-11, 07-12 and 07-13; the worktree-path entry is byte-identical to before.
    - `git diff --stat .planning/` shows changes to exactly three files.
  </acceptance_criteria>
  <done>REQUIREMENTS.md's TIME rows, the Manual-Only table, the sign-off checkboxes and the deferred item all match the recorded evidence, with internally consistent counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> a future agent's destructive decision | `vice-wedge-triage/SKILL.md` is the instruction an agent follows before calling the destructive `vice_recycle`; a wrong or overstated claim there causes a healthy instance to be killed |
| `docs/stock-vice-parity.md` -> a future maintainer's debugging | A false "live-confirmed" claim sends a maintainer looking for the bug in the wrong place, which is exactly what happened between 07-10 and this batch |
| `.planning/REQUIREMENTS.md` -> release gating | A `Complete` marking that outruns its evidence lets a milestone ship on an unproven requirement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-18-01 | Spoofing | "live-confirmed" claims in docs and the skill | mitigate | Every such claim in the edited sections must name the binary path, the observed version quad, and the plan whose SUMMARY holds the evidence; the acceptance criteria require enumerating them. |
| T-07-18-02 | Elevation of Privilege | an agent escalating to `vice_recycle` on a non-verdict answer | mitigate | Task 2's new `diagnosis_unavailable` triage row explicitly prescribes "do not recycle on this answer alone", mirroring the wording 07-15 built into the message itself, so the instruction and the tool agree. |
| T-07-18-03 | Repudiation | overwriting an outstanding proof with a green claim | mitigate | Task 3 requires the sign-off value and every table row to be derived from the SUMMARYs; `nyquist_compliant: true` is permitted only when no row is short, and a remaining `OUTSTANDING` must name a current blocker. |
| T-07-18-04 | Tampering | scope creep into Phase 8's `SKILL-01` sweep | mitigate | Task 2 fences the edits: one skill fully, one bounded correction in a second skill, and an explicit prohibition on install docs, the support table and `BACK-05`. A `git diff --stat` criterion enforces the file count. |
| T-07-18-05 | Information Disclosure | documenting the binary monitor's unauthenticated nature | accept | Already documented in `CLAUDE.md` and the existing docs; restating the 127.0.0.1-only rule is a security benefit, not a disclosure. |
| T-07-18-SC | Tampering | npm/pip/cargo installs | mitigate | Documentation-only plan; no installs, no dependency changes, no code changes. |
</threat_model>

<verification>
- `cd .claude/mcp/vice && node test-gate.mjs` shows only the documented pre-existing worktree-path failure (this plan changes no code, so the suite must be unchanged).
- `grep -c 'so the capability now actually resolves to' docs/stock-vice-parity.md` is 0.
- `grep -c 'diagnosis_unavailable' docs/stock-vice-parity.md .claude/skills/vice-wedge-triage/SKILL.md` is >= 1 for each file.
- `grep -c 'has no working timeout' .claude/skills/c64-program-recon/references/tool-selection.md` is 0.
- `git diff --stat` shows exactly the six files in this plan's `files_modified` and no others.
- Every "live-confirmed" occurrence in the edited sections names a binary and a version quad.
</verification>

<success_criteria>
- No document claims a live confirmation this phase's verification disproved.
- `vice-wedge-triage`'s opening move has a documented, actionable response for all five verdicts and for `diagnosis_unavailable`.
- `REQUIREMENTS.md`'s TIME rows, and its derived counts, match the recorded evidence.
- `07-VALIDATION.md`'s Manual-Only table and `nyquist_compliant` value are derived from the gap-closure SUMMARYs, with any remaining gap named.
- `deferred-items.md` records how the Route A defect was closed without erasing how it was found.
</success_criteria>

<output>
Create `.planning/phases/07-cycle-timing-and-wedge-triage/07-18-SUMMARY.md` when done. Record: every
claim changed with its source SUMMARY, the enumerated list of surviving "live-confirmed" claims and
what each now names, the final `TIME-01`..`TIME-04` statuses with the arithmetic reconciliation, the
`nyquist_compliant` value chosen and why, and anything still outstanding after this batch.
</output>
