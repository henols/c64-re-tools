---
phase: 07-cycle-timing-and-wedge-triage
plan: 10
type: execute
wave: 7
depends_on: ["07-09"]
files_modified:
  - docs/stock-vice-parity.md
  - .claude/skills/vice-wedge-triage/SKILL.md
  - .planning/phases/07-cycle-timing-and-wedge-triage/07-VALIDATION.md
autonomous: true
requirements: [TIME-01, TIME-02, TIME-03, TIME-04]

must_haves:
  truths:
    - "vice-wedge-triage's documented opening move works on stock: the skill names the stock route and the five-verdict stock vocabulary rather than describing only the fork's six."
    - "The three divergences this phase creates are recorded in docs/stock-vice-parity.md: stale_read_path's absence on stock (D-03), the honest wraparound refusal below VICE 3.10, and the screenshot-free stock incident record (D-01)."
    - "07-VALIDATION.md's Per-Task Verification Map has no TBD rows: every behaviour resolves to a real task ID."
    - "The skill's two now-false claims are corrected: vice_run_until does have a working timeout on stock (D-02), and the vice_ping-based manual bracket is fork-only."
  artifacts:
    - path: "docs/stock-vice-parity.md"
      provides: "Phase 7's divergence record, replacing the superseded parity items 3 and 4"
      contains: "stale_read_path"
    - path: ".claude/skills/vice-wedge-triage/SKILL.md"
      provides: "the stock route for the opening move and the stock verdict table"
      contains: "monitor_held_elsewhere"
    - path: ".planning/phases/07-cycle-timing-and-wedge-triage/07-VALIDATION.md"
      provides: "the resolved Per-Task Verification Map"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".claude/skills/vice-wedge-triage/SKILL.md"
      to: ".claude/mcp/vice/tools-manifest.stock.json"
      via: "the skill's verdict list matches the manifest's pinned verdict enum"
      pattern: "monitor_held_elsewhere"
---

<objective>
Close the phase's documentation obligations: make `vice-wedge-triage`'s documented
opening move correct on the stock backend (ROADMAP success criterion 4), record this
phase's three deliberate divergences in `docs/stock-vice-parity.md`, and resolve
`07-VALIDATION.md`'s `TBD` task IDs so the phase's own validation contract is complete.

Purpose: criterion 4 is a skill-documentation criterion, and an undocumented missing
verdict is exactly the silent capability gap Phase 8 exists to prevent — recording it
here is what keeps Phase 8's manifest-derived support table honest.

**Scope fence, restated:** this plan touches `vice-wedge-triage/SKILL.md` **only** as far
as criterion 4 requires. It does not perform Phase 8's `SKILL-01` methodology sweep over
the other five skills, does not add the `BACK-05` runtime capability error, and does not
build the manifest-derived support table.
Output: three corrected documents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cycle-timing-and-wedge-triage/07-VALIDATION.md
@.planning/phases/07-cycle-timing-and-wedge-triage/07-05-SUMMARY.md
@.planning/phases/07-cycle-timing-and-wedge-triage/07-06-SUMMARY.md
@.planning/phases/07-cycle-timing-and-wedge-triage/07-07-SUMMARY.md
@.planning/phases/07-cycle-timing-and-wedge-triage/07-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record Phase 7's divergences in docs/stock-vice-parity.md</name>
  <files>docs/stock-vice-parity.md</files>
  <read_first>
    - `docs/stock-vice-parity.md` — §A items 3 (`vice_run_until` "for N cycles") and 4
      (`vice_cycles_stopwatch`), both written before this phase and both now partly
      superseded; §A item 7 (expected divergences licensed by design) for the house
      style of a licensed-divergence entry; §B item 1 (CPU instruction-history trace)
      and item 3 (full resource get/set), which this phase partly consumes.
    - `.claude/mcp/vice/stock-timing.ts` — the actual Route A / Route B behaviour and
      the `exactness` / `caveat` / `measurable` fields the answer carries.
    - `.claude/mcp/vice/stock-diagnose.ts` — `STOCK_DIAGNOSE_VERDICTS` and the comment
      recording why `stale_read_path` is unreachable on stock.
    - `.claude/mcp/vice/stock-recycle.ts` — the four evidence items and the absent
      screenshot.
    - `.planning/phases/07-cycle-timing-and-wedge-triage/07-01-SUMMARY.md` — the
      `probeCpuHistory()` fix, which is a stock **gain**, not a loss.
  </read_first>
  <action>
Rewrite §A item 4 (`vice_cycles_stopwatch`) to describe what actually shipped, replacing
the superseded "second, always-available route via `LIN`/`CYC` plus a frame count"
description — that frame-count half was rejected (see the D-11 trace-guard finding, now
recorded as SUPERSEDED in `docs/phase0-binmon-findings.md` §1). State: Route A on VICE
>= 3.10 is exact for any bracket length; Route B below 3.10 reports an exact delta only
when no frame wraparound is detected, labels it `within-one-frame-unverified` with an
explicit caveat, and **refuses by name** — carrying no cycles figure at all, never a zero
— when a frame boundary is proven crossed. Note that "atomic reset_and_read" is
client-side baseline math on both routes, which the old text already had right.

Amend §A item 3 (`vice_run_until` "for N cycles"): run-until-*address* is exact on stock;
halting after exactly N cycles is still not native and the `cycles` argument is refused
in the fork's own words on both backends; and stock now additionally exposes an
**optional `timeout_ms`** (default 30000) that the fork does not have, so an unreachable
address is bounded on stock and unbounded on the fork.

Add three new entries in the same house style:
- **`vice_diagnose`'s verdict set differs by one, deliberately (D-03).** Stock answers
  five verdicts; `stale_read_path` is absent because it exists on the fork only to
  describe a state where one read path is stale while another advances, which requires
  the fork's non-pausing `vice_ping` alongside pausing reads. On stock every read pauses
  uniformly, so the state is unreachable by construction, not merely unimplemented. Note
  that stock also gains a verdict the fork has no name for — `monitor_held_elsewhere` —
  because stock VICE's binary monitor services exactly one client.
- **`vice_recycle`'s stock incident record carries no screenshot (D-01).** Stock has no
  screenshot tool and `SHOT-*` is out of this milestone's scope, so the stock record
  carries four evidence items (bracket, registers, armed checkpoints, resolved IRQ
  handler) instead of the fork's six. The record is still written **before** the
  destructive RPC on both backends — that ordering is not a divergence.
- **A stock correctness gain (BACK-04, this phase).** `probeCpuHistory()` sent
  `CPUHISTORY_GET` with `count=0`, which real VICE rejects with `InvalidParameter`
  (0x81); the whole stock connect handshake therefore failed on any genuine VICE >= 3.10
  build. Fixed here, so the capability now actually reaches `"available"` where the
  build supports it.

Also update §B item 1's framing: `CPUHISTORY_GET` is no longer purely a future gain —
this phase consumes its cycle field for the stopwatch, while the tracing feature set
(the cut Phase 6 `GAIN-*` work) remains unbuilt. Do not claim tracing shipped.
  </action>
  <acceptance_criteria>
    - `grep -c stale_read_path docs/stock-vice-parity.md` returns at least 1, and the
      surrounding text contains both `unreachable by construction` and
      `monitor_held_elsewhere`.
    - `grep -c "second, always-available" docs/stock-vice-parity.md` returns 0 — the
      superseded claim is gone from this file too.
    - The `vice_cycles_stopwatch` entry contains the string
      `within-one-frame-unverified` and states that a proven wraparound is refused.
    - The `vice_run_until` entry names `timeout_ms` and its `30000` default.
    - A `vice_recycle` entry exists naming the four evidence items and the absent
      screenshot, and states the record-before-RPC ordering is shared by both backends.
    - An entry records the `probeCpuHistory()` `count=0` / `0x81` fix as a stock gain.
    - §B item 1 does not claim CPU-history tracing shipped.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "stale_read_path" docs/stock-vice-parity.md && grep -q "within-one-frame-unverified" docs/stock-vice-parity.md && grep -q "timeout_ms" docs/stock-vice-parity.md && grep -q "monitor_held_elsewhere" docs/stock-vice-parity.md && test "$(grep -c 'second, always-available' docs/stock-vice-parity.md)" = "0"</automated>
  </verify>
  <done>All three Phase 7 divergences plus the one correctness gain are recorded in the parity document, and the two superseded parity entries now describe what shipped.</done>
</task>

<task type="auto">
  <name>Task 2: Make vice-wedge-triage/SKILL.md correct on the stock backend</name>
  <files>.claude/skills/vice-wedge-triage/SKILL.md</files>
  <read_first>
    - `.claude/skills/vice-wedge-triage/SKILL.md` — the whole file. Specifically: the
      four-state table at the top, the "returns a closed five-verdict vocabulary" line
      naming `stale_read_path`, "The order" list, the Verdict -> response table, the
      "Two traps that read as a wedge and are not" section (which claims
      `vice_run_until` has no working timeout), "The manual fallback" section (whose
      four-call bracket polls with `vice_ping` ×3), the Provenance table, and the
      Troubleshooting table.
    - `.claude/mcp/vice/tools-manifest.stock.json` — the pinned `vice_diagnose` verdict
      enum, so the skill's list and the schema agree exactly.
    - `.claude/mcp/vice/stock-diagnose.ts` — the check order the skill describes must
      match what stock actually does.
    - `docs/stock-vice-parity.md` — task 1's new entries, which the skill can point at
      rather than restating.
  </read_first>
  <action>
Make the minimum set of edits that make the documented opening move correct on stock.
Keep the skill's voice and structure; do not restructure it.

1. **The verdict vocabulary.** Replace the single "closed five-verdict vocabulary" line
   with a backend-aware statement: the fork answers `restarted`, `checkpoint_trap`,
   `wedged`, `stale_read_path`, `live`; stock answers `restarted`, `checkpoint_trap`,
   `wedged`, `monitor_held_elsewhere`, `live`. State in one sentence why the sets differ,
   citing `docs/stock-vice-parity.md` rather than repeating the reasoning.
2. **The Verdict -> response table.** Mark the `stale_read_path` row **fork only**, and
   add a `monitor_held_elsewhere` row: it means another client already holds this
   instance's single binary-monitor socket, which is behaviourally identical to a hang
   from outside; the response is to release or identify the other holder, and it is
   **never** a reason to recycle — recycling here destroys an instance that is not even
   wedged.
3. **The state table at the top.** Its "Four states look identical from outside" framing
   becomes five on stock. Add the monitor-contention row with its cheap tell and its safe
   action.
4. **The `vice_run_until` trap.** The claim "there is no working timeout to bound it" is
   now false on stock. Rewrite it: on the fork, `cycles` remains documented as not
   implemented and there is no bound; on stock, pass `timeout_ms` (default 30000) and an
   unreachable address returns a bounded, explicit timeout answer rather than looking
   like a wedge. Keep the underlying judgement — check whether you asked for an address
   that cannot be reached — because it is still the right first question.
5. **The manual fallback.** Its four-call bracket polls with `vice_ping` ×3 and cites the
   986,693-vs-991,569 cycles/s measurement; that measurement is fork-only, because on
   stock **any** inbound byte halts the machine and there is no non-pausing call at all.
   Mark the existing procedure fork-only and add the stock equivalent: one state read,
   one `vice_execution_run`, a real wait with **no calls at all**, then one state read —
   and note that `vice_diagnose` already does exactly this on stock, so the manual
   fallback is for when the broker itself is unreachable.
6. **Provenance table.** Update the `vice_run_until has no working timeout` row to say
   fork-only, and add a row for the stock five-verdict path with its evidence grade
   (unit-proven plus the live checks recorded in this phase's own verification) — do not
   overstate: if the live pass has not been recorded yet, grade it accordingly.
7. **Troubleshooting table.** Add a row for the monitor-contention symptom: `vice_diagnose`
   answers `monitor_held_elsewhere`, or a call hangs with no reply and no EOF — the fix
   is to find the other client, and it is not a wedge.

Do **not** touch the other five skills — that sweep is Phase 8's `SKILL-01`.
  </action>
  <acceptance_criteria>
    - `grep -c monitor_held_elsewhere .claude/skills/vice-wedge-triage/SKILL.md` returns
      at least 3 (the vocabulary line, the verdict table row, the troubleshooting row).
    - The `stale_read_path` row in the verdict table is marked fork-only; the file states
      the stock set is the five in the manifest enum.
    - The file no longer contains an unqualified claim that `vice_run_until` has no
      working timeout — the string `timeout_ms` appears, with the `30000` default.
    - The `vice_ping` ×3 manual bracket is explicitly marked fork-only, and a stock
      procedure with zero calls during the wait is described.
    - `git diff --name-only` shows no other file under `.claude/skills/` changed.
    - `node scripts/check-skill-tool-coverage.mjs` (the blocking coverage check added in
      Phase 5) still passes.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "monitor_held_elsewhere" .claude/skills/vice-wedge-triage/SKILL.md && grep -q "timeout_ms" .claude/skills/vice-wedge-triage/SKILL.md && node scripts/check-skill-tool-coverage.mjs</automated>
  </verify>
  <done>The skill's opening move, verdict table, traps, manual fallback, provenance and troubleshooting are all correct on stock, and no other skill was touched.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve 07-VALIDATION.md's Per-Task Verification Map</name>
  <files>.planning/phases/07-cycle-timing-and-wedge-triage/07-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/07-cycle-timing-and-wedge-triage/07-VALIDATION.md` — the whole
      file: the frontmatter flags, the Per-Task Verification Map's `TBD` rows and their
      keyed behaviours, the Wave 0 Requirements checklist, the Manual-Only Verifications
      table, and the Validation Sign-Off checklist.
    - Every `07-NN-SUMMARY.md` produced by plans 01 through 09 — the actual task IDs,
      the actual test file names, and any behaviour that moved between plans during
      execution.
  </read_first>
  <action>
Fill in the Per-Task Verification Map. Each row's `Task ID` becomes the real
`{phase}-{plan}-{task}` identifier from the shipped plans, `Plan` becomes the plan
number, and `Wave` becomes that plan's actual wave — note the waves in the map's current
draft (0/1/2) were placeholders and the shipped structure is waves 1 through 7, so
correct them rather than preserving the draft numbering. Set `File Exists` to the real
state and `Status` to the observed result of running each row's command.

Keep the existing threat references (`T-07-01`, `T-07-02`, `T-07-03`) attached to the
same behaviours, and extend the map with any behaviour a plan added that the draft did
not anticipate — at minimum: the `RESOURCE_GET` encoder/parser round-trip, the
video-standard fallback, the bounded session acquisition in `vice_diagnose`, and the
record-before-RPC ordering proof in `stock-recycle.test.ts`. Reference the additional
threat IDs those plans introduced rather than leaving the column blank.

Tick the Wave 0 Requirements checklist against what actually landed, and correct its
`tools-manifest.stock.json` line: the count went 34 -> 36 -> 38 across plans 07-08 and
07-09, not 34 -> 37 as the draft says.

Record a result for every Manual-Only Verification row, or mark it explicitly outstanding
with what remains — a blank row is not a pass. `/usr/bin/x64sc` on this machine is
genuine unpatched stock VICE 3.9, so these are runnable; remember the flag-order gotcha
(`-default` must precede `-binarymonitor` or the monitor never binds).

Set `nyquist_compliant: true` in the frontmatter **only** when no `TBD` remains, every
row has a command and a status, and the Validation Sign-Off checklist is fully ticked.
If any manual row is still outstanding, leave the flag `false` and say which row blocks
it — do not tick a checklist to reach a flag.
  </action>
  <acceptance_criteria>
    - `grep -c TBD .planning/phases/07-cycle-timing-and-wedge-triage/07-VALIDATION.md`
      returns 0.
    - Every Per-Task Verification Map row has a non-empty Task ID, Plan, Wave,
      Requirement, Test Type, Automated Command and Status.
    - The Wave 0 Requirements checklist has no unticked box that in fact shipped, and its
      manifest line reads 34 -> 36 -> 38.
    - Every Manual-Only Verification row carries either a recorded result or an explicit
      outstanding note naming what remains.
    - `nyquist_compliant` is `true` only if no row is outstanding; otherwise it is
      `false` with the blocking row named in the document.
    - Running every distinct Automated Command listed in the map passes.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c TBD .planning/phases/07-cycle-timing-and-wedge-triage/07-VALIDATION.md)" = "0" && cd .claude/mcp/vice && npm run test:automated</automated>
  </verify>
  <done>The phase's validation contract names a real task for every behaviour it samples, its manifest counts are corrected, and its compliance flag reflects the recorded evidence rather than an aspiration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill documentation -> Claude at runtime | `SKILL.md` is the methodology an agent follows when deciding whether to destroy a running emulator |
| parity documentation -> future phases | `docs/stock-vice-parity.md` is what Phase 8's capability-honesty work reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-14 | Elevation of Privilege (a healthy instance recycled on a misread verdict) | `SKILL.md` verdict table | mitigate | The `monitor_held_elsewhere` row states explicitly that it is never a reason to recycle, and `stale_read_path` is marked fork-only so a stock agent does not wait for a verdict that cannot arrive |
| T-07-01 | Information Disclosure (an undocumented capability gap) | `docs/stock-vice-parity.md` | mitigate | The absent verdict, the wraparound refusal and the screenshot-free record are each recorded with their reason, which is what keeps Phase 8's manifest-derived support table honest |
| T-07-11 | Tampering (via stale guidance) | `SKILL.md` manual fallback and `vice_run_until` trap | mitigate | The `vice_ping`-poll bracket is marked fork-only (on stock every inbound byte halts the machine, so that procedure measures nothing) and the no-timeout claim is corrected with the real `timeout_ms` default |
| T-07-19 | Repudiation (unverifiable validation claim) | `07-VALIDATION.md` | mitigate | `nyquist_compliant` may only be set true when no row is outstanding; an outstanding manual row must be named rather than absorbed by a ticked checklist |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs no packages |
</threat_model>

<verification>
- `grep -c TBD .planning/phases/07-cycle-timing-and-wedge-triage/07-VALIDATION.md`
  returns 0.
- `node scripts/check-skill-tool-coverage.mjs` passes.
- `cd .claude/mcp/vice && npm run test:automated` passes.
- `docs/stock-vice-parity.md` contains `stale_read_path`,
  `within-one-frame-unverified`, `timeout_ms` and `monitor_held_elsewhere`, and no
  longer contains `second, always-available`.
- `git diff --name-only .claude/skills/` lists only `vice-wedge-triage/SKILL.md`.
</verification>

<success_criteria>
- `vice-wedge-triage`'s opening move, verdict table and manual fallback are correct on
  stock.
- The three Phase 7 divergences and the one correctness gain are recorded in the parity
  document.
- The Per-Task Verification Map has no `TBD` rows and its counts are corrected.
- No other skill and no Phase 8 work was pulled forward.
</success_criteria>

<output>
Create `.planning/phases/07-cycle-timing-and-wedge-triage/07-10-SUMMARY.md` when done
</output>
