---
phase: 07-cycle-timing-and-wedge-triage
plan: 09
type: execute
wave: 6
depends_on: ["07-06", "07-07", "07-08"]
files_modified:
  - .claude/mcp/vice/stock-dispatch.ts
  - .claude/mcp/vice/stock-derived.ts
  - .claude/mcp/vice/tools-manifest.stock.json
  - .claude/mcp/vice/package.json
  - .claude/mcp/vice/stock-dispatch.test.ts
autonomous: true
requirements: [TIME-04]

must_haves:
  truths:
    - "vice_diagnose on the stock backend answers a verdict instead of being refused by name — the wedge-triage skill's documented opening move works (D-03's five verdicts)."
    - "vice_recycle on the stock backend performs the recovery the wedged verdict points at (D-01), instead of being refused by name."
    - "The two proxy-local tools are advertised on stock through the same manifest the rest of the stock surface uses, so the bidirectional table/manifest agreement guard stays intact rather than being loosened."
    - "vice_diagnose's needsSession:false registration is a named, documented exception to the derived-pure-handler rule, not a silent contradiction of it."
  artifacts:
    - path: ".claude/mcp/vice/tools-manifest.stock.json"
      provides: "38 tools — 36 plus vice_diagnose and vice_recycle"
    - path: ".claude/mcp/vice/stock-dispatch.ts"
      provides: "the two remaining withDerivedTool entries"
      contains: "handleDiagnoseStock"
  key_links:
    - from: ".claude/mcp/vice/vice-proxy.ts"
      to: ".claude/mcp/vice/stock-dispatch.ts"
      via: "buildBackendAwareTool already routes DIAGNOSE_TOOL and RECYCLE_TOOL to dispatchStock on stock — no vice-proxy.ts change"
      pattern: "buildBackendAwareTool"
    - from: ".claude/mcp/vice/tools-manifest.stock.json"
      to: "stock-dispatch.test.ts PROXY_LOCAL_TOOLS"
      via: "a third named category for tools the fork serves proxy-locally and therefore never lists in its own manifest"
      pattern: "PROXY_LOCAL_TOOLS"
---

<objective>
Register the two proxy-local tools on the stock backend: `vice_diagnose` (the five-verdict
handler from plan 07-06) and `vice_recycle` (the stock evidence gatherer plus broker RPC
from plan 07-07). Both are already routed to `dispatchStock()` on stock by
`buildBackendAwareTool()`, and both currently land in that function's refuse-by-name
branch — only the table entries and their manifest counterparts are missing.

Purpose: ROADMAP success criteria 3 and 4. No `vice-proxy.ts` change is needed or
permitted here: a structural test already asserts that `handleDiagnose` and
`handleRecycle` are each referenced by exactly one registration and that it is
backend-aware.
Output: 38 tools on the stock manifest, four verdict-and-recovery paths dispatchable, and
one new named test category recording why these two have no fork manifest entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cycle-timing-and-wedge-triage/07-PATTERNS.md
@.planning/phases/07-cycle-timing-and-wedge-triage/07-06-SUMMARY.md
@.planning/phases/07-cycle-timing-and-wedge-triage/07-07-SUMMARY.md
@.planning/phases/07-cycle-timing-and-wedge-triage/07-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch entries, derived registry, files[], and the documented needsSession exception</name>
  <files>.claude/mcp/vice/stock-dispatch.ts, .claude/mcp/vice/stock-derived.ts, .claude/mcp/vice/package.json</files>
  <read_first>
    - `.claude/mcp/vice/stock-dispatch.ts` — the table entries plan 07-08 added, and
      `dispatchStock()`'s refuse-by-name miss branch (the branch these two currently
      hit).
    - `.claude/mcp/vice/stock-derived.ts` — `STOCK_DERIVED_TOOLS`, and
      `DerivedPureHandler`'s doc comment claiming a `needsSession:false` handler
      "structurally cannot reach the wire". That claim needs amending here, because
      `vice_diagnose` acquires its own session deliberately.
    - `.claude/mcp/vice/stock-diagnose.ts` and `.claude/mcp/vice/stock-recycle.ts` — the
      exported handler names (`handleDiagnoseStock`, `handleRecycleStock`) and the
      header comment in `stock-diagnose.ts` explaining why it owns its own session
      acquisition.
    - `.claude/mcp/vice/vice-proxy.ts` — `buildBackendAwareTool()` and the two
      registration lines for `RECYCLE_TOOL` and `DIAGNOSE_TOOL`, to confirm no change is
      needed there.
    - `.claude/mcp/vice/package.json` — `files[]`.
  </read_first>
  <action>
Add two `STOCK_DISPATCH_TABLE` entries under a `// derived (TIME-04)` comment:

- `vice_diagnose: withDerivedTool("vice_diagnose", { needsSession: false }, handleDiagnoseStock)`
- `vice_recycle: withDerivedTool("vice_recycle", { needsSession: true }, handleRecycleStock)`

Note the deliberate asymmetry and comment it at the call site: `vice_diagnose` uses
`needsSession: false` because it must convert a `MonitorOwnershipError` into the
`monitor_held_elsewhere` **verdict** rather than let `withDerivedTool()`'s preamble turn
it into refusal text — the exact generic error string that verdict exists to replace.
`vice_recycle` keeps `needsSession: true`: it needs a live session to gather evidence
and has no verdict vocabulary to preserve.

Add both names to `STOCK_DERIVED_TOOLS` with `// Phase 7, TIME-04` comments.

Amend `DerivedPureHandler`'s doc comment in `stock-derived.ts`: it currently asserts that
a `needsSession:false` handler structurally cannot reach the wire. State the one
declared exception by name — `vice_diagnose` acquires its own session through
`ensureStockSession(deps)` inside its own try/catch so that every one of its five
verdicts, including the two derived from handshake errors, stays inside a single
`isError:false` answer shape matching the fork's own `handleDiagnose()` contract. Keep
the general rule and its rationale; add the exception rather than deleting the claim.

Add `"stock-diagnose.ts"` and `"stock-recycle.ts"` to `package.json`'s `files[]`, in the
same commit.

Make no change to `vice-proxy.ts`. The structural CR-07 tests already require that
`handleDiagnose` and `handleRecycle` each have exactly one registration and that it goes
through `buildBackendAwareTool()`; those registrations already route to `dispatchStock()`
on the stock arm.
  </action>
  <acceptance_criteria>
    - `grep -c handleDiagnoseStock .claude/mcp/vice/stock-dispatch.ts` returns 1 and the
      same for `handleRecycleStock`.
    - `grep -c vice_diagnose .claude/mcp/vice/stock-derived.ts` returns 1 and the same
      for `vice_recycle`.
    - `git diff --stat` shows `vice-proxy.ts` unchanged by this plan.
    - `DerivedPureHandler`'s doc comment names `vice_diagnose` as the declared exception.
    - Reading `package.json` shows `files` containing both `stock-diagnose.ts` and
      `stock-recycle.ts`.
    - `node scripts/check-npm-packages.mjs` from the repo root passes.
    - `npx tsc --noEmit -p .claude/mcp/vice/tsconfig.json` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/mcp/vice && npx tsc --noEmit -p tsconfig.json && node -e "const p=require('./package.json'); if(!(p.files.includes('stock-diagnose.ts')&&p.files.includes('stock-recycle.ts'))) process.exit(1)" && node ../../../scripts/check-npm-packages.mjs</automated>
  </verify>
  <done>Both tools dispatch to their stock handlers, the needsSession asymmetry is documented at both the call site and the type's doc comment, and both modules ship.</done>
</task>

<task type="auto">
  <name>Task 2: Manifest entries (36 -> 38) and the PROXY_LOCAL_TOOLS category</name>
  <files>.claude/mcp/vice/tools-manifest.stock.json, .claude/mcp/vice/stock-dispatch.test.ts</files>
  <read_first>
    - `.claude/mcp/vice/stock-dispatch.test.ts` — the D-03 name-coverage test and
      `STOCK_ONLY_TOOLS` (with the comment explaining that a stock-only addition is a
      deliberate edit to a named list, never a loosened assertion), the bidirectional
      table/manifest agreement test (this is what forces a manifest entry for every
      dispatch-table key), the outputSchema-presence test, `REGISTERED_TOOL_NAMES` and
      the count assertions plan 07-08 set to 36.
    - `.claude/mcp/vice/vice-proxy.ts` — `RECYCLE_TOOL` and `DIAGNOSE_TOOL`'s
      `ToolDefinition` literals: their exact `description` text and `inputSchema`
      (`vice_recycle` requires a `reason` string; `vice_diagnose` takes no arguments).
      The stock manifest entries must not contradict them, since both backends serve
      these names.
    - `.claude/mcp/vice/stock-diagnose.ts` — `STOCK_DIAGNOSE_VERDICTS`, the exact five
      values the manifest `enum` must carry.
    - `.claude/mcp/vice/stock-recycle.ts` — the exact payload keys `handleRecycleStock`
      emits on its success path.
  </read_first>
  <action>
Add two entries to `tools-manifest.stock.json`, taking it from 36 to 38.

**`vice_diagnose`** — `inputSchema` an object with no properties and no `required`,
matching `DIAGNOSE_TOOL`. The description must state the stock verdict set explicitly and
name the divergence: five verdicts, and `stale_read_path` is **absent on stock** because
every stock read pauses the machine uniformly, so the state the fork's sixth verdict
describes is unreachable by construction. `outputSchema` requires `verdict`, `report` and
`runState`; `verdict` carries an `enum` of exactly the five values in
`STOCK_DIAGNOSE_VERDICTS`, in the same order. `evidence` and `machinePaused` are optional
properties.

**`vice_recycle`** — `inputSchema` with a required `reason` string, matching
`RECYCLE_TOOL`'s required set exactly. The description must keep the fork's
DESTRUCTIVE framing and add the stock specifics: the incident record is written before
anything is killed, and **no screenshot** is captured on stock (`SHOT-*` is out of scope
and stock has no screenshot tool). `outputSchema` requires `requested`, `outcome`,
`recordPath` and `runState`, with the remaining fields optional.

Then, in `stock-dispatch.test.ts`, add a **third** named category beside
`STOCK_ONLY_TOOLS`: `PROXY_LOCAL_TOOLS = new Set(["vice_diagnose", "vice_recycle"])`,
with a comment explaining precisely why it exists — these two are served proxy-locally on
**both** backends and are therefore absent from `tools-manifest.json` (which is
regenerated from the host fork server's own `tools/list` and cannot know about them), so
they are neither fork-matched nor genuinely stock-only. Extend the D-03 name-coverage
test to skip `PROXY_LOCAL_TOOLS` members in the fork-counterpart branch, and add an
assertion that every `PROXY_LOCAL_TOOLS` member is (a) present in the stock manifest,
(b) absent from the fork manifest, and (c) present in `STOCK_ONLY_TOOLS`'s complement —
i.e. explicitly not mislabelled stock-only, since both backends serve them.

Update `REGISTERED_TOOL_NAMES` to 38 entries and both count assertions to `38`. Do not
loosen the bidirectional agreement test — the point of adding these manifest entries is
that the guard stays exactly as strict as it is.
  </action>
  <acceptance_criteria>
    - `tools-manifest.stock.json` parses and its `tools` array has length exactly 38.
    - `vice_diagnose`'s `outputSchema.properties.verdict.enum` deep-equals
      `["restarted", "checkpoint_trap", "wedged", "monitor_held_elsewhere", "live"]`.
    - `vice_diagnose`'s description contains the string `stale_read_path` and the word
      `absent` (the divergence is stated where a caller reads it).
    - `vice_recycle`'s stock `inputSchema.required` deep-equals `["reason"]`, matching
      `RECYCLE_TOOL`.
    - `PROXY_LOCAL_TOOLS` exists in `stock-dispatch.test.ts` with exactly those two
      members, and neither appears in `STOCK_ONLY_TOOLS`.
    - `REGISTERED_TOOL_NAMES` has length 38 and both count assertions read 38.
    - `node --test stock-dispatch.test.ts` passes, including the unmodified
      bidirectional agreement and outputSchema-presence tests.
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/mcp/vice && node -e "const m=require('./tools-manifest.stock.json'); if(m.tools.length!==38) process.exit(1)" && node --test stock-dispatch.test.ts</automated>
  </verify>
  <done>Both proxy-local tools are advertised on stock with the five-verdict enum pinned and the stale_read_path divergence stated in the tool's own description, under a new named category that keeps the manifest guards strict.</done>
</task>

<task type="auto">
  <name>Task 3: Conformance cases for vice_diagnose and vice_recycle</name>
  <files>.claude/mcp/vice/stock-dispatch.test.ts</files>
  <read_first>
    - `.claude/mcp/vice/stock-dispatch.test.ts` — `CONFORMANCE_BROKER_CONTROL`,
      `buildConformanceSession()`, `buildConformanceDeps()`, `conformanceTest()`,
      `assertAnswerConforms()` and the completeness guard. `CONFORMANCE_BROKER_CONTROL`
      today exposes only `claimMonitor`/`releaseMonitor` and must gain a `recycle` stub
      for `vice_recycle`'s case.
    - `.claude/mcp/vice/incident-record.test.ts` — the `VICE_INCIDENTS_DIR` redirection
      discipline; the recycle conformance case writes a record and must not write into
      the real `.planning/incidents/`.
    - `.claude/mcp/vice/stock-diagnose.ts` — the two environment overrides
      (`VICE_STOCK_DIAGNOSE_BRACKET_MS`, `VICE_STOCK_DIAGNOSE_SESSION_TIMEOUT_MS`) that
      keep the case fast.
  </read_first>
  <action>
Add one `conformanceTest()` case per tool, both dispatched through the real
`dispatchStock()` path.

**`vice_diagnose`** — set `VICE_STOCK_DIAGNOSE_BRACKET_MS` to a single-digit value for
the duration of the case so the bracket does not add a quarter second per run, and
restore it afterwards. Drive the `sendImpl` stub so the answer lands on a deterministic
verdict (the `checkpoint_trap` shape is the cheapest: it never resumes). Validate the
answer against the declared `outputSchema` and additionally assert the `verdict` value
is a member of the manifest's own enum.

**`vice_recycle`** — extend `CONFORMANCE_BROKER_CONTROL` with a `recycle` stub returning
an ok result, redirect `VICE_INCIDENTS_DIR` to a temp directory for the case, dispatch
with a non-empty `reason`, and validate the answer against the declared `outputSchema`.
Clean the temp directory afterwards.

Add both names to `CONFORMANCE_TOOL_NAMES` so the completeness guard passes against the
38-entry manifest.

Also add one non-conformance regression test asserting the behaviour this whole phase
turns on: `stockHandlerFor("vice_diagnose")` and `stockHandlerFor("vice_recycle")` are
both functions, so a stock call no longer reaches `dispatchStock()`'s refuse-by-name
branch. Assert the refusal text (`is not implemented by the stock backend`) is **not**
present in either tool's dispatched answer.
  </action>
  <acceptance_criteria>
    - `CONFORMANCE_TOOL_NAMES` contains both names and the completeness guard passes
      against the 38-entry manifest.
    - Both cases call `dispatchStock(...)` and `assertAnswerConforms(...)`.
    - The `vice_diagnose` case asserts its `verdict` is a member of the manifest enum.
    - The `vice_recycle` case writes only into its temp directory;
      `git status --porcelain .planning/incidents` is empty after the run.
    - The regression test asserts the text `is not implemented by the stock backend` is
      absent from both dispatched answers.
    - `node --test stock-dispatch.test.ts` passes with 0 failures.
    - `npm run test:automated` from `.claude/mcp/vice` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/mcp/vice && node --test stock-dispatch.test.ts && npm run test:automated</automated>
  </verify>
  <done>Both proxy-local tools' real dispatch answers are schema-validated, the five-verdict enum is cross-checked against the manifest, and the refuse-by-name regression is guarded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client (Claude) -> advertised tool surface | A skill reading `tools/list` decides what it may call; a destructive tool's advertised contract is what a caller relies on before invoking it |
| MCP server -> broker control plane | `vice_recycle` becomes reachable on a second backend here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Denial of Service | `vice_diagnose` on stock | mitigate | The handler's bounded session acquisition (plan 07-06) is what makes advertising it safe; the conformance case exercises the real dispatch path with the bracket override so the suite proves the bounded shape rather than the production timings |
| T-07-16 | Elevation of Privilege (destructive tool advertised without its guard) | `vice_recycle` manifest entry | mitigate | The stock `inputSchema.required` deep-equals `["reason"]`, identical to the fork, so the reason gate is part of the advertised contract on both backends; the description keeps the DESTRUCTIVE framing |
| T-07-01 | Information Disclosure (an undocumented missing capability) | `vice_diagnose` description and verdict enum | mitigate | The five-verdict `enum` is pinned to `STOCK_DIAGNOSE_VERDICTS` and the absence of `stale_read_path` is stated in the tool's own description, so a caller reading the schema learns the divergence rather than discovering it |
| T-07-17 | Spoofing (manifest/table disagreement) | `PROXY_LOCAL_TOOLS` | mitigate | The new category is additive and named; the bidirectional agreement test is left exactly as strict, so a future proxy-local tool must be declared rather than silently exempted |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs no packages |
</threat_model>

<verification>
- `cd .claude/mcp/vice && npx tsc --noEmit -p tsconfig.json` exits 0.
- `cd .claude/mcp/vice && node --test stock-dispatch.test.ts` passes.
- `cd .claude/mcp/vice && npm run test:automated` passes.
- `node scripts/check-npm-packages.mjs` from the repo root passes.
- `git diff --stat` shows no change to `vice-proxy.ts`.
- `tools-manifest.stock.json` has exactly 38 tools.
</verification>

<success_criteria>
- `vice_diagnose` and `vice_recycle` dispatch to their stock handlers instead of being
  refused by name.
- The five-verdict vocabulary is pinned in the manifest and the `stale_read_path`
  divergence is stated in the tool description.
- `vice_recycle`'s reason gate is part of the advertised contract on stock.
- The manifest guards are satisfied by a new named category, not by loosening.
</success_criteria>

<output>
Create `.planning/phases/07-cycle-timing-and-wedge-triage/07-09-SUMMARY.md` when done
</output>
