---
phase: 07-cycle-timing-and-wedge-triage
plan: 08
type: execute
wave: 5
depends_on: ["07-03", "07-05"]
files_modified:
  - .claude/mcp/vice/stock-dispatch.ts
  - .claude/mcp/vice/stock-derived.ts
  - .claude/mcp/vice/tools-manifest.stock.json
  - .claude/mcp/vice/package.json
  - .claude/mcp/vice/stock-dispatch.test.ts
autonomous: true
requirements: [TIME-01, TIME-02, TIME-03]

must_haves:
  truths:
    - "Calling vice_cycles_stopwatch or vice_run_until on the stock backend reaches the new handlers instead of dispatchStock()'s refuse-by-name branch."
    - "Both tools are advertised on stock with argument shapes identical to the fork's, so an existing skill call site keeps working unchanged."
    - "The stock-only timeout_ms extra is optional (D-02), satisfying the rule that a stock extra property is never in stock's required set."
    - "Both new modules ship in the published tarball — a tool registered but not packaged fails at module load in the published package."
  artifacts:
    - path: ".claude/mcp/vice/tools-manifest.stock.json"
      provides: "36 tools — the 34 existing plus vice_cycles_stopwatch and vice_run_until, each with inputSchema and outputSchema"
    - path: ".claude/mcp/vice/stock-dispatch.ts"
      provides: "two new withDerivedTool table entries"
      contains: "vice_cycles_stopwatch"
    - path: ".claude/mcp/vice/package.json"
      provides: "files[] gains stock-timing.ts and stock-run-until.ts"
  key_links:
    - from: ".claude/mcp/vice/stock-dispatch.ts"
      to: ".claude/mcp/vice/stock-derived.ts"
      via: "withDerivedTool refuses any name not in STOCK_DERIVED_TOOLS"
      pattern: "STOCK_DERIVED_TOOLS"
    - from: ".claude/mcp/vice/tools-manifest.stock.json"
      to: "stock-dispatch.test.ts conformance cases"
      via: "the completeness guard requires one conformance case per manifest entry"
      pattern: "CONFORMANCE_TOOL_NAMES"
---

<objective>
Register `vice_cycles_stopwatch` (TIME-01/TIME-03) and `vice_run_until` (TIME-02) on the
stock backend: dispatch table, derived-tool registry, manifest, published `files[]`, and
the conformance cases the manifest's own completeness guard requires.

**A correction this plan carries.** `07-RESEARCH.md` states the stock manifest goes
34 -> 37. Reading the code shows otherwise: `vice_diagnose` and `vice_recycle` are
proxy-local synthetic tools with no manifest entry on either backend today, so this plan
takes the manifest 34 -> **36**, and plan 07-09 takes it 36 -> 38 when it registers those
two (which the bidirectional table/manifest agreement test in `stock-dispatch.test.ts`
requires of any dispatch entry). Record the correction in this plan's SUMMARY.

Purpose: without registration the two handlers built in plans 07-03 and 07-05 are
unreachable — `dispatchStock()` refuses both by name today.
Output: two tools advertised and dispatchable on stock, conformance-validated against
their own declared output schemas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cycle-timing-and-wedge-triage/07-PATTERNS.md
@.planning/phases/07-cycle-timing-and-wedge-triage/07-03-SUMMARY.md
@.planning/phases/07-cycle-timing-and-wedge-triage/07-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch table, STOCK_DERIVED_TOOLS and package files[]</name>
  <files>.claude/mcp/vice/stock-dispatch.ts, .claude/mcp/vice/stock-derived.ts, .claude/mcp/vice/package.json</files>
  <read_first>
    - `.claude/mcp/vice/stock-dispatch.ts` — the family-module import block near the top
      and the `STOCK_DISPATCH_TABLE` entries for Phase 5's derived tools (the
      `vice_sprite_get` / `vice_sprite_inspect` lines are the exact shape to copy),
      plus `withDerivedTool()`'s refuse-if-undeclared branch.
    - `.claude/mcp/vice/stock-derived.ts` — `STOCK_DERIVED_TOOLS` and the comment above
      it, plus its warning that `package.json`'s `files[]` must gain a new module in the
      same commit as its registration.
    - `.claude/mcp/vice/package.json` — the `files[]` array and where the existing
      `stock-*.ts` entries sit.
    - `.claude/mcp/vice/stock-timing.ts` and `.claude/mcp/vice/stock-run-until.ts` — the
      exported handler names to wire (`handleCyclesStopwatch`, `handleRunUntil`).
  </read_first>
  <action>
Add the two modules to `stock-dispatch.ts`'s family-module import block, and two entries
to `STOCK_DISPATCH_TABLE` in the existing style, each under a requirement-id comment:

- under `// derived (TIME-01)`:
  `vice_cycles_stopwatch: withDerivedTool("vice_cycles_stopwatch", { needsSession: true }, handleCyclesStopwatch)`
- under `// derived (TIME-02)`:
  `vice_run_until: withDerivedTool("vice_run_until", { needsSession: true }, handleRunUntil)`

Add both names to `STOCK_DERIVED_TOOLS` in `stock-derived.ts` with the same
trailing-comment convention the existing entries use (`// Phase 7, TIME-01` and
`// Phase 7, TIME-02`). `withDerivedTool()` refuses at call time any name absent from
that set, so a missing entry here is a runtime refusal rather than a type error.

Add `"stock-timing.ts"` and `"stock-run-until.ts"` to `package.json`'s `files[]`, beside
the other `stock-*.ts` entries, in the same commit — a registered-but-unpackaged module
fails at module load in the published tarball, which is what `stock-derived.ts`'s own
warning names.
  </action>
  <acceptance_criteria>
    - `grep -c vice_cycles_stopwatch .claude/mcp/vice/stock-dispatch.ts` returns 1, and
      the same for `vice_run_until`.
    - `grep -c vice_cycles_stopwatch .claude/mcp/vice/stock-derived.ts` returns 1, and
      the same for `vice_run_until`.
    - Reading `package.json` shows `files` containing both `stock-timing.ts` and
      `stock-run-until.ts`.
    - `node scripts/check-npm-packages.mjs` from the repo root passes: the tarball
      contains both new production modules and leaks no test files.
    - `npx tsc --noEmit -p .claude/mcp/vice/tsconfig.json` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/mcp/vice && npx tsc --noEmit -p tsconfig.json && node -e "const p=require('./package.json'); if(!(p.files.includes('stock-timing.ts')&&p.files.includes('stock-run-until.ts'))) process.exit(1)" && node ../../../scripts/check-npm-packages.mjs</automated>
  </verify>
  <done>Both tools dispatch to their handlers, are declared derived, and ship in the published file list.</done>
</task>

<task type="auto">
  <name>Task 2: Manifest entries (34 -> 36) and the registered-name guards</name>
  <files>.claude/mcp/vice/tools-manifest.stock.json, .claude/mcp/vice/stock-dispatch.test.ts</files>
  <read_first>
    - `.claude/mcp/vice/tools-manifest.stock.json` — an existing entry with both an
      `inputSchema` and an `outputSchema` (`vice_execution_step` is a good model: note
      the `required` array on the output schema and the `enum`-pinned `runState`).
    - `.claude/mcp/vice/tools-manifest.json` — the fork's `vice_cycles_stopwatch` entry
      (`action` required, enum `reset`/`read`/`reset_and_read`) and `vice_run_until`
      entry (`address` typed `string`, `cycles` typed `number` described as "not yet
      implemented", no `required` array).
    - `.claude/mcp/vice/stock-dispatch.test.ts` — the manifest-contract tests that will
      now cover these entries: the D-03 name-coverage test and `STOCK_ONLY_TOOLS`, the
      D-03 input-compatibility test (equal required-argument SETS, stock extras must be
      optional, shared properties must have equal `type`), the bidirectional
      table/manifest agreement test, the outputSchema-presence test,
      `REGISTERED_TOOL_NAMES` and the "the table's key count is exactly 34" test, and
      `TRIMMED_TOOL_DECISIONS`.
    - `.claude/mcp/vice/stock-timing.ts` and `.claude/mcp/vice/stock-run-until.ts` — the
      exact payload keys each handler emits, so the output schemas describe what is
      actually returned rather than what was imagined.
  </read_first>
  <action>
Add two entries to `tools-manifest.stock.json`, taking it from 34 to 36 tools.

**`vice_cycles_stopwatch`** — `inputSchema` identical to the fork's: `action` typed
`string` with enum `["reset", "read", "reset_and_read"]`, and `required: ["action"]`.
Neither backend takes any other argument, so no stock extra is introduced here. The
description must state the stock behaviour honestly in one sentence: on VICE >= 3.10 the
figure is exact; below 3.10 it is a within-one-frame reconstruction, and a bracket that
crossed a frame boundary is refused rather than approximated.

`outputSchema` (type `object`) with `required` limited to the keys **every** answer
carries: `requested`, `action`, `route`, `measurable`, `runState`. Declare the
conditional keys as optional properties: `cycles` (number), `cyclesExact` (string),
`exactness` (string), `caveat` (string), `standard` (string), `standardAssumed`
(boolean), `reason` (string). `cycles` being optional rather than required is the schema
expressing TIME-03 directly: an unmeasurable bracket omits it rather than reporting
zero. `route` carries an `enum` of `["cpu_history", "frame_position", "unavailable"]`
and `runState` the usual `["running", "stopped", "unknown"]`.

**`vice_run_until`** — `inputSchema` declaring the fork's two properties with the fork's
own types (`address` string, `cycles` number, with `cycles`'s description preserving the
"not yet implemented" wording on both backends) plus the stock-only optional
`timeout_ms` (number) documenting the `30000` default, the accepted range, and that it
is stock-only. No `required` array, matching the fork exactly — the compatibility test
compares required SETS, and a stock extra must never be required.

`outputSchema` with `required` limited to `requested`, `reached`, `address`,
`timeoutMs`, `runState`, and optional `checkpointId`, `hitCount`, `timedOut`, `cleanup`,
`timeoutClamped`, `explanation`.

Then update `stock-dispatch.test.ts`'s registered-name guards: add both names to
`REGISTERED_TOOL_NAMES`, and change the count assertions from `34` to `36` in both
places they appear (the `.length` check and the resolved-handler count). Do **not**
loosen any manifest-contract test to accommodate the new entries — if the
input-compatibility test fails, the manifest entry is wrong, not the test.

While in `TRIMMED_TOOL_DECISIONS`, correct `vice_joystick_tap`'s stale reason string:
it currently reads "needs a resume plus Phase 7's timing route", which stops being true
when this phase ships. Replace it with the actual disposition — cut from scope because no
skill calls it and no requirement names it. The tool stays absent from the manifest;
only the recorded reason changes.
  </action>
  <acceptance_criteria>
    - `tools-manifest.stock.json` parses and its `tools` array has length exactly 36.
    - Both new entries carry an `outputSchema` whose `type` is `"object"`.
    - `vice_cycles_stopwatch`'s stock `inputSchema.required` deep-equals the fork's
      (`["action"]`) and its `action` enum deep-equals the fork's three values.
    - `vice_run_until`'s stock `inputSchema` declares `address`, `cycles` and
      `timeout_ms`; `timeout_ms` does not appear in any `required` array; `address` and
      `cycles` have the same `type` values as the fork's entry.
    - `cycles` is NOT in `vice_cycles_stopwatch`'s `outputSchema.required`.
    - `REGISTERED_TOOL_NAMES` has length 36 and both count assertions read 36.
    - `TRIMMED_TOOL_DECISIONS`'s `vice_joystick_tap` reason no longer contains the
      string `Phase 7`.
    - `node --test stock-dispatch.test.ts` passes — including the D-03 name-coverage,
      input-compatibility, bidirectional-agreement and outputSchema-presence tests.
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/mcp/vice && node -e "const m=require('./tools-manifest.stock.json'); if(m.tools.length!==36) process.exit(1)" && node --test stock-dispatch.test.ts</automated>
  </verify>
  <done>Both tools are advertised on stock with fork-compatible inputs, honest output schemas that make the cycles field optional, and every manifest-contract guard still green.</done>
</task>

<task type="auto">
  <name>Task 3: Two conformance cases</name>
  <files>.claude/mcp/vice/stock-dispatch.test.ts</files>
  <read_first>
    - `.claude/mcp/vice/stock-dispatch.test.ts` — the D-02 answer-conformance harness:
      `buildConformanceSession()`, `buildConformanceDeps()`, `conformanceTest()`,
      `assertAnswerConforms()`, `CONFORMANCE_TOOL_NAMES`, the completeness guard tying
      the case list to the manifest's own names, and the negative control proving the
      checker is not vacuous. Read an existing case (for example `vice_ping`'s) for the
      shape.
    - `.claude/mcp/vice/stock-timing.ts` and `.claude/mcp/vice/stock-run-until.ts` — the
      wire calls each handler makes, so the `sendImpl` stub answers them.
  </read_first>
  <action>
Add one `conformanceTest()` case per new tool, dispatched through the real
`dispatchStock()` path (never by calling the handler directly), each validating its
answer against its own declared `outputSchema`.

**`vice_cycles_stopwatch`** — dispatch with `{ action: "reset" }`. `reset` is the action
that produces an ok answer with no prior state, so the case needs no baseline setup.
The `sendImpl` stub answers `REGISTERS_AVAILABLE`/`REGISTERS_GET` (or `CPUHISTORY_GET`,
depending on the capabilities the conformance session declares — pick one and state it
in a comment) with a well-formed synthetic reply.

**`vice_run_until`** — dispatch with an `address` and an explicit small `timeout_ms`
(for example `25`). The conformance harness's client emits no checkpoint hit, so this
exercises the timeout answer shape; the stub must answer `CHECKPOINT_SET` with a
synthetic `checkpoint_info` reply, accept the `EXIT`, and answer `CHECKPOINT_DELETE`.
Comment the case explaining that a small `timeout_ms` is what keeps the suite fast and
that the timeout answer is a legitimate conformance subject — it is the shape a caller
sees on an unreachable address.

Add both names to `CONFORMANCE_TOOL_NAMES`. The completeness guard requires exactly one
case per manifest entry and vice versa, so a missing case fails as a named assertion.
  </action>
  <acceptance_criteria>
    - `CONFORMANCE_TOOL_NAMES` contains both names and the completeness guard passes
      against the 36-entry manifest.
    - Both cases call `dispatchStock(...)` and `assertAnswerConforms(...)`; neither
      calls `handleCyclesStopwatch` or `handleRunUntil` directly.
    - The `vice_run_until` case completes in well under one second (it must not wait on
      the 30000 ms default).
    - `node --test stock-dispatch.test.ts` passes with 0 failures.
    - `npm run test:automated` from `.claude/mcp/vice` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd .claude/mcp/vice && node --test stock-dispatch.test.ts && npm run test:automated</automated>
  </verify>
  <done>Both new tools' real dispatch answers are validated against their own declared output schemas, and the manifest-to-conformance completeness guard is satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client (Claude) -> advertised tool surface | `tools/list` is what a skill or agent believes it may call; an advertised tool with no handler, or a handler with no advertisement, is a lie about capability |
| published npm tarball -> installing user | `files[]` decides what actually ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-17 | Spoofing (advertised capability that does not exist) | manifest / dispatch-table agreement | mitigate | The bidirectional agreement test requires a handler for every manifest entry and a manifest entry for every table key; the conformance completeness guard requires one validated case per entry |
| T-07-01 | Information Disclosure (schema that permits a dishonest answer) | `vice_cycles_stopwatch` outputSchema | mitigate | `cycles` is deliberately **not** in `required`, so the schema itself expresses that an unmeasurable bracket omits the figure rather than reporting zero |
| T-07-18 | Denial of Service (shipped package fails at module load) | `package.json` files[] | mitigate | Both new modules are added to `files[]` in the same commit as their registration, verified by `scripts/check-npm-packages.mjs` |
| T-07-02 | Denial of Service (slow test suite masking regressions) | conformance case for `vice_run_until` | mitigate | The case passes an explicit small `timeout_ms` so the suite never waits on the production default |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs no packages; it only edits the published file list |
</threat_model>

<verification>
- `cd .claude/mcp/vice && npx tsc --noEmit -p tsconfig.json` exits 0.
- `cd .claude/mcp/vice && node --test stock-dispatch.test.ts` passes.
- `cd .claude/mcp/vice && npm run test:automated` passes.
- `node scripts/check-npm-packages.mjs` from the repo root passes.
- `tools-manifest.stock.json` has exactly 36 tools.
</verification>

<success_criteria>
- `vice_cycles_stopwatch` and `vice_run_until` dispatch to their handlers on stock.
- Both are advertised with fork-compatible argument shapes and an optional stock-only
  `timeout_ms`.
- Both answers are conformance-validated against their own output schemas.
- Both modules ship in the published tarball.
</success_criteria>

<output>
Create `.planning/phases/07-cycle-timing-and-wedge-triage/07-08-SUMMARY.md` when done
</output>
