---
phase: 07-cycle-timing-and-wedge-triage
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .planning/PROJECT.md
  - docs/phase0-binmon-findings.md
autonomous: true
requirements: [TIME-01, TIME-04]

must_haves:
  truths:
    - "The interception constraint a future contributor reads cites the line where rewriteArguments() actually is, not a line it drifted away from."
    - "The frame-counter stopwatch fallback is marked superseded at the place someone would read it, so it is not implemented by a later reader in good faith."
  artifacts:
    - path: "CLAUDE.md"
      provides: "the corrected derived-tool interception constraint"
      contains: "vice-proxy.ts:2889"
    - path: "docs/phase0-binmon-findings.md"
      provides: "§1's frame-counter fallback marked SUPERSEDED with the arithmetic that rejects it"
      contains: "SUPERSEDED"
  key_links:
    - from: "CLAUDE.md"
      to: ".planning/PROJECT.md"
      via: "the same Constraints bullet, kept in sync"
      pattern: "vice-proxy\\.ts:2889"
---

<objective>
Correct two documents that currently point later work at the wrong thing: CLAUDE.md's
(and PROJECT.md's) interception constraint cites `vice-proxy.ts:2773` for
`rewriteArguments()`, which has drifted to **2889**; and
`docs/phase0-binmon-findings.md` §1 recommends a frame-counter stopwatch fallback that
this project's own D-11 trace-hazard guard auto-disables within half a second.

Purpose: both are load-bearing prose. The first is the constraint every future derived
tool is checked against; the second is a recommendation someone will implement in good
faith unless it is marked superseded at the point of reading. Neither is a code change,
so this plan runs in wave 1 alongside the code work rather than waiting behind it.
Output: three corrected documents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cycle-timing-and-wedge-triage/07-RESEARCH.md
@.planning/phases/07-cycle-timing-and-wedge-triage/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the rewriteArguments() line citations in CLAUDE.md and PROJECT.md</name>
  <files>CLAUDE.md, .planning/PROJECT.md</files>
  <read_first>
    - `CLAUDE.md` — the Constraints section, specifically the `**Architecture**` bullet
      beginning "The transport swap happens behind `vice.ts`'s `call()` seam".
    - `.planning/PROJECT.md` — the identical bullet (the two files carry the same text;
      PROJECT.md is the source the CLAUDE.md section is generated from, so both must
      change or the next regeneration reverts the fix).
    - `.claude/mcp/vice/vice-proxy.ts` — confirm the current call sites before editing:
      `rewriteArguments(args, name)` inside `forwardToVice()`, and
      `rewriteArguments({ path: screenshotContainerPath }, "vice_display_screenshot")`
      inside `gatherWedgeEvidence()`.
    - `.planning/phases/07-cycle-timing-and-wedge-triage/07-PATTERNS.md` — the section
      that located both sites and recorded the drift.
  </read_first>
  <action>
In both `CLAUDE.md` and `.planning/PROJECT.md`, change `vice-proxy.ts:2773` to
`vice-proxy.ts:2889` in the Architecture constraint bullet. Give the second site its own
line number too: the bullet currently ends "Second site with the same cause:
`gatherWedgeEvidence()` calls `rewriteArguments()` itself" — extend that to cite
`vice-proxy.ts:1368`.

Add a short parenthetical stating that line numbers in this bullet are checked against
the source at each phase and drift between phases, so a future reader treats a mismatch
as drift to re-verify rather than as evidence the constraint changed. Keep the two files
byte-identical for this bullet; a divergence between them is how the number drifted in
the first place.

Do not restate or reword the constraint itself — the rule ("derived tools must be
intercepted before `forwardToVice()`, not behind `call()`") is correct and hard-won.
Only the citations change.
  </action>
  <acceptance_criteria>
    - `grep -rn "vice-proxy.ts:2773" CLAUDE.md .planning/PROJECT.md` returns no matches.
    - `grep -c "vice-proxy.ts:2889" CLAUDE.md` returns 1 and the same for
      `.planning/PROJECT.md`.
    - `grep -c "vice-proxy.ts:1368" CLAUDE.md` returns 1 and the same for
      `.planning/PROJECT.md`.
    - The Architecture bullet is textually identical in the two files (verify with a
      diff of the extracted bullet lines; they must match exactly).
    - The phrase "intercepted before `forwardToVice()`" still appears in both files —
      the constraint itself was not reworded away.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(grep -rn 'vice-proxy.ts:2773' CLAUDE.md .planning/PROJECT.md)" && grep -q 'vice-proxy.ts:2889' CLAUDE.md && grep -q 'vice-proxy.ts:2889' .planning/PROJECT.md && grep -q 'vice-proxy.ts:1368' CLAUDE.md && grep -q 'vice-proxy.ts:1368' .planning/PROJECT.md</automated>
  </verify>
  <done>Both documents cite the real current call sites for rewriteArguments(), and the constraint text itself is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Mark the frame-counter stopwatch fallback superseded in phase0-binmon-findings.md §1</name>
  <files>docs/phase0-binmon-findings.md</files>
  <read_first>
    - `docs/phase0-binmon-findings.md` — §1 in full ("Cycle stopwatch — RESOLVED"),
      including the bullet proposing `cycles = frames * 19656 (PAL) + Δ(LIN * 63 + CYC)`
      with the frame count from a non-stopping exec checkpoint at `$EA31`, and the
      following bullet calling that reconstruction "a second, always-available
      stopwatch".
    - `.claude/mcp/vice/stock-checkpoints.ts` — the D-11 trace guard and its exact
      `TRACE_HITS_PER_SECOND_LIMIT` constant and auto-disable behaviour
      (`CHECKPOINT_TOGGLE` with `enabled:false`).
    - `.planning/phases/07-cycle-timing-and-wedge-triage/07-RESEARCH.md` — Pitfall 1,
      which carries the arithmetic and the conclusion.
  </read_first>
  <action>
Insert a clearly marked `**SUPERSEDED (Phase 7, 2026-08-18)**` note directly beneath the
frame-counter bullet in §1 — at the point of reading, not in a footer someone scrolls
past. State the rejection concretely and with its numbers:

- A frame-boundary exec checkpoint fires ~50.1 times/second on PAL and ~59.8 on NTSC.
- This client's own D-11 trace-hazard guard caps a non-stopping checkpoint at
  `TRACE_HITS_PER_SECOND_LIMIT = 20` hits/second (`stock-checkpoints.ts`) and sends its
  own `CHECKPOINT_TOGGLE` with `enabled:false` when that is exceeded — so the checkpoint
  auto-disables roughly 0.4-0.5 real seconds into any bracket.
- The guard is a safety mechanism this project deliberately built; a stopwatch that
  fights it is a stopwatch built against its own codebase.
- Name the two routes that replaced it: **Route A**, `CPUHISTORY_GET`'s per-entry
  monotonic uint64 cycle field on VICE >= 3.10; **Route B** below 3.10, `LIN`/`CYC`
  frame-position reconstruction that reports an exact delta only when no wraparound is
  detected and **refuses explicitly** — never returning zero and never guessing a
  `+ k * cyclesPerFrame` correction — when a frame boundary is proven crossed.

Also correct the following bullet's claim that the reconstruction is "a **second,
always-available** stopwatch" — with the guard in place it is not always available, and
Phase 7 did not "pick between the two routes with measured socket cost", it rejected one
outright. Leave the rest of §1 (the `e_Cycle`/`e_Rasterline` finding, the
`REGISTERS_GET` `LIN`/`CYC` availability, the `CPUHISTORY_GET` version gate, the
`RL`/`CY` condition-token finding) untouched — all of it is still correct and still
cited elsewhere.
  </action>
  <acceptance_criteria>
    - `grep -c "SUPERSEDED" docs/phase0-binmon-findings.md` returns at least 1, and the
      match sits inside §1 (line number greater than the `## 1.` heading and less than
      the `## 2.` heading).
    - The superseded note contains the literal strings `TRACE_HITS_PER_SECOND_LIMIT`
      and `20`, and both `Route A` and `Route B`.
    - `grep -c "second, always-available" docs/phase0-binmon-findings.md` returns 0 —
      the superseded claim is corrected, not merely annotated.
    - `grep -c "RL" docs/phase0-binmon-findings.md` still returns a non-zero count and
      the `CPUHISTORY_GET` version-gate bullet is still present, proving the rest of §1
      was not collaterally edited.
  </acceptance_criteria>
  <verify>
    <automated>awk '/^## 1\./{a=NR} /^## 2\./{b=NR} /SUPERSEDED/{s=NR} END{exit !(s>a && s<b)}' docs/phase0-binmon-findings.md && grep -q 'TRACE_HITS_PER_SECOND_LIMIT' docs/phase0-binmon-findings.md && test "$(grep -c 'second, always-available' docs/phase0-binmon-findings.md)" = "0"</automated>
  </verify>
  <done>§1's frame-counter fallback carries a superseded note with the arithmetic that rejects it and names the two routes that replaced it; the rest of §1 is intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> future implementer | Prose in CLAUDE.md and docs/ is what a later contributor (human or agent) acts on without re-deriving |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Tampering (via misleading guidance) | `docs/phase0-binmon-findings.md` §1 | mitigate | The rejected frame-counter fallback would arm a `stop:false` checkpoint at 50-60 Hz, which the D-11 guard disables mid-bracket — a silent wrong measurement. Marked SUPERSEDED at the point of reading with the arithmetic, and the "always-available" claim removed rather than annotated |
| T-07-12 | Repudiation (unverifiable citation) | `CLAUDE.md` / `PROJECT.md` interception constraint | mitigate | The cited line is corrected to the verified current call site and a second site is cited; a note records that these numbers are re-checked per phase, so a future mismatch reads as drift rather than as the constraint having changed |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs no packages |
</threat_model>

<verification>
- `grep -rn "vice-proxy.ts:2773" CLAUDE.md .planning/PROJECT.md` returns nothing.
- The Architecture constraint bullet is byte-identical between `CLAUDE.md` and
  `.planning/PROJECT.md`.
- `docs/phase0-binmon-findings.md` §1 contains a SUPERSEDED note naming
  `TRACE_HITS_PER_SECOND_LIMIT`, Route A and Route B.
- `cd .claude/mcp/vice && npm run test:automated` still passes (no code touched, but the
  gate confirms nothing incidental broke).
</verification>

<success_criteria>
- No document in the repo points a future contributor at `vice-proxy.ts:2773`.
- The second `rewriteArguments()` site is cited by line.
- The frame-counter fallback is marked superseded with the numbers that reject it, and
  the two replacement routes are named.
</success_criteria>

<output>
Create `.planning/phases/07-cycle-timing-and-wedge-triage/07-04-SUMMARY.md` when done
</output>
