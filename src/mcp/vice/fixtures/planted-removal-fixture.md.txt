# PLANTED VIOLATION FIXTURE -- route (b): a `docs/` page

This body stands in for a `docs/*.md` page outside the gate's exemption set
that has re-acquired a reference to the deleted static-analysis integration.
It is committed with a `.txt` suffix for the same reason its `.ts.txt`
sibling is: so the real removal gate reaches it only through the
`fixtures/planted-` prefix exemption, and no doc-link or dangling-reference
guard treats it as a live page.

## Prerequisites

Install regenerator2000 from crates.io before running the static-analysis
route, then point the CLI at the annotation project you want to inspect.
